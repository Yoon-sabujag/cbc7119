---
phase: 02-stabilization-code-quality
plan: 04
type: execute
wave: 3
depends_on: [02-01, 02-02]
files_modified:
  - cha-bio-safety/functions/api/
  - cha-bio-safety/src/pages/
  - cha-bio-safety/src/utils/
autonomous: true
requirements: [STAB-01, STAB-02, STAB-03, STAB-04, STAB-05, STAB-06, STAB-07]

must_haves:
  truths:
    - "02-01-BUG-REPORT.md와 02-02-BUG-REPORT.md의 모든 critical 버그가 수정된다"
    - "02-01-BUG-REPORT.md와 02-02-BUG-REPORT.md의 major 버그가 수정된다"
    - "minor 버그 중 코드 1-5줄 수정으로 해결되는 항목이 수정된다"
    - "npm run build가 버그 수정 후 성공한다"
  artifacts:
    - path: "cha-bio-safety/dist/"
      provides: "버그 수정이 적용된 프로덕션 빌드"
  key_links:
    - from: "cha-bio-safety/src/ 및 functions/"
      to: "https://cbc7119.pages.dev (02-05-PLAN.md 최종 검증)"
      via: "npm run deploy"
      pattern: "배포 후 최종 smoke test"
---

<objective>
02-01-BUG-REPORT.md와 02-02-BUG-REPORT.md에서 식별된 버그를 심각도 순으로 일괄 수정하고, 수정 후 빌드가 성공하는지 검증한다. (D-03: 테스트 완료 후 일괄 수정)

Purpose: 전수 테스트에서 발견된 모든 critical/major 버그와 간단한 minor 버그를 이 플랜에서 일괄 처리한다. 복잡한 아키텍처 변경이 필요한 minor 버그는 다음 Phase로 이관한다.
Output: 버그 수정이 반영된 소스 코드 + 성공하는 빌드
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-stabilization-code-quality/02-01-SUMMARY.md
@.planning/phases/02-stabilization-code-quality/02-02-SUMMARY.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/CONCERNS.md

<!-- Bug reports will be read at execution time -->
@.planning/phases/02-stabilization-code-quality/02-01-BUG-REPORT.md
@.planning/phases/02-stabilization-code-quality/02-02-BUG-REPORT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Critical 버그 수정</name>
  <files>
    (02-01-BUG-REPORT.md, 02-02-BUG-REPORT.md의 Critical 섹션 내용에 따라 결정)
  </files>
  <read_first>
    - .planning/phases/02-stabilization-code-quality/02-01-BUG-REPORT.md (Critical 섹션)
    - .planning/phases/02-stabilization-code-quality/02-02-BUG-REPORT.md (Critical 섹션)
    - .planning/codebase/CONVENTIONS.md (코딩 컨벤션 준수)
    - 버그가 지목하는 소스 파일 (보고서의 "관련 파일 추정" 열 참조)
  </read_first>
  <action>
    02-01-BUG-REPORT.md와 02-02-BUG-REPORT.md의 **Critical 버그** (C-xx 항목) 전체를 수정한다.

    **수정 원칙:**
    1. 버그 보고서의 "재현 방법"을 읽고 root cause를 파악한다
    2. CONVENTIONS.md 코딩 패턴을 준수하여 수정한다
    3. 가장 최소한의 변경으로 수정한다 (과도한 리팩토링 금지)
    4. 각 버그 수정 후 해당 파일을 git commit한다

    **수정 후 각 버그별 검증:**
    - API 엔드포인트 버그: `npm run build`로 TypeScript 컴파일 확인
    - UI 버그: 컴파일 성공 확인 후 02-05에서 프로덕션 재테스트

    **Critical 버그가 없는 경우:**
    "버그 없음 확인됨" 메시지를 출력하고 Task 2로 진행.

    **예상 Critical 버그 유형 및 대응:**
    CONCERNS.md 기준:
    - API 엔드포인트 try-catch 누락으로 저장 실패 → 해당 handler에 try-catch 추가, `{ success: false, error: '서버 오류' }` 반환
    - D1 쿼리 바인딩 오류 → 쿼리 파라미터 수정
    - 저장 후 화면이 갱신되지 않는 경우 → TanStack Query invalidateQueries 또는 refetch 추가

    **수정 금지 항목 (scope 외):**
    - CORS 정책 변경 (CONCERNS.md Security 항목)
    - 비밀번호 해싱 방식 변경
    - 폴링 interval 변경
    - Web Worker 이전
    위 항목들은 CONCERNS.md에 기록된 기술 부채이나 이번 Phase 범위 외임.
  </action>
  <verify>
    ```bash
    cd /Users/jykevin/Documents/20260328/cha-bio-safety
    npm run build 2>&1 | tail -20
    # 기대: TypeScript 에러 없음, Rollup 빌드 성공
    ```
  </verify>
  <acceptance_criteria>
    - 02-01-BUG-REPORT.md Critical 섹션의 모든 버그가 수정됨
    - 02-02-BUG-REPORT.md Critical 섹션의 모든 버그가 수정됨
    - `npm run build` 성공 (exit code 0)
    - 수정된 각 파일이 git commit됨
  </acceptance_criteria>
  <done>Critical 버그 전체 수정 완료 또는 "Critical 버그 없음" 확인, 빌드 성공</done>
</task>

<task type="auto">
  <name>Task 2: Major 및 Minor 버그 수정</name>
  <files>
    (02-01-BUG-REPORT.md, 02-02-BUG-REPORT.md의 Major/Minor 섹션 내용에 따라 결정)
  </files>
  <read_first>
    - .planning/phases/02-stabilization-code-quality/02-01-BUG-REPORT.md (Major/Minor 섹션)
    - .planning/phases/02-stabilization-code-quality/02-02-BUG-REPORT.md (Major/Minor 섹션)
    - .planning/codebase/CONVENTIONS.md
    - 각 버그의 관련 소스 파일
  </read_first>
  <action>
    **Major 버그 (M-xx 항목) 전체 수정:**

    같은 원칙 적용 (최소 변경, 컨벤션 준수).
    Major 버그 예상 유형:
    - 특정 카테고리에서 점검 플로우 중단 → 해당 카테고리 데이터 또는 UI 로직 수정
    - 엑셀 파일 열렸으나 특정 셀 데이터 누락 → generateExcel.ts 해당 함수 셀 패치 로직 수정
    - 캘린더 특정 날짜 일정 표시 오류 → 날짜 쿼리 파라미터 형식 수정

    **Minor 버그 (m-xx 항목) — 선별 수정:**

    다음 기준으로 수정 여부 결정:
    - 수정 = 1-10줄 변경으로 완료 가능 → 즉시 수정
    - 수정 = UI 레이아웃 대규모 변경 필요 → 이번 Phase 스킵, SUMMARY에 "다음 Phase 이관" 기록
    - 수정 = 아키텍처 변경 필요 → 이번 Phase 스킵

    Minor 버그 예상 유형:
    - UI 텍스트 오타 → 즉시 수정
    - 빈 상태 메시지 누락 → 단순 JSX 추가
    - 날짜 포맷 불일치 → toLocaleDateString 파라미터 조정
    - toast 메시지 문구 수정 → 즉시 수정

    **수정 후 빌드 검증:**
    ```bash
    cd /Users/jykevin/Documents/20260328/cha-bio-safety
    npm run build 2>&1 | tail -20
    ```

    **스킵한 버그 기록:**
    수정하지 않은 minor 버그는 02-04-SUMMARY.md의 "이관 항목" 섹션에 기록.
  </action>
  <verify>
    ```bash
    # 빌드 성공 확인
    cd /Users/jykevin/Documents/20260328/cha-bio-safety
    npm run build 2>&1 | grep -E "error TS|built in|Error"
    # 기대: "built in" 포함, "error TS" 없음

    # dist/index.html 존재 확인
    ls /Users/jykevin/Documents/20260328/cha-bio-safety/dist/index.html
    ```
  </verify>
  <acceptance_criteria>
    - 모든 Major 버그 수정 완료
    - 수정 가능한 Minor 버그(1-10줄) 수정 완료
    - 스킵한 Minor 버그는 이관 이유와 함께 SUMMARY에 기록됨
    - `npm run build` 성공 (exit code 0)
    - 모든 수정 파일이 git commit됨
  </acceptance_criteria>
  <done>Major 버그 전체 수정, Minor 버그 선별 수정, 빌드 성공</done>
</task>

<task type="auto">
  <name>Task 3: 버그 수정 후 프로덕션 배포</name>
  <files>
    (배포 전용 — 소스 파일 변경 없음)
  </files>
  <read_first>
    - .planning/phases/01-deployment-infrastructure/01-01-SUMMARY.md (빌드/배포 패턴)
    - cha-bio-safety/package.json (deploy 스크립트 확인)
  </read_first>
  <action>
    버그 수정 및 의존성 정리(02-03)가 반영된 코드를 프로덕션에 배포한다.

    **전제 조건 확인:**
    - 02-03-PLAN.md(의존성 정리)가 완료되었는지 확인
    - 02-03이 완료되지 않았다면: 02-03 완료 후 이 Task를 실행

    **배포 실행:**
    ```bash
    cd /Users/jykevin/Documents/20260328/cha-bio-safety

    # 최종 빌드 + Cloudflare Pages 배포
    npm run deploy 2>&1
    ```

    `npm run deploy` = `npm run build && npx wrangler pages deploy dist`

    **배포 성공 기준:**
    - 출력에 "Deployment complete" 또는 배포 URL 표시
    - https://cbc7119.pages.dev 에 새 버전이 반영됨 (배포 후 1-2분 소요)

    **배포 실패 시:**
    - 인증 오류: `npx wrangler login` 또는 Cloudflare API 토큰 재확인
    - 빌드 오류: 빌드 에러 메시지 분석 후 수정

    **참고:** Wrangler 인증이 만료된 경우 checkpoint:human-action이 필요할 수 있음.
    인증 오류가 발생하면 즉시 사용자에게 `npx wrangler login` 실행 요청.
  </action>
  <verify>
    ```bash
    # 배포 성공 확인 — 프로덕션 health endpoint 응답
    curl -s https://cbc7119.pages.dev/api/health 2>/dev/null | head -c 200
    # 기대: {"success":true,...} 형태의 JSON 응답
    ```
  </verify>
  <acceptance_criteria>
    - `npm run deploy` 성공 (Cloudflare Pages 배포 완료)
    - https://cbc7119.pages.dev/api/health 가 200 응답을 반환함
    - 배포된 버전에 버그 수정 + 의존성 정리가 포함됨
  </acceptance_criteria>
  <done>프로덕션 배포 완료, /api/health 200 응답 확인</done>
</task>

</tasks>

<verification>
```bash
# 빌드 성공 확인
ls /Users/jykevin/Documents/20260328/cha-bio-safety/dist/index.html

# health API 응답 확인 (배포 후)
curl -s https://cbc7119.pages.dev/api/health | head -c 100
```
</verification>

<success_criteria>
- Critical 버그 전체 수정 완료
- Major 버그 전체 수정 완료
- Minor 버그 선별 수정 완료 (복잡한 항목은 이관 사유 기록)
- `npm run build` 성공
- `npm run deploy` 성공
- https://cbc7119.pages.dev/api/health 200 응답
</success_criteria>

<output>
After completion, create `.planning/phases/02-stabilization-code-quality/02-04-SUMMARY.md`
Include "이관 항목" section listing any minor bugs deferred to next phase.
</output>
