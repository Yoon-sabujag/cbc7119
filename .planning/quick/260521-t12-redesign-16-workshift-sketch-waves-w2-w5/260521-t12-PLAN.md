---
phase: quick-260521-t12
plan: 01
type: execute
wave: 1
depends_on: []
quick_id: 260521-t12
branch: redesign/16-workshift
date: 2026-05-21
autonomous: true
files_modified:
  - cha-bio-safety/docs/redesign-context/16-workshift/sketch-wave-2-header-select.html
  - cha-bio-safety/docs/redesign-context/16-workshift/sketch-wave-3-shift-table.html
  - cha-bio-safety/docs/redesign-context/16-workshift/sketch-wave-4-legend.html
  - cha-bio-safety/docs/redesign-context/16-workshift/wave-5-tsx-conversion-checklist.md
requirements:
  - redesign-16-workshift-w2-header-select
  - redesign-16-workshift-w3-shift-table
  - redesign-16-workshift-w4-legend
  - redesign-16-workshift-w5-tsx-checklist
must_haves:
  truths:
    - "W2~W4 sketch HTML 3개 + W5 checklist markdown 1개, 총 4 파일이 16-workshift/ 직속 평면 배치로 생성된다"
    - "WorkShiftPage.tsx (226 lines) + shiftCalc.ts + generateExcel.ts 모두 코드 변경 0건 — sketch + checklist 만 산출"
    - "negative gate 3 sketch + 1 checklist 모두 통과 (이모지 0 / linear-gradient 0 / 9·10·11px 0 / status- prefix 0 / w-8 h-8 0 / 옛 alias 토큰 0)"
    - "OQ #1~#5 LOCKED 결정 5건 모두 4 파일 안에 verbatim 반영 (엑셀 버튼 bg-safe-bar solid / 헤더 raised + 모바일 only back button / 폰트 격상 매핑 / SHIFT_COLOR hex+22 알파 인라인 / today border-accent + 공휴일 text-danger)"
    - "각 task 마다 atomic git commit 1개씩 — 총 4 commit"
  artifacts:
    - path: "cha-bio-safety/docs/redesign-context/16-workshift/sketch-wave-2-header-select.html"
      provides: "헤더 (영역 2) + 년·월 select (영역 3) sketch (모바일/데스크톱 분기)"
      contains: "data-theme dark+light 4 frame / bg-surface-raised / bg-safe-bar / w-[34px] h-[34px] rounded-sm / Lucide ChevronLeft size={15} / 월간 출근부 / 엑셀 저장 / 생성중... / 2025년~2027년 / 1월~12월"
      min_lines: 350
    - path: "cha-bio-safety/docs/redesign-context/16-workshift/sketch-wave-3-shift-table.html"
      provides: "표 영역 (영역 4) sketch — 이름 열 고정 + 날짜 열 가로 스크롤 + today highlight + 공휴일·주말 색 + shift 4종 셀"
      contains: "border-accent / bg-[rgba(59,130,246,0.15)] / text-danger / SHIFT_COLOR 4 hex (#ef4444 #3b82f6 #f59e0b #6b7280) / +'22' 알파 / 이름 / overflow-x:auto / 당 비 주 휴 / HDR_H=52 / ROW_H=46"
      min_lines: 500
    - path: "cha-bio-safety/docs/redesign-context/16-workshift/sketch-wave-4-legend.html"
      provides: "범례 (영역 5) sketch — 4 shift 박스 (24×24) + 한글 라벨 + 모바일/데스크톱 padding 분기"
      contains: "당직 / 비번 / 주간 / 휴무 / w-6 h-6 / rounded-[5px] / border-[1.5px] / SHIFT_COLOR 4 hex / +'22' 알파 / text-label font-extrabold leading-none / text-caption leading-none"
      min_lines: 200
    - path: "cha-bio-safety/docs/redesign-context/16-workshift/wave-5-tsx-conversion-checklist.md"
      provides: "TSX 변환 verify checklist (markdown, 12 섹션)"
      contains: "§1 변환 범위 / §2 보존 (18+ row) / §3 변환 매핑 (영역 2~5 verbatim) / §4 OQ LOCKED 5건 / §5 negative gate / §6 positive gate / §7 build tsc gate / §8 자체 verify 명령 / §9 Tailwind cheatsheet / §10 비즈 보존 체크박스 / §11 메모리 룰 inline 12+ / §12 다음 단계"
      min_lines: 350
  key_links:
    - from: "wave-1-index.md §7 OQ #1 LOCKED"
      to: "sketch-wave-2-header-select.html + wave-5-tsx-conversion-checklist.md"
      via: "엑셀 저장 버튼 bg-safe-bar solid + text-text-on-accent + disabled opacity-60 (var(--acl) 폐기)"
      pattern: "bg-safe-bar"
    - from: "wave-1-index.md §7 OQ #2 LOCKED"
      to: "sketch-wave-2-header-select.html"
      via: "헤더 bg-surface-raised + 모바일만 back button (Lucide ChevronLeft size={15}) + 데스크톱 back button 추가 X"
      pattern: "bg-surface-raised|ChevronLeft"
    - from: "wave-1-index.md §7 OQ #3 LOCKED"
      to: "sketch-wave-2 + sketch-wave-3 + sketch-wave-4 + wave-5-checklist"
      via: "폰트 격상 매핑 verbatim — 10→text-caption / 12→text-caption leading-none 유지 / 13→text-label font-bold / 14→text-body-sm font-bold (모바일 헤더 타이틀만 16 격상) / 15→text-body / 16→text-body"
      pattern: "text-caption|text-label|text-body"
    - from: "wave-1-index.md §7 OQ #4 LOCKED"
      to: "sketch-wave-3 + sketch-wave-4 + wave-5-checklist"
      via: "SHIFT_COLOR[sh]+'22' hex+22 알파 인라인 유지 — 셀 배경 + 셀 글자색 + 범례 박스 + 범례 글자 모두 SHIFT_COLOR 동적 inline style"
      pattern: "SHIFT_COLOR|#ef4444|#3b82f6|#f59e0b|#6b7280"
    - from: "wave-1-index.md §7 OQ #5 LOCKED"
      to: "sketch-wave-3 + wave-5-checklist"
      via: "today border var(--acl) → border-accent / today bg rgba(59,130,246,0.15) 인라인 유지 / 공휴일·주말 #ef4444 → text-danger 토큰 (tokens.css grep 검증 후 fallback)"
      pattern: "border-accent|text-danger|rgba\\(59,130,246,0\\.15"
---

<objective>
redesign/16-workshift 의 W1 인덱스(`wave-1-index.md`, 413 lines, OQ 5건 LOCKED 완료)를 기반으로
W2~W5 4 sub-wave 산출물을 **단일 quick task** 안에서 **atomic 4 commit** 으로 한 번에 만든다.

- **W2 (T1):** 헤더 (영역 2) + 년·월 select (영역 3) sketch — 모바일/데스크톱 분기 4 frame
- **W3 (T2):** 표 영역 (영역 4) sketch — 이름 열 고정 + 날짜 열 가로 스크롤 + today highlight + 공휴일·주말 색 + shift 4종 셀
- **W4 (T3):** 범례 (영역 5) sketch — 4 shift 박스 + 한글 라벨 + padding 모바일/데스크톱 분기
- **W5 (T4):** TSX 변환 verify checklist markdown (sketch 아님, W2~W4 grep 추출 검증 룰 + 12 섹션)

**Purpose:** 13-schedule / 14-reports / 27-login 와 동일한 평면(flat sibling) 패턴으로 16-workshift sketch 단계를
4 wave 분량을 한 turn 에 처리하여 cbc7119-preview 자동 배포 사이클 1회로 사용자 시각 검수 가능하게 함.

**Output:** `cha-bio-safety/docs/redesign-context/16-workshift/` 직속 4 파일 (sketch HTML 3개 + checklist md 1개).
`WorkShiftPage.tsx` + `shiftCalc.ts` + `generateExcel.ts` **3 파일 모두** 코드 변경 0건 (verify gate 마지막에 확인).

**27-login f01 PLAN.md mirror:** 본 PLAN 은 `.planning/quick/260521-f01-redesign-27-login-sketch-waves-w2-w5-4-s/260521-f01-PLAN.md` 구조 (단일 plan / 4 task / atomic 4 commit / verify gate 그대로) 의 정확한 mirror. 차이점 = LoginPage 220 → WorkShiftPage 226 / OQ 결정 내용 / source 3 파일 untouched (LoginPage 는 1 파일).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jykevin/Documents/cbc7119-design/CLAUDE.md
@/Users/jykevin/Documents/cbc7119-design/CLAUDE.local.md

# W1 인덱스 (단일 진입점, OQ 5건 LOCKED 완료)
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/16-workshift/wave-1-index.md

# Precedent — 27-login f01 PLAN.md + SUMMARY.md (정확한 mirror 소스)
@/Users/jykevin/Documents/cbc7119-design/.planning/quick/260521-f01-redesign-27-login-sketch-waves-w2-w5-4-s/260521-f01-PLAN.md
@/Users/jykevin/Documents/cbc7119-design/.planning/quick/260521-f01-redesign-27-login-sketch-waves-w2-w5-4-s/260521-f01-SUMMARY.md

# Precedent — 27-login 산출물 4 파일 (시각/구조 mirror)
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/27-login/sketch-wave-2-mobile-shell.html
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/27-login/sketch-wave-3-staff-card-grid.html
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/27-login/sketch-wave-4-login-form.html
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/27-login/wave-5-tsx-conversion-checklist.md

# Source TSX + utils (sketch 대상, 변경 0 — 3 파일 모두 untouched)
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/src/pages/WorkShiftPage.tsx
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/src/utils/shiftCalc.ts
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/src/utils/generateExcel.ts

# 16-workshift redesign 컨텍스트 (토큰/타이포 source)
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/16-workshift/16-workshift.md
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/16-workshift/design-system.md
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/16-workshift/tokens.css
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/16-workshift/typography.css

<rules_verbatim>
### OQ LOCKED 5건 (wave-1-index.md §7 verbatim)

**OQ #1 (엑셀 저장 버튼 bg-safe-bar solid):**
> LOCKED (2026-05-21): 엑셀 저장 버튼 = `bg-safe-bar text-text-on-accent` solid + disabled `opacity-60` 유지. `var(--acl)` 폐기. W2 sketch + TSX 변환 양쪽 동일 적용.

**OQ #2 (헤더 raised + 모바일 only back button):**
> LOCKED (2026-05-21): 헤더 배경 = `bg-surface-raised` 유지 + 데스크톱 back button 추가 X (모바일만 유지). 모바일 back button = chrome 룰 §7.2 패턴 적용 (`w-[34px] h-[34px] rounded-sm bg-surface-sunken border-border-default` + Lucide `ChevronLeft size={15}`).

**OQ #3 (폰트 격상 verbatim):**
> LOCKED (2026-05-21): 폰트 격상 매핑 verbatim (sketch + TSX 변환 양쪽):
> - fontSize:10 (staff title / DOW_KO 요일) → `text-caption` (12) `leading-none`
> - fontSize:12 (이름 헤더 / 범례 라벨 / 엑셀 버튼) → `text-caption` (12) `leading-none` 유지
> - fontSize:13 (셀 day / select / 범례 박스 글자) → `text-label` (13) `font-bold leading-none` (셀 day 는 font-bold 유지)
> - fontSize:14 (모바일 헤더 타이틀 / staff name) → `text-body-sm` (14) `font-bold` — 단 모바일 헤더 타이틀 14 → 16 격상 (`text-body font-bold`, §1.3 모바일/데스크톱 동일 폰트 룰 부합)
> - fontSize:15 (셀 shift) → `text-body` (16) `font-bold` (셀 가독성 우선)
> - fontSize:16 (데스크톱 헤더 타이틀) → `text-body` (16) `font-bold` 유지

**OQ #4 (SHIFT_COLOR hex+22 알파 인라인):**
> LOCKED (2026-05-21): `SHIFT_COLOR[sh]+'22'` hex+22 알파 인라인 유지. 셀 배경 + 셀 글자색 + 범례 박스 + 범례 글자 모두 SHIFT_COLOR 동적 inline `style={{ background: ..., borderColor: ..., color: ... }}` 패턴 유지. shiftCalc.ts SHIFT_COLOR 상수 1 byte 변경 X.

**OQ #5 (today border-accent + 공휴일 text-danger):**
> LOCKED (2026-05-21): today border `var(--acl)` → `border-accent` 토큰 / today bg `rgba(59,130,246,0.15)` → 인라인 `bg-[rgba(59,130,246,0.15)]` 유지 (신규 토큰 정의 비용 회피) / 공휴일·주말 글자 `#ef4444` → `text-danger` 토큰 치환. W5 sketch 진입 시 tokens.css grep 으로 `--status-danger` hex 가 `#ef4444` 와 일치하는지 1줄 검증 후 적용. 일치 안 하면 인라인 `text-[#ef4444]` fallback.

### 디자인 토큰 패턴 (wave-1-index.md §3 + planning constraints verbatim)
- `bg-surface-page` (var(--bg) → --surface-page) 페이지 외곽
- `bg-surface-raised` (var(--bg2) → --surface-raised) 헤더/카드/표 셀
- `bg-surface-sunken` (var(--bg3) → --surface-sunken) input/select/이름 헤더
- `border-border-default` (var(--bd) → --border-default)
- `border-border-strong` (var(--bd2) → --border-strong)
- `text-text-primary` (var(--t1) → --text-primary)
- `text-text-secondary` (var(--t2) → --text-secondary)
- `text-text-tertiary` (var(--t3) → --text-tertiary)
- `text-text-on-accent` (액센트 버튼 위 흰 글자)
- `bg-safe-bar` / `bg-accent` / `border-accent` (status- prefix 없음 — memory feedback_tailwind_token_class_pattern)
- `text-danger` (status- prefix 없음)
- radius: `rounded-sm` 8 / `rounded-md` 12 / `rounded-lg` 16 / 토큰 없는 값은 `rounded-[Npx]` 인라인 (예: 9, 5)

### Negative rule (모든 task verify gate)
- 이모지 0건 (sketch 본문)
- `linear-gradient` 0건 (모든 sketch + checklist)
- 9·10·11px fontSize 0건 (OQ #3 격상 결과)
- `(text|bg|border)-status-` class prefix 0건 (`text-status-safe` X, `bg-status-fire` X)
- `\b(w|h)-8\b` 0건 (tailwind config w-8=48 함정, memory feedback_tailwind_w8_h8_is_48px)
- 옛 alias 토큰 (`var(--bg)` `var(--bg2)` `var(--bg3)` `var(--bd)` `var(--bd2)` `var(--t1)` `var(--t2)` `var(--t3)` `var(--acl)` `var(--accent)`) 0건 in sketch (단, tokens.css 정의 블록 인용 시 fence 안 예외)

### 평면 폴더 패턴 (13-schedule + 14-reports + 27-login 일관)
- 16-workshift/ 직속에 4 파일 배치
- `sketch/` 서브폴더 절대 금지
</rules_verbatim>
</context>

<tasks>

<task type="auto">
  <name>Task 1 (W2): 헤더 + 년·월 select sketch 생성 (모바일/데스크톱 4 frame, bg-safe-bar solid)</name>
  <files>cha-bio-safety/docs/redesign-context/16-workshift/sketch-wave-2-header-select.html</files>
  <action>
    27-login/sketch-wave-2-mobile-shell.html 패턴 mirror 로 새 sketch 파일 작성. 영역 2 (헤더, WorkShiftPage.tsx line 87~111) + 영역 3 (년·월 select, line 113~121) 시각화. 모바일 + 데스크톱 분기 모두 표현. **OQ #1 LOCKED (엑셀 버튼 bg-safe-bar solid)** + **OQ #2 LOCKED (헤더 raised + 모바일 only back button)** + **OQ #3 LOCKED (폰트 격상)** 3건이 본 wave 의 핵심.

    **구조 (27-login W2 mirror):**
    - `<!DOCTYPE html>` + `<html lang="ko">` + `<head><meta charset="utf-8"><title>redesign/16-workshift — W2 헤더 + 년·월 select</title>` + `<style>` 블록
    - `<style>` 안: v0.1.1 tokens.css 의 dark+light 양쪽 정의 verbatim 인용 (`[data-theme="dark"]` + `[data-theme="light"]` selector), typography.css 의 `text-caption` `text-label` `text-body-sm` `text-body` `font-bold` 정의 verbatim 인용
    - `<body>` 안 4 frame side-by-side:
      | frame | viewport | 시각화 |
      |---|---|---|
      | 1 | 모바일 다크 393×320 | `data-theme="dark"` + 헤더 (back button + 타이틀 "월간 출근부" + 엑셀 저장 버튼 default) + 년·월 select row |
      | 2 | 모바일 라이트 393×320 | `data-theme="light"` + 동일 (라이트 명도 차 확인) |
      | 3 | 데스크톱 다크 1280×320 | `data-theme="dark"` + 헤더 (height:54 + padding '0 20px' + back button 없음 + 타이틀 16px + 엑셀 저장 버튼) + 년·월 select row |
      | 4 | 데스크톱 라이트 1280×320 | `data-theme="light"` + 동일 |

    **모바일 헤더 마크업 룰 (frame 1, 2 — OQ #1 + #2 + #3 LOCKED 반영):**
    ```html
    <header class="bg-surface-raised border-b border-border-default flex items-center" style="padding:8px 12px 9px;gap:8px;flex-shrink:0;">
      <!-- 모바일 back button (OQ #2 LOCKED: chrome §7.2 패턴 + Lucide ChevronLeft size={15}) -->
      <button class="w-[34px] h-[34px] rounded-sm bg-surface-sunken border border-border-default flex items-center justify-center" style="cursor:pointer;">
        <!-- Lucide ChevronLeft size={15} (sketch 에서는 SVG 그대로 표현, TSX 변환 시 lucide-react Import) -->
        <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="text-text-secondary">
          <polyline points="15 18 9 12 15 6"></polyline>
        </svg>
      </button>
      <!-- 타이틀 (OQ #3 LOCKED: 모바일 헤더 타이틀 14→16 격상, text-body font-bold) -->
      <span class="text-body font-bold text-text-primary" style="flex:1;">월간 출근부</span>
      <!-- 엑셀 저장 버튼 (OQ #1 LOCKED: bg-safe-bar solid + text-text-on-accent, var(--acl) 폐기) -->
      <button class="bg-safe-bar text-text-on-accent text-caption font-bold leading-none rounded-sm border-0" style="height:34px;padding:0 14px;cursor:pointer;">
        엑셀 저장
      </button>
    </header>
    ```

    **데스크톱 헤더 마크업 룰 (frame 3, 4 — OQ #2 LOCKED: 데스크톱 back button 추가 X):**
    ```html
    <header class="bg-surface-raised border-b border-border-default flex items-center" style="height:54px;padding:0 20px;gap:10px;flex-shrink:0;">
      <!-- 데스크톱은 back button 없음 (OQ #2 LOCKED) -->
      <!-- 타이틀 (OQ #3 LOCKED: 데스크톱 16, text-body font-bold) -->
      <span class="text-body font-bold text-text-primary" style="flex:1;">월간 출근부</span>
      <!-- 엑셀 저장 버튼 (OQ #1 LOCKED) -->
      <button class="bg-safe-bar text-text-on-accent text-caption font-bold leading-none rounded-sm border-0" style="height:34px;padding:0 14px;cursor:pointer;">
        엑셀 저장
      </button>
    </header>
    ```

    **disabled (생성중) variant (frame 1 또는 별도 variant row 안 추가):**
    ```html
    <button class="bg-safe-bar text-text-on-accent text-caption font-bold leading-none rounded-sm border-0" style="height:34px;padding:0 14px;cursor:not-allowed;opacity:.6;" disabled>
      생성중...
    </button>
    ```

    **년·월 select 마크업 룰 (4 frame 공통 — OQ #3 LOCKED 반영):**
    ```html
    <div class="bg-surface-raised border-b border-border-default flex" style="gap:8px;padding:10px 12px;flex-shrink:0;">
      <select class="bg-surface-sunken border border-border-strong text-text-primary text-label rounded-[9px]" style="padding:7px 10px;outline:none;">
        <option>2025년</option>
        <option selected>2026년</option>
        <option>2027년</option>
      </select>
      <select class="bg-surface-sunken border border-border-strong text-text-primary text-label rounded-[9px]" style="padding:7px 10px;outline:none;">
        <option>1월</option>
        <option>2월</option>
        <option>3월</option>
        <option>4월</option>
        <option selected>5월</option>
        <option>6월</option>
        <option>7월</option>
        <option>8월</option>
        <option>9월</option>
        <option>10월</option>
        <option>11월</option>
        <option>12월</option>
      </select>
    </div>
    ```
    (월 옵션 12개 모두 verbatim — memory `feedback_sketch_realistic_data` 의 카피 룰 적용.)

    **rules 박스 (sketch 하단, viewport-label 외 메타):**
    - LOCKED 결정: OQ #1 (bg-safe-bar solid) + OQ #2 (헤더 raised + 모바일 only back button + ChevronLeft size={15}) + OQ #3 (모바일 헤더 타이틀 14→16 격상 / 엑셀 버튼 12 text-caption leading-none / select 13 text-label) verbatim 인용 1 fence
    - 토큰 매핑: `var(--bg) → --surface-page` / `var(--bg2) → --surface-raised` / `var(--bg3) → --surface-sunken` / `var(--bd) → --border-default` / `var(--bd2) → --border-strong` / `var(--t1) → --text-primary` / `var(--t2) → --text-secondary` / `var(--acl) → --safe-bar (CTA)` / radius:8 → `rounded-sm` / radius:9 (select) → `rounded-[9px]` 인라인 (토큰 없음)
    - negative gate 자체매칭 회피: "이모지 0 / linear-gradient 0 / 9·10·11px 0 / status- prefix 0 / w-8 h-8 0 / 옛 alias 토큰 0" 메타 줄은 `<code>` fence 안 또는 escape 표현 (`w-8`/`h-8` 같은 문자열 sketch 본문에 등장하지 않도록 fence 처리)

    **commit 메시지:**
    `docs(quick-260521-t12): redesign/16-workshift W2 header + year/month select sketch (4 frame, bg-safe-bar solid + Lucide ChevronLeft)`

    **commit 명령:**
    `git add cha-bio-safety/docs/redesign-context/16-workshift/sketch-wave-2-header-select.html && git commit -m "docs(quick-260521-t12): redesign/16-workshift W2 header + year/month select sketch (4 frame, bg-safe-bar solid + Lucide ChevronLeft)"`
  </action>
  <verify>
    <automated>
test -f cha-bio-safety/docs/redesign-context/16-workshift/sketch-wave-2-header-select.html \
  && wc -l cha-bio-safety/docs/redesign-context/16-workshift/sketch-wave-2-header-select.html | awk '{ if ($1 < 200) { print "FAIL: too short"; exit 1 } else print "PASS lines:", $1 }' \
  && grep -c 'data-theme="dark"' cha-bio-safety/docs/redesign-context/16-workshift/sketch-wave-2-header-select.html | awk '{ if ($1 < 2) { print "FAIL: dark frame <2"; exit 1 } else print "PASS dark frame:", $1 }' \
  && grep -c 'data-theme="light"' cha-bio-safety/docs/redesign-context/16-workshift/sketch-wave-2-header-select.html | awk '{ if ($1 < 2) { print "FAIL: light frame <2"; exit 1 } else print "PASS light frame:", $1 }' \
  && grep -c 'bg-safe-bar' cha-bio-safety/docs/redesign-context/16-workshift/sketch-wave-2-header-select.html | awk '{ if ($1 < 4) { print "FAIL: bg-safe-bar <4 (4 frame x 1 each)"; exit 1 } else print "PASS bg-safe-bar:", $1 }' \
  && grep -c 'text-text-on-accent' cha-bio-safety/docs/redesign-context/16-workshift/sketch-wave-2-header-select.html | awk '{ if ($1 < 4) { print "FAIL: text-text-on-accent <4"; exit 1 } else print "PASS text-text-on-accent:", $1 }' \
  && grep -c 'bg-surface-raised' cha-bio-safety/docs/redesign-context/16-workshift/sketch-wave-2-header-select.html | awk '{ if ($1 < 4) { print "FAIL: bg-surface-raised <4 (헤더 4 frame)"; exit 1 } else print "PASS bg-surface-raised:", $1 }' \
  && grep -c '월간 출근부' cha-bio-safety/docs/redesign-context/16-workshift/sketch-wave-2-header-select.html | awk '{ if ($1 < 4) { print "FAIL: 타이틀 <4"; exit 1 } else print "PASS 월간 출근부:", $1 }' \
  && grep -c '엑셀 저장' cha-bio-safety/docs/redesign-context/16-workshift/sketch-wave-2-header-select.html | awk '{ if ($1 < 4) { print "FAIL: 엑셀 저장 <4"; exit 1 } else print "PASS 엑셀 저장:", $1 }' \
  && grep -c '2026년' cha-bio-safety/docs/redesign-context/16-workshift/sketch-wave-2-header-select.html | awk '{ if ($1 < 4) { print "FAIL: 2026년 <4"; exit 1 } else print "PASS 2026년:", $1 }' \
  && grep -c 'w-\[34px\]' cha-bio-safety/docs/redesign-context/16-workshift/sketch-wave-2-header-select.html | awk '{ if ($1 < 2) { print "FAIL: 모바일 back button w-[34px] <2 (모바일 frame 2개)"; exit 1 } else print "PASS w-[34px]:", $1 }' \
  && grep -cE 'linear-gradient' cha-bio-safety/docs/redesign-context/16-workshift/sketch-wave-2-header-select.html | awk '{ if ($1 > 0) { print "FAIL: linear-gradient found (OQ #1 LOCKED 위반)"; exit 1 } else print "PASS no gradient" }' \
  && grep -cE '(text|bg|border)-status-(safe|fire|warning|danger|caution)' cha-bio-safety/docs/redesign-context/16-workshift/sketch-wave-2-header-select.html | awk '{ if ($1 > 0) { print "FAIL: status- prefix found"; exit 1 } else print "PASS no status- prefix" }' \
  && grep -v '^<!--' cha-bio-safety/docs/redesign-context/16-workshift/sketch-wave-2-header-select.html | grep -cE 'font-size:\s*(9|10|11)[^0-9]|fontSize:\s*(9|10|11)[^0-9]' | awk '{ if ($1 > 0) { print "FAIL: 9/10/11 px found"; exit 1 } else print "PASS no 9-11px" }' \
  && grep -cE '\bw-8\b|\bh-8\b' cha-bio-safety/docs/redesign-context/16-workshift/sketch-wave-2-header-select.html | awk '{ if ($1 > 0) { print "FAIL: w-8/h-8 found (48px 함정)"; exit 1 } else print "PASS no w-8 h-8" }' \
  && grep -cE 'OQ #1|OQ #2|OQ #3' cha-bio-safety/docs/redesign-context/16-workshift/sketch-wave-2-header-select.html | awk '{ if ($1 < 3) { print "FAIL: OQ #1/#2/#3 citation <3"; exit 1 } else print "PASS OQ citations:", $1 }' \
  && git log --oneline -1 | grep -q '260521-t12.*W2' && echo "PASS commit W2"
    </automated>
  </verify>
  <done>
    - sketch-wave-2-header-select.html 파일 존재 + ≥200 lines (≥350 권장)
    - data-theme="dark" / data-theme="light" 각 ≥2 frame (총 4 frame)
    - bg-safe-bar / text-text-on-accent (엑셀 버튼 default) ≥4 (4 frame x 1 each)
    - bg-surface-raised (헤더 + 년월 컨테이너) ≥4
    - "월간 출근부" / "엑셀 저장" / "2026년" 각 ≥4 (4 frame)
    - w-[34px] (모바일 back button) ≥2 (모바일 2 frame)
    - linear-gradient 0건 / status- prefix 0건 / 9·10·11px fontSize 0건 / w-8 h-8 0건
    - OQ #1 + OQ #2 + OQ #3 인용 ≥3건
    - atomic commit `docs(quick-260521-t12): ... W2 header + year/month select sketch ...` 1개
  </done>
</task>

<task type="auto">
  <name>Task 2 (W3): 표 영역 sketch 생성 (이름 열 고정 + 날짜 열 가로 스크롤 + today highlight + 공휴일·주말 색 + shift 4종 셀)</name>
  <files>cha-bio-safety/docs/redesign-context/16-workshift/sketch-wave-3-shift-table.html</files>
  <action>
    27-login/sketch-wave-3-staff-card-grid.html 패턴 mirror 로 새 sketch 파일 작성. 영역 4 (표 영역, WorkShiftPage.tsx line 123~210) 시각화. **이름 열 고정 + 날짜 열 가로 스크롤** 2-table 구조 (memory `feedback_dashboard_horizontal_scroll` flex-wrap 금지 룰 명시). today highlight + 공휴일·주말 색 + shift 4종 셀 (당/비/주/휴) 모두 매트릭스 표현. **OQ #3 LOCKED (폰트 격상)** + **OQ #4 LOCKED (SHIFT_COLOR hex+22 알파 인라인)** + **OQ #5 LOCKED (today border-accent + 공휴일 text-danger)** 3건이 본 wave 의 핵심.

    **사전 작업 (T2 진입 시점 1줄 검증, OQ #5 LOCKED 요구사항):**
    `grep -E '\-\-status-danger:' cha-bio-safety/docs/redesign-context/16-workshift/tokens.css` 으로 `--status-danger` hex 확인.
    - `#ef4444` 면 → `text-danger` 토큰 사용 (default).
    - hex 불일치면 → 인라인 `text-[#ef4444]` fallback 표현.
    sketch HTML 안 rules 박스에 검증 결과 1줄 명시.

    **구조 (27-login W3 mirror):**
    - `<!DOCTYPE html>` + `<head><title>redesign/16-workshift — W3 표 영역</title>` + `<style>` 블록 (W2 와 동일 tokens + typography 인용)
    - `<body>` 안 4 frame side-by-side:
      | frame | viewport | 시각화 |
      |---|---|---|
      | 1 | 모바일 다크 393×600 today + 공휴일 강조 | `data-theme="dark"` + 표 (이름 열 4행 + 날짜 열 7일 부분 — 오늘 highlight + 공휴일 1일 + 주말 2일 표현) + 직원 4명 (STAFF_ORDER 석현민/김병조/윤종엽/박보융) + shift 4종 (당/비/주/휴) 모두 분포 |
      | 2 | 모바일 다크 393×600 전체 시즌 | `data-theme="dark"` + 동일 표 + 일자 14~21 범위 (셀 가독성 다른 위치 확인) |
      | 3 | 모바일 라이트 393×600 | `data-theme="light"` + frame 1 mirror (라이트 명도 차) |
      | 4 | 데스크톱 다크 1280×800 centered + 12vh padding-top | `data-theme="dark"` + 외곽 (paddingTop:12vh + flex center) + 표 (이름 열 + 날짜 열 31일 일부 + 가로 scroll cue) |

    **표 마크업 룰 (각 frame 공통 — OQ #3 + #4 + #5 LOCKED 반영):**
    ```html
    <div style="flex:1;overflow:auto;display:flex;flex-direction:column;align-items:center;padding-top:0;">
      <div style="display:inline-flex;flex-direction:column;padding:16px 24px;">
        <div style="display:flex;">
          <!-- 이름 열 (고정, flex-shrink:0) -->
          <div style="flex-shrink:0;">
            <table style="border-collapse:collapse;">
              <thead>
                <tr>
                  <!-- 이름 헤더: HDR_H=52, w:82, OQ #3 LOCKED text-caption font-bold leading-none -->
                  <th class="bg-surface-sunken border border-border-default text-text-secondary text-caption font-bold leading-none" style="height:52px;width:82px;padding:0 10px;white-space:nowrap;">
                    이름
                  </th>
                </tr>
              </thead>
              <tbody>
                <!-- staff 4명, ROW_H=46 -->
                <tr>
                  <td class="bg-surface-raised border border-border-default" style="height:46px;padding:0 10px;white-space:nowrap;">
                    <!-- staff name: OQ #3 LOCKED text-body-sm font-bold -->
                    <div class="text-body-sm font-bold text-text-primary">석현민</div>
                    <!-- staff title: OQ #3 LOCKED 10→text-caption leading-none -->
                    <div class="text-caption leading-none text-text-tertiary" style="margin-top:2px;">반장</div>
                  </td>
                </tr>
                <!-- 김병조 / 윤종엽 / 박보융 (STAFF_ORDER verbatim) -->
              </tbody>
            </table>
          </div>

          <!-- 날짜 열 (가로 스크롤, scrollRef) -->
          <!-- 가로 스크롤 cue: overflow-x:auto + flex:1 (memory feedback_dashboard_horizontal_scroll 룰 — flex-wrap 금지) -->
          <div style="flex:1;overflow-x:auto;">
            <table style="border-collapse:collapse;">
              <thead>
                <tr>
                  <!-- day cell × N일 — minWidth:40, padding:4px 2px, HDR_H=52 -->
                  <!-- normal day -->
                  <th class="bg-surface-sunken border border-border-default text-text-secondary" style="height:52px;min-width:40px;padding:4px 2px;text-align:center;">
                    <!-- d: OQ #3 LOCKED text-label font-bold leading-none -->
                    <div class="text-label font-bold leading-none">1</div>
                    <!-- DOW_KO: OQ #3 LOCKED 10→text-caption leading-none -->
                    <div class="text-caption leading-none" style="margin-top:2px;">월</div>
                  </th>
                  <!-- today day cell — OQ #5 LOCKED border-accent + 인라인 bg-[rgba(59,130,246,0.15)] -->
                  <th class="border-2 border-accent text-text-secondary" style="height:52px;min-width:40px;padding:4px 2px;text-align:center;background:rgba(59,130,246,0.15);">
                    <div class="text-label font-bold leading-none">2</div>
                    <div class="text-caption leading-none" style="margin-top:2px;">화</div>
                  </th>
                  <!-- 공휴일/주말 day cell — OQ #5 LOCKED text-danger (또는 fallback text-[#ef4444]) -->
                  <th class="bg-surface-sunken border border-border-default text-danger" style="height:52px;min-width:40px;padding:4px 2px;text-align:center;">
                    <div class="text-label font-bold leading-none">7</div>
                    <div class="text-caption leading-none" style="margin-top:2px;">일</div>
                  </th>
                </tr>
              </thead>
              <tbody>
                <!-- staff × day shift cell — ROW_H=46, OQ #4 LOCKED SHIFT_COLOR[sh]+'22' 인라인 -->
                <tr>
                  <!-- 당직 (빨강 #ef4444) -->
                  <td class="border border-border-default text-body font-bold" style="height:46px;min-width:40px;padding:0 2px;text-align:center;color:#ef4444;background:#ef444422;">
                    당
                  </td>
                  <!-- today 셀 + 비번 (파랑 #3b82f6) — OQ #5 LOCKED border-accent + OQ #4 LOCKED SHIFT_COLOR -->
                  <td class="border-2 border-accent text-body font-bold" style="height:46px;min-width:40px;padding:0 2px;text-align:center;color:#3b82f6;background:#3b82f622;">
                    비
                  </td>
                  <!-- 주간 (노랑 #f59e0b) -->
                  <td class="border border-border-default text-body font-bold" style="height:46px;min-width:40px;padding:0 2px;text-align:center;color:#f59e0b;background:#f59e0b22;">
                    주
                  </td>
                  <!-- 휴무 (회색 #6b7280) -->
                  <td class="border border-border-default text-body font-bold" style="height:46px;min-width:40px;padding:0 2px;text-align:center;color:#6b7280;background:#6b728022;">
                    휴
                  </td>
                </tr>
                <!-- 직원 4명 모두 표현, shift 4종 모두 분포 -->
              </tbody>
            </table>
          </div>
        </div>
      </div>
    </div>
    ```

    **SHIFT_COLOR 4 hex verbatim (shiftCalc.ts line 109~114 source):**
    ```
    당: #ef4444 (빨강) — 당직
    비: #3b82f6 (파랑) — 비번
    주: #f59e0b (노랑) — 주간
    휴: #6b7280 (회색) — 휴무
    ```
    각 셀 글자색 = SHIFT_COLOR[sh] verbatim hex / 셀 배경 = SHIFT_COLOR[sh]+'22' (hex+22 알파 흉내) 인라인 유지 (OQ #4 LOCKED).

    **데스크톱 frame 4 특이사항:**
    - `paddingTop: 12vh` 인라인 유지 (line 129 verbatim)
    - `padding: 0 32px` (line 131, 데스크톱 분기) verbatim
    - 가로 scroll cue (overflow-x:auto) callout 박스로 표현

    **STAFF_ORDER 4명 verbatim:**
    `['석현민', '김병조', '윤종엽', '박보융']` (line 43 source) — sketch 안 4 staff row 모두 이 순서 + 직책 mock ('반장'/'책임'/'주임'/'주임' 또는 실제 staff 데이터 가져오지 않음, memory `feedback_sketch_realistic_data`).

    **rules 박스:**
    - LOCKED 결정: OQ #3 (폰트 격상 표) + OQ #4 (SHIFT_COLOR hex+22 알파 인라인) + OQ #5 (border-accent + text-danger / tokens.css grep 검증 결과 1줄) verbatim 인용 1 fence
    - 토큰 매핑: 이름 헤더 `bg-surface-sunken border border-border-default` / staff name 셀 `bg-surface-raised border border-border-default` / day cell normal `bg-surface-sunken border border-border-default text-text-secondary` / day cell today `border-2 border-accent + 인라인 bg-[rgba(59,130,246,0.15)]` / day cell 공휴일·주말 `text-danger` (또는 인라인 fallback) / shift cell `border border-border-default + 인라인 color/background SHIFT_COLOR`
    - 가로 스크롤 cue 명시: "memory feedback_dashboard_horizontal_scroll — overflow-x:auto + flex:1, flex-wrap 금지 (단일행 31일)"

    **commit 메시지:**
    `docs(quick-260521-t12): redesign/16-workshift W3 shift table sketch (이름 열 고정 + 가로 스크롤 + today border-accent + 공휴일 text-danger + shift 4종 hex 인라인)`

    **commit 명령:**
    `git add cha-bio-safety/docs/redesign-context/16-workshift/sketch-wave-3-shift-table.html && git commit -m "docs(quick-260521-t12): redesign/16-workshift W3 shift table sketch (이름 열 고정 + 가로 스크롤 + today border-accent + 공휴일 text-danger + shift 4종 hex 인라인)"`
  </action>
  <verify>
    <automated>
test -f cha-bio-safety/docs/redesign-context/16-workshift/sketch-wave-3-shift-table.html \
  && wc -l cha-bio-safety/docs/redesign-context/16-workshift/sketch-wave-3-shift-table.html | awk '{ if ($1 < 300) { print "FAIL: too short"; exit 1 } else print "PASS lines:", $1 }' \
  && grep -c 'data-theme="dark"' cha-bio-safety/docs/redesign-context/16-workshift/sketch-wave-3-shift-table.html | awk '{ if ($1 < 3) { print "FAIL: dark frame <3"; exit 1 } else print "PASS dark frame:", $1 }' \
  && grep -c 'border-accent' cha-bio-safety/docs/redesign-context/16-workshift/sketch-wave-3-shift-table.html | awk '{ if ($1 < 1) { print "FAIL: today border-accent <1 (OQ #5 LOCKED 위반)"; exit 1 } else print "PASS border-accent:", $1 }' \
  && grep -cE 'text-danger|text-\[#ef4444\]' cha-bio-safety/docs/redesign-context/16-workshift/sketch-wave-3-shift-table.html | awk '{ if ($1 < 1) { print "FAIL: text-danger 공휴일 <1 (OQ #5 LOCKED 위반)"; exit 1 } else print "PASS text-danger:", $1 }' \
  && grep -c 'rgba(59,130,246,0\.15' cha-bio-safety/docs/redesign-context/16-workshift/sketch-wave-3-shift-table.html | awk '{ if ($1 < 1) { print "FAIL: today bg rgba <1 (OQ #5 LOCKED 위반)"; exit 1 } else print "PASS today bg:", $1 }' \
  && grep -c '#ef4444' cha-bio-safety/docs/redesign-context/16-workshift/sketch-wave-3-shift-table.html | awk '{ if ($1 < 1) { print "FAIL: SHIFT_COLOR 당직 #ef4444 <1 (OQ #4 LOCKED 위반)"; exit 1 } else print "PASS 당직 hex:", $1 }' \
  && grep -c '#3b82f6' cha-bio-safety/docs/redesign-context/16-workshift/sketch-wave-3-shift-table.html | awk '{ if ($1 < 1) { print "FAIL: SHIFT_COLOR 비번 #3b82f6 <1"; exit 1 } else print "PASS 비번 hex:", $1 }' \
  && grep -c '#f59e0b' cha-bio-safety/docs/redesign-context/16-workshift/sketch-wave-3-shift-table.html | awk '{ if ($1 < 1) { print "FAIL: SHIFT_COLOR 주간 #f59e0b <1"; exit 1 } else print "PASS 주간 hex:", $1 }' \
  && grep -c '#6b7280' cha-bio-safety/docs/redesign-context/16-workshift/sketch-wave-3-shift-table.html | awk '{ if ($1 < 1) { print "FAIL: SHIFT_COLOR 휴무 #6b7280 <1"; exit 1 } else print "PASS 휴무 hex:", $1 }' \
  && grep -cE '#ef444422|#3b82f622|#f59e0b22|#6b728022|SHIFT_COLOR\[sh\]\+.22' cha-bio-safety/docs/redesign-context/16-workshift/sketch-wave-3-shift-table.html | awk '{ if ($1 < 1) { print "FAIL: hex+22 알파 <1 (OQ #4 LOCKED 위반)"; exit 1 } else print "PASS hex+22 알파:", $1 }' \
  && grep -c '이름' cha-bio-safety/docs/redesign-context/16-workshift/sketch-wave-3-shift-table.html | awk '{ if ($1 < 4) { print "FAIL: 이름 헤더 <4 (4 frame)"; exit 1 } else print "PASS 이름:", $1 }' \
  && grep -c '석현민' cha-bio-safety/docs/redesign-context/16-workshift/sketch-wave-3-shift-table.html | awk '{ if ($1 < 1) { print "FAIL: STAFF_ORDER 석현민 missing"; exit 1 } else print "PASS 석현민:", $1 }' \
  && grep -cE 'overflow-x:\s*auto|overflowX:.*auto' cha-bio-safety/docs/redesign-context/16-workshift/sketch-wave-3-shift-table.html | awk '{ if ($1 < 1) { print "FAIL: 가로 스크롤 cue missing"; exit 1 } else print "PASS overflow-x auto:", $1 }' \
  && grep -cE '\b당\b' cha-bio-safety/docs/redesign-context/16-workshift/sketch-wave-3-shift-table.html | awk '{ if ($1 < 1) { print "FAIL: shift 당 missing"; exit 1 } else print "PASS shift 당:", $1 }' \
  && grep -cE '\b비\b' cha-bio-safety/docs/redesign-context/16-workshift/sketch-wave-3-shift-table.html | awk '{ if ($1 < 1) { print "FAIL: shift 비 missing"; exit 1 } else print "PASS shift 비:", $1 }' \
  && grep -cE '\b주\b' cha-bio-safety/docs/redesign-context/16-workshift/sketch-wave-3-shift-table.html | awk '{ if ($1 < 1) { print "FAIL: shift 주 missing"; exit 1 } else print "PASS shift 주:", $1 }' \
  && grep -cE '\b휴\b' cha-bio-safety/docs/redesign-context/16-workshift/sketch-wave-3-shift-table.html | awk '{ if ($1 < 1) { print "FAIL: shift 휴 missing"; exit 1 } else print "PASS shift 휴:", $1 }' \
  && grep -cE 'linear-gradient' cha-bio-safety/docs/redesign-context/16-workshift/sketch-wave-3-shift-table.html | awk '{ if ($1 > 0) { print "FAIL: linear-gradient found"; exit 1 } else print "PASS no gradient" }' \
  && grep -cE '(text|bg|border)-status-(safe|fire|warning|danger|caution)' cha-bio-safety/docs/redesign-context/16-workshift/sketch-wave-3-shift-table.html | awk '{ if ($1 > 0) { print "FAIL: status- prefix found"; exit 1 } else print "PASS no status- prefix" }' \
  && grep -v '^<!--' cha-bio-safety/docs/redesign-context/16-workshift/sketch-wave-3-shift-table.html | grep -cE 'font-size:\s*(9|10|11)[^0-9]|fontSize:\s*(9|10|11)[^0-9]' | awk '{ if ($1 > 0) { print "FAIL: 9/10/11 px found"; exit 1 } else print "PASS no 9-11px" }' \
  && grep -cE '\bw-8\b|\bh-8\b' cha-bio-safety/docs/redesign-context/16-workshift/sketch-wave-3-shift-table.html | awk '{ if ($1 > 0) { print "FAIL: w-8/h-8 found"; exit 1 } else print "PASS no w-8 h-8" }' \
  && grep -cE 'OQ #3|OQ #4|OQ #5' cha-bio-safety/docs/redesign-context/16-workshift/sketch-wave-3-shift-table.html | awk '{ if ($1 < 3) { print "FAIL: OQ #3/#4/#5 citation <3"; exit 1 } else print "PASS OQ citations:", $1 }' \
  && git log --oneline -1 | grep -q '260521-t12.*W3' && echo "PASS commit W3"
    </automated>
  </verify>
  <done>
    - sketch-wave-3-shift-table.html 파일 존재 + ≥300 lines (≥500 권장)
    - data-theme="dark" ≥3 / data-theme="light" ≥1 (총 4 frame)
    - border-accent (today) ≥1 / text-danger 또는 text-[#ef4444] (공휴일) ≥1 / rgba(59,130,246,0.15) (today bg) ≥1
    - SHIFT_COLOR 4 hex (#ef4444 / #3b82f6 / #f59e0b / #6b7280) 모두 등장
    - hex+22 알파 표현 (#ef444422 등 또는 SHIFT_COLOR[sh]+'22' 인용) ≥1
    - "이름" 헤더 ≥4 (4 frame) / "석현민" (STAFF_ORDER 1번) ≥1
    - overflow-x:auto 또는 overflowX 가로 스크롤 cue ≥1
    - shift 4종 (당/비/주/휴) 모두 ≥1 등장 (단일 글자 word boundary)
    - linear-gradient 0건 / status- prefix 0건 / 9·10·11px fontSize 0건 / w-8 h-8 0건
    - OQ #3 + OQ #4 + OQ #5 인용 ≥3건
    - atomic commit `docs(quick-260521-t12): ... W3 shift table sketch ...` 1개
  </done>
</task>

<task type="auto">
  <name>Task 3 (W4): 범례 sketch 생성 (4 shift 박스 24×24 + 한글 라벨 + 모바일/데스크톱 padding 분기)</name>
  <files>cha-bio-safety/docs/redesign-context/16-workshift/sketch-wave-4-legend.html</files>
  <action>
    27-login/sketch-wave-4-login-form.html 패턴 mirror 로 새 sketch 파일 작성. 영역 5 (범례, WorkShiftPage.tsx line 212~220) 시각화. 4 shift 박스 (24×24 = `w-6 h-6`) + 한글 라벨 (SHIFT_LABEL) + 모바일/데스크톱 padding 분기 정리. **OQ #3 LOCKED (폰트 격상)** + **OQ #4 LOCKED (SHIFT_COLOR hex+22 알파 인라인)** 2건이 본 wave 의 핵심.

    **구조 (27-login W4 mirror):**
    - `<!DOCTYPE html>` + `<head><title>redesign/16-workshift — W4 범례</title>` + `<style>` 블록 (W2/W3 와 동일 tokens + typography 인용)
    - `<body>` 안 3 frame side-by-side:
      | frame | viewport | 시각화 |
      |---|---|---|
      | 1 | 모바일 다크 393×200 | `data-theme="dark"` + 범례 컨테이너 (flex-wrap + justify-center + gap-14 + padding:10px 0 28px) + 4 shift 박스 (당/비/주/휴) + 라벨 4개 |
      | 2 | 모바일 라이트 393×200 | `data-theme="light"` + frame 1 mirror (라이트 명도 차) |
      | 3 | 데스크톱 다크 1280×200 | `data-theme="dark"` + 범례 (데스크톱 padding 분기 확인 — 표 아래 자연스러운 위치) + 4 shift 박스 |

    **범례 마크업 룰 (각 frame 공통 — OQ #3 + OQ #4 LOCKED 반영):**
    ```html
    <div style="display:flex;gap:14px;padding:10px 0 28px;flex-wrap:wrap;justify-content:center;">
      <!-- shift 당직 (빨강 #ef4444) — w-6 h-6 (24px 안전, w-8=48 함정 피함) + rounded-[5px] + border-[1.5px] -->
      <div style="display:flex;align-items:center;gap:5px;">
        <!-- shift box: OQ #4 LOCKED hex+22 알파 인라인 / OQ #3 LOCKED text-label font-extrabold leading-none -->
        <div class="w-6 h-6 rounded-[5px] border-[1.5px] flex items-center justify-center text-label font-extrabold leading-none" style="background:#ef444422;border-color:#ef4444;color:#ef4444;">
          당
        </div>
        <!-- 라벨: OQ #3 LOCKED text-caption leading-none -->
        <span class="text-caption leading-none text-text-secondary">당직</span>
      </div>
      <!-- shift 비번 (파랑 #3b82f6) -->
      <div style="display:flex;align-items:center;gap:5px;">
        <div class="w-6 h-6 rounded-[5px] border-[1.5px] flex items-center justify-center text-label font-extrabold leading-none" style="background:#3b82f622;border-color:#3b82f6;color:#3b82f6;">
          비
        </div>
        <span class="text-caption leading-none text-text-secondary">비번</span>
      </div>
      <!-- shift 주간 (노랑 #f59e0b) -->
      <div style="display:flex;align-items:center;gap:5px;">
        <div class="w-6 h-6 rounded-[5px] border-[1.5px] flex items-center justify-center text-label font-extrabold leading-none" style="background:#f59e0b22;border-color:#f59e0b;color:#f59e0b;">
          주
        </div>
        <span class="text-caption leading-none text-text-secondary">주간</span>
      </div>
      <!-- shift 휴무 (회색 #6b7280) -->
      <div style="display:flex;align-items:center;gap:5px;">
        <div class="w-6 h-6 rounded-[5px] border-[1.5px] flex items-center justify-center text-label font-extrabold leading-none" style="background:#6b728022;border-color:#6b7280;color:#6b7280;">
          휴
        </div>
        <span class="text-caption leading-none text-text-secondary">휴무</span>
      </div>
    </div>
    ```

    **SHIFT_LABEL 4종 verbatim (WorkShiftPage.tsx line 9 source):**
    ```
    '당': '당직'
    '비': '비번'
    '주': '주간'
    '휴': '휴무'
    ```
    한글 라벨 4종 sketch 안에 모두 등장 (memory `feedback_sketch_realistic_data` 카피 verbatim).

    **모바일/데스크톱 padding 분기 정리:**
    - 16-workshift.md 섹션 4 + W1 §1.1 확인: 범례 영역 padding 분기는 명시 없음 (외곽 `padding:'10px 0 28px'` 인라인 유지)
    - 데스크톱은 표 영역 외곽 `paddingTop:12vh` (line 129) 안에서 범례가 자연스럽게 배치되므로 범례 자체 padding 은 모바일/데스크톱 공통

    **flex-wrap 의도:**
    - **이 영역은 flex-wrap:wrap 의도된 디자인** (line 213 verbatim) — 4 박스가 좁은 폭에서 줄바꿈 OK
    - 표 영역의 `overflow-x:auto` (W3) 와는 다른 패턴 — 혼동 금지 (memory `feedback_dashboard_horizontal_scroll` 룰은 dashboard 월간 도넛 한정, 범례는 flex-wrap 의도)

    **rules 박스:**
    - LOCKED 결정: OQ #3 (범례 박스 글자 13 text-label font-extrabold leading-none / 라벨 12 text-caption leading-none) + OQ #4 (SHIFT_COLOR 4 hex + hex+22 알파 인라인 유지) verbatim 인용 1 fence
    - 토큰 매핑: 컨테이너 `flex flex-wrap gap-[14px] justify-center` 인라인 / shift box `w-6 h-6 rounded-[5px] border-[1.5px]` (24px = w-6 안전, w-8=48 함정 피함, memory `feedback_tailwind_w8_h8_is_48px`) / 인라인 hex (background / border-color / color) 모두 SHIFT_COLOR 4종 verbatim
    - flex-wrap 의도 명시: "범례는 flex-wrap 의도된 디자인 (line 213 verbatim) — dashboard 월간 도넛 룰과 별개 패턴"

    **commit 메시지:**
    `docs(quick-260521-t12): redesign/16-workshift W4 legend sketch (4 shift box w-6 h-6 + SHIFT_COLOR 4 hex+22 알파 + SHIFT_LABEL 한글 라벨)`

    **commit 명령:**
    `git add cha-bio-safety/docs/redesign-context/16-workshift/sketch-wave-4-legend.html && git commit -m "docs(quick-260521-t12): redesign/16-workshift W4 legend sketch (4 shift box w-6 h-6 + SHIFT_COLOR 4 hex+22 알파 + SHIFT_LABEL 한글 라벨)"`
  </action>
  <verify>
    <automated>
test -f cha-bio-safety/docs/redesign-context/16-workshift/sketch-wave-4-legend.html \
  && wc -l cha-bio-safety/docs/redesign-context/16-workshift/sketch-wave-4-legend.html | awk '{ if ($1 < 150) { print "FAIL: too short"; exit 1 } else print "PASS lines:", $1 }' \
  && grep -c 'data-theme="dark"' cha-bio-safety/docs/redesign-context/16-workshift/sketch-wave-4-legend.html | awk '{ if ($1 < 2) { print "FAIL: dark frame <2"; exit 1 } else print "PASS dark frame:", $1 }' \
  && grep -c 'data-theme="light"' cha-bio-safety/docs/redesign-context/16-workshift/sketch-wave-4-legend.html | awk '{ if ($1 < 1) { print "FAIL: light frame <1"; exit 1 } else print "PASS light frame:", $1 }' \
  && grep -cE '\bw-6\b' cha-bio-safety/docs/redesign-context/16-workshift/sketch-wave-4-legend.html | awk '{ if ($1 < 4) { print "FAIL: w-6 (24px shift box) <4 (4 box per frame x 3 frame, ≥12 권장)"; exit 1 } else print "PASS w-6:", $1 }' \
  && grep -c '#ef4444' cha-bio-safety/docs/redesign-context/16-workshift/sketch-wave-4-legend.html | awk '{ if ($1 < 3) { print "FAIL: 당직 #ef4444 <3 (3 frame x 1)"; exit 1 } else print "PASS 당직 hex:", $1 }' \
  && grep -c '#3b82f6' cha-bio-safety/docs/redesign-context/16-workshift/sketch-wave-4-legend.html | awk '{ if ($1 < 3) { print "FAIL: 비번 #3b82f6 <3"; exit 1 } else print "PASS 비번 hex:", $1 }' \
  && grep -c '#f59e0b' cha-bio-safety/docs/redesign-context/16-workshift/sketch-wave-4-legend.html | awk '{ if ($1 < 3) { print "FAIL: 주간 #f59e0b <3"; exit 1 } else print "PASS 주간 hex:", $1 }' \
  && grep -c '#6b7280' cha-bio-safety/docs/redesign-context/16-workshift/sketch-wave-4-legend.html | awk '{ if ($1 < 3) { print "FAIL: 휴무 #6b7280 <3"; exit 1 } else print "PASS 휴무 hex:", $1 }' \
  && grep -cE '#ef444422|#3b82f622|#f59e0b22|#6b728022' cha-bio-safety/docs/redesign-context/16-workshift/sketch-wave-4-legend.html | awk '{ if ($1 < 4) { print "FAIL: hex+22 알파 (4 색) <4 (OQ #4 LOCKED)"; exit 1 } else print "PASS hex+22:", $1 }' \
  && grep -c '당직' cha-bio-safety/docs/redesign-context/16-workshift/sketch-wave-4-legend.html | awk '{ if ($1 < 3) { print "FAIL: SHIFT_LABEL 당직 <3"; exit 1 } else print "PASS 당직:", $1 }' \
  && grep -c '비번' cha-bio-safety/docs/redesign-context/16-workshift/sketch-wave-4-legend.html | awk '{ if ($1 < 3) { print "FAIL: SHIFT_LABEL 비번 <3"; exit 1 } else print "PASS 비번:", $1 }' \
  && grep -c '주간' cha-bio-safety/docs/redesign-context/16-workshift/sketch-wave-4-legend.html | awk '{ if ($1 < 3) { print "FAIL: SHIFT_LABEL 주간 <3"; exit 1 } else print "PASS 주간:", $1 }' \
  && grep -c '휴무' cha-bio-safety/docs/redesign-context/16-workshift/sketch-wave-4-legend.html | awk '{ if ($1 < 3) { print "FAIL: SHIFT_LABEL 휴무 <3"; exit 1 } else print "PASS 휴무:", $1 }' \
  && grep -c 'text-label font-extrabold leading-none' cha-bio-safety/docs/redesign-context/16-workshift/sketch-wave-4-legend.html | awk '{ if ($1 < 4) { print "FAIL: shift box class <4 (OQ #3 LOCKED)"; exit 1 } else print "PASS shift box class:", $1 }' \
  && grep -c 'text-caption leading-none' cha-bio-safety/docs/redesign-context/16-workshift/sketch-wave-4-legend.html | awk '{ if ($1 < 4) { print "FAIL: 라벨 class <4"; exit 1 } else print "PASS 라벨 class:", $1 }' \
  && grep -c 'rounded-\[5px\]' cha-bio-safety/docs/redesign-context/16-workshift/sketch-wave-4-legend.html | awk '{ if ($1 < 4) { print "FAIL: rounded-[5px] <4"; exit 1 } else print "PASS rounded-[5px]:", $1 }' \
  && grep -c 'border-\[1.5px\]' cha-bio-safety/docs/redesign-context/16-workshift/sketch-wave-4-legend.html | awk '{ if ($1 < 4) { print "FAIL: border-[1.5px] <4"; exit 1 } else print "PASS border-[1.5px]:", $1 }' \
  && grep -cE 'linear-gradient' cha-bio-safety/docs/redesign-context/16-workshift/sketch-wave-4-legend.html | awk '{ if ($1 > 0) { print "FAIL: linear-gradient found"; exit 1 } else print "PASS no gradient" }' \
  && grep -cE '(text|bg|border)-status-(safe|fire|warning|danger|caution)' cha-bio-safety/docs/redesign-context/16-workshift/sketch-wave-4-legend.html | awk '{ if ($1 > 0) { print "FAIL: status- prefix found"; exit 1 } else print "PASS no status- prefix" }' \
  && grep -v '^<!--' cha-bio-safety/docs/redesign-context/16-workshift/sketch-wave-4-legend.html | grep -cE 'font-size:\s*(9|10|11)[^0-9]|fontSize:\s*(9|10|11)[^0-9]' | awk '{ if ($1 > 0) { print "FAIL: 9/10/11 px found"; exit 1 } else print "PASS no 9-11px" }' \
  && grep -cE '\bw-8\b|\bh-8\b' cha-bio-safety/docs/redesign-context/16-workshift/sketch-wave-4-legend.html | awk '{ if ($1 > 0) { print "FAIL: w-8/h-8 found"; exit 1 } else print "PASS no w-8 h-8" }' \
  && grep -cE 'OQ #3|OQ #4' cha-bio-safety/docs/redesign-context/16-workshift/sketch-wave-4-legend.html | awk '{ if ($1 < 2) { print "FAIL: OQ #3/#4 citation <2"; exit 1 } else print "PASS OQ citations:", $1 }' \
  && git log --oneline -1 | grep -q '260521-t12.*W4' && echo "PASS commit W4"
    </automated>
  </verify>
  <done>
    - sketch-wave-4-legend.html 파일 존재 + ≥150 lines (≥200 권장)
    - data-theme="dark" ≥2 / data-theme="light" ≥1 (총 3 frame)
    - w-6 (24px shift box) ≥4 (4 box per frame, 3 frame = ≥12 권장)
    - SHIFT_COLOR 4 hex (#ef4444 / #3b82f6 / #f59e0b / #6b7280) 각 ≥3 (3 frame)
    - hex+22 알파 표현 (#ef444422 등) 4 색 모두 ≥1 (총 ≥4)
    - SHIFT_LABEL 4종 (당직/비번/주간/휴무) 각 ≥3 (3 frame)
    - `text-label font-extrabold leading-none` (shift box) ≥4
    - `text-caption leading-none` (라벨) ≥4
    - rounded-[5px] ≥4 / border-[1.5px] ≥4
    - linear-gradient 0건 / status- prefix 0건 / 9·10·11px fontSize 0건 / w-8 h-8 0건
    - OQ #3 + OQ #4 인용 ≥2건
    - atomic commit `docs(quick-260521-t12): ... W4 legend sketch ...` 1개
  </done>
</task>

<task type="auto">
  <name>Task 4 (W5): TSX 변환 verify checklist markdown 생성 (12 섹션) + WorkShiftPage.tsx + shiftCalc.ts + generateExcel.ts 변경 0 최종 확인</name>
  <files>cha-bio-safety/docs/redesign-context/16-workshift/wave-5-tsx-conversion-checklist.md</files>
  <action>
    27-login/wave-5-tsx-conversion-checklist.md 패턴 mirror 로 새 markdown 파일 작성. **sketch 가 아닌 TSX 변환 wave 진입을 위한 verify checklist**. W2~W4 sketch 의 모든 Tailwind class / CSS 토큰 / OQ LOCKED 결정 / negative gate / positive gate / build gate / 자체 verify 명령을 **12 섹션**으로 박제. 27-login 의 8 섹션보다 4 섹션 더 (§9 Tailwind cheatsheet / §10 비즈 보존 체크박스 / §11 메모리 룰 inline 12+ / §12 다음 단계 = 27-login 의 SUMMARY 일부 + W1 §5 메모리 룰 mirror).

    **frontmatter:**
    ```yaml
    ---
    title: "redesign/16-workshift — wave 5 (TSX conversion checklist)"
    status: pending  # TSX 변환 wave 시작 전 사용자 컨펌 후 in-progress
    created: 2026-05-21
    quick_id: 260521-t12
    branch: redesign/16-workshift
    source_tsx: cha-bio-safety/src/pages/WorkShiftPage.tsx
    source_tsx_lines: 226
    source_utils:
      - cha-bio-safety/src/utils/shiftCalc.ts (SHIFT_COLOR / DOW_KO / RawShift / getMonthlySchedule export, 변경 0)
      - cha-bio-safety/src/utils/generateExcel.ts (generateShiftExcel export, sketch 영역 아님, 변경 0)
    sketch_sources:
      - cha-bio-safety/docs/redesign-context/16-workshift/sketch-wave-2-header-select.html
      - cha-bio-safety/docs/redesign-context/16-workshift/sketch-wave-3-shift-table.html
      - cha-bio-safety/docs/redesign-context/16-workshift/sketch-wave-4-legend.html
    mirror_of: cha-bio-safety/docs/redesign-context/27-login/wave-5-tsx-conversion-checklist.md (260521-f01)
    ---
    ```

    **§1. 변환 범위 + 산출 파일**
    - 단일 파일 in-place 수정: `cha-bio-safety/src/pages/WorkShiftPage.tsx` (226 lines → 약 230~260 lines 예상, Tailwind v0.1.1 className 으로 인라인 style 치환 + Lucide ChevronLeft import 1줄 추가)
    - W2 (헤더 + 년·월 select) + W3 (표 영역 — 이름 열 + 날짜 열 + today + 공휴일·주말 + shift 4종) + W4 (범례 4 shift 박스 + 라벨) 3 sketch 결정 → className 1:1 매핑
    - **components.css 신규 생성 X** (페이지 로컬 인라인 토큰 유지)
    - **shiftCalc.ts + generateExcel.ts 변경 0** (import 시그니처 보존)
    - 비즈 로직 0 diff (state/handler/effect/hook 모두 보존, §10 체크박스)

    **§2. 보존 (WorkShiftPage.tsx 비즈 로직 100% 보존 목록 18+ row)**

    | line | 항목 | 보존 방식 |
    |---|---|---|
    | 1~7 | import 7개 (useState/useRef/useEffect / useNavigate / useQuery / shiftCalc / RawShift / useStaffList / useIsDesktop) | 그대로 + lucide-react `ChevronLeft` 1줄 추가 |
    | 9 | `SHIFT_LABEL: Record<RawShift, string>` 4종 | 그대로 ('당직'/'비번'/'주간'/'휴무') |
    | 10~11 | `HDR_H = 52` / `ROW_H = 46` 상수 | 그대로 (자동 스크롤 vh 영향) |
    | 14~16 | useNavigate / useIsDesktop / today 변수 | 그대로 |
    | 17~19 | useState 3종 (year / month / dlLoading) | 그대로 |
    | 20~21 | useRef 2종 (scrollRef / todayRef) | 그대로 |
    | 23~40 | useQuery `['holidays-dates']` + fetch hyunbin.page + try/catch → [] | 그대로 (memory `feedback_korean_holidays_library_gap`) |
    | 42~48 | useStaffList + STAFF_ORDER 4명 정렬 (석현민/김병조/윤종엽/박보융) | 그대로 |
    | 49 | getMonthlySchedule(year, month, staffForCalc) → { daysInMonth, staffRows } | 그대로 |
    | 51~52 | `isToday(d)` helper | 그대로 |
    | 54~60 | `isRed(d)` helper (주말 + 공휴일) | 그대로 |
    | 63~68 | useEffect 자동 가운데 스크롤 + requestAnimationFrame | 그대로 |
    | 70~82 | `handleExcel` dynamic import + generateShiftExcel + toast.error | 그대로 |
    | 96 | `!isDesktop &&` 모바일 only back button 분기 | 그대로 (OQ #2 LOCKED) |
    | 97 | `navigate(-1)` onClick | 그대로 |
    | 105 | `handleExcel` onClick | 그대로 |
    | 106 | `disabled={dlLoading}` | 그대로 |
    | 109 | `{dlLoading ? '생성중...' : '엑셀 저장'}` 카피 분기 | 그대로 |
    | 115, 118 | select onChange `setYear(Number(e.target.value))` / `setMonth(...)` | 그대로 |
    | 116, 119 | select 옵션 map `[2025,2026,2027].map(y => ...)` / `Array.from({length:12},(_,i) => i+1).map(...)` | 그대로 |
    | 144~152 | `staffRows.map(s => ...)` 이름 열 row | 그대로 |
    | 161~182 | day cell map `Array.from({length:daysInMonth},(_,i) => ...)` + tdy `todayRef` 분기 | 그대로 |
    | 169 | `ref={tdy ? todayRef : undefined}` | 그대로 |
    | 188~205 | shift cell map `s.shifts.map((sh, i) => ...)` + tdy 분기 + SHIFT_COLOR 인라인 | 그대로 (OQ #4 LOCKED) |
    | 214~219 | 범례 4 shift map `(['당','비','주','휴'] as RawShift[]).map(sh => ...)` | 그대로 |

    **§3. 변환 매핑 (영역별 className/토큰/폰트 변환 표 — W2/W3/W4 sketch verbatim 인용)**

    **§3.1 영역 2 — 헤더 (line 88~111, W2 sketch 출처)**

    | 현재 (인라인 style) | 변환 후 (className + 인라인) | sketch 출처 |
    |---|---|---|
    | `<header style={{ background:'var(--bg2)', borderBottom:'1px solid var(--bd)', ...isDesktop?{...}:{...} }}>` | `<header class="bg-surface-raised border-b border-border-default flex items-center" style={{ flexShrink:0, ...isDesktop?{height:54,padding:'0 20px',gap:10}:{padding:'8px 12px 9px',gap:8} }}>` | W2 frame 1, 3 |
    | back button (모바일 only, line 96~102) width:34, height:34, borderRadius:8, background:'var(--bg3)', border 1px var(--bd) + 인라인 SVG | `class="w-[34px] h-[34px] rounded-sm bg-surface-sunken border border-border-default flex items-center justify-center"` + `<ChevronLeft size={15} className="text-text-secondary" />` (OQ #2 LOCKED Lucide 교체, lucide-react import 추가) | W2 frame 1, 2 |
    | 타이틀 fontSize: isDesktop ? 16 : 14, fontWeight:700, color:'var(--t1)' | `class="text-body font-bold text-text-primary"` style={{ flex:1 }} (OQ #3 LOCKED 모바일 14→16 격상) | W2 모든 frame |
    | 엑셀 버튼 height:34, padding:'0 14px', borderRadius:8, background:'var(--acl)', color:'#fff', fontSize:12, fontWeight:600, opacity: dlLoading ? 0.6 : 1 | `class={\`bg-safe-bar text-text-on-accent text-caption font-bold leading-none rounded-sm border-0 \${dlLoading?'opacity-60 cursor-not-allowed':'cursor-pointer'}\`}` style={{ height:34, padding:'0 14px' }} (OQ #1 LOCKED solid + OQ #3 LOCKED 12 text-caption leading-none) | W2 모든 frame |

    **§3.2 영역 3 — 년·월 select (line 113~121, W2 sketch 출처)**

    | 현재 | 변환 후 | sketch |
    |---|---|---|
    | 컨테이너 background:'var(--bg2)', borderBottom 1px var(--bd), padding:'10px 12px', gap:8, flexShrink:0 | `class="bg-surface-raised border-b border-border-default flex"` style={{ gap:8, padding:'10px 12px', flexShrink:0 }} | W2 모든 frame |
    | select padding:'7px 10px', borderRadius:9, background:'var(--bg3)', border 1px var(--bd2), color:'var(--t1)', fontSize:13, outline:none | `class="bg-surface-sunken border border-border-strong text-text-primary text-label rounded-[9px]"` style={{ padding:'7px 10px', outline:'none' }} (OQ #3 LOCKED 13 text-label) | W2 |

    **§3.3 영역 4 — 표 영역 (line 123~210, W3 sketch 출처)**

    | 현재 | 변환 후 | sketch |
    |---|---|---|
    | 외곽 flex:1, overflow:auto, display flex column items-center, paddingTop: isDesktop ? '12vh' : 0 | 인라인 유지 (style 그대로) — Tailwind 변환 비용 vs 인라인 1줄 | W3 frame 4 |
    | 내곽 inline-flex column, padding: isDesktop ? '0 32px' : '16px 24px' | 인라인 유지 | W3 |
    | 이름 헤더 height:HDR_H, width:82, padding:'0 10px', border 1px var(--bd), background:'var(--bg3)', color:'var(--t2)', fontSize:12, fontWeight:700, whiteSpace:nowrap | `class="bg-surface-sunken border border-border-default text-text-secondary text-caption font-bold leading-none"` style={{ height:HDR_H, width:82, padding:'0 10px', whiteSpace:'nowrap' }} (OQ #3 LOCKED) | W3 |
    | staff name td bg:var(--bg2), border 1px var(--bd) | `class="bg-surface-raised border border-border-default"` style={{ height:ROW_H, padding:'0 10px', whiteSpace:'nowrap' }} | W3 |
    | staff name div fontSize:14, fontWeight:700, var(--t1) | `class="text-body-sm font-bold text-text-primary"` (OQ #3 LOCKED 14→text-body-sm) | W3 |
    | staff title div fontSize:10, var(--t3), marginTop:2 | `class="text-caption leading-none text-text-tertiary"` style={{ marginTop:2 }} (OQ #3 LOCKED 10→text-caption) | W3 |
    | day cell normal: border 1px var(--bd), background:'var(--bg3)', color:'var(--t2)' | `class="bg-surface-sunken border border-border-default text-text-secondary"` (red 면 text-danger, today 면 border-2 border-accent + 인라인 bg) | W3 |
    | day cell today: border '2px solid var(--acl)', background:'rgba(59,130,246,0.15)' | `class="border-2 border-accent"` style={{ background:'rgba(59,130,246,0.15)' }} (OQ #5 LOCKED border-accent + 인라인 bg 유지) | W3 |
    | day cell red (공휴일·주말): color:'#ef4444' | `class="text-danger"` (OQ #5 LOCKED, tokens.css 검증 후 / 불일치 시 `text-[#ef4444]` fallback) | W3 |
    | d fontSize:13, fontWeight:700 | `class="text-label font-bold leading-none"` (OQ #3 LOCKED 13→text-label) | W3 |
    | DOW_KO fontSize:10 | `class="text-caption leading-none"` style={{ marginTop:2 }} (OQ #3 LOCKED 10→text-caption) | W3 |
    | shift cell border tdy ? '2px solid var(--acl)' : '1px solid var(--bd)' + fontSize:15, fontWeight:700 + color:SHIFT_COLOR[sh] + background:SHIFT_COLOR[sh]+'22' | `class={tdy ? 'border-2 border-accent text-body font-bold' : 'border border-border-default text-body font-bold'}` style={{ height:ROW_H, minWidth:40, padding:'0 2px', textAlign:'center', color:SHIFT_COLOR[sh], background:SHIFT_COLOR[sh]+'22' }} (OQ #3 LOCKED 15→text-body 16 셀 가독성 우선 + OQ #4 LOCKED 인라인 hex 유지) | W3 |

    **§3.4 영역 5 — 범례 (line 212~220, W4 sketch 출처)**

    | 현재 | 변환 후 | sketch |
    |---|---|---|
    | 컨테이너 display:flex, gap:14, padding:'10px 0 28px', flexWrap:wrap, justifyContent:center | `class="flex flex-wrap justify-center"` style={{ gap:14, padding:'10px 0 28px' }} | W4 |
    | shift row display:flex, alignItems:center, gap:5, fontSize:12 | `class="flex items-center"` style={{ gap:5 }} (fontSize 는 box/span 분리 적용) | W4 |
    | shift box width:24, height:24, borderRadius:5, background:SHIFT_COLOR[sh]+'22', border 1.5px solid SHIFT_COLOR[sh], fontWeight:800, fontSize:13, color:SHIFT_COLOR[sh] | `class="w-6 h-6 rounded-[5px] border-[1.5px] flex items-center justify-center text-label font-extrabold leading-none"` style={{ background:SHIFT_COLOR[sh]+'22', borderColor:SHIFT_COLOR[sh], color:SHIFT_COLOR[sh] }} (OQ #3 LOCKED 13→text-label font-extrabold leading-none + OQ #4 LOCKED 인라인 hex 유지 + memory `feedback_tailwind_w8_h8_is_48px` w-6=24 안전) | W4 |
    | 라벨 fontSize:12, color:'var(--t2)' | `class="text-caption leading-none text-text-secondary"` (OQ #3 LOCKED 12 유지 + leading-none) | W4 |

    **§4. OQ LOCKED 5건 변환 결과 반영 (W1 §7 verbatim + 코드 적용 line 범위)**

    ```
    OQ #1 (엑셀 저장 버튼 bg-safe-bar solid):
      - line 107: background:'var(--acl)' → className "bg-safe-bar text-text-on-accent"
      - line 107: opacity: dlLoading ? 0.6 : 1 → className 분기 "opacity-60 cursor-not-allowed" / "cursor-pointer"
      - 영향 line: 104~110

    OQ #2 (헤더 raised + 모바일 only back button + Lucide ChevronLeft):
      - line 93: background:'var(--bg2)' → className "bg-surface-raised"
      - line 96: !isDesktop && (모바일 only 분기) 그대로
      - line 97: width:34, height:34, borderRadius:8, background:'var(--bg3)', border 1px var(--bd) → className "w-[34px] h-[34px] rounded-sm bg-surface-sunken border border-border-default"
      - line 98~100: 인라인 SVG `<svg width={15} height={15} ... d="M15 19l-7-7 7-7"/>` → `<ChevronLeft size={15} className="text-text-secondary" />` (line 1 또는 7 다음 lucide-react ChevronLeft import 추가)

    OQ #3 (폰트 격상 verbatim):
      - line 103 타이틀: fontSize: isDesktop ? 16 : 14 → className "text-body font-bold" (모바일 14→16 격상)
      - line 107 엑셀 버튼: fontSize:12 → className "text-caption font-bold leading-none"
      - line 115, 118 select: fontSize:13 → className "text-label"
      - line 138 이름 헤더: fontSize:12 → className "text-caption font-bold leading-none"
      - line 147 staff name: fontSize:14 → className "text-body-sm font-bold"
      - line 148 staff title: fontSize:10 → className "text-caption leading-none"
      - line 178 d: fontSize:13, fontWeight:700 → className "text-label font-bold leading-none"
      - line 179 DOW_KO: fontSize:10 → className "text-caption leading-none"
      - line 197 shift cell: fontSize:15, fontWeight:700 → className "text-body font-bold" (15→16 격상)
      - line 216 범례 box: fontSize:13, fontWeight:800 → className "text-label font-extrabold leading-none"
      - line 217 범례 라벨: fontSize:12 → className "text-caption leading-none"

    OQ #4 (SHIFT_COLOR hex+22 알파 인라인 유지):
      - line 198 shift cell: color:SHIFT_COLOR[sh], background:SHIFT_COLOR[sh]+'22' 인라인 그대로 유지
      - line 216 범례 box: background:SHIFT_COLOR[sh]+'22', border:`1.5px solid ${SHIFT_COLOR[sh]}`, color:SHIFT_COLOR[sh] 인라인 그대로 유지
      - shiftCalc.ts SHIFT_COLOR 상수 1 byte 변경 X

    OQ #5 (today border-accent + 공휴일·주말 text-danger):
      - line 172, 196 today border: '2px solid var(--acl)' → className "border-2 border-accent"
      - line 173, NOT-today border: '1px solid var(--bd)' → className "border border-border-default"
      - line 173 today bg: 'rgba(59,130,246,0.15)' 인라인 유지 (신규 토큰 정의 비용 회피)
      - line 174 red (공휴일·주말): color:'#ef4444' → className "text-danger" (tokens.css `--status-danger` hex 일치 검증 후, 불일치 시 `text-[#ef4444]` fallback)
      - line 174 not-red: color:'var(--t2)' → className "text-text-secondary"
    ```

    **§5. negative gate (TSX 변환 후 WorkShiftPage.tsx 가 통과해야 할 grep gate)**

    ```bash
    # 1. 이모지 0건 (sketch 본문 + TSX 변환 결과 모두)
    grep -cE '[😀-🛿🤀-🧿🩰-🩴✅✊👍👎❌⛔🔥💥💧⚠️📛]' cha-bio-safety/src/pages/WorkShiftPage.tsx == 0

    # 2. linear-gradient 0건 (OQ #1 LOCKED)
    grep -c 'linear-gradient' cha-bio-safety/src/pages/WorkShiftPage.tsx == 0

    # 3. 9·10·11px 0건 (OQ #3 LOCKED 격상 결과, fontSize/font-size 양쪽)
    grep -v '^\s*//' cha-bio-safety/src/pages/WorkShiftPage.tsx | grep -cE 'fontSize:\s*(9|10|11)[^0-9]|font-size:\s*(9|10|11)[^0-9]' == 0

    # 4. status- prefix 0건 (memory feedback_tailwind_token_class_pattern)
    grep -cE '(text|bg|border)-status-(safe|fire|warning|danger|caution)' cha-bio-safety/src/pages/WorkShiftPage.tsx == 0

    # 5. w-8 / h-8 0건 (w-8=48 함정, memory feedback_tailwind_w8_h8_is_48px)
    grep -cE '\bw-8\b|\bh-8\b' cha-bio-safety/src/pages/WorkShiftPage.tsx == 0

    # 6. 옛 alias 토큰 0건 (var(--bg)/var(--bg2)/var(--bg3)/var(--bd)/var(--bd2)/var(--t1)/var(--t2)/var(--t3)/var(--acl)/var(--accent))
    grep -cE 'var\(--(bg|bg2|bg3|bd|bd2|t1|t2|t3|acl|accent)\)' cha-bio-safety/src/pages/WorkShiftPage.tsx == 0
    # (단, rgba(59,130,246,0.15) 같은 직접 rgba 는 OQ #5 LOCKED 예외)

    # 7. 비즈 로직 diff 0 (handler/state/effect/hook 보존)
    git diff HEAD~N cha-bio-safety/src/pages/WorkShiftPage.tsx | grep -E '^[+-]\s*(useState|useEffect|useRef|useNavigate|useQuery|useIsDesktop|useStaffList|setYear|setMonth|setDlLoading|handleExcel|getMonthlySchedule|isToday|isRed|STAFF_ORDER|SHIFT_LABEL|HDR_H|ROW_H)' == 0
    # (logic line 의 - 추가 / + 제거 또는 변경이 없어야 함)
    ```

    **§6. positive gate (변환 후 등장해야 할 패턴)**

    ```bash
    # 1. lucide-react import — ChevronLeft 1개 추가 (OQ #2 LOCKED)
    grep -c "from 'lucide-react'" cha-bio-safety/src/pages/WorkShiftPage.tsx >= 1
    grep -c 'ChevronLeft' cha-bio-safety/src/pages/WorkShiftPage.tsx >= 2  # import + 사용

    # 2. v0.1.1 토큰 class 카운트 (≥3 핵심)
    grep -c 'bg-surface-raised' cha-bio-safety/src/pages/WorkShiftPage.tsx >= 3  # 헤더 + 년월 + staff name 셀
    grep -c 'bg-surface-sunken' cha-bio-safety/src/pages/WorkShiftPage.tsx >= 3  # back button + select + 이름 헤더 + day cell
    grep -c 'border-border-default' cha-bio-safety/src/pages/WorkShiftPage.tsx >= 3
    grep -c 'border-border-strong' cha-bio-safety/src/pages/WorkShiftPage.tsx >= 1  # select
    grep -c 'text-text-primary' cha-bio-safety/src/pages/WorkShiftPage.tsx >= 2  # 타이틀 + staff name + select value
    grep -c 'text-text-secondary' cha-bio-safety/src/pages/WorkShiftPage.tsx >= 2  # 이름 헤더 + day cell normal + ChevronLeft
    grep -c 'text-text-tertiary' cha-bio-safety/src/pages/WorkShiftPage.tsx >= 1  # staff title
    grep -c 'bg-safe-bar' cha-bio-safety/src/pages/WorkShiftPage.tsx >= 1  # 엑셀 버튼
    grep -c 'border-accent' cha-bio-safety/src/pages/WorkShiftPage.tsx >= 1  # today border (OQ #5 LOCKED)
    grep -cE 'text-danger|text-\[#ef4444\]' cha-bio-safety/src/pages/WorkShiftPage.tsx >= 1  # 공휴일·주말 (OQ #5 LOCKED)

    # 3. 폰트 토큰 (OQ #3 LOCKED 격상)
    grep -c 'text-caption' cha-bio-safety/src/pages/WorkShiftPage.tsx >= 5  # 엑셀 버튼 + 이름 헤더 + staff title + DOW_KO + 범례 라벨
    grep -c 'text-label' cha-bio-safety/src/pages/WorkShiftPage.tsx >= 3  # select + d + 범례 box
    grep -c 'text-body-sm' cha-bio-safety/src/pages/WorkShiftPage.tsx >= 1  # staff name
    grep -c 'text-body' cha-bio-safety/src/pages/WorkShiftPage.tsx >= 2  # 타이틀 + shift cell

    # 4. 인라인 px 사이즈 (OQ #2 + memory feedback_tailwind_w8_h8_is_48px)
    grep -c 'w-\[34px\]' cha-bio-safety/src/pages/WorkShiftPage.tsx >= 1  # 모바일 back button
    grep -c 'h-\[34px\]' cha-bio-safety/src/pages/WorkShiftPage.tsx >= 1
    grep -cE '\bw-6\b' cha-bio-safety/src/pages/WorkShiftPage.tsx >= 1  # 범례 shift box 24px
    grep -c 'rounded-\[9px\]' cha-bio-safety/src/pages/WorkShiftPage.tsx >= 1  # select
    grep -c 'rounded-\[5px\]' cha-bio-safety/src/pages/WorkShiftPage.tsx >= 1  # 범례 box

    # 5. SHIFT_COLOR 인라인 (OQ #4 LOCKED)
    grep -c "SHIFT_COLOR\[sh\]\+'22'" cha-bio-safety/src/pages/WorkShiftPage.tsx >= 2  # shift cell + 범례 box
    grep -c 'SHIFT_COLOR\[sh\]' cha-bio-safety/src/pages/WorkShiftPage.tsx >= 4  # color + background + borderColor 등

    # 6. 카피 verbatim (memory feedback_sketch_realistic_data)
    grep -c '월간 출근부' cha-bio-safety/src/pages/WorkShiftPage.tsx >= 1
    grep -c '엑셀 저장' cha-bio-safety/src/pages/WorkShiftPage.tsx >= 1
    grep -c '생성중' cha-bio-safety/src/pages/WorkShiftPage.tsx >= 1
    grep -c '이름' cha-bio-safety/src/pages/WorkShiftPage.tsx >= 1
    grep -c 'STAFF_ORDER' cha-bio-safety/src/pages/WorkShiftPage.tsx >= 1
    grep -c '석현민' cha-bio-safety/src/pages/WorkShiftPage.tsx >= 1
    ```

    **§7. build / tsc gate**

    ```bash
    cd cha-bio-safety && npx tsc --noEmit                # 0 error
    cd cha-bio-safety && npm run build                   # exit 0
    # WorkShiftPage chunk size 보고 (Vite output 의 dist/assets/WorkShiftPage-*.js)
    ls -la cha-bio-safety/dist/assets/WorkShiftPage-*.js  # size 확인
    ```

    **§8. 자체 verify 명령 (TSX 변환 wave 진입 시점 + 완료 시점 양쪽 실행)**

    각 gate 의 grep/wc/git 명령을 fence 안에 verbatim 박제.

    ```bash
    # 진입 시점: sketch 3 파일 모두 존재 확인
    ls cha-bio-safety/docs/redesign-context/16-workshift/sketch-wave-{2-header-select,3-shift-table,4-legend}.html

    # 완료 시점: §5 negative + §6 positive + §7 build 모두 PASS
    # tokens.css `--status-danger` hex 검증 (OQ #5 LOCKED)
    grep -E '\-\-status-danger:' cha-bio-safety/docs/redesign-context/16-workshift/tokens.css

    # shiftCalc.ts + generateExcel.ts 변경 0 확인 (TSX 변환 wave 끝나도 유지)
    git diff HEAD -- cha-bio-safety/src/utils/shiftCalc.ts | wc -l == 0
    git diff HEAD -- cha-bio-safety/src/utils/generateExcel.ts | wc -l == 0
    ```

    **§9. Tailwind cheatsheet (v0.1.1 토큰 → utility class 매핑 표)**

    | v0.1.1 토큰 | Tailwind utility | 16-workshift 적용 위치 |
    |---|---|---|
    | `--surface-page` | `bg-surface-page` | 페이지 외곽 (사용 없음) |
    | `--surface-raised` | `bg-surface-raised` | 헤더 + 년월 + staff name td |
    | `--surface-sunken` | `bg-surface-sunken` | back button + select + 이름 헤더 + day cell normal |
    | `--border-default` | `border-border-default` | 헤더 border-b + 이름 표 + day cell normal |
    | `--border-strong` | `border-border-strong` | select border |
    | `--text-primary` | `text-text-primary` | 타이틀 + staff name + select value |
    | `--text-secondary` | `text-text-secondary` | 이름 헤더 + day cell normal + ChevronLeft |
    | `--text-tertiary` | `text-text-tertiary` | staff title |
    | `--safe-bar` | `bg-safe-bar` | 엑셀 저장 버튼 (OQ #1) |
    | `--accent` | `border-accent` | today border (OQ #5) |
    | `--status-danger` | `text-danger` | 공휴일·주말 글자 (OQ #5, 검증 후) |
    | (radius 8) | `rounded-sm` | back button + 엑셀 버튼 |
    | (radius 9, 토큰 없음) | `rounded-[9px]` | select |
    | (radius 5, 토큰 없음) | `rounded-[5px]` | 범례 box |
    | (W-24, 토큰 spacing) | `w-6 h-6` | 범례 shift box (24×24 안전) |
    | (W-34, 토큰 없음) | `w-[34px] h-[34px]` | 모바일 back button (w-8=48 함정 회피) |

    **§10. 비즈 보존 체크박스 (TSX 변환 wave 완료 후 직접 체크)**

    - [ ] `useNavigate()` line 14 그대로
    - [ ] `useIsDesktop()` line 15 그대로
    - [ ] `useState` 3종 (year/month/dlLoading) line 17~19 그대로
    - [ ] `useRef` 2종 (scrollRef/todayRef) line 20~21 그대로
    - [ ] `useQuery(['holidays-dates'])` + try/catch → [] line 23~40 그대로
    - [ ] `useStaffList()` + STAFF_ORDER 정렬 line 42~48 그대로
    - [ ] `getMonthlySchedule(year, month, staffForCalc)` line 49 그대로
    - [ ] `isToday(d)` / `isRed(d)` helpers line 51~60 그대로
    - [ ] useEffect 자동 가운데 스크롤 line 63~68 그대로
    - [ ] `handleExcel` dynamic import + generateShiftExcel line 70~82 그대로
    - [ ] `!isDesktop && (back button)` 모바일 분기 line 96 그대로
    - [ ] `navigate(-1)` line 97 그대로
    - [ ] `setYear` / `setMonth` line 115, 118 그대로
    - [ ] select 옵션 verbatim ([2025,2026,2027] + 12개월) line 116, 119 그대로
    - [ ] `staffRows.map` 이름 열 + 날짜 열 + shift 셀 map line 144~205 그대로
    - [ ] 범례 4 shift map (['당','비','주','휴']) line 214 그대로
    - [ ] `shiftCalc.ts` SHIFT_COLOR / DOW_KO / RawShift / getMonthlySchedule export 0 byte 변경
    - [ ] `generateExcel.ts` generateShiftExcel export 0 byte 변경

    **§11. 메모리 룰 inline 인용 (W1 §5 mirror, 12+ 룰)**

    각 룰 작용 케이스 한 줄 — `feedback_*.md` 파일명 + How (16-workshift 컨텍스트).

    1. `feedback_design_sketch_first.md` — spacing/sizing 변경 시에도 sketch 먼저 컨펌. T1~T3 에서 셀 크기 (HDR_H=52 / ROW_H=46 / minWidth:40) 조정 시 sketch 먼저.
    2. `feedback_redesign_sketch_rule_enforcement.md` — SHIFT_COLOR 4 카테고리 색 (당 빨강 / 비 파랑 / 주 노랑 / 휴 회색) 은 status 임계치 색이 아니라 카테고리/duty 색. `bg-status-danger-bg` 같은 status 토큰 사용 금지.
    3. `feedback_sketch_realistic_data.md` — 카피 (월간 출근부 / 엑셀 저장 / 생성중... / SHIFT_LABEL 4종 / STAFF_ORDER 4명 / 이름 / 1월~12월 / 2025~2027년) verbatim. 표시 분기 / 라벨 변경 금지.
    4. `feedback_planner_prompt_sketch_verbatim.md` — TSX 변환 진입 시 W2~W4 sketch 의 모든 Tailwind class / CSS 토큰 grep 으로 추출 → 본 §3 verbatim 인용. today border `'2px solid var(--acl) + rgba(59,130,246,0.15)'` + 공휴일 `#ef4444` + 셀 `SHIFT_COLOR[sh]+'22'` 추측 X — sketch verbatim.
    5. `feedback_tailwind_token_class_pattern.md` — `text-danger` O / `text-status-danger` X (status- prefix 없음). Lucide `<ChevronLeft size={15} />` prop 사용 (className `w-N h-N` 금지).
    6. `feedback_tailwind_w8_h8_is_48px.md` — `w-8 h-8` = 48×48 함정. 모바일 back button 34×34 → `w-[34px] h-[34px]` 인라인. 범례 shift box 24×24 → `w-6 h-6` (24 = w-6 안전).
    7. `feedback_text_caption_leading_none.md` — `text-caption` lh:1.5 가 작은 컨테이너 안에서 시각 패딩 발생. 이름 헤더 (height:52) + day cell DOW_KO (padding:4px 2px) + 범례 box (24×24) + 라벨 모두 `leading-none` 명시.
    8. `feedback_tsx_wave_emoji_dot_gap.md` — sketch 본문 이모지 0건 강제. WorkShiftPage 본문에도 이모지 0건 (현재 잘 지켜짐). dot span 추가 패턴 무관.
    9. `feedback_tsx_wave_stat_card_drift.md` — 16-workshift 에는 Stat Card §6.3 적용 대상 element 없음 (§3.5 미적용 메타 명시). today 분기 매트릭스 / 공휴일·주말 색 분기 / SHIFT_COLOR 4 카테고리 색 variant 는 새 패턴이므로 verbatim 인용.
    10. `feedback_avoid_premature_confirmation.md` — TSX 변환 후 "approved 거의 일치" 자체 판단 금지. 결과 보여주고 사용자 판단.
    11. ★ `feedback_korean_holidays_library_gap.md` (WorkShiftPage 특화) — useQuery `['holidays-dates']` 의 fetch hyunbin.page + try/catch → [] fallback 패턴 보존. `@hyunbinseo/holidays-kr` 등 다른 라이브러리로 교체 금지.
    12. ★ `feedback_dashboard_horizontal_scroll.md` (WorkShiftPage 특화) — 날짜 열 (line 157) `flex:1, overflowX:auto` 가로 스크롤 의도된 디자인. flex-wrap 줄바꿈 금지. 단, 범례 영역 (line 213) 은 의도적 flex-wrap (4 박스 좁은 폭에서 줄바꿈 OK) — 다른 패턴 혼동 금지.

    **§12. 다음 단계**

    1. 본 wave (W5 checklist) 작성 완료 → 4 commit + push.
    2. 사용자 시각 검수 — cbc7119-preview 배포 사이클 1회 (main 머지 후 자동) — sketch 3 HTML 직접 열어 4 frame 시각 확인.
    3. 사용자 컨펌 받으면 **다음 quick task 시작** (`/clear` 권장 — memory `feedback_gsd_workflow_strict`) — TSX 변환 wave 진입.
       - 새 quick id (예: 260522-XX) 생성
       - PLAN: 1 task (W6 — TSX 변환), action = §3 매핑 표 verbatim + §5/§6/§7/§8 verify gate 그대로 적용
       - 산출: WorkShiftPage.tsx 1 파일 in-place 수정 + atomic commit 1개 (`feat(16-workshift): W6 TSX 변환 ...`)
    4. TSX 변환 wave 완료 → 사용자 시각 검수 → main 머지 → cbc7119-preview 배포 → 직원도메인 별도 배포 (메모리 `feedback_deploy_test` 룰 — design 작업은 사용자 명시 컨펌 후만).
    5. 16-workshift 완결 status 메모 (memory `project_redesign_*_status` 패턴).

    **§ 자체 verify (본 W5 checklist 가 통과해야 할 gate)**
    - WorkShiftPage.tsx + shiftCalc.ts + generateExcel.ts 3 파일 변경 0 (`git diff HEAD~4 HEAD -- {3 paths}` empty)
    - 12 섹션 헤더 ≥1 each (§1~§12)
    - verify 명령 fence ≥4 (§5/§6/§7/§8)
    - sketch 출처 frontmatter sketch_sources 3개 (W2/W3/W4) 모두 존재 확인 (test -f)
    - OQ #1~#5 인용 ≥5건
    - 메모리 룰 ≥12 인용

    **commit 메시지:**
    `docs(quick-260521-t12): redesign/16-workshift W5 TSX conversion checklist (12 sections, sketch sources 3 + verify gates + WorkShiftPage 변경 0)`

    **commit 명령:**
    `git add cha-bio-safety/docs/redesign-context/16-workshift/wave-5-tsx-conversion-checklist.md && git commit -m "docs(quick-260521-t12): redesign/16-workshift W5 TSX conversion checklist (12 sections, sketch sources 3 + verify gates + WorkShiftPage 변경 0)"`

    **마지막 단계 (commit 후 3 파일 변경 0 최종 확인):**
    `git diff --name-only HEAD~4 HEAD -- cha-bio-safety/src/pages/WorkShiftPage.tsx cha-bio-safety/src/utils/shiftCalc.ts cha-bio-safety/src/utils/generateExcel.ts` 의 결과가 빈 줄이어야 함 (최근 4 commit 동안 3 파일 모두 변경 0).
  </action>
  <verify>
    <automated>
test -f cha-bio-safety/docs/redesign-context/16-workshift/wave-5-tsx-conversion-checklist.md \
  && wc -l cha-bio-safety/docs/redesign-context/16-workshift/wave-5-tsx-conversion-checklist.md | awk '{ if ($1 < 300) { print "FAIL: too short"; exit 1 } else print "PASS lines:", $1 }' \
  && grep -cE '^# §1[0-2]?\b|^## §1[0-2]?\b|^### §1[0-2]?\b|^\*\*§1[0-2]?\b|^# §[1-9]\b|^## §[1-9]\b|^### §[1-9]\b|^\*\*§[1-9]\b' cha-bio-safety/docs/redesign-context/16-workshift/wave-5-tsx-conversion-checklist.md | awk '{ if ($1 < 12) { print "FAIL: §1~§12 헤더 <12"; exit 1 } else print "PASS sections:", $1 }' \
  && grep -c '^```' cha-bio-safety/docs/redesign-context/16-workshift/wave-5-tsx-conversion-checklist.md | awk '{ if ($1 < 8) { print "FAIL: fence <8 (4 verify section x 2 open/close each)"; exit 1 } else print "PASS fences:", $1 }' \
  && grep -cE 'OQ #1|OQ #2|OQ #3|OQ #4|OQ #5' cha-bio-safety/docs/redesign-context/16-workshift/wave-5-tsx-conversion-checklist.md | awk '{ if ($1 < 5) { print "FAIL: OQ #1~#5 citations <5"; exit 1 } else print "PASS OQ citations:", $1 }' \
  && grep -c 'WorkShiftPage\.tsx' cha-bio-safety/docs/redesign-context/16-workshift/wave-5-tsx-conversion-checklist.md | awk '{ if ($1 < 10) { print "FAIL: WorkShiftPage.tsx ref <10"; exit 1 } else print "PASS WorkShiftPage refs:", $1 }' \
  && grep -c 'shiftCalc\.ts' cha-bio-safety/docs/redesign-context/16-workshift/wave-5-tsx-conversion-checklist.md | awk '{ if ($1 < 2) { print "FAIL: shiftCalc.ts ref <2"; exit 1 } else print "PASS shiftCalc refs:", $1 }' \
  && grep -c 'generateExcel\.ts' cha-bio-safety/docs/redesign-context/16-workshift/wave-5-tsx-conversion-checklist.md | awk '{ if ($1 < 2) { print "FAIL: generateExcel.ts ref <2"; exit 1 } else print "PASS generateExcel refs:", $1 }' \
  && grep -c 'bg-safe-bar' cha-bio-safety/docs/redesign-context/16-workshift/wave-5-tsx-conversion-checklist.md | awk '{ if ($1 < 2) { print "FAIL: bg-safe-bar citation <2"; exit 1 } else print "PASS bg-safe-bar:", $1 }' \
  && grep -c 'border-accent' cha-bio-safety/docs/redesign-context/16-workshift/wave-5-tsx-conversion-checklist.md | awk '{ if ($1 < 1) { print "FAIL: border-accent missing"; exit 1 } else print "PASS border-accent:", $1 }' \
  && grep -cE 'text-danger|text-\[#ef4444\]' cha-bio-safety/docs/redesign-context/16-workshift/wave-5-tsx-conversion-checklist.md | awk '{ if ($1 < 1) { print "FAIL: text-danger missing"; exit 1 } else print "PASS text-danger:", $1 }' \
  && grep -c 'SHIFT_COLOR' cha-bio-safety/docs/redesign-context/16-workshift/wave-5-tsx-conversion-checklist.md | awk '{ if ($1 < 3) { print "FAIL: SHIFT_COLOR ref <3"; exit 1 } else print "PASS SHIFT_COLOR refs:", $1 }' \
  && grep -c 'ChevronLeft' cha-bio-safety/docs/redesign-context/16-workshift/wave-5-tsx-conversion-checklist.md | awk '{ if ($1 < 2) { print "FAIL: ChevronLeft ref <2 (OQ #2 LOCKED)"; exit 1 } else print "PASS ChevronLeft:", $1 }' \
  && grep -c 'sketch-wave-2-header-select' cha-bio-safety/docs/redesign-context/16-workshift/wave-5-tsx-conversion-checklist.md | awk '{ if ($1 < 1) { print "FAIL: sketch W2 ref missing"; exit 1 } else print "PASS W2 ref:", $1 }' \
  && grep -c 'sketch-wave-3-shift-table' cha-bio-safety/docs/redesign-context/16-workshift/wave-5-tsx-conversion-checklist.md | awk '{ if ($1 < 1) { print "FAIL: sketch W3 ref missing"; exit 1 } else print "PASS W3 ref:", $1 }' \
  && grep -c 'sketch-wave-4-legend' cha-bio-safety/docs/redesign-context/16-workshift/wave-5-tsx-conversion-checklist.md | awk '{ if ($1 < 1) { print "FAIL: sketch W4 ref missing"; exit 1 } else print "PASS W4 ref:", $1 }' \
  && grep -oE 'feedback_[a-z_]+' cha-bio-safety/docs/redesign-context/16-workshift/wave-5-tsx-conversion-checklist.md | sort -u | wc -l | awk '{ if ($1 < 12) { print "FAIL: memory rules unique <12"; exit 1 } else print "PASS memory rules:", $1 }' \
  && (git diff --name-only HEAD~4 HEAD -- cha-bio-safety/src/pages/WorkShiftPage.tsx | wc -l | awk '{ if ($1 != 0) { print "FAIL: WorkShiftPage.tsx changed in last 4 commits"; exit 1 } else print "PASS WorkShiftPage.tsx untouched" }') \
  && (git diff --name-only HEAD~4 HEAD -- cha-bio-safety/src/utils/shiftCalc.ts | wc -l | awk '{ if ($1 != 0) { print "FAIL: shiftCalc.ts changed in last 4 commits"; exit 1 } else print "PASS shiftCalc.ts untouched" }') \
  && (git diff --name-only HEAD~4 HEAD -- cha-bio-safety/src/utils/generateExcel.ts | wc -l | awk '{ if ($1 != 0) { print "FAIL: generateExcel.ts changed in last 4 commits"; exit 1 } else print "PASS generateExcel.ts untouched" }') \
  && (ls cha-bio-safety/docs/redesign-context/16-workshift/sketch-wave-{2-header-select,3-shift-table,4-legend}.html 2>/dev/null | wc -l | awk '{ if ($1 != 3) { print "FAIL: 3 sketch files missing"; exit 1 } else print "PASS all 3 sketches present" }') \
  && git log --oneline -1 | grep -q '260521-t12.*W5' && echo "PASS commit W5"
    </automated>
  </verify>
  <done>
    - wave-5-tsx-conversion-checklist.md 파일 존재 + ≥300 lines (≥350 권장)
    - §1~§12 12 섹션 헤더 모두 존재
    - 자체 verify fence ≥8 (§5/§6/§7/§8 각 명령 fence open+close)
    - OQ #1~#5 5건 모두 인용
    - WorkShiftPage.tsx ref ≥10건 / shiftCalc.ts ref ≥2건 / generateExcel.ts ref ≥2건
    - bg-safe-bar ≥2 / border-accent ≥1 / text-danger 또는 text-[#ef4444] ≥1 / SHIFT_COLOR ≥3 / ChevronLeft ≥2 (OQ #2 LOCKED)
    - sketch W2/W3/W4 ref 각 ≥1
    - 메모리 룰 (feedback_*.md) unique ≥12
    - **WorkShiftPage.tsx + shiftCalc.ts + generateExcel.ts 3 파일 모두 변경 0** (git diff HEAD~4 HEAD 결과 empty)
    - 3 sketch HTML 파일 (W2/W3/W4) 모두 존재 확인
    - atomic commit `docs(quick-260521-t12): ... W5 TSX conversion checklist ...` 1개
  </done>
</task>

</tasks>

<verification>
**전체 4 task 종합 verify (executor 가 마지막 task 4 종료 직후 실행):**

```bash
# 1. 4 산출 파일 모두 존재
test -f cha-bio-safety/docs/redesign-context/16-workshift/sketch-wave-2-header-select.html
test -f cha-bio-safety/docs/redesign-context/16-workshift/sketch-wave-3-shift-table.html
test -f cha-bio-safety/docs/redesign-context/16-workshift/sketch-wave-4-legend.html
test -f cha-bio-safety/docs/redesign-context/16-workshift/wave-5-tsx-conversion-checklist.md

# 2. WorkShiftPage.tsx + shiftCalc.ts + generateExcel.ts 변경 0 (가장 중요)
[[ -z "$(git diff --name-only HEAD~4 HEAD -- cha-bio-safety/src/pages/WorkShiftPage.tsx)" ]] || echo "FAIL: WorkShiftPage.tsx changed"
[[ -z "$(git diff --name-only HEAD~4 HEAD -- cha-bio-safety/src/utils/shiftCalc.ts)" ]] || echo "FAIL: shiftCalc.ts changed"
[[ -z "$(git diff --name-only HEAD~4 HEAD -- cha-bio-safety/src/utils/generateExcel.ts)" ]] || echo "FAIL: generateExcel.ts changed"

# 3. 평면 폴더 패턴 — sketch/ 서브폴더 없음
test ! -d cha-bio-safety/docs/redesign-context/16-workshift/sketch/

# 4. 4 atomic commit 확인
git log --oneline HEAD~4..HEAD | grep -c '260521-t12' | awk '{ if ($1 != 4) { print "FAIL: 4 commit 아님"; exit 1 } else print "PASS 4 commits" }'

# 5. 16-workshift/ 외 변경 0 (다른 페이지 영향 없음)
[[ -z "$(git diff --name-only HEAD~4 HEAD -- ':!cha-bio-safety/docs/redesign-context/16-workshift/')" ]] || echo "FAIL: 다른 페이지 변경 있음"
```

전체 PASS 시 후속 단계 (`/clear` + 다음 quick — TSX 변환 wave) 진입 가능.
</verification>

<success_criteria>
- 4 산출 파일 모두 `16-workshift/` 직속 평면 배치로 생성 (sketch/ 서브폴더 없음)
- WorkShiftPage.tsx (226 lines) + shiftCalc.ts + generateExcel.ts **3 파일 모두 변경 0건**
- 4 atomic git commit (T1~T4 각 1개씩, 260521-t12 prefix)
- negative gate 4 산출 파일 모두 통과 (이모지 / linear-gradient / 9·10·11px / status- prefix / w-8 h-8 / 옛 alias 토큰 0건)
- OQ #1~#5 LOCKED 5건 모두 4 파일 안에 verbatim 반영
- 16-workshift/ 외 변경 0 (다른 페이지 영향 없음)
- 사용자가 cbc7119-preview 배포 사이클 1회로 4 sketch HTML 시각 검수 가능
- 후속 quick task (TSX 변환 wave W6) 진입 자격 확보
</success_criteria>

<output>
After completion, create `.planning/quick/260521-t12-redesign-16-workshift-sketch-waves-w2-w5/260521-t12-SUMMARY.md` (27-login f01 SUMMARY 패턴 mirror — frontmatter + 4 task 실행 결과 + verify gate 결과 + 다음 단계).
</output>
