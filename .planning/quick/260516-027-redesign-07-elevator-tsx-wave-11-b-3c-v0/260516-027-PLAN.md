---
phase: quick-260516-027
plan: 01
type: execute
wave: 1
depends_on: [260515-wvq, 260515-rfh, 260515-p3v]
files_modified:
  - cha-bio-safety/src/pages/ElevatorPage.tsx
autonomous: true
requirements:
  - QUICK-260516-027
must_haves:
  truths:
    - "안전관리자 탭 변환 영역 (`tab === 'safety'` IIFE 블록, 현 line 1592~1712, ~120줄) 내 인라인 style 속성 0건 (또는 화이트리스트 dynamic value 만 — gridTemplateColumns/gridColumn 등) + var(--bg2/bd/bd2/t1/t2/t3/bg3/danger/safe/warn/info) 인라인 0건 + 9·10·11px 폰트 0건 + 본문 이모지 0건 (👤📚🏢🛗↕️✓✗ 모두 제거)"
    - "lucide-react import 확장 — Wave 10 기존 18종 (Package/UtensilsCrossed/MoveDiagonal/ChevronLeft/ChevronRight/ChevronUp/ChevronDown/AlertTriangle/Wrench/X/AlertOctagon/ClipboardCheck/FileSearch/CheckCircle2/Camera/Loader2/ClipboardList/Search) + 신규 3종 추가 (User/BookOpen/Building2). ElevatorIcon 은 `../components/ui/icons` 에서 이미 import 중 (line 17) — Wave 11 에서 재import 0건"
    - "이모지/문자 → lucide 매핑: 👤→User (프로필 avatar size=28 + 빈 상태 EmptyState size=36) / 📚→BookOpen (교육 헤더 size=16) / 🏢→Building2 (등록 헤더 size=16) / 🛗→ElevatorIcon (등록 그리드 헤더 1 size=14 — 이미 import) / ↕️→MoveDiagonal (등록 그리드 헤더 2 size=14 — 이미 import) / ✓→CheckCircle2 (등록 chip size=11 — 이미 import) / ✗→X (등록 chip size=11 — 이미 import)"
    - "EmptyState 호출 변환: line 1596 `<EmptyState icon=\"👤\" ...>` → `<EmptyState icon={<User size={36} />} text=\"안전관리자 정보가 없어요\" />` (Wave 9 시그니처 React.ReactNode 활용)"
    - "프로필 카드 (line 1614~1633) — wrapper `bg-surface-raised border border-border-default rounded-xl p-4 flex-shrink-0` / 48×48 avatar `bg-surface-sunken rounded-full flex items-center justify-center` + `<User size={28} className=\"text-text-secondary\" />` / 이름 `text-[16px] font-bold text-text-primary` (16px 토큰 미정의 arbitrary value 허용) / 부제 `text-caption text-text-tertiary mt-0.5` (11→12 격상) / 2-col grid `grid grid-cols-2 gap-2 mt-3` + cell `bg-surface-sunken rounded-lg px-2.5 py-2` + 라벨 `text-caption text-text-tertiary mb-0.5` + 값 `text-body-sm font-bold text-text-primary`"
    - "교육 현황 카드 (line 1635~1669) — wrapper `bg-surface-raised border border-border-default rounded-xl p-4 flex-shrink-0` / 헤더 `text-body-sm font-bold text-text-primary mb-3 flex items-center gap-1.5` + `<BookOpen size={16} className=\"text-text-secondary\" />` + '교육 현황' / 보수(재) row `bg-surface-sunken rounded-xl px-3 py-2.5 mb-2` + 라벨·D-day chip 1줄 (`text-body-sm font-bold` + chip `text-caption font-bold ${ddayClass.text} ${ddayClass.bg} px-2 py-0.5 rounded-md`) + 유효기간·다음 마감 보조 텍스트 (`text-caption text-text-tertiary`, 11/10 → 12 격상) / 신규 row 동일 패턴 — daysLeft<0 완료 chip (`text-safe bg-safe-bg`) / >=0 D-N chip (`text-warning bg-warning-bg`)"
    - "공단 등록 현황 카드 (line 1671~1708) — wrapper `bg-surface-raised border border-border-default rounded-xl p-4 flex-shrink-0` / 헤더 `text-body-sm font-bold text-text-primary mb-2 flex items-center gap-1.5` + `<Building2 size={16} className=\"text-text-secondary\" />` + '공단 등록 현황' / 등록 수 텍스트 `text-body-sm text-text-secondary mb-2.5` + 등록 수 inline `text-safe font-bold` + 미등록 inline `text-warning ml-1.5` / 호기 그리드 — Tailwind arbitrary `grid grid-cols-[repeat(4,1fr)_6px_repeat(2,1fr)] gap-1 items-center` (또는 인라인 gridTemplateColumns 화이트리스트 fallback) + 헤더 행 (ElevatorIcon size=14 + '엘리베이터' col-span-4 또는 gridColumn:'1/5' / 6px sep / MoveDiagonal size=14 + '에스컬레이터' col-span-2 또는 gridColumn:'6/8', 헤더 텍스트 `text-caption font-bold text-text-tertiary` 9→12 격상) + chip 함수 (`text-caption font-semibold px-2 py-0.5 rounded-md text-center inline-flex items-center justify-center gap-0.5` + isReg ? `bg-safe-bg text-safe` : `bg-warning-bg text-warning` + 'EV{n}'/'ES{n}' 라벨 보존 + CheckCircle2 size=11(isReg) 또는 X size=11(!isReg))"
    - "ddayClass 헬퍼 (또는 fmtDday 색 반환만 교체) — { text, bg } className 객체 반환. 4 분기 키워드 절대 변경 X: <0=danger / ≤60=warning / ≤365=info / >365=safe. text-danger·bg-danger-bg / text-warning·bg-warning-bg / text-info·bg-info-bg / text-safe·bg-safe-bg 매핑. 분기 표현식 (`days < 0`, `days <= 60`, `days <= 365`) 그대로"
    - "5 상태 변환 — (1) 로딩 line 1595 `<div className=\"text-center py-10 text-caption text-text-tertiary\">공단 데이터 조회 중...</div>` / (2) 빈 line 1596 `<EmptyState icon={<User size={36} />} text=\"안전관리자 정보가 없어요\" />` / (3) 정상 (A 프로필 / B 교육 / C 등록) / (4) D-day 색 4 분기 / (5) 등록 chip 2 분기"
    - "비즈니스 로직 100% 보존 (한 줄도 수정 X) — safetyMgrQuery / safetyMgrQuery.data / safetyMgrQuery.isLoading / data.manager (m.realName, m.maskedName, m.appointedAt, m.eduDate, m.eduValidFrom, m.eduValidTo) / data.education (edu.refreshEdu.daysLeft + deadline, edu.newEdu.daysLeft + deadline) / data.registration (reg.total, reg.registered, reg.registeredIds) / fmtDday 분기 키워드 (<0/≤60/≤365/>365) / elevators / evMap / find / grid 정의 (3 rows × 7 cols, null 분리자, find('ev'/'es', num)) / chip 함수 내부 분기 (ev.type === 'escalator' ? 'ES' : 'EV')"
    - "본문 이모지 0건 — 안전관리자 IIFE 영역 (현 line 1592~1712) 에서 👤/📚/🏢/🛗/↕️/✓/✗ 모두 제거 후 lucide 컴포넌트로 치환. EmptyState icon prop string '👤' 도 React 요소 `<User size={36} />` 로 교체"
    - "9·10·11px 폰트 모두 12px 이상 격상 — text-[Xpx] arbitrary 및 fontSize:9/10/11 모두 0. 이름 16px 만 예외(`text-[16px]` arbitrary 또는 데스크톱 분기). text-caption=12 / text-body-sm=13 / text-body=14 토큰 사용"
    - "TypeScript 컴파일 0 에러 (`npm run build` PASS)"
    - "다른 컴포넌트/페이지 0건 수정 — git status 에 ElevatorPage.tsx + PLAN + SUMMARY 만 표시 (icons.tsx / tailwind.config.js / KoelsaHistorySection.tsx / 다른 페이지 모두 변경 X)"
    - "Wave 1~10 변환 결과 단 한 줄도 수정 X (list 탭 / TYPE_ICON_COMPONENT / EvSelector / EsBtn / EsNodeMap / Fault 3 모달 / RepairNewModal / EvDetailModal / 헬퍼 4종 / 고장 탭 본문 / 수리 탭 RepairListSection / FAB / 점검 기록 탭 / 검사 기록 탭 / KoelsaHistorySection.tsx)"
    - "sketch (cf538a3 safety-mgr-sketch.html) 1:1 매핑 — 색 결정 / 폰트 격상 / 아이콘 매핑 / chip 라벨 'EV{n}'/'ES{n}' / D-day 4 분기 / 등록 chip 2 분기 모두 sketch 권위 그대로"
  artifacts:
    - path: "cha-bio-safety/src/pages/ElevatorPage.tsx"
      provides: "옵션 B 3C 변환 — 안전관리자 탭 본문 v0.1.1 토큰 + Tailwind + lucide"
      contains: "tab === 'safety' IIFE (프로필 + 교육 + 등록 3 카드)"
  key_links:
    - from: "cha-bio-safety/src/pages/ElevatorPage.tsx"
      to: "cha-bio-safety/docs/redesign-context/07-elevator/sketch/safety-mgr-sketch.html"
      via: "1:1 매핑 — 4 viewport (모바일다크/라이트 + 데스크톱다크/라이트) 색/스페이싱/아이콘 결정 source (commit cf538a3, Wave 11 시안 권위)"
      pattern: "safety-mgr-sketch"
    - from: "Wave 11 변환 패턴"
      to: "Wave 10 변환 (260515-rfh)"
      via: "1:1 mirror — frontmatter 구조, verify gate Section A~H, ddayClass 헬퍼 패턴 (dispClass mirror), EmptyState ReactNode 시그니처, lucide import 확장 패턴"
      pattern: "wave-10-mirror"
---

<objective>
redesign/07-elevator 옵션 B 마지막 변환 wave (Wave 11) — sketch (commit cf538a3, safety-mgr-sketch.html, 1596라인) 권위로 ElevatorPage.tsx 의 안전관리자 탭 본문 (`tab === 'safety'` IIFE, line 1592~1712, ~120줄) v0.1.1 토큰 + Tailwind + lucide 변환. Wave 10 (260515-rfh) 1:1 mirror structure.

Purpose: 옵션 B 3C sketch 사용자 검수 OK. 1:1 매핑 변환. 옵션 B 5탭 본문 시리즈 완결 (3A=Wave 9, 3B=Wave 10, 3C=Wave 11). 이후 사용자 검수 → main 머지 → redesign/07-elevator 페이지 완료.

Output: ElevatorPage.tsx 1개 파일 변경. 약 +60/-100 줄 추정 (인라인 style 제거로 감소). 다른 파일 0건. sketch 0건 변경 (이미 commit cf538a3 완료).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/quick/260516-027-redesign-07-elevator-tsx-wave-11-b-3c-v0/

# 메모리 권위
# - project_redesign_07_elevator_status.md → Wave 11 = 옵션 B 3C 변환 (마지막 wave)
# - feedback_redesign_sketch_rule_enforcement.md → sketch 권위 + 4중 verify gate
# - feedback_sketch_realistic_data.md → 비즈니스 로직 100% 보존, 시각만 손봄
# - project_design_tokens_branch.md → v0.1.1 시각 토큰
# - project_elevator_page_tabs.md → 안전관리자 = API 자동 (입력 모달 없음). 디스플레이 전용

# 직전 변환 (참고만, 수정 X)
# - Wave 1~10 모두 보존 (list 탭 + 5 모달 + 헬퍼 4종 + 고장 탭 + 수리 탭 RepairListSection + FAB + 점검 탭 + 검사 탭 + KoelsaHistorySection)
# - Wave 10 (260515-rfh) commit 8481d45 — dispClass 헬퍼 패턴, EmptyState React.ReactNode 시그니처, ::before 좌측 색바, lucide import 확장 — 모두 본 wave 에 인프라 재사용

# sketch 권위 (1:1 매핑 source — Wave 11)
# - cf538a3 safety-mgr-sketch.html (1596 라인)
# - 4 viewport: 모바일다크(정상 3 카드) + 모바일라이트(D-day 4 분기 카탈로그 + 빈/로딩) + 데스크톱다크(정상 풀화면) + 데스크톱라이트(등록 variant + D-day 가로 카탈로그)
# - 3 정보 카드: 프로필 / 교육 현황 / 공단 등록 현황
# - 5 상태: 정상 / D-day 카탈로그 / 빈 / 로딩 / 미등록 강조
# - 이모지 → lucide 7 매핑: 👤→User / 📚→BookOpen / 🏢→Building2 / 🛗→ElevatorIcon / ↕️→MoveDiagonal / ✓→CheckCircle2 / ✗→X
# - D-day 4 분기 색: <0 danger / ≤60 warning / ≤365 info / >365 safe
# - 등록 chip 2 분기: isReg ? safe (CheckCircle2) / !isReg ? warning (X)
# - chip 라벨 'EV{n}'/'ES{n}' 그대로 (UI 라벨 보존, EV-NN 형식 ID 와 별개)

# 코드 source (Read 검증 완료, 2026-05-16)
# - 안전관리자 탭 본문 (line 1592~1712, ~120줄): IIFE 블록.
#   · 로딩 (line 1595): 인라인 var(--t3) fontSize:12 padding:'40px 0'
#   · 빈 (line 1596): EmptyState icon="👤" text="안전관리자 정보가 없어요"
#   · fmtDday 함수 (line 1602~1608): 4 분기 색 var() 반환
#   · 프로필 카드 (line 1614~1633): bg-bg2 border-bd radius 12 pad 16 + 48×48 avatar bg-bg3 + 이름 16 bold + '승강기 안전관리자' fontSize 11 + 2-col grid (선임일 / 교육이수일) bg-bg3 radius 8 pad 8 10 fontSize 11
#   · 교육 현황 카드 (line 1635~1669): 헤더 fontSize 12 bold '📚 교육 현황' + 보수교육 row + 신규교육 row (fontSize 10/11 다수)
#   · 등록 현황 카드 (line 1671~1708): 헤더 fontSize 12 bold '🏢 공단 등록 현황' + 등록수 텍스트 + 호기 그리드 3 row × 7 col (gridTemplateColumns 'repeat(4,1fr) 6px repeat(2,1fr)' gap 4 + 헤더 행 fontSize 9 + chip fontSize 10)
# - 안전관리자 탭 외 영역 (line 1~1591, 1713~3463): 본 wave 영역 외 — 단 한 줄도 수정 X
# - lucide-react import (line 16): Wave 10 후 18개 — Package/UtensilsCrossed/MoveDiagonal/ChevronLeft/ChevronRight/ChevronUp/ChevronDown/AlertTriangle/Wrench/X/AlertOctagon/ClipboardCheck/FileSearch/CheckCircle2/Camera/Loader2/ClipboardList/Search
# - 추가할 lucide: User, BookOpen, Building2 (3개 신규)
# - ElevatorIcon: line 17 `import { ElevatorIcon } from '../components/ui/icons'` — 이미 import. 재import 0건.
# - tailwind.config.js 토큰: text-text-primary/secondary/tertiary, bg-surface-raised/sunken, border-border-default, bg-safe-bg/warning-bg/danger-bg/info-bg, safe/warning/danger/info (foreground)
# - EmptyState 시그니처 (Wave 9): icon: React.ReactNode — string 도 호환, React 요소 도 호환

@cha-bio-safety/src/pages/ElevatorPage.tsx
@cha-bio-safety/docs/redesign-context/07-elevator/sketch/safety-mgr-sketch.html
@cha-bio-safety/tailwind.config.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: 안전관리자 탭 본문 변환 + verify gate</name>
  <files>cha-bio-safety/src/pages/ElevatorPage.tsx</files>
  <action>
sketch (cf538a3 safety-mgr-sketch.html) 권위 1:1 매핑. 안전관리자 탭 IIFE 블록 (`tab === 'safety'`, 현 line 1592~1712, ~120줄) 단일 영역 변환.

### 1. lucide-react import 확장 (line 16)

기존 (Wave 10 후, 18종):
```ts
import { Package, UtensilsCrossed, MoveDiagonal, ChevronLeft, ChevronRight, ChevronUp, ChevronDown, AlertTriangle, Wrench, X, AlertOctagon, ClipboardCheck, FileSearch, CheckCircle2, Camera, Loader2, ClipboardList, Search } from 'lucide-react'
```

추가 3종 (`User`, `BookOpen`, `Building2`):
```ts
import { Package, UtensilsCrossed, MoveDiagonal, ChevronLeft, ChevronRight, ChevronUp, ChevronDown, AlertTriangle, Wrench, X, AlertOctagon, ClipboardCheck, FileSearch, CheckCircle2, Camera, Loader2, ClipboardList, Search, User, BookOpen, Building2 } from 'lucide-react'
```

`ElevatorIcon` 은 line 17 `import { ElevatorIcon } from '../components/ui/icons'` 로 이미 import — 재import 0건. lucide 가 아닌 ui/icons 자체 컴포넌트라는 점 주의.

### 2. ddayClass 헬퍼 신설 (또는 fmtDday 색 반환만 교체)

기존 fmtDday (line 1602~1608) 함수는 var() 색 반환. **권장 방식: 별도 헬퍼 `ddayClass` 추가** (fmtDday 함수는 그대로 둬도 됨 — refreshDday 변수 사용처가 변환 영역 한정이라 둘 다 가능). 분기 키워드 (`days < 0`, `days <= 60`, `days <= 365`) **절대 변경 X**.

옵션 A (권장, 부작용 최소): 신규 헬퍼 ddayClass 정의 후 chip 렌더에서만 사용:
```ts
const ddayClass = (days: number | null): { text: string; bg: string; label: string } | null => {
  if (days === null) return null
  if (days < 0) return { text: 'text-danger', bg: 'bg-danger-bg', label: `D+${Math.abs(days)} 초과` }
  if (days <= 60) return { text: 'text-warning', bg: 'bg-warning-bg', label: `D-${days}` }
  if (days <= 365) return { text: 'text-info', bg: 'bg-info-bg', label: `D-${days}` }
  return { text: 'text-safe', bg: 'bg-safe-bg', label: `D-${days}` }
}
const refreshDday = ddayClass(edu.refreshEdu.daysLeft)
```

기존 `fmtDday(edu.refreshEdu.daysLeft)` 호출 (line 1610) → `ddayClass(edu.refreshEdu.daysLeft)` 로 교체. 기존 fmtDday 함수 정의는 사용처 0건 되면 제거 가능 (변환 영역 내 한정 사용 — line 1610 만, 다른 영역 사용 없음 확인).

옵션 B (간단): 기존 fmtDday 시그니처 보존하되 반환을 className 으로:
```ts
const fmtDday = (days: number | null) => {
  if (days === null) return null
  if (days < 0) return { text: `D+${Math.abs(days)} 초과`, textCls: 'text-danger', bgCls: 'bg-danger-bg' }
  if (days <= 60) return { text: `D-${days}`, textCls: 'text-warning', bgCls: 'bg-warning-bg' }
  if (days <= 365) return { text: `D-${days}`, textCls: 'text-info', bgCls: 'bg-info-bg' }
  return { text: `D-${days}`, textCls: 'text-safe', bgCls: 'bg-safe-bg' }
}
```
chip 렌더: `<span className={`text-caption font-bold ${refreshDday.textCls} ${refreshDday.bgCls} px-2 py-0.5 rounded-md`}>{refreshDday.text}</span>`

**둘 중 어느 방식이든 분기 키워드(<0/<=60/<=365) 와 4 분기 색 결정 룰 (danger/warning/info/safe) 절대 변경 X.**

### 3. 로딩 분기 (line 1595)

기존:
```tsx
if (safetyMgrQuery.isLoading) return <div style={{ textAlign:'center', padding:'40px 0', color:'var(--t3)', fontSize:12 }}>공단 데이터 조회 중...</div>
```

변환:
```tsx
if (safetyMgrQuery.isLoading) return <div className="text-center py-10 text-caption text-text-tertiary">공단 데이터 조회 중...</div>
```
- padding:'40px 0' → py-10 (40px)
- fontSize:12 → text-caption (12px)
- var(--t3) → text-text-tertiary

### 4. 빈 분기 (line 1596)

기존:
```tsx
if (!data?.manager) return <EmptyState icon="👤" text="안전관리자 정보가 없어요" />
```

변환 (Wave 9 EmptyState 시그니처 React.ReactNode 활용):
```tsx
if (!data?.manager) return <EmptyState icon={<User size={36} />} text="안전관리자 정보가 없어요" />
```

### 5. 프로필 카드 변환 (line 1614~1633)

기존:
```tsx
<div style={{ background:'var(--bg2)', border:'1px solid var(--bd)', borderRadius:12, padding:'16px', flexShrink:0 }}>
  <div style={{ display:'flex', alignItems:'center', gap:12, marginBottom:12 }}>
    <div style={{ width:48, height:48, borderRadius:'50%', background:'var(--bg3)', display:'flex', alignItems:'center', justifyContent:'center', fontSize:24 }}>👤</div>
    <div>
      <div style={{ fontSize:16, fontWeight:700, color:'var(--t1)' }}>{m.realName ?? m.maskedName}</div>
      <div style={{ fontSize:11, color:'var(--t3)', marginTop:2 }}>승강기 안전관리자</div>
    </div>
  </div>
  <div style={{ display:'grid', gridTemplateColumns:'1fr 1fr', gap:8, fontSize:11 }}>
    <div style={{ background:'var(--bg3)', borderRadius:8, padding:'8px 10px' }}>
      <div style={{ color:'var(--t3)', marginBottom:2 }}>선임일</div>
      <div style={{ fontWeight:700, color:'var(--t1)' }}>{m.appointedAt}</div>
    </div>
    <div style={{ background:'var(--bg3)', borderRadius:8, padding:'8px 10px' }}>
      <div style={{ color:'var(--t3)', marginBottom:2 }}>교육이수일</div>
      <div style={{ fontWeight:700, color:'var(--t1)' }}>{m.eduDate}</div>
    </div>
  </div>
</div>
```

변환:
```tsx
<div className="bg-surface-raised border border-border-default rounded-xl p-4 flex-shrink-0">
  <div className="flex items-center gap-3 mb-3">
    <div className="w-12 h-12 rounded-full bg-surface-sunken flex items-center justify-center flex-shrink-0">
      <User size={28} className="text-text-secondary" />
    </div>
    <div>
      <div className="text-[16px] font-bold text-text-primary">{m.realName ?? m.maskedName}</div>
      <div className="text-caption text-text-tertiary mt-0.5">승강기 안전관리자</div>
    </div>
  </div>
  <div className="grid grid-cols-2 gap-2">
    <div className="bg-surface-sunken rounded-lg px-2.5 py-2">
      <div className="text-caption text-text-tertiary mb-0.5">선임일</div>
      <div className="text-body-sm font-bold text-text-primary">{m.appointedAt}</div>
    </div>
    <div className="bg-surface-sunken rounded-lg px-2.5 py-2">
      <div className="text-caption text-text-tertiary mb-0.5">교육이수일</div>
      <div className="text-body-sm font-bold text-text-primary">{m.eduDate}</div>
    </div>
  </div>
</div>
```

매핑 요약:
- bg-bg2 → bg-surface-raised / bd → border-border-default
- bg-bg3 → bg-surface-sunken
- t1 → text-text-primary / t3 → text-text-tertiary
- 48×48: w-12 h-12 / 12px gap: gap-3 / 12px mb: mb-3
- 8px 10px padding: px-2.5 py-2
- fontSize 16: text-[16px] (arbitrary, 16px 토큰 미정의)
- fontSize 11: text-caption (12px 격상)
- fontSize 24 이모지 → User size=28 lucide
- gridTemplateColumns '1fr 1fr' → grid-cols-2

### 6. 교육 현황 카드 변환 (line 1635~1669)

기존 헤더 (line 1637):
```tsx
<div style={{ fontSize:12, fontWeight:700, color:'var(--t1)', marginBottom:12 }}>📚 교육 현황</div>
```
변환:
```tsx
<div className="text-body-sm font-bold text-text-primary mb-3 flex items-center gap-1.5">
  <BookOpen size={16} className="text-text-secondary" />
  교육 현황
</div>
```

보수(재) 교육 row (line 1640~1653):
```tsx
<div className="bg-surface-sunken rounded-xl px-3 py-2.5 mb-2">
  <div className="flex items-center justify-between mb-1.5">
    <span className="text-body-sm font-bold text-text-primary">보수(재) 교육</span>
    {refreshDday && (
      <span className={`text-caption font-bold ${refreshDday.text} ${refreshDday.bg} px-2 py-0.5 rounded-md`}>{refreshDday.label}</span>
    )}
  </div>
  <div className="text-caption text-text-tertiary">
    유효기간: {m.eduValidFrom} ~ {m.eduValidTo}
  </div>
  <div className="text-caption text-text-tertiary mt-1">
    다음 교육 마감: {edu.refreshEdu.deadline ?? '-'} (직전 이수일 + 3년)
  </div>
</div>
```
- bg-bg3 → bg-surface-sunken / radius 10 → rounded-xl
- padding 10px 12px → px-3 py-2.5
- marginBottom 8 → mb-2
- mb 6 → mb-1.5
- fontSize 12 → text-body-sm (13 — `text-body-sm` 매핑 또는 13px 결정. sketch 권위 13 이라면 body-sm)
- fontSize 11 → text-caption (12)
- fontSize 10 → text-caption (12 격상)
- ddayClass / fmtDday 색 헬퍼 사용 (Section 2 참조)

신규 교육 row (line 1656~1668):
```tsx
<div className="bg-surface-sunken rounded-xl px-3 py-2.5">
  <div className="flex items-center justify-between mb-1.5">
    <span className="text-body-sm font-bold text-text-primary">신규 교육</span>
    {edu.newEdu.daysLeft !== null && edu.newEdu.daysLeft < 0 ? (
      <span className="text-caption font-bold text-safe bg-safe-bg px-2 py-0.5 rounded-md">완료</span>
    ) : edu.newEdu.daysLeft !== null ? (
      <span className="text-caption font-bold text-warning bg-warning-bg px-2 py-0.5 rounded-md">D-{edu.newEdu.daysLeft}</span>
    ) : null}
  </div>
  <div className="text-caption text-text-tertiary">
    마감: {edu.newEdu.deadline ?? '-'} (선임일 + 3개월)
  </div>
</div>
```
- 완료 chip — text-safe bg-safe-bg (rgba(34,197,94,.12) → bg-safe-bg)
- D-N chip — text-warning bg-warning-bg (rgba(245,158,11,.12) → bg-warning-bg)
- 분기 표현식 (daysLeft !== null && daysLeft < 0 / >= 0) 그대로

### 7. 공단 등록 현황 카드 변환 (line 1671~1708)

헤더 (line 1673):
```tsx
<div className="text-body-sm font-bold text-text-primary mb-2 flex items-center gap-1.5">
  <Building2 size={16} className="text-text-secondary" />
  공단 등록 현황
</div>
```

등록 수 텍스트 (line 1674~1679):
```tsx
<div className="text-body-sm text-text-secondary mb-2.5">
  {reg.total}대 중 <span className="text-safe font-bold">{reg.registered}대</span> 등록
  {reg.total - reg.registered > 0 && (
    <span className="text-warning ml-1.5">· 미등록 {reg.total - reg.registered}대</span>
  )}
</div>
```
- fontSize 12 → text-body-sm (13)
- t2 → text-text-secondary
- marginBottom 10 → mb-2.5
- var(--safe) → text-safe / var(--warn) → text-warning
- marginLeft 6 → ml-1.5

호기 그리드 (line 1680~1707):

chip 함수:
```tsx
const chip = (evId: string | undefined) => {
  if (!evId) return <div />
  const ev = evMap.get(evId)
  if (!ev) return <div />
  const isReg = reg.registeredIds.includes(evId)
  const icon = ev.type === 'escalator' ? 'ES' : 'EV'
  return (
    <span className={`text-caption font-semibold px-2 py-0.5 rounded-md text-center inline-flex items-center justify-center gap-0.5 ${isReg ? 'bg-safe-bg text-safe' : 'bg-warning-bg text-warning'}`}>
      {icon}{ev.number}
      {isReg ? <CheckCircle2 size={11} strokeWidth={2.5} /> : <X size={11} strokeWidth={2.5} />}
    </span>
  )
}
```
- fontSize 10 → text-caption (12 격상)
- fontWeight 600 → font-semibold
- padding '3px 8px' → px-2 py-0.5
- borderRadius 6 → rounded-md
- bg/color 분기 — rgba alpha → bg-safe-bg/bg-warning-bg + text-safe/text-warning
- '✓'/'✗' → CheckCircle2 size=11 / X size=11 (strokeWidth=2.5)

그리드 wrapper (line 1697):
```tsx
<div className="grid grid-cols-[repeat(4,1fr)_6px_repeat(2,1fr)] gap-1 items-center">
  <div className="col-span-4 text-caption font-bold text-text-tertiary tracking-wider flex items-center gap-1">
    <ElevatorIcon size={14} className="text-text-tertiary" strokeWidth={1.8} />
    엘리베이터
  </div>
  <div />
  <div className="col-span-2 text-caption font-bold text-text-tertiary tracking-wider flex items-center gap-1">
    <MoveDiagonal size={14} className="text-text-tertiary" />
    에스컬레이터
  </div>
  {grid.map((row, ri) => row.map((id, ci) => {
    if (id === null) return <div key={`${ri}-sep`} />
    return <div key={`${ri}-${ci}`}>{chip(id)}</div>
  }))}
</div>
```
- gridTemplateColumns 'repeat(4, 1fr) 6px repeat(2, 1fr)' → Tailwind arbitrary `grid-cols-[repeat(4,1fr)_6px_repeat(2,1fr)]`
- gap 4 → gap-1
- alignItems center → items-center
- 헤더 행: gridColumn '1/5' → col-span-4 / '6/8' → col-span-2
- fontSize 9 → text-caption (12 격상)
- letterSpacing '.04em' → tracking-wider (Tailwind .025em, 근사값)
- 🛗 → ElevatorIcon size=14 (ui/icons 자체 컴포넌트)
- ↕️ → MoveDiagonal size=14 lucide

**대안 (gridTemplateColumns 화이트리스트 fallback)**: Tailwind arbitrary 가 안 먹는 경우 인라인 `style={{ gridTemplateColumns: 'repeat(4,1fr) 6px repeat(2,1fr)' }}` 도 dynamic value 화이트리스트로 허용 (Wave 10 주의관찰 grid 와 동일 정책). Section B verify gate 는 var() 만 체크 — style={{ count 는 KoelsaHistorySection 영역 한정이고 ElevatorPage 변환 영역은 화이트리스트 dynamic 만 잔존 허용.

### 8. 보존 항목 (절대 건드리지 말 것)

- Wave 1~10 변환 결과 (list 탭 / 5 모달 / 헬퍼 4종 / 고장 탭 / 수리 탭 RepairListSection / FAB / 점검 탭 / 검사 탭 / KoelsaHistorySection.tsx)
- 비즈니스 로직 (safetyMgrQuery / m.realName / m.maskedName / m.appointedAt / m.eduDate / m.eduValidFrom / m.eduValidTo / edu.refreshEdu / edu.newEdu / reg.total / reg.registered / reg.registeredIds / elevators / evMap / find / grid 정의 / chip 함수 분기 / fmtDday 분기 키워드)
- icons.tsx / tailwind.config.js / KoelsaHistorySection.tsx
- 다른 페이지 / 컴포넌트

### 9. 작업 순서 (안전)

1. lucide-react import 확장 (line 16) — User/BookOpen/Building2 3개 추가
2. ddayClass 헬퍼 (또는 fmtDday 색 반환 교체) — 분기 키워드 보존
3. 로딩 / 빈 분기 변환 (line 1595~1596)
4. 프로필 카드 변환 (line 1614~1633) — 👤 → User, 인라인 var() → Tailwind
5. 교육 현황 카드 변환 (line 1635~1669) — 📚 → BookOpen, ddayClass 적용
6. 공단 등록 현황 카드 변환 (line 1671~1708) — 🏢 → Building2 + chip 함수 + 그리드 + 🛗/↕️/✓/✗ lucide 치환
7. fmtDday 사용처 0건 되면 정의 자체 제거 가능 (선택 — Wave 10 패턴 일관성. 단, 사용처 grep 확인 필수)
8. `npm run build` PASS 확인 (TS 0 에러)
9. grep gate 통과 확인 (verify 블록 Section A~H)
10. commit
  </action>
  <verify>
    <automated>
cd cha-bio-safety && \
  echo "=== Section A: 라인 변동 (Wave 11 변환 후 합리적 범위) ===" && \
  echo "ElevatorPage.tsx lines: $(wc -l < src/pages/ElevatorPage.tsx) (Wave 10 기준 3463. expect 3270~3470 — 인라인 제거로 감소 가능)" && \
  echo "" && \
  echo "=== Section B: 변환 영역 인라인 var() 0건 (안전관리자 IIFE 블록 기준 sed 스코프 — Wave 11 변환 후 line range 는 동적, 1592부터 약 ~120라인 추정. 안전한 범위 1580~1730 으로 검증) ===" && \
  sed -n '1580,1730p' src/pages/ElevatorPage.tsx > /tmp/wave11-safety-zone.txt && \
  echo "var(--bg2) in zone: $(grep -c 'var(--bg2)' /tmp/wave11-safety-zone.txt) (expect 0)" && \
  echo "var(--bd) in zone: $(grep -c 'var(--bd)' /tmp/wave11-safety-zone.txt) (expect 0)" && \
  echo "var(--bd2) in zone: $(grep -c 'var(--bd2)' /tmp/wave11-safety-zone.txt) (expect 0)" && \
  echo "var(--t1) in zone: $(grep -c 'var(--t1)' /tmp/wave11-safety-zone.txt) (expect 0)" && \
  echo "var(--t2) in zone: $(grep -c 'var(--t2)' /tmp/wave11-safety-zone.txt) (expect 0)" && \
  echo "var(--t3) in zone: $(grep -c 'var(--t3)' /tmp/wave11-safety-zone.txt) (expect 0)" && \
  echo "var(--bg3) in zone: $(grep -c 'var(--bg3)' /tmp/wave11-safety-zone.txt) (expect 0)" && \
  echo "var(--danger) in zone: $(grep -c 'var(--danger)' /tmp/wave11-safety-zone.txt) (expect 0)" && \
  echo "var(--safe) in zone: $(grep -c 'var(--safe)' /tmp/wave11-safety-zone.txt) (expect 0)" && \
  echo "var(--warn) in zone: $(grep -c 'var(--warn)' /tmp/wave11-safety-zone.txt) (expect 0)" && \
  echo "var(--info) in zone: $(grep -c 'var(--info)' /tmp/wave11-safety-zone.txt) (expect 0)" && \
  echo "" && \
  echo "=== Section C: 변환 영역 본문 이모지 0건 (👤📚🏢🛗↕️✓✗ 모두 제거) ===" && \
  echo "👤 in zone: $(grep -c '👤' /tmp/wave11-safety-zone.txt) (expect 0)" && \
  echo "📚 in zone: $(grep -c '📚' /tmp/wave11-safety-zone.txt) (expect 0)" && \
  echo "🏢 in zone: $(grep -c '🏢' /tmp/wave11-safety-zone.txt) (expect 0)" && \
  echo "🛗 in zone: $(grep -c '🛗' /tmp/wave11-safety-zone.txt) (expect 0)" && \
  echo "↕️ in zone: $(grep -c '↕️' /tmp/wave11-safety-zone.txt) (expect 0)" && \
  echo "✓ in zone: $(grep -c '✓' /tmp/wave11-safety-zone.txt) (expect 0)" && \
  echo "✗ in zone: $(grep -c '✗' /tmp/wave11-safety-zone.txt) (expect 0)" && \
  echo "" && \
  echo "=== Section D: 변환 영역 9·10·11px 격상 (text-[Xpx] arbitrary 잔존 0) ===" && \
  echo "text-[9px] in zone: $(grep -c 'text-\\[9px\\]' /tmp/wave11-safety-zone.txt) (expect 0)" && \
  echo "text-[10px] in zone: $(grep -c 'text-\\[10px\\]' /tmp/wave11-safety-zone.txt) (expect 0)" && \
  echo "text-[11px] in zone: $(grep -c 'text-\\[11px\\]' /tmp/wave11-safety-zone.txt) (expect 0)" && \
  echo "fontSize:9 in zone: $(grep -cE 'fontSize: ?9[^0-9]' /tmp/wave11-safety-zone.txt) (expect 0)" && \
  echo "fontSize:10 in zone: $(grep -cE 'fontSize: ?10[^0-9]' /tmp/wave11-safety-zone.txt) (expect 0)" && \
  echo "fontSize:11 in zone: $(grep -cE 'fontSize: ?11[^0-9]' /tmp/wave11-safety-zone.txt) (expect 0)" && \
  echo "" && \
  echo "=== Section E: 변환 영역 인라인 style 속성 (var() 0 + 화이트리스트 dynamic 한정) ===" && \
  echo "style={{ in zone: $(grep -c 'style={{' /tmp/wave11-safety-zone.txt) (expect 0 또는 gridTemplateColumns 화이트리스트 ≤1)" && \
  echo "" && \
  echo "=== Section F: lucide 신규 3종 추가 (ElevatorPage line 16 import) ===" && \
  echo "User import (line 16): $(grep -c 'User' src/pages/ElevatorPage.tsx | head -1)건 (expect >= 3 — import + 2 사용 avatar/EmptyState)" && \
  echo "BookOpen: $(grep -c 'BookOpen' src/pages/ElevatorPage.tsx) (expect >= 2 — import + 1 사용)" && \
  echo "Building2: $(grep -c 'Building2' src/pages/ElevatorPage.tsx) (expect >= 2 — import + 1 사용)" && \
  echo "" && \
  echo "=== Section G: lucide 재사용 (ElevatorIcon/MoveDiagonal/CheckCircle2/X) 변환 영역 사용 ===" && \
  echo "ElevatorIcon in zone: $(grep -c 'ElevatorIcon' /tmp/wave11-safety-zone.txt) (expect >= 1 — 등록 그리드 헤더)" && \
  echo "MoveDiagonal in zone: $(grep -c 'MoveDiagonal' /tmp/wave11-safety-zone.txt) (expect >= 1 — 등록 그리드 헤더)" && \
  echo "CheckCircle2 in zone: $(grep -c 'CheckCircle2' /tmp/wave11-safety-zone.txt) (expect >= 1 — chip isReg)" && \
  echo "X in zone (chip): $(grep -cE '<X size' /tmp/wave11-safety-zone.txt) (expect >= 1 — chip !isReg)" && \
  echo "" && \
  echo "=== Section H: 다른 파일 0건 변경 — git status ===" && \
  git status --short && \
  echo "" && \
  echo "=== Hard gate ===" && \
  test $(grep -cE 'var\\(--(bg2|bd|bd2|t1|t2|t3|bg3|danger|safe|warn|info)\\)' /tmp/wave11-safety-zone.txt) -eq 0 && \
  test $(grep -c '👤' /tmp/wave11-safety-zone.txt) -eq 0 && \
  test $(grep -c '📚' /tmp/wave11-safety-zone.txt) -eq 0 && \
  test $(grep -c '🏢' /tmp/wave11-safety-zone.txt) -eq 0 && \
  test $(grep -c '🛗' /tmp/wave11-safety-zone.txt) -eq 0 && \
  test $(grep -c '↕️' /tmp/wave11-safety-zone.txt) -eq 0 && \
  test $(grep -c '✓' /tmp/wave11-safety-zone.txt) -eq 0 && \
  test $(grep -c '✗' /tmp/wave11-safety-zone.txt) -eq 0 && \
  echo "" && \
  echo "=== TypeScript / npm build ===" && \
  npm run build 2>&1 | tail -10 && \
  echo "=== build PASS ==="
    </automated>
  </verify>
  <done>
- grep gate Section A~H 모두 PASS (라인 변동 합리 / var() in zone 0 / 이모지 in zone 0 / 9·10·11px in zone 0 / 인라인 style 화이트리스트 한정 / lucide 신규 3종 import 보장 / 재사용 4종 사용 보장 / git status ElevatorPage.tsx 만)
- 안전관리자 탭 본문 line 1592~1712 (변환 후 line range 는 동적) sketch (cf538a3) 1:1 매핑 변환
- 프로필 카드 — 48×48 User avatar / 이름 16px / 부제 + 2-col grid (선임일/교육이수일)
- 교육 현황 카드 — BookOpen 헤더 + 보수(재) row (ddayClass 4 분기) + 신규 row (완료 safe / D-N warning)
- 공단 등록 현황 카드 — Building2 헤더 + 등록 수 텍스트 (safe/warning) + 호기 그리드 3×7 (ElevatorIcon/MoveDiagonal 헤더 + chip CheckCircle2/X)
- 이모지 → lucide 치환: 👤→User, 📚→BookOpen, 🏢→Building2, 🛗→ElevatorIcon, ↕️→MoveDiagonal, ✓→CheckCircle2, ✗→X
- D-day 4 분기 색 분기 키워드 (<0/<=60/<=365/>365) 100% 보존, 색만 className 으로 교체
- 등록 chip 2 분기 (isReg ? safe : warning) 100% 보존
- chip 라벨 'EV{n}'/'ES{n}' UI 라벨 그대로
- npm run build PASS (TypeScript 0 에러)
- 다른 파일 0건 변경 (`git status` 에 ElevatorPage.tsx + PLAN.md + SUMMARY.md 만)
- Wave 1~10 변환 결과 단 한 줄도 시각 결정 변경 X
- icons.tsx / tailwind.config.js / KoelsaHistorySection.tsx 0건 수정
  </done>
</task>

<task type="auto">
  <name>Task 2: SUMMARY 작성 + commit</name>
  <files>.planning/quick/260516-027-redesign-07-elevator-tsx-wave-11-b-3c-v0/260516-027-SUMMARY.md</files>
  <action>
SUMMARY 작성 후 single commit. SUMMARY 는 orchestrator 가 별도 docs commit 처리할 수 있도록 untracked 유지 가능 (executor 가 commit 안 함).

### SUMMARY 필수 섹션

- **What changed**: ElevatorPage.tsx 안전관리자 탭 본문 (`tab === 'safety'` IIFE, line 1592~1712 → 변환 후 동적) sketch 1:1 변환.
  - (1) lucide import 확장 — User/BookOpen/Building2 3종 추가 (Wave 10 기존 18종 + 3 = 21종). ElevatorIcon 은 ui/icons 자체 컴포넌트 (line 17), 재import 0건.
  - (2) ddayClass 헬퍼 신설 (또는 fmtDday 색 반환 교체) — 4 분기 키워드 100% 보존, 색만 className 으로 (text-danger/warning/info/safe + bg-*-bg).
  - (3) 로딩 분기 — text-center py-10 text-caption text-text-tertiary (인라인 var(--t3) fontSize 제거).
  - (4) 빈 분기 — EmptyState icon=`<User size={36} />` (string '👤' → React 요소).
  - (5) 프로필 카드 — wrapper bg-surface-raised border rounded-xl p-4 + 48×48 User avatar (bg-surface-sunken rounded-full) + 이름 text-[16px] bold + 부제 text-caption text-tertiary + 2-col grid (선임일/교육이수일) bg-surface-sunken rounded-lg px-2.5 py-2.
  - (6) 교육 현황 카드 — wrapper 동일 + 헤더 BookOpen + 보수(재) row bg-surface-sunken rounded-xl + ddayClass chip + 유효기간/마감 text-caption text-tertiary + 신규 row 동일 패턴 (완료 safe / D-N warning).
  - (7) 공단 등록 현황 카드 — wrapper 동일 + 헤더 Building2 + 등록 수 텍스트 (text-safe 등록 / text-warning 미등록) + 호기 그리드 3×7 (grid-cols-[repeat(4,1fr)_6px_repeat(2,1fr)] arbitrary 또는 인라인 화이트리스트) + 헤더 행 ElevatorIcon/MoveDiagonal (size=14, 9→12 격상) + chip 함수 (bg-safe-bg/bg-warning-bg + CheckCircle2/X size=11, 10→12 격상).
- **Why**: 옵션 B 3C sketch (cf538a3 safety-mgr-sketch.html, 1596 라인) 사용자 검수 OK. 1:1 매핑 변환. 옵션 B 5탭 본문 시리즈 완결 (3A=Wave 9, 3B=Wave 10, 3C=Wave 11). 이후 사용자 검수 → main 머지 → redesign/07-elevator 페이지 완료.
- **Preserved**: 비즈니스 로직 100% (safetyMgrQuery / data.manager / data.education / data.registration / m.realName ?? m.maskedName / m.appointedAt / m.eduDate / m.eduValidFrom / m.eduValidTo / edu.refreshEdu.daysLeft + deadline / edu.newEdu.daysLeft + deadline / reg.total / reg.registered / reg.registeredIds / fmtDday 4 분기 키워드 / elevators / evMap / find / grid 3 rows × 7 cols / null 분리자 / chip 함수 분기 (escalator → 'ES' / 'EV')). Wave 1~10 변환 결과 (list 탭 / 5 모달 / 헬퍼 4종 / 고장 탭 / 수리 탭 RepairListSection / FAB / 점검 탭 / 검사 탭 / KoelsaHistorySection.tsx). icons.tsx / tailwind.config.js / 다른 페이지 0건 수정.
- **Verification**: grep gate Section A~H 모두 PASS, npm build PASS, git status ElevatorPage.tsx + PLAN + SUMMARY 만.
- **Out of scope**: tailwind.config.js typography 토큰 추가 (16px 헤더 `text-[16px]` arbitrary 유지). 안전관리자 데이터 자동 갱신 (cron 영역 — cbc-cron-worker 별도 프로젝트). 안전관리자 입력 UI (디스플레이 전용 — 메모리 룰).
- **Visual impact**: 사용자 인지 sketch (cf538a3) 4 viewport 와 일치 — 프로필 User avatar + 16px 이름 / 교육 BookOpen 헤더 + D-day 4 분기 색 (danger/warning/info/safe) + 신규 완료·D-N 칩 / 등록 Building2 헤더 + 등록 수 safe·warning + 호기 그리드 ElevatorIcon/MoveDiagonal + chip CheckCircle2/X. 노안 가독성 (9·10·11px → 12·13·14px+ 격상).
- **Next**: 사용자 검수 → 만족 시 main 머지(또는 변환 wave 묶음 일괄 머지) → redesign/07-elevator 페이지 완료. 옵션 B 시리즈 종결.

### Commit (code only)

executor 가 ElevatorPage.tsx 만 single commit:
```
feat(260516-027): Wave 11 — 옵션 B 3C 변환 (안전관리자 탭 본문) v0.1.1 토큰 + Tailwind + lucide
```

SUMMARY/PLAN docs commit 은 orchestrator 가 worktree merge 후 처리.
  </action>
  <verify>
    <automated>
cd /Users/jykevin/Documents/cbc7119-design && \
  ls .planning/quick/260516-027-redesign-07-elevator-tsx-wave-11-b-3c-v0/260516-027-SUMMARY.md
    </automated>
  </verify>
  <done>
- SUMMARY.md 작성 완료 (untracked or committed depending on orchestrator policy)
- single code commit (ElevatorPage.tsx 만)
- redesign/07-elevator push (main 머지 X, 배포 X — 사용자 컨펌 대기)
  </done>
</task>

</tasks>

<success_criteria>
- [ ] lucide-react import 확장 — User/BookOpen/Building2 3종 추가 (line 16)
- [ ] 안전관리자 탭 본문 line 1592~1712 변환 — 로딩 (text-caption text-text-tertiary) / 빈 (EmptyState User size=36) / 프로필 카드 (User avatar 28 + 16px 이름 + 2-col grid) / 교육 카드 (BookOpen 헤더 + ddayClass 4 분기 chip + 신규 완료·D-N chip) / 등록 카드 (Building2 헤더 + 등록 수 safe·warning + 호기 그리드 ElevatorIcon/MoveDiagonal + chip CheckCircle2/X)
- [ ] var(--bg2/bd/bd2/t1/t2/t3/bg3/danger/safe/warn/info) 변환 영역 (line 1580~1730) 0건
- [ ] 본문 이모지 변환 영역 0건 (👤/📚/🏢/🛗/↕️/✓/✗ 모두 제거)
- [ ] 9·10·11px 폰트 변환 영역 0건 (text-[9px]/text-[10px]/text-[11px]/fontSize:9/10/11 모두 0)
- [ ] 인라인 style 속성 변환 영역 0건 또는 gridTemplateColumns 화이트리스트 한정
- [ ] D-day 4 분기 키워드 (<0/<=60/<=365/>365) 100% 보존 — 색만 className 교체
- [ ] 등록 chip 2 분기 (isReg ? safe : warning) 100% 보존
- [ ] chip 라벨 'EV{n}'/'ES{n}' UI 라벨 보존 (EV-NN 형식 ID 와 별개)
- [ ] Wave 1~10 변환 결과 단 한 줄도 변경 X
- [ ] icons.tsx / tailwind.config.js / KoelsaHistorySection.tsx 0건 수정
- [ ] TypeScript 0 에러, npm run build PASS
- [ ] SUMMARY 작성 + single commit + push (main 머지 X)
</success_criteria>

<output>
After completion, ensure `.planning/quick/260516-027-redesign-07-elevator-tsx-wave-11-b-3c-v0/260516-027-SUMMARY.md` exists.
</output>
