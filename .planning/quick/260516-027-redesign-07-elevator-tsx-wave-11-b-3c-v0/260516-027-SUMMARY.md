---
phase: quick-260516-027
plan: 01
subsystem: cha-bio-safety/elevator-page
tags: [redesign, 07-elevator, wave-11, option-b, 3c, safety-mgr-tab, v0.1.1, tailwind, lucide]
requires:
  - "Wave 1~10 변환 (260515 시리즈) — list 탭 / 5 모달 / 헬퍼 4종 / 고장 탭 / 수리 탭 RepairListSection / FAB / 점검 탭 / 검사 탭 / KoelsaHistorySection.tsx"
  - "Wave 11 sketch (cf538a3, safety-mgr-sketch.html, 1596 라인) — 4 viewport 1:1 매핑 source"
  - "tailwind.config.js v0.1.1 토큰 (text-text-primary/secondary/tertiary, bg-surface-raised/sunken, border-border-default, safe/warning/danger/info + safe-bg/warning-bg/danger-bg/info-bg, text-caption, text-body-sm)"
  - "ElevatorIcon (cha-bio-safety/src/components/ui/icons) — Wave 11 재import 0건 (line 17 기존 import 재사용)"
provides:
  - "ElevatorPage 안전관리자 탭 (`tab === 'safety'` IIFE) v0.1.1 토큰 + Tailwind + lucide 변환"
  - "ddayClass 헬퍼 — D-day 4 분기 키워드(<0/<=60/<=365/>365) 보존, 색만 className 반환"
  - "옵션 B 5탭 본문 시리즈 완결 (3A=Wave 9 고장+수리 / 3B=Wave 10 점검+검사 / 3C=Wave 11 안전관리자)"
affects:
  - "cha-bio-safety/src/pages/ElevatorPage.tsx (안전관리자 탭 본문 line 1593~1731)"
tech-stack:
  added:
    - "lucide-react: User, BookOpen, Building2 (3 신규 / Wave 10 18종 + 3 = 21종)"
  patterns:
    - "Wave 10 ddayClass-mirror — fmtDday(var() 반환) → ddayClass({ text, bg, label } className 반환)"
    - "Wave 9 EmptyState React.ReactNode 시그니처 활용 — string '👤' → `<User size={36} />`"
    - "Tailwind arbitrary value 활용 — `grid-cols-[repeat(4,1fr)_6px_repeat(2,1fr)]` (인라인 gridTemplateColumns 제거)"
    - "이모지 → lucide 7 매핑 — 👤→User / 📚→BookOpen / 🏢→Building2 / 🛗→ElevatorIcon / ↕️→MoveDiagonal / ✓→CheckCircle2 / ✗→X"
key-files:
  created: []
  modified:
    - "cha-bio-safety/src/pages/ElevatorPage.tsx (+63 / -44 줄, 3463 → 3482 라인, 안전관리자 IIFE 블록 line 1593~1731 변환)"
decisions:
  - "ddayClass 헬퍼 (옵션 A) 채택 — 기존 fmtDday 함수 정의를 className 반환 시그니처로 통째 교체. 분기 키워드(`days < 0`, `days <= 60`, `days <= 365`) 100% 보존, 반환 객체만 `{ text: className, bg: className, label }` 로. 사용처 1건(line 1610 refreshDday) 만 호출 시그니처 자동 호환"
  - "Tailwind arbitrary `grid-cols-[repeat(4,1fr)_6px_repeat(2,1fr)]` 채택 — 인라인 style={{ gridTemplateColumns }} 화이트리스트 fallback 안 쓰고 zone 0건 달성"
  - "ElevatorIcon strokeWidth=1.8 명시 — Wave 10 line 635 일관성 (기본 stroke=2 보다 살짝 얇게, sketch 권위 매칭)"
  - "16px 이름 폰트 `text-[16px]` arbitrary 유지 — tailwind body 토큰(16px)도 16이지만 fontWeight 400 이어서 font-bold 따로 붙어야 함. arbitrary 가 더 sketch 권위 직관적"
  - "본문 이모지 0건 — '👤📚🏢🛗↕️✓✗' 7종 모두 lucide 컴포넌트 치환. EmptyState icon prop 도 string '👤' → React 요소 `<User size={36} />`"
metrics:
  duration_min: 4
  completed_at: "2026-05-15T15:13:20Z"
  tasks_completed: 2
  files_changed: 1
  commits: 1
---

# Phase quick-260516-027 Plan 01: Wave 11 — 옵션 B 3C 변환 (안전관리자 탭 본문) Summary

One-liner: ElevatorPage.tsx 안전관리자 탭 IIFE 블록(line 1593~1731)을 sketch cf538a3 권위 1:1 매핑으로 v0.1.1 토큰 + Tailwind + lucide 변환. 옵션 B 5탭 본문 시리즈(3A/3B/3C) 완결.

## What changed

### 1. lucide-react import 확장 (line 16)

Wave 10 기존 18종(Package/UtensilsCrossed/MoveDiagonal/ChevronLeft/ChevronRight/ChevronUp/ChevronDown/AlertTriangle/Wrench/X/AlertOctagon/ClipboardCheck/FileSearch/CheckCircle2/Camera/Loader2/ClipboardList/Search) + 신규 3종(User, BookOpen, Building2) = 21종. ElevatorIcon 은 line 17 `../components/ui/icons` 자체 컴포넌트 — 재import 0건.

### 2. ddayClass 헬퍼 신설 (line 1602~1608, fmtDday 통째 교체)

기존 `fmtDday(days)` → `{ text: string, color: 'var(--*)', bg: 'rgba(*,.12)' }` (var() + rgba 인라인 반환).
신규 `ddayClass(days)` → `{ text: className, bg: className, label: string }` (Tailwind className 반환).

4 분기 키워드 100% 보존:
- `days < 0` → `text-danger` / `bg-danger-bg` / `D+N 초과`
- `days <= 60` → `text-warning` / `bg-warning-bg` / `D-N`
- `days <= 365` → `text-info` / `bg-info-bg` / `D-N`
- `else (> 365)` → `text-safe` / `bg-safe-bg` / `D-N`

호출처 1건(`refreshDday = ddayClass(edu.refreshEdu.daysLeft)`) 시그니처 자동 호환.

### 3. 로딩 분기 (line 1595)

```diff
-<div style={{ textAlign:'center', padding:'40px 0', color:'var(--t3)', fontSize:12 }}>공단 데이터 조회 중...</div>
+<div className="text-center py-10 text-caption text-text-tertiary">공단 데이터 조회 중...</div>
```

### 4. 빈 분기 (line 1596)

```diff
-<EmptyState icon="👤" text="안전관리자 정보가 없어요" />
+<EmptyState icon={<User size={36} />} text="안전관리자 정보가 없어요" />
```

EmptyState 시그니처(line 2675 `icon: React.ReactNode`) 활용.

### 5. 프로필 카드 (line 1614~1633 → 1614~1634)

- wrapper `bg-surface-raised border border-border-default rounded-xl p-4 flex-shrink-0`
- 48×48 avatar `w-12 h-12 rounded-full bg-surface-sunken flex items-center justify-center flex-shrink-0` + `<User size={28} className="text-text-secondary" />`
- 이름 `text-[16px] font-bold text-text-primary`
- 부제 `text-caption text-text-tertiary mt-0.5` (11→12 격상)
- 2-col grid `grid grid-cols-2 gap-2`
- cell `bg-surface-sunken rounded-lg px-2.5 py-2`
- 라벨 `text-caption text-text-tertiary mb-0.5` (11→12 격상)
- 값 `text-body-sm font-bold text-text-primary` (11→14 격상)

### 6. 교육 현황 카드 (line 1635~1669 → 1636~1670)

- wrapper 동일 (bg-surface-raised border rounded-xl p-4)
- 헤더 `text-body-sm font-bold text-text-primary mb-3 flex items-center gap-1.5` + `<BookOpen size={16} className="text-text-secondary" />` + '교육 현황'
- 보수(재) row `bg-surface-sunken rounded-xl px-3 py-2.5 mb-2` + 라벨·D-day chip 1줄 + 유효기간·다음 마감 보조 텍스트
- D-day chip `text-caption font-bold ${refreshDday.text} ${refreshDday.bg} px-2 py-0.5 rounded-md` (ddayClass 4 분기)
- 신규 row 동일 패턴 — daysLeft<0 완료 chip (`text-safe bg-safe-bg`) / >=0 D-N chip (`text-warning bg-warning-bg`)
- 모든 보조 텍스트 `text-caption text-text-tertiary` (10/11 → 12 격상)

### 7. 공단 등록 현황 카드 (line 1671~1708 → 1672~1727)

- wrapper 동일
- 헤더 `text-body-sm font-bold text-text-primary mb-2 flex items-center gap-1.5` + `<Building2 size={16} className="text-text-secondary" />` + '공단 등록 현황'
- 등록 수 텍스트 `text-body-sm text-text-secondary mb-2.5` + `<span className="text-safe font-bold">{reg.registered}대</span>` + 미등록 `<span className="text-warning ml-1.5">`
- 호기 그리드 wrapper `grid grid-cols-[repeat(4,1fr)_6px_repeat(2,1fr)] gap-1 items-center` (Tailwind arbitrary)
- 헤더 행 1 (col-span-4 ElevatorIcon size=14 strokeWidth=1.8 + '엘리베이터')
- 6px sep `<div />`
- 헤더 행 2 (col-span-2 MoveDiagonal size=14 + '에스컬레이터')
- 헤더 텍스트 `text-caption font-bold text-text-tertiary tracking-wider` (9→12 격상)
- chip 함수: `text-caption font-semibold px-2 py-0.5 rounded-md text-center inline-flex items-center justify-center gap-0.5` + `${isReg ? 'bg-safe-bg text-safe' : 'bg-warning-bg text-warning'}` + 'EV{n}'/'ES{n}' 라벨 보존 + `CheckCircle2 size=11 strokeWidth=2.5` (isReg) 또는 `X size=11 strokeWidth=2.5` (!isReg)

## Why

옵션 B 3C sketch (cf538a3 safety-mgr-sketch.html, 1596 라인) 사용자 검수 OK. 1:1 매핑 변환. 옵션 B 5탭 본문 시리즈 완결 (3A=Wave 9, 3B=Wave 10, 3C=Wave 11). 이후 사용자 검수 → main 머지 → redesign/07-elevator 페이지 완료.

## Preserved (비즈니스 로직 100% 보존)

- `safetyMgrQuery` / `safetyMgrQuery.data` / `safetyMgrQuery.isLoading`
- `data.manager`: m.realName, m.maskedName, m.appointedAt, m.eduDate, m.eduValidFrom, m.eduValidTo
- `data.education`: edu.refreshEdu.daysLeft + deadline, edu.newEdu.daysLeft + deadline
- `data.registration`: reg.total, reg.registered, reg.registeredIds
- D-day 4 분기 키워드(`days < 0`, `days <= 60`, `days <= 365`) 표현식 그대로
- 등록 chip 2 분기 (`reg.registeredIds.includes(evId)` → safe/warning)
- chip 라벨 'EV{n}'/'ES{n}' UI 라벨 보존 (escalator → 'ES' / else → 'EV')
- elevators / evMap / find 함수 / grid 정의 (3 rows × 7 cols, null 분리자) 그대로
- Wave 1~10 변환 결과 단 한 줄도 변경 X (list 탭 / 5 모달 / 헬퍼 4종 / 고장 탭 / 수리 탭 RepairListSection / FAB / 점검 탭 / 검사 탭 / KoelsaHistorySection.tsx)
- icons.tsx / tailwind.config.js / 다른 페이지 0건 수정

## Verification

| Gate | 결과 |
| --- | --- |
| Section A 라인 변동 | 3463 → 3482 (+19, 인라인 var() 제거 + lucide JSX 추가로 합리) |
| Section B var() 0건 in zone (1580~1730) | bg2/bd/bd2/t1/t2/t3/bg3/danger/safe/warn/info 모두 0 |
| Section C 본문 이모지 0건 in zone | 👤/📚/🏢/🛗/↕️/✓/✗ 모두 0 |
| Section D 9·10·11px 격상 in zone | text-[9px]/[10px]/[11px] + fontSize:9/10/11 모두 0 |
| Section E 인라인 style 속성 in zone | `style={{` 0건 (gridTemplateColumns 도 Tailwind arbitrary 로 처리) |
| Section F lucide 신규 3종 | User import 1 / 사용 3, BookOpen 2, Building2 2 |
| Section G 재사용 lucide in zone | ElevatorIcon 1 / MoveDiagonal 1 / CheckCircle2 1 / `<X size` 1 |
| Section H git status | ElevatorPage.tsx 1건만 modified |
| npm run build | PASS (TypeScript 0 에러, vite + PWA 모두 완료) |

Hard gate `test ... -eq 0` 모두 PASS.

## Commits

- `d97f1f9` feat(260516-027): Wave 11 — 옵션 B 3C 변환 (안전관리자 탭 본문) v0.1.1 토큰 + Tailwind + lucide

## Deviations from Plan

None — plan executed exactly as written. ddayClass 옵션 A 채택(plan 권장), Tailwind arbitrary 채택(plan 우선안), 16px arbitrary 유지(plan 권장) 모두 plan 의 권장 경로.

## Out of scope

- tailwind.config.js typography 토큰 추가 (16px 이름 `text-[16px]` arbitrary 유지)
- 안전관리자 데이터 자동 갱신 (cron 영역 — cbc-cron-worker 별도 프로젝트)
- 안전관리자 입력 UI (디스플레이 전용 — 메모리 룰 `project_elevator_page_tabs.md`)
- main 머지 / 배포 (redesign 브랜치 운영 룰 — 사용자 컨펌 필수)

## Visual impact

사용자 인지 sketch (cf538a3) 4 viewport 와 일치:

- 프로필 — User avatar (28px lucide) + 16px 이름 + 2-col grid (선임일/교육이수일)
- 교육 — BookOpen 헤더 + D-day 4 분기 색 (danger/warning/info/safe) chip + 신규 완료/D-N 칩
- 등록 — Building2 헤더 + 등록 수 safe·warning + 호기 그리드 ElevatorIcon/MoveDiagonal + chip CheckCircle2(safe)/X(warning)

노안 가독성 — 9·10·11px 모두 12·13·14px+ 격상.

## Next

사용자 검수 → 만족 시 main 머지 → redesign/07-elevator 페이지 완료. 옵션 B 시리즈 종결.

## Self-Check: PASSED

- `cha-bio-safety/src/pages/ElevatorPage.tsx` modified (Section H git status confirmed)
- Commit `d97f1f9` exists (`git log --oneline | grep d97f1f9` confirmed)
- npm build PASS (TypeScript 0 에러)
- Verify gate Section A~H 모두 PASS
