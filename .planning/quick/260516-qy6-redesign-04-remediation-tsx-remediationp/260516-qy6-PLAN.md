---
phase: 260516-qy6
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cha-bio-safety/src/pages/RemediationPage.tsx
autonomous: false
requirements:
  - REDESIGN-04-REMEDIATION-TSX-WAVE-1
must_haves:
  truths:
    - "RemediationPage 모바일/데스크톱 모두 sketch (커밋 feb8da9 remediation-sketch.html) 시각과 1:1 일치한다"
    - "비즈니스 로직(useQuery, useSearchParams, navigate, downloadReport/downloadPhoto, 필터, 카테고리 select, 기간 버튼, 상세 패널 선택, blink keyframe)이 단 한 줄도 변하지 않는다"
    - "옛 토큰(--bg/--bg2/--bg3/--bg4/--bd/--bd2/--t1/--t2/--t3/--acl/--danger/--warn/--safe/--warning) 인라인 사용이 RemediationPage.tsx 안에서 0건이다"
    - "카드 좌측 색바가 결과(result) 기준이 아니라 상태(status) 기준으로 칠해진다 — 미조치=fire bar, 완료=safe bar"
    - "결과 배지(불량/주의)·상태 배지(미조치/완료)가 sketch §VP1~VP8 토큰 페어와 정확히 매칭된다"
    - "보고서 다운로드 버튼이 단색 var(--accent)이고 그라디언트 사용이 0건이다"
    - "사진 보조 다운로드 버튼이 11px → 12px 로 상향되어 9·10·11px 사용 0건이다"
    - "데스크톱 우측 빈 슬롯에 '좌측에서 항목을 선택하세요' 가이드가 표시된다"
    - "이모지(📄📷⬇)가 모두 lucide-react 아이콘으로 치환되어 0건이다"
    - "npm run build 가 0 TypeScript 에러로 통과한다"
  artifacts:
    - path: "cha-bio-safety/src/pages/RemediationPage.tsx"
      provides: "v0.1.1 토큰 + Tailwind + lucide 적용된 RemediationPage"
      min_lines: 400
  key_links:
    - from: "cha-bio-safety/src/pages/RemediationPage.tsx"
      to: "cha-bio-safety/src/styles/tokens.css"
      via: "var(--surface-*/text-*/border-*/status-*/accent) 토큰 참조"
      pattern: "var\\(--surface-|var\\(--text-(primary|secondary|tertiary)|var\\(--status-|var\\(--accent\\)|var\\(--border-"
    - from: "cha-bio-safety/src/pages/RemediationPage.tsx"
      to: "lucide-react"
      via: "import { Inbox, AlertCircle, Download, Camera } from 'lucide-react'"
      pattern: "from ['\\\"]lucide-react['\\\"]"
---

<objective>
RemediationPage.tsx (567줄) 를 sketch (`remediation-sketch.html` 커밋 feb8da9) 와 1:1 매핑되도록 v0.1.1 토큰 + Tailwind utility + lucide-react 로 재작성한다.

**SOURCE OF TRUTH:** `cha-bio-safety/docs/redesign-context/04-remediation/sketch/remediation-sketch.html` (1504줄, VP1~VP8).
이 파일의 `<style>` 블록 안 `.rem-*` 클래스 정의가 모든 시각 결정의 권위. 추측 금지, 토큰 verbatim 인용 (메모 feedback_planner_prompt_sketch_verbatim 강제).

**ZERO BUSINESS LOGIC CHANGE:** useQuery 두 개, useSearchParams `?tab=open|resolved|all`, useState (categoryFilter/days/selectedId), effectiveSelectedId 셀렉트 로직, useIsDesktop 분기, downloadReport/downloadPhoto/fetchPhotoAsBase64 함수 본문, ZONE_LABEL/recordPlace/SKELETON_STYLE, STATUS_TABS/PERIOD_BUTTONS 상수, navigate('/remediation/' + id), `@keyframes blink` 스타일 태그 — 모두 그대로 둔다. UI 마크업과 className/style 만 교체.

Purpose: 03-qr-scan TSX Wave 1 (커밋 7552bdb) 변환 패턴을 04-remediation 에 동일하게 mirror 해서 4번째 페이지의 v0.1.1 표준화를 완결.

Output: `cha-bio-safety/src/pages/RemediationPage.tsx` 단일 파일 교체본. var() 옛 토큰 0건, 9·10·11px 0건 (보조 사진 버튼 11→12 상향 제외 없음, 0건 보장), 이모지 0건, layout-only 인라인 style 은 Tailwind className 매핑 우선 / 회피 시 화이트리스트 사유 주석 명시.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@cha-bio-safety/src/pages/RemediationPage.tsx
@cha-bio-safety/docs/redesign-context/04-remediation/sketch/remediation-sketch.html
@cha-bio-safety/docs/redesign-context/04-remediation/04-remediation.md
@cha-bio-safety/docs/redesign-context/04-remediation/design-system.md
@cha-bio-safety/docs/redesign-context/04-remediation/tokens.css
@cha-bio-safety/src/styles/tokens.css
@cha-bio-safety/tailwind.config.js
@cha-bio-safety/src/pages/QRScanPage.tsx

<sketch_css_verbatim>
다음은 sketch (`remediation-sketch.html`) `<style>` 블록에서 직접 추출한 RemediationPage 전용 CSS 정의다. 변환 시 이 값들을 그대로 className 또는 인라인 style 로 옮긴다. 추측 금지.

### 페이지 헤더 (`.rem-page-hd` / `.rem-page-hd-title`) — sketch L299-L315
- 컨테이너: `height: 52px; flex-shrink: 0; display: flex; align-items: center; justify-content: space-between; gap: 12px; padding: 0 16px; background: var(--surface-raised); border-bottom: 1px solid var(--border-default);`
- 타이틀: `font-size: 18px; font-weight: 700; color: var(--text-primary); line-height: 1.2;`
- **주의:** 현재 RemediationPage.tsx 는 페이지 헤더 마크업이 없다 (App.tsx GlobalHeader 사용). sketch 의 `rem-page-hd` 는 시안 안에서 viewport-frame 안의 자체 헤더 시각 시연용. TSX 변환에서는 **추가하지 않는다** — 현재 페이지가 GlobalHeader 외부 헤더 없는 구조라면 그대로 둔다. (사용자 컨펌 필요 시 checkpoint 에서 묻기.)

### 필터 바 (`.rem-filter-bar`) — sketch L318-L325
- `flex-shrink: 0; background: var(--surface-raised); border-bottom: 1px solid var(--border-default); position: sticky; top: 0; z-index: 10;`

### 탭 (`.rem-tabs` / `.rem-tab` / `.rem-tab.is-active`) — sketch L328-L352
- 탭 컨테이너: `display: flex; height: 44px; border-bottom: 1px solid var(--border-default);`
- 탭 (비활성): `flex: 1; display: flex; align-items: center; justify-content: center; font-size: 13px; font-weight: 600; cursor: pointer; border: none; background: transparent; color: var(--text-tertiary); border-bottom: 2px solid transparent; transition: color .13s;`
- 탭 (활성): `background: var(--surface-active); color: var(--text-primary); border-bottom: 2px solid var(--accent);`

### 필터 로우 (`.rem-filter-row` / `.rem-cat-select` / `.rem-period-btn`) — sketch L355-L389
- 로우: `display: flex; align-items: center; gap: 8px; padding: 8px 16px;`
- select: `height: 36px; background: var(--surface-sunken); border: 1px solid var(--border-strong); border-radius: var(--radius-sm) [=8px]; color: var(--text-primary); font-size: 12px; padding: 0 8px; flex: 1; min-width: 0;`
- 기간 버튼 (비활성): `height: 32px; padding: 0 12px; border-radius: var(--radius-sm) [=8px]; font-size: 12px; font-weight: 700; border: none; cursor: pointer; background: transparent; color: var(--text-tertiary); white-space: nowrap;`
- 기간 버튼 (활성): `background: var(--surface-active); color: var(--text-primary);`

### 목록 (`.rem-list`) — sketch L392-L402
- `overflow-y: auto; padding: 12px 16px; display: flex; flex-direction: column; gap: 8px; flex: 1; min-height: 0;` + 스크롤바 숨김 (`::-webkit-scrollbar { display: none } scrollbar-width: none`)

### 카드 (`.rem-card` / `.rem-card.is-selected`) — sketch L405-L417
- 카드: `display: flex; gap: 10px; padding: 12px; background: var(--surface-sunken); border: 1px solid var(--border-default); border-radius: var(--radius-md) [=12px]; cursor: pointer; position: relative;`
- 선택됨 (데스크톱): `border: 2px solid var(--accent);` (border-default → accent 로 교체, 두께 1→2)

### 카드 좌측 색바 (`.rem-card-bar.fire` / `.rem-card-bar.safe`) — sketch L420-L427
- 바: `width: 4px; flex-shrink: 0; align-self: stretch; border-radius: 2px;`
- 미조치 (`status === 'open'`): `background: var(--status-fire-bar)` (sketch CSS class `.fire`)
- 완료 (`status === 'resolved'`): `background: var(--status-safe-bar)` (sketch CSS class `.safe`)
- **결정 잠금:** 옛 코드의 `record.result === 'bad' ? --danger : --warn` 색바 룰은 **폐기**. 새 룰은 **status 기준** (`record.status === 'open' ? fire-bar : safe-bar`).

### 배지 (`.rem-badge.{danger|warning|fire|safe}`) — sketch L430-L444
- 공통: `font-size: 12px; font-weight: 700; padding: 2px 6px; border-radius: 5px; line-height: 1; white-space: nowrap; flex-shrink: 0; display: inline-flex; align-items: center;`
- 불량 (`result === 'bad'`): `background: var(--status-danger-bg); color: var(--status-danger);`
- 주의 (`result === 'caution'`): `background: var(--status-warning-bg); color: var(--status-warning);`
- 미조치 (`status === 'open'`): `background: var(--status-fire-bg); color: var(--status-fire);`
- 완료 (`status === 'resolved'`): `background: var(--status-safe-bg); color: var(--status-safe);`
- **leading-none 핵심:** `line-height: 1` 명시 — 메모 feedback_text_caption_leading_none 강제 (text-caption 1.5 가 12px 배지 안에서 시각 패딩 야기).

### 스켈레톤 (`.rem-skeleton`) — sketch L447-L452
- `background: var(--surface-sunken); border-radius: var(--radius-md) [=12px]; height: 88px; flex-shrink: 0;`
- 애니메이션: 기존 `@keyframes blink` 스타일 태그 그대로 (`opacity .6 → .3 → .6`, 2s).

### 빈 상태 (`.rem-empty`) — sketch L455-L464
- `display: flex; flex-direction: column; align-items: center; justify-content: center; gap: 8px; padding: 60px 16px; color: var(--text-secondary); flex: 1;`
- 안: lucide `Inbox` icon-3xl (`width: 36px; height: 36px`) `color: var(--text-tertiary)` + text-body bold `var(--text-primary)` + text-caption `var(--text-secondary)` center

### 에러 상태 (`.rem-error`) — sketch L467-L476
- `text-align: center; padding: 40px 16px; color: var(--text-secondary); font-size: 14px; display: flex; flex-direction: column; align-items: center; gap: 8px;`
- 안: lucide `AlertCircle` icon-2xl (`28px`) `color: var(--status-danger-bar); margin-bottom: 8px;` + text-body-sm `var(--text-secondary)` + text-caption `var(--text-tertiary)`

### 데스크톱 상세 패널 (`.rem-detail-pane` / `.rem-detail-empty`) — sketch L479-L495
- pane: `overflow-y: auto; padding: 20px 28px; height: 100%; box-sizing: border-box;` + 스크롤바 숨김
- empty: `height: 100%; display: flex; align-items: center; justify-content: center; color: var(--text-tertiary); font-size: 13px;` 텍스트 `"좌측에서 항목을 선택하세요"`

### 상세 헤더 (`.rem-detail-hd` / `.rem-detail-title` / `.rem-detail-sub`) — sketch L497-L512
- 컨테이너: `display: flex; align-items: flex-start; gap: 12px; margin-bottom: 16px;`
- 타이틀: `font-size: 18px; font-weight: 700; color: var(--text-primary);`
- 서브: `font-size: 12px; color: var(--text-tertiary); margin-top: 2px;`

### 보고서 다운로드 버튼 (`.rem-download-btn`) — sketch L515-L530
- `padding: 8px 14px; border-radius: var(--radius-sm) [=8px]; border: none; background: var(--accent); color: var(--text-on-accent); font-size: 12px; font-weight: 700; cursor: pointer; display: inline-flex; align-items: center; gap: 4px; white-space: nowrap; flex-shrink: 0;`
- 안: lucide `Download` icon-sm (14px) + "보고서 다운로드"
- **결정 잠금:** 옛 `linear-gradient(135deg,#1d4ed8,#2563eb)` **폐기** → 단색 `var(--accent)`. 이모지 `📄` 제거.

### KV 테이블 (`.rem-kv-table` / `th` / `td`) — sketch L533-L558
- 테이블: `width: 100%; border-collapse: collapse; margin-bottom: 20px;`
- th: `width: 110px; padding: 8px 12px; background: var(--surface-sunken); border: 1px solid var(--border-default); font-size: 12px; font-weight: 700; color: var(--text-secondary); text-align: left; vertical-align: top; line-height: 1.5;`
- td: `padding: 8px 12px; border: 1px solid var(--border-default); font-size: 13px; color: var(--text-primary); white-space: pre-wrap; vertical-align: top; line-height: 1.6;`

### 사진 그리드 (`.rem-photo-grid` / `.rem-photo-card` / `.rem-photo-hd` / `.rem-photo-label` / `.rem-photo-dl-btn` / `.rem-photo-img` / `.rem-photo-empty`) — sketch L561-L619
- 그리드: `display: grid; grid-template-columns: 1fr 1fr; gap: 16px;`
- 카드: `border: 1px solid var(--border-default); border-radius: 10px; padding: 12px; background: var(--surface-raised);`
- 헤더: `display: flex; align-items: center; justify-content: space-between; margin-bottom: 8px;`
- 라벨: `font-size: 12px; font-weight: 700; color: var(--text-secondary); display: inline-flex; align-items: center; gap: 4px;` (안에 lucide `Camera` icon-xs `12px`)
- 다운로드 보조 버튼: `padding: 4px 10px; border-radius: 6px; border: 1px solid var(--border-strong); background: var(--surface-sunken); color: var(--text-secondary); font-size: 11px; font-weight: 700; cursor: pointer; display: inline-flex; align-items: center; gap: 3px; line-height: 1;` 안에 lucide `Download` icon-xs (12px) + "다운로드"
- **결정 잠금:** sketch 주석 (L593) 에 명시된 노안 룰 옵션 → `font-size: 11px` 를 **12px 로 상향**한다. 이모지 `⬇` 제거.
- 이미지: `width: 100%; max-height: 280px; object-fit: contain; border-radius: 6px; background: #000; display: block;`
- 빈 사진: `height: 180px; display: flex; align-items: center; justify-content: center; color: var(--text-tertiary); font-size: 12px; background: var(--surface-sunken); border-radius: 6px;`

### 데스크톱 분할 (좌 50% / 우 50%) — sketch L964, L1054
- 좌측: `width: 50%; flex-shrink: 0; min-width: 0; border-right: 1px solid var(--border-default); display: flex; flex-direction: column; height: 100%;`
- 우측: `width: 50%; flex-shrink: 0; min-width: 0; height: 100%; overflow: hidden;` (안의 `.rem-detail-pane` 가 자체 overflow-y:auto)

### 상태 색 매핑 요약표 (참고)
| 옛 인라인 (RemediationPage L186-L202, L308-L316) | v0.1.1 신규 |
|---|---|
| `rgba(239,68,68,.13)` + `var(--danger)` | `var(--status-danger-bg)` + `var(--status-danger)` |
| `rgba(245,158,11,.13)` + `var(--warn)` | `var(--status-warning-bg)` + `var(--status-warning)` |
| `rgba(249,115,22,.15)` + `var(--danger)` (옛 미조치) | `var(--status-fire-bg)` + `var(--status-fire)` |
| `rgba(34,197,94,.13)` + `var(--safe)` (옛 완료) | `var(--status-safe-bg)` + `var(--status-safe)` |
| `'2px solid var(--danger)'` 색바 (불량) | `var(--status-fire-bar)` (미조치 기준) |
| `'2px solid var(--warn)'` 색바 (주의) | `var(--status-safe-bar)` (완료 기준) — 단, status 가 'open' 이면 fire-bar |

### lucide-react 매핑 (sketch verbatim)
| sketch 위치 | lucide 컴포넌트 | size |
|---|---|---|
| L850, L1163 빈 상태 | `Inbox` | 36 |
| L940 에러 | `AlertCircle` | 28 |
| L1064, L1407 보고서 다운로드 | `Download` | 14 |
| L1107, L1121, L1467, L1481 사진 카드 헤더 | `Camera` | 12 |
| L1111, L1471 사진 보조 다운로드 | `Download` | 12 |

</sketch_css_verbatim>

<reference_pattern>
03-qr-scan TSX Wave 1 (커밋 7552bdb, QRScanPage.tsx) 패턴 mirror:
- `import { ... } from 'lucide-react'` 페이지 상단
- 옛 `var(--bg)/var(--t1)/var(--acl)` → Tailwind `bg-surface-page text-text-primary text-accent` 또는 인라인 `var(--surface-page)/var(--text-primary)/var(--accent)`
- 작은 컨테이너 안 text-caption → `leading-none` 명시 (메모 feedback_text_caption_leading_none)
- `bg-danger-bg border border-danger-bar/40` 형태로 status 페어 활용
- 단색 accent 버튼: `bg-accent text-on-accent`
- 비즈니스 로직(state/queries/handlers)은 라인 단위 그대로 보존

이 패턴을 RemediationPage 에 그대로 적용.
</reference_pattern>

</context>

<tasks>

<task type="auto">
  <name>Task 1: RemediationPage.tsx 전체 변환 (v0.1.1 토큰 + Tailwind + lucide)</name>
  <files>cha-bio-safety/src/pages/RemediationPage.tsx</files>
  <action>
**SOURCE OF TRUTH 우선:** `cha-bio-safety/docs/redesign-context/04-remediation/sketch/remediation-sketch.html` (커밋 feb8da9) 의 `<style>` 블록과 VP1~VP8 마크업이 모든 시각 결정의 권위. 모든 px / color / radius / padding 값은 `<sketch_css_verbatim>` 에 추출된 verbatim 값을 그대로 인용. 추측·근사 금지.

**비즈니스 로직 절대 보존 (단 한 줄도 변경 금지):**
- L1-L9: 모든 import (`useState`, `useNavigate`/`useSearchParams`, `useQuery`, `remediationApi`, `useIsDesktop`, `toast`, `fmtKstDate`/`fmtKstDateTime`)
- L10-L19: `ZONE_LABEL`, `zoneLabel`, `recordPlace` 함수 본문
- L21-L51: `downloadPhoto`, `fetchPhotoAsBase64` 함수 본문
- L53-L105: `downloadReport` 함수 본문 — HTML 템플릿 문자열 안의 `style` 태그, `<table>`, `<tr>`, `class="badge ..."`, 이모지 `📷` 등은 **클라이언트 생성 HTML 보고서 안의 것이라 절대 변경 금지** (다운로드 결과물 형식)
- L114-L147: `RemediationPage` 함수 상단의 `useNavigate`, `useIsDesktop`, `useSearchParams`, `statusTab`, `setStatusTab`, `categoryFilter`, `days`, `selectedId` state + `useQuery` 두 개 + `effectiveSelectedId` 셀렉트 로직
- L149-L160: `STATUS_TABS`, `PERIOD_BUTTONS` 상수
- L166, L495 의 `onClick` 핸들러 — `isDesktop` 분기, `navigate('/remediation/' + record.id)`, `setSelectedId(record.id)` 모두 그대로

**제거 / 폐기 대상:**
- L107-L112: `SKELETON_STYLE` 객체 안의 `background: 'var(--bg3)'` — `'var(--surface-sunken)'` 로 교체 (BLINK 키프레임 그대로)
- L177: 카드 좌측 borderLeft 의 `record.result === 'bad' ? '4px solid var(--danger)' : '4px solid var(--warn)'` 룰 → **status 기준** `record.status === 'open' ? 'var(--status-fire-bar)' : 'var(--status-safe-bar)'` (단, 데스크톱 selected 일 때는 sketch `.rem-card.is-selected` 룰 = `border: 2px solid var(--accent)`)
- L286: `background: 'linear-gradient(135deg,#1d4ed8,#2563eb)'` + `📄` 이모지 → 단색 `var(--accent)` + lucide `<Download size={14} />`
- L353, L370: 사진 보조 버튼 `fontSize: 10` + `⬇` 이모지 → `fontSize: 12` + lucide `<Download size={12} />` (sketch 주석 L593 노안 룰 명시)
- L350, L367: 사진 라벨 `📷` 이모지 → lucide `<Camera size={12} />`
- L487-L489, L262-L265: 빈 상태 → lucide `<Inbox size={36} className="text-text-tertiary" />` 추가 (sketch L850, L1163)
- L481, L259: 에러 상태 → lucide `<AlertCircle size={28} className="text-danger-bar mb-2" />` 추가 (sketch L940)

**옛 토큰 → v0.1.1 일괄 치환 표 (RemediationPage.tsx 안 모든 등장 위치):**
| 옛 (src 인라인) | v0.1.1 (인라인 또는 Tailwind 클래스) |
|---|---|
| `var(--bg)` | `var(--surface-page)` / `bg-surface-page` |
| `var(--bg2)` | `var(--surface-raised)` / `bg-surface-raised` |
| `var(--bg3)` | `var(--surface-sunken)` / `bg-surface-sunken` |
| `var(--bg4)` | `var(--surface-active)` / `bg-surface-active` |
| `var(--bd)` | `var(--border-default)` / `border-border-default` |
| `var(--bd2)` | `var(--border-strong)` / `border-border-strong` |
| `var(--t1)` | `var(--text-primary)` / `text-text-primary` |
| `var(--t2)` | `var(--text-secondary)` / `text-text-secondary` |
| `var(--t3)` | `var(--text-tertiary)` / `text-text-tertiary` |
| `var(--acl)` | `var(--accent)` / `text-accent` / `bg-accent` |
| `var(--danger)` | `var(--status-danger)` (text/배지) 또는 `var(--status-danger-bar)` (테두리) — 컨텍스트에 맞춰 |
| `var(--warn)` | `var(--status-warning)` 또는 `var(--status-warning-bar)` |
| `var(--safe)` | `var(--status-safe)` 또는 `var(--status-safe-bar)` |
| `rgba(239,68,68,.13)` | `var(--status-danger-bg)` |
| `rgba(245,158,11,.13)` | `var(--status-warning-bg)` |
| `rgba(249,115,22,.15)` | `var(--status-fire-bg)` |
| `rgba(34,197,94,.13)` | `var(--status-safe-bg)` |

**구조적 변환 영역 (sketch verbatim 그대로):**

1. **모바일 컨테이너 (L391):** 기존 `style={{ flex:1, display:'flex', flexDirection:'column', background:'var(--bg)', minHeight:0, overflow:'hidden' }}` → `className="flex-1 flex flex-col min-h-0 overflow-hidden bg-surface-page"`. blink keyframe `<style>` 태그 그대로 유지.

2. **필터 바 (L395-L467):** sketch `.rem-filter-bar` + `.rem-tabs` + `.rem-tab` + `.rem-filter-row` + `.rem-cat-select` + `.rem-period-btn` 1:1.
   - 탭 폰트: `font-size: 13px; font-weight: 600;` (sketch L338-L339, **옛 12/700 → 13/600 으로 sketch 인용 정합**)
   - 활성 탭 배경: `var(--surface-active)`, 비활성: `transparent`
   - 활성 탭 border-bottom: `2px solid var(--accent)`
   - select height 36px, border `1px solid var(--border-strong)`, radius 8px, font-size 12px
   - 기간 버튼 height 32px, padding `0 12px`, radius 8px, 비활성 background transparent + color tertiary, 활성 background surface-active + color text-primary
   - **모바일 페이지 헤더 sketch L299 의 `rem-page-hd` 추가하지 않음** — 현재 RemediationPage 는 GlobalHeader 사용 (페이지 헤더 마크업 없음). 사용자에게 컨펌 받기 전 추가 금지.

3. **카드 (L492-L562 모바일, L163-L207 데스크톱):** 단일 `renderCard` 함수 통합 권장 (Tailwind className 기반). 비즈니스 로직(`onClick`, `key`)은 그대로.
   - 카드 컨테이너: sketch `.rem-card` = `bg-surface-sunken border border-border-default rounded-md p-3 flex gap-[10px] cursor-pointer` (padding 12px = `p-3`, gap 10px = `gap-[10px]`)
   - 데스크톱 선택 시: `border-2 border-accent` (sketch `.rem-card.is-selected`)
   - 좌측 색바: 별도 `<div>` 추가 `<div className="w-1 rounded-[2px]" style={{ background: record.status === 'open' ? 'var(--status-fire-bar)' : 'var(--status-safe-bar)', alignSelf: 'stretch', flexShrink: 0 }} />` (sketch `.rem-card-bar`, w-1 = 4px)
   - **옛 borderLeft 인라인 룰 제거** — 별도 색바 div 로 대체.
   - 카드 우측 컨텐츠 영역: `flex-1 min-w-0 flex flex-col gap-[3px]`
   - Line 1: 카테고리 `text-body-sm font-bold text-text-primary` + truncate, 결과 배지 (`.rem-badge.danger`/`.rem-badge.warning`)
   - Line 2: 위치 `text-caption text-text-secondary`
   - Line 3: 메모 `text-caption` + truncate, 컬러 conditional (`memo ? text-text-secondary : text-text-tertiary`)
   - Line 4: 날짜 `text-caption text-text-tertiary`, 상태 배지 (`.rem-badge.fire`/`.rem-badge.safe`)
   - 배지: 모두 `text-caption font-bold px-1.5 py-0.5 rounded-[5px] leading-none whitespace-nowrap shrink-0 inline-flex items-center` + status 페어 (예: `bg-danger-bg text-danger`, `bg-warning-bg text-warning`, `bg-fire-bg text-fire`, `bg-safe-bg text-safe`). **`leading-none` 명시 필수** (메모 feedback_text_caption_leading_none).

4. **목록 영역 (L470 / L256):** `.rem-list` = `flex-1 min-h-0 overflow-y-auto px-4 py-3 flex flex-col gap-2` + 스크롤바 숨김 (sketch L401-L402: `::-webkit-scrollbar { display: none }` — Tailwind `scrollbar-hide` 또는 인라인 `[&::-webkit-scrollbar]:hidden [scrollbar-width:none]` — QRScanPage 패턴 mirror)

5. **스켈레톤 (L107-L112):** sketch `.rem-skeleton` 인용. `background: var(--surface-sunken); border-radius: 12px; height: 88px;` + `animation: blink 2s ease-in-out infinite` (blink keyframe 인라인 `<style>` 태그 그대로 유지). VP3 sketch L895-L897 은 두 번째/세 번째에 `animation-delay: 0.3s` / `0.6s` 적용했지만 옛 코드는 동일 delay — 옛 동작 보존(전체 동일 delay).

6. **빈 상태 (L485-L490 / L261-L266):** sketch `.rem-empty` 1:1.
   - 컨테이너: `flex-1 flex flex-col items-center justify-center gap-2 py-[60px] px-4`
   - lucide `<Inbox size={36} className="text-text-tertiary shrink-0" />`
   - 타이틀: `<div className="text-body font-bold text-text-primary">조치 항목 없음</div>` (sketch L851 `font-weight: 700`)
   - 서브: `<div className="text-caption text-text-secondary text-center">선택한 조건에 해당하는 불량/주의 항목이 없습니다.</div>`

7. **에러 상태 (L479-L483 / L258-L260):** sketch `.rem-error` 1:1.
   - 컨테이너: `text-center py-10 px-4 flex flex-col items-center gap-2`
   - lucide `<AlertCircle size={28} className="text-danger-bar mb-2 shrink-0" />`
   - 텍스트: `text-body-sm text-text-secondary` + (모바일에만 보조 줄 `text-caption text-text-tertiary` "화면을 당겨서 다시 시도하세요" — 옛 동작 보존)

8. **데스크톱 분할 (L246-L388):** sketch L964 + L1054 1:1.
   - 외부 컨테이너: `flex-1 flex flex-col bg-surface-page min-h-0 overflow-hidden`
   - 내부 split: `flex-1 flex overflow-hidden`
   - 좌측 50%: `style={{ width: '50%' }} className="shrink-0 min-w-0 border-r border-border-default flex flex-col h-full"` (width 50% 는 layout-only 인라인 화이트리스트 — Tailwind 에 `w-1/2` 가 있지만 sketch verbatim `width: 50%` 와 동일하므로 `w-1/2` 사용 가능. 둘 중 하나 선택 시 주석으로 사유.)
   - 우측 50%: `className="w-1/2 shrink-0 min-w-0 h-full overflow-hidden"` (sketch L1055: 우측은 overflow:hidden, 안의 detail-pane 이 overflow-y:auto)

9. **데스크톱 상세 패널 (L271-L385):** sketch `.rem-detail-pane` 1:1.
   - `!detail` 빈 슬롯: sketch L1172 → `<div className="h-full flex items-center justify-center text-text-tertiary" style={{ fontSize: '13px' }}>좌측에서 항목을 선택하세요</div>` (label 토큰 13px 매핑)
   - 상세 헤더 (sketch L1058-L1067): `flex items-start gap-3 mb-4` + 좌(타이틀/서브) + 우(다운로드 버튼)
     - 타이틀: `text-title font-bold text-text-primary` (18px = `text-title`)
     - 서브: `text-caption text-text-tertiary mt-0.5`
     - 다운로드 버튼: `<button className="px-3.5 py-2 rounded-sm bg-accent text-on-accent text-caption font-bold inline-flex items-center gap-1 shrink-0 whitespace-nowrap cursor-pointer border-0"><Download size={14} />보고서 다운로드</button>` (sketch L515-L530 정확, **그라디언트 폐기**)
   - KV 테이블 (sketch L1069-L1100): `.rem-kv-table` th/td 인용. `<table className="w-full border-collapse mb-5">` + th `style={{ width: 110, padding: '8px 12px', background: 'var(--surface-sunken)', border: '1px solid var(--border-default)', fontSize: 12, fontWeight: 700, color: 'var(--text-secondary)', textAlign: 'left', verticalAlign: 'top', lineHeight: 1.5 }}` + td `style={{ padding: '8px 12px', border: '1px solid var(--border-default)', fontSize: 13, color: 'var(--text-primary)', whiteSpace: 'pre-wrap', verticalAlign: 'top', lineHeight: 1.6 }}` (테이블 셀 인라인 style 은 화이트리스트 — sketch verbatim, Tailwind 매핑 시 가독성 손상. 주석 명시.)
     - **판정결과/상태 행** 안의 배지는 위 카드 배지 룰 동일 (`.rem-badge.danger|warning|fire|safe`).
   - 사진 그리드 (sketch L1102-L1127): `.rem-photo-grid` = `grid grid-cols-2 gap-4`. 각 photo-card = `border border-border-default rounded-[10px] p-3 bg-surface-raised`
     - 헤더: `flex items-center justify-between mb-2`
     - 라벨: `<span className="text-caption font-bold text-text-secondary inline-flex items-center gap-1"><Camera size={12} />조치 전 (불량)</span>` (sketch L1106 1:1, 이모지 폐기)
     - 보조 다운로드 버튼: `<button className="px-2.5 py-1 rounded-md border border-border-strong bg-surface-sunken text-text-secondary text-caption font-bold inline-flex items-center gap-[3px] leading-none cursor-pointer"><Download size={12} />다운로드</button>` (font-size 12px = `text-caption`, **노안 룰 11→12 상향 명시**)
     - 이미지: `<img className="w-full max-h-[280px] object-contain rounded-md bg-black block" />` (sketch L602-L609)
     - 빈 사진: `<div className="h-[180px] flex items-center justify-center text-text-tertiary text-caption bg-surface-sunken rounded-md">사진 없음</div>` (status 'open' 일 때 텍스트 "아직 조치 전")

**금지 사항:**
- var(--bg|bg2|bg3|bg4|bd|bd2|t1|t2|t3|acl|danger|warn|safe|warning) 0건 (downloadReport HTML 문자열 안은 예외 — 클라이언트 다운로드 결과물)
- 9·10·11px 0건 (사진 보조 버튼 11→12 강제)
- 이모지 (📄 📷 ⬇) 0건 (downloadReport HTML 문자열 안 `📷` 은 예외 — 다운로드 결과물)
- 그라디언트 0건 (downloadReport HTML 문자열 안 예외 동일)
- 인라인 style 토큰: 화이트리스트만 (`var(--surface-*)`, `var(--text-*)`, `var(--border-*)`, `var(--status-*)`, `var(--accent)`). 옛 토큰 사용 0건.
- layout-only 인라인 style 은 Tailwind className 매핑 우선. 회피 시 인접 줄에 주석 `// sketch verbatim — Tailwind 매핑 시 가독성 손상` 명시.

**비즈니스 로직 grep 검증 (변환 후):**
- `useQuery` 호출 2건 보존 (queryKey 그대로)
- `useSearchParams` + `setSearchParams(prev => ...)` 그대로
- `setSelectedId(record.id)` / `navigate('/remediation/' + record.id)` 그대로
- `downloadReport(detail)` / `downloadPhoto(...)` 호출 위치/인자 그대로
- `recordPlace(record)` / `fmtDate(record.checkedAt)` / `fmtDateTime(...)` 호출 그대로
- `effectiveSelectedId = selectedId ?? (isDesktop && records.length > 0 ? records[0].id : null)` 그대로
- `@keyframes blink { 0%,100%{opacity:.6} 50%{opacity:.3} }` 스타일 태그 그대로

**작업 순서:**
1. import 라인 수정 — `lucide-react` 에서 `Inbox`, `AlertCircle`, `Download`, `Camera` 추가
2. SKELETON_STYLE 의 `var(--bg3)` → `var(--surface-sunken)`
3. `renderCard` 함수 — 카드 좌측 색바 분리 div + 배지 모두 토큰 페어로 교체 + status 기준 색바 룰 적용
4. `filterBar` 함수 — 탭/select/기간버튼 토큰 교체 (탭 폰트 13/600 sketch 정합)
5. 모바일 return — 컨테이너 className 변환, 필터바/목록/카드/빈/에러/로딩 모두 sketch 토큰 1:1
6. 데스크톱 return — 50/50 split, 좌측 목록(`filterBar` 재사용), 우측 detail-pane (빈 슬롯 + 헤더 + 다운로드 버튼 단색 accent + KV 테이블 + 사진 그리드)
7. 모든 이모지 lucide 치환, 보조 다운로드 버튼 11→12 상향
8. `npm run build` 통과 확인

**한 번의 Edit 으로 전체 교체 vs 여러 번의 Edit 분할:** 567줄 단일 파일이라 한 번의 Write 또는 여러 번의 Edit 모두 가능. Edit 우선 (diff 추적 용이). 비즈니스 로직 라인을 정확히 보존하기 위해 함수 단위로 Edit (예: `renderCard`, `filterBar`, 모바일 return, 데스크톱 return).
  </action>
  <verify>
    <automated>
      cd /Users/jykevin/Documents/cbc7119-design/cha-bio-safety && \
      grep -v '^[[:space:]]*//\|^[[:space:]]*\*' src/pages/RemediationPage.tsx > /tmp/rem-stripped.tsx && \
      echo "--- 옛 토큰 잔존 (downloadReport HTML 문자열 안 제외 검증은 별도) ---" && \
      grep -c "var(--bg)\|var(--bg2)\|var(--bg3)\|var(--bg4)\|var(--bd)\|var(--bd2)\|var(--t1)\|var(--t2)\|var(--t3)\|var(--acl)" /tmp/rem-stripped.tsx && \
      echo "--- 옛 status 토큰 (downloadReport HTML 예외) ---" && \
      grep -nE "var\(--(danger|warn|safe|fire|warning)\)" /tmp/rem-stripped.tsx | grep -v "html =" | grep -v "background:#" && \
      echo "--- 9·10·11px (downloadReport HTML 예외 — '13px;' '12px;' 등은 OK) ---" && \
      grep -nE "fontSize:\s*(9|10|11)([^0-9]|$)|font-size:\s*(9|10|11)px" /tmp/rem-stripped.tsx && \
      echo "--- 이모지 (downloadReport HTML 안 📷 제외) ---" && \
      grep -nE "📄|⬇" /tmp/rem-stripped.tsx && \
      echo "--- 그라디언트 (downloadReport HTML 예외) ---" && \
      grep -n "linear-gradient" /tmp/rem-stripped.tsx | grep -v "html =" && \
      echo "--- lucide import ---" && \
      grep -E "from 'lucide-react'" src/pages/RemediationPage.tsx && \
      echo "--- 비즈니스 로직 보존 grep ---" && \
      grep -c "useQuery\|useSearchParams\|setSelectedId\|navigate(.*remediation\|downloadReport(detail)\|fetchPhotoAsBase64\|effectiveSelectedId\|@keyframes blink" src/pages/RemediationPage.tsx && \
      echo "--- TypeScript build ---" && \
      npm run build 2>&1 | tail -20
    </automated>
  </verify>
  <done>
    - 검증 grep: 옛 토큰 카운트 0 (downloadReport HTML 문자열 안 제외)
    - 9·10·11px 매치 0건 (fontSize / font-size 패턴)
    - 이모지 📄 ⬇ 매치 0건 (downloadReport HTML 안 📷 은 예외)
    - linear-gradient 매치 0건 (downloadReport HTML 안 예외)
    - `from 'lucide-react'` import 라인 1건 존재, `Inbox`, `AlertCircle`, `Download`, `Camera` 모두 포함
    - 비즈니스 로직 grep 카운트 ≥ 7 (모든 핵심 호출 보존)
    - `npm run build` exit 0 (TypeScript 0 에러)
    - sketch verbatim 인용 검증: 카드 배지 4종 (`bg-danger-bg`, `bg-warning-bg`, `bg-fire-bg`, `bg-safe-bg`) 모두 등장, 좌측 색바 status 기준 (`status === 'open' ? fire-bar : safe-bar`) 적용, 보고서 다운로드 버튼 `bg-accent` 단색 사용
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    RemediationPage.tsx 가 sketch (`remediation-sketch.html` VP1~VP8) 와 1:1 매핑된 v0.1.1 토큰 + Tailwind + lucide 변환을 마쳤다.

    주요 시각 변경:
    1. 카드 좌측 색바 — 결과(불량/주의) 기준 → **상태(미조치/완료) 기준** (fire-bar / safe-bar)
    2. 결과 배지 (불량/주의) — 인라인 rgba → `--status-danger-bg+danger` / `--status-warning-bg+warning` 페어
    3. 상태 배지 (미조치/완료) — 인라인 rgba → `--status-fire-bg+fire` / `--status-safe-bg+safe` 페어
    4. 보고서 다운로드 버튼 — 그라디언트 + 이모지 📄 → **단색 var(--accent) + lucide Download**
    5. 사진 영역 — 📷⬇ 이모지 → lucide Camera/Download, 보조 다운로드 버튼 11px → 12px (노안 룰)
    6. 데스크톱 우측 빈 슬롯 — "좌측에서 항목을 선택하세요" 가이드 표시
    7. 빈 상태 / 에러 상태 — lucide Inbox / AlertCircle 추가
    8. 모든 옛 토큰 (--bg/--t1/--bd/--acl/--danger 등) → v0.1.1 (--surface-*/--text-*/--border-*/--accent/--status-*)

    비즈니스 로직 (useQuery, useSearchParams, navigate, downloadReport, 필터, 상세 패널 선택, blink 애니메이션, downloadReport HTML 문자열 안의 옛 스타일) 은 100% 보존.

    배포된 도메인: https://cbc7119-preview.pages.dev/remediation (main 머지 후 자동 배포)
  </what-built>
  <how-to-verify>
    1. **로컬 dev 서버 또는 cbc7119-preview 배포 확인** (사용자 결정):
       - 모바일 viewport (393×852) 다크 모드 — 탭 "미조치" 선택, 카드 6개 mix (불량/주의 × 미조치/완료) 시각 확인
       - 모바일 빈 상태 — 카테고리/기간으로 결과 없게 필터 → Inbox 아이콘 + "조치 항목 없음" 확인
       - 모바일 에러 상태 — 네트워크 끊고 새로고침 → AlertCircle + "목록을 불러오지 못했습니다" 확인
       - 데스크톱 viewport (1280×720) 다크/라이트 — 좌(50%) 목록 + 우(50%) 상세 패널 + 보고서 다운로드 단색 accent 버튼 확인
       - 데스크톱 우측 빈 슬롯 — 선택 안 한 상태에서 "좌측에서 항목을 선택하세요" 확인
       - 카드 좌측 색바 — 미조치 카드는 주황(fire), 완료 카드는 초록(safe) 인지 확인 (옛 빨강/노랑 폐기 확인)
       - 보고서 다운로드 버튼 누르면 HTML 파일 다운로드 + 안의 콘텐츠/스타일 그대로 작동
       - 사진 다운로드 버튼 12px 크기 적절한지 확인
       - 라이트 모드에서도 동일 시각

    2. **시안 대비 검증 (sketch verbatim 룰):**
       - sketch (`docs/redesign-context/04-remediation/sketch/remediation-sketch.html`) 를 브라우저에서 열고 VP1~VP8 와 실제 페이지를 나란히 비교
       - 카드/배지/필터/탭/상세 패널/사진 그리드 모든 시각이 일치하는지

    3. **GSD 검수 룰 그대로 통과 확인 (Task 1 verify grep 모두 0/expected):**
       - 옛 토큰 0건
       - 9·10·11px 0건
       - 이모지 (📄⬇) 0건 (downloadReport HTML 안 📷 예외)
       - npm run build TypeScript 0 에러

    4. **메모 룰 강제 (사용자 컨펌 영역):**
       - 데스크톱 페이지 헤더 추가 안 함 (현재 GlobalHeader 사용) — 추가 필요한지 사용자 판단
       - 단일 `RemediationCard` 컴포넌트 추출 vs 인라인 유지 (현재 plan 은 `renderCard` 함수 통합 유지) — 별도 컴포넌트 분리 원하는지 사용자 판단
       - 사진 보조 버튼 12px 상향 결정 (sketch 주석 옵션) — 옵션이지만 적용함. 보존 원하는지 확인
  </how-to-verify>
  <resume-signal>"approved" 또는 수정 사항 (예: "보조 버튼 11px 복원" / "카드 색바 결과 기준 복원" / "단일 컴포넌트 분리")</resume-signal>
</task>

</tasks>

<verification>
- Task 1 verify grep 모두 통과 (옛 토큰 0, 11px 0, 이모지 0, build 0 에러)
- Task 2 사용자 시각 검수 통과
- sketch (커밋 feb8da9) VP1~VP8 와 실제 페이지 1:1 매핑
- 비즈니스 로직 grep 카운트 ≥ 7 보존
</verification>

<success_criteria>
- RemediationPage.tsx 가 v0.1.1 토큰 + Tailwind utility + lucide-react 로 완전 변환
- 카드 좌측 색바가 status 기준 (미조치=fire / 완료=safe) 으로 작동
- 보고서 다운로드 버튼 단색 var(--accent)
- 데스크톱 우측 빈 슬롯 가이드 표시
- 사진 보조 버튼 12px 상향
- 모든 검수 룰 통과 (Task 1 verify + Task 2 시각 컨펌)
- 비즈니스 로직 100% 보존 (useQuery / useSearchParams / navigate / downloadReport / blink keyframe / downloadReport HTML 결과물 변경 없음)
</success_criteria>

<output>
After completion, create `.planning/quick/260516-qy6-redesign-04-remediation-tsx-remediationp/260516-qy6-SUMMARY.md`
</output>
