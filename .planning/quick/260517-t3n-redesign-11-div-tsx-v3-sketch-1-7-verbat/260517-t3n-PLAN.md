---
phase: quick-260517-t3n
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cha-bio-safety/src/pages/DivPage.tsx
autonomous: false
requirements:
  - REDESIGN-11-DIV-V3-TSX
must_haves:
  truths:
    - "모바일 헤더 + 4탭 + 측정점 카드 (정상/주의/이상) + IntervalBar 카드 + 상세 모달이 sketch 1~6 verbatim 시각으로 표시"
    - "데스크톱 헤더 + 알림 배너 + 매트릭스 + 우측 패널 (빈 상태/선택 상태)이 sketch 7a~7c verbatim 시각으로 표시"
    - "옛 var() 토큰 (--bg/--bg2/--bg3/--bd/--t1/--t2/--t3/--acl/--safe/--warn/--danger/--info/--fire) 0건"
    - "raw hex (#3b82f6/#f97316/#22c55e/#38bdf8/#f59e0b) 0건 잔존 (단 #8b4513 brown 만 IntervalBar/RightPanel 1~3건 예외 허용)"
    - "비즈니스 로직 (useQuery 7개 / pressureStatus / FLOOR_GROUPS / setSelDiv / setTab / fetchLogs) 100% 보존"
    - "tsc + build 통과"
  artifacts:
    - path: "cha-bio-safety/src/pages/DivPage.tsx"
      provides: "v3 sketch verbatim TSX (className + tokens + lucide icons)"
      contains: "from 'lucide-react'"
    - path: ".planning/quick/260517-t3n-redesign-11-div-tsx-v3-sketch-1-7-verbat/260517-t3n-SUMMARY.md"
      provides: "각 wave atomic commit hash + verify gate 9종 결과 + 사용자 컨펌 대기 안내"
  key_links:
    - from: "DivPage.tsx 자체 헤더 (line 1095~1100)"
      to: "sketch 01-header verbatim"
      via: "h-12 + px-3 + bg-surface-raised + 백버튼 w-8 h-8 rounded-[7px] bg-surface-sunken + svg 15px"
    - from: "DivPage.tsx 모바일 4탭 (line 1103~1122)"
      to: "sketch 02-tabs verbatim"
      via: "flex bg-surface-raised border-b border-border-default + 각 버튼 flex-1 px-1 py-2.5 text-[12px] font-semibold + 선택 text-accent border-accent / 비선택 text-text-tertiary border-transparent"
    - from: "renderPressureTab (line 265~343)"
      to: "sketch 03 옵션 B + sketch 04 좌측 색바"
      via: "1열 flex-col gap-2 + padding 10/12 + 헤더 14/13/12 + 압력값 22px mono + 정상 border-default 1px / 주의 border-l-4 status-warning-bar / 이상 border-l-4 status-danger-bar + 배지 12px"
    - from: "IntervalBar (line 77~114) + renderLogTab (line 346~383)"
      to: "sketch 05 IntervalBar 카드 (옵션 B 격상)"
      via: "barW 13→24 / gap 3→6 / barMaxH 15→36 / fontSize 5/5.5→10 + 빈 상태 8→13 + drain status-info / oil status-fire-bar / comp_drain raw #8b4513"
    - from: "renderDivDetail (line 386~550)"
      to: "sketch 06 측정점 상세 모달"
      via: "fixed inset-0 + 바텀시트 max-h-[80vh] + 차트 라벨 12 / Y축 12 / X축 11 / 값 11 + 수치 테이블 헤더 12 / 행 월 13 / 행 값 14"
    - from: "renderDesktopLayout (line 937~1085)"
      to: "sketch 07a 헤더+배너 + sketch 07b 매트릭스"
      via: "헤더 h-[54px] + 배너 lucide AlertTriangle 14px + 칩 색 점 div 8x8 + 매트릭스 grid 80px/3x1fr + 셀 minHeight 80 + 개소번호 13 + 월 12 + 중앙 18px mono + 범례 14×14 박스 + 12px 라벨"
    - from: "renderDesktopRightPanel (line 757~934)"
      to: "sketch 07c-1 빈 상태 + sketch 07c-2 선택 상태"
      via: "섹션 헤더 lucide BarChart3/Droplets/AlertTriangle 16px + 카운터 28px mono + 배수/오일 카드 12/18/12 + 알림 row 14/12/12 + 선택 상태 4탭 padding 10/4 + text-[13px] font-semibold"
    - from: "renderDesktopPressureChart + renderDesktopLogTimeline (line 552~754)"
      to: "sketch 06 차트 + sketch 05 IntervalBar 패턴 mirror"
      via: "차트 라벨 12 / Y축 12 / X축 11 / 값 11 + 타임라인 막대 60×variable / 일수 20px / 월 16px / 일 14px"
---

<objective>
DivPage.tsx (1136 LOC) 의 인라인 style + var() 토큰을 sketch 01~07c 의 className 구조 + v0.1.1 토큰 + lucide 아이콘 으로 verbatim 매핑한다. 색·폰트·spacing·아이콘 모두 영역별 sketch에 맞춰 일관 적용하고 비즈니스 로직은 100% 보존한다.

Purpose: v1 "한꺼번에 짜니까 개판" + v2 "안되겠다" 실패 후, 영역별 sketch 7개 단위로 사용자 컨펌 누적된 결정사항을 그대로 코드에 옮긴다. wave atomic commit 6개 + analysis 1 + verify checkpoint 1 로 작업 진행.

Output: 변환된 DivPage.tsx + 6개 atomic commit + verify gate 9종 PASS + SUMMARY.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md

# Sketch (verbatim source — wave별 매핑 대상)
@cha-bio-safety/docs/redesign-context/11-div/sketch/01-header-sketch.html
@cha-bio-safety/docs/redesign-context/11-div/sketch/02-tabs-sketch.html
@cha-bio-safety/docs/redesign-context/11-div/sketch/03-card-normal-sketch.html
@cha-bio-safety/docs/redesign-context/11-div/sketch/04-card-variants-sketch.html
@cha-bio-safety/docs/redesign-context/11-div/sketch/05-intervalbar-card-sketch.html
@cha-bio-safety/docs/redesign-context/11-div/sketch/06-detail-modal-sketch.html
@cha-bio-safety/docs/redesign-context/11-div/sketch/07a-desktop-header-banner-sketch.html
@cha-bio-safety/docs/redesign-context/11-div/sketch/07b-desktop-matrix-sketch.html
@cha-bio-safety/docs/redesign-context/11-div/sketch/07c-desktop-right-empty-sketch.html
@cha-bio-safety/docs/redesign-context/11-div/sketch/07c-desktop-right-selected-sketch.html

# v0.1.1 tokens (필수 참고)
@cha-bio-safety/docs/redesign-context/11-div/tokens.css
@cha-bio-safety/docs/redesign-context/11-div/typography.css

# 변환 대상
@cha-bio-safety/src/pages/DivPage.tsx

<color_lock>
영역 3/4/5/6/7 모두 일관:

| sketch raw / 옛 토큰 | 변환 후 className |
|---|---|
| `var(--bg)` | `bg-surface-page` |
| `var(--bg2)` | `bg-surface-raised` |
| `var(--bg3)` | `bg-surface-sunken` |
| `var(--bd)` | `border-border-default` |
| `var(--t1)` | `text-text-primary` |
| `var(--t2)` | `text-text-secondary` |
| `var(--t3)` | `text-text-tertiary` |
| `var(--acl)` | `text-accent` / `bg-accent` / `border-accent` |
| `var(--safe)` | `text-status-safe` (또는 status-safe-bar 컬럼 색) |
| `var(--warn)` | `text-status-warning` (또는 status-warning-bar bar 용) |
| `var(--danger)` | `text-status-danger` (또는 status-danger-bar bar 용) |
| `#3b82f6` (1차 압력) | `text-accent` / SVG: `fill="var(--accent)"` |
| `#f97316` (2차 / oil) | `text-status-fire-bar` / SVG: `fill="var(--status-fire-bar)"` |
| `#22c55e` (세팅) | `text-status-safe-bar` / SVG: `fill="var(--status-safe-bar)"` |
| `#38bdf8` (drain) | `text-status-info` (또는 IntervalBar `color` prop = `var(--status-info)`) |
| `#f59e0b` (warn raw) | `text-status-warning-bar` |
| `#fbbf24` (warn 컬럼 값) | `text-status-warning` |
| `#8b4513` (comp_drain 갈색) | **raw 유지 — 토큰 미정의 예외** (IntervalBar color prop / RightPanel 카드 라벨만 사용) |
| `rgba(34,197,94,.12)` (safe bg) | `bg-status-safe-bg` |
| `rgba(34,197,94,.25)` (safe bd) | `border-status-safe-bar/25` |
| `rgba(245,158,11,.18)` (warn bg) | `bg-status-warning-bg` |
| `rgba(245,158,11,.40)` (warn bd) | `border-status-warning-bar/40` |
| `rgba(245,158,11,.30)` (warn bd soft) | `border-status-warning-bar/30` |
| `rgba(245,158,11,.08)` (warn soft bg) | `bg-status-warning-bg/50` 또는 raw `rgba(245,158,11,0.08)` inline (저밀도 → raw 유지 OK) |
| `rgba(239,68,68,.18)` (danger bg) | `bg-status-danger-bg` |
| `rgba(239,68,68,.40)` (danger bd) | `border-status-danger-bar/40` |
| `rgba(239,68,68,.30)` (danger bd soft) | `border-status-danger-bar/30` |
| `rgba(239,68,68,.08)` (danger soft bg) | `bg-status-danger-bg/50` 또는 raw inline |
| `rgba(139,148,158,...)` SVG opacity 변형 | SVG raw 유지 (의도된 opacity 변형) |
| `rgba(0,0,0,0.5)` 모달 dim | raw 유지 |
| `rgba(255,255,255,0.05)` 차트 grid line | raw 유지 (SVG) |

**raw 유지 허용:** 
- SVG fill/stroke 의 `rgba(139,148,158,...)` 변형 (label opacity)
- 모달 dim `rgba(0,0,0,0.5)`
- 차트 grid line `rgba(255,255,255,0.05)`
- `#8b4513` (comp_drain) — IntervalBar color prop / RightPanel 라벨 색
</color_lock>

<typography_lock>
영역 3/4/5/6/7 일관 — wave별 적용:

**Wave 1 (헤더+탭):**
- 헤더 제목 `text-[13px] font-bold text-text-primary` (sketch 01)
- 탭 폰트 `text-[12px] font-semibold` + 선택 `text-accent border-b-2 border-accent` / 비선택 `text-text-tertiary border-b-2 border-transparent` (sketch 02)

**Wave 2 (모바일 측정점 카드 + IntervalBar):**
- 카드 헤더 좌 `text-[14px] font-bold text-text-primary flex-shrink-0` ("DIV #1") (sketch 03 옵션 B)
- 카드 헤더 중 `text-[13px] text-text-secondary flex-1 truncate` (location)
- 카드 헤더 우 (월/배지) `text-[12px] text-text-tertiary flex-shrink-0`
- 주의/이상 배지 `text-[12px] font-bold text-status-warning-bar` 또는 `text-status-danger`
- 압력값 `text-[22px] font-bold leading-none font-mono` + 컬럼 색 (sketch 03/04 옵션 B)
- 컬럼 라벨 `text-[12px] text-text-tertiary mt-1.5` ("1차" / "2차" / "세팅")
- 카드 padding `p-[10px_12px]` (정상) / `p-[10px_12px_10px_9px]` (주의/이상 — 좌측 4px bar 공간)
- 카드 border 정상 `border border-border-default` / 주의 `border border-border-default border-l-4 border-l-status-warning-bar` / 이상 `border border-border-default border-l-4 border-l-status-danger-bar`
- IntervalBar SVG (line 77~114): barW 13→**24**, gap 3→**6**, barMaxH 15→**36**, fontSize 5/5.5→**10**, labelH 11→**20**, topPad 6→**10** (sketch 05)
- "기록 없음" `text-[13px] text-text-tertiary` (verbatim 8 → 13 격상, sketch 05)
- 층 라벨 `text-[12px] font-bold text-text-tertiary tracking-[0.04em] mb-2 pl-0.5` (verbatim 9 → 12 격상)

**Wave 3 (모바일 상세 모달):**
- 모달 dim `fixed inset-0 z-40 flex flex-col justify-end bg-black/50`
- 시트 `bg-surface-raised rounded-t-2xl p-[16px_16px_36px] max-h-[80vh] overflow-y-auto`
- 타이틀 1줄 `text-[16px] font-bold text-text-primary` (verbatim 유지)
- 타이틀 2줄 `text-[12px] text-text-tertiary mt-0.5` (verbatim 11 → 12 격상)
- 연도 ‹ › 버튼 `bg-surface-sunken rounded-md px-2 py-1 text-text-secondary text-[15px]` (verbatim — 텍스트 ‹ › 유지)
- 연도 숫자 `text-[13px] font-bold text-text-primary w-[38px] text-center` (verbatim)
- 닫기 ✕ `bg-transparent border-0 text-text-tertiary text-[20px]` (텍스트 ✕ 유지)
- 차트 라벨 `text-[12px] font-bold` + 컬럼 색 (verbatim 10 → 12 격상)
- SVG Y축 tick `font-size="12"` (verbatim 11 → 12 격상)
- SVG X축 월 라벨 `font-size="11"` (verbatim 10 → 11 격상)
- SVG 값 텍스트 `font-size="11"` (verbatim 11 유지)
- 데이터 없음 `text-text-tertiary py-[30px] text-center text-[13px]` (verbatim 유지)
- 테이블 헤더 cell `text-[12px] font-bold text-text-tertiary text-center` (verbatim 9 → 12 격상)
- 테이블 행 월 cell `text-[13px] text-text-secondary text-center font-mono` (verbatim 10 → 13 격상, text-tertiary → text-secondary 격상)
- 테이블 행 값 cell `text-[14px] font-bold text-center font-mono` + 컬럼 색 (verbatim 12 → 14 격상)
- 테이블 cell padding `p-[8px_10px]` (verbatim 7 → 8 격상)

**Wave 4 (데스크톱 헤더+배너+매트릭스):**
- 헤더 `h-[54px] bg-surface-raised border-b border-border-default px-5 flex items-center gap-2.5`
- 헤더 제목 `flex-1 text-[16px] font-bold text-text-primary` ("DIV 압력 관리") (verbatim 14 → 16 유지/격상 — sketch 07a 명시)
- 배너 wrapper `bg-surface-raised border-b border-border-default p-[8px_24px] flex items-center gap-2.5 overflow-x-auto`
- 배너 라벨 lucide `<AlertTriangle size={14} />` + `text-[12px] font-bold text-text-secondary` ("이상 N건 · 주의 N건")
- 배너 칩 (이상) `border border-status-danger-bar/40 bg-status-danger-bg text-status-danger rounded-2xl px-2.5 py-1 text-[11px] font-bold whitespace-nowrap flex items-center gap-1.5` + 색 점 div `w-2 h-2 rounded-full bg-status-danger` (이모지 ● 폐기)
- 배너 칩 (주의) `border border-status-warning-bar/40 bg-status-warning-bg text-status-warning-bar ...` + 색 점 div `w-2 h-2 rounded-full bg-status-warning-bar`
- 빈 상태 `text-[11px] text-text-tertiary` ("모든 포인트 정상")
- 매트릭스 wrapper `flex-1 min-w-0 border-r border-border-default overflow-y-auto p-[20px_24px]`
- 매트릭스 헤더 row `grid grid-cols-[80px_repeat(3,1fr)] gap-2 mb-2.5 pb-2.5 border-b border-border-default` (verbatim 6 → 8 / 8 → 10 격상)
- 헤더 cell `text-[12px] font-bold text-text-tertiary` ("층") + `text-center` 컬럼 라벨
- 각 row `grid grid-cols-[80px_repeat(3,1fr)] gap-2 mb-2` (verbatim 6 → 8)
- 층 라벨 cell `text-[13px] font-bold text-text-secondary flex items-center pl-1` (verbatim 12 → 13 격상)
- 셀 컨테이너 `min-h-[80px] rounded-lg relative cursor-pointer` (verbatim 64 → 80 격상)
- 셀 padding 미선택 `p-[10px_12px]` / 선택 `p-[9px_11px]`
- 셀 정상 `bg-status-safe-bg border border-status-safe-bar/25`
- 셀 주의 `bg-status-warning-bg border border-status-warning-bar/40`
- 셀 이상 `bg-status-danger-bg border border-status-danger-bar/40`
- 셀 해당없음 `bg-surface-sunken border border-dashed border-border-default flex items-center justify-center text-text-tertiary text-[16px]` ("—")
- 셀 선택 `border-2 border-accent` (border 1px → 2px + padding 보정)
- 셀 배지 (주의/이상) `absolute top-1.5 right-2.5 text-[11px] font-bold` + 색
- 셀 좌상단 ID `text-[13px] font-bold text-text-primary leading-none` (verbatim 10 → 13 격상)
- 셀 좌상단 월 `text-[12px] text-text-tertiary leading-none mt-0.5` (verbatim 9 → 12 격상)
- 셀 중앙 값 wrapper `absolute bottom-2.5 left-0 right-0 flex justify-center items-center gap-2.5`
- 셀 중앙 값 `text-[18px] font-bold leading-none font-mono` + 컬럼 색 (verbatim 15 → 18 격상)
- 셀 중앙 구분자 `text-[13px] text-text-tertiary font-normal` ("|") (verbatim 12 → 13)
- 범례 wrapper `mt-4 p-[12px_12px] border-t border-border-default flex gap-4 flex-wrap` (verbatim 10/12 → 12/12 격상)
- 범례 항목 `flex items-center gap-1.5 text-[12px] text-text-tertiary` (verbatim 11 → 12 격상)
- 범례 박스 `inline-block w-[14px] h-[14px] rounded-[3px]` + status bg + border (verbatim 12×12 → 14×14 격상)

**Wave 5 (데스크톱 우측 패널):**

빈 상태 (no selDiv):
- wrapper `flex flex-col gap-[18px]`
- 섹션 헤더 `inline-flex items-center gap-2 text-[13px] font-bold text-text-secondary mb-2.5` (verbatim 12 → 13 격상)
- 섹션 1 lucide `<BarChart3 size={16} />` + "34개 측정점 현황" (◆ → lucide)
- 섹션 2 lucide `<Droplets size={16} />` + "배수/오일 현황" (── → lucide)
- 섹션 3 lucide `<AlertTriangle size={16} />` + "이상/주의 포인트" (── → lucide)
- 카운터 grid `grid grid-cols-3 gap-2.5`
- 카운터 카드 `bg-status-{safe|warning|danger}-bg border border-status-{...}-bar/{25|40} rounded-[10px] p-[14px_12px] text-center` (verbatim 14/12 유지)
- 카운터 숫자 `text-[28px] font-extrabold leading-none font-mono` + status 색 (정상 `text-status-safe-bar` / 주의 `text-status-warning-bar` / 이상 `text-status-danger`)
- 카운터 라벨 `text-[12px] text-text-secondary mt-1.5 font-semibold` (verbatim 11 → 12 격상)
- 배수/오일 카드 grid `grid grid-cols-3 gap-2.5`
- 배수/오일 카드 `bg-surface-raised border border-border-default rounded-[10px] p-[12px_12px]`
- 배수/오일 라벨 `text-[12px] font-bold tracking-[0.04em] mb-1.5` + 탭 색 (chamber: `text-status-info` / tank: raw `text-[#8b4513]` / oil: `text-status-fire-bar`) (verbatim 11 → 12 격상)
- 배수/오일 평균 `text-[18px] font-extrabold text-text-primary font-mono` (verbatim 16 → 18 격상)
- 배수/오일 최근 `text-[12px] text-text-tertiary mt-1 font-mono` (verbatim 10 → 12 격상)
- 알림 row 빈 상태 `text-[12px] text-text-tertiary p-4 text-center bg-surface-raised border border-border-default rounded-lg`
- 알림 row 컨테이너 `flex flex-col gap-1.5`
- 알림 row (이상) `flex items-center gap-2.5 p-[10px_12px] bg-status-danger-bg/50 border border-status-danger-bar/30 rounded-lg cursor-pointer text-left`
- 알림 row (주의) `bg-status-warning-bg/50 border border-status-warning-bar/30`
- 알림 row 상태 라벨 `inline-flex items-center gap-1.5 text-[12px] font-bold flex-shrink-0 min-w-[56px]` + 색 점 div `w-2 h-2 rounded-full bg-status-{danger|warning-bar}` (●/◐ 이모지 폐기)
- 알림 row ID `text-[14px] font-bold text-text-primary font-mono flex-shrink-0 min-w-[80px]` (verbatim 13 → 14 격상)
- 알림 row 위치 `text-[12px] text-text-secondary flex-1 overflow-hidden whitespace-nowrap text-ellipsis` (verbatim 11 → 12 격상)
- 알림 row pct `text-[12px] font-bold font-mono flex-shrink-0` + status 색

선택 상태 (selDiv):
- wrapper `flex flex-col gap-3.5 pb-10`
- 헤더 row `flex items-center gap-2.5`
- 헤더 1줄 `text-[16px] font-bold text-text-primary` (verbatim 유지)
- 헤더 2줄 `text-[12px] text-text-tertiary mt-0.5` (verbatim 11 → 12 격상)
- 연도 네비 `flex items-center gap-1 flex-shrink-0` (pressure 탭만 표시)
- ‹ / › 버튼 `bg-surface-sunken border-0 rounded-md px-2 py-1 text-text-secondary text-[15px] cursor-pointer` (verbatim — 텍스트 ‹ › 유지)
- 연도 숫자 `text-[13px] font-bold text-text-primary w-[38px] text-center` (verbatim)
- 닫기 ✕ `bg-transparent border-0 text-text-tertiary text-[22px] flex-shrink-0` (텍스트 ✕ 유지)
- 4탭 wrapper `flex border-b border-border-default`
- 4탭 버튼 `flex-1 p-[10px_4px] border-0 bg-transparent text-[13px] font-semibold` + 선택 `text-accent border-b-2 border-accent` / 비선택 `text-text-tertiary border-b-2 border-transparent` (verbatim 8/4 → 10/4 + 12 → 13 격상)

**Wave 6 (데스크톱 차트 + 타임라인):**
- renderDesktopPressureChart (line 552~): Wave 3 sketch 06 매핑과 동일 (W=600 + 헤더 라벨 12 / Y축 12 / X축 11 / 값 11)
- renderDesktopLogTimeline (line 685~): sketch 05 IntervalBar 패턴 mirror, 단 더 큰 사이즈
  - 헤더 라벨 `text-[12px] font-bold tracking-[0.04em]` + 탭 색 (verbatim 11 → 12 격상)
  - 차트 박스 `bg-surface-raised border border-border-default rounded-[10px] p-[18px_20px]`
  - 빈 상태 `text-text-tertiary text-[13px] flex items-center justify-center h-[{chartH}]`
  - SVG: 막대 60×variable rounded-[6px] + 일수 `font-size="20" font-weight="700"` + 탭 색
  - SVG 월 라벨 `font-size="16" font-weight="700"` text-secondary
  - SVG 일 라벨 `font-size="14"` text-tertiary
  - "최근 기록" 라벨 `text-[12px] font-bold text-text-tertiary mt-1.5` (verbatim 11 → 12 격상)
  - 빈 리스트 `text-[13px] text-text-tertiary p-4 text-center` (verbatim 12 → 13 격상)
  - 날짜 row `flex gap-2.5 p-[8px_12px] bg-surface-raised rounded-lg border border-border-default`
  - 날짜 row 텍스트 `text-[13px] font-mono text-text-primary` (verbatim 12 → 13 미세 격상)

</typography_lock>

<icon_lock>
영역 7a, 7c-1 일관 — wave별 적용:

**Wave 4 (배너):**
- `import { AlertTriangle } from 'lucide-react'`
- 배너 라벨 `⚠` → `<AlertTriangle className="w-[14px] h-[14px]" />`
- 배너 칩 `●` → `<span className="w-2 h-2 rounded-full bg-status-danger inline-block" />`
- 배너 칩 `◐` → `<span className="w-2 h-2 rounded-full bg-status-warning-bar inline-block" />`

**Wave 5 (우측 패널 빈 상태):**
- `import { AlertTriangle, BarChart3, Droplets } from 'lucide-react'` (Wave 4 의 AlertTriangle 과 같이 import)
- 섹션 1 `◆ 34개 측정점 현황` → `<BarChart3 size={16} />` + "34개 측정점 현황"
- 섹션 2 `── 배수/오일 현황 ──` → `<Droplets size={16} />` + "배수/오일 현황"
- 섹션 3 `── 이상/주의 포인트 ──` → `<AlertTriangle size={16} />` + "이상/주의 포인트"
- 알림 row `● 이상` → 색 점 div `w-2 h-2 rounded-full bg-status-danger` + "이상" 텍스트
- 알림 row `◐ 주의` → 색 점 div `w-2 h-2 rounded-full bg-status-warning-bar` + "주의" 텍스트

**Verbatim 유지 (lucide 사용 X):**
- 모바일 헤더 백 버튼 svg (line 1097) — App GlobalHeader 와 일치
- 모달 닫기 `✕` (line 442) — 텍스트 verbatim 유지
- 우측 패널 닫기 `✕` (line 903) — 텍스트 verbatim 유지
- 연도 네비 `‹ / ›` — 텍스트 verbatim 유지
- 차트 SVG 의 모든 폰트/도형 — SVG verbatim 유지

</icon_lock>

<wave_overview>
| Wave | 영역 | DivPage.tsx 라인 | sketch 매핑 | Commit |
|---|---|---|---|---|
| 1 | 자체 헤더 + 4탭 | 1091~1135 | 01 + 02 | feat(t3n): Wave 1 모바일 chrome (헤더 + 4탭) |
| 2 | 모바일 측정점 + IntervalBar | 77~114, 265~383 | 03 + 04 + 05 | feat(t3n): Wave 2 모바일 측정점 + IntervalBar |
| 3 | 모바일 상세 모달 | 386~550 | 06 | feat(t3n): Wave 3 모바일 상세 모달 |
| 4 | 데스크톱 chrome + 매트릭스 | 937~1085 | 07a + 07b | feat(t3n): Wave 4 데스크톱 chrome + 매트릭스 |
| 5 | 데스크톱 우측 패널 | 757~934 | 07c-1 + 07c-2 | feat(t3n): Wave 5 데스크톱 우측 패널 |
| 6 | 데스크톱 차트 + 타임라인 | 552~754 | 06 차트 + 05 IntervalBar mirror | feat(t3n): Wave 6 데스크톱 차트 + 타임라인 |

코드 변경 = DivPage.tsx 만. 다른 파일 변경 0건 (lucide-react 는 이미 dependency).
</wave_overview>

</context>

<tasks>

<task type="auto">
  <name>Task 1: read-only 분석 — DivPage.tsx 1136 LOC 흡수 + 10 sketch verbatim 매핑표 작성</name>
  <files>
    (read-only) cha-bio-safety/src/pages/DivPage.tsx, cha-bio-safety/docs/redesign-context/11-div/sketch/*.html, cha-bio-safety/docs/redesign-context/11-div/tokens.css, cha-bio-safety/docs/redesign-context/11-div/typography.css
  </files>
  <action>
    **목표:** 변환 시작 전 source 흡수. 코드 0건 변경. wave별 정확한 verbatim 매핑 노트를 머리에 새긴 후 Wave 1 로 진입.

    1. **DivPage.tsx 전체 흡수 — 6 구간 분할 read (한 번씩만, 중복 read 금지):**
       - 1~120 (헤더 import + STATUS_COLOR + FLOOR_GROUPS + IntervalBar 컴포넌트 본체)
       - 120~265 (DivPage 본문 진입 + useQuery 7개 + pressureMap/dateMap useMemo + pointStatusList useMemo)
       - 265~385 (renderPressureTab + renderLogTab — Wave 2 대상)
       - 386~550 (renderDivDetail — Wave 3 대상)
       - 552~754 (renderDesktopPressureChart + renderDesktopLogTimeline — Wave 6 대상)
       - 757~1136 (renderDesktopRightPanel + renderDesktopLayout + 모바일 return — Wave 4/5/1 대상)
    2. **sketch 10 종 verbatim 분석 — 각 sketch 한 번씩만 read.** Wave 1~6 에 매핑되는 className 패턴 추출.
    3. **비즈니스 로직 인벤토리** — wave 변환 시 보존해야 할 카운트 (verify gate 8 기준):
       - useQuery: 7개 (pressure / drain / comp_drain / oil / history) — selDiv 의존 history 포함
       - pressureStatus: 함수 정의 1 + 호출 다수
       - FLOOR_GROUPS.map: 3회 (renderPressureTab / renderLogTab / renderDesktopLayout)
       - setSelDiv: 4+회 (renderPressureTab onClick / renderDesktopRightPanel 빈상태 알림 row / renderDesktopLayout 배너 칩 / renderDesktopLayout 매트릭스 셀)
       - setTab: 2+회 (모바일 4탭 / 데스크톱 우측 패널 4탭)
       - fetchLogs: 호출 3회 ('drain' / 'comp_drain' / 'compressor')
    4. **컬럼명 '챔버' / '챔버배수' 차이 확인** — 모바일 탭 라벨 = "챔버배수주기" / 데스크톱 탭 라벨 = "챔버배수". 잘못 줄여 "챔버" 단독 사용 금지 (verify gate 5).
    5. 이 분석은 코드 출력 0. 파일 작성 0. wave 진입 준비 끝나면 다음 task.
  </action>
  <verify>
    <automated>echo "Task 1 = read-only analysis. No file written. Wave 1 진입 준비 완료."</automated>
  </verify>
  <done>
    1136 LOC + 10 sketch 흡수 완료. wave별 매핑 패턴 + 비즈니스 로직 인벤토리 + 컬럼명 가드 머리에 있음. Wave 1 진입.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wave 1 — 모바일 chrome (자체 헤더 + 4탭) — sketch 01 + 02 verbatim</name>
  <files>cha-bio-safety/src/pages/DivPage.tsx</files>
  <action>
    **변환 대상:** DivPage.tsx line 1091~1135 (모바일 return 영역) — 헤더 + 4탭 + 컨텐츠 wrapper + 오버레이 호출.

    **변환 규칙 (sketch 01 + 02 verbatim, color_lock + typography_lock + icon_lock 적용):**

    1. **외곽 wrapper (line 1092):**
       - 전: `<div style={{ height: '100%', display: 'flex', flexDirection: 'column', overflow: 'hidden', background: 'var(--bg)' }}>`
       - 후: `<div className="h-full flex flex-col overflow-hidden bg-surface-page">`

    2. **자체 헤더 (line 1095~1100) — sketch 01 App GlobalHeader verbatim:**
       - 전: `<header style={{ flexShrink: 0, background: 'var(--bg2)', borderBottom: '1px solid var(--bd)', padding: '8px 12px', display: 'flex', alignItems: 'center', gap: 8 }}>`
       - 후: `<header className="flex-shrink-0 flex items-center h-12 px-3 bg-surface-raised border-b border-border-default">`
       - 백 버튼 전: `style={{ width: 34, height: 34, borderRadius: 8, background: 'var(--bg3)', border: '1px solid var(--bd)', cursor: 'pointer', display: 'flex', alignItems: 'center', justifyContent: 'center' }}`
       - 백 버튼 후: `className="w-8 h-8 rounded-[7px] bg-surface-sunken flex items-center justify-center text-text-secondary cursor-pointer border-0"` (border 제거 — sketch 01 verbatim "border 없음")
       - 백 버튼 svg: stroke `var(--t2)` → `currentColor` (button text 색 상속)
       - 제목 전: `style={{ flex: 1, fontSize: 14, fontWeight: 700, color: 'var(--t1)' }}`
       - 제목 후: `className="flex-1 text-[13px] font-bold text-text-primary ml-2"` (sketch 01: ml-2 + 13px verbatim)

    3. **4탭 (line 1103~1122) — sketch 02 verbatim:**
       - wrapper 전: `style={{ flexShrink: 0, display: 'flex', background: 'var(--bg2)', borderBottom: '1px solid var(--bd)' }}`
       - wrapper 후: `className="flex-shrink-0 flex bg-surface-raised border-b border-border-default"`
       - 각 버튼 전: `style={{ flex: 1, padding: '10px 4px', border: 'none', cursor: 'pointer', fontSize: 12, fontWeight: 600, background: 'transparent', color: tab === t.key ? 'var(--acl)' : 'var(--t3)', borderBottom: '2px solid ...', transition: 'color .15s' }}`
       - 각 버튼 후: `className={\`flex-1 px-1 py-2.5 text-[12px] font-semibold bg-transparent cursor-pointer transition-colors border-b-2 ${tab === t.key ? 'text-accent border-accent' : 'text-text-tertiary border-transparent'}\`}`
       - 라벨 verbatim 유지: "압력 트렌드 / 챔버배수주기 / 오일 주기 / 탱크배수주기"

    4. **컨텐츠 wrapper (line 1125):**
       - 전: `<div style={{ flex: 1, overflowY: 'auto' }}>`
       - 후: `<div className="flex-1 overflow-y-auto">`

    5. **오버레이 호출 line 1133 `{renderDivDetail()}` 그대로.**

    **Wave 1 commit:**
    ```bash
    git add -A && git -c commit.gpgsign=false commit -m "feat(t3n): Wave 1 — 모바일 chrome (헤더 + 4탭) verbatim (sketch 01+02)" -m "- 자체 헤더 h-12 + px-3 + bg-surface-raised + 백 버튼 w-8 h-8 rounded-[7px] bg-surface-sunken (App GlobalHeader verbatim)" -m "- 백 버튼 svg 15px stroke currentColor (verbatim)" -m "- 제목 text-[13px] font-bold text-text-primary ml-2 (sketch 01)" -m "- 4탭 flex bg-surface-raised border-b border-border-default + 각 버튼 flex-1 px-1 py-2.5 text-[12px] font-semibold (sketch 02 verbatim)" -m "- 선택 text-accent border-accent / 비선택 text-text-tertiary border-transparent" -m "- 외곽 + 컨텐츠 wrapper className 변환"
    ```

    **자체 검수:**
    - line 1091~1135 영역에 `style={{` 0건 (svg 1개의 stroke/strokeWidth 는 svg 속성으로 유지)
    - `var(--(bg|bg2|bg3|bd|t1|t2|t3|acl))` 0건
    - 비즈니스 로직 보존: setTab 호출 1, navigate(-1) 1, tab 라벨 4종 verbatim
  </action>
  <verify>
    <automated>cd cha-bio-safety && npx tsc --noEmit 2>&1 | grep -E "DivPage" || echo "Wave 1 tsc PASS"</automated>
  </verify>
  <done>
    Wave 1 변환 + atomic commit 완료. tsc DivPage 0 에러. 모바일 헤더 + 4탭 sketch 01+02 verbatim.
  </done>
</task>

<task type="auto">
  <name>Task 3: Wave 2 — 모바일 측정점 카드 + IntervalBar — sketch 03 옵션 B + sketch 04 + sketch 05 격상</name>
  <files>cha-bio-safety/src/pages/DivPage.tsx</files>
  <action>
    **변환 대상:** 
    - IntervalBar 컴포넌트 본체 line 77~114 (모바일/데스크톱 공유)
    - renderPressureTab line 265~343
    - renderLogTab line 346~383

    **변환 규칙 (sketch 03 옵션 B + sketch 04 좌측 색바 + sketch 05 격상):**

    1. **IntervalBar 컴포넌트 (line 77~114) — sketch 05 격상:**
       - "기록 없음" line 80 전: `style={{ height: 34, display: 'flex', alignItems: 'center', justifyContent: 'center', fontSize: 8, color: 'var(--t3)' }}`
       - "기록 없음" 후: `className="h-[70px] flex items-center justify-center text-[13px] text-text-tertiary"` (height 34→70 + fontSize 8→13 격상)
       - 사이즈 상수: barW 13 → **24** / gap 3 → **6** / topPad 6 → **10** / barMaxH 15 → **36** / labelH 11 → **20**
       - SVG `<rect>` rx 2 → **3**
       - SVG 일수 text `fontSize="5.5"` → **"10"** + opacity 0.9 유지
       - SVG mm text `fontSize="5"` → **"10"** + fill `rgba(139,148,158,0.55)` → **`rgba(139,148,158,0.65)`** 유지 OK
       - SVG dd text `fontSize="5"` → **"10"** + fill `rgba(139,148,158,0.45)` → **`rgba(139,148,158,0.5)`** 유지 OK
       - mm/dd y 좌표 보정: topPad + barMaxH + 7 → topPad + barMaxH + 12 (mm); topPad + barMaxH + 14 → topPad + barMaxH + 22 (dd) — labelH 20 만큼 늘어남 반영
       - SVG `fill={color}` (drain/oil/comp_drain) — color prop 그대로 받음 (renderLogTab 에서 토큰 var 전달)

    2. **renderPressureTab (line 265~343) — sketch 03 옵션 B + sketch 04 변형:**
       - 외곽 wrapper 전: `<div style={{ padding: '6px 8px 80px' }}>`
       - 외곽 wrapper 후: `<div className="p-[12px_12px_80px]">` (sketch 04 padding 12/12 verbatim, sketch 03 옵션 B 와 일치)
       - 층 그룹 wrapper 전: `style={{ marginBottom: 4 }}` → `className="mb-1"` (사이즈 유지)
       - 층 라벨 전: `style={{ fontSize: 9, fontWeight: 700, color: 'var(--t3)', marginBottom: 2, paddingLeft: 2, letterSpacing: '0.04em' }}`
       - 층 라벨 후: `className="text-[12px] font-bold text-text-tertiary mb-2 pl-0.5 tracking-[0.04em]"` (sketch 04: 12px 격상)
       - 카드 grid 전: `style={{ display: 'grid', gridTemplateColumns: 'repeat(3, 1fr)', gap: 4 }}` → 후 `className="flex flex-col gap-2"` (sketch 03 옵션 B: 3열 → 1열 격상, gap 4→8)
       - **각 카드 (sketch 04 변형 — alarm/warn 우선순위로 좌측 4px bar + 배지):**
         - alarm/warn 계산 그대로 (s1/s2/sc + alarm/warn 변수)
         - 카드 컨테이너 후:
           ```tsx
           <div
             key={div.id}
             onClick={() => setSelDiv(div)}
             className={`bg-surface-raised rounded-lg cursor-pointer ${
               alarm ? 'border border-border-default border-l-4 border-l-status-danger-bar p-[10px_12px_10px_9px]' :
               warn  ? 'border border-border-default border-l-4 border-l-status-warning-bar p-[10px_12px_10px_9px]' :
                       'border border-border-default p-[10px_12px]'
             }`}
           >
           ```
         - 헤더 row 전: `style={{ display: 'flex', alignItems: 'center', gap: 3, marginBottom: 2 }}`
         - 헤더 row 후: `className="flex items-center gap-2 mb-2"` (sketch 04: gap 3→8 / mb 2→8 격상)
         - "#{pos}" 전: `style={{ fontSize: 8, fontWeight: 700, color: 'var(--t3)', flexShrink: 0 }}` → 후 `className="text-[14px] font-bold text-text-primary flex-shrink-0"` + 텍스트 `DIV #{pos}` (sketch 04: "DIV #1" 라벨 + 14px text-primary 격상)
         - 위치 전: `style={{ fontSize: 7, color: 'var(--t2)', flex: 1, overflow: 'hidden', whiteSpace: 'nowrap', textOverflow: 'ellipsis' }}` → 후 `className="text-[13px] text-text-secondary flex-1 overflow-hidden whitespace-nowrap text-ellipsis"` (sketch 04: 7→13 격상)
         - alarm/warn 배지: alarm 후 `className="text-[12px] font-bold text-status-danger flex-shrink-0"` "이상" / warn 후 `className="text-[12px] font-bold text-status-warning-bar flex-shrink-0"` "주의" (sketch 04: 7→12 격상 + warn 색 #f59e0b → status-warning-bar)
         - 월 표시 전: `style={{ fontSize: 7, color: 'var(--t3)', flexShrink: 0 }}` → 후 `className="text-[12px] text-text-tertiary flex-shrink-0"` (sketch 04: 7→12)
       - 압력값 wrapper 전: `style={{ height: 34, display: 'flex', alignItems: 'center' }}` → 후 wrapper 제거 (sketch 03 옵션 B: height 고정 없음, 컨텐츠가 결정)
       - "-" 전: `style={{ width: '100%', textAlign: 'center', fontSize: 9, color: 'var(--t3)' }}` → 후 `className="w-full text-center text-[13px] text-text-tertiary"`
       - 압력값 row 전: `style={{ display: 'flex', justifyContent: 'space-around', width: '100%' }}` → 후 `className="flex justify-around"`
       - 각 컬럼 wrapper 전: `style={{ textAlign: 'center', flex: 1 }}` → 후 `className="text-center flex-1"`
       - **컬럼 값 변환 (sketch 04 individual status 기준):**
         - col 매핑 변경: `col` 필드 제거, status 별로 직접 className 결정
         - 값 div 전: `style={{ fontSize: 14, fontWeight: 800, lineHeight: 1, fontFamily: 'JetBrains Mono, monospace', color: p.s === 'danger' ? 'var(--danger)' : p.s === 'warn' ? '#f59e0b' : p.col }}`
         - 값 div 후: 
           ```tsx
           <div className={`text-[22px] font-bold leading-none font-mono ${
             p.s === 'danger' ? 'text-status-danger' :
             p.s === 'warn'   ? 'text-status-warning' :
             p.label === '1차' ? 'text-accent' :
             p.label === '2차' ? 'text-status-fire-bar' :
                                 'text-status-safe-bar'
           }`}>{p.val.toFixed(1)}</div>
           ```
         - 컬럼 라벨 전: `style={{ fontSize: 7, color: 'var(--t3)', marginTop: 3 }}` → 후 `className="text-[12px] text-text-tertiary mt-1.5"`
         - 컬럼 배열 단순화: `[{label:'1차', val:last.v1, s:s1}, {label:'2차', val:last.v2, s:s2}, {label:'세팅', val:last.vc, s:sc}]` (col 필드 제거 — className 로 색 결정)

    3. **renderLogTab (line 346~383) — sketch 05 verbatim:**
       - 외곽 wrapper: padding 6/8/80 → padding 12/12/80 (`className="p-[12px_12px_80px]"`)
       - 층 그룹 wrapper: 변환 동일
       - 층 라벨 변환: 동일 (sketch 05 도 동일)
       - 카드 grid: 3열 → 1열 (`className="flex flex-col gap-2"`)
       - color 변수: `'#38bdf8'` → `'var(--status-info)'` / `'#8b4513'` → `'#8b4513'` (raw 유지 — 토큰 미정의 예외) / `'#f97316'` → `'var(--status-fire-bar)'`
       - 카드 컨테이너 전: `style={{ background: 'var(--bg2)', border: '1px solid var(--bd)', borderRadius: 8, padding: '5px 5px 4px' }}` → 후 `className="bg-surface-raised border border-border-default rounded-lg p-[10px_12px]"`
       - 헤더 row 변환: pressure 카드와 동일 (gap-2 mb-2 + "DIV #1" 14px + 위치 13px text-secondary)
       - 최근 기록일 추가: dates 가 있으면 `<span className="text-[12px] text-text-tertiary flex-shrink-0">최근 {dates[dates.length-1]?.slice(5)?.replace('-','/') ?? ''}</span>` — sketch 05 의 "최근 5/12" verbatim
       - IntervalBar 호출 동일 — color prop 변경된 토큰 var 전달

    **Wave 2 commit:**
    ```bash
    git add -A && git -c commit.gpgsign=false commit -m "feat(t3n): Wave 2 — 모바일 측정점 + IntervalBar verbatim (sketch 03+04+05)" -m "- 측정점 카드 1열 flex + p-[10px_12px] + 헤더 14/13/12px + 압력값 22px mono (sketch 03 옵션 B)" -m "- 주의/이상 좌측 4px bar (status-warning-bar / status-danger-bar) + 배지 12px font-bold (sketch 04)" -m "- IntervalBar 격상 barW 24 / gap 6 / barMaxH 36 / fontSize 10 + 빈 상태 13px (sketch 05)" -m "- color prop 토큰화: drain → status-info / oil → status-fire-bar / comp_drain → raw #8b4513 (예외)" -m "- 컬럼 색: 1차 accent / 2차 status-fire-bar / 세팅 status-safe-bar / warn → status-warning / danger → status-danger"
    ```

    **자체 검수:**
    - line 77~114, 265~383 영역에 `style={{` 0건 (단 SVG 의 `viewBox=...` 동적 표현식은 SVG 속성으로 유지)
    - raw hex `#3b82f6` / `#f97316` / `#22c55e` / `#38bdf8` / `#f59e0b` 잔존 0건 (이 영역에 한정)
    - `#8b4513` 1건 잔존 OK (renderLogTab color 변수)
    - 비즈니스 로직 보존: pressureStatus 호출 3회 (s1/s2/sc) + FLOOR_GROUPS.map + setSelDiv onClick
  </action>
  <verify>
    <automated>cd cha-bio-safety && npx tsc --noEmit 2>&1 | grep -E "DivPage" || echo "Wave 2 tsc PASS"</automated>
  </verify>
  <done>
    Wave 2 변환 + atomic commit 완료. 모바일 측정점 카드 + IntervalBar sketch 03/04/05 verbatim. tsc 0 에러.
  </done>
</task>

<task type="auto">
  <name>Task 4: Wave 3 — 모바일 상세 모달 — sketch 06 verbatim + 글씨 노안 격상</name>
  <files>cha-bio-safety/src/pages/DivPage.tsx</files>
  <action>
    **변환 대상:** renderDivDetail line 386~550.

    **변환 규칙 (sketch 06 verbatim + 글씨 격상):**

    1. **모달 dim (line 416):**
       - 전: `style={{ position: 'fixed', inset: 0, zIndex: 40, display: 'flex', flexDirection: 'column', justifyContent: 'flex-end', background: 'rgba(0,0,0,0.5)' }}`
       - 후: `className="fixed inset-0 z-40 flex flex-col justify-end bg-black/50"`

    2. **시트 (line 420):**
       - 전: `style={{ background: 'var(--bg2)', borderRadius: '16px 16px 0 0', padding: '16px 16px 36px', maxHeight: '80vh', overflowY: 'auto' }}`
       - 후: `className="bg-surface-raised rounded-t-2xl p-[16px_16px_36px] max-h-[80vh] overflow-y-auto"`

    3. **타이틀 row (line 424):**
       - 전: `style={{ display: 'flex', alignItems: 'center', gap: 10, marginBottom: 16 }}`
       - 후: `className="flex items-center gap-2.5 mb-4"`
       - 1줄 전: `style={{ fontSize: 16, fontWeight: 700, color: 'var(--t1)' }}` → 후 `className="text-[16px] font-bold text-text-primary"`
       - 2줄 전: `style={{ fontSize: 11, color: 'var(--t3)', marginTop: 2 }}` → 후 `className="text-[12px] text-text-tertiary mt-0.5"` (verbatim 11 → 12 격상)

    4. **연도 네비 (line 430~438):**
       - 외곽 전: `style={{ display: 'flex', alignItems: 'center', marginLeft: 'auto' }}` → 후 `className="flex items-center ml-auto"`
       - 양옆 wrapper 전: `style={{ width: 30, display: 'flex', justifyContent: 'center' }}` → 후 `className="w-[30px] flex justify-center"`
       - 버튼 전: `style={{ background: 'var(--bg3)', border: 'none', cursor: 'pointer', borderRadius: 6, padding: '4px 8px', color: 'var(--t2)', fontSize: 15 }}`
       - 버튼 후: `className="bg-surface-sunken border-0 rounded-md px-2 py-1 text-text-secondary text-[15px] cursor-pointer"` (텍스트 ‹ › 유지)
       - 연도 숫자 전: `style={{ fontSize: 13, fontWeight: 700, color: 'var(--t1)', width: 38, textAlign: 'center', display: 'inline-block' }}` → 후 `className="text-[13px] font-bold text-text-primary w-[38px] text-center inline-block"`

    5. **닫기 ✕ (line 439):**
       - 전: `style={{ background: 'none', border: 'none', color: 'var(--t3)', fontSize: 20, cursor: 'pointer' }}`
       - 후: `className="bg-transparent border-0 text-text-tertiary text-[20px] cursor-pointer"` (텍스트 ✕ 유지)

    6. **데이터 없음 (line 447):**
       - 전: `style={{ textAlign: 'center', color: 'var(--t3)', padding: '30px 0', fontSize: 13 }}`
       - 후: `className="text-center text-text-tertiary py-[30px] text-[13px]"`

    7. **차트 3종 wrapper (line 449):**
       - 전: `style={{ display: 'flex', flexDirection: 'column', gap: 10 }}` → 후 `className="flex flex-col gap-2.5"`
       - 각 차트 컨테이너 `<div key={key}>`:
         - 라벨 전: `style={{ fontSize: 10, fontWeight: 700, color, marginBottom: 3 }}` → 후 `style={{ color }}` 유지 (color 가 raw hex — 변경: `color === '#3b82f6' ? 'var(--accent)' : color === '#f97316' ? 'var(--status-fire-bar)' : 'var(--status-safe-bar)'` 헬퍼 ternary 인라인 또는 차트 매핑 배열 자체에 토큰 `var(--*)` 사용)
         - **방안 (권장):** 차트 매핑 배열의 `color` 필드를 raw hex 대신 토큰 var 로 변경:
           ```tsx
           [
             { key: 'pressure_1' as const,   label: '1차압',  color: 'var(--accent)',          dashed: false },
             { key: 'pressure_2' as const,   label: '2차압',  color: 'var(--status-fire-bar)', dashed: false },
             { key: 'pressure_set' as const, label: '세팅압', color: 'var(--status-safe-bar)', dashed: true  },
           ]
           ```
         - 라벨 후: `className="text-[12px] font-bold mb-1"` + `style={{ color }}` (verbatim 10 → 12 격상, 색은 SVG 와 일관성 위해 inline style 유지)
       - overflow-x-auto wrapper 전: `style={{ overflowX: 'auto' }}` → 후 `className="overflow-x-auto"`

    8. **차트 SVG 내부 (line 470~519):**
       - svg `style={{ display: 'block' }}` 유지 OK (또는 `className="block"`)
       - Y축 tick text `fontSize="11"` → **`fontSize="12"`** (verbatim 11 → 12 격상)
       - Y축 tick fill `"rgba(139,148,158,0.7)"` 유지 (raw 허용)
       - grid line `stroke="rgba(255,255,255,0.05)"` 유지 (raw 허용)
       - X축 월 label `fontSize="10"` → **`fontSize="11"`** (verbatim 10 → 11 격상)
       - X축 fill `"rgba(139,148,158,0.6)"` 유지
       - polyline `stroke={color}` 유지 (color 는 위 토큰 var 로 변경됨)
       - circle `fill={isLate ? color : 'var(--bg2)'}` → `fill={isLate ? color : 'var(--surface-raised)'}` 변환
       - circle stroke `stroke={color}` 유지
       - 데이터 값 text `fontSize="11"` 유지 (sketch 06 매핑 동일)
       - 데이터 값 text fill `fill={color}` 유지

    9. **수치 테이블 (line 528~546):**
       - 외곽 전: `style={{ marginTop: 14, borderRadius: 10, border: '1px solid var(--bd)', overflow: 'hidden' }}` → 후 `className="mt-3.5 rounded-[10px] border border-border-default overflow-hidden"`
       - 헤더 row 전: `style={{ display: 'grid', gridTemplateColumns: '60px 1fr 1fr 1fr', background: 'var(--bg3)', padding: '7px 10px' }}` → 후 `className="grid grid-cols-[60px_1fr_1fr_1fr] bg-surface-sunken p-[8px_10px]"` (verbatim 7 → 8 padding 격상)
       - 헤더 cell 전: `style={{ fontSize: 9, fontWeight: 700, color: 'var(--t3)', textAlign: 'center' }}` → 후 `className="text-[12px] font-bold text-text-tertiary text-center"` (verbatim 9 → 12 격상)
       - 행 row 전: `style={{ display: 'grid', gridTemplateColumns: '60px 1fr 1fr 1fr', padding: '7px 10px', borderTop: '1px solid var(--bd)' }}` → 후 `className="grid grid-cols-[60px_1fr_1fr_1fr] p-[8px_10px] border-t border-border-default"`
       - 월 cell 전: `style={{ fontSize: 10, color: 'var(--t3)', textAlign: 'center', fontFamily: 'JetBrains Mono, monospace' }}` → 후 `className="text-[13px] text-text-secondary text-center font-mono"` (verbatim 10 → 13 격상 + text-tertiary → text-secondary)
       - 값 cell 변환 — raw hex `['#3b82f6','#f97316','#22c55e'][i]` 제거, className 으로 대체:
         ```tsx
         {[r.pressure_1, r.pressure_2, r.pressure_set].map((v, i) => (
           <div key={i} className={`text-[14px] font-bold text-center font-mono ${
             i === 0 ? 'text-accent' :
             i === 1 ? 'text-status-fire-bar' :
                       'text-status-safe-bar'
           }`}>{v != null ? v.toFixed(1) : '-'}</div>
         ))}
         ```
       - (verbatim 12 → 14 격상)

    **Wave 3 commit:**
    ```bash
    git add -A && git -c commit.gpgsign=false commit -m "feat(t3n): Wave 3 — 모바일 상세 모달 verbatim (sketch 06)" -m "- 모달 dim bg-black/50 + 시트 rounded-t-2xl p-[16/16/36] max-h-[80vh]" -m "- 타이틀 16px primary / 2줄 12px tertiary (verbatim 11→12 격상)" -m "- 연도 네비 ‹/› 텍스트 verbatim + 닫기 ✕ 텍스트 verbatim" -m "- 차트 매핑 배열 color → var(--accent/status-fire-bar/status-safe-bar) 토큰화" -m "- SVG Y축 12 / X축 11 / 값 11 + circle fill var(--surface-raised) (verbatim 11→12 + 10→11 격상)" -m "- 수치 테이블 헤더 12 / 행 월 13 / 행 값 14 (verbatim 9→12 / 10→13 / 12→14 격상)"
    ```

    **자체 검수:**
    - line 386~550 영역에 `style={{` = ternary color 매핑 inline ~3건 (차트 라벨 + circle fill 분기) 잔존 허용
    - `var(--(bg2|bg3|bd|t1|t2|t3|safe|warn|danger))` 0건
    - raw hex `#3b82f6` / `#f97316` / `#22c55e` 잔존 0건 (이 영역)
    - 비즈니스 로직 보존: hist 정렬 + 연도 필터 + sTicks + spx/spy + labels merge + circle isLate
  </action>
  <verify>
    <automated>cd cha-bio-safety && npx tsc --noEmit 2>&1 | grep -E "DivPage" || echo "Wave 3 tsc PASS"</automated>
  </verify>
  <done>
    Wave 3 변환 + atomic commit 완료. 모바일 상세 모달 sketch 06 verbatim + 노안 격상. tsc 0 에러.
  </done>
</task>

<task type="auto">
  <name>Task 5: Wave 4 — 데스크톱 chrome + 매트릭스 — sketch 07a + 07b verbatim + lucide</name>
  <files>cha-bio-safety/src/pages/DivPage.tsx</files>
  <action>
    **변환 대상:** renderDesktopLayout line 937~1085 (헤더 + 알림 배너 + 좌측 매트릭스 + 본문 wrapper).

    **선행: lucide import 추가 (Wave 4 진입 시점)**

    line 8 부근에 추가:
    ```tsx
    import { AlertTriangle } from 'lucide-react'
    ```
    
    (Wave 5 에서 BarChart3 + Droplets 추가될 때 같은 import 라인에 합치므로, 여기서는 일단 AlertTriangle 만 추가 — Wave 5 에서 확장)

    **변환 규칙 (sketch 07a + 07b verbatim + 글씨 격상 + lucide 아이콘):**

    1. **외곽 wrapper (line 939):**
       - 전: `style={{ height: '100%', display: 'flex', flexDirection: 'column', overflow: 'hidden', background: 'var(--bg)' }}`
       - 후: `className="h-full flex flex-col overflow-hidden bg-surface-page"`

    2. **헤더 (line 941~943) — sketch 07a verbatim:**
       - 전: `style={{ flexShrink: 0, height: 54, background: 'var(--bg2)', borderBottom: '1px solid var(--bd)', padding: '0 20px', display: 'flex', alignItems: 'center', gap: 10 }}`
       - 후: `className="flex-shrink-0 h-[54px] bg-surface-raised border-b border-border-default px-5 flex items-center gap-2.5"`
       - 제목 전: `style={{ flex: 1, fontSize: 16, fontWeight: 700, color: 'var(--t1)' }}` → 후 `className="flex-1 text-[16px] font-bold text-text-primary"` (verbatim — sketch 07a 16px)

    3. **알림 배너 (line 946~970) — sketch 07a VP4 (lucide + 색 점 div):**
       - wrapper 전: `style={{ flexShrink: 0, background: 'var(--bg2)', borderBottom: '1px solid var(--bd)', padding: '8px 24px', display: 'flex', alignItems: 'center', gap: 10, overflowX: 'auto' }}`
       - wrapper 후: `className="flex-shrink-0 bg-surface-raised border-b border-border-default p-[8px_24px] flex items-center gap-2.5 overflow-x-auto"`
       - 라벨 전: `<span style={{ fontSize: 12, fontWeight: 700, color: 'var(--t2)', flexShrink: 0 }}>⚠ 이상 {dangerList.length}건 · 주의 {warnList.length}건</span>`
       - 라벨 후:
         ```tsx
         <span className="inline-flex items-center gap-1.5 text-[12px] font-bold text-text-secondary flex-shrink-0">
           <AlertTriangle className="w-[14px] h-[14px]" />
           이상 {dangerList.length}건 · 주의 {warnList.length}건
         </span>
         ```
       - 칩 전:
         ```tsx
         style={{ flexShrink: 0, border: '1px solid', borderColor: item.status === 'danger' ? 'rgba(239,68,68,.4)' : 'rgba(245,158,11,.4)', background: item.status === 'danger' ? 'rgba(239,68,68,.12)' : 'rgba(245,158,11,.12)', color: item.status === 'danger' ? 'var(--danger)' : '#f59e0b', borderRadius: 16, padding: '4px 10px', fontSize: 11, fontWeight: 700, cursor: 'pointer', whiteSpace: 'nowrap' }}
         ```
       - 칩 후:
         ```tsx
         className={`flex-shrink-0 inline-flex items-center gap-1.5 border rounded-2xl px-2.5 py-1 text-[11px] font-bold cursor-pointer whitespace-nowrap ${
           item.status === 'danger'
             ? 'border-status-danger-bar/40 bg-status-danger-bg text-status-danger'
             : 'border-status-warning-bar/40 bg-status-warning-bg text-status-warning-bar'
         }`}
         ```
       - 칩 내용 (이모지 ●/◐ → 색 점 div):
         ```tsx
         <span className={`w-2 h-2 rounded-full inline-block ${item.status === 'danger' ? 'bg-status-danger' : 'bg-status-warning-bar'}`} />
         {item.point.id} {item.worstKind ?? ''}{item.pct != null ? ` ${item.pct > 0 ? '+' : ''}${item.pct}%` : ''}
         ```
       - 빈 상태 전: `<span style={{ fontSize: 11, color: 'var(--t3)' }}>모든 포인트 정상</span>` → 후 `<span className="text-[11px] text-text-tertiary">모든 포인트 정상</span>`

    4. **본문 wrapper (line 973):**
       - 전: `style={{ flex: 1, display: 'flex', overflow: 'hidden' }}` → 후 `className="flex-1 flex overflow-hidden"`

    5. **좌측 매트릭스 wrapper (line 975) — sketch 07b verbatim:**
       - 전: `style={{ flex: 1, minWidth: 0, borderRight: '1px solid var(--bd)', overflowY: 'auto', padding: '20px 24px' }}`
       - 후: `className="flex-1 min-w-0 border-r border-border-default overflow-y-auto p-[20px_24px]"`

    6. **테이블 헤더 (line 977~982) — sketch 07b 격상:**
       - 외곽 전: `style={{ display: 'grid', gridTemplateColumns: '80px repeat(3, 1fr)', gap: 6, marginBottom: 8, paddingBottom: 8, borderBottom: '1px solid var(--bd)' }}`
       - 외곽 후: `className="grid grid-cols-[80px_repeat(3,1fr)] gap-2 mb-2.5 pb-2.5 border-b border-border-default"` (verbatim 6 → 8 / 8 → 10 미세 격상)
       - 헤더 cell "층" 전: `style={{ fontSize: 11, fontWeight: 700, color: 'var(--t3)' }}` → 후 `className="text-[12px] font-bold text-text-tertiary"` (verbatim 11 → 12)
       - 컬럼 헤더 (3개) 전: `style={{ fontSize: 11, fontWeight: 700, color: 'var(--t3)', textAlign: 'center' }}` → 후 `className="text-[12px] font-bold text-text-tertiary text-center"`

    7. **각 행 (line 984~1059) — sketch 07b 셀 격상:**
       - 행 wrapper 전: `style={{ display: 'grid', gridTemplateColumns: '80px repeat(3, 1fr)', gap: 6, marginBottom: 6 }}` → 후 `className="grid grid-cols-[80px_repeat(3,1fr)] gap-2 mb-2"`
       - 층 라벨 전: `style={{ fontSize: 12, fontWeight: 700, color: 'var(--t2)', display: 'flex', alignItems: 'center', paddingLeft: 4 }}` → 후 `className="text-[13px] font-bold text-text-secondary flex items-center pl-1"` (verbatim 12 → 13)
       - **해당없음 셀 (line 996~1000):**
         - 전: `style={{ background: 'var(--bg3)', border: '1px dashed var(--bd)', borderRadius: 8, minHeight: 54, display: 'flex', alignItems: 'center', justifyContent: 'center', color: 'var(--t3)', fontSize: 14 }}`
         - 후: `className="bg-surface-sunken border border-dashed border-border-default rounded-lg min-h-[80px] flex items-center justify-center text-text-tertiary text-[16px] p-[10px_12px]"` (verbatim minHeight 54 → 80, fontSize 14 → 16, padding 통일 — sketch 07b "해당없음 셀 padding 동일 정렬")
       - **셀 컨테이너 (line 1014~1028) — verbatim 정상/주의/이상/선택 변형:**
         - bg/border 계산:
           ```tsx
           let bgClass = 'bg-surface-raised'
           let borderClass = 'border border-border-default'
           if (status === 'danger')      { bgClass = 'bg-status-danger-bg';  borderClass = 'border border-status-danger-bar/40' }
           else if (status === 'warn')   { bgClass = 'bg-status-warning-bg'; borderClass = 'border border-status-warning-bar/40' }
           else if (last)                { bgClass = 'bg-status-safe-bg';    borderClass = 'border border-status-safe-bar/25' }
           // 선택 시 border-2 + accent
           const selectedBorder = selected ? 'border-2 border-accent' : borderClass
           const padding = selected ? 'p-[9px_11px]' : 'p-[10px_12px]'
           ```
         - 컨테이너 후:
           ```tsx
           <div
             key={pos}
             onClick={() => setSelDiv(div)}
             className={`${bgClass} ${selectedBorder} ${padding} rounded-lg relative min-h-[80px] flex flex-col cursor-pointer transition-colors`}
           >
           ```
           (verbatim minHeight 64 → 80, padding 8/10 → 10/12 격상)
       - 배지 (line 1032~1036):
         - danger 후: `<span className="absolute top-1.5 right-2.5 text-[11px] font-bold text-status-danger">이상</span>` (verbatim 8 → 11 격상)
         - warn 후: `<span className="absolute top-1.5 right-2.5 text-[11px] font-bold text-status-warning-bar">주의</span>`
       - 좌상단 ID+월 (line 1038~1043):
         - wrapper 후: `className="absolute top-2 left-3 flex flex-col gap-0.5"`
         - ID 후: `className="text-[13px] font-bold text-text-primary leading-none"` (verbatim 10 → 13 격상)
         - 월 후: `className="text-[12px] text-text-tertiary leading-none"` (verbatim 9 → 12 격상)
       - 중앙 값 wrapper (line 1046):
         - 전: `style={{ flex: 1, display: 'flex', justifyContent: 'center', alignItems: 'center', gap: 8, fontFamily: 'JetBrains Mono, monospace', fontSize: 15, fontWeight: 700, lineHeight: 1 }}`
         - 후: `className="absolute bottom-2.5 left-0 right-0 flex justify-center items-center gap-2.5 font-mono leading-none"` (sketch 07b 의 absolute bottom 패턴 채택)
       - 값 span (verbatim 15 → 18 격상 + 컬럼 색 individual status):
         - 권장 — info(last) 가 worstKind 와 pct 가지므로 컬럼별 status 재계산. 단 sketch 07b 매핑은 컬럼별 색을 가짐. info 의 ranked top 만으로는 1차/2차/세팅 individual status 부족. **간단 처리:** pressureMap 의 hist[last-1] 다시 가져와 individual status 재계산 또는 info.worstKind 만 사용해 해당 컬럼만 status 색.
         - **간단 처리 채택:** pointStatusList 의 info 객체에 추가 정보 없으므로, 매트릭스 셀의 컬럼 색은 **status === 'ok' || status === 'warn' || status === 'danger' 의 worstKind 가 그 컬럼이면 status 색, 아니면 정상 컬럼 색** 으로:
           ```tsx
           const colColor = (col: '1차' | '2차' | '세팅') => {
             if (info?.worstKind === col) {
               return status === 'danger' ? 'text-status-danger' : status === 'warn' ? 'text-status-warning' : ''
             }
             return col === '1차' ? 'text-accent' : col === '2차' ? 'text-status-fire-bar' : 'text-status-safe-bar'
           }
           ```
         - 값 span:
           ```tsx
           <span className={`text-[18px] font-bold ${colColor('1차')}`}>{last.v1.toFixed(1)}</span>
           <span className="text-[13px] text-text-tertiary font-normal">|</span>
           <span className={`text-[18px] font-bold ${colColor('2차')}`}>{last.v2.toFixed(1)}</span>
           <span className="text-[13px] text-text-tertiary font-normal">|</span>
           <span className={`text-[18px] font-bold ${colColor('세팅')}`}>{last.vc.toFixed(1)}</span>
           ```
         - (verbatim 12 → 13 구분자 + 15 → 18 값 격상)

    8. **범례 (line 1062~1075) — sketch 07b 격상:**
       - 외곽 전: `style={{ marginTop: 16, padding: '10px 12px', borderTop: '1px solid var(--bd)', display: 'flex', gap: 16, fontSize: 11, color: 'var(--t3)', flexWrap: 'wrap' }}`
       - 외곽 후: `className="mt-4 p-[12px_12px] border-t border-border-default flex gap-4 flex-wrap text-[12px] text-text-tertiary"` (verbatim 10/12 → 12/12 + 11 → 12)
       - 항목 wrapper 후: `className="flex items-center gap-1.5"`
       - 색 박스 (정상): `className="inline-block w-[14px] h-[14px] bg-status-safe-bg border border-status-safe-bar/25 rounded-[3px]"` (verbatim 12 → 14)
       - 색 박스 (주의): `bg-status-warning-bg border border-status-warning-bar/40`
       - 색 박스 (이상): `bg-status-danger-bg border border-status-danger-bar/40`
       - 색 박스 (해당없음): `bg-surface-sunken border border-dashed border-border-default`

    9. **우측 상세 패널 wrapper (line 1079):**
       - 전: `style={{ flex: 1, minWidth: 0, overflowY: 'auto', padding: '20px 24px' }}` → 후 `className="flex-1 min-w-0 overflow-y-auto p-[20px_24px]"`

    **Wave 4 commit:**
    ```bash
    git add -A && git -c commit.gpgsign=false commit -m "feat(t3n): Wave 4 — 데스크톱 chrome + 매트릭스 verbatim (sketch 07a+07b)" -m "- lucide-react AlertTriangle import" -m "- 헤더 h-[54px] + px-5 + bg-surface-raised + 16px font-bold" -m "- 알림 배너 lucide AlertTriangle 14px + 칩 색 점 div w-2 h-2 (이모지 ⚠/●/◐ 폐기)" -m "- 칩 색 토큰화: status-danger-bg/bar/40 / status-warning-bg/bar/40" -m "- 매트릭스 grid 80px/3x1fr + 헤더 12 / 층 13 / 셀 minHeight 80 (verbatim 11→12 / 12→13 / 64→80 격상)" -m "- 셀 ID 13 / 월 12 / 중앙 18px mono / 구분자 13 (verbatim 10→13 / 9→12 / 15→18 / 12→13 격상)" -m "- 셀 컬럼 색 individual status: worstKind 일치 시 status 색 / 아니면 정상 색" -m "- 해당없음 셀 padding 통일 (정상 셀과 동일 p-[10px_12px])" -m "- 범례 14×14 박스 + 12px 라벨 (verbatim 12 → 14 / 11 → 12 격상)"
    ```

    **자체 검수:**
    - line 937~1085 영역에 `style={{` 0건 (단 dynamic 한 className 분기는 OK)
    - raw hex `#f59e0b` / `rgba(239,68,68,...)` / `rgba(245,158,11,...)` / `rgba(34,197,94,...)` 잔존 0건 (이 영역)
    - lucide AlertTriangle 호출 1+회
    - 비즈니스 로직 보존: pointStatusList 사용 + selDiv 비교 + setSelDiv 4 호출 (배너 + 매트릭스 셀)
  </action>
  <verify>
    <automated>cd cha-bio-safety && npx tsc --noEmit 2>&1 | grep -E "DivPage" || echo "Wave 4 tsc PASS"</automated>
  </verify>
  <done>
    Wave 4 변환 + atomic commit 완료. 데스크톱 chrome + 매트릭스 sketch 07a+07b verbatim + 글씨 격상 + lucide. tsc 0 에러.
  </done>
</task>

<task type="auto">
  <name>Task 6: Wave 5 — 데스크톱 우측 패널 — sketch 07c-1 + 07c-2 verbatim + lucide BarChart3/Droplets</name>
  <files>cha-bio-safety/src/pages/DivPage.tsx</files>
  <action>
    **변환 대상:** renderDesktopRightPanel line 757~934 (빈 상태 757~869 + 선택 상태 871~932).

    **lucide import 확장 (Wave 4 의 AlertTriangle 옆에 BarChart3, Droplets 추가):**
    ```tsx
    import { AlertTriangle, BarChart3, Droplets } from 'lucide-react'
    ```

    **변환 규칙 (sketch 07c-1 빈 상태 + sketch 07c-2 선택 상태):**

    1. **빈 상태 (no selDiv) — line 757~869:**

       **외곽 wrapper (line 794):**
       - 전: `style={{ display: 'flex', flexDirection: 'column', gap: 18 }}`
       - 후: `className="flex flex-col gap-[18px]"`

       **섹션 1: 측정점 현황 (line 796~806):**
       - 섹션 헤더 전: `<div style={{ fontSize: 12, fontWeight: 700, color: 'var(--t2)', marginBottom: 8 }}>◆ 34개 측정점 현황</div>`
       - 섹션 헤더 후:
         ```tsx
         <div className="inline-flex items-center gap-2 text-[13px] font-bold text-text-secondary mb-2.5">
           <BarChart3 className="w-4 h-4" />
           34개 측정점 현황
         </div>
         ```
       - 카운터 grid 전: `style={{ display: 'grid', gridTemplateColumns: 'repeat(3, 1fr)', gap: 10 }}` → 후 `className="grid grid-cols-3 gap-2.5"`
       - counters 배열: raw hex 제거, status 토큰 className 사용:
         ```tsx
         const counters = [
           { label: '정상', count: okCount,         bgClass: 'bg-status-safe-bg',    borderClass: 'border-status-safe-bar/25',    textClass: 'text-status-safe-bar' },
           { label: '주의', count: warnList.length, bgClass: 'bg-status-warning-bg', borderClass: 'border-status-warning-bar/40', textClass: 'text-status-warning-bar' },
           { label: '이상', count: dangerList.length, bgClass: 'bg-status-danger-bg',  borderClass: 'border-status-danger-bar/40',  textClass: 'text-status-danger' },
         ]
         ```
       - 카드 후:
         ```tsx
         <div key={c.label} className={`${c.bgClass} border ${c.borderClass} rounded-[10px] p-[14px_12px] text-center`}>
           <div className={`text-[28px] font-extrabold leading-none font-mono ${c.textClass}`}>{c.count}</div>
           <div className="text-[12px] text-text-secondary mt-1.5 font-semibold">{c.label}</div>
         </div>
         ```
         (verbatim 11 → 12 라벨 격상)

       **섹션 2: 배수/오일 (line 808~824):**
       - 섹션 헤더 후:
         ```tsx
         <div className="inline-flex items-center gap-2 text-[13px] font-bold text-text-secondary mb-2.5">
           <Droplets className="w-4 h-4" />
           배수/오일 현황
         </div>
         ```
       - grid 전: `style={{ display: 'grid', gridTemplateColumns: 'repeat(3, 1fr)', gap: 10 }}` → 후 `className="grid grid-cols-3 gap-2.5"`
       - logCards 배열 color 토큰화:
         ```tsx
         const logCards = [
           { label: '챔버 배수', sum: drainSum,     colorClass: 'text-status-info' },
           { label: '탱크 배수', sum: compDrainSum, colorClass: 'text-[#8b4513]' },  // raw 예외
           { label: '오일 보충', sum: oilSum,       colorClass: 'text-status-fire-bar' },
         ]
         ```
       - 카드 후:
         ```tsx
         <div key={card.label} className="bg-surface-raised border border-border-default rounded-[10px] p-[12px_12px]">
           <div className={`text-[12px] font-bold mb-1.5 tracking-[0.04em] ${card.colorClass}`}>{card.label}</div>
           <div className="text-[18px] font-extrabold text-text-primary font-mono">
             {card.sum.avg != null ? `평균 ${card.sum.avg}일` : '기록 없음'}
           </div>
           <div className="text-[12px] text-text-tertiary mt-1 font-mono">
             {card.sum.recent ? `최근 ${card.sum.recent}` : '—'}
           </div>
         </div>
         ```
         (verbatim 11/16/10 → 12/18/12 격상)

       **섹션 3: 이상/주의 리스트 (line 826~866):**
       - 섹션 헤더 후:
         ```tsx
         <div className="inline-flex items-center gap-2 text-[13px] font-bold text-text-secondary mb-2.5">
           <AlertTriangle className="w-4 h-4" />
           이상/주의 포인트
         </div>
         ```
       - alertItems.length === 0 빈 상태 전: `style={{ fontSize: 12, color: 'var(--t3)', padding: 16, textAlign: 'center', background: 'var(--bg2)', border: '1px solid var(--bd)', borderRadius: 8 }}`
       - 빈 상태 후: `className="text-[12px] text-text-tertiary p-4 text-center bg-surface-raised border border-border-default rounded-lg"`
       - 리스트 wrapper 전: `style={{ display: 'flex', flexDirection: 'column', gap: 6 }}` → 후 `className="flex flex-col gap-1.5"`
       - 각 row button 전: 
         ```tsx
         style={{ display: 'flex', alignItems: 'center', gap: 10, padding: '10px 12px', background: isDanger ? 'rgba(239,68,68,.08)' : 'rgba(245,158,11,.08)', border: \`1px solid ${isDanger ? 'rgba(239,68,68,.3)' : 'rgba(245,158,11,.3)'}\`, borderRadius: 8, cursor: 'pointer', textAlign: 'left' }}
         ```
       - 각 row button 후:
         ```tsx
         className={`flex items-center gap-2.5 p-[10px_12px] rounded-lg cursor-pointer text-left border ${
           isDanger
             ? 'bg-status-danger-bg/50 border-status-danger-bar/30'
             : 'bg-status-warning-bg/50 border-status-warning-bar/30'
         }`}
         ```
         (verbatim soft bg/border raw 를 토큰 + opacity modifier 로 변환. /50 으로 status-bg 의 .12 → .06 근사 — 시각적으로 .08 가까움. 만약 색 불일치 명확하면 `style={{ background: isDanger ? 'rgba(239,68,68,0.08)' : 'rgba(245,158,11,0.08)' }}` raw 유지 fallback OK)
       - 상태 라벨 전:
         ```tsx
         <span style={{ fontSize: 11, fontWeight: 700, color: isDanger ? 'var(--danger)' : '#f59e0b', flexShrink: 0, minWidth: 32 }}>{isDanger ? '● 이상' : '◐ 주의'}</span>
         ```
       - 상태 라벨 후 (lucide 안 씀 — sketch 07c-1 의 색 점 div 패턴):
         ```tsx
         <span className={`inline-flex items-center gap-1.5 text-[12px] font-bold flex-shrink-0 min-w-[56px] ${isDanger ? 'text-status-danger' : 'text-status-warning-bar'}`}>
           <span className={`w-2 h-2 rounded-full inline-block ${isDanger ? 'bg-status-danger' : 'bg-status-warning-bar'}`} />
           {isDanger ? '이상' : '주의'}
         </span>
         ```
         (verbatim 11 → 12, minWidth 32 → 56)
       - ID 전: `style={{ fontSize: 13, fontWeight: 700, color: 'var(--t1)', fontFamily: 'JetBrains Mono, monospace', flexShrink: 0, minWidth: 48 }}` → 후 `className="text-[14px] font-bold text-text-primary font-mono flex-shrink-0 min-w-[80px]"` (verbatim 13 → 14, minWidth 48 → 80)
       - 위치 전: `style={{ fontSize: 11, color: 'var(--t2)', flex: 1, overflow: 'hidden', whiteSpace: 'nowrap', textOverflow: 'ellipsis' }}` → 후 `className="text-[12px] text-text-secondary flex-1 overflow-hidden whitespace-nowrap text-ellipsis"` (11 → 12)
       - pct 전: `style={{ fontSize: 11, fontWeight: 700, color: isDanger ? 'var(--danger)' : '#f59e0b', fontFamily: 'JetBrains Mono, monospace', flexShrink: 0 }}` → 후 `className={\`text-[12px] font-bold font-mono flex-shrink-0 ${isDanger ? 'text-status-danger' : 'text-status-warning-bar'}\`}` (11 → 12)

    2. **선택 상태 (selDiv) — line 871~933 — sketch 07c-2 verbatim:**

       **외곽 wrapper (line 874):**
       - 전: `style={{ display: 'flex', flexDirection: 'column', gap: 14, paddingBottom: 40 }}` → 후 `className="flex flex-col gap-3.5 pb-10"`

       **헤더 row (line 876):**
       - 전: `style={{ display: 'flex', alignItems: 'center', gap: 10 }}` → 후 `className="flex items-center gap-2.5"`
       - 좌측 정보 컨테이너 전: `style={{ flex: 1, minWidth: 0 }}` → 후 `className="flex-1 min-w-0"`
       - 1줄 전: `style={{ fontSize: 16, fontWeight: 700, color: 'var(--t1)' }}` → 후 `className="text-[16px] font-bold text-text-primary"`
       - 2줄 전: `style={{ fontSize: 11, color: 'var(--t3)', marginTop: 2 }}` → 후 `className="text-[12px] text-text-tertiary mt-0.5"` (11 → 12)
       - 연도 네비 wrapper 전: `style={{ display: 'flex', alignItems: 'center', gap: 4, flexShrink: 0 }}` → 후 `className="flex items-center gap-1 flex-shrink-0"`
       - 양옆 wrapper 전: `style={{ width: 30, display: 'flex', justifyContent: 'center' }}` → 후 `className="w-[30px] flex justify-center"`
       - ‹ / › 버튼 전: `style={{ background: 'var(--bg3)', border: 'none', cursor: 'pointer', borderRadius: 6, padding: '4px 8px', color: 'var(--t2)', fontSize: 15 }}` → 후 `className="bg-surface-sunken border-0 rounded-md px-2 py-1 text-text-secondary text-[15px] cursor-pointer"` (텍스트 ‹ › verbatim 유지)
       - 연도 숫자 전: `style={{ fontSize: 13, fontWeight: 700, color: 'var(--t1)', width: 38, textAlign: 'center', display: 'inline-block' }}` → 후 `className="text-[13px] font-bold text-text-primary w-[38px] text-center inline-block"`
       - 닫기 ✕ 전: `style={{ background: 'none', border: 'none', color: 'var(--t3)', fontSize: 22, cursor: 'pointer', flexShrink: 0 }}` → 후 `className="bg-transparent border-0 text-text-tertiary text-[22px] cursor-pointer flex-shrink-0"` (텍스트 ✕ verbatim 유지)

       **내부 4탭 (line 907~925) — sketch 07c-2 verbatim:**
       - wrapper 전: `style={{ display: 'flex', borderBottom: '1px solid var(--bd)' }}` → 후 `className="flex border-b border-border-default"`
       - 각 버튼 전: `style={{ flex: 1, padding: '8px 4px', border: 'none', background: 'transparent', cursor: 'pointer', fontSize: 12, fontWeight: 600, color: tab === t.key ? 'var(--acl)' : 'var(--t3)', borderBottom: \`2px solid ${tab === t.key ? 'var(--acl)' : 'transparent'}\` }}`
       - 각 버튼 후:
         ```tsx
         className={`flex-1 p-[10px_4px] border-0 bg-transparent cursor-pointer text-[13px] font-semibold border-b-2 ${
           tab === t.key ? 'text-accent border-accent' : 'text-text-tertiary border-transparent'
         }`}
         ```
         (verbatim 8/4 → 10/4, 12 → 13 격상 — sketch 07c-2 명시)
       - 라벨 verbatim 유지: "압력 트렌드 / 챔버배수 / 오일 / 탱크배수" (모바일과 다름 — 짧은 형태 유지)

       **탭 콘텐츠 호출 (line 928~931) 그대로** — Wave 6 에서 변환됨.

    **Wave 5 commit:**
    ```bash
    git add -A && git -c commit.gpgsign=false commit -m "feat(t3n): Wave 5 — 데스크톱 우측 패널 verbatim (sketch 07c-1+07c-2)" -m "- lucide-react import 확장: AlertTriangle + BarChart3 + Droplets" -m "- 빈 상태 섹션 헤더 lucide BarChart3/Droplets/AlertTriangle 16px (◆/── → lucide)" -m "- 카운터 카드 28px mono extrabold + status 토큰 (정상 safe-bar / 주의 warning-bar / 이상 danger)" -m "- 배수/오일 카드 12/18/12 (verbatim 11/16/10 격상) + tank 색 raw #8b4513 예외" -m "- 알림 row 색 점 div w-2 h-2 + 14/12/12 + min-w-[80px] (verbatim 11/13/11 격상)" -m "- 선택 상태 헤더 16/12 + 연도 ‹/› + 닫기 ✕ verbatim 텍스트" -m "- 내부 4탭 p-[10px_4px] + text-[13px] font-semibold (verbatim 8/4 → 10/4 + 12 → 13 격상)" -m "- 4탭 라벨 데스크톱 짧은 형태 유지: 압력 트렌드 / 챔버배수 / 오일 / 탱크배수"
    ```

    **자체 검수:**
    - line 757~934 영역에 `style={{` 0건 (단 dynamic colorClass 매핑 OK)
    - raw hex `#f59e0b` / `rgba(239,68,68,.08)` / `rgba(245,158,11,.08)` 잔존 가능 (status-bg/50 fallback 시) — verify gate 에서 별도 체크
    - `#8b4513` 1건 잔존 OK (logCards 의 tank 색)
    - lucide BarChart3 + Droplets + AlertTriangle 호출 각 1+회
    - 비즈니스 로직 보존: summarizeMap 로직 + counters/logCards/alertItems 구성 + setSelDiv (알림 row) + setTab (4탭)
  </action>
  <verify>
    <automated>cd cha-bio-safety && npx tsc --noEmit 2>&1 | grep -E "DivPage" || echo "Wave 5 tsc PASS"</automated>
  </verify>
  <done>
    Wave 5 변환 + atomic commit 완료. 데스크톱 우측 패널 빈 상태 + 선택 상태 sketch 07c-1+07c-2 verbatim + lucide. tsc 0 에러.
  </done>
</task>

<task type="auto">
  <name>Task 7: Wave 6 — 데스크톱 차트 + 타임라인 — sketch 06 차트 + sketch 05 IntervalBar 패턴 mirror</name>
  <files>cha-bio-safety/src/pages/DivPage.tsx</files>
  <action>
    **변환 대상:**
    - renderDesktopPressureChart line 552~682
    - renderDesktopLogTimeline line 685~754

    **변환 규칙:**

    1. **renderDesktopPressureChart (line 552~682) — sketch 06 차트 + 수치 테이블 매핑 (Wave 3 와 동일 패턴):**

       빈 상태 (line 580):
       - 전: `style={{ color: 'var(--t3)', padding: '30px 0', textAlign: 'center', fontSize: 13 }}` → 후 `className="text-text-tertiary py-[30px] text-center text-[13px]"`

       차트 3종 외곽 (line 585):
       - 전: `style={{ display: 'flex', flexDirection: 'column', gap: 10 }}` → 후 `className="flex flex-col gap-2.5"`

       차트 매핑 배열 (line 587~589) — Wave 3 와 동일하게 color 를 토큰 var 로:
       ```tsx
       [
         { key: 'pressure_1' as const,   label: '1차압',  color: 'var(--accent)',          dashed: false },
         { key: 'pressure_2' as const,   label: '2차압',  color: 'var(--status-fire-bar)', dashed: false },
         { key: 'pressure_set' as const, label: '세팅압', color: 'var(--status-safe-bar)', dashed: true  },
       ]
       ```

       각 차트 라벨 (line 604):
       - 전: `style={{ fontSize: 10, fontWeight: 700, color, marginBottom: 3 }}` → 후 `className="text-[12px] font-bold mb-1"` + `style={{ color }}` (Wave 3 동일 패턴)

       SVG 내부 (line 606~654) — Wave 3 와 동일:
       - Y축 tick text `fontSize="11"` → **`"12"`**
       - X축 월 라벨 `fontSize="10"` → **`"11"`**
       - 데이터 값 text `fontSize="11"` 유지
       - circle `fill={isLate ? color : 'var(--bg2)'}` → `fill={isLate ? color : 'var(--surface-raised)'}`

       overflow-x-auto wrapper (line 605):
       - 전: `style={{ overflowX: 'auto' }}` → 후 `className="overflow-x-auto"`

       svg `style={{ display: 'block' }}` → `className="block"`

       수치 테이블 (line 663~679) — Wave 3 와 동일:
       - 외곽: `className="mt-3.5 rounded-[10px] border border-border-default overflow-hidden"`
       - 헤더 row: `className="grid grid-cols-[60px_1fr_1fr_1fr] bg-surface-sunken p-[8px_10px]"`
       - 헤더 cell: `className="text-[12px] font-bold text-text-tertiary text-center"`
       - 행 row: `className="grid grid-cols-[60px_1fr_1fr_1fr] p-[8px_10px] border-t border-border-default"`
       - 월 cell: `className="text-[13px] text-text-secondary text-center font-mono"`
       - 값 cell — raw hex 배열 제거, className 분기:
         ```tsx
         {[r.pressure_1, r.pressure_2, r.pressure_set].map((v, i) => (
           <div key={i} className={`text-[14px] font-bold text-center font-mono ${
             i === 0 ? 'text-accent' :
             i === 1 ? 'text-status-fire-bar' :
                       'text-status-safe-bar'
           }`}>{v != null ? v.toFixed(1) : '-'}</div>
         ))}
         ```

    2. **renderDesktopLogTimeline (line 685~754) — sketch 05 IntervalBar 패턴 mirror + 데스크톱 큰 사이즈:**

       color 변수 (line 688):
       - 전: `color = type === 'drain' ? '#38bdf8' : type === 'comp_drain' ? '#8b4513' : '#f97316'`
       - 후: `color = type === 'drain' ? 'var(--status-info)' : type === 'comp_drain' ? '#8b4513' : 'var(--status-fire-bar)'`

       외곽 wrapper (line 708):
       - 전: `style={{ display: 'flex', flexDirection: 'column', gap: 14 }}` → 후 `className="flex flex-col gap-3.5"`

       라벨 (line 709) — Wave 5 의 배수/오일 카드 패턴 mirror:
       - 전: `style={{ fontSize: 11, fontWeight: 700, color, letterSpacing: '.04em' }}` → 후 `className="text-[12px] font-bold tracking-[0.04em]"` + `style={{ color }}` (verbatim 11 → 12 격상)

       차트 박스 (line 710):
       - 전: `style={{ background: 'var(--bg2)', border: '1px solid var(--bd)', borderRadius: 10, padding: '18px 20px' }}` → 후 `className="bg-surface-raised border border-border-default rounded-[10px] p-[18px_20px]"`

       빈 상태 (line 712):
       - 전: `style={{ height: chartH, display: 'flex', alignItems: 'center', justifyContent: 'center', color: 'var(--t3)', fontSize: 13 }}`
       - 후: `className="flex items-center justify-center text-text-tertiary text-[13px]"` + `style={{ height: chartH }}` (chartH 는 동적)

       SVG 내부 (line 716~737) — sketch 05 IntervalBar 격상 패턴 mirror + 데스크톱 더 큰 사이즈:
       - 그대로 유지 (svg dynamic 좌표 + fill={color} 토큰 var 로 자동 적용)
       - `rect rx={6}` 유지
       - 일수 text `fontSize="20" fontWeight="700"` 유지 (이미 sketch 05 의 mobile 10 → desktop 20 매핑)
       - 월 label text `fontSize="16" fontWeight="700" fill="var(--t2)"` → `fill="var(--text-secondary)"` 변환
       - 일 label text `fontSize="14" fill="var(--t3)"` → `fill="var(--text-tertiary)"` 변환

       "최근 기록" 라벨 (line 740):
       - 전: `style={{ fontSize: 11, fontWeight: 700, color: 'var(--t3)', marginTop: 6 }}` → 후 `className="text-[12px] font-bold text-text-tertiary mt-1.5"` (11 → 12)

       빈 리스트 (line 742):
       - 전: `style={{ fontSize: 12, color: 'var(--t3)', padding: 16, textAlign: 'center' }}` → 후 `className="text-[13px] text-text-tertiary p-4 text-center"` (12 → 13)

       리스트 wrapper (line 744):
       - 전: `style={{ display: 'flex', flexDirection: 'column', gap: 4 }}` → 후 `className="flex flex-col gap-1"`

       날짜 row (line 746):
       - 전: `style={{ display: 'flex', gap: 10, padding: '8px 12px', background: 'var(--bg2)', borderRadius: 8, border: '1px solid var(--bd)' }}` → 후 `className="flex gap-2.5 p-[8px_12px] bg-surface-raised rounded-lg border border-border-default"`
       - 날짜 text 전: `style={{ fontSize: 12, fontFamily: 'JetBrains Mono, monospace', color: 'var(--t1)' }}` → 후 `className="text-[13px] font-mono text-text-primary"` (12 → 13)

    **Wave 6 commit:**
    ```bash
    git add -A && git -c commit.gpgsign=false commit -m "feat(t3n): Wave 6 — 데스크톱 차트 + 타임라인 verbatim (sketch 06 차트 + 05 IntervalBar mirror)" -m "- renderDesktopPressureChart: 차트 매핑 color 토큰화 + SVG Y축 12 / X축 11 / 값 11 + circle var(--surface-raised) (Wave 3 동일)" -m "- 수치 테이블 헤더 12 / 행 월 13 / 행 값 14 + 컬럼 색 className 분기" -m "- renderDesktopLogTimeline: color 토큰화 (status-info / #8b4513 / status-fire-bar)" -m "- 라벨 12 + 차트 박스 bg-surface-raised + SVG fill var(--text-secondary/tertiary)" -m "- 최근 기록 12 + 빈 리스트 13 + 날짜 row 13 (verbatim 11→12 / 12→13 격상)"
    ```

    **자체 검수:**
    - line 552~754 영역에 `style={{` = SVG 의 `style={{ display: 'block' }}` (또는 className 변환), 차트 라벨 color inline (~3건), 빈 상태 height (~1건). 의도된 dynamic 만 잔존 OK.
    - raw hex `#3b82f6` / `#f97316` / `#22c55e` / `#38bdf8` / `#f59e0b` 잔존 0건 (이 영역)
    - `#8b4513` 1건 잔존 OK (renderDesktopLogTimeline color 변수)
    - 비즈니스 로직 보존: hist 정렬 + 차트 좌표 계산 + intervals + maxDays + barMaxH 큰 사이즈
  </action>
  <verify>
    <automated>cd cha-bio-safety && npx tsc --noEmit 2>&1 | grep -E "DivPage" || echo "Wave 6 tsc PASS"</automated>
  </verify>
  <done>
    Wave 6 변환 + atomic commit 완료. 데스크톱 차트 + 타임라인 sketch 06+05 verbatim + 격상. tsc 0 에러. 6 wave 모두 끝.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 8: verify gate 9종 + SUMMARY + 사용자 컨펌</name>
  <what-built>
    DivPage.tsx (1136 LOC) 의 v3 sketch verbatim TSX 변환 완료.

    - **Wave 1:** 모바일 chrome (자체 헤더 sketch 01 + 4탭 sketch 02 verbatim)
    - **Wave 2:** 모바일 측정점 카드 + IntervalBar (sketch 03 옵션 B + sketch 04 좌측 색바 + sketch 05 격상)
    - **Wave 3:** 모바일 상세 모달 (sketch 06 verbatim + 글씨 노안 격상)
    - **Wave 4:** 데스크톱 chrome + 매트릭스 (sketch 07a + 07b verbatim + lucide AlertTriangle + 색 점 div)
    - **Wave 5:** 데스크톱 우측 패널 빈 상태 + 선택 상태 (sketch 07c-1 + 07c-2 verbatim + lucide BarChart3/Droplets)
    - **Wave 6:** 데스크톱 차트 + 타임라인 (sketch 06 차트 + sketch 05 IntervalBar 패턴 mirror)
    - 6개 atomic commit + lucide-react import 추가
  </what-built>
  <how-to-verify>
    **자동화 검수 9종 — 마지막 task 본인이 실행해서 결과 사용자에게 보여주기:**

    1. **tsc 0 에러:**
       ```bash
       cd cha-bio-safety && npx tsc --noEmit 2>&1 | tail -20
       ```
       → DivPage 관련 에러 0건

    2. **build PASS:**
       ```bash
       cd cha-bio-safety && npm run build 2>&1 | tail -30
       ```
       → "✓ built in ..." + 0 errors

    3. **옛 var() 토큰 0건:**
       ```bash
       grep -nE 'var\(--(bg|bg2|bg3|bd|bd2|t1|t2|t3|acl|safe|warn|danger|info|fire)\)' cha-bio-safety/src/pages/DivPage.tsx | grep -v '^\s*//' | wc -l
       ```
       → 0

    4. **raw hex 5종 0건 (단 #8b4513 만 예외 1-3건 허용):**
       ```bash
       grep -nE '#(3b82f6|f97316|22c55e|38bdf8|f59e0b)' cha-bio-safety/src/pages/DivPage.tsx | grep -v '^\s*//' | wc -l
       ```
       → 0
       ```bash
       grep -nE '#8b4513' cha-bio-safety/src/pages/DivPage.tsx | wc -l
       ```
       → 1-3 (IntervalBar/RightPanel)

    5. **컬럼명 '챔버' 오기 차단:**
       ```bash
       grep -n '챔버' cha-bio-safety/src/pages/DivPage.tsx | grep -vE '챔버배수|챔버 배수'
       ```
       → 0 결과 (출력 없음 = PASS)

    6. **lucide-react import 4종:**
       ```bash
       grep -nE "from 'lucide-react'" cha-bio-safety/src/pages/DivPage.tsx
       ```
       → 1 결과, named imports 에 AlertTriangle + BarChart3 + Droplets 포함

    7. **`style={{` 카운트 (의도된 dynamic 만):**
       ```bash
       grep -cE 'style=\{\{' cha-bio-safety/src/pages/DivPage.tsx
       ```
       → < 15 (대략 SVG style + 차트 라벨 color inline + 빈 상태 height dynamic + alert row bg raw fallback 정도)

    8. **비즈니스 로직 카운트:**
       ```bash
       echo "useQuery: $(grep -c 'useQuery' cha-bio-safety/src/pages/DivPage.tsx)"
       echo "pressureStatus: $(grep -c 'pressureStatus' cha-bio-safety/src/pages/DivPage.tsx)"
       echo "FLOOR_GROUPS.map: $(grep -c 'FLOOR_GROUPS.map' cha-bio-safety/src/pages/DivPage.tsx)"
       echo "setSelDiv: $(grep -c 'setSelDiv' cha-bio-safety/src/pages/DivPage.tsx)"
       echo "setTab: $(grep -c 'setTab' cha-bio-safety/src/pages/DivPage.tsx)"
       echo "fetchLogs: $(grep -c 'fetchLogs' cha-bio-safety/src/pages/DivPage.tsx)"
       ```
       → useQuery >= 5 (define 1 + 호출 4-5) / pressureStatus >= 4 / FLOOR_GROUPS.map = 3 / setSelDiv >= 4 / setTab >= 2 / fetchLogs >= 3

    9. **모바일 측정점 카드 격상 확인:**
       ```bash
       grep -cE 'text-\[22px\]' cha-bio-safety/src/pages/DivPage.tsx
       ```
       → > 0 (1열 큰 카드 패턴 적용)

    **자동화 결과 + 6개 wave commit 해시 + SUMMARY.md 작성 후 사용자에게 보고:**

    ```bash
    git log --oneline -8
    ```

    **SUMMARY.md 작성 (.planning/quick/260517-t3n-redesign-11-div-tsx-v3-sketch-1-7-verbat/260517-t3n-SUMMARY.md):**
    
    @$HOME/.claude/get-shit-done/templates/summary.md 형식 따라:
    - 6 wave atomic commit 해시 + 각 wave 변환 라인 범위 + sketch 매핑 요약
    - verify gate 9종 결과 표 (PASS/FAIL + 카운트)
    - 변환 후 lucide-react import 표 (AlertTriangle/BarChart3/Droplets)
    - 변환 후 `style={{` 의도된 잔존 위치 목록 (혹시 정상이 아니라면)
    - **사용자 컨펌 대기 안내:**
      "디자인 브랜치 작업이므로 user 명시 컨펌 후 main 머지 + production 배포 진행. 컨펌 키워드 = '머지 ㄱㄱ' 또는 'approved'. 그 전엔 redesign/11-div 브랜치에 push 만."

    **사용자가 직접 시각 확인할 수 있게 안내:**
    1. `npm run dev` 띄우고 모바일 viewport (393px) 에서 DIV 페이지 — 헤더/4탭/측정점 카드 (정상/주의/이상 변형 있으면)/상세 모달 확인
    2. 데스크톱 viewport (1280+) 에서 — 헤더/배너/매트릭스/우측 패널 빈 상태/측정점 클릭 후 선택 상태 확인
    3. 라이트/다크 모드 토글
    4. 옛 v1/v2 와 어떻게 다른지 (글씨 격상 + 좌측 색바 + lucide 아이콘 + 토큰 통일) 확인

    **사용자 결정 후 명시 컨펌 (예: "머지 ㄱㄱ") 받으면:**
    ```bash
    git checkout main && git merge --no-ff redesign/11-div && cd cha-bio-safety && npm run build && npx wrangler pages deploy dist --project-name cbc7119 --branch production --commit-message "feat(t3n): redesign 11-div TSX v3 sketch 1~7 verbatim merged"
    ```
    배포 후 https://cbc7119.pages.dev/div 결과 사용자에게 보고.
  </how-to-verify>
  <resume-signal>
    검수 통과 + SUMMARY 작성 + push 완료 후 사용자에게 결과 보고. 사용자 키워드 "머지 ㄱㄱ" 또는 "approved" 받기 전엔 main 머지/배포 안 함. "수정 필요" 받으면 wave별 fix 진입 (별도 quick 생성 또는 현재 plan 안에서 hotfix).
  </resume-signal>
</task>

</tasks>

<verification>
- DivPage.tsx 변환 후 tsc + build 0 에러
- 옛 var() 토큰 / raw hex 5종 0건 (단 #8b4513 1~3건 예외 허용)
- lucide-react import 추가 (AlertTriangle / BarChart3 / Droplets)
- `style={{` 카운트 < 15 (의도된 SVG/dynamic 만 잔존)
- 비즈니스 로직 카운트 보존 (useQuery / pressureStatus / FLOOR_GROUPS / setSelDiv / setTab / fetchLogs)
- 모바일 22px 압력값 className 적용 확인
- 6개 atomic commit + 다른 파일 변경 0건
</verification>

<success_criteria>
- 6 wave 모두 atomic commit 완료
- verify gate 9종 모두 PASS
- SUMMARY.md 작성 완료
- 사용자에게 시각 확인 안내 + 컨펌 대기 (디자인 브랜치 룰)
- redesign/11-div 브랜치에 push 완료
- 사용자 명시 컨펌 후에만 main 머지 + production 배포 진행
</success_criteria>

<output>
After completion, create `.planning/quick/260517-t3n-redesign-11-div-tsx-v3-sketch-1-7-verbat/260517-t3n-SUMMARY.md`.
</output>
