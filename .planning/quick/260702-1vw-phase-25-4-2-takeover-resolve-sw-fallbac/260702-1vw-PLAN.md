---
phase: quick-260702-1vw
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cha-bio-safety/src/pages/InspectionPage.tsx
  - cha-bio-safety/src/hooks/usePinchZoom.ts
  - cha-bio-safety/src/sw.ts
autonomous: true
requirements: [P2-1, P2-2, P2-3, SW-FALLBACK]

must_haves:
  truths:
    - "데스크톱에서 첫 경보 인지 후 두번째(다른 id) 경보가 오면 takeover 오버레이가 다시 뜬다"
    - "편집 중 15s 폴링이 alarm 상태를 뒤집어도 저장은 모달/pane 오픈 시점 스냅샷 기준으로 resolve↔create 분기한다"
    - "터치 더블탭 줌이 뒤이은 합성 dblclick 으로 자기리셋되지 않는다"
    - "url 없는 레거시/리마인더 푸시 클릭은 /fire-alarm 이 아닌 루트('/')로 이동한다 (prod 수렴)"
  artifacts:
    - path: "cha-bio-safety/src/pages/InspectionPage.tsx"
      provides: "P2-1 ackedId id-비교 takeover 게이트 + P2-2 오픈타임 스냅샷 저장분기(모바일/데스크톱)"
    - path: "cha-bio-safety/src/hooks/usePinchZoom.ts"
      provides: "P2-3 lastTouchAt 최근터치 가드로 합성 dblclick 무시"
    - path: "cha-bio-safety/src/sw.ts"
      provides: "SW-FALLBACK url 없는 푸시 fallback = '/' (prod 바이트 수렴)"
  key_links:
    - from: "handleAlarmAck (setAckedId)"
      to: "takeover gate (ackedId !== activeAlarm.id)"
      via: "인지한 alarm.id 저장 → 다른 id 는 게이트 통과"
      pattern: "ackedId !== activeAlarm.id"
    - from: "모달/pane open"
      to: "handleSave / handlePanelSave 분기"
      via: "openAlarmRef / panelOpenAlarmRef 스냅샷"
      pattern: "resolve\\(snap.id"
---

<objective>
Phase 25 화재수신반 프론트 코드리뷰 기능버그 4건을 surgical edit 으로 수정한다. design/main 에서 작업 → main 머지 시 cbc7119-preview 자동 배포 → 검증 후 prod 재승격 대상. 트랙 수렴이 목적.

Purpose: 두번째 경보 takeover 미노출(P2-1), 편집 중 폴링으로 인한 resolve↔create 오분기(P2-2), 줌 더블탭 자기리셋(P2-3), sw.ts fallback prod 미수렴(SW-FALLBACK) — 4건 모두 저노출·저리스크의 정확 패치.

Output: InspectionPage.tsx / usePinchZoom.ts / sw.ts 3개 파일의 국소 edit. 신규 파일 없음, 신규 패키지 없음, API 계약 변경 없음.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
# 이 워크트리는 DESIGN 트랙 (cbc7119-design). wrangler / npm run deploy 절대 금지.
# main 머지 = cbc7119-preview 자동 배포. 직원 도메인(cbc7119)은 이 워크트리에서 다루지 않는다.

@cha-bio-safety/src/pages/InspectionPage.tsx   # P2-1, P2-2 (FireAlarmModal 5350+, DesktopInspectionView 6022+)
@cha-bio-safety/src/hooks/usePinchZoom.ts       # P2-3 (182줄)
@cha-bio-safety/src/sw.ts                        # SW-FALLBACK (78줄)

## 불변 원칙 (모든 태스크 공통)
- DISPLAY 변수(`mode` / `panelMode` 및 모든 UI 데코)는 LIVE 유지 — 절대 스냅샷으로 바꾸지 않는다. 스냅샷 전환 대상은 **저장 분기(save-branch) + prefill** 뿐.
- FloorPlanPage.tsx 는 절대 수정 금지 (usePinchZoom 헤더 경고: 배포된 도면 줌의 source of truth). 오직 usePinchZoom.ts 만 변경.
- `Alarm` 타입은 InspectionPage.tsx line 6 에서 이미 import 됨 (`type Alarm`) → `useRef<Alarm | null | undefined>` 컴파일 OK.
- 각 edit 의 OLD 앵커는 verbatim 유일 문자열. resolve/create 본문·invalidate·toast·onClose 는 그대로 두고 조건과 `activeAlarm.id`→`snap.id` 참조만 바꾼다.
</context>

<tasks>

<task type="auto">
  <name>Task 1: FIX-1 (P2-1) 데스크톱 두번째 경보 takeover — sticky boolean → ackedId id-비교</name>
  <files>cha-bio-safety/src/pages/InspectionPage.tsx</files>
  <action>
DesktopInspectionView 에서 `alarmAcked` sticky boolean 을 `ackedId` (인지한 alarm.id) 로 교체한다. 3개 edit, 순서 무관하나 모두 적용.

ROOT CAUSE: `alarmAcked` 는 한번 true 가 되면 영구 유지 → 두번째(activeAlarm.id 가 바뀐) 경보가 와도 takeover 가 다시 뜨지 않는다. id 비교로 바꾸면 새 alarm.id ≠ 저장된 ackedId → takeover 재노출.

Edit A — 상태 선언 (line 6088):
  OLD: `  const [alarmAcked, setAlarmAcked] = useState(false)`
  NEW: `  const [ackedId, setAckedId] = useState<string | null>(null)`

Edit B — handleAlarmAck (line 6142-6146). 주석 한 줄 추가 + 본문 교체:
  OLD 앵커(4줄):
    `  // 경보 인지(ack) — 데스크톱 takeover 모달` 다음 `const handleAlarmAck = async () => {` 블록 전체:
    `    setAlarmAcked(true)`
    `    if (activeAlarm) { try { await alarmApi.ack(activeAlarm.id); qc.invalidateQueries({ queryKey: ['alarm-active'] }) } catch { /* 미배포 폴백 */ } }`
    `  }`
  NEW 본문:
    handleAlarmAck 첫 줄에 `const ackId = activeAlarm?.id ?? null`, 이어 `setAckedId(ackId)`, 이어 `if (ackId) { try { await alarmApi.ack(ackId); qc.invalidateQueries({ queryKey: ['alarm-active'] }) } catch { /* 미배포 폴백 */ } }`. 함수 위 주석에 `// P2-1: ackedId=인지한 alarm.id — 두번째(다른 id) 경보는 takeover 재노출` 추가.

Edit C — takeover 게이트 (line 6709):
  OLD: `      {activeAlarm && categoryIdx === null && !alarmAcked && (`
  NEW: `      {activeAlarm && categoryIdx === null && ackedId !== activeAlarm.id && (`

`alarmAcked` / `setAlarmAcked` 잔존 참조는 위 3곳이 전부 (grep 확인됨) — 교체 후 0건이어야 한다.
  </action>
  <verify>
    <automated>cd cha-bio-safety && grep -c "ackedId !== activeAlarm.id" src/pages/InspectionPage.tsx | grep -qx 1 && grep -c "setAckedId(ackId)" src/pages/InspectionPage.tsx | grep -qx 1 && [ "$(grep -c 'alarmAcked' src/pages/InspectionPage.tsx)" = "0" ] && echo P2-1-OK</automated>
  </verify>
  <done>`alarmAcked` 심볼이 파일에서 완전 제거되고, takeover 게이트가 `ackedId !== activeAlarm.id` 로 바뀌었으며, handleAlarmAck 이 `setAckedId(ackId)` + `alarmApi.ack(ackId)` 를 호출한다. DISPLAY 로직(panelMode 등) 무변경.</done>
</task>

<task type="auto">
  <name>Task 2: FIX-2 (P2-2) resolve↔create 오분기 — 오픈타임 스냅샷 (모바일 FireAlarmModal + 데스크톱 pane 양쪽)</name>
  <files>cha-bio-safety/src/pages/InspectionPage.tsx</files>
  <action>
ROOT CAUSE: handleSave / handlePanelSave 가 15s 폴링되는 LIVE `activeAlarm`/`mode` 로 분기한다. 편집 중 폴링이 alarm→normal (또는 반대)로 뒤집히면 저장이 잘못된 분기 → 중복 create 또는 오분 resolve. prefill 도 동일 hazard.
원칙: 오픈 시점 activeAlarm 을 1회 스냅샷 → SAVE 분기·PREFILL 이 스냅샷 사용. DISPLAY(`mode`/`panelMode` 및 UI 데코)는 LIVE 유지. 4개 edit (2a 모바일 2건 + 2b 데스크톱 2건).

── 2a. FireAlarmModal (모바일, 함수 line 5350). 모달은 오픈 시 mount → component-mount = open. ──

Edit 2a-1 — prefill effect 교체 (line 5410-5420, `// 경보중: activeAlarm.detectedAt` 주석 ~ `}, [mode, activeAlarm])` 블록 전체):
  기존 `const prefilledRef = useRef(false)` 를 제거하고(중복 선언 금지, prefilledRef 는 아래 블록 안에 딱 하나만), 아래 merged 스냅샷+prefill effect 로 교체:
    - `const openAlarmRef = useRef<Alarm | null | undefined>(undefined)`  // undefined=쿼리 미settle, null/Alarm=settle 스냅샷
    - `const prefilledRef = useRef(false)`
    - useEffect(() => { if (openAlarmRef.current === undefined && activeAlarm !== undefined) { openAlarmRef.current = maintOn ? null : (activeAlarm ?? null); const snap = openAlarmRef.current; if (snap && !prefilledRef.current) { prefilledRef.current = true; const [d, t] = (snap.detectedAt || '').split(' '); if (d) setDate(d); if (t) setTime(t.slice(0, 5)); setType('non_fire') } } }, [activeAlarm, maintOn])
    - 주석: `// P2-2: 폴링(15s)이 편집 중 alarm 상태를 뒤집어 resolve↔create 오분기하는 것 방지. 오픈 시점 activeAlarm 1회 스냅샷 → save 분기·prefill 이 스냅샷 사용(폴링값 X). display(mode)는 live 유지.`

Edit 2a-2 — handleSave 분기 (line 5426, 5428). 조건과 resolve 인자만 변경:
  OLD 앵커:
    `      if (mode === 'alarm' && activeAlarm) {`
    (다음 줄 주석 유지) ...
    `        await alarmApi.resolve(activeAlarm.id, { type, occurredAt: \`${date} ${time}:00\`, location, cause, action })`
  NEW:
    `      const snap = openAlarmRef.current`
    `      if (snap) {`
    ... 주석 그대로 ...
    `        await alarmApi.resolve(snap.id, { type, occurredAt: \`${date} ${time}:00\`, location, cause, action })`
  else 분기(fireAlarmApi.create)·invalidate·toast·onClose 는 그대로.

── 2b. DesktopInspectionView pane (함수 6022+, isPanel=line 6037, panelMode=line 6060). pane 은 mount 유지 상태에서 열림/닫힘 → isPanel 로 스냅샷 키잉, 닫힘 시 리셋해 재오픈마다 재스냅샷. ──

Edit 2b-1 — prefill effect 교체 (line 6091-6101, `// 경보중: activeAlarm.detectedAt` 주석 ~ `}, [panelMode, activeAlarm])` 블록 전체):
  기존 `const panelPrefilledRef = useRef(false)` 제거(중복 금지, 아래 블록 안 하나만). 교체:
    - `const panelOpenAlarmRef = useRef<Alarm | null | undefined>(undefined)`
    - `const panelPrefilledRef = useRef(false)`
    - useEffect(() => { if (isPanel) { if (panelOpenAlarmRef.current === undefined && activeAlarm !== undefined) { panelOpenAlarmRef.current = maintOn ? null : (activeAlarm ?? null); const snap = panelOpenAlarmRef.current; if (snap && !panelPrefilledRef.current) { panelPrefilledRef.current = true; const [d, t] = (snap.detectedAt || '').split(' '); if (d) setPaDate(d); if (t) setPaTime(t.slice(0, 5)); setPaType('non_fire') } } } else { panelOpenAlarmRef.current = undefined; panelPrefilledRef.current = false } }, [isPanel, activeAlarm, maintOn])
    - 주석: `// P2-2: 화재수신반 pane 열릴 때 activeAlarm 1회 스냅샷 → handlePanelSave 분기·prefill 이 스냅샷 사용(폴링값 X). display(panelMode)는 live 유지.`

Edit 2b-2 — handlePanelSave 분기 (line 6107-6108):
  OLD:
    `      if (panelMode === 'alarm' && activeAlarm) {`
    `        await alarmApi.resolve(activeAlarm.id, { type: paType, occurredAt: \`${paDate} ${paTime}:00\`, location: paLocation, cause: paCause, action: paAction })`
  NEW:
    `      const snap = panelOpenAlarmRef.current`
    `      if (snap) {`
    `        await alarmApi.resolve(snap.id, { type: paType, occurredAt: \`${paDate} ${paTime}:00\`, location: paLocation, cause: paCause, action: paAction })`
  else(create)·invalidate·toast·setCategoryIdx(null) 그대로.

주의: `mode` / `panelMode` 변수 및 렌더링 데코는 절대 손대지 않는다 (DISPLAY LIVE). `resolve(activeAlarm.id` 참조는 교체 후 0건, `resolve(snap.id` 는 2건이어야 한다.
  </action>
  <verify>
    <automated>cd cha-bio-safety && [ "$(grep -c 'resolve(activeAlarm.id' src/pages/InspectionPage.tsx)" = "0" ] && grep -c "resolve(snap.id" src/pages/InspectionPage.tsx | grep -qx 2 && grep -q "const openAlarmRef = useRef<Alarm" src/pages/InspectionPage.tsx && grep -q "const panelOpenAlarmRef = useRef<Alarm" src/pages/InspectionPage.tsx && [ "$(grep -c 'const prefilledRef = useRef' src/pages/InspectionPage.tsx)" = "1" ] && [ "$(grep -c 'const panelPrefilledRef = useRef' src/pages/InspectionPage.tsx)" = "1" ] && echo P2-2-OK</automated>
  </verify>
  <done>모바일·데스크톱 양쪽에서 저장 분기가 스냅샷(`openAlarmRef`/`panelOpenAlarmRef`) 기준으로 바뀌고 prefill 도 스냅샷 사용. `resolve(activeAlarm.id` 잔존 0건. `prefilledRef`/`panelPrefilledRef` 중복 선언 없음(각 1건). `mode`/`panelMode` DISPLAY 무변경.</done>
</task>

<task type="auto">
  <name>Task 3: FIX-3 (P2-3) 줌 더블탭 자기리셋 — 최근터치 가드로 합성 dblclick 무시</name>
  <files>cha-bio-safety/src/hooks/usePinchZoom.ts</files>
  <action>
ROOT CAUSE: 터치 시 onTouchStart(73-94)가 더블탭 줌을 이미 처리하는데, 브라우저가 이어서 `dblclick` MouseEvent 를 합성 → onDoubleClick(158)이 scale>1.5 를 보고 reset() 호출 → 방금 만든 줌을 취소. `dblclick` 은 pointerType 없는 MouseEvent 라 touch-recency 가드가 robust 한 해법. 훅 내부 self-contained, consumer 변경 없음. FloorPlanPage.tsx 절대 미수정.

Edit A — ref 추가 (line 46, `const lastTap = useRef(0)` 바로 다음 줄):
    `  const lastTouchAt = useRef(0)   // P2-3: 마지막 터치 시각 — 합성 dblclick 무시용`

Edit B — onTouchStart 첫 줄 (line 64 콜백 본문 첫 줄, `const ts = Array.from(e.touches)` 앞):
    `    lastTouchAt.current = Date.now()`

Edit C — onTouchEnd 첫 줄 (line 133 콜백 본문 첫 줄, `if (e.touches.length === 0)` 앞):
    `    lastTouchAt.current = Date.now()`

Edit D — onDoubleClick early-return (line 158-159):
  OLD:
    `  const onDoubleClick = useCallback((e: React.MouseEvent) => {`
    `    if (scaleRef.current > 1.5) {`
  NEW (사이에 2줄 삽입):
    `  const onDoubleClick = useCallback((e: React.MouseEvent) => {`
    `    // 터치 더블탭은 onTouchStart 가 이미 처리 → 뒤이어 합성되는 dblclick 은 무시 (자기리셋 방지)`
    `    if (Date.now() - lastTouchAt.current < 700) return`
    `    if (scaleRef.current > 1.5) {`

700ms 창은 터치 직후 수백 ms 내 발화하는 합성 dblclick 을 포함. maxScale/doubleTapScale 등 기존 줌 로직은 손대지 않는다.
  </action>
  <verify>
    <automated>cd cha-bio-safety && grep -c "Date.now() - lastTouchAt.current < 700" src/hooks/usePinchZoom.ts | grep -qx 1 && [ "$(grep -c 'lastTouchAt.current = Date.now()' src/hooks/usePinchZoom.ts)" = "2" ] && grep -q "const lastTouchAt = useRef(0)" src/hooks/usePinchZoom.ts && echo P2-3-OK</automated>
  </verify>
  <done>onDoubleClick 이 최근(700ms) 터치가 있으면 early-return 하여 합성 dblclick 이 reset() 을 호출하지 못한다. `lastTouchAt` 은 onTouchStart/onTouchEnd 에서 세팅(2곳). FloorPlanPage.tsx 무변경.</done>
</task>

<task type="auto">
  <name>Task 4: FIX-4 (SW-FALLBACK) sw.ts notificationclick fallback = '/' — prod 바이트 수렴</name>
  <files>cha-bio-safety/src/sw.ts</files>
  <action>
prod 브랜치와 바이트 일치 수렴 (git diff main production 으로 사전 검증됨). 실제 화재 경보는 data.url 을 실어 /fire-alarm 딥링크 유지되고, url 없는 레거시/리마인더 푸시만 fallthrough → 이는 '/fire-alarm' 이 아닌 '/' 로 가야 한다. 주석 2줄 + fallback 값 1개만 변경, 핸들러 나머지(matchAll/navigate/openWindow) 무변경.

Edit — line 57-63:
  OLD:
    `  // 목적지: data.url (백엔드 per-type url) 우선, 없으면 fallback`
    `  //   fire -> /fire-alarm (화재 takeover), equip -> /inspection?panel=fire-alarm (화재수신반 페이지)`
    `  const data = event.notification.data || {}`
    `  const fallback =`
    `    data.alarmType === 'equip' || data.type === 'equip'`
    `      ? '/inspection?panel=fire-alarm'`
    `      : '/fire-alarm'`
  NEW:
    `  // 목적지: data.url (백엔드 per-type url = /fire-alarm) 우선.`
    `  // url 없는 레거시/비-패널 푸시(일일·일정 리마인더 등)는 루트로 — 회귀 방지 (prod 260701-pnl).`
    `  const data = event.notification.data || {}`
    `  const fallback =`
    `    data.alarmType === 'equip' || data.type === 'equip'`
    `      ? '/inspection?panel=fire-alarm'`
    `      : '/'`
  (equip 분기 `/inspection?panel=fire-alarm` 는 그대로. 오직 마지막 `: '/fire-alarm'` → `: '/'` + 주석 2줄.)
  </action>
  <verify>
    <automated>cd cha-bio-safety && grep -q "회귀 방지 (prod 260701-pnl)" src/sw.ts && grep -Eq "^\s*: '/'$" src/sw.ts && [ "$(grep -c "'/fire-alarm'" src/sw.ts)" = "0" ] && echo SW-FALLBACK-OK</automated>
  </verify>
  <done>notificationclick fallback 이 equip=`/inspection?panel=fire-alarm`, 그 외=`'/'` 로 바뀌어 prod 와 수렴. `'/fire-alarm'` 리터럴 잔존 0건 (실제 화재는 data.url 로 딥링크).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| service worker push payload → notificationclick | data.url 은 백엔드가 세팅 (per-type). fallback 은 클라이언트 하드코딩 상수 |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1vw-01 | Tampering | 저장 분기(resolve↔create) | mitigate | 오픈타임 스냅샷으로 폴링 레이스 제거 (P2-2). 조작 아닌 타이밍 버그이나 결과는 데이터 무결성(중복/오분 레코드) 위협 → 스냅샷이 방어 |
| T-1vw-02 | Elevation/Info | sw.ts fallback url | accept | fallback 은 클라이언트 상수 (`'/'` / `'/inspection?panel=fire-alarm'`), 외부 입력 미포함. data.url 은 인증된 백엔드 origin. 낮은 리스크 |
| T-1vw-SC | Tampering | npm/pip/cargo installs | accept | 신규 패키지 설치 없음. 기존 의존성만 사용 → 공급망 표면 무변경 |
</threat_model>

<verification>
전체: `cd cha-bio-safety && npx tsc --noEmit` 타입체크 통과 (신규 `useRef<Alarm | null | undefined>` 포함).
개별: 각 태스크 verify 의 grep 마커 (P2-1-OK / P2-2-OK / P2-3-OK / SW-FALLBACK-OK).
불변 확인:
- FloorPlanPage.tsx 무변경 (git status 에 미등장).
- `mode`/`panelMode` DISPLAY 로직 무변경 (스냅샷은 save-branch + prefill 한정).
</verification>

<success_criteria>
- [ ] `npx tsc --noEmit` 통과
- [ ] `alarmAcked` 심볼 완전 제거, takeover 게이트 = `ackedId !== activeAlarm.id`
- [ ] `resolve(activeAlarm.id` 잔존 0건, `resolve(snap.id` 2건, prefilledRef/panelPrefilledRef 각 1건
- [ ] usePinchZoom 700ms 터치 가드 1건 + lastTouchAt 세팅 2건
- [ ] sw.ts fallback `'/fire-alarm'` 리터럴 0건, `'/'` fallback 존재
- [ ] FloorPlanPage.tsx / DISPLAY(mode·panelMode) 무변경
</success_criteria>

<output>
Create `.planning/quick/260702-1vw-phase-25-4-2-takeover-resolve-sw-fallbac/260702-1vw-SUMMARY.md` when done.
검증 후 main 머지 시 cbc7119-preview 자동 배포 (wrangler/deploy 직접 실행 금지). prod 재승격은 사용자 컨펌 후 별도 진행.
</output>
