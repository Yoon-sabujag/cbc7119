---
phase: quick-260515-m4h
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cha-bio-safety/src/pages/ElevatorPage.tsx
autonomous: true
requirements:
  - QUICK-260515-m4h
must_haves:
  truths:
    - "ElevatorPage.tsx 내 식별자 `var(--bg2)` / `var(--bd)` / `var(--bd2)` / `var(--t1)` / `var(--t2)` / `var(--t3)` / `var(--bg3)` 사용처 0건 (헬퍼 4종 본체 인라인 var 모두 v0.1.1 토큰으로 변환)"
    - "ElevatorPage.tsx 내 `zIndex:100` 정수 / `z-\\[100\\]` Tailwind 클래스 사용처 0건 (모두 110 으로 상승)"
    - "ElevatorPage.tsx 내 ✕ 이모지 (U+2715) 0건 (모두 lucide X 로 대체)"
    - "ElevatorPage.tsx 내 📷 이모지 (U+1F4F7) 0건 (MultiPhotoUpload 의 Camera 버튼은 lucide Camera 로 대체)"
    - "ElevatorPage.tsx 내 `type=\"datetime-local\"` 3건 input 의 className 에 `min-w-0` 토큰 존재 (iOS Safari intrinsic width overflow fix)"
    - "ElevatorPage.tsx 의 lucide-react import 에 `Camera` 추가 (다른 import 보존)"
    - "function ModalWrap / Field / EmptyState / MultiPhotoUpload 4개 정의 모두 보존 (시그니처 변경 0건 — 호출처 변경 불필요)"
    - "EmptyState 의 `icon: string` prop 시그니처 유지 (이모지 받아서 렌더 — 호출처 영향 0)"
    - "TypeScript 컴파일 0 에러 (`npm run build` 통과)"
    - "다른 컴포넌트/페이지/파일 0건 수정 (`git diff --name-only` 에 ElevatorPage.tsx + PLAN.md + SUMMARY.md 만 표시)"
    - "Wave 1~6 변환 결과 (list 탭, TYPE_ICON_COMPONENT, EvSelector, EsBtn, EsNodeMap, Fault 3 모달, RepairNewModal, EvDetailModal) 단 한 줄도 시각 결정 변경 X — z-index 110 상승만 적용"
  artifacts:
    - path: "cha-bio-safety/src/pages/ElevatorPage.tsx"
      provides: "헬퍼 4종 본체가 v0.1.1 토큰 + Tailwind + lucide 로 변환되고 BottomNav 회귀 + datetime-local overflow 가 fix된 ElevatorPage"
      contains: "function ModalWrap / function Field / function EmptyState / function MultiPhotoUpload"
  key_links: []
---

<objective>
redesign/07-elevator TSX 변환 시리즈 Wave 7. 옵션 A 묶음: (1) 헬퍼 4종 본체 변환 + (2) BottomNav 회귀 fix + (3) datetime-local input overflow fix. 단일 wave 단일 commit.

Purpose:
- Wave 1~6 에서 사용자 입력 모달은 다 변환됐지만 모달 외곽/필드/빈 상태/사진 업로드 헬퍼 본체는 "헬퍼 본체 수정 0건" 룰로 보존됐다. 호출 모달이 v0.1.1 토큰인데 외곽이 옛 인라인 var(--bg2) 그대로면 사용자 시각상 큰 영향이 있어 변환 필요.
- EvDetailModal 의 z-index 100 == BottomNav z-index 100 으로 동일 stacking level + DOM 순서로 BottomNav 가 위로 paint 됨. modal z-index 110 으로 상승해 root fix.
- iOS Safari 의 datetime-local native picker 가 box-sizing border-box 적용해도 intrinsic width 가 부모 boundary 넘는 overflow. `min-w-0 max-w-full appearance-none` 으로 fix.

Output: ElevatorPage.tsx 단일 파일 변경. 추정 +60/-90 줄 (인라인 style 압축). 다른 파일 0건. sketch 0건 (메모리 권위: `project_redesign_07_elevator_status.md` "sketch 불필요 (헬퍼는 시각 결정 단순)").
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/quick/260515-m4h-redesign-07-elevator-tsx-wave-7-helpers-fix/

# 메모리 권위
# - project_redesign_07_elevator_status.md → Wave 7 옵션 A 정의 (헬퍼 본체 + 회귀 fix). 사용자 옵션 A 컨펌 (2026-05-15)
# - feedback_redesign_sketch_rule_enforcement.md → executor 가 v0.1.1 토큰 룰 준수 + verify gate
# - project_redesign_workflow.md → redesign/07-elevator 브랜치 + 즉시 푸시, main 머지는 차수별 컨펌 후
# - feedback_inspection_unresolved_color.md (참고) → 색 토큰 룰 일관성
# - feedback_design_changes_ask_first.md → 시각/디자인 변경 시 컨펌 룰. 본 작업은 사용자 옵션 A 컨펌으로 권위 확보.

# 직전 변환 (참고만, 수정 X)
# - Wave 1 (260515-3mc): list 탭 + TYPE_ICON_COMPONENT + ElevatorIcon
# - Wave 2 (260515-ia6): EvSelector + EsBtn + EsNodeMap
# - Wave 3 (260515-iz1): FaultNewModal + FaultNewFullscreen + FaultResolveModal
# - Wave 4 (260515-jp3): RepairNewModal
# - Wave 5 (260515-k5p): EvDetailModal (KIND_STYLE)
# - Wave 6 (260515-kr9): InspectModal dead code cleanup

# 타깃 위치 (Read 검증 완료, 2026-05-15, main fast-forward 후)
# 1. z-index fix (BottomNav 회귀):
#    - line 1740: EvDetailModal isDesktop true → `'fixed z-[100] ...'`  → `z-[110]`
#    - line 1741: EvDetailModal isDesktop false → `'fixed z-[100] ...'` → `z-[110]`
#    - line 2188: FaultNewFullscreen → `style={{ ... zIndex:100, ... }}` → `zIndex:110`
#    - line 2519: ModalWrap isDesktop true → `zIndex:100` → `zIndex:110`
#    - line 2520: ModalWrap isDesktop false → `zIndex:100` → `zIndex:110`
#    - backdrop 90 은 그대로 (dim 은 BottomNav 보다 아래여도 의도된 동작)
#
# 2. datetime-local fix:
#    - line 2101 (FaultNewModal): input className 끝에 ` min-w-0 max-w-full appearance-none` 추가
#    - line 2223 (FaultNewFullscreen): 동일
#    - line 2314 (FaultResolveModal '수리 완료 일시'): 동일
#
# 3. 헬퍼 4종 본체 변환 (line 2512~2545, 2967~3013):
#    - ModalWrap (2512): 인라인 → Tailwind + v0.1.1 토큰 + lucide X
#    - Field (2531): 인라인 label → Tailwind 토큰
#    - EmptyState (2539): 인라인 → Tailwind 토큰 (icon prop 시그니처 유지)
#    - MultiPhotoUpload (2967): 인라인 → Tailwind 토큰 + 📷 → lucide Camera + ✕ → lucide X
#
# 4. import:
#    - line 16: lucide-react import 에 `Camera` 추가 (X 는 이미 있음)

@cha-bio-safety/src/pages/ElevatorPage.tsx
@cha-bio-safety/src/components/BottomNav.tsx
@cha-bio-safety/tailwind.config.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: z-index 상승 + datetime-local fix + 헬퍼 4종 본체 변환</name>
  <files>cha-bio-safety/src/pages/ElevatorPage.tsx</files>
  <action>
ElevatorPage.tsx 의 4 영역을 변경. 다른 파일 0건.

### 1. lucide-react import 에 Camera 추가 (line 16)

```ts
import { Package, UtensilsCrossed, MoveDiagonal, ChevronRight, ChevronUp, ChevronDown, AlertTriangle, Wrench, X, AlertOctagon, ClipboardCheck, FileSearch, CheckCircle2 } from 'lucide-react'
```
→
```ts
import { Package, UtensilsCrossed, MoveDiagonal, ChevronRight, ChevronUp, ChevronDown, AlertTriangle, Wrench, X, AlertOctagon, ClipboardCheck, FileSearch, CheckCircle2, Camera } from 'lucide-react'
```

### 2. z-index 5곳 100 → 110 (BottomNav 회귀 root fix)

BottomNav 의 z-index 100 과 동일 stacking level + DOM 순서로 BottomNav 가 위로 paint 됨. 모달 z-index 를 110 으로 명시적으로 상승.

- line 1740: `'fixed z-[100] bg-surface-raised rounded-2xl ...'` 내 `z-[100]` → `z-[110]`
- line 1741: `'fixed z-[100] left-0 right-0 bg-surface-raised ...'` 내 `z-[100]` → `z-[110]`
- line 2188: FaultNewFullscreen style 객체 내 `zIndex:100` → `zIndex:110`
- line 2519: ModalWrap isDesktop true style 객체 내 `zIndex:100` → `zIndex:110`
- line 2520: ModalWrap isDesktop false style 객체 내 `zIndex:100` → `zIndex:110`

backdrop 의 zIndex:90 은 보존 (의도된 동작 — dim 은 모달보다 아래 + BottomNav 와 같은 level 또는 아래로 두는 게 자연스러움).

### 3. datetime-local input 3곳 iOS overflow fix

세 위치 모두 input className 끝에 ` min-w-0 max-w-full appearance-none` 추가:

- line 2101 (FaultNewModal): className 의 `box-border` 다음에 ` min-w-0 max-w-full appearance-none` 추가
- line 2223 (FaultNewFullscreen): 동일
- line 2314 (FaultResolveModal): 동일

추가 후 className 예시:
```
className="w-full h-[42px] px-3 bg-surface-sunken border border-border-default rounded-lg text-text-primary text-body-sm focus:border-border-strong outline-none transition box-border min-w-0 max-w-full appearance-none"
```

### 4. ModalWrap 본체 변환 (line 2512~2529)

기존:
```tsx
function ModalWrap({ title, onClose, children }: { title:string; onClose:()=>void; children:React.ReactNode }) {
  const isDesktop = useIsDesktop()
  return (
    <>
      <div onClick={onClose} style={{ position:'fixed', inset:0, background:'rgba(0,0,0,.6)', zIndex:90 }} />
      <div style={
        isDesktop
          ? { position:'fixed', top:'50%', left:'50%', transform:'translate(-50%, -50%)', zIndex:110, background:'var(--bg2)', borderRadius:14, padding:'0 24px 24px', width:540, maxWidth:'90vw', maxHeight:'85vh', overflowY:'auto', overflowX:'hidden', border:'1px solid var(--bd2)', boxShadow:'0 20px 60px rgba(0,0,0,.5)' }
          : { position:'fixed', bottom:NAV_H, left:0, right:0, zIndex:110, background:'var(--bg2)', borderRadius:'20px 20px 0 0', padding:'0 16px 32px', maxHeight:'calc(100dvh - var(--sat, 44px) - var(--sab, 0px) - 54px)', overflowY:'auto', overflowX:'hidden' }
      }>
        <div style={{ display:'flex', alignItems:'center', padding:'14px 0 12px', borderBottom:'1px solid var(--bd)', marginBottom:14, position: isDesktop ? 'sticky' : 'static', top:0, background:'var(--bg2)', zIndex:1 }}>
          <span style={{ fontSize:14, fontWeight:700, color:'var(--t1)', flex:1 }}>{title}</span>
          <button onClick={onClose} style={{ background:'none', border:'none', color:'var(--t3)', cursor:'pointer', fontSize:18 }}>✕</button>
        </div>
        {children}
      </div>
    </>
  )
}
```

→ 변환:
```tsx
function ModalWrap({ title, onClose, children }: { title:string; onClose:()=>void; children:React.ReactNode }) {
  const isDesktop = useIsDesktop()
  return (
    <>
      <div onClick={onClose} className="fixed inset-0 bg-surface-overlay z-[90]" />
      <div
        className={
          isDesktop
            ? 'fixed z-[110] bg-surface-raised rounded-2xl w-[540px] max-w-[90vw] max-h-[85vh] overflow-y-auto overflow-x-hidden border border-border-strong px-6 pb-6'
            : 'fixed z-[110] left-0 right-0 bg-surface-raised rounded-t-[20px] px-4 pb-8 overflow-y-auto overflow-x-hidden'
        }
        style={
          isDesktop
            ? { top:'50%', left:'50%', transform:'translate(-50%, -50%)', boxShadow:'0 20px 60px rgba(0,0,0,.5)' }
            : { bottom:NAV_H, maxHeight:'calc(100dvh - var(--sat, 44px) - var(--sab, 0px) - 54px)' }
        }
      >
        <div
          className={[
            'flex items-center pt-[14px] pb-3 border-b border-border-default mb-[14px] bg-surface-raised',
            isDesktop ? 'sticky top-0 z-[1]' : '',
          ].join(' ')}
        >
          <span className="text-body-sm font-bold text-text-primary flex-1">{title}</span>
          <button
            onClick={onClose}
            className="bg-transparent border-0 text-text-tertiary cursor-pointer flex items-center justify-center p-1"
          >
            <X size={20} />
          </button>
        </div>
        {children}
      </div>
    </>
  )
}
```

토큰 매핑:
- `var(--bg2)` → `bg-surface-raised`
- `var(--bd)` → `border-border-default`
- `var(--bd2)` → `border-border-strong`
- `var(--t1)` → `text-text-primary`
- `var(--t3)` → `text-text-tertiary`
- `rgba(0,0,0,.6)` → `bg-surface-overlay`
- `fontSize:14, fontWeight:700` → `text-body-sm font-bold` (EvDetailModal Wave 5 와 동일 매핑)
- `padding:'14px 0 12px'` → `pt-[14px] pb-3`
- `marginBottom:14` → `mb-[14px]`
- `padding:'0 24px 24px'` (데스크톱) → `px-6 pb-6`
- `padding:'0 16px 32px'` (모바일) → `px-4 pb-8`
- `borderRadius:14` (데스크톱) → `rounded-2xl` (EvDetailModal 와 동일)
- `borderRadius:'20px 20px 0 0'` → `rounded-t-[20px]`
- `width:540, maxWidth:'90vw', maxHeight:'85vh'` → `w-[540px] max-w-[90vw] max-h-[85vh]`
- 보존 (Tailwind 없음): `boxShadow`, `top:'50%' transform`, `bottom:NAV_H`, dynamic `maxHeight:calc(...)`

### 5. Field 본체 변환 (line 2531~2537)

기존:
```tsx
function Field({ label, children, style }: { label:string; children:React.ReactNode; style?:React.CSSProperties }) {
  return (
    <div style={style}>
      <label style={{ fontSize:11, fontWeight:700, color:'var(--t2)', display:'block', marginBottom:5 }}>{label}</label>
      {children}
    </div>
  )
}
```

→ 변환:
```tsx
function Field({ label, children, style }: { label:string; children:React.ReactNode; style?:React.CSSProperties }) {
  return (
    <div style={style}>
      <label className="block text-label font-bold text-text-secondary mb-[5px]">{label}</label>
      {children}
    </div>
  )
}
```

토큰 매핑:
- `fontSize:11, fontWeight:700` → `text-label font-bold` (다른 Field 라벨 Wave 3/4 와 동일 매핑)
- `color:'var(--t2)'` → `text-text-secondary`
- `display:'block'` → `block`
- `marginBottom:5` → `mb-[5px]`
- `style` prop 은 보존 (호출처에서 `{ flex:1 }` 등 전달)

### 6. EmptyState 본체 변환 (line 2539~2545)

기존:
```tsx
function EmptyState({ icon, text }: { icon:string; text:string }) {
  return (
    <div style={{ display:'flex', flexDirection:'column', alignItems:'center', justifyContent:'center', padding:'40px 0', gap:10 }}>
      <div style={{ fontSize:36 }}>{icon}</div>
      <div style={{ fontSize:13, color:'var(--t3)' }}>{text}</div>
    </div>
  )
}
```

→ 변환:
```tsx
function EmptyState({ icon, text }: { icon:string; text:string }) {
  return (
    <div className="flex flex-col items-center justify-center py-10 gap-2.5">
      <div className="text-[36px]">{icon}</div>
      <div className="text-body-sm text-text-tertiary">{text}</div>
    </div>
  )
}
```

토큰 매핑:
- `display:'flex', flexDirection:'column'` → `flex flex-col`
- `alignItems:'center', justifyContent:'center'` → `items-center justify-center`
- `padding:'40px 0'` → `py-10`
- `gap:10` → `gap-2.5`
- `fontSize:36` → `text-[36px]` (이모지 사이즈 직접 토큰화)
- `fontSize:13` → `text-body-sm`
- `color:'var(--t3)'` → `text-text-tertiary`

**icon prop 시그니처 `string` 유지** — 호출처에서 이모지 문자열 전달 (변경 0건).

### 7. MultiPhotoUpload 본체 변환 (line 2967~3013)

기존 (전체 47줄, 인라인 style 가득 + 📷 + ✕ 이모지):
- div 라벨 (fontSize:11, fontWeight:700, color:'var(--t2)') → Tailwind 토큰
- input 2개 (cam/album, display:'none') → `hidden`
- 사진 추가 버튼 (width:64, height:64, dashed border, 📷 + '추가' 텍스트) → Tailwind + lucide Camera
- 사진 카드 (이미지 + ✕ 삭제 버튼) → Tailwind + lucide X
- gap, overflowX 등 → Tailwind

→ 변환:
```tsx
function MultiPhotoUpload({ label, keys, setKeys, max = 5 }: { label: string; keys: string[]; setKeys: (k: string[]) => void; max?: number }) {
  const [uploading, setUploading] = useState(false)
  const [showPicker, setShowPicker] = useState(false)
  const camRef = useRef<HTMLInputElement>(null)
  const albRef = useRef<HTMLInputElement>(null)

  const handleAdd = async (file: File) => {
    if (keys.length >= max) { toast.error(`사진은 최대 ${max}장까지 가능합니다`); return }
    setUploading(true)
    try {
      const { compressImage } = await import('../utils/imageUtils')
      const compressed = await compressImage(file)
      const fd = new FormData()
      fd.append('file', compressed)
      const token = useAuthStore.getState().token
      const res = await fetch('/api/uploads', { method: 'POST', body: fd, headers: { Authorization: `Bearer ${token}` } })
      const json = await res.json() as any
      if (!json.success) throw new Error(json.error)
      setKeys([...keys, json.data.key])
    } catch { toast.error('업로드 실패') }
    setUploading(false)
  }

  const onFileChange = (e: React.ChangeEvent<HTMLInputElement>) => { const f = e.target.files?.[0]; if (f) handleAdd(f); e.target.value = '' }

  return (
    <div>
      <div className="text-label font-bold text-text-secondary mb-1.5">{label} ({keys.length}/{max})</div>
      <input ref={camRef} type="file" accept="image/*" capture="environment" className="hidden" onChange={onFileChange} />
      <input ref={albRef} type="file" accept="image/*" className="hidden" onChange={onFileChange} />
      <PhotoSourceModal open={showPicker} onClose={() => setShowPicker(false)} onCamera={() => camRef.current?.click()} onAlbum={() => albRef.current?.click()} />
      <div className="flex gap-1.5 overflow-x-auto">
        {keys.length < max && (
          <button
            onClick={() => !uploading && setShowPicker(true)}
            className={[
              'w-16 h-16 flex-shrink-0 rounded-lg border border-dashed border-border-strong bg-surface-sunken',
              'flex flex-col items-center justify-center gap-0.5',
              uploading ? 'cursor-wait' : 'cursor-pointer',
            ].join(' ')}
          >
            <Camera size={18} className="text-text-tertiary" />
            <span className="text-[8px] text-text-tertiary font-semibold">{uploading ? '...' : '추가'}</span>
          </button>
        )}
        {keys.map((key, idx) => (
          <div key={key} className="relative w-16 h-16 flex-shrink-0">
            <img
              src={`/api/uploads/${key}`}
              alt=""
              className="w-16 h-16 object-cover rounded-lg border border-border-default"
            />
            <button
              onClick={() => setKeys(keys.filter((_,i) => i !== idx))}
              className="absolute -top-1 -right-1 w-4 h-4 rounded-full bg-danger text-white border-0 cursor-pointer flex items-center justify-center"
            >
              <X size={9} strokeWidth={3} />
            </button>
          </div>
        ))}
      </div>
    </div>
  )
}
```

토큰 매핑:
- 라벨 fontSize:11/fontWeight:700/var(--t2)/marginBottom:6 → `text-label font-bold text-text-secondary mb-1.5`
- `display:'none'` → `hidden`
- 사진 추가 버튼: width:64/height:64 → `w-16 h-16`, `flexShrink:0` → `flex-shrink-0`, `borderRadius:8` → `rounded-lg`, `border:'1px dashed var(--bd2)'` → `border border-dashed border-border-strong`, `background:'var(--bg3)'` → `bg-surface-sunken`, flex/col/items/justify → Tailwind
- `gap:2` → `gap-0.5`
- 📷 (fontSize:18) → `<Camera size={18} className="text-text-tertiary" />`
- 추가 텍스트 fontSize:8/color:var(--t3)/fontWeight:600 → `text-[8px] text-text-tertiary font-semibold`
- 카드 wrapper `position:'relative', width:64, height:64, flexShrink:0` → `relative w-16 h-16 flex-shrink-0`
- 이미지 width:64/height:64/objectFit:'cover'/borderRadius:8/border → `w-16 h-16 object-cover rounded-lg border border-border-default`
- ✕ 삭제 버튼 position:'absolute', top:-4, right:-4 → `absolute -top-1 -right-1`
- width:16/height:16/borderRadius:'50%' → `w-4 h-4 rounded-full`
- background:'var(--danger)', color:'#fff' → `bg-danger text-white`
- border:'none' → `border-0`
- ✕ 이모지 (fontSize:9) → `<X size={9} strokeWidth={3} />` (작은 사이즈에서 가독성 위해 strokeWidth 3)
- gap:6, overflowX:'auto' (gallery) → `gap-1.5 overflow-x-auto`

### 보존 항목 (절대 건드리지 말 것)

- ModalWrap / Field / EmptyState / MultiPhotoUpload 4개의 시그니처 (호출처 변경 0건)
- backdrop zIndex:90
- input ref logic, file change handler, photo upload mutation, compressImage import
- PhotoSourceModal import 와 사용
- 다른 컴포넌트 (EvDetailModal / Fault 3 모달 / RepairNewModal / RepairListSection / CertViewerModal / EsBtn / EsNodeMap / EvSelector / TYPE_ICON_COMPONENT)
- 다른 페이지/파일, tailwind.config.js, icons.tsx

### 작업 순서 (안전)

1. **import line 16 수정** — Camera 추가 (TypeScript 가 사용처에서 자동 체크)
2. **z-index 5곳 100 → 110** (간단한 정수 변경, 영향 최소)
3. **datetime-local 3곳 className 토큰 추가** (append-only, 다른 클래스 영향 없음)
4. **ModalWrap 본체 변환** (line 2512~2529)
5. **Field 본체 변환** (line 2531~2537)
6. **EmptyState 본체 변환** (line 2539~2545)
7. **MultiPhotoUpload 본체 변환** (line 2967~3013)
8. `npm run build` PASS 확인 — TS 0 에러, ElevatorPage chunk 빌드 성공
9. grep gate 통과 확인 (verify 블록)
10. commit
  </action>
  <verify>
    <automated>
cd cha-bio-safety && \
  echo "=== Section A: 인라인 var() 제거 검증 (헬퍼 본체 변환) ===" && \
  echo "var(--bg2) in ElevatorPage: $(grep -c \"var(--bg2)\" src/pages/ElevatorPage.tsx) (expect 0)" && \
  echo "var(--bd) in ElevatorPage: $(grep -c \"var(--bd)\" src/pages/ElevatorPage.tsx) (expect 0)" && \
  echo "var(--bd2) in ElevatorPage: $(grep -c \"var(--bd2)\" src/pages/ElevatorPage.tsx) (expect 0)" && \
  echo "var(--t1) in ElevatorPage: $(grep -c \"var(--t1)\" src/pages/ElevatorPage.tsx) (expect 0)" && \
  echo "var(--t2) in ElevatorPage: $(grep -c \"var(--t2)\" src/pages/ElevatorPage.tsx) (expect 0)" && \
  echo "var(--t3) in ElevatorPage: $(grep -c \"var(--t3)\" src/pages/ElevatorPage.tsx) (expect 0)" && \
  echo "var(--bg3) in ElevatorPage: $(grep -c \"var(--bg3)\" src/pages/ElevatorPage.tsx) (expect 0)" && \
  echo "" && \
  echo "=== Section B: z-index 110 상승 검증 ===" && \
  echo "zIndex:100 in ElevatorPage: $(grep -c 'zIndex:100' src/pages/ElevatorPage.tsx) (expect 0)" && \
  echo "z-[100] in ElevatorPage: $(grep -c 'z-\\[100\\]' src/pages/ElevatorPage.tsx) (expect 0)" && \
  echo "zIndex:110 in ElevatorPage: $(grep -c 'zIndex:110' src/pages/ElevatorPage.tsx) (expect 3 — ModalWrap x2 + FaultNewFullscreen x1)" && \
  echo "z-[110] in ElevatorPage: $(grep -c 'z-\\[110\\]' src/pages/ElevatorPage.tsx) (expect 2 — EvDetailModal isDesktop true/false)" && \
  echo "" && \
  echo "=== Section C: 이모지 제거 + lucide 변환 검증 ===" && \
  echo "Camera lucide import: $(grep -c \"Camera\" src/pages/ElevatorPage.tsx) (expect >= 2 — import 1 + 사용 1)" && \
  echo "✕ emoji (U+2715) in ElevatorPage: $(grep -c '✕' src/pages/ElevatorPage.tsx) (expect 0)" && \
  echo "📷 emoji in ElevatorPage: $(grep -c '📷' src/pages/ElevatorPage.tsx) (expect 0)" && \
  echo "" && \
  echo "=== Section D: datetime-local fix ===" && \
  echo "datetime-local total: $(grep -c 'type=\"datetime-local\"' src/pages/ElevatorPage.tsx) (expect 3)" && \
  echo "datetime-local with min-w-0: $(grep 'type=\"datetime-local\"' src/pages/ElevatorPage.tsx | head -3) " && \
  test $(grep -c 'min-w-0 max-w-full appearance-none' src/pages/ElevatorPage.tsx) -ge 3 && echo "min-w-0 적용 >= 3 (PASS)" || { echo "min-w-0 적용 < 3 (FAIL)"; exit 1; } && \
  echo "" && \
  echo "=== Section E: 헬퍼 시그니처 보존 ===" && \
  echo "function ModalWrap: $(grep -c '^function ModalWrap' src/pages/ElevatorPage.tsx) (expect 1)" && \
  echo "function Field: $(grep -c '^function Field' src/pages/ElevatorPage.tsx) (expect 1)" && \
  echo "function EmptyState: $(grep -c '^function EmptyState' src/pages/ElevatorPage.tsx) (expect 1)" && \
  echo "function MultiPhotoUpload: $(grep -c '^function MultiPhotoUpload' src/pages/ElevatorPage.tsx) (expect 1)" && \
  echo "" && \
  echo "=== Hard gate: 모든 var() 0건 ===" && \
  test $(grep -cE "var\\(--(bg2|bd|bd2|t1|t2|t3|bg3)\\)" src/pages/ElevatorPage.tsx) -eq 0 && \
  test $(grep -c 'zIndex:100' src/pages/ElevatorPage.tsx) -eq 0 && \
  test $(grep -c 'z-\\[100\\]' src/pages/ElevatorPage.tsx) -eq 0 && \
  test $(grep -c '✕' src/pages/ElevatorPage.tsx) -eq 0 && \
  test $(grep -c '📷' src/pages/ElevatorPage.tsx) -eq 0 && \
  echo "" && \
  echo "=== npm build ===" && \
  npm run build 2>&1 | tail -30 && \
  echo "=== build PASS ==="
    </automated>
  </verify>
  <done>
- grep gate 모두 통과: var(--bg2/bd/bd2/t1/t2/t3/bg3) 0건, zIndex:100/z-[100] 0건, ✕/📷 0건
- zIndex:110 3건 + z-[110] 2건 (총 5건 z-index 상승)
- datetime-local 3건 모두 min-w-0 max-w-full appearance-none 적용
- 헬퍼 4종 함수 정의 시그니처 보존 (호출처 변경 0건)
- npm run build PASS — TypeScript 0 에러, ElevatorPage chunk 빌드 성공
- 다른 파일 0건 변경 (`git status` 에 ElevatorPage.tsx + PLAN.md + SUMMARY.md 만)
  </done>
</task>

<task type="auto">
  <name>Task 2: SUMMARY 작성 + commit</name>
  <files>.planning/quick/260515-m4h-redesign-07-elevator-tsx-wave-7-helpers-fix/260515-m4h-SUMMARY.md</files>
  <action>
SUMMARY 작성 후 single commit.

### SUMMARY 내용

@$HOME/.claude/get-shit-done/templates/summary.md 구조 따름. 필수 섹션:

- **What changed**: ElevatorPage.tsx 4 영역. (1) lucide-react import 에 Camera 추가. (2) z-index 5곳 100 → 110 (ModalWrap x2 + EvDetailModal x2 + FaultNewFullscreen x1). (3) datetime-local input 3곳 className 에 `min-w-0 max-w-full appearance-none` 추가. (4) 헬퍼 4종 본체 (ModalWrap / Field / EmptyState / MultiPhotoUpload) 인라인 → Tailwind v0.1.1 토큰 + lucide (✕→X, 📷→Camera) 변환.
- **Why**:
  - z-index fix: BottomNav z-100 동일 stacking → DOM 순서로 BottomNav 가 모달 위로 paint 됨. 모달 z-110 으로 명시적 상승해 root fix.
  - datetime-local fix: iOS Safari native picker 가 box-border 적용해도 intrinsic width 가 부모 boundary 넘어서 우측 overflow. min-w-0 + max-w-full + appearance-none 로 강제 컨테이너 boundary 준수.
  - 헬퍼 본체: Wave 1~6 사용자 입력 모달은 v0.1.1 토큰화됐는데 외곽/필드/빈 상태/사진 업로드는 옛 인라인 var(--bg2) 그대로 → 일관성 결손. 메모리 권위 (project_redesign_07_elevator_status.md) "sketch 불필요, 헬퍼는 시각 결정 단순" 룰로 sketch 0건 + 다른 변환된 모달과 동일 토큰 매핑 직접 적용.
- **Preserved**: 헬퍼 4종 시그니처 (호출처 변경 0건), backdrop zIndex:90, photo upload mutation logic, Wave 1~6 변환 결과 (z-index 상승만 적용, 다른 시각 결정 0건 변경).
- **Verification**: grep gate Section A~E 모두 PASS, npm build PASS, git status ElevatorPage.tsx + PLAN.md + SUMMARY.md 만.
- **Out of scope**: 5 탭 본문 변환 (옵션 B). InspectionPage 등 다른 페이지.
- **Visual impact**:
  - 헬퍼 변환으로 모든 모달 외곽이 일관된 v0.1.1 토큰 표시 (이전엔 모달 콘텐츠 ≠ 모달 외곽 톤 불일치)
  - 이모지 → lucide 로 폰트 의존성 제거 (모든 OS 에서 동일 모양)
  - BottomNav 회귀 해결 (모달이 BottomNav 위에 정상 표시)
  - iOS datetime-local input 컨테이너 boundary 준수
- **Next**: 사용자 검수 후 main 머지 + 배포. 이어서 5 탭 본문 sketch+변환 (옵션 B) — 탭별 wave.

### Commit

```bash
cd /Users/jykevin/Documents/cbc7119-design
git add cha-bio-safety/src/pages/ElevatorPage.tsx \
        .planning/quick/260515-m4h-redesign-07-elevator-tsx-wave-7-helpers-fix/260515-m4h-PLAN.md \
        .planning/quick/260515-m4h-redesign-07-elevator-tsx-wave-7-helpers-fix/260515-m4h-SUMMARY.md
git commit -m "feat(260515-m4h): Wave 7 — 헬퍼 4종(ModalWrap/Field/EmptyState/MultiPhotoUpload) v0.1.1 토큰 + Tailwind + lucide(Camera/X) 변환 + BottomNav 회귀 fix(z-100→110) + datetime-local overflow fix"
git push origin redesign/07-elevator
```

배포 X. redesign/07-elevator 브랜치 push 만 (메모리 feedback_deploy_test.md — main 머지+배포는 사용자 명시 컨펌 후).
  </action>
  <verify>
    <automated>
cd /Users/jykevin/Documents/cbc7119-design && \
  ls .planning/quick/260515-m4h-redesign-07-elevator-tsx-wave-7-helpers-fix/260515-m4h-SUMMARY.md && \
  git log --oneline -1 | grep -q '260515-m4h' && \
  echo "=== final build verify ===" && \
  cd cha-bio-safety && npm run build 2>&1 | tail -10
    </automated>
  </verify>
  <done>
- SUMMARY.md 작성 완료
- single commit 생성 (PLAN + SUMMARY + ElevatorPage.tsx)
- 최종 npm build PASS 재확인
- redesign/07-elevator 브랜치 push (사용자 별도 컨펌 전 main 머지 X)
  </done>
</task>

</tasks>

<verification>
## 전체 검증

```bash
cd cha-bio-safety

# 1. var() 토큰 0건 (헬퍼 본체 변환)
grep -cE "var\\(--(bg2|bd|bd2|t1|t2|t3|bg3)\\)" src/pages/ElevatorPage.tsx   # 0

# 2. z-index 100 0건
grep -cE 'zIndex:100|z-\\[100\\]' src/pages/ElevatorPage.tsx                  # 0

# 3. z-index 110 새로 추가
grep -c 'zIndex:110' src/pages/ElevatorPage.tsx                               # 3
grep -c 'z-\\[110\\]' src/pages/ElevatorPage.tsx                              # 2

# 4. 이모지 제거
grep -c '✕' src/pages/ElevatorPage.tsx                                        # 0
grep -c '📷' src/pages/ElevatorPage.tsx                                       # 0

# 5. datetime-local fix
grep -c 'min-w-0 max-w-full appearance-none' src/pages/ElevatorPage.tsx       # >= 3

# 6. Camera lucide import
grep -c 'Camera' src/pages/ElevatorPage.tsx                                   # >= 2 (import + 사용)

# 7. 헬퍼 시그니처 보존
grep -c '^function ModalWrap\\|^function Field\\|^function EmptyState\\|^function MultiPhotoUpload' src/pages/ElevatorPage.tsx   # 4

# 8. build PASS
npm run build

# 9. 다른 파일 0건
cd .. && git status   # ElevatorPage.tsx + PLAN.md + SUMMARY.md 만
```
</verification>

<success_criteria>
- [ ] lucide-react import 에 Camera 추가됨
- [ ] z-index 100 → 110: ModalWrap x2 (zIndex:110), EvDetailModal x2 (z-[110]), FaultNewFullscreen x1 (zIndex:110) — 총 5건
- [ ] datetime-local input 3건 모두 min-w-0 max-w-full appearance-none 적용
- [ ] ModalWrap 본체 인라인 → Tailwind 변환 + ✕ → lucide X
- [ ] Field 본체 인라인 label → Tailwind 토큰
- [ ] EmptyState 본체 인라인 → Tailwind 토큰 (icon: string 시그니처 유지)
- [ ] MultiPhotoUpload 본체 인라인 → Tailwind 토큰 + 📷 → lucide Camera + ✕ → lucide X
- [ ] 헬퍼 4종 시그니처 보존 (호출처 변경 0건)
- [ ] var(--bg2/bd/bd2/t1/t2/t3/bg3) 사용처 0건 (헬퍼 외 다른 컴포넌트는 이미 0건 — Wave 1~6 변환됨)
- [ ] zIndex:100 / z-[100] 사용처 0건
- [ ] ✕ / 📷 이모지 0건
- [ ] backdrop zIndex:90 보존 (1735 라인)
- [ ] Wave 1~6 변환 결과 시각 결정 0건 변경 (z-index 110 상승만)
- [ ] TypeScript 0 에러, npm build PASS
- [ ] SUMMARY 작성 + single commit + push
</success_criteria>

<output>
After completion, ensure `.planning/quick/260515-m4h-redesign-07-elevator-tsx-wave-7-helpers-fix/260515-m4h-SUMMARY.md` exists.
</output>
