---
phase: quick-260526-85a
plan: 01
type: execute
wave: 1
depends_on: []
quick_id: 260526-85a
branch: redesign/22-documents (based on origin/main ca7545f, W1 SUMMARY 6ba8f89 직후)
date: 2026-05-26
autonomous: true
files_modified:
  - cha-bio-safety/docs/redesign-context/22-documents/sketch-wave-2-chrome.html
  - cha-bio-safety/docs/redesign-context/22-documents/sketch-wave-3-document-section.html
  - cha-bio-safety/docs/redesign-context/22-documents/sketch-wave-4-upload-form-inline.html
  - cha-bio-safety/docs/redesign-context/22-documents/sketch-wave-5-modal-confirm.html
requirements:
  - redesign-22-documents-w2-chrome
  - redesign-22-documents-w3-document-section
  - redesign-22-documents-w4-upload-form-inline
  - redesign-22-documents-w5-modal-confirm
must_haves:
  truths:
    - "W2~W5 sketch HTML 4개가 22-documents/ 직속 평면 배치로 생성된다 (sketch/ 서브폴더 절대 X — 18-worklog mbr + 19-legal 40p + 20-legal-findings wic + 21-legal-finding-detail 4of + 23-education gmp + 28-splash 2q6 평면 패턴 mirror)"
    - "src 3 파일 (DocumentsPage.tsx 162 + DocumentSection.tsx 517 + DocumentUploadForm.tsx 402 = 1081 lines) + 외부 의존 6 파일 (utils/api documentsApi + utils/multipartUpload runMultipartUpload/formatBytes/formatEta + utils/downloadBlob downloadDocument + hooks/useIsDesktop + stores/authStore + components.css + App.tsx) 모두 코드 변경 0 byte"
    - "OQ #1~#5 LOCKED 5건 default 모두 4 파일 안 verbatim 반영 — #1 chrome 강조색 토큰 통일 (bg-accent / border-accent) / #2 submit button 그라데이션 (linear-gradient 135deg #1d4ed8 #0ea5e9, T3 박제 ≥3 anchor) / #3 최신 pill 12 격상 leading-none / #4 모달 title 22 격상 + Error 카피 14 격상 + 빈 FileText 48 유지 / #5 var(--bg4) → bg-surface-sunken 통일"
    - "비즈 시그니처 anchor 9 그룹 (documentsApi 2종 + queryKey ['documents', type] + ALLOWED 6 ext + empty MIME fallback + multipart upload + abort/retry + beforeunload guard + admin 권한 + delete confirm gate) 모두 4 파일 안 1 byte 변경 0 + grep anchor 박제"
    - "각 task atomic git commit 1개씩 — 총 4 commit (executor cherry-pick 사고 6회 precedent 회피용 commit 명령 verbatim 박제)"
    - "negative gate 4 sketch 모두 통과 (이모지 0 / 9·10·11px 0 / status- prefix 0 / w-8 h-8 0 / 옛 alias 토큰 0 / linear-gradient T4 만 anchor ≥1)"
    - "카피 verbatim — typeLabel '소방계획서/소방훈련자료' + 최신 '최신' + 빈 '아직 업로드된 문서가 없습니다' + 오류 '문서 목록을 불러오지 못했습니다.' + 다운로드/삭제/업로드 toast 12종 + delete confirm full string + beforeunload + abort confirm + 파일 보조 'PDF, XLSX, DOCX, PPTX, HWP, ZIP · 최대 500MB' 모두 1 byte 변경 0"
  artifacts:
    - path: "cha-bio-safety/docs/redesign-context/22-documents/sketch-wave-2-chrome.html"
      provides: "DocumentsPage chrome — 모바일 탭 sticky (plan/drill) + 데스크톱 좌우 2단 (maxWidth 1200 gap 48) + upload shell BottomSheet (slideUp + handle 40x4) + 데스크톱 Modal (fade-in + min(480px,92vw)). 4 frame: 모바일 다크 탭 plan / 모바일 다크 탭 drill / 데스크톱 다크 2단 / 모바일 라이트 upload shell open. OQ #1 토큰 통일 + backdrop NO-OP 코멘트 verbatim + tabBtnStyle 박제"
      contains: "data-theme dark+light / bg-surface-page 외곽 / bg-surface-raised 모달/시트 body / border-b border-border-default 탭 borderBottom container / border-accent active tab borderBottom (OQ #1 LOCKED) / @keyframes docs-slide-up + docs-fade-in 인라인 박제 / handle bar 40x4 bg-border-default rounded-sm mx-auto / backdrop bg-black/55 + onClick NO-OP 코멘트 verbatim `/* backdrop no-op — user must use 취소 button so confirm guard fires */` / 탭 카피 verbatim '소방계획서' '소방훈련자료' / typeLabel 분기 anchor / 데스크톱 maxWidth 1200 gap 48 padding 24 / useIsDesktop ≥1024px 분기 anchor / OQ #1 + OQ #5 citation"
      min_lines: 280
    - path: "cha-bio-safety/docs/redesign-context/22-documents/sketch-wave-3-document-section.html"
      provides: "DocumentSection 카드 + 4 state 매트릭스 (loading 96+56+56 스켈레톤 / error 다시시도 / empty FileText 48 + admin/non-admin 카피 분기 / data Hero card + 과거 이력). 4 frame: 모바일 다크 loading / 모바일 다크 error / 모바일 라이트 empty (admin) / 데스크톱 다크 data (Hero + 과거 3건 + admin Trash2). OQ #2 회색 통일 안 — 카드 색 var(--bg2) bg-surface-raised 유지 + Year tile bg-surface-sunken + 최신 pill bg-accent. OQ #3 최신 pill 12 격상 + leading-none. admin Trash2 32x32 w-[32px] h-[32px] arbitrary (w-8=48 함정 회피)"
      contains: "data-theme dark+light / Hero card minHeight 96 padding 24 rounded-md bg-surface-raised border-border-default / Year tile 64x64 bg-surface-sunken rounded-sm JetBrains Mono 28/600 / 최신 pill bg-accent text-text-on-accent text-caption font-bold leading-none rounded-full px-2 py-1 (OQ #3 12 격상) / Meta verbatim 포맷 `${filename} · ${formatBytes(size)} · ${uploaded_by_name ?? '알 수 없음'} · ${formatDate(uploaded_at)}` / 과거 이력 row minHeight 56 padding 16 + 연결된 borderRadius (isFirst rounded-t / isLast rounded-b) / 과거 row 1번째 라인 `${year}년 · ${title}` + 2번째 라인 `${formatBytes(size)} · ${uploaded_by_name ?? '알 수 없음'} · ${formatDate(uploaded_at, 'date-only')}` / admin Trash2 w-[32px] h-[32px] text-danger border-border-default rounded-sm / FileText size={48} 빈 상태 (OQ #4 LOCKED 유지) / empty 카피 verbatim '아직 업로드된 문서가 없습니다' + admin '우측 상단 업로드 버튼으로 ${title}를 추가하세요.' + non-admin '관리자가 문서를 업로드하면 이곳에 표시됩니다.' / error 카피 verbatim '문서 목록을 불러오지 못했습니다.' + 다시 시도 button bg-accent / loading 스켈레톤 96+56+56 bg-surface-raised / uploadBtn 분기 (모바일 w-[40px] h-[40px] icon-only Plus size={20} / 데스크톱 h-10 pill bg-accent Plus size={16} '업로드') / useQuery queryKey ['documents', type] staleTime 60_000 anchor / documentsApi.list anchor / @keyframes docsec-spin anchor / OQ #2 + OQ #3 + OQ #4 citation"
      min_lines: 280
    - path: "cha-bio-safety/docs/redesign-context/22-documents/sketch-wave-4-upload-form-inline.html"
      provides: "DocumentUploadForm 폼 inline (시트/모달 외곽 없이 폼 콘텐츠만) — year select descending currentYear+1 → 2020 + title input (auto-prefill `${year}년 ${typeLabel(type)}`) + file dashed button + drag-drop 시각 + ALLOWED 6 ext 보조 + Progress bar (height 8 transition 240ms) + Error block. 4 frame: 모바일 다크 초기 / 모바일 다크 파일 선택 후 / 데스크톱 다크 업로드 중 (progress 50%) / 라이트 데스크톱 오류 retry. OQ #3 12 격상 (보조 카피) + OQ #4 모달 title 22 격상 + Error 카피 14 격상 + OQ #5 var(--bg4) → bg-surface-sunken 통일"
      contains: "data-theme dark+light / 모달 title '${label} 업로드' text-heading 22/600 (OQ #4 격상) / 라벨 verbatim '연도' '제목' '파일' text-body-sm 14/600 / inputBaseStyle width 100% h-11 px-3 bg-surface-sunken text-text-primary border-border-default rounded-sm text-body-sm (OQ #5 var(--bg4) → bg-surface-sunken 통일) / year select yearOptions descending currentYear+1 → 2020 verbatim anchor / title input placeholder verbatim '예: ${currentYear}년 ${label}' / file dashed button border-dashed border-border-strong bg-surface-sunken text-text-secondary (file 선택 전) / text-text-primary (file 선택 후) + 'filename · formatBytes(size)' verbatim 표시 / 파일 보조 verbatim 'PDF, XLSX, DOCX, PPTX, HWP, ZIP · 최대 500MB' text-caption leading-none 12 (OQ #3) / Progress bar height-2 bg-surface-sunken rounded-sm overflow-hidden + fill h-full bg-accent transition-[width] duration-[240ms] ease-linear / Progress meta JetBrains Mono % 16/600 + speedBps 14 verbatim 라벨 '속도 계산 중…' / `${MB/s} · 남은 시간 ${formatEta}` / Error block bg-surface-sunken border-border-default rounded-sm text-danger text-body-sm 14 font-medium (OQ #4 14 격상 — 13 폐기) + 다시 시도 button bg-accent / Action row submit (flex 1 h-11 bg-accent rounded-sm '업로드' / '업로드 중…' / disabled bg-surface-sunken text-text-tertiary) + cancel (flex 1 h-11 transparent border-border-default rounded-sm '취소') / ALLOWED 6 ext anchor fence verbatim / MAX_SIZE 500 * 1024 * 1024 anchor / EXT_TO_MIME 매핑 anchor / empty MIME fallback (HWP/ZIP iOS Safari) anchor + 조건 `f.type && !mimes.includes(f.type)` verbatim / runMultipartUpload signature anchor / auto-prefill title `${year}년 ${typeLabel(type)}` anchor / OQ #3 + OQ #4 + OQ #5 citation"
      min_lines: 280
    - path: "cha-bio-safety/docs/redesign-context/22-documents/sketch-wave-5-modal-confirm.html"
      provides: "삭제 confirm gate (window.confirm full string verbatim) + admin 권한 진입 (403 → '관리자만 업로드할 수 있습니다.') + multipart upload abort/retry confirm (handleCancel + handleRetry) + beforeunload guard. 4 frame: 모바일 다크 delete confirm dialog + admin Trash2 anchor / 데스크톱 다크 abort confirm + Error retry / 모바일 라이트 403 에러 / 데스크톱 라이트 beforeunload 시각. OQ #4 (Error 카피 14 격상) + delete confirm 1 byte 변경 금지 (memory project_legal_findings_delete_incident_260520 일반화)"
      contains: "data-theme dark+light / ★ delete confirm full string verbatim `\"${item.title}\"\\n(${item.filename})\\n\\n정말 삭제하시겠습니까? 이 작업은 되돌릴 수 없습니다.` (memory project_legal_findings_delete_incident_260520 1 byte 변경 금지) / abort confirm verbatim `업로드를 취소하시겠습니까? 지금까지 전송된 데이터는 저장되지 않습니다.` (handleCancel line 157~159) / beforeunload returnValue verbatim `업로드 중입니다. 페이지를 나가면 전송이 중단됩니다.` (handleSubmit useEffect line 67~68) / abortRef cleanup on unmount anchor fence (line 75~79 verbatim) / handleRetry anchor (line 168~171 verbatim — setError(null) + handleSubmit 재호출) / 403 에러 카피 verbatim '관리자만 업로드할 수 있습니다.' (line 141) + ApiError other 카피 verbatim '업로드 중 네트워크 오류가 발생했습니다. 다시 시도해주세요.' / AbortError 카피 verbatim '업로드가 취소되었습니다.' (toast 미발송, error block 만 표시) / toast.error verbatim '파일 크기가 500MB를 초과합니다.' + '지원하지 않는 파일 형식입니다. (PDF, XLSX, DOCX, PPTX, HWP, ZIP)' + '연도, 제목, 파일을 모두 입력해주세요.' / toast.success verbatim '업로드가 완료되었습니다.' + '문서를 삭제했습니다' + '다운로드를 시작했습니다' / toast.loading verbatim '다운로드 중입니다…' / toast.error verbatim '다운로드에 실패했습니다. 네트워크를 확인해주세요.' + '문서 목록을 불러오지 못했습니다. 잠시 후 다시 시도해주세요.' + '문서 삭제에 실패했습니다' / admin 권한 분기 anchor `useAuthStore((s) => s.staff?.role === 'admin')` verbatim / Error block 14 격상 (OQ #4) / OQ #4 citation + memory project_legal_findings_delete_incident_260520 + memory feedback_ios_pwa_push_silent_drop (참고)"
      min_lines: 280
  key_links:
    - from: "wave-1-index.md §7 OQ #1 default (chrome 강조색 토큰 통일 bg-accent / border-accent)"
      to: "sketch-wave-2-chrome.html + sketch-wave-3-document-section.html + sketch-wave-4-upload-form-inline.html"
      via: "모바일 탭 active borderBottom + 최신 pill bg + uploadBtn 데스크톱 pill + submit button (W4 OQ #2 그라데이션) + Error 다시 시도 button + DocumentSection error 다시 시도 — 모두 `bg-accent` / `border-accent` 토큰 통일. tokens.css `--accent` = #2f81f7 일치. arbitrary `bg-[#2f81f7]` fallback 패턴 (16-workshift 사례) 검토."
      pattern: "bg-accent|border-accent"
    - from: "wave-1-index.md §7 OQ #2 default (submit button 그라데이션 linear-gradient 135deg #1d4ed8 #0ea5e9, T4 LOCKED)"
      to: "sketch-wave-4-upload-form-inline.html (≥3 anchor — 23-education / 16-workshift / 17-annual-plan OQ 일관 패턴)"
      via: "submit button (DocumentUploadForm line 371) → 인라인 `style={{ background: 'linear-gradient(135deg, #1d4ed8, #0ea5e9)' }}` 또는 sketch 안 `background:linear-gradient(135deg, #1d4ed8, #0ea5e9)`. T2/T3 = linear-gradient 0건 / **T4 만 예외 anchor ≥3** (초기 frame + 파일 선택 후 frame + retry frame). canSubmit false 시 `bg-surface-sunken text-text-tertiary cursor-not-allowed` 분기 보존."
      pattern: "linear-gradient\\(135deg, #1d4ed8, #0ea5e9\\)"
    - from: "wave-1-index.md §7 OQ #3 default (최신 pill 11 → 12 격상 + leading-none, 파일 보조 12 leading-none)"
      to: "sketch-wave-3-document-section.html + sketch-wave-4-upload-form-inline.html"
      via: "최신 pill (현재 11/600) → `text-caption font-bold leading-none` (12px). 파일 보조 'PDF, XLSX, DOCX, PPTX, HWP, ZIP · 최대 500MB' (현재 12) → `text-caption leading-none`. design-system §1.1 노안 마지노선 12 / 9·10·11 사용 금지. memory `feedback_text_caption_leading_none` 일관."
      pattern: "text-caption|leading-none"
    - from: "wave-1-index.md §7 OQ #4 default (모달 title 20 → 22 격상 + Error 카피 13 → 14 격상 + 빈 FileText 48 유지)"
      to: "sketch-wave-3-document-section.html + sketch-wave-4-upload-form-inline.html + sketch-wave-5-modal-confirm.html"
      via: "모달 title '${label} 업로드' 20/600 → text-heading 22/600 (노안 룰). Error 카피 13 → text-body-sm 14 font-medium (13px 사용 금지). 빈 상태 FileText size={48} 유지 (23-education 빈 아이콘 추가와 다름 — 본 페이지 이미 존재). Progress meta 14 유지 (격상 안 함)."
      pattern: "text-heading|text-body-sm|FileText size=\\{48\\}|font-medium"
    - from: "wave-1-index.md §7 OQ #5 default (var(--bg4) → bg-surface-sunken 통일)"
      to: "sketch-wave-4-upload-form-inline.html"
      via: "inputBaseStyle bg var(--bg4) + Progress bar 배경 var(--bg4) → 모두 `bg-surface-sunken` 통일. var(--bg3) (Year tile + dashed file button) + var(--bg4) 모두 동일 sunken 매핑. design-system §2.1 5단계 (page/raised/sunken/active/overlay) 안에 사용. 새 토큰 신설 안 함."
      pattern: "bg-surface-sunken"
    - from: "DocumentSection.tsx handleDelete confirm 카피 (line 87~89 verbatim)"
      to: "sketch-wave-5-modal-confirm.html"
      via: "★ delete confirm full string `\"${item.title}\"\\n(${item.filename})\\n\\n정말 삭제하시겠습니까? 이 작업은 되돌릴 수 없습니다.` 1 byte 변경 금지. memory `project_legal_findings_delete_incident_260520` (2026-05-20 22행 삭제 사고 → 가드 강화) 일반화. T4 frame 1 (모바일 다크 delete confirm dialog) 안 verbatim 박제."
      pattern: "정말 삭제하시겠습니까|되돌릴 수 없습니다"
    - from: "DocumentUploadForm.tsx beforeunload + abort confirm (line 67~68 + 157~159 verbatim)"
      to: "sketch-wave-5-modal-confirm.html"
      via: "beforeunload returnValue '업로드 중입니다. 페이지를 나가면 전송이 중단됩니다.' + abort confirm '업로드를 취소하시겠습니까? 지금까지 전송된 데이터는 저장되지 않습니다.' 모두 1 byte 변경 금지. T4 frame 2 (데스크톱 abort confirm) + frame 4 (라이트 beforeunload 시각) 안 verbatim 박제."
      pattern: "업로드 중입니다|업로드를 취소하시겠습니까"
  scope_negatives:
    - "sketch HTML 4개 만 산출. TSX 변환은 W6 별도 wave (본 PLAN 범위 외)"
    - "src 3 파일 (DocumentsPage.tsx + DocumentSection.tsx + DocumentUploadForm.tsx) + utils/api + utils/multipartUpload + utils/downloadBlob + hooks/useIsDesktop + stores/authStore + components.css + App.tsx 모두 0 byte 변경"
    - "wrangler 명령 절대 X / npm run deploy 절대 X (CLAUDE.local.md + memory `feedback_cbc7119_design_never_wrangler`)"
    - "옛 alias 토큰 (var(--bg/bg2/bg3/bg4/bd/bd2/t1/t2/t3/acl/accent/safe/warn/danger)) sketch 본문 안 0 (tokens.css 정의 인용 fence 안 예외)"
    - "status- prefix 0 (bg-status-safe / text-status-danger 등 사용 시 verify FAIL, memory feedback_tailwind_token_class_pattern)"
    - "이모지 0 (sketch 본문) — Lucide 아이콘 anchor 만 텍스트 인용 (FileText / Plus / Loader2 / Trash2 / Upload / X)"
    - "w-8 / h-8 className 0 (48px 함정, memory feedback_tailwind_w8_h8_is_48px). 32x32 = w-[32px] h-[32px] arbitrary 또는 w-7 h-7. 40x40 = w-[40px] h-[40px]. 44 = w-11 h-11"
    - "9·10·11 fontSize 인라인 0 (text-caption 12 leading-none 마지노선, OQ #3 LOCKED)"
    - "linear-gradient — T1/T2/T3 = 0건 / **T4 만 예외 anchor ≥3** (submit button OQ #2 LOCKED). T5 = code fence 인용 ≥1 OK"
    - "본 워크트리 = cbc7119-design (cbc7119-preview.pages.dev 만). 직원 도메인 cbc7119.pages.dev 절대 손대지 않음 (20260328 워크트리 책임)"
    - "sketch/ 서브폴더 절대 금지 — 4 파일 모두 22-documents/ 직속 평면 배치 (18/19/20/21/23/28 6 페이지 일관)"
---

<objective>
redesign/22-documents 의 W1 인덱스(`wave-1-index.md`, 564 lines, OQ 5건 default LOCKED 2026-05-26 7qg)를 기반으로
W2~W5 4 sub-wave sketch 산출물을 **단일 quick task** 안에서 **atomic 4 commit** 으로 한 번에 만든다.

- **W2 (T1):** DocumentsPage chrome — 모바일 탭 sticky (plan/drill) + 데스크톱 좌우 2단 (maxWidth 1200 gap 48) + upload shell BottomSheet (모바일 slideUp + handle 40x4) + 데스크톱 Modal (fade-in + min(480px,92vw)) + backdrop NO-OP 코멘트 verbatim — 4 frame
- **W3 (T2):** DocumentSection 카드 — 헤더 행 (h2 + uploadBtn 분기) + 4 state 매트릭스 (loading 스켈레톤 96+56+56 / error 다시시도 / empty FileText 48 + admin/non-admin 카피 분기 / data Hero card 최신 + 과거 이력 list) + admin Trash2 32x32 — 4 frame (다양한 state)
- **W4 (T3):** DocumentUploadForm 폼 inline (시트/모달 외곽 없이 폼 콘텐츠만) — year select + title input + file dashed button (drag-drop 시각 포함) + ALLOWED 6 ext 보조 + Progress bar (height 8 transition 240ms) + Error block + Action row + **OQ #2 LOCKED submit button 그라데이션 ≥3 박제** — 4 frame (초기 / 파일 선택 후 / 업로드 중 50% / 오류 retry)
- **W5 (T4):** 삭제 confirm gate (window.confirm full string verbatim) + admin 권한 진입 (403 에러) + multipart abort/retry confirm (handleCancel + handleRetry) + beforeunload guard — 4 frame (모바일 delete confirm / 데스크톱 abort confirm + Error retry / 모바일 403 / 라이트 beforeunload)

**Purpose:** 18-worklog mbr / 19-legal 40p / 20-legal-findings wic / 21-legal-finding-detail 4of / 23-education gmp / 28-splash 2q6 등 다수 mirror 와 동일한 평면(flat sibling) 패턴으로 22-documents sketch 단계 4 wave 분량을 한 turn 에 처리. DocumentsPage 가 3 파일 통합 (1081 lines) + useIsDesktop ≥1024px 분기 + 모바일 탭 + 데스크톱 좌우 2단 + BottomSheet/Modal 분기 + admin 권한 도구 + multipart upload + abort/retry + beforeunload guard + delete confirm gate — 본 PLAN 에서 sketch 4 wave 한 번에 처리해 cbc7119-preview 자동 배포 사이클 1회로 사용자 시각 검수 가능하게 함.

**Output:** `cha-bio-safety/docs/redesign-context/22-documents/` 직속 4 파일 (sketch HTML 4개). `DocumentsPage.tsx` (162) + `DocumentSection.tsx` (517) + `DocumentUploadForm.tsx` (402) 합계 1081 lines + 외부 의존 6 파일 모두 코드 변경 0 byte.

**mbr/wic mirror:** 본 PLAN 은 18-worklog mbr PLAN + 20-legal-findings wic PLAN 의 정확한 mirror. mirror 와 차이 2건:
- (1) 3 파일 통합 (DocumentsPage + DocumentSection + DocumentUploadForm) — 18-worklog 단일 1216 / 19-legal 단일 통합 와 다름. multi-file 외부 의존 패턴은 19-legal mirror 일관
- (2) multipart upload (R2 5MB 단위) + abort/retry + beforeunload guard — 18-worklog 의 단순 read-only 패턴과 다름. W5 confirm gate sketch 단독 wave 가 본 페이지 핵심

**중요 — executor cherry-pick 사고 회피 (memory project_redesign_28_splash 6회 사례):** executor worktree commit 후 명시적 보고 필수 (commit hash + message 1줄) + orchestrator 가 필요 시 자동 cherry-pick 으로 복구. 본 PLAN 각 task 의 commit 명령은 명시 박제.

**mbr deviation 2건 mirror (negative gate self-collision 회피):** "warning glyph" / "lin-grad" 약어 패턴 사용 — 본문 안 `linear-gradient` 단어 자체는 T4 anchor 박제에만 등장. T1/T2/T3 sketch 본문은 약어 또는 fence escape 사용. (mbr verify pattern 일관)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jykevin/Documents/cbc7119-design/CLAUDE.md
@/Users/jykevin/Documents/cbc7119-design/CLAUDE.local.md

# W1 인덱스 (단일 진입점, OQ 5건 default LOCKED 2026-05-26 7qg, 564 lines)
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/22-documents/wave-1-index.md

# Source TSX (sketch 대상, 변경 0 — 3 파일 모두 untouched, 합계 1081 lines)
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/22-documents/DocumentsPage.tsx
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/22-documents/DocumentSection.tsx
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/22-documents/DocumentUploadForm.tsx

# 22-documents redesign 토큰/타이포 (design-system v0.1.1)
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/22-documents/design-system.md
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/22-documents/tokens.css
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/22-documents/typography.css
</context>

<rules_verbatim>
### OQ LOCKED 5건 default (wave-1-index.md §7, 2026-05-26)

**OQ #1 (chrome 강조색 토큰화):** `bg-accent` / `border-accent` 토큰 통일. 16-workshift / 17-annual-plan / 23-education / 28-splash 4 페이지 일관. tokens.css `--accent` = #2f81f7. arbitrary `bg-[#2f81f7]` fallback 패턴 (16-workshift 사례) 검토.

**OQ #2 (submit button 그라데이션):** **lin-grad 채택**. design-system §6.4 CTA 룰 + 14-reports / 16-workshift / 17-annual-plan / 23-education W1 OQ #3 일관. submit button = 인라인 `linear-gradient(135deg, #1d4ed8, #0ea5e9)` (T4 만 anchor ≥3). canSubmit false = `bg-surface-sunken text-text-tertiary cursor-not-allowed`.

**OQ #3 (최신 pill 11 → 12 격상):** 12 격상 + `text-caption leading-none`. design-system §1.1 노안 마지노선 12 / 9·10·11 사용 금지. memory `feedback_text_caption_leading_none` 일관. 동시에 모바일 폼 12 보조 'PDF, XLSX, DOCX, PPTX, HWP, ZIP · 최대 500MB' 도 `leading-none` 명시.

**OQ #4 (빈/오류/Progress 시각 일관성):** 현재 유지 (최소 격상). 빈 FileText size={48} 유지 (오류 아이콘 추가 안 함). Progress meta 14 → text-body-sm 유지. 모달 title 20 → text-heading 22 격상 (노안 룰). Error 카피 13 → text-body-sm 14 격상 (13px 사용 금지).

**OQ #5 (`var(--bg4)` 토큰 매핑):** `bg-surface-sunken` 통일. var(--bg3) (Year tile + dashed file button) + var(--bg4) (input bg + progress bar 배경) 모두 sunken 매핑. 새 토큰 신설 안 함.

### 디자인 토큰 패턴 (wave-1-index.md §3 + §4)
- `bg-surface-page` (var(--bg) → --surface-page, 페이지 외곽)
- `bg-surface-raised` (var(--bg2) → --surface-raised, 모달/시트 body + Hero card + 과거 row + loading 스켈레톤 + 빈 상태 wrapper)
- `bg-surface-sunken` (var(--bg3)+var(--bg4) → --surface-sunken, Year tile + dashed file button + input bg + progress bar 배경 + Error block) — OQ #5
- `border-border-default` (var(--bd) → --border-default, 카드 border + 탭 borderBottom + 모바일 헤더)
- `border-border-strong` (var(--bd2) → --border-strong, dashed file button border, 단 dashed 보존)
- `text-text-primary` (var(--t1) → --text-primary, h2 / Hero title / 과거 row title / 모달 title)
- `text-text-secondary` (var(--t2) → --text-secondary, Meta / 빈 보조 / 오류 카피 / Progress meta)
- `text-text-tertiary` (var(--t3) → --text-tertiary, 빈 FileText 색 + canSubmit false 글자)
- `text-danger` (var(--danger) → --danger, Trash2 색 + Error 카피 — status- prefix 없음)
- `bg-accent` / `border-accent` / `text-text-on-accent` (--accent solid, OQ #1 통일)
- 인라인 `linear-gradient(135deg, #1d4ed8, #0ea5e9)` (T4 submit button OQ #2 LOCKED 만 anchor ≥3)
- radius: `rounded-sm` 8 / `rounded-md` 12 / `rounded-full` 999 / `rounded-t-2xl` 16 (BottomSheet body) / `rounded-xl` 12 (모달 body)
- Lucide size prop: 16 (Trash2 + Plus 데스크톱 + Loader2) / 20 (Plus 모바일) / 48 (FileText 빈) — w-N/h-N className 금지 (memory feedback_tailwind_token_class_pattern)

### Negative rule (모든 task verify gate, mbr deviation 2건 mirror)
- 이모지 0건 (sketch 본문) — Lucide 텍스트 anchor 만 인용
- `linear-gradient` — T1/T2/T3 = 0건 / **T4 만 예외 anchor ≥3** (submit button OQ #2 LOCKED). T5 = code fence 인용 ≥1 OK. **본문 안 "lin-grad" 약어 패턴 사용 (mbr deviation mirror, negative gate self-collision 회피)**
- 9·10·11px fontSize 0건 (text-caption 12 leading-none 마지노선, OQ #3 LOCKED). **본문 안 "warning glyph" 약어 패턴 (mbr deviation mirror)**
- `(text|bg|border)-status-` class prefix 0건 (memory feedback_tailwind_token_class_pattern)
- `\b(w|h)-8\b` 0건 (tailwind config w-8=48 함정, memory feedback_tailwind_w8_h8_is_48px). 32x32 = w-[32px] h-[32px] / 40x40 = w-[40px] h-[40px] / 44 = w-11 h-11
- 옛 alias 토큰 (var(--bg/bg2/bg3/bg4/bd/bd2/t1/t2/t3/acl/accent/safe/warn/danger)) 본문 안 0 (tokens.css 정의 인용 fence 안 예외)

### 평면 폴더 패턴 (18/19/20/21/23/28 6 페이지 일관)
- 22-documents/ 직속에 4 파일 배치 (sketch-wave-{2,3,4,5}-{slug}.html)
- `sketch/` 서브폴더 절대 금지

### ★ 비즈 anchor 9 그룹 (1 byte 변경 금지, W2~W5 모두 박제)

**[react-query / 시그니처]**
1. `useQuery({ queryKey: ['documents', type], queryFn: () => documentsApi.list(type), staleTime: 60_000 })` (DocumentSection line 44~48)
2. `queryClient.invalidateQueries({ queryKey: ['documents', type] })` (모든 mutation 후, 키 정확히 일치)
3. `documentsApi` 2종 — `list(type: 'plan' | 'drill')` + `remove(id: number)` (utils/api 시그니처 변경 금지). `downloadDocument(id, filename)` (utils/downloadBlob) + `formatBytes(size)` + `formatEta(seconds)` (utils/multipartUpload) 모두 시그니처 보존

**[multipart upload + abort + beforeunload]**
4. `runMultipartUpload({ file, type, year, title, contentType, signal, onProgress })` (utils/multipartUpload 시그니처 변경 금지). `AbortController` ref + `signal: ctrl.signal` 패턴 보존
5. `useEffect(beforeunload guard)` (DocumentUploadForm line 64~72) — isUploading 동안만 등록 + `e.returnValue = '업로드 중입니다. 페이지를 나가면 전송이 중단됩니다.'` verbatim
6. `useEffect(unmount abort cleanup)` (DocumentUploadForm line 75~79) — `abortRef.current?.abort()` on unmount

**[file 검증 + ALLOWED + auto-prefill]**
7. `ALLOWED` 6 ext (PDF/XLSX/DOCX/PPTX/HWP/ZIP) + `EXT_TO_MIME` 매핑 + `MAX_SIZE = 500 * 1024 * 1024` (DocumentUploadForm line 23~41 verbatim). empty MIME fallback (HWP/ZIP iOS Safari) — 조건 `f.type && !mimes.includes(f.type)` verbatim 보존. auto-prefill title `${year}년 ${typeLabel(type)}` (D-19, line 102). yearOptions descending `currentYear+1 → 2020` (line 174~175)

**[admin 권한 + delete confirm gate]**
8. `isAdmin = useAuthStore((s) => s.staff?.role === 'admin')` (DocumentSection line 40) — admin 만 uploadBtn + Trash2 표시. assistant 는 다운로드/조회만. ApiError 403 → '관리자만 업로드할 수 있습니다.' (line 141 verbatim)
9. ★ delete confirm full string verbatim `"${item.title}"\n(${item.filename})\n\n정말 삭제하시겠습니까? 이 작업은 되돌릴 수 없습니다.` (handleDelete line 87~89) — memory `project_legal_findings_delete_incident_260520` (2026-05-20 22행 삭제 사고 + Time Travel 복구) 일반화 — **1 byte 변경 금지** + Action row destructive button 위치 보존

### 카피 verbatim (memory feedback_sketch_realistic_data)

**typeLabel + 헤더:** 'plan' → '소방계획서' / 'drill' → '소방훈련자료' (line 37, DocumentSection + line 44, DocumentUploadForm)

**DocumentSection toast (5종):**
- '다운로드 중입니다…' (loading)
- '다운로드를 시작했습니다' (success, duration 2000)
- '다운로드에 실패했습니다. 네트워크를 확인해주세요.' (error)
- '문서 목록을 불러오지 못했습니다. 잠시 후 다시 시도해주세요.' (error toast)
- '문서를 삭제했습니다' (success) / '문서 삭제에 실패했습니다' (error fallback)

**DocumentSection 본문 카피:**
- 빈 '아직 업로드된 문서가 없습니다' (line 252) + admin 분기 '우측 상단 업로드 버튼으로 ${title}를 추가하세요.' + non-admin 분기 '관리자가 문서를 업로드하면 이곳에 표시됩니다.'
- 오류 '문서 목록을 불러오지 못했습니다.' (line 213) + '다시 시도' button
- 최신 pill '최신' (line 300)
- 과거 이력 라벨 '과거 이력' (line 408)
- Hero meta `${filename} · ${formatBytes(size)} · ${uploaded_by_name ?? '알 수 없음'} · ${formatDate(uploaded_at)}` (line 352~353)
- 과거 row 1번째 `${year}년 · ${title}` (line 456) + 2번째 `${formatBytes(size)} · ${uploaded_by_name ?? '알 수 없음'} · ${formatDate(uploaded_at, 'date-only')}` (line 468~469)

**DocumentUploadForm toast (7종):**
- '파일 크기가 500MB를 초과합니다.' (error)
- '지원하지 않는 파일 형식입니다. (PDF, XLSX, DOCX, PPTX, HWP, ZIP)' (error, 2회 — MIME 검증 + ext 검증)
- '연도, 제목, 파일을 모두 입력해주세요.' (error)
- '업로드가 완료되었습니다.' (success)
- '업로드가 취소되었습니다.' (AbortError, toast 미발송 — error block 만)
- '관리자만 업로드할 수 있습니다.' (ApiError 403)
- '업로드 중 네트워크 오류가 발생했습니다. 다시 시도해주세요.' (ApiError other / 기타)

**DocumentUploadForm 본문 카피:**
- 모달 title '${label} 업로드' (line 204)
- 라벨 '연도' / '제목' / '파일' (line 208 / 225 / 238)
- title input placeholder '예: ${currentYear}년 ${label}' (line 230)
- file 선택 button '파일 선택' (file 없을 때) / 'filename · formatBytes(size)' (file 있을 때) (line 267)
- 파일 보조 'PDF, XLSX, DOCX, PPTX, HWP, ZIP · 최대 500MB' (line 270)
- Progress 라벨 '속도 계산 중…' (speedBps < 100KB) / `${MB/s} · 남은 시간 ${formatEta}` (line 312~318 verbatim)
- submit '업로드' (canSubmit) / '업로드 중…' (isUploading) (line 380)
- cancel '취소' (line 397)
- beforeunload returnValue '업로드 중입니다. 페이지를 나가면 전송이 중단됩니다.' (line 67~68)
- handleCancel confirm '업로드를 취소하시겠습니까? 지금까지 전송된 데이터는 저장되지 않습니다.' (line 157~159)

### @keyframes 3종 (DocumentsPage 인라인 + DocumentSection 인라인, 변경 금지)
- `docs-slide-up` (translateY 100% → 0) — BottomSheet 240ms ease-out
- `docs-fade-in` (opacity 0 → 1) — Modal 180ms ease-out
- `docsec-spin` (rotate 360deg) — Loader2 1s linear infinite

### Tailwind cheatsheet (v0.1.1, 22-documents 적용)
| v0.1.1 토큰 | Tailwind utility | 22-documents 적용 위치 |
|---|---|---|
| `--surface-page` | `bg-surface-page` | 페이지 외곽 + 모바일 고정 하단 wrapper (없음, 본 페이지) |
| `--surface-raised` | `bg-surface-raised` | Hero card + 과거 row + loading 스켈레톤 + 빈 상태 wrapper + 모달/시트 body |
| `--surface-sunken` | `bg-surface-sunken` (OQ #5) | Year tile + dashed file button + input bg + progress bar 배경 + Error block + canSubmit false bg |
| `--border-default` | `border-border-default` | Hero / 과거 / Trash2 / Action row cancel / 모달 카드 border + 탭 borderBottom container |
| `--border-strong` | `border-border-strong` (dashed 보존) | file 선택 button dashed border 만 |
| `--text-primary` | `text-text-primary` | h2 / Hero title / 과거 title / 모달 title / file 선택 후 글자 |
| `--text-secondary` | `text-text-secondary` | Meta / 빈 보조 / 오류 카피 / Progress meta / file 선택 전 글자 |
| `--text-tertiary` | `text-text-tertiary` | 빈 FileText 색 + canSubmit false 글자 |
| `--danger` | `text-danger` | Trash2 색 + Error 카피 (status- prefix 없음) |
| `--accent` solid | `bg-accent text-text-on-accent` (OQ #1) | 최신 pill + uploadBtn 데스크톱 + 다시 시도 button + Progress fill |
| `--accent` (CTA) | 인라인 `linear-gradient(135deg, #1d4ed8, #0ea5e9)` (OQ #2) | submit button (W4 LOCKED ≥3) |
| (radius 8) | `rounded-sm` | input + Year tile + Trash2 + Error block + Action row button |
| (radius 12) | `rounded-md` | Hero / 과거 / 모달 body |
| (radius 16) | `rounded-t-2xl` | BottomSheet body |
| (radius 999) | `rounded-full` | 최신 pill |

### 메모리 룰 inline (T1~T4 sketch rules 박스 박제, ≥12 unique)
- `feedback_tailwind_token_class_pattern` — status- prefix 없음 + Lucide size prop
- `feedback_tailwind_w8_h8_is_48px` — w-8/h-8 = 48px 함정
- `feedback_text_caption_leading_none` — 작은 컨테이너 text-caption(12) leading-none
- `feedback_sketch_realistic_data` — 표시 분기/라벨 룰 코드 그대로
- `feedback_design_changes_ask_first` — 디자인 변경 전 사용자 컨펌
- `feedback_design_sketch_first` — spacing/sizing 도 sketch HTML 시안 후 인라인
- `feedback_avoid_premature_confirmation` — 시각 작업 자신감 표현 금지
- `feedback_planner_prompt_sketch_verbatim` — sketch CSS verbatim 인용 (추측 토큰명 금지)
- `feedback_tsx_wave_emoji_dot_gap` — sketch 의 이모지 0건 negative gate
- `feedback_redesign_sketch_rule_enforcement` — design-system 룰 negative gate 강제
- `feedback_check_branch_before_edit` — main 아니거나 dirty 면 사용자 컨펌
- `feedback_cbc7119_design_never_wrangler` — 디자인 wave 중 wrangler 절대 X
- `project_redesign_19_legal` + `project_redesign_21_legal_finding_detail` — multi-file 외부 의존 미수정 패턴
- `project_legal_findings_delete_incident_260520` — delete confirm 가드 강화 룰 (T4 핵심)
</rules_verbatim>

<tasks>

<task type="auto">
  <name>Task 1 (W2): DocumentsPage chrome sketch — 모바일 탭 (plan/drill) + 데스크톱 좌우 2단 + upload shell BottomSheet + Modal (backdrop NO-OP 코멘트 verbatim, 4 frame)</name>
  <files>cha-bio-safety/docs/redesign-context/22-documents/sketch-wave-2-chrome.html</files>
  <action>
    20-legal-findings/sketch-wave-2-chrome.html + 23-education/sketch-wave-2-*.html 패턴 mirror 로 새 sketch 파일 작성. DocumentsPage.tsx 전체 (162 lines, line 17~162) 시각화 — 모바일 isDesktop=false 분기 (line 41~64 탭 sticky + DocumentSection 1개) + 데스크톱 isDesktop=true 분기 (line 66~83 좌우 2단 maxWidth 1200 gap 48 padding 24) + upload shell 모바일 BottomSheet (line 86~126 slideUp 240ms + handle 40x4 + backdrop NO-OP) + 데스크톱 Modal (line 128~159 fade-in 180ms + min(480px,92vw) + backdrop NO-OP). **OQ #1 LOCKED chrome 강조색 토큰 통일 (bg-accent / border-accent)** + **backdrop NO-OP 코멘트 verbatim** + **@keyframes docs-slide-up + docs-fade-in 인라인 박제** 3건 본 wave 핵심.

    **구조 (mbr W2 + wic W2 mirror, 4 frame):**
    - `<!DOCTYPE html>` + `<html lang="ko">` + `<head><meta charset="utf-8"><title>redesign/22-documents — W2 chrome + 탭 + 2단 + shell</title>` + `<style>`
    - `<style>` 안: v0.1.1 tokens.css dark+light verbatim + typography.css verbatim + `@keyframes docs-slide-up { from { transform: translateY(100%); } to { transform: translateY(0); } }` + `@keyframes docs-fade-in { from { opacity: 0; } to { opacity: 1; } }` (DocumentsPage line 36~39 verbatim)
    - `<body>` 안 4 frame side-by-side:

      | frame | viewport | 시각화 |
      |---|---|---|
      | 1 | 모바일 다크 393×800 탭 plan active | `data-theme="dark"` + bg-surface-page 외곽 + padding '16px 16px 0 16px' + 탭 sticky (top 0 zIndex 10 flex bg-surface-page border-b border-border-default mb-4) + plan active (text-text-primary font-semibold border-b-2 **border-accent** OQ #1) + drill inactive (text-text-secondary font-normal border-b-2 border-transparent) + 탭 카피 verbatim '소방계획서' '소방훈련자료' + DocumentSection placeholder 영역 |
      | 2 | 모바일 다크 393×800 탭 drill active | `data-theme="dark"` + 동일 chrome + plan inactive + drill active (border-b-2 **border-accent** OQ #1) + DocumentSection placeholder |
      | 3 | 데스크톱 다크 1440×900 좌우 2단 | `data-theme="dark"` + bg-surface-page 외곽 + flex gap-12 maxWidth 1200 mx-auto p-6 + 좌측 DocumentSection (type="plan" minWidth 0 flex-1) + 우측 DocumentSection (type="drill" minWidth 0 flex-1) — 탭 없음 (데스크톱 동시 표시) |
      | 4 | 모바일 라이트 393×800 upload shell open (BottomSheet) | `data-theme="light"` + 모바일 chrome + BottomSheet wrapper (fixed inset-0 bg-black/55 z-[1000] flex items-end justify-center) + **backdrop onClick NO-OP 코멘트 verbatim** `<!-- /* backdrop no-op — user must use 취소 button so confirm guard fires */ -->` + body (w-full max-h-[85vh] overflow-y-auto bg-surface-raised rounded-t-2xl p-6 animate-[docs-slide-up_240ms_ease-out]) + handle bar (w-10 h-1 bg-border-default rounded-sm mx-auto mb-4, 40x4) + DocumentUploadForm placeholder (W4 에서 채움) |

    **모바일 탭 마크업 룰 (frame 1/2 — line 41~64 verbatim, OQ #1 LOCKED):**
    ```html
    <!-- 모바일 탭 sticky (line 43~60 verbatim, OQ #1 border-accent) -->
    <div style="position:sticky;top:0;z-index:10;display:flex;background:var(--bg);border-bottom:1px solid var(--bd);margin-bottom:16px;">
      <!-- active (plan) — text-text-primary font-semibold border-b-2 border-accent -->
      <button type="button" class="text-text-primary font-semibold" style="flex:1;height:44px;background:transparent;font-size:16px;border:none;border-bottom:2px solid var(--acl);cursor:pointer;">소방계획서</button>
      <!-- inactive (drill) — text-text-secondary font-normal border-b-2 border-transparent -->
      <button type="button" class="text-text-secondary font-normal" style="flex:1;height:44px;background:transparent;font-size:16px;border:none;border-bottom:2px solid transparent;cursor:pointer;">소방훈련자료</button>
    </div>
    ```

    **데스크톱 좌우 2단 마크업 룰 (frame 3 — line 66~83 verbatim):**
    ```html
    <!-- 데스크톱 2단 (line 67~75 verbatim) -->
    <div style="display:flex;gap:48px;max-width:1200px;margin:0 auto;padding:24px;">
      <div style="flex:1;min-width:0;">
        <!-- DocumentSection type="plan" — W3 placeholder -->
      </div>
      <div style="flex:1;min-width:0;">
        <!-- DocumentSection type="drill" — W3 placeholder -->
      </div>
    </div>
    ```

    **upload shell 모바일 BottomSheet 마크업 룰 (frame 4 — line 86~126 verbatim, ★ backdrop NO-OP):**
    ```html
    <!-- ★ backdrop onClick NO-OP — 사용자가 반드시 '취소' 버튼 사용해야 beforeunload guard 동작 (line 97~99 코멘트 verbatim) -->
    <div style="position:fixed;inset:0;background:rgba(0,0,0,0.55);z-index:1000;display:flex;align-items:flex-end;justify-content:center;" data-backdrop-no-op="true">
      <!-- /* backdrop no-op — user must use 취소 button so confirm guard fires */ -->
      <div style="width:100%;max-height:85vh;overflow-y:auto;background:var(--bg2);border-top-left-radius:16px;border-top-right-radius:16px;padding:24px;animation:docs-slide-up 240ms ease-out;" onclick="event.stopPropagation()">
        <!-- handle bar 40x4 (line 114~122 verbatim) -->
        <div style="width:40px;height:4px;background:var(--bd2);border-radius:2px;margin:0 auto 16px auto;"></div>
        <!-- DocumentUploadForm placeholder (W4 에서 채움) -->
      </div>
    </div>
    ```

    **데스크톱 Modal 마크업 룰 (frame 4 또는 5번째 anchor — line 128~159 verbatim):**
    ```html
    <!-- 데스크톱 Modal (line 128~158 verbatim, fade-in 180ms + min(480px,92vw)) -->
    <div style="position:fixed;inset:0;background:rgba(0,0,0,0.55);z-index:1000;display:flex;align-items:center;justify-content:center;animation:docs-fade-in 180ms ease-out;" data-backdrop-no-op="true">
      <!-- /* backdrop no-op — user must use 취소 button so confirm guard fires */ -->
      <div style="width:min(480px, 92vw);max-height:85vh;overflow-y:auto;background:var(--bg2);border:1px solid var(--bd2);border-radius:12px;padding:24px;" onclick="event.stopPropagation()">
        <!-- DocumentUploadForm placeholder -->
      </div>
    </div>
    ```

    **카피 verbatim:**
    - 탭 plan: '소방계획서' (line 55) / 탭 drill: '소방훈련자료' (line 58)
    - typeLabel 분기 anchor (line 37 verbatim): `'plan' → '소방계획서' / 'drill' → '소방훈련자료'`

    **rules 박스 (sketch 하단):**
    - LOCKED 결정: OQ #1 (chrome 강조색 토큰 통일 `bg-accent` / `border-accent`) + OQ #5 (var(--bg4) → bg-surface-sunken — DocumentSection 카드에서 적용, W3 핵심) verbatim 인용 1 fence
    - **비즈 anchor 9 그룹 박제 fence** (rules_verbatim 박제 list 그대로 인용 — 본 wave 는 useIsDesktop ≥1024px 분기 + @keyframes 3종 + backdrop NO-OP 중심)
    - **★ backdrop NO-OP 코멘트 verbatim 박제** (line 97~99 + 140~142 — 사용자 '취소' 버튼 강제)
    - **@keyframes 3종 anchor fence** (docs-slide-up + docs-fade-in + docsec-spin)
    - 토큰 매핑: `var(--bg) → bg-surface-page` / `var(--bg2) → bg-surface-raised` (모달/시트 body) / `var(--bd) → border-border-default` (탭 borderBottom) / `var(--bd2) → border-border-default` (handle bar bg) / `var(--acl) → bg-accent` `border-accent` (OQ #1)
    - **mbr deviation mirror** — 본문 안 "lin-grad" / "warning glyph" 약어 없음 (T4 anchor 박제 시 자체매칭 회피)
    - negative gate self-check: HTML 주석 `<!--` prefix 또는 entity escape (`&#9;`/`&#10;`) 로 "9·10·11" 숫자 직접 인용 회피

    **commit 명령:**
    ```
    git add cha-bio-safety/docs/redesign-context/22-documents/sketch-wave-2-chrome.html
    git commit -m "feat(quick-260526-85a): redesign/22-documents sketch wave 2 (chrome — 모바일 탭 plan/drill + 데스크톱 좌우 2단 maxWidth 1200 gap 48 + upload shell BottomSheet/Modal + backdrop NO-OP 코멘트 verbatim + bg-accent border-accent OQ #1 통일)"
    ```
  </action>
  <verify>
    <automated>
test -f cha-bio-safety/docs/redesign-context/22-documents/sketch-wave-2-chrome.html \
  && wc -l cha-bio-safety/docs/redesign-context/22-documents/sketch-wave-2-chrome.html | awk '{ if ($1 < 200) { print "FAIL: too short"; exit 1 } else print "PASS lines:", $1 }' \
  && grep -c 'data-theme="dark"' cha-bio-safety/docs/redesign-context/22-documents/sketch-wave-2-chrome.html | awk '{ if ($1 < 3) { print "FAIL: dark frame <3"; exit 1 } else print "PASS dark frame:", $1 }' \
  && grep -c 'data-theme="light"' cha-bio-safety/docs/redesign-context/22-documents/sketch-wave-2-chrome.html | awk '{ if ($1 < 1) { print "FAIL: light frame <1"; exit 1 } else print "PASS light frame:", $1 }' \
  && grep -c 'bg-surface-page' cha-bio-safety/docs/redesign-context/22-documents/sketch-wave-2-chrome.html | awk '{ if ($1 < 3) { print "FAIL: bg-surface-page <3"; exit 1 } else print "PASS bg-surface-page:", $1 }' \
  && grep -cE 'bg-accent|border-accent' cha-bio-safety/docs/redesign-context/22-documents/sketch-wave-2-chrome.html | awk '{ if ($1 < 3) { print "FAIL: bg-accent/border-accent <3 (OQ #1)"; exit 1 } else print "PASS bg-accent/border-accent:", $1 }' \
  && grep -c '소방계획서' cha-bio-safety/docs/redesign-context/22-documents/sketch-wave-2-chrome.html | awk '{ if ($1 < 2) { print "FAIL: 탭 소방계획서 <2"; exit 1 } else print "PASS 소방계획서:", $1 }' \
  && grep -c '소방훈련자료' cha-bio-safety/docs/redesign-context/22-documents/sketch-wave-2-chrome.html | awk '{ if ($1 < 2) { print "FAIL: 탭 소방훈련자료 <2"; exit 1 } else print "PASS 소방훈련자료:", $1 }' \
  && grep -c 'max-width:1200' cha-bio-safety/docs/redesign-context/22-documents/sketch-wave-2-chrome.html | awk '{ if ($1 < 1) { print "FAIL: 데스크톱 maxWidth 1200 missing"; exit 1 } else print "PASS maxWidth 1200:", $1 }' \
  && grep -cE 'gap:48px|gap-12' cha-bio-safety/docs/redesign-context/22-documents/sketch-wave-2-chrome.html | awk '{ if ($1 < 1) { print "FAIL: 데스크톱 gap 48 missing"; exit 1 } else print "PASS gap 48:", $1 }' \
  && grep -c 'docs-slide-up' cha-bio-safety/docs/redesign-context/22-documents/sketch-wave-2-chrome.html | awk '{ if ($1 < 2) { print "FAIL: @keyframes docs-slide-up anchor <2"; exit 1 } else print "PASS docs-slide-up:", $1 }' \
  && grep -c 'docs-fade-in' cha-bio-safety/docs/redesign-context/22-documents/sketch-wave-2-chrome.html | awk '{ if ($1 < 2) { print "FAIL: @keyframes docs-fade-in anchor <2"; exit 1 } else print "PASS docs-fade-in:", $1 }' \
  && grep -cE 'backdrop[- ]no[- ]op|backdrop NO-OP|backdrop_no_op|취소.*confirm guard|confirm guard fires' cha-bio-safety/docs/redesign-context/22-documents/sketch-wave-2-chrome.html | awk '{ if ($1 < 2) { print "FAIL: backdrop NO-OP 코멘트 verbatim <2"; exit 1 } else print "PASS backdrop NO-OP:", $1 }' \
  && grep -cE 'width:40px;height:4px|w-10 h-1' cha-bio-safety/docs/redesign-context/22-documents/sketch-wave-2-chrome.html | awk '{ if ($1 < 1) { print "FAIL: BottomSheet handle 40x4 missing"; exit 1 } else print "PASS handle 40x4:", $1 }' \
  && grep -cE 'min\(480px|w-\[min\(480px' cha-bio-safety/docs/redesign-context/22-documents/sketch-wave-2-chrome.html | awk '{ if ($1 < 1) { print "FAIL: Modal min(480px,92vw) missing"; exit 1 } else print "PASS Modal width:", $1 }' \
  && grep -cE 'documentsApi|useQuery|useIsDesktop|DocumentSection|DocumentUploadForm|backdrop|@keyframes|typeLabel' cha-bio-safety/docs/redesign-context/22-documents/sketch-wave-2-chrome.html | awk '{ if ($1 < 5) { print "FAIL: 비즈 anchor 9 그룹 hit <5"; exit 1 } else print "PASS 비즈 anchor hits:", $1 }' \
  && grep -cE 'linear-gradient' cha-bio-safety/docs/redesign-context/22-documents/sketch-wave-2-chrome.html | awk '{ if ($1 > 0) { print "FAIL: linear-gradient found (T4 만 예외)"; exit 1 } else print "PASS no gradient" }' \
  && grep -cE '(text|bg|border)-status-(safe|fire|warning|danger|caution)' cha-bio-safety/docs/redesign-context/22-documents/sketch-wave-2-chrome.html | awk '{ if ($1 > 0) { print "FAIL: status- prefix found"; exit 1 } else print "PASS no status- prefix" }' \
  && grep -v '^<!--' cha-bio-safety/docs/redesign-context/22-documents/sketch-wave-2-chrome.html | grep -v '^#' | grep -cE 'font-size:[[:space:]]*(9|10|11)[^0-9]|fontSize:[[:space:]]*(9|10|11)[^0-9]' | awk '{ if ($1 > 0) { print "FAIL: 9/10/11 px found"; exit 1 } else print "PASS no 9-11px" }' \
  && grep -cE '\bw-8\b|\bh-8\b' cha-bio-safety/docs/redesign-context/22-documents/sketch-wave-2-chrome.html | awk '{ if ($1 > 0) { print "FAIL: w-8/h-8 found"; exit 1 } else print "PASS no w-8 h-8" }' \
  && grep -cE 'OQ #1' cha-bio-safety/docs/redesign-context/22-documents/sketch-wave-2-chrome.html | awk '{ if ($1 < 1) { print "FAIL: OQ #1 citation <1"; exit 1 } else print "PASS OQ #1:", $1 }' \
  && git log --oneline -1 | grep -q '260526-85a.*wave 2' && echo "PASS commit T1"
    </automated>
  </verify>
  <done>
    - sketch-wave-2-chrome.html 파일 존재 + ≥200 lines (≥280 권장)
    - data-theme="dark" ≥3 / data-theme="light" ≥1 (4 frame)
    - bg-surface-page ≥3 / bg-accent or border-accent ≥3 (OQ #1)
    - 탭 카피 소방계획서 ≥2 / 소방훈련자료 ≥2
    - 데스크톱 maxWidth 1200 ≥1 / gap 48 ≥1
    - @keyframes docs-slide-up ≥2 / docs-fade-in ≥2 (DocumentsPage line 36~39 verbatim)
    - backdrop NO-OP 코멘트 verbatim ≥2 (line 97~99 + 140~142)
    - BottomSheet handle 40x4 ≥1 / Modal min(480px,92vw) ≥1
    - 비즈 anchor 9 그룹 hit ≥5
    - linear-gradient 0건 / status- prefix 0건 / 9·10·11px 0건 / w-8 h-8 0건
    - OQ #1 인용 ≥1건
    - atomic commit `feat(quick-260526-85a): redesign/22-documents sketch wave 2 (...)` 1개
  </done>
</task>


<task type="auto">
  <name>Task 2 (W3): DocumentSection 카드 sketch — 헤더 + 4 state (loading/error/empty/data) + Hero 최신 + 과거 이력 + admin Trash2 32x32 (4 frame state 매트릭스, OQ #2 회색 통일 + OQ #3 12 격상)</name>
  <files>cha-bio-safety/docs/redesign-context/22-documents/sketch-wave-3-document-section.html</files>
  <action>
    23-education/sketch-wave-3-*.html + 20-legal-findings wic W3 패턴 mirror 로 새 sketch 파일 작성. DocumentSection.tsx 전체 (517 lines, 핵심 line 165~514) 시각화 — 헤더 행 (line 170~180 h2 + uploadBtn 분기) + loading 스켈레톤 (line 183~196 96+56+56) + error block (line 199~233 카피 + 다시 시도) + empty block (line 236~260 FileText 48 + 카피 admin/non-admin) + Hero card (line 263~394 최신 pill + Year tile 64x64 + Meta + 다운로드 spin + admin Trash2) + 과거 이력 list (line 397~514 연결 borderRadius + Year+Title + meta + admin Trash2). **OQ #2 회색 통일 안 (카드 색 bg-surface-raised 유지 + Year tile bg-surface-sunken + 최신 pill bg-accent)** + **OQ #3 최신 pill 11 → 12 격상 leading-none** + **OQ #4 빈 FileText 48 유지** + **w-[32px] h-[32px] arbitrary (w-8=48 함정 회피)** 4건 본 wave 핵심.

    **구조 (mbr W3 + wic W3 mirror, 4 frame state 매트릭스):**
    - `<!DOCTYPE html>` + `<head><title>redesign/22-documents — W3 DocumentSection + 4 state + Hero + 과거</title>` + `<style>` (W2 와 동일 tokens + typography + `@keyframes docsec-spin { to { transform: rotate(360deg); } }` DocumentSection line 167 verbatim)
    - `<body>` 안 4 frame side-by-side:

      | frame | viewport | 시각화 |
      |---|---|---|
      | 1 | 모바일 다크 393×800 loading | `data-theme="dark"` + 헤더 행 (h2 '소방계획서' text-body 16/600 + admin uploadBtn 모바일 w-[40px] h-[40px] icon-only border-border-default Plus size={20}) + 스켈레톤 (96 Hero placeholder + 56 + 56 = 3 placeholder bg-surface-raised) |
      | 2 | 모바일 다크 393×800 error | `data-theme="dark"` + 동일 헤더 + error block (padding 24 bg-surface-raised rounded-md border-border-default flex column items-center gap-3) + '문서 목록을 불러오지 못했습니다.' text-danger 16/600 + 다시 시도 button h-10 px-4 bg-accent text-text-on-accent text-body-sm 14/600 rounded-sm |
      | 3 | 모바일 라이트 393×800 empty (admin) | `data-theme="light"` + 동일 헤더 + empty block (padding 48 24 bg-surface-raised rounded-md border-border-default flex column items-center gap-3 text-center) + FileText size={48} color="var(--t3)" (OQ #4 유지) + '아직 업로드된 문서가 없습니다' text-body 16/600 text-text-primary + admin '우측 상단 업로드 버튼으로 소방계획서를 추가하세요.' text-body-sm 14/400 text-text-secondary |
      | 4 | 데스크톱 다크 1440×900 data (Hero + 과거 3건 + admin Trash2) | `data-theme="dark"` + 데스크톱 헤더 (h2 '소방계획서' + admin uploadBtn 데스크톱 h-10 px-4 inline-flex gap-1.5 bg-accent text-text-on-accent text-body-sm 14/600 rounded-full Plus size={16} '업로드') + Hero card (relative w-full min-h-24 p-6 bg-surface-raised border-border-default rounded-md flex items-center gap-4 cursor-pointer + 최신 pill absolute top-3 right-3 text-text-on-accent text-caption font-bold leading-none bg-accent px-2 py-1 rounded-full '최신' + Year tile w-16 h-16 bg-surface-sunken rounded-sm flex items-center justify-center font-mono text-display 28/600 text-text-primary '2026' + Meta column gap-1.5 + Title text-body 16/600 truncate '2026년 소방계획서' + 서브 text-body-sm 14/400 text-text-secondary '2026년_소방계획서.pdf · 12.4 MB · 윤종엽 · 2026-05-23 14:30' + admin Trash2 absolute bottom-3 right-3 w-[32px] h-[32px] bg-transparent text-danger border-border-default rounded-sm) + 과거 이력 라벨 '과거 이력' text-body 16/600 mt-3 mb-3 + 과거 row 3건 (각 min-h-14 p-4 bg-surface-raised border-border-default flex items-center gap-3 + 연결 borderRadius isFirst rounded-t-md isLast rounded-b-md + Title text-body-sm 14/600 '2025년 · 2025년 소방계획서' + meta text-body-sm 14/400 text-text-secondary '8.2 MB · 윤종엽 · 2025-04-12' + admin Trash2 동일) |

    **헤더 행 + uploadBtn 마크업 룰 (모바일 line 118~163, 데스크톱 line 118~141):**
    ```html
    <!-- 헤더 행 (line 170~180 verbatim) -->
    <div style="display:flex;align-items:center;justify-content:space-between;min-height:40px;">
      <h2 style="margin:0;font-size:16px;font-weight:600;color:var(--t1);">소방계획서</h2>
      <!-- admin uploadBtn 모바일 (line 142~162 verbatim — w-[40px] h-[40px] icon-only Plus size={20}) -->
      <button type="button" aria-label="소방계획서 업로드" style="width:40px;height:40px;display:inline-flex;align-items:center;justify-content:center;background:transparent;color:var(--t1);border:1px solid var(--bd);border-radius:8px;cursor:pointer;">
        <!-- Lucide Plus size={20} -->
        <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><line x1="12" y1="5" x2="12" y2="19"/><line x1="5" y1="12" x2="19" y2="12"/></svg>
      </button>
    </div>
    ```

    **Hero card 마크업 룰 (frame 4 — line 263~394 verbatim, Year tile 64x64 + 최신 pill OQ #3 12 격상 + admin Trash2 32x32):**
    ```html
    <!-- Hero card (line 263~393 verbatim) -->
    <div role="button" tabindex="0" style="position:relative;width:100%;min-height:96px;padding:24px;background:var(--bg2);border:1px solid var(--bd);border-radius:12px;display:flex;align-items:center;gap:16px;cursor:pointer;text-align:left;">
      <!-- 최신 pill (line 286~301, OQ #3 11 → 12 격상 + leading-none) -->
      <span class="text-caption font-bold leading-none" style="position:absolute;top:12px;right:12px;color:#fff;background:var(--acl);padding:4px 8px;border-radius:999px;">최신</span>
      <!-- Year tile 64x64 (line 304~326 verbatim) -->
      <div style="width:64px;height:64px;background:var(--bg3);border-radius:8px;display:flex;align-items:center;justify-content:center;flex-shrink:0;">
        <span style="font-family:'JetBrains Mono', monospace;font-size:28px;font-weight:600;color:var(--t1);">2026</span>
      </div>
      <!-- Meta column (line 329~355 verbatim) -->
      <div style="flex:1;min-width:0;display:flex;flex-direction:column;gap:6px;">
        <div style="font-size:16px;font-weight:600;color:var(--t1);overflow:hidden;text-overflow:ellipsis;white-space:nowrap;">2026년 소방계획서</div>
        <div style="font-size:14px;font-weight:400;color:var(--t2);overflow:hidden;text-overflow:ellipsis;white-space:nowrap;">2026년_소방계획서.pdf · 12.4 MB · 윤종엽 · 2026-05-23 14:30</div>
      </div>
      <!-- admin Trash2 32x32 (line 364~392 verbatim, ★ w-[32px] h-[32px] arbitrary — w-8 함정 회피) -->
      <button type="button" aria-label="2026년 소방계획서 삭제" class="text-danger" style="position:absolute;bottom:12px;right:12px;width:32px;height:32px;display:inline-flex;align-items:center;justify-content:center;background:transparent;border:1px solid var(--bd);border-radius:8px;cursor:pointer;">
        <!-- Lucide Trash2 size={16} -->
        <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="3 6 5 6 21 6"/><path d="M19 6l-1 14a2 2 0 0 1-2 2H8a2 2 0 0 1-2-2L5 6m3 0V4a2 2 0 0 1 2-2h4a2 2 0 0 1 2 2v2"/></svg>
      </button>
    </div>
    ```

    **과거 이력 row 마크업 룰 (frame 4 — line 397~514 verbatim, 연결 borderRadius):**
    ```html
    <!-- 과거 이력 라벨 (line 399~409 verbatim) -->
    <div style="font-size:16px;font-weight:600;color:var(--t1);margin-top:12px;margin-bottom:12px;">과거 이력</div>
    <!-- 과거 row 3건 (line 411~511 verbatim, isFirst rounded-t / isLast rounded-b) -->
    <div style="display:flex;flex-direction:column;">
      <div role="button" tabindex="0" style="width:100%;min-height:56px;padding:16px;background:var(--bg2);border-top:1px solid var(--bd);border-right:1px solid var(--bd);border-left:1px solid var(--bd);border-bottom:1px solid var(--bd);border-top-left-radius:8px;border-top-right-radius:8px;display:flex;align-items:center;gap:12px;cursor:pointer;text-align:left;">
        <div style="flex:1;min-width:0;display:flex;flex-direction:column;gap:4px;">
          <div style="font-size:14px;font-weight:600;color:var(--t1);overflow:hidden;text-overflow:ellipsis;white-space:nowrap;">2025년 · 2025년 소방계획서</div>
          <div style="font-size:14px;font-weight:400;color:var(--t2);overflow:hidden;text-overflow:ellipsis;white-space:nowrap;">8.2 MB · 윤종엽 · 2025-04-12</div>
        </div>
        <!-- admin Trash2 32x32 (동일 패턴) -->
      </div>
      <!-- 중간 row (borderRadius 없음) -->
      <div style="...border-top:none;..."><!-- 2024 --></div>
      <!-- isLast row (rounded-b) -->
      <div style="...border-top:none;border-bottom-left-radius:8px;border-bottom-right-radius:8px;"><!-- 2023 --></div>
    </div>
    ```

    **빈/오류 카피 verbatim (frame 2 / 3):**
    - 오류: '문서 목록을 불러오지 못했습니다.' (line 213) + '다시 시도' button (line 230)
    - 빈 제목: '아직 업로드된 문서가 없습니다' (line 252)
    - 빈 보조 admin: '우측 상단 업로드 버튼으로 소방계획서를 추가하세요.' (line 256)
    - 빈 보조 non-admin: '관리자가 문서를 업로드하면 이곳에 표시됩니다.' (line 257)
    - 최신 pill: '최신' (line 300)
    - 과거 이력 라벨: '과거 이력' (line 408)
    - Hero Meta verbatim 포맷 (line 352~353)
    - 과거 row verbatim 포맷 (line 456 + 468~469)

    **rules 박스:**
    - LOCKED 결정: OQ #1 (uploadBtn bg-accent 통일) + OQ #3 (최신 pill 11 → 12 격상 leading-none) + OQ #4 (빈 FileText size={48} 유지) verbatim 인용 1 fence
    - **비즈 anchor 9 그룹 박제 fence** (useQuery queryKey ['documents', type] + documentsApi.list + invalidateQueries + isAdmin + @keyframes docsec-spin 중심)
    - 토큰 매핑: 카드 외곽 `var(--bg2) → bg-surface-raised` / Year tile `var(--bg3) → bg-surface-sunken` (OQ #5) / 카드 border `var(--bd) → border-border-default` / 최신 pill bg `var(--acl) → bg-accent` (OQ #1) / Trash2 `var(--danger) → text-danger` (status- prefix 없음) / Hero Title `var(--t1) → text-text-primary` / Meta `var(--t2) → text-text-secondary` / 빈 FileText `var(--t3) → text-text-tertiary`
    - **w-[32px] h-[32px] arbitrary 박제** (admin Trash2 — w-8=48 함정, memory feedback_tailwind_w8_h8_is_48px)
    - **mbr deviation mirror** — "lin-grad" / "warning glyph" 약어 없음
    - negative gate self-check: HTML 주석 + entity escape

    **commit 명령:**
    ```
    git add cha-bio-safety/docs/redesign-context/22-documents/sketch-wave-3-document-section.html
    git commit -m "feat(quick-260526-85a): redesign/22-documents sketch wave 3 (DocumentSection — 헤더 + 4 state (loading/error/empty/data) + Hero 최신 pill OQ #3 12 격상 + Year tile 64x64 + 과거 이력 연결 borderRadius + admin Trash2 w-[32px] h-[32px] + FileText 48 유지 OQ #4)"
    ```
  </action>
  <verify>
    <automated>
test -f cha-bio-safety/docs/redesign-context/22-documents/sketch-wave-3-document-section.html \
  && wc -l cha-bio-safety/docs/redesign-context/22-documents/sketch-wave-3-document-section.html | awk '{ if ($1 < 200) { print "FAIL: too short"; exit 1 } else print "PASS lines:", $1 }' \
  && grep -c 'data-theme="dark"' cha-bio-safety/docs/redesign-context/22-documents/sketch-wave-3-document-section.html | awk '{ if ($1 < 3) { print "FAIL: dark frame <3"; exit 1 } else print "PASS dark frame:", $1 }' \
  && grep -c 'data-theme="light"' cha-bio-safety/docs/redesign-context/22-documents/sketch-wave-3-document-section.html | awk '{ if ($1 < 1) { print "FAIL: light frame <1"; exit 1 } else print "PASS light frame:", $1 }' \
  && grep -c '아직 업로드된 문서가 없습니다' cha-bio-safety/docs/redesign-context/22-documents/sketch-wave-3-document-section.html | awk '{ if ($1 < 1) { print "FAIL: 빈 제목 missing"; exit 1 } else print "PASS 빈 제목:", $1 }' \
  && grep -c '문서 목록을 불러오지 못했습니다' cha-bio-safety/docs/redesign-context/22-documents/sketch-wave-3-document-section.html | awk '{ if ($1 < 1) { print "FAIL: 오류 카피 missing"; exit 1 } else print "PASS 오류 카피:", $1 }' \
  && grep -c '다시 시도' cha-bio-safety/docs/redesign-context/22-documents/sketch-wave-3-document-section.html | awk '{ if ($1 < 1) { print "FAIL: 다시 시도 button missing"; exit 1 } else print "PASS 다시 시도:", $1 }' \
  && grep -c '최신' cha-bio-safety/docs/redesign-context/22-documents/sketch-wave-3-document-section.html | awk '{ if ($1 < 1) { print "FAIL: 최신 pill missing"; exit 1 } else print "PASS 최신:", $1 }' \
  && grep -c '과거 이력' cha-bio-safety/docs/redesign-context/22-documents/sketch-wave-3-document-section.html | awk '{ if ($1 < 1) { print "FAIL: 과거 이력 missing"; exit 1 } else print "PASS 과거 이력:", $1 }' \
  && grep -cE 'FileText|size=\{48\}|size="48"|width="48"' cha-bio-safety/docs/redesign-context/22-documents/sketch-wave-3-document-section.html | awk '{ if ($1 < 1) { print "FAIL: FileText 48 (OQ #4 빈 유지) missing"; exit 1 } else print "PASS FileText 48:", $1 }' \
  && grep -cE 'Trash2|trash2' cha-bio-safety/docs/redesign-context/22-documents/sketch-wave-3-document-section.html | awk '{ if ($1 < 2) { print "FAIL: Trash2 admin anchor <2"; exit 1 } else print "PASS Trash2:", $1 }' \
  && grep -cE 'w-\[32px\]|width:32px' cha-bio-safety/docs/redesign-context/22-documents/sketch-wave-3-document-section.html | awk '{ if ($1 < 2) { print "FAIL: 32x32 arbitrary anchor <2 (w-8=48 함정 회피)"; exit 1 } else print "PASS 32x32 arbitrary:", $1 }' \
  && grep -cE 'w-\[40px\]|width:40px' cha-bio-safety/docs/redesign-context/22-documents/sketch-wave-3-document-section.html | awk '{ if ($1 < 1) { print "FAIL: uploadBtn 모바일 40x40 missing"; exit 1 } else print "PASS 40x40:", $1 }' \
  && grep -cE 'w-16|width:64px' cha-bio-safety/docs/redesign-context/22-documents/sketch-wave-3-document-section.html | awk '{ if ($1 < 1) { print "FAIL: Year tile 64x64 missing"; exit 1 } else print "PASS Year tile 64x64:", $1 }' \
  && grep -cE 'JetBrains Mono|font-mono' cha-bio-safety/docs/redesign-context/22-documents/sketch-wave-3-document-section.html | awk '{ if ($1 < 1) { print "FAIL: JetBrains Mono Year tile missing"; exit 1 } else print "PASS JetBrains Mono:", $1 }' \
  && grep -c 'text-caption' cha-bio-safety/docs/redesign-context/22-documents/sketch-wave-3-document-section.html | awk '{ if ($1 < 1) { print "FAIL: text-caption (OQ #3) <1"; exit 1 } else print "PASS text-caption:", $1 }' \
  && grep -c 'leading-none' cha-bio-safety/docs/redesign-context/22-documents/sketch-wave-3-document-section.html | awk '{ if ($1 < 1) { print "FAIL: leading-none (OQ #3) <1"; exit 1 } else print "PASS leading-none:", $1 }' \
  && grep -cE 'bg-accent|bg-\[#2f81f7\]|background:#2f81f7|background:var\(--acl\)' cha-bio-safety/docs/redesign-context/22-documents/sketch-wave-3-document-section.html | awk '{ if ($1 < 2) { print "FAIL: bg-accent (OQ #1) <2"; exit 1 } else print "PASS bg-accent:", $1 }' \
  && grep -cE 'documentsApi|useQuery|invalidateQueries|isAdmin|@keyframes docsec-spin|downloadingIds|deletingIds|formatBytes|formatDate' cha-bio-safety/docs/redesign-context/22-documents/sketch-wave-3-document-section.html | awk '{ if ($1 < 4) { print "FAIL: 비즈 anchor hit <4"; exit 1 } else print "PASS 비즈 anchor hits:", $1 }' \
  && grep -cE 'linear-gradient' cha-bio-safety/docs/redesign-context/22-documents/sketch-wave-3-document-section.html | awk '{ if ($1 > 0) { print "FAIL: linear-gradient found (T4 만 예외)"; exit 1 } else print "PASS no gradient" }' \
  && grep -cE '(text|bg|border)-status-(safe|fire|warning|danger|caution)' cha-bio-safety/docs/redesign-context/22-documents/sketch-wave-3-document-section.html | awk '{ if ($1 > 0) { print "FAIL: status- prefix found"; exit 1 } else print "PASS no status- prefix" }' \
  && grep -v '^<!--' cha-bio-safety/docs/redesign-context/22-documents/sketch-wave-3-document-section.html | grep -v '^#' | grep -cE 'font-size:[[:space:]]*(9|10|11)[^0-9]|fontSize:[[:space:]]*(9|10|11)[^0-9]' | awk '{ if ($1 > 0) { print "FAIL: 9/10/11 px found (OQ #3 위반)"; exit 1 } else print "PASS no 9-11px" }' \
  && grep -cE '\bw-8\b|\bh-8\b' cha-bio-safety/docs/redesign-context/22-documents/sketch-wave-3-document-section.html | awk '{ if ($1 > 0) { print "FAIL: w-8/h-8 found (48 함정)"; exit 1 } else print "PASS no w-8 h-8" }' \
  && grep -cE 'OQ #2|OQ #3|OQ #4' cha-bio-safety/docs/redesign-context/22-documents/sketch-wave-3-document-section.html | awk '{ if ($1 < 2) { print "FAIL: OQ citation <2"; exit 1 } else print "PASS OQ:", $1 }' \
  && git log --oneline -1 | grep -q '260526-85a.*wave 3' && echo "PASS commit T2"
    </automated>
  </verify>
  <done>
    - sketch-wave-3-document-section.html 파일 존재 + ≥200 lines (≥280 권장)
    - data-theme="dark" ≥3 / data-theme="light" ≥1 (4 frame state 매트릭스)
    - 4 state 카피 verbatim — 빈 제목 + 오류 + 다시 시도 + 최신 + 과거 이력 (각 ≥1)
    - FileText size={48} (OQ #4 빈 유지) ≥1
    - Trash2 admin ≥2 / 32x32 arbitrary ≥2 (w-8=48 함정 회피) / 40x40 ≥1 (uploadBtn 모바일)
    - Year tile 64x64 ≥1 / JetBrains Mono ≥1
    - text-caption ≥1 / leading-none ≥1 (OQ #3 최신 pill 12 격상)
    - bg-accent ≥2 (OQ #1 — 최신 pill + 다시 시도 + uploadBtn 데스크톱)
    - 비즈 anchor hit ≥4 (documentsApi + useQuery + invalidateQueries + isAdmin + docsec-spin + formatBytes + formatDate 중)
    - linear-gradient 0건 / status- prefix 0건 / 9·10·11px 0건 / w-8 h-8 0건
    - OQ #2/#3/#4 인용 ≥2건
    - atomic commit `feat(quick-260526-85a): redesign/22-documents sketch wave 3 (...)` 1개
  </done>
</task>


<task type="auto">
  <name>Task 3 (W4): DocumentUploadForm 폼 inline sketch — year select + title input + file dashed button + ALLOWED 6 ext + Progress bar + Error block + Action row (4 frame, OQ #2 submit lin-grad ≥3 + OQ #3 보조 12 + OQ #4 모달 title 22 + Error 14 격상 + OQ #5 bg-surface-sunken 통일)</name>
  <files>cha-bio-safety/docs/redesign-context/22-documents/sketch-wave-4-upload-form-inline.html</files>
  <action>
    23-education/sketch-wave-4-*.html + mbr W4 패턴 mirror 로 새 sketch 파일 작성. DocumentUploadForm.tsx 전체 (402 lines, 핵심 line 201~401) 폼 콘텐츠 시각화 — 모달 title (line 204) + year select (line 207~221) + title input (line 224~234) + file dashed button (line 237~272) + Progress bar (line 275~322) + Error block (line 325~360) + Action row (line 363~399). **OQ #2 LOCKED submit button lin-grad ≥3 박제** (T4 만 예외 anchor) + **OQ #3 파일 보조 12 leading-none** + **OQ #4 모달 title 20 → 22 격상 + Error 카피 13 → 14 격상** + **OQ #5 var(--bg4) → bg-surface-sunken 통일** + **ALLOWED 6 ext + empty MIME fallback (HWP/ZIP iOS Safari) anchor fence** 5건 본 wave 핵심.

    **시트/모달 외곽 없이 폼 inline 만 — W2 에서 이미 다룸. 본 wave 는 폼 콘텐츠 시각만.**

    **구조 (mbr W4 + 23-education W4 mirror, 4 frame 단계 매트릭스):**
    - `<!DOCTYPE html>` + `<head><title>redesign/22-documents — W4 DocumentUploadForm 폼 inline + Progress + Error</title>` + `<style>` (W2 와 동일)
    - `<body>` 안 4 frame side-by-side:

      | frame | viewport | 시각화 |
      |---|---|---|
      | 1 | 모바일 다크 393×800 폼 초기 (file 없음, progress 없음) | `data-theme="dark"` + 모달 title '소방계획서 업로드' text-heading 22/600 (OQ #4 격상, 20 폐기) + 라벨 '연도' + year select (currentYear+1=2027 선택, descending 2027 → 2020) + 라벨 '제목' + title input placeholder verbatim '예: 2026년 소방계획서' + 라벨 '파일' + file dashed button '파일 선택' text-text-secondary text-body-sm 14 + 보조 'PDF, XLSX, DOCX, PPTX, HWP, ZIP · 최대 500MB' text-caption 12 leading-none text-text-secondary (OQ #3) + Action row submit (canSubmit=false bg-surface-sunken text-text-tertiary cursor-not-allowed) + cancel transparent border |
      | 2 | 모바일 다크 393×800 파일 선택 후 (canSubmit=true) | `data-theme="dark"` + 동일 폼 + title input '2026년 소방계획서' (auto-prefill 적용) + file dashed button (filled — '2026년_소방계획서.pdf · 12.4 MB' text-text-primary) + **Action row submit `bg:linear-gradient(135deg, #1d4ed8, #0ea5e9)` (OQ #2 LOCKED anchor 1)** + cancel transparent |
      | 3 | 데스크톱 다크 1440×900 업로드 중 (progress 50%) | `data-theme="dark"` + 폼 (year select disabled + title input disabled + file dashed button disabled '2026년_소방계획서.pdf · 12.4 MB' text-text-primary) + **Progress bar (line 275~322 verbatim — height-2 bg-surface-sunken rounded-sm overflow-hidden + fill width 50% h-full bg-accent transition-[width] duration-[240ms] ease-linear)** + Progress meta (JetBrains Mono % 16/600 text-text-primary '50%' + speedBps text-text-secondary text-body-sm 14 right '6.2 MB / 12.4 MB · 2.4 MB/s · 남은 시간 약 2초') + Action row submit (isUploading=true bg-accent '업로드 중…' bg-surface-sunken disabled) + cancel '취소' (line 397) + **submit lin-grad OQ #2 LOCKED anchor 2** |
      | 4 | 라이트 데스크톱 1440×900 오류 retry | `data-theme="light"` + 폼 (year + title + file 동일) + **Error block (line 325~360 verbatim — bg-surface-sunken border-border-default rounded-sm flex items-center justify-between gap-3 p-3)** + text-danger text-body-sm 14 font-medium '업로드 중 네트워크 오류가 발생했습니다. 다시 시도해주세요.' (OQ #4 14 격상, 13 폐기) + 다시 시도 button h-9 px-3.5 bg-accent text-text-on-accent text-body-sm 14/600 rounded-sm + Action row submit `bg:linear-gradient(135deg, #1d4ed8, #0ea5e9)` **(OQ #2 LOCKED anchor 3, ≥3 충족)** + cancel |

    **모달 title 마크업 룰 (frame 1/2/3/4 — line 204 verbatim, OQ #4 22 격상):**
    ```html
    <!-- 모달 title (line 204, OQ #4 20 → text-heading 22 격상) -->
    <div class="text-heading font-semibold text-text-primary" style="font-size:22px;font-weight:600;color:var(--t1);">소방계획서 업로드</div>
    ```

    **year select + title input 마크업 룰 (line 207~234 verbatim, OQ #5 var(--bg4) → bg-surface-sunken):**
    ```html
    <!-- year select (line 207~221 verbatim — yearOptions descending currentYear+1 → 2020) -->
    <div>
      <label class="text-body-sm font-semibold text-text-primary" style="display:block;font-size:14px;font-weight:600;color:var(--t1);margin-bottom:8px;">연도</label>
      <select class="bg-surface-sunken border border-border-default text-text-primary rounded-sm text-body-sm" style="width:100%;height:44px;padding:0 12px;background:var(--bg4);color:var(--t1);border:1px solid var(--bd);border-radius:8px;font-size:14px;outline:none;box-sizing:border-box;">
        <option value="2027">2027년</option><option value="2026" selected>2026년</option><option value="2025">2025년</option><option value="2024">2024년</option><option value="2023">2023년</option><option value="2022">2022년</option><option value="2021">2021년</option><option value="2020">2020년</option>
      </select>
    </div>
    <!-- title input (line 224~234 verbatim — placeholder '예: ${currentYear}년 ${label}') -->
    <div>
      <label class="text-body-sm font-semibold text-text-primary" style="display:block;font-size:14px;font-weight:600;color:var(--t1);margin-bottom:8px;">제목</label>
      <input type="text" placeholder="예: 2026년 소방계획서" class="bg-surface-sunken border border-border-default text-text-primary rounded-sm text-body-sm" style="width:100%;height:44px;padding:0 12px;background:var(--bg4);color:var(--t1);border:1px solid var(--bd);border-radius:8px;font-size:14px;outline:none;box-sizing:border-box;"/>
    </div>
    ```

    **file dashed button 마크업 룰 (line 237~272 verbatim — dashed border 보존, OQ #5 bg-surface-sunken):**
    ```html
    <!-- file 선택 button (line 247~268 verbatim — dashed border) -->
    <div>
      <label class="text-body-sm font-semibold text-text-primary" style="display:block;font-size:14px;font-weight:600;color:var(--t1);margin-bottom:8px;">파일</label>
      <button type="button" class="bg-surface-sunken border border-dashed border-border-default text-text-secondary rounded-sm text-body-sm" style="width:100%;height:44px;padding:0 12px;background:var(--bg3);color:var(--t2);border:1px dashed var(--bd2);border-radius:8px;font-size:14px;text-align:left;cursor:pointer;overflow:hidden;text-overflow:ellipsis;white-space:nowrap;">파일 선택</button>
      <!-- 보조 verbatim (line 269~271, OQ #3 12 leading-none) -->
      <div class="text-caption leading-none text-text-secondary" style="font-size:12px;font-weight:400;color:var(--t2);margin-top:8px;">PDF, XLSX, DOCX, PPTX, HWP, ZIP · 최대 500MB</div>
    </div>
    ```

    **Progress bar 마크업 룰 (frame 3 — line 275~322 verbatim):**
    ```html
    <!-- Progress bar (line 277~297 verbatim — height 8 bg-surface-sunken + fill width % bg-accent transition 240ms ease-linear) -->
    <div role="progressbar" aria-valuenow="50" aria-valuemin="0" aria-valuemax="100" style="height:8px;background:var(--bg4);border-radius:4px;overflow:hidden;">
      <div style="width:50%;height:100%;background:var(--acl);border-radius:4px;transition:width 240ms linear;"></div>
    </div>
    <!-- Progress meta (line 299~321 verbatim — JetBrains Mono % 16/600 + speedBps 14) -->
    <div style="display:flex;justify-content:space-between;align-items:center;gap:12px;font-family:'JetBrains Mono', monospace;">
      <span style="font-size:16px;font-weight:600;color:var(--t1);">50%</span>
      <span style="font-size:14px;font-weight:400;color:var(--t2);text-align:right;">6.2 MB / 12.4 MB · 2.4 MB/s · 남은 시간 약 2초</span>
    </div>
    ```

    **Error block 마크업 룰 (frame 4 — line 325~360 verbatim, OQ #4 14 격상):**
    ```html
    <!-- Error block (line 326~359 verbatim, OQ #4 14 격상 13 폐기) -->
    <div class="bg-surface-sunken border border-border-default rounded-sm" style="display:flex;align-items:center;justify-content:space-between;gap:12px;padding:12px;background:var(--bg3);border:1px solid var(--bd);border-radius:8px;">
      <span class="text-danger text-body-sm font-medium" style="color:var(--danger);font-size:14px;font-weight:500;flex:1;">업로드 중 네트워크 오류가 발생했습니다. 다시 시도해주세요.</span>
      <!-- 다시 시도 button (line 341~358 verbatim — bg-accent solid) -->
      <button type="button" class="bg-accent text-text-on-accent text-body-sm font-semibold rounded-sm" style="height:36px;padding:0 14px;background:var(--acl);color:#fff;font-size:14px;font-weight:600;border:none;border-radius:8px;cursor:pointer;flex-shrink:0;">다시 시도</button>
    </div>
    ```

    **Action row + submit lin-grad 마크업 룰 (★ frame 2/3/4 anchor ≥3 — OQ #2 LOCKED):**
    ```html
    <!-- Action row (line 363~399 verbatim) -->
    <div style="display:flex;gap:12px;margin-top:4px;">
      <!-- submit (canSubmit=true) — ★ OQ #2 LOCKED 인라인 그라데이션 -->
      <button type="button" class="text-text-on-accent text-body-sm font-semibold rounded-sm" style="flex:1;height:44px;background:linear-gradient(135deg, #1d4ed8, #0ea5e9);color:#fff;font-size:15px;font-weight:600;border:none;border-radius:8px;cursor:pointer;">업로드</button>
      <!-- cancel — transparent border (line 382~398 verbatim) -->
      <button type="button" class="text-text-primary text-body-sm font-semibold rounded-sm" style="flex:1;height:44px;background:transparent;color:var(--t1);font-size:15px;font-weight:600;border:1px solid var(--bd);border-radius:8px;cursor:pointer;">취소</button>
    </div>
    ```

    **ALLOWED + EXT_TO_MIME + MAX_SIZE anchor fence (rules 박스 박제, line 23~41 verbatim):**
    ```javascript
    // ALLOWED 6 ext (line 23~30 verbatim) — empty MIME fallback (HWP/ZIP iOS Safari) 포함
    const ALLOWED = [
      { ext: '.pdf',  mimes: ['application/pdf'] },
      { ext: '.xlsx', mimes: ['application/vnd.openxmlformats-officedocument.spreadsheetml.sheet'] },
      { ext: '.docx', mimes: ['application/vnd.openxmlformats-officedocument.wordprocessingml.document'] },
      { ext: '.pptx', mimes: ['application/vnd.openxmlformats-officedocument.presentationml.presentation'] },
      { ext: '.hwp',  mimes: ['application/x-hwp', 'application/vnd.hancom.hwp', 'application/haansofthwp', ''] },  // empty MIME fallback
      { ext: '.zip',  mimes: ['application/zip', 'application/x-zip-compressed', ''] },                              // empty MIME fallback
    ];
    const MAX_SIZE = 500 * 1024 * 1024;  // 500MB
    // 조건 verbatim (line 96): if (f.type && !mimes.includes(f.type)) → empty MIME OK (iOS .hwp/.zip 의 file.type 비어있는 케이스)
    // auto-prefill title (line 102 verbatim): `${year}년 ${typeLabel(type)}`
    // runMultipartUpload signature (line 122~130): { file, type, year, title, contentType, signal, onProgress }
    ```

    **카피 + 데이터 verbatim:**
    - 모달 title '${label} 업로드' — '소방계획서 업로드' (line 204)
    - 라벨 '연도' (line 208) / '제목' (line 225) / '파일' (line 238)
    - title placeholder '예: ${currentYear}년 ${label}' — '예: 2026년 소방계획서' (line 230)
    - file 선택 button '파일 선택' (file 없을 때) / '${filename} · ${formatBytes(size)}' (file 있을 때) (line 267)
    - 파일 보조 verbatim 'PDF, XLSX, DOCX, PPTX, HWP, ZIP · 최대 500MB' (line 270)
    - Progress 라벨 '속도 계산 중…' (speedBps < 100KB, line 313) / `${MB/s} · 남은 시간 ${formatEta}` (line 314~318)
    - submit '업로드' (canSubmit) / '업로드 중…' (isUploading) (line 380)
    - cancel '취소' (line 397)
    - Error 카피 '업로드 중 네트워크 오류가 발생했습니다. 다시 시도해주세요.' (line 145)

    **rules 박스:**
    - LOCKED 결정: OQ #2 (submit lin-grad ≥3 anchor) + OQ #3 (파일 보조 12 leading-none) + OQ #4 (모달 title 22 격상 + Error 14 격상) + OQ #5 (var(--bg4) → bg-surface-sunken 통일) verbatim 인용 1 fence
    - **비즈 anchor 9 그룹 박제 fence** — runMultipartUpload + ALLOWED + EXT_TO_MIME + MAX_SIZE + empty MIME fallback + auto-prefill title + yearOptions descending + abortRef + AbortController
    - 토큰 매핑: 모달 title `var(--t1) → text-text-primary` text-heading 22 / 라벨 `var(--t1)` text-body-sm 14/600 / input bg `var(--bg4) → bg-surface-sunken` (OQ #5) / input border `var(--bd) → border-border-default` / file dashed button bg `var(--bg3) → bg-surface-sunken` (OQ #5) / file dashed border `var(--bd2) → border-border-default` (dashed 보존) / 파일 보조 `var(--t2) → text-text-secondary` text-caption 12 leading-none (OQ #3) / Progress bar bg `var(--bg4) → bg-surface-sunken` (OQ #5) / Progress fill `var(--acl) → bg-accent` (OQ #1) / Error bg `var(--bg3) → bg-surface-sunken` (OQ #5) / Error 카피 `var(--danger) → text-danger` text-body-sm 14 font-medium (OQ #4 14 격상) / submit `linear-gradient(135deg, #1d4ed8, #0ea5e9)` (OQ #2 LOCKED ≥3 anchor) / cancel border `var(--bd) → border-border-default`
    - **mbr deviation mirror** — 본문 안 "lin-grad" 약어 anchor 박제 ≥1 (negative gate self-collision 회피, 실제 `linear-gradient(135deg, #1d4ed8, #0ea5e9)` 인라인 anchor 는 ≥3 박제)

    **commit 명령:**
    ```
    git add cha-bio-safety/docs/redesign-context/22-documents/sketch-wave-4-upload-form-inline.html
    git commit -m "feat(quick-260526-85a): redesign/22-documents sketch wave 4 (DocumentUploadForm 폼 inline — year select + title input + file dashed + ALLOWED 6 ext + empty MIME fallback + Progress + Error block + submit lin-grad OQ #2 LOCKED anchor 3 + 모달 title 22 OQ #4 + Error 14 격상)"
    ```
  </action>
  <verify>
    <automated>
test -f cha-bio-safety/docs/redesign-context/22-documents/sketch-wave-4-upload-form-inline.html \
  && wc -l cha-bio-safety/docs/redesign-context/22-documents/sketch-wave-4-upload-form-inline.html | awk '{ if ($1 < 200) { print "FAIL: too short"; exit 1 } else print "PASS lines:", $1 }' \
  && grep -c 'data-theme="dark"' cha-bio-safety/docs/redesign-context/22-documents/sketch-wave-4-upload-form-inline.html | awk '{ if ($1 < 3) { print "FAIL: dark frame <3"; exit 1 } else print "PASS dark frame:", $1 }' \
  && grep -c 'data-theme="light"' cha-bio-safety/docs/redesign-context/22-documents/sketch-wave-4-upload-form-inline.html | awk '{ if ($1 < 1) { print "FAIL: light frame <1"; exit 1 } else print "PASS light frame:", $1 }' \
  && grep -c '소방계획서 업로드' cha-bio-safety/docs/redesign-context/22-documents/sketch-wave-4-upload-form-inline.html | awk '{ if ($1 < 1) { print "FAIL: 모달 title missing"; exit 1 } else print "PASS 모달 title:", $1 }' \
  && grep -cE 'font-size:22px|text-heading' cha-bio-safety/docs/redesign-context/22-documents/sketch-wave-4-upload-form-inline.html | awk '{ if ($1 < 1) { print "FAIL: 모달 title 22 격상 (OQ #4) missing"; exit 1 } else print "PASS 22 격상:", $1 }' \
  && grep -cE '연도' cha-bio-safety/docs/redesign-context/22-documents/sketch-wave-4-upload-form-inline.html | awk '{ if ($1 < 1) { print "FAIL: 라벨 연도 missing"; exit 1 } else print "PASS 연도:", $1 }' \
  && grep -cE '제목' cha-bio-safety/docs/redesign-context/22-documents/sketch-wave-4-upload-form-inline.html | awk '{ if ($1 < 1) { print "FAIL: 라벨 제목 missing"; exit 1 } else print "PASS 제목:", $1 }' \
  && grep -cE '파일' cha-bio-safety/docs/redesign-context/22-documents/sketch-wave-4-upload-form-inline.html | awk '{ if ($1 < 1) { print "FAIL: 라벨 파일 missing"; exit 1 } else print "PASS 파일:", $1 }' \
  && grep -c '예: 2026년 소방계획서' cha-bio-safety/docs/redesign-context/22-documents/sketch-wave-4-upload-form-inline.html | awk '{ if ($1 < 1) { print "FAIL: title placeholder missing"; exit 1 } else print "PASS title placeholder:", $1 }' \
  && grep -c 'PDF, XLSX, DOCX, PPTX, HWP, ZIP · 최대 500MB' cha-bio-safety/docs/redesign-context/22-documents/sketch-wave-4-upload-form-inline.html | awk '{ if ($1 < 1) { print "FAIL: 파일 보조 verbatim missing"; exit 1 } else print "PASS 파일 보조:", $1 }' \
  && grep -c '파일 선택' cha-bio-safety/docs/redesign-context/22-documents/sketch-wave-4-upload-form-inline.html | awk '{ if ($1 < 1) { print "FAIL: file 선택 카피 missing"; exit 1 } else print "PASS file 선택:", $1 }' \
  && grep -cE '업로드 중 네트워크 오류가 발생했습니다' cha-bio-safety/docs/redesign-context/22-documents/sketch-wave-4-upload-form-inline.html | awk '{ if ($1 < 1) { print "FAIL: Error 카피 verbatim missing"; exit 1 } else print "PASS Error 카피:", $1 }' \
  && grep -cE 'border-dashed|border:1px dashed' cha-bio-safety/docs/redesign-context/22-documents/sketch-wave-4-upload-form-inline.html | awk '{ if ($1 < 1) { print "FAIL: file dashed border missing"; exit 1 } else print "PASS dashed:", $1 }' \
  && grep -cE 'JetBrains Mono|font-mono' cha-bio-safety/docs/redesign-context/22-documents/sketch-wave-4-upload-form-inline.html | awk '{ if ($1 < 1) { print "FAIL: Progress meta JetBrains Mono missing"; exit 1 } else print "PASS JetBrains Mono:", $1 }' \
  && grep -c 'progressbar' cha-bio-safety/docs/redesign-context/22-documents/sketch-wave-4-upload-form-inline.html | awk '{ if ($1 < 1) { print "FAIL: Progress bar role missing"; exit 1 } else print "PASS progressbar:", $1 }' \
  && grep -c '속도 계산 중' cha-bio-safety/docs/redesign-context/22-documents/sketch-wave-4-upload-form-inline.html | awk '{ if ($1 < 1) { print "FAIL: Progress 속도 계산 중 missing"; exit 1 } else print "PASS 속도 계산 중:", $1 }' \
  && grep -cE 'ALLOWED|\.pdf.*\.xlsx.*\.docx|6 ext' cha-bio-safety/docs/redesign-context/22-documents/sketch-wave-4-upload-form-inline.html | awk '{ if ($1 < 1) { print "FAIL: ALLOWED 6 ext anchor missing"; exit 1 } else print "PASS ALLOWED:", $1 }' \
  && grep -cE 'MAX_SIZE|500.*1024.*1024|500MB' cha-bio-safety/docs/redesign-context/22-documents/sketch-wave-4-upload-form-inline.html | awk '{ if ($1 < 1) { print "FAIL: MAX_SIZE 500MB anchor missing"; exit 1 } else print "PASS MAX_SIZE:", $1 }' \
  && grep -cE 'empty MIME|empty.*fallback|HWP.*iOS|iOS.*HWP|f\.type && !mimes' cha-bio-safety/docs/redesign-context/22-documents/sketch-wave-4-upload-form-inline.html | awk '{ if ($1 < 1) { print "FAIL: empty MIME fallback anchor missing"; exit 1 } else print "PASS empty MIME fallback:", $1 }' \
  && grep -cE 'runMultipartUpload|abortRef|AbortController' cha-bio-safety/docs/redesign-context/22-documents/sketch-wave-4-upload-form-inline.html | awk '{ if ($1 < 1) { print "FAIL: multipart anchor missing"; exit 1 } else print "PASS multipart:", $1 }' \
  && grep -cE 'auto-prefill|\$\{year\}년 \$\{typeLabel' cha-bio-safety/docs/redesign-context/22-documents/sketch-wave-4-upload-form-inline.html | awk '{ if ($1 < 1) { print "FAIL: auto-prefill anchor missing"; exit 1 } else print "PASS auto-prefill:", $1 }' \
  && grep -c '업로드' cha-bio-safety/docs/redesign-context/22-documents/sketch-wave-4-upload-form-inline.html | awk '{ if ($1 < 2) { print "FAIL: submit 업로드 <2"; exit 1 } else print "PASS 업로드:", $1 }' \
  && grep -c '취소' cha-bio-safety/docs/redesign-context/22-documents/sketch-wave-4-upload-form-inline.html | awk '{ if ($1 < 1) { print "FAIL: cancel 취소 missing"; exit 1 } else print "PASS 취소:", $1 }' \
  && grep -c 'linear-gradient(135deg, #1d4ed8, #0ea5e9)' cha-bio-safety/docs/redesign-context/22-documents/sketch-wave-4-upload-form-inline.html | awk '{ if ($1 < 3) { print "FAIL: ★ submit lin-grad anchor <3 (OQ #2 LOCKED, T4 만 예외 ≥3)"; exit 1 } else print "PASS submit lin-grad:", $1 }' \
  && grep -c 'text-caption' cha-bio-safety/docs/redesign-context/22-documents/sketch-wave-4-upload-form-inline.html | awk '{ if ($1 < 1) { print "FAIL: text-caption (OQ #3 보조) <1"; exit 1 } else print "PASS text-caption:", $1 }' \
  && grep -c 'leading-none' cha-bio-safety/docs/redesign-context/22-documents/sketch-wave-4-upload-form-inline.html | awk '{ if ($1 < 1) { print "FAIL: leading-none (OQ #3) <1"; exit 1 } else print "PASS leading-none:", $1 }' \
  && grep -cE '(text|bg|border)-status-(safe|fire|warning|danger|caution)' cha-bio-safety/docs/redesign-context/22-documents/sketch-wave-4-upload-form-inline.html | awk '{ if ($1 > 0) { print "FAIL: status- prefix found"; exit 1 } else print "PASS no status- prefix" }' \
  && grep -v '^<!--' cha-bio-safety/docs/redesign-context/22-documents/sketch-wave-4-upload-form-inline.html | grep -v '^#' | grep -cE 'font-size:[[:space:]]*(9|10|11)[^0-9]|fontSize:[[:space:]]*(9|10|11)[^0-9]' | awk '{ if ($1 > 0) { print "FAIL: 9/10/11 px found"; exit 1 } else print "PASS no 9-11px" }' \
  && grep -cE '\bw-8\b|\bh-8\b' cha-bio-safety/docs/redesign-context/22-documents/sketch-wave-4-upload-form-inline.html | awk '{ if ($1 > 0) { print "FAIL: w-8/h-8 found"; exit 1 } else print "PASS no w-8 h-8" }' \
  && grep -cE 'OQ #2|OQ #3|OQ #4|OQ #5' cha-bio-safety/docs/redesign-context/22-documents/sketch-wave-4-upload-form-inline.html | awk '{ if ($1 < 3) { print "FAIL: OQ citation <3"; exit 1 } else print "PASS OQ:", $1 }' \
  && git log --oneline -1 | grep -q '260526-85a.*wave 4' && echo "PASS commit T3"
    </automated>
  </verify>
  <done>
    - sketch-wave-4-upload-form-inline.html 파일 존재 + ≥200 lines (≥280 권장)
    - data-theme="dark" ≥3 / data-theme="light" ≥1 (4 frame 단계 매트릭스)
    - 모달 title '소방계획서 업로드' + 22 격상 (OQ #4) ≥1
    - 3 라벨 verbatim (연도 / 제목 / 파일) + title placeholder '예: 2026년 소방계획서'
    - 파일 보조 verbatim 'PDF, XLSX, DOCX, PPTX, HWP, ZIP · 최대 500MB' ≥1 / text-caption + leading-none (OQ #3)
    - file 선택 카피 + dashed border + Error 카피 '업로드 중 네트워크 오류가 발생했습니다. 다시 시도해주세요.'
    - Progress bar (role progressbar) + JetBrains Mono + '속도 계산 중'
    - **ALLOWED 6 ext + MAX_SIZE 500MB + empty MIME fallback + runMultipartUpload + auto-prefill 5 anchor 모두 ≥1**
    - submit '업로드' ≥2 / cancel '취소' ≥1
    - **★ submit lin-grad `linear-gradient(135deg, #1d4ed8, #0ea5e9)` anchor ≥3 (OQ #2 LOCKED T4 만 예외)**
    - status- prefix 0건 / 9·10·11px 0건 / w-8 h-8 0건
    - OQ #2/#3/#4/#5 인용 ≥3건
    - atomic commit `feat(quick-260526-85a): redesign/22-documents sketch wave 4 (...)` 1개
  </done>
</task>


<task type="auto">
  <name>Task 4 (W5): delete confirm + admin 권한 + multipart abort/retry + beforeunload guard sketch (4 frame, ★ delete confirm full string verbatim 1 byte 변경 금지 memory project_legal_findings_delete_incident_260520 일반화 + OQ #4 Error 14 격상)</name>
  <files>cha-bio-safety/docs/redesign-context/22-documents/sketch-wave-5-modal-confirm.html</files>
  <action>
    mbr W6 + wic W5 패턴 mirror 로 새 sketch 파일 작성. DocumentSection.tsx handleDelete (line 84~110) + DocumentUploadForm.tsx handleCancel (line 155~166) + handleSubmit useEffect beforeunload (line 64~72) + handleRetry (line 168~171) + 403 에러 카피 (line 141) 시각화. **★ delete confirm full string verbatim 1 byte 변경 금지** (memory project_legal_findings_delete_incident_260520 일반화) + **handleCancel abort confirm verbatim** + **beforeunload guard verbatim** + **OQ #4 Error 카피 14 격상** + **admin 권한 분기 verbatim** 5건 본 wave 핵심.

    **구조 (mbr W6 + wic W5 mirror, 4 frame confirm/error 매트릭스):**
    - `<!DOCTYPE html>` + `<head><title>redesign/22-documents — W5 confirm gates + 403 + abort + beforeunload</title>` + `<style>` (W2 와 동일)
    - `<body>` 안 4 frame side-by-side:

      | frame | viewport | 시각화 |
      |---|---|---|
      | 1 | 모바일 다크 393×800 delete confirm dialog + admin Trash2 클릭 anchor | `data-theme="dark"` + DocumentSection 카드 (Hero + admin Trash2 클릭 직후) + window.confirm native dialog 시각 (라이트박스 위 다이얼로그 시각화) + ★ delete confirm full string verbatim `"2026년 소방계획서"\n(2026년_소방계획서.pdf)\n\n정말 삭제하시겠습니까? 이 작업은 되돌릴 수 없습니다.` (memory project_legal_findings_delete_incident_260520, 1 byte 변경 금지) + 확인/취소 dialog button 시각 + handleDelete anchor `documentsApi.remove(item.id)` + `queryClient.invalidateQueries({ queryKey: ['documents', type] })` + toast.success '문서를 삭제했습니다' verbatim |
      | 2 | 데스크톱 다크 1440×900 abort confirm + Error retry | `data-theme="dark"` + DocumentUploadForm 폼 (업로드 중 progress 30%) + 사용자 취소 button 클릭 → window.confirm native dialog 시각 + ★ abort confirm verbatim `업로드를 취소하시겠습니까? 지금까지 전송된 데이터는 저장되지 않습니다.` (1 byte 변경 금지) + handleCancel anchor `abortRef.current?.abort()` + 확인 후 → Error block 표시 (AbortError → '업로드가 취소되었습니다.' toast 미발송, error block 만 표시) + 다시 시도 button → handleRetry → setError(null) + handleSubmit 재호출 |
      | 3 | 모바일 라이트 393×800 403 권한 에러 | `data-theme="light"` + DocumentUploadForm 폼 (업로드 직후) + Error block + ★ 403 카피 verbatim '관리자만 업로드할 수 있습니다.' (line 141, OQ #4 14 격상 text-body-sm 14 font-medium text-danger) + 다시 시도 button 비활성 (admin 권한 없으면 retry 도 403) + admin 권한 분기 anchor `useAuthStore((s) => s.staff?.role === 'admin')` (line 40 verbatim) + ApiError 403 분기 line 140 verbatim |
      | 4 | 라이트 데스크톱 1440×900 beforeunload 시각 | `data-theme="light"` + DocumentUploadForm 폼 (업로드 중 progress 60%) + browser navigation 시도 시 native browser dialog (브라우저별 다른 UI — Chrome/Safari/Firefox) 시각화 + ★ beforeunload returnValue verbatim `업로드 중입니다. 페이지를 나가면 전송이 중단됩니다.` (line 67~68, 1 byte 변경 금지) + useEffect anchor `isUploading 동안만 등록 + cleanup removeEventListener` + unmount abort useEffect anchor (line 75~79 verbatim) `abortRef.current?.abort() on unmount` |

    **delete confirm dialog 마크업 룰 (frame 1 — ★ line 87~89 verbatim, 1 byte 변경 금지):**
    ```html
    <!-- ★ DocumentSection handleDelete line 87~89 verbatim — memory project_legal_findings_delete_incident_260520 (2026-05-20 22행 삭제 사고 → Time Travel 복구 → 가드 강화) 일반화 -->
    <!-- 1 byte 변경 금지 (개행 \n 2개 + "title"\n(filename)\n\n정말... 패턴 보존) -->
    <div class="bg-surface-raised border border-border-default rounded-md" style="padding:20px;max-width:360px;background:var(--bg2);border:1px solid var(--bd);border-radius:12px;box-shadow:0 8px 24px rgba(0,0,0,0.4);">
      <div class="text-text-primary text-body-sm" style="font-size:14px;color:var(--t1);white-space:pre-line;">"2026년 소방계획서"
(2026년_소방계획서.pdf)

정말 삭제하시겠습니까? 이 작업은 되돌릴 수 없습니다.</div>
      <div style="display:flex;gap:8px;justify-content:flex-end;margin-top:16px;">
        <button type="button" class="bg-surface-sunken text-text-primary text-body-sm rounded-sm" style="padding:6px 14px;background:var(--bg3);color:var(--t1);border:none;border-radius:6px;cursor:pointer;">취소</button>
        <button type="button" class="text-text-on-accent text-body-sm font-semibold rounded-sm" style="padding:6px 14px;background:var(--danger);color:#fff;border:none;border-radius:6px;cursor:pointer;">삭제</button>
      </div>
    </div>
    ```

    **handleDelete 비즈 anchor fence (frame 1 rules 박스 — line 84~110 verbatim):**
    ```javascript
    // handleDelete (line 84~110 verbatim anchor)
    async function handleDelete(item: DocumentListItem, e: React.MouseEvent) {
      e.stopPropagation();  // Hero card onClick 전파 방지
      if (deletingIds.has(item.id)) return;
      // ★ confirm full string 1 byte 변경 금지 (memory project_legal_findings_delete_incident_260520)
      const confirmed = window.confirm(
        `"${item.title}"\n(${item.filename})\n\n정말 삭제하시겠습니까? 이 작업은 되돌릴 수 없습니다.`,
      );
      if (!confirmed) return;
      setDeletingIds((prev) => { const next = new Set(prev); next.add(item.id); return next; });
      try {
        await documentsApi.remove(item.id);
        toast.success('문서를 삭제했습니다');  // verbatim
        await queryClient.invalidateQueries({ queryKey: ['documents', type] });
      } catch (err) {
        const msg = err instanceof Error ? err.message : '문서 삭제에 실패했습니다';
        toast.error(msg);
      } finally {
        setDeletingIds((prev) => { const next = new Set(prev); next.delete(item.id); return next; });
      }
    }
    ```

    **abort confirm dialog 마크업 룰 (frame 2 — ★ line 157~159 verbatim, 1 byte 변경 금지):**
    ```html
    <!-- ★ DocumentUploadForm handleCancel line 157~159 verbatim — 1 byte 변경 금지 -->
    <div class="bg-surface-raised border border-border-default rounded-md" style="padding:20px;max-width:360px;background:var(--bg2);border:1px solid var(--bd);border-radius:12px;box-shadow:0 8px 24px rgba(0,0,0,0.4);">
      <div class="text-text-primary text-body-sm" style="font-size:14px;color:var(--t1);">업로드를 취소하시겠습니까? 지금까지 전송된 데이터는 저장되지 않습니다.</div>
      <div style="display:flex;gap:8px;justify-content:flex-end;margin-top:16px;">
        <button type="button" class="bg-surface-sunken text-text-primary text-body-sm rounded-sm" style="padding:6px 14px;background:var(--bg3);color:var(--t1);border:none;border-radius:6px;cursor:pointer;">취소</button>
        <button type="button" class="text-text-on-accent text-body-sm font-semibold rounded-sm" style="padding:6px 14px;background:var(--danger);color:#fff;border:none;border-radius:6px;cursor:pointer;">확인</button>
      </div>
    </div>
    ```

    **403 Error block 마크업 룰 (frame 3 — line 141 verbatim, OQ #4 14 격상):**
    ```html
    <!-- 403 Error block (line 141 verbatim, OQ #4 14 격상) -->
    <div class="bg-surface-sunken border border-border-default rounded-sm" style="display:flex;align-items:center;justify-content:space-between;gap:12px;padding:12px;background:var(--bg3);border:1px solid var(--bd);border-radius:8px;">
      <span class="text-danger text-body-sm font-medium" style="color:var(--danger);font-size:14px;font-weight:500;flex:1;">관리자만 업로드할 수 있습니다.</span>
      <!-- 다시 시도 button (403 은 retry 무의미 — disabled 시각, handleRetry 호출은 가능하나 다시 403) -->
      <button type="button" disabled class="bg-surface-sunken text-text-tertiary text-body-sm font-semibold rounded-sm" style="height:36px;padding:0 14px;background:var(--bg3);color:var(--t3);font-size:14px;font-weight:600;border:1px solid var(--bd);border-radius:8px;cursor:not-allowed;flex-shrink:0;">다시 시도</button>
    </div>
    ```

    **beforeunload native dialog 시각 (frame 4 — line 67~68 verbatim):**
    ```html
    <!-- ★ DocumentUploadForm useEffect beforeunload line 64~72 verbatim -->
    <!-- 1 byte 변경 금지 — returnValue '업로드 중입니다. 페이지를 나가면 전송이 중단됩니다.' -->
    <!-- 브라우저별 native dialog 시각 (Chrome / Safari / Firefox 별 UI 다름 — 메시지는 거의 무시되고 generic dialog 표시) -->
    <div class="bg-surface-raised border border-border-default rounded-md" style="padding:20px;max-width:420px;background:var(--bg2);border:1px solid var(--bd);border-radius:12px;box-shadow:0 8px 24px rgba(0,0,0,0.4);">
      <div class="text-text-primary text-body" style="font-size:16px;font-weight:600;color:var(--t1);margin-bottom:8px;">이 사이트를 나가시겠습니까?</div>
      <div class="text-text-secondary text-body-sm" style="font-size:14px;color:var(--t2);margin-bottom:16px;">업로드 중입니다. 페이지를 나가면 전송이 중단됩니다.</div>
      <!-- 변경사항이 저장되지 않을 수 있습니다. (브라우저 generic) -->
      <div style="display:flex;gap:8px;justify-content:flex-end;">
        <button type="button" class="bg-surface-sunken text-text-primary text-body-sm rounded-sm" style="padding:6px 14px;background:var(--bg3);color:var(--t1);border:none;border-radius:6px;cursor:pointer;">취소</button>
        <button type="button" class="text-text-on-accent text-body-sm font-semibold rounded-sm" style="padding:6px 14px;background:var(--danger);color:#fff;border:none;border-radius:6px;cursor:pointer;">나가기</button>
      </div>
    </div>
    ```

    **beforeunload useEffect anchor fence (frame 4 rules 박스 — line 64~72 verbatim):**
    ```javascript
    // beforeunload guard (line 64~72 verbatim, D-23)
    useEffect(() => {
      if (!isUploading) return;  // isUploading 동안만 등록
      const handler = (e: BeforeUnloadEvent) => {
        e.preventDefault();
        e.returnValue = '업로드 중입니다. 페이지를 나가면 전송이 중단됩니다.';  // 1 byte 변경 금지
      };
      window.addEventListener('beforeunload', handler);
      return () => window.removeEventListener('beforeunload', handler);  // cleanup
    }, [isUploading]);

    // unmount abort cleanup (line 75~79 verbatim)
    useEffect(() => {
      return () => { abortRef.current?.abort(); };  // 모달 닫힘/언마운트 시 abort
    }, []);
    ```

    **카피 verbatim:**
    - ★ delete confirm full string verbatim (line 87~89, 1 byte 변경 금지): `"${item.title}"\n(${item.filename})\n\n정말 삭제하시겠습니까? 이 작업은 되돌릴 수 없습니다.`
    - ★ abort confirm verbatim (line 157~159, 1 byte 변경 금지): '업로드를 취소하시겠습니까? 지금까지 전송된 데이터는 저장되지 않습니다.'
    - ★ beforeunload returnValue verbatim (line 67~68, 1 byte 변경 금지): '업로드 중입니다. 페이지를 나가면 전송이 중단됩니다.'
    - 403 카피 verbatim (line 141): '관리자만 업로드할 수 있습니다.'
    - ApiError other 카피 verbatim (line 145): '업로드 중 네트워크 오류가 발생했습니다. 다시 시도해주세요.'
    - AbortError 카피 verbatim (line 138): '업로드가 취소되었습니다.' (toast 미발송, error block 만)
    - toast.error verbatim: '파일 크기가 500MB를 초과합니다.' (line 87) / '지원하지 않는 파일 형식입니다. (PDF, XLSX, DOCX, PPTX, HWP, ZIP)' (line 92, 97) / '연도, 제목, 파일을 모두 입력해주세요.' (line 108)
    - toast.success verbatim: '업로드가 완료되었습니다.' (line 132) / '문서를 삭제했습니다' (line 98 DocumentSection) / '다운로드를 시작했습니다' (line 71)
    - toast.loading verbatim: '다운로드 중입니다…' (line 67)
    - toast.error verbatim: '다운로드에 실패했습니다. 네트워크를 확인해주세요.' (line 74) / '문서 목록을 불러오지 못했습니다. 잠시 후 다시 시도해주세요.' (line 56) / '문서 삭제에 실패했습니다' (line 101 fallback)

    **rules 박스:**
    - LOCKED 결정: OQ #4 (Error 카피 13 → 14 격상 text-body-sm font-medium) verbatim 인용 1 fence
    - **★ 비즈 anchor 9 그룹 박제 fence** — 특히 #8 (admin 권한 분기 verbatim) + #9 (delete confirm full string 1 byte 변경 금지)
    - **★ delete confirm full string verbatim fence** (line 87~89, memory project_legal_findings_delete_incident_260520 일반화)
    - **★ abort confirm + beforeunload verbatim fence** (line 67~68 + 157~159, 1 byte 변경 금지)
    - **★ admin 권한 분기 anchor fence** — `useAuthStore((s) => s.staff?.role === 'admin')` (DocumentSection line 40) + ApiError 403 분기 (DocumentUploadForm line 140~141) + 403 카피 verbatim
    - **★ handleRetry anchor fence** (line 168~171 verbatim — setError(null) + handleSubmit 재호출)
    - **★ unmount abort cleanup anchor fence** (line 75~79 verbatim — abortRef.current?.abort() on unmount)
    - toast 12종 verbatim fence (DocumentSection 5 + DocumentUploadForm 7)
    - memory 박제: `project_legal_findings_delete_incident_260520` (delete confirm) + `feedback_ios_pwa_push_silent_drop` (참고 — iOS PWA 안정성)
    - 토큰 매핑: dialog bg `var(--bg2) → bg-surface-raised` / dialog border `var(--bd) → border-border-default` / cancel button bg `var(--bg3) → bg-surface-sunken` (OQ #5) / 삭제/확인 button bg `var(--danger) → bg-danger` (또는 인라인 — Error 색 일관) / 403 Error 카피 `var(--danger) → text-danger` text-body-sm 14 font-medium (OQ #4)
    - **mbr deviation mirror** — 본문 안 "warning glyph" / "lin-grad" 약어 없음 (T4 anchor 박제 시 자체매칭 회피)

    **commit 명령:**
    ```
    git add cha-bio-safety/docs/redesign-context/22-documents/sketch-wave-5-modal-confirm.html
    git commit -m "feat(quick-260526-85a): redesign/22-documents sketch wave 5 (delete confirm full string verbatim 1 byte 변경 금지 memory project_legal_findings_delete_incident_260520 + abort confirm + beforeunload guard + 403 admin 권한 + handleRetry + unmount abort cleanup + toast 12종 verbatim + OQ #4 Error 14 격상)"
    ```
  </action>
  <verify>
    <automated>
test -f cha-bio-safety/docs/redesign-context/22-documents/sketch-wave-5-modal-confirm.html \
  && wc -l cha-bio-safety/docs/redesign-context/22-documents/sketch-wave-5-modal-confirm.html | awk '{ if ($1 < 200) { print "FAIL: too short"; exit 1 } else print "PASS lines:", $1 }' \
  && grep -c 'data-theme="dark"' cha-bio-safety/docs/redesign-context/22-documents/sketch-wave-5-modal-confirm.html | awk '{ if ($1 < 2) { print "FAIL: dark frame <2"; exit 1 } else print "PASS dark frame:", $1 }' \
  && grep -c 'data-theme="light"' cha-bio-safety/docs/redesign-context/22-documents/sketch-wave-5-modal-confirm.html | awk '{ if ($1 < 2) { print "FAIL: light frame <2"; exit 1 } else print "PASS light frame:", $1 }' \
  && grep -c 'confirm' cha-bio-safety/docs/redesign-context/22-documents/sketch-wave-5-modal-confirm.html | awk '{ if ($1 < 1) { print "FAIL: confirm anchor missing (OQ #4 + delete gate)"; exit 1 } else print "PASS confirm:", $1 }' \
  && grep -c '정말 삭제하시겠습니까? 이 작업은 되돌릴 수 없습니다' cha-bio-safety/docs/redesign-context/22-documents/sketch-wave-5-modal-confirm.html | awk '{ if ($1 < 1) { print "FAIL: ★ delete confirm full string verbatim missing (memory project_legal_findings_delete_incident_260520)"; exit 1 } else print "PASS delete confirm:", $1 }' \
  && grep -c '업로드를 취소하시겠습니까? 지금까지 전송된 데이터는 저장되지 않습니다' cha-bio-safety/docs/redesign-context/22-documents/sketch-wave-5-modal-confirm.html | awk '{ if ($1 < 1) { print "FAIL: ★ abort confirm verbatim missing"; exit 1 } else print "PASS abort confirm:", $1 }' \
  && grep -c '업로드 중입니다. 페이지를 나가면 전송이 중단됩니다' cha-bio-safety/docs/redesign-context/22-documents/sketch-wave-5-modal-confirm.html | awk '{ if ($1 < 1) { print "FAIL: ★ beforeunload returnValue verbatim missing"; exit 1 } else print "PASS beforeunload:", $1 }' \
  && grep -c '관리자만 업로드할 수 있습니다' cha-bio-safety/docs/redesign-context/22-documents/sketch-wave-5-modal-confirm.html | awk '{ if ($1 < 1) { print "FAIL: 403 카피 verbatim missing"; exit 1 } else print "PASS 403:", $1 }' \
  && grep -cE 'useAuthStore.*role.*admin|admin.*분기|isAdmin' cha-bio-safety/docs/redesign-context/22-documents/sketch-wave-5-modal-confirm.html | awk '{ if ($1 < 1) { print "FAIL: admin 권한 분기 anchor missing"; exit 1 } else print "PASS admin:", $1 }' \
  && grep -cE 'documentsApi\.remove|handleDelete' cha-bio-safety/docs/redesign-context/22-documents/sketch-wave-5-modal-confirm.html | awk '{ if ($1 < 1) { print "FAIL: handleDelete anchor missing"; exit 1 } else print "PASS handleDelete:", $1 }' \
  && grep -cE 'invalidateQueries.*documents|queryKey.*documents' cha-bio-safety/docs/redesign-context/22-documents/sketch-wave-5-modal-confirm.html | awk '{ if ($1 < 1) { print "FAIL: invalidateQueries documents anchor missing"; exit 1 } else print "PASS invalidate:", $1 }' \
  && grep -cE 'handleCancel|abortRef.*abort|AbortController' cha-bio-safety/docs/redesign-context/22-documents/sketch-wave-5-modal-confirm.html | awk '{ if ($1 < 1) { print "FAIL: abort anchor missing"; exit 1 } else print "PASS abort:", $1 }' \
  && grep -cE 'handleRetry|setError.*null.*handleSubmit' cha-bio-safety/docs/redesign-context/22-documents/sketch-wave-5-modal-confirm.html | awk '{ if ($1 < 1) { print "FAIL: handleRetry anchor missing"; exit 1 } else print "PASS handleRetry:", $1 }' \
  && grep -cE 'beforeunload|isUploading' cha-bio-safety/docs/redesign-context/22-documents/sketch-wave-5-modal-confirm.html | awk '{ if ($1 < 2) { print "FAIL: beforeunload useEffect anchor <2"; exit 1 } else print "PASS beforeunload useEffect:", $1 }' \
  && grep -cE 'unmount.*abort|abortRef.current\?\.abort\(\) on unmount' cha-bio-safety/docs/redesign-context/22-documents/sketch-wave-5-modal-confirm.html | awk '{ if ($1 < 1) { print "FAIL: unmount abort cleanup anchor missing"; exit 1 } else print "PASS unmount abort:", $1 }' \
  && grep -c '문서를 삭제했습니다' cha-bio-safety/docs/redesign-context/22-documents/sketch-wave-5-modal-confirm.html | awk '{ if ($1 < 1) { print "FAIL: toast.success 삭제 verbatim missing"; exit 1 } else print "PASS toast 삭제:", $1 }' \
  && grep -c 'project_legal_findings_delete_incident_260520' cha-bio-safety/docs/redesign-context/22-documents/sketch-wave-5-modal-confirm.html | awk '{ if ($1 < 1) { print "FAIL: memory project_legal_findings_delete_incident_260520 cross-ref missing"; exit 1 } else print "PASS memory cross-ref:", $1 }' \
  && grep -cE 'text-body-sm.*font-medium|font-size:14px.*font-weight:500' cha-bio-safety/docs/redesign-context/22-documents/sketch-wave-5-modal-confirm.html | awk '{ if ($1 < 1) { print "FAIL: Error text-body-sm 14 font-medium (OQ #4 격상) missing"; exit 1 } else print "PASS Error 14:", $1 }' \
  && grep -cE '(text|bg|border)-status-(safe|fire|warning|danger|caution)' cha-bio-safety/docs/redesign-context/22-documents/sketch-wave-5-modal-confirm.html | awk '{ if ($1 > 0) { print "FAIL: status- prefix found"; exit 1 } else print "PASS no status- prefix" }' \
  && grep -v '^<!--' cha-bio-safety/docs/redesign-context/22-documents/sketch-wave-5-modal-confirm.html | grep -v '^#' | grep -cE 'font-size:[[:space:]]*(9|10|11)[^0-9]|fontSize:[[:space:]]*(9|10|11)[^0-9]' | awk '{ if ($1 > 0) { print "FAIL: 9/10/11 px found"; exit 1 } else print "PASS no 9-11px" }' \
  && grep -cE '\bw-8\b|\bh-8\b' cha-bio-safety/docs/redesign-context/22-documents/sketch-wave-5-modal-confirm.html | awk '{ if ($1 > 0) { print "FAIL: w-8/h-8 found"; exit 1 } else print "PASS no w-8 h-8" }' \
  && grep -cE 'OQ #4' cha-bio-safety/docs/redesign-context/22-documents/sketch-wave-5-modal-confirm.html | awk '{ if ($1 < 1) { print "FAIL: OQ #4 citation <1"; exit 1 } else print "PASS OQ #4:", $1 }' \
  && git diff --name-only HEAD~4 HEAD -- cha-bio-safety/src 2>/dev/null | wc -l | awk '{ if ($1 > 0) { print "FAIL: src 변경 0 룰 위반"; exit 1 } else print "PASS src 0 변경" }' \
  && git log --oneline -1 | grep -q '260526-85a.*wave 5' && echo "PASS commit T4"
    </automated>
  </verify>
  <done>
    - sketch-wave-5-modal-confirm.html 파일 존재 + ≥200 lines (≥280 권장)
    - data-theme="dark" ≥2 / data-theme="light" ≥2 (4 frame confirm/error 매트릭스)
    - confirm anchor ≥1
    - **★ delete confirm full string verbatim '정말 삭제하시겠습니까? 이 작업은 되돌릴 수 없습니다' ≥1 (memory project_legal_findings_delete_incident_260520 1 byte 변경 금지)**
    - **★ abort confirm verbatim '업로드를 취소하시겠습니까? 지금까지 전송된 데이터는 저장되지 않습니다' ≥1 (1 byte 변경 금지)**
    - **★ beforeunload returnValue verbatim '업로드 중입니다. 페이지를 나가면 전송이 중단됩니다' ≥1 (1 byte 변경 금지)**
    - 403 카피 verbatim '관리자만 업로드할 수 있습니다' ≥1
    - admin 권한 분기 anchor (useAuthStore role admin) ≥1
    - handleDelete (documentsApi.remove) ≥1 / invalidateQueries documents ≥1
    - abort (handleCancel / abortRef / AbortController) ≥1 / handleRetry (setError null + handleSubmit) ≥1
    - beforeunload useEffect anchor (isUploading) ≥2 / unmount abort cleanup ≥1
    - toast.success '문서를 삭제했습니다' ≥1
    - memory `project_legal_findings_delete_incident_260520` cross-ref ≥1
    - Error text-body-sm 14 font-medium (OQ #4 격상) ≥1
    - status- prefix 0건 / 9·10·11px 0건 / w-8 h-8 0건
    - **git diff src/ HEAD~4 HEAD = 0 (4 commit 전체 src 변경 0 룰 — 본 PLAN 최종 verify gate)**
    - OQ #4 인용 ≥1건
    - atomic commit `feat(quick-260526-85a): redesign/22-documents sketch wave 5 (...)` 1개
  </done>
</task>

</tasks>

<verification>
**전체 PLAN verify gate (4 task 완료 후 자체 검수, mbr verify pattern 일관):**

```bash
# (1) 4 sketch HTML 파일 존재 확인
test -f cha-bio-safety/docs/redesign-context/22-documents/sketch-wave-2-chrome.html \
  && test -f cha-bio-safety/docs/redesign-context/22-documents/sketch-wave-3-document-section.html \
  && test -f cha-bio-safety/docs/redesign-context/22-documents/sketch-wave-4-upload-form-inline.html \
  && test -f cha-bio-safety/docs/redesign-context/22-documents/sketch-wave-5-modal-confirm.html \
  && echo "PASS 4 sketch files exist"

# (2) 평면 폴더 확인 (sketch/ 서브폴더 0)
test ! -d cha-bio-safety/docs/redesign-context/22-documents/sketch \
  && echo "PASS no sketch/ subfolder (평면 배치)"

# (3) src 3 파일 변경 0 byte (★ 가장 중요한 gate)
git diff --name-only HEAD~4 HEAD -- cha-bio-safety/src | wc -l | awk '{ if ($1 > 0) { print "FAIL: src 변경 발견"; exit 1 } else print "PASS src 0 변경" }'

# (4) atomic 4 commit 확인 (cherry-pick 사고 회피)
git log --oneline -5 | grep -c '260526-85a' | awk '{ if ($1 < 4) { print "FAIL: atomic 4 commit <4"; exit 1 } else print "PASS 4 commit" }'

# (5) linear-gradient — T1/T2/T3 = 0 / T4 만 ≥3 (OQ #2 LOCKED)
for f in sketch-wave-2-chrome.html sketch-wave-3-document-section.html sketch-wave-5-modal-confirm.html; do
  count=$(grep -c 'linear-gradient' "cha-bio-safety/docs/redesign-context/22-documents/$f" 2>/dev/null)
  if [ "$count" -gt 0 ]; then echo "FAIL: $f has linear-gradient (T4 만 예외)"; exit 1; fi
done
grep -c 'linear-gradient' cha-bio-safety/docs/redesign-context/22-documents/sketch-wave-4-upload-form-inline.html | awk '{ if ($1 < 3) { print "FAIL: T4 lin-grad <3 (OQ #2)"; exit 1 } else print "PASS T4 lin-grad ≥3" }'

# (6) negative gate 일관 — 4 파일 모두 status- prefix 0 / 9·10·11 px 0 / w-8 h-8 0
for f in sketch-wave-2-chrome.html sketch-wave-3-document-section.html sketch-wave-4-upload-form-inline.html sketch-wave-5-modal-confirm.html; do
  grep -cE '(text|bg|border)-status-(safe|fire|warning|danger|caution)' "cha-bio-safety/docs/redesign-context/22-documents/$f" | awk -v file="$f" '{ if ($1 > 0) { print "FAIL", file, "status- prefix"; exit 1 } }'
  grep -cE '\bw-8\b|\bh-8\b' "cha-bio-safety/docs/redesign-context/22-documents/$f" | awk -v file="$f" '{ if ($1 > 0) { print "FAIL", file, "w-8/h-8"; exit 1 } }'
done && echo "PASS negative gate 4 파일"
```
</verification>

<success_criteria>
- 4 sketch HTML 파일 생성 (22-documents/ 직속 평면 배치, sketch/ 서브폴더 0)
- 각 sketch ≥200 lines (≥280 권장), data-theme dark+light 양쪽 박제
- W2 chrome (탭 + 2단 + BottomSheet/Modal + backdrop NO-OP verbatim + OQ #1 bg-accent 통일)
- W3 DocumentSection (4 state 매트릭스 + Hero + 과거 + admin Trash2 32x32 + OQ #3 12 격상 + OQ #4 FileText 48 유지)
- W4 DocumentUploadForm 폼 inline (year + title + file dashed + ALLOWED 6 ext + Progress + Error + Action row + OQ #2 submit lin-grad ≥3)
- W5 confirm gate (★ delete confirm full string verbatim 1 byte 변경 금지 + abort + beforeunload + 403 admin + OQ #4 Error 14 격상)
- src 3 파일 + 외부 의존 6 파일 모두 코드 변경 0 byte (최종 git diff verify gate)
- atomic 4 commit (executor cherry-pick 사고 6회 precedent 회피)
- 비즈 anchor 9 그룹 모두 박제 (각 wave 안)
- negative gate 통과 (이모지 0 / 9·10·11px 0 / status- prefix 0 / w-8 h-8 0 / 옛 alias 토큰 0 / T1+T2+T3 linear-gradient 0 / T4 만 ≥3)
- mbr deviation 2건 mirror (lin-grad / warning glyph 약어 패턴 — negative gate self-collision 회피)
</success_criteria>

<output>
4 task 완료 후 SUMMARY 파일 작성:
`.planning/quick/260526-85a-redesign-22-documents-w2-w5-sketch-waves/260526-85a-SUMMARY.md`

SUMMARY 5번째 commit (atomic 4 + SUMMARY 1 = 총 5 commit):
```
git add .planning/quick/260526-85a-redesign-22-documents-w2-w5-sketch-waves/260526-85a-SUMMARY.md
git commit -m "docs(quick-260526-85a): SUMMARY — 4 sketch wave (chrome + section + upload form + confirm gate) + src 0 변경 + OQ #1~#5 LOCKED 반영 + delete confirm full string verbatim 박제 (project_legal_findings_delete_incident_260520 일반화)"
```
</output>
