---
phase: 260515-2r5-redesign-02-inspection-tsx-wave-6-powerp
plan: 01
type: quick
subsystem: redesign-02-inspection
tags:
  - redesign
  - inspection
  - tsx-conversion
  - design-tokens-v0.1.1
  - tailwind-only
  - powerpanel-modal
  - parkinggate-modal
  - misc-modals
  - wave6
  - last-wave
dependency_graph:
  requires:
    - cha-bio-safety/src/pages/InspectionPage.tsx (Wave 1~5 변환 완료본)
    - cha-bio-safety/docs/redesign-context/02-inspection/sketch/inspection-sketch-misc-modals.html (시안 권위 582줄)
    - cha-bio-safety/docs/design-system.md (§6.4 그라디언트 CTA / §7.1 결과 picker)
  provides:
    - PowerPanelModal v0.1.1 정합 (소방용전원공급반 PP-X-Y 단위, EXCEL-02 자탐 매핑 데이터 흐름 보존)
    - ParkingGateModal v0.1.1 정합 (주차장비/회전문, 북문/남문 분기 보존)
    - InspectionPage TSX 전 변환 완료 (Wave 1~6 모든 모달 v0.1.1 정합)
  affects:
    - cha-bio-safety/src/pages/InspectionPage.tsx
tech_stack:
  added: []
  patterns:
    - "v0.1.1 토큰 + Tailwind only (인라인 style 화이트리스트만 — position/top/bottom/transform/transition cubic-bezier/§6.4 그라디언트 CTA + 시그니처 그림자/비활성 저장 var(--border-default))"
    - "lucide outline 아이콘 통일 (Zap / Car / ChevronLeft / ChevronRight / CheckCircle2 / AlertTriangle / XCircle)"
    - "segmented 활성 우선 분기 (active accent / done safe-bg / inactive border-strong) — done-mark `✓` 텍스트 1글자 span 유지"
    - "결과 picker §7.1 일관성 (flex-row 가로 1줄 + 16px lucide + text-label + status outline+tinted bg) — resultIcon 헬퍼 local"
    - "loc-card 좌우 스와이프 (touch-pan-y + onTouchStart/onTouchEnd + _swX + dx 분기) — 비즈니스 한 줄도 변경 없음"
    - "EXCEL-02 자탐 매핑 보존 — onSave(selectedCP.id, ...) / cp.category === '소방용전원공급반' / FLOOR_ORDER sort 한 줄도 변경 없음"
key_files:
  modified:
    - cha-bio-safety/src/pages/InspectionPage.tsx
decisions:
  - "PowerPanel 헤더 background 시안 정합 — var(--bg2) raised → bg-surface-page (시안 .mh 권위, 1p0 Wave 5 패턴 동일)"
  - "결과 picker 시각 변경 — 현재 코드 flex-col 카드 (큰 아이콘 20 + 라벨 11) → 시안 권위 flex-row 가로 1줄 (16px lucide + text-label) (§7.1 일관성, 0l7/tbj 톤)"
  - "opt.icon 텍스트 이모지 의존 제거 — lucide resultIcon 헬퍼 local 정의 (각 모달 격리, Task 1/2 재정의 — 1p0 패턴)"
  - "PowerPanel nav 버튼 텍스트 ‹/› → lucide ChevronLeft/ChevronRight (size=18) — 1p0 패턴 동일 (touchAction:'pan-y' 인라인 → touch-pan-y Tailwind 클래스 우선)"
  - "Parking subItem empty state ('북문 또는 남문을 선택해 주세요') text-text-tertiary/text-label 로 토큰화 — 시안 권위"
  - "zoneBtnStyle (PowerPanel) / btnStyle (Parking) 인라인 헬퍼 함수 제거 — Tailwind 분기로 대체 (각 모달 안에서만 정의 → 안전)"
  - "증상 피커 미도입 — 자동화 5종(유도등/소화기/소화전/방화셔터/전실제연댐퍼) 아님. 메모리 룰 강제, placeholder='특이사항을 입력하세요' 보존"
metrics:
  duration: 약 50분
  completed_date: "2026-05-14"
  tasks: 2
  files: 1
---

# Phase 260515-2r5 Plan 01: redesign/02-inspection TSX Wave 6 — PowerPanel + Parking (마지막) Summary

**One-liner:** redesign/02-inspection TSX Wave 6 (마지막) — PowerPanelModal + ParkingGateModal v0.1.1 토큰 + Tailwind 변환 (InspectionPage.tsx +183/-96, 5937→6024줄). EXCEL-02 자탐 매핑(소방용전원공급반 onSave / cp.category 필터 / FLOOR_ORDER sort) 한 줄도 변경 없음 + loc-card 좌우 스와이프 onTouchStart/onTouchEnd 한 줄도 변경 없음 + Parking cpId useMemo (주차장비 단일 / 회전문 && cp.location === subItem) + showForm 분기 + useInspectionRevisitPopup category('소방용전원공급반'/'주차장비') 한 줄도 변경 없음. 증상 피커 미도입 (자동화 5종 아님 — 메모리 룰). lucide Zap/Car/ChevronLeft/ChevronRight/CheckCircle2/AlertTriangle/XCircle 통일. Wave 6 완료 = InspectionPage TSX 전 변환 완료.

## What Was Done

### Task 1: PowerPanelModal v0.1.1 토큰 + Tailwind 변환

**범위:** `function PowerPanelModal` 본문 (이전 라인 2260~2466, 신규 2260~2504, 약 +44줄). 시안 권위 = `inspection-sketch-misc-modals.html` 라인 358~421 (PowerPanelModal Mobile Light 영역).

**시각 변환 핵심 포인트:**
- 외곽 모달 컨테이너 — `bg:'var(--bg)'` 인라인 → `bg-surface-page` Tailwind 클래스 (position/top/bottom/transform/transition 인라인 화이트리스트만 유지)
- 헤더 .mh — `bg:'var(--bg2)'` → `bg-surface-page` (시안 권위, 1p0 Wave 5 패턴 동일). 좌측 `{group.icon}` ⚡ 이모지 → `<Zap size={18} />` (18px outline)
- 구역 segmented (연구동/사무동/지하) — `zoneBtnStyle` 인라인 헬퍼 제거 → §A.3 활성 우선 Tailwind 분기 (active `border-accent bg-accent text-text-on-accent` / done `border-safe-bar bg-safe-bg text-safe` / inactive `border-border-strong bg-surface-page text-text-secondary`). done-mark `✓` 텍스트 1글자 span 유지 (시안 권위)
- loc-card — `touchAction:'pan-y'` 인라인 → `touch-pan-y` Tailwind 클래스. `onTouchStart` / `onTouchEnd` 본문 한 줄도 변경 없음 (_swX / dx > 40 / dx < -40 / setPickerIdx)
- nav 버튼 — 텍스트 `‹` / `›` (fontSize:20) → `<ChevronLeft size={18}>` / `<ChevronRight size={18}>` lucide. 활성/비활성 opacity-30 + cursor 분기 그대로
- loc-cap (fontSize:11) → `text-caption text-text-tertiary font-semibold` (12 노안 친화)
- loc-title (fontSize:15) → `text-[15px] font-bold text-text-primary mt-0.5 truncate` (시안 .loc-title 15/700 일치)
- !zone empty state (fontSize:13) → `text-text-tertiary text-label`
- isDone 미니 뱃지 — rgba 인라인 → `bg-safe-bg border border-safe-bar text-safe` + 좌측 `<CheckCircle2 size={14} className="text-safe">` (✓ 이모지 제거)
- 결과 picker — **현재 코드 flex-col 카드 (큰 아이콘 20 + 라벨 11)** → **시안 권위 flex-row 가로 1줄 (16px lucide + text-label)**. `opt.icon` 텍스트 이모지 의존 제거 → `resultIcon(opt.value)` 로컬 헬퍼 (CheckCircle2/AlertTriangle/XCircle 16x16). 비활성 `border-border-default bg-surface-raised text-text-tertiary` / 활성 `border-{safe|warning|danger}-bar bg-{safe|warning|danger}-bg text-{safe|warning|danger}` 분기
- 특이사항 + 사진 — fontSize:12 → text-label, var(--bg2)/var(--bd2)/var(--t1) → bg-surface-raised/border-border-default/text-text-primary
- submitError / justSaved (fontSize:11) → text-label, rgba 인라인 → bg-{danger|safe}-bg / border-{danger|safe}-bar / text-{danger|safe}. justSaved 좌측 `<CheckCircle2 size={14}>` + "저장 완료"
- footer-bar — fontSize:13 → text-body(14, 시안 .btn-save 14/700 일치 + 노안 친화). §6.4 그라디언트 CTA + boxShadow 시그니처 그림자 화이트리스트 인라인 (비활성 시 `var(--border-default)` + `var(--text-tertiary)` 시그니처)

**비즈니스 보존 (한 줄도 변경 없음):**
- props 시그니처 8개 그대로
- state 8개 / useRef 2개 / useEffect 3개 (rAF mount + prevZone reset + prevIdx reset, eslint-disable-line 포함) 한 줄도 변경 없음
- `useMemo zoneCPs` — `cp.category === '소방용전원공급반' && cp.locationNo?.startsWith(PP_ZONE_PREFIX[zone])` 필터 + FLOOR_ORDER + .sort 한 줄도 변경 없음 (**EXCEL-02 자탐 매핑 메모리 룰**)
- selectedCP / isDone / doneCount / totalCount 헬퍼 그대로
- `useInspectionRevisitPopup({ checkpointId: selectedCP?.id ?? null, category: '소방용전원공급반', monthRecords, scheduleItems })` 그대로
- `handleSave` 본문 한 줄도 변경 없음 — `await onSave(selectedCP.id, result, memo, photoKey ?? undefined)` (자탐 엑셀 출력 측 데이터 소스 영향 0)
- loc-card 좌우 스와이프 `onTouchStart` / `onTouchEnd` (`_swX` / dx > 40 / dx < -40) 한 줄도 변경 없음
- 사진 흐름(usePhotoUpload / PhotoButton 호출 / photo.upload()) 그대로
- `navigate('/remediation/' + recordId)` 그대로

**Commit:** `5c340e4` — feat(260515-2r5): task 1 — PowerPanelModal v0.1.1 토큰 + Tailwind 변환 (+92/-48)

---

### Task 2: ParkingGateModal v0.1.1 토큰 + Tailwind 변환

**범위:** `function ParkingGateModal` 본문 (이전 라인 2467~2667, Task 1 line shift 반영 신규 2511~2754, 약 +43줄). 시안 권위 = `inspection-sketch-misc-modals.html` 라인 295~355 (ParkingModal Mobile Dark 영역).

**시각 변환 핵심 포인트:**
- 외곽 모달 / 헤더 / footer-bar — PowerPanel Task 1 §A.1/§A.2/§A.13 동일 패턴
- 좌측 `{group.icon}` 🚗 → `<Car size={18} className="text-text-secondary">` (18px outline)
- mh-sub (`group.labels.length > 1` 분기, fontSize:10) → `text-caption text-text-tertiary mt-0.5` "· 회전문" (12 노안 친화)
- 항목 segmented (주차장비/회전문) — `btnStyle` 인라인 헬퍼 제거 → PowerPanel §A.3 활성 우선 Tailwind 분기 동일
- **회전문 시 문 segmented (북문/남문)** 조건부 — PowerPanel §A.3 패턴 동일 (active accent / done safe / inactive border-strong)
- !item empty state ("항목을 선택해 주세요", fontSize:13) → `text-text-tertiary text-label`
- **item === '회전문' && !subItem empty state** ("북문 또는 남문을 선택해 주세요", fontSize:13) → `text-text-tertiary text-label`
- isDone 미니 뱃지 + 결과 picker + 특이사항/사진 + submitError/justSaved — PowerPanel Task 1 §A.9~A.12 동일 (flex-row 결과 picker 16px lucide + resultIcon local 헬퍼)
- footer-bar btn-save 인라인 — `!canSave` 분기 (PowerPanel 의 `!selectedCP` 와 의미 동일 — 변경 금지). §6.4 그라디언트 CTA + boxShadow 시그니처 그림자 화이트리스트 인라인

**비즈니스 보존 (한 줄도 변경 없음):**
- props 시그니처 8개 그대로
- state 8개 (item / subItem / result / memo / submitting / justSaved / submitError / visible) / useRef 2개 (prevItem / prevSub) / useEffect 3개 (rAF mount + prevItem reset + prevSub reset, eslint-disable-line 포함) 한 줄도 변경 없음
- `useMemo cpId` — `item === '주차장비' ? allCheckpoints.find(cp => cp.category === '주차장비')?.id ?? null : item === '회전문' && subItem ? allCheckpoints.find(cp => cp.category === '회전문' && cp.location === subItem)?.id ?? null : null` 한 줄도 변경 없음 (북문/남문 매핑)
- isDone / canSave / showForm 헬퍼 그대로 (`showForm = item === '주차장비' || (item === '회전문' && !!subItem)`)
- `useInspectionRevisitPopup({ checkpointId: cpId, category: '주차장비', monthRecords, scheduleItems })` 그대로 — **회전문도 주 카테고리 '주차장비' 기준 변경 금지**
- `handleSave` 본문 한 줄도 변경 없음 — `await onSave(cpId, result, memo, photoKey ?? undefined)`
- `(['주차장비','회전문'] as const).map(label => ...)` / `(['북문','남문'] as const).map(door => ...)` 순회 그대로
- 사진 흐름 / navigate 그대로

**Commit:** `a3d5999` — feat(260515-2r5): task 2 — ParkingGateModal v0.1.1 토큰 + Tailwind 변환 (+91/-48)

---

## Verification

### Gate Table (자동 검증)

| Gate | PowerPanel | Parking | Pass |
|------|------------|---------|------|
| 옛 토큰 (var(--bg/bg2/bd/bd2/acl/t1~3/safe/warn/danger/fire)) | 0건 | 0건 | ✅ |
| 9/10/11px 폰트 사이즈 | 0건 | 0건 | ✅ |
| 이모지 (⚡/🚗/📷/📹/✅) | 0건 | 0건 | ✅ |
| segmented done-mark `✓` 텍스트 1글자 span | 1건 (구역) | 4건 (항목 2 + 문 2) | ✅ |
| onSave 호출 보존 (단일 cp, EXCEL-02 자탐) | onSave(selectedCP.id, ...) 1건 | onSave(cpId, ...) 1건 | ✅ |
| 카테고리 필터 보존 | `cp.category === '소방용전원공급반'` 2건 | `cp.category === '주차장비'` 1건 + `cp.category === '회전문' && cp.location === subItem` 1건 | ✅ |
| PP_ZONE_PREFIX / PP_ZONE_LABELS | 3건 (zoneCPs + segmented) | — | ✅ |
| loc-card 좌우 스와이프 (onTouchStart/onTouchEnd/_swX) | 3건 | — | ✅ |
| useInspectionRevisitPopup category | '소방용전원공급반' 1건 | '주차장비' 1건 | ✅ |
| showForm 분기 (`item === '주차장비' \|\| (item === '회전문' && !!subItem)`) | — | 1건 | ✅ |
| lucide 통일 (Zap/Car/ChevronLeft/ChevronRight/CheckCircle2/AlertTriangle/XCircle) | 8건 | 6건 | ✅ |
| 증상 피커 미도입 (symptomPick 0건) | 0건 | 0건 | ✅ |
| TypeScript compile (`npx tsc --noEmit`) | — | — | ✅ 0 에러 |
| npm run build | — | — | ✅ 통과 |
| Vite chunk hash 변경 (`dist/assets/InspectionPage-*.js`) | BAP7S7JY (Task 1) | C43v-WZ5 (Task 2) | ✅ |

### 다른 영역 비변경 (line shift 만 허용)

- 전체 diff hunk 8건이 모두 PowerPanelModal (start=2329..2504) 또는 ParkingGateModal (start=2580..2748) 범위 내 — 자동 확인 완료
- DamperModal — 이전 라인 2668 → 신규 라인 2755 (Task 1+2 합산 +87 line shift). 본문 한 줄도 변경 없음
- PP_ZONE_PREFIX / PP_ZONE_LABELS / PPZone 타입 (이전 라인 2256~2258) 한 줄도 변경 없음
- Wave 1~5 변환 영역 (라인 1~2259) 한 줄도 변경 없음
- STAIRWELLS / CCTV_DVRS / CATEGORY_GROUPS / INSPECT_RESULT_OPTIONS 상수 한 줄도 변경 없음
- 외부 컴포넌트 (PhotoButton / InspectionRevisitPopup / AccessBlockedPopup) 비변경
- API / hooks / utils 비변경
- 다른 페이지 (.tsx) 비변경

### Build Output

```
✓ 87 modules transformed.
✓ built in 13.17s
dist/assets/InspectionPage-C43v-WZ5.js  (chunk hash 자동 갱신 — PWA 캐시 무효화)
```

---

## Business Logic Preservation Check (byte-for-byte 비교)

### PowerPanelModal.handleSave (EXCEL-02 자탐 매핑 메모리 룰)

```tsx
// 변환 전후 동일 — 한 줄도 변경 없음
const handleSave = async () => {
  if (!selectedCP) return
  setSubmitting(true); setSubmitError(null)
  try {
    const photoKey = await photo.upload()
    await onSave(selectedCP.id, result, memo, photoKey ?? undefined)  // ← 자탐 엑셀 출력 측 데이터 소스
    setJustSaved(true); setMemo(''); photo.reset()
  } catch (e: any) {
    setSubmitError(e.message ?? '저장 오류')
  } finally {
    setSubmitting(false)
  }
}
```

**검증:** `generateExcel.ts` 측에서 자탐 엑셀에 소방용전원공급반 record 사용 (EXCEL-02 매핑 — 메모리). PowerPanel onSave → record 저장 흐름 변경 없음 → 자탐 엑셀 출력 영향 0. ✅

### PowerPanelModal.zoneCPs useMemo (EXCEL-02 자탐 카테고리 필터)

```tsx
// 변환 전후 동일 — 한 줄도 변경 없음
const zoneCPs = useMemo(() => {
  if (!zone) return []
  const FLOOR_ORDER: string[] = ['8F','7F','6F','5F','3F','2F','1F','B1','B2','B3','B4','B5']
  return allCheckpoints
    .filter(cp => cp.category === '소방용전원공급반' && cp.locationNo?.startsWith(PP_ZONE_PREFIX[zone]))
    .sort((a, b) => FLOOR_ORDER.indexOf(a.floor) - FLOOR_ORDER.indexOf(b.floor))
}, [zone, allCheckpoints])
```

**검증:** PP_ZONE_PREFIX[zone].startsWith / FLOOR_ORDER + .sort / cp.category === '소방용전원공급반' 매핑 한 줄도 변경 없음. ✅

### PowerPanelModal loc-card 좌우 스와이프

```tsx
// 변환 전후 동일 — 한 줄도 변경 없음 (touchAction:'pan-y' 인라인 → touch-pan-y Tailwind 클래스만 차이, 동작 동일)
onTouchStart={e => { (e.currentTarget as any)._swX = e.touches[0].clientX }}
onTouchEnd={e => {
  const sx = (e.currentTarget as any)._swX
  if (sx == null) return
  const dx = e.changedTouches[0].clientX - sx
  if (dx > 40 && pickerIdx > 0) setPickerIdx(pickerIdx - 1)
  else if (dx < -40 && pickerIdx < zoneCPs.length - 1) setPickerIdx(pickerIdx + 1)
}}
```

**검증:** _swX / dx > 40 / dx < -40 / setPickerIdx 분기 한 줄도 변경 없음. ✅

### ParkingGateModal.cpId useMemo (북문/남문 매핑)

```tsx
// 변환 전후 동일 — 한 줄도 변경 없음
const cpId = useMemo(() => {
  if (item === '주차장비') return allCheckpoints.find(cp => cp.category === '주차장비')?.id ?? null
  if (item === '회전문' && subItem) return allCheckpoints.find(cp => cp.category === '회전문' && cp.location === subItem)?.id ?? null
  return null
}, [item, subItem, allCheckpoints])
```

**검증:** 주차장비 단일 cp / 회전문 && cp.location === subItem (북문/남문 키) 매핑 한 줄도 변경 없음. ✅

### ParkingGateModal.handleSave / showForm

```tsx
// 변환 전후 동일 — 한 줄도 변경 없음
const showForm = item === '주차장비' || (item === '회전문' && !!subItem)

const handleSave = async () => {
  if (!cpId) return
  setSubmitting(true); setSubmitError(null)
  try {
    const photoKey = await photo.upload()
    await onSave(cpId, result, memo, photoKey ?? undefined)
    setJustSaved(true); setMemo(''); photo.reset()
  } catch (e: any) {
    setSubmitError(e.message ?? '저장 오류')
  } finally {
    setSubmitting(false)
  }
}
```

**검증:** showForm 분기 / canSave / handleSave 본문 한 줄도 변경 없음. ✅

---

## Deviations from Plan

None — 플랜대로 실행.

특이사항:
- npm install 1회 수행 (worktree 환경에 node_modules 부재 — install 후 tsc/build 정상)
- Task 2 verification gate 11 (`git diff HEAD~2` 헤더 변경 grep) 은 대신 hunk 라인 범위 추출로 검증 (`git diff 9a8a54b..HEAD ... awk` — 8 hunk 모두 PowerPanel(2329..2504) 또는 Parking(2580..2748) 범위 내 확인) — 동등 검증, 동등 결과

---

## Out of Scope

- DamperModal (라인 2755~) — Wave 4 (tbj) 완료, 본 변환에서 손대지 않음. line shift +87 만 자연 반영
- BaeyeonModal — Wave 4 (tbj) 완료
- StairwellModal / CctvModal — Wave 5 (1p0) 완료
- DivModal / CompressorModal / DivUnderPicker / DivTrendSubview — Wave 3 (0l7) 완료
- 5 증상 피커 — Wave 2 (sp7) / Wave 4 (tbj) 완료
- 메인 page render / InspectionModal 셸 / 5 보조 컴포넌트 / FireAlarmModal — Wave 1 (i4r) 완료
- 외부 컴포넌트 (PhotoButton / InspectionRevisitPopup / AccessBlockedPopup) — pnr 정합 완료, 본 변환에서 비수정
- API / hooks / utils 비수정
- 다른 페이지 (.tsx) 비수정
- PP_ZONE_PREFIX / PP_ZONE_LABELS / PPZone 타입 (라인 2256~2258) / STAIRWELLS / CCTV_DVRS / CATEGORY_GROUPS / INSPECT_RESULT_OPTIONS 상수 비수정
- 자동화 5종 아님 → symptomPick state / setter 신설 0건
- EXCEL-02 자탐 매핑 — onSave / cp.category / record 저장 흐름 비수정
- cbc7119 디자인 격리 리포 한정 — 원본 PWA (cha-bio-safety) 영향 0 (메모리 룰)

---

## Commits

- `5c340e4` — feat(260515-2r5): task 1 — PowerPanelModal v0.1.1 토큰 + Tailwind 변환 (+92/-48, 144 lines touched)
- `a3d5999` — feat(260515-2r5): task 2 — ParkingGateModal v0.1.1 토큰 + Tailwind 변환 (+91/-48, 139 lines touched)

**합계:** +183/-96 (InspectionPage.tsx 5937 → 6024 lines, Δ+87)

---

## Files Created/Modified

- `cha-bio-safety/src/pages/InspectionPage.tsx` — 5937 → 6024 lines (+87)

---

## Self-Check: PASSED

- `cha-bio-safety/src/pages/InspectionPage.tsx` — FOUND (6024 lines)
- Commit `5c340e4` — FOUND
- Commit `a3d5999` — FOUND
- `dist/assets/InspectionPage-C43v-WZ5.js` — FOUND (chunk hash 갱신)
- TypeScript compile — PASSED (`npx tsc --noEmit` 0 에러)
- npm run build — PASSED
- 자동 gate 13/13 PASS (옛 토큰 0건 / 9/10/11px 0건 / 이모지 0건 / handleSave 보존 / 카테고리 필터 보존 / loc-card 스와이프 보존 / showForm 보존 / useInspectionRevisitPopup category 보존 / lucide 통일 / 증상 피커 미도입 / TypeScript / build / chunk hash)

---

## Milestone Note

**Wave 6 (마지막) 완료 = InspectionPage TSX 전 변환 완료** (Wave 1~6 모든 모달 v0.1.1 정합)

| Wave | 변환 영역 | 커밋 |
|------|-----------|------|
| Wave 1 (i4r) | 메인 page render + 일반 InspectionModal 셸 + 5 보조 컴포넌트 + FireAlarmModal | 400a865 + 8d59adf (pnr fix) |
| Wave 2 (sp7) | 5 증상 피커 (유도등/소화기/소화전/방화셔터/전실제연댐퍼) | 3aad9bd |
| Wave 3 (0l7) | DivUnderPicker / DivTrendSubview / DivModal / CompressorModal | 5e95414 |
| Wave 4 (tbj) | BaeyeonModal + DamperModal + 댐퍼 증상 피커 신설 | 6232e65 |
| Wave 5 (1p0) | StairwellModal + CctvModal | 809964f |
| **Wave 6 (2r5)** | **PowerPanelModal + ParkingGateModal** | **5c340e4 + a3d5999** |

**다음:**
- main 머지 + 시안 검수
- 다음 페이지 재디자인 (03-checkpoints 등) 시안 작업
- cbc7119 디자인 리포 한정 — 운영 PWA (cha-bio-safety) 영향 0
