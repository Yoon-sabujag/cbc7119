---
phase: 260515-2r5-redesign-02-inspection-tsx-wave-6-powerp
plan: 01
type: execute
wave: 6
depends_on:
  - 260514-i4r-redesign-02-inspection-tsx                      # Wave 1 — InspectionPage 메인 셸 변환 패턴 권위 + lucide import 블록 정착
  - 260514-pnr-wave-1-fix-photobutton-revisitpopup-acce        # Wave 1 fix — PhotoButton / InspectionRevisitPopup lucide+토큰 정합
  - 260514-sp7-redesign-02-inspection-tsx-wave-2-5             # Wave 2 — 5 증상 피커 변환 패턴 (PowerPanel/Parking 미도입 reference)
  - 260514-tbj-redesign-02-inspection-tsx-wave-4               # Wave 4 — BaeyeonModal/DamperModal 변환 패턴 (segmented + result picker)
  - 260515-0l7-redesign-02-inspection-tsx-wave-3-div           # Wave 3 — DivModal/CompressorModal §6.4 그라디언트 CTA / §7.1 결과 picker / footer 패턴 1:1 권위 (loc-card 좌우 스와이프 동일)
  - 260515-1p0-redesign-02-inspection-tsx-wave-5-cctv          # Wave 5 — StairwellModal/CctvModal 변환 패턴 (직전 wave — segmented + alert-success + result-mini 톤 권위)
files_modified:
  - cha-bio-safety/src/pages/InspectionPage.tsx
autonomous: true
requirements:
  - REDESIGN-02-INSPECTION-TSX-WAVE6
tags:
  - redesign
  - inspection
  - tsx-conversion
  - design-tokens-v0.1.1
  - tailwind-only
  - powerpanel-modal
  - parkinggate-modal
  - misc-modals
  - wave6
  - last-wave

must_haves:
  truths:
    - "PowerPanelModal (라인 2260~2466, 약 206줄) + ParkingGateModal (라인 2467~2667, 약 200줄) 2 컴포넌트의 비즈니스 로직(props 시그니처 / state / useRef / useMemo / useEffect / 핸들러 / 사진 업로드 / fetch / onSave 호출 / KST timestamp / useInspectionRevisitPopup 호출 / PP_ZONE_PREFIX / PP_ZONE_LABELS / PPZone 타입 / zoneCPs 매핑 / selectedCP / isDone / doneCount/totalCount / cpId / canSave / showForm / 좌우 스와이프 touch 핸들러 / pickerIdx 변경 / FLOOR_ORDER 정렬)이 100% 보존된다 (변경 전후 동작 동일)"
    - "PowerPanelModal: lucide Zap 헤더 아이콘 (이미 import 됨, '⚡' 이모지 제거) + 구역 segmented 3 버튼(연구동/사무동/지하 — `PP_ZONE_LABELS[z]`) + 개소 picker loc-card (좌우 ChevronLeft/ChevronRight 18px nav + 가운데 loc-cap/loc-title/loc-sub) + 결과 picker §7.1 (정상/주의/불량 CheckCircle2/AlertTriangle/XCircle 16px + tinted bg) + 메모/사진 + alert-success 'X 이미 점검 완료' + footer-bar §6.4 그라디언트 CTA 시각이 시안 권위 (`inspection-sketch-misc-modals.html` 라인 358~421) 1:1 매핑된다"
    - "ParkingGateModal: lucide Car 헤더 아이콘 (이미 import 됨, '🚗' 이모지 제거) + 항목 segmented 2 버튼(주차장비/회전문) + 회전문 선택 시 문 segmented 2 버튼(북문/남문 — 조건부 표시) + showForm(주차장비 || (회전문 && subItem)) 분기로 결과 picker §7.1 + 메모/사진 + alert-success + footer-bar §6.4 그라디언트 CTA 시각이 시안 권위 (`inspection-sketch-misc-modals.html` 라인 295~355) 1:1 매핑된다. **회전문 미선택 empty state ('북문 또는 남문을 선택해 주세요')도 시안 권위로 토큰화**"
    - "결과 picker (정상/주의/불량) 3 버튼 — §7.1 일관성 (큰 사이즈 카드 picker — flex-col + 아이콘 20→24 + 라벨 11→13/text-label + status outline + tinted bg). 0l7(DIV/Comp)/tbj(Baeyeon) loc-card 옆 큰 결과 picker 톤과 동일. **opt.icon 텍스트 이모지 의존 제거** — lucide 컴포넌트로 대체 (resultIcon 헬퍼 또는 인라인 분기)"
    - "구역/항목/문 segmented 버튼 (PowerPanel `zoneBtnStyle` + Parking `btnStyle` 인라인 헬퍼) — Wave 5 (1p0) 의 segmented 패턴과 동일 톤: 활성=`border-[1.5px] border-accent bg-accent text-text-on-accent`, done=`border-[1.5px] border-safe-bar bg-safe-bg text-safe`, 비활성=`border-border-strong bg-surface-page text-text-secondary`. done-mark `✓` 텍스트 1글자 span 유지 (시안 권위, lucide Check 대안 폐기)"
    - "PowerPanel 개소 picker loc-card (라인 2370~2393) — 좌우 스와이프 touchAction:'pan-y' + onTouchStart/onTouchEnd 한 줄도 변경 금지 + 좌/우 nav 버튼 ChevronLeft/ChevronRight 14~18px (현재 `‹`/`›` 텍스트 → lucide 교체) + opacity/cursor 동적 분기 (pickerIdx 경계값) 보존. 시안 .loc-card / .loc-nav / .loc-info / .loc-cap / .loc-title / .loc-sub 시각 1:1"
    - "PowerPanelModal/ParkingGateModal 본문 안에 인라인 style={{...}} 의 금지 키(color / background / padding / margin / fontSize / fontWeight / borderRadius / display / flex* / grid* / gap / width / height — 정적값) 사용이 0개이다 (i4r/sp7/tbj/0l7/1p0 화이트리스트와 동일 — position / top/left/right/bottom (NAV_BOTTOM / var(--sat, 0px) 동적) / transform (slide-up 동적) / transition cubic-bezier / animation / touchAction:'pan-y' 동적 / §6.4 그라디언트 CTA / boxShadow 그라디언트 시그니처 그림자 / 비활성 저장 배경 var(--border-default) 또는 var(--border-strong) — 화이트리스트만 허용)"
    - "PowerPanelModal/ParkingGateModal 변환 영역에서 9 / 10 / 11px 폰트 사이즈 사용이 0개이다 (text-caption(12) ~ text-display(28) 또는 text-[Npx] arbitrary 12 이상만)"
    - "옛 토큰(var(--bg) / var(--bg2) / var(--bg3) / var(--bd) / var(--bd2) / var(--acl) / var(--t1) / var(--t2) / var(--t3) / var(--safe) / var(--warn) / var(--danger) / var(--fire)) 사용이 변환 영역(라인 2260~2665) 내 0개이다"
    - "이모지(⚡ / 🚗 / ✅ / ✓ / 📷 / ‹ / ›) 사용이 변환 영역 내 0개 (segmented done-mark `✓` 텍스트 1글자 span 만 허용 — 1p0 Wave 5 전례)"
    - "**증상 피커 도입 X — PowerPanelModal/ParkingGateModal 은 자동화 5종(유도등/소화기/소화전/방화셔터/전실제연댐퍼)이 아니므로 symptomPick 신설 금지** (메모리 룰). 메모는 그대로 placeholder='특이사항을 입력하세요' 유지"
    - "**EXCEL-02 자탐 카테고리 매핑 — 자탐 엑셀에 소방용전원공급반 기록 사용은 의도된 설계 (메모리)**. PowerPanelModal 의 onSave 호출 / cpId / record 저장 흐름 한 줄도 변경 금지 — 엑셀 출력 측 영향 0 확인 필수"
    - "DamperModal (라인 2668~) 본문 + DIV/CompressorModal (Wave 3) + BaeyeonModal/DamperModal (Wave 4) + StairwellModal/CctvModal (Wave 5) 변환 영역 + 그 외 모든 영역은 한 줄도 변경 없이 보존된다. 라인 shift 만 허용"
    - "TypeScript 컴파일 0 에러, npm run build 통과 (chunk hash 자동 변경 — InspectionPage.tsx 본체 수정)"
  artifacts:
    - path: "cha-bio-safety/src/pages/InspectionPage.tsx"
      provides: "Wave 6 (마지막) 변환 완료된 PowerPanelModal + ParkingGateModal. PowerPanel: 구역 segmented + 개소 picker loc-card (좌우 스와이프 + ChevronLeft/Right nav) + 결과 picker §7.1. Parking: 항목 segmented + 회전문 시 문 segmented (조건부) + 결과 picker. lucide Zap/Car/ChevronLeft/ChevronRight/CheckCircle2/AlertTriangle/XCircle 통일. 인라인 style 금지 키 0건 + 9/10/11px 0건 + 옛 토큰 0건 + 이모지 0건 + 증상 피커 미도입 + EXCEL-02 자탐 카테고리 매핑 영향 0."
      min_lines: 5950
      contains: "function PowerPanelModal, function ParkingGateModal, PP_ZONE_PREFIX, PP_ZONE_LABELS, PPZone, zoneCPs, selectedCP, cpId, useInspectionRevisitPopup, '소방용전원공급반', '주차장비', '회전문', '북문', '남문'"
  key_links:
    - from: "cha-bio-safety/src/pages/InspectionPage.tsx (PowerPanelModal/ParkingGateModal)"
      to: "tailwind.config.js fontSize tokens + colors (status / text / surface / border / accent)"
      via: "Tailwind utility 클래스 (text-caption / text-label / text-body / bg-surface-raised / text-text-primary / border-border-default / bg-{safe|warning|danger}-bg / border-{safe|warning|danger}-bar / text-{safe|warning|danger} / bg-accent / text-text-on-accent)"
      pattern: "text-(caption|label|body-sm|body|title)|bg-surface-(page|raised|sunken|active)|text-text-(primary|secondary|tertiary|on-accent)|border-border-(default|strong)|bg-(safe|warning|danger)(-bar|-bg)?|text-(safe|warning|danger)|bg-accent|text-text-on-accent"
    - from: "cha-bio-safety/src/pages/InspectionPage.tsx (PowerPanelModal)"
      to: "PP_ZONE_PREFIX / PP_ZONE_LABELS / PPZone 타입 (라인 2256~2258, 변경 금지)"
      via: "zoneCPs useMemo (cp.category === '소방용전원공급반' && cp.locationNo?.startsWith(PP_ZONE_PREFIX[zone])) + FLOOR_ORDER sort + zone segmented 버튼 3종 매핑 (research/office/underground)"
      pattern: "PP_ZONE_PREFIX\\[(zone|z)\\]|PP_ZONE_LABELS\\[z\\]|'소방용전원공급반'|PPZone"
    - from: "cha-bio-safety/src/pages/InspectionPage.tsx (PowerPanelModal loc-card)"
      to: "좌우 스와이프 + ChevronLeft/ChevronRight nav 동작 (한 줄도 변경 금지)"
      via: "touchAction:'pan-y' + onTouchStart/onTouchEnd 핸들러 / setPickerIdx 분기 / opacity/cursor 동적 분기 (pickerIdx 경계값)"
      pattern: "_swX|onTouchStart|onTouchEnd|setPickerIdx\\(pickerIdx\\s*[-+]\\s*1\\)"
    - from: "cha-bio-safety/src/pages/InspectionPage.tsx (ParkingGateModal)"
      to: "cpId useMemo + showForm 분기 (한 줄도 변경 금지)"
      via: "cp.category === '주차장비' 단일 cp / cp.category === '회전문' && cp.location === subItem 분기 — '북문'/'남문' 매핑 + showForm = item === '주차장비' || (item === '회전문' && !!subItem)"
      pattern: "cp\\.category\\s*===\\s*'주차장비'|cp\\.category\\s*===\\s*'회전문'\\s*&&\\s*cp\\.location\\s*===\\s*subItem|showForm\\s*="
    - from: "cha-bio-safety/src/pages/InspectionPage.tsx (PowerPanelModal/ParkingGateModal handleSave)"
      to: "onSave(cpId, result, memo, photoKey ?? undefined) 단일 호출"
      via: "단일 cp 점검 (Stairwell/CCTV 의 batch loop 아님) — photoTargetCp 분기 없음. PowerPanel: selectedCP.id / Parking: cpId. EXCEL-02 자탐 매핑 영향 0"
      pattern: "await\\s+onSave\\((selectedCP\\.id|cpId),\\s*result,\\s*memo,\\s*photoKey\\s*\\?\\?\\s*undefined\\)"
    - from: "cha-bio-safety/src/pages/InspectionPage.tsx (PowerPanelModal/ParkingGateModal)"
      to: "lucide-react (Zap / Car / ChevronLeft / ChevronRight / CheckCircle2 / AlertTriangle / XCircle)"
      via: "이모지(⚡/🚗/✅/✓/‹/›/opt.icon) 제거 + lucide 통일. 모든 아이콘 이미 import 됨 (라인 22, 24)"
      pattern: "<(Zap|Car|ChevronLeft|ChevronRight|CheckCircle2|AlertTriangle|XCircle)\\s+size="
    - from: "cha-bio-safety/src/pages/InspectionPage.tsx (PowerPanelModal/ParkingGateModal)"
      to: "useInspectionRevisitPopup({ checkpointId, category, monthRecords, scheduleItems }) 호출"
      via: "PowerPanel: checkpointId = selectedCP?.id ?? null, category = '소방용전원공급반' / Parking: checkpointId = cpId, category = '주차장비' (회전문도 주 카테고리 '주차장비' 기준 — 현재 코드 그대로)"
      pattern: "useInspectionRevisitPopup\\(\\{[\\s\\S]*?category:\\s*'(소방용전원공급반|주차장비)'"
---

<objective>
InspectionPage TSX 변환 **Wave 6 (마지막)** — 6차 시안(`inspection-sketch-misc-modals.html` 582줄) 을 단일 진실 소스로 삼아 `cha-bio-safety/src/pages/InspectionPage.tsx` 의 **PowerPanelModal (라인 2260~2466, ~206줄) + ParkingGateModal (라인 2467~2667, ~200줄)** 2 영역(총 ~406줄)을 v0.1.1 디자인 토큰 + Tailwind only 로 교체한다.

**Purpose:**
- Wave 1 (i4r) / Wave 2 (sp7) / Wave 3 (0l7) / Wave 4 (tbj) / Wave 5 (1p0) 의 변환 패턴(인라인 금지 키 0건 + 9/10/11px 0건 + lucide 통일 + tokens.css 자동 라이트/다크 분기 + §7.1 결과 picker 일관성 + §6.4 그라디언트 CTA) 을 **마지막 2 모달**(소방용전원공급반/주차장비·회전문) 에 정착 — InspectionPage TSX 전 영역 v0.1.1 완전 정합
- 두 모달 모두 단일 cp 점검 (Stairwell/CCTV 의 batch loop 아님) — Wave 3 (0l7) DIV/CompressorModal 의 loc-card 좌우 스와이프 + §7.1 결과 picker 톤과 직접 연속
- **자동화 5종 카테고리 아님 → 증상 피커 미도입** (메모리 룰 — 자동화 5종은 유도등/소화기/소화전/방화셔터/전실제연댐퍼)
- **EXCEL-02 자탐 카테고리 매핑 (메모리)** — 자탐 엑셀에 소방용전원공급반 기록 사용은 의도된 설계. PowerPanel 의 onSave 호출 / cpId / record 저장 흐름 한 줄도 손대지 않고 시각만 교체 → 엑셀 출력 영향 0 보장
- DamperModal (라인 2668~) 직전까지가 Wave 6 경계. Wave 6 완료 시 InspectionPage TSX 전 변환 완료 (모든 모달 v0.1.1 정합)

**Output:**
- 비즈니스 로직 100% 보존하면서 Wave 6 변환 영역(라인 2260~2665) 의 마크업/스타일이 6차 시안과 1:1 매칭된 새 `InspectionPage.tsx`
- Wave 1~5 + DamperModal/InspectionModal/FireAlarmModal 본문 + 그 외 모든 영역은 **한 줄도 변경 없이 그대로 보존** (라인 shift 만 허용)

**Out of scope (Wave 6 범위 외):**
- `DamperModal` (라인 2668~) — Wave 4 (tbj) 완료, 본 변환에서 손대지 않음
- `BaeyeonModal` — Wave 4 (tbj) 완료
- `StairwellModal` / `CctvModal` — Wave 5 (1p0) 완료
- `DivModal` / `CompressorModal` / `DivUnderPicker` / `DivTrendSubview` — Wave 3 (0l7) 완료
- 5 증상 피커 — Wave 2 (sp7) / Wave 4 (tbj) 에서 완료
- 메인 page render / InspectionModal 셸 / 5 보조 컴포넌트 / FireAlarmModal — Wave 1 (i4r) 완료
- 외부 컴포넌트(PhotoButton / InspectionRevisitPopup / AccessBlockedPopup) 본체 수정 금지 — pnr 에서 이미 토큰 정합
- API / hooks / utils 수정 금지
- 다른 페이지 (.tsx) 수정 금지
- `PP_ZONE_PREFIX` / `PP_ZONE_LABELS` / `PPZone` 타입 (라인 2256~2258) / `STAIRWELLS` / `CCTV_DVRS` / `CATEGORY_GROUPS` / `INSPECT_RESULT_OPTIONS` 상수 변경 금지
- 자동화 5종 카테고리 아님 → **symptomPick state / setter 신설 절대 금지**
- **EXCEL-02 자탐 매핑** — 소방용전원공급반 onSave 호출 / record 저장 흐름 한 줄도 변경 금지 (엑셀 출력 측 영향 0)

**핵심 메모리 룰 강제 (작업 전 재확인):**
- **"EXCEL-02 자탐 카테고리 매핑 — 자탐 엑셀에 소방용전원공급반 기록 사용은 의도된 설계"** — PowerPanelModal.onSave(selectedCP.id, result, memo, photoKey ?? undefined) 호출 한 줄도 변경 금지. 자탐 엑셀 출력(`src/utils/generateExcel.ts`) 측 영향 0 — Wave 6 는 시각만 교체
- **"자동화 5종 카테고리 룰"** — 소방용전원공급반/주차장비/회전문 은 5종에 미포함. **증상 피커(symptomPick) 신설 금지**. 메모는 `placeholder='특이사항을 입력하세요'` 그대로
- **"디자인 변경 전 상의 필수"** — Wave 1~5 의 변환 패턴 외 새로운 디자인 결정이 필요하면 시안과 일치 확인. 시안이 권위
- **"디자인 조정은 시안 먼저"** — 시안 `inspection-sketch-misc-modals.html` 582줄이 마크업/색/구조 단일 진실 소스. 단 시안의 결과 picker 는 가로 1줄 (3 버튼) — 현재 코드는 세로 카드 (아이콘 위 / 라벨 아래). 시안 권위 따라 **결과 picker 시각은 시안의 가로 .result-picker 1줄 로 통일** (현재 코드 flex-col → flex-row, 큰 카드 → 컴팩트 가로 픽커)
- **"iOS web getUserMedia / KST timestamp / 사진 업로드 한 줄도 변경 금지"** — `photo.upload()` 호출 / usePhotoUpload 훅 / PhotoButton 호출 시그니처 보존
- **"PWA 캐시 무효화"** — InspectionPage.tsx 본체 수정 → chunk hash 자동 변경. 빌드 후 `dist/assets/InspectionPage-*.js` 파일명 hash 차이 확인 권장
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/STATE.md

# Wave 1 (i4r) — InspectionPage 변환 패턴 권위 + lucide import 블록 정착 (Zap/Car/ChevronLeft/ChevronRight 이미 import 됨)
@.planning/quick/260514-i4r-redesign-02-inspection-tsx/260514-i4r-PLAN.md

# Wave 3 (0l7) — DIV/CompressorModal 변환 패턴 (loc-card 좌우 스와이프 + §6.4 그라디언트 CTA + §7.1 결과 picker — PowerPanel 와 동일 톤)
@.planning/quick/260515-0l7-redesign-02-inspection-tsx-wave-3-div/260515-0l7-PLAN.md

# Wave 5 (1p0) — StairwellModal/CctvModal 변환 패턴 (직전 wave — segmented + alert-success + result-mini 톤 권위)
@.planning/quick/260515-1p0-redesign-02-inspection-tsx-wave-5-cctv/260515-1p0-PLAN.md
@.planning/quick/260515-1p0-redesign-02-inspection-tsx-wave-5-cctv/260515-1p0-SUMMARY.md

# 6차 sketch HTML — Wave 6 의 마크업/색/구조 단일 진실 소스 (582줄, PowerPanel + Parking + FireAlarm + 일반 InspectionModal 4뷰포트)
@cha-bio-safety/docs/redesign-context/02-inspection/sketch/inspection-sketch-misc-modals.html
@cha-bio-safety/docs/redesign-context/02-inspection/sketch/inspection-audit-design-system.html

# 디자인 시스템 v0.1.1 — §6.1 Progress / §6.4 그라디언트 / §7.1 결과 picker 일관성 / §7.2 카테고리 아이콘 매핑 / §7.3 결과 아이콘
@cha-bio-safety/docs/design-system.md

# 변환 대상 — 비즈니스 로직 100% 보존
@cha-bio-safety/src/pages/InspectionPage.tsx

# 토큰 / Tailwind 카탈로그
@cha-bio-safety/src/styles/tokens.css
@cha-bio-safety/tailwind.config.js

# 공통 UI / 외부 컴포넌트 — 그대로 사용 (수정 금지)
@cha-bio-safety/src/components/ui/icons.tsx
@cha-bio-safety/src/components/PhotoButton.tsx
@cha-bio-safety/src/components/InspectionRevisitPopup.tsx
</context>

<interfaces>
<!-- 이 영역이 의존하는 외부/내부 시그니처. 변경 금지. -->
<!-- Executor 는 아래 인터페이스를 직접 사용 (codebase 추가 탐색 불필요). -->

# 1. PowerPanelModal — props (변경 금지)
function PowerPanelModal({ group, allCheckpoints, records, monthRecords, scheduleItems, onClose, onSave }: {
  group:          typeof CATEGORY_GROUPS[0]    // { labels:['소방용전원공급반'], icon:'⚡', categories:['소방용전원공급반'], ... }
  allCheckpoints: CheckPoint[]
  records:        Record<string, CheckResult>
  monthRecords:   Record<string, MonthRecordEntry>
  scheduleItems:  ScheduleItem[]
  onClose:        () => void
  onSave:         (cpId: string, result: CheckResult, memo: string, photoKey?: string) => Promise<void>
})
// state 8개 — 한 줄도 변경 금지:
//   zone(PPZone|null) / pickerIdx(number, default 0) / result(CheckResult, default 'normal') / memo(string) /
//   submitting / justSaved / submitError / visible
// useRef 2개: prevZone / prevIdx
// useEffect 3개:
//   - requestAnimationFrame(() => setVisible(true)) — 슬라이드업 mount
//   - prevZone 비교 → pickerIdx/result/memo/submitError/justSaved reset + photo.reset() (eslint-disable-line, no deps)
//   - prevIdx 비교 → result/memo/submitError/justSaved reset + photo.reset() (eslint-disable-line, no deps)
// useMemo: zoneCPs = allCheckpoints
//   .filter(cp => cp.category === '소방용전원공급반' && cp.locationNo?.startsWith(PP_ZONE_PREFIX[zone]))
//   .sort((a, b) => FLOOR_ORDER.indexOf(a.floor) - FLOOR_ORDER.indexOf(b.floor))
//   FLOOR_ORDER 는 useMemo 내부 local 배열 — 그대로
// 헬퍼: selectedCP = zoneCPs[pickerIdx] ?? null / isDone / doneCount / totalCount
// useInspectionRevisitPopup({ checkpointId: selectedCP?.id ?? null, category: '소방용전원공급반', monthRecords, scheduleItems })
//   → { popupState, dismiss }

# 2. PowerPanelModal.handleSave — 한 줄도 변경 금지 (EXCEL-02 자탐 매핑 메모리 룰)
const handleSave = async () => {
  if (!selectedCP) return
  setSubmitting(true); setSubmitError(null)
  try {
    const photoKey = await photo.upload()
    // 단일 cp 저장 (Stairwell/CCTV 의 batch loop 아님)
    await onSave(selectedCP.id, result, memo, photoKey ?? undefined)
    setJustSaved(true); setMemo(''); photo.reset()
  } catch (e: any) {
    setSubmitError(e.message ?? '저장 오류')
  } finally {
    setSubmitting(false)
  }
}

# 3. PowerPanelModal loc-card 좌우 스와이프 — 한 줄도 변경 금지
// touch 핸들러:
onTouchStart={e => { (e.currentTarget as any)._swX = e.touches[0].clientX }}
onTouchEnd={e => {
  const sx = (e.currentTarget as any)._swX
  if (sx == null) return
  const dx = e.changedTouches[0].clientX - sx
  if (dx > 40 && pickerIdx > 0) setPickerIdx(pickerIdx - 1)
  else if (dx < -40 && pickerIdx < zoneCPs.length - 1) setPickerIdx(pickerIdx + 1)
}}
// touchAction:'pan-y' 인라인 화이트리스트 (Tailwind touch-pan-y 도 가능)
// 좌 nav: pickerIdx > 0 일 때만 활성 (opacity/cursor/color 동적)
// 우 nav: pickerIdx < zoneCPs.length - 1 일 때만 활성

# 4. ParkingGateModal — props (변경 금지)
function ParkingGateModal({ group, allCheckpoints, records, monthRecords, scheduleItems, onClose, onSave }: {
  group:          typeof CATEGORY_GROUPS[0]    // { labels:['주차장비','회전문'], icon:'🚗', categories:['주차장비','회전문'], ... }
  allCheckpoints: CheckPoint[]
  records:        Record<string, CheckResult>
  monthRecords:   Record<string, MonthRecordEntry>
  scheduleItems:  ScheduleItem[]
  onClose:        () => void
  onSave:         (cpId: string, result: CheckResult, memo: string, photoKey?: string) => Promise<void>
})
// state 8개 — 한 줄도 변경 금지:
//   item('주차장비'|'회전문'|null) / subItem('북문'|'남문'|null) / result(CheckResult, default 'normal') / memo(string) /
//   submitting / justSaved / submitError / visible
// useRef 2개: prevItem / prevSub
// useEffect 3개:
//   - requestAnimationFrame(() => setVisible(true))
//   - prevItem 비교 → subItem/result/memo/submitError/justSaved reset + photo.reset() (eslint-disable-line)
//   - prevSub 비교 → result/memo/submitError/justSaved reset + photo.reset() (eslint-disable-line)
// useMemo: cpId =
//   item === '주차장비' ? allCheckpoints.find(cp => cp.category === '주차장비')?.id ?? null
//   : item === '회전문' && subItem ? allCheckpoints.find(cp => cp.category === '회전문' && cp.location === subItem)?.id ?? null
//   : null
// 헬퍼: isDone = cpId ? !!records[cpId] : false / canSave = !!(item === '주차장비' ? cpId : cpId && subItem) / showForm = item === '주차장비' || (item === '회전문' && !!subItem)
// useInspectionRevisitPopup({ checkpointId: cpId, category: '주차장비', monthRecords, scheduleItems })
//   → { popupState, dismiss }
//   ※ 회전문도 주 카테고리 '주차장비' 기준 — 현재 코드 그대로 (변경 금지)

# 5. ParkingGateModal.handleSave — 한 줄도 변경 금지
const handleSave = async () => {
  if (!cpId) return
  setSubmitting(true); setSubmitError(null)
  try {
    const photoKey = await photo.upload()
    await onSave(cpId, result, memo, photoKey ?? undefined)
    setJustSaved(true); setMemo(''); photo.reset()
  } catch (e: any) {
    setSubmitError(e.message ?? '저장 오류')
  } finally {
    setSubmitting(false)
  }
}

# 6. 외부 상수 / 헬퍼 — 변경 금지 (라인 2256~2258)
type PPZone = 'research' | 'office' | 'underground'
const PP_ZONE_LABELS: Record<PPZone, string> = { research:'연구동', office:'사무동', underground:'지하' }
const PP_ZONE_PREFIX: Record<PPZone, string> = { research:'PP-R', office:'PP-O', underground:'PP-U' }
NAV_BOTTOM = 'calc(54px + env(safe-area-inset-bottom, 20px))'
CATEGORY_GROUPS  // 라인 ~51: 주차장비 (icon:'🚗', categories:['주차장비','회전문'], labels:['주차장비','회전문']) / 소방용전원공급반 (icon:'⚡', categories:['소방용전원공급반'], labels:['소방용전원공급반'])
INSPECT_RESULT_OPTIONS  // [{ value:'normal', label:'정상', icon:..., color:..., bg:... }, caution, bad]

# 7. 훅 — 변경 금지
useAuthStore() → { staff, token, ... }
usePhotoUpload() → { blob, preview, key, uploadFromFile, pick, reset, upload(), uploading, ... }
useInspectionRevisitPopup({ checkpointId, category, monthRecords, scheduleItems })
  → { popupState: { variant, checkedAt, inspectorName, recordId? } | null, dismiss(): void }
useNavigate / useState / useRef / useMemo / useEffect

# 8. 외부 컴포넌트 — 그대로 사용 (수정 금지)
PhotoButton({ hook, label, noCapture })                  // pnr 에서 Camera lucide + 토큰 정합 완료
InspectionRevisitPopup({ variant, checkedAt, inspectorName, recordId?, onClose, onGoToRemediation? })
  // pnr 정합 완료, 부모 박스 position:relative 필수 (selectedCP/showForm 블록 내 wrapper 가 그 역할)

# 9. lucide 아이콘 매핑 (Wave 6 — 모두 이미 import 됨, 신규 import 0건)
- ⚡ / PowerPanelModal 헤더 → **Zap (lucide, size={18}, className="text-text-secondary")** — 이미 import 됨 (라인 24)
- 🚗 / ParkingGateModal 헤더 → **Car (lucide, size={18}, className="text-text-secondary")** — 이미 import 됨 (라인 24)
- ‹ / loc-card 좌 nav → **ChevronLeft (lucide, size={18}, className="text-text-secondary")** — 이미 import 됨 (라인 22). pickerIdx === 0 일 때 className 에 opacity-30 추가, cursor 동적 분기
- › / loc-card 우 nav → **ChevronRight (lucide, size={18}, className="text-text-secondary")** — 이미 import 됨. pickerIdx === zoneCPs.length - 1 일 때 동일 분기
- ✓ / segmented done-mark → 텍스트 `✓` 1글자 span 유지 (시안 권위, 1p0 Wave 5 전례 동일)
- ✓ / "이미 점검 완료" alert → **CheckCircle2 (lucide, size={14}, className="text-safe flex-shrink-0")** — 이미 import 됨
- ✓ / "저장 완료" alert → **CheckCircle2 (lucide, size={14})** — 이미 import 됨
- 📷 / 사진 촬영 → **Camera (lucide)** — PhotoButton 안에서 이미 처리됨 (pnr 완료)
- 결과 picker 3종 (정상/주의/불량) → **CheckCircle2 / AlertTriangle / XCircle (lucide, size={16})** — 시안 .ic 16px. 이미 import 됨
- opt.icon (텍스트 이모지) → lucide 컴포넌트 분기 (resultIcon 헬퍼 도입 권장 — 두 모달 안에서 공유 또는 각 모달 local 정의)

# 10. 결과 picker 시각화 룰 (시안 권위 — 가로 1줄 .result-picker — 현재 코드의 세로 카드 폐기)
- 컨테이너: `flex gap-1.5` (시안 .result-picker — 6px gap)
- 버튼 기본: `flex-1 py-2.5 rounded-md border-[1.5px] inline-flex items-center justify-center gap-1.5 text-label font-bold cursor-pointer whitespace-nowrap transition-colors`
- 비활성: `border-border-default bg-surface-raised text-text-tertiary`
- 활성(정상/is-safe): `border-safe-bar bg-safe-bg text-safe`
- 활성(주의/is-warn): `border-warning-bar bg-warning-bg text-warning`
- 활성(불량/is-danger): `border-danger-bar bg-danger-bg text-danger`
- lucide outline 16x16 (CheckCircle2 / AlertTriangle / XCircle), `className="flex-shrink-0"`
- 라벨: 한글 텍스트 그대로 (`정상 / 주의 / 불량`)
- **현재 코드 flex-col + 큰 아이콘 20 + 라벨 11 → 시안 권위 따라 flex-row 가로 픽커 + 아이콘 16 + 라벨 13(text-label) 로 통일** (0l7/tbj 와 동일 톤)

# 11. 인라인 style 허용 화이트리스트 (i4r/sp7/tbj/0l7/1p0 전례 그대로)
- position / top / left / right / bottom (NAV_BOTTOM 등 동적 calc / `var(--sat, 0px)`)
- transform (slide-up / translateY 동적 — visible 분기)
- transition (cubic-bezier(0.32,0.72,0,1) 0.26s 등 — Tailwind 미정의 cubic-bezier)
- animation (keyframe 미정의)
- touchAction:'pan-y' (loc-card 스와이프 — Tailwind `touch-pan-y` 클래스도 가능 — 가능하면 클래스 우선)
- §6.4 그라디언트 CTA: `background: 'linear-gradient(135deg,#1d4ed8,#0ea5e9)'` (저장 버튼만) + `boxShadow:'0 4px 14px rgba(37,99,235,0.35)'` (시그니처 그림자)
- 비활성 저장 배경: `background:'var(--border-default)'` 또는 `var(--border-strong)` (Tailwind `bg-border-default` / `bg-border-strong` 가능하면 우선)
- bg-[rgba(...)] arbitrary value (필요 시) — 가능하면 surface 토큰 우선

# 12. 옛 토큰 → v0.1.1 매핑 (Wave 1~5 와 동일)
| 기존 | 새 토큰 |
|---|---|
| `var(--bg)` | `bg-surface-page` |
| `var(--bg2)` | `bg-surface-raised` |
| `var(--bg3)` | `bg-surface-sunken` |
| `var(--bd)` | `border-border-default` |
| `var(--bd2)` | `border-border-strong` |
| `var(--acl)` | `bg-accent` / `text-accent` / `text-text-on-accent` (활성 라벨) |
| `var(--t1)` | `text-text-primary` |
| `var(--t2)` | `text-text-secondary` |
| `var(--t3)` | `text-text-tertiary` |
| `var(--safe)` | `text-safe` / `bg-safe-bar` / `bg-safe-bg` |
| `var(--warn)` | `text-warning` / `bg-warning-bar` / `bg-warning-bg` |
| `var(--danger)` | `text-danger` / `bg-danger-bar` / `bg-danger-bg` |
| `var(--fire)` | `text-fire` / `bg-fire-bar` / `bg-fire-bg` |
| `INSPECT_RESULT_OPTIONS[*].color` / `.bg` 인라인 참조 | Tailwind status 토큰 클래스 분기로 대체 (opt.icon 텍스트 이모지 의존 제거 — lucide 컴포넌트 분기 헬퍼) |
</interfaces>

<design_rules_wave6>
**시안 매핑 (6차 sketch — `inspection-sketch-misc-modals.html` 582줄):**

### A. PowerPanelModal 영역 (시안 라인 358~421)

1. **외곽 모달 컨테이너 (라인 2341):**
   - 클래스: `fixed left-0 right-0 z-[99] flex flex-col overflow-hidden bg-surface-page`
   - 인라인 (화이트리스트): `style={{ top:'var(--sat, 0px)', bottom: NAV_BOTTOM, transform: visible ? 'translateY(0)' : 'translateY(100%)', transition:'transform 0.26s cubic-bezier(0.32,0.72,0,1)' }}`
   - **현재 `background:'var(--bg)'` 인라인 제거** (Tailwind `bg-surface-page` 로 대체)

2. **헤더 (라인 2344-2349 — 시안 .mh):**
   - 외곽 div: `flex items-center px-4 py-2.5 bg-surface-page border-b border-border-default flex-shrink-0 gap-2.5`
   - **현재 `background:'var(--bg2)'` → `bg-surface-page` (시안 권위, 1p0 Wave 5 패턴 동일)**
   - 좌측 아이콘 (라인 2345 — `{group.icon}` ⚡ 텍스트 이모지): `<Zap size={18} className="text-text-secondary flex-shrink-0" />`
   - 우측 텍스트 컨테이너 (라인 2346-2348): `flex-1 min-w-0`
     - mh-title (라인 2347): `text-[16px] font-bold text-text-primary` "{group.labels[0]}" (16/700 시안 권위)

3. **구역 선택 segmented (라인 2352-2365 — 시안 .bar-section + .seg):**
   - 외곽 div (라인 2352): `bg-surface-raised border-b border-border-default px-3.5 py-2 flex-shrink-0`
   - 라벨 (라인 2353): `text-caption font-semibold text-text-tertiary mb-1.5 tracking-wider` "구역 선택" (10 → 12 노안 친화)
   - 버튼 컨테이너 (라인 2354): `flex gap-1.5`
   - 버튼 (라인 2358-2362):
     - 기본: `flex-1 basis-0 min-w-0 px-2 py-2 rounded-sm border text-label font-bold cursor-pointer whitespace-nowrap transition-colors inline-flex items-center justify-center`
     - 활성(zone === z): `border-[1.5px] border-accent bg-accent text-text-on-accent`
     - done(allDone): `border-[1.5px] border-safe-bar bg-safe-bg text-safe`
     - 비활성: `border-border-strong bg-surface-page text-text-secondary`
     - **활성 우선:** zone === z 면 활성, 아니고 allDone 이면 done, 아니면 비활성
     - 라벨 + done-mark: `{PP_ZONE_LABELS[z]}{allDone && <span className="text-caption ml-1 opacity-85">✓</span>}` (라인 2360 의 `fontSize:10` → text-caption)
   - **`zoneBtnStyle` 인라인 헬퍼 (라인 2332-2338) 제거** — Tailwind 분기로 대체 (1p0 패턴 동일)

4. **개소 선택 loc-card (라인 2368-2395 — 시안 .loc-card / .loc-nav / .loc-info — `zone && zoneCPs.length >= 1` 조건부):**
   - 외곽 wrapper (라인 2369): `bg-surface-raised border-b border-border-default px-3.5 pt-2.5 pb-2 flex-shrink-0` (시안 그대로 .bar-section 톤)
   - loc-card 내부 (라인 2370-2393): `bg-surface-page border border-border-default rounded-md px-3 py-2.5 flex items-center gap-2.5`
   - 인라인 화이트리스트: `touchAction:'pan-y'` (Tailwind `touch-pan-y` 대안 가능 — 우선) — **onTouchStart/onTouchEnd 핸들러 그대로 (한 줄도 변경 금지)**
   - 좌 nav 버튼 (라인 2381-2384):
     - 클래스 기본: `w-9 h-9 rounded-sm border border-border-default bg-surface-page flex items-center justify-center flex-shrink-0 transition-opacity`
     - 활성(pickerIdx > 0): `cursor-pointer text-text-primary opacity-100`
     - 비활성: `cursor-default text-text-tertiary opacity-30`
     - 내용: 텍스트 `‹` 제거 → `<ChevronLeft size={18} className="flex-shrink-0" />` (현재 fontSize 20 → lucide 18)
   - loc-info 가운데 (라인 2385-2388): `flex-1 text-center min-w-0`
     - loc-cap (라인 2386): `text-caption text-text-tertiary font-semibold` "개소 ({pickerIdx + 1}/{totalCount}) · {doneCount}/{totalCount} 완료" (11 → 12 = text-caption)
     - loc-title (라인 2387): `text-body-sm font-bold text-text-primary mt-0.5 truncate` "{selectedCP?.location ?? ''}" (15 → text-body-sm(15) — 시안 .loc-title 15/700 일치) — 또는 `text-[15px]` arbitrary
     - **시안 라인 386~388 에는 loc-sub `사무동 5층 전기실` 도 있음 — 현재 코드에는 없음. Wave 6 변환은 현재 코드 동작 보존 → loc-sub 추가 안 함**
   - 우 nav 버튼 (라인 2389-2392): 좌 nav 와 동일 패턴 + `<ChevronRight size={18} />` 대체. 활성 조건은 `pickerIdx < zoneCPs.length - 1`

5. **modal-body 본문 영역 (라인 2398 — 시안 .modal-body):**
   - `flex-1 overflow-y-auto px-3.5 py-3 flex flex-col gap-3 relative`

6. **!zone empty state (라인 2399-2401):**
   - `flex-1 flex items-center justify-center text-text-tertiary text-label` "구역을 선택해 주세요" (13 → text-label)

7. **selectedCP wrapper (라인 2404 — 팝업 부모):**
   - `relative flex flex-col gap-3` (position:relative + flex col + gap-12 → gap-3)

8. **InspectionRevisitPopup 호출 (라인 2406-2415):** 그대로 (pnr 정합 완료)

9. **isDone 미니 뱃지 (라인 2416-2418 — 시안 .alert-success):**
   - `bg-safe-bg border border-safe-bar rounded-sm px-3 py-2 text-label font-semibold text-safe inline-flex items-center gap-1.5`
   - 좌측 `<CheckCircle2 size={14} className="text-safe flex-shrink-0" />` (✓ 이모지 제거)
   - 텍스트: "이미 점검 완료된 항목입니다" (12 → text-label(13) 노안 친화)

10. **결과 picker (라인 2420-2431 — 시안 .result-picker 가로 1줄):**
    - 라벨 (라인 2422): `text-caption font-semibold text-text-tertiary mb-1.5 tracking-wider` "점검 결과" (10 → 12)
    - 컨테이너 (라인 2423): `flex gap-1.5` (현재 gap:6 = 1.5)
    - 버튼 (라인 2424-2429):
      - **현재 코드 flex-col + 카드 픽커 (아이콘 위 + 라벨 아래) → 시안 권위 따라 flex-row 가로 픽커 1줄로 통일**
      - 기본: `flex-1 py-2.5 rounded-md border-[1.5px] inline-flex items-center justify-center gap-1.5 text-label font-bold cursor-pointer whitespace-nowrap transition-colors`
      - 비활성(`result !== opt.value`): `border-border-default bg-surface-raised text-text-tertiary`
      - 활성(normal): `border-safe-bar bg-safe-bg text-safe`
      - 활성(caution): `border-warning-bar bg-warning-bg text-warning`
      - 활성(bad): `border-danger-bar bg-danger-bg text-danger`
      - lucide 16x16: `opt.value === 'normal'` → `<CheckCircle2 size={16} className="flex-shrink-0">`, `caution` → `<AlertTriangle size={16}>`, `bad` → `<XCircle size={16}>`
      - 라벨: 텍스트 `{opt.label}` 그대로 (정상/주의/불량)
    - **opt.icon (텍스트 이모지) 의존 제거** — lucide 컴포넌트 분기 헬퍼 도입 권장 (`resultIcon(value: CheckResult): JSX.Element`) — 두 모달 안에 각각 local 정의 (1p0 Wave 5 의 resultMiniIcon 패턴과 동일, 각 모달 격리)
    - **시각이 1p0 Wave 5 result-mini 와 다른 점:** Wave 5 는 셀 안 컴팩트 (12px, py-1.5, text-caption) / Wave 6 는 큰 가로 픽커 (16px, py-2.5, text-label) — 0l7/tbj 톤

11. **특이사항 + 사진 (라인 2433-2443):**
    - 라벨 row (라인 2434-2437): `flex items-center justify-between mb-1.5`
      - 좌 label (라인 2436): `text-caption font-semibold text-text-tertiary tracking-wider` "특이사항 (선택)" (10 → 12)
      - 우 span (라인 2437): `text-caption text-text-tertiary` "점검 사진 (선택)" (10 → 12)
    - memo-row (라인 2438): `flex gap-2 items-start`
    - textarea (라인 2439): `flex-1 h-[72px] px-3 py-2.5 rounded-md bg-surface-raised border border-border-default text-text-primary text-label resize-none outline-none box-border font-sans placeholder:text-text-tertiary`
      - **placeholder="특이사항을 입력하세요" 그대로 — 증상 피커 미도입 룰**
      - 12 → text-label(13) 노안 친화
    - PhotoButton 그대로 (`<PhotoButton hook={photo} label="촬영" noCapture />`)

12. **submitError / justSaved 박스 (라인 2444 / 2445):**
    - submitError: `bg-danger-bg border border-danger-bar text-danger rounded-sm px-3 py-2 text-label font-semibold` (11 → text-label)
    - justSaved: `bg-safe-bg border border-safe-bar text-safe rounded-sm px-3 py-2 text-label font-semibold inline-flex items-center gap-1.5` + 좌측 `<CheckCircle2 size={14} className="flex-shrink-0">` (✓ 이모지 제거) + "저장 완료"

13. **footer-bar (라인 2452-2461 — 시안 .footer-bar):**
    - 컨테이너 (라인 2452): `flex gap-2 px-3.5 pt-2.5 pb-3 bg-surface-raised border-t border-border-default flex-shrink-0`
    - btn-close-mini (라인 2453): `px-4 py-3 rounded-md bg-surface-page border border-border-strong text-text-secondary text-label font-semibold cursor-pointer transition-colors`
    - btn-save (라인 2454-2460):
      - 클래스: `flex-1 py-3.5 rounded-md text-body font-bold border-0 transition-all`
      - 인라인 (§6.4 그라디언트 + 시그니처 그림자 화이트리스트):
        ```tsx
        style={{
          background: (submitting || photo.uploading || !selectedCP) ? 'var(--border-default)' : 'linear-gradient(135deg,#1d4ed8,#0ea5e9)',
          color: (submitting || photo.uploading || !selectedCP) ? 'var(--text-tertiary)' : '#fff',
          cursor: (submitting || photo.uploading || !selectedCP) ? 'default' : 'pointer',
          boxShadow: (submitting || photo.uploading || !selectedCP) ? 'none' : '0 4px 14px rgba(37,99,235,0.35)',
        }}
        ```
    - **현재 코드 fontSize:13 → text-body(14) — 시안 .btn-save 14/700 일치 + 노안 친화**
    - 저장 라벨 분기 그대로 (`photo.uploading ? '사진 업로드 중...' : submitting ? '저장 중...' : '점검 기록 저장'`)

### B. ParkingGateModal 영역 (시안 라인 295~355)

1. **외곽 모달 컨테이너 (라인 2548):** PowerPanel §A.1 동일 패턴

2. **헤더 (라인 2551-2557 — 시안 .mh):**
   - 외곽 div: PowerPanel §A.2 동일 (`bg-surface-page`)
   - 좌측 아이콘 (라인 2552 — `{group.icon}` 🚗): `<Car size={18} className="text-text-secondary flex-shrink-0" />`
   - 우측 텍스트:
     - mh-title (라인 2554): `text-[16px] font-bold text-text-primary` "{group.labels[0]}" → "주차장비"
     - mh-sub (라인 2555 — `group.labels.length > 1` 분기 그대로): `text-caption text-text-tertiary mt-0.5` "{group.labels.slice(1).join(' · ')}" → "회전문" (10 → 12 노안 친화)

3. **항목 선택 segmented (라인 2560-2574 — 시안 .bar-section + .seg):**
   - 외곽 div (라인 2560): `bg-surface-raised border-b border-border-default px-3.5 py-2 flex-shrink-0`
   - 라벨 (라인 2561): `text-caption font-semibold text-text-tertiary mb-1.5 tracking-wider` "항목 선택" (10 → 12)
   - 버튼 컨테이너 (라인 2562): `flex gap-1.5`
   - 버튼 (라인 2567-2571):
     - 기본/활성/done/비활성 분기: PowerPanel §A.3 segmented 패턴 그대로
     - 라벨: `{label}` ("주차장비" 또는 "회전문") + done-mark `✓` (allDone 시 `<span className="text-caption ml-1 opacity-85">✓</span>`)
   - **`btnStyle` 인라인 헬퍼 (라인 2539-2545) 제거** — Tailwind 분기로 대체

4. **회전문 선택 시 문 segmented (라인 2577-2593 — `item === '회전문'` 조건부, 시안 .bar-section):**
   - 외곽 div (라인 2578): `bg-surface-raised border-b border-border-default px-3.5 py-2 flex-shrink-0` (시안 권위 동일 톤)
   - 라벨 (라인 2579): `text-caption font-semibold text-text-tertiary mb-1.5 tracking-wider` "문 선택" (10 → 12)
   - 버튼 컨테이너 (라인 2580): `flex gap-1.5`
   - 버튼 (라인 2585-2589):
     - 기본/활성/done/비활성 분기: PowerPanel §A.3 segmented 패턴 그대로
     - 라벨: `{door}` ("북문" 또는 "남문") + done-mark `✓` (doneDoor 시)

5. **modal-body 본문 영역 (라인 2596):**
   - `flex-1 overflow-y-auto px-3.5 py-3 flex flex-col gap-3 relative`

6. **!item empty state (라인 2597-2599):**
   - `flex-1 flex items-center justify-center text-text-tertiary text-label` "항목을 선택해 주세요" (13 → text-label)

7. **item === '회전문' && !subItem empty state (라인 2600-2602):**
   - `flex-1 flex items-center justify-center text-text-tertiary text-label` "북문 또는 남문을 선택해 주세요" (13 → text-label)

8. **showForm wrapper (라인 2605):** `relative flex flex-col gap-3`

9. **InspectionRevisitPopup 호출 (라인 2607-2616):** 그대로 (pnr 정합 완료)

10. **isDone 미니 뱃지 (라인 2617-2619):** PowerPanel §A.9 동일 (alert-success + CheckCircle2 14)

11. **결과 picker (라인 2621-2632):** PowerPanel §A.10 동일 — flex-row 가로 1줄 + lucide 16 + text-label

12. **특이사항 + 사진 (라인 2634-2644):** PowerPanel §A.11 동일

13. **submitError / justSaved (라인 2645 / 2646):** PowerPanel §A.12 동일

14. **footer-bar (라인 2652-2661):** PowerPanel §A.13 동일. 단 disabled 조건은 `submitting || photo.uploading || !canSave` (PowerPanel 의 `!selectedCP` 와 의미 동일)
</design_rules_wave6>

<conversion_strategy>
**시안 → TSX 변환 (i4r/sp7/tbj/0l7/1p0 패턴 그대로, Wave 6 영역 한정):**

1. **파일 영역 한정 작업 (1p0 패턴 그대로):**
   - InspectionPage.tsx 5937 줄 중 Wave 6 변환 영역은 라인 2260~2665 부근 (총 약 406줄)
   - **다른 영역(라인 1~2259 / 라인 2667~끝) 은 한 줄도 건드리지 않는다**
   - Edit 단위로 영역별 정밀 교체 — Write 전체 덮어쓰기는 금지(다른 wave 변환 영역 안전 보장)
   - DamperModal (라인 2668 시작 — `function DamperModal({ group, allCheckpoints ...`) 직전까지가 Wave 6 영역 경계
   - **PP_ZONE_PREFIX / PP_ZONE_LABELS / PPZone 타입 (라인 2256~2258) 변경 금지** — 상수 영역

2. **모든 `style={{...}}` 의 금지 키 제거 → Tailwind 또는 토큰 변수 클래스로:**
   - 색: `color:'var(--t1)'` → `className="text-text-primary"`
   - 배경: `background:'var(--bg2)'` → `className="bg-surface-raised"`
   - 테두리: `border:'1px solid var(--bd)'` → `className="border border-border-default"`
   - 패딩: `padding:'10px 16px'` → `className="px-4 py-2.5"`
   - 폰트: `fontSize:13, fontWeight:700` → `className="text-label font-bold"`
   - 11px → 13px (text-label) 또는 12px (text-caption) — 노안 친화
   - 9/10px → 12px (text-caption) — 노안 친화
   - 보존: position / top / left / right / bottom (`var(--sat, 0px)` / NAV_BOTTOM 동적) / transform (slide-up 동적) / transition cubic-bezier / animation / touchAction:'pan-y' (Tailwind `touch-pan-y` 대안 가능) / §6.4 그라디언트 CTA / boxShadow 시그니처 그림자 / 비활성 저장 배경 var(--border-default)

3. **lucide 통일:**
   - `{group.icon}` (⚡) 라인 2345 → `<Zap size={18} className="text-text-secondary flex-shrink-0" />` (이미 import 됨)
   - `{group.icon}` (🚗) 라인 2552 → `<Car size={18} className="text-text-secondary flex-shrink-0" />` (이미 import 됨)
   - 라인 2384 (`‹` 텍스트, fontSize:20) → `<ChevronLeft size={18} className="flex-shrink-0" />` (이미 import 됨)
   - 라인 2392 (`›` 텍스트) → `<ChevronRight size={18} />` (이미 import 됨)
   - 라인 2360, 2569, 2587 (segmented done-mark `✓`): 텍스트 ✓ 유지 (1글자 span — 시안 권위, 1p0 Wave 5 전례)
   - 라인 2417, 2618 (alert-success `✓ 이미 점검 완료`): `<CheckCircle2 size={14} className="text-safe flex-shrink-0">` (이미 import 됨)
   - 라인 2446, 2647 (저장 완료 `✓ 저장 완료`): `<CheckCircle2 size={14} className="flex-shrink-0">` (이미 import 됨)
   - 결과 picker (라인 2425-2428 / 2625-2628 — `{opt.icon} {opt.label}`): **lucide 컴포넌트 분기 헬퍼 도입** (`resultIcon(value)` → CheckCircle2/AlertTriangle/XCircle 16x16) — opt.icon 텍스트 이모지 의존 제거. 각 모달 안에 local helper 정의 (1p0 패턴)

4. **비즈니스 로직 보존 (가장 중요) — 한 줄도 변경 금지:**
   - props 시그니처 두 모달 모두 그대로
   - state 16개 (PowerPanel 8 + Parking 8)
   - useRef 4개 (prevZone / prevIdx / prevItem / prevSub)
   - useEffect 6개 (visible mount × 2 + prevZone reset + prevIdx reset + prevItem reset + prevSub reset) 한 줄도 변경 없음 (eslint-disable-line 포함)
   - useMemo zoneCPs (FLOOR_ORDER + .sort) / cpId (item/subItem 분기) 한 줄도 변경 없음
   - selectedCP / isDone / doneCount / totalCount / canSave / showForm 헬퍼 그대로
   - useInspectionRevisitPopup 호출 (category: '소방용전원공급반' / '주차장비') 그대로
   - **handleSave 한 줄도 변경 금지** — 단일 cp 점검 onSave 호출 (EXCEL-02 자탐 매핑 메모리)
   - **loc-card 좌우 스와이프 onTouchStart/onTouchEnd 한 줄도 변경 금지** (`_swX` / dx > 40 / dx < -40 / setPickerIdx)
   - `await photo.upload()` / PhotoButton 호출 / usePhotoUpload 훅 그대로
   - `navigate('/remediation/' + recordId)` 호출 그대로
   - `setPickerIdx(pickerIdx - 1)` / `setPickerIdx(pickerIdx + 1)` 분기 그대로
   - PP_ZONE_PREFIX[zone].startsWith / PP_ZONE_LABELS[z] / FLOOR_ORDER.indexOf 매핑 그대로
   - INSPECT_RESULT_OPTIONS 순회 그대로 — 단 버튼 스타일은 §10 (가로 픽커) 룰로 교체

5. **증상 피커 미도입 (메모리 룰 강제):**
   - PowerPanelModal / ParkingGateModal 에 symptomPick state 신설 금지
   - 메모 placeholder='특이사항을 입력하세요' 그대로 (자동화 5종 카테고리 아님)

6. **EXCEL-02 자탐 카테고리 매핑 보존 (메모리 룰 강제):**
   - PowerPanelModal.handleSave 의 `await onSave(selectedCP.id, result, memo, photoKey ?? undefined)` 한 줄도 변경 금지
   - cp.category === '소방용전원공급반' 필터 / cp.locationNo.startsWith(PP_ZONE_PREFIX[zone]) 매핑 한 줄도 변경 금지
   - 자탐 엑셀 출력(`src/utils/generateExcel.ts`) 측 데이터 소스 동일성 보장

7. **dead code 정리 (i4r/0l7/1p0 전례):**
   - `zoneBtnStyle` 인라인 헬퍼 함수 (라인 2332-2338) — Tailwind 분기로 교체되면 unused → 제거 OK
   - `btnStyle` 인라인 헬퍼 함수 (라인 2539-2545) — Tailwind 분기로 교체되면 unused → 제거 OK
   - 단 사용처 다 잡아내고 unused 확인 후 제거 (각 모달 안에서만 정의되므로 안전)

8. **헤더 background 시안 정합:**
   - 시안 .mh 는 `var(--bg)` (page), 현재 코드는 `var(--bg2)` (raised) — **시안 권위 따라 `bg-surface-page` 로 통일** (1p0 Wave 5 패턴 동일)
   - 단, .bar-section (구역/항목/문 선택) 은 시안에서 `var(--bg2)` (raised) — `bg-surface-raised` 유지

9. **결과 picker 시각 변경 — opt.icon/opt.color/opt.bg 인라인 의존 제거 (1p0 패턴):**
   - 현재 코드 (라인 2425-2428 / 2625-2628) 는 flex-col + 카드 픽커 (큰 아이콘 20 + 라벨 11) + `opt.color`/`opt.bg` 인라인 색 직접 사용
   - 변환 시: **시안 권위 따라 flex-row 가로 픽커 1줄 (큰 사이즈 16px lucide + text-label) + status 토큰 분기**
   - `resultIcon(value: CheckResult): JSX.Element` 헬퍼 도입 (CheckCircle2/AlertTriangle/XCircle 16x16 반환) + 인라인 클래스 분기 — 각 모달 안에 local 정의
   - opt.icon 텍스트 이모지는 더 이상 표시 안 함 — lucide 컴포넌트로 대체

10. **PWA 캐시 무효화 확인:**
    - InspectionPage.tsx 본체 수정 — Vite 자동 chunk hash 갱신
    - npm run build 후 `dist/assets/InspectionPage-*.js` 파일명 hash 변경 확인 권장 (이전 vs 신규)
</conversion_strategy>

<tasks>

<task type="auto">
  <name>Task 1: PowerPanelModal 변환 (소방용전원공급반 PP-X-Y 단위, 라인 2260~2466, ~206줄)</name>
  <files>cha-bio-safety/src/pages/InspectionPage.tsx</files>
  <action>
**범위:** `function PowerPanelModal({ group, allCheckpoints, records, monthRecords, scheduleItems, onClose, onSave })` 본문 (라인 2260~2466). ParkingGateModal 은 Task 2 — 이 Task 에서 절대 건드리지 않는다.

**시안:** `cha-bio-safety/docs/redesign-context/02-inspection/sketch/inspection-sketch-misc-modals.html` 라인 358~421 (PowerPanelModal Mobile Light 영역) — 권위.

**lucide import 확인 (파일 상단 import 블록, 라인 21~29):**
- Zap (PowerPanel 헤더) — **이미 import 됨 (라인 24)**, 추가 작업 없음
- ChevronLeft / ChevronRight (loc-card nav) — **이미 import 됨 (라인 22)**
- CheckCircle2 / AlertTriangle / XCircle (결과 picker + alert) — **이미 import 됨 (라인 28)**
- **Wave 6 신규 lucide import 0건** — 모든 아이콘이 이미 import 되어 있음. import 블록 손대지 않는다

**비즈니스 보존 (한 줄도 변경 금지) — 절대 룰 (EXCEL-02 자탐 매핑 메모리):**
- props 시그니처 그대로 (group, allCheckpoints, records, monthRecords, scheduleItems, onClose, onSave)
- state 8개 (zone / pickerIdx / result / memo / submitting / justSaved / submitError / visible) 그대로
- useRef 2개 (prevZone / prevIdx) 그대로
- useEffect 3개 (rAF mount + prevZone reset + prevIdx reset, eslint-disable-line 포함) 한 줄도 변경 없음
- useMemo zoneCPs (FLOOR_ORDER 배열 + .filter(cp => cp.category === '소방용전원공급반' && cp.locationNo?.startsWith(PP_ZONE_PREFIX[zone])) + .sort) 한 줄도 변경 없음 — **EXCEL-02 자탐 매핑 데이터 흐름 영향 0 보장**
- selectedCP / isDone / doneCount / totalCount 헬퍼 그대로
- useInspectionRevisitPopup({ checkpointId: selectedCP?.id ?? null, category: '소방용전원공급반', monthRecords, scheduleItems }) 그대로
- **handleSave 본문 한 줄도 변경 금지 — EXCEL-02 자탐 매핑 메모리 룰:**
  - `if (!selectedCP) return` 그대로
  - `setSubmitting(true); setSubmitError(null)` 그대로
  - `const photoKey = await photo.upload()` 그대로
  - `await onSave(selectedCP.id, result, memo, photoKey ?? undefined)` 한 줄도 변경 금지 (자탐 엑셀 출력 측 데이터 소스)
  - try/catch/finally 본문 그대로
- **loc-card 좌우 스와이프 onTouchStart/onTouchEnd 한 줄도 변경 금지** (`_swX` / dx > 40 / dx < -40 / setPickerIdx 분기)
- usePhotoUpload() / PhotoButton 호출 시그니처 그대로 (`<PhotoButton hook={photo} label="촬영" noCapture />`)
- `navigate('/remediation/' + recordId)` (라인 2413) 그대로

**시각 변환 (v0.1.1 토큰 + Tailwind):**

§A.1~A.13 의 design_rules_wave6 룰을 라인 단위로 정밀 적용:

1. **외곽 모달 (라인 2341):**
   - `className="fixed left-0 right-0 z-[99] flex flex-col overflow-hidden bg-surface-page"`
   - 인라인 (화이트리스트): `style={{ top:'var(--sat, 0px)', bottom: NAV_BOTTOM, transform: visible ? 'translateY(0)' : 'translateY(100%)', transition:'transform 0.26s cubic-bezier(0.32,0.72,0,1)' }}`

2. **헤더 (라인 2344):**
   - `className="flex items-center px-4 py-2.5 bg-surface-page border-b border-border-default flex-shrink-0 gap-2.5"`
   - 좌측 아이콘: `<Zap size={18} className="text-text-secondary flex-shrink-0" />` ({group.icon} ⚡ 제거)
   - 우측 텍스트: `<div className="flex-1 min-w-0"><div className="text-[16px] font-bold text-text-primary">{group.labels[0]}</div></div>`

3. **구역 선택 (라인 2352-2365):**
   - 외곽 div: `bg-surface-raised border-b border-border-default px-3.5 py-2 flex-shrink-0`
   - 라벨: `text-caption font-semibold text-text-tertiary mb-1.5 tracking-wider`
   - 버튼 컨테이너: `flex gap-1.5`
   - 버튼 분기 (§A.3 활성 우선 분기 — zone === z 활성 / allDone done / 기본 비활성):
     ```tsx
     const zCPs = allCheckpoints.filter(cp => cp.category === '소방용전원공급반' && cp.locationNo?.startsWith(PP_ZONE_PREFIX[z]))
     const allDone = zCPs.length > 0 && zCPs.every(cp => records[cp.id])
     const isActive = zone === z
     const baseCls = 'flex-1 basis-0 min-w-0 px-2 py-2 rounded-sm text-label font-bold cursor-pointer whitespace-nowrap transition-colors inline-flex items-center justify-center'
     const stateCls = isActive ? 'border-[1.5px] border-accent bg-accent text-text-on-accent'
                    : allDone   ? 'border-[1.5px] border-safe-bar bg-safe-bg text-safe'
                    :             'border border-border-strong bg-surface-page text-text-secondary'
     ```
   - done-mark: `{!isActive && allDone && <span className="text-caption ml-1 opacity-85">✓</span>}` (라인 2360 의 fontSize:10 → text-caption)
   - **`zoneBtnStyle` 인라인 헬퍼 (라인 2332-2338) 제거**

4. **개소 선택 loc-card (라인 2368-2395 — `zone && zoneCPs.length >= 1` 조건부):**
   - 외곽 wrapper (라인 2369): `bg-surface-raised border-b border-border-default px-3.5 pt-2.5 pb-2 flex-shrink-0`
   - loc-card 내부 (라인 2370): `className="bg-surface-page border border-border-default rounded-md px-3 py-2.5 flex items-center gap-2.5 touch-pan-y"` (`touchAction:'pan-y'` 인라인 → Tailwind `touch-pan-y` 클래스 우선)
   - `onTouchStart` / `onTouchEnd` 핸들러 본문 **한 줄도 변경 금지** (`_swX` / dx 분기)
   - 좌 nav 버튼 (라인 2381-2384):
     ```tsx
     const canLeft = pickerIdx > 0
     <button
       onClick={() => { if (canLeft) setPickerIdx(pickerIdx - 1) }}
       disabled={!canLeft}
       className={`w-9 h-9 rounded-sm border border-border-default bg-surface-page flex items-center justify-center flex-shrink-0 transition-opacity ${canLeft ? 'cursor-pointer text-text-primary opacity-100' : 'cursor-default text-text-tertiary opacity-30'}`}
     >
       <ChevronLeft size={18} className="flex-shrink-0" />
     </button>
     ```
   - loc-info (라인 2385-2388):
     ```tsx
     <div className="flex-1 text-center min-w-0">
       <div className="text-caption text-text-tertiary font-semibold">개소 ({pickerIdx + 1}/{totalCount}) · {doneCount}/{totalCount} 완료</div>
       <div className="text-[15px] font-bold text-text-primary mt-0.5 truncate">{selectedCP?.location ?? ''}</div>
     </div>
     ```
   - 우 nav 버튼 (라인 2389-2392): 좌 nav 와 동일 패턴 + `pickerIdx < zoneCPs.length - 1` 분기 + `<ChevronRight size={18}>`

5. **modal-body 영역 (라인 2398):**
   - `flex-1 overflow-y-auto px-3.5 py-3 flex flex-col gap-3 relative`

6. **!zone empty state (라인 2399-2401):**
   - `flex-1 flex items-center justify-center text-text-tertiary text-label` "구역을 선택해 주세요"

7. **selectedCP wrapper (라인 2404):**
   - `relative flex flex-col gap-3` (인라인 position/display/flex 제거)

8. **InspectionRevisitPopup (라인 2406-2415):** 그대로 (pnr 정합 완료)

9. **isDone 미니 뱃지 (라인 2416-2418):**
   - `bg-safe-bg border border-safe-bar rounded-sm px-3 py-2 text-label font-semibold text-safe inline-flex items-center gap-1.5`
   - 좌측 `<CheckCircle2 size={14} className="text-safe flex-shrink-0" />` (✓ 이모지 제거)
   - 텍스트: "이미 점검 완료된 항목입니다"

10. **결과 picker (라인 2420-2431) — 시안 권위 따라 가로 1줄 변환:**
    - 라벨: `text-caption font-semibold text-text-tertiary mb-1.5 tracking-wider` "점검 결과"
    - 컨테이너: `flex gap-1.5`
    - 버튼 (§A.10 룰):
      - 기본: `flex-1 py-2.5 rounded-md border-[1.5px] inline-flex items-center justify-center gap-1.5 text-label font-bold cursor-pointer whitespace-nowrap transition-colors`
      - 비활성: `border-border-default bg-surface-raised text-text-tertiary`
      - 활성(normal): `border-safe-bar bg-safe-bg text-safe`
      - 활성(caution): `border-warning-bar bg-warning-bg text-warning`
      - 활성(bad): `border-danger-bar bg-danger-bg text-danger`
    - lucide 16x16 분기 헬퍼 (PowerPanelModal 안 local 정의):
      ```tsx
      const resultIcon = (v: CheckResult) => {
        if (v === 'normal')  return <CheckCircle2 size={16} className="flex-shrink-0" />
        if (v === 'caution') return <AlertTriangle size={16} className="flex-shrink-0" />
        return <XCircle size={16} className="flex-shrink-0" />
      }
      ```
    - 버튼 내용: `{resultIcon(opt.value)}<span>{opt.label}</span>` (opt.icon 텍스트 이모지 제거)
    - **현재 코드 flex-col 카드 + 큰 아이콘 20 + 라벨 11 → 시안 권위 따라 flex-row 가로 픽커 + 아이콘 16 + 라벨 13 로 통일**

11. **특이사항 + 사진 (라인 2434-2443):**
    - 라벨 row: `flex items-center justify-between mb-1.5`
      - 좌 label: `text-caption font-semibold text-text-tertiary tracking-wider` "특이사항 (선택)"
      - 우 span: `text-caption text-text-tertiary` "점검 사진 (선택)"
    - memo-row: `flex gap-2 items-start`
    - textarea: `flex-1 h-[72px] px-3 py-2.5 rounded-md bg-surface-raised border border-border-default text-text-primary text-label resize-none outline-none box-border font-sans placeholder:text-text-tertiary`
      - **placeholder="특이사항을 입력하세요" 그대로 — 증상 피커 미도입 룰**
    - PhotoButton 그대로

12. **submitError / justSaved (라인 2444 / 2445):**
    - submitError: `bg-danger-bg border border-danger-bar text-danger rounded-sm px-3 py-2 text-label font-semibold`
    - justSaved: `bg-safe-bg border border-safe-bar text-safe rounded-sm px-3 py-2 text-label font-semibold inline-flex items-center gap-1.5` + 좌측 `<CheckCircle2 size={14} className="flex-shrink-0" />` (✓ 이모지 제거) + "저장 완료"

13. **footer-bar (라인 2452-2461):**
    - 컨테이너: `flex gap-2 px-3.5 pt-2.5 pb-3 bg-surface-raised border-t border-border-default flex-shrink-0`
    - btn-close-mini: `px-4 py-3 rounded-md bg-surface-page border border-border-strong text-text-secondary text-label font-semibold cursor-pointer transition-colors`
    - btn-save 클래스: `flex-1 py-3.5 rounded-md text-body font-bold border-0 transition-all`
    - btn-save 인라인 (§6.4 그라디언트 화이트리스트):
      ```tsx
      style={{
        background: (submitting || photo.uploading || !selectedCP) ? 'var(--border-default)' : 'linear-gradient(135deg,#1d4ed8,#0ea5e9)',
        color: (submitting || photo.uploading || !selectedCP) ? 'var(--text-tertiary)' : '#fff',
        cursor: (submitting || photo.uploading || !selectedCP) ? 'default' : 'pointer',
        boxShadow: (submitting || photo.uploading || !selectedCP) ? 'none' : '0 4px 14px rgba(37,99,235,0.35)',
      }}
      ```
    - 저장 라벨 분기 그대로 (`photo.uploading ? '사진 업로드 중...' : submitting ? '저장 중...' : '점검 기록 저장'`)

**커밋 메시지 (커밋 1):** `feat(260515-2r5): task 1 — PowerPanelModal v0.1.1 토큰 + Tailwind 변환`
  </action>
  <verify>
    <automated>
cd /Users/jykevin/Documents/cbc7119-design/cha-bio-safety &&
# 1. PowerPanel 영역 옛 토큰 0건 (라인 2260~2465 부근)
sed -n '2260,2465p' src/pages/InspectionPage.tsx | grep -E "var\(--bg2?\)|var\(--bd2?\)|var\(--acl\)|var\(--t[123]\)|var\(--safe\)|var\(--warn\)|var\(--danger\)|var\(--fire\)" | grep -v "var(--text-tertiary)" | grep -v "var(--border-default)" | grep -v "var(--border-strong)" | wc -l | tr -d ' ' | grep -q '^0$' &&
# 2. PowerPanel 영역 9/10/11px 0건
sed -n '2260,2465p' src/pages/InspectionPage.tsx | grep -E "fontSize:\s*(9|10|11)[^0-9]|text-\[(9|10|11)px\]" | wc -l | tr -d ' ' | grep -q '^0$' &&
# 3. PowerPanel 영역 이모지(⚡/📷/✅/‹/›) 0건 (segmented done-mark ✓ 텍스트 1글자만 허용)
sed -n '2260,2465p' src/pages/InspectionPage.tsx | grep -E "⚡|📷|📹|✅|🚨" | wc -l | tr -d ' ' | grep -q '^0$' &&
# 4. EXCEL-02 자탐 매핑 보존 — onSave(selectedCP.id, ...) 호출 1건
sed -n '2260,2465p' src/pages/InspectionPage.tsx | grep -cE "onSave\(selectedCP\.id,\s*result,\s*memo,\s*photoKey\s*\?\?\s*undefined\)" | grep -q '^1$' &&
# 5. EXCEL-02 자탐 매핑 보존 — cp.category === '소방용전원공급반' 필터 ≥2 (zoneCPs useMemo + zone 매핑)
sed -n '2260,2465p' src/pages/InspectionPage.tsx | grep -cE "cp\.category\s*===\s*'소방용전원공급반'" | awk '$1>=2' | grep -q . &&
# 6. PP_ZONE_PREFIX / PP_ZONE_LABELS 사용 보존
sed -n '2260,2465p' src/pages/InspectionPage.tsx | grep -cE "PP_ZONE_PREFIX|PP_ZONE_LABELS" | awk '$1>=3' | grep -q . &&
# 7. loc-card 좌우 스와이프 onTouchStart/onTouchEnd 보존
sed -n '2260,2465p' src/pages/InspectionPage.tsx | grep -cE "onTouchStart|onTouchEnd|_swX" | awk '$1>=3' | grep -q . &&
# 8. lucide Zap/ChevronLeft/ChevronRight/CheckCircle2 사용 ≥4
sed -n '2260,2465p' src/pages/InspectionPage.tsx | grep -cE "<(Zap|ChevronLeft|ChevronRight|CheckCircle2|AlertTriangle|XCircle)\s+size=" | awk '$1>=4' | grep -q . &&
# 9. useInspectionRevisitPopup category 보존
sed -n '2260,2465p' src/pages/InspectionPage.tsx | grep -cE "category:\s*'소방용전원공급반'" | grep -q '^1$' &&
# 10. symptomPick 신설 0건
sed -n '2260,2465p' src/pages/InspectionPage.tsx | grep -cE "symptomPick" | grep -q '^0$' &&
# 11. TypeScript compile + build
npx tsc --noEmit 2>&1 | tail -5 &&
npm run build 2>&1 | tail -8
    </automated>
  </verify>
  <done>
- PowerPanelModal 본문(라인 2260~약 2470, line shift OK) 의 모든 인라인 style 금지 키(color/background/padding/margin/fontSize/fontWeight/borderRadius/display/flex*/grid*/gap/width/height) 제거 — 화이트리스트(position/top/bottom/transform/transition/touchAction[Tailwind 가능]/§6.4 그라디언트/boxShadow/var(--border-default) 비활성 배경)만 남음
- 옛 토큰(var(--bg/bg2/bd/bd2/acl/t1~3/safe/warn/danger/fire)) 0건 (변환 영역 내)
- 9/10/11px 폰트 사이즈 0건
- 이모지(⚡/📷/✅/‹/›) 0건 — segmented done-mark `✓` 텍스트 1글자 span 만 허용
- Zap (헤더) + ChevronLeft/ChevronRight (loc-card nav) + CheckCircle2/AlertTriangle/XCircle (결과 picker + alert) lucide 사용 확인
- 결과 picker §A.10: flex-row 가로 1줄 + 16px lucide + text-label + status outline+tinted bg
- **EXCEL-02 자탐 카테고리 매핑 보존:** `onSave(selectedCP.id, result, memo, photoKey ?? undefined)` 한 줄도 변경 없음 / `cp.category === '소방용전원공급반'` 필터 한 줄도 변경 없음 / FLOOR_ORDER + .sort 한 줄도 변경 없음
- 좌우 스와이프 onTouchStart/onTouchEnd 본문 한 줄도 변경 없음 (`_swX` / dx > 40 / dx < -40)
- useInspectionRevisitPopup category: '소방용전원공급반' 호출 한 줄도 변경 없음
- 증상 피커 미도입 룰 강제 — symptomPick 신설 0건
- ParkingGateModal (라인 2467~) 본문은 한 줄도 변경 없음 — line shift 만 허용 (PowerPanel 변환으로 인한 자연스러운 +/-)
- DamperModal (라인 2668~) 본문은 한 줄도 변경 없음
- Wave 1~5 변환 영역 + 그 외 모든 영역 한 줄도 변경 없음
- TypeScript 컴파일 0 에러
- npm run build 통과 (chunk hash 자동 변경 — InspectionPage.tsx 본체 수정)
- 단일 commit (`feat(260515-2r5): task 1 — PowerPanelModal v0.1.1 토큰 + Tailwind 변환`)
  </done>
</task>

<task type="auto">
  <name>Task 2: ParkingGateModal 변환 (주차장비/회전문 — 북문/남문, 라인 2467~2667, ~200줄)</name>
  <files>cha-bio-safety/src/pages/InspectionPage.tsx</files>
  <action>
**범위:** `function ParkingGateModal({ group, allCheckpoints, records, monthRecords, scheduleItems, onClose, onSave })` 본문 (라인 2467~2667 부근, Task 1 으로 인한 line shift 반영된 새 라인 사용). PowerPanelModal 은 Task 1 — 이 Task 에서 절대 건드리지 않는다.

**시안:** `cha-bio-safety/docs/redesign-context/02-inspection/sketch/inspection-sketch-misc-modals.html` 라인 295~355 (ParkingModal Mobile Dark 영역) — 권위.

**lucide import 확인:** Task 1 에서 모든 아이콘이 이미 import 됨. **Wave 6 신규 lucide import 0건** — import 블록 손대지 않는다.

**비즈니스 보존 (한 줄도 변경 금지) — 절대 룰:**
- props 시그니처 그대로 (group, allCheckpoints, records, monthRecords, scheduleItems, onClose, onSave)
- state 8개 (item / subItem / result / memo / submitting / justSaved / submitError / visible) 그대로
- useRef 2개 (prevItem / prevSub) 그대로
- useEffect 3개 (rAF mount + prevItem reset + prevSub reset, eslint-disable-line 포함) 한 줄도 변경 없음
- useMemo cpId (item === '주차장비' / item === '회전문' && subItem 분기) 한 줄도 변경 없음 — `cp.category === '회전문' && cp.location === subItem` 매핑 보존 (북문/남문 키)
- isDone / canSave / showForm 헬퍼 그대로 (`showForm = item === '주차장비' || (item === '회전문' && !!subItem)`)
- useInspectionRevisitPopup({ checkpointId: cpId, category: '주차장비', monthRecords, scheduleItems }) 그대로 — **회전문도 주 카테고리 '주차장비' 기준 변경 금지**
- **handleSave 본문 한 줄도 변경 금지:**
  - `if (!cpId) return` 그대로
  - `const photoKey = await photo.upload()` 그대로
  - `await onSave(cpId, result, memo, photoKey ?? undefined)` 그대로
  - try/catch/finally 본문 그대로
- usePhotoUpload() / PhotoButton 호출 시그니처 그대로 (`<PhotoButton hook={photo} label="촬영" noCapture />`)
- `navigate('/remediation/' + recordId)` (라인 2614) 그대로
- `allCheckpoints.find(cp => cp.category === '주차장비')` / `allCheckpoints.find(cp => cp.category === '회전문' && cp.location === subItem)` 매핑 그대로
- `(['주차장비','회전문'] as const).map(label => ...)` / `(['북문','남문'] as const).map(door => ...)` 순회 그대로

**시각 변환 (v0.1.1 토큰 + Tailwind):**

§B.1~B.14 의 design_rules_wave6 룰을 라인 단위로 정밀 적용:

1. **외곽 모달 (라인 2548):**
   - `className="fixed left-0 right-0 z-[99] flex flex-col overflow-hidden bg-surface-page"`
   - 인라인 (화이트리스트): `style={{ top:'var(--sat, 0px)', bottom: NAV_BOTTOM, transform: visible ? 'translateY(0)' : 'translateY(100%)', transition:'transform 0.26s cubic-bezier(0.32,0.72,0,1)' }}`

2. **헤더 (라인 2551):**
   - `className="flex items-center px-4 py-2.5 bg-surface-page border-b border-border-default flex-shrink-0 gap-2.5"`
   - 좌측 아이콘: `<Car size={18} className="text-text-secondary flex-shrink-0" />` ({group.icon} 🚗 제거)
   - 우측 텍스트:
     ```tsx
     <div className="flex-1 min-w-0">
       <div className="text-[16px] font-bold text-text-primary">{group.labels[0]}</div>
       {group.labels.length > 1 && (
         <div className="text-caption text-text-tertiary mt-0.5">· {group.labels.slice(1).join(' · ')}</div>
       )}
     </div>
     ```

3. **항목 선택 (라인 2560-2574):**
   - 외곽 div: `bg-surface-raised border-b border-border-default px-3.5 py-2 flex-shrink-0`
   - 라벨: `text-caption font-semibold text-text-tertiary mb-1.5 tracking-wider` "항목 선택"
   - 버튼 컨테이너: `flex gap-1.5`
   - 버튼 분기 (PowerPanel §A.3 segmented 패턴 동일):
     ```tsx
     {(['주차장비','회전문'] as const).map(label => {
       const catCPs  = allCheckpoints.filter(cp => cp.category === label)
       const allDone = catCPs.length > 0 && catCPs.every(cp => records[cp.id])
       const isActive = item === label
       const baseCls = 'flex-1 basis-0 min-w-0 px-2 py-2 rounded-sm text-label font-bold cursor-pointer whitespace-nowrap transition-colors inline-flex items-center justify-center'
       const stateCls = isActive ? 'border-[1.5px] border-accent bg-accent text-text-on-accent'
                      : allDone   ? 'border-[1.5px] border-safe-bar bg-safe-bg text-safe'
                      :             'border border-border-strong bg-surface-page text-text-secondary'
       return (
         <button key={label} onClick={() => setItem(label)} className={`${baseCls} ${stateCls}`}>
           {label}{!isActive && allDone && <span className="text-caption ml-1 opacity-85">✓</span>}
         </button>
       )
     })}
     ```
   - **`btnStyle` 인라인 헬퍼 (라인 2539-2545) 제거**

4. **회전문 선택 시 문 segmented (라인 2577-2593 — `item === '회전문'` 조건부):**
   - 외곽 div: `bg-surface-raised border-b border-border-default px-3.5 py-2 flex-shrink-0`
   - 라벨: `text-caption font-semibold text-text-tertiary mb-1.5 tracking-wider` "문 선택"
   - 버튼 컨테이너: `flex gap-1.5`
   - 버튼 분기 (북문/남문 매핑):
     ```tsx
     {(['북문','남문'] as const).map(door => {
       const doorCP   = allCheckpoints.find(cp => cp.category === '회전문' && cp.location === door)
       const doneDoor = doorCP ? !!records[doorCP.id] : false
       const isActive = subItem === door
       const baseCls = 'flex-1 basis-0 min-w-0 px-2 py-2 rounded-sm text-label font-bold cursor-pointer whitespace-nowrap transition-colors inline-flex items-center justify-center'
       const stateCls = isActive ? 'border-[1.5px] border-accent bg-accent text-text-on-accent'
                      : doneDoor  ? 'border-[1.5px] border-safe-bar bg-safe-bg text-safe'
                      :             'border border-border-strong bg-surface-page text-text-secondary'
       return (
         <button key={door} onClick={() => setSubItem(door)} className={`${baseCls} ${stateCls}`}>
           {door}{!isActive && doneDoor && <span className="text-caption ml-1 opacity-85">✓</span>}
         </button>
       )
     })}
     ```

5. **modal-body 영역 (라인 2596):**
   - `flex-1 overflow-y-auto px-3.5 py-3 flex flex-col gap-3 relative`

6. **!item empty state (라인 2597-2599):**
   - `flex-1 flex items-center justify-center text-text-tertiary text-label` "항목을 선택해 주세요"

7. **`item === '회전문' && !subItem` empty state (라인 2600-2602):**
   - `flex-1 flex items-center justify-center text-text-tertiary text-label` "북문 또는 남문을 선택해 주세요"

8. **showForm wrapper (라인 2605):**
   - `relative flex flex-col gap-3` (인라인 position/display/flex 제거)

9. **InspectionRevisitPopup (라인 2607-2616):** 그대로 (pnr 정합 완료)

10. **isDone 미니 뱃지 (라인 2617-2619):**
    - `bg-safe-bg border border-safe-bar rounded-sm px-3 py-2 text-label font-semibold text-safe inline-flex items-center gap-1.5`
    - 좌측 `<CheckCircle2 size={14} className="text-safe flex-shrink-0" />` + "이미 점검 완료된 항목입니다"

11. **결과 picker (라인 2621-2632) — 시안 권위 따라 가로 1줄 변환 (Task 1 §A.10 동일):**
    - 라벨: `text-caption font-semibold text-text-tertiary mb-1.5 tracking-wider` "점검 결과"
    - 컨테이너: `flex gap-1.5`
    - 버튼 (§A.10 룰):
      - 기본: `flex-1 py-2.5 rounded-md border-[1.5px] inline-flex items-center justify-center gap-1.5 text-label font-bold cursor-pointer whitespace-nowrap transition-colors`
      - 비활성/활성 분기: §A.10 동일
    - lucide 16x16 분기 헬퍼 (ParkingGateModal 안 local 정의 — Task 1 의 resultIcon 과 독립적으로 재정의):
      ```tsx
      const resultIcon = (v: CheckResult) => {
        if (v === 'normal')  return <CheckCircle2 size={16} className="flex-shrink-0" />
        if (v === 'caution') return <AlertTriangle size={16} className="flex-shrink-0" />
        return <XCircle size={16} className="flex-shrink-0" />
      }
      ```
    - 버튼 내용: `{resultIcon(opt.value)}<span>{opt.label}</span>` (opt.icon 텍스트 이모지 제거)

12. **특이사항 + 사진 (라인 2634-2644):** Task 1 §A.11 동일

13. **submitError / justSaved (라인 2645 / 2646):** Task 1 §A.12 동일 (`<CheckCircle2 size={14}>` + "저장 완료")

14. **footer-bar (라인 2652-2661):** Task 1 §A.13 동일. **disabled 조건은 `submitting || photo.uploading || !canSave`** (PowerPanel 의 `!selectedCP` 와 의미 동일 — 변경 금지)
    - btn-save 인라인:
      ```tsx
      style={{
        background: (submitting || photo.uploading || !canSave) ? 'var(--border-default)' : 'linear-gradient(135deg,#1d4ed8,#0ea5e9)',
        color: (submitting || photo.uploading || !canSave) ? 'var(--text-tertiary)' : '#fff',
        cursor: (submitting || photo.uploading || !canSave) ? 'default' : 'pointer',
        boxShadow: (submitting || photo.uploading || !canSave) ? 'none' : '0 4px 14px rgba(37,99,235,0.35)',
      }}
      ```

**커밋 메시지 (커밋 2):** `feat(260515-2r5): task 2 — ParkingGateModal v0.1.1 토큰 + Tailwind 변환 (Wave 6 마지막)`
  </action>
  <verify>
    <automated>
cd /Users/jykevin/Documents/cbc7119-design/cha-bio-safety &&
# 1. ParkingGate 영역 옛 토큰 0건 (function ParkingGateModal ~ function DamperModal 사이)
PG_START=$(grep -n "^function ParkingGateModal" src/pages/InspectionPage.tsx | cut -d: -f1) &&
PG_END=$(grep -n "^function DamperModal" src/pages/InspectionPage.tsx | cut -d: -f1) &&
sed -n "${PG_START},${PG_END}p" src/pages/InspectionPage.tsx | grep -E "var\(--bg2?\)|var\(--bd2?\)|var\(--acl\)|var\(--t[123]\)|var\(--safe\)|var\(--warn\)|var\(--danger\)|var\(--fire\)" | grep -v "var(--text-tertiary)" | grep -v "var(--border-default)" | grep -v "var(--border-strong)" | wc -l | tr -d ' ' | grep -q '^0$' &&
# 2. ParkingGate 영역 9/10/11px 0건
sed -n "${PG_START},${PG_END}p" src/pages/InspectionPage.tsx | grep -E "fontSize:\s*(9|10|11)[^0-9]|text-\[(9|10|11)px\]" | wc -l | tr -d ' ' | grep -q '^0$' &&
# 3. ParkingGate 영역 이모지 0건
sed -n "${PG_START},${PG_END}p" src/pages/InspectionPage.tsx | grep -E "🚗|📷|📹|✅|🚨" | wc -l | tr -d ' ' | grep -q '^0$' &&
# 4. handleSave 보존 — onSave(cpId, ...) 1건
sed -n "${PG_START},${PG_END}p" src/pages/InspectionPage.tsx | grep -cE "onSave\(cpId,\s*result,\s*memo,\s*photoKey\s*\?\?\s*undefined\)" | grep -q '^1$' &&
# 5. cpId useMemo 분기 보존 — cp.category === '주차장비' / cp.category === '회전문' && cp.location === subItem
sed -n "${PG_START},${PG_END}p" src/pages/InspectionPage.tsx | grep -cE "cp\.category\s*===\s*'주차장비'" | awk '$1>=1' | grep -q . &&
sed -n "${PG_START},${PG_END}p" src/pages/InspectionPage.tsx | grep -cE "cp\.category\s*===\s*'회전문'\s*&&\s*cp\.location\s*===\s*subItem" | grep -q '^1$' &&
# 6. 북문/남문 매핑 보존
sed -n "${PG_START},${PG_END}p" src/pages/InspectionPage.tsx | grep -cE "'북문'.*'남문'|북문|남문" | awk '$1>=2' | grep -q . &&
# 7. lucide Car/CheckCircle2/AlertTriangle/XCircle 사용 ≥3
sed -n "${PG_START},${PG_END}p" src/pages/InspectionPage.tsx | grep -cE "<(Car|CheckCircle2|AlertTriangle|XCircle)\s+size=" | awk '$1>=3' | grep -q . &&
# 8. useInspectionRevisitPopup category: '주차장비' 보존
sed -n "${PG_START},${PG_END}p" src/pages/InspectionPage.tsx | grep -cE "category:\s*'주차장비'" | grep -q '^1$' &&
# 9. showForm 보존
sed -n "${PG_START},${PG_END}p" src/pages/InspectionPage.tsx | grep -cE "showForm\s*=\s*item\s*===\s*'주차장비'\s*\|\|\s*\(item\s*===\s*'회전문'\s*&&\s*!!subItem\)" | grep -q '^1$' &&
# 10. symptomPick 신설 0건
sed -n "${PG_START},${PG_END}p" src/pages/InspectionPage.tsx | grep -cE "symptomPick" | grep -q '^0$' &&
# 11. DamperModal 본문 보존 (Task 1+2 line shift 만 허용, 본문 변경 0)
git diff HEAD~2 -- src/pages/InspectionPage.tsx 2>/dev/null | grep -E "^\+|^-" | grep -E "DamperModal|StairwellModal\b|CctvModal\b|DivModal\b|CompressorModal|BaeyeonModal\b|FireAlarmModal" | grep -v "^+++\|^---" | head -5 &&
# 12. TypeScript compile + build
npx tsc --noEmit 2>&1 | tail -5 &&
npm run build 2>&1 | tail -8
    </automated>
  </verify>
  <done>
- ParkingGateModal 본문(라인 ~2467~2667, Task 1 line shift 반영) 의 모든 인라인 style 금지 키 제거 — 화이트리스트만 남음
- 옛 토큰(var(--bg/bg2/bd/bd2/acl/t1~3/safe/warn/danger/fire)) 0건 (변환 영역 내)
- 9/10/11px 폰트 사이즈 0건
- 이모지(🚗/📷/✅/✓ 제외 1글자 segmented done-mark) 0건
- Car (헤더) + CheckCircle2/AlertTriangle/XCircle (결과 picker + alert) lucide 사용 확인
- 항목/문 segmented (활성 우선 분기) + 회전문 시 조건부 문 segmented + showForm 분기 + empty state 2종
- 결과 picker §B.11 (Task 1 §A.10 동일): flex-row 가로 1줄 + 16px lucide + text-label
- **비즈니스 보존:** `cpId` useMemo + `cp.category === '회전문' && cp.location === subItem` 매핑 한 줄도 변경 없음 / `onSave(cpId, ...)` 호출 한 줄도 변경 없음 / `useInspectionRevisitPopup({ category: '주차장비' })` 한 줄도 변경 없음 / `showForm = item === '주차장비' || (item === '회전문' && !!subItem)` 한 줄도 변경 없음
- 증상 피커 미도입 룰 강제 — symptomPick 신설 0건
- DamperModal (라인 2668~) 본문 한 줄도 변경 없음 — line shift 만 허용
- Wave 1~5 변환 영역 + 그 외 모든 영역 한 줄도 변경 없음
- PowerPanelModal 본문 한 줄도 변경 없음 (Task 1 결과 보존)
- TypeScript 컴파일 0 에러
- npm run build 통과 (chunk hash 자동 변경)
- 단일 commit (`feat(260515-2r5): task 2 — ParkingGateModal v0.1.1 토큰 + Tailwind 변환 (Wave 6 마지막)`)
- **Wave 6 완료 = InspectionPage TSX 전 변환 완료 (모든 모달 v0.1.1 정합)**
  </done>
</task>

</tasks>

<verification>
**InspectionPage.tsx 전체 검증 (Wave 6 완료 후):**

1. **Wave 6 변환 영역 정합 (PowerPanel + ParkingGate):**
   - PowerPanelModal/ParkingGateModal 본문 안 옛 토큰 0건 (var(--bg/bg2/bd/bd2/acl/t1~3/safe/warn/danger/fire)) — 화이트리스트 비활성 저장 인라인은 var(--border-default)/var(--text-tertiary) 만 허용
   - 9/10/11px 폰트 사이즈 0건
   - 이모지(⚡/🚗/📷/📹/✅) 0건 — segmented done-mark `✓` 텍스트 1글자 span 4건 (PowerPanel 1 + Parking 항목 2 + Parking 문 2 — 코드 분기당 1개씩, 활성 시 제외) — 정확히 ≥1 ≤4 범위 OK
   - 인라인 style 금지 키(color/background/padding/margin/fontSize/fontWeight/borderRadius/display/flex*/grid*/gap/width/height) 0건
   - 화이트리스트 인라인: position/top/bottom (NAV_BOTTOM/var(--sat,0px)) / transform (translateY 동적) / transition cubic-bezier / §6.4 그라디언트 CTA + boxShadow 시그니처 그림자 / 비활성 저장 var(--border-default) — 모두 정당화 가능

2. **비즈니스 로직 보존:**
   - PowerPanel `await onSave(selectedCP.id, result, memo, photoKey ?? undefined)` 1건 — **EXCEL-02 자탐 매핑 메모리 룰**
   - Parking `await onSave(cpId, result, memo, photoKey ?? undefined)` 1건
   - PowerPanel `cp.category === '소방용전원공급반' && cp.locationNo?.startsWith(PP_ZONE_PREFIX[zone])` 매핑 ≥2 (zoneCPs useMemo + zone segmented)
   - Parking `cp.category === '회전문' && cp.location === subItem` 매핑 1건 (cpId useMemo)
   - PowerPanel onTouchStart/onTouchEnd (`_swX` / dx > 40 / dx < -40) 보존
   - PowerPanel `useInspectionRevisitPopup({ category: '소방용전원공급반' })` 1건
   - Parking `useInspectionRevisitPopup({ category: '주차장비' })` 1건
   - Parking `showForm = item === '주차장비' || (item === '회전문' && !!subItem)` 1건
   - **symptomPick 신설 0건** (자동화 5종 아님 — 메모리 룰)

3. **다른 영역 비변경 확인:**
   - Wave 1~5 변환 영역(라인 1~2259) + DamperModal (라인 2668~) + 그 외 모든 영역 한 줄도 변경 없음
   - 라인 shift 만 허용 (Task 1+2 변환으로 인한 자연스러운 +/-)
   - PP_ZONE_PREFIX / PP_ZONE_LABELS / PPZone 타입 (라인 2256~2258) 한 줄도 변경 없음
   - STAIRWELLS / CCTV_DVRS / CATEGORY_GROUPS / INSPECT_RESULT_OPTIONS 상수 한 줄도 변경 없음

4. **빌드 검증:**
   - `npx tsc --noEmit` → 0 에러
   - `npm run build` → 통과
   - `dist/assets/InspectionPage-*.js` chunk hash 자동 변경 (PWA 캐시 무효화)

5. **시안 권위 정합 (`inspection-sketch-misc-modals.html`):**
   - PowerPanelModal 라인 358~421 매핑: 헤더 .mh (Zap + 16/700) + .bar-section "구역 선택" + .loc-card (좌우 ChevronLeft/Right + .loc-cap + .loc-title 15/700) + .result-picker (가로 1줄 + 16px lucide + text-label) + .memo-row + .footer-bar (§6.4 그라디언트 CTA)
   - ParkingGateModal 라인 295~355 매핑: 헤더 .mh (Car + 16/700 + sub) + .bar-section "항목 선택" + .bar-section "문 선택" (회전문 시 조건부) + .result-picker (가로 1줄) + .memo-row + .footer-bar
</verification>

<success_criteria>
**Wave 6 (마지막) 완료 조건 — 모두 만족해야 함:**

- [ ] PowerPanelModal 본문 v0.1.1 토큰 + Tailwind 변환 완료 (Task 1)
- [ ] ParkingGateModal 본문 v0.1.1 토큰 + Tailwind 변환 완료 (Task 2)
- [ ] 변환 영역 내 옛 토큰(var(--bg/bg2/bd/bd2/acl/t1~3/safe/warn/danger/fire)) 0건 (화이트리스트 var(--border-default)/var(--text-tertiary) 비활성 저장 인라인 제외)
- [ ] 변환 영역 내 9/10/11px 폰트 사이즈 0건
- [ ] 변환 영역 내 이모지(⚡/🚗/📷/📹/✅) 0건 — segmented done-mark `✓` 1글자 텍스트 span 만 허용
- [ ] 변환 영역 내 인라인 style 금지 키(color/background/padding/margin/fontSize/fontWeight/borderRadius/display/flex*/grid*/gap/width/height) 0건
- [ ] lucide Zap + Car + ChevronLeft + ChevronRight + CheckCircle2 + AlertTriangle + XCircle 사용 확인 (모두 이미 import 됨 — 신규 import 0건)
- [ ] **EXCEL-02 자탐 매핑 보존:** PowerPanel `onSave(selectedCP.id, result, memo, photoKey ?? undefined)` 한 줄도 변경 없음 / `cp.category === '소방용전원공급반'` 필터 한 줄도 변경 없음 / FLOOR_ORDER + .sort 한 줄도 변경 없음 → 자탐 엑셀 출력 측 영향 0
- [ ] **PowerPanel loc-card 좌우 스와이프 보존:** onTouchStart/onTouchEnd 핸들러 (`_swX` / dx > 40 / dx < -40 / setPickerIdx) 한 줄도 변경 없음
- [ ] **Parking cpId useMemo 분기 보존:** `cp.category === '주차장비'` / `cp.category === '회전문' && cp.location === subItem` 매핑 한 줄도 변경 없음
- [ ] **Parking showForm 분기 보존:** `showForm = item === '주차장비' || (item === '회전문' && !!subItem)` 한 줄도 변경 없음
- [ ] useInspectionRevisitPopup 호출 보존 (PowerPanel category: '소방용전원공급반' / Parking category: '주차장비')
- [ ] **증상 피커 미도입 (메모리 룰):** symptomPick 신설 0건 — 자동화 5종 카테고리 아님
- [ ] DamperModal (라인 2668~) 본문 한 줄도 변경 없음 — line shift 만 허용
- [ ] Wave 1~5 변환 영역 + 그 외 모든 영역 한 줄도 변경 없음
- [ ] PP_ZONE_PREFIX / PP_ZONE_LABELS / PPZone 타입 (라인 2256~2258) 한 줄도 변경 없음
- [ ] TypeScript 컴파일 0 에러
- [ ] `npm run build` 통과 + `dist/assets/InspectionPage-*.js` chunk hash 자동 변경 (PWA 캐시 무효화)
- [ ] 2개의 단일 commit (Task 1 / Task 2)
- [ ] cbc7119 디자인 리포 한정 — 원본 PWA(cha-bio-safety) 영향 0
- [ ] **Wave 6 완료 = InspectionPage TSX 전 변환 완료 (모든 모달 v0.1.1 정합)**
</success_criteria>

<output>
After completion, create `.planning/quick/260515-2r5-redesign-02-inspection-tsx-wave-6-powerp/260515-2r5-SUMMARY.md` with:
- One-liner (Wave 6 마지막 — PowerPanel + Parking 변환 + EXCEL-02 자탐 매핑 한 줄도 변경 없음)
- What was done (Task 1 + Task 2 — 라인 변동량, 시각 변환 핵심 포인트, 비즈니스 보존 강조)
- Verification (gate 표 — 옛 토큰 / 9/10/11px / 이모지 / handleSave / loc-card 스와이프 / showForm / 증상 피커 미도입 / TypeScript / build chunk hash)
- Business Logic Preservation Check (handleSave / cpId useMemo / loc-card 스와이프 byte-for-byte 비교 — EXCEL-02 자탐 매핑 보존 확인)
- Deviations from Plan (None — 플랜대로 실행 권장)
- Out of scope (Wave 1~5 / DamperModal / 외부 컴포넌트 / API / utils 비변경)
- Commits (Task 1 + Task 2 SHA)
- Self-Check (PASSED — 모든 success_criteria 통과)
- **Milestone note:** Wave 6 = InspectionPage TSX 전 변환 완료. 다음은 main 머지 + 다음 페이지 (03-checkpoints 등) 시안 작업
</output>
