---
phase: quick-260612-6bv
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cha-bio-safety/src/components/SettingsPanel.tsx
autonomous: true
requirements: [QUICK-260612-6bv]

must_haves:
  truths:
    - "admin SettingsPanel 데이터베이스 섹션에 '자동백업' 펼침 Row 가 DB [백업][업로드] 줄 아래·'파일' caption 위에 나타난다"
    - "펼치면 GET /api/database/r2-list 로 backups/db/*.sql 목록을 날짜 내림차순으로 보여준다"
    - "각 항목 다운로드 아이콘 탭 시 r2-download?key= blob 을 cha-bio-safety_<date>.sql 로 다운로드한다"
    - "로딩/빈결과/실패 상태가 각각 1줄 또는 toast 로 표시된다"
    - "백엔드(r2-list.ts / r2-download.ts)와 기존 핸들러(handleDbBackup/handleR2Backup)·LOCKED svg 블록은 1 byte 도 변경되지 않는다"
  artifacts:
    - path: "cha-bio-safety/src/components/SettingsPanel.tsx"
      provides: "자동백업 인라인 펼침 Row + r2-list 필터 + 항목별 다운로드 핸들러"
      contains: "backups/db/"
  key_links:
    - from: "SettingsPanel 자동백업 펼침"
      to: "/api/database/r2-list"
      via: "fetch on expand"
      pattern: "database/r2-list"
    - from: "SettingsPanel 항목 다운로드"
      to: "/api/database/r2-download"
      via: "blob a.download"
      pattern: "database/r2-download"
---

<objective>
admin 전용 SettingsPanel "데이터베이스" 섹션에, cbc-cron-worker 가 매일 KST 03:32 R2 `backups/db/<date>.sql` 로 적재하는 자동백업 파일들을 인라인 펼침 Row 로 노출하고 항목별 다운로드를 제공한다. 백엔드/스키마/API 무수정, 단일 프런트 파일 추가만으로 구현한다.

Purpose: 지금까지 자동백업 .sql 을 받으려면 wrangler 또는 r2-download 직접 key 지정뿐이었음 (UI backlog). admin 이 패널에서 바로 받을 수 있게 함.
Output: cha-bio-safety/src/components/SettingsPanel.tsx 단일 파일 변경.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@cha-bio-safety/src/components/SettingsPanel.tsx
@cha-bio-safety/functions/api/database/r2-list.ts
@cha-bio-safety/functions/api/database/r2-download.ts

# 검증된 사실 (이미 읽음 — 재확인 위해 다시 읽지 말 것):
#
# r2-list.ts (L8-28): role!=='admin' → 403. STORAGE.list 순회하며
#   backups/r2/*.zip 만 cronZips 로 분리하고 나머지 전부 keys 배열에 push.
#   응답: { success, data: { keys:[{key,size,uploaded}], totalSize, count, cronZips } }
#   → backups/db/*.sql 은 keys 에 들어있음. (수정 금지)
#
# r2-download.ts (L4-22): GET ?key= → role admin 체크 → STORAGE.get(key) → obj.body 스트림.
#   (수정 금지)
#
# SettingsPanel.tsx 기존 패턴 — 이 정의들 그대로 재사용:
#   L1: import { useState, useEffect, useRef } from 'react'
#   L2: import { ChevronRight, X, Send, Loader2 } from 'lucide-react'  ← Download 추가 대상
#   L17-20: ChevronRight size={14} className=`... transition-transform duration-150 ${collapsed?'':'rotate-90'}`
#   L347-353: useState 클러스터 (cacheClearing/dbBackingUp/dbRestoring/r2BackingUp/r2BackupProgress/r2Restoring) ← 신규 state 추가 위치
#   L501-521: handleDbBackup blob 다운로드 패턴 (verbatim 참고):
#     token = useAuthStore.getState().token
#     fetch(`${import.meta.env.VITE_API_BASE_URL || '/api'}/database/...`, { headers:{ Authorization:`Bearer ${token}` } })
#     blob = await res.blob(); url = URL.createObjectURL(blob)
#     a = document.createElement('a'); a.href=url; a.download=`cha-bio-safety_<date>.sql`; a.click(); URL.revokeObjectURL(url)
#   L860-918: admin 데이터베이스 섹션. 삽입 지점 = L890 (DB 버튼 div `</div>`) 와 L891 (`<div className="text-caption text-text-tertiary mb-1">파일 (점검 사진 등)</div>`) 사이.
#   L875-887, L900-912: LOCKED svg 인라인 주석 블록 — 1 byte 도 건드리지 말 것.
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 자동백업 state + 데이터 fetch/필터/다운로드 핸들러 추가</name>
  <files>cha-bio-safety/src/components/SettingsPanel.tsx</files>
  <action>
SettingsPanel.tsx 에 자동백업 Row 의 데이터 계층을 추가한다. 마크업은 Task 2 에서.

1. L2 lucide import 에 `Download` 추가. 변경 후: `import { ChevronRight, X, Send, Loader2, Download } from 'lucide-react'`. 다른 import 건드리지 말 것.

2. L347-353 useState 클러스터(r2Restoring 선언 직후)에 신규 state 4개 추가:
   - `const [autoBackupOpen, setAutoBackupOpen] = useState(false)` — 펼침 토글, 기본 접힘, localStorage 영속 불필요 (패널 열 때마다 접힘).
   - `const [autoBackupLoading, setAutoBackupLoading] = useState(false)`
   - `const [autoBackupList, setAutoBackupList] = useState<{ date: string; size: number; key: string }[]>([])`
   - `const [autoBackupDownloading, setAutoBackupDownloading] = useState<string | null>(null)` — 다운로드 중인 항목의 key (중복탭 방지용).

3. handleDbBackup(L501-521) 근처에 fetch 핸들러 추가 — `fetchAutoBackups`:
   - `setAutoBackupLoading(true)` 후 try.
   - token = useAuthStore.getState().token; base = import.meta.env.VITE_API_BASE_URL || '/api'.
   - `fetch(`${base}/database/r2-list`, { headers: { Authorization: `Bearer ${token}` } })` → json.
   - `if (!json.success) throw new Error(json.error || '목록 조회 실패')`.
   - keys = json.data.keys as { key:string; size:number; uploaded:string }[].
   - 필터: `k.key.startsWith('backups/db/') && k.key.endsWith('.sql')`.
   - 매핑: date = `k.key.replace('backups/db/','').replace('.sql','')`, size = k.size, key = k.key.
   - 내림차순 정렬: `.sort((a,b) => b.date.localeCompare(a.date))`.
   - `setAutoBackupList(...)`.
   - catch (e:any): `toast.error(e.message || '백업 목록을 불러오지 못했습니다')`, `setAutoBackupList([])`.
   - finally: `setAutoBackupLoading(false)`.

4. 펼침 토글 핸들러 추가 — `toggleAutoBackup`: `setAutoBackupOpen(prev => { const next = !prev; if (next) fetchAutoBackups(); return next })`. (펼칠 때마다 재fetch — admin 수동 조작, 4인 도구라 단순하게.)

5. 항목 다운로드 핸들러 추가 — `downloadAutoBackup(item: { date:string; size:number; key:string })`:
   - 중복탭 가드: `if (autoBackupDownloading) return`.
   - `setAutoBackupDownloading(item.key)` 후 try.
   - handleDbBackup(L501-521) blob 흐름 verbatim 따름:
     - token = useAuthStore.getState().token; base = import.meta.env.VITE_API_BASE_URL || '/api'.
     - `fetch(`${base}/database/r2-download?key=${encodeURIComponent(item.key)}`, { headers: { Authorization: `Bearer ${token}` } })`.
     - `if (!res.ok) { const j = await res.json().catch(() => ({})); throw new Error((j as any).error || '다운로드 실패') }`.
     - blob = await res.blob(); url = URL.createObjectURL(blob).
     - a = document.createElement('a'); a.href = url; `a.download = `cha-bio-safety_${item.date}.sql``; a.click(); URL.revokeObjectURL(url).
     - `toast.success('백업 파일이 다운로드되었습니다')`.
   - catch (e:any): `toast.error(e.message || '다운로드 실패')`.
   - finally: `setAutoBackupDownloading(null)`.

r2-list.ts / r2-download.ts / handleDbBackup / handleDbRestore / handleR2Backup / handleR2Restore 본문은 변경하지 말 것. 신규 코드만 추가.
  </action>
  <verify>
    <automated>cd cha-bio-safety && grep -q "backups/db/" src/components/SettingsPanel.tsx && grep -q "fetchAutoBackups" src/components/SettingsPanel.tsx && grep -q "Download" src/components/SettingsPanel.tsx && grep -q "cha-bio-safety_\${item.date}.sql" src/components/SettingsPanel.tsx && echo OK</automated>
  </verify>
  <done>autoBackup* state 4개 + fetchAutoBackups + toggleAutoBackup + downloadAutoBackup 핸들러가 존재하고 backups/db/*.sql 필터·날짜 내림차순·blob 다운로드 흐름이 들어감. lucide Download import 추가됨. 기존 핸들러/백엔드 무변경.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 자동백업 인라인 펼침 Row 마크업 삽입</name>
  <files>cha-bio-safety/src/components/SettingsPanel.tsx</files>
  <action>
Task 1 의 state/핸들러를 쓰는 마크업을, 데이터베이스 섹션 내부 — DB 버튼 div `</div>`(L890) 와 `<div className="text-caption text-text-tertiary mb-1">파일 (점검 사진 등)</div>`(L891) **사이** — 에 삽입한다.

기존 토큰만 사용 (CLAUDE.md + 메모리 디자인 룰):
- 컨테이너/항목 배경: `bg-surface-sunken`, 테두리 `border-border-default`, 라운드 `rounded-[9px]`.
- 캡션: `text-caption text-text-tertiary`. 라벨: `text-body font-medium text-text-primary`.
- 작은 컨테이너 안 text-caption 은 leading-none 고려 (헤더/항목 한 줄 높이).
- 이모지 절대 금지 — lucide 만. lucide 는 `size={N}` prop (className 으로 size 지정 X).
- LOCKED svg 인라인 블록(L875-887, L900-912) 근처를 편집하더라도 그 블록 자체는 1 byte 도 변경 금지.

삽입할 마크업 (구조, 클래스는 위 토큰으로):

1. 헤더 줄 — onClick={toggleAutoBackup} 인 버튼/div, `flex items-center justify-between` + 위아래 약간 간격 (예: `mb-2.5` 일관). 왼쪽 텍스트 블록:
   - 라벨: `<div className="text-body font-medium text-text-primary">자동백업</div>`
   - sub: `<div className="text-caption text-text-tertiary mt-px">매일 03:32 생성 · 14일 보관</div>`
   오른쪽 ChevronRight 토글: `<ChevronRight size={14} className={`text-text-tertiary transition-transform duration-150 ${autoBackupOpen ? 'rotate-90' : ''}`} />` (SectionHeader L17-20 패턴 재사용 — 펼침 시 rotate-90).

2. `{autoBackupOpen && (...)}` 블록 안:
   - 로딩: `{autoBackupLoading && (<div className="text-caption text-text-tertiary px-3 py-2">불러오는 중...</div>)}`
   - 빈 결과: `{!autoBackupLoading && autoBackupList.length === 0 && (<div className="text-caption text-text-tertiary px-3 py-2">자동백업 없음</div>)}`
   - 리스트: `{!autoBackupLoading && autoBackupList.map(item => (...))}` 각 항목:
     - 컨테이너: `flex items-center justify-between px-3 py-2 bg-surface-sunken rounded-[9px] mb-[5px]` + onClick={() => downloadAutoBackup(item)} + 다운로드 중이면 `opacity-50 cursor-default`(중복탭 방지), 아니면 `cursor-pointer`.
     - 왼쪽: 날짜 `<span className="text-body font-medium text-text-primary">{item.date}</span>` + 크기 `<span className="text-caption text-text-tertiary ml-2">{(item.size / 1048576).toFixed(1)}MB</span>`.
     - 오른쪽: 다운로드 중(`autoBackupDownloading === item.key`)이면 `<Loader2 size={16} className="text-text-tertiary animate-spin" />`, 아니면 `<Download size={16} className="text-text-tertiary" />`.

기존 [백업][업로드] 버튼 줄(L867-890), '파일 (점검 사진 등)' 그룹(L891-915), LOCKED svg 블록은 무변경. 새 마크업은 두 그룹 사이에만 삽입.

마크업에 fenced code block 이나 이모지를 절대 넣지 말 것. tailwind w-8=48px 함정 주의 (아이콘 size prop 으로만 지정하므로 해당 없음 — 컨테이너에 w-8/h-8 쓰지 말 것).
  </action>
  <verify>
    <automated>cd cha-bio-safety && grep -q "자동백업" src/components/SettingsPanel.tsx && grep -q "매일 03:32 생성" src/components/SettingsPanel.tsx && grep -q "toggleAutoBackup" src/components/SettingsPanel.tsx && grep -q "downloadAutoBackup" src/components/SettingsPanel.tsx && echo OK</automated>
  </verify>
  <done>데이터베이스 섹션 안, DB 버튼 줄 아래·'파일' caption 위에 '자동백업' 펼침 Row 마크업이 삽입됨. 헤더 토글·로딩/빈결과/리스트·항목별 Download/Loader2 아이콘이 들어가고 기존 토큰만 사용. LOCKED svg/기존 그룹 무변경.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→admin API | 브라우저에서 /api/database/r2-list, r2-download 호출 (Bearer token) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-6bv-01 | Information Disclosure | r2-list / r2-download | accept | 두 엔드포인트 모두 서버에서 `role !== 'admin'` → 403 강제 (r2-list.ts L6, r2-download.ts L6). 본 변경은 UI 만 추가, 권한 게이트 무변경. 섹션 자체도 `staff?.role === 'admin'` 조건부 렌더. |
| T-6bv-02 | Tampering | npm/pip/cargo installs | mitigate | 신규 패키지 설치 없음 — lucide Download 는 기존 lucide-react import. slopcheck 해당 없음. |
| T-6bv-03 | Denial of Service | 펼칠 때마다 r2-list fetch | accept | admin 수동 조작 + 4인 내부 도구 + 트래픽 매우 낮음. 캐싱 불필요. |
| T-6bv-SC | Tampering | npm/pip/cargo installs | mitigate | install task 없음 → 패키지 정당성 게이트 비해당. |
</threat_model>

<verification>
사용자 task_details 검증 게이트 전체 실행:

1. `cd cha-bio-safety && grep -n "자동백업" src/components/SettingsPanel.tsx` → 헤더 라벨 존재
2. `cd cha-bio-safety && grep -n "backups/db/" src/components/SettingsPanel.tsx` → 필터 prefix 존재
3. `git diff --name-only` → `cha-bio-safety/src/components/SettingsPanel.tsx` 단일 파일만
4. `git diff` 에 r2-list.ts / r2-download.ts / handleDbBackup / handleR2Backup / LOCKED svg 블록 미등장 (확인: `git diff cha-bio-safety/functions/api/database/ | wc -l` → 0)
5. 이모지 게이트: `node cha-bio-safety/scripts/check-emoji.cjs` (존재 확인됨) 실행 → PASS
6. `cd cha-bio-safety && npx tsc --noEmit` → 0 errors
7. `cd cha-bio-safety && npm run build` → PASS (precache 84 근처)
</verification>

<success_criteria>
- admin SettingsPanel 데이터베이스 섹션에 '자동백업' 펼침 Row 가 DB 버튼 줄 아래·'파일' caption 위에 위치
- 펼치면 backups/db/*.sql 목록을 날짜 내림차순으로 표시 (로딩/빈결과/실패 상태 포함)
- 항목 탭 → cha-bio-safety_<date>.sql 다운로드, 다운로드 중 스피너 + 중복탭 방지
- git diff = SettingsPanel.tsx 단일 파일, 백엔드/기존 핸들러/LOCKED svg 무변경
- 이모지 0, lucide size={N} prop 사용, 기존 토큰만
- tsc 0 errors + build PASS
</success_criteria>

<output>
Create `.planning/quick/260612-6bv-settingspanel-row/260612-6bv-SUMMARY.md` when done.
배포는 메인 Claude(오케스트레이터)가 직접 수행 — 이 plan 에 배포 task 없음.
</output>
