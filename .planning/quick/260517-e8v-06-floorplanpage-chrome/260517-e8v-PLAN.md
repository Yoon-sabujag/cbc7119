---
phase: 260517-e8v
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cha-bio-safety/docs/redesign-context/06-floorplan/sketch/floorplan-chrome-sketch.html
autonomous: true
requirements:
  - QUICK-260517-e8v-chrome-sketch
must_haves:
  truths:
    - "사용자가 sketch HTML 을 브라우저에서 열어 06 FloorPlanPage chrome 의 모바일/데스크톱 다크/라이트 4개 viewport 를 한 번에 시각 검토할 수 있다."
    - "헤더 / 도면 종류 탭 wrapper / 층 칩 wrapper 의 3-layer 배경 (page → raised → page) 이 분명히 보인다."
    - "도면 종류 탭 wrapper 와 층 칩 wrapper 위에 각각 라벨 '도면 종류' / '층 선택' 이 표기되어 있다."
    - "도면 종류 탭 4개 (유도등 / 감지기 / 스프링클러 / 소화기·소화전) 가 flex-1 basis-0 균등 분배 + 아이콘 + 라벨 패턴으로 렌더된다."
    - "층 칩 13개 (8-1F, 8F, 7F, 6F, 5F, 3F, 2F, 1F, B1, B2, B3, B4, B5) 가 가로 스크롤 + flex-shrink-0 패턴으로 렌더된다."
    - "선택 상태 = 1.5px accent border + bg-accent + text-on-accent / 비선택 = 1px strong border + bg-surface-page + text-secondary 로 시각 차이가 명확하다."
    - "데스크톱 viewport 에서는 뒤로가기 버튼이 없고 헤더가 h-[54px] 로, 모바일에서는 뒤로가기 버튼 + py-2.5 로 렌더된다."
  artifacts:
    - path: "cha-bio-safety/docs/redesign-context/06-floorplan/sketch/floorplan-chrome-sketch.html"
      provides: "06 FloorPlanPage chrome 통일 시안 (mobile dark/light + desktop dark/light 4 viewport)"
      contains: "viewport-mobile, viewport-desktop, data-theme=dark, data-theme=light, 도면 종류, 층 선택"
  key_links:
    - from: "sketch HTML"
      to: "inspection-modal-chrome-rules.md §7.2"
      via: "1:1 매핑 — 모든 className 이 룰 문서 §7.2 표의 적용 컬럼과 일치"
      pattern: "bg-surface-page.*border-b border-border-default|bg-surface-raised.*px-3.5 py-2|flex-1 basis-0 min-w-0.*px-2 py-2 rounded-sm text-label font-bold|flex-shrink-0 px-3.5 py-1.5 rounded-sm text-label font-bold|border-\\[1.5px\\] border-accent bg-accent text-text-on-accent"
---

<objective>
06 FloorPlanPage 의 chrome (헤더 / 도면 종류 탭 / 층 칩) 을 02 inspection 통일 룰 (`inspection-modal-chrome-rules.md` §7.2 매핑 가이드) 대로 재디자인한 시안 HTML 한 파일을 작성한다.

Purpose: TSX 변환 전 사용자 시각 검토 게이트. 02 inspection 통일 작업과 같은 워크플로우 (sketch → 컨펌 → TSX 변환) 를 06 에 적용. TSX 변환은 본 plan 의 범위 밖 (별도 quick task).

Output: `cha-bio-safety/docs/redesign-context/06-floorplan/sketch/floorplan-chrome-sketch.html` — Tailwind CDN + tokens 인라인, 4 viewport (모바일 dark/light + 데스크톱 dark/light) stand-alone HTML 1개.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

# 룰 문서 (필수 reference — sketch 는 §7.2 매핑대로)
@cha-bio-safety/docs/redesign-context/00-design-context/inspection-modal-chrome-rules.md

# 변환 대상 (현재 인라인 style + var() 토큰 — chrome 부분 940~1020 라인)
@cha-bio-safety/src/pages/FloorPlanPage.tsx

# 참고 패턴 (02 sketch 가 사용하는 stand-alone HTML 구조)
@cha-bio-safety/docs/redesign-context/02-inspection/sketch/inspection-sketch-main.html

<interfaces>
# FloorPlanPage 의 chrome 관련 상수 (FloorPlanPage.tsx 18~28 라인 — verbatim)

const PLAN_TYPES: { key: PlanType; label: string; ready: boolean }[] = [
  { key: 'guidelamp',    label: '유도등',       ready: true  },
  { key: 'detector',     label: '감지기',       ready: true  },
  { key: 'sprinkler',    label: '스프링클러',    ready: true  },
  { key: 'extinguisher', label: '소화기·소화전', ready: true  },
]

const FLOORS = [
  '8-1F','8F','7F','6F','5F','3F','2F','1F','B1','B2','B3','B4','B5',
]

# 현재 chrome 인라인 style 사용 토큰 (FloorPlanPage.tsx 940~1020 — verbatim)
# 헤더:  bg-surface-raised (var(--bg2))   ← 룰 위반: bg-surface-page 로 fix 필요
# 도면 종류 탭 wrapper:  bg-surface-raised  ← 유지 ✓
# 층 칩 wrapper:  bg-surface-raised        ← 유지 ✓
# 비선택 탭/칩:  bg-surface-sunken (var(--bg3))  ← 룰 위반: bg-surface-page + border-strong 로 fix
# 선택 탭/칩:  bg-accent (var(--acl))     ← 1.5px border-accent 추가

# 룰 문서 §7.2 핵심 className (verbatim)
# 헤더 wrapper:  `${isDesktop ? 'h-[54px] px-5 gap-2.5' : 'px-4 py-2.5 gap-2.5'} bg-surface-page border-b border-border-default flex-shrink-0 flex items-center`
# 뒤로가기:    `w-8 h-8 rounded-sm bg-surface-sunken border border-border-default text-text-secondary inline-flex items-center justify-center` + ChevronLeft size=15
# 타이틀:      `text-body font-bold text-text-primary truncate` (16px) — "소방 시설 도면"
# 우측 버튼:   `h-input px-3 rounded-sm bg-surface-sunken border border-border-default text-text-secondary text-caption font-semibold` (active editMode: `bg-accent border-accent text-text-on-accent`)
# 탭 wrapper:  `bg-surface-raised border-b border-border-default px-3.5 py-2 flex-shrink-0`
# 라벨:        `text-caption font-semibold text-text-tertiary mb-1.5 tracking-wider`  ("도면 종류" / "층 선택")
# 탭 버튼:     `flex-1 basis-0 min-w-0 inline-flex items-center justify-center gap-1.5 px-2 py-2 rounded-sm text-label font-bold whitespace-nowrap cursor-pointer transition-colors`
# 층 칩:       `flex-shrink-0 px-3.5 py-1.5 rounded-sm text-label font-bold whitespace-nowrap cursor-pointer transition-colors`
# 상태색 (선택):    `border-[1.5px] border-accent bg-accent text-text-on-accent`
# 상태색 (비선택):  `border border-border-strong bg-surface-page text-text-secondary`
# 도면 종류 아이콘 size: 14 (Lucide CDN 으로 시안에서는 근사 — 유도등:door-open / 감지기:radar / 스프링클러:droplets / 소화기:flame; TSX 변환 시 실제 커스텀 아이콘 사용)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: floorplan-chrome-sketch.html 작성 (4 viewport — mobile dark/light + desktop dark/light)</name>
  <files>cha-bio-safety/docs/redesign-context/06-floorplan/sketch/floorplan-chrome-sketch.html</files>
  <action>
02 inspection 통일 룰 (`inspection-modal-chrome-rules.md` §7.2) 매핑대로 06 FloorPlanPage 의 chrome 만 (헤더 + 도면 종류 탭 + 층 칩) 시각화한 stand-alone HTML 시안 1개 작성.

### 구조 (02 sketch 패턴 따름)

```
<!doctype html>
<html lang="ko">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>06 FloorPlanPage Chrome 통일 시안 (룰 적용 후)</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/gh/orioncactus/pretendard@v1.3.9/dist/web/variable/pretendardvariable.min.css" />
  <script src="https://unpkg.com/lucide@latest/dist/umd/lucide.min.js"></script>
  <style>
    /* tokens.css 인라인 — 02 sketch (inspection-sketch-main.html 25~155 라인) 에서 verbatim 복사:
       [data-theme="dark"] / [data-theme="light"] 두 :root 블록 + :root spacing tokens.
       sketch 전용 페이지 utility: viewport-frame, viewport-mobile (375x812), viewport-desktop (1280x800). */

    /* Tailwind config (window.tailwind.config) — surface-page/raised/sunken/active, text-primary/secondary/tertiary/on-accent,
       border-default/strong/focus, accent, safe/safe-bg, h-input, text-body/caption/label 토큰을 var() 매핑. */
  </style>
</head>
<body class="p-8 bg-gray-100">
  <!-- 페이지 헤더 (시안 메타) -->
  <header>
    <h1>06 FloorPlanPage Chrome 통일 시안 v0.1</h1>
    <p>룰 문서: inspection-modal-chrome-rules.md §7.2 — TSX 변환 전 사용자 시각 검토용</p>
  </header>

  <!-- 4 viewport 그리드 (2x2 또는 1x4 세로) -->
  <main class="grid grid-cols-2 gap-8">

    <!-- 1. Mobile Dark -->
    <section>
      <h2>모바일 · 다크</h2>
      <div class="viewport-frame viewport-mobile" data-theme="dark">
        [헤더 (page) + 도면 종류 탭 wrapper (raised, 라벨 "도면 종류" + 4탭 균등) + 층 칩 wrapper (raised, 라벨 "층 선택" + 13층 가로 스크롤) + 도면 캔버스 mock-up placeholder]
      </div>
    </section>

    <!-- 2. Mobile Light -->
    <section>
      <h2>모바일 · 라이트</h2>
      <div class="viewport-frame viewport-mobile" data-theme="light">
        [동일 구조]
      </div>
    </section>

    <!-- 3. Desktop Dark -->
    <section>
      <h2>데스크톱 · 다크</h2>
      <div class="viewport-frame viewport-desktop" data-theme="dark">
        [헤더 h-[54px] px-5, 뒤로가기 버튼 없음, 나머지 동일]
      </div>
    </section>

    <!-- 4. Desktop Light -->
    <section>
      <h2>데스크톱 · 라이트</h2>
      <div class="viewport-frame viewport-desktop" data-theme="light">
        [동일 데스크톱 구조]
      </div>
    </section>

  </main>

  <!-- 적용 룰 요약 박스 -->
  <section class="mt-8">
    <h2>적용된 룰</h2>
    <ul>
      <li>헤더 bg-surface-page (이전: raised → page 로 fix)</li>
      <li>도면 종류 탭 / 층 칩 wrapper bg-surface-raised + 라벨 ("도면 종류" / "층 선택") 추가</li>
      <li>비선택 탭/칩 = bg-surface-page + border-strong (3-layer 안에서 박스 윤곽만)</li>
      <li>선택 탭/칩 = border-[1.5px] border-accent bg-accent text-text-on-accent</li>
      <li>도면 종류 탭 = flex-1 basis-0 균등 / 층 칩 = flex-shrink-0 가로 스크롤</li>
      <li>데스크톱 헤더 h-[54px], 뒤로가기 버튼 없음 (사이드바 nav)</li>
    </ul>
  </section>

  <script>lucide.createIcons();</script>
</body>
</html>
```

### 핵심 className (verbatim — `<interfaces>` 의 룰 §7.2 표 그대로 사용 — 추측 토큰 금지)

각 viewport 안의 markup 은 `<interfaces>` 블록의 className 을 verbatim 복사. 임의로 토큰명/크기/spacing 변형 금지.

### 도면 종류 4탭 (PLAN_TYPES verbatim — 유도등 / 감지기 / 스프링클러 / 소화기·소화전)

Lucide CDN 으로 시안 한정 근사 아이콘 사용 (`<i data-lucide="door-open">` / `<i data-lucide="radar">` / `<i data-lucide="droplets">` / `<i data-lucide="flame">`), `size={14}`. TSX 변환 시 실제 커스텀 아이콘 (ExitSignIcon / SmokeVentIcon 또는 detector 전용 / sprinkler 전용 / FireExtinguisherCustom) 으로 교체 — 시안 박스에 명시 주석 추가.

### 13층 (FLOORS verbatim)

`['8-1F','8F','7F','6F','5F','3F','2F','1F','B1','B2','B3','B4','B5']` 순서 그대로. 모바일 viewport 에서는 가로 스크롤로 일부만 보임 (예: '8-1F' 선택 상태 + 우측 잘림). 데스크톱 viewport 에서는 더 많이 또는 전부 보임.

### Mock 상태

각 viewport 의 선택 상태:
- 도면 종류: '유도등' 선택 / 나머지 3개 비선택
- 층: '3F' 선택 / 나머지 12개 비선택 (예시값. 룰 검증 목적이므로 의미 없음)
- editMode: 비활성 (마커 편집 버튼 비-active 상태)

도면 캔버스 영역은 단순 `<div className="flex-1 bg-[#1a1f2b] flex items-center justify-center text-text-tertiary">도면 캔버스 (시안 범위 밖 — chrome 만 검증)</div>` placeholder 로 처리.

### 룰 위반 방지 self-check (sketch 작성 후 자체 검증)

작성 후 grep 으로 verbatim 확인:
1. `grep -c "bg-surface-page border-b border-border-default flex-shrink-0"` ≥ 4 (헤더 4개)
2. `grep -c "bg-surface-raised border-b border-border-default px-3.5 py-2 flex-shrink-0"` ≥ 8 (도면 종류 wrapper + 층 wrapper × 4 viewport)
3. `grep -c "text-caption font-semibold text-text-tertiary mb-1.5 tracking-wider"` ≥ 8 (라벨 2종 × 4 viewport)
4. `grep -c "flex-1 basis-0 min-w-0 inline-flex items-center justify-center gap-1.5 px-2 py-2 rounded-sm text-label font-bold whitespace-nowrap"` ≥ 16 (도면 종류 탭 4 × 4 viewport)
5. `grep -c "flex-shrink-0 px-3.5 py-1.5 rounded-sm text-label font-bold whitespace-nowrap"` ≥ 52 (층 칩 13 × 4 viewport)
6. `grep -c "border-\[1.5px\] border-accent bg-accent text-text-on-accent"` ≥ 8 (선택 상태: 도면 종류 1개 + 층 1개 × 4 viewport)
7. `grep -c "border border-border-strong bg-surface-page text-text-secondary"` ≥ 64 (비선택 상태: 도면 종류 3 + 층 12 × 4 viewport)
8. `grep -v '^[[:space:]]*\*\|^[[:space:]]*//' floorplan-chrome-sketch.html | grep -c "var(--bg)\|var(--bg2)\|var(--bg3)\|var(--bd)\|var(--t1)\|var(--t2)\|var(--t3)\|var(--acl)"` = 0 (시안 본문에 옛 alias 토큰 0건 — tokens.css 인라인 블록 안의 alias 정의 줄은 주석 제외 grep 으로 검증 안 됨 → 본문 영역만 grep 으로 확인하거나 inspection-sketch 의 alias 블록을 시안에 포함하지 않음. **권장:** alias 블록 (--bg/--bg2/--bg3/--bd/--t1/--t2/--t3/--acl) 자체를 tokens 인라인에서 제거하고 v0.1.1 토큰 (--surface-page 등) 만 정의)

### 작업 순서

1. 02 sketch 의 head/style 블록 (Tailwind config + tokens 인라인) 을 참조해 06 시안용으로 복사 (alias 토큰 제거)
2. viewport-frame / viewport-mobile / viewport-desktop CSS 정의 복사
3. mobile dark viewport markup 작성 (chrome 3-layer + 캔버스 placeholder)
4. mobile light / desktop dark / desktop light 로 복제 (data-theme 만 변경, desktop 은 헤더 h-[54px] px-5 + 뒤로가기 제거)
5. Lucide createIcons 호출
6. self-check grep 8가지 모두 통과 확인
  </action>
  <verify>
    <automated>cd /Users/jykevin/Documents/cbc7119-design && test -f cha-bio-safety/docs/redesign-context/06-floorplan/sketch/floorplan-chrome-sketch.html && \
      grep -c "bg-surface-page border-b border-border-default flex-shrink-0" cha-bio-safety/docs/redesign-context/06-floorplan/sketch/floorplan-chrome-sketch.html | awk '$1>=4 {exit 0} {exit 1}' && \
      grep -c "bg-surface-raised border-b border-border-default px-3.5 py-2 flex-shrink-0" cha-bio-safety/docs/redesign-context/06-floorplan/sketch/floorplan-chrome-sketch.html | awk '$1>=8 {exit 0} {exit 1}' && \
      grep -c 'text-caption font-semibold text-text-tertiary mb-1.5 tracking-wider' cha-bio-safety/docs/redesign-context/06-floorplan/sketch/floorplan-chrome-sketch.html | awk '$1>=8 {exit 0} {exit 1}' && \
      grep -c "flex-1 basis-0 min-w-0 inline-flex items-center justify-center gap-1.5 px-2 py-2 rounded-sm text-label font-bold whitespace-nowrap" cha-bio-safety/docs/redesign-context/06-floorplan/sketch/floorplan-chrome-sketch.html | awk '$1>=16 {exit 0} {exit 1}' && \
      grep -c "flex-shrink-0 px-3.5 py-1.5 rounded-sm text-label font-bold whitespace-nowrap" cha-bio-safety/docs/redesign-context/06-floorplan/sketch/floorplan-chrome-sketch.html | awk '$1>=52 {exit 0} {exit 1}' && \
      grep -c 'border-\[1.5px\] border-accent bg-accent text-text-on-accent' cha-bio-safety/docs/redesign-context/06-floorplan/sketch/floorplan-chrome-sketch.html | awk '$1>=8 {exit 0} {exit 1}' && \
      grep -c "border border-border-strong bg-surface-page text-text-secondary" cha-bio-safety/docs/redesign-context/06-floorplan/sketch/floorplan-chrome-sketch.html | awk '$1>=64 {exit 0} {exit 1}' && \
      grep -c 'viewport-mobile\|viewport-desktop' cha-bio-safety/docs/redesign-context/06-floorplan/sketch/floorplan-chrome-sketch.html | awk '$1>=4 {exit 0} {exit 1}' && \
      grep -c '도면 종류\|층 선택' cha-bio-safety/docs/redesign-context/06-floorplan/sketch/floorplan-chrome-sketch.html | awk '$1>=8 {exit 0} {exit 1}' && \
      grep -c "8-1F\|B5" cha-bio-safety/docs/redesign-context/06-floorplan/sketch/floorplan-chrome-sketch.html | awk '$1>=8 {exit 0} {exit 1}' && \
      grep -c '유도등\|감지기\|스프링클러\|소화기' cha-bio-safety/docs/redesign-context/06-floorplan/sketch/floorplan-chrome-sketch.html | awk '$1>=16 {exit 0} {exit 1}'</automated>
  </verify>
  <done>
    sketch HTML 파일이 존재하고, 룰 §7.2 의 핵심 className 9가지 grep gate 가 모두 PASS (4 viewport × 각 요소 개수). 4개 viewport (모바일 dark/light + 데스크톱 dark/light) 모두 렌더되고, "도면 종류" / "층 선택" 라벨 + 4 도면 종류 + 13층 chip 이 viewport 당 모두 포함. 사용자가 브라우저로 열어 시각 검토 가능 상태.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>06 FloorPlanPage chrome 통일 시안 HTML (4 viewport — mobile dark/light + desktop dark/light)</what-built>
  <how-to-verify>
1. 브라우저에서 파일 열기:
   `open cha-bio-safety/docs/redesign-context/06-floorplan/sketch/floorplan-chrome-sketch.html`
2. 4개 viewport 가 모두 렌더되는지 확인 (mobile 2 + desktop 2)
3. 각 viewport 에서 다음 시각 검증:
   - 헤더 (검은) → 도면 종류 wrapper (회색) → 층 wrapper (회색) → 본문 (검은) 의 3-layer 가 분명히 보임
   - "도면 종류" / "층 선택" 라벨이 wrapper 좌상단에 표기
   - 도면 종류 4탭이 균등 분배 (flex-1) + 아이콘 + 라벨
   - 층 13개 칩이 가로 스크롤 (모바일) / 균등 (데스크톱 또는 일부 보임)
   - 선택 상태 (1개씩) = 1.5px accent + bg-accent / 비선택 = 1px strong + bg-page
   - 데스크톱: 헤더에 뒤로가기 없음 + h-[54px]
   - 모바일: 헤더에 뒤로가기 + py-2.5
4. 02 inspection 통일 결과 (예: redesign/02-inspection-unification 의 InspectionModal 또는 inspection-unification-sketch.html) 와 헤더/wrapper/탭/칩 의 시각 일관성 확인
5. 어색하거나 룰 위반이 있으면 코멘트로 알려주기
  </how-to-verify>
  <resume-signal>"approved" 또는 수정사항 코멘트. 컨펌 시 별도 quick task 로 TSX 변환 진행 예정.</resume-signal>
</task>

</tasks>

<verification>
1. 파일 존재: `test -f cha-bio-safety/docs/redesign-context/06-floorplan/sketch/floorplan-chrome-sketch.html`
2. grep gate 9종 모두 PASS (Task 1 verify 의 automated)
3. 사용자 브라우저 검토 통과 (Task 2 checkpoint)
</verification>

<success_criteria>
- sketch HTML 파일 생성 및 git 트래킹 (다음 작업에서 커밋)
- 룰 §7.2 의 모든 매핑 className 이 verbatim 적용
- 4 viewport (mobile dark/light + desktop dark/light) 모두 렌더
- "도면 종류" / "층 선택" 라벨 새로 추가됨
- 옛 var() alias 토큰 (`--bg`/`--bg2`/`--bg3`/`--bd`/`--t1`/`--t2`/`--t3`/`--acl`) 0건 — v0.1.1 토큰만 사용
- 사용자 시각 검토 컨펌
</success_criteria>

<output>
After completion, create `.planning/quick/260517-e8v-06-floorplanpage-chrome/260517-e8v-SUMMARY.md` summarizing:
- sketch 파일 경로
- 적용된 룰 §7.2 매핑 항목 목록
- 4 viewport 의 mock 상태값 (선택된 도면 종류 / 층 / editMode)
- 사용자 컨펌 결과 + 코멘트 (있다면)
- 다음 단계 (TSX 변환 quick task) 안내
</output>
