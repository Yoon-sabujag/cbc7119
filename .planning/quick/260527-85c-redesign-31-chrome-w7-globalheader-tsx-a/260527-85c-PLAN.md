---
phase: 260527-85c
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cha-bio-safety/src/components/GlobalHeader.tsx
autonomous: true
requirements:
  - W7-GH-01
user_setup: []

must_haves:
  truths:
    - "GlobalHeader.tsx 가 v0.1.1 의미 토큰 Tailwind class 로 변환되어 인라인 style={} 0건 / legacy alias var(--bg2|bg3|bd|t1|t2) body markup 0건 상태로 빌드된다"
    - "비즈 anchor 7건 (Props interface 4 필드 / export function 시그니처 / leftSlot ?? fallback / svg path verbatim / title span 분기 / rightSlot || placeholder / header flex-shrink:0) 1 byte 변경 0 으로 보존된다"
    - "햄버거 svg path 'M4 6h16M4 12h16M4 18h16' 가 verbatim 으로 유지되어 OQ #5 LOCKED (인라인 svg 유지, Lucide Menu 교체 X) 가 반영된다"
    - "노안 룰 격상 4건 (title 13->18 text-title / button 32->48 w-12 h-12 / svg 15->20 / placeholder 32->48) 이 모두 적용된다"
    - "보호 파일 (App.tsx + tokens.css + typography.css + tailwind.config.js + 31-chrome/ 13 파일 + 00-design-context/) git diff 0 byte 로 보존된다"
    - "tsc --noEmit 0 errors 로 통과한다"
  artifacts:
    - path: "cha-bio-safety/src/components/GlobalHeader.tsx"
      provides: "Tailwind class 만으로 구성된 모바일 메인 헤더 (인라인 style 0)"
      contains: "GlobalHeaderProps / export function GlobalHeader / M4 6h16M4 12h16M4 18h16 / leftSlot ?? / rightSlot ||"
  key_links:
    - from: "cha-bio-safety/src/App.tsx (line 196)"
      to: "cha-bio-safety/src/components/GlobalHeader.tsx"
      via: "import { GlobalHeader } from './components/GlobalHeader' + <GlobalHeader title=... onMenuOpen=... rightSlot=... leftSlot=... />"
      pattern: "import.*GlobalHeader.*from.*components/GlobalHeader"
    - from: "cha-bio-safety/src/components/GlobalHeader.tsx"
      to: "cha-bio-safety/tailwind.config.js + cha-bio-safety/src/styles/tokens.css"
      via: "Tailwind utility class (bg-surface-raised / border-border-default / bg-surface-sunken / text-text-primary / text-text-secondary / text-title)"
      pattern: "bg-surface-raised|border-border-default|bg-surface-sunken|text-text-primary|text-text-secondary|text-title"
---

<objective>
redesign/31-chrome W7 — chrome 4 컴포넌트 TSX 변환의 첫 번째 sub-wave. `cha-bio-safety/src/components/GlobalHeader.tsx` (45 라인) 을 단일 atomic 1-commit 으로 v0.1.1 의미 토큰 Tailwind class 변환.

비즈 anchor 7건 (Props 인터페이스 / export 시그니처 / `<header>` shrink-0 / leftSlot `??` fallback / svg path verbatim / title span 분기 / rightSlot `||` placeholder) 1 byte 변경 0 강제. 노안 룰 격상 4건 (title 13→18 / button 32→48 / svg 15→20 / placeholder 32→48). 보호 파일 (App.tsx + tokens.css + typography.css + tailwind.config.js + 31-chrome/ 13 파일 + 00-design-context/) git diff 0 byte.

Purpose: chrome 통일 룰 적용의 시작점. W8/W9/W10 (SideMenu/SettingsPanel/MenuSettingsSection) 변환의 precedent 패턴 박제.

Output: `cha-bio-safety/src/components/GlobalHeader.tsx` 단일 파일 (45 → 약 25~35 라인 예상, Tailwind class 가독성 향상).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@CLAUDE.local.md
@cha-bio-safety/src/components/GlobalHeader.tsx
@cha-bio-safety/docs/redesign-context/31-chrome/wave-1-index.md
@cha-bio-safety/docs/redesign-context/31-chrome/sketch-wave-2-global-header.html
@cha-bio-safety/docs/redesign-context/31-chrome/wave-6-tsx-conversion-checklist.md
@cha-bio-safety/docs/redesign-context/31-chrome/tokens.css
@cha-bio-safety/docs/redesign-context/31-chrome/typography.css
@cha-bio-safety/docs/redesign-context/31-chrome/design-system.md
@cha-bio-safety/tailwind.config.js
@cha-bio-safety/src/pages/NotFoundPage.tsx

<interfaces>
<!-- ============================================================ -->
<!-- LIVE source — `cha-bio-safety/src/components/GlobalHeader.tsx` (45 라인) verbatim -->
<!-- 변환 전 상태. 7 비즈 anchor 1 byte 변경 0 강제 비교 기준. -->
<!-- ============================================================ -->

```tsx
interface GlobalHeaderProps {
  title: string
  onMenuOpen: () => void
  rightSlot?: React.ReactNode
  leftSlot?: React.ReactNode  // 햄버거 대신 표시 (예: 뒤로가기 버튼)
}

export function GlobalHeader({ title, onMenuOpen, rightSlot, leftSlot }: GlobalHeaderProps) {
  return (
    <header style={{
      display: 'flex',
      alignItems: 'center',
      height: 48,
      padding: '0 12px',
      background: 'var(--bg2)',
      borderBottom: '1px solid var(--bd)',
      flexShrink: 0,
    }}>
      {leftSlot ?? (
        <button
          onClick={onMenuOpen}
          aria-label="메뉴 열기"
          style={{
            width: 32, height: 32, borderRadius: 7,
            background: 'var(--bg3)', border: 'none',
            color: 'var(--t2)', cursor: 'pointer',
            display: 'flex', alignItems: 'center', justifyContent: 'center',
          }}
        >
          <svg width={15} height={15} fill="none" viewBox="0 0 24 24" stroke="var(--t2)" strokeWidth={2}>
            <path strokeLinecap="round" strokeLinejoin="round" d="M4 6h16M4 12h16M4 18h16"/>
          </svg>
        </button>
      )}
      <span style={{
        flex: 1, textAlign: rightSlot ? 'left' : 'center',
        fontSize: 13, fontWeight: 700, color: 'var(--t1)',
        marginLeft: rightSlot ? 8 : 0,
      }}>
        {title}
      </span>
      {rightSlot || <div style={{ width: 32 }} />}
    </header>
  )
}
```

<!-- ============================================================ -->
<!-- W2 sketch CSS verbatim (sketch-wave-2-global-header.html line 279~317) — TSX 변환 대상 -->
<!-- memory `feedback_planner_prompt_sketch_verbatim` 룰 적용: sketch CSS 정의를 verbatim 인용 -->
<!-- ============================================================ -->

```css
.global-header {
  display: flex;
  align-items: center;
  height: 48px;             /* h-12 — source 부합 */
  padding: 0 var(--space-3);
  background: var(--surface-raised);
  border-bottom: 1px solid var(--border-default);
  /* flex-shrink: 0 의도 — frame 안에서는 컨테이너가 컬럼 1개라 생략 가능 */
}

.gh-icon-btn {
  width: 48px; height: 48px;            /* 32->48 격상 */
  border-radius: var(--radius-sm);      /* 7->8 */
  background: var(--surface-sunken);
  border: none;
  color: var(--text-secondary);
  cursor: pointer;
  display: flex; align-items: center; justify-content: center;
  flex-shrink: 0;
}

.gh-title {
  flex: 1;
  font-size: 18px;          /* 13->18 (text-title) */
  font-weight: 600;         /* 700->600 */
  line-height: 1.4;
  color: var(--text-primary);
  /* textAlign / marginLeft 분기는 modifier 로 표현 */
}
.gh-title--center { text-align: center; margin-left: 0; }
.gh-title--left   { text-align: left;   margin-left: 8px; }

.gh-right-spacer {
  width: 48px;
  height: 48px;
  flex-shrink: 0;
}   /* placeholder symmetry — source 의 <div width:32> 와 동치 */
```

<!-- ============================================================ -->
<!-- W2 sketch HTML markup verbatim (frame 1 다크 햄버거 default) — line 429~437 -->
<!-- ============================================================ -->

```html
<header class="global-header">
  <button class="gh-icon-btn" aria-label="메뉴 열기">
    <svg width="20" height="20" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
      <path stroke-linecap="round" stroke-linejoin="round" d="M4 6h16M4 12h16M4 18h16"/>
    </svg>
  </button>
  <span class="gh-title gh-title--center">대시보드</span>
  <div class="gh-right-spacer"></div>
</header>
```

<!-- ============================================================ -->
<!-- W6 §10 Tailwind cheatsheet verbatim (legacy alias → v0.1.1 토큰 → Tailwind class) -->
<!-- ============================================================ -->

| legacy alias | v0.1.1 토큰 | Tailwind class |
|---|---|---|
| `var(--bg2)` | `--surface-raised` | `bg-surface-raised` |
| `var(--bg3)` | `--surface-sunken` | `bg-surface-sunken` |
| `var(--bd)` | `--border-default` | `border-border-default` |
| `var(--t1)` | `--text-primary` | `text-text-primary` |
| `var(--t2)` | `--text-secondary` | `text-text-secondary` |

<!-- Tailwind config 확인 (cha-bio-safety/tailwind.config.js): -->
<!--   - `text-title` = 18px / lineHeight 1.4 / fontWeight 500 (tailwind.config.js line 33) -->
<!--   - `text-text-primary` / `text-text-secondary` / `bg-surface-raised` / `bg-surface-sunken` / `border-border-default` = var(--...) alias (line 41~57) -->
<!--   - spacing override: `8` = 48px / `7` = 32px / `3` = 12px (line 99~117) → w-8 함정 의식 -->
<!--   - rounded-sm = 8px / rounded-md = 12px (line 92~97) -->
<!--   - `12` 는 spacing override 없음 → Tailwind 기본 `h-12` / `w-12` = 3rem = 48px (정상 동작) -->

<!-- ============================================================ -->
<!-- 기존 precedent 확인 (codebase grep) -->
<!-- ============================================================ -->

- `cha-bio-safety/src/pages/DivPage.tsx:1105` —
  `<header className="flex-shrink-0 flex items-center h-12 px-3 bg-surface-raised border-b border-border-default">`
  (W7 변환과 동일 패턴 precedent. `flex-shrink-0` 또는 `shrink-0` 둘 다 valid.)
- `cha-bio-safety/src/pages/ElevatorPage.tsx:1617` —
  `<div className="w-12 h-12 rounded-full bg-surface-sunken flex items-center justify-center flex-shrink-0">`
  (`w-12 h-12 = 48px` precedent — Tailwind 기본값.)
- `cha-bio-safety/src/pages/NotFoundPage.tsx` (30-not-found W2 결과) — 11→18 라인 단일 atomic precedent.
  `text-title font-semibold` 사용 (W2 sketch 와 동일 — fontWeight 600).

<!-- ============================================================ -->
<!-- W6 §12.1 W7 verify gate (≥8 항목 + 보호 파일 17 paths) -->
<!-- ============================================================ -->

- `test -f cha-bio-safety/src/components/GlobalHeader.tsx`
- `grep -c "GlobalHeaderProps" cha-bio-safety/src/components/GlobalHeader.tsx` ≥ 2
- `grep -c "M4 6h16M4 12h16M4 18h16" cha-bio-safety/src/components/GlobalHeader.tsx` = 1
- `grep -c "leftSlot \?\?" cha-bio-safety/src/components/GlobalHeader.tsx` = 1
- `grep -c "rightSlot \|\|" cha-bio-safety/src/components/GlobalHeader.tsx` = 1
- `grep -cE "fontSize: ?(9|10|11)[,$]" cha-bio-safety/src/components/GlobalHeader.tsx` = 0
- `grep -cE "var\(--(bg|bg2|bg3|bg4|bd|bd2|t1|t2|t3|acl)\)" cha-bio-safety/src/components/GlobalHeader.tsx` = 0
- `grep -cE "style=\{\{" cha-bio-safety/src/components/GlobalHeader.tsx` = 0
- `git diff --stat` 보호 파일 17개 모두 빈 줄 (0 byte)
- `cd cha-bio-safety && npx tsc --noEmit` 0 errors

<!-- ============================================================ -->
<!-- 비즈 anchor 7건 verbatim 표 (W6 §4.1) — 1 byte 변경 0 강제 -->
<!-- ============================================================ -->

| # | 영역 | source line | anchor (1 byte 변경 0) | verify grep |
|---|---|---|---|---|
| 1 | Props interface | 1~6 | `title / onMenuOpen / rightSlot? / leftSlot?` 4 필드 + 주석 "햄버거 대신 표시 (예: 뒤로가기 버튼)" | `grep -c "GlobalHeaderProps"` ≥ 2 |
| 2 | export function 시그니처 | 8 | `export function GlobalHeader({ title, onMenuOpen, rightSlot, leftSlot }: GlobalHeaderProps)` | `grep -c "export function GlobalHeader"` = 1 |
| 3 | `<header>` + flex-shrink:0 | 10~18 | height 48 (`h-12`) / padding '0 12px' (`px-3`) / flexShrink:0 (`shrink-0`) | `grep -c "shrink-0"` ≥ 1 |
| 4 | leftSlot ?? fallback | 19~33 | `??` (`||` 아님) — 0/false/'' 도 leftSlot 으로 인정 | `grep -c "leftSlot ??"` = 1 |
| 5 | svg path verbatim | 30~32 | `M4 6h16M4 12h16M4 18h16` + viewBox "0 0 24 24" + strokeWidth 2 + strokeLinecap/Linejoin "round" | `grep -c "M4 6h16M4 12h16M4 18h16"` = 1 |
| 6 | title span 분기 | 35~41 | textAlign `rightSlot ? 'left' : 'center'` + marginLeft `rightSlot ? 8 : 0` (Tailwind 조건부 class 로 표현 OK) | `grep -c "rightSlot ?"` ≥ 1 |
| 7 | rightSlot \|\| fallback | 42 | `\|\|` (`??` 아님) — placeholder 로 symmetry 보존 | `grep -c "rightSlot ||"` = 1 |

추가 verbatim 보존:
- `aria-label="메뉴 열기"` (line 22)
- `onClick={onMenuOpen}` (line 21)

<!-- ============================================================ -->
<!-- 노안 룰 격상 4건 (W6 §7 + W2 sketch §4) -->
<!-- ============================================================ -->

| 위치 | source 현재 | sketch 격상 | Tailwind class | 근거 |
|---|---|---|---|---|
| title fontSize (line 37) | 13 | 18 | `text-title` | 페이지 헤더 = 카드 제목급 (W6 §7 verbatim) |
| title fontWeight (line 37) | 700 | 600 | `font-semibold` | v0.1.1 룰 (700 무거움) — W2 sketch §4 표 verbatim |
| button width/height (line 24) | 32×32 | 48×48 | `w-12 h-12` | 터치 타겟 44 마지노선 + 8pt — w-12=48px Tailwind 기본 |
| button borderRadius (line 24) | 7 | 8 | `rounded-sm` | v0.1.1 §radius (8/12/16/99) — `rounded-sm` = 8px |
| svg width/height (line 30) | 15×15 | 20×20 | `width={20} height={20}` | Lucide size 표준 (인라인 svg 유지 OQ #5) |
| svg strokeWidth | 2 | 2 (유지) | `strokeWidth={2}` | 유지 |
| placeholder width (line 42) | 32 | 48 | `w-12` | symmetry — 햄버거와 동일 |
| header height | 48 | 48 (유지) | `h-12` | 매치 — Tailwind 기본 48px |
| header padding | '0 12px' | px-3 | `px-3` | 매치 — 12px |
| title marginLeft (rightSlot 분기) | 8 | 8 (유지) | `ml-2` | spacing override `2` = 8px |
| title span flex | 1 | 1 | `flex-1` | 매치 |

<!-- ============================================================ -->
<!-- OQ LOCKED 적용 (W6 §5 verbatim — GlobalHeader 관련) -->
<!-- ============================================================ -->

- **OQ #1 다크+라이트 양쪽**: tokens.css 의 `:root` + `[data-theme="light"]` 자동 분기. TSX 내 별도 분기 코드 X (Tailwind class 가 var(--...) 통해 자동 처리)
- **OQ #2 부분 격상**: 위 격상 표 적용
- **OQ #5 인라인 svg 유지**: 햄버거 svg `M4 6h16M4 12h16M4 18h16` verbatim. Lucide Menu 교체 절대 X
- **OQ #8 W7 TSX 4 분할**: 본 plan 은 W7 의 첫 sub-wave (GlobalHeader). W8/W9/W10 은 별도 wave 책임

<!-- ============================================================ -->
<!-- Negative gate (W6 §11 + sketch §6 — W7 변환 후 verify) -->
<!-- ============================================================ -->

- `style={{` 인라인 0건
- `var(--bg2|bg3|bd|t1|t2|bg|bg4|bd2|t3|acl)` body markup 0건
- raw hex 0건
- 9·10·11 px 0건 (격상 적용 결과)
- linear-gradient 0건 (GlobalHeader 그라데이션 사용 X — §6.4 매치 X)
- 이모지 0건 (memory `feedback_tsx_wave_emoji_dot_gap`)
- `✕` 텍스트 글리프 0건 (GlobalHeader 에는 원래 없음)
- `status-` prefix class 단독 사용 0건 (chrome 은 status 아님 — accent / surface / text 만)
- `w-8 / h-8` 함정 의식: 본 변환에서는 `w-12 h-12` (Tailwind 기본 48px) 사용 — `w-8 h-8` (override 48px) 도 OK 이나 sketch §3 verbatim 매치 위해 `w-12 h-12` 채택. memory `feedback_tailwind_w8_h8_is_48px` 위반 X
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: GlobalHeader.tsx 인라인 style → Tailwind class 단일 atomic 변환</name>
  <files>cha-bio-safety/src/components/GlobalHeader.tsx</files>

  <behavior>
  - 변환 후 GlobalHeader.tsx 가 인라인 `style={{...}}` 0건 / legacy alias `var(--bg2|bg3|bd|t1|t2)` body markup 0건으로 빌드된다
  - 비즈 anchor 7건 (Props interface 4 필드 + 주석 / export function 시그니처 / `<header>` + shrink-0 / leftSlot `??` fallback / svg path `M4 6h16M4 12h16M4 18h16` verbatim / title span 분기 / rightSlot `||` placeholder) 1 byte 변경 0
  - `aria-label="메뉴 열기"` + `onClick={onMenuOpen}` verbatim 보존
  - 노안 룰 격상 4건 적용: title 13→18 (`text-title`) / button 32→48 (`w-12 h-12`) / svg 15→20 / placeholder 32→48 (`w-12`)
  - title fontWeight 700→600 적용 (`font-semibold`) — W2 sketch §4 표 verbatim
  - 보호 파일 17개 git diff 0 byte (App.tsx + tokens.css + typography.css + tailwind.config.js + 31-chrome/ 13 파일 + 00-design-context/)
  - tsc --noEmit 0 errors
  </behavior>

  <action>
[Step 1] 브랜치 / 워크트리 확인 (memory `feedback_check_branch_before_edit`)
```bash
cd /Users/jykevin/Documents/cbc7119-design
pwd                                  # cbc7119-design 워크트리 확인 (디자인 전용)
git rev-parse --abbrev-ref HEAD      # redesign/31-chrome 또는 그에 해당하는 W7 브랜치
git status --short                   # dirty 면 사용자에게 먼저 알리기
```
브랜치가 `redesign/31-chrome` 계열이 아니면 사용자에게 컨펌 요청. dirty 면 사용자에게 알리기.

[Step 2] LIVE source verbatim 재확인 (변환 전 상태 박제)
```bash
cat /Users/jykevin/Documents/cbc7119-design/cha-bio-safety/src/components/GlobalHeader.tsx
wc -l /Users/jykevin/Documents/cbc7119-design/cha-bio-safety/src/components/GlobalHeader.tsx
```
45 라인 / 7 비즈 anchor (Props 인터페이스 4 필드 + 주석 / export 시그니처 / `<header>` flex-shrink:0 / leftSlot `??` button / svg path / title span 분기 / rightSlot `||` placeholder) 위치 재확인.

[Step 3] GlobalHeader.tsx 전체 재작성 (Write tool — 인라인 style 0건)

`Write` tool 로 `/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/src/components/GlobalHeader.tsx` 를 다음 내용으로 완전 교체:

```tsx
interface GlobalHeaderProps {
  title: string
  onMenuOpen: () => void
  rightSlot?: React.ReactNode
  leftSlot?: React.ReactNode  // 햄버거 대신 표시 (예: 뒤로가기 버튼)
}

export function GlobalHeader({ title, onMenuOpen, rightSlot, leftSlot }: GlobalHeaderProps) {
  return (
    <header className="flex items-center h-12 px-3 bg-surface-raised border-b border-border-default shrink-0">
      {leftSlot ?? (
        <button
          onClick={onMenuOpen}
          aria-label="메뉴 열기"
          className="w-12 h-12 rounded-sm bg-surface-sunken text-text-secondary border-none cursor-pointer flex items-center justify-center shrink-0"
        >
          <svg width={20} height={20} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
            <path strokeLinecap="round" strokeLinejoin="round" d="M4 6h16M4 12h16M4 18h16" />
          </svg>
        </button>
      )}
      <span
        className={`flex-1 text-title font-semibold text-text-primary ${rightSlot ? 'text-left ml-2' : 'text-center'}`}
      >
        {title}
      </span>
      {rightSlot || <div className="w-12 shrink-0" />}
    </header>
  )
}
```

verbatim 규칙 (1 byte 변경 0 강제):
- **interface GlobalHeaderProps** 4 필드 + 주석 "햄버거 대신 표시 (예: 뒤로가기 버튼)" verbatim 유지
- **export function GlobalHeader({ title, onMenuOpen, rightSlot, leftSlot }: GlobalHeaderProps)** 시그니처 verbatim
- **leftSlot ??** (`??` — `||` 와 다름, 0/false/'' 도 leftSlot 인정) verbatim
- **onClick={onMenuOpen}** + **aria-label="메뉴 열기"** verbatim
- **svg path "M4 6h16M4 12h16M4 18h16"** verbatim — OQ #5 LOCKED, Lucide Menu 교체 X
- **viewBox="0 0 24 24"** + **strokeWidth={2}** + **strokeLinecap="round"** + **strokeLinejoin="round"** verbatim
- **rightSlot ? 'text-left' : 'text-center'** + **rightSlot ? 'ml-2' : ''** 조건부 분기 (textAlign + marginLeft 분기 보존)
- **rightSlot ||** (`||` — `??` 와 다름, placeholder 로 symmetry) verbatim

Tailwind class 매핑 (W6 §10 verbatim):
- `display: flex / alignItems: center` → `flex items-center`
- `height: 48` → `h-12` (Tailwind 기본 3rem = 48px, override 없음)
- `padding: '0 12px'` → `px-3` (spacing `3` = 12px override)
- `background: 'var(--bg2)'` → `bg-surface-raised`
- `borderBottom: '1px solid var(--bd)'` → `border-b border-border-default`
- `flexShrink: 0` → `shrink-0`
- 햄버거 `width: 32, height: 32` → `w-12 h-12` (격상 48 — Tailwind 기본 3rem = 48)
- 햄버거 `borderRadius: 7` → `rounded-sm` (격상 8)
- 햄버거 `background: 'var(--bg3)'` → `bg-surface-sunken`
- 햄버거 `border: 'none'` → `border-none`
- 햄버거 `color: 'var(--t2)'` → `text-text-secondary` (svg `stroke="currentColor"` 가 이 색을 상속)
- 햄버거 `cursor: 'pointer'` → `cursor-pointer`
- 햄버거 `display: flex, alignItems: center, justifyContent: center` → `flex items-center justify-center`
- 햄버거 추가: `shrink-0` (sketch §3 verbatim)
- svg `width={15} height={15}` → `width={20} height={20}` (격상 — Lucide 표준)
- svg `stroke="var(--t2)"` → `stroke="currentColor"` (button text-text-secondary 상속)
- title `flex: 1` → `flex-1`
- title `fontSize: 13` → `text-title` (격상 18 — Tailwind config line 33 verbatim: 18px / lh 1.4 / fw 500. fw 는 `font-semibold` 로 600 override)
- title `fontWeight: 700` → `font-semibold` (격상 600 — W2 sketch §4 verbatim)
- title `color: 'var(--t1)'` → `text-text-primary`
- title `textAlign: rightSlot ? 'left' : 'center'` → `${rightSlot ? 'text-left' : 'text-center'}`
- title `marginLeft: rightSlot ? 8 : 0` → `${rightSlot ? 'ml-2' : ''}` (spacing `2` = 8px override) — `text-left` 와 같이 묶어 `'text-left ml-2'`
- placeholder `<div style={{ width: 32 }} />` → `<div className="w-12 shrink-0" />` (격상 48 + shrink-0 sketch §3 verbatim)

precedent: `cha-bio-safety/src/pages/DivPage.tsx:1105` 의 header 와 동일 패턴 (`flex-shrink-0 flex items-center h-12 px-3 bg-surface-raised border-b border-border-default`). `cha-bio-safety/src/pages/NotFoundPage.tsx` 의 `text-title font-semibold` 와 동일 폰트 패턴.

[Step 4] 보호 파일 1 byte 변경 0 검증 (W6 §12.5 verbatim)
```bash
cd /Users/jykevin/Documents/cbc7119-design
git diff --stat \
  cha-bio-safety/src/App.tsx \
  cha-bio-safety/src/styles/tokens.css \
  cha-bio-safety/src/styles/typography.css \
  cha-bio-safety/tailwind.config.js \
  cha-bio-safety/docs/redesign-context/00-design-context/ \
  cha-bio-safety/docs/redesign-context/31-chrome/wave-1-index.md \
  cha-bio-safety/docs/redesign-context/31-chrome/GlobalHeader.tsx \
  cha-bio-safety/docs/redesign-context/31-chrome/SideMenu.tsx \
  cha-bio-safety/docs/redesign-context/31-chrome/SettingsPanel.tsx \
  cha-bio-safety/docs/redesign-context/31-chrome/MenuSettingsSection.tsx \
  cha-bio-safety/docs/redesign-context/31-chrome/sketch-wave-2-global-header.html \
  cha-bio-safety/docs/redesign-context/31-chrome/sketch-wave-3-side-menu.html \
  cha-bio-safety/docs/redesign-context/31-chrome/sketch-wave-4-settings-panel.html \
  cha-bio-safety/docs/redesign-context/31-chrome/sketch-wave-5-menu-settings-section.html \
  cha-bio-safety/docs/redesign-context/31-chrome/design-system.md \
  cha-bio-safety/docs/redesign-context/31-chrome/tokens.css \
  cha-bio-safety/docs/redesign-context/31-chrome/typography.css \
  cha-bio-safety/docs/redesign-context/31-chrome/wave-6-tsx-conversion-checklist.md
```
**기대 출력: 빈 줄 (0 byte 변경)**. 어느 파일이라도 diff 나오면 즉시 사용자에게 알리고 stop (cherry-pick 사고 방지 — memory `project_redesign_17_annual_plan_status` / `project_redesign_28_splash_status`).

또한 W8/W9/W10 책임 파일 변경 0 확인:
```bash
git diff --stat \
  cha-bio-safety/src/components/SideMenu.tsx \
  cha-bio-safety/src/components/SettingsPanel.tsx \
  cha-bio-safety/src/components/MenuSettingsSection.tsx
```
**기대 출력: 빈 줄**.

페이지 변경 0 확인 (혹시라도 src/pages/ 가 건드려졌는지):
```bash
git diff --stat cha-bio-safety/src/pages/
```
**기대 출력: 빈 줄**.

[Step 5] verify gate 실행 (W6 §12.1 verbatim ≥8 항목)
```bash
cd /Users/jykevin/Documents/cbc7119-design

# Positive gate
test -f cha-bio-safety/src/components/GlobalHeader.tsx && echo "OK: file exists"
grep -c "GlobalHeaderProps" cha-bio-safety/src/components/GlobalHeader.tsx                # ≥ 2 기대 (interface + 시그니처)
grep -c "M4 6h16M4 12h16M4 18h16" cha-bio-safety/src/components/GlobalHeader.tsx          # = 1
grep -c "leftSlot ??" cha-bio-safety/src/components/GlobalHeader.tsx                      # = 1
grep -c "rightSlot ||" cha-bio-safety/src/components/GlobalHeader.tsx                     # = 1
grep -c "aria-label=\"메뉴 열기\"" cha-bio-safety/src/components/GlobalHeader.tsx          # = 1
grep -c "onClick={onMenuOpen}" cha-bio-safety/src/components/GlobalHeader.tsx             # = 1
grep -c "export function GlobalHeader" cha-bio-safety/src/components/GlobalHeader.tsx     # = 1

# Negative gate (W6 §11 + sketch §6)
grep -cE "fontSize: ?(9|10|11)[,$]" cha-bio-safety/src/components/GlobalHeader.tsx        # = 0
grep -cE "var\(--(bg|bg2|bg3|bg4|bd|bd2|t1|t2|t3|acl|safe|warn|danger|fire|info)\)" cha-bio-safety/src/components/GlobalHeader.tsx  # = 0
grep -cE "style=\{\{" cha-bio-safety/src/components/GlobalHeader.tsx                      # = 0
grep -cE "#[0-9a-fA-F]{3,8}" cha-bio-safety/src/components/GlobalHeader.tsx               # = 0 (raw hex 없음)
grep -c "linear-gradient" cha-bio-safety/src/components/GlobalHeader.tsx                  # = 0
grep -c "✕" cha-bio-safety/src/components/GlobalHeader.tsx                                # = 0
# 이모지 체크 (Korean text "메뉴 열기" 는 제외 — Hangul, 이모지 아님)
grep -P "[\x{1F300}-\x{1FAFF}\x{2600}-\x{27BF}]" cha-bio-safety/src/components/GlobalHeader.tsx | wc -l   # = 0

# Tailwind class 적용 확인
grep -c "bg-surface-raised" cha-bio-safety/src/components/GlobalHeader.tsx                # ≥ 1
grep -c "bg-surface-sunken" cha-bio-safety/src/components/GlobalHeader.tsx                # ≥ 1
grep -c "border-border-default" cha-bio-safety/src/components/GlobalHeader.tsx            # ≥ 1
grep -c "text-text-primary" cha-bio-safety/src/components/GlobalHeader.tsx                # ≥ 1
grep -c "text-text-secondary" cha-bio-safety/src/components/GlobalHeader.tsx              # ≥ 1
grep -c "text-title" cha-bio-safety/src/components/GlobalHeader.tsx                       # ≥ 1
grep -c "shrink-0" cha-bio-safety/src/components/GlobalHeader.tsx                         # ≥ 1
grep -c "w-12" cha-bio-safety/src/components/GlobalHeader.tsx                             # ≥ 2 (button + placeholder)
grep -c "h-12" cha-bio-safety/src/components/GlobalHeader.tsx                             # ≥ 2 (header + button)
```
모두 기대값 매치 확인.

[Step 6] TypeScript 컴파일 검증
```bash
cd /Users/jykevin/Documents/cbc7119-design/cha-bio-safety
npx tsc --noEmit 2>&1 | tee /tmp/tsc-w7-globalheader.log
echo "exit code: $?"
```
**기대: 0 errors, exit 0.** 에러 나오면 즉시 멈추고 사용자에게 알리기 (사고 방지).

[Step 7] 결과 시각 측정 (변환 전후 라인 수 + chunk 시각)
```bash
wc -l /Users/jykevin/Documents/cbc7119-design/cha-bio-safety/src/components/GlobalHeader.tsx
git diff --stat cha-bio-safety/src/components/GlobalHeader.tsx
```
약 25~35 라인 예상 (45 → ~30, Tailwind class 가독성 향상).

[Step 8] atomic commit (산출물 = GlobalHeader.tsx 1 파일만)
```bash
cd /Users/jykevin/Documents/cbc7119-design
git status --short
git add cha-bio-safety/src/components/GlobalHeader.tsx
git status --short                   # GlobalHeader.tsx 1 파일만 staged 확인
git diff --cached --stat             # 정확히 1 파일 변경 확인
git commit -m "$(cat <<'EOF'
refactor(31-chrome): W7 GlobalHeader.tsx Tailwind class 변환 (45→~30 lines)

v0.1.1 의미 토큰 Tailwind class 만으로 재구성. 인라인 style 0건 / legacy alias 0건.

비즈 anchor 7건 verbatim:
- GlobalHeaderProps interface 4 필드
- export function 시그니처
- <header> flex-shrink-0 (shrink-0)
- leftSlot ?? fallback (?? 보존)
- svg path "M4 6h16M4 12h16M4 18h16" verbatim (OQ #5 LOCKED)
- title span textAlign + marginLeft 분기 (조건부 class)
- rightSlot || placeholder (|| 보존)

노안 룰 격상 (OQ #2 LOCKED 부분 절충):
- title 13->18 (text-title) + 700->600 (font-semibold)
- button 32->48 (w-12 h-12) + radius 7->8 (rounded-sm)
- svg 15->20 (Lucide 표준, 인라인 svg 유지)
- placeholder 32->48 (w-12 symmetry)

Tailwind 토큰 매핑:
- var(--bg2) -> bg-surface-raised
- var(--bg3) -> bg-surface-sunken
- var(--bd)  -> border-border-default
- var(--t1)  -> text-text-primary
- var(--t2)  -> text-text-secondary (svg stroke=currentColor 상속)

보호 파일 17개 git diff 0 byte.
W8/W9/W10 (SideMenu/SettingsPanel/MenuSettingsSection) 변경 0 — 별도 wave.

Verify: tsc --noEmit 0 errors.
EOF
)"
git log -1 --stat
```
**기대: 1 파일 변경 (GlobalHeader.tsx).** 다른 파일이 staged 면 즉시 stop + 사용자에게 알리기.
  </action>

  <verify>
    <automated>cd /Users/jykevin/Documents/cbc7119-design && \
test -f cha-bio-safety/src/components/GlobalHeader.tsx && \
[ "$(grep -c 'GlobalHeaderProps' cha-bio-safety/src/components/GlobalHeader.tsx | tr -d ' ')" -ge 2 ] && \
[ "$(grep -c 'M4 6h16M4 12h16M4 18h16' cha-bio-safety/src/components/GlobalHeader.tsx | tr -d ' ')" = "1" ] && \
[ "$(grep -c 'leftSlot ??' cha-bio-safety/src/components/GlobalHeader.tsx | tr -d ' ')" = "1" ] && \
[ "$(grep -c 'rightSlot ||' cha-bio-safety/src/components/GlobalHeader.tsx | tr -d ' ')" = "1" ] && \
[ "$(grep -c 'aria-label=\"메뉴 열기\"' cha-bio-safety/src/components/GlobalHeader.tsx | tr -d ' ')" = "1" ] && \
[ "$(grep -c 'export function GlobalHeader' cha-bio-safety/src/components/GlobalHeader.tsx | tr -d ' ')" = "1" ] && \
[ "$(grep -cE 'fontSize: ?(9|10|11)[,$]' cha-bio-safety/src/components/GlobalHeader.tsx | tr -d ' ')" = "0" ] && \
[ "$(grep -cE 'var\(--(bg|bg2|bg3|bg4|bd|bd2|t1|t2|t3|acl)\)' cha-bio-safety/src/components/GlobalHeader.tsx | tr -d ' ')" = "0" ] && \
[ "$(grep -cE 'style=\{\{' cha-bio-safety/src/components/GlobalHeader.tsx | tr -d ' ')" = "0" ] && \
[ "$(grep -c 'bg-surface-raised' cha-bio-safety/src/components/GlobalHeader.tsx | tr -d ' ')" -ge 1 ] && \
[ "$(grep -c 'text-title' cha-bio-safety/src/components/GlobalHeader.tsx | tr -d ' ')" -ge 1 ] && \
[ "$(git diff --stat cha-bio-safety/src/App.tsx cha-bio-safety/src/styles/tokens.css cha-bio-safety/src/styles/typography.css cha-bio-safety/tailwind.config.js cha-bio-safety/docs/redesign-context/00-design-context/ cha-bio-safety/docs/redesign-context/31-chrome/ cha-bio-safety/src/components/SideMenu.tsx cha-bio-safety/src/components/SettingsPanel.tsx cha-bio-safety/src/components/MenuSettingsSection.tsx cha-bio-safety/src/pages/ | wc -l | tr -d ' ')" = "0" ] && \
cd cha-bio-safety && npx tsc --noEmit && \
echo "ALL W7 VERIFY GATES PASSED"</automated>
  </verify>

  <done>
- `cha-bio-safety/src/components/GlobalHeader.tsx` 가 Tailwind class 만으로 재구성됨 (인라인 `style={{` 0건, legacy alias `var(--bg|bg2|bg3|bg4|bd|bd2|t1|t2|t3|acl)` 0건)
- 비즈 anchor 7건 verbatim 보존 (GlobalHeaderProps interface 4 필드 / export function 시그니처 / `<header>` shrink-0 / leftSlot `??` / svg path `M4 6h16M4 12h16M4 18h16` / title span 분기 / rightSlot `||` placeholder)
- `aria-label="메뉴 열기"` + `onClick={onMenuOpen}` verbatim
- 노안 룰 격상 4건 적용 (title `text-title font-semibold` / button `w-12 h-12 rounded-sm` / svg 20 / placeholder `w-12`)
- 보호 파일 17개 (App.tsx + tokens.css + typography.css + tailwind.config.js + 31-chrome/ 13 파일 + 00-design-context/) + W8/W9/W10 책임 컴포넌트 3 파일 + src/pages/ 전체 git diff 0 byte
- `cd cha-bio-safety && npx tsc --noEmit` 0 errors
- atomic 1-commit (GlobalHeader.tsx 1 파일만 staged + commit)
  </done>
</task>

</tasks>

<verification>
**verify gate (W6 §12.1 verbatim ≥8 항목 + 보호 파일 17 paths + W8/W9/W10 + src/pages/)**

Positive (변환 결과 = 기대):
- `test -f cha-bio-safety/src/components/GlobalHeader.tsx`
- `grep -c "GlobalHeaderProps" cha-bio-safety/src/components/GlobalHeader.tsx` ≥ 2
- `grep -c "M4 6h16M4 12h16M4 18h16" cha-bio-safety/src/components/GlobalHeader.tsx` = 1
- `grep -c "leftSlot ??" cha-bio-safety/src/components/GlobalHeader.tsx` = 1
- `grep -c "rightSlot ||" cha-bio-safety/src/components/GlobalHeader.tsx` = 1
- `grep -c "aria-label=\"메뉴 열기\"" cha-bio-safety/src/components/GlobalHeader.tsx` = 1
- `grep -c "export function GlobalHeader" cha-bio-safety/src/components/GlobalHeader.tsx` = 1
- `grep -c "bg-surface-raised" cha-bio-safety/src/components/GlobalHeader.tsx` ≥ 1
- `grep -c "text-title" cha-bio-safety/src/components/GlobalHeader.tsx` ≥ 1
- `grep -c "shrink-0" cha-bio-safety/src/components/GlobalHeader.tsx` ≥ 1

Negative (W6 §11 — 위반 0):
- `grep -cE "fontSize: ?(9|10|11)[,$]" cha-bio-safety/src/components/GlobalHeader.tsx` = 0
- `grep -cE "var\(--(bg|bg2|bg3|bg4|bd|bd2|t1|t2|t3|acl|safe|warn|danger|fire|info)\)" cha-bio-safety/src/components/GlobalHeader.tsx` = 0
- `grep -cE "style=\{\{" cha-bio-safety/src/components/GlobalHeader.tsx` = 0
- `grep -cE "#[0-9a-fA-F]{3,8}" cha-bio-safety/src/components/GlobalHeader.tsx` = 0
- `grep -c "linear-gradient" cha-bio-safety/src/components/GlobalHeader.tsx` = 0
- `grep -c "✕" cha-bio-safety/src/components/GlobalHeader.tsx` = 0

보호 파일 17 paths + W8/W9/W10 + src/pages/ git diff 0 byte:
```bash
git diff --stat \
  cha-bio-safety/src/App.tsx \
  cha-bio-safety/src/styles/tokens.css \
  cha-bio-safety/src/styles/typography.css \
  cha-bio-safety/tailwind.config.js \
  cha-bio-safety/docs/redesign-context/00-design-context/ \
  cha-bio-safety/docs/redesign-context/31-chrome/ \
  cha-bio-safety/src/components/SideMenu.tsx \
  cha-bio-safety/src/components/SettingsPanel.tsx \
  cha-bio-safety/src/components/MenuSettingsSection.tsx \
  cha-bio-safety/src/pages/
```
→ 빈 줄.

빌드 검증:
- `cd cha-bio-safety && npx tsc --noEmit` 0 errors / exit 0
</verification>

<success_criteria>
- `cha-bio-safety/src/components/GlobalHeader.tsx` 단일 파일 변환 완료 (45 → ~25~35 라인)
- 인라인 `style={{` 0건 / legacy alias `var(--bg2|bg3|bd|t1|t2)` body markup 0건
- 비즈 anchor 7건 + `aria-label` + `onClick` verbatim 1 byte 변경 0
- svg path `M4 6h16M4 12h16M4 18h16` verbatim (OQ #5 LOCKED)
- 노안 룰 격상 4건 (title 13→18 `text-title font-semibold` / button 32→48 `w-12 h-12 rounded-sm` / svg 15→20 / placeholder 32→48 `w-12`)
- 보호 파일 17개 + W8/W9/W10 책임 3 컴포넌트 + src/pages/ git diff 0 byte
- tsc --noEmit 0 errors
- atomic 1-commit (GlobalHeader.tsx 1 파일만)
</success_criteria>

<output>
After completion, create `.planning/quick/260527-85c-redesign-31-chrome-w7-globalheader-tsx-a/260527-85c-SUMMARY.md` with:
- 변환 전/후 라인 수 (45 → 실측 N)
- Tailwind class 매핑 표 (legacy → v0.1.1 토큰 → Tailwind class) 적용 결과
- 비즈 anchor 7건 verbatim 보존 grep 결과
- 노안 룰 격상 4건 적용 결과
- verify gate (positive + negative + 보호 파일 17 paths + W8/W9/W10 + src/pages/) 결과
- tsc --noEmit 결과
- atomic 1-commit 해시 + 1 파일 변경 확인
- 다음 wave (W8 SideMenu) 진입 조건 박제
- precedent 패턴 박제 (W8/W9/W10 sub-wave 가 본 W7 패턴 mirror 할 수 있도록)
</output>
