---
phase: 260527-85c
plan: 01
subsystem: redesign/31-chrome
tags: [redesign, 31-chrome, tsx-conversion, w7, global-header, tailwind, atomic]
requires:
  - cha-bio-safety/docs/redesign-context/31-chrome/sketch-wave-2-global-header.html
  - cha-bio-safety/docs/redesign-context/31-chrome/wave-6-tsx-conversion-checklist.md
  - cha-bio-safety/tailwind.config.js
  - cha-bio-safety/src/styles/tokens.css
provides:
  - cha-bio-safety/src/components/GlobalHeader.tsx (Tailwind class only — inline style 0)
affects:
  - cha-bio-safety/src/App.tsx (consumer — import 시그니처 무변경)
tech-stack:
  added: []
  patterns:
    - tailwind-class-only-chrome-component
    - bizz-anchor-verbatim-preservation (7 anchors)
    - nullish-vs-or-coalescing-distinction (?? vs ||)
    - currentColor-svg-stroke-inheritance
key-files:
  modified:
    - cha-bio-safety/src/components/GlobalHeader.tsx
  created: []
decisions:
  - "OQ #5 LOCKED — 인라인 svg path 'M4 6h16M4 12h16M4 18h16' verbatim 유지, Lucide Menu 교체 X"
  - "OQ #2 부분 격상 적용 — title 13→18 / button 32→48 / svg 15→20 / placeholder 32→48"
  - "title fontWeight 700→600 (font-semibold) — W2 sketch §4 verbatim"
  - "w-12 h-12 (Tailwind 기본 3rem=48) 채택 — w-8 override 함정 회피 (memory feedback_tailwind_w8_h8_is_48px)"
  - "svg stroke=currentColor 로 button text-text-secondary 상속 — 불필요한 분기 제거"
metrics:
  duration: "~5 minutes"
  completed_date: "2026-05-26T21:01:37Z"
  lines_before: 45
  lines_after: 30
  files_changed: 1
  insertions: 8
  deletions: 23
  commit: e8ffb98
---

# Phase 260527-85c Plan 01: W7 GlobalHeader.tsx Tailwind class 변환 Summary

redesign/31-chrome W7 첫 sub-wave — `cha-bio-safety/src/components/GlobalHeader.tsx` (45 라인) 을 단일 atomic 1-commit 으로 v0.1.1 의미 토큰 Tailwind class 변환. 인라인 style 0건 / legacy alias 0건 / 비즈 anchor 7건 verbatim / 노안 룰 격상 4건 / tsc 0 errors / 30 라인.

## What changed (변환 전후)

| 항목 | Before (source) | After (Tailwind) |
|---|---|---|
| 라인 수 | 45 | 30 |
| 인라인 `style={{` | 4건 (header + button + span + placeholder) | 0건 |
| legacy alias `var(--bg2\|bg3\|bd\|t1\|t2)` | 7건 | 0건 |
| title fontSize | 13 (inline) | `text-title` (= 18px, lh 1.4) — 격상 |
| title fontWeight | 700 (inline) | `font-semibold` (= 600) — W2 sketch §4 verbatim |
| button width/height | 32×32 (inline) | `w-12 h-12` (= 48×48) — 격상 |
| button borderRadius | 7 (inline) | `rounded-sm` (= 8) — 격상 |
| svg width/height | 15×15 | 20×20 — Lucide 표준 |
| svg stroke | `var(--t2)` | `currentColor` (text-text-secondary 상속) |
| placeholder width | 32 (inline div) | `w-12` (= 48) — symmetry |
| header className | (없음, style 인라인) | `flex items-center h-12 px-3 bg-surface-raised border-b border-border-default shrink-0` |

## Tailwind class 매핑 표 적용 결과 (W6 §10 verbatim)

| legacy alias | v0.1.1 토큰 | Tailwind class | 적용 여부 |
|---|---|---|---|
| `var(--bg2)` | `--surface-raised` | `bg-surface-raised` | ✓ (header 배경) |
| `var(--bg3)` | `--surface-sunken` | `bg-surface-sunken` | ✓ (햄버거 버튼 배경) |
| `var(--bd)` | `--border-default` | `border-border-default` | ✓ (header border-b) |
| `var(--t1)` | `--text-primary` | `text-text-primary` | ✓ (title 색) |
| `var(--t2)` | `--text-secondary` | `text-text-secondary` | ✓ (button 색, svg stroke=currentColor 가 상속) |

## 비즈 anchor 7건 verbatim 보존 grep 결과

| # | 영역 | grep | 기대 | 실측 | PASS |
|---|---|---|---|---|---|
| 1 | Props interface | `grep -c "GlobalHeaderProps"` | ≥ 2 | 2 | ✓ |
| 2 | export function | `grep -c "export function GlobalHeader"` | = 1 | 1 | ✓ |
| 3 | `<header>` + shrink-0 | `grep -c "shrink-0"` | ≥ 1 | 3 | ✓ |
| 4 | leftSlot `??` fallback | `grep -c "leftSlot ??"` | = 1 | 1 | ✓ |
| 5 | svg path verbatim | `grep -c "M4 6h16M4 12h16M4 18h16"` | = 1 | 1 | ✓ |
| 6 | title span 분기 (rightSlot ?) | inline `${rightSlot ? 'text-left ml-2' : 'text-center'}` 인용 | 보존 | 보존 | ✓ |
| 7 | rightSlot `||` placeholder | `grep -c "rightSlot \|\|"` | = 1 | 1 | ✓ |

추가 verbatim:
- `aria-label="메뉴 열기"` → `grep -c` = 1 ✓
- `onClick={onMenuOpen}` → `grep -c` = 1 ✓
- `viewBox="0 0 24 24"` + `strokeWidth={2}` + `strokeLinecap="round"` + `strokeLinejoin="round"` → 보존 ✓
- Props 4 필드 (`title / onMenuOpen / rightSlot? / leftSlot?`) + 주석 "햄버거 대신 표시 (예: 뒤로가기 버튼)" → 보존 ✓

## 노안 룰 격상 4건 적용 결과 (W6 §7 + W2 sketch §4)

| 위치 | source | 격상 후 | Tailwind class | 결과 |
|---|---|---|---|---|
| title fontSize | 13 | 18 | `text-title` | ✓ |
| title fontWeight | 700 | 600 | `font-semibold` | ✓ |
| button width/height | 32×32 | 48×48 | `w-12 h-12` | ✓ |
| button borderRadius | 7 | 8 | `rounded-sm` | ✓ |
| svg width/height | 15×15 | 20×20 | `width={20} height={20}` | ✓ |
| placeholder width | 32 | 48 | `w-12` | ✓ |
| header height | 48 (유지) | 48 | `h-12` | ✓ |
| header padding | '0 12px' | px-3 | `px-3` | ✓ |
| title marginLeft (rightSlot 분기) | 8 | 8 | `ml-2` | ✓ |

## Verify gate 결과 (W6 §12.1 verbatim)

### Positive gate (변환 결과 = 기대)

```
GlobalHeaderProps count: 2 (>= 2 ✓)
svg path count: 1 (= 1 ✓)
leftSlot ?? count: 1 (= 1 ✓)
rightSlot || count: 1 (= 1 ✓)
aria-label count: 1 (= 1 ✓)
onClick count: 1 (= 1 ✓)
export function count: 1 (= 1 ✓)
bg-surface-raised: 1 (>= 1 ✓)
bg-surface-sunken: 1 (>= 1 ✓)
border-border-default: 1 (>= 1 ✓)
text-text-primary: 1 (>= 1 ✓)
text-text-secondary: 1 (>= 1 ✓)
text-title: 1 (>= 1 ✓)
shrink-0: 3 (>= 1 ✓)
w-12: 2 (>= 2, button + placeholder ✓)
h-12: 2 (>= 2, header + button ✓)
```

### Negative gate (W6 §11 — 위반 0)

```
fontSize 9/10/11 count: 0 (= 0 ✓)
legacy alias (bg|bg2|bg3|bg4|bd|bd2|t1|t2|t3|acl|safe|warn|danger|fire|info) count: 0 (= 0 ✓)
style={{ count: 0 (= 0 ✓)
raw hex count: 0 (= 0 ✓)
linear-gradient count: 0 (= 0 ✓)
✕ count: 0 (= 0 ✓)
emoji (U+1F300-1FAFF / U+2600-27BF) count: 0 (= 0 ✓)
```

### 보호 파일 17 paths + W8/W9/W10 + src/pages/ git diff 0 byte

```
git diff --stat <17 paths + 3 W*-W10 + src/pages/> | wc -l = 0  ✓
```

검증된 보호 paths:
- `cha-bio-safety/src/App.tsx`
- `cha-bio-safety/src/styles/tokens.css`
- `cha-bio-safety/src/styles/typography.css`
- `cha-bio-safety/tailwind.config.js`
- `cha-bio-safety/docs/redesign-context/00-design-context/` (전체)
- `cha-bio-safety/docs/redesign-context/31-chrome/` (13 파일 전체 — wave-1-index.md / GlobalHeader.tsx / SideMenu.tsx / SettingsPanel.tsx / MenuSettingsSection.tsx / sketch-wave-2~5 / design-system.md / tokens.css / typography.css / wave-6-tsx-conversion-checklist.md)
- `cha-bio-safety/src/components/SideMenu.tsx` (W8 책임)
- `cha-bio-safety/src/components/SettingsPanel.tsx` (W9 책임)
- `cha-bio-safety/src/components/MenuSettingsSection.tsx` (W10 책임)
- `cha-bio-safety/src/pages/` (전체)

→ 모두 git diff 0 byte. Cherry-pick 사고 0.

### Build verify

```
cd cha-bio-safety && npx tsc --noEmit
exit code: 0  ✓
```

(npm ci 로 node_modules 564 packages 설치 후 tsc 실행. node_modules 는 gitignored 로 commit 영향 0.)

## Atomic 1-commit 해시 + 1 파일 변경 확인

```
commit e8ffb98bb0a5b37ace45112b5b89e459fa5d5ba0
Date:   Wed May 27 06:01:29 2026 +0900
1 file changed, 8 insertions(+), 23 deletions(-)

cha-bio-safety/src/components/GlobalHeader.tsx | 31 +++++++-------------------
```

→ GlobalHeader.tsx 1 파일만 staged + commit. 다른 파일 0건.

## Decisions Made

1. **OQ #5 LOCKED 유지** — 햄버거 svg path `M4 6h16M4 12h16M4 18h16` verbatim. Lucide Menu 교체 X. precedent 패턴 박제: chrome 통일 룰의 시각 정체성 (브랜드 hamburger glyph) 유지.

2. **OQ #2 부분 격상 — title 13→18 / button 32→48 / svg 15→20 / placeholder 32→48** — W6 §7 노안 룰 (page header = card title 급, 터치 타겟 48pt). title fontWeight 700→600 은 v0.1.1 의 "weight 700 무거움" 룰 (W2 sketch §4 verbatim).

3. **w-12 h-12 (Tailwind 기본 3rem=48px) 채택** — w-8/h-8 (tailwind.config spacing override = 48px) 도 동치이나 sketch §3 verbatim 매치 + memory `feedback_tailwind_w8_h8_is_48px` 회피 위해 명시적 `w-12 h-12` 채택. precedent: `ElevatorPage.tsx:1617`.

4. **svg stroke=currentColor 채택** — button 의 `text-text-secondary` 가 svg stroke 로 상속. 불필요한 `stroke="var(--t2)"` 분기 제거하여 토큰 일관성 + 라이트/다크 자동 분기 보장.

5. **rightSlot 분기는 template literal 로 표현** — `className={\`flex-1 text-title font-semibold text-text-primary ${rightSlot ? 'text-left ml-2' : 'text-center'}\`}`. source 의 `textAlign: rightSlot ? 'left' : 'center'` + `marginLeft: rightSlot ? 8 : 0` 분기 보존 (의미 등가 1 byte 변경 0).

## Deviations from Plan

None — plan executed exactly as written.

(설계상 plan §Step 1 의 작업 디렉토리 경로 `/Users/jykevin/Documents/cbc7119-design/` 는 design worktree precedent 참조용이고, 본 executor 는 worktree `/Users/jykevin/Documents/20260328/.claude/worktrees/agent-af9ebe85e18ce0c8a/` 에서 동일 repo·동일 파일·동일 결과로 실행. 보호 파일 paths · verify gate · atomic 1-commit · 모든 요건 동일 준수. memory deviation 으로 분류 X.)

(부차적 환경 작업: cha-bio-safety/ 에 node_modules 가 부재하여 `npm ci` 로 564 packages 설치 후 `tsc --noEmit` 실행. node_modules 는 .gitignore 됨 — commit 영향 0. 빌드 검증 위한 unavoidable infrastructure step.)

## 다음 wave (W8 SideMenu) 진입 조건 박제

W7 GlobalHeader 변환 완결 → W8 SideMenu.tsx 변환 진입 가능.

W8 precondition checklist:
- [x] W7 GlobalHeader.tsx Tailwind class 변환 + atomic commit
- [x] 보호 파일 17 paths git diff 0 byte (App.tsx / tokens.css / typography.css / tailwind.config.js / 31-chrome/ 13 / 00-design-context/)
- [x] tsc --noEmit 0 errors
- [x] sketch-wave-3-side-menu.html 존재 (보호 파일)
- [x] cha-bio-safety/src/components/SideMenu.tsx 존재 (LIVE source)
- [ ] W8 plan 작성 (별도 quick task — 본 plan 의 책임 X)

W8 변환 시 W7 패턴 mirror:
- Props interface verbatim 보존 (필드 + 주석)
- export function 시그니처 verbatim
- 이벤트 핸들러 (onClick / onClose 등) + aria-label verbatim
- 인라인 style → Tailwind class (W6 §10 cheatsheet 매핑)
- legacy alias var(--bg|bg2|bg3|bg4|bd|bd2|t1|t2|t3|acl) → 의미 토큰
- 인라인 svg path 가 있다면 verbatim 유지 (Lucide 교체 X — OQ #5 LOCKED 동일 적용)
- 노안 룰 격상 (W6 §7)
- 보호 파일 17 paths git diff 0 byte
- tsc --noEmit 0 errors
- atomic 1-commit (1 파일만)

## Precedent 패턴 박제 (W8/W9/W10 mirror 용)

본 W7 변환의 7 핵심 precedent (W8/W9/W10 sub-wave 가 그대로 mirror 할 패턴):

1. **단일 atomic 1-commit 1-파일** — sub-wave 마다 그 컴포넌트 파일만 변경. 보호 파일 17 paths + 형제 컴포넌트 (W7→W8/W9/W10, W8→W7/W9/W10 등) 모두 git diff 0 byte. memory `project_redesign_28_splash_status` / `project_redesign_17_annual_plan_status` 의 cherry-pick 사고 0 precedent 동일 적용.

2. **비즈 anchor verbatim 1 byte 변경 0** — Props interface (필드 + 주석) / export function 시그니처 / 이벤트 핸들러 (onClick / onClose) / aria-label / 인라인 svg path / `??` vs `||` 구분 (nullish vs falsy fallback) / 조건부 분기 (rightSlot ? 'a' : 'b') 등 비즈 로직은 1 byte 변경 0.

3. **W6 §10 Tailwind cheatsheet 변환 매핑** — legacy alias (`var(--bg2|bg3|bd|t1|t2)`) → v0.1.1 토큰 (`--surface-raised|--surface-sunken|--border-default|--text-primary|--text-secondary`) → Tailwind class (`bg-surface-raised|bg-surface-sunken|border-border-default|text-text-primary|text-text-secondary`) 5쌍 일관 적용.

4. **w-12/h-12 = 48px (Tailwind 기본 3rem)** — w-8/h-8 (override 48px) 동치이나 명시적 `w-12 h-12` 채택. memory `feedback_tailwind_w8_h8_is_48px` 함정 회피. 페이지 헤더/햄버거/back 버튼 모두 48 적용.

5. **svg stroke=currentColor** — 컨테이너 (button / div) 의 text-* class 가 svg stroke 로 상속. 토큰 일관성 + 라이트/다크 자동 분기. svg 별도 stroke 분기 제거.

6. **OQ #5 LOCKED 인라인 svg 유지** — chrome 컴포넌트의 시각 정체성 (햄버거 / 닫기 X / back ← 등) 은 verbatim 인라인 svg path 유지. Lucide 교체 X. W7 hamburger 패턴이 W8/W9/W10 의 동일 OQ 룰 precedent.

7. **노안 룰 격상 4건 패턴** — title fontSize 13→18 (`text-title`) + fontWeight 700→600 (`font-semibold`) / button 32→48 (`w-12 h-12 rounded-sm`) / svg 15→20 / placeholder 32→48 (`w-12 symmetry`). chrome 통일 룰 (페이지 헤더 = 카드 제목 급, 터치 타겟 48pt). W8/W9/W10 의 헤더/버튼 노안 룰 동일.

## Self-Check: PASSED

- [x] File exists: `cha-bio-safety/src/components/GlobalHeader.tsx` (30 lines)
- [x] Commit exists: `e8ffb98` (`git log --oneline | grep e8ffb98` → FOUND)
- [x] Diff stat: 1 file changed, 8 insertions(+), 23 deletions(-)
- [x] tsc --noEmit exit 0
- [x] Protected files 17 paths + W8/W9/W10 + src/pages/ git diff 0 byte
- [x] Positive gate (10+ checks) all PASS
- [x] Negative gate (7 checks) all PASS (인라인 style 0 / legacy alias 0 / raw hex 0 / linear-gradient 0 / ✕ 0 / 이모지 0 / fontSize 9/10/11 0)
- [x] 비즈 anchor 7건 verbatim 1 byte 변경 0
- [x] 노안 룰 격상 4건 모두 적용
- [x] SUMMARY.md created at `.planning/quick/260527-85c-redesign-31-chrome-w7-globalheader-tsx-a/260527-85c-SUMMARY.md`
