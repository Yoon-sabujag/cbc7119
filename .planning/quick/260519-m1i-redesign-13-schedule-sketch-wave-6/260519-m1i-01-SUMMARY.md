---
phase: 260519-m1i
plan: 01
subsystem: redesign/13-schedule
tags:
  - sketch
  - schedule
  - excel-download
  - toast-catalog
  - wave-6
requires:
  - cha-bio-safety/docs/redesign-context/13-schedule/sketch-wave-5.html (chrome inheritance)
  - cha-bio-safety/docs/redesign-context/13-schedule/tokens.css
  - cha-bio-safety/docs/redesign-context/13-schedule/typography.css
  - cha-bio-safety/docs/redesign-context/13-schedule/SchedulePage.tsx (verbatim source)
provides:
  - cha-bio-safety/docs/redesign-context/13-schedule/sketch-wave-6.html
affects: []
tech-stack:
  added: []
  patterns:
    - lucide CheckCircle2 / AlertCircle SVG inline 16×16 (이모지 대체)
    - toast-cat-row 카탈로그 컴포넌트 (12-staff W9 mirror)
    - excel-btn-default / excel-btn-loading / excel-btn-tokenized 3-state 버튼
    - meaning-card + oq-patch zone 시각 패턴
    - data-theme dark/light 동시 분기 (5 frame matrix)
key-files:
  created:
    - cha-bio-safety/docs/redesign-context/13-schedule/sketch-wave-6.html
  modified: []
decisions:
  - "W6 OQ #1 — 엑셀 버튼 색 (a) source gradient verbatim vs (b) var(--status-safe-bar) solid 양안 시각 비교, 사용자 답변 대기"
  - "토스트 디자인 12-staff W9 mirror — surface-raised + border-default + radius-md + box-shadow + lucide 16px"
  - "generateMonthlyPlan 의미 = 월간 단발 export (1년치 일괄 아님) 명시 — 사용자 표현 '자동 생성 패널' 정정"
  - "이모지 0 룰 강화 — meta-commentary 의 ✓/! 글리프도 lexicon 으로 대체 (verify gate 통과)"
metrics:
  duration: ~25min
  completed: 2026-05-19T07:03:38Z
  tasks: 1
  files_changed: 1
  lines_added: 1302
---

# Phase 260519-m1i Plan 01: redesign/13-schedule sketch wave 6 Summary

엑셀 다운로드 버튼 상태 시연 + 토스트 13종 카탈로그 + generateMonthlyPlan 의미 카드 — 단일 HTML 시안 1302 라인, 5 frame, 12/12 gate PASS.

## What changed

### Created
- `cha-bio-safety/docs/redesign-context/13-schedule/sketch-wave-6.html` (1302 lines)
  - W1~W5 sketch chrome 100% mirror (tokens.css + typography.css 임베드, data-theme 분기, frame-shell/mobile/desktop)
  - 5 frame matrix
    - Frame A — 모바일 다크 / 엑셀 버튼 default + 토스트 strip 13종 + 의미 카드 + OQ patch zone
    - Frame B — 모바일 다크 / planLoading=true + success/error floating 토스트 시연
    - Frame C — 모바일 라이트 mirror (대표 토스트 + event-dot 라이트 #94a3b8 hardcode 시연)
    - Frame D — 데스크톱 1280 다크 / 액션바 엑셀 버튼 + 2-column 토스트 카탈로그 + planLoading 시연 row
    - Frame E — 데스크톱 1280 라이트 mirror

## Toast 13종 verbatim 매핑 (source line → 카피 → 종류)

### Success 6종
| Line | 카피 | 트리거 |
|---|---|---|
| L209 | 완료 처리됐습니다 | handleStatus(status==='done') |
| L209 | 상태 변경됐습니다 | handleStatus(status!=='done') |
| L216 | 삭제됐습니다 | handleDelete success |
| L229 | 엑셀이 다운로드됐습니다 | handlePlanDownload success |
| L414 | 일정 추가됨 | AddModal onSaved |
| L424 | 수정됐습니다 | EditModal onSaved |

### Error 7종
| Line | 카피 | 트리거 |
|---|---|---|
| L218 | 삭제 중 오류가 발생했습니다 | handleDelete catch (fallback) |
| L231 | 생성 중 오류 | handlePlanDownload catch (fallback) |
| L713 | 날짜를 입력하세요 | AddModal validation (date) |
| L719 | 점검 분류를 선택하세요 | AddModal validation (inspect category) |
| L729 · L977 | 제목을 입력하세요 | AddModal etc / EditModal validation (카피 동일, 카탈로그 1회 노출) |
| L752 | 저장 실패 | AddModal catch |
| L983 | 수정 실패 | EditModal catch |

총 13종 unique 카피 (L729/L977 동일 카피, 1회 노출).

## 엑셀 버튼 상태 시연

| 상태 | 스타일 | 라벨 | 사용처 |
|---|---|---|---|
| default | `linear-gradient(135deg,#15803d,#22c55e)` + `color:#fff` + `cursor:pointer` | `엑셀 다운로드` | Frame A/C 헤더, Frame D/E 액션바 |
| planLoading=true | `background:var(--bg3)` + `color:var(--t3)` + `cursor:default` + `disabled` | `생성 중...` | Frame B 헤더, Frame D planLoading row |
| tokenized (OQ b 변형) | `background:var(--status-safe-bar)` solid | `엑셀 다운로드 (b)` | Frame A OQ patch zone 비교용 |

모두 source line 477~485 (모바일) / 437~441 (데스크톱) 의 markup 을 verbatim 카피.

## generateMonthlyPlan 의미 안내 카드

사용자가 "자동 생성 패널" 이라고 부르던 기능은 별도 UI 패널이 아니라 헤더의 **엑셀 다운로드 버튼 1개** 입니다. 클릭 시 `generateMonthlyPlan(year, month, holidays)` 가 호출되어 **현재 보고 있는 달**의 점검 계획표를 엑셀 파일 1개로 export — source line 224~235 verbatim.

**중요:** 1년치 일괄 자동 생성이 **아님**. 매월 단위 호출.

이 사실을 시안의 meaning-card (Frame A/C/D/E 4개 frame 모두 노출) 로 명시. 향후 PRD/문서 작성 시 "자동 생성 = 월간 단발 export" 로 정정.

## OQ #1 — LOCKED (2026-05-19 사용자 답변)

**OQ #1 ▶ LOCKED b)** — 엑셀 버튼 색 = `var(--status-safe-bar)` solid (디자인 시스템 토큰 일관, W4 OQ #2 LOCKED b 패턴 mirror, 다크 #22c55e / 라이트 #15803d 자동 분기).

**적용 위치 (4곳):**
- `.excel-btn-default` CSS 룰 background: `linear-gradient(135deg,#15803d,#22c55e)` → `var(--status-safe-bar)`
- Frame A/C/D/E 의 엑셀 버튼 4곳 모두 동일 클래스 사용 (자동 적용)
- `.excel-btn-tokenized` 클래스 제거 (중복 제거)
- OQ patch zone (Frame A 안 a/b 비교 영역) 제거 — 더 이상 비교 시각화 불필요

## W1~W5 LOCKED 결정 일관 mirror

- SCHED_CATEGORIES 5 hex 정의 reference 유지 (#a78bfa / #facc15 / #22c55e / #06b6d4 / #94a3b8) — wave 6 본문 카테고리 표시는 없지만 chrome 변수 정의 일관
- 라이트 event #94a3b8 hardcode override — `event-dot-themed` rule 유지 + Frame C/E 에 시연 row 노출
- 이모지 0 — lucide CheckCircle2 + AlertCircle SVG 16×16 inline 만 사용, ✓/! 글리프는 negative-rule meta-commentary 에서도 lexicon 으로 대체
- 저장 버튼 = `var(--accent)` solid (W4 OQ #2 LOCKED b) — wave 6 본문에 저장 버튼 없음, "+추가" 버튼이 accent solid 로 chrome consistency
- BottomSheet 90dvh (W4 OQ #1 LOCKED a) — wave 6 모달 없음, 상수만 유지
- "오늘" 본문 0회 — 검증됨 (grep 0 match)
- status- prefix className 0 — `var(--status-safe)` / `var(--status-danger)` inline style 만
- 9·10·11px font-size 0 — text-caption 12 마지노선 준수

## Verify gate 12/12 PASS log

```
GATE 1: lines=1302 PASS                    (≥ 400)
GATE 2: emoji 0 PASS                       (Python regex 0 match)
GATE 3: small font 0 PASS                  (9·10·11px 0)
GATE 4: status- prefix 0 PASS              (className 검출 0)
GATE 5: category hex...
  #a78bfa PASS
  #facc15 PASS
  #22c55e PASS
  #06b6d4 PASS
  #94a3b8 PASS
GATE 6: light event #94a3b8 PASS           (event-dot-themed rule + hex 검출)
GATE 7: tokens.css PASS                    (--surface-raised + --border-default)
GATE 8: typography.css PASS                (text-body-sm + text-caption)
GATE 9: themes PASS                        (data-theme 다크/라이트 모두)
GATE 10: toast verbatim ALL 13 PASS        (success 6 + error 7 카피 모두)
GATE 11: button labels PASS                ("엑셀 다운로드" + "생성 중...")
GATE 12a: CheckCircle2 PASS                (SVG path 검출)
GATE 12b: AlertCircle PASS                 (SVG path 검출)
GATE 12c: check glyph 0 PASS               (U+2713 / U+2714 0 매치)
---
ALL 12 GATES PASS
```

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] 이모지 0 verify gate 실패 → meta-commentary glyph 제거**
- **Found during:** Task 1 verify gate
- **Issue:** Plan 의 negative rule 설명 텍스트 안에 `✓/!` 글리프가 직접 사용되었음 (file head comment + OQ footer Verify gate 설명 4곳). Python regex `[\U0001F300-\U0001FAFF]|[☀-➿]|[✀-➿]` 가 U+2713 ✓ 글리프를 emoji 로 검출 → Gate 2/12c FAIL.
- **Fix:** "이모지 0 (✓/! 직접 사용 금지)" → "이모지 0 (check/alert glyph 직접 사용 금지)" 로 4건 lexicon 대체. 룰 의미는 동일 유지, file 안에 글리프 직접 사용 0.
- **Files modified:** sketch-wave-6.html (4 lines edited, content semantic 동일)
- **Commit:** f010c29 (single atomic commit — fix 가 task 완성 일부로 통합)
- **교훈:** feedback_tsx_wave_emoji_dot_gap.md 의 "이모지 0" 룰은 narrative/comment 영역에도 strict 적용됨. 다음 wave 부터는 시안 작성 단계에서 lexicon 만 사용하고 글리프는 절대 첨자하지 않을 것.

## Next steps

1. **OQ #1 사용자 답변** → wave 6 LOCK (운영 일관 (a) 추천 / 토큰 일관 (b) 추천 중 택1)
2. **wave 1~6 전체 main 머지** → cbc7119-preview 자동 배포 (디자인 도메인)
3. **TSX 변환 wave 시작** — sketch wave 1~6 의 chrome/카테고리/모달/카탈로그/엑셀 패턴을 SchedulePage.tsx 에 Tailwind 토큰 클래스 + 12-staff W9 mirror toast 컴포넌트로 적용

## Self-Check

- [x] File exists: cha-bio-safety/docs/redesign-context/13-schedule/sketch-wave-6.html (1302 lines)
- [x] Commit exists: f010c29 (verified via `git log --oneline -3`)
- [x] All 12 verify gates PASS
- [x] 13 toast verbatim copies confirmed
- [x] W1~W5 LOCKED inheritance verified

## Self-Check: PASSED
