---
phase: quick-260515-p3v
plan: 01
type: execute
wave: 1
depends_on: [260515-n7q]
files_modified:
  - cha-bio-safety/src/pages/ElevatorPage.tsx
autonomous: true
requirements:
  - QUICK-260515-p3v
must_haves:
  truths:
    - "고장 탭 본문 변환 영역 (line 1143~1202) 내 인라인 style 속성 0건 + var(--bg2/bd/bd2/t1/t2/t3/bg3) 사용 0건 + 9·10·11px 폰트 0건 + 본문 이모지 0건 (✅, 🚨 제거)"
    - "RepairListSection 본체 (line 2821~2946) 내 인라인 style 속성 0건 + 모든 var() 인라인 0건 + 9·10·11px 0건 + 본문 이모지 0건 (✅, 🔧, ⏳, 📷 제거)"
    - "FAB 버튼 영역 (line 1610~1625) 내 인라인 style 0건 + 이모지 0건 (🚨, 🔧 제거) + 수리 FAB 그라디언트 warning yellow(#854d0e/#eab308) → accent 그라디언트 (sketch 결정)"
    - "lucide-react import 에 추가 아이콘 보장: CheckCircle2 + AlertTriangle + Wrench + Camera + Loader2 (Camera 는 Wave 7 이미 추가됨, 나머지는 코드 사용처 확인 후 추가)"
    - "TYPE_ICON 사용처는 TYPE_ICON_COMPONENT 매퍼로 교체 (Wave 1 패턴) — 고장 카드 line 1158, 수리 카드 line 2887/2888 의 인라인 이모지 매퍼 제거"
    - "EmptyState 호출의 icon prop 이모지 → lucide 아이콘 매핑 (EmptyState 시그니처 변경 필요 — icon: string → icon: React.ReactNode). 호출처 3건 모두 lucide 컴포넌트 전달: 고장 빈상태=<CheckCircle2/>, 수리 빈상태=<Wrench/>, 수리 로딩=<Loader2 className='animate-spin'/>"
    - "inputSt 글로벌 상수 (line 2791) 사용처는 RepairListSection 의 select/input 3건 — Tailwind className 으로 대체 후 inputSt 글로벌 상수 정의 자체 제거"
    - "RepairListSection 의 sourceType 칩 색 = SOURCE_TYPE_LABEL.color 그대로 보존 (코드 데이터 권위 — 사용자 컨펌 메모리 룰)"
    - "sketch (fault-repair-lists-sketch.html commit 0d52f0b) 의 4 viewport 색/스페이싱/아이콘 결정 1:1 매핑 source. 변환 후 사용자 인지 sketch 와 일치"
    - "비즈니스 로직 100% 보존: faults.map / repairs.map / filteredElevators / useQuery / setExpandedId / setSelectedFault / setModal / deleteRecord / elevatorRepairApi / qc.invalidateQueries / setEditRepair / toast / renderPhotos 모두 한 줄도 수정 X"
    - "props 시그니처 보존: RepairListSection ({ elevators, navigate }), RepairImageViewer ({ src, onClose }), EmptyState ({ icon, text }) 모두 호출처 0건 영향. EmptyState 만 icon: string → icon: React.ReactNode 시그니처 변경 — 모든 호출처 ElevatorPage.tsx 내부 만 (외부 컴포넌트 없음, grep 으로 검증)"
    - "TypeScript 컴파일 0 에러 (`npm run build` PASS)"
    - "다른 컴포넌트/페이지 0건 수정 — git status 에 ElevatorPage.tsx + PLAN + SUMMARY 만 표시"
    - "Wave 1~7 변환 결과 단 한 줄도 수정 X (list 탭 / TYPE_ICON_COMPONENT / EvSelector / EsBtn / EsNodeMap / Fault 3 모달 / RepairNewModal / EvDetailModal / 헬퍼 4종 변환된 본체)"
  artifacts:
    - path: "cha-bio-safety/src/pages/ElevatorPage.tsx"
      provides: "옵션 B 3A 변환 — 고장 탭 + 수리 탭(RepairListSection) v0.1.1 토큰 + Tailwind + lucide"
      contains: "function ElevatorPage / function RepairListSection / FAB 버튼 영역"
  key_links:
    - from: "cha-bio-safety/src/pages/ElevatorPage.tsx"
      to: "cha-bio-safety/docs/redesign-context/07-elevator/sketch/fault-repair-lists-sketch.html"
      via: "1:1 매핑 — 4 viewport 색/스페이싱/아이콘 결정 source"
      pattern: "fault-repair-lists-sketch"
---

<objective>
redesign/07-elevator 옵션 B 첫 변환 wave (Wave 9) — sketch (commit 0d52f0b, fault-repair-lists-sketch.html) 권위로 ElevatorPage.tsx 의 고장 탭 본문 + RepairListSection 본체 + FAB 버튼 3 영역 v0.1.1 토큰 + Tailwind + lucide 변환.

Purpose: 옵션 B 3A sketch 사용자 검수 OK. 1:1 매핑 변환. 옵션 B 1/3 묶음 처리. 다음 wave: 3B sketch (점검+검사) → 변환 → 3C sketch (안전관리자) → 변환.

Output: ElevatorPage.tsx 단일 파일 변경. 약 +120/-180 줄 추정. 다른 파일 0건. sketch 0건 변경 (별도 wave).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/quick/260515-p3v-redesign-07-elevator-tsx-wave-9-fault-repair-lists/

# 메모리 권위
# - project_redesign_07_elevator_status.md → Wave 9 = 옵션 B 3A 변환
# - feedback_redesign_sketch_rule_enforcement.md → sketch 권위 + 4중 verify gate
# - feedback_sketch_realistic_data.md → 비즈니스 로직 100% 보존, 시각만 손봄
# - feedback_inspection_unresolved_color.md → 미해결 = fire
# - project_design_tokens_branch.md → v0.1.1 시각 토큰

# 직전 변환 (참고만, 수정 X)
# - Wave 1~7 모두 보존 (list 탭 + 5 모달 + 헬퍼 4종)

# sketch 권위 (1:1 매핑 source)
# - 0d52f0b fault-repair-lists-sketch.html (1837 라인)
# - 4 viewport: 모바일다크/라이트 + 데스크톱다크/라이트
# - 카드 변형 + 필터 + 펼침 + 빈/로딩 + FAB + admin + sourceType 카탈로그
# - 색 매핑: 미해결=fire / 수리완료=safe / 승객=danger / 발생층=info / FAB 고장=fire 그라디언트 / FAB 수리=accent 그라디언트
# - 아이콘 매핑: TYPE_ICON 4종 매퍼 + CheckCircle2 / AlertTriangle / Wrench / Camera / Loader2 / ChevronRight

# 코드 source (Read 검증 완료, 2026-05-15)
# - 고장 탭 본문 (line 1143~1202) ~60줄: 인라인 var() + ✅🚨 이모지
# - 수리 탭 호출 (line 1205~1207): 한 줄 (RepairListSection 호출만, 변환 영향 0)
# - FAB 영역 (line 1610~1625): 인라인 그라디언트 + 🚨🔧 이모지. 수리 FAB warning yellow → accent (sketch 결정)
# - RepairListSection 본체 (line 2821~2946) ~125줄: 필터 바 + 카드 list + 펼침 + 빈/로딩 + admin
# - inputSt 글로벌 상수 (line 2791): RepairListSection select/input 3건 사용 (line 2866/2871/2875) — Tailwind 으로 대체 후 글로벌 상수 정의 제거
# - EmptyState 컴포넌트 (line 2539): icon: string prop. 시그니처 변경 필요 (icon: React.ReactNode)
# - EmptyState 호출처: ElevatorPage 내부만 (외부 0건, grep 으로 검증). 고장 빈상태 + 수리 빈상태 + 수리 로딩 3건

@cha-bio-safety/src/pages/ElevatorPage.tsx
@cha-bio-safety/docs/redesign-context/07-elevator/sketch/fault-repair-lists-sketch.html
@cha-bio-safety/tailwind.config.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: 3 영역 v0.1.1 토큰 + Tailwind + lucide 변환</name>
  <files>cha-bio-safety/src/pages/ElevatorPage.tsx</files>
  <action>
sketch (0d52f0b) 권위 1:1 매핑. 3 영역 변환:

### 1. lucide-react import 확장 (line 16)

기존 (Wave 7 후):
```ts
import { Package, UtensilsCrossed, MoveDiagonal, ChevronRight, ChevronUp, ChevronDown, AlertTriangle, Wrench, X, AlertOctagon, ClipboardCheck, FileSearch, CheckCircle2, Camera } from 'lucide-react'
```

추가: `Loader2` (Wrench/CheckCircle2/AlertTriangle/Camera 이미 있음).

```ts
import { Package, UtensilsCrossed, MoveDiagonal, ChevronRight, ChevronUp, ChevronDown, AlertTriangle, Wrench, X, AlertOctagon, ClipboardCheck, FileSearch, CheckCircle2, Camera, Loader2 } from 'lucide-react'
```

### 2. EmptyState 시그니처 변경 (line 2539)

기존:
```tsx
function EmptyState({ icon, text }: { icon:string; text:string }) {
  return (
    <div className="flex flex-col items-center justify-center py-10 gap-2.5">
      <div className="text-[36px]">{icon}</div>
      <div className="text-body-sm text-text-tertiary">{text}</div>
    </div>
  )
}
```

→ 변경:
```tsx
function EmptyState({ icon, text }: { icon:React.ReactNode; text:string }) {
  return (
    <div className="flex flex-col items-center justify-center py-10 gap-2.5">
      <div className="text-text-tertiary">{icon}</div>
      <div className="text-body-sm text-text-tertiary">{text}</div>
    </div>
  )
}
```

- icon: string → icon: React.ReactNode
- `<div className="text-[36px]">` (이모지 fontSize:36) 제거 — lucide 아이콘은 size prop 으로 사이즈 결정
- text-text-tertiary 색 통일

### 3. EmptyState 호출처 변경 (3건)

ElevatorPage.tsx 내부 grep 으로 모든 호출 검색:
- line 1145 (고장 빈상태): `<EmptyState icon="✅" text="고장 기록이 없어요" />` → `<EmptyState icon={<CheckCircle2 size={36} />} text="고장 기록이 없어요" />`
- RepairListSection 내 (line 2878/2879 추정): `<EmptyState icon="⏳" text="불러오는 중..." />` + `<EmptyState icon="🔧" text="수리 내역이 없어요" />` → `<EmptyState icon={<Loader2 size={36} className="animate-spin" />} ...` + `<EmptyState icon={<Wrench size={36} />} ...`

외부 호출처 확인:
```bash
grep -rn "<EmptyState" cha-bio-safety/src/
```
ElevatorPage.tsx 외 0건 보장 — 시그니처 변경 안전.

### 4. 고장 탭 본문 변환 (line 1143~1202)

sketch VP1/VP2/VP4 의 카드 변형 1:1 매핑:

기존 인라인 카드 (line 1153~1198):
```tsx
<div key={f.id} style={{ background:'var(--bg2)', border:..., borderRadius:12, padding:'10px 12px', display:'flex', alignItems:'center', gap:10, minHeight:72 }}>
  {/* 아이콘 */}
  <div style={{ width:52, height:52, ... fontSize:36 }}>
    {TYPE_ICON[f.elevator_type]}
  </div>
  {/* 호기+발생층 / 증상 */}
  <div style={{ flex:1, ... }}>
    <div style={{ display:'flex', alignItems:'center', gap:6 }}>
      <span style={{ fontSize:14, fontWeight:700, color:'var(--t1)' }}>{f.elevator_number}호기</span>
      {!isEs && floorLabel && (
        <span style={{ fontSize:11, fontWeight:700, color:'var(--info)', background:'rgba(14,165,233,.12)', padding:'2px 7px', borderRadius:8 }}>{floorLabel}</span>
      )}
      {!isEs && passengerMatch && (
        <span style={{ fontSize:10, fontWeight:700, color:'var(--danger)', background:'rgba(239,68,68,.12)', padding:'2px 6px', borderRadius:8 }}>승객🚨</span>
      )}
    </div>
    <div style={{ fontSize:13, color:'var(--t2)', overflow:..., whiteSpace:'nowrap' }}>{pureSymptoms}</div>
    <div style={{ fontSize:10, color:'var(--t3)' }}>{fmtKstDateTime(f.fault_at)}</div>
  </div>
  {/* 수리내역 or 수리 중 */}
  <div style={{ flexShrink:0, maxWidth:90, textAlign:'right' }}>
    {f.is_resolved && f.repair_detail ? <span ...>{f.repair_detail}</span> : <span ...>고장 수리 중</span>}
  </div>
  {/* 수리완료 버튼 or ✅ */}
  {!f.is_resolved ? <button ...>{'수리\n내용\n입력'}</button> : <div ...>✅ + 삭제btn</div>}
</div>
```

→ Tailwind 변환 (sketch 권위):
- 카드 wrapper: `relative bg-surface-raised rounded-xl px-3 py-2.5 flex items-center gap-2.5 min-h-[72px]` + 좌측 색바 (미해결=fire / 수리완료=safe) `before:absolute before:left-0 before:top-0 before:bottom-0 before:w-[3px] before:rounded-l-xl` + 미해결=`before:bg-fire-bar border border-fire-bar/40` / 수리완료=`before:bg-safe-bar border border-border-default`
- TYPE_ICON → TYPE_ICON_COMPONENT 매퍼: `{(() => { const Icon = TYPE_ICON_COMPONENT[f.elevator_type]; return <Icon size={32} className="text-text-secondary" /> })()}` (wrapper div: `w-[52px] h-[52px] flex-shrink-0 flex items-center justify-center`)
- 호기 라벨: `text-body font-bold text-text-primary` (14px → text-body)
- 발생층 칩: `text-caption font-bold text-info bg-info-bg border border-info-bar rounded-md px-1.5 py-0.5` (11px → text-caption)
- 승객 칩 (이모지 🚨 제거): `text-caption font-bold text-danger bg-danger-bg border border-danger-bar rounded-md px-1.5 py-0.5 flex items-center gap-1` + `<AlertTriangle size={12} />` 텍스트 "탑승"
- 증상: `text-body-sm text-text-secondary overflow-hidden text-ellipsis whitespace-nowrap`
- 시각: `text-caption text-text-tertiary` (10px → text-caption)
- 수리내역/수리중: `text-body-sm font-semibold` (safe=수리완료 / danger=고장 수리 중). maxWidth → `max-w-[90px] text-right shrink-0 leading-snug`
- 수리완료 버튼: `flex-shrink-0 w-[52px] h-[52px] rounded-lg border-0 cursor-pointer bg-safe-bg text-safe text-caption font-bold whitespace-pre-line text-center leading-snug` (수리\n내용\n입력)
- ✅ → `<CheckCircle2 size={32} className="text-safe" />` (wrapper: `flex-shrink-0 w-[52px] h-[52px] flex flex-col items-center gap-1`)
- admin 삭제 btn: `text-[10px] text-danger bg-transparent border-0 cursor-pointer font-semibold opacity-60 hover:opacity-100` — 9px → text-caption 또는 text-[10px] (sketch 권위 확인)

### 5. FAB 영역 변환 (line 1610~1625)

기존 인라인 wrapper:
```tsx
<div style={{ flexShrink:0, padding:'8px 12px', background:'var(--bg)' }}>
  {tab === 'fault' && <button style={{ ... background:'linear-gradient(135deg,#991b1b,#ef4444)', ... }}>🚨 고장 접수</button>}
  {tab === 'repair' && <button style={{ ... background:'linear-gradient(135deg,#854d0e,#eab308)', ... }}>🔧 수리 기록 입력</button>}
</div>
```

→ Tailwind 변환 (sketch 권위):
- wrapper: `flex-shrink-0 px-3 py-2 bg-surface-page`
- 고장 FAB: `w-full h-13 rounded-2xl border-0 cursor-pointer text-body font-bold text-white shadow-fire flex items-center justify-center gap-2` + `style={{ background: 'linear-gradient(135deg, var(--c-fire-grad-from), var(--c-fire-grad-to))' }}` (또는 Tailwind 정의된 fire 그라디언트 토큰 — bg-gradient-fire) + 🚨 → `<AlertTriangle size={18} />` + 텍스트 "고장 접수"
- 수리 FAB: 동일 패턴이지만 **accent 그라디언트** (sketch 결정 — 옛 warning yellow 에서 변경): `style={{ background: 'linear-gradient(135deg, var(--c-accent-grad-from), var(--c-accent-grad-to))' }}` 또는 `bg-gradient-accent` + 🔧 → `<Wrench size={18} />` + 텍스트 "수리 기록 입력"
- shadow: `shadow-fire` / `shadow-accent` 토큰 (tailwind.config.js 정의 확인 후 인라인 boxShadow 사용 가능)

**고장 FAB 그라디언트 색 토큰**: 기존 `#991b1b,#ef4444` 는 fire 그라디언트. 다른 모달 (FaultNew CTA) 와 동일 — Wave 3 변환 시 sketch 권위 결정 그대로 사용.

**수리 FAB 그라디언트 색**: sketch 에서 accent 그라디언트 결정. Wave 5 EvDetail 의 .ev-btn-selected 또는 RepairNewModal CTA bg-accent 와 동일 톤. 정확한 값은 sketch CSS 토큰 확인 후 적용.

### 6. RepairListSection 본체 변환 (line 2821~2946) ~125줄

sketch VP2/VP3 의 카드 + 필터 + 펼침 시각 1:1 매핑:

**(a) 필터 바 (line 2865~2876)**
- wrapper: `flex gap-1.5 flex-wrap mb-2 flex-shrink-0`
- evType select / filterEv select / keyword input — 모두 `inputSt` 글로벌 상수 사용 → Tailwind 으로 대체:
  - 공통 className: `h-9 px-2 rounded-lg bg-surface-sunken border border-border-default text-text-primary text-caption focus:border-border-strong outline-none transition box-border`
  - evType: `flex-[1_1_90px]`
  - filterEv: `flex-[1_1_100px]`
  - keyword: `flex-[2_1_100px]`
- 11px → text-caption (12px 격상)

**(b) 빈 상태 / 로딩 상태 (line 2878~2879)** — EmptyState 호출 변경 (위 #3 참조)

**(c) 카드 헤더 (접힌 상태, line 2884~2902)**
- wrapper: `bg-surface-raised border border-border-default rounded-xl overflow-hidden flex-shrink-0 mb-0.5`
- 클릭 영역: `px-3 py-2.5 flex items-center gap-2 cursor-pointer`
- TYPE_ICON → TYPE_ICON_COMPONENT 매퍼 (Wave 1 패턴):
  - 기존 line 2887/2888: 이모지 `{TYPE_ICON[r.elevatorType] ?? '🔧'}` (22px)
  - → `{(() => { const Icon = TYPE_ICON_COMPONENT[r.elevatorType]; return Icon ? <Icon size={22} className="text-text-secondary" /> : <Wrench size={22} className="text-text-secondary" /> })()}`
  - wrapper: `w-9 h-9 flex-shrink-0 flex items-center justify-center`
- 호기 라벨 (line 2892): `text-body-sm font-bold text-text-primary` (13px → body-sm)
- sourceType 칩 (line 2893): 코드 데이터 `st.color` 보존 — 인라인 style 유지 (화이트리스트 — 데이터 의존). `text-[10px] font-bold py-0.5 px-1.5 rounded` + `style={{ background: st.color + '18', color: st.color }}` (10px 격상 → text-caption 또는 화이트리스트 유지)
  - **결정**: 10px 폰트는 격상 (text-caption 12px), 색은 인라인 style 화이트리스트 보존 (sourceType.color 데이터 권위)
- target 칩 (line 2894): `text-caption font-bold py-0.5 px-1.5 rounded bg-surface-sunken text-text-tertiary`
- title (line 2896): `text-caption text-text-primary mt-0.5 font-semibold overflow-hidden text-ellipsis whitespace-nowrap` (12px → caption)
- 시각/회사/사진 표시 (line 2897~2900): `text-[10px] text-text-tertiary mt-px` → `text-caption text-text-tertiary mt-px` (10px → caption)
- 📷 이모지 (line 2899) → `<Camera size={10} className="inline-block align-text-bottom" />` + 문자열 ' · ' 보존
- ChevronRight (line 2902 inline SVG): lucide `<ChevronRight size={14} className={['flex-shrink-0 text-text-tertiary transition-transform duration-150', isExpanded ? 'rotate-90' : ''].join(' ')} />`

**(d) 펼친 영역 (line 2905~2939)**
- wrapper: `px-3 pb-3 border-t border-border-default`
- detail 텍스트 (line 2907~2909): `pt-2.5 text-caption text-text-secondary leading-relaxed whitespace-pre-wrap` (12px → caption — sketch 결정 확인)
- renderPhotos 함수 (line 2842~2858) 본체 변환:
  - 라벨: `text-[10px] font-bold text-text-tertiary mb-1` → `text-caption font-bold text-text-tertiary mb-1`
  - 사진 row: `flex gap-1 overflow-x-auto`
  - 이미지: `w-14 h-14 object-cover rounded-md border border-border-default cursor-pointer flex-shrink-0`
- 수정/삭제 버튼 row (line 2914~2937):
  - wrapper: `flex gap-2 mt-2.5`
  - 수정 btn (ghost-info): `flex-1 py-2 rounded-lg bg-info-bg border border-info-bar text-info text-caption font-semibold cursor-pointer`
  - 삭제 btn (ghost-danger): `flex-1 py-2 rounded-lg bg-danger-bg border border-danger-bar text-danger text-caption font-semibold cursor-pointer`

### 7. inputSt 글로벌 상수 제거 (line 2791)

기존 정의:
```tsx
const inputSt: React.CSSProperties = {
  ...
}
```

→ 제거 (RepairListSection 의 3건 사용처 Tailwind 으로 교체 완료 후).

확인: `grep -c 'inputSt' src/pages/ElevatorPage.tsx` = 0 보장 (정의 + 모든 사용처 제거).

### 보존 항목 (절대 건드리지 말 것)

- Wave 1~7 변환 결과 (list 탭, 5 모달, 헬퍼 4종)
- 비즈니스 로직 (state, handler, mutation, query, fetch, navigation, modal toggle, photo upload)
- TYPE_ICON 글로벌 상수 정의 자체 — 다른 곳에서 사용되면 유지. 본 변환 영역에서만 매퍼로 교체.
  - 확인: `grep -c 'TYPE_ICON\b' src/pages/ElevatorPage.tsx` — 사용처 0건 되면 TYPE_ICON 정의도 제거 (Wave 1 처럼 새 매퍼만 사용)
- SOURCE_TYPE_LABEL 데이터 + .color (sourceType 칩 색 권위)
- RepairImageViewer 본체 (line 2949~) — sketch 스코프 외, 다른 wave 처리
- 다른 페이지/파일, icons.tsx, tailwind.config.js

### 작업 순서 (안전)

1. lucide-react import 에 Loader2 추가 (line 16)
2. EmptyState 시그니처 변경 (line 2539) + 3 호출처 lucide 컴포넌트 전달
3. 고장 탭 본문 변환 (line 1143~1202) — sketch VP1/VP4 1:1
4. FAB 영역 변환 (line 1610~1625) — sketch VP1/VP2 1:1, 수리 FAB warning → accent 그라디언트
5. RepairListSection 본체 변환 (line 2821~2946) — sketch VP2/VP3 1:1
   - 5.1: 필터 바
   - 5.2: 빈/로딩 상태 호출
   - 5.3: 카드 헤더 (접힘)
   - 5.4: 펼친 영역 + renderPhotos + 수정/삭제 btn
6. inputSt 글로벌 상수 제거 (line 2791)
7. TYPE_ICON 사용처 0건 되면 정의도 제거
8. `npm run build` PASS 확인 (TS 0 에러)
9. grep gate 통과 확인 (verify 블록)
10. commit
  </action>
  <verify>
    <automated>
cd cha-bio-safety && \
  echo "=== Section A: 인라인 var() 0건 (변환 영역) ===" && \
  echo "var(--bg2): $(grep -c 'var(--bg2)' src/pages/ElevatorPage.tsx) (expect 0)" && \
  echo "var(--bd): $(grep -c 'var(--bd)' src/pages/ElevatorPage.tsx) (expect 0)" && \
  echo "var(--bd2): $(grep -c 'var(--bd2)' src/pages/ElevatorPage.tsx) (expect 0)" && \
  echo "var(--t1): $(grep -c 'var(--t1)' src/pages/ElevatorPage.tsx) (expect 0)" && \
  echo "var(--t2): $(grep -c 'var(--t2)' src/pages/ElevatorPage.tsx) (expect 0)" && \
  echo "var(--t3): $(grep -c 'var(--t3)' src/pages/ElevatorPage.tsx) (expect 0)" && \
  echo "var(--bg3): $(grep -c 'var(--bg3)' src/pages/ElevatorPage.tsx) (expect 0)" && \
  echo "" && \
  echo "=== Section B: 이모지 제거 ===" && \
  echo "✅ emoji: $(grep -c '✅' src/pages/ElevatorPage.tsx) (expect 0)" && \
  echo "🚨 emoji: $(grep -c '🚨' src/pages/ElevatorPage.tsx) (expect 0)" && \
  echo "🔧 emoji: $(grep -c '🔧' src/pages/ElevatorPage.tsx) (expect 0)" && \
  echo "⏳ emoji: $(grep -c '⏳' src/pages/ElevatorPage.tsx) (expect 0)" && \
  echo "📷 emoji: $(grep -c '📷' src/pages/ElevatorPage.tsx) (expect 0)" && \
  echo "" && \
  echo "=== Section C: lucide 추가 ===" && \
  echo "Loader2: $(grep -c 'Loader2' src/pages/ElevatorPage.tsx) (expect >= 2 — import + 사용)" && \
  echo "Camera: $(grep -c 'Camera' src/pages/ElevatorPage.tsx) (expect >= 3 — import + 2 사용)" && \
  echo "Wrench: $(grep -c 'Wrench' src/pages/ElevatorPage.tsx) (expect >= 3 — import + 2 사용)" && \
  echo "" && \
  echo "=== Section D: inputSt 글로벌 제거 ===" && \
  echo "inputSt total: $(grep -c 'inputSt' src/pages/ElevatorPage.tsx) (expect 0)" && \
  echo "" && \
  echo "=== Section E: EmptyState 시그니처 ===" && \
  grep -c 'icon:string' src/pages/ElevatorPage.tsx && \
  echo "icon: React.ReactNode: $(grep -c 'icon:React.ReactNode\\|icon: React.ReactNode' src/pages/ElevatorPage.tsx) (expect >= 1)" && \
  echo "" && \
  echo "=== Section F: 9·10·11px 격상 (text-[Xpx] 잔존 확인) ===" && \
  echo "text-[10px]: $(grep -c 'text-\\[10px\\]' src/pages/ElevatorPage.tsx) (sketch admin 삭제 btn 등 화이트리스트)" && \
  echo "text-[9px]: $(grep -c 'text-\\[9px\\]' src/pages/ElevatorPage.tsx) (expect 0)" && \
  echo "text-[11px]: $(grep -c 'text-\\[11px\\]' src/pages/ElevatorPage.tsx) (expect 0)" && \
  echo "" && \
  echo "=== Hard gate ===" && \
  test $(grep -cE 'var\\(--(bg2|bd|bd2|t1|t2|t3|bg3)\\)' src/pages/ElevatorPage.tsx) -eq 0 && \
  test $(grep -c '✅' src/pages/ElevatorPage.tsx) -eq 0 && \
  test $(grep -c '🚨' src/pages/ElevatorPage.tsx) -eq 0 && \
  test $(grep -c '🔧' src/pages/ElevatorPage.tsx) -eq 0 && \
  test $(grep -c '⏳' src/pages/ElevatorPage.tsx) -eq 0 && \
  test $(grep -c '📷' src/pages/ElevatorPage.tsx) -eq 0 && \
  test $(grep -c 'inputSt' src/pages/ElevatorPage.tsx) -eq 0 && \
  echo "" && \
  echo "=== npm build ===" && \
  npm run build 2>&1 | tail -10 && \
  echo "=== build PASS ==="
    </automated>
  </verify>
  <done>
- grep gate Section A~F 모두 PASS (var() 0 / 이모지 0 / inputSt 0 / Loader2/Wrench import 보장)
- EmptyState 시그니처 icon: React.ReactNode 변경 + 3 호출처 lucide 컴포넌트 전달
- 고장 탭 본문 + FAB + RepairListSection 본체 sketch 1:1 매핑 변환
- 수리 FAB 그라디언트 warning yellow → accent (sketch 결정)
- npm run build PASS (TypeScript 0 에러)
- 다른 파일 0건 변경 (`git status` 에 ElevatorPage.tsx + PLAN.md + SUMMARY.md 만)
- Wave 1~7 변환 결과 단 한 줄도 시각 결정 변경 X
  </done>
</task>

<task type="auto">
  <name>Task 2: SUMMARY 작성 + commit</name>
  <files>.planning/quick/260515-p3v-redesign-07-elevator-tsx-wave-9-fault-repair-lists/260515-p3v-SUMMARY.md</files>
  <action>
SUMMARY 작성 후 single commit. SUMMARY 는 orchestrator 가 별도 docs commit 처리할 수 있도록 untracked 유지 가능 (executor 가 commit 안 함).

### SUMMARY 필수 섹션
- **What changed**: ElevatorPage.tsx 3 영역 sketch 1:1 변환. (1) 고장 탭 본문 카드 (좌측 색바 fire/safe + TYPE_ICON_COMPONENT + 발생층 info 칩 + 승객 danger 칩 + 수리내역/수리중 + 수리완료 btn + 미해결 ✅→CheckCircle2 + admin 삭제). (2) FAB 영역 (고장 fire 그라디언트 + 수리 accent 그라디언트 — 옛 warning yellow 에서 변경. 이모지 🚨→AlertTriangle / 🔧→Wrench). (3) RepairListSection 본체 (필터 바 Tailwind / TYPE_ICON_COMPONENT 매퍼 / sourceType 칩 데이터 색 보존 / 펼침 + 4단계 사진 row + 수정/삭제 ghost btn / ChevronRight lucide / 📷→Camera 인라인 / 빈상태 Wrench / 로딩 Loader2 spin). 부가: EmptyState icon: string → React.ReactNode 시그니처 변경 + 3 호출처 lucide 전달. inputSt 글로벌 제거. lucide Loader2 추가.
- **Why**: 옵션 B 3A sketch (0d52f0b) 사용자 검수 OK. 1:1 매핑 변환. 옵션 B 1/3 묶음 처리.
- **Preserved**: 비즈니스 로직 100% (state/handler/mutation/query/photo upload/admin 분기). props 시그니처. Wave 1~7 변환 결과. SOURCE_TYPE_LABEL.color 데이터 권위. RepairImageViewer 본체.
- **Verification**: grep gate Section A~F 모두 PASS, npm build PASS, git status ElevatorPage.tsx + PLAN + SUMMARY 만.
- **Out of scope**: 3B (점검+검사), 3C (안전관리자) 별도 wave.
- **Visual impact**: 사용자 인지 sketch (commit 0d52f0b) 4 viewport 와 일치. 수리 FAB 색 변경 (warning yellow → accent — sketch 결정).
- **Next**: 사용자 검수 → main 머지 → 3B sketch.

### Commit (code only)

executor 가 ElevatorPage.tsx 만 single commit:
```
feat(260515-p3v): Wave 9 — 옵션 B 3A 변환 (고장 탭 + 수리 탭 RepairListSection + FAB) v0.1.1 토큰 + Tailwind + lucide
```

SUMMARY/PLAN docs commit 은 orchestrator 가 worktree merge 후 처리.
  </action>
  <verify>
    <automated>
cd /Users/jykevin/Documents/cbc7119-design && \
  ls .planning/quick/260515-p3v-redesign-07-elevator-tsx-wave-9-fault-repair-lists/260515-p3v-SUMMARY.md
    </automated>
  </verify>
  <done>
- SUMMARY.md 작성 완료 (untracked or committed depending on orchestrator policy)
- single code commit (ElevatorPage.tsx 만)
- redesign/07-elevator push
  </done>
</task>

</tasks>

<success_criteria>
- [ ] lucide-react import 에 Loader2 추가
- [ ] EmptyState 시그니처 icon: React.ReactNode + 3 호출처 lucide 전달
- [ ] 고장 탭 본문 line 1143~1202 변환 — 좌측 색바 fire/safe / TYPE_ICON_COMPONENT / 발생층 info 칩 / 승객 AlertTriangle 칩 / CheckCircle2 + admin 삭제
- [ ] FAB 변환 — 고장 fire 그라디언트 / 수리 **accent 그라디언트** (옛 warning yellow 에서 변경) / 이모지 → lucide
- [ ] RepairListSection 본체 line 2821~2946 변환 — 필터 바 Tailwind / TYPE_ICON_COMPONENT / sourceType 칩 데이터 색 보존 / 펼침 + 4단계 사진 / 수정/삭제 ghost btn / ChevronRight lucide / Camera 인라인
- [ ] inputSt 글로벌 상수 정의 + 사용처 0건
- [ ] var(--bg2/bd/bd2/t1/t2/t3/bg3) 사용처 0건
- [ ] ✅/🚨/🔧/⏳/📷 이모지 0건
- [ ] 9·11px 폰트 0건 (10px 화이트리스트 sketch 확인)
- [ ] Wave 1~7 변환 결과 단 한 줄도 변경 X
- [ ] TypeScript 0 에러, npm build PASS
- [ ] SUMMARY 작성 + single commit + push
</success_criteria>

<output>
After completion, ensure `.planning/quick/260515-p3v-redesign-07-elevator-tsx-wave-9-fault-repair-lists/260515-p3v-SUMMARY.md` exists.
</output>
