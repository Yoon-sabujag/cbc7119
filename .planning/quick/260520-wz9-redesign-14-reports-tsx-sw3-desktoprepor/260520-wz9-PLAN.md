---
phase: quick-260520-wz9
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cha-bio-safety/src/pages/ReportsPage.tsx
autonomous: true
requirements:
  - REDESIGN-14-SW3
user_setup: []

must_haves:
  truths:
    - "DesktopReportsPage (line 149~304) 의 인라인 style 가 SW1 components.css class 로 대체된다 (className ≥30, 인라인 style ≤8)"
    - "SELECT_STYLE 상수 (line 306~313) 가 삭제되고 .toolbar-select class 로 대체된다 (W7 OQ #2 LOCKED)"
    - "linear-gradient(135deg,#1d4ed8,#2563eb) 일괄 다운로드 버튼 그라데이션이 폐기되고 .toolbar-batch-btn (var(--status-safe-bar)) 으로 대체된다 (W1 OQ #3 LOCKED)"
    - "DESKTOP_SECTIONS 섹션 B 라벨 '소화전 · 가스 · 비상콘센트' 의 가운뎃점 2개가 dot-meta span 2개로 변환된다"
    - "비즈 로직 (handleDownload / handleDownloadAll / setSelectedType / hoverType / selectedCard / ExcelPreview prop) 시그니처가 1 byte 변경 없이 보존된다"
    - "MobileReportsPage / SELECT_STYLE 외 상수 / REPORT_CARDS / MATRIX_CONFIG / downloadReport / generateReportBlob / downloadAllAsZip / DESKTOP_SECTIONS / default export 영역은 변경되지 않는다 (SW2 처리분만 예외)"
    - "tsc --noEmit + npm run build 모두 exit 0, chunk size delta ≤ ±5KB"
  artifacts:
    - path: "cha-bio-safety/src/pages/ReportsPage.tsx"
      provides: "DesktopReportsPage v0.1.1 Tailwind 재작성 (toolbar 9 class + sidelist 8 class + preview-wrapper 1 class 모두 SW1 components.css 참조)"
      contains: "className=\"toolbar\""
    - path: ".planning/quick/260520-wz9-redesign-14-reports-tsx-sw3-desktoprepor/260520-wz9-SUMMARY.md"
      provides: "SW3 완결 + W7 14-reports 변환 wave 마지막 sub-wave 종료 선언, 변환 매핑 표, verify gate 결과, 비즈 로직 보존 체크, chunk size delta"
  key_links:
    - from: "cha-bio-safety/src/pages/ReportsPage.tsx"
      to: "cha-bio-safety/src/styles/components.css"
      via: "SW1 정의된 18 class 중 toolbar 9 + sidelist 8 + preview-wrapper 1 사용"
      pattern: "className=\"(toolbar|sidelist|preview-wrapper)"
    - from: "cha-bio-safety/src/pages/ReportsPage.tsx (DesktopReportsPage)"
      to: "cha-bio-safety/src/components/ExcelPreview.tsx"
      via: "<ExcelPreview reportType={selectedType} year={year} month={month} />"
      pattern: "<ExcelPreview "
---

<objective>
redesign/14-reports TSX 변환 wave 의 마지막 sub-wave (SW3) — `cha-bio-safety/src/pages/ReportsPage.tsx` 의 `DesktopReportsPage` 함수 (line 149~304) 와 `SELECT_STYLE` 상수 (line 306~313) 를 v0.1.1 디자인 토큰 + SW1 components.css class 기반 Tailwind/className 형태로 재작성한다.

Purpose: SW1 에서 신규 정의한 18 class (toolbar 9 / sidelist 8 / preview-wrapper 1) 중 17 class 를 DesktopReportsPage 에서 실제 소비, SW2 (MobileReportsPage) 와 합쳐 14-reports TSX 변환 wave 를 완결한다. linear-gradient 폐기 (W1 OQ #3 LOCKED), SELECT_STYLE 상수 폐기 (W7 OQ #2 LOCKED) 로 디자인 시스템 일관성 회복.

Output:
- 수정 1: `cha-bio-safety/src/pages/ReportsPage.tsx` (line 149~313 영역, atomic 1-commit)
- 작성 1: `.planning/quick/260520-wz9-redesign-14-reports-tsx-sw3-desktoprepor/260520-wz9-SUMMARY.md` (SW3 완결 + W7 변환 wave 종료 선언)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.local.md
@cha-bio-safety/src/styles/components.css
@cha-bio-safety/src/pages/ReportsPage.tsx
@cha-bio-safety/docs/redesign-context/14-reports/sketch-wave-4-desktop-toolbar.html
@cha-bio-safety/docs/redesign-context/14-reports/sketch-wave-5-desktop-sidelist.html
@cha-bio-safety/docs/redesign-context/14-reports/sketch-wave-6-desktop-preview-wrapper.html
@cha-bio-safety/docs/redesign-context/14-reports/wave-7-tsx-conversion-checklist.md
@cha-bio-safety/docs/redesign-context/14-reports/wave-1-index.md

<interfaces>
<!-- SW1 components.css 에 이미 정의된 17 class (verbatim, 변환 wave executor 는 그대로 className 으로 사용) -->

From cha-bio-safety/src/styles/components.css (toolbar 9):
```css
.toolbar { display: flex; align-items: center; gap: 12px; padding: 8px 16px; border-bottom: 1px solid var(--border-default); background: var(--surface-raised); flex-shrink: 0; }
.toolbar-year-label { font-size: 12px; color: var(--text-secondary); line-height: 1; }
.toolbar-select { background: var(--surface-sunken); color: var(--text-primary); border: 1px solid var(--border-strong); border-radius: 4px; padding: 4px 8px; font-size: 12px; line-height: 1; font-family: inherit; cursor: pointer; }
.toolbar-batch-btn { height: 32px; padding: 0 14px; background: var(--status-safe-bar); border: 0; border-radius: 6px; color: var(--text-on-accent); font-size: 12px; font-weight: 700; cursor: pointer; display: flex; align-items: center; gap: 6px; line-height: 1; font-family: inherit; }
.toolbar-batch-btn--loading { background: var(--surface-sunken); color: var(--text-tertiary); cursor: default; }
.toolbar-spacer { flex: 1; }
.toolbar-selected-title { font-size: 14px; font-weight: 700; color: var(--text-primary); line-height: 1; }
.toolbar-individual-btn { height: 32px; padding: 0 14px; background: var(--surface-sunken); border: 1px solid var(--border-strong); border-radius: 6px; color: var(--text-primary); font-size: 12px; font-weight: 700; cursor: pointer; display: flex; align-items: center; gap: 6px; line-height: 1; font-family: inherit; }
.toolbar-individual-btn--loading { opacity: 0.5; cursor: default; }
```

From cha-bio-safety/src/styles/components.css (sidelist 8):
```css
.sidelist { width: 260px; flex-shrink: 0; border-right: 1px solid var(--border-default); overflow-y: auto; background: var(--surface-raised); }
.sidelist-section-header { padding: 8px 16px 4px; font-size: 12px; font-weight: 700; color: var(--text-tertiary); letter-spacing: 0.05em; line-height: 1; }
.sidelist-row { padding: 8px 16px; cursor: pointer; background: transparent; border-left: 3px solid transparent; }
.sidelist-row--selected { background: var(--surface-sunken); border-left: 3px solid var(--accent); }
.sidelist-row--hover { background: var(--surface-sunken); border-left: 3px solid transparent; }
.sidelist-row-title { font-size: 14px; color: var(--text-primary); font-weight: 400; }
.sidelist-row-title--selected { color: var(--accent); font-weight: 700; }
.sidelist-row-sub { font-size: 12px; color: var(--text-tertiary); margin-top: 1px; }
```

From cha-bio-safety/src/styles/components.css (preview-wrapper 1 + dot-meta):
```css
.dot-meta { display: inline-block; width: 4px; height: 4px; border-radius: 9999px; background: var(--text-tertiary); flex-shrink: 0; }
.preview-wrapper { flex: 1; overflow: hidden; background: var(--surface-page); display: flex; align-items: flex-start; justify-content: center; padding: 32px; }
```

From cha-bio-safety/src/pages/ReportsPage.tsx (보존해야 할 비즈 로직 시그니처, line 149~175):
```ts
function DesktopReportsPage() {
  const [year, setYear] = useState(CURRENT_YEAR)
  const [selectedType, setSelectedType] = useState<ReportType>('div-early')
  const [loading, setLoading] = useState<ReportType | null>(null)
  const [zipLoading, setZipLoading] = useState<string | null>(null)
  const [hoverType, setHoverType] = useState<ReportType | null>(null)
  const month = new Date().getMonth() + 1
  const handleDownload = async (type: ReportType) => { /* setLoading 호출 + downloadReport(type, year) + finally setLoading(null) */ }
  const handleDownloadAll = async () => { /* setZipLoading 호출 + downloadAllAsZip(year, month, setZipLoading) + finally setZipLoading(null) */ }
  const selectedCard = REPORT_CARDS.find(c => c.type === selectedType)
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DesktopReportsPage v0.1.1 Tailwind 재작성 + SELECT_STYLE 상수 삭제</name>
  <files>cha-bio-safety/src/pages/ReportsPage.tsx</files>
  <action>
ReportsPage.tsx 의 line 149~313 영역만 부분 수정. 다른 라인 (1~148, 314~405) 은 변경 0 (SW2 가 이미 처리한 영역 예외 — Mobile 영역이라 본 SW3 와 무관).

**A. DesktopReportsPage 함수 변환 (line 149~304)**:

A-1. root container (line 178): 인라인 style 유지 — viewport root layout primitive (W7 §11 별표). `<div style={{ display: 'flex', flexDirection: 'column', height: '100%', overflow: 'hidden' }}>` 그대로.

A-2. toolbar (line 181~243):
- outer `<div style={{ padding: '8px 16px', borderBottom: ..., display: 'flex', ... }}>` (line 181~189) → `<div className="toolbar">`
- 연도 label (line 190): `<label style={{ fontSize: 12, color: 'var(--t2)' }}>연도</label>` → `<label className="toolbar-year-label">연도</label>`
- select (line 191~195): `<select value={year} onChange={e => setYear(Number(e.target.value))} style={SELECT_STYLE}>` → `<select value={year} onChange={e => setYear(Number(e.target.value))} className="toolbar-select">` (options 분기 동일)
- 일괄 다운로드 button (line 196~216): linear-gradient 폐기 (W1 OQ #3 LOCKED). 변환 후:
```tsx
<button
  className={zipLoading ? 'toolbar-batch-btn toolbar-batch-btn--loading' : 'toolbar-batch-btn'}
  onClick={handleDownloadAll}
  disabled={!!zipLoading}
>
  <Download size={13} />
  {zipLoading ?? '일괄 다운로드'}
</button>
```
- spacer (line 218): `<div style={{ flex: 1 }} />` → `<div className="toolbar-spacer" />`
- selected title (line 220): `<span style={{ fontSize: 14, fontWeight: 700, color: 'var(--t1)' }}>{selectedCard?.title}</span>` → `<span className="toolbar-selected-title">{selectedCard?.title}</span>`
- 개별 button (line 221~242): 변환 후:
```tsx
<button
  className={loading === selectedType ? 'toolbar-individual-btn toolbar-individual-btn--loading' : 'toolbar-individual-btn'}
  onClick={() => handleDownload(selectedType)}
  disabled={loading === selectedType}
>
  <Download size={13} />
  {loading === selectedType ? '생성 중...' : '엑셀 다운로드'}
</button>
```

A-3. 하단 layout (line 246): `<div style={{ flex: 1, display: 'flex', overflow: 'hidden' }}>` → `<div className="flex flex-1 overflow-hidden">` (Tailwind utility, layout primitive — SW1 class 없음).

A-4. sidelist (line 249~295):
- outer (line 249~255): `<div style={{ width: 260, ... background: 'var(--bg2)' }}>` → `<div className="sidelist">`
- DESKTOP_SECTIONS.map (line 256~293) 안:
  - section-header (line 258~266): 인라인 style 제거, `<div className="sidelist-section-header">` + 안에 가운뎃점 분할 추가.

    **섹션 B 라벨 처리 — DESKTOP_SECTIONS 의 `'소화전 · 가스 · 비상콘센트'` (line 144) 는 가운뎃점 2개 포함**. dot-meta span 으로 변환:
    ```tsx
    <div className="sidelist-section-header">
      {section.label.split(' · ').map((part, i, arr) => (
        <span key={i}>
          {part}
          {i < arr.length - 1 && <span className="dot-meta"></span>}
        </span>
      ))}
    </div>
    ```
    (다른 섹션 — '유수검지 장치' / '연간 점검일지' / '소방펌프' — 는 가운뎃점 없어 split 1 entry, dot span 0 추가됨. 섹션 B 만 dot span 2개 생성 → W7 §11 SW3 #5 gate ≥2 충족.)

  - section.types.map (line 267~291) 안 row:
```tsx
{section.types.map(type => {
  const card = REPORT_CARDS.find(c => c.type === type)
  if (!card) return null
  const isSelected = selectedType === type
  const isHover = hoverType === type && !isSelected
  return (
    <div
      key={type}
      onClick={() => setSelectedType(type)}
      onMouseEnter={() => setHoverType(type)}
      onMouseLeave={() => setHoverType(null)}
      className={
        isSelected ? 'sidelist-row sidelist-row--selected' :
        isHover ? 'sidelist-row sidelist-row--hover' :
        'sidelist-row'
      }
    >
      <div className={isSelected ? 'sidelist-row-title sidelist-row-title--selected' : 'sidelist-row-title'}>
        {card.title}
      </div>
      <div className="sidelist-row-sub">{card.sub}</div>
    </div>
  )
})}
```

A-5. preview area (line 297~300):
```tsx
<div className="preview-wrapper">
  <ExcelPreview reportType={selectedType} year={year} month={month} />
</div>
```
(인라인 `style={{ flex: 1, overflow: 'hidden', background: 'var(--bg)' }}` 제거. ExcelPreview prop verbatim 보존.)

**B. SELECT_STYLE 상수 삭제 (line 306~313)**:

다음 블록 통째 삭제:
```tsx
const SELECT_STYLE: React.CSSProperties = {
  background: 'var(--bg3)',
  color: 'var(--t1)',
  border: '1px solid var(--bd2)',
  borderRadius: 4,
  padding: '4px 8px',
  fontSize: 12,
}
```
이유: `.toolbar-select` 가 verbatim 등가 정의 (SW1 components.css line 47) — W7 OQ #2 LOCKED default (a).

**C. NEGATIVE — 변경 금지 영역**:

다음 라인은 1 byte 도 손대지 않는다 (SW2 가 이미 처리한 MobileReportsPage 영역 제외):
- line 1~7: imports (이미 SW2 가 `Download, ChevronLeft` 확장 — 추가 import 없음)
- line 9~10: ReportType type
- line 12~23: REPORT_CARDS array
- line 25~30: MATRIX_CONFIG
- line 32~36: CURRENT_YEAR / MIN_YEAR / ANNUAL_TYPES
- line 39~71: downloadReport 함수 (특히 line 49~57 ASSISTANTS 랜덤 채움 룰 → W7 §11 SW3 #8 검증)
- line 73~109: generateReportBlob 함수 (특히 line 86~93 ASSISTANTS 룰)
- line 111~139: downloadAllAsZip 함수
- line 142~147: DESKTOP_SECTIONS array
- line 314~398: MobileReportsPage 함수 영역 (SW2 처리분) — 본 SW3 무관, 0 변경
- line 400~405: default export 함수

**D. 비즈 로직 시그니처 보존 체크 (DesktopReportsPage 안)**:
- `useState(CURRENT_YEAR)` (line 150) 유지
- `useState<ReportType>('div-early')` (line 151) 유지
- `useState<ReportType | null>(null) loading` (line 152) 유지
- `useState<string | null>(null) zipLoading` (line 153) 유지
- `useState<ReportType | null>(null) hoverType` (line 154) 유지
- `month = new Date().getMonth() + 1` (line 155) 유지
- `handleDownload(type)` (line 157~164) signature 동일
- `handleDownloadAll()` (line 166~173) signature 동일
- `selectedCard = REPORT_CARDS.find(c => c.type === selectedType)` (line 175) 유지
- `<ExcelPreview reportType={selectedType} year={year} month={month} />` ExcelPreview prop 3개 동일

**E. 워크트리 룰 (이 워크트리 = cbc7119-design, CLAUDE.local.md)**:
- wrangler 명령 0건 (`.claude/settings.local.json` deny 강제됨)
- `npm run deploy` 0건 (직원 도메인 경로)
- 빌드 검증은 `cd cha-bio-safety && npx tsc --noEmit && npm run build` 만
- 머지/배포는 사용자 컨펌 후, GitHub Actions cbc7119-preview 자동 배포만

**F. atomic commit**:

커밋 메시지 (verbatim):
```
tsx(14-reports): SW3 — DesktopReportsPage v0.1.1 Tailwind 재작성 (toolbar + sidelist + preview wrapper, 마지막 sub-wave)
```

1-commit, ReportsPage.tsx 단일 파일만.
  </action>
  <verify>
    <automated>cd cha-bio-safety && npx tsc --noEmit && npm run build && cd .. && \
echo "=== Negative gates (모두 0) ===" && \
NG1=$(grep -c -F '⬇' cha-bio-safety/src/pages/ReportsPage.tsx); echo "NG1 emoji ⬇ count: $NG1 (expect 0)" && [ "$NG1" = "0" ] && \
NG2=$(grep -c -F 'linear-gradient' cha-bio-safety/src/pages/ReportsPage.tsx); echo "NG2 linear-gradient count: $NG2 (expect 0)" && [ "$NG2" = "0" ] && \
NG3=$(grep -cE 'fontSize:\s*1[01][^0-9]|font-size:\s*1[01]px' cha-bio-safety/src/pages/ReportsPage.tsx); echo "NG3 11/10px font count: $NG3 (expect 0)" && [ "$NG3" = "0" ] && \
NG4=$(grep -cE '\bw-8\b|\bh-8\b' cha-bio-safety/src/pages/ReportsPage.tsx); echo "NG4 w-8/h-8 count: $NG4 (expect 0)" && [ "$NG4" = "0" ] && \
NG5=$(grep -cE '(text|bg)-status-' cha-bio-safety/src/pages/ReportsPage.tsx); echo "NG5 status- prefix count: $NG5 (expect 0)" && [ "$NG5" = "0" ] && \
NG6=$(grep -cE 'const SELECT_STYLE' cha-bio-safety/src/pages/ReportsPage.tsx); echo "NG6 SELECT_STYLE const: $NG6 (expect 0)" && [ "$NG6" = "0" ] && \
NG7=$(grep -c -F 'SELECT_STYLE' cha-bio-safety/src/pages/ReportsPage.tsx); echo "NG7 SELECT_STYLE 사용처: $NG7 (expect 0)" && [ "$NG7" = "0" ] && \
echo "=== Positive gates (≥1) ===" && \
P1=$(grep -c 'className="toolbar"' cha-bio-safety/src/pages/ReportsPage.tsx); echo "P1 .toolbar: $P1 (≥1)" && [ "$P1" -ge "1" ] && \
P2=$(grep -c 'className="toolbar-year-label"' cha-bio-safety/src/pages/ReportsPage.tsx); echo "P2 toolbar-year-label: $P2 (≥1)" && [ "$P2" -ge "1" ] && \
P3=$(grep -c 'className="toolbar-select"' cha-bio-safety/src/pages/ReportsPage.tsx); echo "P3 toolbar-select: $P3 (≥1)" && [ "$P3" -ge "1" ] && \
P4=$(grep -cE 'className=.*toolbar-batch-btn' cha-bio-safety/src/pages/ReportsPage.tsx); echo "P4 toolbar-batch-btn: $P4 (≥1)" && [ "$P4" -ge "1" ] && \
P5=$(grep -c 'className="toolbar-spacer"' cha-bio-safety/src/pages/ReportsPage.tsx); echo "P5 toolbar-spacer: $P5 (≥1)" && [ "$P5" -ge "1" ] && \
P6=$(grep -c 'className="toolbar-selected-title"' cha-bio-safety/src/pages/ReportsPage.tsx); echo "P6 toolbar-selected-title: $P6 (≥1)" && [ "$P6" -ge "1" ] && \
P7=$(grep -cE 'className=.*toolbar-individual-btn' cha-bio-safety/src/pages/ReportsPage.tsx); echo "P7 toolbar-individual-btn: $P7 (≥1)" && [ "$P7" -ge "1" ] && \
P8=$(grep -c 'className="sidelist"' cha-bio-safety/src/pages/ReportsPage.tsx); echo "P8 sidelist: $P8 (≥1)" && [ "$P8" -ge "1" ] && \
P9=$(grep -c 'className="sidelist-section-header"' cha-bio-safety/src/pages/ReportsPage.tsx); echo "P9 sidelist-section-header: $P9 (≥1)" && [ "$P9" -ge "1" ] && \
P10=$(grep -cE 'className=.*sidelist-row' cha-bio-safety/src/pages/ReportsPage.tsx); echo "P10 sidelist-row: $P10 (≥1)" && [ "$P10" -ge "1" ] && \
P11=$(grep -cE 'className=.*sidelist-row-title' cha-bio-safety/src/pages/ReportsPage.tsx); echo "P11 sidelist-row-title: $P11 (≥1)" && [ "$P11" -ge "1" ] && \
P12=$(grep -c 'className="sidelist-row-sub"' cha-bio-safety/src/pages/ReportsPage.tsx); echo "P12 sidelist-row-sub: $P12 (≥1)" && [ "$P12" -ge "1" ] && \
P13=$(grep -c 'className="preview-wrapper"' cha-bio-safety/src/pages/ReportsPage.tsx); echo "P13 preview-wrapper: $P13 (≥1)" && [ "$P13" -ge "1" ] && \
P14=$(grep -c -F 'handleDownloadAll' cha-bio-safety/src/pages/ReportsPage.tsx); echo "P14 handleDownloadAll: $P14 (≥1)" && [ "$P14" -ge "1" ] && \
P15=$(grep -c -F 'setSelectedType' cha-bio-safety/src/pages/ReportsPage.tsx); echo "P15 setSelectedType: $P15 (≥1)" && [ "$P15" -ge "1" ] && \
P16=$(grep -c -F 'DESKTOP_SECTIONS' cha-bio-safety/src/pages/ReportsPage.tsx); echo "P16 DESKTOP_SECTIONS: $P16 (≥2)" && [ "$P16" -ge "2" ] && \
P17=$(grep -cE 'REPORT_CARDS\.find' cha-bio-safety/src/pages/ReportsPage.tsx); echo "P17 REPORT_CARDS.find: $P17 (≥1)" && [ "$P17" -ge "1" ] && \
P18=$(grep -c -F 'onMouseEnter' cha-bio-safety/src/pages/ReportsPage.tsx); echo "P18 onMouseEnter: $P18 (≥1)" && [ "$P18" -ge "1" ] && \
P19=$(grep -c -F 'onMouseLeave' cha-bio-safety/src/pages/ReportsPage.tsx); echo "P19 onMouseLeave: $P19 (≥1)" && [ "$P19" -ge "1" ] && \
P20=$(grep -c -F 'dot-meta' cha-bio-safety/src/pages/ReportsPage.tsx); echo "P20 dot-meta count: $P20 (≥2 — 섹션 B 가운뎃점 2개)" && [ "$P20" -ge "2" ] && \
echo "=== SW3 §11 W7 gate ===" && \
CN=$(sed -n '149,304p' cha-bio-safety/src/pages/ReportsPage.tsx | grep -cE 'className='); echo "SW3#1 className= 카운트 (line 149~304): $CN (≥30 기대)" && [ "$CN" -ge "30" ] && \
SI=$(sed -n '149,304p' cha-bio-safety/src/pages/ReportsPage.tsx | grep -cE 'style=\{\{'); echo "SW3#2 인라인 style 카운트 (line 149~304): $SI (≤8 기대)" && [ "$SI" -le "8" ] && \
echo "=== File-level guards (변경 0) ===" && \
G1=$(git diff --name-only HEAD~..HEAD -- cha-bio-safety/src/components/ExcelPreview.tsx | wc -l | tr -d ' '); echo "G1 ExcelPreview.tsx diff: $G1 (expect 0)" && [ "$G1" = "0" ] && \
G2=$(git diff --name-only HEAD~..HEAD -- cha-bio-safety/src/App.tsx | wc -l | tr -d ' '); echo "G2 App.tsx diff: $G2 (expect 0)" && [ "$G2" = "0" ] && \
G3=$(git diff --name-only HEAD~..HEAD -- cha-bio-safety/src/styles/ | wc -l | tr -d ' '); echo "G3 styles/ diff: $G3 (expect 0)" && [ "$G3" = "0" ] && \
G4=$(git diff --name-only HEAD~..HEAD -- cha-bio-safety/src/main.tsx | wc -l | tr -d ' '); echo "G4 main.tsx diff: $G4 (expect 0)" && [ "$G4" = "0" ] && \
echo "All 30+ gates PASS"</automated>
  </verify>
  <done>
- ReportsPage.tsx line 149~304 의 DesktopReportsPage 함수가 SW1 components.css 의 17 class (toolbar 9 + sidelist 8 + preview-wrapper 1 — line 178 root 인라인 style 제외) 를 사용한다
- line 306~313 SELECT_STYLE 상수가 통째 삭제됐다
- linear-gradient 0건 (W1 OQ #3 LOCKED 적용)
- 섹션 B 라벨 '소화전 · 가스 · 비상콘센트' 가 dot-meta span 2개로 변환됐다
- sed line 149~304 region 안 className= ≥30 / 인라인 style ≤8 (W7 §11 SW3 #1, #2 gate)
- 비즈 로직 시그니처 (handleDownload / handleDownloadAll / setSelectedType / hoverType / selectedCard / ExcelPreview prop) 1 byte 변경 없음
- ExcelPreview.tsx / App.tsx / styles/* / main.tsx git diff 0줄
- `npx tsc --noEmit` exit 0, `npm run build` exit 0
- atomic 1-commit (커밋 메시지 verbatim)
  </done>
</task>

<task type="auto">
  <name>Task 2: SUMMARY.md 작성 + W7 14-reports 변환 wave 완결 선언</name>
  <files>.planning/quick/260520-wz9-redesign-14-reports-tsx-sw3-desktoprepor/260520-wz9-SUMMARY.md</files>
  <action>
Task 1 완료 후 SUMMARY.md 작성. atomic commit 동일 (Task 1 commit 안에 포함 가능 또는 별 commit — 본 워크트리 관행 따름).

SUMMARY 내용:

### 1. 변환 매핑 표 (DesktopReportsPage)

| source line | source (인라인 style or 상수) | target className (SW1 components.css) |
|---|---|---|
| line 181~189 | `style={{ padding: '8px 16px', borderBottom: ..., display: 'flex', alignItems: 'center', gap: 12, flexShrink: 0, background: 'var(--bg2)' }}` | `className="toolbar"` |
| line 190 | `style={{ fontSize: 12, color: 'var(--t2)' }}` | `className="toolbar-year-label"` |
| line 191 | `style={SELECT_STYLE}` | `className="toolbar-select"` |
| line 199~212 (zipLoading false) | `background: 'linear-gradient(135deg,#1d4ed8,#2563eb)' ...` | `className="toolbar-batch-btn"` (linear-gradient → var(--status-safe-bar), W1 OQ #3 LOCKED) |
| line 199~212 (zipLoading true) | `background: 'var(--bg3)', color: 'var(--t3)' ...` | `className="toolbar-batch-btn toolbar-batch-btn--loading"` |
| line 218 | `style={{ flex: 1 }}` | `className="toolbar-spacer"` |
| line 220 | `style={{ fontSize: 14, fontWeight: 700, color: 'var(--t1)' }}` | `className="toolbar-selected-title"` |
| line 224~238 (loading false) | `style={{ height: 32, padding: '0 14px', background: 'var(--bg3)', border: '1px solid var(--bd2)', ... }}` | `className="toolbar-individual-btn"` |
| line 224~238 (loading true) | `style={{ ..., opacity: 0.5 }}` | `className="toolbar-individual-btn toolbar-individual-btn--loading"` |
| line 246 | `style={{ flex: 1, display: 'flex', overflow: 'hidden' }}` | `className="flex flex-1 overflow-hidden"` (Tailwind primitive) |
| line 249~255 | `style={{ width: 260, flexShrink: 0, borderRight: '1px solid var(--bd)', overflowY: 'auto', background: 'var(--bg2)' }}` | `className="sidelist"` |
| line 258~266 | `style={{ padding: '8px 16px 4px', fontSize: 11, fontWeight: 700, color: 'var(--t3)', letterSpacing: '0.05em' }}` + section.label 직접 출력 | `className="sidelist-section-header"` + split(' · ') + `<span className="dot-meta">` (섹션 B 만 dot 2개) |
| line 278~283 (selected) | `style={{ padding: '8px 16px', background: 'var(--bg3)', borderLeft: '3px solid var(--acl)', ... }}` | `className="sidelist-row sidelist-row--selected"` |
| line 278~283 (hover) | `background: 'var(--bg3)', borderLeft: '3px solid transparent'` | `className="sidelist-row sidelist-row--hover"` |
| line 278~283 (default) | `background: 'transparent', borderLeft: '3px solid transparent'` | `className="sidelist-row"` |
| line 285~287 (selected title) | `fontSize: 13, color: 'var(--acl)', fontWeight: 700` | `className="sidelist-row-title sidelist-row-title--selected"` |
| line 285~287 (default title) | `fontSize: 13, color: 'var(--t1)', fontWeight: 400` | `className="sidelist-row-title"` |
| line 288 | `style={{ fontSize: 11, color: 'var(--t3)', marginTop: 1 }}` | `className="sidelist-row-sub"` |
| line 298~300 | `style={{ flex: 1, overflow: 'hidden', background: 'var(--bg)' }}` | `className="preview-wrapper"` |
| line 306~313 | `const SELECT_STYLE: React.CSSProperties = { ... }` | **삭제** (W7 OQ #2 LOCKED) |

총 17 SW1 class 소비 (toolbar 9 + sidelist 8) + dot-meta + preview-wrapper. SW1 정의 18 class 중 17 class 활용 (line 178 root 는 viewport primitive 라 인라인 유지).

### 2. verify gate 결과

Task 1 verify 명령 30+ gate 결과 캡쳐. 각 NG/P/SW3#/G 항목 PASS 명시.

### 3. 비즈 로직 보존 체크

- handleDownload(type) signature 동일 — `async (type: ReportType) => { setLoading(type); try { await downloadReport(type, year) } finally { setLoading(null) } }`
- handleDownloadAll() signature 동일 — `async () => { setZipLoading('준비 중...'); try { await downloadAllAsZip(year, month, setZipLoading) } finally { setZipLoading(null) } }`
- selectedCard = REPORT_CARDS.find(c => c.type === selectedType) 유지
- hoverType / setHoverType 상태 + onMouseEnter/onMouseLeave 보존
- ExcelPreview prop (reportType / year / month) verbatim
- DESKTOP_SECTIONS 4 섹션 + REPORT_CARDS 10 entry verbatim
- ANNUAL_TYPES / MATRIX_CONFIG / downloadReport / generateReportBlob / downloadAllAsZip 변경 0

### 4. 빌드 결과 (chunk size delta)

`npm run build` 후 ReportsPage chunk size 변환 전/후 비교 (±5KB 이내 = W7 OQ #5 default 통과). 실제 측정값 기록.

### 5. negative scope 보호 (변경 0)

- ExcelPreview.tsx / App.tsx / main.tsx / styles/* git diff 0줄 — `git diff --name-only HEAD~..HEAD -- <path>` 결과로 검증
- MobileReportsPage (line 314~398, SW2 영역) 0 영향
- imports / type 정의 / 상수 / utility 함수 0 영향

### 6. W7 14-reports 변환 wave 완결 선언

> **SW1 (components.css 신규)** + **SW2 (MobileReportsPage 변환)** + **SW3 (DesktopReportsPage 변환)** 3 sub-wave atomic commit 모두 완료.
> redesign/14-reports = sketch W1~W6 + W7 checklist + SW1~SW3 변환 wave 모두 main 머지 + cbc7119-preview 자동 배포 완결.
> 다음 페이지 진행 시점 (cbc7119-design 워크트리 관행: `/clear` + 새 redesign/NN 브랜치).

### 7. 워크트리 룰 준수

- `wrangler` 명령 0건
- `npm run deploy` 0건
- 직원 도메인 (cbc7119.pages.dev) 0 영향 — cbc7119-preview.pages.dev 자동 배포만 (GitHub Actions on main push)
- 운영 PWA (20260328 워크트리) 0 영향

### 8. 후속 quick 후보 (이번 SW3 에서 처리 X)

- 13-schedule / 12-staff-service / 11-div 등 다른 페이지 component.css 통합 패턴 mirror 시 SW1 의 18 class 가 다른 페이지 컨텍스트에서 재사용 가능한지 분석
- iconBtn / navBtn / SELECT_STYLE 와 비슷한 page-local 상수 폐기 패턴 추출
- 본 SW3 와 무관 — 별 quick 으로 분리.
  </action>
  <verify>
    <automated>test -f .planning/quick/260520-wz9-redesign-14-reports-tsx-sw3-desktoprepor/260520-wz9-SUMMARY.md && \
SECT1=$(grep -c '변환 매핑 표' .planning/quick/260520-wz9-redesign-14-reports-tsx-sw3-desktoprepor/260520-wz9-SUMMARY.md); echo "SUMMARY §1 mapping 표: $SECT1 (≥1)" && [ "$SECT1" -ge "1" ] && \
SECT2=$(grep -c 'verify gate' .planning/quick/260520-wz9-redesign-14-reports-tsx-sw3-desktoprepor/260520-wz9-SUMMARY.md); echo "SUMMARY §2 verify gate 결과: $SECT2 (≥1)" && [ "$SECT2" -ge "1" ] && \
SECT3=$(grep -c '비즈 로직' .planning/quick/260520-wz9-redesign-14-reports-tsx-sw3-desktoprepor/260520-wz9-SUMMARY.md); echo "SUMMARY §3 비즈 로직 보존: $SECT3 (≥1)" && [ "$SECT3" -ge "1" ] && \
SECT4=$(grep -cE 'chunk size|chunk-size' .planning/quick/260520-wz9-redesign-14-reports-tsx-sw3-desktoprepor/260520-wz9-SUMMARY.md); echo "SUMMARY §4 chunk size delta: $SECT4 (≥1)" && [ "$SECT4" -ge "1" ] && \
SECT5=$(grep -cE 'SW1.*SW2.*SW3|완결 선언|완결' .planning/quick/260520-wz9-redesign-14-reports-tsx-sw3-desktoprepor/260520-wz9-SUMMARY.md); echo "SUMMARY §6 완결 선언: $SECT5 (≥1)" && [ "$SECT5" -ge "1" ] && \
echo "SUMMARY 5 sections PASS"</automated>
  </verify>
  <done>
- SUMMARY.md 8 section 모두 작성 (변환 매핑 / verify 결과 / 비즈 로직 / chunk size / negative scope / W7 완결 선언 / 워크트리 룰 / 후속 quick 후보)
- 변환 매핑 표 18 row (line 181 toolbar outer ~ line 306~313 SELECT_STYLE 삭제)
- W7 14-reports 변환 wave (SW1+SW2+SW3) 완결 선언 명시
- 본 워크트리 wrangler 0건 / npm run deploy 0건 / 직원 도메인 0 영향 명시
  </done>
</task>

</tasks>

<verification>

## 종합 verify (Task 1 + Task 2 합산)

1. **빌드** — `cd cha-bio-safety && npx tsc --noEmit && npm run build` 둘 다 exit 0, chunk size delta ≤ ±5KB
2. **W7 §11 SW3 gate (≥10)** — line 149~304 className= ≥30 / 인라인 style ≤8 / Download import 유지 / linear-gradient 0 / dot-meta ≥2 / SELECT_STYLE 0 / iconBtn|navBtn 0 (Mobile SW2 처리 후 잔존 검증) / ASSISTANTS 2 hits / build 통과 / handleDownloadAll signature 보존 — 모두 PASS
3. **추가 negative (≥8)** — emoji ⬇ 0 / 11/10px font 0 / w-8/h-8 0 / text-status- / bg-status- 0 / SELECT_STYLE 사용처 0
4. **추가 positive (≥13)** — toolbar 9 + sidelist 8 + preview-wrapper 1 class 사용 ≥1 / handleDownloadAll / setSelectedType / DESKTOP_SECTIONS ≥2 / REPORT_CARDS.find ≥1 / onMouseEnter/Leave / dot-meta ≥2 모두 충족
5. **file-level guard (=0)** — ExcelPreview.tsx / App.tsx / styles/* / main.tsx git diff 0줄
6. **atomic commit** — ReportsPage.tsx + SUMMARY.md 1 commit (또는 2 commit, 본 워크트리 관행)
7. **워크트리 룰** — wrangler / npm run deploy 0건, cbc7119-preview 자동 배포만

</verification>

<success_criteria>
- [x] ReportsPage.tsx line 149~304 DesktopReportsPage 함수 변환 (인라인 style → SW1 components.css class 17개)
- [x] ReportsPage.tsx line 306~313 SELECT_STYLE 상수 삭제 (W7 OQ #2 LOCKED)
- [x] linear-gradient 폐기 — `.toolbar-batch-btn` var(--status-safe-bar) 사용 (W1 OQ #3 LOCKED)
- [x] DESKTOP_SECTIONS 섹션 B 라벨 dot-meta span 2개 변환
- [x] 비즈 로직 시그니처 (handleDownload / handleDownloadAll / hoverType / selectedCard / ExcelPreview prop) 1 byte 변경 없음
- [x] W7 §11 SW3 gate ≥10 + 추가 negative ≥8 + 추가 positive ≥13 + file-level guard ≥4 = 총 ≥35 gate 모두 PASS
- [x] tsc --noEmit + npm run build exit 0, chunk size delta ≤ ±5KB
- [x] atomic commit, 커밋 메시지 `tsx(14-reports): SW3 — DesktopReportsPage v0.1.1 Tailwind 재작성 (toolbar + sidelist + preview wrapper, 마지막 sub-wave)`
- [x] SUMMARY.md 8 section 작성, W7 14-reports 변환 wave (SW1+SW2+SW3) 완결 선언
- [x] 본 워크트리 룰 준수 — wrangler / npm run deploy 0건, cbc7119-preview 자동 배포만
- [x] ExcelPreview.tsx / App.tsx / styles/* / main.tsx 0 영향
- [x] MobileReportsPage (line 314~398, SW2 영역) 0 영향
</success_criteria>

<output>
After completion, create `.planning/quick/260520-wz9-redesign-14-reports-tsx-sw3-desktoprepor/260520-wz9-SUMMARY.md` per Task 2.
</output>
