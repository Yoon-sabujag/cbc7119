---
phase: quick-260521-25s
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cha-bio-safety/docs/redesign-context/15-daily-report/sketch-wave-2-mobile-header-date-nav.html
autonomous: true
requirements:
  - REDESIGN-15-W2-MOBILE-HEADER
  - REDESIGN-15-W2-DATE-NAV
  - REDESIGN-15-W2-INHERIT-14-REPORTS
tags:
  - redesign
  - 15-daily-report
  - sketch
  - wave-2
  - mobile-header
  - date-nav

must_haves:
  truths:
    - "다크/라이트 × 오늘/미래 4 frame side-by-side 시각 비교 가능"
    - "각 frame 안 모바일 자체 헤더가 14-reports W2 와 동일한 chrome 패턴(.page-header / .back-btn / .page-title)으로 시각화"
    - "dateNav 영역에 ‹ + 날짜(YYYY-MM-DD) + (› or 28px spacer) 3 요소가 source line 387~401 verbatim 매핑"
    - "오늘 frame 은 › 버튼 노출, 미래 frame 은 28px spacer 로 자리 유지 (canForward 분기 시연)"
    - "노안 격상 폰트 룰 적용 — 14→18 (헤더 타이틀), 13→14 (날짜), 9·10·11px 0건"
    - "이모지 / 그라데이션 / status- prefix / 가운뎃점 텍스트 0건 — 모든 negative gate PASS"
    - "lucide ChevronLeft + ChevronRight inline SVG 가 back-btn + dateNav 양쪽에 사용 (글리프 ‹/› 텍스트 0건)"
  artifacts:
    - path: "cha-bio-safety/docs/redesign-context/15-daily-report/sketch-wave-2-mobile-header-date-nav.html"
      provides: "W2 sketch — 모바일 헤더 + dateNav, 다크/라이트 × 오늘/미래 4 frame"
      contains: "frame-shell × 4 + .page-header × 4 + .date-nav × 4 + .date-display × 4"
      min_lines: 400
  key_links:
    - from: "sketch-wave-2-mobile-header-date-nav.html"
      to: "cha-bio-safety/docs/redesign-context/14-reports/sketch-wave-2-mobile-header-card.html"
      via: "frame-shell 393px + page-bg-dark/light + variant-label + page-header/back-btn/page-title 패턴 1:1 mirror"
      pattern: "frame-shell.*393px"
    - from: "sketch-wave-2-mobile-header-date-nav.html"
      to: "cha-bio-safety/src/pages/DailyReportPage.tsx#L387-L458"
      via: "헤더 markup line 449~458 verbatim + dateNav line 387~401 verbatim"
      pattern: "일일 업무 일지|YYYY-MM-DD"
    - from: "sketch-wave-2-mobile-header-date-nav.html"
      to: "cha-bio-safety/docs/redesign-context/15-daily-report/wave-1-index.md"
      via: "§2.1 W2 행 토큰 매핑 + §4.1 14-reports inherit class + §5 메모리 룰 12건"
      pattern: "wave-1-index"
---

<objective>
redesign/15-daily-report sketch wave 2 — 모바일 자체 헤더 + dateNav 의 디자인 시안 1개 HTML 생성.

**Purpose:**
- 15-daily-report 의 모바일 진입 chrome (헤더 + 날짜 네비) 을 14-reports W2 패턴 inherit + 신규 `.date-nav-*` 3 class 정의로 시각 컨펌
- DailyReportPage.tsx (line 387~401 dateNav + line 449~458 모바일 헤더) 의 verbatim markup 을 보존하면서 노안 룰 (14→18, 13→14) 적용
- W3 (EditableCard + 인원현황 카드) 진입 전 사용자 컨펌용 단일 산출물

**Output:**
- `cha-bio-safety/docs/redesign-context/15-daily-report/sketch-wave-2-mobile-header-date-nav.html` 1개 파일
- 다크/라이트 × 오늘/미래 = 4 frame 393px side-by-side
- 14-reports W2 (sketch-wave-2-mobile-header-card.html) 의 frame-shell / page-bg / page-header / back-btn / page-title 패턴 inherit
- 신규 정의 3 class: `.date-nav` / `.date-nav-btn` / `.date-display` (+ `.date-nav-spacer`)
- W3 진입 전 OQ ≥3건 (default 답 명시)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.local.md

@cha-bio-safety/docs/redesign-context/15-daily-report/wave-1-index.md
@cha-bio-safety/docs/redesign-context/15-daily-report/design-system.md
@cha-bio-safety/docs/redesign-context/15-daily-report/tokens.css
@cha-bio-safety/docs/redesign-context/14-reports/sketch-wave-2-mobile-header-card.html

<interfaces>
<!-- 본 sketch 가 verbatim 매핑할 source markup. executor 가 추측하지 않도록 박제. -->

## ① 모바일 헤더 (DailyReportPage.tsx line 449~458) — verbatim source
```tsx
<div style={{ height: '100%', display: 'flex', flexDirection: 'column', overflow: 'hidden', background: 'var(--bg)' }}>
  {/* 헤더 */}
  <header style={{ flexShrink: 0, background: 'var(--bg2)', borderBottom: '1px solid var(--bd)', padding: '8px 12px 9px', display: 'flex', alignItems: 'center', gap: 8 }}>
    <button onClick={() => navigate(-1)} style={iconBtn} aria-label="뒤로 가기">
      <svg width={15} height={15} fill="none" viewBox="0 0 24 24" stroke="var(--t2)" strokeWidth={2}>
        <path strokeLinecap="round" strokeLinejoin="round" d="M15 19l-7-7 7-7"/>
      </svg>
    </button>
    <span style={{ flex: 1, fontSize: 14, fontWeight: 700, color: 'var(--t1)' }}>일일 업무 일지</span>
    {dateNav}
  </header>

  {/* 스크롤 본문 */}
  <div style={{ flex: 1, overflowY: 'auto', padding: '12px 16px' }}>
    {formContent}
  </div>
</div>
```

## ② dateNav (DailyReportPage.tsx line 387~401) — verbatim source
```tsx
// ── 날짜 네비게이터 (공통) ─────────────────────────────
const dateNav = (
  <div style={{ display: 'flex', alignItems: 'center', gap: 2 }}>
    <button onClick={goBack} style={navBtn}>‹</button>
    <span style={{ width: 90, textAlign: 'center', fontSize: 13, fontWeight: 700, color: 'var(--t1)' }}>
      {date}
    </span>
    <div style={{ width: 28, display: 'flex', justifyContent: 'center' }}>
      {canForward
        ? <button onClick={goForward} style={navBtn}>›</button>
        : <span style={{ width: 28 }}/>
      }
    </div>
  </div>
)
```

## ③ 상수 (DailyReportPage.tsx line 30~41) — verbatim source
```tsx
const iconBtn: React.CSSProperties = {
  width: 34, height: 34, borderRadius: 8, flexShrink: 0,
  background: 'var(--bg3)', border: '1px solid var(--bd)',
  cursor: 'pointer', display: 'flex', alignItems: 'center', justifyContent: 'center',
}

const navBtn: React.CSSProperties = {
  width: 28, height: 28, borderRadius: 7, border: '1px solid var(--bd)',
  background: 'var(--bg3)', color: 'var(--t1)', fontSize: 16, fontWeight: 700,
  cursor: 'pointer', display: 'flex', alignItems: 'center', justifyContent: 'center',
  lineHeight: '1',
}
```

## ④ 14-reports W2 inherit class (components.css SW1) — verbatim, 재정의 X
```css
.page-header { flex-shrink: 0; background: var(--surface-raised); border-bottom: 1px solid var(--border-default); padding: 8px 12px 9px; display: flex; align-items: center; gap: 8px; }
.back-btn { width: 34px; height: 34px; border-radius: 8px; flex-shrink: 0; background: var(--surface-sunken); border: 1px solid var(--border-default); color: var(--text-secondary); cursor: pointer; display: flex; align-items: center; justify-content: center; }
.page-title { flex: 1; font-size: 18px; font-weight: 700; color: var(--text-primary); line-height: 1.4; }
```

## ⑤ 신규 정의 3+1 class (본 sketch 안 `<style>` 박제, W7 TSX wave 가 components.css 추가)
```css
.date-nav {
  display: flex;
  align-items: center;
  gap: 4px;  /* source navBtn gap 2 → 4, 노안 격상 */
}
.date-nav-btn {
  width: 28px;
  height: 28px;
  border-radius: 6px;  /* source navBtn 7 → 6, sm-token 일관 */
  border: 1px solid var(--border-default);
  background: var(--surface-sunken);
  color: var(--text-primary);
  font-size: 16px;
  font-weight: 700;
  cursor: pointer;
  display: flex;
  align-items: center;
  justify-content: center;
  line-height: 1;
}
.date-display {
  min-width: 96px;  /* source width 90 → 96 (4자리 연도 + 한글 안전 폭, 14-reports fix1 일관) */
  text-align: center;
  font-size: 14px;  /* source 13 → 14 (text-body-sm), 노안 격상 */
  font-weight: 700;
  color: var(--text-primary);
  line-height: 1;
  white-space: nowrap;
  padding: 0 4px;
}
.date-nav-spacer {
  width: 28px;
  height: 28px;
  flex-shrink: 0;
}
```

## ⑥ 14-reports W2 의 frame-shell / page-bg / variant-label / global-header-placeholder 패턴
(sketch-wave-2-mobile-header-card.html line 295~320 / 466~474 / 537~711 mirror — 본 sketch 도 동일 frame 구조 재사용)

- `.page-bg-dark` / `.page-bg-light` — 외부 컨테이너 톤 분기
- `.frame-shell` — 393px width, border 1px var(--border-default), border-radius 18px, overflow hidden
- `.global-header-placeholder` — h-48 회색 톤 (App.tsx GlobalHeader placeholder)
- `.variant-label` — 12px JetBrains Mono, sketch 외부 chrome 만 적용 (frame 안 본문 아님)

## ⑦ data-theme / tailwind config / typography (14-reports W2 verbatim 인용)
- tokens.css line 16~119 (다크/라이트 토큰) verbatim
- tokens.css line 122~146 spacing verbatim
- tokens.css line 165~172 radius verbatim
- typography.css line 28~95 (text-caption ~ text-display) verbatim
- tailwind.config inline script — 14-reports W2 line 477~518 verbatim
</interfaces>

</context>

<tasks>

<task type="auto">
  <name>Task 1: sketch-wave-2-mobile-header-date-nav.html 생성 — 다크/라이트 × 오늘/미래 4 frame</name>
  <files>cha-bio-safety/docs/redesign-context/15-daily-report/sketch-wave-2-mobile-header-date-nav.html</files>
  <action>
**목표:** 15-daily-report W2 sketch HTML 1개 파일을 생성한다. 14-reports W2 (sketch-wave-2-mobile-header-card.html) 의 frame 구조 / chrome 패턴을 1:1 mirror 하되, 본 페이지의 책임 영역인 **모바일 헤더 + dateNav** 만 시각화한다.

**파일 경로:** `cha-bio-safety/docs/redesign-context/15-daily-report/sketch-wave-2-mobile-header-date-nav.html` (15-daily-report 폴더 평면 sibling)

---

## ① HTML 셋업 — 14-reports W2 line 1~64 mirror

- `<!DOCTYPE html>` + `<html lang="ko" data-theme="dark">` (외부 컨테이너 기본 다크)
- 상단 주석 블록 (14-reports W2 line 3~58 패턴):
  - 파일명 / 작성일 (2026-05-21) / Quick ID (260521-25s) / 범위 / out of scope
  - W1 LOCKED 5건 매트릭스 (wave-1-index.md §7 OQ #1~#5 default 인용)
  - W2 의 OQ ≥3건 (default 답 명시)
  - 메모리 룰 박제 ≥7건 (wave-1-index §5 verbatim slug — feedback_design_sketch_first / feedback_sketch_realistic_data / feedback_planner_prompt_sketch_verbatim / feedback_tailwind_token_class_pattern / feedback_tailwind_w8_h8_is_48px / feedback_tsx_wave_emoji_dot_gap / feedback_text_caption_leading_none)
- `<head>` Pretendard CDN + tailwind CDN 동일

---

## ② `<style>` 블록 — 토큰/타이포/sketch 헬퍼

14-reports W2 line 65~474 verbatim 복제 후 본 페이지 신규 class 4건 추가.

**복제 (14-reports W2 verbatim):**
- 다크/라이트 토큰 (tokens.css line 16~119 verbatim)
- spacing (line 122~146 + @media min-width 768 분기)
- radius (line 165~172)
- Typography (typography.css line 28~95 verbatim — `.text-caption` `.text-label` `.text-body-sm` `.text-body` `.text-title` `.text-heading` `.text-display` + `.text-mono` `.font-medium` `.font-semibold` `.leading-none` `.dot-meta`)
- Sketch 헬퍼: `.page-bg-dark` / `.page-bg-light` / `.frame-shell` (393px) / `.global-header-placeholder` (h-48) / `.variant-label` (12px JetBrains Mono)
- **inherit (재정의 X, comment 로 인용 명시):**
  - `.page-header` — components.css line 23 verbatim
  - `.back-btn` — components.css line 24 verbatim
  - `.page-title` — components.css line 25 verbatim

**신규 정의 (본 sketch 안 박제, W7 TSX wave 가 components.css 추가):**
```css
/* ── §1.5 dateNav (W2 신규 정의 — DailyReportPage line 387~401 매핑) ─────── */
.date-nav { display: flex; align-items: center; gap: 4px; }
.date-nav-btn { width: 28px; height: 28px; border-radius: 6px; border: 1px solid var(--border-default); background: var(--surface-sunken); color: var(--text-primary); font-size: 16px; font-weight: 700; cursor: pointer; display: flex; align-items: center; justify-content: center; line-height: 1; }
.date-display { min-width: 96px; text-align: center; font-size: 14px; font-weight: 700; color: var(--text-primary); line-height: 1; white-space: nowrap; padding: 0 4px; }
.date-nav-spacer { width: 28px; height: 28px; flex-shrink: 0; }
```

**필수 comment (verify gate #8 / #9 / #10 만족):**
- `.page-header` 위 comment: `/* 14-reports SW1 inherit (components.css line 23 verbatim) — 재정의 X */`
- `.back-btn` 위 comment: `/* 14-reports SW1 inherit (components.css line 24 verbatim) — 재정의 X */`
- `.page-title` 위 comment: `/* 14-reports SW1 inherit (components.css line 25 verbatim) — 18px font-size, 노안 격상 (DailyReportPage line 456 fontSize:14 → 18) */`

---

## ③ tailwind.config inline `<script>` — 14-reports W2 line 477~518 verbatim 복제

---

## ④ `<body class="page-bg-dark min-h-screen p-6">` + 헤더 캡션

```html
<header class="mb-6 max-w-[900px] mx-auto">
  <h1 class="text-heading text-white mb-2">15-daily-report sketch W2 — 모바일 헤더 + dateNav</h1>
  <p class="text-label text-zinc-400">
    모바일 393px × 다크/라이트 × 오늘(2026-05-21) / 미래(2026-05-25) 4 frame. 
    14-reports W2 chrome 패턴 inherit (page-header / back-btn / page-title) + 신규 dateNav 3 class 정의.
    DailyReportPage.tsx line 387~401 (dateNav) + line 449~458 (모바일 헤더) verbatim 매핑.
  </p>
</header>
```

---

## ⑤ 본문 — 4 frame side-by-side (다크/라이트 × 오늘/미래)

`<section class="flex gap-8 flex-wrap items-start justify-center mb-12">` 안 frame 4개. 14-reports W2 line 537~711 의 dual-frame 패턴을 quad-frame 으로 확장.

### F1 — 다크 / 오늘 (2026-05-21, canForward 분기 = `›` 노출)

```html
<div>
  <div class="variant-label" style="color:#f6f8fa;">다크 모드 / 오늘 (393px 모바일)</div>
  <div data-theme="dark" class="frame-shell">

    <!-- App.tsx GlobalHeader placeholder (모바일 무노출, chrome 룰 mirror) -->
    <div class="global-header-placeholder">App GlobalHeader (h-12, /daily-report 자체 헤더 페이지)</div>

    <!-- ① 자체 헤더 — DailyReportPage.tsx line 449~458 verbatim -->
    <header class="page-header">
      <!-- 백 버튼 (line 451~455) — iconBtn 34x34 + lucide ChevronLeft size=15 (raw svg 폐기, design-system §7.1) -->
      <button class="back-btn" aria-label="뒤로 가기">
        <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <path d="m15 18-6-6 6-6"/>
        </svg>
      </button>

      <!-- 페이지 타이틀 (line 456) — "일일 업무 일지" verbatim + 노안 격상 14 → 18 -->
      <span class="page-title">일일 업무 일지</span>

      <!-- ② dateNav (line 387~401 verbatim 매핑) -->
      <div class="date-nav">
        <!-- ‹ navBtn (line 390) → lucide ChevronLeft size=16 -->
        <button class="date-nav-btn" aria-label="이전 날짜">
          <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
            <path d="m15 18-6-6 6-6"/>
          </svg>
        </button>
        <!-- 날짜 라벨 (line 391~393) — width 90 → min-width 96 + fontSize 13 → 14 -->
        <span class="date-display">2026-05-21</span>
        <!-- canForward 분기 (line 394~399) — 오늘 frame: › 노출 -->
        <button class="date-nav-btn" aria-label="다음 날짜">
          <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
            <path d="m9 18 6-6-6-6"/>
          </svg>
        </button>
      </div>
    </header>

    <!-- ③ 본문 placeholder (W3~W4 책임) — dashed bar 1줄 -->
    <div style="padding: 12px 16px; min-height: 320px; background: var(--surface-page);">
      <div style="border: 1px dashed var(--border-default); border-radius: 8px; padding: 14px; color: var(--text-tertiary); font-size: 12px; text-align: center; line-height: 1.6;">
        W3~W4 책임 영역 — EditableCard × 3 + 인원현황 카드 + 다운로드 버튼 (본 W2 시안 범위 외)
      </div>
    </div>

  </div>
</div>
```

### F2 — 다크 / 미래 (2026-05-25, canForward 분기 = spacer 28px)

F1 markup 1:1 mirror하되 dateNav 만 변경:
- `date-display` 텍스트: `2026-05-25`
- `›` 버튼 제거 → `<span class="date-nav-spacer" aria-hidden="true"></span>` 로 교체
- variant-label: `"다크 모드 / 미래 시도 후 (393px 모바일)"`
- 본문 placeholder 멘트: `"미래 날짜 표시 — canForward=false 시 › 자리에 28px spacer (chevron 자체 숨김)"` 로 변경 (W3/W4 영역 외 보조 설명)

### F3 — 라이트 / 오늘 (F1 mirror, data-theme="light")

F1 markup 1:1 mirror, `data-theme="dark"` → `data-theme="light"`, variant-label `"라이트 모드 / 오늘 (393px 모바일)"`.

### F4 — 라이트 / 미래 (F2 mirror, data-theme="light")

F2 markup 1:1 mirror, `data-theme="dark"` → `data-theme="light"`, variant-label `"라이트 모드 / 미래 시도 후 (393px 모바일)"`.

---

## ⑥ Notes block — sketch 검토자에게 안내 (frame 외부)

14-reports W2 line 716~734 의 Notes 패턴 mirror:

```html
<section class="max-w-[900px] mx-auto text-zinc-400 text-label" style="line-height:1.6;">
  <h2 class="text-title text-white mb-3">W2 검토 포인트</h2>
  <ul class="list-disc pl-5 space-y-1">
    <li>헤더 chrome = 14-reports SW1 inherit (.page-header / .back-btn / .page-title) — 재정의 0건.</li>
    <li>dateNav 신규 3 class — .date-nav / .date-nav-btn / .date-display (+ .date-nav-spacer). W7 TSX wave 가 components.css 추가.</li>
    <li>헤더 타이틀 = 18px (text-title). 노안 격상 14 → 18 (DailyReportPage line 456 fontSize:14).</li>
    <li>날짜 라벨 = 14px (text-body-sm) + leading-none + min-width 96px. 노안 격상 13 → 14 + width 90 → 96 (4자리 연도 + 안전 폭, 14-reports fix1 일관).</li>
    <li>‹/› 버튼 = 28x28 + radius 6 (source 7 → 6, sm-token 일관). lucide ChevronLeft/Right size=16 (글리프 텍스트 0건).</li>
    <li>back-btn = 34x34 source verbatim 보존. lucide ChevronLeft size=15 (raw svg 폐기).</li>
    <li>오늘 frame (F1/F3) = › 노출 / 미래 frame (F2/F4) = 28px spacer. canForward 분기 시연.</li>
    <li>날짜 포맷 = YYYY-MM-DD verbatim (todayKST 리턴값 그대로). 다른 페이지와 일관, 향후 통일 결정 시 별도 wave.</li>
  </ul>

  <h2 class="text-title text-white mb-3 mt-6">W3 진입 전 OQ ≥3건 (default 답 채택 시 별 의견 없는 것으로 진행)</h2>
  <ul class="list-disc pl-5 space-y-1">
    <li>OQ #1 — dateNav 의 calendar 진입점 (날짜 picker) 추가? <strong class="text-zinc-200">default: 미추가</strong> (소스에 없음, memory feedback_sketch_realistic_data 룰).</li>
    <li>OQ #2 — 오늘 날짜 시각 강조 (bg highlight, accent text, "오늘" 배지)? <strong class="text-zinc-200">default: 평범 유지</strong> (소스 그대로, 단순한 게 더 인지 좋음).</li>
    <li>OQ #3 — 날짜 포맷 — YYYY-MM-DD (소스) vs 2026.05.21 vs 5/21(수) vs 5월 21일 (수)? <strong class="text-zinc-200">default: YYYY-MM-DD 유지</strong> (소스 verbatim, 다른 페이지와 일관).</li>
    <li>OQ #4 (선택) — dateNav 위치 — 헤더 안 (모바일, line 457) vs 본문 위 별도 row (데스크톱 line 410)? <strong class="text-zinc-200">default: 헤더 안 유지</strong> (모바일 — 1줄 컴팩트).</li>
  </ul>
</section>
```

---

## ⑦ verify gate (≥15) — 본 sketch 자체 검증

executor 가 파일 작성 직후 다음 명령으로 self-verify (verify 섹션에 동일 명령 박제):

**Negative (모두 0):**
1. `grep -c '⬇' [file]` → `=0` (이모지 0)
2. `grep -c 'linear-gradient' [file]` → `=0`
3. `grep -cE 'text-status-|bg-status-' [file]` → `=0` (status- prefix 0)
4. `grep -v '^[[:space:]]*<!--' [file] | grep -v '^[[:space:]]*//' | grep -cE '[^a-zA-Z]·[^a-zA-Z]'` → `=0` (본문 가운뎃점 0, dot span 만 허용 — W2 영역 자연 0)
5. `grep -cE 'w-8|h-8' [file]` → `=0` (본문 utility 0, 인라인 28px 허용)
6. `grep -vE '^[[:space:]]*(/\*|\*|<!--)' [file] | grep -cE 'font-size:[[:space:]]*(9|10|11)px'` → `=0` (본문 폰트 9·10·11px 0)
7. `grep -cE '📄|✅|❌|⚠️|🎯' [file]` → `=0`

**Positive:**
8. `.page-header` inherit comment 포함 사용 ≥1: `grep -c '\.page-header' [file]` → `≥1`
9. `.back-btn` inherit comment 포함 사용 ≥1: `grep -c '\.back-btn\|class="back-btn"' [file]` → `≥1` (실제 frame 4 + 정의 1 ≥5)
10. `.page-title` inherit comment 포함 사용 ≥1: `grep -c '\.page-title\|class="page-title"' [file]` → `≥1`
11. `.date-nav` 정의 ≥1: `grep -c '\.date-nav[^-]' [file]` → `≥1` (정의 1 + frame 4 ≥5)
12. `.date-nav-btn` 정의 ≥1: `grep -c '\.date-nav-btn\|class="date-nav-btn"' [file]` → `≥1`
13. `.date-display` 정의 ≥1: `grep -c '\.date-display\|class="date-display"' [file]` → `≥1`
14. `.date-nav-spacer` 정의 ≥1 + F2/F4 사용 ≥2: `grep -c 'date-nav-spacer' [file]` → `≥3`
15. lucide ChevronLeft inline SVG ≥6: `grep -cE '<path d="m15 18-6-6 6-6"/>' [file]` → `≥6` (back-btn × 4 + dateNav ‹ × 4 = 8)
16. lucide ChevronRight inline SVG ≥2: `grep -cE '<path d="m9 18 6-6-6-6"/>' [file]` → `≥2` (F1+F3 오늘 frame 만)
17. spacer demo (F2+F4 미래 frame) ≥2: `grep -c 'date-nav-spacer' [file]` → `≥2`
18. 메모리 룰 inline unique feedback_* slug ≥7: `grep -oE 'feedback_[a-z_]+' [file] | sort -u | wc -l` → `≥7`
19. dual frame label (다크/라이트) ≥4: `grep -cE '다크 모드|라이트 모드' [file]` → `≥4`
20. '일일 업무 일지' 타이틀 verbatim ≥4: `grep -c '일일 업무 일지' [file]` → `≥4`
21. 날짜 YYYY-MM-DD 표시 ≥4: `grep -cE '2026-05-2[15]' [file]` → `≥4`

---

## ⑧ negative scope (이 task 에서 절대 하지 않는 것, memory `feedback_cbc7119_design_never_wrangler` + `feedback_check_branch_before_edit`)

- ❌ `cha-bio-safety/src/pages/DailyReportPage.tsx` 수정 — 본 wave 분석 대상이지 수정 대상 아님
- ❌ `cha-bio-safety/src/styles/components.css` 수정 — 14-reports SW1 결과물 그대로 inherit, 신규 .date-nav-* 추가는 W7 TSX wave 가 처리
- ❌ 다른 페이지 (`13-schedule` / `14-reports` / `02-inspection` / `06-floor-plan` 등) sketch / TSX 수정
- ❌ 다른 W3~W7 sketch 파일 생성 (본 wave 산출물은 sketch-wave-2-mobile-header-date-nav.html 1개만)
- ❌ wrangler 명령 / `npm run deploy` (CLAUDE.local.md + memory `feedback_cbc7119_design_never_wrangler` 룰)
- ❌ App.tsx 수정 (이미 MOBILE_NO_NAV_PATHS 등재됨)

---

## ⑨ 메모리 룰 inline 인용 (≥7건, verbatim slug — wave-1-index.md §5)

본 sketch HTML 상단 주석 블록에 다음 메모리 룰 slug 를 inline 박제 (executor 가 grep 으로 verify 가능하도록 unique slug 형태):

1. `feedback_design_sketch_first.md` — spacing/sizing 도 sketch 시안 먼저
2. `feedback_sketch_realistic_data.md` — 표시 분기/라벨 룰은 코드 그대로 (`{date}` = YYYY-MM-DD verbatim, '일일 업무 일지' verbatim)
3. `feedback_planner_prompt_sketch_verbatim.md` — sketch CSS 토큰/사이즈는 grep 추출 후 W7 변환 wave 에 인용
4. `feedback_tailwind_token_class_pattern.md` — status- prefix 없음 (text-status-fire-bar X / text-fire-bar O)
5. `feedback_tailwind_w8_h8_is_48px.md` — w-8 = 48px (32 아님), w-7 = 32px. 본 sketch 는 source iconBtn 34x34 / navBtn 28x28 모두 var() / 명시 px 사용
6. `feedback_tsx_wave_emoji_dot_gap.md` — 이모지 0 + dot span 추가 markup. ‹/› 글리프 0건 / lucide ChevronLeft/Right SVG 사용
7. `feedback_text_caption_leading_none.md` — text-caption lh:1.5 가 h-8 컨테이너 안에서 시각 패딩. .date-display + .date-nav-btn 모두 line-height: 1 명시

추가 보너스 (≥1, 합산 ≥8):
8. `feedback_cbc7119_design_never_wrangler.md` — 디자인 wave 중 wrangler 명령 금지, main push 만 자동 cbc7119-preview 배포

---

## 작업 순서

1. 14-reports W2 (`sketch-wave-2-mobile-header-card.html`) 의 line 1~474 (HTML 셋업 + style + tailwind.config) verbatim 복제 → 새 파일 생성
2. 상단 주석 블록을 15-daily-report 컨텍스트로 수정 (제목, Quick ID 260521-25s, 범위, W1 LOCKED, W2 OQ, 메모리 룰 ≥7건)
3. style 블록 끝부분에 `.date-nav` / `.date-nav-btn` / `.date-display` / `.date-nav-spacer` 4 class 추가 + inherit comment 명시
4. body 안 헤더 캡션 + 4 frame side-by-side (F1 다크/오늘 → F2 다크/미래 → F3 라이트/오늘 → F4 라이트/미래) 작성
5. 각 frame 안 markup:
   - `.global-header-placeholder` (h-48)
   - `.page-header` (inherit)
     - `.back-btn` (inherit) + lucide ChevronLeft size=15 inline SVG
     - `.page-title` (inherit) `일일 업무 일지`
     - `.date-nav` (신규)
       - `.date-nav-btn` + lucide ChevronLeft size=16 inline SVG
       - `.date-display` `2026-05-21` 또는 `2026-05-25`
       - F1/F3 (오늘): `.date-nav-btn` + lucide ChevronRight size=16 inline SVG
       - F2/F4 (미래): `.date-nav-spacer`
   - 본문 placeholder dashed bar 1줄
6. Notes block (W2 검토 포인트 ≥8건 + W3 진입 전 OQ ≥3건 default 답)
7. 작성 직후 self-verify gate ≥15 (≥21 항목) 명령 실행 — 모두 PASS 시 완료
8. git status / diff 확인 (cha-bio-safety/src 변경 0 라인 검증)
9. 단일 atomic commit:
   `docs(15-daily-report): sketch wave 2 — 모바일 헤더 + dateNav (다크/라이트 393px, 미래 날짜 spacer)`
  </action>
  <verify>
    <automated>
FILE="/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/15-daily-report/sketch-wave-2-mobile-header-date-nav.html"
echo "── 파일 존재 ──"
test -f "$FILE" && echo "PASS: exists" || { echo "FAIL: missing"; exit 1; }
echo "── 라인수 ≥400 ──"
LINES=$(wc -l < "$FILE")
echo "lines=$LINES"
[ "$LINES" -ge 400 ] && echo "PASS" || { echo "FAIL"; exit 1; }
echo "── Negative gates (모두 0) ──"
G1=$(grep -c '⬇' "$FILE" || echo 0)
G2=$(grep -c 'linear-gradient' "$FILE" || echo 0)
G3=$(grep -cE 'text-status-|bg-status-' "$FILE" || echo 0)
G5=$(grep -vE '^[[:space:]]*(<!--|//)' "$FILE" | grep -cE '(^| )w-8($| )|(^| )h-8($| )' || echo 0)
G6=$(grep -vE '^[[:space:]]*(/\*|\*|<!--)' "$FILE" | grep -cE 'font-size:[[:space:]]*(9|10|11)px' || echo 0)
G7=$(grep -cE '📄|✅|❌|⚠️|🎯' "$FILE" || echo 0)
echo "G1(⬇)=$G1 G2(grad)=$G2 G3(status-)=$G3 G5(w-8/h-8)=$G5 G6(9-11px)=$G6 G7(이모지)=$G7"
[ "$G1" = 0 ] && [ "$G2" = 0 ] && [ "$G3" = 0 ] && [ "$G5" = 0 ] && [ "$G6" = 0 ] && [ "$G7" = 0 ] && echo "PASS: all negative=0" || { echo "FAIL: negative gates not all 0"; exit 1; }
echo "── Positive gates ──"
P8=$(grep -c 'page-header' "$FILE")
P9=$(grep -c 'back-btn' "$FILE")
P10=$(grep -c 'page-title' "$FILE")
P11=$(grep -cE 'date-nav[^-]|\.date-nav[[:space:]{]' "$FILE")
P12=$(grep -c 'date-nav-btn' "$FILE")
P13=$(grep -c 'date-display' "$FILE")
P14=$(grep -c 'date-nav-spacer' "$FILE")
P15=$(grep -cE '<path d="m15 18-6-6 6-6"/>' "$FILE")
P16=$(grep -cE '<path d="m9 18 6-6-6-6"/>' "$FILE")
P18=$(grep -oE 'feedback_[a-z_]+' "$FILE" | sort -u | wc -l | tr -d ' ')
P19=$(grep -cE '다크 모드|라이트 모드' "$FILE")
P20=$(grep -c '일일 업무 일지' "$FILE")
P21=$(grep -cE '2026-05-2[15]' "$FILE")
echo "P8(page-header)=$P8 P9(back-btn)=$P9 P10(page-title)=$P10 P11(date-nav)=$P11 P12(date-nav-btn)=$P12 P13(date-display)=$P13 P14(date-nav-spacer)=$P14"
echo "P15(ChevLeft)=$P15 P16(ChevRight)=$P16 P18(feedback_*)=$P18 P19(다크/라이트)=$P19 P20(타이틀)=$P20 P21(날짜)=$P21"
[ "$P8" -ge 1 ] && [ "$P9" -ge 1 ] && [ "$P10" -ge 1 ] && [ "$P11" -ge 1 ] && [ "$P12" -ge 1 ] && [ "$P13" -ge 1 ] && [ "$P14" -ge 3 ] && [ "$P15" -ge 6 ] && [ "$P16" -ge 2 ] && [ "$P18" -ge 7 ] && [ "$P19" -ge 4 ] && [ "$P20" -ge 4 ] && [ "$P21" -ge 4 ] && echo "PASS: all positive gates met" || { echo "FAIL: positive gates not all met"; exit 1; }
echo "── 변경 scope (cha-bio-safety/src 변경 0 줄) ──"
SRC_CHANGES=$(cd /Users/jykevin/Documents/cbc7119-design && git diff --name-only HEAD -- cha-bio-safety/src 2>/dev/null | wc -l | tr -d ' ')
echo "src changes=$SRC_CHANGES"
[ "$SRC_CHANGES" = 0 ] && echo "PASS: src untouched" || { echo "FAIL: src modified"; exit 1; }
echo "── 다른 sketch 폴더 변경 0 ──"
OTHER_CHANGES=$(cd /Users/jykevin/Documents/cbc7119-design && git diff --name-only HEAD -- 'cha-bio-safety/docs/redesign-context/14-reports' 'cha-bio-safety/docs/redesign-context/13-schedule' 'cha-bio-safety/docs/redesign-context/02-inspection' 'cha-bio-safety/docs/redesign-context/06-floor-plan' 2>/dev/null | wc -l | tr -d ' ')
echo "other sketch changes=$OTHER_CHANGES"
[ "$OTHER_CHANGES" = 0 ] && echo "PASS: other pages untouched" || { echo "FAIL: other pages modified"; exit 1; }
echo ""
echo "✓ ALL GATES PASS — sketch W2 작성 + verify 완료"
    </automated>
  </verify>
  <done>
- sketch-wave-2-mobile-header-date-nav.html 파일 생성 (≥400 lines)
- 4 frame (F1 다크/오늘 + F2 다크/미래 + F3 라이트/오늘 + F4 라이트/미래) side-by-side 시각화
- 14-reports SW1 inherit 3 class (.page-header / .back-btn / .page-title) 사용 + inherit comment 명시
- 신규 정의 4 class (.date-nav / .date-nav-btn / .date-display / .date-nav-spacer) 박제
- lucide ChevronLeft size=15 (back-btn) + size=16 (dateNav ‹) + ChevronRight size=16 (dateNav ›) inline SVG (글리프 텍스트 0건)
- 미래 frame (F2/F4) 의 spacer 28px 시연 (canForward=false 분기)
- Notes block 검토 포인트 ≥8건 + W3 진입 전 OQ ≥3건 default 답
- 메모리 룰 unique feedback_* slug ≥7건 inline 박제
- verify gate 21개 모두 PASS
- cha-bio-safety/src 및 다른 페이지 sketch 변경 0 라인
- atomic 단일 commit: `docs(15-daily-report): sketch wave 2 — 모바일 헤더 + dateNav (다크/라이트 393px, 미래 날짜 spacer)`
  </done>
</task>

</tasks>

<verification>
## 전체 검증 (task 1 완료 후 자동 실행 — verify 섹션 박제됨)

**파일 존재 + 라인 ≥400 + Negative 6 + Positive 13 + scope 2 = 22 gate**

모두 PASS 시 W3 (EditableCard + 인원현황 카드) 진입 자격 확보. 단, W3 진입은 사용자 컨펌 후에만 (memory `feedback_avoid_premature_confirmation`).

## SUMMARY 요구 사항

`.planning/quick/260521-25s-redesign-15-daily-report-sketch-wave-2-d/260521-25s-SUMMARY.md` 작성 시 포함:
- frame matrix 4 (다크/라이트 × 오늘/미래) 시각 결과 박제
- inherit class 3 (14-reports SW1) + 신규 class 4 (W2) 명단
- verify gate 22개 결과 PASS/FAIL 표
- W3 진입 전 OQ ≥3건 default 답 명시 (사용자 컨펌 항목)
- 메모리 룰 inline unique slug 개수
- 다음 단계: 사용자가 OQ 답변 → W3 (EditableCard × 3 + 인원현황 카드) sketch wave 진입
</verification>

<success_criteria>
- [ ] sketch-wave-2-mobile-header-date-nav.html 파일 1개 생성 (≥400 lines)
- [ ] 4 frame (다크/라이트 × 오늘/미래) side-by-side 시각 확인 가능
- [ ] 14-reports SW1 inherit class 3개 (.page-header / .back-btn / .page-title) 재정의 0건
- [ ] 신규 정의 4 class (.date-nav / .date-nav-btn / .date-display / .date-nav-spacer) sketch 안 박제
- [ ] DailyReportPage.tsx line 387~401 (dateNav) + line 449~458 (모바일 헤더) verbatim 매핑
- [ ] 노안 격상: 헤더 타이틀 14→18 / 날짜 라벨 13→14 / width 90→96
- [ ] lucide ChevronLeft size=15 (back-btn × 4) + ChevronLeft size=16 (dateNav ‹ × 4) + ChevronRight size=16 (dateNav › × 2, F1+F3 만)
- [ ] 미래 frame (F2/F4) spacer 28px 시연 = ≥2
- [ ] negative gate 7개 모두 0 (이모지 / 그라데이션 / status- prefix / 가운뎃점 / w-8 h-8 / 9·10·11px / 추가 이모지)
- [ ] positive gate 13개 모두 충족
- [ ] 메모리 룰 unique feedback_* slug ≥7건 inline
- [ ] Notes block 검토 포인트 ≥8건 + W3 OQ ≥3건 default
- [ ] cha-bio-safety/src 변경 0 라인 / 다른 페이지 sketch 변경 0 라인
- [ ] atomic 단일 commit: `docs(15-daily-report): sketch wave 2 — 모바일 헤더 + dateNav (다크/라이트 393px, 미래 날짜 spacer)`
</success_criteria>

<output>
After completion, create `.planning/quick/260521-25s-redesign-15-daily-report-sketch-wave-2-d/260521-25s-SUMMARY.md` with:
- frame matrix 4 결과 (다크/오늘, 다크/미래, 라이트/오늘, 라이트/미래)
- inherit class (14-reports SW1) 3건 + 신규 class (W2) 4건 명단
- verify gate 22개 결과
- W3 진입 전 OQ ≥3건 default 답
- 메모리 룰 inline unique slug 개수
- 사용자 next step: OQ 답변 → W3 (EditableCard + 인원현황 카드) sketch 진입
</output>
