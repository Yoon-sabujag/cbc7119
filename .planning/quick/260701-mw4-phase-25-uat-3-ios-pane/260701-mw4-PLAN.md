---
phase: 260701-mw4
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cha-bio-safety/src/pages/DashboardPage.tsx
  - cha-bio-safety/src/pages/InspectionPage.tsx
autonomous: true
requirements: [FIX-1, FIX-2, FIX-3]

must_haves:
  truths:
    - "모바일 대시보드가 iOS PWA(100vh)에서 5행 그리드로 렌더되어 ⑤ 월간 도넛 카드가 항상 보인다 (붕괴/미렌더 0)"
    - "모바일 대시보드에서 ①배너 · ①-b라이브 · ②현황 이 겹치지 않는다"
    - "데스크톱 화재수신반 3분할 pane 헤더가 형제 '조치 상세' pane 헤더와 동일 크롬(px-5 py-3 / w-8 h-8 / ChevronLeft size 14)으로 보인다"
    - "데스크톱 대시보드 우측 오늘일정 카드 top == 좌측 빠른도구 top (1920×1080)"
  artifacts:
    - path: "cha-bio-safety/src/pages/DashboardPage.tsx"
      provides: "FIX-1 모바일 5행 그리드 + ④제거 / FIX-3 데스크톱 우측 컬럼 재구성"
    - path: "cha-bio-safety/src/pages/InspectionPage.tsx"
      provides: "FIX-2 화재수신반 pane 헤더 크롬 정렬"
  key_links:
    - from: "DashboardPage main grid (모바일)"
      to: "gridTemplateRows 5 tracks"
      via: "grid child 5개(①배너 ①-b라이브 ②현황 ③도구 ⑤도넛)"
      pattern: "minmax\\(125px, 1fr\\)"
    - from: "InspectionPage 화재수신반 id-head"
      to: "형제 '조치 상세' pane 헤더 표준"
      via: "동일 크롬 클래스"
      pattern: "gap-2.5 px-5 py-3 border-b"
---

<objective>
Phase 25 화재수신반 원격감시 UI 의 cbc7119-preview 시각 UAT 후속 순수 시각/레이아웃 수정 3건. 콘솔1(production)이 실제 코드 실측으로 원인·라인 특정 완료, 사용자 시안 승인 완료.

- FIX-1: 모바일 대시보드 iOS 그리드 붕괴 (④오늘일정 제거 + 6행→5행 재구성)
- FIX-2: 데스크톱 화재수신반 pane 헤더 크롬을 형제 pane 표준에 정렬
- FIX-3: 데스크톱 우측 캘린더/오늘일정 정렬 (approach a 구조 정렬)

Purpose: Phase 25 배포된 UI 의 iOS 실기기 레이아웃 붕괴 + 데스크톱 헤더/정렬 시각 결함 해소.
Output: DashboardPage.tsx (모바일+데스크톱 두 영역) · InspectionPage.tsx 수정, fix 별 atomic commit 3개.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@./CLAUDE.md
@./CLAUDE.local.md
@.planning/quick/260701-mw4-phase-25-uat-3-ios-pane/260701-mw4-sketch.html
@cha-bio-safety/src/pages/DashboardPage.tsx
@cha-bio-safety/src/pages/InspectionPage.tsx

## 워크트리 룰 (반드시 준수)
- 이 워크트리는 디자인 redesign 전용. **wrangler / npm run deploy 절대 금지.**
- commit 은 현재 브랜치(예상: `redesign/25-panel-uat-fixes`)에 fix 별 atomic 하게. **push/deploy 는 오케스트레이터가 마지막에 1회** — executor 는 push 하지 않는다.
- 표시 분기/라벨 룰(없음/N개/X-Y/완료 등)은 코드 그대로. 시안에서 손볼 건 시각 디자인/레이아웃 뿐.
- tailwind 토큰 함정: **w-8/h-8 = 48px, w-7/h-7 = 32px** (config override). status- prefix 없음.

## 라인 drift 주의
아래 라인번호는 실측값(2026-07-01 기준)이나, 편집 전 executor 가 **반드시 grep/Read 로 재확인** 후 편집할 것. 값을 추측으로 바꾸지 말 것 — 모두 확정 스펙이다.
</context>

<tasks>

<task type="auto">
  <name>Task 1 (FIX-1): 모바일 대시보드 iOS 그리드 붕괴 수정 — ④오늘일정 제거 + 5행 재구성</name>
  <files>cha-bio-safety/src/pages/DashboardPage.tsx (모바일 트리, ~:624-836)</files>
  <action>
원인(콘솔1 RCA): Phase 25 가 ①-b 화재수신반 라이브(aspect-video min-h-[180px], ~:652-662)를 그리드 행으로 추가했으나 gridTemplateRows(~:627-631)는 옛 6행 높이예산 그대로. iOS 분기 'auto auto auto auto 1fr auto' 에서 ④오늘일정(1fr) 0붕괴("얇은 선"), 라이브가 트랙 밖 오버플로해 ②오늘현황과 겹침, ⑤월간 도넛 카드(overflow-hidden)가 near-0 트랙에 눌려 iOS 미렌더.

편집 전: `grep -n "④ 오늘 일정" src/pages/DashboardPage.tsx` 와 `grep -n "gridTemplateRows" src/pages/DashboardPage.tsx` 로 현재 라인 재확인.

변경 (승인됨):
1) ④ 오늘 일정 카드 블록 **완전 제거** — `{/* ④ 오늘 일정 */}` 주석(~:767)부터 그 카드를 닫는 `</div>`(~:789)까지 전부 삭제. 내부 `timed`/`untimed` 렌더(ScheduleRow map 포함) 전부 제거. 정보 손실 0 — 오늘일정 카운트는 ② 오늘 현황 stat card 의 '오늘 일정'(`stats.scheduleCount`, ~:700-707)에 이미 존재. (②·⑤ 및 그 외 카드는 무변경.)
2) gridTemplateRows(~:627-631) 를 6행 → 5행으로. 정확한 old_string/new_string:
   old:
     gridTemplateRows: IS_ANDROID
       ? 'auto auto auto auto 1fr minmax(140px, auto)'
       : 'auto auto auto auto 1fr auto',
   new:
     gridTemplateRows: IS_ANDROID
       ? 'auto auto auto auto minmax(140px, 1fr)'
       : 'auto auto auto auto minmax(125px, 1fr)',
   이유: ④ 제거로 grid child 6→5. 마지막 행(⑤ 도넛)에 minmax(min, 1fr) — 1fr 유지(핀치줌 원복 깜빡임 방지, 메모리 룰 feedback_dashboard_grid_1fr) + 결정론적 min(iOS 125 / Android 140)로 도넛 항상 렌더. **금지된 minmax(0,1fr) 아님** (min>0 이라 플리커 없음).
3) ⑤ 이번 달 점검 현황 도넛 카드(~:791-836)는 **내부 로직/구조 무변경** — Donut size44 / flex-nowrap 가로스크롤 / IS_ANDROID height 125·101 그대로 둔다. 마지막 행 min 확보만으로 iOS 도넛 렌더됨. ⑤ 카드의 animation delay(`.20s`)도 그대로 (재넘버링 X).
   폴백(우선 적용 X): 만약 iOS 실기기에서 도넛 여전히 눌리면, iOS 도넛 카드 inner strip(~:804-812)에 flex-1 부여 검토. 우선은 grid 변경만으로 충분.

지킬 룰: 월간 도넛 가로스크롤 유지(flex-nowrap, Donut size44) / grid 마지막 행 1fr 존재(minmax 형태) / minmax(0,1fr) 금지 / 겹침 0 / viewport 단위 무변경(iOS Safari 100dvh, iOS PWA 100vh, Android 100svh — 실사용자 iOS PWA). grid child 정확히 5개(①배너 ①-b라이브 ②현황 ③도구 ⑤도넛).
  </action>
  <verify>
    <automated>cd cha-bio-safety && grep -c "④ 오늘 일정" src/pages/DashboardPage.tsx | grep -qx 0 && grep -c "minmax(0" src/pages/DashboardPage.tsx | grep -qx 0 && grep -c "1fr minmax(140px, auto)" src/pages/DashboardPage.tsx | grep -qx 0 && grep -c "minmax(125px, 1fr)" src/pages/DashboardPage.tsx | grep -qx 1 && grep -c "minmax(140px, 1fr)" src/pages/DashboardPage.tsx | grep -qx 1 && grep -q "flex-nowrap" src/pages/DashboardPage.tsx && grep -q "size={44}" src/pages/DashboardPage.tsx && npx tsc --noEmit && echo FIX1_OK</automated>
    <human-check>cbc7119-preview iOS PWA(실기기 또는 100vh 에뮬)에서 대시보드 진입 → ⑤ 월간 도넛 카드가 항상 보이고(붕괴/미렌더 0), ①배너·①-b라이브·②현황 겹침 0, 월간 도넛 가로 스크롤 정상.</human-check>
  </verify>
  <done>④ 오늘 일정 카드 및 timed/untimed 렌더 완전 제거. gridTemplateRows 5행(iOS minmax(125px,1fr) / Android minmax(140px,1fr)). minmax(0,1fr) 부재. ⑤ 도넛 카드 내부 무변경. tsc PASS. grid child 5개.</done>
</task>

<task type="auto">
  <name>Task 2 (FIX-2): 데스크톱 화재수신반 pane 헤더 크롬을 형제 pane 표준에 정렬</name>
  <files>cha-bio-safety/src/pages/InspectionPage.tsx (화재수신반 id-head, ~:6356-6360)</files>
  <action>
현황: 화재수신반 3분할 상세 pane 헤더(id-head, ~:6356)가 형제 상세 pane 헤더('조치 상세', ~:6540)와 다른 크롬 → 화재수신반만 헤더 ≈56px, 형제 ≈72px. 형제 pane 헤더가 표준(실측 확인 완료).

편집 전: InspectionPage.tsx 는 6700+ 줄이라 라인 drift 가능. `isPanel ?` 분기 안의 **`화재수신반` id-head div**(BellRing + '화재수신반' + 점검모드 토글 포함된 헤더, `() => setCategoryIdx(null)` onClick 백버튼, ~:6357)로 특정할 것. 형제 '조치 상세' 헤더(`setRecordId(null)`, ~:6540)와 절대 헷갈리지 말 것.

형제 표준(~:6540, 참고용 — **변경 금지**):
  container: `shrink-0 px-5 py-3 border-b border-border-default bg-surface-raised flex items-center gap-2.5`
  back btn : `w-8 h-8 rounded-sm bg-surface-sunken border border-border-default cursor-pointer flex items-center justify-center hover:bg-surface-active transition-colors`
  chevron  : `<ChevronLeft size={14} className="text-text-secondary" />`

변경 (화재수신반 id-head ~:6356-6360 을 형제 크롬에 정렬):
1) container className:
   old: `shrink-0 flex items-center gap-[11px] px-4 py-[11px] border-b border-border-default bg-surface-raised`
   new: `shrink-0 flex items-center gap-2.5 px-5 py-3 border-b border-border-default bg-surface-raised`
   (gap-[11px]→gap-2.5, px-4→px-5, py-[11px]→py-3) — 이 container 문자열은 파일에 유일(1건) 확인됨.
2) back button className (~:6358):
   old: `w-7 h-7 flex items-center justify-center rounded-[7px] bg-surface-sunken text-text-secondary cursor-pointer shrink-0 hover:bg-surface-active transition-colors`
   new: `w-8 h-8 rounded-sm bg-surface-sunken border border-border-default cursor-pointer flex items-center justify-center shrink-0 hover:bg-surface-active transition-colors`
   (w-7 h-7→w-8 h-8 [=32px→48px, tailwind override], rounded-[7px]→rounded-sm, +border border-border-default, text-text-secondary 는 chevron 으로 이동, shrink-0 유지)
   ⚠ 주의: `w-7 h-7 ... rounded-[7px]` 백버튼은 파일에 **2곳** — 패널(:6358, 끝에 `hover:bg-surface-active transition-colors` 붙음) + **별개 :5482**(hover 없음, `... cursor-pointer shrink-0">` 로 끝). old_string 은 **위 hover 접미사까지 포함한 전체 문자열**로 주어야 패널만 유일 특정됨. :5482 는 절대 건드리지 말 것.
3) chevron (~:6359):
   old: `<ChevronLeft size={16} />`
   new: `<ChevronLeft size={14} className="text-text-secondary" />`
   (`ChevronLeft size={16}` 은 파일에 유일(1건) 확인됨.)

무변경: 점검모드 토글 버튼(~:6366-6378, switch h-[22px], py-1.5 → 48px 헤더에 수용). 타이틀 div(~:6361-6364, BellRing + '화재수신반'). 형제 '조치 상세' pane(~:6540) 및 표준 크롬을 쓰는 나머지 헤더들 절대 변경 금지.

지킬 룰: w-7=32px / w-8=48px override 혼동 금지. 헤더 통일 룰(헤더 내 버튼 동일 크기, 타이틀 백버튼 옆 좌측). 변경 후 화재수신반 헤더가 형제(:6540)와 px-5 py-3 / w-8 h-8 / ChevronLeft size 14 로 일치.
  </action>
  <verify>
    <automated>cd cha-bio-safety && grep -c "gap-\[11px\] px-4 py-\[11px\]" src/pages/InspectionPage.tsx | grep -qx 0 && grep -c "rounded-\[7px\] bg-surface-sunken text-text-secondary cursor-pointer shrink-0 hover:bg-surface-active" src/pages/InspectionPage.tsx | grep -qx 0 && grep -c "ChevronLeft size={16}" src/pages/InspectionPage.tsx | grep -qx 0 && grep -c "flex items-center gap-2.5 px-5 py-3 border-b" src/pages/InspectionPage.tsx | grep -qx 1 && grep -c "px-5 py-3 border-b border-border-default bg-surface-raised flex items-center gap-2.5" src/pages/InspectionPage.tsx | grep -qx 3 && npx tsc --noEmit && echo FIX2_OK</automated>
    <human-check>cbc7119-preview 데스크톱(1920×1080)에서 점검 → 화재수신반 3분할 pane 진입 → id-head 헤더가 형제 '조치 상세' pane 헤더와 같은 높이/백버튼 크기(48px)/시각으로 보임. 점검모드 토글 48px 헤더에 정상 수용.</human-check>
  </verify>
  <done>화재수신반 id-head container=gap-2.5 px-5 py-3, back btn=w-8 h-8 rounded-sm +border, ChevronLeft size 14 text-text-secondary. 옛 패널 gap-[11px]/px-4/py-[11px](0) 및 패널 w-7 h-7 rounded-[7px] hover 백버튼(0) 및 ChevronLeft size 16(0) 부재. 표준 크롬 형제 헤더 3건 유지(무변경). :5482 별개 백버튼 무변경. 점검모드 토글/타이틀 무변경. tsc PASS.</done>
</task>

<task type="auto">
  <name>Task 3 (FIX-3): 데스크톱 우측 캘린더/오늘일정 정렬 (approach a 구조 정렬)</name>
  <files>cha-bio-safety/src/pages/DashboardPage.tsx (데스크톱 Row 3 우측 컬럼, ~:459-595)</files>
  <action>
원인: 우측 컬럼(~:459 `w-[340px] shrink-0 flex flex-col gap-4`)의 캘린더가 shrink-0(내용 높이)라 좌측 점검현황(flex-1) 바닥을 안 따라감 → 오늘일정 top 이 빠른도구 top 보다 아래에서 시작(어긋남). 목표: "캘린더 바닥 == 점검현황 바닥" → "오늘일정 top == 빠른도구 top".

편집 전: `grep -n "화재수신반 라이브 위젯" src/pages/DashboardPage.tsx` / `grep -n "미니 캘린더" src/pages/DashboardPage.tsx` / `grep -n "오늘 일정" src/pages/DashboardPage.tsx` 로 데스크톱 우측 3개 카드 라인 재확인. (모바일 트리와 헷갈리지 말 것 — 이 카드들은 `w-[340px]` 우측 컬럼 자식.)

현 우측 컬럼(~:459) 자식 3개: 라이브 위젯(~:461-511, shrink-0) / 미니 캘린더(~:513-566, shrink-0) / 오늘 일정(~:568-594, `flex flex-col flex-1 min-h-0`).

변경 (approach a — 승인된 시안대로):
1) 라이브 위젯 + 미니 캘린더 두 카드를 **새 wrapper 로 감싼다**:
   `<div className="flex-1 min-h-0 flex flex-col gap-4">` ...라이브 위젯 div... ...미니 캘린더 div... `</div>`
   즉 라이브 위젯 여는 div 바로 앞에 wrapper 여는 태그, 미니 캘린더 닫는 div 바로 뒤(오늘 일정 주석 앞)에 wrapper 닫는 태그. 이 상단 그룹이 좌측 점검현황(flex-1)을 미러 → 우측 컬럼 높이 대부분 채움. (`flex-1 min-h-0 flex flex-col gap-4` 는 파일에 없는 유일 문자열 확인됨 — 좌측 컬럼은 `flex-1 flex flex-col gap-4`(min-h-0 없음), Row3 outer 는 `flex gap-4 flex-1 min-h-0`(flex-col 없음)로 다름.)
2) 미니 캘린더 카드(~:514) className 에 `mt-auto` 추가:
   old: `bg-surface-raised border border-border-default rounded-lg px-3.5 py-4 shrink-0`
   new: `bg-surface-raised border border-border-default rounded-lg px-3.5 py-4 shrink-0 mt-auto`
   → 상단 그룹 바닥에 캘린더 정렬 → 캘린더 바닥 == 상단그룹 바닥 == 점검현황 바닥. (캘린더 grid/cell 자체 무변경 — 자연 크기 유지.)
3) 오늘 일정 카드(~:569) className:
   old: `bg-surface-raised border border-border-default rounded-lg overflow-hidden flex flex-col flex-1 min-h-0`
   new: `bg-surface-raised border border-border-default rounded-lg overflow-hidden flex flex-col shrink-0 h-[125px]`
   (flex-1 min-h-0 → shrink-0 + 명시 높이) — 좌측 빠른도구(~:439 `grid grid-cols-4 gap-3 shrink-0`) row 높이와 일치. 시작값 h-[125px](콘솔1 실측 ≈125px, 시안 sketch.html 은 118px 근사). 내부 리스트 `overflow-y-auto`(~:574) 유지.
   **executor 필수**: 편집 후 1920×1080 에서 빠른도구 실제 렌더 높이에 오늘일정 높이를 맞춰 **오늘일정 top == 빠른도구 top** 이 되게 검증. 안 맞으면 **오늘일정 h-[Npx] 값만** 미세 조정(범위 110~129, 권장 118~125). 다른 요소 건드리지 말 것.

무변경: 좌측 점검현황(도넛 size76 6·7·6 balanced grid, ~:388-436) **절대 무변경**. 우측 컬럼 outer(~:459) gap-4 유지 — wrapper 내부 gap-4(라이브↔캘린더) + outer gap-4(wrapper↔오늘일정)로 기존 spacing 보존.

지킬 룰: 좌측 점검현황 도넛 size76 balanced 그리드(6·7·6) 유지 — 캘린더/우측만 손봄. 최종 목표: 오늘일정 top == 빠른도구 top(1920×1080). 승인 시안 sketch.html '제안(정렬)' 이 시각 계약.
  </action>
  <verify>
    <automated>cd cha-bio-safety && grep -c "flex-1 min-h-0 flex flex-col gap-4" src/pages/DashboardPage.tsx | grep -qx 1 && grep -c "px-3.5 py-4 shrink-0 mt-auto" src/pages/DashboardPage.tsx | grep -qx 1 && grep -Eq "overflow-hidden flex flex-col shrink-0 h-\[1(1[0-9]|2[0-9])px\]" src/pages/DashboardPage.tsx && grep -q "size={76}" src/pages/DashboardPage.tsx && npx tsc --noEmit && echo FIX3_OK</automated>
    <human-check>cbc7119-preview 데스크톱(1920×1080)에서 대시보드 진입 → 우측 오늘일정 카드 top 이 좌측 빠른도구 카드 top 과 정렬됨. 좌측 점검현황 도넛 size76 6·7·6 그리드 그대로. 캘린더 자연 크기 유지.</human-check>
  </verify>
  <done>라이브+캘린더가 `flex-1 min-h-0 flex flex-col gap-4` wrapper 로 묶임(1건). 캘린더 mt-auto 추가. 오늘일정 shrink-0 + h-[~125px](110~129 범위), 내부 overflow-y-auto 유지. 좌측 점검현황(size76) 무변경. 1920×1080 에서 오늘일정 top == 빠른도구 top. tsc PASS.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

순수 시각/레이아웃 CSS 클래스 변경. 새 데이터 흐름 · 입력 경계 · 네트워크 · 패키지 설치 없음. 새 trust boundary 도입 없음.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-mw4-01 | Tampering | 표시 분기/라벨 로직(없음/N개/완료) 우발 변경 | mitigate | 시각/레이아웃 클래스만 수정, 비즈니스 로직·조건부 렌더 anchor 무변경. tsc PASS + grep gate 로 확인 |
| T-mw4-SC | Tampering | npm/pip/cargo installs | accept | 패키지 설치 없음(순수 클래스 변경). 신규 의존성 0 |
</threat_model>

<verification>
- 세 fix 모두 `cd cha-bio-safety && npx tsc --noEmit` PASS (공통 게이트).
- FIX-1: grid child 5개, minmax(0,1fr) 부재, 월간 도넛 flex-nowrap/size44 유지.
- FIX-2: 화재수신반 id-head = 형제 :6540 크롬, 표준 크롬 형제 헤더 3건 유지(무변경), :5482 별개 백버튼 무변경.
- FIX-3: wrapper 1건, 캘린더 mt-auto, 오늘일정 shrink-0+명시높이, 점검현황 size76 무변경, 1920×1080 top 정렬.
- 배포/wrangler 없음 — commit 은 현재 브랜치에 fix 별 atomic, push 는 오케스트레이터.
</verification>

<success_criteria>
- iOS PWA 모바일 대시보드: ⑤ 도넛 항상 렌더, 겹침 0, 붕괴 0 (FIX-1).
- 데스크톱 화재수신반 pane 헤더 == 형제 pane 헤더 크롬 (FIX-2).
- 데스크톱 우측 오늘일정 top == 좌측 빠른도구 top @1920×1080 (FIX-3).
- 표시 분기/라벨 룰 무변경, tsc PASS, atomic commit 3개.
</success_criteria>

<output>
각 fix 완료 시 fix 별 atomic commit (현재 브랜치). 플랜 종료 후 오케스트레이터가 push/배포 1회 수행.
</output>
