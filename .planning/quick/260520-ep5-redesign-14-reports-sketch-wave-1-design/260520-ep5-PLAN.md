---
phase: quick-260520-ep5
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cha-bio-safety/docs/redesign-context/14-reports/wave-1-index.md
autonomous: true
requirements:
  - REDESIGN-14-WAVE1
must_haves:
  truths:
    - "wave-1-index.md 파일이 14-reports/ 디렉토리에 생성됨"
    - "7개 필수 섹션(§1~§7) 모두 채워짐"
    - "ReportsPage.tsx 인벤토리 표가 모바일 + 데스크톱 영역 모두 포함"
    - "6 sub-wave 분배 표가 W2~W7 행을 모두 포함"
    - "design-system.md §1.1/§1.2/§1.3/§6/§7/§10 인용이 fence 안 verbatim 으로 포함"
    - "메모리 룰 10개가 inline 인용 (각 1줄 요약 + Why + How)"
    - "negative rule 섹션이 sketch HTML 금지/코드 수정 금지/wrangler 금지 포함"
    - "OQ 5건이 §7 에 정리됨"
    - "ReportsPage.tsx / ExcelPreview.tsx 파일은 변경되지 않음"
  artifacts:
    - path: "cha-bio-safety/docs/redesign-context/14-reports/wave-1-index.md"
      provides: "W2~W7 진입을 위한 단일 진입점 인덱스 + 룰 verbatim 인용 + sub-wave 분배 매핑"
      contains: "§1. ReportsPage.tsx 인벤토리, §2. 6 sub-wave 분배, §3. design-system v0.1.1 verbatim, §4. chrome 통일 룰 적용 여부, §5. 메모리 룰 10개 inline, §6. negative rule, §7. open questions"
  key_links:
    - from: "wave-1-index.md"
      to: "cha-bio-safety/src/pages/ReportsPage.tsx"
      via: "§1 인벤토리에 line 범위 인용 + §2 sub-wave 분배 표 element/line 매핑"
      pattern: "line [0-9]+"
    - from: "wave-1-index.md"
      to: "cha-bio-safety/docs/redesign-context/14-reports/design-system.md"
      via: "§3 fence verbatim 인용"
      pattern: "design-system.md §"
    - from: "wave-1-index.md"
      to: "cha-bio-safety/docs/redesign-context/00-design-context/inspection-modal-chrome-rules.md"
      via: "§4 chrome 룰 적용 여부 명시"
      pattern: "inspection-modal-chrome-rules"
---

<objective>
redesign/14-reports sketch 작업의 wave 1 — 후속 wave(W2~W7) 의 단일 진입점이 되는 인덱스/룰 정리 문서 1개만 작성한다.

Purpose: ReportsPage.tsx (405 라인) 의 모든 element 를 6 sub-wave 로 분배하고, design-system.md v0.1.1 룰과 메모리 룰 10개를 verbatim 박제해서 후속 sketch wave 작업자(자기 자신이든 다른 세션이든)가 이 인덱스만 보면 일관되게 작업할 수 있도록 한다.

Output: `cha-bio-safety/docs/redesign-context/14-reports/wave-1-index.md` 단 1개 파일. 코드 변경 0건. sketch HTML 생성 0건 (그건 W2 부터).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@./CLAUDE.local.md
@.planning/STATE.md

# Source files (이 wave 에서 분석하는 대상, 수정하지 않음)
@cha-bio-safety/src/pages/ReportsPage.tsx
@cha-bio-safety/src/components/ExcelPreview.tsx

# Redesign context (이 wave 가 산출할 인덱스가 인용/참조하는 문서들)
@cha-bio-safety/docs/redesign-context/14-reports/14-reports.md
@cha-bio-safety/docs/redesign-context/14-reports/design-system.md
@cha-bio-safety/docs/redesign-context/00-design-context/inspection-modal-chrome-rules.md

# 13-schedule (직전 페이지) sketch 폴더 구조 참고용 — 산출물 형태 mirror
# 실제 디렉토리: cha-bio-safety/docs/redesign-context/13-schedule/
# 패턴 발견: 13-schedule 는 sketch/ 서브폴더 없이 평면 구조로 `sketch-wave-N.html` 사용.
# 단, 본 wave 산출물은 인덱스 markdown 이므로 파일명 `wave-1-index.md` (확장자 .md) 로 작성.
# 14-reports/ 디렉토리에 평면으로 sibling 으로 생성 (sketch/ 서브폴더 만들지 말 것).
</context>

<interfaces>
<!-- 후속 wave 가 산출할 sketch 파일 명명 규칙 (이 인덱스가 §2 표에서 인용) -->
<!-- 13-schedule 패턴 mirror — sketch/ 서브폴더 없이 14-reports/ 평면에 위치 -->

W2 → cha-bio-safety/docs/redesign-context/14-reports/sketch-wave-2-mobile-header-card.html
W3 → cha-bio-safety/docs/redesign-context/14-reports/sketch-wave-3-mobile-card-list.html
W4 → cha-bio-safety/docs/redesign-context/14-reports/sketch-wave-4-desktop-toolbar.html
W5 → cha-bio-safety/docs/redesign-context/14-reports/sketch-wave-5-desktop-sidelist.html
W6 → cha-bio-safety/docs/redesign-context/14-reports/sketch-wave-6-desktop-preview-wrapper.html
W7 → cha-bio-safety/docs/redesign-context/14-reports/wave-7-tsx-conversion-checklist.md (markdown, sketch 아님)

(주의: task_scope 의 원래 파일명에는 `wave-N-*` 접두가 있었으나, 13-schedule 실측 패턴 일관성을 위해 `sketch-wave-N-*` 로 통일한다. 인덱스 §2 표는 이 통일된 명명을 사용한다.)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: wave-1-index.md 작성</name>
  <files>cha-bio-safety/docs/redesign-context/14-reports/wave-1-index.md</files>
  <action>
ReportsPage.tsx (405 라인) 와 ExcelPreview.tsx (535 라인) 를 우선 끝까지 읽고, 14-reports.md / design-system.md / inspection-modal-chrome-rules.md 를 읽은 뒤 아래 7개 섹션을 가진 단일 markdown 파일을 작성한다. 파일은 Write 도구로 생성한다 (heredoc/cat 금지).

---

# 파일 헤더

상단에 다음 frontmatter/메타 1블록:
- 제목: `# redesign/14-reports — sketch wave 1 (index)`
- 1-2줄 설명: 본 문서는 W2~W7 진입의 단일 진입점이며, 이 인덱스만 봐도 후속 wave 가 디자인 룰 / 메모리 룰 / sub-wave 분배 / OQ 를 알 수 있도록 한다.
- 산출일자: 2026-05-20 / Quick ID 260520-ep5 / branch redesign/14-reports

---

# §1. ReportsPage.tsx 인벤토리

ReportsPage.tsx 의 element 를 모바일/데스크톱 영역으로 나눠 표로 정리. 각 행은 (영역 / element / source line 범위 / 역할 / 비즈 로직 연결 / 후속 wave 매핑) 6 컬럼.

**모바일 (MobileReportsPage):**
- header — 뒤로 버튼 + 타이틀 "점검 일지 출력" + 연도 셀렉터 ‹·›
  - source line 범위는 실제 ReportsPage.tsx 를 읽어 확정 (task_scope 가 line 331~353 추정치 제시 — 실측으로 보정)
  - 비즈: useNavigate, setYear, year 토글 ‹·› 로직
  - 후속 wave: W2
- 카드 리스트 — REPORT_CARDS 10종 (DIV early/late, 소화전, 청정소화약제, 비상콘센트, 피난방화, 방화셔터, 제연, 자탐, 소방펌프)
  - 각 카드 = 제목 + sub 라인 + "엑셀 다운로드" 그라데이션 버튼
  - source line 범위 실측
  - 비즈: handleDownload(card.type) 호출, ASSISTANTS 랜덤 채움 룰 (line 50~57 추정 — 실측 보정)
  - 후속 wave: W3 (전체 10종) / W2 (1종 샘플)
- footer — "다운로드 후 엑셀에서 인쇄 (A4 용지 자동 맞춤 설정됨)"
  - source line 실측
  - 비즈: 정적 문자열
  - 후속 wave: W3

**데스크톱 (DesktopReportsPage):**
- 상단 바 — 연도 셀렉트 + 일괄 다운로드 버튼 (그라데이션) + flex spacer + 선택된 보고서 타이틀 + 개별 엑셀 다운로드 버튼
  - source line 실측 (task_scope 추정 line 181~243)
  - 비즈: handleDownload, handleDownloadAll, setYear, setMonth, selectedType
  - 후속 wave: W4
- 좌측 패널 260px — DESKTOP_SECTIONS 4 섹션 (유수검지 장치 / 소화전·가스·비상콘센트 / 연간 점검일지 / 소방펌프)
  - 각 섹션 헤더 + 보고서 row (제목 + sub, isSelected 면 좌측 3px accent 색바)
  - source line 실측 (task_scope 추정 line 249~295)
  - 비즈: selectedType state, setSelectedType
  - 후속 wave: W5
- 우측 ExcelPreview 영역 — `<ExcelPreview reportType={selectedType} year={year} month={month} />`
  - source line 실측 (task_scope 추정 line 298~300)
  - 비즈: ExcelPreview 컴포넌트 자체는 손대지 않음 — wrapper layout 만
  - 후속 wave: W6

인벤토리 작성 시 task_scope 의 line 추정치는 참고용이며, 실측이 우선. 단, 만약 ReportsPage.tsx 가 task_scope 가 가정한 405 라인보다 짧거나/길면 그 사실도 1줄로 명시.

---

# §2. 6 sub-wave 분배 plan

다음 표 그대로 박제 (단, 파일명은 위 <interfaces> 의 통일된 `sketch-wave-N-*` 패턴 사용):

| Wave | scope | 대상 element | 산출 파일 |
|---|---|---|---|
| W2 | 모바일 헤더 + 카드 1종 (DIV early) | MobileReportsPage 헤더 + REPORT_CARDS[0] 1매 | sketch-wave-2-mobile-header-card.html |
| W3 | 모바일 카드 그리드 10종 + footer | MobileReportsPage 전체 카드 리스트 + footer | sketch-wave-3-mobile-card-list.html |
| W4 | 데스크톱 상단 바 (연도 + 일괄 + 선택 + 개별) | DesktopReportsPage 상단 바 | sketch-wave-4-desktop-toolbar.html |
| W5 | 데스크톱 좌측 4섹션 카드 목록 | DesktopReportsPage 좌측 패널 | sketch-wave-5-desktop-sidelist.html |
| W6 | 데스크톱 ExcelPreview 영역 wrapper (내부 ExcelPreview.tsx 손대지 않음) | DesktopReportsPage 우측 영역 wrapper layout | sketch-wave-6-desktop-preview-wrapper.html |
| W7 | TSX 변환 verify checklist (sketch 아님, markdown) | W2~W6 sketch + ReportsPage.tsx 비즈 로직 보존 룰 + Tailwind cheatsheet | wave-7-tsx-conversion-checklist.md |

각 wave 행 아래에 boldface "보존 / 토큰 / 폰트" 3 미니 섹션:
- **보존**: 변환 후 보존해야 할 비즈 로직 호출 — 예: handleDownload(type), handleDownloadAll, setSelectedType, setYear, setMonth, ASSISTANTS 랜덤 채움 룰
- **토큰**: 적용할 디자인 토큰 — 예: text-body / text-title / bg-surface-raised / bg-safe-bar (그라데이션 폐기) / text-text-secondary / border-border-default. status- prefix 없음 (memory feedback_tailwind_token_class_pattern 룰)
- **폰트**: 모바일/데스크톱 동일 폰트 룰 (design-system §1.3), 헤더 18px, 본문 16px, 9~11px 금지

---

# §3. design-system.md v0.1.1 인용 (verbatim 발췌, fence 안)

design-system.md 를 읽고 아래 항목을 각각 별도의 ```fence 블록``` 안에 **원문 그대로** 박제 (불가피한 줄바꿈 제외 정확히 일치):

- §1.1 노안 친화 (본문 16px, 9~11px 금지, 터치 44px)
- §1.2 정보 인지 > 미적 정제
- §1.3 모바일/데스크톱 동일 폰트
- §6 Progress Color Rule → fence 인용 후 1줄 메타: "§6 미적용 — 14-reports 페이지에는 진척률 도넛/카드 없음."
- §7 Stat Card → fence 인용 후 1줄 메타: "§7 미적용 — 14-reports 페이지에는 통계 숫자 카드 없음."
- §10 Iconography → fence 인용 후 1줄 메타: "§10 — Lucide Download 단일. 커스텀 SVG 6종 미사용."

만약 design-system.md 안 섹션 번호/제목이 task_scope 가정과 다르면 실제 파일 기준으로 §번호를 맞춰 인용하고, 1줄 메타에서 그 차이를 명시.

추가:
- 기존 디자인 = `linear-gradient(135deg,#1d4ed8,#2563eb)` 버튼 → `bg-safe-bar` solid 로 치환. 근거: **13-schedule W6 LOCKED b 일관** + memory `feedback_design_sketch_first` + `feedback_tailwind_token_class_pattern`. 이 결정은 §7 OQ #1 에서 사용자 컨펌 받음 (단, 13-schedule 와 일관 정책상 OQ default = solid).

---

# §4. 02+06 chrome 통일 룰 적용 여부

`inspection-modal-chrome-rules.md` 를 읽고 1~2 단락으로 작성:

- 14-reports 페이지는 점검 페이지 시리즈가 아닌 **출력 페이지** → chrome 룰 자체는 미적용
- 단, 다음 3가지 패턴은 mirror:
  1. 헤더 폰트 크기 — 타이틀 text-title 18px 기준
  2. back button 패턴 — lucide ChevronLeft + bg-surface-sunken 등 (13-schedule W7 와 동일)
  3. BottomNav 숨김 — MOBILE_NO_NAV_PATHS 에 14-reports 가 이미 등재되어 있는지 1줄로 확인 (코드 확인 결과를 명시. 등재 안 되어 있으면 "W7 TSX 변환 wave 진입 시 MOBILE_NO_NAV_PATHS 확인 필요" 액션 아이템으로 §7 OQ 에 추가)

---

# §5. 메모리 룰 inline 인용 (verbatim)

아래 10개 룰을 **각각 별도 미니 카드**로 박제. 각 카드 포맷 (boldface 라벨):

```
### 룰 N — {룰 슬러그}
- **요약**: 1줄
- **Why**: 1줄
- **How to apply (14-reports)**: 1줄
```

10개 룰:
1. `feedback_design_sketch_first` — spacing/sizing 도 sketch HTML 시안 먼저
2. `feedback_redesign_sketch_rule_enforcement` — §6.2 negative rule (위험 임계치 아닌 카드 status 색 금지) / §6.3 §7.1 일관성
3. `feedback_sketch_realistic_data` — 표시 분기/라벨 룰은 코드 그대로, 시각 디자인만 손봄
4. `feedback_planner_prompt_sketch_verbatim` — TSX 변환 wave 진입 시 sketch CSS 정의 grep 추출 → 그대로 인용
5. `feedback_tailwind_token_class_pattern` — `text-fire-bar` O / `text-status-fire-bar` X (status- prefix 없음)
6. `feedback_tailwind_w8_h8_is_48px` — tailwind config spacing override w-8=48 / w-7=32
7. `feedback_text_caption_leading_none` — 헤더 토글/배지/칩 작은 영역은 leading-none
8. `feedback_tsx_wave_emoji_dot_gap` — 이모지 제거 + dot span 추가 룰 강제
9. `feedback_tsx_wave_stat_card_drift` — Stat Card §6.3 룰 verbatim 인용 (14-reports 에는 Stat Card 없음 → "미적용. 단, sketch 새 패턴은 무엇이든 verbatim 인용" 으로 적용)
10. `feedback_avoid_premature_confirmation` — "거의 일치" 자신감 표현 금지. 결과 보여주고 사용자 판단

각 룰의 Why/How 는 실제로 14-reports 페이지에서 어떤 element/상황에 적용될지 구체적으로 (포괄적 추상적 X). 예: 룰 1 → "사용자가 W3 진입 직전에 카드 spacing 조정도 sketch 로 먼저 보고 컨펌"; 룰 5 → "W3 카드 배경 색은 bg-surface-raised / bg-safe-bar (status- prefix 사용 시 W7 verify gate FAIL)".

---

# §6. negative rule (이 wave 에서 금지된 것)

bullet list:
- sketch HTML 생성 금지 (wave 2 부터)
- ReportsPage.tsx / ExcelPreview.tsx 코드 수정 금지 (이 wave 의 산출물은 markdown 1개)
- 다른 페이지 (13-schedule 등) 영향 금지
- wrangler 명령 금지 (CLAUDE.local.md 룰 — `.claude/settings.local.json` deny 강제)
- `npm run deploy` 금지 (CLAUDE.local.md 룰 — 직원 도메인 경로)
- 13-schedule sketch 폴더 구조와 다른 패턴 (sketch/ 서브폴더 등) 도입 금지 — 평면 sibling 으로 통일

---

# §7. open questions (W2 진입 직전 사용자 컨펌)

bullet list 5건:
- OQ #1: 모바일 카드 그라데이션 버튼 (`linear-gradient(135deg,#1d4ed8,#2563eb)`) → `bg-safe-bar` solid 로 통일 OK? (default 답: OK — 13-schedule W6 LOCKED b 일관)
- OQ #2: 데스크톱 좌측 패널 너비 — 현재 260px 유지 / 디자인 룰 토큰 (--sidebar-width 있다면) 일관 / 너비 조정 중 어느 것?
- OQ #3: 데스크톱 상단 바 일괄 다운로드 버튼 그라데이션 → solid 통일 OK?
- OQ #4: 모바일 footer 안내 ("다운로드 후 엑셀에서 인쇄...") — 유지 / 제거 / 위치 변경 중 어느 것?
- OQ #5: 카드 sub 라인 (`DIV · 34개소`) 의 가운뎃점 처리 — dot span (memory feedback_tsx_wave_emoji_dot_gap) vs 텍스트 가운뎃점, 13-schedule sub-wave 일관 패턴 따를지?

각 OQ 아래에 "default 답" 1줄 (사용자가 별 의견 없으면 이 답으로 진행할 것이라는 reasonable call). 단, "approved" 받기 전까지 W2 진입 금지.

---

작성 완료 후 자체 verify:
1. 7개 섹션 모두 존재 (§1~§7) — grep `^# §[1-7]` 카운트 = 7
2. 메모리 룰 10개 인용 — 각 룰 슬러그 `feedback_*` 가 본문에 등장하는지 grep 카운트 ≥ 10
3. sub-wave 분배 표가 W2~W7 6행 — 표 안에 `| W[2-7] |` 카운트 ≥ 6
4. design-system 인용 fence 가 최소 3개 (§1.1 / §1.2 / §1.3 필수) — ```` ``` ```` fence 카운트 ≥ 6 (open+close)
5. 코드/sketch 파일 생성 0 — `git status` 에 wave-1-index.md 외 변경 0
6. ReportsPage.tsx / ExcelPreview.tsx 변경 0 — `git diff --name-only` 가 cha-bio-safety/src/* 포함 안 함
  </action>
  <verify>
    <automated>test -f cha-bio-safety/docs/redesign-context/14-reports/wave-1-index.md && \
echo "--- section count ---" && \
grep -c '^# §[1-7]' cha-bio-safety/docs/redesign-context/14-reports/wave-1-index.md && \
echo "--- subwave rows ---" && \
grep -E '^\| W[2-7] \|' cha-bio-safety/docs/redesign-context/14-reports/wave-1-index.md | wc -l && \
echo "--- memory rules ---" && \
grep -oE 'feedback_[a-z_]+' cha-bio-safety/docs/redesign-context/14-reports/wave-1-index.md | sort -u | wc -l && \
echo "--- code untouched ---" && \
git diff --name-only HEAD -- cha-bio-safety/src/pages/ReportsPage.tsx cha-bio-safety/src/components/ExcelPreview.tsx | wc -l</automated>
  </verify>
  <done>
- `cha-bio-safety/docs/redesign-context/14-reports/wave-1-index.md` 파일 존재
- 7개 섹션 (§1~§7) 모두 존재, grep 결과 = 7
- §2 sub-wave 표가 W2~W7 6행 모두 포함
- 메모리 룰 10개 (`feedback_*` 슬러그) 모두 인용, unique count ≥ 10
- design-system.md 인용 fence 최소 3개 이상 (§1.1, §1.2, §1.3 필수)
- ReportsPage.tsx / ExcelPreview.tsx 코드 변경 0 — `git diff --name-only` 가 src/ 포함 안 함
- 어떠한 sketch-wave-*.html 도 생성하지 않음
- §7 OQ 5건 모두 정리, 각각 default 답 1줄 포함
  </done>
</task>

</tasks>

<verification>
이 wave 의 verify 는 task 1 의 automated block 으로 충분. 추가 phase-level 검증 없음.

자체 검수 흐름:
1. `git status` — wave-1-index.md 1개 신규, 그 외 변경 0
2. `wc -l cha-bio-safety/docs/redesign-context/14-reports/wave-1-index.md` — 합리적 길이 (대략 200~400 줄 예상, 너무 짧으면 섹션 누락 가능성)
3. 사용자에게 "wave-1-index.md 작성 완료 / §7 OQ 5건 답변 필요" 보고 후 컨펌 대기 (W2 자동 진입 금지)
</verification>

<success_criteria>
- wave-1-index.md 7개 섹션 모두 채워짐 (§1 인벤토리 / §2 sub-wave 분배 / §3 design-system verbatim / §4 chrome 룰 / §5 메모리 룰 10개 / §6 negative / §7 OQ)
- 코드 변경 0건 (ReportsPage.tsx, ExcelPreview.tsx untouched)
- sketch HTML 0건 생성 (W2 부터)
- 13-schedule 의 평면 sketch-wave-*.html 패턴 mirror — sketch/ 서브폴더 안 만듦
- 사용자 컨펌 받을 OQ 5건 정리됨
- automated verify 명령이 PASS (section=7 / subwave≥6 / rules≥10 / src 변경=0)
</success_criteria>

<output>
After completion, return summary to user:
- 생성 파일: `cha-bio-safety/docs/redesign-context/14-reports/wave-1-index.md`
- §7 OQ 5건 사용자 컨펌 대기
- W2 진입 = OQ #1 답변 후
</output>
