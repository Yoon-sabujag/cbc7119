---
phase: 260521-u5n
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cha-bio-safety/src/pages/WorkShiftPage.tsx
autonomous: true
requirements:
  - W5-checklist-§1-scope
  - W5-checklist-§2-biz-preserve-18
  - W5-checklist-§3-region-mapping-4
  - W5-checklist-§4-OQ-LOCKED-5
  - W5-checklist-§5-negative-gate-7
  - W5-checklist-§6-positive-gate
  - W5-checklist-§7-build-gate
  - W5-checklist-§10-biz-checkbox-18
tags:
  - redesign
  - 16-workshift
  - tsx-conversion
  - v0.1.1
  - quick

must_haves:
  truths:
    - "WorkShiftPage.tsx 단일 파일 in-place 수정 1건. git diff --name-only HEAD~ HEAD 에 'cha-bio-safety/src/pages/WorkShiftPage.tsx' + 본 PLAN.md/SUMMARY.md 만 등장 (다른 파일 0)."
    - "shiftCalc.ts + generateExcel.ts + App.tsx + tailwind.config.js + functions/ + templates/ 모두 변경 0 byte."
    - "변환 후 WorkShiftPage.tsx 라인 수 226 ± 30 (원본 226, 변환 후 226~260 예상)."
    - "비즈 로직 0 diff: useState 3 (year/month/dlLoading) line 17~19 / useRef 2 (scrollRef/todayRef) line 20~21 그대로."
    - "비즈 로직 0 diff: useNavigate / useIsDesktop / today 변수 line 14~16 그대로. navigate(-1) onClick 그대로."
    - "비즈 로직 0 diff: useQuery(['holidays-dates']) + fetch hyunbin.page + try/catch fallback [] + staleTime 24h line 23~40 그대로."
    - "비즈 로직 0 diff: useStaffList + STAFF_ORDER = ['석현민','김병조','윤종엽','박보융'] + sort line 42~48 그대로."
    - "비즈 로직 0 diff: getMonthlySchedule(year, month, staffForCalc) line 49 그대로."
    - "비즈 로직 0 diff: isToday(d) line 51~52 / isRed(d) line 54~60 helpers 그대로."
    - "비즈 로직 0 diff: useEffect 자동 가운데 스크롤 + requestAnimationFrame + todayRef.scrollIntoView line 63~68 그대로."
    - "비즈 로직 0 diff: handleExcel — setDlLoading(true) + dynamic import '../utils/generateExcel' + generateShiftExcel + toast.error catch + finally setDlLoading(false) line 70~82 그대로."
    - "비즈 로직 0 diff: !isDesktop 모바일 only back button 분기 line 96 / disabled={dlLoading} line 106 / {dlLoading ? '생성중...' : '엑셀 저장'} line 109 그대로."
    - "비즈 로직 0 diff: select onChange setYear/setMonth + option map [2025,2026,2027] + Array.from({length:12}) line 115~120 그대로."
    - "비즈 로직 0 diff: staffRows.map 이름 열 / day cell map / shift cell map / ref={tdy ? todayRef : undefined} / 범례 4 shift map (['당','비','주','휴']) 모두 그대로."
    - "비즈 로직 0 diff: 상수 SHIFT_LABEL 4종 line 9 / HDR_H = 52 line 10 / ROW_H = 46 line 11 / 카피 verbatim ('월간 출근부' / '엑셀 저장' / '생성중...' / '이름' / '년'/'월') 그대로."
    - "OQ #1 LOCKED 적용: 엑셀 저장 버튼 default = `bg-safe-bar text-text-on-accent`, dlLoading 시 className 분기 `opacity-60 cursor-not-allowed`. `var(--acl)` 인라인 0건."
    - "OQ #2 LOCKED 적용: 헤더 = `bg-surface-raised border-b border-border-default`. 모바일 back button = `w-[34px] h-[34px] rounded-sm bg-surface-sunken border border-border-default` + Lucide `<ChevronLeft size={15} className=\"text-text-secondary\" />`. 인라인 SVG 제거. `import { ChevronLeft } from 'lucide-react'` 1줄 추가. 데스크톱 back button 추가 X (`!isDesktop &&` 분기 유지)."
    - "OQ #3 LOCKED 적용: 폰트 격상 — fontSize 10 (staff title / DOW_KO) → `text-caption leading-none`, fontSize 12 (이름 헤더 / 범례 라벨 / 엑셀 버튼) → `text-caption leading-none`, fontSize 13 (셀 day / select / 범례 박스 글자) → `text-label font-bold leading-none`, fontSize 14 (staff name) → `text-body-sm font-bold`, 모바일 헤더 타이틀 14→16 격상 → `text-body font-bold`, 셀 shift 15→16 격상 → `text-body font-bold`."
    - "OQ #4 LOCKED 적용: SHIFT_COLOR 인라인 style 유지 — `color: SHIFT_COLOR[sh], background: SHIFT_COLOR[sh]+'22'` (셀 + 범례 박스 + 범례 글자) 4건 이상. shiftCalc.ts SHIFT_COLOR 상수 1 byte 변경 X."
    - "OQ #5 LOCKED 적용: today border = `border-2 border-accent` (var(--acl) 폐기). today bg = 인라인 `rgba(59,130,246,0.15)` 유지. 공휴일·주말 글자 = `text-[#ef4444]` arbitrary 인라인 (tokens.css `--status-danger` 가 다크 #f87171 / 라이트 #991b1b 로 `#ef4444` 불일치 검증 완료 → text-danger 토큰 사용 불가, arbitrary fallback 채택)."
    - "Negative gate 7건 모두 PASS: 이모지 0 / linear-gradient 0 / fontSize 9·10·11 0 / status- class prefix 0 / w-8·h-8 0 / 옛 alias var(--bg|bg2|bg3|bd|bd2|t1|t2|t3|acl|accent) 0 / raw hex #ef4444 잔존 (style 인라인 또는 arbitrary 만 OK)."
    - "Positive gate 모두 PASS: v0.1.1 토큰 className (bg-surface-page|raised|sunken / border-border-default|strong / text-text-primary|secondary|tertiary|on-accent) ≥3 / 인라인 px (`w-[34px]` `h-[34px]`) ≥1 / `bg-safe-bar` ≥1 / `border-accent` ≥1 / `text-[#ef4444]` ≥1 / Lucide `ChevronLeft` ≥2 (import + 사용) / `SHIFT_COLOR[sh]` 인라인 호출 ≥4 / `text-caption` ≥5 / `text-label` ≥3 / `text-body-sm` ≥1 / `text-body` ≥2."
    - "Build gate: `cd cha-bio-safety && npm run build` exit 0 (tsc --noEmit 0 errors + Vite build PASS). WorkShiftPage chunk size 보고."
    - "Atomic 1-commit: `tsx(quick-260521-u5n): redesign/16-workshift WorkShiftPage.tsx v0.1.1 토큰 변환 (W2/W3/W4 sketch verbatim + OQ 5건 LOCKED)`."
  artifacts:
    - path: cha-bio-safety/src/pages/WorkShiftPage.tsx
      provides: v0.1.1 토큰 className 적용 + OQ 5건 LOCKED 반영 + 비즈 로직 0 diff + Lucide ChevronLeft 1줄 import 추가
      contains: "bg-surface-raised / bg-surface-sunken / bg-safe-bar / border-border-default / border-border-strong / border-accent / text-text-primary / text-text-secondary / text-text-tertiary / text-text-on-accent / text-caption / text-label / text-body-sm / text-body / text-[#ef4444] / w-[34px] / h-[34px] / rounded-sm / rounded-[9px] / rounded-[5px] / w-6 / h-6 / ChevronLeft / SHIFT_COLOR / SHIFT_LABEL / STAFF_ORDER / getMonthlySchedule"
      min_lines: 200
  key_links:
    - from: "W5 §3.1 (영역 2 — 헤더, line 88~111)"
      to: WorkShiftPage.tsx line 88~111
      via: "bg-surface-raised border-b border-border-default + w-[34px] h-[34px] rounded-sm bg-surface-sunken border border-border-default + <ChevronLeft size={15} className='text-text-secondary' /> + text-body font-bold text-text-primary + bg-safe-bar text-text-on-accent text-caption font-bold leading-none rounded-sm + opacity-60 cursor-not-allowed 분기"
      pattern: "bg-surface-raised border-b border-border-default"
    - from: "W5 §3.2 (영역 3 — 년·월 select, line 113~121)"
      to: WorkShiftPage.tsx line 113~121
      via: "bg-surface-raised border-b border-border-default + bg-surface-sunken border border-border-strong text-text-primary text-label rounded-[9px]"
      pattern: "rounded-\\[9px\\]"
    - from: "W5 §3.3 (영역 4 — 표 영역, line 123~210)"
      to: WorkShiftPage.tsx line 123~210
      via: "bg-surface-sunken border border-border-default text-text-secondary text-caption font-bold leading-none (이름 헤더) + bg-surface-raised border border-border-default (staff name td) + text-body-sm font-bold text-text-primary (staff name) + text-caption leading-none text-text-tertiary (staff title) + bg-surface-sunken border border-border-default (day cell normal) + border-2 border-accent (today, OQ #5) + text-[#ef4444] (red, OQ #5 fallback) + text-label font-bold leading-none (d) + text-caption leading-none (DOW_KO) + border border-border-default / border-2 border-accent (shift cell normal/today) + text-body font-bold (shift cell) + SHIFT_COLOR[sh] 인라인 (color + background hex+22, OQ #4)"
      pattern: "border-accent"
    - from: "W5 §3.4 (영역 5 — 범례, line 212~220)"
      to: WorkShiftPage.tsx line 212~220
      via: "flex flex-wrap justify-center + w-6 h-6 rounded-[5px] border-[1.5px] flex items-center justify-center text-label font-extrabold leading-none + SHIFT_COLOR[sh] 인라인 (border-color + background + color, OQ #4) + text-caption leading-none text-text-secondary (라벨)"
      pattern: "rounded-\\[5px\\]"
    - from: "OQ #5 LOCKED tokens.css 검증"
      to: WorkShiftPage.tsx line 174 (day cell red)
      via: "tokens.css --status-danger 다크 #f87171 / 라이트 #991b1b — 둘 다 #ef4444 와 불일치 → text-[#ef4444] arbitrary fallback 채택"
      pattern: "text-\\[#ef4444\\]"
---

<objective>
redesign/16-workshift TSX 변환 — WorkShiftPage.tsx (226 lines) 단일 atomic in-place 수정.

W2/W3/W4 sketch + W5 12 섹션 checklist + W1 OQ 5건 LOCKED 결정을 1-commit 으로 적용.

Purpose:
- v0.1.1 토큰 className 으로 옛 alias `var(--bg|bg2|bg3|bd|bd2|t1|t2|t3|acl|accent)` 0건 치환
- OQ #1 LOCKED: 엑셀 저장 버튼 `var(--acl)` → `bg-safe-bar text-text-on-accent` solid
- OQ #2 LOCKED: 헤더 raised + 모바일 only back button + 인라인 SVG → Lucide `ChevronLeft`
- OQ #3 LOCKED: 9·10·11px 폰트 격상 (text-caption 12 / text-label 13 / text-body 16) + 모바일 헤더 14→16 + 셀 shift 15→16
- OQ #4 LOCKED: SHIFT_COLOR hex+22 알파 인라인 유지 (셀 + 범례 박스 + 범례 글자)
- OQ #5 LOCKED: today `var(--acl)` → `border-accent` 토큰 / today bg `rgba(59,130,246,0.15)` 인라인 유지 / 공휴일·주말 `#ef4444` → `text-[#ef4444]` arbitrary fallback (tokens.css 불일치 검증 결과)
- 비즈 로직 0 diff (state/handler/effect/hook/SHIFT_LABEL/HDR_H/ROW_H/STAFF_ORDER/카피 모두 보존)

Output:
- cha-bio-safety/src/pages/WorkShiftPage.tsx 단일 파일 in-place 수정 (226 → 226~260 lines)
- shiftCalc.ts + generateExcel.ts 0 byte 변경
- 다른 파일 변경 0건 (App.tsx / tailwind.config.js / functions/ / templates/ 모두 무영향)
- `lucide-react` import 1줄 추가 (`ChevronLeft`)
- `npm run build` PASS
- atomic 1-commit
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jykevin/Documents/cbc7119-design/CLAUDE.md
@/Users/jykevin/Documents/cbc7119-design/CLAUDE.local.md
@/Users/jykevin/Documents/cbc7119-design/.planning/STATE.md

# W5 TSX 변환 checklist (383 lines, source of truth — §3 매핑 표 verbatim)
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/16-workshift/wave-5-tsx-conversion-checklist.md

# W1 OQ LOCKED 결정 + 메모리 룰
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/16-workshift/wave-1-index.md

# W2~W4 sketch (변환 매핑 시각 검증용)
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/16-workshift/sketch-wave-2-header-select.html
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/16-workshift/sketch-wave-3-shift-table.html
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/16-workshift/sketch-wave-4-legend.html

# 토큰 / 타이포 source
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/16-workshift/tokens.css
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/16-workshift/typography.css

# Precedent — 27-login gox PLAN.md (정확한 mirror, 단일 atomic 1-commit / W5 checklist verbatim 적용)
@/Users/jykevin/Documents/cbc7119-design/.planning/quick/260521-gox-redesign-27-login-tsx-loginpage-tsx-220-/260521-gox-PLAN.md
@/Users/jykevin/Documents/cbc7119-design/.planning/quick/260521-gox-redesign-27-login-tsx-loginpage-tsx-220-/260521-gox-SUMMARY.md

# 변환 대상 (in-place 수정)
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/src/pages/WorkShiftPage.tsx

# 변환 무영향 — 0 byte 변경
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/src/utils/shiftCalc.ts
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/src/utils/generateExcel.ts

<interfaces>
<!-- WorkShiftPage.tsx 안의 비즈 로직 시그니처 — 모두 보존 (변환 시 1 byte 도 손대지 않음) -->

```typescript
// imports (line 1~7) — 그대로 + line 1 또는 8 다음 lucide-react ChevronLeft 1줄 추가
import { useState, useRef, useEffect } from 'react'
import { useNavigate } from 'react-router-dom'
import { useQuery } from '@tanstack/react-query'
import { getMonthlySchedule, DOW_KO, SHIFT_COLOR } from '../utils/shiftCalc'
import type { RawShift } from '../utils/shiftCalc'
import { useStaffList } from '../hooks/useStaffList'
import { useIsDesktop } from '../hooks/useIsDesktop'
import { ChevronLeft } from 'lucide-react'   // ← OQ #2 LOCKED 신규 추가 1줄

// 상수 (line 9~11) — 그대로
const SHIFT_LABEL: Record<RawShift, string> = { '당':'당직','비':'비번','주':'주간','휴':'휴무' }
const HDR_H = 52
const ROW_H = 46

// WorkShiftPage 시그니처 (line 13) — 그대로
export default function WorkShiftPage()

// state / ref / hook (line 14~21) — 그대로
const navigate = useNavigate()
const isDesktop = useIsDesktop()
const today = new Date()
const [year,  setYear]  = useState(today.getFullYear())
const [month, setMonth] = useState(today.getMonth() + 1)
const [dlLoading, setDlLoading] = useState(false)
const scrollRef = useRef<HTMLDivElement>(null)
const todayRef  = useRef<HTMLTableCellElement>(null)

// useQuery holidays (line 23~40) — 그대로
// useStaffList + STAFF_ORDER (line 42~48) — 그대로
// getMonthlySchedule (line 49) — 그대로
// isToday / isRed (line 51~60) — 그대로
// useEffect 자동 스크롤 (line 63~68) — 그대로
// handleExcel (line 70~82) — 그대로
```
</interfaces>

<tokens_css_validation>
<!-- W5 §8 진입 시점 검증 결과 — `text-danger` 토큰 사용 가능 여부 -->

```
$ grep -nE '\-\-status-danger:|\-\-safe-bar:|\-\-accent:' cha-bio-safety/docs/redesign-context/16-workshift/tokens.css
39:  --accent:          #3b82f6;        ← OQ #5 today border 일치 (선명한 파랑)
46:  --status-danger:   #f87171;        ← 다크 모드, #ef4444 와 불일치
92:  --accent:          #1f6feb;        ← 라이트 모드
99:  --status-danger:   #991b1b;        ← 라이트 모드, #ef4444 와 불일치
```

**결론:** `--status-danger` 가 다크 `#f87171` / 라이트 `#991b1b` 로 `#ef4444` 와 둘 다 불일치.
→ **OQ #5 LOCKED fallback 적용:** 공휴일·주말 글자는 `text-[#ef4444]` **arbitrary 인라인** 으로 변환.
→ `text-danger` 토큰 사용 0건 (잘못된 토큰 매핑 방지).
</tokens_css_validation>

<tailwind_token_cheatsheet>
<!-- W5 §9 verbatim 인용 — 옛 alias → v0.1.1 className 1:1 매핑 (16-workshift 적용 위치) -->

| 옛 인라인 / alias                                       | v0.1.1 className              | 16-workshift 적용 위치                     |
|---------------------------------------------------------|-------------------------------|--------------------------------------------|
| `var(--bg)`                                             | `bg-surface-page`             | (사용 없음 — 페이지 외곽 height:100%만)    |
| `var(--bg2)`                                            | `bg-surface-raised`           | 헤더 + 년월 + staff name td                |
| `var(--bg3)`                                            | `bg-surface-sunken`           | back button + select + 이름 헤더 + day cell normal |
| `var(--bd)`                                             | `border-border-default`       | 헤더 border-b + 이름 표 + day cell normal  |
| `var(--bd2)`                                            | `border-border-strong`        | select border                              |
| `var(--t1)`                                             | `text-text-primary`           | 타이틀 + staff name + select value         |
| `var(--t2)`                                             | `text-text-secondary`         | 이름 헤더 + day cell normal + ChevronLeft + 범례 라벨 |
| `var(--t3)`                                             | `text-text-tertiary`          | staff title                                |
| `#ffffff` (텍스트, accent 위)                            | `text-text-on-accent`         | 엑셀 버튼 글자                             |
| `var(--acl)` (엑셀 버튼 bg)                             | `bg-safe-bar` (OQ #1 LOCKED)  | 엑셀 저장 버튼 default                     |
| `var(--acl)` (today border)                             | `border-accent` (OQ #5 LOCKED)| today 셀 (day th + shift td)               |
| `#ef4444` (공휴일·주말 글자)                            | `text-[#ef4444]` arbitrary fallback (OQ #5 LOCKED, tokens.css 불일치) | day cell red 글자 |
| `rgba(59,130,246,0.15)` (today bg)                     | 인라인 유지 (OQ #5 LOCKED)    | today 셀 background                        |
| `fontSize: 10` (staff title / DOW_KO)                  | `text-caption leading-none` (12, OQ #3 격상) | staff title + DOW_KO   |
| `fontSize: 12` (이름 헤더 / 엑셀 버튼 / 범례 라벨)     | `text-caption leading-none` (OQ #3)         | 3 곳                    |
| `fontSize: 13` (셀 day / select / 범례 박스 글자)      | `text-label font-bold leading-none` (OQ #3) | 3 곳                    |
| `fontSize: 14` (staff name)                            | `text-body-sm font-bold` (OQ #3)            | 1 곳                    |
| `fontSize: 14` (모바일 헤더 타이틀)                    | `text-body font-bold` (16, OQ #3 격상)      | 모바일 헤더 타이틀      |
| `fontSize: 15` (셀 shift)                              | `text-body font-bold` (16, OQ #3 격상)      | shift cell 가독성       |
| `fontSize: 16` (데스크톱 헤더 타이틀)                  | `text-body font-bold` (유지, OQ #3)         | 데스크톱 헤더 타이틀    |

**status- class prefix 0건** (memory `feedback_tailwind_token_class_pattern`):
- 정확 = `bg-safe-bar` / `text-safe-bar`
- 잘못 = `bg-status-safe-bar` / `text-status-safe-bar` ← 11-div TSX hotfix(4ce707e) 사고 회피

**w-8 / h-8 = 48px 함정** (memory `feedback_tailwind_w8_h8_is_48px`):
- tailwind.config spacing override: `w-8 = 48`, `w-7 = 32`, `w-6 = 24` (기본 다름)
- back button 34px → `w-[34px] h-[34px]` arbitrary 인라인 (w-8 사용 X)
- 범례 box 24px → `w-6 h-6` 안전 (w-6 = 24 정확 일치)

**작은 컨테이너 text-caption → leading-none** (memory `feedback_text_caption_leading_none`):
- 이름 헤더 (height:52) + day cell DOW_KO (padding:4px 2px) + 범례 box (24x24) + 라벨 모두 `leading-none` 명시
- staff title (margin-top:2) + 엑셀 버튼 (height:34) 도 `leading-none`
</tailwind_token_cheatsheet>
</context>

<tasks>

<task type="auto">
  <name>Task 1: WorkShiftPage.tsx v0.1.1 토큰 변환 (in-place, atomic 1-commit)</name>
  <files>cha-bio-safety/src/pages/WorkShiftPage.tsx</files>
  <action>

WorkShiftPage.tsx 226 lines 를 in-place 수정하여 v0.1.1 토큰 className 으로 치환한다. **단일 atomic commit**. 비즈 로직(useState/useRef/useEffect/useQuery/useStaffList/handler/STAFF_ORDER/SHIFT_LABEL/HDR_H/ROW_H) 1 byte 변경 0.

## 0. 사전 확인 (필수)

```bash
git branch --show-current   # 기대: redesign/16-workshift-tsx
git status --short          # 기대: 빈 출력 (clean)
wc -l cha-bio-safety/src/pages/WorkShiftPage.tsx   # 기대: 226

# tokens.css --status-danger 검증 (이미 PLAN 작성 단계에서 검증 완료, 재확인용)
grep -nE '\-\-status-danger:|\-\-accent:' cha-bio-safety/docs/redesign-context/16-workshift/tokens.css
# 기대: --status-danger 다크 #f87171 / 라이트 #991b1b — 둘 다 #ef4444 와 불일치
# → OQ #5 LOCKED fallback: text-[#ef4444] arbitrary 인라인 사용 (text-danger 토큰 X)
```

## 1. 변환 매핑 (W5 §3.1~§3.4 verbatim — 추측 0건)

### §3.1 영역 2 — 헤더 (WorkShiftPage.tsx line 88~111, W2 sketch 출처)

| 현재 (인라인 style) | 변환 후 (className + 인라인) | sketch |
|---|---|---|
| `<header style={{ flexShrink:0, ...(isDesktop ? { height:54, padding:'0 20px' } : { padding:'8px 12px 9px' }), background:'var(--bg2)', borderBottom:'1px solid var(--bd)', display:'flex', alignItems:'center', gap: isDesktop?10:8 }}>` | `<header className="bg-surface-raised border-b border-border-default flex items-center" style={{ flexShrink:0, ...(isDesktop ? { height:54, padding:'0 20px', gap:10 } : { padding:'8px 12px 9px', gap:8 }) }}>` | W2 frame 1, 3 |
| back button (모바일 only, line 96~102) `width:34, height:34, borderRadius:8, background:'var(--bg3)', border:'1px solid var(--bd)', cursor:'pointer', display:'flex', alignItems:'center', justifyContent:'center'` + 인라인 SVG `<svg width={15} height={15} fill="none" viewBox="0 0 24 24" stroke="var(--t2)" strokeWidth={2}><path strokeLinecap="round" strokeLinejoin="round" d="M15 19l-7-7 7-7"/></svg>` | `<button onClick={() => navigate(-1)} className="w-[34px] h-[34px] rounded-sm bg-surface-sunken border border-border-default flex items-center justify-center" style={{ cursor:'pointer' }}><ChevronLeft size={15} className="text-text-secondary" /></button>` (OQ #2 LOCKED, line 7 다음 `import { ChevronLeft } from 'lucide-react'` 1줄 추가) | W2 frame 1, 2 |
| 타이틀 `<span style={{ flex:1, fontSize: isDesktop?16:14, fontWeight:700, color:'var(--t1)' }}>월간 출근부</span>` | `<span className="text-body font-bold text-text-primary" style={{ flex:1 }}>월간 출근부</span>` (OQ #3 LOCKED 모바일 14→16 격상, 데스크톱 16 유지) | W2 모든 frame |
| 엑셀 버튼 `<button onClick={handleExcel} disabled={dlLoading} style={{ height:34, padding:'0 14px', borderRadius:8, background:'var(--acl)', border:'none', color:'#fff', fontSize:12, fontWeight:600, cursor:'pointer', opacity: dlLoading ? 0.6 : 1 }}>{dlLoading ? '생성중...' : '엑셀 저장'}</button>` | ``<button onClick={handleExcel} disabled={dlLoading} className={`bg-safe-bar text-text-on-accent text-caption font-bold leading-none rounded-sm border-0 ${dlLoading ? 'opacity-60 cursor-not-allowed' : 'cursor-pointer'}`} style={{ height:34, padding:'0 14px' }}>{dlLoading ? '생성중...' : '엑셀 저장'}</button>`` (OQ #1 LOCKED solid + OQ #3 LOCKED 12 → text-caption font-bold leading-none) | W2 모든 frame |

### §3.2 영역 3 — 년·월 select (WorkShiftPage.tsx line 113~121, W2 sketch 출처)

| 현재 | 변환 후 | sketch |
|---|---|---|
| 컨테이너 `<div style={{ flexShrink:0, display:'flex', gap:8, padding:'10px 12px', background:'var(--bg2)', borderBottom:'1px solid var(--bd)' }}>` | `<div className="bg-surface-raised border-b border-border-default flex" style={{ flexShrink:0, gap:8, padding:'10px 12px' }}>` | W2 모든 frame |
| year select `<select value={year} onChange={e => setYear(Number(e.target.value))} style={{ padding:'7px 10px', borderRadius:9, background:'var(--bg3)', border:'1px solid var(--bd2)', color:'var(--t1)', fontSize:13, outline:'none' }}>` | `<select value={year} onChange={e => setYear(Number(e.target.value))} className="bg-surface-sunken border border-border-strong text-text-primary text-label rounded-[9px]" style={{ padding:'7px 10px', outline:'none' }}>` (OQ #3 LOCKED 13 → text-label) | W2 |
| month select 동일 패턴 | year select 동일 className/style 적용 | W2 |
| option map `[2025,2026,2027]` / `Array.from({length:12},(_,i) => i+1)` | **변경 0** | W2 |

### §3.3 영역 4 — 표 영역 (WorkShiftPage.tsx line 123~210, W3 sketch 출처)

**메타 — tokens.css 검증 결과 (OQ #5 LOCKED fallback):**

> `--status-danger` 다크 `#f87171` / 라이트 `#991b1b` — **둘 다 `#ef4444` 와 불일치**. 공휴일·주말 글자는 **인라인 `text-[#ef4444]` arbitrary fallback** 채택. `text-danger` 토큰 사용 0건.

| 현재 | 변환 후 | sketch |
|---|---|---|
| 외곽 `<div style={{ flex:1, overflow:'auto', display:'flex', flexDirection:'column', alignItems:'center', justifyContent:'flex-start', paddingTop: isDesktop?'12vh':0 }}>` | **인라인 유지** (style 그대로 — Tailwind 변환 비용 vs 인라인 1줄, sketch 도 동일) | W3 frame 4 |
| 내곽 `<div style={{ display:'inline-flex', flexDirection:'column', padding: isDesktop?'0 32px':'16px 24px' }}>` | **인라인 유지** | W3 |
| 행 wrapper `<div style={{ display:'flex' }}>` | **인라인 유지** 또는 `className="flex"` (선택, 인라인 OK) | W3 |
| 이름 열 wrapper `<div style={{ flexShrink:0 }}>` | **인라인 유지** | W3 |
| 이름 헤더 th `<th style={{ height:HDR_H, width:82, padding:'0 10px', border:'1px solid var(--bd)', background:'var(--bg3)', color:'var(--t2)', fontSize:12, fontWeight:700, whiteSpace:'nowrap' }}>이름</th>` | `<th className="bg-surface-sunken border border-border-default text-text-secondary text-caption font-bold leading-none" style={{ height:HDR_H, width:82, padding:'0 10px', whiteSpace:'nowrap' }}>이름</th>` (OQ #3 LOCKED 12 → text-caption font-bold leading-none) | W3 |
| staff name td `<td style={{ height:ROW_H, padding:'0 10px', border:'1px solid var(--bd)', background:'var(--bg2)', whiteSpace:'nowrap' }}>` | `<td className="bg-surface-raised border border-border-default" style={{ height:ROW_H, padding:'0 10px', whiteSpace:'nowrap' }}>` | W3 |
| staff name div `<div style={{ fontSize:14, fontWeight:700, color:'var(--t1)' }}>{s.name}</div>` | `<div className="text-body-sm font-bold text-text-primary">{s.name}</div>` (OQ #3 LOCKED 14 → text-body-sm) | W3 |
| staff title div `<div style={{ fontSize:10, color:'var(--t3)', marginTop:2 }}>{s.title}</div>` | `<div className="text-caption leading-none text-text-tertiary" style={{ marginTop:2 }}>{s.title}</div>` (OQ #3 LOCKED 10 → text-caption leading-none) | W3 |
| 날짜 열 wrapper `<div ref={scrollRef} style={{ flex:1, overflowX:'auto' }}>` | **인라인 유지** (가로 스크롤 의도, memory `feedback_dashboard_horizontal_scroll`) | W3 |
| day th — 인라인 분기 `border: tdy ? '2px solid var(--acl)' : '1px solid var(--bd)', background: tdy ? 'rgba(59,130,246,0.15)' : 'var(--bg3)', color: red ? '#ef4444' : 'var(--t2)'` | ``className={`${tdy ? 'border-2 border-accent' : 'border border-border-default'} ${tdy ? '' : 'bg-surface-sunken'} ${red ? 'text-[#ef4444]' : 'text-text-secondary'}`}`` + `style={{ height:HDR_H, minWidth:40, padding:'4px 2px', textAlign:'center', ...(tdy ? { background:'rgba(59,130,246,0.15)' } : {}) }}` (OQ #5 LOCKED border-accent + 인라인 bg 유지 + text-[#ef4444] fallback) | W3 |
| day th 자식 d `<div style={{ fontWeight:700, fontSize:13 }}>{d}</div>` | `<div className="text-label font-bold leading-none">{d}</div>` (OQ #3 LOCKED 13 → text-label) | W3 |
| day th 자식 DOW `<div style={{ fontSize:10 }}>{DOW_KO[dow]}</div>` | `<div className="text-caption leading-none" style={{ marginTop:2 }}>{DOW_KO[dow]}</div>` (OQ #3 LOCKED 10 → text-caption leading-none, marginTop:2 인라인 추가 — sketch 정렬 매칭) | W3 |
| shift td — 인라인 `border: tdy ? '2px solid var(--acl)' : '1px solid var(--bd)', textAlign:'center', fontWeight:700, fontSize:15, color: SHIFT_COLOR[sh], background: SHIFT_COLOR[sh]+'22'` | ``className={`${tdy ? 'border-2 border-accent' : 'border border-border-default'} text-body font-bold`}`` + `style={{ height:ROW_H, minWidth:40, padding:'0 2px', textAlign:'center', color: SHIFT_COLOR[sh], background: SHIFT_COLOR[sh]+'22' }}` (OQ #3 LOCKED 15 → text-body 16 셀 가독성 우선 + OQ #4 LOCKED 인라인 hex 유지 + OQ #5 LOCKED border-accent) | W3 |

### §3.4 영역 5 — 범례 (WorkShiftPage.tsx line 212~220, W4 sketch 출처)

| 현재 | 변환 후 | sketch |
|---|---|---|
| 컨테이너 `<div style={{ display:'flex', gap:14, padding:'10px 0 28px', flexWrap:'wrap', justifyContent:'center' }}>` | `<div className="flex flex-wrap justify-center" style={{ gap:14, padding:'10px 0 28px' }}>` | W4 |
| shift row `<div style={{ display:'flex', alignItems:'center', gap:5, fontSize:12 }}>` | `<div className="flex items-center" style={{ gap:5 }}>` (fontSize 는 box/span 분리 적용) | W4 |
| shift box `<div style={{ width:24, height:24, borderRadius:5, background:SHIFT_COLOR[sh]+'22', border:`1.5px solid ${SHIFT_COLOR[sh]}`, display:'flex', alignItems:'center', justifyContent:'center', fontWeight:800, fontSize:13, color:SHIFT_COLOR[sh] }}>{sh}</div>` | `<div className="w-6 h-6 rounded-[5px] border-[1.5px] flex items-center justify-center text-label font-extrabold leading-none" style={{ background:SHIFT_COLOR[sh]+'22', borderColor:SHIFT_COLOR[sh], color:SHIFT_COLOR[sh] }}>{sh}</div>` (OQ #3 LOCKED 13 → text-label font-extrabold leading-none + OQ #4 LOCKED 인라인 hex 유지 + w-6/h-6 = 24 안전, memory `feedback_tailwind_w8_h8_is_48px`) | W4 |
| 라벨 `<span style={{ color:'var(--t2)' }}>{SHIFT_LABEL[sh]}</span>` | `<span className="text-caption leading-none text-text-secondary">{SHIFT_LABEL[sh]}</span>` (OQ #3 LOCKED 12 유지 + leading-none) | W4 |

### §3.5 외곽 wrapper (WorkShiftPage.tsx line 85)

| 현재 | 변환 후 |
|---|---|
| 최외곽 `<div style={{ height:'100%', display:'flex', flexDirection:'column', overflow:'hidden', background:'var(--bg)' }}>` | `<div className="bg-surface-page" style={{ height:'100%', display:'flex', flexDirection:'column', overflow:'hidden' }}>` |

## 2. OQ 5건 LOCKED 결정 (W1 §7 + W5 §4 verbatim — 위반 0건)

### OQ #1 LOCKED — 엑셀 저장 버튼 `bg-safe-bar` solid (line 104~110)
- `background:'var(--acl)'` 인라인 완전 제거 → className `'bg-safe-bar'`
- `color:'#fff'` 인라인 제거 → className `'text-text-on-accent'`
- `fontWeight:600` 인라인 제거 → className `'font-bold'`
- `fontSize:12` 인라인 제거 → className `'text-caption leading-none'`
- `borderRadius:8` 인라인 제거 → className `'rounded-sm'`
- `border:'none'` 인라인 제거 → className `'border-0'`
- `opacity: dlLoading ? 0.6 : 1` + `cursor:'pointer'` 인라인 제거 → className 분기 `${dlLoading ? 'opacity-60 cursor-not-allowed' : 'cursor-pointer'}`
- 인라인 잔존: `height:34, padding:'0 14px'`

### OQ #2 LOCKED — 헤더 raised + 모바일 only back button + Lucide ChevronLeft (line 88~102)
- `background:'var(--bg2)'` → className `'bg-surface-raised'`
- `borderBottom:'1px solid var(--bd)'` → className `'border-b border-border-default'`
- `!isDesktop && (back button)` 모바일 only 분기 그대로 유지
- back button `width:34, height:34, borderRadius:8, background:'var(--bg3)', border:'1px solid var(--bd)'` → className `'w-[34px] h-[34px] rounded-sm bg-surface-sunken border border-border-default flex items-center justify-center'`
- 인라인 SVG (line 98~100) 완전 제거 → `<ChevronLeft size={15} className="text-text-secondary" />` 1줄 교체
- **line 7 다음 신규 import 1줄 추가:** `import { ChevronLeft } from 'lucide-react'`
- 데스크톱 back button 추가 X (`!isDesktop &&` 분기 유지)

### OQ #3 LOCKED — 폰트 격상 매핑 (W5 §4 verbatim)
- line 103 모바일 헤더 타이틀 `fontSize: isDesktop?16:14` → className `'text-body font-bold'` (모바일 14→16 격상, 데스크톱 16 유지)
- line 107 엑셀 버튼 `fontSize:12, fontWeight:600` → className `'text-caption font-bold leading-none'`
- line 115, 118 select `fontSize:13` → className `'text-label'`
- line 138 이름 헤더 `fontSize:12, fontWeight:700` → className `'text-caption font-bold leading-none'`
- line 147 staff name `fontSize:14, fontWeight:700` → className `'text-body-sm font-bold'`
- line 148 staff title `fontSize:10` → className `'text-caption leading-none'`
- line 178 day d `fontSize:13, fontWeight:700` → className `'text-label font-bold leading-none'`
- line 179 DOW_KO `fontSize:10` → className `'text-caption leading-none'`
- line 197 shift cell `fontSize:15, fontWeight:700` → className `'text-body font-bold'` (15→16 격상)
- line 216 범례 box `fontSize:13, fontWeight:800` → className `'text-label font-extrabold leading-none'`
- line 215, 217 범례 row + 라벨 `fontSize:12` → className `'text-caption leading-none'` (라벨에 적용, row 는 box/span 으로 분리)

### OQ #4 LOCKED — SHIFT_COLOR hex+22 알파 인라인 유지 (line 197~199 + line 216)
- shift cell `color: SHIFT_COLOR[sh], background: SHIFT_COLOR[sh]+'22'` 인라인 그대로 유지
- 범례 box `background:SHIFT_COLOR[sh]+'22', borderColor:SHIFT_COLOR[sh], color:SHIFT_COLOR[sh]` 인라인 그대로 유지
- shiftCalc.ts SHIFT_COLOR 상수 1 byte 변경 X (별도 파일 git diff = 0)

### OQ #5 LOCKED — today border-accent + 공휴일·주말 text-[#ef4444] fallback (line 172, 174, 196)
- line 172, 196 today border `'2px solid var(--acl)'` → className `'border-2 border-accent'`
- line 172, 196 not-today border `'1px solid var(--bd)'` → className `'border border-border-default'`
- line 173 today bg `'rgba(59,130,246,0.15)'` **인라인 유지** (신규 토큰 정의 비용 회피, W5 §3.3 명시)
- line 173 not-today bg `'var(--bg3)'` → className `'bg-surface-sunken'`
- line 174 red (공휴일·주말) `color:'#ef4444'` → className `'text-[#ef4444]'` (arbitrary fallback, tokens.css 불일치 검증 결과 → `text-danger` 토큰 사용 불가)
- line 174 not-red `color:'var(--t2)'` → className `'text-text-secondary'`

## 3. 비즈 로직 0 diff 보존 (W5 §2 + §10 18 항목)

다음은 **변경 0** — 인라인 style 제거 외 logic 라인 손대지 않음:

- line 1~7 imports 7개 (lucide-react import 1줄만 추가 — 다른 import 변경 0)
- line 9 SHIFT_LABEL 4종
- line 10~11 HDR_H = 52 / ROW_H = 46
- line 13 export default function WorkShiftPage()
- line 14~16 useNavigate / useIsDesktop / today
- line 17~19 state 3개 (year/month/dlLoading)
- line 20~21 useRef 2개 (scrollRef/todayRef)
- line 23~40 useQuery holidays + fetch hyunbin.page + try/catch → []
- line 42~48 useStaffList + STAFF_ORDER 4명 정렬
- line 49 getMonthlySchedule
- line 51~60 isToday / isRed helpers
- line 63~68 useEffect 자동 스크롤
- line 70~82 handleExcel — setDlLoading + dynamic import + generateShiftExcel + toast.error + finally
- line 96 `!isDesktop && (back button)` 모바일 분기
- line 97 onClick `() => navigate(-1)`
- line 105 onClick handleExcel / line 106 disabled={dlLoading} / line 109 카피 분기 `{dlLoading ? '생성중...' : '엑셀 저장'}`
- line 115, 118 select onChange setYear/setMonth
- line 116, 119 select 옵션 verbatim ([2025,2026,2027] + Array.from({length:12}))
- line 144~152 staffRows.map 이름 열 row
- line 161~182 day th map + tdy todayRef 분기 + ref={tdy ? todayRef : undefined}
- line 188~205 shift td map + tdy 분기 + SHIFT_COLOR 인라인
- line 214 범례 4 shift map (['당','비','주','휴'])
- 카피 verbatim: '월간 출근부' / '엑셀 저장' / '생성중...' / '이름' / `${y}년` / `${m}월` / SHIFT_LABEL 4종 (당직/비번/주간/휴무) / STAFF_ORDER 4명 (석현민/김병조/윤종엽/박보융)

## 4. 작업 순서

1. `wc -l cha-bio-safety/src/pages/WorkShiftPage.tsx` 226 확인
2. WorkShiftPage.tsx 한 번 Read 로 전체 226 라인 읽기
3. Edit / Write 로 in-place 수정 — **§3.1 헤더** → **§3.2 select** → **§3.3 표** → **§3.4 범례** → **§3.5 외곽 wrapper** 순서 (line 순서 + import 1줄 추가)
4. import line 7 다음 `import { ChevronLeft } from 'lucide-react'` 1줄 추가
5. `cd cha-bio-safety && npm run build` 실행 → tsc 0 errors + Vite build PASS 확인
6. §5 negative gate 7건 + §6 positive gate + §7 비즈 보존 sample + §8 scope 모두 PASS 확인
7. atomic 1-commit:
   ```
   git add cha-bio-safety/src/pages/WorkShiftPage.tsx .planning/quick/260521-u5n-redesign-16-workshift-tsx-workshiftpage-/
   git commit -m "tsx(quick-260521-u5n): redesign/16-workshift WorkShiftPage.tsx v0.1.1 토큰 변환 (W2/W3/W4 sketch verbatim + OQ 5건 LOCKED)"
   ```

## 5. 금지 사항

- wrangler 명령 0건 (CLAUDE.local.md 강제)
- npm run deploy 0건
- WorkShiftPage.tsx 외 파일 수정 0건 (shiftCalc.ts / generateExcel.ts / App.tsx / tailwind.config.js / tokens.css / typography.css / functions/ / templates/ 모두 무영향)
- `bg-status-safe-bar` / `text-status-safe-bar` 등 status- prefix className 0건 (memory `feedback_tailwind_token_class_pattern`)
- `w-8` / `h-8` 토큰 사용 0건 (=48px 함정, memory `feedback_tailwind_w8_h8_is_48px`)
- `var(--bg|bg2|bg3|bd|bd2|t1|t2|t3|acl|accent)` 잔존 0건
- `linear-gradient` 0건 (16-workshift 본문 원래 없음, verify 로 박제)
- fontSize 9·10·11 인라인 0건 (OQ #3 LOCKED 격상)
- 이모지 0건 (sketch 본문 + WorkShiftPage 본문 모두 이모지 없음)
- 비즈 로직 (state/handler/effect/hook/SHIFT_LABEL/HDR_H/ROW_H/STAFF_ORDER/카피) 변경 0건
- `text-danger` 토큰 사용 0건 (tokens.css `--status-danger` 가 `#ef4444` 와 불일치 검증 완료, `text-[#ef4444]` arbitrary fallback 만 사용)

  </action>
  <verify>
    <automated>
# Path 변수 (실행 디렉토리 = worktree 루트)
F="cha-bio-safety/src/pages/WorkShiftPage.tsx"

# ─── Negative gate 7건 (W5 §5 verbatim) ───
echo "=== Negative gate ==="
test "$(grep -oP '[\x{1F300}-\x{1FAFF}\x{2600}-\x{26FF}]' "$F" 2>/dev/null | wc -l | tr -d ' ')" = "0" && echo "neg1 (emoji) PASS" || echo "neg1 FAIL"
test "$(grep -c 'linear-gradient' "$F")" = "0" && echo "neg2 (no linear-gradient) PASS" || echo "neg2 FAIL"
test "$(grep -v '^\s*//' "$F" | grep -cE 'fontSize:\s*(9|10|11)[^0-9]|font-size:\s*(9|10|11)[^0-9]')" = "0" && echo "neg3 (no 9·10·11 fontSize) PASS" || echo "neg3 FAIL"
test "$(grep -cE '\b(text|bg|border)-status-(safe|fire|warning|danger|caution)' "$F")" = "0" && echo "neg4 (no status- prefix) PASS" || echo "neg4 FAIL"
test "$(grep -cE '\bw-8\b|\bh-8\b' "$F")" = "0" && echo "neg5 (no w-8/h-8) PASS" || echo "neg5 FAIL"
test "$(grep -cE 'var\(--(bg|bg2|bg3|bd|bd2|t1|t2|t3|acl|accent)\)' "$F")" = "0" && echo "neg6 (no old alias) PASS" || echo "neg6 FAIL"
test "$(grep -c 'text-danger' "$F")" = "0" && echo "neg7 (no text-danger token, OQ #5 fallback) PASS" || echo "neg7 FAIL"

# ─── Positive gate (W5 §6 verbatim) ───
echo "=== Positive gate ==="
test "$(grep -cE 'bg-surface-(page|raised|sunken)|border-border-(default|strong)|text-text-(primary|secondary|tertiary|on-accent)' "$F")" -ge 3 && echo "pos1 (v0.1.1 tokens ≥3) PASS" || echo "pos1 FAIL"
test "$(grep -c 'w-\[34px\]' "$F")" -ge 1 && echo "pos2 (w-[34px] back button) PASS" || echo "pos2 FAIL"
test "$(grep -c 'h-\[34px\]' "$F")" -ge 1 && echo "pos3 (h-[34px] back button) PASS" || echo "pos3 FAIL"
test "$(grep -c 'bg-safe-bar' "$F")" -ge 1 && echo "pos4 (bg-safe-bar 엑셀) PASS" || echo "pos4 FAIL"
test "$(grep -c 'border-accent' "$F")" -ge 1 && echo "pos5 (border-accent today, OQ #5) PASS" || echo "pos5 FAIL"
test "$(grep -c 'text-\[#ef4444\]' "$F")" -ge 1 && echo "pos6 (text-[#ef4444] red, OQ #5 fallback) PASS" || echo "pos6 FAIL"
test "$(grep -c 'ChevronLeft' "$F")" -ge 2 && echo "pos7 (Lucide ChevronLeft import + 사용) PASS" || echo "pos7 FAIL"
test "$(grep -c \"from 'lucide-react'\" "$F")" -ge 1 && echo "pos8 (lucide-react import) PASS" || echo "pos8 FAIL"
test "$(grep -c 'SHIFT_COLOR\[sh\]' "$F")" -ge 4 && echo "pos9 (SHIFT_COLOR 인라인 ≥4, OQ #4) PASS" || echo "pos9 FAIL"
test "$(grep -c 'text-caption' "$F")" -ge 5 && echo "pos10 (text-caption ≥5) PASS" || echo "pos10 FAIL"
test "$(grep -c 'text-label' "$F")" -ge 3 && echo "pos11 (text-label ≥3) PASS" || echo "pos11 FAIL"
test "$(grep -c 'text-body-sm' "$F")" -ge 1 && echo "pos12 (text-body-sm ≥1) PASS" || echo "pos12 FAIL"
test "$(grep -cE '\btext-body\b' "$F")" -ge 2 && echo "pos13 (text-body ≥2) PASS" || echo "pos13 FAIL"
test "$(grep -c 'rounded-\[9px\]' "$F")" -ge 1 && echo "pos14 (rounded-[9px] select) PASS" || echo "pos14 FAIL"
test "$(grep -c 'rounded-\[5px\]' "$F")" -ge 1 && echo "pos15 (rounded-[5px] 범례) PASS" || echo "pos15 FAIL"

# ─── 비즈 로직 보존 (W5 §10 sampling) ───
echo "=== Biz preserve ==="
test "$(grep -c 'useIsDesktop' "$F")" -ge 1 && echo "biz1 (useIsDesktop) PASS" || echo "biz1 FAIL"
test "$(grep -c 'getMonthlySchedule' "$F")" -ge 1 && echo "biz2 (getMonthlySchedule) PASS" || echo "biz2 FAIL"
test "$(grep -c 'useStaffList' "$F")" -ge 1 && echo "biz3 (useStaffList) PASS" || echo "biz3 FAIL"
test "$(grep -c 'generateShiftExcel' "$F")" -ge 1 && echo "biz4 (generateShiftExcel) PASS" || echo "biz4 FAIL"
test "$(grep -c 'holidays' "$F")" -ge 3 && echo "biz5 (holidays ≥3) PASS" || echo "biz5 FAIL"
test "$(grep -c 'STAFF_ORDER' "$F")" -ge 1 && echo "biz6 (STAFF_ORDER) PASS" || echo "biz6 FAIL"
test "$(grep -c '석현민' "$F")" -ge 1 && echo "biz7 (석현민) PASS" || echo "biz7 FAIL"
test "$(grep -c "'당':'당직'" "$F")" -ge 1 && echo "biz8 (SHIFT_LABEL 당직) PASS" || echo "biz8 FAIL"
test "$(grep -c 'HDR_H = 52' "$F")" -ge 1 && echo "biz9 (HDR_H=52) PASS" || echo "biz9 FAIL"
test "$(grep -c 'ROW_H = 46' "$F")" -ge 1 && echo "biz10 (ROW_H=46) PASS" || echo "biz10 FAIL"
test "$(grep -c '월간 출근부' "$F")" -ge 1 && echo "biz11 (월간 출근부 카피) PASS" || echo "biz11 FAIL"
test "$(grep -c '엑셀 저장' "$F")" -ge 1 && echo "biz12 (엑셀 저장 카피) PASS" || echo "biz12 FAIL"
test "$(grep -c '생성중' "$F")" -ge 1 && echo "biz13 (생성중 카피) PASS" || echo "biz13 FAIL"
test "$(grep -c 'todayRef' "$F")" -ge 3 && echo "biz14 (todayRef ≥3) PASS" || echo "biz14 FAIL"
test "$(grep -c "'holidays-dates'" "$F")" -ge 1 && echo "biz15 (useQuery holidays-dates) PASS" || echo "biz15 FAIL"
test "$(grep -c 'navigate(-1)' "$F")" -ge 1 && echo "biz16 (navigate(-1)) PASS" || echo "biz16 FAIL"

# ─── shiftCalc.ts + generateExcel.ts 0 byte 변경 ───
echo "=== Utils preserve ==="
test "$(git diff HEAD~ HEAD -- cha-bio-safety/src/utils/shiftCalc.ts 2>/dev/null | wc -l | tr -d ' ')" = "0" && echo "utils1 (shiftCalc.ts 0 diff) PASS" || echo "utils1 FAIL"
test "$(git diff HEAD~ HEAD -- cha-bio-safety/src/utils/generateExcel.ts 2>/dev/null | wc -l | tr -d ' ')" = "0" && echo "utils2 (generateExcel.ts 0 diff) PASS" || echo "utils2 FAIL"

# ─── Scope (다른 파일 변경 0) ───
echo "=== Scope ==="
test "$(git diff --name-only HEAD~ HEAD 2>/dev/null | grep -vE 'cha-bio-safety/src/pages/WorkShiftPage\.tsx|^\.planning/quick/260521-u5n-' | wc -l | tr -d ' ')" = "0" && echo "scope (no extra files) PASS" || echo "scope FAIL — files: $(git diff --name-only HEAD~ HEAD 2>/dev/null | grep -vE 'cha-bio-safety/src/pages/WorkShiftPage\.tsx|^\.planning/quick/260521-u5n-')"

# ─── Line count sanity ───
echo "=== Line count ==="
LC=$(wc -l < "$F" | tr -d ' ')
test "$LC" -ge 196 -a "$LC" -le 260 && echo "lc ($LC lines, 226 ± 30) PASS" || echo "lc FAIL ($LC lines)"

# ─── Build gate ───
echo "=== Build ==="
(cd cha-bio-safety && npm run build 2>&1 | tail -5)
echo "=== WorkShiftPage chunk size ==="
ls -la cha-bio-safety/dist/assets/WorkShiftPage-*.js 2>/dev/null || echo "(chunk file 미존재 — Vite 빌드 결과 확인 필요)"
    </automated>
  </verify>
  <done>
- WorkShiftPage.tsx in-place 수정 완료, 라인 수 226 ± 30
- Negative gate 7건 모두 PASS (이모지·linear-gradient·9·10·11·status-·w-8/h-8·var(--alias)·text-danger 토큰 0건)
- Positive gate 15건 모두 PASS (v0.1.1 토큰 ≥3 / w-[34px] h-[34px] / bg-safe-bar ≥1 / border-accent ≥1 / text-[#ef4444] ≥1 / ChevronLeft ≥2 / lucide-react import ≥1 / SHIFT_COLOR 인라인 ≥4 / text-caption ≥5 / text-label ≥3 / text-body-sm ≥1 / text-body ≥2 / rounded-[9px] ≥1 / rounded-[5px] ≥1)
- 비즈 로직 보존 16건 (useIsDesktop·getMonthlySchedule·useStaffList·generateShiftExcel·holidays·STAFF_ORDER·석현민·SHIFT_LABEL·HDR_H·ROW_H·카피 3건·todayRef·useQuery·navigate(-1) 모두 PASS)
- shiftCalc.ts + generateExcel.ts 모두 0 byte 변경 (git diff 0 lines)
- Scope: cha-bio-safety/src/pages/WorkShiftPage.tsx + .planning/quick/260521-u5n-*/ 외 파일 변경 0건
- `npm run build` exit 0 (tsc 0 errors + Vite PASS), WorkShiftPage chunk size 보고
- Atomic 1-commit: `tsx(quick-260521-u5n): redesign/16-workshift WorkShiftPage.tsx v0.1.1 토큰 변환 (W2/W3/W4 sketch verbatim + OQ 5건 LOCKED)`
  </done>
</task>

</tasks>

<verification>
## 전체 PLAN 통과 조건

1. **Negative gate 7건** — W5 §5 verbatim (+ text-danger 토큰 0건 추가, OQ #5 fallback 강제)
2. **Positive gate 15건** — W5 §6 verbatim
3. **비즈 로직 0 diff** — W5 §10 18 항목 (verify 의 biz1~biz16 sampling)
4. **shiftCalc.ts + generateExcel.ts 0 byte 변경** — git diff 0 lines (utils1, utils2)
5. **Scope** — 단일 파일 변경 (cha-bio-safety/src/pages/WorkShiftPage.tsx) + PLAN.md/SUMMARY.md
6. **Build** — `npm run build` exit 0
7. **Atomic 1-commit** — 단일 commit 안에 모든 변경 포함

## 검수 시각 (cbc7119-preview 자동 배포 후)

- 모바일 viewport (375×812 등 iPhone 13 mini): 모바일 헤더 분기 (back button + 14→16 타이틀) + 모바일 외곽 분기 동작 확인 (line 96 `!isDesktop &&`)
- 데스크톱 viewport (1920×1080 또는 ≥768): 데스크톱 헤더 (height:54, padding '0 20px', 뒤로가기 X) 동작 확인 + 표 영역 12vh padding-top 확인
- 엑셀 저장 버튼 default 색상 = `bg-safe-bar` (solid 파랑) + 글자 `text-text-on-accent` (흰)
- 엑셀 저장 버튼 dlLoading 시 `opacity-60` 회색 표시
- today 셀 (day th + shift td) 두 종 border-accent (var(--accent) = #3b82f6 다크 / #1f6feb 라이트) + 인라인 bg rgba(59,130,246,0.15)
- 공휴일·주말 날짜 글자 `#ef4444` (red 톤) — tokens.css `--status-danger` 와 다르지만 의도된 #ef4444 톤
- shift 4 카테고리 (당/비/주/휴) SHIFT_COLOR 카테고리 색 + hex+22 알파 background 유지
- 범례 4 박스 24x24 + border 1.5px + rounded-[5px] + 라벨 한글 (당직/비번/주간/휴무)

## 사용자 컨펌 후 다음 단계

- `main` 머지 (사용자 명시 컨펌 후, memory `feedback_deploy_test`)
- GitHub Actions → cbc7119-preview 자동 배포
- 직원 도메인 (cbc7119) 배포는 별도 worktree (20260328) 담당 (이 워크트리는 절대 다루지 않음, CLAUDE.local.md)
- `project_redesign_16_workshift_status` 메모리 박제 (신규)
</verification>

<success_criteria>
- [ ] WorkShiftPage.tsx 226 ± 30 lines, in-place 수정
- [ ] Negative gate 7건 모두 0 (이모지/linear-gradient/9·10·11 fontSize/status- prefix/w-8·h-8/var(--alias)/text-danger 토큰)
- [ ] Positive gate 15건 모두 임계치 이상
- [ ] 비즈 로직 보존 16건 (sampling) 모두 PASS
- [ ] shiftCalc.ts + generateExcel.ts 0 byte 변경
- [ ] 다른 파일 변경 0건
- [ ] `npm run build` exit 0
- [ ] Atomic 1-commit
- [ ] OQ #1: bg-safe-bar ≥1 + var(--acl) 0 + linear-gradient 0
- [ ] OQ #2: w-[34px] h-[34px] ≥1 + ChevronLeft ≥2 + lucide-react import ≥1 + 인라인 SVG 0
- [ ] OQ #3: fontSize 9·10·11 인라인 0 + text-caption ≥5 + text-label ≥3 + text-body-sm ≥1 + text-body ≥2
- [ ] OQ #4: SHIFT_COLOR 인라인 ≥4 + shiftCalc.ts 0 byte
- [ ] OQ #5: border-accent ≥1 + text-[#ef4444] ≥1 + text-danger 토큰 0
</success_criteria>

<output>
After completion, create `.planning/quick/260521-u5n-redesign-16-workshift-tsx-workshiftpage-/260521-u5n-SUMMARY.md` documenting:

- 변환 전후 WorkShiftPage.tsx wc -l
- 적용된 v0.1.1 토큰 className 목록 (grep 카운트)
- OQ #1~#5 LOCKED 각 적용 결과 확인
- tokens.css `--status-danger` 검증 결과 + `text-[#ef4444]` arbitrary fallback 채택 명시
- Negative gate 7건 + Positive gate 15건 + biz16건 + utils2건 PASS 결과 (verify automated 출력 첨부)
- `npm run build` 결과 (chunk size 포함)
- atomic commit hash + main 머지 대기 상태 명시
- 다음 단계 (사용자 컨펌 → main 머지 → cbc7119-preview 자동 배포 → `project_redesign_16_workshift_status` 메모리 박제)
</output>
</content>
</invoke>