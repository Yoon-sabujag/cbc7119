---
phase: 260517-00y
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cha-bio-safety/src/pages/FloorPlanPage.tsx
  - cha-bio-safety/docs/redesign-context/06-floorplan/sketch/floorplan-modals-sketch.html
autonomous: true
requirements:
  - FP-FIX-01  # 점검 모달 결과 3택 02 mirror (icon + rounded-pill + 1.5px border)
  - FP-FIX-02  # rounded-lg 18곳 sketch verbatim 정정 (전부 rounded-sm = 8px)
tags:
  - redesign
  - 06-floorplan
  - radius
  - inspection-modal

must_haves:
  truths:
    - "FloorPlanPage 점검 모달의 결과 3택 (정상/주의/불량) 이 02 InspectionPage 와 동일한 visual 을 가진다 (rounded-pill + 1.5px border + icon)."
    - "결과 3택 active ↔ inactive 토글 시 border 두께가 변하지 않아 버튼 폭이 jitter 없이 일정하다."
    - "FloorPlanPage TSX 의 rounded-lg 18곳이 sketch CSS verbatim 매핑에 따라 정정되어, sketch deviation = 0 이 된다."
    - "점검·마커·자재 비즈니스 로직 (state/API/handler/result 분기) 이 변환 전과 100% 동일하다."
    - "npm run build 통과 (TypeScript 0 error)."
  artifacts:
    - path: cha-bio-safety/src/pages/FloorPlanPage.tsx
      provides: "FloorPlanPage 컴포넌트 (점검 모달 결과 3택 02 mirror + 18 radius 정정)"
      contains: "from 'lucide-react'"
    - path: cha-bio-safety/docs/redesign-context/06-floorplan/sketch/floorplan-modals-sketch.html
      provides: ".fp-result-btn 시안 정의 (rounded-pill + 1.5px border, 02 mirror)"
      contains: ".fp-result-btn"
  key_links:
    - from: "FloorPlanPage.tsx 점검 모달 결과 3택 (line 1739)"
      to: "InspectionPage.tsx 점검 결과 3택 (line 928-932)"
      via: "verbatim className 1:1 mirror"
      pattern: "rounded-pill border-\\[1\\.5px\\] .* whitespace-nowrap"
    - from: "FloorPlanPage.tsx paired BC 결과 3택 (line 1797)"
      to: "InspectionPage.tsx 점검 결과 3택 (line 928-932)"
      via: "verbatim className 1:1 mirror"
      pattern: "rounded-pill border-\\[1\\.5px\\] .* whitespace-nowrap"
    - from: "FloorPlanPage.tsx 18 rounded-* 변경 위치"
      to: "sketch CSS verbatim"
      via: "radius 매핑 표 (PLAN 본문 §radius-mapping 참고)"
      pattern: "rounded-sm"
---

<objective>
redesign/06-floorplan TSX 변환 (6 wave, 머지+배포 완결) 이후 자체 검수 중 발견된 2 건 정합 fix.

**Issue 1 — 점검 모달 결과 3택 02 mirror 미적용**:
- FloorPlanPage 점검 모달의 결과 3택 (정상/주의/불량) 이 02 InspectionPage 의 통일된 패턴과 다르다.
- 현재: `flex-1 h-11 rounded-[10px]` + active 시 `border-2`, inactive 시 `border` (단순 border) → active 토글 시 폭이 1px jitter
- 02 정답 (line 928-932): `flex-1 px-2 py-[9px] rounded-pill border-[1.5px] gap-1.5` + Icon (CheckCircle2/AlertTriangle/XCircle) — 항상 1.5px 두께 유지 → jitter 0

**Issue 2 — rounded-lg 18곳 sketch verbatim 정정 누락**:
- 6 wave TSX 변환 시 `border-radius: 8px` 영역에 Tailwind `rounded-lg` (= 16px) 매핑이 잘못 들어감 (sketch 8px ≠ tailwind lg=16px).
- 18곳 모두 sketch verbatim 으로는 `rounded-sm` (8px) 이 정답.

Purpose:
- 02 패턴 통일로 결과 3택 jitter 제거 + 시각·접근성 정합 (icon 추가는 색맹 사용자 보조)
- sketch verbatim 정합 회복 (시안 = 실제 일치) — feedback_planner_prompt_sketch_verbatim 룰 강제

Output:
- FloorPlanPage.tsx 변경 (단일 파일)
- floorplan-modals-sketch.html `.fp-result-btn` 정의 재작성
- 변경 영역 외 비즈니스 로직 / 다른 페이지 0건 수정
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md

# 02 paired-precedent SOURCE (verbatim 1:1 mirror 대상)
@cha-bio-safety/src/pages/InspectionPage.tsx

# 변환 대상 — 단일 파일
@cha-bio-safety/src/pages/FloorPlanPage.tsx

# Sketch SOURCE — verbatim CSS 정답
@cha-bio-safety/docs/redesign-context/06-floorplan/sketch/floorplan-modals-sketch.html
@cha-bio-safety/docs/redesign-context/06-floorplan/sketch/floorplan-sketch.html

# Radius 매핑 reference
@cha-bio-safety/tailwind.config.js

<interfaces>
<!-- 02 paired-precedent (verbatim — 그대로 mirror) -->
<!-- InspectionPage.tsx line 919-934 -->

```tsx
<div className="flex gap-2">
  {INSPECT_RESULT_OPTIONS.map(opt => {
    const Icon = opt.value === 'normal' ? CheckCircle2 : opt.value === 'caution' ? AlertTriangle : XCircle
    const active = result === opt.value
    const stateCls = active
      ? opt.value === 'normal' ? 'border-safe bg-safe-bg text-safe'
        : opt.value === 'caution' ? 'border-warning bg-warning-bg text-warning'
        : 'border-danger bg-danger-bg text-danger'
      : 'border-border-default bg-surface-raised text-text-tertiary'
    return (
      <button key={opt.value} onClick={() => setResult(opt.value)}
        className={`flex-1 px-2 py-[9px] rounded-pill border-[1.5px] text-body-sm font-bold whitespace-nowrap inline-flex items-center justify-center gap-1.5 cursor-pointer transition-colors ${stateCls}`}>
        <Icon className="w-4 h-4 flex-shrink-0" />
        {opt.label}
      </button>
    )
  })}
</div>
```

<!-- FloorPlanPage.tsx 변환 대상 — 결과 3택 2개 영역 -->

```tsx
// (a) 메인 점검 결과 3택 — line 1732-1747 (현재)
<div className="text-caption font-semibold text-text-tertiary mb-1.5">점검 결과</div>
<div className="flex gap-1.5 mb-3.5">
  {([
    ['normal', '정상', 'safe'],
    ['caution', '주의', 'warning'],
    ['bad',    '불량', 'danger'],
  ] as const).map(([val, label, tok]) => (
    <button key={val} onClick={() => setInspectResult(val as 'normal' | 'caution' | 'bad')} className={`flex-1 h-11 rounded-[10px] text-body-sm font-bold cursor-pointer inline-flex items-center justify-center ${
      inspectResult === val
        ? tok === 'safe'    ? 'bg-safe-bg border-2 border-safe-bar text-safe'
        : tok === 'warning' ? 'bg-warning-bg border-2 border-warning-bar text-warning'
        :                    'bg-danger-bg border-2 border-danger-bar text-danger'
        : 'bg-surface-sunken border border-border-default text-text-tertiary'
    }`}>{label}</button>
  ))}
</div>

// (b) paired BC 결과 3택 — line 1790-1805 (현재)
<div className="text-caption font-semibold text-text-tertiary mb-1.5 tracking-[0.05em]">비상콘센트 점검 결과</div>
<div className="flex gap-1.5">
  {([
    ['normal', '정상', 'safe'],
    ['caution', '주의', 'warning'],
    ['bad',    '불량', 'danger'],
  ] as const).map(([val, label, tok]) => (
    <button key={val} onClick={() => setInspectBcResult(val as 'normal' | 'caution' | 'bad')} className={`flex-1 h-11 rounded-[10px] text-body-sm font-bold cursor-pointer inline-flex items-center justify-center ${
      inspectBcResult === val
        ? tok === 'safe'    ? 'bg-safe-bg border-2 border-safe-bar text-safe'
        : tok === 'warning' ? 'bg-warning-bg border-2 border-warning-bar text-warning'
        :                    'bg-danger-bg border-2 border-danger-bar text-danger'
        : 'bg-surface-sunken border border-border-default text-text-tertiary'
    }`}>{label}</button>
  ))}
</div>
```

<!-- 기존 lucide import (line 5) -->
```tsx
import { ChevronLeft, Trash2, X } from 'lucide-react'
```

<!-- Tailwind config radius 매핑 -->
```js
borderRadius: {
  sm:   '8px',   // ← 18곳 정답
  md:   '12px',
  lg:   '16px',  // ← 현재 잘못 들어간 매핑
  pill: '99px',
}
```
</interfaces>

<radius-mapping>
<!-- 18 rounded-lg 위치 verbatim 매핑 표 — sketch CSS 직접 확인 후 작성 -->
<!-- 결론: 18곳 전부 sketch `border-radius: 8px` → Tailwind `rounded-sm` -->

| # | TSX line | TSX 컨텍스트 (현재) | Sketch 대응 class | Sketch line | Sketch CSS | 정정 |
|---|----------|--------------------|-------------------|-------------|-----------|------|
| 1 | 951 | header back btn (w-8 h-8) | `.fp-back-btn` | sketch:300 | `border-radius: 8px` | `rounded-sm` |
| 2 | 960 | header 편집 toggle btn (h-8 px-3) | `.fp-hd-btn` | sketch:315 | `border-radius: 8px` | `rounded-sm` |
| 3 | 967 | header 설정 btn (h-8 px-3) | `.fp-hd-btn` | sketch:315 | `border-radius: 8px` | `rounded-sm` |
| 4 | 979 | plan-type tab (flex-1 h-9) | `.fp-plan-tab` | sketch:341 | `border-radius: 8px` | `rounded-sm` |
| 5 | 993 | floor chip (h-8 px-3) | `.fp-floor-chip` | sketch:368 | `border-radius: 8px` | `rounded-sm` |
| 6 | 1441 | editMarker 구역 3택 (flex-1 py-2) | `.fp-zone-btn` | modals:508 | `border-radius: 8px` | `rounded-sm` |
| 7 | 1458 | editMarker 마커 종류 3-col grid (px-1 py-2) | `.fp-marker-option-btn` | modals:483 | `border-radius: 8px` | `rounded-sm` |
| 8 | 1475 | editMarker 라벨 input (w-full px-3 py-2.5) | `.fp-input` | modals:525 | `border-radius: 8px` | `rounded-sm` |
| 9 | 1490 | editMarker 소화기 분리 (flex-1 py-2 danger) | `.fp-zone-btn`-shape | modals:508 | `border-radius: 8px` | `rounded-sm` |
| 10 | 1553 | addMarker 구역 3택 (flex-1 py-2) | `.fp-zone-btn` | modals:508 | `border-radius: 8px` | `rounded-sm` |
| 11 | 1570 | addMarker 마커 종류 3-col grid (px-1 py-2) | `.fp-marker-option-btn` | modals:483 | `border-radius: 8px` | `rounded-sm` |
| 12 | 1590 | addMarker 개소명 input (extinguisher) | `.fp-input` | modals:525 | `border-radius: 8px` | `rounded-sm` |
| 13 | 1602 | addMarker 구역 3택 (extinguisher) | `.fp-zone-btn` | modals:508 | `border-radius: 8px` | `rounded-sm` |
| 14 | 1618 | addMarker 라벨 input (non-extinguisher) | `.fp-input` | modals:525 | `border-radius: 8px` | `rounded-sm` |
| 15 | 1720 | inspect 모달 "정보 수정" btn (h-9 surface) | `.fp-hd-btn`-shape | sketch:315 | `border-radius: 8px` | `rounded-sm` |
| 16 | 1727 | inspect 모달 "소화기 분리" btn (h-9 danger) | `.fp-hd-btn`-shape | sketch:315 | `border-radius: 8px` | `rounded-sm` |
| 17 | 2038 | resolve 자재명 input (px-2.5) | `.fp-input` | modals:525 | `border-radius: 8px` | `rounded-sm` |
| 18 | 2047 | resolve 자재 수량 input (pl-2.5 pr-7) | `.fp-input` | modals:525 | `border-radius: 8px` | `rounded-sm` |

**검증**: 18곳 모두 sketch 의 8px 영역 (`.fp-back-btn` / `.fp-hd-btn` / `.fp-plan-tab` / `.fp-floor-chip` / `.fp-zone-btn` / `.fp-marker-option-btn` / `.fp-input`) 와 1:1 일치. 따라서 결론: 18 곳 전부 `rounded-lg` → `rounded-sm`.

**제외 확인**: 
- 결과 3택 (line 1739 메인, 1797 paired BC) 은 작업 1 에서 `rounded-pill` 로 변경 — 이 표에 포함 ✗
- 사이즈 위반 없음 (sketch 10px / 14px 영역은 이미 `rounded-[10px]` / `rounded-[14px]` 임의값으로 들어가 있어 rounded-lg 18 목록에 없음)
</radius-mapping>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 결과 3택 02 mirror — sketch 재작성 + TSX 2영역 + lucide import</name>
  <files>
    cha-bio-safety/docs/redesign-context/06-floorplan/sketch/floorplan-modals-sketch.html,
    cha-bio-safety/src/pages/FloorPlanPage.tsx
  </files>
  <action>
**(1) Sketch `.fp-result-btn` 재작성** — modals-sketch.html line 379-403 (`.fp-result-btn` ~ `.fp-result-btn.is-active-danger` 전체 블록) 을 다음으로 **완전 교체**:

```css
/* ── 결과 토글 3택 (정상/주의/불량) — 02 mirror (height auto + rounded-pill + 1.5px border + icon) ── */
.fp-result-row { display: flex; gap: 6px; margin-bottom: 14px; }
.fp-result-btn {
  flex: 1;
  padding: 9px 8px;
  border-radius: 99px;
  background: var(--surface-raised);
  border: 1.5px solid var(--border-default);
  color: var(--text-tertiary);
  font-size: 14px; font-weight: 700; line-height: 1;
  display: inline-flex; align-items: center; justify-content: center; gap: 6px;
  cursor: pointer; font-family: inherit;
  white-space: nowrap;
  transition: background-color .13s, border-color .13s, color .13s;
}
.fp-result-btn.is-active-safe    { background: var(--status-safe-bg);    border-color: var(--status-safe);    color: var(--status-safe); }
.fp-result-btn.is-active-warning { background: var(--status-warning-bg); border-color: var(--status-warning); color: var(--status-warning); }
.fp-result-btn.is-active-danger  { background: var(--status-danger-bg);  border-color: var(--status-danger);  color: var(--status-danger); }
```

**핵심 변경**:
- `height: 44px` 제거 → `padding: 9px 8px` (내용에 따라 자연스럽게 ~44px 유지)
- `border-radius: 10px` → `99px` (rounded-pill)
- `border: 1px` (inactive) ↔ `border: 2px` (active) → 모두 `border: 1.5px` 고정 (jitter 0)
- 활성 색 토큰 변경: `--status-safe-bar` → `--status-safe` (foreground, 02 mirror)
- `transition` 추가 (배경/테두리/글자색 부드러운 전환)
- 시안 viewport 의 `.fp-result-btn` 사용 markup 은 그대로 (`<button class="fp-result-btn is-active-safe">` 형식 — class 만 매칭되면 OK). 단 시안에 아이콘이 텍스트 옆에 들어가는 영역이 있으면 lucide SVG 자리 비워두지 말고 sketch viewport 의 정상/주의/불량 버튼 안에 `<svg width="14" height="14"></svg>` 같은 placeholder 1개씩 넣어줘서 시안에서도 아이콘 자리가 보이게 한다 (sketch viewport HTML 변경은 `.fp-result-btn` 인스턴스 1~3 곳에 한정).

**(2) FloorPlanPage 기존 lucide import (line 5) 에 결과 아이콘 3종 추가**:

현재:
```tsx
import { ChevronLeft, Trash2, X } from 'lucide-react'
```

변경:
```tsx
import { ChevronLeft, Trash2, X, CheckCircle2, AlertTriangle, XCircle } from 'lucide-react'
```

(별도 import 라인 추가 금지 — 기존 라인에 병합.)

**(3) FloorPlanPage 메인 점검 결과 3택 (line 1732-1747) 02 verbatim mirror**:

현재 block (line 1732-1747) 전체를 다음으로 **완전 교체**:

```tsx
<div className="text-caption font-semibold text-text-tertiary mb-1.5">점검 결과</div>
<div className="flex gap-2 mb-3.5">
  {([
    ['normal',  '정상', CheckCircle2,   'safe'],
    ['caution', '주의', AlertTriangle,  'warning'],
    ['bad',     '불량', XCircle,        'danger'],
  ] as const).map(([val, label, Icon, tok]) => {
    const active = inspectResult === val
    const stateCls = active
      ? tok === 'safe'    ? 'border-safe bg-safe-bg text-safe'
        : tok === 'warning' ? 'border-warning bg-warning-bg text-warning'
        :                     'border-danger bg-danger-bg text-danger'
      : 'border-border-default bg-surface-raised text-text-tertiary'
    return (
      <button
        key={val}
        onClick={() => setInspectResult(val as 'normal' | 'caution' | 'bad')}
        className={`flex-1 px-2 py-[9px] rounded-pill border-[1.5px] text-body-sm font-bold whitespace-nowrap inline-flex items-center justify-center gap-1.5 cursor-pointer transition-colors ${stateCls}`}
      >
        <Icon className="w-4 h-4 flex-shrink-0" />
        {label}
      </button>
    )
  })}
</div>
```

**핵심 변경**:
- `gap-1.5` → `gap-2` (02 spacing — InspectionPage 1.row spacing 과 정합)
- `h-11 rounded-[10px]` → `px-2 py-[9px] rounded-pill border-[1.5px]`
- active/inactive border 두께 동일 (1.5px) — jitter 제거
- Icon prop을 tuple 4번째 원소로 추가
- 토큰: `border-safe-bar` (변환 후) → `border-safe` (02 mirror, foreground 토큰)
- 비-active 배경: `bg-surface-sunken` → `bg-surface-raised` (02 mirror)
- `whitespace-nowrap` 추가 (좁은 viewport 에서 한글 wrap 방지)
- `transition-colors` 추가 (호버/토글 부드러운 전환)
- `inspectResult` state 이름, `setInspectResult` setter, `'normal' | 'caution' | 'bad'` 타입 캐스트, 비즈니스 로직 100% 보존

**(4) FloorPlanPage paired BC 결과 3택 (line 1790-1805) 02 verbatim mirror**:

현재 block (line 1790-1805) 전체를 다음으로 **완전 교체**:

```tsx
<div className="text-caption font-semibold text-text-tertiary mb-1.5 tracking-[0.05em]">비상콘센트 점검 결과</div>
<div className="flex gap-2">
  {([
    ['normal',  '정상', CheckCircle2,   'safe'],
    ['caution', '주의', AlertTriangle,  'warning'],
    ['bad',     '불량', XCircle,        'danger'],
  ] as const).map(([val, label, Icon, tok]) => {
    const active = inspectBcResult === val
    const stateCls = active
      ? tok === 'safe'    ? 'border-safe bg-safe-bg text-safe'
        : tok === 'warning' ? 'border-warning bg-warning-bg text-warning'
        :                     'border-danger bg-danger-bg text-danger'
      : 'border-border-default bg-surface-raised text-text-tertiary'
    return (
      <button
        key={val}
        onClick={() => setInspectBcResult(val as 'normal' | 'caution' | 'bad')}
        className={`flex-1 px-2 py-[9px] rounded-pill border-[1.5px] text-body-sm font-bold whitespace-nowrap inline-flex items-center justify-center gap-1.5 cursor-pointer transition-colors ${stateCls}`}
      >
        <Icon className="w-4 h-4 flex-shrink-0" />
        {label}
      </button>
    )
  })}
</div>
```

(메인 3택과 동일 — state 이름만 `inspectResult` → `inspectBcResult` 차이.)

**비즈니스 로직 100% 보존 체크**:
- `inspectResult` / `inspectBcResult` state 이름 동일
- `setInspectResult` / `setInspectBcResult` setter 동일
- `'normal' | 'caution' | 'bad'` 타입 캐스트 동일
- `setSubmitError`/`handleSave`/`needSymptom` 등 관련 핸들러 영향 없음
- tuple 의 `tok` 원소(`'safe'|'warning'|'danger'`) 사용 위치 = active 시 색 분기. 02 패턴에서는 `opt.value === 'normal'` 으로 직접 분기하지만, 06 의 기존 tuple 패턴 (`tok`) 을 유지해도 결과 동일 — 06 의 markup 보존을 우선.
  </action>
  <verify>
    <automated>cd cha-bio-safety && grep -nE 'CheckCircle2|AlertTriangle|XCircle' src/pages/FloorPlanPage.tsx | grep -v '^#'</automated>
    기대: 4건 이상 (import 1줄 + 메인 3택 tuple 3건 + paired BC tuple 3건 = 7건 매칭)
  </verify>
  <done>
    - sketch `.fp-result-btn` 정의가 `border-radius: 99px` + `border: 1.5px solid` 로 변경됨 (옛 `border-radius: 10px` + `border: 1px/2px` 토글 0건)
    - FloorPlanPage line 5 import 에 `CheckCircle2, AlertTriangle, XCircle` 3종 추가 (기존 라인 병합)
    - 메인 점검 모달 결과 3택 (이전 line 1732-1747) `rounded-pill border-[1.5px] gap-1.5 + <Icon />` 패턴 적용
    - paired BC 결과 3택 (이전 line 1790-1805) 동일 패턴 적용
    - 비즈니스 로직 (state/setter/타입/handler) 변경 0건
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: rounded-lg 18곳 → rounded-sm 일괄 정정 (radius-mapping 표 verbatim)</name>
  <files>
    cha-bio-safety/src/pages/FloorPlanPage.tsx
  </files>
  <action>
PLAN 본문 `<radius-mapping>` 섹션의 18행 매핑 표 verbatim 으로 **18 곳 모두** `rounded-lg` → `rounded-sm` 치환.

**구체 위치 (line 번호는 Task 1 변경 후 기준이 아닌 변경 전 기준 — Task 1 은 line 1732-1805 영역만 건드리므로 18 곳 line 번호 모두 변동 없음)**:

| # | line | 변경 전 (Edit 의 `old_string` 핵심 fragment) | 변경 후 (`new_string` 핵심 fragment) |
|---|------|--------------------------------------------|--------------------------------------|
| 1 | 951  | `flex-shrink-0 rounded-lg bg-surface-sunken` | `flex-shrink-0 rounded-sm bg-surface-sunken` |
| 2 | 960  | `h-8 px-3 rounded-lg text-caption` | `h-8 px-3 rounded-sm text-caption` |
| 3 | 967  | `h-8 px-3 rounded-lg bg-surface-sunken border` | `h-8 px-3 rounded-sm bg-surface-sunken border` |
| 4 | 979  | `flex-1 h-9 rounded-lg text-label` | `flex-1 h-9 rounded-sm text-label` |
| 5 | 993  | `flex-shrink-0 h-8 px-3 rounded-lg text-caption` | `flex-shrink-0 h-8 px-3 rounded-sm text-caption` |
| 6 | 1441 | `flex-1 py-2 rounded-lg text-caption font-bold` (editZone) | `flex-1 py-2 rounded-sm text-caption font-bold` |
| 7 | 1458 | `px-1 py-2 rounded-lg text-caption font-semibold` (editMarkerType grid) | `px-1 py-2 rounded-sm text-caption font-semibold` |
| 8 | 1475 | `w-full px-3 py-2.5 rounded-lg bg-surface-sunken border border-border-default text-text-primary text-body-sm mb-3.5 box-border` (editLabel) | `w-full px-3 py-2.5 rounded-sm bg-surface-sunken border border-border-default text-text-primary text-body-sm mb-3.5 box-border` |
| 9 | 1490 | `flex-1 py-2 rounded-lg text-caption font-bold cursor-pointer bg-danger-bg text-danger` (editMarker 소화기 분리) | `flex-1 py-2 rounded-sm text-caption font-bold cursor-pointer bg-danger-bg text-danger` |
| 10 | 1553 | `flex-1 py-2 rounded-lg text-caption font-bold` (addZone, guidelamp) | `flex-1 py-2 rounded-sm text-caption font-bold` |
| 11 | 1570 | `px-1 py-2 rounded-lg text-caption font-semibold` (addMarkerType grid) | `px-1 py-2 rounded-sm text-caption font-semibold` |
| 12 | 1590 | `w-full px-3 py-2.5 rounded-lg bg-surface-sunken border border-border-default text-text-primary text-body-sm mb-3.5 box-border` (addLabel extinguisher) | `w-full px-3 py-2.5 rounded-sm bg-surface-sunken border border-border-default text-text-primary text-body-sm mb-3.5 box-border` |
| 13 | 1602 | `flex-1 py-2 rounded-lg text-caption font-bold` (addZone, extinguisher) | `flex-1 py-2 rounded-sm text-caption font-bold` |
| 14 | 1618 | `w-full px-3 py-2.5 rounded-lg bg-surface-sunken border border-border-default text-text-primary text-body-sm mb-3.5 box-border` (addLabel non-extinguisher) | `w-full px-3 py-2.5 rounded-sm bg-surface-sunken border border-border-default text-text-primary text-body-sm mb-3.5 box-border` |
| 15 | 1720 | `flex-1 h-9 rounded-lg bg-surface-sunken border border-border-default text-text-secondary text-caption font-semibold leading-none cursor-pointer inline-flex items-center justify-center` (소화기 정보 수정) | `flex-1 h-9 rounded-sm bg-surface-sunken border border-border-default text-text-secondary text-caption font-semibold leading-none cursor-pointer inline-flex items-center justify-center` |
| 16 | 1727 | `flex-1 h-9 rounded-lg bg-danger-bg border border-danger-bar/30 text-danger text-caption font-semibold leading-none cursor-pointer inline-flex items-center justify-center` (소화기 분리) | `flex-1 h-9 rounded-sm bg-danger-bg border border-danger-bar/30 text-danger text-caption font-semibold leading-none cursor-pointer inline-flex items-center justify-center` |
| 17 | 2038 | `flex-1 min-h-0 min-w-0 w-full px-2.5 rounded-lg bg-surface-sunken border border-border-strong text-text-primary text-body-sm box-border font-[inherit]` (자재명 input) | `flex-1 min-h-0 min-w-0 w-full px-2.5 rounded-sm bg-surface-sunken border border-border-strong text-text-primary text-body-sm box-border font-[inherit]` |
| 18 | 2047 | `w-full h-full min-w-0 pl-2.5 pr-7 rounded-lg bg-surface-sunken border border-border-strong text-text-primary text-body-sm box-border font-[inherit]` (수량 input) | `w-full h-full min-w-0 pl-2.5 pr-7 rounded-sm bg-surface-sunken border border-border-strong text-text-primary text-body-sm box-border font-[inherit]` |

**실행 방식**:
- Edit 도구로 한 곳씩 정확히 치환 (`rounded-lg` 만 `rounded-sm` 로 변경, 주변 클래스 0건 변경).
- 또는 sed 일괄: `sed -i '' 's/rounded-lg/rounded-sm/g' cha-bio-safety/src/pages/FloorPlanPage.tsx` 가능 — 단 사후 grep 으로 `rounded-lg` 0건 확인 필수 (현 파일에는 18곳만 있음).
- 비즈니스 로직 / 다른 className / 다른 페이지 0건 영향.

**제외 영역 재확인 (18곳 외 — 변경 금지)**:
- line 1242/1250/1259/1265/1289/1305/1338/1504/1510/1526/1624/1627/1739/1775/1797/1817/1826/1893/2006/2020/2066/2073/2107 등의 `rounded-[10px]` (sketch 10px verbatim)
- line 1289 의 `rounded-[14px]` (sketch 14px verbatim)
- line 1305 의 `rounded-[9px]` (마커 아이콘 박스 — 도면 sketch verbatim)
- line 1319 의 `rounded-[6px]` (close btn — sketch:323 verbatim)
- line 1338 의 `rounded-[10px]` (40px icon box — sketch 10px verbatim)
- line 1426/1538/1684/1692/1907/1932/1957/1993 의 `rounded-2xl` (모달 카드 = `--radius-lg`)
- line 1335 의 `rounded-sm` (이미 정답 — grab handle 2px? 확인 필요. 실제로는 sketch:464 `border-radius: 2px` 인데 현재 코드 `rounded-sm` (8px) 매핑은 약간 deviation 이지만 본 fix 범위 외 — 발견 1건 별도 quick 후보로 STATE 만 기록)
- line 1997 의 `rounded-xl` (경고 배지 12px — sketch 12px → tailwind md 가 정답이지만 본 fix 범위 외)
- line 1684/1692 의 `rounded-2xl` (모달 24px — sketch sketch:228 24px 와 일치)

이 외 영역은 **건드리지 않는다**.
  </action>
  <verify>
    <automated>cd cha-bio-safety && grep -cE 'rounded-lg' src/pages/FloorPlanPage.tsx</automated>
    기대: 0 (18곳 모두 정정 완료 → `rounded-lg` 잔존 0건)
  </verify>
  <done>
    - FloorPlanPage.tsx 의 `rounded-lg` 0건 (grep 으로 검증)
    - 18 곳 모두 `rounded-sm` 로 정정
    - 다른 페이지 / 다른 className / 비즈니스 로직 0건 변경
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: verify gate — sketch verbatim 정합 + 빌드 PASS</name>
  <files>
    (verification only — no file modifications)
  </files>
  <action>
변경 후 자체 검수. 모든 검증 명령은 `cwd = cha-bio-safety/` 에서 실행한다.

**검증 (전체 PASS 확인 후 종료)**:

1. **rounded-lg 잔존 0 건**:
   ```bash
   grep -cE 'rounded-lg' src/pages/FloorPlanPage.tsx
   ```
   기대: `0`

2. **결과 아이콘 3종 사용 확인** (import 1줄 + 메인 tuple 3건 + paired BC tuple 3건 = **7건**):
   ```bash
   grep -cE 'CheckCircle2|AlertTriangle|XCircle' src/pages/FloorPlanPage.tsx
   ```
   기대: `>= 7`

3. **rounded-pill 사용 위치 = 결과 3택 2영역** (메인 1 + paired BC 1 = 최소 **2건**):
   ```bash
   grep -nE 'rounded-pill border-\[1\.5px\]' src/pages/FloorPlanPage.tsx
   ```
   기대: 2 줄 이상 (메인 + paired BC)

4. **sketch 의 옛 `.fp-result-btn` 정의 (border-radius: 10px) 잔존 0건**:
   ```bash
   grep -nA 2 '\.fp-result-btn {' docs/redesign-context/06-floorplan/sketch/floorplan-modals-sketch.html
   ```
   기대: `border-radius: 99px` 로 시작, `10px` 잔존 0건

5. **빌드 PASS**:
   ```bash
   npm run build
   ```
   기대: TypeScript 0 error / Vite build PASS

6. **다른 페이지 / 비즈니스 로직 변경 0건** (git diff 확인):
   ```bash
   git diff --stat
   ```
   기대: 변경 파일 = `cha-bio-safety/src/pages/FloorPlanPage.tsx` + `cha-bio-safety/docs/redesign-context/06-floorplan/sketch/floorplan-modals-sketch.html` 2건만.

**한 항목이라도 FAIL 이면 즉시 Task 1/2 로 돌아가 수정. 모두 PASS 면 SUMMARY 작성 후 종료.**
  </action>
  <verify>
    <automated>cd cha-bio-safety && [ "$(grep -cE 'rounded-lg' src/pages/FloorPlanPage.tsx)" = "0" ] && [ "$(grep -cE 'CheckCircle2|AlertTriangle|XCircle' src/pages/FloorPlanPage.tsx)" -ge "7" ] && [ "$(grep -cE 'rounded-pill border-\[1\.5px\]' src/pages/FloorPlanPage.tsx)" -ge "2" ] && npm run build</automated>
    기대: 모든 조건 만족 + npm build PASS (exit 0)
  </verify>
  <done>
    - 6 검증 항목 모두 PASS
    - 변경 파일 2건 외 다른 파일 변경 0건 (`git diff --stat` 으로 확인)
    - SUMMARY 파일 작성 준비 완료
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 클라이언트 React UI ↔ 사용자 | 시각 디자인 변경 한정 — 사용자 입력/세션/권한 영향 없음 |
| FloorPlanPage TSX ↔ /api/inspections/* | 결과 3택 onClick 핸들러 의 비즈니스 분기 (`'normal' \| 'caution' \| 'bad'`) — 이번 변경 후에도 동일 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-260517-00y-01 | Tampering | 결과 3택 onClick (setInspectResult / setInspectBcResult) | accept | 시각 변경만, state setter 시그니처/타입 동일. Task 1 action 의 "비즈니스 로직 100% 보존 체크" 명시 + Task 3 의 git diff 검증으로 회귀 없음 보장 |
| T-260517-00y-02 | Spoofing | 결과 3택 active 시각 표현 | mitigate | active 색 토큰 페어 (border-safe/bg-safe-bg/text-safe 등) 유지 — 사용자 인지(완료/주의/불량)는 변경 전후 동일 |
| T-260517-00y-03 | Denial of Service | npm run build 실패 시 배포 차단 | mitigate | Task 3 의 verify gate 에서 `npm run build` PASS 필수 — FAIL 시 배포 단계 진입 불가 |
</threat_model>

<verification>
- `grep -cE 'rounded-lg' cha-bio-safety/src/pages/FloorPlanPage.tsx` → `0`
- `grep -cE 'CheckCircle2|AlertTriangle|XCircle' cha-bio-safety/src/pages/FloorPlanPage.tsx` → `>= 7`
- `grep -cE 'rounded-pill border-\[1\.5px\]' cha-bio-safety/src/pages/FloorPlanPage.tsx` → `>= 2`
- modals-sketch.html `.fp-result-btn` 정의 첫 `border-radius` 값 = `99px` (10px 잔존 0건)
- `cd cha-bio-safety && npm run build` → PASS
- `git diff --stat` 변경 파일 = 2 (FloorPlanPage.tsx + floorplan-modals-sketch.html)
</verification>

<success_criteria>
- 점검 모달 결과 3택 (정상/주의/불량) 이 02 InspectionPage 와 시각 동일 (rounded-pill + 1.5px border + Icon, jitter 0)
- paired BC 비상콘센트 결과 3택도 동일 패턴 적용
- FloorPlanPage.tsx 의 `rounded-lg` 0건 (18곳 → `rounded-sm` 일괄 정정)
- 시안 `.fp-result-btn` 정의가 02 mirror 로 재작성
- 비즈니스 로직 (state/setter/handler/API/타입) 0건 변경
- npm run build PASS
- 다른 페이지 / 다른 className 0건 영향
</success_criteria>

<output>
After completion, create `.planning/quick/260517-00y-redesign-06-floorplan-fix-02-mirror-radi/260517-00y-SUMMARY.md` containing:

- 변경 파일 2건 (FloorPlanPage.tsx + floorplan-modals-sketch.html) 의 git diff stat
- 18 rounded-lg → rounded-sm 정정 위치 표 (PLAN radius-mapping 표 재사용)
- 결과 3택 2영역 02 mirror 적용 위치 (line 번호)
- lucide 신규 import 3종
- 자체 검수 6 항목 PASS 결과
- 비즈니스 로직 0건 변경 증거 (state/setter/handler 시그니처 그대로)
- 후속 STATE.md 업데이트 문구 후보 (사용자 검수 → main 머지+배포 단계로 이어짐)
</output>
