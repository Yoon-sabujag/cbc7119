---
phase: quick-260614-5i0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cha-bio-safety/src/pages/ElevatorPage.tsx
autonomous: true
requirements: [QUICK-260614-5i0]

must_haves:
  truths:
    - "데스크톱 우측 분할 패널 호기 정보 헤더 아이콘이 옛 이모지가 아닌 Lucide 컴포넌트로 렌더된다"
    - "파일 전체에서 옛 이모지 객체 TYPE_ICON 을 렌더하는 곳이 0건이다"
    - "dead 상수 TYPE_ICON 정의가 제거되어 미사용 상수가 남지 않는다"
  artifacts:
    - path: "cha-bio-safety/src/pages/ElevatorPage.tsx"
      provides: "데스크톱 우측 헤더 Lucide 아이콘 렌더 + dead TYPE_ICON 제거"
      contains: "SelectedDesktopTypeIcon"
  key_links:
    - from: "selectedDesktopEv.type"
      to: "TYPE_ICON_COMPONENT"
      via: "SelectedDesktopTypeIcon 변수 매핑"
      pattern: "SelectedDesktopTypeIcon"
---

<objective>
승강기 페이지(ElevatorPage.tsx) 데스크톱 우측 분할 패널 최상단 "호기 정보 헤더" 아이콘을 옛 이모지 객체(`TYPE_ICON` 🛗📦🔲↕️)에서 Lucide 컴포넌트(`TYPE_ICON_COMPONENT`: ElevatorIcon/Package/UtensilsCrossed/MoveDiagonal)로 통일한다. 이 자리는 redesign emoji→Lucide sweep 에서 유일하게 누락된 1곳이며, 좌측 카드(line 553/580)와 동일 패턴/토큰을 적용한다. 변환 후 미사용이 되는 dead 상수 `TYPE_ICON` 정의도 함께 제거한다.

Purpose: 데스크톱 우측 패널 헤더만 이모지로 남아 좌측 카드/그룹 라벨/모달 등 나머지 12곳의 Lucide 아이콘과 시각적으로 불일치하던 문제 교정. 사용자 신고 + 컨펌 완료.
Output: `cha-bio-safety/src/pages/ElevatorPage.tsx` 단일 파일 수정.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 데스크톱 우측 헤더 아이콘 Lucide 통일 + dead TYPE_ICON 제거</name>
  <files>cha-bio-safety/src/pages/ElevatorPage.tsx</files>
  <action>
정확히 아래 3가지 편집만 수행한다. 추측 추가 금지, 범위 밖 무접촉.

(1) 컴포넌트 변수 추가 — line ~544 `const selectedDesktopEv = detailEv ?? evs[0] ?? ess[0] ?? null` 줄 바로 다음 줄에 한 줄 삽입:
   `    const SelectedDesktopTypeIcon = selectedDesktopEv ? TYPE_ICON_COMPONENT[selectedDesktopEv.type] : null`
   (들여쓰기는 인접 줄과 동일하게 4-space. JSX 컴포넌트 렌더는 대문자 변수가 필요하고, selectedDesktopEv 가 null 일 수 있어 null 가드.)

(2) line ~677 헤더 아이콘 렌더 교체:
   - old: `<div style={{ fontSize: 28 }}>{TYPE_ICON[selectedDesktopEv.type]}</div>`
   - new: `{SelectedDesktopTypeIcon && <SelectedDesktopTypeIcon size={28} className="text-text-secondary" />}`
   좌측 카드(line 580)와 동일 color 토큰 `text-text-secondary`, 헤더라 size 만 28 로 기존 이모지 footprint 보존. 부모 헤더 div(line 676)의 `gap: 12` 인라인 스타일은 그대로 둔다 — 아이콘이 div wrapper 없이 직접 flex item 이 되어도 gap 간격은 유지된다.

(3) dead 상수 제거 + stale 주석 정리:
   - line 194 `const TYPE_ICON:  Record<string,string> = { passenger:'🛗', cargo:'📦', dumbwaiter:'🔲', escalator:'↕️' }` 줄 전체 삭제 (교체 후 파일 전체 미사용).
   - line 196 주석에서 이모지 병행 언급 제거. 새 문구는 executor 재량이되 이모지 미사용 사실을 반영할 것. 예: `// Wave 1 신설 — list 탭 카드 + 그룹 라벨 + 데스크톱 우측 헤더 공용 컴포넌트 매퍼` 수준.

손대지 말 것 (preserve):
   - `TYPE_ICON_COMPONENT` 4 매핑(line 197~202) 무변경.
   - 우측 헤더 호기번호/location/status 배지 인라인 스타일(line 678~684) 무변경.
   - 부모 헤더 div(line 676)의 padding/borderBottom/display/alignItems/gap 인라인 스타일 무변경.
   - `TYPE_LABEL`, `STATUS_STYLE` 무변경.
   - 모바일 뷰 / 다른 탭 / 모달 / 좌측 카드 무접촉.
   - 정확히 이 1개 파일만 수정.

빌드/배포: npm run build 는 verify 용 1회 실행 OK. wrangler 배포는 절대 실행 금지 (메인 Claude 가 머지 후 직접 수행 — CLAUDE.md 콘솔 룰 + feedback_subagent_production_deploy_forbidden 준수). branch=production 유지.
  </action>
  <verify>
    <automated>cd cha-bio-safety && \
      test "$(grep -c 'TYPE_ICON\[' src/pages/ElevatorPage.tsx)" = "0" && \
      test "$(grep -c 'const TYPE_ICON:' src/pages/ElevatorPage.tsx)" = "0" && \
      test "$(grep -c 'SelectedDesktopTypeIcon' src/pages/ElevatorPage.tsx)" -ge "2" && \
      npx tsc --noEmit && \
      npm run build && \
      test "$(git diff --stat -- src/pages/ElevatorPage.tsx | grep -c 'ElevatorPage.tsx')" -ge "1" && \
      test "$(git diff --name-only | grep -vc 'cha-bio-safety/src/pages/ElevatorPage.tsx')" = "0"</automated>
  </verify>
  <done>
- `grep -n "TYPE_ICON\[" cha-bio-safety/src/pages/ElevatorPage.tsx` → 0건 (이모지 렌더 자리 소멸).
- `grep -n "const TYPE_ICON:" cha-bio-safety/src/pages/ElevatorPage.tsx` → 0건 (dead 상수 제거 확인).
- `grep -n "SelectedDesktopTypeIcon" cha-bio-safety/src/pages/ElevatorPage.tsx` → 정의 1 + 렌더 1 = 2건 이상.
- `cd cha-bio-safety && npx tsc --noEmit` → 0 errors.
- `cd cha-bio-safety && npm run build` → PASS.
- `git diff --name-only` → 정확히 `cha-bio-safety/src/pages/ElevatorPage.tsx` 1 파일만.
  </done>
</task>

</tasks>

<verification>
- 데스크톱 우측 패널을 열었을 때 호기 정보 헤더 아이콘이 좌측 카드와 동일한 Lucide 아이콘(passenger=ElevatorIcon, cargo=Package, dumbwaiter=UtensilsCrossed, escalator=MoveDiagonal)으로 렌더된다.
- 파일 전체에서 옛 이모지(🛗📦🔲↕️) 렌더 경로가 0건이고, 미사용 상수가 남지 않는다.
- 빌드/타입체크 통과, 변경 파일은 ElevatorPage.tsx 단 1개.
</verification>

<success_criteria>
- 우측 헤더 아이콘 Lucide 통일 완료 (좌측 카드와 시각 일치).
- dead 상수 TYPE_ICON 제거 + 주석 stale 문구 정리 완료.
- `npx tsc --noEmit` + `npm run build` PASS, 1 파일만 변경.
- wrangler 배포 미실행 (머지는 메인 Claude 담당).
</success_criteria>

<output>
Create `.planning/quick/260614-5i0-elevator-desktop-right-panel-header-icon/260614-5i0-SUMMARY.md` when done.
</output>
