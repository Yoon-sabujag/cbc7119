---
phase: quick-260525-4of
plan: 01
type: execute
wave: 1
depends_on: []
quick_id: 260525-4of
branch: redesign/21-legal-finding-detail (based on redesign/20-legal-findings HEAD or main)
date: 2026-05-25
autonomous: true
files_modified:
  - cha-bio-safety/docs/redesign-context/21-legal-finding-detail/sketch-wave-2-chrome.html
  - cha-bio-safety/docs/redesign-context/21-legal-finding-detail/sketch-wave-3-finding-info.html
  - cha-bio-safety/docs/redesign-context/21-legal-finding-detail/sketch-wave-4-resolve-form.html
  - cha-bio-safety/docs/redesign-context/21-legal-finding-detail/wave-5-tsx-conversion-checklist.md
requirements:
  - redesign-21-legal-finding-detail-w2-chrome
  - redesign-21-legal-finding-detail-w3-finding-info
  - redesign-21-legal-finding-detail-w4-resolve-form
  - redesign-21-legal-finding-detail-w5-tsx-checklist
must_haves:
  truths:
    - "W2~W4 sketch HTML 3개 + W5 checklist markdown 1개, 총 4 파일이 21-legal-finding-detail/ 직속 평면 배치로 생성된다 (sketch/ 서브폴더 절대 X — 20-legal-findings wic + 19-legal 40p + 28-splash 2q6 + 23-education o7b + 17-annual-plan 0j3 + 16-workshift + 27-login + 13-schedule + 14-reports 평면 패턴 mirror)"
    - "LegalFindingDetailPage.tsx (279 lines) + 외부 6 파일 (PhotoGrid / PhotoSourceModal / useMultiPhotoUpload / findingDownload / api / authStore) + App.tsx + 조부모 LegalPage + 부모 LegalFindingsPage 총 10 src 파일 모두 코드 변경 0 byte (verify gate 마지막 task git diff)"
    - "OQ #1~#5 LOCKED 5건 모두 4 파일 안에 verbatim 반영 — #1 모바일 자체 헤더 bg-surface-raised + border-b border-border-default + 타이틀 '지적 상세' 정중앙 (정적 문자열, 20-legal-findings 의 동적 분기와 다름) / #2 KVRow value 좌측 강조 없음 + status 표시는 화면 모드 자체 (form vs 결과, finding 칩 본 페이지 없음) — status- prefix 없음 + KVRow 컴포넌트만 사용 / #3 fontSize 12 + leading-none 격상 (제거 button '✕' 11 + canAdd '사진 첨부' 11 + uploading overlay '업로드 중' 10 → 12 격상) / #4 조치 완료 CTA T3 그라데이션 ≥3 anchor (모바일 고정 하단 + 데스크톱 inline + admin frame 별 anchor) + admin 다운로드 button 작은 도구 solid 유지 / #5 Lucide ChevronLeft size={20} + Download size={18} + Loader2 size={24} + Camera size={22} 교체 + back button 44x44 + admin 다운로드 모바일 44x44 격상 (w-11 h-11)"
    - "비즈 시그니처 anchor 11건 모두 4 파일 안에 verbatim 박제 — legalApi 2종 (getFinding(roundId, findingId) + resolveFinding(roundId, findingId, snake_case payload)) + useQuery 1종 (['legal-finding', id, fid]) + useMutation 1종 (uploadAll 선행 + 4 키 invalidate ['legal-finding', id, fid]+['legal-findings', id]+['legal-rounds']+['legal-round', id] + reset + navigate(-1)) + useMultiPhotoUpload 직접 사용 (사진 5장 슬롯 풀 컨트롤 — 본 페이지 FindingFormSheet 우회 X) + buildMetaTxt + fflate zipSync 동적 import + ZIP 파일명 `지적사항_${name}.zip` (name = location 안전화, **20-legal-findings round.title 기반과 다름** + 19-legal과 일치) + handleZipDownload iOS PWA `<a download>` + setTimeout(URL.revokeObjectURL, 3000) + handleResolve memo.trim() validation + finding.status open/resolved 분기 (open = textarea + 사진 5장 + CTA / resolved = KVRow 3행 + PhotoGrid) + admin 다운로드 button 조건부 (staff?.role === 'admin' && finding, selector 패턴 — 20-legal-findings 의 getState() 와 다름) + toast 5종 verbatim (success '조치 완료'/'다운로드 완료' + error '조치 처리 실패'/'조치 내용을 입력하세요'/'다운로드 실패') + KVRow (label 12 width 64 + children 14 flex 1 lineHeight 1.5) + SectionHeader (12/700 var(--t3) marginBottom 10) + @keyframes spin 중복 정의 (line 45 + line 147)"
    - "OQ #4 LOCKED T3 (resolve-form) 그라데이션 예외 — T3 sketch 안 `linear-gradient(135deg, #1d4ed8, #0ea5e9)` ≥3 (조치 완료 CTA anchor 모바일 고정 하단 + 데스크톱 inline + admin frame 별 anchor) + T1/T2/T4 는 linear-gradient 0건"
    - "19/20-legal 차이 5건 sketch 시각 반영 — (1) 단일 finding 페이지 (지적사항 1건의 상세 + 조치 처리 전용, 20-legal-findings LegalFindingsPage 의 다건 목록 + 19-legal LegalPage 의 마스터-디테일 우측 패널과 다름) (2) resolution form 풀 페이지 — 사진 5장 useMultiPhotoUpload **직접 사용** (20-legal-findings 가 FindingFormSheet 우회한 것과 다름, 본 페이지가 훅 직접 호출) (3) 조치 완료 button 풀폭 CTA 그라데이션 후보 (메인 액션 단일 — addButton 류와 같은 §6.4 후보 → OQ #4 LOCKED 그라데이션) (4) admin ZIP 다운로드 단일 finding 기반 — **파일명 location 기반** `지적사항_${name}.zip` (20-legal-findings 의 round.title 기반과 다름, **fid 기반 아님 — name = location 안전화**) (5) status open/resolved 분기로 화면 모드 자체가 바뀜 (open = textarea + 사진 슬롯 + CTA / resolved = 결과 KVRow 3행 + PhotoGrid, mutually exclusive)"
    - "각 task 마다 atomic git commit 1개씩 — 총 4 commit (executor cherry-pick 사고 6회 precedent 회피용 commit 명령 verbatim 박제, 20-legal-findings wic + 19-legal 40p + 28-splash 2q6 + 23-education o7b 패턴 동일)"
    - "평면 배치 — 4 파일 모두 cha-bio-safety/docs/redesign-context/21-legal-finding-detail/ 직속 (sketch/ 서브폴더 절대 X)"
    - "negative gate 4 sketch + 1 checklist 모두 통과 (이모지 0 — '📷' Camera Lucide 교체 / 9·10·11px 0 / status- prefix 0 / w-8 h-8 0 / 옛 alias 토큰 0)"
    - "W5 checklist 12 섹션 (§1 변환 범위 / §2 비즈 anchor 11건 보존 / §3 변환 매핑 영역 1~3 verbatim / §4 OQ LOCKED 5건 반영 / §5 negative gate (T3 linear-gradient 예외 anchor 명시) / §6 positive gate / §7 build/tsc / §8 자체 verify grep / §9 Tailwind cheatsheet / §10 비즈 보존 체크박스 / §11 메모리 룰 inline 12+ / §12 다음 단계 — 단일 finding 페이지 자체로 종결, 부모 페이지 (LegalFindingsPage) 변환은 별도 wave)"
  artifacts:
    - path: "cha-bio-safety/docs/redesign-context/21-legal-finding-detail/sketch-wave-2-chrome.html"
      provides: "모바일 자체 헤더 (Lucide ChevronLeft size={20} + 44x44 격상 OQ #5 + 타이틀 '지적 상세' 정중앙 정적 OQ #1 + admin 다운로드 button Lucide Download size={18} + 44x44 격상) + 데스크톱 타이틀 영역 (글로벌 chrome 0 — App.tsx line 117 정규식 cover, 좌측 '지적 상세' 22/800 + 우측 admin 다운로드 button 텍스트형 h 36) + Spinner Lucide Loader2 + 에러 단일 문장 + 모바일 고정 하단 CTA placeholder (T3 sketch 에서 채움) — 4 frame: 모바일 다크 빈/로딩 / 모바일 라이트 오류 / 데스크톱 다크 outline 글로벌 chrome 0"
      contains: "data-theme dark+light / bg-surface-page 외곽 / bg-surface-raised 모바일 헤더 (OQ #1 LOCKED) / border-b border-border-default borderBottom / Lucide ChevronLeft size={20} 모바일 back 44x44 (OQ #5 LOCKED, w-11 h-11 또는 arbitrary w-[44px] h-[44px]) / Lucide Download size={18} 모바일 admin 다운로드 button 44x44 (OQ #5 LOCKED, staff?.role === 'admin' && finding 조건부 line 159) / 타이틀 '지적 상세' verbatim 정중앙 정적 (line 158 16/700, **20-legal-findings 의 동적 분기와 다름**) / Spinner Lucide Loader2 size={24} animate-spin (OQ #5 line 41~48 div 폐기) / 데스크톱 타이틀 padding '24px 32px 12px' flex space-between + 좌측 '지적 상세' 22/800 (line 170 verbatim) + 우측 admin 다운로드 button 텍스트형 (13/700 h 36 bg-surface-sunken border border-border-strong padding '0 16px' rounded-sm) — 텍스트 '다운로드' / '다운로드 중...' (downloading) / 에러 단일 문장 '항목을 불러오지 못했습니다. 뒤로 가서 다시 시도하세요.' (line 185 verbatim, **20-legal-findings 의 '화면을 당겨서 다시 시도하세요' 와 다름**) / 글로벌 chrome 0건 anchor (App.tsx line 117 정규식 verbatim `!location.pathname.match(/^\\/legal\\/.+/)` → showNav=false) / 모바일 고정 하단 CTA placeholder (status open 한정, T3 에서 그라데이션 채움) / OQ #1 + OQ #5 citation"
      min_lines: 320
    - path: "cha-bio-safety/docs/redesign-context/21-legal-finding-detail/sketch-wave-3-finding-info.html"
      provides: "Section 1 (지적 정보 KVRow 4행 — 지적 내용 whiteSpace pre-wrap + 위치 ?? '-' + 등록일 fmtDate + 등록자 createdByName ?? createdBy) + Section 2 (지적 사진 — PhotoGrid 또는 '사진 없음') + SectionHeader 패턴 (12/700 var(--t3) marginBottom 10) + KVRow 컴포넌트 (label 64 flexShrink 0 + children flex 1) — 4 frame: 다크 모바일 sectionPad '20px 16px' + 사진 0건 / 다크 데스크톱 sectionPad '20px 32px' + maxWidth 700 + 사진 3개 / 라이트 모바일 위치 fallback '-' / 라이트 데스크톱 사진 5개"
      contains: "data-theme dark+light / Section 외곽 padding sectionPad (`isDesktop ? '20px 32px' : '20px 16px'`) borderBottom 1px var(--bd) → border-b border-border-default / SectionHeader '지적 정보' (line 198) + '지적 사진' (line 209) 12/700 text-text-tertiary marginBottom 10 / KVRow 4행 verbatim — '지적 내용' (description whiteSpace pre-wrap, 14 text-text-primary) / '위치' (location ?? '-' fallback, line 202) / '등록일' (fmtDate(createdAt) verbatim — null → '-' / ${y}.${m}.${d} ${HH}:${mm} zero-padded **분까지 표시**, 20-legal-findings 의 일까지만 표시와 다름) / '등록자' (createdByName ?? createdBy fallback) / KVRow 컴포넌트 anchor (line 22~29 verbatim) label 12 width 64 flexShrink 0 text-text-tertiary leading-none + children 14 flex 1 text-text-primary lineHeight 1.5 + gap 12 alignItems flex-start / Section 1 내부 flex column gap 8 (KVRow 4행 사이) / Section 2 PhotoGrid photoUrls={photoKeys.map(k => '/api/uploads/' + k)} marginTop 8 / Section 2 '사진 없음' (line 213 verbatim, 13 text-text-tertiary marginTop 8) / **finding 상태 칩 본 페이지 없음** (status 표시는 화면 모드 자체로 표현 — open 이면 form / resolved 면 결과 — OQ #2 칩 추가 안 함 LOCKED) / 데스크톱 maxWidth 700 중앙 정렬 (line 195) / 비즈 anchor 11건 박제 fence / OQ #2 + OQ #3 citation"
      min_lines: 320
    - path: "cha-bio-safety/docs/redesign-context/21-legal-finding-detail/sketch-wave-4-resolve-form.html"
      provides: "status open 분기 Section 3 (조치 내용 — textarea controlled + 사진 5장 useMultiPhotoUpload 슬롯 + canAdd '+사진 첨부' + 데스크톱 inline CTA marginTop 16) + status resolved 분기 Section 4 (조치 결과 KVRow 3행 — 조치일시 / 조치자 / 조치 내용 + 조치 사진 PhotoGrid) + 두 모드 매트릭스 (open form vs resolved 결과 mutually exclusive) + admin ZIP 다운로드 단일 finding 기반 (**파일명 location 기반** `지적사항_${name}.zip`) + 모바일 고정 하단 CTA (open 한정) + **★ OQ #4 LOCKED `linear-gradient(135deg, #1d4ed8, #0ea5e9)` ≥3 anchor** (조치 완료 CTA 모바일 고정 + 데스크톱 inline + admin frame 별 anchor) — 4 frame: 다크 모바일 open form + 사진 슬롯 3 (img+uploading+canAdd) + 고정 하단 CTA / 다크 데스크톱 open form + 사진 슬롯 5 + 데스크톱 inline CTA / 다크 모바일 resolved 결과 + 조치 사진 PhotoGrid / 라이트 데스크톱 resolved 결과 + admin ZIP zipLoading 단계"
      contains: "data-theme dark+light / Section 3 padding sectionPad borderBottom + SectionHeader '조치 내용' (line 220 verbatim) / textarea (line 221 verbatim — placeholder '조치 내용을 입력하세요' rows 3 width 100% bg-surface-sunken border border-border-strong text-text-primary text-label rounded-sm padding '10px 12px' boxSizing border-box fontFamily inherit lineHeight 1.5 resize vertical outline none) / 조치 사진 라벨 '조치 사진 (최대 5장)' (line 223 verbatim, text-caption font-bold leading-none text-text-tertiary marginBottom 6, OQ #3 격상 12) / input cameraRef + albumRef hidden + accept 'image/*' + capture 'environment' / PhotoSourceModal {open: showPicker, onClose: closePicker, onCamera: pickCamera, onAlbum: pickAlbum} / 슬롯 외곽 flex gap 8 overflowX auto paddingBottom 4 / 슬롯 img 72x72 objectFit cover rounded-sm border border-border-default block / 제거 button (-6,-6) 20x20 rounded-full bg-danger text-white text-caption font-bold leading-none '✕' (line 231 verbatim, **OQ #3 격상 11 → 12**) / uploading overlay inset 0 bg-black/40 rounded-sm text-caption leading-none text-white '업로드 중' (line 232 verbatim, **OQ #3 격상 10 → 12**) / canAdd 72x72 rounded-sm bg-surface-sunken border border-border-strong border-dashed text-text-tertiary text-caption font-semibold leading-none + Lucide Camera size={22} (OQ #5 교체) + '사진 첨부' (line 237 verbatim, **OQ #3 격상 11 → 12**) / **★ 데스크톱 inline CTA (line 244~248 verbatim, marginTop 16 width 100% h 48 rounded-md text-body-sm font-bold text-text-on-accent — 조치 완료 / 처리 중... isSubmitting opacity 0.5 cursor not-allowed + 인라인 `style={{ background: 'linear-gradient(135deg, #1d4ed8, #0ea5e9)' }}` (OQ #4 LOCKED anchor 1)** / Section 4 (status resolved 분기, line 253~265) padding sectionPad borderBottom + SectionHeader '조치 결과' (line 255 verbatim) + KVRow 3행 verbatim — '조치일시' (fmtDate(resolvedAt)) + '조치자' (resolvedByName ?? resolvedBy ?? '-') + '조치 내용' (resolutionMemo ?? '-' whiteSpace pre-wrap) + 내부 flex column gap 8 / Section 4 PhotoGrid (resolutionPhotoKeys length > 0 조건부, marginTop 12) / **★ 모바일 고정 하단 CTA (line 270~275 verbatim, status open 한정 — !isDesktop && !isLoading && !error && finding && finding.status === 'open' 조건부) position fixed bottom 0 left 0 right 0 bg-surface-page border-t border-border-default padding '12px 16px' paddingBottom 'calc(12px + var(--sab, 0px))' iOS safe-area zIndex 20 + button width 100% h 48 rounded-md text-body-sm font-bold text-text-on-accent transition 'opacity 0.15s' — '조치 완료' / '처리 중...' + 인라인 `style={{ background: 'linear-gradient(135deg, #1d4ed8, #0ea5e9)' }}` (OQ #4 LOCKED anchor 2)** / **★ admin frame ZIP 다운로드 single finding (OQ #4 LOCKED anchor 3 데스크톱 inline CTA 별도 frame 또는 admin 다운로드 button hover frame) — handleDownload (line 89~130) fflate zipSync 동적 import + buildMetaTxt → 내용.txt always + photoKeys/resolutionPhotoKeys Promise.allSettled + iOS PWA `<a download>` (createElement + body.appendChild + click + removeChild + setTimeout(URL.revokeObjectURL, 3000)) + 파일명 `지적사항_${name}.zip` (line 119, name = (finding.location ?? '위치없음').replace(/[\\/\\\\:*?\"<>|]/g, '_'), **20-legal-findings round.title 기반과 다름 + fid 기반 아님 — location 기반**) + 사진 파일명 `지적사진-${j+1}.jpg` / `조치사진-${j+1}.jpg`** / handleResolve memo.trim() 빈 값 → toast.error '조치 내용을 입력하세요' (line 134 verbatim) + 아니면 resolveMutation.mutate() / resolveMutation uploadAll 선행 anchor (line 67~87 verbatim) + 4 키 invalidate + toast.success '조치 완료' + reset + navigate(-1) / status open/resolved 분기 mutually exclusive anchor / paddingBottom 분기 'calc(72px + var(--sab, 0px))' (open 모바일) / 24 (open 데스크톱 / resolved 양쪽) / 비즈 anchor 11건 박제 fence + ZIP 패턴 + iOS PWA anchor / OQ #4 + OQ #5 citation"
      min_lines: 320
    - path: "cha-bio-safety/docs/redesign-context/21-legal-finding-detail/wave-5-tsx-conversion-checklist.md"
      provides: "TSX 변환 verify checklist (markdown, 12 섹션) — LegalFindingDetailPage.tsx 279 lines 단일 atomic 변환 룰 + 비즈 anchor 11건 박제 + Tailwind cheatsheet + 메모리 룰 12건 cross-ref + 단일 finding 페이지 자체로 종결 명시"
      contains: "§1 변환 범위 (LegalFindingDetailPage.tsx 단일 atomic 279 lines, 3 영역 통합 — 상단 imports/fmtDate/KVRow/SectionHeader/Spinner + 메인 함수 + JSX render) / §2 보존 (비즈 anchor 11건 verbatim fence) / §3 변환 매핑 (영역 1~3 verbatim) / §4 OQ LOCKED 5건 반영 매핑 표 / §5 negative gate (T3 linear-gradient 예외 anchor 명시) / §6 positive gate / §7 build/tsc / §8 자체 verify grep 모음 / §9 Tailwind cheatsheet (v0.1.1 토큰 → utility class 매핑, 21-legal-finding-detail 적용) / §10 비즈 보존 체크박스 (legalApi 2종 + useQuery 1 + useMutation 1 uploadAll 선행 + 4 키 invalidate + navigate(-1) + handleDownload iOS PWA + ZIP location 기반 + handleResolve validation + finding.status open/resolved 분기 + admin selector 패턴 + toast 5 + KVRow + SectionHeader) / §11 메모리 룰 inline 12+ (feedback_inspection_unresolved_color + project_inspection_completion_rule + feedback_tailwind_w8_h8_is_48px + feedback_tailwind_token_class_pattern + feedback_text_caption_leading_none + feedback_sketch_realistic_data + feedback_design_changes_ask_first + feedback_planner_prompt_sketch_verbatim + feedback_tsx_wave_emoji_dot_gap + feedback_tsx_wave_stat_card_drift + feedback_subagent_production_deploy_forbidden + feedback_redesign_sketch_rule_enforcement + feedback_cbc7119_design_never_wrangler) / §12 다음 단계 (단일 finding 페이지 자체로 종결, 부모 LegalFindingsPage 변환은 별도 wave, 조부모 LegalPage 의 19-legal 변환 완료 확인)"
      min_lines: 320
  key_links:
    - from: "wave-1-index.md §7 OQ #1 LOCKED (모바일 자체 헤더 bg-surface-raised + border-b border-border-default + 타이틀 '지적 상세' 정중앙 정적)"
      to: "sketch-wave-2-chrome.html + wave-5-tsx-conversion-checklist.md"
      via: "모바일 자체 헤더 (line 149~165) = `bg-surface-raised border-b border-border-default` (옛 `rgba(22,27,34,0.97)` 인라인 폐기). height 48 + 타이틀 '지적 상세' 정중앙 정적 (line 158 verbatim, **20-legal-findings 의 동적 분기와 다름**) + back button 44x44 (OQ #5 격상) + admin 다운로드 button 44x44 (OQ #5 격상). 20-legal-findings wic + 19-legal 40p + 16-workshift + 17-annual-plan + 28-splash + 23-education 7 페이지 일관."
      pattern: "bg-surface-raised|border-b border-border-default|지적 상세"
    - from: "wave-1-index.md §7 OQ #2 LOCKED (status 표시는 화면 모드 자체로 — finding 칩 본 페이지 없음 + KVRow value 좌측 강조 없음)"
      to: "sketch-wave-3-finding-info.html + sketch-wave-4-resolve-form.html + wave-5-tsx-conversion-checklist.md"
      via: "finding 상태 칩 본 페이지 없음 (20-legal-findings 의 '미조치/완료' 칩 + borderLeft 2px 분기 패턴이 본 페이지에는 없음 — 단일 finding 상세 페이지 → status 표시는 화면 모드 자체로 표현). open = Section 3 (form) / resolved = Section 4 (결과) mutually exclusive. KVRow 컴포넌트 (line 22~29) 만 사용 — label 12 width 64 flexShrink 0 + children 14 flex 1 lineHeight 1.5 + gap 12. **status- prefix 없음** (memory `feedback_tailwind_token_class_pattern`)."
      pattern: "finding\\.status === 'open'|finding\\.status === 'resolved'|KVRow"
    - from: "wave-1-index.md §7 OQ #3 LOCKED (§1.1 9·10·11 fontSize 12 격상 + leading-none)"
      to: "sketch-wave-4-resolve-form.html + wave-5-tsx-conversion-checklist.md"
      via: "uploading overlay '업로드 중' 10 (line 232) + 제거 button '✕' 11 (line 231) + canAdd '사진 첨부' 11 (line 237) 모두 → `text-caption font-bold leading-none` (12px) 격상. KVRow label 12 + SectionHeader 12 + 조치 사진 라벨 12 (이미 12) — leading-none 명시. 14 (KVRow children + 에러 + CTA button) + 13 (textarea + '사진 없음' + 데스크톱 admin 다운로드 button) + 16 (모바일 헤더 타이틀) + 22 (데스크톱 타이틀) 보존. 20-legal-findings wic + 19-legal 40p + 23-education + 16-workshift + 17-annual-plan OQ 일관."
      pattern: "text-caption|leading-none"
    - from: "wave-1-index.md §7 OQ #4 LOCKED (조치 완료 CTA 그라데이션 ≥3 + admin 다운로드 button 작은 도구 solid 유지)"
      to: "sketch-wave-4-resolve-form.html + wave-5-tsx-conversion-checklist.md"
      via: "조치 완료 CTA (모바일 고정 line 270~275 + 데스크톱 inline line 244~248) = `style={{ background: 'linear-gradient(135deg, #1d4ed8, #0ea5e9)' }}` 인라인 그라데이션 (T3 만 linear-gradient negative gate 예외 anchor ≥3). admin 다운로드 button (작은 도구, 모바일 36x36→44x44 icon + 데스크톱 텍스트 h 36) = solid `bg-surface-sunken border border-border-strong` 유지 또는 icon 색만 (모바일). disabled = opacity 0.5 cursor not-allowed."
      pattern: "linear-gradient\\(135deg, #1d4ed8, #0ea5e9\\)|조치 완료"
    - from: "wave-1-index.md §7 OQ #5 LOCKED (Lucide ChevronLeft + Download + Loader2 + Camera 교체 + back/admin 다운로드 44x44 격상)"
      to: "sketch-wave-2-chrome.html + sketch-wave-4-resolve-form.html + wave-5-tsx-conversion-checklist.md"
      via: "(1) 모바일 헤더 back button — 인라인 SVG ChevronLeft (line 156 path `M15 19l-7-7 7-7` strokeWidth 2 size 20) → Lucide `<ChevronLeft size={20} />` 교체 + **36x36 → 44x44 격상**. w-8 함정 회피 → `w-11 h-11` (44px tailwind 기본 spacing 11). (2) 모바일 헤더 admin 다운로드 button — 인라인 SVG Download (line 161 path `M12 5v14m0 0l-6-6m6 6l6-6M5 19h14` size 18) → Lucide `<Download size={18} />` 교체 + **36x36 → 44x44 격상**. (3) Spinner div + @keyframes spin (line 41~48 + line 147 외곽 keyframes 중복) → Lucide `<Loader2 className=\"animate-spin\" size={24} />` 교체 + 양쪽 keyframes 폐기 가능. (4) canAdd button '📷' 이모지 (line 237 fontSize 22) → Lucide `<Camera size={22} />` 교체 — **19-legal LegalPage 의 FindingDetailPanel 안 '📷' 첨부 button 이모지와 일치 패턴**. import 추가: `import { ChevronLeft, Download, Loader2, Camera } from 'lucide-react'`."
      pattern: "ChevronLeft|Download|Loader2|Camera|lucide-react"
    - from: "wave-5-tsx-conversion-checklist.md §3 변환 매핑"
      to: "LegalFindingDetailPage.tsx (변경 0 byte — TSX 변환은 본 PLAN 범위 외, W6 별도 wave)"
      via: "W5 checklist 는 W2~W4 sketch + LegalFindingDetailPage.tsx 279 lines 단일 atomic 변환 룰 박제 (다음 turn 의 W6 변환 wave 진입점). 본 PLAN 에서는 TSX 변경 0 byte."
      pattern: "LegalFindingDetailPage.tsx"
    - from: "wave-5-tsx-conversion-checklist.md §11 메모리 룰 inline + §12 다음 단계"
      to: "wave-1-index.md §5 메모리 룰 12건 cross-ref + 단일 finding 페이지 자체로 종결 (부모 LegalFindingsPage 별도 wave 완료 확인)"
      via: "feedback_inspection_unresolved_color (status 표시 화면 모드 자체로 일반화 — 칩 없음, finding.status === 'open'/'resolved' 분기 source of truth) + project_inspection_completion_rule (admin role 분기 + finding.status 분기 source of truth 일반화) + feedback_tailwind_token_class_pattern + feedback_tailwind_w8_h8_is_48px + feedback_text_caption_leading_none + 단일 finding 페이지 자체로 종결 명시 (20-legal-findings + 19-legal 변환 완료 확인)"
      pattern: "feedback_inspection_unresolved_color|project_inspection_completion_rule|LegalFindingDetailPage"
  scope_negatives:
    - "sketch HTML 3개 + checklist md 1개 만 산출. TSX 변환은 W6 별도 wave (본 PLAN 범위 외)"
    - "LegalFindingDetailPage.tsx + 외부 6 파일 (PhotoGrid / PhotoSourceModal / useMultiPhotoUpload / findingDownload / api / authStore) + App.tsx + 조부모 LegalPage + 부모 LegalFindingsPage 총 10 src 파일 모두 0 byte 변경 (verify gate 최종 git diff)"
    - "wrangler 명령 절대 X / npm run deploy 절대 X (CLAUDE.local.md + memory `feedback_cbc7119_design_never_wrangler`)"
    - "옛 alias 토큰 (var(--bg/bg2/bg3/bg4/bd/bd2/t1/t2/t3/acl/accent/safe/warn/danger)) sketch 본문 안 0 (tokens.css 정의 인용 fence 안 예외)"
    - "status- prefix 0 (bg-status-safe / text-status-danger 등 사용 시 verify FAIL, OQ #2 위반)"
    - "이모지 0 (sketch 본문 — '📷' Camera 본 페이지 직접 사용, Lucide Camera 교체 anchor 만 텍스트 인용)"
    - "w-8 / h-8 className 0 (48px 함정, memory `feedback_tailwind_w8_h8_is_48px`)"
    - "9·10·11 fontSize 인라인 0 (text-caption 12 leading-none 마지노선, OQ #3 LOCKED, 제거 button '✕' 11 + canAdd '사진 첨부' 11 + uploading overlay '업로드 중' 10 모두 격상)"
    - "linear-gradient — T1/T2/T4 = 0건 / **T3 만 예외 anchor ≥3** (OQ #4 LOCKED 조치 완료 CTA 그라데이션 anchor)"
    - "sketch/ 서브폴더 절대 금지 — 4 파일 모두 21-legal-finding-detail/ 직속 평면 배치"
    - "TSX 변환 wave (W6) 는 별도 turn — 본 PLAN 에서 LegalFindingDetailPage.tsx 1 byte 도 수정 금지"
---

<objective>
redesign/21-legal-finding-detail 의 W1 인덱스(`wave-1-index.md`, 678 lines, OQ 5건 default LOCKED 완료 2026-05-25 013)를 기반으로
W2~W5 4 sub-wave 산출물을 **단일 quick task** 안에서 **atomic 4 commit** 으로 한 번에 만든다.

- **W2 (T1):** 모바일 자체 헤더 (Lucide ChevronLeft + 44x44 격상 OQ #5 + 타이틀 '지적 상세' 정중앙 정적 OQ #1 + admin 다운로드 button Lucide Download + 44x44 격상) + 데스크톱 타이틀 영역 (글로벌 chrome 0 — App.tsx line 117 정규식 `^\/legal\/.+` 매칭 → showNav=false, 좌측 '지적 상세' 22/800 + 우측 admin 다운로드 button 텍스트형) + Spinner Lucide Loader2 + 에러 단일 문장 + 모바일 고정 하단 CTA placeholder (T3 sketch 에서 채움) — 4 frame
- **W3 (T2):** Section 1 (지적 정보 KVRow 4행) + Section 2 (지적 사진 PhotoGrid 또는 '사진 없음') + SectionHeader 패턴 + KVRow 컴포넌트 패턴 — **status 칩 없음** (OQ #2 finding 칩 본 페이지 없음, status 표시는 화면 모드 자체로) — 4 frame
- **W4 (T3):** status open 분기 Section 3 (조치 내용 textarea + 사진 5장 useMultiPhotoUpload 슬롯 + canAdd + 데스크톱 inline CTA marginTop 16) + status resolved 분기 Section 4 (조치 결과 KVRow 3행 + 조치 사진 PhotoGrid) + 모바일 고정 하단 CTA (open 한정) + admin ZIP 다운로드 단일 finding 기반 (**파일명 location 기반** `지적사항_${name}.zip`) + **OQ #4 LOCKED 조치 완료 CTA 그라데이션 ≥3 박제** + handleZipDownload iOS PWA — 4 frame (open 모바일 / open 데스크톱 / resolved 모바일 / resolved 데스크톱 + admin ZIP)
- **W5 (T4):** TSX 변환 verify checklist markdown (sketch 아님, W2~W4 grep 추출 검증 룰 + 12 섹션 + 비즈 anchor 11건 박제 + LegalFindingDetailPage.tsx 279 lines 단일 atomic 변환 룰 + 단일 finding 페이지 자체로 종결 명시)

**Purpose:** 20-legal-findings wic / 19-legal 40p / 28-splash 2q6 / 17-annual-plan 0j3 / 16-workshift sjj / 27-login c6p / 14-reports / 13-schedule / 23-education o7b 와 동일한 평면(flat sibling) 패턴으로 21-legal-finding-detail sketch 단계 4 wave 분량을 한 turn 에 처리. LegalFindingDetailPage 가 279 lines 단일 export (내부 panel 없음, 20-legal-findings 와 동일 평면 구조) + 모바일/데스크톱 useIsDesktop 분기 (≥768px) + 데스크톱 maxWidth 700 중앙 정렬 (20-legal-findings 800 보다 좁음) + 모바일 고정 하단 CTA (status open 한정) + admin 다운로드 button 분기 (selector 패턴 — staff?.role === 'admin' && finding) + finding.status open/resolved 분기 (화면 모드 자체) + KVRow + SectionHeader 컴포넌트 패턴 + 사진 5장 useMultiPhotoUpload 직접 사용 + resolveMutation uploadAll 선행 + 4 키 invalidate + navigate(-1) + ZIP 단일 finding 다운로드 (location 기반) — 본 PLAN 에서 sketch 3 wave + checklist 1 wave 한 번에 처리해 cbc7119-preview 자동 배포 사이클 1회로 사용자 시각 검수 가능하게 함.

**Output:** `cha-bio-safety/docs/redesign-context/21-legal-finding-detail/` 직속 4 파일 (sketch HTML 3개 + checklist md 1개).
`LegalFindingDetailPage.tsx` (279 lines) + 외부 6 파일 + App.tsx + 조부모 LegalPage + 부모 LegalFindingsPage **총 10 src 파일 모두** 코드 변경 0 byte (verify gate 마지막 task 에서 확인).

**20-legal-findings wic PLAN.md mirror:** 본 PLAN 은 20-legal-findings wic PLAN (809 lines, 단일 plan / 4 task / atomic 4 commit / verify gate per task) 의 정확한 mirror. 19/20-legal 과 차이 5건:
- (1) 단일 finding 페이지 (지적사항 1건의 상세 + 조치 처리 전용, 20-legal-findings LegalFindingsPage 의 다건 목록 + 19-legal LegalPage 의 마스터-디테일 우측 패널과 다름 — 본 페이지는 풀 페이지 단독)
- (2) resolution form 풀 페이지 — 사진 5장 useMultiPhotoUpload **직접 사용** (line 59, 20-legal-findings 가 FindingFormSheet 우회한 것과 다름 — 본 페이지가 훅 직접 호출)
- (3) 조치 완료 button 풀폭 CTA 그라데이션 후보 (메인 액션 단일 — addButton 류와 같은 §6.4 후보 → OQ #4 LOCKED `linear-gradient(135deg, #1d4ed8, #0ea5e9)` 그라데이션)
- (4) admin ZIP 다운로드 단일 finding 기반 — **파일명 location 기반** `지적사항_${name}.zip` (line 119, name = (finding.location ?? '위치없음').replace(/[\/\\:*?"<>|]/g, '_'), **20-legal-findings 의 round.title 기반과 다름 + fid 기반 아님 — name = location 안전화** + 19-legal LegalPage 의 FindingDetailPanel 단일 다운로드 패턴과 일치)
- (5) finding.status open/resolved 분기로 화면 모드 자체가 바뀜 (line 218 open / line 253 resolved, mutually exclusive — open = Section 3 textarea + 사진 슬롯 + CTA / resolved = Section 4 KVRow 3행 + PhotoGrid)

**중요 — executor commit 부모 머지 사고 회피:** 20-legal-findings wic + 19-legal 40p + 16-workshift sjj + 17-annual-plan wmq + 28-splash 2q6 + 23-education o7b 등 다수 quick 에서 executor worktree commit 이 부모 브랜치로 자동 머지 안 되어 cherry-pick 복구 필요했음 (6회 재현). **executor 는 worktree 안에서 `git commit` 후 명시적 보고 필수** (commit hash + message 1줄) + orchestrator 가 필요 시 자동 cherry-pick 으로 복구한다. 본 PLAN 각 task 의 commit 명령은 명시 박제.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jykevin/Documents/cbc7119-design/CLAUDE.md
@/Users/jykevin/Documents/cbc7119-design/CLAUDE.local.md

# W1 인덱스 (단일 진입점, OQ 5건 default LOCKED 완료 2026-05-25 013, 678 lines)
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/21-legal-finding-detail/wave-1-index.md

# Precedent — 20-legal-findings wic PLAN.md + 4 산출 파일 (정확한 mirror 소스, 본 PLAN 의 구조 원본)
@/Users/jykevin/Documents/cbc7119-design/.planning/quick/260523-wic-redesign-20-legal-findings-w2-w5-4-atomi/260523-wic-PLAN.md
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/20-legal-findings/sketch-wave-2-chrome.html
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/20-legal-findings/sketch-wave-3-finding-list.html
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/20-legal-findings/sketch-wave-4-admin-tools.html
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/20-legal-findings/wave-5-tsx-conversion-checklist.md

# Source TSX (sketch 대상, 변경 0 — 10 파일 모두 untouched)
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/src/pages/LegalFindingDetailPage.tsx
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/src/App.tsx
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/src/pages/LegalPage.tsx
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/src/pages/LegalFindingsPage.tsx
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/src/utils/api.ts

# 21-legal-finding-detail redesign 컨텍스트 (토큰/타이포 source, design-system v0.1.1)
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/21-legal-finding-detail/design-system.md
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/21-legal-finding-detail/tokens.css
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/21-legal-finding-detail/typography.css

# 02+06 chrome 통일 룰 (21-legal-finding-detail = 점검 시리즈 손주-route → 단 App.tsx line 117 showNav=false 특수 케이스)
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/00-design-context/inspection-modal-chrome-rules.md
</context>

<rules_verbatim>
### OQ LOCKED 5건 (wave-1-index.md §7 default 답 verbatim, 사용자 "다음 진행" 으로 LOCKED 2026-05-25)

**OQ #1 (모바일 자체 헤더 토큰 + 타이틀 정적 '지적 상세'):**
> LOCKED (2026-05-25): LegalFindingDetailPage 모바일 자체 헤더 (line 149~165) = `bg-surface-raised border-b border-border-default`. 기존 `rgba(22,27,34,0.97)` (raised 변형 alpha) + `1px solid var(--bd)` 인라인 토큰 완전 폐기. height 48 + 타이틀 '지적 상세' (line 158 verbatim, **정적 문자열 — 20-legal-findings 의 동적 분기와 다름**) 정중앙 + back button 44x44 (OQ #5 격상) + admin 다운로드 button 44x44 (OQ #5 격상, `staff?.role === 'admin' && finding` 조건부) + position absolute left 12 / right 12. W2 sketch 모바일 frame + TSX 변환 양쪽 동일 적용. 20-legal-findings wic + 19-legal 40p + 16-workshift + 17-annual-plan + 28-splash + 23-education 6 페이지 일관.

**OQ #2 (status 표시는 화면 모드 자체로 — finding 칩 본 페이지 없음 + KVRow value 좌측 강조 없음):**
> LOCKED (2026-05-25): finding 상태 칩 **본 페이지 없음** (20-legal-findings 의 '미조치/완료' 칩 + borderLeft 2px 분기 패턴이 본 페이지에는 없음 — 단일 finding 상세 페이지 → status 표시는 **화면 모드 자체로 표현**, finding.status === 'open' 이면 Section 3 form / finding.status === 'resolved' 이면 Section 4 결과, mutually exclusive). KVRow 컴포넌트 (line 22~29) 만 사용 — label 12 width 64 flexShrink 0 + children 14 flex 1 lineHeight 1.5 + gap 12 alignItems flex-start. **value 좌측 강조 (borderLeft 등) 없음** — KVRow 단순 패턴 유지. **status- prefix 없음** (`text-status-safe` 같은 패턴 사용 시 W5 verify FAIL, memory `feedback_tailwind_token_class_pattern`).

**OQ #3 (§1.1 9·10·11 fontSize 12 격상 + leading-none):**
> LOCKED (2026-05-25): uploading overlay '업로드 중' 10 (line 232) + 제거 button '✕' 11 (line 231) + canAdd '사진 첨부' 11 (line 237) 모두 → `text-caption font-bold leading-none` (12px) 격상. KVRow label 12 + SectionHeader 12 + 조치 사진 라벨 12 (이미 12) — leading-none 명시. 격상 후 padding 미세 조정 (canAdd 72x72 보존, 제거 button 20x20 보존). 14 (KVRow children + 에러 + CTA button) + 13 (textarea + '사진 없음' + 데스크톱 admin 다운로드 button) + 16 (모바일 헤더 타이틀) + 22 (데스크톱 타이틀) 보존. 20-legal-findings wic + 19-legal 40p + 23-education + 16-workshift + 17-annual-plan OQ 일관 (§1.1 노안 룰 우선 정신).

**OQ #4 (조치 완료 CTA 그라데이션 + admin 다운로드 button 작은 도구 solid 유지):**
> LOCKED (2026-05-25): 조치 완료 CTA (모바일 고정 line 270~275 + 데스크톱 inline line 244~248) = `style={{ background: 'linear-gradient(135deg, #1d4ed8, #0ea5e9)' }}` 인라인 그라데이션 (디자인 강조용, design-system §6.4 CTA 룰 일관 — 메인 액션 단일). 토큰 채택 X (linear-gradient negative gate 의 **T3 만 예외 anchor ≥3**). admin 다운로드 button (작은 도구, 모바일 36x36→44x44 icon + 데스크톱 텍스트 h 36) = solid `bg-surface-sunken border border-border-strong text-text-primary` 유지 또는 icon 색만 (모바일). disabled = `opacity 0.5 cursor not-allowed`. handleResolve memo.trim() 빈 값 → toast.error '조치 내용을 입력하세요' (line 134 verbatim) validation 보존.

**OQ #5 (Lucide ChevronLeft + Download + Loader2 + Camera 교체 + back/admin 다운로드 44x44 격상):**
> LOCKED (2026-05-25): (1) 모바일 헤더 back button — 인라인 SVG ChevronLeft (line 156, path `M15 19l-7-7 7-7` strokeWidth 2 size 20) → Lucide `<ChevronLeft size={20} />` 교체. **back button 36x36 → 44x44 격상** (§1.1 터치 마지노선 44px). w-8 함정 회피 → `w-11 h-11` (44px tailwind 기본 spacing 11) 또는 `w-[44px] h-[44px]` arbitrary. position absolute left 12 유지. (2) 모바일 헤더 admin 다운로드 button — 인라인 SVG Download (line 161, path `M12 5v14m0 0l-6-6m6 6l6-6M5 19h14` size 18) → Lucide `<Download size={18} />` 교체 + **36x36 → 44x44 격상**. position absolute right 12 유지. (3) Spinner div + @keyframes spin (line 41~48 + line 147 외곽 keyframes 중복 정의) → Lucide `<Loader2 className="animate-spin" size={24} />` 교체. Spinner 함수 line 41~48 폐기 + 외곽 인라인 keyframe 정의 (line 147) 도 폐기 가능. (4) canAdd button '📷' 이모지 (line 237 fontSize 22) → Lucide `<Camera size={22} />` 교체 — **19-legal LegalPage 의 FindingDetailPanel 안 '📷' 첨부 button 이모지와 일치 패턴**. import 추가: `import { ChevronLeft, Download, Loader2, Camera } from 'lucide-react'`. W2 sketch + W4 sketch + TSX 변환 양쪽 동일 적용.

### 디자인 토큰 패턴 (wave-1-index.md §3 + planning constraints verbatim)
- `bg-surface-page` (var(--bg) → --surface-page, 페이지 외곽)
- `bg-surface-raised` (var(--bg2) → --surface-raised, 모바일 헤더 OQ #1)
- `bg-surface-sunken` (var(--bg3) → --surface-sunken, textarea + canAdd button + 데스크톱 admin 다운로드 button)
- `border-border-default` (var(--bd) → --border-default, 모바일 헤더 borderBottom + 모바일 고정 CTA borderTop + Section borderBottom + 슬롯 img border)
- `border-border-strong` (var(--bd2) → --border-strong, Spinner border + 데스크톱 admin 다운로드 button border + textarea border + canAdd button border-dashed)
- `text-text-primary` (var(--t1) → --text-primary, 헤더 타이틀 + 데스크톱 타이틀 + KVRow children + textarea color + 데스크톱 admin 다운로드 button color)
- `text-text-secondary` (var(--t2) → --text-secondary, 에러)
- `text-text-tertiary` (var(--t3) → --text-tertiary, KVRow label + SectionHeader + '사진 없음' + 조치 사진 라벨 + canAdd button color)
- `text-danger` / `bg-danger` (var(--danger) → --danger, 제거 button bg)
- `bg-accent text-text-on-accent` (CTA solid 또는 그라데이션 — 조치 완료 CTA OQ #4 LOCKED 그라데이션)
- radius: `rounded-sm` 8/9/10 / `rounded-md` 12

### Negative rule (모든 task verify gate)
- 이모지 0건 (sketch 본문, '📷' Camera 본 페이지 직접 사용 — Lucide Camera 교체 anchor 만 텍스트 인용)
- `linear-gradient` — T1/T2/T4 = 0건 / **T3 만 예외 anchor** (OQ #4 LOCKED 조치 완료 CTA 그라데이션 `linear-gradient(135deg, #1d4ed8, #0ea5e9)` ≥3 박제)
- 9&#183;10&#183;11px fontSize 0건 (text-caption 12 leading-none 마지노선, OQ #3 LOCKED, uploading overlay 10 + 제거 button 11 + canAdd 11 모두 격상)
- `(text|bg|border)-status-` class prefix 0건 (`bg&#8209;status&#8209;safe X`, memory `feedback_tailwind_token_class_pattern`)
- `\b(w|h)-8\b` 0건 (tailwind config w&#8209;8=48 함정, memory `feedback_tailwind_w8_h8_is_48px`)
- 옛 alias 토큰 (`var(--bg)` `var(--bg2)` `var(--bg3)` `var(--bg4)` `var(--bd)` `var(--bd2)` `var(--t1)` `var(--t2)` `var(--t3)` `var(--acl)` `var(--accent)` `var(--safe)` `var(--warn)` `var(--danger)`) 0건 in sketch body (tokens.css 정의 인용 fence 안 예외)

### 평면 폴더 패턴 (13/14/27/16/17/28/23/19/20 9 페이지 일관)
- 21-legal-finding-detail/ 직속에 4 파일 배치
- `sketch/` 서브폴더 절대 금지

### ★ 비즈 anchor 11건 (1 byte 변경 금지, W2~W5 모두 박제)

**[react-query / 비즈 시그니처]**
1. `useQuery({ queryKey: ['legal-finding', id, fid], queryFn: () => legalApi.getFinding(id!, fid!), enabled: !!id && !!fid })` (line 61~65)
2. `useMutation({ mutationFn: async () => { const photoKeys = await resolutionPhotos.uploadAll(); return legalApi.resolveFinding(id!, fid!, { resolution_memo: memo.trim(), resolution_photo_keys: photoKeys.length > 0 ? photoKeys : undefined }) }, ... })` (line 67~87 — **uploadAll 선행 + photoKeys.length > 0 분기 + snake_case payload** 1 byte 변경 금지)
3. `queryClient.invalidateQueries` 4 키 onSuccess — ['legal-finding', id, fid] + ['legal-findings', id] + ['legal-rounds'] + ['legal-round', id] + toast.success '조치 완료' + resolutionPhotos.reset() + navigate(-1) (순서 보존)

**[utils/api.ts legalApi 2종]**
4. legalApi.getFinding(roundId, findingId): Promise<LegalFinding> + legalApi.resolveFinding(roundId, findingId, { resolution_memo?, resolution_photo_keys? }): Promise<void> — **snake_case payload 변경 금지**. (note: legalApi.list / get / getFindings / updateResult / deleteFinding 5종은 LegalFindingDetailPage 미사용 — 19-legal LegalPage / 20-legal-findings LegalFindingsPage 가 각각 사용)

**[useMultiPhotoUpload 직접 사용 시그니처]**
5. resolutionPhotos = useMultiPhotoUpload() (line 59) — 사진 5장 슬롯 풀 컨트롤 (slots / canAdd / openPicker / closePicker / pickCamera / pickAlbum / handleFiles / removeSlot / uploadAll / reset / isUploading / cameraRef / albumRef / showPicker) 모두 직접 호출. **본 페이지가 훅 직접 사용 — FindingFormSheet 우회 X** (20-legal-findings 의 FindingFormSheet 우회 패턴과 다름)

**[비즈 로직 함수 + 분기]**
6. `handleResolve` (line 132~138): memo.trim() 빈 값 → toast.error '조치 내용을 입력하세요' return / 아니면 resolveMutation.mutate() (validation 보존)
7. `finding.status` 분기 (line 218 open / line 253 resolved): **mutually exclusive** — open 이면 Section 3 (textarea + 사진 5장 슬롯 + CTA) / resolved 이면 Section 4 (KVRow 3행 + PhotoGrid). UI/시안에서 분기 변경 금지 (memory `project_inspection_completion_rule` 일반화)
8. admin 다운로드 button 조건부 — `staff?.role === 'admin' && finding` (line 159 모바일 + line 171 데스크톱). staff = useAuthStore(s => s.staff) (line 58, **selector 패턴** — 20-legal-findings 의 getState() 와 다름). assistant: 다운로드 button 미렌더 / 조치 입력/완료/결과는 모두 표시 (memory `project_inspection_completion_rule` 일반화)

**[handleDownload + iOS PWA + animation + 카피]**
9. `handleDownload` (line 89~130): fflate dynamic `import('fflate').zipSync` + buildMetaTxt → 내용.txt always + Promise.allSettled photoKeys/resolutionPhotoKeys + iOS PWA `<a download>` 패턴 (createElement + body.appendChild + click + removeChild + setTimeout(URL.revokeObjectURL, 3000)). **ZIP 파일명 `지적사항_${name}.zip` (line 119) — name = (finding.location ?? '위치없음').replace(/[\/\\:*?"<>|]/g, '_') = location 안전화** (**20-legal-findings 의 round.title 기반과 다름 + fid 기반 아님 — location 기반** + 19-legal LegalPage 의 FindingDetailPanel 단일 다운로드 패턴과 일치). 사진 파일명 `지적사진-${j+1}.jpg` / `조치사진-${j+1}.jpg`. **1 byte 변경 금지** (iOS 안정성 검증된 패턴)
10. `@keyframes spin { to{transform:rotate(360deg)} }` (line 45 Spinner 함수 + line 147 JSX 외곽 중복 정의) — 두 곳 모두 동일 정의 보존 (OQ #5 LOCKED 시 Lucide Loader2 교체 + 양쪽 keyframes 폐기 가능)
11. **toast 카피 5종 verbatim**: success 2 — '조치 완료' (line 80, resolveMutation onSuccess) / '다운로드 완료' (line 124, handleDownload) + error 3 — '조치 처리 실패' (line 85, resolveMutation onError) / '조치 내용을 입력하세요' (line 134, handleResolve validation) / '다운로드 실패' (line 126, handleDownload catch). 추가 verbatim 카피 다수 — 모바일 + 데스크톱 헤더 타이틀 '지적 상세' (line 158, 170) / 에러 단일 문장 '항목을 불러오지 못했습니다. 뒤로 가서 다시 시도하세요.' (line 185, **20-legal-findings 의 '화면을 당겨서 다시 시도하세요' 와 다름**) / SectionHeader 4종 '지적 정보' (line 198) / '지적 사진' (line 209) / '조치 내용' (line 220) / '조치 결과' (line 255) / KVRow label 7종 '지적 내용' / '위치' / '등록일' / '등록자' / '조치일시' / '조치자' / '조치 내용' (line 200~204, 256~260 verbatim) / 위치 fallback '-' (line 202, **20-legal-findings 의 '위치 미지정' 과 다름 — 본 페이지는 '-'**) / 등록자/조치자 fallback `??` 패턴 / '사진 없음' (line 213) / 조치 사진 라벨 '조치 사진 (최대 5장)' (line 223) / textarea placeholder '조치 내용을 입력하세요' (line 221) / canAdd 카피 '사진 첨부' (line 237) / 슬롯 제거 button '✕' (line 231) / 슬롯 업로드 중 '업로드 중' (line 232) / CTA button '조치 완료' / '처리 중...' (isSubmitting) (line 246, 273) / 데스크톱 admin 다운로드 button '다운로드' / '다운로드 중...' (line 173)

### 19/20-legal 차이 5건 sketch 시각 반영 (W2~W4 sketch + W5 checklist 모두 박제)
1. **단일 finding 페이지** — 지적사항 1건의 상세 + 조치 처리 전용 (20-legal-findings LegalFindingsPage 의 다건 목록 + 19-legal LegalPage 의 마스터-디테일 우측 패널과 다름 — 본 페이지는 풀 페이지 단독, 부모 LegalFindingsPage 의 findingCard 클릭 시 navigate(`/legal/${id}/finding/${fid}`) 로 진입)
2. **resolution form 풀 페이지 — 사진 5장 useMultiPhotoUpload 직접 사용** — line 59 `resolutionPhotos = useMultiPhotoUpload()` 본 페이지가 훅 직접 호출 (20-legal-findings 가 FindingFormSheet 우회한 것과 다름). slots / canAdd / pickCamera / pickAlbum / handleFiles / removeSlot / uploadAll / reset / isUploading / cameraRef / albumRef / showPicker / closePicker 모두 직접 사용
3. **조치 완료 button 풀폭 CTA 그라데이션** — 메인 액션 단일 (addButton 류와 같은 §6.4 후보 → OQ #4 LOCKED `linear-gradient(135deg, #1d4ed8, #0ea5e9)`). 모바일 고정 line 270~275 + 데스크톱 inline line 244~248 = 동일 스타일 (width 100% h 48), 위치만 다름. T4 sketch (resolve-form) 에서 그라데이션 ≥3 anchor 박제
4. **admin ZIP 다운로드 단일 finding 기반 — 파일명 location 기반** `지적사항_${name}.zip` (line 119), name = (finding.location ?? '위치없음').replace(/[\/\\:*?"<>|]/g, '_'). **20-legal-findings round.title 기반과 다름 + fid 기반 아님 — name = location 안전화** + 19-legal LegalPage 의 FindingDetailPanel 단일 다운로드 패턴과 일치. T4 sketch 에서 ZIP 패턴 anchor 박제
5. **finding.status open/resolved 분기로 화면 모드 자체가 바뀜** — line 218 open `&&` Section 3 (form) / line 253 resolved `&&` Section 4 (결과). **mutually exclusive** — open && resolved 양쪽 동시 렌더 불가. T4 sketch 4 frame 매트릭스 (open 모바일/데스크톱 + resolved 모바일/데스크톱) 박제. W5 checklist §10 비즈 보존 체크박스에 분기 anchor 명시

### Tailwind cheatsheet (v0.1.1 토큰 → utility class 매핑, 21-legal-finding-detail 적용)
| v0.1.1 토큰 | Tailwind utility | 21-legal-finding-detail 적용 위치 |
|---|---|---|
| `--surface-page` | `bg-surface-page` | LegalFindingDetailPage 외곽 (line 146) + 모바일 고정 CTA bg (line 271) |
| `--surface-raised` | `bg-surface-raised` | 모바일 헤더 (line 149, OQ #1) |
| `--surface-sunken` | `bg-surface-sunken` | textarea (line 221) + canAdd button (line 234) + 데스크톱 admin 다운로드 button (line 172) |
| `--border-default` | `border-border-default` | 모바일 헤더 borderBottom + 모바일 고정 CTA borderTop + Section borderBottom + 슬롯 img border |
| `--border-strong` | `border-border-strong` | Spinner border + 데스크톱 admin 다운로드 button border + textarea border + canAdd button border-dashed |
| `--text-primary` | `text-text-primary` | 헤더 타이틀 + 데스크톱 타이틀 + KVRow children + textarea color + 데스크톱 admin 다운로드 button color |
| `--text-secondary` | `text-text-secondary` | 에러 단일 문장 (line 185) |
| `--text-tertiary` | `text-text-tertiary` | KVRow label + SectionHeader + '사진 없음' + 조치 사진 라벨 + canAdd button color |
| `--danger` | `bg-danger` | 제거 button bg (line 231) |
| `--accent` (CTA gradient) | 인라인 `linear-gradient(135deg, #1d4ed8, #0ea5e9)` | 조치 완료 CTA (모바일 고정 line 270~275 + 데스크톱 inline line 244~248, OQ #4 LOCKED **T3 만 예외 anchor ≥3**) |
| (radius 8/9/10) | `rounded-sm` | textarea + canAdd button + 슬롯 img + 데스크톱 admin 다운로드 button + 제거 button → `rounded-full` |
| (radius 12) | `rounded-md` | CTA button |
| (radius 50%) | `rounded-full` | 제거 button (-6,-6) 20x20 |

### 기타 메모리 룰 inline (W5 §11 박제)
- `feedback_inspection_unresolved_color` — status 표시 화면 모드 자체로 일반화 (칩 없음, finding.status === 'open'/'resolved' 분기 source of truth)
- `project_inspection_completion_rule` — admin role 분기 (selector 패턴) + finding.status 분기 source of truth 일반화
- `feedback_tailwind_token_class_pattern` — status- prefix 없음 + lucide `<Icon size={N} />` prop
- `feedback_tailwind_w8_h8_is_48px` — w&#8209;8/h&#8209;8 = 48px 함정 (back button + admin 다운로드 button 44x44 = `w-11 h-11`, OQ #5 LOCKED 격상)
- `feedback_text_caption_leading_none` — 작은 컨테이너 안 text-caption(12) leading-none 명시 (uploading overlay + 제거 button + canAdd 모두 격상 후 명시)
- `feedback_sketch_realistic_data` — 표시 분기/라벨 룰 코드 그대로
- `feedback_design_changes_ask_first` — 디자인 변경 전 사용자 컨펌
- `feedback_planner_prompt_sketch_verbatim` — sketch CSS verbatim 인용 (추측 토큰명 금지)
- `feedback_tsx_wave_emoji_dot_gap` — sketch 의 이모지 0건 negative gate (Lucide Camera 교체 후 본 페이지 직접 사용 이모지 폐기)
- `feedback_tsx_wave_stat_card_drift` — source outline 패턴 보존 + sketch 새 패턴 누락 방지 (단일 finding 상세 페이지 = 진척률 도넛/통계 카드/카테고리 카드 없음 — 미적용)
- `feedback_subagent_production_deploy_forbidden` — wrangler 명령 절대 X (디자인 wave)
- `feedback_redesign_sketch_rule_enforcement` — §6.2 negative rule (단 결과 status 토큰은 룰 11 예외) + §6.3/§7.1 일관성 강제
- `feedback_cbc7119_design_never_wrangler` — 디자인 작업 wrangler 명령 자체 금지

### T3 만 linear-gradient 예외 anchor (OQ #4 LOCKED)
- T1 sketch (chrome): `linear-gradient` 0건 — 모바일 고정 하단 CTA placeholder (T3 sketch 에서 채움), 또는 solid bg-accent 박제 OK
- T2 sketch (finding-info): `linear-gradient` 0건
- **T3 sketch (resolve-form): `linear-gradient(135deg, #1d4ed8, #0ea5e9)` ≥3 박제** (조치 완료 CTA 모바일 고정 + 데스크톱 inline + admin frame 별 anchor ≥3)
- T4 checklist: code fence 안 인용 ≥1
</rules_verbatim>

<tasks>

<task type="auto">
  <name>Task 1 (W2): LegalFindingDetailPage 모바일 자체 헤더 (Lucide ChevronLeft + 44x44 OQ #5 + 타이틀 '지적 상세' 정중앙 정적 OQ #1 + admin 다운로드 button Lucide Download + 44x44) + 데스크톱 타이틀 영역 (글로벌 chrome 0) + Spinner/에러 4 state sketch 생성 (4 frame)</name>
  <files>cha-bio-safety/docs/redesign-context/21-legal-finding-detail/sketch-wave-2-chrome.html</files>
  <action>
    20-legal-findings/sketch-wave-2-chrome.html + 19-legal/sketch-wave-2-chrome.html 패턴 mirror 로 새 sketch 파일 작성. LegalFindingDetailPage.tsx 모바일 자체 헤더 (line 149~165) + 데스크톱 타이틀 영역 (line 168~177, 글로벌 chrome 0 — App.tsx line 117 정규식 `^\/legal\/.+` 매칭 → showNav=false) + Spinner (line 41~48) + 에러 fallback (line 183~187 단일 문장) + 모바일 고정 하단 CTA placeholder (T3 sketch 에서 채움) + 외곽 (line 146 flex 1 column overflow hidden) + 인라인 keyframes spin (line 147) 시각화. **OQ #1 LOCKED (모바일 헤더 `bg-surface-raised border-b border-border-default` + 타이틀 '지적 상세' 정중앙 정적)** + **OQ #5 LOCKED (Lucide ChevronLeft size={20} + Download size={18} + Loader2 size={24} 교체 + back button + admin 다운로드 button 44x44 격상)** 2건이 본 wave 의 핵심.

    **구조 (20-legal-findings wic W2 mirror, 4 frame):**
    - `<!DOCTYPE html>` + `<html lang="ko">` + `<head><meta charset="utf-8"><title>redesign/21-legal-finding-detail — W2 chrome + empty/loading/error</title>` + `<style>` 블록
    - `<style>` 안: v0.1.1 tokens.css 의 dark+light 양쪽 정의 verbatim 인용 + typography.css 정의 verbatim 인용 + `@keyframes spin { to{transform:rotate(360deg)} }` 박제 (line 45 + line 147 중복 정의 anchor)
    - `<body>` 안 4 frame side-by-side (state 매트릭스):

      | frame | viewport | 시각화 |
      |---|---|---|
      | 1 | 모바일 다크 393×800 콘텐츠 빈 상태 (status open finding placeholder) | `data-theme="dark"` + bg-surface-page 외곽 + 자체 헤더 (h 48, bg-surface-raised + border-b border-border-default, OQ #1) + Lucide ChevronLeft size={20} back 44x44 (OQ #5, w-11 h-11, position absolute left 12) + 타이틀 '지적 상세' 16/700 정중앙 정적 (line 158 verbatim) + Lucide Download size={18} admin 다운로드 button 44x44 (OQ #5, position absolute right 12, staff?.role === 'admin' && finding 조건부 line 159) + 콘텐츠 영역 placeholder + 모바일 고정 하단 CTA placeholder (T3 에서 채움) |
      | 2 | 모바일 다크 393×800 로딩 상태 | `data-theme="dark"` + 동일 헤더 (admin 다운로드 button hidden — finding 미로딩이라 조건 미충족) + Spinner (Lucide Loader2 animate-spin size={24} OQ #5 교체) flex center flex 1 |
      | 3 | 데스크톱 다크 1440×900 콘텐츠 + 빈 상태 (글로벌 chrome 0) | `data-theme="dark"` + **글로벌 AppHeader 표시 X + 사이드바 표시 X** (App.tsx line 117 정규식 박제 fence) + 데스크톱 타이틀 영역 (padding '24px 32px 12px' flex space-between) + 좌측 '지적 상세' 22/800 (line 170 verbatim 정적) + 우측 admin 다운로드 button 텍스트형 (13/700 h 36 bg-surface-sunken border border-border-strong text-text-primary padding '0 16px' rounded-sm) — 텍스트 '다운로드' (line 173 verbatim) + 콘텐츠 영역 maxWidth 700 중앙 정렬 (line 195) |
      | 4 | 모바일 라이트 393×800 오류 상태 | `data-theme="light"` + 동일 헤더 + 단일 문장 오류 '항목을 불러오지 못했습니다. 뒤로 가서 다시 시도하세요.' (line 185 verbatim, 14 text-text-secondary, **20-legal-findings 의 '화면을 당겨서 다시 시도하세요' 와 다름**) + flex 1 alignItems center padding '0 24px' |

    **모바일 헤더 마크업 룰 (frame 1/2/4 공통 — OQ #1 + OQ #5):**
    ```html
    <!-- 모바일 자체 헤더 (OQ #1 LOCKED) -->
    <div class="bg-surface-raised border-b border-border-default" style="height:48px;display:flex;align-items:center;justify-content:center;position:relative;flex-shrink:0;">
      <!-- back button (OQ #5 LOCKED: Lucide ChevronLeft size={20} + 44x44 격상, w-11 h-11) -->
      <button class="text-text-primary" style="position:absolute;left:12px;width:44px;height:44px;background:none;border:none;cursor:pointer;display:flex;align-items:center;justify-content:center;" aria-label="Lucide ChevronLeft size={20}, back button 44x44 (w-11 h-11)">
        <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="15 18 9 12 15 6" /></svg>
      </button>
      <!-- 타이틀 '지적 상세' verbatim 정중앙 정적 (line 158, 20-legal-findings 동적 분기와 다름) -->
      <span class="text-body font-bold text-text-primary">지적 상세</span>
      <!-- admin 다운로드 button (OQ #5 LOCKED: Lucide Download size={18} + 44x44 격상, staff?.role === 'admin' && finding 조건부 line 159) -->
      <button class="text-text-primary" style="position:absolute;right:12px;width:44px;height:44px;background:none;border:none;cursor:pointer;display:flex;align-items:center;justify-content:center;" aria-label="Lucide Download size={18}, admin 다운로드 button 44x44 (w-11 h-11)">
        <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/><polyline points="7 10 12 15 17 10"/><line x1="12" y1="15" x2="12" y2="3"/></svg>
      </button>
    </div>
    ```

    **글로벌 chrome 0건 anchor (frame 3 — App.tsx line 117 정규식 verbatim):**
    ```html
    <!-- 데스크톱 외곽: 글로벌 AppHeader 표시 X + 사이드바 표시 X -->
    <!-- App.tsx line 117 verbatim anchor: !location.pathname.match(/^\/legal\/.+/) → showNav=false -->
    <!-- 자체 타이틀 (line 168~177) 만 chrome 의 유일한 외곽 -->
    <div class="bg-surface-page" style="display:flex;flex-direction:column;height:100%;">
      <!-- 데스크톱 타이틀 영역 (line 168~177, padding '24px 32px 12px' flex space-between) -->
      <div style="padding:24px 32px 12px;display:flex;align-items:center;justify-content:space-between;flex-shrink:0;">
        <!-- 좌측 타이틀 '지적 상세' 22/800 verbatim 정적 (line 170) -->
        <span class="text-headline font-extrabold text-text-primary" style="font-size:22px;line-height:1.2;">지적 상세</span>
        <!-- 우측 admin 다운로드 button 텍스트형 (line 171~175, 데스크톱 13/700 h 36) -->
        <button class="bg-surface-sunken border border-border-strong text-text-primary text-label font-bold rounded-sm" style="height:36px;padding:0 16px;cursor:pointer;flex-shrink:0;">다운로드</button>
      </div>
      <!-- 콘텐츠 영역 (maxWidth 700 데스크톱 중앙, 20-legal-findings 800 보다 좁음) -->
      <div style="flex:1;overflow-y:auto;padding:20px 32px;max-width:700px;margin:0 auto;width:100%;">
        <!-- placeholder (T2 + T3 에서 채움) -->
      </div>
    </div>
    ```

    **Spinner 마크업 룰 (frame 2 로딩 — OQ #5 Lucide Loader2 교체):**
    ```html
    <!-- Spinner (line 41~48 div 폐기 → Lucide Loader2 animate-spin size={24} 교체) -->
    <div style="display:flex;align-items:center;justify-content:center;flex:1;" aria-label="Lucide Loader2 animate-spin size={24}">
      <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" style="animation:spin 1s linear infinite;color:var(--acl);"><path d="M21 12a9 9 0 1 1-6.219-8.56"/></svg>
    </div>
    ```

    **오류 상태 마크업 룰 (frame 4 — line 183~187 verbatim, 단일 문장, **20-legal-findings 와 다름**):**
    ```html
    <div style="display:flex;align-items:center;justify-content:center;flex:1;padding:0 24px;text-align:center;">
      <div class="text-body-sm text-text-secondary">항목을 불러오지 못했습니다. 뒤로 가서 다시 시도하세요.</div>
    </div>
    ```

    **카피 verbatim (memory `feedback_sketch_realistic_data`):**
    - 모바일 헤더 타이틀: '지적 상세' (line 158 verbatim, **정적 — 20-legal-findings 동적 분기와 다름**)
    - 데스크톱 타이틀: '지적 상세' (line 170 verbatim 정적, 22/800)
    - 모바일 admin 다운로드 button: Lucide Download size={18} icon only (line 161 SVG)
    - 데스크톱 admin 다운로드 button: '다운로드' / '다운로드 중...' (line 173 verbatim)
    - 모바일 오류 단일 문장: '항목을 불러오지 못했습니다. 뒤로 가서 다시 시도하세요.' (line 185 verbatim, **20-legal-findings 의 '화면을 당겨서 다시 시도하세요' 와 다름**)

    **rules 박스 (sketch 하단):**
    - LOCKED 결정: OQ #1 (모바일 자체 헤더 `bg-surface-raised border-b border-border-default` + 타이틀 '지적 상세' 정중앙 정적, 옛 `rgba(22,27,34,0.97)` 인라인 폐기) + OQ #5 (Lucide ChevronLeft size={20} + Download size={18} + Loader2 size={24} 교체 + back button + admin 다운로드 button 44x44 격상 = w-11 h-11) verbatim 인용 1 fence
    - **비즈 anchor 11건 박제 fence** (full list — rules_verbatim 박제 list 그대로 인용)
    - **19/20-legal 차이 5건 fence** (단일 finding / form 풀 페이지 / 사진 5장 직접 / ZIP location 기반 / status open 분기)
    - **App.tsx line 117 정규식 anchor fence**: `!location.pathname.match(/^\/legal\/.+/)` → showNav=false 박제
    - 토큰 매핑: `var(--bg) → bg-surface-page` / `var(--bg2) → bg-surface-raised` (모바일 헤더 OQ #1) / `var(--bg3) → bg-surface-sunken` (데스크톱 admin 다운로드 button) / `var(--bd) → border-border-default` (모바일 헤더 borderBottom) / `var(--bd2) → border-border-strong` (Spinner border + 데스크톱 admin 다운로드 button border) / `var(--t1) → text-text-primary` (헤더 타이틀 + 데스크톱 타이틀 + admin 다운로드 button color) / `var(--t2) → text-text-secondary` (에러 단일 문장) / `var(--acl) → bg-accent` (Spinner color)
    - negative gate 자체매칭 회피: "이모지 0 / linear-gradient 0 / 9·10·11px 0 / status- prefix 0 / w-8 h-8 0 / 옛 alias 토큰 0" 메타 줄은 `<code>` fence 안 또는 HTML entity escape

    **commit 명령 (executor 가 실행 후 commit hash + message 보고):**
    ```
    git add cha-bio-safety/docs/redesign-context/21-legal-finding-detail/sketch-wave-2-chrome.html
    git commit -m "feat(quick-260525-4of): redesign/21-legal-finding-detail sketch wave 2 (chrome — 모바일 헤더 bg-surface-raised + 타이틀 '지적 상세' 정중앙 정적 + 데스크톱 타이틀 글로벌 chrome 0 + Lucide ChevronLeft/Download/Loader2 44x44 격상 + 비즈 anchor 박제)"
    ```
  </action>
  <verify>
    <automated>
test -f cha-bio-safety/docs/redesign-context/21-legal-finding-detail/sketch-wave-2-chrome.html \
  && wc -l cha-bio-safety/docs/redesign-context/21-legal-finding-detail/sketch-wave-2-chrome.html | awk '{ if ($1 < 200) { print "FAIL: too short"; exit 1 } else print "PASS lines:", $1 }' \
  && grep -c 'data-theme="dark"' cha-bio-safety/docs/redesign-context/21-legal-finding-detail/sketch-wave-2-chrome.html | awk '{ if ($1 < 3) { print "FAIL: dark frame <3"; exit 1 } else print "PASS dark frame:", $1 }' \
  && grep -c 'data-theme="light"' cha-bio-safety/docs/redesign-context/21-legal-finding-detail/sketch-wave-2-chrome.html | awk '{ if ($1 < 1) { print "FAIL: light frame <1"; exit 1 } else print "PASS light frame:", $1 }' \
  && grep -c 'bg-surface-page' cha-bio-safety/docs/redesign-context/21-legal-finding-detail/sketch-wave-2-chrome.html | awk '{ if ($1 < 4) { print "FAIL: bg-surface-page <4"; exit 1 } else print "PASS bg-surface-page:", $1 }' \
  && grep -c 'bg-surface-raised' cha-bio-safety/docs/redesign-context/21-legal-finding-detail/sketch-wave-2-chrome.html | awk '{ if ($1 < 3) { print "FAIL: bg-surface-raised <3 (OQ #1 모바일 헤더)"; exit 1 } else print "PASS bg-surface-raised:", $1 }' \
  && grep -c 'border-b border-border-default' cha-bio-safety/docs/redesign-context/21-legal-finding-detail/sketch-wave-2-chrome.html | awk '{ if ($1 < 3) { print "FAIL: border-b border-border-default <3 (OQ #1)"; exit 1 } else print "PASS border-b:", $1 }' \
  && grep -cE 'Lucide ChevronLeft|<ChevronLeft|ChevronLeft size=' cha-bio-safety/docs/redesign-context/21-legal-finding-detail/sketch-wave-2-chrome.html | awk '{ if ($1 < 3) { print "FAIL: Lucide ChevronLeft anchor <3 (OQ #5)"; exit 1 } else print "PASS Lucide ChevronLeft anchor:", $1 }' \
  && grep -cE 'Lucide Download|<Download|Download size=' cha-bio-safety/docs/redesign-context/21-legal-finding-detail/sketch-wave-2-chrome.html | awk '{ if ($1 < 2) { print "FAIL: Lucide Download anchor <2 (OQ #5)"; exit 1 } else print "PASS Lucide Download anchor:", $1 }' \
  && grep -cE 'w-11 h-11|w-\[44px\] h-\[44px\]|width:44px' cha-bio-safety/docs/redesign-context/21-legal-finding-detail/sketch-wave-2-chrome.html | awk '{ if ($1 < 3) { print "FAIL: back/admin button 44x44 anchor <3 (OQ #5)"; exit 1 } else print "PASS 44x44 anchor:", $1 }' \
  && grep -cE 'Loader2|loader2' cha-bio-safety/docs/redesign-context/21-legal-finding-detail/sketch-wave-2-chrome.html | awk '{ if ($1 < 1) { print "FAIL: Lucide Loader2 anchor <1 (OQ #5)"; exit 1 } else print "PASS Loader2 anchor:", $1 }' \
  && grep -c '지적 상세' cha-bio-safety/docs/redesign-context/21-legal-finding-detail/sketch-wave-2-chrome.html | awk '{ if ($1 < 2) { print "FAIL: 타이틀 정적 지적 상세 <2 (모바일 line 158 + 데스크톱 line 170)"; exit 1 } else print "PASS 지적 상세:", $1 }' \
  && grep -c '항목을 불러오지 못했습니다. 뒤로 가서 다시 시도하세요' cha-bio-safety/docs/redesign-context/21-legal-finding-detail/sketch-wave-2-chrome.html | awk '{ if ($1 < 1) { print "FAIL: 오류 단일 문장 missing (20-legal-findings 와 다름)"; exit 1 } else print "PASS 오류 단일 문장:", $1 }' \
  && grep -cE '다운로드|다운로드 중' cha-bio-safety/docs/redesign-context/21-legal-finding-detail/sketch-wave-2-chrome.html | awk '{ if ($1 < 1) { print "FAIL: admin 다운로드 button 카피 missing"; exit 1 } else print "PASS 다운로드 카피:", $1 }' \
  && grep -cE '@keyframes spin' cha-bio-safety/docs/redesign-context/21-legal-finding-detail/sketch-wave-2-chrome.html | awk '{ if ($1 < 1) { print "FAIL: spin keyframes missing"; exit 1 } else print "PASS spin keyframes:", $1 }' \
  && grep -cE 'legalApi|useQuery|useMutation|useMultiPhotoUpload|handleResolve|handleDownload|finding\.status|role.*admin|toast|KVRow|SectionHeader' cha-bio-safety/docs/redesign-context/21-legal-finding-detail/sketch-wave-2-chrome.html | awk '{ if ($1 < 6) { print "FAIL: 비즈 anchor 11건 박제 hit <6"; exit 1 } else print "PASS 비즈 anchor hits:", $1 }' \
  && grep -cE 'showNav=false|/\\^\\\\/legal' cha-bio-safety/docs/redesign-context/21-legal-finding-detail/sketch-wave-2-chrome.html | awk '{ if ($1 < 1) { print "FAIL: App.tsx line 117 정규식 anchor missing (글로벌 chrome 0건)"; exit 1 } else print "PASS chrome 0 anchor:", $1 }' \
  && grep -cE 'linear-gradient' cha-bio-safety/docs/redesign-context/21-legal-finding-detail/sketch-wave-2-chrome.html | awk '{ if ($1 > 0) { print "FAIL: linear-gradient found (T3 만 예외)"; exit 1 } else print "PASS no gradient" }' \
  && grep -cE '(text|bg|border)-status-(safe|fire|warning|danger|caution)' cha-bio-safety/docs/redesign-context/21-legal-finding-detail/sketch-wave-2-chrome.html | awk '{ if ($1 > 0) { print "FAIL: status- prefix found"; exit 1 } else print "PASS no status- prefix" }' \
  && grep -v '^<!--' cha-bio-safety/docs/redesign-context/21-legal-finding-detail/sketch-wave-2-chrome.html | grep -v '^#' | grep -cE 'font-size:[[:space:]]*(9|10|11)[^0-9]|fontSize:[[:space:]]*(9|10|11)[^0-9]' | awk '{ if ($1 > 0) { print "FAIL: 9/10/11 px found"; exit 1 } else print "PASS no 9-11px" }' \
  && grep -cE '\bw-8\b|\bh-8\b' cha-bio-safety/docs/redesign-context/21-legal-finding-detail/sketch-wave-2-chrome.html | awk '{ if ($1 > 0) { print "FAIL: w-8/h-8 found"; exit 1 } else print "PASS no w-8 h-8" }' \
  && grep -cE 'OQ #1|OQ #5' cha-bio-safety/docs/redesign-context/21-legal-finding-detail/sketch-wave-2-chrome.html | awk '{ if ($1 < 2) { print "FAIL: OQ #1/#5 citation <2"; exit 1 } else print "PASS OQ citations:", $1 }' \
  && git log --oneline -1 | grep -q '260525-4of.*wave 2' && echo "PASS commit T1"
    </automated>
  </verify>
  <done>
    - sketch-wave-2-chrome.html 파일 존재 + ≥200 lines (≥320 권장)
    - data-theme="dark" ≥3 / data-theme="light" ≥1 (4 frame)
    - bg-surface-page ≥4 / bg-surface-raised (모바일 헤더) ≥3 (OQ #1) / border-b border-border-default ≥3 (OQ #1)
    - Lucide ChevronLeft anchor ≥3 (OQ #5) / Lucide Download anchor ≥2 (OQ #5) / back/admin button 44x44 anchor ≥3 (OQ #5) / Loader2 anchor ≥1 (OQ #5)
    - 타이틀 정적 '지적 상세' ≥2 (모바일 line 158 + 데스크톱 line 170)
    - 오류 단일 문장 '항목을 불러오지 못했습니다. 뒤로 가서 다시 시도하세요' ≥1 (20-legal-findings 와 다름)
    - admin 다운로드 button 카피 '다운로드' ≥1
    - @keyframes spin anchor ≥1 / 비즈 anchor 11건 박제 hit ≥6
    - 글로벌 chrome 0건 anchor (App.tsx line 117 정규식) ≥1
    - linear-gradient 0건 (T3 만 예외) / status- prefix 0건 / 9·10·11px 0건 / w-8 h-8 0건
    - OQ #1 + OQ #5 인용 ≥2건
    - atomic commit `feat(quick-260525-4of): redesign/21-legal-finding-detail sketch wave 2 (...)` 1개
  </done>
</task>


<task type="auto">
  <name>Task 2 (W3): LegalFindingDetailPage finding 정보 sketch (Section 1 KVRow 4행 + Section 2 PhotoGrid/사진 없음 + SectionHeader + status 칩 없음 OQ #2, 4 frame)</name>
  <files>cha-bio-safety/docs/redesign-context/21-legal-finding-detail/sketch-wave-3-finding-info.html</files>
  <action>
    20-legal-findings/sketch-wave-3-finding-list.html 패턴 mirror (단, 본 페이지는 finding 카드 목록 아닌 단일 finding 정보 표시) 로 새 sketch 파일 작성. LegalFindingDetailPage.tsx Section 1 지적 정보 (line 197~205, KVRow 4행 — 지적 내용 / 위치 / 등록일 / 등록자) + Section 2 지적 사진 (line 208~215, PhotoGrid 또는 '사진 없음') + KVRow 컴포넌트 (line 22~29) + SectionHeader 컴포넌트 (line 32~38) + fmtDate (line 15~19, **분까지 표시** zero-padded) + 콘텐츠 영역 외곽 (line 190~195, paddingBottom 분기 + maxWidth 700 데스크톱) 시각화. **OQ #2 LOCKED (finding 칩 본 페이지 없음 + KVRow value 좌측 강조 없음, status 표시는 화면 모드 자체로)** + **OQ #3 LOCKED (§1.1 12 격상 + leading-none — KVRow label + SectionHeader)** 2건이 본 wave 의 핵심.

    **구조 (20-legal-findings wic W3 mirror, 4 frame finding 정보 + 사진 매트릭스):**
    - `<!DOCTYPE html>` + `<head><title>redesign/21-legal-finding-detail — W3 finding info + KVRow + PhotoGrid</title>` + `<style>` (W2 와 동일)
    - `<body>` 안 4 frame side-by-side:

      | frame | viewport | 시각화 |
      |---|---|---|
      | 1 | 모바일 다크 393×800 finding 정보 + 사진 0건 | `data-theme="dark"` + 모바일 헤더 (W2 mirror) + Section 1 (sectionPad '20px 16px' borderBottom + SectionHeader '지적 정보' + KVRow 4행: '지적 내용' '계단실 비상조명등 점등불량' / '위치' '3F 비상계단실' / '등록일' '2026.03.15 14:32' / '등록자' '윤종엽') + Section 2 (sectionPad '20px 16px' borderBottom + SectionHeader '지적 사진' + '사진 없음' line 213 verbatim 13 text-text-tertiary marginTop 8) |
      | 2 | 데스크톱 다크 1440×900 finding 정보 + 사진 3개 | `data-theme="dark"` + 데스크톱 타이틀 + maxWidth 700 중앙 + Section 1 (sectionPad '20px 32px' borderBottom + KVRow 4행 동일) + Section 2 (sectionPad '20px 32px' + PhotoGrid photoUrls 3개 marginTop 8) |
      | 3 | 모바일 라이트 393×800 finding 정보 + 위치 fallback '-' | `data-theme="light"` + 동일 chrome + Section 1 (KVRow '위치' fallback **'-'** line 202 verbatim, **20-legal-findings 의 '위치 미지정' 과 다름**) + Section 2 (사진 0건 '사진 없음') |
      | 4 | 데스크톱 라이트 1440×900 finding 정보 + 사진 5개 | `data-theme="light"` + 데스크톱 타이틀 + maxWidth 700 + Section 1 (KVRow 4행 + 등록일 fmtDate **분까지** '2026.03.15 14:32' 분 표시) + Section 2 (PhotoGrid photoUrls 5개) |

    **KVRow 마크업 룰 (line 22~29 verbatim, OQ #3 격상):**
    ```html
    <!-- KVRow 컴포넌트 (line 22~29 verbatim — label 12 width 64 flexShrink 0 + children 14 flex 1 lineHeight 1.5) -->
    <div style="display:flex;gap:12px;align-items:flex-start;">
      <span class="text-caption font-bold leading-none text-text-tertiary" style="width:64px;flex-shrink:0;">지적 내용</span>
      <span class="text-body-sm text-text-primary" style="flex:1;line-height:1.5;white-space:pre-wrap;">계단실 비상조명등 점등불량</span>
    </div>
    ```

    **SectionHeader 마크업 룰 (line 32~38 verbatim, OQ #3 격상):**
    ```html
    <!-- SectionHeader 컴포넌트 (line 32~38 verbatim — 12/700 var(--t3) marginBottom 10) -->
    <div class="text-caption font-bold leading-none text-text-tertiary" style="margin-bottom:10px;">지적 정보</div>
    ```

    **Section 1 + Section 2 마크업 룰 (line 197~215 verbatim):**
    ```html
    <!-- Section 1: 지적 정보 (line 197~205) -->
    <div style="padding:20px 16px;border-bottom:1px solid var(--bd);" class="border-b border-border-default">
      <div class="text-caption font-bold leading-none text-text-tertiary" style="margin-bottom:10px;">지적 정보</div>
      <div style="display:flex;flex-direction:column;gap:8px;">
        <!-- KVRow 4행 verbatim -->
        <div style="display:flex;gap:12px;align-items:flex-start;">
          <span class="text-caption font-bold leading-none text-text-tertiary" style="width:64px;flex-shrink:0;">지적 내용</span>
          <span class="text-body-sm text-text-primary" style="flex:1;line-height:1.5;white-space:pre-wrap;">계단실 비상조명등 점등불량</span>
        </div>
        <div style="display:flex;gap:12px;align-items:flex-start;">
          <span class="text-caption font-bold leading-none text-text-tertiary" style="width:64px;flex-shrink:0;">위치</span>
          <span class="text-body-sm text-text-primary" style="flex:1;line-height:1.5;">3F 비상계단실</span>
        </div>
        <div style="display:flex;gap:12px;align-items:flex-start;">
          <span class="text-caption font-bold leading-none text-text-tertiary" style="width:64px;flex-shrink:0;">등록일</span>
          <span class="text-body-sm text-text-primary" style="flex:1;line-height:1.5;">2026.03.15 14:32</span>
        </div>
        <div style="display:flex;gap:12px;align-items:flex-start;">
          <span class="text-caption font-bold leading-none text-text-tertiary" style="width:64px;flex-shrink:0;">등록자</span>
          <span class="text-body-sm text-text-primary" style="flex:1;line-height:1.5;">윤종엽</span>
        </div>
      </div>
    </div>

    <!-- Section 2: 지적 사진 (line 208~215) — PhotoGrid 또는 '사진 없음' -->
    <div style="padding:20px 16px;border-bottom:1px solid var(--bd);" class="border-b border-border-default">
      <div class="text-caption font-bold leading-none text-text-tertiary" style="margin-bottom:10px;">지적 사진</div>
      <!-- '사진 없음' (line 213 verbatim) -->
      <div class="text-label text-text-tertiary" style="margin-top:8px;">사진 없음</div>
      <!-- 또는 PhotoGrid photoUrls={photoKeys.map(k => '/api/uploads/' + k)} marginTop 8 (다른 frame) -->
    </div>
    ```

    **카피 + 데이터 verbatim (memory `feedback_sketch_realistic_data`):**
    - SectionHeader verbatim: '지적 정보' (line 198) / '지적 사진' (line 209)
    - KVRow label verbatim (line 200~204): '지적 내용' / '위치' / '등록일' / '등록자'
    - 위치 fallback: `location ?? '-'` (line 202 verbatim, **20-legal-findings 의 '위치 미지정' 과 다름 — 본 페이지는 '-'**)
    - 등록일 fmtDate: `${y}.${m}.${d} ${HH}:${mm}` zero-padded (**분까지 표시** — 20-legal-findings 의 일까지만 표시와 다름)
    - 등록자 fallback: `createdByName ?? createdBy` (line 204 verbatim)
    - description: whiteSpace pre-wrap (line 201 verbatim)
    - '사진 없음' (line 213 verbatim, 13 text-text-tertiary marginTop 8)
    - description 예시: '계단실 비상조명등 점등불량' / '소화기 압력불량 (재충전)' / '방화셔터 동작불량'
    - 위치 예시: '3F 비상계단실' / '7F 복도' / **'-' (fallback)**
    - **finding 상태 칩 본 페이지 없음** (OQ #2 LOCKED — 20-legal-findings 의 '미조치/완료' 칩 + borderLeft 2px 패턴 본 페이지 미사용)

    **rules 박스:**
    - LOCKED 결정: OQ #2 (finding 칩 본 페이지 없음 + KVRow value 좌측 강조 없음 — status 표시는 화면 모드 자체로, finding.status === 'open'/'resolved' 분기 source of truth + KVRow 컴포넌트 단순 패턴 유지 + status- prefix 없음) + OQ #3 (§1.1 12 격상 — KVRow label 12 + SectionHeader 12 leading-none 명시) verbatim 인용 1 fence
    - **비즈 anchor 11건 박제 fence** (W2 와 동일 list) + KVRow + SectionHeader 컴포넌트 verbatim + fmtDate 분까지 표시 anchor
    - **19/20-legal 차이 5건 fence** (단일 finding 페이지 + 위치 fallback '-' emphasis)
    - 토큰 매핑: Section borderBottom `var(--bd) → border-b border-border-default` / SectionHeader color `var(--t3) → text-text-tertiary` / KVRow label color `var(--t3) → text-text-tertiary` / KVRow children color `var(--t1) → text-text-primary` / '사진 없음' color `var(--t3) → text-text-tertiary`
    - negative gate 자체매칭 회피: HTML entity escape

    **commit 명령:**
    ```
    git add cha-bio-safety/docs/redesign-context/21-legal-finding-detail/sketch-wave-3-finding-info.html
    git commit -m "feat(quick-260525-4of): redesign/21-legal-finding-detail sketch wave 3 (finding-info — Section 1 KVRow 4행 + Section 2 PhotoGrid/사진 없음 + status 칩 없음 OQ #2 + fmtDate 분까지 + 위치 fallback '-' + 비즈 anchor 박제)"
    ```
  </action>
  <verify>
    <automated>
test -f cha-bio-safety/docs/redesign-context/21-legal-finding-detail/sketch-wave-3-finding-info.html \
  && wc -l cha-bio-safety/docs/redesign-context/21-legal-finding-detail/sketch-wave-3-finding-info.html | awk '{ if ($1 < 200) { print "FAIL: too short"; exit 1 } else print "PASS lines:", $1 }' \
  && grep -c 'data-theme="dark"' cha-bio-safety/docs/redesign-context/21-legal-finding-detail/sketch-wave-3-finding-info.html | awk '{ if ($1 < 2) { print "FAIL: dark frame <2"; exit 1 } else print "PASS dark frame:", $1 }' \
  && grep -c 'data-theme="light"' cha-bio-safety/docs/redesign-context/21-legal-finding-detail/sketch-wave-3-finding-info.html | awk '{ if ($1 < 2) { print "FAIL: light frame <2"; exit 1 } else print "PASS light frame:", $1 }' \
  && grep -c '지적 정보' cha-bio-safety/docs/redesign-context/21-legal-finding-detail/sketch-wave-3-finding-info.html | awk '{ if ($1 < 4) { print "FAIL: SectionHeader 지적 정보 <4 (4 frame)"; exit 1 } else print "PASS 지적 정보:", $1 }' \
  && grep -c '지적 사진' cha-bio-safety/docs/redesign-context/21-legal-finding-detail/sketch-wave-3-finding-info.html | awk '{ if ($1 < 4) { print "FAIL: SectionHeader 지적 사진 <4 (4 frame)"; exit 1 } else print "PASS 지적 사진:", $1 }' \
  && grep -c '지적 내용' cha-bio-safety/docs/redesign-context/21-legal-finding-detail/sketch-wave-3-finding-info.html | awk '{ if ($1 < 4) { print "FAIL: KVRow 지적 내용 label <4"; exit 1 } else print "PASS 지적 내용 label:", $1 }' \
  && grep -cE '위치|등록일|등록자' cha-bio-safety/docs/redesign-context/21-legal-finding-detail/sketch-wave-3-finding-info.html | awk '{ if ($1 < 12) { print "FAIL: KVRow 위치/등록일/등록자 label <12 (4 frame x 3)"; exit 1 } else print "PASS KVRow labels:", $1 }' \
  && grep -c '사진 없음' cha-bio-safety/docs/redesign-context/21-legal-finding-detail/sketch-wave-3-finding-info.html | awk '{ if ($1 < 1) { print "FAIL: 사진 없음 line 213 missing"; exit 1 } else print "PASS 사진 없음:", $1 }' \
  && grep -cE 'KVRow|key-value' cha-bio-safety/docs/redesign-context/21-legal-finding-detail/sketch-wave-3-finding-info.html | awk '{ if ($1 < 1) { print "FAIL: KVRow 컴포넌트 anchor missing"; exit 1 } else print "PASS KVRow anchor:", $1 }' \
  && grep -cE 'SectionHeader' cha-bio-safety/docs/redesign-context/21-legal-finding-detail/sketch-wave-3-finding-info.html | awk '{ if ($1 < 1) { print "FAIL: SectionHeader 컴포넌트 anchor missing"; exit 1 } else print "PASS SectionHeader anchor:", $1 }' \
  && grep -cE 'width:64px|width:\[64' cha-bio-safety/docs/redesign-context/21-legal-finding-detail/sketch-wave-3-finding-info.html | awk '{ if ($1 < 4) { print "FAIL: KVRow label width 64 anchor <4"; exit 1 } else print "PASS label width 64:", $1 }' \
  && grep -cE 'border-b border-border-default' cha-bio-safety/docs/redesign-context/21-legal-finding-detail/sketch-wave-3-finding-info.html | awk '{ if ($1 < 4) { print "FAIL: Section borderBottom <4 (Section 1+2 x dark+light)"; exit 1 } else print "PASS Section border:", $1 }' \
  && grep -c 'text-caption' cha-bio-safety/docs/redesign-context/21-legal-finding-detail/sketch-wave-3-finding-info.html | awk '{ if ($1 < 8) { print "FAIL: text-caption <8 (OQ #3 KVRow label + SectionHeader)"; exit 1 } else print "PASS text-caption:", $1 }' \
  && grep -c 'leading-none' cha-bio-safety/docs/redesign-context/21-legal-finding-detail/sketch-wave-3-finding-info.html | awk '{ if ($1 < 8) { print "FAIL: leading-none <8 (OQ #3)"; exit 1 } else print "PASS leading-none:", $1 }' \
  && grep -cE 'whiteSpace.*pre-wrap|white-space:\s*pre-wrap' cha-bio-safety/docs/redesign-context/21-legal-finding-detail/sketch-wave-3-finding-info.html | awk '{ if ($1 < 1) { print "FAIL: description whiteSpace pre-wrap (line 201) missing"; exit 1 } else print "PASS pre-wrap:", $1 }' \
  && grep -cE 'fmtDate|\\$\\{y\\}|HH:mm|14:32' cha-bio-safety/docs/redesign-context/21-legal-finding-detail/sketch-wave-3-finding-info.html | awk '{ if ($1 < 1) { print "FAIL: fmtDate 분까지 표시 anchor missing (20-legal-findings 일까지와 다름)"; exit 1 } else print "PASS fmtDate anchor:", $1 }' \
  && grep -cE 'legalApi|useQuery|useMutation|useMultiPhotoUpload|handleResolve|handleDownload|finding\.status|role.*admin|toast|KVRow|SectionHeader' cha-bio-safety/docs/redesign-context/21-legal-finding-detail/sketch-wave-3-finding-info.html | awk '{ if ($1 < 6) { print "FAIL: 비즈 anchor 11건 박제 hit <6"; exit 1 } else print "PASS 비즈 anchor hits:", $1 }' \
  && grep -cE '미조치|완료.*chip|border-l-2|finding.*칩' cha-bio-safety/docs/redesign-context/21-legal-finding-detail/sketch-wave-3-finding-info.html | awk '{ if ($1 > 2) { print "FAIL: finding 칩/borderLeft 본 페이지 없음 (OQ #2 위반)"; exit 1 } else print "PASS no finding 칩 (OQ #2)" }' \
  && grep -cE 'linear-gradient' cha-bio-safety/docs/redesign-context/21-legal-finding-detail/sketch-wave-3-finding-info.html | awk '{ if ($1 > 0) { print "FAIL: linear-gradient found (T3 만 예외)"; exit 1 } else print "PASS no gradient" }' \
  && grep -cE '(text|bg|border)-status-(safe|fire|warning|danger|caution)' cha-bio-safety/docs/redesign-context/21-legal-finding-detail/sketch-wave-3-finding-info.html | awk '{ if ($1 > 0) { print "FAIL: status- prefix found (OQ #2 위반)"; exit 1 } else print "PASS no status- prefix" }' \
  && grep -v '^<!--' cha-bio-safety/docs/redesign-context/21-legal-finding-detail/sketch-wave-3-finding-info.html | grep -v '^#' | grep -cE 'font-size:[[:space:]]*(9|10|11)[^0-9]|fontSize:[[:space:]]*(9|10|11)[^0-9]' | awk '{ if ($1 > 0) { print "FAIL: 9/10/11 px found (OQ #3)"; exit 1 } else print "PASS no 9-11px" }' \
  && grep -cE '\bw-8\b|\bh-8\b' cha-bio-safety/docs/redesign-context/21-legal-finding-detail/sketch-wave-3-finding-info.html | awk '{ if ($1 > 0) { print "FAIL: w-8/h-8 found"; exit 1 } else print "PASS no w-8 h-8" }' \
  && grep -cE 'OQ #2|OQ #3' cha-bio-safety/docs/redesign-context/21-legal-finding-detail/sketch-wave-3-finding-info.html | awk '{ if ($1 < 2) { print "FAIL: OQ #2/#3 citation <2"; exit 1 } else print "PASS OQ citations:", $1 }' \
  && git log --oneline -1 | grep -q '260525-4of.*wave 3' && echo "PASS commit T2"
    </automated>
  </verify>
  <done>
    - sketch-wave-3-finding-info.html 파일 존재 + ≥200 lines (≥320 권장)
    - data-theme="dark" ≥2 / data-theme="light" ≥2 (4 frame 매트릭스)
    - SectionHeader '지적 정보' ≥4 / '지적 사진' ≥4 (4 frame)
    - KVRow '지적 내용' label ≥4 / '위치/등록일/등록자' labels ≥12 (4 frame x 3)
    - '사진 없음' (line 213) ≥1 / KVRow + SectionHeader 컴포넌트 anchor ≥1 각각
    - KVRow label width 64 anchor ≥4 / Section border-b border-border-default ≥4
    - text-caption ≥8 (OQ #3) / leading-none ≥8 (OQ #3)
    - description whiteSpace pre-wrap ≥1 / fmtDate 분까지 표시 anchor ≥1 (20-legal-findings 와 다름)
    - 비즈 anchor 11건 박제 hit ≥6
    - **finding 칩/borderLeft 본 페이지 없음** (OQ #2, ≤2)
    - linear-gradient 0건 / status- prefix 0건 / 9·10·11px 0건 / w-8 h-8 0건
    - OQ #2 + OQ #3 인용 ≥2건
    - atomic commit `feat(quick-260525-4of): redesign/21-legal-finding-detail sketch wave 3 (...)` 1개
  </done>
</task>


<task type="auto">
  <name>Task 3 (W4): LegalFindingDetailPage status open form (Section 3 textarea + 사진 5장 useMultiPhotoUpload 슬롯 + 데스크톱 inline CTA) + status resolved 결과 (Section 4 KVRow 3행 + PhotoGrid) + admin ZIP 다운로드 단일 finding (location 기반) + 모바일 고정 하단 CTA (open 한정) sketch 생성 (4 frame, **★ OQ #4 LOCKED linear-gradient anchor ≥3**)</name>
  <files>cha-bio-safety/docs/redesign-context/21-legal-finding-detail/sketch-wave-4-resolve-form.html</files>
  <action>
    20-legal-findings/sketch-wave-4-admin-tools.html 패턴 mirror 로 새 sketch 파일 작성. LegalFindingDetailPage.tsx Section 3 status open 분기 (line 218~250, textarea + 사진 5장 slot + canAdd + 데스크톱 inline CTA) + Section 4 status resolved 분기 (line 253~265, KVRow 3행 + PhotoGrid) + 모바일 고정 하단 CTA (line 270~275, status open 한정 position fixed + paddingBottom calc iOS safe-area) + handleDownload (line 89~130, fflate + iOS PWA `<a download>` + setTimeout 3000 + **파일명 location 기반** `지적사항_${name}.zip`) + handleResolve (line 132~138, memo.trim() validation) + resolveMutation (line 67~87, uploadAll 선행 + 4 키 invalidate + navigate(-1)) + useMultiPhotoUpload (line 59, 직접 사용) 시각화. **★ OQ #4 LOCKED (조치 완료 CTA 인라인 그라데이션 `linear-gradient(135deg, #1d4ed8, #0ea5e9)` ≥3 박제 — T3 만 linear-gradient negative gate 예외 anchor)** + status open/resolved 분기 화면 모드 + 사진 슬롯 5종 + admin ZIP location 기반 시각이 본 wave 의 핵심.

    **구조 (20-legal-findings wic W4 mirror, 4 frame status 분기 + admin 매트릭스):**
    - `<!DOCTYPE html>` + `<head><title>redesign/21-legal-finding-detail — W4 resolve-form + status 분기 + 조치 완료 CTA 그라데이션</title>` + `<style>` (W2 와 동일)
    - `<body>` 안 4 frame side-by-side:

      | frame | viewport | 시각화 |
      |---|---|---|
      | 1 | 다크 모바일 393×800 status open + 사진 슬롯 3종 + 고정 하단 CTA | `data-theme="dark"` + 모바일 헤더 + Section 3 (sectionPad '20px 16px' borderBottom + SectionHeader '조치 내용' + textarea 'placeholder 조치 내용을 입력하세요' rows 3 + 조치 사진 라벨 '조치 사진 (최대 5장)' + 사진 슬롯 3종 [img filled / uploading overlay / canAdd '+사진 첨부'] + 데스크톱 inline CTA hidden — !isDesktop) + paddingBottom 'calc(72px + var(--sab, 0px))' + **모바일 고정 하단 CTA position fixed bottom 0 + button width 100% h 48 인라인 그라데이션 OQ #4 LOCKED anchor 1번째** + paddingBottom 'calc(12px + var(--sab, 0px))' iOS safe-area + zIndex 20 |
      | 2 | 다크 데스크톱 1440×900 status open + 사진 슬롯 5개 + **데스크톱 inline CTA OQ #4 LOCKED anchor 2번째** | `data-theme="dark"` + 데스크톱 타이틀 + maxWidth 700 + Section 3 (sectionPad '20px 32px' + textarea + 사진 슬롯 5개 [img x4 + canAdd 1] + **데스크톱 inline CTA marginTop 16 width 100% h 48 인라인 그라데이션 line 244~248 verbatim OQ #4 LOCKED anchor 2번째**) + paddingBottom 24 (open 데스크톱) + 모바일 고정 CTA hidden (isDesktop) |
      | 3 | 다크 모바일 393×800 status resolved + 조치 사진 PhotoGrid | `data-theme="dark"` + 모바일 헤더 + Section 4 (sectionPad '20px 16px' borderBottom + SectionHeader '조치 결과' + KVRow 3행: '조치일시' '2026.03.18 10:15' / '조치자' '김방재' / '조치 내용' '비상조명등 LED 교체 완료' whiteSpace pre-wrap + 조치 사진 PhotoGrid marginTop 12 photoUrls 3개) + paddingBottom 24 (resolved 양쪽) + 모바일 고정 CTA hidden (finding.status === 'resolved') |
      | 4 | 라이트 데스크톱 1440×900 status resolved + admin ZIP zipLoading 단계 + **OQ #4 LOCKED anchor 3번째** | `data-theme="light"` + 데스크톱 타이틀 + maxWidth 700 + Section 4 (resolved 결과 KVRow 3행 + PhotoGrid) + **admin frame 안 ZIP 다운로드 button hover 또는 별도 영역 OQ #4 LOCKED anchor 3번째 인라인 그라데이션 박제** (CTA 그라데이션 anchor ≥3 충족용 — handleDownload 카피 + fflate + iOS PWA `<a download>` + 파일명 `지적사항_${name}.zip` location 기반 anchor fence) |

    **Section 3 status open 마크업 룰 (line 218~250 verbatim, OQ #3 격상 + OQ #4 LOCKED CTA 그라데이션):**
    ```html
    <!-- Section 3: 조치 내용 (status open 분기) -->
    <div style="padding:20px 32px;border-bottom:1px solid var(--bd);" class="border-b border-border-default">
      <div class="text-caption font-bold leading-none text-text-tertiary" style="margin-bottom:10px;">조치 내용</div>
      <!-- textarea (line 221 verbatim) -->
      <textarea placeholder="조치 내용을 입력하세요" rows="3" class="bg-surface-sunken border border-border-strong text-text-primary text-label rounded-sm" style="width:100%;padding:10px 12px;box-sizing:border-box;font-family:inherit;line-height:1.5;resize:vertical;outline:none;"></textarea>
      <!-- 조치 사진 영역 (line 223~242) -->
      <div style="margin-top:12px;">
        <!-- 조치 사진 라벨 (line 223 verbatim, OQ #3 12 격상) -->
        <div class="text-caption font-bold leading-none text-text-tertiary" style="margin-bottom:6px;">조치 사진 (최대 5장)</div>
        <!-- 슬롯 영역 (line 226~241) -->
        <div style="display:flex;gap:8px;overflow-x:auto;padding-bottom:4px;">
          <!-- 슬롯 img 72x72 (line 229) + 제거 button (line 230~231 verbatim, OQ #3 격상 11→12) -->
          <div style="position:relative;width:72px;height:72px;flex-shrink:0;">
            <img class="rounded-sm border border-border-default" style="width:72px;height:72px;object-fit:cover;display:block;" src="placeholder.jpg" />
            <button class="bg-danger text-caption font-bold leading-none rounded-full" style="position:absolute;top:-6px;right:-6px;width:20px;height:20px;color:#fff;border:none;cursor:pointer;line-height:1;">✕</button>
          </div>
          <!-- uploading overlay (line 232 verbatim, OQ #3 격상 10→12) -->
          <div style="position:relative;width:72px;height:72px;flex-shrink:0;">
            <img class="rounded-sm border border-border-default" style="width:72px;height:72px;object-fit:cover;display:block;" src="placeholder.jpg" />
            <div class="bg-black/40 text-caption leading-none rounded-sm" style="position:absolute;inset:0;display:flex;align-items:center;justify-content:center;color:#fff;">업로드 중</div>
          </div>
          <!-- canAdd 72x72 (line 234~239 verbatim, OQ #3 격상 11→12 + OQ #5 Camera 교체) -->
          <button class="bg-surface-sunken border border-border-strong border-dashed text-text-tertiary text-caption font-semibold leading-none rounded-sm" style="width:72px;height:72px;flex-shrink:0;display:flex;flex-direction:column;align-items:center;justify-content:center;cursor:pointer;gap:4px;" aria-label="Lucide Camera size={22}">
            <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M23 19a2 2 0 0 1-2 2H3a2 2 0 0 1-2-2V8a2 2 0 0 1 2-2h4l2-3h6l2 3h4a2 2 0 0 1 2 2z"/><circle cx="12" cy="13" r="4"/></svg>
            <span>사진 첨부</span>
          </button>
        </div>
      </div>
      <!-- ★ 데스크톱 inline CTA (line 244~248 verbatim, isDesktop 조건 + OQ #4 LOCKED 인라인 그라데이션 anchor 2번째) -->
      <button class="text-text-on-accent text-body-sm font-bold rounded-md" style="margin-top:16px;width:100%;height:48px;border:none;cursor:pointer;background:linear-gradient(135deg, #1d4ed8, #0ea5e9);">조치 완료</button>
    </div>
    ```

    **Section 4 status resolved 마크업 룰 (line 253~265 verbatim):**
    ```html
    <!-- Section 4: 조치 결과 (status resolved 분기) -->
    <div style="padding:20px 32px;border-bottom:1px solid var(--bd);" class="border-b border-border-default">
      <div class="text-caption font-bold leading-none text-text-tertiary" style="margin-bottom:10px;">조치 결과</div>
      <div style="display:flex;flex-direction:column;gap:8px;">
        <!-- KVRow 3행 verbatim (line 256~260) -->
        <div style="display:flex;gap:12px;align-items:flex-start;">
          <span class="text-caption font-bold leading-none text-text-tertiary" style="width:64px;flex-shrink:0;">조치일시</span>
          <span class="text-body-sm text-text-primary" style="flex:1;line-height:1.5;">2026.03.18 10:15</span>
        </div>
        <div style="display:flex;gap:12px;align-items:flex-start;">
          <span class="text-caption font-bold leading-none text-text-tertiary" style="width:64px;flex-shrink:0;">조치자</span>
          <span class="text-body-sm text-text-primary" style="flex:1;line-height:1.5;">김방재</span>
        </div>
        <div style="display:flex;gap:12px;align-items:flex-start;">
          <span class="text-caption font-bold leading-none text-text-tertiary" style="width:64px;flex-shrink:0;">조치 내용</span>
          <span class="text-body-sm text-text-primary" style="flex:1;line-height:1.5;white-space:pre-wrap;">비상조명등 LED 교체 완료</span>
        </div>
      </div>
      <!-- PhotoGrid (resolutionPhotoKeys length > 0 조건부, marginTop 12) -->
      <div style="margin-top:12px;">[PhotoGrid placeholder]</div>
    </div>
    ```

    **★ 모바일 고정 하단 CTA 마크업 룰 (line 270~275 verbatim, status open 한정 + OQ #4 LOCKED 인라인 그라데이션 anchor 1번째):**
    ```html
    <!-- 모바일 고정 하단 CTA (status open 한정 — !isDesktop && !isLoading && !error && finding && finding.status === 'open' 조건부) -->
    <div class="bg-surface-page border-t border-border-default" style="position:fixed;bottom:0;left:0;right:0;padding:12px 16px;padding-bottom:calc(12px + var(--sab, 0px));z-index:20;">
      <!-- ★ 조치 완료 CTA (line 273 verbatim, OQ #4 LOCKED 인라인 그라데이션 anchor 1번째) -->
      <button class="text-text-on-accent text-body-sm font-bold rounded-md" style="width:100%;height:48px;border:none;cursor:pointer;transition:opacity 0.15s;background:linear-gradient(135deg, #1d4ed8, #0ea5e9);">조치 완료</button>
    </div>
    ```

    **handleDownload anchor fence (frame 4 — line 89~130 verbatim, 20-legal-findings round.title 기반과 다름):**
    ```javascript
    // handleDownload (line 89~130 verbatim anchor)
    // ZIP 파일명 (line 119): `지적사항_${name}.zip` where name = (finding.location ?? '위치없음').replace(/[\/\\:*?"<>|]/g, '_')
    // — name = location 안전화 (20-legal-findings 의 round.title 기반과 다름 + fid 기반 아님 — location 기반 + 19-legal LegalPage 의 FindingDetailPanel 단일 다운로드 패턴과 일치)
    // 사진 파일명: `지적사진-${j+1}.jpg` / `조치사진-${j+1}.jpg`
    // 내용.txt: encoder.encode(buildMetaTxt(finding)) — 사진 0건이어도 always 포함
    // iOS PWA `<a download>` 패턴 verbatim:
    //   const a = document.createElement('a');
    //   a.href = url; a.download = filename;
    //   document.body.appendChild(a); a.click(); document.body.removeChild(a);
    //   setTimeout(() => URL.revokeObjectURL(url), 3000);
    // 1 byte 변경 금지 (iOS 안정성 검증된 패턴)
    ```

    **OQ #4 LOCKED anchor 3번째 (frame 4 — admin CTA 그라데이션 별도 박제 ≥3 충족):**
    ```html
    <!-- ★ OQ #4 LOCKED anchor 3번째 (≥3 충족용) — admin frame 안 별도 CTA 또는 hover state 그라데이션 박제 -->
    <button class="text-text-on-accent text-body-sm font-bold rounded-md" style="width:100%;height:48px;border:none;cursor:pointer;background:linear-gradient(135deg, #1d4ed8, #0ea5e9);">조치 완료 (admin frame anchor)</button>
    ```

    **카피 verbatim (memory `feedback_sketch_realistic_data`):**
    - SectionHeader: '조치 내용' (line 220) / '조치 결과' (line 255)
    - textarea placeholder: '조치 내용을 입력하세요' (line 221 verbatim)
    - 조치 사진 라벨: '조치 사진 (최대 5장)' (line 223 verbatim)
    - canAdd 카피: '사진 첨부' (line 237 verbatim, **OQ #3 격상 11→12** + Lucide Camera size={22} 교체 OQ #5)
    - 슬롯 제거 button: '✕' (line 231 verbatim, **OQ #3 격상 11→12**)
    - 슬롯 업로드 중: '업로드 중' (line 232 verbatim, **OQ #3 격상 10→12**)
    - CTA button: '조치 완료' (line 246, 273 verbatim) / '처리 중...' (isSubmitting)
    - KVRow label 3행 (Section 4): '조치일시' / '조치자' / '조치 내용' (line 256~260 verbatim)
    - KVRow value fallback: `resolvedByName ?? resolvedBy ?? '-'` / `resolutionMemo ?? '-' whiteSpace pre-wrap`
    - toast.success: '조치 완료' (line 80) / '다운로드 완료' (line 124)
    - toast.error: '조치 처리 실패' (line 85) / '조치 내용을 입력하세요' (line 134) / '다운로드 실패' (line 126)

    **rules 박스:**
    - LOCKED 결정: **OQ #4 (조치 완료 CTA 인라인 `linear-gradient(135deg, #1d4ed8, #0ea5e9)` — T3 만 linear-gradient negative gate 예외 anchor ≥3 박제, admin 다운로드 button = 작은 도구 solid bg-surface-sunken border 유지)** + status open/resolved 분기 (mutually exclusive line 218/253) + admin ZIP location 기반 파일명 (line 119) + useMultiPhotoUpload 직접 사용 (line 59) + resolveMutation uploadAll 선행 + 4 키 invalidate + navigate(-1) (line 67~87) + handleResolve validation (line 132~138) verbatim 인용 1 fence
    - **비즈 anchor 11건 박제 fence** + handleDownload + iOS PWA `<a download>` + ZIP location 기반 + buildMetaTxt + fflate + toast 5종 emphasis
    - **19/20-legal 차이 5건 fence** (form 풀 페이지 + 사진 5장 직접 + ZIP location 기반 + status open 분기 emphasis)
    - 토큰 매핑: Section 3+4 padding sectionPad borderBottom `var(--bd) → border-b border-border-default` / textarea bg `var(--bg3) → bg-surface-sunken` + border `var(--bd2) → border-border-strong` + color `var(--t1) → text-text-primary` / 슬롯 img border `var(--bd) → border-border-default` / 제거 button bg `var(--danger) → bg-danger` / uploading overlay bg rgba(0,0,0,0.4) → bg-black/40 / canAdd bg `var(--bg3) → bg-surface-sunken` + border `var(--bd2) → border-border-strong border-dashed` + color `var(--t3) → text-text-tertiary` / **CTA button OQ #4 LOCKED 인라인 그라데이션 (T3 만 예외)** / 모바일 고정 하단 CTA bg `var(--bg) → bg-surface-page` + borderTop `var(--bd) → border-t border-border-default`
    - negative gate 자체매칭 회피: fence escape — 단 `linear-gradient(135deg, #1d4ed8, #0ea5e9)` 그라데이션 본문 ≥3 박제 (OQ #4 LOCKED 예외)

    **commit 명령:**
    ```
    git add cha-bio-safety/docs/redesign-context/21-legal-finding-detail/sketch-wave-4-resolve-form.html
    git commit -m "feat(quick-260525-4of): redesign/21-legal-finding-detail sketch wave 4 (resolve-form — status open form + 사진 5장 useMultiPhotoUpload 직접 + 조치 완료 CTA 그라데이션 ≥3 + status resolved KVRow 3행 + admin ZIP location 기반 + 모바일 고정 하단 CTA + 비즈 anchor 박제)"
    ```
  </action>
  <verify>
    <automated>
test -f cha-bio-safety/docs/redesign-context/21-legal-finding-detail/sketch-wave-4-resolve-form.html \
  && wc -l cha-bio-safety/docs/redesign-context/21-legal-finding-detail/sketch-wave-4-resolve-form.html | awk '{ if ($1 < 200) { print "FAIL: too short"; exit 1 } else print "PASS lines:", $1 }' \
  && grep -c 'data-theme="dark"' cha-bio-safety/docs/redesign-context/21-legal-finding-detail/sketch-wave-4-resolve-form.html | awk '{ if ($1 < 3) { print "FAIL: dark frame <3"; exit 1 } else print "PASS dark frame:", $1 }' \
  && grep -c 'data-theme="light"' cha-bio-safety/docs/redesign-context/21-legal-finding-detail/sketch-wave-4-resolve-form.html | awk '{ if ($1 < 1) { print "FAIL: light frame <1"; exit 1 } else print "PASS light frame:", $1 }' \
  && grep -cE 'linear-gradient\(135deg,\s*#1d4ed8,\s*#0ea5e9\)' cha-bio-safety/docs/redesign-context/21-legal-finding-detail/sketch-wave-4-resolve-form.html | awk '{ if ($1 < 3) { print "FAIL: 조치 완료 CTA 그라데이션 anchor <3 (★ OQ #4 LOCKED T3 만 예외 ≥3)"; exit 1 } else print "PASS 그라데이션 anchor:", $1 }' \
  && grep -c '조치 내용' cha-bio-safety/docs/redesign-context/21-legal-finding-detail/sketch-wave-4-resolve-form.html | awk '{ if ($1 < 2) { print "FAIL: SectionHeader 조치 내용 + KVRow label <2"; exit 1 } else print "PASS 조치 내용:", $1 }' \
  && grep -c '조치 결과' cha-bio-safety/docs/redesign-context/21-legal-finding-detail/sketch-wave-4-resolve-form.html | awk '{ if ($1 < 1) { print "FAIL: SectionHeader 조치 결과 missing"; exit 1 } else print "PASS 조치 결과:", $1 }' \
  && grep -c '조치 완료' cha-bio-safety/docs/redesign-context/21-legal-finding-detail/sketch-wave-4-resolve-form.html | awk '{ if ($1 < 3) { print "FAIL: CTA 조치 완료 <3 (그라데이션 3 anchor)"; exit 1 } else print "PASS 조치 완료:", $1 }' \
  && grep -c '조치 내용을 입력하세요' cha-bio-safety/docs/redesign-context/21-legal-finding-detail/sketch-wave-4-resolve-form.html | awk '{ if ($1 < 1) { print "FAIL: textarea placeholder missing"; exit 1 } else print "PASS placeholder:", $1 }' \
  && grep -c '조치 사진 (최대 5장)' cha-bio-safety/docs/redesign-context/21-legal-finding-detail/sketch-wave-4-resolve-form.html | awk '{ if ($1 < 1) { print "FAIL: 조치 사진 라벨 missing"; exit 1 } else print "PASS 조치 사진 라벨:", $1 }' \
  && grep -c '사진 첨부' cha-bio-safety/docs/redesign-context/21-legal-finding-detail/sketch-wave-4-resolve-form.html | awk '{ if ($1 < 1) { print "FAIL: canAdd 사진 첨부 missing"; exit 1 } else print "PASS 사진 첨부:", $1 }' \
  && grep -c '업로드 중' cha-bio-safety/docs/redesign-context/21-legal-finding-detail/sketch-wave-4-resolve-form.html | awk '{ if ($1 < 1) { print "FAIL: uploading overlay 업로드 중 missing"; exit 1 } else print "PASS 업로드 중:", $1 }' \
  && grep -cE '조치일시|조치자' cha-bio-safety/docs/redesign-context/21-legal-finding-detail/sketch-wave-4-resolve-form.html | awk '{ if ($1 < 2) { print "FAIL: KVRow label 조치일시+조치자 <2"; exit 1 } else print "PASS Section 4 KVRow:", $1 }' \
  && grep -cE 'position:fixed|position: fixed' cha-bio-safety/docs/redesign-context/21-legal-finding-detail/sketch-wave-4-resolve-form.html | awk '{ if ($1 < 1) { print "FAIL: 모바일 고정 하단 CTA position fixed missing"; exit 1 } else print "PASS position fixed:", $1 }' \
  && grep -cE 'calc\(12px \+ var\(--sab|padding-bottom:calc.*sab' cha-bio-safety/docs/redesign-context/21-legal-finding-detail/sketch-wave-4-resolve-form.html | awk '{ if ($1 < 1) { print "FAIL: iOS safe-area paddingBottom calc missing"; exit 1 } else print "PASS iOS safe-area:", $1 }' \
  && grep -cE '지적사항_\$\{name|name = \(finding\.location|location 안전화|지적사항_\$\{.*location' cha-bio-safety/docs/redesign-context/21-legal-finding-detail/sketch-wave-4-resolve-form.html | awk '{ if ($1 < 1) { print "FAIL: ZIP 파일명 location 기반 anchor missing (20-legal-findings round.title 기반과 다름)"; exit 1 } else print "PASS ZIP location 기반:", $1 }' \
  && grep -cE '<a download|createElement.*a.*download|URL\.revokeObjectURL' cha-bio-safety/docs/redesign-context/21-legal-finding-detail/sketch-wave-4-resolve-form.html | awk '{ if ($1 < 1) { print "FAIL: iOS PWA <a download> 패턴 anchor missing"; exit 1 } else print "PASS iOS PWA anchor:", $1 }' \
  && grep -cE "finding\.status === 'open'|finding\.status === 'resolved'|status open|status resolved" cha-bio-safety/docs/redesign-context/21-legal-finding-detail/sketch-wave-4-resolve-form.html | awk '{ if ($1 < 2) { print "FAIL: status open/resolved 분기 anchor <2"; exit 1 } else print "PASS status 분기:", $1 }' \
  && grep -cE 'useMultiPhotoUpload|resolutionPhotos|uploadAll' cha-bio-safety/docs/redesign-context/21-legal-finding-detail/sketch-wave-4-resolve-form.html | awk '{ if ($1 < 1) { print "FAIL: useMultiPhotoUpload 직접 사용 anchor missing (19/20-legal 차이 2)"; exit 1 } else print "PASS useMultiPhotoUpload anchor:", $1 }' \
  && grep -cE 'resolveMutation|invalidateQueries|navigate\(-1\)' cha-bio-safety/docs/redesign-context/21-legal-finding-detail/sketch-wave-4-resolve-form.html | awk '{ if ($1 < 1) { print "FAIL: resolveMutation + 4 키 invalidate + navigate(-1) anchor missing"; exit 1 } else print "PASS resolveMutation anchor:", $1 }' \
  && grep -cE 'legalApi|useQuery|useMutation|useMultiPhotoUpload|handleResolve|handleDownload|finding\.status|role.*admin|toast|KVRow|SectionHeader' cha-bio-safety/docs/redesign-context/21-legal-finding-detail/sketch-wave-4-resolve-form.html | awk '{ if ($1 < 6) { print "FAIL: 비즈 anchor 11건 박제 hit <6"; exit 1 } else print "PASS 비즈 anchor hits:", $1 }' \
  && grep -cE '(text|bg|border)-status-(safe|fire|warning|danger|caution)' cha-bio-safety/docs/redesign-context/21-legal-finding-detail/sketch-wave-4-resolve-form.html | awk '{ if ($1 > 0) { print "FAIL: status- prefix found"; exit 1 } else print "PASS no status- prefix" }' \
  && grep -v '^<!--' cha-bio-safety/docs/redesign-context/21-legal-finding-detail/sketch-wave-4-resolve-form.html | grep -v '^#' | grep -cE 'font-size:[[:space:]]*(9|10|11)[^0-9]|fontSize:[[:space:]]*(9|10|11)[^0-9]' | awk '{ if ($1 > 0) { print "FAIL: 9/10/11 px found"; exit 1 } else print "PASS no 9-11px" }' \
  && grep -cE '\bw-8\b|\bh-8\b' cha-bio-safety/docs/redesign-context/21-legal-finding-detail/sketch-wave-4-resolve-form.html | awk '{ if ($1 > 0) { print "FAIL: w-8/h-8 found"; exit 1 } else print "PASS no w-8 h-8" }' \
  && grep -cE 'OQ #4|OQ #5' cha-bio-safety/docs/redesign-context/21-legal-finding-detail/sketch-wave-4-resolve-form.html | awk '{ if ($1 < 2) { print "FAIL: OQ #4/#5 citation <2"; exit 1 } else print "PASS OQ citations:", $1 }' \
  && git log --oneline -1 | grep -q '260525-4of.*wave 4' && echo "PASS commit T3"
    </automated>
  </verify>
  <done>
    - sketch-wave-4-resolve-form.html 파일 존재 + ≥200 lines (≥320 권장)
    - data-theme="dark" ≥3 / data-theme="light" ≥1 (4 frame status 분기 + admin 매트릭스)
    - **★ linear-gradient(135deg, #1d4ed8, #0ea5e9) ≥3 박제 (OQ #4 LOCKED T3 만 예외 anchor — 모바일 고정 + 데스크톱 inline + admin frame)**
    - SectionHeader '조치 내용' ≥2 / '조치 결과' ≥1
    - CTA '조치 완료' ≥3 / textarea placeholder ≥1 / 조치 사진 라벨 ≥1 / canAdd '사진 첨부' ≥1 / '업로드 중' ≥1
    - Section 4 KVRow '조치일시'+'조치자' ≥2
    - 모바일 고정 하단 CTA position fixed ≥1 / iOS safe-area paddingBottom calc ≥1
    - ZIP 파일명 **location 기반** anchor ≥1 (20-legal-findings round.title 기반과 다름)
    - iOS PWA `<a download>` 패턴 anchor ≥1
    - status open/resolved 분기 anchor ≥2
    - useMultiPhotoUpload 직접 사용 anchor ≥1 (19/20-legal 차이 2)
    - resolveMutation + 4 키 invalidate + navigate(-1) anchor ≥1
    - 비즈 anchor 11건 박제 hit ≥6
    - status- prefix 0건 / 9·10·11px 0건 / w-8 h-8 0건
    - OQ #4 + OQ #5 인용 ≥2건
    - atomic commit `feat(quick-260525-4of): redesign/21-legal-finding-detail sketch wave 4 (...)` 1개
  </done>
</task>


<task type="auto">
  <name>Task 4 (W5): LegalFindingDetailPage.tsx TSX 변환 verify checklist markdown 생성 (12 섹션, 비즈 anchor 11건 + Tailwind cheatsheet + 메모리 룰 12+ + 단일 finding 페이지 자체로 종결 명시 + 최종 src 10 파일 0 byte verify gate)</name>
  <files>cha-bio-safety/docs/redesign-context/21-legal-finding-detail/wave-5-tsx-conversion-checklist.md</files>
  <action>
    20-legal-findings/wave-5-tsx-conversion-checklist.md 패턴 mirror 로 새 checklist markdown 작성. **sketch HTML 아님 — markdown checklist** (다음 turn 의 W6 TSX 변환 wave 진입점). LegalFindingDetailPage.tsx 279 lines 단일 atomic 변환 룰 + 비즈 anchor 11건 박제 + Tailwind cheatsheet (v0.1.1 토큰 → utility class 매핑) + 메모리 룰 inline 12+ + 단일 finding 페이지 자체로 종결 명시 (부모 LegalFindingsPage 변환은 별도 wave, 조부모 LegalPage 의 19-legal 변환 이미 완료 확인).

    **12 섹션 구조 (20-legal-findings wic W5 mirror, 단 10 src 파일 적용):**

    **§1 변환 범위** — LegalFindingDetailPage.tsx 단일 atomic (279 lines, 3 영역 통합 — 상단 imports/fmtDate/KVRow/SectionHeader/Spinner + 메인 함수 + JSX render). 외부 6 파일 + App.tsx + 조부모 LegalPage + 부모 LegalFindingsPage **0 byte 변경** (verify gate 최종 git diff). 단일 finding 페이지 자체로 종결 (§12 다음 단계).

    **§2 비즈 anchor 11건 보존 (verbatim fence)** — rules_verbatim 의 비즈 anchor 11건 list 그대로 인용 (legalApi 2종 getFinding+resolveFinding + useQuery 1 + useMutation 1 uploadAll 선행 + 4 키 invalidate + navigate(-1) + useMultiPhotoUpload 직접 사용 + handleResolve memo.trim() validation + finding.status open/resolved 분기 + admin role selector 패턴 + handleDownload iOS PWA + ZIP location 기반 + @keyframes spin 중복 정의 + toast 5 + 추가 카피 verbatim 다수).

    **§3 변환 매핑 (영역 1~3 verbatim)** — wave-1-index.md §1.1 영역별 인벤토리 표 3개 (영역 1 상단 imports/포맷터/KVRow/SectionHeader/Spinner + 영역 2 메인 함수 + 영역 3 JSX render) 그대로 인용 + 각 영역의 후속 wave 매핑 (W2/W3/W4) 박제.

    **§4 OQ LOCKED 5건 반영 매핑 표** — rules_verbatim 의 OQ #1~#5 LOCKED 5건 verbatim + 각 OQ 의 TSX 변환 후 적용 위치 (line 번호) + sketch wave 매핑 (W2/W3/W4) 표.

    **§5 negative gate** — 이모지 0 ('📷' Camera Lucide 교체 후 본 페이지 직접 사용 이모지 폐기) / `linear-gradient` (T1/T2/T4 = 0 / **T3 만 예외 anchor ≥3** OQ #4 LOCKED) / 9·10·11px 0 (uploading overlay 10 + 제거 button 11 + canAdd 11 모두 격상) / status- prefix 0 / w-8 h-8 0 / 옛 alias 토큰 0 (sketch 본문 안) + W5 checklist 자체 본문 안 code fence 인용 ≥1.

    **§6 positive gate** — 4 sketch 파일 모두 존재 (W2 chrome + W3 finding-info + W4 resolve-form) + W5 checklist 자체 + 토큰 매핑 완결 + 비즈 anchor 박제 hit ≥6 / 파일 + OQ #1~#5 인용 4 sketch 분산.

    **§7 build/tsc** — `npm run build` PASS + `tsc --noEmit` PASS (W6 TSX 변환 wave 시점 — 본 PLAN 에서는 verify gate 없음).

    **§8 자체 verify grep 모음** — 핵심 grep 12종 (status- prefix 0 / w-8 h-8 0 / 9·10·11px 0 / linear-gradient T3 만 ≥3 / Lucide ChevronLeft + Download + Loader2 + Camera / 타이틀 정적 '지적 상세' / KVRow + SectionHeader + width 64 / fmtDate 분까지 / status open/resolved 분기 + mutually exclusive / useMultiPhotoUpload 직접 사용 / handleResolve memo.trim() validation / handleDownload location 기반 + iOS PWA / 모바일 고정 하단 CTA position fixed + paddingBottom calc / admin selector 패턴 / @keyframes spin 중복 정의)

    **§9 Tailwind cheatsheet (v0.1.1 토큰 → utility class 매핑, 21-legal-finding-detail 적용)** — rules_verbatim 의 Tailwind cheatsheet 표 그대로 인용 (v0.1.1 토큰 + Tailwind utility + 21-legal-finding-detail 적용 위치).

    **§10 비즈 보존 체크박스** — legalApi 2종 / useQuery 1 / useMutation 1 (uploadAll 선행 + 4 키 invalidate + navigate(-1)) / useMultiPhotoUpload 직접 사용 / handleResolve validation / finding.status open/resolved 분기 (mutually exclusive) / admin role selector 패턴 / handleDownload iOS PWA + ZIP location 기반 + buildMetaTxt / @keyframes spin 중복 정의 / toast 5 / 에러/placeholder/라벨 카피 verbatim — 각 항목 체크박스 + line 번호.

    **§11 메모리 룰 inline (12+ rule citations)** — feedback_inspection_unresolved_color + project_inspection_completion_rule + feedback_tailwind_token_class_pattern + feedback_tailwind_w8_h8_is_48px + feedback_text_caption_leading_none + feedback_sketch_realistic_data + feedback_design_changes_ask_first + feedback_planner_prompt_sketch_verbatim + feedback_tsx_wave_emoji_dot_gap + feedback_tsx_wave_stat_card_drift + feedback_subagent_production_deploy_forbidden + feedback_redesign_sketch_rule_enforcement + feedback_cbc7119_design_never_wrangler (wrangler 명령 절대 X)

    **§12 다음 단계** — W6 TSX 변환 wave (LegalFindingDetailPage.tsx 279 lines 단일 atomic) + 단일 finding 페이지 자체로 종결 (조부모 LegalPage 의 19-legal 변환 완료 확인 + 부모 LegalFindingsPage 변환은 별도 wave) + 직원 도메인 X (cbc7119-preview 만 자동 배포).

    **최종 src 10 파일 0 byte verify gate (본 task verify gate 안 포함):**
    ```bash
    # src 10 파일 0 byte 변경 확인
    git diff --name-only HEAD~4 HEAD -- \
      cha-bio-safety/src/pages/LegalFindingDetailPage.tsx \
      cha-bio-safety/src/components/PhotoGrid.tsx \
      cha-bio-safety/src/components/PhotoSourceModal.tsx \
      cha-bio-safety/src/hooks/useMultiPhotoUpload.ts \
      cha-bio-safety/src/utils/findingDownload.ts \
      cha-bio-safety/src/utils/api.ts \
      cha-bio-safety/src/stores/authStore.ts \
      cha-bio-safety/src/App.tsx \
      cha-bio-safety/src/pages/LegalPage.tsx \
      cha-bio-safety/src/pages/LegalFindingsPage.tsx
    # 결과 empty 여야 함 (4 sketch+checklist commit 만 — src 변경 X)
    ```

    **commit 명령:**
    ```
    git add cha-bio-safety/docs/redesign-context/21-legal-finding-detail/wave-5-tsx-conversion-checklist.md
    git commit -m "feat(quick-260525-4of): redesign/21-legal-finding-detail sketch wave 5 (TSX checklist — LegalFindingDetailPage.tsx 279 lines 단일 atomic 변환 룰 + 비즈 anchor 11건 + Tailwind cheatsheet + 단일 finding 페이지 자체로 종결 + 10 src 파일 0 byte verify gate)"
    ```
  </action>
  <verify>
    <automated>
test -f cha-bio-safety/docs/redesign-context/21-legal-finding-detail/wave-5-tsx-conversion-checklist.md \
  && wc -l cha-bio-safety/docs/redesign-context/21-legal-finding-detail/wave-5-tsx-conversion-checklist.md | awk '{ if ($1 < 300) { print "FAIL: too short (≥300 권장)"; exit 1 } else print "PASS lines:", $1 }' \
  && grep -cE '^## §1\.|^## §1[^.0-9]|^## §1 ' cha-bio-safety/docs/redesign-context/21-legal-finding-detail/wave-5-tsx-conversion-checklist.md | awk '{ if ($1 < 1) { print "FAIL: §1 변환 범위 missing"; exit 1 } else print "PASS §1" }' \
  && grep -cE '^## §[0-9]+|^### §[0-9]+|^# §[0-9]+' cha-bio-safety/docs/redesign-context/21-legal-finding-detail/wave-5-tsx-conversion-checklist.md | awk '{ if ($1 < 8) { print "FAIL: 12 섹션 헤더 <8 (§1~§12)"; exit 1 } else print "PASS 섹션 수:", $1 }' \
  && grep -cE 'legalApi|useQuery|useMutation|useMultiPhotoUpload|handleResolve|handleDownload|finding\.status|role.*admin|toast|KVRow|SectionHeader' cha-bio-safety/docs/redesign-context/21-legal-finding-detail/wave-5-tsx-conversion-checklist.md | awk '{ if ($1 < 12) { print "FAIL: 비즈 anchor 11건 박제 hit <12"; exit 1 } else print "PASS 비즈 anchor hits:", $1 }' \
  && grep -c 'LegalFindingDetailPage.tsx' cha-bio-safety/docs/redesign-context/21-legal-finding-detail/wave-5-tsx-conversion-checklist.md | awk '{ if ($1 < 3) { print "FAIL: LegalFindingDetailPage.tsx <3"; exit 1 } else print "PASS LegalFindingDetailPage.tsx:", $1 }' \
  && grep -cE 'LegalFindingsPage|단일 finding|자체로 종결' cha-bio-safety/docs/redesign-context/21-legal-finding-detail/wave-5-tsx-conversion-checklist.md | awk '{ if ($1 < 2) { print "FAIL: 단일 finding 페이지 자체로 종결 명시 <2 (§12)"; exit 1 } else print "PASS 단일 finding:", $1 }' \
  && grep -cE 'OQ #1|OQ #2|OQ #3|OQ #4|OQ #5' cha-bio-safety/docs/redesign-context/21-legal-finding-detail/wave-5-tsx-conversion-checklist.md | awk '{ if ($1 < 5) { print "FAIL: OQ #1~#5 5건 citation <5"; exit 1 } else print "PASS OQ citations:", $1 }' \
  && grep -cE 'feedback_inspection_unresolved_color|project_inspection_completion_rule|feedback_tailwind_token_class_pattern|feedback_tailwind_w8_h8_is_48px|feedback_text_caption_leading_none' cha-bio-safety/docs/redesign-context/21-legal-finding-detail/wave-5-tsx-conversion-checklist.md | awk '{ if ($1 < 5) { print "FAIL: 메모리 룰 inline <5 (§11)"; exit 1 } else print "PASS memory rules:", $1 }' \
  && grep -cE "finding\.status === 'open'|finding\.status === 'resolved'|mutually exclusive" cha-bio-safety/docs/redesign-context/21-legal-finding-detail/wave-5-tsx-conversion-checklist.md | awk '{ if ($1 < 1) { print "FAIL: status open/resolved 분기 mapping <1"; exit 1 } else print "PASS status 분기:", $1 }' \
  && grep -cE 'linear-gradient\(135deg' cha-bio-safety/docs/redesign-context/21-legal-finding-detail/wave-5-tsx-conversion-checklist.md | awk '{ if ($1 < 1) { print "FAIL: T3 linear-gradient 예외 anchor citation <1 (§5)"; exit 1 } else print "PASS gradient citation:", $1 }' \
  && grep -cE 'cbc7119|wrangler' cha-bio-safety/docs/redesign-context/21-legal-finding-detail/wave-5-tsx-conversion-checklist.md | awk '{ if ($1 < 1) { print "FAIL: wrangler/cbc7119 메모리 룰 citation missing"; exit 1 } else print "PASS cbc7119 citation:", $1 }' \
  && grep -cE 'Tailwind cheatsheet|v0\.1\.1' cha-bio-safety/docs/redesign-context/21-legal-finding-detail/wave-5-tsx-conversion-checklist.md | awk '{ if ($1 < 1) { print "FAIL: §9 Tailwind cheatsheet missing"; exit 1 } else print "PASS Tailwind cheatsheet:", $1 }' \
  && grep -cE 'showNav=false|/\\^\\\\/legal' cha-bio-safety/docs/redesign-context/21-legal-finding-detail/wave-5-tsx-conversion-checklist.md | awk '{ if ($1 < 1) { print "FAIL: 글로벌 chrome 0건 anchor (App.tsx line 117) missing"; exit 1 } else print "PASS chrome 0 anchor:", $1 }' \
  && grep -cE '지적사항_\$\{name|location 안전화|location 기반' cha-bio-safety/docs/redesign-context/21-legal-finding-detail/wave-5-tsx-conversion-checklist.md | awk '{ if ($1 < 1) { print "FAIL: ZIP location 기반 anchor missing (19/20-legal 차이 4)"; exit 1 } else print "PASS ZIP location 기반:", $1 }' \
  && grep -cE 'useMultiPhotoUpload|직접 사용' cha-bio-safety/docs/redesign-context/21-legal-finding-detail/wave-5-tsx-conversion-checklist.md | awk '{ if ($1 < 1) { print "FAIL: useMultiPhotoUpload 직접 사용 anchor missing (19/20-legal 차이 2)"; exit 1 } else print "PASS useMultiPhotoUpload anchor:", $1 }' \
  && git log --oneline -1 | grep -q '260525-4of.*wave 5' && echo "PASS commit T4" \
  && echo "=== 최종 src 10 파일 0 byte verify gate ===" \
  && CHANGED=$(git diff --name-only HEAD~4 HEAD -- \
       cha-bio-safety/src/pages/LegalFindingDetailPage.tsx \
       cha-bio-safety/src/components/PhotoGrid.tsx \
       cha-bio-safety/src/components/PhotoSourceModal.tsx \
       cha-bio-safety/src/hooks/useMultiPhotoUpload.ts \
       cha-bio-safety/src/utils/findingDownload.ts \
       cha-bio-safety/src/utils/api.ts \
       cha-bio-safety/src/stores/authStore.ts \
       cha-bio-safety/src/App.tsx \
       cha-bio-safety/src/pages/LegalPage.tsx \
       cha-bio-safety/src/pages/LegalFindingsPage.tsx 2>/dev/null | wc -l) \
  && if [ "$CHANGED" -gt 0 ]; then echo "FAIL: src 파일 $CHANGED 개 변경됨 (0 byte 룰 위반)"; exit 1; else echo "PASS src 10 파일 0 byte 변경 (4 sketch+checklist commit 만)"; fi
    </automated>
  </verify>
  <done>
    - wave-5-tsx-conversion-checklist.md 파일 존재 + ≥300 lines (≥450 권장)
    - 12 섹션 헤더 (§1~§12) ≥8건 (§1 변환 범위 / §2 비즈 anchor 보존 / §3 변환 매핑 / §4 OQ LOCKED / §5 negative gate / §6 positive gate / §7 build/tsc / §8 자체 verify / §9 Tailwind cheatsheet / §10 비즈 보존 체크박스 / §11 메모리 룰 / §12 다음 단계)
    - 비즈 anchor 11건 박제 hit ≥12
    - LegalFindingDetailPage.tsx ≥3 / 단일 finding 페이지 자체로 종결 명시 ≥2 (§12)
    - OQ #1~#5 5건 citation ≥5
    - 메모리 룰 inline ≥5 (feedback_inspection_unresolved_color + project_inspection_completion_rule + feedback_tailwind_token_class_pattern + feedback_tailwind_w8_h8_is_48px + feedback_text_caption_leading_none)
    - status open/resolved 분기 mapping ≥1 (mutually exclusive)
    - T3 linear-gradient 예외 anchor citation ≥1 (§5)
    - wrangler/cbc7119 메모리 룰 citation ≥1
    - §9 Tailwind cheatsheet ≥1
    - 글로벌 chrome 0건 anchor (App.tsx line 117) ≥1
    - ZIP location 기반 anchor ≥1 (19/20-legal 차이 4)
    - useMultiPhotoUpload 직접 사용 anchor ≥1 (19/20-legal 차이 2)
    - atomic commit `feat(quick-260525-4of): redesign/21-legal-finding-detail sketch wave 5 (...)` 1개
    - **최종 src 10 파일 0 byte verify gate PASS** (LegalFindingDetailPage.tsx + 외부 6 + App.tsx + 조부모 LegalPage + 부모 LegalFindingsPage 모두 변경 0 byte)
  </done>
</task>

</tasks>

<verification>
- 4 파일 모두 cha-bio-safety/docs/redesign-context/21-legal-finding-detail/ 직속 평면 배치 (sketch/ 서브폴더 X)
- 각 task 마다 atomic git commit 1개씩 (총 4 commit, executor cherry-pick 사고 회피용)
- T1/T2/T4 sketch: linear-gradient 0건 / T3 sketch: linear-gradient(135deg, #1d4ed8, #0ea5e9) ≥3 (★ OQ #4 LOCKED T3 만 예외)
- OQ LOCKED 5건 모두 4 파일 안에 verbatim 반영 (OQ #1 모바일 헤더 + 정적 '지적 상세' / #2 finding 칩 없음 + KVRow value 좌측 강조 없음 / #3 fontSize 12 + leading-none / #4 조치 완료 CTA 그라데이션 / #5 Lucide ChevronLeft + Download + Loader2 + Camera + 44x44)
- 비즈 anchor 11건 모두 4 파일 안에 verbatim 박제 (legalApi 2 + useQuery 1 + useMutation 1 uploadAll 선행 + 4 키 invalidate + useMultiPhotoUpload 직접 + handleResolve validation + finding.status 분기 mutually exclusive + admin selector 패턴 + handleDownload iOS PWA + ZIP location 기반 + @keyframes spin 중복 + toast 5)
- 19/20-legal 차이 5건 sketch 시각 반영 (단일 finding / form 풀 페이지 / 사진 5장 직접 / ZIP location 기반 / status open 분기)
- negative gate 4 sketch + 1 checklist 모두 통과 (이모지 0 / 9·10·11px 0 / status- prefix 0 / w-8 h-8 0 / 옛 alias 토큰 0)
- 10 src 파일 0 byte 변경 (LegalFindingDetailPage + 외부 6 + App.tsx + 조부모 LegalPage + 부모 LegalFindingsPage)
</verification>

<success_criteria>
- 4 파일 모두 wc -l ≥200 (sketch HTML 3개) / ≥300 (checklist md 1개)
- 각 task 의 verify gate <automated> 블록 모두 PASS
- 최종 git diff --name-only HEAD~4 HEAD -- src 10 파일 empty
- 사용자가 cbc7119-preview 자동 배포에서 4 frame × 3 sketch + checklist 1 차수 시각 검수 가능
</success_criteria>

<output>
After completion, no separate SUMMARY required (quick task — atomic 4 commit 으로 박제 완결).
Optional: STATE.md row + Last session 1줄 박제 (사용자 요청 시).
</output>
