---
phase: 260420-ee1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cha-bio-safety/src/pages/DivPage.tsx
autonomous: true
requirements:
  - QUICK-260420-ee1
must_haves:
  truths:
    - "1024px 이상 뷰포트에서 DIV 페이지는 마스터-디테일 2열 레이아웃을 보여준다(좌: 매트릭스, 우: 상세 패널)"
    - "1024px 미만 뷰포트에서는 기존 모바일 UI가 그대로 유지되고 회귀가 없다"
    - "상단 알림 배너에 전체 34개 포인트 워스트케이스 기반의 이상/주의 카운트가 표시된다"
    - "배너의 이상/주의 칩을 클릭하면 해당 DIV가 우측 상세 패널에 선택된다"
    - "좌측 매트릭스는 14개 층 × 3열(#1/#2/#3) 테이블이며, 각 셀은 pressureStatus 기반 상태색 배경과 '최근 MM월초/말' 라벨을 보여준다"
    - "DIV_POINTS에 없는 (층, pos) 조합은 '—'로 표시된다"
    - "셀을 클릭하면 setSelDiv(div)가 호출되어 우측 상세 패널에 해당 DIV가 로드된다"
    - "포인트 미선택 상태의 우측 패널은 정상/주의/이상 카운트 요약 + 배수/오일 평균 주기 + 이상/주의 리스트를 보여준다"
    - "포인트 선택 상태의 우측 패널은 내부 탭(압력 트렌드/챔버배수/탱크배수/오일) + 선택된 탭 콘텐츠를 보여주며, 압력 트렌드 기본값이다"
    - "우측 패널의 압력 트렌드는 모바일 renderDivDetail과 동일한 3단(1차/2차/세팅) SVG 차트 + 연도 네비게이션을 표시한다"
    - "우측 패널의 챔버/탱크/오일 탭은 해당 div_id의 IntervalBar 타임라인 + 날짜 리스트를 표시한다"
    - "우측 패널 헤더의 ✕ 버튼을 누르면 selDiv가 초기화되어 빈 선택 상태로 돌아간다"
  artifacts:
    - path: "cha-bio-safety/src/pages/DivPage.tsx"
      provides: "Mobile + desktop dual layout for DIV pressure management"
      contains: "useIsDesktop"
      min_lines: 700
  key_links:
    - from: "DivPage return"
      to: "renderDesktopLayout"
      via: "if (isDesktop) return <desktop JSX>"
      pattern: "if \\(isDesktop\\)"
    - from: "desktop matrix cell onClick"
      to: "setSelDiv"
      via: "existing state hook reuse"
      pattern: "setSelDiv\\(div\\)"
    - from: "desktop right-panel tabs"
      to: "existing tab state"
      via: "shared 'tab' useState ('pressure'|'drain'|'comp_drain'|'compressor')"
      pattern: "setTab\\("
    - from: "desktop pressure chart"
      to: "selHistory useQuery"
      via: "reuse existing /api/div/pressure?location= fetch"
      pattern: "div-history"
    - from: "alert banner chip"
      to: "setSelDiv"
      via: "onClick handler on each 이상/주의 chip"
      pattern: "setSelDiv"
---

<objective>
DIV 압력 관리 페이지(`cha-bio-safety/src/pages/DivPage.tsx`)에 1024px 이상 데스크톱 뷰를 추가한다.

- 현재: 단일 모바일 레이아웃(헤더 + 4개 탭 + 층별 3열 그리드 + 바텀시트 상세).
- 변경 후: `useIsDesktop()` 분기로
  - 모바일(<1024px): 기존 JSX 그대로 유지(회귀 없음).
  - 데스크톱(≥1024px): 상단 알림 배너 + 좌 50% 매트릭스(층×DIV) + 우 50% 상세 패널(빈 상태 통계 또는 선택된 DIV 상세) 마스터-디테일 레이아웃.

Purpose: 방재실 데스크톱(1920×1080)에서 34개 DIV 포인트 상태를 한눈에 파악하고 이상치를 바로 클릭-드릴다운할 수 있게 한다. 모바일 점검 플로우는 건드리지 않는다.

Output: 수정된 `cha-bio-safety/src/pages/DivPage.tsx` 1개 파일. TypeScript `npx tsc --noEmit` 통과.

Backend/DB 변경 없음. 모든 데이터는 이미 `/api/div/pressure`, `/api/div/logs?type={drain|comp_drain|compressor}`에서 공급됨. 기존 `useQuery` 훅(`pressureRecs`, `drainLogs`, `compDrainLogs`, `oilLogs`, `selHistory`)을 그대로 공유한다.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@cha-bio-safety/src/pages/DivPage.tsx
@cha-bio-safety/src/hooks/useIsDesktop.ts
@cha-bio-safety/src/pages/ElevatorPage.tsx
@cha-bio-safety/functions/api/div/logs.ts

<interfaces>
<!-- DivPage.tsx에서 이미 정의된 심볼들. 데스크톱 브랜치는 이 심볼들을 직접 재사용한다. 새로 만들지 말 것. -->

From cha-bio-safety/src/pages/DivPage.tsx:
```typescript
// 상수 (함수 바깥, 재사용 대상)
const DIV_POINTS: readonly { floor: number; pos: number; id: string; floorLabel: string; loc: string }[]
type DivPoint = typeof DIV_POINTS[number]
const FLOOR_GROUPS: DivPoint[][]           // 층 내림차순, 각 층 안에서 pos 1→2→3
const POS_LABEL: Record<number, string>    // { 1: 'DIV #1', 2: 'DIV #2', 3: 'DIV #3' }
const STATUS_COLOR = { ok: 'var(--safe)', warn: 'var(--warn)', danger: 'var(--danger)' }

// 순수 함수
function pressureStatus(val: number, ref: number | null, alertOn: 'rise' | 'fall'): 'ok' | 'warn' | 'danger'
function daysBetween(a: string, b: string): number
function IntervalBar(props: { dates: string[]; color: string }): JSX.Element

// 타입
type Tab = 'pressure' | 'drain' | 'comp_drain' | 'compressor'

// DivPage 컴포넌트 내부 훅/상태 (데스크톱 브랜치에서 그대로 참조 가능)
const [tab, setTab] = useState<Tab>('pressure')
const [year, setYear] = useState(today.getFullYear())
const [selDiv, setSelDiv] = useState<DivPoint | null>(null)
pressureMap: Record<string, { year, month, timing, v1, v2, vc }[]>  // useMemo 결과
drainDateMap, compDrainDateMap, oilDateMap: Record<string, string[]>  // useMemo 결과
selHistory: any[]  // useQuery 결과, selDiv 변경 시 재fetch
```

From cha-bio-safety/src/hooks/useIsDesktop.ts:
```typescript
export function useIsDesktop(): boolean   // matchMedia('(min-width: 1024px)') 구독
```

From cha-bio-safety/src/pages/ElevatorPage.tsx (참조 패턴):
```typescript
// L313: const isDesktop = useIsDesktop()
// L528: if (isDesktop) { ... return <desktop JSX> }   ← 현재 return 앞에서 early-return
// 좌/우 분할: <div style={{ flex: 1, display: 'flex', overflow: 'hidden' }}> 안에
//   좌: <div style={{ flex: 1, minWidth: 0, borderRight: '1px solid var(--bd)', overflowY: 'auto', padding: '20px 24px' }}>
//   우: <div style={{ flex: 1, overflowY: 'auto', padding: '20px 24px' }}>
```

CSS 변수 팔레트 (DivPage에서 이미 사용 중):
- 배경: `var(--bg)`, `var(--bg2)`, `var(--bg3)`
- 텍스트: `var(--t1)`, `var(--t2)`, `var(--t3)`
- 테두리: `var(--bd)`
- 상태색: `var(--safe)`(녹), `var(--warn)`(앰버), `var(--danger)`(적), `var(--acl)`(강조 파랑)
- 상태 배경 투명도: `rgba(34,197,94,0.15)`(ok), `rgba(245,158,11,0.18)`(warn), `rgba(239,68,68,0.18)`(danger) — 모바일에서 이미 이 스케일로 사용 중
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 데스크톱 분기 + 좌측 매트릭스 + 상단 알림 배너 구현</name>
  <files>cha-bio-safety/src/pages/DivPage.tsx</files>
  <action>
1. 파일 상단 import 블록에 `import { useIsDesktop } from '../hooks/useIsDesktop'` 한 줄 추가.

2. `DivPage` 컴포넌트 내부, 기존 상태/훅 선언 다음 줄에 `const isDesktop = useIsDesktop()` 추가. 기존 `useState`, `useQuery`, `useMemo`는 그대로 둔다(데스크톱과 모바일 모두 공유).

3. `useMemo`로 데스크톱 전용 파생값을 계산한다. 기존 `pressureMap`, `drainDateMap`, `compDrainDateMap`, `oilDateMap` 바로 아래에 위치.
   ```typescript
   // ── 데스크톱: 전체 포인트 상태 요약 ──
   const pointStatusList = useMemo(() => {
     // 각 DIV 포인트별 { point, worstStatus, worstLabel, pctInfo, last }
     // pressureMap에서 last 2개 꺼내 pressureStatus 3회 호출 후 worst-case 산정
     // 결과: { point: DivPoint; status: 'ok'|'warn'|'danger'; worstKind: '1차압'|'2차압'|'세팅'|null; pct: number|null; last: {...}|null }[]
     return DIV_POINTS.map(point => {
       const hist = pressureMap[point.id] ?? []
       const last = hist[hist.length - 1]
       const prev = hist[hist.length - 2]
       if (!last || !prev) return { point, status: 'ok' as const, worstKind: null, pct: null, last: last ?? null }
       const cases = [
         { kind: '1차압' as const, val: last.v1, ref: prev.v1, alert: 'rise' as const },
         { kind: '2차압' as const, val: last.v2, ref: prev.v2, alert: 'fall' as const },
         { kind: '세팅' as const, val: last.vc, ref: prev.vc, alert: 'fall' as const },
       ]
       const ranked = cases.map(c => ({ ...c, s: pressureStatus(c.val, c.ref, c.alert) }))
       const sev = (s: 'ok'|'warn'|'danger') => s === 'danger' ? 2 : s === 'warn' ? 1 : 0
       ranked.sort((a, b) => sev(b.s) - sev(a.s))
       const top = ranked[0]
       const pct = top.ref && top.ref !== 0 ? Math.round(((top.val - top.ref) / top.ref) * 100) : null
       return { point, status: top.s, worstKind: top.kind, pct, last }
     })
   }, [pressureMap])

   const dangerList = pointStatusList.filter(p => p.status === 'danger')
   const warnList   = pointStatusList.filter(p => p.status === 'warn')
   const okCount    = pointStatusList.filter(p => p.status === 'ok').length
   ```

4. 데스크톱 JSX를 별도 헬퍼 함수 `renderDesktopLayout()`로 컴포넌트 내부에 선언한다(모바일 return 전, `renderDivDetail` 근처). 구조:
   ```jsx
   <div style={{ height: '100%', display: 'flex', flexDirection: 'column', overflow: 'hidden', background: 'var(--bg)' }}>
     {/* 헤더 */}
     <header style={{ flexShrink: 0, background: 'var(--bg2)', borderBottom: '1px solid var(--bd)', padding: '10px 24px', display: 'flex', alignItems: 'center', gap: 12 }}>
       <button onClick={() => navigate(-1)} …>← 뒤로</button>
       <span style={{ flex: 1, fontSize: 15, fontWeight: 700 }}>DIV 압력 관리</span>
     </header>

     {/* 상단 알림 배너 (dangerList/warnList 기반) */}
     <div style={{ flexShrink: 0, background: 'var(--bg2)', borderBottom: '1px solid var(--bd)', padding: '8px 24px', display: 'flex', alignItems: 'center', gap: 10, overflowX: 'auto' }}>
       <span style={{ fontSize: 12, fontWeight: 700, color: 'var(--t2)', flexShrink: 0 }}>
         ⚠ 이상 {dangerList.length}건 · 주의 {warnList.length}건
       </span>
       {[...dangerList, ...warnList].map(item => (
         <button key={item.point.id} onClick={() => setSelDiv(item.point as DivPoint)}
           style={{ flexShrink: 0, border: '1px solid', borderColor: item.status === 'danger' ? 'rgba(239,68,68,.4)' : 'rgba(245,158,11,.4)',
             background: item.status === 'danger' ? 'rgba(239,68,68,.12)' : 'rgba(245,158,11,.12)',
             color: item.status === 'danger' ? 'var(--danger)' : '#f59e0b',
             borderRadius: 16, padding: '4px 10px', fontSize: 11, fontWeight: 700, cursor: 'pointer' }}>
           {item.status === 'danger' ? '●' : '◐'} {item.point.id} {item.worstKind ?? ''} {item.pct != null ? `${item.pct > 0 ? '+' : ''}${item.pct}%` : ''}
         </button>
       ))}
       {dangerList.length === 0 && warnList.length === 0 && (
         <span style={{ fontSize: 11, color: 'var(--t3)' }}>모든 포인트 정상</span>
       )}
     </div>

     {/* 본문: 좌 매트릭스 / 우 상세 */}
     <div style={{ flex: 1, display: 'flex', overflow: 'hidden' }}>
       <div style={{ flex: 1, minWidth: 0, borderRight: '1px solid var(--bd)', overflowY: 'auto', padding: '20px 24px' }}>
         {/* 매트릭스: 층×DIV 테이블 */}
       </div>
       <div style={{ flex: 1, minWidth: 0, overflowY: 'auto', padding: '20px 24px' }}>
         {renderDesktopRightPanel()}  {/* Task 2에서 구현 */}
       </div>
     </div>
   </div>
   ```

5. 좌측 매트릭스 구현 (위 좌측 div 안쪽):
   - 테이블 헤더: `층 · #1 연구동 · #2 연구동 · #3 사무동` (4열 grid `80px repeat(3, 1fr)` gap 6).
   - `FLOOR_GROUPS.map(group => ...)` 각 층마다 1행 렌더.
   - 각 행: 좌측 셀은 `group[0].floorLabel`.
   - 나머지 3셀: `[1,2,3].map(pos => { const div = group.find(g => g.pos === pos); ... })` — div가 없으면 회색 박스에 `—`.
   - div가 있으면 `pointStatusList`에서 해당 status 찾아 배경색 결정:
     - danger: `background: 'rgba(239,68,68,.18)'`, border: `rgba(239,68,68,.4)`
     - warn: `background: 'rgba(245,158,11,.18)'`, border: `rgba(245,158,11,.4)`
     - ok + last 있음: `background: 'rgba(34,197,94,.12)'`, border: `rgba(34,197,94,.25)`
     - ok + last 없음(회색): `background: 'var(--bg2)'`, border: `var(--bd)`
   - 셀 내부: `{div.id}` (10px 굵게), 두 번째 줄에 `last ? ${last.month}월${timing 접미사}` : '기록 없음'` (9px, 서브텍스트색), danger/warn일 때 우상단에 `이상`/`주의` 라벨(8px).
   - 셀 `onClick={() => setSelDiv(div)}`, `cursor: 'pointer'`.
   - 선택된 셀(`selDiv?.id === div.id`)은 border 2px `var(--acl)`로 강조.
   - 매트릭스 하단에 범례: `■ 정상  ▓ 주의  █ 이상  — 해당없음` (11px, 얇은 보더).

6. 기존 모바일 return 앞에 분기를 삽입한다. 현재 `return ( <div style={{...}}> ...헤더 + 탭 + 컨텐츠... </div> )` 앞에:
   ```typescript
   if (isDesktop) {
     return (
       <>
         {renderDesktopLayout()}
         {/* 모바일 바텀시트는 데스크톱에선 사용하지 않음; 우측 패널로 대체 */}
       </>
     )
   }
   ```
   우측 패널은 Task 2에서 구현하되, 이 태스크에서는 `renderDesktopRightPanel()`을 `<div style={{ padding: 20, color: 'var(--t3)', fontSize: 12 }}>Task 2에서 구현</div>` 반환하는 자리표시자로 일단 선언해둔다 (컴파일 통과 목적).

7. 수동 확인: `npm run dev`(또는 사용자의 배포 환경)에서
   - 1024px 미만: 기존 모바일 UI 완전히 동일하게 보임(회귀 0건).
   - 1024px 이상: 헤더 + 배너 + 좌 매트릭스 + 우 자리표시자 보임. 셀 클릭 시 `selDiv` 세팅되어 콘솔에서 확인 가능(아직 우측 패널에 반영은 X).
</action>
  <verify>
    <automated>cd cha-bio-safety &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <done>
- `useIsDesktop` import 및 `isDesktop` 변수 선언 존재.
- `pointStatusList`, `dangerList`, `warnList`, `okCount` useMemo 도출 완료.
- `renderDesktopLayout()` 함수가 존재하며 헤더 + 배너 + 좌측 14행 매트릭스 + 우측 자리표시자를 렌더.
- 기존 모바일 return 앞에 `if (isDesktop) return ...` 분기 삽입됨.
- `npx tsc --noEmit` 통과.
- 1024px 경계에서 레이아웃 전환 확인(수동).
  </done>
</task>

<task type="auto">
  <name>Task 2: 우측 상세 패널 (빈 상태 통계 + 선택된 DIV 탭 뷰)</name>
  <files>cha-bio-safety/src/pages/DivPage.tsx</files>
  <action>
Task 1의 `renderDesktopRightPanel()` 자리표시자를 실제 구현으로 교체한다.

**A. 빈 상태(`selDiv === null`):**
전역 통계 요약 카드들을 렌더. Task 1에서 선언한 `pointStatusList`, `dangerList`, `warnList`, `okCount` 재사용.

구조:
```jsx
<div style={{ display: 'flex', flexDirection: 'column', gap: 16 }}>
  {/* 섹션 1: 측정점 현황 */}
  <div>
    <div style={{ fontSize: 12, fontWeight: 700, color: 'var(--t2)', marginBottom: 8 }}>◆ 34개 측정점 현황</div>
    <div style={{ display: 'grid', gridTemplateColumns: 'repeat(3, 1fr)', gap: 10 }}>
      {/* 3개 카운터 카드: 정상(okCount), 주의(warnList.length), 이상(dangerList.length) */}
      {/* 각 카드: 배경색 연하게, 큰 숫자 + 라벨 */}
    </div>
  </div>

  {/* 섹션 2: 배수/오일 현황 */}
  <div>
    <div style={{ fontSize: 12, fontWeight: 700, color: 'var(--t2)', marginBottom: 8 }}>── 배수/오일 현황 ──</div>
    <div style={{ display: 'grid', gridTemplateColumns: 'repeat(3, 1fr)', gap: 10 }}>
      {/* 챔버 배수 / 탱크 배수 / 오일 3개 요약 */}
    </div>
  </div>

  {/* 섹션 3: 이상/주의 리스트 */}
  <div>
    <div style={{ fontSize: 12, fontWeight: 700, color: 'var(--t2)', marginBottom: 8 }}>── 이상/주의 포인트 ──</div>
    {/* dangerList + warnList map, 없으면 '해당 없음' 안내 */}
  </div>
</div>
```

상세:
1. **측정점 현황 3-카운터**: 각 카드는 `{ status, count, color, bg }`로 구성. 색상:
   - 정상: `#22c55e` / `rgba(34,197,94,.12)`
   - 주의: `#f59e0b` / `rgba(245,158,11,.18)`
   - 이상: `var(--danger)` / `rgba(239,68,68,.18)`
   - 카드 내부: 큰 숫자(28px 굵게) + 서브 라벨(11px).

2. **배수/오일 현황 3카드**: 각각 `drainDateMap`, `compDrainDateMap`, `oilDateMap` 기준.
   - 전체 포인트 평균 간격: 각 div_id에 대해 `recent[-1] - recent[-2]` 일수를 모아 평균. 날짜 2개 미만이면 제외.
   - 최근 기록 날짜: 모든 날짜 중 max.
   - 카드 내부: `챔버 배수 / 탱크 배수 / 오일 보충` 라벨 + `평균 NN일` + `최근 YYYY-MM-DD` 혹은 `기록 없음`.

3. **이상/주의 리스트**: `[...dangerList, ...warnList].map(item => ...)` 없으면 `<div style={{ fontSize: 12, color: 'var(--t3)', padding: 16, textAlign: 'center' }}>이상·주의 포인트가 없습니다</div>`. 있으면 각 항목을 한 줄 row로:
   `[상태뱃지] [point.id] [point.floorLabel · point.loc] [worstKind pct%]` — 클릭 시 `setSelDiv(item.point as DivPoint)`.

**B. 선택 상태(`selDiv !== null`):**
기존 모바일 `renderDivDetail()`의 핵심 로직(차트 SVG, 수치 테이블)을 재사용하되, 오버레이/바텀시트가 아니라 인라인 패널로 렌더한다.

구조:
```jsx
<div style={{ display: 'flex', flexDirection: 'column', gap: 14, paddingBottom: 40 }}>
  {/* 헤더: 제목 + 연도 네비 + ✕ */}
  <div style={{ display: 'flex', alignItems: 'center', gap: 10 }}>
    <div>
      <div style={{ fontSize: 16, fontWeight: 700, color: 'var(--t1)' }}>
        {selDiv.floorLabel} · {selDiv.loc}
      </div>
      <div style={{ fontSize: 11, color: 'var(--t3)', marginTop: 2 }}>
        {POS_LABEL[selDiv.pos]} · {selDiv.id}
      </div>
    </div>
    {/* 연도 네비: '압력 트렌드' 탭에서만 노출 */}
    {tab === 'pressure' && (
      <div style={{ display: 'flex', alignItems: 'center', marginLeft: 'auto' }}>
        {year > 2023 && <button onClick={() => setYear(y => y - 1)}>‹</button>}
        <span>{year}</span>
        {year < new Date().getFullYear() && <button onClick={() => setYear(y => y + 1)}>›</button>}
      </div>
    )}
    <button onClick={closeDetail} style={{ marginLeft: tab === 'pressure' ? 0 : 'auto', background: 'none', border: 'none', color: 'var(--t3)', fontSize: 22, cursor: 'pointer' }}>✕</button>
  </div>

  {/* 내부 탭 (기존 tab 상태 재사용) */}
  <div style={{ display: 'flex', borderBottom: '1px solid var(--bd)' }}>
    {([
      { key: 'pressure',   label: '압력 트렌드' },
      { key: 'drain',      label: '챔버배수' },
      { key: 'comp_drain', label: '탱크배수' },
      { key: 'compressor', label: '오일' },
    ] as { key: Tab; label: string }[]).map(t => (
      <button key={t.key} onClick={() => setTab(t.key)}
        style={{ flex: 1, padding: '8px 4px', border: 'none', background: 'transparent', cursor: 'pointer',
          fontSize: 12, fontWeight: 600,
          color: tab === t.key ? 'var(--acl)' : 'var(--t3)',
          borderBottom: `2px solid ${tab === t.key ? 'var(--acl)' : 'transparent'}` }}>
        {t.label}
      </button>
    ))}
  </div>

  {/* 탭 콘텐츠 */}
  {tab === 'pressure' && renderDesktopPressureChart(selDiv)}
  {tab === 'drain'    && renderDesktopLogTimeline(selDiv, 'drain')}
  {tab === 'comp_drain' && renderDesktopLogTimeline(selDiv, 'comp_drain')}
  {tab === 'compressor' && renderDesktopLogTimeline(selDiv, 'compressor')}
</div>
```

**C. `renderDesktopPressureChart(selDiv)`**:
기존 `renderDivDetail` 내부의 차트 로직(L444~508 범위)을 거의 그대로 복사하되, 다음만 수정:
- 바깥 오버레이/바텀시트 래퍼 제거. 단순 `<div>` 안에 3개 SVG 스택.
- `W` 계산을 `const W = 600` 고정(우측 패널 너비 기준. 실제 그리드 크기는 컨테이너에 맡기고 SVG는 100% width + preserveAspectRatio 또는 고정 600px 중 선택 → 코드 단순성 위해 600px 고정, `overflowX:'auto'` 유지).
- `hist` 계산 로직(현재 연도 = 최근 12개월, 과거 연도 = 1~12월)은 그대로 재사용.
- 데이터 없을 때 `<div style={{ color: 'var(--t3)', padding: '30px 0', textAlign: 'center', fontSize: 13 }}>데이터 없음</div>` 표시.
- 기존 하단의 수치 테이블(L511~529)도 그대로 포함.

> 모바일 `renderDivDetail`은 건드리지 말 것(회귀 방지). 복사해서 새 함수를 만들 것.

**D. `renderDesktopLogTimeline(selDiv, type)`**:
해당 div_id의 날짜 배열 + `IntervalBar` + 날짜 리스트를 렌더:
```jsx
const dateMap = type === 'drain' ? drainDateMap : type === 'comp_drain' ? compDrainDateMap : oilDateMap
const dates = dateMap[selDiv.id] ?? []
const color = type === 'drain' ? '#38bdf8' : type === 'comp_drain' ? '#8b4513' : '#f97316'
const label = type === 'drain' ? '챔버 배수' : type === 'comp_drain' ? '탱크 배수' : '오일 보충'

return (
  <div style={{ display: 'flex', flexDirection: 'column', gap: 14 }}>
    <div style={{ fontSize: 11, fontWeight: 700, color, letterSpacing: '.04em' }}>{label} 간격</div>
    <div style={{ background: 'var(--bg2)', border: '1px solid var(--bd)', borderRadius: 10, padding: '14px 16px' }}>
      <IntervalBar dates={dates} color={color} />
    </div>
    <div style={{ fontSize: 11, fontWeight: 700, color: 'var(--t3)', marginTop: 6 }}>최근 기록</div>
    {dates.length === 0 ? (
      <div style={{ fontSize: 12, color: 'var(--t3)', padding: 16, textAlign: 'center' }}>기록 없음</div>
    ) : (
      <div style={{ display: 'flex', flexDirection: 'column', gap: 4 }}>
        {[...dates].reverse().slice(0, 20).map(d => (
          <div key={d} style={{ display: 'flex', gap: 10, padding: '8px 12px', background: 'var(--bg2)', borderRadius: 8, border: '1px solid var(--bd)' }}>
            <span style={{ fontSize: 12, fontFamily: 'JetBrains Mono, monospace', color: 'var(--t1)' }}>{d}</span>
          </div>
        ))}
      </div>
    )}
  </div>
)
```

**중요:** `drain`/`comp_drain`/`compressor` 탭의 `useQuery`는 `enabled: tab === '...'`로 되어있다. 데스크톱 우측 패널에서 해당 탭이 활성화되면 자동으로 fetch가 트리거된다(기존 훅 그대로 작동). 추가 enabled 조건 수정 불필요.

**E. 기존 모바일 흐름 보호:**
- `closeDetail`의 `fromNavRef.current` 기반 `navigate(-1)` 로직은 그대로 유지(모바일에서 점검 페이지→DIV 열림 플로우 호환).
- 데스크톱에서 ✕ 클릭 시에도 같은 `closeDetail`이 호출되지만, `fromNavRef.current`가 false면 단순히 `setSelDiv(null)`만 실행되므로 문제 없음.

검증:
1. `npx tsc --noEmit` 통과.
2. 데스크톱에서:
   - 초기 진입 시 우측에 통계 카드 3개 보임.
   - 왼쪽 셀 클릭 → 우측 상세 뷰로 전환, 탭 '압력 트렌드'에 3단 차트 표시.
   - 내부 탭 '챔버배수' 클릭 → 해당 div_id의 IntervalBar + 날짜 리스트.
   - 내부 탭 '탱크배수'/'오일' 동일.
   - ✕ 클릭 → 통계 뷰로 복귀.
   - 배너 칩 클릭 → 해당 DIV 선택, 우측 상세로 이동.
   - 연도 네비는 '압력 트렌드' 탭에서만 보임.
3. 모바일(~1023px) 전환 시 기존 플로우 영향 없음.
  </action>
  <verify>
    <automated>cd cha-bio-safety &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <done>
- `renderDesktopRightPanel()`이 `selDiv` null/non-null 두 경로를 모두 렌더.
- `renderDesktopPressureChart()` + `renderDesktopLogTimeline()` 헬퍼 존재, 기존 `pressureMap`/`selHistory`/`*DateMap`/`IntervalBar` 재사용.
- 내부 탭이 기존 `tab` 상태와 동기화되어 동작.
- 빈 상태 통계(정상/주의/이상 카운트, 배수/오일 평균·최근일, 이상/주의 리스트) 표시.
- 배너 칩 클릭으로 해당 DIV 선택 가능.
- `npx tsc --noEmit` 통과.
- 모바일 레이아웃 회귀 없음(수동 확인).
  </done>
</task>

</tasks>

<verification>
전체 검증:

1. **타입 체크:** `cd cha-bio-safety && npx tsc --noEmit` 에러 0.
2. **모바일 회귀:** 뷰포트 <1024px에서
   - 4개 탭(압력 트렌드/챔버배수주기/탱크배수주기/오일 주기) 그대로.
   - 층별 3열 그리드 그대로.
   - 셀 클릭 시 기존 바텀시트 상세 열림(연도 네비, 3단 차트, 수치 테이블).
   - 점검 페이지에서 `openDivId` state로 진입한 경우 자동 열림.
3. **데스크톱 신규:** 뷰포트 ≥1024px에서
   - 헤더 + 상단 알림 배너 + 좌 50% 매트릭스 + 우 50% 상세.
   - 매트릭스: 14행 × 4열(층/#1/#2/#3), 상태색 배경, 미존재 포인트 `—`.
   - 범례 표시.
   - 빈 상태 우측: 3-카운터 + 배수/오일 3카드 + 이상/주의 리스트.
   - 선택 상태 우측: 제목/연도네비/✕ + 내부 탭 + 차트/타임라인.
   - 배너 칩 → 해당 DIV 선택.
4. **반응형:** 브라우저 리사이즈(1024px 경계 넘나들기) 시 레이아웃 즉시 전환, React state 소실 없음(selDiv/tab/year 유지).
</verification>

<success_criteria>
- `cha-bio-safety/src/pages/DivPage.tsx` 수정 완료, 단일 파일 변경.
- `npx tsc --noEmit` 통과.
- 모바일(<1024px) 레이아웃/동작 회귀 0.
- 데스크톱(≥1024px) 마스터-디테일 레이아웃이 `<target_ui_spec>`에 기술된 모양 그대로 표시.
- 모든 데이터는 기존 API/훅 재사용 (새 fetch 코드 0).
- 배포는 사용자가 수동으로 `wrangler deploy --branch production` 수행.
</success_criteria>

<output>
이 플랜은 quick task이므로 별도 SUMMARY.md 작성 필요 없음(선택). 필요시 `.planning/quick/260420-ee1-div/260420-ee1-SUMMARY.md`에 간단히 완료 기록.
</output>
