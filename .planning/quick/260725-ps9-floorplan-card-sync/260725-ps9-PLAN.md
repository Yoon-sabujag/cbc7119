---
phase: quick-260725-ps9
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cha-bio-safety/src/components/inspection/familyHelpers.ts
  - cha-bio-safety/src/components/inspection/InspectionCardModal.tsx
  - cha-bio-safety/src/pages/InspectionPage.tsx
  - cha-bio-safety/src/pages/FloorPlanPage.tsx
autonomous: false
requirements: [QUICK-260725-ps9]
must_haves:
  truths:
    - "도면점검에서 소화전 마커 점검 시 check_records 에 line_results + remediation_symbol 이 저장된다"
    - "도면점검에서 완강기 마커(CP-{floor}-{n}-WK) 점검 시 check_records 에 line_results 가 저장된다"
    - "소화전 개소에 paired 비상콘센트가 있으면 비상콘센트 카드도 함께 저장된다(같은 line_results 계약)"
    - "도면 저장분이 일반점검 재진입 시 카드 done 오버레이 + line_results→faMarks 로 복원된다"
    - "소화전/비상콘센트(sheet3·4)·완강기(피난방화 sheet6) 점검일지 엑셀이 주의=△·불량=Ｘ 를 정확히 출력한다"
    - "일반점검 소화전/비상콘센트/완강기/방화셔터 동작·완료 카운팅·재방문 팝업이 무변경이다"
  artifacts:
    - path: "cha-bio-safety/src/components/inspection/familyHelpers.ts"
      provides: "InspectionPage 인라인에서 추출한 faAutoMemoFor + hydrantRemediationSymbol 공용 SSOT"
      exports: ["faAutoMemoFor", "hydrantRemediationSymbol"]
    - path: "cha-bio-safety/src/components/inspection/InspectionCardModal.tsx"
      provides: "lockToPoint 카드 모달(소화전7+paired BC7 / 완강기4). self-fetch 복원 + line_results/remediation_symbol 저장"
      exports: ["InspectionCardModal"]
    - path: "cha-bio-safety/src/pages/FloorPlanPage.tsx"
      provides: "indoor_hydrant/descending_lifeline 마커 → InspectionCardModal 라우팅 + cardSaveAdapter"
    - path: "cha-bio-safety/src/pages/InspectionPage.tsx"
      provides: "추출된 helper import 로 리팩터(동작 불변)"
  key_links:
    - from: "FloorPlanPage indoor_hydrant/descending_lifeline marker"
      to: "InspectionCardModal (lockToPoint)"
      via: "openInspectModal 분기 + revisit onClose 분기 + 마운트 블록"
      pattern: "InspectionCardModal"
    - from: "InspectionCardModal"
      to: "POST /inspections/:sid/records"
      via: "cardSaveAdapter (get-or-create session + submitRecord with line_results/remediation_symbol)"
      pattern: "submitRecord"
    - from: "InspectionCardModal"
      to: "GET /inspections/records?month=YYYY-MM"
      via: "getMonthRecords self-fetch (lineResults→faMarks 복원)"
      pattern: "getMonthRecords"
    - from: "InspectionPage / InspectionCardModal"
      to: "familyHelpers"
      via: "import faAutoMemoFor, hydrantRemediationSymbol"
      pattern: "familyHelpers"
---

<objective>
도면점검(FloorPlanPage)에서 소화전(indoor_hydrant)/완강기(descending_lifeline) 마커를 점검할 때, 일반점검(InspectionPage)의 "점검 내용 카드"와 동일한 데이터 계약(check_records.line_results JSON + remediation_symbol)으로 저장하도록 수정한다. DIV 마커가 이미 쓰는 공용 모달 lockToPoint 라우팅 선례(DivInspectModal)를 그대로 미러링한다.

Purpose: 현재 도면 저장분은 {result, memo} 스칼라만 저장 → line_results/remediation_symbol NULL → (1) 엑셀 점검일지 전부 '○'(양호) 폴백으로 법정 일지 오표기, (2) check-monthly fold 가 NULL 행으로 카드 상세 덮어씀, (3) 일반점검 재진입 시 빈 카드, (4) 조치 심볼 끊김. 두 진입점의 데이터 계약을 통일해 이 4중 발산을 제거한다.

Output:
- 신규 공용 카드 모달 InspectionCardModal.tsx (소화전/완강기/paired 비상콘센트)
- InspectionPage 인라인 헬퍼 2종 공용 추출(familyHelpers.ts, 동작 불변)
- FloorPlanPage 라우팅(indoor_hydrant/descending_lifeline → 신규 모달)
- 서버/D1/스키마 변경 0, backfill 0

LOCKED 설계 근거: 260725-ps9-CONTEXT.md (재설계 금지). 조사 완료된 근본원인·A안 결정·file:line 앵커 참조.
</objective>

<planner_findings>
## 계획 중 확정한 미결 3건 (코드/D1 조회 결과)

Q1 — 도면 전용 UI(개소 정보수정/소화기 분리) 병존 필요? → 불필요.
FloorPlanPage.tsx:1856-1888 의 개소정보 블록 + "정보 수정"/"소화기 분리" 버튼은 inspectExtDetail 이 non-null 일 때만 렌더. inspectExtDetail 은 extinguisherApi.getDetail(check_point_id) (functions/api/extinguishers/[checkPointId].ts) → SQL SELECT ... FROM extinguishers WHERE check_point_id=? AND status='active' 로만 채워짐. 소화전/완강기 check_point 에는 extinguishers 자산 행이 없어 data:null 반환 → 이 affordance 는 소화전/완강기 마커에 원래 렌더되지 않음. 편집모달의 "소화기 배치" 버튼(~1584)도 EXT_ASSET_MARKER_TYPES(소화기 4종)만 게이트 → indoor_hydrant/descending_lifeline 제외. 결론: 신규 카드 모달에 병존 불필요. (소화기 마커는 계속 generic 모달 사용 — 무변경.)

Q2 — 비상콘센트 단독 마커 존재? → 없음. 단독 라우팅 경로 불필요.
floor_plan_markers marker_type 에 emergency_outlet/비상콘센트/outlet 계열 타입 자체가 없음(존재: indoor_hydrant 107, descending_lifeline 13, div_marker 34, 나머지 유도등/소화기 계열). 활성 비상콘센트 check_point 48개 전부 매칭 소화전 location_no 보유(bc_paired=48, orphan=0). → 비상콘센트는 오직 소화전 마커의 paired 카드로만 접근. (소화전 107 활성 중 paired BC 있는 곳은 48 → BC 없는 소화전은 소화전 카드 단독 렌더. InspectionPage 미러.)

Q3 — 완강기 개소 마커 + lockToPoint 저장 경로? → 확인. 단, CONTEXT 가정 정정.
활성 descending_lifeline 마커 13개는 CP-{floor}-{n}-WK check_point 를 가리킴(CP-3F-1-WK, CP-3F-2-WK, CP-3F-3-WK, CP-5F-1/2/3-WK, CP-6F-1/2/3-WK, CP-7F-1/2/3-WK, CP-8F-1-WK = 13개 is_active=1). CONTEXT 가 언급한 CP-{floor}-완강기(및 inspectionContent['완강기'].meta.checkpoints)는 legacy(is_active=0, location_no=null)로 마커가 가리키지 않음. → 신규 모달은 마커의 selected.check_point_id 를 그대로 lockToPoint 로 저장(하드코딩된 완강기 CP 목록 사용 금지). 완강기는 pairedBC 없음(1개소 고정). 엑셀 완강기 출력은 ReportsPage.tsx:27 secondaryCategory='완강기' → 피난방화 sheet6 rows 6-9 로 category='완강기' 집계 → 이 CP 들의 line_results 를 그대로 읽음.

## 추가 확정 기술 사실 (executor 필수 참조)
- 저장 API 수용: functions/api/inspections/[sessionId]/records.ts:29,55-56,74-75,92-93 — line_results, remediation_symbol 수용 + 서버 worst 롤업(rollupResult). 컬럼·수용 이미 존재 → 서버 변경 0.
- 복원 소스: inspectionApi.getMonthRecords(month) = GET /inspections/records?month=YYYY-MM (api.ts:337). 응답은 camelCase lineResults(JSON 문자열)·remediationSymbol·memo·checkedAt·id·status (functions/api/inspections/records.ts:44-49). 파싱 패턴은 InspectionPage.tsx:3822-3837 참조.
- 저장 헬퍼: inspectionApi.submitRecord(sid, body) (api.ts:335), getSessions/createSession get-or-create 패턴 = divSaveAdapter(FloorPlanPage.tsx:387-397).
- paired BC 식별 룰: inspectionApi.getCheckpoints(floor) → sh = all.find(id===checkpointId); bc = all.find(category==='비상콘센트' && locationNo===sh.locationNo) (FloorPlanPage.tsx:457-474, InspectionPage.tsx:2375).
- 이미 공용(추출 불필요): familyCard.tsx exports FamilyACard, faLineResults, faAllResolved, faAutoMemo, faWorst, RESULT_ICONS, INSPECT_RESULT_OPTIONS, FaMark.
- 추출 필요(현재 InspectionPage 인라인 비-export): faAutoMemoFor(InspectionPage.tsx:2099-2122), hydrantRemediationSymbol(2124-2140). faAutoMemoFor 는 댐퍼(1707/1711)·소화전(2484/2655) 에서도 호출 중 → 추출 후 모든 호출부가 import 사용.
- 잘못된 소화전 피커 정정 방식: SYMPTOM_OPTIONS_BY_PLAN['extinguisher']=['받침 파손','연한 만료','직접 입력'](FloorPlanPage.tsx:63-66)은 소화기용으로는 올바름. 소화전에 오적용된 원인은 소화전이 planType='extinguisher' generic 모달을 공유했기 때문. 소화전/완강기를 카드 모달로 재라우팅하면 이 피커는 소화기 마커에만 남아 자동 정상화됨 → 이 상수를 삭제하지 말 것(삭제 시 소화기 회귀).
</planner_findings>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/quick/260725-ps9-floorplan-card-sync/260725-ps9-CONTEXT.md
@cha-bio-safety/src/components/div/DivInspectModal.tsx
@cha-bio-safety/src/components/inspection/familyCard.tsx
@cha-bio-safety/src/data/inspectionContent.ts
@cha-bio-safety/src/pages/InspectionPage.tsx
@cha-bio-safety/src/pages/FloorPlanPage.tsx
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: faAutoMemoFor + hydrantRemediationSymbol 공용 추출 (동작 불변)</name>
  <files>
    cha-bio-safety/src/components/inspection/familyHelpers.ts (신규)
    cha-bio-safety/src/pages/InspectionPage.tsx (인라인 정의 → import 교체)
  </files>
  <action>
    실행 전 grep 으로 현재 라인 재확인(앵커 ~2099/~2124, 조사시점 값): grep -n "function faAutoMemoFor|function hydrantRemediationSymbol|FAMILY_A_CATEGORIES" src/pages/InspectionPage.tsx.

    신규 familyHelpers.ts 생성: InspectionPage 인라인 faAutoMemoFor(현 2099-2122)와 hydrantRemediationSymbol(현 2124-2140) 두 함수를 문자 그대로 이동해 export 로 노출. import 는 faAutoMemo 를 './familyCard' 에서, inspectionContent 와 type InspectionItem 을 '../../data/inspectionContent' 에서, type FaMark 를 './familyCard' 에서 가져온다.

    순환 import 회피 근거: familyCard.tsx 는 타입만 import(값 import 없음), inspectionContent.ts 는 어떤 컴포넌트도 import 안 함 → familyHelpers → {familyCard, inspectionContent} 단방향, cycle 0 (familyCard.tsx:1-3 주석의 추출 선례와 동일 목적).

    InspectionPage.tsx: 위 두 함수의 인라인 정의를 삭제하고 familyHelpers 에서 import. 나머지 인라인 헬퍼(damperRemediationSymbol, fireShutterRemediationSymbol)는 신규 모달이 쓰지 않으므로 이동하지 말고 그대로 InspectionPage 에 둔다(diff 표면 최소화, 회귀 0). 모든 기존 호출부(댐퍼 1707/1711, 소화전 2484/2655)는 import 된 동일 함수를 그대로 사용 — 동작 완전 동일.

    이 task 는 순수 리팩터: 로직·시그니처·반환값 변경 금지. 회귀 0 이 필수(과거 개소명 3중저장·DIV 라벨스왑 divergence 사고 방지 — Family A 로직 SSOT 유지).
  </action>
  <verify>
    <automated>cd cha-bio-safety && npx tsc --noEmit 2>&1 | head -20 && echo TSC_OK</automated>
    <human-check>grep 로 familyHelpers.ts 가 faAutoMemoFor·hydrantRemediationSymbol 2건 export 하고, InspectionPage 에 인라인 function 정의가 사라지고 import 로 대체됐는지 확인. npm run build 통과.</human-check>
  </verify>
  <done>familyHelpers.ts 가 두 함수를 export. InspectionPage 는 인라인 정의 삭제 후 familyHelpers 에서 import. tsc 0, build 통과. 로직 변경 없음(순수 이동).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: InspectionCardModal.tsx 신규 (DivInspectModal lockToPoint 미러)</name>
  <files>
    cha-bio-safety/src/components/inspection/InspectionCardModal.tsx (신규)
  </files>
  <action>
    DivInspectModal.tsx 를 template 로 삼되, DIV 압력/트렌드/컴프레셔 로직은 전부 제거하고 Family A 카드 저장만 남긴 경량 모달을 만든다. lockToPoint 고정(zone/line/네비 없음, 단일 개소, 저장 후 onClose).

    Props:
    - category: '소화전' | '완강기'
    - checkpointId: string (마커의 selected.check_point_id 그대로 — 소화전 CP / 완강기 CP-{floor}-{n}-WK. Q3: 하드코딩 CP 목록 금지)
    - floor: string (getCheckpoints 인자)
    - onSave: (cpId, result, memo, photoKey?, extra?: { line_results?: string; remediation_symbol?: string }) => Promise<void>
    - onClose: () => void

    카드 구성(familyCard.tsx 의 FamilyACard 재사용 — 제2 사본 금지):
    - 주 카드: inspectionContent[category].items (소화전 7 / 완강기 4). faMarks 상태 + faChecked(첫 진입 전체선택) + 결과버튼(INSPECT_RESULT_OPTIONS/applyResult) + 특이사항 textarea + PhotoButton. DivInspectModal 의 faChecked/toggleItem/toggleSelectAll/applyResult/faAllChecked 패턴(626-628) 축약 재사용.
    - 소화전 특례(카드 i0/i3): i3(소화전함·호스)가 caution|bad 로 체크되면 증상 피커(경종/호스걸이/직접 입력 = inspectionContent['소화전'].special['3'].picker) 노출 + 직접입력 시 custom 텍스트. i0(위치표시등)은 고정심볼(피커 없음). auto 특이사항은 familyHelpers.faAutoMemoFor('소화전', items, marks, { hydrantPick, hydrantCustom }) 로 생성(InspectionPage.tsx:2655 미러).
    - paired 비상콘센트(소화전 + BC 매핑 있을 때만): getCheckpoints(floor) → sh=find(id===checkpointId) → bc=find(category==='비상콘센트' && locationNo===sh.locationNo) (FloorPlanPage.tsx:457-474 룰). bc 있으면 두 번째 FamilyACard(inspectionContent['비상콘센트'].items 7개) + 별도 faMarks2/faChecked2/bcMemo/bcPhoto 렌더. 없으면 소화전 카드 단독. 완강기는 pairedBC 없음.

    self-fetch 복원(재진입 카드 복원):
    - 마운트 시 getMonthRecords(현재 YYYY-MM) 호출 → entry = find(checkpointId===this cp). entry.lineResults(camelCase JSON 문자열) 파싱 → 배열 v[i] 가 normal/caution/bad 면 faMarks[i] 복원. entry.remediationSymbol 있으면 소화전 i3 피커 역매핑(InspectionPage.tsx:2471-2480 룰: special['3'].symbols 역방향; 매칭 없으면 직접입력 custom). pairedBC 도 bc entry.lineResults → faMarks2 복원. 파싱 패턴 InspectionPage.tsx:3822-3837 참조(camelCase).
    - GOTCHA(DIV 선례): FloorPlanPage 자체 evalRevisit 게이트가 재진입 팝업을 이미 처리하므로 이 모달 내부에는 재진입 팝업(useInspectionRevisitPopup) 넣지 않는다(이중 팝업 방지).

    저장(handleSave):
    - 주 카드: lineResults = faLineResults(items, faMarks); auto = faAutoMemoFor(category, items, faMarks, {hydrantPick, hydrantCustom}); finalMemo = [auto, memo].filter(Boolean).join(개행); photoKey = await photo.upload(). extra = { line_results: JSON.stringify(lineResults) }; 소화전이면 sym = hydrantRemediationSymbol(faMarks, hydrantPick, hydrantCustom); if(sym) extra.remediation_symbol = sym. await onSave(checkpointId, faWorst(faMarks), finalMemo, photoKey, extra) (InspectionPage.tsx:2652-2666 미러).
    - pairedBC: lr2 = faLineResults(bcItems, faMarks2); memo2 = [faAutoMemo(bcItems, faMarks2), bcMemo].filter(Boolean).join(개행); await onSave(bc.id, faWorst(faMarks2), memo2, bcPhotoKey, { line_results: JSON.stringify(lr2) }) (InspectionPage.tsx:2669-2673 미러; 특례 없음 일반 C/D).
    - 비원자 저장은 현행 유지(범위 밖): 주 저장 성공 후 BC 저장(별도 호출). 완료 시 onClose.
    - 저장 가능 조건: faAllResolved(items, faMarks) && (pairedBC 없거나 faAllResolved(bcItems, faMarks2)). DivInspectModal 하단 버튼 blocked 로직(970-972) 축약 재사용.

    UI chrome: fixed 풀스크린(top:var(--sat), bottom:0 = lockToPoint), 헤더 category + 개소명(floor/CP), 하단 저장/닫기 바(safe-area paddingBottom). DivInspectModal 631-986 의 lockToPoint 분기 스타일 재사용. chrome 통일 룰(메모리 inspection-modal-chrome-rules): 컨테이너 h-12/버튼 32/타이틀 18·600.

    Q1 확정: 개소 정보수정/소화기 분리 affordance 는 소화기 전용(inspectExtDetail 기반) → 이 모달에 넣지 않는다.
  </action>
  <verify>
    <automated>cd cha-bio-safety && npx tsc --noEmit 2>&1 | head -20 && echo TSC_OK</automated>
    <human-check>grep 로 InspectionCardModal 이 FamilyACard·faLineResults·getMonthRecords·hydrantRemediationSymbol·faAutoMemoFor 를 재사용/참조(제2 사본 아님)하는지 확인. npm run build 통과.</human-check>
  </verify>
  <done>InspectionCardModal 이 FamilyACard + familyHelpers/familyCard 헬퍼를 재사용해 소화전(7+paired BC7)·완강기(4) 카드를 렌더하고, getMonthRecords 로 복원, onSave 로 line_results/remediation_symbol 저장. tsc 0, build 통과. Family A 로직 복제 0.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: FloorPlanPage 라우팅 — indoor_hydrant/descending_lifeline → 신규 모달</name>
  <files>
    cha-bio-safety/src/pages/FloorPlanPage.tsx
  </files>
  <action>
    실행 전 grep 로 앵커 재확인: "openInspectModal =", "div_marker"(분기), "DivInspectModal", "marker_type !== 'div_marker'"(generic 게이트), "pairedBC".

    (a) import 추가: InspectionCardModal from '../components/inspection/InspectionCardModal'.

    (b) 상태 추가(divInspectLoc 옆 ~376 패턴 복제): const [cardInspect, setCardInspect] = useState 로 { category:'소화전'|'완강기'; checkpointId:string } | null 관리.

    (c) cardSaveAdapter 추가(divSaveAdapter ~387-397 미러, 단 extra 전달): useCallback async (cpId, result, memo, photoKey?, extra?) => { today=todayKstYmd(); sessions=await getSessions(today); sid = sessions[0]?.id ?? (await createSession({date:today})).id; await submitRecord(sid, { checkpointId:cpId, result, memo:memo||undefined, photoKey:photoKey??undefined, ...(extra??{}) }); }. divSaveAdapter 와 별개(DIV 는 div_pressures 자체 기록이라 extra 미사용, 카드 경로는 extra 필수).

    (d) 라우팅 분기 2곳에 소화전/완강기 추가(div_marker 분기 바로 아래 동일 패턴):
        - openInspectModal(~1314-1319): div_marker return 아래에 — if (selected?.marker_type === 'indoor_hydrant' && selected.check_point_id) { setCardInspect({ category:'소화전', checkpointId:selected.check_point_id }); return }. if (selected?.marker_type === 'descending_lifeline' && selected.check_point_id) { setCardInspect({ category:'완강기', checkpointId:selected.check_point_id }); return }.
        - revisit onClose 완료(가) 재점검 경로(~1786-1799): div_marker 분기 아래에 동일 두 분기 추가 + return.
        방어: check_point_id 없으면 기존 generic 경로 폴백(D1 확인상 소화전 107/완강기 13 전부 with_cp 라 실질 항상 존재).

    (e) 마운트 블록 추가(divInspectLoc 블록 ~1753-1767 바로 아래 복제): cardInspect 있을 때 InspectionCardModal 렌더 — category/checkpointId 전달, floor={floor}, onSave={cardSaveAdapter}, onClose={() => { qc.invalidateQueries({ queryKey:['floorplan-markers', floor, planType] }); setCardInspect(null); setSelected(null) }}.

    (f) generic 모달 게이트 배제(~1808): 기존 `... && selected.marker_type !== 'div_marker' && ...` 에 소화전/완강기도 배제 추가 → `&& selected.marker_type !== 'indoor_hydrant' && selected.marker_type !== 'descending_lifeline'` (div_marker 배제 선례 그대로).

    (g) dead 경로 정리(SSOT — paired BC 이중 저장 경로 제거): indoor_hydrant 가 generic 모달에 도달하지 않으므로 (1) pairedBC 식별 effect(~457-474)와 모달 닫힘 초기화 BC 부분(~477-485), (2) generic 모달 내부 paired 비상콘센트 UI(~1955-2015)와 저장부(~2061 "paired BC 동시 저장" 블록), (3) 저장버튼 disabled/label 의 pairedBcLoading 분기(~2017, ~2088)를 제거. 이들은 indoor_hydrant 전용이라 재라우팅 후 실행 불가 dead code — 남기면 divergence 트랩(constraint: Family A/paired 저장 제2 사본 금지). 관련 상태(pairedBC/pairedBcLoading/inspectBcPhoto/inspectBcResult/inspectBcMemo) 미사용 시 제거.

    (h) SYMPTOM_OPTIONS_BY_PLAN(~63-66)은 삭제하지 말 것. 재라우팅으로 이 피커는 소화기 마커에만 적용되어 자동 정상화(planner_findings Q 정정). 소화기 generic 모달 경로는 전혀 건드리지 않는다.

    회귀 방지: 유도등/소화기/DIV 마커 경로, 완료 카운팅(evalRevisit), 재방문 팝업(revisitPopup) 무변경. isExtAsset/EXT_ASSET_MARKER_TYPES 소화기 로직 무변경.
  </action>
  <verify>
    <automated>cd cha-bio-safety && npx tsc --noEmit 2>&1 | head -20 && echo TSC_OK</automated>
    <human-check>grep 로 FloorPlanPage 의 InspectionCardModal 라우팅(import+상태+어댑터+마운트+분기2곳)과 generic 게이트의 indoor_hydrant/descending_lifeline 배제, SYMPTOM_OPTIONS_BY_PLAN['extinguisher'] 존치를 확인. npm run build 통과.</human-check>
  </verify>
  <done>indoor_hydrant/descending_lifeline 마커가 InspectionCardModal 로 라우팅되고 generic 모달에서 배제됨. cardSaveAdapter 가 line_results/remediation_symbol 를 submitRecord 로 전달. dead paired-BC generic 경로 제거. 소화기 피커 존치. tsc 0, build 통과.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: 로컬 dev 헤드리스 CDP 검증 + 회귀 확인 (배포 전 게이트)</name>
  <what-built>
    도면점검 소화전/완강기 마커가 일반점검과 동일한 line_results/remediation_symbol 계약으로 저장되도록 라우팅됨. 신규 InspectionCardModal + 공용 헬퍼 추출 + FloorPlanPage 재라우팅 완료.
  </what-built>
  <how-to-verify>
    자동 게이트 먼저:
    1. cd cha-bio-safety && npx tsc --noEmit → 0 errors. npm run build → 성공.
    2. 로컬 dev(dev:front no-API 목킹 또는 dev:api) + 헤드리스 CDP(reference_local_dev_headless_cdp_verify 패턴):
       (a) 라우팅 진입: FloorPlanPage(소화기·소화전 plan) → indoor_hydrant 마커 → "점검 기록 입력" → InspectionCardModal(소화전 7카드, paired BC 있으면 비상콘센트 7카드) 렌더. descending_lifeline 마커 → 완강기 4카드 모달 렌더.
       (b) 저장 페이로드: 카드 일부를 주의/불량으로 표시 후 저장 → POST /inspections/:sid/records body 에 line_results(JSON 배열, caution/bad 반영) 포함, 소화전 i3 증상 선택 시 remediation_symbol 포함(네트워크 캡처 또는 저장 후 getMonthRecords 재조회로 lineResults/remediationSymbol 확인).
       (c) 재진입 복원: 같은 개소 재진입 → 저장 marks 가 카드에 복원 + 일반점검(InspectionPage) 소화전/완강기 진입 시 done 오버레이 + line_results 복원(교차 진입점 동기).
       (d) 엑셀 △/Ｘ: ReportsPage/ExcelPreview 로 소화전 sheet3·비상콘센트 sheet4 해당 개소 행이 주의=△·불량=Ｘ, 완강기 피난방화 sheet6 rows 6-9 정확 출력(과거 전부 ○ 폴백 → 정정).
    3. 회귀 확인(무변경 필수):
       (a) 일반점검 소화전/비상콘센트/완강기/방화셔터 카드 저장·복원 무변경(헬퍼 import 리팩터 회귀 0).
       (b) 완료 카운팅(대시보드/층별/피커 done)·재방문 팝업(useInspectionRevisitPopup) 무변경.
       (c) 소화기 마커 generic 모달 + 증상 피커(받침 파손/연한 만료) 정상, 소화기 정보수정/분리 버튼 정상.
       (d) DIV 마커 도면 점검 무변경.
    검증 결과(스크린샷/네트워크 캡처)를 사용자에게 제시. 자신감 표현("approved 주세요") 금지 — 결과만 보여주고 사용자 판단(feedback_avoid_premature_confirmation).

    배포(주의): executor 는 배포하지 않는다. 검증 통과 + 사용자 승인 후, 메인 에이전트와 사용자가 같은 턴에서 명시할 때만 수동 배포(cd cha-bio-safety 하위에서 pages deploy --branch production, --commit-message ASCII 별도). 서브에이전트 자율 배포 금지(feedback_subagent_production_deploy_forbidden). production 브랜치 직접 커밋(worktree OFF).
  </how-to-verify>
  <resume-signal>검증 결과 확인 후 "확인/이슈 설명" — 이슈 없으면 사용자가 배포 지시</resume-signal>
</task>

</tasks>

<verification>
전체 게이트(모든 task 후):
- cd cha-bio-safety && npx tsc --noEmit → 0 errors (strict:false 이나 타입 오류 0).
- npm run build → 성공(순환 import 없음 — familyHelpers 단방향).
- grep: FloorPlanPage 에 InspectionCardModal 라우팅(분기2+마운트) + generic 게이트 배제. InspectionPage 인라인 헬퍼 2종 → import 전환. SYMPTOM_OPTIONS_BY_PLAN['extinguisher'] 존치.
- 헤드리스 CDP: 라우팅 진입 / 저장 payload line_results+remediation_symbol / 재진입 복원 / 엑셀 △·Ｘ (Task 4).
- 회귀: 일반점검 소화전·비상콘센트·완강기·방화셔터·DIV·소화기 동작, 완료 카운팅, 재방문 팝업 무변경.
</verification>

<success_criteria>
- 도면점검 소화전 마커 저장 시 check_records.line_results(JSON) + (해당 시)remediation_symbol 기록.
- 도면점검 완강기 마커(CP-*-WK) 저장 시 check_records.line_results 기록.
- paired 비상콘센트(48개소) 동반 저장, orphan 라우팅 불필요(Q2).
- 일반점검 ↔ 도면점검 동일 개소 재진입 시 카드 상태 상호 복원(교차 저장 소실 제거).
- 소화전/비상콘센트/완강기 점검일지 엑셀 △/Ｘ 정확.
- 서버/D1/스키마/backfill 변경 0. Family A/paired 저장 SSOT(제2 사본 0).
- 회귀 0: 일반점검·완료 카운팅·재방문 팝업·소화기·DIV 무변경.
</success_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client(FloorPlanPage/모달) → /api/inspections/:sid/records | 카드 결과·line_results·remediation_symbol JSON 이 서버로 전달. 서버가 rollupResult 로 스칼라 result 재계산(클라 result 불신). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-ps9-01 | Tampering | InspectionCardModal 저장 payload | accept | 서버가 line_results 로 worst 롤업(records.ts:37) — 클라 result 조작 무효화. 기존 계약 그대로, 신규 위험 없음. |
| T-ps9-02 | Information Disclosure | getMonthRecords self-fetch | accept | _middleware JWT 게이트 하 기존 엔드포인트 재사용. 신규 노출면 0. |
| T-ps9-03 | Denial(data integrity) | paired BC 비원자 저장 | accept | CONTEXT 확정: 비원자 현행 유지(범위 밖). 주 저장 성공 후 BC 저장 — 부분 실패 시 재점검 보정. |
| T-ps9-SC | Tampering | npm 패키지 설치 | mitigate | 신규 의존성 0(기존 lucide/react-query/api 재사용). 설치 task 없음 → 패키지 게이트 불필요. |
</threat_model>

<output>
Create `.planning/quick/260725-ps9-floorplan-card-sync/260725-ps9-SUMMARY.md` when done.
</output>
