---
phase: quick-260612-e17
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cha-bio-safety/src/constants/divPoints.ts
  - cha-bio-safety/src/utils/generateExcel.ts
  - cha-bio-safety/migrations/0026_div_34points.sql
  - cha-bio-safety/migrations/0059_compressor.sql
autonomous: true
requirements: [QUICK-260612-e17]

must_haves:
  truths:
    - "앱 DivPage/InspectionPage 에서 B1층 DIV #2 개소명이 '식당 뒤', DIV #3 이 '화장실' 로 표시된다"
    - "자탐/DIV 엑셀 점검일지에서 B5층 -5-2 가 '휀룸2', -5-3 가 '휀룸1' 로 출력된다"
    - "재시드 시 0026/0059 시드가 현장 진실(B1: 식당뒤/화장실)과 일치한다"
    - "측정 데이터(div_pressures/comp_inspections/check_records) 는 단 한 행도 건드리지 않는다"
    - "tsc/build 가 에러 없이 통과한다"
  artifacts:
    - path: "cha-bio-safety/src/constants/divPoints.ts"
      provides: "DIV 측정점 단일 정의 — B1 loc 스왑"
      contains: "-1-2"
    - path: "cha-bio-safety/src/utils/generateExcel.ts"
      provides: "DIV_NAMES 엑셀 라벨 맵 — B5 휀룸 스왑"
      contains: "DIV_NAMES"
    - path: "cha-bio-safety/migrations/0026_div_34points.sql"
      provides: "DIV 34 측정점 시드 — B1/B5 description 스왑"
      contains: "CP-DIV--1-2"
    - path: "cha-bio-safety/migrations/0059_compressor.sql"
      provides: "컴프레셔 시드 — B1 description 스왑"
      contains: "CP-COMP--1-2"
  key_links:
    - from: "divPoints.ts DIV_POINTS"
      to: "DivPage / InspectionPage / dailyReportCalc"
      via: "DIV_POINT_LABEL 조회"
      pattern: "DIV_POINT_LABEL"
    - from: "generateExcel.ts DIV_NAMES"
      to: "DIV/자탐 엑셀 점검일지 출력"
      via: "div_id 라벨 lookup"
      pattern: "DIV_NAMES"
---

<objective>
DIV 측정점 개소명 라벨이 2026-03-28 최초 이식 때 두 곳에서 반대로 들어간 것을 현장 진실 기준으로 교정한다. 순수 문자열 스왑만 수행하며 측정 데이터·정렬·구조는 일절 변경하지 않는다.

배경(RCA 완료): B4층 동류 사고 전례 `c0a58dc`(2026-04-22). 사용자가 현장 기준 확정 —
- B1층 진실: `-1-2`(DIV #2)=식당 뒤 / `-1-3`(DIV #3)=화장실. 앱(divPoints.ts)이 반대 → 교정. 엑셀 DIV_NAMES 의 B1(`-1-2`=식당/`-1-3`=화장실)은 이미 올바름 → 무변경.
- B5층 진실: `-5-2`=2번팬룸 / `-5-3`=1번팬룸. 앱(divPoints.ts)은 이미 올바름 → 무변경. 엑셀 DIV_NAMES 의 B5(`-5-2`=휀룸1/`-5-3`=휀룸2)가 반대 → 교정.
- 시드 0026: B1·B5 description 모두 현장과 반대(B1=화장실/식당뒤, B5=1번팬룸/2번팬룸) → 4행 교정(재시드 재발 방지).
- 시드 0059: B1 COMP description 반대(화장실/식당뒤) → 2행 교정. B5 COMP(`-5-2`=2번팬룸/`-5-3`=1번팬룸)는 이미 올바름 → 무변경.

Purpose: 앱 화면과 엑셀 출력의 개소명이 현장 위치와 일치하도록 라벨을 바로잡고, 시드 재실행 시 동일 오류가 재발하지 않게 한다.
Output: 4파일 문자열 스왑 (각 2~4줄), tsc/build 통과.

범위 밖(이 plan 에서 절대 하지 않음):
- prod D1 check_points.description 4행(B1 DIV/COMP 쌍) UPDATE 는 메인 Claude 가 별도 직접 수행.
- 배포는 메인 Claude 가 직접 수행.
- 측정 데이터 무수정.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@cha-bio-safety/src/constants/divPoints.ts
@cha-bio-safety/src/utils/generateExcel.ts
@cha-bio-safety/migrations/0026_div_34points.sql
@cha-bio-safety/migrations/0059_compressor.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: 앱 소스 2파일 라벨 스왑 (divPoints B1 + generateExcel B5)</name>
  <files>cha-bio-safety/src/constants/divPoints.ts, cha-bio-safety/src/utils/generateExcel.ts</files>
  <action>
divPoints.ts (L27/L28) 의 B1 두 행 loc 값만 서로 교환한다. L27 `-1-2` 행의 `loc: '지) B1층 화장실'` 를 `loc: '지) B1층 식당 뒤'` 로, L28 `-1-3` 행의 `loc: '지) B1층 식당 뒤'` 를 `loc: '지) B1층 화장실'` 로 바꾼다. floor/pos/id/floorLabel 및 그 외 모든 행(특히 B5 의 L37 `-5-2`='지) B5층 2번팬룸' / L38 `-5-3`='지) B5층 1번팬룸')은 이미 올바르므로 절대 손대지 않는다.

generateExcel.ts DIV_NAMES (L21) 의 B5 두 항목만 서로 교환한다. `'-5-2': 'B5F 휀룸1'` 를 `'-5-2': 'B5F 휀룸2'` 로, `'-5-3': 'B5F 휀룸2'` 를 `'-5-3': 'B5F 휀룸1'` 로 바꾼다. L17 의 B1 항목(`'-1-2': 'B1F 식당'`, `'-1-3': 'B1F 화장실'`)은 이미 올바르므로 무변경. DIV_ORDER(L23-28) 와 정렬용 공백도 무변경.

키-값 매핑만 바꾸는 단순 스왑이며 코드 로직/구조 변경은 없다.
  </action>
  <verify>
    <automated>grep -A1 "id: '-1-2'" cha-bio-safety/src/constants/divPoints.ts | grep -q "식당 뒤" && grep -A1 "id: '-1-2'" cha-bio-safety/src/constants/divPoints.ts | grep -A2 "식당 뒤" | grep -q "화장실" && grep -q "'-5-2': 'B5F 휀룸2'" cha-bio-safety/src/utils/generateExcel.ts && grep -q "'-5-3': 'B5F 휀룸1'" cha-bio-safety/src/utils/generateExcel.ts && grep -q "'-1-2': 'B1F 식당'" cha-bio-safety/src/utils/generateExcel.ts && echo SWAP_OK</automated>
  </verify>
  <done>divPoints.ts -1-2 loc='지) B1층 식당 뒤' / -1-3 loc='지) B1층 화장실'. generateExcel.ts DIV_NAMES -5-2='B5F 휀룸2' / -5-3='B5F 휀룸1', -1-2='B1F 식당'/-1-3='B1F 화장실' 유지. B5 divPoints 행 및 DIV_ORDER 무변경.</done>
</task>

<task type="auto">
  <name>Task 2: 시드 SQL 2파일 description 스왑 + 빌드 게이트</name>
  <files>cha-bio-safety/migrations/0026_div_34points.sql, cha-bio-safety/migrations/0059_compressor.sql</files>
  <action>
0026_div_34points.sql 의 B1·B5 DIV 4행 description(7번째 컬럼)을 현장 진실로 교정한다 (prod 엔 이미 적용된 시드라 파일 수정의 prod 영향 0, 재시드 재발 방지 목적):
- L33 `CP-DIV--1-2` 행: `'지) B1층 화장실'` → `'지) B1층 식당 뒤'`
- L34 `CP-DIV--1-3` 행: `'지) B1층 식당 뒤'` → `'지) B1층 화장실'`
- L43 `CP-DIV--5-2` 행: `'지) B5층 1번팬룸'` → `'지) B5층 2번팬룸'`
- L44 `CP-DIV--5-3` 행: `'지) B5층 2번팬룸'` → `'지) B5층 1번팬룸'`

0059_compressor.sql 의 B1 COMP 2행 description 을 교정한다:
- L54 `CP-COMP--1-2` 행: `'지) B1층 화장실'` → `'지) B1층 식당 뒤'`
- L55 `CP-COMP--1-3` 행: `'지) B1층 식당 뒤'` → `'지) B1층 화장실'`
- B5 COMP 행(L64 `-5-2`='지) B5층 2번팬룸' / L65 `-5-3`='지) B5층 1번팬룸')은 이미 올바르므로 절대 무변경.

각 파일의 컬럼 정렬용 다중 공백은 가능한 한 보존한다(스왑으로 문자열 길이가 달라져 정렬이 약간 틀어져도 기능 무관). description 외 컬럼(point_id/qr/floor/category/name/type/div_id)은 무변경.

스왑 후 빌드 게이트 실행: divPoints.ts 가 DivPage·InspectionPage·dailyReportCalc 에서 import 되므로 타입 무결성 확인이 필수다. `cha-bio-safety` 디렉토리에서 `npx tsc --noEmit` 0 errors, `npm run build` PASS(precache 84) 를 확인한다. git diff 가 정확히 4파일·스왑 줄만 포함하는지도 점검한다.
  </action>
  <verify>
    <automated>grep -q "('CP-DIV--1-2'.*'지) B1층 식당 뒤'.*'-1-2')" cha-bio-safety/migrations/0026_div_34points.sql && grep -q "('CP-DIV--1-3'.*'지) B1층 화장실'.*'-1-3')" cha-bio-safety/migrations/0026_div_34points.sql && grep -q "('CP-DIV--5-2'.*'지) B5층 2번팬룸'.*'-5-2')" cha-bio-safety/migrations/0026_div_34points.sql && grep -q "('CP-DIV--5-3'.*'지) B5층 1번팬룸'.*'-5-3')" cha-bio-safety/migrations/0026_div_34points.sql && grep -q "('CP-COMP--1-2'.*'지) B1층 식당 뒤'.*'-1-2')" cha-bio-safety/migrations/0059_compressor.sql && grep -q "('CP-COMP--1-3'.*'지) B1층 화장실'.*'-1-3')" cha-bio-safety/migrations/0059_compressor.sql && grep -q "('CP-COMP--5-2'.*'지) B5층 2번팬룸'.*'-5-2')" cha-bio-safety/migrations/0059_compressor.sql && [ "$(git diff --stat -- cha-bio-safety/src/constants/divPoints.ts cha-bio-safety/src/utils/generateExcel.ts cha-bio-safety/migrations/0026_div_34points.sql cha-bio-safety/migrations/0059_compressor.sql | grep -c '|')" = "4" ] && (cd cha-bio-safety && npx tsc --noEmit && npm run build) && echo BUILD_OK</automated>
  </verify>
  <done>0026: CP-DIV--1-2=식당 뒤/-1-3=화장실/-5-2=2번팬룸/-5-3=1번팬룸. 0059: CP-COMP--1-2=식당 뒤/-1-3=화장실, B5 COMP 무변경. git diff 정확히 4파일. tsc 0 errors, build PASS.</done>
</task>

</tasks>

<verification>
1. `grep -A1 "id: '-1-2'" cha-bio-safety/src/constants/divPoints.ts` → -1-2 loc='지) B1층 식당 뒤', 이어 -1-3 loc='지) B1층 화장실'.
2. `grep "'-5-" cha-bio-safety/src/utils/generateExcel.ts` → '-5-2': 'B5F 휀룸2', '-5-3': 'B5F 휀룸1'.
3. `grep "CP-DIV--1-2\|CP-DIV--1-3\|CP-DIV--5-2\|CP-DIV--5-3" cha-bio-safety/migrations/0026_div_34points.sql` → -1-2=식당 뒤 / -1-3=화장실 / -5-2=2번팬룸 / -5-3=1번팬룸.
4. `grep "CP-COMP--1-2\|CP-COMP--1-3" cha-bio-safety/migrations/0059_compressor.sql` → -1-2=식당 뒤 / -1-3=화장실. B5 COMP(-5-2/-5-3) 행은 diff 에 미등장.
5. `git diff --stat` → 정확히 4파일, 변경 줄 수 각 2~4줄.
6. `cd cha-bio-safety && npx tsc --noEmit` → 0 errors.
7. `cd cha-bio-safety && npm run build` → PASS (precache 84).
</verification>

<success_criteria>
- divPoints.ts B1 loc 스왑 완료, B5 행·기타 행 무변경.
- generateExcel.ts DIV_NAMES B5 휀룸 스왑 완료, B1 항목·DIV_ORDER 무변경.
- 0026 B1·B5 DIV 4행 description 스왑 완료.
- 0059 B1 COMP 2행 description 스왑 완료, B5 COMP 무변경.
- 측정 데이터·정렬 구조 무변경. tsc 0 errors, build PASS.
- (범위 밖: prod D1 UPDATE 및 배포는 메인 Claude 직접 수행.)
</success_criteria>

<output>
Create `.planning/quick/260612-e17-div-b1-b5/260612-e17-SUMMARY.md` when done.
권장 커밋: `fix(260612-e17): DIV B1/B5 개소명 라벨 스왑 교정 — divPoints/DIV_NAMES/시드`
</output>
