---
phase: 260515-3mc-redesign-07-elevator-tsx-wave-1-list
plan: 01
type: execute
wave: 1
depends_on:
  - 260510-c2z-redesign-07-elevator-1-list-4-type-optio        # 1차 sketch (재작성판) — list 탭 4분류 구조 + ElevatorIcon 단일 진실 소스 (`elevator-sketch.html` 1993줄)
  - 260509-5xl-redesign-01-dashboard-tsx                       # Dashboard 변환 전례 — 인라인 style 화이트리스트 + 9/10/11px 0건 + lucide 통일 패턴 권위
  - 260514-i4r-redesign-02-inspection-tsx                      # InspectionPage Wave 1 — must_haves 구조 권위 + carrier 패턴 (페이지 전체가 아닌 영역 한정 변환)
  - 260515-2r5-redesign-02-inspection-tsx-wave-6-powerp        # Wave 6 PowerPanel/Parking — task별 1 함수/영역 분리 패턴 (직전 wave reference)
files_modified:
  - cha-bio-safety/src/pages/ElevatorPage.tsx
  - cha-bio-safety/src/components/ui/icons.tsx
autonomous: true
requirements:
  - REDESIGN-07-ELEVATOR-TSX-WAVE1-LIST
tags:
  - redesign
  - elevator
  - tsx-conversion
  - design-tokens-v0.1.1
  - tailwind-only
  - list-tab
  - wave1
  - elevator-icon

must_haves:
  truths:
    - "ElevatorPage 의 모든 비즈니스 로직(useQuery / useQueries / useMutation / useState / useRef / useMemo / useCallback / useEffect / 핸들러 / 네비게이션 / sessionStorage / nextInspMap / availableMonths / koelsaMonth / TABS 배열 생성 / unresolvedCount / repairCount / STATUS_STYLE / OVERALL_STYLE / INSPECT_TYPE_LABEL / RESULT_STYLE / EV_FLOORS / EV_GROUPS_FAULT / EV_GROUPS_ANNUAL / ES_NODES_FAULT / ES_NODES_ANNUAL / CHECK_ITEMS_EV / CHECK_ITEMS_ES / TKE_TEL / fetchElevators / fetchFaults / fetchKoelsaInspection / fetchKoelsaAll / fetchEvHistory / openClipReport / deleteRecord)이 100% 보존된다 (변경 전후 동작 동일, 라인 shift 만 허용)"
    - "변환 영역 1 (Wave 1 의 자체 헤더 + list 탭 본문 + 데스크톱 좌측 호기 배치도) 안에 인라인 style={{...}} 의 금지 키(color / background / backgroundColor / padding / paddingLeft|Right|Top|Bottom / margin / marginLeft|Right|Top|Bottom / fontSize / fontWeight / borderRadius / borderTopLeftRadius|... / display / flex / flexDirection / flexShrink / flexGrow / justifyContent / alignItems / alignSelf / grid / gridTemplateColumns / gridColumn / gap / rowGap / columnGap / width / height / minWidth / maxWidth / minHeight / maxHeight / overflow / overflowX|Y / textAlign / lineHeight / wordBreak / whiteSpace / textTransform / letterSpacing / opacity (정적값) / cursor / textOverflow / boxSizing / border / borderTop|... — 정적값) 사용이 0개이다 (5xl/i4r/2r5 화이트리스트와 동일 — position / top/left/right/bottom (NAV_H 등 동적) / transform (chevron rotate 동적) / transition / animation — 화이트리스트만 허용)"
    - "변환 영역 안에 9 / 10 / 11px 폰트 사이즈 사용이 0개이다 (Tailwind text-caption(12) / text-label(13) / text-body-sm(14) / text-body(16) / text-title(18) / text-heading(22) / text-display(28) 또는 12 이상 arbitrary value 만)"
    - "변환 영역 안에 옛 토큰(var(--bg) / var(--bg2) / var(--bg3) / var(--bd) / var(--bd2) / var(--acl) / var(--t1) / var(--t2) / var(--t3) / var(--safe) / var(--warn) / var(--danger) / var(--fire) / var(--info)) 사용이 0개이다 — 모두 Tailwind utility (bg-surface-raised / text-text-primary / border-border-default / bg-safe-bg / text-fire / border-safe-bar 등) 로 교체"
    - "변환 영역 안에 이모지(🛗 / 📦 / 🔲 / ↕️ / 🚨 / 🔧 / ✅) 사용이 0개 — TYPE_ICON 객체는 ReactNode/컴포넌트 매퍼(TYPE_ICON_COMPONENT 또는 인라인 분기)로 교체, 헤더 액션 버튼은 lucide(AlertTriangle / RefreshCw / 등) 또는 텍스트 라벨로 교체"
    - "ElevatorIcon (커스텀 SVG, sketch 의 passenger 카드 svg 마크업 1:1 — `<rect x=4 y=3 width=16 height=18 rx=1.5/><line x1=12 y1=3 x2=12 y2=21/><polyline points='6.5 9 8 7 9.5 9'/><polyline points='14.5 15 16 17 17.5 15'/><line x1=8 y1=7 x2=8 y2=11/><line x1=16 y1=13 x2=16 y2=17/>`, strokeWidth=1.8, 24×24 viewBox, stroke=currentColor) 가 `src/components/ui/icons.tsx` 에 추가 export 되고, 인승용 카드 + 인승용 그룹 라벨 + (sketch 의 데스크톱 사이드바 외에는 적용 안 함) 에서 사용된다. StairsIcon 패턴(stroke-only SVG 헬퍼 함수) 동일"
    - "TYPE_ICON_COMPONENT (또는 동등 매퍼) 가 type 4분류 → lucide/커스텀 컴포넌트 매핑한다: passenger=ElevatorIcon (커스텀) / cargo=Package (lucide) / dumbwaiter=UtensilsCrossed (lucide) / escalator=MoveDiagonal (lucide). 모두 회색 (text-text-secondary, 또는 fire 카드일 때 text-fire) — §6.3 카테고리 카드 일관성"
    - "list 탭 모바일 본문 — type 4분류 그룹별 카드 (라인 ~1039~1098 영역) 가 시안 권위(`elevator-sketch.html` 라인 592~810 모바일 카드 + 카드 좌측 3px 색바 `ev-bar-safe/warn/fire/mute` 클래스) 1:1 매핑. 카드 좌측 3px 색바 = 상태별 (normal=safe / fault=fire / maintenance=warning / out_of_service=mute), `before:absolute before:left-0 before:top-0 before:bottom-0 before:w-[3px] before:bg-{safe|warning|fire}-bar` 또는 동등 토큰 utility 로 구현"
    - "list 탭 모바일 카드 안의 메타 데이터 (호기번호 + 위치 + (에스컬: 공단 N호기) + 최근 점검일/점검 기록 없음 + 미해결 N건 + 우측 상태 배지 + 다음 점검 배지 + chevron-right) 가 시각만 시안과 일치하고 데이터/표시 분기(`ev.last_inspect_date ? '최근 점검: ...' : '점검 기록 없음'`, `(ev.active_faults ?? 0) > 0 && 미해결`, `ev.type === 'escalator' && ev.public_number != null && (공단 N호기)`, `ni.status === 'due_soon' && ni.daysUntil != null && D-N`, `ni.status === 'overdue' && 검사 초과`, `ni.status === 'no_record' && 기록 없음`) 는 한 줄도 변경되지 않는다 — **메모리 룰: 시안에서 손볼 건 시각 디자인뿐, 표시 분기/라벨 룰 그대로**"
    - "다음 점검 배지 (D-N / 검사 초과 / 기록 없음) 3 상태가 모두 시안 권위 토큰화 — D-N=warning bg+text, 검사 초과=danger bg+text, 기록 없음=info bg+text (현재 인라인 #fff3e0/#e65100, #ffebee/#c62828, #e3f2fd/#1565c0 hex → Tailwind bg-warning-bg/text-warning, bg-danger-bg/text-danger, bg-info-bg/text-info 또는 동등 utility 로 교체)"
    - "모바일 자체 헤더 영역 (라인 1014~1033) — 미해결 N건 칩 + 6 탭 가로 스크롤 — 이 시각만 토큰 + Tailwind 로 교체. **헤더 구조는 현재 코드 그대로 유지** (sketch 의 메뉴+페이지제목+검색 도입은 본 Wave 의 out of scope — 검색은 데이터 필터 비즈니스 로직이 없으므로 Wave 1 변환 영역에 추가하지 않음). 탭 활성 시각: bg-accent + text-text-on-accent, 비활성: bg-surface-sunken + text-text-tertiary. main 영역(라인 1036) padding/gap 도 토큰화"
    - "데스크톱 좌측 호기 배치도 영역 (`isDesktop` 분기 안의 좌측 컬럼 — 라인 626~652 — `엘리베이터` 라벨 + evGroups (4 그룹 × 호기) + `에스컬레이터` 라벨 + ess 4컬럼 그리드) + 좌측 컬럼 본체 컨테이너(라인 626 `<div style={{ flex: 1, minWidth: 0, borderRight: ..., overflowY: 'auto', padding: '20px 24px' }}>`) + renderEvCard 함수(라인 564~592) 가 시각만 토큰화 — 카드 좌측 3px 색바 추가 (`ev-bar-*` Tailwind 동등 utility), 이모지 (🛗/↕️) 제거 → lucide MoveDiagonal (에스컬 라벨) + ElevatorIcon (엘리베이터 라벨), TYPE_ICON[ev.type] (라인 580) → TYPE_ICON_COMPONENT 컴포넌트 렌더, 호기 번호/위치 텍스트 토큰화 + 미해결 N건 배지 토큰화. **renderEvCard 의 onClick / setDetailEv / selectedDesktopEv 로직 + esRow1Left/esRow1Right/esRow2 그룹핑 + evGroups.map 구조는 한 줄도 변경 금지**"
    - "데스크톱 자체 헤더 (라인 605~620) — `승강기 관리` 타이틀 + 미해결 N건 칩 + `🚨 고장 접수` / `🔧 수리 기록` 버튼 — 이 영역은 시각만 토큰화: 이모지 제거 → lucide(AlertTriangle / Wrench) , 헤더 컨테이너 토큰화, 미해결 칩 토큰화. **§6.4 그라디언트 룰** — `🚨 고장 접수` 는 위험성/긴급(fire) 의미이므로 그라디언트 CTA 허용 (linear-gradient(135deg,#991b1b,#ef4444))는 인라인 style 화이트리스트(그라디언트 + 동적값) 로 보존; `🔧 수리 기록` 도 동등 그라디언트 보존. 버튼 onClick + setSelectedEv + setModal 로직 한 줄도 변경 금지"
    - "데스크톱 우측 컬럼 (라인 654~995, desktopRightTab 영역, ElevatorInfoCard / 5 탭 / 본문 / KoelsaHistorySection / RepairListSection 등) 는 한 줄도 변경 없이 보존된다 — Wave 2+ 후속 처리"
    - "5 모달 (FaultNewModal / FaultNewFullscreen / FaultResolveModal / InspectModal / RepairNewModal / EvDetailModal) 본문 + EvSelector + EsNodeMap + EsBtn (route diagram) + ModalWrap / Field / EmptyState / CertSummary / CertBlock / FindingsPanel / FindingCountBadge / CertViewerModal / KoelsaHistorySection / RepairListSection / ElevatorInfoCard 컴포넌트 본체는 한 줄도 변경 없이 보존된다 — Wave 2+ 후속"
    - "다른 탭 본문 (모바일 tab === 'fault' / 'repair' / 'inspect' / 'annual' / 'safety') 은 한 줄도 변경 없이 보존된다 — Wave 2+ 후속"
    - "icons.tsx 추가 export (ElevatorIcon) 외에는 다른 컴포넌트(StairsIcon / ShutterIcon / ExitSignIcon / SmokeVentIcon / HoseReelIcon / FireExtinguisherCustom / IconProps / StrokeSvg / baseStrokeProps) 본체는 한 줄도 변경 없이 보존된다"
    - "다른 페이지 (.tsx) / 컴포넌트 / hooks / utils / API / 라우팅 / D1 스키마 / DesignPage / App.tsx / BottomNav / DesktopSidebar / GlobalHeader / Tailwind config / tokens.css 수정 0건"
    - "TypeScript 컴파일 0 에러, `npm run build` (또는 `tsc -p tsconfig.json --noEmit`) 통과. cbc7119-preview.pages.dev 자동 배포 대상이지만 본 Wave 의 verify 는 빌드 통과까지 — 시각 검수는 사용자가 push 후 별도 진행"
  artifacts:
    - path: "cha-bio-safety/src/components/ui/icons.tsx"
      provides: "ElevatorIcon (커스텀 SVG, sketch 1:1 path) 추가 export. StairsIcon 패턴(stroke-only) 동일. IconProps 호환. 기존 6개 export(StairsIcon / ShutterIcon / ExitSignIcon / SmokeVentIcon / HoseReelIcon / FireExtinguisherCustom) + IconProps / StrokeSvg / baseStrokeProps 보존"
      min_lines: 145
      contains: "export function ElevatorIcon"
    - path: "cha-bio-safety/src/pages/ElevatorPage.tsx"
      provides: "Wave 1 변환 완료 — list 탭 (모바일 본문 라인 ~1039~1098 + 모바일 자체 헤더 라인 ~1014~1033) + 데스크톱 자체 헤더 (라인 ~605~620) + 데스크톱 좌측 호기 배치도 컬럼 (라인 ~564~652, renderEvCard + evGroups + ess 그리드) 가 v0.1.1 토큰 + Tailwind only. lucide-react import 블록 신설(Package / UtensilsCrossed / MoveDiagonal / ChevronRight / AlertTriangle / RefreshCw / Wrench 등) + ElevatorIcon import. TYPE_ICON 이모지 객체는 보존 (다른 탭/모달이 의존) 또는 TYPE_ICON_COMPONENT 별도 매퍼 신설. 5 모달 + 다른 탭 본문 + 데스크톱 우측 컬럼 + KoelsaHistorySection 호출 + RepairListSection 호출 + ElevatorInfoCard 호출 + 비즈니스 로직 100% 보존."
      min_lines: 3200
      contains: "TYPE_ICON, STATUS_STYLE, EV_FLOORS, EV_GROUPS_FAULT, EV_GROUPS_ANNUAL, ES_NODES_FAULT, ES_NODES_ANNUAL, CHECK_ITEMS_EV, CHECK_ITEMS_ES, ElevatorPage, renderEvCard, FaultNewModal, FaultNewFullscreen, FaultResolveModal, RepairNewModal, EvDetailModal, KoelsaHistorySection, RepairListSection, ElevatorInfoCard, ElevatorIcon, MoveDiagonal, Package, UtensilsCrossed, ChevronRight"
  key_links:
    - from: "cha-bio-safety/src/pages/ElevatorPage.tsx (Wave 1 변환 영역)"
      to: "tailwind.config.js fontSize tokens (caption ~ display) + colors (status / text / surface / border / accent)"
      via: "Tailwind utility 클래스 (text-caption / text-label / text-body-sm / text-body / text-title / bg-surface-raised / text-text-primary / border-border-default / bg-{safe|warning|danger|fire|info}-bg / text-{safe|warning|danger|fire|info} / border-{safe|warning|danger|fire|info}-bar / bg-accent / text-text-on-accent)"
      pattern: "text-(caption|label|body-sm|body|title|heading|display)|bg-surface-(page|raised|sunken|active)|text-text-(primary|secondary|tertiary|on-accent)|border-border-(default|strong|focus)|(bg|text|border)-(safe|warning|danger|info|fire)(-bar|-bg)?|bg-accent|text-text-on-accent"
    - from: "cha-bio-safety/src/pages/ElevatorPage.tsx (Wave 1 변환 영역)"
      to: "cha-bio-safety/src/components/ui/icons.tsx (ElevatorIcon)"
      via: "named import (ElevatorIcon 신설 + 기존 6종 export 보존)"
      pattern: "import\\s*\\{[^}]*ElevatorIcon[^}]*\\}\\s*from\\s+['\"]\\.\\./components/ui/icons['\"]"
    - from: "cha-bio-safety/src/pages/ElevatorPage.tsx (TYPE_ICON_COMPONENT 매퍼)"
      to: "lucide-react (Package / UtensilsCrossed / MoveDiagonal / ChevronRight / AlertTriangle / RefreshCw / Wrench / etc) + icons.tsx (ElevatorIcon)"
      via: "이모지(🛗📦🔲↕️🚨🔧✅) 제거 → lucide/커스텀 컴포넌트로 교체. passenger=ElevatorIcon / cargo=Package / dumbwaiter=UtensilsCrossed / escalator=MoveDiagonal"
      pattern: "<(ElevatorIcon|Package|UtensilsCrossed|MoveDiagonal|ChevronRight|AlertTriangle|RefreshCw|Wrench)\\s+(size|className)"
    - from: "cha-bio-safety/src/pages/ElevatorPage.tsx (list 탭 카드 좌측 3px 색바)"
      to: "design-system.md v0.1.1 §6.1 Progress Color Rule + §6.3 카테고리 카드"
      via: "ev.status (normal/fault/maintenance/out_of_service) → safe/fire/warning/mute 색바 매핑. `before:absolute before:left-0 before:top-0 before:bottom-0 before:w-[3px] before:bg-{token}-bar before:rounded-l-md` 또는 동등 Tailwind utility"
      pattern: "before:w-\\[3px\\]\\s+before:bg-(safe|warning|danger|fire)-bar|before:bg-text-tertiary"
    - from: "cha-bio-safety/src/pages/ElevatorPage.tsx (renderEvCard 함수 + 데스크톱 좌측 컬럼)"
      to: "selectedDesktopEv 동적 highlight + detailEv setter + ev.status fault 분기"
      via: "onClick={() => setDetailEv(ev)} / isSel = selectedDesktopEv?.id === ev.id / border 색 동적 (selected accent / fault danger-bar / default border-default)"
      pattern: "setDetailEv\\(ev\\)|selectedDesktopEv\\?\\.id\\s*===\\s*ev\\.id|ev\\.status\\s*===\\s*'fault'"
    - from: "cha-bio-safety/src/pages/ElevatorPage.tsx (Wave 1 변환 영역)"
      to: "비즈니스 로직 (한 줄도 변경 금지)"
      via: "TYPE_ICON / TYPE_LABEL 객체 그대로 보존(다른 탭/모달에서 사용) / TYPE_ICON_COMPONENT 신설 매퍼는 별도 / setDetailEv + setModal('ev_detail') 호출 / nextInspMap.get / setSelectedEv + setModal('fault_new' | 'repair_new') 호출 / unresolvedCount / TABS 배열 / setTab"
      pattern: "setDetailEv\\(ev\\);\\s*setModal\\('ev_detail'\\)|nextInspMap\\.get\\(ev\\.id\\)|setSelectedEv\\(selectedDesktopEv\\);\\s*setModal\\('(fault_new|repair_new)'\\)|setTab\\(t\\.key\\)"
---

<objective>
ElevatorPage TSX 변환 **Wave 1** — 1차 sketch(`elevator-sketch.html` 1993줄, c2z 재작성판) 을 단일 진실 소스로 삼아 `cha-bio-safety/src/pages/ElevatorPage.tsx` (3209줄) 의 **list 탭 영역(모바일 + 데스크톱 좌측 컬럼) + 자체 헤더(모바일 + 데스크톱) + ElevatorIcon 신설** 을 v0.1.1 디자인 토큰 + Tailwind only 로 교체한다. 5 모달 + 다른 탭 본문 + 데스크톱 우측 컬럼은 후속 Wave 2+ 에서 처리.

**Purpose:**
- 5xl Dashboard / i4r Inspection Wave 1 / 2r5 PowerPanel-Parking 의 변환 패턴(인라인 style 금지 키 0건 + 9/10/11px 0건 + 옛 토큰 0건 + 이모지 0건 + lucide+커스텀 아이콘 통일 + tokens.css 자동 라이트/다크 분기 + §6.1 Progress Color Rule + §6.3 카테고리 카드 일관성 + §6.5 hover + §6.7 그림자 X + §7.1 아이콘 일관성) 을 ElevatorPage 의 가장 빈도 높은 진입점인 list 탭에 정착
- **ElevatorIcon (커스텀 SVG)** 신설 — sketch 의 passenger 카드 안에 임베디드된 SVG path (rect/line/polyline 6개) 를 icons.tsx 에 `ElevatorIcon` 함수로 추출. StairsIcon 패턴(stroke-only 헬퍼) 동일. 인승용 카드(8대) + 인승용 그룹 라벨에서 사용. 모든 다른 type 은 lucide(Package / UtensilsCrossed / MoveDiagonal) — sketch 의 결정(c2z 후 사용자 수정으로 ArrowUpDown → ElevatorIcon 으로 전환된 최신 상태) 1:1 반영
- type 4분류 (passenger 8 / cargo 2 / dumbwaiter 1 / escalator 6) 카드 좌측 3px 색바 (§6.1) 도입 — 상태별 색 매핑 (normal=safe / fault=fire / maintenance=warning / out_of_service=mute). c2z sketch 의 `ev-bar-safe/warn/fire/mute` 클래스를 Tailwind `before:w-[3px] before:bg-{token}-bar` 동등 utility 로 구현
- list 탭이 ElevatorPage 의 entry point — 4인 방재팀이 가장 자주 보는 화면. Wave 1 에서 시각이 정착되어야 후속 Wave 2+ (5 모달) 변환 시 spawned 색/spacing/typography 가 흔들리지 않음
- Wave 2+ (5 모달 + 다른 탭 본문 + 데스크톱 우측 컬럼 + KOELSA 이력) 후속 트랙의 fixed reference point 확보

**Output:**
- 비즈니스 로직 100% 보존하면서 Wave 1 변환 영역의 마크업/스타일이 sketch 와 1:1 매칭된 새 `ElevatorPage.tsx`
- `src/components/ui/icons.tsx` 에 `ElevatorIcon` 추가 (기존 6 export 보존)
- 5 모달 함수 (FaultNewModal / FaultNewFullscreen / FaultResolveModal / RepairNewModal / EvDetailModal) + 다른 탭 본문 (fault/repair/inspect/annual/safety) + 데스크톱 우측 컬럼 (desktopRightTab 영역) + KoelsaHistorySection 호출 + RepairListSection 호출 + ElevatorInfoCard 컴포넌트 + EvSelector + EsNodeMap + EsBtn (모달 안에서만 사용) 은 **인라인 style 그대로 보존** (다음 Wave 트랙에서 변환)

**Out of scope (Wave 2+ follow-up):**
- 5 모달 본문 (`FaultNewModal` / `FaultNewFullscreen` / `FaultResolveModal` / `RepairNewModal` / `EvDetailModal` / `InspectModal`) — 인라인 style 보존
- `EvSelector` (5그룹 호기 선택) + `EsNodeMap` (에스컬레이터 노선도) + `EsBtn` — 모달 안에서만 사용, 보존
- 다른 탭 본문 (모바일 `tab === 'fault' / 'repair' / 'inspect' / 'annual' / 'safety'`) — 인라인 style 보존
- 데스크톱 우측 컬럼 (`desktopRightTab` 영역, ElevatorInfoCard / 5 탭 / 본문) — 인라인 style 보존
- `KoelsaHistorySection` (별도 컴포넌트) — 호출만 보존, 본체 수정 금지
- `RepairListSection` (별도 컴포넌트) — 호출만 보존, 본체 수정 금지
- `ElevatorInfoCard` (검사성적서 상단 정보 카드) — 호출만 보존, 본체 수정 금지
- `CertViewerModal` / `FindingCountBadge` / `FindingsPanel` / `ModalWrap` / `Field` / `EmptyState` / `CertSummary` / `CertBlock` 컴포넌트 — 본체 수정 금지
- 비즈니스 로직 (useQuery / useMutation / useState / useRef / useMemo / useCallback / useEffect / 핸들러 / sessionStorage / nextInspMap / STATUS_STYLE 매핑 / OVERALL_STYLE / RESULT_STYLE / API 호출 / route navigation / TKE_TEL / openClipReport / deleteRecord) 100% 보존
- 다른 페이지 (.tsx) / 컴포넌트 / hooks / utils / API / 라우팅 / D1 스키마 / DesignPage / App.tsx / BottomNav / DesktopSidebar / GlobalHeader / Tailwind config / tokens.css 수정 금지

**핵심 메모리 룰 강제 (작업 전 재확인):**
- **"디자인 변경 전 상의 필수"** — sketch 권위. 시안과 다른 새 디자인 결정 시 사용자 컨펌 필요. 본 Wave 1 은 sketch 가 결정한 것 (list 탭 4분류 / ElevatorIcon / 카드 좌측 3px 색바) 만 반영
- **"디자인 조정은 시안 먼저"** — `elevator-sketch.html` 1993줄이 마크업/색/구조 단일 진실 소스. 단, 본 Wave 1 의 헤더는 **현재 코드 구조 유지 + 토큰만 교체** (sketch 의 메뉴+페이지제목+검색 도입은 비즈니스 로직 추가가 필요하므로 Wave 1 out of scope — 변환만 한다)
- **"재디자인 시안은 디자인만 — 표시 분기/라벨 룰(없음/N개/X-Y/완료)은 코드 그대로"** — 다음 점검 배지 (D-N / 검사 초과 / 기록 없음), 미해결 N건, (공단 N호기), 최근 점검: ... / 점검 기록 없음 분기는 한 줄도 변경 금지. 시각만 토큰화
- **"cbc7119 = 디자인 격리 리포"** — 본 변환은 cbc7119-design 에서만 수행. 원본 PWA(cha-bio-safety) 영향 없음. 단, 본 리포는 cha-bio-safety 폴더가 worktree 안에 있으므로 cbc7119 내부의 cha-bio-safety/src/pages/ElevatorPage.tsx 를 수정
- **"PWA 캐시가 배포 무시함"** — ElevatorPage.tsx 본체 수정 → chunk hash 자동 변경. 빌드 후 `dist/assets/ElevatorPage-*.js` 파일명 hash 차이 확인 권장
- **"운영 관찰 모드"** — 신규 기능 추가 금지. 본 Wave 1 은 시각만 교체 (비즈니스 로직 0 변경). 새로운 데이터/필터/검색 추가 금지

**핵심 메모리 룰 — 작업하지 말 것 (sketch 와 코드 차이 인식):**
- sketch 모바일 헤더는 메뉴 아이콘 + "방재팀 시설관리"/"승강기 관리" 타이틀 + 새로고침/미해결 액션 + 검색 입력 + 6탭 — **본 Wave 1 은 검색 입력 / 메뉴 아이콘 / 타이틀 도입 안 함** (비즈니스 로직 추가 = scope creep). 현재 코드의 헤더(미해결 칩 + 6탭) 구조 그대로 유지, 시각만 토큰화
- sketch 모바일 본문 첫 영역에 "호기별 통계 4종 (정상/고장/점검중/운행중지) Stat Card" 가 있음 — **본 Wave 1 은 도입 안 함** (통계 계산 비즈니스 로직 추가 = scope creep). 카드 리스트만 변환
- sketch 데스크톱은 **단일 컬럼** (사이드바 + 헤더 + 본문 = 4열 그리드) 인 반면 현재 코드는 **2분할 컬럼** (좌=배치도 / 우=호기별 이력 탭). 본 Wave 1 은 **좌측 컬럼만 토큰화 + 4열 그리드 유지 안 함** (현재 코드의 evGroups flexbox 가로 배치를 그대로 유지). 데스크톱 레이아웃 자체 재구성은 별도 Wave (사용자 컨펌 필요)
- 즉, **헤더/레이아웃 구조 자체는 그대로 두고, 토큰/색/아이콘만 v0.1.1 정합** — 4 컬럼 그리드 도입, 메뉴 아이콘 추가, 통계 카드 추가, 검색 입력 추가 등은 Wave 2+ 또는 별도 후속에서 사용자 컨펌 받고 처리
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

# Sketch (단일 진실 소스)
@cha-bio-safety/docs/redesign-context/07-elevator/sketch/elevator-sketch.html

# 변환 대상
@cha-bio-safety/src/pages/ElevatorPage.tsx

# 커스텀 아이콘 (ElevatorIcon 추가 대상)
@cha-bio-safety/src/components/ui/icons.tsx

# 디자인 시스템 v0.1.1
@cha-bio-safety/docs/redesign-context/00-design-context/design-system.md
@cha-bio-safety/tailwind.config.js

# Carrier 패턴 (직전 wave reference — task별 1 영역 분리)
@.planning/quick/260515-2r5-redesign-02-inspection-tsx-wave-6-powerp/260515-2r5-PLAN.md
@.planning/quick/260515-2r5-redesign-02-inspection-tsx-wave-6-powerp/260515-2r5-SUMMARY.md

# c2z sketch 재작성 결정 (4분류 / ElevatorIcon Out-of-scope)
@.planning/quick/260510-c2z-redesign-07-elevator-1-list-4-type-optio/260510-c2z-SUMMARY.md

<interfaces>
<!-- Wave 1 변환 영역에서 사용할 lucide + 커스텀 아이콘 컨트랙트 -->

From cha-bio-safety/src/components/ui/icons.tsx (현재):
```typescript
export interface IconProps extends Omit<SVGProps<SVGSVGElement>, 'color' | 'size'> {
  size?: number | string;
  color?: string;
  strokeWidth?: number | string;
  style?: CSSProperties;
  className?: string;
}
// 기존 export: StairsIcon, ShutterIcon, ExitSignIcon, SmokeVentIcon, HoseReelIcon, FireExtinguisherCustom
// 신설: export function ElevatorIcon(props: IconProps)
```

ElevatorIcon SVG path (sketch elevator-sketch.html 라인 595 1:1 추출):
```html
<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round">
  <rect x="4" y="3" width="16" height="18" rx="1.5"/>
  <line x1="12" y1="3" x2="12" y2="21"/>
  <polyline points="6.5 9 8 7 9.5 9"/>
  <polyline points="14.5 15 16 17 17.5 15"/>
  <line x1="8" y1="7" x2="8" y2="11"/>
  <line x1="16" y1="13" x2="16" y2="17"/>
</svg>
```

From lucide-react (Wave 1 신규 import):
```typescript
import {
  Package,           // 화물용
  UtensilsCrossed,   // 덤웨이터
  MoveDiagonal,      // 에스컬레이터
  ChevronRight,      // 카드 우측 화살표 (현재 인라인 SVG 교체)
  AlertTriangle,     // 미해결 N건 칩 (sketch badge-danger 액션)
  RefreshCw,         // 새로고침 (sketch 데스크톱 헤더 — 본 Wave 도입 안 함, 보류)
  Wrench,            // 데스크톱 헤더 🔧 수리 기록 버튼
} from 'lucide-react';
```

From cha-bio-safety/src/pages/ElevatorPage.tsx (변경 금지 — 비즈니스 로직):
```typescript
// 라인 191~199 — 보존 (다른 탭/모달이 의존)
const TYPE_ICON:  Record<string,string> = { passenger:'🛗', cargo:'📦', dumbwaiter:'🔲', escalator:'↕️' }
const TYPE_LABEL: Record<string,string> = { passenger:'인승용', cargo:'화물용', dumbwaiter:'덤웨이터', escalator:'에스컬레이터' }
const STATUS_STYLE: Record<string,{ label:string; color:string; bg:string }> = { /* 4 status */ }

// 신설 (Wave 1 변환 영역에서만 사용 — TYPE_ICON 보존 + 별도 컴포넌트 매퍼)
const TYPE_ICON_COMPONENT: Record<string, React.ComponentType<{ size?: number; className?: string }>> = {
  passenger:  ElevatorIcon,
  cargo:      Package,
  dumbwaiter: UtensilsCrossed,
  escalator:  MoveDiagonal,
}

// list 탭 모바일 본문 (라인 1039~1098) — 4분류 카드 리스트
{tab === 'list' && (<>{['passenger','cargo','dumbwaiter','escalator'].map(type => ...)}</>)}

// 다음 점검 배지 비즈니스 로직 (보존):
// ni.status === 'due_soon' && ni.daysUntil != null → "D-N" warning
// ni.status === 'overdue' → "검사 초과" danger
// ni.status === 'no_record' → "기록 없음" info

// 데스크톱 분기 (라인 545):
if (isDesktop) {
  const evs  = elevators.filter(e => e.type !== 'escalator').sort(...)
  const ess  = elevators.filter(e => e.type === 'escalator').sort(...)
  const evGroups = [...]   // 4 그룹 (1-3, 4-6, 7-8, 9-11)
  const esRow1Left  = ess.filter(e => e.number === 3 || e.number === 4)
  const esRow1Right = ess.filter(e => e.number === 5 || e.number === 6)
  const esRow2      = ess.filter(e => e.number === 1 || e.number === 2)
  const renderEvCard = (ev: Elevator) => { ... }   // 라인 564~592 — 시각 토큰화 대상
  ...
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ElevatorIcon icons.tsx 추가 + lucide import 블록 + TYPE_ICON_COMPONENT 매퍼 신설</name>
  <files>cha-bio-safety/src/components/ui/icons.tsx, cha-bio-safety/src/pages/ElevatorPage.tsx</files>
  <action>
**Step 1 — icons.tsx 에 ElevatorIcon 추가:**

`cha-bio-safety/src/components/ui/icons.tsx` 끝(라인 134 `FireExtinguisherCustom` 함수 다음) 에 `ElevatorIcon` 함수를 추가한다. StairsIcon 패턴(직접 SVG 마크업 — StrokeSvg 헬퍼 사용 X, fill 처리 필요 없음) 이 아니라 **StrokeSvg 헬퍼 사용 패턴** (ShutterIcon / ExitSignIcon 동일) 으로 작성 — 모든 요소가 stroke-only 이기 때문. 단 sketch SVG 는 strokeWidth=1.8 인 반면 StrokeSvg 의 기본은 2 이므로 IconProps 의 `strokeWidth` 를 호출 측에서 1.8 로 명시 가능하도록 props 그대로 통과시킨다 (기본값 2 유지, 호출 측에서 override).

추가 코드 (icons.tsx 끝, FireExtinguisherCustom 함수 다음):

```typescript
/** 인승용 엘리베이터 — 본체 프레임 + 중앙 분할선 + 위/아래 방향 화살표 (열림/닫힘 메타포). sketch elevator-sketch.html 라인 595 1:1 추출. strokeWidth 기본 1.8 권장. */
export function ElevatorIcon({ strokeWidth = 1.8, ...props }: IconProps) {
  return (
    <StrokeSvg strokeWidth={strokeWidth} {...props}>
      <rect x={4} y={3} width={16} height={18} rx={1.5} />
      <line x1={12} y1={3} x2={12} y2={21} />
      <polyline points="6.5 9 8 7 9.5 9" />
      <polyline points="14.5 15 16 17 17.5 15" />
      <line x1={8} y1={7} x2={8} y2={11} />
      <line x1={16} y1={13} x2={16} y2={17} />
    </StrokeSvg>
  );
}
```

기존 6 export(StairsIcon / ShutterIcon / ExitSignIcon / SmokeVentIcon / HoseReelIcon / FireExtinguisherCustom) + IconProps / StrokeSvg / baseStrokeProps 는 한 줄도 변경하지 않는다. 라인 shift 만 허용.

**Step 2 — ElevatorPage.tsx 의 import 블록 보강 (라인 1~15 영역):**

기존 import 블록 안에 lucide-react + icons.tsx 의 ElevatorIcon import 추가. 현재 ElevatorPage 에는 lucide import 가 없으므로 신규 import 라인 1줄 + 기존 ElevatorIcon import 1줄 추가. **import 추가 위치는 라인 16 (마지막 import) 다음** — 기존 import 순서 그대로 보존.

추가 import (라인 16 다음):

```typescript
import { Package, UtensilsCrossed, MoveDiagonal, ChevronRight, AlertTriangle, Wrench } from 'lucide-react'
import { ElevatorIcon } from '../components/ui/icons'
```

(RefreshCw 는 본 Wave 1 에서 사용 안 함 — 데스크톱 새로고침 버튼은 sketch 가 도입한 것이지만 현재 코드에 새로고침 UI 가 없으므로 도입 안 함. Wave 2+ 에서 사용자 컨펌 받고 추가)

**Step 3 — TYPE_ICON_COMPONENT 매퍼 추가 (라인 192 `TYPE_ICON` 다음):**

기존 `TYPE_ICON` 객체(라인 192) 는 **다른 탭/모달이 의존하므로 보존**한다. 별도로 컴포넌트 매퍼 객체를 추가:

```typescript
// Wave 1 신설 — list 탭 카드 + 그룹 라벨용 컴포넌트 매퍼 (TYPE_ICON 이모지 객체와 병행 사용. 이모지는 다른 탭/모달에서 계속 사용됨)
const TYPE_ICON_COMPONENT: Record<string, React.ComponentType<{ size?: number; className?: string; strokeWidth?: number | string }>> = {
  passenger:  ElevatorIcon,
  cargo:      Package,
  dumbwaiter: UtensilsCrossed,
  escalator:  MoveDiagonal,
}
```

**Step 4 — 검증:**

본 task 는 list 탭 본문 / 헤더 / 데스크톱 좌측 컬럼 JSX 는 아직 손대지 않는다 (다음 task). 본 task 는:
- icons.tsx 에 ElevatorIcon export 1 추가
- ElevatorPage.tsx 에 lucide import 1줄 + icons.tsx import 1줄 추가
- TYPE_ICON_COMPONENT 매퍼 객체 1개 추가

TypeScript 컴파일 0 에러 확인. 기존 동작(빌드 통과) 보존.
  </action>
  <verify>
    <automated>cd cha-bio-safety && grep -c "^export function ElevatorIcon" src/components/ui/icons.tsx | tee /tmp/3mc-t1-1.txt && [ "$(cat /tmp/3mc-t1-1.txt)" = "1" ] && grep -c "TYPE_ICON_COMPONENT" src/pages/ElevatorPage.tsx | tee /tmp/3mc-t1-2.txt && [ "$(cat /tmp/3mc-t1-2.txt)" -ge "1" ] && grep -c "from 'lucide-react'" src/pages/ElevatorPage.tsx | tee /tmp/3mc-t1-3.txt && [ "$(cat /tmp/3mc-t1-3.txt)" -ge "1" ] && grep -c "ElevatorIcon.*from.*icons" src/pages/ElevatorPage.tsx | tee /tmp/3mc-t1-4.txt && [ "$(cat /tmp/3mc-t1-4.txt)" -ge "1" ] && npx tsc -p tsconfig.json --noEmit 2>&1 | head -30 | tee /tmp/3mc-t1-tsc.txt && ! grep -E "error TS[0-9]+" /tmp/3mc-t1-tsc.txt</automated>
  </verify>
  <done>icons.tsx 에 ElevatorIcon export 1 추가 (StrokeSvg 패턴, sketch SVG path 1:1). ElevatorPage.tsx 에 `from 'lucide-react'` import 1줄 + `ElevatorIcon ... from '../components/ui/icons'` import 1줄 + `TYPE_ICON_COMPONENT` 매퍼 객체 1개 추가. 기존 export(StairsIcon 등 6종) + TYPE_ICON 이모지 객체 + 다른 import 0 변경. tsc 0 에러.</done>
</task>

<task type="auto">
  <name>Task 2: list 탭 모바일 본문(라인 ~1039~1098) + 모바일 자체 헤더(라인 ~1014~1036) 토큰화</name>
  <files>cha-bio-safety/src/pages/ElevatorPage.tsx</files>
  <action>
**변환 영역:** 라인 1011 `// ── 모바일 ─────` 부터 라인 1098 `</>)` 까지. 즉:
- 라인 1012~1013 — 모바일 페이지 컨테이너 (`<div style={{ flex:1, minHeight:0, display:'flex', flexDirection:'column', overflow:'hidden' }}>`)
- 라인 1014~1033 — 모바일 자체 헤더(`<header>`) — 미해결 N건 칩 + 6 탭 가로 스크롤
- 라인 1035~1036 — `<main>` 컨테이너 + 본문 시작
- 라인 1039~1098 — `{tab === 'list' && (<>...</>)}` 블록 (type 4분류 그룹별 카드 리스트)

**보존 영역:** 라인 1101 이후 (`{tab === 'fault' && ...}` 등 다른 탭 본문) 은 **한 줄도 변경 금지**.

**Step 1 — 모바일 페이지 컨테이너 + 헤더 + main 토큰화:**

`<div style={{...}}>` (라인 1013) →
```tsx
<div className="flex-1 min-h-0 flex flex-col overflow-hidden">
```

`<header style={{...}}>` (라인 1015) →
```tsx
<header className="flex-shrink-0 bg-surface-raised border-b border-border-default px-3 pt-2 pb-2">
```

미해결 N건 칩 (라인 1016~1022) — `display:flex / justifyContent:flex-end / marginBottom:6` 컨테이너 + `<span>` 칩 → 칩 자체는 Wave 1 의 ChevronRight 다음 영역, 텍스트 콘텐츠는 보존 + AlertTriangle 아이콘 추가:

```tsx
{unresolvedCount > 0 && (
  <div className="flex justify-end mb-1.5">
    <span className="inline-flex items-center gap-1 text-caption font-semibold text-danger bg-danger-bg border border-danger-bar rounded-pill px-2 py-0.5">
      <AlertTriangle size={12} />
      미해결 {unresolvedCount}건
    </span>
  </div>
)}
```

(주: sketch 의 미해결 색은 `badge-danger` 인데, 메모리 "미조치 색 = status-fire (주황)" 룰은 점검의 미조치이며, 승강기 고장 미해결은 메모리에 다른 룰이 없으므로 sketch 권위로 `danger` 적용. 단, 메모리 §6.1 fire 의미 고정 — "조치 대기 / 긴급. 방화셔터 미조치, 승강기 고장 미수리 등" 에 따르면 **승강기 고장 미수리는 fire 색이어야 한다**. sketch 도 fault 카드의 색바를 `ev-bar-fire` 로 매핑. 따라서 미해결 N건 칩도 `fire` 색이 정확:)

```tsx
{unresolvedCount > 0 && (
  <div className="flex justify-end mb-1.5">
    <span className="inline-flex items-center gap-1 text-caption font-semibold text-fire bg-fire-bg border border-fire-bar rounded-pill px-2 py-0.5">
      <AlertTriangle size={12} />
      미해결 {unresolvedCount}건
    </span>
  </div>
)}
```

→ **fire 사용 (메모리 룰 + sketch 권위)**.

6 탭 가로 스크롤 (라인 1023~1032):

```tsx
<div className="flex gap-1.5 overflow-x-auto no-scrollbar">
  {TABS.map(t => (
    <button
      key={t.key}
      onClick={() => setTab(t.key)}
      className={
        'px-3 py-1.5 rounded-pill text-caption font-semibold whitespace-nowrap flex-shrink-0 transition-colors ' +
        (tab === t.key
          ? 'bg-accent text-text-on-accent'
          : 'bg-surface-sunken text-text-tertiary hover:bg-surface-active hover:text-text-secondary')
      }
    >
      {t.label}
    </button>
  ))}
</div>
```

(주: `no-scrollbar` 가 Tailwind 에 없으면 인라인 `[&::-webkit-scrollbar]:hidden [scrollbar-width:none]` 또는 sketch 의 CSS 클래스 동등 한 줄로 추가. Tailwind arbitrary variant 로 처리)

`<main style={{...}}>` (라인 1036):

```tsx
<main className="flex-1 min-h-0 overflow-y-auto px-3 py-2.5 flex flex-col gap-2" style={{ paddingBottom: 'calc(80px + var(--sab, 0px))' }}>
```

(`paddingBottom` 의 `var(--sab)` 는 동적 safe-area 값이므로 화이트리스트 — 인라인 style 보존)

**Step 2 — list 탭 본문(라인 1039~1098) 토큰화:**

```tsx
{tab === 'list' && (
  <>
    {(['passenger','cargo','dumbwaiter','escalator'] as const).map(type => {
      const group = elevators.filter(e => e.type === type)
      if (!group.length) return null
      const TypeIcon = TYPE_ICON_COMPONENT[type]
      return (
        <div key={type}>
          <div className="flex items-center gap-1.5 text-caption font-bold text-text-tertiary uppercase tracking-wider mb-1.5 mt-1">
            <TypeIcon size={14} className="text-text-tertiary" />
            <span>{TYPE_LABEL[type]} ({group.length}대)</span>
          </div>
          {group.map(ev => {
            const st = STATUS_STYLE[ev.status] ?? STATUS_STYLE.normal
            const ni = nextInspMap.get(ev.id)
            // 좌측 색바 매핑 — §6.1 + sketch 권위 (메모리 "미조치 색 = status-fire" 일관성)
            const barClass =
              ev.status === 'fault'          ? 'before:bg-fire-bar'    :
              ev.status === 'maintenance'    ? 'before:bg-warning-bar' :
              ev.status === 'out_of_service' ? 'before:bg-text-tertiary' :
                                                'before:bg-safe-bar'
            // 상태 배지 텍스트색 + 배경색 매핑 (STATUS_STYLE 보존 + Tailwind 동등)
            const badgeClass =
              ev.status === 'fault'          ? 'text-fire bg-fire-bg'       :
              ev.status === 'maintenance'    ? 'text-warning bg-warning-bg' :
              ev.status === 'out_of_service' ? 'text-text-tertiary bg-surface-sunken' :
                                                'text-safe bg-safe-bg'
            // 카드 border (fault 강조)
            const borderClass = ev.status === 'fault' ? 'border-fire-bar' : 'border-border-default'
            // 운행중지 카드 살짝 흐림 (§6.3 0% 카드 opacity 0.6 응용 — sketch ev-disabled)
            const dimmedClass = ev.status === 'out_of_service' ? 'opacity-60' : ''
            // 아이콘 박스 색 (fault 시 fire 강조)
            const iconBoxClass =
              ev.status === 'fault'          ? 'bg-fire-bg text-fire' :
              ev.status === 'out_of_service' ? 'bg-surface-sunken text-text-tertiary' :
                                                'bg-surface-sunken text-text-secondary'
            return (
              <div
                key={ev.id}
                onClick={() => { setDetailEv(ev); setModal('ev_detail') }}
                className={
                  'relative bg-surface-raised border rounded-md px-3 py-2.5 flex items-center gap-2.5 mb-1.5 cursor-pointer ' +
                  'overflow-hidden transition-[border-color,transform,background-color] duration-150 hover:border-border-strong hover:-translate-y-px ' +
                  'before:content-[\'\'] before:absolute before:left-0 before:top-0 before:bottom-0 before:w-[3px] ' +
                  barClass + ' ' + borderClass + ' ' + dimmedClass
                }
              >
                <div className={'w-10 h-10 rounded-md flex items-center justify-center flex-shrink-0 ' + iconBoxClass}>
                  <TypeIcon size={20} />
                </div>
                <div className="flex-1 min-w-0">
                  <div className="text-body font-semibold text-text-primary">
                    {ev.number}호기
                    <span className="text-caption font-normal text-text-tertiary ml-1.5">{ev.location}</span>
                    {ev.type === 'escalator' && ev.public_number != null && (
                      <span className="text-caption font-normal text-text-tertiary ml-1.5">(공단 {ev.public_number}호기)</span>
                    )}
                  </div>
                  <div className="text-caption text-text-tertiary mt-0.5">
                    {ev.last_inspect_date ? `최근 점검: ${ev.last_inspect_date}` : '점검 기록 없음'}
                    {(ev.active_faults ?? 0) > 0 && <span className="text-danger ml-1.5">미해결 {ev.active_faults}건</span>}
                  </div>
                </div>
                <div className="flex flex-col items-end gap-1 flex-shrink-0">
                  <span className={'text-caption font-semibold rounded-pill px-2 py-0.5 ' + badgeClass}>{st.label}</span>
                  {ni && ni.status === 'due_soon' && ni.daysUntil != null && (
                    <span className="text-caption font-semibold rounded-sm px-2 py-0.5 bg-warning-bg text-warning">D-{ni.daysUntil}</span>
                  )}
                  {ni && ni.status === 'overdue' && (
                    <span className="text-caption font-semibold rounded-sm px-2 py-0.5 bg-danger-bg text-danger">검사 초과</span>
                  )}
                  {ni && ni.status === 'no_record' && (
                    <span className="text-caption font-semibold rounded-sm px-2 py-0.5 bg-info-bg text-info">기록 없음</span>
                  )}
                </div>
                <ChevronRight size={14} className="text-text-tertiary flex-shrink-0" />
              </div>
            )
          })}
        </div>
      )
    })}
  </>
)}
```

**핵심 보존 룰:**
- `setDetailEv(ev); setModal('ev_detail')` 호출 그대로
- `nextInspMap.get(ev.id)` 그대로
- `(ev.active_faults ?? 0) > 0 && ...` 분기 그대로
- `ev.type === 'escalator' && ev.public_number != null && ...` 분기 그대로
- `ni.status === 'due_soon' && ni.daysUntil != null` 분기 그대로
- `ni.status === 'overdue'` / `'no_record'` 분기 그대로
- `STATUS_STYLE[ev.status] ?? STATUS_STYLE.normal` 호출 그대로 (st.label 만 사용, st.color/st.bg 는 Tailwind utility 로 교체)
- 9/10/11px 폰트 → text-caption(12) 으로 상향 (10→12, 9→12)
- 이모지 (`{TYPE_ICON[type]}` 의 라인 1047 그룹 라벨 + 라인 1058 아이콘 박스) → `<TypeIcon size=... />` 컴포넌트로 교체. **TYPE_ICON 객체 자체는 보존** (다른 탭/모달 사용)

**Step 3 — verify (변환 영역 한정):**

라인 1011~1098 영역에서:
- 인라인 style 금지 키 0건 (단, `<main>` 의 `paddingBottom: 'calc(80px + var(--sab, 0px))'` 화이트리스트 — `var(--sab)` 동적값)
- 9/10/11px 0건 (text-caption 이상)
- 이모지 0건
- 옛 토큰(var(--bg2)/var(--bd)/var(--t1)/var(--t3)/var(--acl)/var(--safe)/var(--warn)/var(--danger)/var(--info)) 0건
  </action>
  <verify>
    <automated>cd cha-bio-safety && awk 'NR>=1010 && NR<=1100' src/pages/ElevatorPage.tsx > /tmp/3mc-t2-region.txt && grep -E "fontSize|fontWeight|padding|margin|color\s*:|background|borderRadius|display\s*:|flex|gridTemplate|width\s*:|height\s*:|gap\s*:" /tmp/3mc-t2-region.txt | grep -v "var(--sab\|class\|//\|paddingBottom.*var(--sab" | grep -v "^\s*\*" > /tmp/3mc-t2-inline.txt; [ ! -s /tmp/3mc-t2-inline.txt ] && grep -E "(^|[^0-9])(9|10|11)px" /tmp/3mc-t2-region.txt | grep -v "^\s*//\|^\s*\*" > /tmp/3mc-t2-px.txt; [ ! -s /tmp/3mc-t2-px.txt ] && grep -E "🛗|📦|🔲|↕️|🚨|🔧|✅" /tmp/3mc-t2-region.txt > /tmp/3mc-t2-emoji.txt; [ ! -s /tmp/3mc-t2-emoji.txt ] && grep -E "var\(--(bg[23]?|bd[2]?|acl|t[123]|safe|warn|danger|info|fire)\)" /tmp/3mc-t2-region.txt > /tmp/3mc-t2-oldtok.txt; [ ! -s /tmp/3mc-t2-oldtok.txt ] && grep -c "setDetailEv(ev); setModal('ev_detail')" /tmp/3mc-t2-region.txt | tee /tmp/3mc-t2-biz1.txt && [ "$(cat /tmp/3mc-t2-biz1.txt)" -ge "1" ] && grep -c "nextInspMap.get(ev.id)" /tmp/3mc-t2-region.txt | tee /tmp/3mc-t2-biz2.txt && [ "$(cat /tmp/3mc-t2-biz2.txt)" -ge "1" ] && grep -c "ev.type === 'escalator' && ev.public_number" /tmp/3mc-t2-region.txt | tee /tmp/3mc-t2-biz3.txt && [ "$(cat /tmp/3mc-t2-biz3.txt)" -ge "1" ] && grep -c "ni.status === 'due_soon'\|ni.status === 'overdue'\|ni.status === 'no_record'" /tmp/3mc-t2-region.txt | tee /tmp/3mc-t2-biz4.txt && [ "$(cat /tmp/3mc-t2-biz4.txt)" -ge "3" ] && npx tsc -p tsconfig.json --noEmit 2>&1 | head -30 | tee /tmp/3mc-t2-tsc.txt && ! grep -E "error TS[0-9]+" /tmp/3mc-t2-tsc.txt</automated>
  </verify>
  <done>모바일 자체 헤더(라인 ~1014~1033) + main 컨테이너(라인 1036) + list 탭 본문(라인 1039~1098) 이 Tailwind utility + v0.1.1 토큰 only. 인라인 style 금지 키 0건(paddingBottom var(--sab) 화이트리스트만), 9/10/11px 0건, 이모지 0건, 옛 토큰 0건. 비즈니스 로직(setDetailEv/setModal/nextInspMap/ev.active_faults/ev.public_number/ni.status 분기) 1:1 보존. 카드 좌측 3px 색바 ev.status 매핑 (normal=safe / fault=fire / maintenance=warning / out_of_service=mute). 다른 탭(fault/repair/inspect/annual/safety) 본문은 한 줄도 변경 안 됨. tsc 0 에러.</done>
</task>

<task type="auto">
  <name>Task 3: 데스크톱 자체 헤더(라인 ~605~620) + 데스크톱 좌측 컬럼(라인 ~626~652) + renderEvCard 함수(라인 ~564~592) 토큰화</name>
  <files>cha-bio-safety/src/pages/ElevatorPage.tsx</files>
  <action>
**변환 영역:** `if (isDesktop)` 블록(라인 545~1009) 안의 **3 영역만**:
1. renderEvCard 함수 (라인 564~592)
2. 데스크톱 자체 헤더 (라인 605~620)
3. 데스크톱 좌측 컬럼 (라인 626~652)

**보존 영역:** 라인 654~995 (데스크톱 우측 컬럼 `desktopRightTab` 영역, ElevatorInfoCard 호출, RIGHT_TABS, KoelsaHistorySection 호출, RepairListSection 호출 등) — 한 줄도 변경 금지.

**Step 1 — renderEvCard 함수 토큰화 (라인 564~592):**

```tsx
const renderEvCard = (ev: Elevator) => {
  const st = STATUS_STYLE[ev.status] ?? STATUS_STYLE.normal
  const isSel = selectedDesktopEv?.id === ev.id
  const TypeIcon = TYPE_ICON_COMPONENT[ev.type]
  // 좌측 색바 (sketch 권위 — fault=fire)
  const barClass =
    ev.status === 'fault'          ? 'before:bg-fire-bar'    :
    ev.status === 'maintenance'    ? 'before:bg-warning-bar' :
    ev.status === 'out_of_service' ? 'before:bg-text-tertiary' :
                                      'before:bg-safe-bar'
  // 카드 border (selected accent 우선 / fault border-fire / default)
  const borderClass = isSel
    ? 'border-accent'
    : ev.status === 'fault' ? 'border-fire-bar/40' : 'border-border-default'
  // 운행중지 흐림
  const dimmedClass = ev.status === 'out_of_service' ? 'opacity-60' : ''
  // 상태 배지 색
  const badgeClass =
    ev.status === 'fault'          ? 'text-fire bg-fire-bg'       :
    ev.status === 'maintenance'    ? 'text-warning bg-warning-bg' :
    ev.status === 'out_of_service' ? 'text-text-tertiary bg-surface-sunken' :
                                      'text-safe bg-safe-bg'
  // 선택 시 배경 강조
  const bgClass = isSel ? 'bg-accent/10' : 'bg-surface-raised'
  return (
    <div
      key={ev.id}
      onClick={() => setDetailEv(ev)}
      className={
        'relative w-full h-32 box-border overflow-hidden rounded-md border-2 cursor-pointer ' +
        'flex flex-col items-center gap-1 px-1.5 py-2.5 transition-colors duration-150 ' +
        'before:content-[\'\'] before:absolute before:left-0 before:top-0 before:bottom-0 before:w-[3px] ' +
        bgClass + ' ' + borderClass + ' ' + barClass + ' ' + dimmedClass
      }
    >
      <TypeIcon size={22} className="text-text-secondary" />
      <div className="text-label font-bold text-text-primary">{ev.number}호기</div>
      <div className="text-caption text-text-tertiary text-center leading-snug break-keep max-w-full">
        {ev.location}
        {ev.type === 'escalator' && ev.public_number != null && (
          <div className="text-caption text-text-tertiary mt-0.5">공단 {ev.public_number}호기</div>
        )}
      </div>
      <span className={'mt-auto text-caption font-bold rounded-pill px-1.5 py-0.5 ' + badgeClass}>{st.label}</span>
      {(ev.active_faults ?? 0) > 0 && (
        <span className="text-caption font-bold text-danger">미해결 {ev.active_faults}</span>
      )}
    </div>
  )
}
```

**핵심 보존 룰:**
- `setDetailEv(ev)` 호출 그대로
- `selectedDesktopEv?.id === ev.id` 비교 그대로
- `STATUS_STYLE[ev.status] ?? STATUS_STYLE.normal` 호출 그대로 (st.label 만 사용)
- `(ev.active_faults ?? 0) > 0` 분기 그대로
- `ev.type === 'escalator' && ev.public_number != null` 분기 그대로
- 9/10px 폰트 → text-caption(12) 상향 (10→12, 9→12, 13유지)
- 이모지 `{TYPE_ICON[ev.type]}` (라인 580) → `<TypeIcon size={22} />` 컴포넌트로 교체
- `<div>` 카드 root 의 `width: '100%', height: 130, boxSizing: 'border-box', overflow: 'hidden'` → Tailwind utility (`w-full h-32 box-border overflow-hidden`)

**Step 2 — 데스크톱 자체 헤더 토큰화 (라인 605~620):**

```tsx
<header className="flex-shrink-0 h-14 bg-surface-raised border-b border-border-default px-5 flex items-center gap-2.5">
  <span className="flex-1 text-body font-bold text-text-primary">승강기 관리</span>
  {unresolvedCount > 0 && (
    <span className="inline-flex items-center gap-1 text-caption font-bold text-fire bg-fire-bg border border-fire-bar rounded-pill px-2.5 py-1">
      <AlertTriangle size={12} />
      미해결 고장 {unresolvedCount}건
    </span>
  )}
  <button
    onClick={() => { setSelectedEv(selectedDesktopEv); setModal('fault_new') }}
    className="inline-flex items-center gap-1.5 h-8 px-3.5 rounded-sm border-0 text-text-on-accent text-caption font-bold cursor-pointer"
    style={{ background: 'linear-gradient(135deg,#991b1b,#ef4444)' }}
  >
    <AlertTriangle size={14} />
    고장 접수
  </button>
  <button
    onClick={() => { setSelectedEv(selectedDesktopEv); setModal('repair_new') }}
    className="inline-flex items-center gap-1.5 h-8 px-3.5 rounded-sm border-0 text-text-on-accent text-caption font-bold cursor-pointer"
    style={{ background: 'linear-gradient(135deg,#854d0e,#eab308)' }}
  >
    <Wrench size={14} />
    수리 기록
  </button>
</header>
```

**핵심 보존 룰:**
- `setSelectedEv(selectedDesktopEv); setModal('fault_new'|'repair_new')` 그대로
- 그라디언트 CTA (§6.4 — 위험성/긴급 의미) 는 인라인 style 화이트리스트 — `linear-gradient(...)` 보존
- 이모지 🚨 / 🔧 → AlertTriangle / Wrench lucide 컴포넌트로 교체

**Step 3 — 데스크톱 좌측 컬럼 토큰화 (라인 626~652):**

```tsx
<div className="flex-1 min-w-0 border-r border-border-default overflow-y-auto px-6 py-5">
  <div className="flex items-center gap-1.5 text-caption font-bold text-text-tertiary tracking-wider mb-2.5">
    <ElevatorIcon size={14} className="text-text-tertiary" />
    <span>엘리베이터</span>
  </div>
  <div className="flex gap-2.5 mb-6">
    {evGroups.map((group, gi) => (
      <div key={gi} className="flex-1 min-w-0 flex flex-col gap-2">
        {group.map(ev => renderEvCard(ev))}
      </div>
    ))}
  </div>

  {ess.length > 0 && (
    <>
      <div className="flex items-center gap-1.5 text-caption font-bold text-text-tertiary tracking-wider mb-2.5">
        <MoveDiagonal size={14} className="text-text-tertiary" />
        <span>에스컬레이터</span>
      </div>
      <div className="grid grid-cols-4 gap-2.5">
        {esRow1Left[0]  && <div className="min-w-0">{renderEvCard(esRow1Left[0])}</div>}
        {esRow1Left[1]  && <div className="min-w-0">{renderEvCard(esRow1Left[1])}</div>}
        {esRow1Right[0] && <div className="min-w-0">{renderEvCard(esRow1Right[0])}</div>}
        {esRow1Right[1] && <div className="min-w-0">{renderEvCard(esRow1Right[1])}</div>}
        {esRow2[0] && <div className="col-start-1 min-w-0">{renderEvCard(esRow2[0])}</div>}
        {esRow2[1] && <div className="col-start-2 min-w-0">{renderEvCard(esRow2[1])}</div>}
      </div>
    </>
  )}
</div>
```

**핵심 보존 룰:**
- `evGroups.map((group, gi) => ...)` 구조 그대로 (4 그룹 flexbox 가로 배치)
- `esRow1Left / esRow1Right / esRow2` 분기 그대로
- `ess.length > 0 &&` 조건 그대로
- 이모지 🛗 / ↕️ (라인 627, 638) → ElevatorIcon / MoveDiagonal 컴포넌트로 교체
- `grid grid-cols-4 gap-2.5` (sketch 의 데스크톱 4컬럼 그리드 유지)

**Step 4 — verify (변환 영역 한정):**

라인 564~592 (renderEvCard) + 605~620 (데스크톱 헤더) + 626~652 (좌측 컬럼) 에서:
- 인라인 style 금지 키 0건 (단, 헤더 2 버튼의 `linear-gradient` 그라디언트는 §6.4 화이트리스트)
- 9/10/11px 0건
- 이모지 0건
- 옛 토큰 0건

**보존 영역 검증** (한 줄도 변경 안 됐는지):
- 라인 654~995 (desktopRightTab 영역) 라인 수 보존 (정확히 일치 어렵지만 ±0 ~ ±5 라인 shift 만 허용)
- 5 모달 정의 (FaultNewModal / FaultNewFullscreen / FaultResolveModal / RepairNewModal / EvDetailModal — 페이지 함수 외부) 본체 0 변경
  </action>
  <verify>
    <automated>cd cha-bio-safety && awk 'NR>=560 && NR<=655' src/pages/ElevatorPage.tsx > /tmp/3mc-t3-region.txt && grep -E "fontSize|fontWeight|padding|margin|color\s*:|backgroundColor|borderRadius|display\s*:|flexDirection|gridTemplate|width\s*:|height\s*:|gap\s*:|justifyContent|alignItems" /tmp/3mc-t3-region.txt | grep -v "//\|background.*linear-gradient" | grep -v "^\s*\*" > /tmp/3mc-t3-inline.txt; [ ! -s /tmp/3mc-t3-inline.txt ] && grep -E "(^|[^0-9])(9|10|11)px" /tmp/3mc-t3-region.txt | grep -v "^\s*//\|^\s*\*" > /tmp/3mc-t3-px.txt; [ ! -s /tmp/3mc-t3-px.txt ] && grep -E "🛗|📦|🔲|↕️|🚨|🔧|✅" /tmp/3mc-t3-region.txt > /tmp/3mc-t3-emoji.txt; [ ! -s /tmp/3mc-t3-emoji.txt ] && grep -E "var\(--(bg[23]?|bd[2]?|acl|t[123]|safe|warn|danger|info|fire)\)" /tmp/3mc-t3-region.txt > /tmp/3mc-t3-oldtok.txt; [ ! -s /tmp/3mc-t3-oldtok.txt ] && grep -c "setDetailEv(ev)" /tmp/3mc-t3-region.txt | tee /tmp/3mc-t3-biz1.txt && [ "$(cat /tmp/3mc-t3-biz1.txt)" -ge "1" ] && grep -c "selectedDesktopEv?.id === ev.id" /tmp/3mc-t3-region.txt | tee /tmp/3mc-t3-biz2.txt && [ "$(cat /tmp/3mc-t3-biz2.txt)" -ge "1" ] && grep -c "evGroups.map\|esRow1Left\|esRow1Right\|esRow2" /tmp/3mc-t3-region.txt | tee /tmp/3mc-t3-biz3.txt && [ "$(cat /tmp/3mc-t3-biz3.txt)" -ge "4" ] && awk 'NR>=595 && NR<=625' src/pages/ElevatorPage.tsx > /tmp/3mc-t3-header.txt && grep -c "setSelectedEv(selectedDesktopEv); setModal('fault_new')" /tmp/3mc-t3-header.txt | tee /tmp/3mc-t3-biz4.txt && [ "$(cat /tmp/3mc-t3-biz4.txt)" -ge "1" ] && grep -c "setSelectedEv(selectedDesktopEv); setModal('repair_new')" /tmp/3mc-t3-header.txt | tee /tmp/3mc-t3-biz5.txt && [ "$(cat /tmp/3mc-t3-biz5.txt)" -ge "1" ] && npx tsc -p tsconfig.json --noEmit 2>&1 | head -30 | tee /tmp/3mc-t3-tsc.txt && ! grep -E "error TS[0-9]+" /tmp/3mc-t3-tsc.txt</automated>
  </verify>
  <done>renderEvCard 함수(라인 ~564~592) + 데스크톱 자체 헤더(라인 ~605~620) + 데스크톱 좌측 컬럼(라인 ~626~652) 토큰화. 인라인 style 금지 키 0건 (헤더 2 버튼의 `background: linear-gradient(...)` 화이트리스트만), 9/10/11px 0건, 이모지 0건, 옛 토큰 0건. 비즈니스 로직(setDetailEv/setSelectedEv/setModal('fault_new'|'repair_new')/evGroups.map/esRow1Left/esRow1Right/esRow2/STATUS_STYLE/ev.public_number/active_faults) 보존. 카드 좌측 3px 색바 ev.status 매핑. 데스크톱 우측 컬럼(desktopRightTab 영역) + 5 모달 본체 + KoelsaHistorySection / RepairListSection / ElevatorInfoCard 호출은 한 줄도 변경 안 됨. tsc 0 에러.</done>
</task>

<task type="auto">
  <name>Task 4: 전체 변환 영역 회귀 검증 + npm run build 통과 + 보존 영역 무손실 확인</name>
  <files>cha-bio-safety/src/pages/ElevatorPage.tsx, cha-bio-safety/src/components/ui/icons.tsx</files>
  <action>
**Step 1 — 전체 변환 영역 통합 회귀 검증:**

Wave 1 변환 영역 = 다음 3 블록 (라인 번호는 task 1~3 변경 후 shift 됨, 영역 식별은 sentinel 으로):
- A. 모바일 자체 헤더 + main + list 탭 본문: `// ── 모바일 ─────` 부터 `{tab === 'list' && (` 다음 닫는 `</>` `)}` 까지
- B. renderEvCard 함수 정의: `const renderEvCard = (ev: Elevator) =>` 부터 그 함수의 닫는 `}` 까지
- C. 데스크톱 자체 헤더 + 좌측 컬럼: `{/* 헤더 — 데스크톱 표준 ... */}` 부터 `{/* ── 우측: 호기별 이력 ── */}` 직전까지

awk + sentinel 으로 추출:

```bash
# A 블록 추출 (모바일 헤더 + list)
awk '/^  \/\/ ── 모바일 ─/,/{tab === \x27fault\x27 && \(/' src/pages/ElevatorPage.tsx | head -n -1 > /tmp/3mc-t4-A.txt
# B 블록 추출 (renderEvCard)
awk '/const renderEvCard = \(ev: Elevator\) =>/,/^    \}$/' src/pages/ElevatorPage.tsx | head -50 > /tmp/3mc-t4-B.txt
# C 블록 추출 (데스크톱 헤더 + 좌측)
awk '/{\/\* 헤더 — 데스크톱 표준/,/{\/\* ── 우측: 호기별 이력/' src/pages/ElevatorPage.tsx | head -n -1 > /tmp/3mc-t4-C.txt
cat /tmp/3mc-t4-A.txt /tmp/3mc-t4-B.txt /tmp/3mc-t4-C.txt > /tmp/3mc-t4-wave1.txt
```

검증 (Wave 1 영역 한정):

1. 인라인 style 금지 키 0건 (화이트리스트만): `position`, `top/left/right/bottom`, `transform`, `transition`, `animation`, `paddingBottom: 'calc(80px + var(--sab, 0px))'`, `background: 'linear-gradient(135deg,#991b1b,#ef4444)'`, `background: 'linear-gradient(135deg,#854d0e,#eab308)'` 만 허용
2. 9 / 10 / 11px 0건
3. 이모지 (🛗 / 📦 / 🔲 / ↕️ / 🚨 / 🔧 / ✅) 0건
4. 옛 토큰 (var(--bg) / var(--bg2) / var(--bg3) / var(--bd) / var(--bd2) / var(--acl) / var(--t1) / var(--t2) / var(--t3) / var(--safe) / var(--warn) / var(--danger) / var(--info) / var(--fire)) 0건

**Step 2 — 보존 영역 무손실 확인:**

5 모달 본체 + 데스크톱 우측 컬럼 + 다른 탭 본문 + KoelsaHistorySection 호출 + RepairListSection 호출 + ElevatorInfoCard 본체 + 비즈니스 로직 — 본 task 에서 새로 변경된 게 없는지 sentinel 카운트:

- `function FaultNewModal` 1
- `function FaultNewFullscreen` 1
- `function FaultResolveModal` 1
- `function RepairNewModal` 1
- `function EvDetailModal` 1
- `function ElevatorInfoCard` 1
- `function RepairListSection` 1
- `function EmptyState` 1 (있다면)
- `tab === 'fault'` / `tab === 'repair'` / `tab === 'inspect'` / `tab === 'annual'` / `tab === 'safety'` 각 1
- `useQuery({ queryKey:['elevators']` 1
- `useQuery({ queryKey:['elevator_faults']` 1
- `submitFault.mutate` ≥ 1 / `resolveFault.mutate` ≥ 1
- `KoelsaHistorySection` 호출 ≥ 1
- `RepairListSection` 호출 ≥ 1

**Step 3 — 빌드 통과 + chunk hash 변경 확인:**

```bash
cd cha-bio-safety
npm run build 2>&1 | tail -40 > /tmp/3mc-t4-build.txt
grep -E "error|✓ built|dist/assets/ElevatorPage" /tmp/3mc-t4-build.txt
ls dist/assets/ElevatorPage-*.js 2>/dev/null | head -3
```

`✓ built` 출력 확인. ElevatorPage-{hash}.js 파일 존재 확인 (chunk hash 자동 변경 → 사용자 PWA 캐시 무효화 보장).

**Step 4 — 결과 보고:**

verify 결과를 다음 정보로 콘솔에 요약 출력:
- Wave 1 변환 영역 라인 수 (대략)
- 인라인 style 위반 0건 / 9-11px 위반 0건 / 이모지 위반 0건 / 옛 토큰 위반 0건
- 보존 영역 sentinel 카운트 (5 모달 + 다른 탭 본문 + 컴포넌트 호출 보존)
- 빌드 통과 / 새 chunk hash

본 task 는 코드 변경 0 — 검증 + 회귀 + 빌드만.
  </action>
  <verify>
    <automated>cd cha-bio-safety && awk '/^  \/\/ ── 모바일 ─/,/\{tab === .fault. \&\& \(/' src/pages/ElevatorPage.tsx > /tmp/3mc-t4-A.txt && awk '/const renderEvCard = \(ev: Elevator\) =>/,/^    \}$/' src/pages/ElevatorPage.tsx | head -60 > /tmp/3mc-t4-B.txt && awk '/{\/\* 헤더 — 데스크톱 표준/,/{\/\* ── 우측: 호기별 이력/' src/pages/ElevatorPage.tsx > /tmp/3mc-t4-C.txt && cat /tmp/3mc-t4-A.txt /tmp/3mc-t4-B.txt /tmp/3mc-t4-C.txt > /tmp/3mc-t4-wave1.txt && wc -l /tmp/3mc-t4-wave1.txt && grep -E "fontSize|fontWeight|padding\s*:|paddingLeft|paddingRight|paddingTop|margin\s*:|marginLeft|marginRight|color\s*:|borderRadius|display\s*:|flexDirection|gridTemplate|width\s*:|height\s*:|gap\s*:|justifyContent|alignItems|opacity\s*:|cursor\s*:|textAlign" /tmp/3mc-t4-wave1.txt | grep -v "var(--sab\|linear-gradient\|//\|^\s*\*\|className" > /tmp/3mc-t4-inline-violations.txt; [ ! -s /tmp/3mc-t4-inline-violations.txt ] && grep -vE "^\s*//|^\s*\*" /tmp/3mc-t4-wave1.txt | grep -E "(^|[^0-9])(9|10|11)px" > /tmp/3mc-t4-px-violations.txt; [ ! -s /tmp/3mc-t4-px-violations.txt ] && grep -E "🛗|📦|🔲|↕️|🚨|🔧|✅" /tmp/3mc-t4-wave1.txt > /tmp/3mc-t4-emoji-violations.txt; [ ! -s /tmp/3mc-t4-emoji-violations.txt ] && grep -E "var\(--(bg[23]?|bd[2]?|acl|t[123]|safe|warn|danger|info|fire)\)" /tmp/3mc-t4-wave1.txt > /tmp/3mc-t4-oldtok-violations.txt; [ ! -s /tmp/3mc-t4-oldtok-violations.txt ] && grep -c "^function FaultNewModal\|^function FaultNewFullscreen\|^function FaultResolveModal\|^function RepairNewModal\|^function EvDetailModal\|^function ElevatorInfoCard\|^function RepairListSection" src/pages/ElevatorPage.tsx | tee /tmp/3mc-t4-preserved.txt && [ "$(cat /tmp/3mc-t4-preserved.txt)" -ge "7" ] && grep -cE "tab === 'fault'|tab === 'repair'|tab === 'inspect'|tab === 'annual'|tab === 'safety'" src/pages/ElevatorPage.tsx | tee /tmp/3mc-t4-tabs.txt && [ "$(cat /tmp/3mc-t4-tabs.txt)" -ge "5" ] && grep -c "<KoelsaHistorySection" src/pages/ElevatorPage.tsx | tee /tmp/3mc-t4-koelsa.txt && [ "$(cat /tmp/3mc-t4-koelsa.txt)" -ge "1" ] && grep -c "<RepairListSection" src/pages/ElevatorPage.tsx | tee /tmp/3mc-t4-repair.txt && [ "$(cat /tmp/3mc-t4-repair.txt)" -ge "1" ] && npm run build 2>&1 | tail -30 | tee /tmp/3mc-t4-build.txt && grep -E "✓ built|✓ Compiled|built in" /tmp/3mc-t4-build.txt && ! grep -E "^error|ERROR|✘" /tmp/3mc-t4-build.txt && ls dist/assets/ElevatorPage-*.js 2>/dev/null | head -3</automated>
  </verify>
  <done>Wave 1 변환 영역 3 블록(모바일 헤더+list 본문, renderEvCard, 데스크톱 헤더+좌측 컬럼) 통합 회귀 검증 PASS: 인라인 style 금지 키 0건(var(--sab) + linear-gradient 화이트리스트만), 9/10/11px 0건, 이모지 0건, 옛 토큰 0건. 보존 영역 sentinel: 5 모달 함수 + ElevatorInfoCard + RepairListSection 정의 ≥ 7, 다른 탭 5종 본문 분기 보존, KoelsaHistorySection / RepairListSection 호출 보존. npm run build 통과(✓ built), dist/assets/ElevatorPage-{newhash}.js 생성. tsc 0 에러.</done>
</task>

</tasks>

<verification>
변환 영역 한정 자동 검증 (Wave 1 = 3 블록):
- 인라인 style 금지 키 0건 (화이트리스트 only)
- 9 / 10 / 11px 폰트 0건
- 이모지(🛗📦🔲↕️🚨🔧✅) 0건
- 옛 토큰(var(--bg)/var(--bd)/var(--acl)/var(--t1)/var(--safe)/var(--warn)/var(--danger)/var(--info)/var(--fire)) 0건
- ElevatorIcon export 신설 1건 (icons.tsx)
- TYPE_ICON_COMPONENT 매퍼 신설 1건 (ElevatorPage.tsx)
- 비즈니스 로직 sentinel: setDetailEv/setSelectedEv/setModal/nextInspMap/evGroups.map/esRow1Left/esRow1Right/esRow2/ev.public_number/active_faults/ni.status 분기 모두 보존
- 보존 영역 sentinel: 5 모달 + 다른 탭 5종 + KoelsaHistorySection + RepairListSection + ElevatorInfoCard 호출 보존
- TypeScript 컴파일 0 에러
- npm run build 통과 (✓ built)
- 새 ElevatorPage-{hash}.js chunk 생성 (PWA 캐시 무효화)
</verification>

<success_criteria>
- ElevatorIcon (커스텀 SVG, sketch path 1:1, StrokeSvg 패턴) 이 `src/components/ui/icons.tsx` 에 추가됨
- ElevatorPage.tsx 에 lucide-react import 신설(Package / UtensilsCrossed / MoveDiagonal / ChevronRight / AlertTriangle / Wrench) + ElevatorIcon import 추가
- TYPE_ICON_COMPONENT 매퍼 신설 (passenger=ElevatorIcon / cargo=Package / dumbwaiter=UtensilsCrossed / escalator=MoveDiagonal) — TYPE_ICON 이모지 객체와 병행
- 모바일 자체 헤더 + main + list 탭 본문 (라인 ~1014~1098) Tailwind utility + v0.1.1 토큰
- renderEvCard 함수 (라인 ~564~592) Tailwind utility + v0.1.1 토큰
- 데스크톱 자체 헤더 (라인 ~605~620) Tailwind utility + v0.1.1 토큰 (그라디언트 CTA 2 버튼 화이트리스트)
- 데스크톱 좌측 컬럼 (라인 ~626~652) Tailwind utility + v0.1.1 토큰
- 변환 영역 인라인 style 금지 키 0건 + 9/10/11px 0건 + 이모지 0건 + 옛 토큰 0건
- 비즈니스 로직 (모든 React hooks, 핸들러, 상수, API 호출, sessionStorage, nextInspMap, STATUS_STYLE, OVERALL_STYLE, RESULT_STYLE, EV_FLOORS, ES_NODES_*, CHECK_ITEMS_*, openClipReport, deleteRecord, TKE_TEL) 100% 보존
- 5 모달 (FaultNewModal / FaultNewFullscreen / FaultResolveModal / RepairNewModal / EvDetailModal) + EvSelector + EsNodeMap + EsBtn + 다른 탭 본문 (fault/repair/inspect/annual/safety) + 데스크톱 우측 컬럼 (desktopRightTab) + KoelsaHistorySection / RepairListSection / ElevatorInfoCard 본체 + CertViewerModal 등 보조 컴포넌트 한 줄도 변경 안 됨
- 다른 페이지 / 컴포넌트 / hooks / utils / API / 라우팅 / Tailwind config / tokens.css 0 변경
- TypeScript 컴파일 0 에러, npm run build 통과
- 새 ElevatorPage-{hash}.js chunk 생성 (PWA 캐시 무효화 보장)
</success_criteria>

<output>
After completion, create `.planning/quick/260515-3mc-redesign-07-elevator-tsx-wave-1-list/260515-3mc-SUMMARY.md` with:
- What changed (file + region + line range delta)
- Inline style 금지 키 violation count (0 expected)
- 9/10/11px / 이모지 / 옛 토큰 violation count (each 0 expected)
- 비즈니스 로직 보존 sentinel grep count
- 보존 영역(5 모달 + 다른 탭 + 우측 컬럼) sentinel count
- npm run build 결과 + new ElevatorPage-{hash}.js
- Next wave: Wave 2 = 5 모달 (FaultNewModal 우선 — entry point) 또는 다른 탭 본문 (fault 우선 — 미해결 칩 클릭 진입점)
</output>
