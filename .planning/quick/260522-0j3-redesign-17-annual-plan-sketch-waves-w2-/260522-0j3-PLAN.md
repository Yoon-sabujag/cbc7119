---
phase: quick-260522-0j3
plan: 01
type: execute
wave: 1
depends_on: []
quick_id: 260522-0j3
branch: redesign/17-annual-plan
date: 2026-05-22
autonomous: true
files_modified:
  - cha-bio-safety/docs/redesign-context/17-annual-plan/sketch-wave-2-chrome.html
  - cha-bio-safety/docs/redesign-context/17-annual-plan/sketch-wave-3-preview-calibration.html
  - cha-bio-safety/docs/redesign-context/17-annual-plan/sketch-wave-4-download.html
  - cha-bio-safety/docs/redesign-context/17-annual-plan/wave-5-tsx-conversion-checklist.md
requirements:
  - redesign-17-annual-plan-w2-chrome
  - redesign-17-annual-plan-w3-preview-calibration
  - redesign-17-annual-plan-w4-download
  - redesign-17-annual-plan-w5-tsx-checklist
must_haves:
  truths:
    - "W2~W4 sketch HTML 3개 + W5 checklist markdown 1개, 총 4 파일이 17-annual-plan/ 직속 평면 배치로 생성된다 (sketch/ 서브폴더 절대 X)"
    - "AnnualPlanPage.tsx (225 lines) + generateAnnualPlan.ts (48 lines) 모두 코드 변경 0건 — sketch + checklist 만 산출"
    - "negative gate 3 sketch + 1 checklist 모두 통과 (이모지 0 / linear-gradient 0 / 9·10·11px 0 / status- prefix 0 / w-8 h-8 0 / 옛 alias 토큰 0)"
    - "OQ #1~#5 LOCKED 결정 5건 모두 4 파일 안에 verbatim 반영 (다운로드 bg-safe-bar solid / 위치조정 토글 border-accent + bg-accent/10 / 폰트 격상 매핑 / Lucide ChevronLeft + Download / preview border-accent + 캘리브 안내 칩 rgba 인라인)"
    - "캘리브 좌표 시스템 시그니처 5건 (STORAGE_KEY / FINGER_OFFSET / handleImageClick / handleImageTouch / yearPos overlay) 100% 보존 — W3 sketch + W5 checklist 안 verbatim 명시"
    - "각 task 마다 atomic git commit 1개씩 — 총 4 commit"
  artifacts:
    - path: "cha-bio-safety/docs/redesign-context/17-annual-plan/sketch-wave-2-chrome.html"
      provides: "모바일 헤더 (영역 4 상단 line 169~192) + 데스크톱 상단 바 (영역 3 상단 line 110~146) sketch — chrome 통일 4 frame"
      contains: "data-theme dark+light 4 frame / w-[34px] h-[34px] rounded-sm / Lucide ChevronLeft size={15} / 연간 업무 추진 계획 / 위치 조정 / 취소 / border-accent bg-accent/10 text-accent (active) / bg-surface-sunken border-border-strong text-text-secondary (평시) / bg-surface-raised (모바일 헤더 + 데스크톱 상단 바)"
      min_lines: 350
    - path: "cha-bio-safety/docs/redesign-context/17-annual-plan/sketch-wave-3-preview-calibration.html"
      provides: "preview + 캘리브 좌표 시스템 sketch (영역 2 line 61~103) — 평시/캘리브/yearPos 표시 4 frame 매트릭스"
      contains: "STORAGE_KEY / FINGER_OFFSET / handleImageClick / handleImageTouch / yearPos / border-2 border-accent (캘리브 모드) / border border-border-default (평시) / rgba(59,130,246,0.9) 캘리브 안내 칩 / min(1.4vw, 16px) 연도 오버레이 / Malgun Gothic / 연도가 들어갈 위치를 클릭하세요 / /templates/preview/annual-plan.png"
      min_lines: 400
    - path: "cha-bio-safety/docs/redesign-context/17-annual-plan/sketch-wave-4-download.html"
      provides: "다운로드 버튼 + 설명 sketch (모바일 line 200~221 + 데스크톱 line 130~145) — bg-safe-bar solid + Lucide Download + disabled 분기 4 frame"
      contains: "bg-safe-bar / text-text-on-accent / bg-surface-sunken (disabled) / text-text-tertiary (disabled) / Lucide Download size={15} (데스크톱) / Download size={16} (모바일) / 엑셀 다운로드 / 생성 중... / 대상 연도 / 표지 및 일정표 연도가 자동 설정됩니다"
      min_lines: 400
    - path: "cha-bio-safety/docs/redesign-context/17-annual-plan/wave-5-tsx-conversion-checklist.md"
      provides: "TSX 변환 verify checklist (markdown, 12 섹션)"
      contains: "§1 변환 범위 / §2 보존 (캘리브 시그니처 + 비즈 로직) / §3 변환 매핑 (영역 5 데스크톱 + 모바일 + 영역 2 + 영역 5 다운로드) / §4 OQ LOCKED 5건 / §5 negative gate / §6 positive gate / §7 build tsc gate / §8 자체 verify 명령 / §9 Tailwind cheatsheet / §10 비즈 보존 체크박스 / §11 메모리 룰 inline 13+ / §12 다음 단계"
      min_lines: 350
  key_links:
    - from: "wave-1-index.md §7 OQ #1 LOCKED"
      to: "sketch-wave-4-download.html + wave-5-tsx-conversion-checklist.md"
      via: "다운로드 버튼 bg-safe-bar text-text-on-accent solid + disabled bg-surface-sunken text-text-tertiary cursor-not-allowed (그라데이션 완전 폐기)"
      pattern: "bg-safe-bar"
    - from: "wave-1-index.md §7 OQ #2 LOCKED"
      to: "sketch-wave-2-chrome.html + wave-5-tsx-conversion-checklist.md"
      via: "위치조정 토글 active = border-accent bg-accent/10 text-accent / 평시 = bg-surface-sunken border-border-strong text-text-secondary"
      pattern: "border-accent|bg-accent/10|text-accent"
    - from: "wave-1-index.md §7 OQ #3 LOCKED"
      to: "sketch-wave-2 + sketch-wave-3 + sketch-wave-4 + wave-5-checklist"
      via: "폰트 격상 매핑 verbatim — 11→text-caption(12) leading-none / 12→text-caption leading-none 유지 / 13→text-label(13) leading-none / 14→text-body(16) font-bold 격상 / 연도 오버레이 min(1.4vw, 16px) 유지"
      pattern: "text-caption|text-label|text-body"
    - from: "wave-1-index.md §7 OQ #4 LOCKED"
      to: "sketch-wave-2 + sketch-wave-4 + wave-5-checklist"
      via: "모바일 back button = Lucide ChevronLeft size={15} / 다운로드 = Lucide Download size={15} (데스크톱) + size={16} (모바일). svg path 인라인 폐기 + lucide-react import 추가"
      pattern: "ChevronLeft|Download"
    - from: "wave-1-index.md §7 OQ #5 LOCKED"
      to: "sketch-wave-3 + wave-5-checklist"
      via: "preview border (캘리브 모드) = border-2 border-accent / 평시 = border border-border-default / 캘리브 안내 칩 = 인라인 bg-[rgba(59,130,246,0.9)] text-white 유지 (color #fff + fontWeight 700 + whiteSpace nowrap + pointerEvents none 보존)"
      pattern: "border-accent|rgba\\(59,130,246,0\\.9"
---

<objective>
redesign/17-annual-plan 의 W1 인덱스(`wave-1-index.md`, 442 lines, OQ 5건 LOCKED 완료 2026-05-22)를 기반으로
W2~W5 4 sub-wave 산출물을 **단일 quick task** 안에서 **atomic 4 commit** 으로 한 번에 만든다.

- **W2 (T1):** 모바일 헤더 (line 169~192) + 데스크톱 상단 바 (line 110~146) sketch — chrome 통일 4 frame
- **W3 (T2):** preview + 캘리브 좌표 시스템 sketch — 평시/캘리브/yearPos null/저장후 4 state 매트릭스 (★ 캘리브 단독 wave, 옵션 C, 15-daily-report SW3 precedent)
- **W4 (T3):** 다운로드 버튼 + 설명 sketch (모바일 + 데스크톱) — bg-safe-bar solid + Lucide Download + disabled 분기
- **W5 (T4):** TSX 변환 verify checklist markdown (sketch 아님, W2~W4 grep 추출 검증 룰 + 12 섹션)

**Purpose:** 13-schedule / 14-reports / 27-login / 16-workshift 와 동일한 평면(flat sibling) 패턴으로 17-annual-plan sketch 단계를 4 wave 분량을 한 turn 에 처리하여 cbc7119-preview 자동 배포 사이클 1회로 사용자 시각 검수 가능하게 함. 캘리브 좌표 시스템은 페이지 핵심 동작이므로 W3 단독 wave 로 분리 (옵션 C — 15-daily-report SW3 portraitPos precedent 와 동일).

**Output:** `cha-bio-safety/docs/redesign-context/17-annual-plan/` 직속 4 파일 (sketch HTML 3개 + checklist md 1개).
`AnnualPlanPage.tsx` + `generateAnnualPlan.ts` **2 파일 모두** 코드 변경 0건 (verify gate 마지막에 확인).

**27-login f01 + 16-workshift t12 PLAN.md mirror:** 본 PLAN 은 27-login f01 + 16-workshift t12 PLAN 구조 (단일 plan / 4 task / atomic 4 commit / verify gate 그대로) 의 정확한 mirror. 차이점 = AnnualPlanPage 225 lines (LoginPage 220 / WorkShiftPage 226 와 거의 동일) / OQ 결정 내용 (다운로드 그라데이션 폐기 + 캘리브 좌표 시스템 보존) / source 2 파일 untouched (AnnualPlanPage + generateAnnualPlan).

**중요 — executor commit 부모 머지 사고 회피:** 16-workshift sjj + 17-annual-plan wmq 모두 executor worktree commit 이 부모 브랜치로 자동 머지 안 되어 cherry-pick 복구 필요했음. **executor 는 worktree 안에서 `git commit` 후 명시적 보고 필수** + orchestrator 가 자동 cherry-pick 으로 복구한다. 본 PLAN 각 task 의 commit 명령은 명시 박제 — executor 가 실행 후 commit hash + message 1줄 보고.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jykevin/Documents/cbc7119-design/CLAUDE.md
@/Users/jykevin/Documents/cbc7119-design/CLAUDE.local.md

# W1 인덱스 (단일 진입점, OQ 5건 LOCKED 완료 2026-05-22)
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/17-annual-plan/wave-1-index.md

# Precedent — 16-workshift t12 PLAN.md + SUMMARY.md (정확한 mirror 소스)
@/Users/jykevin/Documents/cbc7119-design/.planning/quick/260521-t12-redesign-16-workshift-sketch-waves-w2-w5/260521-t12-PLAN.md
@/Users/jykevin/Documents/cbc7119-design/.planning/quick/260521-t12-redesign-16-workshift-sketch-waves-w2-w5/260521-t12-SUMMARY.md

# Precedent — 27-login f01 PLAN.md (4-task 구조 mirror)
@/Users/jykevin/Documents/cbc7119-design/.planning/quick/260521-f01-redesign-27-login-sketch-waves-w2-w5-4-s/260521-f01-PLAN.md

# Precedent — 16-workshift 산출 4 파일 (시각/구조 mirror)
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/16-workshift/sketch-wave-2-header-select.html
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/16-workshift/sketch-wave-3-shift-table.html
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/16-workshift/sketch-wave-4-legend.html
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/16-workshift/wave-5-tsx-conversion-checklist.md

# Source TSX + utils (sketch 대상, 변경 0 — 2 파일 모두 untouched)
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/src/pages/AnnualPlanPage.tsx

# 17-annual-plan redesign 컨텍스트 (토큰/타이포 source)
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/17-annual-plan/17-annual-plan.md
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/17-annual-plan/design-system.md
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/17-annual-plan/tokens.css
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/17-annual-plan/typography.css

<rules_verbatim>
### OQ LOCKED 5건 (wave-1-index.md §7 verbatim, 2026-05-22)

**OQ #1 (다운로드 버튼 bg-safe-bar solid + 그라데이션 폐기):**
> LOCKED (2026-05-22): 다운로드 버튼 = `bg-safe-bar text-text-on-accent` solid + disabled `bg-surface-sunken text-text-tertiary cursor-not-allowed`. 그라데이션 완전 폐기. W4 sketch + TSX 변환 양쪽 동일 적용.

**OQ #2 (위치조정 토글 토큰화):**
> LOCKED (2026-05-22): 위치조정 토글 — active = `border-accent bg-accent/10 text-accent`, 평시 = `bg-surface-sunken border-border-strong text-text-secondary`. W2 sketch + TSX 변환 양쪽 동일.

**OQ #3 (폰트 격상 verbatim):**
> LOCKED (2026-05-22): 폰트 격상 매핑 verbatim (sketch + TSX 양쪽):
> - fontSize:11 (모바일 위치조정 토글) → `text-caption` (12) `leading-none`
> - fontSize:12 (데스크톱 설명·토글·캘리브 안내 칩) → `text-caption` (12) `leading-none` 유지
> - fontSize:13 (모바일 설명·데스크톱 다운로드) → `text-label` (13) `leading-none`
> - fontSize:14 (모바일 헤더 타이틀·모바일 다운로드 버튼) → `text-body` (16) `font-bold` 격상
> - 연도 오버레이 `'min(1.4vw, 16px)'` 유지 (캘리브 좌표 시스템 일부, 엑셀 표지 시각 일치)

**OQ #4 (Lucide 아이콘 교체):**
> LOCKED (2026-05-22): 모바일 back button = Lucide `ChevronLeft size={15}` + 다운로드 = Lucide `Download size={15}` (데스크톱) / `Download size={16}` (모바일). lucide-react import 추가 (svg path 인라인 폐기). W2 모바일 chrome + W4 다운로드 양쪽 적용.

**OQ #5 (preview border 토큰 + 캘리브 안내 칩 인라인):**
> LOCKED (2026-05-22): preview border (캘리브 모드) = `border-2 border-accent` 토큰 / 평시 = `border border-border-default`. 캘리브 안내 칩 = 인라인 `bg-[rgba(59,130,246,0.9)] text-white` 유지 (color #fff + fontWeight 700 + whiteSpace nowrap + pointerEvents none 보존). W3 preview-calibration sketch + TSX 변환 양쪽 동일.

### 디자인 토큰 패턴 (wave-1-index.md §3 + planning constraints verbatim)
- `bg-surface-page` (var(--bg) → --surface-page) 페이지 외곽
- `bg-surface-raised` (var(--bg2) → --surface-raised) 헤더/데스크톱 상단 바
- `bg-surface-sunken` (var(--bg3) → --surface-sunken) back button / 토글 평시 / disabled 다운로드
- `border-border-default` (var(--bd) → --border-default)
- `border-border-strong` (var(--bd2) → --border-strong) 토글 평시 border
- `text-text-primary` (var(--t1) → --text-primary) 타이틀
- `text-text-secondary` (var(--t2) → --text-secondary) 토글 평시 / ChevronLeft
- `text-text-tertiary` (var(--t3) → --text-tertiary) 설명 / disabled 다운로드
- `text-text-on-accent` (다운로드 버튼 위 흰 글자)
- `bg-safe-bar` / `bg-accent` / `border-accent` / `text-accent` (status- prefix 없음 — memory feedback_tailwind_token_class_pattern)
- `bg-accent/10` (위치조정 토글 active 배경, alpha 변종)
- radius: `rounded-sm` 8 / `rounded-md` 12 / `rounded-lg` 16 / 토큰 없는 값은 `rounded-[Npx]` 인라인

### Negative rule (모든 task verify gate)
- 이모지 0건 (sketch 본문)
- `linear-gradient` 0건 (모든 sketch + checklist — OQ #1 LOCKED 그라데이션 완전 폐기 증거)
- 9·10·11px fontSize 0건 (OQ #3 LOCKED 격상 결과)
- `(text|bg|border)-status-` class prefix 0건 (`text-status-safe` X, `bg-status-fire` X)
- `\b(w|h)-8\b` 0건 (tailwind config w-8=48 함정, memory feedback_tailwind_w8_h8_is_48px)
- 옛 alias 토큰 (`var(--bg)` `var(--bg2)` `var(--bg3)` `var(--bd)` `var(--bd2)` `var(--t1)` `var(--t2)` `var(--t3)` `var(--acl)` `var(--accent)`) 0건 in sketch (단, tokens.css 정의 블록 인용 시 fence 안 예외)

### 평면 폴더 패턴 (13-schedule + 14-reports + 27-login + 16-workshift 일관)
- 17-annual-plan/ 직속에 4 파일 배치
- `sketch/` 서브폴더 절대 금지

### ★ 캘리브 좌표 시스템 시그니처 보존 (W3 + W5 필수, 15-daily-report SW3 precedent)
다음 식별자/값은 1 byte 변경 금지 — W3 sketch 안 verbatim 명시 + W5 checklist 안 verbatim 박제:
- `STORAGE_KEY = 'annual_plan_year_pos'` (line 7) — localStorage key
- `FINGER_OFFSET = 60` (line 8) — TouchEvent y 보정 px
- `yearPos: { x: number; y: number } | null` — % 좌표 (분모 100 변경 금지)
- `handleImageClick` (line 35~45) 좌표 계산식 `((e.clientX - rect.left) / rect.width) * 100` verbatim
- `handleImageTouch` (line 47~59) `touch.clientY - FINGER_OFFSET` 보정 식 verbatim
- 오버레이 inline style `fontSize:'min(1.4vw, 16px)' + fontFamily:'Malgun Gothic, 맑은 고딕, sans-serif'` (line 83~84)
- preview 자산 `'/templates/preview/annual-plan.png'` (line 65)
- `nextYear = new Date().getFullYear() + 1` (line 21)
- `generateAnnualPlan()` direct import (line 4, dynamic import 아님)
</rules_verbatim>
</context>

<tasks>

<task type="auto">
  <name>Task 1 (W2): 모바일 헤더 + 데스크톱 상단 바 sketch 생성 (chrome 통일 4 frame, border-accent + Lucide ChevronLeft)</name>
  <files>cha-bio-safety/docs/redesign-context/17-annual-plan/sketch-wave-2-chrome.html</files>
  <action>
    16-workshift/sketch-wave-2-header-select.html 패턴 mirror 로 새 sketch 파일 작성. 영역 4 상단 (모바일 헤더, AnnualPlanPage.tsx line 169~192) + 영역 3 상단 (데스크톱 상단 바, line 110~146) 시각화. **OQ #2 LOCKED (위치조정 토글 border-accent + bg-accent/10 + text-accent)** + **OQ #3 LOCKED (폰트 격상)** + **OQ #4 LOCKED (Lucide ChevronLeft size={15})** 3건이 본 wave 의 핵심. 단, **다운로드 버튼 자체는 W4 sketch 에서 sketch** — 본 W2 데스크톱 상단 바에서는 다운로드 버튼 placeholder/짧은 표현만.

    **구조 (16-workshift W2 mirror):**
    - `<!DOCTYPE html>` + `<html lang="ko">` + `<head><meta charset="utf-8"><title>redesign/17-annual-plan — W2 모바일 헤더 + 데스크톱 상단 바</title>` + `<style>` 블록
    - `<style>` 안: v0.1.1 tokens.css 의 dark+light 양쪽 정의 verbatim 인용 (`[data-theme="dark"]` + `[data-theme="light"]` selector), typography.css 의 `text-caption` `text-label` `text-body-sm` `text-body` `font-bold` 정의 verbatim 인용
    - `<body>` 안 4 frame side-by-side:
      | frame | viewport | 시각화 |
      |---|---|---|
      | 1 | 모바일 다크 393×320 | `data-theme="dark"` + 모바일 헤더 (back button + 타이틀 "연간 업무 추진 계획" + 위치조정 토글 평시) + 위치조정 토글 active variant row (분기 시각화) |
      | 2 | 모바일 라이트 393×320 | `data-theme="light"` + 동일 (라이트 명도 차 확인) |
      | 3 | 데스크톱 다크 1280×320 | `data-theme="dark"` + 데스크톱 상단 바 (설명 + 위치조정 토글 평시 + 다운로드 버튼 placeholder "[W4 다운로드 버튼 자리]") + 위치조정 active variant row |
      | 4 | 데스크톱 라이트 1280×320 | `data-theme="light"` + 동일 |

    **모바일 헤더 마크업 룰 (frame 1, 2 — OQ #2 + #3 + #4 LOCKED 반영):**
    ```html
    <header class="bg-surface-raised border-b border-border-default flex items-center" style="padding:8px 12px 9px;gap:8px;flex-shrink:0;">
      <!-- 모바일 back button (OQ #4 LOCKED: Lucide ChevronLeft size={15} + 34×34 w-[34px] h-[34px] 인라인, w-8=48 함정 회피) -->
      <button class="w-[34px] h-[34px] rounded-sm bg-surface-sunken border border-border-default flex items-center justify-center" style="cursor:pointer;flex-shrink:0;">
        <!-- Lucide ChevronLeft size={15} (sketch SVG 표현, TSX 변환 시 lucide-react import) -->
        <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="text-text-secondary">
          <polyline points="15 18 9 12 15 6"></polyline>
        </svg>
      </button>
      <!-- 타이틀 (OQ #3 LOCKED: 모바일 헤더 타이틀 14→16 격상, text-body font-bold) -->
      <span class="text-body font-bold text-text-primary" style="flex:1;">연간 업무 추진 계획</span>
      <!-- 위치조정 토글 평시 (OQ #2 LOCKED: bg-surface-sunken border-border-strong text-text-secondary + OQ #3 LOCKED: 11→text-caption 격상 + leading-none) -->
      <button class="bg-surface-sunken border border-border-strong text-text-secondary text-caption font-bold leading-none rounded-sm" style="padding:6px 10px;cursor:pointer;">
        위치 조정
      </button>
    </header>
    ```

    **모바일 헤더 active variant (frame 1 별도 row 또는 frame variant — OQ #2 LOCKED 토글 active 시각화):**
    ```html
    <header class="bg-surface-raised border-b border-border-default flex items-center" style="padding:8px 12px 9px;gap:8px;flex-shrink:0;">
      <button class="w-[34px] h-[34px] rounded-sm bg-surface-sunken border border-border-default flex items-center justify-center" style="cursor:pointer;flex-shrink:0;">
        <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="text-text-secondary">
          <polyline points="15 18 9 12 15 6"></polyline>
        </svg>
      </button>
      <span class="text-body font-bold text-text-primary" style="flex:1;">연간 업무 추진 계획</span>
      <!-- 위치조정 토글 active (OQ #2 LOCKED: border-accent + bg-accent/10 + text-accent) -->
      <button class="border-accent bg-accent/10 text-accent border text-caption font-bold leading-none rounded-sm" style="padding:6px 10px;cursor:pointer;">
        취소
      </button>
    </header>
    ```

    **데스크톱 상단 바 마크업 룰 (frame 3, 4 — OQ #2 + #3 LOCKED 반영, 다운로드 버튼은 placeholder):**
    ```html
    <div class="border-b border-border-default flex items-center" style="padding:14px 28px;gap:12px;flex-shrink:0;">
      <!-- 설명 (OQ #3 LOCKED: 12 text-caption leading-none 유지, dense layout 절충) -->
      <div class="text-caption leading-none text-text-tertiary" style="flex:1;">
        대상 연도 <strong class="text-text-primary font-bold">2027년</strong> — 표지 및 일정표 연도가 자동 설정됩니다.
      </div>
      <!-- 위치조정 토글 평시 (OQ #2 LOCKED + OQ #3 LOCKED: 12 text-caption leading-none + font-bold) -->
      <button class="bg-surface-sunken border border-border-strong text-text-secondary text-caption font-bold leading-none rounded-sm" style="padding:8px 14px;cursor:pointer;">
        위치 조정
      </button>
      <!-- 다운로드 버튼 placeholder (W4 sketch 에서 sketch — 본 W2 는 자리만 표시) -->
      <div class="text-caption leading-none text-text-tertiary" style="padding:8px 20px;border:1px dashed var(--border-default);border-radius:8px;opacity:.5;">
        [W4 다운로드 버튼 자리]
      </div>
    </div>
    ```

    **데스크톱 active variant (frame 3 별도 row):**
    ```html
    <div class="border-b border-border-default flex items-center" style="padding:14px 28px;gap:12px;flex-shrink:0;">
      <div class="text-caption leading-none text-text-tertiary" style="flex:1;">
        대상 연도 <strong class="text-text-primary font-bold">2027년</strong> — 표지 및 일정표 연도가 자동 설정됩니다.
      </div>
      <!-- 위치조정 토글 active (OQ #2 LOCKED: border-accent + bg-accent/10 + text-accent) -->
      <button class="border-accent bg-accent/10 text-accent border text-caption font-bold leading-none rounded-sm" style="padding:8px 14px;cursor:pointer;">
        취소
      </button>
      <div class="text-caption leading-none text-text-tertiary" style="padding:8px 20px;border:1px dashed var(--border-default);border-radius:8px;opacity:.5;">
        [W4 다운로드 버튼 자리]
      </div>
    </div>
    ```

    **카피 verbatim (memory `feedback_sketch_realistic_data`):**
    - '연간 업무 추진 계획' (모바일 타이틀)
    - '위치 조정' (토글 평시)
    - '취소' (토글 active)
    - '대상 연도' / '표지 및 일정표 연도가 자동 설정됩니다.' (데스크톱 설명)
    - '2027년' (nextYear placeholder, 2026년 + 1)

    **rules 박스 (sketch 하단, viewport-label 외 메타):**
    - LOCKED 결정: OQ #2 (위치조정 토글 border-accent + bg-accent/10 + text-accent active / bg-surface-sunken + border-border-strong + text-text-secondary 평시) + OQ #3 (모바일 헤더 타이틀 14→16 격상 / 모바일 위치조정 11→12 격상 + leading-none / 데스크톱 설명·토글 12 유지 + leading-none) + OQ #4 (Lucide ChevronLeft size={15} 교체, svg path 인라인 폐기) verbatim 인용 1 fence
    - 토큰 매핑: `var(--bg2) → --surface-raised` / `var(--bg3) → --surface-sunken` / `var(--bd) → --border-default` / `var(--bd2) → --border-strong` / `var(--t1) → --text-primary` / `var(--t2) → --text-secondary` / `var(--t3) → --text-tertiary` / `var(--acl) → --accent` (위치조정 토글 active) / radius:8 → `rounded-sm`
    - negative gate 자체매칭 회피: "이모지 0 / linear-gradient 0 / 9·10·11px 0 / status- prefix 0 / w-8 h-8 0 / 옛 alias 토큰 0" 메타 줄은 `<code>` fence 안 또는 escape (`w-8`/`h-8` 같은 문자열 sketch 본문에 등장하지 않도록 fence 처리, 16-workshift t12 deviation 참고)

    **commit 메시지:**
    `docs(quick-260522-0j3): redesign/17-annual-plan W2 chrome sketch (4 frame, border-accent + Lucide ChevronLeft + 모바일 헤더 + 데스크톱 상단 바)`

    **commit 명령 (executor 가 실행 후 commit hash + message 보고):**
    `git add cha-bio-safety/docs/redesign-context/17-annual-plan/sketch-wave-2-chrome.html && git commit -m "docs(quick-260522-0j3): redesign/17-annual-plan W2 chrome sketch (4 frame, border-accent + Lucide ChevronLeft + 모바일 헤더 + 데스크톱 상단 바)"`
  </action>
  <verify>
    <automated>
test -f cha-bio-safety/docs/redesign-context/17-annual-plan/sketch-wave-2-chrome.html \
  && wc -l cha-bio-safety/docs/redesign-context/17-annual-plan/sketch-wave-2-chrome.html | awk '{ if ($1 < 200) { print "FAIL: too short"; exit 1 } else print "PASS lines:", $1 }' \
  && grep -c 'data-theme="dark"' cha-bio-safety/docs/redesign-context/17-annual-plan/sketch-wave-2-chrome.html | awk '{ if ($1 < 2) { print "FAIL: dark frame <2"; exit 1 } else print "PASS dark frame:", $1 }' \
  && grep -c 'data-theme="light"' cha-bio-safety/docs/redesign-context/17-annual-plan/sketch-wave-2-chrome.html | awk '{ if ($1 < 2) { print "FAIL: light frame <2"; exit 1 } else print "PASS light frame:", $1 }' \
  && grep -c 'border-accent' cha-bio-safety/docs/redesign-context/17-annual-plan/sketch-wave-2-chrome.html | awk '{ if ($1 < 2) { print "FAIL: border-accent <2 (OQ #2 LOCKED active variant 위반)"; exit 1 } else print "PASS border-accent:", $1 }' \
  && grep -c 'bg-accent/10' cha-bio-safety/docs/redesign-context/17-annual-plan/sketch-wave-2-chrome.html | awk '{ if ($1 < 2) { print "FAIL: bg-accent/10 <2 (OQ #2 LOCKED active variant 위반)"; exit 1 } else print "PASS bg-accent/10:", $1 }' \
  && grep -c 'text-accent' cha-bio-safety/docs/redesign-context/17-annual-plan/sketch-wave-2-chrome.html | awk '{ if ($1 < 2) { print "FAIL: text-accent <2"; exit 1 } else print "PASS text-accent:", $1 }' \
  && grep -c 'bg-surface-sunken' cha-bio-safety/docs/redesign-context/17-annual-plan/sketch-wave-2-chrome.html | awk '{ if ($1 < 4) { print "FAIL: bg-surface-sunken <4 (back button + 토글 평시)"; exit 1 } else print "PASS bg-surface-sunken:", $1 }' \
  && grep -c 'border-border-strong' cha-bio-safety/docs/redesign-context/17-annual-plan/sketch-wave-2-chrome.html | awk '{ if ($1 < 2) { print "FAIL: border-border-strong <2 (토글 평시)"; exit 1 } else print "PASS border-border-strong:", $1 }' \
  && grep -c '연간 업무 추진 계획' cha-bio-safety/docs/redesign-context/17-annual-plan/sketch-wave-2-chrome.html | awk '{ if ($1 < 2) { print "FAIL: 모바일 타이틀 <2 (모바일 frame 2개)"; exit 1 } else print "PASS 연간 업무 추진 계획:", $1 }' \
  && grep -c '위치 조정' cha-bio-safety/docs/redesign-context/17-annual-plan/sketch-wave-2-chrome.html | awk '{ if ($1 < 4) { print "FAIL: 위치 조정 <4 (4 frame x 1 평시)"; exit 1 } else print "PASS 위치 조정:", $1 }' \
  && grep -c '취소' cha-bio-safety/docs/redesign-context/17-annual-plan/sketch-wave-2-chrome.html | awk '{ if ($1 < 2) { print "FAIL: 취소 <2 (active variant)"; exit 1 } else print "PASS 취소:", $1 }' \
  && grep -c '대상 연도' cha-bio-safety/docs/redesign-context/17-annual-plan/sketch-wave-2-chrome.html | awk '{ if ($1 < 2) { print "FAIL: 대상 연도 <2 (데스크톱 frame 2개)"; exit 1 } else print "PASS 대상 연도:", $1 }' \
  && grep -c 'w-\[34px\]' cha-bio-safety/docs/redesign-context/17-annual-plan/sketch-wave-2-chrome.html | awk '{ if ($1 < 2) { print "FAIL: 모바일 back button w-[34px] <2 (모바일 frame 2개)"; exit 1 } else print "PASS w-[34px]:", $1 }' \
  && grep -cE 'ChevronLeft|polyline points="15 18 9 12 15 6"' cha-bio-safety/docs/redesign-context/17-annual-plan/sketch-wave-2-chrome.html | awk '{ if ($1 < 2) { print "FAIL: Lucide ChevronLeft <2 (OQ #4 LOCKED)"; exit 1 } else print "PASS ChevronLeft:", $1 }' \
  && grep -cE 'linear-gradient' cha-bio-safety/docs/redesign-context/17-annual-plan/sketch-wave-2-chrome.html | awk '{ if ($1 > 0) { print "FAIL: linear-gradient found (OQ #1 LOCKED 위반)"; exit 1 } else print "PASS no gradient" }' \
  && grep -cE '(text|bg|border)-status-(safe|fire|warning|danger|caution)' cha-bio-safety/docs/redesign-context/17-annual-plan/sketch-wave-2-chrome.html | awk '{ if ($1 > 0) { print "FAIL: status- prefix found"; exit 1 } else print "PASS no status- prefix" }' \
  && grep -v '^<!--' cha-bio-safety/docs/redesign-context/17-annual-plan/sketch-wave-2-chrome.html | grep -v '^#' | grep -cE 'font-size:[[:space:]]*(9|10|11)[^0-9]|fontSize:[[:space:]]*(9|10|11)[^0-9]' | awk '{ if ($1 > 0) { print "FAIL: 9/10/11 px found"; exit 1 } else print "PASS no 9-11px" }' \
  && grep -cE '\bw-8\b|\bh-8\b' cha-bio-safety/docs/redesign-context/17-annual-plan/sketch-wave-2-chrome.html | awk '{ if ($1 > 0) { print "FAIL: w-8/h-8 found (48px 함정)"; exit 1 } else print "PASS no w-8 h-8" }' \
  && grep -cE 'OQ #2|OQ #3|OQ #4' cha-bio-safety/docs/redesign-context/17-annual-plan/sketch-wave-2-chrome.html | awk '{ if ($1 < 3) { print "FAIL: OQ #2/#3/#4 citation <3"; exit 1 } else print "PASS OQ citations:", $1 }' \
  && git log --oneline -1 | grep -q '260522-0j3.*W2' && echo "PASS commit W2"
    </automated>
  </verify>
  <done>
    - sketch-wave-2-chrome.html 파일 존재 + ≥200 lines (≥350 권장)
    - data-theme="dark" / data-theme="light" 각 ≥2 frame (총 4 frame)
    - border-accent / bg-accent/10 / text-accent (active variant) 각 ≥2 (OQ #2 LOCKED)
    - bg-surface-sunken (back button + 토글 평시) ≥4 / border-border-strong (토글 평시) ≥2
    - "연간 업무 추진 계획" (모바일 타이틀) ≥2 / "위치 조정" (평시) ≥4 / "취소" (active) ≥2 / "대상 연도" (데스크톱) ≥2
    - w-[34px] (모바일 back button) ≥2 / Lucide ChevronLeft polyline 15-18 / 9-12 / 15-6 ≥2 (OQ #4 LOCKED)
    - linear-gradient 0건 (OQ #1 LOCKED) / status- prefix 0건 / 9·10·11px fontSize 0건 / w-8 h-8 0건
    - OQ #2 + OQ #3 + OQ #4 인용 ≥3건
    - atomic commit `docs(quick-260522-0j3): ... W2 chrome sketch ...` 1개
  </done>
</task>

<task type="auto">
  <name>Task 2 (W3): preview + 캘리브 좌표 시스템 sketch 생성 (★ 캘리브 단독 wave, 4 frame 매트릭스, border-accent + rgba 인라인 + yearPos overlay)</name>
  <files>cha-bio-safety/docs/redesign-context/17-annual-plan/sketch-wave-3-preview-calibration.html</files>
  <action>
    16-workshift/sketch-wave-3-shift-table.html 패턴 mirror 로 새 sketch 파일 작성. 영역 2 단독 (previewImage element, AnnualPlanPage.tsx line 61~103) 시각화. **캘리브 좌표 시스템 단독 wave** (옵션 C, 15-daily-report SW3 portraitPos precedent 동일 적용). **OQ #3 LOCKED (캘리브 안내 칩 12 text-caption leading-none + font-bold / 연도 오버레이 min(1.4vw, 16px) 유지)** + **OQ #5 LOCKED (preview border-2 border-accent 캘리브 / border border-border-default 평시 / 캘리브 안내 칩 인라인 rgba(59,130,246,0.9) + text-white)** 2건이 본 wave 의 핵심.

    **★ 캘리브 좌표 시스템 시그니처 보존 (W3 sketch 안 verbatim 명시 필수, 15-daily-report SW3 precedent):**

    sketch HTML 의 rules 박스 안에 다음 5건을 verbatim fence 로 박제 — W5 checklist 진입 시 동일 5건 재확인.

    ```
    1. STORAGE_KEY = 'annual_plan_year_pos'              (line 7, localStorage key)
    2. FINGER_OFFSET = 60                                (line 8, TouchEvent y 보정 px)
    3. handleImageClick: e.currentTarget.getBoundingClientRect() → ((e.clientX - rect.left) / rect.width) * 100 → setYearPos + localStorage.setItem + setCalibMode(false)
    4. handleImageTouch: touch.clientY - FINGER_OFFSET 보정 → ((... - rect.top) / rect.height) * 100 → 동일 흐름
    5. yearPos overlay inline style: top:`${yearPos.y}%` left:`${yearPos.x}%` transform:'translate(-50%,-50%)' fontSize:'min(1.4vw, 16px)' fontFamily:'Malgun Gothic, 맑은 고딕, sans-serif' fontWeight:700 color:'#000' pointerEvents:'none'
    ```

    **구조 (16-workshift W3 mirror):**
    - `<!DOCTYPE html>` + `<head><title>redesign/17-annual-plan — W3 preview + 캘리브 좌표 시스템</title>` + `<style>` 블록 (W2 와 동일 tokens + typography 인용)
    - `<body>` 안 4 frame side-by-side (상태 매트릭스):
      | frame | viewport | 시각화 |
      |---|---|---|
      | 1 | 모바일 다크 393×600 평시 (yearPos null) | `data-theme="dark"` + preview img placeholder (background:#fff border border-border-default rounded-sm) — 오버레이 / 안내 칩 둘 다 없음 |
      | 2 | 모바일 다크 393×600 캘리브 모드 (yearPos null) | `data-theme="dark"` + preview img (border-2 border-accent + cursor crosshair) + 캘리브 안내 칩 (인라인 rgba(59,130,246,0.9) + text-white + 'on top:8') |
      | 3 | 데스크톱 다크 1280×800 평시 (yearPos 저장후) | `data-theme="dark"` + 외곽 (flex center padding:24 background var(--bg)) + preview wrapper (maxWidth: 'calc((100vh - 140px) * 1.414)') + preview img (border border-border-default) + 연도 오버레이 ('2027' 가운데 표시, min(1.4vw, 16px) + Malgun Gothic) |
      | 4 | 데스크톱 다크 1280×800 캘리브 모드 (yearPos 저장후) | `data-theme="dark"` + 위 + preview img (border-2 border-accent + cursor crosshair) + 캘리브 안내 칩 + 연도 오버레이 (둘 다 동시) |

    **preview 마크업 룰 (각 frame 공통 — OQ #3 + #5 LOCKED 반영):**

    **평시 (frame 1, 3 — yearPos null 또는 저장됨, calibMode false):**
    ```html
    <div style="position:relative;width:100%;height:100%;">
      <img
        src="/templates/preview/annual-plan.png"
        alt="연간 업무 추진 계획 미리보기"
        class="border border-border-default rounded-sm"
        style="width:100%;height:100%;object-fit:contain;background:#fff;cursor:default;"
      />
      <!-- frame 3: yearPos 저장됨 → 연도 오버레이 표시 -->
      <!-- 연도 오버레이 (yearPos overlay, line 78~89 verbatim) -->
      <div style="position:absolute;top:62%;left:52%;transform:translate(-50%,-50%);font-size:min(1.4vw, 16px);font-weight:700;color:#000;font-family:'Malgun Gothic, 맑은 고딕, sans-serif';pointer-events:none;">
        2027
      </div>
    </div>
    ```

    **캘리브 모드 (frame 2, 4 — calibMode true, OQ #5 LOCKED border-2 border-accent + 캘리브 안내 칩 rgba 인라인):**
    ```html
    <div style="position:relative;width:100%;height:100%;">
      <!-- preview img (OQ #5 LOCKED: border-2 border-accent 토큰 + cursor crosshair 인라인 분기) -->
      <img
        src="/templates/preview/annual-plan.png"
        alt="연간 업무 추진 계획 미리보기"
        class="border-2 border-accent rounded-sm"
        style="width:100%;height:100%;object-fit:contain;background:#fff;cursor:crosshair;"
      />
      <!-- 캘리브 안내 칩 (OQ #5 LOCKED: 인라인 rgba(59,130,246,0.9) + text-white + OQ #3 LOCKED: 12 text-caption leading-none + font-bold) -->
      <div class="text-caption font-bold leading-none text-white rounded-sm" style="position:absolute;top:8px;left:50%;transform:translateX(-50%);background:rgba(59,130,246,0.9);padding:6px 16px;white-space:nowrap;pointer-events:none;">
        연도가 들어갈 위치를 클릭하세요
      </div>
      <!-- frame 4: yearPos 저장됨 + calibMode true → 둘 다 표시 -->
      <div style="position:absolute;top:62%;left:52%;transform:translate(-50%,-50%);font-size:min(1.4vw, 16px);font-weight:700;color:#000;font-family:'Malgun Gothic, 맑은 고딕, sans-serif';pointer-events:none;">
        2027
      </div>
    </div>
    ```

    **데스크톱 wrapper 마크업 (frame 3, 4 — line 148~161 verbatim):**
    ```html
    <div style="flex:1;min-height:0;overflow:hidden;display:flex;align-items:center;justify-content:center;padding:24px;background:var(--surface-page);">
      <div style="width:100%;height:100%;max-width:calc((100vh - 140px) * 1.414);max-height:100%;">
        <!-- previewImage 마운트 -->
      </div>
    </div>
    ```

    **모바일 wrapper 마크업 (frame 1, 2 — line 194~198 verbatim):**
    ```html
    <div style="flex:1;overflow:auto;padding:16px;display:flex;flex-direction:column;gap:16px;">
      <div style="width:100%;">
        <!-- previewImage 마운트 -->
      </div>
    </div>
    ```

    **카피 verbatim (memory `feedback_sketch_realistic_data`):**
    - '연간 업무 추진 계획 미리보기' (img alt, line 66 verbatim)
    - '연도가 들어갈 위치를 클릭하세요' (캘리브 안내 칩, line 99 verbatim)
    - '2027' (nextYear placeholder = new Date().getFullYear() + 1, 2026년 + 1)

    **rules 박스 (sketch 하단, viewport-label 외 메타 — 캘리브 좌표 시스템 시그니처 5건 박제 필수):**
    - LOCKED 결정: OQ #3 (캘리브 안내 칩 12 text-caption leading-none + font-bold / 연도 오버레이 min(1.4vw, 16px) 유지) + OQ #5 (preview border 캘리브 모드 border-2 border-accent / 평시 border border-border-default / 캘리브 안내 칩 인라인 rgba(59,130,246,0.9) + text-white + color #fff + fontWeight 700 + whiteSpace nowrap + pointerEvents none 보존) verbatim 인용 1 fence
    - **★ 캘리브 좌표 시스템 시그니처 5건 verbatim fence:**
      ```
      STORAGE_KEY = 'annual_plan_year_pos'
      FINGER_OFFSET = 60
      handleImageClick: ((e.clientX - rect.left) / rect.width) * 100
      handleImageTouch: ((touch.clientY - FINGER_OFFSET - rect.top) / rect.height) * 100
      yearPos overlay: top:${y}% left:${x}% transform:translate(-50%,-50%) fontSize:'min(1.4vw, 16px)' fontFamily:'Malgun Gothic, 맑은 고딕, sans-serif' fontWeight:700 color:'#000' pointerEvents:'none'
      ```
    - 토큰 매핑: preview border 평시 `1px solid var(--bd)` → `border border-border-default` / 캘리브 `2px solid var(--acl)` → `border-2 border-accent` / 외곽 wrapper background `var(--bg)` → `var(--surface-page)` (CSS 변수 사용은 sketch raw HTML 표현, TSX 변환 시 Tailwind class 또는 인라인) / cursor 분기 'crosshair' / 'default' 인라인 유지
    - 15-daily-report SW3 precedent 명시: "15-daily-report SW3 의 portraitPos 보존 룰 동일 적용. 좌표 시스템 시그니처 5건 1 byte 변경 시 사용자 좌표 손실 → W5 TSX 변환 wave grep gate 강제"
    - negative gate 자체매칭 회피: "이모지 0 / linear-gradient 0 / 9·10·11px 0 / status- prefix 0 / w-8 h-8 0 / 옛 alias 토큰 0" fence 안 또는 escape

    **commit 메시지:**
    `docs(quick-260522-0j3): redesign/17-annual-plan W3 preview-calibration sketch (4 frame 매트릭스, border-accent 캘리브 + 캘리브 안내 칩 rgba 인라인 + yearPos overlay + 캘리브 좌표 시스템 시그니처 5건 박제)`

    **commit 명령 (executor 가 실행 후 commit hash + message 보고):**
    `git add cha-bio-safety/docs/redesign-context/17-annual-plan/sketch-wave-3-preview-calibration.html && git commit -m "docs(quick-260522-0j3): redesign/17-annual-plan W3 preview-calibration sketch (4 frame 매트릭스, border-accent 캘리브 + 캘리브 안내 칩 rgba 인라인 + yearPos overlay + 캘리브 좌표 시스템 시그니처 5건 박제)"`
  </action>
  <verify>
    <automated>
test -f cha-bio-safety/docs/redesign-context/17-annual-plan/sketch-wave-3-preview-calibration.html \
  && wc -l cha-bio-safety/docs/redesign-context/17-annual-plan/sketch-wave-3-preview-calibration.html | awk '{ if ($1 < 250) { print "FAIL: too short"; exit 1 } else print "PASS lines:", $1 }' \
  && grep -c 'data-theme="dark"' cha-bio-safety/docs/redesign-context/17-annual-plan/sketch-wave-3-preview-calibration.html | awk '{ if ($1 < 4) { print "FAIL: dark frame <4 (4 frame 모두 다크 매트릭스)"; exit 1 } else print "PASS dark frame:", $1 }' \
  && grep -c 'border-2 border-accent' cha-bio-safety/docs/redesign-context/17-annual-plan/sketch-wave-3-preview-calibration.html | awk '{ if ($1 < 2) { print "FAIL: border-2 border-accent <2 (캘리브 모드 frame 2/4, OQ #5 LOCKED 위반)"; exit 1 } else print "PASS border-2 border-accent:", $1 }' \
  && grep -c 'border-border-default' cha-bio-safety/docs/redesign-context/17-annual-plan/sketch-wave-3-preview-calibration.html | awk '{ if ($1 < 1) { print "FAIL: border-border-default <1 (평시 preview frame 1)"; exit 1 } else print "PASS border-border-default:", $1 }' \
  && grep -c 'rgba(59,130,246,0.9)' cha-bio-safety/docs/redesign-context/17-annual-plan/sketch-wave-3-preview-calibration.html | awk '{ if ($1 < 2) { print "FAIL: 캘리브 안내 칩 rgba(59,130,246,0.9) <2 (OQ #5 LOCKED 위반)"; exit 1 } else print "PASS 안내 칩 rgba:", $1 }' \
  && grep -c 'text-white' cha-bio-safety/docs/redesign-context/17-annual-plan/sketch-wave-3-preview-calibration.html | awk '{ if ($1 < 2) { print "FAIL: text-white <2 (안내 칩 OQ #5 LOCKED)"; exit 1 } else print "PASS text-white:", $1 }' \
  && grep -c 'min(1.4vw, 16px)' cha-bio-safety/docs/redesign-context/17-annual-plan/sketch-wave-3-preview-calibration.html | awk '{ if ($1 < 2) { print "FAIL: 연도 오버레이 min(1.4vw, 16px) <2 (frame 3/4 OQ #3 LOCKED)"; exit 1 } else print "PASS min(1.4vw, 16px):", $1 }' \
  && grep -c 'Malgun Gothic' cha-bio-safety/docs/redesign-context/17-annual-plan/sketch-wave-3-preview-calibration.html | awk '{ if ($1 < 2) { print "FAIL: Malgun Gothic <2 (캘리브 좌표 시스템 일부)"; exit 1 } else print "PASS Malgun Gothic:", $1 }' \
  && grep -c 'STORAGE_KEY' cha-bio-safety/docs/redesign-context/17-annual-plan/sketch-wave-3-preview-calibration.html | awk '{ if ($1 < 1) { print "FAIL: STORAGE_KEY 시그니처 박제 missing"; exit 1 } else print "PASS STORAGE_KEY:", $1 }' \
  && grep -c 'FINGER_OFFSET' cha-bio-safety/docs/redesign-context/17-annual-plan/sketch-wave-3-preview-calibration.html | awk '{ if ($1 < 1) { print "FAIL: FINGER_OFFSET 시그니처 박제 missing"; exit 1 } else print "PASS FINGER_OFFSET:", $1 }' \
  && grep -c 'handleImageClick' cha-bio-safety/docs/redesign-context/17-annual-plan/sketch-wave-3-preview-calibration.html | awk '{ if ($1 < 1) { print "FAIL: handleImageClick 시그니처 박제 missing"; exit 1 } else print "PASS handleImageClick:", $1 }' \
  && grep -c 'handleImageTouch' cha-bio-safety/docs/redesign-context/17-annual-plan/sketch-wave-3-preview-calibration.html | awk '{ if ($1 < 1) { print "FAIL: handleImageTouch 시그니처 박제 missing"; exit 1 } else print "PASS handleImageTouch:", $1 }' \
  && grep -c 'yearPos' cha-bio-safety/docs/redesign-context/17-annual-plan/sketch-wave-3-preview-calibration.html | awk '{ if ($1 < 1) { print "FAIL: yearPos 시그니처 박제 missing"; exit 1 } else print "PASS yearPos:", $1 }' \
  && grep -c '연도가 들어갈 위치를 클릭하세요' cha-bio-safety/docs/redesign-context/17-annual-plan/sketch-wave-3-preview-calibration.html | awk '{ if ($1 < 2) { print "FAIL: 안내 칩 카피 verbatim <2 (캘리브 모드 frame 2/4)"; exit 1 } else print "PASS 안내 칩 카피:", $1 }' \
  && grep -c '/templates/preview/annual-plan.png' cha-bio-safety/docs/redesign-context/17-annual-plan/sketch-wave-3-preview-calibration.html | awk '{ if ($1 < 1) { print "FAIL: preview 자산 경로 verbatim missing"; exit 1 } else print "PASS preview 자산:", $1 }' \
  && grep -c '2027' cha-bio-safety/docs/redesign-context/17-annual-plan/sketch-wave-3-preview-calibration.html | awk '{ if ($1 < 2) { print "FAIL: nextYear 2027 placeholder <2 (overlay frame 3/4)"; exit 1 } else print "PASS 2027:", $1 }' \
  && grep -c 'crosshair' cha-bio-safety/docs/redesign-context/17-annual-plan/sketch-wave-3-preview-calibration.html | awk '{ if ($1 < 2) { print "FAIL: cursor crosshair <2 (캘리브 모드 frame 2/4)"; exit 1 } else print "PASS crosshair:", $1 }' \
  && grep -cE 'linear-gradient' cha-bio-safety/docs/redesign-context/17-annual-plan/sketch-wave-3-preview-calibration.html | awk '{ if ($1 > 0) { print "FAIL: linear-gradient found"; exit 1 } else print "PASS no gradient" }' \
  && grep -cE '(text|bg|border)-status-(safe|fire|warning|danger|caution)' cha-bio-safety/docs/redesign-context/17-annual-plan/sketch-wave-3-preview-calibration.html | awk '{ if ($1 > 0) { print "FAIL: status- prefix found"; exit 1 } else print "PASS no status- prefix" }' \
  && grep -v '^<!--' cha-bio-safety/docs/redesign-context/17-annual-plan/sketch-wave-3-preview-calibration.html | grep -v '^#' | grep -cE 'font-size:[[:space:]]*(9|10|11)[^0-9]|fontSize:[[:space:]]*(9|10|11)[^0-9]' | awk '{ if ($1 > 0) { print "FAIL: 9/10/11 px found"; exit 1 } else print "PASS no 9-11px" }' \
  && grep -cE '\bw-8\b|\bh-8\b' cha-bio-safety/docs/redesign-context/17-annual-plan/sketch-wave-3-preview-calibration.html | awk '{ if ($1 > 0) { print "FAIL: w-8/h-8 found"; exit 1 } else print "PASS no w-8 h-8" }' \
  && grep -cE 'OQ #3|OQ #5' cha-bio-safety/docs/redesign-context/17-annual-plan/sketch-wave-3-preview-calibration.html | awk '{ if ($1 < 2) { print "FAIL: OQ #3/#5 citation <2"; exit 1 } else print "PASS OQ citations:", $1 }' \
  && git log --oneline -1 | grep -q '260522-0j3.*W3' && echo "PASS commit W3"
    </automated>
  </verify>
  <done>
    - sketch-wave-3-preview-calibration.html 파일 존재 + ≥250 lines (≥400 권장)
    - data-theme="dark" ≥4 (4 frame 매트릭스 모두 다크)
    - border-2 border-accent (캘리브 모드) ≥2 / border-border-default (평시) ≥1
    - rgba(59,130,246,0.9) (캘리브 안내 칩) ≥2 / text-white (안내 칩) ≥2
    - min(1.4vw, 16px) (연도 오버레이) ≥2 / Malgun Gothic (폰트 패밀리) ≥2
    - 캘리브 좌표 시스템 시그니처 5건 (STORAGE_KEY / FINGER_OFFSET / handleImageClick / handleImageTouch / yearPos) 모두 ≥1 박제
    - '연도가 들어갈 위치를 클릭하세요' (캘리브 안내 칩 카피 verbatim) ≥2
    - '/templates/preview/annual-plan.png' (preview 자산 경로 verbatim) ≥1
    - '2027' (nextYear placeholder) ≥2 / 'crosshair' (cursor 분기) ≥2
    - linear-gradient 0건 / status- prefix 0건 / 9·10·11px fontSize 0건 / w-8 h-8 0건
    - OQ #3 + OQ #5 인용 ≥2건
    - atomic commit `docs(quick-260522-0j3): ... W3 preview-calibration sketch ...` 1개
  </done>
</task>

<task type="auto">
  <name>Task 3 (W4): 다운로드 버튼 + 설명 sketch 생성 (모바일 + 데스크톱 4 frame, bg-safe-bar solid + Lucide Download + disabled 분기)</name>
  <files>cha-bio-safety/docs/redesign-context/17-annual-plan/sketch-wave-4-download.html</files>
  <action>
    16-workshift/sketch-wave-4-legend.html 패턴 mirror 로 새 sketch 파일 작성. 영역 3 데스크톱 다운로드 버튼 (line 130~145) + 영역 5 모바일 설명/다운로드 (line 200~221) 시각화. **OQ #1 LOCKED (다운로드 bg-safe-bar solid + 그라데이션 완전 폐기 + disabled bg-surface-sunken text-text-tertiary cursor-not-allowed)** + **OQ #3 LOCKED (모바일 다운로드 14→16 text-body font-bold 격상 / 모바일 설명 13 text-label leading-relaxed / 데스크톱 다운로드 13 text-label leading-none / 데스크톱 설명 12 text-caption leading-none 유지)** + **OQ #4 LOCKED (Lucide Download size={15} 데스크톱 / Download size={16} 모바일)** 3건이 본 wave 의 핵심.

    **구조 (16-workshift W4 mirror, 4 frame):**
    - `<!DOCTYPE html>` + `<head><title>redesign/17-annual-plan — W4 다운로드 버튼 + 설명</title>` + `<style>` 블록 (W2/W3 와 동일 tokens + typography 인용)
    - `<body>` 안 4 frame side-by-side:
      | frame | viewport | 시각화 |
      |---|---|---|
      | 1 | 모바일 다크 393×280 default | `data-theme="dark"` + 설명 + 다운로드 버튼 (bg-safe-bar solid + Lucide Download size={16} + "엑셀 다운로드") |
      | 2 | 모바일 다크 393×280 loading disabled | `data-theme="dark"` + 설명 + 다운로드 버튼 disabled (bg-surface-sunken text-text-tertiary cursor-not-allowed + Lucide Download size={16} + "생성 중...") |
      | 3 | 데스크톱 다크 1280×280 default | `data-theme="dark"` + 데스크톱 상단 바 일부 (설명 + 위치조정 토글 평시) + 다운로드 버튼 (bg-safe-bar solid + Lucide Download size={15} + "엑셀 다운로드") |
      | 4 | 데스크톱 라이트 1280×280 default | `data-theme="light"` + 동일 (라이트 명도 차) |

    **모바일 다운로드 마크업 룰 (frame 1 default — OQ #1 + OQ #3 + OQ #4 LOCKED 반영):**
    ```html
    <div style="text-align:center;padding:16px;">
      <!-- 설명 (OQ #3 LOCKED: 13 text-label leading-relaxed) -->
      <div class="text-label leading-relaxed text-text-tertiary" style="margin-bottom:12px;">
        표지 및 일정표 연도가 2027년으로 자동 설정됩니다.
      </div>
      <!-- 다운로드 버튼 default (OQ #1 LOCKED: bg-safe-bar solid + text-text-on-accent + OQ #3 LOCKED: 14→16 text-body font-bold 격상 + OQ #4 LOCKED: Lucide Download size={16}) -->
      <button class="bg-safe-bar text-text-on-accent text-body font-bold rounded-md flex items-center justify-center" style="width:100%;padding:14px;gap:8px;border:none;cursor:pointer;">
        <!-- Lucide Download size={16} (sketch SVG 표현, TSX 변환 시 lucide-react import) -->
        <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"></path>
          <polyline points="7 10 12 15 17 10"></polyline>
          <line x1="12" y1="15" x2="12" y2="3"></line>
        </svg>
        엑셀 다운로드
      </button>
    </div>
    ```

    **모바일 다운로드 disabled variant (frame 2 — OQ #1 LOCKED: bg-surface-sunken text-text-tertiary cursor-not-allowed):**
    ```html
    <div style="text-align:center;padding:16px;">
      <div class="text-label leading-relaxed text-text-tertiary" style="margin-bottom:12px;">
        표지 및 일정표 연도가 2027년으로 자동 설정됩니다.
      </div>
      <!-- 다운로드 버튼 disabled (OQ #1 LOCKED) -->
      <button disabled class="bg-surface-sunken text-text-tertiary text-body font-bold rounded-md flex items-center justify-center" style="width:100%;padding:14px;gap:8px;border:none;cursor:not-allowed;">
        <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"></path>
          <polyline points="7 10 12 15 17 10"></polyline>
          <line x1="12" y1="15" x2="12" y2="3"></line>
        </svg>
        생성 중...
      </button>
    </div>
    ```

    **데스크톱 다운로드 마크업 룰 (frame 3, 4 — 상단 바 일부 + 다운로드 버튼):**
    ```html
    <div class="border-b border-border-default flex items-center" style="padding:14px 28px;gap:12px;flex-shrink:0;">
      <!-- 설명 (OQ #3 LOCKED: 12 text-caption leading-none 유지) -->
      <div class="text-caption leading-none text-text-tertiary" style="flex:1;">
        대상 연도 <strong class="text-text-primary font-bold">2027년</strong> — 표지 및 일정표 연도가 자동 설정됩니다.
      </div>
      <!-- 위치조정 토글 평시 (W2 sketch 와 동일 표현 — frame 동일 chrome) -->
      <button class="bg-surface-sunken border border-border-strong text-text-secondary text-caption font-bold leading-none rounded-sm" style="padding:8px 14px;cursor:pointer;">
        위치 조정
      </button>
      <!-- 데스크톱 다운로드 버튼 default (OQ #1 + OQ #3 + OQ #4 LOCKED) -->
      <button class="bg-safe-bar text-text-on-accent text-label font-bold leading-none rounded-sm flex items-center" style="padding:8px 20px;gap:8px;border:none;cursor:pointer;flex-shrink:0;">
        <!-- Lucide Download size={15} -->
        <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"></path>
          <polyline points="7 10 12 15 17 10"></polyline>
          <line x1="12" y1="15" x2="12" y2="3"></line>
        </svg>
        엑셀 다운로드
      </button>
    </div>
    ```

    **카피 verbatim (memory `feedback_sketch_realistic_data`):**
    - '엑셀 다운로드' (default, line 144 + 219 verbatim)
    - '생성 중...' (loading disabled, line 144 + 219 verbatim)
    - '표지 및 일정표 연도가 2027년으로 자동 설정됩니다.' (모바일 설명, line 203 verbatim with nextYear placeholder)
    - '대상 연도 2027년 — 표지 및 일정표 연도가 자동 설정됩니다.' (데스크톱 설명, line 116 verbatim)
    - '위치 조정' (데스크톱 토글 평시 — W2 와 동일 표현, chrome 일관)

    **rules 박스:**
    - LOCKED 결정: OQ #1 (다운로드 default bg-safe-bar text-text-on-accent solid / disabled bg-surface-sunken text-text-tertiary cursor-not-allowed / 그라데이션 완전 폐기) + OQ #3 (모바일 설명 13 text-label leading-relaxed / 모바일 다운로드 14→16 text-body font-bold 격상 / 데스크톱 설명 12 text-caption leading-none 유지 / 데스크톱 다운로드 13 text-label leading-none) + OQ #4 (Lucide Download size={15} 데스크톱 / size={16} 모바일 / svg path 인라인 폐기) verbatim 인용 1 fence
    - 토큰 매핑: 모바일 다운로드 `padding:14, borderRadius:10` → `rounded-md` (12) + padding 인라인 (radius 10 → 12 마이그레이션 §4.3 절충, sketch 단계 결정) / 데스크톱 다운로드 `padding:'8px 20px', borderRadius:8` → `rounded-sm` (8) + padding 인라인 / 데스크톱 wrapper `padding:'14px 28px', borderBottom:'1px solid var(--bd)'` → `border-b border-border-default` + padding 인라인 / `var(--acl) → --safe-bar (CTA)` / `var(--bg3) → --surface-sunken` / `var(--t3) → --text-tertiary`
    - negative gate 자체매칭 회피: "이모지 0 / linear-gradient 0 / 9·10·11px 0 / status- prefix 0 / w-8 h-8 0 / 옛 alias 토큰 0" fence 안 또는 escape

    **commit 메시지:**
    `docs(quick-260522-0j3): redesign/17-annual-plan W4 download sketch (4 frame, bg-safe-bar solid + Lucide Download + disabled bg-surface-sunken + 모바일/데스크톱 분기)`

    **commit 명령 (executor 가 실행 후 commit hash + message 보고):**
    `git add cha-bio-safety/docs/redesign-context/17-annual-plan/sketch-wave-4-download.html && git commit -m "docs(quick-260522-0j3): redesign/17-annual-plan W4 download sketch (4 frame, bg-safe-bar solid + Lucide Download + disabled bg-surface-sunken + 모바일/데스크톱 분기)"`
  </action>
  <verify>
    <automated>
test -f cha-bio-safety/docs/redesign-context/17-annual-plan/sketch-wave-4-download.html \
  && wc -l cha-bio-safety/docs/redesign-context/17-annual-plan/sketch-wave-4-download.html | awk '{ if ($1 < 250) { print "FAIL: too short"; exit 1 } else print "PASS lines:", $1 }' \
  && grep -c 'data-theme="dark"' cha-bio-safety/docs/redesign-context/17-annual-plan/sketch-wave-4-download.html | awk '{ if ($1 < 3) { print "FAIL: dark frame <3 (frame 1/2/3)"; exit 1 } else print "PASS dark frame:", $1 }' \
  && grep -c 'data-theme="light"' cha-bio-safety/docs/redesign-context/17-annual-plan/sketch-wave-4-download.html | awk '{ if ($1 < 1) { print "FAIL: light frame <1 (frame 4)"; exit 1 } else print "PASS light frame:", $1 }' \
  && grep -c 'bg-safe-bar' cha-bio-safety/docs/redesign-context/17-annual-plan/sketch-wave-4-download.html | awk '{ if ($1 < 3) { print "FAIL: bg-safe-bar <3 (default 3 frame: 모바일 1 + 데스크톱 2, OQ #1 LOCKED)"; exit 1 } else print "PASS bg-safe-bar:", $1 }' \
  && grep -c 'text-text-on-accent' cha-bio-safety/docs/redesign-context/17-annual-plan/sketch-wave-4-download.html | awk '{ if ($1 < 3) { print "FAIL: text-text-on-accent <3"; exit 1 } else print "PASS text-text-on-accent:", $1 }' \
  && grep -c 'bg-surface-sunken' cha-bio-safety/docs/redesign-context/17-annual-plan/sketch-wave-4-download.html | awk '{ if ($1 < 2) { print "FAIL: bg-surface-sunken <2 (disabled 1 + 토글 평시 2)"; exit 1 } else print "PASS bg-surface-sunken:", $1 }' \
  && grep -c 'cursor-not-allowed\|cursor:not-allowed' cha-bio-safety/docs/redesign-context/17-annual-plan/sketch-wave-4-download.html | awk '{ if ($1 < 1) { print "FAIL: cursor-not-allowed missing (disabled OQ #1 LOCKED)"; exit 1 } else print "PASS cursor-not-allowed:", $1 }' \
  && grep -cE 'polyline points="7 10 12 15 17 10"' cha-bio-safety/docs/redesign-context/17-annual-plan/sketch-wave-4-download.html | awk '{ if ($1 < 3) { print "FAIL: Lucide Download polyline <3 (OQ #4 LOCKED)"; exit 1 } else print "PASS Lucide Download:", $1 }' \
  && grep -c '엑셀 다운로드' cha-bio-safety/docs/redesign-context/17-annual-plan/sketch-wave-4-download.html | awk '{ if ($1 < 3) { print "FAIL: 엑셀 다운로드 카피 <3 (default 3 frame)"; exit 1 } else print "PASS 엑셀 다운로드:", $1 }' \
  && grep -c '생성 중' cha-bio-safety/docs/redesign-context/17-annual-plan/sketch-wave-4-download.html | awk '{ if ($1 < 1) { print "FAIL: 생성 중 카피 missing (frame 2 disabled)"; exit 1 } else print "PASS 생성 중:", $1 }' \
  && grep -c '대상 연도' cha-bio-safety/docs/redesign-context/17-annual-plan/sketch-wave-4-download.html | awk '{ if ($1 < 2) { print "FAIL: 대상 연도 (데스크톱 설명) <2 (frame 3/4)"; exit 1 } else print "PASS 대상 연도:", $1 }' \
  && grep -c '표지 및 일정표 연도가' cha-bio-safety/docs/redesign-context/17-annual-plan/sketch-wave-4-download.html | awk '{ if ($1 < 3) { print "FAIL: 표지 및 일정표 카피 <3 (모바일 설명 frame 1/2 + 데스크톱 설명 frame 3/4)"; exit 1 } else print "PASS 표지 및 일정표:", $1 }' \
  && grep -cE 'linear-gradient' cha-bio-safety/docs/redesign-context/17-annual-plan/sketch-wave-4-download.html | awk '{ if ($1 > 0) { print "FAIL: linear-gradient found (OQ #1 LOCKED 위반 — 그라데이션 완전 폐기)"; exit 1 } else print "PASS no gradient" }' \
  && grep -cE '(text|bg|border)-status-(safe|fire|warning|danger|caution)' cha-bio-safety/docs/redesign-context/17-annual-plan/sketch-wave-4-download.html | awk '{ if ($1 > 0) { print "FAIL: status- prefix found"; exit 1 } else print "PASS no status- prefix" }' \
  && grep -v '^<!--' cha-bio-safety/docs/redesign-context/17-annual-plan/sketch-wave-4-download.html | grep -v '^#' | grep -cE 'font-size:[[:space:]]*(9|10|11)[^0-9]|fontSize:[[:space:]]*(9|10|11)[^0-9]' | awk '{ if ($1 > 0) { print "FAIL: 9/10/11 px found"; exit 1 } else print "PASS no 9-11px" }' \
  && grep -cE '\bw-8\b|\bh-8\b' cha-bio-safety/docs/redesign-context/17-annual-plan/sketch-wave-4-download.html | awk '{ if ($1 > 0) { print "FAIL: w-8/h-8 found"; exit 1 } else print "PASS no w-8 h-8" }' \
  && grep -cE 'OQ #1|OQ #3|OQ #4' cha-bio-safety/docs/redesign-context/17-annual-plan/sketch-wave-4-download.html | awk '{ if ($1 < 3) { print "FAIL: OQ #1/#3/#4 citation <3"; exit 1 } else print "PASS OQ citations:", $1 }' \
  && git log --oneline -1 | grep -q '260522-0j3.*W4' && echo "PASS commit W4"
    </automated>
  </verify>
  <done>
    - sketch-wave-4-download.html 파일 존재 + ≥250 lines (≥400 권장)
    - data-theme="dark" ≥3 / data-theme="light" ≥1 (총 4 frame)
    - bg-safe-bar (다운로드 default) ≥3 (모바일 1 + 데스크톱 2) / text-text-on-accent ≥3 (OQ #1 LOCKED)
    - bg-surface-sunken ≥2 (disabled 1 + 토글 평시) / cursor-not-allowed (또는 cursor:not-allowed) ≥1 (disabled OQ #1 LOCKED)
    - Lucide Download polyline "7 10 12 15 17 10" ≥3 (OQ #4 LOCKED)
    - '엑셀 다운로드' ≥3 (default 3 frame) / '생성 중' ≥1 (disabled frame 2)
    - '대상 연도' ≥2 (데스크톱 설명 frame 3/4) / '표지 및 일정표 연도가' ≥3 (모바일 frame 1/2 + 데스크톱 1)
    - linear-gradient 0건 (OQ #1 LOCKED 그라데이션 완전 폐기) / status- prefix 0건 / 9·10·11px fontSize 0건 / w-8 h-8 0건
    - OQ #1 + OQ #3 + OQ #4 인용 ≥3건
    - atomic commit `docs(quick-260522-0j3): ... W4 download sketch ...` 1개
  </done>
</task>

<task type="auto">
  <name>Task 4 (W5): TSX 변환 verify checklist markdown 생성 (12 섹션) + AnnualPlanPage.tsx + generateAnnualPlan.ts 변경 0 최종 확인</name>
  <files>cha-bio-safety/docs/redesign-context/17-annual-plan/wave-5-tsx-conversion-checklist.md</files>
  <action>
    16-workshift/wave-5-tsx-conversion-checklist.md 패턴 mirror 로 새 markdown 파일 작성. **sketch 가 아닌 TSX 변환 wave 진입을 위한 verify checklist**. W2~W4 sketch 의 모든 Tailwind class / CSS 토큰 / OQ LOCKED 결정 / negative gate / positive gate / build gate / 자체 verify 명령을 **12 섹션**으로 박제. **★ 캘리브 좌표 시스템 시그니처 5건 1 byte 변경 금지 룰** 별도 박스 (§2 보존 + §10 비즈 보존 체크박스 + §11 메모리 룰).

    **frontmatter:**
    ```yaml
    ---
    title: "redesign/17-annual-plan — wave 5 (TSX conversion checklist)"
    status: pending  # TSX 변환 wave 시작 전 사용자 컨펌 후 in-progress
    created: 2026-05-22
    quick_id: 260522-0j3
    branch: redesign/17-annual-plan
    source_tsx: cha-bio-safety/src/pages/AnnualPlanPage.tsx
    source_tsx_lines: 225
    source_util: cha-bio-safety/src/utils/generateAnnualPlan.ts (generateAnnualPlan export, 변경 0)
    sketch_sources:
      - cha-bio-safety/docs/redesign-context/17-annual-plan/sketch-wave-2-chrome.html
      - cha-bio-safety/docs/redesign-context/17-annual-plan/sketch-wave-3-preview-calibration.html
      - cha-bio-safety/docs/redesign-context/17-annual-plan/sketch-wave-4-download.html
    mirror_of: cha-bio-safety/docs/redesign-context/16-workshift/wave-5-tsx-conversion-checklist.md (260521-t12)
    calibration_precedent: cha-bio-safety/docs/redesign-context/15-daily-report/wave-1-index.md (260521-* SW3) — 캘리브 좌표 시스템 보존 룰 동일 적용
    ---
    ```

    **§1. 변환 범위 + 산출 파일**
    - 단일 파일 in-place 수정: `cha-bio-safety/src/pages/AnnualPlanPage.tsx` (225 lines → 약 230~270 lines 예상, Tailwind v0.1.1 className 으로 인라인 style 치환 + Lucide ChevronLeft + Download import 2줄 추가)
    - W2 (chrome — 모바일 헤더 + 데스크톱 상단 바) + W3 (preview + 캘리브 좌표 시스템) + W4 (다운로드 버튼 + 설명) 3 sketch 결정 → className 1:1 매핑
    - **components.css 신규 생성 X** (페이지 로컬 인라인 토큰 유지)
    - **generateAnnualPlan.ts 변경 0** (xlsx-js-style 엑셀 생성 + 표지/일정표 연도 셀 패치 비즈 로직 보존)
    - **★ 캘리브 좌표 시스템 시그니처 5건 1 byte 변경 금지** (STORAGE_KEY / FINGER_OFFSET / handleImageClick / handleImageTouch / yearPos overlay — 15-daily-report SW3 precedent)
    - 비즈 로직 0 diff (state/handler/effect/hook 모두 보존, §10 체크박스)

    **§2. 보존 (AnnualPlanPage.tsx 비즈 로직 100% 보존 목록 15+ row + ★ 캘리브 좌표 시스템 시그니처 5건 박스)**

    **★ 캘리브 좌표 시스템 시그니처 5건 (1 byte 변경 금지 박스, 15-daily-report SW3 precedent):**

    ```
    1. STORAGE_KEY = 'annual_plan_year_pos'              (line 7, localStorage key — 변경 시 기존 사용자 좌표 손실)
    2. FINGER_OFFSET = 60                                (line 8, TouchEvent y 보정 px — 손가락 가리는 영역 보정값)
    3. handleImageClick (line 35~45):
       - e.currentTarget.getBoundingClientRect()
       - ((e.clientX - rect.left) / rect.width) * 100
       - ((e.clientY - rect.top) / rect.height) * 100
       - setYearPos / localStorage.setItem / setCalibMode(false)
    4. handleImageTouch (line 47~59):
       - touch.clientY - FINGER_OFFSET 보정
       - 동일 % 변환 흐름
    5. yearPos overlay inline style (line 78~89):
       - top:`${yearPos.y}%` left:`${yearPos.x}%`
       - transform:'translate(-50%,-50%)'
       - fontSize:'min(1.4vw, 16px)'
       - fontFamily:'Malgun Gothic, 맑은 고딕, sans-serif'
       - fontWeight:700 color:'#000' pointerEvents:'none'
    ```

    **AnnualPlanPage.tsx 비즈 로직 보존 표 (line 별):**

    | line | 항목 | 보존 방식 |
    |---|---|---|
    | 1~5 | import 5개 (useState/useRef / useNavigate / toast / generateAnnualPlan / useIsDesktop) | 그대로 + lucide-react `ChevronLeft, Download` 1줄 추가 |
    | 7~8 | `STORAGE_KEY` + `FINGER_OFFSET` 상수 | **1 byte 변경 금지** (★ 캘리브 시그니처) |
    | 10~12 | `loadPos()` helper (try/catch JSON.parse fallback null) | 그대로 |
    | 14~16 | useNavigate / useIsDesktop / useState (loading) | 그대로 |
    | 17~19 | useState 3종 (loading / calibMode / yearPos initial=loadPos) | 그대로 |
    | 20~21 | useRef (imgRef) + `nextYear = new Date().getFullYear() + 1` | 그대로 |
    | 23~33 | `handleDownload` async + `generateAnnualPlan()` direct import (dynamic import 아님) + toast.success/error | 그대로 |
    | 35~45 | `handleImageClick` 좌표 계산식 | **1 byte 변경 금지** (★ 캘리브 시그니처) |
    | 47~59 | `handleImageTouch` FINGER_OFFSET 보정 식 | **1 byte 변경 금지** (★ 캘리브 시그니처) |
    | 61~103 | previewImage element 공통 (img + 연도 오버레이 + 캘리브 안내 칩) | UI markup 만 재작성, 좌표/이벤트/카피 verbatim 보존 |
    | 65 | preview 자산 `'/templates/preview/annual-plan.png'` | **경로 변경 금지** (404 사고 회피) |
    | 66 | img alt `'연간 업무 추진 계획 미리보기'` | 카피 verbatim |
    | 67~68 | onClick / onTouchStart handler 바인딩 | 그대로 |
    | 78~89 | yearPos overlay inline style | **1 byte 변경 금지** (★ 캘리브 시그니처) |
    | 91~101 | 캘리브 안내 칩 inline style + 카피 '연도가 들어갈 위치를 클릭하세요' | 인라인 + 카피 verbatim 보존 (OQ #5 LOCKED) |
    | 106 | useIsDesktop 분기 | 그대로 |
    | 119, 181 | `setCalibMode(m => !m)` 토글 | 그대로 |
    | 128, 190 | 카피 분기 '취소' / '위치 조정' | 그대로 |
    | 131, 206 | `handleDownload` onClick | 그대로 |
    | 132, 207 | `disabled={loading}` | 그대로 |
    | 144, 219 | 카피 분기 '엑셀 다운로드' / '생성 중...' | 그대로 |
    | 170 | `navigate(-1)` onClick (모바일 back button) | 그대로 |
    | 179 | 모바일 헤더 타이틀 '연간 업무 추진 계획' | 카피 verbatim |

    **§3. 변환 매핑 (영역별 className/토큰/폰트 — W2/W3/W4 sketch verbatim 인용)**

    **§3.1 영역 5 데스크톱 — 상단 바 (line 110~146, W2 sketch 출처 + W4 다운로드 버튼)**

    | 현재 (인라인 style) | 변환 후 (className + 인라인) | sketch 출처 |
    |---|---|---|
    | 외곽 wrapper `width:100%, height:100%, flexDirection:column, overflow:hidden` (line 108) | 인라인 유지 (style 그대로) | W2 |
    | 상단 바 wrapper `flexShrink:0, padding:'14px 28px', display:flex, alignItems:center, gap:12, borderBottom:'1px solid var(--bd)'` (line 110~114) | `class="border-b border-border-default flex items-center"` style={{ flexShrink:0, padding:'14px 28px', gap:12 }} | W2 frame 3/4 |
    | 설명 div `flex:1, fontSize:12, color:'var(--t3)'` + `<strong color:'var(--t1)' fontWeight:700>` (line 115~117) | `class="text-caption leading-none text-text-tertiary"` style={{ flex:1 }} + `<strong class="text-text-primary font-bold">` (OQ #3 LOCKED 12 유지 + leading-none) | W2 frame 3/4 |
    | 위치조정 토글 평시 `padding:'8px 14px', borderRadius:8, border:'1px solid var(--bd2)', background:'var(--bg3)', color:'var(--t2)', fontSize:12, fontWeight:700` (line 118~125, 평시) | `class="bg-surface-sunken border border-border-strong text-text-secondary text-caption font-bold leading-none rounded-sm"` style={{ padding:'8px 14px', cursor:'pointer' }} (OQ #2 LOCKED 평시 + OQ #3 LOCKED 12 + leading-none) | W2 frame 3/4 |
    | 위치조정 토글 active `border:'1px solid var(--acl)', background:'rgba(59,130,246,0.1)', color:'var(--acl)'` (line 122~124, calibMode true) | `class="border-accent bg-accent/10 text-accent border text-caption font-bold leading-none rounded-sm"` (OQ #2 LOCKED active — status- prefix 없음) | W2 frame 3/4 active row |
    | 다운로드 버튼 `padding:'8px 20px', borderRadius:8, border:'none'` (line 134) | `class="bg-safe-bar text-text-on-accent text-label font-bold leading-none rounded-sm flex items-center"` style={{ padding:'8px 20px', gap:8, border:'none', flexShrink:0 }} (OQ #1 LOCKED solid + OQ #3 LOCKED 13 text-label + OQ #4 LOCKED Lucide) | W4 frame 3/4 |
    | 다운로드 background 분기 `loading ? 'var(--bg3)' : 'linear-gradient(135deg,#1e40af,#3b82f6)'` (line 135) | `className={\`... ${loading ? 'bg-surface-sunken text-text-tertiary cursor-not-allowed' : 'bg-safe-bar text-text-on-accent cursor-pointer'}\`}` (OQ #1 LOCKED — 그라데이션 완전 폐기) | W4 frame 1/3 disabled |
    | 다운로드 svg `width:15 height:15 viewBox path "M12 5v14m0 0l-4-4m4 4l4-4M4 19h16"` (line 141~143) | `<Download size={15} />` (OQ #4 LOCKED Lucide 교체, lucide-react import 추가) | W4 frame 3/4 |
    | 하단 미리보기 wrapper `flex:1, minHeight:0, overflow:hidden, padding:24, background:'var(--bg)'` (line 149~152) | 인라인 유지 (background `var(--bg)` → `var(--surface-page)` 마이그레이션) | W3 frame 3/4 |
    | preview wrapper `maxWidth:'calc((100vh - 140px) * 1.414)'` (line 156) | 인라인 유지 (A4 비율 시각 anchor) | W3 frame 3/4 |

    **§3.2 영역 4 모바일 — 헤더 (line 169~192, W2 sketch 출처)**

    | 현재 | 변환 후 | sketch |
    |---|---|---|
    | 외곽 wrapper `width:100%, height:100%, flexDirection:column, overflow:hidden, background:'var(--bg)'` (line 168) | 인라인 유지 (`var(--bg)` → `var(--surface-page)` 마이그레이션 메모) | W2 frame 1/2 |
    | header `flexShrink:0, background:'var(--bg2)', borderBottom:'1px solid var(--bd)', padding:'8px 12px 9px', gap:8` (line 169) | `class="bg-surface-raised border-b border-border-default flex items-center"` style={{ padding:'8px 12px 9px', gap:8, flexShrink:0 }} | W2 frame 1/2 |
    | back button `width:34, height:34, borderRadius:8, background:'var(--bg3)', border:'1px solid var(--bd)'` (line 170~173) | `class="w-[34px] h-[34px] rounded-sm bg-surface-sunken border border-border-default flex items-center justify-center"` style={{ flexShrink:0, cursor:'pointer' }} (memory `feedback_tailwind_w8_h8_is_48px` — w-[34px] 인라인 안전) | W2 frame 1/2 |
    | back button svg `width:15 height:15 stroke:'var(--t2)' path "M15 19l-7-7 7-7"` (line 175~177) | `<ChevronLeft size={15} className="text-text-secondary" />` (OQ #4 LOCKED Lucide 교체) | W2 frame 1/2 |
    | 타이틀 span `flex:1, fontSize:14, fontWeight:700, color:'var(--t1)'` (line 179) | `class="text-body font-bold text-text-primary"` style={{ flex:1 }} (OQ #3 LOCKED 14→16 격상) | W2 frame 1/2 |
    | 위치조정 토글 평시 `padding:'6px 10px', borderRadius:8, border:'1px solid var(--bd2)', background:'var(--bg3)', color:'var(--t2)', fontSize:11, fontWeight:700` (line 180~187) | `class="bg-surface-sunken border border-border-strong text-text-secondary text-caption font-bold leading-none rounded-sm"` style={{ padding:'6px 10px', cursor:'pointer' }} (OQ #2 LOCKED 평시 + OQ #3 LOCKED 11→12 격상 + leading-none) | W2 frame 1/2 |
    | 위치조정 토글 active `border:'1px solid var(--acl)', background:'rgba(59,130,246,0.1)', color:'var(--acl)'` (line 184~186, calibMode true) | `class="border-accent bg-accent/10 text-accent border text-caption font-bold leading-none rounded-sm"` (OQ #2 LOCKED active) | W2 frame 1/2 active row |

    **§3.3 영역 2 — preview + 캘리브 좌표 시스템 (line 61~103, W3 sketch 출처)**

    | 현재 | 변환 후 | sketch |
    |---|---|---|
    | 외곽 div `position:'relative', width:'100%', height:'100%'` (line 62) | 인라인 유지 (style 그대로) | W3 all frames |
    | preview img `width:'100%', height:'100%', objectFit:'contain', borderRadius:8, background:'#fff'` (line 70~73) | `class="rounded-sm"` style={{ width:'100%', height:'100%', objectFit:'contain', background:'#fff' }} | W3 all frames |
    | preview img border 분기 `calibMode ? '2px solid var(--acl)' : '1px solid var(--bd)'` (line 72) | `className={\`... ${calibMode ? 'border-2 border-accent' : 'border border-border-default'}\`}` (OQ #5 LOCKED) | W3 frame 2/4 vs 1/3 |
    | preview img cursor 분기 `calibMode ? 'crosshair' : 'default'` (line 74) | 인라인 유지 (분기 가독성) | W3 frame 2/4 |
    | yearPos overlay `position:'absolute', top:${y}%, left:${x}%, transform:'translate(-50%,-50%)', fontSize:'min(1.4vw, 16px)', fontWeight:700, color:'#000', fontFamily:'Malgun Gothic, 맑은 고딕, sans-serif', pointerEvents:'none'` (line 79~88) | **인라인 100% 유지** (★ 캘리브 좌표 시스템 시그니처 — 1 byte 변경 금지) | W3 frame 3/4 |
    | 캘리브 안내 칩 `background:'rgba(59,130,246,0.9)', color:'#fff', padding:'6px 16px', borderRadius:8, fontSize:12, fontWeight:700, whiteSpace:'nowrap', pointerEvents:'none', top:8, left:'50%', transform:'translateX(-50%)'` (line 92~98) | `class="text-caption font-bold leading-none text-white rounded-sm"` style={{ position:'absolute', top:8, left:'50%', transform:'translateX(-50%)', background:'rgba(59,130,246,0.9)', padding:'6px 16px', whiteSpace:'nowrap', pointerEvents:'none' }} (OQ #5 LOCKED 인라인 rgba 유지 + OQ #3 LOCKED 12 text-caption + leading-none) | W3 frame 2/4 |
    | 카피 '연도가 들어갈 위치를 클릭하세요' (line 99) | verbatim 유지 | W3 |

    **§3.4 영역 5 모바일 — 다운로드 묶음 (line 194~221, W4 sketch 출처)**

    | 현재 | 변환 후 | sketch |
    |---|---|---|
    | 본문 wrapper `flex:1, overflow:'auto', padding:16, flexDirection:column, gap:16` (line 194) | 인라인 유지 | W3 + W4 frame 1/2 |
    | 모바일 미리보기 wrapper `width:'100%'` (line 196) | 인라인 유지 | W3 frame 1/2 |
    | 설명 + 다운로드 wrapper `textAlign:'center'` (line 201) | 인라인 유지 | W4 frame 1/2 |
    | 모바일 설명 `fontSize:13, color:'var(--t3)', marginBottom:12` (line 202) | `class="text-label leading-relaxed text-text-tertiary"` style={{ marginBottom:12 }} (OQ #3 LOCKED 13 text-label) | W4 frame 1/2 |
    | 모바일 다운로드 버튼 `width:'100%', padding:14, borderRadius:10, border:'none', fontSize:14, fontWeight:700, display:'flex', alignItems:'center', justifyContent:'center', gap:8` (line 205~214) | `class="bg-safe-bar text-text-on-accent text-body font-bold rounded-md flex items-center justify-center"` style={{ width:'100%', padding:14, gap:8, border:'none', cursor:'pointer' }} (OQ #1 LOCKED solid + OQ #3 LOCKED 14→16 text-body font-bold 격상 + radius 10→12 `rounded-md` 마이그레이션) | W4 frame 1 |
    | 모바일 background 분기 `loading ? 'var(--bg3)' : 'linear-gradient(135deg,#1e40af,#3b82f6)'` (line 210) | `className={\`... ${loading ? 'bg-surface-sunken text-text-tertiary cursor-not-allowed' : 'bg-safe-bar text-text-on-accent cursor-pointer'}\`}` (OQ #1 LOCKED) | W4 frame 1 vs 2 |
    | 모바일 svg `width:16 height:16 viewBox path "M12 5v14m0 0l-4-4m4 4l4-4M4 19h16"` (line 216~218) | `<Download size={16} />` (OQ #4 LOCKED Lucide 교체) | W4 frame 1/2 |
    | 카피 분기 `loading ? '생성 중...' : '엑셀 다운로드'` (line 219) | 그대로 | W4 frame 1/2 |

    **§4. OQ LOCKED 5건 변환 결과 반영 (W1 §7 verbatim + 코드 적용 line 범위)**

    ```
    OQ #1 (다운로드 버튼 bg-safe-bar solid + 그라데이션 완전 폐기):
      - line 135 (데스크톱): background 분기 → className "bg-safe-bar text-text-on-accent cursor-pointer" / "bg-surface-sunken text-text-tertiary cursor-not-allowed"
      - line 210 (모바일): 동일 분기
      - linear-gradient(135deg,#1e40af,#3b82f6) 완전 폐기 (grep gate 0건 강제)
      - 영향 line: 130~145 (데스크톱) + 200~221 (모바일)

    OQ #2 (위치조정 토글 토큰화):
      - line 122~124 (데스크톱) + line 184~186 (모바일): active 분기 → className "border-accent bg-accent/10 text-accent border"
      - line 122~124 (데스크톱) + line 184~186 (모바일): 평시 → className "bg-surface-sunken border border-border-strong text-text-secondary"
      - 영향 line: 118~129 (데스크톱) + 180~191 (모바일)

    OQ #3 (폰트 격상 verbatim):
      - line 179 모바일 헤더 타이틀: fontSize:14 → "text-body font-bold" (14→16 격상)
      - line 187 모바일 위치조정 토글: fontSize:11 → "text-caption font-bold leading-none" (11→12 격상)
      - line 125 데스크톱 위치조정 토글: fontSize:12 → "text-caption font-bold leading-none" (12 유지)
      - line 115 데스크톱 설명: fontSize:12 → "text-caption leading-none" (12 유지)
      - line 96 캘리브 안내 칩: fontSize:12 → "text-caption font-bold leading-none" (12 유지)
      - line 202 모바일 설명: fontSize:13 → "text-label leading-relaxed" (13 유지)
      - line 137 데스크톱 다운로드: fontSize:13 → "text-label font-bold leading-none" (13 유지)
      - line 212 모바일 다운로드: fontSize:14 → "text-body font-bold" (14→16 격상)
      - line 83 연도 오버레이: fontSize:'min(1.4vw, 16px)' → 인라인 유지 (★ 캘리브 좌표 시스템 일부, 1 byte 변경 금지)

    OQ #4 (Lucide 아이콘 교체):
      - line 1 또는 6 import: `import { ChevronLeft, Download } from 'lucide-react'` 추가
      - line 175~177 모바일 back button svg → <ChevronLeft size={15} className="text-text-secondary" />
      - line 141~143 데스크톱 다운로드 svg → <Download size={15} />
      - line 216~218 모바일 다운로드 svg → <Download size={16} />
      - svg path 인라인 폐기 (grep gate "M12 5v14" / "M15 19l-7-7" 0건 강제)

    OQ #5 (preview border 토큰 + 캘리브 안내 칩 인라인 유지):
      - line 72 preview border: calibMode 분기 → className 분기 "border-2 border-accent" / "border border-border-default"
      - line 92~98 캘리브 안내 칩: background 'rgba(59,130,246,0.9)' 인라인 100% 유지 (alpha 정밀도 토큰 비용 회피)
      - line 94 color:'#fff' → className "text-white" + 인라인 fontWeight 700 / whiteSpace nowrap / pointerEvents none 모두 보존
    ```

    **§5. negative gate (TSX 변환 후 AnnualPlanPage.tsx 가 통과해야 할 grep gate)**

    ```bash
    # 1. 이모지 0건 (sketch 본문 + TSX 변환 결과 모두)
    grep -v '^#' cha-bio-safety/src/pages/AnnualPlanPage.tsx | grep -cE '[😀-🛿🤀-🧿🩰-🩴✅✊👍👎❌⛔🔥💥💧⚠️📛]' == 0

    # 2. linear-gradient 0건 (OQ #1 LOCKED 그라데이션 완전 폐기 증거)
    grep -c 'linear-gradient' cha-bio-safety/src/pages/AnnualPlanPage.tsx == 0

    # 3. 9·10·11px 0건 (OQ #3 LOCKED 격상 결과, fontSize/font-size 양쪽 — 단 'min(1.4vw, 16px)' 의 16 은 11px 아님)
    grep -v '^\s*//' cha-bio-safety/src/pages/AnnualPlanPage.tsx | grep -cE "fontSize:\s*(9|10|11)[^0-9px]|font-size:\s*(9|10|11)[^0-9px]" == 0

    # 4. status- prefix 0건 (memory feedback_tailwind_token_class_pattern)
    grep -cE '(text|bg|border)-status-(safe|fire|warning|danger|caution)' cha-bio-safety/src/pages/AnnualPlanPage.tsx == 0

    # 5. w-8 / h-8 0건 (w-8=48 함정, memory feedback_tailwind_w8_h8_is_48px)
    grep -cE '\bw-8\b|\bh-8\b' cha-bio-safety/src/pages/AnnualPlanPage.tsx == 0

    # 6. 옛 alias 토큰 0건 (var(--bg)/var(--bg2)/var(--bg3)/var(--bd)/var(--bd2)/var(--t1)/var(--t2)/var(--t3)/var(--acl)/var(--accent))
    grep -cE 'var\(--(bg|bg2|bg3|bd|bd2|t1|t2|t3|acl|accent)\)' cha-bio-safety/src/pages/AnnualPlanPage.tsx == 0
    # (단, rgba(59,130,246,0.9) + rgba(59,130,246,0.1) 직접 rgba 는 OQ #5/#2 LOCKED 예외)

    # 7. 옛 svg path 0건 (OQ #4 LOCKED Lucide 교체 — svg path 인라인 폐기 증거)
    grep -c 'M12 5v14m0 0l-4-4m4 4l4-4M4 19h16' cha-bio-safety/src/pages/AnnualPlanPage.tsx == 0
    grep -c 'M15 19l-7-7 7-7' cha-bio-safety/src/pages/AnnualPlanPage.tsx == 0

    # 8. 비즈 로직 diff 0 (handler/state/effect/hook 보존)
    git diff HEAD~N cha-bio-safety/src/pages/AnnualPlanPage.tsx | grep -E '^[+-]\s*(useState|useEffect|useRef|useNavigate|useIsDesktop|generateAnnualPlan|setLoading|setCalibMode|setYearPos|handleDownload|handleImageClick|handleImageTouch|loadPos|STORAGE_KEY|FINGER_OFFSET|imgRef|nextYear)' == 0
    # (logic line 의 - 추가 / + 제거 또는 변경이 없어야 함)

    # 9. ★ 캘리브 좌표 시스템 시그니처 5건 보존 (15-daily-report SW3 precedent — 1 byte 변경 금지)
    grep -c "STORAGE_KEY = 'annual_plan_year_pos'" cha-bio-safety/src/pages/AnnualPlanPage.tsx >= 1
    grep -c "FINGER_OFFSET = 60" cha-bio-safety/src/pages/AnnualPlanPage.tsx >= 1
    grep -c "((e.clientX - rect.left) / rect.width) \* 100" cha-bio-safety/src/pages/AnnualPlanPage.tsx >= 1
    grep -c "FINGER_OFFSET" cha-bio-safety/src/pages/AnnualPlanPage.tsx >= 2  # 정의 + 사용
    grep -c "'min(1.4vw, 16px)'" cha-bio-safety/src/pages/AnnualPlanPage.tsx >= 1
    grep -c "'Malgun Gothic, 맑은 고딕, sans-serif'" cha-bio-safety/src/pages/AnnualPlanPage.tsx >= 1
    grep -c "/templates/preview/annual-plan.png" cha-bio-safety/src/pages/AnnualPlanPage.tsx >= 1
    ```

    **§6. positive gate (변환 후 등장해야 할 패턴)**

    ```bash
    # 1. lucide-react import — ChevronLeft + Download 2개 추가 (OQ #4 LOCKED)
    grep -c "from 'lucide-react'" cha-bio-safety/src/pages/AnnualPlanPage.tsx >= 1
    grep -c 'ChevronLeft' cha-bio-safety/src/pages/AnnualPlanPage.tsx >= 2  # import + 사용
    grep -c 'Download' cha-bio-safety/src/pages/AnnualPlanPage.tsx >= 3  # import + 데스크톱 + 모바일

    # 2. v0.1.1 토큰 class 카운트 (≥3 핵심)
    grep -c 'bg-surface-raised' cha-bio-safety/src/pages/AnnualPlanPage.tsx >= 1  # 모바일 헤더
    grep -c 'bg-surface-sunken' cha-bio-safety/src/pages/AnnualPlanPage.tsx >= 3  # back button + 토글 평시 (모바일+데스크톱) + 다운로드 disabled
    grep -c 'border-border-default' cha-bio-safety/src/pages/AnnualPlanPage.tsx >= 3  # 모바일 헤더 + back button + 데스크톱 상단 바 + preview 평시
    grep -c 'border-border-strong' cha-bio-safety/src/pages/AnnualPlanPage.tsx >= 2  # 위치조정 토글 평시 (모바일+데스크톱)
    grep -c 'text-text-primary' cha-bio-safety/src/pages/AnnualPlanPage.tsx >= 2  # 모바일 헤더 타이틀 + 데스크톱 설명 strong
    grep -c 'text-text-secondary' cha-bio-safety/src/pages/AnnualPlanPage.tsx >= 2  # 토글 평시 + ChevronLeft
    grep -c 'text-text-tertiary' cha-bio-safety/src/pages/AnnualPlanPage.tsx >= 3  # 데스크톱 설명 + 모바일 설명 + 다운로드 disabled
    grep -c 'bg-safe-bar' cha-bio-safety/src/pages/AnnualPlanPage.tsx >= 2  # 다운로드 default (모바일+데스크톱)
    grep -c 'text-text-on-accent' cha-bio-safety/src/pages/AnnualPlanPage.tsx >= 2  # 다운로드 default
    grep -c 'border-accent' cha-bio-safety/src/pages/AnnualPlanPage.tsx >= 2  # 위치조정 토글 active + preview 캘리브 모드
    grep -c 'bg-accent/10' cha-bio-safety/src/pages/AnnualPlanPage.tsx >= 1  # 위치조정 토글 active
    grep -c 'text-accent' cha-bio-safety/src/pages/AnnualPlanPage.tsx >= 1  # 위치조정 토글 active

    # 3. 폰트 토큰 (OQ #3 LOCKED 격상)
    grep -c 'text-caption' cha-bio-safety/src/pages/AnnualPlanPage.tsx >= 4  # 데스크톱 설명 + 데스크톱 토글 + 모바일 토글 + 캘리브 안내 칩
    grep -c 'text-label' cha-bio-safety/src/pages/AnnualPlanPage.tsx >= 2  # 모바일 설명 + 데스크톱 다운로드
    grep -c 'text-body' cha-bio-safety/src/pages/AnnualPlanPage.tsx >= 2  # 모바일 헤더 타이틀 + 모바일 다운로드
    grep -c 'leading-none' cha-bio-safety/src/pages/AnnualPlanPage.tsx >= 3  # 작은 컨테이너 caption/label

    # 4. 인라인 px 사이즈 (OQ #4 + memory feedback_tailwind_w8_h8_is_48px)
    grep -c 'w-\[34px\]' cha-bio-safety/src/pages/AnnualPlanPage.tsx >= 1  # 모바일 back button
    grep -c 'h-\[34px\]' cha-bio-safety/src/pages/AnnualPlanPage.tsx >= 1
    grep -c 'rounded-sm' cha-bio-safety/src/pages/AnnualPlanPage.tsx >= 4  # back button + 토글 + 데스크톱 다운로드 + preview
    grep -c 'rounded-md' cha-bio-safety/src/pages/AnnualPlanPage.tsx >= 1  # 모바일 다운로드 (radius 10→12)

    # 5. 카피 verbatim (memory feedback_sketch_realistic_data)
    grep -c '연간 업무 추진 계획' cha-bio-safety/src/pages/AnnualPlanPage.tsx >= 1
    grep -c '연간 업무 추진 계획 미리보기' cha-bio-safety/src/pages/AnnualPlanPage.tsx >= 1  # img alt
    grep -c '연도가 들어갈 위치를 클릭하세요' cha-bio-safety/src/pages/AnnualPlanPage.tsx >= 1
    grep -c '위치 조정' cha-bio-safety/src/pages/AnnualPlanPage.tsx >= 1
    grep -c '취소' cha-bio-safety/src/pages/AnnualPlanPage.tsx >= 1
    grep -c '엑셀 다운로드' cha-bio-safety/src/pages/AnnualPlanPage.tsx >= 2  # 모바일 + 데스크톱
    grep -c '생성 중' cha-bio-safety/src/pages/AnnualPlanPage.tsx >= 2  # 모바일 + 데스크톱
    grep -c '엑셀이 다운로드됐습니다' cha-bio-safety/src/pages/AnnualPlanPage.tsx >= 1  # toast.success
    grep -c '연도 위치 저장됨' cha-bio-safety/src/pages/AnnualPlanPage.tsx >= 1  # toast
    grep -c '대상 연도' cha-bio-safety/src/pages/AnnualPlanPage.tsx >= 1  # 데스크톱 설명
    grep -c '표지 및 일정표 연도가' cha-bio-safety/src/pages/AnnualPlanPage.tsx >= 2  # 모바일 + 데스크톱
    ```

    **§7. build / tsc gate**

    ```bash
    cd cha-bio-safety && npx tsc --noEmit                # 0 error
    cd cha-bio-safety && npm run build                   # exit 0
    # AnnualPlanPage chunk size 보고 (Vite output 의 dist/assets/AnnualPlanPage-*.js)
    ls -la cha-bio-safety/dist/assets/AnnualPlanPage-*.js  # size 확인
    ```

    **§8. 자체 verify 명령 (TSX 변환 wave 진입 시점 + 완료 시점 양쪽 실행)**

    각 gate 의 grep/wc/git 명령을 fence 안에 verbatim 박제.

    ```bash
    # 진입 시점: sketch 3 파일 모두 존재 확인
    ls cha-bio-safety/docs/redesign-context/17-annual-plan/sketch-wave-{2-chrome,3-preview-calibration,4-download}.html

    # 완료 시점: §5 negative + §6 positive + §7 build 모두 PASS

    # generateAnnualPlan.ts 변경 0 확인 (TSX 변환 wave 끝나도 유지)
    git diff HEAD -- cha-bio-safety/src/utils/generateAnnualPlan.ts | wc -l == 0
    ```

    **§9. Tailwind cheatsheet (v0.1.1 토큰 → utility class 매핑 표)**

    | v0.1.1 토큰 | Tailwind utility | 17-annual-plan 적용 위치 |
    |---|---|---|
    | `--surface-page` | `bg-surface-page` | 모바일 외곽 + 데스크톱 미리보기 wrapper |
    | `--surface-raised` | `bg-surface-raised` | 모바일 헤더 |
    | `--surface-sunken` | `bg-surface-sunken` | back button + 위치조정 토글 평시 (모바일+데스크톱) + 다운로드 disabled |
    | `--border-default` | `border-border-default` | 모바일 헤더 border-b + back button + 데스크톱 상단 바 border-b + preview 평시 border |
    | `--border-strong` | `border-border-strong` | 위치조정 토글 평시 border (모바일+데스크톱) |
    | `--text-primary` | `text-text-primary` | 모바일 헤더 타이틀 + 데스크톱 설명 `<strong>` |
    | `--text-secondary` | `text-text-secondary` | 위치조정 토글 평시 + ChevronLeft |
    | `--text-tertiary` | `text-text-tertiary` | 데스크톱 설명 + 모바일 설명 + 다운로드 disabled |
    | `--text-on-accent` | `text-text-on-accent` | 다운로드 default (CTA 위 흰 글자) |
    | `--safe-bar` | `bg-safe-bar` | 다운로드 default (OQ #1) |
    | `--accent` | `border-accent` + `bg-accent/10` + `text-accent` | 위치조정 토글 active + preview 캘리브 모드 (OQ #2, #5) |
    | (radius 8) | `rounded-sm` | back button + 위치조정 토글 + 데스크톱 다운로드 + preview |
    | (radius 12) | `rounded-md` | 모바일 다운로드 (radius 10→12 마이그레이션) |
    | (W-34, 토큰 없음) | `w-[34px] h-[34px]` | 모바일 back button (w-8=48 함정 회피) |
    | (rgba 인라인 OQ #5) | `style={{ background:'rgba(59,130,246,0.9)' }}` | 캘리브 안내 칩 (alpha 정밀도 토큰 비용 회피) |
    | (font min(1.4vw, 16px) OQ #3) | `style={{ fontSize:'min(1.4vw, 16px)' }}` | 연도 오버레이 (★ 캘리브 좌표 시스템 일부) |
    | (font Malgun Gothic) | `style={{ fontFamily:'Malgun Gothic, ...' }}` | 연도 오버레이 (엑셀 표지 시각 일치, memory `feedback_pdflib_subset_false`) |

    **§10. 비즈 보존 체크박스 (TSX 변환 wave 완료 후 직접 체크)**

    - [ ] `useNavigate()` line 15 그대로
    - [ ] `useIsDesktop()` line 16 그대로
    - [ ] `useState` 3종 (loading / calibMode / yearPos) line 17~19 그대로
    - [ ] `useRef` (imgRef) line 20 그대로
    - [ ] `nextYear = new Date().getFullYear() + 1` line 21 그대로
    - [ ] `STORAGE_KEY = 'annual_plan_year_pos'` line 7 **1 byte 변경 금지** (★ 캘리브 시그니처)
    - [ ] `FINGER_OFFSET = 60` line 8 **1 byte 변경 금지** (★ 캘리브 시그니처)
    - [ ] `loadPos()` helper line 10~12 그대로
    - [ ] `handleDownload` async + `generateAnnualPlan()` direct import line 23~33 그대로
    - [ ] `handleImageClick` 좌표 계산식 line 35~45 **1 byte 변경 금지** (★ 캘리브 시그니처)
    - [ ] `handleImageTouch` FINGER_OFFSET 보정 식 line 47~59 **1 byte 변경 금지** (★ 캘리브 시그니처)
    - [ ] yearPos overlay inline style (fontSize/fontFamily) line 78~89 **1 byte 변경 금지** (★ 캘리브 시그니처)
    - [ ] 캘리브 안내 칩 inline rgba + 카피 line 91~101 verbatim 보존 (OQ #5 LOCKED)
    - [ ] preview 자산 `/templates/preview/annual-plan.png` line 65 경로 변경 금지
    - [ ] img alt '연간 업무 추진 계획 미리보기' line 66 카피 verbatim
    - [ ] `setCalibMode(m => !m)` 토글 line 119, 181 그대로
    - [ ] `handleDownload` onClick + `disabled={loading}` line 131~132, 206~207 그대로
    - [ ] `navigate(-1)` 모바일 back button line 170 그대로
    - [ ] `generateAnnualPlan.ts` export 0 byte 변경

    **§11. 메모리 룰 inline 인용 (W1 §5 mirror, 13+ 룰 — AnnualPlanPage 특화 룰 2건 포함)**

    각 룰 작용 케이스 한 줄 — `feedback_*.md` 파일명 + How (17-annual-plan 컨텍스트).

    1. `feedback_design_sketch_first` — spacing/sizing 변경 시에도 sketch 먼저 컨펌. T1~T3 에서 다운로드 버튼 크기 / 캘리브 안내 칩 크기 / 위치조정 토글 padding 조정 시 sketch 먼저.
    2. `feedback_redesign_sketch_rule_enforcement` — 위치조정 토글 active 색 = accent (모드 분기 강조) — `bg-status-safe-bg` 같은 위험 색 사용 금지. 다운로드 = CTA → `bg-safe-bar` solid (의미: "이 작업 실행" 정상 CTA, OQ #1 LOCKED).
    3. `feedback_sketch_realistic_data` — 카피 verbatim (연간 업무 추진 계획 / 위치 조정 / 취소 / 연도가 들어갈 위치를 클릭하세요 / 엑셀 다운로드 / 생성 중... / 대상 연도 / 표지 및 일정표 연도가 / 엑셀이 다운로드됐습니다 / 연도 위치 저장됨 / 연간 업무 추진 계획 미리보기). 시안에서 변경 금지.
    4. `feedback_planner_prompt_sketch_verbatim` — TSX 변환 진입 시 W2~W4 sketch 의 모든 Tailwind class / CSS 토큰 grep 으로 추출 → 본 §3 verbatim 인용. 특히 캘리브 안내 칩 `background:rgba(59,130,246,0.9)` / 연도 오버레이 `fontSize:'min(1.4vw, 16px)'` / A4 비율 `maxWidth:'calc((100vh - 140px) * 1.414)'` / 다운로드 그라데이션 폐기 모두 추측 X — sketch verbatim.
    5. `feedback_tailwind_token_class_pattern` — 다운로드 그라데이션 → `bg-safe-bar` solid 치환 (OQ #1). `bg-status-safe-bar` (status- prefix) 사용 시 W5 verify FAIL. 위치조정 토글 active → `border-accent bg-accent/10 text-accent` (`status-accent` X). preview border calibMode → `border-2 border-accent`. Lucide `ChevronLeft size={15}` + `Download size={15/16}` prop 사용 — className 으로 `w-4 h-4` 금지.
    6. `feedback_tailwind_w8_h8_is_48px` — 모바일 back button 34×34 (line 171) → `w-8 h-8` 사용 시 48×48 (1.4배 확대 사고) — `w-[34px] h-[34px]` 인라인 필수. 다운로드 버튼 padding 14 (line 209) → 자동 height ≈ 48px (터치 마지노선 OK) — w-기반 함정과 별개 padding 인라인 안전.
    7. `feedback_text_caption_leading_none` — 모바일 위치조정 토글 11px (line 187, h≈30) → `text-caption font-bold leading-none` (12 + leading-none, 작은 컨테이너 시각 패딩 방지) / 데스크톱 위치조정 12px (line 125, h≈34) + 데스크톱 설명 12px (line 115) → `text-caption leading-none` / 캘리브 안내 칩 12px (line 96, padding `6px 16px` 작은 칩) → `text-caption leading-none font-bold` / 데스크톱 다운로드 13px (line 137, h≈34) → `text-label font-bold leading-none`.
    8. `feedback_tsx_wave_emoji_dot_gap` — sketch 본문 이모지 0건 강제. AnnualPlanPage 본문에도 이모지 0건 (현재 잘 지켜짐 — 다운로드 svg 는 path 이지 이모지 아님). 데스크톱 설명의 `—` (em dash, line 116) 는 콘텐츠 글리프로 유지 (이모지 아님).
    9. `feedback_tsx_wave_stat_card_drift` — 연간 업무 추진 계획 페이지에 Stat Card (28px display 숫자) 없음 → §3.5 "미적용" 메타 명시. 단, sketch 새 패턴 (캘리브 모드 분기 매트릭스 4 state) 은 verbatim 인용해 W5 checklist 박제. source 의 `'2px solid var(--acl)'` calibMode 분기가 sketch 의 `border-2 border-accent` 토큰 패턴을 덮어쓰지 않도록 명시.
    10. `feedback_avoid_premature_confirmation` — TSX 변환 후 "approved 거의 일치" 자체 판단 금지. 결과 보여주고 사용자 판단. 특히 캘리브 좌표 시스템 시각 결과 (yearPos 저장 후 오버레이 위치) 는 사용자 판단 영역.
    11. ★ `feedback_pdflib_subset_false` (AnnualPlanPage 특화 — 폰트 패밀리 보존 일반화) — 연도 오버레이 `fontFamily: 'Malgun Gothic, 맑은 고딕, sans-serif'` (line 84) 는 엑셀 표지 글꼴과 시각 일치를 위한 의도된 폰트 — `var(--font-sans)` 같은 디자인 시스템 폰트로 교체 금지. 폰트 패밀리 = 출력물 시각 anchor 룰.
    12. ★ `project_redesign_15_daily_report_status` (AnnualPlanPage 특화 — 캘리브 좌표 시스템 precedent) — 15-daily-report SW3 의 portraitPos 보존 룰 동일 적용. STORAGE_KEY / FINGER_OFFSET / handleImageClick / handleImageTouch / yearPos overlay 모두 1 byte 변경 금지. SW3 변환 시 좌표 시스템 무손실 변환 성공 패턴 mirror.
    13. `feedback_check_branch_before_edit` — TSX 변환 wave 진입 전 현재 branch 가 `redesign/17-annual-plan` 인지 확인 (main 단일-trunk 운영, dirty 면 사용자 컨펌 먼저).

    **§12. 다음 단계**

    1. 본 wave (W5 checklist) 작성 완료 → 4 commit + push.
    2. 사용자 시각 검수 — cbc7119-preview 배포 사이클 1회 (main 머지 후 자동) — sketch 3 HTML 직접 열어 4 frame 시각 확인.
    3. 사용자 컨펌 받으면 **다음 quick task 시작** (`/clear` 권장 — memory `feedback_gsd_workflow_strict`) — TSX 변환 wave 진입.
       - 새 quick id (예: 260523-XX) 생성
       - PLAN: 1 task (W6 — TSX 변환), action = §3 매핑 표 verbatim + §5/§6/§7/§8 verify gate 그대로 적용
       - 산출: AnnualPlanPage.tsx 1 파일 in-place 수정 + atomic commit 1개 (`feat(17-annual-plan): W6 TSX 변환 ...`)
    4. TSX 변환 wave 완료 → 사용자 시각 검수 → main 머지 → cbc7119-preview 배포 → 직원도메인 별도 배포 (메모리 `feedback_deploy_test` 룰 — design 작업은 사용자 명시 컨펌 후만).
    5. 17-annual-plan 완결 status 메모 (memory `project_redesign_*_status` 패턴).

    **§ 자체 verify (본 W5 checklist 가 통과해야 할 gate)**
    - AnnualPlanPage.tsx + generateAnnualPlan.ts 2 파일 변경 0 (`git diff HEAD~4 HEAD -- {2 paths}` empty)
    - 12 섹션 헤더 ≥1 each (§1~§12)
    - verify 명령 fence ≥4 (§5/§6/§7/§8)
    - sketch 출처 frontmatter sketch_sources 3개 (W2/W3/W4) 모두 존재 확인 (test -f)
    - OQ #1~#5 인용 ≥5건
    - 메모리 룰 ≥13 인용 (10 기본 + AnnualPlanPage 특화 3건)
    - 캘리브 좌표 시스템 시그니처 5건 (STORAGE_KEY / FINGER_OFFSET / handleImageClick / handleImageTouch / yearPos) 모두 verbatim 박제

    **commit 메시지:**
    `docs(quick-260522-0j3): redesign/17-annual-plan W5 TSX conversion checklist (12 sections, sketch sources 3 + verify gates + 캘리브 좌표 시그니처 5건 박제 + AnnualPlanPage.tsx + generateAnnualPlan.ts 변경 0)`

    **commit 명령 (executor 가 실행 후 commit hash + message 보고):**
    `git add cha-bio-safety/docs/redesign-context/17-annual-plan/wave-5-tsx-conversion-checklist.md && git commit -m "docs(quick-260522-0j3): redesign/17-annual-plan W5 TSX conversion checklist (12 sections, sketch sources 3 + verify gates + 캘리브 좌표 시그니처 5건 박제 + AnnualPlanPage.tsx + generateAnnualPlan.ts 변경 0)"`

    **마지막 단계 (commit 후 2 파일 변경 0 최종 확인):**
    `git diff --name-only HEAD~4 HEAD -- cha-bio-safety/src/pages/AnnualPlanPage.tsx cha-bio-safety/src/utils/generateAnnualPlan.ts` 의 결과가 빈 줄이어야 함 (최근 4 commit 동안 2 파일 모두 변경 0).
  </action>
  <verify>
    <automated>
test -f cha-bio-safety/docs/redesign-context/17-annual-plan/wave-5-tsx-conversion-checklist.md \
  && wc -l cha-bio-safety/docs/redesign-context/17-annual-plan/wave-5-tsx-conversion-checklist.md | awk '{ if ($1 < 300) { print "FAIL: too short"; exit 1 } else print "PASS lines:", $1 }' \
  && grep -cE '^# §1[0-2]?\b|^## §1[0-2]?\b|^### §1[0-2]?\b|^\*\*§1[0-2]?\b|^# §[1-9]\b|^## §[1-9]\b|^### §[1-9]\b|^\*\*§[1-9]\b' cha-bio-safety/docs/redesign-context/17-annual-plan/wave-5-tsx-conversion-checklist.md | awk '{ if ($1 < 12) { print "FAIL: §1~§12 헤더 <12"; exit 1 } else print "PASS sections:", $1 }' \
  && grep -c '^```' cha-bio-safety/docs/redesign-context/17-annual-plan/wave-5-tsx-conversion-checklist.md | awk '{ if ($1 < 8) { print "FAIL: fence <8 (4 verify section x 2 open/close each)"; exit 1 } else print "PASS fences:", $1 }' \
  && grep -cE 'OQ #1|OQ #2|OQ #3|OQ #4|OQ #5' cha-bio-safety/docs/redesign-context/17-annual-plan/wave-5-tsx-conversion-checklist.md | awk '{ if ($1 < 5) { print "FAIL: OQ #1~#5 citations <5"; exit 1 } else print "PASS OQ citations:", $1 }' \
  && grep -c 'AnnualPlanPage\.tsx' cha-bio-safety/docs/redesign-context/17-annual-plan/wave-5-tsx-conversion-checklist.md | awk '{ if ($1 < 10) { print "FAIL: AnnualPlanPage.tsx ref <10"; exit 1 } else print "PASS AnnualPlanPage refs:", $1 }' \
  && grep -c 'generateAnnualPlan' cha-bio-safety/docs/redesign-context/17-annual-plan/wave-5-tsx-conversion-checklist.md | awk '{ if ($1 < 2) { print "FAIL: generateAnnualPlan ref <2"; exit 1 } else print "PASS generateAnnualPlan refs:", $1 }' \
  && grep -c 'bg-safe-bar' cha-bio-safety/docs/redesign-context/17-annual-plan/wave-5-tsx-conversion-checklist.md | awk '{ if ($1 < 2) { print "FAIL: bg-safe-bar citation <2"; exit 1 } else print "PASS bg-safe-bar:", $1 }' \
  && grep -c 'border-accent' cha-bio-safety/docs/redesign-context/17-annual-plan/wave-5-tsx-conversion-checklist.md | awk '{ if ($1 < 2) { print "FAIL: border-accent citation <2"; exit 1 } else print "PASS border-accent:", $1 }' \
  && grep -c 'bg-accent/10' cha-bio-safety/docs/redesign-context/17-annual-plan/wave-5-tsx-conversion-checklist.md | awk '{ if ($1 < 1) { print "FAIL: bg-accent/10 missing"; exit 1 } else print "PASS bg-accent/10:", $1 }' \
  && grep -c 'ChevronLeft' cha-bio-safety/docs/redesign-context/17-annual-plan/wave-5-tsx-conversion-checklist.md | awk '{ if ($1 < 2) { print "FAIL: ChevronLeft ref <2 (OQ #4 LOCKED)"; exit 1 } else print "PASS ChevronLeft:", $1 }' \
  && grep -c 'Download' cha-bio-safety/docs/redesign-context/17-annual-plan/wave-5-tsx-conversion-checklist.md | awk '{ if ($1 < 3) { print "FAIL: Download ref <3 (OQ #4 LOCKED)"; exit 1 } else print "PASS Download:", $1 }' \
  && grep -c 'STORAGE_KEY' cha-bio-safety/docs/redesign-context/17-annual-plan/wave-5-tsx-conversion-checklist.md | awk '{ if ($1 < 2) { print "FAIL: STORAGE_KEY 시그니처 박제 <2 (★ 캘리브)"; exit 1 } else print "PASS STORAGE_KEY:", $1 }' \
  && grep -c 'FINGER_OFFSET' cha-bio-safety/docs/redesign-context/17-annual-plan/wave-5-tsx-conversion-checklist.md | awk '{ if ($1 < 2) { print "FAIL: FINGER_OFFSET 시그니처 박제 <2 (★ 캘리브)"; exit 1 } else print "PASS FINGER_OFFSET:", $1 }' \
  && grep -c 'handleImageClick' cha-bio-safety/docs/redesign-context/17-annual-plan/wave-5-tsx-conversion-checklist.md | awk '{ if ($1 < 2) { print "FAIL: handleImageClick 박제 <2"; exit 1 } else print "PASS handleImageClick:", $1 }' \
  && grep -c 'handleImageTouch' cha-bio-safety/docs/redesign-context/17-annual-plan/wave-5-tsx-conversion-checklist.md | awk '{ if ($1 < 2) { print "FAIL: handleImageTouch 박제 <2"; exit 1 } else print "PASS handleImageTouch:", $1 }' \
  && grep -c 'yearPos' cha-bio-safety/docs/redesign-context/17-annual-plan/wave-5-tsx-conversion-checklist.md | awk '{ if ($1 < 2) { print "FAIL: yearPos 박제 <2"; exit 1 } else print "PASS yearPos:", $1 }' \
  && grep -c 'min(1.4vw, 16px)' cha-bio-safety/docs/redesign-context/17-annual-plan/wave-5-tsx-conversion-checklist.md | awk '{ if ($1 < 1) { print "FAIL: 연도 오버레이 fontSize verbatim missing"; exit 1 } else print "PASS min(1.4vw, 16px):", $1 }' \
  && grep -c 'Malgun Gothic' cha-bio-safety/docs/redesign-context/17-annual-plan/wave-5-tsx-conversion-checklist.md | awk '{ if ($1 < 1) { print "FAIL: Malgun Gothic verbatim missing"; exit 1 } else print "PASS Malgun Gothic:", $1 }' \
  && grep -c 'sketch-wave-2-chrome' cha-bio-safety/docs/redesign-context/17-annual-plan/wave-5-tsx-conversion-checklist.md | awk '{ if ($1 < 1) { print "FAIL: sketch W2 ref missing"; exit 1 } else print "PASS W2 ref:", $1 }' \
  && grep -c 'sketch-wave-3-preview-calibration' cha-bio-safety/docs/redesign-context/17-annual-plan/wave-5-tsx-conversion-checklist.md | awk '{ if ($1 < 1) { print "FAIL: sketch W3 ref missing"; exit 1 } else print "PASS W3 ref:", $1 }' \
  && grep -c 'sketch-wave-4-download' cha-bio-safety/docs/redesign-context/17-annual-plan/wave-5-tsx-conversion-checklist.md | awk '{ if ($1 < 1) { print "FAIL: sketch W4 ref missing"; exit 1 } else print "PASS W4 ref:", $1 }' \
  && grep -oE 'feedback_[a-z_]+|project_redesign_[a-z0-9_]+' cha-bio-safety/docs/redesign-context/17-annual-plan/wave-5-tsx-conversion-checklist.md | sort -u | wc -l | awk '{ if ($1 < 13) { print "FAIL: memory rules unique <13"; exit 1 } else print "PASS memory rules:", $1 }' \
  && (git diff --name-only HEAD~4 HEAD -- cha-bio-safety/src/pages/AnnualPlanPage.tsx | wc -l | awk '{ if ($1 != 0) { print "FAIL: AnnualPlanPage.tsx changed in last 4 commits"; exit 1 } else print "PASS AnnualPlanPage.tsx untouched" }') \
  && (git diff --name-only HEAD~4 HEAD -- cha-bio-safety/src/utils/generateAnnualPlan.ts | wc -l | awk '{ if ($1 != 0) { print "FAIL: generateAnnualPlan.ts changed in last 4 commits"; exit 1 } else print "PASS generateAnnualPlan.ts untouched" }') \
  && (ls cha-bio-safety/docs/redesign-context/17-annual-plan/sketch-wave-{2-chrome,3-preview-calibration,4-download}.html 2>/dev/null | wc -l | awk '{ if ($1 != 3) { print "FAIL: 3 sketch files missing"; exit 1 } else print "PASS all 3 sketches present" }') \
  && git log --oneline -1 | grep -q '260522-0j3.*W5' && echo "PASS commit W5"
    </automated>
  </verify>
  <done>
    - wave-5-tsx-conversion-checklist.md 파일 존재 + ≥300 lines (≥350 권장)
    - §1~§12 12 섹션 헤더 모두 존재
    - 자체 verify fence ≥8 (§5/§6/§7/§8 각 명령 fence open+close)
    - OQ #1~#5 5건 모두 인용
    - AnnualPlanPage.tsx ref ≥10건 / generateAnnualPlan ref ≥2건
    - bg-safe-bar ≥2 / border-accent ≥2 / bg-accent/10 ≥1 / ChevronLeft ≥2 / Download ≥3 (OQ #1, #2, #4 LOCKED)
    - ★ 캘리브 좌표 시스템 시그니처 5건 모두 박제 (STORAGE_KEY ≥2 / FINGER_OFFSET ≥2 / handleImageClick ≥2 / handleImageTouch ≥2 / yearPos ≥2 / min(1.4vw, 16px) ≥1 / Malgun Gothic ≥1)
    - sketch W2/W3/W4 ref 각 ≥1
    - 메모리 룰 (feedback_*.md + project_redesign_*) unique ≥13
    - **AnnualPlanPage.tsx + generateAnnualPlan.ts 2 파일 모두 변경 0** (git diff HEAD~4 HEAD 결과 empty)
    - 3 sketch HTML 파일 (W2/W3/W4) 모두 존재 확인
    - atomic commit `docs(quick-260522-0j3): ... W5 TSX conversion checklist ...` 1개
  </done>
</task>

</tasks>

<verification>
**전체 4 task 종합 verify (executor 가 마지막 task 4 종료 직후 실행):**

```bash
# 1. 4 산출 파일 모두 존재
test -f cha-bio-safety/docs/redesign-context/17-annual-plan/sketch-wave-2-chrome.html
test -f cha-bio-safety/docs/redesign-context/17-annual-plan/sketch-wave-3-preview-calibration.html
test -f cha-bio-safety/docs/redesign-context/17-annual-plan/sketch-wave-4-download.html
test -f cha-bio-safety/docs/redesign-context/17-annual-plan/wave-5-tsx-conversion-checklist.md

# 2. AnnualPlanPage.tsx + generateAnnualPlan.ts 변경 0 (가장 중요)
[[ -z "$(git diff --name-only HEAD~4 HEAD -- cha-bio-safety/src/pages/AnnualPlanPage.tsx)" ]] || echo "FAIL: AnnualPlanPage.tsx changed"
[[ -z "$(git diff --name-only HEAD~4 HEAD -- cha-bio-safety/src/utils/generateAnnualPlan.ts)" ]] || echo "FAIL: generateAnnualPlan.ts changed"

# 3. 평면 폴더 패턴 — sketch/ 서브폴더 없음
test ! -d cha-bio-safety/docs/redesign-context/17-annual-plan/sketch/

# 4. 4 atomic commit 확인
git log --oneline HEAD~4..HEAD | grep -c '260522-0j3' | awk '{ if ($1 != 4) { print "FAIL: 4 commit 아님"; exit 1 } else print "PASS 4 commits" }'

# 5. 17-annual-plan/ 외 변경 0 (다른 페이지 영향 없음)
git diff --name-only HEAD~4 HEAD | grep -v '^cha-bio-safety/docs/redesign-context/17-annual-plan/' | wc -l | awk '{ if ($1 != 0) { print "FAIL: 다른 페이지 변경 감지"; exit 1 } else print "PASS only 17-annual-plan/ changed" }'

# 6. 캘리브 좌표 시스템 시그니처 5건 박제 — W3 sketch + W5 checklist 양쪽
grep -c 'STORAGE_KEY' cha-bio-safety/docs/redesign-context/17-annual-plan/sketch-wave-3-preview-calibration.html | awk '{ if ($1 < 1) { print "FAIL: STORAGE_KEY W3 sketch missing"; exit 1 } else print "PASS STORAGE_KEY W3:", $1 }'
grep -c 'FINGER_OFFSET' cha-bio-safety/docs/redesign-context/17-annual-plan/sketch-wave-3-preview-calibration.html | awk '{ if ($1 < 1) { print "FAIL: FINGER_OFFSET W3 sketch missing"; exit 1 } else print "PASS FINGER_OFFSET W3:", $1 }'
```
</verification>

<success_criteria>
- W2~W4 sketch HTML 3개 + W5 checklist markdown 1개 = 총 4 파일 모두 `cha-bio-safety/docs/redesign-context/17-annual-plan/` 직속 평면 배치 (sketch/ 서브폴더 X)
- AnnualPlanPage.tsx (225 lines) + generateAnnualPlan.ts 둘 다 변경 0 byte
- OQ #1~#5 LOCKED 결정 5건 모두 4 파일 안 verbatim 반영:
  - OQ #1: 다운로드 bg-safe-bar solid + 그라데이션 완전 폐기 (W4 + W5)
  - OQ #2: 위치조정 토글 border-accent bg-accent/10 text-accent (W2 + W5)
  - OQ #3: 폰트 격상 매핑 11→12 / 12 유지 / 13 유지 / 14→16 (W2~W5 모두)
  - OQ #4: Lucide ChevronLeft size={15} + Download size={15/16} (W2 + W4 + W5)
  - OQ #5: preview border-2 border-accent (캘리브) + 안내 칩 인라인 rgba (W3 + W5)
- ★ 캘리브 좌표 시스템 시그니처 5건 (STORAGE_KEY / FINGER_OFFSET / handleImageClick / handleImageTouch / yearPos overlay) 모두 W3 sketch + W5 checklist 양쪽 verbatim 박제
- negative gate (이모지 / linear-gradient / 9·10·11px / status- prefix / w-8 h-8 / 옛 alias 토큰) 4 파일 모두 0건
- 각 task atomic commit 1개씩 = 총 4 commit (`docs(quick-260522-0j3): ...`)
- 17-annual-plan/ 외 변경 0 (다른 페이지 영향 없음)
</success_criteria>

<output>
- W2~W4 sketch HTML 3개 + W5 checklist markdown 1개 = 총 4 파일이 `cha-bio-safety/docs/redesign-context/17-annual-plan/` 직속 평면 배치로 생성됨
- 4 atomic commit (W2 / W3 / W4 / W5) 모두 worktree 안에서 실행됨 — orchestrator 가 cherry-pick 으로 부모 브랜치 머지 (executor commit 자동 머지 안 됨, 16-workshift sjj + 17-annual-plan wmq precedent)
- `.planning/quick/260522-0j3-redesign-17-annual-plan-sketch-waves-w2-/260522-0j3-SUMMARY.md` 생성 (다음 wave 인수인계 — sketch sources 3 + checklist + 캘리브 시그니처 박제 + 사용자 시각 검수 안내)
- 사용자 시각 검수 후 W6 (TSX 변환) wave 진입 가능 — `/clear` + 새 quick id (260523-XX) + PLAN 1 task (action = §3 매핑 verbatim, verify = §5/§6/§7/§8 그대로)
</output>
