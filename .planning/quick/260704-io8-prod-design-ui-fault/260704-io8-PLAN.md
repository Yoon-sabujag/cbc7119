---
phase: quick-260704-io8
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cha-bio-safety/src/utils/api.ts
  - cha-bio-safety/src/utils/panelEvents.ts
  - cha-bio-safety/src/components/PanelEventRow.tsx
  - cha-bio-safety/src/pages/FireAlarmHistoryPage.tsx
  - cha-bio-safety/src/App.tsx
  - cha-bio-safety/src/pages/InspectionPage.tsx
  - cha-bio-safety/src/pages/DashboardPage.tsx
  - cha-bio-safety/src/components/panel/LivePanelImage.tsx
  - cha-bio-safety/src/pages/FireAlarmPage.tsx
autonomous: true
requirements:
  - PANEL-UI-PORT  # prod→design 화재수신반 UI 갭 이관 (DESIGN-HANDOFF 260704-hzz)

must_haves:
  truths:
    - "화재수신반 이벤트 카드가 자동감지+수동기록 48h 병합 뷰로 표시된다 (모바일 FireAlarmModal + 데스크톱 pane)"
    - "모바일에서 /fire-alarm-history 라우트로 전체 이력 페이지에 진입할 수 있다"
    - "데스크톱 화재수신반 pane 에서 '전체 이력' 토글로 in-pane 이력(썸네일 포함)을 볼 수 있다"
    - "fault(고장) 활성경보가 풀스크린에 주황(#f59e0b) variant 로 렌더된다"
    - "모바일 라이브카드가 Android 트랙 축소 fix(object-fill)를 반영하고 iOS mw4 그리드는 무회귀다"
    - "cd cha-bio-safety && npm run build (tsc+vite) 가 통과한다"
  artifacts:
    - path: "cha-bio-safety/src/utils/panelEvents.ts"
      provides: "mergePanelEvents/useRecentPanelEvents/kstStr/KIND_BADGE (자동+수동 병합)"
      contains: "export function useRecentPanelEvents"
    - path: "cha-bio-safety/src/components/PanelEventRow.tsx"
      provides: "카드 ×2 + 이력 페이지 공용 이벤트 행 (thumb prop)"
      contains: "export function PanelEventRow"
    - path: "cha-bio-safety/src/pages/FireAlarmHistoryPage.tsx"
      provides: "FireAlarmHistoryView + /fire-alarm-history 풀페이지"
      exports: ["FireAlarmHistoryView", "default"]
    - path: "cha-bio-safety/src/utils/api.ts"
      provides: "AlarmSummary.type / Alarm.type 에 'fault' 추가"
      contains: "'fire' | 'equip' | 'fault'"
    - path: "cha-bio-safety/src/App.tsx"
      provides: "/fire-alarm-history 라우트 + no-nav 등록"
      contains: "fire-alarm-history"
    - path: "cha-bio-safety/src/pages/InspectionPage.tsx"
      provides: "병합 카드(모바일+데스크톱) + in-pane 전체 이력"
      contains: "useRecentPanelEvents"
    - path: "cha-bio-safety/src/components/panel/LivePanelImage.tsx"
      provides: "aspectClass/objectClass 추가 props (하위호환)"
      contains: "objectClass"
    - path: "cha-bio-safety/src/pages/DashboardPage.tsx"
      provides: "모바일 라이브카드 Android 트랙 fix + object-fill"
      contains: "minmax(0,189px)"
    - path: "cha-bio-safety/src/pages/FireAlarmPage.tsx"
      provides: "fault 풀스크린 variant"
      contains: "isFault"
  key_links:
    - from: "cha-bio-safety/src/pages/InspectionPage.tsx"
      to: "cha-bio-safety/src/components/PanelEventRow.tsx"
      via: "mergedEvents/mergedPanelEvents.map(ev => <PanelEventRow .../>)"
      pattern: "mergedEvents\\.map|mergedPanelEvents\\.map"
    - from: "cha-bio-safety/src/pages/InspectionPage.tsx"
      to: "cha-bio-safety/src/pages/FireAlarmHistoryPage.tsx"
      via: "panelHistoryOpen ? <FireAlarmHistoryView thumb />"
      pattern: "FireAlarmHistoryView"
    - from: "cha-bio-safety/src/App.tsx"
      to: "cha-bio-safety/src/pages/FireAlarmHistoryPage.tsx"
      via: "/fire-alarm-history Route"
      pattern: "fire-alarm-history"
    - from: "cha-bio-safety/src/utils/panelEvents.ts"
      to: "cha-bio-safety/src/utils/api.ts"
      via: "fireAlarmApi.getByYear + alarmApi.getEvents"
      pattern: "fireAlarmApi\\.getByYear|alarmApi\\.getEvents"
    - from: "cha-bio-safety/src/pages/DashboardPage.tsx"
      to: "cha-bio-safety/src/components/panel/LivePanelImage.tsx"
      via: "objectClass=\"object-fill\" / aspectClass=\"h-full\""
      pattern: "objectClass="
---

<objective>
prod(`origin/production`, worktree `~/Documents/20260328`)에 직접 들어간 Phase 25 화재수신반 UI 진화를 design/`main`(현재 HEAD `e524b90a` = 260702-1vw)로 이관한다. 하이브리드 전략: **신규 3파일은 prod 최종본 통짜 cp**(retype 금지, drift 0), **공유 6파일은 정확 old_string/new_string 의도 이식**(prod 인라인 old_string 은 design 에 no-op 아니지만 형상은 동일 토큰 — 아래 실측 diff 사용).

Purpose: 다음 redesign wave 가 `main` 에서 분기할 때 패널 UI 가 회귀하지 않도록 `main` 을 prod 현실에 정렬.
Output: 병합 이벤트 카드(모바일+데스크톱) + 전체 이력 페이지/in-pane + fault 풀스크린 + 모바일 라이브카드 fix. `npm run build` 통과.

CRITICAL 룰:
- **functions/ · migrations/ 절대 미포함** (데이터 트랙, 별도 배포). src/ 만 이식.
- **FloorPlanPage.tsx 무변경.** 데스크톱 캘린더/오늘일정(h-[126px])·좌측 점검현황 size76 무변경.
- **wrangler/deploy 명령 금지** (이 워크트리 룰). `npm run build` 는 `tsc && vite build` 로 wrangler 미호출 — 검증 게이트로 안전.
- 신규 3파일은 반드시 cp 통짜. 라인번호 stale 시 grep 재확인.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@/Users/jongyupyoon/Documents/20260328/.planning/quick/260704-hzz-prod-to-staging-and-prod-to-design-panel/DESIGN-HANDOFF.md

레퍼런스 (읽기 전용, 절대 수정 금지) — prod 원본:
- /Users/jongyupyoon/Documents/20260328/cha-bio-safety/src/utils/panelEvents.ts
- /Users/jongyupyoon/Documents/20260328/cha-bio-safety/src/components/PanelEventRow.tsx
- /Users/jongyupyoon/Documents/20260328/cha-bio-safety/src/pages/FireAlarmHistoryPage.tsx
- /Users/jongyupyoon/Documents/20260328/cha-bio-safety/src/pages/InspectionPage.tsx  (병합 섹션: 모바일 L4329-4344 / 데스크톱 in-pane L5128-5147 / 데스크톱 evt-card L5265-5280 / imports L14-16 / hook L4134·L4813·state L4851)
- /Users/jongyupyoon/Documents/20260328/cha-bio-safety/src/pages/DashboardPage.tsx  (모바일 grid L636 / ①-b 라이브 L664-674 / 데스크톱 위젯 L466-479 무변경)
- /Users/jongyupyoon/Documents/20260328/cha-bio-safety/src/components/panel/LivePanelImage.tsx
- /Users/jongyupyoon/Documents/20260328/cha-bio-safety/src/pages/FireAlarmPage.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: api.ts fault 타입 + 신규 3파일 cp + App.tsx 라우트 (foundational, build-clean)</name>
  <files>cha-bio-safety/src/utils/api.ts, cha-bio-safety/src/utils/panelEvents.ts, cha-bio-safety/src/components/PanelEventRow.tsx, cha-bio-safety/src/pages/FireAlarmHistoryPage.tsx, cha-bio-safety/src/App.tsx</files>
  <action>
자기완결 foundational task — 신규 파일 도입 + 타입/라우트 배선 후 빌드가 깨끗해야 Task 2~4 가 그 위에 얹힌다.

(1a) **api.ts** — 유일 델타는 `type` union 2곳에 `| 'fault'` 추가 (design `location` 은 이미 보유, 추가 X):
  - `interface AlarmSummary` 안 `type: 'fire' | 'equip'` → `type: 'fire' | 'equip' | 'fault'`
  - `interface Alarm` 안 `type: 'fire' | 'equip'` → `type: 'fire' | 'equip' | 'fault'`
  두 old_string 이 동일(`type: 'fire' | 'equip'`)이므로 각 interface 블록을 앵커로 구분해 replace_all 대신 개별 edit. **주의: L724-727 주석 헤더("미배포")는 design 데이터 트랙 기준으로 정확 — 절대 수정하지 말 것** (prod 주석 "prod 에 배포됨" 을 가져오지 말 것).

(1b) **신규 3파일 통짜 cp** (prod 최종본 — retype 금지, Bash cp 사용):
  - `cp /Users/jongyupyoon/Documents/20260328/cha-bio-safety/src/utils/panelEvents.ts /Users/jongyupyoon/Documents/cbc7119-design/cha-bio-safety/src/utils/panelEvents.ts`
  - `cp /Users/jongyupyoon/Documents/20260328/cha-bio-safety/src/components/PanelEventRow.tsx /Users/jongyupyoon/Documents/cbc7119-design/cha-bio-safety/src/components/PanelEventRow.tsx`
  - `cp /Users/jongyupyoon/Documents/20260328/cha-bio-safety/src/pages/FireAlarmHistoryPage.tsx /Users/jongyupyoon/Documents/cbc7119-design/cha-bio-safety/src/pages/FireAlarmHistoryPage.tsx`
  세 파일 모두 design 에 부재(신규 생성). imports(fireAlarmApi/getByYear·alarmApi/getEvents·type Alarm·mergePanelEvents/kstStr·PanelEventRow·react-query·router·lucide) 전부 design 에 존재 확인됨.

(1c) **App.tsx** — 4개 edit (prod cf019347 형상; 단 **DESKTOP_NO_NAV 가 아니라 DESKTOP_HEADER_HIDE 에 등록** — prod 실측 반영):
  - lazy import: `const FireAlarmPage             = lazy(() => import('./pages/FireAlarmPage'))` 다음 줄에 `const FireAlarmHistoryPage      = lazy(() => import('./pages/FireAlarmHistoryPage'))` 추가.
  - `MOBILE_NO_NAV_PATHS` 배열 끝 `'/fire-alarm']` → `'/fire-alarm', '/fire-alarm-history']`.
  - `DESKTOP_HEADER_HIDE_PATHS` 배열 `['/elevator', '/div', '/floorplan', '/workshift']` → `['/elevator', '/div', '/floorplan', '/workshift', '/fire-alarm-history']`. (DESKTOP_NO_NAV_PATHS 는 무변경 — prod 도 여기엔 안 넣음. 데스크톱 이력은 InspectionPage in-pane 이 주경로이고, 직접 진입 시 App 제목 헤더만 숨기면 됨.)
  - Route: `<Route path="/fire-alarm"    element={<Auth><FireAlarmPage /></Auth>} />` 다음 줄에 `<Route path="/fire-alarm-history" element={<Auth><FireAlarmHistoryPage /></Auth>} />` 추가.
  </action>
  <verify>
    <automated>cd /Users/jongyupyoon/Documents/cbc7119-design/cha-bio-safety && test -f src/utils/panelEvents.ts && test -f src/components/PanelEventRow.tsx && test -f src/pages/FireAlarmHistoryPage.tsx && [ "$(grep -c "'fire' | 'equip' | 'fault'" src/utils/api.ts)" = "2" ] && [ "$(grep -c 'fire-alarm-history' src/App.tsx)" = "3" ] && grep -q "import('./pages/FireAlarmHistoryPage')" src/App.tsx && npm run build</automated>
  </verify>
  <done>3 신규파일 존재 + api.ts fault union 2곳 + App.tsx: lazy import(FireAlarmHistoryPage) + hyphen 경로 3회(MOBILE/DESKTOP_HEADER/Route) + `npm run build` (tsc+vite) exit 0.</done>
</task>

<task type="auto">
  <name>Task 2: InspectionPage.tsx 병합 카드 + in-pane 전체 이력 의도 이식</name>
  <files>cha-bio-safety/src/pages/InspectionPage.tsx</files>
  <action>
design InspectionPage(6766줄)의 화재수신반 자동감지-전용 이벤트 리스트를 prod 의 **병합모듈 + PanelEventRow** 로 교체. design 과 prod 는 동일 Tailwind 토큰(redesign 이 prod 에도 머지됨)이라 마크업이 거의 일치 — 아래 실측 old/new 사용. 라인번호 stale 가능 → 문자열 앵커로 grep 재확인.

(2a) **imports** — 기존 `import { inspectionApi, ..., type Alarm } from '../utils/api'` 줄(design L6) 바로 다음에 3줄 추가:
  `import { PanelEventRow } from '../components/PanelEventRow'`
  `import { useRecentPanelEvents } from '../utils/panelEvents'`
  `import { FireAlarmHistoryView } from './FireAlarmHistoryPage'`

(2b) **모바일 `FireAlarmModal`** (design ~L5350 시작):
  - 이 컴포넌트에는 `navigate` 가 없음 → `const qc = useQueryClient()` 줄 바로 다음에 `const navigate = useNavigate()` 추가 (useNavigate 는 이미 top import 됨).
  - `events` useQuery 블록(`queryKey: ['alarm-events']`, ~L5371-5391) 닫힘 `})` 다음에 `const mergedEvents = useRecentPanelEvents(events)` 추가.
  - **모바일 evt-card 교체** (design L5572-5602). 헤더 라벨 `최근 이벤트 (자동감지)` → `최근 이벤트 (최근 48시간)`. non-alarm 브랜치의 `<span ...>최근 48시간</span>` 배지를 아래 버튼으로 교체:
    `<button onClick={() => navigate('/fire-alarm-history')} className="inline-flex items-center gap-[3px] rounded-pill text-[11px] font-bold text-text-secondary bg-surface-sunken border border-border-default pl-2.5 pr-2 py-1 leading-none cursor-pointer">전체 이력<ChevronRight size={13} /></button>`
    리스트 본문 `{events.length === 0 ? (<div ...>최근 48시간 자동감지 이벤트 없음</div>) : (events.map(ev => { const b = badge2(ev); return (...inline 행...) }))}` 를 `{mergedEvents.length === 0 ? (<div className="p-[14px_12px] text-caption text-text-tertiary text-center">최근 48시간 이벤트 없음</div>) : (mergedEvents.map(ev => <PanelEventRow key={ev.id} item={ev} />))}` 로 교체.
    **주의: alarm 브랜치의 `감지중 N` 배지는 `events.filter(...)` 유지** (병합 아닌 원본 status 카운트). prod L4329-4344 참조.

(2c) **데스크톱 `DesktopInspectionView`** (design ~L6028 시작):
  - `panelEvents` useQuery 블록(`queryKey: ['alarm-events']`, ~L6058-6063) 닫힘 다음에 `const mergedPanelEvents = useRecentPanelEvents(panelEvents)` 추가.
  - `panelHistoryOpen` 상태 추가: 폼 상태 근처(design ~L6085 `const [paType, ...]` 위 또는 아래)에 `const [panelHistoryOpen, setPanelHistoryOpen] = useState(false)` 추가.
  - **isPanel pane 을 panelHistoryOpen 삼항으로 래핑** (prod L5128-5147). design L6371-6373:
    `        {isPanel ? (` / `          // ── 화재수신반 3분할 상세 pane (Surface 6) ──` / `          <>` 를 아래로 교체 — 이력 브랜치를 앞에 삽입하고 기존 pane 을 else 로:
    ```
            {isPanel ? (
              panelHistoryOpen ? (
                <>
                  <div className="shrink-0 flex items-center gap-2.5 px-5 py-2 border-b border-border-default bg-surface-raised">
                    <button onClick={() => setPanelHistoryOpen(false)} className="w-7 h-7 rounded-[7px] bg-surface-sunken border border-border-default cursor-pointer flex items-center justify-center shrink-0 hover:bg-surface-active transition-colors">
                      <ChevronLeft size={14} className="text-text-secondary" />
                    </button>
                    <div className="flex-1 flex items-center gap-1.5 text-[16px] font-bold text-text-primary min-w-0">
                      <BellRing size={16} className="text-text-secondary shrink-0" />
                      <span className="truncate">화재수신반 이력</span>
                    </div>
                  </div>
                  <div className="flex-1 min-h-0">
                    <FireAlarmHistoryView thumb />
                  </div>
                </>
              ) : (
              // ── 화재수신반 3분할 상세 pane (Surface 6) ──
              <>
    ```
    (들여쓰기는 design 기존 컨텍스트에 맞출 것 — 위는 구조 예시.) 그리고 이 pane 이 닫히는 `</>` (design L6557, `) : recordId && detail ? (` 바로 위) 다음에 삼항 닫기 `)` 를 한 줄 추가.
  - **데스크톱 evt-card 교체** (design L6492-6526). 헤더 라벨 `최근 이벤트 (자동감지)` → `최근 이벤트 (최근 48시간)`. non-alarm 브랜치 `<span ...>최근 48시간</span>` → `<button onClick={() => setPanelHistoryOpen(true)} className="inline-flex items-center gap-[3px] rounded-pill text-[11px] font-bold text-text-secondary bg-surface-sunken border border-border-default pl-2.5 pr-2 py-1 leading-none cursor-pointer">전체 이력<ChevronRight size={13} /></button>`. 리스트 본문 `{panelEvents.length === 0 ? (<div ...>) : (panelEvents.map(ev => { const b = panelBadge2(ev); return (...inline 썸네일 행...) }))}` → `{mergedPanelEvents.length === 0 ? (<div className="px-3 py-[14px] text-caption text-text-tertiary text-center">최근 48시간 이벤트 없음</div>) : (mergedPanelEvents.map(ev => <PanelEventRow key={ev.id} item={ev} thumb />))}`. **alarm 브랜치 `감지중 N` 는 `panelEvents.filter(...)` 유지.** prod L5265-5280 참조.

  참고: 교체 후 `badge2`/`panelBadge2` 헬퍼가 미사용될 수 있으나 tsconfig `noUnusedLocals:false` 라 빌드 통과 — 그대로 두어도 무방(제거하려면 미사용 확인 후).
  </action>
  <verify>
    <automated>cd /Users/jongyupyoon/Documents/cbc7119-design/cha-bio-safety && [ "$(grep -c 'PanelEventRow' src/pages/InspectionPage.tsx)" -ge 3 ] && [ "$(grep -c 'useRecentPanelEvents' src/pages/InspectionPage.tsx)" -ge 3 ] && [ "$(grep -c 'FireAlarmHistoryView' src/pages/InspectionPage.tsx)" -ge 2 ] && [ "$(grep -ci 'panelHistoryOpen' src/pages/InspectionPage.tsx)" -ge 3 ] && npm run build</automated>
  </verify>
  <done>모바일 FireAlarmModal + 데스크톱 pane 이 mergedEvents/mergedPanelEvents.map(PanelEventRow) 사용, 데스크톱 in-pane 전체 이력(panelHistoryOpen) 토글 동작, `npm run build` exit 0.</done>
</task>

<task type="auto">
  <name>Task 3: DashboardPage.tsx 모바일 라이브카드 + LivePanelImage.tsx (⚠️ mw4 무회귀)</name>
  <files>cha-bio-safety/src/components/panel/LivePanelImage.tsx, cha-bio-safety/src/pages/DashboardPage.tsx</files>
  <action>
⚠️ **최고위험 — 260701-mw4(main `7a4676b`)의 모바일 iOS 그리드/데스크톱 정렬을 회귀시키지 말 것.** 실측 결과: **모바일 grid 의 iOS 브랜치는 design==prod 로 동일**(`auto auto auto auto minmax(125px, 1fr)`) → iOS 는 건드릴 게 없다. **Android 브랜치만** 다르다. 데스크톱(우측 캘린더/오늘일정 h-[126px]·좌측 점검현황 size76·데스크톱 라이브 위젯 L461-479)은 **전부 무변경**.

(3a) **LivePanelImage.tsx** — prod 의 하위호환 additive props 2개 이식(4 edit):
  - interface `LivePanelImageProps` 에 `imgClassName?: string` 다음 2줄 추가:
    `  aspectClass?: string   // 프레임 비율/높이 (기본 16:9 aspect-video). 고정높이(h-[..]) 로 오버라이드.`
    `  objectClass?: string   // object-fit (기본 cover). object-fill = 짜부(비율 무시 박스 채움).`
  - 구조분해 기본값 `imgClassName = '',` 다음에 `  aspectClass = 'aspect-video',` 와 `  objectClass = 'object-cover',` 추가.
  - 컨테이너 className `relative w-full aspect-video bg-black overflow-hidden` → `relative w-full ${aspectClass} bg-black overflow-hidden` (템플릿 리터럴).
  - img className `w-full h-full object-cover ${imgClassName}` → `w-full h-full ${objectClass} ${imgClassName}`.
  (design LivePanelImage 와 prod 는 이 props 외 동일 — 데스크톱/경보 소비자는 기본값으로 무영향.)

(3b) **DashboardPage.tsx 모바일** — 2 edit (데스크톱 절대 금지):
  - **모바일 grid Android 트랙만 교체** (design L631-633 `gridTemplateRows`). old:
    ```
              gridTemplateRows: IS_ANDROID
                ? 'auto auto auto auto minmax(140px, 1fr)'
                : 'auto auto auto auto minmax(125px, 1fr)',
    ```
    new (Android 라이브 트랙을 minmax(0,189px) 로, 점검현황 min 은 125 로; **iOS 줄은 글자 하나도 바꾸지 말 것**):
    ```
              gridTemplateRows: IS_ANDROID
                ? 'auto minmax(0,189px) auto auto minmax(125px,1fr)'
                : 'auto auto auto auto minmax(125px, 1fr)',
    ```
    바로 위 주석(design L629 `// gridTemplateRows 는 IS_ANDROID 동적 분기 ...`)은 prod L634 주석 `// gridTemplateRows 인라인 허용 키. Android 만 라이브(2번째) 트랙 minmax(0,189px) 로 축소 허용(작은 화면 짜부). 점검현황(5번째) 공통 minmax(125,1fr).` 로 갱신.
  - **①-b 라이브카드 교체** (design L659-667). old:
    ```
            <div
              onClick={() => navigate('/inspection?panel=fire-alarm')}
              className="rounded-md overflow-hidden bg-black cursor-pointer min-h-[180px] [animation:slideUp_.28s_.03s_ease-out_both]"
            >
              <LivePanelImage
                frameUpdatedAt={frameUpdatedAt}
                imgClassName="w-full h-full object-cover"
              />
            </div>
    ```
    new (prod L664-674):
    ```
            <div
              onClick={() => navigate('/inspection?panel=fire-alarm')}
              // 라이브 높이 = grid 트랙이 결정. iOS=h-[120px] 고정 트랙 / Android=minmax(0,189px) 트랙(공간 부족하면 축소). 이미지는 object-fill 로 트랙 높이에 짜부(클립 X).
              className={`rounded-md overflow-hidden bg-black cursor-pointer ${IS_ANDROID ? '' : 'h-[120px]'} [animation:slideUp_.28s_.03s_ease-out_both]`}
            >
              <LivePanelImage
                frameUpdatedAt={frameUpdatedAt}
                aspectClass="h-full"
                objectClass="object-fill"
              />
            </div>
    ```
    (바깥 `{/* ①-b 화재수신반 라이브 ... */}` 주석 블록 design L655-658 은 prod L660-663 과 동일 — 무변경.)
  </action>
  <verify>
    <automated>cd /Users/jongyupyoon/Documents/cbc7119-design/cha-bio-safety && [ "$(grep -c "'auto minmax(0,189px) auto auto minmax(125px,1fr)'" src/pages/DashboardPage.tsx)" = "1" ] && [ "$(grep -c 'auto auto auto auto minmax(125px, 1fr)' src/pages/DashboardPage.tsx)" = "1" ] && [ "$(grep -c 'object-fill' src/pages/DashboardPage.tsx)" -ge 1 ] && [ "$(grep -c 'h-\[126px\]' src/pages/DashboardPage.tsx)" -ge 1 ] && [ "$(grep -c 'objectClass' src/components/panel/LivePanelImage.tsx)" -ge 2 ] && npm run build</automated>
  </verify>
  <done>LivePanelImage 에 aspectClass/objectClass props 추가, 모바일 Android 트랙 minmax(0,189px)+object-fill 반영, **iOS grid 줄(`auto auto auto auto minmax(125px, 1fr)`) 정확히 1회 유지(mw4 무회귀)**, 데스크톱 오늘일정 h-[126px] 유지, `npm run build` exit 0.</done>
</task>

<task type="auto">
  <name>Task 4: FireAlarmPage.tsx fault variant + 최종 전체 빌드 게이트</name>
  <files>cha-bio-safety/src/pages/FireAlarmPage.tsx</files>
  <action>
prod 0afae824 의 fault(고장) 풀스크린 variant 를 이식 (design 204줄 ↔ prod 209줄, fault 분기만 추가). 8개 localized edit — prod FireAlarmPage 를 verbatim 레퍼런스로 사용:
  - `const isFire = kind === 'fire'` 다음 줄에 `const isFault = kind === 'fault'` 추가. 위 주석 `// 'fire' | 'equip' | undefined` → `// 'fire' | 'equip' | 'fault' | undefined`.
  - 주석 `// 색 = 고정 의미 (fire danger #ef4444 / equip safe #22c55e)` → `// 색 = 고정 의미 (fire danger #ef4444 / fault warning #f59e0b / equip safe #22c55e)`.
  - `washBg` 삼항: `isFire ? 'radial-...(239,68,68...)' : 'radial-...(34,197,94...)'` 를 `isFire ? '...(239,68,68...)' : isFault ? 'radial-gradient(circle at center, rgba(245,158,11,.45), rgba(120,72,8,.93))' : '...(34,197,94...)'` 로.
  - `const iconBg = isFire ? '#ef4444' : '#22c55e'` → `... : isFault ? '#f59e0b' : '#22c55e'`.
  - `const frameFilter = isFire ? 'brightness(.42) saturate(1.2)' : 'brightness(.4)'` → `... : isFault ? 'brightness(.42) saturate(1.15)' : 'brightness(.4)'`.
  - fa-icon 96px 안 `{isFire ? <Flame size={46} ...> : <Settings size={44} ...>}` → `{isFire ? <Flame size={46} className="text-white" /> : isFault ? <AlertTriangle size={46} className="text-white" /> : <Settings size={44} className="text-white" />}`.
  - fa-kind: `{isFire ? <Flame size={28} /> : <Settings size={28} />}` → `... : isFault ? <AlertTriangle size={28} /> : <Settings size={28} />}` 및 `<span>{isFire ? '화재 발생' : '설비 동작'}</span>` → `<span>{isFire ? '화재 발생' : isFault ? '고장 발생' : '설비 동작'}</span>` (주석 `화재 발생 / 설비 동작` → `화재 발생 / 고장 발생 / 설비 동작`).
  - fa-sub: `{isFire ? '현장 확인...' : '설비 동작 감지...'}` → `{isFire ? '현장 확인...' : isFault ? '수신반 고장 신호 감지 · 정보성 알림 (단발 · 재발송 없음)' : '설비 동작 감지 · 정보성 알림 (단발 · 재발송 없음)'}`.
  - fa-ack 버튼 style color: `isFire ? '#b91c1c' : '#15803d'` → `isFire ? '#b91c1c' : isFault ? '#b45309' : '#15803d'`.
  - fa-note: `{isFire && <AlertTriangle size={15} />}` → `{(isFire || isFault) && <AlertTriangle size={15} />}`. (span 텍스트 삼항은 prod 도 isFire 기준 유지 — 변경 X.)
  lucide 는 이미 `Flame, Settings, AlertTriangle` import 됨(추가 X). LivePanelImage 는 Task 3 후 aspectClass/objectClass 지원 — FireAlarmPage 의 `imgClassName="h-full"` 사용은 기본값과 호환.

최종 전체 빌드 게이트 (tsc + vite 전량).
  </action>
  <verify>
    <automated>cd /Users/jongyupyoon/Documents/cbc7119-design/cha-bio-safety && [ "$(grep -c 'isFault' src/pages/FireAlarmPage.tsx)" -ge 6 ] && [ "$(grep -c '고장 발생' src/pages/FireAlarmPage.tsx)" -ge 1 ] && [ "$(grep -c 'f59e0b' src/pages/FireAlarmPage.tsx)" -ge 1 ] && npm run build</automated>
  </verify>
  <done>FireAlarmPage 에 fault(주황) variant 8분기 반영, `cd cha-bio-safety && npm run build` (tsc+vite) 전량 통과.</done>
</task>

</tasks>

<verification>
- 전체: `cd /Users/jongyupyoon/Documents/cbc7119-design/cha-bio-safety && npm run build` (tsc + vite build) exit 0.
- functions/·migrations/ 무변경: `git -C /Users/jongyupyoon/Documents/cbc7119-design status --short cha-bio-safety/functions cha-bio-safety/migrations` → 빈 출력.
- FloorPlanPage 무변경: `git -C /Users/jongyupyoon/Documents/cbc7119-design status --short cha-bio-safety/src/pages/FloorPlanPage.tsx` → 빈 출력.
- mw4 무회귀: DashboardPage 에 iOS grid 줄 `auto auto auto auto minmax(125px, 1fr)` 정확히 1회 + 데스크톱 오늘일정 `h-[126px]` 유지.
</verification>

<success_criteria>
- 신규 3파일(panelEvents.ts/PanelEventRow.tsx/FireAlarmHistoryPage.tsx) 통짜 cp 로 생성, prod 와 byte 동일.
- 공유 6파일(api.ts/App.tsx/InspectionPage.tsx/DashboardPage.tsx/LivePanelImage.tsx/FireAlarmPage.tsx) 의도 이식 완료, prod 최종 동작 재현.
- `npm run build` 통과 (wrangler 미호출).
- functions/·migrations/·FloorPlanPage.tsx 무변경, mw4 iOS 그리드/데스크톱 정렬 무회귀.
</success_criteria>

<output>
Create `.planning/quick/260704-io8-prod-design-ui-fault/260704-io8-SUMMARY.md` when done.
배포는 사용자 컨펌 후 `main` push → cbc7119-preview 자동. (이 워크트리 wrangler 금지.)
</output>
