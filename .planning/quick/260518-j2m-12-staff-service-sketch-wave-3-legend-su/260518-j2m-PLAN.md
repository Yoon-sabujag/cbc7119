---
phase: quick-260518-j2m
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cha-bio-safety/docs/redesign-context/12-staff-service/sketch/03-legend-summary-sketch.html
autonomous: true
requirements:
  - UI-SPEC-12-W3
  - UI-SPEC-12-§5.4
  - UI-SPEC-12-§5.5
  - UI-SPEC-12-§6.3
  - UI-SPEC-12-§3.5
  - UI-SPEC-12-§3.6
  - UI-SPEC-12-§7.1
  - UI-SPEC-12-§14-OQ
must_haves:
  truths:
    - "사용자가 11개 dot 범례 row 를 보고 duty 4종 + 카테고리 6종 + 반차 1종을 한눈에 분별할 수 있다"
    - "사용자가 4개 stat card (연차 잔여 / 제공식수 / 미사용식수 / 주말식대) 를 각자 다른 좌측 3px 색바로 구분할 수 있다"
    - "연차 잔여 카드가 임계치 분기 3 단계 (안전 / 주의 < 3일 / 위험 < 1일) 를 색바와 값 색으로 시각화한다"
    - "다크 + 라이트 frame 양쪽에서 동일 시각 강도 확인이 가능하다 (정규화 hex set 보존)"
    - "라벨 12px / 값 18px — 9·10·11 px 노안 마지노선 위반 0건"
  artifacts:
    - path: "cha-bio-safety/docs/redesign-context/12-staff-service/sketch/03-legend-summary-sketch.html"
      provides: "W3 sketch — legend row + 4종 summary stat cards + threshold matrix"
      contains: "data-theme=\"dark\", data-theme=\"light\", 11 legend dots, 4 main cards + 3 threshold variants"
  key_links:
    - from: "tokens.css (line 16~119)"
      to: "sketch :root + [data-theme]"
      via: "verbatim CSS variable copy (W1/W2 패턴 동일)"
      pattern: "--surface-page|--surface-raised|--status-warning-bar|--status-danger-bar"
    - from: "UI-SPEC §3.5 정규화 카테고리 hex"
      to: "legend dots + summary card 좌측 3px 바"
      via: "raw hex 인라인 (W2 verbatim pattern)"
      pattern: "#42d778|#8f42d7|#d78042|#d74242|#d7428c|#4244d7"
    - from: "UI-SPEC §6.3 stat card 룰"
      to: ".stat-card 의 box-shadow inset 3px 0 0 {hex} 또는 border-left"
      via: "좌측 3px 색바 패턴"
      pattern: "border-left: 3px solid|box-shadow.*inset 3px 0 0"
---

<objective>
W3 sketch deliverable — `03-legend-summary-sketch.html`. UI-SPEC §12 W3 row 가 요구하는 "범례 row + 4종 요약 카드" 의 시각 검증 sketch. Stat Card §6.3 룰 (좌측 3px 색바 + 위험 임계치 색 분기) 강제. 다크 + 라이트 393px 프레임 + 임계치 분기 mini-strip.

Purpose: W2 정규화 카테고리 hex set 을 사용해 legend / summary cards 의 색·typography·임계치 분기를 사용자 컨펌 받음. TSX 변환 (StaffServicePage source line 800~840) 의 색·라벨·값 사이즈를 본 sketch 가 source-of-truth 로 고정.

Output: 1 sketch HTML 파일. src/ 일체 미수정.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@cha-bio-safety/docs/redesign-context/12-staff-service/UI-SPEC.md
@cha-bio-safety/docs/redesign-context/12-staff-service/tokens.css
@cha-bio-safety/docs/redesign-context/12-staff-service/typography.css
@cha-bio-safety/docs/redesign-context/12-staff-service/sketch/01-mobile-shell-sketch.html
@cha-bio-safety/docs/redesign-context/12-staff-service/sketch/02-calendar-grid-sketch.html

<interfaces>
<!-- tokens.css 의 :root 블록 (line 13~119 dark + light, line 124~146 spacing, line 167~172 radius). -->
<!-- W1/W2 sketch 가 verbatim copy 한 패턴을 그대로 따른다. alias 블록 (line 177~197) 은 절대 복사 금지. -->

W3 에서 사용하는 토큰:
- --surface-page, --surface-raised, --surface-sunken
- --text-primary, --text-secondary, --text-tertiary
- --border-default, --border-strong
- --accent
- --status-warning, --status-warning-bar, --status-warning-bg
- --status-danger, --status-danger-bar, --status-danger-bg
- --duty-day, --duty-night, --duty-off, --duty-leave
- --card-padding (모바일 14px / 데스크톱 10px — 본 sketch 는 모바일만)
- --card-gap, --page-padding, --section-gap
- --radius-md (12px), --radius-sm (8px), --radius-pill (99px)

NOTE: --status-info / --status-info-bar / --status-info-bg / --status-safe / --status-safe-bar / --status-safe-bg 토큰은 본 sketch 에서 사용 안 함 (legend 의 cyan #06b6d4 은 status-info 가 아닌 식단 카테고리 hex — UI-SPEC §3.6).

NOTE: --status-fire / --status-fire-bar / --status-fire-bg / --text-link / --text-disabled / --text-on-accent 토큰도 본 sketch 에서 사용 안 함. UI-SPEC §3.4.1 룰로 status-fire 는 StaffServicePage 전체 미사용.

W2 verbatim pattern (sketch/02-calendar-grid-sketch.html line 9~163):
```css
:root,
[data-theme="dark"] {
  --surface-page: #0a0d12;
  --surface-raised: #1a1f27;
  /* ... line 16~63 verbatim ... */
}
[data-theme="light"] {
  --surface-page: #ffffff;
  /* ... line 68~110 verbatim ... */
}
:root {
  /* spacing line 124~146 + radius line 167~172 verbatim */
}
```

StaffServicePage.tsx source (line 800~840) 의 OLD 패턴 — 본 sketch 가 교체:
- legend 색: `var(--c-night)` alias / `#22c55e` 구 hex → 본 sketch 는 `--duty-night` / `#42d778` (정규화)
- legend 라벨: 10px text-tertiary → 12px text-tertiary (노안 룰)
- summary card 라벨: 9px → 12px font-semibold (노안 룰)
- summary card 값: 14px font-extrabold → 18px font-extrabold (노안 룰)
- summary card 패턴: 카테고리 hex 의 `bg`/`bd` rgba 인라인 + `color: hex` (전체 카드 색칠) → 본 sketch §6.3 stat card 룰 (좌측 3px 색바만 색, 카드 surface 는 `--surface-raised`, 값 색은 `--text-primary` 기본 + 임계치 분기만 status 색)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 03-legend-summary-sketch.html 작성 (verbatim token block + legend row + 4 summary cards + threshold mini-strip + rules)</name>
  <files>cha-bio-safety/docs/redesign-context/12-staff-service/sketch/03-legend-summary-sketch.html</files>
  <action>
W1/W2 sketch 패턴을 그대로 따라 단일 HTML 파일을 작성한다. 작업 순서:

────────────────────────────────────────
A. HTML 골격 + `<style>` 블록 (W1/W2 verbatim copy)
────────────────────────────────────────

1. `<!DOCTYPE html>` + `<html lang="ko">` + `<head>` 에 charset / viewport / title (`12 연차 및 식사 — sketch W3 범례 + 요약 카드`) / Pretendard CDN `<link>` (W1 line 7 verbatim).

2. `<style>` 블록 안에:

   a) **다크 모드 :root, [data-theme="dark"]** — tokens.css line 16~69 verbatim (W2 sketch line 11~63 그대로 복사). 주의:
      - `--status-fire`, `--status-fire-bar`, `--status-fire-bg` 3 토큰은 의도적 누락 (UI-SPEC §3.4.1 룰, W2 와 동일).
      - 주석 "긴급/조치-대기 톤은 의도적 누락 — 이 페이지(StaffServicePage)는 §3.4.1 룰로 미사용" 유지.

   b) **라이트 모드 [data-theme="light"]** — tokens.css line 74~119 verbatim (W2 sketch line 68~110 그대로). 동일하게 fire 3 토큰 누락.

   c) **Spacing :root 블록** — tokens.css line 124~146 verbatim + `@media (min-width: 768px)` 블록 line 149~162 verbatim (W2 sketch line 115~153 그대로). 본 sketch 는 모바일만이지만 토큰 블록은 항상 W1/W2 와 동일하게 양쪽 다 포함.

   d) **Radius :root** — tokens.css line 167~172 verbatim (W2 sketch line 158~163).

   e) **sketch 외곽 body** — W1 line 168~188 verbatim:
      - `body { padding: 32px; background: #0f1218; ... }` (raw hex 예외 — 페이지 외곽 만)
      - `h1 { font-size: 18px; font-weight: 700; }`
      - `p.lead { font-size: 13px; color: var(--text-tertiary); max-width: 860px; }`

   f) **`.frame-row`** — W1 line 193~203 verbatim (`display: flex; flex-wrap: wrap; gap: 32px; align-items: flex-start;` + `.vp-label`).

   g) **`.frame-mobile`** — W1 line 208~218 verbatim (393px, raw hex `#2a2f3a` frame border, `border-radius: 12px`, `overflow: hidden`, `position: relative`). W3 frame 안 chrome 없음 (W2 와 동일 패턴).

   h) **`.legend-summary-region`** (W2 의 `.calendar-region` 자리에 대응) — `padding: 16px; background: var(--surface-page);`. 본 sketch 의 두 region (legend + summary cards) 컨테이너.

────────────────────────────────────────
B. legend row CSS (UI-SPEC §5.4)
────────────────────────────────────────

   `.legend-row` 클래스:
   ```css
   .legend-row {
     display: flex;
     flex-wrap: wrap;
     gap: 8px 12px;       /* row-gap 8 / column-gap 12 */
     align-items: center;
     padding: 6px 0 0;    /* source 의 6px 12px 0 — frame padding 16 안에 들어가므로 좌우 0 */
   }
   .legend-item {
     display: flex;
     align-items: center;
     gap: 6px;            /* dot ↔ label */
   }
   .legend-dot {
     width: 12px;
     height: 12px;
     border-radius: 50%;
     flex-shrink: 0;
     display: inline-block;
   }
   .legend-label {
     font-size: 12px;
     line-height: 1;      /* leading-none — 작은 컨테이너 안 caption 룰 */
     font-weight: 400;
     color: var(--text-tertiary);
   }

   /* dot 색 — duty 4종 + 카테고리 6종 + 반차 1종 */
   .dot-duty-night  { background: var(--duty-night); }
   .dot-duty-off    { background: var(--duty-off); }
   .dot-duty-day    { background: var(--duty-day); }
   .dot-duty-leave  { background: var(--duty-leave); }
   .dot-cat-annual  { background: #42d778; }
   .dot-cat-official{ background: #8f42d7; }
   .dot-cat-condol  { background: #d78042; }
   .dot-cat-sick    { background: #d74242; }
   .dot-cat-health  { background: #d7428c; }
   .dot-cat-other   { background: #4244d7; }
   .dot-half        { background: linear-gradient(135deg, #42d778 50%, var(--duty-day) 50%); }
   ```

   주의:
   - 12×12 (모바일) 단일 — 데스크톱 분기 없음 (본 sketch 는 모바일만).
   - 반차 그라디언트: 연차 #42d778 ↔ duty-day (W2 verbatim 의 bg-half-am-annual 패턴 그대로).
   - 라벨 12px font-normal leading-none (작은 컨테이너 안 caption 룰 — feedback_text_caption_leading_none).

────────────────────────────────────────
C. summary cards CSS (UI-SPEC §5.5 + §6.3 stat card 룰)
────────────────────────────────────────

   `.summary-cards` 클래스:
   ```css
   .summary-cards {
     display: flex;
     gap: var(--card-gap);    /* 8 모바일 */
     overflow-x: auto;
     -webkit-overflow-scrolling: touch;
     padding-bottom: 4px;     /* scroll bar / focus halo 여유 */
     margin-top: 16px;        /* legend ↔ summary 분리 */
   }
   .stat-card {
     flex: 1 0 0;
     min-width: 84px;         /* 4개 컬럼 393-16*2=361 / 4 ≈ 90, gap 감안 84 */
     background: var(--surface-raised);
     border: 1px solid var(--border-default);
     border-radius: var(--radius-md);
     padding: var(--card-padding);  /* 14 모바일 */
     position: relative;
     overflow: hidden;
     /* §6.3 좌측 3px 색바 — box-shadow inset 으로 구현 (padding 손상 없음) */
   }
   .stat-card .stat-label {
     font-size: 12px;
     line-height: 1;
     font-weight: 600;
     color: var(--text-tertiary);
     margin-bottom: 6px;
     white-space: nowrap;
   }
   .stat-card .stat-value {
     font-size: 18px;
     line-height: 1;
     font-weight: 800;
     color: var(--text-primary);
     white-space: nowrap;
     letter-spacing: -0.01em;
   }

   /* 카드별 좌측 3px 색바 — box-shadow inset (W3 적용) */
   .bar-cat-annual   { box-shadow: inset 3px 0 0 #42d778; }
   .bar-meal-A       { box-shadow: inset 3px 0 0 #06b6d4; }
   .bar-meal-B       { box-shadow: inset 3px 0 0 #d7428c; }
   .bar-weekend      { box-shadow: inset 3px 0 0 #8f42d7; }

   /* 임계치 분기 — 연차 잔여 카드 전용 */
   .bar-warning      { box-shadow: inset 3px 0 0 var(--status-warning-bar); }
   .bar-danger       { box-shadow: inset 3px 0 0 var(--status-danger-bar); }
   .stat-value-warning { color: var(--status-warning); }
   .stat-value-danger  { color: var(--status-danger); }
   ```

   주의:
   - source 가 `bg: rgba(34,197,94,0.1)` 전체 카드 색칠 패턴이었던 것 → 본 sketch §6.3 룰로 카드 surface 는 `--surface-raised` 단일, 색은 좌측 3px 바에만 표현.
   - 값 색 = `--text-primary` 기본 (source 의 `color: c.color` 전체 컬러 → 폐기). 임계치 분기 (연차 잔여 < 3 / < 1) 에서만 값 색이 warning/danger 토큰 색.
   - label 은 항상 `--text-tertiary` (source 와 동일).
   - 좌측 3px 바는 `box-shadow: inset` 으로 구현 — padding 손상 없음, 카드 코너 radius 와 함께 깔끔하게 잘림.
   - 4개 카드 가로 합산 393-16*2 = 361. 카드 4개 + gap 3개*8 = 24. 카드당 (361-24)/4 ≈ 84.25. min-width 84 안전.

────────────────────────────────────────
D. threshold mini-strip CSS (연차 잔여 임계치 분기 데모)
────────────────────────────────────────

   `.threshold-strip` 클래스 — 다크/라이트 frame 하단 별도 영역으로, 안전/주의/위험 3 카드 가로 배치:
   ```css
   .threshold-section {
     margin-top: 40px;
     max-width: 1100px;
   }
   .threshold-section h2 {
     font-size: 16px;
     color: var(--text-primary);
     margin: 0 0 12px 0;
     font-weight: 700;
   }
   .threshold-section p.threshold-lead {
     font-size: 13px;
     color: var(--text-tertiary);
     margin: 0 0 16px 0;
   }
   .threshold-grid {
     display: grid;
     grid-template-columns: repeat(3, minmax(140px, 1fr));
     gap: 12px;
     padding: 16px;
     border-radius: 12px;
   }
   .threshold-grid.dark {
     background: var(--surface-page);
     border: 1px solid #2a2f3a;
     margin-bottom: 16px;
   }
   .threshold-grid.light {
     background: #ffffff;
     border: 1px solid #d0d7de;
   }
   .threshold-card-wrap {
     display: flex;
     flex-direction: column;
     gap: 6px;
   }
   .threshold-card-wrap .stat-card {
     min-width: 0;
   }
   .threshold-card-wrap .annotation {
     font-size: 12px;
     line-height: 1.35;
     color: var(--text-secondary);
     font-weight: 600;
   }
   ```

   주의:
   - W2 sketch 의 `.matrix-section` / `.matrix-grid` 패턴 verbatim 변형. 다크/라이트 두 grid 분리.
   - 다크 grid bg = `--surface-page`, 라이트 grid bg = `#ffffff` (raw hex 예외 — W2 와 동일).

────────────────────────────────────────
E. rules 박스 CSS — W1/W2 verbatim
────────────────────────────────────────

   ```css
   .rules {
     margin-top: 40px;
     padding: 16px 20px;
     background: rgba(255, 255, 255, 0.03);
     border: 1px solid var(--border-default);
     border-radius: 8px;
     max-width: 860px;
   }
   .rules h2 { font-size: 16px; margin: 0 0 6px 0; color: var(--text-primary); }
   .rules h3 { font-size: 14px; margin: 16px 0 6px 0; color: var(--text-primary); }
   .rules p  { font-size: 13px; color: var(--text-tertiary); margin: 0 0 10px 0; }
   .rules ul, .rules ol { margin: 0; padding-left: 20px; font-size: 13px; color: var(--text-secondary); }
   .rules li { margin-bottom: 4px; }
   ```

────────────────────────────────────────
F. `<body>` 마크업
────────────────────────────────────────

1. `<h1>12 · 연차 및 식사 — sketch W3 범례 + 요약 카드</h1>`

2. `<p class="lead">` — W3 검증 포인트 설명:
   "W3 검증 포인트 (UI-SPEC §12): 범례 row (duty 4 + 카테고리 6 + 반차 1 = 11 dot) + 4종 요약 stat card (연차 잔여 / 제공식수 / 미사용식수 / 주말식대) + §6.3 stat card 룰 (좌측 3px 색바 + 위험 임계치 색 분기). 다크 + 라이트 양쪽 + 연차 잔여 임계치 3 단계 mini-strip 으로 시각 검증."

3. `<div class="frame-row">` 안:

   **VP1 다크 frame:**
   ```html
   <div>
     <div class="vp-label">VP1 · 모바일 다크 · 393</div>
     <div class="frame-mobile" data-theme="dark">
       <div class="legend-summary-region">
         <!-- legend row -->
         <div class="legend-row">
           <!-- 11 dots, 순서: 당직 / 비번 / 주간 / 휴무 / 연차 / 공가 / 경조 / 병가 / 보건 / 기타 / 반차 -->
           <div class="legend-item"><span class="legend-dot dot-duty-night"></span><span class="legend-label">당직</span></div>
           <div class="legend-item"><span class="legend-dot dot-duty-off"></span><span class="legend-label">비번</span></div>
           <div class="legend-item"><span class="legend-dot dot-duty-day"></span><span class="legend-label">주간</span></div>
           <div class="legend-item"><span class="legend-dot dot-duty-leave"></span><span class="legend-label">휴무</span></div>
           <div class="legend-item"><span class="legend-dot dot-cat-annual"></span><span class="legend-label">연차</span></div>
           <div class="legend-item"><span class="legend-dot dot-cat-official"></span><span class="legend-label">공가</span></div>
           <div class="legend-item"><span class="legend-dot dot-cat-condol"></span><span class="legend-label">경조</span></div>
           <div class="legend-item"><span class="legend-dot dot-cat-sick"></span><span class="legend-label">병가</span></div>
           <div class="legend-item"><span class="legend-dot dot-cat-health"></span><span class="legend-label">보건</span></div>
           <div class="legend-item"><span class="legend-dot dot-cat-other"></span><span class="legend-label">기타</span></div>
           <div class="legend-item"><span class="legend-dot dot-half"></span><span class="legend-label">반차</span></div>
         </div>

         <!-- summary cards (안전 set — 연차 잔여 8.5/15일) -->
         <div class="summary-cards">
           <div class="stat-card bar-cat-annual">
             <div class="stat-label">연차 잔여</div>
             <div class="stat-value">8.5/15일</div>
           </div>
           <div class="stat-card bar-meal-A">
             <div class="stat-label">제공식수</div>
             <div class="stat-value">42끼</div>
           </div>
           <div class="stat-card bar-meal-B">
             <div class="stat-label">미사용식수</div>
             <div class="stat-value">8끼</div>
           </div>
           <div class="stat-card bar-weekend">
             <div class="stat-label">주말식대</div>
             <div class="stat-value">₩48,500</div>
           </div>
         </div>
       </div>
     </div>
   </div>
   ```

   **VP2 라이트 frame:** 위와 동일한 마크업, `data-theme="light"` + `vp-label`만 "VP2 · 모바일 라이트 · 393" 으로 교체.

4. `<div class="threshold-section">`:
   ```html
   <h2>연차 잔여 카드 — 임계치 분기 3 단계 (§6.3)</h2>
   <p class="threshold-lead">
     안전 (≥ 3일) → 카테고리 hex (#42d778) 색바, 값 text-primary.
     주의 (< 3일) → status-warning-bar 색바 + status-warning 값.
     위험 (< 1일) → status-danger-bar 색바 + status-danger 값.
     식수/식대 3 카드는 임계치 분기 없음 (단순 정보, source-of-truth 에 룰 정의 없음).
   </p>

   <!-- 다크 -->
   <div class="threshold-grid dark" data-theme="dark">
     <div class="threshold-card-wrap">
       <div class="stat-card bar-cat-annual">
         <div class="stat-label">연차 잔여</div>
         <div class="stat-value">8.5/15일</div>
       </div>
       <div class="annotation">안전 — 8.5일 (≥ 3)</div>
     </div>
     <div class="threshold-card-wrap">
       <div class="stat-card bar-warning">
         <div class="stat-label">연차 잔여</div>
         <div class="stat-value stat-value-warning">2/15일</div>
       </div>
       <div class="annotation">주의 — 2일 (&lt; 3)</div>
     </div>
     <div class="threshold-card-wrap">
       <div class="stat-card bar-danger">
         <div class="stat-label">연차 잔여</div>
         <div class="stat-value stat-value-danger">0.5/15일</div>
       </div>
       <div class="annotation">위험 — 0.5일 (&lt; 1)</div>
     </div>
   </div>

   <!-- 라이트 -->
   <div class="threshold-grid light" data-theme="light">
     <!-- 동일 3 카드 -->
   </div>
   ```

   라이트 grid 도 동일 3 카드 마크업 (annotation 텍스트 포함).

5. `<div class="rules">` 박스 (W1/W2 패턴 — 마지막에):
   ```html
   <h2>적용 룰 — W3 범례 + 요약 카드</h2>
   <p>UI-SPEC §5.4 legend + §5.5 stat card + §6.3 stat card 룰 (좌측 3px 색바 + 임계치 분기) + §3.5 정규화 카테고리 hex + §3.6 식단 카테고리 hex 단일 source.</p>

   <h3>legend row 룰 (§5.4)</h3>
   <ul>
     <li>11 dots = duty 4 (당직/비번/주간/휴무) + 카테고리 6 (연차/공가/경조/병가/보건/기타) + 반차 1 — 합계 11개</li>
     <li>dot 12×12 (모바일) — source 의 isDesktop 분기는 본 sketch 에서 모바일 단일</li>
     <li>반차 dot = linear-gradient(135deg, #42d778 50%, var(--duty-day) 50%) — W2 calendar 의 bg-half-am-annual 과 동일 패턴</li>
     <li>라벨 = text-caption 12px font-normal leading-none color text-tertiary (source 의 10px → 12px 노안 룰 강제)</li>
     <li>dot ↔ label gap 6px / dot ↔ dot gap 12px / row-gap 8px wrap</li>
   </ul>

   <h3>summary cards 룰 (§5.5 + §6.3)</h3>
   <ul>
     <li>카드 4종 — 연차 잔여 / 제공식수 / 미사용식수 / 주말식대</li>
     <li>카드 surface = var(--surface-raised) 단일 — source 의 카테고리 색 rgba(.., 0.1) 전체 카드 색칠 폐기</li>
     <li>좌측 3px 색바 = box-shadow inset 3px 0 0 {hex} — §6.3 룰. padding 손상 없음, radius 와 함께 깔끔하게 잘림</li>
     <li>카드 1 (연차 잔여) 색바 = #42d778 (정규화 연차 hex) — 안전 set</li>
     <li>카드 2 (제공식수) 색바 = #06b6d4 (식단 cyan, §3.6 중식 A 코너)</li>
     <li>카드 3 (미사용식수) 색바 = #d7428c (정규화 보건 / 식단 B 코너 §3.6 — source 의 #ec4899 폐기)</li>
     <li>카드 4 (주말식대) 색바 = #8f42d7 (정규화 공가 hex — UI-SPEC §5.5 결정. source 의 #f97316 폐기. §5.5 본문상 주말식대 카테고리 색은 보라 #a855f7 였으나 §3.5 정규화 set 적용으로 #8f42d7 로 매핑)</li>
     <li>라벨 = text-caption 12px font-semibold leading-none color text-tertiary (source 9px → 12px 노안 룰)</li>
     <li>값 = 18px font-extrabold leading-none color text-primary letter-spacing -0.01em white-space nowrap (source 14px → 18px 노안 룰. 본 sketch 는 18px 권장. text-display 28 은 4 카드 한 row 에서 폭 부족)</li>
     <li>카드 padding = var(--card-padding) = 14px (모바일)</li>
     <li>카드 radius = var(--radius-md) = 12px / border = 1px solid var(--border-default)</li>
   </ul>

   <h3>임계치 분기 — 연차 잔여 전용 (§6.3)</h3>
   <ul>
     <li>안전 (≥ 3일) → 색바 #42d778 / 값 text-primary</li>
     <li>주의 (&lt; 3일) → 색바 var(--status-warning-bar) / 값 var(--status-warning)</li>
     <li>위험 (&lt; 1일) → 색바 var(--status-danger-bar) / 값 var(--status-danger)</li>
     <li>식수/식대 3 카드는 임계치 분기 없음 — 단순 정보. UI-SPEC §5.5 / §6.3 본문에 임계치 룰 정의 없음.</li>
   </ul>

   <h3>typography 룰 (UI-SPEC §7.1 + 노안 룰)</h3>
   <ul>
     <li>9 · 10 · 11 px 0건 — typography.css §1.1 노안 마지노선 강제</li>
     <li>라벨 12px (legend / stat-label) — 작은 컨테이너 안 caption 룰로 leading-none</li>
     <li>값 18px (stat-value) — text-title 18 letter-spacing -0.01em font-weight 800</li>
     <li>source line 800~840 의 10px / 9px / 14px 모두 폐기</li>
   </ul>

   <h3>색 룰 (§3.4.1 / §3.5 / §3.6)</h3>
   <ul>
     <li>긴급/조치-대기 status (status-fire 변형 4종) 0건 — §3.4.1 룰로 본 페이지 미사용. 토큰 블록에서도 의도적 누락 (W1/W2 와 동일).</li>
     <li>duty 4 토큰 — legend dot 의 4개 (--duty-day/night/off/leave) 에만 사용</li>
     <li>카테고리 6 hex (정규화) — legend dot + 연차 잔여 카드 색바 + 미사용식수 카드 색바 + 주말식대 카드 색바</li>
     <li>식단 cyan #06b6d4 — 제공식수 카드 색바 1회 등장 (§3.6 중식 A 코너 hex)</li>
     <li>status-warning / status-danger 토큰 — 연차 잔여 임계치 분기 2종만</li>
   </ul>

   <h3>source-of-truth 매핑 (TSX 변환 시)</h3>
   <ul>
     <li>StaffServicePage.tsx line 800~824 (legendRow): alias var(--c-night/off/day/leave) + 옛 hex (#22c55e / #a855f7 / #f97316 / #ef4444 / #ec4899 / #6366f1) → 본 sketch 의 정규화 set 으로 교체</li>
     <li>StaffServicePage.tsx line 826~840 (summaryCards): 카테고리 hex 의 bg rgba(0.1) 전체 카드 색칠 + bd rgba(0.25) border + color hex 값 → 본 sketch §6.3 패턴 (surface-raised + box-shadow inset 3px 색바 + text-primary 값) 으로 전환</li>
     <li>주말식대 색 = 정규화 공가 #8f42d7 (§5.5 본문상 보라 hex #a855f7 였던 것 → 정규화 후 #8f42d7. source 의 #f97316 (경조 hex 와 충돌) 폐기)</li>
     <li>미사용식수 색 = 정규화 보건 #d7428c (식단 B 코너 §3.6 과 의도된 일치. source 의 #ec4899 (보건 옛 hex 와 충돌) 폐기)</li>
     <li>연차 잔여 임계치 분기 룰 신규 추가 — source 는 분기 없이 단일 #22c55e 였음. §6.3 stat card 룰 적용으로 < 3 / < 1 분기 도입</li>
   </ul>

   <h3>negative gate (verify grep — 본 sketch 검증)</h3>
   <ul>
     <li>이모지 0건</li>
     <li>9 · 10 · 11 px 0건 (font-size 9 / 10 / 11 px 패턴)</li>
     <li>alias 토큰 (--bg / --bg2 / --bg3 / --bd / --bd2 / --t1 / --t2 / --t3 / --acl / --c-day / --c-night / --c-off / --c-leave) 0건</li>
     <li>status-fire / text-fire / bg-fire 0건 (§3.4.1)</li>
     <li>옛 카테고리 hex (#22c55e / #a855f7 / #f97316 / #ef4444 / #ec4899 / #6366f1) — :root 토큰 정의 블록 (line 47 / 49 / 53 / 60 즈음 — --status-safe-bar / --status-danger-bar / --duty-night) 안 예외. 그 외 0건.</li>
     <li>정규화 hex 6종 (#42d778 / #8f42d7 / #d78042 / #d74242 / #d7428c / #4244d7) 각 ≥ 1회</li>
     <li>다크 + 라이트 frame 양쪽 (data-theme="dark" 와 data-theme="light") 존재</li>
     <li>11 legend dot (4 duty + 6 카테고리 + 1 반차) — `class="legend-dot ` 가 frame 당 11회</li>
     <li>4 main summary card (각 frame) + 3 threshold variant card (각 grid) — 최소 합계 14 stat-card</li>
   </ul>

   <h3>다음 wave</h3>
   <ul>
     <li>W4 — 04-menu-cards-sketch.html (식단 3종 + PDF dropzone + 주말식대 자세히 — 본 W3 의 주말식대 카드는 요약 row 의 일부, W4 에서 region.weekend-allowance 자체를 다룸)</li>
     <li>W5 — 05-bottomsheet-sketch.html (BottomSheet 전체)</li>
   </ul>
   ```

────────────────────────────────────────
G. 정합 체크 (작성 중 / 후)
────────────────────────────────────────

작성 직후 grep 으로 다음을 모두 확인 (Bash):

```bash
F=cha-bio-safety/docs/redesign-context/12-staff-service/sketch/03-legend-summary-sketch.html

# 1. 이모지 0건
python3 -c "import re,sys; c=open('$F').read(); m=re.findall(r'[\U0001F300-\U0001FAFF\U00002600-\U000027BF]', c); print('emoji count:', len(m))"

# 2. 9·10·11 px 0건 — font-size 9px / 10px / 11px 패턴 0
grep -E 'font-size:\s*(9|10|11)px' "$F" | wc -l   # 기대값 0

# 3. alias 토큰 0건
grep -E 'var\(--(bg|bg2|bg3|bg4|bd|bd2|t1|t2|t3|acl|c-day|c-night|c-off|c-leave|safe|warn|danger|info|fire)\)' "$F" | wc -l  # 기대값 0

# 4. status-fire / text-fire / bg-fire 0건
grep -E 'status-fire|text-fire|bg-fire' "$F" | wc -l   # 기대값 0

# 5. 옛 카테고리 hex 0건 (단, :root 토큰 정의 블록은 W2 verbatim 인용이므로 status-safe-bar #22c55e / status-danger-bar #ef4444 / duty-night #ef4444 — line 47 ~ 60 사이 예외).
#    body 안 raw hex 인라인 사용에서만 옛 hex 가 등장하면 안 됨. 본 sketch 는 카테고리 색을 정규화 set 으로만 인라인 → 옛 hex 인라인 0 기대.
grep -nE '#(22c55e|a855f7|f97316|ec4899|6366f1)' "$F"
# → 결과는 line 47 (--status-safe-bar: #22c55e), line 53 (--status-danger-bar: #ef4444 — 단 ef4444 위 grep 에 없음), line 60 (--duty-night: #ef4444) 정도의 토큰 정의 라인만 나오면 OK.
# 본 grep 에 안 들어간 #ef4444 도 별도 확인:
grep -nE '#ef4242|#ef4444' "$F"
# 토큰 정의 블록 안 (다크 line 49 즈음 --status-danger-bar / line 60 --duty-night, 라이트 블록도 raw hex 다르므로 다크만 해당) 만 나와야 OK.

# 6. 정규화 hex 6종 각 ≥ 1회
for h in 42d778 8f42d7 d78042 d74242 d7428c 4244d7; do
  echo -n "#$h: "; grep -c "#$h" "$F"
done
# 6개 모두 ≥ 1

# 7. 다크 + 라이트 frame 양쪽
grep -c 'data-theme="dark"' "$F"   # ≥ 2 (frame-mobile dark + threshold-grid dark)
grep -c 'data-theme="light"' "$F"  # ≥ 2

# 8. legend dot 11 (frame 당)
grep -c 'class="legend-dot' "$F"   # ≥ 22 (2 frame × 11)

# 9. stat-card 최소 14 (4 main × 2 frame + 3 threshold × 2 grid = 14)
grep -cE 'class="stat-card' "$F"   # ≥ 14

# 10. valid HTML — 최소 DOCTYPE 확인
head -1 "$F"  # "<!DOCTYPE html>" 으로 시작
```

위 10개 grep 게이트 모두 통과 후 완료.

주의:
- 위 grep 의 #5 에서 #ef4444 가 토큰 블록 (다크 --status-danger-bar / --duty-night) 에 등장. 본 sketch 가 W1/W2 와 동일하게 tokens.css verbatim 인용이므로 예외 OK. body 안 raw hex 인라인에 #ef4444 등장은 금지 (정규화 set 의 병가 = #d74242 사용).
- threshold-grid.light bg = #ffffff / border = #d0d7de 는 W2 verbatim 예외 (light matrix bg). 본 sketch 도 동일하게 인용.
- frame-mobile border = #2a2f3a / body bg = #0f1218 도 W1/W2 verbatim 예외.

────────────────────────────────────────
H. 파일 작성 방식
────────────────────────────────────────

Write tool 로 단일 HTML 파일 생성. heredoc / cat 금지. 파일 작성 후 위 grep 게이트 10개를 Bash 로 모두 실행해 통과 확인.
  </action>
  <verify>
    <automated>
F=/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/12-staff-service/sketch/03-legend-summary-sketch.html
test -f "$F" && \
python3 -c "import re,sys; c=open('$F').read(); m=re.findall(r'[\U0001F300-\U0001FAFF\U00002600-\U000027BF]', c); sys.exit(0 if len(m)==0 else 1)" && \
[ "$(grep -E 'font-size:\s*(9|10|11)px' "$F" | wc -l | tr -d ' ')" = "0" ] && \
[ "$(grep -cE 'var\(--(bg|bg2|bg3|bg4|bd|bd2|t1|t2|t3|acl|c-day|c-night|c-off|c-leave|safe|warn|danger|info|fire)\)' "$F" | tr -d ' ')" = "0" ] && \
[ "$(grep -cE 'status-fire|text-fire|bg-fire' "$F" | tr -d ' ')" = "0" ] && \
[ "$(grep -c '#42d778' "$F")" -ge 1 ] && \
[ "$(grep -c '#8f42d7' "$F")" -ge 1 ] && \
[ "$(grep -c '#d78042' "$F")" -ge 1 ] && \
[ "$(grep -c '#d74242' "$F")" -ge 1 ] && \
[ "$(grep -c '#d7428c' "$F")" -ge 1 ] && \
[ "$(grep -c '#4244d7' "$F")" -ge 1 ] && \
[ "$(grep -c 'data-theme="dark"' "$F")" -ge 2 ] && \
[ "$(grep -c 'data-theme="light"' "$F")" -ge 2 ] && \
[ "$(grep -c 'class="legend-dot' "$F")" -ge 22 ] && \
[ "$(grep -cE 'class="stat-card' "$F")" -ge 14 ] && \
head -1 "$F" | grep -q "<!DOCTYPE html>" && \
echo "ALL GATES PASSED"
    </automated>
  </verify>
  <done>
    파일 `cha-bio-safety/docs/redesign-context/12-staff-service/sketch/03-legend-summary-sketch.html` 존재.
    10개 grep 게이트 모두 통과 (이모지 0 / 9·10·11 px 0 / alias 0 / fire 0 / 정규화 hex 6종 ≥ 1 / dark·light frame ≥ 2 / legend dot ≥ 22 / stat-card ≥ 14 / DOCTYPE 시작).
    다크 + 라이트 frame 양쪽에서 legend row + 4 main summary card + 3 threshold variant card 가시.
  </done>
</task>

</tasks>

<verification>
**자체 검수 — 작업 완료 직후 확인:**

1. **Output 파일 존재:**
   - `cha-bio-safety/docs/redesign-context/12-staff-service/sketch/03-legend-summary-sketch.html` 존재 (test -f)
   - src/ 의 어떤 파일도 수정되지 않음 (git status — sketch HTML 한 파일만 변경)
   - 다른 sketch 파일 (01, 02) 수정되지 않음

2. **negative gate (grep verify) 10개 모두 통과:**
   - 이모지 0건
   - 9 · 10 · 11 px font-size 0건
   - alias var(--bg / --t1 / --c-day 등) 0건
   - status-fire / text-fire / bg-fire 0건
   - 정규화 카테고리 hex 6종 (#42d778 / #8f42d7 / #d78042 / #d74242 / #d7428c / #4244d7) 각 ≥ 1
   - data-theme="dark" ≥ 2 / data-theme="light" ≥ 2
   - class="legend-dot" ≥ 22 (2 frame × 11 dot)
   - class="stat-card" ≥ 14 (2 frame × 4 + 2 grid × 3)
   - DOCTYPE html 로 시작

3. **시각 정합 (browser 열어 사용자 컨펌 대기):**
   - 다크 frame 에서 legend dot 11개가 한 row (또는 wrap 2 row) 안에 표시
   - 다크 frame 에서 4 stat card 가 좌측 3px 색바 + 값 18px 으로 명확히 구분
   - 임계치 분기 mini-strip 에서 안전 (#42d778 / text-primary) / 주의 (warning-bar / warning) / 위험 (danger-bar / danger) 3 단계가 다르게 표시
   - 라이트 frame 에서 동일 시각 강도 (정규화 hex set 의 라이트 호환성 확인)
   - 라벨 / 값 / 색바 / 카드 surface 의 §6.3 룰 패턴이 일관

4. **HTML 유효성:**
   - `head -1` 이 `<!DOCTYPE html>` 으로 시작
   - 단일 self-contained 파일 (외부 의존성은 Pretendard CDN 1개만)
</verification>

<success_criteria>
- [x] `03-legend-summary-sketch.html` 작성 완료
- [x] 10개 grep 게이트 모두 통과
- [x] 다크 + 라이트 frame 양쪽에서 legend (11 dot) + summary cards (4 카드) 가시
- [x] 임계치 분기 mini-strip 3 카드 × 2 grid 가시
- [x] §6.3 stat card 룰 (좌측 3px 색바 + 임계치 값 색 분기) 일관 적용
- [x] 라벨 12px / 값 18px 노안 룰 강제
- [x] 정규화 카테고리 hex set 인라인 사용 (옛 hex inline 0)
- [x] src/ 또는 다른 sketch 파일 미수정
- [x] 사용자 시각 컨펌 대기
</success_criteria>

<output>
완료 후 `.planning/quick/260518-j2m-12-staff-service-sketch-wave-3-legend-su/260518-j2m-SUMMARY.md` 생성 (quick mode summary 템플릿).
</output>
