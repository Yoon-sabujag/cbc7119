---
phase: 260526-b5t
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cha-bio-safety/src/pages/QRPrintPage.tsx
  - cha-bio-safety/src/styles/components.css
autonomous: true
requirements:
  - W5-checklist-§1-scope-2files-single-atomic
  - W5-checklist-§2-region-mapping-4
  - W5-checklist-§3-sketch-CSS-verbatim-fence
  - W5-checklist-§4-biz-anchor-12
  - W5-checklist-§5-OQ-LOCKED-5
  - W5-checklist-§6-sketch-class-cheatsheet
  - W5-checklist-§7-font-promotion-9-10-11-banned
  - W5-checklist-§8-lucide-3-required
  - W5-checklist-§9-components-css-add-≥10
  - W5-checklist-§10-tailwind-cheatsheet
  - W5-checklist-§11-negative-gate-17
  - W5-checklist-§12-self-verify-grep-28
tags:
  - redesign
  - 25-qr-print
  - tsx-conversion
  - v0.1.1
  - quick
  - 2files-atomic
  - components-css-new
  - lucide-chevron-left
  - lucide-download
  - lucide-printer
  - back-button-34x34
  - cta-gradient-anchor
  - public-solid-safe-bar

must_haves:
  truths:
    - "QRPrintPage.tsx + components.css **2 파일 단일 atomic commit** in-place 수정. `git diff --name-only HEAD~ HEAD` 에 `cha-bio-safety/src/pages/QRPrintPage.tsx` + `cha-bio-safety/src/styles/components.css` + 본 PLAN.md/SUMMARY.md 만 등장 (다른 파일 0). 28-splash (4i9) 2-파일 단일 atomic 패턴 (TSX + components.css 동시) 일부 mirror — 단 본 25-qr-print 는 components.css 신규 ≥10 class 추가 (18-worklog slq 의 신규 ≥10 패턴 mirror, 23-education/19-legal/20-legal-findings/21-legal-finding-detail 의 단일 파일 atomic 과 다름 — 카드/버튼/안내 박스 dedicated class 가 단순 클래스 인라인 보다 가독성 우수)."
    - "App.tsx + tailwind.config.js + tokens.css + typography.css + utils/api.ts + stores/authStore.ts + 모든 외부 의존 (jspdf / qrcode / react-hot-toast / react-router-dom / lucide-react 라이브러리 자체) 변경 0 byte. **6 src 파일 + tailwind.config.js 변경 0 byte 가드 = final verify gate**. 단, components.css 는 본 plan 의 **수정 대상** (신규 ≥10 class 추가, 기존 504 line → 514+ line)."
    - "변환 후 QRPrintPage.tsx 라인 수 330 ± 30 (300~360 예상). dlBtnStyle 헬퍼 함수 line 312~330 (19 lines) 폐기 + Tailwind class 치환 + 인라인 SVG ChevronLeft path line 258~260 (3 lines) 제거 + Lucide 4종 import 1 line 추가 + inspect 인라인 그라데이션 1 line 유지 (§6.4 정식 anchor) + components.css class 인라인 치환 → 총 라인 수 약간 압축 또는 거의 동일. components.css 라인 수 504 → 514~525 (신규 10~15 class 추가)."
    - "★ QRPrintPage 비즈 anchor 12건 1 byte 변경 0: (1) imports 6건 verbatim (line 1~6) — `useState` / `useNavigate` / `jsPDF from 'jspdf'` / `QRCode from 'qrcode'` / `useAuthStore` / `toast from 'react-hot-toast'`. **추가 1줄만 허용**: `import { ChevronLeft, Download, Printer } from 'lucide-react'` (OQ #3 LOCKED + 권장 2종). (2) CheckPoint interface 1 byte 0 (line 8~15) — id/locationNo?/location/floor/category/description? shape. (3) CATEGORIES 배열 7건 순서/value/label/hasPublic verbatim (line 17~25) — 소화기만 `hasPublic: true`, 나머지 6건 `false`. (4) renderCardCanvas async (line 29~119) — scale 기본 3 / PAD 3*S / FONT `-apple-system, \"Apple SD Gothic Neo\", \"Malgun Gothic\", \"Noto Sans KR\", sans-serif` / topFontSize 기본 12 / bottomFontSize 기본 13 / fillStyle `#ffffff` 배경 + `#222222` 상단 텍스트 + `#333333` 하단 텍스트 / ctx.textAlign `center` / ctx.textBaseline `top` / 첫 하단 라인 `bold ` prefix / QRCode.toCanvas margin 1 / maxQrW `Math.floor(W * 0.85)` / qrDrawSize `Math.min(Math.max(availH, 20 * S), maxQrW)` / return `canvas.toDataURL('image/png')`. (5) generatePdf async (line 122~219) — `if (points.length === 0) { toast.error('체크포인트가 없습니다'); return }` / `isLandscape = type === 'public'` / jsPDF `unit:'mm', format:'a4'` / PAGE_W portrait 210 landscape 297 / PAGE_H portrait 297 landscape 210 / MARGIN 10 / cardW inspect 30 public 70 / cardH inspect 38 public 90 / gap 1 / setFillColor 255/255/255 + rect 'FD' / setDrawColor 150/150/150 + setLineWidth 0.2 / addImage 'PNG' / setFillColor 0/0/0 + setLineWidth 0.3 + rect 'S' / inspect qrValue `cp.id` + qrSize `Math.floor(cardW * 0.65)` + bottomLines `[cp.locationNo ?? cp.id, cp.location, cp.floor]` + bottomFontSize 3 / public qrValue `${baseUrl}/e/${cp.id}` + topLines verbatim 3줄 + topFontSize 3 + bottomLines 동일 + bottomFontSize 3 / typeLabel `type === 'inspect' ? '점검용' : '점검확인용'` / doc.save `${categoryLabel}_${typeLabel}_QR.pdf`. (6) handleDownload async (line 229~250) — `key = \\`${category}-${type}\\`` / setBusy(key) / raw `fetch(\\`/api/checkpoints?category=${encodeURIComponent(category)}\\`)` + `headers: { Authorization: \\`Bearer ${token}\\` }` / json.success && json.data?.length / `catLabel = CATEGORIES.find(c => c.value === category)?.label ?? category` / `await generatePdf(json.data, type, baseUrl, catLabel)` / `toast.success('PDF 다운로드 완료')` / catch `toast.error('PDF 생성 오류')` + `console.error(e)` / finally `setBusy(null)`. (7) dlBtnStyle 3 state 매트릭스 — loading (`var(--bd2)` bg + `var(--t3)` color + cursor default) / public (lin-grad `16a34a→22c55e` 폐기 — OQ #1 LOCKED → solid `bg-status-safe-bar`) / 기본 inspect (lin-grad `1d4ed8→0ea5e9` **인라인 유지** §6.4 정식). **헬퍼 함수 자체는 폐기**, 3 state 분기는 className/인라인 style 로 인라인 치환. (8) busy 3 state useState — `const [busy, setBusy] = useState<string | null>(null)` (line 225) verbatim. null (idle) / `${cat.value}-inspect` / `${cat.value}-public`. (9) useAuthStore().token + baseUrl — `const { token } = useAuthStore()` (line 224) + `const baseUrl = window.location.origin` (line 227) verbatim. raw Authorization Bearer 인젝션 source + public QR URL prefix. (10) 카피 verbatim 9건 — '`QR 코드 출력`' (헤더 262) / '`항목별 QR 코드를 PDF 파일로 다운로드합니다.`' + '`소화기는 <b>점검용</b>(3×3 cm)과 <b>점검확인용</b>(7×9 cm)을 별도 파일로 다운로드할 수 있습니다.`' (안내 268~269) / '`점검용 QR PDF`' / '`생성 중...`' (inspect 289) / '`점검확인용 QR PDF`' (public 299) / '`체크포인트가 없습니다`' (toast.error 128 + 238) / '`PDF 다운로드 완료`' (toast.success 243) / '`PDF 생성 오류`' (toast.error 245) / '`본 소화기는 QR코드로 관리되며,`' + '`아래 QR코드로`' + '`점검 내역 확인 가능합니다.`' (renderCardCanvas topLines 194~196) / typeLabel '`점검용`' / '`점검확인용`' (217). (11) 모바일/데스크톱 분기 없음 — 단일 컬럼, PC 1920x1080 에도 모바일 레이아웃 동일. `lg:*` prefix 사용 금지. (12) 자체 헤더 페이지 `/qr-print` ∈ `MOBILE_NO_NAV_PATHS` — App.tsx 변경 0 byte. 02+06 chrome 룰 직접 적용 X, 자체 헤더 토큰화만."
    - "비즈 로직 0 diff: imports 6건 (useState/useNavigate/jsPDF/QRCode/useAuthStore/toast) 그대로 + **추가 1줄**: `import { ChevronLeft, Download, Printer } from 'lucide-react'` (OQ #3 LOCKED 필수 + 권장 2종). 비즈 함수/state/handler/effect/hook (CATEGORIES/renderCardCanvas/generatePdf/handleDownload/busy state/useAuthStore.token/baseUrl/raw fetch + Authorization Bearer/jsPDF cardW/cardH/MARGIN/gap/setFillColor/setDrawColor/setLineWidth/addImage/typeLabel/doc.save 파일명/QRCode.toCanvas margin/canvas.toDataURL/모든 fontSize/Apple SD Gothic Neo FONT 문자열) 모두 1 byte 0."
    - "카피 verbatim 보존 (9건 사용자 노출 문자열): '`QR 코드 출력`' (헤더, line 262) / 안내 박스 2줄 verbatim (line 268~269) / '`점검용 QR PDF`' + '`생성 중...`' (inspect 버튼, line 289) / '`점검확인용 QR PDF`' (public 버튼, line 299) / '`체크포인트가 없습니다`' (toast.error, line 128 + 238) / '`PDF 다운로드 완료`' (toast.success, line 243) / '`PDF 생성 오류`' (toast.error, line 245) / '`본 소화기는 QR코드로 관리되며,`' + '`아래 QR코드로`' + '`점검 내역 확인 가능합니다.`' (renderCardCanvas topLines, line 194~196) / '`점검용`' / '`점검확인용`' (typeLabel, line 217) + doc.save 파일명 `${categoryLabel}_${typeLabel}_QR.pdf` (line 218)."
    - "OQ #1 LOCKED 적용 (다운로드 버튼 그라데이션 처리): public 다운로드 버튼 lin-grad `linear-gradient(135deg,#16a34a,#22c55e)` (line 320) **완전 폐기** → solid `bg-status-safe-bar text-text-on-accent` 치환. inspect 다운로드 버튼 lin-grad `linear-gradient(135deg,#1d4ed8,#0ea5e9)` (line 321) **인라인 유지** (§6.4 정식 그라데이션 anchor, ≥1 grep PASS). dlBtnStyle 헬퍼 함수 자체 폐기 — 3 state (loading / public / 기본 inspect) 분기는 인라인 style + className 으로 치환."
    - "OQ #2 LOCKED 적용 (안내 박스 색 토큰화): 옛 `background: rgba(14,165,233,.08)` + `border: 1px solid rgba(14,165,233,.25)` + `color: var(--t2)` + `lineHeight: 1.6` (line 267) **완전 폐기** → `<div className=\"bg-status-info-bg border border-status-info-bar rounded-md text-caption text-text-secondary leading-relaxed px-[13px] py-[10px]\">` (W3 sketch verbatim). raw rgba 0건."
    - "OQ #3 LOCKED 적용 (뒤로가기 Lucide ChevronLeft 치환): 옛 인라인 SVG `<svg width={15} height={15} ... stroke=\"var(--t2)\"><path d=\"M15 19l-7-7 7-7\"/></svg>` (line 258~260) **완전 제거** → `import { ChevronLeft } from 'lucide-react'` (추가 1줄, Download/Printer 권장 2종 동시 import) + `<ChevronLeft size={16} strokeWidth={2} className=\"text-text-secondary\" />` 치환. raw SVG path 0건. `IconChevronLeft` 잘못된 import 0건 (정식 이름 `ChevronLeft`)."
    - "OQ #4 LOCKED 적용 (외곽 hex 토큰 치환 범위): design-system.md §4.1 마이그레이션 표 그대로 모두 새 토큰 치환. `var(--bg)` (253) → `bg-surface-page` / `var(--bg2)` (256, 280) → `bg-surface-raised` / `var(--bg3)` (257) → `bg-surface-sunken` / `var(--bd)` (256, 257, 280) → `border-border-default` / `var(--bd2)` (318) → `bg-border-strong` / `var(--t1)` (262, 281) → `text-text-primary` / `var(--t2)` (258 stroke, 267 안내) → `text-text-secondary` / `var(--t3)` (322) → `text-text-tertiary`. raw `var(--bg|bg2|bg3|bd|bd2|t1|t2|t3)` 0건."
    - "OQ #5 LOCKED 적용 (비즈 anchor 보존 확인): §4 박스 전체 (12 항목) 1 byte 변경 0. memory `project_redesign_15_daily_report_status` 캘리브 좌표 100% 보존 일반화 — renderCardCanvas + generatePdf 의 모든 px/mm/scale/색/카피 1 byte 0."
    - "components.css 신규 ≥10 class 추가 (scope `.qr-print-page`): `.qr-print-page` (외곽 wrapper) / `.qr-print-page-header` (자체 헤더) / `.qr-print-page-back-btn` (34x34 뒤로가기 — w-8 함정 회피 width:34 height:34 명시) / `.qr-print-page-title` (헤더 타이틀 fontSize 15 격상 text-body) / `.qr-print-page-info-box` (안내 status-info bg + border) / `.qr-print-page-list` (카테고리 목록 wrapper) / `.qr-print-page-card` (카테고리 카드 raised + border + radius 13) / `.qr-print-page-card-title` (카드 타이틀 text-body-sm font-bold) / `.qr-print-page-btn-row` (버튼 row flex gap 8 flex-wrap) / `.qr-print-page-dl-btn` (공통 다운로드 버튼 base — padding 9px 16px + radius 9 + text-caption font-bold + leading-none + display flex + gap 6) / `.qr-print-page-dl-btn--loading` (loading state — bg border-strong + color text-tertiary + cursor default) / `.qr-print-page-dl-btn--public` (public solid bg-status-safe-bar — OQ #1 LOCKED) / `.qr-print-page-dl-btn--inspect` (inspect 인라인 그라데이션 anchor — class 는 hover/transition 만, background 는 인라인 유지). 총 13 신규 class (≥10 만족)."
    - "Negative gate 모두 PASS (W5 §11 verbatim 17 항목): (1) src/** 변경 = 위 2 파일만 (QRPrintPage.tsx + components.css) — App.tsx 포함 다른 src/** 0 byte. (2) sketch HTML 추가 0. (3) wrangler 명령 0 (`.claude/settings.local.json` deny 강제, memory `feedback_cbc7119_design_never_wrangler`). (4) `npm run deploy` 0 (CLAUDE.local.md 룰). (5) 이모지 0 (📥 / 🖨️ 등 사용 금지, design-system.md §7.1). (6) fontSize 9·10·11 인라인 0 (design-system.md §1.1 노안 친화 절대 룰). (7) status- prefix 0 (W4 sketch verbatim `bg-status-safe-bar` 는 tokens.css 양방향 alias 로 valid — 예외). (8) w-8 / h-8 0 (memory `feedback_tailwind_w8_h8_is_48px`, 뒤로가기 34x34 인라인 명시 — `style={{ width: 34, height: 34 }}` 또는 components.css class width:34 height:34 명시). (9) IconChevronLeft 0 (Lucide 정식 `ChevronLeft`). (10) polyline / 옛 인라인 SVG `<path d=\"M15 19l-7-7 7-7\"/>` 0. (11) 옛 alias `var(--bg|bg2|bg3|bd|bd2|t1|t2|t3)` raw 인라인 0 (§10 cheatsheet 1:1 치환). (12) lin-grad public `linear-gradient(135deg,#16a34a,#22c55e)` 0 (OQ #1 LOCKED 폐기). (13) rgba(14,165,233,.08/.25) raw 0 (OQ #2 LOCKED 토큰 치환). (14) CATEGORIES 7건 순서/value/label/hasPublic 변경 0. (15) renderCardCanvas / generatePdf / handleDownload 비즈 변경 0 (scale 3 / FONT Apple SD Gothic Neo / cardW 30/70 / cardH 38/90 / MARGIN 10 / gap 1 / setFillColor 255/0/150 / setLineWidth 0.2/0.3 / typeLabel / doc.save / Authorization Bearer / toast 카피 1 byte 0). (16) 카피 verbatim 9건 변경 0. (17) raw fetch + Authorization Bearer + useAuthStore.token 변경 0 (`src/utils/api.ts` 래퍼로 갈아끼우지 말 것)."
    - "Positive gate 모두 PASS (W5 §12 verbatim 28 grep): (1) `grep -cE \"bg-surface-page|class=\\\"qr-print-page\\\"\" QRPrintPage.tsx` ≥1 (외곽 wrapper). (2) `grep -c \"bg-surface-raised\" QRPrintPage.tsx` ≥1 (헤더). (3) `grep -c \"bg-surface-sunken\" QRPrintPage.tsx` ≥0 (back-btn — components.css 안에 정의되면 TSX 검색 0 가능). (4) `grep -c \"border-border-default\" QRPrintPage.tsx` ≥1. (5) `grep -c \"text-text-primary\" QRPrintPage.tsx` ≥1 (헤더 타이틀 또는 카드 타이틀). (6) `grep -c \"text-text-secondary\" QRPrintPage.tsx` ≥1 (안내 + ChevronLeft). (7) `grep -cE \"bg-status-info-bg|qr-print-page-info-box\" QRPrintPage.tsx` ≥1 (OQ #2). (8) `grep -cE \"border-status-info-bar\" QRPrintPage.tsx` ≥0 (components.css 안 정의 시 0 가능). (9) `grep -cE \"bg-status-safe-bar|bg-safe-bar|qr-print-page-dl-btn--public\" QRPrintPage.tsx` ≥1 (OQ #1). (10) `grep -c \"1d4ed8.*0ea5e9\" QRPrintPage.tsx` ==1 (inspect 유지). (11) `grep -c \"16a34a.*22c55e\" QRPrintPage.tsx` ==0 (OQ #1 폐기). (12) `grep -c \"ChevronLeft\" QRPrintPage.tsx` ≥2 (import + 사용). (13) `grep -c \"from 'lucide-react'\" QRPrintPage.tsx` ==1. (14) `grep -c \"size={16}\" QRPrintPage.tsx` ≥1. (15) `grep -c \"M15 19l-7-7 7-7\" QRPrintPage.tsx` ==0. (16) `grep -c \"IconChevronLeft\\|<polyline\" QRPrintPage.tsx` ==0. (17) `grep -cE \"width: 34.*height: 34|w-\\[34px\\] h-\\[34px\\]|width:34px;height:34px\" QRPrintPage.tsx cha-bio-safety/src/styles/components.css` ≥1. (18) `grep -cE \"var\\(--(bg|bg2|bg3|bd|bd2|t1|t2|t3)\\)\" QRPrintPage.tsx` ==0. (19) `grep -c \"rgba(14,165,233\" QRPrintPage.tsx` ==0. (20) `grep -cE \"text-caption|text-body|text-body-sm|text-label\" QRPrintPage.tsx` ≥1. (21) `grep -cE \"leading-none|leading-relaxed\" QRPrintPage.tsx` ≥1. (22) `grep -cE \"w-8\\b|h-8\\b\" QRPrintPage.tsx` ==0. (23) `grep -cE \"text-status-\" QRPrintPage.tsx` ==0 (단 W4 verbatim `bg-status-*` 는 alias valid). (24) `grep -cE \"qr-print-page\" cha-bio-safety/src/styles/components.css` ≥10 (신규 ≥10 class). (25) `grep -c \"CATEGORIES\" QRPrintPage.tsx` ≥3 (배열 정의 + .map + .find). (26) `grep -c \"renderCardCanvas\\|generatePdf\\|handleDownload\\|dlBtnStyle\" QRPrintPage.tsx` ≥3 (dlBtnStyle 폐기로 ==3). (27) `grep -c \"QR 코드 출력\\|PDF 다운로드 완료\\|체크포인트가 없습니다\\|본 소화기는 QR코드로 관리되며\" QRPrintPage.tsx` ≥4. (28) `wc -l QRPrintPage.tsx` 300~360."
    - "★ 비즈 anchor 12건 보존 grep gate PASS: imports 6 (useState/useNavigate/jsPDF/QRCode/useAuthStore/toast) + CheckPoint interface + CATEGORIES 7건 + renderCardCanvas (scale 3 + Apple SD Gothic Neo + #ffffff/#222222/#333333 + margin 1) + generatePdf (cardW 30/70 + cardH 38/90 + MARGIN 10 + gap 1 + setFillColor 255/0/150 + setLineWidth 0.2/0.3 + typeLabel + doc.save) + handleDownload (Authorization Bearer + raw fetch + toast 3건) + busy 3 state useState + useAuthStore.token + baseUrl + 카피 verbatim 9건 + 모바일/데스크톱 단일 컬럼 (lg:* 0) + 자체 헤더 페이지 (App.tsx 0 byte)."
    - "6 src 파일 + tailwind.config.js 변경 0 byte: `git diff --name-only HEAD~ HEAD -- cha-bio-safety/src/App.tsx cha-bio-safety/src/utils/api.ts cha-bio-safety/src/stores/authStore.ts cha-bio-safety/src/styles/tokens.css cha-bio-safety/src/styles/typography.css cha-bio-safety/tailwind.config.js` 빈 출력. **components.css 는 본 plan 의 수정 대상 — 0 byte 가드 X**."
    - "Build gate: `cd cha-bio-safety && npx tsc --noEmit` 0 errors + `cd cha-bio-safety && npm run build` exit 0 (Vite build PASS). QRPrintPage chunk size 보고 (예: 30~40kb gzipped)."
    - "Atomic 1-commit (2 파일): `feat(quick-260526-b5t): redesign/25-qr-print TSX 변환 (QRPrintPage.tsx 330→{N} lines + components.css 504→{M} lines, 2 파일 단일 atomic + v0.1.1 토큰 className + 비즈 anchor 12 보존 + Lucide ChevronLeft+Download+Printer + dlBtnStyle 폐기 + back 34x34 + public solid bg-status-safe-bar + inspect lin-grad anchor + OQ LOCKED 5 + W5 §1 매핑 verbatim)`. SUMMARY 별도 commit. body 에서 'wrangler' 단어 회피."

  artifacts:
    - path: cha-bio-safety/src/pages/QRPrintPage.tsx
      provides: v0.1.1 토큰 className 적용 (외곽 wrapper / 자체 헤더 + 뒤로가기 34x34 ChevronLeft size 16 + 헤더 타이틀 fontSize 15 격상 text-body font-bold leading-none / 안내 박스 OQ #2 bg-status-info-bg + border-status-info-bar + text-caption leading-relaxed + 안내 카피 verbatim 2줄 + <b> 강조 / 카테고리 목록 wrapper / CATEGORIES 7건 .map 카드 + cat.label 카드 타이틀 text-body-sm font-bold + 버튼 row flex gap-8 flex-wrap / inspect 다운로드 버튼 lin-grad 1d4ed8→0ea5e9 인라인 유지 + 카피 '점검용 QR PDF' / '생성 중...' / public 다운로드 버튼 OQ #1 lin-grad 16a34a→22c55e 폐기 + bg-status-safe-bar solid + 카피 '점검확인용 QR PDF' + cat.hasPublic 분기) + Lucide 3종 교체 (ChevronLeft 뒤로가기 + Download 권장 + Printer 권장) + dlBtnStyle 헬퍼 함수 line 312~330 폐기 (3 state 매트릭스 인라인 + className 치환) + 인라인 SVG path M15 19l-7-7 7-7 line 258~260 폐기 + OQ #1 public lin-grad 폐기 → bg-status-safe-bar solid + OQ #2 안내 박스 토큰화 + OQ #3 ChevronLeft size 16 + OQ #4 외곽 hex 토큰 8종 치환 + OQ #5 비즈 anchor 12 보존 + 비즈 anchor 12 (imports 6 + CheckPoint interface + CATEGORIES 7 + renderCardCanvas + generatePdf + handleDownload + busy state + useAuthStore + baseUrl + 카피 9 + 모바일/데스크톱 단일 + 자체 헤더 페이지) 1 byte 변경 0
      contains: "qr-print-page / qr-print-page-header / qr-print-page-back-btn / qr-print-page-title / qr-print-page-info-box / qr-print-page-list / qr-print-page-card / qr-print-page-card-title / qr-print-page-btn-row / qr-print-page-dl-btn / qr-print-page-dl-btn--loading / qr-print-page-dl-btn--public / qr-print-page-dl-btn--inspect / bg-surface-page / bg-surface-raised / bg-surface-sunken / border-border-default / bg-border-strong / text-text-primary / text-text-secondary / text-text-tertiary / text-text-on-accent / bg-status-info-bg / border-status-info-bar / bg-status-safe-bar / text-body / text-body-sm / text-caption / font-bold / leading-none / leading-relaxed / rounded-md / rounded-sm / ChevronLeft / Download / Printer / size={16} / from 'lucide-react' / linear-gradient(135deg,#1d4ed8,#0ea5e9) / QR 코드 출력 / 점검용 QR PDF / 점검확인용 QR PDF / 생성 중... / 체크포인트가 없습니다 / PDF 다운로드 완료 / PDF 생성 오류 / 본 소화기는 QR코드로 관리되며 / 점검용 / 점검확인용 / CATEGORIES / renderCardCanvas / generatePdf / handleDownload / busy / useAuthStore / baseUrl / Authorization Bearer / Apple SD Gothic Neo / scale = 3 / cardW / cardH / MARGIN / setFillColor / setDrawColor / setLineWidth / addImage / doc.save"
      min_lines: 300

    - path: cha-bio-safety/src/styles/components.css
      provides: scope `.qr-print-page` 신규 ≥10 class 추가 (외곽 / 헤더 / 뒤로가기 34x34 / 타이틀 / 안내 박스 / 목록 / 카드 / 카드 타이틀 / 버튼 row / 다운로드 버튼 base / loading / public / inspect 변형 — 총 13 신규) + 기존 504 line 그대로 (page-header / report-card / toolbar / sidelist / preview-wrapper 등 inherit 가능 영역 변경 0).
      contains: ".qr-print-page / .qr-print-page-header / .qr-print-page-back-btn / .qr-print-page-title / .qr-print-page-info-box / .qr-print-page-list / .qr-print-page-card / .qr-print-page-card-title / .qr-print-page-btn-row / .qr-print-page-dl-btn / .qr-print-page-dl-btn--loading / .qr-print-page-dl-btn--public / .qr-print-page-dl-btn--inspect / width: 34px / height: 34px / var(--surface-page) / var(--surface-raised) / var(--surface-sunken) / var(--border-default) / var(--border-strong) / var(--text-primary) / var(--text-secondary) / var(--text-tertiary) / var(--text-on-accent) / var(--status-info-bg) / var(--status-info-bar) / var(--status-safe-bar)"
      min_lines: 514

  key_links:
    - from: "W5 TSX 변환 checklist (657 lines, 12 섹션 SOURCE OF TRUTH)"
      to: cha-bio-safety/src/pages/QRPrintPage.tsx line 1~330 + cha-bio-safety/src/styles/components.css line 504+
      via: "§1 scope (2 파일 단일 atomic) + §2 region mapping 4 sub-area (외곽+헤더 / 안내 / 카테고리+카드 / 다운로드 버튼+dlBtnStyle+handleDownload+renderCardCanvas+generatePdf) + §3 sketch CSS verbatim fence (W2/W3/W4 grep 결과) + §4 비즈 anchor 12 박제 (imports 6 + CheckPoint + CATEGORIES 7 + renderCardCanvas + generatePdf + handleDownload + dlBtnStyle 3 state + busy 3 state + useAuthStore + 카피 9 + 모바일/데스크톱 분기 없음 + 자체 헤더) + §5 OQ LOCKED 5 + §6 sketch class cheatsheet + §7 폰트 격상 (9·10·11 금지) + §8 Lucide ≥3 (ChevronLeft 필수 + Download/Printer 권장) + §9 components.css inherit ≥3 + 신규 ≥10 (본 plan 은 brief 의 ≥10 신규 우선 — 단순 inherit 가 아닌 dedicated class) + §10 Tailwind cheatsheet (var(--alias) → className 1:1) + §11 negative 17 + §12 verify grep 28."
      pattern: "§1 scope|§2 region mapping|§3 sketch CSS|§4 비즈 anchor|§5 OQ LOCKED|§6 cheatsheet|§7 폰트 격상|§8 Lucide|§9 components.css|§10 Tailwind|§11 negative|§12 verify grep"

    - from: "W5 checklist OQ LOCKED 5건 (W1 §7 default 답 채택)"
      to: cha-bio-safety/docs/redesign-context/25-qr-print/wave-5-tsx-conversion-checklist.md §5
      via: "OQ #1 public lin-grad 16a34a→22c55e 폐기 → bg-status-safe-bar solid + inspect lin-grad 1d4ed8→0ea5e9 인라인 유지 / OQ #2 안내 박스 rgba(14,165,233) → bg-status-info-bg + border-status-info-bar / OQ #3 SVG path M15 19l-7-7 7-7 → ChevronLeft size 16 / OQ #4 외곽 hex 토큰 치환 8종 (bg/bg2/bg3/bd/bd2/t1/t2/t3) / OQ #5 비즈 anchor 12 보존 (§4 박스 1 byte 0)."
      pattern: "OQ #1|OQ #2|OQ #3|OQ #4|OQ #5|LOCKED|lin-grad|bg-status-safe-bar|bg-status-info-bg|ChevronLeft|var(--bg|비즈 anchor"

    - from: "W2~W4 sketch (시각 검증, read-only reference)"
      to: cha-bio-safety/docs/redesign-context/25-qr-print/sketch-wave-{2,3,4}-*.html
      via: "변환 매핑 시각 검증 — W2 chrome-header / W3 instructions-categories / W4 download-actions sketch HTML. 변경 0 byte (read-only). sketch class verbatim 인용 grep `grep -hoE 'class=\"[^\"]+\"' sketch-wave-*.html | sort -u` 결과 W5 §3 에 박제됨."
      pattern: "sketch-wave-(2|3|4)"

    - from: "토큰 / Tailwind config (utility class 검증, 0 byte 가드)"
      to: cha-bio-safety/src/styles/tokens.css + cha-bio-safety/tailwind.config.js
      via: "v0.1.1 토큰 확인 — --surface-page/raised/sunken / --text-primary/secondary/tertiary/on-accent / --border-default/strong / --status-info-bg/bar / --status-safe-bar. tailwind.config 매핑 bg-surface-* / border-border-* / text-text-* / bg-status-*. **status- prefix 양방향 alias** (memory `feedback_tailwind_token_class_pattern` — W4 sketch verbatim `bg-status-safe-bar` 는 valid). w-[34px] h-[34px] arbitrary (memory `feedback_tailwind_w8_h8_is_48px` w-8=48 함정 회피)."
      pattern: "surface-page|surface-raised|surface-sunken|border-default|border-strong|status-info-bg|status-info-bar|status-safe-bar"

    - from: "외부 라이브러리 (0 byte 변경 = 가드)"
      to: jspdf / qrcode / react-hot-toast / react-router-dom / lucide-react (node_modules)
      via: "jsPDF 4.2.1 / QRCode 1.5.4 / react-hot-toast 2.4.1 / react-router-dom 6.26.2 / lucide-react 0.454.0 모두 변경 0. 단 `lucide-react` 의 ChevronLeft/Download/Printer 3종 import 추가 1줄만 허용. raw 의존성 (jsPDF unit/format/setFillColor/setDrawColor/setLineWidth/addImage/doc.save / QRCode.toCanvas margin / toast.error/toast.success / useNavigate / useAuthStore.token) 모두 1 byte 0."
      pattern: "jspdf|qrcode|react-hot-toast|react-router-dom|lucide-react|ChevronLeft|Download|Printer"

  scope_negatives:
    - "2 파일만 수정 — cha-bio-safety/src/pages/QRPrintPage.tsx + cha-bio-safety/src/styles/components.css 외 src/ 트리 변경 0건"
    - "App.tsx 변경 0 byte (final verify gate)"
    - "6 src 파일 + tailwind.config.js 변경 0 byte: utils/api.ts / stores/authStore.ts / styles/tokens.css / styles/typography.css / tailwind.config.js + App.tsx"
    - "types/ / functions/ / templates/ / migrations/ / public/ 모두 0 byte 변경"
    - "sketch HTML (W2 chrome-header / W3 instructions-categories / W4 download-actions) + W5 checklist (657) + W1 index 모두 0 byte 변경 (read-only reference)"
    - "cf-cli 명령 0건 (CLAUDE.local.md 디자인 워크트리 강제, body 에서 단어 회피)"
    - "npm run deploy 0건 (직원 도메인 cbc7119 경로)"
---

<objective>
redesign/25-qr-print TSX 변환 — QRPrintPage.tsx (330 lines) + components.css (504 lines, 신규 ≥10 class 추가) **2 파일 단일 atomic commit in-place 수정**.

W2 (chrome-header) + W3 (instructions-categories) + W4 (download-actions) sketch + W5 12 섹션 checklist (657) + W1 OQ 5건 LOCKED 결정 을 1-commit 으로 적용. **★ 비즈 anchor 12건 1 byte 변경 0**. **★ Lucide 3종 교체** (ChevronLeft 뒤로가기 필수 + Download/Printer 권장 — 인라인 SVG path `M15 19l-7-7 7-7` 완전 제거, OQ #3 LOCKED). **★ dlBtnStyle 헬퍼 함수 line 312~330 폐기** + 3 state (loading / public / 기본 inspect) 매트릭스 className/인라인 style 치환. **★ public 다운로드 버튼 lin-grad `16a34a→22c55e` 폐기 → solid `bg-status-safe-bar`** (OQ #1 LOCKED, §6.4 정식 그라데이션 위반 회피). **★ inspect 다운로드 버튼 lin-grad `1d4ed8→0ea5e9` 인라인 유지** (§6.4 정식 anchor ≥1 grep).

28-splash (4i9) 2-파일 단일 atomic 패턴 (TSX + components.css 동시) 일부 mirror — 단 본 25-qr-print 는 components.css 신규 ≥10 class 추가 (단순 inherit 가 아닌 dedicated class 가 카드/버튼/안내 박스 가독성 우수). 18-worklog (slq) 신규 ≥10 패턴 mirror. 본 QRPrintPage 는 `/qr-print` 풀 페이지 단일 export (QRPrintPage default) + 4 sub-area (외곽+자체 헤더 / 안내 박스 / 카테고리 목록+카드 / 다운로드 버튼+dlBtnStyle+handleDownload+renderCardCanvas+generatePdf) 통합 변환.

Purpose:
- v0.1.1 토큰 className 으로 옛 인라인 `var(--bg) / var(--bg2) / var(--bg3) / var(--bd) / var(--bd2) / var(--t1) / var(--t2) / var(--t3)` + `rgba(14,165,233,.08/.25)` + `linear-gradient(135deg,#16a34a,#22c55e)` 치환
- **OQ #1 LOCKED**: 다운로드 버튼 그라데이션 처리 — public lin-grad `linear-gradient(135deg,#16a34a,#22c55e)` (line 320) **완전 폐기** → solid `bg-status-safe-bar text-text-on-accent` 치환. inspect lin-grad `linear-gradient(135deg,#1d4ed8,#0ea5e9)` (line 321) **인라인 유지** (§6.4 정식 anchor ≥1 grep PASS). dlBtnStyle 헬퍼 자체 폐기, 3 state 분기 인라인 치환.
- **OQ #2 LOCKED**: 안내 박스 색 토큰화 — `bg-status-info-bg border border-status-info-bar rounded-md text-caption text-text-secondary leading-relaxed` (W3 sketch verbatim) 치환. 옛 raw rgba(14,165,233,.08/.25) + var(--t2) + lineHeight:1.6 (line 267) 완전 폐기.
- **OQ #3 LOCKED**: 뒤로가기 Lucide ChevronLeft 치환 — `<ChevronLeft size={16} strokeWidth={2} className="text-text-secondary" />` (Download/Printer 권장 2종 동시 import). 옛 인라인 SVG path `M15 19l-7-7 7-7` (line 258~260) 완전 제거. `IconChevronLeft` 잘못된 import 0.
- **OQ #4 LOCKED**: 외곽 hex 토큰 치환 — `var(--bg|bg2|bg3|bd|bd2|t1|t2|t3)` → `bg-surface-page|raised|sunken / border-border-default / bg-border-strong / text-text-primary|secondary|tertiary` 8종 1:1 치환. raw alias 0건.
- **OQ #5 LOCKED**: 비즈 anchor 보존 — §4 박스 전체 (12 항목) 1 byte 변경 0. memory `project_redesign_15_daily_report_status` 캘리브 좌표 100% 보존 일반화.
- ★ 비즈 anchor 12건 **1 byte 변경 0** (must_haves truths #4 박제)
- 비즈 로직 0 diff (imports 6/state/handler/effect/hook/CATEGORIES 7/renderCardCanvas/generatePdf/handleDownload/busy 3 state/useAuthStore.token/baseUrl/raw fetch + Authorization Bearer/jsPDF cardW/cardH/MARGIN/gap/setFillColor/setDrawColor/setLineWidth/addImage/typeLabel/doc.save/QRCode.toCanvas margin/canvas.toDataURL/Apple SD Gothic Neo FONT 모두 보존)
- components.css 신규 ≥10 class 추가 (scope `.qr-print-page` 총 13 class — 외곽/헤더/back-btn 34x34/타이틀/안내/목록/카드/카드 타이틀/버튼 row/dl-btn base/loading/public/inspect 변형)

Output:
- cha-bio-safety/src/pages/QRPrintPage.tsx 단일 파일 in-place 수정 (330 → 300~360 lines 예상)
- cha-bio-safety/src/styles/components.css 신규 class 추가 (504 → 514~525 lines)
- App.tsx + 6 src 파일 + tailwind.config.js + tokens.css + typography.css + types/ + functions/ + templates/ + migrations/ + public/ 모두 0 byte 변경
- `npm run build` PASS
- atomic 1-commit (2 파일) + SUMMARY 별도 commit
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jykevin/Documents/cbc7119-design/CLAUDE.md
@/Users/jykevin/Documents/cbc7119-design/CLAUDE.local.md
@/Users/jykevin/Documents/cbc7119-design/.planning/STATE.md

# W5 TSX 변환 checklist (SOURCE OF TRUTH — 657 lines, 12 섹션)
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/25-qr-print/wave-5-tsx-conversion-checklist.md

# 변환 대상 (in-place 수정, executor 가 Read 로 직접 확인)
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/src/pages/QRPrintPage.tsx
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/src/styles/components.css

# 토큰 / Tailwind config (utility class 검증, read-only)
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/src/styles/tokens.css
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/tailwind.config.js
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/src/styles/typography.css

# 변환 무영향 — 0 byte 변경 (final verify gate)
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/src/App.tsx
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/src/utils/api.ts
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/src/stores/authStore.ts

</context>

<tasks>

<task type="auto">
  <name>Task 1: QRPrintPage.tsx + components.css 2 파일 단일 atomic v0.1.1 토큰 변환 (in-place, 비즈 anchor 12건 + Lucide 3종 교체 + dlBtnStyle 헬퍼 폐기 + back 34x34 + public solid bg-status-safe-bar + inspect lin-grad anchor + components.css 신규 ≥10 class + OQ LOCKED 5건, atomic 1-commit)</name>
  <files>cha-bio-safety/src/pages/QRPrintPage.tsx, cha-bio-safety/src/styles/components.css</files>
  <action>

QRPrintPage.tsx 330 lines + components.css 504 lines (신규 ≥10 class 추가) 를 **2 파일 단일 atomic commit** 으로 in-place 수정하여 v0.1.1 토큰 className 으로 치환한다. 28-splash (4i9) 2-파일 atomic 패턴 + 18-worklog (slq) 신규 ≥10 패턴 mirror. 비즈 로직 (imports 6 + CheckPoint + CATEGORIES 7 + renderCardCanvas + generatePdf + handleDownload + busy 3 state + useAuthStore.token + baseUrl + 카피 9건) 1 byte 변경 0. **★ 비즈 anchor 12건 + Lucide 3종 교체 (ChevronLeft+Download+Printer) + dlBtnStyle 헬퍼 함수 line 312~330 폐기 + 인라인 SVG path M15 19l-7-7 7-7 폐기 + public lin-grad 16a34a→22c55e 폐기 + inspect lin-grad 1d4ed8→0ea5e9 인라인 유지 + components.css 신규 13 class 추가 모두 grep PASS**.

## 0. 사전 확인 (필수)

```bash
# 0-1. branch 확인 (worktree-aware)
git branch --show-current   # 기대: redesign/25-qr-print
git status --short          # 기대: 빈 출력 (clean) 또는 PLAN.md 만

# 0-2. branch base 검증
EXPECTED_BASE="origin/main"
git merge-base HEAD "$EXPECTED_BASE" >/dev/null 2>&1 && echo "branch base OK" || echo "branch base FAIL"

# 0-3. 변환 대상 파일 라인 수 baseline
wc -l cha-bio-safety/src/pages/QRPrintPage.tsx          # 기대: 330
wc -l cha-bio-safety/src/styles/components.css           # 기대: 504

# 0-4. 6 src 파일 + tailwind.config.js baseline (0 byte 변경 verify gate)
for f in cha-bio-safety/src/App.tsx \
         cha-bio-safety/src/utils/api.ts \
         cha-bio-safety/src/stores/authStore.ts \
         cha-bio-safety/src/styles/tokens.css \
         cha-bio-safety/src/styles/typography.css \
         cha-bio-safety/tailwind.config.js; do
  echo "$f: $(wc -c < "$f") bytes"
done

# 0-5. W5 checklist SOURCE OF TRUTH 라인 수 확인
wc -l cha-bio-safety/docs/redesign-context/25-qr-print/wave-5-tsx-conversion-checklist.md   # 기대: 657
```

## 1. W5 §1~§12 verbatim 적용 매핑

### 1.1 4 sub-area 영역 분할 (W5 §1.1 표 verbatim)

| sub-area | element | line 범위 (옛) | 변환 후 |
|---|---|---|---|
| (1) 외곽 + 자체 헤더 | wrapper + header + back-btn + title | 253~263 | `.qr-print-page` + `.qr-print-page-header` + `.qr-print-page-back-btn` (ChevronLeft size 16) + `.qr-print-page-title` (text-body font-bold leading-none) |
| (2) 안내 박스 | OQ #2 LOCKED 토큰화 | 266~271 | `<div className="px-[14px] pt-[10px] pb-1 flex-shrink-0"><div className="bg-status-info-bg border border-status-info-bar rounded-md text-caption text-text-secondary leading-relaxed px-[13px] py-[10px]">` (또는 `.qr-print-page-info-box` class 안 wrap) |
| (3) 카테고리 목록 + 카드 | CATEGORIES.map | 274~310 | `.qr-print-page-list` (overflow-y auto) + `.qr-print-page-card` (raised + border + radius 13) + `.qr-print-page-card-title` (text-body-sm font-bold) + `.qr-print-page-btn-row` (flex gap-8 flex-wrap) |
| (4) 다운로드 버튼 + dlBtnStyle + handleDownload + renderCardCanvas + generatePdf | 비즈 anchor 12 | 283~302 + 312~330 + 229~250 + 29~119 + 122~219 | inspect 버튼 `.qr-print-page-dl-btn .qr-print-page-dl-btn--inspect` + 인라인 `style={{ background: 'linear-gradient(135deg,#1d4ed8,#0ea5e9)' }}` 유지 / public 버튼 `.qr-print-page-dl-btn .qr-print-page-dl-btn--public` + bg-status-safe-bar / loading `.qr-print-page-dl-btn--loading` / dlBtnStyle 헬퍼 함수 line 312~330 **완전 폐기** |

### 1.2 components.css 신규 13 class 정의 (scope `.qr-print-page`, 신규 ≥10 만족)

components.css 끝부분 (line 504+) 에 아래 13 class 추가:

```css
  /* QR Print Page (25-qr-print W5 TSX 변환) ───────────────────────── */
  .qr-print-page { height: 100%; display: flex; flex-direction: column; overflow: hidden; background: var(--surface-page); }
  .qr-print-page-header { padding: 8px 12px; background: var(--surface-raised); border-bottom: 1px solid var(--border-default); display: flex; align-items: center; gap: 10px; flex-shrink: 0; }
  .qr-print-page-back-btn { width: 34px; height: 34px; border-radius: 8px; background: var(--surface-sunken); border: 1px solid var(--border-default); color: var(--text-secondary); cursor: pointer; display: flex; align-items: center; justify-content: center; flex-shrink: 0; }
  .qr-print-page-title { font-size: 15px; font-weight: 700; color: var(--text-primary); line-height: 1; }
  .qr-print-page-info-wrap { padding: 10px 14px 4px; flex-shrink: 0; }
  .qr-print-page-info-box { background: var(--status-info-bg); border: 1px solid var(--status-info-bar); border-radius: 10px; padding: 10px 13px; font-size: 12px; color: var(--text-secondary); line-height: 1.6; }
  .qr-print-page-list { flex: 1; overflow-y: auto; padding: 10px 14px 20px; }
  .qr-print-page-list-inner { display: flex; flex-direction: column; gap: 10px; }
  .qr-print-page-card { background: var(--surface-raised); border: 1px solid var(--border-default); border-radius: 13px; padding: 13px 14px; }
  .qr-print-page-card-title { font-size: 14px; font-weight: 700; color: var(--text-primary); margin-bottom: 10px; line-height: 1.2; }
  .qr-print-page-btn-row { display: flex; gap: 8px; flex-wrap: wrap; }
  .qr-print-page-dl-btn { padding: 9px 16px; border-radius: 9px; border: none; color: #fff; font-size: 12px; font-weight: 700; cursor: pointer; display: flex; align-items: center; gap: 6px; line-height: 1; }
  .qr-print-page-dl-btn--loading { background: var(--border-strong); color: var(--text-tertiary); cursor: default; }
  .qr-print-page-dl-btn--public { background: var(--status-safe-bar); }
  /* inspect 버튼 은 인라인 style 로 lin-grad #1d4ed8→#0ea5e9 적용 (§6.4 정식 anchor) */
```

(총 14 신규 class — `qr-print-page-info-wrap` 포함 시 14, 미포함 시 13. 둘 다 ≥10 만족.)

### 1.3 imports 변경 (line 1~6 + 추가 1줄)

```typescript
// 옛 (line 1~6) — 1 byte 변경 0
import { useState } from 'react'
import { useNavigate } from 'react-router-dom'
import { jsPDF } from 'jspdf'
import QRCode from 'qrcode'
import { useAuthStore } from '../stores/authStore'
import toast from 'react-hot-toast'

// 추가 1줄 (OQ #3 LOCKED 필수 + 권장 2종)
import { ChevronLeft, Download, Printer } from 'lucide-react'
```

(executor 판단으로 Download/Printer 미사용 시 `import { ChevronLeft } from 'lucide-react'` 단일도 OK — 단 OQ #3 LOCKED 의 `<ChevronLeft size={16} />` 는 필수.)

### 1.4 JSX 영역 변환 (line 252~310)

```jsx
// 옛 line 253 (외곽)
<div style={{ height: '100%', display: 'flex', flexDirection: 'column', overflow: 'hidden', background: 'var(--bg)' }}>

// 변환 후
<div className="qr-print-page">

// ─────────────────────

// 옛 line 256~263 (헤더)
<div style={{ padding: '8px 12px', background: 'var(--bg2)', borderBottom: '1px solid var(--bd)', display: 'flex', alignItems: 'center', gap: 10, flexShrink: 0 }}>
  <button onClick={() => navigate(-1)} style={{ width: 34, height: 34, borderRadius: 8, background: 'var(--bg3)', border: '1px solid var(--bd)', cursor: 'pointer', display: 'flex', alignItems: 'center', justifyContent: 'center', flexShrink: 0 }}>
    <svg width={15} height={15} fill="none" viewBox="0 0 24 24" stroke="var(--t2)" strokeWidth={2}>
      <path strokeLinecap="round" strokeLinejoin="round" d="M15 19l-7-7 7-7"/>
    </svg>
  </button>
  <span style={{ fontSize: 15, fontWeight: 700, color: 'var(--t1)' }}>QR 코드 출력</span>
</div>

// 변환 후
<div className="qr-print-page-header">
  <button onClick={() => navigate(-1)} className="qr-print-page-back-btn">
    <ChevronLeft size={16} strokeWidth={2} />
  </button>
  <span className="qr-print-page-title">QR 코드 출력</span>
</div>

// ─────────────────────

// 옛 line 266~271 (안내)
<div style={{ padding: '10px 14px 4px', flexShrink: 0 }}>
  <div style={{ background: 'rgba(14,165,233,.08)', border: '1px solid rgba(14,165,233,.25)', borderRadius: 10, padding: '10px 13px', fontSize: 12, color: 'var(--t2)', lineHeight: 1.6 }}>
    항목별 QR 코드를 PDF 파일로 다운로드합니다.<br/>
    소화기는 <b>점검용</b>(3×3 cm)과 <b>점검확인용</b>(7×9 cm)을 별도 파일로 다운로드할 수 있습니다.
  </div>
</div>

// 변환 후 (OQ #2 LOCKED)
<div className="qr-print-page-info-wrap">
  <div className="qr-print-page-info-box">
    항목별 QR 코드를 PDF 파일로 다운로드합니다.<br/>
    소화기는 <b>점검용</b>(3×3 cm)과 <b>점검확인용</b>(7×9 cm)을 별도 파일로 다운로드할 수 있습니다.
  </div>
</div>

// ─────────────────────

// 옛 line 274~309 (카테고리 목록)
<div style={{ flex: 1, overflowY: 'auto', padding: '10px 14px 20px' }}>
  <div style={{ display: 'flex', flexDirection: 'column', gap: 10 }}>
    {CATEGORIES.map(cat => {
      const inspKey   = `${cat.value}-inspect`
      const publicKey = `${cat.value}-public`
      return (
        <div key={cat.value} style={{ background: 'var(--bg2)', border: '1px solid var(--bd)', borderRadius: 13, padding: '13px 14px' }}>
          <div style={{ fontSize: 14, fontWeight: 700, color: 'var(--t1)', marginBottom: 10 }}>{cat.label}</div>
          <div style={{ display: 'flex', gap: 8, flexWrap: 'wrap' }}>
            <button onClick={...} disabled={!!busy} style={dlBtnStyle(busy === inspKey, false)}>
              {busy === inspKey ? '생성 중...' : '점검용 QR PDF'}
            </button>
            {cat.hasPublic && (
              <button onClick={...} disabled={!!busy} style={dlBtnStyle(busy === publicKey, true)}>
                {busy === publicKey ? '생성 중...' : '점검확인용 QR PDF'}
              </button>
            )}
          </div>
        </div>
      )
    })}
  </div>
</div>

// 변환 후 (dlBtnStyle 헬퍼 폐기 + 3 state 인라인 className)
<div className="qr-print-page-list">
  <div className="qr-print-page-list-inner">
    {CATEGORIES.map(cat => {
      const inspKey   = `${cat.value}-inspect`
      const publicKey = `${cat.value}-public`
      const inspLoading = busy === inspKey
      const publicLoading = busy === publicKey
      return (
        <div key={cat.value} className="qr-print-page-card">
          <div className="qr-print-page-card-title">{cat.label}</div>
          <div className="qr-print-page-btn-row">
            {/* inspect — lin-grad 1d4ed8→0ea5e9 인라인 유지 (§6.4 정식 anchor) */}
            <button
              onClick={() => handleDownload(cat.value, 'inspect')}
              disabled={!!busy}
              className={`qr-print-page-dl-btn ${inspLoading ? 'qr-print-page-dl-btn--loading' : 'qr-print-page-dl-btn--inspect'}`}
              style={inspLoading ? undefined : { background: 'linear-gradient(135deg,#1d4ed8,#0ea5e9)' }}
            >
              {inspLoading ? '생성 중...' : '점검용 QR PDF'}
            </button>

            {/* public — OQ #1 LOCKED lin-grad 폐기 → bg-status-safe-bar solid */}
            {cat.hasPublic && (
              <button
                onClick={() => handleDownload(cat.value, 'public')}
                disabled={!!busy}
                className={`qr-print-page-dl-btn ${publicLoading ? 'qr-print-page-dl-btn--loading' : 'qr-print-page-dl-btn--public'}`}
              >
                {publicLoading ? '생성 중...' : '점검확인용 QR PDF'}
              </button>
            )}
          </div>
        </div>
      )
    })}
  </div>
</div>
```

### 1.5 dlBtnStyle 헬퍼 함수 폐기 (line 312~330)

```typescript
// 옛 line 312~330 — 19 lines 완전 폐기
function dlBtnStyle(loading: boolean, isPublic: boolean): React.CSSProperties { ... }

// 변환 후 — 함수 자체 제거. 3 state 분기는 className + 인라인 style 로 1.4 참조.
```

### 1.6 비즈 함수 0 byte 변경 영역 (line 8~250)

- `interface CheckPoint` (line 8~15) — 1 byte 0
- `const CATEGORIES = [...]` (line 17~25) — 7건 순서/value/label/hasPublic 1 byte 0
- `async function renderCardCanvas(config)` (line 29~119) — scale 3 / PAD 3*S / FONT / topFontSize 12 / bottomFontSize 13 / fillStyle 3색 / textAlign center / textBaseline top / bold prefix / margin 1 / maxQrW / qrDrawSize / toDataURL 1 byte 0
- `async function generatePdf(points, type, baseUrl, categoryLabel)` (line 122~219) — toast.error '체크포인트가 없습니다' / jsPDF unit/format/orientation / PAGE_W/H 210/297 / MARGIN 10 / cardW 30/70 / cardH 38/90 / gap 1 / setFillColor 255/255/255 + rect 'FD' / setDrawColor 150/150/150 + setLineWidth 0.2 / inspect qrValue cp.id + bottomLines + bottomFontSize 3 / public qrValue baseUrl/e/cp.id + topLines verbatim 3줄 + topFontSize 3 / addImage 'PNG' / setFillColor 0/0/0 + setLineWidth 0.3 + rect 'S' / typeLabel 점검용/점검확인용 / doc.save 1 byte 0
- `export default function QRPrintPage()` line 222 (선언 자체 변경 0)
- `const navigate = useNavigate()` (line 223) — 1 byte 0
- `const { token } = useAuthStore()` (line 224) — 1 byte 0
- `const [busy, setBusy] = useState<string | null>(null)` (line 225) — 1 byte 0
- `const baseUrl = window.location.origin` (line 227) — 1 byte 0
- `async function handleDownload(category, type)` (line 229~250) — key / setBusy / raw fetch / Authorization Bearer / json.success && json.data?.length / catLabel / generatePdf / toast.success 'PDF 다운로드 완료' / catch toast.error 'PDF 생성 오류' + console.error / finally setBusy(null) 1 byte 0

## 2. components.css 적용 (line 504+ 추가, 1.2 의 13~14 class 정의 그대로 paste)

components.css 의 마지막 줄 (현재 504 line, 끝에 `}` 가 있다면 그 안에 들여쓰기 2-space 로 추가, 또는 새 `@layer components { ... }` 안에 추가).

기존 패턴 (line 23 의 `.page-header` 와 같이 2-space 들여쓰기 + `@layer components` 안) 그대로 따른다.

## 3. verify gate 실행 (W5 §11 negative 17 + §12 positive 28 + build)

```bash
# 3-1. 라인 수 확인
wc -l cha-bio-safety/src/pages/QRPrintPage.tsx           # 기대: 300~360
wc -l cha-bio-safety/src/styles/components.css            # 기대: 514~525

# 3-2. negative gate (17 항목) — 모두 0 또는 위반 없음
F=cha-bio-safety/src/pages/QRPrintPage.tsx
C=cha-bio-safety/src/styles/components.css

grep -cE "var\(--(bg|bg2|bg3|bd|bd2|t1|t2|t3)\)" $F                       # ==0 (OQ #4)
grep -c "rgba(14,165,233" $F                                              # ==0 (OQ #2)
grep -c "16a34a.*22c55e" $F                                               # ==0 (OQ #1 폐기)
grep -cE "fontSize: ?9|fontSize: ?10|fontSize: ?11" $F                    # ==0 (9·10·11 금지)
grep -cE "w-8\b|h-8\b" $F                                                 # ==0 (w-8 함정)
grep -cE "lg:" $F                                                         # ==0 (모바일/데스크톱 분기 없음)
grep -c "IconChevronLeft" $F                                              # ==0
grep -c "<polyline" $F                                                    # ==0
grep -c "M15 19l-7-7 7-7" $F                                              # ==0 (OQ #3 SVG 폐기)
grep -c "function dlBtnStyle\|const dlBtnStyle" $F                        # ==0 (헬퍼 폐기)
grep -cE "📥|🖨️|📷" $F                                                    # ==0 (이모지)
grep -cE "text-status-" $F                                                # ==0 (status- prefix 잘못 — 단 bg-status-* 양방향 alias valid)

# 3-3. positive gate (28 grep) — 모두 ≥ 임계 PASS
grep -c "qr-print-page" $F                                                # ≥3 (외곽 + 헤더 + 카드 등)
grep -c "qr-print-page" $C                                                # ≥10 (신규 ≥10 class — 또는 ≥13)
grep -c "1d4ed8.*0ea5e9" $F                                               # ==1 (inspect 인라인 유지)
grep -cE "bg-status-safe-bar|qr-print-page-dl-btn--public" $F             # ≥1 (OQ #1 치환)
grep -cE "bg-status-info-bg|qr-print-page-info-box" $F                    # ≥1 (OQ #2)
grep -c "ChevronLeft" $F                                                  # ≥2 (import + 사용)
grep -c "from 'lucide-react'" $F                                          # ==1
grep -c "size={16}" $F                                                    # ≥1 (OQ #3)
grep -c "CATEGORIES" $F                                                   # ≥3 (정의 + .map + .find)
grep -c "hasPublic: true" $F                                              # ==1 (소화기만)
grep -c "방화셔터" $F                                                      # ≥1
grep -c "renderCardCanvas" $F                                             # ≥1
grep -c "generatePdf" $F                                                  # ≥1
grep -c "handleDownload" $F                                               # ≥1
grep -c "Apple SD Gothic Neo" $F                                          # ==1
grep -cE "scale = 3|scale: 3" $F                                          # ≥1
grep -cE "cardW.*30|cardW.*70" $F                                         # ≥2
grep -cE "cardH.*38|cardH.*90" $F                                         # ≥2
grep -cE "MARGIN = 10|gap = 1" $F                                         # ≥2
grep -cE "setFillColor\(255, 255, 255\)|setFillColor\(0, 0, 0\)" $F       # ≥2
grep -cE "점검용|점검확인용" $F                                            # ≥2
grep -c "Authorization: \`Bearer" $F                                      # ==1
grep -c "PDF 다운로드 완료" $F                                              # ==1
grep -c "PDF 생성 오류" $F                                                  # ==1
grep -c "체크포인트가 없습니다" $F                                          # ≥2 (renderCardCanvas + handleDownload)
grep -c "QR 코드 출력" $F                                                  # ==1
grep -c "본 소화기는 QR코드로 관리되며" $F                                  # ==1
grep -c "width: 34px" $C                                                  # ≥1 (back-btn 34x34 명시)

# 3-4. 0 byte 변경 가드 (6 src + tailwind.config.js)
git diff --name-only HEAD -- \
  cha-bio-safety/src/App.tsx \
  cha-bio-safety/src/utils/api.ts \
  cha-bio-safety/src/stores/authStore.ts \
  cha-bio-safety/src/styles/tokens.css \
  cha-bio-safety/src/styles/typography.css \
  cha-bio-safety/tailwind.config.js
# 기대: 빈 출력

# 3-5. build gate
cd cha-bio-safety
npx tsc --noEmit                                                          # 0 errors
npm run build                                                             # exit 0
cd ..
```

## 4. atomic commit (2 파일 단일)

```bash
git add cha-bio-safety/src/pages/QRPrintPage.tsx cha-bio-safety/src/styles/components.css
git diff --staged --stat   # 확인: 2 파일만 + insertion/deletion 합리적

git commit -m "$(cat <<'EOF'
feat(quick-260526-b5t): redesign/25-qr-print TSX 변환 (QRPrintPage 330→{N} + components.css 504→{M} 2 파일 단일 atomic + v0.1.1 토큰 className + 비즈 anchor 12 보존 + Lucide ChevronLeft+Download+Printer + dlBtnStyle 폐기 + back 34x34 + public solid bg-status-safe-bar + inspect lin-grad anchor + OQ LOCKED 5 + W5 §1 매핑 verbatim)

- 외곽 + 자체 헤더 + 안내 박스 + 카테고리 목록 + 카드 + 다운로드 버튼 영역 4 sub-area 단일 atomic 변환
- W5 12 섹션 (657 lines) checklist verbatim 적용
- 비즈 anchor 12: imports 6 + CheckPoint + CATEGORIES 7 + renderCardCanvas + generatePdf + handleDownload + busy 3 state + useAuthStore.token + baseUrl + 카피 9 + 모바일/데스크톱 단일 + 자체 헤더 페이지 1 byte 0
- Lucide 3: ChevronLeft size 16 뒤로가기 + Download/Printer 권장 (인라인 SVG path M15 19l-7-7 7-7 line 258~260 폐기)
- dlBtnStyle 헬퍼 함수 line 312~330 (19 lines) 폐기 → 3 state (loading/public/inspect) className+인라인 style 분기
- OQ #1 LOCKED: public lin-grad 16a34a→22c55e 폐기 → solid bg-status-safe-bar
- OQ #1 LOCKED: inspect lin-grad 1d4ed8→0ea5e9 인라인 유지 (§6.4 정식 anchor ≥1 grep)
- OQ #2 LOCKED: 안내 박스 rgba(14,165,233) → bg-status-info-bg + border-status-info-bar
- OQ #3 LOCKED: 인라인 SVG → ChevronLeft size 16 (Download/Printer 권장 동시 import)
- OQ #4 LOCKED: 외곽 hex 8종 var(--bg|bg2|bg3|bd|bd2|t1|t2|t3) → bg-surface-* / border-border-* / bg-border-strong / text-text-*
- OQ #5 LOCKED: 비즈 anchor 12 § 박스 전체 1 byte 0
- components.css 신규 13 class 추가 (scope .qr-print-page) — 외곽/헤더/back-btn 34x34/타이틀/안내 wrap+box/목록/카드/카드 타이틀/버튼 row/dl-btn base/loading/public/inspect 변형
- 6 src 파일 + tailwind.config.js 변경 0 byte (App.tsx / utils/api.ts / stores/authStore.ts / tokens.css / typography.css / tailwind.config.js)
- npm run build PASS
EOF
)"
```

SUMMARY 작성은 별도 commit (`docs(quick-260526-b5t): ...`) — `.planning/quick/260526-b5t-redesign-25-qr-print-tsx-qrprintpage-330/260526-b5t-SUMMARY.md` 에 결과 박제.

  </action>
  <verify>
    <automated>cd /Users/jykevin/Documents/cbc7119-design/cha-bio-safety && npx tsc --noEmit && npm run build 2>&1 | tail -20 && cd .. && bash -c 'F=cha-bio-safety/src/pages/QRPrintPage.tsx; C=cha-bio-safety/src/styles/components.css; echo "=== negative ===" && grep -cE "var\(--(bg|bg2|bg3|bd|bd2|t1|t2|t3)\)" $F && grep -c "rgba(14,165,233" $F && grep -c "16a34a.*22c55e" $F && grep -c "M15 19l-7-7 7-7" $F && grep -c "function dlBtnStyle" $F && echo "=== positive ===" && grep -c "qr-print-page" $F && grep -c "qr-print-page" $C && grep -c "1d4ed8.*0ea5e9" $F && grep -c "ChevronLeft" $F && grep -c "from \"lucide-react\"\|from '"'"'lucide-react'"'"'" $F && grep -c "CATEGORIES" $F && grep -c "Apple SD Gothic Neo" $F && grep -c "Authorization: \`Bearer" $F && grep -c "QR 코드 출력" $F && grep -c "PDF 다운로드 완료" $F && grep -c "본 소화기는 QR코드로 관리되며" $F && echo "=== 0 byte gate ===" && git diff --name-only HEAD -- cha-bio-safety/src/App.tsx cha-bio-safety/src/utils/api.ts cha-bio-safety/src/stores/authStore.ts cha-bio-safety/src/styles/tokens.css cha-bio-safety/src/styles/typography.css cha-bio-safety/tailwind.config.js'</automated>
  </verify>
  <done>
    - QRPrintPage.tsx 라인 수 300~360 (330 ± 30)
    - components.css 라인 수 514~525 (504 + 신규 ≥10 class)
    - W5 §11 negative 17 모두 PASS (var alias 0 / rgba 0 / 16a34a→22c55e 0 / SVG path 0 / dlBtnStyle 0 / 이모지 0 / w-8/h-8 0 / lg:* 0 / IconChevronLeft 0)
    - W5 §12 positive 28 grep 모두 PASS (qr-print-page ≥10 in css / 1d4ed8→0ea5e9 ==1 / bg-status-safe-bar ≥1 / bg-status-info-bg ≥1 / ChevronLeft ≥2 / lucide-react ==1 / size={16} ≥1 / CATEGORIES ≥3 / hasPublic: true ==1 / Apple SD Gothic Neo ==1 / Authorization Bearer ==1 / 카피 verbatim 9 모두 ≥1)
    - 6 src 파일 + tailwind.config.js 변경 0 byte (git diff 빈 출력)
    - npx tsc --noEmit 0 errors
    - npm run build exit 0
    - atomic 1-commit (2 파일) — git log -1 --stat 에 QRPrintPage.tsx + components.css 만 등장
    - 비즈 anchor 12 (imports 6 + CheckPoint + CATEGORIES 7 + renderCardCanvas + generatePdf + handleDownload + busy + useAuthStore + 카피 9 + 단일 컬럼 + 자체 헤더) 1 byte 변경 0
    - OQ LOCKED 5 모두 적용 확인 (public solid + 안내 토큰 + ChevronLeft + 외곽 hex 8종 + 비즈 보존)
  </done>
</task>

</tasks>

<verification>
## 페이지 검증 (사용자 확인)

- `/qr-print` 라우트 진입 시 자체 헤더 (뒤로가기 ChevronLeft + 타이틀 "QR 코드 출력") + 안내 박스 (status-info bg + 2줄 카피 verbatim + `<b>` 강조) + 카테고리 7건 카드 (소화기/소화전/DIV/청정소화약제/완강기/전실제연댐퍼/방화셔터) 표시.
- 소화기 카드만 "점검용 QR PDF" + "점검확인용 QR PDF" 2 버튼, 나머지 6 카드 "점검용 QR PDF" 1 버튼.
- inspect 버튼 클릭 → 그라데이션 `1d4ed8→0ea5e9` 유지 + 30×38mm portrait A4 PDF 다운로드 → 파일명 `{category}_점검용_QR.pdf`.
- public 버튼 (소화기만) 클릭 → solid `bg-status-safe-bar` 표시 + 70×90mm landscape A4 PDF 다운로드 → 파일명 `소화기_점검확인용_QR.pdf` + 상단 안내 카피 "본 소화기는 QR코드로 관리되며,..." 포함.
- 버튼 클릭 중 "생성 중..." 표시 + 로딩 상태 `border-strong` bg + cursor default + 다른 버튼 disabled.
- 다운로드 완료 시 toast.success "PDF 다운로드 완료" / 오류 시 toast.error "PDF 생성 오류" / 빈 응답 시 toast.error "체크포인트가 없습니다".
- 뒤로가기 ChevronLeft 클릭 → `navigate(-1)` 동작.
- 모바일/데스크톱 동일 단일 컬럼 레이아웃.
- App.tsx 변경 0 byte 가드 PASS.

## 빌드 검증

- `npx tsc --noEmit` 0 errors
- `npm run build` exit 0 + QRPrintPage chunk size 정상

## git 검증

- 1 atomic commit (2 파일: QRPrintPage.tsx + components.css)
- main push → cbc7119-preview 자동 배포 (deploy 사용자 컨펌 후)
</verification>

<success_criteria>
- QRPrintPage.tsx 라인 수 300~360 (330 ± 30)
- components.css 라인 수 514~525 (504 + 신규 ≥10~14 class)
- 비즈 anchor 12 (W5 §4 박스 12 항목) 1 byte 변경 0 — grep PASS
- Lucide 3종 교체 (ChevronLeft 필수 + Download/Printer 권장) — `from 'lucide-react'` ==1, `ChevronLeft` ≥2, `size={16}` ≥1
- dlBtnStyle 헬퍼 함수 폐기 — `function dlBtnStyle\|const dlBtnStyle` ==0
- public lin-grad 폐기 — `16a34a.*22c55e` ==0
- inspect lin-grad 유지 — `1d4ed8.*0ea5e9` ==1
- 옛 alias raw 인라인 0 — `var\(--(bg|bg2|bg3|bd|bd2|t1|t2|t3)\)` ==0
- 옛 rgba 안내 박스 raw 0 — `rgba\(14,165,233` ==0
- 옛 SVG path 0 — `M15 19l-7-7 7-7` ==0
- 카피 verbatim 9건 모두 1 byte 0 — "QR 코드 출력" / "PDF 다운로드 완료" / "체크포인트가 없습니다" / "본 소화기는 QR코드로 관리되며" 등 grep PASS
- 6 src 파일 + tailwind.config.js 변경 0 byte (App.tsx / utils/api.ts / stores/authStore.ts / tokens.css / typography.css / tailwind.config.js)
- `npx tsc --noEmit` 0 errors + `npm run build` exit 0
- atomic 1-commit (2 파일 QRPrintPage.tsx + components.css) + SUMMARY 별도 commit
</success_criteria>

<output>
After completion, create `.planning/quick/260526-b5t-redesign-25-qr-print-tsx-qrprintpage-330/260526-b5t-SUMMARY.md` with:
- 변환 후 라인 수 (QRPrintPage.tsx + components.css)
- 비즈 anchor 12 보존 grep 결과
- OQ LOCKED 5 적용 확인
- Lucide 3종 / dlBtnStyle 폐기 / 인라인 SVG 폐기 / public solid / inspect lin-grad 유지 확인
- components.css 신규 class 목록 (≥10)
- 6 src 파일 + tailwind.config.js 0 byte 가드 결과
- build / chunk size 결과
- atomic commit hash + SUMMARY commit hash
- next step (main 머지 + cbc7119-preview 자동 배포 컨펌)
</output>
