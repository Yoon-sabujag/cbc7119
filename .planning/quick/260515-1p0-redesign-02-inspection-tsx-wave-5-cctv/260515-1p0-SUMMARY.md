---
phase: 260515-1p0-redesign-02-inspection-tsx-wave-5-cctv
plan: 01
subsystem: redesign/02-inspection
tags:
  - redesign
  - inspection
  - tsx-conversion
  - design-tokens-v0.1.1
  - tailwind-only
  - stairwell-modal
  - cctv-modal
  - batch-input
  - wave5
dependency_graph:
  requires:
    - 260514-i4r-redesign-02-inspection-tsx (Wave 1 메인 셸 + lucide import 블록)
    - 260514-pnr-wave-1-fix-photobutton-revisitpopup-acce (Wave 1 fix — PhotoButton/RevisitPopup 토큰 정합)
    - 260514-sp7-redesign-02-inspection-tsx-wave-2-5 (Wave 2 — 5 증상 피커 패턴 / 미도입 룰 reference)
    - 260514-tbj-redesign-02-inspection-tsx-wave-4 (Wave 4 — BaeyeonModal/DamperModal 일괄 입력 패턴)
    - 260515-0l7-redesign-02-inspection-tsx-wave-3-div (Wave 3 — DivModal/CompressorModal 변환 패턴)
  provides:
    - Wave 5 변환된 StairwellModal + CctvModal (특별피난계단 일괄 입력 + DVR 13대 일괄 입력)
    - resultMiniCls / resultIcon local 헬퍼 패턴 (각 모달 격리 정의)
    - Server lucide import (mh-action "설비 현황")
    - 후속 PowerPanelModal / ParkingGateModal 트랙용 fixed reference point
  affects:
    - cha-bio-safety/src/pages/InspectionPage.tsx
tech_stack:
  added:
    - "lucide-react Server (CctvModal mh-action '설비 현황' 좌측 아이콘)"
  patterns:
    - "resultMiniCls 헬퍼 (active/inactive × normal/caution/bad 분기) — 각 모달 local 정의 (Wave 4 DamperModal stair 모드와 동일)"
    - "resultIcon 헬퍼 (CheckCircle2/AlertTriangle/XCircle 매핑) — 각 모달 local 정의"
    - "segmented 버튼 3 상태 (active/done/inactive) — selectedSW 가 done 보다 우선"
    - "batch-grid 2열 (계단실: swDef.floors.slice / CCTV: Math.ceil(13/2) 좌7우6)"
    - "dvr-tile 두 줄 분리 (라벨 text-label + desc text-caption, 시안 권위)"
key_files:
  modified:
    - cha-bio-safety/src/pages/InspectionPage.tsx (5872 → 5937 줄, +168/-103)
decisions:
  - "resultMiniCls / resultIcon 헬퍼는 각 모달 local 정의 (파일 상단 공유 영역 신설하지 않음 — 다른 wave 영역 비변경 원칙 유지)"
  - "dvr-tile 두 줄 분리 채택 (시안 권위 + 노안 친화 13/12px) — 한 줄 baseline justify-between 대안 폐기"
  - "segmented done-mark 텍스트 ✓ 유지 (1글자 span — 시안 권위, lucide Check 대안 폐기)"
  - "헤더 background 시안 권위 따라 bg-surface-page (현재 코드 var(--bg2) 와 다름 — 시각 차이 의도)"
  - "CCTV 헤더 텍스트 'DVR 12대' 코드 그대로 보존 (시안 '13대' 와 차이 — 코드 권위 / Wave 5 는 시각만 변환)"
  - "결과 토큰 border-safe / border-warning / border-danger 채택 (Wave 3 0l7 / Wave 4 tbj DamperModal stair 모드 패턴과 동일, -bar 변형 폐기)"
  - "btnStyle / resultBtnStyle 인라인 헬퍼 제거 (Tailwind 분기로 대체, opt.color/opt.bg 인라인 의존 사라짐 → unused)"
metrics:
  duration: "단일 세션"
  completed: 2026-05-15
  tasks_completed: 2
  files_modified: 1
  line_count_before: 5872
  line_count_after: 5937
  diff: "+168/-103"
---

# 260515-1p0 (Wave 5) — redesign/02-inspection TSX StairwellModal + CctvModal Summary

**One-liner:** InspectionPage.tsx 의 StairwellModal(특별피난계단 5 계단실 일괄 입력) + CctvModal(DVR 13대 일괄 입력) 2 컴포넌트를 4차 시안 권위로 v0.1.1 디자인 토큰 + Tailwind only 로 교체. **260505-cib photoKey 1건 대표 부여 정책(caution/bad 우선 → 첫 cp/층) handleSave 한 줄도 변경 없음** (전층/전 DVR 중복 저장 버그 재발 방지). **증상 피커 미도입** (자동화 5종 카테고리 아님 — 메모리 룰).

---

## What was done

### Task 1 — StairwellModal v0.1.1 토큰 + Tailwind 변환 (commit e6377ab)

**범위:** `function StairwellModal` 본문 (라인 255~480 → 255~517, +96/-59)

**시각 변환:**
- 외곽 모달: `bg-surface-page flex flex-col overflow-hidden` + 인라인 화이트리스트(top/bottom/transform/transition)
- 헤더: StairsIcon (커스텀 SVG 18px) + text-[16px]/text-caption — 이모지 🚪 제거 (1건)
- 계단실 selectedSW segmented (5 버튼) — 활성/done/비활성 3 상태:
  - 활성: `border-[1.5px] border-accent bg-accent text-text-on-accent`
  - done: `border-[1.5px] border-safe bg-safe-bg text-safe`
  - 비활성: `border border-border-strong bg-surface-page text-text-secondary`
- swDoneCount alert-success: `bg-safe-bg border border-safe text-safe text-label` + CheckCircle2 (14px)
- batch-grid 2열: `swDef.floors.slice(0, leftCount)` / `slice(leftCount)` — 시안 1:1
- floor-tile: `bg-surface-raised border border-border-default rounded-md px-2 pt-2 pb-1.5`
- result-mini (pill + lucide): CheckCircle2/AlertTriangle/XCircle 12px + 한글 라벨 (정상/주의/불량) — resultMiniCls + resultIcon 로컬 헬퍼
- 메모/사진: text-caption(12) 라벨 + textarea text-label(13) + PhotoButton 그대로
- submitError/justSaved: bg-danger-bg/bg-safe-bg + CheckCircle2
- footer-bar: btn-close-mini + btn-save §6.4 그라디언트(linear-gradient(135deg,#1d4ed8,#0ea5e9) + 시그니처 그림자 0 4px 14px rgba(37,99,235,0.35))

**비즈니스 보존 (한 줄도 변경 없음):**
- props 시그니처 (group, allCheckpoints, records, monthRecords, scheduleItems, onClose, onSave)
- state 7개 (selectedSW / floorResults / memo / submitting / justSaved / submitError / visible)
- useRef prevSW
- useEffect 2개 (rAF mount + prevSW 비교 후 init reset)
- useMemo swCPs / swDef = STAIRWELLS.find / swDoneCount
- useInspectionRevisitPopup({ checkpointId: swCPs[0]?.id ?? null, category: '특별피난계단', monthRecords, scheduleItems })
- **handleSave 본문 — 260505-cib photoKey 1건 대표 부여:** photoTargetCp = caution/bad 우선 → 첫 cp / for-loop keyForCp 분배
- usePhotoUpload / PhotoButton / navigate('/remediation/...')
- STAIRWELLS.find / swDef.floors.slice / swCPs.find(c => c.floor === floor) 매핑

**dead code 청소:** btnStyle / resultBtnStyle 인라인 헬퍼 함수 제거 (Tailwind 분기로 대체 → opt.color/opt.bg 인라인 의존 사라짐 → unused).

### Task 2 — CctvModal v0.1.1 토큰 + Tailwind 변환 (commit 4ecb66e)

**범위:** `function CctvModal` 본문 (라인 485~645 → 522~710, +72/-44)

**lucide import 보강:** 라인 24 카테고리 lucide 블록 1단어 추가 (`Server,`) — 다른 import 영역 비변경.

**시각 변환:**
- 외곽 모달 / 헤더: Task 1 과 동일 패턴
- 헤더 아이콘: Video (lucide 18px) — 이모지 📹 제거 (1건)
- mh-title: text-[16px] / mh-sub: `B1F 방재센터 DVR 12대` (코드 텍스트 그대로 보존)
- allDone 미니 뱃지: `text-caption text-safe bg-safe-bg border border-safe` + CheckCircle2 12px — ✓ 이모지 제거
- mh-action 버튼 "설비 현황": `border border-border-strong bg-surface-raised text-text-secondary text-caption` + Server 13px → navigate('/cctv') — 📹 이모지 제거 (2건째)
- doneCnt alert-success: Task 1 과 동일 + CheckCircle2 14px
- batch-grid 2열: Math.ceil(CCTV_DVRS.length/2) = 7 좌/우 분할 (좌7/우6)
- dvr-tile: 시안 권위 따라 두 줄 분리
  - 라벨: `text-label font-bold text-text-primary leading-tight`
  - desc: `text-caption text-text-tertiary mt-0.5 mb-1.5 leading-tight min-h-[1.25em]`
- result-mini: Task 1 과 동일 패턴 — resultMiniCls + resultIcon 로컬 헬퍼 (각 모달 격리)
- 메모/사진/submitError/justSaved/footer-bar: Task 1 과 동일

**비즈니스 보존 (한 줄도 변경 없음):**
- props 시그니처 (allCheckpoints, records, onClose, onSave — monthRecords/scheduleItems 없음, useInspectionRevisitPopup 호출 없음)
- state 6개 (dvrResults / memo / submitting / justSaved / submitError / visible)
- useEffect 2개 (rAF mount + cctvCPs init records 로드 deps:[cctvCPs])
- useMemo cctvCPs / doneCnt / allDone
- **handleSave 본문 — 260505-cib photoKey 1건 대표 부여:** photoTargetCp = caution/bad 우선 → 첫 cp / for-loop keyForCp 분배
- usePhotoUpload / PhotoButton / navigate('/cctv')
- (() => { const half = Math.ceil(CCTV_DVRS.length / 2); ... })() IIFE 본문 / renderCard 클로저 (cp 매핑, null 가드, curResult)
- CCTV_DVRS.slice(0, half) / slice(half) / cctvCPs.find(c => c.locationNo === dvr.no) 짝 매핑

**dead code 청소:** resultBtnStyle 인라인 헬퍼 제거 (Tailwind 분기로 대체).

---

## Verification

| Gate | Expected | Result |
|------|----------|--------|
| Wave 5 변환 영역 (255~710) 옛 토큰 var(--bg/bg2/bd/bd2/acl/t1~3/safe/warn/danger/fire) 사용 | 0 | **0** (저장 비활성 인라인은 var(--border-default)/var(--text-tertiary) — Wave 1 화이트리스트 룰 — grep 패턴이 매치되지 않음) |
| Wave 5 변환 영역 9/10/11px (fontSize/text-[Npx]) | 0 | **0** |
| 이모지 📹/📷/✅/⚠️/🚨/🚪/❌ (변환 영역 내) | 0 (segmented done-mark `✓` 텍스트 1건 허용) | **0** (체크용 ✓ 는 별도 ASCII — 위 정규식이 매치하지 않음) |
| segmented done-mark `✓` 텍스트 (1건만 허용) | 1 | **1** (StairwellModal 라인 296 / `{done && !isActive && <span ...>✓</span>}`) |
| symptomPick 신설 (변환 영역) | 0 | **0** |
| photoTargetCp (260505-cib 보존) | ≥4 (각 모달 2건) | **4** (StairwellModal 2건 + CctvModal 2건) |
| StairwellModal useInspectionRevisitPopup category '특별피난계단' | 1 | **1** |
| STAIRWELLS.find + swDef.floors.slice (Stairwell 영역) | ≥3 (find 1 + slice 2) | **3** |
| CCTV_DVRS.slice (Cctv 영역) | 2 | **2** (slice(0, half) + slice(half)) |
| cctvCPs.find(c => c.locationNo === dvr.no) | 1 | **1** |
| navigate('/cctv') (Cctv 영역) | 1 | **1** |
| navigate('/remediation/' (Stairwell 영역) | 1 | **1** |
| onSave(cp.id, ... ) (각 모달 1건) | ≥2 | **2** |
| StairsIcon/Video/Server lucide 사용 (변환 영역) | ≥3 (Stairs + Video + Server) | **3** |
| Server lucide import (파일 상단 + 사용처) | ≥2 | **2** (라인 24 import + 라인 614 `<Server size={13}>`) |
| BaeyeonModal 시작 라인 변경 없음 | 변경 없음 (라인 shift only 허용) | **718** (이전 653 → 718, line shift 65 — body 내용 한 줄도 변경 없음) |
| TypeScript compile | 0 에러 | **0** |
| Production build | 통과 | **통과** (chunk hash `DHZDEHUm` → `Quuu_RGk` 자동 변경 — 캐시 무효화) |
| Diff scope | 단일 파일 | **단일** (`cha-bio-safety/src/pages/InspectionPage.tsx`, +168/-103) |

---

## Business Logic Preservation Check (260505-cib 메모리 룰 강제)

**StairwellModal.handleSave** — 라인 309~333 (이전 라인 309~333) **byte-for-byte 동일:**
```typescript
const photoKey = await photo.upload()
const photoTargetCp = photoKey
  ? (swCPs.find(cp => {
      const r = floorResults[cp.id] ?? 'normal'
      return r === 'caution' || r === 'bad'
    }) ?? swCPs[0])
  : null
for (const cp of swCPs) {
  const keyForCp = photoTargetCp && cp.id === photoTargetCp.id ? photoKey : undefined
  await onSave(cp.id, floorResults[cp.id] ?? 'normal', memo, keyForCp ?? undefined)
}
```

**CctvModal.handleSave** — 라인 562~586 **byte-for-byte 동일:**
```typescript
const photoKey = await photo.upload()
const photoTargetCp = photoKey
  ? (cctvCPs.find(cp => {
      const r = dvrResults[cp.id] ?? 'normal'
      return r === 'caution' || r === 'bad'
    }) ?? cctvCPs[0])
  : null
for (const cp of cctvCPs) {
  const keyForCp = photoTargetCp && cp.id === photoTargetCp.id ? photoKey : undefined
  await onSave(cp.id, dvrResults[cp.id] ?? 'normal', memo, keyForCp ?? undefined)
}
```

→ 전층/전 DVR 중복 저장 버그 재발 방지 정책 (caution/bad 우선 → 첫 cp/층 1건만 attach) 보존 완료.

---

## Deviations from Plan

**None — 플랜대로 실행.**

- 메모리 룰(자동화 5종 카테고리 아님 → 증상 피커 미도입) 그대로 지킴
- 260505-cib (handleSave photoKey 1건 대표 부여) 그대로 지킴
- 헬퍼 위치 결정(local 정의)은 플랜의 "각 모달 안에 local helper" 옵션 채택
- dvr-tile 두 줄 분리 채택(플랜의 "권장" 옵션)
- 인라인 화이트리스트 외 인라인 style 0건 (변환 영역)
- BaeyeonModal/DamperModal/PowerPanelModal/ParkingGateModal/DivModal/CompressorModal 영역 한 줄도 비변경

---

## Out of scope (이 작업에서 처리 안 함)

- BaeyeonModal — Wave 4 (tbj) 완료, 라인 718 (이전 653 → +65 shift only — body 한 줄도 변경 없음)
- DamperModal — Wave 4 (tbj) 완료
- PowerPanelModal — 후속 quick 트랙
- ParkingGateModal — 후속 quick 트랙
- DivModal / CompressorModal / DivUnderPicker / DivTrendSubview — Wave 3 (0l7) 완료
- 5 증상 피커 (유도등/소화기/소화전/방화셔터/전실제연댐퍼) — Wave 2 (sp7) / Wave 4 (tbj) 완료
- 메인 page render / InspectionModal 셸 / 5 보조 컴포넌트 — Wave 1 (i4r) 완료
- 외부 컴포넌트 (PhotoButton / InspectionRevisitPopup / AccessBlockedPopup) — pnr 정합 완료
- API / hooks / utils 수정 금지
- 다른 페이지 (.tsx) 수정 금지
- STAIRWELLS / CCTV_DVRS / CATEGORY_GROUPS / INSPECT_RESULT_OPTIONS 상수 변경 금지

---

## Commits

- `e6377ab` — feat(260515-1p0): task 1 — StairwellModal v0.1.1 토큰 + Tailwind 변환
- `4ecb66e` — feat(260515-1p0): task 2 — CctvModal v0.1.1 토큰 + Tailwind 변환

---

## Self-Check: PASSED

- [x] Both task commits exist (e6377ab, 4ecb66e) in git log
- [x] InspectionPage.tsx modified (5872 → 5937 줄, +168/-103, 단일 파일)
- [x] StairwellModal function at line 255 (확인됨)
- [x] CctvModal function at line 522 (확인됨)
- [x] BaeyeonModal function at line 718 (이전 653 → +65 line shift only, body 비변경)
- [x] Server lucide import at line 24 (확인됨)
- [x] Server lucide usage at line 614 (`<Server size={13}>`)
- [x] photoTargetCp count = 4 (각 모달 2건 — handleSave 보존)
- [x] symptomPick neuf = 0 (증상 피커 미도입 룰)
- [x] tsc 0 / npm run build 통과 (chunk hash DHZDEHUm → Quuu_RGk)
- [x] OLD_TOKENS = 0 / 9/10/11px = 0 / 이모지 정규식 매치 0 (segmented done-mark ✓ 텍스트만 1건 허용)

cbc7119 디자인 리포 한정 — 운영 PWA (cha-bio-safety) 영향 0.
