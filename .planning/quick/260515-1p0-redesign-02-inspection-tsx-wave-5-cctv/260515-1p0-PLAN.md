---
phase: 260515-1p0-redesign-02-inspection-tsx-wave-5-cctv
plan: 01
type: execute
wave: 5
depends_on:
  - 260514-i4r-redesign-02-inspection-tsx                      # Wave 1 — InspectionPage 메인 셸 변환 패턴 권위 + lucide import 블록 정착
  - 260514-pnr-wave-1-fix-photobutton-revisitpopup-acce        # Wave 1 fix — PhotoButton / InspectionRevisitPopup lucide+토큰 정합
  - 260514-sp7-redesign-02-inspection-tsx-wave-2-5             # Wave 2 — 5 증상 피커 변환 패턴 (CCTV/특별피난계단은 미도입 룰 reference)
  - 260514-tbj-redesign-02-inspection-tsx-wave-4               # Wave 4 — BaeyeonModal/DamperModal 일괄 입력 패턴 (segmented + batch grid + result-mini 동일 톤)
  - 260515-0l7-redesign-02-inspection-tsx-wave-3-div           # Wave 3 — DivModal/CompressorModal §7.1 결과 picker / lucide 매핑 / footer 패턴 1:1 권위 (인접 직전)
files_modified:
  - cha-bio-safety/src/pages/InspectionPage.tsx
autonomous: true
requirements:
  - REDESIGN-02-INSPECTION-TSX-WAVE5
tags:
  - redesign
  - inspection
  - tsx-conversion
  - design-tokens-v0.1.1
  - tailwind-only
  - stairwell-modal
  - cctv-modal
  - batch-input
  - wave5

must_haves:
  truths:
    - "StairwellModal (라인 255~480, 약 230줄) + CctvModal (라인 485~645, 약 160줄) 2 컴포넌트의 비즈니스 로직(props 시그니처 / state / useRef / useMemo / useEffect / 핸들러 / 사진 1건 대표 부여 룰 / fetch / onSave 호출 / KST timestamp / useInspectionRevisitPopup 호출 / STAIRWELLS / CCTV_DVRS 매핑 / records[cp.id] 완료 표시 / cctvCPs locationNo 매핑)이 100% 보존된다 (변경 전후 동작 동일)"
    - "**260505-cib 메모리 룰 — photoKey 1건 대표 부여 정책**: StairwellModal.handleSave 와 CctvModal.handleSave 안의 `const photoTargetCp = photoKey ? (...find(cp => caution/bad) ?? swCPs[0]/cctvCPs[0]) : null` 분기 + `for ... keyForCp = photoTargetCp && cp.id === photoTargetCp.id ? photoKey : undefined` 루프가 한 줄도 변경 없이 보존된다 (전층/전 DVR 중복 저장 버그 재발 방지)"
    - "StairwellModal: 계단실 선택 segmented (5 버튼 1/2/3/4/5) + 선택된 계단실의 좌/우 floor-tile 2열 그리드 + result-mini (pill + lucide + 라벨) 시각이 시안 권위(`inspection-sketch-stairwell-cctv.html` 333~525 라인) 1:1 매핑된다. 계단실 done 표시(✓)는 lucide `<Check size={12}>` 또는 텍스트 ✓ 미니 마크 — done 시 `is-done` 톤(safe border + safe-bg + text-safe) 적용"
    - "CctvModal: lucide Video 헤더 아이콘 + mh-sub `· B1F 방재센터 DVR 13대` + 우측 mh-action 버튼 (lucide Server + '설비 현황' 텍스트 → /cctv 네비) + DVR 13대 dvr-tile 2열 그리드 (좌7/우6 — Math.ceil(13/2)=7) + result-mini 시각이 시안(`inspection-sketch-stairwell-cctv.html` 660~890 라인) 1:1 매핑된다"
    - "결과 picker (정상/주의/불량) 3 버튼 — §7.1 일관성 (pill + lucide CheckCircle2/AlertTriangle/XCircle 12x12 + status outline + tinted bg) 시각화. 0l7/tbj 와 동일 톤이되 floor-tile/dvr-tile 안의 result-mini 는 컴팩트 사이즈(`text-caption font-bold` + `gap-1`)"
    - "헤더 — StairwellModal: 커스텀 `<StairsIcon size={18} className=\"text-text-secondary\" />` (이미 라인 31에서 import 됨) + mh-title `{group.labels[0]}` + mh-sub `· {group.labels.slice(1).join(' · ')}` (1차 시안과 동일 인라인 spread). CctvModal: lucide `<Video size={18} className=\"text-text-secondary\" />` + 'CCTV 점검' + `· B1F 방재센터 DVR 13대` + mh-action `<Server size={13}>` `설비 현황` → /cctv navigate"
    - "StairwellModal/CctvModal 본문(JSX) 안에 인라인 style={{...}} 의 금지 키(color / background / padding / margin / fontSize / fontWeight / borderRadius / display / flex* / grid* / gap / width / height — 정적값) 사용이 0개이다 (i4r/sp7/tbj/0l7 화이트리스트와 동일 — position / top/left/right/bottom 동적 NAV_BOTTOM / transform(슬라이드업 동적) / transition cubic-bezier / animation / §6.4 그라디언트 CTA / boxShadow 그라디언트 시그니처 그림자 / SVG attribute / bg-[rgba()] arbitrary 화이트리스트만 허용)"
    - "StairwellModal/CctvModal 변환 영역에서 9 / 10 / 11px 폰트 사이즈 사용이 0개이다 (text-caption(12) ~ text-display(28) 또는 text-[Npx] arbitrary 12 이상만)"
    - "옛 토큰(var(--bg) / var(--bg2) / var(--bg3) / var(--bd) / var(--bd2) / var(--acl) / var(--t1) / var(--t2) / var(--t3) / var(--safe) / var(--warn) / var(--danger) / var(--fire)) 사용이 변환 영역(라인 255~645) 내 0개이다"
    - "이모지(📹 / ✅ / ✓ / 📷) 사용이 변환 영역 내 0개이다 — lucide(Video / CheckCircle2 / Check / Camera) 로 통일"
    - "**증상 피커 도입 X — StairwellModal/CctvModal 은 자동화 5종(유도등/소화기/소화전/방화셔터/전실제연댐퍼)이 아니므로 symptomPick 신설 금지** (메모리 룰). 메모는 그대로 placeholder='특이사항을 입력하세요' 유지 + bar-label `특이사항 + 사진` 표시"
    - "Wave 4 BaeyeonModal/DamperModal (라인 ~647~) 본문 + Wave 5 변환 영역 직후 BaeyeonModal 시작 라인(현재 653) 은 한 줄도 변경 없이 보존된다"
    - "TypeScript 컴파일 0 에러, npm run build 통과 (chunk hash 자동 변경 — InspectionPage.tsx 본체 수정)"
  artifacts:
    - path: "cha-bio-safety/src/pages/InspectionPage.tsx"
      provides: "Wave 5 변환 완료된 StairwellModal + CctvModal. 260505-cib photoKey 1건 대표 부여 정책 한 줄도 변경 없음. 계단실 일괄 입력 (좌/우 2열 grid) + DVR 13대 일괄 입력 (좌7/우6 2열 grid). lucide StairsIcon/Video/Server/Camera/CheckCircle2/AlertTriangle/XCircle/Check 통일. result-mini pill + status outline+tinted bg. 인라인 style 금지 키 0건 + 9/10/11px 0건 + 옛 토큰 0건 + 이모지 0건 + 증상 피커 미도입."
      min_lines: 5700
      contains: "function StairwellModal, function CctvModal, photoTargetCp, swCPs, dvrResults, cctvCPs, STAIRWELLS, CCTV_DVRS, useInspectionRevisitPopup, '특별피난계단'"
  key_links:
    - from: "cha-bio-safety/src/pages/InspectionPage.tsx (StairwellModal/CctvModal)"
      to: "tailwind.config.js fontSize tokens + colors (status / text / surface / border / accent)"
      via: "Tailwind utility 클래스 (text-caption / bg-surface-raised / text-text-primary / border-border-default / bg-{safe|warning|danger}{-bar|-bg}? / text-{safe|warning|danger})"
      pattern: "text-(caption|label|body-sm|body|title)|bg-surface-(page|raised|sunken|active)|text-text-(primary|secondary|tertiary|on-accent)|border-border-(default|strong)|bg-(safe|warning|danger)(-bar|-bg)?|text-(safe|warning|danger)"
    - from: "cha-bio-safety/src/pages/InspectionPage.tsx (StairwellModal/CctvModal handleSave)"
      to: "onSave(cp.id, result, memo, keyForCp ?? undefined) 직렬 루프"
      via: "260505-cib photoKey 1건 대표 부여 (caution/bad 우선 → 첫 cp/층) — 한 줄도 변경 금지"
      pattern: "photoTargetCp\\s*=\\s*photoKey\\s*\\?\\s*\\([\\s\\S]*?find\\(cp\\s*=>\\s*\\{[\\s\\S]*?return\\s+r\\s*===\\s*'caution'\\s*\\|\\|\\s*r\\s*===\\s*'bad'"
    - from: "cha-bio-safety/src/pages/InspectionPage.tsx (StairwellModal)"
      to: "STAIRWELLS 상수 (5 계단실, floors / leftCount)"
      via: "swCPs 매핑 (allCheckpoints.filter(cp => group.categories.includes(cp.category) && cp.locationNo === `S${selectedSW}`)) + swDef.floors.slice(0, swDef.leftCount) / slice(swDef.leftCount) 좌/우 분할"
      pattern: "STAIRWELLS\\.find\\(s\\s*=>\\s*s\\.id\\s*===\\s*selectedSW\\)|swDef\\.floors\\.slice"
    - from: "cha-bio-safety/src/pages/InspectionPage.tsx (CctvModal)"
      to: "CCTV_DVRS 상수 (13 DVR, label + desc + no)"
      via: "cctvCPs 매핑 (allCheckpoints.filter(cp => cp.category === 'CCTV')) + Math.ceil(CCTV_DVRS.length/2) 좌/우 분할 + cctvCPs.find(c => c.locationNo === dvr.no) 짝 매핑"
      pattern: "CCTV_DVRS\\.slice\\(0,\\s*half\\)|CCTV_DVRS\\.slice\\(half\\)|cctvCPs\\.find\\(c\\s*=>\\s*c\\.locationNo\\s*===\\s*dvr\\.no\\)"
    - from: "cha-bio-safety/src/pages/InspectionPage.tsx (StairwellModal/CctvModal)"
      to: "lucide-react (StairsIcon 커스텀 / Video / Server / Camera / Check / CheckCircle2 / AlertTriangle / XCircle)"
      via: "이모지(📹/✅/✓/📷) 제거 + lucide 통일 — Server 는 신규 import (Wave 1 i4r import 블록 확장)"
      pattern: "import.*Server.*from\\s+['\"]lucide-react['\"]|Server,"
---

<objective>
InspectionPage TSX 변환 Wave 5 — 4차 시안(`inspection-sketch-stairwell-cctv.html` 901줄) 을 단일 진실 소스로 삼아 `cha-bio-safety/src/pages/InspectionPage.tsx` 의 **StairwellModal (라인 255~480, 약 230줄) + CctvModal (라인 485~645, 약 160줄)** 2 영역(총 약 400줄)을 v0.1.1 디자인 토큰 + Tailwind only 로 교체한다.

**Purpose:**
- Wave 1 (i4r) / Wave 2 (sp7) / Wave 3 (0l7) / Wave 4 (tbj) 의 변환 패턴(인라인 금지 키 0건 + 9/10/11px 0건 + lucide 통일 + tokens.css 자동 라이트/다크 분기 + §7.1 결과 picker 일관성 + §6.4 그라디언트 CTA) 을 마지막 batch-input 영역(특별피난계단/CCTV)에도 정착
- 두 모달 모두 일괄 입력 패턴(좌/우 2열 grid + 셀별 result-mini) — Wave 4 DamperModal stair 모드 / 0l7 의 segmented + result picker 톤과 직접 연속
- **자동화 5종 카테고리 아님 → 증상 피커 미도입** (메모리 룰 — 자동화 5종은 유도등/소화기/소화전/방화셔터/전실제연댐퍼)
- 260505-cib 의 photoKey 1건 대표 부여 정책(caution/bad 우선 → 첫 cp/층) 한 줄도 손대지 않고 시각만 교체
- BaeyeonModal (라인 653~) 직전까지가 Wave 5 경계 — 이후 영역은 별도 wave (이미 tbj 완료) 또는 후속 트랙 (PowerPanelModal/ParkingGateModal)

**Output:**
- 비즈니스 로직 100% 보존하면서 Wave 5 변환 영역(라인 255~645) 의 마크업/스타일이 4차 시안과 1:1 매칭된 새 `InspectionPage.tsx`
- BaeyeonModal/DamperModal 본문 + 그 외 모든 영역은 **한 줄도 변경 없이 그대로 보존**

**Out of scope (Wave 5 범위 외):**
- `BaeyeonModal` (라인 653~) — Wave 4 (tbj) 에서 완료, 본 변환에서 손대지 않음
- `DamperModal` — Wave 4 (tbj) 에서 완료
- `PowerPanelModal` (라인 2195~) — 후속 quick 트랙
- `ParkingGateModal` — 후속 quick 트랙
- `DivModal` / `CompressorModal` / `DivUnderPicker` / `DivTrendSubview` — Wave 3 (0l7) 완료
- 5 증상 피커 — Wave 2 (sp7) / Wave 4 (tbj) 에서 완료
- 메인 page render / InspectionModal 셸 / 5 보조 컴포넌트 — Wave 1 (i4r) 완료
- 외부 컴포넌트(PhotoButton / InspectionRevisitPopup / AccessBlockedPopup) 본체 수정 금지 — pnr 에서 이미 토큰 정합
- API / hooks / utils 수정 금지
- 다른 페이지 (.tsx) 수정 금지
- STAIRWELLS / CCTV_DVRS / CATEGORY_GROUPS / INSPECT_RESULT_OPTIONS 상수 변경 금지 (상위 정의부, 라인 ~170 부근)
- 자동화 5종 카테고리 아님 → **symptomPick state / setter 신설 절대 금지**

**핵심 메모리 룰 강제 (작업 전 재확인):**
- **"260505-cib — StairwellModal/CctvModal/DamperModal stair photoKey 1건 대표 부여"** — handleSave 안 `photoTargetCp` 분기 + for-loop 의 `keyForCp` 분배 한 줄도 변경 금지. caution/bad 우선 fallback 첫 cp/층 정책 그대로
- **"자동화 5종 카테고리 룰"** — 특별피난계단/CCTV 는 5종에 미포함. **증상 피커(symptomPick) 신설 금지**. 메모는 `placeholder='특이사항을 입력하세요'` 그대로 (현재 코드와 동일 — Wave 5 는 placeholder 도 변경 안 함)
- **"디자인 변경 전 상의 필수"** — Wave 1/2/3/4 의 변환 패턴 외 새로운 디자인 결정이 필요하면 시안과 일치 확인. 시안이 권위
- **"디자인 조정은 시안 먼저"** — 시안 `inspection-sketch-stairwell-cctv.html` 901줄이 마크업/색/구조 단일 진실 소스
- **"iOS web getUserMedia / KST timestamp / 사진 업로드 한 줄도 변경 금지"** — `photo.upload()` 호출 / usePhotoUpload 훅 / PhotoButton 호출 시그니처 보존
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/STATE.md

# Wave 1 (i4r) — InspectionPage 변환 패턴 권위 + lucide import 블록 정착
@.planning/quick/260514-i4r-redesign-02-inspection-tsx/260514-i4r-PLAN.md

# Wave 4 (tbj) — BaeyeonModal/DamperModal 일괄 입력 변환 패턴 (인접 — segmented 바 + batch grid + result picker §7.1)
@.planning/quick/260514-tbj-redesign-02-inspection-tsx-wave-4/260514-tbj-PLAN.md

# Wave 3 (0l7) — DivModal/CompressorModal 변환 패턴 (직전 wave — segmented / footer / §7.1 / §6.4 그라디언트 CTA 권위)
@.planning/quick/260515-0l7-redesign-02-inspection-tsx-wave-3-div/260515-0l7-PLAN.md
@.planning/quick/260515-0l7-redesign-02-inspection-tsx-wave-3-div/260515-0l7-SUMMARY.md

# 4차 sketch HTML — Wave 5 의 마크업/색/구조 단일 진실 소스 (901줄, 일괄 입력 2열 그리드 권위)
@cha-bio-safety/docs/redesign-context/02-inspection/sketch/inspection-sketch-stairwell-cctv.html
@cha-bio-safety/docs/redesign-context/02-inspection/sketch/inspection-audit-design-system.html

# 디자인 시스템 v0.1.1 — §6.1 Progress / §6.4 그라디언트 / §7.1 결과 picker 일관성 / §7.2 카테고리 아이콘 매핑 / §7.3 결과 아이콘
@cha-bio-safety/docs/design-system.md

# 변환 대상 — 비즈니스 로직 100% 보존
@cha-bio-safety/src/pages/InspectionPage.tsx

# 토큰 / Tailwind 카탈로그
@cha-bio-safety/src/styles/tokens.css
@cha-bio-safety/tailwind.config.js

# 공통 UI / 외부 컴포넌트 — 그대로 사용 (수정 금지)
@cha-bio-safety/src/components/ui/icons.tsx
@cha-bio-safety/src/components/PhotoButton.tsx
@cha-bio-safety/src/components/InspectionRevisitPopup.tsx
</context>

<interfaces>
<!-- 이 영역이 의존하는 외부/내부 시그니처. 변경 금지. -->
<!-- Executor 는 아래 인터페이스를 직접 사용 (codebase 추가 탐색 불필요). -->

# 1. StairwellModal — props (변경 금지)
function StairwellModal({ group, allCheckpoints, records, monthRecords, scheduleItems, onClose, onSave }: {
  group:          typeof CATEGORY_GROUPS[0]    // { labels:['특별피난계단','피난·방화시설','방화문'], icon:'🚪', categories:['특별피난계단'], ... }
  allCheckpoints: CheckPoint[]
  records:        Record<string, CheckResult>
  monthRecords:   Record<string, MonthRecordEntry>
  scheduleItems:  ScheduleItem[]
  onClose:        () => void
  onSave:         (cpId: string, result: CheckResult, memo: string, photoKey?: string) => Promise<void>
})
// state 7개 — 한 줄도 변경 금지:
//   selectedSW(number|null) / floorResults(Record<string,CheckResult>) / memo(string) /
//   submitting / justSaved / submitError / visible
// useRef 1개: prevSW
// useEffect 2개:
//   - requestAnimationFrame(() => setVisible(true)) — 슬라이드업 mount
//   - selectedSW 변경 시 prevSW 비교 → init records 로드 + reset (eslint-disable-line)
// useMemo: swCPs = allCheckpoints.filter(cp => group.categories.includes(cp.category) && cp.locationNo === `S${selectedSW}`)
// 헬퍼: swDef = STAIRWELLS.find(s => s.id === selectedSW)
// 헬퍼: swDoneCount = swCPs.filter(cp => records[cp.id]).length
// useInspectionRevisitPopup({ checkpointId: swCPs[0]?.id ?? null, category: '특별피난계단', monthRecords, scheduleItems })
//   → { popupState, dismiss }

# 2. StairwellModal.handleSave — 한 줄도 변경 금지 (260505-cib 메모리 룰)
const handleSave = async () => {
  if (!swDef || swCPs.length === 0) return
  setSubmitting(true); setSubmitError(null)
  try {
    const photoKey = await photo.upload()
    // 사진 1건 대표 부여 — caution/bad 우선, 없으면 첫 층
    const photoTargetCp = photoKey
      ? (swCPs.find(cp => {
          const r = floorResults[cp.id] ?? 'normal'
          return r === 'caution' || r === 'bad'
        }) ?? swCPs[0])
      : null
    for (const cp of swCPs) {
      const keyForCp = photoTargetCp && cp.id === photoTargetCp.id ? photoKey : undefined
      await onSave(cp.id, floorResults[cp.id] ?? 'normal', memo, keyForCp ?? undefined)
    }
    setJustSaved(true); setMemo(''); photo.reset()
  } catch (e: any) {
    setSubmitError(e.message ?? '저장 오류')
  } finally {
    setSubmitting(false)
  }
}

# 3. CctvModal — props (변경 금지)
function CctvModal({ allCheckpoints, records, onClose, onSave }: {
  allCheckpoints: CheckPoint[]
  records:        Record<string, CheckResult>
  onClose:        () => void
  onSave:         (cpId: string, result: CheckResult, memo: string, photoKey?: string) => Promise<void>
})
// state 6개 — 한 줄도 변경 금지:
//   dvrResults(Record<string,CheckResult>) / memo(string) / submitting / justSaved / submitError / visible
// useEffect 2개:
//   - requestAnimationFrame(() => setVisible(true)) — 슬라이드업 mount
//   - cctvCPs 변경 시 init records 로드 (eslint-disable-line, deps: [cctvCPs])
// useMemo: cctvCPs = allCheckpoints.filter(cp => cp.category === 'CCTV')
// 헬퍼: doneCnt = cctvCPs.filter(cp => records[cp.id]).length / allDone = doneCnt === cctvCPs.length && cctvCPs.length > 0
// useInspectionRevisitPopup 호출 없음 (DVR 13대 batch — 개별 cp 단위 재진입 팝업 X. 단, 이건 현재 코드 동작 그대로 — 변경 금지)

# 4. CctvModal.handleSave — 한 줄도 변경 금지 (260505-cib 메모리 룰)
const handleSave = async () => {
  if (cctvCPs.length === 0) return
  setSubmitting(true); setSubmitError(null)
  try {
    const photoKey = await photo.upload()
    // 사진 1건 대표 부여 — caution/bad 우선, 없으면 첫 cp
    const photoTargetCp = photoKey
      ? (cctvCPs.find(cp => {
          const r = dvrResults[cp.id] ?? 'normal'
          return r === 'caution' || r === 'bad'
        }) ?? cctvCPs[0])
      : null
    for (const cp of cctvCPs) {
      const keyForCp = photoTargetCp && cp.id === photoTargetCp.id ? photoKey : undefined
      await onSave(cp.id, dvrResults[cp.id] ?? 'normal', memo, keyForCp ?? undefined)
    }
    setJustSaved(true); setMemo(''); photo.reset()
  } catch (e: any) {
    setSubmitError(e.message ?? '저장 오류')
  } finally {
    setSubmitting(false)
  }
}

# 5. 외부 상수 / 헬퍼 — 변경 금지
NAV_BOTTOM = 'calc(54px + env(safe-area-inset-bottom, 20px))'
STAIRWELLS  // 5 계단실: { id:1, floors:['8F',...,'1F'], leftCount:4 } / id:2 (12층 좌6우6) / id:3 (8층 좌4우4) / id:4 (12층 좌6우6) / id:5 (11층 좌6우5)
CCTV_DVRS   // 13대 { no, label, desc } — src/utils/cctv.ts
INSPECT_RESULT_OPTIONS  // [{ value:'normal', label:'정상', icon:..., color:..., bg:... }, caution, bad]
CATEGORY_GROUPS  // 라인 ~50: 특별피난계단 (icon:'🚪', categories:['특별피난계단']) / CCTV (icon:'📹', categories:['CCTV'])
StairsIcon  // 커스텀 SVG (src/components/ui/icons.tsx, 이미 라인 31에서 import 됨)

# 6. 훅 — 변경 금지
useAuthStore() → { staff, token, ... }
usePhotoUpload() → { blob, preview, key, uploadFromFile, pick, reset, upload(), uploading, ... }
useInspectionRevisitPopup({ checkpointId, category, monthRecords, scheduleItems })
  → { popupState: { variant, checkedAt, inspectorName, recordId? } | null, dismiss(): void }
useNavigate / useState / useRef / useMemo / useEffect

# 7. 외부 컴포넌트 — 그대로 사용 (수정 금지)
PhotoButton({ hook, label, noCapture })                  // pnr 에서 Camera lucide + 토큰 정합 완료
InspectionRevisitPopup({ variant, checkedAt, inspectorName, recordId?, onClose, onGoToRemediation? })
  // pnr 정합 완료, 부모 박스 position:relative 필수 (Stairwell 의 swDef 블록 내 wrapper 가 그 역할)

# 8. lucide 아이콘 매핑 (Wave 5 신규)
- 🚪 / StairwellModal 헤더 → **StairsIcon (커스텀, size={18}, className="text-text-secondary")** — 이미 import 됨 (라인 31)
- 📹 / CctvModal 헤더 → **Video (lucide, size={18}, className="text-text-secondary")** — 이미 import 됨 (라인 24)
- CCTV mh-action "설비 현황" → **Server (lucide, size={13})** — **신규 import 추가**
- ✓ / "이미 점검 완료" alert + 계단실 done-mark → **Check 또는 CheckCircle2** — 이미 import 됨
  - alert-success 박스 안의 "✓" 텍스트: `<CheckCircle2 size={14} className="text-safe">` 또는 lucide `<Check size={14}>`
  - 계단실 segmented 버튼 안의 done-mark `✓` (작은): `<Check size={12} className="opacity-85">` 또는 텍스트 ✓ 유지 (시안에서는 텍스트 ✓ 사용 — 변환 시 lucide 권장)
- 📷 / 사진 촬영 → **Camera (lucide)** — PhotoButton 안에서 이미 처리됨 (pnr 완료)
- 결과 picker 3종 (정상/주의/불량) → **CheckCircle2 / AlertTriangle / XCircle (lucide, size={12} — 컴팩트 셀 안이라 12)** — 이미 import 됨
- 저장 완료 (✓ 저장 완료 배너): `<CheckCircle2 size={14}>` — 이미 import 됨

# 9. 결과 picker 시각화 룰 (§7.1 일관성 — 컴팩트 컨테이너 안의 result-mini 변형)
- 컨테이너: `flex gap-1`
- 버튼 기본: `flex-1 px-1 py-1.5 rounded-pill border-[1.5px] inline-flex items-center justify-center gap-1 text-caption font-bold cursor-pointer whitespace-nowrap transition-colors`
- 비활성: `border-border-default bg-surface-page text-text-tertiary`
- 활성(정상): `border-safe-bar bg-safe-bg text-safe`
- 활성(주의): `border-warning-bar bg-warning-bg text-warning`
- 활성(불량): `border-danger-bar bg-danger-bg text-danger`
- lucide outline 12x12 (CheckCircle2 / AlertTriangle / XCircle), `className="flex-shrink-0"` (icon-size 12 — 셀 안 컴팩트)
- 라벨: 한글 텍스트 그대로 (`정상 / 주의 / 불량`)

# 10. 인라인 style 허용 화이트리스트 (i4r/sp7/tbj/0l7 전례 그대로)
- position / top / left / right / bottom (NAV_BOTTOM 등 동적 calc / `var(--sat, 0px)`)
- transform (slide-up / translateY 동적 — visible 분기)
- transition (cubic-bezier(0.32,0.72,0,1) 0.26s 등 — Tailwind 미정의 cubic-bezier)
- animation (keyframe 미정의)
- §6.4 그라디언트 CTA: `background: 'linear-gradient(135deg,#1d4ed8,#0ea5e9)'` (저장 버튼만) + `boxShadow:'0 4px 14px rgba(37,99,235,0.35)'` (시그니처 그림자)
- 비활성 저장 배경: `background:'var(--border-default)'` (Tailwind `bg-border-default` 가능하면 우선)
- SVG attribute (StairsIcon 커스텀 SVG 안의 rect / fill="currentColor" 등) — StairsIcon import 사용 시 attribute 신경 안 써도 됨
- bg-[rgba(...)] arbitrary value (예: 시안 toolbar bg) — 가능하면 surface 토큰 우선

# 11. 옛 토큰 → v0.1.1 매핑 (Wave 1/2/3/4 와 동일)
| 기존 | 새 토큰 |
|---|---|
| `var(--bg)` | `bg-surface-page` (또는 `var(--surface-page)`) |
| `var(--bg2)` | `bg-surface-raised` |
| `var(--bg3)` | `bg-surface-sunken` |
| `var(--bd)` | `border-border-default` |
| `var(--bd2)` | `border-border-strong` |
| `var(--acl)` | `bg-accent` / `text-accent` |
| `var(--t1)` | `text-text-primary` |
| `var(--t2)` | `text-text-secondary` |
| `var(--t3)` | `text-text-tertiary` |
| `var(--safe)` | `text-safe` / `bg-safe-bar` / `bg-safe-bg` |
| `var(--warn)` | `text-warning` / `bg-warning-bar` / `bg-warning-bg` |
| `var(--danger)` | `text-danger` / `bg-danger-bar` / `bg-danger-bg` |
| `var(--fire)` | `text-fire` / `bg-fire-bar` / `bg-fire-bg` (StairwellModal/CctvModal 에서는 거의 안 쓰이지만 mapping 룰 그대로) |
| `INSPECT_RESULT_OPTIONS[*].color` / `.bg` 인라인 참조 | Tailwind status 토큰 클래스 분기로 대체 (resultBtnStyle 헬퍼 제거 또는 인라인 분기로 교체) |
</interfaces>

<design_rules_wave5>
**시안 매핑 (4차 sketch — `inspection-sketch-stairwell-cctv.html` 901줄):**

1. **외곽 모달 컨테이너 (StairwellModal 라인 352 / CctvModal 라인 552):**
   - `fixed left-0 right-0 z-[99] bg-surface-page flex flex-col` + 인라인 `style={{ top:'var(--sat, 0px)', bottom: NAV_BOTTOM, transform: visible ? 'translateY(0)' : 'translateY(100%)', transition:'transform 0.26s cubic-bezier(0.32,0.72,0,1)' }}` (position/top/bottom/transform/transition 화이트리스트)
   - **주의:** 현재 코드는 `background:'var(--bg)'` 인라인 — Tailwind `bg-surface-page` 로 교체 후 인라인 background 제거

2. **헤더 (StairwellModal mh / CctvModal mh — 시안 .mh 클래스):**
   - `flex items-center px-4 py-3 bg-surface-page border-b border-border-default flex-shrink-0 gap-2`
   - **주의:** 시안은 `background: var(--bg)` (page) 인데 현재 코드는 `var(--bg2)` (raised). **시안 권위 따라 page (`bg-surface-page`) 로 통일**
   - StairwellModal 아이콘: `<StairsIcon size={18} className="text-text-secondary flex-shrink-0" />` (커스텀 SVG 컴포넌트 — props 로 size/className 받음)
   - CctvModal 아이콘: `<Video size={18} className="text-text-secondary flex-shrink-0" />`
   - mh-title: `<span className="text-body font-bold text-text-primary">{group.labels[0]}</span>` (또는 'CCTV 점검')
   - mh-sub: `<span className="text-caption text-text-tertiary ml-1">· {group.labels.slice(1).join(' · ')}</span>` (StairwellModal — group.labels.length > 1 분기 그대로) / `· B1F 방재센터 DVR 13대` (CctvModal 고정)
   - **현재 코드 mh-sub 의 fontSize:10 → text-caption(12) 노안 친화**
   - CctvModal mh-action (시안 라인 674-677): `ml-auto px-2.5 py-1.5 rounded-sm border border-border-strong bg-surface-raised text-text-secondary text-caption font-bold cursor-pointer inline-flex items-center gap-1.5 flex-shrink-0` + `<Server size={13} className="flex-shrink-0" />` + "설비 현황" → onClick 그대로 (`navigate('/cctv')`)
   - CctvModal allDone 뱃지 (시안 외 — 현재 코드 1561-1563): 시안에서는 modal-body 안의 alert-success 박스로 통합 표현 — **현재 코드의 `allDone && !justSaved` 미니 뱃지는 보존**하되 색만 토큰화: `text-caption text-safe bg-safe-bg border border-safe-bar rounded-sm px-2 py-0.5 ml-auto` (10 → 12 노안 친화)
     - 단, mh-action(설비 현황) 과 allDone 뱃지 둘 다 헤더 우측에 위치 — 시안에는 mh-action 만 있음. 현재 코드 동작 그대로 두려면 allDone 뱃지 + mh-action 둘 다 표시. Wave 5 변환 시점에는 **현재 코드 동작 보존** 우선

3. **계단실 선택 segmented (StairwellModal 라인 364-377 — 시안 .bar-section + .seg):**
   - 외곽: `bg-surface-raised border-b border-border-default px-3.5 py-2 flex-shrink-0` (시안 .bar-section)
   - 라벨: `text-caption font-semibold text-text-tertiary mb-1.5 tracking-wider` "계단실 선택" (10 → 12 노안 친화)
   - 버튼 컨테이너: `flex gap-1.5` (시안 .seg) — flex-wrap 시안에서는 있지만 5 버튼이라 wrap 발생 안 함 — 그대로 적용 OK
   - 버튼 기본: `flex-1 basis-0 min-w-0 px-2 py-2 rounded-sm border border-border-strong bg-surface-page text-text-secondary text-label font-bold cursor-pointer whitespace-nowrap transition-colors` (현재 코드 btnStyle 헬퍼 → 인라인 분기로 교체)
   - 활성(is-active): `border-[1.5px] border-accent bg-accent text-text-on-accent`
   - done(is-done): `border-[1.5px] border-safe-bar bg-safe-bg text-safe`
   - done-mark `✓`: `text-caption ml-1 opacity-85` — 텍스트 ✓ 유지 또는 `<Check size={12}>` (시안은 텍스트 ✓ — 변환 시 텍스트 ✓ 유지 권장 — 1글자 span 으로 최소화)

4. **modal-body 본문 영역 (StairwellModal 라인 380 / CctvModal 라인 573):**
   - `flex-1 overflow-y-auto px-3.5 py-3 flex flex-col gap-3 relative` (시안 .modal-body — position:relative — InspectionRevisitPopup 의 부분 오버레이 부모로 작동)
   - **주의:** 현재 StairwellModal 의 `position:'relative'` 는 swDef 블록 내부 wrapper (라인 386) 에 있어 — modal-body 자체는 fixed/auto. **현재 코드 구조 그대로 보존** (popupState 부모는 swDef && (...) 안 wrapper)

5. **alert-success 박스 (시안 라인 396-399 — `1/12층 이미 점검 완료`):**
   - 외곽: `bg-safe-bg border border-safe-bar rounded-sm px-3 py-2 flex items-center gap-1.5 text-label font-semibold text-safe` (현재 코드 라인 400, 462 / 시안 라인 396-399, 558-561, 575-579, 846-849 4 곳)
   - StairwellModal 라인 400: 좌측 `<CheckCircle2 size={14}>` (또는 텍스트 ✓ 유지) + "✓ {swDoneCount}/{swCPs.length}층 이미 점검 완료" — 11 → 13 (text-label) 또는 12 (text-caption) — **노안 친화 + 시안 13 일치 → text-label 권장**
   - StairwellModal 라인 462 `justSaved`: 동일 패턴 "✓ 저장 완료"
   - CctvModal 라인 576-578: 동일 alert-success "✓ {doneCnt}/{cctvCPs.length}대 이미 점검 완료"
   - submitError 박스 (라인 461 / 628): `bg-danger-bg border border-danger-bar text-danger` 동일 패턴 (rounded-sm px-3 py-2 text-label font-semibold)

6. **batch-grid + batch-col (계단실 floor-tile / DVR dvr-tile 2열 그리드 — 시안 .batch-grid):**
   - 외곽: `grid grid-cols-2 gap-2` (시안 .batch-grid 1fr 1fr)
   - 각 컬럼: `flex flex-col gap-1.5` (시안 .batch-col 6px → 1.5)

7. **floor-tile (StairwellModal 계단실 층 셀 — 시안 .floor-tile):**
   - 외곽: `bg-surface-raised border border-border-default rounded-md px-2 pt-2 pb-1.5`
   - floor-lbl: `text-caption font-bold text-text-secondary mb-1.5` (11 → 12 = text-caption)
   - 셀 안 result-mini: §9 룰 그대로 (컴팩트 — size 12)
   - is-init(시안 라인 421 — 처음 진입 시 fire-bar 강조): 현재 코드에는 없음 — **무시** (Wave 5 변환은 현재 코드 동작 보존, 시안 의 is-init 시각 추가는 follow-up 트랙)

8. **dvr-tile (CctvModal DVR 셀 — 시안 .dvr-tile):**
   - 외곽: `bg-surface-raised border border-border-default rounded-md px-2 pt-1.5 pb-1.5` (시안 padding 8px 8px 6px → 2/1.5/1.5)
   - 첫 줄(라벨 + 데스크): 시안은 라벨 13/700 + desc 12/tertiary 별도 줄. **현재 코드는 한 줄에 label+desc justify-between baseline** (라인 590-593) — 시안 1:1 변환 시 시안 권위 따라 **두 줄 분리** 권장:
     - `<div className="text-label font-bold text-text-primary leading-tight">{dvr.label}</div>`
     - `<div className="text-caption text-text-tertiary mt-0.5 mb-1.5 leading-tight min-h-[1.25em]">{dvr.desc}</div>` (9 → 12 = text-caption 노안 친화)
   - **단:** 현재 코드 한 줄 baseline 레이아웃을 보존하는 옵션도 OK — 그 경우 dvr.label `text-label font-bold text-text-secondary` + dvr.desc `text-caption text-text-tertiary` 로 토큰만 교체. **권장은 시안 권위 따라 두 줄로 분리** (가독성 + 13/12 노안 친화)
   - 셀 안 result-mini: §9 룰 그대로

9. **result-mini (셀 안 컴팩트 픽커 — 시안 .result-mini):**
   - 컨테이너: `flex gap-1`
   - 버튼 기본: `flex-1 px-1 py-1.5 rounded-pill border-[1.5px] inline-flex items-center justify-center gap-1 text-caption font-bold cursor-pointer whitespace-nowrap transition-colors`
     - 비활성: `border-border-default bg-surface-page text-text-tertiary`
     - 활성(normal): `border-safe-bar bg-safe-bg text-safe`
     - 활성(caution): `border-warning-bar bg-warning-bg text-warning`
     - 활성(bad): `border-danger-bar bg-danger-bg text-danger`
   - 아이콘: lucide 12x12 (`<CheckCircle2|AlertTriangle|XCircle size={12} className="flex-shrink-0">`)
   - 라벨: 텍스트 `정상 / 주의 / 불량`
   - **헬퍼 도입 권장:** `resultMiniCls(active, value)` + `resultMiniIcon(value)` 둘 — 두 모달 공유. 또는 인라인 분기. dead code 인 `resultBtnStyle` 헬퍼 제거 OK (TS 경고 회피 — 현재 사용처 다 제거 후 unused 면 삭제)
   - **이모지 제거**: 현재 코드의 `{opt.icon} {opt.label}` (라인 419, 438, 597) → `{resultMiniIcon(opt.value)} {opt.label}` (lucide 컴포넌트 + 한글 텍스트)

10. **특이사항 + 사진 (StairwellModal 라인 450-459 / CctvModal 라인 617-626 — 시안 .memo-row):**
    - 바깥 div: 그대로 (`<div>...</div>`)
    - 라벨 row: 시안 권위 — `text-caption font-semibold text-text-tertiary mb-1.5 tracking-wider` 단일 라벨 "특이사항 + 사진" 또는 좌/우 분할 그대로. **현재 코드의 좌/우 분할 (특이사항 (선택) / 점검 사진 (선택))** 라벨 보존 권장 — 토큰만 교체:
      - 좌측 label: `text-caption font-semibold text-text-tertiary tracking-wider` (10 → 12)
      - 우측 span: `text-caption text-text-tertiary` "점검 사진 (선택)" (10 → 12)
      - 컨테이너: `flex items-center justify-between mb-1.5`
    - memo-row: `flex gap-2 items-start`
    - textarea: `flex-1 h-[72px] px-3 py-2.5 rounded-md bg-surface-raised border border-border-default text-text-primary text-label resize-none outline-none box-border font-sans placeholder:text-text-tertiary` (현재 코드 fontSize 12 → text-label(13) 노안 친화 / 시안 .memo-area 13/700)
    - **placeholder='특이사항을 입력하세요' 그대로 — 증상 피커 미도입 룰 (메모리)**
    - PhotoButton 호출 그대로 (`<PhotoButton hook={photo} label="촬영" noCapture />`)

11. **footer-bar (StairwellModal 라인 467-477 / CctvModal 라인 632-642 — 시안 .footer-bar):**
    - 컨테이너: `flex gap-2 px-3.5 pt-2.5 pb-3 bg-surface-raised border-t border-border-default flex-shrink-0`
    - btn-close-mini: `px-4 py-3 rounded-md bg-surface-page border border-border-strong text-text-secondary text-label font-semibold cursor-pointer` (시안 13/600 → text-label)
    - btn-save (§6.4 그라디언트 — 인라인 화이트리스트):
      ```tsx
      style={{
        background: (submitting || photo.uploading || !selectedSW) ? 'var(--border-default)' : 'linear-gradient(135deg,#1d4ed8,#0ea5e9)',
        color: (submitting || photo.uploading || !selectedSW) ? 'var(--text-tertiary)' : '#fff',
        cursor: (submitting || photo.uploading || !selectedSW) ? 'default' : 'pointer',
        boxShadow: (submitting || photo.uploading || !selectedSW) ? 'none' : '0 4px 14px rgba(37,99,235,0.35)',
      }}
      className="flex-1 py-3.5 rounded-md text-body font-bold border-0 transition-all"
      ```
    - **현재 코드 fontSize:13 → text-body(14) — 시안 .btn-save 14/700 일치 + 노안 친화**
    - 저장 라벨 분기 그대로 (`photo.uploading ? '사진 업로드 중...' : submitting ? '저장 중...' : '계단실 N 점검 저장'` / `'CCTV 점검 저장'`)

12. **swDef 블록 내 wrapper (StairwellModal 라인 386 — InspectionRevisitPopup 부모):**
    - `relative flex flex-col gap-2.5` (position:relative — 팝업이 swDef 블록만 덮음, 260423-htx Task 5 의도)
    - **현재 코드 의 `position:'relative', display:'flex', flexDirection:'column', gap:10` 인라인 → Tailwind `relative flex flex-col gap-2.5` 로 교체**

13. **!selectedSW empty state (StairwellModal 라인 381-383):**
    - `flex-1 flex items-center justify-center text-text-tertiary text-label` "계단실을 선택해 주세요" (13 → text-label)

14. **swDoneCount > 0 미니 뱃지 (StairwellModal 라인 399-403):**
    - alert-success §5 와 동일 (`bg-safe-bg border border-safe-bar rounded-sm px-3 py-2 text-label font-semibold text-safe inline-flex items-center gap-1.5`)
    - 좌측 `<CheckCircle2 size={14}>` (또는 텍스트 ✓ 유지)
    - 11 → text-label(13) 노안 친화

15. **CctvModal doneCnt > 0 미니 뱃지 (라인 575-579):** 동일 alert-success 패턴
</design_rules_wave5>

<conversion_strategy>
**시안 → TSX 변환 (i4r/sp7/tbj/0l7 패턴 그대로, Wave 5 영역 한정):**

1. **파일 영역 한정 작업 (tbj/0l7 패턴 그대로):**
   - InspectionPage.tsx 5872 줄 중 Wave 5 변환 영역은 라인 255~645 부근 (총 약 390줄)
   - **다른 영역(라인 1~254 / 라인 647~끝) 은 한 줄도 건드리지 않는다**
   - Edit 단위로 영역별 정밀 교체 — Write 전체 덮어쓰기는 금지(다른 wave 변환 영역 안전 보장)
   - BaeyeonModal (라인 653 시작 — `function BaeyeonModal({ group, allCheckpoints ...`) 직전까지가 Wave 5 영역 경계

2. **모든 `style={{...}}` 의 금지 키 제거 → Tailwind 또는 토큰 변수 클래스로:**
   - 색: `color:'var(--t1)'` → `className="text-text-primary"`
   - 배경: `background:'var(--bg2)'` → `className="bg-surface-raised"`
   - 테두리: `border:'1px solid var(--bd)'` → `className="border border-border-default"`
   - 패딩: `padding:'10px 16px'` → `className="px-4 py-2.5"`
   - 폰트: `fontSize:13, fontWeight:700` → `className="text-label font-bold"`
   - 11px → 13px (text-label) 또는 12px (text-caption) — 노안 친화 §1.1
   - 9/10px → 12px (text-caption) — 노안 친화
   - 보존: position / top / left / right / bottom (`var(--sat, 0px)` / NAV_BOTTOM 동적 calc) / transform (slide-up 동적) / transition cubic-bezier / animation / SVG attribute / §6.4 그라디언트 CTA / boxShadow 그라디언트 시그니처 그림자 / 비활성 저장 배경 var(--border-default)

3. **lucide 통일:**
   - `{group.icon}` (🚪) 라인 356 → `<StairsIcon size={18} className="text-text-secondary flex-shrink-0" />` (이미 import 됨)
   - `{group.icon}` (📹) 라인 556 → `<Video size={18} className="text-text-secondary flex-shrink-0" />` (이미 import 됨)
   - 라인 568 `📹 설비 현황` → `<><Server size={13} className="flex-shrink-0" />설비 현황</>` (Server 신규 import 필요)
   - 라인 372 (계단실 done-mark `✓`): 텍스트 ✓ 유지 또는 lucide `<Check size={12}>` — 시안 권위 텍스트 ✓ → 텍스트 유지 (1글자 span 최소화)
   - 라인 401, 462, 577, 629 (alert/저장 완료 `✓`): `<CheckCircle2 size={14} className="text-safe flex-shrink-0">` (필요 시 import 추가 — 이미 import 됨)
   - 결과 picker (라인 419, 438, 597 — `{opt.icon} {opt.label}`): `INSPECT_RESULT_OPTIONS[*].icon` 은 텍스트 이모지일 가능성 → **lucide 컴포넌트 분기 헬퍼 도입** (`resultMiniIcon(value)` → CheckCircle2/AlertTriangle/XCircle 12x12)

4. **비즈니스 로직 보존 (가장 중요) — 한 줄도 변경 금지:**
   - props 시그니처 (group, allCheckpoints, records, monthRecords, scheduleItems, onClose, onSave / allCheckpoints, records, onClose, onSave)
   - state 13개 (Stairwell 7개 + CCTV 6개)
   - useRef prevSW
   - useEffect 4개 (visible mount + selectedSW reset + visible mount + cctvCPs init)
   - useMemo swCPs / cctvCPs
   - **handleSave 본문 한 줄도 변경 금지** (`photoTargetCp` 분기 + for-loop 의 `keyForCp` 분배 — 260505-cib 메모리 룰)
   - `await photo.upload()` / PhotoButton 호출 / usePhotoUpload 훅 그대로
   - useInspectionRevisitPopup(category: '특별피난계단') 호출 그대로 (StairwellModal)
   - CctvModal 에는 useInspectionRevisitPopup 호출 없음 — 현재 코드 동작 그대로 (Wave 5 에서 추가 안 함)
   - `navigate('/cctv')` 호출 그대로 (mh-action 클릭 시)
   - swDef.floors.slice(0, swDef.leftCount) / slice(swDef.leftCount) 좌/우 분할 그대로
   - Math.ceil(CCTV_DVRS.length/2) = 7 좌/우 분할 그대로
   - swCPs.find(c => c.floor === floor) / cctvCPs.find(c => c.locationNo === dvr.no) 짝 매핑 그대로
   - INSPECT_RESULT_OPTIONS 순회 그대로 — 단 result 버튼 스타일은 §9 룰로 교체

5. **증상 피커 미도입 (메모리 룰 강제):**
   - StairwellModal / CctvModal 에 symptomPick state 신설 금지
   - 메모 placeholder='특이사항을 입력하세요' 그대로 (자동화 5종 카테고리 아님)

6. **dead code 정리 (i4r/0l7 전례):**
   - `btnStyle` / `resultBtnStyle` 인라인 헬퍼 함수 — Tailwind 분기로 교체되면 unused 가 됨 → 사용처 다 잡아내고 unused 면 제거 OK
   - 단 사용처가 남아 있으면 보존 (다른 모달이 같은 헬퍼 쓰지 않음 — Stairwell/Cctv 안에서만 정의되므로 안전)

7. **헤더 background 시안 정합:**
   - 시안 .mh 는 `var(--bg)` (page), 현재 코드는 `var(--bg2)` (raised) — **시안 권위 따라 `bg-surface-page` 로 통일** (1·2·3·4차 시안 모두 .mh `var(--bg)`)
   - 단, .bar-section (계단실 선택) 은 시안에서 `var(--bg2)` (raised) — `bg-surface-raised` 유지

8. **결과 picker 시각 변경 — opt.icon/opt.color/opt.bg 인라인 의존 제거:**
   - 현재 코드 `resultBtnStyle` 헬퍼는 `opt.color` / `opt.bg` 를 인라인 색으로 직접 사용
   - 변환 시: `resultMiniIcon(value: CheckResult): JSX.Element` 헬퍼 도입 (CheckCircle2/AlertTriangle/XCircle 12x12 반환) + `resultMiniCls(active: boolean, value: CheckResult): string` 헬퍼 또는 인라인 클래스 분기
   - opt.icon 텍스트 이모지는 더 이상 표시 안 함 — lucide 컴포넌트로 대체
</conversion_strategy>

<tasks>

<task type="auto">
  <name>Task 1: StairwellModal 변환 (특별피난계단 일괄 입력, 라인 255~480, 약 230줄)</name>
  <files>cha-bio-safety/src/pages/InspectionPage.tsx</files>
  <action>
**범위:** `function StairwellModal({ group, allCheckpoints, records, monthRecords, scheduleItems, onClose, onSave })` 본문 (라인 255~480). CctvModal 은 Task 2 — 이 Task 에서 절대 건드리지 않는다.

**시안:** `cha-bio-safety/docs/redesign-context/02-inspection/sketch/inspection-sketch-stairwell-cctv.html` 라인 333~648 (특별피난계단 영역 — Mobile Dark + Mobile Light 2 viewport) — 권위.

**lucide import 보강 (파일 상단 import 블록, 라인 21~29):**
- StairsIcon (이미 라인 31 별도 import) — 그대로
- Check (필요 시 alert/done-mark — 현재 import 안 됨이면 추가). 단 alert 의 ✓ 는 CheckCircle2 (이미 import) 로 대체 가능 — Check 신규 추가는 옵션
- CheckCircle2 / AlertTriangle / XCircle (이미 import 됨)
- **`grep -n "from 'lucide-react'" InspectionPage.tsx` 로 기존 import 확인 후 누락만 추가** — 신규 추가 0개여도 OK (Check 사용 안 한다면)

**비즈니스 보존 (한 줄도 변경 금지) — 절대 룰:**
- props 시그니처 그대로 (group, allCheckpoints, records, monthRecords, scheduleItems, onClose, onSave)
- state 7개 (selectedSW / floorResults / memo / submitting / justSaved / submitError / visible) 그대로
- useRef prevSW 그대로
- useEffect 2개 (requestAnimationFrame mount + prevSW 비교 후 init reset) 한 줄도 변경 없음
- useMemo swCPs 그대로
- swDef = STAIRWELLS.find(...) / swDoneCount 헬퍼 그대로
- useInspectionRevisitPopup({ checkpointId: swCPs[0]?.id ?? null, category: '특별피난계단', monthRecords, scheduleItems }) 그대로
- **handleSave 본문 한 줄도 변경 금지 — 260505-cib 메모리 룰:**
  - `const photoKey = await photo.upload()` 그대로
  - `const photoTargetCp = photoKey ? (swCPs.find(cp => { const r = floorResults[cp.id] ?? 'normal'; return r === 'caution' || r === 'bad' }) ?? swCPs[0]) : null` 그대로
  - `for (const cp of swCPs) { const keyForCp = photoTargetCp && cp.id === photoTargetCp.id ? photoKey : undefined; await onSave(cp.id, floorResults[cp.id] ?? 'normal', memo, keyForCp ?? undefined) }` 그대로
  - try/catch/finally 본문 그대로
- usePhotoUpload() / PhotoButton 호출 시그니처 그대로 (`<PhotoButton hook={photo} label="촬영" noCapture />`)
- `navigate('/remediation/' + recordId)` (라인 395) 그대로

**시각 변환 (v0.1.1 토큰 + Tailwind):**

1. **외곽 모달 컨테이너 (라인 352):**
   - 클래스: `fixed left-0 right-0 z-[99] flex flex-col overflow-hidden bg-surface-page`
   - 인라인 (화이트리스트): `style={{ top:'var(--sat, 0px)', bottom: NAV_BOTTOM, transform: visible ? 'translateY(0)' : 'translateY(100%)', transition:'transform 0.26s cubic-bezier(0.32,0.72,0,1)' }}`
   - **현재 `background:'var(--bg)'` 인라인 제거** (Tailwind `bg-surface-page` 로 대체)

2. **헤더 (라인 355-361):**
   - 외곽 div: `flex items-center px-4 py-2.5 bg-surface-page border-b border-border-default flex-shrink-0 gap-2.5`
   - **현재 `padding:'10px 16px', background:'var(--bg2)'` → `bg-surface-page` 로 변경 (시안 권위)**
   - 좌측 아이콘 (라인 356): `<StairsIcon size={18} className="text-text-secondary flex-shrink-0" />` ({group.icon} 텍스트 이모지 제거)
   - 우측 텍스트 컨테이너 (라인 357-360): `flex-1 min-w-0`
     - mh-title (라인 358): `text-body font-bold text-text-primary` "{group.labels[0]}" (16/700 → text-body(14) — 시안 .mh-title 16/700 이므로 16 권장하지만 노안 친화 14 OK / 권장: **시안 정합 16 = `text-[16px] font-bold text-text-primary`** arbitrary value)
     - mh-sub (라인 359 — `group.labels.length > 1` 분기 그대로): `text-caption text-text-tertiary mt-0.5` "· {group.labels.slice(1).join(' · ')}" (10 → 12 노안 친화)

3. **계단실 선택 segmented (라인 364-377):**
   - 외곽 div (라인 364): `bg-surface-raised border-b border-border-default px-3.5 py-2 flex-shrink-0` (시안 .bar-section)
   - 라벨 (라인 365): `text-caption font-semibold text-text-tertiary mb-1.5 tracking-wider` "계단실 선택" (10 → 12)
   - 버튼 컨테이너 (라인 366): `flex gap-1.5`
   - 버튼 (라인 367-374):
     - 기본: `flex-1 basis-0 min-w-0 px-2 py-2 rounded-sm border text-label font-bold cursor-pointer whitespace-nowrap transition-colors inline-flex items-center justify-center`
     - 활성(selectedSW === sw.id): `border-[1.5px] border-accent bg-accent text-text-on-accent`
     - done(`done === true`): `border-[1.5px] border-safe-bar bg-safe-bg text-safe`
     - 비활성(둘 다 아님): `border-border-strong bg-surface-page text-text-secondary`
     - **활성 + done 동시 분기 우선순위:** 활성이 done 보다 우선 (현재 코드는 btnStyle 헬퍼가 sel 만 분기 — done 시각 미반영). **시안 권위 따라 done 시각 추가 — selectedSW 가 활성이고 done 도 true 면 활성 시각 우선** (selectedSW 가 done 이라면 활성 + done done-mark `✓` 표시) — **권장 분기:** `selectedSW === sw.id` 면 활성, 아니고 `done` 이면 done, 아니면 비활성
     - 라벨 + done-mark: `{sw.id}{done && <span className="text-caption ml-1 opacity-85">✓</span>}` (라인 372 의 `<span style={{ fontSize:9, marginLeft:3, opacity:0.8 }}>✓</span>` → 토큰 클래스로)
   - **btnStyle 인라인 헬퍼 함수 (라인 335-341) 제거** — Tailwind 분기로 대체. 단 함수 정의는 그대로 두고 호출만 안 하는 옵션도 OK (TS 경고 회피 권장 = 제거)

4. **modal-body 본문 영역 (라인 380):**
   - `flex-1 overflow-y-auto px-3.5 py-3 flex flex-col gap-2.5 relative` (시안 .modal-body — `position:relative` Tailwind `relative`)

5. **!selectedSW empty state (라인 381-383):**
   - `flex-1 flex items-center justify-center text-text-tertiary text-label` "계단실을 선택해 주세요" (13 → text-label)

6. **swDef && (...) wrapper (라인 386):**
   - `relative flex flex-col gap-2.5` (Tailwind 로 인라인 제거 — position:relative + flex col + gap-10 → gap-2.5)

7. **InspectionRevisitPopup 호출 (라인 388-397):** 그대로 (pnr 정합 완료, 시그니처 변경 없음)

8. **swDoneCount > 0 미니 뱃지 (라인 399-403):**
   - `bg-safe-bg border border-safe-bar rounded-sm px-3 py-1.5 text-label font-semibold text-safe inline-flex items-center gap-1.5`
   - 좌측 `<CheckCircle2 size={14} className="text-safe flex-shrink-0" />` (✓ 이모지 제거)
   - 텍스트: "{swDoneCount}/{swCPs.length}층 이미 점검 완료" (11 → text-label(13) 노안 친화)

9. **batch-grid 2열 (라인 406):**
   - `grid grid-cols-2 gap-2`

10. **batch-col 좌/우 (라인 408 / 428):**
    - `flex flex-col gap-1.5`

11. **floor-tile (라인 414 / 434 — 좌/우 동일 패턴):**
    - 외곽: `bg-surface-raised border border-border-default rounded-md px-2 pt-2 pb-1.5`
    - floor-lbl (라인 415 / 435): `text-caption font-bold text-text-secondary mb-1.5` "{floor}" (11 → 12 = text-caption)
    - result-mini 컨테이너 (라인 416 / 436): `flex gap-1`
    - 버튼 (라인 417-421 / 437-441):
      - 기본: `flex-1 px-1 py-1.5 rounded-pill border-[1.5px] inline-flex items-center justify-center gap-1 text-caption font-bold cursor-pointer whitespace-nowrap transition-colors`
      - 비활성(`curResult !== opt.value`): `border-border-default bg-surface-page text-text-tertiary`
      - 활성(normal): `border-safe-bar bg-safe-bg text-safe`
      - 활성(caution): `border-warning-bar bg-warning-bg text-warning`
      - 활성(bad): `border-danger-bar bg-danger-bg text-danger`
      - lucide 12x12: `r === 'normal'` → `<CheckCircle2 size={12} className="flex-shrink-0">`, `caution` → `<AlertTriangle size={12} className="flex-shrink-0">`, `bad` → `<XCircle size={12} className="flex-shrink-0">`
    - **헬퍼 도입 권장:** `resultMiniIcon(value: CheckResult): JSX.Element` + `resultMiniCls(active: boolean, value: CheckResult): string` (Task 2 와 공유 — StairwellModal/CctvModal 같은 파일이므로 함수 한 곳 정의 가능. 하지만 두 모달이 동시에 export 되지 않는 구조 이슈 회피하려면 **각 모달 안에 local helper 로 두 번 정의** OR **파일 최상단의 헬퍼 영역(라인 ~170 부근)** 에 한 번만 정의 — 후자 권장. 단 다른 wave 영역 안 건드리는 룰과 충돌하지 않아야 함 — 헬퍼는 새 함수 추가이므로 기존 라인 손대지 않고 `// ── result-mini helpers (Wave 5) ──` 영역 신설 OK)
    - **resultBtnStyle 인라인 헬퍼 (라인 343-349) 제거** — opt.color/opt.bg 인라인 의존 사라짐 → unused

12. **특이사항 + 사진 (라인 450-459):**
    - 라벨 row (라인 451-453): `flex items-center justify-between mb-1.5`
      - 좌 label (라인 452): `text-caption font-semibold text-text-tertiary tracking-wider` "특이사항 (선택)" (10 → 12)
      - 우 span (라인 453): `text-caption text-text-tertiary` "점검 사진 (선택)" (10 → 12)
    - memo-row (라인 455): `flex gap-2 items-start`
    - textarea (라인 456): `flex-1 h-[72px] px-3 py-2.5 rounded-md bg-surface-raised border border-border-default text-text-primary text-label resize-none outline-none box-border font-sans placeholder:text-text-tertiary`
      - **placeholder="특이사항을 입력하세요" 그대로 — 증상 피커 미도입 룰**
      - 12 → text-label(13) 노안 친화
    - PhotoButton 그대로

13. **submitError / justSaved 박스 (라인 461 / 462):**
    - submitError: `bg-danger-bg border border-danger-bar text-danger rounded-sm px-3 py-2 text-label font-semibold` (11 → text-label)
    - justSaved: `bg-safe-bg border border-safe-bar text-safe rounded-sm px-3 py-2 text-label font-semibold inline-flex items-center gap-1.5` + 좌측 `<CheckCircle2 size={14} className="flex-shrink-0">` (✓ 이모지 제거) + "저장 완료"

14. **footer-bar (라인 467-477):**
    - 컨테이너 (라인 468): `flex gap-2 px-3.5 pt-2.5 pb-3 bg-surface-raised border-t border-border-default flex-shrink-0`
    - btn-close-mini (라인 469): `px-4 py-3 rounded-md bg-surface-page border border-border-strong text-text-secondary text-label font-semibold cursor-pointer transition-colors`
    - btn-save (라인 470-476):
      - 클래스: `flex-1 py-3.5 rounded-md text-body font-bold border-0 transition-all`
      - 인라인 (§6.4 그라디언트 + 시그니처 그림자 화이트리스트):
        ```tsx
        style={{
          background: (submitting || photo.uploading || !selectedSW) ? 'var(--border-default)' : 'linear-gradient(135deg,#1d4ed8,#0ea5e9)',
          color: (submitting || photo.uploading || !selectedSW) ? 'var(--text-tertiary)' : '#fff',
          cursor: (submitting || photo.uploading || !selectedSW) ? 'default' : 'pointer',
          boxShadow: (submitting || photo.uploading || !selectedSW) ? 'none' : '0 4px 14px rgba(37,99,235,0.35)',
        }}
        ```
      - 라벨 분기 (라인 475 그대로): `{photo.uploading ? '사진 업로드 중...' : submitting ? '저장 중...' : '계단실 ' + (selectedSW ?? '') + ' 점검 저장'}`

**Out of scope (이 Task 에서 건드리지 않음):**
- CctvModal (라인 485 시작) → Task 2
- BaeyeonModal (라인 653 시작) → 이미 Wave 4 (tbj) 완료 — 한 줄도 변경 없음
- STAIRWELLS 상수 (라인 ~172) — 변경 금지
- INSPECT_RESULT_OPTIONS 상수 — 변경 금지 (단 .icon 텍스트 이모지 의존을 lucide 헬퍼로 대체)
- **증상 피커 신설 절대 금지**
- **handleSave 본문 한 줄도 변경 금지** (260505-cib photoKey 1건 대표 부여)

**커밋:** `feat(260515-1p0): task 1 — StairwellModal v0.1.1 토큰 + Tailwind 변환 (260505-cib photoKey 1건 대표 부여 보존, 증상 피커 미도입)`
  </action>
  <verify>
    <automated>cd cha-bio-safety && npx tsc --noEmit 2>&1 | grep -E "error TS" | head -20; echo "---"; npm run build 2>&1 | tail -5; echo "---"; sed -n '255,480p' src/pages/InspectionPage.tsx | grep -vE '^\s*//' | grep -cE 'var\(--bg\)|var\(--bg2\)|var\(--bg3\)|var\(--bd\)|var\(--bd2\)|var\(--acl\)|var\(--t1\)|var\(--t2\)|var\(--t3\)|var\(--safe\)|var\(--warn\)|var\(--danger\)|var\(--fire\)' | xargs -I{} test {} -le 2 && echo "OLD_TOKENS: <=2 (저장 비활성 var(--border-default)/var(--text-tertiary) 화이트리스트)" || echo "OLD_TOKENS: FAIL"; echo "---"; sed -n '255,480p' src/pages/InspectionPage.tsx | grep -vE '^\s*//' | grep -cE 'fontSize:\s*(9|10|11)[^0-9]|text-\[(9|10|11)px\]' | xargs -I{} test {} -eq 0 && echo "9_10_11PX_STAIRWELL: 0" || echo "9_10_11PX_STAIRWELL: FAIL"; echo "---"; sed -n '255,480p' src/pages/InspectionPage.tsx | grep -cE 'symptomPick|setSymptomPick' | xargs -I{} test {} -eq 0 && echo "NO_SYMPTOM_PICKER: OK" || echo "NO_SYMPTOM_PICKER: FAIL (증상 피커 신설 금지 룰 위반)"; echo "---"; sed -n '255,480p' src/pages/InspectionPage.tsx | grep -cE 'photoTargetCp' | xargs -I{} test {} -ge 2 && echo "PHOTOTARGETCP_PRESERVED: OK (260505-cib 메모리 룰)" || echo "PHOTOTARGETCP_PRESERVED: FAIL"; echo "---"; sed -n '255,480p' src/pages/InspectionPage.tsx | grep -cE "category:\s*'특별피난계단'" | xargs -I{} test {} -eq 1 && echo "REVISIT_POPUP_CATEGORY: OK" || echo "REVISIT_POPUP_CATEGORY: FAIL"; echo "---"; sed -n '255,480p' src/pages/InspectionPage.tsx | grep -cE 'STAIRWELLS\.find|swDef\.floors\.slice' | xargs -I{} test {} -ge 3 && echo "STAIRWELLS_MAPPING: OK (find + slice 2회)" || echo "STAIRWELLS_MAPPING: FAIL"; echo "---"; sed -n '255,480p' src/pages/InspectionPage.tsx | grep -cE '🚪|✓|🚨|✅|⚠️' | xargs -I{} test {} -le 1 && echo "EMOJI_REMOVED: OK (done-mark 텍스트 ✓ 1건만 허용)" || echo "EMOJI_REMOVED: FAIL"; echo "---"; grep -n "function CctvModal" src/pages/InspectionPage.tsx | head -1 && echo "CctvModal 시작 라인 (Task 2 영역 — 이 Task 에서 변경 없음)"</automated>
  </verify>
  <done>
    - StairwellModal (255~480) 본문 마크업/스타일이 v0.1.1 토큰 + Tailwind 로 교체
    - 헤더 StairsIcon / done-mark ✓ (텍스트 또는 lucide) / alert CheckCircle2 / 결과 picker CheckCircle2/AlertTriangle/XCircle (12x12) — 이모지 0~1건 (done-mark 텍스트 ✓ 허용)
    - 계단실 선택 segmented (5 버튼) — 활성/done/비활성 3 상태 시안 1:1
    - 좌/우 batch-grid (계단실별 floors.slice(0, leftCount) / slice(leftCount)) 시안 1:1
    - floor-tile 안의 result-mini (pill + lucide + 한글 라벨, §7.1 일관성)
    - **handleSave 본문 한 줄도 변경 없음 (260505-cib photoKey 1건 대표 부여 정책 보존)**
    - props / state 7개 / useRef prevSW / useEffect 2개 / useMemo swCPs / useInspectionRevisitPopup(category: '특별피난계단') 한 줄도 변경 없음
    - usePhotoUpload / PhotoButton / navigate('/remediation/...') 호출 그대로
    - **증상 피커 신설 0건** (특별피난계단은 자동화 5종 카테고리 아님)
    - 옛 토큰 var(--bg/bd/acl/t1~3/safe/warn/danger/fire) 변환 영역 내 ≤2 (저장 비활성 인라인 화이트리스트만)
    - 9/10/11px 폰트가 변환 영역 내 0건
    - CctvModal 시작 라인(현재 485) 그대로 (Task 2 영역 — 한 줄도 변경 없음)
    - BaeyeonModal 시작 라인(현재 653) 그대로
    - tsc 0 에러, npm run build 통과
    - 단일 파일 변경
    - 커밋 hash 1개
  </done>
</task>

<task type="auto">
  <name>Task 2: CctvModal 변환 (DVR 13대 일괄 입력, 라인 485~645, 약 160줄)</name>
  <files>cha-bio-safety/src/pages/InspectionPage.tsx</files>
  <action>
**범위:** `function CctvModal({ allCheckpoints, records, onClose, onSave })` 본문 (라인 485~645). BaeyeonModal (라인 653 시작) 직전까지가 Wave 5 영역 경계 — 이 Task 에서 절대 건드리지 않는다.

**시안:** `inspection-sketch-stairwell-cctv.html` 라인 651~893 (CCTV 영역 — Mobile Dark + Mobile Light 2 viewport) — 권위.

**lucide import 보강 (파일 상단 import 블록):**
- Video (이미 라인 24 import 됨) — 그대로
- **Server (신규 import 추가)** — `<Server size={13}>` mh-action "설비 현황" 좌측 아이콘
- CheckCircle2 / AlertTriangle / XCircle (이미 import 됨)
- import 패치 패턴 (Wave 1 i4r import 블록 라인 21~29 확장):
  ```typescript
  // 변경 전 (예시):
  // 카테고리 lucide (11종)
  Cloud, Shield, Car, Zap, BarChart3, Wind, ArrowDownToLine, Waves, Video, Square,
  // 변경 후 (Server 추가):
  Cloud, Shield, Car, Zap, BarChart3, Wind, ArrowDownToLine, Waves, Video, Square, Server,
  ```
- **단일 라인만 정밀 패치 — 다른 import 영역 손대지 않는다**

**비즈니스 보존 (한 줄도 변경 금지) — 절대 룰:**
- props 시그니처 그대로 (allCheckpoints, records, onClose, onSave — monthRecords / scheduleItems 없음, useInspectionRevisitPopup 호출도 없음 — 현재 코드 동작 그대로)
- state 6개 (dvrResults / memo / submitting / justSaved / submitError / visible) 그대로
- useEffect 2개 (requestAnimationFrame mount + cctvCPs init records 로드 — deps:[cctvCPs] eslint-disable-line) 한 줄도 변경 없음
- useMemo cctvCPs 그대로
- doneCnt / allDone 헬퍼 그대로
- **handleSave 본문 한 줄도 변경 금지 — 260505-cib 메모리 룰:**
  - `const photoKey = await photo.upload()` 그대로
  - `const photoTargetCp = photoKey ? (cctvCPs.find(cp => { const r = dvrResults[cp.id] ?? 'normal'; return r === 'caution' || r === 'bad' }) ?? cctvCPs[0]) : null` 그대로
  - `for (const cp of cctvCPs) { const keyForCp = photoTargetCp && cp.id === photoTargetCp.id ? photoKey : undefined; await onSave(cp.id, dvrResults[cp.id] ?? 'normal', memo, keyForCp ?? undefined) }` 그대로
  - try/catch/finally 본문 그대로
- usePhotoUpload() / PhotoButton 호출 그대로
- `navigate('/cctv')` 호출 (라인 565) 그대로 — mh-action "설비 현황" 클릭
- Math.ceil(CCTV_DVRS.length / 2) = half = 7 좌/우 분할 그대로
- CCTV_DVRS.slice(0, half).map(renderCard) / CCTV_DVRS.slice(half).map(renderCard) 그대로
- cctvCPs.find(c => c.locationNo === dvr.no) 짝 매핑 그대로
- renderCard 클로저 본문 (cp 매핑 / null 가드 / curResult / div 렌더) 본문 그대로 — 단 div 안의 스타일은 §시각 변환 룰 적용

**시각 변환 (v0.1.1 토큰 + Tailwind):**

1. **외곽 모달 컨테이너 (라인 552):**
   - 클래스: `fixed left-0 right-0 z-[99] flex flex-col overflow-hidden bg-surface-page`
   - 인라인: `style={{ top:'var(--sat, 0px)', bottom: NAV_BOTTOM, transform: visible ? 'translateY(0)' : 'translateY(100%)', transition:'transform 0.26s cubic-bezier(0.32,0.72,0,1)' }}`
   - 현재 `background:'var(--bg)'` 인라인 제거

2. **헤더 (라인 555-570):**
   - 외곽 div (라인 555): `flex items-center px-4 py-2.5 bg-surface-page border-b border-border-default flex-shrink-0 gap-2`
   - 좌측 아이콘 (라인 556 — `<span>📹</span>` 제거): `<Video size={18} className="text-text-secondary flex-shrink-0" />`
   - 중앙 텍스트 컨테이너 (라인 557-560): `flex-1 min-w-0`
     - mh-title (라인 558): `text-body font-bold text-text-primary` "CCTV 점검" (16 → text-body(14) 또는 arbitrary `text-[16px]`)
     - mh-sub (라인 559): `text-caption text-text-tertiary mt-0.5 leading-tight` "· B1F 방재센터 DVR 13대" (10 → 12 = text-caption 노안 친화 + 시안 라인 673 `· B1F 방재센터 DVR 13대` — 현재 코드 "DVR 12대" 오타? — **현재 코드의 "B1F 방재센터 DVR 12대" 텍스트 그대로 보존** — 시안과 텍스트 차이는 코드 권위 따름. 단 시안에 맞춰 13대 일치하려면 `B1F 방재센터 DVR {cctvCPs.length}대` 동적 표시도 OK — **권장:** 현재 코드 텍스트 보존 (Wave 5 는 시각만 변환))
   - allDone 미니 뱃지 (라인 561-563 — `allDone && !justSaved` 분기 그대로):
     - 클래스: `ml-auto text-caption font-semibold text-safe bg-safe-bg border border-safe-bar rounded-sm px-2 py-0.5 flex-shrink-0 inline-flex items-center gap-1` (11 → 12)
     - 좌측 `<CheckCircle2 size={12} className="flex-shrink-0">` (✓ 이모지 제거)
     - 텍스트 "완료"
   - mh-action 버튼 "설비 현황" (라인 564-569):
     - 클래스: `flex-shrink-0 px-2.5 py-1.5 rounded-sm border border-border-strong bg-surface-raised text-text-secondary text-caption font-bold cursor-pointer inline-flex items-center gap-1.5 transition-colors`
     - **현재 `display:'inline-flex', flexShrink:0` 인라인 → Tailwind 로 교체**
     - 좌측 아이콘 (📹 제거): `<Server size={13} className="flex-shrink-0" />`
     - 텍스트: "설비 현황"
     - onClick={() => navigate('/cctv')} 그대로
     - **주의:** 시안에서는 allDone 뱃지 없이 mh-action 만 표시. **현재 코드는 둘 다 표시 — 그대로 보존** (allDone 시 뱃지 + 액션 둘 다 — 시안과 시각 차이 OK)

3. **modal-body 본문 영역 (라인 573):**
   - `flex-1 overflow-y-auto px-3.5 py-3 flex flex-col gap-2.5`

4. **doneCnt > 0 미니 뱃지 (라인 575-579):**
   - `bg-safe-bg border border-safe-bar rounded-sm px-3 py-1.5 text-label font-semibold text-safe inline-flex items-center gap-1.5`
   - 좌측 `<CheckCircle2 size={14} className="flex-shrink-0">` (✓ 이모지 제거)
   - 텍스트: "{doneCnt}/{cctvCPs.length}대 이미 점검 완료" (11 → text-label(13) 노안 친화)

5. **batch-grid 2열 + IIFE (라인 582-614 — `(() => { ... })()` 그대로):**
   - half / renderCard / return 본문 — 한 줄도 변경 없음 (비즈니스 로직)
   - batch-grid (라인 605): `grid grid-cols-2 gap-2`
   - 좌 batch-col (라인 606): `flex flex-col gap-1.5`
   - 우 batch-col (라인 609): `flex flex-col gap-1.5`

6. **dvr-tile (라인 589-602 — renderCard 클로저 안):**
   - 외곽 (라인 589): `bg-surface-raised border border-border-default rounded-md px-2 pt-1.5 pb-1.5`
   - **권장 (시안 권위):** 라벨/desc 두 줄 분리 — 현재 코드는 한 줄 baseline justify-between (라인 590-593). 변환 시:
     - 라벨 (라인 591): `<div className="text-label font-bold text-text-primary leading-tight">{dvr.label}</div>` (11/700 → text-label(13))
     - desc (라인 592): `<div className="text-caption text-text-tertiary mt-0.5 mb-1.5 leading-tight min-h-[1.25em]">{dvr.desc}</div>` (9 → text-caption(12) 노안 친화)
   - **대안 (현재 코드 한 줄 baseline 보존):** 시안과 시각 차이 OK 면 인라인 baseline justify-between 그대로 + 토큰만 교체:
     - 컨테이너 (라인 590): `flex items-baseline justify-between mb-1`
     - 라벨 (라인 591): `text-caption font-bold text-text-secondary` (11 → 12)
     - desc (라인 592): `text-caption text-text-tertiary` (9 → 12)
   - **권장은 두 줄 분리 (시안 권위) — 가독성 + 노안 친화**
   - result-mini (라인 594-600): floor-tile 과 동일 패턴 (Task 1 의 §11 참조)
     - 컨테이너: `flex gap-1`
     - 버튼: `flex-1 px-1 py-1.5 rounded-pill border-[1.5px] inline-flex items-center justify-center gap-1 text-caption font-bold cursor-pointer whitespace-nowrap transition-colors` + 활성/비활성 status 토큰 분기 + lucide 12x12 (CheckCircle2/AlertTriangle/XCircle)
   - **resultBtnStyle 인라인 헬퍼 (라인 517-523) 제거** — Tailwind 분기로 대체
   - **resultMiniIcon / resultMiniCls 헬퍼 (Task 1 에서 도입된 것)** 재사용

7. **특이사항 + 사진 (라인 616-626):**
   - 라벨 row (라인 618-620): `flex items-center justify-between mb-1.5`
     - 좌 label (라인 619): `text-caption font-semibold text-text-tertiary tracking-wider` "특이사항 (선택)" (10 → 12)
     - 우 span (라인 620): `text-caption text-text-tertiary` "점검 사진 (선택)" (10 → 12)
   - memo-row (라인 622): `flex gap-2 items-start`
   - textarea (라인 623): `flex-1 h-[72px] px-3 py-2.5 rounded-md bg-surface-raised border border-border-default text-text-primary text-label resize-none outline-none box-border font-sans placeholder:text-text-tertiary` (12 → text-label(13))
   - **placeholder="특이사항을 입력하세요" 그대로 — 증상 피커 미도입 룰**
   - PhotoButton 그대로

8. **submitError / justSaved 박스 (라인 628 / 629):** Task 1 과 동일 패턴
   - submitError: `bg-danger-bg border border-danger-bar text-danger rounded-sm px-3 py-2 text-label font-semibold` (11 → text-label)
   - justSaved: `bg-safe-bg border border-safe-bar text-safe rounded-sm px-3 py-2 text-label font-semibold inline-flex items-center gap-1.5` + `<CheckCircle2 size={14}>` + "저장 완료"

9. **footer-bar (라인 632-642):**
   - 컨테이너: `flex gap-2 px-3.5 pt-2.5 pb-3 bg-surface-raised border-t border-border-default flex-shrink-0`
   - btn-close-mini: `px-4 py-3 rounded-md bg-surface-page border border-border-strong text-text-secondary text-label font-semibold cursor-pointer transition-colors`
   - btn-save (§6.4 그라디언트 + 시그니처 그림자 — Task 1 §14 와 동일 패턴):
     - 클래스: `flex-1 py-3.5 rounded-md text-body font-bold border-0 transition-all`
     - 인라인:
       ```tsx
       style={{
         background: (submitting || photo.uploading) ? 'var(--border-default)' : 'linear-gradient(135deg,#1d4ed8,#0ea5e9)',
         color: (submitting || photo.uploading) ? 'var(--text-tertiary)' : '#fff',
         cursor: (submitting || photo.uploading) ? 'default' : 'pointer',
         boxShadow: (submitting || photo.uploading) ? 'none' : '0 4px 14px rgba(37,99,235,0.35)',
       }}
       ```
     - 라벨 분기 (라인 640 그대로): `{photo.uploading ? '사진 업로드 중...' : submitting ? '저장 중...' : 'CCTV 점검 저장'}`

**Out of scope (이 Task 에서 건드리지 않음):**
- StairwellModal (라인 255~480) — Task 1 완료
- BaeyeonModal (라인 653 시작) — Wave 4 (tbj) 완료 → 한 줄도 변경 없음
- CCTV_DVRS 상수 (src/utils/cctv.ts) — 변경 금지
- INSPECT_RESULT_OPTIONS 상수 — 변경 금지 (단 .icon 텍스트 이모지 의존을 resultMiniIcon 헬퍼로 대체)
- **증상 피커 신설 절대 금지**
- **handleSave 본문 한 줄도 변경 금지** (260505-cib photoKey 1건 대표 부여)

**커밋:** `feat(260515-1p0): task 2 — CctvModal v0.1.1 토큰 + Tailwind 변환 (DVR 13대 일괄 + Server lucide + 260505-cib photoKey 1건 대표 부여 보존, 증상 피커 미도입)`
  </action>
  <verify>
    <automated>cd cha-bio-safety && npx tsc --noEmit 2>&1 | grep -E "error TS" | head -20; echo "---"; npm run build 2>&1 | tail -5; echo "---"; sed -n '485,645p' src/pages/InspectionPage.tsx | grep -vE '^\s*//' | grep -cE 'var\(--bg\)|var\(--bg2\)|var\(--bg3\)|var\(--bd\)|var\(--bd2\)|var\(--acl\)|var\(--t1\)|var\(--t2\)|var\(--t3\)|var\(--safe\)|var\(--warn\)|var\(--danger\)|var\(--fire\)' | xargs -I{} test {} -le 2 && echo "OLD_TOKENS: <=2 (저장 비활성 인라인 화이트리스트)" || echo "OLD_TOKENS: FAIL"; echo "---"; sed -n '485,645p' src/pages/InspectionPage.tsx | grep -vE '^\s*//' | grep -cE 'fontSize:\s*(9|10|11)[^0-9]|text-\[(9|10|11)px\]' | xargs -I{} test {} -eq 0 && echo "9_10_11PX_CCTV: 0" || echo "9_10_11PX_CCTV: FAIL"; echo "---"; sed -n '485,645p' src/pages/InspectionPage.tsx | grep -cE 'symptomPick|setSymptomPick' | xargs -I{} test {} -eq 0 && echo "NO_SYMPTOM_PICKER: OK" || echo "NO_SYMPTOM_PICKER: FAIL"; echo "---"; sed -n '485,645p' src/pages/InspectionPage.tsx | grep -cE 'photoTargetCp' | xargs -I{} test {} -ge 2 && echo "PHOTOTARGETCP_PRESERVED: OK (260505-cib)" || echo "PHOTOTARGETCP_PRESERVED: FAIL"; echo "---"; sed -n '485,645p' src/pages/InspectionPage.tsx | grep -cE "Math\.ceil\(CCTV_DVRS\.length\s*/\s*2\)" | xargs -I{} test {} -eq 1 && echo "CCTV_HALF_SPLIT: OK" || echo "CCTV_HALF_SPLIT: FAIL"; echo "---"; sed -n '485,645p' src/pages/InspectionPage.tsx | grep -cE "cctvCPs\.find\(c\s*=>\s*c\.locationNo\s*===\s*dvr\.no\)" | xargs -I{} test {} -eq 1 && echo "CCTV_PAIR_MAPPING: OK" || echo "CCTV_PAIR_MAPPING: FAIL"; echo "---"; sed -n '485,645p' src/pages/InspectionPage.tsx | grep -cE "navigate\('/cctv'\)" | xargs -I{} test {} -eq 1 && echo "NAVIGATE_CCTV: OK" || echo "NAVIGATE_CCTV: FAIL"; echo "---"; sed -n '485,645p' src/pages/InspectionPage.tsx | grep -cE '📹|📷|✅' | xargs -I{} test {} -eq 0 && echo "EMOJI_REMOVED: OK" || echo "EMOJI_REMOVED: FAIL"; echo "---"; grep -n "Server" src/pages/InspectionPage.tsx | head -3 && echo "Server lucide import 확인"; echo "---"; grep -n "function BaeyeonModal" src/pages/InspectionPage.tsx | head -1 && echo "BaeyeonModal 시작 라인 (이 라인 이후는 Wave 5 영역 아님 — Wave 4 보존)"</automated>
  </verify>
  <done>
    - CctvModal (485~645) 본문 마크업/스타일이 v0.1.1 토큰 + Tailwind 로 교체
    - 헤더 Video / mh-action Server / done CheckCircle2 / 결과 picker CheckCircle2/AlertTriangle/XCircle (12x12) — 이모지(📹/📷/✅) 0건
    - mh-action "설비 현황" 버튼 (lucide Server + 텍스트) → navigate('/cctv') 호출 그대로
    - dvr-tile 13개 batch-grid (Math.ceil(13/2)=7 좌/우 분할) 시안 1:1 — 시안 권위 따라 라벨/desc 두 줄 분리 (권장) 또는 한 줄 baseline 보존
    - dvr-tile 안 result-mini (pill + lucide + 한글 라벨, §7.1 일관성) — Task 1 의 resultMiniIcon / resultMiniCls 헬퍼 재사용
    - **handleSave 본문 한 줄도 변경 없음 (260505-cib photoKey 1건 대표 부여 정책 보존)**
    - props / state 6개 / useEffect 2개 / useMemo cctvCPs 한 줄도 변경 없음
    - usePhotoUpload / PhotoButton / navigate('/cctv') 호출 그대로
    - Math.ceil(CCTV_DVRS.length/2) / cctvCPs.find(c => c.locationNo === dvr.no) 짝 매핑 그대로
    - **Server lucide 신규 import 1건 추가** (다른 import 영역 손대지 않음)
    - **증상 피커 신설 0건** (CCTV 는 자동화 5종 카테고리 아님)
    - 옛 토큰 var(--bg/bd/acl/t1~3/safe/warn/danger/fire) 변환 영역 내 ≤2 (저장 비활성 인라인 화이트리스트만)
    - 9/10/11px 폰트가 변환 영역 내 0건
    - StairwellModal (255~480) 그대로 (Task 1 완료 상태 보존)
    - BaeyeonModal 시작 라인(현재 653) 그대로 — Wave 4 (tbj) 영역 한 줄도 변경 없음
    - tsc 0 에러, npm run build 통과
    - 단일 파일 변경
    - 커밋 hash 1개
  </done>
</task>

</tasks>

<verification>
**Plan 차원 종합 검증 (2 task 완료 후 자체 점검):**

| Gate | Expected | Method |
|------|----------|--------|
| Wave 5 변환 영역(255~645) 옛 토큰 사용 | ≤4 (저장 비활성 인라인 화이트리스트만 — var(--border-default)/var(--text-tertiary) 각 모달 1쌍씩) | `sed -n '255,645p' ... \| grep -cE 'var\(--bg\)\|var\(--bd\)\|var\(--acl\)\|var\(--t[123]\)\|var\(--(safe\|warn\|danger\|fire)\)'` |
| Wave 5 변환 영역 9/10/11px (div text) | 0 | `sed -n '255,645p' ... \| grep -cE 'fontSize:\s*(9\|10\|11)[^0-9]\|text-\[(9\|10\|11)px\]'` |
| 이모지(📹/📷/✅/⚠️/🚨) | 0 (lucide 전환). 단 done-mark `✓` 텍스트 1건은 허용 | `sed -n '255,645p' ... \| grep -cE '📹\|📷\|✅\|⚠️\|🚨'` |
| 증상 피커 신설(Stairwell/CCTV 영역 내) | 0 | `sed -n '255,645p' ... \| grep -cE 'symptomPick\|setSymptomPick'` |
| photoTargetCp (260505-cib 메모리 룰) 보존 | ≥4 (각 모달 2건 — 변수 선언 + cp.id 비교) | `sed -n '255,645p' ... \| grep -cE 'photoTargetCp'` |
| StairwellModal useInspectionRevisitPopup category | 1 (`'특별피난계단'`) | `sed -n '255,480p' ... \| grep -cE "category:\s*'특별피난계단'"` |
| STAIRWELLS.find + swDef.floors.slice | ≥3 (find 1 + slice 좌/우 2) | `sed -n '255,480p' ... \| grep -cE 'STAIRWELLS\.find\|swDef\.floors\.slice'` |
| CCTV_DVRS.slice(0, half) + slice(half) | 2 | `sed -n '485,645p' ... \| grep -cE 'CCTV_DVRS\.slice'` |
| cctvCPs.find(c => c.locationNo === dvr.no) | 1 | `sed -n '485,645p' ... \| grep -cE 'cctvCPs\.find\(c\s*=>\s*c\.locationNo\s*===\s*dvr\.no\)'` |
| navigate('/cctv') 호출 | 1 | `sed -n '485,645p' ... \| grep -cE "navigate\('/cctv'\)"` |
| navigate('/remediation/') 호출 (StairwellModal) | 1 | `sed -n '255,480p' ... \| grep -cE "navigate\('/remediation/"` |
| onSave 호출 시그니처 (cpId, result, memo, photoKey?) | ≥2 (각 모달 1건) | `sed -n '255,645p' ... \| grep -cE 'onSave\(cp\.id,'` |
| Server lucide import (CctvModal mh-action) | ≥1 (import 블록 + 사용처) | `grep -cE '\\bServer\\b' src/pages/InspectionPage.tsx \|\| true` (≥2 권장) |
| StairsIcon import 사용 (StairwellModal 헤더) | ≥1 (사용처) | `sed -n '255,480p' ... \| grep -cE '<StairsIcon'` |
| Video lucide 사용 (CctvModal 헤더) | ≥1 | `sed -n '485,645p' ... \| grep -cE '<Video'` |
| BaeyeonModal 시작 라인 변경 없음 | 보존 (`function BaeyeonModal({ group, allCheckpoints ...`) | `grep -n "function BaeyeonModal" ...` 라인 = 653 |
| TypeScript compile | 0 에러 | `npx tsc --noEmit` |
| Production build | 통과 | `npm run build` 마지막 5줄 |
| Diff scope | 단일 파일 | `git diff --stat HEAD` |

**비즈니스 보존 sanity check (260505-cib 메모리 룰 강제):**
- StairwellModal handleSave 안 `for (const cp of swCPs) { ... await onSave(cp.id, floorResults[cp.id] ?? 'normal', memo, keyForCp ?? undefined) }` 그대로
- CctvModal handleSave 안 `for (const cp of cctvCPs) { ... await onSave(cp.id, dvrResults[cp.id] ?? 'normal', memo, keyForCp ?? undefined) }` 그대로
- `const photoTargetCp = photoKey ? (...find(cp => caution/bad) ?? swCPs[0]/cctvCPs[0]) : null` 두 모달 모두 그대로
- `const keyForCp = photoTargetCp && cp.id === photoTargetCp.id ? photoKey : undefined` 두 모달 모두 그대로

**중요: chunk hash 자동 변경:**
- InspectionPage.tsx 본체 변경이므로 Vite 가 새 chunk hash 생성 → 배포 시 캐시 무효화 자동
- 디자인 리포 cbc7119 한정 (운영 PWA cha-bio-safety 영향 0)
</verification>

<success_criteria>
- 2 task 모두 완료 (커밋 2개)
- StairwellModal + CctvModal 2 컴포넌트 v0.1.1 토큰 + Tailwind 변환 완료
- **260505-cib photoKey 1건 대표 부여 정책(caution/bad 우선 → 첫 cp/층) 두 모달 모두 한 줄도 변경 없음** (전층/전 DVR 중복 저장 버그 재발 방지)
- props / state / useRef / useMemo / useEffect / handleSave / onSave 시그니처 / useInspectionRevisitPopup 호출 / navigate 호출 / STAIRWELLS / CCTV_DVRS 매핑 한 줄도 변경 없음
- 옛 토큰 사용 변환 영역 내 ≤4 (저장 비활성 인라인 화이트리스트만)
- 9/10/11px 폰트 변환 영역 내 0건
- 이모지(📹/📷/✅/⚠️/🚨) 변환 영역 내 0건 (lucide 통일 — Video / Server / Camera / CheckCircle2 / AlertTriangle / XCircle / StairsIcon)
- 증상 피커 신설 0건 (특별피난계단/CCTV 는 자동화 5종 카테고리 아님 — 메모리 룰)
- BaeyeonModal / DamperModal (Wave 4) + 그 외 모든 영역 본문 한 줄도 변경 없음
- tsc 0 에러, npm run build 통과
- 단일 파일 변경 (cha-bio-safety/src/pages/InspectionPage.tsx)
- 후속 트랙 (PowerPanelModal / ParkingGateModal) 의 fixed reference point 확보
</success_criteria>

<output>
After completion, create `.planning/quick/260515-1p0-redesign-02-inspection-tsx-wave-5-cctv/260515-1p0-SUMMARY.md` using the standard summary template (0l7/tbj reference). Include:

- Frontmatter: dependency_graph (requires i4r/pnr/sp7/tbj/0l7, provides Wave 5 변환), tech_stack (Server lucide 추가 / resultMiniIcon · resultMiniCls 헬퍼), key_files (modified: InspectionPage.tsx), decisions (헬퍼 위치 / dvr-tile 두 줄 분리 vs 한 줄 baseline 선택 / done-mark 텍스트 ✓ vs lucide Check), metrics (duration / tasks_completed / line_count_before/after).
- One-liner (260505-cib photoKey 1건 대표 부여 + 증상 피커 미도입 + 일괄 입력 강조)
- What was done (Task 1/2 각 변환 요약 + 핵심 lucide 매핑 + 헬퍼 도입)
- Verification table (모든 grep gate + tsc + build)
- 비즈니스 로직 보존 체크 (변환 전후 비교 — StairwellModal/CctvModal props/state/useEffect/handleSave/photoTargetCp/onSave/navigate/STAIRWELLS/CCTV_DVRS)
- Out of scope / 후속 트랙 (PowerPanelModal / ParkingGateModal)
- Self-Check PASSED 표
</output>
