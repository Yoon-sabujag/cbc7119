---
quick_id: 260521-8mw
mode: quick
branch: redesign/15-daily-report
phase: quick-260521-8mw
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cha-bio-safety/src/styles/components.css
autonomous: true
requirements:
  - "15-daily-report-W7-SW1"
must_haves:
  truths:
    - "components.css 끝부분에 §7~§12 (W2~W6) 신규 54 class 추가됨"
    - "@keyframes blink 정의 (W3 skeleton 의존) 1건 추가됨"
    - "14-reports 기존 §1~§6 (line 1~76) 1 byte 변경 0"
    - "tsc + vite build PASS — CSS bundle 정상 생성"
    - "DailyReportPage.tsx / App.tsx / functions / migrations / public 변경 0 (SW1 scope 분리)"
  artifacts:
    - path: cha-bio-safety/src/styles/components.css
      provides: "W2~W6 신규 54 class + @keyframes blink (line 77 이후 append)"
      contains: ".date-nav, .editable-card, .summary-card, .download-btn, .desktop-layout, .daily-portrait-wrapper, @keyframes blink"
  key_links:
    - from: cha-bio-safety/src/styles/components.css §7
      to: W7 §1.1 verbatim fence (sketch-wave-2 line 386~420)
      via: paste 만 (추측 X, memory feedback_planner_prompt_sketch_verbatim)
      pattern: ".date-nav, .date-nav-btn, .date-display, .date-nav-spacer"
    - from: cha-bio-safety/src/styles/components.css §8
      to: W7 §1.2 verbatim fence (sketch-wave-3 line 427~565)
      via: paste 만
      pattern: ".editable-card*, .summary-card*"
    - from: cha-bio-safety/src/styles/components.css §9
      to: W7 §1.3 verbatim fence (sketch-wave-4 line 429~485)
      via: paste 만
      pattern: ".download-action, .download-btn*"
    - from: cha-bio-safety/src/styles/components.css §10
      to: W7 §1.4 verbatim fence (sketch-wave-5 line 398~448)
      via: paste 만
      pattern: ".desktop-layout, .desktop-edit-panel*, .desktop-portrait-*"
    - from: cha-bio-safety/src/styles/components.css §11
      to: W7 §1.5 verbatim fence (sketch-wave-6 line 450~652)
      via: paste 만
      pattern: ".daily-portrait-*"
    - from: cha-bio-safety/src/styles/components.css §12
      to: W7 §1.2 끝부분 @keyframes blink fence
      via: @layer components 블록 밖 위치 (Tailwind keyframes 패턴 안전)
      pattern: "@keyframes blink"
---

<objective>
**15-daily-report SW1 — components.css 신규 54 class + @keyframes blink 추가.**

Purpose:
- redesign/15-daily-report 의 SW1 (sketch 단계 X, **TSX 변환 sub-wave 1**) 산출물.
- W2~W6 5개 sketch HTML 의 verbatim CSS fence 를 components.css 끝부분 (line 77 `}` 직전) 에 paste.
- 14-reports 의 components.css 기존 §1~§6 (line 1~76, 6 inherit class + sidelist + a4-preview 등) 는 1 byte 도 변경 안 함.
- SW2 (모바일 영역 TSX 변환) / SW3 (데스크톱 + DailyPortraitPreview wrapper 변환) 가 본 class 를 소비.

Output:
- `cha-bio-safety/src/styles/components.css` diff 만 (line 77 직전에 §7~§12 + @keyframes blink append).
- atomic commit 1건: `feat(15-daily-report): SW1 components.css +54 class (W2~W6 sketch verbatim)`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@./CLAUDE.local.md
@cha-bio-safety/docs/redesign-context/15-daily-report/wave-7-tsx-conversion-checklist.md
@cha-bio-safety/docs/redesign-context/15-daily-report/sketch-wave-2-mobile-header-date-nav.html
@cha-bio-safety/docs/redesign-context/15-daily-report/sketch-wave-3-editable-cards-personnel.html
@cha-bio-safety/docs/redesign-context/15-daily-report/sketch-wave-4-download-action.html
@cha-bio-safety/docs/redesign-context/15-daily-report/sketch-wave-5-desktop-layout.html
@cha-bio-safety/docs/redesign-context/15-daily-report/sketch-wave-6-portrait-preview-wrapper.html
@cha-bio-safety/src/styles/components.css

## 현재 components.css 상태 (단일 source-of-truth 박제, 1 byte 변경 0 대상)

`cha-bio-safety/src/styles/components.css` 는 현재 77 lines, 14-reports redesign 의 SW1 결과:

- line 1~18: 헤더 코멘트 (14-reports redesign 박제 — 본 SW1 은 헤더 코멘트도 변경 X, 단 §7 위에 새 구분선 1개 추가 OK)
- line 20: `@layer components {`
- line 22~29: §1. 자체 헤더 (.page-header / .back-btn / .page-title / .year-pager / .year-pager-slot / .year-nav-btn / .year-label)
- line 31~38: §2. 본문 + 카드 (.page-body / .report-card / .report-card-head / .report-card-title / .report-card-sub / .report-card-btn / .report-card--loading .report-card-btn)
- line 40~42: §3. dot-meta + footer (.dot-meta / .page-footer-note)
- line 44~53: §4. 데스크톱 toolbar (.toolbar / .toolbar-year-label / .toolbar-select / .toolbar-batch-btn / .toolbar-batch-btn--loading / .toolbar-spacer / .toolbar-selected-title / .toolbar-individual-btn / .toolbar-individual-btn--loading)
- line 55~63: §5. 데스크톱 sidelist (.sidelist / .sidelist-section-header / .sidelist-row / .sidelist-row--selected / .sidelist-row--hover / .sidelist-row-title / .sidelist-row-title--selected / .sidelist-row-sub)
- line 65~75: §6. ExcelPreview wrapper + A4 placeholder (.preview-wrapper / .a4-preview / .a4-preview-title / .a4-preview-meta / .a4-preview-table-placeholder / .a4-preview-cell / .a4-preview-cell--header)
- line 77: `}`  ← **이 닫는 brace 직전에 §7~§11 + §12 (@keyframes blink) 를 insert**

15-daily-report W2~W6 신규 class 6 inherit (`.page-header / .back-btn / .page-title / .page-body / .dot-meta / .page-footer-note`) 는 14-reports 와 동일 — **재정의 0**.

## W7 §2.3 분할 가이드 (verbatim, components.css 안 순서)

1. §7. 15-daily-report W2 (dateNav) — 4 class (`.date-nav / .date-nav-btn / .date-display / .date-nav-spacer`)
2. §8. 15-daily-report W3 (EditableCard / SummaryCard) — 14 class
   - EditableCard 본체 7 class: `.editable-card / .editable-card-head / .editable-card-label / .editable-card-actions / .editable-card-btn--reset / .editable-card-btn--save / .editable-card-textarea` (+ `:focus` 상태 1건)
   - SummaryCard 본체 3 class: `.summary-card / .summary-card-label / .summary-card-body`
   - SummaryCard 상태 helper 4 class: `.summary-card-skeleton / .summary-card-error / .summary-card-empty / .summary-card-state-label`
3. §9. 15-daily-report W4 (다운로드 액션) — 8 class
   - `.download-action / .download-action--desktop / .download-action--desktop > .download-btn / .download-btn / .download-btn--daily / .download-btn--monthly / .download-btn--disabled / .download-btn--daily.download-btn--disabled / .download-btn-icon`
4. §10. 15-daily-report W5 (데스크톱 layout) — 6 class (`.desktop-layout / .desktop-edit-panel / .desktop-edit-panel-header / .desktop-portrait-wrapper / .desktop-portrait-print-label / .desktop-portrait-placeholder`)
   - `.desktop-portrait-placeholder` 는 sketch only — empty body 정의 1줄 또는 컨테이너 placeholder 코멘트 처리 OK (W7 §1.4 verbatim)
5. §11. 15-daily-report W6 (DailyPortraitPreview wrapper) — 22 class (`.daily-portrait-wrapper / -image / -overlay-area / -overlay-area--calib / -overlay-item / -overlay-item--large / -calib-bar / -calib-bar-step / -calib-bar-label / -calib-bar-coord / -calib-confirm / -calib-cancel / -setup-btn / -setup-btn--ready / -setup-btn--missing / -calib-marker / -calib-marker-crosshair-h / -calib-marker-crosshair-v / -calib-marker-dot / -calib-marker-dot--active / -placeholder-img`)
   - `.daily-portrait-placeholder-img` 는 sketch only — empty body 정의 1줄 또는 코멘트 처리 OK (W7 §1.5 verbatim 끝부분)
6. §12. @keyframes blink (W3 skeleton 의존) — `@layer components { ... }` 블록 **밖** (Tailwind keyframes 안전 패턴, line 77 `}` 닫고 그 다음 줄에 정의)

**total 54 신규 class + @keyframes blink** (W7 §1.6 합계 박제: W2 4 + W3 14 + W4 8 + W5 6 + W6 22 = 54).

## W7 §2.2 / §1.1~§1.5 verbatim CSS fence — executor 는 본 fence 만 복사 (추측 X)

executor 는 `cha-bio-safety/docs/redesign-context/15-daily-report/wave-7-tsx-conversion-checklist.md` 의 §1.1 (W2), §1.2 (W3), §1.3 (W4), §1.4 (W5), §1.5 (W6) 의 CSS fence 를 **그대로** components.css 에 paste 한다. fence 안 inline 코멘트 (예: `/* source navBtn gap 2 → 4 (노안 격상, space-1) */`) 도 그대로 유지. 폰트 격상 px 도 W7 verbatim (`font-size: 14px;` / `16px` 등).

`<style>` 블록 안 fence 가 워크트리에 verbatim 으로 살아있는지 보조 grep 으로 직접 확인 (옵션):
- `grep -nE '^\s*\.[a-z][a-z0-9-]+\s*\{' cha-bio-safety/docs/redesign-context/15-daily-report/sketch-wave-2-mobile-header-date-nav.html | head -20`
- 동일 패턴으로 wave-3 / 4 / 5 / 6 검증

## 14-reports 기존 6 inherit class 보호 (재정의 0 검증 필수)

다음 6 class 는 14-reports 의 components.css 안에 이미 정의됨. 15-daily-report 의 SW1 에서 **재정의 0, 1 byte 변경 0**:

| class | components.css 위치 | 비고 |
|---|---|---|
| `.page-header` | line 23 | 재정의 X (변경 0) |
| `.back-btn` | line 24 | 재정의 X (변경 0) |
| `.page-title` | line 25 | 재정의 X (변경 0) |
| `.page-body` | line 32 | 재정의 X (변경 0) |
| `.dot-meta` | line 41 | 재정의 X (변경 0) |
| `.page-footer-note` | line 42 | 재정의 X (변경 0, W4 sketch padding 8 0 20 fallback 은 본문에 14-reports 8 16 20 verbatim 유지) |

## §7 위 구분선 코멘트 (옵션 1 — default)

§7 위에 다음 구분선 1개 추가 (헤더 코멘트 line 1~18 은 손대지 않음):

```css
  /* ============================================================
   * 15-daily-report SW1 — sections §7~§12 (W2~W6 sketch verbatim)
   * Source: W7 checklist §1.1~§1.5 fence (sketch-wave-2~6 verbatim)
   * 메모리 룰:
   * - feedback_planner_prompt_sketch_verbatim (fence verbatim paste)
   * - feedback_redesign_sketch_rule_enforcement (§6.2 정보 카드 status 색 X)
   * - feedback_sketch_realistic_data (카피 / 시그니처 / 분기 1 byte 변경 0)
   * - feedback_tailwind_w8_h8_is_48px (.date-nav-btn 28 / .back-btn 34 명시 px)
   * - feedback_text_caption_leading_none (작은 컨테이너 line-height: 1)
   * ============================================================ */
```

## 메모리 룰 박제 (≥5건, executor 가 작업 중 의식해야 할 슬러그)

1. **`feedback_planner_prompt_sketch_verbatim`** — sketch CSS fence verbatim 인용. → executor 는 W7 §1.1~§1.5 fence 를 grep / 직접 paste 만, **추측 X**. inline 코멘트 (`/* source ... verbatim */`) 도 그대로 유지.
2. **`feedback_redesign_sketch_rule_enforcement`** — §6.2 정보 카드 status 색 금지. → `.editable-card / .summary-card` 는 `var(--surface-raised) + var(--border-default)` 만, status 색 0. CTA = `.download-btn--daily` (var(--status-safe-bar) solid) 만 예외.
3. **`feedback_sketch_realistic_data`** — 표시 분기 / 라벨 / 시그니처 1 byte 변경 0. → 본 SW1 은 CSS 정의만 추가, DailyReportPage.tsx 사용처 변경 0. EditableCard 9 prop / SummaryCard 카피 24 string 모두 SW2/SW3 에서 보존.
4. **`feedback_tailwind_w8_h8_is_48px`** — `w-8` = 48px (기본 32 아님). → `.date-nav-btn 28px / .back-btn 34px / .date-nav-spacer 28px` 모두 CSS 명시 px (Tailwind w-N utility 안 씀). 헤더 line 24 `.back-btn` 34×34 도 inherit (변경 X).
5. **`feedback_text_caption_leading_none`** — text-caption (12px lh:1.5 = 18px 시각 패딩) 가 작은 컨테이너 안 시각 패딩. → `.date-display / .date-nav-btn / .editable-card-btn--reset / .editable-card-btn--save / .summary-card-state-label / .daily-portrait-calib-confirm / .daily-portrait-calib-cancel / .desktop-portrait-print-label / .daily-portrait-setup-btn` 모두 `line-height: 1` 명시 (W7 fence verbatim).
6. **`feedback_tsx_wave_emoji_dot_gap`** — sketch negative gate (이모지 0) + dot span 추가 markup verify. → 본 SW1 components.css 안 이모지 0건 (`grep -cE '[⬇⚠🎯⚡🔥]' = 0`). dot-meta 정의는 14-reports 의 line 41 inherit 만.
7. **(보너스) `feedback_cbc7119_design_never_wrangler`** — 디자인 wave 중 wr+angler 명령 절대 X. → 본 SW1 은 commit 1건 만. `wr+angler` 0건 / `npm run d+eploy` 0건. main push 시 GitHub Actions 가 cbc7119-preview 자동 배포 (별 trigger 의무 0).
8. **(보너스) `feedback_check_branch_before_edit`** — main 단일-trunk 운영, dirty 면 사용자 컨펌. → 본 wave 진입 전 `git branch --show-current` = `redesign/15-daily-report` 확인. `git status` 결과 clean 또는 untracked 만.
9. **(보너스) `feedback_avoid_premature_confirmation`** — "거의 일치 / 완벽" 자신감 표현 금지. → SW1 verify gate ≥6 positive + ≥7 negative 모두 PASS 해도 "완벽" 표현 X. grep gate 출력 + tsc + vite build 결과 사실 보고 + 사용자 컨펌 요청 만.
</context>

<tasks>

<task type="auto">
  <name>Task 1: components.css §7~§12 추가 (54 신규 class + @keyframes blink, W7 §1.1~§1.5 verbatim paste)</name>
  <files>cha-bio-safety/src/styles/components.css</files>
  <action>
**작업 순서 (atomic 1-commit, line 77 직전에 insert):**

1. **사전 검증 (편집 전, memory `feedback_check_branch_before_edit`):**
   - `git -C /Users/jykevin/Documents/cbc7119-design branch --show-current` 결과 = `redesign/15-daily-report` 확인
   - `git -C /Users/jykevin/Documents/cbc7119-design status --short` 결과 clean 또는 untracked 만
   - components.css 가 현재 77 lines / line 77 = `}` 단일 닫는 brace 확인:
     - `wc -l cha-bio-safety/src/styles/components.css` = 77
     - line 76: 빈 줄 (또는 §6 마지막 class 후 빈 줄)
     - line 77: `}` (단일 brace, `@layer components` 의 닫는 brace)

2. **§7~§11 + §12 verbatim paste:**
   - 편집 위치: line 77 `}` **직전** (즉 line 76 빈 줄 다음)
   - 추가 순서:
     - **(§7 위 구분선 코멘트 1개)** — `<context>` 의 "§7 위 구분선 코멘트" 섹션 verbatim 박제
     - **(§7)** — W7 checklist §1.1 의 W2 4 class fence 그대로 paste. 각 class 시작 indent 는 14-reports 와 동일 (`@layer components {` 안이므로 leading 2 space). 헤더 코멘트: `  /* ── §7. 15-daily-report W2 (dateNav, 4 class) ── */`
     - **(§8)** — W7 checklist §1.2 의 W3 14 class fence 그대로 paste:
       - EditableCard 본체 7 class (+ `:focus` 1 상태 → 정의 8 블록, class count 7)
       - SummaryCard 본체 3 class
       - SummaryCard 상태 helper 4 class
       - 헤더 코멘트: `  /* ── §8. 15-daily-report W3 (EditableCard 7 + SummaryCard 3 + helper 4) ── */`
       - **주의: `@keyframes blink` 는 §8 안에 정의하지 않음 — §12 (라인 77 `}` 닫고 그 다음 줄, @layer 밖) 에서 정의.**
     - **(§9)** — W7 checklist §1.3 의 W4 8 class fence 그대로 paste. 헤더 코멘트: `  /* ── §9. 15-daily-report W4 (다운로드 액션, 8 class) ── */`
     - **(§10)** — W7 checklist §1.4 의 W5 6 class fence 그대로 paste. `.desktop-portrait-placeholder` 는 W7 §1.4 verbatim 코멘트 블록 그대로 유지 (`/* W5 sketch 단계만 — TSX 변환 시 실제 DailyPortraitPreview 컴포넌트로 대체. ... */`) — 빈 body `.desktop-portrait-placeholder { }` 도 OK. 헤더 코멘트: `  /* ── §10. 15-daily-report W5 (데스크톱 layout, 6 class) ── */`
     - **(§11)** — W7 checklist §1.5 의 W6 22 class fence 그대로 paste. `.daily-portrait-placeholder-img` 는 W7 §1.5 끝부분 verbatim 코멘트 블록 그대로 유지 — 빈 body `.daily-portrait-placeholder-img { }` 도 OK. 헤더 코멘트: `  /* ── §11. 15-daily-report W6 (DailyPortraitPreview wrapper, 22 class) ── */`
   - **(§12)** — line 77 `}` **닫고** 그 **다음 줄** (즉 `@layer components { ... }` 블록 **밖**) 에 `@keyframes blink` 정의 추가:
     ```css

     /* ── §12. @keyframes blink (W3 .summary-card-skeleton 의존) ── */
     @keyframes blink {
       0%, 100% { opacity: 0.4; }
       50%      { opacity: 1.0; }
     }
     ```
     **이유:** Tailwind keyframes 패턴 안전 — `@keyframes` 는 `@layer components { ... }` 안에 있어도 동작하지만, Vite + PostCSS + Tailwind 의 layer order 차이로 일부 빌드에서 unsafe. `@layer` 밖이 가장 안전.

3. **14-reports 기존 §1~§6 (line 1~76) 보호 검증:**
   - 편집 후 즉시 `git -C /Users/jykevin/Documents/cbc7119-design diff cha-bio-safety/src/styles/components.css` 실행
   - line 1~76 변경 0 확인 — diff hunk 시작이 모두 line 77 이후이거나, line 77 의 `}` 가 §11 이후로 1줄 밀린 형태만 OK.
   - 기존 6 inherit class (`.page-header / .back-btn / .page-title / .page-body / .dot-meta / .page-footer-note`) 의 정의 1 byte 변경 0 확인.

4. **W7 §10 verify gate ≥6 positive + ≥7 negative 모두 실행 (commit 전):**

   **positive gate:**
   ```bash
   cd /Users/jykevin/Documents/cbc7119-design
   grep -c '^\s*\.date-nav' cha-bio-safety/src/styles/components.css        # ≥3 (date-nav / date-nav-btn / date-nav-spacer / date-display 중 .date-nav* 만 ≥3)
   grep -c '^\s*\.editable-card' cha-bio-safety/src/styles/components.css   # ≥7 (editable-card / -head / -label / -actions / -btn--reset / -btn--save / -textarea)
   grep -c '^\s*\.summary-card' cha-bio-safety/src/styles/components.css    # ≥3 (summary-card / -label / -body, helper 4 포함 시 7)
   grep -c '^\s*\.download-' cha-bio-safety/src/styles/components.css       # ≥7 (download-action / -action--desktop / -btn / -btn--daily / -btn--monthly / -btn--disabled / -btn-icon)
   grep -c '^\s*\.desktop-' cha-bio-safety/src/styles/components.css        # ≥6 (desktop-layout / -edit-panel / -edit-panel-header / -portrait-wrapper / -portrait-print-label / -portrait-placeholder)
   grep -c '^\s*\.daily-portrait-' cha-bio-safety/src/styles/components.css # ≥9 (daily-portrait-wrapper / -image / -overlay-area / -calib-bar / -calib-confirm / -calib-cancel / -setup-btn / -calib-marker / -calib-marker-dot ...)
   grep -cE '^@keyframes blink|^\s*@keyframes blink' cha-bio-safety/src/styles/components.css  # =1
   ```

   **negative gate (이모지 / 그라데이션 / 폰트 격상 / 다른 파일 변경 0):**
   ```bash
   # 이모지 0건 — W7 §6 gate 1 (Perl regex 더 정밀: U+2B07 ⬇, U+26A0 ⚠, U+1F3AF 🎯, U+26A1 ⚡, U+1F525 🔥)
   grep -cP "[\x{2B07}\x{26A0}\x{1F3AF}\x{26A1}\x{1F525}\x{2B06}\x{2B05}\x{27A1}\x{2705}]" cha-bio-safety/src/styles/components.css   # =0
   # linear-gradient 0건 — W1 #1 LOCKED (다운로드 daily 솔리드 통일)
   grep -cE 'linear-gradient\(' cha-bio-safety/src/styles/components.css                                                              # =0
   # 9 / 10 / 11px 0건 — design-system §1.1 본문 폰트 최소 16px 룰 (캘리브 overlayItem 예외는 인라인 px, components.css 안에는 0)
   grep -cE 'font-size:\s*(9|10|11)px' cha-bio-safety/src/styles/components.css                                                       # =0
   # DailyReportPage.tsx / App.tsx / functions / migrations / public 변경 0
   git -C /Users/jykevin/Documents/cbc7119-design diff --name-only HEAD -- cha-bio-safety/src/pages/DailyReportPage.tsx               # 빈 출력
   git -C /Users/jykevin/Documents/cbc7119-design diff --name-only HEAD -- cha-bio-safety/src/App.tsx                                 # 빈 출력
   git -C /Users/jykevin/Documents/cbc7119-design diff --name-only HEAD -- cha-bio-safety/functions                                   # 빈 출력
   git -C /Users/jykevin/Documents/cbc7119-design diff --name-only HEAD -- cha-bio-safety/migrations                                  # 빈 출력
   git -C /Users/jykevin/Documents/cbc7119-design diff --name-only HEAD -- cha-bio-safety/public/templates                            # 빈 출력
   # 14-reports 기존 6 class 정의 변경 0 — page-header / back-btn / page-title / page-body / dot-meta / page-footer-note 의 정의 라인 변경 0
   git -C /Users/jykevin/Documents/cbc7119-design diff cha-bio-safety/src/styles/components.css | grep -E '^-\s*\.(page-header|back-btn|page-title|page-body|dot-meta|page-footer-note)\b'   # 빈 출력
   # 다른 페이지 redesign-context 문서 변경 0
   git -C /Users/jykevin/Documents/cbc7119-design diff --name-only HEAD -- cha-bio-safety/docs/redesign-context                       # 빈 출력
   ```

5. **tsc + vite build (commit 전 최종 검증, W7 §10 gate 8/9):**
   ```bash
   cd /Users/jykevin/Documents/cbc7119-design/cha-bio-safety
   npx tsc --noEmit                                                          # exit 0, 0 error
   npm run build                                                              # exit 0, dist/ 생성, CSS bundle 안 새 54 class 포함
   ```
   - 둘 다 PASS 확인. 실패 시 components.css 의 syntax error 또는 `@layer` brace 매칭 점검.
   - CSS 가 빌드 산출물 (`dist/assets/*.css`) 안 들어가는지 보조 확인 (옵션): `grep -c '\.editable-card' cha-bio-safety/dist/assets/*.css | head -3`

6. **atomic commit:**
   ```bash
   cd /Users/jykevin/Documents/cbc7119-design
   git add cha-bio-safety/src/styles/components.css
   git commit -m "feat(15-daily-report): SW1 components.css +54 class (W2~W6 sketch verbatim)

- §7 W2 dateNav (4 class)
- §8 W3 EditableCard 7 + SummaryCard 3 + helper 4 (14 class)
- §9 W4 다운로드 액션 (8 class)
- §10 W5 데스크톱 layout (6 class)
- §11 W6 DailyPortraitPreview wrapper (22 class)
- §12 @keyframes blink (@layer components 블록 밖)
- 14-reports 기존 §1~§6 (line 1~76) 1 byte 변경 0
- DailyReportPage.tsx / App.tsx / functions / migrations / public / 다른 페이지 docs 변경 0
- W7 checklist §1.1~§1.5 fence verbatim paste (메모리 룰 feedback_planner_prompt_sketch_verbatim)

🤖 Generated with [Claude Code](https://claude.com/claude-code)

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
   git status   # commit 직후 clean 확인
   ```

7. **사용자 보고 (memory `feedback_avoid_premature_confirmation`):**
   - "거의 일치 / 완벽" 자신감 표현 금지.
   - 사실 보고만:
     - "components.css 54 신규 class + @keyframes blink 추가 commit 완료 ({short hash})"
     - positive gate ≥6 / negative gate ≥7 PASS 결과 출력
     - "14-reports 기존 §1~§6 (line 1~76) diff 0 byte 확인"
     - "tsc + vite build PASS"
     - "DailyReportPage.tsx / App.tsx / functions / migrations / public 변경 0"
     - "SW1 자체는 시각 변화 없음 (CSS 정의만 추가, 사용처는 SW2 / SW3 변환 wave 에서 import). main 머지 + cbc7119-preview 자동 배포는 SW4 verify gate 통과 후."
     - "다음 단계 사용자 컨펌 부탁."

**작업 가이드:**

- **CSS verbatim paste 원칙:** W7 checklist §1.1~§1.5 fence (각 wave 별 코멘트 포함) 를 그대로 paste. inline 코멘트 (`/* source line ... verbatim */` 등) 도 그대로 유지. 폰트 격상 값 (16px / 14px / 12px) 도 W7 verbatim. 추측 / 시간 절약 위해 줄임 절대 X.
- **indent:** `@layer components { ... }` 블록 안이므로 leading 2 space. W7 fence 는 4-space indent 일 수 있으니 paste 시 2-space 로 통일 (14-reports 기존 §1~§6 와 동일 스타일).
- **`@keyframes blink` 위치:** `@layer components { ... }` 의 닫는 `}` 다음 줄 (즉 §12 는 @layer 밖). Vite + Tailwind + PostCSS 빌드 안전.
- **`.desktop-portrait-placeholder` / `.daily-portrait-placeholder-img`:** W7 §1.4 / §1.5 끝부분의 verbatim 코멘트 블록 그대로 유지 (sketch only — TSX 변환 시 실 컴포넌트로 대체). 빈 body `{ }` 도 OK, 또는 코멘트만 두고 빈 정의 생략도 OK (54 class 카운트에 포함은 시키지 않아도 무관 — 다른 52 의미 class 가 verify gate 충족).
- **wr+angler 금지 (CLAUDE.local.md 룰 + memory `feedback_cbc7119_design_never_wrangler`):** 본 task 안에서 `wr+angler` / `npm run d+eploy` 명령 0건. cbc7119-preview 자동 배포는 SW4 commit 후 main 머지 시 GitHub Actions trigger.
- **컨텍스트 절감:** W7 checklist 안 fence 가 이미 verbatim 박제 — executor 는 W7 markdown 의 §1.1~§1.5 만 다시 읽고 paste. 다른 페이지 sketch / DailyReportPage.tsx / App.tsx / tokens.css 등 추가 read 0.
  </action>
  <verify>
    <automated>cd /Users/jykevin/Documents/cbc7119-design && grep -c '^\s*\.date-nav' cha-bio-safety/src/styles/components.css | awk '$1>=3 {exit 0} {exit 1}' && grep -c '^\s*\.editable-card' cha-bio-safety/src/styles/components.css | awk '$1>=7 {exit 0} {exit 1}' && grep -c '^\s*\.summary-card' cha-bio-safety/src/styles/components.css | awk '$1>=3 {exit 0} {exit 1}' && grep -c '^\s*\.download-' cha-bio-safety/src/styles/components.css | awk '$1>=7 {exit 0} {exit 1}' && grep -c '^\s*\.desktop-' cha-bio-safety/src/styles/components.css | awk '$1>=6 {exit 0} {exit 1}' && grep -c '^\s*\.daily-portrait-' cha-bio-safety/src/styles/components.css | awk '$1>=9 {exit 0} {exit 1}' && grep -cE '^@keyframes blink|^\s*@keyframes blink' cha-bio-safety/src/styles/components.css | awk '$1==1 {exit 0} {exit 1}' && grep -cP "[\x{2B07}\x{26A0}\x{1F3AF}\x{26A1}\x{1F525}]" cha-bio-safety/src/styles/components.css | awk '$1==0 {exit 0} {exit 1}' && grep -cE 'linear-gradient\(' cha-bio-safety/src/styles/components.css | awk '$1==0 {exit 0} {exit 1}' && grep -cE 'font-size:\s*(9|10|11)px' cha-bio-safety/src/styles/components.css | awk '$1==0 {exit 0} {exit 1}' && (cd cha-bio-safety && npx tsc --noEmit) && (cd cha-bio-safety && npm run build)</automated>
  </verify>
  <done>
- [ ] components.css line 77 직전에 §7~§11 + line 77 `}` 뒤에 §12 (@keyframes blink) append 완료
- [ ] 신규 54 class 추가 (W2 4 + W3 14 + W4 8 + W5 6 + W6 22)
- [ ] @keyframes blink 정의 1건 추가 (@layer 블록 밖)
- [ ] 14-reports 기존 §1~§6 (line 1~76) 1 byte 변경 0 (`git diff` 의 `^-` 라인 안에 기존 6 inherit class 없음)
- [ ] positive gate ≥6 PASS (`.date-nav` ≥3 / `.editable-card` ≥7 / `.summary-card` ≥3 / `.download-` ≥7 / `.desktop-` ≥6 / `.daily-portrait-` ≥9 / `@keyframes blink` =1)
- [ ] negative gate ≥7 PASS (이모지 0 / linear-gradient 0 / fontSize 9-10-11px 0 / DailyReportPage.tsx diff 0 / App.tsx diff 0 / functions diff 0 / migrations diff 0 / public/templates diff 0 / 다른 페이지 docs diff 0)
- [ ] `npx tsc --noEmit` PASS (exit 0, 0 error)
- [ ] `npm run build` PASS (exit 0, dist/ 생성, CSS bundle 안 새 54 class 포함)
- [ ] atomic commit 1건 완료 (메시지: `feat(15-daily-report): SW1 components.css +54 class (W2~W6 sketch verbatim)`)
- [ ] `git status` clean 확인
- [ ] wr+angler 명령 0건 / `npm run d+eploy` 0건 (CLAUDE.local.md 룰 + memory `feedback_cbc7119_design_never_wrangler`)
- [ ] 사용자 보고 — "거의 일치 / 완벽" 표현 0, 사실 + grep gate 출력 + 컨펌 요청만 (memory `feedback_avoid_premature_confirmation`)
  </done>
</task>

</tasks>

<verification>
**1) Positive gate (commit 직전, ≥7 명령):**
- `grep -c '^\s*\.date-nav' components.css` ≥3
- `grep -c '^\s*\.editable-card' components.css` ≥7
- `grep -c '^\s*\.summary-card' components.css` ≥3
- `grep -c '^\s*\.download-' components.css` ≥7
- `grep -c '^\s*\.desktop-' components.css` ≥6
- `grep -c '^\s*\.daily-portrait-' components.css` ≥9
- `grep -cE '^@keyframes blink|^\s*@keyframes blink' components.css` =1

**2) Negative gate (commit 직전, ≥7 명령):**
- `grep -cP "[\x{2B07}\x{26A0}\x{1F3AF}\x{26A1}\x{1F525}]" components.css` =0 (이모지 — U+2B07 ⬇, U+26A0 ⚠, U+1F3AF 🎯, U+26A1 ⚡, U+1F525 🔥)
- `grep -cE 'linear-gradient\(' components.css` =0 (W1 #1 LOCKED 그라데이션 폐기)
- `grep -cE 'font-size:\s*(9|10|11)px' components.css` =0 (design-system §1.1 본문 폰트 최소 16px / 캘리브 overlay 인라인 예외만)
- `git diff --name-only HEAD -- cha-bio-safety/src/pages/DailyReportPage.tsx` = 빈 출력 (SW2/SW3 분리)
- `git diff --name-only HEAD -- cha-bio-safety/src/App.tsx` = 빈 출력
- `git diff --name-only HEAD -- cha-bio-safety/functions` = 빈 출력
- `git diff --name-only HEAD -- cha-bio-safety/migrations` = 빈 출력
- `git diff --name-only HEAD -- cha-bio-safety/public/templates` = 빈 출력
- `git diff --name-only HEAD -- cha-bio-safety/docs/redesign-context` = 빈 출력 (W7 markdown / 다른 페이지 docs 변경 0)
- `git diff cha-bio-safety/src/styles/components.css | grep -E '^-\s*\.(page-header|back-btn|page-title|page-body|dot-meta|page-footer-note)\b'` = 빈 출력 (14-reports 기존 6 class 재정의 0)

**3) Build gate (commit 직전, 2 명령):**
- `(cd cha-bio-safety && npx tsc --noEmit)` PASS (exit 0)
- `(cd cha-bio-safety && npm run build)` PASS (exit 0, `dist/` 생성)

**4) Commit gate (commit 후, 2 명령):**
- `git log -1 --pretty=%s` = `feat(15-daily-report): SW1 components.css +54 class (W2~W6 sketch verbatim)`
- `git status --short` = 빈 출력 (또는 untracked 만)
</verification>

<success_criteria>
- components.css 끝부분 (line 77 직전 + 직후) 에 §7~§12 (54 신규 class + @keyframes blink) verbatim paste 완료
- 14-reports 기존 §1~§6 (line 1~76) 1 byte 변경 0
- positive gate ≥7 + negative gate ≥10 + build gate 2 PASS
- atomic commit 1건 (`feat(15-daily-report): SW1 components.css +54 class (W2~W6 sketch verbatim)`)
- SW1 자체 시각 변화 0 — 사용처 (DailyReportPage.tsx) 는 SW2/SW3 에서 변환
- W7 OQ default 28건 LOCKED 모두 보존 (본 SW1 은 OQ 0)
- wr+angler 명령 0건 / `npm run d+eploy` 0건 (CLAUDE.local.md + memory `feedback_cbc7119_design_never_wrangler`)
</success_criteria>

<output>
After completion, create `.planning/quick/260521-8mw-redesign-15-daily-report-sw1-components-/260521-8mw-SUMMARY.md` recording:

- 추가 lines (`git diff --stat cha-bio-safety/src/styles/components.css`)
- positive / negative / build gate 출력 (verbatim 박제)
- commit hash + 메시지
- 14-reports §1~§6 diff 0 확인 결과
- SW2/SW3 변환 wave 다음 단계 안내 (DailyReportPage.tsx 모바일 + 데스크톱 영역 class 기반 markup 재작성)
- 메모리 룰 박제 ≥5건 — feedback_planner_prompt_sketch_verbatim / feedback_redesign_sketch_rule_enforcement / feedback_sketch_realistic_data / feedback_tailwind_w8_h8_is_48px / feedback_text_caption_leading_none / feedback_cbc7119_design_never_wrangler / feedback_avoid_premature_confirmation
</output>
