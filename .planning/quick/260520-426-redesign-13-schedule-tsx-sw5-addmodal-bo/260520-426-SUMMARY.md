---
title: SW5 — AddModal (BottomSheet + 5 cat 분기 + CTA grid + linear-gradient 폐기) Summary
phase: quick-260520-426
plan: 01
subsystem: 13-schedule
tags: [tsx-conversion, AddModal, schedule, v0.1.1-tokens, lucide-X, linear-gradient-removal, oan-leveling, lbl-inp-helper]
status: completed
completed_date: 2026-05-20

dependency_graph:
  requires:
    - quick-260520-2fg (SW4 — monthlyPlan TSX)
    - quick-260520-1q0 (SW3 — renderCard TSX, status- prefix 학습 inherit)
  provides:
    - "AddModal v0.1.1 토큰 완료 (옛 alias 0 within scope)"
    - "lbl/inp helper v0.1.1 매핑 (SW6 EditModal 출발점)"
    - "lucide X icon import (SW6 EditModal close X 재사용 가능)"
  affects:
    - "SchedulePage.tsx (single-file edit)"

tech_stack:
  added:
    - "lucide-react X icon (close 버튼 SVG path 4줄 치환)"
  patterns:
    - "v0.1.1 토큰 매핑 (옛 --bg2/--bg3/--bd/--bd2/--t1/--t2/--t3/--acl → v0.1.1)"
    - "옵션 A — lbl/inp helper 유지 + var() alias만 v0.1.1 매핑 (layout 무영향)"
    - "Tailwind className + 인라인 style 하이브리드 (chrome은 className, 동적 hex은 인라인)"

key_files:
  created: []
  modified:
    - "cha-bio-safety/src/pages/SchedulePage.tsx (+46 / -40, net +6 lines)"

decisions:
  - "W4 OQ #1 LOCKED a: maxHeight = 90dvh (source verbatim, sketch 90vh 아님 — iOS Safari 안정성)"
  - "W4 OQ #2 LOCKED b: 저장 버튼 = var(--accent) solid (linear-gradient 폐기 + fontSize 14→16 노안)"
  - "W4 OQ #3 LOCKED a: INSP_CATEGORIES 19종 native <select> verbatim"
  - "옵션 A 채택: lbl/inp helper 유지 + var() alias만 v0.1.1 매핑 (Tailwind className 옵션 B 미채택 — markup 폭증 회피, SW6 inherit 효율)"
  - "Rule 4 회피: close X = w-7 h-7 (tailwind.config spacing override: '7':'32px'). w-8 = 48px 함정 회피"

metrics:
  duration: "약 5분"
  files_changed: 1
  lines_added: 46
  lines_removed: 40
  net_delta: "+6"

commit: ac04793
---

# Phase quick-260520-426: SW5 — AddModal TSX 변환 Summary

**One-liner:** SchedulePage AddModal 전 영역 (line 791~1113) + lbl/inp helper (line 1196~1206) v0.1.1 토큰 매핑, linear-gradient 폐기, close X 32×32 lucide 치환, 노안 격상 6건.

---

## 1. 변경 범위

| 영역 | 위치 (변경 후 line) | 변경 |
|------|---------------------|------|
| lucide-react import | 5 | `X` 추가 |
| AddModal overlay + sheet | 902~918 | Tailwind `fixed inset-0 z-[300] flex flex-col bg-surface-raised overflow-y-auto`, 옛 `var(--bg2)` → 토큰 |
| 헤더 컨테이너 | 921 | `flex items-center justify-between` |
| 헤더 타이틀 | 922 | `text-title font-semibold text-text-primary` (15→18 노안) |
| close X 버튼 | 923~925 | `w-7 h-7 rounded-sm border border-border-default bg-surface-sunken ...` (28→32×32, Rule 4 회피) |
| close X SVG → lucide | 926 | SVG path 4줄 → `<X size={16} className="text-text-secondary" strokeWidth={2} />` |
| 5 cat 버튼 | 935~946 | padding 8→10, fontSize 11→12, lineHeight:1, `var(--bd/bg3/t2)` → `--border-default/surface-sunken/text-secondary` |
| 점검 select 영역 | 949~973 | label/inp helper 유지 (helper 매핑으로 토큰화) |
| 업무/행사 (선택) span | 983 | `var(--t3)` → `var(--text-tertiary)` |
| elevator picker 버튼 | 996~1006 | `var(--bd/bg3/t2)` → `--border-default/surface-sunken/text-secondary` |
| elevator (선택) span | 1013 | `var(--t3)` → `var(--text-tertiary)` |
| fire picker 버튼 | 1026~1035 | **fontSize 10→12 노안 격상** + var alias 토큰화 |
| fire (선택) span | 1043 | `var(--t3)` → `var(--text-tertiary)` |
| 시작/종료일 (선택) span × 3 | 1059, 1068, 1073 | `var(--t3)` → `var(--text-tertiary)` |
| N일 미리보기 color | 1081 | `var(--acl)` → `var(--accent)` |
| ‹/› CTA 버튼 × 2 | 1089~1108 | `var(--bd2/bg3/t2)` → `--border-strong/surface-sunken/text-secondary` |
| 저장 버튼 (CRITICAL) | 1095~1101 | `linear-gradient(135deg,#1d4ed8,#2563eb)` 폐기 → `var(--accent)` solid + fontSize 14→16 |
| lbl helper | 1196~1199 | fontSize 11→12 + `lineHeight:1` 추가 + `--t3` → `--text-tertiary` |
| inp helper | 1200~1204 | `--bg3/bd2/t1` → `--surface-sunken/border-strong/text-primary` |

---

## 2. 노안 격상 6건 (verbatim 박제)

| # | 위치 | 옛 사이즈 | 새 사이즈 | sketch 출처 |
|---|------|---------|---------|------------|
| 1 | 헤더 타이틀 (line 922) | 15px | 18px (text-title) | sketch line 434 |
| 2 | close X 버튼 (line 925) | 28×28 | 32×32 (w-7 h-7) | sketch line 435 — Rule 4 회피 |
| 3 | close X SVG (line 926) | 14×14 | 16×16 (size={16}) | sketch line 436 |
| 4 | 카테고리 grid 버튼 (line 942) | 11px / pad 8 / line-h default | 12px / pad 10 / lineHeight:1 | sketch line 446 |
| 5 | fire picker (line 1029) | 10px | 12px | sketch frame 5b verbatim |
| 6 | 저장 버튼 (line 1098) | 14px | 16px | sketch line 503 |
| 7 | lbl helper (line 1197) | 11px (no lh:1) | 12px + lineHeight:1 | sketch line 443 + Rule 5 |

---

## 3. Verify Gate 결과 (PLAN §7)

| # | Gate | 결과 | 비고 |
|---|------|------|-----|
| §4.1 #1 | AddModal close X SVG path 4줄 0 | **PASS** | AddModal scope 0건 (EditModal 잔존 1건 = SW6 스코프) |
| §4.1 #3 | status- prefix 잘못된 패턴 0 | **PASS** | 0건 (SW5 = status 토큰 미사용) |
| §4.1 #4 | AddModal linear-gradient 0 | **PASS** | AddModal scope 0건 (EditModal 잔존 1건 = SW6 스코프) |
| §4.1 #5 | AddModal+helper 영역 옛 alias 0 | **PASS** | 0건 (--bg2/bg3/bd/bd2/t1/t2/t3/acl/safe/info/danger/warn) |
| §4.1 #8 | w-7 h-7 (close X 32×32) Rule 4 회피 | **PASS** | 1건 (close X 버튼) |
| §4.2 #9 | surface-raised + surface-sunken 토큰 | **PASS** | 8건 |
| §4.2 #10 | text-* / var(--text-*) 토큰 | **PASS** | 15건 |
| §4.2 #11 | border-* / var(--border-*) 토큰 | **PASS** | 7건 |
| §4.2 #12 | bg-accent / var(--accent) 토큰 | **PASS** | 2건 (저장 + N일 미리보기) |
| §4.2 #17 | lucide `<X size={16}` | **PASS** | 1건 |
| 추가 | lucide-react import에 X 포함 | **PASS** | `ChevronLeft, ChevronRight, Download, Plus, X` |
| 추가 | text-title 도입 | **PASS** | 1건 (헤더 타이틀) |
| 추가 | 저장 버튼 fontSize:16 | **PASS** | 1건 |
| 추가 | fire picker fontSize:10 폐기 | **PASS** | 0건 |
| 추가 | lbl helper fontSize:12 | **PASS** | 1건 |
| 추가 | lbl helper fontSize:11 폐기 | **PASS** | 0건 |
| 추가 | lbl helper lineHeight:1 추가 | **PASS** | 1건 |
| §7 #19 | tsc --noEmit exit 0 | **PASS** | no output |
| §7 #20 | npm run build exit 0 | **PASS** | ✓ built in 14.24s + sw.js 정상 생성 |

**NEGATIVE 보존 검증:**

| # | 보존 항목 | grep count | 결과 |
|---|---------|-----------|------|
| 1 | toast.error('날짜를 입력하세요') | 1 | **PASS** |
| 2 | toast.error('점검 분류를 선택하세요') | 1 | **PASS** |
| 3 | toast.error('제목을 입력하세요') | 2 (AddModal + EditModal 공유) | **PASS** |
| 4 | toast.error('저장 실패') | 1 | **PASS** |
| 5 | scheduleApi.create | 1 | **PASS** |
| 6 | onSaved() | 2 (AddModal + EditModal) | **PASS** |

---

## 4. W4 OQ LOCKED 결정 3건 적용 확인

| OQ | 결정 | 적용 위치 | 확인 |
|----|-----|---------|-----|
| #1 LOCKED **a** | mobile sheet `maxHeight = 90dvh` | line 916 | `maxHeight: '90dvh'` verbatim (90vh 아님) |
| #2 LOCKED **b** | 저장 = `var(--accent)` solid + 14→16 | line 1095~1101 | `background:'var(--accent)'` + `fontSize:16` |
| #3 LOCKED **a** | INSP_CATEGORIES 19종 native `<select>` | line 953~957 | verbatim 유지 |

---

## 5. §10 인라인 화이트리스트 4건 잔존 확인

| # | 인라인 코드 | 위치 | 잔존 이유 |
|---|-----------|------|---------|
| 1 | `background: \`${c.color}22\`` | line 940 | JS 변수 동적 hex (5 cat × alpha 22) 정적 Tailwind 불가 |
| 2 | `background:'rgba(249,115,22,0.15)'` | line 1001 | elevator picker alpha 0.15 보정 |
| 3 | `background:'rgba(239,68,68,0.13)'` | line 1031 | fire picker alpha 0.13 보정 |
| 4 | `background:'rgba(0,0,0,0.55)'` | line 907 | overlay — sketch W4 line 334 verbatim |

→ 잔존 OK (§10 화이트리스트 verbatim)

---

## 6. NEGATIVE §5 12건 보존 확인

| # | 항목 | 보존? |
|---|-----|------|
| 1 | handleSave 전체 로직 (5 cat 분기 + hasRange 룰) | ✅ |
| 2 | scheduleApi.create 시그니처 (title/date/time/category/assigneeId/inspectionCategory/memo/end_date) | ✅ |
| 3 | toast `'날짜를 입력하세요'` | ✅ |
| 4 | toast `'점검 분류를 선택하세요'` | ✅ |
| 5 | toast `'제목을 입력하세요'` | ✅ |
| 6 | toast `'저장 실패'` | ✅ |
| 7 | onSaved() 호출 | ✅ |
| 8 | INSP_CATEGORIES / INSP_DEFAULTS / ELEV_* / FIRE_* 데이터 (line 36~80) | ✅ |
| 9 | handleCat / handleInsCat / handleElevSub / handleFireSub 로직 | ✅ |
| 10 | shiftDate 로직 | ✅ |
| 11 | rangeDays / workingDays / skippedCount 계산 로직 | ✅ |
| 12 | N일 미리보기 텍스트 verbatim | ✅ |

---

## 7. 사용자 검수 흐름 reminder

**브랜치:** `redesign/13-schedule` (commit ac04793)

**확인 포인트:**
- **모바일 393px:** AddModal 열기 → BottomSheet (rounded-t 20px, maxHeight 90dvh)
  - 헤더 "일정 추가" 18px + close X 32×32 (lucide X 16px)
  - 5 cat 버튼 (점검/업무/행사/승강기/소방) 활성 시 hex@22 배경 / 비활성 시 surface-sunken
  - 카테고리별 분기: inspect → native `<select>` (19종) / task·event → 제목+내용 / elevator → 3 sub-picker + agency / fire → 4 sub-picker 12px + agency
  - 시작일/종료일 + 시간 (각 (선택) 라벨 tertiary)
  - 종료일 < 시작일 X (min=date)
  - **N일 범위 시:** "${rangeDays}일 범위로 1건 추가됩니다 (주말·공휴일 ${skipped}일은 표시에서 제외)" 노출
  - CTA grid: ‹ (border-strong) / 저장 (accent solid 18px or 16px) / › (border-strong)
- **데스크톱 1920×1080:** width 480px 중앙, rounded 16px
- **라이트/다크 토글:** 저장 버튼 accent 솔리드 양쪽 일관 (linear-gradient 폐기 검증)
- **저장 동작:** category 별 finalTitle/finalInsCat 정확히 들어가는지 (handleSave 로직 보존)

**다음 단계 (사용자 컨펌 후):**
1. `git checkout main && git merge redesign/13-schedule --no-ff`
2. `git push origin main` → cbc7119-preview 자동 배포

---

## 8. SW6 (EditModal) 출발점

**SW5 가 SW6 에 제공:**
1. lbl/inp helper 가 v0.1.1 매핑 완료 — EditModal label/input 도 자동으로 v0.1.1 적용됨
2. lucide-react import 의 `X` 가 도입됨 — EditModal close X 도 동일 패턴 재사용 가능
3. W4 OQ #1 (90dvh) / #2 (accent solid) 패턴 박제 — EditModal 도 동일 적용

**SW6 잔여 작업 (out-of-scope here):**
- EditModal overlay + sheet container `var(--bg2)` → `bg-surface-raised`
- EditModal 헤더 타이틀 "일정 수정" 15→18 노안
- EditModal close X 버튼 28→32 (w-7 h-7) + SVG path 4줄 → `<X size={16} />` (line 1149~1153)
- EditModal `var(--t1/bd/bg3/t3)` → v0.1.1
- EditModal 저장 버튼 `linear-gradient` 폐기 → `var(--accent)` + 14→16 (line 1180~1186)

---

## 9. Deviations from Plan

**None — plan executed exactly as written.**

§10 화이트리스트 #4 (overlay rgba 0.55) 의 경우 PLAN.md 가 "`var(--surface-overlay)` 으로 치환 가능 (executor 판단)" 으로 두었으나 sketch W4 line 334 verbatim 유지를 우선시하여 인라인 잔존을 선택. 이는 화이트리스트 안 옵션 — deviation 아님.

`‹` / `›` punctuation 의 경우 PLAN.md 가 lucide ChevronLeft/Right 치환 권장 (executor 판단) 으로 두었으나 source 와 sketch 모두 punctuation 사용 — 보존이 W4 OQ #2 LOCKED a (sketch verbatim) 일관성에 더 부합하여 punctuation 유지. 이도 옵션 — deviation 아님.

---

## 10. Commit

| 항목 | 값 |
|------|-----|
| Hash | ac04793 |
| Message | `tsx(13-schedule): SW5 — AddModal (BottomSheet + 5 cat 분기 + CTA grid + linear-gradient 폐기)` |
| Files | cha-bio-safety/src/pages/SchedulePage.tsx |
| Delta | +46 / -40 (net +6) |
| Branch | redesign/13-schedule |

---

## Self-Check: PASSED

- File modified: `cha-bio-safety/src/pages/SchedulePage.tsx` — FOUND (verified via `git diff --stat`)
- Commit hash `ac04793` — FOUND in `git log -1 --oneline`
- AddModal scope: linear-gradient 0 / SVG path 4줄 0 / 옛 alias 0 — verified
- tsc --noEmit exit 0 — verified
- npm run build exit 0 — verified (✓ built in 14.24s)
- W4 OQ #1/#2/#3 LOCKED 보존 — verified
- §10 화이트리스트 4건 잔존 — verified
- NEGATIVE §5 12건 보존 — verified
