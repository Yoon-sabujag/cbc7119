---
title: SW5 — AddModal (BottomSheet + 5 cat 분기 + CTA grid + linear-gradient 폐기)
phase_id: quick-260520-426
phase: quick-260520-426
plan: 01
type: execute
wave: 1
depends_on: [quick-260520-2fg]
files_modified:
  - cha-bio-safety/src/pages/SchedulePage.tsx
autonomous: true
requirements: [SW5-W7-§3-region.add-modal, SW5-W7-§6-sub-task-5, SW5-W4-OQ1-LOCKED-a, SW5-W4-OQ2-LOCKED-b, SW5-W4-OQ3-LOCKED-a]
status: ready

must_haves:
  truths:
    - "AddModal 전 영역 (line 791~1108) 의 var() alias 가 v0.1.1 으로 매핑되어 옛 alias (--bg2/bg3/bd/bd2/t1/t2/t3/acl/safe/info/danger/warn) 가 AddModal 안 0회 등장한다"
    - "저장 버튼 (line 1090~1096) 의 linear-gradient(135deg,#1d4ed8,#2563eb) 가 폐기되고 bg-accent solid 토큰으로 치환되며 노안 격상 (fontSize 14→16) 이 반영된다"
    - "close X 버튼 (line 917~922) 이 정확히 32×32 (sketch line 435 verbatim) Tailwind 사이즈로 표현되며 lucide <X size={16} /> 로 SVG path 4 줄이 치환된다"
    - "헤더 타이틀 (line 916) 의 fontSize 가 15→18 노안 격상되어 text-title font-semibold 토큰으로 치환된다"
    - "fire 분류 picker 의 fontSize 10 (line 1024) 이 12 (text-caption) 로 노안 격상된다"
    - "handleSave / scheduleApi.create 시그니처 / toast 카피 4건 / 5 cat 분기 로직 / INSP_CATEGORIES 데이터 / handleCat 등 로직 1 byte 도 변경되지 않는다"
    - "tsc --noEmit + npm run build 모두 exit 0 으로 통과한다"
  artifacts:
    - path: "cha-bio-safety/src/pages/SchedulePage.tsx"
      provides: "AddModal TSX 변환 + lbl/inp helper var() alias v0.1.1 매핑 + lucide X import 추가"
      contains: "function AddModal"
  key_links:
    - from: "AddModal close 버튼"
      to: "lucide-react X icon"
      via: "import + JSX"
      pattern: "<X size=\\{16\\}"
    - from: "AddModal 저장 버튼"
      to: "v0.1.1 accent 토큰"
      via: "Tailwind className"
      pattern: "bg-accent"
    - from: "AddModal close 버튼 사이즈"
      to: "sketch line 435 32×32 verbatim"
      via: "Tailwind spacing override 회피"
      pattern: "w-7 h-7|w-\\[32px\\] h-\\[32px\\]"
---

<objective>
SW5: SchedulePage 의 AddModal (line 791~1108) + lbl/inp helper (line 1191~1198) TSX 변환.

**Goal:** AddModal 의 옛 alias (--bg2/bg3/bd/bd2/t1/t2/t3/acl 등) → v0.1.1 토큰 매핑 + linear-gradient 폐기 + close X 32×32 정확 표현 + lucide X 도입 + 노안 격상 (헤더 15→18, 저장 14→16, close 28→32, fire 10→12). W4 OQ #1/#2/#3 LOCKED 결정 verbatim 보존. handleSave 로직 + scheduleApi.create 시그니처 + toast 카피 4건 1 byte 도 변경 X.

**Purpose:** 13-schedule redesign TSX wave 의 SW5 — AddModal chrome/카테고리/inspect/날짜/CTA 전 영역 토큰화. SW6 (EditModal) 이전 lbl/inp helper 도 var() alias 만 v0.1.1 매핑 (layout 보존, EditModal 영향 차단).

**Output:** SchedulePage.tsx 단일 파일 +30~50 lines (linear-gradient 폐기 + lucide X 도입 + className 확장 + 인라인 alias 토큰화). tsc + build exit 0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@cha-bio-safety/docs/redesign-context/13-schedule/wave-7-tsx-conversion-checklist.md
@cha-bio-safety/docs/redesign-context/13-schedule/sketch-wave-4.html
@cha-bio-safety/docs/redesign-context/13-schedule/tokens.css
@cha-bio-safety/docs/redesign-context/13-schedule/typography.css
@cha-bio-safety/src/pages/SchedulePage.tsx
@cha-bio-safety/tailwind.config.js
@.planning/quick/260520-2fg-redesign-13-schedule-tsx-sw4-monthlyplan/260520-2fg-SUMMARY.md
@.planning/quick/260520-1q0-redesign-13-schedule-tsx-sw3-rendercard-/260520-1q0-SUMMARY.md
</context>

---

## 1. Source-of-truth 인용 (W7 §3 region.add-modal / §6 sub-task 5 / §8 W4 OQ verbatim)

### W7 §8 W4 OQ LOCKED 결정 3건 (verbatim 박제)

| OQ | 결정 | 의미 |
|----|-----|------|
| #1 LOCKED **a** | mobile sheet `maxHeight = 90dvh` | source line 910 의 `maxHeight:'90dvh'` 그대로 유지 (sketch 의 `90vh` 가 아닌 `90dvh` — iOS Safari 안 100% 동작 보장) |
| #2 LOCKED **b** | 저장 버튼 = `bg-accent` solid + 노안 14→16 | source `linear-gradient(135deg,#1d4ed8,#2563eb)` 폐기. `var(--accent)` 토큰 일관 |
| #3 LOCKED **a** | INSP_CATEGORIES 19종 입력 = native `<select>` | source line 949~952 verbatim 유지. iOS PWA 키보드 자동 popup 활용 |

### W7 §10 인라인 화이트리스트 4건 (잔존 OK)

| # | 인라인 코드 | 위치 (현재 line) | 이유 |
|---|-----------|----------------|-----|
| 1 | `background: ${c.color}22` | line 935 | JS 변수 동적 hex (5 cat × alpha 22) 정적 Tailwind 불가 |
| 2 | `background: 'rgba(249,115,22,0.15)'` | line 996 | elevator picker alpha 0.15 보정 (Tailwind 정적 미지원) |
| 3 | `background: 'rgba(239,68,68,0.13)'` | line 1026 | fire picker alpha 0.13 보정 |
| 4 | `background:'rgba(0,0,0,0.55)'` | line 903 | overlay — sketch W4 line 334 verbatim. `var(--surface-overlay)` 으로 치환 가능 (executor 판단) |

---

## 2. 변경 위치 (현재 line 기준)

| 영역 | 현재 line | 변경 내용 |
|------|---------|---------|
| `import` 줄 | line 5 | `lucide-react` import 에 `X` 추가 (현재 `ChevronLeft, ChevronRight, Download, Plus`) |
| overlay | 903 | `bg-overlay` (`var(--surface-overlay)`) 또는 `bg-[rgba(0,0,0,0.55)]` arbitrary, `z-[300]` |
| sheet container | 906~912 | `bg-surface-raised` + chrome rounded/padding 토큰화 |
| 헤더 컨테이너 | 915 | `flex items-center justify-between mb-4` |
| 헤더 타이틀 | 916 | `text-title font-semibold text-text-primary` (15→18) |
| close X 버튼 | 917~918 | `w-7 h-7 rounded-sm border border-border-default bg-surface-sunken ...` (32×32 — w-8 함정 회피) |
| close X SVG | 919~921 | lucide `<X size={16} className="text-text-secondary" strokeWidth={2} />` |
| 본문 컨테이너 | 925 | `flex flex-col gap-4` |
| 5 cat grid label | 929 | `lbl` 헬퍼 사용 유지 (var alias 만 v0.1.1) |
| 5 cat grid | 930 | `grid grid-cols-5 gap-[5px]` |
| 5 cat 버튼 active/inactive | 932~939 | `var(--bd)` → `var(--border-default)` / `var(--bg3)` → `var(--surface-sunken)` / `var(--t2)` → `var(--text-secondary)` |
| inspect select | 949~952 | `inp` 헬퍼 사용 유지 (verbatim) |
| inspect 제목/내용 | 956~963 | `inp` 헬퍼 사용 유지 |
| 업무/행사 | 971~983 | `inp` 헬퍼 + `var(--t3)` → `var(--text-tertiary)` |
| elevator picker | 988~1014 | `var(--bd)` → `--border-default` / `var(--bg3)` → `--surface-sunken` / `var(--t2)` → `--text-secondary` / `var(--t3)` → `--text-tertiary` |
| fire picker | 1019~1043 | 위와 동일 + **fontSize 10→12 노안 격상** (line 1024) |
| 시작일/시간 | 1047~1058 | flex layout 유지 + `inp` 사용 + `var(--t3)` → `--text-tertiary` |
| 종료일/시간 | 1061~1072 | 동일 |
| N일 미리보기 | 1075~1081 | `text-caption text-accent font-semibold text-center -mt-2` 또는 인라인 토큰만 매핑 |
| CTA grid | 1083 | `grid grid-cols-[1fr_3fr_1fr] gap-1.5 mt-1` |
| ‹/› 버튼 | 1084~1089, 1097~1102 | `var(--bd2)` → `--border-strong` / `var(--bg3)` → `--surface-sunken` / `var(--t2)` → `--text-secondary` + (executor 판단) lucide ChevronLeft/ChevronRight 치환 권장 |
| 저장 버튼 (CRITICAL) | 1090~1096 | `linear-gradient(...)` **폐기** → `bg-accent` solid + `text-text-on-accent` (또는 `text-white`) + `text-body font-bold` (14→16 노안) |
| lbl helper | 1191~1193 | `fontSize:11→12` + `--t3` → `--text-tertiary` + `lineHeight:1` 추가 |
| inp helper | 1194~1198 | `--bg3` → `--surface-sunken` / `--bd2` → `--border-strong` / `--t1` → `--text-primary` |

---

## 3. Sketch W4 verbatim 인용 (sketch-wave-4.html line 330~510)

### 3.1 모달 chrome (line 330~368)

```html
/* 모바일 overlay */
.modal-overlay-mobile {
  position: absolute; inset: 0;
  background: rgba(0, 0, 0, 0.55);
  display: flex; justify-content: center; align-items: flex-end;
  z-index: 300;
}
/* 모바일 sheet */
.modal-sheet-mobile {
  width: 100%;
  background: var(--surface-raised);
  border-radius: 20px 20px 0 0;
  padding: 20px 16px 40px;
  max-height: 90vh;        /* ← source 는 90dvh (LOCKED #1 a) */
  overflow-y: auto;
  box-shadow: 0 -12px 32px rgba(0, 0, 0, 0.48);
  box-sizing: border-box;
}
/* 데스크톱 sheet */
.modal-sheet-desktop {
  width: 480px; max-width: 90vw;
  background: var(--surface-raised);
  border-radius: 16px;
  padding: 24px 28px 28px;
  max-height: 90vh;
  overflow-y: auto;
  box-shadow: 0 24px 64px rgba(0, 0, 0, 0.48);
  box-sizing: border-box;
}
```

### 3.2 헤더 (line 433~437)

```html
<div style="display:flex; align-items:center; justify-content:space-between; margin-bottom:18px;">
  <span class="text-title font-semibold" style="color:var(--text-primary);">일정 추가</span>
  <button style="width:32px; height:32px; border-radius:8px; border:1px solid var(--border-default); background:var(--surface-sunken); cursor:pointer; display:flex; align-items:center; justify-content:center;" aria-label="닫기">
    <svg width="16" height="16" fill="none" viewBox="0 0 24 24" stroke="var(--text-secondary)" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M6 18L18 6M6 6l12 12"/></svg>
  </button>
</div>
```

### 3.3 카테고리 5 grid (line 442~451)

```html
<div>
  <label class="text-caption" style="font-weight:700; color:var(--text-tertiary); display:block; margin-bottom:6px; line-height:1;">구분</label>
  <div style="display:grid; grid-template-columns:repeat(5,1fr); gap:5px;">
    <button style="padding:10px 0; border-radius:8px; border:1px solid #3b82f6; background:#3b82f622; color:#3b82f6; font-size:12px; font-weight:700; line-height:1; cursor:pointer;">점검</button>
    <!-- inactive: border-color: var(--border-default), background: var(--surface-sunken), color: var(--text-secondary) -->
  </div>
</div>
```

### 3.4 inspect select (line 453~477)

```html
<label class="text-caption" style="font-weight:700; color:var(--text-tertiary); display:block; margin-bottom:6px; line-height:1;">점검 분류</label>
<select style="width:100%; padding:11px 12px; border-radius:10px; box-sizing:border-box; background:var(--surface-sunken); border:1px solid var(--border-strong); color:var(--text-primary); font-size:13px; outline:none; font-family:inherit;">
  <option value="">-- 선택하세요 --</option>
  <!-- INSP_CATEGORIES.map(c => <option key={c} value={c}>{c}</option>) verbatim -->
</select>
```

### 3.5 시작/종료일+시간 (line 478~499)

```html
<div style="display:flex; gap:10px;">
  <div style="flex:0 0 calc(50% - 5px); min-width:0; overflow:hidden;">
    <label class="text-caption" style="font-weight:700; color:var(--text-tertiary); display:block; margin-bottom:6px; line-height:1;">시작일</label>
    <input type="date" style="width:100%; padding:11px 12px; border-radius:10px; box-sizing:border-box; background:var(--surface-sunken); border:1px solid var(--border-strong); color:var(--text-primary); font-size:13px; outline:none; display:block; height:44px;" />
  </div>
  <!-- (선택): <span style="font-weight:400; color:var(--text-tertiary);">(선택)</span> -->
</div>
```

### 3.6 CTA grid (line 500~505)

```html
<div style="display:grid; grid-template-columns:1fr 3fr 1fr; gap:6px; margin-top:4px;">
  <button style="padding:14px 0; border-radius:12px; border:1px solid var(--border-strong); background:var(--surface-sunken); color:var(--text-secondary); font-size:18px; line-height:1; cursor:pointer; display:flex; align-items:center; justify-content:center;">‹</button>
  <button style="padding:14px; border-radius:12px; border:none; background:var(--accent); color:#fff; font-size:16px; font-weight:700; cursor:pointer;">저장</button>
  <button style="padding:14px 0; border-radius:12px; border:1px solid var(--border-strong); background:var(--surface-sunken); color:var(--text-secondary); font-size:18px; line-height:1; cursor:pointer; display:flex; align-items:center; justify-content:center;">›</button>
</div>
```

---

## 4. 변환 매핑 표

### 4.1 옛 alias → v0.1.1 (AddModal 영역 + lbl/inp helper 전부)

| 옛 alias | v0.1.1 | Tailwind 토큰 (대안) |
|---------|--------|------------------|
| `var(--bg2)` | `var(--surface-raised)` | `bg-surface-raised` |
| `var(--bg3)` | `var(--surface-sunken)` | `bg-surface-sunken` |
| `var(--bd)` | `var(--border-default)` | `border-border-default` |
| `var(--bd2)` | `var(--border-strong)` | `border-border-strong` |
| `var(--t1)` | `var(--text-primary)` | `text-text-primary` |
| `var(--t2)` | `var(--text-secondary)` | `text-text-secondary` |
| `var(--t3)` | `var(--text-tertiary)` | `text-text-tertiary` |
| `var(--acl)` | `var(--accent)` | `text-accent` / `bg-accent` |

### 4.2 노안 격상 라인

| 위치 | 옛 사이즈 | 새 사이즈 | 비고 |
|------|---------|---------|------|
| 헤더 타이틀 (line 916) | 15px | 18px (`text-title`) | sketch line 434 |
| close X 버튼 (line 918) | 28×28 | 32×32 (`w-7 h-7`) | sketch line 435 — w-8 함정 회피 |
| close X SVG (line 919) | 14×14 | 16×16 (`size={16}`) | sketch line 436 |
| lbl helper fontSize (line 1192) | 11px | 12px (`text-caption`) | sketch line 443 |
| fire picker fontSize (line 1024) | 10px | 12px (`text-caption`) | sketch frame 5b (line 1067 의 `font-size:12px`) |
| 저장 버튼 fontSize (line 1093) | 14px | 16px (`text-body`) | sketch line 503 |

### 4.3 lbl/inp 헬퍼 매핑 (옵션 A 채택 — LOCKED)

```ts
// AS-IS (line 1191~1198)
const lbl: React.CSSProperties = {
  fontSize:11, fontWeight:700, color:'var(--t3)', display:'block', marginBottom:6,
}
const inp: React.CSSProperties = {
  width:'100%', padding:'11px 12px', borderRadius:10, boxSizing:'border-box',
  background:'var(--bg3)', border:'1px solid var(--bd2)',
  color:'var(--t1)', fontSize:13, outline:'none', fontFamily:'inherit',
}

// TO-BE (var() alias v0.1.1 매핑 + lbl 11→12 노안 격상 + lineHeight:1 추가)
const lbl: React.CSSProperties = {
  fontSize:12, fontWeight:700, color:'var(--text-tertiary)',
  display:'block', marginBottom:6, lineHeight:1,
}
const inp: React.CSSProperties = {
  width:'100%', padding:'11px 12px', borderRadius:10, boxSizing:'border-box',
  background:'var(--surface-sunken)', border:'1px solid var(--border-strong)',
  color:'var(--text-primary)', fontSize:13, outline:'none', fontFamily:'inherit',
}
```

**옵션 A 채택 이유:** AddModal 안 lbl 11회 / inp 13회 사용. Tailwind className 으로 치환하면 markup 크기 폭증. EditModal 도 lbl/inp 공유 — 옵션 B 는 SW6 까지 함수 시그니처 보존 효율 낮음. SW6 후 EditModal 완료 시 헬퍼 삭제 검토.

---

## 5. NEGATIVE 변경 금지 (W7 §2 verbatim)

다음 항목은 1 byte 도 변경 금지:

| # | 위치 (line) | 항목 |
|---|----------|-----|
| 1 | 856~900 | `handleSave` 전체 로직 (5 cat 분기 + finalTitle/finalInsCat 룰 + `hasRange ? { end_date: endDate } : {}`) |
| 2 | 884~893 | `scheduleApi.create` 시그니처 (title/date/time/category/assigneeId/inspectionCategory/memo/end_date) |
| 3 | 857 | toast `'날짜를 입력하세요'` |
| 4 | 863 | toast `'점검 분류를 선택하세요'` |
| 5 | 873 | toast `'제목을 입력하세요'` |
| 6 | 896 | toast `'저장 실패'` |
| 7 | 894 | `onSaved()` 호출 |
| 8 | 36~78 | `INSP_CATEGORIES` / `INSP_DEFAULTS` / `ELEV_SUBCATS` / `ELEV_AGENCY` / `FIRE_SUBCATS` / `FIRE_AGENCY` 데이터 |
| 9 | 800~822 | `handleCat` / `handleInsCat` / `handleElevSub` / `handleFireSub` 로직 |
| 10 | 824~830 | `shiftDate` 로직 |
| 11 | 832~854 | `rangeDays` / `workingDays` / `skippedCount` 계산 로직 |
| 12 | 1075~1081 | N일 미리보기 텍스트 verbatim (`${rangeDays}일 범위로 1건 추가됩니다 ...`) |

---

## 6. 메모리 룰 5건 inline 인용

### Rule 1 — `feedback_tailwind_token_class_pattern.md`

status- prefix 없음. AddModal 은 `text-text-*` / `bg-surface-*` / `border-border-*` / `bg-accent` / `text-accent` 만 사용 (status 토큰 미사용). SW3 정정 학습 inherit (Rule 1 에서 잘못된 `text-status-fire-bar` 사용 → 정정 패턴).

### Rule 2 — `feedback_tsx_wave_emoji_dot_gap.md`

이모지 0 강제. close X SVG path 4 줄 → lucide `<X />` 치환. `‹` (U+2039) / `›` (U+203A) 는 punctuation (이모지 0 grep `[\x{1F300}-\x{1FAFF}]` 범위 밖) — 잔존 OK 또는 lucide `<ChevronLeft />` / `<ChevronRight />` 치환 권장 (W7 OQ #2 LOCKED a 일관성). **executor 판단**.

### Rule 3 — `feedback_planner_prompt_sketch_verbatim.md`

sketch W4 CSS 정의 verbatim 인용 (본 plan §3 + §4 매핑 표 그대로 인용). 추측한 토큰명 / 사이즈 사용 금지 — 03-qr-scan 6건 deviation 사고 reference.

### Rule 4 — `feedback_tailwind_w8_h8_is_48px.md` (CRITICAL)

tailwind.config.js spacing override:
- `'7': '32px'` (Tailwind 기본 28px 가 아닌 32px)
- `'8': '48px'` (Tailwind 기본 32px 가 아닌 48px)

**32×32 close X 는 `w-7 h-7` 또는 arbitrary `w-[32px] h-[32px]` 사용.** sketch line 435 `width:32px height:32px` 를 그대로 `w-8 h-8` 변환하면 48px 가 되어 1.5배 사고 (11-div 백버튼 54a1c8d hotfix reference).

### Rule 5 — `feedback_text_caption_leading_none.md`

label `text-caption` (12px / lh:1.5 = 18px) 가 작은 컨테이너 안에서 시각 패딩 생성. sketch line 443 `line-height:1` verbatim → `leading-none` 명시. lbl helper 에도 `lineHeight:1` 추가 (§4.3 표 참조).

---

## 7. Build 검증

```bash
cd /Users/jykevin/Documents/cbc7119-design/cha-bio-safety
npx tsc --noEmit            # exit 0
npm run build               # exit 0
```

**워크트리 룰 (CLAUDE.local.md):**
- wrangler 명령 X / `npm run deploy` X
- 운영 PWA hotfix X
- 작업 위치: `/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/src/pages/SchedulePage.tsx`
- 브랜치: `redesign/13-schedule` (현재 — 확인됨)
- main 머지 / push 사용자 컨펌 후

---

<tasks>

<task type="auto">
  <name>Task 1: AddModal 전 영역 TSX 변환 + lbl/inp 헬퍼 매핑</name>
  <files>cha-bio-safety/src/pages/SchedulePage.tsx</files>
  <action>
SchedulePage.tsx 의 AddModal (line 791~1108) + lbl/inp 헬퍼 (line 1191~1198) 전 영역을 v0.1.1 토큰으로 변환한다.

**Step 1 — lucide-react import 확장 (line 5):**
```ts
import { ChevronLeft, ChevronRight, Download, Plus, X } from 'lucide-react'
```
`X` 를 추가 (close 버튼 SVG path 치환용).

**Step 2 — overlay + sheet container (line 902~912):**
- overlay `style={{ position:'fixed', inset:0, background:'rgba(0,0,0,0.55)', zIndex:300, ... }}` → Tailwind: `className="fixed inset-0 z-[300] flex flex-col"` + isDesktop 분기 `justify-end` / `justify-center items-center` + `bg-overlay` (또는 `bg-[rgba(0,0,0,0.55)]` 잔존). overlay 토큰은 §10 화이트리스트 — 인라인 잔존 OK.
- sheet container `background:'var(--bg2)'` → `bg-surface-raised`. 나머지 maxHeight/padding/rounded 는 isDesktop 분기 인라인 유지 가능 OR Tailwind 분기:
  - mobile: `rounded-t-[20px] px-4 pt-5 pb-10 max-h-[90dvh] overflow-y-auto` (LOCKED #1 a — 90dvh 정확)
  - desktop: `rounded-2xl w-[480px] max-w-[90vw] px-7 pt-6 pb-7 max-h-[90vh] overflow-y-auto`

**Step 3 — 헤더 (line 914~923):**
- 컨테이너: `<div className="flex items-center justify-between mb-[18px]">` (sketch margin-bottom:18px)
- 타이틀: `<span className="text-title font-semibold text-text-primary">일정 추가</span>` (15→18 격상)
- close X 버튼 (sketch line 435 verbatim, **CRITICAL 32×32 — Rule 4**):
  ```tsx
  <button onClick={onClose}
    className="w-7 h-7 rounded-sm border border-border-default bg-surface-sunken cursor-pointer flex items-center justify-center"
    aria-label="닫기">
    <X size={16} className="text-text-secondary" strokeWidth={2} />
  </button>
  ```
  - `w-7 h-7` = 32×32 (Tailwind config override, NOT `w-8 h-8` = 48px)
  - `rounded-sm` = 8px (Tailwind config 확인 — design tokens 매핑 따름)
  - 기존 SVG path 4 줄 (line 919~921) 전부 lucide `<X />` 1 줄로 치환

**Step 4 — 본문 컨테이너 (line 925):** `<div className="flex flex-col gap-4">` (gap-4 = 16px)

**Step 5 — 5 cat grid (line 927~942):**
- label: `style={lbl}` 유지 (helper 가 §4.3 매핑으로 토큰화됨)
- grid: `<div className="grid grid-cols-5 gap-[5px]">` (sketch gap:5px arbitrary)
- 버튼 인라인 (§10 카테고리 동적 hex `${c.color}22` 잔존 OK):
  ```tsx
  <button key={c.value} onClick={() => handleCat(c.value)}
    style={{
      padding:'10px 0', borderRadius:8,
      border:`1px solid ${cat===c.value ? c.color : 'var(--border-default)'}`,
      background: cat===c.value ? `${c.color}22` : 'var(--surface-sunken)',
      color: cat===c.value ? c.color : 'var(--text-secondary)',
      fontSize:12, fontWeight:700, lineHeight:1, cursor:'pointer',
    }}>
    {c.label}
  </button>
  ```
  - 노안 격상: `padding:'8px 0'` → `'10px 0'` (sketch verbatim), `fontSize:11` → `12`
  - `var(--bd)` → `var(--border-default)` / `var(--bg3)` → `var(--surface-sunken)` / `var(--t2)` → `var(--text-secondary)`
  - `lineHeight:1` 추가 (Rule 5)

**Step 6 — inspect select (line 944~968):**
- 외곽 분기 `{cat === 'inspect' && (...)}` 유지
- label `style={lbl}` 유지
- `<select>` `style={inp}` 유지 (verbatim — LOCKED #3 a)
- `INSP_CATEGORIES.map(c => <option key={c} value={c}>{c}</option>)` verbatim 유지
- 제목 input + 내용 textarea `style={inp}` 유지

**Step 7 — 업무/행사 (line 970~984):**
- `(선택)` span: `style={{ fontWeight:400, color:'var(--text-tertiary)' }}` (`var(--t3)` → `var(--text-tertiary)`)
- 나머지 `style={inp}` 유지

**Step 8 — elevator picker (line 986~1014):**
- grid: `display:'grid', gridTemplateColumns:'repeat(3,1fr)', gap:6` 인라인 유지 OR `className="grid grid-cols-3 gap-1.5"`
- 버튼 인라인 (§10 elevator rgba 0.15 잔존 OK):
  ```tsx
  style={{
    padding:'9px 0', borderRadius:9, fontSize:12, fontWeight:700, cursor:'pointer',
    border:`1px solid ${elevSub===v ? '#f97316' : 'var(--border-default)'}`,
    background: elevSub===v ? 'rgba(249,115,22,0.15)' : 'var(--surface-sunken)',
    color: elevSub===v ? '#f97316' : 'var(--text-secondary)',
  }}
  ```
  - `var(--bd)` → `var(--border-default)` / `var(--bg3)` → `var(--surface-sunken)` / `var(--t2)` → `var(--text-secondary)`
- 업체 input + (선택) span 토큰화

**Step 9 — fire picker (line 1016~1044) — fontSize 10→12 노안 격상:**
- 버튼 인라인 (§10 fire rgba 0.13 잔존 OK):
  ```tsx
  style={{
    padding:'8px 4px', borderRadius:9, fontSize:12, fontWeight:700, cursor:'pointer', lineHeight:1.4,
    border:`1px solid ${fireSub===v ? '#ef4444' : 'var(--border-default)'}`,
    background: fireSub===v ? 'rgba(239,68,68,0.13)' : 'var(--surface-sunken)',
    color: fireSub===v ? '#ef4444' : 'var(--text-secondary)',
  }}
  ```
  - **CRITICAL: `fontSize:10` → `12`** (노안 격상, sketch frame 5b verbatim)

**Step 10 — 시작일/시간 + 종료일/시간 (line 1046~1072):**
- 외곽 `style={{ display:'flex', gap:10 }}` 유지 OR `className="flex gap-2.5"`
- 각 column `style={{ flex:'0 0 calc(50% - 5px)', minWidth:0, overflow:'hidden' }}` 유지
- label `style={lbl}` + (선택) span 토큰화 (`var(--t3)` → `var(--text-tertiary)`)
- input `style={{ ...inp, display:'block', WebkitAppearance:'none', height:44 }}` 유지

**Step 11 — N일 미리보기 (line 1074~1081):**
- 인라인 alias 만 토큰화:
  ```tsx
  <div style={{ fontSize:12, color:'var(--accent)', fontWeight:600, textAlign:'center', marginTop:-8 }}>
    ...
  </div>
  ```
  - `var(--acl)` → `var(--accent)`
- 텍스트 verbatim 유지: `${rangeDays}일 범위로 1건 추가됩니다 (주말·공휴일 ${skippedCount}일은 표시에서 제외)` / `${rangeDays}일 범위로 1건 추가됩니다` (NEGATIVE #12)

**Step 12 — CTA grid (line 1083~1103) — CRITICAL linear-gradient 폐기:**
- 컨테이너 `style={{ display:'grid', gridTemplateColumns:'1fr 3fr 1fr', gap:6, marginTop:4 }}` 유지
- ‹ / › 버튼 인라인:
  ```tsx
  style={{
    padding:'14px 0', borderRadius:12, border:'1px solid var(--border-strong)',
    background:'var(--surface-sunken)', color:'var(--text-secondary)',
    fontSize:18, lineHeight:1, cursor:'pointer',
    display:'flex', alignItems:'center', justifyContent:'center',
  }}
  ```
  - `var(--bd2)` → `var(--border-strong)` / `var(--bg3)` → `var(--surface-sunken)` / `var(--t2)` → `var(--text-secondary)`
  - executor 판단: `‹` / `›` 문자 유지 OR lucide `<ChevronLeft size={18} />` / `<ChevronRight size={18} />` 치환 권장 (W7 OQ #2 LOCKED a 일관)

- **저장 버튼 (LOCKED #2 b — CRITICAL):**
  ```tsx
  <button onClick={handleSave} disabled={saving}
    style={{
      padding:'14px', borderRadius:12, border:'none',
      background:'var(--accent)',
      color:'#fff', fontSize:16, fontWeight:700, cursor:'pointer',
      opacity: saving ? 0.6 : 1,
    }}>
    {saving ? '저장 중...' : '저장'}
  </button>
  ```
  - **`linear-gradient(135deg,#1d4ed8,#2563eb)` 완전 폐기 → `var(--accent)` solid**
  - **`fontSize:14` → `16` 노안 격상**
  - `'저장 중...' : '저장'` 텍스트 verbatim 유지

**Step 13 — lbl/inp 헬퍼 매핑 (line 1191~1198):**

```ts
const lbl: React.CSSProperties = {
  fontSize:12, fontWeight:700, color:'var(--text-tertiary)',
  display:'block', marginBottom:6, lineHeight:1,
}
const inp: React.CSSProperties = {
  width:'100%', padding:'11px 12px', borderRadius:10, boxSizing:'border-box',
  background:'var(--surface-sunken)', border:'1px solid var(--border-strong)',
  color:'var(--text-primary)', fontSize:13, outline:'none', fontFamily:'inherit',
}
```
- `fontSize:11` → `12` 노안 격상
- `lineHeight:1` 추가 (Rule 5)
- 옛 alias 4개 모두 v0.1.1 매핑

**NEGATIVE 강제 (§5 표):**
- handleSave / scheduleApi.create / toast 카피 4건 / onSaved / INSP_CATEGORIES 등 데이터 / handleCat 등 로직 / N일 미리보기 텍스트 → **1 byte 도 변경 X**

**참고 — SW3 학습 (260520-1q0 SUMMARY):**
- status- prefix 사용 금지 (Rule 1) — SW3 에서 `text-status-fire-bar` 잘못 사용 후 정정
- Tailwind 별칭 키 (text-fire-bar, bg-info-bg 등) 직접 사용
- 본 SW5 에서는 status 토큰 미사용 → 해당 없음, 다만 `bg-accent` / `text-accent` / `text-text-on-accent` 만 사용

**참고 — SW4 학습 (260520-2fg SUMMARY):**
- monthlyPlan 영역 토큰화 시 인라인 alias 잔존 0 강제 (SW5 도 동일 강제)
- lucide 도입 시 import 정확 (X 추가)
  </action>
  <verify>
    <automated>
cd /Users/jykevin/Documents/cbc7119-design/cha-bio-safety
npx tsc --noEmit 2>&1 | tail -20
npm run build 2>&1 | tail -20

# Verify gate (W7 §4 — SW5 관련)

# §4.1 #1 — close X 영역 SVG path 4 줄 0 (lucide 치환 완료)
grep -n 'd="M6 18L18 6M6 6l12 12"' src/pages/SchedulePage.tsx | wc -l   # 0

# §4.1 #3 — status- prefix 잘못된 패턴 0
grep -nE 'text-status-|bg-status-|border-status-' src/pages/SchedulePage.tsx | wc -l   # 0

# §4.1 #4 — linear-gradient 0 (저장 버튼 폐기 LOCKED)
grep -n 'linear-gradient' src/pages/SchedulePage.tsx | wc -l   # 0

# §4.1 #5 — AddModal 영역 옛 alias var() 0
# AddModal: line 791~1108 + helper line 1191~1198
sed -n '791,1108p;1191,1198p' src/pages/SchedulePage.tsx > /tmp/sw5_scope.txt
grep -E 'var\(--(bg2|bg3|bd|bd2|t1|t2|t3|acl|safe|info|danger|warn)\)' /tmp/sw5_scope.txt | wc -l   # 0

# §4.1 #8 — w-8 h-8 confusion check (CRITICAL Rule 4)
# AddModal 안에 close X 버튼 영역 w-7 h-7 사용 확인
sed -n '791,1108p' src/pages/SchedulePage.tsx | grep -E 'w-7 h-7|w-\[32px\] h-\[32px\]' | wc -l   # ≥1

# §4.2 #9 — surface 토큰 추가 (bg-surface-raised + bg-surface-sunken)
sed -n '791,1108p;1191,1198p' src/pages/SchedulePage.tsx | grep -E 'surface-raised|surface-sunken' | wc -l   # ≥5

# §4.2 #10 — text 토큰 추가 (text-text-* 또는 var(--text-*))
sed -n '791,1108p;1191,1198p' src/pages/SchedulePage.tsx | grep -E 'text-text-|var\(--text-(primary|secondary|tertiary)' | wc -l   # ≥10

# §4.2 #11 — border 토큰 추가
sed -n '791,1108p;1191,1198p' src/pages/SchedulePage.tsx | grep -E 'border-border-|var\(--border-(default|strong)' | wc -l   # ≥5

# §4.2 #12 — accent 토큰 (저장 버튼 + N일 미리보기)
sed -n '791,1108p' src/pages/SchedulePage.tsx | grep -E 'bg-accent|var\(--accent\)' | wc -l   # ≥2

# §4.2 #17 — lucide X size=16 도입
grep -nE '<X size=\{16\}' src/pages/SchedulePage.tsx | wc -l   # ≥1
grep -nE '^import.*\{[^}]*\bX\b[^}]*\}.*lucide-react' src/pages/SchedulePage.tsx | wc -l   # ≥1

# 노안 격상 검증
# 헤더 타이틀 text-title 도입
sed -n '791,1108p' src/pages/SchedulePage.tsx | grep -E 'text-title' | wc -l   # ≥1
# 저장 버튼 fontSize:16
sed -n '1083,1103p' src/pages/SchedulePage.tsx | grep -E 'fontSize:16|text-body' | wc -l   # ≥1
# fire picker fontSize 10 폐기
sed -n '1016,1044p' src/pages/SchedulePage.tsx | grep -E 'fontSize:10\b' | wc -l   # 0
# lbl helper fontSize 11→12
sed -n '1191,1198p' src/pages/SchedulePage.tsx | grep -E 'fontSize:12' | wc -l   # ≥1
sed -n '1191,1198p' src/pages/SchedulePage.tsx | grep -E 'fontSize:11\b' | wc -l   # 0
# lbl helper lineHeight:1 추가
sed -n '1191,1198p' src/pages/SchedulePage.tsx | grep -E 'lineHeight:1' | wc -l   # ≥1

# NEGATIVE 강제 — handleSave + toast 카피 보존
grep -c "toast.error('날짜를 입력하세요')" src/pages/SchedulePage.tsx   # ≥1
grep -c "toast.error('점검 분류를 선택하세요')" src/pages/SchedulePage.tsx   # ≥1
grep -c "toast.error('제목을 입력하세요')" src/pages/SchedulePage.tsx   # ≥1
grep -c "toast.error('저장 실패')" src/pages/SchedulePage.tsx   # ≥1
grep -c 'scheduleApi.create' src/pages/SchedulePage.tsx   # ≥1
grep -c 'onSaved()' src/pages/SchedulePage.tsx   # ≥1

# §4.3 #19 / #20 — tsc + build PASS
# (위 npx tsc / npm run build 결과 확인)
    </automated>
  </verify>
  <done>
- AddModal (line 791~1108) 의 옛 alias var() (--bg2/bg3/bd/bd2/t1/t2/t3/acl) AddModal 영역 안 0회
- lbl/inp 헬퍼 (line 1191~1198) v0.1.1 매핑 완료 + lbl fontSize 11→12 + lineHeight:1 추가
- close X 버튼 정확히 32×32 (`w-7 h-7` 또는 `w-[32px] h-[32px]`) — w-8 함정 회피
- lucide `<X size={16} />` 도입 (import 에 X 추가) + 기존 SVG path 4 줄 폐기
- 저장 버튼 linear-gradient 폐기 + `bg-accent` solid + fontSize 14→16 노안 격상
- 헤더 타이틀 text-title (15→18) 노안 격상
- fire picker fontSize 10→12 노안 격상
- handleSave / scheduleApi.create / toast 카피 4건 / onSaved / 5 cat 분기 / INSP_CATEGORIES 데이터 / handleCat 등 로직 / N일 미리보기 텍스트 — 1 byte 도 변경 X
- §10 인라인 화이트리스트 4건 (`${c.color}22` / elev rgba 0.15 / fire rgba 0.13 / overlay rgba 0.55) 잔존 OK
- tsc --noEmit exit 0
- npm run build exit 0
- verify gate 모든 grep 조건 통과
  </done>
</task>

</tasks>

<verification>
**Build 검증:**
```bash
cd /Users/jykevin/Documents/cbc7119-design/cha-bio-safety
npx tsc --noEmit   # exit 0
npm run build      # exit 0
```

**Workspace rule (CLAUDE.local.md):**
- wrangler 명령 X / `npm run deploy` X / 운영 PWA hotfix X
- 작업 위치: `/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/src/pages/SchedulePage.tsx`
- 브랜치: `redesign/13-schedule`
- main 머지 / push 사용자 컨펌 후

**commit message (planner 권장):**
`tsx(13-schedule): SW5 — AddModal (BottomSheet + 5 cat 분기 + CTA grid + linear-gradient 폐기)`
</verification>

<success_criteria>
- SchedulePage.tsx 변경 후 line 수 변화 +30 ~ +50 (linear-gradient 폐기 1줄 + lucide X 도입 import 1 + SVG path 4줄 → `<X />` 1줄 + className 확장 + 인라인 alias 토큰화)
- AddModal 영역 (line 791~1108) + lbl/inp 헬퍼 (line 1191~1198) 옛 alias 0
- linear-gradient 전역 0
- close X 정확 32×32 (Rule 4 회피 — `w-7 h-7`)
- lucide `<X size={16} />` import + 사용 ≥1
- W4 OQ #1/#2/#3 LOCKED 결정 verbatim 유지 (90dvh / bg-accent solid / native select)
- 노안 격상 5건 (헤더 15→18, close 28→32, SVG 14→16, fire 10→12, 저장 14→16, lbl 11→12)
- NEGATIVE §5 표 12건 1 byte 도 변경 X
- §10 인라인 화이트리스트 4건 잔존 OK
- tsc + build exit 0
</success_criteria>

<output>
SW5 완료 후 `.planning/quick/260520-426-redesign-13-schedule-tsx-sw5-addmodal-bo/260520-426-SUMMARY.md` 작성:
- 변경 파일: `cha-bio-safety/src/pages/SchedulePage.tsx`
- 변경 line 수
- linear-gradient → bg-accent 치환 verification
- close X 32×32 (w-7 h-7) Rule 4 회피 verification
- 옛 alias 0 verification (AddModal 영역 + helper)
- 노안 격상 5건 verification
- NEGATIVE §5 12건 보존 verification
- tsc + build exit 0 결과
- W4 OQ #1/#2/#3 LOCKED 보존 확인
- §10 인라인 화이트리스트 4건 잔존 확인
- SW6 (EditModal) inherit 학습 사항 (lbl/inp helper 가 v0.1.1 매핑 완료 — SW6 시작점)
- commit hash (사용자 컨펌 후 main 머지)
</output>
