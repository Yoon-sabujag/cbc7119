---
phase: 260611-jo8-staging-f5bb1db-3-prod-kst
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cha-bio-safety/src/components/FindingEditModal.tsx
  - cha-bio-safety/src/components/FindingFormSheet.tsx
  - cha-bio-safety/src/components/PhotoButton.tsx
  - cha-bio-safety/src/components/PhotoGrid.tsx
  - cha-bio-safety/src/components/PhotoSourceModal.tsx
  - cha-bio-safety/src/hooks/useMultiPhotoUpload.ts
  - cha-bio-safety/src/hooks/usePhotoUpload.ts
  - cha-bio-safety/src/pages/FloorPlanPage.tsx
  - cha-bio-safety/src/pages/InspectionPage.tsx
  - cha-bio-safety/src/pages/LegalFindingDetailPage.tsx
  - cha-bio-safety/src/pages/LegalPage.tsx
  - cha-bio-safety/src/pages/SchedulePage.tsx
  - cha-bio-safety/src/utils/generateMonthlyPlan.ts
  - cha-bio-safety/src/utils/photoVault.ts
autonomous: true
requirements: [STABILITY-PHOTO-GUARD, STABILITY-MONTH-CLAMP, STABILITY-KST-SESSION]

must_haves:
  truths:
    - "점검/조치 사진 업로드가 부분 실패하면 저장이 차단되고 토스트로 알린다 (조용한 누락 방지)"
    - "첨부한 사진 원본이 IndexedDB 보관함에 남아 업로드 실패/이탈 후에도 복구 재시도 가능하다"
    - "월 경계에 걸친 일정이 월간 점검 계획 미리보기/엑셀 양쪽 달에서 누락 없이 표시된다"
    - "도면 점검 세션 날짜가 KST 기준으로 귀속된다 (새벽 기록이 전날 세션으로 빠지지 않는다)"
    - "RemediationDetailPage 는 staging 과 동일하게 무수정으로 유지된다"
  artifacts:
    - path: "cha-bio-safety/src/utils/photoVault.ts"
      provides: "IndexedDB 사진 보관함 (vaultPut/Delete/List/Claim/Release/isClaimed)"
      contains: "export async function vaultPut"
    - path: "cha-bio-safety/src/hooks/useMultiPhotoUpload.ts"
      provides: "uploadAll 부분 실패 시 PhotoUploadFailedError throw + 보관함 연동"
      contains: "export class PhotoUploadFailedError"
    - path: "cha-bio-safety/src/hooks/usePhotoUpload.ts"
      provides: "vaultScope 인자 + restoreFromVault + key 재사용"
      contains: "restoreFromVault"
    - path: "cha-bio-safety/src/utils/generateMonthlyPlan.ts"
      provides: "월 경계 일정 YMD 클램프"
      contains: "monthEnd"
  key_links:
    - from: "cha-bio-safety/src/pages/InspectionPage.tsx"
      to: "PhotoUploadFailedError / 사진 업로드 실패 throw"
      via: "photoKey === null 가드"
      pattern: "사진 업로드 실패"
    - from: "cha-bio-safety/src/pages/FloorPlanPage.tsx"
      to: "todayKstYmd()"
      via: "세션 날짜 KST 귀속"
      pattern: "todayKstYmd"
    - from: "cha-bio-safety/src/components/PhotoSourceModal.tsx"
      to: "restoreFromVault"
      via: "restoreCount + onRestore prop"
      pattern: "restoreCount="
---

<objective>
staging `cbc7119-data` f5bb1db 의 안정성 3종 수정을 prod (`cha-bio-safety/`, 브랜치 `production`) 으로 이관한다.

① **사진 업로드 실패 가드 + IndexedDB 보관함** — `useMultiPhotoUpload`/`usePhotoUpload` 부분 실패 시 저장 차단 + 토스트, 첨부 즉시 원본을 IndexedDB(photoVault) 에 캐싱해 실패/이탈 후 복구 재시도. 호출부 가드 15곳 (InspectionPage 12 + FloorPlanPage 3, BC 선업로드 재배치 포함).
③ **월 경계 일정 클램프** — `generateMonthlyPlan.ts` + SchedulePage `MonthlyPlanPreview` 가 월 경계 일정을 대상 월 범위 YMD 로 클램프 (stats.ts 패턴).
④ **KST 세션 날짜** — FloorPlanPage 도면 점검 세션 + InspectionPage `ensureSession` 이 `todayKstYmd()` 로 수렴 (UTC `toISOString()` 제거).

Purpose: 사진 조용한 누락 / 월 경계 일정 누락 / 새벽 세션 오귀속 3종 prod 안정성 결함 제거.
Output: 소스 13파일 패치 + `photoVault.ts` 신규, 단일 atomic commit. 배포는 오케스트레이터(메인 Claude)가 별도 수행.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md
@.planning/quick/260611-jo8-staging-f5bb1db-3-prod-kst/260611-jo8-apply-A.patch

**오케스트레이터가 이미 검증한 사실 (재조사 금지):**
- prod 레포 루트 = repo 루트, 앱 = `cha-bio-safety/` 서브디렉토리, 브랜치 `production` (전환 금지).
- staging repo `~/Documents/cbc7119-data` 는 읽기 전용 — 절대 쓰기 금지.
- `git apply --check --directory=cha-bio-safety 260611-jo8-apply-A.patch` (13파일 + photoVault.ts 신규) = CLEAN. 분화된 4 페이지(FloorPlanPage/InspectionPage/LegalFindingDetailPage/SchedulePage)는 offset 으로 컨텍스트 매칭 성공.
- LegalPage.tsx 만 patch hunk(PhotoSourceModal) 컨텍스트 불일치 → 수동 Edit 4지점 필요 (Task 2 의 verbatim 사용).
- `src/utils/datetime.ts` 에 `todayKstYmd()` 이미 존재 — patch import 가 그대로 동작.
- **RemediationDetailPage(L43/L245) 는 staging 도 무수정 — 절대 건드리지 말 것.**

**워크트리/배포 제약 (반드시 준수):**
- executor 는 `wrangler` / `npm run deploy` / `wrangler d1` 등 배포·D1 명령을 **절대 실행 금지** (서브에이전트 production deploy 금지 룰). 배포는 오케스트레이터(메인 Claude)가 수행한다.
- executor 는 `tsc` / `npm run build` 실행 금지 (worktree 에 node_modules 없음). tsc + build 게이트는 오케스트레이터가 merge 후 수행한다.
- staging repo 와 `.planning/` 외 다른 파일 수정 금지.
- 브랜치 `production` 전환 금지.
</context>

<tasks>

<task type="auto">
  <name>Task 1: patch A 적용 + photoVault 신규 생성</name>
  <files>cha-bio-safety/src/components/FindingEditModal.tsx, cha-bio-safety/src/components/FindingFormSheet.tsx, cha-bio-safety/src/components/PhotoButton.tsx, cha-bio-safety/src/components/PhotoGrid.tsx, cha-bio-safety/src/components/PhotoSourceModal.tsx, cha-bio-safety/src/hooks/useMultiPhotoUpload.ts, cha-bio-safety/src/hooks/usePhotoUpload.ts, cha-bio-safety/src/pages/FloorPlanPage.tsx, cha-bio-safety/src/pages/InspectionPage.tsx, cha-bio-safety/src/pages/LegalFindingDetailPage.tsx, cha-bio-safety/src/pages/SchedulePage.tsx, cha-bio-safety/src/utils/generateMonthlyPlan.ts, cha-bio-safety/src/utils/photoVault.ts</files>
  <action>
repo 루트(`/Users/jongyupyoon/Documents/20260328`)에서 patch A 를 `--directory=cha-bio-safety` 로 적용한다:
`git apply --directory=cha-bio-safety .planning/quick/260611-jo8-staging-f5bb1db-3-prod-kst/260611-jo8-apply-A.patch`

이 patch 는 13개 파일 수정 + `cha-bio-safety/src/utils/photoVault.ts` 신규 생성을 포함한다 (LegalPage.tsx 제외 — Task 2 에서 수동 처리). `--check` 는 오케스트레이터가 이미 CLEAN 확인했으므로 그대로 적용한다.

분화된 4 페이지(FloorPlanPage/InspectionPage/LegalFindingDetailPage/SchedulePage)는 offset 으로 매칭되므로 hunk 가 의도된 위치에 들어간다. apply 가 reject(.rej 생성) 나 fuzz 경고 없이 종료코드 0 으로 끝나야 한다. 실패하면 즉시 중단하고 출력을 보고한다 — 수동 분할 적용 시도 금지.

wrangler / npm / tsc 등 어떤 빌드·배포 명령도 실행하지 않는다.
  </action>
  <verify>
    <automated>cd /Users/jongyupyoon/Documents/20260328 && git status --short | grep -c "cha-bio-safety/src/utils/photoVault.ts" && git status --short | grep -E "cha-bio-safety/src/(components|hooks|pages|utils)" | grep -vc "LegalPage.tsx"</automated>
  </verify>
  <done>patch A 가 종료코드 0 으로 적용됨. `git status --short` 에 변경 13파일(LegalPage 제외) + 신규 `cha-bio-safety/src/utils/photoVault.ts` 가 나타난다. `.rej` 파일 0개.</done>
</task>

<task type="auto">
  <name>Task 2: LegalPage.tsx 수동 4지점 Edit (PhotoUploadFailedError + 보관함 wiring)</name>
  <files>cha-bio-safety/src/pages/LegalPage.tsx</files>
  <action>
patch hunk 컨텍스트 불일치로 자동 적용 불가한 LegalPage.tsx 4지점을 Edit 도구로 정확히 수정한다 (prod 현재 상태 기준 verbatim, 아래 old_string → new_string).

(a) L9 import 확장 — old_string:
`import { useMultiPhotoUpload } from '../hooks/useMultiPhotoUpload'`
new_string:
`import { useMultiPhotoUpload, PhotoUploadFailedError } from '../hooks/useMultiPhotoUpload'`

(b) L830 resPhotos scope 인자 — old_string:
`  const resPhotos = useMultiPhotoUpload()`
new_string:
`  const resPhotos = useMultiPhotoUpload('legal-finding-resolution')`

(c) L856 onError 핸들러 — old_string:
`    onError: () => toast.error('조치 처리 실패'),`
new_string:
`    onError: (err) => {
      // 사진 업로드 부분 실패 — 저장은 차단됐고 성공분은 슬롯에 남아 재시도 시 재업로드되지 않음
      if (err instanceof PhotoUploadFailedError) {
        toast.error(err.message)
        return
      }
      toast.error('조치 처리 실패')
    },`

(d) L926 PhotoSourceModal 에 보관함 prop 추가 — `onAlbum={resPhotos.pickAlbum}` 뒤에 restore prop 2종을 추가하고 self-closing 유지. old_string:
`<PhotoSourceModal open={resPhotos.showPicker} onClose={resPhotos.closePicker} onCamera={resPhotos.pickCamera} onAlbum={resPhotos.pickAlbum} />`
new_string:
`<PhotoSourceModal open={resPhotos.showPicker} onClose={resPhotos.closePicker} onCamera={resPhotos.pickCamera} onAlbum={resPhotos.pickAlbum} restoreCount={resPhotos.vaultPendingCount} onRestore={resPhotos.restoreFromVault} />`

LegalPage.tsx 의 다른 지점은 건드리지 않는다. RemediationDetailPage 등 다른 파일도 손대지 않는다.
  </action>
  <verify>
    <automated>cd /Users/jongyupyoon/Documents/20260328/cha-bio-safety && grep -c "PhotoUploadFailedError" src/pages/LegalPage.tsx && grep -c "useMultiPhotoUpload('legal-finding-resolution')" src/pages/LegalPage.tsx && grep -c "restoreCount={resPhotos.vaultPendingCount}" src/pages/LegalPage.tsx</automated>
  </verify>
  <done>LegalPage.tsx 4지점 모두 적용: `PhotoUploadFailedError` 2회(import + instanceof), `useMultiPhotoUpload('legal-finding-resolution')` 1회, `restoreCount={resPhotos.vaultPendingCount}` 1회. PhotoSourceModal 은 self-closing 유지.</done>
</task>

<task type="auto">
  <name>Task 3: grep verify 게이트 (전수 PASS) + 단일 atomic commit</name>
  <files>cha-bio-safety/ (변경 14파일 + photoVault.ts 신규 — 신규 코드 없음, 검증·커밋만)</files>
  <action>
아래 grep 게이트를 `cha-bio-safety/` 디렉토리 기준으로 전부 실행하고 **모든 항목이 명시된 기대값과 일치(PASS)** 하는지 확인한다. 하나라도 불일치하면 즉시 중단하고 어느 게이트가 어떤 실제값을 냈는지 보고한다 — 임의 수정 금지.

**사진 실패 가드 토스트/throw:**
- `grep -c "사진 업로드 실패" src/pages/InspectionPage.tsx` == 12
- `grep -c "사진 업로드 실패" src/pages/FloorPlanPage.tsx` == 3

**usePhotoUpload scope 인자:**
- `grep -c "usePhotoUpload('inspection')" src/pages/InspectionPage.tsx` == 9
- `grep -c "usePhotoUpload('inspection-bc')" src/pages/InspectionPage.tsx` == 1
- `grep -c "usePhotoUpload('inspection-resolution')" src/pages/InspectionPage.tsx` == 1
- `grep -c "usePhotoUpload('inspection')" src/pages/FloorPlanPage.tsx` == 1
- `grep -c "usePhotoUpload('inspection-bc')" src/pages/FloorPlanPage.tsx` == 1
- `grep -c "usePhotoUpload('inspection-resolution')" src/pages/FloorPlanPage.tsx` == 1

**useMultiPhotoUpload scope 인자 (전 cha-bio-safety/src 기준):**
- `grep -rc "useMultiPhotoUpload('legal-finding')" src/` 합계 == 2 (FindingEditModal + FindingFormSheet)
- `grep -rc "useMultiPhotoUpload('legal-finding-resolution')" src/` 합계 == 3 (FindingEditModal + LegalFindingDetailPage + LegalPage)
  (정확한 합산: `grep -r "useMultiPhotoUpload('legal-finding')" src/ | wc -l` 및 `grep -r "useMultiPhotoUpload('legal-finding-resolution')" src/ | wc -l` 사용)

**KST 세션 날짜:**
- `grep -c "todayKstYmd" src/pages/FloorPlanPage.tsx` == 3 (import 1 + 사용 2)
- `grep -c "todayKstYmd" src/pages/InspectionPage.tsx` == 2 (import 1 + ensureSession 1)
- `grep -c "new Date().toISOString().slice(0, 10)" src/pages/FloorPlanPage.tsx` == 0

**보관함 복구 wiring (restoreCount prop):**
- `grep -r "restoreCount=" src/ | wc -l` == 7 (FindingEditModal 2 + FindingFormSheet 1 + PhotoButton 1 + PhotoGrid 1 + LegalFindingDetailPage 1 + LegalPage 1)

**LegalPage 핸들러:**
- `grep -c "PhotoUploadFailedError" src/pages/LegalPage.tsx` == 2 (import + instanceof)

**월 경계 클램프:**
- `grep -n "monthEnd" src/utils/generateMonthlyPlan.ts` 존재 + `grep -n "monthEnd" src/pages/SchedulePage.tsx` 존재
- `grep -c "padStart(2, '0')" src/utils/generateMonthlyPlan.ts` >= 1 (월 범위 zero-pad)

**FloorPlanPage BC 선업로드 재배치 (bcPhotoKey 정의가 첫 submitRecord 호출보다 앞):**
- `grep -n "bcPhotoKey\|submitRecord" src/pages/FloorPlanPage.tsx` 출력에서 `const bcPhotoKey = pairedBC ? await inspectBcPhoto.upload()` 의 라인번호가 첫 `inspectionApi.submitRecord` 호출 라인번호보다 작아야 한다.

**RemediationDetailPage 불변 + diff scope:**
- repo 루트에서 `git diff --stat` 에 `RemediationDetailPage` 미등장
- repo 루트에서 `git status --short` 변경 14파일(소스 13 + LegalPage) + 신규 `cha-bio-safety/src/utils/photoVault.ts` (총 14 path) 외 다른 파일 없음

**전부 PASS 확인 후 단일 atomic commit:**
repo 루트에서 변경 소스 14파일 + 신규 photoVault.ts 를 한 커밋으로 stage + commit 한다. `.planning/` 산출물은 이 커밋에 포함하지 않는다 (오케스트레이터가 별도 관리).
커밋 메시지:
`fix(260611-jo8): 안정성 3종 — 사진 업로드 실패 가드+보관함, 월 경계 클램프, KST 세션 날짜 (staging f5bb1db sync)`

wrangler / npm / tsc / git push 는 실행하지 않는다 (배포·빌드·푸시는 오케스트레이터 담당).
  </action>
  <verify>
    <automated>cd /Users/jongyupyoon/Documents/20260328 && git log -1 --stat | grep -c "photoVault.ts" && git log -1 --format=%s | grep -c "260611-jo8" && git show --stat HEAD | grep -c "RemediationDetailPage"</automated>
  </verify>
  <done>모든 grep 게이트 PASS. 단일 commit 에 소스 14파일 + photoVault.ts 신규 포함, 메시지에 `260611-jo8` 포함, RemediationDetailPage 미등장(grep -c 결과 0). git push 미실행.</done>
</task>

</tasks>

<verification>
- patch A + LegalPage 수동 4지점 모두 적용, `.rej` 0개.
- 사진 실패 가드 토스트/throw 15곳 (InspectionPage 12 + FloorPlanPage 3) 전수 일치.
- scope 인자: usePhotoUpload inspection 9 + bc 1 + resolution 1 (InspectionPage), 1/1/1 (FloorPlanPage); useMultiPhotoUpload legal-finding 2 + legal-finding-resolution 3.
- `todayKstYmd` FloorPlanPage 3 / InspectionPage 2, UTC `toISOString().slice(0,10)` FloorPlanPage 0.
- restoreCount prop 7곳, LegalPage PhotoUploadFailedError 2회.
- 월 경계: `monthEnd` 양쪽 파일 존재 + zero-pad.
- FloorPlanPage BC 선업로드 재배치(bcPhotoKey 가 첫 submitRecord 앞).
- RemediationDetailPage 무수정 (diff 미등장).
- 단일 atomic commit (소스 14 + photoVault 신규), `.planning/` 미포함.
- tsc / build / 배포 게이트는 오케스트레이터가 merge 후 수행 (executor 미실행).
</verification>

<success_criteria>
- staging f5bb1db 안정성 3종이 prod `cha-bio-safety/` 에 byte-동등하게 이관됨.
- 사진 부분 실패 시 저장 차단 + 토스트, 첨부 원본 IndexedDB 보관 + 복구 경로 노출.
- 월 경계 일정이 미리보기/엑셀 양쪽 달에서 누락 없이 표시.
- 도면/점검 세션 날짜가 KST 귀속.
- RemediationDetailPage 무수정 유지.
- 변경 14파일 + photoVault.ts 신규가 단일 atomic commit.
</success_criteria>

<output>
Create `.planning/quick/260611-jo8-staging-f5bb1db-3-prod-kst/260611-jo8-SUMMARY.md` when done
</output>
