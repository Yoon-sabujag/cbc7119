---
phase: quick-260704-0xr
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cha-bio-safety/src/utils/panelEvents.ts
  - cha-bio-safety/src/components/PanelEventRow.tsx
  - cha-bio-safety/src/pages/InspectionPage.tsx
  - cha-bio-safety/src/pages/FireAlarmHistoryPage.tsx
  - cha-bio-safety/src/App.tsx
autonomous: true
requirements: [QUICK-260704-0xr]
must_haves:
  truths:
    - "화재수신반 카드 '최근 이벤트'가 자동감지(panel_alarms)와 수동기록(fire_alarm_records, 비-panel-agent)을 시각 하나로 병합해 시간 내림차순으로 보여준다"
    - "카드 각 아이템 좌측이 [종류칩] 위/아래 [자동감지 또는 수동 칩] 세로 스택으로 출처를 한눈에 구분한다 (승인 시안 001-B)"
    - "카드 우상단(비-경보 모드) '전체 이력' 버튼이 /fire-alarm-history 로 이동한다. 경보 모드 '감지중 N' 칩은 그대로 유지된다"
    - "신설 '화재수신반 이력' 페이지가 종류/출처 세그먼트 필터 + 월 스테퍼로 병합 이력을 날짜 그룹으로 보여준다 (승인 시안 002-A)"
    - "백엔드(functions/) 는 전혀 건드리지 않고 기존 alarmApi.getEvents / fireAlarmApi.getByYear 만 사용한다"
  artifacts:
    - path: "cha-bio-safety/src/utils/panelEvents.ts"
      provides: "병합 헬퍼(PanelEventItem 타입, mergePanelEvents, KIND_BADGE, useRecentPanelEvents 훅)"
      exports: ["PanelEventItem", "mergePanelEvents", "KIND_BADGE", "useRecentPanelEvents"]
    - path: "cha-bio-safety/src/components/PanelEventRow.tsx"
      provides: "시안 001-B 아이템 1행 렌더 (카드 ×2 + 이력페이지 공용)"
      exports: ["PanelEventRow"]
    - path: "cha-bio-safety/src/pages/FireAlarmHistoryPage.tsx"
      provides: "신설 화재수신반 이력 페이지 (필터+월스텝+날짜그룹 리스트)"
    - path: "cha-bio-safety/src/App.tsx"
      contains: "/fire-alarm-history"
  key_links:
    - from: "cha-bio-safety/src/pages/InspectionPage.tsx"
      to: "cha-bio-safety/src/utils/panelEvents.ts"
      via: "useRecentPanelEvents(events) / useRecentPanelEvents(panelEvents)"
      pattern: "useRecentPanelEvents"
    - from: "cha-bio-safety/src/pages/FireAlarmHistoryPage.tsx"
      to: "cha-bio-safety/src/utils/panelEvents.ts"
      via: "mergePanelEvents(auto, manual)"
      pattern: "mergePanelEvents"
    - from: "cha-bio-safety/src/App.tsx"
      to: "cha-bio-safety/src/pages/FireAlarmHistoryPage.tsx"
      via: "lazy import + <Route path='/fire-alarm-history'>"
      pattern: "fire-alarm-history"
---

<objective>
화재수신반 '최근 이벤트' 카드를 자동감지(panel_alarms) + 수동기록(fire_alarm_records) 병합 뷰로 바꾸고, 승인 시안 001-B(좌측 종류/출처 세로 칩 스택) 아이템 스타일을 적용한다. 카드 우상단에 '전체 이력' 버튼을 달아 신설 '화재수신반 이력' 페이지(승인 시안 002-A: 종류·출처 세그먼트 필터 + 월 스테퍼 + 날짜 그룹 리스트)로 연결한다.

Purpose: 지금 카드는 자동감지만 보여주고 방재팀이 수기로 남긴 화재/비화재보 기록은 카드에 안 뜬다. 병합해서 한 곳에서 최근 흐름을 보고, 전체 이력은 별도 페이지에서 월별로 조회할 수 있게 한다.
Output: 병합 헬퍼 유틸 1 + 공용 행 컴포넌트 1 + 신설 이력 페이지 1 + InspectionPage 카드 2곳 수정 + App.tsx 라우트 1.

제약(반드시 준수):
- **프론트엔드만. functions/ 백엔드 절대 변경 금지.** 기존 엔드포인트(alarmApi.getEvents, fireAlarmApi.getByYear)만 사용. (이유: 이 콘솔은 production 전용 — API/스키마 변경은 staging 먼저 룰. events 720h 캡은 감시가 6월 시작이라 지금 전량 커버돼 문제 없음.)
- worktree OFF, production 브랜치 직접 편집. **배포/커밋-push 지시 없음** — 구현 + typecheck 까지만. (헤드리스 렌더 검증은 오케스트레이터가 별도 수행.)
- 새 npm 의존성 X. 기존 컨벤션(인라인 Tailwind class, 다크 토큰 클래스) 유지.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md

# 승인 시안 (winner=B / winner=A)
@cha-bio-safety/.planning/sketches/001-card-event-item/index.html
@cha-bio-safety/.planning/sketches/002-history-page/index.html

# 데이터 shape / 기존 API (읽기 전용 — 변경 금지)
@cha-bio-safety/src/utils/api.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: 병합 헬퍼 + 공용 행 컴포넌트 + 카드 ×2 개조</name>
  <files>cha-bio-safety/src/utils/panelEvents.ts (신규), cha-bio-safety/src/components/PanelEventRow.tsx (신규), cha-bio-safety/src/pages/InspectionPage.tsx (수정)</files>
  <action>
【1a. 병합 헬퍼 — src/utils/panelEvents.ts (신규)】
- 기존 타입 재사용: `import { fireAlarmApi, type Alarm } from './api'`, `import { useQuery } from '@tanstack/react-query'`.
- 정규화 타입 export: `PanelEventItem = { id: string; kind: 'fire'|'equip'|'fault'; time: string; location: string|null; cause: string|null; source: 'auto'|'manual' }`. time 은 'YYYY-MM-DD HH:MM:SS' 문자열.
- `KIND_BADGE` export (badge2/panelBadge2 와 동일 매핑): fire → { label:'화재', cls:'text-danger bg-danger-bg' } / equip → { label:'설비동작', cls:'text-safe bg-safe-bg' } / fault → { label:'고장', cls:'text-warning bg-warning-bg' }.
- `normalizeAuto(alarms: Alarm[]): PanelEventItem[]` — 각 Alarm 을 { id, kind: a.type, time: a.detectedAt, location: a.location ?? null, cause: a.cause ?? null, source:'auto' } 로 매핑.
- `normalizeManual(records: any[]): PanelEventItem[]` — `(records ?? []).filter(r => r.created_by !== 'panel-agent')` 로 자동초안 제외(panel_alarms 와 중복 방지) 후 { id: r.id, kind:'fire'(fire/non_fire 모두 화재수신반=화재로 취급), time: r.occurred_at, location: r.location ?? null, cause: r.cause ?? null, source:'manual' }.
- `mergePanelEvents(auto: Alarm[], manual: any[]): PanelEventItem[]` — `[...normalizeAuto(auto), ...normalizeManual(manual)]` 를 time **문자열** 내림차순 정렬: `.sort((a,b) => b.time.localeCompare(a.time))` (고정폭 포맷이라 문자열 비교로 시간순 정확).
- KST 포맷 헬퍼 `kstStr(d: Date): string` — Intl.DateTimeFormat('en-CA', { timeZone:'Asia/Seoul', year/month/day '2-digit'(연은 numeric), hour/minute/second '2-digit', hourCycle:'h23' }).formatToParts 로 'YYYY-MM-DD HH:MM:SS' 조립. 브라우저 로컬 tz 무관하게 KST wall-clock 을 얻기 위함(Date epoch 산술 함정 회피).
- 카드용 훅 export: `useRecentPanelEvents(autoEvents: Alarm[], windowHours = 48): PanelEventItem[]`.
    - `const nowStr = kstStr(new Date())`; `const y = Number(nowStr.slice(0,4))`.
    - 연말경계: `const needPrev = nowStr.slice(5,7) === '01' && Number(nowStr.slice(8,10)) <= 2` (1/1~1/2 면 48h 창이 전년으로 넘어감).
    - `const cur = useQuery({ queryKey:['fire-alarm-year', y], queryFn: async()=>{ try { return await fireAlarmApi.getByYear(y) } catch { return [] as any[] } }, staleTime:30_000 })`.
    - `const prev = useQuery({ queryKey:['fire-alarm-year', y-1], queryFn: async()=>{ try { return await fireAlarmApi.getByYear(y-1) } catch { return [] as any[] } }, enabled: needPrev, staleTime:30_000 })` (enabled 로 훅 순서 고정 — 조건부 호출 아님).
    - `const manual = [...(cur.data ?? []), ...(needPrev ? (prev.data ?? []) : [])]`.
    - `const cutoff = kstStr(new Date(Date.now() - windowHours*3600_000))`.
    - `return mergePanelEvents(autoEvents, manual).filter(e => e.time >= cutoff)`.

【1b. 공용 행 컴포넌트 — src/components/PanelEventRow.tsx (신규)】 시안 001-B 아이템 1행. 카드 ×2 와 이력페이지가 공용(드리프트 0).
- `import { KIND_BADGE, type PanelEventItem } from '../utils/panelEvents'`.
- `export function PanelEventRow({ item }: { item: PanelEventItem })` 렌더:
    - 컨테이너: `<div className="flex items-start gap-[9px] px-3 py-[9px] border-b border-border-default last:border-b-0">`.
    - 좌측 세로 스택(lc): `<div className="flex flex-col gap-1 items-start shrink-0 mt-0.5">` 안에 종류칩 `<span className={`rounded-[6px] px-[7px] py-0.5 text-[10.5px] font-extrabold leading-none ${KIND_BADGE[item.kind].cls}`}>{KIND_BADGE[item.kind].label}</span>` + 출처칩: item.source==='auto' ? `<span className="text-[10.5px] font-bold text-info bg-info-bg rounded-sm px-1.5 py-0.5 leading-none">자동감지</span>` : `<span className="text-[10.5px] font-bold text-text-tertiary bg-surface-sunken rounded-sm px-1.5 py-0.5 leading-none">수동</span>`.
    - 우측(rt): `<div className="min-w-0 flex-1">` — line1 `<div className="flex items-center gap-1.5 flex-wrap"><span className="font-mono text-[11.5px] text-text-tertiary tabular-nums">{item.time}</span><span className="text-[13px] font-semibold text-text-primary">{item.location ?? '위치 미상'}</span></div>` + `{item.cause && <div className="text-caption text-text-tertiary mt-0.5">{item.cause}</div>}`.

【1c. InspectionPage.tsx 카드 ×2 개조】 두 카드 동일 처리. import 추가: `import { PanelEventRow } from '../components/PanelEventRow'`, `import { useRecentPanelEvents } from '../utils/panelEvents'`.

  (i) FireAlarmModal (컴포넌트 4104~) — 모바일 전체화면 모달 카드(현재 4331~4361):
    - `const navigate = useNavigate()` 추가 (이 컴포넌트엔 아직 없음; useNavigate 는 이미 파일 import 됨).
    - 기존 `events`(getEvents(48), 4123~) 쿼리는 **그대로 유지**(‘감지중 N’ 카운트가 status 필요). 그 아래에 `const mergedEvents = useRecentPanelEvents(events)` 추가.
    - 카드 헤더 타이틀 '최근 이벤트 (자동감지)' → '최근 이벤트 (최근 48시간)' (4334).
    - 우상단: `mode === 'alarm'` 분기의 '감지중 N' 칩(4336)은 **유지**. else 분기의 '최근 48시간' 칩(4338) → '전체 이력' 버튼으로 교체: `<button onClick={() => navigate('/fire-alarm-history')} className="inline-flex items-center gap-[3px] rounded-pill text-[11px] font-bold text-text-secondary bg-surface-sunken border border-border-default pl-2.5 pr-2 py-1 leading-none cursor-pointer">전체 이력<ChevronRight size={13} /></button>` (ChevronRight 이미 import 됨).
    - 리스트 본문: `events.length === 0` → `mergedEvents.length === 0` (빈 문구 '최근 48시간 이벤트 없음' 로 소폭 수정), 그리고 `events.map(ev => {...badge2...})` 전체(4344~4359)를 `mergedEvents.map(ev => <PanelEventRow key={ev.id} item={ev} />)` 로 교체. 기존 인라인 자동감지칩(4355)·badge2 참조는 컴포넌트로 흡수돼 제거됨. (badge2 함수 4235 는 다른 참조 없으면 삭제 가능.)
    - handleSave 저장 성공 분기(4196~4200)에 `qc.invalidateQueries({ queryKey: ['fire-alarm-year'] })` 한 줄 추가(수동 신규저장 즉시 반영). resolve/create 양쪽 모두.

  (ii) DesktopInspectionView (컴포넌트 4787~) — 데스크톱 pane 카드(현재 5265~5300):
    - 이 컴포넌트엔 `navigate`(4803) 이미 있음. 기존 `panelEvents`(getEvents(48), 4823~) 쿼리 유지 + `const mergedPanelEvents = useRecentPanelEvents(panelEvents)` 추가.
    - 카드 헤더 타이틀 '최근 이벤트 (자동감지)' → '최근 이벤트 (최근 48시간)' (5268).
    - 우상단: 'alarm' 분기 '감지중 N'(5270) 유지. else '최근 48시간' 칩(5272) → 위 (i)와 동일한 '전체 이력' 버튼(navigate('/fire-alarm-history')).
    - 리스트: `panelEvents.length === 0` → `mergedPanelEvents.length === 0`(빈 문구 '최근 48시간 이벤트 없음'), `panelEvents.map(...)`(5278~5299, 84×48 ethumb 썸네일 포함 구버전)을 `mergedPanelEvents.map(ev => <PanelEventRow key={ev.id} item={ev} />)` 로 교체. **주의:** 데스크톱 구버전의 ethumb 썸네일은 승인 시안 001-B(썸네일 없음)로 통일하며 의도적으로 제거 — 수동기록엔 스냅샷이 없어 혼합 표기가 불가하고 시안이 SSOT. panelBadge2(4944) 참조 없어지면 삭제 가능.
    - handlePanelSave 저장 성공 분기(4898~4902)에 `qc.invalidateQueries({ queryKey: ['fire-alarm-year'] })` 추가(resolve/create 양쪽).

주의: functions/ 는 어떤 파일도 열지/수정하지 말 것. 720h 캡·엔드포인트 스펙 변경 금지.
  </action>
  <verify>
    <automated>cd cha-bio-safety && npx tsc --noEmit</automated>
    <automated>cd cha-bio-safety && grep -c "panelEvents\|fire-alarm-history\|useRecentPanelEvents\|PanelEventRow" src/pages/InspectionPage.tsx | grep -qv '^0$' && echo OK</automated>
    <automated>cd cha-bio-safety && grep -rL . functions/ >/dev/null 2>&1; git -C .. diff --name-only -- cha-bio-safety/functions | grep -q . && echo "FAIL: functions changed" || echo "OK: functions untouched"</automated>
  </verify>
  <done>
    - src/utils/panelEvents.ts + src/components/PanelEventRow.tsx 존재, PanelEventItem/mergePanelEvents/KIND_BADGE/useRecentPanelEvents/PanelEventRow export.
    - InspectionPage 카드 2곳 모두 타이틀='최근 이벤트 (최근 48시간)', 비-경보 우상단='전체 이력' 버튼(navigate('/fire-alarm-history')), 경보 우상단='감지중 N' 유지, 리스트=mergedEvents/mergedPanelEvents 를 PanelEventRow 로 렌더.
    - `npx tsc --noEmit` 통과. functions/ diff 0.
  </done>
</task>

<task type="auto">
  <name>Task 2: 화재수신반 이력 페이지 신설 + 라우트 등록</name>
  <files>cha-bio-safety/src/pages/FireAlarmHistoryPage.tsx (신규), cha-bio-safety/src/App.tsx (수정)</files>
  <action>
【2a. src/pages/FireAlarmHistoryPage.tsx (신규)】 승인 시안 002-A. 병합 헬퍼/행 컴포넌트 재사용.
- import: `useState`(react), `useNavigate`(react-router-dom), `useQuery`(@tanstack/react-query), `ChevronLeft, ChevronRight, BellRing`(lucide-react), `alarmApi, fireAlarmApi, type Alarm`(../utils/api), `mergePanelEvents`(../utils/panelEvents), `PanelEventRow`(../components/PanelEventRow). `export default function FireAlarmHistoryPage()`.
- 월 상태: KST 현재연/월 초기값. Intl 로 KST 'YYYY-MM' 구해 `const [ym, setYm] = useState(()=>({ year, month }))`(month 1~12). prevMonth/nextMonth 는 12↔1 롤오버로 year 도 증감. 표시: `{ym.year}년 {ym.month}월`.
- 필터 상태: `const [kindF, setKindF] = useState<'all'|'fire'|'equip'|'fault'>('all')`, `const [srcF, setSrcF] = useState<'all'|'auto'|'manual'>('all')`.
- 데이터: 자동 = `useQuery({ queryKey:['alarm-events-720'], queryFn: async()=>{ try { return await alarmApi.getEvents(720) } catch { return [] as Alarm[] } }, staleTime:30_000 })` (720h=30일, 감시 6월 시작이라 현재 자동 전량 커버). 수동 = `useQuery({ queryKey:['fire-alarm-year', ym.year], queryFn: async()=>{ try { return await fireAlarmApi.getByYear(ym.year) } catch { return [] as any[] } }, staleTime:30_000 })` (Task1 훅과 동일 키 → 캐시 공유).
- 병합·필터: `const merged = mergePanelEvents(auto ?? [], manual ?? [])` → 월 필터 `e.time.slice(0,7) === `${ym.year}-${String(ym.month).padStart(2,'0')}`` → 종류 필터 `kindF==='all' || e.kind===kindF` → 출처 필터 `srcF==='all' || e.source===srcF`. 이미 시간 내림차순.
- 날짜 그룹: KST 오늘/어제 'YYYY-MM-DD' 계산. filtered 를 순회하며 `e.time.slice(0,10)` 연속 동일키로 그룹핑. 그룹 라벨 = today면 `오늘 · ${MM-DD}` / yesterday면 `어제 · ${MM-DD}` / else `${MM-DD}` (MM-DD = e.time.slice(5,10)).
- 마크업(전부 인라인 Tailwind, 시안 002-A CSS 대응):
    - 루트: `<div className="flex flex-col h-full bg-surface-page">`.
    - 헤더(gh, shrink-0): `<div className="flex items-center h-12 px-3 bg-surface-page border-b border-border-default shrink-0">` — 백버튼 `<button onClick={()=>navigate(-1)} className="w-7 h-7 flex items-center justify-center rounded-[7px] bg-surface-sunken text-text-secondary shrink-0"><ChevronLeft size={18} /></button>` + 타이틀 `<div className="flex-1 flex items-center gap-[7px] pl-2.5 text-title font-semibold text-text-primary min-w-0"><BellRing size={18} className="text-text-secondary shrink-0" /><span className="truncate">화재수신반 이력</span></div>`.
    - 필터존(fz, shrink-0): `<div className="shrink-0 flex flex-col gap-[9px] px-3 py-2.5 border-b border-border-default bg-surface-page">`.
        · 종류행: `<div className="flex items-center gap-1.5 flex-wrap"><span className="text-[11px] text-text-tertiary w-[30px] shrink-0">종류</span>{세그먼트: 전체/화재/설비/고장 → kindF}</div>`.
        · 출처행: `<div className="flex items-center gap-1.5 flex-wrap"><span className="text-[11px] text-text-tertiary w-[30px] shrink-0">출처</span>{세그먼트: 전체/자동/수동 → srcF}<span className="flex-1" />{월 스테퍼}</div>`.
    - 세그먼트(seg) 컨트롤 — 재사용 위해 파일 내부 소형 컴포넌트/헬퍼로: 컨테이너 `<div className="inline-flex bg-surface-sunken border border-border-default rounded-pill p-0.5">`, 각 옵션 `<button onClick={...} className={`text-[12px] font-bold px-[11px] py-1 rounded-pill ${on ? 'bg-surface-active text-text-primary' : 'text-text-tertiary'}`}>{라벨}</button>`.
    - 월 스테퍼(per): `<div className="inline-flex items-center gap-1"><button onClick={prevMonth} className="w-[26px] h-[26px] rounded-[7px] bg-surface-sunken border border-border-default text-text-secondary flex items-center justify-center"><ChevronLeft size={14} /></button><span className="text-[13px] font-bold text-text-primary min-w-[88px] text-center tabular-nums">{ym.year}년 {ym.month}월</span><button onClick={nextMonth} className="...동일... "><ChevronRight size={14} /></button></div>`.
    - 리스트(scroll): `<div className="flex-1 overflow-y-auto">` 안에 그룹 순회 — 날짜구분 `<div className="px-3 pt-3 pb-[5px] text-[11px] font-bold text-text-tertiary tracking-wide">{그룹라벨}</div>` + 각 행 `<PanelEventRow key={item.id} item={item} />`. filtered.length===0 이면 `<div className="px-3 py-10 text-caption text-text-tertiary text-center">해당 조건의 이력이 없습니다</div>`.
- '더 보기'는 저빈도 데이터라 미구현(단순 스크롤). 페이지네이션/무한스크롤 없음.

【2b. src/App.tsx 라우트 등록】
- lazy import 추가(다른 lazy 선언부, remediation 근처): `const FireAlarmHistoryPage = lazy(() => import('./pages/FireAlarmHistoryPage'))`.
- `<Routes>` 안, `/remediation` 라우트 인접에 `<Route path="/fire-alarm-history" element={<Auth><FireAlarmHistoryPage /></Auth>} />` 추가.
- 자체 헤더 페이지이므로 nav 처리: `MOBILE_NO_NAV_PATHS` 배열(73줄)에 `'/fire-alarm-history'` 추가(모바일 GlobalHeader+BottomNav 숨김). `DESKTOP_HEADER_HIDE_PATHS`(79줄)에 `'/fire-alarm-history'` 추가(데스크톱 중복 타이틀 헤더만 숨기고 사이드바는 유지 — /elevator·/div·/floorplan·/workshift 패턴). `DESKTOP_NO_NAV_PATHS` 에는 넣지 말 것(사이드바 유지).
- `PAGE_TITLES` 는 자체 헤더라 불필요(추가 안 함).
  </action>
  <verify>
    <automated>cd cha-bio-safety && npx tsc --noEmit</automated>
    <automated>cd cha-bio-safety && grep -q "fire-alarm-history" src/App.tsx && grep -q "FireAlarmHistoryPage" src/App.tsx && echo OK</automated>
    <automated>cd cha-bio-safety && grep -q "mergePanelEvents" src/pages/FireAlarmHistoryPage.tsx && grep -q "PanelEventRow" src/pages/FireAlarmHistoryPage.tsx && echo OK</automated>
    <automated>test -z "$(git -C /Users/jongyupyoon/Documents/20260328 diff --name-only -- cha-bio-safety/functions)" && echo "OK: functions untouched"</automated>
  </verify>
  <done>
    - /fire-alarm-history 라우트가 Auth 래핑으로 등록되고 lazy FireAlarmHistoryPage 로 연결.
    - 페이지가 종류(전체/화재/설비/고장)·출처(전체/자동/수동) 세그먼트 + 월 스테퍼 + 날짜그룹 리스트를 렌더하고 mergePanelEvents/PanelEventRow 재사용.
    - MOBILE_NO_NAV_PATHS·DESKTOP_HEADER_HIDE_PATHS 에 경로 추가.
    - `npx tsc --noEmit` 통과, functions/ diff 0.
  </done>
</task>

</tasks>

<threat_model>
프론트엔드 전용 변경. 새 trust boundary·새 엔드포인트·새 의존성 없음. 소비하는 API(alarmApi.getEvents, fireAlarmApi.getByYear)는 기존 _middleware JWT 인증 경로. 새 사용자 입력 수집 없음(읽기·필터·정렬만). 신규 STRIDE 노출 없음 → 별도 완화 불필요. (functions/ 무변경이 이 무노출을 보증 — verify 의 functions diff 게이트로 강제.)
</threat_model>

<verification>
- `cd cha-bio-safety && npx tsc --noEmit` 무오류.
- functions/ 디렉토리 git diff 0 (백엔드 무변경 게이트).
- 신규 파일 3개 존재: src/utils/panelEvents.ts, src/components/PanelEventRow.tsx, src/pages/FireAlarmHistoryPage.tsx.
- InspectionPage 카드 2곳 타이틀/버튼/리스트 교체 완료, 시안 001-B 세로 칩 스택 반영.
- 헤드리스 렌더 육안 확인은 오케스트레이터가 별도 수행(이 플랜 범위 밖).
</verification>

<success_criteria>
- 병합 헬퍼가 자동(panel_alarms)+수동(fire_alarm_records 중 created_by!=='panel-agent')을 정규화·시간 내림차순 병합.
- 카드 ×2 가 최근 48시간 병합 뷰 + 001-B 아이템 + '전체 이력' 버튼(비-경보) + '감지중 N'(경보) 유지.
- 신설 /fire-alarm-history 가 002-A 필터/월스텝/날짜그룹으로 병합 이력 조회.
- 백엔드 무변경, 새 의존성 없음, typecheck 통과, 커밋까지(배포 X).
</success_criteria>

<output>
Create `.planning/quick/260704-0xr-panel-events-merge-history/260704-0xr-SUMMARY.md` when done
</output>
