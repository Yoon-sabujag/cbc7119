---
phase: quick-260520-h4z
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cha-bio-safety/docs/redesign-context/14-reports/sketch-wave-5-desktop-sidelist.html
autonomous: true
requirements:
  - QUICK-260520-h4z
must_haves:
  truths:
    - "데스크톱 1280px frame 안에서 좌측 sidelist(260px)에 4 섹션(DESKTOP_SECTIONS verbatim)과 10 카드(REPORT_CARDS verbatim) 가 모두 노출된다."
    - "각 row 의 selected 상태 (좌측 3px accent 색바 + bg-surface-sunken + title text-accent font-bold) 가 시각적으로 분명히 식별된다."
    - "F1/F3 에서 selected 가 아닌 row 중 1개에 hover demo state (bg-surface-sunken + border-left transparent) 가 시연된다."
    - "다크 (F1/F2) / 라이트 (F3/F4) 양쪽 모두 동일 4 섹션 × 10 카드 구조로 미러링된다."
    - "F2/F4 는 다른 섹션 (C 연간 점검일지 - 자탐) 의 selected 상태를 시연한다 (섹션 cross visual)."
    - "상단 toolbar 영역 + 우측 ExcelPreview 영역은 dashed placeholder 로만 표기되어 본 wave 가 sidelist scope 임이 명확하다."
    - "섹션 B 라벨 '소화전 · 가스 · 비상콘센트' 의 가운뎃점은 dot span 으로 변환되어 본문 텍스트 가운뎃점 0건이다."
    - "본문 fontSize 모두 12px 이상 (9·10·11px 0건) — 노안 룰 §1.1 마이그레이션 룰 §4.2 준수."
    - "이모지 0건 / lucide 아이콘 사용 0건 (sidelist 영역 본디 아이콘 없음, 단 .dot-meta span 만 사용)."
    - "linear-gradient 0건 / w-8 h-8 utility class 0건 / status- prefix Tailwind 0건."
  artifacts:
    - path: "cha-bio-safety/docs/redesign-context/14-reports/sketch-wave-5-desktop-sidelist.html"
      provides: "Wave 5 sketch — 데스크톱 좌측 sidelist (1280px × 4 frame matrix)"
      contains: "4 frame × 4 섹션 × 10 row + dashed placeholder (W4 toolbar / W6 preview) + Notes block + W7 변환 wave 용 CSS class verbatim 인덱스"
  key_links:
    - from: "sketch-wave-5-desktop-sidelist.html"
      to: "ReportsPage.tsx line 142~147 (DESKTOP_SECTIONS) + line 249~295 (좌측 패널)"
      via: "verbatim text 매핑 (섹션 label / row title / row sub)"
      pattern: "월초 유수검지 장치 점검표|월말 유수검지 장치 점검표|월간 옥내소화전 점검일지|청정소화약제설비 점검일지|월간 비상콘센트 점검일지|월간 피난방화시설 점검일지|월간 방화셔터 점검일지|월간 제연설비 점검일지|자동화재탐지설비 점검일지|월간 소방펌프 점검일지"
    - from: "sketch-wave-5-desktop-sidelist.html"
      to: "sketch-wave-4-desktop-toolbar.html (frame outer + tokens.css + Spacing/Radius/Typography 인용)"
      via: "frame-desktop 1280px + tailwind CDN + tokens.css verbatim 패턴 mirror"
      pattern: "frame-desktop|placeholder-sidelist|placeholder-preview|page-bg-dark|page-bg-light"
    - from: "sketch-wave-5-desktop-sidelist.html"
      to: "sketch-wave-3-mobile-card-list.html (.dot-meta span 패턴 mirror)"
      via: ".dot-meta CSS verbatim 인용"
      pattern: "\\.dot-meta\\b"
---

<objective>
redesign/14-reports sketch wave 5 — 데스크톱 좌측 sidelist 260px × 4 섹션 × 10 카드 시안.

Purpose: W1 LOCKED 5건 + W4 frame 셋업 mirror 위에서 데스크톱 좌측 sidelist 의 selected / hover / default row state 를 다크/라이트 양 테마로 분명히 시각화한다. W6 (ExcelPreview wrapper) 진입 전 사용자 컨펌 받을 마지막 sidelist 시안.

Output: `cha-bio-safety/docs/redesign-context/14-reports/sketch-wave-5-desktop-sidelist.html` 1개 파일 (atomic 1-commit).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.local.md
@cha-bio-safety/docs/redesign-context/14-reports/wave-1-index.md
@cha-bio-safety/docs/redesign-context/14-reports/sketch-wave-4-desktop-toolbar.html
@cha-bio-safety/docs/redesign-context/14-reports/sketch-wave-3-mobile-card-list.html
@cha-bio-safety/docs/redesign-context/14-reports/sketch-wave-2-mobile-header-card.html
@cha-bio-safety/docs/redesign-context/14-reports/tokens.css
@cha-bio-safety/src/pages/ReportsPage.tsx

<interfaces>
<!-- W7 변환 wave executor 가 sidelist 영역을 ReportsPage.tsx 좌측 패널에 1:1 매핑할 때 참조할 핵심 verbatim. -->
<!-- sketch 작성자(본 wave) 도 이 인터페이스를 그대로 dot span / row markup / 섹션 헤더에 박제. -->

## DESKTOP_SECTIONS verbatim (ReportsPage.tsx line 142~147)

```tsx
const DESKTOP_SECTIONS = [
  { label: '유수검지 장치', types: ['div-early', 'div-late'] as ReportType[] },
  { label: '소화전 · 가스 · 비상콘센트', types: ['소화전', '청정소화약제', '비상콘센트'] as ReportType[] },
  { label: '연간 점검일지', types: ['피난방화', '방화셔터', '제연', '자탐'] as ReportType[] },
  { label: '소방펌프', types: ['소방펌프'] as ReportType[] },
]
```

## REPORT_CARDS verbatim (line 12~23) — 10 entry, type/title/sub 모두 보존

| # | type | title | sub |
|---|---|---|---|
| 1 | div-early | 월초 유수검지 장치 점검표 | DIV · 34개소 |
| 2 | div-late | 월말 유수검지 장치 점검표 | DIV · 34개소 |
| 3 | 소화전 | 월간 옥내소화전 점검일지 | 소화전 · 각 층 |
| 4 | 청정소화약제 | 청정소화약제설비 점검일지 | 가스소화 · 3개소 |
| 5 | 비상콘센트 | 월간 비상콘센트 점검일지 | 비상콘센트 · 8개소 |
| 6 | 피난방화 | 월간 피난방화시설 점검일지 | 피난방화 · 연간 |
| 7 | 방화셔터 | 월간 방화셔터 점검일지 | 방화셔터 · 연간 |
| 8 | 제연 | 월간 제연설비 점검일지 | 제연설비 · 연간 |
| 9 | 자탐 | 자동화재탐지설비 점검일지 | 자탐설비 · 연간 |
| 10 | 소방펌프 | 월간 소방펌프 점검일지 | 소방펌프 · 월간 |

> 모든 sub 라인의 ` · ` 가운뎃점은 `<span class="dot-meta"></span>` 으로 변환 (W3 sketch-wave-3 line 288~296 mirror). 본문 텍스트 가운뎃점 0건 강제.

## ReportsPage.tsx 좌측 패널 verbatim (line 249~291)

```tsx
{/* 좌측 항목 목록 */}
<div style={{
  width: 260,
  flexShrink: 0,
  borderRight: '1px solid var(--bd)',
  overflowY: 'auto',
  background: 'var(--bg2)',
}}>
  {DESKTOP_SECTIONS.map(section => (
    <div key={section.label}>
      <div style={{
        padding: '8px 16px 4px',
        fontSize: 11,        /* sketch 변환 시 12 로 상향 (마이그레이션 룰 §4.2) */
        fontWeight: 700,
        color: 'var(--t3)',
        letterSpacing: '0.05em',
      }}>
        {section.label}
      </div>
      {section.types.map(type => {
        const card = REPORT_CARDS.find(c => c.type === type)
        if (!card) return null
        const isSelected = selectedType === type
        const isHover = hoverType === type && !isSelected
        return (
          <div
            key={type}
            onClick={() => setSelectedType(type)}
            onMouseEnter={() => setHoverType(type)}
            onMouseLeave={() => setHoverType(null)}
            style={{
              padding: '8px 16px',
              cursor: 'pointer',
              background: isSelected ? 'var(--bg3)' : isHover ? 'var(--bg3)' : 'transparent',
              borderLeft: isSelected ? '3px solid var(--acl)' : '3px solid transparent',
            }}
          >
            <div style={{ fontSize: 13, color: isSelected ? 'var(--acl)' : 'var(--t1)', fontWeight: isSelected ? 700 : 400 }}>
              {card.title}
            </div>
            <div style={{ fontSize: 11, color: 'var(--t3)', marginTop: 1 }}>{card.sub}</div>
          </div>
        )
      })}
    </div>
  ))}
</div>
```

> 토큰 매핑: `var(--bd) → var(--border-default)`, `var(--bg2) → var(--surface-raised)`, `var(--bg3) → var(--surface-sunken)`, `var(--acl) → var(--accent)`, `var(--t1) → var(--text-primary)`, `var(--t3) → var(--text-tertiary)`.

## sketch CSS class 정의 (W7 변환 wave 가 verbatim 인용할 인덱스)

```css
/* 좌측 패널 outer */
.sidelist {
  width: 260px;
  flex-shrink: 0;
  border-right: 1px solid var(--border-default);
  overflow-y: auto;
  background: var(--surface-raised);
}

/* 섹션 헤더 — 11 → 12 노안 격상 */
.sidelist-section-header {
  padding: 8px 16px 4px;
  font-size: 12px;
  font-weight: 700;
  color: var(--text-tertiary);
  letter-spacing: 0.05em;
  line-height: 1;           /* 메모리 룰 7 (feedback_text_caption_leading_none) */
}

/* row default */
.sidelist-row {
  padding: 8px 16px;
  cursor: pointer;
  background: transparent;
  border-left: 3px solid transparent;
}

/* row selected — 좌측 3px accent + bg-surface-sunken */
.sidelist-row--selected {
  background: var(--surface-sunken);
  border-left: 3px solid var(--accent);
}

/* row hover (selected 가 아닐 때) — source line 281 'isHover ? var(--bg3)' 매핑 */
.sidelist-row--hover {
  background: var(--surface-sunken);
  border-left: 3px solid transparent;
}

/* row 제목 — 13 → 14 (text-body-sm, 데스크톱 §1.3 정보 밀도 유지) */
.sidelist-row-title {
  font-size: 14px;
  color: var(--text-primary);
  font-weight: 400;
}

/* row 제목 selected variant */
.sidelist-row-title--selected {
  color: var(--accent);
  font-weight: 700;
}

/* row sub — 11 → 12 노안 격상 */
.sidelist-row-sub {
  font-size: 12px;
  color: var(--text-tertiary);
  margin-top: 1px;
}

/* dot span — W3 sketch-wave-3 line 288~296 verbatim mirror */
.dot-meta {
  display: inline-block;
  width: 4px;
  height: 4px;
  border-radius: 9999px;
  background: var(--text-tertiary);
  flex-shrink: 0;
  vertical-align: middle;
  margin: 0 6px;             /* 가운뎃점 전후 spacing */
}
```
</interfaces>

</context>

<tasks>

<task type="auto">
  <name>Task 1: sketch-wave-5-desktop-sidelist.html — 데스크톱 좌측 sidelist 1280px × 4 frame matrix 작성</name>
  <files>cha-bio-safety/docs/redesign-context/14-reports/sketch-wave-5-desktop-sidelist.html</files>
  <action>
W4 (sketch-wave-4-desktop-toolbar.html) 의 frame 셋업을 mirror 하여 sketch-wave-5-desktop-sidelist.html 1개 파일을 신규 작성한다. 본 wave 산출은 평면(flat sibling) 배치 — `14-reports/sketch-wave-5-*.html` (sketch/ 서브폴더 0).

## 작성 구조 (단일 HTML)

### A. <head> — W4 frame 셋업 verbatim 인용

W4 의 다음 영역을 1:1 복사:
- Pretendard Variable 폰트 CDN (line 84)
- Tailwind CDN (line 85)
- `<style>` 안 다크/라이트 토큰 정의 (line 87~192, tokens.css verbatim)
- Spacing tokens (line 197~232)
- Radius tokens (line 237~242)
- Typography (line 247~296)
- .leading-none helper (line 299)
- .page-bg-dark / .page-bg-light helper (line 304~305)
- .frame-desktop outer (line 308~318)
- .global-header-placeholder (line 321~332)
- .variant-label (line 474~481)
- .frame-stack (line 484~491)
- `<script>` tailwind.config (line 493~535)

> 이 영역 전체를 W4 에서 그대로 복사하되, frame 구조만 W5 sidelist 시연으로 갈아끼운다. W4 가 이미 tokens.css verbatim 인용한 상태이므로 본 wave 가 별도로 tokens.css 다시 읽을 필요 없음.

### B. <head> — W5 신규 sidelist CSS class 정의

위 W4 셋업 안 `<style>` 영역 끝에 본 plan 의 `<interfaces>` 블록 안 CSS class 정의를 그대로 박제:
- `.sidelist`, `.sidelist-section-header`, `.sidelist-row`, `.sidelist-row--selected`, `.sidelist-row--hover`
- `.sidelist-row-title`, `.sidelist-row-title--selected`, `.sidelist-row-sub`
- `.dot-meta` (W3 sketch-wave-3 line 288~296 verbatim 인용, 단 vertical-align/margin 추가)
- 추가로 placeholder 영역용:
  ```css
  /* 상단 toolbar placeholder — W4 책임 (본 wave dashed bar 1줄) */
  .placeholder-toolbar {
    height: 48px;
    border-bottom: 1px dashed var(--border-default);
    background: var(--surface-raised);
    display: flex;
    align-items: center;
    justify-content: center;
    color: var(--text-tertiary);
    font-family: 'JetBrains Mono', monospace;
    font-size: 12px;
    letter-spacing: 0.05em;
    line-height: 1;
  }
  /* below-toolbar = sidelist + preview 양분할 */
  .below-toolbar {
    display: flex;
    height: 520px;          /* 시연용 — 10 row + 4 섹션 헤더 자연 노출 */
  }
  /* 우측 ExcelPreview wrapper placeholder — W6 책임 */
  .placeholder-preview {
    flex: 1;
    background: var(--surface-page);
    border: 1px dashed var(--border-default);
    margin: 16px;
    display: flex;
    align-items: center;
    justify-content: center;
    color: var(--text-tertiary);
    font-family: 'JetBrains Mono', monospace;
    font-size: 12px;
    letter-spacing: 0.05em;
    line-height: 1.6;
    text-align: center;
  }
  /* hover 시연 chip — F1/F3 에서 ' (HOVER demo) ' 라벨 표기 */
  .hover-demo-chip {
    display: inline-block;
    margin-left: 6px;
    padding: 2px 6px;
    border-radius: 4px;
    background: var(--surface-active);
    color: var(--text-secondary);
    font-size: 12px;
    line-height: 1;
    font-family: 'JetBrains Mono', monospace;
    letter-spacing: 0.05em;
  }
  ```

### C. 상단 헤더 주석 (HTML <!-- --> 블록)

W4 헤더 주석 패턴 mirror. 다음 내용을 포함:
1. 파일명 + 작성일 (2026-05-20) + Quick ID 260520-h4z + 범위 (데스크톱 1280px 좌측 sidelist 260px × 4 섹션 × 10 카드)
2. out of scope: 상단 toolbar 본격 (W4) / ExcelPreview 본체 (W6) / 모바일 frame (W2/W3) / ReportsPage.tsx 코드 수정 / App.tsx / 다른 페이지 / wrangler / npm run deploy
3. 참조: wave-1-index.md §3 + §7 OQ LOCKED 5건 verbatim / W4 frame 셋업 mirror / W3 dot-meta CSS verbatim mirror / ReportsPage.tsx line 142~147 + 249~295 verbatim
4. W1 LOCKED 5건 적용 매트릭스 (W5 관점):
   - OQ #1 LOCKED — 모바일 그라데이션 폐기 (W2 적용, W5 미해당)
   - OQ #2 LOCKED — 좌측 패널 260px 유지 → 본 wave 의 핵심. .sidelist width: 260px verbatim.
   - OQ #3 LOCKED — 데스크톱 일괄 그라데이션 폐기 (W4 적용, W5 미해당)
   - OQ #4 LOCKED — 모바일 footer 유지 (W3 적용, W5 미해당)
   - OQ #5 LOCKED — sub 라인 dot span 패턴 → 본 wave 적용 = O. 섹션 B 라벨 + 10 row sub 모두 dot span 변환.
5. W5 → W6 진입 전 OQ 3건 default (task_scope 인용):
   - OQ #1 default: ExcelPreview 영역 = 단순 미리보기 PNG 자리 (그리드 오버레이는 ExcelPreview.tsx 본체 책임)
   - OQ #2 default: 미리보기 PDF/이미지 톤 = A4 흰배경 고정 (다크/라이트 자동 전환 X) — reference_floorplan_png_algorithm + 12-staff-service W8 결정 inherit
   - OQ #3 default: 빈 상태 UI 불필요 (sidelist 항상 default selected, useState<ReportType>('div-early') 초기값)
6. 메모리 룰 박제 ≥7건 unique (W4 의 10건 mirror + W5 특화):
   - feedback_design_sketch_first.md — sketch 먼저 컨펌 룰. 본 W5 가 W6 진입 전 마지막 sidelist 컨펌.
   - feedback_sketch_realistic_data.md — REPORT_CARDS 10 entry / DESKTOP_SECTIONS 4 entry 라벨 verbatim. 카피 변경 0.
   - feedback_planner_prompt_sketch_verbatim.md — 본 wave 의 .sidelist-* CSS class 전체를 W7 checklist 에 verbatim 인용 예정.
   - feedback_tailwind_token_class_pattern.md — `bg-surface-raised` / `bg-surface-sunken` / `border-l-[3px]` / `text-accent` 패턴 사용. status- prefix 0건. utility w-8/h-8 0건 (인라인 px 값만).
   - feedback_tailwind_w8_h8_is_48px.md — w-8/h-8 utility 0건 강제. 260px / 32px / 4px / 8px / 16px 모두 인라인 명시.
   - feedback_text_caption_leading_none.md — .sidelist-section-header 작은 컨테이너 → line-height: 1 명시.
   - feedback_tsx_wave_emoji_dot_gap.md — 이모지 0건 / 섹션 B 라벨 dot span 변환 / 10 row sub dot span 변환.
   - feedback_avoid_premature_confirmation.md — 작성 완료 후 자체 PASS 판단 0건. 사용자 컨펌 명시 받기.
   - feedback_cbc7119_design_never_wrangler.md — wrangler 0 / npm run deploy 0. main push 자동 cbc7119-preview 만.
   - feedback_tsx_wave_stat_card_drift.md — 14-reports 페이지 Stat Card 미적용 명시. sidelist row 는 카드 아닌 list 항목.

### D. <body class="page-bg-dark min-h-screen p-6">

W4 mirror — 헤더 + frame-stack 안 4 frame.

#### D.1 헤더 (캡션)

```html
<header class="mb-8 max-w-[1280px] mx-auto">
  <h1 class="text-heading text-white mb-2">14-reports sketch W5 — 데스크톱 좌측 sidelist (1280px × 260px)</h1>
  <p class="text-label text-zinc-400">
    데스크톱 1280px × (다크/라이트) × (div-early selected / 자탐 selected) 4 frame matrix.
    W1 LOCKED 5건 중 #2 (좌측 패널 260px 유지) + #5 (sub 라인 dot span) 본 wave 적용.
    ReportsPage.tsx line 142~147 (DESKTOP_SECTIONS) + 249~295 (좌측 패널) verbatim 매핑.
    상단 toolbar = dashed bar placeholder (W4 책임) / 우측 ExcelPreview = dashed placeholder (W6 책임).
  </p>
</header>
```

#### D.2 frame matrix (4 frame)

각 frame 구조:
```html
<div>
  <div class="variant-label" style="color:#f6f8fa;">Frame N / [theme] [state] (1280px 데스크톱)</div>
  <div data-theme="[dark|light]" class="frame-desktop">
    <div class="global-header-placeholder">App GlobalHeader (h-12, /reports 자체 헤더 페이지)</div>
    <div class="placeholder-toolbar">W4 책임 — 상단 toolbar (연도/일괄/spacer/선택 타이틀/개별)</div>
    <div class="below-toolbar">
      <!-- 좌측 sidelist 260px — W5 본격 구현 -->
      <div class="sidelist">
        <!-- 섹션 A: 유수검지 장치 -->
        <div>
          <div class="sidelist-section-header leading-none">유수검지 장치</div>
          <div class="[sidelist-row | sidelist-row sidelist-row--selected | sidelist-row sidelist-row--hover]">
            <div class="sidelist-row-title [sidelist-row-title--selected]">월초 유수검지 장치 점검표</div>
            <div class="sidelist-row-sub">DIV<span class="dot-meta"></span>34개소</div>
          </div>
          <div class="sidelist-row">
            <div class="sidelist-row-title">월말 유수검지 장치 점검표</div>
            <div class="sidelist-row-sub">DIV<span class="dot-meta"></span>34개소</div>
          </div>
        </div>
        <!-- 섹션 B: 소화전 · 가스 · 비상콘센트 — 가운뎃점 dot span 변환 -->
        <div>
          <div class="sidelist-section-header leading-none">
            소화전<span class="dot-meta"></span>가스<span class="dot-meta"></span>비상콘센트
          </div>
          <div class="sidelist-row">
            <div class="sidelist-row-title">월간 옥내소화전 점검일지</div>
            <div class="sidelist-row-sub">소화전<span class="dot-meta"></span>각 층</div>
          </div>
          <div class="sidelist-row">
            <div class="sidelist-row-title">청정소화약제설비 점검일지</div>
            <div class="sidelist-row-sub">가스소화<span class="dot-meta"></span>3개소</div>
          </div>
          <div class="sidelist-row">
            <div class="sidelist-row-title">월간 비상콘센트 점검일지</div>
            <div class="sidelist-row-sub">비상콘센트<span class="dot-meta"></span>8개소</div>
          </div>
        </div>
        <!-- 섹션 C: 연간 점검일지 (4 row) -->
        <div>
          <div class="sidelist-section-header leading-none">연간 점검일지</div>
          <div class="sidelist-row"><div class="sidelist-row-title">월간 피난방화시설 점검일지</div><div class="sidelist-row-sub">피난방화<span class="dot-meta"></span>연간</div></div>
          <div class="sidelist-row"><div class="sidelist-row-title">월간 방화셔터 점검일지</div><div class="sidelist-row-sub">방화셔터<span class="dot-meta"></span>연간</div></div>
          <div class="sidelist-row"><div class="sidelist-row-title">월간 제연설비 점검일지</div><div class="sidelist-row-sub">제연설비<span class="dot-meta"></span>연간</div></div>
          <div class="sidelist-row"><div class="sidelist-row-title">자동화재탐지설비 점검일지</div><div class="sidelist-row-sub">자탐설비<span class="dot-meta"></span>연간</div></div>
        </div>
        <!-- 섹션 D: 소방펌프 -->
        <div>
          <div class="sidelist-section-header leading-none">소방펌프</div>
          <div class="sidelist-row"><div class="sidelist-row-title">월간 소방펌프 점검일지</div><div class="sidelist-row-sub">소방펌프<span class="dot-meta"></span>월간</div></div>
        </div>
      </div>
      <!-- 우측 ExcelPreview placeholder -->
      <div class="placeholder-preview">
        W6 책임<br/>
        ExcelPreview wrapper<br/>
        (reportType=[selected type] year=2026 month=현재)
      </div>
    </div>
  </div>
</div>
```

#### D.3 4 frame variant 차이 (frame 안 동일 sidelist 마크업 복사 후 selected/hover state class 만 분기)

| F | data-theme | selected row | hover row (selected 아닐 때) | placeholder-preview reportType |
|---|---|---|---|---|
| 1 | dark | A 섹션 div-early ("월초 유수검지 장치 점검표") → `.sidelist-row--selected` + title `.sidelist-row-title--selected` | A 섹션 div-late ("월말 유수검지 장치 점검표") → `.sidelist-row--hover` + `<span class="hover-demo-chip">HOVER</span>` 라벨 추가 | div-early |
| 2 | dark | C 섹션 자탐 ("자동화재탐지설비 점검일지") → `.sidelist-row--selected` + title selected | (hover demo 0 — 본 frame 은 다른 섹션 active 시연이 목적) | 자탐 |
| 3 | light | A 섹션 div-early → selected (F1 mirror) | A 섹션 div-late → hover demo (F1 mirror) | div-early |
| 4 | light | C 섹션 자탐 → selected (F2 mirror) | (hover 0) | 자탐 |

> hover-demo-chip 은 F1/F3 에서만 → `.sidelist-row--hover` row 옆에 ` <span class="hover-demo-chip">HOVER</span>` 추가 (sidelist-row-title 내 inline). 사용자가 시각적으로 "이 row 는 hover state" 식별 가능.

#### D.4 Notes block (frame 외부, W4 mirror)

```html
<section class="max-w-[1280px] mx-auto text-zinc-400 text-label" style="line-height:1.6;">
  <h2 class="text-title text-white mb-3">W5 검토 포인트</h2>
  <ul class="list-disc pl-5 space-y-1">
    <li>좌측 sidelist = <code>.sidelist</code> width: 260px (W1 OQ #2 LOCKED). flex-shrink: 0 + border-right + overflow-y: auto + bg-surface-raised.</li>
    <li>섹션 헤더 fontSize: 12 (source 11 → 12 노안 상향). letter-spacing 0.05em + leading-none.</li>
    <li>row 제목 fontSize: 14 (source 13 → 14, text-body-sm). 데스크톱 §1.3 정보 밀도 — 14px 마지노선 채택.</li>
    <li>row sub fontSize: 12 (source 11 → 12 노안 상향). 모든 가운뎃점 → dot-meta span 변환.</li>
    <li>selected row: bg-surface-sunken + border-left 3px var(--accent) + title text-accent font-bold.</li>
    <li>hover row (selected 아닐 때): bg-surface-sunken + border-left 3px transparent (F1/F3 에서 div-late row 에 시연).</li>
    <li>섹션 B 라벨 '소화전 · 가스 · 비상콘센트' 의 가운뎃점 2개 → dot-meta span 2개 변환 (W1 OQ #5 LOCKED).</li>
    <li>이모지 0건 / linear-gradient 0건 / w-8 h-8 utility 0건 / status- prefix Tailwind 0건 / 9·10·11px 본문 0건.</li>
  </ul>

  <h2 class="text-title text-white mb-3 mt-6">W6 진입 전 OQ 3건 (default 답 채택 시 별 의견 없는 것으로 진행)</h2>
  <ul class="list-disc pl-5 space-y-1">
    <li>OQ #1 — ExcelPreview 영역 그리드 오버레이 vs 단순 PNG 자리? <strong class="text-zinc-200">default: 단순 미리보기 PNG 자리</strong> — 내부 그리드 오버레이는 ExcelPreview.tsx 본체 책임. W6 wrapper layout 만.</li>
    <li>OQ #2 — 미리보기 톤 다크/라이트 자동 전환 vs A4 흰배경 고정? <strong class="text-zinc-200">default: A4 흰배경 고정</strong> — 메모리 룰 reference_floorplan_png_algorithm + 12-staff-service W8 결정 inherit.</li>
    <li>OQ #3 — 빈 상태 UI (선택 안 됨)? <strong class="text-zinc-200">default: 빈 상태 표시 불필요</strong> — sidelist 항상 default selected (useState&lt;ReportType&gt;('div-early') 초기값).</li>
  </ul>

  <h2 class="text-title text-white mb-3 mt-6">W7 변환 wave 가 인용할 CSS class verbatim 인덱스</h2>
  <ul class="list-disc pl-5 space-y-1">
    <li><code>.sidelist</code> — width 260, flex-shrink 0, border-right default, overflow-y auto, bg surface-raised</li>
    <li><code>.sidelist-section-header</code> — padding 8/16/4, fontSize 12, fontWeight 700, color text-tertiary, letter-spacing 0.05em, leading 1</li>
    <li><code>.sidelist-row</code> + <code>.sidelist-row--selected</code> + <code>.sidelist-row--hover</code> — padding 8/16, border-left 3px (selected: accent / default+hover: transparent), bg (selected/hover: surface-sunken / default: transparent)</li>
    <li><code>.sidelist-row-title</code> + <code>.sidelist-row-title--selected</code> — fontSize 14, color (selected: accent + fontWeight 700 / default: text-primary fontWeight 400)</li>
    <li><code>.sidelist-row-sub</code> — fontSize 12, color text-tertiary, margin-top 1</li>
    <li><code>.dot-meta</code> — inline-block 4×4, border-radius 9999px, bg text-tertiary, margin 0/6</li>
  </ul>
</section>
```

## 최종 verify (sketch 작성자 자체 grep)

```bash
F=cha-bio-safety/docs/redesign-context/14-reports/sketch-wave-5-desktop-sidelist.html
# Negative
grep -cE '⬇|📄|✅|❌|⚠️' $F                         # =0
grep -c 'linear-gradient' $F                         # =0
grep -cE 'text-status-|bg-status-' $F                # =0
# 본문 ' · ' 가운뎃점 (HTML 주석 안 인용 제외)
grep -v '<!--\\|^[[:space:]]*//' $F | grep -cE ' · ' # =0 (frame 안)
grep -cE 'class="[^"]*\\bw-8\\b|class="[^"]*\\bh-8\\b' $F  # =0
grep -cE 'font-size: ?(9|10|11)px' $F                # =0 (본문 11px 모두 12 로 상향)
# Positive
grep -c '\\.sidelist *{' $F                          # ≥1
grep -c 'sidelist-row--selected' $F                  # ≥4 (CSS 정의 1 + frame 안 사용 ≥4 = ≥5)
grep -c 'sidelist-row--hover' $F                     # ≥2 (F1/F3 div-late hover demo)
grep -c '유수검지 장치' $F                            # ≥4 (4 frame 섹션 헤더)
grep -c '연간 점검일지' $F                            # ≥4
grep -c '소방펌프' $F                                # ≥8 (섹션 헤더 4 + row 4 = ≥8)
grep -c '월초 유수검지 장치 점검표' $F                 # ≥4 (4 frame 안 row 마다)
grep -c '월말 유수검지 장치 점검표' $F                 # ≥4
grep -c '월간 옥내소화전 점검일지' $F                  # ≥4
grep -c '청정소화약제설비 점검일지' $F                 # ≥4
grep -c '월간 비상콘센트 점검일지' $F                  # ≥4
grep -c '월간 피난방화시설 점검일지' $F                # ≥4
grep -c '월간 방화셔터 점검일지' $F                    # ≥4
grep -c '월간 제연설비 점검일지' $F                    # ≥4
grep -c '자동화재탐지설비 점검일지' $F                 # ≥4
grep -c '월간 소방펌프 점검일지' $F                    # ≥4
grep -c 'class="dot-meta"' $F                        # ≥40 (10 row × 2 dot × 4 frame? — 단 row sub dot 1개씩 + 섹션 B 2개)
                                                     # 실제: row sub dot ≈ 10 row × 1 dot × 4 frame = 40
                                                     #      + 섹션 B 라벨 dot × 2 × 4 frame = 8 → 총 ≥48
grep -oE 'feedback_[a-z_0-9]+\\.md' $F | sort -u | wc -l  # ≥7
grep -c 'W6 책임' $F                                 # ≥4 (placeholder-preview frame 마다)
grep -c 'W4 책임' $F                                 # ≥4 (placeholder-toolbar frame 마다)
grep -c '260px\\|width: 260' $F                       # ≥1 (CSS 정의)
grep -c 'data-theme="dark"' $F                       # ≥2 (F1/F2)
grep -c 'data-theme="light"' $F                      # ≥2 (F3/F4)
grep -c 'hover-demo-chip' $F                         # ≥3 (CSS 정의 1 + F1/F3 사용 = ≥3)
```

verify gate ≥15 (task_scope §6 목록과 일치). 모두 PASS 시에만 commit.

## 작성 룰 정리 (negative + positive)

- **wrangler / npm run deploy 0건** (CLAUDE.local.md + memory `feedback_cbc7119_design_never_wrangler.md`)
- **App.tsx / ReportsPage.tsx / 다른 페이지 / src/** 수정 0건** (sketch 1개 파일만)
- **이모지 0건** (⬇ 📄 ✅ ❌ ⚠️ 모두 금지 — memory `feedback_tsx_wave_emoji_dot_gap.md`)
- **본문 가운뎃점 텍스트 0건** (` · ` 모두 `<span class="dot-meta"></span>` 으로 변환. HTML 주석 안 reference 인용은 허용)
- **w-8 / h-8 utility class 0건** (memory `feedback_tailwind_w8_h8_is_48px.md` — 모두 인라인 px 또는 [N] arbitrary value)
- **status- prefix Tailwind 0건** (memory `feedback_tailwind_token_class_pattern.md`)
- **9·10·11px 본문 fontSize 0건** (모두 12px 상향 — design-system §1.1 노안 룰)
- **linear-gradient 0건** (W1 OQ #1/#3 LOCKED 일관)
- **REPORT_CARDS 10 entry title verbatim 보존** (memory `feedback_sketch_realistic_data.md`)
- **DESKTOP_SECTIONS 4 entry label verbatim 보존** (섹션 B 가운뎃점만 dot span 변환)

  </action>
  <verify>
    <automated>F=/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/14-reports/sketch-wave-5-desktop-sidelist.html; test -f "$F" && \
EMOJI=$(grep -cE '⬇|📄|✅|❌|⚠️' "$F") && \
GRAD=$(grep -c 'linear-gradient' "$F") && \
STATUS=$(grep -cE 'text-status-|bg-status-' "$F") && \
DOTTEXT=$(grep -v '<!--' "$F" | grep -cE ' · ') && \
W8=$(grep -cE 'class="[^"]*\bw-8\b|class="[^"]*\bh-8\b' "$F") && \
SMALLFONT=$(grep -cE 'font-size: ?(9|10|11)px' "$F") && \
SIDELIST=$(grep -c '\.sidelist *{' "$F") && \
SELROW=$(grep -c 'sidelist-row--selected' "$F") && \
HOVROW=$(grep -c 'sidelist-row--hover' "$F") && \
SECA=$(grep -c '유수검지 장치' "$F") && \
SECC=$(grep -c '연간 점검일지' "$F") && \
SECD=$(grep -c '소방펌프' "$F") && \
DOTSPAN=$(grep -c 'class="dot-meta"' "$F") && \
MEMRULES=$(grep -oE 'feedback_[a-z_0-9]+\.md' "$F" | sort -u | wc -l | tr -d ' ') && \
W6=$(grep -c 'W6 책임' "$F") && \
W4P=$(grep -c 'W4 책임' "$F") && \
FDARK=$(grep -c 'data-theme="dark"' "$F") && \
FLIGHT=$(grep -c 'data-theme="light"' "$F") && \
HCHIP=$(grep -c 'hover-demo-chip' "$F") && \
echo "EMOJI=$EMOJI GRAD=$GRAD STATUS=$STATUS DOTTEXT=$DOTTEXT W8=$W8 SMALLFONT=$SMALLFONT SIDELIST=$SIDELIST SELROW=$SELROW HOVROW=$HOVROW SECA=$SECA SECC=$SECC SECD=$SECD DOTSPAN=$DOTSPAN MEMRULES=$MEMRULES W6=$W6 W4P=$W4P FDARK=$FDARK FLIGHT=$FLIGHT HCHIP=$HCHIP" && \
[ "$EMOJI" = "0" ] && [ "$GRAD" = "0" ] && [ "$STATUS" = "0" ] && [ "$DOTTEXT" = "0" ] && [ "$W8" = "0" ] && [ "$SMALLFONT" = "0" ] && \
[ "$SIDELIST" -ge 1 ] && [ "$SELROW" -ge 4 ] && [ "$HOVROW" -ge 2 ] && \
[ "$SECA" -ge 4 ] && [ "$SECC" -ge 4 ] && [ "$SECD" -ge 8 ] && \
[ "$DOTSPAN" -ge 40 ] && [ "$MEMRULES" -ge 7 ] && \
[ "$W6" -ge 4 ] && [ "$W4P" -ge 4 ] && \
[ "$FDARK" -ge 2 ] && [ "$FLIGHT" -ge 2 ] && [ "$HCHIP" -ge 3 ]</automated>
  </verify>
  <done>
- `cha-bio-safety/docs/redesign-context/14-reports/sketch-wave-5-desktop-sidelist.html` 파일 생성 완료.
- 4 frame matrix (다크 div-early selected + hover demo / 다크 자탐 selected / 라이트 div-early selected + hover demo / 라이트 자탐 selected) 시각 노출.
- DESKTOP_SECTIONS 4 섹션 × REPORT_CARDS 10 row 모두 frame 마다 표기.
- 섹션 B 라벨 '소화전 · 가스 · 비상콘센트' → dot span 2개 변환.
- 10 row 의 sub 라인 모두 dot span 변환.
- 상단 toolbar dashed placeholder + 우측 ExcelPreview dashed placeholder 표기.
- Notes block 안 W6 진입 OQ 3건 default 답 명시.
- 자체 verify gate ≥15 모두 PASS.
- atomic 1-commit: `docs(14-reports): sketch wave 5 — 데스크톱 좌측 sidelist 260px × 4 섹션 × 10 카드 (다크/라이트 1280px)`
  </done>
</task>

</tasks>

<verification>
## 전체 점검 (commit 직전)

```bash
# 변경 파일 1건만
git status --short cha-bio-safety/docs/redesign-context/14-reports/sketch-wave-5-desktop-sidelist.html
# src/ 변경 0
git diff --name-only HEAD -- cha-bio-safety/src cha-bio-safety/migrations cha-bio-safety/scripts cha-bio-safety/functions
# App.tsx 손대지 않음
git diff --name-only HEAD -- cha-bio-safety/src/App.tsx
# ReportsPage.tsx 손대지 않음
git diff --name-only HEAD -- cha-bio-safety/src/pages/ReportsPage.tsx
# 다른 페이지 (02 / 06 / 13-schedule 등) 손대지 않음
git diff --name-only HEAD -- cha-bio-safety/docs/redesign-context/ | grep -v '14-reports/sketch-wave-5-desktop-sidelist.html'  # 출력 0줄
# wrangler / npm run deploy 명령 0
```

모두 PASS 시 commit.
</verification>

<success_criteria>
- 단일 산출물: `cha-bio-safety/docs/redesign-context/14-reports/sketch-wave-5-desktop-sidelist.html`
- 4 frame × 4 섹션 × 10 row 시각 표기 (dark div-early / dark 자탐 / light div-early / light 자탐)
- W4 frame 셋업 (tokens / spacing / typography / frame-desktop / global-header-placeholder) verbatim mirror
- W3 dot-meta CSS verbatim mirror (margin/vertical-align 추가)
- ReportsPage.tsx line 142~147 + 249~295 verbatim 매핑 — 라벨 / 카피 0 deviation
- 섹션 B '소화전 · 가스 · 비상콘센트' 가운뎃점 → dot span 2개 변환
- 10 row sub 라인 가운뎃점 → dot span 변환
- verify gate ≥15건 모두 PASS (이모지 0 / 그라데이션 0 / status-prefix 0 / w-8/h-8 0 / 9·10·11px 0 + sidelist 정의 / selected ≥4 / hover ≥2 / 섹션 라벨 ≥4 / 메모리 룰 unique ≥7 / W4·W6 placeholder ≥4 / 다크·라이트 ≥2 / hover-chip ≥3)
- W6 진입 전 OQ 3건 default 답 sketch Notes 안 명시
- atomic 1-commit (`docs(14-reports): sketch wave 5 ...`)
- wrangler 0 / npm run deploy 0 / App.tsx / ReportsPage.tsx / 다른 페이지 변경 0
</success_criteria>

<output>
Task 완료 후 다음 단계:
1. `.planning/quick/260520-h4z-redesign-14-reports-sketch-wave-5-sideli/260520-h4z-SUMMARY.md` 작성
   - 산출물: sketch-wave-5-desktop-sidelist.html (1개 파일, atomic commit)
   - Frame matrix 4건 mapping (다크 div-early / 다크 자탐 / 라이트 div-early / 라이트 자탐)
   - DESKTOP_SECTIONS 4 섹션 → 10 row mapping (섹션 A 2 / 섹션 B 3 / 섹션 C 4 / 섹션 D 1 = 10)
   - verify gate ≥15건 결과 (모두 PASS)
   - W6 진입 전 OQ 3건 default 답:
     - OQ #1: ExcelPreview 영역 = 단순 미리보기 PNG 자리 (그리드 오버레이는 ExcelPreview.tsx 본체 책임)
     - OQ #2: 미리보기 톤 = A4 흰배경 고정 (reference_floorplan_png_algorithm + 12-staff-service W8 결정 inherit)
     - OQ #3: 빈 상태 UI 불필요 (sidelist 항상 default selected, useState<ReportType>('div-early') 초기값)
   - W1 LOCKED 5건 적용 매트릭스 (W5 관점) — #2 (260px 유지) + #5 (dot span) 적용
   - 메모리 룰 박제 ≥7건 unique
2. atomic 1-commit:
   ```
   docs(14-reports): sketch wave 5 — 데스크톱 좌측 sidelist 260px × 4 섹션 × 10 카드 (다크/라이트 1280px)
   ```
3. 사용자에게 W5 sketch 컨펌 요청 (자체 PASS 판단 0건 — memory `feedback_avoid_premature_confirmation.md`)
4. 컨펌 후 W6 진입 — `/clear` + 새 quick (sketch-wave-6-desktop-preview-wrapper.html)
</output>
