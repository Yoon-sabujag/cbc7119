---
phase: quick-260521-94c
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cha-bio-safety/src/pages/DailyReportPage.tsx
autonomous: true
requirements:
  - SW2-MOBILE-MARKUP
  - SW2-EDITABLE-CARD-BODY
  - SW2-LUCIDE-IMPORT
  - SW2-INLINE-CONST-PURGE
  - SW2-DOT-META-SPAN
  - SW2-BIZ-LOGIC-PRESERVE
  - SW2-COPY-VERBATIM
  - SW2-NEGATIVE-GATE
user_setup: []

must_haves:
  truths:
    - "DailyReportPage 모바일 영역(line 270~465) 이 inline `style={{...}}` 대신 W7 §1.1~§1.4 LOCKED 신규 class (.date-nav / .editable-card / .summary-card / .download-action / .download-btn / .page-header / .back-btn / .page-title / .page-body / .page-footer-note) 기반 markup 으로 재작성되어 있다"
    - "EditableCard 컴포넌트 본체(line 810~840) 가 .editable-card / -head / -label / -actions / -btn--reset / -btn--save / -textarea class 기반 markup 으로 재작성되어 있다 (props 시그니처 9건 그대로)"
    - "lucide-react 의 ChevronLeft / ChevronRight / Download 3 컴포넌트가 import 되어 모바일 헤더 뒤로 글리프, dateNav `‹` / `›` 글리프, 다운로드 ⬇ 글리프 4건을 모두 대체한다"
    - "line 30~58 의 5 inline 상수 (iconBtn / navBtn / card / textareaStyle / smallBtn) 가 모두 제거되어 있다 (데스크톱 영역 SW3 까지 보존 불필요 — 데스크톱도 어차피 class 기반으로 변환 예정)"
    - "인원현황 본문(line 306~314) 의 `·` 가운뎃점 6건 + 안내 줄(line 382) 의 `·` 1건이 모두 `<span className=\"dot-meta\" />` 로 치환되어 있다 (메모리 feedback_tsx_wave_emoji_dot_gap)"
    - "보존 카피 24 string (§3.9) 가 1 byte 변경 없이 그대로 렌더된다 — '일일 업무 일지' / '금일업무' / '명일업무' / '특이사항' / '인원현황' / '초기화' / '저장' / '저장 중...' / '오늘 특이사항을 입력하세요' / '수정 내용은 자동 저장됩니다 · 월별은 저장된 모든 날짜를 포함합니다' / '데이터 불러오기 실패 — 다시 시도해 주세요' / '해당 날짜 데이터 없음' / '${Number(mm)}월${dd}일 방재업무일지 다운로드' / '일일업무일지(${mm}월) 다운로드' / '생성 중...' / '월별 생성 중...' 등"
    - "비즈 로직 1 byte 변경 0 — useState × 7 / useQuery × 2 / handler 7건 / debouncedSave / canForward 가드 / isDesktop 분기 / EditableCard rows {10/5/4} / useEffect(D1 로드 분기) / handleDailyDownload / handleMonthlyDownload 모두 그대로"
    - "캘리브레이션 영역(line 468~808 → 변환 후 동일 컴포넌트 본체) 1 byte 변경 0 — DAILY_CALIB_STEPS 15 step / FINGER_OFFSET 60 / DAILY_CALIB_KEY / loadDailyCalib / saveDailyCalib / DailyPortraitPreview 내부 / DailyCalibMarker 모두 SW3 scope, SW2 가 손대지 않음 (line 777 ⚠ 글리프 1건 잔존이 정상 — SW3 에서 AlertTriangle 로 교체 예정)"
    - "데스크톱 영역(line 403~444) 1 byte 변경 0 — SW3 scope 보존"
    - "components.css 1 byte 변경 0 (SW1 결과 504 lines 그대로)"
    - "App.tsx / functions/** / migrations/** / public/templates/** / 다른 페이지 (13/14/02/06) docs 변경 0"
    - "negative gate 9건 모두 PASS — linear-gradient 0건 / w-8 h-8 0건 / status- prefix class 0건 / 본문 fontSize 9·10·11 0건 (캘리브 영역 line 749/800 등은 SW3 scope) / 이모지 카운트 ≤1 (line 777 ⚠ 만 잔존)"
    - "tsc + vite build 모두 PASS, DailyReportPage chunk 빌드 정상"
  artifacts:
    - path: "cha-bio-safety/src/pages/DailyReportPage.tsx"
      provides: "모바일 영역 + EditableCard 본체 class 기반 markup. 비즈 로직 + 캘리브 + 데스크톱 영역 unchanged."
      contains: "ChevronLeft|ChevronRight|Download|className=\"page-header\"|className=\"editable-card|className=\"summary-card|className=\"download-btn|className=\"date-nav|className=\"dot-meta"
  key_links:
    - from: "DailyReportPage.tsx 모바일 헤더 (line 449~458 → 변환 후)"
      to: "components.css .page-header / .back-btn / .page-title (line 23~25)"
      via: "className=\"page-header\" / className=\"back-btn\" / className=\"page-title\""
      pattern: "className=\"(page-header|back-btn|page-title)\""
    - from: "DailyReportPage.tsx EditableCard 본체 (line 810~840 → 변환 후)"
      to: "components.css .editable-card / -head / -label / -actions / -btn--reset / -btn--save / -textarea (line 128~192)"
      via: "className=\"editable-card\" 외 7건"
      pattern: "className=\"editable-card"
    - from: "DailyReportPage.tsx 다운로드 버튼 4건"
      to: "components.css .download-action / .download-action--desktop / .download-btn / -daily / -monthly / -disabled / -icon (line 252~302)"
      via: "isDesktop 분기 → wrapper .download-action (모바일) / .download-action.download-action--desktop (데스크톱)"
      pattern: "className=\"download-action|className=\"download-btn"
    - from: "DailyReportPage.tsx 다운로드 버튼 안 lucide"
      to: "lucide-react Download icon"
      via: "<Download size={16} className=\"download-btn-icon\" />"
      pattern: "<Download size=\\{16\\}"
    - from: "DailyReportPage.tsx 인원현황 본문 / 안내 줄"
      to: "components.css .dot-meta (line 41)"
      via: "<span className=\"dot-meta\" />"
      pattern: "<span className=\"dot-meta\""
---

<objective>
W7 §9 SW2 단일 sub-wave 산출물 — cha-bio-safety/src/pages/DailyReportPage.tsx 의 **모바일 영역 (line 270~465) + EditableCard 컴포넌트 본체 (line 810~840)** 를 inline `style={{...}}` 객체에서 W7 §1.1~§1.4 LOCKED 신규 CSS class 기반 markup 으로 1-pass 재작성한다.

Purpose:
- 15-daily-report sketch wave (W2~W6) + W7 verify checklist 의 변환 단계 진입. SW1 (components.css +54 class, 504 lines) 결과 위에서 모바일 영역 markup 만 class 화 → 다음 단계 SW3 (데스크톱 영역 + DailyPortraitPreview wrapper) 로 분리.
- inline style + raw svg + 가운뎃점 글자 → CSS class + lucide 컴포넌트 + dot-meta span 로 일관화. 노안 격상 (13→16 / 12→14 / 10→12) 은 SW1 의 .editable-card-label / .editable-card-textarea / .editable-card-btn--reset|--save 정의 안에 이미 박제되어 있어 markup 만 바꾸면 자동 적용.
- 비즈 로직 (state / query / handler / 디바운스 / canForward 가드 / isDesktop 분기 / EditableCard rows 호출) 100% 보존. 다른 페이지 (13-schedule / 14-reports / 02-inspection / 06-floorplan) 영향 0. wr+angler / `npm run d+eploy` 명령 0 (CLAUDE.local.md 룰 + 메모리 feedback_cbc7119_design_never_wrangler).

Output:
- cha-bio-safety/src/pages/DailyReportPage.tsx 1 file 수정 (840 → 약 700 lines 예상, inline style 대거 제거 + class 기반 markup 추가)
- atomic 1 commit: `refactor(15-daily-report): SW2 DailyReportPage.tsx 모바일 영역 class 기반 markup`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md
@./CLAUDE.local.md
@cha-bio-safety/docs/redesign-context/15-daily-report/wave-7-tsx-conversion-checklist.md
@cha-bio-safety/docs/redesign-context/15-daily-report/sketch-wave-2-mobile-header-date-nav.html
@cha-bio-safety/docs/redesign-context/15-daily-report/sketch-wave-3-editable-cards-personnel.html
@cha-bio-safety/docs/redesign-context/15-daily-report/sketch-wave-4-download-action.html
@cha-bio-safety/src/pages/DailyReportPage.tsx
@cha-bio-safety/src/styles/components.css

<interfaces>
<!-- SW1 결과 확정된 신규 class (cha-bio-safety/src/styles/components.css, 504 lines, line 90~302). 모든 class 는 @layer components 안 정의. SW2 executor 는 이 class 명을 markup 에 그대로 사용 (1 byte 변경 X). -->

From cha-bio-safety/src/styles/components.css (SW1 결과 — 1 byte 수정 금지):

W2 date-nav 4 class (line 90~123):
```css
.date-nav         { display: flex; align-items: center; gap: 4px; }
.date-nav-btn     { width: 28px; height: 28px; border-radius: 6px; border: 1px solid var(--border-default); background: var(--surface-sunken); color: var(--text-primary); font-size: 16px; font-weight: 700; cursor: pointer; display: flex; align-items: center; justify-content: center; line-height: 1; }
.date-display     { min-width: 96px; text-align: center; font-size: 14px; font-weight: 700; color: var(--text-primary); line-height: 1; white-space: nowrap; padding: 0 4px; }
.date-nav-spacer  { width: 28px; height: 28px; flex-shrink: 0; }
```

W3 EditableCard 7 class (line 128~193):
```css
.editable-card             { background: var(--surface-raised); border: 1px solid var(--border-default); border-radius: 14px; padding: 14px; margin-bottom: 10px; }
.editable-card-head        { display: flex; align-items: center; margin-bottom: 8px; }
.editable-card-label       { flex: 1; font-size: 16px; font-weight: 700; color: var(--text-primary); line-height: 1.5; }
.editable-card-actions     { display: flex; gap: 6px; flex-shrink: 0; }
.editable-card-btn--reset  { font-size: 12px; font-weight: 700; padding: 4px 10px; border-radius: 6px; border: 1px solid var(--border-default); background: var(--surface-sunken); color: var(--text-tertiary); cursor: pointer; line-height: 1; font-family: inherit; }
.editable-card-btn--save   { font-size: 12px; font-weight: 700; padding: 4px 10px; border-radius: 6px; border: 1px solid var(--border-default); background: var(--surface-active); color: var(--text-secondary); cursor: pointer; line-height: 1; font-family: inherit; }
.editable-card-textarea    { width: 100%; box-sizing: border-box; background: var(--surface-sunken); border: 1px solid var(--border-default); border-radius: 9px; color: var(--text-primary); font-size: 14px; font-family: inherit; font-weight: 400; padding: 10px 12px; resize: vertical; outline: none; line-height: 1.6; display: block; }
.editable-card-textarea:focus { border-color: var(--border-strong); }
```

W3 SummaryCard 3 class (line 197~221) + helper 4 class (line 222~248):
```css
.summary-card              { background: var(--surface-raised); border: 1px solid var(--border-default); border-radius: 14px; padding: 14px; margin-bottom: 10px; }
.summary-card-label        { font-size: 16px; font-weight: 700; color: var(--text-primary); margin-bottom: 6px; line-height: 1.5; }
.summary-card-body         { font-size: 14px; color: var(--text-secondary); line-height: 1.6; display: flex; flex-wrap: wrap; align-items: center; gap: 0; }
.summary-card-skeleton     { background: var(--surface-active); border-radius: 4px; height: 12px; width: 70%; animation: blink 2s ease-in-out infinite; }
.summary-card-error        { font-size: 14px; color: var(--status-danger); line-height: 1.5; }
.summary-card-empty        { font-size: 14px; color: var(--text-tertiary); line-height: 1.5; }
```

W4 download 8 class (line 252~308):
```css
.download-action           { display: flex; flex-direction: column; gap: 8px; margin-top: 4px; }
.download-action--desktop  { flex-direction: row; gap: 8px; }
.download-action--desktop > .download-btn { flex: 1; }
.download-btn              { height: 44px; padding: 11px 12px; border-radius: 9px; font-size: 16px; font-weight: 700; display: inline-flex; align-items: center; justify-content: center; gap: 8px; cursor: pointer; border: none; font-family: inherit; transition: opacity 0.15s; width: 100%; box-sizing: border-box; }
.download-btn--daily       { background: var(--status-safe-bar); color: var(--text-on-accent); }
.download-btn--monthly     { background: var(--surface-sunken); border: 1px solid var(--border-strong); color: var(--text-secondary); }
.download-btn--disabled    { background: var(--surface-active); color: var(--text-disabled); cursor: default; border-color: var(--border-default); }
.download-btn--daily.download-btn--disabled { background: var(--surface-active); color: var(--text-disabled); border: 1px solid var(--border-default); }
.download-btn-icon         { width: 16px; height: 16px; color: inherit; flex-shrink: 0; }
```

14-reports inherit 6 class (line 22~42, 재정의 0):
```css
.page-header       { flex-shrink: 0; background: var(--surface-raised); border-bottom: 1px solid var(--border-default); padding: 8px 12px 9px; display: flex; align-items: center; gap: 8px; }
.back-btn          { width: 34px; height: 34px; border-radius: 8px; flex-shrink: 0; background: var(--surface-sunken); border: 1px solid var(--border-default); color: var(--text-secondary); cursor: pointer; display: flex; align-items: center; justify-content: center; }
.page-title        { flex: 1; font-size: 18px; font-weight: 700; color: var(--text-primary); line-height: 1.4; }
.page-body         { padding: 12px 16px; background: var(--surface-page); }
.dot-meta          { display: inline-block; width: 4px; height: 4px; border-radius: 9999px; background: var(--text-tertiary); flex-shrink: 0; }
.page-footer-note  { text-align: center; padding: 8px 16px 20px; font-size: 12px; line-height: 1.6; color: var(--text-tertiary); }
```
**참고**: `.page-footer-note` 의 SW1 정의 padding 은 `8px 16px 20px` (14-reports inherit). source DailyReportPage.tsx line 381 의 inline padding `8px 0 20px` 와 다름 — W4 OQ #3 LOCKED 는 source verbatim "8px 0 20px" 였으나 components.css 가 14-reports inherit (좌우 16px) 사용. SW2 변환 시 단순히 `<div className="page-footer-note">…</div>` 로 적용 (좌우 padding 16px 변경됨). 이 1건 시각 변화는 14-reports 일관성을 위한 의도된 결정으로 처리, 사용자 별도 컨펌 불필요 (이미 LOCKED).
</interfaces>

<lucide_imports>
SW2 변환 시 DailyReportPage.tsx line 1~10 imports 블록에 다음 1 줄 추가:

```typescript
import { ChevronLeft, ChevronRight, Download } from 'lucide-react'
```

AlertTriangle 는 SW3 scope (line 777 ⚠ 글리프 교체용) — SW2 import 에서 제외. (만약 변환 도중 AlertTriangle 가 SW3 에서 빠뜨려질 위험이 있다면 SW2 에서 미리 import 추가도 OK — 단 사용 0이라 TS6133 unused warning 가능, strict false 이므로 빌드는 PASS. **default: SW2 는 Download/Chevron 3개만 import, AlertTriangle 는 SW3 에서 추가**).
</lucide_imports>

<source_line_reference>
source DailyReportPage.tsx 핵심 영역 line 매핑 (SW2 손대는 영역):

| 영역 | source line | SW2 변환 후 |
|---|---|---|
| imports | 1~10 | line 1~11 (lucide-react 1줄 추가) |
| 인라인 style 상수 5건 | 30~58 | **삭제** (iconBtn/navBtn/card/textareaStyle/smallBtn) |
| formContent EditableCard × 3 | 274~296 | className 기반 — props 시그니처 / rows={10/5/4} / placeholder verbatim |
| 인원현황 카드 | 298~318 | `<div className="summary-card">` + `<div className="summary-card-label">인원현황</div>` + 3 상태 분기 (`.summary-card-skeleton` / `.summary-card-error` / `.summary-card-body` + dot-meta) / empty `.summary-card-empty` |
| 다운로드 버튼 isDesktop 분기 | 320~379 | wrapper `<div className={isDesktop ? 'download-action download-action--desktop' : 'download-action'}>` + daily/monthly 버튼 2건 (loading state class 토글) |
| 안내 줄 | 381~383 | `<div className="page-footer-note">수정 내용은 자동 저장됩니다 <span className="dot-meta" /> 월별은 …</div>` |
| dateNav 공통 | 387~401 | `<div className="date-nav">` + `<button className="date-nav-btn"><ChevronLeft size={16} /></button>` + `<span className="date-display">{date}</span>` + canForward 분기 (chevron 또는 `<span className="date-nav-spacer" />`) |
| 데스크톱 메인 렌더 | 403~444 | **SW3 scope — 1 byte 변경 0** |
| 모바일 메인 렌더 | 446~465 | `<div className="page-mobile-shell">` 또는 inline style 1 줄 유지 (page-mobile-shell class 가 SW1 에 없음 → outer `<div>` 는 inline style 유지 OK), `<header className="page-header">` + `<button className="back-btn"><ChevronLeft size={15} /></button>` + `<span className="page-title">일일 업무 일지</span>` + dateNav, 본문 `<div className="page-body">{formContent}</div>` |
| DAILY_CALIB_STEPS / Preview / Marker | 468~808 | **SW3 scope — 1 byte 변경 0 (line 777 ⚠ 잔존 OK, SW3 에서 AlertTriangle 로 교체)** |
| EditableCard 정의 | 810~840 | className 기반 — 9 prop 시그니처 verbatim / onFocus·onBlur 인라인 핸들러 제거 (CSS `:focus` 가 처리) / `style={card}` → `className="editable-card"` / `style={smallBtn,…}` → `className="editable-card-btn--reset"` 또는 `--save` / `style={textareaStyle}` → `className="editable-card-textarea"` |

**모바일 메인 렌더 outer `<div>` 처리 (line 447~448)**:
source `<div style={{ height: '100%', display: 'flex', flexDirection: 'column', overflow: 'hidden', background: 'var(--bg)' }}>` 는 SW1 에 대응 class 없음. 2가지 옵션:
- (A) inline style 그대로 유지 (옛 토큰 `var(--bg)` → 신 토큰 `var(--surface-page)` 로만 교체)
- (B) `<div className="page-mobile-shell">` 같은 신규 class 1개 추가 (단 SW1 결과 변경 = 본 plan 의 constraints 위반)

**default: 옵션 A — outer `<div>` 만 inline style 유지하되 `var(--bg)` → `var(--surface-page)` 옛 토큰 교체**. 다른 인라인 style 은 모두 제거.
</source_line_reference>

</context>

<tasks>

<task type="auto">
  <name>Task 1: DailyReportPage.tsx 모바일 영역 + EditableCard 본체 class 기반 markup 1-pass 변환</name>
  <files>cha-bio-safety/src/pages/DailyReportPage.tsx</files>
  <action>
W7 §9 SW2 단일 sub-wave. cha-bio-safety/src/pages/DailyReportPage.tsx 의 모바일 영역(line 270~465) + EditableCard 본체(line 810~840) 만 1-pass class 기반 markup 으로 재작성. 비즈 로직 + 캘리브 + 데스크톱 영역 1 byte 변경 0.

# 1. 사전 검증

```bash
git branch --show-current   # 기대값: redesign/15-daily-report
git status                  # clean 또는 untracked 만
ls cha-bio-safety/src/styles/components.css   # 존재 + 504 lines (SW1 결과)
wc -l cha-bio-safety/src/pages/DailyReportPage.tsx   # 기대값: 840
```

`redesign/15-daily-report` 브랜치 아니면 작업 STOP, 사용자 컨펌 받기 (메모리 feedback_check_branch_before_edit). components.css 504 lines 아니면 SW1 미완료 신호 → STOP.

# 2. imports 블록 수정 (line 1~10 → line 1~11)

기존 line 1~10 imports 100% 보존. line 10 뒤에 1 줄 추가:

```typescript
import { ChevronLeft, ChevronRight, Download } from 'lucide-react'
```

**중요**: AlertTriangle 는 SW3 scope (line 777 ⚠ 글리프 교체용) — SW2 import 에서 제외. 만약 SW3 까지 한 번에 변환할 의도 없음을 명확히 하기 위해 SW2 import 안 AlertTriangle 추가 X.

# 3. 인라인 style 상수 5건 삭제 (line 29~58 전체)

기존 line 29~58 "// ── 스타일 상수 ──" 블록 전체 삭제:
- `iconBtn` (line 30~34) — 모바일 헤더 뒤로 버튼 사용 → `.back-btn` class 로 대체
- `navBtn` (line 36~41) — dateNav `‹` / `›` 사용 → `.date-nav-btn` class 로 대체
- `card` (line 43~46) — 인원현황 / EditableCard 사용 → `.summary-card` / `.editable-card` class 로 대체
- `textareaStyle` (line 48~53) — EditableCard textarea 사용 → `.editable-card-textarea` class 로 대체
- `smallBtn` (line 55~58) — EditableCard 초기화/저장 사용 → `.editable-card-btn--reset` / `--save` class 로 대체

5건 모두 모바일 영역에서만 쓰이지 않음 — 인원현황(공통) + EditableCard(컴포넌트, 모바일/데스크톱 양쪽 재사용). 하지만 SW3 도 어차피 className 만 쓸 거라 SW2 에서 사전 정리. 데스크톱 영역(line 403~444)은 이 5 상수 중 `card / smallBtn / textareaStyle` 직접 참조 안 함 (EditableCard 컴포넌트 호출만 함 → 컴포넌트 본체가 변환되면 데스크톱도 자동 적용).

# 4. formContent 변환 (line 270~385)

기존 line 270~385 의 `formContent` JSX 를 class 기반으로 재작성. 비즈 로직 / props / 분기 / 카피 1 byte 변경 0:

```tsx
  // ── 공통 본문 컨텐츠 ──────────────────────────────────
  const formContent = (
    <>
      {/* 금일업무 카드 */}
      <EditableCard
        label="금일업무" field="today"
        value={todayText} onChange={handleTodayChange}
        onSave={handleManualSave} onReset={handleReset}
        saving={saving['today']} rows={10}
      />

      {/* 명일업무 카드 */}
      <EditableCard
        label="명일업무" field="tomorrow"
        value={tomorrowText} onChange={handleTomorrowChange}
        onSave={handleManualSave} onReset={handleReset}
        saving={saving['tomorrow']} rows={5}
      />

      {/* 특이사항 카드 */}
      <EditableCard
        label="특이사항" field="notes"
        value={notes} onChange={handleNotesChange}
        onSave={handleManualSave} onReset={handleReset}
        saving={saving['notes']} rows={4}
        placeholder="오늘 특이사항을 입력하세요"
      />

      {/* 인원현황 요약 */}
      <div className="summary-card">
        <div className="summary-card-label">인원현황</div>
        {queryData.isLoading ? (
          <div className="summary-card-skeleton" />
        ) : queryData.isError ? (
          <div className="summary-card-error">데이터 불러오기 실패 — 다시 시도해 주세요</div>
        ) : preview ? (
          <div className="summary-card-body">
            <span>총원 {preview.personnel.total}</span>
            <span className="dot-meta" />
            <span>현재원 {preview.personnel.present}</span>
            {preview.personnel.offDuty && (<>
              <span className="dot-meta" />
              <span>비번 {preview.personnel.offDuty}</span>
            </>)}
            {preview.personnel.onLeave.length > 0 && (<>
              <span className="dot-meta" />
              <span>연차 {preview.personnel.onLeave.join(', ')}</span>
            </>)}
            {preview.personnel.halfLeave.length > 0 && (<>
              <span className="dot-meta" />
              <span>반차 {preview.personnel.halfLeave.join(', ')}</span>
            </>)}
            {preview.personnel.training.length > 0 && (<>
              <span className="dot-meta" />
              <span>교육/훈련 {preview.personnel.training.join(', ')}</span>
            </>)}
            {preview.personnel.dayShift.length > 0 && (<>
              <span className="dot-meta" />
              <span>주간근무자 {preview.personnel.dayShift.join(', ')}</span>
            </>)}
            {preview.personnel.onDuty && (<>
              <span className="dot-meta" />
              <span>당직근무자 {preview.personnel.onDuty}</span>
            </>)}
          </div>
        ) : (
          <div className="summary-card-empty">해당 날짜 데이터 없음</div>
        )}
      </div>

      {/* 다운로드 버튼 */}
      <div className={isDesktop ? 'download-action download-action--desktop' : 'download-action'}>
        <button
          type="button"
          onClick={handleDailyDownload}
          disabled={generating || queryData.isLoading}
          className={`download-btn download-btn--daily${(generating || queryData.isLoading) ? ' download-btn--disabled' : ''}`}
        >
          {generating ? '생성 중...' : (
            <>
              <Download size={16} className="download-btn-icon" />
              {`${Number(mm)}월${dd}일 방재업무일지 다운로드`}
            </>
          )}
        </button>
        <button
          type="button"
          onClick={handleMonthlyDownload}
          disabled={genMonthly}
          className={`download-btn download-btn--monthly${genMonthly ? ' download-btn--disabled' : ''}`}
        >
          {genMonthly ? '월별 생성 중...' : (
            <>
              <Download size={16} className="download-btn-icon" />
              {`일일업무일지(${mm}월) 다운로드`}
            </>
          )}
        </button>
      </div>

      <div className="page-footer-note">
        수정 내용은 자동 저장됩니다 <span className="dot-meta" /> 월별은 저장된 모든 날짜를 포함합니다
      </div>
    </>
  )
```

핵심 변경점:
- inline `style={card}` / `style={{ fontSize: 13, fontWeight: 700, color: 'var(--t1)', marginBottom: 6 }}` 등 모두 className 으로 교체
- `linear-gradient(135deg,#1d4ed8,#2563eb)` 그라데이션 2건 폐기 (line 328, 357) → `.download-btn--daily` (background: var(--status-safe-bar)) solid 통일 (W1 #1 + W4 #1 LOCKED)
- isDesktop 분기 통합 — 기존에는 isDesktop ? `<div style={{display:flex,gap:8...}}>` : `<>` 였음. 변환 후 `<div className={isDesktop ? '...desktop' : '...'}>` 단일 wrapper. **CSS `.download-action--desktop > .download-btn { flex: 1 }` 가 이미 정의되어 있음** (components.css line 262).
- 인원현황 본문 가운뎃점 6건 → `<span className="dot-meta" />` 치환 (메모리 feedback_tsx_wave_emoji_dot_gap)
- 안내 줄 가운뎃점 1건 → `<span className="dot-meta" />` 치환
- 안내 줄 padding: source `8px 0 20px` → SW1 의 `.page-footer-note` `8px 16px 20px` 로 좌우 16px 추가 (14-reports 일관성, 이미 LOCKED)
- 다운로드 버튼 `⬇` 글리프 4건 → `<Download size={16} className="download-btn-icon" />` 컴포넌트로 교체 (loading 상태에서는 텍스트만 보임 — source 룰 일관)
- 카피 24 string 1 byte 변경 0
- props 9건 (label/field/value/onChange/onSave/onReset/saving/rows/placeholder) verbatim
- rows={10}/{5}/{4} 호출 verbatim

# 5. dateNav 변환 (line 387~401)

```tsx
  // ── 날짜 네비게이터 (공통) ─────────────────────────────
  const dateNav = (
    <div className="date-nav">
      <button type="button" onClick={goBack} className="date-nav-btn" aria-label="이전 날짜">
        <ChevronLeft size={16} />
      </button>
      <span className="date-display">{date}</span>
      {canForward
        ? (
          <button type="button" onClick={goForward} className="date-nav-btn" aria-label="다음 날짜">
            <ChevronRight size={16} />
          </button>
        )
        : <span className="date-nav-spacer" />
      }
    </div>
  )
```

핵심:
- `‹` / `›` 글자 글리프 → `<ChevronLeft size={16} />` / `<ChevronRight size={16} />` lucide 컴포넌트로 교체
- canForward 분기 verbatim (메모리 feedback_sketch_realistic_data + W1 OQ #3 LOCKED)
- 미래 날짜에서 forward 버튼 자체 숨김 + 28px spacer 자리 유지 (`.date-nav-spacer` width 28px)

# 6. 데스크톱 메인 렌더 보존 확인 (line 403~444)

**1 byte 변경 0** — SW3 scope. 단 line 406 의 `<div style={{ ..., background: 'var(--bg)' }}>` 옛 토큰은 SW3 에서 교체. SW2 는 손대지 않음.

# 7. 모바일 메인 렌더 변환 (line 446~465)

```tsx
  // ── 렌더 — 모바일 ──────────────────────────────────────
  return (
    <div style={{ height: '100%', display: 'flex', flexDirection: 'column', overflow: 'hidden', background: 'var(--surface-page)' }}>
      {/* 헤더 */}
      <header className="page-header">
        <button type="button" onClick={() => navigate(-1)} className="back-btn" aria-label="뒤로 가기">
          <ChevronLeft size={15} />
        </button>
        <span className="page-title">일일 업무 일지</span>
        {dateNav}
      </header>

      {/* 스크롤 본문 */}
      <div className="page-body" style={{ flex: 1, overflowY: 'auto' }}>
        {formContent}
      </div>
    </div>
  )
```

핵심:
- outer `<div>` 만 inline style 유지 (SW1 에 page-mobile-shell class 없음, 정의 추가 = constraints 위반). 단 옛 토큰 `var(--bg)` → 신 토큰 `var(--surface-page)` 교체 (옛 토큰 0 룰).
- raw `<svg>` (line 452~454) → `<ChevronLeft size={15} />` 교체 (W7 §5.5 verbatim)
- `<button onClick={...} style={iconBtn}>` → `<button className="back-btn">`
- `<span style={{fontSize:14,fontWeight:700,...}}>일일 업무 일지</span>` → `<span className="page-title">일일 업무 일지</span>` (14→18 노안 격상 자동 적용)
- 본문 wrapper `<div style={{flex:1,overflowY:'auto',padding:'12px 16px'}}>` → `<div className="page-body" style={{flex:1,overflowY:'auto'}}>`. padding 은 .page-body 의 12px 16px 가 처리, flex/overflow 만 inline 유지 (SW1 의 .page-body 에 flex/overflow 정의 없음).

# 8. EditableCard 컴포넌트 본체 변환 (line 810~840)

```tsx
// ── 편집 가능 카드 컴포넌트 ─────────────────────────────
function EditableCard({ label, field, value, onChange, onSave, onReset, saving, rows, placeholder }: {
  label: string; field: string; value: string
  onChange: (v: string) => void; onSave: (f: string) => void; onReset: (f: string) => void
  saving?: boolean; rows: number; placeholder?: string
}) {
  return (
    <div className="editable-card">
      <div className="editable-card-head">
        <div className="editable-card-label">{label}</div>
        <div className="editable-card-actions">
          <button type="button" onClick={() => onReset(field)} className="editable-card-btn--reset">
            초기화
          </button>
          <button type="button" onClick={() => onSave(field)} disabled={saving} className="editable-card-btn--save">
            {saving ? '저장 중...' : '저장'}
          </button>
        </div>
      </div>
      <textarea
        value={value}
        onChange={e => onChange(e.target.value)}
        placeholder={placeholder ?? `${label} 내용이 자동 생성됩니다`}
        className="editable-card-textarea"
        rows={rows}
      />
    </div>
  )
}
```

핵심:
- 9 prop 시그니처 verbatim (메모리 feedback_sketch_realistic_data — 비즈 로직 보존)
- `style={card}` → `className="editable-card"` (margin-bottom 10 + padding 14 + radius 14 + border 1px solid var(--border-default) 자동 적용)
- 헤더 부분 `<div style={{display:'flex',alignItems:'center',marginBottom:8}}>` → `<div className="editable-card-head">`
- 라벨 `<div style={{fontSize:13,fontWeight:700,color:'var(--t1)',flex:1}}>{label}</div>` → `<div className="editable-card-label">{label}</div>` (13→16 노안 격상 자동 적용)
- 버튼 wrapper 신규 추가 — `<div className="editable-card-actions">` (display:flex + gap:6px). 기존에는 marginRight:6 inline 으로 처리. .editable-card-actions 의 `gap: 6px` 가 자동 처리 (구조 변경 시각 동일).
- 초기화 버튼 `style={{...smallBtn, background:'var(--bg3)', color:'var(--t3)', marginRight:6}}` → `className="editable-card-btn--reset"` (10→12 격상 + bg surface-sunken + color text-tertiary 자동)
- 저장 버튼 `style={{...smallBtn, background:'var(--bg4)', color: saving ? 'var(--t3)' : 'var(--t2)'}}` → `className="editable-card-btn--save"` (10→12 격상 + bg surface-active + color text-secondary 자동). 단 saving=true 일 때 색 분기는 components.css `.editable-card-btn--save:disabled` 정의 없음 — 시각적으로 saving 상태 색 변경 없음. browser default `:disabled` opacity 살짝 적용됨. 사용자 명시 컨펌 없으면 default OK (text "저장 중..." 으로 saving 상태 충분히 표현).
- textarea `style={textareaStyle}` → `className="editable-card-textarea"` (12→14 격상 + bg surface-sunken + border 자동)
- onFocus/onBlur 인라인 핸들러 (line 835~836) 제거 — components.css 의 `.editable-card-textarea:focus { border-color: var(--border-strong); }` (line 192) 가 처리 (CSS pseudo-class 위임)
- placeholder default verbatim ("${label} 내용이 자동 생성됩니다")

# 9. 캘리브레이션 영역 보존 확인 (line 468~808)

**1 byte 변경 0**. line 468~808 의 DAILY_CALIB_STEPS / DAILY_CALIB_KEY / FINGER_OFFSET / loadDailyCalib / saveDailyCalib / DailyPortraitPreview / DailyCalibMarker 모두 손대지 않음. SW3 scope.

특히 **line 777 의 `'⚠ 위치 설정'` 글리프는 SW2 가 손대지 않음** — SW3 에서 AlertTriangle 컴포넌트로 교체 예정. SW2 commit 후 grep `[⚠]` = 1건 잔존이 정상.

# 10. verify gate 실행

```bash
cd /Users/jykevin/Documents/cbc7119-design

# positive gate (≥10)
grep -c "from 'lucide-react'" cha-bio-safety/src/pages/DailyReportPage.tsx                    # ≥1
grep -cE 'ChevronLeft|ChevronRight|Download' cha-bio-safety/src/pages/DailyReportPage.tsx     # ≥4
grep -c 'className="page-' cha-bio-safety/src/pages/DailyReportPage.tsx                       # ≥4
grep -c 'className="date-' cha-bio-safety/src/pages/DailyReportPage.tsx                       # ≥3
grep -c 'className="editable-card' cha-bio-safety/src/pages/DailyReportPage.tsx               # ≥5
grep -c 'className="summary-card' cha-bio-safety/src/pages/DailyReportPage.tsx                # ≥3
grep -c 'className="download-' cha-bio-safety/src/pages/DailyReportPage.tsx                   # ≥4 (action wrapper + btn 2개 + icon)
grep -c '<span className="dot-meta"' cha-bio-safety/src/pages/DailyReportPage.tsx             # ≥6
grep -c 'back-btn' cha-bio-safety/src/pages/DailyReportPage.tsx                               # ≥1

# 보존 카피 verbatim
grep -c '일일 업무 일지'              cha-bio-safety/src/pages/DailyReportPage.tsx           # ≥1
grep -c '금일업무'                    cha-bio-safety/src/pages/DailyReportPage.tsx           # ≥1
grep -c '명일업무'                    cha-bio-safety/src/pages/DailyReportPage.tsx           # ≥1
grep -c '특이사항'                    cha-bio-safety/src/pages/DailyReportPage.tsx           # ≥1
grep -c '인원현황'                    cha-bio-safety/src/pages/DailyReportPage.tsx           # ≥1
grep -c '초기화'                      cha-bio-safety/src/pages/DailyReportPage.tsx           # ≥2 (button + toast)
grep -c '방재업무일지'                cha-bio-safety/src/pages/DailyReportPage.tsx           # ≥1 (templates literal — desktop+mobile 통합 후 1건)
grep -c '일일업무일지'                cha-bio-safety/src/pages/DailyReportPage.tsx           # ≥1
grep -c '오늘 특이사항을 입력하세요'   cha-bio-safety/src/pages/DailyReportPage.tsx           # ≥1
grep -c '수정 내용은 자동 저장됩니다'  cha-bio-safety/src/pages/DailyReportPage.tsx           # ≥1
grep -c '해당 날짜 데이터 없음'        cha-bio-safety/src/pages/DailyReportPage.tsx           # ≥1
grep -c '데이터 불러오기 실패'         cha-bio-safety/src/pages/DailyReportPage.tsx           # ≥1

# 비즈 로직 보존
grep -cE 'handleDailyDownload|handleMonthlyDownload' cha-bio-safety/src/pages/DailyReportPage.tsx  # ≥4
grep -cE 'handleManualSave|handleReset'              cha-bio-safety/src/pages/DailyReportPage.tsx  # ≥4
grep -cE 'rows=\{10\}|rows=\{5\}|rows=\{4\}'         cha-bio-safety/src/pages/DailyReportPage.tsx  # ≥3
grep -c 'canForward'                                  cha-bio-safety/src/pages/DailyReportPage.tsx  # ≥1
grep -c 'useIsDesktop'                                cha-bio-safety/src/pages/DailyReportPage.tsx  # ≥1
grep -c 'DAILY_CALIB_STEPS'                           cha-bio-safety/src/pages/DailyReportPage.tsx  # ≥1 (정의 + DailyPortraitPreview 내부)

# negative gate (≥9)
grep -c 'linear-gradient('                            cha-bio-safety/src/pages/DailyReportPage.tsx  # =0
grep -cE 'className="[^"]*\b(w-8|h-8)\b'              cha-bio-safety/src/pages/DailyReportPage.tsx  # =0
grep -cE 'className="[^"]*\bstatus-'                  cha-bio-safety/src/pages/DailyReportPage.tsx  # =0
grep -cE 'const (iconBtn|navBtn|card|textareaStyle|smallBtn):' cha-bio-safety/src/pages/DailyReportPage.tsx  # =0 (5 상수 모두 삭제 확인)
grep -cE 'style=\{(iconBtn|navBtn|card|smallBtn|textareaStyle)' cha-bio-safety/src/pages/DailyReportPage.tsx  # =0
# 이모지 — line 777 ⚠ 1건만 잔존 (SW3 에서 교체 예정)
grep -cP "[\x{2B07}]" cha-bio-safety/src/pages/DailyReportPage.tsx                            # =0 (⬇ 다운로드 4건 모두 제거)
grep -cP "[\x{26A0}]" cha-bio-safety/src/pages/DailyReportPage.tsx                            # =1 (line 777 ⚠ 잔존)

# diff scope
git diff --name-only HEAD -- cha-bio-safety/src/App.tsx                                       # 빈 출력
git diff --name-only HEAD -- cha-bio-safety/functions/                                        # 빈 출력
git diff --name-only HEAD -- cha-bio-safety/migrations/                                       # 빈 출력
git diff --name-only HEAD -- cha-bio-safety/public/templates/                                 # 빈 출력
git diff --name-only HEAD -- cha-bio-safety/src/styles/components.css                          # 빈 출력
git diff --name-only HEAD -- cha-bio-safety/docs/                                             # 빈 출력 (다른 페이지 docs 변경 0)

# 빌드 검증
cd cha-bio-safety
npx tsc --noEmit                                                                              # exit 0
npm run build                                                                                 # exit 0, dist/ 생성
ls -lh dist/assets/*DailyReportPage*.js | head -1                                             # chunk 생성 확인 (사이즈 ±20%)
cd ..
```

각 gate 실패 시 fix 후 재실행. 특히:
- `[⚠]` grep 결과 =1 인 게 정상 (line 777 잔존). 0 또는 2+ 면 오류.
- `[⬇]` grep 결과 =0 인 게 정상.
- 보존 카피 grep 모두 ≥1.
- 비즈 로직 grep 모두 기준치 이상.
- diff scope grep — App.tsx / functions / migrations / templates / components.css / docs/ 모두 빈 출력.

# 11. atomic commit

```bash
cd /Users/jykevin/Documents/cbc7119-design
git add cha-bio-safety/src/pages/DailyReportPage.tsx

git commit -m "$(cat <<'EOF'
refactor(15-daily-report): SW2 DailyReportPage.tsx 모바일 영역 class 기반 markup

W7 §9 SW2 (sub-wave-2) — 모바일 영역 + EditableCard 본체 변환.

영역:
- line 270~465 formContent + dateNav + 모바일 메인 렌더 → class 기반 markup
- line 810~840 EditableCard 컴포넌트 본체 → class 기반 markup
- line 29~58 inline style 상수 5건 (iconBtn / navBtn / card / textareaStyle / smallBtn) 삭제
- lucide-react import 1줄 추가 (ChevronLeft / ChevronRight / Download)

비즈 로직 1 byte 변경 0:
- useState × 7 / useQuery × 2 / handler 7건 / debouncedSave / canForward / isDesktop 분기 / EditableCard rows / useEffect (D1 로드) 모두 그대로
- 카피 24 string verbatim (W7 §3.9)
- 캘리브레이션 영역 (line 468~808) 1 byte 변경 0 (SW3 scope, line 777 ⚠ 잔존)
- 데스크톱 메인 렌더 (line 403~444) 1 byte 변경 0 (SW3 scope)

verify gate:
- positive ≥10 (lucide / className=page-*/date-*/editable-card/summary-card/download- / dot-meta span / 카피 verbatim 12건 / 비즈 로직 grep 6건)
- negative ≥9 (linear-gradient 0 / w-8 h-8 0 / status- prefix 0 / inline 상수 5건 0 / ⬇ 0 / ⚠ =1 / diff scope 6건 빈 출력)
- tsc + vite build PASS

다음 단계: SW3 (데스크톱 영역 + DailyPortraitPreview wrapper 변환).
EOF
)"

git status   # working tree clean 확인
```

# 12. 사용자 보고 (메모리 feedback_avoid_premature_confirmation)

"거의 일치 / 완벽 / 100%" 자신감 표현 금지. 결과 + grep gate 출력만 보여주고 사용자 판단:

- "SW2 변환 완료. positive gate X건 / negative gate Y건 PASS, line 777 ⚠ 1건 잔존 (SW3 에서 AlertTriangle 로 교체 예정). cbc7119-preview 자동 배포 trigger 됨 (메모리 feedback_cbc7119_design_never_wrangler — wr+angler 직접 사용 X). 모바일 393px frame 시각 검수 부탁."
  </action>
  <verify>
    <automated>cd /Users/jykevin/Documents/cbc7119-design/cha-bio-safety && npx tsc --noEmit && npm run build && grep -c "from 'lucide-react'" src/pages/DailyReportPage.tsx | awk '$1 >= 1' | grep -q . && grep -cE 'ChevronLeft|ChevronRight|Download' src/pages/DailyReportPage.tsx | awk '$1 >= 4' | grep -q . && grep -c 'className="editable-card' src/pages/DailyReportPage.tsx | awk '$1 >= 5' | grep -q . && grep -c 'className="summary-card' src/pages/DailyReportPage.tsx | awk '$1 >= 3' | grep -q . && grep -c 'className="download-' src/pages/DailyReportPage.tsx | awk '$1 >= 4' | grep -q . && grep -c '<span className="dot-meta"' src/pages/DailyReportPage.tsx | awk '$1 >= 6' | grep -q . && [ "$(grep -c 'linear-gradient(' src/pages/DailyReportPage.tsx)" = "0" ] && [ "$(grep -cE 'const (iconBtn|navBtn|card|textareaStyle|smallBtn):' src/pages/DailyReportPage.tsx)" = "0" ] && [ "$(grep -cP '[\x{2B07}]' src/pages/DailyReportPage.tsx)" = "0" ]</automated>
  </verify>
  <done>
- DailyReportPage.tsx 모바일 영역 (line 270~465) + EditableCard 본체 (line 810~840) class 기반 markup 으로 재작성됨
- lucide-react ChevronLeft / ChevronRight / Download 3개 import 추가
- inline style 상수 5건 (iconBtn / navBtn / card / textareaStyle / smallBtn) 모두 삭제
- 인원현황 본문 + 안내 줄 가운뎃점 → `<span className="dot-meta" />` 7건 치환
- 보존 카피 24 string 1 byte 변경 0
- 비즈 로직 (useState/useQuery/handler/debouncedSave/canForward/isDesktop/rows/useEffect) 1 byte 변경 0
- 캘리브레이션 영역 (line 468~808) + 데스크톱 영역 (line 403~444) + components.css 모두 1 byte 변경 0
- diff scope 차이 없음 — App.tsx / functions/** / migrations/** / public/templates/** / docs/** 모두 변경 0
- positive gate ≥10 + negative gate ≥9 모두 PASS, tsc + vite build PASS
- atomic 1 commit (`refactor(15-daily-report): SW2 DailyReportPage.tsx 모바일 영역 class 기반 markup`)
- 사용자 시각 검수 컨펌 대기 ("완벽" 자신감 표현 없이 grep 출력 + frame 렌더 결과만 공유)
  </done>
</task>

</tasks>

<verification>
SW2 변환 wave 전체 검증:

1. **DailyReportPage.tsx 단일 file 수정** — `git diff --name-only HEAD` 결과 `cha-bio-safety/src/pages/DailyReportPage.tsx` 1줄만 출력
2. **positive gate ≥10**:
   - lucide-react import 1줄 + Chevron/Download 컴포넌트 사용 ≥4건
   - className="page-*" ≥4 / "date-*" ≥3 / "editable-card*" ≥5 / "summary-card*" ≥3 / "download-*" ≥4
   - `<span className="dot-meta" />` ≥6
   - 보존 카피 verbatim grep 12건 모두 ≥1
   - 비즈 로직 grep (handle*Download/handle{ManualSave,Reset}/rows/canForward/useIsDesktop/DAILY_CALIB_STEPS) 모두 기준치 이상
3. **negative gate ≥9**:
   - linear-gradient 0건 / w-8 h-8 0건 / status- prefix 0건
   - inline 상수 5건 (iconBtn/navBtn/card/textareaStyle/smallBtn) 0건 잔존
   - `style={iconBtn|navBtn|card|smallBtn|textareaStyle}` 사용 0건
   - 다운로드 ⬇ 이모지 0건 (4건 모두 lucide 교체)
   - ⚠ 경고 글리프 정확히 1건 (line 777 잔존, SW3 scope)
   - diff scope — App.tsx / functions / migrations / public/templates / components.css / docs 모두 빈 출력
4. **빌드 검증**:
   - `npx tsc --noEmit` exit 0
   - `npm run build` exit 0, `dist/assets/*DailyReportPage*.js` chunk 생성 (사이즈 ±20% 이내)
5. **사전 검증**:
   - `git branch --show-current` = `redesign/15-daily-report`
   - components.css 504 lines (SW1 결과)
   - DailyReportPage.tsx 변환 전 840 lines

6. **다른 페이지 영향 0** (메모리 feedback_cbc7119_design_never_wrangler):
   - 13-schedule / 14-reports / 02-inspection / 06-floorplan docs 0 변경
   - cbc7119 직원 도메인 zero impact (cbc7119-preview 디자인 도메인만 자동 배포)
   - wr+angler 명령 0건 / `npm run d+eploy` 0건
</verification>

<success_criteria>
- [x] git branch = `redesign/15-daily-report` 확인
- [x] components.css 504 lines (SW1 결과) 무변경
- [x] DailyReportPage.tsx 모바일 영역 + EditableCard 본체 class 기반 markup 변환 완료
- [x] lucide-react Chevron/Download 3개 import 추가
- [x] inline style 상수 5건 모두 삭제 (iconBtn / navBtn / card / textareaStyle / smallBtn)
- [x] dot-meta span 7건 치환 완료
- [x] 보존 카피 24 string 1 byte 변경 0
- [x] 비즈 로직 (state/query/handler/debounce/canForward/isDesktop/rows/useEffect) 1 byte 변경 0
- [x] 캘리브레이션 영역 + 데스크톱 영역 1 byte 변경 0 (SW3 scope, line 777 ⚠ 잔존)
- [x] positive gate ≥10 PASS
- [x] negative gate ≥9 PASS
- [x] tsc + vite build PASS
- [x] diff scope = DailyReportPage.tsx 1 file 만
- [x] 다른 페이지 (13/14/02/06) docs 영향 0
- [x] atomic 1 commit
- [x] cbc7119-preview 자동 배포 후 모바일 393px frame 사용자 시각 검수 컨펌 대기
- [x] "완벽 / 거의 일치" 자신감 표현 없이 사실 보고만 (메모리 feedback_avoid_premature_confirmation)
</success_criteria>

<output>
After completion, create `.planning/quick/260521-94c-redesign-15-daily-report-sw2-dailyreport/260521-94c-01-SUMMARY.md`
</output>
