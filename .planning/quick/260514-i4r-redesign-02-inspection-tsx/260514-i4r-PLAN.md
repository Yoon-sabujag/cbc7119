---
phase: 260514-i4r-redesign-02-inspection-tsx
plan: 01
type: execute
wave: 1
depends_on:
  - 260510-4li-redesign-02-inspection-sketch-main   # 1차 시안 HTML 2312줄 — 1:1 매핑 source
files_modified:
  - cha-bio-safety/src/pages/InspectionPage.tsx
autonomous: false   # Task 4 = 사용자 시각 검증 checkpoint
requirements:
  - REDESIGN-02-INSPECTION-TSX-WAVE1
tags:
  - redesign
  - inspection
  - tsx-conversion
  - design-tokens-v0.1.1
  - tailwind-only
  - wave1

must_haves:
  truths:
    - "InspectionPage 의 모든 비즈니스 로직(useQuery / useMutation / useState / useRef / useMemo / useCallback / useEffect / 핸들러 / 네비게이션 / 액션시트 / MOCK_* / API 호출 / sessionStorage / localStorage / MultiPhotoUpload / DIV-COMP 1:1 매핑 / isCpCompleted 룰)이 100% 보존된다 (변경 전후 동작 동일)"
    - "InspectionPage 의 Wave 1 변환 영역(메인 페이지 render + DesktopInspectionView + 일반 InspectionModal 셸 + ResolutionModal/ResolutionDetailModal/PhotoViewer + FireAlarmModal) 본문(JSX) 안에 인라인 style={{...}} 의 금지 키(color / background / padding / margin / fontSize / fontWeight / borderRadius / display / flex* / grid* / gap / width / height (정적값)) 사용이 0개이다"
    - "9 / 10 / 11px 폰트 사이즈 사용이 0개이다 (Wave 1 변환 영역 한정 — Tailwind text-caption ~ text-display 또는 12px 이상 arbitrary value 만)"
    - "16종 카테고리 카드(§6.3) — 좌측 3px 색바로 진척률 표현 (100% safe-bar / 50-99% accent / 1-49% warning-bar / 0% text-tertiary + opacity 0.6), 아이콘은 모두 회색 (text-secondary) 통일 (§7.2 표 색상은 카테고리 카드에 적용 안 함)"
    - "16종 카테고리 아이콘이 lucide-react 또는 src/components/ui/icons.tsx (StairsIcon / ShutterIcon / ExitSignIcon / SmokeVentIcon / HoseReelIcon / FireExtinguisherCustom) 로 통일 — 이모지(🚪/☁️/🛡️/🚗/⚡/⬜️/📊/💨/⬅️/🪟/🪢/🚰/🧯/🌊/🔔/📹) CATEGORY_GROUPS.icon 필드 사용 0건"
    - "오늘 점검 현황 카드 — 0건 empty / N건 접힘(카테고리 칩 + 결과 칩 가로 nowrap) / N건 펼침(미조치 / 조치완료 / 정상 리스트) 3 상태 모두 시안과 동일하게 시각화 (recordCount toggle 동작 보존)"
    - "DesktopInspectionView 의 16 카테고리 카드(좌측 50%) 가 §6.3 정확 반영 — 좌측 3px 색바 진척률 + 아이콘 회색. 우측 50% 의 상세/내역 리스트도 토큰화"
    - "일반 InspectionModal 의 Zone 탭(연구동/사무동/지하 + FlaskConical/Building2/TrainFront lucide), 층 칩, WheelPicker(3개 가시, ITEM_H=44 유지), 결과 3종 버튼(정상/주의/불량 + CheckCircle2/AlertTriangle/XCircle lucide), 사진 + 메모 textarea, 저장 CTA(§6.4 그라디언트)가 시안과 동일하게 시각화"
    - "InspectionRevisitPopup / AccessBlockedPopup 의 컴포넌트 자체는 수정 0건 — 페이지 측의 wrapping 컨테이너(position:relative 부모)만 Wave 1 변환 영역에서 토큰화"
    - "TypeScript 컴파일 에러 0개, /inspection 라우트 정상 렌더(라이트/다크/모바일/데스크톱 4 조합 모두)"
    - "Wave 2~6 출구 — 5 특수 모달(StairwellModal / CctvModal / BaeyeonModal / DivModal / CompressorModal / PowerPanelModal / ParkingGateModal / DamperModal) 과 5 증상 피커(InspectionModal 내부의 symptomPick / extSymptomPick / hydrantSymptomPick / shutterSymptomPick / damperSymptomPick JSX) 는 이번 변환에서 인라인 style 그대로 보존 — 후속 quick 트랙에서 처리"
  artifacts:
    - path: "cha-bio-safety/src/pages/InspectionPage.tsx"
      provides: "v0.1.1 토큰 기반 재디자인된 일반 점검 페이지 (Wave 1) — 메인 페이지 render + DesktopInspectionView + 일반 InspectionModal 셸 + Resolution* + FireAlarmModal 변환 완료. 5 특수 모달 + 5 증상 피커 JSX 는 Wave 2~6 보존."
      min_lines: 5000
      contains: "isCpCompleted, CATEGORY_GROUPS, ZONE_CONFIG, INSPECT_RESULT_OPTIONS, ALL_RESULT_OPTIONS, WheelPicker, StairwellModal, CctvModal, BaeyeonModal, DivModal, CompressorModal, PowerPanelModal, ParkingGateModal, DamperModal, InspectionModal, ResolutionDetailModal, ResolutionModal, PhotoViewer, InspectionPage, FireAlarmModal, InspectionSummaryCard, DesktopInspectionView, useInspectionRevisitPopup, AccessBlockedPopup"
  key_links:
    - from: "InspectionPage.tsx"
      to: "tailwind.config.js fontSize tokens (caption ~ display) + colors (status / text / surface / border / accent)"
      via: "Tailwind utility 클래스 (text-caption / bg-surface-raised / text-text-primary / border-border-default / bg-safe-bar 등)"
      pattern: "text-(caption|label|body-sm|body|title|heading|display)|bg-surface-(page|raised|sunken|active)|text-text-(primary|secondary|tertiary|on-accent)|border-border-(default|strong|focus)|bg-(safe|warning|danger|info|fire)(-bar|-bg)?|text-(safe|warning|danger|info|fire)"
    - from: "InspectionPage.tsx"
      to: "src/utils/api.ts (inspectionApi / fireAlarmApi / extinguisherApi / remediationApi / scheduleApi / floorPlanMarkerApi)"
      via: "동일 react-query queryKey / queryFn — 변경 없음"
      pattern: "inspectionApi\\.(getTodayRecords|getMonthRecords|getSessions|createSession|submitRecord|resolveRecord|getCheckpoints)|fireAlarmApi\\.create|remediationApi\\.(list|get)|scheduleApi\\.getByMonth"
    - from: "InspectionPage.tsx"
      to: "src/components/{InspectionRevisitPopup, AccessBlockedPopup, PhotoButton} + src/hooks/{usePhotoUpload, useInspectionRevisitPopup, useIsDesktop}"
      via: "기존 import 경로 / props 시그니처 변경 없음"
      pattern: "InspectionRevisitPopup|AccessBlockedPopup|PhotoButton|usePhotoUpload|useInspectionRevisitPopup|useIsDesktop"
    - from: "InspectionPage.tsx"
      to: "src/components/ui/icons.tsx (StairsIcon / ShutterIcon / ExitSignIcon / SmokeVentIcon / HoseReelIcon / FireExtinguisherCustom) + lucide-react (16 카테고리 + Zone 3 + 결과 3 + 도구)"
      via: "import 추가, 카테고리 그룹 icon 필드는 ReactNode/컴포넌트 참조로 교체"
      pattern: "import.*from\\s+['\"]lucide-react['\"]|import.*from\\s+['\"]\\.\\./components/ui/icons['\"]"
---

<objective>
InspectionPage TSX 변환 Wave 1 — 1차 시안(`inspection-sketch-main.html` 2312줄, audit PASS 26/0/0) 을 단일 진실 소스로 삼아 `cha-bio-safety/src/pages/InspectionPage.tsx` 의 **메인 페이지 render + DesktopInspectionView + 일반 InspectionModal 셸 + Resolution* / PhotoViewer / FireAlarmModal / InspectionSummaryCard** 를 v0.1.1 디자인 토큰 + Tailwind only 로 교체한다.

**Purpose:**
- 디자인 시스템 v0.1.1 (§6.1 Progress Color Rule + §6.3 카테고리 카드 + §6.4 그라디언트 1종 + §7.1 일관성 + §7.2 카테고리 아이콘 매핑) 을 5346줄 짜리 InspectionPage 의 메인 영역에 적용
- 5xl Dashboard 변환 패턴(인라인 style 금지 키 0건 + 9/10/11px 0건 + lucide 통일 + tokens.css 자동 라이트/다크 분기) 을 같은 룰로 InspectionPage 에도 정착
- Wave 2~6 (5 특수 모달 + 5 증상 피커) 후속 트랙의 fixed reference point 확보

**Output:**
- 비즈니스 로직 100% 보존하면서 Wave 1 변환 영역의 마크업/스타일이 시안과 1:1 매칭된 새 `InspectionPage.tsx`
- 5 특수 모달 함수(StairwellModal / CctvModal / BaeyeonModal / DivModal / CompressorModal / PowerPanelModal / ParkingGateModal / DamperModal) 와 InspectionModal 내부의 5 증상 피커 JSX(symptomPick / extSymptomPick / hydrantSymptomPick / shutterSymptomPick / damperSymptomPick) 는 **인라인 style 그대로 보존** (다음 quick 트랙에서 변환)

**Out of scope (Wave 2~6 follow-up):**
- `StairwellModal` (라인 181~410) — 특별피난계단 일괄 입력 모달
- `CctvModal` (라인 411~578) — CCTV DVR 13대 일괄 모달
- `BaeyeonModal` (라인 579~826) — 배연창 zone+층+위치 3단계 모달
- `DivModal` (라인 1036~1607) + `CompressorModal` (라인 1608~1937) — DIV/컴프 트렌드 / DivUnderPicker / DivTrendSubview
- `PowerPanelModal` (라인 1938~2144) — 소방용전원공급반
- `ParkingGateModal` (라인 2145~2345) — 주차장비/회전문
- `DamperModal` (라인 2346~2799) — 전실제연댐퍼/연결송수관 stair 일괄
- InspectionModal 내부 5 증상 피커 JSX 블록 (라인 3460~3550 부근) — symptomPick(유도등) / extSymptomPick(소화기) / hydrantSymptomPick(소화전) / shutterSymptomPick(방화셔터) / damperSymptomPick(전실제연댐퍼) — **InspectionModal 셸은 Wave 1 에서 변환하되 이 5 JSX 블록은 인라인 style 그대로 보존**
- 공통 UI 컴포넌트 (DutyChip / Donut / StatusBadge / CatBar / RoleLabel) 내부 수정 금지 — page 외부 컴포넌트
- BottomNav / DesktopSidebar / GlobalHeader / App.tsx 수정 금지
- `InspectionRevisitPopup.tsx` / `AccessBlockedPopup.tsx` 컴포넌트 본체 수정 금지 — page 측 wrapper(`position:relative` 부모 + 호출부)만 변환
- API / hooks / utils 수정 금지
- 다른 페이지 (.tsx) 수정 금지
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/STATE.md

# 5xl Dashboard 변환 전례 (697 → 775 lines, 3 tasks, 인라인 금지 키 0건, §6.2 + §7.1 적용)
@.planning/quick/260509-5xl-redesign-01-dashboard-tsx/260509-5xl-PLAN.md
@.planning/quick/260509-5xl-redesign-01-dashboard-tsx/260509-5xl-SUMMARY.md

# 1차 sketch (이 변환의 1:1 매핑 source, 2312줄 audit PASS 26/0/0)
@.planning/quick/260510-4li-redesign-02-inspection-sketch-main/260510-4li-PLAN.md
@.planning/quick/260510-4li-redesign-02-inspection-sketch-main/260510-4li-SUMMARY.md

# 페이지 메타 + 작업 지시
@cha-bio-safety/docs/redesign-context/02-inspection/02-inspection.md

# 디자인 시스템 v0.1.1 — 권위 있는 룰 (§6.1 Progress / §6.3 카테고리 카드 / §6.4 그라디언트 / §7.1 일관성 / §7.2 아이콘 매핑)
@cha-bio-safety/docs/design-system.md

# 1차 sketch HTML — Wave 1 의 마크업/색/구조 단일 진실 소스
@cha-bio-safety/docs/redesign-context/02-inspection/sketch/inspection-sketch-main.html
@cha-bio-safety/docs/redesign-context/02-inspection/sketch/inspection-audit-design-system.html
@cha-bio-safety/docs/redesign-context/02-inspection/sketch/inspection-trend-icon-decision.html

# 변환 대상 — 비즈니스 로직 100% 보존, 5346줄
@cha-bio-safety/src/pages/InspectionPage.tsx

# 토큰 / 타이포 / Tailwind 카탈로그
@cha-bio-safety/src/styles/tokens.css
@cha-bio-safety/src/styles/typography.css
@cha-bio-safety/tailwind.config.js

# 공통 UI / 외부 컴포넌트 — 그대로 사용 (수정 금지)
@cha-bio-safety/src/components/ui/index.tsx
@cha-bio-safety/src/components/ui/icons.tsx
@cha-bio-safety/src/components/PhotoButton.tsx
@cha-bio-safety/src/components/InspectionRevisitPopup.tsx
@cha-bio-safety/src/components/AccessBlockedPopup.tsx
</context>

<interfaces>
<!-- 이 페이지가 의존하는 외부 모듈의 시그니처. 변경 금지. -->
<!-- Executor 는 아래 인터페이스를 직접 사용 (codebase 추가 탐색 불필요). -->

# 1. API (src/utils/api.ts) — 호출 그대로 보존
inspectionApi.getCheckpoints(): Promise<CheckPoint[]>
inspectionApi.getTodayRecords(date: string): Promise<Record[]>      // YYYY-MM-DD
inspectionApi.getMonthRecords(yyyymm: string): Promise<Record[]>     // YYYY-MM
inspectionApi.getSessions(date: string): Promise<Session[]>
inspectionApi.createSession({ date, floor }): Promise<Session>
inspectionApi.submitRecord(sid, { checkpointId, result, memo?, photoKey?, guide_light_type?, floor_plan_marker_id? }): Promise<Record>
inspectionApi.resolveRecord(recordId, memo, photoKey?): Promise<void>
fireAlarmApi.create({ type, occurred_at, location, cause, action }): Promise<void>
remediationApi.list({ days, includeNormal? }): Promise<{ records: Record[] }>
remediationApi.get(recordId): Promise<RecordDetail>
scheduleApi.getByMonth(yyyymm): Promise<ScheduleItem[]>
floorPlanMarkerApi.list(floor: Floor, type: 'guidelamp'): Promise<FloorPlanMarker[]>
floorPlanMarkerApi.listAll(type: 'guidelamp'): Promise<FloorPlanMarker[]>
extinguisherApi.get(cpId): Promise<ExtinguisherDetail>

# 2. 외부 컴포넌트 — 그대로 사용 (수정 금지)
PhotoButton({ onClick, photoBlob?, photoPreview?, label? })            // src/components/PhotoButton.tsx
InspectionRevisitPopup({ variant, checkedAt, inspectorName, recordId?, onClose, onGoToRemediation? })  // 부모 박스 position:relative 필수
AccessBlockedPopup({ onConfirm })                                       // 부모 박스 position:relative 필수

# 3. 훅
useAuthStore() → { staff, token, ... }
useIsDesktop() → boolean
usePhotoUpload() → { blob, preview, key, uploadFromFile, pick, reset, ... }
useInspectionRevisitPopup({ allCheckpoints, monthRecords, scheduleItems, recordMeta, currentCpId, isSohwa? })
  → { popupState: { variant: 'completed'|'pending-action', checkedAt, inspectorName, recordId? } | null, dismiss(): void }
useQuery / useMutation / useQueryClient (tanstack/react-query)
useNavigate / useLocation (react-router-dom)

# 4. 디자인 토큰 (Tailwind 매핑 — tailwind.config.js 권위)
색 — bg-surface-{page|raised|sunken|active|overlay} / text-text-{primary|secondary|tertiary|disabled|on-accent|link} / border-border-{default|strong|focus} / bg-accent / bg-{safe|warning|danger|info|fire}{-bar|-bg}? / text-{safe|warning|danger|info|fire}
폰트 — text-{caption(12)|label(13)|body-sm(14)|body(16)|title(18)|heading(22)|display(28)}
radius — rounded-{sm(8)|md(12)|lg(16)|pill(99)}
spacing — p-{card|card-sm|modal|page} / gap-card-gap / 1~8 primitive(4의 배수)

# 5. 헬퍼 / 상수 — 변경 금지
isCpCompleted(entry: MonthRecordEntry | undefined): boolean   // 라인 24-27, single source of truth
NAV_BOTTOM = 'calc(54px + env(safe-area-inset-bottom, 20px))' // 라인 29
GROUND_LIST / UNDER_LIST / GROUND_SET / UNDER_SET             // 라인 32-35
CATEGORY_GROUPS: { labels, icon, color, border, categories }[16]  // 라인 38-55 — icon 필드는 이모지 문자열이지만 페이지 측에서 컴포넌트 참조로 매핑
INSPECT_RESULT_OPTIONS / ALL_RESULT_OPTIONS / RESULT_LABEL / RESULT_COLOR
ZONE_CONFIG: { key: 'research'|'office'|'underground', label, icon }[3]
matchZone(cp, zone) / getAvailableZones / getFloorsByZone

# 6. InspectionPage state (변경 금지)
allCheckpoints, glMarkerCount, loading, selectedGroupIdx, records, prevDayRecords, monthRecords, monthRecordDates,
recordCounts, markerRecords, recordMeta, showTodayDetail, showFireAlarm, sessionId, syncedAt, resolveTarget, detailTarget,
desktopCategoryIdx, desktopRecordId, desktopDateFilter

# 7. CATEGORY_GROUPS → Lucide / 커스텀 SVG 매핑 (§7.2)
// CATEGORY_GROUPS[0]  특별피난계단         → StairsIcon (custom)
// CATEGORY_GROUPS[1]  청정소화약제          → Cloud (lucide)
// CATEGORY_GROUPS[2]  전실제연댐퍼+연결송수관 → Shield (lucide)
// CATEGORY_GROUPS[3]  주차장비+회전문        → Car (lucide)
// CATEGORY_GROUPS[4]  소방용전원공급반        → Zap (lucide)
// CATEGORY_GROUPS[5]  방화셔터              → ShutterIcon (custom)
// CATEGORY_GROUPS[6]  DIV                  → BarChart3 (lucide)
// CATEGORY_GROUPS[7]  컴프레셔              → Wind (lucide)
// CATEGORY_GROUPS[8]  유도등                → ExitSignIcon (custom)
// CATEGORY_GROUPS[9]  배연창                → SmokeVentIcon (custom)
// CATEGORY_GROUPS[10] 완강기                → ArrowDownToLine (lucide)
// CATEGORY_GROUPS[11] 소화전+비상콘센트      → HoseReelIcon (custom)
// CATEGORY_GROUPS[12] 소화기                → FireExtinguisherCustom (custom)
// CATEGORY_GROUPS[13] 소방펌프              → Waves (lucide)
// CATEGORY_GROUPS[14] 화재수신반            → Bell (lucide)
// CATEGORY_GROUPS[15] CCTV                 → Video (lucide)
// 카테고리 카드 안에서는 §6.3 룰에 따라 모두 회색 (text-text-secondary). §7.2 표 색상은 본 페이지 적용 안 함.

# 8. Zone 아이콘 (§7.5)
research → FlaskConical (lucide)
office → Building2 (lucide)
underground → TrainFront (lucide)

# 9. 결과 아이콘 (§7.3)
normal → CheckCircle2 / caution → AlertTriangle / bad → XCircle / unresolved → Wrench / missing → HelpCircle / fire → Flame

# 10. 인라인 style 허용 키 화이트리스트 (5xl 패턴 그대로)
- gridTemplateRows / gridTemplateColumns 동적 분기 (예: IS_ANDROID 분기 — 본 페이지엔 없음)
- animation / transition (keyframe — Tailwind 미정의)
- transform (slide-up 모달 동적 — translateY(100%)→0)
- 동적 색 var() 직참조 (예: catColor[item.category] 같은 카테고리별 동적 색)
- paddingBottom / bottom: 'calc(... + var(--sab/sat, 0px))' (safe-area 동적)
- inset / position 의 동적 값 (NAV_BOTTOM 등 계산값)
- maxHeight / minHeight 의 calc(100dvh - var(...)) 동적

금지 키 (Wave 1 변환 영역 한정):
- color (정적 hex / rgba / var(--t*)) — 모두 Tailwind utility 로
- background (정적 hex / rgba / var(--bg*)) — 모두 Tailwind utility 로
- padding / margin (정적 px 값) — Tailwind spacing util 로
- fontSize (모든 숫자 값) — Tailwind text-* util 로
- fontWeight (정적 값) — Tailwind font-* util 로
- borderRadius (정적 px 값) — Tailwind rounded-* util 로
- display / flexDirection / flex / justifyContent / alignItems / gap (정적) — Tailwind flex/grid util 로
- width / height (정적 px / %) — Tailwind w-* h-* util 로 (단 동적 width/height 는 예외)
- border (정적 1px solid var(--bd)) — Tailwind border + border-border-default 로
</interfaces>

<design_rules_v011>
**§6.1 Progress Color Rule (카테고리 카드 좌측 색바):**
| 진척률 (doneCnt/total) | bar 토큰 | 카드 opacity |
|---|---|---|
| 100% (allDone) | `bg-safe-bar` (또는 카드 배경에 `bg-safe-bg/40` 추가 강조) | 1 |
| 50~99% | `bg-accent` | 1 |
| 1~49% | `bg-warning-bar` | 1 |
| 0% (total>0) | `bg-text-tertiary/40` | 0.6 |
| total=0 (없음) | (색바 없음, 카드 dim) | 0.38 (기존 그대로) |

**§6.2 Stat Card Negative Rule — 이번 페이지 적용 (오늘 점검 현황 카드 내부 칩):**
- "정상 N" 칩: bg-safe-bg + text-safe
- "주의 N" 칩: bg-warning-bg + text-warning
- "불량 N" 칩: bg-danger-bg + text-danger
- "미조치 N" 칩: bg-fire-bg + text-fire
- "조치완 N" 칩: bg-info-bg + text-info (또는 accent 톤)
- 단순 카운트 표시는 negative rule 아님 — 항상 status 톤 적용 OK.

**§6.3 카테고리 카드 룰 (16종 그리드 — 시안의 .cat-card 클래스 매핑):**
- 카드 자체: bg-surface-raised + border border-border-default + rounded-md + padding `px-2 py-2.5` (11px 9px 시안과 유사)
- 좌측 16px 아이콘: text-text-secondary 회색 통일 (§7.2 카탈로그 색상은 이 카드에 적용 안 함)
- 우측 라벨 + 진척률: text-text-primary text-caption font-semibold + 진척률 별도 줄
- allDone 상태: 카드 배경 `bg-safe-bg/40` + border `border-safe-bar/40`
- 0% (total>0): opacity-60
- total=0 (없음): opacity-[0.38] (기존 0.38 유지) + cursor-default
- DIV/현재 선택된 카드: border-2 border-accent + shadow `ring-2 ring-accent/20`

**§6.4 Backgrounds & Gradients:**
- 유일한 그라디언트 2종:
  1. 저장 CTA 버튼 — `bg-[linear-gradient(135deg,#1d4ed8,#0ea5e9)]` (§6.4 — Tailwind arbitrary value)
  2. (이 페이지에는 "오늘 점검 대상" 배너 없음 — Dashboard 전용)
- 그 외 모든 배경은 surface 토큰 단색

**§7.1 빠른 도구 일관성 — 이번 페이지의 결과 입력 버튼 3종(정상/주의/불량):**
- 시안의 .result-btn 클래스 매핑
- 기본: border-border-default + bg-surface-sunken + text-text-secondary
- active: border-{status}-bar + bg-{status}-bg + text-{status}
- 3개 버튼 간 일관 (정상만 큰 강조 같은 차별 금지)

**§7.2 카테고리 아이콘 — Wave 1 매핑 (interfaces 섹션 §7 표 참조):**
- 6 커스텀 SVG (Stairs/Shutter/ExitSign/SmokeVent/HoseReel/FireExtinguisherCustom) + 10 lucide
- 카테고리 카드 내부에서는 모두 text-text-secondary 회색 (§6.3)

**Hover 패턴:**
- 카드 hover: `hover:border-border-strong hover:-translate-y-px transition-all duration-150`
- 버튼 hover: `hover:bg-accent-hover` 또는 `hover:bg-surface-active`

**Animation 패턴:**
- 모달 slide-up: `style={{ transform: visible ? 'translateY(0)' : 'translateY(100%)', transition: 'transform 0.26s cubic-bezier(0.32,0.72,0,1)' }}` — 인라인 허용 (Tailwind 미정의 cubic-bezier)
- 카드 stagger: keyframe `slideUp .28s ease-out` — 인라인 허용
- blink (수신반 dot): keyframe `blink 2s ease-in-out infinite` — 인라인 허용
</design_rules_v011>

<conversion_strategy>
**시안 → TSX 변환 매핑 (5xl 패턴 그대로, InspectionPage 적용):**

1. **파일 전체 한 번에 Read → Write 전략 (5xl 권장):**
   - InspectionPage.tsx 5346줄은 Read 가능 (limit 없이 1회 또는 offset/limit 으로 영역별 1회)
   - 각 Task 마다 전체 파일 Write 1회 — 영역별 Edit 보다 인라인 잔존 방지에 안전
   - **Task 간 다른 영역은 한 줄도 건드리지 않는다** — Task 1 은 메인 page render 만, Task 2 는 DesktopInspectionView + InspectionSummaryCard 만, Task 3 은 InspectionModal 셸 + Resolution* / PhotoViewer / FireAlarmModal 만
   - 5 특수 모달(StairwellModal / CctvModal / BaeyeonModal / DivModal / CompressorModal / PowerPanelModal / ParkingGateModal / DamperModal) 본문은 **한 줄도 변경 없이 그대로 복사**
   - InspectionModal 내부 5 증상 피커 JSX 블록(라인 3460~3550 부근) 도 **한 줄도 변경 없이 그대로 복사**

2. **모든 `style={{...}}` 의 금지 키 제거 → Tailwind 또는 토큰 변수 클래스로** (5xl 매핑 그대로):
   - 색: `color: 'var(--t1)'` → `text-text-primary`
   - 배경: `background: 'var(--bg2)'` → `bg-surface-raised`
   - 테두리: `border: '1px solid var(--bd)'` → `border border-border-default`
   - 패딩: `padding: '8px 13px'` → `px-3 py-2` (또는 토큰 spacing util)
   - 폰트: `fontSize: 13` → `text-label` (12px 이상만)
   - radius: `borderRadius: 12` → `rounded-md`
   - flex/grid: `display: 'grid', gridTemplateColumns: '1fr 1fr 1fr', gap: 8` → `grid grid-cols-3 gap-2`

3. **9 / 10 / 11px 폰트 → 12px+ 로 승격 (메모리 룰 절대 준수):**
   - InspectionPage.tsx Wave 1 변환 영역 안의 모든 fontSize 9/9.5/10/10.5/11 → text-caption (12px) 으로 통일
   - 시안의 Rev 2 결정에 따라 이미 모든 텍스트가 12px 이상

4. **이모지 → Lucide / 커스텀 SVG (interfaces §7 매핑):**
   - CATEGORY_GROUPS[i].icon (이모지 문자열) 사용을 페이지 측에서 매핑 테이블로 치환
   - **CATEGORY_GROUPS 자체는 수정하지 않는다** — 5 특수 모달에서도 group.icon 을 참조하므로 그대로 둠
   - 페이지 측에서 별도 `CATEGORY_ICONS` 매핑(컴포넌트 참조 16개) 을 새로 추가하고 카테고리 카드 렌더 시 `<CATEGORY_ICONS[idx] size={20} className="text-text-secondary" />` 사용
   - 메인 페이지 + DesktopInspectionView 두 영역의 카테고리 카드에서 모두 이모지 대신 매핑 사용
   - 그 외 위치(예: 오늘 점검 현황의 카테고리 칩, 5 특수 모달 헤더 등)는 group.icon 그대로 유지 — Wave 2~6 에서 통일 (단, 메인 페이지 안 "오늘 점검 현황 → 카테고리 칩" 도 Task 1 에서 lucide/custom 으로 교체)

5. **결과 입력 버튼 → §7.1 일관성 + §7.3 Lucide:**
   - `INSPECT_RESULT_OPTIONS` 배열의 `icon` 필드(이모지)는 그대로 두고, 페이지 측 렌더에서 `value` 기반으로 lucide 컴포넌트 (CheckCircle2 / AlertTriangle / XCircle) 매핑
   - 시안의 .result-btn 클래스 매핑

6. **Zone 탭 → §7.5 Lucide:**
   - `ZONE_CONFIG` 의 `icon` 필드(이모지)는 그대로 두고, 페이지 측 렌더에서 `key` 기반으로 lucide (FlaskConical / Building2 / TrainFront) 매핑
   - 이 변환은 InspectionModal 내부 (Task 3) 에서 수행 — InspectionModal 의 Zone 탭 JSX 블록

7. **인라인 onMouseEnter / onMouseLeave hover 핸들러 → Tailwind hover:**
   - Wave 1 변환 영역에서 발견되는 모든 onMouseEnter / onMouseLeave 패턴 제거 → `hover:border-border-strong` 등 utility 로
   - 단, 5 특수 모달 내부에는 손대지 않음

8. **CSS animation keyframe 인라인 허용:**
   - `style={{ transform: visible ? 'translateY(0)' : 'translateY(100%)', transition: 'transform 0.26s cubic-bezier(0.32,0.72,0,1)' }}` — 모달 slide-up 그대로
   - `animation: 'slideUp .28s ease-out'` — Tailwind 정의 안 됨, 인라인 유지

9. **저장 CTA 그라디언트 (§6.4):**
   - `background: 'linear-gradient(135deg,#1d4ed8,#2563eb)'` → Tailwind arbitrary value `bg-[linear-gradient(135deg,#1d4ed8,#0ea5e9)]`
   - 시안 그라디언트 색 `#0ea5e9` 사용 (기존 코드 #2563eb 와 미세 차이 — 시안 권위 따름)

10. **PhotoButton / MultiPhotoUpload — 그대로 사용:**
    - 외부 컴포넌트이므로 props 만 전달, 내부 스타일은 컴포넌트 책임

11. **인라인 style 허용 키 화이트리스트 (5xl 패턴):**
    - 모달 slide-up transform/transition (Tailwind 미정의)
    - bottom: NAV_BOTTOM (calc + env safe-area, 동적)
    - maxHeight: 'calc(100dvh - ...)' (동적)
    - 카테고리/카테고리 칩의 동적 색 (단, Wave 1 에서는 §6.3 룰로 회색 통일이므로 대부분 제거 가능)
    - 5 특수 모달 / 5 증상 피커 JSX 내부 — Wave 2~6 보존
</conversion_strategy>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 메인 페이지 render + FireAlarmModal + 카테고리 16종 그리드 + 오늘 점검 현황</name>
  <files>cha-bio-safety/src/pages/InspectionPage.tsx</files>
  <behavior>
변환 후 InspectionPage 의 메인 페이지(모바일) render 영역(라인 4330~4660) + FireAlarmModal(라인 4663~4764) 가 다음을 만족해야 한다:

- import / 비즈니스 로직 / 데이터 흐름 100% 보존
- 컨테이너 `<div style={{ flex:1, minHeight:0, display:'flex', ... background:'var(--bg)' }}>` → `<div className="flex-1 min-h-0 flex flex-col overflow-hidden bg-surface-page">`
- 스크롤 영역 `<div style={{ flex:1, overflowY:'auto', padding:'12px 14px', paddingBottom:80 }}>` → `<div className="flex-1 overflow-y-auto px-3.5 py-3 pb-20">`
- 동기화 시각 (syncedAt) 라벨 — text-caption text-text-tertiary
- "오늘 점검 현황" 카드 (recordCount === 0 empty / >0 접힘 / >0 펼침 3 상태) — 시안의 `.today-card`, `.today-cat-chip`, `.today-result-chip`, `.today-item` 클래스 1:1 매핑
- 카테고리 그리드 (loading / loaded 두 상태) — `grid grid-cols-3 gap-2` + 16 카드
- 각 카테고리 카드 (§6.3 룰):
  - 16 카드 아이콘은 lucide / 커스텀 SVG 매핑 (CATEGORY_ICONS 별도 배열) + 모두 text-text-secondary 회색
  - 카드 우측 진척률 N/M / "없음" / "✓ 완료" / "기록" (화재수신반)
  - allDone: bg-safe-bg/40 + border-safe-bar/40
  - hasItems=false: opacity-[0.38] cursor-default
  - 0% (total>0, doneCnt=0): opacity-60
- 모달 dispatch wiring (`{selectedGroup && ... }`) — 5 특수 모달 props 그대로, 한 줄도 변경 없음
- FireAlarmModal(라인 4663~4764) — 헤더(Bell lucide), 본문 폼(구분/발생일시/발생장소/발생원인/조치사항), 하단 버튼 바 모두 Tailwind 로
- 인라인 style 금지 키 0개 (메인 page render + FireAlarmModal 영역 한정)
- 9/10/11px 폰트 0개 (위 영역 한정)
- TypeScript 컴파일 에러 0개
  </behavior>
  <action>
**전체 전략 — Read 1회 (필요 시 영역별), Write 1회.**

`Read /Users/jykevin/Documents/cbc7119-design/cha-bio-safety/src/pages/InspectionPage.tsx` 로 전체 또는 영역별 확보 후, **Task 1 영역(라인 1~180 import/상수, 4004~4764 render+FireAlarm) 만 변환** 한 새 버전을 `Write` 한다. 5 특수 모달 본문 + InspectionModal 본문 + Resolution* + DesktopInspectionView + InspectionSummaryCard 등 다른 영역은 **한 줄도 변경 없이 그대로 복사**.

**Step 1.1 — lucide / 커스텀 SVG import 추가 (파일 상단):**

```tsx
// 기존 import 들 아래에 추가:
import {
  Menu, ChevronLeft, Bell,
  // 카테고리 lucide (10종)
  Cloud, Shield, Car, Zap, BarChart3, Wind, ArrowDownToLine, Waves, Video,
  // Zone (3종, Task 3 에서도 사용)
  FlaskConical, Building2, TrainFront,
  // 결과 (3+2종)
  CheckCircle2, AlertTriangle, XCircle, Wrench, HelpCircle, Flame,
} from 'lucide-react'
import {
  StairsIcon, ShutterIcon, ExitSignIcon, SmokeVentIcon, HoseReelIcon, FireExtinguisherCustom,
} from '../components/ui/icons'
import type { ComponentType } from 'react'

// CATEGORY_GROUPS 정의 바로 아래(라인 56 정도)에 추가:
// 16 카테고리 아이콘 컴포넌트 매핑 (§7.2). CATEGORY_GROUPS 와 동일 순서.
// 카테고리 카드 내부에서는 §6.3 룰에 따라 모두 회색 (text-text-secondary).
const CATEGORY_ICONS: ComponentType<{ size?: number; className?: string }>[] = [
  StairsIcon,            // 0. 특별피난계단
  Cloud,                 // 1. 청정소화약제
  Shield,                // 2. 전실제연댐퍼/연결송수관
  Car,                   // 3. 주차장비/회전문
  Zap,                   // 4. 소방용전원공급반
  ShutterIcon,           // 5. 방화셔터
  BarChart3,             // 6. DIV
  Wind,                  // 7. 컴프레셔
  ExitSignIcon,          // 8. 유도등
  SmokeVentIcon,         // 9. 배연창
  ArrowDownToLine,       // 10. 완강기
  HoseReelIcon,          // 11. 소화전/비상콘센트
  FireExtinguisherCustom,// 12. 소화기
  Waves,                 // 13. 소방펌프
  Bell,                  // 14. 화재수신반
  Video,                 // 15. CCTV
]

// 결과 아이콘 매핑 (§7.3) — Task 3 의 InspectionModal 에서도 동일 매핑 재사용
const RESULT_ICONS: Record<string, ComponentType<{ size?: number; className?: string }>> = {
  normal:     CheckCircle2,
  caution:    AlertTriangle,
  bad:        XCircle,
  unresolved: Wrench,
  missing:    HelpCircle,
}
```

**Step 1.2 — 메인 page return JSX(라인 4330~4660) Tailwind 변환:**

5xl 패턴 그대로:
- 컨테이너: `<div className="flex-1 min-h-0 flex flex-col overflow-hidden bg-surface-page">`
- 스크롤 영역: `<div className="flex-1 overflow-y-auto px-3.5 pt-3 pb-20">`
- 동기화 시각:
  ```tsx
  {syncedAt && (
    <div className="flex justify-end mb-1">
      <span className="text-caption text-text-tertiary">동기화 {syncedAt.toLocaleTimeString('ko-KR', { hour:'2-digit', minute:'2-digit', second:'2-digit' })}</span>
    </div>
  )}
  ```

- **오늘 점검 현황 카드** (recordCount toggle + 3 상태 — 시안 .today-card 1:1):
  ```tsx
  <div className="bg-surface-raised border border-border-default rounded-md px-3.5 py-3 mb-4">
    <div onClick={() => recordCount > 0 && setShowTodayDetail(p => !p)}
         className={`flex items-center justify-between ${recordCount > 0 ? 'cursor-pointer' : ''}`}>
      <div className="text-caption font-semibold text-text-tertiary tracking-wider">오늘 점검 현황</div>
      <div className="flex items-center gap-1.5">
        {recordCount > 0 && (
          <div className="text-caption font-bold text-safe-bar bg-safe-bg/60 border border-safe-bar/40 rounded-pill px-2 py-0.5">
            {recordCount}건 완료
          </div>
        )}
        {recordCount > 0 && <span className="text-text-tertiary text-caption">{showTodayDetail ? '▲' : '▼'}</span>}
      </div>
    </div>

    {recordCount === 0 && (
      <div className="flex items-center gap-2 pt-2.5 pb-0.5 text-text-tertiary">
        <span className="text-[20px]">📋</span>
        <span className="text-caption">아직 점검 기록이 없습니다</span>
      </div>
    )}

    {recordCount > 0 && !showTodayDetail && (
      <>
        {/* 카테고리 칩 (gap 5, marginTop 8) */}
        <div className="flex flex-wrap gap-1.5 mt-2">
          {categoryStats.map(s => {
            const Icon = CATEGORY_ICONS[s.idx]
            return (
              <div key={s.idx}
                   className="inline-flex items-center gap-1 bg-safe-bg/40 border border-safe-bar/30 rounded-md px-2 py-0.5">
                <Icon size={14} className="text-text-secondary" />
                <span className="text-caption text-text-primary font-semibold">{s.group.labels[0]}</span>
                <span className="text-caption text-safe-bar font-bold">{s.done}</span>
              </div>
            )
          })}
        </div>
        {/* 결과 칩 (가로 nowrap scroll) */}
        <div className="flex flex-nowrap gap-1 mt-1.5 overflow-x-auto no-scrollbar">
          {/* 정상/주의/불량 3종 */}
          {([
            { key:'normal',  label:'정상', val:resultStats.normal,  chip:'normal'  },
            { key:'caution', label:'주의', val:resultStats.caution, chip:'caution' },
            { key:'bad',     label:'불량', val:resultStats.bad,     chip:'bad'     },
          ] as const).map(({ key, label, val, chip }) => {
            const Icon = RESULT_ICONS[key]
            const cls = chip === 'normal'  ? 'bg-safe-bg/40 border-safe-bar/30 text-safe-bar'
                      : chip === 'caution' ? 'bg-warning-bg/40 border-warning-bar/30 text-warning-bar'
                      :                       'bg-danger-bg/40 border-danger-bar/30 text-danger-bar'
            return (
              <div key={key} className={`inline-flex items-center gap-1 border rounded-pill px-1.5 py-0.5 shrink-0 ${cls}`}>
                <Icon size={12} />
                <span className="text-caption font-bold">{label} {val}</span>
              </div>
            )
          })}
          <span className="text-caption text-text-tertiary self-center shrink-0 px-0.5">—</span>
          {/* 미조치 / 조치완 */}
          <div className="inline-flex items-center gap-1 bg-fire-bg/40 border border-fire-bar/30 rounded-pill px-1.5 py-0.5 shrink-0">
            <Wrench size={12} className="text-fire-bar" />
            <span className="text-caption font-bold text-fire-bar">미조치 {unresolvedItems.length}</span>
          </div>
          <div className="inline-flex items-center gap-1 bg-info-bg/40 border border-info-bar/30 rounded-pill px-1.5 py-0.5 shrink-0">
            <CheckCircle2 size={12} className="text-info-bar" />
            <span className="text-caption font-bold text-info-bar">조치완 {resolvedItems.length}</span>
          </div>
        </div>
      </>
    )}

    {showTodayDetail && (
      <div className="mt-2.5 flex flex-col gap-2.5">
        {/* 미조치 항목 */}
        {unresolvedItems.length > 0 && (
          <div>
            <div className="flex items-center gap-1.5 mb-1.5">
              <Wrench size={14} className="text-danger-bar" />
              <span className="text-caption font-bold text-danger-bar">미조치 항목</span>
              <span className="text-caption font-bold text-text-on-accent bg-danger-bar rounded-pill px-1.5 py-0.5">{unresolvedItems.length}</span>
            </div>
            <div className="flex flex-col gap-1.5">
              {unresolvedItems.map(item => {
                const opt = ALL_RESULT_OPTIONS.find(o => o.value === item.result)!
                const Icon = RESULT_ICONS[item.result]
                return (
                  <div key={item.cpId} className="flex items-center gap-2 px-2.5 py-2 bg-danger-bg/30 border border-danger-bar/30 rounded-md">
                    <Icon size={14} className="text-danger-bar shrink-0" />
                    <div className="flex-1 min-w-0">
                      <div className="text-caption font-semibold text-text-primary truncate">{item.cp!.location}</div>
                      <div className="text-caption text-text-tertiary">{item.cp!.floor} · {item.cp!.category}</div>
                    </div>
                    <button onClick={() => setResolveTarget({ cpId: item.cpId, recordId: item.recordId, result: item.result, photoKey: item.photoKey, memo: item.memo })}
                            className="shrink-0 px-2 py-1 rounded-sm border border-danger-bar bg-danger-bg/40 text-danger-bar text-caption font-bold cursor-pointer hover:bg-danger-bg/60 transition-colors">
                      조치 입력
                    </button>
                  </div>
                )
              })}
            </div>
          </div>
        )}

        {/* 조치 완료 항목 — bg-info-bg/30, border-info-bar/30, text-info-bar */}
        {/* ... 동일 패턴, status 색만 info 로 (icon: CheckCircle2) */}

        {/* 정상 항목 — bg-safe-bg/30, border-safe-bar/30, text-safe-bar */}
        {/* ... 동일 패턴, status 색 safe */}

        {unresolvedItems.length === 0 && resolvedItems.length === 0 && normalItems.length === 0 && (
          <div className="text-center py-2 text-text-tertiary text-caption">점검 항목 없음</div>
        )}
      </div>
    )}
  </div>
  ```

- **카테고리 16종 그리드** (시안 .cat-card 1:1 — §6.3 + §6.1):
  ```tsx
  {loading ? (
    <div className="text-center py-8 text-text-tertiary text-label">체크포인트 불러오는 중...</div>
  ) : (
    <>
      <div className="text-caption font-semibold text-text-tertiary mb-2 tracking-wider">점검 항목 선택</div>
      <div className="grid grid-cols-3 gap-2">
        {(() => {
          const _n = new Date()
          const _todayForCycle = `${_n.getFullYear()}-${String(_n.getMonth()+1).padStart(2,'0')}-${String(_n.getDate()).padStart(2,'0')}`
          return CATEGORY_GROUPS.map((g, idx) => {
            // ... (기존 isGL / cps / total / doneCnt / allDone / hasItems 계산 그대로 보존 — 한 줄도 변경 없음)
            const Icon = CATEGORY_ICONS[idx]

            // §6.1 Progress Color Rule — bar 클래스
            const pct = total > 0 ? (doneCnt / total) * 100 : 0
            const barClass =
              total === 0 ? '' :
              pct === 0   ? 'bg-text-tertiary/40' :
              pct < 50    ? 'bg-warning-bar' :
              pct < 100   ? 'bg-accent' :
                            'bg-safe-bar'

            // 카드 컨테이너 클래스 분기
            const cardClass = [
              'relative bg-surface-raised border border-border-default rounded-md',
              'px-2.5 py-2.5 flex items-start gap-1.5 overflow-hidden min-h-[86px] box-border transition-all duration-150',
              !hasItems ? 'opacity-[0.38] cursor-default' : 'cursor-pointer hover:border-border-strong hover:-translate-y-px',
              hasItems && total > 0 && doneCnt === 0 ? 'opacity-60' : '',
              allDone ? 'bg-safe-bg/40 border-safe-bar/40' : '',
            ].filter(Boolean).join(' ')

            return (
              <div key={idx}
                   onClick={() => { if (g.categories.includes('화재수신반')) { setShowFireAlarm(true); return } if (hasItems) setSelectedGroupIdx(idx) }}
                   className={cardClass}>
                {/* §6.1 좌측 3px 색바 (total>0 일 때만) */}
                {total > 0 && <div className={`absolute left-0 top-0 bottom-0 w-[3px] ${barClass}`} />}
                <Icon size={20} className="text-text-secondary shrink-0" />
                <div className="flex-1 min-w-0 flex flex-col">
                  {g.labels.map(l => (
                    <div key={l} className="text-caption font-semibold text-text-primary truncate leading-snug">{l}</div>
                  ))}
                  <div className={`text-caption mt-0.5 font-medium ${allDone ? 'text-safe-bar font-bold' : doneCnt > 0 ? 'text-warning-bar' : 'text-text-tertiary'}`}>
                    {g.categories.includes('화재수신반') ? '기록'
                      : total === 0 ? '없음'
                      : allDone   ? '✓ 완료'
                      : doneCnt > 0 ? `${doneCnt}/${total}`
                      :               `${total}개`}
                  </div>
                </div>
              </div>
            )
          })
        })()}
      </div>
    </>
  )}
  ```

- 모달 dispatch wiring (`{selectedGroup && ...}`) — **한 줄도 변경 없음**:
  ```tsx
  {selectedGroup && (
    selectedGroup.categories.includes('DIV') ? <DivModal ... /> :
    selectedGroup.categories.includes('컴프레셔') ? <CompressorModal ... /> :
    ... // 8 분기 그대로
    <InspectionModal ... />
  )}
  {resolveTarget && <ResolutionModal ... />}
  {detailTarget && <ResolutionDetailModal ... />}
  {showFireAlarm && <FireAlarmModal onClose={() => setShowFireAlarm(false)} />}
  ```

**Step 1.3 — FireAlarmModal (라인 4663~4764) Tailwind 변환:**

기존 lbl / inp 두 CSSProperties 객체 → Tailwind 클래스 문자열 상수로 대체:
```tsx
const FA_LABEL_CLS = 'text-caption font-semibold text-text-tertiary mb-1.5 block'
const FA_INPUT_CLS = 'w-full box-border px-3 py-2.5 rounded-sm border border-border-default bg-surface-page text-text-primary text-label outline-none font-[inherit] min-w-0 [appearance:none] [-webkit-appearance:none] focus:border-border-focus transition-colors'

// 컨테이너 — top: 'var(--sat, 0px)', bottom: NAV_BOTTOM 동적 → 인라인 허용 (calc/var dynamics)
<div className="fixed left-0 right-0 z-[99] bg-surface-page flex flex-col overflow-hidden"
     style={{ top: 'var(--sat, 0px)', bottom: NAV_BOTTOM }}>

  {/* 헤더 */}
  <div className="shrink-0 bg-surface-raised border-b border-border-default px-3.5 py-2.5 flex items-center gap-2.5">
    <Bell size={20} className="text-text-secondary" />
    <span className="text-body font-bold text-text-primary">화재수신반 기록</span>
  </div>

  {/* 본문 */}
  <div className="flex-1 overflow-y-auto px-4 py-3.5">
    <div className="flex flex-col gap-3.5">
      {/* 구분 — 화재보 / 비화재보 */}
      <div>
        <label className={FA_LABEL_CLS}>구분</label>
        <div className="flex gap-2">
          {([['fire','화재보'],['non_fire','비화재보']] as const).map(([v, l]) => (
            <button key={v} onClick={() => setType(v)}
                    className={`flex-1 px-0 py-2.5 rounded-sm text-label font-bold cursor-pointer transition-colors
                                ${type===v ? 'border-2 border-danger-bar bg-danger-bg text-danger-bar'
                                           : 'border border-border-default bg-surface-sunken text-text-secondary'}`}>
              {l}
            </button>
          ))}
        </div>
      </div>
      {/* 발생일시 + 발생장소 + 발생원인 + 조치사항 — 동일 패턴, className 변환 */}
      ...
    </div>
  </div>

  {/* 하단 버튼 바 — §6.4 그라디언트 저장 CTA */}
  <div className="px-3.5 pt-2.5 pb-3 bg-surface-raised border-t border-border-default shrink-0 flex gap-2">
    <button onClick={onClose}
            className="px-4 py-3 rounded-md bg-surface-page border border-border-strong text-text-secondary text-caption font-semibold cursor-pointer hover:bg-surface-sunken transition-colors">
      닫기
    </button>
    <button onClick={handleSave} disabled={saving}
            className={`flex-1 px-0 py-3.5 rounded-md border-0 text-text-on-accent text-body font-bold transition-shadow
                        ${saving ? 'bg-border-default cursor-default'
                                 : 'bg-[linear-gradient(135deg,#1d4ed8,#0ea5e9)] cursor-pointer shadow-[0_2px_8px_rgba(37,99,235,0.3)]'}`}>
      {saving ? '저장 중...' : '점검 기록 저장'}
    </button>
  </div>
</div>
```

**Step 1.4 — 다른 영역 한 줄도 변경 없이 그대로 복사:**
- 라인 1~3866 (모든 모달 + 헬퍼) — 그대로
- 라인 4660~4662, 4765~5345 — 그대로

**Step 1.5 — npm install + tsc 0 에러 확인:**

5xl 전례에 따라 worktree 에 node_modules 없을 수 있음 → 첫 실행 시 `npm install` 필요.

```bash
cd /Users/jykevin/Documents/cbc7119-design/cha-bio-safety && \
  ([ -d node_modules ] || npm install) && \
  npx tsc --noEmit 2>&1 | tee /tmp/inspection-tsc1.log
```

`grep -i "InspectionPage" /tmp/inspection-tsc1.log` 결과 0건이어야 함.
  </action>
  <verify>
    <automated>
cd /Users/jykevin/Documents/cbc7119-design/cha-bio-safety && \
([ -d node_modules ] || npm install) >/dev/null 2>&1 && \
npx tsc --noEmit 2>&1 | grep -i "InspectionPage" > /tmp/inspection-tsc1.log; \
test ! -s /tmp/inspection-tsc1.log && \
grep -q "import.*CATEGORY_ICONS\|const CATEGORY_ICONS" src/pages/InspectionPage.tsx && \
grep -q "StairsIcon\|ShutterIcon\|ExitSignIcon\|SmokeVentIcon\|HoseReelIcon\|FireExtinguisherCustom" src/pages/InspectionPage.tsx && \
grep -q "from 'lucide-react'" src/pages/InspectionPage.tsx && \
grep -q "RESULT_ICONS\[" src/pages/InspectionPage.tsx && \
grep -q "isCpCompleted" src/pages/InspectionPage.tsx && \
grep -q "CATEGORY_GROUPS" src/pages/InspectionPage.tsx && \
grep -q "useInspectionRevisitPopup" src/pages/InspectionPage.tsx && \
grep -q "computeCardCompletion" src/pages/InspectionPage.tsx && \
grep -q "function FireAlarmModal" src/pages/InspectionPage.tsx && \
grep -q "fireAlarmApi.create" src/pages/InspectionPage.tsx && \
grep -q "setSelectedGroupIdx" src/pages/InspectionPage.tsx && \
grep -q "setShowFireAlarm" src/pages/InspectionPage.tsx && \
grep -q "setResolveTarget" src/pages/InspectionPage.tsx && \
grep -q "ResolutionDetailModal" src/pages/InspectionPage.tsx && \
grep -q "InspectionModal" src/pages/InspectionPage.tsx && \
grep -q "DivModal" src/pages/InspectionPage.tsx && \
grep -q "CompressorModal" src/pages/InspectionPage.tsx && \
grep -q "BaeyeonModal" src/pages/InspectionPage.tsx && \
grep -q "CctvModal" src/pages/InspectionPage.tsx && \
grep -q "StairwellModal" src/pages/InspectionPage.tsx && \
grep -q "PowerPanelModal" src/pages/InspectionPage.tsx && \
grep -q "ParkingGateModal" src/pages/InspectionPage.tsx && \
grep -q "DamperModal" src/pages/InspectionPage.tsx && \
LINES=$(wc -l < src/pages/InspectionPage.tsx) && [ "$LINES" -ge 5000 ] && \
echo "Task 1 PASS lines=$LINES"
    </automated>
  </verify>
  <done>
- 메인 page render(라인 4330~4660) + FireAlarmModal(라인 4663~4764) 가 Tailwind 클래스로 작성됨
- CATEGORY_ICONS 16개 매핑(lucide 10 + 커스텀 6) 추가됨
- RESULT_ICONS 5개 매핑 추가됨
- 비즈니스 로직 (useInspectionRevisitPopup / isCpCompleted / computeCardCompletion / 모든 모달 dispatch / FireAlarm save) 모두 grep 으로 검증됨
- 5 특수 모달(Stairwell/Cctv/Baeyeon/Div/Compressor/PowerPanel/ParkingGate/Damper) + InspectionModal + Resolution* + DesktopInspectionView 본문은 한 줄도 변경 없음
- TypeScript 컴파일 에러 0
- 라인 수 ≥ 5000 (5346 ± 200 범위 — 변환은 1300줄 영역만)
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: DesktopInspectionView + InspectionSummaryCard Tailwind 변환</name>
  <files>cha-bio-safety/src/pages/InspectionPage.tsx</files>
  <behavior>
- DesktopInspectionView(라인 5046~5346) 의 좌(50%) / 우(50%) 분할 구조 + 16 카테고리 카드 + 상세 보기 + 내역 목록 + 빈 상태(좌측 선택 안내) 모두 Tailwind 변환
- 좌측 카테고리 카드(라인 5140~5181) — §6.3 룰 정확 적용 (Task 1 의 CATEGORY_ICONS / barClass 계산 재사용 — 별도 module-level 헬퍼로 추출)
- 우측 상세 보기 테이블(라인 5205~5250) — th/td 토큰화
- 우측 내역 목록 카드(라인 5300~5333) — borderLeft 4px 색바를 Tailwind border-l-4 + 동적 색 클래스로
- InspectionSummaryCard(라인 4769~5043) — 헤더 + 정상/주의/불량 3 박스 + 사진 행 + 보고서 다운로드 버튼 모두 Tailwind 변환
- excludeNormal toggle / dateFilter / categoryIdx / recordId 상태 동작 100% 보존
- 인라인 style 금지 키 0개 (DesktopInspectionView + InspectionSummaryCard 영역 한정)
- 9/10/11px 폰트 0개 (위 영역 한정)
- downloadReport / downloadPhoto / toB64 / photoRow 헬퍼 그대로 보존 (HTML 보고서 내부 inline style 은 보고서 HTML 의 일부이므로 무관 — TSX 의 인라인 아님)
  </behavior>
  <action>
**Step 2.1 — Task 1 이 추가한 CATEGORY_ICONS / RESULT_ICONS 재사용:**

DesktopInspectionView 의 카테고리 카드도 동일 매핑 사용. 카드 클래스 빌더를 module-level 헬퍼로 추출 (Task 1 에서 인라인 IIFE 안에 있던 logic 을 함수화 — 단 비즈니스 로직은 동일):

```tsx
// 라인 56 (CATEGORY_ICONS) 아래에 추가:
function getCatBarClass(total: number, doneCnt: number): string {
  if (total === 0) return ''
  const pct = (doneCnt / total) * 100
  if (pct === 0)   return 'bg-text-tertiary/40'
  if (pct < 50)    return 'bg-warning-bar'
  if (pct < 100)   return 'bg-accent'
  return 'bg-safe-bar'
}
```

Task 1 의 메인 page 카드도 이 헬퍼 사용으로 일관화 (Task 1 에서 inline 으로 둔 barClass 분기를 동일 헬퍼 호출로 교체 가능).

**Step 2.2 — DesktopInspectionView 컨테이너 + 좌/우 분할:**

기존:
```tsx
<div style={{ flex:1, minHeight:0, display:'flex', overflow:'hidden', background:'var(--bg)' }}>
  <div style={{ width:'50%', flexShrink:0, ... borderRight:'1px solid var(--bd)', ... }}>
```

변환:
```tsx
<div className="flex-1 min-h-0 flex overflow-hidden bg-surface-page">
  {/* 좌측 50% — 카테고리 카드 */}
  <div className="w-1/2 shrink-0 min-w-0 border-r border-border-default flex flex-col">
    {/* 좌측 헤더 — 점검 항목 + 기간 필터 */}
    <div className="shrink-0 px-5 py-3 border-b border-border-default bg-surface-raised flex items-center gap-2.5">
      <span className="text-body-sm font-bold text-text-primary flex-1">점검 항목</span>
      <span className="text-caption text-text-tertiary">
        {dateFilter === -1 ? '이번달' : dateFilter === 0 ? '전체' : `최근 ${dateFilter}일`}
      </span>
      <div className="flex gap-1">
        {PERIOD_BUTTONS.map(b => (
          <button key={b.value} onClick={() => setDateFilter(b.value)}
                  className={`px-2.5 py-1 rounded-sm border-0 text-caption font-bold cursor-pointer transition-colors
                              ${dateFilter === b.value
                                ? 'bg-accent text-text-on-accent'
                                : 'bg-surface-sunken text-text-tertiary hover:bg-surface-active'}`}>
            {b.label}
          </button>
        ))}
      </div>
    </div>
    {/* 좌측 본문 — 카테고리 그리드 */}
    <div className="flex-1 overflow-y-auto px-4.5 py-3.5">
      <div className="grid grid-cols-[repeat(auto-fill,minmax(170px,1fr))] gap-2.5">
        {CATEGORY_GROUPS.map((g, idx) => {
          const c = groupCounts[idx]
          const isSel = categoryIdx === idx
          const Icon = CATEGORY_ICONS[idx]
          const barClass = getCatBarClass(c.total, c.completed)
          return (
            <div key={idx}
                 onClick={() => { setCategoryIdx(idx); setRecordId(null) }}
                 className={`relative bg-surface-raised border rounded-md px-2.5 py-3 cursor-pointer flex flex-col gap-1.5 min-h-[100px] overflow-hidden transition-all duration-150 hover:border-border-strong hover:-translate-y-px
                             ${isSel ? 'border-2 border-accent ring-2 ring-accent/20' : 'border-border-default'}`}>
              {/* §6.1 좌측 3px 색바 */}
              {c.total > 0 && <div className={`absolute left-0 top-0 bottom-0 w-[3px] ${barClass}`} />}
              <div className="flex items-center gap-1.5">
                <Icon size={20} className="text-text-secondary shrink-0" />
                <div className="flex-1 min-w-0">
                  {g.labels.map(l => (
                    <div key={l} className="text-caption font-bold text-text-primary leading-snug truncate">{l}</div>
                  ))}
                </div>
              </div>
              <div className="flex items-end justify-between gap-1 mt-auto">
                <div className="flex gap-0.5 flex-wrap flex-1 min-w-0">
                  {c.bad === 0 && c.caution === 0 ? (
                    <span className="text-caption text-text-tertiary">이슈 없음</span>
                  ) : (
                    <>
                      {c.bad > 0 && (
                        <span className="text-caption font-bold text-danger bg-danger-bg px-1.5 py-0.5 rounded-sm">불량 {c.bad}</span>
                      )}
                      {c.caution > 0 && (
                        <span className="text-caption font-bold text-warning bg-warning-bg px-1.5 py-0.5 rounded-sm">주의 {c.caution}</span>
                      )}
                    </>
                  )}
                </div>
                {c.completed > 0 && (
                  <span className="text-caption font-bold text-safe-bar bg-safe-bg/60 px-1.5 py-0.5 rounded-sm shrink-0">
                    ✓ 점검완료 {c.completed}
                  </span>
                )}
              </div>
            </div>
          )
        })}
      </div>
    </div>
  </div>

  {/* 우측 50% — 상세 또는 내역 */}
  <div className="w-1/2 shrink-0 min-w-0 flex flex-col">
    {recordId && detail ? (
      // 상세 보기 — 헤더 + 테이블 + 사진
      <>
        <div className="shrink-0 px-5 py-3 border-b border-border-default bg-surface-raised flex items-center gap-2.5">
          <button onClick={() => setRecordId(null)}
                  className="w-8 h-8 rounded-sm bg-surface-sunken border border-border-default cursor-pointer flex items-center justify-center hover:bg-surface-active transition-colors">
            <ChevronLeft size={14} className="text-text-secondary" />
          </button>
          <span className="text-body-sm font-bold text-text-primary flex-1">조치 상세</span>
          <button onClick={() => navigate('/remediation/' + recordId)}
                  className="px-3 py-1 rounded-sm bg-surface-sunken border border-border-default text-text-secondary text-caption font-semibold cursor-pointer hover:bg-surface-active transition-colors">
            전체 화면 열기
          </button>
        </div>
        <div className="flex-1 overflow-y-auto px-5.5 py-4">
          <div className="text-body font-bold text-text-primary mb-1">{(detail as any).category}</div>
          <div className="text-caption text-text-tertiary mb-3.5">
            {ZONE_LABEL[(detail as any).zone] ?? (detail as any).zone} {(detail as any).floor}
            {(detail as any).location ? ` · ${(detail as any).location}` : ''}
          </div>
          {/* 테이블 — 5xl 의 InspectionSummaryCard 박스 패턴과 유사하게 토큰화 */}
          <table className="w-full border-collapse mb-4">
            <tbody>
              {/* th: bg-surface-sunken border border-border-default text-caption font-bold text-text-secondary text-left align-top px-2.5 py-1.5 w-[90px] */}
              {/* td: border border-border-default text-label text-text-primary whitespace-pre-wrap align-top px-2.5 py-1.5 */}
              ...
            </tbody>
          </table>
          {/* 사진 grid 2-col */}
          <div className="grid grid-cols-2 gap-3">
            {[ /* 조치 전 / 조치 후 */ ].map(...)}
          </div>
        </div>
      </>
    ) : categoryIdx !== null ? (
      // 카테고리 내역 목록
      <>
        <div className="shrink-0 px-5 py-3 border-b border-border-default bg-surface-raised flex items-center gap-2.5">
          {(() => { const Icon = CATEGORY_ICONS[categoryIdx]; return <Icon size={20} className="text-text-secondary" /> })()}
          <span className="text-body-sm font-bold text-text-primary flex-1">{CATEGORY_GROUPS[categoryIdx].labels.join(', ')}</span>
          <button onClick={() => setExcludeNormal(!excludeNormal)}
                  className={`px-2.5 py-1 rounded-sm text-caption font-bold cursor-pointer transition-colors
                              ${excludeNormal ? 'bg-accent/15 border border-accent text-accent' : 'bg-surface-sunken border border-border-default text-text-tertiary'}`}>
            {excludeNormal ? '✓ 정상 제외' : '정상 제외'}
          </button>
          <span className="text-caption text-text-tertiary">{categoryRecords.length}건</span>
        </div>
        <div className="flex-1 overflow-y-auto px-4.5 py-3.5">
          <InspectionSummaryCard categoryIdx={categoryIdx} allRecords={allRecords} />
          {isLoading ? (
            <div className="text-center py-10 text-text-tertiary text-caption">불러오는 중...</div>
          ) : categoryRecords.length === 0 ? (
            <div className="text-center py-10 text-text-tertiary text-caption">점검 내역이 없습니다</div>
          ) : (
            <div className="flex flex-col gap-2">
              {categoryRecords.map(r => {
                const isIssue = r.result === 'bad' || r.result === 'caution'
                const isResolved = isIssue && r.status === 'resolved'
                const borderColorClass = isResolved ? 'border-l-info-bar'
                                       : r.result === 'bad' ? 'border-l-danger-bar'
                                       : r.result === 'caution' ? 'border-l-warning-bar'
                                       : 'border-l-safe-bar'
                return (
                  <div key={r.id} onClick={() => isIssue && setRecordId(r.id)}
                       className={`bg-surface-raised border border-border-default border-l-4 ${borderColorClass} rounded-md px-3 py-2.5 ${isIssue ? 'cursor-pointer hover:bg-surface-sunken' : 'cursor-default'} flex flex-col gap-1 transition-colors`}>
                    <div className="flex items-center gap-2">
                      <span className="text-label font-bold text-text-primary flex-1">{r.category}</span>
                      <span className={`text-caption font-bold px-1.5 py-0.5 rounded-sm
                                        ${r.result === 'bad'     ? 'bg-danger-bg  text-danger'
                                        : r.result === 'caution' ? 'bg-warning-bg text-warning'
                                        :                          'bg-safe-bg    text-safe'}`}>
                        {r.result === 'bad' ? '불량' : r.result === 'caution' ? '주의' : '정상'}
                      </span>
                      {isIssue && (
                        <span className={`text-caption font-bold px-1.5 py-0.5 rounded-sm
                                          ${r.status === 'open' ? 'bg-fire-bg text-fire' : 'bg-info-bg text-info'}`}>
                          {r.status === 'open' ? '미조치' : '조치완료'}
                        </span>
                      )}
                    </div>
                    <div className="text-caption text-text-secondary">
                      {(ZONE_LABEL[r.zone] ?? r.zone)} {r.floor}{r.location ? ` · ${r.location}` : ''}
                    </div>
                    {r.memo && <div className="text-caption text-text-tertiary truncate">{r.memo.split('\n')[0]}</div>}
                    <div className="text-caption text-text-tertiary">{fmtDate(r.checkedAt)}</div>
                  </div>
                )
              })}
            </div>
          )}
        </div>
      </>
    ) : (
      <div className="flex-1 flex items-center justify-center text-text-tertiary text-label">
        좌측에서 점검 항목을 선택하세요
      </div>
    )}
  </div>
</div>
```

**Step 2.3 — InspectionSummaryCard (라인 4769~5043) Tailwind 변환:**

기존 컨테이너 + 헤더 + 3박스(정상/주의/불량) + 사진 행 모두 Tailwind 로:
```tsx
return (
  <div id={`summary-card-${categoryIdx}`}
       className="bg-surface-raised border border-border-default rounded-md p-4 mb-3.5">
    <div className="flex items-center justify-between mb-3">
      <div>
        <div className="text-body-sm font-bold text-text-primary flex items-center gap-1.5">
          {(() => { const Icon = CATEGORY_ICONS[categoryIdx]; return <Icon size={16} className="text-text-secondary" /> })()}
          {schedTitle}
        </div>
        <div className="text-caption text-text-tertiary mt-0.5">
          점검일: {schedDateLabel} · 총 {filteredRecords.length}건
        </div>
        {schedInfos.map((s, i) => s.memo && (
          <div key={i} className="text-caption text-text-tertiary mt-0.5 whitespace-pre-wrap leading-snug">
            {schedInfos.length > 1 && <span className="font-semibold text-text-secondary">[{s.title}]</span>}
            {schedInfos.length > 1 ? '\n' : ''}{s.memo}
          </div>
        ))}
      </div>
      <button onClick={downloadReport}
              className="text-caption font-bold h-8 bg-surface-sunken rounded-sm px-3 border border-border-default text-text-primary cursor-pointer hover:bg-surface-active transition-colors">
        보고서 다운로드
      </button>
    </div>
    <div className="grid grid-cols-3 gap-2.5">
      {/* 정상 */}
      <div className="bg-safe-bg/40 border border-safe-bar/40 rounded-md p-3">
        <div className="flex items-center justify-between mb-1">
          <span className="text-caption font-bold text-safe">정상</span>
          <span className="text-[20px] font-bold font-mono text-safe">{normalRecs.length}</span>
        </div>
        {photoRow(normalPhotos, 'rgba(34,197,94,0.5)')}
      </div>
      {/* 주의 — 동일 패턴, warning 색 */}
      <div className="bg-warning-bg/40 border border-warning-bar/40 rounded-md p-3">
        ...
      </div>
      {/* 불량 — 동일 패턴, danger 색 */}
      <div className="bg-danger-bg/40 border border-danger-bar/40 rounded-md p-3">
        ...
      </div>
    </div>
  </div>
)
```

**photoRow 헬퍼 Tailwind 변환:**
```tsx
const photoRow = (photos: { key: string; label: string }[], color: string) => {
  if (photos.length === 0) return null
  return (
    <div className="flex gap-1.5 overflow-x-auto pt-1.5 no-scrollbar">
      {photos.map((p, i) => (
        <div key={i} className="relative shrink-0">
          <img src={`/api/uploads/${p.key}`} alt={p.label}
               className="w-14 h-14 object-cover rounded-sm cursor-pointer block border-2"
               style={{ borderColor: color }}  // 동적 색 — 인라인 허용 (rgba alpha 토큰 미정의)
               onClick={() => downloadPhoto(p.key, `${p.label}.jpg`)}
               title="클릭하여 다운로드" />
        </div>
      ))}
    </div>
  )
}
```

**downloadReport / toB64 / downloadPhoto 함수 본문은 한 줄도 변경 없음** — 보고서 HTML 출력 안의 inline style 은 TSX 인라인이 아니라 HTML 문자열이므로 grep 검출에서 제외 가능 (단 grep gate 는 TSX style={{ ... }} 패턴만 검사).

**Step 2.4 — 다른 영역 한 줄도 변경 없이 복사:**
- 라인 1~4769 (Task 1 결과 그대로) — 보존
- InspectionSummaryCard / DesktopInspectionView 만 변환
- DesktopInspectionView 가 사용하는 InspectionSummaryCard import 는 변경 없음 (같은 파일 내 함수)

**Step 2.5 — tsc 0 에러 확인:**

```bash
cd /Users/jykevin/Documents/cbc7119-design/cha-bio-safety && \
  npx tsc --noEmit 2>&1 | grep -i "InspectionPage" > /tmp/inspection-tsc2.log
test ! -s /tmp/inspection-tsc2.log
```
  </action>
  <verify>
    <automated>
cd /Users/jykevin/Documents/cbc7119-design/cha-bio-safety && \
npx tsc --noEmit 2>&1 | grep -i "InspectionPage" > /tmp/inspection-tsc2.log; \
test ! -s /tmp/inspection-tsc2.log && \
grep -q "function DesktopInspectionView" src/pages/InspectionPage.tsx && \
grep -q "function InspectionSummaryCard" src/pages/InspectionPage.tsx && \
grep -q "PERIOD_BUTTONS" src/pages/InspectionPage.tsx && \
grep -q "setExcludeNormal" src/pages/InspectionPage.tsx && \
grep -q "remediationApi.list" src/pages/InspectionPage.tsx && \
grep -q "remediationApi.get" src/pages/InspectionPage.tsx && \
grep -q "downloadReport" src/pages/InspectionPage.tsx && \
grep -q "ZONE_LABEL" src/pages/InspectionPage.tsx && \
grep -q "groupCounts" src/pages/InspectionPage.tsx && \
grep -q "categoryRecords" src/pages/InspectionPage.tsx && \
grep -q "border-l-danger-bar\|border-l-warning-bar\|border-l-safe-bar\|border-l-info-bar" src/pages/InspectionPage.tsx && \
grep -q "getCatBarClass\|bg-safe-bar\|bg-warning-bar\|bg-text-tertiary" src/pages/InspectionPage.tsx && \
echo "Task 2 PASS"
    </automated>
  </verify>
  <done>
- DesktopInspectionView 좌(50%) / 우(50%) 분할 + 16 카테고리 카드 + 상세/내역/빈 상태 모두 Tailwind 변환
- §6.3 룰 정확 적용 — DesktopView 카테고리 카드도 좌측 3px 색바 + 아이콘 회색
- InspectionSummaryCard 헤더 + 3박스(정상/주의/불량) + 사진 행 모두 Tailwind
- excludeNormal toggle, dateFilter, categoryIdx, recordId, downloadReport / toB64 / downloadPhoto / photoRow 모두 보존
- 인라인 style 금지 키 0개 (DesktopInspectionView + InspectionSummaryCard 영역 한정)
- 9/10/11px 폰트 0개 (위 영역 한정)
- TypeScript 컴파일 에러 0
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: 일반 InspectionModal 셸 + Resolution* + PhotoViewer + verify gate 마무리</name>
  <files>cha-bio-safety/src/pages/InspectionPage.tsx</files>
  <behavior>
- 일반 InspectionModal(라인 2800~3779) 의 **셸** 변환:
  - 컨테이너 (slide-up modal, transform/transition 동적 — 인라인 허용)
  - 헤더 (group.labels + 닫기 버튼) — group.icon 자리에 CATEGORY_ICONS[selectedGroupIdx] 사용
  - Zone 탭 3종 (FlaskConical / Building2 / TrainFront) — `ZONE_CONFIG` 의 key 기반 매핑
  - 층 칩 (availableFloors)
  - WheelPicker 사용부 (WheelPicker 자체는 라인 103~169 — 내부 인라인 그대로, 단 호출부 컨테이너만 토큰화)
  - 결과 입력 3종 버튼 (정상/주의/불량) — §7.1 일관성 + §7.3 lucide 아이콘 (CheckCircle2 / AlertTriangle / XCircle)
  - 사진 입력 (PhotoButton — props 그대로)
  - 메모 textarea
  - 저장 / 취소 CTA (§6.4 그라디언트)
  - submitError / justSaved 박스
  - 부분 오버레이 wrapper (InspectionRevisitPopup / AccessBlockedPopup) — 부모 `position:relative` 보존 + popup 컴포넌트 props 그대로
- **5 증상 피커 JSX 블록**(라인 ~3460~3550 영역의 symptomPick / extSymptomPick / hydrantSymptomPick / shutterSymptomPick / damperSymptomPick) — **한 줄도 변경 없이 그대로 보존** (인라인 style 유지, Wave 2 follow-up)
- ResolutionModal(라인 3878~4003) Tailwind 변환 — 모달 셸 + 메모 textarea + 사진 + 저장 CTA
- ResolutionDetailModal(라인 3779~3852) Tailwind 변환 — 점검 시 / 조치 후 grid + 사진 viewer 트리거
- PhotoViewer(라인 3868~3877) Tailwind 변환 — 전체화면 다크 오버레이 + img
- WheelPicker(라인 103~169) — 내부 fontSize 13/11/10 → text-label/text-caption 으로 승격(11px → 12px, 10px → 12px) **단, fontSize:dist===0 ? 13 : 11 같은 동적 분기는 className 동적 분기로 변환**
- 인라인 style 금지 키 0개 (위 변환 영역 한정 — 5 증상 피커 JSX 블록 제외)
- 9/10/11px 폰트 0개 (위 영역 한정 — 5 증상 피커 JSX 블록 제외)
- TypeScript 컴파일 에러 0
- 전체 파일 `npm run build` 성공
- WheelPicker / InspectionModal 비즈니스 로직 (snapTo, handleScroll, activeIdx, pickerSourceCPs, isGuideLight 분기, MARKER_TO_GL_COL, GL_COL_LABEL, 소화전/비상콘센트 혼합 그룹, dismissedBlockedId, isAccessBlocked, showAccessBlockedPopup, bcPhoto, bcResult, bcMemo, extSymptomPick / hydrantSymptomPick / shutterSymptomPick / damperSymptomPick / symptomPick state 등) 모두 보존
  </behavior>
  <action>
**Step 3.1 — WheelPicker(라인 103~169) 내부 fontSize 12px+ 승격:**

기존:
```tsx
<div style={{ ... fontSize:dist===0 ? 13 : 11, fontWeight:dist===0 ? 700 : 400, color:'var(--t1)', ... }}>
{done && <span style={{ fontSize:10, ... color:RESULT_COLOR[done], ... }}>{RESULT_LABEL[done]}</span>}
```

변환 — 동적 fontSize → 동적 className. RESULT_COLOR 동적 색은 className 동적 분기로:
```tsx
<div className={`flex-1 truncate text-text-primary ${dist===0 ? 'text-label font-bold' : 'text-caption font-normal'}`}>
  {item.location}
</div>
{done && (
  <span className={`text-caption font-bold shrink-0 ml-2 ${
    done === 'normal'     ? 'text-safe-bar'
  : done === 'caution'    ? 'text-warning-bar'
  : done === 'bad'        ? 'text-danger-bar'
  : done === 'unresolved' ? 'text-fire-bar'
  :                          'text-text-tertiary'
  }`}>
    {RESULT_LABEL[done]}
  </span>
)}
```

WheelPicker 컨테이너 / 중앙 하이라이트 / 페이드 / 스크롤 영역 — Tailwind 로:
```tsx
<div className="relative rounded-md overflow-hidden bg-surface-raised border border-border-default"
     style={{ height: containerH }}>  {/* 동적 height (ITEM_H * VISIBLE 계산) — 인라인 허용 */}
  {/* 중앙 하이라이트 */}
  <div className="absolute top-1/2 left-0 right-0 -translate-y-1/2 bg-info-bg/40 border-t border-b border-info-bar/30 pointer-events-none z-[2]"
       style={{ height: ITEM_H }} />
  {/* 상/하단 페이드 */}
  <div className="absolute top-0 left-0 right-0 pointer-events-none z-[3]"
       style={{ height: pad, background: 'linear-gradient(to bottom, var(--surface-raised) 30%, transparent)' }} />
  <div className="absolute bottom-0 left-0 right-0 pointer-events-none z-[3]"
       style={{ height: pad, background: 'linear-gradient(to top, var(--surface-raised) 30%, transparent)' }} />
  <div ref={scrollRef} onScroll={handleScroll}
       className="h-full overflow-y-auto [scrollbar-width:none] [&::-webkit-scrollbar]:hidden box-border [scroll-snap-type:y_mandatory]"
       style={{ paddingTop: pad, paddingBottom: pad }}>
    {items.map((item, idx) => {
      const dist = Math.abs(idx - activeIdx)
      const done = records[item.id]
      const opacityClass = dist === 0 ? 'opacity-100' : dist === 1 ? 'opacity-[0.48]' : 'opacity-[0.15]'
      return (
        <div key={item.id}
             className={`flex items-center px-3.5 cursor-pointer transition-opacity ${opacityClass} [scroll-snap-align:center]`}
             style={{ height: ITEM_H }}>
          {/* 위의 동적 className 텍스트 + done 배지 */}
        </div>
      )
    })}
  </div>
</div>
```

**Step 3.2 — InspectionModal 셸 변환 (라인 2800~3779):**

매우 큰 함수 (979줄). 영역별로:

**Step 3.2a — 컨테이너 + 헤더 + 닫기:**
```tsx
<div className="fixed left-0 right-0 z-[99] bg-surface-page flex flex-col overflow-hidden"
     style={{ top: 'var(--sat, 0px)', bottom: NAV_BOTTOM, transform: visible ? 'translateY(0)' : 'translateY(100%)', transition: 'transform 0.26s cubic-bezier(0.32,0.72,0,1)' }}>
  {/* 헤더 */}
  <div className="shrink-0 bg-surface-raised border-b border-border-default px-3.5 py-2.5 flex items-center gap-2.5">
    {(() => {
      const groupIdx = CATEGORY_GROUPS.findIndex(g => g === group)
      const Icon = groupIdx >= 0 ? CATEGORY_ICONS[groupIdx] : null
      return Icon ? <Icon size={20} className="text-text-secondary" /> : null
    })()}
    <div className="flex-1 min-w-0">
      <div className="text-label font-bold text-text-primary truncate">{group.labels[0]}</div>
      {group.labels.length > 1 && (
        <div className="text-caption text-text-tertiary truncate">{group.labels.slice(1).join(' · ')}</div>
      )}
    </div>
    <button onClick={() => setVisible(false /* + onClose timeout 동일 */)}
            className="shrink-0 w-9 h-9 rounded-sm bg-surface-sunken border border-border-default flex items-center justify-center cursor-pointer hover:bg-surface-active transition-colors">
      <span className="text-text-secondary text-body">×</span>
    </button>
  </div>
```

**Step 3.2b — Zone 탭 + 층 칩:**

`ZONE_CONFIG` 의 `icon` (이모지) 자리에 lucide 매핑:
```tsx
const ZONE_LUCIDE: Record<ZoneKey, ComponentType<{ size?: number; className?: string }>> = {
  research: FlaskConical,
  office: Building2,
  underground: TrainFront,
}

// Zone 탭
<div className="px-3.5 pt-2.5 shrink-0">
  <div className="text-caption font-semibold text-text-tertiary mb-1.5 tracking-wider">구역 선택</div>
  <div className="flex gap-1.5">
    {availableZones.map(z => {
      const cfg = ZONE_CONFIG.find(c => c.key === z)!
      const Icon = ZONE_LUCIDE[z]
      const isSel = selectedZone === z
      return (
        <button key={z} onClick={() => { setSelectedZone(z); setSelectedFloor(null) }}
                className={`flex-1 px-1 py-2 rounded-sm text-caption font-bold cursor-pointer flex items-center justify-center gap-1.5 transition-colors
                            ${isSel
                              ? 'bg-accent/15 border-2 border-accent text-accent'
                              : 'bg-surface-sunken border border-border-default text-text-secondary hover:bg-surface-active'}`}>
          <Icon size={14} />
          {cfg.label}
        </button>
      )
    })}
  </div>
</div>

{/* 층 칩 */}
<div className="px-3.5 pt-2.5 shrink-0">
  <div className="text-caption font-semibold text-text-tertiary mb-1.5 tracking-wider">층 선택</div>
  <div className="flex flex-wrap gap-1.5">
    {availableFloors.map(f => {
      const fDone = /* 기존 로직 그대로 */
      const isSel = selectedFloor === f
      return (
        <button key={f} onClick={() => { setSelectedFloor(f); setPickerIdx(0) }}
                className={`px-3 py-1.5 rounded-sm text-caption font-bold cursor-pointer transition-colors
                            ${isSel
                              ? 'bg-accent text-text-on-accent'
                              : fDone
                                ? 'bg-safe-bg border border-safe-bar/40 text-safe-bar'
                                : 'bg-surface-sunken border border-border-default text-text-secondary hover:bg-surface-active'}`}>
          {f}{fDone && <CheckCircle2 size={10} className="inline ml-0.5 opacity-75" />}
        </button>
      )
    })}
  </div>
</div>
```

**Step 3.2c — WheelPicker 호출부 (부모 박스가 position:relative 여야 함 — InspectionRevisitPopup / AccessBlockedPopup 의 부분 오버레이 룰):**

```tsx
<div className="relative flex-1 overflow-y-auto px-3.5 py-2.5">
  {/* WheelPicker — 위 Step 3.1 에서 변환 완료, 단순 호출 */}
  <WheelPicker items={pickerSourceCPs} onSelect={setPickerIdx} records={records} />

  {/* 부분 오버레이 — Revisit 또는 AccessBlocked */}
  {showAccessBlockedPopup ? (
    <AccessBlockedPopup onConfirm={() => { /* 기존 핸들러 그대로 */ }} />
  ) : popupState ? (
    <InspectionRevisitPopup
      variant={popupState.variant}
      checkedAt={popupState.checkedAt}
      inspectorName={popupState.inspectorName}
      recordId={popupState.recordId}
      onClose={dismiss}
      onGoToRemediation={(rid) => { dismiss(); navigate('/remediation/' + rid) }}
    />
  ) : null}

  {/* 결과 입력 3종 + 사진 + 메모 + 저장 */}
  <div className="mt-3 flex flex-col gap-3">
    {/* 결과 3종 (§7.1 일관성 + §7.3 lucide) */}
    <div>
      <div className="text-caption font-semibold text-text-tertiary mb-1.5 tracking-wider">점검 결과</div>
      <div className="flex gap-1.5">
        {INSPECT_RESULT_OPTIONS.map(opt => {
          const Icon = RESULT_ICONS[opt.value]
          const isSel = result === opt.value
          const activeCls = opt.value === 'normal'  ? 'border-safe-bar bg-safe-bg text-safe'
                          : opt.value === 'caution' ? 'border-warning-bar bg-warning-bg text-warning'
                          :                           'border-danger-bar bg-danger-bg text-danger'
          return (
            <button key={opt.value} onClick={() => setResult(opt.value)}
                    className={`flex-1 flex flex-col items-center justify-center gap-1 h-button rounded-sm border cursor-pointer transition-colors
                                ${isSel ? `${activeCls}` : 'border-border-default bg-surface-sunken text-text-secondary hover:bg-surface-active'}`}>
              <Icon size={16} />
              <span className="text-label font-semibold">{opt.label}</span>
            </button>
          )
        })}
      </div>
    </div>

    {/* ▼▼▼ 5 증상 피커 JSX 블록 — 한 줄도 변경 없이 그대로 보존 ▼▼▼ */}
    {/* Wave 2 follow-up — symptomPick / extSymptomPick / hydrantSymptomPick / shutterSymptomPick / damperSymptomPick */}
    {/* (기존 라인 3460~3550 인라인 style 코드 그대로 복사) */}
    {/* ▲▲▲ 5 증상 피커 JSX 블록 끝 ▲▲▲ */}

    {/* 특이사항 / 사진 — 라벨 */}
    <div>
      <div className="flex items-center justify-between mb-1.5">
        <label className="text-caption font-semibold text-text-tertiary tracking-wider">특이사항 (선택)</label>
        <span className="text-caption text-text-tertiary">점검 사진 (선택)</span>
      </div>
      <div className="flex gap-2 items-start">
        <textarea value={memo} onChange={e => setMemo(e.target.value)} rows={3}
                  className="flex-1 box-border px-3 py-2.5 rounded-sm border border-border-default bg-surface-page text-text-primary text-label resize-none outline-none font-[inherit] focus:border-border-focus transition-colors"
                  placeholder="점검 특이사항을 입력하세요" />
        <PhotoButton {...photo} />  {/* 외부 컴포넌트, 그대로 */}
      </div>
    </div>

    {/* 소화전/비상콘센트 혼합 그룹의 bcResult / bcMemo / bcPhoto JSX — 동일 패턴 */}
    {isSohwaGroup && /* ... */}

    {/* submitError / justSaved */}
    {submitError && (
      <div className="bg-danger-bg/40 border border-danger-bar/30 rounded-sm px-3 py-2 text-label text-danger">
        {submitError}
      </div>
    )}
    {justSaved && (
      <div className="bg-safe-bg/40 border border-safe-bar/30 rounded-sm px-3 py-2 text-label text-safe">
        <CheckCircle2 size={14} className="inline mr-1 -mt-0.5" /> 저장 완료
      </div>
    )}
  </div>
</div>

{/* 하단 CTA — §6.4 그라디언트 (저장:취소 = 2:1) */}
<div className="shrink-0 px-3.5 pt-2.5 pb-3 bg-surface-raised border-t border-border-default flex gap-2">
  <button onClick={onClose}
          className="flex-1 h-button rounded-sm bg-surface-page border border-border-strong text-text-secondary text-label font-semibold cursor-pointer hover:bg-surface-sunken transition-colors whitespace-nowrap">
    취소
  </button>
  <button onClick={handleSubmit /* 기존 핸들러 그대로 */} disabled={submitting}
          className={`flex-[2] h-button rounded-sm border-0 text-text-on-accent text-body font-bold transition-shadow
                      ${submitting ? 'bg-border-default cursor-default'
                                   : 'bg-[linear-gradient(135deg,#1d4ed8,#0ea5e9)] cursor-pointer hover:shadow-[0_2px_8px_rgba(37,99,235,0.3)]'}`}>
    {submitting ? '저장 중...' : '점검 기록 저장'}
  </button>
</div>
</div>
```

**중요 보존 사항 (InspectionModal):**
- `isGuideLight` 분기 + glMarkers / MARKER_TO_GL_COL / GL_COL_LABEL — 변경 없음
- `isSohwaGroup` (소화전+비상콘센트 혼합) 의 bcResult / bcMemo / bcPhoto / 직렬 저장 — 변경 없음
- `dismissedBlockedId` / `isAccessBlocked` / `showAccessBlockedPopup` / `useInspectionRevisitPopup` 호출 — 변경 없음
- `pickerSourceCPs` 계산 — 변경 없음
- 모든 useEffect / useMemo / useCallback / setSubmitting / setJustSaved / setSubmitError / onSave 호출 — 변경 없음

**Step 3.3 — ResolutionModal (라인 3878~4003) Tailwind 변환:**

slide-up 하단 시트 모달. 모달 헤더(닫기) + 본문(점검 시 그리드, 메모, 사진) + 저장 버튼. transform/transition 인라인 허용, 나머지 모두 Tailwind.

**Step 3.4 — ResolutionDetailModal (라인 3779~3852) Tailwind 변환:**

slide-up 하단 시트. 점검 시 / 조치 후 2 grid + 사진 viewer 트리거. PhotoViewer 함께.

**Step 3.5 — PhotoViewer (라인 3868~3877) Tailwind 변환:**

```tsx
function PhotoViewer({ url, onClose }: { url: string; onClose: () => void }) {
  return (
    <div onClick={onClose}
         className="fixed inset-0 z-[200] bg-black/95 flex items-center justify-center cursor-pointer p-4">
      <img src={url} alt="" className="max-w-full max-h-full object-contain" />
    </div>
  )
}
```

**Step 3.6 — 다른 영역 한 줄도 변경 없이 보존:**
- 라인 1~2799 (5 특수 모달 본문 — Wave 2~6) — 그대로
- 라인 4000~4660 (Task 1 결과) — 그대로
- 라인 4663~4764 (Task 1 의 FireAlarmModal) — 그대로
- 라인 4769~5043 (Task 2 의 InspectionSummaryCard) — 그대로
- 라인 5046~5346 (Task 2 의 DesktopInspectionView) — 그대로

**Step 3.7 — 전체 파일 grep gate + build:**

```bash
cd /Users/jykevin/Documents/cbc7119-design/cha-bio-safety && \
  npx tsc --noEmit 2>&1 | grep -i "InspectionPage" > /tmp/inspection-tsc3.log
test ! -s /tmp/inspection-tsc3.log && \
  npm run build 2>&1 | tail -20
```

전체 파일 grep gate (Wave 1 영역 한정 — 5 특수 모달 + 5 증상 피커는 제외):
- 메인 page render(라인 4330~4660) + FireAlarmModal(라인 4663~4764) + InspectionSummaryCard(라인 4769~5043) + DesktopInspectionView(라인 5046~5346) + InspectionModal 셸(라인 2800~3779 중 5 증상 피커 영역 제외) + Resolution*(라인 3779~4003) + PhotoViewer(라인 3868) + WheelPicker(라인 103~169) 범위 안에서 grep

검출되어야 할 패턴 (PASS = 0건):
- `style={{[^}]*color:[^}]*'rgba\|style={{[^}]*color:[^}]*'var(--t` — TSX 인라인 color 정적
- `style={{[^}]*background:[^}]*'var(--bg` — TSX 인라인 background 정적
- `fontSize: ?9[^0-9]|fontSize: ?10[^0-9]|fontSize: ?11[^0-9]` — 9/10/11px
- `text-\[9px\]|text-\[10px\]|text-\[11px\]` — Tailwind arbitrary 9/10/11

**중요: 5 증상 피커 JSX 블록 (line 3460~3550 부근 — InspectionModal 안) 은 grep gate 에서 제외**. 이 영역은 의도된 잔존이며 Wave 2 에서 처리.

해결책: grep gate 를 sed/awk 로 line-range 한정 또는 별도 marker comment 로 명시:
```tsx
// [WAVE2-PRESERVE-START] 5 증상 피커 — 인라인 style 보존 (다음 quick 트랙에서 변환)
... 기존 라인 3460~3550 코드 그대로 ...
// [WAVE2-PRESERVE-END]
```

검증 스크립트:
```bash
# WAVE2-PRESERVE 블록 사이 코드를 제외하고 grep
awk '/\[WAVE2-PRESERVE-START\]/{skip=1; next} /\[WAVE2-PRESERVE-END\]/{skip=0; next} !skip' \
    src/pages/InspectionPage.tsx > /tmp/inspection-wave1.tsx

# 메인 영역만 추출 — 라인 2800 이상 (InspectionModal 셸 부터)
# 또는 5 특수 모달 본문 제외:
# 단순화 — 전체 파일에 대해 grep 하되 잔존 허용량 < 기존 의 N% 로 정량 확인

# fontSize 9/10/11 (Wave 1 영역 + 비 Wave2-Preserve)
FONT_VIOLATIONS=$(grep -nE "fontSize: ?9[^0-9]|fontSize: ?10[^0-9]|fontSize: ?11[^0-9]" /tmp/inspection-wave1.tsx | grep -cv 'WAVE2-PRESERVE' || echo 0)
test "$FONT_VIOLATIONS" -lt 50  # 5 특수 모달 잔존 허용
```

실용 처방: **5 특수 모달 본문 (라인 181~2799) 도 grep gate 에서 제외** — 이 함수 본문 안의 fontSize / style 잔존은 Wave 2~6 보존 의도. grep 은 `awk` 로 함수 정의 라인 범위만 추출하거나, "main page render + InspectionModal 셸 + Resolution* + DesktopInspectionView + InspectionSummaryCard + FireAlarmModal" 영역만 sed 로 발췌해서 grep.

```bash
# 단순화 — 변환 결과 검증은 line range 기반:
sed -n '103,169p;2800,3459p;3551,3779p;3779,4003p;4004,5346p' src/pages/InspectionPage.tsx > /tmp/inspection-wave1-extracted.tsx
# line 3460~3550 (5 증상 피커) 제외, line 1~180 + 181~2799 (5 특수 모달) 제외

grep -cE "fontSize: ?9[^0-9]|fontSize: ?10[^0-9]|fontSize: ?11[^0-9]" /tmp/inspection-wave1-extracted.tsx  # === 0
grep -cE "color: ?'var\(--t[123]\)" /tmp/inspection-wave1-extracted.tsx  # === 0
grep -cE "background: ?'var\(--bg" /tmp/inspection-wave1-extracted.tsx  # === 0
```

이 line range 는 변환 후 새 line 번호에 맞춰 조정 — Task 3 verify 단계에서 새 grep range 를 정확히 산출.
  </action>
  <verify>
    <automated>
cd /Users/jykevin/Documents/cbc7119-design/cha-bio-safety && \
npx tsc --noEmit 2>&1 | grep -i "InspectionPage" > /tmp/inspection-tsc3.log; \
test ! -s /tmp/inspection-tsc3.log && \
grep -q "function WheelPicker" src/pages/InspectionPage.tsx && \
grep -q "function InspectionModal" src/pages/InspectionPage.tsx && \
grep -q "function ResolutionModal" src/pages/InspectionPage.tsx && \
grep -q "function ResolutionDetailModal" src/pages/InspectionPage.tsx && \
grep -q "function PhotoViewer" src/pages/InspectionPage.tsx && \
grep -q "useInspectionRevisitPopup" src/pages/InspectionPage.tsx && \
grep -q "showAccessBlockedPopup" src/pages/InspectionPage.tsx && \
grep -q "isGuideLight" src/pages/InspectionPage.tsx && \
grep -q "MARKER_TO_GL_COL" src/pages/InspectionPage.tsx && \
grep -q "pickerSourceCPs" src/pages/InspectionPage.tsx && \
grep -q "isSohwaGroup" src/pages/InspectionPage.tsx && \
grep -q "symptomPick" src/pages/InspectionPage.tsx && \
grep -q "extSymptomPick" src/pages/InspectionPage.tsx && \
grep -q "hydrantSymptomPick" src/pages/InspectionPage.tsx && \
grep -q "shutterSymptomPick" src/pages/InspectionPage.tsx && \
grep -q "damperSymptomPick" src/pages/InspectionPage.tsx && \
grep -q "WAVE2-PRESERVE-START" src/pages/InspectionPage.tsx && \
grep -q "WAVE2-PRESERVE-END" src/pages/InspectionPage.tsx && \
grep -q "bg-\[linear-gradient(135deg,#1d4ed8,#0ea5e9)\]" src/pages/InspectionPage.tsx && \
grep -q "ZONE_LUCIDE\|FlaskConical.*Building2.*TrainFront\|TrainFront" src/pages/InspectionPage.tsx && \
grep -q "CheckCircle2\|AlertTriangle\|XCircle" src/pages/InspectionPage.tsx && \
npm run build 2>&1 | grep -qE "built in|✓ built" && \
echo "Task 3 PASS"
    </automated>
  </verify>
  <done>
- InspectionModal 셸(컨테이너 / 헤더 / Zone 탭 / 층 칩 / WheelPicker 호출부 / 결과 3종 / 사진+메모 / 저장 CTA / submitError / justSaved / 부분 오버레이 wrapper) Tailwind 변환
- 5 증상 피커 JSX 블록은 `[WAVE2-PRESERVE-START]` ~ `[WAVE2-PRESERVE-END]` 마커로 명시 보존 (인라인 style 그대로, Wave 2 follow-up)
- ResolutionModal / ResolutionDetailModal / PhotoViewer Tailwind 변환
- WheelPicker fontSize 11/10 → text-caption (12px) 으로 승격, 동적 분기는 className 동적 분기로
- §7.1 결과 3종 일관 룰 + §7.3 lucide 아이콘 + §6.4 그라디언트 저장 CTA + §7.5 Zone lucide (FlaskConical/Building2/TrainFront)
- 비즈니스 로직 (isGuideLight / MARKER_TO_GL_COL / pickerSourceCPs / isSohwaGroup / useInspectionRevisitPopup / showAccessBlockedPopup / 5 증상 피커 state) 모두 보존 — grep 검증
- TypeScript 컴파일 에러 0
- `npm run build` 성공
- 인라인 style 금지 키 0개 (Wave 1 변환 영역 한정 — line range 기반 grep 또는 WAVE2-PRESERVE 블록 제외 grep)
- 9/10/11px 폰트 0개 (Wave 1 변환 영역 한정)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: 사용자 시각 / 기능 검증 (브라우저)</name>
  <what-built>
InspectionPage.tsx Wave 1 재디자인 — 메인 page render + DesktopInspectionView + 일반 InspectionModal 셸 + Resolution* + PhotoViewer + FireAlarmModal + InspectionSummaryCard 가 v0.1.1 토큰 + Tailwind only 로 교체됨. 인라인 style 의 금지 키 0건 (Wave 1 영역 한정), 9/10/11px 폰트 0건, §6.1 Progress Color Rule + §6.3 카테고리 카드 + §6.4 그라디언트 + §7.1 일관성 + §7.2/§7.5 아이콘 매핑 적용, 비즈니스 로직 100% 보존. 5 특수 모달 본문 + InspectionModal 내부 5 증상 피커 JSX 는 그대로 보존 (Wave 2~6 후속 트랙).
  </what-built>
  <how-to-verify>
**1. dev server 띄우기:**
```bash
cd /Users/jykevin/Documents/cbc7119-design/cha-bio-safety
npm run dev:front     # 5xl 전례 — `dev` 가 아닌 `dev:front`
```

**2. 라이트/다크 + 모바일/데스크톱 4 조합 시각 확인:**
- 브라우저 `http://localhost:5173/inspection` 접속 (로그인 후)
- DevTools mobile mode iPhone 13 (390x844)
- 테마 토글: 콘솔 `document.documentElement.dataset.theme = 'light'` (또는 'dark')
- 데스크톱 (1280x800) 두 모드

**a. 메인 페이지 (모바일):**
- [ ] 동기화 시각 라벨 우측 정렬 + 작은 텍스트
- [ ] 오늘 점검 현황 카드 — 0건일 때 "📋 아직 점검 기록이 없습니다" empty
- [ ] 오늘 점검 현황 — N건 접힘 시 카테고리 칩(lucide 아이콘 회색) + 결과 칩 가로 nowrap scroll
- [ ] 오늘 점검 현황 — N건 펼침 시 미조치/조치완료/정상 리스트 3 섹션
- [ ] 점검 항목 선택 — 16 카테고리 그리드 3열
- [ ] **§6.3 — 16개 아이콘 모두 회색 (text-secondary)** — 이모지 0개
- [ ] **§6.1 — 좌측 3px 색바**: 100% safe 초록 / 50-99% accent 파랑 / 1-49% warning 노랑 / 0% tertiary 회색
- [ ] 0% 카드 opacity-60, 없음(total=0) 카드 opacity-[0.38] cursor-default
- [ ] allDone 카드 — bg safe 톤 강조

**b. 데스크톱 페이지:**
- [ ] 좌(50%) 카테고리 카드 + 우(50%) 빈 상태("좌측에서 점검 항목을 선택하세요")
- [ ] 좌측 카드 — §6.3 룰 동일 적용, 선택된 카드는 border-2 accent + ring
- [ ] 기간 필터 5 버튼 (이번달/7일/30일/90일/전체) — 선택 시 accent
- [ ] 카드 클릭 → 우측 내역 목록 + InspectionSummaryCard (정상/주의/불량 3박스)
- [ ] 내역 카드 클릭 → 우측 상세 보기 (이슈 카드만 클릭 가능)
- [ ] 상세 보기 ChevronLeft 뒤로가기 + "전체 화면 열기" → /remediation/:id

**c. 일반 InspectionModal (예: 유도등 / 완강기 / 소화기 / 소화전 / 소방펌프 / CCTV 클릭):**
- [ ] slide-up 애니메이션 (transform translateY(100%)→0, 0.26s)
- [ ] 헤더 — 카테고리 아이콘(lucide/custom 회색) + group.labels + 닫기 ×
- [ ] Zone 탭 — FlaskConical 연구동 / Building2 사무동 / TrainFront 지하 (lucide, 회색 통일)
- [ ] 층 칩 — 완료 층은 safe 톤 + CheckCircle2 lucide
- [ ] WheelPicker — 중앙 항목 text-label 13px / 좌우 항목 text-caption 12px (9/10/11 없음)
- [ ] 결과 3종 버튼 — CheckCircle2 정상 / AlertTriangle 주의 / XCircle 불량 (§7.3) + active 시 status 톤
- [ ] **5 증상 피커 JSX (유도등 / 소화기 / 소화전 / 방화셔터 / 전실제연댐퍼) — 인라인 style 그대로 보존 (Wave 2 작업 대기)** — 디자인 일관성 차이 OK
- [ ] 사진 버튼 + 메모 textarea — Tailwind 토큰
- [ ] 저장 CTA 그라디언트 (§6.4) + 취소 버튼 (저장:취소 2:1)
- [ ] InspectionRevisitPopup / AccessBlockedPopup — 부분 오버레이 정상 동작 (컴포넌트 본체 수정 없음 확인)

**d. 5 특수 모달 (예: 특별피난계단 / CCTV / 배연창 / DIV / 컴프레셔 / 소방용전원공급반 / 주차장비 / 전실제연댐퍼 클릭):**
- [ ] **인라인 style 그대로 보존되어 기존 디자인 유지 — Wave 2~6 작업 대상** (지금 디자인 변경 안 된 것이 정상)
- [ ] 기능적으로는 동작 정상 (저장 / 닫기 / Revisit 팝업 등)

**e. 화재수신반 (카테고리 14번 클릭):**
- [ ] FireAlarmModal — 헤더 Bell lucide / 본문 폼 Tailwind / 저장 CTA 그라디언트
- [ ] 화재보 / 비화재보 토글 — active 시 danger 톤

**f. 조치 입력 / 조치 결과 모달:**
- [ ] ResolutionModal slide-up — 메모 + 사진 + 저장
- [ ] ResolutionDetailModal slide-up — 점검 시 / 조치 후 2 grid + 사진 클릭 시 PhotoViewer
- [ ] PhotoViewer — 전체화면 다크 + 클릭 시 닫힘

**g. 메모리 룰:**
- [ ] 9 / 10 / 11px 폰트 사용 없음 (5 증상 피커 영역 제외 — 거기는 의도된 잔존)
- [ ] BottomNav 갭 자연스러움 — height + paddingBottom 패턴 (App.tsx 가 처리)
- [ ] DIV / 컴프 카드 — 같이 점검되는 페어 표시 보존

**h. 인터랙션 (비즈니스 로직 보존 검증):**
- [ ] 카테고리 카드 클릭 → 적절한 모달 dispatch (DIV → DivModal / 컴프 → CompressorModal / 배연 → BaeyeonModal / 소방용전원공급반 → PowerPanelModal / CCTV → CctvModal / 특별피난계단 → StairwellModal / 주차장비 → ParkingGateModal / 전실제연댐퍼 → DamperModal / 화재수신반 → FireAlarmModal / 그 외 → InspectionModal)
- [ ] 점검 저장 → records 즉시 반영 + 30s 폴링 정상 동작
- [ ] QR 진입 (`state.qrCheckpoint`) — 카테고리 자동 선택
- [ ] 대시보드 진입 (`state.autoSelectCategory`) — 카테고리 자동 선택
- [ ] DIV / 컴프 1:1 매핑 + 2일 연속 점검 완료 판정
- [ ] isCpCompleted 룰 (normal | caution | bad+resolved) 일관 동작

**i. 콘솔 / 빌드:**
- [ ] 브라우저 콘솔 에러 0건
- [x] `npx tsc --noEmit` 통과 (Task 1/2/3 verify 자동 통과)
- [x] `npm run build` 성공 (Task 3 verify 자동 통과)

**3. 시안과 비교:**
- 다른 탭에서 `cha-bio-safety/docs/redesign-context/02-inspection/sketch/inspection-sketch-main.html` 열기
- 모바일 다크 vs 실제 모바일 다크 좌우 비교 — Row 1 (Mobile Dark + Light)
- 데스크톱 다크 vs 실제 데스크톱 다크 — Row 2/3
- 일반 InspectionModal 모달 — Row 4 E-1 (유도등 EL-RES-1F-12 + 휠피커 + 결과 3종 + 사진 + 메모 + CTA) 와 비교
- Revisit / AccessBlocked 팝업 — Row 4 E-2 / E-3 와 비교
  </how-to-verify>
  <resume-signal>
- "approved" → main 머지 컨펌 후 main 머지 + 배포 / Wave 2~6 후속 트랙 plan 으로 진행
- "수정: [구체 항목]" → Task 1/2/3 중 해당 부분 보정
- "롤백" → 3 commits revert 후 디자인 방향 재논의
- **컨펌 정책 (메모리 룰):** redesign 브랜치 작업 + 그 발단 코드 변경은 사용자 명시 컨펌 후에만 main 머지+배포 — Claude 직접 즉시 배포 금지
  </resume-signal>
</task>

</tasks>

<verification>
**자동 검증 (Task 1/2/3 verify gate 합산):**
- TypeScript 컴파일 에러 0 (`npx tsc --noEmit` 의 InspectionPage 관련 출력 0건)
- `npm run build` 성공
- Wave 1 변환 영역(메인 page render 4330~4660 / FireAlarmModal 4663~4764 / InspectionSummaryCard 4769~5043 / DesktopInspectionView 5046~5346 / InspectionModal 셸 2800~3779 minus WAVE2-PRESERVE 3460~3550 / Resolution* 3779~4003 / PhotoViewer 3868~3877 / WheelPicker 103~169) 안의 인라인 style 금지 키 사용 0건
- Wave 1 변환 영역 안의 9 / 10 / 11px 폰트 0건 (정수 + arbitrary value 모두)
- 5 특수 모달 본문(181~2799) — **변경 없음** (의도된 보존, Wave 2~6)
- InspectionModal 내부 5 증상 피커 JSX (WAVE2-PRESERVE 블록) — **변경 없음** (의도된 보존, Wave 2)
- 비즈니스 로직 grep gate (Task 1/2/3 verify 합산):
  - `function InspectionPage` / `function WheelPicker` / `function InspectionModal` / `function ResolutionModal` / `function ResolutionDetailModal` / `function PhotoViewer` / `function FireAlarmModal` / `function InspectionSummaryCard` / `function DesktopInspectionView` 모두 존재
  - `isCpCompleted` / `CATEGORY_GROUPS` / `ZONE_CONFIG` / `INSPECT_RESULT_OPTIONS` / `ALL_RESULT_OPTIONS` / `RESULT_LABEL` / `RESULT_COLOR` 존재
  - `inspectionApi.*` / `fireAlarmApi.create` / `remediationApi.*` / `scheduleApi.getByMonth` / `floorPlanMarkerApi.*` 호출 존재
  - `useInspectionRevisitPopup` / `AccessBlockedPopup` / `PhotoButton` / `usePhotoUpload` import 보존
  - `IS_ANDROID` 는 본 페이지에 없으므로 검사 안 함
- §6.1 적용 grep gate — `bg-safe-bar` / `bg-accent` / `bg-warning-bar` / `bg-text-tertiary` (좌측 색바 4단계) 존재
- §6.3 적용 grep gate — `text-text-secondary` 카테고리 아이콘 통일
- §6.4 그라디언트 — `bg-[linear-gradient(135deg,#1d4ed8,#0ea5e9)]` 등장 ≥ 2회 (InspectionModal 저장 CTA + FireAlarmModal 저장 CTA)
- §7.2 카테고리 아이콘 — lucide 10 + 커스텀 6 모두 import + CATEGORY_ICONS 배열 16 항목
- §7.5 Zone 아이콘 — `FlaskConical` / `Building2` / `TrainFront` 등장
- §7.3 결과 아이콘 — `CheckCircle2` / `AlertTriangle` / `XCircle` 등장 (RESULT_ICONS 매핑 또는 직접 사용)
- WAVE2-PRESERVE 마커 — 시작 + 끝 페어 등장

**수동 검증 (Task 4 — 브라우저):**
- 4 조합(라이트/다크 × 모바일/데스크톱) 시각 확인 — 메인 페이지 + 일반 InspectionModal + FireAlarmModal + Resolution* + 5 특수 모달(인라인 보존 확인)
- 시안 (inspection-sketch-main.html Row 1/2/3/4 E-1/E-2/E-3) 과 비교
- 모든 인터랙션 동작 (모달 dispatch, 점검 저장, QR 진입, 자동 선택, isCpCompleted 룰)
- 콘솔 에러 0
</verification>

<success_criteria>
- InspectionPage.tsx 의 Wave 1 변환 영역이 v0.1.1 토큰 기반 Tailwind only 마크업으로 작성됨 (인라인 style 금지 키 0건, 동적 분기/keyframe/safe-area calc 만 인라인 허용)
- §6.1 Progress Color Rule + §6.3 카테고리 카드 룰 + §6.4 그라디언트 + §7.1 일관성 + §7.2 카테고리 아이콘 매핑 + §7.5 Zone 아이콘 + §7.3 결과 아이콘 모두 정확 적용 (시각 확인 가능: 16 카테고리 아이콘이 모두 회색, 4단계 색바, 그라디언트 저장 CTA 2개)
- 9 / 10 / 11px 폰트 사용 0건 (Wave 1 변환 영역 한정, 5 증상 피커 WAVE2-PRESERVE 블록 + 5 특수 모달 본문 제외)
- 비즈니스 로직 100% 보존 — 모든 핸들러 / API / 네비게이션 / DIV-COMP 1:1 매핑 / isCpCompleted 룰 / QR 진입 / 자동 선택 / 30s 폴링 / 모달 dispatch / Revisit & AccessBlocked / 5 증상 피커 / 소화전+비상콘센트 혼합 / 유도등 마커 / 5 특수 모달 props 동일
- 라이트/다크 두 테마 모두 가독성 OK
- 모바일/데스크톱 자동 분기 (tokens.css 의 spacing 토큰 + isDesktop 분기 보존)
- TypeScript 컴파일 에러 0, `npm run build` 성공, 콘솔 에러 0, /inspection 라우트 정상 렌더
- 사용자 브라우저 시각 검증 approve (Task 4)
- 시안과 1:1 매핑 — 시각 차이 미세 (9/10/11→12px 승격 + 이모지→lucide 교체로 인한 의도된 차이는 허용)
- Wave 2~6 후속 트랙 슬러그/스코프가 PLAN.md 말미에 명시되어 사용자가 다음 quick 작업 발주 시 즉시 사용 가능
</success_criteria>

<output>
After completion, create `.planning/quick/260514-i4r-redesign-02-inspection-tsx/260514-i4r-SUMMARY.md` with:
- 변환된 InspectionPage.tsx 라인 수 (변경 전/후 — 5346 ± 200 예상)
- Wave 1 변환 영역 + 보존 영역 line range 표
- 인라인 style 잔존 키 목록 (gridTemplateRows / animation / paddingBottom calc / dynamic catColor / 5 특수 모달 + 5 증상 피커 — 의도된 예외) + 정량(개수)
- §6.1 / §6.3 / §6.4 / §7.1 / §7.2 / §7.5 / §7.3 적용 결과 (시각 확인 결과)
- 사용자 검토 결과 (approved / 수정요청 / 롤백)
- 다음 단계 — Wave 2~6 follow-up 슬러그 트리거 정보
</output>

---

## Wave 2~6 Follow-up (이 quick 종료 후 후속 트랙)

이번 Wave 1 은 메인 페이지 + 일반 InspectionModal 셸 + Resolution* + DesktopInspectionView + FireAlarmModal 까지만 변환. 다음 7 트랙은 별도 quick 으로 발주.

| Wave | 슬러그 (권장) | 대상 함수 | 시안 source | 라인 범위 (기존) | 예상 task 수 |
|---|---|---|---|---|---|
| **Wave 2** | `redesign-02-inspection-tsx-wave2-symptom-pickers` | InspectionModal 내부 5 증상 피커 JSX (WAVE2-PRESERVE 블록) — symptomPick(유도등) / extSymptomPick(소화기) / hydrantSymptomPick(소화전) / shutterSymptomPick(방화셔터) / damperSymptomPick(전실제연댐퍼) | `inspection-sketch-symptom-pickers.html` | InspectionPage.tsx 라인 3460~3550 부근 (Task 3 직후 라인 번호 재산출) | 2 (5 피커 × 2 = 10 블록 → 그룹화) |
| **Wave 3** | `redesign-02-inspection-tsx-wave3-div-comp` | DivModal + CompressorModal + DivUnderPicker + DivTrendSubview + detectDivTrend | `inspection-sketch-div-comp-cycle.html` | 라인 827~1937 (~1110줄) | 3 (셸 / 트렌드차트 / 폼) |
| **Wave 4** | `redesign-02-inspection-tsx-wave4-baeyeon-damper` | BaeyeonModal + DamperModal | `inspection-sketch-baeyeon-damper.html` | 라인 579~826 + 2346~2799 (~700줄) | 2 |
| **Wave 5** | `redesign-02-inspection-tsx-wave5-stairwell-cctv` | StairwellModal + CctvModal | `inspection-sketch-stairwell-cctv.html` | 라인 181~578 (~400줄) | 2 |
| **Wave 6** | `redesign-02-inspection-tsx-wave6-misc-modals` | PowerPanelModal + ParkingGateModal | `inspection-sketch-misc-modals.html` | 라인 1938~2345 (~410줄) | 2 |

각 Wave 발주 전 사용자 컨펌 (메모리 룰: redesign 브랜치 작업 + 그 발단 코드 변경은 사용자 명시 컨펌 후에만 main 머지+배포).

**Wave 7 (마지막)** — 전체 grep gate clean sweep + 시안 audit 재실행 + main 머지 + 배포.
