---
phase: quick-260521-3es
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cha-bio-safety/docs/redesign-context/15-daily-report/sketch-wave-4-download-action.html
autonomous: true
requirements:
  - W4-SKETCH
must_haves:
  truths:
    - "sketch-wave-4-download-action.html 파일 1개가 다운로드 액션 4 frame (다크/라이트 × 모바일/데스크톱) 을 시각적으로 박제한다"
    - "각 frame 안에 daily 버튼 (bg-safe-bar solid) + monthly 버튼 (bg-surface-sunken border) + 안내 줄 3 요소가 보존 카피 그대로 렌더된다"
    - "각 frame 안에 disabled state variant (generating='생성 중...' / monthly='월별 생성 중...') 가 시각적으로 표시된다"
    - "이모지/그라데이션 0건 — ⬇ 글리프 제거 + lucide Download SVG 교체 + linear-gradient 0건"
    - "9·10·11px font-size 0건 — 버튼 16px (text-body) + 안내 12px (text-caption) 상향"
    - "신규 class ≥7건 (.download-action / .download-action--desktop / .download-btn / .download-btn--daily / .download-btn--monthly / .download-btn--disabled / .download-btn-icon / .page-footer-note) 가 W7 grep 추출 가능한 형태로 인라인 <style> 정의된다"
    - "메모리 룰 unique slug ≥7건 + W4 OQ ≥3건 (default 박제) 가 주석 헤더에 박제된다"
  artifacts:
    - path: "cha-bio-safety/docs/redesign-context/15-daily-report/sketch-wave-4-download-action.html"
      provides: "W4 sketch HTML — 다운로드 액션 4 frame (다크/라이트 × 모바일/데스크톱) + disabled variants + 인라인 CSS + 자체 verify gate 주석"
      min_lines: 280
      contains: "sketch-wave-4-download-action"
  key_links:
    - from: "sketch-wave-4-download-action.html"
      to: "DailyReportPage.tsx line 197 handleDailyDownload / line 212 handleMonthlyDownload / line 320~379 다운로드 영역 / line 381~383 안내 줄"
      via: "verbatim 코멘트 박제 (W7 변환 시 핸들러 시그니처 + 카피 보존 근거)"
      pattern: "handleDailyDownload|handleMonthlyDownload"
    - from: "sketch-wave-4-download-action.html"
      to: "wave-1-index.md §1.4 / §2.1 W4 / §3.4 §3.7 §6.4 / §4.1 / §5 / §7 OQ #1"
      via: "주석 헤더 인용 (W4 scope + LOCKED OQ default 인용)"
      pattern: "wave-1-index"
    - from: "sketch-wave-4-download-action.html"
      to: "sketch-wave-2-mobile-header-date-nav.html + sketch-wave-3-editable-cards-personnel.html"
      via: "frame 셋업 패턴 1:1 mirror — 393px frame + 토큰 분기 (data-theme) + tokens.css/typography.css link"
      pattern: "data-theme"
---

<objective>
redesign/15-daily-report 의 sketch wave 4 — DailyReportPage.tsx line 320~383 의 다운로드 액션 (daily + monthly 버튼 + 안내 줄) 영역을 다크/라이트 × 모바일/데스크톱 4 frame matrix 로 시각화한 sketch HTML 1개를 생성한다. W7 TSX 변환 wave 에서 grep 으로 verbatim 추출 가능하도록 신규 class ≥7건 + 메모리 룰 ≥7건 + W4 OQ ≥3건 + verify gate ≥8 + negative gate ≥3 을 인라인 주석에 박제한다.

Purpose:
- W2 (헤더+dateNav) / W3 (EditableCard+SummaryCard) 완결 후 formContent 영역 마지막 sketch 청크 (다운로드 영역) 디자인 확정 — 시각 컨펌 받기 위함
- 그라데이션 폐기 (§6.4 + 14-reports W6 LOCKED b 일관) + 이모지 ⬇ 제거 + 폰트 노안 상향 3 가지 시각 변경의 시안화
- W5 (데스크톱 layout) 진입 전 마지막 단일 컬럼 영역 — W5 부터는 좌우 분할이라 다운로드 영역은 좌측 패널 안 단순 stack 으로 들어감

Output: cha-bio-safety/docs/redesign-context/15-daily-report/sketch-wave-4-download-action.html (단일 sketch HTML, 인라인 <style>, 4 frame + disabled variant, ≥280 줄)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@CLAUDE.local.md

@cha-bio-safety/docs/redesign-context/15-daily-report/wave-1-index.md
@cha-bio-safety/docs/redesign-context/15-daily-report/sketch-wave-2-mobile-header-date-nav.html
@cha-bio-safety/docs/redesign-context/15-daily-report/sketch-wave-3-editable-cards-personnel.html
@cha-bio-safety/docs/redesign-context/15-daily-report/design-system.md
@cha-bio-safety/docs/redesign-context/15-daily-report/tokens.css
@cha-bio-safety/docs/redesign-context/15-daily-report/typography.css

<interfaces>
<!-- DailyReportPage.tsx 의 verbatim 보존 인용. W4 sketch 안 코멘트 박제에 그대로 사용. -->
<!-- 핸들러 시그니처 (line 197, 212) — W4 sketch 는 시각만 다루지만 핸들러 시그니처는 주석에 박제 -->

From cha-bio-safety/src/pages/DailyReportPage.tsx line 197~209 (handleDailyDownload):
```typescript
const handleDailyDownload = async () => {
  if (generating) return
  setGenerating(true)
  try {
    await dailyReportApi.saveNotes({ date, today_text: todayText, tomorrow_text: tomorrowText, content: notes })
    if (!auto) throw new Error('데이터 없음')
    const data = { ...auto, todayText, tomorrowText, notes }
    const [y, m, d] = date.split('-').map(Number)
    await generateDailyExcel('daily', y, m, { [d]: data }, d)
  } catch { toast.error('엑셀 생성 중 오류가 발생했습니다') }
  finally { setGenerating(false) }
}
```

From cha-bio-safety/src/pages/DailyReportPage.tsx line 212~265 (handleMonthlyDownload — 시그니처만 박제):
```typescript
const handleMonthlyDownload = async () => {
  if (genMonthly) return
  setGenMonthly(true)
  try {
    // ... (lazy save + month aggregation, sketch 무관)
    await generateDailyExcel('monthly', y, m, dayDataMap, limitDay)
  } catch { toast.error('월별 엑셀 생성 중 오류가 발생했습니다') }
  finally { setGenMonthly(false) }
}
```

From cha-bio-safety/src/pages/DailyReportPage.tsx line 320~379 (다운로드 영역 — sketch 가 변환할 source 마크업):
```tsx
{/* 다운로드 버튼 */}
{isDesktop ? (
  <div style={{ display: 'flex', gap: 8, marginTop: 4 }}>
    <button
      onClick={handleDailyDownload}
      disabled={generating || queryData.isLoading}
      style={{
        flex: 1, padding: 11, borderRadius: 9, border: 'none',
        background: (generating || queryData.isLoading) ? 'var(--bg3)' : 'linear-gradient(135deg,#1d4ed8,#2563eb)',
        color: (generating || queryData.isLoading) ? 'var(--t3)' : '#fff',
        fontSize: 13, fontWeight: 700,
        cursor: (generating || queryData.isLoading) ? 'default' : 'pointer',
      }}
    >
      {generating ? '생성 중...' : `⬇ ${Number(mm)}월${dd}일 방재업무일지 다운로드`}
    </button>
    <button
      onClick={handleMonthlyDownload}
      disabled={genMonthly}
      style={{
        flex: 1, padding: 11, borderRadius: 9, border: '1px solid var(--bd)',
        background: genMonthly ? 'var(--bg3)' : 'var(--bg2)',
        color: genMonthly ? 'var(--t3)' : 'var(--t1)',
        fontSize: 13, fontWeight: 700,
        cursor: genMonthly ? 'default' : 'pointer',
      }}
    >
      {genMonthly ? '월별 생성 중...' : `⬇ 일일업무일지(${mm}월) 다운로드`}
    </button>
  </div>
) : (
  <>
    <button
      onClick={handleDailyDownload}
      disabled={generating || queryData.isLoading}
      style={{
        width: '100%', padding: 11, borderRadius: 9, border: 'none',
        background: (generating || queryData.isLoading) ? 'var(--bg3)' : 'linear-gradient(135deg,#1d4ed8,#2563eb)',
        color: (generating || queryData.isLoading) ? 'var(--t3)' : '#fff',
        fontSize: 13, fontWeight: 700, marginBottom: 8,
        cursor: (generating || queryData.isLoading) ? 'default' : 'pointer',
      }}
    >
      {generating ? '생성 중...' : `⬇ ${Number(mm)}월${dd}일 방재업무일지 다운로드`}
    </button>
    <button
      onClick={handleMonthlyDownload}
      disabled={genMonthly}
      style={{
        width: '100%', padding: 11, borderRadius: 9, border: '1px solid var(--bd)',
        background: genMonthly ? 'var(--bg3)' : 'var(--bg2)',
        color: genMonthly ? 'var(--t3)' : 'var(--t1)',
        fontSize: 13, fontWeight: 700,
        cursor: genMonthly ? 'default' : 'pointer',
      }}
    >
      {genMonthly ? '월별 생성 중...' : `⬇ 일일업무일지(${mm}월) 다운로드`}
    </button>
  </>
)}

<div style={{ fontSize: 10, color: 'var(--t3)', textAlign: 'center', padding: '8px 0 20px' }}>
  수정 내용은 자동 저장됩니다 · 월별은 저장된 모든 날짜를 포함합니다
</div>
```

From cha-bio-safety/src/styles/components.css line 41~42 (W2/W3 에서 이미 박제, W4 inherit):
```css
.dot-meta { display: inline-block; width: 4px; height: 4px; border-radius: 9999px; background: var(--text-tertiary); flex-shrink: 0; }
.page-footer-note { text-align: center; padding: 8px 16px 20px; font-size: 12px; line-height: 1.6; color: var(--text-tertiary); }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: sketch-wave-4-download-action.html 생성 (4 frame matrix + disabled variants + ≥7 신규 class + ≥7 메모리 룰 + ≥4 OQ default + ≥8 verify gate + ≥3 negative gate)</name>
  <files>cha-bio-safety/docs/redesign-context/15-daily-report/sketch-wave-4-download-action.html</files>
  <action>
W4 sketch HTML 1개를 다음 구조로 생성한다. **단일 파일 newFile**, 다른 파일 변경 0.

## A. 파일 헤더 (HTML5 doctype + 메타)

```html
<!DOCTYPE html>
<html lang="ko" data-theme="dark">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>redesign/15-daily-report — sketch wave 4 (다운로드 액션)</title>
  <link rel="stylesheet" href="./tokens.css">
  <link rel="stylesheet" href="./typography.css">
```

## B. 코멘트 헤더 (≥80 줄 박제 — 후속 wave 작업자 단일 진입점)

다음 메타를 주석으로 박제 (sketch-wave-3 패턴 1:1 mirror):

1. **파일 정체성 4줄**: 파일명 / 작성일 (2026-05-21) / Quick ID (260521-3es) / Branch (redesign/15-daily-report)

2. **범위 정의 5줄**:
   - "다운로드 액션 영역 = daily 버튼 + monthly 버튼 + 안내 줄 (DailyReportPage.tsx line 320~383)"
   - "× 다크/라이트 × 모바일 393px / 데스크톱 ~640px = 4 frame"
   - "각 frame 안 disabled state variant (generating / monthly) 시각 표시"
   - out of scope 명시: W2 헤더/dateNav 완결 / W3 EditableCard+SummaryCard 완결 / W5 데스크톱 layout (좌우 분할) / W6 DailyPortraitPreview / W7 TSX checklist

3. **참조 6줄**: wave-1-index.md §1.4 / §2.1 W4 / §3.4 §3.7 §6.4 / §4.1 / §5 / §7 OQ #1, DailyReportPage.tsx line 197/212/320~383 verbatim, sketch-wave-2/3 frame 셋업 패턴 mirror, components.css line 41~42 (.dot-meta + .page-footer-note inherit)

4. **W1 LOCKED 5건 적용 매트릭스** (wave-1-index §7 OQ default 인용):
   - OQ #1 ▶ default OK — **W4 적용** — 그라데이션 → bg-safe-bar solid (verbatim 인용: 14-reports W6 LOCKED b 일관 + design-system §6.4 폐기)
   - OQ #2 ▶ default (c) — W3 완결, W4 미해당
   - OQ #3 ▶ default 유지 — W2 완결, W4 미해당
   - OQ #4 ▶ default (a) — W6 scope, W4 미해당
   - OQ #5 ▶ default (a) — 모바일/데스크톱 분기 유지 — **W4 적용** (4 frame 중 2 frame 이 데스크톱 분기 시각화)

5. **보존 항목 박제 (memory feedback_sketch_realistic_data)** ≥10 항목:
   - daily 버튼 카피 verbatim: `${Number(mm)}월${dd}일 방재업무일지 다운로드` (sample = `"5월21일 방재업무일지 다운로드"`)
   - daily 로딩 카피 verbatim: `"생성 중..."`
   - monthly 버튼 카피 verbatim: `일일업무일지(${mm}월) 다운로드` (sample = `"일일업무일지(05월) 다운로드"`)
   - monthly 로딩 카피 verbatim: `"월별 생성 중..."`
   - 안내 카피 verbatim: `"수정 내용은 자동 저장됩니다 · 월별은 저장된 모든 날짜를 포함합니다"`
   - 가운뎃점 `·` (안내 줄 1건) → `<span class="dot-meta"></span>` 4×4 회색 dot 치환 (memory feedback_tsx_wave_emoji_dot_gap, 4 frame × 1건 = 총 4 dot span)
   - handleDailyDownload 시그니처 verbatim 인용 (코멘트 박제, sketch 가 호출하지 않음 — TSX 변환 시 보존 근거)
   - handleMonthlyDownload 시그니처 verbatim 인용 (동일)
   - disabled 조건 verbatim: `generating || queryData.isLoading` (daily) / `genMonthly` (monthly)
   - 모바일/데스크톱 구조 verbatim: 모바일 = full-width stack (marginBottom 8), 데스크톱 = flex row gap 8 (each flex:1)

6. **폰트 노안 상향 박제 (design-system §1.1, 9·10·11px 0건)**:
   - 버튼 13 → 16 (text-body) — source line 330/343/359/372 verbatim
   - 안내 10 → 12 (text-caption) — source line 381 verbatim
   - 정확한 negative gate: `font-size:\s*(9px|10px|11px)` 매치 0건

7. **그라데이션 폐기 박제 (§6.4 + W1 OQ #1 LOCKED OK)**:
   - source line 328/357 `linear-gradient(135deg,#1d4ed8,#2563eb)` → `bg-safe-bar` (= `var(--status-safe-bar)`) solid 통일
   - negative gate: `linear-gradient` 매치 0건
   - 14-reports W6 LOCKED b 일관 명시

8. **이모지 제거 박제 (memory feedback_tsx_wave_emoji_dot_gap + design-system §7.1)**:
   - source line 334/347/363/376 의 `⬇` 글리프 (U+2B07) 4건 → lucide `<svg>` (Download icon) inline
   - sketch 안에 lucide Download 16 의 inline SVG 직접 작성:
     ```html
     <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="download-btn-icon" aria-hidden="true"><path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/><polyline points="7 10 12 15 17 10"/><line x1="12" y1="15" x2="12" y2="3"/></svg>
     ```
   - sketch 본문 이모지 0건 (verify gate)
   - currentColor 상속으로 active/disabled 색 자동 분기

9. **§6.2 / §6.1 미적용 명시 (memory feedback_tsx_wave_stat_card_drift)**:
   - "다운로드 버튼은 CTA 액션, Stat Card (§6.2: 28px display + 좌측 색바) 아님 — 적용 금지"
   - "다운로드 버튼은 진척률 도넛 아님 — §6.1 Progress Color Rule 미적용"
   - W7 변환 wave 가 source 의 Stat Card 패턴을 이 sketch 에 잘못 적용하지 않도록 drift 방지 박제

10. **신규 class 정의 표 (W7 grep 추출용)** ≥7건:
    | class | role | tokens (verbatim) |
    |---|---|---|
    | `.download-action` | 모바일 stack 컨테이너 | `display: flex; flex-direction: column; gap: 8px; margin-top: 4px;` |
    | `.download-action--desktop` | 데스크톱 row 컨테이너 (modifier) | `flex-direction: row; gap: 8px;` |
    | `.download-btn` | 공통 base (모든 버튼) | `height: 44px; padding: 11px 12px; border-radius: 9px; font-size: 16px; font-weight: 700; display: inline-flex; align-items: center; justify-content: center; gap: 8px; cursor: pointer; border: none; transition: opacity .15s;` |
    | `.download-btn--daily` | primary CTA (bg-safe-bar solid) | `background: var(--status-safe-bar); color: var(--text-on-accent);` |
    | `.download-btn--monthly` | 보조 액션 (border + sunken) | `background: var(--surface-sunken); border: 1px solid var(--border-strong); color: var(--text-secondary);` |
    | `.download-btn--disabled` | disabled state modifier | `background: var(--surface-active); color: var(--text-disabled); cursor: default; border-color: var(--border-default);` |
    | `.download-btn-icon` | lucide Download 16 SVG | `width: 16px; height: 16px; color: inherit; flex-shrink: 0;` |
    | `.page-footer-note` | 안내 줄 (14-reports inherit, 동명 재사용 OK) | `text-align: center; padding: 8px 0 20px; font-size: 12px; line-height: 1.6; color: var(--text-tertiary);` |

    데스크톱 row 의 flex 1 분배는 `.download-action--desktop > .download-btn { flex: 1; }` 패턴.

11. **W4 OQ ≥4건 (W5 진입 전 사용자 컨펌 항목, default 1줄 박제)**:
    - **W4-OQ #1**: 모바일 stack 의 daily/monthly 사이 spacing — **default: 8px** (source line 359 `marginBottom: 8` verbatim) — gap-8 OK?
    - **W4-OQ #2**: 데스크톱 row 의 daily/monthly flex 비율 — **default: 1:1** (source line 322 `gap: 8` + flex:1 verbatim) — 비대칭 비율 OK?
    - **W4-OQ #3**: 안내 줄 padding — **default: 8px 0 20px** (source line 381 `padding: '8px 0 20px'` verbatim) — 14-reports `.page-footer-note` (8px 16px 20px) 와 좌우 padding 다름 OK?
    - **W4-OQ #4**: monthly 버튼 outline 강도 — **default: border-border-strong** (보조 액션, source line 340/369 `var(--bd)` 보다 약간 강조) — 또는 border-default 로 약화?

12. **메모리 룰 unique slug ≥7건 박제 (verbatim slug, 1줄 요약 + 15-daily-report W4 How)** — ≥10 권장:
    1. **feedback_design_sketch_first.md** — spacing/sizing 도 sketch 먼저 / W4: 버튼 height 44 / padding / radius 9 모두 sketch 안 명시
    2. **feedback_redesign_sketch_rule_enforcement.md** — §6.2 negative + §6.4 그라데이션 폐기 / W4: daily 버튼 bg-safe-bar solid (그라데이션 0), monthly 는 border 만 (status 색 X)
    3. **feedback_sketch_realistic_data.md** — 카피 verbatim / W4: 5개 카피 verbatim (daily/monthly/loading×2/안내) — 임의 변경 0
    4. **feedback_planner_prompt_sketch_verbatim.md** — W7 TSX wave 가 grep 추출용 verbatim CSS / W4: 신규 class 7건 인라인 `<style>` 정의, grep 으로 추출 가능
    5. **feedback_tailwind_token_class_pattern.md** — `bg-safe-bar` O / `bg-status-safe-bar` X (status- prefix 없음) / W4: `var(--status-safe-bar)` token 값 사용하지만 class 명은 `.download-btn--daily` (status- prefix 0)
    6. **feedback_tailwind_w8_h8_is_48px.md** — w-8 = 48px 함정 / W4: 버튼 height 44px (`h-11` 또는 인라인 px), w-8 / h-8 사용 0건
    7. **feedback_tsx_wave_emoji_dot_gap.md** — sketch 이모지 0 + dot span 치환 / W4: ⬇ 0건 + lucide Download SVG 인라인 + 안내 줄 `·` 1건 → `<span class="dot-meta">` 치환 (4 frame × 1건 = 4 dot)
    8. **feedback_tsx_wave_stat_card_drift.md** — source 패턴 보존이 sketch 새 패턴 덮어쓰기 사고 / W4: source fontSize 13 (line 330) 가 sketch 16 을 덮어쓰지 않도록 verbatim 박제
    9. **feedback_avoid_premature_confirmation.md** — "거의 일치" 자신감 표현 금지 / W4: 사용자 컨펌 = OQ #1~4 답변으로만
    10. **feedback_cbc7119_design_never_wrangler.md** (마스킹 표기) — 디자인 wave 중 `wr*ngler` (또는 `wr` + `angler` 분리) 금지 / W4: sketch HTML 1개 생성만, `wr*ngler` / `npm run deploy` 명령 0건

## C. <style> 인라인 정의 (≥7 신규 class)

위 §B-10 표의 ≥7 신규 class + 4 frame 공통 셋업 (body / .frame / .frame-label / data-theme) + 인라인 SVG icon 색 상속 + .dot-meta inherit (W2/W3 동일 정의 재인용 또는 components.css inherit 코멘트).

다크/라이트 분기는 sketch-wave-2/3 와 동일한 `data-theme="dark"` (root html) + 각 frame 안 `data-theme="light"` 또는 `data-theme="dark"` 명시. tokens.css 의 `[data-theme="dark"]` / `[data-theme="light"]` 분기로 자동 처리.

## D. <body> 4 frame matrix

```html
<body class="sketch-body">
  <header class="sketch-header">
    <h1>redesign/15-daily-report — W4 다운로드 액션 sketch</h1>
    <p>다크/라이트 × 모바일 393px / 데스크톱 ~640px = 4 frame + disabled variants</p>
  </header>

  <!-- F1: 다크 모바일 393px -->
  <div class="frame" data-theme="dark" style="width: 393px;">
    <div class="frame-label">F1 · 다크 · 모바일 393px (active)</div>
    <div class="download-action">
      <button class="download-btn download-btn--daily" type="button">
        <svg ... class="download-btn-icon">...</svg>
        5월21일 방재업무일지 다운로드
      </button>
      <button class="download-btn download-btn--monthly" type="button">
        <svg ... class="download-btn-icon">...</svg>
        일일업무일지(05월) 다운로드
      </button>
    </div>
    <div class="page-footer-note">수정 내용은 자동 저장됩니다<span class="dot-meta"></span>월별은 저장된 모든 날짜를 포함합니다</div>

    <!-- disabled variant (F1-D) -->
    <div class="frame-sub-label">F1-D · disabled state (generating + genMonthly)</div>
    <div class="download-action">
      <button class="download-btn download-btn--daily download-btn--disabled" type="button" disabled>
        <svg ... class="download-btn-icon">...</svg>
        생성 중...
      </button>
      <button class="download-btn download-btn--monthly download-btn--disabled" type="button" disabled>
        <svg ... class="download-btn-icon">...</svg>
        월별 생성 중...
      </button>
    </div>
  </div>

  <!-- F2: 라이트 모바일 393px (mirror) -->
  <div class="frame" data-theme="light" style="width: 393px;">
    <div class="frame-label">F2 · 라이트 · 모바일 393px</div>
    ... (F1 markup 1:1 mirror, data-theme 만 변경)
  </div>

  <!-- F3: 다크 데스크톱 ~640px -->
  <div class="frame" data-theme="dark" style="width: 640px;">
    <div class="frame-label">F3 · 다크 · 데스크톱 640px (row)</div>
    <div class="download-action download-action--desktop">
      <button class="download-btn download-btn--daily" type="button">
        <svg ... class="download-btn-icon">...</svg>
        5월21일 방재업무일지 다운로드
      </button>
      <button class="download-btn download-btn--monthly" type="button">
        <svg ... class="download-btn-icon">...</svg>
        일일업무일지(05월) 다운로드
      </button>
    </div>
    <div class="page-footer-note">수정 내용은 자동 저장됩니다<span class="dot-meta"></span>월별은 저장된 모든 날짜를 포함합니다</div>

    <!-- disabled variant -->
    <div class="frame-sub-label">F3-D · disabled state</div>
    <div class="download-action download-action--desktop">
      <button class="download-btn download-btn--daily download-btn--disabled" type="button" disabled>...생성 중...</button>
      <button class="download-btn download-btn--monthly download-btn--disabled" type="button" disabled>...월별 생성 중...</button>
    </div>
  </div>

  <!-- F4: 라이트 데스크톱 ~640px (mirror) -->
  <div class="frame" data-theme="light" style="width: 640px;">
    ... (F3 markup 1:1 mirror)
  </div>
</body>
</html>
```

## E. 자체 verify gate ≥8 (HTML 끝부분 주석 박제, 작성자가 직접 실행)

각 gate 는 작성 완료 후 grep/wc 명령으로 검증 후 PASS 표기:

| gate | 검증 명령 | 기대값 |
|---|---|---|
| G1. 4 frame 존재 | `grep -c 'class="frame"' sketch-wave-4-download-action.html` | ≥4 |
| G2. 데이터 테마 분기 (dark/light 각 2) | `grep -c 'data-theme="dark"' ...` / `grep -c 'data-theme="light"' ...` | 각 ≥2 (F1/F3 다크, F2/F4 라이트) |
| G3. 신규 class 정의 ≥7 | `grep -cE '^\s*\.download-' sketch-wave-4-download-action.html` | ≥7 |
| G4. 메모리 룰 unique slug ≥7 | `grep -oE 'feedback_[a-z_]+\.md' sketch-wave-4-download-action.html \| sort -u \| wc -l` | ≥7 (목표 10) |
| G5. W4 OQ ≥3 | `grep -cE 'W4-OQ #' sketch-wave-4-download-action.html` | ≥3 (목표 4) |
| G6. 보존 카피 verbatim 5건 | `grep -c '방재업무일지 다운로드' ...` ≥2 AND `grep -c '일일업무일지(' ...` ≥2 AND `grep -c '생성 중\.\.\.' ...` ≥2 AND `grep -c '월별 생성 중' ...` ≥2 AND `grep -c '수정 내용은 자동 저장됩니다' ...` ≥4 | 모두 PASS |
| G7. lucide Download SVG ≥8 (4 frame × 2 button = 8 svg) | `grep -c 'class="download-btn-icon"' ...` | ≥8 |
| G8. dot-meta span (안내 줄 1 × 4 frame) | `grep -c '<span class="dot-meta"></span>' ...` | ≥4 |
| G9 (보너스). 헤더 §주석 항목 (보존/폰트/그라데이션/이모지/§6.2/신규class/OQ/메모리룰) ≥8개 | `grep -cE '^\s+\d+\.\s+\*\*' sketch-wave-4-download-action.html` (코멘트 헤더 박제 번호) | ≥8 |

## F. negative gate ≥3 (작성 완료 후 명령으로 검증, ALL FAIL 시 절대 commit 안 함)

| gate | 검증 명령 | 기대값 |
|---|---|---|
| N1. 이모지 0건 | `grep -E '⬇\|⚠\|🎯\|⚡\|🔥' sketch-wave-4-download-action.html` | 매치 0 |
| N2. 그라데이션 0건 | `grep -cE 'linear-gradient\(' sketch-wave-4-download-action.html` | 매치 0 (CSS 실제 호출 `linear-gradient(` 패턴만 검사 — 코멘트 안 단어 인용은 통과) |
| N3. 9·10·11px 0건 | `grep -E 'font-size:\s*(9px\|10px\|11px)' sketch-wave-4-download-action.html` | 매치 0 |
| N4. status- prefix class 0건 | `grep -E 'bg-status-\|text-status-' sketch-wave-4-download-action.html` | 매치 0 |
| N5. w-8 / h-8 0건 | `grep -E '\bw-8\b\|\bh-8\b' sketch-wave-4-download-action.html` | 매치 0 |
| N6. wr+angler / npm run deploy 0건 (마스킹 표기만 OK) | `grep -wE 'wrangler' sketch-wave-4-download-action.html` AND `grep 'npm run deploy' sketch-wave-4-download-action.html` | 매치 0 (메모리 룰 인용은 `wr*ngler` 마스킹) |
| N7. src/** 영향 0 | `git diff --name-only HEAD -- cha-bio-safety/src` | 빈 출력 |
| N8. components.css 수정 0 | `git diff --name-only HEAD -- cha-bio-safety/src/styles/components.css` | 빈 출력 |
| N9. App.tsx 수정 0 | `git diff --name-only HEAD -- cha-bio-safety/src/App.tsx` | 빈 출력 |
| N10. 다른 페이지 디자인 폴더 영향 0 | `git diff --name-only HEAD -- cha-bio-safety/docs/redesign-context/ \| grep -v '^cha-bio-safety/docs/redesign-context/15-daily-report/sketch-wave-4-download-action.html$'` | 빈 출력 |

**N2 처리:** N2 gate 의 검증 패턴은 `linear-gradient\(` (CSS 실제 호출, 여는 괄호 포함) 로 좁힌다. 이렇게 하면 코멘트 헤더에서 그라데이션 폐기 정책을 박제할 때 `linear-gradient` 단어를 그대로 인용해도 gate 통과한다 — 마스킹 / zero-width 트릭 불필요. 단 실제 CSS background 선언으로 호출 시 (예: `background: linear-gradient(135deg,...);`) 매치 1+ → FAIL.

## G. CRITICAL — 작성 절차

1. Write tool 로 단일 파일 작성 (heredoc / cat << 금지). 최소 280줄.
2. 파일 작성 후 G1~G9 + N1~N10 검증을 Bash 로 실행.
3. **모든 gate PASS 시에만** sketch 완성 보고. 단 한 gate 라도 FAIL 시 즉시 재수정.
4. ad-hoc PLAN/SUMMARY 직접 작성 금지 (memory feedback_gsd_workflow_strict). 단 sketch HTML 자체는 이 PLAN 의 산출물 = OK.
5. src/** / components.css / App.tsx / 다른 페이지 docs 손대지 0.
6. `wr*ngler` / `npm run deploy` 명령 실행 0 (메모리 룰 인용 코멘트는 마스킹 표기).
7. **사용자 컨펌 없이 자동 진행** — system reminder "work without stopping for clarifying questions" 룰. OQ #1~4 default 박제 후 진행. 사용자가 다른 답 원하면 후속 redirect.

## H. 출력 보고 형식

작성 + 모든 gate PASS 후 다음 형식으로 1~2 문단 보고:
- 산출 파일 절대 경로 1줄
- G1~G9 PASS / N1~N10 PASS 표 (요약)
- 사용자 컨펌 요청: "W4-OQ #1~4 default 박제로 진행했습니다. F1~F4 frame 시각 컨펌 + OQ 답변 부탁드립니다. 다음은 W5 데스크톱 layout sketch입니다."
- "approved" / "거의 일치" 같은 자기 판단 표현 금지 (memory feedback_avoid_premature_confirmation).
  </action>
  <verify>
    <automated>
test "$(grep -c 'class="frame"' /Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/15-daily-report/sketch-wave-4-download-action.html)" -ge 4 \
  && test "$(grep -cE '^\s*\.download-' /Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/15-daily-report/sketch-wave-4-download-action.html)" -ge 7 \
  && test "$(grep -oE 'feedback_[a-z_]+\.md' /Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/15-daily-report/sketch-wave-4-download-action.html | sort -u | wc -l)" -ge 7 \
  && test "$(grep -cE 'W4-OQ #' /Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/15-daily-report/sketch-wave-4-download-action.html)" -ge 3 \
  && test "$(grep -c 'class="download-btn-icon"' /Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/15-daily-report/sketch-wave-4-download-action.html)" -ge 8 \
  && test "$(grep -c '<span class="dot-meta"></span>' /Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/15-daily-report/sketch-wave-4-download-action.html)" -ge 4 \
  && ! grep -qE '⬇|⚠|🎯|⚡|🔥' /Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/15-daily-report/sketch-wave-4-download-action.html \
  && ! grep -qE 'linear-gradient\(' /Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/15-daily-report/sketch-wave-4-download-action.html \
  && ! grep -qE 'font-size:\s*(9px|10px|11px)' /Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/15-daily-report/sketch-wave-4-download-action.html \
  && ! grep -qE '\bw-8\b|\bh-8\b' /Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/15-daily-report/sketch-wave-4-download-action.html \
  && ! grep -qwE 'wrangler' /Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/15-daily-report/sketch-wave-4-download-action.html \
  && ! grep -q 'npm run deploy' /Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/15-daily-report/sketch-wave-4-download-action.html \
  && test -z "$(git -C /Users/jykevin/Documents/cbc7119-design diff --name-only HEAD -- cha-bio-safety/src 2>/dev/null)"
    </automated>
  </verify>
  <done>
- sketch-wave-4-download-action.html ≥280 줄, 단일 파일로 생성됨
- 4 frame (F1 다크 모바일 393 / F2 라이트 모바일 393 / F3 다크 데스크톱 640 / F4 라이트 데스크톱 640) + disabled variant 각 frame 안 표시
- 신규 class ≥7건 (.download-action / .download-action--desktop / .download-btn / .download-btn--daily / .download-btn--monthly / .download-btn--disabled / .download-btn-icon / .page-footer-note) 인라인 <style> 정의
- 메모리 룰 unique slug ≥7건 (목표 10) 코멘트 헤더 박제
- W4-OQ ≥3건 (목표 4) + default 답 박제
- 보존 카피 5건 (daily/monthly/loading×2/안내) verbatim
- lucide Download SVG ≥8건 (4 frame × 2 button)
- `·` → `<span class="dot-meta"></span>` ≥4건 (4 frame × 1 안내 줄)
- 이모지 0 / linear-gradient( 0 / 9·10·11px 0 / status- prefix 0 / w-8 h-8 0 / wr+angler+npm run deploy 0
- src/** / components.css / App.tsx / 다른 페이지 docs 변경 0
- 사용자 컨펌 보고 시 자기 판단 표현 0
  </done>
</task>

</tasks>

<verification>
**산출물 단일:** `cha-bio-safety/docs/redesign-context/15-daily-report/sketch-wave-4-download-action.html` 만 존재 (newFile)

**검증 절차:**
1. `git status` 결과 — 위 파일 1개만 untracked, 다른 변경 0
2. `wc -l sketch-wave-4-download-action.html` ≥280
3. `<task> Task 1` 의 <verify><automated> 명령 PASS
4. 브라우저로 sketch HTML open → 4 frame 시각 확인 (자동화 불가, 사용자 컨펌)

**WT 시점 사용자 컨펌 4 frame matrix:**
- F1 다크 모바일: daily bg-safe-bar solid + monthly bg-surface-sunken border + 안내 (dot 1)
- F2 라이트 모바일: F1 mirror
- F3 다크 데스크톱: daily + monthly flex 1:1 row gap 8
- F4 라이트 데스크톱: F3 mirror
- 각 frame 안 disabled state variant (생성 중... / 월별 생성 중...) 시각 표시
</verification>

<success_criteria>
- sketch-wave-4-download-action.html 생성됨 (≥280 줄)
- G1~G9 verify gate ALL PASS (코멘트 자체 검수)
- N1~N10 negative gate ALL PASS (이모지/그라데이션/9·10·11px/status-/w-8 h-8/wr+angler 0)
- 보존 카피 5건 verbatim + 핸들러 시그니처 2건 코멘트 박제
- 신규 class ≥7건 인라인 <style> 정의 (W7 grep 추출 가능)
- 메모리 룰 unique slug ≥7건 박제
- W4-OQ ≥3건 + default 박제 (사용자 컨펌 항목)
- W4 작업 완료 후 사용자 컨펌 보고 (자기 판단 표현 0)
- 사용자 redirect 없으면 W5 데스크톱 layout sketch 진입 대기
</success_criteria>

<output>
After completion, create `.planning/quick/260521-3es-redesign-15-daily-report-sketch-wave-4-4/260521-3es-SUMMARY.md` with:
- 산출 파일 절대 경로
- G1~G9 + N1~N10 gate 결과 (PASS/FAIL)
- 4 frame matrix 요약 (F1/F2/F3/F4 각각 markup 핵심)
- W4-OQ 4건 + default 답 박제
- 메모리 룰 unique slug count (≥7)
- 신규 class 7건 명단 (W7 grep 추출용)
- 사용자 컨펌 대기 status (W5 진입 전)
</output>
