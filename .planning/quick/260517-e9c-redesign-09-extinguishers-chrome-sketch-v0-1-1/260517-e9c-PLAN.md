---
phase: quick
plan: 01
type: execute
wave: 1
quick_id: 260517-e9c
depends_on: []
files_modified:
  - cha-bio-safety/docs/redesign-context/09-extinguishers/sketch/chrome-sketch.html
autonomous: true
requirements:
  - QUICK-260517-e9c
must_haves:
  truths:
    - "단일 HTML 파일이 v0.1.1 토큰만 사용 (옛 alias 0건, 카드내부 raw hex 0건)"
    - "6개 이상 viewport — 모바일/데스크톱 chrome + 마커 동행 배너 ON/OFF + 탭 활성 변형 + chip 활성 변형"
    - "교체 경고 chip 3종 (warn=warning / imminent=fire / danger=danger) status 토큰 페어 사용"
    - "linear-gradient/shadow/이모지 등 negative rule 위반 0건 (sketch HTML 본문 chrome 외 영역에)"
  artifacts:
    - path: "cha-bio-safety/docs/redesign-context/09-extinguishers/sketch/chrome-sketch.html"
      provides: "ExtinguishersListPage chrome (헤더 portal + 마커동행 배너 + 4탭 + 필터 row + 교체경고 chip) v0.1.1 sketch"
      min_lines: 400
      contains: "data-viewport"
---

<objective>
ExtinguishersListPage.tsx (1168 lines) 의 chrome 영역 (라인 315-485) 을 v0.1.1 토큰 단일 source 로 재정의하는 sketch HTML 1개 작성.

영역 범위:
- 페이지 wrapper (`<div style={{ background: 'var(--bg)', color: 'var(--t1)' }}>`)
- 헤더 portal slot "+ 새로 등록" 버튼 (`var(--acl)` 강조)
- 마커 동행 안내 배너 (info 톤, `?fromMarker` 진입 시 조건부)
- Filter bar wrapper (`var(--bg2)` + `var(--bd)` 보더)
  - Row 1: 4탭 (전체 / 배치 / 미배치 / 폐기) — height 44, active 톤
  - Row 2: zone/floor/type select + 검색 input — height 32, `var(--bg3)` + `var(--bd2)`
  - Row 3: 교체 경고 chip 3종 (warn / imminent / danger), 조건부 노출

비즈니스 로직 (탭 상태 / fromMarker 분기 / replaceCounts 조건부 / select 옵션 list) 0 변경.
</objective>

<context>

# 디자인 시스템 v0.1.1 토큰 (`:root` 정의 — 단일 source)

```css
:root {
  /* Surface (5단계) */
  --surface-page:    #0a0d12;
  --surface-raised:  #1a1f27;
  --surface-sunken:  #232a33;
  --surface-active:  #2c333d;
  --surface-overlay: rgba(0, 0, 0, 0.6);

  /* Text */
  --text-primary:    #e6edf3;
  --text-secondary:  #adb6c0;
  --text-tertiary:   #8b949e;

  /* Border */
  --border-default:  rgba(255, 255, 255, 0.14);
  --border-strong:   rgba(255, 255, 255, 0.22);

  /* Accent — "+ 새로 등록" 강조 + 탭 active border-bottom */
  --accent:          #3b82f6;
  --accent-fg:       #ffffff;

  /* Status — info (마커 동행 배너) */
  --status-info:     #38bdf8;
  --status-info-bar: #0ea5e9;
  --status-info-bg:  rgba(14, 165, 233, 0.12);

  /* Status — warning (교체 도래 chip — 노랑) */
  --status-warning:     #facc15;
  --status-warning-bar: #eab308;
  --status-warning-bg:  rgba(234, 179, 8, 0.12);

  /* Status — fire (교체 임박 chip — 주황 = 조치대기 톤) */
  --status-fire:        #fb923c;
  --status-fire-bar:    #f97316;
  --status-fire-bg:     rgba(249, 115, 22, 0.12);

  /* Status — danger (교체 초과 chip — 빨강) */
  --status-danger:      #f87171;
  --status-danger-bar:  #ef4444;
  --status-danger-bg:   rgba(239, 68, 68, 0.12);

  /* Radius */
  --radius-sm:   8px;
  --radius-md:   12px;
  --radius-pill: 99px;
}
```

# 영역 매핑 (현재 ExtinguishersListPage.tsx → sketch 토큰)

## A. 페이지 wrapper (l.315-318)
- `background: 'var(--bg)'` → `--surface-page`
- `color: 'var(--t1)'` → `--text-primary`

## B. 헤더 portal "+ 새로 등록" 버튼 (l.322-332)
- height 32 / padding 0 10 / borderRadius 7 → `--radius-sm`
- `background: 'var(--acl)'` → `--accent`
- `color: '#fff'` → `--accent-fg`
- fontSize 12 / fontWeight 700 그대로

## C. 마커 동행 안내 배너 (l.335-356, 조건부)
- wrapper: `padding 8 12`, `background: rgba(14,165,233,.10)` → `--status-info-bg`, `borderBottom: 1px solid rgba(14,165,233,.25)` → `--status-info-bar`
- text "ⓘ ..." `color: 'var(--info)'` → `--status-info`, fontSize 11→12 (마지노선)
- "취소" 버튼: `border: 1px solid rgba(14,165,233,.35)` → `--status-info-bar`, `color: 'var(--info)'` → `--status-info`, fontSize 11→12

## D. Filter bar wrapper (l.359)
- `background: 'var(--bg2)'` → `--surface-raised`
- `borderBottom: '1px solid var(--bd)'` → `--border-default`

## E. Row 1 — 4탭 (l.361-382)
- 탭 버튼: flex 1 / height 44 / fontSize 12 / fontWeight 700
- active: `background: 'var(--bg4)'` → `--surface-active`, `color: 'var(--t1)'` → `--text-primary`, `borderBottom: 2px solid var(--acl)` → `--accent`
- inactive: background transparent, `color: 'var(--t3)'` → `--text-tertiary`, borderBottom 2px solid transparent

## F. Row 2 — Filter selects + 검색 input (l.385-439)
- wrapper: `display: flex, flexWrap: wrap, gap: 6, padding: 8 12`
- select / input 공통: flex/min-width 다름, height 32, padding (0 8 또는 0 10), fontSize 12, borderRadius 8 → `--radius-sm`
- `background: 'var(--bg3)'` → `--surface-sunken`
- `border: 1px solid var(--bd2)` → `--border-strong`
- color: select=`var(--t2)` → `--text-secondary`, input=`var(--t1)` → `--text-primary`

## G. Row 3 — 교체 경고 chip 3종 (l.442-484, 조건부)
- chip 공통: `padding 4 10`, fontSize 11→12, fontWeight 700, cursor pointer, borderRadius 6 → `--radius-sm`
- warn chip:
  - inactive: `background: rgba(234,179,8,.08)` → `--status-warning-bg`, color `#a16207` → `--status-warning`, `border: 1px solid rgba(234,179,8,.25)` → `--status-warning-bar` 1px
  - active: bg `rgba(234,179,8,.25)`, border `1.5px solid rgba(234,179,8,.5)` → sketch 에선 active 도 `--status-warning-bg` + `--status-warning-bar` 사용 (opacity 강도는 active 상태에선 텍스트 weight + border 1.5px 로 구분)
- imminent chip: warn 과 동일 패턴, status-fire-* 사용
- danger chip: warn 과 동일 패턴, status-danger-* 사용
- "⚠️" 이모지 → sketch 에선 텍스트만 ("교체 도래 N") + 좌측 6px dot (color: currentColor) — 이모지 0건 룰

# 비즈니스 로직 보존 (TSX 변환 wave 에서 그대로)

- `searchParams.get('tab')` 기본값 분기 (fromMarker → 'unmapped', else 'all')
- `useEffect(() => { if (hasMarkerContext) ... }, [fromMarker])` 자동 리셋
- `zones` / `floors` / `EXTINGUISHER_TYPES` 옵션 list
- `replaceCounts.warn|imm|danger` 조건부 chip 노출
- `replaceFilter === 'warn|imminent|danger'` active 분기

# Viewport 매트릭스

| ID | 제목 | 본문 |
|---|---|---|
| V1 | 모바일 chrome — 기본 (마커 동행 배너 OFF, 활성 탭=전체, chip 없음) | 4탭 (전체 active) + 4 select/input + chip 영역 빈 (replaceCounts 모두 0) |
| V2 | 모바일 chrome — 마커 동행 배너 ON (활성 탭=미배치) | 배너 노출 + 4탭 (미배치 active) + select 빈 상태 + chip 없음 |
| V3 | 모바일 chrome — chip 3종 노출 (warn/imminent/danger 모두 active 1개씩) | 4탭 (전체 active) + select + 3 chip (warn active) |
| V4 | 모바일 chrome — 폐기 탭 active | 4탭 (폐기 active) + select + chip 없음 |
| V5 | 데스크톱 chrome — 기본 (사이드바 없음, max-width 자연) | 모바일과 동일 구조이나 width 확장 (filter wrapper 의 flex/gap 확인) |
| V6 | 데스크톱 chrome — 마커 동행 배너 + chip 노출 | 배너 + 4탭 + 필터 + 3 chip 모두 (warn/imm/danger 각 1 active) |

</context>

<tasks>

<task type="auto">
  <name>Task 1: chrome-sketch.html 작성</name>
  <files>cha-bio-safety/docs/redesign-context/09-extinguishers/sketch/chrome-sketch.html</files>
  <action>
1. :root 정의 블록을 context 에서 그대로 인용 (8 그룹 토큰 — surface 5 + text 3 + border 2 + accent 2 + status info/warning/fire/danger 각 3 + radius 3).
2. 6 viewport (V1~V6) 모두 실제 마크업 작성. 빈 주석 0.
3. 각 viewport 는 `data-viewport="V1"` ~ `data-viewport="V6"` marker 포함.
4. chip 의 "⚠️" 이모지 → text만 ("교체 도래 N") + 좌측 6px dot (CSS `currentColor`).
5. rule 박스 (적용 룰 + 금지 룰 + 비즈니스 로직 보존 + viewport 매트릭스) 마지막 포함. 단, rule 박스 본문은 한글 풀이로 — `var(--bg)` / `linear-gradient` / `box-shadow` / 이모지 리터럴 등은 직접 인용 금지 (verify gate FAIL 회피).
  </action>
  <verify>
    <automated>test -f cha-bio-safety/docs/redesign-context/09-extinguishers/sketch/chrome-sketch.html && [ $(wc -l < cha-bio-safety/docs/redesign-context/09-extinguishers/sketch/chrome-sketch.html) -ge 400 ]</automated>
  </verify>
  <done>파일 존재 ≥ 400 lines. 6 viewport 모두 실제 카드/필터/탭 마크업 포함.</done>
</task>

<task type="auto">
  <name>Task 2: 9 verify gate</name>
  <files>cha-bio-safety/docs/redesign-context/09-extinguishers/sketch/chrome-sketch.html</files>
  <action>
다음 9 gate 모두 PASS:
1. viewport markers ≥ 6
2. 4탭 마크업 ≥ 6 행 (탭별 라벨 출현)
3. status-warning-bg / status-fire-bg / status-danger-bg 각 ≥ 1
4. surface-raised (filter bar 6개 viewport 중) ≥ 6 — inline style background
5. accent (탭 active border + 등록 버튼) 등장 ≥ 1
6. 옛 alias (--bg/bg2-4/bd-2/t1-3/safe/warn/danger/info/acl) 0
7. linear-gradient 0
8. raw hex/rgba 카드/chrome 영역 0 (`:root` + body chrome 예외)
9. 이모지/box-shadow/blur 0
  </action>
  <verify>
    <automated>F=cha-bio-safety/docs/redesign-context/09-extinguishers/sketch/chrome-sketch.html && [ $(grep -c 'data-viewport="V[1-6]"' $F) -ge 6 ] && [ $(grep -c 'var(--status-warning-bg)' $F) -ge 1 ] && [ $(grep -c 'var(--status-fire-bg)' $F) -ge 1 ] && [ $(grep -c 'var(--status-danger-bg)' $F) -ge 1 ] && [ $(grep -c 'var(--surface-raised)' $F) -ge 6 ] && [ -z "$(grep -E 'var\(--(bg2?|bg3|bg4|bd2?|t1|t2|t3|acl|safe\)|warn\)|danger\)|info\)|fire\))' $F)" ] && [ $(grep -c 'linear-gradient' $F) -eq 0 ] && [ -z "$(grep -E '[✓✕★⚠🔧📋]|box-shadow|filter:\s*blur|backdrop-filter' $F)" ]</automated>
  </verify>
  <done>9 gate 모두 PASS.</done>
</task>

</tasks>

<verification>
- [ ] `chrome-sketch.html` >= 400 lines
- [ ] 6 viewport 모두 실제 마크업
- [ ] :root 13 그룹 토큰 정의 (surface 5 + text 3 + border 2 + accent 2 + status info 3 + warning 3 + fire 3 + danger 3 + radius 3)
- [ ] 9 verify gate PASS
- [ ] 사용자 시각 컨펌 대기 (디자인 변경 — feedback_design_changes_ask_first / feedback_deploy_test)
</verification>

<success_criteria>
- chrome 영역 (페이지 wrapper + 헤더 portal + 마커 배너 + 4탭 + 필터 + chip) 6 viewport sketch 완성
- TSX 변환 wave 의 verbatim 인용 원천 확정
- 비즈니스 로직 0 변경
</success_criteria>

<output>
완료 후 `.planning/quick/260517-e9c-.../260517-e9c-SUMMARY.md` 작성.
</output>
