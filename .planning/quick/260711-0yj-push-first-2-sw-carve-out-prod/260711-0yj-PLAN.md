---
quick_id: 260711-0yj
slug: push-first-2-sw-carve-out-prod
status: in-progress
date: 2026-07-11
description: 화재수신반 push-first + 라이브뷰 2초 통일 + SW carve-out prod(cbc7119) 이식
source: staging cbc7119-data fe58cd6 (검증 완료)
---

# Quick 260711-0yj — 화재수신반 push-first + 2초 라이브폴 + SW carve-out (prod 이식)

## 목표
staging(cbc7119-data, fe58cd6)에서 검증된 "OCR을 푸시 경로에서 분리(push-first)" 구조를 prod 코드에 동일 적용.
- 트리거 시 `location=null`로 즉시 푸시 → 에이전트가 OCR 완료 후 신규 엔드포인트로 위치 patch.
- 라이브뷰 폴 간격 2초 통일(경보중), 위치 폴백 문구 "수신반 확인 필요"로 통일.
- SW가 실시간 패널 경로(`/api/public/panel/*`, `/api/panel/status`)를 NetworkOnly로 우회.

## 사전 확인 (완료)
- `panel_alarms.location` 컬럼: prod `cha-bio-db` **이미 존재**(pragma count=1). 신규 마이그레이션 불필요.
- 브랜치 `production`, HEAD=origin/production=`fbc7ee14`(clean). production-sync 게이트 baseline 일치.
- worktree OFF(config), branch_name null → production 브랜치 직접 작업.

## 백엔드 (cha-bio-safety/functions/)

### T-B1 신규 `api/alarm/[id]/location.ts` (에이전트 OCR patch)
- `POST /api/alarm/:id/location`, X-Agent-Key 가드(`assertAgentKey`), body `{location}` trim, 빈문자/누락=null.
- import 깊이 `../../../` (ack.ts/resolve.ts 동일).

### T-B2 `_middleware.ts` — 동적 id JWT 예외
- PUBLIC_PREFIX 정의 직후 `const PUBLIC_PATTERN = [/^\/api\/alarm\/[^/]+\/location$/]` 추가.
- 게이트 skip-auth `if` 끝에 `|| PUBLIC_PATTERN.some(re => re.test(url.pathname))` 추가.

### T-B3 `_lib/alarm.ts` — LOCATION_LABEL 제거, null 통과
- `export const LOCATION_LABEL = '방재실 화재수신반'` (+ 주석) 삭제.
- mapAlarm: `location: r.location ?? LOCATION_LABEL` → `location: r.location`.
- mapAlarmSummary: `location: r.location ?? LOCATION_LABEL` → `location: r.location`.

### T-B4 `_lib/push.ts` — 폴백 문구
- buildPanelPayload: `const loc = a.location ? \` · ${a.location}\` : ''` → 거짓 분기 `' · 수신반 확인 필요'`.

### T-B5 `api/alarm/trigger.ts`
- import에서 `LOCATION_LABEL` 제거.
- `location: body.location ?? LOCATION_LABEL` → `location: body.location ?? null`.

### T-B6 `api/alarm/renotify.ts`
- import에서 `LOCATION_LABEL` 제거.
- `location: row.location ?? LOCATION_LABEL` → `location: row.location` (patch 완료본이 실제 위치 표기).

## 프론트 (cha-bio-safety/src/)

### T-F1 `pages/FireAlarmPage.tsx`
- 풀스크린 위치 `{alarm?.location ?? '장소 확인 필요'}` → 리터럴 `수신반 확인 필요` 고정.
- alarm-active `refetchInterval: 15_000` → `2_000`.

### T-F2 `components/PanelEventRow.tsx`
- `{item.location ?? '위치 미상'}` → `?? '수신반 확인 필요'`.

### T-F3 `pages/InspectionPage.tsx` (FireAlarmModal + DesktopInspectionView 양쪽)
- panel-status·alarm-active 폴 `15_000` → `2_000` (4곳, replace_all).
- alarm-events 폴 `refetchInterval: 30_000` → `activeAlarm ? 2_000 : 30_000` (2곳, replace_all — 상시 2초 아님, 경보중만).
- 위치 폴백 `?? '위치 확인중'` → `?? '수신반 확인 필요'` (3곳, replace_all).

### T-F4 `pages/DashboardPage.tsx` (계획서 밖 — 주의)
- panel-status·alarm-active `refetchInterval: 30_000` → `2_000` (타깃 2곳, staleTime·타 쿼리 불변).
- 라이브카드 폴백 `?? '장소 확인'` → `?? '수신반 확인 필요'` (2곳). `|| '장소 미기록'`(수동 이력)은 불변.

### T-F5 `sw.ts`
- import에 `NetworkOnly` 추가.
- 일반 `/api/` NetworkFirst **앞에** carve-out 등록: GET & (`/api/public/panel/` prefix || `=== /api/panel/status`) → NetworkOnly.

## 검증
1. `npx tsc --noEmit` exit 0 + `npm run build` 성공.
2. 각 변경 grep 검증(LOCATION_LABEL 잔존 0, PUBLIC_PATTERN 존재, 폴 간격/문구 반영).
3. 적대적 diff 리뷰(read-only 에이전트) — 스펙 대조, 의도외 변경 0.
4. 배포 후 curl 스모크: `POST /api/alarm/PA-X/location`(무인증)→`agent unauthorized`, `GET /api/alarm/active`(무JWT)→`인증이 필요합니다`.

## 배포
production-sync 게이트 통과 → `npx wrangler pages deploy dist --project-name=cbc7119 --branch=production` (CWD=cha-bio-safety). 배포 후 스모크 + production-sync.md 갱신.

## 범위 밖
- 에이전트 `~/panel-agent/agent.py` §1-A(트리거 location=null + OCR 비동기 → patch POST)는 별도(이 repo 아님).
- events.ts 720h 캡 해제 / b5v 재적용은 staging-first(이 콘솔 아님).
