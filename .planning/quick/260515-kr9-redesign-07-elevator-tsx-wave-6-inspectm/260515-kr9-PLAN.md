---
phase: quick-260515-kr9
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cha-bio-safety/src/pages/ElevatorPage.tsx
autonomous: true
requirements:
  - QUICK-260515-kr9
must_haves:
  truths:
    - "ElevatorPage.tsx 내 `function InspectModal(` 정의 0건"
    - "ElevatorPage.tsx 내 문자열 `'inspect_new'` 0건 (Modal type union 멤버 + JSX 분기 둘 다 제거)"
    - "ElevatorPage.tsx 내 식별자 `submitInspect` 0건 (useMutation 정의 + onSubmit 호출 둘 다 제거)"
    - "ElevatorPage.tsx 내 식별자 `primaryBtnSt` 0건 (글로벌 상수 정의 + 사용처 모두 0)"
    - "ElevatorPage.tsx 내 식별자 `inputSt` 사용처 3건 보존 (RepairListSection — line 2975/2980/2984 상당) + 글로벌 정의 1건 보존"
    - "Modal type union 의 다른 멤버 보존: `fault_new` / `fault_resolve` / `repair_new` / `ev_detail` 4개 모두 그대로"
    - "Wave 1~5 변환 결과 (list 탭 / TYPE_ICON_COMPONENT / EvSelector / EsBtn / EsNodeMap / Fault 3 모달 / RepairNewModal / EvDetailModal) 단 한 줄도 수정 X"
    - "ModalWrap / Field / MultiPhotoUpload / CertViewerModal / RepairListSection / icons.tsx / tailwind.config.js / 다른 컴포넌트 0건 수정"
    - "TypeScript 컴파일 0 에러 (`npx tsc --noEmit` 또는 vite build 통과)"
    - "`npm run build` PASS — 새 ElevatorPage chunk 빌드 성공"
    - "ElevatorPage.tsx 라인 수 약 -110줄 (InspectModal 함수 약 95줄 + submitInspect mutation 약 27줄 + primaryBtnSt 약 5줄 + Modal type 멤버 1개 + JSX 분기 1줄)"
    - "시각적 변경 0건 — InspectModal 은 진입점 없는 dead code 였으므로 사용자가 보는 페이지 동일"
  artifacts:
    - path: "cha-bio-safety/src/pages/ElevatorPage.tsx"
      provides: "InspectModal/submitInspect/primaryBtnSt/'inspect_new' 제거된 cleanup 후 ElevatorPage"
      contains: "function ElevatorPage"
  key_links: []
---

<objective>
redesign/07-elevator TSX 변환 시리즈 Wave 6 (마지막). 사용자 입력 진입점 0건인 InspectModal dead code 와 부속 자원 (submitInspect mutation, 'inspect_new' Modal type 멤버, JSX 렌더 분기, primaryBtnSt 글로벌 상수) 을 ElevatorPage.tsx 에서 제거한다.

Purpose: Wave 1~5 변환으로 ElevatorPage 의 사용자 입력 모달은 고장 3종 + 수리 + 상세 5개로 확정됨. InspectModal 은 메모 (project_elevator_page_tabs.md) 및 2C SUMMARY (260515-g61) 에서 dead code 로 사후 정정됨. primaryBtnSt 는 Wave 4 RepairNewModal 변환 시 inline 으로 교체되어 InspectModal 외 사용처 0건. inputSt 는 RepairListSection 3건 사용 중이므로 보존.

Output: ElevatorPage.tsx 약 -110줄. 다른 파일/컴포넌트/시각 변경 0건.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/quick/260515-kr9-redesign-07-elevator-tsx-wave-6-inspectm/

# 메모리 권위
# - project_elevator_page_tabs.md → InspectModal = dead code (사용자 입력 모달은 고장/수리 탭만)
# - 2C SUMMARY (260515-g61) → InspectModal 사후 정정 마킹
# - project_redesign_workflow.md → redesign/NN 브랜치 + 즉시 푸시
# - feedback_redesign_sketch_rule_enforcement.md → cleanup 도 verify gate 강제

# 직전 변환 (참고만, 수정 X)
# - Wave 1 (260515-3mc): list 탭 + TYPE_ICON_COMPONENT
# - Wave 2 (260515-ia6): EvSelector + EsBtn + EsNodeMap
# - Wave 3 (260515-iz1): Fault 3 모달
# - Wave 4 (260515-jp3): RepairNewModal (primaryBtnSt 사용 제거)
# - Wave 5 (260515-k5p): EvDetailModal

# 타깃 라인 (Read 검증 완료, 2026-05-15)
# - line 95: `type Modal = null | 'fault_new' | 'fault_resolve' | 'inspect_new' | 'repair_new' | 'ev_detail'`
#   → `'inspect_new' | ` 만 제거 (다른 4 멤버 보존)
# - line 483~509: `const submitInspect = useMutation({ ... })` — 전체 블록 (mutationFn / onSuccess / onError 포함)
#   → onError 줄 ` })` 이 line 509. 다음 빈 줄 1개 포함 가능 (포맷 자유)
# - line 1670: `{modal === 'inspect_new'  && <InspectModal      elevators={elevators} selected={selectedEv} onClose={() => setModal(null)} onSubmit={b => submitInspect.mutate(b)} loading={submitInspect.isPending} />}`
#   → 전체 한 줄 삭제
# - line 2365~2460: `function InspectModal({ elevators, selected, onClose, onSubmit, loading }: { ... }) { ... }`
#   → 끝은 line 2460 의 닫는 `}` (다음 line 2462 가 `// ── 인증서 뷰어 모달 ─────────`). 약 95줄 함수 전체 + 위 빈 줄 1개 + 카테고리 주석 (있다면) 제거. 단 line 2462 의 다음 컴포넌트 헤더 주석은 보존
# - line 2895~2899: `const primaryBtnSt: React.CSSProperties = { ... }` (5줄)
#   → 전체 제거. 위 line 2894 의 `// ── 스타일 ────` 헤더 주석은 inputSt 가 같은 섹션에 있으므로 보존
# - line 2900~ : `const inputSt: React.CSSProperties = { ... }` — 보존 (RepairListSection 3건 사용)

@cha-bio-safety/src/pages/ElevatorPage.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: InspectModal dead code + 부속 자원 5개 제거 + 검증</name>
  <files>cha-bio-safety/src/pages/ElevatorPage.tsx</files>
  <action>
ElevatorPage.tsx 에서 InspectModal dead code 및 부속 자원 6개 항목을 제거한다. 추가 코드 0건.

### 제거 항목 (정확한 위치)

**1. Modal type union 의 `'inspect_new'` 멤버 (line 95 근처)**

Edit (old → new):
```
type Modal = null | 'fault_new' | 'fault_resolve' | 'inspect_new' | 'repair_new' | 'ev_detail'
```
→
```
type Modal = null | 'fault_new' | 'fault_resolve' | 'repair_new' | 'ev_detail'
```

**2. `submitInspect` useMutation 정의 (line 483~509 전체 블록)**

`const submitInspect = useMutation({` 부터 매칭되는 `})` 까지 전체 블록 제거. mutationFn / onSuccess / onError 모두 포함. 블록 위/아래 빈 줄은 1개로 유지 (포맷 정상화).

확인된 끝 라인: line 509 의 `})`. 그 위는 onError, 그 다음 line 510 빈 줄, line 511 `const unresolvedCount = ...` 보존.

**3. `<InspectModal>` JSX 렌더 분기 (line 1670)**

다음 한 줄 통째로 삭제:
```
      {modal === 'inspect_new'  && <InspectModal      elevators={elevators} selected={selectedEv} onClose={() => setModal(null)} onSubmit={b => submitInspect.mutate(b)} loading={submitInspect.isPending} />}
```
주변 다른 모달 분기 (`fault_new` / `repair_new` / `ev_detail` 등) 는 보존.

**4. `function InspectModal(...)` 정의 (line 2365~2460 약 95줄)**

`function InspectModal({ elevators, selected, onClose, onSubmit, loading }: {` 부터 매칭되는 `}` 까지 전체 함수 제거. 함수 위의 카테고리 주석이 InspectModal 전용이면 같이 제거. line 2462 의 `// ── 인증서 뷰어 모달 ───` 헤더 주석과 CertViewerModal 함수는 보존.

**5. `const primaryBtnSt` 글로벌 상수 (line 2895~2899)**

다음 정의 블록 제거:
```
const primaryBtnSt: React.CSSProperties = {
  width:'100%', padding:'13px 0', borderRadius:12, border:'none',
  background:'linear-gradient(135deg,#1d4ed8,#0ea5e9)',
  color:'#fff', fontSize:13, fontWeight:700, cursor:'pointer',
}
```
line 2894 의 `// ── 스타일 ──────` 헤더 주석은 바로 아래 inputSt 가 같은 섹션을 차지하므로 보존.

**6. 미사용 import 확인**

InspectModal 은 같은 파일 내 함수라 별도 import 없음. 그러나 InspectModal 안에서만 쓰던 외부 import (예: useState 외 특수 hook, util) 가 다른 곳에서도 쓰이는지 grep 확인. 본 파일은 React/hook/icons/api 만 import 하므로 추가 import 정리 필요 없음 가능성 99%. 단, 빌드 후 unused import 경고 시 해당 import 만 제거.

### 보존 항목 (절대 건드리지 말 것)

- `const inputSt: React.CSSProperties = { ... }` (line 2900~) — RepairListSection 3건 사용 중
- Modal type union 의 4 멤버: `fault_new` / `fault_resolve` / `repair_new` / `ev_detail`
- Wave 1~5 변환 결과 전체 (list 탭, TYPE_ICON_COMPONENT, EvSelector, EsBtn, EsNodeMap, Fault 3 모달, RepairNewModal, EvDetailModal)
- ModalWrap / Field / MultiPhotoUpload / CertViewerModal / RepairListSection
- icons.tsx, tailwind.config.js, 다른 페이지 파일

### 작업 순서 (안전)

1. type Modal union 수정 (line 95) — 타입 좁히기 먼저, TS 에러로 후속 사용처 자동 검출 가능
2. JSX 렌더 분기 삭제 (line 1670)
3. submitInspect useMutation 정의 삭제 (line 483~509)
4. function InspectModal 정의 삭제 (line 2365~2460)
5. primaryBtnSt 상수 정의 삭제 (line 2895~2899)
6. `npx tsc --noEmit` 또는 vite build 로 TS 에러 0 확인 (auto: `npm run build` 가 tsc 포함)
7. grep gate 통과 확인 (verify 블록)
8. commit
  </action>
  <verify>
    <automated>
cd cha-bio-safety && \
  echo "=== grep gates ===" && \
  echo "function InspectModal: $(grep -c 'function InspectModal' src/pages/ElevatorPage.tsx) (expect 0)" && \
  echo "'inspect_new': $(grep -c \"'inspect_new'\" src/pages/ElevatorPage.tsx) (expect 0)" && \
  echo "submitInspect: $(grep -c 'submitInspect' src/pages/ElevatorPage.tsx) (expect 0)" && \
  echo "primaryBtnSt: $(grep -c 'primaryBtnSt' src/pages/ElevatorPage.tsx) (expect 0)" && \
  echo "inputSt total: $(grep -c 'inputSt' src/pages/ElevatorPage.tsx) (expect 4 — 1 def + 3 uses)" && \
  echo "Modal union members preserved: $(grep -c \"'fault_new'\\|'fault_resolve'\\|'repair_new'\\|'ev_detail'\" src/pages/ElevatorPage.tsx) (expect >= 4 lines)" && \
  test $(grep -c 'function InspectModal' src/pages/ElevatorPage.tsx) -eq 0 && \
  test $(grep -c "'inspect_new'" src/pages/ElevatorPage.tsx) -eq 0 && \
  test $(grep -c 'submitInspect' src/pages/ElevatorPage.tsx) -eq 0 && \
  test $(grep -c 'primaryBtnSt' src/pages/ElevatorPage.tsx) -eq 0 && \
  test $(grep -c 'inputSt' src/pages/ElevatorPage.tsx) -ge 3 && \
  echo "=== npm build ===" && \
  npm run build 2>&1 | tail -30 && \
  echo "=== build PASS ==="
    </automated>
  </verify>
  <done>
- grep gate 모두 통과: InspectModal/'inspect_new'/submitInspect/primaryBtnSt 0건, inputSt >= 3 보존
- Modal type union 의 4 멤버 (fault_new/fault_resolve/repair_new/ev_detail) 보존
- npm run build PASS — TypeScript 0 에러, ElevatorPage chunk 빌드 성공
- ElevatorPage.tsx 라인 수 약 -110줄 (수치 검증: `git diff --stat cha-bio-safety/src/pages/ElevatorPage.tsx`)
- Wave 1~5 변환 결과 코드는 단 한 줄도 수정되지 않음 (diff 로 검증)
- 다른 파일 0건 수정 (`git status` 에 ElevatorPage.tsx 만 표시)
  </done>
</task>

<task type="auto">
  <name>Task 2: SUMMARY 작성 + commit</name>
  <files>.planning/quick/260515-kr9-redesign-07-elevator-tsx-wave-6-inspectm/260515-kr9-SUMMARY.md</files>
  <action>
SUMMARY 작성 후 single commit 으로 마무리.

### SUMMARY 내용

@$HOME/.claude/get-shit-done/templates/summary.md 구조 따름. 필수 섹션:

- **What changed**: ElevatorPage.tsx 에서 InspectModal dead code + 부속 자원 (submitInspect mutation, 'inspect_new' Modal type 멤버, JSX 렌더 분기, primaryBtnSt 글로벌 상수) 6개 항목 제거. 약 -110줄.
- **Why**: 메모리 (project_elevator_page_tabs.md, 2C SUMMARY 260515-g61) 권위로 InspectModal 은 사용자 입력 진입점 0건인 dead code 로 확정. Wave 4 RepairNewModal 변환으로 primaryBtnSt 사용처 0건. Wave 1~5 변환 완료 후 마지막 cleanup.
- **Preserved**: inputSt 글로벌 상수 (RepairListSection 3건 사용), Modal type 의 다른 4 멤버, Wave 1~5 변환 결과, 다른 컴포넌트/파일.
- **Verification**: grep gate 5개 통과, npm build PASS, git diff stat -110줄, git status ElevatorPage.tsx 만 변경.
- **Out of scope**: inputSt 글로벌 상수 → RepairListSection 변환 시 처리. 다른 dead code → 별도 wave.
- **Visual impact**: 0건 (dead code 제거).
- **Next**: redesign/07-elevator TSX 변환 시리즈 Wave 6 = 마지막. main 머지 후 다음 페이지로.

### Commit

```bash
git add cha-bio-safety/src/pages/ElevatorPage.tsx \
        .planning/quick/260515-kr9-redesign-07-elevator-tsx-wave-6-inspectm/260515-kr9-PLAN.md \
        .planning/quick/260515-kr9-redesign-07-elevator-tsx-wave-6-inspectm/260515-kr9-SUMMARY.md
git commit -m "chore(260515-kr9): Wave 6 cleanup — InspectModal dead code + primaryBtnSt 제거"
```

배포 X. redesign/07-elevator 브랜치 push 만 (사용자 명시 컨펌 후 main 머지 — feedback_deploy_test.md).
  </action>
  <verify>
    <automated>
cd /Users/jykevin/Documents/cbc7119-design && \
  ls .planning/quick/260515-kr9-redesign-07-elevator-tsx-wave-6-inspectm/260515-kr9-SUMMARY.md && \
  git log --oneline -1 | grep -q '260515-kr9' && \
  echo "=== final build verify ===" && \
  cd cha-bio-safety && npm run build 2>&1 | tail -10
    </automated>
  </verify>
  <done>
- SUMMARY.md 작성 완료
- single commit 생성 (PLAN + SUMMARY + ElevatorPage.tsx)
- 최종 npm build PASS 재확인
- redesign/07-elevator 브랜치 push (사용자 별도 컨펌 전 main 머지 X)
  </done>
</task>

</tasks>

<verification>
## 전체 검증

```bash
cd cha-bio-safety

# 1. grep gates (모두 expected 값 일치)
grep -c 'function InspectModal' src/pages/ElevatorPage.tsx   # 0
grep -c "'inspect_new'" src/pages/ElevatorPage.tsx           # 0
grep -c 'submitInspect' src/pages/ElevatorPage.tsx           # 0
grep -c 'primaryBtnSt' src/pages/ElevatorPage.tsx            # 0
grep -c 'inputSt' src/pages/ElevatorPage.tsx                 # >= 3 (정의 1 + RepairListSection 3 = 보통 4)

# 2. Modal union 다른 멤버 보존
grep -E "type Modal = " src/pages/ElevatorPage.tsx
# expect: type Modal = null | 'fault_new' | 'fault_resolve' | 'repair_new' | 'ev_detail'

# 3. build
npm run build   # PASS

# 4. diff stat
cd .. && git diff --stat cha-bio-safety/src/pages/ElevatorPage.tsx
# expect: 약 -110 줄

# 5. 다른 파일 0건 변경
git status   # ElevatorPage.tsx + .planning/quick/.../{PLAN,SUMMARY}.md 만
```
</verification>

<success_criteria>
- [ ] InspectModal 함수 정의 0건
- [ ] 'inspect_new' Modal type 멤버 0건
- [ ] `<InspectModal>` JSX 분기 0건
- [ ] submitInspect useMutation 정의 + 호출 0건
- [ ] primaryBtnSt 글로벌 상수 + 사용처 0건
- [ ] inputSt 글로벌 정의 + RepairListSection 3건 사용 보존
- [ ] Modal type union 의 4 멤버 (fault_new/fault_resolve/repair_new/ev_detail) 보존
- [ ] Wave 1~5 변환 결과 0건 수정
- [ ] 다른 컴포넌트/파일 0건 수정
- [ ] TypeScript 0 에러, npm build PASS
- [ ] ElevatorPage.tsx 약 -110줄
- [ ] SUMMARY 작성 + single commit
- [ ] redesign/07-elevator push (main 머지는 사용자 컨펌 후)
</success_criteria>

<output>
After completion, ensure `.planning/quick/260515-kr9-redesign-07-elevator-tsx-wave-6-inspectm/260515-kr9-SUMMARY.md` exists.
</output>
