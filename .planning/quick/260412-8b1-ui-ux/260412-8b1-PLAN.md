---
phase: quick
plan: 260412-8b1
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/LoginPage.tsx
autonomous: true
must_haves:
  truths:
    - "Desktop (1920x1080)에서 로그인 카드가 화면 중앙에 위치한다"
    - "Mobile에서 기존 full-width 레이아웃이 그대로 동작한다"
    - "모든 기존 기능(담당자 선택, 폼, 제출)이 정상 작동한다"
  artifacts:
    - path: "src/pages/LoginPage.tsx"
      provides: "Responsive login page with desktop card layout"
  key_links:
    - from: "LoginPage.tsx"
      to: "authApi.login"
      via: "handleSubmit"
      pattern: "authApi\\.login"
---

<objective>
데스크톱 환경(1920x1080)에서 로그인 페이지를 중앙 카드 레이아웃으로 개선한다.

Purpose: 현재 모바일 전용 full-width 레이아웃이 데스크톱에서 늘어져 보이는 문제 해결. 모바일 레이아웃은 유지하면서 데스크톱에서 전문적인 센터 카드 UI를 제공한다.
Output: 반응형 LoginPage.tsx (모바일: 기존 유지, 데스크톱: 중앙 카드)
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/pages/LoginPage.tsx
@src/App.tsx

CSS variables already defined:
- --bg (#0d1117), --bg2 (#161b22), --bg3, --bd, --bd2, --t1, --t3
- Dark theme throughout the app
</context>

<tasks>

<task type="auto">
  <name>Task 1: Desktop responsive card layout for LoginPage</name>
  <files>src/pages/LoginPage.tsx</files>
  <action>
Refactor LoginPage.tsx to add desktop-responsive layout while preserving ALL existing functionality and mobile layout.

**Desktop layout (min-width: 768px):**
- Outer container: full viewport height, display:flex, alignItems:center, justifyContent:center, background with subtle gradient or pattern on var(--bg)
- Inner card: max-width 420px, centered vertically and horizontally, borderRadius 20px, background var(--bg2), border 1px solid var(--bd), box-shadow for depth (e.g., `0 8px 32px rgba(0,0,0,0.4)`)
- Header section (logo + title) integrated INTO the card top area, not a separate full-width bar
- Staff selection grid (2x2) inside the card
- Form inside the card
- Help text below the card or inside card footer

**Mobile layout (below 768px):**
- Keep the current full-width, column-flow layout exactly as-is
- No visual changes on mobile

**Implementation approach:**
- Use CSS media query via a `<style>` tag injected in the component (or inline style + window.matchMedia via a `useMediaQuery` custom hook)
- Preferred: Add a small `useMediaQuery` hook inside the file using `window.matchMedia('(min-width: 768px)')` with useState + useEffect listener. Use the boolean to conditionally apply desktop wrapper styles.
- Desktop wrapper styles: `{ minHeight: '100dvh', display: 'flex', alignItems: 'center', justifyContent: 'center', background: 'var(--bg)', padding: '20px' }`
- Desktop card styles: `{ maxWidth: 420, width: '100%', borderRadius: 20, background: 'var(--bg2)', border: '1px solid var(--bd)', boxShadow: '0 8px 32px rgba(0,0,0,0.4)', overflow: 'hidden' }`
- On mobile (isDesktop=false): return existing JSX structure unchanged

Keep all existing:
- STAFF_LIST constant
- selectStaff function
- handleSubmit function
- inputStyle object
- useState/useRef hooks
- All form inputs, buttons, and event handlers
- CSS variable usage (--bg, --bg2, --bg3, --bd, --bd2, --t1, --t3)

Do NOT:
- Add new npm dependencies
- Change the dark theme colors
- Remove or alter any form functionality
- Use Tailwind classes (project uses inline styles for this component)
  </action>
  <verify>
    <automated>cd /Users/jykevin/Documents/20260328/cha-bio-safety && npx tsc --noEmit src/pages/LoginPage.tsx 2>&1 | head -20</automated>
  </verify>
  <done>
- Desktop (768px+): 로그인 카드가 화면 중앙에 max-width 420px로 표시됨
- Mobile (768px 미만): 기존 full-width 레이아웃 그대로 유지
- 모든 기존 기능(담당자 선택, 비밀번호 입력, 로그인 제출) 정상 동작
- TypeScript 컴파일 에러 없음
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>데스크톱 반응형 로그인 페이지 (중앙 카드 레이아웃)</what-built>
  <how-to-verify>
    1. 프로덕션 배포 후 PC 브라우저(1920x1080)에서 로그인 페이지 접속
    2. 확인 사항:
       - 로그인 카드가 화면 정중앙에 위치하는지
       - 카드에 그림자/테두리가 적용되어 부유감이 있는지
       - 담당자 선택 버튼 4개가 2x2 그리드로 정상 표시되는지
       - 비밀번호 입력 + 로그인 버튼이 정상 동작하는지
    3. 모바일(iPhone)에서도 접속하여 기존 레이아웃 유지 확인
  </how-to-verify>
  <resume-signal>"approved" 또는 수정 사항 설명</resume-signal>
</task>

</tasks>

<verification>
- TypeScript 컴파일 성공
- 데스크톱에서 카드 중앙 배치 확인
- 모바일에서 기존 레이아웃 유지 확인
- 로그인 기능 정상 동작
</verification>

<success_criteria>
- 1920x1080 화면에서 로그인 카드가 중앙에 전문적으로 표시됨
- 768px 미만에서 기존 모바일 레이아웃 변경 없음
- 기존 모든 기능 유지
</success_criteria>

<output>
After completion, create `.planning/quick/260412-8b1-ui-ux/260412-8b1-SUMMARY.md`
</output>
