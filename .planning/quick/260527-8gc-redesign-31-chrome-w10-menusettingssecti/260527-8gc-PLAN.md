---
phase: quick-260527-8gc
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cha-bio-safety/src/components/MenuSettingsSection.tsx
autonomous: true
requirements:
  - W10-MenuSettingsSection-TSX-atomic-conversion
must_haves:
  truths:
    - "MenuSettingsSection.tsx 418 라인이 Tailwind class only 형태로 재작성되어 모든 inline style 이 0 (단 ToggleSmall knob translateX 분기 같이 동적 분기는 Tailwind arbitrary class 로 표현)"
    - "비즈 anchor 43건 (W6 §4.4) + W5 sketch 47 anchor + 17 Korean copy + Phase 21 forward-merge 의존 (DEFAULT_SIDE_MENU import) 모두 1 byte 변경 0"
    - "9·10·11 px body markup 0 — 8 노안 격상 (L173 9→12 / L214 9→12 / L263 9→12 / L294 10→12 / L297 10→12 / L301 10→12 / L307 10→12 / L407 10→12) 모두 적용"
    - "legacy alias var(--t1/--t2/--t3/--bg3/--bg4/--bd2/--danger/--acl) body markup 0 — v0.1.1 semantic 토큰 (text-text-primary/text-text-secondary/text-text-tertiary/bg-surface-sunken/bg-surface-active/border-border-strong/bg-status-danger/bg-accent) 으로 치환"
    - "ToggleSmall accent — raw '#2563eb' → bg-accent-active, knob raw '#fff' → bg-text-on-accent (W5-OQ #B LOCKED)"
    - "cha-bio-safety 빌드 통과 — npx tsc --noEmit 0 errors"
    - "보호 파일 17개 git diff 0 byte (W6 §12.5)"
    - "단일 atomic 1-commit (W6 §1.1 OQ #8 LOCKED) — MenuSettingsSection.tsx 1 파일만 변경"
  artifacts:
    - path: "cha-bio-safety/src/components/MenuSettingsSection.tsx"
      provides: "Tailwind class only MenuSettingsSection — 5 state / 8 mutators / 1 effect / queryKey ['menu-config'] / 17 Korean copy / 4 subcomponents (ArrowButton, ToggleSmall, DividerTitleInput, DeleteConfirmInline) verbatim"
      contains: "export function MenuSettingsSection"
  key_links:
    - from: "src/components/MenuSettingsSection.tsx"
      to: "src/components/SideMenu.tsx"
      via: "import { MENU } from './SideMenu'"
      pattern: "import \\{ MENU \\} from './SideMenu'"
    - from: "src/components/MenuSettingsSection.tsx"
      to: "src/utils/api.ts"
      via: "import { settingsApi, type SideMenuEntry, type MenuConfig, DEFAULT_SIDE_MENU } from '../utils/api'"
      pattern: "DEFAULT_SIDE_MENU"
    - from: "src/components/MenuSettingsSection.tsx"
      to: "src/stores/authStore.ts"
      via: "import { useAuthStore } from '../stores/authStore'"
      pattern: "useAuthStore"
    - from: "src/components/MenuSettingsSection.tsx (ToggleSmall)"
      to: "tailwind.config.js (accent-active token)"
      via: "bg-accent-active class (W5-OQ #B LOCKED — raw #2563eb 폐기)"
      pattern: "bg-accent-active"
---

<objective>
redesign/31-chrome 의 W10 sub-wave — `cha-bio-safety/src/components/MenuSettingsSection.tsx` (418 라인) 단일 atomic TSX 변환.

W7 GlobalHeader 변환 (precedent) 의 패턴을 mirror 하여 모든 inline style 을 Tailwind class only 형태로 재작성한다. 비즈 anchor 43건 (W6 §4.4) 과 W5 sketch 47 anchor + 17 Korean copy + Phase 21 forward-merge 의존성 (DEFAULT_SIDE_MENU import) 은 1 byte 변경 0. 8 노안 격상 (fontSize 9·10 → 12) 과 ToggleSmall accent 토큰화 (raw #2563eb → `bg-accent-active`, raw #fff knob → `bg-text-on-accent`) 적용.

Purpose: chrome 4 컴포넌트 (GlobalHeader / SideMenu / SettingsPanel / MenuSettingsSection) TSX 변환의 두 번째 atomic 커밋. MENU 상수만 import 하는 독립 컴포넌트라 SettingsPanel/SideMenu 변환과 직교 — W7 (완료) 다음에 가장 안전하게 워밍업 후 진입 가능한 컴포넌트.

Output: `cha-bio-safety/src/components/MenuSettingsSection.tsx` (418 → 약 350~400 라인, Tailwind class 가독성으로 약간 줄어듦). 단일 atomic 1-commit (W6 §1.1 OQ #8 LOCKED). 사용자가 main 머지 → cbc7119-preview 자동 배포.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@CLAUDE.local.md
@cha-bio-safety/src/components/MenuSettingsSection.tsx
@cha-bio-safety/src/components/GlobalHeader.tsx
@cha-bio-safety/docs/redesign-context/31-chrome/sketch-wave-5-menu-settings-section.html
@cha-bio-safety/docs/redesign-context/31-chrome/wave-6-tsx-conversion-checklist.md
@cha-bio-safety/tailwind.config.js

<interfaces>
<!-- Key contracts the executor needs — extracted verbatim from source. -->
<!-- 이 블록을 그대로 인용해 변환 — 코드베이스 추가 탐색 불필요. -->

From cha-bio-safety/src/components/MenuSettingsSection.tsx (변환 대상 — 1 byte 변경 0 강제 시그니처):
```typescript
import { useState, useEffect, useMemo, useRef } from 'react'
import { useQuery, useQueryClient, useMutation } from '@tanstack/react-query'
import toast from 'react-hot-toast'
import { ChevronUp, ChevronDown, ChevronRight, Trash2 } from 'lucide-react'
import {
  settingsApi,
  type SideMenuEntry,
  type MenuConfig,
  DEFAULT_SIDE_MENU,
} from '../utils/api'
import { MENU } from './SideMenu'
import { useAuthStore } from '../stores/authStore'

// path → label lookup (from SideMenu.MENU)
const PATH_LABEL: Record<string, string> = (() => {
  const m: Record<string, string> = {}
  MENU.forEach(s => s.items.forEach(i => { m[i.path] = i.label }))
  return m
})()

const DESKTOP_ONLY_PATHS = new Set(
  MENU.flatMap(s => s.items).filter(i => i.desktopOnly).map(i => i.path),
)

const ADMIN_PATHS = new Set(
  MENU.flatMap(s => s.items).filter(i => i.role === 'admin').map(i => i.path)
)

function newDividerId(): string {
  return `d-${Date.now().toString(36)}-${Math.random().toString(36).slice(2, 7)}`
}

function entriesEqual(a: SideMenuEntry[], b: SideMenuEntry[]): boolean { /* deep compare verbatim */ }

export function MenuSettingsSection()                              // 시그니처 verbatim
function ArrowButton({ dir, disabled, onClick }: { dir: 'up' | 'down'; disabled: boolean; onClick: () => void })
function ToggleSmall({ on, onChange, ariaLabel }: { on: boolean; onChange: () => void; ariaLabel: string })
function DividerTitleInput({ initial, onCommit }: { initial: string; onCommit: (value: string) => void })
function DeleteConfirmInline({ onCancel, onConfirm }: { onCancel: () => void; onConfirm: () => void })
```

From cha-bio-safety/src/components/GlobalHeader.tsx (W7 precedent — Tailwind class 패턴 mirror):
```tsx
// W7 변환 결과 — atomic 1-file commit, 비즈 anchor verbatim, svg path verbatim, Tailwind class only.
// MenuSettingsSection 도 동일 패턴: Tailwind class only / 인라인 0 (불가피한 동적 분기는 arbitrary class).
<header className="flex items-center h-12 px-3 bg-surface-raised border-b border-border-default shrink-0">
  <button
    className="w-12 h-12 rounded-sm bg-surface-sunken text-text-secondary border-none cursor-pointer flex items-center justify-center shrink-0"
  >
    <svg width={20} height={20} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
      <path strokeLinecap="round" strokeLinejoin="round" d="M4 6h16M4 12h16M4 18h16" />
    </svg>
  </button>
  <span className={`flex-1 text-title font-semibold text-text-primary ${rightSlot ? 'text-left ml-2' : 'text-center'}`}>{title}</span>
  {rightSlot || <div className="w-12 shrink-0" />}
</header>
```

From cha-bio-safety/tailwind.config.js (spacing override + 토큰 — w-8 함정 의식):
```js
// spacing override: '7': '32px', '8': '48px'  (memory feedback_tailwind_w8_h8_is_48px)
// 따라서 32×32 ArrowButton / Trash button = w-7 h-7 (NOT w-8 h-8 = 48px).
// 48×48 (W7 햄버거) = w-12 h-12 또는 w-8 h-8.

// 토큰 매핑 (Tailwind class 형태):
// surface-raised  : bg-surface-raised
// surface-sunken  : bg-surface-sunken           (divider/item row / DividerTitleInput bg)
// surface-active  : bg-surface-active           (Toggle off / 취소 button bg)
// text-primary    : text-text-primary
// text-secondary  : text-text-secondary
// text-tertiary   : text-text-tertiary
// text-on-accent  : text-text-on-accent / bg-text-on-accent  (knob + button text)
// border-strong   : border-border-strong        (divider borderLeft + '+ 구분선 추가' dashed)
// accent          : bg-accent                   (DividerTitleInput border + 설정 저장 button)
// accent-active   : bg-accent-active            (Toggle ON — W5-OQ #B LOCKED)
// status-danger   : bg-status-danger / text-status-danger  (Trash2 + 초기화/삭제 button)
// radius-sm (8)   : rounded-sm
// radius-pill (99): rounded-full                 (Toggle outer)
```
</interfaces>

<sketch_css_verbatim>
<!-- W5 sketch line 640~957 verbatim CSS — Tailwind class 매핑의 직접 source (memory feedback_planner_prompt_sketch_verbatim) -->

```css
/* SectionHeader button (sketch L640~669) */
.ms-section-header { display: flex; align-items: center; justify-content: space-between; width: 100%; padding: 0; background: none; border: none; cursor: pointer; color: inherit; margin-bottom: 6px; }
.ms-section-header--collapsed { margin-bottom: 0; }
.ms-section-label { font-size: 12px; line-height: 1; font-weight: 700; color: var(--text-tertiary); letter-spacing: .08em; text-transform: uppercase; }
.ms-section-chevron { color: var(--text-tertiary); transition: transform 0.15s; }
.ms-section-chevron--open { transform: rotate(90deg); }

/* Entry list (sketch L672~676) */
.ms-entry-list { display: flex; flex-direction: column; gap: 5px; }

/* Divider row (sketch L679~708) */
.ms-divider { display: flex; align-items: center; gap: 8px; padding: 10px 12px; background: var(--surface-sunken); border-radius: 9px; border-left: 2px solid var(--border-strong); }
.ms-divider-spacer { width: 16px; flex-shrink: 0; }
.ms-divider-body { flex: 1; min-width: 0; }
.ms-divider-title { font-size: 12px; line-height: 1; font-weight: 700; color: var(--text-secondary); letter-spacing: .08em; text-transform: uppercase; cursor: pointer; display: block; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }

/* Item row (sketch L711~744) */
.ms-item { display: flex; align-items: center; gap: 8px; padding: 10px 12px; background: var(--surface-sunken); border-radius: 9px; opacity: 1; }
.ms-item--hidden { opacity: 0.4; }
.ms-item-body { flex: 1; min-width: 0; display: flex; align-items: center; gap: 6px; }
.ms-item-label { font-size: 12px; line-height: 1.4; font-weight: 400; color: var(--text-primary); overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
.ms-item-hidden-tag { font-size: 12px; line-height: 1; color: var(--text-tertiary); flex-shrink: 0; }

/* ArrowButton / Trash button — 32×32 (sketch L747~781) */
.ms-arrow { width: 32px; height: 32px; display: flex; align-items: center; justify-content: center; background: none; border: none; cursor: pointer; color: var(--text-secondary); opacity: 1; pointer-events: auto; padding: 0; flex-shrink: 0; }
.ms-arrow--disabled { opacity: 0.25; pointer-events: none; cursor: default; }
.ms-trash { width: 32px; height: 32px; display: flex; align-items: center; justify-content: center; background: none; border: none; cursor: pointer; color: var(--status-danger); padding: 0; flex-shrink: 0; }

/* ToggleSmall — 38×21 outer / 17×17 knob (sketch L784~818, W5-OQ #B LOCKED) */
.ms-toggle { width: 38px; height: 21px; border-radius: var(--radius-pill); border: none; cursor: pointer; background: var(--surface-active); position: relative; transition: background 0.18s; flex-shrink: 0; padding: 0; }
.ms-toggle--on { background: var(--accent-active); }
.ms-toggle-knob { position: absolute; top: 2px; left: 2px; width: 17px; height: 17px; border-radius: 50%; background: var(--text-on-accent); transition: transform 0.18s; transform: translateX(0); display: block; }
.ms-toggle--on .ms-toggle-knob { transform: translateX(17px); }

/* DividerTitleInput — height 40 (sketch L821~833) */
.ms-divider-input { height: 40px; width: 100%; box-sizing: border-box; background: var(--surface-sunken); border: 1px solid var(--accent); border-radius: var(--radius-sm); padding: 0 12px; font-size: 13px; color: var(--text-primary); outline: none; font-family: inherit; }

/* DeleteConfirmInline (sketch L836~869) */
.ms-delete-confirm { display: flex; align-items: center; gap: 6px; }
.ms-delete-prompt { font-size: 12px; line-height: 1; color: var(--text-secondary); }
.ms-delete-cancel { background: var(--surface-active); color: var(--text-secondary); border: none; border-radius: 6px; padding: 4px 8px; font-size: 12px; line-height: 1; font-weight: 700; cursor: pointer; font-family: inherit; }
.ms-delete-confirm-btn { background: var(--status-danger); color: var(--text-on-accent); border: none; border-radius: 6px; padding: 4px 8px; font-size: 12px; line-height: 1; font-weight: 700; cursor: pointer; font-family: inherit; }

/* + 구분선 추가 button (sketch L872~885) */
.ms-add-divider { margin-top: 10px; width: 100%; height: 36px; border: 1px dashed var(--border-strong); background: transparent; color: var(--text-secondary); border-radius: var(--radius-sm); font-size: 12px; line-height: 1; font-weight: 400; cursor: pointer; font-family: inherit; }

/* Reset confirm row (sketch L888~936) */
.ms-reset-wrap { margin-top: 8px; display: flex; justify-content: flex-end; }
.ms-reset-confirm { display: flex; align-items: center; gap: 6px; }
.ms-reset-prompt { font-size: 12px; line-height: 1; color: var(--text-secondary); }
.ms-reset-cancel { background: var(--surface-active); color: var(--text-secondary); border: none; border-radius: 6px; padding: 4px 8px; font-size: 12px; line-height: 1; font-weight: 700; cursor: pointer; font-family: inherit; }
.ms-reset-confirm-btn { background: var(--status-danger); color: var(--text-on-accent); border: none; border-radius: 6px; padding: 4px 8px; font-size: 12px; line-height: 1; font-weight: 700; cursor: pointer; font-family: inherit; }
.ms-reset-link { background: none; border: none; color: var(--text-tertiary); font-size: 12px; line-height: 1; cursor: pointer; padding: 0; font-family: inherit; }

/* Save button (sketch L939~957) */
.ms-save { margin-top: 8px; width: 100%; height: 40px; background: var(--accent); color: var(--text-on-accent); border: none; border-radius: 9px; font-size: 12px; line-height: 1; font-weight: 700; cursor: pointer; opacity: 1; font-family: inherit; }
.ms-save--disabled { cursor: not-allowed; opacity: 0.4; }
```
</sketch_css_verbatim>

<tailwind_mapping_table>
<!-- W6 §10 cheatsheet + sketch CSS verbatim 의 1:1 Tailwind class 매핑 -->
<!-- 노안 격상 8건 (fontSize 9·10 → 12) 적용된 결과 -->

| 영역 | source inline | Tailwind class |
|---|---|---|
| Root `<div>` (L163) | `padding: '12px 13px 5px'` | `pt-3 px-[13px] pb-[5px]` |
| SectionHeader button (L164~171) | flex+space-between+w-full+p-0+bg-none+border-none+cursor-pointer | `flex items-center justify-between w-full p-0 bg-transparent border-none cursor-pointer` |
| SectionHeader marginBottom 분기 (L169) | `collapsed ? 0 : 6` | `${collapsed ? '' : 'mb-1.5'}` |
| "메뉴 설정" span (L173) — 9→12 격상 | `fontSize: 9, fontWeight: 700, color: 'var(--t3)', letterSpacing: '.08em', textTransform: 'uppercase'` | `text-caption leading-none font-bold text-text-tertiary tracking-[.08em] uppercase` |
| ChevronRight rotate (L179) | `transform: collapsed ? 'rotate(0deg)' : 'rotate(90deg)', transition: 'transform 0.15s'` | `transition-transform duration-150 ${collapsed ? '' : 'rotate-90'}` (color="currentColor" + parent text-text-tertiary 로 색 전달) |
| Entry list (L186) | `display: 'flex', flexDirection: 'column', gap: 5` | `flex flex-col gap-[5px]` |
| Divider row (L198~202) | `display: 'flex', alignItems: 'center', gap: 8, padding: '10px 12px', background: 'var(--bg3)', borderRadius: 9, borderLeft: '2px solid var(--bd2)'` | `flex items-center gap-2 py-2.5 px-3 bg-surface-sunken rounded-[9px] border-l-2 border-border-strong` |
| Spacer (L203) | `width: 16, flexShrink: 0` | `w-4 shrink-0` |
| Divider body (L204) | `flex: 1, minWidth: 0` | `flex-1 min-w-0` |
| Divider title span (L213~218) — 9→12 격상 | `fontSize: 9, fontWeight: 700, color: 'var(--t2)', letterSpacing: '.08em', textTransform: 'uppercase', cursor: 'pointer', display: 'block', overflow: 'hidden', textOverflow: 'ellipsis', whiteSpace: 'nowrap'` | `text-caption leading-none font-bold text-text-secondary tracking-[.08em] uppercase cursor-pointer block overflow-hidden text-ellipsis whitespace-nowrap` |
| Item row (L252~256) | `display: 'flex', alignItems: 'center', gap: 8, padding: '10px 12px', background: 'var(--bg3)', borderRadius: 9, opacity: visible ? 1 : 0.4` | `flex items-center gap-2 py-2.5 px-3 bg-surface-sunken rounded-[9px] ${entry.visible ? '' : 'opacity-40'}` |
| Item body (L258) | `flex: 1, minWidth: 0, display: 'flex', alignItems: 'center', gap: 6` | `flex-1 min-w-0 flex items-center gap-1.5` |
| Item label (L259) | `fontSize: 12, fontWeight: 400, color: 'var(--t1)', overflow: 'hidden', textOverflow: 'ellipsis', whiteSpace: 'nowrap'` | `text-caption font-normal text-text-primary overflow-hidden text-ellipsis whitespace-nowrap` |
| "숨김" span (L263) — 9→12 격상 | `fontSize: 9, color: 'var(--t3)', flexShrink: 0` | `text-caption leading-none text-text-tertiary shrink-0` |
| "+ 구분선 추가" button (L281~285) | `marginTop: 10, width: '100%', height: 36, border: '1px dashed var(--bd2)', background: 'transparent', color: 'var(--t2)', borderRadius: 8, fontSize: 12, fontWeight: 400, cursor: 'pointer'` | `mt-2.5 w-full h-9 border border-dashed border-border-strong bg-transparent text-text-secondary rounded-sm text-caption leading-none font-normal cursor-pointer` |
| Reset wrap (L291) | `marginTop: 8, display: 'flex', justifyContent: 'flex-end'` | `mt-2 flex justify-end` |
| Reset confirm row (L293) | `display: 'flex', alignItems: 'center', gap: 6` | `flex items-center gap-1.5` |
| "기본 배치로 되돌릴까요?" (L294) — 10→12 격상 | `fontSize: 10, color: 'var(--t2)'` | `text-caption leading-none text-text-secondary` |
| 취소 button (L297) — 10→12 격상 | `background: 'var(--bg4)', color: 'var(--t2)', border: 'none', borderRadius: 6, padding: '4px 8px', fontSize: 10, fontWeight: 700` | `bg-surface-active text-text-secondary border-none rounded-md px-2 py-1 text-caption leading-none font-bold cursor-pointer` (note: rounded-md = 12px, sketch 명시 6px → arbitrary `rounded-[6px]`) → 최종 사용: `rounded-[6px]` |
| 초기화 button (L301) — 10→12 격상 | `background: 'var(--danger)', color: '#fff', border: 'none', borderRadius: 6, padding: '4px 8px', fontSize: 10, fontWeight: 700` | `bg-status-danger text-text-on-accent border-none rounded-[6px] px-2 py-1 text-caption leading-none font-bold cursor-pointer` |
| "기본값으로 초기화" link (L307) — 10→12 격상 | `background: 'none', border: 'none', color: 'var(--t3)', fontSize: 10, cursor: 'pointer', padding: 0` | `bg-transparent border-none text-text-tertiary text-caption leading-none cursor-pointer p-0` |
| Save button (L319~323) | `marginTop: 8, width: '100%', height: 40, background: 'var(--acl)', color: '#fff', border: 'none', borderRadius: 9, fontSize: 12, fontWeight: 700, opacity: dirty && !pending ? 1 : 0.4, cursor: dirty && !pending ? 'pointer' : 'not-allowed'` | `mt-2 w-full h-10 bg-accent text-text-on-accent border-none rounded-[9px] text-caption leading-none font-bold ${dirty && !saveMutation.isPending ? 'cursor-pointer opacity-100' : 'cursor-not-allowed opacity-40'}` |
| ArrowButton 32×32 (L343~349) | `width: 32, height: 32, display: 'flex', alignItems: 'center', justifyContent: 'center', background: 'none', border: 'none', cursor: disabled ? 'default' : 'pointer', color: 'var(--t2)', opacity: disabled ? 0.25 : 1, pointerEvents: disabled ? 'none' : 'auto'` | `w-7 h-7 flex items-center justify-center bg-transparent border-none text-text-secondary shrink-0 p-0 ${disabled ? 'opacity-25 pointer-events-none cursor-default' : 'opacity-100 cursor-pointer'}` (w-7 = 32 per tailwind.config) |
| Trash button 32×32 (L237) | `width: 32, height: 32, display: 'flex', alignItems: 'center', justifyContent: 'center', background: 'none', border: 'none', cursor: 'pointer', color: 'var(--danger)'` | `w-7 h-7 flex items-center justify-center bg-transparent border-none text-status-danger shrink-0 p-0 cursor-pointer` |
| ToggleSmall outer (L362~366) — W5-OQ #B | `width: 38, height: 21, borderRadius: 11, border: 'none', cursor: 'pointer', background: on ? '#2563eb' : 'var(--bg4)', position: 'relative', transition: 'background 0.18s', flexShrink: 0` | `relative w-[38px] h-[21px] rounded-full border-none cursor-pointer shrink-0 p-0 transition-colors duration-200 ${on ? 'bg-accent-active' : 'bg-surface-active'}` |
| ToggleSmall knob (L368~373) — W5-OQ #B | `position: 'absolute', top: 2, left: 2, width: 17, height: 17, borderRadius: '50%', background: '#fff', transition: 'transform 0.18s', transform: on ? 'translateX(17px)' : 'translateX(0)', display: 'block'` | `absolute top-0.5 left-0.5 w-[17px] h-[17px] rounded-full bg-text-on-accent block transition-transform duration-200 ${on ? 'translate-x-[17px]' : 'translate-x-0'}` |
| DividerTitleInput (L395~399) | `height: 40, width: '100%', boxSizing: 'border-box', background: 'var(--bg3)', border: '1px solid var(--acl)', borderRadius: 8, padding: '0 12px', fontSize: 13, color: 'var(--t1)', outline: 'none'` | `h-10 w-full box-border bg-surface-sunken border border-accent rounded-sm px-3 text-label text-text-primary outline-none` |
| DeleteConfirmInline wrap (L406) | `display: 'flex', alignItems: 'center', gap: 6` | `flex items-center gap-1.5` |
| "삭제할까요?" (L407) — 10→12 격상 | `fontSize: 10, color: 'var(--t2)'` | `text-caption leading-none text-text-secondary` |
| Delete 취소 button (L410) | `background: 'var(--bg4)', color: 'var(--t2)', border: 'none', borderRadius: 6, padding: '4px 8px', fontSize: 12, fontWeight: 700, cursor: 'pointer'` | `bg-surface-active text-text-secondary border-none rounded-[6px] px-2 py-1 text-caption leading-none font-bold cursor-pointer` |
| Delete 삭제 button (L414) | `background: 'var(--danger)', color: '#fff', border: 'none', borderRadius: 6, padding: '4px 8px', fontSize: 12, fontWeight: 700, cursor: 'pointer'` | `bg-status-danger text-text-on-accent border-none rounded-[6px] px-2 py-1 text-caption leading-none font-bold cursor-pointer` |
</tailwind_mapping_table>

<biz_anchor_full_list>
<!-- W6 §4.4 43건 비즈 anchor + W5 sketch 47 + 17 Korean copy verbatim — 1 byte 변경 0 강제 -->

**Imports + 모듈 레벨 (5건, line 1~48):**
1. `import { useState, useEffect, useMemo, useRef } from 'react'` (L1)
2. `import { useQuery, useQueryClient, useMutation } from '@tanstack/react-query'` (L2)
3. `import toast from 'react-hot-toast'` (L3)
4. `import { ChevronUp, ChevronDown, ChevronRight, Trash2 } from 'lucide-react'` (L4)
5. `import { settingsApi, type SideMenuEntry, type MenuConfig, DEFAULT_SIDE_MENU } from '../utils/api'` + `import { MENU } from './SideMenu'` + `import { useAuthStore } from '../stores/authStore'`

**모듈 상수 + 유틸 (5건, line 14~48):**
6. `PATH_LABEL: Record<string, string>` IIFE — MENU.forEach lookup (L15~19) verbatim
7. `DESKTOP_ONLY_PATHS: Set` (L22~24) verbatim
8. `ADMIN_PATHS: Set` (L27~29) verbatim
9. `newDividerId()` — `` `d-${Date.now().toString(36)}-${Math.random().toString(36).slice(2, 7)}` `` verbatim
10. `entriesEqual(a, b)` — array deep compare (L36~48) verbatim — length / type / path / visible / id / title

**Main hook + state (10건, line 50~95):**
11. `useQueryClient` + `useAuthStore(s => s.staff)` + `isAdmin = staff?.role === 'admin'`
12. `useQuery<MenuConfig>({ queryKey: ['menu-config'], queryFn: () => settingsApi.getMenu(), staleTime: 300_000 })`
13. `[draft, setDraft] = useState<SideMenuEntry[]>([])`
14. `[editingDividerIdx, setEditingDividerIdx] = useState<number | null>(null)`
15. `[confirmDeleteIdx, setConfirmDeleteIdx] = useState<number | null>(null)`
16. `[confirmReset, setConfirmReset] = useState(false)`
17. `confirmTimerRef = useRef<number | null>(null)`
18. `[collapsed, setCollapsed] = useState<boolean>(() => { try { return localStorage.getItem('menuSettings.collapsed') !== 'false' } catch { return true } })` + `useEffect (() => { try { localStorage.setItem('menuSettings.collapsed', String(collapsed)) } catch {} }, [collapsed])`
19. draft init `useEffect` (L74~80) — serverConfig.sideMenu spread `{ ...e }` / null fallback → `DEFAULT_SIDE_MENU.map(e => ({ ...e }))`
20. `dirty = useMemo(...)` (L82~85) + `saveMutation = useMutation({ mutationFn: () => settingsApi.saveMenu({ sideMenu: draft }), onSuccess: () => { toast.success('메뉴 설정이 저장되었습니다'); qc.invalidateQueries({ queryKey: ['menu-config'] }) }, onError: () => toast.error('저장에 실패했습니다. 다시 시도해주세요.') })`

**Mutators (7건, line 96~149):**
21. `moveUp(idx)` — `idx <= 0` guard + swap (L97~104)
22. `moveDown(idx)` — `idx >= prev.length - 1` guard + swap (L105~112)
23. `toggleVisible(idx)` — `e.type !== 'item'` skip + `!e.visible` (L113~118)
24. `renameDivider(idx, title)` — `.trim().slice(0, 20)` + empty silent revert (L119~131)
25. `deleteDivider(idx)` — filter + null + `window.clearTimeout(confirmTimerRef.current)` (L132~136)
26. `addDivider()` — `newDividerId()` + `'새 구분선'` + `setTimeout(() => setEditingDividerIdx(next.length - 1), 0)` (L137~145)
27. `resetToDefaults()` — `DEFAULT_SIDE_MENU.map(e => ({ ...e }))` + `setConfirmReset(false)` (L146~149)

**5s auto-dismiss useEffect (1건, line 152~159):**
28. `useEffect(() => { if (confirmDeleteIdx === null) return; if (confirmTimerRef.current) window.clearTimeout(confirmTimerRef.current); confirmTimerRef.current = window.setTimeout(() => setConfirmDeleteIdx(null), 5000); return () => { if (confirmTimerRef.current) window.clearTimeout(confirmTimerRef.current) } }, [confirmDeleteIdx])`

**Render 분기 (8건, line 161~331):**
29. Root padding `12px 13px 5px` (L163) — Tailwind: `pt-3 px-[13px] pb-[5px]`
30. SectionHeader collapsible button — `aria-expanded={!collapsed}` + "메뉴 설정" span (9→12 격상) + ChevronRight `size={14}` rotate 0/90deg `transition 0.15s` (L164~181)
31. Entry list `flex flex-col gap-[5px]` (L186)
32. `draft.map((entry, idx) => ...)` admin 필터: `!isAdmin && entry.type === 'item' && ADMIN_PATHS.has(entry.path)` → null (L188~189)
33. divider 분기: `entry.type === 'divider'` (L195) — isEditing → DividerTitleInput, isConfirmingDelete → DeleteConfirmInline, default → ArrowButton up/down + Trash2 button
34. divider title span fontSize 9→12 격상 (L214)
35. item 분기: `if (!(entry.path in PATH_LABEL)) return null; if (DESKTOP_ONLY_PATHS.has(entry.path)) return null;` (L248, 249) + opacity 0.4 / visible + label `PATH_LABEL[entry.path]` + ArrowButton up/down + ToggleSmall on `entry.visible` onChange `toggleVisible(idx)` ariaLabel `` `${label} 표시` ``
36. "숨김" span — `!entry.visible &&` (L262) fontSize 9→12 격상

**Buttons (3건):**
37. "+ 구분선 추가" button — dashed border + width 100% h 36 (L279~288)
38. 초기화 button — `confirmReset` 분기 (L291~312) — "기본 배치로 되돌릴까요?" + 취소/초기화 inline (fontSize 10→12 격상 4건: L294/L297/L301/L307) + "기본값으로 초기화" link
39. "설정 저장" button — `!dirty || saveMutation.isPending` disabled + opacity 0.4 / 1 + `'저장 중…'`(U+2026) / `'설정 저장'` (L315~327)

**Subcomponents (4종, line 334~418):**
40. `ArrowButton({ dir, disabled, onClick })` (L336~354) — `dir: 'up'|'down'`, `disabled: boolean`, `onClick: () => void`, `Icon = dir === 'up' ? ChevronUp : ChevronDown`, `aria-label: dir === 'up' ? '위로 이동' : '아래로 이동'`, 32×32, `Icon size={14}`, disabled opacity 0.25 pointer-events none
41. `ToggleSmall({ on, onChange, ariaLabel })` (L356~376) — 38×21 outer (radius 11/pill), knob 17×17 (radius 50%), `bg-accent-active` (W5-OQ #B) / `bg-surface-active`, knob `bg-text-on-accent` translate-x 0 / 17px (W5-OQ #B), `aria-pressed={on}`
42. `DividerTitleInput({ initial, onCommit })` (L378~402) — `[value, setValue] = useState(initial)`, `useRef<HTMLInputElement>`, `useEffect(() => { ref.current?.focus(); ref.current?.select() }, [])`, `maxLength={20}`, `onBlur={() => onCommit(value)}`, `onKeyDown`: Enter → `e.currentTarget.blur()`, Escape → `setValue(initial); setTimeout(() => onCommit(initial), 0)`, `aria-label="구분선 제목"`, height 40
43. `DeleteConfirmInline({ onCancel, onConfirm })` (L404~418) — `flex items-center gap-1.5`, "삭제할까요?" (10→12 격상) + 취소 (var(--bg4)+var(--t2)+fontSize 12) + 삭제 (var(--danger)+#fff+fontSize 12)

**Phase 21 forward-merge (1건):**
44. `migrateLegacyMenuConfig` (utils/api.ts L449) 의존 — `DEFAULT_SIDE_MENU` import (L9) 를 hand-rolled 상수로 치환 **금지**. settingsApi.getMenu API 응답 형식 변경 0.

**17 Korean copy (verbatim, W5 sketch L290~310):**
1. `'메뉴 설정'` (SectionHeader 라벨, L174)
2. `'구분선 삭제'` (aria-label, L236)
3. `'위로 이동'` (ArrowButton aria-label up, L342)
4. `'아래로 이동'` (ArrowButton aria-label down, L342)
5. `'+ 구분선 추가'` (button text, L287 — '+' 는 literal ASCII)
6. `'기본 배치로 되돌릴까요?'` (reset confirm prompt, L294)
7. `'취소'` (reset / delete confirm, L298 / L411)
8. `'초기화'` (reset confirm 초기화 button, L302)
9. `'기본값으로 초기화'` (reset default link, L309)
10. `'저장 중…'` (save pending, L326 — U+2026 single ellipsis)
11. `'설정 저장'` (save default, L326)
12. `'삭제할까요?'` (delete confirm prompt, L407)
13. `'삭제'` (delete confirm 삭제 button, L415)
14. `'숨김'` (item hidden meta, L263)
15. `'새 구분선'` (addDivider default title, L138)
16. `'구분선 제목'` (DividerTitleInput aria-label, L394)
17. `` `${label} 표시` `` (ToggleSmall aria-label template literal, L271)
+ toast: `'메뉴 설정이 저장되었습니다'` (L90) / `'저장에 실패했습니다. 다시 시도해주세요.'` (L93)
</biz_anchor_full_list>

<protected_files>
<!-- W6 §12.5 — 다음 17개 파일은 git diff 0 byte 강제 -->

- `cha-bio-safety/src/App.tsx`
- `cha-bio-safety/src/styles/tokens.css`
- `cha-bio-safety/src/styles/typography.css`
- `cha-bio-safety/tailwind.config.js`
- `cha-bio-safety/docs/redesign-context/00-design-context/`
- `cha-bio-safety/docs/redesign-context/31-chrome/wave-1-index.md`
- `cha-bio-safety/docs/redesign-context/31-chrome/GlobalHeader.tsx` (snapshot)
- `cha-bio-safety/docs/redesign-context/31-chrome/SideMenu.tsx` (snapshot)
- `cha-bio-safety/docs/redesign-context/31-chrome/SettingsPanel.tsx` (snapshot)
- `cha-bio-safety/docs/redesign-context/31-chrome/MenuSettingsSection.tsx` (snapshot)
- `cha-bio-safety/docs/redesign-context/31-chrome/sketch-wave-2-global-header.html`
- `cha-bio-safety/docs/redesign-context/31-chrome/sketch-wave-3-side-menu.html`
- `cha-bio-safety/docs/redesign-context/31-chrome/sketch-wave-4-settings-panel.html`
- `cha-bio-safety/docs/redesign-context/31-chrome/sketch-wave-5-menu-settings-section.html`
- `cha-bio-safety/docs/redesign-context/31-chrome/design-system.md`
- `cha-bio-safety/docs/redesign-context/31-chrome/tokens.css`
- `cha-bio-safety/docs/redesign-context/31-chrome/typography.css`

추가 보호 (live files):
- `cha-bio-safety/src/components/GlobalHeader.tsx` (W7 완결 — 변경 0)
- `cha-bio-safety/src/components/SideMenu.tsx` (W8 책임 — 본 W10 변경 0)
- `cha-bio-safety/src/components/SettingsPanel.tsx` (W9 책임 — 본 W10 변경 0)
- `cha-bio-safety/src/utils/api.ts` (DEFAULT_SIDE_MENU + SideMenuEntry + MenuConfig + settingsApi 시그니처)
- `cha-bio-safety/src/stores/authStore.ts`
- `cha-bio-safety/src/pages/` 전체
</protected_files>

<memory_rules>
<!-- 본 W10 변환에서 직접 영향 13개 unique slug -->

- **memory `feedback_planner_prompt_sketch_verbatim`** — sketch CSS 정의 grep verbatim 인용 (위 sketch_css_verbatim 블록 + tailwind_mapping_table)
- **memory `feedback_tailwind_token_class_pattern`** — `status-` prefix 없음. `text-status-fire-bar` O / `text-fire-bar` X. 단 본 W10 은 `bg-status-danger` / `text-status-danger` 같이 풀 이름 사용 OK (tailwind.config danger alias 도 OK — `bg-danger` 도 동일하지만 명시성 위해 status-danger 권장)
- **memory `feedback_tailwind_w8_h8_is_48px`** — `w-8 = h-8 = 48px` (기본 32 아님). 32×32 ArrowButton/Trash 는 `w-7 h-7` (= 32px) 사용. 48px 는 `w-8 h-8` 또는 `w-12 h-12`
- **memory `feedback_text_caption_leading_none`** — `text-caption` lh 1.5 (18px) 가 작은 컨테이너 안에서 시각적 패딩 발생 → uppercase 라벨 / 작은 inline 텍스트 모두 `leading-none` 명시
- **memory `feedback_tsx_wave_emoji_dot_gap`** — 이모지 negative gate 강제. MenuSettingsSection 은 이모지 0건 (단 '+' ASCII 0x2B literal 은 예외)
- **memory `feedback_tsx_wave_stat_card_drift`** — sketch 의 새 패턴 + source 의 비즈 anchor 양쪽 verbatim 인용 필수 (위 sketch_css_verbatim + biz_anchor_full_list 블록 양쪽 보존)
- **memory `feedback_cbc7119_design_never_wrangler`** — 디자인 작업은 wrangler 명령 자체 금지. main push 만 cbc7119-preview 자동
- **memory `project_cbc7119_design_repo`** — cbc7119 = 디자인 격리 리포
- **memory `feedback_check_branch_before_edit`** — 현재 branch `redesign/31-chrome` 확인됨 (사용자 컨펌 후 main 머지)
- **memory `feedback_design_changes_ask_first`** — 본 W10 은 W5 sketch + W6 checklist 가 이미 컨펌된 디자인 결정. 추가 변경 X, 그대로 적용
- **memory `feedback_sketch_realistic_data`** — 표시 분기/라벨 룰 (없음/N개) 코드 그대로. sketch 의 8 entry seed 는 실제 DEFAULT_SIDE_MENU
- **memory `feedback_redesign_sketch_rule_enforcement`** — §6.2 negative rule (위험 임계치 아닌 카드는 status 색 금지). 본 W10 의 reset/delete 의 status-danger 는 "삭제/초기화 = 위험 액션" 의미 일치 OK
- **memory `feedback_design_sketch_first`** — sketch 먼저 컨펌 받음 (W5 완결). 본 W10 = TSX 변환만
</memory_rules>

<negative_gate>
<!-- W6 §11 + §12.4 verify gate — 변환 후 자동 실행 -->

1. `style={` 인라인 사용 0 (불가피한 동적 분기는 Tailwind arbitrary class 또는 변수 인라인 OK — 단 1건 ASN 사유 코멘트 명시. 본 변환은 0 목표)
2. legacy alias `var(--t1/--t2/--t3/--bg3/--bg4/--bd2/--danger/--acl)` body markup 0
3. raw hex `#2563eb / #fff` body markup 0 (W5-OQ #B LOCKED — `bg-accent-active` / `bg-text-on-accent` 교체)
4. `fontSize: ?(9|10|11)[,$]` 또는 `text-\[(9|10|11)px\]` body markup 0 — 8 격상 모두 적용
5. `linear-gradient` 0 (MenuSettingsSection 그라데이션 0)
6. 이모지 (`[\u{1F300}-\u{1FAFF}\u{2600}-\u{27BF}]`) 0 ('+' ASCII 예외)
7. `✕` U+2715 텍스트 글리프 0 (Trash2 는 이미 Lucide)
8. `status-` prefix className 단독 사용 0 (`text-status-danger` / `bg-status-danger` 같이 풀 이름은 OK)
9. `w-8 / h-8` 함정 의식 — 32×32 ArrowButton/Trash 는 `w-7 h-7`, 48×48 은 `w-8 h-8` 또는 `w-12 h-12`
10. `MENU` 상수 import 경로 / 시그니처 변경 0 (SideMenu.tsx 정의 — W8 책임)
11. `DEFAULT_SIDE_MENU` import 변경 0 (utils/api.ts 정의 — Phase 21 의존)
12. `PATH_LABEL / DESKTOP_ONLY_PATHS / ADMIN_PATHS` 비즈 1 byte 변경 0
13. `menuSettings.collapsed` localStorage 키 1 byte 변경 0
14. App.tsx / tokens.css / typography.css / tailwind.config.js / 31-chrome 13 파일 git diff 0 byte
15. src/components/{GlobalHeader,SideMenu,SettingsPanel}.tsx 본 W10 변경 0
16. src/pages/ 전체 변경 0
17. src/utils/api.ts 변경 0
18. src/stores/authStore.ts 변경 0
19. wrangler / npm run deploy 명령 실행 0 (CLAUDE.local.md + memory `feedback_cbc7119_design_never_wrangler`)
20. 4 subcomponent 시그니처 1 byte 변경 0 (ArrowButton / ToggleSmall / DividerTitleInput / DeleteConfirmInline props 인터페이스)
21. Main 시그니처 1 byte 변경 0 — `export function MenuSettingsSection()` (no props)
22. 5 state / 8 mutators / 1 useEffect / queryKey ['menu-config'] / toast 문구 / `'저장 중…'` (U+2026) / `'설정 저장'` 1 byte 변경 0
23. 4 subcomponent (ArrowButton / ToggleSmall / DividerTitleInput / DeleteConfirmInline) 내부 비즈 (deepCompare / focus/select on mount / Enter blur / Escape revert / maxLength 20 / 5000ms timeout) 1 byte 변경 0
</negative_gate>
</context>

<tasks>

<task type="auto">
  <name>Task 1: MenuSettingsSection.tsx 전체 Tailwind class 변환 — atomic 1-commit</name>
  <files>cha-bio-safety/src/components/MenuSettingsSection.tsx</files>
  <action>
**STEP 1 — 사전 검증 (작업 시작 전):**

```bash
cd /Users/jykevin/Documents/cbc7119-design
# 0a. branch 확인 (memory feedback_check_branch_before_edit)
git branch --show-current   # 결과: redesign/31-chrome
# 0b. dirty check
git status --porcelain      # 결과: empty (또는 untracked .planning/quick/ 만)
# 0c. 변환 대상 파일 존재 확인
test -f cha-bio-safety/src/components/MenuSettingsSection.tsx && wc -l cha-bio-safety/src/components/MenuSettingsSection.tsx   # 418
# 0d. tailwind.config 의 w-7 = 32px / w-8 = 48px 재확인 (memory feedback_tailwind_w8_h8_is_48px)
grep -nE "'7': '32px'|'8': '48px'" cha-bio-safety/tailwind.config.js   # 2 매치
```

**STEP 2 — 변환 작업 (단일 Write tool 호출, 전체 파일 재작성):**

위 `<biz_anchor_full_list>` 의 43 anchor + 4 subcomponent 시그니처 + 17 Korean copy + Phase 21 의존 (DEFAULT_SIDE_MENU import) 을 **verbatim 1 byte 변경 0** 강제로 유지하며, `<tailwind_mapping_table>` 의 1:1 매핑에 따라 모든 inline `style={{...}}` 을 `className` 으로 치환한다.

치환 규칙 (verbatim 인용 — sketch_css_verbatim 블록의 CSS 값 기준):

A. **모든 inline `style={{` 제거** — 단 ToggleSmall knob 의 분기 시각화도 Tailwind arbitrary class 로 처리 (`translate-x-0` / `translate-x-[17px]`).

B. **노안 격상 8건 (정확히 8개 fontSize 치환):**
   - L173 `fontSize: 9` → `text-caption leading-none` (12px lh:1, "메뉴 설정" SectionHeader)
   - L214 `fontSize: 9` → `text-caption leading-none` (12px lh:1, divider title uppercase)
   - L263 `fontSize: 9` → `text-caption leading-none` (12px lh:1, "숨김" 라벨)
   - L294 `fontSize: 10` → `text-caption leading-none` (12px lh:1, "기본 배치로 되돌릴까요?")
   - L297 `fontSize: 10` → `text-caption leading-none font-bold` (12px lh:1, 취소 button)
   - L301 `fontSize: 10` → `text-caption leading-none font-bold` (12px lh:1, 초기화 button)
   - L307 `fontSize: 10` → `text-caption leading-none` (12px lh:1, "기본값으로 초기화" link)
   - L407 `fontSize: 10` → `text-caption leading-none` (12px lh:1, "삭제할까요?")
   다른 모든 속성 (fontWeight / letterSpacing / textTransform / color) 은 동일 의미 Tailwind class 로 치환.

C. **legacy alias → v0.1.1 semantic 토큰 치환:**
   - `var(--t1)` → `text-text-primary`
   - `var(--t2)` → `text-text-secondary`
   - `var(--t3)` → `text-text-tertiary`
   - `var(--bg3)` → `bg-surface-sunken`
   - `var(--bg4)` → `bg-surface-active`
   - `var(--bd2)` → `border-border-strong`
   - `var(--danger)` → `text-status-danger` (텍스트) / `bg-status-danger` (배경)
   - `var(--acl)` → `bg-accent` (배경) / `border-accent` (border)
   - raw `#2563eb` → `bg-accent-active` (W5-OQ #B LOCKED, Toggle on)
   - raw `#fff` (knob + button 텍스트) → `bg-text-on-accent` / `text-text-on-accent`

D. **수치 매핑 (sketch CSS verbatim 기준):**
   - 32×32 (ArrowButton / Trash) → `w-7 h-7` (tailwind '7':'32px')
   - 48×48 (없음 — 본 W10 에는 없음)
   - 38×21 (ToggleSmall outer) → `w-[38px] h-[21px]` (arbitrary — tailwind 표준에 38 없음)
   - 17×17 (ToggleSmall knob) → `w-[17px] h-[17px]`
   - h 36 ("+ 구분선 추가") → `h-9` (9*4=36)
   - h 40 (DividerTitleInput + 설정 저장) → `h-10` (10*4=40)
   - radius 6 (confirm button) → `rounded-[6px]` (tailwind sm=8 / md=12 사이)
   - radius 8 (input / + 구분선 추가) → `rounded-sm` (tailwind sm=8)
   - radius 9 (Row / Save button / divider/item) → `rounded-[9px]` (tailwind 표준 8↔12 사이, sketch verbatim)
   - radius 11 (Toggle outer) → `rounded-full` (= pill 99px) — sketch §10.1 의 `--radius-pill` 권장 매핑
   - radius 50% (knob) → `rounded-full`
   - gap 5 (entry list) → `gap-[5px]`
   - gap 6 (item-body / delete-confirm) → `gap-1.5` (1.5*4=6)
   - gap 8 (divider / item row) → `gap-2` (2*4=8)
   - padding `10px 12px` (divider/item) → `py-2.5 px-3` (2.5*4=10, 3*4=12)
   - padding `4px 8px` (confirm button) → `px-2 py-1`
   - padding `0 12px` (input) → `px-3`
   - padding `12px 13px 5px` (root) → `pt-3 px-[13px] pb-[5px]`
   - margin-bottom `0` / `6` 분기 (SectionHeader) → `${collapsed ? '' : 'mb-1.5'}`
   - margin-top `8` (Reset wrap / Save) → `mt-2`
   - margin-top `10` (+ 구분선 추가) → `mt-2.5`
   - top 2 / left 2 (knob) → `top-0.5 left-0.5` (0.5*4=2)

E. **Lucide icon 4종 — `size={14}` 유지 + `color="currentColor"` (Lucide 기본). 부모 className 의 text-color 가 자동 currentColor 로 전달. ChevronRight 의 `style={transform: ...}` → `className={...rotate-90 transition-transform duration-150}` 형태 분기. ChevronRight 의 `color` prop 제거 → 부모 text-text-tertiary 가 자동 적용.

F. **Save button 분기** — `cursor` / `opacity` 가 `dirty && !saveMutation.isPending` 분기. Tailwind ternary class 패턴: `` `... ${dirty && !saveMutation.isPending ? 'cursor-pointer opacity-100' : 'cursor-not-allowed opacity-40'}` ``. `disabled={!dirty || saveMutation.isPending}` 속성도 유지.

G. **ArrowButton disabled 분기** — opacity 0.25 / pointer-events none / cursor default 모두 disabled 분기. Tailwind ternary class: `` `... ${disabled ? 'opacity-25 pointer-events-none cursor-default' : 'opacity-100 cursor-pointer'}` ``. `disabled` HTML attribute 도 유지.

H. **ToggleSmall on/off 분기** — outer bg / knob transform 모두 분기. Tailwind ternary:
   - outer: `` `relative w-[38px] h-[21px] rounded-full border-none cursor-pointer shrink-0 p-0 transition-colors duration-200 ${on ? 'bg-accent-active' : 'bg-surface-active'}` ``
   - knob: `` `absolute top-0.5 left-0.5 w-[17px] h-[17px] rounded-full bg-text-on-accent block transition-transform duration-200 ${on ? 'translate-x-[17px]' : 'translate-x-0'}` ``

I. **DividerTitleInput** — `box-border` / `outline-none` / `font-family inherit` 모두 Tailwind 표준 처리. fontSize 13 → `text-label` (tailwind.config typography.label = 13px). `border 1px solid var(--acl)` → `border border-accent`.

J. **DeleteConfirmInline / Reset confirm — 동일 패턴 confirm row** — 둘 다 `flex items-center gap-1.5`. 취소 (`bg-surface-active text-text-secondary`) / 위험 액션 (`bg-status-danger text-text-on-accent`) + `rounded-[6px] px-2 py-1 text-caption leading-none font-bold cursor-pointer border-none`.

K. **비즈 anchor 절대 보존 (1 byte 변경 0):**
   - 5 imports (line 1~12) — Lucide 4종 / settingsApi / SideMenuEntry / MenuConfig / DEFAULT_SIDE_MENU / MENU / useAuthStore
   - PATH_LABEL IIFE / DESKTOP_ONLY_PATHS / ADMIN_PATHS Set 정의 (line 14~29)
   - newDividerId() / entriesEqual() (line 32~48)
   - useQuery queryKey `['menu-config']` + staleTime 300_000 (line 54~58)
   - 5 state + confirmTimerRef (line 61~65)
   - collapsed localStorage 'menuSettings.collapsed' 키 + default true (line 66~71)
   - draft init useEffect spread `{ ...e }` (line 74~80)
   - dirty useMemo + entriesEqual 비교 (line 82~85)
   - saveMutation mutationFn / onSuccess / onError verbatim (line 87~94)
   - 7 mutators (moveUp / moveDown / toggleVisible / renameDivider / deleteDivider / addDivider / resetToDefaults) verbatim (line 96~149)
   - 5s auto-dismiss useEffect (line 152~159, `5000`ms)
   - admin 필터 / PATH_LABEL 필터 / DESKTOP_ONLY 필터 verbatim (line 189 / 248 / 249)
   - 4 subcomponent 시그니처 props (ArrowButton / ToggleSmall / DividerTitleInput / DeleteConfirmInline) — interface 그대로
   - 17 Korean copy + 2 toast 메시지 (위 biz_anchor_full_list 참조)

L. **WRITE 시 한 번에 전체 파일 작성** — `Read` 로 다시 읽지 말고, 이 PLAN 의 source 인용 + tailwind_mapping_table + sketch_css_verbatim 만으로 결정론적으로 변환. 변환 후 파일은 약 350~400 라인 예상.

**STEP 3 — verify gate (W6 §12.4 자동 실행 — 모든 검사 PASS 강제):**

```bash
cd /Users/jykevin/Documents/cbc7119-design/cha-bio-safety

# (A) Imports / 시그니처 (≥6 매치)
grep -c "import { MENU } from './SideMenu'" src/components/MenuSettingsSection.tsx   # = 1
grep -c "DEFAULT_SIDE_MENU" src/components/MenuSettingsSection.tsx                     # ≥ 2
grep -c "PATH_LABEL\|DESKTOP_ONLY_PATHS\|ADMIN_PATHS" src/components/MenuSettingsSection.tsx  # ≥ 6

# (B) 비즈 anchor (≥5 매치)
grep -c "5000" src/components/MenuSettingsSection.tsx        # = 1 (5s auto-dismiss)
grep -c "menu-config" src/components/MenuSettingsSection.tsx  # ≥ 1
grep -c "menuSettings.collapsed" src/components/MenuSettingsSection.tsx  # ≥ 2
grep -cE "기본 배치로 되돌릴까요|\+ 구분선 추가|설정 저장|기본값으로 초기화|삭제할까요" src/components/MenuSettingsSection.tsx  # = 5
grep -c "메뉴 설정이 저장되었습니다" src/components/MenuSettingsSection.tsx  # = 1
grep -c "저장에 실패했습니다" src/components/MenuSettingsSection.tsx        # = 1
grep -c "새 구분선" src/components/MenuSettingsSection.tsx                    # = 1

# (C) 4 subcomponent 시그니처
grep -c "function ArrowButton" src/components/MenuSettingsSection.tsx        # = 1
grep -c "function ToggleSmall" src/components/MenuSettingsSection.tsx        # = 1
grep -c "function DividerTitleInput" src/components/MenuSettingsSection.tsx  # = 1
grep -c "function DeleteConfirmInline" src/components/MenuSettingsSection.tsx # = 1

# (D) W5-OQ #B Toggle 토큰화
grep -c "#2563eb" src/components/MenuSettingsSection.tsx       # = 0  (raw 폐기)
grep -c "bg-accent-active" src/components/MenuSettingsSection.tsx  # ≥ 1 (Toggle on)

# (E) negative gate — body markup 안 위반 0
grep -cE "fontSize: ?(9|10|11)[,$]" src/components/MenuSettingsSection.tsx   # = 0
grep -cE "text-\[(9|10|11)px\]" src/components/MenuSettingsSection.tsx        # = 0
grep -cE "var\(--(bg3|bg4|bd2|t1|t2|t3|danger|acl)\)" src/components/MenuSettingsSection.tsx  # = 0
grep -c "style={{" src/components/MenuSettingsSection.tsx                      # = 0
grep -c "linear-gradient" src/components/MenuSettingsSection.tsx               # = 0
grep -c "✕" src/components/MenuSettingsSection.tsx                              # = 0
grep -P "[\x{1F300}-\x{1FAFF}\x{2600}-\x{27BF}]" src/components/MenuSettingsSection.tsx  # 매치 0

# (F) w-8 함정 — 32×32 = w-7 사용 확인
grep -c "w-7 h-7" src/components/MenuSettingsSection.tsx   # ≥ 2 (ArrowButton + Trash)

# (G) Tailwind class 가독성 — 약 350~400 라인 (참고용, 강제 아님)
wc -l src/components/MenuSettingsSection.tsx

# (H) tsc + build
npx tsc --noEmit   # 0 errors
npm run build      # PASS

# (I) 보호 파일 git diff 0 byte (17개 + live 5개 = 22개)
cd /Users/jykevin/Documents/cbc7119-design
git diff --stat \
  cha-bio-safety/src/App.tsx \
  cha-bio-safety/src/styles/tokens.css \
  cha-bio-safety/src/styles/typography.css \
  cha-bio-safety/tailwind.config.js \
  cha-bio-safety/docs/redesign-context/00-design-context/ \
  cha-bio-safety/docs/redesign-context/31-chrome/wave-1-index.md \
  cha-bio-safety/docs/redesign-context/31-chrome/GlobalHeader.tsx \
  cha-bio-safety/docs/redesign-context/31-chrome/SideMenu.tsx \
  cha-bio-safety/docs/redesign-context/31-chrome/SettingsPanel.tsx \
  cha-bio-safety/docs/redesign-context/31-chrome/MenuSettingsSection.tsx \
  cha-bio-safety/docs/redesign-context/31-chrome/sketch-wave-2-global-header.html \
  cha-bio-safety/docs/redesign-context/31-chrome/sketch-wave-3-side-menu.html \
  cha-bio-safety/docs/redesign-context/31-chrome/sketch-wave-4-settings-panel.html \
  cha-bio-safety/docs/redesign-context/31-chrome/sketch-wave-5-menu-settings-section.html \
  cha-bio-safety/docs/redesign-context/31-chrome/design-system.md \
  cha-bio-safety/docs/redesign-context/31-chrome/tokens.css \
  cha-bio-safety/docs/redesign-context/31-chrome/typography.css \
  cha-bio-safety/src/components/GlobalHeader.tsx \
  cha-bio-safety/src/components/SideMenu.tsx \
  cha-bio-safety/src/components/SettingsPanel.tsx \
  cha-bio-safety/src/utils/api.ts \
  cha-bio-safety/src/stores/authStore.ts
# 출력 빈 줄 (모든 보호 파일 0 byte 변경)
```

위 모든 검사가 PASS 한 뒤에 atomic 1-commit 진행.

**STEP 4 — atomic 1-commit + 사용자 컨펌 대기:**

```bash
cd /Users/jykevin/Documents/cbc7119-design

# 변경 파일 확인 — MenuSettingsSection.tsx 1 파일만 modified
git status --porcelain   # M cha-bio-safety/src/components/MenuSettingsSection.tsx 단 1 줄

# atomic 1-commit (W6 §1.1 OQ #8 LOCKED)
git add cha-bio-safety/src/components/MenuSettingsSection.tsx
git commit -m "$(cat <<'EOF'
feat(redesign/31-chrome-W10): MenuSettingsSection.tsx Tailwind class only atomic 변환

— 418 라인 inline style 전부 Tailwind class only 로 재작성 (W6 §10 cheatsheet)
— legacy alias --t1/--t2/--t3/--bg3/--bg4/--bd2/--danger/--acl body 0 → v0.1.1 의미 토큰
— W5-OQ #B LOCKED Toggle accent: raw #2563eb → bg-accent-active, knob #fff → bg-text-on-accent
— 8 노안 격상 (L173/L214/L263 9→12 + L294/L297/L301/L307/L407 10→12) 적용
— 비즈 anchor 43건 + 17 Korean copy + Phase 21 DEFAULT_SIDE_MENU forward-merge 의존 1 byte 변경 0
— 4 subcomponent (ArrowButton / ToggleSmall / DividerTitleInput / DeleteConfirmInline) 시그니처 verbatim
— 보호 파일 22개 git diff 0 byte / npx tsc --noEmit 0 errors / npm run build PASS
— chrome 4 컴포넌트 TSX 변환 의 2/4 (W7 GlobalHeader 다음, W8 SideMenu / W9 SettingsPanel 남음)

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"

git log --oneline -1
git status --porcelain   # 빈 줄
```

커밋 후 **사용자에게 main 머지 + cbc7119-preview 자동 배포 컨펌 요청** (memory `feedback_deploy_test` 의 redesign 트랙 룰 — 사용자 명시 컨펌 후에만 main 머지). wrangler / `npm run deploy` 절대 실행 금지 (memory `feedback_cbc7119_design_never_wrangler`).
  </action>
  <verify>
    <automated>cd /Users/jykevin/Documents/cbc7119-design/cha-bio-safety && npx tsc --noEmit && npm run build && grep -c "import { MENU } from './SideMenu'" src/components/MenuSettingsSection.tsx | grep -q '^1$' && grep -c "DEFAULT_SIDE_MENU" src/components/MenuSettingsSection.tsx | awk '$1>=2{exit 0}{exit 1}' && grep -c "PATH_LABEL\|DESKTOP_ONLY_PATHS\|ADMIN_PATHS" src/components/MenuSettingsSection.tsx | awk '$1>=6{exit 0}{exit 1}' && grep -c "5000" src/components/MenuSettingsSection.tsx | grep -q '^1$' && grep -cE "기본 배치로 되돌릴까요|\+ 구분선 추가|설정 저장|기본값으로 초기화|삭제할까요" src/components/MenuSettingsSection.tsx | grep -q '^5$' && [ "$(grep -c '#2563eb' src/components/MenuSettingsSection.tsx)" = "0" ] && grep -c "bg-accent-active" src/components/MenuSettingsSection.tsx | awk '$1>=1{exit 0}{exit 1}' && [ "$(grep -cE 'fontSize: ?(9|10|11)[,$]' src/components/MenuSettingsSection.tsx)" = "0" ] && [ "$(grep -cE 'var\(--(bg3|bg4|bd2|t1|t2|t3|danger|acl)\)' src/components/MenuSettingsSection.tsx)" = "0" ] && [ "$(grep -c 'style={{' src/components/MenuSettingsSection.tsx)" = "0" ] && [ "$(grep -c '✕' src/components/MenuSettingsSection.tsx)" = "0" ] && [ "$(grep -c 'linear-gradient' src/components/MenuSettingsSection.tsx)" = "0" ] && echo PASS</automated>
  </verify>
  <done>
- `cha-bio-safety/src/components/MenuSettingsSection.tsx` 가 Tailwind class only 형태로 재작성됨 (style={{}} 인라인 0, legacy alias 0, raw #2563eb/#fff body 0)
- 비즈 anchor 43건 + 17 Korean copy + 4 subcomponent 시그니처 + Phase 21 DEFAULT_SIDE_MENU 의존 1 byte 변경 0
- 8 노안 격상 (L173/L214/L263 9→12 + L294/L297/L301/L307/L407 10→12) 적용
- W5-OQ #B LOCKED Toggle: `bg-accent-active` (on) + `bg-text-on-accent` (knob) 사용 — raw #2563eb 0 / raw #fff body 0
- 32×32 ArrowButton/Trash 는 `w-7 h-7` (w-8=48 함정 회피)
- `npx tsc --noEmit` 0 errors
- `npm run build` PASS
- 보호 파일 22개 (W6 §12.5 17개 + live 5개) `git diff` 빈 줄 (0 byte)
- 단일 atomic 1-commit (W6 §1.1 OQ #8 LOCKED) — MenuSettingsSection.tsx 1 파일만 변경
- 사용자에게 main 머지 + cbc7119-preview 자동 배포 컨펌 요청 (wrangler / `npm run deploy` 실행 0)
  </done>
</task>

</tasks>

<verification>
**최종 검증 (W6 §12.4 + §12.5):**

1. **파일 존재 + 시그니처 보존:**
   - `test -f cha-bio-safety/src/components/MenuSettingsSection.tsx`
   - `grep -c "export function MenuSettingsSection" .../MenuSettingsSection.tsx` = 1
   - `grep -c "function ArrowButton\|function ToggleSmall\|function DividerTitleInput\|function DeleteConfirmInline" .../MenuSettingsSection.tsx` = 4

2. **비즈 anchor 보존 (W6 §4.4 43건 grep — sample ≥10):**
   - `grep -c "MENU.forEach(s => s.items.forEach"` ≥ 1
   - `grep -c "MENU.flatMap(s => s.items).filter(i => i.desktopOnly)"` = 1
   - `grep -c "MENU.flatMap(s => s.items).filter(i => i.role === 'admin')"` = 1
   - `grep -c "settingsApi.getMenu\|settingsApi.saveMenu"` ≥ 2
   - `grep -c "menu-config"` ≥ 1
   - `grep -c "DEFAULT_SIDE_MENU.map(e => ({ ...e }))"` ≥ 1 (draft init + resetToDefaults)
   - `grep -c "300_000\|staleTime"` ≥ 1
   - `grep -c "entriesEqual"` ≥ 2
   - `grep -c "newDividerId"` ≥ 2
   - `grep -c "confirmTimerRef"` ≥ 3
   - `grep -c "menuSettings.collapsed"` ≥ 2
   - `grep -c "saveMutation.isPending"` ≥ 2
   - `grep -c "저장 중…"` = 1 (U+2026 single ellipsis 보존)

3. **negative gate (≥10):**
   - fontSize 9/10/11 body 0
   - legacy alias body 0
   - raw #2563eb 0 / raw `#fff` body 0
   - `style={{` 0
   - `linear-gradient` 0
   - `✕` 0
   - 이모지 0 (BMP unicode 범위 매치 0)

4. **빌드 + 타입:**
   - `cd cha-bio-safety && npx tsc --noEmit` 0 errors
   - `cd cha-bio-safety && npm run build` PASS

5. **보호 파일 git diff 0 byte (22개 — 위 negative_gate 항목 14):**
   - 모두 빈 줄

6. **atomic commit:**
   - `git status --porcelain` 빈 줄 (commit 후)
   - `git log --oneline -1` 메시지에 `redesign/31-chrome-W10` 포함
   - `git show --stat HEAD` 1 파일 (`cha-bio-safety/src/components/MenuSettingsSection.tsx`) 만 변경
</verification>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MenuSettingsSection ↔ MENU (SideMenu.tsx) | MENU 상수 import — 본 W10 변환은 read-only 사용, MENU 시그니처 변경 0 |
| MenuSettingsSection ↔ utils/api.ts | settingsApi.getMenu / saveMenu + DEFAULT_SIDE_MENU + SideMenuEntry/MenuConfig 타입 — Phase 21 forward-merge 의존, 시그니처 변경 0 |
| MenuSettingsSection ↔ authStore | useAuthStore(s => s.staff) — read-only ADMIN_PATHS 필터에만 사용 |
| MenuSettingsSection ↔ localStorage | 'menuSettings.collapsed' 키 — boolean string |
| MenuSettingsSection ↔ DOM | input focus/select on mount, 5s setTimeout |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-W10-01 | Tampering | DividerTitleInput value | accept | maxLength 20 + `.trim().slice(0, 20)` (서버 saveMenu API 가 추가 validation 책임 — utils/api.ts 변경 0) |
| T-W10-02 | Information Disclosure | ADMIN_PATHS 필터 | accept | role 체크는 클라이언트 UX only (서버 권한 분리 = 별도 백엔드 책임) — Phase 21 미변경 |
| T-W10-03 | Denial of Service | 5s auto-dismiss + setTimeout(0) | mitigate | confirmTimerRef cleanup useEffect 보존 (line 152~159 verbatim) — TSX 변환에서 1 byte 변경 0 |
| T-W10-04 | Repudiation | localStorage 'menuSettings.collapsed' tampering | accept | UX state only (보안 비밀 X) — XSS 가 발생하지 않는 한 무관 |
| T-W10-05 | Spoofing | DEFAULT_SIDE_MENU hand-roll 치환 | mitigate | grep gate: `grep -c "DEFAULT_SIDE_MENU" .../MenuSettingsSection.tsx` ≥ 2 강제 (Phase 21 forward-merge 의존 보존) |
| T-W10-06 | Elevation of Privilege | ADMIN_PATHS Set 조작 | accept | `MENU.flatMap(...).filter(i => i.role === 'admin')` 정적 IIFE — runtime 조작 불가 (SideMenu.tsx 정의가 source of truth, W8 책임) |
</threat_model>

<success_criteria>
- 단일 atomic 1-commit: `cha-bio-safety/src/components/MenuSettingsSection.tsx` 1 파일만 변경 (`git status --porcelain` empty 또는 .planning/quick/ 만)
- `npx tsc --noEmit` 0 errors
- `npm run build` PASS
- W6 §12.4 verify gate ≥10 항목 모두 PASS
- W6 §11 negative gate ≥20 항목 모두 PASS (단 본 W10 적용 항목 모두)
- 보호 파일 22개 git diff 0 byte
- 비즈 anchor 43건 + 17 Korean copy + Phase 21 DEFAULT_SIDE_MENU 의존 + 4 subcomponent 시그니처 1 byte 변경 0
- 사용자 컨펌 받은 후 main 머지 (wrangler / `npm run deploy` 실행 0)
- chrome 4 컴포넌트 TSX 변환의 2/4 단계 완결 (W7 다음, W8/W9 남음)
</success_criteria>

<output>
After completion, create `.planning/quick/260527-8gc-redesign-31-chrome-w10-menusettingssecti/260527-8gc-SUMMARY.md` documenting:
- 변환 line count (before/after) — 418 → ?
- 격상된 8건 fontSize 매핑 결과 (실측 line)
- W5-OQ #B Toggle 토큰화 결과 (raw #2563eb / #fff 폐기 grep 0)
- legacy alias 8종 → v0.1.1 semantic 토큰 치환 결과
- 4 subcomponent 시그니처 verbatim 유지 grep 결과
- 보호 파일 22개 git diff 0 byte 확인
- 빌드 결과 (tsc 0 errors + npm run build PASS)
- atomic 1-commit hash + main 머지 컨펌 대기 상태
- chrome 4 컴포넌트 TSX 변환 진행 (W7 완결 / W10 완결 / W8 + W9 남음)
- 메모리 박제 필요한 새 사실 (없으면 N/A)
</output>
