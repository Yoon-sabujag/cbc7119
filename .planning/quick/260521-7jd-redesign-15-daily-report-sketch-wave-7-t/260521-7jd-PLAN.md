---
quick_id: 260521-7jd
type: execute
wave: 1
depends_on: []
files_modified:
  - cha-bio-safety/docs/redesign-context/15-daily-report/wave-7-tsx-conversion-checklist.md
autonomous: true
requirements: ["redesign-15-w7"]
mirror_of: cha-bio-safety/docs/redesign-context/14-reports/wave-7-tsx-conversion-checklist.md
sub_wave: W7 (final sketch wave — TSX 변환 verify checklist markdown)
branch: redesign/15-daily-report
target_lines: ">=700"
must_haves:
  truths:
    - "wave-7-tsx-conversion-checklist.md 파일이 신규 생성되어 ≥700 lines 로 W2~W6 sketch + DailyReportPage.tsx 비즈 로직 보존 룰을 한 곳에 박제한다"
    - "§1~§10 모든 헤더가 채워져 있고 빈 섹션 0건"
    - "W2~W6 신규 CSS class 정의 ≥51건이 sketch HTML 에서 grep verbatim 추출되어 fence 안에 인용됨"
    - "DailyReportPage.tsx 비즈 로직 보존 항목 ≥20 entries 명시"
    - "폰트 격상 매트릭스 ≥15 entries + 9·10·11px 0건 룰 명시"
    - "메모리 룰 unique slug ≥10 박제 (feedback_*.md)"
    - "W1~W6 OQ default LOCKED 매트릭스 28건 LOCKED 상태 명시"
    - "negative gate ≥7 entries + 자체 verify gate ≥10 entries"
    - "SW1~SW4 변환 wave 분배 plan 표 ≥4 row"
    - "이모지 0 / wrangler 0 / npm run deploy 0 / src/** 변경 0"
  artifacts:
    - path: "cha-bio-safety/docs/redesign-context/15-daily-report/wave-7-tsx-conversion-checklist.md"
      provides: "W7 TSX 변환 verify checklist markdown (sketch HTML 아님, sketch 마지막 wave)"
      min_lines: 700
  key_links:
    - from: "wave-7-tsx-conversion-checklist.md §1"
      to: "W2~W6 sketch-wave-*.html"
      via: "grep verbatim 인용"
      pattern: "신규 .class 정의 ≥51건"
    - from: "wave-7-tsx-conversion-checklist.md §3"
      to: "DailyReportPage.tsx"
      via: "line-by-line 비즈 로직 보존 룰"
      pattern: "useQuery × 2 / useState × 7 / 핸들러 7건 / 캘리브 로직 / props 시그니처"
    - from: "wave-7-tsx-conversion-checklist.md §8"
      to: "wave-1-index.md §7 + W2~W6 sketch OQ"
      via: "OQ default LOCKED 매트릭스"
      pattern: "28건 LOCKED 상태"
---

<objective>
**Quick task**: cbc7119-design 워크트리 (redesign/15-daily-report 브랜치) 에서 sketch wave 7 (TSX 변환 verify checklist markdown) 1개 신규 생성. 14-reports W7 패턴 mirror (`cha-bio-safety/docs/redesign-context/14-reports/wave-7-tsx-conversion-checklist.md` 가 template).

**핵심 산출물**: `cha-bio-safety/docs/redesign-context/15-daily-report/wave-7-tsx-conversion-checklist.md` — ≥700 lines, §1~§10 모든 섹션 채움, W2~W6 sketch 의 신규 .class 정의 ≥51건 verbatim 인용, DailyReportPage.tsx 비즈 로직 보존 ≥20 entries, 폰트 격상 ≥15 entries, 메모리 룰 unique slug ≥10, W1~W6 OQ default LOCKED 매트릭스 28건, negative gate ≥7, 자체 verify gate ≥10, SW1~SW4 분배 plan 표 ≥4 row.

**Purpose**: W7 자체는 sketch 단계 마지막 — OQ 없음 (모든 OQ 는 W1~W6 에서 컨펌 완료). TSX 변환 wave (SW1~SW4) executor 가 본 markdown 1개 + W2~W6 sketch 5개 + DailyReportPage.tsx 만 input 으로 atomic 변환 가능하도록 컨텍스트 박제.

**Output**: 신규 markdown 1개. src/** 변경 0. components.css 변경 0. App.tsx 변경 0. wrangler 0. npm run deploy 0.

**Negative gates**:
- markdown 본문은 sketch HTML 아님 — `<html>` / `<body>` / `<style>` 블록 없음
- 이모지 0건 (Unicode 1F300~1FAFF / 2600~26FF / 2700~27BF 범위 0). §5 "이모지 교체 매트릭스" 에서 source line 인용 시는 backtick 또는 ASCII 표기 OK (예: ⬇ 글리프 → `⬇` 또는 ASCII 설명)
- wrangler 명령 0건 / `npm run deploy` 0건 (CLAUDE.local.md 룰 + memory `feedback_cbc7119_design_never_wrangler`)
- 다른 페이지 (13-schedule / 14-reports / 02-inspection / 06-floorplan) 영향 0
- `cha-bio-safety/src/**` 변경 0 (검증: `git diff --name-only HEAD -- cha-bio-safety/src` = 0 lines)
- `cha-bio-safety/src/styles/components.css` 변경 0 (W7 SW1 변환 wave 에서만 추가, 본 wave 는 markdown 만)
- `cha-bio-safety/src/App.tsx` 변경 0 (MOBILE_NO_NAV_PATHS 이미 등재됨)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md
@./CLAUDE.local.md
@cha-bio-safety/docs/redesign-context/15-daily-report/wave-1-index.md
@cha-bio-safety/docs/redesign-context/15-daily-report/sketch-wave-2-mobile-header-date-nav.html
@cha-bio-safety/docs/redesign-context/15-daily-report/sketch-wave-3-editable-cards-personnel.html
@cha-bio-safety/docs/redesign-context/15-daily-report/sketch-wave-4-download-action.html
@cha-bio-safety/docs/redesign-context/15-daily-report/sketch-wave-5-desktop-layout.html
@cha-bio-safety/docs/redesign-context/15-daily-report/sketch-wave-6-portrait-preview-wrapper.html
@cha-bio-safety/docs/redesign-context/15-daily-report/design-system.md
@cha-bio-safety/docs/redesign-context/14-reports/wave-7-tsx-conversion-checklist.md
@cha-bio-safety/src/pages/DailyReportPage.tsx
@cha-bio-safety/src/styles/components.css

<interfaces>
<!-- Key interfaces / props / state from DailyReportPage.tsx that the checklist MUST verbatim quote -->
<!-- Source: cha-bio-safety/src/pages/DailyReportPage.tsx (840 lines) -->

From line 1~10 (imports):
```typescript
import { useState, useEffect, useCallback, useRef } from 'react'
import { useNavigate } from 'react-router-dom'
import { useQuery, useQueryClient } from '@tanstack/react-query'
import toast from 'react-hot-toast'
import { dailyReportApi } from '../utils/api'
import { buildDailyReportData } from '../utils/dailyReportCalc'
import { generateDailyExcel } from '../utils/generateExcel'
import { useStaffList } from '../hooks/useStaffList'
import { useIsDesktop } from '../hooks/useIsDesktop'
```

From line 13~27 (date utilities):
```typescript
function todayKST(): string {...}
function addDays(dateStr: string, n: number): string {...}
function nowKSTHour(): number {...}
```

From line 30~58 (style constants — 모두 변환 후 신규 CSS class 로 대체됨):
```typescript
const iconBtn: React.CSSProperties = { width: 34, height: 34, borderRadius: 8, ... }   // → .back-btn (14-reports inherit)
const navBtn: React.CSSProperties = { width: 28, height: 28, borderRadius: 7, fontSize: 16, ..., lineHeight: '1' }   // → .date-nav-btn (border-radius 7 → 6)
const card: React.CSSProperties = { background: 'var(--bg2)', borderRadius: 14, ..., padding: 14, marginBottom: 10 }   // → .editable-card / .summary-card
const textareaStyle: React.CSSProperties = { ..., fontSize: 12, ... }   // → .editable-card-textarea (fontSize 12 → 14)
const smallBtn: React.CSSProperties = { fontSize: 10, fontWeight: 700, padding: '4px 10px', borderRadius: 6, ... }   // → .editable-card-btn--reset / --save (fontSize 10 → 12)
```

From line 61~80 (state + computed):
```typescript
export default function DailyReportPage() {
  const navigate = useNavigate()
  const queryClient = useQueryClient()
  const isDesktop = useIsDesktop()
  const { data: staffList } = useStaffList()
  const staffData = staffList?.map(s => ({ id: s.id, name: s.name, title: s.title ?? '' }))

  const [date, setDate] = useState<string>(todayKST())
  const [todayText, setTodayText] = useState<string>('')
  const [tomorrowText, setTomorrowText] = useState<string>('')
  const [notes, setNotes] = useState<string>('')
  const [generating, setGenerating] = useState(false)
  const [genMonthly, setGenMonthly] = useState(false)
  const [saving, setSaving] = useState<Record<string, boolean>>({})
  const [loaded, setLoaded] = useState<string>('')

  const today = todayKST()
  const [mm, dd] = [date.split('-')[1], date.split('-')[2]]
  const isFutureDate = date > today
  const canForward = date < today
```

From line 86~98 (queries):
```typescript
const queryData = useQuery({ queryKey: ['daily-report', date], queryFn: () => dailyReportApi.getData(date), retry: 1 })
const queryNotes = useQuery({ queryKey: ['daily-notes', date], queryFn: () => dailyReportApi.getNotes(date), retry: 1, staleTime: 0, gcTime: 0 })
```

From line 150~162 (debouncedSave):
```typescript
const debouncedSave = useCallback((field: string, value: string) => {
  if (loaded !== date) return
  if (debounceRef.current[field]) clearTimeout(debounceRef.current[field])
  debounceRef.current[field] = setTimeout(() => { ... dailyReportApi.saveNotes(payload) ... }, 2000)   // 2초 디바운스
}, [date, loaded])
```

From line 169~190 (handlers):
```typescript
const handleManualSave = async (field: string) => { setSaving... await dailyReportApi.saveNotes(payload) ... toast.success('저장되었습니다') ... }
const handleReset = async (field: string) => { ... toast.success('초기화되었습니다') }
```

From line 193~194 (date nav):
```typescript
const goBack = () => setDate(d => addDays(d, -1))
const goForward = () => { if (canForward) setDate(d => addDays(d, 1)) }
```

From line 197~265 (downloads):
```typescript
const handleDailyDownload = async () => { ... await generateDailyExcel('daily', y, m, { [d]: data }, d) ... }
const handleMonthlyDownload = async () => { ... await generateDailyExcel('monthly', y, m, dayDataMap, limitDay) ... }
```

From line 387~401 (dateNav — verbatim):
```typescript
const dateNav = (
  <div style={{ display: 'flex', alignItems: 'center', gap: 2 }}>
    <button onClick={goBack} style={navBtn}>‹</button>
    <span style={{ width: 90, textAlign: 'center', fontSize: 13, fontWeight: 700, color: 'var(--t1)' }}>{date}</span>
    <div style={{ width: 28, display: 'flex', justifyContent: 'center' }}>
      {canForward
        ? <button onClick={goForward} style={navBtn}>›</button>
        : <span style={{ width: 28 }}/>
      }
    </div>
  </div>
)
```

From line 434~440 (DailyPortraitPreview props — 100% 보존):
```typescript
<DailyPortraitPreview
  date={date}
  todayText={todayText}
  tomorrowText={tomorrowText}
  notes={notes}
  personnel={preview?.personnel}
/>
```

From line 449~458 (mobile header — verbatim):
```typescript
<header style={{ flexShrink: 0, background: 'var(--bg2)', borderBottom: '1px solid var(--bd)', padding: '8px 12px 9px', display: 'flex', alignItems: 'center', gap: 8 }}>
  <button onClick={() => navigate(-1)} style={iconBtn} aria-label="뒤로 가기">
    <svg width={15} height={15} fill="none" viewBox="0 0 24 24" stroke="var(--t2)" strokeWidth={2}>
      <path strokeLinecap="round" strokeLinejoin="round" d="M15 19l-7-7 7-7"/>
    </svg>
  </button>
  <span style={{ flex: 1, fontSize: 14, fontWeight: 700, color: 'var(--t1)' }}>일일 업무 일지</span>
  {dateNav}
</header>
```

From line 469~485 (DAILY_CALIB_STEPS — 15 step, 100% 보존):
```typescript
const DAILY_CALIB_STEPS = [
  { key: 'date',        label: '????년 ??월 ??일', color: '#3b82f6' },
  { key: 'today',       label: '금일업무',         color: '#22c55e' },
  { key: 'tomorrow',    label: '명일업무',         color: '#10b981' },
  { key: 'notes',       label: '특이사항',         color: '#06b6d4' },
  { key: 'pTotal',      label: '총원',             color: '#f59e0b' },
  { key: 'pPresent',    label: '현재원',           color: '#d97706' },
  { key: 'pDuty',       label: '당직',             color: '#ef4444' },
  { key: 'pOff',        label: '비번',             color: '#a855f7' },
  { key: 'pRest',       label: '휴무',             color: '#7c3aed' },
  { key: 'pLeave',      label: '연차',             color: '#ec4899' },
  // ... 총 15 step (pHalf / pTraining / pAbsent / dayWorker / dutyWorker)
]
const DAILY_CALIB_KEY = 'calib_daily_report'
const FINGER_OFFSET = 60   // 손가락 가림 보정
```

From `cha-bio-safety/src/styles/components.css` (14-reports SW1 결과, 6 inherit candidates):
- Line 1: `.page-header`
- Line 2: `.back-btn`
- Line 3: `.page-title`
- Line 8: `.page-body`
- Line 15: `.dot-meta`
- Line 16: `.page-footer-note`

</interfaces>

<copy_verbatim>
<!-- Korean UI copy that MUST be preserved verbatim in checklist §3 비즈 로직 보존 룰 -->
<!-- Source: DailyReportPage.tsx — memory feedback_sketch_realistic_data 룰 -->

1. "일일 업무 일지"  (line 456 — 헤더 타이틀)
2. "금일업무"          (line 275 — EditableCard label)
3. "명일업무"          (line 283 — EditableCard label)
4. "특이사항"          (line 291 — EditableCard label)
5. "오늘 특이사항을 입력하세요"  (line 295 — placeholder)
6. "초기화"            (EditableCard 초기화 버튼)
7. "저장"              (EditableCard 저장 버튼)
8. "저장 중..."        (saving state)
9. "저장되었습니다"    (line 178 — toast)
10. "초기화되었습니다" (line 189 — toast)
11. "인원현황"         (line 300 — 인원현황 카드 라벨)
12. "총원" / "현재원" / "비번" / "연차" / "반차" / "교육/훈련" / "주간근무자" / "당직근무자"  (line 307~313)
13. "데이터 불러오기 실패 — 다시 시도해 주세요"  (line 304 — error)
14. "해당 날짜 데이터 없음"  (line 316 — empty)
15. `${Number(mm)}월${dd}일 방재업무일지 다운로드`  (line 334, 363 — daily 버튼 카피)
16. `일일업무일지(${mm}월) 다운로드`  (line 347, 376 — monthly 버튼 카피)
17. "생성 중..."        (line 334, 363 — loading state)
18. "월별 생성 중..."   (line 347, 376 — monthly loading)
19. "수정 내용은 자동 저장됩니다 · 월별은 저장된 모든 날짜를 포함합니다"  (line 382 — 안내 줄)
20. "엑셀 생성 중 오류가 발생했습니다"  (line 207 — toast error)
21. "월별 엑셀 생성 중 오류가 발생했습니다"  (line 263 — toast error)
22. "인쇄 미리보기"     (line 432 — 데스크톱 우측 라벨)
23. "위치 재설정" / "⚠ 위치 설정"  (line 777 — 캘리브 버튼)
24. "확인" / "취소"     (캘리브 안내 바)

</copy_verbatim>
</context>

<tasks>

<task type="auto">
  <name>Task 1: wave-7-tsx-conversion-checklist.md 신규 생성 (≥700 lines, §1~§10)</name>
  <files>cha-bio-safety/docs/redesign-context/15-daily-report/wave-7-tsx-conversion-checklist.md</files>
  <action>
**경로**: `cha-bio-safety/docs/redesign-context/15-daily-report/wave-7-tsx-conversion-checklist.md` (신규).

**범위**: markdown 1개, ≥700 lines, §1~§10 모든 섹션 채움.

**구성 (10 sections 강제, 빈 섹션 0건)**:

---

### YAML frontmatter (필수)

```yaml
---
title: "15-daily-report — W7 TSX 변환 verify checklist"
status: draft
created: 2026-05-21
quick_id: 260521-7jd
branch: redesign/15-daily-report
source_tsx: cha-bio-safety/src/pages/DailyReportPage.tsx
source_tsx_lines: 840
sketches_referenced: [W1, W2, W3, W4, W5, W6]
locked_decisions: "W1: 7 / W2~W6: 21 / W7 (본 wave): 0 (sketch 마지막 wave)"
sub_wave_count: 4   # SW1 components.css / SW2 모바일 / SW3 데스크톱 / SW4 verify gate
verify_gate_count: ">=10"
mirror_of: cha-bio-safety/docs/redesign-context/14-reports/wave-7-tsx-conversion-checklist.md
consumed_by: "15-daily-report TSX 변환 wave (SW1~SW4) executor"
---
```

---

### §1. W2~W6 sketch verbatim class 인벤토리 (≥51 신규 class)

**§1 인트로**: 각 wave 의 신규 .class 정의를 `grep -oE '^\\.[a-z][a-z0-9-]+\\s*\\{' sketch-wave-N-*.html` 으로 추출한 결과 박제. 14-reports `components.css` SW1 결과 6 class inherit + 신규 51 class.

**§1.0 14-reports `components.css` SW1 inherit (6 class, 재정의 X)**

표 형식 (≥6 row):
| class | components.css 위치 | 15-daily-report 적용 element | 비고 |
|---|---|---|---|
| `.page-header` | line 1 | line 449~458 모바일 자체 헤더 | bg-surface-raised, padding 8 12 9 |
| `.back-btn` | line 2 | line 451 뒤로 버튼 (34×34) | iconBtn line 30 verbatim |
| `.page-title` | line 3 | line 456 "일일 업무 일지" | 18px (현 14 → 18 노안 격상) |
| `.page-body` | line 8 | line 461 스크롤 본문 | padding 12 16 |
| `.dot-meta` | line 15 | 인원현황 카드 "·" 구분자 (line 307~313) | 4×4 회색 dot |
| `.page-footer-note` | line 16 | line 381~383 안내 줄 | text-align center, 12px |

**§1.1 W2 신규 class (4건, sketch-wave-2 line 386~420)**

```css
/* sketch-wave-2-mobile-header-date-nav.html line 386~420 verbatim */
.date-nav {
  display: flex;
  align-items: center;
  gap: 4px;   /* source navBtn gap 2 → 4 (노안 격상, space-1) */
}
.date-nav-btn {
  width: 28px;
  height: 28px;
  border-radius: 6px;   /* source navBtn 7 → 6 (sm-token 일관) */
  border: 1px solid var(--border-default);
  background: var(--surface-sunken);
  color: var(--text-primary);
  font-size: 16px;
  font-weight: 700;
  cursor: pointer;
  display: flex;
  align-items: center;
  justify-content: center;
  line-height: 1;   /* source line 40 lineHeight:'1' verbatim */
}
.date-display {
  min-width: 96px;   /* source width 90 → 96 (4자리 연도 + 한글 안전 폭) */
  text-align: center;
  font-size: 14px;   /* source 13 → 14 (text-body-sm, 노안 격상) */
  font-weight: 700;
  color: var(--text-primary);
  line-height: 1;
  white-space: nowrap;
  padding: 0 4px;
}
.date-nav-spacer {
  width: 28px;   /* source line 397 width:28 verbatim */
  height: 28px;
  flex-shrink: 0;
}
```

**§1.2 W3 신규 class (14건 — EditableCard 7 + SummaryCard 3 + helper 4, sketch-wave-3 line 427~565)**

```css
/* W3 — EditableCard 7 class */
.editable-card { background: var(--surface-raised); border: 1px solid var(--border-default); border-radius: 14px; padding: 14px; margin-bottom: 10px; }
.editable-card-head { display: flex; align-items: center; margin-bottom: 8px; }
.editable-card-label { flex: 1; font-size: 16px; font-weight: 700; color: var(--text-primary); line-height: 1.5; }   /* source line 300 fontSize 13 → 16 노안 격상 */
.editable-card-actions { display: flex; gap: 6px; flex-shrink: 0; }
.editable-card-btn--reset { font-size: 12px; font-weight: 700; padding: 4px 10px; border-radius: 6px; border: 1px solid var(--border-default); background: var(--surface-sunken); color: var(--text-tertiary); cursor: pointer; line-height: 1; font-family: inherit; }   /* smallBtn line 55 fontSize 10 → 12 */
.editable-card-btn--save { font-size: 12px; font-weight: 700; padding: 4px 10px; border-radius: 6px; border: 1px solid var(--border-default); background: var(--surface-active); color: var(--text-secondary); cursor: pointer; line-height: 1; font-family: inherit; }
.editable-card-textarea { width: 100%; box-sizing: border-box; background: var(--surface-sunken); border: 1px solid var(--border-default); border-radius: 9px; color: var(--text-primary); font-size: 14px; font-family: inherit; font-weight: 400; padding: 10px 12px; resize: vertical; outline: none; line-height: 1.6; display: block; }   /* textareaStyle line 48 fontSize 12 → 14 */
.editable-card-textarea:focus { border-color: var(--border-strong); }

/* W3 — SummaryCard 3 class */
.summary-card { background: var(--surface-raised); border: 1px solid var(--border-default); border-radius: 14px; padding: 14px; margin-bottom: 10px; }
.summary-card-label { font-size: 16px; font-weight: 700; color: var(--text-primary); margin-bottom: 6px; line-height: 1.5; }
.summary-card-body { font-size: 14px; color: var(--text-secondary); line-height: 1.6; display: flex; flex-wrap: wrap; align-items: center; gap: 0; }

/* W3 — SummaryCard 상태 helper 4 class (gate count 미포함) */
.summary-card-skeleton { background: var(--surface-active); border-radius: 4px; height: 12px; width: 70%; animation: blink 2s ease-in-out infinite; }
.summary-card-error { font-size: 14px; color: var(--status-danger); line-height: 1.5; }
.summary-card-empty { font-size: 14px; color: var(--text-tertiary); line-height: 1.5; }
.summary-card-state-label { display: inline-block; font-size: 12px; color: var(--text-tertiary); font-family: 'JetBrains Mono', monospace; letter-spacing: 0.05em; margin-bottom: 6px; text-transform: uppercase; line-height: 1; }

@keyframes blink { 0%, 100% { opacity: 0.4; } 50% { opacity: 1; } }
```

**§1.3 W4 신규 class (8건, sketch-wave-4 line 429~485)**

```css
/* W4 — 다운로드 액션 8 class */
.download-action { display: flex; flex-direction: column; gap: 8px; margin-top: 4px; }
.download-action--desktop { flex-direction: row; gap: 8px; }
.download-action--desktop > .download-btn { flex: 1; }
.download-btn { height: 44px; padding: 11px 12px; border-radius: 9px; font-size: 16px; font-weight: 700; display: inline-flex; align-items: center; justify-content: center; gap: 8px; cursor: pointer; border: none; font-family: inherit; transition: opacity 0.15s; width: 100%; box-sizing: border-box; }
.download-btn--daily { background: var(--status-safe-bar); color: var(--text-on-accent); }   /* OQ #1 LOCKED — 그라데이션 폐기 → solid */
.download-btn--monthly { background: var(--surface-sunken); border: 1px solid var(--border-strong); color: var(--text-secondary); }
.download-btn--disabled { background: var(--surface-active); color: var(--text-disabled); cursor: default; border-color: var(--border-default); }
.download-btn--daily.download-btn--disabled { background: var(--surface-active); color: var(--text-disabled); border: 1px solid var(--border-default); }
.download-btn-icon { width: 16px; height: 16px; color: inherit; flex-shrink: 0; }
/* .page-footer-note — 14-reports inherit, 동명 재사용 (재정의 X) */
```

**§1.4 W5 신규 class (6건, sketch-wave-5 line 398~440)**

```css
/* W5 — 데스크톱 layout 6 class */
.desktop-layout { display: flex; flex-direction: row; height: 100%; overflow: hidden; background: var(--surface-page); }
.desktop-edit-panel { flex: 1; overflow: auto; padding: 24px 32px; }
.desktop-edit-panel-header { display: flex; align-items: center; justify-content: flex-end; margin-bottom: 20px; }
.desktop-portrait-wrapper { aspect-ratio: 210 / 297; height: 100%; flex-shrink: 0; border-left: 1px solid var(--border-default); overflow: hidden; background: var(--surface-page); display: flex; align-items: center; justify-content: center; position: relative; }
.desktop-portrait-print-label { position: absolute; top: 8px; left: 0; right: 0; text-align: center; font-size: 12px;   /* source 11 → 12 노안 격상 */ color: var(--text-secondary); font-weight: 700; text-transform: uppercase; pointer-events: none; z-index: 5; line-height: 1; letter-spacing: 0.05em; }
.desktop-portrait-placeholder { /* W5 sketch 단계만, TSX 변환 시 실제 DailyPortraitPreview 컴포넌트로 대체 */ }
```

**§1.5 W6 신규 class (22건, sketch-wave-6 line 450~660)**

```css
/* W6 — DailyPortraitPreview wrapper 22 class */
.daily-portrait-wrapper { width: 100%; height: 100%; position: relative; display: flex; align-items: center; justify-content: center; }
.daily-portrait-image { max-width: 100%; max-height: 100%; object-fit: contain; border-radius: 8px; background: #ffffff; display: block; }
.daily-portrait-overlay-area { position: absolute; top: 0; left: 0; right: 0; bottom: 0; pointer-events: none; }
.daily-portrait-overlay-area--calib { pointer-events: auto; cursor: crosshair; }
.daily-portrait-overlay-item { position: absolute; transform: translate(-50%, -50%); font-size: 11px; font-weight: 700; color: #000; white-space: nowrap; pointer-events: none; }
.daily-portrait-overlay-item--large { font-size: 12px; }   /* LARGE_KEYS modifier */
.daily-portrait-calib-bar { position: absolute; top: 8px; left: 50%; transform: translateX(-50%); background: rgba(15, 23, 42, 0.95); color: #fff; padding: 10px 20px; border-radius: 9px; display: flex; align-items: center; gap: 12px; z-index: 10; }
.daily-portrait-calib-bar-step { width: 24px; height: 24px; border-radius: 99px; display: flex; align-items: center; justify-content: center; font-size: 14px; font-weight: 700; background: var(--accent); color: #fff; }
.daily-portrait-calib-bar-label { font-size: 14px; font-weight: 700; }
.daily-portrait-calib-bar-coord { font-size: 14px; color: rgba(255,255,255,0.8); font-family: 'JetBrains Mono', monospace; }
.daily-portrait-calib-confirm { background: var(--status-safe-bar); color: #fff; padding: 6px 14px; border-radius: 6px; font-size: 14px; font-weight: 700; border: 0; cursor: pointer; line-height: 1; }
.daily-portrait-calib-cancel { background: rgba(255,255,255,0.15); color: #fff; padding: 6px 14px; border-radius: 6px; font-size: 12px; font-weight: 700; border: 0; cursor: pointer; line-height: 1; }
.daily-portrait-setup-btn { position: absolute; bottom: 8px; right: 8px; padding: 6px 12px; border-radius: 6px; font-size: 12px; font-weight: 700; line-height: 1; cursor: pointer; border: 0; z-index: 10; display: inline-flex; align-items: center; gap: 4px; }
.daily-portrait-setup-btn--ready { background: rgba(15, 23, 42, 0.7); color: rgba(255,255,255,0.9); }
.daily-portrait-setup-btn--missing { background: rgba(239, 68, 68, 0.9); color: #fff; }
.daily-portrait-calib-marker { position: absolute; transform: translate(-50%, -50%); pointer-events: none; z-index: 8; }
.daily-portrait-calib-marker-crosshair-h { position: absolute; width: 40px; height: 2px; background: currentColor; top: 50%; left: 50%; transform: translate(-50%, -50%); }
.daily-portrait-calib-marker-crosshair-v { position: absolute; width: 2px; height: 40px; background: currentColor; top: 50%; left: 50%; transform: translate(-50%, -50%); }
.daily-portrait-calib-marker-dot { position: absolute; top: 50%; left: 50%; width: 16px; height: 16px; transform: translate(-50%, -50%); border-radius: 99px; background: currentColor; }
.daily-portrait-calib-marker-dot--active { width: 20px; height: 20px; }   /* drag 중 시각 강조 */
.daily-portrait-placeholder-img { /* W6 sketch 단계만, TSX 변환 시 실제 /templates/preview/daily-1.png src 그대로 */ }
```

**§1.6 합계**: W2 (4) + W3 (14) + W4 (8) + W5 (6) + W6 (22) = **54건** — 신규 class 합계. 가이드라인 ≥51 충족.

---

### §2. components.css 신규 추가 명단 (≥51 verbatim quote — SW1 변환 wave 가 직접 paste)

**§2.1 14-reports `components.css` (현재) 6 class — 재정의 X, 그대로 inherit**

표 형식 (6 row): `.page-header` / `.back-btn` / `.page-title` / `.page-body` / `.dot-meta` / `.page-footer-note`. SW1 변환 wave 에서 components.css 끝부분에 새 줄 추가만 함 (기존 6 class 손대지 않음).

**§2.2 신규 54 class (§1.1~§1.5 의 모든 fence verbatim 복사)**

§1.1~§1.5 의 CSS fence 를 그대로 components.css 에 추가. SW1 변환 wave executor 는 본 섹션의 fence 만 복사하면 됨 (추측 X, memory `feedback_planner_prompt_sketch_verbatim` 룰).

**§2.3 분할 가이드 (components.css 안 순서)**

components.css 끝부분에 다음 순서로 추가:
1. `/* ── 15-daily-report W2 (dateNav) ── */` + W2 4 class
2. `/* ── 15-daily-report W3 (EditableCard / SummaryCard) ── */` + W3 14 class
3. `/* ── 15-daily-report W4 (다운로드 액션) ── */` + W4 8 class
4. `/* ── 15-daily-report W5 (데스크톱 layout) ── */` + W5 6 class
5. `/* ── 15-daily-report W6 (DailyPortraitPreview wrapper) ── */` + W6 22 class
6. `/* ── @keyframes blink (W3 skeleton 의존) ── */` + keyframes

---

### §3. DailyReportPage.tsx 비즈 로직 보존 룰 (≥20 entries, line-by-line)

**§3.1 imports / hooks 보존 (line 1~10, ≥3 entries)**
- [ ] line 1~10 imports 모두 보존 (`useState/useEffect/useCallback/useRef`, `useNavigate`, `useQuery/useQueryClient`, `toast`, `dailyReportApi`, `buildDailyReportData`, `generateDailyExcel`, `useStaffList`, `useIsDesktop`). 추가 import: `lucide-react` 의 `ChevronLeft / ChevronRight / Download / AlertTriangle` 4건.
- [ ] line 12~27 date utils (`todayKST` / `addDays` / `nowKSTHour`) 100% 보존
- [ ] line 64 `useIsDesktop` 분기 보존 (분기 함수 통합 안 함, OQ #5 default a)

**§3.2 state / refs 보존 (line 68~84, ≥3 entries)**
- [ ] line 68 `const [date, setDate] = useState<string>(todayKST())` — 날짜 형식 `YYYY-MM-DD` 보존
- [ ] line 69~75 `todayText / tomorrowText / notes / generating / genMonthly / saving / loaded` 7건 useState 보존
- [ ] line 83 `debounceRef = useRef<Record<string, ReturnType<typeof setTimeout>>>({})` 보존
- [ ] line 77~80 `today / mm / dd / isFutureDate / canForward` 계산 로직 보존

**§3.3 queries 보존 (line 86~98, ≥2 entries)**
- [ ] line 86~90 `queryData = useQuery({ queryKey: ['daily-report', date], retry: 1 })` 보존
- [ ] line 92~98 `queryNotes = useQuery({ queryKey: ['daily-notes', date], retry: 1, staleTime: 0, gcTime: 0 })` 보존

**§3.4 핸들러 보존 (line 150~265, ≥7 entries)**
- [ ] line 150~162 `debouncedSave` (2초 setTimeout) 보존 — `2000` ms 그대로
- [ ] line 164~166 `handleTodayChange / handleTomorrowChange / handleNotesChange` 보존
- [ ] line 169~181 `handleManualSave` 보존 — toast "저장되었습니다" / "저장 실패" verbatim
- [ ] line 184~190 `handleReset` 보존 — toast "초기화되었습니다" verbatim
- [ ] line 193 `goBack = () => setDate(d => addDays(d, -1))` 보존
- [ ] line 194 `goForward = () => { if (canForward) setDate(d => addDays(d, 1)) }` 보존 — canForward 가드 그대로
- [ ] line 197~209 `handleDailyDownload` 보존 — `generateDailyExcel('daily', y, m, { [d]: data }, d)` 그대로
- [ ] line 212~265 `handleMonthlyDownload` 보존 — `generateDailyExcel('monthly', y, m, dayDataMap, limitDay)` + lazy save 로직 그대로

**§3.5 useEffect — D1 로드/자동저장 분기 (line 110~147, ≥1 entries)**
- [ ] line 110~147 `useEffect` 100% 보존 — `prevDateRef` / `isAutoSaved` / lazy auto-save (`isPast || isAfter17`) 분기 그대로

**§3.6 캘리브레이션 로직 보존 (line 468~808, ≥5 entries)**
- [ ] `DAILY_CALIB_STEPS` 15 step 배열 100% 보존 (line 469~485) — key / label / color 각 항목 1 byte 변경 0
- [ ] `DAILY_CALIB_KEY = 'calib_daily_report'` 보존 (line 487, localStorage key)
- [ ] `FINGER_OFFSET = 60` 보존 (line 488 — 손가락 가림 보정)
- [ ] `LARGE_KEYS` / `overlayItems` 좌표 시스템 보존
- [ ] `loadDailyCalib` / `saveDailyCalib` 함수 보존 (line 494~499 — localStorage IO)
- [ ] `DailyPortraitPreview` (line 501~782) wrapper 만 손댐, 내부 좌표/오버레이/캘리브 100% 보존 (12-staff W8 lp[] 패턴 mirror)
- [ ] `DailyCalibMarker` 컴포넌트 (line 784~808) 100% 보존
- [ ] `<img src="/templates/preview/daily-1.png" />` (line 662) 경로 보존 (외부 의존 변경 0)

**§3.7 props 시그니처 보존 (verbatim, ≥2 entries)**
- [ ] `DailyPortraitPreview` 5 prop (line 434~440): `date / todayText / tomorrowText / notes / personnel` — 1 byte 변경 0
- [ ] `EditableCard` 9 prop (line 274~296): `label / field / value / onChange / onSave / onReset / saving / rows / placeholder` — 1 byte 변경 0

**§3.8 미래 날짜 spacer 보존 (line 394~399, ≥1 entries)**
- [ ] `canForward ? <button onClick={goForward}>›</button> : <span style={{ width: 28 }}/>` 분기 보존 — chevron 자체 숨김 + 28px spacer 자리 유지 (OQ #3 default 유지)

**§3.9 카피 verbatim 리스트 (≥18 string, frontmatter `<copy_verbatim>` 박스 그대로 인용)**
"일일 업무 일지" / "금일업무" / "명일업무" / "특이사항" / "오늘 특이사항을 입력하세요" / "초기화" / "저장" / "저장 중..." / "저장되었습니다" / "초기화되었습니다" / "인원현황" / "총원" / "현재원" / "비번" / "연차" / "반차" / "교육/훈련" / "주간근무자" / "당직근무자" / "데이터 불러오기 실패 — 다시 시도해 주세요" / "해당 날짜 데이터 없음" / "${Number(mm)}월${dd}일 방재업무일지 다운로드" / "일일업무일지(${mm}월) 다운로드" / "생성 중..." / "월별 생성 중..." / "수정 내용은 자동 저장됩니다 · 월별은 저장된 모든 날짜를 포함합니다" / "엑셀 생성 중 오류가 발생했습니다" / "월별 엑셀 생성 중 오류가 발생했습니다" / "인쇄 미리보기" / "위치 재설정" / "위치 설정" / "확인" / "취소" — 합계 ≥18 string verbatim.

**§3 합계**: 3 + 4 + 2 + 8 + 1 + 8 + 2 + 1 = **29 entries**. ≥20 충족.

---

### §4. 폰트 격상 매트릭스 (≥15 entries, source fontSize → 토큰)

표 형식 (≥15 row):

| # | 영역 | source line | source fontSize | 변환 후 토큰 | 변환 후 px |
|---|---|---|---|---|---|
| 1 | 헤더 타이틀 "일일 업무 일지" | 456 | 14px | text-title / `.page-title` | 18px |
| 2 | dateNav 날짜 "{date}" | 391 | 13px | `.date-display` | 14px |
| 3 | dateNav ‹/› 버튼 | 38 (navBtn) | 16px | `.date-nav-btn` | 16px (유지) |
| 4 | EditableCard 라벨 (금일/명일/특이사항) | 819 (label) | 13px | `.editable-card-label` | 16px |
| 5 | EditableCard 초기화/저장 버튼 | 55 (smallBtn) | 10px | `.editable-card-btn--reset/--save` | 12px |
| 6 | EditableCard textarea | 48 (textareaStyle) | 12px | `.editable-card-textarea` | 14px |
| 7 | 인원현황 카드 라벨 "인원현황" | 300 | 13px | `.summary-card-label` | 16px |
| 8 | 인원현황 카드 본문 ("총원 N · 현재원 M ·...") | 306 | 11px | `.summary-card-body` | 14px |
| 9 | 인원현황 카드 에러 ("데이터 불러오기 실패") | 304 | 11px | `.summary-card-error` | 14px |
| 10 | 인원현황 카드 empty ("해당 날짜 데이터 없음") | 316 | 11px | `.summary-card-empty` | 14px |
| 11 | 다운로드 버튼 (daily / monthly) | 330, 343, 359, 372 | 13px | `.download-btn` | 16px |
| 12 | 안내 줄 ("수정 내용은 자동 저장됩니다 ...") | 381 | 10px | `.page-footer-note` | 12px |
| 13 | 데스크톱 우측 라벨 "인쇄 미리보기" | 428 | 11px | `.desktop-portrait-print-label` | 12px |
| 14 | 캘리브 안내 바 step | (W6 sketch line 517) | 12px | `.daily-portrait-calib-bar-step` | 14px |
| 15 | 캘리브 안내 바 라벨 | (W6 sketch line 529) | 14px | `.daily-portrait-calib-bar-label` | 14px (유지) |
| 16 | 캘리브 좌표 표시 | (W6 sketch line 535) | 11px | `.daily-portrait-calib-bar-coord` | 14px |
| 17 | 캘리브 확인 버튼 "확인" | (W6 sketch line 543) | 13px | `.daily-portrait-calib-confirm` | 14px |
| 18 | 캘리브 취소 버튼 "취소" | (W6 sketch line 554) | 12px | `.daily-portrait-calib-cancel` | 12px (유지) |
| 19 | 위치 설정 버튼 "위치 설정/재설정" | 774 | 12px | `.daily-portrait-setup-btn` | 12px (유지) |
| 20 | 캘리브 마커 dot label | (W6 sketch line 613) | 10px | `.daily-portrait-overlay-item` | 11px (오버레이 UX 예외) |

**§4.1 9·10·11px 0건 룰 (필수 박제)**

design-system §1.1 "본문 폰트 최소 16px. 9·10·11px 사용 금지." 룰 박제. 본문/카드/CTA 영역 9·10·11px = **0건**. 단 예외 2종 (오버레이 UX 우선):
- `.daily-portrait-overlay-item` 11px (A4 인쇄 미리보기 안 좌표 라벨 — 인쇄 위치 시각화)
- `.daily-portrait-overlay-item--large` 12px (LARGE_KEYS modifier — date/today/tomorrow/notes 4 key 만)

**§4 합계**: 20 entries (≥15 충족) + 9·10·11px 0건 룰 명시.

---

### §5. 이모지/글리프 교체 매트릭스

**§5.1 ⬇ 글리프 (U+2B07) 4건 교체**

표 형식 (4 row):
| # | source line | source 카피 | 변환 후 |
|---|---|---|---|
| 1 | line 334 | `⬇ ${Number(mm)}월${dd}일 방재업무일지 다운로드` | `<Download size={16} className="download-btn-icon" />` + 카피 그대로 (글리프 제거) |
| 2 | line 347 | `⬇ 일일업무일지(${mm}월) 다운로드` | 동상 |
| 3 | line 363 | `⬇ ${Number(mm)}월${dd}일 방재업무일지 다운로드` | 동상 |
| 4 | line 376 | `⬇ 일일업무일지(${mm}월) 다운로드` | 동상 |

**§5.2 ⚠ 글리프 1건 교체**

| # | source line | source 카피 | 변환 후 |
|---|---|---|---|
| 5 | line 777 | `hasCalib ? '위치 재설정' : '⚠ 위치 설정'` | `hasCalib ? '위치 재설정' : (<><AlertTriangle size={14} /> 위치 설정</>)` (OQ #7 default a) |

**§5.3 가운뎃점 `·` dot span 치환 (≥6건)**

표 형식 (인원현황 카드 line 305~313 + 안내 줄 line 382):
| # | source line | source 패턴 | 변환 후 |
|---|---|---|---|
| 6 | line 307 | ` · 현재원 ${preview.personnel.present}` | `<span className="dot-meta" /> 현재원 ${preview.personnel.present}` |
| 7 | line 308 | ` · 비번 ${preview.personnel.offDuty}` | dot-meta span 치환 |
| 8 | line 309 | ` · 연차 ${preview.personnel.onLeave.join(', ')}` | dot-meta span 치환 |
| 9 | line 310 | ` · 반차 ${preview.personnel.halfLeave.join(', ')}` | dot-meta span 치환 |
| 10 | line 311 | ` · 교육/훈련 ${preview.personnel.training.join(', ')}` | dot-meta span 치환 |
| 11 | line 312 | ` · 주간근무자 ${preview.personnel.dayShift.join(', ')}` | dot-meta span 치환 |
| 12 | line 313 | ` · 당직근무자 ${preview.personnel.onDuty}` | dot-meta span 치환 |
| 13 | line 382 | `수정 내용은 자동 저장됩니다 · 월별은 저장된 모든 날짜를 포함합니다` | `자동 저장됩니다 <span className="dot-meta" /> 월별은 ...` |

**§5.4 lucide-react import (필수 박제)**

```typescript
import { ChevronLeft, ChevronRight, Download, AlertTriangle } from 'lucide-react'
```

**§5.5 raw `<svg>` 폐기 (line 452~454)**

| # | source | 변환 후 |
|---|---|---|
| 14 | line 452~454 raw `<svg>` 안 path d="M15 19l-7-7 7-7" | `<ChevronLeft size={15} />` (design-system §7.1) |
| 15 | line 390 navBtn 안 텍스트 `‹` | `<ChevronLeft size={16} />` |
| 16 | line 396 navBtn 안 텍스트 `›` | `<ChevronRight size={16} />` |

**§5 합계**: ⬇ 4건 + ⚠ 1건 + dot-meta ≥7건 + lucide import 1건 + raw svg/glyph 3건 교체 = **16+ 항목**.

---

### §6. negative gate (TSX 변환 후 grep 검증, ≥7 entries)

표 형식 (≥7 row):

| # | gate | 검증 명령 | 기대값 |
|---|---|---|---|
| 1 | 이모지 0건 | `grep -cE '[⬇⚠🎯⬆⬅➡✅]' cha-bio-safety/src/pages/DailyReportPage.tsx` | =0 |
| 2 | linear-gradient 0건 | `grep -c 'linear-gradient(' cha-bio-safety/src/pages/DailyReportPage.tsx` | =0 |
| 3 | status- prefix 0건 (잘못된 패턴) | `grep -cE '\\b(bg\|text)-status-' cha-bio-safety/src/pages/DailyReportPage.tsx` | =0 (.bg-safe-bar 패턴 사용 또는 components.css var(--status-*) 토큰 사용) |
| 4 | w-8 / h-8 0건 (48px 함정) | `grep -cE '\\b(w-8\|h-8)\\b' cha-bio-safety/src/pages/DailyReportPage.tsx` | =0 |
| 5 | fontSize 9/10/11px 0건 (본문 영역) | `grep -cE 'fontSize: (9\|10\|11)\\b' cha-bio-safety/src/pages/DailyReportPage.tsx` | =0 (캘리브 overlayItem 예외만 인라인 유지 OK) |
| 6 | var(--bg) / var(--bg2) / var(--bg3) 옛 토큰 0건 (본문) | `grep -cE 'var\\(--(bg\|bg2\|bg3\|bd\|t1\|t2\|t3)\\)' cha-bio-safety/src/pages/DailyReportPage.tsx` | 캘리브 내부 예외만 (DailyPortraitPreview 내부 인라인 토큰은 OQ #4 default a 로 보존) |
| 7 | 인라인 style 대거 제거 | `grep -c 'style={{' cha-bio-safety/src/pages/DailyReportPage.tsx` | 큰 폭 감소 (캘리브 내부 + dynamic 위치 좌표 예외) |
| 8 | `npx tsc --noEmit` PASS | `cd cha-bio-safety && npx tsc --noEmit` | exit 0, 0 error |
| 9 | vite build PASS | `cd cha-bio-safety && npm run build` | exit 0, dist/ 생성 |
| 10 | DailyReportPage chunk size 합리적 | `ls -lh cha-bio-safety/dist/assets/*DailyReportPage*.js` | chunk 크기 변화 ±20% 이내 |

**§6 합계**: 10 gate (≥7 충족).

---

### §7. 메모리 룰 박제 (≥10 unique slug)

각 slug — 1줄 요약 + How (15-daily-report 컨텍스트):

1. **`feedback_design_sketch_first.md`** — spacing/sizing 도 sketch 시안 먼저. → W7 변환 wave 진입 전 W2~W6 sketch 5개 모두 컨펌 받은 상태에서만 시작.
2. **`feedback_redesign_sketch_rule_enforcement.md`** — §6.2 negative rule (위험 임계치 아닌 카드 status 색 금지). → EditableCard / SummaryCard 는 정보 카드 → `bg-surface-raised border-default` 만 (status 색 X). 다운로드 daily 만 CTA → `bg-safe-bar` solid OK.
3. **`feedback_sketch_realistic_data.md`** — 표시 분기/라벨 룰 verbatim. → 카피 §3.9 전체 ≥18 string verbatim, 카피 1 byte 변경 0. canForward 분기 (line 394~399) 보존.
4. **`feedback_planner_prompt_sketch_verbatim.md`** — sketch CSS 토큰 grep verbatim 인용. → 본 W7 markdown §1.1~§1.5 의 모든 CSS fence 가 `grep -nE '^\\.[a-z][a-z0-9-]+\\s*\\{' sketch-wave-N-*.html` 결과 박제. SW1 executor 는 fence 만 복사 (추측 X).
5. **`feedback_tailwind_token_class_pattern.md`** — `bg-safe-bar` (status- prefix 없음) / lucide `<Icon size={N} />` prop. → CSS class 는 `var(--status-safe-bar)` CSS 변수 직접 사용 (components.css 안), Tailwind class 는 `bg-safe-bar` 형식 OK. lucide = `<Download size={16} />` (className `w-4 h-4` 금지).
6. **`feedback_tailwind_w8_h8_is_48px.md`** — `w-8` = 48px (기본 32 아님). → `.back-btn` 34px / `.date-nav-btn` 28px / `.date-nav-spacer` 28px 모두 CSS 명시 px 사용 (Tailwind w-N utility 안 씀).
7. **`feedback_text_caption_leading_none.md`** — text-caption lh:1.5 (18px) 가 작은 컨테이너 안 시각 패딩. → `.date-display` / `.date-nav-btn` / `.editable-card-btn--reset` / `.editable-card-btn--save` / `.daily-portrait-calib-confirm` / `.daily-portrait-calib-cancel` 모두 `line-height: 1` 명시.
8. **`feedback_tsx_wave_emoji_dot_gap.md`** — sketch negative gate (이모지 0) + dot span 추가 markup verify. → §5 매트릭스 ⬇ 4건 + ⚠ 1건 + dot-meta ≥7건 + raw svg 3건 교체 verify gate. SW4 wave 가 grep gate 강제.
9. **`feedback_tsx_wave_stat_card_drift.md`** — executor 가 source outline 보존, sketch 새 패턴 누락 가능. → 15-daily-report 는 Stat Card 없음, design-system §6.2 미적용 명시 (드리프트 방지). EditableCard label 13→16 / textarea 12→14 / smallBtn 10→12 격상은 §4 매트릭스에 verbatim 박제.
10. **`feedback_avoid_premature_confirmation.md`** — "거의 일치" 자신감 표현 금지. → SW4 verify gate 통과해도 "변환 완벽" 표현 금지. 결과 + grep gate 출력 보여주고 사용자 판단.
11. **(보너스) `feedback_cbc7119_design_never_wrangler.md`** — 디자인 wave 중 wrangler 명령 절대 X. → 본 wave 산출물은 markdown 1개 + atomic commit 만. wrangler 0건 / `npm run deploy` 0건. main push 시 GitHub Actions 가 cbc7119-preview 자동 배포.
12. **(보너스) `feedback_check_branch_before_edit.md`** — main 단일-trunk 운영, dirty 면 사용자 컨펌. → 본 wave 진입 전 `git status` + `git branch --show-current` 확인. 현재 `redesign/15-daily-report` 브랜치인 것 검증.

**§7 합계**: 12 unique slug (≥10 충족).

---

### §8. W1~W6 OQ default LOCKED 매트릭스 (28건 LOCKED, 표 형식)

표 형식 (28 row, 모두 "LOCKED" 상태):

| Wave | OQ ID | LOCKED 결정 (default 답) | 적용 위치 (line / class) |
|---|---|---|---|
| W1 | #1 LOCKED | 다운로드 버튼 그라데이션 → `bg-safe-bar` solid 통일 (그라데이션 폐기) | line 328, 357 → `.download-btn--daily` solid |
| W1 | #2 LOCKED | 인원현황 = 단순 정보 카드 (Progress / Stat Card 아님) | line 298~318 → `.summary-card` |
| W1 | #3 LOCKED | 미래 날짜 비활성 UX — chevron 자체 숨김 + 28px spacer (소스 그대로) | line 394~399 → `.date-nav-spacer` |
| W1 | #4 LOCKED | DailyPortraitPreview wrapper 만 변환, 내부 캘리브/오버레이 100% 보존 | line 501~782 → `.daily-portrait-wrapper` 외곽만 |
| W1 | #5 LOCKED | 모바일/데스크톱 분기 유지 (`useIsDesktop` 분기 보존, lg:* 단일 함수 X) | line 64, 404 → `.desktop-layout` |
| W1 | #6 LOCKED | 안내 줄 "수정 내용은 자동 저장됩니다 ..." 유지 (카피 verbatim, 시각만 변경) | line 381~383 → `.page-footer-note` |
| W1 | #7 LOCKED | 위치 설정 버튼 ⚠ 글리프 → lucide `<AlertTriangle size={14} />` 교체 | line 777 → `<AlertTriangle size={14} />` |
| W2 | #1 LOCKED | dateNav 안 calendar 진입점 (날짜 picker) 미추가 (소스에 없음) | line 387~401 — picker UI 0 |
| W2 | #2 LOCKED | 오늘 날짜 시각 강조 ("오늘" 배지 / accent text) 미적용 (소스 그대로) | `.date-display` 단순 표시 |
| W2 | #3 LOCKED | 날짜 포맷 YYYY-MM-DD 유지 (소스 line 391 verbatim, 다른 페이지 일관) | `{date}` state YYYY-MM-DD |
| W2 | #4 LOCKED | dateNav 위치 = 헤더 안 유지 (모바일 1줄 컴팩트) | line 457 — header 안 |
| W3 | #1 LOCKED | EditableCard 신규 7 class — `.editable-card / -head / -label / -actions / -btn--reset / -btn--save / -textarea` | line 810~840 |
| W3 | #2 LOCKED | SummaryCard 신규 3 class — `.summary-card / -label / -body` (helper 4 보조) | line 298~318 |
| W3 | #3 LOCKED | 라벨 13 → 16 / 버튼 10 → 12 / textarea 12 → 14 노안 격상 (§4 매트릭스) | §4 row 4, 5, 6 |
| W3 | #4 LOCKED | EditableCard 단일 컴포넌트 3 카드 재사용 (금일/명일/특이사항) — 분리 X | line 274, 282, 290 |
| W4 | #1 LOCKED | 다운로드 daily 버튼 = `.download-btn--daily` solid (그라데이션 폐기, OQ W1 #1 일관) | line 328, 357 |
| W4 | #2 LOCKED | 다운로드 monthly 버튼 = `.download-btn--monthly` (surface-sunken + border-strong, 보조 위계) | line 340, 369 |
| W4 | #3 LOCKED | 안내 줄 padding "8px 0 20px" (source line 381 verbatim, 14-reports 8px 16px 20px 와 좌우 다름) | `.page-footer-note` |
| W4 | #4 LOCKED | ⬇ 글리프 4건 → lucide `<Download size={16} />` 교체 (§5.1) | line 334, 347, 363, 376 |
| W5 | #1 LOCKED | 데스크톱 좌측 padding "24px 32px" 유지 (소스 line 408 verbatim) | `.desktop-edit-panel` |
| W5 | #2 LOCKED | 데스크톱 dateNav 정렬 = justifyContent flex-end + marginBottom 20 (소스 line 410) | `.desktop-edit-panel-header` |
| W5 | #3 LOCKED | 우측 wrapper aspect-ratio "210 / 297" + height 100% + flexShrink 0 + borderLeft (소스 line 417~420) | `.desktop-portrait-wrapper` |
| W5 | #4 LOCKED | "인쇄 미리보기" 라벨 = position absolute top 8 + uppercase + pointerEvents none + zIndex 5 (소스 line 426~430) | `.desktop-portrait-print-label` |
| W5 | #5 LOCKED | "인쇄 미리보기" 라벨 fontSize 11 → 12 노안 격상 (마이그레이션 §4.2) | §4 row 13 |
| W6 | #1 LOCKED | DailyPortraitPreview 내부 캘리브레이션 100% 보존 (12-staff W8 lp[] 패턴 mirror, OQ W1 #4 일관) | line 501~782 외곽 wrapper 만 |
| W6 | #2 LOCKED | 캘리브 안내 바 step 12 → 14 / 좌표 11 → 14 / 확인 13 → 14 노안 격상 | §4 row 14, 16, 17 |
| W6 | #3 LOCKED | 위치 설정 버튼 hasCalib 분기 — `.daily-portrait-setup-btn--ready` (덜 강조) / `--missing` (rgba(239,68,68,0.9) 주의 환기) | line 766~779 |
| W6 | #4 LOCKED | `<img src="/templates/preview/daily-1.png" />` 경로 변경 0 (외부 의존 보존) | line 662 |

**§8 합계**: W1 (7) + W2 (4) + W3 (4) + W4 (4) + W5 (5) + W6 (4) = **28건 LOCKED**.

---

### §9. SW1~SW4 변환 wave 분배 plan

표 형식 (4 row, ≥4 충족):

| Sub-wave | Scope | 산출물 | Verify gate (요약) |
|---|---|---|---|
| **SW1** | `cha-bio-safety/src/styles/components.css` 끝부분에 신규 54 class 추가 (W2 4 + W3 14 + W4 8 + W5 6 + W6 22). 14-reports 6 class 손대지 않음. `@keyframes blink` 추가. | components.css diff 만 (DailyReportPage.tsx 손대지 않음) | `grep -c '^\\.date-nav' components.css ≥3` / `grep -c '^\\.editable-card' components.css ≥7` / `grep -c '^\\.download-btn' components.css ≥4` / `grep -c '^\\.desktop-' components.css ≥6` / `grep -c '^\\.daily-portrait-' components.css ≥9` / `npx tsc --noEmit` PASS / `vite build` PASS |
| **SW2** | `cha-bio-safety/src/pages/DailyReportPage.tsx` 모바일 영역 변환 (line 270~465). 비즈 로직 §3 보존, EditableCard / SummaryCard / 다운로드 버튼 / 모바일 헤더 / dateNav 모두 class 기반 markup 으로 재작성. 캘리브레이션 컴포넌트 (line 468~840) 손대지 않음. | DailyReportPage.tsx 모바일 영역 diff | §6 gate 1~5 (이모지 0 / linear-gradient 0 / w-8 h-8 0 / fontSize 9-11 0 / status- prefix 0) / 카피 verbatim grep (≥18 string) / `useState × 7` / `useQuery × 2` / handler 7건 보존 |
| **SW3** | `cha-bio-safety/src/pages/DailyReportPage.tsx` 데스크톱 영역 변환 (line 403~444) + DailyPortraitPreview wrapper 변환 (line 501~782 중 외곽만). 캘리브레이션 내부 좌표/오버레이/마커 로직 100% 보존. `<img>` src 그대로. | DailyReportPage.tsx 데스크톱 + wrapper diff | `.desktop-layout` / `.desktop-edit-panel` / `.desktop-portrait-wrapper` 사용 / 캘리브 15 step 변경 0 (`grep -c "DAILY_CALIB_STEPS" DailyReportPage.tsx ≥1`) / FINGER_OFFSET 60 보존 / 5 prop 시그니처 verbatim |
| **SW4** | verify gate 전체 + lucide import + atomic commit. tsc + vite build + chunk size 측정. cbc7119-preview 자동 배포 후 모바일/데스크톱 양 렌더 시각 검수 사용자 컨펌 단계. | git diff 검토 + 커밋 + push (자동 배포 trigger) | §6 gate 1~10 모두 PASS / 모바일 393px + 데스크톱 1920px frame 양쪽 시각 검수 / 9·10·11px 0건 (캘리브 예외 OK) |

**§9.1 atomic commit 룰**

각 sub-wave 1 commit. 메시지 형식:
- `feat(15-daily-report): SW1 components.css +54 class (W2~W6 sketch verbatim)`
- `refactor(15-daily-report): SW2 DailyReportPage.tsx 모바일 영역 class 기반 markup`
- `refactor(15-daily-report): SW3 DailyReportPage.tsx 데스크톱 + DailyPortraitPreview wrapper`
- `chore(15-daily-report): SW4 verify gate PASS + 시각 검수 컨펌`

---

### §10. 자체 verify gate (markdown 작성 후 검증, ≥10 gate)

표 형식 (≥10 row):

| # | gate | 검증 명령 | 기대값 |
|---|---|---|---|
| 1 | §1~§10 헤더 ≥10 | `grep -cE '^### §[1-9]\|^### §10' wave-7-tsx-conversion-checklist.md` | ≥10 |
| 2 | sub-wave 분배 표 ≥4 row | `grep -cE '^\| \*\*SW[1-4]\*\*' wave-7-tsx-conversion-checklist.md` | ≥4 |
| 3 | 신규 class 명단 ≥51 entries | `grep -cE '^\\.[a-z][a-z0-9-]+\\s*\\{' wave-7-tsx-conversion-checklist.md` | ≥51 |
| 4 | 비즈 로직 보존 ≥20 entries | `grep -cE '^- \\[ \\] line ' wave-7-tsx-conversion-checklist.md` | ≥20 |
| 5 | 폰트 격상 ≥15 entries | `grep -cE '^\| [0-9]+ \| .*\| [0-9]+ \| [0-9]+px' wave-7-tsx-conversion-checklist.md` 또는 §4 표 row count | ≥15 |
| 6 | 이모지 교체 ≥3 entries (≥1 카테고리당) | `grep -cE '^\| [0-9]+ \| line ' wave-7-tsx-conversion-checklist.md` (§5 표) | ≥3 (실제 16+) |
| 7 | 메모리 룰 unique slug ≥10 | `grep -oE 'feedback_[a-z_]+\\.md' wave-7-tsx-conversion-checklist.md \| sort -u \| wc -l` | ≥10 |
| 8 | W1~W6 OQ default 매트릭스 = 28건 LOCKED | `grep -c '\\bLOCKED\\b' wave-7-tsx-conversion-checklist.md` (§8 표) | ≥28 |
| 9 | negative gate ≥7 entries | `grep -cE '^\| [0-9]+ \| [^|]+\| `grep' wave-7-tsx-conversion-checklist.md` (§6 표) | ≥7 (실제 10) |
| 10 | 이모지 0건 (markdown 본문, §5 source line 인용 예외) | `grep -E '[🎯⬆⬅➡✅✨🔥]' wave-7-tsx-conversion-checklist.md` | 0 매치 |
| 11 | wrangler 0건 (마스킹 외) | `grep -c '\\bwrangler\\b' wave-7-tsx-conversion-checklist.md` | =0 또는 §6/§negative_gates "wrangler 0건" 명시만 |
| 12 | npm run deploy 0건 (마스킹 외) | `grep -c 'npm run deploy' wave-7-tsx-conversion-checklist.md` | =0 또는 §6/§negative_gates "npm run deploy 0건" 명시만 |
| 13 | src/** 변경 0 (file 본 자체) | `git diff --name-only HEAD -- cha-bio-safety/src` | 빈 출력 |
| 14 | components.css 변경 0 (file 본 자체) | `git diff --name-only HEAD -- cha-bio-safety/src/styles/components.css` | 빈 출력 |
| 15 | 라인 수 ≥700 | `wc -l cha-bio-safety/docs/redesign-context/15-daily-report/wave-7-tsx-conversion-checklist.md` | ≥700 |

**§10 합계**: 15 gate (≥10 충족).

---

**최종 negative_gates re-statement** (markdown 본문 끝에 박제):

- markdown 본문은 sketch HTML 아님 (`<html>` / `<body>` / `<style>` 블록 0건)
- 이모지 0건 (Unicode 1F300~1FAFF / 2600~26FF / 2700~27BF 범위, §5 source line 인용 시 ASCII 또는 backtick 표기 예외)
- wrangler 명령 0건 (CLAUDE.local.md 룰)
- `npm run deploy` 0건 (CLAUDE.local.md 룰)
- 다른 페이지 (13/14/02/06) 영향 0
- `cha-bio-safety/src/**` 변경 0
- `cha-bio-safety/src/styles/components.css` 변경 0 (SW1 변환 wave 에서만 추가)
- `cha-bio-safety/src/App.tsx` 변경 0

---

**작업 순서 (executor 실행 절차)**:

1. **사전 검증** (수정 전):
   - `git branch --show-current` = `redesign/15-daily-report` 확인
   - `git status` 결과 clean 확인 (또는 untracked 만)
   - `ls cha-bio-safety/docs/redesign-context/15-daily-report/wave-7-tsx-conversion-checklist.md` = 파일 없음 확인 (신규 생성)

2. **markdown 작성** (Write tool 1회 호출, 본 task 의 action 안 §1~§10 + frontmatter + negative_gates + 작업 순서 메모 모두 포함):
   - 위 §1~§10 모든 섹션을 markdown 으로 작성
   - 모든 CSS fence 는 sketch HTML 에서 grep verbatim 추출 결과 (추측 X)
   - 모든 line ref 는 DailyReportPage.tsx 실측 line number (840 line)
   - 14-reports W7 mirror 패턴 (`cha-bio-safety/docs/redesign-context/14-reports/wave-7-tsx-conversion-checklist.md`) 구조 따름

3. **자체 verify gate** (§10 의 15 gate 실행):
   - `wc -l wave-7-tsx-conversion-checklist.md` ≥700 확인
   - `grep -cE '^### §' wave-7-tsx-conversion-checklist.md` ≥10 확인
   - `grep -oE 'feedback_[a-z_]+\\.md' wave-7-tsx-conversion-checklist.md | sort -u | wc -l` ≥10 확인
   - `grep -c '\\bLOCKED\\b' wave-7-tsx-conversion-checklist.md` ≥28 확인
   - `grep -cE '^\\.[a-z][a-z0-9-]+\\s*\\{' wave-7-tsx-conversion-checklist.md` ≥51 확인
   - `grep -E '[🎯⬆⬅➡✅✨🔥]' wave-7-tsx-conversion-checklist.md` = 0 매치 확인
   - `git diff --name-only HEAD -- cha-bio-safety/src` = 빈 출력 확인
   - `git diff --name-only HEAD -- cha-bio-safety/src/styles/components.css` = 빈 출력 확인

4. **사용자 보고**:
   - "wave-7-tsx-conversion-checklist.md 신규 생성 완료, ≥700 lines, §1~§10 모두 작성, verify gate 15건 PASS"
   - cbc7119-design 워크트리 룰 준수 — src/** 변경 0, components.css 변경 0, wrangler 0, npm run deploy 0
   - "다음 단계: SW1~SW4 별도 quick task 로 TSX 변환 진행 (사용자 컨펌 후)"
  </action>
  <verify>
    <automated>
      bash -c '
      F=cha-bio-safety/docs/redesign-context/15-daily-report/wave-7-tsx-conversion-checklist.md;
      if [ ! -f "$F" ]; then echo FAIL_FILE_MISSING; exit 1; fi;
      LINES=$(wc -l < "$F");
      if [ "$LINES" -lt 700 ]; then echo "FAIL_LINES=$LINES (expected >= 700)"; exit 1; fi;
      SEC=$(grep -cE "^### §" "$F");
      if [ "$SEC" -lt 10 ]; then echo "FAIL_SECTIONS=$SEC (expected >= 10)"; exit 1; fi;
      CLS=$(grep -cE "^\.[a-z][a-z0-9-]+\s*\{" "$F");
      if [ "$CLS" -lt 51 ]; then echo "FAIL_CLASSES=$CLS (expected >= 51)"; exit 1; fi;
      SLUG=$(grep -oE "feedback_[a-z_]+\.md" "$F" | sort -u | wc -l);
      if [ "$SLUG" -lt 10 ]; then echo "FAIL_SLUGS=$SLUG (expected >= 10)"; exit 1; fi;
      LOCK=$(grep -c "\bLOCKED\b" "$F");
      if [ "$LOCK" -lt 28 ]; then echo "FAIL_LOCKED=$LOCK (expected >= 28)"; exit 1; fi;
      SRC_DIFF=$(git diff --name-only HEAD -- cha-bio-safety/src | wc -l);
      if [ "$SRC_DIFF" -ne 0 ]; then echo "FAIL_SRC_CHANGED=$SRC_DIFF"; exit 1; fi;
      CSS_DIFF=$(git diff --name-only HEAD -- cha-bio-safety/src/styles/components.css | wc -l);
      if [ "$CSS_DIFF" -ne 0 ]; then echo "FAIL_CSS_CHANGED=$CSS_DIFF"; exit 1; fi;
      echo "PASS lines=$LINES sections=$SEC classes=$CLS slugs=$SLUG locked=$LOCK src_diff=$SRC_DIFF css_diff=$CSS_DIFF";
      '
    </automated>
  </verify>
  <done>
    - wave-7-tsx-conversion-checklist.md 신규 생성, ≥700 lines
    - §1~§10 모든 헤더 채워짐 (빈 섹션 0)
    - 신규 CSS class 정의 ≥51건 sketch HTML grep verbatim 추출 (`^\\.[a-z]...{` 패턴)
    - DailyReportPage.tsx 비즈 로직 보존 항목 ≥20 entries (line ref 명시)
    - 폰트 격상 매트릭스 ≥15 entries + 9·10·11px 0건 룰 박제
    - 이모지 교체 매트릭스: ⬇ 4건 (line 334/347/363/376) + ⚠ 1건 (line 777) + dot-meta ≥6건 (line 307~313 + line 382) + lucide import 1건
    - W1~W6 OQ default LOCKED 매트릭스 28건 (W1 7 + W2 4 + W3 4 + W4 4 + W5 5 + W6 4)
    - SW1~SW4 변환 wave 분배 plan 표 4 row + atomic commit 메시지 4건 박제
    - negative gate ≥7 entries (실제 10)
    - 자체 verify gate ≥10 entries (실제 15)
    - 메모리 룰 unique slug ≥10 (실제 12)
    - 이모지 0건 / wrangler 0건 / `npm run deploy` 0건 / src/** 변경 0 / components.css 변경 0 / App.tsx 변경 0
    - 14-reports `wave-7-tsx-conversion-checklist.md` mirror 패턴 따름
  </done>
</task>

</tasks>

<verification>
**Phase-level checks** (task 1 완료 후):

1. **File existence**: `ls cha-bio-safety/docs/redesign-context/15-daily-report/wave-7-tsx-conversion-checklist.md` → 존재
2. **Line count**: `wc -l cha-bio-safety/docs/redesign-context/15-daily-report/wave-7-tsx-conversion-checklist.md` → ≥700
3. **Section headers**: `grep -cE '^### §' wave-7-tsx-conversion-checklist.md` → ≥10
4. **CSS class definitions**: `grep -cE '^\\.[a-z][a-z0-9-]+\\s*\\{' wave-7-tsx-conversion-checklist.md` → ≥51
5. **Memory rule slugs**: `grep -oE 'feedback_[a-z_]+\\.md' wave-7-tsx-conversion-checklist.md | sort -u | wc -l` → ≥10
6. **OQ LOCKED count**: `grep -c '\\bLOCKED\\b' wave-7-tsx-conversion-checklist.md` → ≥28
7. **No emoji**: `grep -cE '[🎯⬆⬅➡✅✨🔥]' wave-7-tsx-conversion-checklist.md` → =0
8. **No src/** changes**: `git diff --name-only HEAD -- cha-bio-safety/src` → 빈 출력
9. **No components.css changes**: `git diff --name-only HEAD -- cha-bio-safety/src/styles/components.css` → 빈 출력
10. **No App.tsx changes**: `git diff --name-only HEAD -- cha-bio-safety/src/App.tsx` → 빈 출력
11. **No wrangler**: `grep -c 'wrangler' wave-7-tsx-conversion-checklist.md` → minimal (negative_gates 박제 1~2건 외 0)
12. **No npm run deploy**: `grep -c 'npm run deploy' wave-7-tsx-conversion-checklist.md` → minimal (negative_gates 박제 1~2건 외 0)

**Negative checks (cross-page isolation):**
- `git status` 결과에 cha-bio-safety/src/ 변경 0
- `git status` 결과에 13-schedule / 14-reports / 02 / 06 폴더 변경 0
- W7 wave 자체는 sketch 단계 마지막 — OQ 없음 (모든 OQ 는 W1~W6 에서 컨펌 완료)
</verification>

<success_criteria>
Quick task 완료 조건:

1. 신규 markdown 1개 생성 (`cha-bio-safety/docs/redesign-context/15-daily-report/wave-7-tsx-conversion-checklist.md`)
2. ≥700 lines, §1~§10 모든 섹션 채움
3. W2~W6 sketch 신규 .class 정의 ≥51건 verbatim 인용
4. DailyReportPage.tsx 비즈 로직 보존 ≥20 entries
5. 폰트 격상 매트릭스 ≥15 entries + 9·10·11px 0건 룰 명시
6. 메모리 룰 unique slug ≥10
7. W1~W6 OQ default LOCKED 매트릭스 28건
8. SW1~SW4 변환 wave 분배 plan 표 ≥4 row
9. negative gate ≥7 + 자체 verify gate ≥10
10. cbc7119-design 워크트리 룰 준수 — src/** 변경 0, components.css 변경 0, wrangler 0, npm run deploy 0
11. 다른 페이지 (13/14/02/06) 영향 0
12. 14-reports `wave-7-tsx-conversion-checklist.md` mirror 패턴 따름
</success_criteria>

<output>
**Quick task 완료 후**:

본 task 는 atomic 1-commit 단위. 사용자가 `/gsd:quick` 으로 시작한 wave 이므로 commit 은 quick workflow 의 commit 단계에서 처리. plan 작성자 (현 phase) 는 plan 만 작성하고 종료.

SUMMARY 는 quick workflow 의 후속 단계 (execute → commit) 에서 자동 생성.

**다음 단계 (W7 markdown 작성 후)**:
- W7 사용자 컨펌 + main 머지 → cbc7119-preview 자동 배포 (markdown 만이라 시각 변화 없음)
- 별도 quick task 로 SW1~SW4 (TSX 변환) 진행:
  - SW1: components.css 신규 54 class 추가
  - SW2: DailyReportPage.tsx 모바일 영역 변환
  - SW3: DailyReportPage.tsx 데스크톱 + DailyPortraitPreview wrapper 변환
  - SW4: verify gate PASS + 시각 검수 사용자 컨펌
</output>
