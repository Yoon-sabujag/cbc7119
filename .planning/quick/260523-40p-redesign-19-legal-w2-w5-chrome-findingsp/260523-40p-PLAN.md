---
phase: quick-260523-40p
plan: 01
type: execute
wave: 1
depends_on: []
quick_id: 260523-40p
branch: redesign/19-legal
date: 2026-05-23
autonomous: true
files_modified:
  - cha-bio-safety/docs/redesign-context/19-legal/sketch-wave-2-chrome.html
  - cha-bio-safety/docs/redesign-context/19-legal/sketch-wave-3-round-card.html
  - cha-bio-safety/docs/redesign-context/19-legal/sketch-wave-4-findings-panel.html
  - cha-bio-safety/docs/redesign-context/19-legal/wave-5-tsx-conversion-checklist.md
requirements:
  - redesign-19-legal-w2-chrome
  - redesign-19-legal-w3-round-card
  - redesign-19-legal-w4-findings-panel
  - redesign-19-legal-w5-tsx-checklist
must_haves:
  truths:
    - "W2~W4 sketch HTML 3개 + W5 checklist markdown 1개, 총 4 파일이 19-legal/ 직속 평면 배치로 생성된다 (sketch/ 서브폴더 절대 X — 28-splash 2q6 + 17-annual-plan 0j3 + 16-workshift + 27-login + 13-schedule + 14-reports + 23-education o7b 평면 패턴 mirror)"
    - "LegalPage.tsx (571 lines) + App.tsx + 외부 6 파일 (PhotoGrid / PhotoSourceModal / FindingFormSheet / useMultiPhotoUpload / findingDownload / api) + sub-route 페이지 2개 (LegalFindingsPage + LegalFindingDetailPage) 총 10 src 파일 모두 코드 변경 0건 — sketch + checklist 만 산출 (verify gate 마지막 task 에서 git diff --name-only HEAD~4 HEAD -- cha-bio-safety/src/pages/LegalPage.tsx + App.tsx 모두 empty)"
    - "negative gate 3 sketch + 1 checklist 모두 통과 (이모지 0 / 9·10·11px 0 / status- prefix 0 / w-8 h-8 0 / 옛 alias 토큰 0)"
    - "linear-gradient negative gate — W2/W3/W5 = 0 / W4 만 OQ #4 LOCKED 예외 anchor (조치 완료 메인 CTA `linear-gradient(135deg, #1d4ed8, #0ea5e9)` ≥3 박제, 작은 도구 button 은 solid bg-accent 유지)"
    - "OQ #1~#5 LOCKED 결정 5건 모두 4 파일 안에 verbatim 반영 — #1 모바일 헤더 raised + border-border-default / #2 accentColor 4분기 + ResultBadge + finding 칩 status 토큰 매핑 (status- prefix 없음) / #3 §1.1 9·10·11 fontSize 12 격상 + leading-none / #4 조치 완료 CTA 그라데이션 (작은 도구 solid) + 빈/오류 아이콘 무 / #5 Lucide ChevronLeft size={20} + Camera size={18} + Loader2 animate-spin 교체 + back button 44x44 격상"
    - "비즈 시그니처 anchor 17건 (useQuery 4종 ['legal-rounds', year] / ['legal-round', roundId] / ['legal-findings', roundId] / ['legal-finding', roundId, findingId] + useMutation resolveMutation + legalApi 7종 (list/get/getFindings/updateResult/deleteFinding/getFinding/resolveFinding) + accentColor 4분기 + ResultBadge map 4 라벨 + filterRounds 3분기 + TABS key/label mismatch + sorted open-first + handleRoundClick isDesktop 분기 + role admin 도구 분기 + useMultiPhotoUpload 5장 + buildMetaTxt + fflate ZIP 파일명 + @keyframes blink (.6/.3) + spin + fmtDate/fmtDateTime + KVRow + toast 11종 + 빈/오류 카피 다수 + 데스크톱 3분할 500+500+flex 1 + 모바일 자체 헤더 h 48) 모두 4 파일 안에 verbatim 명시 (W5 §2 + W2~W4 sketch rules 박스 박제)"
    - "OQ #4 LOCKED W4 그라데이션 예외 — W4 sketch 안 'linear-gradient(135deg, #1d4ed8, #0ea5e9)' ≥3 (조치 완료 button 4 frame 중 ≥3) + 다른 W2/W3/W5 는 linear-gradient 0건"
    - "각 task 마다 atomic git commit 1개씩 — 총 4 commit (executor cherry-pick 사고 6회 precedent 회피용 commit 명령 verbatim 박제, 28-splash 2q6 + 23-education o7b 패턴 동일)"
    - "평면 배치 — 4 파일 모두 cha-bio-safety/docs/redesign-context/19-legal/ 직속 (sketch/ 서브폴더 절대 X)"
    - "W5 checklist 12 섹션 (§1 변환 범위 / §2 비즈 anchor 17건 보존 / §3 변환 매핑 영역 1~4 verbatim / §4 OQ LOCKED 5건 반영 / §5 negative gate / §6 positive gate / §7 build/tsc / §8 자체 verify / §9 Tailwind cheatsheet / §10 비즈 보존 체크박스 / §11 메모리 룰 inline 12+ / §12 다음 단계)"
  artifacts:
    - path: "cha-bio-safety/docs/redesign-context/19-legal/sketch-wave-2-chrome.html"
      provides: "모바일 자체 헤더 (Lucide ChevronLeft + 44x44 격상 + '소방 점검 관리' + spacer) + 데스크톱 3분할 outline (좌 500 + 중 500 + 우 flex 1 borderRight 1px) + 빈/로딩/오류 5 state 매트릭스 (4 frame: 다크 모바일 빈 / 다크 모바일 로딩 / 다크 데스크톱 3분할 / 라이트 모바일 오류) — OQ #1 모바일 헤더 raised+border + OQ #5 Lucide ChevronLeft + 44x44"
      contains: "data-theme dark+light / bg-surface-page 외곽 / bg-surface-raised 모바일 헤더 (OQ #1 LOCKED) / border-b border-border-default borderBottom / Lucide ChevronLeft size={20} 모바일 back button 44x44 (OQ #5 LOCKED, w&#8209;11 h&#8209;11 또는 arbitrary w-[44px] h-[44px]) / '소방 점검 관리' 타이틀 verbatim / SKELETON height 72 + @keyframes blink (.6/.3) animation / 3 SKELETON rows (로딩) / 데스크톱 좌 500 + 중 500 + 우 flex 1 / borderRight 1px border-border-default / 중앙 fallback '좌측에서 점검을 선택하세요' / 우측 fallback '중앙에서 지적사항을 선택하세요' / '점검을 먼저 선택하세요' / 모바일 빈 '소방 점검 관리 이력 없음' + '소방 일정 페이지에서 종합정밀 또는 작동기능 점검을 등록하면 여기에 표시됩니다.' / 모바일 오류 '목록을 불러오지 못했습니다.' + '다시 시도' / 데스크톱 좌측 오류 '불러오기 실패' + '재시도' / 데스크톱 좌측 빈 '점검 이력 없음' / FindingsPanel 빈 '지적사항 없음' / FindingDetailPanel 빈 '항목을 불러오지 못했습니다.' / OQ #1 + OQ #4 (아이콘 무) + OQ #5 citation"
      min_lines: 320
    - path: "cha-bio-safety/docs/redesign-context/19-legal/sketch-wave-3-round-card.html"
      provides: "라운드 카드 + 탭 (전체/진행 중/완료, key/label mismatch 의도) + 연도 필터 + accentColor 4분기 borderLeft 3px + ResultBadge 4 라벨 매트릭스 (4 frame: 다크 모바일 pass / 다크 모바일 fail / 다크 데스크톱 conditional+selected / 라이트 모바일 결과 미입력) — OQ #2 status 토큰 매트릭스 + filterRounds 3분기 + handleRoundClick isDesktop 분기 verbatim"
      contains: "data-theme dark+light / 카드 bg-surface-sunken + border border-border-default + 모바일 rounded-md 12 (padding 12) / 데스크톱 rounded-sm 10 (padding 10) / selected `border-2 border-accent` (1.5→2, 데스크톱만, OQ #3 selected) / borderLeft 3px status 토큰 4분기 (OQ #2 LOCKED — pass `border-l-[3px] border-safe-bar` / fail `border-l-[3px] border-danger-bar` / conditional `border-l-[3px] border-warning-bar` / null `border-l-[3px] border-border-strong`) / ResultBadge 4 라벨 verbatim '적합' / '부적합' / '조건부적합' / '결과 미입력' + status 토큰 (`bg-safe-bg text-safe` / `bg-danger-bg text-danger` / `bg-warning-bg text-warning` / `bg-transparent text-text-tertiary`) / 탭 TABS 매핑 (전체/진행 중/완료, key '미조치' mismatch label '진행 중' verbatim) / 탭 활성 `bg-surface-active text-text-primary border-b-2 border-accent` / 연도 select 옵션 `${y}년` / 데스크톱 탭 h 38 + 모바일 탭 h 44 / 메타 verbatim — 데스크톱 '· 지적 N · 완료 N' / 모바일 '· 지적 N건 · 완료 N건' + endDate 분기 ' ~ ' / 비즈 anchor 17건 박제 fence / OQ #2 + OQ #3 citation"
      min_lines: 320
    - path: "cha-bio-safety/docs/redesign-context/19-legal/sketch-wave-4-findings-panel.html"
      provides: "FindingsPanel (데스크톱 중앙 — 헤더 + admin 도구 select/저장/보고서 + sorted findings open-first + finding 카드 status borderLeft + 칩 + 수정/삭제 + FindingFormSheet) + FindingDetailPanel (데스크톱 우측 — 헤더 + admin 다운로드 + 지적 정보 KVRow 4 + 지적 사진 PhotoGrid + 조치 입력 textarea + 사진 5장 + 조치 완료 button + 조치 결과 KVRow 3) 통합 (4 frame: 다크 데스크톱 admin role 평시 / 다크 데스크톱 assistant role / 다크 모바일 sub-route 위임 안내 / 라이트 데스크톱 결과 입력 + 보고서 업로드) — **OQ #4 CTA 그라데이션 negative gate 예외 anchor ≥3** + OQ #5 Camera+Loader2 교체 + 사진 5장 PhotoGrid 시각"
      contains: "data-theme dark+light / FindingsPanel + FindingDetailPanel 통합 / role admin 도구 (select 4 옵션 '미입력'/'적합'/'부적합'/'조건부적합' + '저장' + '보고서'/'보고서 업로드'/'...') / role assistant 도구 숨김 frame / sorted findings open-first (status open 먼저 + createdAt desc) / finding 카드 status borderLeft (open `border-l-[3px] border-danger-bar` / resolved `border-l-[3px] border-safe-bar`) + 상태 칩 (open `bg-danger-bg text-danger` '미조치' / resolved `bg-safe-bg text-safe` '완료') / '위치 미지정' / '수정' '삭제' 액션 / FindingDetailPanel 헤더 '지적 상세' + admin '다운로드' / 지적 정보 KVRow 4 ('지적 내용' / '위치' / '등록일' / '등록자') / 지적 사진 PhotoGrid + 빈 '사진 없음' / 조치 입력 textarea placeholder '조치 내용을 입력하세요' / '조치 사진 (최대 5장)' / 사진 슬롯 64x64 + ✕ 18x18 + canAdd 첨부 button 64x64 + Lucide Camera size={18} (OQ #5 LOCKED, 옛 '📷' 이모지 폐기) / **조치 완료 button width 100% h 44 인라인 `linear-gradient(135deg, #1d4ed8, #0ea5e9)` (OQ #4 LOCKED 예외 anchor ≥3 박제)** / '조치 완료' / '처리 중...' / 조치 결과 KVRow 3 ('조치일시' / '조치자' / '조치 내용') + PhotoGrid / Lucide Loader2 animate-spin 24 (OQ #5 LOCKED, 옛 div spinner @keyframes spin 폐기) / 모바일 frame sub-route 위임 안내 '모바일에서는 카드를 탭하면 별도 페이지로 이동합니다' / 비즈 anchor 17건 박제 fence / OQ #4 + OQ #5 citation"
      min_lines: 320
    - path: "cha-bio-safety/docs/redesign-context/19-legal/wave-5-tsx-conversion-checklist.md"
      provides: "TSX 변환 verify checklist (markdown, 12 섹션) — LegalPage.tsx 571 lines 단일 atomic 변환 룰 + 비즈 anchor 17건 박제 + Tailwind cheatsheet + 메모리 룰 12건 cross-ref"
      contains: "§1 변환 범위 (LegalPage.tsx 단일 atomic, 4 영역 통합 — 상단 유틸 + FindingsPanel + FindingDetailPanel + 메인 LegalPage) / §2 보존 (비즈 anchor 17건 verbatim fence) / §3 변환 매핑 (영역 1~4 verbatim) / §4 OQ LOCKED 5건 반영 매핑 표 / §5 negative gate (W4 linear-gradient 예외 anchor 명시) / §6 positive gate / §7 build/tsc / §8 자체 verify grep 모음 / §9 Tailwind cheatsheet (v0.1.1 토큰 → utility class 매핑) / §10 비즈 보존 체크박스 (accentColor 4분기 + ResultBadge + filterRounds + sorted open-first + role admin + handleRoundClick + useMultiPhotoUpload + buildMetaTxt + invalidate query key 일치) / §11 메모리 룰 inline 12+ (feedback_inspection_unresolved_color + project_inspection_completion_rule + feedback_tailwind_w8_h8_is_48px + feedback_tailwind_token_class_pattern + feedback_text_caption_leading_none 등) / §12 다음 단계"
      min_lines: 320
  key_links:
    - from: "wave-1-index.md §7 OQ #1 LOCKED (모바일 헤더 bg-surface-raised + alpha 0.97)"
      to: "sketch-wave-2-chrome.html + wave-5-tsx-conversion-checklist.md"
      via: "모바일 자체 헤더 = `bg-surface-raised border-b border-border-default` (옛 `rgba(22,27,34,0.97)` + `1px solid var(--bd)` 인라인 폐기, alpha 미세 차이는 토큰 채택). 16-workshift + 17-annual-plan + 02 + 28-splash + 23-education 5 페이지 일관."
      pattern: "bg-surface-raised|border-b border-border-default"
    - from: "wave-1-index.md §7 OQ #2 LOCKED (accentColor + ResultBadge + finding 칩 status 토큰 매핑, status- prefix 없음)"
      to: "sketch-wave-3-round-card.html + sketch-wave-4-findings-panel.html + wave-5-tsx-conversion-checklist.md"
      via: "accentColor 4분기 borderLeft → `border-l-[3px] border-{safe|danger|warning}-bar` / null → `border-l-[3px] border-border-strong` (status- prefix 없음). ResultBadge → `bg-{safe|danger|warning}-bg text-{safe|danger|warning}` / null → `bg-transparent text-text-tertiary`. finding open → danger / resolved → safe. 4분기 + 4 라벨 + 2분기 1 byte 변경 금지 (memory `feedback_inspection_unresolved_color` 일반화)."
      pattern: "border-l-\\[3px\\] border-(safe|danger|warning)-bar|bg-(safe|danger|warning)-bg text-(safe|danger|warning)"
    - from: "wave-1-index.md §7 OQ #3 LOCKED (§1.1 9·10·11 fontSize 12 격상 + leading-none)"
      to: "sketch-wave-3-round-card.html + sketch-wave-4-findings-panel.html + wave-5-tsx-conversion-checklist.md"
      via: "ResultBadge 11 + admin 도구 button 11 + 데스크톱 탭 11 + finding 칩 10 + 메타 10/11 + 첨부 button 10 모두 → `text-caption font-bold leading-none` (12px) 격상. 23-education + 16-workshift + 17-annual-plan OQ 일관."
      pattern: "text-caption|leading-none"
    - from: "wave-1-index.md §7 OQ #4 LOCKED (조치 완료 CTA 그라데이션 + 빈/오류 아이콘 무)"
      to: "sketch-wave-4-findings-panel.html + wave-5-tsx-conversion-checklist.md"
      via: "조치 완료 button (line 346) = `style={{ background: 'linear-gradient(135deg, #1d4ed8, #0ea5e9)' }}` 인라인 그라데이션 (W4 만 linear-gradient negative gate 예외 anchor ≥3). admin 작은 도구 button (저장/보고서/다운로드 h 28) = solid `bg-accent text-text-on-accent` 유지. 빈/오류 아이콘 무 (현 카피만)."
      pattern: "linear-gradient\\(135deg, #1d4ed8, #0ea5e9\\)|bg-accent"
    - from: "wave-1-index.md §7 OQ #5 LOCKED (Lucide ChevronLeft + Camera + Loader2 교체 + back 44x44)"
      to: "sketch-wave-2-chrome.html + sketch-wave-4-findings-panel.html + wave-5-tsx-conversion-checklist.md"
      via: "모바일 헤더 back button — 인라인 SVG ChevronLeft (line 511~513) → Lucide `<ChevronLeft size={20} />` 교체 + 36x36 → 44x44 격상 (memory `feedback_tailwind_w8_h8_is_48px` 회피 = `w-11 h-11` 또는 `w-[44px] h-[44px]`). FindingDetailPanel 첨부 button '📷' (line 340) → Lucide `<Camera size={18} />` 교체. 로딩 spinner div + @keyframes spin (line 291) → Lucide `<Loader2 className=\"animate-spin\" size={24} />` 교체."
      pattern: "ChevronLeft|Camera|Loader2|lucide-react"
    - from: "wave-5-tsx-conversion-checklist.md §3 변환 매핑"
      to: "LegalPage.tsx (변경 0건 — TSX 변환은 본 PLAN 범위 외, W6 별도 wave)"
      via: "W5 checklist 는 W2~W4 sketch + LegalPage.tsx 571 lines 단일 atomic 변환 룰 박제 (다음 turn 의 W6 변환 wave 진입점). 본 PLAN 에서는 TSX 변경 0 byte."
      pattern: "LegalPage.tsx"
    - from: "wave-5-tsx-conversion-checklist.md §11 메모리 룰 inline"
      to: "wave-1-index.md §5 메모리 룰 12건 cross-ref"
      via: "feedback_inspection_unresolved_color (accentColor + ResultBadge + finding 칩 결과 status 토큰 일반화) + project_inspection_completion_rule (role admin 도구 분기 + filterRounds + sorted open-first + handleRoundClick source of truth 일반화) + feedback_tailwind_token_class_pattern + feedback_tailwind_w8_h8_is_48px + feedback_text_caption_leading_none 등 12+ rule citations"
      pattern: "feedback_inspection_unresolved_color|project_inspection_completion_rule|feedback_tailwind_token_class_pattern|feedback_tailwind_w8_h8_is_48px|feedback_text_caption_leading_none"
  scope_negatives:
    - "sketch HTML + checklist md 만 산출. TSX 변환은 W6 별도 wave (본 PLAN 범위 외)"
    - "LegalPage.tsx + App.tsx + 외부 6 파일 (PhotoGrid / PhotoSourceModal / FindingFormSheet / useMultiPhotoUpload / findingDownload / api) + sub-route 페이지 2개 모두 0 byte 변경 (verify gate 최종 git diff)"
    - "wrangler 명령 절대 X / npm run deploy 절대 X (CLAUDE.local.md + memory `feedback_cbc7119_design_never_wrangler`)"
    - "옛 alias 토큰 (var(--bg/bg2/bg3/bg4/bd/bd2/t1/t2/t3/acl/accent/safe/warn/danger)) sketch 본문 안 0 (tokens.css 정의 인용 fence 안 예외)"
    - "status- prefix 0 (bg-status-safe / text-status-danger 등 사용 시 verify FAIL)"
    - "이모지 0 (sketch 본문 — Lucide 교체 anchor 만 텍스트 인용)"
    - "w-8 / h-8 className 0 (48px 함정, memory `feedback_tailwind_w8_h8_is_48px`)"
    - "9·10·11 fontSize 인라인 0 (text-caption 12 leading-none 마지노선, OQ #3 LOCKED)"
    - "linear-gradient — W2/W3/W5 = 0 / **W4 만 예외 anchor ≥3** (OQ #4 LOCKED 조치 완료 CTA 그라데이션)"
    - "sketch/ 서브폴더 절대 금지 — 4 파일 모두 19-legal/ 직속 평면 배치"
---

<objective>
redesign/19-legal 의 W1 인덱스(`wave-1-index.md`, 758 lines, OQ 5건 default LOCKED 완료 2026-05-22 sa7)를 기반으로
W2~W5 4 sub-wave 산출물을 **단일 quick task** 안에서 **atomic 4 commit** 으로 한 번에 만든다.

- **W2 (T1):** 모바일 자체 헤더 (Lucide ChevronLeft + 44x44 격상) + 데스크톱 3분할 outline (좌 500 + 중 500 + 우 flex 1) + 빈/로딩/오류 5 state 매트릭스 (LegalPage.tsx line 50 SKELETON / 291 spin / 428~437 데스크톱 좌측 / 462~500 데스크톱 3분할 / 507~551 모바일 헤더 + 빈/오류 영역) — 4 frame
- **W3 (T2):** 라운드 카드 + 탭 (전체/진행 중/완료, key/label mismatch) + 연도 필터 + accentColor 4분기 borderLeft 3px + ResultBadge 4 라벨 (line 27~32 accentColor / 35~47 ResultBadge / 53~67 TABS+filterRounds+genYears / 404~461 roundList / 438~458 데스크톱 카드 / 552~567 모바일 카드 영역) — 4 frame
- **W4 (T3):** FindingsPanel + FindingDetailPanel 통합 (line 82~228 FindingsPanel + 233~367 FindingDetailPanel) — admin 도구 + assistant + open vs resolved + 사진 5장 + 조치 완료 CTA 그라데이션, 4 frame (admin 평시 / assistant / 모바일 sub-route 위임 / 라이트 결과 입력)
- **W5 (T4):** TSX 변환 verify checklist markdown (sketch 아님, W2~W4 grep 추출 검증 룰 + 12 섹션 + 비즈 anchor 17건 박제 + LegalPage.tsx 571 lines 단일 atomic 변환 룰)

**Purpose:** 28-splash 2q6 / 17-annual-plan 0j3 / 16-workshift sjj / 27-login c6p / 14-reports / 13-schedule / 23-education o7b 와 동일한 평면(flat sibling) 패턴으로 19-legal sketch 단계를 4 wave 분량을 한 turn 에 처리. LegalPage 가 571 lines 단일 파일 + 3개 내부 컴포넌트 통합 (FindingsPanel + FindingDetailPanel + 메인 LegalPage) + 데스크톱 3분할 마스터-디테일-디테일 (좌 500 + 중 500 + 우 flex 1) + 모바일은 단일 컬럼 + sub-route /legal/:id 위임 + 결과 4분기 + role admin 도구 분기 + 사진 5장 + ZIP 다운로드 — 본 PLAN 에서 sketch 3 wave + checklist 1 wave 한 번에 처리해 cbc7119-preview 자동 배포 사이클 1회로 사용자 시각 검수 가능하게 함.

**Output:** `cha-bio-safety/docs/redesign-context/19-legal/` 직속 4 파일 (sketch HTML 3개 + checklist md 1개).
`LegalPage.tsx` (571 lines) + `App.tsx` + **외부 6 파일** (PhotoGrid / PhotoSourceModal / FindingFormSheet / useMultiPhotoUpload / findingDownload / api) + **sub-route 페이지 2개** (LegalFindingsPage / LegalFindingDetailPage) **총 10 src 파일 모두** 코드 변경 0건 (verify gate 마지막 task 에서 확인).

**23-education o7b PLAN.md mirror:** 본 PLAN 은 23-education o7b PLAN (977 lines, 단일 plan / 4 task / atomic 4 commit / verify gate per task) 의 정확한 mirror. 차이점:
- LegalPage 571 lines 단일 파일 (Education 591 의 0.97배, 거의 동일 통합 규모)
- OQ 결정 내용 (모바일 헤더 raised + accentColor 4분기 status 토큰 + ResultBadge 4 라벨 + finding 칩 2분기 + 조치 완료 CTA 그라데이션 + Lucide ChevronLeft+Camera+Loader2 + back 44x44)
- W4 만 linear-gradient 예외 (OQ #4 LOCKED 조치 완료 CTA 채택)
- source 10 파일 untouched (LegalPage + App.tsx + 외부 6 + sub-route 2)
- 19-legal 은 **점검 시리즈 = chrome 룰 직접 적용 케이스** (02 InspectionPage 와 동일 도메인, Education 보수교육과 다름)
- 모바일은 **sub-route 네비게이션** (Education 의 바텀시트와 다름) — 본 PLAN 의 W4 sketch 는 데스크톱 패널 중심, 모바일 frame 은 "sub-route 위임 안내" 만 표시

**중요 — executor commit 부모 머지 사고 회피:** 16-workshift sjj + 17-annual-plan wmq + 28-splash 2q6 + 23-education o7b 등 다수 quick 에서 executor worktree commit 이 부모 브랜치로 자동 머지 안 되어 cherry-pick 복구 필요했음 (6회 재현). **executor 는 worktree 안에서 `git commit` 후 명시적 보고 필수** (commit hash + message 1줄) + orchestrator 가 필요 시 자동 cherry-pick 으로 복구한다. 본 PLAN 각 task 의 commit 명령은 명시 박제.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jykevin/Documents/cbc7119-design/CLAUDE.md
@/Users/jykevin/Documents/cbc7119-design/CLAUDE.local.md

# W1 인덱스 (단일 진입점, OQ 5건 default LOCKED 완료 2026-05-22 sa7, 758 lines)
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/19-legal/wave-1-index.md

# Precedent — 23-education o7b PLAN.md + SUMMARY.md (정확한 mirror 소스, 본 PLAN 의 구조 원본)
@/Users/jykevin/Documents/cbc7119-design/.planning/quick/260522-o7b-redesign-23-education-w2-w5/260522-o7b-PLAN.md

# Precedent — 23-education o7b 산출 4 파일 (4 frame + 룰 박스 구조 mirror)
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/23-education/sketch-wave-2-chrome-empty.html
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/23-education/sketch-wave-3-staff-card.html
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/23-education/sketch-wave-4-edit-modal.html
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/23-education/wave-5-tsx-conversion-checklist.md

# Source TSX (sketch 대상, 변경 0 — 10 파일 모두 untouched)
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/src/pages/LegalPage.tsx
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/src/App.tsx

# 19-legal redesign 컨텍스트 (토큰/타이포 source, design-system v0.1.1)
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/19-legal/19-legal.md
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/19-legal/design-system.md
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/19-legal/tokens.css
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/19-legal/typography.css
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/19-legal/PhotoGrid.tsx
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/19-legal/PhotoSourceModal.tsx
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/19-legal/useMultiPhotoUpload.ts
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/19-legal/ui-index.tsx

# 02+06 chrome 통일 룰 (19-legal = 점검 시리즈 → 직접 적용 케이스, 23-education 과 차이)
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/00-design-context/inspection-modal-chrome-rules.md
</context>
</content>
</invoke>
<rules_verbatim>
### OQ LOCKED 5건 (wave-1-index.md §7 default 답 verbatim, 사용자 "다음 진행" 으로 LOCKED 2026-05-22)

**OQ #1 (모바일 자체 헤더 토큰 + alpha):**
> LOCKED (2026-05-22): LegalPage 모바일 자체 헤더 = `bg-surface-raised border-b border-border-default`. 기존 `rgba(22,27,34,0.97)` (raised 변형 alpha) + `1px solid var(--bd)` 인라인 토큰 완전 폐기. alpha 0.97 vs tokens.css raised full opacity 미세 차이는 토큰 채택 (불일치 시 `bg-[rgba(22,27,34,0.97)]` arbitrary fallback). height 48 + 자체 헤더 구조 (back button 44x44 OQ #5 격상 + 타이틀 '소방 점검 관리' 정중앙 + right spacer 44) 1 byte 변경 금지. W2 sketch 모바일 frame + TSX 변환 양쪽 동일 적용. 16-workshift + 17-annual-plan + 02 + 28-splash + 23-education 5 페이지 일관.

**OQ #2 (accentColor + ResultBadge + finding 칩 status 토큰 매핑, status- prefix 없음):**
> LOCKED (2026-05-22): accentColor 4분기 borderLeft 색 — `border-l-[3px] border-safe-bar` (pass) / `border-l-[3px] border-danger-bar` (fail) / `border-l-[3px] border-warning-bar` (conditional) / `border-l-[3px] border-border-strong` (null). ResultBadge map — `bg-safe-bg text-safe` (pass '적합') / `bg-danger-bg text-danger` (fail '부적합') / `bg-warning-bg text-warning` (conditional '조건부적합') / `bg-transparent text-text-tertiary` (null '결과 미입력'). finding 칩 — `bg-danger-bg text-danger` (open '미조치') / `bg-safe-bg text-safe` (resolved '완료'). finding borderLeft — `border-l-[3px] border-danger-bar` (open) / `border-l-[3px] border-safe-bar` (resolved). **임계치 + 라벨 + 매핑 1 byte 변경 금지** (memory `feedback_inspection_unresolved_color` 일반화). **status- prefix 없음** (`text-status-safe` 같은 패턴 사용 시 W5 verify FAIL, memory `feedback_tailwind_token_class_pattern`). alpha 0.13/0.15 vs tokens.css safe-bg 0.16 미세 차이는 executor 가 tokens.css 의 status 토큰 값을 그대로 채택 (불일치 시 인라인 fallback `bg-[rgba(...)]` arbitrary).

**OQ #3 (§1.1 9·10·11 fontSize 12 격상 + leading-none):**
> LOCKED (2026-05-22): ResultBadge 11 + admin 도구 button 11 (저장/보고서/다운로드) + 데스크톱 탭 11 + finding 칩 10 + finding 메타 10/11 + 첨부 button 10 + 수정/삭제 button 10 모두 → `text-caption font-bold leading-none` (12px). 격상 후 padding 미세 조정 (예: 배지/칩 padding `1px 6px` → `2px 8px` 시각 균형). 16 (모바일 타이틀 / 빈 제목 / 모바일 카드 title) + 15 (FindingsPanel + FindingDetailPanel 헤더) + 14 (모바일 오류) + 13 (모바일 탭 / 데스크톱 fallback / 라운드 카드 데스크톱 title) 모두 보존. 23-education + 16-workshift + 17-annual-plan OQ 일관 (§1.1 노안 룰 우선 정신).

**OQ #4 (조치 완료 CTA 그라데이션 + 빈/오류 아이콘 무):**
> LOCKED (2026-05-22): FindingDetailPanel 조치 완료 button (line 346) = `style={{ background: 'linear-gradient(135deg, #1d4ed8, #0ea5e9)' }}` 인라인 그라데이션 (디자인 강조용, design-system §6.4 CTA 룰 일관). 토큰 채택 X (linear-gradient negative gate 의 W4 만 예외 anchor). admin 작은 도구 button (저장/보고서/다운로드 h 28) + 모바일 다시 시도 button + 데스크톱 재시도 button = **solid `bg-accent text-text-on-accent` 유지** (작은 도구 그라데이션 = 시각 잡음). disabled = `bg-surface-sunken text-text-tertiary cursor-not-allowed`. 빈/오류 상태 = 아이콘 추가 X, 현재 카피 verbatim 유지 (Lucide `FolderOpen` 빈 / `AlertCircle` 오류 옵션은 executor 판단으로 추가 가능 — default 무). W4 sketch + TSX 변환 양쪽 동일 적용.

**OQ #5 (Lucide ChevronLeft + Camera + Loader2 교체 + back button 44x44 격상):**
> LOCKED (2026-05-22): (1) 모바일 헤더 back button — 인라인 SVG ChevronLeft (line 511~513, path `d="M15 19l-7-7 7-7"` strokeWidth 2 size 20) → Lucide `<ChevronLeft size={20} />` 교체. **back button 36x36 → 44x44 격상** (§1.1 터치 마지노선 44px). w&#8209;8 함정 회피 → `w-11 h-11` (44px tailwind 기본 spacing 11) 또는 `w-[44px] h-[44px]` arbitrary. position absolute left 12 → left 8 또는 left 12 유지. (2) FindingDetailPanel 첨부 button '📷' 이모지 (line 340) → Lucide `<Camera size={18} />` 교체. 첨부 button 외곽 64x64 유지, 내부 아이콘만 교체. (3) FindingDetailPanel 로딩 spinner div + @keyframes spin (line 291) → Lucide `<Loader2 className="animate-spin" size={24} />` 교체. import 추가: `import { ChevronLeft, Camera, Loader2 } from 'lucide-react'`. W2 + W4 sketch + TSX 변환 양쪽 동일 적용.

### 디자인 토큰 패턴 (wave-1-index.md §3 + planning constraints verbatim)
- `bg-surface-page` (var(--bg) → --surface-page, 페이지 외곽)
- `bg-surface-raised` (var(--bg2) → --surface-raised, 모바일 헤더 OQ #1)
- `bg-surface-sunken` (var(--bg3) → --surface-sunken, 카드 + SKELETON + input/select + 사진 슬롯 + admin 도구 button + textarea + disabled)
- `bg-surface-active` (var(--bg4) → --surface-active, 탭 활성)
- `border-border-default` (var(--bd) → --border-default, 카드 평시 + 모바일 헤더 borderBottom + 데스크톱 분할 borderRight)
- `border-border-strong` (var(--bd2) → --border-strong, input/select + null accentColor + admin 도구 button border)
- `text-text-primary` (var(--t1) → --text-primary)
- `text-text-secondary` (var(--t2) → --text-secondary)
- `text-text-tertiary` (var(--t3) → --text-tertiary)
- `text-danger` (var(--danger) → --danger)
- `bg-safe-bg text-safe` / `border-safe-bar` (status-safe 토큰, OQ #2 LOCKED, status- prefix 없음)
- `bg-warning-bg text-warning` / `border-warning-bar` (status-warning 토큰, OQ #2 LOCKED)
- `bg-danger-bg text-danger` / `border-danger-bar` (status-danger 토큰, OQ #2 LOCKED)
- `border-accent` (var(--acl) → --accent, selected card border-2 OQ #3-selected, 데스크톱만)
- `bg-accent text-text-on-accent` (CTA solid, admin 작은 도구 button)
- radius: `rounded-sm` 8 / `rounded-md` 12 / `rounded-full` (아바타)

### Negative rule (모든 task verify gate)
- 이모지 0건 (sketch 본문)
- `linear-gradient` — W2/W3/W5 = 0건 / **W4 만 예외 anchor** (OQ #4 LOCKED 조치 완료 CTA `linear-gradient(135deg, #1d4ed8, #0ea5e9)` ≥3 박제)
- 9&#183;10&#183;11px fontSize 0건 (text-caption 12 leading-none 마지노선, OQ #3 LOCKED)
- `(text|bg|border)-status-` class prefix 0건 (`bg&#8209;status&#8209;safe X`, memory `feedback_tailwind_token_class_pattern`)
- `\b(w|h)-8\b` 0건 (tailwind config w&#8209;8=48 함정, memory `feedback_tailwind_w8_h8_is_48px`)
- 옛 alias 토큰 (`var(--bg)` `var(--bg2)` `var(--bg3)` `var(--bg4)` `var(--bd)` `var(--bd2)` `var(--t1)` `var(--t2)` `var(--t3)` `var(--acl)` `var(--accent)` `var(--safe)` `var(--warn)` `var(--danger)`) 0건 in sketch body (tokens.css 정의 인용 fence 안 예외)

### 평면 폴더 패턴 (13-schedule + 14-reports + 27-login + 16-workshift + 17-annual-plan + 28-splash + 23-education 일관)
- 19-legal/ 직속에 4 파일 배치
- `sketch/` 서브폴더 절대 금지

### ★ 비즈 anchor 17건 (1 byte 변경 금지, W2~W5 모두 박제)

**[react-query / 비즈 시그니처]**
1. `useQuery({ queryKey: ['legal-rounds', year], queryFn: () => legalApi.list(year), staleTime: 30_000 })` (line 386~390) — 변경 금지
2. `useQuery({ queryKey: ['legal-round', roundId], queryFn: () => legalApi.get(roundId), enabled: !!roundId })` (line 96~100)
3. `useQuery({ queryKey: ['legal-findings', roundId], queryFn: () => legalApi.getFindings(roundId), enabled: !!roundId, staleTime: 30_000 })` (line 102~106)
4. `useQuery({ queryKey: ['legal-finding', roundId, findingId], ...})` (line 241~245) + useMutation resolveMutation (line 247~265) + 4 키 invalidate

**[utils/api.ts legalApi 7종]**
5. legalApi 7종 — list(year) / get(roundId) / getFindings(roundId) / updateResult(roundId, { result?, report_file_key? }) / deleteFinding(roundId, findingId) / getFinding(roundId, findingId) / resolveFinding(roundId, findingId, { resolution_memo, resolution_photo_keys? }) — snake_case payload + camelCase props 혼용 보존

**[비즈 로직 함수 + 분기]**
6. `accentColor(result)` (line 27~32): pass → `'var(--safe)'` / fail → `'var(--danger)'` / conditional → `'var(--warn)'` / 그 외 → `'var(--bd2)'` — **4분기 1 byte 변경 금지** (memory `feedback_inspection_unresolved_color` 일반화)
7. ResultBadge map (line 35~47): pass `{bg:'rgba(34,197,94,.13)', color:'var(--safe)', label:'적합'}` / fail `{bg:'rgba(239,68,68,.15)', color:'var(--danger)', label:'부적합'}` / conditional `{bg:'rgba(245,158,11,.15)', color:'var(--warn)', label:'조건부적합'}` / null → `'결과 미입력'` `var(--t3)` — **4 라벨 + rgba + var() 1 byte 변경 금지**
8. `filterRounds(rounds, tab)` (line 59~63): 미조치 → `findingCount > resolvedCount` / 완료 → `findingCount > 0 && findingCount === resolvedCount` / 전체 → 그대로 (운영 룰, memory `project_inspection_completion_rule` 일반화)
9. TABS (line 54~58): `[{key:'전체',label:'전체'},{key:'미조치',label:'진행 중'},{key:'완료',label:'완료'}]` — **key/label mismatch ('미조치' key → '진행 중' label)** 의도된 디자인
10. `sorted` findings (line 147~151): status 'open' 먼저 + 그 외 createdAt desc localeCompare — 운영 룰 source of truth
11. `handleRoundClick` (line 394~401): `isDesktop ? (setSelectedRoundId(round.id), setSelectedFindingId(null)) : navigate('/legal/' + round.id)` — isDesktop 분기 변경 금지
12. **role admin 도구 분기** — FindingsPanel `role === 'admin'` (line 162) → select+저장+보고서 / FindingDetailPanel `staff?.role === 'admin'` (line 299) → 다운로드 button — 운영 룰 (memory `project_inspection_completion_rule` 일반화)

**[useMultiPhotoUpload + ZIP + animation + 카피]**
13. `useMultiPhotoUpload()` 시그니처 + **5장 제한** (canAdd = slots.length < 5)
14. `buildMetaTxt(finding)` + fflate dynamic `import('fflate').zipSync` + ZIP 파일명 `지적사항_${(location ?? '').replace(/[\\/\\\\:*?"<>|]/g, '_')}.zip` + 사진 파일명 `지적사진-${j+1}.jpg` / `조치사진-${j+1}.jpg`
15. `@keyframes blink { 0%,100%{opacity:.6} 50%{opacity:.3} }` (line 467, 505) — **Education 의 1/0.4 와 다름** + `@keyframes spin { to{transform:rotate(360deg)} }` (line 291, OQ #5 LOCKED 시 Lucide Loader2 교체)
16. **toast 카피 11종 verbatim**: success 5 — '점검 결과 저장' / '보고서 업로드 완료' / '삭제됨' / '조치 완료' / '다운로드 완료' + error 6 — '저장 실패' / '업로드 실패' / `err?.message ?? '삭제 실패'` / '조치 처리 실패' / '다운로드 실패' / '조치 내용을 입력하세요'
17. **빈/오류/fallback 카피 verbatim 다수** — 모바일 빈 '소방 점검 관리 이력 없음' + '소방 일정 페이지에서 종합정밀 또는 작동기능 점검을 등록하면 여기에 표시됩니다.' / 모바일 오류 '목록을 불러오지 못했습니다.' + '다시 시도' / 데스크톱 좌측 오류 '불러오기 실패' + '재시도' / 데스크톱 좌측 빈 '점검 이력 없음' / FindingsPanel 빈 '지적사항 없음' / FindingDetailPanel 빈 '항목을 불러오지 못했습니다.' / 데스크톱 중앙 fallback '좌측에서 점검을 선택하세요' / 데스크톱 우측 fallback '중앙에서 지적사항을 선택하세요' / '점검을 먼저 선택하세요' / 모바일 헤더 타이틀 '소방 점검 관리' / 섹션 라벨 '지적 정보' / '지적 사진' / '조치 내용' / '조치 결과' / KVRow 라벨 (지적 정보 4) '지적 내용' '위치' '등록일' '등록자' / (조치 결과 3) '조치일시' '조치자' '조치 내용' / 결과 select 4 옵션 '미입력' '적합' '부적합' '조건부적합' / '위치 미지정' / '사진 없음' / FindingDetailPanel 헤더 '지적 상세' / 다운로드 button '다운로드' / '...' / 조치 textarea placeholder '조치 내용을 입력하세요' / 조치 사진 라벨 '조치 사진 (최대 5장)' / 조치 완료 button '조치 완료' / '처리 중...' / finding 액션 '수정' / '삭제' / finding 상태 칩 '미조치' / '완료' / 메타 verbatim (데스크톱 '· 지적 N · 완료 N' 건 없음 / 모바일 '· 지적 N건 · 완료 N건' + endDate '~' 분기)

### 데스크톱 3분할 비즈 anchor (구조 보존)
- 데스크톱 좌측 width 500 → `w-[500px] flex-shrink-0 border-r border-border-default`
- 데스크톱 중앙 width 500 → `w-[500px] flex-shrink-0 border-r border-border-default`
- 데스크톱 우측 flex 1 → `flex-1`
- **width 500/500/flex 1 변경 금지** (의도된 디자인)

### Tailwind cheatsheet (v0.1.1 토큰 → utility class 매핑, 19-legal 적용)
| v0.1.1 토큰 | Tailwind utility | 19-legal 적용 위치 |
|---|---|---|
| `--surface-page` | `bg-surface-page` | LegalPage 외곽 (line 466 데스크톱 / 504 모바일) |
| `--surface-raised` | `bg-surface-raised` | 모바일 헤더 (line 508, OQ #1) |
| `--surface-sunken` | `bg-surface-sunken` | 카드 (line 443 데스크톱 / 554 모바일) + SKELETON (line 50) + admin select / 보고서 / 다운로드 / textarea / 첨부 button |
| `--surface-active` | `bg-surface-active` | 탭 활성 (line 411 / 522) |
| `--border-default` | `border-border-default` | 모바일 헤더 borderBottom + 카드 평시 + 데스크톱 분할 borderRight + 조치 borderTop |
| `--border-strong` | `border-border-strong` | input/select border + admin 도구 button border + 첨부 button border-dashed + null accentColor |
| `--text-primary` | `text-text-primary` | 이름 + 모바일 타이틀 + 카드 title + 빈 제목 + KVRow children + finding description |
| `--text-secondary` | `text-text-secondary` | 카드 메타 + 모바일 빈 보조 + back button + 모바일 오류 + 데스크톱 좌측 오류 |
| `--text-tertiary` | `text-text-tertiary` | 데스크톱 fallback + 데스크톱 좌측 빈 + FindingsPanel 빈 + FindingDetailPanel 빈 + KVRow 라벨 + 섹션 라벨 + null ResultBadge + 첨부 color |
| `--danger` | `text-danger` | finding 칩 open / ResultBadge fail / 오류 카피 |
| `--safe` + rgba(34,197,94,.13) | `bg-safe-bg text-safe` / `border-safe-bar` | ResultBadge pass / accentColor pass / finding 칩 resolved (OQ #2) |
| `--warn` + rgba(245,158,11,.15) | `bg-warning-bg text-warning` / `border-warning-bar` | ResultBadge conditional / accentColor conditional (OQ #2) |
| `--danger` + rgba(239,68,68,.15) | `bg-danger-bg text-danger` / `border-danger-bar` | ResultBadge fail / accentColor fail / finding 칩 open / finding borderLeft open (OQ #2) |
| `--bd2` (null accent) | `border-border-strong` (border-l-[3px]) | accentColor null |
| `--accent` (acl) | `border-accent` (border-2) | selected card border (line 192, 444, OQ #3-selected, 데스크톱만) |
| `--accent` (CTA) | `bg-accent` solid 또는 인라인 그라데이션 | admin 도구 solid (line 170) / 조치 완료 CTA 인라인 (line 346, OQ #4 LOCKED 예외) |
| (radius 6) | `rounded-sm` | admin 도구 button + finding 칩 + ResultBadge |
| (radius 8) | `rounded-sm` | 사진 슬롯 + 첨부 button + 모바일 다시 시도 button |
| (radius 9) | `rounded-md` 또는 `rounded-[9px]` arbitrary | 조치 textarea |
| (radius 10) | `rounded-md` | 라운드 카드 데스크톱 + finding 카드 + 조치 완료 button |
| (radius 12) | `rounded-md` | SKELETON + 라운드 카드 모바일 |

### 기타 메모리 룰 inline (W5 §11 박제)
- `feedback_inspection_unresolved_color` — accentColor + ResultBadge + finding 칩 결과 status 토큰 일반화 (OQ #2 LOCKED)
- `project_inspection_completion_rule` — role admin 도구 분기 + filterRounds + sorted open-first + handleRoundClick isDesktop 분기 source of truth 일반화
- `feedback_tailwind_token_class_pattern` — status- prefix 없음 + lucide `<Icon size={N} />` prop
- `feedback_tailwind_w8_h8_is_48px` — w&#8209;8/h&#8209;8 = 48px 함정 (back button 44x44 = `w-11 h-11`, OQ #5 LOCKED 격상)
- `feedback_text_caption_leading_none` — 작은 컨테이너 안 text-caption(12) leading-none 명시
- `feedback_sketch_realistic_data` — 표시 분기/라벨 룰 코드 그대로
- `feedback_design_changes_ask_first` — 디자인 변경 전 사용자 컨펌
- `feedback_planner_prompt_sketch_verbatim` — sketch CSS verbatim 인용 (추측 토큰명 금지)
- `feedback_tsx_wave_emoji_dot_gap` — sketch 의 이모지 0건 negative gate ('📷' 이모지 사고 케이스 OQ #5 Camera 교체)
- `feedback_tsx_wave_stat_card_drift` — source outline 패턴 보존 + sketch 새 패턴 누락 방지
- `feedback_subagent_production_deploy_forbidden` — wrangler 명령 절대 X (디자인 wave)
- `feedback_redesign_sketch_rule_enforcement` — §6.2 negative rule (단 결과 status 토큰은 룰 11 예외) + §6.3/§7.1 일관성 강제

### W4 만 linear-gradient 예외 anchor (OQ #4 LOCKED)
- W2 sketch: `linear-gradient` 0건
- W3 sketch: `linear-gradient` 0건
- **W4 sketch: `linear-gradient(135deg, #1d4ed8, #0ea5e9)` ≥3 박제**
- W5 checklist: code fence 안 인용 ≥1

</rules_verbatim>
</context>


<tasks>

<task type="auto">
  <name>Task 1 (W2): LegalPage 모바일 자체 헤더 (Lucide ChevronLeft + 44x44 격상) + 데스크톱 3분할 outline + 빈/로딩/오류 5 state sketch 생성 (4 frame)</name>
  <files>cha-bio-safety/docs/redesign-context/19-legal/sketch-wave-2-chrome.html</files>
  <action>
    23-education/sketch-wave-2-chrome-empty.html 패턴 mirror 로 새 sketch 파일 작성. LegalPage.tsx 모바일 자체 헤더 (line 507~515) + 데스크톱 3분할 outline (line 464~500, 좌 500 + 중 500 + 우 flex 1) + 빈/로딩/오류 5 state (line 50 SKELETON / 428~437 데스크톱 좌측 / 539~551 모바일 / 184 FindingsPanel 빈 / 292 FindingDetailPanel 빈) + 인라인 `@keyframes blink { 0%,100%{opacity:.6} 50%{opacity:.3} }` (line 467, 505) 시각화. **OQ #1 LOCKED (모바일 헤더 `bg-surface-raised border-b border-border-default`)** + **OQ #4 LOCKED (빈/오류 아이콘 무 카피만 유지)** + **OQ #5 LOCKED (Lucide ChevronLeft size={20} + back button 44x44 격상)** 3건이 본 wave 의 핵심.

    **구조 (23-education o7b W2 mirror, 4 frame):**
    - `<!DOCTYPE html>` + `<html lang="ko">` + `<head><meta charset="utf-8"><title>redesign/19-legal — W2 chrome + empty/loading/error</title>` + `<style>` 블록
    - `<style>` 안: v0.1.1 tokens.css 의 dark+light 양쪽 정의 verbatim 인용 + typography.css 정의 verbatim 인용 + `@keyframes blink { 0%,100%{opacity:.6} 50%{opacity:.3} }` 박제 (line 467, 505 anchor — **Education .4 와 다름**)
    - `<body>` 안 4 frame side-by-side (state 매트릭스):

      | frame | viewport | 시각화 |
      |---|---|---|
      | 1 | 모바일 다크 393×800 빈 상태 | `data-theme="dark"` + bg-surface-page 외곽 + 자체 헤더 (h 48, bg-surface-raised + border-b border-border-default, OQ #1) + Lucide ChevronLeft size={20} back 44x44 (OQ #5, w-11 h-11) + 타이틀 '소방 점검 관리' + right spacer 44 + 스크롤 영역 안 빈 '소방 점검 관리 이력 없음' (16/700 var(--t1)) + '소방 일정 페이지에서 종합정밀 또는 작동기능 점검을 등록하면 여기에 표시됩니다.' (12 var(--t2)) — OQ #4 아이콘 무 |
      | 2 | 모바일 다크 393×800 로딩 상태 | `data-theme="dark"` + 동일 헤더 + 스크롤 영역 안 3 SKELETON rows (height 72 + animation blink, line 50 anchor) |
      | 3 | 데스크톱 다크 1440×900 (글로벌 AppHeader + 사이드바 placeholder 포함) + 3분할 outline | `data-theme="dark"` + 좌 500 + 중 500 + 우 flex 1 (line 470/475/483 verbatim) + 좌측 borderRight 1px border-border-default + 중앙 borderRight 1px border-border-default + 좌측 padding (W3 영역 채울 placeholder) + 중앙 fallback '좌측에서 점검을 선택하세요' (line 484) + 우측 fallback '중앙에서 지적사항을 선택하세요' / '점검을 먼저 선택하세요' (line 494, 양 분기 모두 박제) + 데스크톱 좌측 오류 '불러오기 실패' + '재시도' button 박스 + 데스크톱 좌측 빈 '점검 이력 없음' 박스 + FindingsPanel 빈 '지적사항 없음' 박스 + FindingDetailPanel 빈 '항목을 불러오지 못했습니다.' 박스 |
      | 4 | 모바일 라이트 393×800 오류 상태 | `data-theme="light"` + 동일 헤더 + 스크롤 영역 안 '목록을 불러오지 못했습니다.' (line 542, 14 var(--t2)) + '다시 시도' button (bg-accent text-text-on-accent, line 543) |

    **모바일 헤더 마크업 룰 (frame 1/2/4 공통 — OQ #1 LOCKED + OQ #5 LOCKED):**
    ```html
    <!-- 모바일 자체 헤더 (OQ #1 LOCKED) -->
    <div class="bg-surface-raised border-b border-border-default" style="height:48px;display:flex;align-items:center;justify-content:center;position:relative;flex-shrink:0;">
      <!-- back button (OQ #5 LOCKED: Lucide ChevronLeft size={20} + 44x44 격상, w-11 h-11) -->
      <button class="text-text-secondary" style="position:absolute;left:8px;width:44px;height:44px;background:none;border:none;cursor:pointer;display:flex;align-items:center;justify-content:center;" aria-label="Lucide ChevronLeft size={20}, back button 44x44 (w-11 h-11)">
        <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="15 18 9 12 15 6" /></svg>
      </button>
      <!-- 타이틀 (line 514 verbatim '소방 점검 관리', 16/700 정중앙) -->
      <span class="text-body font-bold text-text-primary">소방 점검 관리</span>
    </div>
    ```

    **SKELETON 마크업 룰 (frame 2 로딩 — line 50 + 467/505 anchor):**
    ```html
    <!-- 3 SKELETON rows (line 50 SKELETON CSSProperties verbatim, height 72 + blink animation .6/.3) -->
    <div style="display:flex;flex-direction:column;gap:8px;padding:12px 16px;">
      <div class="bg-surface-sunken rounded-md" style="height:72px;animation:blink 2s ease-in-out infinite;"></div>
      <div class="bg-surface-sunken rounded-md" style="height:72px;animation:blink 2s ease-in-out infinite;"></div>
      <div class="bg-surface-sunken rounded-md" style="height:72px;animation:blink 2s ease-in-out infinite;"></div>
    </div>
    ```

    **빈 상태 마크업 룰 (frame 1 모바일 — line 547~551 verbatim, OQ #4 아이콘 X):**
    ```html
    <div style="display:flex;flex-direction:column;align-items:center;justify-content:center;flex:1;text-align:center;padding:60px 16px;gap:8px;">
      <div class="text-body font-bold text-text-primary">소방 점검 관리 이력 없음</div>
      <div class="text-caption leading-none text-text-secondary">소방 일정 페이지에서 종합정밀 또는 작동기능 점검을 등록하면 여기에 표시됩니다.</div>
    </div>
    ```

    **오류 상태 마크업 룰 (frame 4 모바일 — line 542~543 verbatim, OQ #4):**
    ```html
    <div style="display:flex;flex-direction:column;align-items:center;justify-content:center;flex:1;gap:12px;padding:40px 16px;">
      <div class="text-body-sm text-text-secondary">목록을 불러오지 못했습니다.</div>
      <button class="bg-accent text-text-on-accent text-body-sm font-bold rounded-sm" style="padding:8px 24px;border:none;cursor:pointer;">다시 시도</button>
    </div>
    ```

    **데스크톱 3분할 마크업 룰 (frame 3 — line 464~500 verbatim outline):**
    ```html
    <!-- 데스크톱 외곽: 글로벌 AppHeader 표시 (PAGE_TITLES '/legal': '소방 점검 관리') + 좌측 사이드바 placeholder + 좌/중/우 3분할 (500/500/flex 1) -->
    <div class="bg-surface-page" style="display:flex;height:100%;">
      <!-- 좌측: 카드 목록 (W3 채울 영역) — width 500 borderRight 1px -->
      <div class="border-r border-border-default" style="width:500px;flex-shrink:0;display:flex;flex-direction:column;height:100%;">
        <div style="flex:1;overflow-y:auto;padding:8px 12px;">
          <div class="text-label text-text-tertiary">(W3 에서 라운드 카드 + 탭 + 연도 select 렌더)</div>
          <!-- 데스크톱 좌측 오류 box (line 431~432 verbatim) -->
          <div style="margin-top:16px;display:flex;flex-direction:column;align-items:center;gap:8px;padding:16px;">
            <div class="text-label text-text-secondary">불러오기 실패</div>
            <button class="bg-accent text-text-on-accent text-caption font-bold leading-none rounded-sm" style="padding:6px 16px;border:none;cursor:pointer;">재시도</button>
          </div>
          <!-- 데스크톱 좌측 빈 box (line 436 verbatim) -->
          <div style="margin-top:16px;display:flex;align-items:center;justify-content:center;padding:24px;">
            <div class="text-label text-text-tertiary">점검 이력 없음</div>
          </div>
        </div>
      </div>
      <!-- 중앙: 지적사항 목록 fallback (line 484 verbatim) + FindingsPanel 빈 box (line 184) -->
      <div class="border-r border-border-default" style="width:500px;flex-shrink:0;display:flex;flex-direction:column;height:100%;">
        <div style="display:flex;align-items:center;justify-content:center;flex:1;padding:24px;">
          <div class="text-label text-text-tertiary">좌측에서 점검을 선택하세요</div>
        </div>
        <!-- FindingsPanel 빈 (W4 진입 시) -->
        <div style="display:flex;align-items:center;justify-content:center;padding:24px;border-top:1px dashed var(--bd);">
          <div class="text-label text-text-tertiary">지적사항 없음</div>
        </div>
      </div>
      <!-- 우측: 상세 패널 fallback (line 494 verbatim 양 분기) + FindingDetailPanel 빈 box (line 292) -->
      <div style="flex:1;overflow-y:auto;display:flex;flex-direction:column;">
        <div style="display:flex;align-items:center;justify-content:center;flex:1;padding:24px;">
          <div class="text-label text-text-tertiary">중앙에서 지적사항을 선택하세요</div>
        </div>
        <div style="display:flex;align-items:center;justify-content:center;padding:24px;border-top:1px dashed var(--bd);">
          <div class="text-label text-text-tertiary">점검을 먼저 선택하세요</div>
        </div>
        <div style="display:flex;align-items:center;justify-content:center;padding:24px;border-top:1px dashed var(--bd);">
          <div class="text-label text-text-tertiary">항목을 불러오지 못했습니다.</div>
        </div>
      </div>
    </div>
    ```

    **카피 verbatim (memory `feedback_sketch_realistic_data`):**
    - '소방 점검 관리' (line 514 모바일 타이틀)
    - '소방 점검 관리 이력 없음' (line 548 모바일 빈 제목)
    - '소방 일정 페이지에서 종합정밀 또는 작동기능 점검을 등록하면 여기에 표시됩니다.' (line 549 모바일 빈 보조)
    - '목록을 불러오지 못했습니다.' (line 542 모바일 오류)
    - '다시 시도' (line 543 모바일 오류 button)
    - '불러오기 실패' (line 431 데스크톱 좌측 오류)
    - '재시도' (line 432 데스크톱 좌측 오류 button)
    - '점검 이력 없음' (line 436 데스크톱 좌측 빈)
    - '좌측에서 점검을 선택하세요' (line 484 데스크톱 중앙 fallback)
    - '중앙에서 지적사항을 선택하세요' (line 494 데스크톱 우측 fallback, selectedRoundId 있을 때)
    - '점검을 먼저 선택하세요' (line 494 데스크톱 우측 fallback, selectedRoundId 없을 때)
    - '지적사항 없음' (line 184 FindingsPanel 빈)
    - '항목을 불러오지 못했습니다.' (line 292 FindingDetailPanel 빈)

    **rules 박스 (sketch 하단, viewport-label 외 메타):**
    - LOCKED 결정: OQ #1 (모바일 자체 헤더 `bg-surface-raised border-b border-border-default`, 옛 `rgba(22,27,34,0.97)` 인라인 폐기) + OQ #4 (빈/오류 아이콘 무 카피만) + OQ #5 (Lucide ChevronLeft size={20} 교체 + back button 44x44 격상 = w-11 h-11) verbatim 인용 1 fence
    - **비즈 anchor 17건 박제 fence** (full list — rules_verbatim 박제 list 그대로 인용, sketch 본문 안 fence 안)
    - 토큰 매핑 (영역: 모바일 헤더 + 데스크톱 outline 영역): `var(--bg) → bg-surface-page` / `var(--bg2) → bg-surface-raised` (모바일 헤더, OQ #1) / `var(--bg3) → bg-surface-sunken` (SKELETON) / `var(--bd) → border-border-default` (모바일 헤더 borderBottom + 데스크톱 좌/중 borderRight) / `var(--t1) → text-text-primary` (모바일 타이틀 + 빈 제목) / `var(--t2) → text-text-secondary` (back button + 빈 보조 + 오류 카피) / `var(--t3) → text-text-tertiary` (데스크톱 fallback + 빈)
    - negative gate 자체매칭 회피: "이모지 0 / linear-gradient 0 / 9·10·11px 0 / status- prefix 0 / w-8 h-8 0 / 옛 alias 토큰 0" 메타 줄은 `<code>` fence 안 또는 HTML entity escape (sketch 본문 안 직접 등장 금지)

    **commit 명령 (executor 가 실행 후 commit hash + message 보고):**
    ```
    git add cha-bio-safety/docs/redesign-context/19-legal/sketch-wave-2-chrome.html
    git commit -m "docs(quick-260523-40p): redesign/19-legal W2 chrome sketch (4 frame, bg-surface-raised 모바일 헤더 + 데스크톱 3분할 outline + 빈/로딩/오류 5 state + Lucide ChevronLeft 44x44 + 비즈 anchor 박제)"
    ```
  </action>
  <verify>
    <automated>
test -f cha-bio-safety/docs/redesign-context/19-legal/sketch-wave-2-chrome.html \
  && wc -l cha-bio-safety/docs/redesign-context/19-legal/sketch-wave-2-chrome.html | awk '{ if ($1 < 200) { print "FAIL: too short"; exit 1 } else print "PASS lines:", $1 }' \
  && grep -c 'data-theme="dark"' cha-bio-safety/docs/redesign-context/19-legal/sketch-wave-2-chrome.html | awk '{ if ($1 < 3) { print "FAIL: dark frame <3"; exit 1 } else print "PASS dark frame:", $1 }' \
  && grep -c 'data-theme="light"' cha-bio-safety/docs/redesign-context/19-legal/sketch-wave-2-chrome.html | awk '{ if ($1 < 1) { print "FAIL: light frame <1"; exit 1 } else print "PASS light frame:", $1 }' \
  && grep -c 'bg-surface-page' cha-bio-safety/docs/redesign-context/19-legal/sketch-wave-2-chrome.html | awk '{ if ($1 < 4) { print "FAIL: bg-surface-page <4"; exit 1 } else print "PASS bg-surface-page:", $1 }' \
  && grep -c 'bg-surface-raised' cha-bio-safety/docs/redesign-context/19-legal/sketch-wave-2-chrome.html | awk '{ if ($1 < 3) { print "FAIL: bg-surface-raised <3 (OQ #1 모바일 헤더)"; exit 1 } else print "PASS bg-surface-raised:", $1 }' \
  && grep -c 'border-b border-border-default' cha-bio-safety/docs/redesign-context/19-legal/sketch-wave-2-chrome.html | awk '{ if ($1 < 3) { print "FAIL: border-b border-border-default <3 (OQ #1)"; exit 1 } else print "PASS border-b:", $1 }' \
  && grep -c '소방 점검 관리' cha-bio-safety/docs/redesign-context/19-legal/sketch-wave-2-chrome.html | awk '{ if ($1 < 3) { print "FAIL: 소방 점검 관리 타이틀 <3"; exit 1 } else print "PASS 소방 점검 관리:", $1 }' \
  && grep -cE 'Lucide ChevronLeft|<ChevronLeft|ChevronLeft size=' cha-bio-safety/docs/redesign-context/19-legal/sketch-wave-2-chrome.html | awk '{ if ($1 < 3) { print "FAIL: Lucide ChevronLeft anchor <3 (OQ #5)"; exit 1 } else print "PASS Lucide anchor:", $1 }' \
  && grep -cE 'w-11 h-11|w-\[44px\] h-\[44px\]|width:44px' cha-bio-safety/docs/redesign-context/19-legal/sketch-wave-2-chrome.html | awk '{ if ($1 < 3) { print "FAIL: back button 44x44 anchor <3 (OQ #5)"; exit 1 } else print "PASS 44x44 anchor:", $1 }' \
  && grep -c '소방 점검 관리 이력 없음' cha-bio-safety/docs/redesign-context/19-legal/sketch-wave-2-chrome.html | awk '{ if ($1 < 1) { print "FAIL: 모바일 빈 제목 missing"; exit 1 } else print "PASS 빈 제목:", $1 }' \
  && grep -c '소방 일정 페이지에서 종합정밀 또는 작동기능 점검을 등록하면' cha-bio-safety/docs/redesign-context/19-legal/sketch-wave-2-chrome.html | awk '{ if ($1 < 1) { print "FAIL: 모바일 빈 보조 missing"; exit 1 } else print "PASS 빈 보조:", $1 }' \
  && grep -c '목록을 불러오지 못했습니다' cha-bio-safety/docs/redesign-context/19-legal/sketch-wave-2-chrome.html | awk '{ if ($1 < 1) { print "FAIL: 모바일 오류 카피 missing"; exit 1 } else print "PASS 오류 카피:", $1 }' \
  && grep -c '좌측에서 점검을 선택하세요' cha-bio-safety/docs/redesign-context/19-legal/sketch-wave-2-chrome.html | awk '{ if ($1 < 1) { print "FAIL: 중앙 fallback missing"; exit 1 } else print "PASS 중앙 fallback:", $1 }' \
  && grep -c '중앙에서 지적사항을 선택하세요' cha-bio-safety/docs/redesign-context/19-legal/sketch-wave-2-chrome.html | awk '{ if ($1 < 1) { print "FAIL: 우측 fallback (selectedRoundId) missing"; exit 1 } else print "PASS 우측 fallback A:", $1 }' \
  && grep -c '점검을 먼저 선택하세요' cha-bio-safety/docs/redesign-context/19-legal/sketch-wave-2-chrome.html | awk '{ if ($1 < 1) { print "FAIL: 우측 fallback (no select) missing"; exit 1 } else print "PASS 우측 fallback B:", $1 }' \
  && grep -c '지적사항 없음' cha-bio-safety/docs/redesign-context/19-legal/sketch-wave-2-chrome.html | awk '{ if ($1 < 1) { print "FAIL: FindingsPanel 빈 missing"; exit 1 } else print "PASS FindingsPanel 빈:", $1 }' \
  && grep -c '항목을 불러오지 못했습니다' cha-bio-safety/docs/redesign-context/19-legal/sketch-wave-2-chrome.html | awk '{ if ($1 < 1) { print "FAIL: FindingDetailPanel 빈 missing"; exit 1 } else print "PASS FindingDetailPanel 빈:", $1 }' \
  && grep -c '@keyframes blink' cha-bio-safety/docs/redesign-context/19-legal/sketch-wave-2-chrome.html | awk '{ if ($1 < 1) { print "FAIL: blink keyframes missing"; exit 1 } else print "PASS blink keyframes:", $1 }' \
  && grep -cE 'animation:blink|animation: blink' cha-bio-safety/docs/redesign-context/19-legal/sketch-wave-2-chrome.html | awk '{ if ($1 < 3) { print "FAIL: SKELETON animation <3"; exit 1 } else print "PASS SKELETON animation:", $1 }' \
  && grep -cE 'height:72px|height: 72' cha-bio-safety/docs/redesign-context/19-legal/sketch-wave-2-chrome.html | awk '{ if ($1 < 3) { print "FAIL: SKELETON height 72 <3 (line 50 anchor)"; exit 1 } else print "PASS SKELETON height 72:", $1 }' \
  && grep -cE 'width:500px|w-\[500px\]' cha-bio-safety/docs/redesign-context/19-legal/sketch-wave-2-chrome.html | awk '{ if ($1 < 2) { print "FAIL: 데스크톱 3분할 500 width <2"; exit 1 } else print "PASS 500 width:", $1 }' \
  && grep -cE 'legalApi|useQuery|accentColor|ResultBadge|filterRounds|TABS|sorted|handleRoundClick|useMultiPhotoUpload|buildMetaTxt|@keyframes blink|@keyframes spin|role.*admin|toast' cha-bio-safety/docs/redesign-context/19-legal/sketch-wave-2-chrome.html | awk '{ if ($1 < 8) { print "FAIL: 비즈 anchor 17건 박제 hit <8"; exit 1 } else print "PASS 비즈 anchor hits:", $1 }' \
  && grep -cE 'linear-gradient' cha-bio-safety/docs/redesign-context/19-legal/sketch-wave-2-chrome.html | awk '{ if ($1 > 0) { print "FAIL: linear-gradient found (W2 W4 만 예외)"; exit 1 } else print "PASS no gradient" }' \
  && grep -cE '(text|bg|border)-status-(safe|fire|warning|danger|caution)' cha-bio-safety/docs/redesign-context/19-legal/sketch-wave-2-chrome.html | awk '{ if ($1 > 0) { print "FAIL: status- prefix found"; exit 1 } else print "PASS no status- prefix" }' \
  && grep -v '^<!--' cha-bio-safety/docs/redesign-context/19-legal/sketch-wave-2-chrome.html | grep -v '^#' | grep -cE 'font-size:[[:space:]]*(9|10|11)[^0-9]|fontSize:[[:space:]]*(9|10|11)[^0-9]' | awk '{ if ($1 > 0) { print "FAIL: 9/10/11 px found"; exit 1 } else print "PASS no 9-11px" }' \
  && grep -cE '\bw-8\b|\bh-8\b' cha-bio-safety/docs/redesign-context/19-legal/sketch-wave-2-chrome.html | awk '{ if ($1 > 0) { print "FAIL: w-8/h-8 found"; exit 1 } else print "PASS no w-8 h-8" }' \
  && grep -cE 'OQ #1|OQ #4|OQ #5' cha-bio-safety/docs/redesign-context/19-legal/sketch-wave-2-chrome.html | awk '{ if ($1 < 3) { print "FAIL: OQ #1/#4/#5 citation <3"; exit 1 } else print "PASS OQ citations:", $1 }' \
  && git log --oneline -1 | grep -q '260523-40p.*W2' && echo "PASS commit W2"
    </automated>
  </verify>
  <done>
    - sketch-wave-2-chrome.html 파일 존재 + ≥200 lines (≥320 권장)
    - data-theme="dark" ≥3 / data-theme="light" ≥1 (4 frame)
    - bg-surface-page ≥4 / bg-surface-raised (모바일 헤더) ≥3 (OQ #1)
    - border-b border-border-default ≥3 (모바일 헤더 borderBottom OQ #1)
    - '소방 점검 관리' 타이틀 ≥3 (모바일 헤더 3 frame)
    - Lucide ChevronLeft anchor ≥3 (OQ #5)
    - back button 44x44 anchor (w-11 h-11 또는 w-[44px] h-[44px] 또는 width:44px) ≥3 (OQ #5)
    - 빈/오류/fallback 카피 verbatim 모두 박제 (소방 점검 관리 이력 없음 / 소방 일정 페이지에서... / 목록을 불러오지 못했습니다 / 좌측에서 점검을 선택하세요 / 중앙에서 지적사항을 선택하세요 / 점검을 먼저 선택하세요 / 지적사항 없음 / 항목을 불러오지 못했습니다 — 각 ≥1, OQ #4)
    - @keyframes blink anchor ≥1 / SKELETON animation:blink ≥3 (frame 2 3 rows) / height:72px ≥3 (line 50 anchor)
    - 데스크톱 3분할 500 width ≥2 (좌+중)
    - 비즈 anchor 17건 박제 hit ≥8
    - linear-gradient 0건 (W4 만 예외) / status- prefix 0건 / 9·10·11px 0건 / w-8 h-8 0건
    - OQ #1 + OQ #4 + OQ #5 인용 ≥3건
    - atomic commit `docs(quick-260523-40p): ... W2 chrome sketch ...` 1개
  </done>
</task>


<task type="auto">
  <name>Task 2 (W3): LegalPage 라운드 카드 + 탭 + 연도 필터 + accentColor 4분기 + ResultBadge 4 라벨 sketch 생성 (4 frame, status 토큰 매트릭스)</name>
  <files>cha-bio-safety/docs/redesign-context/19-legal/sketch-wave-3-round-card.html</files>
  <action>
    23-education/sketch-wave-3-staff-card.html 패턴 mirror 로 새 sketch 파일 작성. LegalPage.tsx accentColor (line 27~32) + ResultBadge (line 35~47) + TABS (line 54~58) + filterRounds (line 59~63) + genYears (line 64~67) + roundList JSX (line 404~461) + 라운드 카드 데스크톱 (line 438~458, padding 10 + borderRadius 10 + selected 1.5px → border-2) + 라운드 카드 모바일 (line 552~567, padding 12 + borderRadius 12) 시각화. **OQ #2 LOCKED (accentColor 4분기 + ResultBadge 4 라벨 + finding 칩 status 토큰 매핑, status- prefix 없음)** + **OQ #3 LOCKED (§1.1 9·10·11 → 12 격상 + leading-none)** + **OQ #3-selected (border-2 border-accent, 데스크톱만)** 3건이 본 wave 의 핵심.

    **구조 (23-education o7b W3 mirror, 4 frame accentColor 매트릭스):**
    - `<!DOCTYPE html>` + `<head><title>redesign/19-legal — W3 round card + accentColor + ResultBadge</title>` + `<style>` 블록 (W2 와 동일 tokens + typography 인용)
    - `<body>` 안 4 frame side-by-side (accentColor 4 임계치 + 라이트):

      | frame | viewport | 시각화 |
      |---|---|---|
      | 1 | 모바일 다크 393×800 pass safe | `data-theme="dark"` + 모바일 헤더 + 탭 (전체/진행 중/완료) + 연도 select (`2026년`) + 라운드 카드 (accentColor pass = `border-l-[3px] border-safe-bar` + ResultBadge `bg-safe-bg text-safe '적합'` + title '2026 상반기 종합정밀점검' + 메타 `2026.03.15 · 지적 5건 · 완료 5건`) |
      | 2 | 모바일 다크 393×800 fail danger | `data-theme="dark"` + 동일 chrome + 라운드 카드 (accentColor fail = `border-l-[3px] border-danger-bar` + ResultBadge `bg-danger-bg text-danger '부적합'` + '2026 작동기능점검' + 메타 `2026.05.10 ~ 2026.05.15 · 지적 8건 · 완료 3건`) |
      | 3 | 데스크톱 다크 1440×900 conditional warning + selected | `data-theme="dark"` + 좌측 패널 (500 width) + 탭 데스크톱 h 38 (전체/진행 중 활성/완료) + 연도 select + 선택 카드 (OQ #3-selected `border-2 border-accent` + accentColor conditional = `border-l-[3px] border-warning-bar` + ResultBadge `bg-warning-bg text-warning '조건부적합'` + 다른 카드 + 우측 패널 placeholder (W4 영역) |
      | 4 | 모바일 라이트 393×800 null 결과 미입력 | `data-theme="light"` + 라운드 카드 (accentColor null = `border-l-[3px] border-border-strong` + ResultBadge `bg-transparent text-text-tertiary '결과 미입력'` + '2026 정기점검 예정' + 메타) |

    **라운드 카드 마크업 룰 — 데스크톱 (line 438~458 verbatim, OQ #2 + OQ #3-selected):**
    ```html
    <!-- 데스크톱 카드 (selected 분기) -->
    <div class="bg-surface-sunken border border-border-default border-l-[3px] border-warning-bar rounded-md" style="padding:10px;cursor:pointer;display:flex;flex-direction:column;gap:3px;">
      <!-- 상단: title + ResultBadge -->
      <div style="display:flex;align-items:center;justify-content:space-between;gap:8px;">
        <span class="text-label font-bold text-text-primary" style="flex:1;overflow:hidden;text-overflow:ellipsis;white-space:nowrap;">2026 상반기 종합정밀점검</span>
        <!-- ResultBadge conditional (OQ #2: bg-warning-bg text-warning + OQ #3: text-caption leading-none 격상) -->
        <span class="bg-warning-bg text-warning text-caption font-bold leading-none rounded-sm" style="padding:2px 8px;flex-shrink:0;">조건부적합</span>
      </div>
      <!-- 메타 (데스크톱 verbatim, '건' 없음) -->
      <div class="text-caption leading-none text-text-secondary">2026.03.15 · 지적 5 · 완료 3</div>
    </div>

    <!-- selected 카드 (OQ #3-selected: border-2 border-accent, 데스크톱만) -->
    <div class="bg-surface-sunken border-2 border-accent border-l-[3px] border-warning-bar rounded-md" style="padding:10px;cursor:pointer;">
      <!-- 내용 동일 -->
    </div>
    ```

    **라운드 카드 마크업 룰 — 모바일 (line 552~567 verbatim, OQ #2):**
    ```html
    <!-- 모바일 카드 (selected 분기 없음 — sub-route 진입) -->
    <div class="bg-surface-sunken border border-border-default border-l-[3px] border-safe-bar rounded-md" style="padding:12px;cursor:pointer;display:flex;flex-direction:column;gap:4px;">
      <div style="display:flex;align-items:center;justify-content:space-between;gap:8px;">
        <span class="text-body font-bold text-text-primary" style="flex:1;overflow:hidden;text-overflow:ellipsis;white-space:nowrap;">2026 상반기 종합정밀점검</span>
        <span class="bg-safe-bg text-safe text-caption font-bold leading-none rounded-sm" style="padding:2px 8px;flex-shrink:0;">적합</span>
      </div>
      <!-- 메타 (모바일 verbatim, '건' 있음 + endDate 분기) -->
      <div class="text-caption leading-none text-text-secondary">2026.03.15 · 지적 5건 · 완료 5건</div>
    </div>
    ```

    **탭 마크업 룰 (TABS line 54~58 verbatim, key/label mismatch 의도):**
    ```html
    <!-- 데스크톱 탭 h 38 (line 409~417) -->
    <div style="display:flex;flex-shrink:0;border-bottom:1px solid var(--bd);">
      <button class="text-caption font-bold leading-none text-text-tertiary" style="flex:1;height:38px;background:transparent;border:none;border-bottom:2px solid transparent;cursor:pointer;">전체</button>
      <button class="bg-surface-active text-caption font-bold leading-none text-text-primary" style="flex:1;height:38px;border:none;border-bottom:2px solid var(--acl);cursor:pointer;" data-key="미조치" data-label="진행 중">진행 중</button>
      <button class="text-caption font-bold leading-none text-text-tertiary" style="flex:1;height:38px;background:transparent;border:none;border-bottom:2px solid transparent;cursor:pointer;">완료</button>
    </div>
    <!-- key '미조치' / label '진행 중' mismatch verbatim — TABS line 56 의도된 디자인 -->

    <!-- 모바일 탭 h 44 (line 520~528) -->
    <div style="display:flex;flex-shrink:0;border-bottom:1px solid var(--bd);background:var(--bg2);">
      <button class="text-label font-bold text-text-tertiary" style="flex:1;height:44px;background:transparent;border:none;border-bottom:2px solid transparent;cursor:pointer;">전체</button>
      <button class="bg-surface-active text-label font-bold text-text-primary" style="flex:1;height:44px;border:none;border-bottom:2px solid var(--acl);cursor:pointer;">진행 중</button>
      <button class="text-label font-bold text-text-tertiary" style="flex:1;height:44px;background:transparent;border:none;border-bottom:2px solid transparent;cursor:pointer;">완료</button>
    </div>
    ```

    **연도 select 마크업 룰 (line 419~422 / 530~534 verbatim):**
    ```html
    <!-- 데스크톱 select (padding '4px 8px' + borderRadius 6 + fontSize 12 → text-caption leading-none) -->
    <select class="bg-surface-sunken border border-border-strong text-caption leading-none text-text-primary rounded-sm" style="padding:4px 8px;appearance:none;-webkit-appearance:none;">
      <option>2024년</option>
      <option>2025년</option>
      <option selected>2026년</option>
    </select>
    <!-- 모바일 select (padding '6px 12px' + borderRadius 8 + fontSize 13 → text-label) -->
    <select class="bg-surface-sunken border border-border-strong text-label text-text-primary rounded-sm" style="padding:6px 12px;appearance:none;-webkit-appearance:none;">
      <option>2024년</option>
      <option>2025년</option>
      <option selected>2026년</option>
    </select>
    ```

    **카피 + 데이터 verbatim (memory `feedback_sketch_realistic_data`):**
    - 탭 라벨 (TABS line 54~58 verbatim): '전체' / '진행 중' (key '미조치' mismatch) / '완료'
    - 연도 옵션: `${y}년` (line 421, 532 verbatim)
    - 라운드 title 예시: '2026 상반기 종합정밀점검' / '2026 작동기능점검' / '2026 정기점검 예정'
    - 메타 데스크톱: `${fmtDate(date)} · 지적 ${findingCount} · 완료 ${resolvedCount}` ('건' 없음, line 455 verbatim)
    - 메타 모바일: `${fmtDate(date)}${endDate ? ' ~ ' + fmtDate(endDate) : ''} · 지적 ${findingCount}건 · 완료 ${resolvedCount}건` ('건' 있음 + endDate 분기, line 564 verbatim)
    - ResultBadge 라벨 4종: '적합' (pass) / '부적합' (fail) / '조건부적합' (conditional) / '결과 미입력' (null) — line 37~44 verbatim

    **rules 박스:**
    - LOCKED 결정: OQ #2 (accentColor 4분기 borderLeft 토큰 — pass `border-l-[3px] border-safe-bar` / fail `border-l-[3px] border-danger-bar` / conditional `border-l-[3px] border-warning-bar` / null `border-l-[3px] border-border-strong`. ResultBadge 4 라벨 — pass `bg-safe-bg text-safe '적합'` / fail `bg-danger-bg text-danger '부적합'` / conditional `bg-warning-bg text-warning '조건부적합'` / null `bg-transparent text-text-tertiary '결과 미입력'`. **status- prefix 없음** + 4분기/4 라벨 1 byte 변경 금지) + OQ #3 (§1.1 9·10·11 → 12 격상 + leading-none — ResultBadge 11 + 데스크톱 탭 11 모두 `text-caption font-bold leading-none`) + OQ #3-selected (selected card `border-2 border-accent`, 1.5 → 2 데스크톱만) verbatim 인용 1 fence
    - **비즈 anchor 17건 박제 fence** (W2 와 동일 list)
    - 토큰 매핑: 카드 외곽 `var(--bg3) → bg-surface-sunken` / 평시 border `var(--bd) → border-border-default` / selected `var(--acl) → border-2 border-accent` (OQ #3-selected) / borderLeft accentColor OQ #2 LOCKED 4 토큰 / ResultBadge OQ #2 LOCKED 4 토큰 / 탭 활성 bg `var(--bg4) → bg-surface-active` + color `var(--t1) → text-text-primary` / 탭 비활성 color `var(--t3) → text-text-tertiary` / 탭 borderBottom 활성 `var(--acl) → border-accent` / 연도 select bg `var(--bg3) → bg-surface-sunken` + border `var(--bd2) → border-border-strong` + color `var(--t1) → text-text-primary`
    - negative gate 자체매칭 회피: HTML entity escape

    **commit 명령:**
    ```
    git add cha-bio-safety/docs/redesign-context/19-legal/sketch-wave-3-round-card.html
    git commit -m "docs(quick-260523-40p): redesign/19-legal W3 round-card sketch (4 frame, accentColor 4분기 + ResultBadge 4 라벨 + 탭 + 연도 + selected border-2 + 비즈 anchor 박제)"
    ```
  </action>
  <verify>
    <automated>
test -f cha-bio-safety/docs/redesign-context/19-legal/sketch-wave-3-round-card.html \
  && wc -l cha-bio-safety/docs/redesign-context/19-legal/sketch-wave-3-round-card.html | awk '{ if ($1 < 200) { print "FAIL: too short"; exit 1 } else print "PASS lines:", $1 }' \
  && grep -c 'data-theme="dark"' cha-bio-safety/docs/redesign-context/19-legal/sketch-wave-3-round-card.html | awk '{ if ($1 < 3) { print "FAIL: dark frame <3"; exit 1 } else print "PASS dark frame:", $1 }' \
  && grep -c 'data-theme="light"' cha-bio-safety/docs/redesign-context/19-legal/sketch-wave-3-round-card.html | awk '{ if ($1 < 1) { print "FAIL: light frame <1"; exit 1 } else print "PASS light frame:", $1 }' \
  && grep -c 'border-safe-bar' cha-bio-safety/docs/redesign-context/19-legal/sketch-wave-3-round-card.html | awk '{ if ($1 < 1) { print "FAIL: accentColor pass (border-safe-bar) <1 (OQ #2)"; exit 1 } else print "PASS pass accent:", $1 }' \
  && grep -c 'border-danger-bar' cha-bio-safety/docs/redesign-context/19-legal/sketch-wave-3-round-card.html | awk '{ if ($1 < 1) { print "FAIL: accentColor fail (border-danger-bar) <1 (OQ #2)"; exit 1 } else print "PASS fail accent:", $1 }' \
  && grep -c 'border-warning-bar' cha-bio-safety/docs/redesign-context/19-legal/sketch-wave-3-round-card.html | awk '{ if ($1 < 1) { print "FAIL: accentColor conditional (border-warning-bar) <1 (OQ #2)"; exit 1 } else print "PASS conditional accent:", $1 }' \
  && grep -c 'border-border-strong' cha-bio-safety/docs/redesign-context/19-legal/sketch-wave-3-round-card.html | awk '{ if ($1 < 1) { print "FAIL: accentColor null (border-border-strong) <1 (OQ #2)"; exit 1 } else print "PASS null accent:", $1 }' \
  && grep -c 'bg-safe-bg text-safe' cha-bio-safety/docs/redesign-context/19-legal/sketch-wave-3-round-card.html | awk '{ if ($1 < 1) { print "FAIL: ResultBadge pass <1 (OQ #2)"; exit 1 } else print "PASS ResultBadge pass:", $1 }' \
  && grep -c 'bg-danger-bg text-danger' cha-bio-safety/docs/redesign-context/19-legal/sketch-wave-3-round-card.html | awk '{ if ($1 < 1) { print "FAIL: ResultBadge fail <1 (OQ #2)"; exit 1 } else print "PASS ResultBadge fail:", $1 }' \
  && grep -c 'bg-warning-bg text-warning' cha-bio-safety/docs/redesign-context/19-legal/sketch-wave-3-round-card.html | awk '{ if ($1 < 1) { print "FAIL: ResultBadge conditional <1 (OQ #2)"; exit 1 } else print "PASS ResultBadge conditional:", $1 }' \
  && grep -c '적합\|부적합\|조건부적합\|결과 미입력' cha-bio-safety/docs/redesign-context/19-legal/sketch-wave-3-round-card.html | awk '{ if ($1 < 4) { print "FAIL: ResultBadge 라벨 4종 <4 verbatim (OQ #2)"; exit 1 } else print "PASS ResultBadge 라벨:", $1 }' \
  && grep -c 'border-2 border-accent' cha-bio-safety/docs/redesign-context/19-legal/sketch-wave-3-round-card.html | awk '{ if ($1 < 1) { print "FAIL: selected card border-2 border-accent <1 (frame 3 데스크톱, OQ #3-selected)"; exit 1 } else print "PASS selected:", $1 }' \
  && grep -c 'bg-surface-sunken' cha-bio-safety/docs/redesign-context/19-legal/sketch-wave-3-round-card.html | awk '{ if ($1 < 4) { print "FAIL: bg-surface-sunken (카드 외곽) <4"; exit 1 } else print "PASS bg-surface-sunken:", $1 }' \
  && grep -c '진행 중' cha-bio-safety/docs/redesign-context/19-legal/sketch-wave-3-round-card.html | awk '{ if ($1 < 2) { print "FAIL: 탭 라벨 진행 중 <2 (TABS key '\''미조치'\'' label mismatch)"; exit 1 } else print "PASS 탭 진행 중:", $1 }' \
  && grep -c '전체' cha-bio-safety/docs/redesign-context/19-legal/sketch-wave-3-round-card.html | awk '{ if ($1 < 2) { print "FAIL: 탭 라벨 전체 <2"; exit 1 } else print "PASS 탭 전체:", $1 }' \
  && grep -c '완료' cha-bio-safety/docs/redesign-context/19-legal/sketch-wave-3-round-card.html | awk '{ if ($1 < 2) { print "FAIL: 탭 라벨 완료 <2"; exit 1 } else print "PASS 탭 완료:", $1 }' \
  && grep -c '2026년\|2025년\|2024년' cha-bio-safety/docs/redesign-context/19-legal/sketch-wave-3-round-card.html | awk '{ if ($1 < 3) { print "FAIL: 연도 옵션 verbatim <3"; exit 1 } else print "PASS 연도 옵션:", $1 }' \
  && grep -c 'text-caption' cha-bio-safety/docs/redesign-context/19-legal/sketch-wave-3-round-card.html | awk '{ if ($1 < 5) { print "FAIL: text-caption <5 (OQ #3 격상 anchor)"; exit 1 } else print "PASS text-caption:", $1 }' \
  && grep -c 'leading-none' cha-bio-safety/docs/redesign-context/19-legal/sketch-wave-3-round-card.html | awk '{ if ($1 < 5) { print "FAIL: leading-none <5 (OQ #3)"; exit 1 } else print "PASS leading-none:", $1 }' \
  && grep -cE 'legalApi|useQuery|accentColor|ResultBadge|filterRounds|TABS|sorted|handleRoundClick|useMultiPhotoUpload|buildMetaTxt|@keyframes blink|role.*admin|toast' cha-bio-safety/docs/redesign-context/19-legal/sketch-wave-3-round-card.html | awk '{ if ($1 < 8) { print "FAIL: 비즈 anchor 17건 박제 hit <8"; exit 1 } else print "PASS 비즈 anchor hits:", $1 }' \
  && grep -cE 'linear-gradient' cha-bio-safety/docs/redesign-context/19-legal/sketch-wave-3-round-card.html | awk '{ if ($1 > 0) { print "FAIL: linear-gradient found (W3 W4 만 예외)"; exit 1 } else print "PASS no gradient" }' \
  && grep -cE '(text|bg|border)-status-(safe|fire|warning|danger|caution)' cha-bio-safety/docs/redesign-context/19-legal/sketch-wave-3-round-card.html | awk '{ if ($1 > 0) { print "FAIL: status- prefix found (OQ #2 위반)"; exit 1 } else print "PASS no status- prefix" }' \
  && grep -v '^<!--' cha-bio-safety/docs/redesign-context/19-legal/sketch-wave-3-round-card.html | grep -v '^#' | grep -cE 'font-size:[[:space:]]*(9|10|11)[^0-9]|fontSize:[[:space:]]*(9|10|11)[^0-9]' | awk '{ if ($1 > 0) { print "FAIL: 9/10/11 px found (OQ #3)"; exit 1 } else print "PASS no 9-11px" }' \
  && grep -cE '\bw-8\b|\bh-8\b' cha-bio-safety/docs/redesign-context/19-legal/sketch-wave-3-round-card.html | awk '{ if ($1 > 0) { print "FAIL: w-8/h-8 found"; exit 1 } else print "PASS no w-8 h-8" }' \
  && grep -cE 'OQ #2|OQ #3' cha-bio-safety/docs/redesign-context/19-legal/sketch-wave-3-round-card.html | awk '{ if ($1 < 2) { print "FAIL: OQ #2/#3 citation <2"; exit 1 } else print "PASS OQ citations:", $1 }' \
  && git log --oneline -1 | grep -q '260523-40p.*W3' && echo "PASS commit W3"
    </automated>
  </verify>
  <done>
    - sketch-wave-3-round-card.html 파일 존재 + ≥200 lines (≥320 권장)
    - data-theme="dark" ≥3 / data-theme="light" ≥1 (4 frame accentColor 매트릭스)
    - accentColor 4분기 모두 박제 — border-safe-bar ≥1 / border-danger-bar ≥1 / border-warning-bar ≥1 / border-border-strong ≥1 (OQ #2)
    - ResultBadge 4 라벨 verbatim — '적합' + '부적합' + '조건부적합' + '결과 미입력' 총 ≥4
    - ResultBadge 색 토큰 — bg-safe-bg text-safe ≥1 / bg-danger-bg text-danger ≥1 / bg-warning-bg text-warning ≥1 (OQ #2)
    - selected card `border-2 border-accent` ≥1 (frame 3 데스크톱만, OQ #3-selected)
    - bg-surface-sunken (카드 외곽) ≥4 / 탭 라벨 '진행 중' ≥2 (TABS mismatch verbatim) / '전체' ≥2 / '완료' ≥2
    - 연도 옵션 verbatim ≥3 / text-caption ≥5 (OQ #3) / leading-none ≥5 (OQ #3)
    - 비즈 anchor 17건 박제 hit ≥8
    - linear-gradient 0건 / **status- prefix 0건 (OQ #2)** / 9·10·11px 0건 / w-8 h-8 0건
    - OQ #2 + OQ #3 인용 ≥2건
    - atomic commit `docs(quick-260523-40p): ... W3 round-card sketch ...` 1개
  </done>
</task>


<task type="auto">
  <name>Task 3 (W4): LegalPage FindingsPanel + FindingDetailPanel 통합 sketch 생성 (4 frame, admin/assistant 분기 + open/resolved + 사진 5장 + 조치 완료 CTA 그라데이션 ≥3 + Lucide Camera+Loader2)</name>
  <files>cha-bio-safety/docs/redesign-context/19-legal/sketch-wave-4-findings-panel.html</files>
  <action>
    23-education/sketch-wave-4-edit-modal.html 패턴 mirror 로 새 sketch 파일 작성. LegalPage.tsx FindingsPanel (line 82~228, 데스크톱 중앙 — 헤더 + admin 도구 + sorted findings open-first + finding 카드 + 수정/삭제 + FindingFormSheet) + FindingDetailPanel (line 233~367, 데스크톱 우측 — 헤더 + admin 다운로드 + 지적 정보 KVRow 4 + 지적 사진 PhotoGrid + 조치 입력 textarea + 사진 5장 + 조치 완료 button + 조치 결과 KVRow 3) 통합 시각화. **OQ #4 LOCKED (조치 완료 CTA 인라인 그라데이션 `linear-gradient(135deg, #1d4ed8, #0ea5e9)` ≥3 박제 — W4 만 linear-gradient negative gate 예외 anchor)** + **OQ #5 LOCKED (Lucide Camera size={18} + Loader2 animate-spin 교체)** + role admin 도구 분기 + open/resolved 분기 + 사진 5장 PhotoGrid 시각이 본 wave 의 핵심.

    **구조 (23-education o7b W4 mirror, 4 frame admin/assistant + open/resolved 매트릭스):**
    - `<!DOCTYPE html>` + `<head><title>redesign/19-legal — W4 findings panels</title>` + `<style>` 블록 (W2 와 동일)
    - `<body>` 안 4 frame side-by-side:

      | frame | viewport | 시각화 |
      |---|---|---|
      | 1 | 다크 데스크톱 1440×900 admin role 평시 (open finding 상세) | `data-theme="dark"` + FindingsPanel (헤더 '2026 상반기 종합정밀점검' + admin 도구 select 4 옵션 + '저장' + '보고서 업로드' + sorted findings (open 먼저 — 카드 borderLeft danger + 칩 '미조치' / resolved 카드 borderLeft safe + 칩 '완료') + '수정' + '삭제' 액션) + FindingDetailPanel (헤더 '지적 상세' + admin '다운로드' + 지적 정보 KVRow 4 + PhotoGrid 지적 사진 3장 + 조치 입력 textarea + 사진 5장 (3 슬롯 + canAdd 첨부 button Lucide Camera size={18}) + **조치 완료 button width 100% h 44 인라인 `linear-gradient(135deg, #1d4ed8, #0ea5e9)` OQ #4 LOCKED anchor 1번째**) |
      | 2 | 다크 데스크톱 1440×900 assistant role (admin 도구 숨김) | `data-theme="dark"` + FindingsPanel (헤더 only — admin 도구 select+저장+보고서 미렌더, role !== 'admin' 분기 line 162) + FindingDetailPanel (헤더 '지적 상세' — admin 다운로드 button 숨김 line 299, assistant 분기) + 조치 입력 동일 + **조치 완료 button 인라인 그라데이션 OQ #4 LOCKED anchor 2번째** |
      | 3 | 다크 모바일 393×800 sub-route 위임 안내 (FindingsPanel/FindingDetailPanel 미렌더) | `data-theme="dark"` + 모바일 헤더 + 안내 영역 ('모바일에서는 카드를 탭하면 별도 페이지로 이동합니다' + `<Lucide ChevronLeft />` 아이콘 시각화 + `/legal/:id` sub-route 박제) — **본 frame 만 linear-gradient 미사용** + handleRoundClick isDesktop 분기 (line 394~401 verbatim 박제) |
      | 4 | 라이트 데스크톱 1440×900 결과 입력 + 보고서 업로드 (admin) | `data-theme="light"` + FindingsPanel (헤더 + admin 도구 — select 'fail' 선택 + '저장' button + '보고서' 열기 (reportFileKey 있을 때) + FindingFormSheet 수정 모달 placeholder line 217~225) + FindingDetailPanel (resolved finding 표시 — 조치 결과 KVRow 3 ('조치일시' / '조치자' / '조치 내용') + 조치 사진 PhotoGrid + `<Lucide Loader2 className="animate-spin" size={24} />` 로딩 spinner anchor OQ #5) + **조치 완료 button 인라인 그라데이션 OQ #4 LOCKED anchor 3번째** (다른 finding 으로 진입 시) |

    **FindingsPanel admin 도구 마크업 룰 (frame 1/4 — line 162~178 verbatim, OQ #3 격상):**
    ```html
    <!-- admin 분기: role === 'admin' (line 162) -->
    <div style="padding:0 16px 8px;display:flex;gap:6px;flex-wrap:wrap;">
      <!-- 결과 select 4 옵션 (line 164~168 verbatim, OQ #3 격상 11 → text-caption 12 leading-none) -->
      <select class="bg-surface-sunken border border-border-strong text-caption font-bold leading-none text-text-primary rounded-sm" style="padding:4px 8px;appearance:none;-webkit-appearance:none;">
        <option value="">미입력</option>
        <option value="pass">적합</option>
        <option value="fail">부적합</option>
        <option value="conditional">조건부적합</option>
      </select>
      <!-- 저장 button (line 170 solid bg-accent, OQ #4 작은 도구 solid 유지) -->
      <button class="bg-accent text-text-on-accent text-caption font-bold leading-none rounded-sm" style="height:28px;padding:0 10px;border:none;cursor:pointer;">저장</button>
      <!-- 보고서 button (line 173/175, reportFileKey 분기) -->
      <button class="bg-surface-sunken border border-border-strong text-caption font-bold leading-none text-text-primary rounded-sm" style="height:28px;padding:0 10px;cursor:pointer;">보고서 업로드</button>
    </div>
    ```

    **sorted findings 카드 마크업 룰 (line 187~211 verbatim, OQ #2 status borderLeft + 칩):**
    ```html
    <!-- open finding (sorted 먼저, line 147~151 verbatim 정렬 룰) — borderLeft danger + 칩 '미조치' -->
    <div class="bg-surface-sunken border border-border-default border-l-[3px] border-danger-bar rounded-md" style="padding:10px;cursor:pointer;display:flex;flex-direction:column;gap:2px;">
      <div style="display:flex;align-items:center;justify-content:space-between;gap:8px;">
        <span class="text-label text-text-primary" style="flex:1;overflow:hidden;text-overflow:ellipsis;white-space:nowrap;">계단실 비상조명등 점등불량</span>
        <!-- 상태 칩 open (OQ #2: bg-danger-bg text-danger, OQ #3 격상 leading-none) -->
        <span class="bg-danger-bg text-danger text-caption font-bold leading-none rounded-sm" style="padding:1px 6px;flex-shrink:0;">미조치</span>
      </div>
      <span class="text-caption leading-none text-text-secondary">3F 비상계단실</span>
      <!-- 메타 + 액션 (line 203~211 verbatim) -->
      <div style="display:flex;align-items:center;justify-content:space-between;">
        <span class="text-caption leading-none text-text-tertiary">2026.03.15</span>
        <div style="display:flex;gap:4px;">
          <button class="text-caption leading-none text-text-tertiary" style="padding:1px 3px;background:none;border:none;cursor:pointer;">수정</button>
          <button class="text-caption leading-none text-text-tertiary" style="padding:1px 3px;background:none;border:none;cursor:pointer;">삭제</button>
        </div>
      </div>
    </div>

    <!-- resolved finding — borderLeft safe + 칩 '완료' -->
    <div class="bg-surface-sunken border border-border-default border-l-[3px] border-safe-bar rounded-md" style="padding:10px;">
      <!-- 동일 구조 + 칩 `bg-safe-bg text-safe '완료'` -->
      <span class="bg-safe-bg text-safe text-caption font-bold leading-none rounded-sm" style="padding:1px 6px;">완료</span>
    </div>
    ```

    **FindingDetailPanel — 헤더 + admin 다운로드 (line 296~302 verbatim):**
    ```html
    <div style="display:flex;align-items:center;justify-content:space-between;margin-bottom:16px;">
      <span class="text-body-sm font-bold text-text-primary">지적 상세</span>
      <!-- admin 분기: staff?.role === 'admin' (line 299) -->
      <button class="bg-surface-sunken border border-border-strong text-caption font-bold leading-none text-text-primary rounded-sm" style="height:28px;padding:0 10px;cursor:pointer;">다운로드</button>
    </div>
    ```

    **FindingDetailPanel — 지적 정보 KVRow 4 (line 305~313 verbatim):**
    ```html
    <div style="margin-bottom:16px;">
      <div class="text-caption leading-none font-bold text-text-tertiary" style="margin-bottom:8px;">지적 정보</div>
      <!-- KVRow 4건 (라벨 width 64, gap 12, OQ #3 격상) -->
      <div style="display:flex;flex-direction:column;gap:8px;">
        <div style="display:flex;gap:12px;align-items:flex-start;"><span class="text-caption leading-none text-text-tertiary" style="width:64px;flex-shrink:0;">지적 내용</span><span class="text-label text-text-primary" style="flex:1;line-height:1.5;">계단실 비상조명등 점등불량 (전구 교체 필요)</span></div>
        <div style="display:flex;gap:12px;align-items:flex-start;"><span class="text-caption leading-none text-text-tertiary" style="width:64px;flex-shrink:0;">위치</span><span class="text-label text-text-primary" style="flex:1;">3F 비상계단실</span></div>
        <div style="display:flex;gap:12px;align-items:flex-start;"><span class="text-caption leading-none text-text-tertiary" style="width:64px;flex-shrink:0;">등록일</span><span class="text-label text-text-primary" style="flex:1;">2026.03.15 14:32</span></div>
        <div style="display:flex;gap:12px;align-items:flex-start;"><span class="text-caption leading-none text-text-tertiary" style="width:64px;flex-shrink:0;">등록자</span><span class="text-label text-text-primary" style="flex:1;">윤종엽</span></div>
      </div>
    </div>
    ```

    **조치 입력 영역 마크업 룰 (line 322~349 verbatim, open finding 만, OQ #4 + OQ #5):**
    ```html
    <!-- 조치 입력 borderTop (line 322) -->
    <div class="border-t border-border-default" style="padding-top:16px;">
      <!-- 조치 내용 라벨 -->
      <div class="text-caption leading-none font-bold text-text-tertiary" style="margin-bottom:8px;">조치 내용</div>
      <!-- textarea (line 325 verbatim placeholder) -->
      <textarea class="bg-surface-sunken border border-border-strong text-label text-text-primary rounded-md" rows="3" placeholder="조치 내용을 입력하세요" style="width:100%;padding:10px 12px;line-height:1.5;resize:vertical;outline:none;box-sizing:border-box;font-family:inherit;"></textarea>

      <!-- 조치 사진 라벨 (line 327 verbatim '조치 사진 (최대 5장)') -->
      <div class="text-caption leading-none font-bold text-text-tertiary" style="margin-top:12px;margin-bottom:8px;">조치 사진 (최대 5장)</div>

      <!-- 사진 슬롯 + 첨부 button (line 332~342, useMultiPhotoUpload canAdd < 5) -->
      <div style="display:flex;gap:8px;flex-wrap:wrap;">
        <!-- 슬롯 3장 (64x64 + ✕ 18x18) -->
        <div style="position:relative;width:64px;height:64px;"><div class="bg-surface-sunken rounded-sm" style="width:64px;height:64px;background-image:url(data:image/svg+xml;base64,...);background-size:cover;"></div><button style="position:absolute;top:-4px;right:-4px;width:18px;height:18px;background:var(--danger);color:#fff;border:none;border-radius:50%;font-size:10px;cursor:pointer;">✕</button></div>
        <div style="position:relative;width:64px;height:64px;"><div class="bg-surface-sunken rounded-sm" style="width:64px;height:64px;"></div><button style="position:absolute;top:-4px;right:-4px;width:18px;height:18px;background:var(--danger);color:#fff;border:none;border-radius:50%;">✕</button></div>
        <div style="position:relative;width:64px;height:64px;"><div class="bg-surface-sunken rounded-sm" style="width:64px;height:64px;"></div><button style="position:absolute;top:-4px;right:-4px;width:18px;height:18px;background:var(--danger);color:#fff;border:none;border-radius:50%;">✕</button></div>
        <!-- canAdd 첨부 button (OQ #5 LOCKED: Lucide Camera size={18}, '📷' 이모지 폐기) -->
        <button class="bg-surface-sunken border border-dashed border-border-strong text-text-tertiary rounded-sm" style="width:64px;height:64px;display:flex;flex-direction:column;align-items:center;justify-content:center;gap:2px;cursor:pointer;" aria-label="Lucide Camera size={18}">
          <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M14.5 4h-5L7 7H4a2 2 0 0 0-2 2v9a2 2 0 0 0 2 2h16a2 2 0 0 0 2-2V9a2 2 0 0 0-2-2h-3l-2.5-3z"/><circle cx="12" cy="13" r="3"/></svg>
          <span class="text-caption leading-none font-bold">첨부</span>
        </button>
      </div>

      <!-- 조치 완료 button (OQ #4 LOCKED: 인라인 그라데이션 anchor, W4 만 linear-gradient 예외, line 346 verbatim) -->
      <button class="text-text-on-accent text-label font-bold rounded-md" style="width:100%;height:44px;border:none;cursor:pointer;margin-top:12px;background:linear-gradient(135deg, #1d4ed8, #0ea5e9);">조치 완료</button>
    </div>
    ```

    **조치 결과 영역 마크업 룰 (frame 4 resolved finding — line 352~364 verbatim):**
    ```html
    <!-- resolved finding — 조치 결과 KVRow 3 + PhotoGrid -->
    <div class="border-t border-border-default" style="padding-top:16px;">
      <div class="text-caption leading-none font-bold text-text-tertiary" style="margin-bottom:8px;">조치 결과</div>
      <div style="display:flex;flex-direction:column;gap:8px;">
        <div style="display:flex;gap:12px;"><span class="text-caption leading-none text-text-tertiary" style="width:64px;flex-shrink:0;">조치일시</span><span class="text-label text-text-primary" style="flex:1;">2026.04.10 09:15</span></div>
        <div style="display:flex;gap:12px;"><span class="text-caption leading-none text-text-tertiary" style="width:64px;flex-shrink:0;">조치자</span><span class="text-label text-text-primary" style="flex:1;">김재선</span></div>
        <div style="display:flex;gap:12px;"><span class="text-caption leading-none text-text-tertiary" style="width:64px;flex-shrink:0;">조치 내용</span><span class="text-label text-text-primary" style="flex:1;white-space:pre-wrap;">전구 교체 완료 (LED 12W 5개)</span></div>
      </div>
      <!-- PhotoGrid 조치 사진 -->
      <div style="margin-top:12px;display:flex;gap:8px;flex-wrap:wrap;">
        <div class="bg-surface-sunken rounded-sm" style="width:64px;height:64px;"></div>
      </div>
    </div>

    <!-- FindingDetailPanel 로딩 (frame 4 placeholder, OQ #5: Lucide Loader2 animate-spin 교체, line 291 div spinner + @keyframes spin 폐기) -->
    <div style="display:flex;align-items:center;justify-content:center;padding:24px;" aria-label="Lucide Loader2 animate-spin size={24}">
      <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" style="animation:spin 1s linear infinite;color:var(--acl);"><path d="M21 12a9 9 0 1 1-6.219-8.56"/></svg>
    </div>
    ```

    **모바일 frame 3 sub-route 위임 안내 (handleRoundClick isDesktop 분기 line 394~401 verbatim):**
    ```html
    <!-- 모바일 frame: FindingsPanel + FindingDetailPanel 미렌더 (sub-route /legal/:id 위임) -->
    <div style="display:flex;flex-direction:column;align-items:center;justify-content:center;flex:1;text-align:center;padding:60px 16px;gap:12px;">
      <svg width="48" height="48" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" style="color:var(--t3);"><polyline points="9 18 15 12 9 6"/></svg>
      <div class="text-body font-bold text-text-primary">모바일에서는 카드를 탭하면 별도 페이지로 이동합니다</div>
      <div class="text-caption leading-none text-text-secondary">handleRoundClick → navigate('/legal/' + round.id) 분기 (line 394~401, isDesktop false)</div>
      <div class="text-caption leading-none text-text-tertiary">sub-route /legal/:id (LegalFindingsPage) + /legal/:id/finding/:fid (LegalFindingDetailPage)는 별도 wave 에서 다룸</div>
    </div>
    ```

    **카피 + 데이터 verbatim (memory `feedback_sketch_realistic_data`):**
    - FindingsPanel 헤더 round?.title fallback '지적사항 목록' (line 157 verbatim, frame 에는 실제 title '2026 상반기 종합정밀점검' 사용)
    - admin 결과 select 4 옵션 verbatim: '미입력' / '적합' / '부적합' / '조건부적합' (line 165~168)
    - admin 저장 button '저장' (line 170) / 보고서 button '보고서 업로드' / '보고서' (열기) / '...' (uploadingReport) (line 173, 175)
    - finding 상태 칩 '미조치' (open) / '완료' (resolved) (line 200 verbatim)
    - finding 액션 '수정' / '삭제' (line 209, 210 verbatim)
    - finding location fallback '위치 미지정' (line 202 verbatim)
    - FindingDetailPanel 헤더 '지적 상세' (line 298 verbatim) / 다운로드 button '다운로드' / '...' (line 300)
    - 섹션 라벨 verbatim: '지적 정보' (line 306) / '지적 사진' (line 317) / '조치 내용' (line 324) / '조치 결과' (line 354)
    - KVRow 라벨 verbatim (지적 정보 4): '지적 내용' / '위치' / '등록일' / '등록자' (line 308~311)
    - KVRow 라벨 verbatim (조치 결과 3): '조치일시' / '조치자' / '조치 내용' (line 356~358)
    - 조치 textarea placeholder '조치 내용을 입력하세요' (line 325 verbatim, toast.error '조치 내용을 입력하세요' line 345 와 동일 카피)
    - 조치 사진 라벨 '조치 사진 (최대 5장)' (line 327 verbatim)
    - 조치 완료 button '조치 완료' / '처리 중...' (isSubmitting, line 346 verbatim)
    - 첨부 button '첨부' (line 340 verbatim, '📷' 이모지 → Lucide Camera 교체 OQ #5)
    - 사진 없음 '사진 없음' (line 318 verbatim, photoKeys 빈)

    **rules 박스:**
    - LOCKED 결정: **OQ #4 (조치 완료 button 인라인 `linear-gradient(135deg, #1d4ed8, #0ea5e9)` — W4 만 linear-gradient negative gate 예외 anchor ≥3 박제, admin 작은 도구 button (저장/보고서/다운로드 h 28) + 다시 시도 button = solid `bg-accent text-text-on-accent` 유지)** + OQ #5 (Lucide Camera size={18} 교체 + Lucide Loader2 animate-spin size={24} 교체, 옛 '📷' + div spinner + @keyframes spin 폐기) + role admin/assistant 도구 분기 (FindingsPanel role === 'admin' line 162 + FindingDetailPanel staff?.role === 'admin' line 299) verbatim 인용 1 fence
    - **비즈 anchor 17건 박제 fence** + useMutation resolveMutation + useMultiPhotoUpload 5장 + buildMetaTxt + fflate ZIP + toast 11종 emphasis
    - 토큰 매핑: finding 카드 bg `var(--bg3) → bg-surface-sunken` / 평시 border `var(--bd) → border-border-default` / selected `var(--acl) → border-2 border-accent` (OQ #3-selected) / borderLeft open `var(--danger) → border-l-[3px] border-danger-bar` / borderLeft resolved `var(--safe) → border-l-[3px] border-safe-bar` (OQ #2 LOCKED) / 칩 open `bg-danger-bg text-danger` / 칩 resolved `bg-safe-bg text-safe` (OQ #2 LOCKED) / admin select bg `var(--bg3) → bg-surface-sunken` + border `var(--bd2) → border-border-strong` / admin 저장 button bg `var(--acl) → bg-accent` solid / 보고서/다운로드 button bg `var(--bg3) → bg-surface-sunken` + border `var(--bd2) → border-border-strong` / textarea bg `var(--bg3) → bg-surface-sunken` / 첨부 button border-dashed `var(--bd2) → border-dashed border-border-strong` / **조치 완료 button 인라인 그라데이션 OQ #4 LOCKED (W4 만 예외)**
    - negative gate 자체매칭 회피: fence escape — 단 `linear-gradient(135deg, #1d4ed8, #0ea5e9)` 그라데이션 본문 ≥3 박제 (OQ #4 LOCKED 예외)

    **commit 명령:**
    ```
    git add cha-bio-safety/docs/redesign-context/19-legal/sketch-wave-4-findings-panel.html
    git commit -m "docs(quick-260523-40p): redesign/19-legal W4 findings-panel sketch (4 frame, FindingsPanel + FindingDetailPanel 통합 + role admin 분기 + 사진 5장 + 조치 완료 CTA 그라데이션 ≥3 + Lucide Camera + Loader2 + 비즈 anchor 박제)"
    ```
  </action>
  <verify>
    <automated>
test -f cha-bio-safety/docs/redesign-context/19-legal/sketch-wave-4-findings-panel.html \
  && wc -l cha-bio-safety/docs/redesign-context/19-legal/sketch-wave-4-findings-panel.html | awk '{ if ($1 < 200) { print "FAIL: too short"; exit 1 } else print "PASS lines:", $1 }' \
  && grep -c 'data-theme="dark"' cha-bio-safety/docs/redesign-context/19-legal/sketch-wave-4-findings-panel.html | awk '{ if ($1 < 3) { print "FAIL: dark frame <3"; exit 1 } else print "PASS dark frame:", $1 }' \
  && grep -c 'data-theme="light"' cha-bio-safety/docs/redesign-context/19-legal/sketch-wave-4-findings-panel.html | awk '{ if ($1 < 1) { print "FAIL: light frame <1"; exit 1 } else print "PASS light frame:", $1 }' \
  && grep -c 'linear-gradient(135deg, #1d4ed8, #0ea5e9)' cha-bio-safety/docs/redesign-context/19-legal/sketch-wave-4-findings-panel.html | awk '{ if ($1 < 3) { print "FAIL: 조치 완료 그라데이션 <3 (OQ #4 LOCKED W4 만 예외 anchor)"; exit 1 } else print "PASS CTA gradient:", $1 }' \
  && grep -cE 'Lucide Camera|<Camera|Camera size=' cha-bio-safety/docs/redesign-context/19-legal/sketch-wave-4-findings-panel.html | awk '{ if ($1 < 1) { print "FAIL: Lucide Camera anchor <1 (OQ #5)"; exit 1 } else print "PASS Camera anchor:", $1 }' \
  && grep -cE 'Lucide Loader2|<Loader2|Loader2 ' cha-bio-safety/docs/redesign-context/19-legal/sketch-wave-4-findings-panel.html | awk '{ if ($1 < 1) { print "FAIL: Lucide Loader2 anchor <1 (OQ #5)"; exit 1 } else print "PASS Loader2 anchor:", $1 }' \
  && grep -c '미입력\|적합\|부적합\|조건부적합' cha-bio-safety/docs/redesign-context/19-legal/sketch-wave-4-findings-panel.html | awk '{ if ($1 < 4) { print "FAIL: admin select 4 옵션 <4 verbatim"; exit 1 } else print "PASS admin select:", $1 }' \
  && grep -c '저장\|보고서\|다운로드' cha-bio-safety/docs/redesign-context/19-legal/sketch-wave-4-findings-panel.html | awk '{ if ($1 < 3) { print "FAIL: admin 도구 button verbatim <3"; exit 1 } else print "PASS admin 도구:", $1 }' \
  && grep -c '미조치' cha-bio-safety/docs/redesign-context/19-legal/sketch-wave-4-findings-panel.html | awk '{ if ($1 < 1) { print "FAIL: finding 칩 open 미조치 missing"; exit 1 } else print "PASS 칩 open:", $1 }' \
  && grep -c '완료' cha-bio-safety/docs/redesign-context/19-legal/sketch-wave-4-findings-panel.html | awk '{ if ($1 < 1) { print "FAIL: finding 칩 resolved 완료 missing"; exit 1 } else print "PASS 칩 resolved:", $1 }' \
  && grep -c '지적 상세' cha-bio-safety/docs/redesign-context/19-legal/sketch-wave-4-findings-panel.html | awk '{ if ($1 < 1) { print "FAIL: 지적 상세 헤더 missing (line 298)"; exit 1 } else print "PASS 지적 상세:", $1 }' \
  && grep -c '지적 내용\|지적 정보\|지적 사진\|조치 내용\|조치 결과' cha-bio-safety/docs/redesign-context/19-legal/sketch-wave-4-findings-panel.html | awk '{ if ($1 < 5) { print "FAIL: 섹션 라벨 + KVRow 라벨 <5 verbatim"; exit 1 } else print "PASS 섹션 라벨:", $1 }' \
  && grep -c '조치 내용을 입력하세요' cha-bio-safety/docs/redesign-context/19-legal/sketch-wave-4-findings-panel.html | awk '{ if ($1 < 1) { print "FAIL: textarea placeholder missing"; exit 1 } else print "PASS textarea placeholder:", $1 }' \
  && grep -c '조치 사진 (최대 5장)' cha-bio-safety/docs/redesign-context/19-legal/sketch-wave-4-findings-panel.html | awk '{ if ($1 < 1) { print "FAIL: 조치 사진 라벨 verbatim missing"; exit 1 } else print "PASS 조치 사진 라벨:", $1 }' \
  && grep -c '조치 완료' cha-bio-safety/docs/redesign-context/19-legal/sketch-wave-4-findings-panel.html | awk '{ if ($1 < 1) { print "FAIL: 조치 완료 button verbatim missing"; exit 1 } else print "PASS 조치 완료:", $1 }' \
  && grep -c '수정\|삭제' cha-bio-safety/docs/redesign-context/19-legal/sketch-wave-4-findings-panel.html | awk '{ if ($1 < 2) { print "FAIL: finding 액션 verbatim <2"; exit 1 } else print "PASS 액션:", $1 }' \
  && grep -c 'border-l-\[3px\] border-danger-bar' cha-bio-safety/docs/redesign-context/19-legal/sketch-wave-4-findings-panel.html | awk '{ if ($1 < 1) { print "FAIL: finding borderLeft open <1 (OQ #2)"; exit 1 } else print "PASS finding borderLeft open:", $1 }' \
  && grep -c 'border-l-\[3px\] border-safe-bar' cha-bio-safety/docs/redesign-context/19-legal/sketch-wave-4-findings-panel.html | awk '{ if ($1 < 1) { print "FAIL: finding borderLeft resolved <1 (OQ #2)"; exit 1 } else print "PASS finding borderLeft resolved:", $1 }' \
  && grep -c 'bg-danger-bg text-danger' cha-bio-safety/docs/redesign-context/19-legal/sketch-wave-4-findings-panel.html | awk '{ if ($1 < 1) { print "FAIL: 칩 open token <1 (OQ #2)"; exit 1 } else print "PASS 칩 open token:", $1 }' \
  && grep -c 'bg-safe-bg text-safe' cha-bio-safety/docs/redesign-context/19-legal/sketch-wave-4-findings-panel.html | awk '{ if ($1 < 1) { print "FAIL: 칩 resolved token <1 (OQ #2)"; exit 1 } else print "PASS 칩 resolved token:", $1 }' \
  && grep -cE 'role.*admin|admin.*role|staff\?.role|role === .admin' cha-bio-safety/docs/redesign-context/19-legal/sketch-wave-4-findings-panel.html | awk '{ if ($1 < 2) { print "FAIL: role admin 분기 anchor <2"; exit 1 } else print "PASS role admin:", $1 }' \
  && grep -c 'handleRoundClick\|isDesktop\|navigate.*legal' cha-bio-safety/docs/redesign-context/19-legal/sketch-wave-4-findings-panel.html | awk '{ if ($1 < 1) { print "FAIL: handleRoundClick isDesktop 분기 anchor <1"; exit 1 } else print "PASS handleRoundClick:", $1 }' \
  && grep -cE 'legalApi|useQuery|useMutation|resolveMutation|useMultiPhotoUpload|buildMetaTxt|fflate|ZIP|@keyframes spin|toast|sorted|filterRounds|accentColor|ResultBadge' cha-bio-safety/docs/redesign-context/19-legal/sketch-wave-4-findings-panel.html | awk '{ if ($1 < 8) { print "FAIL: 비즈 anchor 17건 박제 hit <8"; exit 1 } else print "PASS 비즈 anchor hits:", $1 }' \
  && grep -cE 'OQ #4|OQ #5' cha-bio-safety/docs/redesign-context/19-legal/sketch-wave-4-findings-panel.html | awk '{ if ($1 < 2) { print "FAIL: OQ #4/#5 citation <2"; exit 1 } else print "PASS OQ citations:", $1 }' \
  && grep -cE '(text|bg|border)-status-(safe|fire|warning|danger|caution)' cha-bio-safety/docs/redesign-context/19-legal/sketch-wave-4-findings-panel.html | awk '{ if ($1 > 0) { print "FAIL: status- prefix found"; exit 1 } else print "PASS no status- prefix" }' \
  && grep -v '^<!--' cha-bio-safety/docs/redesign-context/19-legal/sketch-wave-4-findings-panel.html | grep -v '^#' | grep -cE 'font-size:[[:space:]]*(9|10|11)[^0-9]|fontSize:[[:space:]]*(9|10|11)[^0-9]' | awk '{ if ($1 > 0) { print "FAIL: 9/10/11 px found"; exit 1 } else print "PASS no 9-11px" }' \
  && grep -cE '\bw-8\b|\bh-8\b' cha-bio-safety/docs/redesign-context/19-legal/sketch-wave-4-findings-panel.html | awk '{ if ($1 > 0) { print "FAIL: w-8/h-8 found"; exit 1 } else print "PASS no w-8 h-8" }' \
  && git log --oneline -1 | grep -q '260523-40p.*W4' && echo "PASS commit W4"
    </automated>
  </verify>
  <done>
    - sketch-wave-4-findings-panel.html 파일 존재 + ≥200 lines (≥320 권장)
    - data-theme="dark" ≥3 / data-theme="light" ≥1 (4 frame admin/assistant + open/resolved + sub-route 위임)
    - **조치 완료 CTA 그라데이션 `linear-gradient(135deg, #1d4ed8, #0ea5e9)` ≥3 박제 (OQ #4 LOCKED W4 예외 anchor, frame 1/2/4 조치 완료 button)**
    - Lucide Camera anchor ≥1 (OQ #5, 첨부 button '📷' 폐기 anchor) / Lucide Loader2 anchor ≥1 (OQ #5, div spinner + @keyframes spin 폐기 anchor)
    - admin select 4 옵션 verbatim ('미입력' / '적합' / '부적합' / '조건부적합') 총 ≥4
    - admin 도구 button verbatim ('저장' / '보고서' / '다운로드') 총 ≥3
    - finding 칩 verbatim ('미조치' open / '완료' resolved) 각 ≥1
    - FindingDetailPanel 헤더 '지적 상세' ≥1 + 섹션 라벨 + KVRow 라벨 (지적 내용 / 지적 정보 / 지적 사진 / 조치 내용 / 조치 결과) ≥5
    - textarea placeholder '조치 내용을 입력하세요' ≥1 / 조치 사진 라벨 '조치 사진 (최대 5장)' ≥1 / 조치 완료 button ≥1
    - finding 액션 ('수정' / '삭제') ≥2
    - finding borderLeft status 토큰 — `border-l-[3px] border-danger-bar` (open) ≥1 + `border-l-[3px] border-safe-bar` (resolved) ≥1 (OQ #2)
    - finding 칩 색 토큰 — `bg-danger-bg text-danger` ≥1 + `bg-safe-bg text-safe` ≥1 (OQ #2)
    - role admin 분기 anchor ≥2 (FindingsPanel role === 'admin' + FindingDetailPanel staff?.role === 'admin')
    - handleRoundClick isDesktop 분기 anchor ≥1 (모바일 frame 3 sub-route 위임 안내)
    - 비즈 anchor 17건 박제 hit ≥8
    - **OQ #4 + OQ #5 인용 ≥2건**
    - status- prefix 0건 / 9·10·11px 0건 / w-8 h-8 0건
    - atomic commit `docs(quick-260523-40p): ... W4 findings-panel sketch ...` 1개
  </done>
</task>


<task type="auto">
  <name>Task 4 (W5): LegalPage TSX 변환 verify checklist markdown 생성 (12 섹션, 571 lines 단일 atomic + 비즈 anchor 17건 + OQ LOCKED 5건 + 메모리 룰 12+ inline + 최종 src 변경 0건 verify gate)</name>
  <files>cha-bio-safety/docs/redesign-context/19-legal/wave-5-tsx-conversion-checklist.md</files>
  <action>
    23-education/wave-5-tsx-conversion-checklist.md 패턴 mirror 로 새 markdown 파일 작성 (sketch HTML 아님). LegalPage.tsx 571 lines 단일 atomic TSX 변환 wave 의 verify checklist. **본 PLAN 의 마지막 task 이므로 `src/pages/LegalPage.tsx` + `src/App.tsx` + 외부 6 + sub-route 2 = 총 10 src 파일 코드 변경 0건 확인 verify gate 추가.**

    **구조 (23-education o7b W5 mirror, 12 섹션):**

    `---` frontmatter:
    ```
    ---
    title: "redesign/19-legal — wave 5 (TSX 변환 verify checklist)"
    status: ready_for_tsx_wave
    created: 2026-05-23
    quick_id: 260523-40p
    branch: redesign/19-legal
    source_tsx: cha-bio-safety/src/pages/LegalPage.tsx
    source_tsx_lines: 571
    sketch_inputs:
      - cha-bio-safety/docs/redesign-context/19-legal/sketch-wave-2-chrome.html
      - cha-bio-safety/docs/redesign-context/19-legal/sketch-wave-3-round-card.html
      - cha-bio-safety/docs/redesign-context/19-legal/sketch-wave-4-findings-panel.html
    mirror_of: cha-bio-safety/docs/redesign-context/23-education/wave-5-tsx-conversion-checklist.md (260522-o7b) + cha-bio-safety/docs/redesign-context/28-splash/wave-5-tsx-conversion-checklist.md (260522-2q6)
    oq_locked_count: 5
    biz_anchor_count: 17
    memory_rules_inline: 12
    ---
    ```

    **§1. 변환 범위 (LegalPage.tsx 단일 atomic, 4 영역 통합)**
    - 571 lines 단일 파일 (상단 유틸 + 포맷터 + 스켈레톤 + 탭 + KVRow / FindingsPanel / FindingDetailPanel / 메인 LegalPage 4 영역 통합)
    - **atomic 1-commit 변환** (23-education o7b 591 lines + 28-splash 2q6 320 lines precedent)
    - components.css 신규 X (현재 tokens.css + typography.css 만 사용)
    - `src/App.tsx` 변경 X (PAGE_TITLES + MOBILE_NO_NAV_PATHS + Route 3개 + 특수 regex 모두 현재 유지)
    - 외부 6 파일 + sub-route 2 파일 변경 X (PhotoGrid / PhotoSourceModal / FindingFormSheet / useMultiPhotoUpload / findingDownload / api + LegalFindingsPage + LegalFindingDetailPage)
    - Lucide import 추가 1 줄 — `import { ChevronLeft, Camera, Loader2 } from 'lucide-react'` (OQ #5 LOCKED)

    **§2. 보존 — 비즈 anchor 17건 verbatim** (W2 sketch rules 박스의 17건 박제 fence verbatim 인용)

    **§3. 변환 매핑 (영역 1~4 verbatim)**
    - **영역 1 — 상단 유틸 / 포맷터 / 스켈레톤 / 탭 / KVRow** (line 1~77):
      - imports: useState / useRef / useNavigate+useSearchParams / useQuery+useMutation+useQueryClient / toast / legalApi / useIsDesktop / useAuthStore / useMultiPhotoUpload / PhotoGrid / PhotoSourceModal / FindingFormSheet / buildMetaTxt / type 모두 verbatim 유지 + **import 추가** `import { ChevronLeft, Camera, Loader2 } from 'lucide-react'` (OQ #5 LOCKED)
      - fmtDate / fmtDateTime (line 16~24) verbatim 유지
      - **accentColor (line 27~32) verbatim 유지** — `'var(--safe)'` / `'var(--danger)'` / `'var(--warn)'` / `'var(--bd2)'` 4분기 (1 byte 변경 금지, memory `feedback_inspection_unresolved_color` 일반화). TSX 변환 후 borderLeft 적용 위치 (line 445 데스크톱 카드 / 555 모바일 카드) 에서 `style={{ borderLeft: '3px solid ' + accentColor(round.result) }}` 인라인은 보존 OR Tailwind 매핑: `border-l-[3px] ${accentColor(round.result) === 'var(--safe)' ? 'border-safe-bar' : accentColor(round.result) === 'var(--danger)' ? 'border-danger-bar' : accentColor(round.result) === 'var(--warn)' ? 'border-warning-bar' : 'border-border-strong'}` (OQ #2 LOCKED)
      - **ResultBadge (line 35~47) — map 4 라벨 verbatim 유지** + 외곽 style 인라인 11/700 → `text-caption font-bold leading-none` (OQ #3 격상) + bg/color 토큰 치환 (OQ #2):
        ```tsx
        function ResultBadge({ result }: { result: LegalInspectionResult | null }) {
          const map: Record<string, { cls: string; label: string }> = {
            pass: { cls: 'bg-safe-bg text-safe', label: '적합' },
            fail: { cls: 'bg-danger-bg text-danger', label: '부적합' },
            conditional: { cls: 'bg-warning-bg text-warning', label: '조건부적합' },
          }
          const m = result ? map[result] : null
          return (
            <span className={`${m?.cls ?? 'bg-transparent text-text-tertiary'} text-caption font-bold leading-none rounded-sm`} style={{ padding: '2px 8px', flexShrink: 0 }}>
              {m?.label ?? '결과 미입력'}
            </span>
          )
        }
        ```
      - SKELETON (line 50) — height 72 + animation 'blink 2s ease-in-out infinite' verbatim (var(--bg3) → 인라인 유지 또는 className 'bg-surface-sunken')
      - TabKey + TABS verbatim (line 53~58, **key '미조치' label '진행 중' mismatch 1 byte 변경 금지**)
      - filterRounds verbatim (line 59~63, 3분기 변경 금지)
      - genYears verbatim
      - KVRow (line 70~77) — 라벨 12 var(--t3) → className `text-caption leading-none text-text-tertiary` + children 14 var(--t1) → className `text-label text-text-primary`
    - **영역 2 — FindingsPanel** (line 82~228): OQ #2 + OQ #3 적용
      - useQueryClient / useAuthStore / role / state verbatim 유지
      - useQuery × 2 (round + findings) — queryKey + queryFn + enabled 1 byte 변경 금지
      - handleSaveResult / handleReportUpload / handleDelete — toast 카피 verbatim 유지 ('점검 결과 저장' / '저장 실패' / '보고서 업로드 완료' / '업로드 실패' / '삭제됨' / err?.message ?? '삭제 실패')
      - sorted findings (line 147~151) — open-first 정렬 룰 verbatim 1 byte 변경 금지
      - 헤더 — round?.title fallback '지적사항 목록' verbatim
      - **admin 도구 (role === 'admin' 분기 line 162)** — select 4 옵션 verbatim + 저장/보고서 button. OQ #3 격상 (11 → text-caption leading-none):
        ```tsx
        {role === 'admin' && round && (
          <div style={{ padding: '0 16px 8px', display: 'flex', gap: 6, flexWrap: 'wrap' }}>
            <select className="bg-surface-sunken border border-border-strong text-caption font-bold leading-none text-text-primary rounded-sm" style={{ padding: '4px 8px', appearance: 'none' }} value={effectiveResult} onChange={e => setSelectedResult(e.target.value)}>
              <option value="">미입력</option>
              <option value="pass">적합</option>
              <option value="fail">부적합</option>
              <option value="conditional">조건부적합</option>
            </select>
            <button onClick={handleSaveResult} disabled={savingResult} className="bg-accent text-text-on-accent text-caption font-bold leading-none rounded-sm" style={{ height: 28, padding: '0 10px', border: 'none', cursor: 'pointer', opacity: savingResult ? 0.6 : 1 }}>저장</button>
            {/* 보고서 button (reportFileKey 분기) */}
          </div>
        )}
        ```
      - finding 카드 (line 187~211) — bg `var(--bg3) → bg-surface-sunken` / border `selectedFindingId === id ? 'border-2 border-accent' : 'border border-border-default'` (OQ #3-selected 1.5 → 2) / borderLeft (OQ #2): `border-l-[3px] ${status === 'open' ? 'border-danger-bar' : 'border-safe-bar'}` / 칩 (OQ #2): open `bg-danger-bg text-danger '미조치'` / resolved `bg-safe-bg text-safe '완료'` + OQ #3 격상 (10 → text-caption leading-none) / 액션 '수정' / '삭제' verbatim
      - 수정 모달 FindingFormSheet — mount 그대로 (props 변경 X)
    - **영역 3 — FindingDetailPanel** (line 233~367): OQ #4 + OQ #5 적용
      - useQuery getFinding + resolveMutation verbatim 유지 (4 키 invalidate 동일)
      - handleDownload (admin 다운로드 ZIP) verbatim 유지 (fflate + buildMetaTxt + 파일명 정규식)
      - **isLoading spinner (line 291) — Lucide Loader2 교체 (OQ #5)**:
        ```tsx
        if (isLoading) return (
          <div style={{ display: 'flex', alignItems: 'center', justifyContent: 'center', padding: 24 }}>
            <Loader2 className="animate-spin text-accent" size={24} />
          </div>
        )
        // 인라인 div + @keyframes spin 모두 제거
        ```
      - !finding 빈 — '항목을 불러오지 못했습니다.' verbatim
      - 헤더 + admin 다운로드 — OQ #3 격상 (11 → text-caption leading-none)
      - 지적 정보 KVRow 4 + 지적 사진 PhotoGrid verbatim
      - **조치 입력 영역 (open finding)**:
        - borderTop `var(--bd) → border-t border-border-default`
        - textarea — `className="bg-surface-sunken border border-border-strong text-label text-text-primary rounded-md"` + 인라인 padding 유지 (rows={3} verbatim)
        - 사진 슬롯 64x64 + ✕ 18x18 verbatim
        - **첨부 button — Lucide Camera 교체 (OQ #5)**:
          ```tsx
          {canAdd && (
            <button className="bg-surface-sunken border border-dashed border-border-strong text-text-tertiary rounded-sm" style={{ width: 64, height: 64, display: 'flex', flexDirection: 'column', alignItems: 'center', justifyContent: 'center', gap: 2, cursor: 'pointer' }} onClick={openPicker}>
              <Camera size={18} />
              <span className="text-caption leading-none font-bold">첨부</span>
            </button>
          )}
          ```
        - **조치 완료 button — 인라인 그라데이션 OQ #4 LOCKED**:
          ```tsx
          <button onClick={handleResolve} disabled={isSubmitting} className="text-text-on-accent text-label font-bold rounded-md" style={{ width: '100%', height: 44, border: 'none', cursor: isSubmitting ? 'default' : 'pointer', marginTop: 12, opacity: isSubmitting ? 0.6 : 1, background: 'linear-gradient(135deg, #1d4ed8, #0ea5e9)' }}>
            {isSubmitting ? '처리 중...' : '조치 완료'}
          </button>
          ```
      - 조치 결과 영역 (resolved finding) — KVRow 3 + PhotoGrid verbatim
    - **영역 4 — 메인 LegalPage** (line 372~571):
      - useNavigate / useIsDesktop / useSearchParams / state / useQuery legal-rounds / filtered verbatim 유지
      - **handleRoundClick (line 394~401) isDesktop 분기 verbatim 유지** — `isDesktop ? (setSelectedRoundId(round.id), setSelectedFindingId(null)) : navigate('/legal/' + round.id)` (1 byte 변경 금지, memory `project_inspection_completion_rule` 일반화)
      - roundList JSX (line 404~461):
        - 탭 (TABS 매핑) — 데스크톱 h 38 / 모바일 h 44, OQ #3 격상 (11 → text-caption leading-none)
        - 연도 select — 데스크톱 padding '4px 8px' / 모바일 padding '6px 12px'
        - 라운드 카드 데스크톱 (line 438~458) — selected `border-2 border-accent` (OQ #3-selected) + borderLeft accentColor (OQ #2 LOCKED 4분기 매핑) + 메타 verbatim ('건' 없음)
        - 라운드 카드 모바일 (line 552~567) — borderLeft accentColor (OQ #2) + 메타 verbatim ('건' 있음 + endDate '~' 분기)
      - **데스크톱 3분할 (line 464~500)**:
        - 외곽 — `bg-surface-page flex h-full` (var(--bg) 인라인 폐기)
        - 인라인 `<style>{`@keyframes blink { 0%,100%{opacity:.6} 50%{opacity:.3} }`}</style>` (line 467) 보존
        - 좌측 — `w-[500px] flex-shrink-0 border-r border-border-default flex flex-col h-full` (1 byte 변경 금지)
        - 중앙 — `w-[500px] flex-shrink-0 border-r border-border-default flex flex-col` + FindingsPanel mount + fallback '좌측에서 점검을 선택하세요' verbatim
        - 우측 — `flex-1 flex flex-col` + FindingDetailPanel mount + fallback `selectedRoundId ? '중앙에서 지적사항을 선택하세요' : '점검을 먼저 선택하세요'` verbatim
      - **모바일 (line 503~570)**:
        - 외곽 — `bg-surface-page flex flex-col h-full overflow-hidden`
        - 인라인 `<style>{`@keyframes blink ...`}</style>` (line 505) 보존
        - **모바일 자체 헤더 (line 507~515) — OQ #1 LOCKED + OQ #5 LOCKED**:
          ```tsx
          <div className="bg-surface-raised border-b border-border-default" style={{ height: 48, display: 'flex', alignItems: 'center', justifyContent: 'center', position: 'relative', flexShrink: 0 }}>
            <button onClick={() => navigate(-1)} className="text-text-secondary" style={{ position: 'absolute', left: 8, width: 44, height: 44, background: 'none', border: 'none', cursor: 'pointer', display: 'flex', alignItems: 'center', justifyContent: 'center' }}>
              <ChevronLeft size={20} />
            </button>
            <span className="text-body font-bold text-text-primary">소방 점검 관리</span>
          </div>
          ```
          (옛 inline SVG ChevronLeft polyline + IconChevronLeft 폐기, back button 36x36 → 44x44 격상)
        - 필터 영역 / 카드 영역 verbatim
        - 빈/오류 카피 verbatim

    **§4. OQ LOCKED 5건 반영 매핑 표**

    | OQ | 위치 | 변환 룰 | 검증 grep |
    |---|---|---|---|
    | #1 | 모바일 헤더 line 507~515 | `bg-surface-raised border-b border-border-default` + back button 44x44 | `bg-surface-raised border-b border-border-default` ≥1 |
    | #2 | accentColor + ResultBadge + finding 칩/borderLeft (line 27~47, 187~200, 438~458, 552~567) | 4분기 status 토큰 매핑 (status- prefix 없음) | `border-(safe|warning|danger)-bar` 각 ≥1 / `bg-(safe|warning|danger)-bg text-(safe|warning|danger)` 각 ≥1 |
    | #3 | ResultBadge + admin 도구 + 데스크톱 탭 + finding 칩 + 메타 + 첨부/수정/삭제 button | 11·10 → `text-caption font-bold leading-none` 격상 | `text-caption` + `leading-none` ≥10 |
    | #4 | 조치 완료 button line 346 + admin 작은 도구 + 다시 시도 | 조치 완료 인라인 그라데이션 + 작은 도구 solid bg-accent + 빈/오류 아이콘 무 | `linear-gradient(135deg, #1d4ed8, #0ea5e9)` ≥1 / `bg-accent` ≥2 |
    | #5 | 모바일 헤더 back button line 511 + 첨부 button line 340 + 로딩 spinner line 291 | Lucide ChevronLeft + Camera + Loader2 교체 + back 44x44 격상 | `import { ChevronLeft, Camera, Loader2 } from 'lucide-react'` ≥1 + `<ChevronLeft size={20}` ≥1 + `<Camera size={18}` ≥1 + `<Loader2` ≥1 + IconChevronLeft = 0 + `'📷'` = 0 |

    **§5. Negative gate (TSX 변환 후 검증)**
    - 이모지 0건 (TSX 본문)
    - `linear-gradient` — **FindingDetailPanel 조치 완료 button 인라인 `linear-gradient(135deg, #1d4ed8, #0ea5e9)` ≥1 만 허용** (OQ #4 LOCKED 예외 anchor) / 다른 linear-gradient 0건
    - 9·10·11px fontSize 0건 (OQ #3 LOCKED text-caption 12 leading-none 마지노선)
    - `(text|bg|border)-status-` class prefix 0건 (OQ #2 LOCKED 위반 시 FAIL, memory `feedback_tailwind_token_class_pattern`)
    - `\b(w|h)-8\b` 0건 (memory `feedback_tailwind_w8_h8_is_48px`)
    - 옛 alias 토큰 — TSX 본문 안 인라인 style 안 단계적 제거, 변환 wave 마무리 시 0건 목표

    **§6. Positive gate (TSX 변환 후 검증)**
    - 비즈 anchor 17건 모두 변경 없이 보존
    - OQ LOCKED 5건 모두 verify grep PASS
    - Tailwind utility class 패턴 (status- prefix 없음 + w-8/h-8 함정 회피) 준수
    - LegalPage.tsx 단일 atomic commit + 다른 9 파일 변경 0 byte

    **§7. Build/tsc**
    - `cd cha-bio-safety && npx tsc --noEmit` PASS (0 errors)
    - `cd cha-bio-safety && npm run build` PASS
    - cbc7119-preview 자동 배포 후 모바일/데스크톱 시각 검수

    **§8. 자체 verify (변환 후 grep 모음)**
    ```bash
    # OQ #1
    grep -c 'bg-surface-raised border-b border-border-default' cha-bio-safety/src/pages/LegalPage.tsx
    # OQ #2
    grep -cE 'border-(safe|warning|danger)-bar' cha-bio-safety/src/pages/LegalPage.tsx
    grep -cE 'bg-(safe|warning|danger)-bg text-(safe|warning|danger)' cha-bio-safety/src/pages/LegalPage.tsx
    # OQ #3
    grep -c 'text-caption' cha-bio-safety/src/pages/LegalPage.tsx
    grep -c 'leading-none' cha-bio-safety/src/pages/LegalPage.tsx
    # OQ #4
    grep -c 'linear-gradient(135deg, #1d4ed8, #0ea5e9)' cha-bio-safety/src/pages/LegalPage.tsx
    grep -c 'bg-accent' cha-bio-safety/src/pages/LegalPage.tsx
    # OQ #5
    grep -c 'import { ChevronLeft, Camera, Loader2 } from .lucide-react' cha-bio-safety/src/pages/LegalPage.tsx
    grep -c '<ChevronLeft size={20}' cha-bio-safety/src/pages/LegalPage.tsx
    grep -c '<Camera size={18}' cha-bio-safety/src/pages/LegalPage.tsx
    grep -c '<Loader2' cha-bio-safety/src/pages/LegalPage.tsx
    grep -c '📷' cha-bio-safety/src/pages/LegalPage.tsx  # 0 기대 (이모지 제거)
    grep -c 'IconChevronLeft' cha-bio-safety/src/pages/LegalPage.tsx  # 0 기대 (해당 없음)
    # 비즈 anchor
    grep -cE 'legal-rounds|legal-round|legal-findings|legal-finding' cha-bio-safety/src/pages/LegalPage.tsx  # ≥4
    grep -cE 'legalApi\.(list|get|getFindings|updateResult|deleteFinding|getFinding|resolveFinding)' cha-bio-safety/src/pages/LegalPage.tsx  # ≥7
    # negative
    grep -cE '(text|bg|border)-status-' cha-bio-safety/src/pages/LegalPage.tsx  # 0
    grep -cE '\bw-8\b|\bh-8\b' cha-bio-safety/src/pages/LegalPage.tsx  # 0
    ```

    **§9. Tailwind cheatsheet** (W2 rules 박스 안 cheatsheet verbatim 인용)

    **§10. 비즈 보존 체크박스 (17건)**
    - [ ] useQuery × 4 queryKey 정확히 일치 + 4 키 invalidate 정확
    - [ ] useMutation resolveMutation — uploadAll + legalApi.resolveFinding(snake_case)
    - [ ] legalApi 7종 시그니처 + snake_case payload + camelCase props 혼용 보존
    - [ ] accentColor 4분기 (pass/fail/conditional/null) 1 byte 변경 0
    - [ ] ResultBadge map 4 라벨 verbatim ('적합' / '부적합' / '조건부적합' / '결과 미입력')
    - [ ] filterRounds 3분기 (미조치 / 완료 / 전체) 운영 룰
    - [ ] TABS key/label mismatch ('미조치' key → '진행 중' label) 의도된 디자인
    - [ ] sorted findings open-first + createdAt desc localeCompare
    - [ ] handleRoundClick isDesktop 분기 (데스크톱 selectedRoundId + 모바일 navigate sub-route)
    - [ ] role admin 도구 분기 (FindingsPanel role === 'admin' + FindingDetailPanel staff?.role === 'admin')
    - [ ] useMultiPhotoUpload 5장 제한 (canAdd = slots.length < 5)
    - [ ] buildMetaTxt + fflate ZIP + 파일명 패턴 (지적사항_{location}.zip / 지적사진-{N}.jpg / 조치사진-{N}.jpg)
    - [ ] @keyframes blink (.6/.3, Education .4 와 다름) + spin (OQ #5 Loader2 교체 후 spin 제거)
    - [ ] toast 카피 11종 verbatim (success 5 / error 6)
    - [ ] 빈/오류/fallback 카피 다수 verbatim (모바일 + 데스크톱 + FindingsPanel + FindingDetailPanel)
    - [ ] 데스크톱 3분할 width 500/500/flex 1 + borderRight 1px
    - [ ] 모바일 자체 헤더 h 48 + 타이틀 '소방 점검 관리' verbatim

    **§11. 메모리 룰 inline (12+ rule citations)**
    - `feedback_inspection_unresolved_color` — accentColor + ResultBadge + finding 칩 결과 status 토큰 일반화 (OQ #2 LOCKED)
    - `project_inspection_completion_rule` — role admin 도구 분기 + filterRounds + sorted open-first + handleRoundClick isDesktop 분기 source of truth 일반화
    - `feedback_tailwind_token_class_pattern` — status- prefix 없음 + lucide `<Icon size={N} />` prop
    - `feedback_tailwind_w8_h8_is_48px` — w-8/h-8 = 48px 함정 (back button 44x44 = `w-11 h-11`)
    - `feedback_text_caption_leading_none` — 작은 컨테이너 안 text-caption(12) leading-none 명시
    - `feedback_sketch_realistic_data` — 표시 분기/라벨 룰 코드 그대로
    - `feedback_design_changes_ask_first` — 디자인 변경 전 사용자 컨펌
    - `feedback_planner_prompt_sketch_verbatim` — sketch CSS verbatim 인용
    - `feedback_tsx_wave_emoji_dot_gap` — sketch 의 이모지 0건 + '📷' 사고 케이스 (OQ #5 Camera 교체)
    - `feedback_tsx_wave_stat_card_drift` — source outline 패턴 보존 + sketch 새 패턴 누락 방지
    - `feedback_subagent_production_deploy_forbidden` — wrangler 명령 금지
    - `feedback_redesign_sketch_rule_enforcement` — §6.2 negative rule + §6.3/§7.1 일관성 강제

    **§12. 다음 단계 (TSX 변환 wave 진입)**
    - 본 checklist 가 W6 변환 wave 의 단일 진입점
    - W6 = LegalPage.tsx 571 lines 단일 atomic 변환 (4 영역 통합)
    - 변환 후 cbc7119-preview 자동 배포 → 사용자 모바일/데스크톱 시각 검수
    - 통과 시 main 머지 + cbc7119-preview 재배포 → status 박제 (memory `project_redesign_19_legal_status` 신규)
    - sub-route 페이지 (LegalFindingsPage + LegalFindingDetailPage) 는 별도 wave 진행

    **commit 명령 (executor 가 실행 후 commit hash + message 보고):**
    ```
    git add cha-bio-safety/docs/redesign-context/19-legal/wave-5-tsx-conversion-checklist.md
    git commit -m "docs(quick-260523-40p): redesign/19-legal W5 TSX conversion checklist (12 섹션, OQ 5건 LOCKED 반영 + 비즈 anchor 17건 보존 + 메모리 룰 12+ inline)"
    ```

    **마지막 — TSX 변경 0건 verify gate (4 task 누적 git diff 확인):**
    ```bash
    # 본 PLAN 의 4 commit 누적 diff 에서 src 10 파일 모두 등장하면 안 됨
    git diff --name-only HEAD~4 HEAD -- cha-bio-safety/src/pages/LegalPage.tsx cha-bio-safety/src/App.tsx cha-bio-safety/src/utils/api.ts cha-bio-safety/src/utils/findingDownload.ts cha-bio-safety/src/hooks/useMultiPhotoUpload.ts cha-bio-safety/src/components/PhotoGrid.tsx cha-bio-safety/src/components/PhotoSourceModal.tsx cha-bio-safety/src/components/FindingFormSheet.tsx cha-bio-safety/src/pages/LegalFindingsPage.tsx cha-bio-safety/src/pages/LegalFindingDetailPage.tsx
    # empty (no output) 가 PASS / 어떤 결과라도 나오면 FAIL
    ```
  </action>
  <verify>
    <automated>
test -f cha-bio-safety/docs/redesign-context/19-legal/wave-5-tsx-conversion-checklist.md \
  && wc -l cha-bio-safety/docs/redesign-context/19-legal/wave-5-tsx-conversion-checklist.md | awk '{ if ($1 < 200) { print "FAIL: too short"; exit 1 } else print "PASS lines:", $1 }' \
  && grep -cE '^## §|^### §|^# §' cha-bio-safety/docs/redesign-context/19-legal/wave-5-tsx-conversion-checklist.md | awk '{ if ($1 < 12) { print "FAIL: section count <12"; exit 1 } else print "PASS sections:", $1 }' \
  && grep -c 'LegalPage.tsx' cha-bio-safety/docs/redesign-context/19-legal/wave-5-tsx-conversion-checklist.md | awk '{ if ($1 < 5) { print "FAIL: LegalPage.tsx 인용 <5"; exit 1 } else print "PASS source ref:", $1 }' \
  && grep -cE 'OQ #1|OQ #2|OQ #3|OQ #4|OQ #5' cha-bio-safety/docs/redesign-context/19-legal/wave-5-tsx-conversion-checklist.md | awk '{ if ($1 < 5) { print "FAIL: OQ #1~#5 citation <5"; exit 1 } else print "PASS OQ citations:", $1 }' \
  && grep -c 'bg-surface-raised border-b border-border-default' cha-bio-safety/docs/redesign-context/19-legal/wave-5-tsx-conversion-checklist.md | awk '{ if ($1 < 1) { print "FAIL: OQ #1 anchor missing"; exit 1 } else print "PASS OQ #1 anchor:", $1 }' \
  && grep -cE 'border-(safe|warning|danger)-bar' cha-bio-safety/docs/redesign-context/19-legal/wave-5-tsx-conversion-checklist.md | awk '{ if ($1 < 3) { print "FAIL: OQ #2 borderLeft 토큰 <3 (4분기 중 3개 status 토큰)"; exit 1 } else print "PASS OQ #2 borderLeft:", $1 }' \
  && grep -cE 'bg-(safe|warning|danger)-bg text-(safe|warning|danger)' cha-bio-safety/docs/redesign-context/19-legal/wave-5-tsx-conversion-checklist.md | awk '{ if ($1 < 3) { print "FAIL: OQ #2 ResultBadge 토큰 <3"; exit 1 } else print "PASS OQ #2 ResultBadge:", $1 }' \
  && grep -c 'linear-gradient(135deg, #1d4ed8, #0ea5e9)' cha-bio-safety/docs/redesign-context/19-legal/wave-5-tsx-conversion-checklist.md | awk '{ if ($1 < 1) { print "FAIL: OQ #4 그라데이션 anchor missing"; exit 1 } else print "PASS OQ #4 gradient:", $1 }' \
  && grep -cE 'ChevronLeft|Camera|Loader2|lucide-react' cha-bio-safety/docs/redesign-context/19-legal/wave-5-tsx-conversion-checklist.md | awk '{ if ($1 < 4) { print "FAIL: OQ #5 Lucide anchor <4 (3 아이콘 + import)"; exit 1 } else print "PASS OQ #5 Lucide:", $1 }' \
  && grep -c '소방 점검 관리' cha-bio-safety/docs/redesign-context/19-legal/wave-5-tsx-conversion-checklist.md | awk '{ if ($1 < 1) { print "FAIL: 모바일 타이틀 verbatim missing"; exit 1 } else print "PASS 타이틀:", $1 }' \
  && grep -c '적합\|부적합\|조건부적합\|결과 미입력' cha-bio-safety/docs/redesign-context/19-legal/wave-5-tsx-conversion-checklist.md | awk '{ if ($1 < 4) { print "FAIL: ResultBadge 4 라벨 verbatim <4"; exit 1 } else print "PASS ResultBadge 라벨:", $1 }' \
  && grep -cE 'legal-rounds|legal-round|legal-findings|legal-finding|legalApi|accentColor|ResultBadge|filterRounds|TABS|sorted|handleRoundClick|useMultiPhotoUpload|buildMetaTxt|fflate|@keyframes|toast|role.*admin|isDesktop' cha-bio-safety/docs/redesign-context/19-legal/wave-5-tsx-conversion-checklist.md | awk '{ if ($1 < 12) { print "FAIL: 비즈 anchor 17건 박제 hit <12"; exit 1 } else print "PASS 비즈 anchor hits:", $1 }' \
  && grep -cE 'feedback_inspection_unresolved_color|project_inspection_completion_rule|feedback_tailwind_token_class_pattern|feedback_tailwind_w8_h8_is_48px|feedback_text_caption_leading_none' cha-bio-safety/docs/redesign-context/19-legal/wave-5-tsx-conversion-checklist.md | awk '{ if ($1 < 5) { print "FAIL: 메모리 룰 citation <5"; exit 1 } else print "PASS 메모리 룰:", $1 }' \
  && grep -cE '(text|bg|border)-status-(safe|fire|warning|danger|caution)' cha-bio-safety/docs/redesign-context/19-legal/wave-5-tsx-conversion-checklist.md | awk '{ if ($1 > 0) { print "FAIL: status- prefix found in checklist body"; exit 1 } else print "PASS no status- prefix" }' \
  && LEGAL_DIFF=$(git diff --name-only HEAD~4 HEAD -- cha-bio-safety/src/pages/LegalPage.tsx 2>/dev/null) && if [ -z "$LEGAL_DIFF" ]; then echo "PASS LegalPage.tsx unchanged"; else echo "FAIL LegalPage.tsx changed: $LEGAL_DIFF"; exit 1; fi \
  && APP_DIFF=$(git diff --name-only HEAD~4 HEAD -- cha-bio-safety/src/App.tsx 2>/dev/null) && if [ -z "$APP_DIFF" ]; then echo "PASS App.tsx unchanged"; else echo "FAIL App.tsx changed: $APP_DIFF"; exit 1; fi \
  && EXTERNAL_DIFF=$(git diff --name-only HEAD~4 HEAD -- cha-bio-safety/src/utils/api.ts cha-bio-safety/src/utils/findingDownload.ts cha-bio-safety/src/hooks/useMultiPhotoUpload.ts cha-bio-safety/src/components/PhotoGrid.tsx cha-bio-safety/src/components/PhotoSourceModal.tsx cha-bio-safety/src/components/FindingFormSheet.tsx 2>/dev/null) && if [ -z "$EXTERNAL_DIFF" ]; then echo "PASS external 6 files unchanged"; else echo "FAIL external changed: $EXTERNAL_DIFF"; exit 1; fi \
  && SUBROUTE_DIFF=$(git diff --name-only HEAD~4 HEAD -- cha-bio-safety/src/pages/LegalFindingsPage.tsx cha-bio-safety/src/pages/LegalFindingDetailPage.tsx 2>/dev/null) && if [ -z "$SUBROUTE_DIFF" ]; then echo "PASS sub-route 2 files unchanged"; else echo "FAIL sub-route changed: $SUBROUTE_DIFF"; exit 1; fi \
  && git log --oneline -1 | grep -q '260523-40p.*W5' && echo "PASS commit W5"
    </automated>
  </verify>
  <done>
    - wave-5-tsx-conversion-checklist.md 파일 존재 + ≥200 lines (≥320 권장)
    - 12 섹션 모두 박제 (§1 ~ §12) — heading 수 ≥12
    - LegalPage.tsx 인용 ≥5건
    - OQ #1~#5 citation 모두 박제 (총 ≥5건)
    - OQ #1 anchor (`bg-surface-raised border-b border-border-default`) ≥1
    - OQ #2 anchor — `border-{safe|warning|danger}-bar` ≥3 (3분기 status 토큰) + `bg-{safe|warning|danger}-bg text-{safe|warning|danger}` ≥3
    - OQ #4 anchor — `linear-gradient(135deg, #1d4ed8, #0ea5e9)` ≥1
    - OQ #5 anchor — `ChevronLeft|Camera|Loader2|lucide-react` ≥4 (3 아이콘 + import)
    - 카피 verbatim — '소방 점검 관리' ≥1 / ResultBadge 4 라벨 ≥4 (적합/부적합/조건부적합/결과 미입력)
    - 비즈 anchor 17건 박제 hit ≥12 (전체 §2 + §3 + §10 cumulative)
    - 메모리 룰 5+ citation (feedback_inspection_unresolved_color / project_inspection_completion_rule / feedback_tailwind_token_class_pattern / feedback_tailwind_w8_h8_is_48px / feedback_text_caption_leading_none 각 ≥1)
    - **status- prefix 0건 (checklist 본문)**
    - **★ TSX 변경 0건 verify (PLAN 의 4 commit 누적 diff)** — LegalPage.tsx + App.tsx + 외부 6 + sub-route 2 = 총 10 src 파일 모두 empty
    - atomic commit `docs(quick-260523-40p): ... W5 TSX conversion checklist ...` 1개
  </done>
</task>

</tasks>

<verification>
모든 4 task verify gate PASS + 4 atomic commit 누적 + src 코드 변경 0건 (LegalPage.tsx + App.tsx + 외부 6 + sub-route 2 = 총 10 src 파일).

orchestrator 가 본 PLAN 의 4 commit 을 cherry-pick 으로 부모 브랜치 (redesign/19-legal) 로 자동 복구 (executor worktree commit 부모 머지 사고 6회 precedent 회피 — 단, 10-cctv-info + 28-splash 2q6 0건 자동 도달 precedent 도 있으므로 worktree → parent 자동 머지 성공 시 cherry-pick 불필요).

cbc7119-preview 자동 배포 → 사용자 모바일/데스크톱 시각 검수 (브라우저에서 sketch HTML 3 파일 직접 열어 확인 + checklist md 검토).
</verification>

<success_criteria>
1. **4 산출 파일 모두 존재 + ≥200 lines 각각** (sketch 3 + checklist 1, ≥320 권장)
2. **평면 배치** — 4 파일 모두 `cha-bio-safety/docs/redesign-context/19-legal/` 직속 (sketch/ 서브폴더 X)
3. **negative gate 4 파일 모두 PASS** — 이모지 0 / status- prefix 0 / 9·10·11px 0 / w-8 h-8 0
4. **linear-gradient negative gate 룰** — W2/W3/W5 = 0건 / **W4 만 `linear-gradient(135deg, #1d4ed8, #0ea5e9)` ≥3 박제 (OQ #4 LOCKED 예외 anchor)**
5. **OQ LOCKED 5건 모두 반영** — #1 모바일 헤더 토큰 / #2 accentColor + ResultBadge + finding 칩 status 토큰 매핑 (status- prefix 없음) / #3 §1.1 9·10·11 → 12 격상 + leading-none / #4 조치 완료 CTA 그라데이션 + 작은 도구 solid + 빈/오류 아이콘 무 / #5 Lucide ChevronLeft + Camera + Loader2 + back 44x44 격상
6. **비즈 anchor 17건** 4 파일 모두 박제 (W2 rules + W3 rules + W4 rules + W5 §2/§3/§10)
7. **atomic 4 commit** — 각 task 마다 1 commit, 메시지 `docs(quick-260523-40p): ... W{N} ...` 형식
8. **★ src 코드 변경 0건** — `git diff --name-only HEAD~4 HEAD --` 4 영역 모두 empty:
   - LegalPage.tsx + App.tsx
   - 외부 6 파일 (utils/api.ts / utils/findingDownload.ts / hooks/useMultiPhotoUpload.ts / components/PhotoGrid.tsx / components/PhotoSourceModal.tsx / components/FindingFormSheet.tsx)
   - sub-route 2 파일 (LegalFindingsPage.tsx / LegalFindingDetailPage.tsx)
9. **cbc7119-preview 자동 배포 사이클 1회** 후 사용자 시각 검수 가능
</success_criteria>

<output>
**executor 보고 (각 task 끝마다):**
- commit hash (e.g. `abc1234`) + commit message 1줄
- verify gate awk PASS line 모음 (각 grep 의 `PASS ...` 출력)
- 산출 파일 line 수 (`wc -l` 결과)

**최종 보고 (4 task 끝나면):**
- 4 commit hash + message 모두 (latest first)
- 4 파일 line 수 합계
- src 변경 0건 verify gate PASS 확인 (LegalPage + App + 외부 6 + sub-route 2 모두 empty diff)
- 다음 단계: orchestrator 가 cherry-pick 필요 시 자동 복구 + 사용자에게 cbc7119-preview 시각 검수 안내

**SUMMARY.md 산출 (4 task 모두 끝나면):**
- path: `.planning/quick/260523-40p-redesign-19-legal-w2-w5-chrome-findingsp/260523-40p-SUMMARY.md`
- 자체 verify gate 결과 + 4 commit 해시 + deviation 명시 + 다음 wave 진입 안내 (W6 TSX 변환 wave)
</output>
