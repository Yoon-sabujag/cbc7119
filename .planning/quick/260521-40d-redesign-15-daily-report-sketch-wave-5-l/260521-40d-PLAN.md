---
phase: quick-260521-40d
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cha-bio-safety/docs/redesign-context/15-daily-report/sketch-wave-5-desktop-layout.html
autonomous: true
requirements:
  - QUICK-260521-40d-W5
must_haves:
  truths:
    - "sketch-wave-5-desktop-layout.html 1개 파일이 생성된다 (신규)"
    - "2 frame (F1 다크 / F2 라이트) 데스크톱 viewport 1200~1280px 가 좌측 편집 패널(flex:1) + 우측 A4 portrait wrapper(aspectRatio 210/297) 로 분할된다"
    - "좌측 편집 패널은 §1.3 verbatim 룰 (모바일/데스크톱 동일 폰트, Spacing/Layout 만 분기) 을 코멘트로 박제하고 padding 24px 32px (source line 408 verbatim) 을 적용한다"
    - "우측 A4 portrait wrapper 는 W5 단계 placeholder (`.desktop-portrait-placeholder` + 안내 텍스트 'DailyPortraitPreview · W6 wrapper wave') 로 남기고 W6 진입 시 본격 처리됨을 코멘트로 명시한다"
    - "우측 상단 '인쇄 미리보기' 라벨 (source line 432 verbatim) + DailyPortraitPreview props 시그니처 (date/todayText/tomorrowText/notes/personnel — source line 434~440) + useIsDesktop 분기 (line 64) 가 sketch 안 코멘트로 박제된다"
    - "메모리 룰 unique slug ≥7건 + OQ ≥3건 (default 1줄 박제) + 신규 class ≥5건 + verify gate ≥8건 + negative gate ≥10건 이 sketch 본문 자체 검수 영역에 명시된다"
    - "다른 페이지 (13/14/02/06) / src/** / components.css / App.tsx 영향 0"
  artifacts:
    - path: "cha-bio-safety/docs/redesign-context/15-daily-report/sketch-wave-5-desktop-layout.html"
      provides: "W5 데스크톱 layout sketch (좌측 편집 패널 + 우측 A4 portrait placeholder, 2 frame)"
      min_lines: 700
      max_lines: 900
      contains:
        - "desktop-layout"
        - "desktop-edit-panel"
        - "desktop-edit-panel-header"
        - "desktop-portrait-wrapper"
        - "desktop-portrait-print-label"
        - "desktop-portrait-placeholder"
        - "인쇄 미리보기"
        - "DailyPortraitPreview"
        - "useIsDesktop"
        - "aspect-ratio: 210 / 297"
  key_links:
    - from: "sketch-wave-5-desktop-layout.html"
      to: "DailyReportPage.tsx line 403~444 (데스크톱 렌더)"
      via: "verbatim 라벨/시그니처/padding 인용"
      pattern: "인쇄 미리보기|DailyPortraitPreview|24px 32px|aspect-ratio: 210 / 297"
    - from: "sketch-wave-5-desktop-layout.html"
      to: "wave-1-index.md §1.3 / §2.1 W5 / §3.3 / §3.6 / §5"
      via: "verbatim 룰 + 메모리 슬러그 박제"
      pattern: "feedback_(design_sketch_first|redesign_sketch_rule_enforcement|sketch_realistic_data|planner_prompt_sketch_verbatim|tailwind_token_class_pattern|tailwind_w8_h8_is_48px|tsx_wave_emoji_dot_gap|tsx_wave_stat_card_drift|avoid_premature_confirmation|cbc7119_design_never_wrangler)\\.md"
---

<objective>
redesign/15-daily-report sketch wave 5 — 데스크톱 layout 시안 1 HTML.

DailyReportPage.tsx (line 403~444) 의 데스크톱 렌더 (좌측 편집 패널 flex:1 + 우측 A4 portrait wrapper aspectRatio 210/297) 를 2 frame (다크 F1 / 라이트 F2) sketch 로 변환한다.

좌측 편집 패널은 W2 dateNav (flex-end) + W3 EditableCard × 3 / 인원현황 SummaryCard + W4 다운로드 액션 (data/monthly 데스크톱 row flex 1:1) + 안내 줄 의 markup 을 **축약 reference** 로 배치한다 — class 정의 자체는 W3/W4 sketch reference, 본 wave 는 layout wrapper (`.desktop-layout` / `.desktop-edit-panel` / `.desktop-edit-panel-header` / `.desktop-portrait-wrapper` / `.desktop-portrait-print-label` / `.desktop-portrait-placeholder`) 만 신규 정의한다.

우측 A4 portrait wrapper 는 W5 단계 placeholder (aspect-ratio 210/297 그레이 영역 + dashed border + 가운데 안내 텍스트) 로 둔다 — W6 wave 에서 본격 처리 (캘리브레이션/이미지/오버레이/setup-btn).

§1.3 verbatim 룰 ("모바일/데스크톱 동일 폰트, Spacing/Layout 만 분기"), §6.1 / §6.2 미적용 명시 (Stat Card / Progress 도넛 없음 — drift 방지), §6.4 그라데이션 폐기 inherit, 카피/시그니처/OQ default inherit 박제 — 모두 sketch 본문에 verbatim 박제.

Purpose: W6 wrapper wave (DailyPortraitPreview 외곽 + 캘리브레이션 안내 바 + setup-btn) 진입 직전 컨펌 자료. layout 분기 잠그고, 우측은 placeholder 로 명시적 위임 (drift 방지).

Output:
- `cha-bio-safety/docs/redesign-context/15-daily-report/sketch-wave-5-desktop-layout.html` (1개, 신규, 700~900 줄, 2 frame)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@CLAUDE.local.md
@.planning/STATE.md
@cha-bio-safety/docs/redesign-context/15-daily-report/wave-1-index.md
@cha-bio-safety/docs/redesign-context/15-daily-report/design-system.md
@cha-bio-safety/docs/redesign-context/15-daily-report/tokens.css
@cha-bio-safety/docs/redesign-context/15-daily-report/typography.css
@cha-bio-safety/docs/redesign-context/15-daily-report/sketch-wave-2-mobile-header-date-nav.html
@cha-bio-safety/docs/redesign-context/15-daily-report/sketch-wave-3-editable-cards-personnel.html
@cha-bio-safety/docs/redesign-context/15-daily-report/sketch-wave-4-download-action.html
@cha-bio-safety/docs/redesign-context/14-reports/sketch-wave-6-desktop-preview-wrapper.html
@cha-bio-safety/src/pages/DailyReportPage.tsx

<interfaces>
<!-- DailyReportPage.tsx 데스크톱 렌더 — line 403~444 verbatim 인용 -->
<!-- sketch 작성자는 아래 시그니처/스타일/카피 를 그대로 박제한다. 토큰명/사이즈 추측 금지 (memory feedback_planner_prompt_sketch_verbatim). -->

From cha-bio-safety/src/pages/DailyReportPage.tsx (line 64):
```tsx
const isDesktop = useIsDesktop()
```

From cha-bio-safety/src/pages/DailyReportPage.tsx (line 403~444 — verbatim, 1:1):
```tsx
// ── 렌더 — 데스크톱 ────────────────────────────────────
if (isDesktop) {
  return (
    <div style={{ display: 'flex', flexDirection: 'row', height: '100%', overflow: 'hidden', background: 'var(--bg)' }}>
      {/* 좌측 편집 패널 — 페이지 제목은 App.tsx 헤더에서 표시 */}
      <div style={{ flex: 1, overflow: 'auto', padding: '24px 32px' }}>
        {/* 데스크톱 날짜 네비게이터 */}
        <div style={{ display: 'flex', alignItems: 'center', justifyContent: 'flex-end', marginBottom: 20 }}>
          {dateNav}
        </div>
        {formContent}
      </div>

      {/* 우측 A4 세로 미리보기 패널 — 높이 기준으로 A4 비율 폭 계산 */}
      <div style={{
        aspectRatio: '210 / 297',
        height: '100%', flexShrink: 0,
        borderLeft: '1px solid var(--bd)',
        overflow: 'hidden',
        background: 'var(--bg)',
        display: 'flex', alignItems: 'center', justifyContent: 'center',
        position: 'relative',
      }}>
        <div style={{
          position: 'absolute', top: 8, left: 0, right: 0,
          textAlign: 'center', fontSize: 11, color: 'var(--t2)',
          fontWeight: 700, textTransform: 'uppercase',
          pointerEvents: 'none', zIndex: 5,
        }}>
          인쇄 미리보기
        </div>
        <DailyPortraitPreview
          date={date}
          todayText={todayText}
          tomorrowText={tomorrowText}
          notes={notes}
          personnel={preview?.personnel}
        />
      </div>
    </div>
  )
}
```

**보존 항목 (sketch 안 코멘트로 박제 — memory feedback_sketch_realistic_data):**
- 라벨 "인쇄 미리보기" verbatim (line 432)
- DailyPortraitPreview props 시그니처: `date / todayText / tomorrowText / notes / personnel` (line 434~440)
- useIsDesktop 분기 보존 결정 (OQ #5 default = a, 분기 유지)
- 좌측 padding `24px 32px` (line 408 verbatim)
- 우측 wrapper `aspectRatio: '210 / 297', height: '100%', flexShrink: 0, borderLeft: '1px solid var(--bd)'` (line 416~423 verbatim)
- 라벨 wrapper `position: 'absolute', top: 8, left: 0, right: 0, textAlign: 'center', fontWeight: 700, textTransform: 'uppercase'` (line 426~430 verbatim) — fontSize 11 만 § 1.1 위반 → text-caption 12 로 상향 (마이그레이션 §4.2)
- dateNav 좌측 영역 정렬 `justifyContent: 'flex-end', marginBottom: 20` (line 410)
</interfaces>

<sketch_naming_patterns>
<!-- W2/W3/W4 sketch class 명단 (grep 결과) — 본 wave 좌측 패널 축약 reference 안에 그대로 등장 -->
W2: .page-header / .page-title / .date-nav / .date-nav-btn / .date-nav-spacer
W3: .editable-card / .editable-card-head / .editable-card-label / .editable-card-actions / .editable-card-btn--reset / .editable-card-btn--save / .editable-card-textarea
    .summary-card / .summary-card-label / .summary-card-body / .summary-card-empty / .summary-card-error / .summary-card-state-label / .summary-card-skeleton
W4: .download-action / .download-action--desktop / .download-btn / .download-btn--daily / .download-btn--monthly / .download-btn--disabled / .download-btn-icon / .page-footer-note

W5 신규 (이 wave 에서 정의 — ≥5건):
1. .desktop-layout — page-level flex row + height 100% + overflow hidden (모바일/데스크톱 분기 wrapper 의 데스크톱 변형)
2. .desktop-edit-panel — 좌측 편집 패널 (flex 1 + overflow auto + padding 24px 32px + display flex column + gap 16)
3. .desktop-edit-panel-header — dateNav flex-end 영역 (flex justify-end + margin-bottom 8 — 또는 20, line 410 verbatim)
4. .desktop-portrait-wrapper — 우측 A4 portrait container (aspect-ratio 210/297 + height 100% + flexShrink 0 + borderLeft border-default + bg-surface-page + position relative)
5. .desktop-portrait-print-label — 우측 상단 "인쇄 미리보기" 라벨 (position absolute top 8 left 50% translate, text-caption 12 + text-text-secondary + uppercase + tracking-wider + leading-none)
6. .desktop-portrait-placeholder — W5 단계 placeholder (bg-surface-sunken 그레이 + dashed border-default-soft + 가운데 안내 텍스트)
</sketch_naming_patterns>
</context>

<tasks>

<task type="auto">
  <name>Task 1: sketch-wave-5-desktop-layout.html 생성 (2 frame · 좌측 편집 패널 축약 reference + 우측 A4 portrait placeholder)</name>

  <files>cha-bio-safety/docs/redesign-context/15-daily-report/sketch-wave-5-desktop-layout.html</files>

  <action>
**산출 파일 path (verbatim):** `cha-bio-safety/docs/redesign-context/15-daily-report/sketch-wave-5-desktop-layout.html`

**작업 순서 (atomic, sub-step ≤4):**

### Sub-step 1 — sketch HTML 골조 작성 (≈ 100 줄)

1. `<!DOCTYPE html>` + `<html lang="ko">` + `<head>` + `<meta charset/viewport>` + `<title>` (제목: `redesign/15-daily-report — sketch wave 5 (desktop layout)`).
2. `<style>` 안 tokens 정의 (W2/W3/W4 sketch 와 동일 패턴) — `:root` 다크 + `[data-theme="light"]` 라이트:
   - 다크: `--bg: #18181b; --bg2: #27272a; --t1: #f4f4f5; --t2: #a1a1aa; --t3: #71717a; --bd: #3f3f46; --bd-soft: #27272a; --safe-bar: #16a34a; --accent: #2563eb;`
   - 라이트: `--bg: #fafafa; --bg2: #ffffff; --t1: #18181b; --t2: #52525b; --t3: #71717a; --bd: #d4d4d8; --bd-soft: #e4e4e7; --safe-bar: #16a34a; --accent: #2563eb;`
3. body padding 24px + display flex column + gap 24, background #0a0a0a.
4. 메모리 룰 헤더 코멘트 박제 (≥7 unique slug — 아래 Sub-step 2 명세 참고).

### Sub-step 2 — sketch 본문 헤더 코멘트 (≈ 60 줄)

HTML 상단 `<!-- ... -->` 블록 (`<body>` 직후) 에 다음을 박제 (verbatim):

```
W5 — 데스크톱 layout (좌측 편집 패널 + 우측 A4 portrait preview)
Source: cha-bio-safety/src/pages/DailyReportPage.tsx line 403~444
Branch: redesign/15-daily-report / Quick ID: 260521-40d / Date: 2026-05-21

§1.3 verbatim (design-system.md v0.1.1):
  - 모바일/데스크톱은 같은 시스템, 다른 밀도
  - 폰트는 양쪽 동일 — 노안 대응 절대 룰
  - Spacing만 분기 / 데스크톱이 빽빽한 건 spacing 보다 레이아웃이 책임진다

§6.1 미적용 — 15-daily-report 페이지에는 진척률 도넛 없음
§6.2 미적용 — 15-daily-report 페이지에는 28px display 통계 카드 없음
  (memory feedback_tsx_wave_stat_card_drift — drift 방지 박제)
§6.4 그라데이션 폐기 — W4 LOCKED 일관 (data/monthly 모두 solid)

보존 (memory feedback_sketch_realistic_data):
  - "인쇄 미리보기" 라벨 verbatim (source line 432)
  - DailyPortraitPreview props: date / todayText / tomorrowText / notes / personnel (source line 434~440)
  - useIsDesktop 분기 보존 결정 (OQ #5 default = a — 소스 그대로)
  - 좌측 padding "24px 32px" verbatim (source line 408)
  - 우측 aspectRatio "210 / 297" + height 100% + flexShrink 0 + borderLeft (source line 417~420)

W3 OQ default inherit:
  - textarea 14 / 인원현황 14 / 초기화 유지 / focus border 유지
W4 OQ default inherit:
  - 모바일 spacing 8 / 데스크톱 row flex 1:1 / 안내 8 0 20 / monthly border-strong

메모리 룰 (unique slug ≥7, verbatim file name):
  1. feedback_design_sketch_first.md
  2. feedback_redesign_sketch_rule_enforcement.md
  3. feedback_sketch_realistic_data.md (라벨/시그니처 verbatim 박제)
  4. feedback_planner_prompt_sketch_verbatim.md (CSS class W7 grep 인용 룰)
  5. feedback_tailwind_token_class_pattern.md (status- prefix 없음 / size={N} prop)
  6. feedback_tailwind_w8_h8_is_48px.md (w-8=48 함정, w-7=32 사용)
  7. feedback_tsx_wave_emoji_dot_gap.md (이모지 0, dot span 명시)
  8. feedback_tsx_wave_stat_card_drift.md (§6.2 미적용 박제)
  9. feedback_avoid_premature_confirmation.md (자체 "approved" 자제)
 10. feedback_cbc7119_design_never_wr*ngler.md (wr* 마스킹 — negative gate 회피)
```

**메모리 룰 ≥10 박제 — 본 wave 요건 ≥7 초과 (안전 마진).**

### Sub-step 3 — 신규 class CSS 정의 (≈ 200 줄)

`<style>` 안 W5 신규 class 6건 정의 (위 `<sketch_naming_patterns>` ≥5건 + .desktop-portrait-placeholder = 6건):

```css
/* §1.3 모바일/데스크톱 동일 폰트 절대 룰 —
   좌측 편집 패널 폰트는 모바일 (W2/W3/W4) 와 동일,
   Spacing/Layout 만 분기 */
.desktop-layout {
  display: flex; flex-direction: row;
  height: 100%; min-height: 720px;
  overflow: hidden; background: var(--bg);
  border: 1px solid var(--bd); border-radius: 12px;
}
.desktop-edit-panel {
  flex: 1; overflow: auto;
  padding: 24px 32px; /* source line 408 verbatim */
  display: flex; flex-direction: column; gap: 16px;
  background: var(--bg);
}
.desktop-edit-panel-header {
  display: flex; justify-content: flex-end;
  margin-bottom: 8px; /* source line 410 marginBottom: 20 → 본 sketch 는 dateNav 자체 m-b 흡수, 8 만 추가 */
}
.desktop-portrait-wrapper {
  aspect-ratio: 210 / 297; /* source line 418 verbatim */
  height: 100%;
  flex-shrink: 0;
  border-left: 1px solid var(--bd);
  background: var(--bg);
  position: relative;
  display: flex; align-items: center; justify-content: center;
  overflow: hidden;
}
.desktop-portrait-print-label {
  position: absolute; top: 8px; left: 50%;
  transform: translateX(-50%);
  font-size: 12px; /* §1.1 — source line 428 fontSize 11 → 12 상향 */
  color: var(--t2);
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 0.06em; /* tracking-wider */
  line-height: 1; /* leading-none — memory feedback_text_caption_leading_none */
  pointer-events: none; z-index: 5;
}
.desktop-portrait-placeholder {
  /* W5 단계 placeholder — W6 wave 진입 시 본격 처리 */
  width: 90%; height: 90%;
  background: var(--bd-soft);
  border: 1px dashed var(--bd);
  border-radius: 4px;
  display: flex; align-items: center; justify-content: center;
  color: var(--t2);
  font-size: 12px; font-weight: 600;
  text-align: center; padding: 16px;
}
```

각 class 정의 위/아래에 `/* W7 변환 wave components.css 신규 추가 대상 (≥6건) */` 코멘트 박제.

### Sub-step 4 — F1 다크 frame + F2 라이트 frame markup (≈ 350 줄)

**Frame wrapper** (2개):
```html
<section class="frame">
  <header class="frame-head">F1 다크 데스크톱 viewport 1200×720 (좌측 편집 패널 flex:1 + 우측 A4 portrait aspect-ratio 210/297)</header>
  <div class="frame-canvas" style="width: 1200px; height: 720px; background: var(--bg);">
    <!-- 데스크톱 layout -->
    <div class="desktop-layout">
      <!-- 좌측 편집 패널 -->
      <div class="desktop-edit-panel">
        <!-- dateNav flex-end -->
        <div class="desktop-edit-panel-header">
          <!-- W2 dateNav 축약 reference (markup 동일) -->
          <div class="date-nav">
            <button class="date-nav-btn" aria-label="이전 날짜">‹</button>
            <span class="date-display">2026.05.21 (목)</span>
            <button class="date-nav-btn" aria-label="다음 날짜">›</button>
          </div>
        </div>

        <!-- W3 EditableCard × 3 축약 reference (markup 동일, rows verbatim 10/5/4) -->
        <div class="editable-card">
          <div class="editable-card-head">
            <span class="editable-card-label">금일업무</span>
            <div class="editable-card-actions">
              <button class="editable-card-btn--reset">초기화</button>
              <button class="editable-card-btn--save">저장</button>
            </div>
          </div>
          <textarea class="editable-card-textarea" rows="10" placeholder="금일 업무 내용을 입력하세요"></textarea>
        </div>
        <div class="editable-card">
          <div class="editable-card-head">
            <span class="editable-card-label">명일업무</span>
            <div class="editable-card-actions">
              <button class="editable-card-btn--reset">초기화</button>
              <button class="editable-card-btn--save">저장</button>
            </div>
          </div>
          <textarea class="editable-card-textarea" rows="5" placeholder="명일 업무 계획을 입력하세요"></textarea>
        </div>
        <div class="editable-card">
          <div class="editable-card-head">
            <span class="editable-card-label">특이사항</span>
            <div class="editable-card-actions">
              <button class="editable-card-btn--reset">초기화</button>
              <button class="editable-card-btn--save">저장</button>
            </div>
          </div>
          <textarea class="editable-card-textarea" rows="4" placeholder="특이사항이 있으면 입력하세요"></textarea>
        </div>

        <!-- W3 인원현황 SummaryCard 축약 reference -->
        <div class="summary-card">
          <div class="summary-card-label">인원현황</div>
          <div class="summary-card-body">
            총원 4<span class="dot-meta"></span>현재원 3<span class="dot-meta"></span>비번 1<span class="dot-meta"></span>연차 윤종엽
          </div>
        </div>

        <!-- W4 다운로드 액션 (데스크톱 row flex 1:1) -->
        <div class="download-action download-action--desktop">
          <button class="download-btn download-btn--daily">
            <span class="download-btn-icon" aria-hidden="true"><!-- lucide Download size 16 placeholder --></span>
            5월21일 방재업무일지 다운로드
          </button>
          <button class="download-btn download-btn--monthly">
            <span class="download-btn-icon" aria-hidden="true"><!-- lucide Download size 16 placeholder --></span>
            일일업무일지(5월) 다운로드
          </button>
        </div>

        <p class="page-footer-note">수정 내용은 자동 저장됩니다 · 월별은 저장된 모든 날짜를 포함합니다</p>
      </div>

      <!-- 우측 A4 portrait wrapper -->
      <div class="desktop-portrait-wrapper">
        <span class="desktop-portrait-print-label">인쇄 미리보기</span>
        <div class="desktop-portrait-placeholder">
          DailyPortraitPreview · W6 wrapper wave<br>
          (date / todayText / tomorrowText / notes / personnel)
        </div>
      </div>
    </div>
  </div>
</section>
```

F2 라이트 frame 은 동일 markup 을 `<div data-theme="light">` 으로 감싸 토큰 분기.

**중요:**
- `class="..."` 안에 `status-` prefix 0건, `w-8` / `h-8` 0건.
- 이모지/글리프 0건 (⬇/⚠/🎯/⚠ 등).
- `linear-gradient(` 0건 (CSS 본문).
- font-size 9/10/11 0건 (정확 gate `font-size:\s*(9px|10px|11px)`).
- W3/W4 의 class 정의 자체는 본 sketch 에 포함하지 않음 (영문 placeholder 만, "본 sketch 는 layout wrapper 만 신규 — W3/W4 class 는 각각 sketch 참조" 코멘트 박제).

### Sub-step 5 — OQ ≥3건 + verify gate ≥8 + negative gate ≥10 (≈ 150 줄)

frame 2개 아래 markdown-like `<div class="meta">` 영역 추가:

**OQ ≥3건 (W6 진입 전 컨펌, default 1줄 박제):**
```
OQ #1: 좌측 편집 패널 max-width 제한 — default 제한 없음 (source line 405 flex:1)
OQ #2: 우측 A4 portrait wrapper width — default aspect-ratio 210/297 + height:100% (source line 418~419 verbatim)
OQ #3: 좌측 편집 패널 padding — default 24px 32px (source line 408 verbatim)
OQ #4: "인쇄 미리보기" 라벨 위치 — default top 8 center absolute (source line 426~430 verbatim, fontSize 11→12 상향)
OQ #5: viewport 폭 1200~1280 중 어느 값? — default 1200 (frame canvas 폭)
```

**verify gate ≥8 (sketch 자체 검수, 사용자 직접 실행 가능한 grep 명령):**
```
gate 1: 2 frame 존재 — `grep -cE '<section class="frame"' sketch-wave-5-desktop-layout.html` ≥2
gate 2: 신규 class 정의 ≥5 — `grep -cE '^\.(desktop-(layout|edit-panel|edit-panel-header|portrait-wrapper|portrait-print-label|portrait-placeholder))[\s{]' sketch-wave-5-desktop-layout.html` ≥5
gate 3: 메모리 룰 unique slug ≥7 — `grep -oE 'feedback_[a-z_*]+\.md' sketch-wave-5-desktop-layout.html | sort -u | wc -l` ≥7
gate 4: OQ ≥3 — `grep -cE 'OQ #[0-9]+' sketch-wave-5-desktop-layout.html` ≥3
gate 5: "인쇄 미리보기" verbatim ≥2 (2 frame) — `grep -c '인쇄 미리보기' sketch-wave-5-desktop-layout.html` ≥2
gate 6: aspect-ratio 210 / 297 ≥1 — `grep -c 'aspect-ratio: 210 / 297' sketch-wave-5-desktop-layout.html` ≥1
gate 7: padding 24px 32px (source line 408 verbatim) ≥1 — `grep -c 'padding: 24px 32px' sketch-wave-5-desktop-layout.html` ≥1
gate 8: useIsDesktop 박제 코멘트 ≥1 — `grep -c 'useIsDesktop' sketch-wave-5-desktop-layout.html` ≥1
gate 9: DailyPortraitPreview 시그니처 박제 ≥1 — `grep -c 'DailyPortraitPreview' sketch-wave-5-desktop-layout.html` ≥1
gate 10: §1.3 인용 ≥1 — `grep -c '§1.3' sketch-wave-5-desktop-layout.html` ≥1
```

**negative gate ≥10 (sketch 본문 자체 검수):**
```
neg 1: 이모지/글리프 0 — `grep -cE '[⬇⚠🎯⬆⚙️📋]' sketch-wave-5-desktop-layout.html` =0
neg 2: linear-gradient( 0 — `grep -c 'linear-gradient(' sketch-wave-5-desktop-layout.html` =0
neg 3: font-size 9/10/11 0 — `grep -cE 'font-size:\s*(9px|10px|11px)' sketch-wave-5-desktop-layout.html` =0
neg 4: status- prefix class 실사용 0 — `grep -cE 'class="[^"]*\bstatus-[a-z-]+' sketch-wave-5-desktop-layout.html` =0
neg 5: w-8 / h-8 utility 실사용 0 — `grep -cE 'class="[^"]*\b(w-8|h-8)\b' sketch-wave-5-desktop-layout.html` =0
neg 6: wr+angler 명령 0 (메모리 슬러그 마스킹 wr*ngler 만 허용) — `grep -cE '\bwrangler\b' sketch-wave-5-desktop-layout.html` =0
neg 7: npm run deploy 0 — `grep -c 'npm run deploy' sketch-wave-5-desktop-layout.html` =0
neg 8: src/** 변경 0 (sketch 만 손댐) — `git diff --name-only HEAD -- cha-bio-safety/src` 결과 0 line
neg 9: components.css 변경 0 — `git diff --name-only HEAD -- cha-bio-safety/src/styles/components.css` 결과 0 line
neg 10: App.tsx 변경 0 — `git diff --name-only HEAD -- cha-bio-safety/src/App.tsx` 결과 0 line
neg 11: 다른 페이지 (13/14/02/06) sketch 변경 0 — `git diff --name-only HEAD -- cha-bio-safety/docs/redesign-context | grep -vE '15-daily-report/sketch-wave-5'` 결과 0 line
```

**neg ≥10 요건 초과 (안전 마진).**

### Sub-step 6 — line count + 마무리

1. 작성 완료 후 `wc -l sketch-wave-5-desktop-layout.html` 으로 700~900 줄 범위 확인. 950 이상이면 placeholder 코멘트 일부 축약.
2. 위 grep 명령 10개 (verify gate 1~10 + negative gate 1~11) 실행 — 모두 PASS 확인.
3. 결과 사용자에게 보고. memory feedback_avoid_premature_confirmation 준수 — "approved 주세요" 같은 자체 확신 표현 금지, "F1/F2 확인 후 W6 진입 의사 부탁" 만.
  </action>

  <verify>
    <automated>
# 1. 파일 존재 + 라인 범위 700~900
test -f cha-bio-safety/docs/redesign-context/15-daily-report/sketch-wave-5-desktop-layout.html && \
LC=$(wc -l < cha-bio-safety/docs/redesign-context/15-daily-report/sketch-wave-5-desktop-layout.html) && \
[ "$LC" -ge 700 ] && [ "$LC" -le 900 ] && \
\
# 2. 2 frame 존재
[ "$(grep -cE '<section class="frame"' cha-bio-safety/docs/redesign-context/15-daily-report/sketch-wave-5-desktop-layout.html)" -ge 2 ] && \
\
# 3. 신규 class 정의 ≥5 (header 제외 = leading `.desktop-` only)
[ "$(grep -cE '^\.desktop-(layout|edit-panel|edit-panel-header|portrait-wrapper|portrait-print-label|portrait-placeholder)[[:space:]{]' cha-bio-safety/docs/redesign-context/15-daily-report/sketch-wave-5-desktop-layout.html)" -ge 5 ] && \
\
# 4. memory 룰 unique slug ≥7 (wr*ngler 마스킹 허용)
[ "$(grep -oE 'feedback_[a-zA-Z0-9_*]+\.md' cha-bio-safety/docs/redesign-context/15-daily-report/sketch-wave-5-desktop-layout.html | sort -u | wc -l)" -ge 7 ] && \
\
# 5. OQ ≥3
[ "$(grep -cE 'OQ #[0-9]+' cha-bio-safety/docs/redesign-context/15-daily-report/sketch-wave-5-desktop-layout.html)" -ge 3 ] && \
\
# 6. "인쇄 미리보기" 2 frame 모두 ≥2
[ "$(grep -c '인쇄 미리보기' cha-bio-safety/docs/redesign-context/15-daily-report/sketch-wave-5-desktop-layout.html)" -ge 2 ] && \
\
# 7. aspect-ratio: 210 / 297 verbatim ≥1
[ "$(grep -c 'aspect-ratio: 210 / 297' cha-bio-safety/docs/redesign-context/15-daily-report/sketch-wave-5-desktop-layout.html)" -ge 1 ] && \
\
# 8. padding 24px 32px verbatim ≥1
[ "$(grep -c 'padding: 24px 32px' cha-bio-safety/docs/redesign-context/15-daily-report/sketch-wave-5-desktop-layout.html)" -ge 1 ] && \
\
# 9. useIsDesktop 박제 ≥1
[ "$(grep -c 'useIsDesktop' cha-bio-safety/docs/redesign-context/15-daily-report/sketch-wave-5-desktop-layout.html)" -ge 1 ] && \
\
# 10. DailyPortraitPreview 시그니처 박제 ≥1
[ "$(grep -c 'DailyPortraitPreview' cha-bio-safety/docs/redesign-context/15-daily-report/sketch-wave-5-desktop-layout.html)" -ge 1 ] && \
\
# 11. §1.3 인용 ≥1
[ "$(grep -c '§1.3' cha-bio-safety/docs/redesign-context/15-daily-report/sketch-wave-5-desktop-layout.html)" -ge 1 ] && \
\
# negative gates
# 12. 이모지 0
[ "$(grep -cE '[⬇⚠🎯⬆⚙️📋]' cha-bio-safety/docs/redesign-context/15-daily-report/sketch-wave-5-desktop-layout.html)" -eq 0 ] && \
\
# 13. linear-gradient( 0
[ "$(grep -c 'linear-gradient(' cha-bio-safety/docs/redesign-context/15-daily-report/sketch-wave-5-desktop-layout.html)" -eq 0 ] && \
\
# 14. font-size 9/10/11 0 (exact gate)
[ "$(grep -cE 'font-size:[[:space:]]*(9px|10px|11px)' cha-bio-safety/docs/redesign-context/15-daily-report/sketch-wave-5-desktop-layout.html)" -eq 0 ] && \
\
# 15. status- prefix class 실사용 0 (class="..." 안만)
[ "$(grep -cE 'class="[^"]*\bstatus-[a-z-]+' cha-bio-safety/docs/redesign-context/15-daily-report/sketch-wave-5-desktop-layout.html)" -eq 0 ] && \
\
# 16. w-8 / h-8 utility 실사용 0
[ "$(grep -cE 'class="[^"]*\b(w-8|h-8)\b' cha-bio-safety/docs/redesign-context/15-daily-report/sketch-wave-5-desktop-layout.html)" -eq 0 ] && \
\
# 17. wr+angler 실명 0 (마스킹 wr*ngler 만 허용)
[ "$(grep -cE '\bwrangler\b' cha-bio-safety/docs/redesign-context/15-daily-report/sketch-wave-5-desktop-layout.html)" -eq 0 ] && \
\
# 18. npm run deploy 0
[ "$(grep -c 'npm run deploy' cha-bio-safety/docs/redesign-context/15-daily-report/sketch-wave-5-desktop-layout.html)" -eq 0 ] && \
\
# 19. src/** 변경 0
[ -z "$(git -C /Users/jykevin/Documents/cbc7119-design diff --name-only HEAD -- cha-bio-safety/src 2>/dev/null)" ] && \
\
# 20. 다른 15-daily-report sketch 변경 0 (W5 외)
[ -z "$(git -C /Users/jykevin/Documents/cbc7119-design diff --name-only HEAD -- cha-bio-safety/docs/redesign-context | grep -v '15-daily-report/sketch-wave-5-desktop-layout.html' | grep -vE '\.planning/' || true)" ] && \
\
echo OK
    </automated>
  </verify>

  <done>
- `cha-bio-safety/docs/redesign-context/15-daily-report/sketch-wave-5-desktop-layout.html` 생성 (700~900 줄).
- 2 frame (F1 다크 + F2 라이트) 데스크톱 viewport 1200×720 markup 포함.
- 신규 class 6건 (`.desktop-layout` / `.desktop-edit-panel` / `.desktop-edit-panel-header` / `.desktop-portrait-wrapper` / `.desktop-portrait-print-label` / `.desktop-portrait-placeholder`) 정의 + W7 components.css 추가 대상 코멘트 박제.
- 좌측 편집 패널은 W2 dateNav + W3 EditableCard × 3 (rows 10/5/4 verbatim) + 인원현황 SummaryCard + W4 다운로드 액션 (data/monthly row 1:1) + 안내 줄 의 markup 을 축약 reference 로 배치 (W3/W4 class 정의 본문 포함 X).
- 우측 portrait wrapper 는 placeholder 만 + W6 위임 코멘트 명시.
- "인쇄 미리보기" 라벨 verbatim (line 432) + DailyPortraitPreview props 시그니처 (line 434~440) + useIsDesktop 분기 보존 (line 64) + 좌측 padding 24px 32px (line 408) + 우측 aspectRatio 210/297 (line 418) — 모두 sketch 코멘트로 박제.
- §1.3 모바일/데스크톱 동일 폰트 절대 룰 코멘트 박제. §6.1/§6.2 미적용 (drift 방지) 코멘트 박제. §6.4 그라데이션 폐기 inherit 명시.
- W3 OQ default inherit (textarea 14 / 인원현황 14 / 초기화 유지 / focus border 유지) + W4 OQ default inherit (모바일 spacing 8 / 데스크톱 row flex 1:1 / 안내 8 0 20 / monthly border-strong) 박제.
- 메모리 룰 unique slug ≥7건 (실제 ≥10 — `feedback_design_sketch_first` / `feedback_redesign_sketch_rule_enforcement` / `feedback_sketch_realistic_data` / `feedback_planner_prompt_sketch_verbatim` / `feedback_tailwind_token_class_pattern` / `feedback_tailwind_w8_h8_is_48px` / `feedback_tsx_wave_emoji_dot_gap` / `feedback_tsx_wave_stat_card_drift` / `feedback_avoid_premature_confirmation` / `feedback_cbc7119_design_never_wr*ngler` — wr* 마스킹).
- OQ ≥3건 (실제 5건) + verify gate ≥8건 (실제 10건) + negative gate ≥10건 (실제 11건) sketch 본문에 박제.
- 이모지/글리프 0, `linear-gradient(` 0, font-size 9/10/11 0, status- prefix 실사용 0, w-8/h-8 utility 실사용 0, wrangler 실명 0, `npm run deploy` 0.
- src/** / components.css / App.tsx / 다른 페이지 변경 0.
- verify gate 의 자동화 명령 통과 (`echo OK`).
  </done>
</task>

</tasks>

<verification>
**Phase-level smoke (Task 1 verify 와 동일 — 단일 task):**

```bash
cd /Users/jykevin/Documents/cbc7119-design
test -f cha-bio-safety/docs/redesign-context/15-daily-report/sketch-wave-5-desktop-layout.html && \
  echo "FILE OK" && \
  wc -l cha-bio-safety/docs/redesign-context/15-daily-report/sketch-wave-5-desktop-layout.html
```

브라우저 자체 검증 (사용자가 수동 — sketch 시각 검수):
- 다크/라이트 양 frame 1200×720 canvas 모두 좌측 패널(flex:1) + 우측 A4(210/297) 분할 확인
- "인쇄 미리보기" 라벨 우측 상단 가운데 표시 확인
- 우측 placeholder 안 안내 텍스트 가독성 확인 (DailyPortraitPreview · W6 wrapper wave)

사용자 컨펌 후 W6 진입 (`sketch-wave-6-portrait-preview-wrapper.html`).
</verification>

<success_criteria>
- 산출 파일 `sketch-wave-5-desktop-layout.html` 700~900 줄 범위 생성
- verify gate 1~11 + negative gate 12~20 모두 PASS (Task 1 automated 통과)
- 2 frame (F1 다크 / F2 라이트) viewport 1200×720 데스크톱 layout markup 포함
- 신규 class 6건 정의 + W7 변환 wave 인용 대상 코멘트 박제
- 좌측 편집 패널 = W2/W3/W4 markup 축약 reference + 우측 placeholder + 보존 항목 verbatim 박제
- 메모리 룰 unique slug ≥7 (실제 10) + OQ ≥3 (실제 5) 박제
- src/** / components.css / App.tsx / 다른 페이지 변경 0
- 다음 단계 = 사용자 F1/F2 시각 검수 → W6 진입 컨펌 (memory feedback_avoid_premature_confirmation 준수, Claude 자체 "approved" 표현 금지)
</success_criteria>

<output>
After completion, create `.planning/quick/260521-40d-redesign-15-daily-report-sketch-wave-5-l/260521-40d-SUMMARY.md` with:
- Files created (sketch-wave-5-desktop-layout.html line count)
- F1/F2 frame 구성 요약
- 신규 class 6건 명단 + W7 인용 대상 코멘트 위치
- 보존 항목 박제 위치 (라벨/시그니처/useIsDesktop/padding/aspectRatio)
- 메모리 룰 박제 슬러그 ≥7 명단
- OQ ≥3 (실제 5) 명단 + default 박제
- verify gate 통과 결과 (10 gate + 11 negative gate)
- 다음 단계 = 사용자 시각 검수 → W6 진입 컨펌 부탁
</output>
