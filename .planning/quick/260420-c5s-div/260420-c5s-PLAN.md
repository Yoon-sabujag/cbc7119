---
phase: 260420-c5s
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cha-bio-safety/src/pages/DivPage.tsx
autonomous: true
requirements:
  - QUICK-260420-c5s
must_haves:
  truths:
    - "DIV 페이지에 4개 탭이 보인다: '압력 트렌드', '챔버배수주기', '탱크배수주기', '오일 주기'"
    - "'챔버배수주기' 탭은 기존 '배수 주기' 탭과 동일한 데이터(div_drain_log)를 동일한 하늘색 막대그래프로 보여준다"
    - "'탱크배수주기' 탭은 comp_drain_log 데이터를 갈색 막대그래프로 층별 3열 그리드에 보여준다"
    - "'탱크배수주기' 탭을 처음 눌렀을 때 /api/div/logs?type=comp_drain이 호출된다"
  artifacts:
    - path: "cha-bio-safety/src/pages/DivPage.tsx"
      provides: "4-tab DIV page with tank-drain tab"
      contains: "comp_drain"
  key_links:
    - from: "DivPage.tsx tab list"
      to: "renderLogTab dispatch"
      via: "tab === 'comp_drain' branch"
      pattern: "tab === 'comp_drain'"
    - from: "fetchLogs('comp_drain')"
      to: "/api/div/logs?type=comp_drain"
      via: "useQuery with enabled: tab === 'comp_drain'"
      pattern: "type=\\$\\{type\\}"
---

<objective>
DIV 압력 관리 페이지에 '탱크배수주기' 탭을 추가하고 기존 '배수 주기' 탭을 '챔버배수주기'로 이름 변경한다.

- 기존: 압력 트렌드 / 배수 주기(drain) / 오일 주기(compressor) — 3개 탭
- 변경 후: 압력 트렌드 / 챔버배수주기(drain) / 탱크배수주기(comp_drain, NEW) / 오일 주기(compressor) — 4개 탭

Purpose: 컴프레셔 탱크 배수 이력(comp_drain_log)을 페이지에서 직접 확인할 수 있게 해 현장에서 주기 관리 가시성 확보.

Output: 수정된 `cha-bio-safety/src/pages/DivPage.tsx` 1개 파일.

Backend/DB는 이미 준비되어 있음 (logs.ts의 TABLE_MAP에 comp_drain 매핑 존재, 마이그레이션 0059 적용됨). 이 플랜은 프론트엔드 단독 변경.
</objective>

<context>
@cha-bio-safety/src/pages/DivPage.tsx
@cha-bio-safety/functions/api/div/logs.ts
@cha-bio-safety/migrations/0059_compressor.sql

<interfaces>
<!-- 기존 DivPage.tsx에서 재사용할 핵심 함수 시그니처 (이미 genericized되어 있음) -->

fetchLogs (line 82):
```typescript
async function fetchLogs(type: 'drain' | 'compressor') {
  const res = await fetch(`/api/div/logs?type=${type}`, { headers: authHeader() })
  const j   = await res.json() as { ok: boolean; logs: any[] }
  return j.logs ?? []
}
```
→ 타입 유니온에 'comp_drain' 추가만 하면 됨.

renderLogTab (line 334):
```typescript
function renderLogTab(type: 'drain' | 'compressor') {
  const dateMap = type === 'drain' ? drainDateMap : oilDateMap
  const color   = type === 'drain' ? '#38bdf8' : '#f97316'
  ...
}
```
→ 타입 유니온에 'comp_drain' 추가, dateMap/color 분기 1개씩 추가.

comp_drain_log 스키마 (migration 0059):
- `div_id` TEXT NOT NULL
- `drained_at` TEXT NOT NULL (YYYY-MM-DD)
→ div_drain_log와 동일 shape이므로 기존 drainDateMap 패턴(line 213-221)을 그대로 복제 가능.

Tab 타입 (line 155):
```typescript
type Tab = 'pressure' | 'drain' | 'compressor'
```
→ 'comp_drain' 추가.

탭 버튼 배열 (line 532-536), 렌더 dispatch (line 552-555)도 각각 1개씩 엔트리 추가.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DivPage에 '탱크배수주기' 탭 추가 및 '배수 주기' → '챔버배수주기' 라벨 변경</name>
  <files>cha-bio-safety/src/pages/DivPage.tsx</files>
  <action>
DivPage.tsx 파일을 아래 7곳 순서대로 수정한다. 모든 변경은 기존 패턴(drain/compressor 분기)을 대칭적으로 복제하는 것이며, 백엔드 API는 이미 `comp_drain` 타입을 지원한다(logs.ts:10 확인 완료).

**1. 파일 상단 doc comment 업데이트 (line 2-6)**

기존:
```
 * 탭 1: 압력 트렌드 — 34개 측정점 1차압/2차압/챔버압
 * 탭 2: 배수 주기   — 배수 이력 기록
 * 탭 3: 오일 주기   — 컴프레셔 오일 보충 이력
```
변경:
```
 * 탭 1: 압력 트렌드     — 34개 측정점 1차압/2차압/챔버압
 * 탭 2: 챔버배수주기   — DIV 챔버 배수 이력 (div_drain_log)
 * 탭 3: 탱크배수주기   — 컴프레셔 탱크 배수 이력 (comp_drain_log)
 * 탭 4: 오일 주기       — 컴프레셔 오일 보충 이력 (div_compressor_log)
```

**2. fetchLogs 타입 유니온 확장 (line 82)**

기존:
```typescript
async function fetchLogs(type: 'drain' | 'compressor') {
```
변경:
```typescript
async function fetchLogs(type: 'drain' | 'compressor' | 'comp_drain') {
```
(함수 본문은 그대로 — 이미 템플릿 리터럴로 type을 쿼리 파라미터에 주입.)

**3. Tab 유니온 타입 확장 (line 155)**

기존:
```typescript
type Tab = 'pressure' | 'drain' | 'compressor'
```
변경:
```typescript
type Tab = 'pressure' | 'drain' | 'comp_drain' | 'compressor'
```

**4. useQuery 블록 추가 (line 192 `oilLogs` 쿼리 바로 위에 삽입)**

기존 drainLogs/oilLogs 패턴을 그대로 복제하여 `compDrainLogs`를 추가한다. `oilLogs` useQuery 블록(line 192-196) 바로 앞에 아래 블록을 삽입:

```typescript
  const { data: compDrainLogs = [] } = useQuery({
    queryKey: ['div-comp-drain'],
    queryFn: () => fetchLogs('comp_drain'),
    enabled: tab === 'comp_drain',
  })
```

**5. compDrainDateMap useMemo 추가 (line 223 `oilDateMap` useMemo 바로 위에 삽입)**

comp_drain_log 행은 div_drain_log와 동일하게 `div_id` + `drained_at` 컬럼을 가지므로 drainDateMap 패턴을 그대로 복제. `oilDateMap` useMemo 블록 바로 앞에 삽입:

```typescript
  const compDrainDateMap = useMemo(() => {
    const m: Record<string, string[]> = {}
    for (const log of compDrainLogs) {
      if (!m[log.div_id]) m[log.div_id] = []
      if (!m[log.div_id].includes(log.drained_at)) m[log.div_id].push(log.drained_at)
    }
    for (const k of Object.keys(m)) m[k].sort()
    return m
  }, [compDrainLogs])
```

**6. renderLogTab 함수 확장 (line 334-336)**

기존:
```typescript
  function renderLogTab(type: 'drain' | 'compressor') {
    const dateMap = type === 'drain' ? drainDateMap : oilDateMap
    const color   = type === 'drain' ? '#38bdf8' : '#f97316'
```
변경:
```typescript
  function renderLogTab(type: 'drain' | 'comp_drain' | 'compressor') {
    const dateMap = type === 'drain' ? drainDateMap : type === 'comp_drain' ? compDrainDateMap : oilDateMap
    const color   = type === 'drain' ? '#38bdf8' : type === 'comp_drain' ? '#8b4513' : '#f97316'
```
(갈색: `#8b4513` saddle brown — 기존 하늘색/주황색과 명확히 구분되고 자연스러운 고동색.)

**7. 탭 버튼 배열 및 라벨 변경 (line 532-536)**

기존:
```typescript
        {([
          { key: 'pressure',   label: '압력 트렌드' },
          { key: 'drain',      label: '배수 주기' },
          { key: 'compressor', label: '오일 주기' },
        ] as { key: Tab; label: string }[]).map(t => (
```
변경:
```typescript
        {([
          { key: 'pressure',   label: '압력 트렌드' },
          { key: 'drain',      label: '챔버배수주기' },
          { key: 'comp_drain', label: '탱크배수주기' },
          { key: 'compressor', label: '오일 주기' },
        ] as { key: Tab; label: string }[]).map(t => (
```

**8. 렌더 dispatch에 comp_drain 케이스 추가 (line 552-556)**

기존:
```typescript
      <div style={{ flex: 1, overflowY: 'auto' }}>
        {tab === 'pressure'   && renderPressureTab()}
        {tab === 'drain'      && renderLogTab('drain')}
        {tab === 'compressor' && renderLogTab('compressor')}
      </div>
```
변경:
```typescript
      <div style={{ flex: 1, overflowY: 'auto' }}>
        {tab === 'pressure'   && renderPressureTab()}
        {tab === 'drain'      && renderLogTab('drain')}
        {tab === 'comp_drain' && renderLogTab('comp_drain')}
        {tab === 'compressor' && renderLogTab('compressor')}
      </div>
```

**준수 사항:**
- 2-space indentation, inline styles 유지 (기존 파일 컨벤션 그대로).
- 탭 순서는 `압력 트렌드 → 챔버배수주기 → 탱크배수주기 → 오일 주기`. 챔버 다음에 탱크가 오도록(배수 계열을 인접 배치).
- 색상은 반드시 `#8b4513` (saddle brown) 사용. amber-800(#92400e)은 오일 `#f97316`과 너무 가까워 피함.
- 새 코드 어디에도 backwards-compat 코멘트, v1, 임시, TODO 추가 금지.
- 8곳 외에는 파일을 건드리지 않는다 (특히 renderPressureTab, renderDivDetail은 수정 범위 밖).
  </action>
  <verify>
    <automated>cd cha-bio-safety && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "DivPage|error TS" | head -20</automated>
Expected: DivPage.tsx 관련 타입 에러 0건. (기존 파일의 다른 non-strict 경고는 무시.)

Manual check (after deploy `wrangler deploy --branch production`):
1. 앱에서 DIV 페이지 진입 → 탭 4개 표시: `압력 트렌드`, `챔버배수주기`, `탱크배수주기`, `오일 주기`
2. `챔버배수주기` 탭 선택 → 기존 배수 주기와 동일한 하늘색(#38bdf8) 막대그래프 표시
3. `탱크배수주기` 탭 선택 → 네트워크 탭에서 `GET /api/div/logs?type=comp_drain` 호출 확인 → 층별 3열 그리드에 갈색(#8b4513) 막대그래프 표시 (데이터 없는 DIV는 "기록 없음")
4. `오일 주기` 탭은 기존과 동일 작동
  </verify>
  <done>
- DivPage.tsx에 Tab 유니온이 'pressure' | 'drain' | 'comp_drain' | 'compressor' 4개 값을 포함한다
- fetchLogs와 renderLogTab 시그니처가 'comp_drain'을 받을 수 있다
- compDrainLogs useQuery와 compDrainDateMap useMemo가 존재하며 drain 패턴과 대칭적이다
- 탭 버튼에 '챔버배수주기'(key: drain)와 '탱크배수주기'(key: comp_drain)가 순서대로 나타난다
- 렌더 dispatch에 `tab === 'comp_drain' && renderLogTab('comp_drain')` 라인이 존재한다
- `#8b4513` 색상 리터럴이 renderLogTab의 color 삼항식에 포함되어 있다
- tsc --noEmit에서 DivPage 관련 신규 타입 에러가 없다
  </done>
</task>

</tasks>

<verification>
- TypeScript 컴파일: `cd cha-bio-safety && npx tsc --noEmit` — DivPage.tsx 관련 신규 에러 0건
- 프로덕션 배포 후 (사용자 직접 수행): `wrangler deploy --branch production`
- 브라우저에서 4개 탭 정상 표시 + 탱크배수주기 탭이 comp_drain API 호출 확인
</verification>

<success_criteria>
- DIV 페이지 탭 개수 3 → 4로 증가
- '배수 주기' 라벨이 화면에서 사라지고 '챔버배수주기'로 대체됨
- '탱크배수주기' 탭이 `comp_drain_log` 데이터를 층별 3열 갈색 막대그래프로 보여줌
- 수정 파일은 DivPage.tsx 1개뿐 (DB/API/migration 수정 없음)
</success_criteria>

<output>
After completion, create `.planning/quick/260420-c5s-div/260420-c5s-SUMMARY.md` with:
- 변경된 8개 편집 포인트 요약
- 선택한 갈색 hex(#8b4513) 및 근거
- 프로덕션 배포 후 4개 탭 스모크 테스트 체크리스트
</output>
