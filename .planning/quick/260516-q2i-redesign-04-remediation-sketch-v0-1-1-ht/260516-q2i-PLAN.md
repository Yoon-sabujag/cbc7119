---
phase: 260516-q2i
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cha-bio-safety/docs/redesign-context/04-remediation/sketch/remediation-sketch.html
autonomous: false
requirements:
  - REM-SKETCH-01
must_haves:
  truths:
    - "단일 HTML 파일에 모바일/데스크톱 + 다크/라이트 + 카드 상태(불량/주의 × 미조치/완료) + 빈/로딩/에러 상태가 모두 시각화된다"
    - "모든 status 색은 v0.1.1 토큰(--status-*-bar / --status-*-bg / --status-*)만 사용한다 (인라인 hex 0건, --bg/--t1/--danger 등 옛 alias 0건)"
    - "카드 좌측 색바는 '결과(불량/주의)' 가 아니라 '상태(미조치=fire / 완료=safe)' 기준이다 (메모 feedback_inspection_unresolved_color 룰 강제)"
    - "카드 안 결과 배지는 --status-danger-bg/--status-danger (불량) / --status-warning-bg/--status-warning (주의) 페어로만 채워진다"
    - "데스크톱은 좌 50% 목록 / 우 50% 보고서 상세 패널 분할이고, 우측은 KV 테이블 + 조치전/조치후 사진 그리드 + 보고서 다운로드 버튼 + 사진 다운로드 버튼이 모두 노출된다"
    - "필터 바(전체/미조치/완료 탭 + 카테고리 select + 7/30/90/전체 기간 버튼)가 sticky 로 상단 고정되며 활성/비활성 토큰이 일관되게 분기된다"
    - "lucide CDN 아이콘이 사용된다 (이모지 0건). 카테고리/도구 아이콘은 §7.2/§7.3/§7.4 매핑에 따른다"
    - "1px hairline 작은 컨테이너(배지/칩) 안의 텍스트는 leading-none 명시 (메모 feedback_text_caption_leading_none 룰)"
  artifacts:
    - path: "cha-bio-safety/docs/redesign-context/04-remediation/sketch/remediation-sketch.html"
      provides: "조치 관리 페이지 v0.1.1 시안 단일 HTML — 모바일/데스크톱 × 다크/라이트 × 5 상태"
      min_lines: 700
      contains: "data-theme=\"dark\", data-theme=\"light\", viewport-mobile, viewport-desktop, --status-fire-bar, --status-safe-bar, --status-danger-bg, --status-warning-bg, lucide"
  key_links:
    - from: "remediation-sketch.html <head>"
      to: "tokens.css 인라인 dark/light :root 블록"
      via: "[data-theme=\"dark\"] / [data-theme=\"light\"] CSS 변수 정의 verbatim"
      pattern: "--status-(safe|warning|danger|info|fire)-(bar|bg)"
    - from: "각 viewport-frame section"
      to: "data-theme attribute"
      via: "section data-theme=\"dark|light\" → 내부 토큰 자동 분기"
      pattern: "data-theme=\"(dark|light)\""
    - from: "카드 좌측 색바 (.rem-card-bar.fire / .rem-card-bar.safe)"
      to: "status fire/safe bar 토큰"
      via: "background: var(--status-fire-bar) / var(--status-safe-bar)"
      pattern: "background:\\s*var\\(--status-(fire|safe)-bar\\)"
---

<objective>
조치 관리 페이지(`/remediation`)의 v0.1.1 디자인 시스템 정합 시안 HTML을 단일 파일로 작성한다.

Purpose: 03-qr-scan/sketch/qr-scan-sketch.html 의 검증된 패턴(단일 HTML + 다중 viewport-frame + dark/light × 모바일/데스크톱 × 5 상태)을 그대로 차용해, 4-remediation TSX 변환 단계 진입 전에 시각 디자인을 사용자가 한 번에 검토 승인할 수 있도록 한다.

Output: `cha-bio-safety/docs/redesign-context/04-remediation/sketch/remediation-sketch.html` 단일 파일. React/빌드 없음. Tailwind CDN + lucide CDN + 인라인 CSS/JS 만 사용.

비-목표:
- 비즈니스 로직 변경 금지 (display 분기/라벨 룰은 RemediationPage.tsx 그대로 유지 — TSX 변환 페이즈에서 처리)
- TSX 작성 금지 (사용자가 시안 컨펌 후 별도 quick 으로 변환)
- 실제 API/DB 연동 없음 (목 데이터로 한국어 fire-safety 도메인 데이터 하드코딩)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@cha-bio-safety/docs/redesign-context/04-remediation/04-remediation.md
@cha-bio-safety/docs/redesign-context/04-remediation/design-system.md
@cha-bio-safety/docs/redesign-context/04-remediation/RemediationPage.tsx
@cha-bio-safety/docs/redesign-context/04-remediation/tokens.css
@cha-bio-safety/docs/redesign-context/04-remediation/typography.css
@cha-bio-safety/docs/redesign-context/04-remediation/ui-index.tsx
@cha-bio-safety/docs/redesign-context/04-remediation/PhotoGrid.tsx
@cha-bio-safety/docs/redesign-context/03-qr-scan/sketch/qr-scan-sketch.html

<reference_pattern>
**검증된 패턴 (qr-scan-sketch.html line 1~200 verbatim 차용):**
- `<head>` 안에 Tailwind CDN, Pretendard, lucide CDN, tokens.css 인라인, typography.css 인라인
- `[data-theme="dark"]` / `[data-theme="light"]` CSS 변수 블록 (tokens.css 86~119, 16~69 verbatim)
- `.viewport-frame` (radius 24px, box-shadow, overflow hidden, background var(--surface-page))
- `.viewport-mobile { width: 393px; height: 852px; }`
- `.viewport-desktop { width: 1280px; height: 720px; }`
- `.page-shell { display:flex; flex-direction:column; height:100%; }`
- `.desktop-shell { display:flex; height:100%; }` (좌/우 분할 — 04-remediation 은 50/50)
- `.meta-label` (시안 외곽 라벨 — `📱 VP1 · 모바일 · 다크 · ...`)
- 각 `<section data-theme="dark|light">` 안에 viewport-frame 1개
- `<script>lucide.createIcons();</script>` body 끝
</reference_pattern>

<verbatim_tokens>
**design-system.md / tokens.css 에서 verbatim 인용해야 할 핵심 값 (이 페이지 한정):**

Status (다크):
- `--status-safe-bar: #22c55e;`     `--status-safe-bg: rgba(34, 197, 94, 0.16);`     `--status-safe: #4ade80;`
- `--status-warning-bar: #f59e0b;`  `--status-warning-bg: rgba(245, 158, 11, 0.16);` `--status-warning: #fbbf24;`
- `--status-danger-bar: #ef4444;`   `--status-danger-bg: rgba(239, 68, 68, 0.16);`   `--status-danger: #f87171;`
- `--status-fire-bar: #f97316;`     `--status-fire-bg: rgba(249, 115, 22, 0.16);`    `--status-fire: #fb923c;`

Status (라이트):
- `--status-safe-bar: #15803d;`     `--status-safe-bg: #dcfce7;`    `--status-safe: #166534;`
- `--status-warning-bar: #b45309;`  `--status-warning-bg: #fef3c7;` `--status-warning: #854d0e;`
- `--status-danger-bar: #b91c1c;`   `--status-danger-bg: #fee2e2;`  `--status-danger: #991b1b;`
- `--status-fire-bar: #c2410c;`     `--status-fire-bg: #ffedd5;`    `--status-fire: #9a3412;`

Spacing (모바일 기본):
- `--card-padding: 14px;`  `--card-gap: 8px;`  `--page-padding: 16px;`  `--section-gap: 24px;`
- `--input-height: 44px;`  `--button-height: 44px;`

Spacing (데스크톱 ≥768px):
- `--card-padding: 10px;`  `--card-gap: 6px;`  `--page-padding: 24px;`  `--section-gap: 32px;`
- `--input-height: 40px;`  `--button-height: 40px;`

Radius:
- `--radius-sm: 8px;`  `--radius-md: 12px;`  `--radius-lg: 16px;`  `--radius-pill: 99px;`

Typography (verbatim from typography.css line 33~81):
- `.text-caption  { font-size: 12px; line-height: 1.5; font-weight: 400; }`
- `.text-label    { font-size: 13px; line-height: 1.5; font-weight: 400; }`
- `.text-body-sm  { font-size: 14px; line-height: 1.6; font-weight: 400; }`
- `.text-body     { font-size: 16px; line-height: 1.7; font-weight: 400; }`
- `.text-title    { font-size: 18px; line-height: 1.4; font-weight: 500; }`
- `.text-heading  { font-size: 22px; line-height: 1.3; font-weight: 600; letter-spacing: -0.01em; }`
- `.text-display  { font-size: 28px; line-height: 1.0; font-weight: 500; letter-spacing: -0.02em; }`

§6.4 그라디언트 — 보고서 다운로드 버튼은 v0.1.1 룰상 **단색 accent** 권장 (3C 일관). 옛 `linear-gradient(135deg,#1d4ed8,#2563eb)` 그라디언트는 변환 시 단색 `var(--accent)` 로 교체 (qr-scan-sketch VP4 비교 카탈로그 결정 근거와 동일).

§7.3 상태/결과 아이콘:
- 정상: `CheckCircle2` / 주의: `AlertTriangle` / 불량: `XCircle` / 미조치: `Wrench` / 긴급/조치대기: `Flame`

§7.4 도구 아이콘 (이 페이지 사용):
- 보고서 다운로드: `Download` (lucide)
- 사진 다운로드: `Download` (lucide, sm size)
- 카메라 자리: `Camera` (lucide)
- 빈 상태: `ClipboardCheck` 또는 `Inbox`
- 에러: `AlertCircle`
</verbatim_tokens>

<state_inventory>
**RemediationPage.tsx 분석으로 도출된 시안에 그려야 할 5 상태:**

| 상태 | 트리거 | 시각 요소 |
|---|---|---|
| **A. 정상 리스트** | records.length > 0 | 카드 N개 (불량/주의 × 미조치/완료 mix) + sticky 필터바 |
| **B. 빈 상태** | records.length === 0, !isLoading, !isError | "조치 항목 없음" + 보조 안내 (60px 위아래 패딩 중앙 정렬) |
| **C. 로딩** | isLoading | skeleton 카드 3개 (88px height, blink animation) |
| **D. 에러** | isError && !isLoading | 텍스트 "목록을 불러오지 못했습니다." (40px 패딩 중앙 정렬) |
| **E. 데스크톱 우측 빈 슬롯** | !detail (선택 안 됨) | "좌측에서 항목을 선택하세요" 중앙 정렬 |

**카드 좌측 색바 룰 (메모 feedback_inspection_unresolved_color 강제):**
- `record.status === 'open'` (미조치) → `--status-fire-bar` (주황)  ← **결과 색이 아닌 상태 색!**
- `record.status === 'resolved'` (완료) → `--status-safe-bar` (녹색)
- 옛 코드는 `record.result === 'bad' ? '--danger' : '--warn'` 로 결과 기준 색바였음 → **시안에서 상태 기준으로 교체**

**카드 안 배지 2개 (개별):**
- 결과 배지: 불량 = `--status-danger-bg` + `--status-danger`, 주의 = `--status-warning-bg` + `--status-warning`
- 상태 배지: 미조치 = `--status-fire-bg` + `--status-fire`, 완료 = `--status-safe-bg` + `--status-safe`

**데스크톱 우측 패널 보고서 KV 테이블 (RemediationPage.tsx line 292~343):**
- 카테고리 / 위치 / 점검일시 / 점검자 / 판정결과 (배지) / 상태 (배지) / 점검 메모
- status === 'resolved' 일 때 추가: 조치일시 / 조치자 / 조치 내용 / 소모 자재
- KV 행 높이/패딩: th 110px width, padding 8px 12px, fontSize 12px (text-caption)
- 사진 그리드: 2-col grid (조치 전 / 조치 후), 각 셀 border + radius 10px + 다운로드 버튼 우상단

**모바일 카드 1열 / 데스크톱 좌측 패널 카드 1열 (50% width 안에서) — 카드 자체 디자인은 동일.**
</state_inventory>

<mock_data>
**시안에 사용할 한국어 fire-safety 목 데이터 (lorem ipsum 금지):**

카드 6~8개 (불량/주의 × 미조치/완료 mix):
1. **자동화재탐지설비** · B2 사무동 · "감지기 동작불량 - 1차 패턴 미검출" · 2026-05-15 · 불량 · **미조치** · 윤종엽
2. **유도등** · 3F 연구동 A동 EXIT-A12 · "LED 불점등 (3개소)" · 2026-05-14 · 불량 · **미조치** · 김철수
3. **소화기** · 1F 연구동 SE-101 · "압력 미달 (2.3 MPa)" · 2026-05-13 · 주의 · **미조치** · 이영희
4. **방화셔터** · B1 사무동 · "비상시 작동 미확인 — 컨트롤러 점등" · 2026-05-12 · 불량 · 완료 · 윤종엽
5. **전실제연댐퍼** · RF 옥상 · "자동 개방 지연 8초 (기준 5초)" · 2026-05-10 · 주의 · 완료 · 김철수
6. **옥내소화전** · 2F 연구동 B동 · "압력계 미세 떨림 — 관경 점검 권고" · 2026-05-09 · 주의 · 완료 · 윤종엽
7. **가스계소화설비** · B2 사무동 · "기동용기 압력 게이지 ↓" · 2026-05-08 · 불량 · 완료 · 이영희
8. **스프링클러** · 4F 연구동 · "경보밸브 누설 미세" · 2026-05-07 · 주의 · **미조치** · 김철수

카테고리 select option: `전체 카테고리`, 자동화재탐지설비, 옥내소화전, 유도등, 방화셔터, 소화기, 전실제연댐퍼, 가스계소화설비, 스프링클러

날짜 포맷: `2026-05-15` 또는 `5/15` (RemediationPage.tsx 의 `fmtDate` 와 동일 톤)
</mock_data>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 시안 HTML 작성 — 인프라(head/tokens/typography) + 모바일 4 viewport (정상/빈/로딩/에러) + 데스크톱 2 viewport (분할 정상/우측 빈 슬롯) + 라이트 분기 1 viewport</name>
  <files>cha-bio-safety/docs/redesign-context/04-remediation/sketch/remediation-sketch.html</files>
  <action>
**Step 0 — 디렉토리 보장:**
- `cha-bio-safety/docs/redesign-context/04-remediation/sketch/` 디렉토리가 없으면 생성. 이미 다른 sketch 페이지가 동일 패턴으로 존재하므로 부모는 있음.

**Step 1 — `<head>` 인프라 작성 (qr-scan-sketch.html line 1~200 verbatim 차용):**
- `<title>조치 관리 재디자인 시안 v0.1.1 — Remediation 5 상태 (260516-q2i)</title>`
- Tailwind CDN: `<script src="https://cdn.tailwindcss.com"></script>`
- Pretendard CSS: `<link rel="stylesheet" href="https://cdn.jsdelivr.net/gh/orioncactus/pretendard@v1.3.9/dist/web/variable/pretendardvariable.min.css" />`
- JetBrains Mono: `<link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=JetBrains+Mono:wght@400;500;600;700&display=swap" />`
- lucide CDN: `<script src="https://unpkg.com/lucide@latest/dist/umd/lucide.min.js"></script>`
- `<style>` 블록 안에:
  - `[data-theme="dark"]` 변수 블록 — `tokens.css` line 16~69 verbatim 복붙 (--surface-*, --text-*, --border-*, --accent-*, --status-* 4 페어 × 5 종)
  - `[data-theme="light"]` 변수 블록 — `tokens.css` line 74~119 verbatim 복붙
  - `:root` spacing/radius 블록 — `tokens.css` line 124~146 verbatim. **데스크톱 분기 `@media (min-width: 768px)` 는 단일 HTML 시안에서 viewport-frame 자체가 786px 미만일 수 있어 적용이 모호하므로, 별도 `.viewport-desktop` selector 안에서 데스크톱 토큰을 강제하는 보조 클래스로 처리하지 말고 — 단순히 `@media (min-width: 768px)` 그대로 두고, 시안 외곽 body 가 1920+ 가정. viewport-mobile 안에서도 토큰이 desktop 값으로 평가되는 한계는 메타 라벨에 명시.**
  - Radius `tokens.css` line 167~172 verbatim
  - typography.css line 33~91 verbatim 7 클래스 + .text-mono
  - 토큰 → CSS class 매핑 (qr-scan-sketch.html line 159~207 verbatim 그대로):
    - `.bg-page`, `.bg-raised`, `.bg-sunken`, `.bg-active`
    - `.text-t1/.text-t2/.text-t3`, `.text-acl`, `.text-link`
    - `.text-safe/.text-warn/.text-danger/.text-info/.text-fire`
    - `.bg-safe/.bg-warn/.bg-danger/.bg-info/.bg-fire` (solid bar 색)
    - `.bg-safe-soft/.bg-warn-soft/.bg-danger-soft/.bg-info-soft/.bg-fire-soft` (bg 채움)
    - `.bd`, `.bd-strong`, `.bd-b`, `.bd-t`, `.bd-r`, `.bd-l`
  - 시안 메타 스타일: `body { background: #1a1d22; color: #e6edf3; font-family: var(--font-sans); }`, `.meta-label { ... }`, `.viewport-frame { ... }`, `.viewport-mobile { width: 393px; height: 852px; }`, `.viewport-desktop { width: 1280px; height: 720px; }`, `.page-shell`, `.desktop-shell`, `.no-scrollbar`
  - 아이콘 사이즈: `.icon-xs/.icon-sm/.icon-md/.icon-lg/.icon-xl/.icon-2xl/.icon-3xl/.icon-5xl` (qr-scan-sketch line 786~793 verbatim)
  - blink keyframes (RemediationPage.tsx line 250 와 동일): `@keyframes blink { 0%,100% { opacity: 0.6 } 50% { opacity: 0.3 } } .anim-blink { animation: blink 2s ease-in-out infinite; }`
  - 페이지 외곽 타이틀: `.page-section-title { font-size: 28px; font-weight: 700; line-height: 1.15; color: #e6edf3; }`, `.page-section-sub { font-size: 16px; margin-top: 8px; color: #8b949e; }`

**Step 2 — Remediation 페이지 전용 CSS 추가 (지정 클래스명, 모두 토큰 기반):**
- `.rem-page-hd` 자체 헤더 (높이 52px, padding 0 16px, bg var(--surface-raised), border-bottom var(--border-default))
  - `.rem-page-hd-title { font-size: 18px; font-weight: 700; color: var(--text-primary); }`
- `.rem-filter-bar` (sticky 영역, bg var(--surface-raised), border-b var(--border-default))
- `.rem-tabs` (display flex, height 44px) — 각 탭 active 시 bg var(--surface-active) + color var(--text-primary) + border-bottom 2px solid var(--accent), 비활성 transparent + color var(--text-tertiary)
- `.rem-filter-row` (flex, gap 8px, padding 8px 16px)
- `.rem-cat-select` (height 36px, bg var(--surface-sunken), border 1px var(--border-strong), radius var(--radius-sm), color var(--text-primary), font-size 12px, padding 0 8px)
- `.rem-period-btn` (height 32px, padding 0 12px, radius var(--radius-sm), font-size 12px, font-weight 700) — active bg var(--surface-active) + color var(--text-primary), 비활성 bg transparent + color var(--text-tertiary)
- `.rem-list` (overflow-y auto, padding 12px 16px, display flex, flex-direction column, gap 8px)
- `.rem-card` (display flex, gap 10px, padding 12px, bg var(--surface-sunken), border 1px var(--border-default), radius var(--radius-md), cursor pointer, position relative)
  - `.rem-card-bar { width: 4px; flex-shrink: 0; align-self: stretch; border-radius: 2px; }`
  - `.rem-card-bar.fire { background: var(--status-fire-bar); }` ← 미조치
  - `.rem-card-bar.safe { background: var(--status-safe-bar); }` ← 완료
  - `.rem-card.is-selected { border: 2px solid var(--accent); }` (데스크톱 우측 패널 선택 카드)
- `.rem-badge` (font-size 12px, font-weight 700, padding 2px 6px, radius 5px, line-height 1, white-space nowrap, flex-shrink 0)
  - `.rem-badge.danger { background: var(--status-danger-bg); color: var(--status-danger); }` ← 불량
  - `.rem-badge.warning { background: var(--status-warning-bg); color: var(--status-warning); }` ← 주의
  - `.rem-badge.fire { background: var(--status-fire-bg); color: var(--status-fire); }` ← 미조치
  - `.rem-badge.safe { background: var(--status-safe-bg); color: var(--status-safe); }` ← 완료
  - **leading-none 명시** (메모 feedback_text_caption_leading_none 룰 — 작은 컨테이너 안 12px 텍스트)
- `.rem-skeleton { background: var(--surface-sunken); border-radius: var(--radius-md); height: 88px; }` + .anim-blink 클래스 병기
- `.rem-empty` (flex column, align center, justify center, gap 8px, padding 60px 16px, color var(--text-secondary))
- `.rem-error` (text-align center, padding 40px 16px, color var(--text-secondary), font-size 14px)
- `.rem-detail-pane` (overflow-y auto, padding 20px 28px, height 100%)
- `.rem-detail-empty` (height 100%, display flex, align center, justify center, color var(--text-tertiary), font-size 13px)
- `.rem-detail-hd` (display flex, align center, gap 12px, margin-bottom 16px)
- `.rem-detail-title { font-size: 18px; font-weight: 700; color: var(--text-primary); }`
- `.rem-detail-sub { font-size: 12px; color: var(--text-tertiary); margin-top: 2px; }`
- `.rem-download-btn` (padding 8px 14px, radius var(--radius-sm), border none, **bg var(--accent)** [3C 일관 단색], color var(--text-on-accent), font-size 12px, font-weight 700, cursor pointer, display inline-flex, gap 4px, align-items center)
  - **그라디언트 사용 금지** (qr-scan-sketch VP4 비교 카탈로그 결정과 동일 — 옛 linear-gradient 폐기)
- `.rem-kv-table` (width 100%, border-collapse collapse, margin-bottom 20px)
  - `.rem-kv-table th { width: 110px; padding: 8px 12px; background: var(--surface-sunken); border: 1px solid var(--border-default); font-size: 12px; font-weight: 700; color: var(--text-secondary); text-align: left; vertical-align: top; line-height: 1.5; }`
  - `.rem-kv-table td { padding: 8px 12px; border: 1px solid var(--border-default); font-size: 13px; color: var(--text-primary); white-space: pre-wrap; vertical-align: top; line-height: 1.6; }`
- `.rem-photo-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 16px; }`
- `.rem-photo-card { border: 1px solid var(--border-default); border-radius: 10px; padding: 12px; background: var(--surface-raised); }`
- `.rem-photo-hd { display: flex; align-items: center; justify-content: space-between; margin-bottom: 8px; }`
- `.rem-photo-label { font-size: 12px; font-weight: 700; color: var(--text-secondary); display: inline-flex; align-items: center; gap: 4px; }`
- `.rem-photo-dl-btn { padding: 4px 10px; border-radius: 6px; border: 1px solid var(--border-strong); background: var(--surface-sunken); color: var(--text-secondary); font-size: 11px; font-weight: 700; cursor: pointer; display: inline-flex; align-items: center; gap: 3px; line-height: 1; }`
  - **font-size 11px 는 의도된 예외** — 다운로드 보조 버튼이라 11px 허용 (RemediationPage.tsx line 354 line 371 와 1:1 매칭). 단 노안 룰상 12px 권장이므로, **시안에 메모로 12px 상향 검토 옵션을 코멘트로 남길 것**.
- `.rem-photo-img { width: 100%; max-height: 280px; object-fit: contain; border-radius: 6px; background: #000; display: block; }`
- `.rem-photo-empty { height: 180px; display: flex; align-items: center; justify-content: center; color: var(--text-tertiary); font-size: 12px; background: var(--surface-sunken); border-radius: 6px; }`

**Step 3 — `<body>` 외곽 (qr-scan-sketch.html line 808~842 패턴):**
- `body { background: #1a1d22; padding: 32px; color: #e6edf3; }`
- `<header>` 페이지 타이틀: `<h1 class="page-section-title">조치 관리 재디자인 시안 v0.1.1</h1><p class="page-section-sub">Remediation Page — 모바일/데스크톱 × 다크/라이트 × 5 상태 시각화 (260516-q2i)</p>`
- 그 아래 4-column 카탈로그 박스 (`.cat-box-info` / `.cat-box-danger` / `.cat-box-mute` / `.cat-box-warn` 4개):
  1. **카드 좌측 색바 룰**: "결과 색이 아닌 **상태 색** — 미조치 = `--status-fire-bar`, 완료 = `--status-safe-bar`. 메모 `feedback_inspection_unresolved_color` 강제. 옛 코드 `result === 'bad' ? --danger : --warn` 폐기."
  2. **결과 배지**: "불량 = `--status-danger-bg` + `--status-danger`, 주의 = `--status-warning-bg` + `--status-warning`. 옛 인라인 `rgba(239,68,68,.13)` / `rgba(245,158,11,.13)` 4건 → 토큰 페어로 교체."
  3. **보고서 다운로드 버튼**: "단색 `var(--accent)`. 옛 `linear-gradient(135deg,#1d4ed8,#2563eb)` 그라디언트 → §6.4 룰상 단색 v0.1.1 accent 로 교체 (qr-scan-sketch VP4 결정 동일)."
  4. **데스크톱 분할**: "좌 50% 목록 / 우 50% 보고서 상세. 우측 빈 슬롯 = '좌측에서 항목을 선택하세요'. 사진 카드 다운로드 버튼은 11px 보조 — 12px 상향 검토 옵션 표기."

**Step 4 — VP1 모바일 다크 정상 리스트 (393×852):**
- `<section data-theme="dark"><div class="meta-label">📱 VP1 · 모바일 · 다크 · 정상 리스트 (불량/주의 × 미조치/완료 mix 6 카드)</div><div class="viewport-frame viewport-mobile" data-theme="dark"><div class="page-shell">...`
- `.rem-page-hd`: 좌 "조치 관리" 18px 타이틀, 우 (필요 시 새로고침 ghost 버튼 — 단순화 위해 생략 가능)
- `.rem-filter-bar` (sticky):
  - Row 1 `.rem-tabs`: [전체] [미조치 (active)] [완료] 3개 (각 flex 1, height 44px)
  - Row 2 `.rem-filter-row`: select `<option>전체 카테고리</option><option>자동화재탐지설비</option>...` + 4 period button [7일][30일 (active)][90일][전체]
- `.rem-list` (gap 8px): 카드 6개 (mock_data §1~6 사용 — '미조치' 탭이라 실제로는 미조치 4개만 노출이 자연스러우나 시안은 "정상 리스트 시각화" 우선이므로 mix 노출. meta-label 에 명시)
  - 각 카드 구조 (RemediationPage.tsx line 492~563 시각만 v0.1.1 토큰화):
    ```
    <div class="rem-card">
      <div class="rem-card-bar fire|safe"></div>  ← 상태 기준
      <div style="flex: 1; min-width: 0; display: flex; flex-direction: column; gap: 3px;">
        <div style="display: flex; align-items: center; justify-content: space-between; gap: 8px;">
          <span class="text-body-sm" style="font-weight: 700; color: var(--text-primary); flex: 1; overflow: hidden; text-overflow: ellipsis; white-space: nowrap;">자동화재탐지설비</span>
          <span class="rem-badge danger|warning">불량|주의</span>
        </div>
        <div class="text-caption text-t2">사무동 B2</div>
        <div class="text-caption" style="color: var(--text-secondary); overflow: hidden; text-overflow: ellipsis; white-space: nowrap;">감지기 동작불량 - 1차 패턴 미검출</div>
        <div style="display: flex; align-items: center; justify-content: space-between; gap: 8px;">
          <span class="text-caption text-t3">2026-05-15</span>
          <span class="rem-badge fire|safe">미조치|완료</span>
        </div>
      </div>
    </div>
    ```
  - **인라인 style 은 시안에서만 허용** — TSX 변환 시 Tailwind utility 로 교체. 시안에서는 가독성 위해 인라인도 OK (qr-scan-sketch 도 동일).
  - **leading-none 명시 위치**: `.rem-badge` 안 텍스트 (12px 배지 안 텍스트 — line-height 1.5 면 수직 패딩 시각적 과다)
- BottomNav placeholder 제거 (RemediationPage.tsx 는 자체 BottomNav 없이 App.tsx 가 그림 — qr-scan-sketch 처럼 작은 placeholder 박스 1행만 추가)

**Step 5 — VP2 모바일 다크 빈 상태 (393×852):**
- `.meta-label`: 📱 VP2 · 모바일 · 다크 · 빈 상태 (records.length === 0)
- 동일 헤더 + 필터바 ('미조치' 탭 active)
- `.rem-list` 안에 `.rem-empty` 1개:
  - lucide `Inbox` icon-3xl size, color var(--text-tertiary)
  - "조치 항목 없음" text-body, font-weight 700, color var(--text-primary)
  - "선택한 조건에 해당하는 불량/주의 항목이 없습니다." text-caption, color var(--text-secondary)

**Step 6 — VP3 모바일 다크 로딩 (393×852):**
- `.meta-label`: 📱 VP3 · 모바일 · 다크 · 로딩 (skeleton 3개, blink animation)
- 동일 헤더 + 필터바
- `.rem-list` 안에 `.rem-skeleton.anim-blink` 3개

**Step 7 — VP4 모바일 다크 에러 (393×852):**
- `.meta-label`: 📱 VP4 · 모바일 · 다크 · 에러 (목록 로드 실패)
- 동일 헤더 + 필터바
- `.rem-list` 안에 `.rem-error`:
  - lucide `AlertCircle` icon-2xl, color var(--status-danger-bar) (위 8px 마진)
  - "목록을 불러오지 못했습니다." text-body-sm, color var(--text-secondary)
  - "화면을 당겨서 다시 시도하세요." text-caption, color var(--text-tertiary)

**Step 8 — VP5 데스크톱 다크 분할 정상 (1280×720):**
- `.meta-label`: 🖥️ VP5 · 데스크톱 · 다크 · 좌(50%) 목록 / 우(50%) 보고서 상세 (자동화재탐지설비 미조치 선택)
- `.viewport-frame.viewport-desktop`
- `.desktop-shell` (display flex, height 100%):
  - 좌측 컨테이너 (width 50%, border-right 1px var(--border-default), display flex flex-direction column):
    - `.rem-filter-bar` (탭 + select + 기간)
    - `.rem-list` (카드 4개, 첫 카드 `.is-selected` — border 2px var(--accent))
  - 우측 컨테이너 (width 50%, overflow-y auto):
    - `.rem-detail-pane`:
      - `.rem-detail-hd`:
        - 좌: `.rem-detail-title` "자동화재탐지설비" + `.rem-detail-sub` "사무동 B2"
        - 우: `.rem-download-btn` (lucide `Download` icon-sm + "보고서 다운로드" 12px font-weight 700)
      - `.rem-kv-table`:
        - 카테고리 / 자동화재탐지설비
        - 위치 / 사무동 B2
        - 점검일시 / 2026-05-15 14:32
        - 점검자 / 윤종엽
        - 판정결과 / `.rem-badge.danger` "불량"
        - 상태 / `.rem-badge.fire` "미조치"
        - 점검 메모 / "감지기 동작불량\n1차 패턴 미검출\n전원 재공급 필요"
      - `.rem-photo-grid`:
        - 조치 전 카드: `.rem-photo-label` (lucide `Camera` icon-xs + "조치 전 (불량)") + `.rem-photo-dl-btn` (lucide `Download` 12px + "다운로드") + `.rem-photo-empty` (목 시안이라 "사진 placeholder" 텍스트, 또는 회색 placeholder 박스)
        - 조치 후 카드: `.rem-photo-label` "조치 후" + `.rem-photo-empty` "아직 조치 전" (status open 이므로)

**Step 9 — VP6 데스크톱 다크 우측 빈 슬롯 (1280×720):**
- `.meta-label`: 🖥️ VP6 · 데스크톱 · 다크 · 우측 빈 슬롯 (목록 비어있고 detail 미선택)
- 동일 desktop-shell. 좌측은 `.rem-empty` (모바일 VP2 와 동일 컨텐츠), 우측은 `.rem-detail-empty` "좌측에서 항목을 선택하세요"

**Step 10 — VP7 모바일 라이트 정상 리스트 (393×852):**
- `.meta-label`: 📱 VP7 · 모바일 · 라이트 · 정상 리스트 (라이트 토큰 분기 검증 — 동일 데이터)
- `data-theme="light"`. VP1 과 동일 마크업, 색만 자동 분기

**Step 11 — VP8 데스크톱 라이트 분할 정상 완료 카드 (1280×720) (옵션 — 시간 허용 시):**
- `.meta-label`: 🖥️ VP8 · 데스크톱 · 라이트 · 좌목록/우상세 (방화셔터 완료 선택 — resolved KV 4 row 추가 + 조치 후 사진)
- VP5 와 동일 구조이되:
  - 좌측: 카드 4개 (mock_data 4~7), 4번 방화셔터 완료 선택
  - 우측 KV 테이블에 status === 'resolved' 추가 4 row (조치일시 / 조치자 / 조치 내용 / 소모 자재)
  - 우측 사진 그리드: 양쪽 모두 placeholder 표시 ("사진 placeholder" 회색 박스 + 다운로드 버튼 활성)

**Step 12 — `<body>` 끝 `<script>lucide.createIcons();</script>` 추가**

**전체 viewport 배치:**
- 외곽 컨테이너 `<main>` 안에 `<section>` 8개를 vertical stack (gap 32px). 각 viewport-frame 위에 meta-label 1개.

**검증 체크리스트 (작성 중 self-check):**
- [ ] `--bg`, `--bg2`, `--bg3`, `--bg4`, `--t1`, `--t2`, `--t3`, `--bd`, `--bd2`, `--acl`, `--safe`, `--warn`, `--danger`, `--fire` 옛 alias 0건 (`grep -E '\-\-(bg[234]?|t[123]|bd2?|acl|safe|warn|danger|fire)\b'` → 0)
- [ ] 인라인 hex 색 (status용) 0건 — `rgba(239,68,68`, `rgba(245,158,11`, `rgba(34,197,94`, `rgba(249,115,22` 가 토큰 정의 블록 외부에 등장하지 않음
- [ ] 모든 `.rem-card-bar` 가 `.fire` 또는 `.safe` 둘 중 하나만 사용 (불량/주의 색바 0건)
- [ ] `linear-gradient` 0건 (보고서 다운로드 버튼 단색 accent)
- [ ] 이모지 0건 (`📷` 같은 이모지 → lucide `Camera` 아이콘으로 교체. 단 메타 라벨 외곽 `📱` `🖥️` 시안용 이모지는 OK — viewport 내부 콘텐츠와 무관)
- [ ] `<i data-lucide="...">` 사용 + `<script>lucide.createIcons();</script>` 호출
- [ ] 8 viewport 모두 노출 (VP8 옵션은 시간 허용 시)
  </action>
  <verify>
    <automated>
# 파일 존재 + 최소 사이즈
test -f cha-bio-safety/docs/redesign-context/04-remediation/sketch/remediation-sketch.html && \
  test $(wc -l < cha-bio-safety/docs/redesign-context/04-remediation/sketch/remediation-sketch.html) -ge 700 && \
# 옛 alias 0건 (토큰 정의 매핑 블록 자체는 var(--bg) → var(--surface-page) 같은 매핑이 있을 수 있으므로, '본문 사용'만 잡기 위해 var\(--bg\), var\(--t1\) 등 raw alias 'sole' 패턴만 체크)
grep -cE 'var\(--(bg2|bg3|bg4|t1|t2|t3|bd2|acl|safe|warn|danger|fire)\)' cha-bio-safety/docs/redesign-context/04-remediation/sketch/remediation-sketch.html | grep -q '^0$' && \
# v0.1.1 핵심 토큰 사용 검증
grep -q 'status-fire-bar' cha-bio-safety/docs/redesign-context/04-remediation/sketch/remediation-sketch.html && \
grep -q 'status-safe-bar' cha-bio-safety/docs/redesign-context/04-remediation/sketch/remediation-sketch.html && \
grep -q 'status-danger-bg' cha-bio-safety/docs/redesign-context/04-remediation/sketch/remediation-sketch.html && \
grep -q 'status-warning-bg' cha-bio-safety/docs/redesign-context/04-remediation/sketch/remediation-sketch.html && \
# data-theme 양쪽 노출
grep -q 'data-theme="dark"' cha-bio-safety/docs/redesign-context/04-remediation/sketch/remediation-sketch.html && \
grep -q 'data-theme="light"' cha-bio-safety/docs/redesign-context/04-remediation/sketch/remediation-sketch.html && \
# viewport 양쪽 노출
grep -q 'viewport-mobile' cha-bio-safety/docs/redesign-context/04-remediation/sketch/remediation-sketch.html && \
grep -q 'viewport-desktop' cha-bio-safety/docs/redesign-context/04-remediation/sketch/remediation-sketch.html && \
# lucide CDN + createIcons 호출
grep -q 'unpkg.com/lucide' cha-bio-safety/docs/redesign-context/04-remediation/sketch/remediation-sketch.html && \
grep -q 'lucide.createIcons' cha-bio-safety/docs/redesign-context/04-remediation/sketch/remediation-sketch.html && \
# linear-gradient 0건 (보고서 다운로드 버튼 단색 accent)
test $(grep -c 'linear-gradient' cha-bio-safety/docs/redesign-context/04-remediation/sketch/remediation-sketch.html) -eq 0 && \
# 카드 좌측 색바 fire/safe 둘 다 노출 (상태 기준 색바 룰)
grep -q 'rem-card-bar fire' cha-bio-safety/docs/redesign-context/04-remediation/sketch/remediation-sketch.html && \
grep -q 'rem-card-bar safe' cha-bio-safety/docs/redesign-context/04-remediation/sketch/remediation-sketch.html && \
echo "VERIFY OK"
    </automated>
  </verify>
  <done>
remediation-sketch.html 단일 파일이 700+ 라인, 8 viewport (모바일 4 + 데스크톱 2 + 라이트 1~2) 시각화 완료. 옛 alias raw 사용 0건, 토큰 인용 verbatim, lucide 아이콘 사용, linear-gradient 0건, 카드 좌측 색바 상태 기준(fire/safe) 적용. 사용자 컨펌 대기.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 사용자 시안 검토 — 시각 디자인 + 토큰 매핑 + 5 상태 정합 확인</name>
  <what-built>
remediation-sketch.html 단일 파일에 모바일/데스크톱 × 다크/라이트 × 5 상태(정상 리스트 / 빈 / 로딩 / 에러 / 데스크톱 우측 빈 슬롯) 시각화. 카드 좌측 색바는 상태 기준(미조치 fire / 완료 safe), 결과 배지는 result 기준(불량 danger / 주의 warning), 보고서 다운로드 버튼은 단색 accent.
  </what-built>
  <how-to-verify>
1. 파일 열기:
   ```
   open cha-bio-safety/docs/redesign-context/04-remediation/sketch/remediation-sketch.html
   ```
2. 8 viewport 모두 스크롤하면서 확인:
   - VP1 모바일 다크 정상 리스트 — 카드 6개, 미조치는 좌측 주황 바, 완료는 좌측 녹색 바
   - VP2 모바일 다크 빈 상태 — Inbox 아이콘 + 안내문
   - VP3 모바일 다크 로딩 — skeleton 3개 blink
   - VP4 모바일 다크 에러 — AlertCircle 아이콘 + 메시지
   - VP5 데스크톱 다크 분할 — 좌 카드 4개 / 우 KV 테이블 + 사진 그리드
   - VP6 데스크톱 다크 우측 빈 슬롯 — "좌측에서 항목을 선택하세요"
   - VP7 모바일 라이트 — 다크와 동일 데이터 라이트 토큰 분기
   - (VP8 옵션) 데스크톱 라이트 완료 카드 상세
3. 페이지 상단 4-column 카탈로그 박스에서 디자인 결정 사항(상태 기준 색바, 단색 accent, 11px 다운로드 버튼 옵션 등) 정합 검토
4. 아래 항목에 의견:
   - 색바/배지 룰 OK?
   - 보고서 다운로드 버튼 단색 accent OK? (옛 그라디언트 폐기 결정)
   - 다운로드 보조 버튼 11px 유지 vs 12px 상향?
   - 추가/수정 viewport 필요?
   - 빠진 상태/요소?
  </how-to-verify>
  <resume-signal>
"approved" → 시안 확정 (다음 단계: TSX 변환 quick 또는 redesign/04-remediation 브랜치 작업).
또는 수정사항 (문장으로 직접 — Claude 가 시안 수정 후 재확인).
  </resume-signal>
</task>

</tasks>

<threat_model>
(시안 HTML 단일 파일, 외부 입력/네트워크 호출 없음 — 순수 정적 시각화. 위협 모델 비대상.)
</threat_model>

<verification>
- 파일 존재 + 700+ 라인
- 옛 alias `var(--bg2|bg3|bg4|t1|t2|t3|bd2|acl|safe|warn|danger|fire)` 사용 0건 (토큰 정의 매핑 블록은 무관)
- v0.1.1 status 토큰 4 페어 모두 사용
- data-theme 다크/라이트 양쪽 노출
- viewport-mobile / viewport-desktop 양쪽 노출
- lucide CDN + createIcons 호출
- linear-gradient 0건
- 카드 좌측 색바 fire / safe 둘 다 노출 (상태 기준 색바 룰)
- 사용자 시안 컨펌 (approved)
</verification>

<success_criteria>
1. `cha-bio-safety/docs/redesign-context/04-remediation/sketch/remediation-sketch.html` 단일 파일 작성 완료
2. 자동 검증 grep 가드 8개 모두 통과
3. 사용자 "approved" 컨펌 (또는 반복 수정 후 컨펌)
</success_criteria>

<output>
After completion, create `.planning/quick/260516-q2i-redesign-04-remediation-sketch-v0-1-1-ht/260516-q2i-01-SUMMARY.md` with:
- 작성한 viewport 목록 + 각 viewport 의 핵심 결정
- 토큰 사용 매핑 (옛 → v0.1.1)
- 사용자 피드백 요약 (있을 시)
- 다음 단계 권고 (TSX 변환 quick 인보크 트리거 / redesign 브랜치 머지)
</output>
