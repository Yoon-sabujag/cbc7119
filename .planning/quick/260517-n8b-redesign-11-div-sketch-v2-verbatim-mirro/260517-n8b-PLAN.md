---
phase: 260517-n8b
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cha-bio-safety/docs/redesign-context/11-div/sketch/div-sketch.html
  - .planning/quick/260517-n8b-redesign-11-div-sketch-v2-verbatim-mirro/260517-n8b-SUMMARY.md
autonomous: false
requirements:
  - REDESIGN-11-DIV-SKETCH-V2
must_haves:
  truths:
    - "div-sketch.html 안에 VP1~VP4 4종 viewport(모바일 다크/라이트, 데스크톱 다크/라이트)가 모두 시각화되어 있다"
    - "각 viewport 안에 4탭(압력 트렌드 / 챔버배수주기 / 오일 주기 / 탱크배수주기) 본문이 모두 펼쳐져 있다 (renderDivContent line 1126-1129 의 4 분기 모두)"
    - "측정점 카드는 3열 grid (실제 line 309 의 grid-template-columns: repeat(3, 1fr), gap:4) 로 표시된다"
    - "컬럼 라벨은 '1차 / 2차 / 세팅' (실제 line 318-321) — '챔버' 라고 쓰지 않았다"
    - "헤더는 백 버튼 34×34 round-8 + 제목 14px (실제 line 1095) — 우측 액션 없음"
    - "탭은 라벨/아이콘 없는 4 버튼 (실제 line 1103-1110) — wrapper 라벨 X / lucide 아이콘 X"
    - "층별 그룹 라벨은 9px / fontWeight 700 / text-tertiary / letterSpacing 0.04em (실제 line 296-301) verbatim"
    - "압력 값 폰트는 14px (status 컬럼 line 339-355) verbatim — 노안 룰 격상 X"
    - "보조 텍스트는 7-9px verbatim — 격상 X (사용자 결정 = 정보 밀도 우선)"
    - "색은 v0.1.1 토큰으로 매핑되어 className (Tailwind) 형태로 들어가 있다 — 인라인 hex 잔존은 압력 컬럼/IntervalBar 등 §F 결정 항목만"
    - "모달 3종(배수/오일/탱크 — renderDivDetail line 386~) 이 단일 파일 안에 별도 영역으로 포함되어 있다"
    - "DivPage.tsx 1136 LOC 단 한 줄도 수정되지 않았다 (sketch 작업, 코드 0건 변경)"
    - "데스크톱 사이드바(renderDesktopLayout line 937~) 측정점 리스트가 데스크톱 VP3/VP4 에 그려져 있다"
  artifacts:
    - path: "cha-bio-safety/docs/redesign-context/11-div/sketch/div-sketch.html"
      provides: "v2 verbatim mirror sketch — 단일 파일, 4 VP + 모달 3종"
      min_lines: 800
    - path: ".planning/quick/260517-n8b-redesign-11-div-sketch-v2-verbatim-mirro/260517-n8b-SUMMARY.md"
      provides: "§A~§F 검토 항목 + 색 매핑표 + 잔존 raw hex 사유"
      min_lines: 80
  key_links:
    - from: "div-sketch.html"
      to: "tokens.css / typography.css"
      via: "<link rel=stylesheet> 또는 <style> 안 @import"
      pattern: "tokens\\.css|typography\\.css"
    - from: "div-sketch.html"
      to: "v0.1.1 design-system.md 토큰 카탈로그"
      via: "className surface-page / surface-raised / surface-sunken / text-primary 등"
      pattern: "surface-(page|raised|sunken)|text-(primary|secondary|tertiary)|status-(safe|warning|danger|fire|info)"
---

<objective>
redesign/11 DivPage 의 sketch v2 작성. 옛 sketch (260517-m2x) 가 실제 페이지를 안 보고 v0.1.1 시스템으로 "자체 재구성" 한 결과 — 헤더 우측에 없는 연도 필터 추가, 탭 wrapper 라벨/아이콘 신규 생성, 카드 1열 (실제는 3열 grid), '챔버' 컬럼명 오기, 폰트 7-9px 을 16px+ 로 부풀림 — 이번엔 실제 DivPage.tsx (1136 LOC) 를 verbatim mirror 하고 v0.1.1 토큰화는 색에만 한정한다.

Purpose: 사용자 결정 = "옵션 1 (전체 폐기 + 재작업) + ①A 토큰화만 / ②A 폰트 verbatim / ③A 단일 파일". sketch 가 실제 페이지의 시각 디자인 결정을 보존해야 다음 단계(TSX 변환)에서 시각 회귀가 없다.

Output: 단일 HTML 파일 `cha-bio-safety/docs/redesign-context/11-div/sketch/div-sketch.html` 안에 VP1~VP4 4종 viewport + 4탭 본문 4 분기 모두 + 모달 3종 + 데스크톱 사이드바, 모두 실제 페이지 verbatim 시각 구조. 색만 v0.1.1 토큰 className. 코드 0건 변경.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md

@cha-bio-safety/src/pages/DivPage.tsx
@cha-bio-safety/docs/redesign-context/11-div/11-div.md
@cha-bio-safety/docs/redesign-context/11-div/design-system.md
@cha-bio-safety/docs/redesign-context/11-div/tokens.css
@cha-bio-safety/docs/redesign-context/11-div/typography.css

<interfaces>
<!-- 실제 DivPage.tsx 핵심 시각 구조 (executor 는 sketch 작성 전에 1136 LOC 전체를 읽어야 하지만, 핵심 좌표를 여기 미리 명시) -->

자체 헤더 (line 1095-1100):
- div bg: var(--bg2), padding: 8px 12px, borderBottom: 1px solid var(--bd)
- 백 버튼: 34×34, borderRadius:8, var(--bg3), ArrowLeft 18px (lucide)
- 제목: fontSize:14, fontWeight:600, color: var(--t1), text="DIV (RANGEMASTER)"
- 우측 액션: 없음

탭 (line 1103-1110):
- 컨테이너: display:flex, bg: var(--bg2), borderBottom: 1px solid var(--bd), padding: 0
- 탭 버튼: flex:1, padding:'10px 4px', fontSize:12, fontWeight:500
- selected: color var(--acl), borderBottom 2px solid var(--acl)
- unselected: color var(--t2), borderBottom 2px solid transparent
- 4 탭: ['압력 트렌드', '챔버배수주기', '오일 주기', '탱크배수주기']
- 아이콘 X, wrapper 라벨 X

renderDivContent (line 1126-1129):
- selectedTab='pressure' → renderPressureTrend()
- selectedTab='drain' → renderDrainCycle()
- selectedTab='oil' → renderOilCycle()
- selectedTab='tank' → renderTankCycle()

측정점 카드 grid (line 309-313):
- display:grid, gridTemplateColumns: 'repeat(3, 1fr)', gap:4

층별 그룹 라벨 (line 296-301):
- fontSize:9, fontWeight:700, color: var(--t3)
- letterSpacing:'0.04em', textTransform:'uppercase'
- padding:'6px 4px 4px', borderBottom:'1px solid var(--bd)'

측정점 카드 (line 314~):
- bg: var(--bg2), border 1px solid var(--bd), borderRadius:8
- padding:'5px 5px 4px'
- danger 상태: borderColor 'rgba(239,68,68,.4)'
- warn 상태: borderColor 'rgba(245,158,11,.3)'

카드 헤더 (line 314~):
- 코드명: fontSize:8, fontWeight:600, color var(--t1)
- 상태 아이콘: 7px

카드 본문 컬럼 (line 318-321):
- 3열 grid (1차 / 2차 / 세팅) — '챔버' 아님
- 컬럼 라벨: fontSize:7, color var(--t3)
- 값 (압력): fontSize:14, fontWeight:700
- 1차 색: #3b82f6 (blue)
- 2차 색: #f97316 (orange)
- 세팅 색: #22c55e (green)
- 단위 표시: 작은 'kg', fontSize:7

renderDesktopLayout (line 937~):
- 좌측 사이드바: 측정점 리스트
- 우측 메인: 선택된 측정점 상세 트렌드 차트

renderDivDetail (line 386~):
- 모달 3종: 배수 / 오일 / 탱크
- 모달 헤더 + 사이클 정보 + IntervalBar
- IntervalBar 색:
  - drain: #38bdf8 (sky)
  - comp_drain: #8b4513 (brown)
  - compressor: #f97316 (orange)
</interfaces>

<token_mapping_pre>
<!-- §B 색 토큰 매핑 (Task 1 에서 확정. 변경 필요 시 SUMMARY 에 사유 명시) -->

v0.1.1 → Tailwind className:
- var(--bg)   → bg-surface-page
- var(--bg2)  → bg-surface-raised
- var(--bg3)  → bg-surface-sunken
- var(--bd)   → border-border-default
- var(--t1)   → text-text-primary
- var(--t2)   → text-text-secondary
- var(--t3)   → text-text-tertiary
- var(--acl)  → text-accent / bg-accent / border-accent
- var(--safe) → text-status-safe
- var(--warn) → text-status-warning
- var(--danger) → text-status-danger
- rgba(239,68,68,.4) → border-status-danger-bar/40 (또는 hex 그대로 + SUMMARY 사유)
- rgba(245,158,11,.3) → border-status-warning-bar/30 (또는 hex 그대로 + SUMMARY 사유)

압력 컬럼 색 (§F 결정 항목, executor 가 둘 다 옵션 표기):
- #3b82f6 (1차 blue) → text-status-info 매핑 시도, raw hex 잔존 가능
- #f97316 (2차 orange) → text-status-fire 매핑 시도, raw hex 잔존 가능
- #22c55e (세팅 green) → text-status-safe 매핑 시도, raw hex 잔존 가능

IntervalBar 색 (§C 결정 항목):
- #38bdf8 (drain sky) → bg-status-info-bar 시도
- #8b4513 (comp_drain brown) → 토큰 매칭 없음 — raw hex 유지
- #f97316 (compressor orange) → bg-status-fire-bar 시도
</token_mapping_pre>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DivPage.tsx 1136 LOC 전체 읽기 + 색 매핑 미리 결정</name>
  <files>(읽기만 — 산출물 0)</files>
  <action>
    **이 task 의 목적: 옛 sketch 의 핵심 실패 원인 (페이지 안 보고 자체 재구성) 을 차단하기 위해 sketch 작성 전에 페이지 전체를 한 번에 읽고 시각 구조를 내재화한다.**

    1. Read tool 로 `cha-bio-safety/src/pages/DivPage.tsx` 전체 (line 1~1136) 를 1~2 번의 Read 호출로 흡수. 절대로 line range 가 겹치게 다시 읽지 말 것.

    2. 다음 영역의 인라인 style 객체를 머릿속에 정확히 매핑:
       - 자체 헤더 (line 1095~)
       - 4탭 (line 1103~)
       - renderPressureTrend (압력 트렌드 본문)
       - renderDrainCycle / renderOilCycle / renderTankCycle (3 사이클 탭 본문)
       - renderDivDetail (line 386~ 모달)
       - renderDesktopLayout (line 937~ 데스크톱)
       - 측정점 카드 (line 309~) — 3열 grid, 1차/2차/세팅 컬럼, 폰트 7-9-14px

    3. tokens.css / typography.css 를 Read tool 로 1회 읽어서 v0.1.1 토큰 실제 이름 확인 (interfaces 의 token_mapping_pre 가 맞는지 검증).

    4. design-system.md 의 색 카탈로그를 Read tool 로 1회 읽어서:
       - status-info / status-fire / status-safe 토큰이 실제로 존재하는지 확인
       - 존재하지 않으면 sketch 에서는 raw hex 잔존 + Task 3 SUMMARY 의 §B 에 사유 명시

    5. 11-div.md 작업 지시문을 Read tool 로 1회 읽어서 §C IntervalBar 색 결정 + §F TSX 변환 wave 분할 후보 의 사용자 요구사항 확인.

    **금지 사항:**
    - 페이지 읽지 않고 추측으로 sketch 작성하기 (옛 sketch 의 실패 원인)
    - 같은 파일을 line range 겹쳐서 다시 읽기
    - "거의 일치" 같은 자신감 표현으로 verbatim 검증 생략
    - 11-div.md 의 작업 지시문이 실제 페이지와 충돌할 경우 — **실제 페이지가 source of truth** (지시문은 참고)

    **산출물: 코드 0건. 다음 task 에서 sketch 를 작성할 때 추측 없이 작성 가능한 상태가 되는 것.**
  </action>
  <verify>
    <automated>test -f /Users/jykevin/Documents/cbc7119-design/cha-bio-safety/src/pages/DivPage.tsx && wc -l /Users/jykevin/Documents/cbc7119-design/cha-bio-safety/src/pages/DivPage.tsx | awk '{exit ($1==1136) ? 0 : 1}'</automated>
  </verify>
  <done>
    DivPage.tsx 의 7개 주요 영역 (헤더/4탭/4 사이클 본문/모달/데스크톱) 의 인라인 style 값(색/폰트/spacing/borderRadius) 을 executor 가 sketch 에 그대로 옮길 수 있는 상태. 토큰 매핑표 (§B) 최종 확정.
  </done>
</task>

<task type="auto">
  <name>Task 2: div-sketch.html 단일 파일 작성 (4 VP + 4 탭 본문 + 모달 3종 + 데스크톱)</name>
  <files>cha-bio-safety/docs/redesign-context/11-div/sketch/div-sketch.html</files>
  <action>
    Task 1 에서 흡수한 DivPage.tsx 의 시각 구조를 verbatim mirror 하는 단일 sketch HTML 작성.

    **파일 구조:**
    ```html
    <!DOCTYPE html>
    <html lang="ko">
    <head>
      <meta charset="utf-8" />
      <title>11-div sketch v2 (verbatim mirror)</title>
      <link rel="stylesheet" href="../tokens.css" />
      <link rel="stylesheet" href="../typography.css" />
      <script src="https://cdn.tailwindcss.com"></script>
      <style>
        /* Tailwind config inline — v0.1.1 색 토큰만 매핑 */
        /* IntervalBar 같은 raw hex 보조 색은 인라인 style 로 유지 */
      </style>
    </head>
    <body class="bg-neutral-900 p-6">
      <!-- VP1: 모바일 다크 393×852 -->
      <section class="dark">
        <h2 class="text-white text-xl mb-2">VP1 — 모바일 다크 (393×852)</h2>
        <div style="width:393px;height:852px" class="bg-surface-page text-text-primary overflow-y-auto">
          <!-- 헤더 (verbatim line 1095) -->
          <div class="bg-surface-raised border-b border-border-default" style="padding:8px 12px">
            <button class="bg-surface-sunken rounded-lg flex items-center justify-center" style="width:34px;height:34px">
              <!-- ArrowLeft 18px (inline svg or unicode) -->
              ←
            </button>
            <span style="font-size:14px;font-weight:600" class="text-text-primary ml-2">DIV (RANGEMASTER)</span>
          </div>

          <!-- 4탭 (verbatim line 1103) -->
          <div class="flex bg-surface-raised border-b border-border-default">
            <button class="flex-1 text-accent border-b-2 border-accent" style="padding:10px 4px;font-size:12px;font-weight:500">압력 트렌드</button>
            <button class="flex-1 text-text-secondary border-b-2 border-transparent" style="padding:10px 4px;font-size:12px;font-weight:500">챔버배수주기</button>
            <button class="flex-1 text-text-secondary border-b-2 border-transparent" style="padding:10px 4px;font-size:12px;font-weight:500">오일 주기</button>
            <button class="flex-1 text-text-secondary border-b-2 border-transparent" style="padding:10px 4px;font-size:12px;font-weight:500">탱크배수주기</button>
          </div>

          <!-- 4 탭 본문 모두 펼치기 -->
          <!-- 4-1. 압력 트렌드 본문 -->
          <div class="border-b-4 border-accent/40 pb-4">
            <div class="text-xs text-text-tertiary px-2 py-1">[탭 1: 압력 트렌드]</div>
            <!-- 층별 그룹 라벨 (line 296) -->
            <div class="border-b border-border-default text-text-tertiary uppercase" style="font-size:9px;font-weight:700;letter-spacing:0.04em;padding:6px 4px 4px">B1F</div>
            <!-- 측정점 3열 grid (line 309) -->
            <div class="grid" style="grid-template-columns:repeat(3, 1fr);gap:4px;padding:4px">
              <!-- 카드 1 (정상) -->
              <div class="bg-surface-raised border border-border-default" style="border-radius:8px;padding:5px 5px 4px">
                <div style="font-size:8px;font-weight:600" class="text-text-primary">DIV-B1-1</div>
                <div class="grid" style="grid-template-columns:repeat(3, 1fr);gap:2px;margin-top:3px">
                  <div>
                    <div class="text-text-tertiary" style="font-size:7px">1차</div>
                    <div style="font-size:14px;font-weight:700;color:#3b82f6">7.2<span style="font-size:7px"> kg</span></div>
                  </div>
                  <div>
                    <div class="text-text-tertiary" style="font-size:7px">2차</div>
                    <div style="font-size:14px;font-weight:700;color:#f97316">5.0<span style="font-size:7px"> kg</span></div>
                  </div>
                  <div>
                    <div class="text-text-tertiary" style="font-size:7px">세팅</div>
                    <div style="font-size:14px;font-weight:700;color:#22c55e">5.0<span style="font-size:7px"> kg</span></div>
                  </div>
                </div>
              </div>
              <!-- 카드 2 (warn) -->
              <div class="bg-surface-raised" style="border:1px solid rgba(245,158,11,.3);border-radius:8px;padding:5px 5px 4px">
                <!-- 동일 구조, 값만 warn 케이스 -->
              </div>
              <!-- 카드 3 (danger) -->
              <div class="bg-surface-raised" style="border:1px solid rgba(239,68,68,.4);border-radius:8px;padding:5px 5px 4px">
                <!-- 동일 구조, 값만 danger 케이스 -->
              </div>
            </div>
            <!-- B1F 외 2~3 floor 더 (3F까지 대표 케이스 6~9 행 정도) -->
          </div>

          <!-- 4-2. 챔버배수주기 본문 (renderDrainCycle 시각 구조 그대로) -->
          <div class="border-b-4 border-accent/40 pb-4">
            <div class="text-xs text-text-tertiary px-2 py-1">[탭 2: 챔버배수주기]</div>
            <!-- 측정점 카드 — 압력 자리에 사이클 정보 (IntervalBar) -->
          </div>

          <!-- 4-3. 오일 주기 본문 -->
          <div class="border-b-4 border-accent/40 pb-4">
            <div class="text-xs text-text-tertiary px-2 py-1">[탭 3: 오일 주기]</div>
            <!-- 동일 패턴 -->
          </div>

          <!-- 4-4. 탱크배수주기 본문 -->
          <div class="pb-4">
            <div class="text-xs text-text-tertiary px-2 py-1">[탭 4: 탱크배수주기]</div>
            <!-- 동일 패턴 -->
          </div>
        </div>
      </section>

      <!-- VP2: 모바일 라이트 — VP1 동일 구조, body class 만 light -->
      <!-- VP3: 데스크톱 다크 1280×720 (또는 1920×1080) — renderDesktopLayout: 좌측 사이드바 + 우측 메인 -->
      <!-- VP4: 데스크톱 라이트 — VP3 동일 구조, light 모드 -->

      <!-- 모달 3종 (renderDivDetail line 386~) -->
      <section>
        <h2 class="text-white text-xl mb-2">모달 — 배수 (drain)</h2>
        <div style="width:393px" class="bg-surface-raised border border-border-default rounded-xl p-3">
          <!-- 헤더 + 사이클 정보 + IntervalBar drain (#38bdf8 또는 status-info-bar) -->
        </div>
        <h2 class="text-white text-xl mb-2 mt-6">모달 — 오일 (comp_drain)</h2>
        <div style="width:393px" class="bg-surface-raised border border-border-default rounded-xl p-3">
          <!-- IntervalBar comp_drain (#8b4513 raw — 토큰 매칭 없음 / SUMMARY §C 명시) -->
        </div>
        <h2 class="text-white text-xl mb-2 mt-6">모달 — 탱크 (compressor)</h2>
        <div style="width:393px" class="bg-surface-raised border border-border-default rounded-xl p-3">
          <!-- IntervalBar compressor (#f97316 또는 status-fire-bar) -->
        </div>
      </section>
    </body>
    </html>
    ```

    **verbatim 룰 (강제):**

    1. **시각 구조 100% 보존** — 헤더 size(8px 12px padding, 백 버튼 34×34, 제목 14px), 탭 layout(flex-1, 10px 4px padding, 12px font, border-bottom 2px), 카드 grid(3열, gap 4), 카드 padding(5px 5px 4px), borderRadius(8), 층별 라벨(9px 700 letter-spacing 0.04em) — 모두 DivPage.tsx 값 verbatim.

    2. **폰트 사이즈 verbatim 보존** — 7px / 8px / 9px / 14px 모두 그대로. **노안 룰(16px 이상) 위반이지만 사용자 결정 = 정보 밀도 우선**. 격상 X, 변경 X. 사용자가 다음 단계 "옛 자유분방 디자인" 검토 트리거.

    3. **색만 토큰화** — interfaces 의 token_mapping_pre 표 그대로 적용. 토큰이 존재하지 않으면 raw hex 잔존 + SUMMARY §B 에 사유.

    4. **컬럼 라벨 verbatim** — '1차 / 2차 / 세팅' (line 318-321). **'챔버' 라고 쓰면 안 됨** (옛 sketch 오류).

    5. **헤더/탭 통일 룰 X** — 02+06 chrome 통일 룰 (h-12 헤더, w-8 백 버튼, 탭 wrapper bg-raised, 라벨 신규 등) **적용 안 함**. DivPage 의 실제 헤더(34×34 백 / 8px 12px / 14px) 와 탭(flex-1 / 10px 4px / 12px) 보존.

    6. **인라인 style → className 변환만** — 변환 어려운 경우(예: 정확한 borderRadius:8, padding:'5px 5px 4px', font-size:7~14 같은 비표준 값) 는 인라인 style 유지. Tailwind 표준 토큰(rounded-lg/text-xs 등)으로 1:1 치환되지 않으면 추측 X.

    7. **헤더 우측 액션 만들지 말 것** — 실제 line 1095-1100 헤더에 우측 액션 없음. "연도 필터" 등 만들어내지 말 것 (옛 sketch 오류).

    8. **탭 wrapper 라벨 만들지 말 것** — 실제 line 1103 탭에 라벨 없음. "점검 주기 선택" 등 만들어내지 말 것 (옛 sketch 오류).

    9. **탭 아이콘 만들지 말 것** — 실제 line 1109-1110 탭 버튼은 라벨 텍스트만. lucide 아이콘 추가 X (옛 sketch 오류).

    10. **데이터 흉내** — 34 측정점 모두 그리지 말고 대표 케이스 (B1~3F 정도) 6~9 행 + 데스크톱 사이드바 측정점 리스트 부분. 한 floor 당 3-4 카드 (정상/warn/danger 케이스 섞기).

    11. **데스크톱 (VP3/VP4) 사이드바** — renderDesktopLayout (line 937~) 의 좌측 측정점 리스트 (코드명 + 압력 mini) + 우측 메인 (트렌드 차트 placeholder) 구조 보존. 차트는 빈 박스 + "압력 트렌드 그래프 (24h)" 라벨 정도.

    **금지 사항:**
    - DivPage.tsx 코드 수정 (이번에도 강제, 1136 LOC 단 한 줄도 건드리지 말 것)
    - 옛 sketch (260517-m2x) 디렉토리 참조 (reset 으로 폐기됨)
    - "거의 일치" 자체 판단 후 사용자 검토 없이 SUMMARY 작성
    - 11-div.md 작업 지시문이 실제 페이지와 충돌하면 — **실제 페이지 우선**
    - 추측한 토큰명 적기 (token_mapping_pre 에 없는 토큰은 raw 유지 + SUMMARY §B 에 사유)

    **per 사용자 결정 = ①A 토큰화만 / ②A 폰트 verbatim / ③A 단일 파일.**
  </action>
  <verify>
    <automated>test -f /Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/11-div/sketch/div-sketch.html && grep -c "1차\|2차\|세팅" /Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/11-div/sketch/div-sketch.html | awk '{exit ($1>=3) ? 0 : 1}' && ! grep -q "챔버" /Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/11-div/sketch/div-sketch.html && grep -q "VP1\|VP2\|VP3\|VP4" /Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/11-div/sketch/div-sketch.html && grep -q "압력 트렌드" /Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/11-div/sketch/div-sketch.html && grep -q "챔버배수주기" /Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/11-div/sketch/div-sketch.html && grep -q "오일 주기" /Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/11-div/sketch/div-sketch.html && grep -q "탱크배수주기" /Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/11-div/sketch/div-sketch.html && git -C /Users/jykevin/Documents/cbc7119-design diff --quiet cha-bio-safety/src/pages/DivPage.tsx</automated>
  </verify>
  <done>
    div-sketch.html 안에 VP1~VP4 4종 viewport 모두 + 4탭 본문 4 분기 모두 + 모달 3종 + 데스크톱 사이드바/메인 시각화 완료. '1차/2차/세팅' 컬럼 라벨 verbatim, '챔버' 오기 없음. 헤더 우측 액션/탭 wrapper 라벨/탭 아이콘 모두 없음 (옛 sketch 오류 재현 X). DivPage.tsx 단 한 줄도 수정 안 됨 (git diff clean).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: SUMMARY 작성 + 사용자 검토 대기 (§A~§F 검토 항목)</name>
  <what-built>
    - cha-bio-safety/docs/redesign-context/11-div/sketch/div-sketch.html (단일 파일)
    - 4 viewport (VP1 모바일 다크 / VP2 모바일 라이트 / VP3 데스크톱 다크 / VP4 데스크톱 라이트)
    - 4 탭 본문 (압력 트렌드 / 챔버배수주기 / 오일 주기 / 탱크배수주기) 모두 펼친 형태
    - 모달 3종 (배수 / 오일 / 탱크) 단일 파일 안 별도 영역
    - 데스크톱 사이드바 + 메인 (renderDesktopLayout 구조 보존)
  </what-built>
  <how-to-verify>
    1. **SUMMARY 작성** — `.planning/quick/260517-n8b-redesign-11-div-sketch-v2-verbatim-mirro/260517-n8b-SUMMARY.md` 에 다음 §A~§F 검토 항목 모두 답변:

       **§A 변환 범위 확인:**
       - 토큰화 = 색만 적용 (O/X)
       - 시각 구조 = DivPage.tsx 와 일치하는가 (헤더 size, 탭 layout, 카드 grid, 폰트 사이즈, 컬럼 라벨)
       - 인라인 style 잔존 항목 목록 (Tailwind 1:1 치환 불가 사유 명시)

       **§B 색 토큰 매핑 적절성:**
       - var(--bg)/bg2/bg3/bd/t1/t2/t3/acl 매핑 결과
       - status-safe/warning/danger 매핑 결과
       - rgba(239,68,68,.4) / rgba(245,158,11,.3) — 토큰 매핑 성공 / 실패 (raw 잔존 사유)
       - 압력 컬럼 색 (#3b82f6 / #f97316 / #22c55e) — status-info/fire/safe 채택 vs raw hex 유지 추천

       **§C IntervalBar 탭 색:**
       - drain #38bdf8 → status-info-bar 매핑 결과
       - comp_drain #8b4513 → 토큰 매칭 없음 (raw 유지) — 사용자 확정 요청
       - compressor #f97316 → status-fire-bar 매핑 결과

       **§D 노안 이슈 발생 여부:**
       - 7px / 8px / 9px / 14px 폰트 verbatim 적용 결과
       - 모바일 393px viewport 에서 실 사용 시 가독성 예측
       - 다음 단계 "옛 자유분방 디자인" 검토 트리거 추천 여부

       **§E 모달 3종 시각:**
       - 배수 / 오일 / 탱크 모달이 renderDivDetail (line 386~) 과 일치하는가
       - IntervalBar 시각 — 사이클 라인 + 마커 + 다음 사이클 카운트다운

       **§F TSX 변환 wave 분할 후보:**
       - Wave 1: 헤더 + 4탭 chrome
       - Wave 2: 압력 트렌드 본문 (renderPressureTrend)
       - Wave 3: 3 사이클 탭 본문 (drain/oil/tank)
       - Wave 4: 모달 3종 (renderDivDetail)
       - Wave 5: 데스크톱 사이드바 (renderDesktopLayout)
       - 분할이 합리적인지 / 다른 분할 권고 / TSX 변환 별도 quick 등록 추천

    2. **시각 검토** — `open cha-bio-safety/docs/redesign-context/11-div/sketch/div-sketch.html` 또는 브라우저에서 file:// 으로 열어서 4 VP + 모달 3종 확인.

    3. **회귀 검증** — 사용자가 실제 DivPage.tsx 와 sketch 를 비교해서:
       - 헤더 우측 액션 없음 확인 (옛 sketch 오류 재현 X)
       - 탭 wrapper 라벨 없음 확인 (옛 sketch 오류 재현 X)
       - 탭 아이콘 없음 확인 (옛 sketch 오류 재현 X)
       - '1차/2차/세팅' 컬럼명 확인 ('챔버' 오기 X)
       - 카드 3열 grid 확인 (1열 오기 X)
       - 폰트 7-9-14px verbatim 확인 (16px 부풀림 X)

    4. **사용자 결정 요청 항목 (next-step):**
       - §B 압력 컬럼 색 — status 토큰 채택 / raw hex 유지 / 다른 옵션
       - §C IntervalBar comp_drain — raw #8b4513 유지 확정 / 다른 토큰 매핑
       - §D 노안 이슈 — 옛 자유분방 디자인 검토 트리거 / 현재 verbatim 유지
       - §F TSX 변환 — 별도 quick 등록 (변환 wave 안) / 현재 sketch 추가 보완 필요

  </how-to-verify>
  <resume-signal>
    "approved" — sketch v2 확정, TSX 변환 별도 quick 진행 가능
    "fix §X — [수정 요청]" — 해당 영역 재작업
    "옛 자유분방 디자인으로 전환" — verbatim 보존 폐기, 다음 단계 진행
  </resume-signal>
</task>

</tasks>

<verification>
  - div-sketch.html 단일 파일 존재
  - VP1~VP4 4종 모두 시각화
  - 4탭 본문 모두 펼친 형태 (4 분기)
  - 모달 3종 별도 영역 포함
  - 데스크톱 사이드바 + 메인 포함
  - '1차/2차/세팅' 컬럼명 verbatim ('챔버' 오기 X)
  - 헤더 우측 액션 / 탭 wrapper 라벨 / 탭 아이콘 — 모두 없음 (옛 sketch 오류 재현 X)
  - DivPage.tsx git diff clean (코드 0건 변경)
  - SUMMARY.md §A~§F 모두 답변
</verification>

<success_criteria>
  - 사용자가 sketch v2 를 시각 검토해서 "옛 sketch 오류 없음 + 실제 페이지 verbatim" 확인
  - §B/§C/§D/§F 4건의 결정 항목에 대해 사용자 결정 명시
  - TSX 변환 별도 quick 진행 가능 상태 (변환 wave 분할 확정)
</success_criteria>

<output>
After completion, create `.planning/quick/260517-n8b-redesign-11-div-sketch-v2-verbatim-mirro/260517-n8b-SUMMARY.md` with §A~§F sections.
</output>
