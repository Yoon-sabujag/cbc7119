---
phase: 260522-1hj
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cha-bio-safety/src/pages/AnnualPlanPage.tsx
autonomous: true
requirements:
  - W5-checklist-§1-scope
  - W5-checklist-§2-biz-preserve-15+
  - W5-checklist-§3-region-mapping-4
  - W5-checklist-§4-OQ-LOCKED-5
  - W5-checklist-§5-negative-gate-9
  - W5-checklist-§6-positive-gate
  - W5-checklist-§7-build-gate
  - W5-checklist-§10-biz-checkbox-19
  - W5-checklist-calib-signature-5
tags:
  - redesign
  - 17-annual-plan
  - tsx-conversion
  - v0.1.1
  - quick
  - calibration-preserve

must_haves:
  truths:
    - "AnnualPlanPage.tsx 단일 파일 in-place 수정 1건. `git diff --name-only HEAD~ HEAD` 에 'cha-bio-safety/src/pages/AnnualPlanPage.tsx' + 본 PLAN.md/SUMMARY.md 만 등장 (다른 파일 0)."
    - "generateAnnualPlan.ts + App.tsx + tailwind.config.js + tokens.css + functions/ + templates/ + migrations/ + public/ 모두 변경 0 byte."
    - "변환 후 AnnualPlanPage.tsx 라인 수 225 ± 30 (원본 225, 변환 후 225~270 예상)."
    - "★ 캘리브 좌표 시스템 시그니처 5건 1 byte 변경 0: STORAGE_KEY = 'annual_plan_year_pos' (line 7) / FINGER_OFFSET = 60 (line 8) / loadPos() try/catch (line 10~12) / handleImageClick % 좌표 계산 (line 35~45) / handleImageTouch FINGER_OFFSET 보정 (line 47~59) / yearPos overlay inline style (line 78~89) — 모두 verbatim 보존."
    - "비즈 로직 0 diff: imports 5개 (useState/useRef / useNavigate / toast / generateAnnualPlan / useIsDesktop) line 1~5 그대로 + lucide-react import 1줄 신규 추가."
    - "비즈 로직 0 diff: useNavigate / useIsDesktop line 15~16 그대로."
    - "비즈 로직 0 diff: useState 3종 (loading / calibMode / yearPos=loadPos initial) line 17~19 그대로."
    - "비즈 로직 0 diff: useRef (imgRef) line 20 + nextYear = new Date().getFullYear() + 1 line 21 그대로."
    - "비즈 로직 0 diff: handleDownload async + generateAnnualPlan() direct import (dynamic 아님) + toast.success/error + finally setLoading(false) line 23~33 그대로."
    - "비즈 로직 0 diff: previewImage common element (img + 연도 overlay + 캘리브 안내 칩) line 61~103 — UI markup className 만 재작성, 좌표/이벤트/카피/inline rgba 보존."
    - "비즈 로직 0 diff: img src '/templates/preview/annual-plan.png' (line 65) + alt '연간 업무 추진 계획 미리보기' (line 66) verbatim."
    - "비즈 로직 0 diff: setCalibMode(m => !m) 토글 line 119, 181 그대로. handleDownload onClick line 131, 206 + disabled={loading} line 132, 207 그대로."
    - "비즈 로직 0 diff: 카피 verbatim — '연간 업무 추진 계획' / '연간 업무 추진 계획 미리보기' / '연도가 들어갈 위치를 클릭하세요' / '위치 조정' / '취소' / '엑셀 다운로드' / '생성 중...' / '엑셀이 다운로드됐습니다' / '연도 위치 저장됨' / '대상 연도' / '표지 및 일정표 연도가'."
    - "OQ #1 LOCKED 적용: 다운로드 버튼 default = `bg-safe-bar text-text-on-accent` solid (모바일+데스크톱). disabled = `bg-surface-sunken text-text-tertiary cursor-not-allowed`. `linear-gradient(135deg,#1e40af,#3b82f6)` 완전 폐기 (grep gate 0건 강제)."
    - "OQ #2 LOCKED 적용: 위치조정 토글 active = `border-accent bg-accent/10 text-accent border` / 평시 = `bg-surface-sunken border border-border-strong text-text-secondary` (모바일+데스크톱 동일 토큰)."
    - "OQ #3 LOCKED 적용: 폰트 격상 — fontSize 11 (모바일 위치조정 토글) → `text-caption font-bold leading-none` (12 격상). fontSize 12 (데스크톱 설명 / 데스크톱 위치조정 / 캘리브 안내 칩) → `text-caption ... leading-none` (12 유지). fontSize 13 (모바일 설명 / 데스크톱 다운로드) → `text-label` (13 유지). fontSize 14 (모바일 헤더 타이틀 / 모바일 다운로드) → `text-body font-bold` (14→16 격상). 연도 오버레이 `fontSize:'min(1.4vw, 16px)'` 인라인 유지 (★ 캘리브 시그니처)."
    - "OQ #4 LOCKED 적용: lucide-react import 신규 1줄 추가 (`import { ChevronLeft, Download } from 'lucide-react'`). 모바일 back button svg (line 175~177) → `<ChevronLeft size={15} className=\"text-text-secondary\" />`. 데스크톱 다운로드 svg (line 141~143) → `<Download size={15} />`. 모바일 다운로드 svg (line 216~218) → `<Download size={16} />`. svg path 인라인 폐기 (grep gate 'M12 5v14' / 'M15 19l-7-7' 0건)."
    - "OQ #5 LOCKED 적용: preview border 캘리브 모드 = `border-2 border-accent` / 평시 = `border border-border-default` (line 72 className 분기). 캘리브 안내 칩 (line 92~98) — `background:'rgba(59,130,246,0.9)'` 인라인 유지 (alpha 정밀도 토큰 비용 회피) + `color:'#fff'` → `text-white` className + `fontWeight 700` + `whiteSpace nowrap` + `pointerEvents none` 모두 보존."
    - "Negative gate 9건 모두 PASS: 이모지 0 / linear-gradient 0 / fontSize 9·10·11 인라인 0 / status- prefix 0 / w-8·h-8 0 / 옛 alias var(--bg|bg2|bg3|bd|bd2|t1|t2|t3|acl|accent) 0 / svg path 'M12 5v14' 0 / svg path 'M15 19l-7-7' 0 / text-danger 토큰 0."
    - "Positive gate 모두 PASS: v0.1.1 토큰 className ≥3 / bg-safe-bar ≥2 (모바일+데스크톱 다운로드) / border-accent ≥2 (위치조정 active + preview 캘리브) / bg-accent/10 ≥2 / text-accent ≥2 / bg-surface-sunken ≥3 / border-border-default ≥3 / border-border-strong ≥2 / ChevronLeft ≥2 / Download ≥3 / lucide-react import ≥1 / text-caption ≥4 / text-label ≥2 / text-body ≥2 / rounded-sm ≥4 / w-[34px] h-[34px] ≥1."
    - "★ 캘리브 시그니처 보존 grep gate 7건 PASS: STORAGE_KEY ≥1 / FINGER_OFFSET ≥2 (정의 + 사용) / loadPos ≥1 / handleImageClick ≥1 / handleImageTouch ≥1 / 'min(1.4vw, 16px)' ≥1 / 'Malgun Gothic, 맑은 고딕, sans-serif' ≥1 / '/templates/preview/annual-plan.png' ≥1 / rgba(59,130,246,0.9) 인라인 ≥1."
    - "Build gate: `cd cha-bio-safety && npm run build` exit 0 (tsc --noEmit 0 errors + Vite build PASS). AnnualPlanPage chunk size 보고."
    - "Atomic 1-commit: `tsx(quick-260522-1hj): redesign/17-annual-plan AnnualPlanPage.tsx v0.1.1 토큰 변환 (W2/W3/W4 sketch verbatim + OQ 5건 LOCKED + 캘리브 시그니처 5건 보존)`."
  artifacts:
    - path: cha-bio-safety/src/pages/AnnualPlanPage.tsx
      provides: v0.1.1 토큰 className 적용 + OQ 5건 LOCKED 반영 + 캘리브 좌표 시스템 시그니처 5건 1 byte 변경 0 + Lucide ChevronLeft/Download import 1줄 추가 + 비즈 로직 0 diff
      contains: "bg-surface-raised / bg-surface-sunken / bg-safe-bar / border-border-default / border-border-strong / border-accent / bg-accent/10 / text-accent / text-text-primary / text-text-secondary / text-text-tertiary / text-text-on-accent / text-white / text-caption / text-label / text-body / w-[34px] / h-[34px] / rounded-sm / rounded-md / ChevronLeft / Download / STORAGE_KEY / FINGER_OFFSET / loadPos / handleImageClick / handleImageTouch / yearPos / generateAnnualPlan / Malgun Gothic / annual-plan.png / min(1.4vw, 16px) / rgba(59,130,246,0.9)"
      min_lines: 200
  key_links:
    - from: "W5 §3.1 (영역 5 데스크톱 상단 바, AnnualPlanPage.tsx line 110~146)"
      to: AnnualPlanPage.tsx line 110~146
      via: "border-b border-border-default flex items-center (상단 바 wrapper) + text-caption leading-none text-text-tertiary (설명) + text-text-primary font-bold (strong) + bg-surface-sunken border border-border-strong text-text-secondary text-caption font-bold leading-none rounded-sm (위치조정 평시) + border-accent bg-accent/10 text-accent border (위치조정 active) + bg-safe-bar text-text-on-accent text-label font-bold leading-none rounded-sm + Download size={15} (다운로드)"
      pattern: "bg-safe-bar.*text-text-on-accent"
    - from: "W5 §3.2 (영역 4 모바일 헤더, AnnualPlanPage.tsx line 169~192)"
      to: AnnualPlanPage.tsx line 169~192
      via: "bg-surface-raised border-b border-border-default flex items-center + w-[34px] h-[34px] rounded-sm bg-surface-sunken border border-border-default + ChevronLeft size={15} text-text-secondary + text-body font-bold text-text-primary (14→16 격상 타이틀) + bg-surface-sunken border border-border-strong text-text-secondary text-caption font-bold leading-none rounded-sm (위치조정 평시, 11→12 격상)"
      pattern: "w-\\[34px\\]"
    - from: "W5 §3.3 (영역 2 preview + 캘리브, AnnualPlanPage.tsx line 61~103)"
      to: AnnualPlanPage.tsx line 61~103
      via: "rounded-sm + border-2 border-accent (캘리브 모드) / border border-border-default (평시) + yearPos overlay 인라인 100% 보존 (fontSize 'min(1.4vw, 16px)' / fontFamily 'Malgun Gothic, 맑은 고딕, sans-serif' / pointerEvents none / color #000 / fontWeight 700) + 캘리브 안내 칩 className text-caption font-bold leading-none text-white rounded-sm + 인라인 background rgba(59,130,246,0.9) 유지"
      pattern: "border-2 border-accent|border border-border-default"
    - from: "W5 §3.4 (영역 5 모바일 다운로드, AnnualPlanPage.tsx line 200~221)"
      to: AnnualPlanPage.tsx line 200~221
      via: "text-label leading-relaxed text-text-tertiary (모바일 설명, 13 유지) + bg-safe-bar text-text-on-accent text-body font-bold rounded-md (모바일 다운로드 default, 14→16 격상 + radius 10→12) + bg-surface-sunken text-text-tertiary cursor-not-allowed (disabled) + Download size={16}"
      pattern: "rounded-md"
    - from: "★ 캘리브 좌표 시스템 시그니처 5건 (15-daily-report SW3 precedent)"
      to: AnnualPlanPage.tsx line 7~8, 35~45, 47~59, 78~89
      via: "STORAGE_KEY = 'annual_plan_year_pos' / FINGER_OFFSET = 60 / handleImageClick 좌표 계산식 (((e.clientX - rect.left) / rect.width) * 100) / handleImageTouch FINGER_OFFSET 보정식 (((touch.clientY - FINGER_OFFSET) - rect.top) / rect.height) * 100) / yearPos overlay inline style (position absolute / top y% / left x% / transform translate(-50%,-50%) / fontSize 'min(1.4vw, 16px)' / fontWeight 700 / color #000 / fontFamily 'Malgun Gothic, 맑은 고딕, sans-serif' / pointerEvents none) — 1 byte 변경 금지"
      pattern: "STORAGE_KEY|FINGER_OFFSET|handleImageClick|handleImageTouch|Malgun Gothic"
---

<objective>
redesign/17-annual-plan TSX 변환 — AnnualPlanPage.tsx (225 lines) 단일 atomic in-place 수정.

W2/W3/W4 sketch + W5 12 섹션 checklist + W1 OQ 5건 LOCKED 결정을 1-commit 으로 적용. **★ 캘리브 좌표 시스템 시그니처 5건 (STORAGE_KEY / FINGER_OFFSET / handleImageClick / handleImageTouch / yearPos overlay) 1 byte 변경 0** — 15-daily-report SW3 precedent 동일 적용.

Purpose:
- v0.1.1 토큰 className 으로 옛 alias `var(--bg|bg2|bg3|bd|bd2|t1|t2|t3|acl|accent)` 0건 치환
- **OQ #1 LOCKED**: 다운로드 버튼 (모바일+데스크톱) → `bg-safe-bar text-text-on-accent` solid + `linear-gradient(135deg,#1e40af,#3b82f6)` 완전 폐기 / disabled → `bg-surface-sunken text-text-tertiary cursor-not-allowed`
- **OQ #2 LOCKED**: 위치조정 토글 active → `border-accent bg-accent/10 text-accent border` / 평시 → `bg-surface-sunken border border-border-strong text-text-secondary` (모바일+데스크톱 동일)
- **OQ #3 LOCKED**: 폰트 격상 — fontSize 11 → text-caption (12 격상) / 12 → text-caption (유지) / 13 → text-label (유지) / 14 → text-body (16 격상, 모바일 헤더·다운로드)
- **OQ #4 LOCKED**: lucide-react import 신규 1줄 + 인라인 svg 3건 (back button + 데스크톱 다운로드 + 모바일 다운로드) → Lucide `ChevronLeft size={15}` + `Download size={15/16}` 교체
- **OQ #5 LOCKED**: preview border 캘리브 모드 → `border-2 border-accent` 토큰 / 평시 → `border border-border-default` 토큰. 캘리브 안내 칩 `background:rgba(59,130,246,0.9)` 인라인 유지 (alpha 정밀도 토큰 비용 회피)
- ★ 캘리브 좌표 시스템 시그니처 5건 (STORAGE_KEY / FINGER_OFFSET / loadPos / handleImageClick / handleImageTouch / yearPos overlay inline style) **1 byte 변경 금지** — 15-daily-report SW3 precedent
- 비즈 로직 0 diff (state/handler/effect/hook/카피/preview 자산 경로 모두 보존)

Output:
- cha-bio-safety/src/pages/AnnualPlanPage.tsx 단일 파일 in-place 수정 (225 → 225~270 lines)
- generateAnnualPlan.ts 0 byte 변경
- 다른 파일 변경 0건 (App.tsx / tailwind.config.js / tokens.css / functions/ / templates/ / migrations/ / public/ 모두 무영향)
- `lucide-react` import 1줄 추가 (`ChevronLeft, Download`)
- `npm run build` PASS
- atomic 1-commit
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jykevin/Documents/cbc7119-design/CLAUDE.md
@/Users/jykevin/Documents/cbc7119-design/CLAUDE.local.md
@/Users/jykevin/Documents/cbc7119-design/.planning/STATE.md

# W5 TSX 변환 checklist (427 lines, SOURCE OF TRUTH — §3 매핑 표 verbatim)
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/17-annual-plan/wave-5-tsx-conversion-checklist.md

# W1 OQ LOCKED 결정 (5건) + 메모리 룰 13개
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/17-annual-plan/wave-1-index.md

# W2~W4 sketch (변환 매핑 시각 검증용)
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/17-annual-plan/sketch-wave-2-chrome.html
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/17-annual-plan/sketch-wave-3-preview-calibration.html
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/17-annual-plan/sketch-wave-4-download.html

# 토큰 / 타이포 source
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/17-annual-plan/tokens.css
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/tailwind.config.js

# Precedent — 27-login gox + 16-workshift u5n PLAN.md (정확한 mirror, 단일 atomic 1-commit / W5 checklist verbatim 적용)
@/Users/jykevin/Documents/cbc7119-design/.planning/quick/260521-gox-redesign-27-login-tsx-loginpage-tsx-220-/260521-gox-PLAN.md
@/Users/jykevin/Documents/cbc7119-design/.planning/quick/260521-u5n-redesign-16-workshift-tsx-workshiftpage-/260521-u5n-PLAN.md
@/Users/jykevin/Documents/cbc7119-design/.planning/quick/260521-u5n-redesign-16-workshift-tsx-workshiftpage-/260521-u5n-SUMMARY.md

# 변환 대상 (in-place 수정)
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/src/pages/AnnualPlanPage.tsx

# 변환 무영향 — 0 byte 변경
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/src/utils/generateAnnualPlan.ts

<interfaces>
<!-- AnnualPlanPage.tsx 안의 비즈 로직 시그니처 — 모두 보존 (변환 시 1 byte 도 손대지 않음) -->

```typescript
// imports (line 1~5) — 그대로 + line 5 다음 또는 line 1 위치에 lucide-react 1줄 추가
import { useState, useRef } from 'react'
import { useNavigate } from 'react-router-dom'
import toast from 'react-hot-toast'
import { generateAnnualPlan } from '../utils/generateAnnualPlan'
import { useIsDesktop } from '../hooks/useIsDesktop'
import { ChevronLeft, Download } from 'lucide-react'   // ← OQ #4 LOCKED 신규 추가 1줄

// ★ 캘리브 좌표 시스템 시그니처 — 1 byte 변경 금지
const STORAGE_KEY = 'annual_plan_year_pos'   // line 7
const FINGER_OFFSET = 60                      // line 8

function loadPos(): { x: number; y: number } | null {   // line 10~12
  try { return JSON.parse(localStorage.getItem(STORAGE_KEY) ?? 'null') } catch { return null }
}

// AnnualPlanPage 시그니처 (line 14) — 그대로
export default function AnnualPlanPage()

// state / ref / hook (line 15~21) — 그대로
const navigate = useNavigate()
const isDesktop = useIsDesktop()
const [loading, setLoading] = useState(false)
const [calibMode, setCalibMode] = useState(false)
const [yearPos, setYearPos] = useState<{ x: number; y: number } | null>(loadPos)
const imgRef = useRef<HTMLImageElement>(null)
const nextYear = new Date().getFullYear() + 1

// handleDownload (line 23~33) — 그대로 (dynamic import 아님, 직접 import 호출)
const handleDownload = async () => {
  setLoading(true)
  try {
    await generateAnnualPlan()
    toast.success('엑셀이 다운로드됐습니다')
  } catch (e: any) {
    toast.error(e?.message ?? '생성 중 오류')
  } finally {
    setLoading(false)
  }
}

// ★ handleImageClick — 1 byte 변경 금지 (캘리브 시그니처)
// line 35~45 — getBoundingClientRect / % 좌표 / setYearPos / localStorage.setItem / setCalibMode(false) / toast.success
const handleImageClick = (e: React.MouseEvent<HTMLImageElement>) => {
  if (!calibMode) return
  const rect = e.currentTarget.getBoundingClientRect()
  const x = ((e.clientX - rect.left) / rect.width) * 100
  const y = ((e.clientY - rect.top) / rect.height) * 100
  const pos = { x, y }
  setYearPos(pos)
  localStorage.setItem(STORAGE_KEY, JSON.stringify(pos))
  setCalibMode(false)
  toast.success(`연도 위치 저장됨 (${x.toFixed(1)}%, ${y.toFixed(1)}%)`)
}

// ★ handleImageTouch — 1 byte 변경 금지 (캘리브 시그니처, FINGER_OFFSET 보정)
// line 47~59
const handleImageTouch = (e: React.TouchEvent<HTMLImageElement>) => {
  if (!calibMode) return
  e.preventDefault()
  const touch = e.touches[0]
  const rect = e.currentTarget.getBoundingClientRect()
  const x = ((touch.clientX - rect.left) / rect.width) * 100
  const y = (((touch.clientY - FINGER_OFFSET) - rect.top) / rect.height) * 100
  const pos = { x, y }
  setYearPos(pos)
  localStorage.setItem(STORAGE_KEY, JSON.stringify(pos))
  setCalibMode(false)
  toast.success(`연도 위치 저장됨`)
}

// ★ yearPos overlay — 인라인 style 1 byte 변경 금지 (캘리브 시그니처)
// line 78~89
{yearPos && (
  <div style={{
    position:'absolute',
    top:`${yearPos.y}%`, left:`${yearPos.x}%`,
    transform:'translate(-50%,-50%)',
    fontSize:'min(1.4vw, 16px)', fontWeight:700,
    color:'#000', fontFamily:'Malgun Gothic, 맑은 고딕, sans-serif',
    pointerEvents:'none',
  }}>
    {nextYear}
  </div>
)}
```
</interfaces>

<calibration_signature_lockdown>
<!-- 15-daily-report SW3 precedent — 캘리브 좌표 시스템 시그니처 보존 = 사용자 좌표 무손실 변환 조건 -->

**5건 시그니처 — 모두 1 byte 변경 금지:**

1. **`STORAGE_KEY = 'annual_plan_year_pos'`** (line 7)
   - localStorage key — 변경 시 기존 사용자 좌표 손실 (기 등록된 yearPos 모두 무효화)
   - 따옴표 종류 / 공백 / 값 모두 동일

2. **`FINGER_OFFSET = 60`** (line 8)
   - TouchEvent y 보정 px — 손가락이 가리는 영역 보정값
   - 60 → 다른 숫자 변경 시 모바일 캘리브 좌표 어긋남

3. **`loadPos()` helper** (line 10~12)
   - try/catch + JSON.parse + localStorage.getItem + fallback null
   - 시그니처 함수 본문 전체 그대로

4. **`handleImageClick` 좌표 계산식** (line 35~45)
   - `getBoundingClientRect()` / `((clientX - rect.left) / rect.width) * 100` / `((clientY - rect.top) / rect.height) * 100`
   - `setYearPos` / `localStorage.setItem(STORAGE_KEY, JSON.stringify(pos))` / `setCalibMode(false)` / `toast.success` 흐름 그대로

5. **`handleImageTouch` 좌표 계산식** (line 47~59)
   - `e.preventDefault()` / `touch.clientY - FINGER_OFFSET` 보정 / 동일 % 계산 식 / localStorage / setCalibMode 흐름 그대로

**6. yearPos overlay inline style** (line 78~89) — 별도 박제:
   - `position: 'absolute'`
   - `top: ${yearPos.y}%`, `left: ${yearPos.x}%`
   - `transform: 'translate(-50%,-50%)'`
   - `fontSize: 'min(1.4vw, 16px)'` — 반응형 vw + 16px cap (엑셀 표지 글꼴 크기 시각 일치 anchor)
   - `fontWeight: 700`
   - `color: '#000'`
   - `fontFamily: 'Malgun Gothic, 맑은 고딕, sans-serif'` — 엑셀 표지 폰트 일치 anchor (`var(--font-sans)` 교체 금지, memory `feedback_pdflib_subset_false` 폰트 출력물 anchor 룰)
   - `pointerEvents: 'none'`

**확인 grep gate (verify automated 안에 박제):**
```bash
grep -c "STORAGE_KEY = 'annual_plan_year_pos'" cha-bio-safety/src/pages/AnnualPlanPage.tsx  # >= 1
grep -c "FINGER_OFFSET = 60" cha-bio-safety/src/pages/AnnualPlanPage.tsx                    # >= 1
grep -c 'loadPos' cha-bio-safety/src/pages/AnnualPlanPage.tsx                               # >= 1
grep -c 'handleImageClick' cha-bio-safety/src/pages/AnnualPlanPage.tsx                      # >= 1
grep -c 'handleImageTouch' cha-bio-safety/src/pages/AnnualPlanPage.tsx                      # >= 1
grep -c 'min(1.4vw, 16px)' cha-bio-safety/src/pages/AnnualPlanPage.tsx                      # >= 1
grep -c 'Malgun Gothic' cha-bio-safety/src/pages/AnnualPlanPage.tsx                         # >= 1
grep -c '/templates/preview/annual-plan.png' cha-bio-safety/src/pages/AnnualPlanPage.tsx    # >= 1
grep -c 'rgba(59,130,246,0.9)' cha-bio-safety/src/pages/AnnualPlanPage.tsx                  # >= 1  (OQ #5 캘리브 안내 칩)
```
</calibration_signature_lockdown>

<tailwind_token_cheatsheet>
<!-- W5 §9 verbatim 인용 — 옛 alias → v0.1.1 className 1:1 매핑 (17-annual-plan 적용 위치) -->

| 옛 인라인 / alias                                       | v0.1.1 className              | 17-annual-plan 적용 위치                    |
|---------------------------------------------------------|-------------------------------|---------------------------------------------|
| `var(--bg)` (외곽)                                      | `bg-surface-page` (또는 인라인 유지) | 모바일 외곽 + 데스크톱 미리보기 wrapper       |
| `var(--bg2)`                                            | `bg-surface-raised`           | 모바일 헤더                                  |
| `var(--bg3)`                                            | `bg-surface-sunken`           | back button + 위치조정 토글 평시 (모바일+데스크톱) + 다운로드 disabled |
| `var(--bd)`                                             | `border-border-default`       | 모바일 헤더 border-b + back button + 데스크톱 상단 바 border-b + preview 평시 |
| `var(--bd2)`                                            | `border-border-strong`        | 위치조정 토글 평시 border (모바일+데스크톱)   |
| `var(--t1)`                                             | `text-text-primary`           | 모바일 헤더 타이틀 + 데스크톱 설명 `<strong>` |
| `var(--t2)`                                             | `text-text-secondary`         | 위치조정 토글 평시 + ChevronLeft             |
| `var(--t3)`                                             | `text-text-tertiary`          | 데스크톱 설명 + 모바일 설명 + 다운로드 disabled |
| `#ffffff` (CTA 위 텍스트)                                | `text-text-on-accent`         | 다운로드 default                             |
| `#fff` (캘리브 안내 칩 글자)                              | `text-white`                  | 캘리브 안내 칩 (OQ #5)                       |
| `var(--acl)` (다운로드 bg, linear-gradient)              | `bg-safe-bar` (OQ #1 LOCKED)  | 다운로드 default (모바일+데스크톱)            |
| `var(--acl)` (위치조정 토글 active)                      | `border-accent bg-accent/10 text-accent` (OQ #2 LOCKED) | 위치조정 토글 active (모바일+데스크톱) |
| `var(--acl)` (preview border 캘리브)                    | `border-accent` (OQ #5 LOCKED) | preview border 캘리브 모드                  |
| `rgba(59,130,246,0.9)` (캘리브 안내 칩 bg)              | 인라인 유지 (OQ #5 LOCKED)    | 캘리브 안내 칩 background                    |
| `rgba(59,130,246,0.1)` (위치조정 active bg)             | `bg-accent/10` (OQ #2 LOCKED) | 위치조정 active 배경                         |
| `fontSize: 11` (모바일 위치조정 토글)                   | `text-caption font-bold leading-none` (12 격상, OQ #3) | 모바일 위치조정 토글 |
| `fontSize: 12` (데스크톱 설명 / 데스크톱 위치조정 / 캘리브 칩) | `text-caption ... leading-none` (12 유지, OQ #3) | 3 곳 |
| `fontSize: 13` (모바일 설명 / 데스크톱 다운로드)        | `text-label` (13 유지, OQ #3) | 2 곳 |
| `fontSize: 14` (모바일 헤더 타이틀 / 모바일 다운로드)   | `text-body font-bold` (16 격상, OQ #3) | 모바일 헤더 + 모바일 다운로드 |
| `fontSize: 'min(1.4vw, 16px)'` (연도 오버레이)         | **인라인 유지** (★ 캘리브 시그니처, OQ #3 예외) | 연도 오버레이 (1 byte 변경 금지) |
| `borderRadius: 8` (back button / 토글 / 데스크톱 다운로드 / preview img) | `rounded-sm` | 4곳 |
| `borderRadius: 10` (모바일 다운로드)                    | `rounded-md` (10→12 마이그레이션) | 모바일 다운로드 |
| `width: 34, height: 34` (모바일 back button)            | `w-[34px] h-[34px]` (memory `feedback_tailwind_w8_h8_is_48px`) | 모바일 back button |

**status- class prefix 0건** (memory `feedback_tailwind_token_class_pattern`):
- 정확 = `bg-safe-bar` / `border-accent` / `text-accent` / `bg-accent/10`
- 잘못 = `bg-status-safe-bar` / `border-status-accent` ← 11-div TSX hotfix(4ce707e) 사고 회피

**w-8 / h-8 = 48px 함정** (memory `feedback_tailwind_w8_h8_is_48px`):
- tailwind.config spacing override: `w-8 = 48`, `w-7 = 32`, `w-6 = 24` (기본 다름)
- back button 34px → `w-[34px] h-[34px]` arbitrary 인라인 (w-8 사용 X)

**작은 컨테이너 text-caption → leading-none** (memory `feedback_text_caption_leading_none`):
- 데스크톱 위치조정 (h≈34, padding 8/14) + 모바일 위치조정 (h≈30, padding 6/10) + 캘리브 안내 칩 (padding 6/16) 모두 `leading-none` 명시
- 데스크톱 설명 (인라인 텍스트, line-height 의도적 1) 도 `leading-none`
- 데스크톱 다운로드 (h≈34, padding 8/20) `leading-none`

**linear-gradient 폐기 (OQ #1 LOCKED) — 그라데이션 완전 제거:**
- `linear-gradient(135deg,#1e40af,#3b82f6)` 2건 (line 135 데스크톱, line 210 모바일) 모두 폐기
- 대체: `bg-safe-bar text-text-on-accent` solid (W4 sketch frame 1/3 verbatim)
- disabled 상태: `bg-surface-sunken text-text-tertiary cursor-not-allowed`
</tailwind_token_cheatsheet>
</context>

<tasks>

<task type="auto">
  <name>Task 1: AnnualPlanPage.tsx v0.1.1 토큰 변환 + 캘리브 시그니처 5건 보존 (in-place, atomic 1-commit)</name>
  <files>cha-bio-safety/src/pages/AnnualPlanPage.tsx</files>
  <action>

AnnualPlanPage.tsx 225 lines 를 in-place 수정하여 v0.1.1 토큰 className 으로 치환한다. **단일 atomic commit**. 비즈 로직(useState/useRef/handler/effect/hook/카피/preview 자산 경로) 1 byte 변경 0. **★ 캘리브 좌표 시스템 시그니처 5건 (STORAGE_KEY / FINGER_OFFSET / loadPos / handleImageClick / handleImageTouch / yearPos overlay inline style) 1 byte 변경 금지**.

## 0. 사전 확인 (필수)

```bash
git branch --show-current   # 기대: redesign/17-annual-plan-tsx
git status --short          # 기대: 빈 출력 (clean)
wc -l cha-bio-safety/src/pages/AnnualPlanPage.tsx   # 기대: 225

# generateAnnualPlan.ts 현 상태 (변환 후 0 diff 확인용 baseline)
wc -l cha-bio-safety/src/utils/generateAnnualPlan.ts
```

## 1. 변환 매핑 (W5 §3.1~§3.4 verbatim — 추측 0건)

### §3.1 영역 5 데스크톱 — 상단 바 (AnnualPlanPage.tsx line 110~146, W2 sketch + W4 다운로드)

| 현재 (인라인 style, line 번호) | 변환 후 (className + 인라인) | sketch 출처 |
|---|---|---|
| 외곽 wrapper line 108: `style={{ width:'100%', height:'100%', display:'flex', flexDirection:'column', overflow:'hidden' }}` | **인라인 유지** (style 그대로) | W2 |
| 상단 바 line 110~114: `style={{ flexShrink:0, padding:'14px 28px', display:'flex', alignItems:'center', gap:12, borderBottom:'1px solid var(--bd)' }}` | `className="border-b border-border-default flex items-center"` + `style={{ flexShrink:0, padding:'14px 28px', gap:12 }}` | W2 frame 3/4 |
| 설명 div line 115: `style={{ flex:1, fontSize:12, color:'var(--t3)' }}` | `className="text-caption leading-none text-text-tertiary"` + `style={{ flex:1 }}` (OQ #3 LOCKED 12 유지) | W2 frame 3/4 |
| `<strong>` line 116: `style={{ color:'var(--t1)', fontWeight:700 }}` | `className="text-text-primary font-bold"` | W2 |
| 위치조정 버튼 line 118~129 — 평시 분기 `{ padding:'8px 14px', borderRadius:8, border:'1px solid var(--bd2)', background:'var(--bg3)', color:'var(--t2)', fontSize:12, fontWeight:700, cursor:'pointer' }` | `className={\`text-caption font-bold leading-none rounded-sm border \${calibMode ? 'border-accent bg-accent/10 text-accent' : 'bg-surface-sunken border-border-strong text-text-secondary'}\`}` + `style={{ padding:'8px 14px', cursor:'pointer' }}` (OQ #2 LOCKED + OQ #3 LOCKED 12 + leading-none) | W2 frame 3/4 |
| 위치조정 버튼 active 분기 — `border:'1px solid var(--acl)', background:'rgba(59,130,246,0.1)', color:'var(--acl)'` | className 분기로 통합 (위 row) — `linear-gradient` 사용 X / `rgba(59,130,246,0.1)` 인라인 사용 X (`bg-accent/10` 토큰 사용) | W2 |
| 다운로드 버튼 line 130~145 — `<button onClick={handleDownload} disabled={loading} style={{ padding:'8px 20px', borderRadius:8, border:'none', background: loading ? 'var(--bg3)' : 'linear-gradient(135deg,#1e40af,#3b82f6)', color: loading ? 'var(--t3)' : '#fff', fontSize:13, fontWeight:700, cursor: loading ? 'default' : 'pointer', display:'flex', alignItems:'center', gap:8, flexShrink:0 }}>` | `<button onClick={handleDownload} disabled={loading} className={\`text-label font-bold leading-none rounded-sm flex items-center \${loading ? 'bg-surface-sunken text-text-tertiary cursor-not-allowed' : 'bg-safe-bar text-text-on-accent cursor-pointer'}\`} style={{ padding:'8px 20px', gap:8, border:'none', flexShrink:0 }}>` (OQ #1 LOCKED 그라데이션 완전 폐기 + OQ #3 LOCKED 13 text-label + leading-none) | W4 frame 3/4 |
| 다운로드 svg line 141~143 `<svg width={15} height={15} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}><path strokeLinecap="round" strokeLinejoin="round" d="M12 5v14m0 0l-4-4m4 4l4-4M4 19h16"/></svg>` | `<Download size={15} />` (OQ #4 LOCKED Lucide 교체) | W4 frame 3/4 |
| 카피 line 144: `{loading ? '생성 중...' : '엑셀 다운로드'}` | **변경 0** | W4 |
| 미리보기 wrapper line 149~152: `style={{ flex:1, minHeight:0, overflow:'hidden', display:'flex', alignItems:'center', justifyContent:'center', padding:24, background:'var(--bg)' }}` | `className="bg-surface-page"` + `style={{ flex:1, minHeight:0, overflow:'hidden', display:'flex', alignItems:'center', justifyContent:'center', padding:24 }}` (`var(--bg)` → `bg-surface-page` 토큰 마이그레이션) | W3 frame 3/4 |
| 내부 wrapper line 154~158: `style={{ width:'100%', height:'100%', maxWidth:'calc((100vh - 140px) * 1.414)', maxHeight:'100%' }}` | **인라인 유지** (A4 비율 시각 anchor, 변경 금지) | W3 |

### §3.2 영역 4 모바일 — 헤더 (AnnualPlanPage.tsx line 167~192, W2 sketch)

| 현재 | 변환 후 | sketch |
|---|---|---|
| 외곽 wrapper line 168: `style={{ width:'100%', height:'100%', display:'flex', flexDirection:'column', overflow:'hidden', background:'var(--bg)' }}` | `className="bg-surface-page"` + `style={{ width:'100%', height:'100%', display:'flex', flexDirection:'column', overflow:'hidden' }}` | W2 frame 1/2 |
| header line 169: `style={{ flexShrink:0, background:'var(--bg2)', borderBottom:'1px solid var(--bd)', padding:'8px 12px 9px', display:'flex', alignItems:'center', gap:8 }}` | `className="bg-surface-raised border-b border-border-default flex items-center"` + `style={{ flexShrink:0, padding:'8px 12px 9px', gap:8 }}` | W2 frame 1/2 |
| back button line 170~178 — `<button onClick={() => navigate(-1)} style={{ width:34, height:34, borderRadius:8, flexShrink:0, background:'var(--bg3)', border:'1px solid var(--bd)', cursor:'pointer', display:'flex', alignItems:'center', justifyContent:'center' }}>` | `<button onClick={() => navigate(-1)} className="w-[34px] h-[34px] rounded-sm bg-surface-sunken border border-border-default flex items-center justify-center" style={{ flexShrink:0, cursor:'pointer' }}>` (memory `feedback_tailwind_w8_h8_is_48px` — w-[34px] 인라인 안전) | W2 frame 1/2 |
| back button svg line 175~177 `<svg width={15} height={15} fill="none" viewBox="0 0 24 24" stroke="var(--t2)" strokeWidth={2}><path strokeLinecap="round" strokeLinejoin="round" d="M15 19l-7-7 7-7"/></svg>` | `<ChevronLeft size={15} className="text-text-secondary" />` (OQ #4 LOCKED Lucide 교체) | W2 frame 1/2 |
| 타이틀 line 179: `<span style={{ flex:1, fontSize:14, fontWeight:700, color:'var(--t1)' }}>연간 업무 추진 계획</span>` | `<span className="text-body font-bold text-text-primary" style={{ flex:1 }}>연간 업무 추진 계획</span>` (OQ #3 LOCKED 14→16 격상) | W2 frame 1/2 |
| 위치조정 버튼 line 180~191 — 평시 `{ padding:'6px 10px', borderRadius:8, border:'1px solid var(--bd2)', background:'var(--bg3)', color:'var(--t2)', fontSize:11, fontWeight:700, cursor:'pointer' }` | `className={\`text-caption font-bold leading-none rounded-sm border \${calibMode ? 'border-accent bg-accent/10 text-accent' : 'bg-surface-sunken border-border-strong text-text-secondary'}\`}` + `style={{ padding:'6px 10px', cursor:'pointer' }}` (OQ #2 LOCKED + OQ #3 LOCKED 11→12 격상 + leading-none) | W2 frame 1/2 |
| 위치조정 active 분기 — `border:'1px solid var(--acl)', background:'rgba(59,130,246,0.1)', color:'var(--acl)'` | className 분기로 통합 (위 row) | W2 |
| 카피 line 190: `{calibMode ? '취소' : '위치 조정'}` | **변경 0** | W2 |

### §3.3 영역 2 — preview + 캘리브 좌표 시스템 (AnnualPlanPage.tsx line 61~103, W3 sketch)

**★ 메타 — 캘리브 좌표 시스템 시그니처 5건 1 byte 변경 금지 (15-daily-report SW3 precedent):**

> `STORAGE_KEY` / `FINGER_OFFSET` / `loadPos` / `handleImageClick` / `handleImageTouch` 시그니처 함수 본문 + 상수 + yearPos overlay inline style **모두 1 byte 변경 금지**. UI markup 만 className 적용.

| 현재 | 변환 후 | sketch |
|---|---|---|
| 외곽 div line 62: `style={{ position:'relative', width:'100%', height:'100%' }}` | **인라인 유지** | W3 all frames |
| img line 63~76 — `<img ref={imgRef} src="/templates/preview/annual-plan.png" alt="연간 업무 추진 계획 미리보기" onClick={handleImageClick} onTouchStart={handleImageTouch} style={{ width:'100%', height:'100%', objectFit:'contain', borderRadius:8, border: calibMode ? '2px solid var(--acl)' : '1px solid var(--bd)', background:'#fff', cursor: calibMode ? 'crosshair' : 'default' }} />` | `<img ref={imgRef} src="/templates/preview/annual-plan.png" alt="연간 업무 추진 계획 미리보기" onClick={handleImageClick} onTouchStart={handleImageTouch} className={\`rounded-sm \${calibMode ? 'border-2 border-accent' : 'border border-border-default'}\`} style={{ width:'100%', height:'100%', objectFit:'contain', background:'#fff', cursor: calibMode ? 'crosshair' : 'default' }} />` (OQ #5 LOCKED border 토큰 분기. src/alt/onClick/onTouchStart/cursor 분기 1 byte 변경 0) | W3 frame 2/4 vs 1/3 |
| **★ yearPos overlay line 78~89** — `<div style={{ position:'absolute', top:\`${yearPos.y}%\`, left:\`${yearPos.x}%\`, transform:'translate(-50%,-50%)', fontSize:'min(1.4vw, 16px)', fontWeight:700, color:'#000', fontFamily:'Malgun Gothic, 맑은 고딕, sans-serif', pointerEvents:'none' }}>{nextYear}</div>` | **인라인 100% 유지 — 1 byte 변경 금지** (★ 캘리브 시그니처) | W3 frame 3/4 |
| 캘리브 안내 칩 line 91~101 — `<div style={{ position:'absolute', top:8, left:'50%', transform:'translateX(-50%)', background:'rgba(59,130,246,0.9)', color:'#fff', padding:'6px 16px', borderRadius:8, fontSize:12, fontWeight:700, whiteSpace:'nowrap', pointerEvents:'none' }}>연도가 들어갈 위치를 클릭하세요</div>` | `<div className="text-caption font-bold leading-none text-white rounded-sm" style={{ position:'absolute', top:8, left:'50%', transform:'translateX(-50%)', background:'rgba(59,130,246,0.9)', padding:'6px 16px', whiteSpace:'nowrap', pointerEvents:'none' }}>연도가 들어갈 위치를 클릭하세요</div>` (OQ #5 LOCKED — rgba 인라인 유지 + OQ #3 LOCKED 12 text-caption + leading-none + text-white + rounded-sm) | W3 frame 2/4 |
| 카피 line 99: `연도가 들어갈 위치를 클릭하세요` | **변경 0** | W3 |

### §3.4 영역 5 모바일 — 다운로드 묶음 (AnnualPlanPage.tsx line 194~221, W4 sketch)

| 현재 | 변환 후 | sketch |
|---|---|---|
| 본문 wrapper line 194: `style={{ flex:1, overflow:'auto', padding:16, display:'flex', flexDirection:'column', gap:16 }}` | **인라인 유지** | W3 + W4 frame 1/2 |
| 모바일 미리보기 wrapper line 196: `style={{ width:'100%' }}` | **인라인 유지** | W3 frame 1/2 |
| 설명 + 다운로드 wrapper line 201: `style={{ textAlign:'center' }}` | **인라인 유지** | W4 frame 1/2 |
| 모바일 설명 line 202: `style={{ fontSize:13, color:'var(--t3)', marginBottom:12 }}` | `className="text-label leading-relaxed text-text-tertiary"` + `style={{ marginBottom:12 }}` (OQ #3 LOCKED 13 → text-label) | W4 frame 1/2 |
| 모바일 다운로드 line 205~220 — `<button onClick={handleDownload} disabled={loading} style={{ width:'100%', padding:'14px', borderRadius:10, border:'none', background: loading ? 'var(--bg3)' : 'linear-gradient(135deg,#1e40af,#3b82f6)', color: loading ? 'var(--t3)' : '#fff', fontSize:14, fontWeight:700, cursor: loading ? 'default' : 'pointer', display:'flex', alignItems:'center', justifyContent:'center', gap:8 }}>` | `<button onClick={handleDownload} disabled={loading} className={\`text-body font-bold rounded-md flex items-center justify-center \${loading ? 'bg-surface-sunken text-text-tertiary cursor-not-allowed' : 'bg-safe-bar text-text-on-accent cursor-pointer'}\`} style={{ width:'100%', padding:'14px', gap:8, border:'none' }}>` (OQ #1 LOCKED solid + OQ #3 LOCKED 14→16 격상 text-body + radius 10→12 rounded-md) | W4 frame 1 vs 2 |
| 모바일 다운로드 svg line 216~218 `<svg width={16} height={16} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}><path strokeLinecap="round" strokeLinejoin="round" d="M12 5v14m0 0l-4-4m4 4l4-4M4 19h16"/></svg>` | `<Download size={16} />` (OQ #4 LOCKED Lucide 교체) | W4 frame 1/2 |
| 카피 line 219: `{loading ? '생성 중...' : '엑셀 다운로드'}` | **변경 0** | W4 |

## 2. import 추가 (OQ #4 LOCKED)

line 5 다음 또는 line 1 위치에 신규 1줄 추가:

```typescript
import { ChevronLeft, Download } from 'lucide-react'
```

다른 imports (line 1~5) 변경 0.

## 3. OQ 5건 LOCKED 결정 (W1 §7 + W5 §4 verbatim — 위반 0건)

### OQ #1 LOCKED — 다운로드 버튼 `bg-safe-bar` solid + 그라데이션 완전 폐기
- **데스크톱 다운로드 (line 130~145)** + **모바일 다운로드 (line 205~220)** 양쪽:
  - `linear-gradient(135deg,#1e40af,#3b82f6)` 인라인 완전 제거 → className 분기 `${loading ? 'bg-surface-sunken text-text-tertiary cursor-not-allowed' : 'bg-safe-bar text-text-on-accent cursor-pointer'}`
  - `color: loading ? 'var(--t3)' : '#fff'` 인라인 제거 → 위 className 의 `text-text-tertiary` / `text-text-on-accent`
  - `cursor: loading ? 'default' : 'pointer'` 인라인 제거 → 위 className 의 `cursor-not-allowed` / `cursor-pointer`
  - `border:'none'` 인라인 잔존
  - `fontSize:13` (데스크톱) → className `text-label font-bold leading-none`
  - `fontSize:14` (모바일) → className `text-body font-bold` (16 격상)
  - `borderRadius:8` (데스크톱) → className `rounded-sm`
  - `borderRadius:10` (모바일) → className `rounded-md` (radius 10→12 마이그레이션, sketch 명시)
  - `display:'flex', alignItems:'center', gap:8 + flexShrink:0` 일부는 className `flex items-center`, gap 은 인라인 유지

### OQ #2 LOCKED — 위치조정 토글 토큰화 (border-accent + bg-accent/10 + text-accent)
- **데스크톱 위치조정 (line 118~129)** + **모바일 위치조정 (line 180~191)** 양쪽:
  - 평시: className 분기 `bg-surface-sunken border-border-strong text-text-secondary` + 공통 `text-caption font-bold leading-none rounded-sm border`
  - active (calibMode true): className 분기 `border-accent bg-accent/10 text-accent` + 공통 동일
  - `rgba(59,130,246,0.1)` 인라인 사용 0 (`bg-accent/10` 토큰 사용)
  - `padding:'8px 14px'` (데스크톱) / `padding:'6px 10px'` (모바일) 인라인 유지
  - `cursor:'pointer'` 인라인 유지

### OQ #3 LOCKED — 폰트 격상 매핑 (W5 §4 verbatim)
- line 115 데스크톱 설명 `fontSize:12, color:'var(--t3)'` → `text-caption leading-none text-text-tertiary`
- line 116 strong `color:'var(--t1)', fontWeight:700` → `text-text-primary font-bold`
- line 125 데스크톱 위치조정 `fontSize:12, fontWeight:700` → `text-caption font-bold leading-none`
- line 137 데스크톱 다운로드 `fontSize:13, fontWeight:700` → `text-label font-bold leading-none`
- line 179 모바일 헤더 타이틀 `fontSize:14, fontWeight:700` → `text-body font-bold` (14→16 격상)
- line 187 모바일 위치조정 `fontSize:11, fontWeight:700` → `text-caption font-bold leading-none` (11→12 격상)
- line 202 모바일 설명 `fontSize:13, color:'var(--t3)'` → `text-label leading-relaxed text-text-tertiary`
- line 212 모바일 다운로드 `fontSize:14, fontWeight:700` → `text-body font-bold` (14→16 격상)
- line 96 캘리브 안내 칩 `fontSize:12, fontWeight:700` → `text-caption font-bold leading-none`
- **line 83 연도 오버레이 `fontSize:'min(1.4vw, 16px)'` → 인라인 100% 유지** (★ 캘리브 시그니처, 1 byte 변경 금지)

### OQ #4 LOCKED — Lucide 아이콘 교체 (svg path 인라인 폐기)
- **line 1~5 다음 신규 import 1줄 추가**: `import { ChevronLeft, Download } from 'lucide-react'`
- line 175~177 모바일 back button svg → `<ChevronLeft size={15} className="text-text-secondary" />`
- line 141~143 데스크톱 다운로드 svg → `<Download size={15} />`
- line 216~218 모바일 다운로드 svg → `<Download size={16} />`
- svg path 인라인 완전 폐기 (grep gate 'M12 5v14' / 'M15 19l-7-7' 모두 0건)

### OQ #5 LOCKED — preview border 토큰 + 캘리브 안내 칩 rgba 인라인 유지
- line 72 preview img border 분기: `'2px solid var(--acl)' / '1px solid var(--bd)'` → className 분기 `border-2 border-accent` / `border border-border-default`
- line 92~98 캘리브 안내 칩:
  - `background:'rgba(59,130,246,0.9)'` **인라인 100% 유지** (alpha 정밀도 토큰 비용 회피, OQ #5 명시)
  - `color:'#fff'` → className `text-white`
  - `borderRadius:8` → className `rounded-sm`
  - `fontSize:12, fontWeight:700` → className `text-caption font-bold leading-none`
  - 인라인 잔존: `position:'absolute', top:8, left:'50%', transform:'translateX(-50%)', padding:'6px 16px', whiteSpace:'nowrap', pointerEvents:'none'`

## 4. ★ 캘리브 좌표 시스템 시그니처 5건 보존 (15-daily-report SW3 precedent — 1 byte 변경 금지)

다음은 **변경 0** — 인라인 style 제거 외 logic / 상수 / 값 라인 손대지 않음:

1. **line 7**: `const STORAGE_KEY = 'annual_plan_year_pos'` — 따옴표 / 공백 / 값 모두 그대로
2. **line 8**: `const FINGER_OFFSET = 60` — 값 60 변경 0
3. **line 10~12**: `function loadPos(): { x: number; y: number } | null { try { return JSON.parse(localStorage.getItem(STORAGE_KEY) ?? 'null') } catch { return null } }` — 함수 본문 그대로
4. **line 35~45**: `handleImageClick` — `getBoundingClientRect` / % 계산식 / `setYearPos` / `localStorage.setItem` / `setCalibMode(false)` / `toast.success` 흐름 그대로
5. **line 47~59**: `handleImageTouch` — `e.preventDefault()` / `touch.clientY - FINGER_OFFSET` 보정식 / 동일 % 계산 / localStorage / setCalibMode 흐름 그대로
6. **line 78~89 yearPos overlay inline style** — position/top/left/transform/fontSize/fontWeight/color/fontFamily/pointerEvents 모두 100% 유지 (★ 캘리브 시그니처 일부, OQ #3 LOCKED 예외)

## 5. 비즈 로직 0 diff 보존 (W5 §2 + §10 19+ 항목)

다음은 **변경 0** — 인라인 style 제거 외 logic 라인 손대지 않음:

- line 1~5 imports 5개 (useState/useRef / useNavigate / toast / generateAnnualPlan / useIsDesktop) — lucide-react 1줄만 추가, 다른 import 변경 0
- line 14 `export default function AnnualPlanPage()`
- line 15~16 useNavigate / useIsDesktop
- line 17~19 useState 3종 (loading / calibMode / yearPos initial=loadPos)
- line 20~21 useRef (imgRef) + nextYear
- line 23~33 handleDownload async + `generateAnnualPlan()` direct import + toast.success/error + finally setLoading(false)
- line 63~68 img onClick={handleImageClick} / onTouchStart={handleImageTouch} 핸들러 바인딩
- line 65 img src `/templates/preview/annual-plan.png` — **경로 변경 금지**
- line 66 img alt `'연간 업무 추진 계획 미리보기'` — 카피 verbatim
- line 119, 181 `setCalibMode(m => !m)` 토글
- line 131, 206 handleDownload onClick / line 132, 207 disabled={loading}
- line 128, 190 카피 분기 `{calibMode ? '취소' : '위치 조정'}`
- line 144, 219 카피 분기 `{loading ? '생성 중...' : '엑셀 다운로드'}`
- line 170 모바일 back button onClick `() => navigate(-1)`
- line 179 모바일 헤더 타이틀 `'연간 업무 추진 계획'`
- line 116 데스크톱 설명 카피 `대상 연도 ... 표지 및 일정표 연도가 자동 설정됩니다.`
- line 203 모바일 설명 카피 `표지 및 일정표 연도가 {nextYear}년으로 자동 설정됩니다.`
- 카피 verbatim: '연간 업무 추진 계획' / '연간 업무 추진 계획 미리보기' / '연도가 들어갈 위치를 클릭하세요' / '위치 조정' / '취소' / '엑셀 다운로드' / '생성 중...' / '엑셀이 다운로드됐습니다' / '연도 위치 저장됨' / '대상 연도' / '표지 및 일정표 연도가'

## 6. 작업 순서

1. `wc -l cha-bio-safety/src/pages/AnnualPlanPage.tsx` 225 확인
2. AnnualPlanPage.tsx 한 번 Read 로 전체 225 라인 읽기 (이미 PLAN 작성 시점에 완료)
3. Edit / Write 로 in-place 수정 — 순서:
   - **import 추가** (line 5 다음 lucide-react)
   - **§3.3 preview** (line 61~103) — img border 분기 + 캘리브 안내 칩 (yearPos overlay 는 1 byte 변경 금지)
   - **§3.1 데스크톱 상단 바** (line 110~146) — 상단 바 wrapper + 설명 + 위치조정 + 다운로드 (svg → Download)
   - **§3.1 데스크톱 미리보기 wrapper** (line 149~152)
   - **§3.2 모바일 헤더** (line 168~192) — 외곽 + header + back button + 타이틀 + 위치조정
   - **§3.4 모바일 본문** (line 194~221) — 설명 + 다운로드 (svg → Download)
4. `cd cha-bio-safety && npx tsc --noEmit` 실행 → 0 errors 확인
5. `cd cha-bio-safety && npm run build` 실행 → tsc 0 + Vite build PASS 확인
6. §5 negative gate 9건 + §6 positive gate + §캘리브 시그니처 7건 + §비즈 보존 sample + §scope 모두 PASS 확인
7. atomic 1-commit:
   ```
   git add cha-bio-safety/src/pages/AnnualPlanPage.tsx .planning/quick/260522-1hj-redesign-17-annual-plan-tsx-annualplanpa/
   git commit -m "tsx(quick-260522-1hj): redesign/17-annual-plan AnnualPlanPage.tsx v0.1.1 토큰 변환 (W2/W3/W4 sketch verbatim + OQ 5건 LOCKED + 캘리브 시그니처 5건 보존)"
   ```

## 7. 금지 사항

- wrangler 명령 0건 (CLAUDE.local.md 강제)
- npm run deploy 0건
- AnnualPlanPage.tsx 외 파일 수정 0건 (generateAnnualPlan.ts / App.tsx / tailwind.config.js / tokens.css / typography.css / hooks/ / functions/ / templates/ / migrations/ / public/ 모두 무영향)
- `bg-status-safe-bar` / `text-status-accent` 등 status- prefix className 0건 (memory `feedback_tailwind_token_class_pattern`)
- `w-8` / `h-8` 토큰 사용 0건 (=48px 함정, memory `feedback_tailwind_w8_h8_is_48px`)
- `var(--bg|bg2|bg3|bd|bd2|t1|t2|t3|acl|accent)` 잔존 0건
- `linear-gradient(135deg,#1e40af,#3b82f6)` 0건 (OQ #1 LOCKED 완전 폐기)
- 인라인 svg path `M12 5v14m0 0l-4-4m4 4l4-4M4 19h16` / `M15 19l-7-7 7-7` 0건 (OQ #4 LOCKED Lucide 교체 증거)
- fontSize 9·10·11 인라인 0건 (OQ #3 LOCKED 격상, 단 `'min(1.4vw, 16px)'` 의 16 은 예외)
- 이모지 0건 (sketch 본문 + AnnualPlanPage 본문 모두 이모지 없음)
- 비즈 로직 (state/handler/effect/hook/카피/preview 자산 경로) 변경 0건
- **★ 캘리브 좌표 시스템 시그니처 5건 (STORAGE_KEY / FINGER_OFFSET / loadPos / handleImageClick / handleImageTouch / yearPos overlay inline style) 1 byte 변경 0건** — 15-daily-report SW3 precedent
- `text-danger` 토큰 사용 0건 (17-annual-plan 본문에 danger 컬러 사용 케이스 없음)
- `rgba(59,130,246,0.1)` 인라인 사용 0 (OQ #2 LOCKED `bg-accent/10` 토큰 사용)

  </action>
  <verify>
    <automated>
# Path 변수 (실행 디렉토리 = worktree 루트)
F="cha-bio-safety/src/pages/AnnualPlanPage.tsx"

# ─── Negative gate 9건 (W5 §5 verbatim) ───
echo "=== Negative gate ==="
test "$(grep -oP '[\x{1F300}-\x{1FAFF}\x{2600}-\x{26FF}]' "$F" 2>/dev/null | wc -l | tr -d ' ')" = "0" && echo "neg1 (emoji) PASS" || echo "neg1 FAIL"
test "$(grep -c 'linear-gradient' "$F")" = "0" && echo "neg2 (no linear-gradient) PASS" || echo "neg2 FAIL"
test "$(grep -v '^\s*//' "$F" | grep -cE 'fontSize:\s*(9|10|11)[^0-9px]|font-size:\s*(9|10|11)[^0-9px]')" = "0" && echo "neg3 (no 9·10·11 fontSize) PASS" || echo "neg3 FAIL"
test "$(grep -cE '\b(text|bg|border)-status-(safe|fire|warning|danger|caution|accent)' "$F")" = "0" && echo "neg4 (no status- prefix) PASS" || echo "neg4 FAIL"
test "$(grep -cE '\bw-8\b|\bh-8\b' "$F")" = "0" && echo "neg5 (no w-8/h-8) PASS" || echo "neg5 FAIL"
test "$(grep -cE 'var\(--(bg|bg2|bg3|bd|bd2|t1|t2|t3|acl|accent)\)' "$F")" = "0" && echo "neg6 (no old alias) PASS" || echo "neg6 FAIL"
test "$(grep -c 'M12 5v14m0 0l-4-4m4 4l4-4M4 19h16' "$F")" = "0" && echo "neg7 (no Download svg path, OQ #4 Lucide) PASS" || echo "neg7 FAIL"
test "$(grep -c 'M15 19l-7-7 7-7' "$F")" = "0" && echo "neg8 (no ChevronLeft svg path, OQ #4 Lucide) PASS" || echo "neg8 FAIL"
test "$(grep -c 'text-danger' "$F")" = "0" && echo "neg9 (no text-danger token) PASS" || echo "neg9 FAIL"

# ─── Positive gate (W5 §6 verbatim) ───
echo "=== Positive gate ==="
test "$(grep -cE 'bg-surface-(page|raised|sunken)|border-border-(default|strong)|text-text-(primary|secondary|tertiary|on-accent)' "$F")" -ge 3 && echo "pos1 (v0.1.1 tokens ≥3) PASS" || echo "pos1 FAIL"
test "$(grep -c 'bg-surface-raised' "$F")" -ge 1 && echo "pos2 (bg-surface-raised ≥1) PASS" || echo "pos2 FAIL"
test "$(grep -c 'bg-surface-sunken' "$F")" -ge 3 && echo "pos3 (bg-surface-sunken ≥3) PASS" || echo "pos3 FAIL"
test "$(grep -c 'border-border-default' "$F")" -ge 3 && echo "pos4 (border-border-default ≥3) PASS" || echo "pos4 FAIL"
test "$(grep -c 'border-border-strong' "$F")" -ge 2 && echo "pos5 (border-border-strong ≥2) PASS" || echo "pos5 FAIL"
test "$(grep -c 'bg-safe-bar' "$F")" -ge 2 && echo "pos6 (bg-safe-bar ≥2, OQ #1) PASS" || echo "pos6 FAIL"
test "$(grep -c 'text-text-on-accent' "$F")" -ge 2 && echo "pos7 (text-text-on-accent ≥2) PASS" || echo "pos7 FAIL"
test "$(grep -c 'border-accent' "$F")" -ge 2 && echo "pos8 (border-accent ≥2, OQ #2 + #5) PASS" || echo "pos8 FAIL"
test "$(grep -c 'bg-accent/10' "$F")" -ge 2 && echo "pos9 (bg-accent/10 ≥2, OQ #2) PASS" || echo "pos9 FAIL"
test "$(grep -c 'text-accent' "$F")" -ge 2 && echo "pos10 (text-accent ≥2, OQ #2) PASS" || echo "pos10 FAIL"
test "$(grep -c 'w-\[34px\]' "$F")" -ge 1 && echo "pos11 (w-[34px] back button) PASS" || echo "pos11 FAIL"
test "$(grep -c 'h-\[34px\]' "$F")" -ge 1 && echo "pos12 (h-[34px] back button) PASS" || echo "pos12 FAIL"
test "$(grep -c 'ChevronLeft' "$F")" -ge 2 && echo "pos13 (Lucide ChevronLeft import + 사용) PASS" || echo "pos13 FAIL"
test "$(grep -c 'Download' "$F")" -ge 3 && echo "pos14 (Lucide Download import + 사용 2건) PASS" || echo "pos14 FAIL"
test "$(grep -c "from 'lucide-react'" "$F")" -ge 1 && echo "pos15 (lucide-react import) PASS" || echo "pos15 FAIL"
test "$(grep -c 'text-caption' "$F")" -ge 4 && echo "pos16 (text-caption ≥4) PASS" || echo "pos16 FAIL"
test "$(grep -c 'text-label' "$F")" -ge 2 && echo "pos17 (text-label ≥2) PASS" || echo "pos17 FAIL"
test "$(grep -cE '\btext-body\b' "$F")" -ge 2 && echo "pos18 (text-body ≥2) PASS" || echo "pos18 FAIL"
test "$(grep -c 'rounded-sm' "$F")" -ge 4 && echo "pos19 (rounded-sm ≥4) PASS" || echo "pos19 FAIL"
test "$(grep -c 'rounded-md' "$F")" -ge 1 && echo "pos20 (rounded-md ≥1 모바일 다운로드) PASS" || echo "pos20 FAIL"
test "$(grep -c 'leading-none' "$F")" -ge 3 && echo "pos21 (leading-none ≥3) PASS" || echo "pos21 FAIL"
test "$(grep -c 'text-white' "$F")" -ge 1 && echo "pos22 (text-white 캘리브 칩) PASS" || echo "pos22 FAIL"

# ─── ★ 캘리브 좌표 시스템 시그니처 5건 보존 ───
echo "=== Calibration signature lockdown ==="
test "$(grep -c \"STORAGE_KEY = 'annual_plan_year_pos'\" "$F")" -ge 1 && echo "calib1 (STORAGE_KEY) PASS" || echo "calib1 FAIL"
test "$(grep -c 'FINGER_OFFSET = 60' "$F")" -ge 1 && echo "calib2 (FINGER_OFFSET=60 정의) PASS" || echo "calib2 FAIL"
test "$(grep -c 'FINGER_OFFSET' "$F")" -ge 2 && echo "calib3 (FINGER_OFFSET 정의+사용 ≥2) PASS" || echo "calib3 FAIL"
test "$(grep -c 'loadPos' "$F")" -ge 1 && echo "calib4 (loadPos helper) PASS" || echo "calib4 FAIL"
test "$(grep -c 'handleImageClick' "$F")" -ge 1 && echo "calib5 (handleImageClick) PASS" || echo "calib5 FAIL"
test "$(grep -c 'handleImageTouch' "$F")" -ge 1 && echo "calib6 (handleImageTouch) PASS" || echo "calib6 FAIL"
test "$(grep -c "'min(1.4vw, 16px)'" "$F")" -ge 1 && echo "calib7 (min(1.4vw, 16px) overlay fontSize) PASS" || echo "calib7 FAIL"
test "$(grep -c 'Malgun Gothic' "$F")" -ge 1 && echo "calib8 (Malgun Gothic overlay fontFamily) PASS" || echo "calib8 FAIL"
test "$(grep -c '/templates/preview/annual-plan.png' "$F")" -ge 1 && echo "calib9 (preview 자산 경로) PASS" || echo "calib9 FAIL"
test "$(grep -c 'rgba(59,130,246,0.9)' "$F")" -ge 1 && echo "calib10 (캘리브 칩 rgba 인라인, OQ #5) PASS" || echo "calib10 FAIL"

# ─── 비즈 로직 보존 (W5 §10 sampling) ───
echo "=== Biz preserve ==="
test "$(grep -c 'useIsDesktop' "$F")" -ge 1 && echo "biz1 (useIsDesktop) PASS" || echo "biz1 FAIL"
test "$(grep -c 'generateAnnualPlan' "$F")" -ge 1 && echo "biz2 (generateAnnualPlan import) PASS" || echo "biz2 FAIL"
test "$(grep -c 'navigate(-1)' "$F")" -ge 1 && echo "biz3 (navigate(-1)) PASS" || echo "biz3 FAIL"
test "$(grep -c 'setCalibMode' "$F")" -ge 4 && echo "biz4 (setCalibMode 토글 + click + touch) PASS" || echo "biz4 FAIL"
test "$(grep -c 'setYearPos' "$F")" -ge 2 && echo "biz5 (setYearPos click + touch) PASS" || echo "biz5 FAIL"
test "$(grep -c 'setLoading' "$F")" -ge 2 && echo "biz6 (setLoading) PASS" || echo "biz6 FAIL"
test "$(grep -c '연간 업무 추진 계획' "$F")" -ge 2 && echo "biz7 (연간 업무 추진 계획 카피 2건) PASS" || echo "biz7 FAIL"
test "$(grep -c '엑셀 다운로드' "$F")" -ge 2 && echo "biz8 (엑셀 다운로드 모바일+데스크톱) PASS" || echo "biz8 FAIL"
test "$(grep -c '생성 중' "$F")" -ge 2 && echo "biz9 (생성 중 모바일+데스크톱) PASS" || echo "biz9 FAIL"
test "$(grep -c '엑셀이 다운로드됐습니다' "$F")" -ge 1 && echo "biz10 (toast success) PASS" || echo "biz10 FAIL"
test "$(grep -c '연도 위치 저장됨' "$F")" -ge 2 && echo "biz11 (toast 연도 위치 저장 2건) PASS" || echo "biz11 FAIL"
test "$(grep -c '연도가 들어갈 위치를 클릭하세요' "$F")" -ge 1 && echo "biz12 (캘리브 안내 카피) PASS" || echo "biz12 FAIL"
test "$(grep -c '위치 조정' "$F")" -ge 1 && echo "biz13 (위치 조정 카피) PASS" || echo "biz13 FAIL"
test "$(grep -c '취소' "$F")" -ge 1 && echo "biz14 (취소 카피) PASS" || echo "biz14 FAIL"
test "$(grep -c '대상 연도' "$F")" -ge 1 && echo "biz15 (대상 연도 카피) PASS" || echo "biz15 FAIL"
test "$(grep -c '표지 및 일정표 연도가' "$F")" -ge 2 && echo "biz16 (표지 및 일정표 카피 모바일+데스크톱) PASS" || echo "biz16 FAIL"
test "$(grep -c 'nextYear' "$F")" -ge 3 && echo "biz17 (nextYear 정의+overlay+모바일+데스크톱) PASS" || echo "biz17 FAIL"
test "$(grep -c 'imgRef' "$F")" -ge 2 && echo "biz18 (imgRef 정의+사용) PASS" || echo "biz18 FAIL"
test "$(grep -c 'onTouchStart={handleImageTouch}' "$F")" -ge 1 && echo "biz19 (onTouchStart 바인딩) PASS" || echo "biz19 FAIL"

# ─── generateAnnualPlan.ts 0 byte 변경 ───
echo "=== Utils preserve ==="
test "$(git diff HEAD~ HEAD -- cha-bio-safety/src/utils/generateAnnualPlan.ts 2>/dev/null | wc -l | tr -d ' ')" = "0" && echo "utils1 (generateAnnualPlan.ts 0 diff) PASS" || echo "utils1 FAIL"

# ─── Scope (다른 파일 변경 0) ───
echo "=== Scope ==="
test "$(git diff --name-only HEAD~ HEAD 2>/dev/null | grep -vE 'cha-bio-safety/src/pages/AnnualPlanPage\.tsx|^\.planning/quick/260522-1hj-' | wc -l | tr -d ' ')" = "0" && echo "scope (no extra files) PASS" || echo "scope FAIL — files: $(git diff --name-only HEAD~ HEAD 2>/dev/null | grep -vE 'cha-bio-safety/src/pages/AnnualPlanPage\.tsx|^\.planning/quick/260522-1hj-')"

# ─── Line count sanity ───
echo "=== Line count ==="
LC=$(wc -l < "$F" | tr -d ' ')
test "$LC" -ge 195 -a "$LC" -le 270 && echo "lc ($LC lines, 225 ± 30) PASS" || echo "lc FAIL ($LC lines)"

# ─── Build gate ───
echo "=== Build ==="
(cd cha-bio-safety && npm run build 2>&1 | tail -5)
echo "=== AnnualPlanPage chunk size ==="
ls -la cha-bio-safety/dist/assets/AnnualPlanPage-*.js 2>/dev/null || echo "(chunk file 미존재 — Vite 빌드 결과 확인 필요)"
    </automated>
  </verify>
  <done>
- AnnualPlanPage.tsx in-place 수정 완료, 라인 수 225 ± 30
- Negative gate 9건 모두 PASS (이모지·linear-gradient·9·10·11 fontSize·status- prefix·w-8/h-8·var(--alias)·svg path 2종·text-danger 토큰 0건)
- Positive gate 22건 모두 PASS (v0.1.1 토큰 ≥3 / bg-surface-raised|sunken|page / border-border-default|strong / bg-safe-bar ≥2 / text-text-on-accent ≥2 / border-accent ≥2 / bg-accent/10 ≥2 / text-accent ≥2 / w-[34px] h-[34px] ≥1 / ChevronLeft ≥2 / Download ≥3 / lucide-react import ≥1 / text-caption ≥4 / text-label ≥2 / text-body ≥2 / rounded-sm ≥4 / rounded-md ≥1 / leading-none ≥3 / text-white ≥1)
- ★ 캘리브 시그니처 보존 10건 PASS (STORAGE_KEY / FINGER_OFFSET 정의+사용 / loadPos / handleImageClick / handleImageTouch / 'min(1.4vw, 16px)' / Malgun Gothic / annual-plan.png 경로 / rgba(59,130,246,0.9) 인라인)
- 비즈 로직 보존 19건 (useIsDesktop / generateAnnualPlan / navigate(-1) / setCalibMode ≥4 / setYearPos ≥2 / setLoading ≥2 / 카피 11종 / nextYear ≥3 / imgRef ≥2 / onTouchStart) 모두 PASS
- generateAnnualPlan.ts 0 byte 변경 (utils1 PASS)
- Scope: cha-bio-safety/src/pages/AnnualPlanPage.tsx + .planning/quick/260522-1hj-*/ 외 파일 변경 0건
- `npm run build` exit 0 (tsc 0 errors + Vite PASS), AnnualPlanPage chunk size 보고
- Atomic 1-commit: `tsx(quick-260522-1hj): redesign/17-annual-plan AnnualPlanPage.tsx v0.1.1 토큰 변환 (W2/W3/W4 sketch verbatim + OQ 5건 LOCKED + 캘리브 시그니처 5건 보존)`
  </done>
</task>

</tasks>

<verification>
## 전체 PLAN 통과 조건

1. **Negative gate 9건** — W5 §5 verbatim (이모지·linear-gradient·9·10·11·status-·w-8/h-8·var(--alias)·svg path 2종·text-danger)
2. **Positive gate 22건** — W5 §6 verbatim (v0.1.1 토큰 + Lucide + 토큰 className 카운트 + 폰트 토큰)
3. **★ 캘리브 시그니처 10건** — STORAGE_KEY / FINGER_OFFSET (정의+사용) / loadPos / handleImageClick / handleImageTouch / 'min(1.4vw, 16px)' / Malgun Gothic / annual-plan.png 경로 / rgba(59,130,246,0.9)
4. **비즈 로직 0 diff** — W5 §10 19+ 항목 (verify 의 biz1~biz19 sampling)
5. **generateAnnualPlan.ts 0 byte 변경** — git diff 0 lines (utils1)
6. **Scope** — 단일 파일 변경 (cha-bio-safety/src/pages/AnnualPlanPage.tsx) + PLAN.md/SUMMARY.md
7. **Build** — `npm run build` exit 0
8. **Atomic 1-commit** — 단일 commit 안에 모든 변경 포함

## 검수 시각 (cbc7119-preview 자동 배포 후)

- 모바일 viewport (375×812 등 iPhone 13 mini): 모바일 헤더 분기 (back button + ChevronLeft + 14→16 타이틀) + 모바일 다운로드 (rounded-md + Download size 16 + text-body) 확인
- 데스크톱 viewport (≥768): 데스크톱 상단 바 (설명 + 위치조정 + 다운로드 Download size 15 + text-label) 확인
- 다운로드 버튼 default = `bg-safe-bar` (solid 파랑) + 글자 `text-text-on-accent` (흰) — 그라데이션 사용 0건 확인
- 다운로드 버튼 loading 시 `bg-surface-sunken text-text-tertiary cursor-not-allowed` 회색 disabled 표시
- 위치조정 토글 평시 = `bg-surface-sunken border-border-strong text-text-secondary` 톤다운
- 위치조정 토글 active (calibMode true) = `border-accent bg-accent/10 text-accent` 파랑 강조
- preview img border 평시 = `border border-border-default` / 캘리브 모드 = `border-2 border-accent` 파랑 강조
- 캘리브 안내 칩 = `text-white text-caption font-bold` + 인라인 `rgba(59,130,246,0.9)` 파랑 배경 (alpha 0.9)
- 캘리브 모드 진입 후 preview 클릭/터치 시 yearPos 좌표 저장 → 연도 오버레이가 클릭 위치에 정확히 표시 (`min(1.4vw, 16px)` 사이즈 + Malgun Gothic 폰트)
- 모바일 터치 시 FINGER_OFFSET 60 px y 보정 정상 동작 (손가락 가리는 영역 회피)
- 새로고침 후 yearPos 좌표 localStorage 복원 (STORAGE_KEY = 'annual_plan_year_pos')

## 사용자 컨펌 후 다음 단계

- `main` 머지 (사용자 명시 컨펌 후, memory `feedback_deploy_test`)
- GitHub Actions → cbc7119-preview 자동 배포
- 직원 도메인 (cbc7119) 배포는 별도 worktree (20260328) 담당 (이 워크트리는 절대 다루지 않음, CLAUDE.local.md)
- `project_redesign_17_annual_plan_status` 메모리 박제 (신규)
</verification>

<success_criteria>
- [ ] AnnualPlanPage.tsx 225 ± 30 lines, in-place 수정
- [ ] Negative gate 9건 모두 0 (이모지/linear-gradient/9·10·11 fontSize/status- prefix/w-8·h-8/var(--alias)/svg path 2종/text-danger 토큰)
- [ ] Positive gate 22건 모두 임계치 이상
- [ ] ★ 캘리브 시그니처 10건 모두 PASS (STORAGE_KEY / FINGER_OFFSET / loadPos / handleImageClick / handleImageTouch / 'min(1.4vw, 16px)' / Malgun Gothic / annual-plan.png / rgba(59,130,246,0.9))
- [ ] 비즈 로직 보존 19건 (sampling) 모두 PASS
- [ ] generateAnnualPlan.ts 0 byte 변경
- [ ] 다른 파일 변경 0건
- [ ] `npm run build` exit 0
- [ ] Atomic 1-commit
- [ ] OQ #1: bg-safe-bar ≥2 + linear-gradient 0
- [ ] OQ #2: border-accent ≥2 + bg-accent/10 ≥2 + text-accent ≥2 (위치조정 + preview)
- [ ] OQ #3: fontSize 9·10·11 인라인 0 + text-caption ≥4 + text-label ≥2 + text-body ≥2
- [ ] OQ #4: ChevronLeft ≥2 + Download ≥3 + lucide-react import ≥1 + svg path 인라인 0
- [ ] OQ #5: border-accent (preview) + rgba(59,130,246,0.9) 인라인 1+
</success_criteria>

<output>
After completion, create `.planning/quick/260522-1hj-redesign-17-annual-plan-tsx-annualplanpa/260522-1hj-SUMMARY.md` documenting:

- 변환 전후 AnnualPlanPage.tsx wc -l
- 적용된 v0.1.1 토큰 className 목록 (grep 카운트)
- OQ #1~#5 LOCKED 각 적용 결과 확인
- ★ 캘리브 시그니처 5건 (STORAGE_KEY / FINGER_OFFSET / loadPos / handleImageClick / handleImageTouch / yearPos overlay) 1 byte 변경 0 검증 결과 명시 (15-daily-report SW3 precedent mirror)
- Negative gate 9건 + Positive gate 22건 + 캘리브 시그니처 10건 + biz 19건 + utils 1건 PASS 결과 (verify automated 출력 첨부)
- `npm run build` 결과 (chunk size 포함)
- atomic commit hash + main 머지 대기 상태 명시
- 다음 단계 (사용자 컨펌 → main 머지 → cbc7119-preview 자동 배포 → `project_redesign_17_annual_plan_status` 메모리 박제)
</output>
</content>
