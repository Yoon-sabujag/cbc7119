---
phase: quick-260520-gq7
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cha-bio-safety/docs/redesign-context/14-reports/sketch-wave-4-desktop-toolbar.html
autonomous: true
requirements:
  - QUICK-260520-gq7-W4
tags:
  - redesign/14-reports
  - sketch
  - desktop
  - toolbar

must_haves:
  truths:
    - "단일 산출물 sketch-wave-4-desktop-toolbar.html 1개 파일이 docs/redesign-context/14-reports/ 평면 sibling 위치에 생성된다"
    - "다크/라이트 × 평상시/일괄-loading 4 frame matrix 가 1280px 데스크톱 폭으로 시연된다"
    - "각 frame 안에 ReportsPage.tsx line 181~243 의 상단 바 (연도 라벨 + select + 일괄 다운로드 + spacer + 선택 타이틀 + 개별 다운로드) markup 이 W2/W3 CSS class 패턴 inherit 으로 변환되어 있다"
    - "일괄 다운로드 버튼 = var(--status-safe-bar) solid (그라데이션 0건, W1 OQ #3 LOCKED 적용)"
    - "개별 다운로드 버튼 = var(--surface-sunken) + border var(--border-default) (보조 액션 위계, W4 OQ #2 default)"
    - "상단 바 아래 placeholder = 좌측 sidelist 260px + 우측 ExcelPreview 영역 dashed border + 'W5 책임' / 'W6 책임' 라벨"
    - "이모지 0건 / linear-gradient 0건 / status- prefix Tailwind 0건 / 9·10·11px fontSize 0건 (negative gate 모두 통과)"
    - "ReportsPage.tsx / ExcelPreview.tsx / App.tsx / 다른 페이지 코드 수정 0건 (sketch wave 는 docs 만)"
    - "wrangler 명령 0건 / npm run deploy 0건 (CLAUDE.local.md 룰 + 본 워크트리 deny)"
  artifacts:
    - path: "cha-bio-safety/docs/redesign-context/14-reports/sketch-wave-4-desktop-toolbar.html"
      provides: "데스크톱 1280px 상단 바 4 frame matrix sketch"
      contains: "toolbar / toolbar-year-label / toolbar-select / toolbar-batch-btn / toolbar-spacer / toolbar-selected-title / toolbar-individual-btn"
  key_links:
    - from: "sketch-wave-4-desktop-toolbar.html"
      to: "wave-1-index.md §3 토큰 + §7 OQ LOCKED 5건"
      via: "tokens.css line 16~119 verbatim 임베드 + W1 OQ #1/#3 적용 코멘트"
      pattern: "OQ #3.*LOCKED"
    - from: "sketch-wave-4-desktop-toolbar.html"
      to: "ReportsPage.tsx line 181~243"
      via: "source verbatim 매핑 (속성/값 1:1)"
      pattern: "line 181~243"
    - from: "sketch-wave-4-desktop-toolbar.html"
      to: "sketch-wave-2/3 (CSS class 패턴 inherit)"
      via: "frame-shell + frame-desktop + variant-label 패턴 mirror"
      pattern: "frame-(desktop|label|shell)"
---

<objective>
redesign/14-reports W4 — DesktopReportsPage 상단 바 (연도 셀렉트 + 일괄 다운로드 + 선택 타이틀 + 개별 다운로드) 의 데스크톱 1280px sketch HTML 1개를 작성한다.

Purpose:
- W1 (wave-1-index.md) LOCKED 5건 + W2/W3 sketch 패턴을 데스크톱 toolbar 영역에 적용해 사용자 컨펌을 받는 단계.
- W5 (좌측 sidelist) / W6 (ExcelPreview wrapper) 진입 전 데스크톱 chrome 상단의 시각 디자인을 단일 시안으로 박제.
- TSX 변환 wave (W7) 가 verbatim 인용할 CSS class 패턴 (`toolbar-*`) 을 확정한다.

Output:
- `cha-bio-safety/docs/redesign-context/14-reports/sketch-wave-4-desktop-toolbar.html` 단일 파일.
- 다크/라이트 × 평상시/일괄-loading 4 frame matrix.
- 좌측 sidelist 260px + 우측 ExcelPreview 영역의 dashed placeholder ("W5 책임" / "W6 책임").
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.local.md
@cha-bio-safety/docs/redesign-context/14-reports/wave-1-index.md
@cha-bio-safety/docs/redesign-context/14-reports/sketch-wave-2-mobile-header-card.html
@cha-bio-safety/docs/redesign-context/14-reports/sketch-wave-3-mobile-card-list.html
@cha-bio-safety/docs/redesign-context/14-reports/tokens.css
@cha-bio-safety/src/pages/ReportsPage.tsx

<interfaces>
<!-- ReportsPage.tsx 의 W4 책임 영역 (line 181~243) verbatim — sketch 변환 1:1 매핑 source -->

DesktopReportsPage 상단 바 outer (line 181~189):
```tsx
<div style={{
  padding: '8px 16px',
  borderBottom: '1px solid var(--bd)',
  display: 'flex',
  alignItems: 'center',
  gap: 12,
  flexShrink: 0,
  background: 'var(--bg2)',
}}>
```

연도 라벨 + select (line 190~195):
```tsx
<label style={{ fontSize: 12, color: 'var(--t2)' }}>연도</label>
<select value={year} onChange={e => setYear(Number(e.target.value))} style={SELECT_STYLE}>
  {Array.from({ length: CURRENT_YEAR - MIN_YEAR + 1 }, (_, i) => CURRENT_YEAR - i).map(y => (
    <option key={y} value={y}>{y}년</option>
  ))}
</select>
```

SELECT_STYLE (line 306~313 추정):
```tsx
const SELECT_STYLE: React.CSSProperties = {
  background: 'var(--bg3)',
  color: 'var(--t1)',
  border: '1px solid var(--bd2)',
  borderRadius: 4,
  padding: '4px 8px',
  fontSize: 12,
}
```

일괄 다운로드 버튼 (line 196~216):
```tsx
<button
  onClick={handleDownloadAll}
  disabled={!!zipLoading}
  style={{
    height: 32,
    padding: '0 14px',
    background: zipLoading ? 'var(--bg3)' : 'linear-gradient(135deg,#1d4ed8,#2563eb)',
    border: 'none',
    borderRadius: 6,
    color: zipLoading ? 'var(--t3)' : '#fff',
    fontSize: 12,
    fontWeight: 700,
    cursor: zipLoading ? 'default' : 'pointer',
    display: 'flex',
    alignItems: 'center',
    gap: 6,
  }}
>
  <Download size={13} />
  {zipLoading ?? '일괄 다운로드'}
</button>
```

spacer + 선택 타이틀 (line 218~220):
```tsx
<div style={{ flex: 1 }} />
<span style={{ fontSize: 14, fontWeight: 700, color: 'var(--t1)' }}>{selectedCard?.title}</span>
```

개별 다운로드 버튼 (line 221~243):
```tsx
<button
  onClick={() => handleDownload(selectedType)}
  disabled={loading === selectedType}
  style={{
    height: 32,
    padding: '0 14px',
    background: 'var(--bg3)',
    border: '1px solid var(--bd2)',
    borderRadius: 6,
    color: 'var(--t1)',
    fontSize: 12,
    fontWeight: 700,
    cursor: loading === selectedType ? 'default' : 'pointer',
    display: 'flex',
    alignItems: 'center',
    gap: 6,
    opacity: loading === selectedType ? 0.5 : 1,
  }}
>
  <Download size={13} />
  {loading === selectedType ? '생성 중...' : '엑셀 다운로드'}
</button>
```

REPORT_CARDS[0] (선택 타이틀 verbatim source, line 13):
```tsx
{ type: 'div-early',  title: '월초 유수검지 장치 점검표', sub: 'DIV · 34개소' }
```

DESKTOP_SECTIONS verbatim (line 142~147) — placeholder 영역 라벨 정확도 참고:
```tsx
const DESKTOP_SECTIONS = [
  { label: '유수검지 장치', types: ['div-early', 'div-late'] },
  { label: '소화전 · 가스 · 비상콘센트', types: ['소화전', '청정소화약제', '비상콘센트'] },
  { label: '연간 점검일지', types: ['피난방화', '방화셔터', '제연', '자탐'] },
  { label: '소방펌프', types: ['소방펌프'] },
]
```

zipLoading 카피 변종 (downloadAllAsZip 의 setZipLoading 호출 verbatim — W4 OQ default 시연용):
- '준비 중...' (line 167 verbatim 초기값)
- '생성 중... (3/10)' (3번째 보고서 생성 중 시연용 — source patterns/inspections/...zip generation typical)

토큰 alias (tokens.css line 178~197):
- `--bg = --surface-page`, `--bg2 = --surface-raised`, `--bg3 = --surface-sunken`
- `--bd = --border-default`, `--bd2 = --border-strong`
- `--t1 = --text-primary`, `--t2 = --text-secondary`, `--t3 = --text-tertiary`
- `--acl = --accent`
- `--safe = --status-safe-bar` (W1 OQ #3 LOCKED 적용 target)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: sketch-wave-4-desktop-toolbar.html 생성 — 다크/라이트 × 평상시/일괄-loading 4 frame matrix</name>
  <files>cha-bio-safety/docs/redesign-context/14-reports/sketch-wave-4-desktop-toolbar.html</files>
  <action>
**산출 위치:** `cha-bio-safety/docs/redesign-context/14-reports/sketch-wave-4-desktop-toolbar.html` (평면 sibling, 13-schedule/14-reports W2/W3 와 동일 패턴, sketch/ 서브폴더 만들지 않음).

**파일 구조 (W2/W3 mirror):**

1. 파일 머리 comment block — wave 메타 (작성일 2026-05-20 / quick 260520-gq7 / 범위 / out of scope / 참조 / W1 LOCKED 5건 적용 매트릭스 / W5 진입 전 OQ 3건 default / 메모리 룰 박제 ≥7건)
2. `<head>` — tokens.css line 16~119 verbatim 임베드 (다크 + 라이트), tokens.css line 122~146 spacing primitive + media (min-width:768px) override, line 165~172 radius, typography.css helper (text-caption / text-label / text-body-sm / text-body / text-title / leading-none / dot-meta), tailwind.config script (W2/W3 verbatim 패턴)
3. `<body class="page-bg-dark min-h-screen p-6">` (다크 페이지 배경)
4. `<header>` — sketch 캡션 (h1 + 설명 p)
5. Frame 1 — 다크 평상시 (1280px)
6. Frame 2 — 다크 일괄 loading '생성 중... (3/10)' (1280px)
7. Frame 3 — 라이트 평상시 (1280px)
8. Frame 4 — 라이트 일괄 loading '생성 중... (3/10)' (1280px)
9. `<section>` Notes block — W4 검토 포인트 + W5 진입 전 OQ 3건 default

**Frame 외곽 (.frame-desktop) 패턴 — 13-schedule sketch-wave-3.html line 402~411 verbatim mirror:**
```css
.frame-desktop {
  width: 100%;
  max-width: 1280px;
  margin: 0 auto;
  background: var(--surface-page);
  border: 1px solid var(--border-default);
  border-radius: 18px;
  overflow: hidden;
  color: var(--text-primary);
}
```

**Frame 내부 markup 순서 (각 frame):**
a. App.tsx GlobalHeader placeholder (h-12, 회색 톤, 다크/라이트 무관) — W2 의 .global-header-placeholder mirror
b. 상단 바 `<div class="toolbar">` (W4 책임 핵심) — 아래 CSS class 정의 그대로 사용
c. 하단 placeholder `<div class="below-toolbar">` — display:flex 좌측 sidelist 260px + 우측 ExcelPreview 영역

**CSS class 정의 (W4 verbatim — task_scope 의 source verbatim 매핑 그대로):**

```css
.toolbar {
  display: flex;
  align-items: center;
  gap: 12px;
  padding: 8px 16px;
  border-bottom: 1px solid var(--border-default);   /* source 'var(--bd)' */
  background: var(--surface-raised);                /* source 'var(--bg2)' */
  flex-shrink: 0;
}

.toolbar-year-label {
  font-size: 12px;
  color: var(--text-secondary);                      /* source 'var(--t2)' */
  line-height: 1;                                    /* 메모리 룰 7: 작은 컨테이너 leading-none */
}

.toolbar-select {
  background: var(--surface-sunken);                 /* SELECT_STYLE 'var(--bg3)' */
  color: var(--text-primary);                        /* SELECT_STYLE 'var(--t1)' */
  border: 1px solid var(--border-default);           /* SELECT_STYLE 'var(--bd2)' — source bd2 였으나 14-reports 디자인 일관 위해 default 채용. 또는 strong 유지 가능 — 결정: source verbatim 보존 = border-strong */
  border-radius: 4px;
  padding: 4px 8px;
  font-size: 12px;
  /* 주: SELECT_STYLE source 'var(--bd2)' = border-strong. 본 sketch 는 source verbatim 보존 */
}

/* 일괄 다운로드 — W1 OQ #3 LOCKED: 그라데이션 폐기 → bg-safe-bar solid */
.toolbar-batch-btn {
  height: 32px;
  padding: 0 14px;
  background: var(--status-safe-bar);                /* W1 OQ #3 LOCKED */
  border: 0;
  border-radius: 6px;
  color: var(--text-on-accent);                      /* #ffffff */
  font-size: 12px;
  font-weight: 700;
  cursor: pointer;
  display: flex;
  align-items: center;
  gap: 6px;
}

.toolbar-batch-btn--loading {
  background: var(--surface-sunken);                 /* source 'var(--bg3)' */
  color: var(--text-tertiary);                       /* source 'var(--t3)' */
  cursor: default;
}

.toolbar-spacer { flex: 1; }

.toolbar-selected-title {
  font-size: 14px;
  font-weight: 700;
  color: var(--text-primary);                        /* source 'var(--t1)' */
}

/* 개별 다운로드 — W4 OQ #2 default: 보조 액션 위계 = surface-sunken + border */
.toolbar-individual-btn {
  height: 32px;
  padding: 0 14px;
  background: var(--surface-sunken);                 /* source 'var(--bg3)' */
  border: 1px solid var(--border-strong);            /* source 'var(--bd2)' */
  border-radius: 6px;
  color: var(--text-primary);                        /* source 'var(--t1)' */
  font-size: 12px;
  font-weight: 700;
  cursor: pointer;
  display: flex;
  align-items: center;
  gap: 6px;
}

.toolbar-individual-btn--loading {
  opacity: 0.5;
  cursor: default;
}

/* 하단 placeholder — W5/W6 책임 영역 */
.below-toolbar {
  display: flex;
  height: 480px;                                     /* 시연용 고정 — frame 내부 비율 보여주기 */
}

.placeholder-sidelist {
  width: 260px;
  flex-shrink: 0;
  border-right: 1px dashed var(--border-default);
  background: var(--surface-raised);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--text-tertiary);
  font-family: 'JetBrains Mono', monospace;
  font-size: 12px;
  letter-spacing: 0.05em;
  text-align: center;
  padding: 16px;
}

.placeholder-preview {
  flex: 1;
  background: var(--surface-page);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--text-tertiary);
  font-family: 'JetBrains Mono', monospace;
  font-size: 12px;
  letter-spacing: 0.05em;
  border: 1px dashed var(--border-default);          /* dashed callout */
  margin: 16px;
}
```

**lucide Download SVG 인라인 (W2 W3 와 동일 패턴, source `<Download size={13} />` mirror — size 14 로 W2 와 일관 유지 가능, 또는 source verbatim 13):**
- 본 sketch 는 **size 13 (source verbatim)** 사용. 추후 W7 변환 wave 가 `<Download size={13} />` 로 1:1 매핑.
- SVG path: `<path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/><polyline points="7 10 12 15 17 10"/><line x1="12" y1="15" x2="12" y2="3"/>` (W2/W3 SVG path verbatim)

**각 frame 의 toolbar markup (Frame 1 다크 평상시 기준, Frame 2~4 는 state/theme 변경):**

```html
<div class="toolbar">
  <label class="toolbar-year-label">연도</label>
  <select class="toolbar-select">
    <option>2026년</option>
    <option>2025년</option>
    <option>2024년</option>
    <option>2023년</option>
  </select>
  <button class="toolbar-batch-btn" aria-label="일괄 다운로드">
    <svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
      <path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/>
      <polyline points="7 10 12 15 17 10"/>
      <line x1="12" y1="15" x2="12" y2="3"/>
    </svg>
    일괄 다운로드
  </button>
  <div class="toolbar-spacer"></div>
  <span class="toolbar-selected-title">월초 유수검지 장치 점검표</span>
  <button class="toolbar-individual-btn" aria-label="엑셀 다운로드">
    <svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
      <path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/>
      <polyline points="7 10 12 15 17 10"/>
      <line x1="12" y1="15" x2="12" y2="3"/>
    </svg>
    엑셀 다운로드
  </button>
</div>
<div class="below-toolbar">
  <div class="placeholder-sidelist">
    W5 책임<br/>
    좌측 sidelist 260px<br/>
    (4 섹션 × DESKTOP_SECTIONS)
  </div>
  <div class="placeholder-preview">
    W6 책임<br/>
    ExcelPreview wrapper<br/>
    (reportType=div-early year=2026 month=현재)
  </div>
</div>
```

**Frame 2 (다크 일괄 loading) 의 일괄 버튼:**
- class 추가: `toolbar-batch-btn toolbar-batch-btn--loading`
- disabled 속성
- SVG 유지 (source line 214: lucide Download 항상 표시. `{zipLoading ?? '일괄 다운로드'}` 는 텍스트만 분기)
- 텍스트: `생성 중... (3/10)` (downloadAllAsZip 의 setZipLoading 갱신 패턴 시연)
- 개별 다운로드 버튼은 평상시 그대로 (개별과 일괄은 독립 state)

**Frame 3 (라이트 평상시):**
- 외곽 frame `<div data-theme="light" class="frame-desktop">` (다크 frame markup 1:1 mirror, data-theme 만 차이)
- 토큰 자동 매핑: status-safe-bar = #15803d (라이트), surface-sunken = #ebeef1, 등

**Frame 4 (라이트 일괄 loading):** Frame 2 markup 1:1 mirror + data-theme="light"

**메모리 룰 unique ≥7건 — 파일 머리 comment block 안 인용:**
1. feedback_design_sketch_first.md
2. feedback_sketch_realistic_data.md
3. feedback_planner_prompt_sketch_verbatim.md
4. feedback_tailwind_token_class_pattern.md
5. feedback_tailwind_w8_h8_is_48px.md
6. feedback_tsx_wave_emoji_dot_gap.md
7. feedback_text_caption_leading_none.md
8. feedback_avoid_premature_confirmation.md (W3 패턴 mirror)
9. feedback_cbc7119_design_never_wrangler.md (워크트리 룰 명시 — wrangler 0 / npm run deploy 0)
10. feedback_tsx_wave_stat_card_drift.md (Stat Card 14-reports 미적용 명시)

**W5 진입 전 OQ 3건 default (Notes block 안):**
- OQ #1: 일괄 다운로드 클릭 시 confirm 모달 vs 즉시 시작 — **default: 즉시 시작** (source `handleDownloadAll` 는 confirm 없음, 로딩 라벨로 진행 알림. confirm 추가 = 비즈 로직 변경 위험)
- OQ #2: 개별 다운로드 버튼 색 — **default: 회색 (bg-surface-sunken)** 유지. 일괄과 위계 구분. safe-bar 동일색은 두 버튼 평준화 위험
- OQ #3: 일괄 다운로드 후 zip 완료 시 toast 알림 — **default: toast 0** (source verbatim, browser native a.click() 만). 추후 UX 개선 검토

**자체 verify (§6 verify gate 15건) — Task 1 완료 직후 본 task 내부 grep 으로 통과 확인 (Verify 단계에서 실행):**

Negative (모두 0):
1. ⬇ 이모지 0 — `grep -c '⬇' sketch-wave-4-desktop-toolbar.html` == 0
2. linear-gradient 0 — `grep -c 'linear-gradient' sketch-wave-4-desktop-toolbar.html` == 0
3. status- prefix Tailwind 0 (CSS 변수 var(--status-*) 는 OK, Tailwind class `text-status-*` / `bg-status-*` 만 금지) — `grep -v '^#' sketch-wave-4-desktop-toolbar.html | grep -cE '(text-status-|bg-status-)'` == 0
4. 가운뎃점 ' · ' 텍스트 본문 0 — 본 wave 는 카드 sub 없음 자연 0건. comment 안 DESKTOP_SECTIONS 설명에 ' · ' 가 나올 수 있으니 frame markup 영역에 한해 확인 (frame 내부 텍스트에 0)
5. w-8 h-8 class 본문 0 — `grep -cE '(class="[^"]*w-8 h-8|class="[^"]*h-8 w-8)' sketch-wave-4-desktop-toolbar.html` == 0
6. 9·10·11px fontSize 0 (font-size:9px/10px/11px) — `grep -cE 'font-size:\s*(9|10|11)px' sketch-wave-4-desktop-toolbar.html` == 0
7. 📄 ✅ ❌ ⚠️ 본문 0 — `grep -cE '(📄|✅|❌|⚠️)' sketch-wave-4-desktop-toolbar.html` == 0

Positive:
8. Download lucide SVG ≥4 — `grep -c 'polyline points="7 10 12 15 17 10"' sketch-wave-4-desktop-toolbar.html` ≥ 4 (4 frame × 2 button = 8 가능, ≥4 통과)
9. var(--status-safe-bar) 일괄 ≥2 — `grep -c 'var(--status-safe-bar)' sketch-wave-4-desktop-toolbar.html` ≥ 2 (CSS 정의 1 + 사용 여러 frame)
10. var(--surface-sunken) 개별 ≥2 — `grep -c 'var(--surface-sunken)' sketch-wave-4-desktop-toolbar.html` ≥ 2
11. dual frame (다크 / 라이트) ≥2 — `grep -cE 'data-theme="(dark|light)"' sketch-wave-4-desktop-toolbar.html` ≥ 4 (`:root,` 정의 + frame 4개)
12. 메모리 룰 unique feedback_* ≥7 — `grep -oE 'feedback_[a-z_]+\.md' sketch-wave-4-desktop-toolbar.html | sort -u | wc -l` ≥ 7
13. selectedCard?.title verbatim '월초 유수검지 장치 점검표' ≥2 — `grep -c '월초 유수검지 장치 점검표' sketch-wave-4-desktop-toolbar.html` ≥ 2
14. '일괄 다운로드' 라벨 ≥4 (또는 '생성 중... (3/10)' loading variant 도 포함) — `grep -cE '(일괄 다운로드|생성 중\.\.\. \(3/10\))' sketch-wave-4-desktop-toolbar.html` ≥ 4
15. '엑셀 다운로드' 라벨 ≥2 — `grep -c '엑셀 다운로드' sketch-wave-4-desktop-toolbar.html` ≥ 2
16. ExcelPreview placeholder (W5/W6 라벨) ≥2 — `grep -cE '(W5 책임|W6 책임)' sketch-wave-4-desktop-toolbar.html` ≥ 2

**§7 negative scope (절대 금지):**
- 좌측 sidelist 4 섹션 cards 실제 렌더링 (W5 책임) — placeholder dashed callout 만
- 우측 ExcelPreview 본체 (W6 책임) — placeholder dashed callout 만
- 모바일 frame 미포함 (W2/W3 책임)
- ReportsPage.tsx / 다른 페이지 코드 수정 0
- App.tsx 수정 0
- wrangler 명령 0건 (CLAUDE.local.md 룰 + memory feedback_cbc7119_design_never_wrangler)
- `npm run deploy` 0건
  </action>
  <verify>
<automated>cd /Users/jykevin/Documents/cbc7119-design && F="cha-bio-safety/docs/redesign-context/14-reports/sketch-wave-4-desktop-toolbar.html" && test -f "$F" && echo "exists OK" && echo "--- negative gates (must be 0) ---" && echo "1. ⬇ emoji: $(grep -c '⬇' "$F")" && echo "2. linear-gradient: $(grep -c 'linear-gradient' "$F")" && echo "3. status- prefix Tailwind: $(grep -v '^#' "$F" | grep -cE '(text-status-|bg-status-)')" && echo "5. w-8 h-8: $(grep -cE '(class="[^"]*w-8 h-8|class="[^"]*h-8 w-8)' "$F")" && echo "6. 9/10/11px: $(grep -cE 'font-size:\s*(9|10|11)px' "$F")" && echo "7. 📄✅❌⚠️: $(grep -cE '(📄|✅|❌|⚠️)' "$F")" && echo "--- positive gates ---" && echo "8. lucide Download polyline >=4: $(grep -c 'polyline points="7 10 12 15 17 10"' "$F")" && echo "9. var(--status-safe-bar) >=2: $(grep -c 'var(--status-safe-bar)' "$F")" && echo "10. var(--surface-sunken) >=2: $(grep -c 'var(--surface-sunken)' "$F")" && echo "11. data-theme dark/light >=4: $(grep -cE 'data-theme=\"(dark|light)\"' "$F")" && echo "12. feedback_* unique >=7: $(grep -oE 'feedback_[a-z_0-9]+\.md' "$F" | sort -u | wc -l | tr -d ' ')" && echo "13. 월초 유수검지 장치 점검표 >=2: $(grep -c '월초 유수검지 장치 점검표' "$F")" && echo "14. 일괄 다운로드 or 생성 중... (3/10) >=4: $(grep -cE '(일괄 다운로드|생성 중\.\.\. \(3/10\))' "$F")" && echo "15. 엑셀 다운로드 >=2: $(grep -c '엑셀 다운로드' "$F")" && echo "16. W5/W6 책임 placeholder >=2: $(grep -cE '(W5 책임|W6 책임)' "$F")" && echo "--- src/ untouched ---" && git diff --name-only -- cha-bio-safety/src cha-bio-safety/functions migrations | wc -l | xargs -I{} echo "src changes (must be 0): {}"</automated>
  </verify>
  <done>
- `cha-bio-safety/docs/redesign-context/14-reports/sketch-wave-4-desktop-toolbar.html` 1개 파일 존재
- 4 frame matrix (다크 평상시 / 다크 일괄 loading / 라이트 평상시 / 라이트 일괄 loading) 모두 1280px 데스크톱 폭
- 각 frame 안 toolbar (연도 라벨 + select + 일괄 + spacer + 선택 타이틀 + 개별) + below-toolbar placeholder (sidelist 260px + ExcelPreview)
- §6 verify gate 15건 + 추가 negative gate 모두 통과 (위 Verify 명령 결과 7건 negative = 0 / 9건 positive ≥ 임계값)
- cha-bio-safety/src / functions / migrations 변경 0건 (git diff 0 lines)
- 본 task 완료 후 SUMMARY.md 작성 + atomic 1-commit
  </done>
</task>

</tasks>

<verification>
**Plan 단위 종합 verify (executor 가 atomic commit 직전 실행):**

```bash
F="cha-bio-safety/docs/redesign-context/14-reports/sketch-wave-4-desktop-toolbar.html"

# 1. 파일 존재 + 다른 영역 무변경
test -f "$F" || exit 1
git diff --name-only -- cha-bio-safety/src cha-bio-safety/functions migrations scripts | head -1
# expect: (empty output)

# 2. 16 verify gate (Task 1 verify 명령 그대로)
# (위 Verify 단계 명령 사용 — 모든 gate PASS 확인)

# 3. 다른 14-reports sketch 파일 무변경
git diff --name-only -- cha-bio-safety/docs/redesign-context/14-reports/sketch-wave-2-mobile-header-card.html cha-bio-safety/docs/redesign-context/14-reports/sketch-wave-3-mobile-card-list.html cha-bio-safety/docs/redesign-context/14-reports/wave-1-index.md
# expect: (empty output)

# 4. wrangler / deploy 사용 0건 (당일 명령 이력 확인 — manually skip if 이력 추적 어려움)
```

**Atomic 1-commit (스코프 컨펌 후):**
```bash
git add cha-bio-safety/docs/redesign-context/14-reports/sketch-wave-4-desktop-toolbar.html \
        .planning/quick/260520-gq7-redesign-14-reports-sketch-wave-4/260520-gq7-PLAN.md \
        .planning/quick/260520-gq7-redesign-14-reports-sketch-wave-4/260520-gq7-SUMMARY.md
git commit -m "docs(14-reports): sketch wave 4 — 데스크톱 상단 바 (연도 + 일괄 + 선택 + 개별, 다크/라이트 1280px)"
```
</verification>

<success_criteria>
- 단일 파일 `sketch-wave-4-desktop-toolbar.html` 작성 완료 (평면 sibling 위치)
- 4 frame matrix (다크/라이트 × 평상시/loading) 모두 1280px frame-desktop 패턴
- 일괄 다운로드 = `var(--status-safe-bar)` solid (W1 OQ #3 LOCKED 적용, linear-gradient 0건)
- 개별 다운로드 = `var(--surface-sunken)` + border (보조 액션 위계 구분)
- 좌측 sidelist 260px + 우측 ExcelPreview = dashed placeholder + "W5 책임" / "W6 책임" 라벨
- §6 verify gate 15건 + negative gate 7건 모두 통과
- 메모리 룰 unique ≥7건 + 워크트리 룰 (wrangler 0 / npm run deploy 0) inline 박제
- ReportsPage.tsx / ExcelPreview.tsx / App.tsx / src/ / functions/ / migrations/ 변경 0건
- SUMMARY.md 에 frame matrix 4건 / W2/W3 CSS class inherit 매핑 / verify gate ≥15건 / W5 OQ 3건 default 정리
- atomic 1-commit (sketch + PLAN + SUMMARY)
</success_criteria>

<output>
Task 1 완료 후:
- 작성: `.planning/quick/260520-gq7-redesign-14-reports-sketch-wave-4/260520-gq7-SUMMARY.md`
- SUMMARY 안 필수 섹션:
  1. 작성 산출물 (file path + 라인 수)
  2. Frame matrix 4건 (다크 평상시 / 다크 일괄 loading / 라이트 평상시 / 라이트 일괄 loading)
  3. W2/W3 CSS class inherit 매핑표 (.toolbar / .toolbar-year-label / .toolbar-select / .toolbar-batch-btn / .toolbar-spacer / .toolbar-selected-title / .toolbar-individual-btn — 각각 W2/W3 패턴 어느 것에서 inherit 했는지)
  4. ReportsPage.tsx line 181~243 verbatim 매핑 표 (source line / sketch class / 변경 사항)
  5. W1 LOCKED 5건 적용 매트릭스 (OQ #1~#5 어느 것이 W4 에 적용되고 어느 것이 W4 미해당인지)
  6. §6 verify gate 15건 통과 결과 (각 gate 의 실제 grep 결과 값)
  7. 메모리 룰 unique 7+ 건 + 14-reports 컨텍스트 How
  8. W5 진입 전 OQ 3건 default 답안 (confirm 미적용 / 개별 회색 유지 / toast 0)
  9. Atomic commit SHA + 메시지 verbatim
</output>
