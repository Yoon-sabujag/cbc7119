---
phase: quick-260612-3lt
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cha-bio-safety/src/hooks/usePhotoUpload.ts
  - cha-bio-safety/src/hooks/useMultiPhotoUpload.ts
  - cha-bio-safety/src/pages/InspectionPage.tsx
  - cha-bio-safety/src/pages/FloorPlanPage.tsx
  - cha-bio-safety/src/pages/RemediationDetailPage.tsx
autonomous: true
requirements: [UX-PHOTO-FAIL-1]
must_haves:
  truths:
    - "upload() 가 기내모드/CF 5xx HTML 응답에도 throw 하지 않고 null 을 반환한다 (호출부가 raw 문구 노출하지 않음)"
    - "사진 업로드 실패 시 보관함 백업 여부에 따라 문구가 분기된다 (백업 시 '기기에 임시저장됨')"
    - "RemediationDetailPage 조치 저장 성공 시 보관함이 정리된다 (photo.reset)"
    - "기존 하드코딩 옛 문구가 가드 16곳에서 사라지고 photoUploadFailMsg 헬퍼로 통일된다"
    - "LegalFindingsPage 의 '사진 업로드 실패'(보고서 PDF 라벨) 는 변경되지 않는다"
  artifacts:
    - path: "cha-bio-safety/src/hooks/usePhotoUpload.ts"
      provides: "photoUploadFailMsg export + upload() catch→null + vaultBacked 노출"
      contains: "export function photoUploadFailMsg"
    - path: "cha-bio-safety/src/hooks/useMultiPhotoUpload.ts"
      provides: "PhotoUploadFailedError vaultBacked 분기 + import photoUploadFailMsg"
      contains: "photoUploadFailMsg"
    - path: "cha-bio-safety/src/pages/RemediationDetailPage.tsx"
      provides: "scope 'inspection-resolution' + 저장 성공 photo.reset()"
      contains: "usePhotoUpload('inspection-resolution')"
  key_links:
    - from: "cha-bio-safety/src/hooks/useMultiPhotoUpload.ts"
      to: "cha-bio-safety/src/hooks/usePhotoUpload.ts"
      via: "import photoUploadFailMsg"
      pattern: "import \\{ photoUploadFailMsg \\} from './usePhotoUpload'"
---

<objective>
staging cbc7119-data 의 4e169fa (사진 업로드 실패 UX 1단계) 를 prod `production` 브랜치로 단일 patch 이관한다.

검증된 patch (`260612-3lt-staging-4e169fa.patch`, 5파일 +49/-23) 를 `git apply --directory=cha-bio-safety` 로 적용하고, grep 게이트로 적용 결과를 정합 검증한 뒤 단일 atomic commit 한다.

Purpose: 기내모드/CF 5xx 같은 네트워크 실패 시 호출부가 raw 에러 문구를 노출하던 UX 를 무던짐(null)화하고, 보관함 백업 여부에 따라 사용자 친화 문구를 분기. RemediationDetailPage(조치상세) 를 보관함 흐름에 편입.
Output: cha-bio-safety/ 5파일 수정 + 단일 commit. 신규 소스 파일 없음.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/quick/260612-3lt-staging-4e169fa-ux-1-prod-upload-vaultba/260612-3lt-staging-4e169fa.patch

# 콘솔 스코프 (production 전용 — 브랜치 전환 금지, staging repo 쓰기 금지)
@./CLAUDE.md
</context>

<constraints>
- 작업 디렉토리 = repo 루트 `/Users/jongyupyoon/Documents/20260328`. 브랜치 `production` 전환 금지.
- staging repo `~/Documents/cbc7119-data` 절대 쓰기 금지. `.planning/` 외 파일 + patch 가 명시한 5파일 외 수정 금지.
- wrangler / 배포 / D1 / tsc / npm build / git push 모두 금지 — 빌드·배포 게이트는 오케스트레이터가 merge 후 수행. worktree 에 node_modules 없음.
- `.planning/production-sync.md` 는 patch 무관 기존 uncommitted 변경이 있을 수 있음 — 건드리지 말 것 (commit 대상 아님).
- LegalFindingsPage 의 '사진 업로드 실패'(보고서 PDF 업로드 라벨)는 patch 무접촉 — 변경 금지.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: patch 적용 (5파일)</name>
  <files>cha-bio-safety/src/hooks/usePhotoUpload.ts, cha-bio-safety/src/hooks/useMultiPhotoUpload.ts, cha-bio-safety/src/pages/InspectionPage.tsx, cha-bio-safety/src/pages/FloorPlanPage.tsx, cha-bio-safety/src/pages/RemediationDetailPage.tsx</files>
  <action>
  repo 루트에서 먼저 `git apply --check --directory=cha-bio-safety .planning/quick/260612-3lt-staging-4e169fa-ux-1-prod-upload-vaultba/260612-3lt-staging-4e169fa.patch` 로 CLEAN 확인 (offset 매칭으로 통과 검증됨). 통과하면 `--check` 없이 동일 명령으로 실제 적용한다.

  적용 후 `git status --short` 로 cha-bio-safety/ 5파일이 modified 상태인지 확인한다. 신규 소스 파일이 생성되면 STOP (patch 는 5파일 수정만 — RemediationDetailPage 포함 신규 파일 0). `.planning/production-sync.md` 가 이미 modified 로 보일 수 있으나 이는 patch 와 무관한 기존 변경이므로 정상 — patch 가 추가로 production-sync.md 를 건드리지 않았는지(diff 에 등장하지 않는지) 확인만 한다.

  `git apply` 가 reject(.rej) 파일을 남기거나 종료코드가 0 이 아니면 STOP 하고 원인을 보고한다 (--check 가 통과했으므로 발생하지 않아야 함).
  </action>
  <verify>
    <automated>cd /Users/jongyupyoon/Documents/20260328 && git apply --check --directory=cha-bio-safety .planning/quick/260612-3lt-staging-4e169fa-ux-1-prod-upload-vaultba/260612-3lt-staging-4e169fa.patch && echo CHECK-CLEAN</automated>
  </verify>
  <done>patch 적용 완료. cha-bio-safety/ 5파일이 modified 상태이고 신규 소스/.rej 파일 없음.</done>
</task>

<task type="auto">
  <name>Task 2: grep verify 게이트 + 단일 atomic commit</name>
  <files>cha-bio-safety/src/hooks/usePhotoUpload.ts, cha-bio-safety/src/hooks/useMultiPhotoUpload.ts, cha-bio-safety/src/pages/InspectionPage.tsx, cha-bio-safety/src/pages/FloorPlanPage.tsx, cha-bio-safety/src/pages/RemediationDetailPage.tsx</files>
  <action>
  cha-bio-safety/ 기준으로 verify 게이트 스크립트(`verify-gate.sh`)를 실행한다. GATE-PASS 가 나오지 않으면 STOP 하고 FAIL 로 찍힌 항목의 diff 를 열어 원인 확인. 카운트 오프셋이 patch 의도와 부합하면 실측값으로 스크립트의 기대값을 갱신해도 되나, 의도 불일치(예: 가드 누락, LegalFindingsPage 변경)면 STOP.

  스크립트가 강제하는 실측 확정 기대값 (planner 가 patch 를 임시 적용해 측정):
  - `grep -c photoUploadFailMsg src/hooks/usePhotoUpload.ts` == 1 (export 정의 1줄. 헬퍼 자체 사용 0 — 정의 존재가 핵심)
  - `grep -c photoUploadFailMsg src/hooks/useMultiPhotoUpload.ts` == 2 (import 1 + PhotoUploadFailedError 생성자 사용 1)
  - `grep -c photoUploadFailMsg src/pages/InspectionPage.tsx` == 13 (import 1 + 가드 12)
  - `grep -c photoUploadFailMsg src/pages/FloorPlanPage.tsx` == 4 (import 1 + 가드 3)
  - `grep -c photoUploadFailMsg src/pages/RemediationDetailPage.tsx` == 2 (import 1 + 가드 1)
  - 옛 하드코딩 문구 `업로드 실패 — 다시` 잔존 0 (Inspection/FloorPlan/Remediation 각 0)
  - BC 가드: `bcPhoto.vaultBacked` (Inspection) 1건 + `inspectBcPhoto.vaultBacked` (FloorPlan) 1건
  - RemediationDetailPage: `usePhotoUpload('inspection-resolution')` 1건 + `photo.reset()` 1건 (toast.success('조치 완료') 직후, navigate(-1) 직전)
  - usePhotoUpload: `export function photoUploadFailMsg` + `const vaultBacked = !!vaultIdRef.current` + `catch {` 블록 존재 (catch 내 return null 로 무던짐화)
  - LegalFindingsPage `사진 업로드 실패` 1건 (라인 103 toast.error — patch 전후 동일, 변경 0)
  - `git diff --name-only -- cha-bio-safety/` 가 정확히 5파일

  모든 게이트 PASS(GATE-PASS) 후 단일 atomic commit. patch 5파일만 stage 한다 (`.planning/production-sync.md` 등 다른 dirty 파일은 stage 금지):
  `git add cha-bio-safety/src/hooks/usePhotoUpload.ts cha-bio-safety/src/hooks/useMultiPhotoUpload.ts cha-bio-safety/src/pages/InspectionPage.tsx cha-bio-safety/src/pages/FloorPlanPage.tsx cha-bio-safety/src/pages/RemediationDetailPage.tsx`
  그 다음 `git status --short` 로 staged 가 정확히 5파일인지 확인 후 commit.
  메시지: `feat(260612-3lt): 사진 업로드 실패 UX 1단계 — upload 무던짐화 + 보관함 백업 문구 분기 + 조치상세 편입 (staging 4e169fa sync)`

  commit 후 push 하지 말 것 (오케스트레이터 게이트). `git log --oneline -1` 로 commit 생성만 확인.
  </action>
  <verify>
    <automated>bash .planning/quick/260612-3lt-staging-4e169fa-ux-1-prod-upload-vaultba/verify-gate.sh</automated>
  </verify>
  <done>verify-gate.sh GATE-PASS. cha-bio-safety/ 정확히 5파일만 변경. 단일 atomic commit 생성 (push 안 함).</done>
</task>

</tasks>

<verification>
- patch 적용 후 `bash .planning/quick/260612-3lt-staging-4e169fa-ux-1-prod-upload-vaultba/verify-gate.sh` 가 GATE-PASS 출력 (모든 grep 카운트 정합 + LegalFindingsPage 무접촉 + cha-bio-safety/ 5파일).
- `git log --oneline -1` 이 새 commit 을 보여줌. push 미수행.
- worktree 에 신규 소스/.rej 없음. `.planning/production-sync.md` 등 patch 외 dirty 파일은 stage/commit 되지 않음.
</verification>

<success_criteria>
- staging 4e169fa 의 5파일 변경이 prod production 브랜치에 byte-수준 의도 그대로 이관됨.
- upload() 가 throw 대신 null (catch→return null), 실패 문구가 vaultBacked 분기, RemediationDetailPage 가 보관함 흐름에 편입(scope + reset).
- 단일 atomic commit. 빌드/배포/push 는 오케스트레이터가 수행.
</success_criteria>

<output>
Create `.planning/quick/260612-3lt-staging-4e169fa-ux-1-prod-upload-vaultba/260612-3lt-SUMMARY.md` when done.
</output>
