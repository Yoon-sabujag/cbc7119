---
phase: quick-260526-n10
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cha-bio-safety/docs/redesign-context/26-staff-manage/sketch-wave-2-frame-guard-header.html
  - cha-bio-safety/docs/redesign-context/26-staff-manage/sketch-wave-3-staff-list.html
  - cha-bio-safety/docs/redesign-context/26-staff-manage/sketch-wave-4-modal-form-confirm.html
autonomous: true
requirements:
  - REDESIGN-26-STAFF-MANAGE-W2
  - REDESIGN-26-STAFF-MANAGE-W3
  - REDESIGN-26-STAFF-MANAGE-W4

must_haves:
  truths:
    - "W2 sketch (sketch-wave-2-frame-guard-header.html) 가 외곽 wrapper + admin 가드 visual placeholder + <style> keyframes (blink/slideUp) 토큰화 + 데스크톱 헤더 (직원 관리+카운트+직원 추가) + 모바일 헤더 (카운트 only) 분기를 ≥150 lines 으로 시각화 (OQ #4 외곽 hex + #5 폰트 + #6 Lucide Plus)"
    - "W3 sketch (sketch-wave-3-staff-list.html) 가 콘텐츠 (모바일 카드 + 데스크톱 테이블 7 컬럼 + skeleton/error/empty 4 상태 + 모바일 FAB) 를 ≥250 lines 으로 시각화 (OQ #4 외곽 hex + #5 폰트 + #6 ChevronDown/Plus + duty 토큰 색)"
    - "W4 sketch (sketch-wave-4-modal-form-confirm.html) 가 BottomSheet/DesktopModal wrapper + StaffModalContent 등록 폼 (8 필드 + role toggle + confirmReset + confirmDeactivate + 교체 버튼) + ReplaceModalContent 를 ≥300 lines 으로 시각화 (OQ #1 인라인 보존 + #2 CTA 단색 + #3 shift 미노출 + #4 + #5 + #6 X close)"
    - "3 sketch 모두 평면 sibling (26-staff-manage/sketch-wave-N-*.html, sketch/ 서브폴더 X) 으로 배치"
    - "3 sketch 어디에도 cha-bio-safety/src/** / components.css / App.tsx 비즈 anchor 1 byte 변경 X"
    - "3 sketch 어디에도 fontSize 9/10/11 인라인 0건 + linear-gradient 0건 + 이모지 0건 + status- prefix 0건"
    - "3 sketch 각각에 비즈 anchor 주석 (StaffManagePage.tsx line 범위 인용) ≥1건 + OQ # anchor ≥1건"
    - "각 sketch HTML 은 단독 self-contained (head + style + body 완비, var() 토큰 인용 OK, 모바일+데스크톱+라이트/다크 양쪽 frame)"
  artifacts:
    - path: "cha-bio-safety/docs/redesign-context/26-staff-manage/sketch-wave-2-frame-guard-header.html"
      provides: "W2 외곽 wrapper + admin 가드 + keyframes 토큰화 + 헤더 (데스크톱/모바일 분기) sketch"
      min_lines: 150
    - path: "cha-bio-safety/docs/redesign-context/26-staff-manage/sketch-wave-3-staff-list.html"
      provides: "W3 콘텐츠 (모바일 카드 + 데스크톱 테이블 7 컬럼 + 4 상태 + FAB) sketch"
      min_lines: 250
    - path: "cha-bio-safety/docs/redesign-context/26-staff-manage/sketch-wave-4-modal-form-confirm.html"
      provides: "W4 BottomSheet/DesktopModal + 등록 폼 8 필드 + confirmReset + confirmDeactivate + ReplaceModal sketch"
      min_lines: 300
  key_links:
    - from: "sketch-wave-2-frame-guard-header.html"
      to: "wave-1-index.md §1.1 sub-area 1+2 (line 58~73)"
      via: "verbatim 인용 (재서술 X) — wave-1-index.md §2.1 W2 row + §1.3 외곽 hex/카피"
      pattern: "wave-1-index"
    - from: "sketch-wave-3-staff-list.html"
      to: "wave-1-index.md §1.1 sub-area 3 (line 74~84) + StaffCard (318~339) + SKELETON (342~345)"
      via: "verbatim 인용 — wave-1-index.md §2.1 W3 row"
      pattern: "wave-1-index"
    - from: "sketch-wave-4-modal-form-confirm.html"
      to: "wave-1-index.md §1.1 sub-area 4 (line 85~104) + ReplaceModalContent (81~149)"
      via: "verbatim 인용 — wave-1-index.md §2.1 W4 row + §1.3 폼 8 필드/카피/placeholder/toast"
      pattern: "wave-1-index"
    - from: "3 atomic commit"
      to: "main branch (W1 인덱스 e9462f8 후속)"
      via: "1 task = 1 commit, SUMMARY 별도"
      pattern: "atomic"
---

<objective>
redesign/26-staff-manage W2~W4 sketch waves 3 atomic.

Purpose: StaffManagePage.tsx (530 lines, admin 전용) 4 sub-area 를 3 sketch HTML 로 시각화. wave-1-index.md §2.1 sub-wave 표 그대로 mirror (W2 외곽+가드+헤더 통합 / W3 콘텐츠+FAB / W4 모달+폼+confirm+ReplaceModal).

Output: 3 sketch HTML (평면 sibling) + 3 atomic git commit. fga (24-checkpoints) precedent 4 atomic 보다 1 단계 축소 (헤더가 카운트 only 모바일 + 단순 데스크톱 한 줄로 가벼움 → W2 와 통합).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@cha-bio-safety/docs/redesign-context/26-staff-manage/wave-1-index.md

**단일 진입점 룰 (mbr/fga precedent):**
- wave-1-index.md 1개 (665 lines) 만 읽으면 모든 룰/인벤토리/sub-wave 분배/OQ default 6건 박제됨.
- 본 PLAN 은 task 박스만 가지고, wave-1-index.md §1·§2·§3·§4·§5·§6·§7 verbatim 재서술 금지.
- 각 task action 은 §2.1 의 "영역 / 라인 범위 / 비즈 anchor / OQ #" 박제 + executor 가 wave-1-index.md 직접 grep.

**Negative gate (3 task 공통):**
- `git diff --name-only HEAD~3 HEAD -- cha-bio-safety/src` → 빈 출력 (src 손대지 않음)
- `git diff --name-only HEAD~3 HEAD -- cha-bio-safety/src/styles/components.css` → 빈 출력
- `git diff --name-only HEAD~3 HEAD -- cha-bio-safety/src/App.tsx` → 빈 출력
- `grep -rE 'wrangler|npm run deploy' cha-bio-safety/docs/redesign-context/26-staff-manage/sketch-wave-*.html` → 빈 출력 (CLAUDE.local.md 디자인 워크트리 룰)

**OQ default LOCKED (wave-1-index.md §7 — W2 진입 직전 LOCKED, 변경 X):**
- OQ #1: BottomSheet/DesktopModal 인라인 보존 (CheckpointsPage 24 공통화는 별도 task)
- OQ #2: CTA 버튼 단색 `--accent-primary` 유지 (§6.4 그라데이션 미적용) + 교체 버튼 warning 톤 유지
- OQ #3: shift_offset / shift_fixed 폼 UI 미노출 유지 (ReplaceModal 만) + role toggle button row height 36 → 40 (데스크톱) / 44 (모바일) 격상
- OQ #4: 외곽 hex 토큰 치환 (`--bg`→`--surface-page` / `--bg2`→`--surface-raised` / `--bg3`→`--surface-sunken` / `--bg4`→`--surface-active` / `--bd`→`--border-default` / `--bd2`→`--border-strong` / `--t1/--t2/--t3`→`--text-primary/--secondary/--tertiary` / `--acl`→`--accent-primary` / `--safe`→`--status-safe-bar` / `--danger`→`--status-danger` / `--warn`→`--status-warning-bar`)
- OQ #5: fontSize 9 (StaffCard role 배지) / 10 (테이블 role 배지 + 보조 라벨) / 11 (테이블 status) → `text-caption` (12px) 일률 격상 + 작은 영역 `leading-none`
- OQ #6: IconUserPlus SVG → Lucide `<UserPlus size={16} />` (데스크톱 헤더) + `<UserPlus size={18} />` (모바일 FAB, 18 보존) — duty 색 카드 신규 도입 미적용

**OQ # 분포 (task 별):**
- W2 = OQ #4 + OQ #5 + OQ #6 (외곽 hex + 보조 라벨 격상 + Plus 치환)
- W3 = OQ #4 + OQ #5 + OQ #6 (외곽 hex + 카드/테이블 9·10·11 격상 + Plus/ChevronDown 치환 + duty 토큰 색 미도입 표시)
- W4 = OQ #1 + OQ #2 + OQ #3 + OQ #4 + OQ #5 + OQ #6 (인라인 보존 + 단색 + shift 미노출 + role height 격상 + 외곽 hex + 폼 11 격상 + X close)

**메모리 룰 slug (wave-1-index.md §5 박제, 재서술 X):**
- feedback_design_sketch_first / feedback_design_changes_ask_first / feedback_redesign_sketch_rule_enforcement
- feedback_sketch_realistic_data / feedback_tsx_wave_stat_card_drift / feedback_planner_prompt_sketch_verbatim
- feedback_tailwind_token_class_pattern / feedback_tailwind_w8_h8_is_48px / feedback_text_caption_leading_none
- feedback_avoid_premature_confirmation / project_redesign_15_daily_report_status / feedback_cbc7119_design_never_wrangler
- (특화) project_div_compressor_pair / project_meal_calc_rules — shiftOffset+shiftFixed 비즈 데이터 1 byte X

**비즈 anchor 박제 (3 sketch 공통 — 디자인만 손댐, 1 byte X):**
- admin 가드 useEffect + early return (line 371~373 / 385)
- React Query queryKey `['staff-list']` (line 87 ReplaceModal, line 376 main list) + invalidate (line 106, 168, 174, 186)
- useMutation 4건 (create / update / resetPw / deactivate, line 166~188)
- staffApi 4종 호출 (list / create / update / resetPassword) — 시그니처 + 인자 + 응답 처리 모두 보존
- ReplaceModalContent 2× staffApi.update 시퀀스 (line 81~149) — candidates filter (active===1 && shiftOffset===null && shiftFixed===null) / shiftLabel 3분기 / 2단계 await 시퀀스
- rankOfTitle 정렬 (대리=0 / 주임=1 / 기사=2 / 기타=3, line 347~354)
- 사번 10자리 정규식 `/^\d{10}$/` (line 194) + appointedAt 자동 채움 `${p.slice(0,4)}-${p.slice(4,6)}-${p.slice(6,8)}` (line 224~233)
- shiftOffset 0~3 + shiftFixed 'day'/null (4-shift 사이클, 폼 UI 미노출)
- role 옵션 `(['admin', 'assistant'] as Role[])` (line 246) — '관리자' / '보조자' 라벨
- window.__openReplaceModal 훅 (line 365~368) + 100ms setTimeout 호출 패턴
- BottomSheet/DesktopModal 함수 (line 22~57) + ModalWrapper = isDesktop ? DesktopModal : BottomSheet (line 387)
- StaffCard (line 318~339, 8x8 dot + name 16 + title 12 + role 배지 9px → §1.1 위반 격상 대상 + '수정 ▸')
- SKELETON_STYLE (line 342~345, height 64 + blink 2s)
- 카피 verbatim (wave-1-index.md §1.3): '직원 관리' / '직원 추가' / '직원 수정' / '직원 교체' / placeholder 7건 / toast 8건 / ReplaceModal 카피
</context>

<tasks>

<task type="auto">
  <name>T1: sketch-wave-2-frame-guard-header.html (외곽 wrapper + admin 가드 + keyframes + 헤더 모바일/데스크톱 분기)</name>
  <files>cha-bio-safety/docs/redesign-context/26-staff-manage/sketch-wave-2-frame-guard-header.html</files>
  <action>
StaffManagePage.tsx line 371~373 (admin 가드 useEffect) + 385 (early return) + 389~395 (외곽 wrapper + `<style>` keyframes blink/slideUp + input/select focus border-color) + 398~408 (데스크톱 헤더) + 409~413 (모바일 헤더) sketch HTML 단일 self-contained 파일 산출. 라인 추정 150~220.

**영역 (wave-1-index.md §2.1 W2 row + §1.1 sub-area 1+2 verbatim 인용 — executor 가 wave-1-index.md 직접 grep):**
- 외곽 wrapper `flex column / bg var(--bg) / height 100% / overflow hidden` (line 389~390)
- `<style>` 태그 keyframes (blink + slideUp + input/select focus border-color `var(--acl)`, line 391~395)
- admin 가드 visual placeholder — `me?.role !== 'admin'` 시 `navigate('/dashboard', { replace: true })` + early return null
- 데스크톱 헤더 (line 398~408) — `flex / alignItems center / padding 12px 24px / borderBottom var(--bd)` + "직원 관리" 타이틀 + 카운트 `{N}명` + "직원 추가" button (height 36, bg var(--acl), IconUserPlus size 16, 라벨 '직원 추가')
- 모바일 헤더 (line 409~413) — `flex / alignItems center / padding 8px 16px / flexShrink 0` + 카운트 only (`{N}명`, fontSize 12, var(--t3))

**Frame 권장 4~5 (라이트/다크 + 모바일/데스크톱):**
- Frame A: 데스크톱 헤더 default — bg `--surface-page` + 좌측 "직원 관리" (text-body fontWeight 700) + 카운트 (예 "12명", text-caption var(--text-secondary)) + 우측 "직원 추가" button (bg `--accent-primary` solid, Lucide `<UserPlus size={16} />`, 라벨 '직원 추가', height 36)
- Frame B: 모바일 헤더 default — 카운트 only ("12명", text-caption, var(--text-tertiary), padding 8px 16px) — '직원 추가' button 없음 (FAB 가 W3 에 있음)
- Frame C: admin 가드 발동 (`me?.role !== 'admin'`) — 빈 wrapper + redirect 메타 코멘트 (`navigate('/dashboard', { replace: true })` + early return null)
- Frame D: keyframes 시각화 — blink 2s ease-in-out infinite (skeleton 미리보기) + slideUp 0.28s ease-out (BottomSheet 진입 미리보기, W4 에서 사용) + focus border-color `--accent-primary` (input/select 포커스 ring)
- Frame E (선택): 라이트/다크 토글 — 같은 헤더 양쪽 테마

**OQ # anchor 박스 (이 sketch 적용):**
```
<!-- OQ #4: 외곽 hex 토큰 치환 — --bg→--surface-page / --bg2→--surface-raised / --bg3→--surface-sunken / --bd→--border-default / --t1/--t3→--text-primary/--text-tertiary / --acl→--accent-primary -->
<!-- OQ #5: 데스크톱 헤더 카운트 fontSize 12 (line 401) / 모바일 헤더 카운트 fontSize 12 (line 411) 보존 OK (§1.1 마지노선). 작은 영역 leading-none 명시. -->
<!-- OQ #6: IconUserPlus SVG (line 11~20 + 헤더 line 405 size=16) → Lucide <UserPlus size={16} /> (§7.1 사이즈 룰 16/20/24) -->
```

**비즈 anchor 주석 (시안 안 메타 코멘트, ≥1건):**
```
<!-- 비즈 anchor (StaffManagePage.tsx, 1 byte X):
     - admin 가드 useEffect (line 371~373): useEffect(() => { if (me?.role !== 'admin') navigate('/dashboard', { replace: true }) }, [me, navigate])
     - early return (line 385): if (me?.role !== 'admin') return null
     - 외곽 wrapper (line 389~390): style={{ display:'flex', flexDirection:'column', background:'var(--bg)', height:'100%', overflow:'hidden' }}
     - keyframes (line 391~395): @keyframes blink + @keyframes slideUp + input:focus border-color var(--acl)
     - 데스크톱 헤더 (line 398~408): isDesktop && {...} + '직원 관리' 타이틀 + `{staffList.length}명` 카운트 + onClick={() => setModal({ open: true, mode: 'add' })} + Plus 16
     - 모바일 헤더 (line 409~413): !isDesktop && {...} + `{staffList.length}명` only
     - 카피 verbatim: '직원 관리' / '{N}명' / '직원 추가'
-->
```

**Negative gate (positive gate 6건):**
- linear-gradient 0건
- 이모지 0건 (`grep -cP '[\x{1F300}-\x{1F6FF}\x{2600}-\x{27BF}]'` == 0)
- fontSize 9·10·11 인라인 0건 (positive gate)
- `status-` prefix 0건 (memory `feedback_tailwind_token_class_pattern` — `text-safe-bar` O / `text-status-safe-bar` X)
- OQ # anchor ≥1건 (실제 3건 — #4 #5 #6)
- 비즈 anchor 주석 ≥1건

산출 후 commit:
```
git commit -m "docs(26-staff-manage): sketch wave 2 — frame guard header sub-wave HTML"
```
  </action>
  <verify>
    <automated>test -f cha-bio-safety/docs/redesign-context/26-staff-manage/sketch-wave-2-frame-guard-header.html && wc -l cha-bio-safety/docs/redesign-context/26-staff-manage/sketch-wave-2-frame-guard-header.html | awk '{exit !($1>=150)}' && grep -cE 'linear-gradient' cha-bio-safety/docs/redesign-context/26-staff-manage/sketch-wave-2-frame-guard-header.html | awk '{exit !($1==0)}' && grep -cP '[\x{1F300}-\x{1F6FF}\x{2600}-\x{27BF}]' cha-bio-safety/docs/redesign-context/26-staff-manage/sketch-wave-2-frame-guard-header.html | awk '{exit !($1==0)}' && grep -cE 'fontSize:\s*(9|10|11)px|text-\[(9|10|11)px\]|font-size:\s*(9|10|11)px' cha-bio-safety/docs/redesign-context/26-staff-manage/sketch-wave-2-frame-guard-header.html | awk '{exit !($1==0)}' && grep -cE 'status-(safe|danger|info|warn|fire)-bar|text-status-|bg-status-' cha-bio-safety/docs/redesign-context/26-staff-manage/sketch-wave-2-frame-guard-header.html | awk '{exit !($1==0)}' && grep -cE 'OQ #(4|5|6)' cha-bio-safety/docs/redesign-context/26-staff-manage/sketch-wave-2-frame-guard-header.html | awk '{exit !($1>=3)}' && grep -ciE '비즈 anchor|StaffManagePage' cha-bio-safety/docs/redesign-context/26-staff-manage/sketch-wave-2-frame-guard-header.html | awk '{exit !($1>=1)}' && git log -1 --pretty=%s | grep -qE 'sketch wave 2|W2 sketch'</automated>
  </verify>
  <done>sketch-wave-2 HTML ≥150 lines + linear-gradient 0 + 이모지 0 + 9·10·11px 0 + `status-` prefix 0 + OQ #4+#5+#6 anchor ≥3 + 비즈 anchor 주석 ≥1 + atomic commit 1건</done>
</task>

<task type="auto">
  <name>T2: sketch-wave-3-staff-list.html (콘텐츠 — 모바일 카드 + 데스크톱 테이블 7 컬럼 + 4 상태 + FAB)</name>
  <files>cha-bio-safety/docs/redesign-context/26-staff-manage/sketch-wave-3-staff-list.html</files>
  <action>
StaffManagePage.tsx line 416~501 (콘텐츠) + 318~339 (StaffCard) + 342~345 (SKELETON_STYLE) + 505~513 (모바일 FAB) sketch HTML 단일 self-contained 파일 산출. 라인 추정 250~340.

**영역 (wave-1-index.md §2.1 W3 row + §1.1 sub-area 3 verbatim 인용):**
- 콘텐츠 wrapper (`flex 1 / overflow auto / minHeight 0`, line 416)
- 4 상태: skeleton 3개 (417~423, SKELETON_STYLE height 64 blink) / 에러 (424~428) / 데이터 / empty
- 데스크톱 테이블 7 컬럼 (431~486) — 이름/사번/직책/역할/연락처/상태/액션 + row hover bg `var(--bg3)` → `--surface-sunken` + 데이터 empty (446~448)
- role 배지 (459~467) — admin: `rgba(59,130,246,.13)` + `var(--acl)` + '관리자' / assistant: `rgba(110,118,129,.15)` + `var(--t2)` + '보조자' (fontSize 10 → 12 OQ #5)
- 상태 dot 라벨 (469~476) — 활성 `var(--safe)` + '활성' / 비활성 `var(--t3)` + '비활성' (fontSize 11 → 12 OQ #5)
- StaffCard 모바일 카드 (318~339) — 8x8 dot bg `var(--safe)` (active===1) / `var(--t3)` (active===0, opacity 0.5) + name 16 + title 12 + role 배지 9 → 12 (OQ #5) + 사번 JetBrains Mono + '수정 ▸'
- 모바일 카드 리스트 (489~501) + empty (491~495, '등록된 직원이 없습니다' + '직원 추가 버튼을 눌러 첫 번째 직원을 등록하세요')
- 모바일 FAB (505~513) — `sticky bottom + bg var(--acl) + Plus 18 + '직원 추가' + paddingBottom calc(16px + var(--sab))`

**Frame 권장 4~5:**
- Frame A: 데스크톱 테이블 default — 7 컬럼 head + 4~6 row (이름 16 + 사번 JetBrains Mono + 직책 12 + role 배지 (관리자 bg `status-info-bg` 알리아스 / 보조자 bg `--surface-active` 알리아스) + 연락처 + 활성 dot 8x8 `--status-safe-bar` + '수정' link)
- Frame B: 데스크톱 테이블 4 상태 — skeleton 3 row (height 64 blink animation) + error ('데이터를 불러오지 못했습니다. 잠시 후 다시 시도해 주세요') + empty ('등록된 직원이 없습니다' colSpan 7) + 비활성 row (`s.active === 0`, opacity 0.5)
- Frame C: 모바일 카드 리스트 default — 4~5 StaffCard (8x8 dot bg `--status-safe-bar` + name 16 + title 12 + role 배지 12 (OQ #5 격상 + leading-none, memory `feedback_text_caption_leading_none`) + 사번 12 JetBrains Mono + '수정 ▸')
- Frame D: 모바일 카드 empty + FAB — 빈 안내 '등록된 직원이 없습니다' + '직원 추가 버튼을 눌러 첫 번째 직원을 등록하세요' + sticky FAB bg `--accent-primary` solid + Lucide `<UserPlus size={18} />` + '직원 추가' + paddingBottom calc(16 + var(--sab)) safe-area
- Frame E (선택): 비활성 StaffCard (s.active===0, opacity 0.5, dot bg `--text-tertiary`)

**OQ # anchor 박스 (이 sketch 적용):**
```
<!-- OQ #4: 외곽 hex 토큰 치환 (--bg2→--surface-raised / --bg3→--surface-sunken / --t1/t2/t3→--text-primary/secondary/tertiary / --safe→--status-safe-bar / --acl→--accent-primary / --bd→--border-default) -->
<!-- OQ #5: 카드 role 배지 fontSize 9 (line 327) / 테이블 role 배지 10 (line 461) / 테이블 상태 dot 라벨 11 (line 471) → text-caption (12px) 일률 격상 + 작은 영역 leading-none -->
<!-- OQ #6: IconUserPlus SVG (FAB line 509 size=18) → Lucide <UserPlus size={18} /> (size 18 보존, §7.1 사이즈 룰 16/20/24 중 인접 16 또는 20 격상 검토 — default 18 보존) + duty 토큰 색 카드 신규 도입 미적용 (별도 task) -->
```

**비즈 anchor 주석 (≥1건):**
```
<!-- 비즈 anchor (StaffManagePage.tsx, 1 byte X):
     - 콘텐츠 wrapper (line 416): flex 1 / overflow auto / minHeight 0
     - 4 상태 분기: isLoading skeleton 3개 (417~423, SKELETON_STYLE bg var(--bg3) radius 12 height 64 blink 2s) / isError && !isLoading error (424~428) / staffList.map data / staffList.length === 0 empty
     - 데스크톱 테이블 7 컬럼 (431~486): th '이름/사번/직책/역할/연락처/상태/액션' + row hover e.currentTarget.style.background='var(--bg3)' (453~454) → hover:bg-surface-sunken
     - role 배지 (459~467): s.role === 'admin' ? '관리자' (bg rgba(59,130,246,.13) color var(--acl)) : '보조자' (bg rgba(110,118,129,.15) color var(--t2)), fontSize 10 → 12 (OQ #5)
     - 상태 dot 라벨 (469~476): s.active !== 0 ? '활성' (dot var(--safe)) : '비활성' (dot var(--t3)), fontSize 11 → 12 (OQ #5)
     - StaffCard (line 318~339): onClick edit / 8x8 dot bg var(--safe) (active===1) / var(--t3) (active===0 opacity 0.5) / name 16 fontWeight 600 / title 12 var(--t3) / role 배지 9 → 12 (OQ #5) / 사번 12 JetBrains Mono var(--t3) / borderRadius 12 / bg var(--bg3) → --surface-sunken
     - SKELETON_STYLE (line 342~345): bg var(--bg3) / borderRadius 12 / height 64 / animation 'blink 2s ease-in-out infinite'
     - 모바일 FAB (line 505~513): position sticky / bottom 0 / bg var(--acl) / Plus 18 / '직원 추가' / paddingBottom 'calc(16px + var(--sab))' (iOS safe-area, memory `feedback_bottomnav_gap_style` 친척) / height 52
     - 사번 폰트 JetBrains Mono (테이블 + StaffCard, source preserve)
     - rankOfTitle 정렬 (line 347~354) — 대리=0/주임=1/기사=2/기타=3, sort 후 id 오름차 (변경 X, 시안 데이터 시안 순서 동일)
     - 카피 verbatim 5건: '등록된 직원이 없습니다' (447, 493) / '직원 추가 버튼을 눌러 첫 번째 직원을 등록하세요' (494) / '데이터를 불러오지 못했습니다. 잠시 후 다시 시도해 주세요' (426) / '활성' / '비활성' (475) / '관리자' / '보조자' (line 462) / '수정 ▸' (336) / '수정' (테이블 479) / '직원 추가' (FAB 510)
-->
```

**Negative gate (6건):**
- linear-gradient 0건
- 이모지 0건
- fontSize 9·10·11 인라인 0건 (OQ #5 격상 적용 — 9→12 / 10→12 / 11→12)
- `status-` prefix 0건 (memory `feedback_tailwind_token_class_pattern`)
- OQ # anchor ≥1건 (실제 3건 — #4 #5 #6)
- 비즈 anchor 주석 ≥1건

산출 후 commit:
```
git commit -m "docs(26-staff-manage): sketch wave 3 — staff list sub-wave HTML"
```
  </action>
  <verify>
    <automated>test -f cha-bio-safety/docs/redesign-context/26-staff-manage/sketch-wave-3-staff-list.html && wc -l cha-bio-safety/docs/redesign-context/26-staff-manage/sketch-wave-3-staff-list.html | awk '{exit !($1>=250)}' && grep -cE 'linear-gradient' cha-bio-safety/docs/redesign-context/26-staff-manage/sketch-wave-3-staff-list.html | awk '{exit !($1==0)}' && grep -cP '[\x{1F300}-\x{1F6FF}\x{2600}-\x{27BF}]' cha-bio-safety/docs/redesign-context/26-staff-manage/sketch-wave-3-staff-list.html | awk '{exit !($1==0)}' && grep -cE 'fontSize:\s*(9|10|11)px|text-\[(9|10|11)px\]|font-size:\s*(9|10|11)px' cha-bio-safety/docs/redesign-context/26-staff-manage/sketch-wave-3-staff-list.html | awk '{exit !($1==0)}' && grep -cE 'status-(safe|danger|info|warn|fire)-bar|text-status-|bg-status-' cha-bio-safety/docs/redesign-context/26-staff-manage/sketch-wave-3-staff-list.html | awk '{exit !($1==0)}' && grep -cE 'OQ #(4|5|6)' cha-bio-safety/docs/redesign-context/26-staff-manage/sketch-wave-3-staff-list.html | awk '{exit !($1>=3)}' && grep -ciE '비즈 anchor|StaffManagePage' cha-bio-safety/docs/redesign-context/26-staff-manage/sketch-wave-3-staff-list.html | awk '{exit !($1>=1)}' && git log -1 --pretty=%s | grep -qE 'sketch wave 3|W3 sketch'</automated>
  </verify>
  <done>sketch-wave-3 HTML ≥250 lines + linear-gradient 0 + 이모지 0 + 9·10·11px 0 + `status-` prefix 0 + OQ #4+#5+#6 anchor ≥3 + 비즈 anchor 주석 ≥1 + atomic commit 1건</done>
</task>

<task type="auto">
  <name>T3: sketch-wave-4-modal-form-confirm.html (BottomSheet/DesktopModal + 폼 8 필드 + role toggle + confirmReset + confirmDeactivate + ReplaceModal)</name>
  <files>cha-bio-safety/docs/redesign-context/26-staff-manage/sketch-wave-4-modal-form-confirm.html</files>
  <action>
StaffManagePage.tsx line 22~57 (BottomSheet + DesktopModal 함수) + 60~67 (INPUT_STYLE + LABEL_STYLE 상수) + 151~316 (StaffModalContent — 등록 폼 8 필드 + role toggle + confirmReset + confirmDeactivate + 교체 버튼) + 81~149 (ReplaceModalContent — 인라인 박제) + 516~527 (모달 호출 wrapper) sketch HTML 단일 self-contained 파일 산출. 라인 추정 300~460.

**영역 (wave-1-index.md §2.1 W4 row + §1.1 sub-area 4 verbatim 인용):**
- BottomSheet 함수 (22~40) — overlay rgba(0,0,0,0.6) + bg var(--bg2) + radius 16/16/0/0 + slideUp 0.28s + maxHeight 90vh + handle bar 32x4 + backdrop click close
- DesktopModal 함수 (42~57) — overlay rgba(0,0,0,0.5) + bg var(--bg2) + radius 12 + width 440 + maxHeight 85vh + boxShadow `0 8px 32px rgba(0,0,0,.18)`
- 모달 호출 wrapper (516~527) — `{modal.open && <ModalWrapper title={mode==='add'?'직원 추가':'직원 수정'}>...</ModalWrapper>}` + `{replaceModal.open && replaceModal.target && <ModalWrapper title='직원 교체'>...</ModalWrapper>}`
- INPUT_STYLE (60~67) — height 44 / bg var(--bg3) / radius 8 / padding 0 12 / fontSize 14 / border 1px solid var(--bd) / boxSizing border-box
- 폼 필드 8건 (207~253): 1=이름 input (required + placeholder '홍길동' + 빨간 *) / 2=사번 input (JetBrains Mono + '0000000000' + inputMode numeric + edit 시 disabled opacity 0.5) / 3=연락처 input ('010-0000-0000' + type tel) / 4=이메일 input ('email@example.com' + type email) / 5=입사일 input (사번 앞 8자리 auto + readonly opacity 0.5 + 보조 라벨 '(사번 앞 8자리에서 자동)' fontSize 10 → 12 OQ #5) / 6=생년월일 input (type date + 보조 '(휴가신청서 자동 채움)' 10 → 12) / 7=직책 input ('소방안전관리자') / 8=역할 toggle button row (admin '관리자' / assistant '보조자', flex 1 / height 36 → 40/44 OQ #3 + fontSize 12 + selected bg var(--acl) / unselected bg var(--bg4))
- confirmReset (255~274, edit 모드 only) — 토글 링크 '비밀번호 초기화' (var(--warn) underline) → 확인 박스 (bg `rgba(245,158,11,.08)` → `--status-warning-bg` + 카피 '사번 뒷 4자리로 비밀번호를 초기화합니다. 계속하시겠습니까?' + 취소/초기화 버튼 height 32)
- 액션 row (277~312) — 취소 / 저장 button (canSave + isBusy, height 44) + edit 시 교체 button row (height 40 `rgba(245,158,11,.1)` + `#d97706` → `--status-warning-bar`) + confirmDeactivate 안내 박스 (bg `rgba(239,68,68,.08)` → `--status-danger-bg` + 카피 '이 직원을 비활성화합니다. 점검 기록은 보존됩니다.' + 빨간 '비활성화' 버튼)
- ReplaceModalContent (81~149) — info 박스 (`rgba(59,130,246,.08)` → `--status-info-bg` + '근무 패턴을 이전합니다.' + `근무 패턴: ${shiftLabel}` + '기존 점검 기록은 보존되며, 개인정보(연락처/이메일)는 삭제됩니다.') + 교체할 직원 select (`'선택하세요'` + candidates filter) + 취소 / 교체 button + no candidates 시 '교체 가능한 직원이 없습니다. 먼저 "직원 추가"로 신규 직원을 등록해주세요.'

**Frame 권장 5~7 (가장 무거움):**
- Frame A: BottomSheet (모바일) wrapper + 등록 폼 mode='add' default — handle bar 32x4 (memory `feedback_tailwind_w8_h8_is_48px` arbitrary `w-[32px] h-[4px]`) + title '직원 추가' + INPUT_STYLE 8 필드 (이름 / 사번 / 연락처 / 이메일 / 입사일 readonly / 생년월일 / 직책 / role toggle button row) + 액션 row '취소' / '저장'
- Frame B: DesktopModal (데스크톱) wrapper + 동일 폼 — width 440 + radius 12 + boxShadow + bg `--surface-raised` (라이트/다크 양쪽 토글)
- Frame C: mode='edit' 모달 — title '직원 수정' + 사번 disabled (opacity 0.5) + 추가 영역: confirmReset 토글 링크 (var(--warn) underline '비밀번호 초기화') + 교체 button row + 액션 row (왼쪽 '비활성화' button 빨간 / 오른쪽 '취소' / '저장')
- Frame D: confirmReset=true 토글 펼친 상태 — 안내 박스 (bg `--status-warning-bg`) + 카피 verbatim + 취소(height 32) / 초기화(height 32, bg `--status-warning-bar`) 버튼 row
- Frame E: confirmDeactivate=true — 액션 row 영역 빨간 안내 박스 (bg `--status-danger-bg`) + 카피 '이 직원을 비활성화합니다. 점검 기록은 보존됩니다.' + 빨간 '비활성화' 버튼 (bg `--status-danger`)
- Frame F: ReplaceModal — title '직원 교체' + info 박스 (bg `--status-info-bg`) + select '선택하세요' + 후보 옵션 (예: 김OO (사번 1234567890, 미설정)) + 취소 / 교체 버튼 / '처리 중...' busy state
- Frame G: canSave=false 또는 isBusy 시 저장 button disabled state (bg `--surface-sunken` text `--text-tertiary`) + role toggle button row height 40 (데스크톱) / 44 (모바일) 양쪽 frame

**OQ # anchor 박스 (이 sketch 적용 — 모달 wave 최대 6건):**
```
<!-- OQ #1: BottomSheet/DesktopModal 인라인 보존 (CheckpointsPage 24 공통화는 별도 task, 이 wave 박제만 — line 22~57 verbatim) -->
<!-- OQ #2: 저장 button + 직원 추가 button + 비활성화 button + 교체 button 모두 단색 (저장/추가=--accent-primary, 비활성화=--status-danger, 교체=--status-warning-bar, §6.4 그라데이션 미적용) -->
<!-- OQ #3: shiftOffset / shiftFixed 폼 UI 미노출 유지 (EMPTY_STAFF_FORM 에 데이터만 유지, ReplaceModal 의 staffApi.update 시퀀스에서만 변경) + role toggle button row height 36 → 40 (데스크톱) / 44 (모바일) 격상 (§1.1 모바일 44px 룰 준수) -->
<!-- OQ #4: 외곽 hex 토큰 치환 (--bg2→--surface-raised / --bg3→--surface-sunken / --bg4→--surface-active / --bd/--bd2→--border-default/strong / --t1/t2/t3→--text-primary/secondary/tertiary / --acl→--accent-primary / --danger→--status-danger / --warn→--status-warning-bar / --safe→--status-safe-bar / overlay rgba 직접 유지 OK) -->
<!-- OQ #5: 보조 라벨 fontSize 10 (입사일 line 224 + 생년월일 line 236) / ReplaceModal info 박스 보조 라벨 11 (line 121) → text-caption (12px) 일률 격상 + leading-none. 모달 title 16 → text-body 또는 text-title 격상 검토. -->
<!-- OQ #6: 모달 close 버튼 (BottomSheet/DesktopModal 의 backdrop click close — e.target === e.currentTarget) 외 inline SVG 없음. UserPlus 는 W2/W3 에서 처리. 모달 안 명시적 X close icon 추가 검토 (현재 backdrop only). -->
```

**비즈 anchor 주석 (≥1건, 모달 wave 가장 무거움):**
```
<!-- 비즈 anchor (StaffManagePage.tsx, 1 byte X):
     - BottomSheet (line 22~40): overlay position fixed inset 0 / bg rgba(0,0,0,0.6) / zIndex 50 / flex column / justifyContent flex-end / panel bg var(--bg2) borderRadius 16/16/0/0 / animation 'slideUp 0.28s ease-out both' / maxHeight 90vh / handle bar 32x4 paddingTop 12 / title 16/700/var(--t1) padding '12px 16px 0' / backdrop click close (e.target === e.currentTarget)
     - DesktopModal (line 42~57): overlay bg rgba(0,0,0,0.5) / panel borderRadius 12 / width 440 / maxHeight 85vh / boxShadow '0 8px 32px rgba(0,0,0,.18)' / title padding '20px 24px 0'
     - ModalWrapper = isDesktop ? DesktopModal : BottomSheet (line 387) — useIsDesktop 훅 분기
     - INPUT_STYLE (line 60~67): width 100% / height 44 / padding '0 12px' / fontSize 14 / bg var(--bg3) / border 1px solid var(--bd) / borderRadius 8 / color var(--t1) / boxSizing border-box / outline none
     - 폼 필드 8 (line 207~253): name input '홍길동' required + 빨간 * / id input '0000000000' JetBrains Mono inputMode numeric edit 시 disabled / phone input '010-0000-0000' type tel / email input 'email@example.com' type email / appointedAt input readonly opacity 0.5 + 보조 라벨 '(사번 앞 8자리에서 자동)' / birthDate input type date + 보조 라벨 '(휴가신청서 자동 채움)' / title input '소방안전관리자' / role toggle button row (['admin','assistant'] as Role[]) verbatim '관리자' / '보조자'
     - confirmReset (line 255~274, edit only): 토글 링크 '비밀번호 초기화' var(--warn) underline → 안내 박스 rgba(245,158,11,.08) + '사번 뒷 4자리로 비밀번호를 초기화합니다. 계속하시겠습니까?' + 취소/초기화 height 32
     - 액션 row (line 277~312): 취소 / 저장 button canSave (form.name.trim() !== '' && form.id.trim() !== '') / isBusy disabled / edit 시 교체 button row 'rgba(245,158,11,.1)' + '#d97706' / confirmDeactivate=true 안내 박스 'rgba(239,68,68,.08)' + '이 직원을 비활성화합니다. 점검 기록은 보존됩니다.' + 빨간 '비활성화' 버튼
     - ReplaceModalContent (line 81~149): candidates = allStaff.filter(s => s.active === 1 && s.id !== oldStaff.id && s.shiftOffset === null && s.shiftFixed === null) / shiftLabel '평일 주간 고정' | `3교대 (오프셋 ${N})` | '미설정' / 2× await staffApi.update 시퀀스 (1) selectedId set shiftOffset+shiftFixed (2) oldStaff.id active=0 phone='' email='' shiftOffset=null shiftFixed=null / invalidate ['staff-list'] / toast.success `${oldStaff.name} → ${newStaff.name} 교체 완료` / toast.error e.message || '교체 실패'
     - 모달 호출 wrapper (line 516~527): {modal.open && <ModalWrapper title={mode==='add'?'직원 추가':'직원 수정'}>} + {replaceModal.open && replaceModal.target && <ModalWrapper title='직원 교체'>}
     - 사번 10자리 정규식 (line 194, 1 byte X): /^\d{10}$/.test(form.id) — 시안에서는 ✱ 표시 시각만, 정규식 변경 X
     - appointedAt 자동 채움 (line 224~233, 1 byte X): p = (form.id ?? '').slice(0, 8) / /^[0-9]{8}$/.test(p) 시 `${p.slice(0,4)}-${p.slice(4,6)}-${p.slice(6,8)}` / readonly opacity 0.5
     - shiftOffset 0~3 + shiftFixed 'day'/null 폼 UI 미노출 — EMPTY_STAFF_FORM (line 76~78) + 초기화 (line 157) 만, ReplaceModal staffApi.update 만 변경 (memory `project_div_compressor_pair` + `project_meal_calc_rules`)
     - window.__openReplaceModal 훅 (line 365~368, 1 byte X) — 시안은 비주얼만, 훅 변경 X
     - 카피 verbatim: '직원 추가' / '직원 수정' / '직원 교체' / '저장' / '취소' / '비활성화' / '교체' / '관리자' / '보조자' / '비밀번호 초기화' / '초기화' / '이 직원을 비활성화합니다. 점검 기록은 보존됩니다.' / '사번 뒷 4자리로 비밀번호를 초기화합니다. 계속하시겠습니까?' / '(사번 앞 8자리에서 자동)' / '(휴가신청서 자동 채움)' / '근무 패턴을 이전합니다.' / '근무 패턴:' / '기존 점검 기록은 보존되며, 개인정보(연락처/이메일)는 삭제됩니다.' / '교체할 직원 선택' / '교체 가능한 직원이 없습니다. 먼저 "직원 추가"로 신규 직원을 등록해주세요.' / '평일 주간 고정' / `3교대 (오프셋 ${N})` / '미설정' / '처리 중...'
     - placeholder verbatim 7건: '홍길동' / '0000000000' / '010-0000-0000' / 'email@example.com' / '사번 입력 시 자동 채워짐' / '소방안전관리자' / '선택하세요'
     - toast 카피 verbatim 8건: '직원이 추가되었습니다' / '직원 정보가 수정되었습니다' / '저장에 실패했습니다. 입력값을 확인해 주세요' / '비밀번호가 초기화되었습니다 (사번 뒷 4자리)' / '비밀번호 초기화에 실패했습니다' / '직원이 비활성화되었습니다' / '비활성화에 실패했습니다' / '사번은 10자리 숫자여야 합니다' / `${oldStaff.name} → ${newStaff.name} 교체 완료` / '교체 실패'
-->
```

**Negative gate (6건):**
- linear-gradient 0건 (OQ #2 — 저장 + 교체 + 비활성화 button 모두 단색)
- 이모지 0건
- fontSize 9·10·11 인라인 0건 (OQ #5 — 보조 라벨 10/11 → 12 격상)
- `status-` prefix 0건 (memory `feedback_tailwind_token_class_pattern`)
- OQ # anchor ≥1건 (실제 6건 — #1 #2 #3 #4 #5 #6)
- 비즈 anchor 주석 ≥1건

산출 후 commit (3번째 atomic):
```
git commit -m "docs(26-staff-manage): sketch wave 4 — modal form confirm sub-wave HTML"
```
  </action>
  <verify>
    <automated>test -f cha-bio-safety/docs/redesign-context/26-staff-manage/sketch-wave-4-modal-form-confirm.html && wc -l cha-bio-safety/docs/redesign-context/26-staff-manage/sketch-wave-4-modal-form-confirm.html | awk '{exit !($1>=300)}' && grep -cE 'linear-gradient' cha-bio-safety/docs/redesign-context/26-staff-manage/sketch-wave-4-modal-form-confirm.html | awk '{exit !($1==0)}' && grep -cP '[\x{1F300}-\x{1F6FF}\x{2600}-\x{27BF}]' cha-bio-safety/docs/redesign-context/26-staff-manage/sketch-wave-4-modal-form-confirm.html | awk '{exit !($1==0)}' && grep -cE 'fontSize:\s*(9|10|11)px|text-\[(9|10|11)px\]|font-size:\s*(9|10|11)px' cha-bio-safety/docs/redesign-context/26-staff-manage/sketch-wave-4-modal-form-confirm.html | awk '{exit !($1==0)}' && grep -cE 'status-(safe|danger|info|warn|fire)-bar|text-status-|bg-status-' cha-bio-safety/docs/redesign-context/26-staff-manage/sketch-wave-4-modal-form-confirm.html | awk '{exit !($1==0)}' && grep -cE 'OQ #(1|2|3|4|5|6)' cha-bio-safety/docs/redesign-context/26-staff-manage/sketch-wave-4-modal-form-confirm.html | awk '{exit !($1>=6)}' && grep -ciE '비즈 anchor|StaffManagePage' cha-bio-safety/docs/redesign-context/26-staff-manage/sketch-wave-4-modal-form-confirm.html | awk '{exit !($1>=1)}' && git diff --name-only HEAD~3 HEAD -- cha-bio-safety/src cha-bio-safety/src/styles/components.css cha-bio-safety/src/App.tsx | wc -l | awk '{exit !($1==0)}' && git log --oneline -3 | grep -cE 'sketch wave [2-4]|W[2-4] sketch' | awk '{exit !($1==3)}'</automated>
  </verify>
  <done>sketch-wave-4 HTML ≥300 lines + linear-gradient 0 + 이모지 0 + 9·10·11px 0 + `status-` prefix 0 + OQ #1+#2+#3+#4+#5+#6 anchor ≥6 + 비즈 anchor 주석 ≥1 + src/components.css/App.tsx 3 commit 전체 0 byte + 3 atomic commit (W2~W4) 모두 검출</done>
</task>

</tasks>

<verification>
**전체 phase verify (3 task 완료 후 일괄):**

```bash
# 1. 3 sketch HTML 모두 존재 (평면 sibling, sketch/ 서브폴더 X)
ls cha-bio-safety/docs/redesign-context/26-staff-manage/sketch-wave-{2,3,4}-*.html

# 2. 3 atomic commit 검증 (W2~W4 순서)
git log --oneline -3 | grep -cE 'sketch wave [2-4]'   # = 3

# 3. src / components.css / App.tsx 0 byte diff (디자인만 손댐)
git diff --name-only HEAD~3 HEAD -- cha-bio-safety/src                          # 빈 출력
git diff --name-only HEAD~3 HEAD -- cha-bio-safety/src/styles/components.css    # 빈 출력
git diff --name-only HEAD~3 HEAD -- cha-bio-safety/src/App.tsx                  # 빈 출력

# 4. 디자인 워크트리 룰 (wrangler / npm run deploy 흔적 X)
grep -rE 'wrangler|npm run deploy' cha-bio-safety/docs/redesign-context/26-staff-manage/sketch-wave-*.html    # 빈 출력

# 5. 9·10·11px 0건 (3 sketch 전체)
for f in cha-bio-safety/docs/redesign-context/26-staff-manage/sketch-wave-{2,3,4}-*.html; do
  c=$(grep -cE 'fontSize:\s*(9|10|11)px|text-\[(9|10|11)px\]|font-size:\s*(9|10|11)px' "$f")
  [ "$c" = "0" ] || echo "FAIL: $f has $c bad fontSize"
done

# 6. OQ # anchor 박제 3 sketch 분산 (memory `feedback_planner_prompt_sketch_verbatim`)
grep -c 'OQ #' cha-bio-safety/docs/redesign-context/26-staff-manage/sketch-wave-2-*.html    # ≥3 (#4 #5 #6)
grep -c 'OQ #' cha-bio-safety/docs/redesign-context/26-staff-manage/sketch-wave-3-*.html    # ≥3 (#4 #5 #6)
grep -c 'OQ #' cha-bio-safety/docs/redesign-context/26-staff-manage/sketch-wave-4-*.html    # ≥6 (#1 #2 #3 #4 #5 #6)

# 7. linear-gradient 0건 + 이모지 0건 + status- prefix 0건 (3 sketch 전체)
for f in cha-bio-safety/docs/redesign-context/26-staff-manage/sketch-wave-{2,3,4}-*.html; do
  lg=$(grep -cE 'linear-gradient' "$f")
  em=$(grep -cP '[\x{1F300}-\x{1F6FF}\x{2600}-\x{27BF}]' "$f")
  st=$(grep -cE 'status-(safe|danger|info|warn|fire)-bar|text-status-|bg-status-' "$f")
  [ "$lg$em$st" = "000" ] || echo "FAIL: $f lg=$lg em=$em st=$st"
done
```

**최종 SUMMARY commit (4번째, 본 plan 외 별도 — execute-plan workflow 가 처리):**
```
git commit -m "docs(quick-260526-n10): redesign/26-staff-manage W2~W4 3 sketch waves atomic SUMMARY"
```
</verification>

<success_criteria>
- 3 sketch HTML (sketch-wave-2-frame-guard-header / sketch-wave-3-staff-list / sketch-wave-4-modal-form-confirm) 평면 sibling 패턴으로 26-staff-manage/ 폴더에 존재
- 라인 추정 충족: W2 ≥150 / W3 ≥250 / W4 ≥300
- 3 atomic commit (W2 → W3 → W4 순서) 모두 main 직진 직선 history
- 4번째 SUMMARY commit (별도 — execute-plan workflow)
- src / components.css / App.tsx 3 commit 전체 0 byte 변경
- 9·10·11px 0건 (OQ #5 — 9→12, 10→12, 11→12 일률 격상)
- linear-gradient 0건 (OQ #2 — CTA 단색 유지)
- 이모지 0건 (§7.1 — Lucide 통일)
- `status-` prefix 0건 (memory `feedback_tailwind_token_class_pattern`)
- OQ # anchor 분포: W2 ≥3 (#4 #5 #6) / W3 ≥3 (#4 #5 #6) / W4 ≥6 (#1 #2 #3 #4 #5 #6)
- 비즈 anchor 주석 3 sketch 각 ≥1건 (admin 가드 / staffApi 4종 / useMutation 4 / ReplaceModal 2× update 시퀀스 / queryKey ['staff-list'] / rankOfTitle / 사번 10자리 정규식 / appointedAt 자동 채움 / shiftOffset+shiftFixed 비즈 데이터 / role 옵션 / window.__openReplaceModal / 카피 verbatim + placeholder 7건 + toast 8건 + ReplaceModal 카피)
- 메모리 슬러그 wave-1-index.md §5 mirror 인용 (재서술 X) + 특화 2건 (project_div_compressor_pair / project_meal_calc_rules)
</success_criteria>

<output>
After completion:
- 3 sketch HTML at cha-bio-safety/docs/redesign-context/26-staff-manage/sketch-wave-{2,3,4}-*.html
- 3 atomic commit on main (after e9462f8 W1 인덱스)
- SUMMARY at .planning/quick/260526-n10-redesign-26-staff-manage-w2-w4-sketch-wa/260526-n10-SUMMARY.md (별도 4번째 commit)
</output>
