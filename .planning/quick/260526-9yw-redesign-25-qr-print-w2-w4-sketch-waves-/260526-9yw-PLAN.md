---
phase: quick-260526-9yw
plan: 01
type: execute
wave: 1
depends_on: []
quick_id: 260526-9yw
branch: redesign/25-qr-print (HEAD 43cbd026a77e10f70221d2dd28a3af2485dfdb4e — W1 SUMMARY 직후)
date: 2026-05-26
autonomous: true
files_modified:
  - cha-bio-safety/docs/redesign-context/25-qr-print/sketch-wave-2-chrome-header.html
  - cha-bio-safety/docs/redesign-context/25-qr-print/sketch-wave-3-instructions-categories.html
  - cha-bio-safety/docs/redesign-context/25-qr-print/sketch-wave-4-download-actions.html
requirements:
  - redesign-25-qr-print-w2-chrome-header
  - redesign-25-qr-print-w3-instructions-categories
  - redesign-25-qr-print-w4-download-actions
must_haves:
  truths:
    - "W2~W4 sketch HTML 3개, 총 3 파일이 25-qr-print/ 직속 평면 배치로 생성된다 (sketch/ 서브폴더 절대 X — 28-splash 2q6 + 23-education o7b + 10-cctv-info + 27-login c6p + 17-annual-plan 0j3 + 16-workshift + 13-schedule + 14-reports 평면 패턴 mirror)"
    - "QRPrintPage.tsx (330 lines) src/** 코드 변경 0 byte (verify gate 마지막 task git diff)"
    - "OQ #1~#5 LOCKED 5건 모두 3 파일 안에 verbatim 반영 — #1 다운로드 버튼 그라데이션 default (a) public solid bg-status-safe-bar + inspect linear-gradient 유지 / #2 안내 박스 bg-status-info-bg + border-status-info-bar + text-text-secondary / #3 뒤로가기 Lucide ChevronLeft size={16} strokeWidth={2} text-text-secondary 치환 / #4 외곽 hex 토큰 치환 (--bg→--surface-page, --bg2→--surface-raised, --bg3→--surface-sunken, --bd→--border-default) / #5 비즈 anchor 1 byte 변경 금지 default"
    - "비즈 시그니처 anchor 9건 + 카피 verbatim 모두 3 파일 안에 박제 — CATEGORIES 배열 7건 (소화기 hasPublic true + 6건 false 순서 verbatim) + renderCardCanvas 비즈 로직 fence (scale 3 / PAD 3*S / FONT verbatim / topFontSize 12 / bottomFontSize 13 / maxQrW 0.85 / margin 1) + generatePdf 비즈 로직 fence (jsPDF a4 / MARGIN 10 / cardW inspect 30 public 70 / cardH inspect 38 public 90 / gap 1 / qrSize 0.65 / typeLabel '점검용/점검확인용' / 파일명 verbatim) + handleDownload (useAuthStore token / fetch `/api/checkpoints?category=...` / catLabel find / generatePdf 호출 / toast 카피) + baseUrl window.location.origin + `${baseUrl}/e/${cp.id}` 공개 URL + dlBtnStyle 함수 시그니처 + busy 3 state matrix (loading / public / 기본)"
    - "카피 verbatim 9건 — '항목별 QR 코드를 PDF 파일로 다운로드합니다.' + '소화기는 <b>점검용</b>(3×3 cm)과 <b>점검확인용</b>(7×9 cm)을 별도 파일로 다운로드할 수 있습니다.' + 'QR 코드 출력' + '점검용 QR PDF' + '점검확인용 QR PDF' + '생성 중...' + '체크포인트가 없습니다' + 'PDF 다운로드 완료' + 'PDF 생성 오류' + '본 소화기는 QR코드로 관리되며,/아래 QR코드로/점검 내역 확인 가능합니다.'"
    - "각 task 마다 atomic git commit 1개씩 — 총 3 commit (executor cherry-pick 사고 6회 precedent 회피용 commit 명령 verbatim 박제, 28-splash 2q6 + 23-education o7b + 10-cctv-info 패턴 동일)"
    - "평면 배치 — 3 파일 모두 cha-bio-safety/docs/redesign-context/25-qr-print/ 직속 (sketch/ 서브폴더 절대 X)"
    - "negative gate 3 sketch 모두 통과 (이모지 0 — 뒤로가기 SVG → Lucide ChevronLeft / 9·10·11px 0 / status- prefix 0 / w-8 h-8 0 / 옛 alias 토큰 0 / linear-gradient T3 만 inspect 버튼 anchor)"
  artifacts:
    - path: "cha-bio-safety/docs/redesign-context/25-qr-print/sketch-wave-2-chrome-header.html"
      provides: "외곽 wrapper (height:100% / flex column / overflow hidden / bg-surface-page) + 자체 헤더 (bg-surface-raised + border-b border-border-default + flex gap 10 + padding '8px 12px' + flexShrink 0) + 뒤로가기 버튼 (34x34 rounded-sm bg-surface-sunken border-border-default + Lucide ChevronLeft size={16} strokeWidth={2} text-text-secondary, OQ #3 LOCKED) + 헤더 타이틀 ('QR 코드 출력' 16/700 text-text-primary leading-none, OQ #3 폰트 16 격상 검토 — 15→16 노안 친화) + 빈/로딩/오류 frame placeholder — 4 frame: 모바일 다크 정상 / 모바일 라이트 정상 / 모바일 다크 빈 상태 (CATEGORIES 0건 가정) / 모바일 라이트 로딩 (busy 가짜 상태)"
      contains: "data-theme dark+light / bg-surface-page 외곽 / bg-surface-raised 헤더 (OQ #4 LOCKED) / border-b border-border-default borderBottom (OQ #4 LOCKED) / 뒤로가기 button 34x34 (w-[34px] h-[34px] arbitrary — w-8 함정 회피 memory `feedback_tailwind_w8_h8_is_48px`) bg-surface-sunken border border-border-default rounded-sm / Lucide ChevronLeft size={16} strokeWidth={2} text-text-secondary (OQ #3 LOCKED, 인라인 SVG path `M15 19l-7-7 7-7` 폐기) / 타이틀 'QR 코드 출력' verbatim (text-body fontWeight 700 text-text-primary leading-none — 16/700) / flex gap 10 padding '8px 12px' flexShrink 0 / 빈/로딩/오류 placeholder (W3/W4 sketch 에서 실제 데이터 채움) / 비즈 anchor 박제 fence (외곽 wrapper line 253 + 헤더 line 256~263 verbatim TSX 원본) / OQ #3 + OQ #4 citation / 평면 배치 anchor / 4 frame matrix (다크/라이트 × 정상/빈/로딩)"
      min_lines: 280
    - path: "cha-bio-safety/docs/redesign-context/25-qr-print/sketch-wave-3-instructions-categories.html"
      provides: "안내 박스 (bg-status-info-bg + border border-status-info-bar + rounded-md + padding '10px 13px' + text-caption text-text-secondary lineHeight 1.6, OQ #2 LOCKED) + 안내 카피 verbatim ('항목별 QR 코드를 PDF 파일로 다운로드합니다.' + '소화기는 <b>점검용</b>(3×3 cm)과 <b>점검확인용</b>(7×9 cm)을 별도 파일로 다운로드할 수 있습니다.') + 카테고리 목록 wrapper (flex-1 overflow-y-auto padding '10px 14px 20px') + 카테고리 column (flex column gap 10) + CATEGORIES 7건 카드 (bg-surface-raised + border border-border-default + rounded-md + padding '13px 14px') + 카드 타이틀 (text-body-sm font-bold text-text-primary marginBottom 10, 14→14 보존) + 버튼 row placeholder (flex gap 8 flex-wrap, W4 sketch 에서 다운로드 버튼 채움) — 4 frame: 다크 모바일 7 카드 정상 / 라이트 모바일 7 카드 정상 / 다크 모바일 안내 박스 단독 강조 / 라이트 모바일 카드 hover 상태 (border-border-strong)"
      contains: "data-theme dark+light / 안내 박스 padding '10px 14px 4px' wrapper flexShrink 0 / 안내 박스 자체 bg-status-info-bg border border-status-info-bar rounded-md padding '10px 13px' text-caption (12) text-text-secondary leading-relaxed (lineHeight 1.6, OQ #2 LOCKED — `rgba(14,165,233,.08/.25)` 인라인 폐기) / 안내 카피 2줄 verbatim — '항목별 QR 코드를 PDF 파일로 다운로드합니다.' + '소화기는 <b>점검용</b>(3×3 cm)과 <b>점검확인용</b>(7×9 cm)을 별도 파일로 다운로드할 수 있습니다.' (`<b>` 강조 위치 verbatim, line 268~269) / 카테고리 목록 wrapper flex-1 overflow-y-auto padding '10px 14px 20px' / 카테고리 column flex column gap 10 / CATEGORIES.map 카드 7건 verbatim (소화기 hasPublic true + 소화전/DIV/청정소화약제/완강기/전실제연댐퍼/방화셔터 hasPublic false, key=cat.value, line 17~25 박제 fence) bg-surface-raised border border-border-default rounded-md padding '13px 14px' (OQ #4 LOCKED) / 카드 타이틀 cat.label verbatim text-body-sm font-bold text-text-primary leading-none marginBottom 10 (14/700) / 버튼 row placeholder flex gap 8 flex-wrap (W4 에서 inspect + public 버튼 채움) / 비즈 anchor 박제 fence (CATEGORIES 배열 + 카드 wrapper line 274~306 verbatim) / OQ #2 + OQ #4 citation / 평면 배치 anchor / 4 frame matrix"
      min_lines: 320
    - path: "cha-bio-safety/docs/redesign-context/25-qr-print/sketch-wave-4-download-actions.html"
      provides: "다운로드 버튼 — inspect (point '점검용 QR PDF' 또는 '생성 중...', busy === inspKey, dlBtnStyle(busy, false), 인라인 linear-gradient(135deg,#1d4ed8,#0ea5e9) 또는 `bg-accent text-text-on-accent` 토큰) + public (소화기만 cat.hasPublic 분기, '점검확인용 QR PDF' 또는 '생성 중...', busy === publicKey, dlBtnStyle(busy, true), OQ #1 LOCKED default (a) **solid `bg-status-safe-bar text-text-on-accent` 치환** — public 그라데이션 폐기) + loading 상태 (`bg-border-strong text-text-tertiary cursor-default`) + PDF 생성 진행 표시 ('생성 중...' 텍스트 verbatim) + 인쇄 모드 분기 (inspect 세로 30x38mm / public 가로 70x90mm 카드 미리보기 박제 fence) — 4 frame: 다크 모바일 소화기 카드 2 버튼 (inspect 기본 + public solid) / 다크 모바일 소화기 카드 inspect 로딩 / 라이트 모바일 소화전 카드 1 버튼 inspect 만 / 라이트 데스크톱 7 카드 매트릭스 (전체 카테고리 버튼 상태)"
      contains: "data-theme dark+light / 점검용 버튼 (onClick handleDownload(cat.value,'inspect') disabled={!!busy} padding '9px 16px' rounded-sm text-caption font-bold leading-none color text-text-on-accent gap 6 — label 'busy === inspKey ? \"생성 중...\" : \"점검용 QR PDF\"' verbatim) + 인라인 `style={{ background: 'linear-gradient(135deg, #1d4ed8, #0ea5e9)' }}` (T3 만 linear-gradient negative gate 예외 anchor — inspect 버튼 ≥3 박제 / design-system §6.4 '저장/CTA 버튼' 그라데이션 룰 일치) / 점검확인용 버튼 (cat.hasPublic && — 소화기만 line 293, onClick handleDownload(cat.value,'public') disabled={!!busy} padding '9px 16px' rounded-sm text-caption font-bold leading-none color text-text-on-accent — label 'busy === publicKey ? \"생성 중...\" : \"점검확인용 QR PDF\"' verbatim) + **OQ #1 LOCKED default (a) solid `bg-status-safe-bar`** — `linear-gradient(135deg,#16a34a,#22c55e)` 폐기 사유 fence (design-system §6.4 '유일한 그라디언트 2종' 위반) / loading 상태 (busy === key 시 `bg-border-strong text-text-tertiary cursor-default opacity 0.5`, dlBtnStyle line 312~330 verbatim 박제 fence — `var(--bd2)` → `bg-border-strong`, `var(--t3)` → `text-text-tertiary`) / busy 3 state matrix (loading / public / 기본 분기 표) / inspect 카드 미리보기 박제 fence — 30x38mm scale 3 / qrSize Math.floor(30 * 0.65) = 19mm / bottomLines [locationNo??id, location, floor] / bottomFontSize 3 (line 174~186) / public 카드 미리보기 박제 fence — 70x90mm landscape / qrSize Math.floor(70 * 0.65) = 45mm / topLines '본 소화기는 QR코드로 관리되며,/아래 QR코드로/점검 내역 확인 가능합니다.' verbatim (line 193~197) / topFontSize 3 / bottomFontSize 3 (line 189~205) / handleDownload anchor (line 229~250 verbatim) — useAuthStore token + fetch `/api/checkpoints?category=${encodeURIComponent(category)}` + Authorization Bearer + json.success / json.data?.length 체크 + catLabel = CATEGORIES.find(c => c.value === category)?.label ?? category + generatePdf 호출 + toast.success 'PDF 다운로드 완료' + toast.error 'PDF 생성 오류' + '체크포인트가 없습니다' + finally setBusy(null) / generatePdf 비즈 fence (jsPDF unit 'mm' format 'a4' / isLandscape = type === 'public' / PAGE_W 210 or 297 / PAGE_H 297 or 210 / MARGIN 10 / cardW inspect 30 public 70 / cardH inspect 38 public 90 / gap 1 / cols/rows Math.floor / setFillColor 255,255,255 / setDrawColor 150,150,150 / setLineWidth 0.2 / typeLabel '점검용/점검확인용' / 파일명 `${categoryLabel}_${typeLabel}_QR.pdf` verbatim line 218) / renderCardCanvas 비즈 fence (scale 3 / PAD 3*S / FONT `-apple-system, \"Apple SD Gothic Neo\", \"Malgun Gothic\", \"Noto Sans KR\", sans-serif` verbatim / topFontSize 12 / bottomFontSize 13 / topH 계산 / bottomH 계산 / maxQrW Math.floor(W * 0.85) / qrDrawSize Math.min(Math.max(availH, 20 * S), maxQrW) / QRCode.toCanvas margin 1 / fillStyle '#ffffff' bg / '#222222' top / '#333333' bottom / textAlign center / textBaseline top / 첫 하단 라인 'bold ' 추가) / OQ #1 + OQ #5 citation / 평면 배치 anchor / 4 frame matrix"
      min_lines: 360
  key_links:
    - from: "wave-1-index.md §7 OQ #1 LOCKED default (a) — public 버튼 그라데이션 폐기 solid bg-status-safe-bar 치환"
      to: "sketch-wave-4-download-actions.html"
      via: "dlBtnStyle isPublic=true 분기 (line 320 `linear-gradient(135deg,#16a34a,#22c55e)`) → `bg-status-safe-bar text-text-on-accent` solid 치환. inspect 버튼 (line 321 `linear-gradient(135deg,#1d4ed8,#0ea5e9)`) 만 디자인 §6.4 '저장/CTA 버튼' 그라데이션 anchor 유지 (≥3 박제). loading 상태 `var(--bd2)` → `bg-border-strong text-text-tertiary cursor-default`. T4 sketch 만 linear-gradient negative gate 예외."
      pattern: "bg-status-safe-bar|linear-gradient\\(135deg, ?#1d4ed8, ?#0ea5e9\\)"
    - from: "wave-1-index.md §7 OQ #2 LOCKED default (a) — 안내 박스 bg-status-info-bg + border-status-info-bar 치환"
      to: "sketch-wave-3-instructions-categories.html"
      via: "안내 박스 (line 267 `bg:rgba(14,165,233,.08) / border:rgba(14,165,233,.25)`) → `bg-status-info-bg border border-status-info-bar` tokens.css 알리아스 치환. color `var(--t2)` → `text-text-secondary`. lineHeight 1.6 → `leading-relaxed`. 인라인 rgba 폐기 0건."
      pattern: "bg-status-info-bg|border-status-info-bar"
    - from: "wave-1-index.md §7 OQ #3 LOCKED default (a) — 뒤로가기 Lucide ChevronLeft size={16} 치환"
      to: "sketch-wave-2-chrome-header.html"
      via: "뒤로가기 인라인 SVG (line 258~260 width=15 height=15 stroke `var(--t2)` strokeWidth 2 path `M15 19l-7-7 7-7`) → `<ChevronLeft size={16} strokeWidth={2} className=\"text-text-secondary\" />` Lucide 교체 (size={16} = line 258 width=15 와 가장 근접). 뒤로가기 button 34x34 (w-[34px] h-[34px] arbitrary — w-8 함정 회피 memory `feedback_tailwind_w8_h8_is_48px`) bg-surface-sunken border border-border-default rounded-sm. import 추가: `import { ChevronLeft } from 'lucide-react'`."
      pattern: "ChevronLeft|lucide-react"
    - from: "wave-1-index.md §7 OQ #4 LOCKED default (a) — 외곽 hex 토큰 치환 design-system §4.1 마이그레이션"
      to: "sketch-wave-2-chrome-header.html + sketch-wave-3-instructions-categories.html"
      via: "외곽 wrapper `var(--bg)` (line 253) → `bg-surface-page`. 헤더 `var(--bg2)` (line 256) → `bg-surface-raised`. 헤더 borderBottom `var(--bd)` → `border-b border-border-default`. 뒤로가기 button `var(--bg3)` (line 257) → `bg-surface-sunken`. 카드 wrapper `var(--bg2)` (line 280) → `bg-surface-raised`. 카드 border `var(--bd)` → `border border-border-default`. 헤더 타이틀 `var(--t1)` → `text-text-primary`. 카드 타이틀 `var(--t1)` → `text-text-primary`. 옛 alias 0건 (sketch body 안)."
      pattern: "bg-surface-page|bg-surface-raised|bg-surface-sunken|border-border-default"
    - from: "wave-1-index.md §7 OQ #5 LOCKED default (a) — 비즈 anchor §1.3 전체 1 byte 변경 금지"
      to: "sketch-wave-3-instructions-categories.html + sketch-wave-4-download-actions.html"
      via: "CATEGORIES 배열 7건 (line 17~25 순서/value/label/hasPublic 박제 fence) + renderCardCanvas 비즈 로직 (line 29~119 scale 3 / PAD / FONT / topFontSize / bottomFontSize / maxQrW / qrDrawSize / fillStyle 색 / textAlign / textBaseline / 첫 하단 'bold ' 박제 fence) + generatePdf 비즈 로직 (line 122~219 jsPDF a4 / MARGIN 10 / cardW inspect 30 public 70 / cardH inspect 38 public 90 / gap 1 / cols/rows / qrSize Math.floor(cardW * 0.65) / typeLabel / 파일명 박제 fence) + handleDownload (line 229~250 useAuthStore token + fetch + json.success + catLabel + generatePdf 호출 + toast 박제 fence) + dlBtnStyle (line 312~330 loading/public/기본 3 state 박제 fence) + baseUrl window.location.origin + 카피 verbatim 9건. 모두 sketch fence 박스 안에 인용 — sketch 본문 안 변경 안 함."
      pattern: "CATEGORIES|renderCardCanvas|generatePdf|handleDownload|dlBtnStyle"
  scope_negatives:
    - "sketch HTML 3개 만 산출. TSX 변환은 W5 별도 wave (본 PLAN 범위 외)"
    - "QRPrintPage.tsx + App.tsx 등 src/** 0 byte 변경 (verify gate 최종 git diff)"
    - "wrangler 명령 절대 X / npm run deploy 절대 X (CLAUDE.local.md + memory `feedback_cbc7119_design_never_wrangler`)"
    - "옛 alias 토큰 (var(--bg/bg2/bg3/bd/bd2/t1/t2/t3)) sketch 본문 안 0 (tokens.css 정의 인용 fence 안 예외)"
    - "status- prefix 0 (bg-status-safe / text-status-danger 등 className 사용 시 verify FAIL — bg-status-info-bg / border-status-info-bar / bg-status-safe-bar 는 status-info-bg/status-info-bar/status-safe-bar 가 단일 토큰명이므로 예외)"
    - "이모지 0 (sketch 본문 — 뒤로가기 SVG → Lucide ChevronLeft 교체 anchor 만 텍스트 인용)"
    - "w-8 / h-8 className 0 (48px 함정, memory `feedback_tailwind_w8_h8_is_48px` — 34x34 뒤로가기 → w-[34px] h-[34px] arbitrary)"
    - "9·10·11 fontSize 인라인 0 (text-caption 12 leading-none 마지노선, OQ #3 LOCKED 노안 친화 §1.1)"
    - "linear-gradient — T1/T2 = 0건 / **T3 만 예외 anchor ≥3** (OQ #1 LOCKED — inspect 버튼 `linear-gradient(135deg, #1d4ed8, #0ea5e9)` 만, public 그라데이션 폐기)"
    - "sketch/ 서브폴더 절대 금지 — 3 파일 모두 25-qr-print/ 직속 평면 배치"
    - "TSX 변환 wave (W5) 는 별도 turn — 본 PLAN 에서 QRPrintPage.tsx 1 byte 도 수정 금지"
    - "components.css 변경 0 — 14-reports 기존 components.css 재사용만, 신규 추가 0 (W1 §4 박제)"
    - "App.tsx / MOBILE_NO_NAV_PATHS 라우팅 변경 0 — `/qr-print` 이미 등재"
    - "CATEGORIES 7건 / hasPublic 분기 변경 0 — 비즈 데이터, sketch 에서 결정/변경 불가"
    - "renderCardCanvas / generatePdf 비즈 로직 변경 0 — W5 에서도 변경 금지. 본 wave 는 박제 fence 만"
---

<objective>
redesign/25-qr-print 의 W1 인덱스(`wave-1-index.md`, 484 lines, OQ 5건 default LOCKED 완료 2026-05-26 9nd)를 기반으로
W2~W4 3 sub-wave 산출물을 **단일 quick task** 안에서 **atomic 3 commit** 으로 한 번에 만든다.

- **W2 (T1):** 외곽 wrapper + 자체 헤더 + 빈/로딩/오류 placeholder — 4 frame (모바일 다크/라이트 정상 + 빈 + 로딩) — `sketch-wave-2-chrome-header.html`
- **W3 (T2):** 안내 박스 (OQ #2 status-info 토큰 치환) + CATEGORIES 7건 카드 grid (OQ #4 외곽 hex 토큰 치환) + 카드 타이틀 + 버튼 row placeholder (W4 에서 채움) — 4 frame — `sketch-wave-3-instructions-categories.html`
- **W4 (T3):** 다운로드 버튼 (inspect linear-gradient anchor + public OQ #1 solid 치환) + PDF 생성 진행 표시 + 인쇄 모드 분기 (inspect 30x38mm 세로 / public 70x90mm 가로 카드 미리보기 박제 fence) + dlBtnStyle 3 state matrix + handleDownload + generatePdf + renderCardCanvas 비즈 anchor fence — 4 frame — `sketch-wave-4-download-actions.html`

**Purpose:** 28-splash 2q6 / 23-education o7b / 10-cctv-info / 27-login c6p / 17-annual-plan 0j3 / 16-workshift / 13-schedule / 14-reports 등 평면(flat sibling) 패턴으로 25-qr-print sketch 단계 3 wave 분량을 한 turn 에 처리. QRPrintPage 가 330 lines 단일 export (외부 컴포넌트 import 없음, jsPDF + QRCode 라이브러리 직접 사용) + 자체 헤더 페이지 (`/qr-print` ∈ MOBILE_NO_NAV_PATHS, BottomNav 숨김) + 4 sub-area (외곽+헤더 / 안내 박스 / 카테고리 카드 / 다운로드 버튼+PDF 생성) + canvas → jsPDF 종이 인쇄 파이프라인 비즈 (renderCardCanvas line 29~119 + generatePdf line 122~219, scale 3 / cardW mm / qrSize ratio 0.65 / margin 10mm / gap 1mm / FONT Apple SD Gothic Neo / 색 fillStyle / 첫 하단 'bold ' / 파일명 `${categoryLabel}_${typeLabel}_QR.pdf` 1 byte 변경 금지) + CATEGORIES 7건 (소화기 hasPublic true + 6건 false) + handleDownload (raw fetch + Authorization Bearer + toast) + dlBtnStyle 3 state — 본 PLAN 에서 sketch 3 wave 한 번에 처리해 cbc7119-preview 자동 배포 사이클 1회로 사용자 시각 검수 가능하게 함.

**Output:** `cha-bio-safety/docs/redesign-context/25-qr-print/` 직속 3 파일 (sketch HTML 3개).
`QRPrintPage.tsx` (330 lines) + App.tsx 등 **src/** 파일 모두** 코드 변경 0 byte (verify gate 마지막 task 에서 확인).

**28-splash 2q6 / 23-education o7b mirror:** 본 PLAN 은 28-splash 2q6 PLAN + 23-education o7b PLAN (4 atomic 패턴) 의 mirror — 25-qr-print 는 단순 페이지로 3 sub-wave 가 자연스러움 (W1 §2.1 표 박제 — 4-sub-wave 분할은 안내 박스 단독 분리할 정도의 디자인 결정 부담 없음, W3 에 통합). 28-splash + 23-education + 10-cctv-info 와 차이:
- (1) 단일 export 페이지 (외부 컴포넌트 0, jsPDF + QRCode 라이브러리 직접 사용 — 18-worklog/13-schedule 의 외부 컴포넌트 분리 패턴과 다름)
- (2) 자체 헤더 페이지 (`MOBILE_NO_NAV_PATHS` 등재 — 점검 시리즈 02+06 chrome 룰 직접 적용 X)
- (3) canvas → jsPDF 종이 인쇄 비즈 파이프라인 (renderCardCanvas + generatePdf 의 모든 px/mm/scale/색/카피 1 byte 변경 금지 — 15-daily-report SW3 캘리브 좌표 시스템 보존 룰 일반화)
- (4) 다운로드 버튼 3 state matrix (loading / public / 기본) — busy state + dlBtnStyle 함수 (line 312~330) + isPublic 분기

**중요 — executor commit 부모 머지 사고 회피:** 28-splash 2q6 + 23-education o7b + 10-cctv-info 등 다수 quick 에서 executor worktree commit 이 부모 브랜치로 자동 머지 안 되어 cherry-pick 복구 필요했음 (6회 재현). **executor 는 worktree 안에서 `git commit` 후 명시적 보고 필수** (commit hash + message 1줄) + orchestrator 가 필요 시 자동 cherry-pick 으로 복구한다. 본 PLAN 각 task 의 commit 명령은 명시 박제.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jykevin/Documents/cbc7119-design/CLAUDE.md
@/Users/jykevin/Documents/cbc7119-design/CLAUDE.local.md

# W1 인덱스 (단일 진입점, OQ 5건 default LOCKED 완료 2026-05-26 9nd, 484 lines)
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/25-qr-print/wave-1-index.md

# Source TSX (sketch 대상, 변경 0 — 1 파일 untouched)
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/src/pages/QRPrintPage.tsx

# 25-qr-print redesign 컨텍스트 (토큰/타이포 source, design-system v0.1.1)
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/25-qr-print/25-qr-print.md
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/25-qr-print/design-system.md
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/25-qr-print/tokens.css
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/25-qr-print/typography.css

# 평면 패턴 mirror precedent
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/28-splash/
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/23-education/
</context>

<rules_verbatim>
### OQ LOCKED 5건 (wave-1-index.md §7 default 답 verbatim, 사용자 "다음 진행" 으로 LOCKED 2026-05-26)

**OQ #1 (다운로드 버튼 그라데이션 처리 — default (a) public solid 치환):**
> LOCKED (2026-05-26): `dlBtnStyle` public 분기 그라데이션 `linear-gradient(135deg,#16a34a,#22c55e)` 은 design-system §6.4 "유일한 그라디언트 2종" 위반. **default (a) solid `bg-status-safe-bar text-text-on-accent` 치환**. inspect 분기 그라데이션 `linear-gradient(135deg,#1d4ed8,#0ea5e9)` 은 §6.4 "저장/CTA 버튼" 그라데이션 anchor 유지 (≥3 박제). 토큰 채택 X (linear-gradient negative gate 의 T3 만 예외 anchor — inspect 버튼). loading 상태 `var(--bd2)` → `bg-border-strong text-text-tertiary cursor-default`. dlBtnStyle 함수 시그니처 (line 312~330) 보존 — sketch 본문 안 박제 fence.

**OQ #2 (안내 박스 색 토큰화 — default (a) status-info 알리아스 치환):**
> LOCKED (2026-05-26): 안내 박스 (line 267 `bg:rgba(14,165,233,.08) / border:rgba(14,165,233,.25) / color:var(--t2)`) → **default (a) `bg-status-info-bg border border-status-info-bar text-text-secondary` 치환**. tokens.css 알리아스 사용. lineHeight 1.6 → `leading-relaxed`. 25-qr-print.md §4 요구사항 ("페이지 화면만 토큰화") 일치. 인라인 `rgba(14,165,233,.08/.25)` 폐기 0건.

**OQ #3 (뒤로가기 Lucide ChevronLeft 치환 — default (a) size={16}):**
> LOCKED (2026-05-26): 뒤로가기 인라인 SVG (line 258~260 width=15 height=15 stroke `var(--t2)` strokeWidth=2 path `M15 19l-7-7 7-7`) → **default (a) `<ChevronLeft size={16} strokeWidth={2} className="text-text-secondary" />`** Lucide 교체 (size={16} = line 258 width=15 와 가장 근접). 02+06 chrome 룰의 자체 헤더 통일 영역. import 추가: `import { ChevronLeft } from 'lucide-react'`. 뒤로가기 button 34x34 (w-[34px] h-[34px] arbitrary — w-8 함정 회피 memory `feedback_tailwind_w8_h8_is_48px`) bg-surface-sunken border border-border-default rounded-sm 유지.

**OQ #4 (외곽 hex 토큰 치환 범위 — default (a) design-system §4.1 마이그레이션 표 그대로):**
> LOCKED (2026-05-26): `var(--bg)/--bg2/--bg3/--bd/--bd2/--t1/--t2/--t3` raw 변수 모두 새 토큰 치환 — **default (a) design-system.md §4.1 마이그레이션 표 그대로 적용**. `--bg` → `--surface-page` (외곽 wrapper line 253) / `--bg2` → `--surface-raised` (헤더 line 256 + 카드 line 280) / `--bg3` → `--surface-sunken` (뒤로가기 button line 257) / `--bd` → `--border-default` (헤더 borderBottom + 카드 border) / `--bd2` → `--border-strong` (loading button bg) / `--t1` → `--text-primary` (헤더 타이틀 + 카드 타이틀) / `--t2` → `--text-secondary` (안내 박스 color + 뒤로가기 stroke) / `--t3` → `--text-tertiary` (loading button color). 25-qr-print.md §4 요구사항 일치.

**OQ #5 (비즈 anchor 보존 확인 — default (a) §1.3 전체 1 byte 변경 금지):**
> LOCKED (2026-05-26): wave-1-index.md §1.3 박스의 모든 식별자/값 (CATEGORIES 7건 / renderCardCanvas / generatePdf / handleDownload / 카피 verbatim 9건 / 종이 인쇄 px 값 / mm 값 / scale 3 등) 모두 W5 TSX 변환에서 1 byte 변경 금지 **default**. 본 wave (W2~W4 sketch) 는 비즈 anchor 박제 fence 만 — sketch 본문 안 변경 안 함. 사용자가 OQ #1~#4 외 추가 변경 요청 시 별도 처리 (default (a) — 변경 안 함).

### 디자인 토큰 패턴 (wave-1-index.md §3 + planning constraints verbatim)
- `bg-surface-page` (var(--bg) → --surface-page, 페이지 외곽 line 253)
- `bg-surface-raised` (var(--bg2) → --surface-raised, 헤더 line 256 + 카드 line 280)
- `bg-surface-sunken` (var(--bg3) → --surface-sunken, 뒤로가기 button line 257)
- `border-border-default` (var(--bd) → --border-default, 헤더 borderBottom + 카드 border)
- `border-border-strong` (var(--bd2) → --border-strong, loading button bg)
- `text-text-primary` (var(--t1) → --text-primary, 헤더 타이틀 + 카드 타이틀)
- `text-text-secondary` (var(--t2) → --text-secondary, 안내 박스 + 뒤로가기 stroke)
- `text-text-tertiary` (var(--t3) → --text-tertiary, loading button color)
- `bg-status-info-bg` + `border-status-info-bar` (안내 박스 OQ #2 LOCKED)
- `bg-status-safe-bar` + `text-text-on-accent` (public 버튼 OQ #1 LOCKED solid)
- inspect 버튼 `style={{ background: 'linear-gradient(135deg, #1d4ed8, #0ea5e9)' }}` 인라인 그라데이션 (T3 만 예외 anchor ≥3)
- radius: `rounded-sm` 8/9/10 / `rounded-md` 12/13

### Negative rule (모든 task verify gate)
- 이모지 0건 (sketch 본문, 뒤로가기 SVG → Lucide ChevronLeft 교체 anchor 만 텍스트 인용)
- `linear-gradient` — T1/T2 = 0건 / **T3 만 예외 anchor ≥3** (OQ #1 LOCKED inspect 버튼 `linear-gradient(135deg, #1d4ed8, #0ea5e9)` 박제)
- 9&#183;10&#183;11px fontSize 0건 (text-caption 12 leading-none 마지노선, OQ #3 LOCKED §1.1 노안 룰)
- `(text|bg|border)-status-` className 0건 in sketch body (`bg-status-info-bg` / `border-status-info-bar` / `bg-status-safe-bar` 는 단일 토큰명이므로 예외 — design-system §2.3 verbatim, memory `feedback_tailwind_token_class_pattern`)
- `\b(w|h)-8\b` 0건 (tailwind config w&#8209;8=48 함정, memory `feedback_tailwind_w8_h8_is_48px` — 34x34 뒤로가기 → w-[34px] h-[34px] arbitrary)
- 옛 alias 토큰 (`var(--bg)` `var(--bg2)` `var(--bg3)` `var(--bd)` `var(--bd2)` `var(--t1)` `var(--t2)` `var(--t3)`) 0건 in sketch body (tokens.css 정의 인용 fence 안 예외)

### 평면 폴더 패턴 (28-splash + 23-education + 10-cctv-info + 27-login + 17-annual-plan + 16-workshift + 13-schedule + 14-reports 8 페이지 일관)
- 25-qr-print/ 직속에 3 파일 배치
- `sketch/` 서브폴더 절대 금지

### ★ 비즈 anchor 9건 (1 byte 변경 금지, W2~W4 모두 박제 fence)

**[CATEGORIES 7건 배열 (line 17~25, 순서 변경 금지)]**
1. `{ value: '소화기',      label: '소화기',      hasPublic: true  }`
2. `{ value: '소화전',      label: '소화전',      hasPublic: false }`
3. `{ value: 'DIV',         label: 'DIV',         hasPublic: false }`
4. `{ value: '청정소화약제', label: '청정소화약제', hasPublic: false }`
5. `{ value: '완강기',      label: '완강기',      hasPublic: false }`
6. `{ value: '전실제연댐퍼', label: '전실제연댐퍼', hasPublic: false }`
7. `{ value: '방화셔터',    label: '방화셔터',    hasPublic: false }`

**[renderCardCanvas 비즈 로직 (line 29~119, 변경 금지)]**
- scale 기본 3 / PAD = 3 * S (상하 여백)
- FONT = `-apple-system, "Apple SD Gothic Neo", "Malgun Gothic", "Noto Sans KR", sans-serif`
- topFontSize 기본 12 / bottomFontSize 기본 13
- topH = topLines.length * (fs + 2*S) + 2*S / bottomH = 첫 +1 나머지 -1 fontSize 누적
- availH = H - PAD*2 - topH - bottomH - 4*S / maxQrW = Math.floor(W * 0.85)
- qrDrawSize = Math.min(Math.max(availH, 20*S), maxQrW)
- QRCode.toCanvas margin 1 / fillStyle '#ffffff' bg / '#222222' top / '#333333' bottom
- textAlign 'center' / textBaseline 'top' / 첫 하단 'bold ' 추가
- return canvas.toDataURL('image/png')

**[generatePdf 비즈 로직 (line 122~219, 변경 금지)]**
- if (points.length === 0) toast.error('체크포인트가 없습니다')
- isLandscape = type === 'public' / jsPDF unit 'mm' format 'a4' orientation
- PAGE_W portrait 210 / landscape 297 / PAGE_H portrait 297 / landscape 210 / MARGIN 10
- cardW inspect 30 / public 70 / cardH inspect 38 / public 90 / gap 1
- cols/rows Math.floor / offsetX/Y 중앙정렬 / setFillColor 255,255,255 / rect 'FD'
- setDrawColor 150,150,150 / setLineWidth 0.2 / rect 외곽
- inspect: qrValue = cp.id / qrSize = Math.floor(cardW * 0.65) / bottomLines [locationNo??id, location, floor] / bottomFontSize 3
- public:  qrValue = `${baseUrl}/e/${cp.id}` / qrSize 동일 / topLines 3 verbatim ('본 소화기는 QR코드로 관리되며,' / '아래 QR코드로' / '점검 내역 확인 가능합니다.') / topFontSize 3 / bottomLines 동일 / bottomFontSize 3
- addImage 'PNG' / setFillColor 0,0,0 + setLineWidth 0.3 + rect 'S' (테두리 재 그리기)
- doc.save(`${categoryLabel}_${typeLabel}_QR.pdf`) — typeLabel inspect '점검용' / public '점검확인용'

**[handleDownload 비즈 (line 229~250, 변경 금지)]**
- key = `${category}-${type}` / setBusy(key)
- fetch(`/api/checkpoints?category=${encodeURIComponent(category)}`, headers: { Authorization: `Bearer ${token}` })
- if (!json.success || !json.data?.length) toast.error('체크포인트가 없습니다')
- catLabel = CATEGORIES.find(c => c.value === category)?.label ?? category
- await generatePdf(json.data, type, baseUrl, catLabel)
- toast.success('PDF 다운로드 완료') / catch: toast.error('PDF 생성 오류') / console.error
- finally: setBusy(null)

**[dlBtnStyle 함수 (line 312~330, 변경 금지)]**
- padding '9px 16px' / borderRadius 9 / border none
- loading: `var(--bd2)` → `bg-border-strong` / isPublic: solid OQ #1 LOCKED `bg-status-safe-bar` / 기본: `linear-gradient(135deg,#1d4ed8,#0ea5e9)` 그라데이션 유지
- color: loading `var(--t3)` → `text-text-tertiary` / 활성 `#fff` → `text-text-on-accent`
- fontSize 12 → `text-caption leading-none font-bold`
- cursor loading `default` / 활성 `pointer` / display flex / alignItems center / gap 6

**[useAuthStore + baseUrl + busy state]**
- `const { token } = useAuthStore()` (line 224)
- `const baseUrl = window.location.origin` (line 227)
- `const [busy, setBusy] = useState<string | null>(null)` (line 225 — 3 state matrix: null / inspKey / publicKey)

**[카피 verbatim 9건]**
- 'QR 코드 출력' (헤더 타이틀, line 262)
- '항목별 QR 코드를 PDF 파일로 다운로드합니다.' (안내 1줄, line 268)
- '소화기는 <b>점검용</b>(3×3 cm)과 <b>점검확인용</b>(7×9 cm)을 별도 파일로 다운로드할 수 있습니다.' (안내 2줄, line 269)
- '점검용 QR PDF' / '생성 중...' (inspect 버튼, line 289)
- '점검확인용 QR PDF' / '생성 중...' (public 버튼, line 299)
- '체크포인트가 없습니다' (toast.error, line 128 + line 238)
- 'PDF 다운로드 완료' (toast.success, line 243)
- 'PDF 생성 오류' (toast.error, line 245)
- '본 소화기는 QR코드로 관리되며,\n아래 QR코드로\n점검 내역 확인 가능합니다.' (renderCardCanvas topLines, line 194~196)
- 파일명 `${categoryLabel}_${typeLabel}_QR.pdf` (generatePdf save, line 218)
- '점검용' / '점검확인용' typeLabel (line 217)

**[기타 anchor]**
- baseUrl = window.location.origin / `${baseUrl}/e/${cp.id}` 공개 URL prefix
- `staff?.role === 'admin'` 분기 없음 (본 페이지에 admin role 분기 없음 — 모든 사용자 동일 UI)
- useAuthStore raw fetch + Authorization Bearer 직접 인젝션 패턴

### 메모리 룰 inline 12+ (wave-1-index.md §5 cross-ref)
1. `feedback_design_sketch_first` — sketch HTML 시안 + 사용자 컨펌 후 인라인 적용
2. `feedback_design_changes_ask_first` — 레이아웃/색/그라데이션 변경 OQ 거쳐 컨펌
3. `feedback_redesign_sketch_rule_enforcement` — §6.2/§6.3/§7.1 negative rule + 일관성 4중 강화
4. `feedback_sketch_realistic_data` — 표시 분기/라벨 룰 코드 그대로 (CATEGORIES 7건 / hasPublic 분기 / busy 3 state)
5. `feedback_tsx_wave_stat_card_drift` — sketch 새 패턴 누락 가능, plan verbatim 인용 + verify gate
6. `feedback_planner_prompt_sketch_verbatim` — 변환 wave 시 sketch CSS 정의 verbatim 인용
7. `feedback_tailwind_token_class_pattern` — status- prefix 없음 (`bg-status-info-bg` 단일 토큰명은 예외) + lucide `size={N}` prop
8. `feedback_tailwind_w8_h8_is_48px` — w-8 함정 회피 (34x34 뒤로가기 → w-[34px] h-[34px] arbitrary)
9. `feedback_text_caption_leading_none` — 작은 컨테이너 안 text-caption leading-none 명시
10. `feedback_avoid_premature_confirmation` — "approved 주세요" 자제, 결과 보여주고 사용자 판단
11. `project_redesign_15_daily_report_status` — 캘리브 좌표 시스템 100% 보존 일반화 → 비즈 anchor 1 byte 변경 금지
12. `feedback_cbc7119_design_never_wrangler` — wrangler 절대 X. main push 자동 cbc7119-preview 만
</rules_verbatim>

<tasks>

<task type="auto">
  <name>T1: sketch-wave-2-chrome-header.html (외곽 + 헤더 + 빈/로딩/오류 placeholder, 4 frame)</name>
  <files>cha-bio-safety/docs/redesign-context/25-qr-print/sketch-wave-2-chrome-header.html</files>
  <action>
W2 sketch HTML 1 파일 생성. 280+ lines. 4 frame matrix (모바일 다크 정상 / 모바일 라이트 정상 / 모바일 다크 빈 / 모바일 라이트 로딩).

**구조:**
1. `<!DOCTYPE html>` + head (UTF-8, viewport 모바일, lang ko, title "25-qr-print W2 chrome+header sketch")
2. `<style>` 인라인 — tokens.css 발췌 (--surface-page / --surface-raised / --surface-sunken / --border-default / --text-primary / --text-secondary 다크+라이트 분기, data-theme 속성 토글). lin-grad (linear-gradient) 사용 0건 (T2 에서도 0, T3 만 inspect 버튼 anchor).
3. `<body>` 4 `<section data-theme="dark|light">` frame:
   - frame 1: 모바일 다크 정상 (외곽 bg-surface-page + 헤더 bg-surface-raised + border-b border-border-default + 뒤로가기 w-[34px] h-[34px] bg-surface-sunken border border-border-default rounded-sm + Lucide ChevronLeft size={16} strokeWidth={2} text-text-secondary 인라인 SVG 시각 표현 — `<svg width="16" height="16" stroke="currentColor" stroke-width="2" fill="none" viewBox="0 0 24 24"><polyline points="15 18 9 12 15 6"></polyline></svg>` + 타이틀 'QR 코드 출력' text-body fontWeight 700 text-text-primary leading-none + flex gap 10 padding '8px 12px' flexShrink 0 + content 영역 placeholder "안내 박스 + 카테고리 카드 (W3 sketch 에서 채움)" 라벨)
   - frame 2: 모바일 라이트 정상 (동일 구조 + data-theme="light")
   - frame 3: 모바일 다크 빈 상태 ("CATEGORIES 0건 가정 — 표시 안 함, sketch 시각 placeholder 만")
   - frame 4: 모바일 라이트 로딩 (busy="소화기-inspect" 가짜 상태, 헤더 영역만 동일 + content 영역 placeholder "다운로드 버튼 로딩 — W4 sketch 에서 채움" 라벨)
4. **비즈 anchor 박제 fence** (`<pre>` 박스 — sketch 본문 안 인용만, 변경 안 함):
   - QRPrintPage.tsx line 253 외곽 wrapper 인라인 그대로
   - line 256~263 헤더 + 뒤로가기 + 타이틀 인라인 그대로
   - OQ #3 LOCKED Lucide ChevronLeft size={16} 교체 anchor + import 문 anchor
   - OQ #4 LOCKED 토큰 매핑 표 (--bg → --surface-page 등 8건)
   - memory `feedback_tailwind_w8_h8_is_48px` 인용 (w-[34px] h-[34px] arbitrary 사유)
5. **카피 verbatim 1건:** 'QR 코드 출력' (line 262)
6. **헤더 박스:** title + quick_id `260526-9yw` + branch `redesign/25-qr-print` + 평면 배치 anchor + OQ #3 + OQ #4 citation + 4 frame 요약표 + 비즈 anchor 1건 박제 (헤더 영역 line 256~263)

**Negative gate (T1 verify):**
- 이모지 0건
- linear-gradient 0건 (T3 만 예외, T1 에서는 사용 안 함)
- 9·10·11px fontSize 0건 (text-caption 12 마지노선)
- `(text|bg|border)-status-` className 0건 (T1 에서는 status 토큰 미사용)
- `\bw-8\b` / `\bh-8\b` 0건 (34x34 뒤로가기 → w-[34px] h-[34px] arbitrary)
- 옛 alias 토큰 (var(--bg/bg2/bg3/bd/bd2/t1/t2/t3)) 0건 in sketch body (비즈 anchor fence 안 예외 — fence 박스 안 line 253/256~263 verbatim 인용)
- src/** 변경 0 byte

**Atomic commit:**
```bash
git add cha-bio-safety/docs/redesign-context/25-qr-print/sketch-wave-2-chrome-header.html
git commit -m "$(cat <<'EOF'
sketch(25-qr-print): W2 chrome+header — 외곽 wrapper + 자체 헤더 (Lucide ChevronLeft size={16}) + 빈/로딩/오류 placeholder, 4 frame (OQ #3+#4 LOCKED)

quick: 260526-9yw

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```
  </action>
  <verify>
파일 존재 + 4 frame + 'QR 코드 출력' verbatim + ChevronLeft + 토큰 patterns + negative gates.

```bash
test -f cha-bio-safety/docs/redesign-context/25-qr-print/sketch-wave-2-chrome-header.html
wc -l cha-bio-safety/docs/redesign-context/25-qr-print/sketch-wave-2-chrome-header.html  # ≥280
grep -c '<section data-theme=' cha-bio-safety/docs/redesign-context/25-qr-print/sketch-wave-2-chrome-header.html  # ≥4
grep -c 'QR 코드 출력' cha-bio-safety/docs/redesign-context/25-qr-print/sketch-wave-2-chrome-header.html  # ≥1
grep -c 'ChevronLeft' cha-bio-safety/docs/redesign-context/25-qr-print/sketch-wave-2-chrome-header.html  # ≥3
grep -cE 'bg-surface-(page|raised|sunken)' cha-bio-safety/docs/redesign-context/25-qr-print/sketch-wave-2-chrome-header.html  # ≥3
grep -cE 'border-border-default' cha-bio-safety/docs/redesign-context/25-qr-print/sketch-wave-2-chrome-header.html  # ≥1
grep -cE 'w-\[34px\]|h-\[34px\]' cha-bio-safety/docs/redesign-context/25-qr-print/sketch-wave-2-chrome-header.html  # ≥1
# negative gates (sketch body 안 0)
grep -vE '^(#|<pre|fence|verbatim|박제|anchor|memory|`var)' cha-bio-safety/docs/redesign-context/25-qr-print/sketch-wave-2-chrome-header.html | grep -cE 'linear-gradient'  # 0
grep -vE '^(#|<pre|fence|verbatim|박제|anchor|memory)' cha-bio-safety/docs/redesign-context/25-qr-print/sketch-wave-2-chrome-header.html | grep -cE '\bfontSize:\s*(9|10|11)\b|font-size:\s*(9|10|11)px'  # 0
grep -cE '\b(w|h)-8\b' cha-bio-safety/docs/redesign-context/25-qr-print/sketch-wave-2-chrome-header.html  # 0
# src 변경 0
git diff --name-only HEAD -- cha-bio-safety/src/ | wc -l  # 0
# atomic commit 확인
git log -1 --pretty=%s | grep -c "sketch(25-qr-print): W2"  # 1
```
  </verify>
  <done>
sketch-wave-2-chrome-header.html 생성 (≥280 lines, 4 frame, OQ #3+#4 LOCKED 반영, 비즈 anchor 박제 fence, negative gate 통과). src/** 0 byte 변경. atomic 1 commit.
  </done>
</task>

<task type="auto">
  <name>T2: sketch-wave-3-instructions-categories.html (안내 박스 OQ #2 + CATEGORIES 7건 카드 grid OQ #4, 4 frame)</name>
  <files>cha-bio-safety/docs/redesign-context/25-qr-print/sketch-wave-3-instructions-categories.html</files>
  <action>
W3 sketch HTML 1 파일 생성. 320+ lines. 4 frame matrix (다크 모바일 7 카드 / 라이트 모바일 7 카드 / 다크 모바일 안내 박스 단독 / 라이트 모바일 카드 hover).

**구조:**
1. `<!DOCTYPE html>` + head + `<style>` 인라인 (tokens 발췌 + status-info-bg/bar 알리아스 + status-safe-bar 발췌, 다크+라이트 분기)
2. `<body>` 4 `<section data-theme="dark|light">` frame:
   - frame 1: 다크 모바일 7 카드 정상 (외곽 bg-surface-page + 헤더 placeholder "W2 sketch 참조" + 안내 박스 wrapper padding '10px 14px 4px' flexShrink 0 + 안내 박스 자체 bg-status-info-bg border border-status-info-bar rounded-md padding '10px 13px' text-caption text-text-secondary leading-relaxed + 안내 카피 2줄 verbatim with `<b>` 강조 + 카테고리 목록 wrapper flex-1 overflow-y-auto padding '10px 14px 20px' + 카테고리 column flex column gap 10 + CATEGORIES 7건 카드 (소화기/소화전/DIV/청정소화약제/완강기/전실제연댐퍼/방화셔터 순서) bg-surface-raised border border-border-default rounded-md padding '13px 14px' + 카드 타이틀 text-body-sm font-bold text-text-primary leading-none marginBottom 10 + 버튼 row placeholder flex gap 8 flex-wrap "W4 sketch 다운로드 버튼")
   - frame 2: 라이트 모바일 7 카드 정상 (동일 구조 + light)
   - frame 3: 다크 모바일 안내 박스 단독 강조 (안내 박스만 확대 시각 검수, 색 대비 검증)
   - frame 4: 라이트 모바일 카드 hover (소화기 카드 1개 hover 상태 — border-border-strong + translateY -1px 시각 표현, design-system §6.5 hover 룰 anchor)
3. **비즈 anchor 박제 fence** (`<pre>` 박스):
   - CATEGORIES 배열 7건 verbatim (line 17~25, 순서/value/label/hasPublic 그대로)
   - 안내 박스 인라인 (line 266~270 verbatim)
   - 카테고리 목록 wrapper (line 274) + column wrapper (line 275) + 카드 wrapper (line 280~306) verbatim
   - OQ #2 LOCKED 안내 박스 토큰 치환 표 (`rgba(14,165,233,.08)` → `bg-status-info-bg` / `rgba(14,165,233,.25)` → `border-status-info-bar` / `var(--t2)` → `text-text-secondary`)
   - OQ #4 LOCKED 카드 토큰 매핑 표
   - memory `feedback_tailwind_token_class_pattern` 인용 (status-info-bg/status-info-bar 단일 토큰명은 예외 사유)
4. **카피 verbatim 3건:**
   - '항목별 QR 코드를 PDF 파일로 다운로드합니다.' (line 268)
   - '소화기는 <b>점검용</b>(3×3 cm)과 <b>점검확인용</b>(7×9 cm)을 별도 파일로 다운로드할 수 있습니다.' (line 269)
   - 카드 타이틀 7건 ('소화기' / '소화전' / 'DIV' / '청정소화약제' / '완강기' / '전실제연댐퍼' / '방화셔터')
5. **헤더 박스:** title + quick_id `260526-9yw` + 평면 배치 + OQ #2 + OQ #4 citation + 4 frame 요약표 + 비즈 anchor 1건 박제 (CATEGORIES 7건)

**Negative gate (T2 verify):**
- 이모지 0건
- linear-gradient 0건 (T3 만 예외)
- 9·10·11px fontSize 0건
- `(text|bg|border)-status-` className — `bg-status-info-bg` + `border-status-info-bar` 만 예외 (status-info-bg/status-info-bar 가 단일 토큰명, design-system §2.3 verbatim). 그 외 `bg-status-safe` `text-status-danger` 등 0건
- `\bw-8\b` / `\bh-8\b` 0건
- 옛 alias 토큰 0건 in sketch body (비즈 anchor fence 안 예외)
- src/** 변경 0 byte

**Atomic commit:**
```bash
git add cha-bio-safety/docs/redesign-context/25-qr-print/sketch-wave-3-instructions-categories.html
git commit -m "$(cat <<'EOF'
sketch(25-qr-print): W3 instructions+categories — 안내 박스 status-info 알리아스 + CATEGORIES 7건 카드 grid, 4 frame (OQ #2+#4 LOCKED)

quick: 260526-9yw

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```
  </action>
  <verify>
파일 존재 + 4 frame + 안내 카피 + CATEGORIES 7건 + 토큰 patterns + negative gates.

```bash
test -f cha-bio-safety/docs/redesign-context/25-qr-print/sketch-wave-3-instructions-categories.html
wc -l cha-bio-safety/docs/redesign-context/25-qr-print/sketch-wave-3-instructions-categories.html  # ≥320
grep -c '<section data-theme=' cha-bio-safety/docs/redesign-context/25-qr-print/sketch-wave-3-instructions-categories.html  # ≥4
grep -c '항목별 QR 코드를 PDF 파일로 다운로드합니다' cha-bio-safety/docs/redesign-context/25-qr-print/sketch-wave-3-instructions-categories.html  # ≥1
grep -c '점검확인용' cha-bio-safety/docs/redesign-context/25-qr-print/sketch-wave-3-instructions-categories.html  # ≥1
grep -cE '소화기|소화전|DIV|청정소화약제|완강기|전실제연댐퍼|방화셔터' cha-bio-safety/docs/redesign-context/25-qr-print/sketch-wave-3-instructions-categories.html  # ≥7
grep -c 'bg-status-info-bg' cha-bio-safety/docs/redesign-context/25-qr-print/sketch-wave-3-instructions-categories.html  # ≥1
grep -c 'border-status-info-bar' cha-bio-safety/docs/redesign-context/25-qr-print/sketch-wave-3-instructions-categories.html  # ≥1
grep -c 'bg-surface-raised' cha-bio-safety/docs/redesign-context/25-qr-print/sketch-wave-3-instructions-categories.html  # ≥1
grep -c 'hasPublic' cha-bio-safety/docs/redesign-context/25-qr-print/sketch-wave-3-instructions-categories.html  # ≥7 (fence 안 verbatim)
# negative gates
grep -vE '^(#|<pre|fence|verbatim|박제|anchor|memory|`var)' cha-bio-safety/docs/redesign-context/25-qr-print/sketch-wave-3-instructions-categories.html | grep -cE 'linear-gradient'  # 0
grep -vE '^(#|<pre|fence|verbatim|박제|anchor|memory)' cha-bio-safety/docs/redesign-context/25-qr-print/sketch-wave-3-instructions-categories.html | grep -cE '\bfontSize:\s*(9|10|11)\b|font-size:\s*(9|10|11)px'  # 0
grep -cE '\b(w|h)-8\b' cha-bio-safety/docs/redesign-context/25-qr-print/sketch-wave-3-instructions-categories.html  # 0
# status- prefix 0 except status-info-bg/bar (warning glyph 회피 — info 알리아스만 OK)
grep -oE '(text|bg|border)-status-[a-z-]+' cha-bio-safety/docs/redesign-context/25-qr-print/sketch-wave-3-instructions-categories.html | sort -u  # bg-status-info-bg, border-status-info-bar 만
# src 변경 0
git diff --name-only HEAD -- cha-bio-safety/src/ | wc -l  # 0
git log -1 --pretty=%s | grep -c "sketch(25-qr-print): W3"  # 1
```
  </verify>
  <done>
sketch-wave-3-instructions-categories.html 생성 (≥320 lines, 4 frame, OQ #2+#4 LOCKED 반영, CATEGORIES 7건 박제 fence, 안내 카피 verbatim 2줄, negative gate 통과). src/** 0 byte 변경. atomic 1 commit.
  </done>
</task>

<task type="auto">
  <name>T3: sketch-wave-4-download-actions.html (다운로드 버튼 + PDF 생성 + 인쇄 모드 분기, 4 frame)</name>
  <files>cha-bio-safety/docs/redesign-context/25-qr-print/sketch-wave-4-download-actions.html</files>
  <action>
W4 sketch HTML 1 파일 생성. 360+ lines. 4 frame matrix (다크 모바일 소화기 카드 2 버튼 / 다크 모바일 소화기 inspect 로딩 / 라이트 모바일 소화전 카드 1 버튼 / 라이트 데스크톱 7 카드 매트릭스).

**구조:**
1. `<!DOCTYPE html>` + head + `<style>` 인라인 (tokens 발췌 + status-safe-bar + bg-border-strong + text-text-tertiary + text-text-on-accent 다크+라이트). 인라인 그라데이션 inspect 버튼 anchor 만 (linear-gradient `135deg, #1d4ed8, #0ea5e9`).
2. `<body>` 4 `<section data-theme="dark|light">` frame:
   - frame 1: 다크 모바일 소화기 카드 2 버튼 정상 (헤더 + 안내 + 카드 placeholder "W2/W3 참조" + 카드 wrapper 안 버튼 row flex gap 8 flex-wrap:
     - inspect 버튼 (`<button>` padding '9px 16px' rounded-sm text-caption font-bold leading-none text-text-on-accent gap 6 + 인라인 `style="background: linear-gradient(135deg, #1d4ed8, #0ea5e9);"` + label '점검용 QR PDF')
     - public 버튼 (OQ #1 LOCKED solid `bg-status-safe-bar text-text-on-accent` padding '9px 16px' rounded-sm text-caption font-bold leading-none gap 6 + label '점검확인용 QR PDF')
   - frame 2: 다크 모바일 소화기 inspect 로딩 (inspect 버튼 `bg-border-strong text-text-tertiary cursor-default opacity 0.5` + label '생성 중...' + public 버튼 disabled `opacity 0.5 cursor not-allowed` — busy 3 state matrix 시각 표현)
   - frame 3: 라이트 모바일 소화전 카드 1 버튼 (cat.hasPublic false → inspect 버튼만 표시, public 버튼 미표시 — 분기 시각 검증)
   - frame 4: 라이트 데스크톱 7 카드 매트릭스 (모든 카테고리 버튼 상태 한눈 비교 — 데스크톱 maxWidth 1024 가짜 가로 레이아웃 시각 표현)
3. **카드 미리보기 박제 fence** (`<pre>` 박스):
   - inspect 카드 (30x38mm 세로) — scale 3 / qrSize Math.floor(30 * 0.65) = 19mm / bottomLines [locationNo??id, location, floor] / bottomFontSize 3 (line 174~186 verbatim)
   - public 카드 (70x90mm 가로) — qrSize Math.floor(70 * 0.65) = 45mm / topLines '본 소화기는 QR코드로 관리되며,/아래 QR코드로/점검 내역 확인 가능합니다.' verbatim + topFontSize 3 + bottomFontSize 3 (line 189~205 verbatim)
4. **비즈 anchor 박제 fence**:
   - handleDownload (line 229~250 verbatim) — useAuthStore + fetch + Authorization Bearer + json.success/data?.length 체크 + catLabel find + generatePdf 호출 + toast 5종
   - generatePdf (line 122~219 verbatim 발췌 — jsPDF a4 / MARGIN 10 / cardW/cardH / gap 1 / cols/rows / setFillColor / setDrawColor / typeLabel / 파일명)
   - renderCardCanvas (line 29~119 verbatim 발췌 — scale 3 / PAD / FONT verbatim / topFontSize 12 / bottomFontSize 13 / maxQrW 0.85 / qrDrawSize / fillStyle 색 / textAlign / textBaseline / 첫 하단 'bold ')
   - dlBtnStyle (line 312~330 verbatim) — loading/public/기본 3 state + OQ #1 LOCKED public solid 치환 표
   - busy 3 state matrix 표 (null / inspKey / publicKey)
5. **카피 verbatim 9건 anchor 박스:** 'QR 코드 출력' + 안내 2줄 + '점검용 QR PDF' + '점검확인용 QR PDF' + '생성 중...' + '체크포인트가 없습니다' + 'PDF 다운로드 완료' + 'PDF 생성 오류' + '본 소화기는 QR코드로 관리되며,/아래 QR코드로/점검 내역 확인 가능합니다.' + 파일명 `${categoryLabel}_${typeLabel}_QR.pdf` + typeLabel '점검용/점검확인용'
6. **헤더 박스:** title + quick_id `260526-9yw` + 평면 배치 + OQ #1 + OQ #5 citation + 4 frame 요약표 + 비즈 anchor 5건 박제 (CATEGORIES + renderCardCanvas + generatePdf + handleDownload + dlBtnStyle)

**Negative gate (T3 verify):**
- 이모지 0건
- **linear-gradient — inspect 버튼 만 예외 anchor (≥3 박제 — `linear-gradient(135deg, #1d4ed8, #0ea5e9)`)**. public 그라데이션 0건 (OQ #1 LOCKED solid 치환 — `#16a34a/#22c55e` 0건 in sketch body, 폐기 사유 anchor 박스 안만 텍스트 인용)
- 9·10·11px fontSize 0건
- `(text|bg|border)-status-` className — `bg-status-safe-bar` 만 예외 (단일 토큰명). 그 외 0건
- `\bw-8\b` / `\bh-8\b` 0건
- 옛 alias 토큰 0건 in sketch body (비즈 anchor fence 안 예외)
- src/** 변경 0 byte (3 task 누적 최종 확인)

**Atomic commit:**
```bash
git add cha-bio-safety/docs/redesign-context/25-qr-print/sketch-wave-4-download-actions.html
git commit -m "$(cat <<'EOF'
sketch(25-qr-print): W4 download+actions — inspect lin-grad + public solid + busy 3 state + PDF 카드 미리보기 박제, 4 frame (OQ #1+#5 LOCKED)

quick: 260526-9yw

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```
  </action>
  <verify>
파일 존재 + 4 frame + 다운로드 버튼 카피 + 비즈 anchor 박제 + linear-gradient inspect anchor + negative gates.

```bash
test -f cha-bio-safety/docs/redesign-context/25-qr-print/sketch-wave-4-download-actions.html
wc -l cha-bio-safety/docs/redesign-context/25-qr-print/sketch-wave-4-download-actions.html  # ≥360
grep -c '<section data-theme=' cha-bio-safety/docs/redesign-context/25-qr-print/sketch-wave-4-download-actions.html  # ≥4
grep -c '점검용 QR PDF' cha-bio-safety/docs/redesign-context/25-qr-print/sketch-wave-4-download-actions.html  # ≥1
grep -c '점검확인용 QR PDF' cha-bio-safety/docs/redesign-context/25-qr-print/sketch-wave-4-download-actions.html  # ≥1
grep -c '생성 중\.\.\.' cha-bio-safety/docs/redesign-context/25-qr-print/sketch-wave-4-download-actions.html  # ≥1
grep -c '체크포인트가 없습니다' cha-bio-safety/docs/redesign-context/25-qr-print/sketch-wave-4-download-actions.html  # ≥1
grep -c 'PDF 다운로드 완료' cha-bio-safety/docs/redesign-context/25-qr-print/sketch-wave-4-download-actions.html  # ≥1
grep -c 'PDF 생성 오류' cha-bio-safety/docs/redesign-context/25-qr-print/sketch-wave-4-download-actions.html  # ≥1
grep -c '본 소화기는 QR코드로 관리되며' cha-bio-safety/docs/redesign-context/25-qr-print/sketch-wave-4-download-actions.html  # ≥1
# T3 만 예외 — inspect 버튼 linear-gradient anchor ≥3
grep -cE 'linear-gradient\(135deg, ?#1d4ed8, ?#0ea5e9\)' cha-bio-safety/docs/redesign-context/25-qr-print/sketch-wave-4-download-actions.html  # ≥3
# public 그라데이션 0건 in sketch body (OQ #1 LOCKED 폐기 — fence 안 텍스트 인용만 예외)
grep -vE '^(#|<pre|fence|verbatim|박제|anchor|memory|폐기|사유)' cha-bio-safety/docs/redesign-context/25-qr-print/sketch-wave-4-download-actions.html | grep -cE 'linear-gradient\(135deg, ?#16a34a, ?#22c55e\)'  # 0
# 비즈 anchor 5건 박제 확인
grep -c 'handleDownload' cha-bio-safety/docs/redesign-context/25-qr-print/sketch-wave-4-download-actions.html  # ≥1
grep -c 'generatePdf' cha-bio-safety/docs/redesign-context/25-qr-print/sketch-wave-4-download-actions.html  # ≥1
grep -c 'renderCardCanvas' cha-bio-safety/docs/redesign-context/25-qr-print/sketch-wave-4-download-actions.html  # ≥1
grep -c 'dlBtnStyle' cha-bio-safety/docs/redesign-context/25-qr-print/sketch-wave-4-download-actions.html  # ≥1
grep -c 'bg-status-safe-bar' cha-bio-safety/docs/redesign-context/25-qr-print/sketch-wave-4-download-actions.html  # ≥1
# negative gates
grep -vE '^(#|<pre|fence|verbatim|박제|anchor|memory)' cha-bio-safety/docs/redesign-context/25-qr-print/sketch-wave-4-download-actions.html | grep -cE '\bfontSize:\s*(9|10|11)\b|font-size:\s*(9|10|11)px'  # 0
grep -cE '\b(w|h)-8\b' cha-bio-safety/docs/redesign-context/25-qr-print/sketch-wave-4-download-actions.html  # 0
# status- prefix 0 except status-safe-bar
grep -oE '(text|bg|border)-status-[a-z-]+' cha-bio-safety/docs/redesign-context/25-qr-print/sketch-wave-4-download-actions.html | sort -u  # bg-status-safe-bar 만
# 최종 src 변경 0 (3 task 누적)
git diff --name-only HEAD~3 HEAD -- cha-bio-safety/src/ | wc -l  # 0
git log -1 --pretty=%s | grep -c "sketch(25-qr-print): W4"  # 1
# 3 atomic commit 누적 확인
git log -3 --pretty=%s | grep -cE 'sketch\(25-qr-print\): W[2-4]'  # 3
# wrangler / npm run deploy 0
git log -3 --pretty=%B | grep -cE 'wrangler|npm run deploy'  # 0
```
  </verify>
  <done>
sketch-wave-4-download-actions.html 생성 (≥360 lines, 4 frame, OQ #1+#5 LOCKED 반영, inspect linear-gradient anchor ≥3, public 그라데이션 폐기 solid 치환, 비즈 anchor 5건 박제 fence, 카피 verbatim 9건, negative gate 통과). 3 task 누적 src/** 0 byte 변경. 3 atomic commit (W2/W3/W4). wrangler/npm run deploy 0.
  </done>
</task>

</tasks>

<verification>
3 task 완료 후 최종 phase verify (전체 sketch 시리즈):

```bash
# 1. 3 파일 모두 평면 배치 (sketch/ 서브폴더 0)
ls cha-bio-safety/docs/redesign-context/25-qr-print/sketch-wave-*.html | wc -l  # 3
find cha-bio-safety/docs/redesign-context/25-qr-print/sketch -type d 2>/dev/null | wc -l  # 0

# 2. 3 atomic commit 확인
git log -3 --pretty=%s | grep -cE 'sketch\(25-qr-print\): W[2-4]'  # 3

# 3. 누적 src/** 0 byte 변경
git diff --name-only HEAD~3 HEAD -- cha-bio-safety/src/ | wc -l  # 0
git diff --name-only HEAD~3 HEAD -- cha-bio-safety/src/pages/QRPrintPage.tsx | wc -l  # 0

# 4. wrangler / npm run deploy 미실행
git log -3 --pretty=%B | grep -cE 'wrangler|npm run deploy'  # 0

# 5. components.css / App.tsx 변경 0
git diff --name-only HEAD~3 HEAD -- 'cha-bio-safety/src/styles/components.css' 'cha-bio-safety/src/App.tsx' | wc -l  # 0

# 6. OQ #1~#5 LOCKED 5건 모두 3 파일 안에 박제 (cross-ref)
grep -lE 'OQ #1' cha-bio-safety/docs/redesign-context/25-qr-print/sketch-wave-*.html  # T3 (download)
grep -lE 'OQ #2' cha-bio-safety/docs/redesign-context/25-qr-print/sketch-wave-*.html  # T2 (info box)
grep -lE 'OQ #3' cha-bio-safety/docs/redesign-context/25-qr-print/sketch-wave-*.html  # T1 (ChevronLeft)
grep -lE 'OQ #4' cha-bio-safety/docs/redesign-context/25-qr-print/sketch-wave-*.html  # T1+T2 (외곽 hex)
grep -lE 'OQ #5' cha-bio-safety/docs/redesign-context/25-qr-print/sketch-wave-*.html  # T3 (비즈 anchor)

# 7. 카피 verbatim 9건 cross-ref (모든 sketch 합쳐서 9건 모두 등장)
cat cha-bio-safety/docs/redesign-context/25-qr-print/sketch-wave-*.html | grep -cE 'QR 코드 출력|항목별 QR 코드를 PDF|점검용 QR PDF|점검확인용 QR PDF|생성 중\.\.\.|체크포인트가 없습니다|PDF 다운로드 완료|PDF 생성 오류|본 소화기는 QR코드로 관리되며'  # ≥9

# 8. 비즈 anchor 9건 cross-ref (CATEGORIES + renderCardCanvas + generatePdf + handleDownload + dlBtnStyle + useAuthStore + busy + baseUrl + 파일명)
cat cha-bio-safety/docs/redesign-context/25-qr-print/sketch-wave-*.html | grep -cE 'CATEGORIES|renderCardCanvas|generatePdf|handleDownload|dlBtnStyle|useAuthStore|busy|baseUrl|categoryLabel'  # ≥9

# 9. SUMMARY 박제 — 본 PLAN 종료 후 별도 quick `260526-9yw-SUMMARY.md` 작성 단계로 진행 (orchestrator 가 wrapper)
```
</verification>

<success_criteria>
- 3 sketch HTML 모두 평면 배치 (`25-qr-print/` 직속, `sketch/` 서브폴더 0)
- 3 atomic git commit (W2/W3/W4 각 1 commit)
- `QRPrintPage.tsx` + `src/**` 모두 0 byte 변경
- `wrangler` / `npm run deploy` 미실행
- 4 frame matrix per sketch (총 12 frame)
- OQ #1~#5 LOCKED 5건 모두 적절한 sketch 안 verbatim 박제
- 비즈 anchor 9건 (CATEGORIES + renderCardCanvas + generatePdf + handleDownload + dlBtnStyle + useAuthStore + busy + baseUrl + 카피) 박제 fence
- 카피 verbatim 9건 모두 sketch 본문 안 등장
- negative gate 모두 통과 (이모지 0 / linear-gradient T3 만 inspect anchor / 9·10·11 0 / status- prefix `bg-status-info-bg`+`border-status-info-bar`+`bg-status-safe-bar` 만 예외 / w-8 h-8 0 / 옛 alias 0)
- T3 inspect 버튼 linear-gradient anchor ≥3 / public 그라데이션 0건 (OQ #1 LOCKED solid 치환 폐기 anchor 박스 안만 텍스트 인용 예외)
</success_criteria>

<output>
After completion, create `.planning/quick/260526-9yw-redesign-25-qr-print-w2-w4-sketch-waves-/260526-9yw-SUMMARY.md` (orchestrator wrapper task).
</output>
