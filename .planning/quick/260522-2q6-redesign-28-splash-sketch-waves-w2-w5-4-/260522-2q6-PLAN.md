---
phase: quick-260522-2q6
plan: 01
type: execute
wave: 1
depends_on: []
quick_id: 260522-2q6
branch: redesign/28-splash
date: 2026-05-22
autonomous: true
files_modified:
  - cha-bio-safety/docs/redesign-context/28-splash/sketch-wave-2-splash.html
  - cha-bio-safety/docs/redesign-context/28-splash/sketch-wave-3-install-main.html
  - cha-bio-safety/docs/redesign-context/28-splash/sketch-wave-4-install-guides.html
  - cha-bio-safety/docs/redesign-context/28-splash/wave-5-tsx-conversion-checklist.md
requirements:
  - redesign-28-splash-w2-splash
  - redesign-28-splash-w3-install-main
  - redesign-28-splash-w4-install-guides
  - redesign-28-splash-w5-tsx-checklist
must_haves:
  truths:
    - "W2~W4 sketch HTML 3개 + W5 checklist markdown 1개, 총 4 파일이 28-splash/ 직속 평면 배치로 생성된다 (sketch/ 서브폴더 절대 X, 17-annual-plan 0j3 패턴 mirror)"
    - "SplashScreen.tsx (83 lines) + InstallPrompt.tsx (237 lines) + pwaInstall.ts + versionCheck.ts 총 4 src 파일 모두 코드 변경 0건 — sketch + checklist 만 산출"
    - "negative gate 3 sketch + 1 checklist 모두 통과 (이모지 0 단 ⋮ ⎋ 예외 anchor / linear-gradient 0 / 9·10·11px 0 / status- prefix 0 / w-8 h-8 0 / 옛 alias 토큰 0)"
    - "OQ #1~#5 LOCKED 결정 5건 모두 4 파일 안에 verbatim 반영 (설치 버튼 bg-safe-bar solid / 진행 바 bg-accent solid / 폰트 격상 매핑 / 토큰 치환 + 인라인 유지 4건 / iOS ⋮ ⎋ 글리프 보존)"
    - "비즈 시그니처 16건 (SplashScreen 8 + InstallPrompt 8) 100% 보존 — W5 checklist 안 verbatim 명시 + W2~W4 sketch rules 박스 박제"
    - "각 task 마다 atomic git commit 1개씩 — 총 4 commit (executor cherry-pick 사고 4회 precedent 회피용 명령 박제)"
  artifacts:
    - path: "cha-bio-safety/docs/redesign-context/28-splash/sketch-wave-2-splash.html"
      provides: "SplashScreen 전체 sketch — 외곽 + 로고 + 타이틀 + 진행 바 + 버전 + slideUp animation (4 frame: 다크 0% / 다크 50% / 다크 100% '준비 완료' / 라이트)"
      contains: "data-theme dark+light / bg-surface-page / 88x88 로고 박스 / CBC 방재 text-heading font-black tracking-tight / 소방안전 통합관리 시스템 text-caption leading-relaxed / 160 width 진행 바 / bg-accent solid fill + bg-[rgba(255,255,255,0.07)] 트랙 / 시스템 초기화 중 / 데이터 불러오는 중 / 준비 완료 / v{__APP_VERSION__} 경기도 성남시 분당구 / slideUp 0.4s ease-out keyframes"
      min_lines: 320
    - path: "cha-bio-safety/docs/redesign-context/28-splash/sketch-wave-3-install-main.html"
      provides: "InstallPrompt 메인 view sketch — 오버레이 + 카드 + 아이콘 + 제목 + 설치 버튼 + 나중에 (3 frame: hasPrompt true / hasPrompt false / 라이트)"
      contains: "rgba(0,0,0,0.85) 오버레이 인라인 / bg-surface-raised 카드 + rgba(59,130,246,0.3) border 인라인 / boxShadow 0 8px 40px 인라인 / 64x64 아이콘 박스 / CBC 방재 text-title font-extrabold / 홈 화면에 앱을 설치하면 더 빠르고 편리하게 / bg-safe-bar text-text-on-accent text-body font-bold 설치 버튼 / 홈 화면에 설치 / 설치 방법 보기 분기 / 나중에 할게요"
      min_lines: 320
    - path: "cha-bio-safety/docs/redesign-context/28-splash/sketch-wave-4-install-guides.html"
      provides: "InstallPrompt Android + iOS 가이드 view sketch — 노란 경고 + 3-step + 특수 글리프 ⋮ ⎋ (3 frame: 다크 Android / 다크 iOS / 라이트 Android)"
      contains: "text-warning bg-warning-bg border-warning/25 노란 경고 (Android 만) / 28x28 숫자 원 bg-accent/15 text-accent / 3-step 각 시그니처 / 크롬 우상단 ⋮ 메뉴 (U+22EE) / Safari 하단 공유 아이콘 ⎋ (U+238B verticalAlign middle) / 홈 화면에 추가 / 확인했습니다 bg-accent/15 text-accent 버튼"
      min_lines: 380
    - path: "cha-bio-safety/docs/redesign-context/28-splash/wave-5-tsx-conversion-checklist.md"
      provides: "TSX 변환 verify checklist (markdown, 12 섹션) — 2 파일 단일 atomic 변환 룰 + 비즈 anchor 16건 박제"
      contains: "§1 변환 범위 (2 파일 단일 atomic, components.css 신규 X) / §2 보존 (비즈 anchor 16건 SplashScreen 8 + InstallPrompt 8) / §3 변환 매핑 (SplashScreen 영역 + InstallPrompt 메인 영역 + 가이드 영역 verbatim) / §4 OQ LOCKED 5건 반영 / §5 negative gate (⋮ ⎋ 예외 anchor) / §6 positive gate / §7 build/tsc / §8 자체 verify / §9 Tailwind cheatsheet / §10 비즈 보존 체크박스 (시간 anchor + animation + localStorage TTL + 3 view state) / §11 메모리 룰 inline 12+ / §12 다음 단계"
      min_lines: 320
  key_links:
    - from: "wave-1-index.md §7 OQ #1 LOCKED"
      to: "sketch-wave-3-install-main.html + wave-5-tsx-conversion-checklist.md"
      via: "InstallPrompt 설치 버튼 = bg-safe-bar text-text-on-accent solid (그라데이션 linear-gradient(135deg, #2563eb, #0ea5e9) 완전 폐기)"
      pattern: "bg-safe-bar"
    - from: "wave-1-index.md §7 OQ #2 LOCKED"
      to: "sketch-wave-2-splash.html + wave-5-tsx-conversion-checklist.md"
      via: "SplashScreen 진행 바 fill = bg-accent solid / 트랙 = bg-[rgba(255,255,255,0.07)] 인라인. 그라데이션 linear-gradient(90deg,#3b82f6,#0ea5e9) 완전 폐기"
      pattern: "bg-accent"
    - from: "wave-1-index.md §7 OQ #3 LOCKED"
      to: "sketch-wave-2 + sketch-wave-3 + sketch-wave-4 + wave-5-checklist"
      via: "폰트 격상 매핑 verbatim — 10→text-caption(12) leading-none / 11→text-caption leading-none / 12 유지 (부제 leading-relaxed) / 13→text-label leading-none / 15→text-body(16) 격상 / 16 인라인 (글리프) / 18→text-title font-extrabold / 22→text-heading font-black tracking-tight"
      pattern: "text-caption|text-label|text-body|text-title|text-heading"
    - from: "wave-1-index.md §7 OQ #4 LOCKED"
      to: "sketch-wave-2 + sketch-wave-3 + sketch-wave-4 + wave-5-checklist"
      via: "토큰 치환 — #161b22 → bg-surface-page / #1c2128 → bg-surface-raised / 4 text 색 → text-text-primary/secondary/tertiary/disabled / 노란 경고 → text-warning + bg-warning-bg + border-warning/25 / 숫자 원 → bg-accent/15 + text-accent. 인라인 유지 4건: 진행 바 트랙 rgba / 카드 border rgba(59,130,246,0.3) / 오버레이 rgba(0,0,0,0.85) / boxShadow"
      pattern: "bg-surface-page|bg-surface-raised|text-text-primary|text-warning|bg-warning-bg"
    - from: "wave-1-index.md §7 OQ #5 LOCKED"
      to: "sketch-wave-4-install-guides.html + wave-5-tsx-conversion-checklist.md"
      via: "iOS 글리프 ⋮ U+22EE + ⎋ U+238B 인라인 span fontSize:16 유지 + verticalAlign:'middle' 보존. Lucide 교체 X. emoji negative grep 시 ⋮ ⎋ 예외 anchor 추가"
      pattern: "⋮|⎋|U\\+22EE|U\\+238B"
---

<objective>
redesign/28-splash 의 W1 인덱스(`wave-1-index.md`, 562 lines, OQ 5건 LOCKED 완료 2026-05-22)를 기반으로
W2~W5 4 sub-wave 산출물을 **단일 quick task** 안에서 **atomic 4 commit** 으로 한 번에 만든다.

- **W2 (T1):** SplashScreen 전체 sketch (line 38~82 영역) — 외곽 + 로고 + 타이틀 + 진행 바 + 버전 + slideUp animation, 4 frame 진행률 매트릭스
- **W3 (T2):** InstallPrompt 메인 view sketch (line 57~111 영역) — 오버레이 + 카드 + 아이콘 + 제목 + 설치 버튼 + 나중에, 3 frame
- **W4 (T3):** InstallPrompt 가이드 view sketch (line 112~215 영역) — Android 3-step (노란 경고 포함) + iOS 3-step (⋮ ⎋ 글리프), 3 frame
- **W5 (T4):** TSX 변환 verify checklist markdown (sketch 아님, W2~W4 grep 추출 검증 룰 + 12 섹션 + 비즈 anchor 16건 박제)

**Purpose:** 17-annual-plan / 16-workshift / 27-login / 14-reports / 13-schedule 와 동일한 평면(flat sibling) 패턴으로 28-splash sketch 단계를 4 wave 분량을 한 turn 에 처리하여 cbc7119-preview 자동 배포 사이클 1회로 사용자 시각 검수 가능하게 함. InstallPrompt 는 메인/가이드 분리 (W3 + W4) — 메인은 설치 버튼 OQ #1 결정 핵심 / 가이드는 ⋮ ⎋ 글리프 OQ #5 결정 핵심.

**Output:** `cha-bio-safety/docs/redesign-context/28-splash/` 직속 4 파일 (sketch HTML 3개 + checklist md 1개).
`SplashScreen.tsx` + `InstallPrompt.tsx` + `pwaInstall.ts` + `versionCheck.ts` **총 4 src 파일 모두** 코드 변경 0건 (verify gate 마지막에 확인).

**17-annual-plan 0j3 PLAN.md mirror:** 본 PLAN 은 17-annual-plan 0j3 PLAN 구조 (단일 plan / 4 task / atomic 4 commit / verify gate 그대로) 의 정확한 mirror. 차이점 = SplashScreen 83 lines + InstallPrompt 237 lines (총 320 lines, AnnualPlanPage 225 보다 큼) / OQ 결정 내용 (설치 버튼/진행 바 그라데이션 폐기 + ⋮ ⎋ 글리프 보존) / source 4 파일 untouched (SplashScreen + InstallPrompt + pwaInstall + versionCheck).

**중요 — executor commit 부모 머지 사고 회피:** 16-workshift sjj + 17-annual-plan wmq + 다수 quick 에서 executor worktree commit 이 부모 브랜치로 자동 머지 안 되어 cherry-pick 복구 필요했음 (4회 재현). **executor 는 worktree 안에서 `git commit` 후 명시적 보고 필수** (commit hash + message 1줄) + orchestrator 가 자동 cherry-pick 으로 복구한다. 본 PLAN 각 task 의 commit 명령은 명시 박제.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jykevin/Documents/cbc7119-design/CLAUDE.md
@/Users/jykevin/Documents/cbc7119-design/CLAUDE.local.md

# W1 인덱스 (단일 진입점, OQ 5건 LOCKED 완료 2026-05-22)
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/28-splash/wave-1-index.md

# Precedent — 17-annual-plan 0j3 PLAN.md + SUMMARY.md (정확한 mirror 소스)
@/Users/jykevin/Documents/cbc7119-design/.planning/quick/260522-0j3-redesign-17-annual-plan-sketch-waves-w2-/260522-0j3-PLAN.md
@/Users/jykevin/Documents/cbc7119-design/.planning/quick/260522-0j3-redesign-17-annual-plan-sketch-waves-w2-/260522-0j3-SUMMARY.md

# Precedent — 17-annual-plan 산출 4 파일 (시각/구조 mirror)
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/17-annual-plan/sketch-wave-2-chrome.html
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/17-annual-plan/sketch-wave-3-preview-calibration.html
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/17-annual-plan/sketch-wave-4-download.html
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/17-annual-plan/wave-5-tsx-conversion-checklist.md

# Source TSX (sketch 대상, 변경 0 — 4 파일 모두 untouched)
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/src/pages/SplashScreen.tsx
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/src/components/InstallPrompt.tsx

# 28-splash redesign 컨텍스트 (토큰/타이포 source)
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/28-splash/28-splash.md
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/28-splash/design-system.md
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/28-splash/tokens.css
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/28-splash/typography.css

<rules_verbatim>
### OQ LOCKED 5건 (wave-1-index.md §7 verbatim, 2026-05-22)

**OQ #1 (InstallPrompt 설치 버튼 bg-safe-bar solid + 그라데이션 폐기):**
> LOCKED (2026-05-22): InstallPrompt 설치 버튼 = `bg-safe-bar text-text-on-accent` solid. 기존 `linear-gradient(135deg, #2563eb, #0ea5e9)` 완전 폐기. W3 sketch + TSX 변환 양쪽 동일 적용.

**OQ #2 (SplashScreen 진행 바 bg-accent solid + 그라데이션 폐기):**
> LOCKED (2026-05-22): SplashScreen 진행 바 fill = `bg-accent` solid / 트랙 = `bg-[rgba(255,255,255,0.07)]` 인라인. 기존 `linear-gradient(90deg,#3b82f6,#0ea5e9)` 완전 폐기. W2 sketch + TSX 변환 양쪽 동일.

**OQ #3 (폰트 격상 verbatim):**
> LOCKED (2026-05-22): 폰트 격상 매핑 verbatim (sketch + TSX 양쪽):
> - fontSize:10 (SplashScreen 버전 문자열 line 73) → `text-caption` (12) `leading-none` 격상
> - fontSize:11 (SplashScreen 진행 라벨 line 68) → `text-caption` (12) `leading-none` 격상
> - fontSize:12 (SplashScreen 부제 line 59 + InstallPrompt 부제 line 84) → `text-caption` (12) 유지 (부제만 `leading-relaxed`)
> - fontSize:13 (InstallPrompt step 제목 line 122/130/136/160/177/194 + 버튼 line 143/209) → `text-label` (13) `leading-none`
> - fontSize:15 (InstallPrompt 설치 버튼 line 96) → `text-body` (16) `font-bold` 격상
> - fontSize:16 (InstallPrompt iOS 글리프 line 122/164) → 인라인 유지 (★ 글리프 시각 anchor, OQ #5)
> - fontSize:18 (InstallPrompt 카드 제목 line 81) → `text-title` (18) `font-extrabold`
> - fontSize:22 (SplashScreen 타이틀 line 58) → `text-heading` (22) `font-black` `tracking-tight`

**OQ #4 (토큰 치환 + 인라인 유지 4건):**
> LOCKED (2026-05-22): 토큰 치환 매핑 verbatim:
> - 페이지 background `#161b22` → `bg-surface-page`
> - 카드 background `#1c2128` → `bg-surface-raised`
> - text `#e6edf3` → `text-text-primary`
> - text `#8b949e` → `text-text-secondary`
> - text `#6e7681` → `text-text-tertiary`
> - text `#3d444d` → `text-text-disabled`
> - 노란 경고 `#f59e0b` + `rgba(245,158,11,0.1)` + `rgba(245,158,11,0.25)` → `text-warning` + `bg-warning-bg` + `border-warning/25`
> - 숫자 원 `rgba(59,130,246,0.15)` + `#3b82f6` → `bg-accent/15` + `text-accent`
> - **인라인 유지 4건** (alpha 정밀도 + 시스템 anchor): 진행 바 트랙 `rgba(255,255,255,0.07)` / 카드 border `rgba(59,130,246,0.3)` / 오버레이 `rgba(0,0,0,0.85)` / `boxShadow: '0 8px 40px rgba(0,0,0,0.5)'`

**OQ #5 (iOS ⋮ ⎋ 글리프 인라인 span fontSize:16 보존):**
> LOCKED (2026-05-22): iOS Safari 가이드의 `⋮` (U+22EE, Android Chrome 메뉴, line 122) + `⎋` (U+238B, iOS 공유, line 164) 글리프는 **인라인 span fontSize:16 + verticalAlign:'middle' 유지**. Lucide 아이콘 교체 X (글꼴 의미 anchor). emoji negative grep 시 `⋮` `⎋` 2 글리프 예외 anchor 추가 — sketch + TSX + verify gate 모두 동일.

### 디자인 토큰 패턴 (wave-1-index.md §3 + planning constraints verbatim)
- `bg-surface-page` (#161b22 페이지 외곽)
- `bg-surface-raised` (#1c2128 카드)
- `border-border-default` (var(--bd) → --border-default)
- `text-text-primary` (#e6edf3) `text-text-secondary` (#8b949e) `text-text-tertiary` (#6e7681) `text-text-disabled` (#3d444d)
- `bg-safe-bar` (CTA solid, OQ #1) / `text-text-on-accent` (흰 글자)
- `bg-accent` (진행 바 solid, OQ #2) / `bg-accent/15` (숫자 원 배경) / `text-accent` (숫자 원 글자 + 확인했습니다 버튼)
- `text-warning` + `bg-warning-bg` + `border-warning/25` (노란 경고, OQ #4)
- radius: `rounded-sm` 8 / `rounded-md` 12 / `rounded-lg` 16 / `rounded-[20px]` (카드, 토큰 없음)
- 인라인 유지 4건 (OQ #4): rgba(255,255,255,0.07) 진행 바 트랙 / rgba(59,130,246,0.3) 카드 border / rgba(0,0,0,0.85) 오버레이 / boxShadow

### Negative rule (모든 task verify gate)
- 이모지 0건 (sketch 본문) — **단 `⋮` (U+22EE) + `⎋` (U+238B) 2 글리프는 OQ #5 LOCKED 예외 anchor**
- `linear-gradient` 0건 (모든 sketch + checklist — OQ #1 + OQ #2 LOCKED 그라데이션 완전 폐기 증거 = 설치 버튼 그라데이션 + 진행 바 그라데이션 2건 폐기)
- 9·10·11px fontSize 0건 (OQ #3 LOCKED 격상 결과)
- `(text|bg|border)-status-` class prefix 0건 (memory `feedback_tailwind_token_class_pattern`)
- `\b(w|h)-8\b` 0건 (tailwind config w-8=48 함정, memory `feedback_tailwind_w8_h8_is_48px`)
- 옛 alias 토큰 (`var(--bg)` `var(--bg2)` `var(--bg3)` `var(--bd)` `var(--bd2)` `var(--t1)` `var(--t2)` `var(--t3)` `var(--acl)` `var(--accent)`) 0건 in sketch (단, tokens.css 정의 블록 인용 시 fence 안 예외)

### 평면 폴더 패턴 (13-schedule + 14-reports + 27-login + 16-workshift + 17-annual-plan 일관)
- 28-splash/ 직속에 4 파일 배치
- `sketch/` 서브폴더 절대 금지

### ★ 비즈 anchor 16건 (1 byte 변경 금지, W2~W5 모두 박제)
**SplashScreen 8건:**
1. `__APP_VERSION__` Vite define (line 73)
2. `@keyframes slideUp { from{opacity:0;transform:translateY(16px)} to{opacity:1;transform:translateY(0)} }` (line 79)
3. `48ms` setInterval tick (line 18)
4. `1600ms` setTimeout (line 26)
5. `+4` increment per tick (line 18)
6. `checkVersionAndRefresh()` void 호출 (line 16)
7. `isAuthenticated ? '/dashboard' : '/login'` 분기 (line 24, 34)
8. `shouldShowInstallPrompt()` 분기 (line 21)

**InstallPrompt 8건:**
1. `isStandalone()` 함수 (line 5~10, iOS standalone + display-mode standalone/fullscreen 3분기)
2. `isIOS()` 함수 (line 12~17, iPhone/iPad/iPod + Macintosh+maxTouchPoints fallback)
3. `getDeferredInstallPrompt` / `subscribeInstallPrompt` / `showInstallPrompt` import (line 2)
4. `shouldShowInstallPrompt()` export (line 224~233, isStandalone + 24h TTL)
5. `dismissInstallPrompt()` export (line 235~237, localStorage.setItem)
6. localStorage key `'pwa-install-dismissed'` + TTL `24 * 60 * 60 * 1000`
7. outcome 3 분기: `'accepted'` → onDismiss / `'unavailable'` → setShowAndroidGuide(true) / `'dismissed'` → no-op (line 40~47)
8. 3 view state: 메인 (!showIOSGuide && !showAndroidGuide) / Android (showAndroidGuide) / iOS (else) — line 88~215

### Tailwind cheatsheet (v0.1.1 토큰 → utility class 매핑)
| v0.1.1 토큰 | Tailwind utility | 28-splash 적용 위치 |
|---|---|---|
| `--surface-page` | `bg-surface-page` | SplashScreen 외곽 |
| `--surface-raised` | `bg-surface-raised` | InstallPrompt 카드 |
| `--text-primary` | `text-text-primary` | 모든 제목 / step 본문 |
| `--text-secondary` | `text-text-secondary` | 부제 / step 하위 설명 |
| `--text-tertiary` | `text-text-tertiary` | "나중에 할게요" 버튼 / 진행 라벨 |
| `--text-disabled` | `text-text-disabled` | SplashScreen 버전 문자열 |
| `--safe-bar` | `bg-safe-bar` + `text-text-on-accent` | InstallPrompt 설치 버튼 (OQ #1) |
| `--accent` | `bg-accent` / `bg-accent/15` / `text-accent` | 진행 바 fill (OQ #2) / 숫자 원 / 확인했습니다 버튼 |
| `--warning` | `text-warning` / `bg-warning-bg` / `border-warning/25` | Android 노란 경고 (OQ #4) |
| (radius 8) | `rounded-sm` | 모든 step 카드 / 확인했습니다 버튼 |
| (radius 10) | `rounded-md` | "나중에 할게요" / 확인했습니다 (line 104/142/207) |
| (radius 12) | `rounded-md` | 설치 버튼 (line 93) |
| (radius 16) | `rounded-lg` | InstallPrompt 아이콘 박스 (line 73) |
| (radius 20) | `rounded-[20px]` | InstallPrompt 카드 (line 65, 토큰 없음) |
| (radius 22) | `rounded-[22px]` | SplashScreen 로고 박스 (line 48, 토큰 없음) |
| 인라인 유지 (OQ #4) | `style={{ background:'rgba(255,255,255,0.07)' }}` | 진행 바 트랙 |
| 인라인 유지 (OQ #4) | `style={{ border:'1px solid rgba(59,130,246,0.3)' }}` | 카드 border |
| 인라인 유지 (OQ #4) | `style={{ background:'rgba(0,0,0,0.85)' }}` | 오버레이 |
| 인라인 유지 (OQ #4) | `style={{ boxShadow:'0 8px 40px rgba(0,0,0,0.5)' }}` | 카드 shadow |
| 인라인 유지 (OQ #5) | `<span style={{ fontSize:16, verticalAlign:'middle' }}>⋮</span>` | Android ⋮ 메뉴 |
| 인라인 유지 (OQ #5) | `<span style={{ fontSize:16, verticalAlign:'middle' }}>⎋</span>` | iOS ⎋ 공유 |
</rules_verbatim>
</context>

<tasks>

<task type="auto">
  <name>Task 1 (W2): SplashScreen 전체 sketch 생성 (4 frame 진행률 매트릭스, bg-accent solid 진행 바 + slideUp animation + 폰트 격상)</name>
  <files>cha-bio-safety/docs/redesign-context/28-splash/sketch-wave-2-splash.html</files>
  <action>
    17-annual-plan/sketch-wave-2-chrome.html 패턴 mirror 로 새 sketch 파일 작성. SplashScreen.tsx 전체 (line 38~82) 시각화. **OQ #2 LOCKED (진행 바 bg-accent solid + 그라데이션 폐기)** + **OQ #3 LOCKED (타이틀 22→text-heading font-black tracking-tight / 부제 12 text-caption leading-relaxed 유지 / 진행 라벨 11→12 text-caption leading-none 격상 / 버전 10→12 text-caption leading-none 격상)** + **OQ #4 LOCKED (page #161b22 → bg-surface-page / 진행 바 트랙 rgba(255,255,255,0.07) 인라인 유지)** 3건이 본 wave 의 핵심.

    **구조 (17-annual-plan W2 mirror, 4 frame):**
    - `<!DOCTYPE html>` + `<html lang="ko">` + `<head><meta charset="utf-8"><title>redesign/28-splash — W2 SplashScreen</title>` + `<style>` 블록
    - `<style>` 안: v0.1.1 tokens.css 의 dark+light 양쪽 정의 verbatim 인용 + typography.css 의 `text-caption` `text-label` `text-body` `text-title` `text-heading` `leading-none` `leading-relaxed` `font-bold` `font-extrabold` `font-black` `tracking-tight` 정의 verbatim 인용 + `@keyframes slideUp { from{opacity:0;transform:translateY(16px)} to{opacity:1;transform:translateY(0)} }` 박제 (anchor)
    - `<body>` 안 4 frame side-by-side (진행률 매트릭스):
      | frame | viewport | 시각화 |
      |---|---|---|
      | 1 | 모바일 다크 393×800 진행 0% | `data-theme="dark"` + bg-surface-page 외곽 + 88x88 로고 박스 + 'CBC 방재' + '소방안전 통합관리 시스템' + 진행 바 0% + '시스템 초기화 중...' + 버전 |
      | 2 | 모바일 다크 393×800 진행 50% | `data-theme="dark"` + 동일 + 진행 바 width:50% + '데이터 불러오는 중...' |
      | 3 | 모바일 다크 393×800 진행 100% '준비 완료' | `data-theme="dark"` + 동일 + 진행 바 width:100% + '준비 완료' |
      | 4 | 모바일 라이트 393×800 진행 50% | `data-theme="light"` + 동일 (라이트 명도 차 확인) |

    **외곽 마크업 룰 (frame 공통 — OQ #4 LOCKED page 토큰):**
    ```html
    <div class="bg-surface-page" style="min-height:100dvh;display:flex;flex-direction:column;align-items:center;justify-content:center;gap:0;position:relative;">
      <!-- 로고 + 타이틀 묶음 -->
      <div style="animation:slideUp .4s ease-out;display:flex;flex-direction:column;align-items:center;gap:20px;margin-bottom:56px;">
        <!-- 로고 박스 (88x88 rounded-[22px], OQ #4 LOCKED 인라인 rgba 유지) -->
        <div style="position:relative;">
          <div class="rounded-[22px]" style="width:88px;height:88px;background:rgba(37,99,235,0.2);border:1px solid rgba(59,130,246,0.3);display:flex;align-items:center;justify-content:center;overflow:hidden;">
            <img src="/icons/icon-192.png" alt="" style="width:64px;height:64px;border-radius:14px;" />
          </div>
        </div>
        <!-- 타이틀 (OQ #3 LOCKED: 22→text-heading font-black tracking-tight) -->
        <div style="text-align:center;">
          <h1 class="text-heading font-black tracking-tight text-text-primary" style="margin:0;">CBC 방재</h1>
          <!-- 부제 (OQ #3 LOCKED: 12 text-caption leading-relaxed 유지 + letterSpacing 인라인) -->
          <p class="text-caption leading-relaxed text-text-tertiary" style="margin:6px 0 0;letter-spacing:.1em;">소방안전 통합관리 시스템</p>
        </div>
      </div>

      <!-- 진행 바 (160 width) -->
      <div style="width:160px;">
        <!-- 트랙 (OQ #4 LOCKED 인라인 rgba 유지) -->
        <div style="height:2px;background:rgba(255,255,255,0.07);border-radius:2px;overflow:hidden;">
          <!-- fill (OQ #2 LOCKED bg-accent solid, 그라데이션 폐기) -->
          <div class="bg-accent" style="height:100%;border-radius:2px;width:50%;transition:width .05s linear;"></div>
        </div>
        <!-- 진행 라벨 (OQ #3 LOCKED: 11→12 text-caption leading-none 격상) -->
        <p class="text-caption leading-none text-text-tertiary" style="text-align:center;margin-top:10px;">데이터 불러오는 중...</p>
      </div>

      <!-- 버전 (OQ #3 LOCKED: 10→12 text-caption leading-none 격상) -->
      <p class="text-caption leading-none text-text-disabled" style="position:absolute;bottom:20px;">v0.1.1 · 경기도 성남시 분당구</p>
    </div>
    ```

    **frame 별 진행 라벨 매핑 (line 69 verbatim):**
    - frame 1 (0%): 'pct < 40 → 시스템 초기화 중...'
    - frame 2 (50%): 'pct >= 40 && pct < 80 → 데이터 불러오는 중...'
    - frame 3 (100%): 'pct >= 80 → 준비 완료'
    - frame 4 (50%): '데이터 불러오는 중...' (라이트 명도 차 확인)

    **카피 verbatim (memory `feedback_sketch_realistic_data`):**
    - 'CBC 방재' (line 58)
    - '소방안전 통합관리 시스템' (line 59)
    - '시스템 초기화 중...' / '데이터 불러오는 중...' / '준비 완료' (line 69 3 분기)
    - 'v0.1.1 · 경기도 성남시 분당구' (line 73 — sketch 에서는 `__APP_VERSION__` 대신 v0.1.1 placeholder)

    **rules 박스 (sketch 하단, viewport-label 외 메타):**
    - LOCKED 결정: OQ #2 (진행 바 bg-accent solid + linear-gradient(90deg,#3b82f6,#0ea5e9) 완전 폐기 / 트랙 rgba(255,255,255,0.07) 인라인 유지) + OQ #3 (타이틀 22→text-heading font-black tracking-tight / 부제 12 text-caption leading-relaxed 유지 / 진행 라벨 11→12 text-caption leading-none 격상 / 버전 10→12 text-caption leading-none 격상) + OQ #4 (page #161b22 → bg-surface-page / text #e6edf3 → text-text-primary / text #6e7681 → text-text-tertiary / text #3d444d → text-text-disabled / 진행 바 트랙 + 카드 border + 오버레이 + boxShadow 인라인 유지) verbatim 인용 1 fence
    - **비즈 anchor 8건 박제 fence** (SplashScreen):
      ```
      1. __APP_VERSION__ Vite define (line 73)
      2. @keyframes slideUp { from{opacity:0;transform:translateY(16px)} to{opacity:1;transform:translateY(0)} } (line 79)
      3. 48ms setInterval tick (line 18)
      4. 1600ms setTimeout (line 26)
      5. +4 increment per tick (line 18)
      6. checkVersionAndRefresh() void 호출 (line 16)
      7. isAuthenticated ? '/dashboard' : '/login' 분기 (line 24, 34)
      8. shouldShowInstallPrompt() 분기 (line 21)
      ```
    - 토큰 매핑: `#161b22 → bg-surface-page` / `#e6edf3 → text-text-primary` / `#6e7681 → text-text-tertiary` / `#3d444d → text-text-disabled` / 진행 바 fill `linear-gradient(90deg,#3b82f6,#0ea5e9) → bg-accent` (OQ #2) / radius 22 → `rounded-[22px]` (토큰 없음)
    - negative gate 자체매칭 회피: "이모지 0 / linear-gradient 0 / 9·10·11px 0 / status- prefix 0 / w-8 h-8 0 / 옛 alias 토큰 0" 메타 줄은 `<code>` fence 안 또는 escape (sketch 본문 안 직접 등장 금지)

    **commit 메시지:**
    `docs(quick-260522-2q6): redesign/28-splash W2 splash sketch (4 frame 진행률 매트릭스, bg-accent solid 진행 바 + 폰트 격상 + slideUp animation + 비즈 anchor 8건 박제)`

    **commit 명령 (executor 가 실행 후 commit hash + message 보고):**
    `git add cha-bio-safety/docs/redesign-context/28-splash/sketch-wave-2-splash.html && git commit -m "docs(quick-260522-2q6): redesign/28-splash W2 splash sketch (4 frame 진행률 매트릭스, bg-accent solid 진행 바 + 폰트 격상 + slideUp animation + 비즈 anchor 8건 박제)"`
  </action>
  <verify>
    <automated>
test -f cha-bio-safety/docs/redesign-context/28-splash/sketch-wave-2-splash.html \
  && wc -l cha-bio-safety/docs/redesign-context/28-splash/sketch-wave-2-splash.html | awk '{ if ($1 < 200) { print "FAIL: too short"; exit 1 } else print "PASS lines:", $1 }' \
  && grep -c 'data-theme="dark"' cha-bio-safety/docs/redesign-context/28-splash/sketch-wave-2-splash.html | awk '{ if ($1 < 3) { print "FAIL: dark frame <3 (frame 1/2/3)"; exit 1 } else print "PASS dark frame:", $1 }' \
  && grep -c 'data-theme="light"' cha-bio-safety/docs/redesign-context/28-splash/sketch-wave-2-splash.html | awk '{ if ($1 < 1) { print "FAIL: light frame <1 (frame 4)"; exit 1 } else print "PASS light frame:", $1 }' \
  && grep -c 'bg-surface-page' cha-bio-safety/docs/redesign-context/28-splash/sketch-wave-2-splash.html | awk '{ if ($1 < 4) { print "FAIL: bg-surface-page <4 (4 frame 외곽, OQ #4 LOCKED)"; exit 1 } else print "PASS bg-surface-page:", $1 }' \
  && grep -c 'bg-accent' cha-bio-safety/docs/redesign-context/28-splash/sketch-wave-2-splash.html | awk '{ if ($1 < 4) { print "FAIL: bg-accent <4 (진행 바 fill 4 frame, OQ #2 LOCKED)"; exit 1 } else print "PASS bg-accent:", $1 }' \
  && grep -c 'rgba(255,255,255,0.07)' cha-bio-safety/docs/redesign-context/28-splash/sketch-wave-2-splash.html | awk '{ if ($1 < 4) { print "FAIL: 진행 바 트랙 rgba <4 (OQ #4 LOCKED 인라인 유지)"; exit 1 } else print "PASS 진행 바 트랙 rgba:", $1 }' \
  && grep -c 'text-heading' cha-bio-safety/docs/redesign-context/28-splash/sketch-wave-2-splash.html | awk '{ if ($1 < 4) { print "FAIL: text-heading <4 (타이틀 OQ #3 LOCKED 22→text-heading)"; exit 1 } else print "PASS text-heading:", $1 }' \
  && grep -c 'font-black' cha-bio-safety/docs/redesign-context/28-splash/sketch-wave-2-splash.html | awk '{ if ($1 < 4) { print "FAIL: font-black <4 (타이틀)"; exit 1 } else print "PASS font-black:", $1 }' \
  && grep -c 'tracking-tight' cha-bio-safety/docs/redesign-context/28-splash/sketch-wave-2-splash.html | awk '{ if ($1 < 4) { print "FAIL: tracking-tight <4 (타이틀 OQ #3)"; exit 1 } else print "PASS tracking-tight:", $1 }' \
  && grep -c 'leading-relaxed' cha-bio-safety/docs/redesign-context/28-splash/sketch-wave-2-splash.html | awk '{ if ($1 < 4) { print "FAIL: leading-relaxed <4 (부제 OQ #3 12 유지)"; exit 1 } else print "PASS leading-relaxed:", $1 }' \
  && grep -c 'text-text-disabled' cha-bio-safety/docs/redesign-context/28-splash/sketch-wave-2-splash.html | awk '{ if ($1 < 4) { print "FAIL: text-text-disabled <4 (버전 OQ #4 #3d444d)"; exit 1 } else print "PASS text-text-disabled:", $1 }' \
  && grep -c 'CBC 방재' cha-bio-safety/docs/redesign-context/28-splash/sketch-wave-2-splash.html | awk '{ if ($1 < 4) { print "FAIL: CBC 방재 타이틀 <4 (4 frame)"; exit 1 } else print "PASS CBC 방재:", $1 }' \
  && grep -c '소방안전 통합관리 시스템' cha-bio-safety/docs/redesign-context/28-splash/sketch-wave-2-splash.html | awk '{ if ($1 < 4) { print "FAIL: 부제 <4 (4 frame)"; exit 1 } else print "PASS 부제:", $1 }' \
  && grep -c '시스템 초기화 중' cha-bio-safety/docs/redesign-context/28-splash/sketch-wave-2-splash.html | awk '{ if ($1 < 1) { print "FAIL: 시스템 초기화 중 카피 missing (frame 1 0%)"; exit 1 } else print "PASS 시스템 초기화 중:", $1 }' \
  && grep -c '데이터 불러오는 중' cha-bio-safety/docs/redesign-context/28-splash/sketch-wave-2-splash.html | awk '{ if ($1 < 2) { print "FAIL: 데이터 불러오는 중 카피 <2 (frame 2/4 50%)"; exit 1 } else print "PASS 데이터 불러오는 중:", $1 }' \
  && grep -c '준비 완료' cha-bio-safety/docs/redesign-context/28-splash/sketch-wave-2-splash.html | awk '{ if ($1 < 1) { print "FAIL: 준비 완료 카피 missing (frame 3 100%)"; exit 1 } else print "PASS 준비 완료:", $1 }' \
  && grep -c '경기도 성남시 분당구' cha-bio-safety/docs/redesign-context/28-splash/sketch-wave-2-splash.html | awk '{ if ($1 < 4) { print "FAIL: 위치 카피 <4 (버전 4 frame)"; exit 1 } else print "PASS 위치 카피:", $1 }' \
  && grep -c '@keyframes slideUp' cha-bio-safety/docs/redesign-context/28-splash/sketch-wave-2-splash.html | awk '{ if ($1 < 1) { print "FAIL: slideUp keyframes anchor missing"; exit 1 } else print "PASS slideUp keyframes:", $1 }' \
  && grep -c '__APP_VERSION__' cha-bio-safety/docs/redesign-context/28-splash/sketch-wave-2-splash.html | awk '{ if ($1 < 1) { print "FAIL: __APP_VERSION__ anchor missing (비즈 anchor 박제)"; exit 1 } else print "PASS __APP_VERSION__:", $1 }' \
  && grep -c '48ms\|1600ms\|+4 increment\|checkVersionAndRefresh\|shouldShowInstallPrompt\|isAuthenticated' cha-bio-safety/docs/redesign-context/28-splash/sketch-wave-2-splash.html | awk '{ if ($1 < 4) { print "FAIL: 비즈 anchor 8건 박제 <4"; exit 1 } else print "PASS 비즈 anchor hits:", $1 }' \
  && grep -cE 'linear-gradient' cha-bio-safety/docs/redesign-context/28-splash/sketch-wave-2-splash.html | awk '{ if ($1 > 0) { print "FAIL: linear-gradient found (OQ #2 LOCKED 위반)"; exit 1 } else print "PASS no gradient" }' \
  && grep -cE '(text|bg|border)-status-(safe|fire|warning|danger|caution)' cha-bio-safety/docs/redesign-context/28-splash/sketch-wave-2-splash.html | awk '{ if ($1 > 0) { print "FAIL: status- prefix found"; exit 1 } else print "PASS no status- prefix" }' \
  && grep -v '^<!--' cha-bio-safety/docs/redesign-context/28-splash/sketch-wave-2-splash.html | grep -v '^#' | grep -cE 'font-size:[[:space:]]*(9|10|11)[^0-9]|fontSize:[[:space:]]*(9|10|11)[^0-9]' | awk '{ if ($1 > 0) { print "FAIL: 9/10/11 px found"; exit 1 } else print "PASS no 9-11px" }' \
  && grep -cE '\bw-8\b|\bh-8\b' cha-bio-safety/docs/redesign-context/28-splash/sketch-wave-2-splash.html | awk '{ if ($1 > 0) { print "FAIL: w-8/h-8 found (48px 함정)"; exit 1 } else print "PASS no w-8 h-8" }' \
  && grep -cE 'OQ #2|OQ #3|OQ #4' cha-bio-safety/docs/redesign-context/28-splash/sketch-wave-2-splash.html | awk '{ if ($1 < 3) { print "FAIL: OQ #2/#3/#4 citation <3"; exit 1 } else print "PASS OQ citations:", $1 }' \
  && git log --oneline -1 | grep -q '260522-2q6.*W2' && echo "PASS commit W2"
    </automated>
  </verify>
  <done>
    - sketch-wave-2-splash.html 파일 존재 + ≥200 lines (≥320 권장)
    - data-theme="dark" ≥3 / data-theme="light" ≥1 (총 4 frame 진행률 매트릭스)
    - bg-surface-page (외곽) ≥4 / bg-accent (진행 바 fill) ≥4 (OQ #2 + OQ #4 LOCKED)
    - rgba(255,255,255,0.07) (진행 바 트랙) ≥4 (OQ #4 LOCKED 인라인 유지)
    - text-heading + font-black + tracking-tight (타이틀) 각 ≥4 (OQ #3 LOCKED 22→text-heading 격상)
    - leading-relaxed (부제 12 유지) ≥4 / text-text-disabled (버전 #3d444d → 토큰) ≥4
    - 'CBC 방재' / '소방안전 통합관리 시스템' / '경기도 성남시 분당구' 각 ≥4
    - 진행 라벨 3 분기 모두 박제 ('시스템 초기화 중' ≥1 / '데이터 불러오는 중' ≥2 / '준비 완료' ≥1)
    - @keyframes slideUp anchor ≥1 / __APP_VERSION__ anchor ≥1
    - 비즈 anchor 8건 박제 — 48ms / 1600ms / +4 increment / checkVersionAndRefresh / shouldShowInstallPrompt / isAuthenticated hit ≥4
    - linear-gradient 0건 (OQ #2 LOCKED) / status- prefix 0건 / 9·10·11px fontSize 0건 / w-8 h-8 0건
    - OQ #2 + OQ #3 + OQ #4 인용 ≥3건
    - atomic commit `docs(quick-260522-2q6): ... W2 splash sketch ...` 1개
  </done>
</task>

<task type="auto">
  <name>Task 2 (W3): InstallPrompt 메인 view sketch 생성 (3 frame, bg-safe-bar solid 설치 버튼 + 카드 + 오버레이 + hasPrompt 분기)</name>
  <files>cha-bio-safety/docs/redesign-context/28-splash/sketch-wave-3-install-main.html</files>
  <action>
    17-annual-plan/sketch-wave-3-preview-calibration.html 패턴 mirror 로 새 sketch 파일 작성. InstallPrompt.tsx 메인 view (line 57~111, `!showIOSGuide && !showAndroidGuide` 분기) 시각화. **OQ #1 LOCKED (설치 버튼 bg-safe-bar solid + 그라데이션 완전 폐기)** + **OQ #3 LOCKED (카드 제목 18→text-title font-extrabold / 부제 12 text-caption leading-relaxed 유지 / 설치 버튼 15→16 text-body font-bold 격상 / 나중에 할게요 12 text-caption font-bold leading-none 유지)** + **OQ #4 LOCKED (카드 background #1c2128 → bg-surface-raised / 카드 border rgba(59,130,246,0.3) 인라인 유지 / 오버레이 rgba(0,0,0,0.85) 인라인 유지 / boxShadow 인라인 유지 / text #e6edf3 → text-text-primary / text #8b949e → text-text-secondary / text #6e7681 → text-text-tertiary)** 3건이 본 wave 의 핵심.

    **구조 (17-annual-plan W3 mirror, 3 frame):**
    - `<!DOCTYPE html>` + `<head><title>redesign/28-splash — W3 InstallPrompt 메인 view</title>` + `<style>` 블록 (W2 와 동일 tokens + typography 인용)
    - `<body>` 안 3 frame side-by-side:
      | frame | viewport | 시각화 |
      |---|---|---|
      | 1 | 모바일 다크 393×700 hasPrompt=true | `data-theme="dark"` + 오버레이 rgba(0,0,0,0.85) + 카드 bg-surface-raised + 64x64 아이콘 박스 + 'CBC 방재' text-title font-extrabold + 부제 + 설치 버튼 '홈 화면에 설치' bg-safe-bar + '나중에 할게요' |
      | 2 | 모바일 다크 393×700 hasPrompt=false | `data-theme="dark"` + 동일 + 설치 버튼 카피 '설치 방법 보기' (line 99 ternary fallback) |
      | 3 | 모바일 라이트 393×700 hasPrompt=true | `data-theme="light"` + 동일 (라이트 명도 차) |

    **오버레이 + 카드 마크업 룰 (frame 공통 — OQ #4 LOCKED 인라인 유지 + OQ #3 LOCKED 폰트 + OQ #1 LOCKED 설치 버튼):**
    ```html
    <!-- 오버레이 (OQ #4 LOCKED 인라인 rgba 유지) -->
    <div style="position:fixed;inset:0;z-index:9999;background:rgba(0,0,0,0.85);display:flex;align-items:center;justify-content:center;padding:24px;">
      <!-- 카드 (OQ #4 LOCKED: bg-surface-raised + 인라인 border rgba + boxShadow) -->
      <div class="bg-surface-raised rounded-[20px]" style="padding:28px 24px;max-width:340px;width:100%;text-align:center;border:1px solid rgba(59,130,246,0.3);box-shadow:0 8px 40px rgba(0,0,0,0.5);">
        <!-- 아이콘 박스 (64x64 rounded-lg, OQ #4 LOCKED 인라인 rgba 유지) -->
        <div class="rounded-lg" style="width:64px;height:64px;margin:0 auto 16px;background:rgba(37,99,235,0.2);border:1px solid rgba(59,130,246,0.3);display:flex;align-items:center;justify-content:center;">
          <img src="/icons/icon-192.png" alt="" style="width:48px;height:48px;border-radius:12px;" />
        </div>
        <!-- 제목 (OQ #3 LOCKED: 18→text-title font-extrabold) -->
        <h2 class="text-title font-extrabold text-text-primary" style="margin:0 0 8px;">CBC 방재</h2>
        <!-- 부제 (OQ #3 LOCKED: 12 text-caption leading-relaxed 유지) -->
        <p class="text-caption leading-relaxed text-text-secondary" style="margin:0 0 20px;">
          홈 화면에 앱을 설치하면<br/>더 빠르고 편리하게 사용할 수 있습니다
        </p>
        <!-- 메인 버튼 묶음 (showIOSGuide && showAndroidGuide false 분기) -->
        <div style="display:flex;flex-direction:column;gap:10px;">
          <!-- 설치 버튼 (OQ #1 LOCKED: bg-safe-bar solid + OQ #3 LOCKED: 15→16 text-body font-bold 격상, 그라데이션 폐기) -->
          <button class="bg-safe-bar text-text-on-accent text-body font-bold rounded-md" style="width:100%;height:48px;border:none;cursor:pointer;">
            홈 화면에 설치
          </button>
          <!-- 나중에 할게요 (OQ #3 LOCKED: 12 text-caption font-bold leading-none 유지, OQ #4 LOCKED: text #6e7681 → text-text-tertiary) -->
          <button class="text-caption font-bold leading-none text-text-tertiary rounded-md" style="width:100%;height:40px;background:transparent;border:1px solid rgba(255,255,255,0.1);cursor:pointer;">
            나중에 할게요
          </button>
        </div>
      </div>
    </div>
    ```

    **hasPrompt 분기 (line 99 verbatim):**
    - frame 1 (hasPrompt true, !ios): '홈 화면에 설치'
    - frame 2 (hasPrompt false 또는 ios true): '설치 방법 보기'
    - frame 3 (hasPrompt true, !ios): '홈 화면에 설치' (라이트)

    **카피 verbatim (memory `feedback_sketch_realistic_data`):**
    - 'CBC 방재' (line 82)
    - '홈 화면에 앱을 설치하면\n더 빠르고 편리하게 사용할 수 있습니다' (line 85 — `<br/>` 보존)
    - '홈 화면에 설치' / '설치 방법 보기' (line 99 ternary 분기)
    - '나중에 할게요' (line 109)

    **rules 박스:**
    - LOCKED 결정: OQ #1 (설치 버튼 bg-safe-bar text-text-on-accent solid + linear-gradient(135deg, #2563eb, #0ea5e9) 완전 폐기) + OQ #3 (카드 제목 18→text-title font-extrabold / 부제 12 text-caption leading-relaxed 유지 / 설치 버튼 15→16 text-body font-bold 격상 / 나중에 할게요 12 text-caption font-bold leading-none 유지) + OQ #4 (카드 #1c2128 → bg-surface-raised / text #e6edf3 → text-text-primary / text #8b949e → text-text-secondary / text #6e7681 → text-text-tertiary / **인라인 유지 4건** — 오버레이 rgba(0,0,0,0.85) / 카드 border rgba(59,130,246,0.3) / boxShadow 0 8px 40px rgba(0,0,0,0.5) / 아이콘 박스 rgba(37,99,235,0.2) + rgba(59,130,246,0.3)) verbatim 인용 1 fence
    - **비즈 anchor 8건 박제 fence** (InstallPrompt):
      ```
      1. isStandalone() (line 5~10, iOS standalone + display-mode standalone/fullscreen 3분기)
      2. isIOS() (line 12~17, iPhone/iPad/iPod + Macintosh+maxTouchPoints fallback)
      3. getDeferredInstallPrompt / subscribeInstallPrompt / showInstallPrompt import (line 2)
      4. shouldShowInstallPrompt() export (line 224~233, isStandalone + 24h TTL)
      5. dismissInstallPrompt() export (line 235~237, localStorage.setItem)
      6. localStorage key 'pwa-install-dismissed' + TTL 24 * 60 * 60 * 1000
      7. outcome 3 분기: 'accepted' → onDismiss / 'unavailable' → setShowAndroidGuide(true) / 'dismissed' → no-op (line 40~47)
      8. 3 view state: 메인 (!showIOSGuide && !showAndroidGuide) / Android / iOS (line 88~215)
      ```
    - 토큰 매핑: 카드 background `#1c2128 → bg-surface-raised` / radius 20 → `rounded-[20px]` (토큰 없음) / 설치 버튼 그라데이션 `linear-gradient(135deg, #2563eb, #0ea5e9) → bg-safe-bar` (OQ #1) / 아이콘 박스 radius 16 → `rounded-lg`
    - negative gate 자체매칭 회피: fence escape

    **commit 메시지:**
    `docs(quick-260522-2q6): redesign/28-splash W3 install-main sketch (3 frame, bg-safe-bar solid 설치 버튼 + bg-surface-raised 카드 + 인라인 유지 4건 + 비즈 anchor 8건 박제)`

    **commit 명령:**
    `git add cha-bio-safety/docs/redesign-context/28-splash/sketch-wave-3-install-main.html && git commit -m "docs(quick-260522-2q6): redesign/28-splash W3 install-main sketch (3 frame, bg-safe-bar solid 설치 버튼 + bg-surface-raised 카드 + 인라인 유지 4건 + 비즈 anchor 8건 박제)"`
  </action>
  <verify>
    <automated>
test -f cha-bio-safety/docs/redesign-context/28-splash/sketch-wave-3-install-main.html \
  && wc -l cha-bio-safety/docs/redesign-context/28-splash/sketch-wave-3-install-main.html | awk '{ if ($1 < 200) { print "FAIL: too short"; exit 1 } else print "PASS lines:", $1 }' \
  && grep -c 'data-theme="dark"' cha-bio-safety/docs/redesign-context/28-splash/sketch-wave-3-install-main.html | awk '{ if ($1 < 2) { print "FAIL: dark frame <2"; exit 1 } else print "PASS dark frame:", $1 }' \
  && grep -c 'data-theme="light"' cha-bio-safety/docs/redesign-context/28-splash/sketch-wave-3-install-main.html | awk '{ if ($1 < 1) { print "FAIL: light frame <1"; exit 1 } else print "PASS light frame:", $1 }' \
  && grep -c 'bg-safe-bar' cha-bio-safety/docs/redesign-context/28-splash/sketch-wave-3-install-main.html | awk '{ if ($1 < 3) { print "FAIL: bg-safe-bar <3 (설치 버튼 3 frame, OQ #1 LOCKED)"; exit 1 } else print "PASS bg-safe-bar:", $1 }' \
  && grep -c 'text-text-on-accent' cha-bio-safety/docs/redesign-context/28-splash/sketch-wave-3-install-main.html | awk '{ if ($1 < 3) { print "FAIL: text-text-on-accent <3"; exit 1 } else print "PASS text-text-on-accent:", $1 }' \
  && grep -c 'bg-surface-raised' cha-bio-safety/docs/redesign-context/28-splash/sketch-wave-3-install-main.html | awk '{ if ($1 < 3) { print "FAIL: bg-surface-raised <3 (카드 3 frame, OQ #4 LOCKED)"; exit 1 } else print "PASS bg-surface-raised:", $1 }' \
  && grep -c 'rgba(0,0,0,0.85)' cha-bio-safety/docs/redesign-context/28-splash/sketch-wave-3-install-main.html | awk '{ if ($1 < 3) { print "FAIL: 오버레이 rgba <3 (OQ #4 LOCKED 인라인 유지)"; exit 1 } else print "PASS 오버레이 rgba:", $1 }' \
  && grep -c 'rgba(59,130,246,0.3)' cha-bio-safety/docs/redesign-context/28-splash/sketch-wave-3-install-main.html | awk '{ if ($1 < 3) { print "FAIL: 카드 border rgba <3 (OQ #4 LOCKED 인라인 유지)"; exit 1 } else print "PASS 카드 border rgba:", $1 }' \
  && grep -c 'box-shadow:0 8px 40px\|boxShadow' cha-bio-safety/docs/redesign-context/28-splash/sketch-wave-3-install-main.html | awk '{ if ($1 < 3) { print "FAIL: boxShadow <3 (OQ #4 LOCKED 인라인 유지)"; exit 1 } else print "PASS boxShadow:", $1 }' \
  && grep -c 'text-title' cha-bio-safety/docs/redesign-context/28-splash/sketch-wave-3-install-main.html | awk '{ if ($1 < 3) { print "FAIL: text-title <3 (카드 제목 OQ #3 LOCKED 18→text-title)"; exit 1 } else print "PASS text-title:", $1 }' \
  && grep -c 'font-extrabold' cha-bio-safety/docs/redesign-context/28-splash/sketch-wave-3-install-main.html | awk '{ if ($1 < 3) { print "FAIL: font-extrabold <3 (카드 제목)"; exit 1 } else print "PASS font-extrabold:", $1 }' \
  && grep -c 'text-body' cha-bio-safety/docs/redesign-context/28-splash/sketch-wave-3-install-main.html | awk '{ if ($1 < 3) { print "FAIL: text-body <3 (설치 버튼 OQ #3 LOCKED 15→16 격상)"; exit 1 } else print "PASS text-body:", $1 }' \
  && grep -c 'text-text-secondary' cha-bio-safety/docs/redesign-context/28-splash/sketch-wave-3-install-main.html | awk '{ if ($1 < 3) { print "FAIL: text-text-secondary <3 (부제 OQ #4 #8b949e)"; exit 1 } else print "PASS text-text-secondary:", $1 }' \
  && grep -c 'CBC 방재' cha-bio-safety/docs/redesign-context/28-splash/sketch-wave-3-install-main.html | awk '{ if ($1 < 3) { print "FAIL: CBC 방재 제목 <3 (3 frame)"; exit 1 } else print "PASS CBC 방재:", $1 }' \
  && grep -c '홈 화면에 앱을 설치하면' cha-bio-safety/docs/redesign-context/28-splash/sketch-wave-3-install-main.html | awk '{ if ($1 < 3) { print "FAIL: 부제 카피 <3"; exit 1 } else print "PASS 부제 카피:", $1 }' \
  && grep -c '홈 화면에 설치' cha-bio-safety/docs/redesign-context/28-splash/sketch-wave-3-install-main.html | awk '{ if ($1 < 2) { print "FAIL: 홈 화면에 설치 카피 <2 (hasPrompt true frame 1/3)"; exit 1 } else print "PASS 홈 화면에 설치:", $1 }' \
  && grep -c '설치 방법 보기' cha-bio-safety/docs/redesign-context/28-splash/sketch-wave-3-install-main.html | awk '{ if ($1 < 1) { print "FAIL: 설치 방법 보기 카피 missing (frame 2 hasPrompt false)"; exit 1 } else print "PASS 설치 방법 보기:", $1 }' \
  && grep -c '나중에 할게요' cha-bio-safety/docs/redesign-context/28-splash/sketch-wave-3-install-main.html | awk '{ if ($1 < 3) { print "FAIL: 나중에 할게요 <3"; exit 1 } else print "PASS 나중에 할게요:", $1 }' \
  && grep -c 'isStandalone\|isIOS\|shouldShowInstallPrompt\|dismissInstallPrompt\|pwa-install-dismissed\|outcome.*accepted\|3 view state' cha-bio-safety/docs/redesign-context/28-splash/sketch-wave-3-install-main.html | awk '{ if ($1 < 4) { print "FAIL: InstallPrompt 비즈 anchor 8건 박제 hit <4"; exit 1 } else print "PASS 비즈 anchor hits:", $1 }' \
  && grep -cE 'linear-gradient' cha-bio-safety/docs/redesign-context/28-splash/sketch-wave-3-install-main.html | awk '{ if ($1 > 0) { print "FAIL: linear-gradient found (OQ #1 LOCKED 위반)"; exit 1 } else print "PASS no gradient" }' \
  && grep -cE '(text|bg|border)-status-(safe|fire|warning|danger|caution)' cha-bio-safety/docs/redesign-context/28-splash/sketch-wave-3-install-main.html | awk '{ if ($1 > 0) { print "FAIL: status- prefix found"; exit 1 } else print "PASS no status- prefix" }' \
  && grep -v '^<!--' cha-bio-safety/docs/redesign-context/28-splash/sketch-wave-3-install-main.html | grep -v '^#' | grep -cE 'font-size:[[:space:]]*(9|10|11)[^0-9]|fontSize:[[:space:]]*(9|10|11)[^0-9]' | awk '{ if ($1 > 0) { print "FAIL: 9/10/11 px found"; exit 1 } else print "PASS no 9-11px" }' \
  && grep -cE '\bw-8\b|\bh-8\b' cha-bio-safety/docs/redesign-context/28-splash/sketch-wave-3-install-main.html | awk '{ if ($1 > 0) { print "FAIL: w-8/h-8 found"; exit 1 } else print "PASS no w-8 h-8" }' \
  && grep -cE 'OQ #1|OQ #3|OQ #4' cha-bio-safety/docs/redesign-context/28-splash/sketch-wave-3-install-main.html | awk '{ if ($1 < 3) { print "FAIL: OQ #1/#3/#4 citation <3"; exit 1 } else print "PASS OQ citations:", $1 }' \
  && git log --oneline -1 | grep -q '260522-2q6.*W3' && echo "PASS commit W3"
    </automated>
  </verify>
  <done>
    - sketch-wave-3-install-main.html 파일 존재 + ≥200 lines (≥320 권장)
    - data-theme="dark" ≥2 / data-theme="light" ≥1 (총 3 frame)
    - bg-safe-bar (설치 버튼) ≥3 / text-text-on-accent ≥3 (OQ #1 LOCKED solid)
    - bg-surface-raised (카드) ≥3 (OQ #4 LOCKED)
    - 인라인 유지 4건 grep — rgba(0,0,0,0.85) ≥3 / rgba(59,130,246,0.3) ≥3 / boxShadow ≥3
    - text-title + font-extrabold (카드 제목 OQ #3 LOCKED 18→text-title) 각 ≥3
    - text-body (설치 버튼 OQ #3 LOCKED 15→16 격상) ≥3
    - text-text-secondary (부제 OQ #4 #8b949e) ≥3
    - 'CBC 방재' ≥3 / '홈 화면에 앱을 설치하면' ≥3 / '홈 화면에 설치' ≥2 / '설치 방법 보기' ≥1 / '나중에 할게요' ≥3
    - InstallPrompt 비즈 anchor 8건 박제 (isStandalone / isIOS / shouldShowInstallPrompt / dismissInstallPrompt / pwa-install-dismissed / outcome accepted / 3 view state) hit ≥4
    - linear-gradient 0건 (OQ #1 LOCKED) / status- prefix 0건 / 9·10·11px fontSize 0건 / w-8 h-8 0건
    - OQ #1 + OQ #3 + OQ #4 인용 ≥3건
    - atomic commit `docs(quick-260522-2q6): ... W3 install-main sketch ...` 1개
  </done>
</task>

<task type="auto">
  <name>Task 3 (W4): InstallPrompt Android + iOS 가이드 view sketch 생성 (3 frame, 노란 경고 + 3-step + 특수 글리프 ⋮ ⎋ 보존)</name>
  <files>cha-bio-safety/docs/redesign-context/28-splash/sketch-wave-4-install-guides.html</files>
  <action>
    17-annual-plan/sketch-wave-4-download.html 패턴 mirror 로 새 sketch 파일 작성. InstallPrompt.tsx Android 가이드 (line 112~147, `showAndroidGuide`) + iOS 가이드 (line 148~215, else 분기) 시각화. **OQ #3 LOCKED (step 제목 13 text-label leading-none / 하위 설명 11→12 text-caption leading-none 격상 / 숫자 원 13 text-label leading-none + font-extrabold / 확인했습니다 버튼 13 text-label leading-none)** + **OQ #4 LOCKED (노란 경고 #f59e0b → text-warning + bg-warning-bg + border-warning/25 / 숫자 원 rgba(59,130,246,0.15) + #3b82f6 → bg-accent/15 + text-accent / text #e6edf3 → text-text-primary / text #8b949e → text-text-secondary)** + **OQ #5 LOCKED (Android ⋮ U+22EE + iOS ⎋ U+238B 인라인 span fontSize:16 + verticalAlign:'middle' 보존, Lucide 교체 X, emoji negative grep 시 ⋮ ⎋ 예외 anchor)** 3건이 본 wave 의 핵심.

    **구조 (17-annual-plan W4 mirror, 3 frame):**
    - `<!DOCTYPE html>` + `<head><title>redesign/28-splash — W4 InstallPrompt 가이드 view</title>` + `<style>` 블록
    - `<body>` 안 3 frame side-by-side:
      | frame | viewport | 시각화 |
      |---|---|---|
      | 1 | 모바일 다크 393×800 Android 가이드 | `data-theme="dark"` + 오버레이 + 카드 + 노란 경고 + 3-step (1. ⋮ 메뉴 / 2. 앱 설치 / 3. 설치) + 확인했습니다 |
      | 2 | 모바일 다크 393×800 iOS 가이드 | `data-theme="dark"` + 오버레이 + 카드 + (노란 경고 없음) + 3-step (1. ⎋ 공유 / 2. 홈 화면에 추가 / 3. 추가) + 확인했습니다 |
      | 3 | 모바일 라이트 393×800 Android 가이드 | `data-theme="light"` + 동일 (라이트 명도 차) |

    **Android 가이드 마크업 룰 (frame 1, 3 — OQ #4 LOCKED 노란 경고 + OQ #5 LOCKED ⋮ 글리프):**
    ```html
    <!-- 카드 안 내용 (오버레이 + 카드 wrapper 는 W3 와 동일) -->
    <div style="text-align:left;">
      <!-- 노란 경고 (OQ #4 LOCKED: text-warning + bg-warning-bg + border-warning/25 + OQ #3 LOCKED: 12 text-caption leading-relaxed) -->
      <div class="text-caption leading-relaxed text-warning bg-warning-bg border-warning/25 rounded-sm" style="border:1px solid;padding:8px 10px;margin-bottom:14px;">
        자동 설치 창이 뜨지 않으면 아래 순서로 설치해 주세요.
      </div>
      <!-- 3-step -->
      <div style="display:flex;flex-direction:column;gap:14px;">
        <!-- step 1 (OQ #4 LOCKED: 숫자 원 bg-accent/15 + text-accent + OQ #3 LOCKED: 13 text-label leading-none font-extrabold) -->
        <div style="display:flex;align-items:flex-start;gap:12px;">
          <div class="bg-accent/15 text-accent text-label font-extrabold leading-none rounded-sm flex items-center justify-center" style="width:28px;height:28px;flex-shrink:0;">1</div>
          <div>
            <!-- step 제목 (OQ #3 LOCKED: 13 text-label leading-none) -->
            <div class="text-label leading-none text-text-primary" style="font-weight:700;">
              크롬 우상단 <span style="font-size:16px;">⋮</span> 메뉴
            </div>
            <!-- 하위 설명 (OQ #3 LOCKED: 11→12 text-caption leading-relaxed 격상, OQ #4 #8b949e → text-text-secondary) -->
            <div class="text-caption leading-relaxed text-text-secondary" style="margin-top:2px;">주소창 오른쪽 점 세 개 메뉴를 누르세요</div>
          </div>
        </div>
        <!-- step 2 (앱 설치) -->
        <div style="display:flex;align-items:flex-start;gap:12px;">
          <div class="bg-accent/15 text-accent text-label font-extrabold leading-none rounded-sm flex items-center justify-center" style="width:28px;height:28px;flex-shrink:0;">2</div>
          <div>
            <div class="text-label leading-none text-text-primary" style="font-weight:700;">'앱 설치' 또는 '홈 화면에 추가' 선택</div>
            <div class="text-caption leading-relaxed text-text-secondary" style="margin-top:2px;">메뉴에서 <strong class="text-text-primary">앱 설치</strong>(또는 <strong class="text-text-primary">홈 화면에 추가</strong>)를 누르세요</div>
          </div>
        </div>
        <!-- step 3 (설치) -->
        <div style="display:flex;align-items:flex-start;gap:12px;">
          <div class="bg-accent/15 text-accent text-label font-extrabold leading-none rounded-sm flex items-center justify-center" style="width:28px;height:28px;flex-shrink:0;">3</div>
          <div>
            <div class="text-label leading-none text-text-primary" style="font-weight:700;">'설치' 확인</div>
            <div class="text-caption leading-relaxed text-text-secondary" style="margin-top:2px;">팝업에서 <strong class="text-text-primary">설치</strong>를 누르면 완료</div>
          </div>
        </div>
      </div>
      <!-- 확인했습니다 버튼 (OQ #4 LOCKED: bg-accent/15 + text-accent + OQ #3 LOCKED: 13 text-label leading-none) -->
      <button class="bg-accent/15 text-accent text-label font-bold leading-none rounded-md" style="width:100%;height:44px;margin-top:18px;border:1px solid rgba(59,130,246,0.3);cursor:pointer;">
        확인했습니다
      </button>
    </div>
    ```

    **iOS 가이드 마크업 룰 (frame 2 — 노란 경고 없음 + OQ #5 LOCKED ⎋ 글리프):**
    ```html
    <!-- 카드 안 내용 (노란 경고 없음) -->
    <div style="text-align:left;">
      <div style="display:flex;flex-direction:column;gap:14px;">
        <!-- step 1 (⎋ 공유 — OQ #5 LOCKED: U+238B 인라인 span fontSize:16 + verticalAlign:'middle' 보존) -->
        <div style="display:flex;align-items:flex-start;gap:12px;">
          <div class="bg-accent/15 text-accent text-label font-extrabold leading-none rounded-sm flex items-center justify-center" style="width:28px;height:28px;flex-shrink:0;">1</div>
          <div>
            <div class="text-label leading-none text-text-primary" style="font-weight:700;">하단 공유 버튼 터치</div>
            <div class="text-caption leading-relaxed text-text-secondary" style="margin-top:2px;">
              Safari 하단의 <span style="font-size:16px;vertical-align:middle;">⎋</span> 공유 아이콘을 누르세요
            </div>
          </div>
        </div>
        <!-- step 2 (홈 화면에 추가) -->
        <div style="display:flex;align-items:flex-start;gap:12px;">
          <div class="bg-accent/15 text-accent text-label font-extrabold leading-none rounded-sm flex items-center justify-center" style="width:28px;height:28px;flex-shrink:0;">2</div>
          <div>
            <div class="text-label leading-none text-text-primary" style="font-weight:700;">'홈 화면에 추가' 선택</div>
            <div class="text-caption leading-relaxed text-text-secondary" style="margin-top:2px;">스크롤해서 <strong class="text-text-primary">홈 화면에 추가</strong>를 찾아 누르세요</div>
          </div>
        </div>
        <!-- step 3 (추가) -->
        <div style="display:flex;align-items:flex-start;gap:12px;">
          <div class="bg-accent/15 text-accent text-label font-extrabold leading-none rounded-sm flex items-center justify-center" style="width:28px;height:28px;flex-shrink:0;">3</div>
          <div>
            <div class="text-label leading-none text-text-primary" style="font-weight:700;">'추가' 터치</div>
            <div class="text-caption leading-relaxed text-text-secondary" style="margin-top:2px;">오른쪽 상단 <strong class="text-text-primary">추가</strong> 버튼을 누르면 완료!</div>
          </div>
        </div>
      </div>
      <button class="bg-accent/15 text-accent text-label font-bold leading-none rounded-md" style="width:100%;height:44px;margin-top:18px;border:1px solid rgba(59,130,246,0.3);cursor:pointer;">
        확인했습니다
      </button>
    </div>
    ```

    **카피 verbatim (memory `feedback_sketch_realistic_data`):**
    - '자동 설치 창이 뜨지 않으면 아래 순서로 설치해 주세요.' (line 116 Android only)
    - '크롬 우상단 ⋮ 메뉴' / '주소창 오른쪽 점 세 개 메뉴를 누르세요' (line 122~123 Android step 1)
    - ''앱 설치' 또는 '홈 화면에 추가' 선택' / '메뉴에서 앱 설치(또는 홈 화면에 추가)를 누르세요' (line 129~130 Android step 2)
    - ''설치' 확인' / '팝업에서 설치를 누르면 완료' (line 136~137 Android step 3)
    - '하단 공유 버튼 터치' / 'Safari 하단의 ⎋ 공유 아이콘을 누르세요' (line 161~164 iOS step 1)
    - ''홈 화면에 추가' 선택' / '스크롤해서 홈 화면에 추가를 찾아 누르세요' (line 178~181 iOS step 2)
    - ''추가' 터치' / '오른쪽 상단 추가 버튼을 누르면 완료!' (line 195~198 iOS step 3)
    - '확인했습니다' (line 145 + 212 양쪽 verbatim)

    **rules 박스 (특수 글리프 처리 anchor 필수):**
    - LOCKED 결정: OQ #3 (step 제목 13 text-label leading-none / 하위 설명 11→12 text-caption leading-relaxed 격상 / 숫자 원 13 text-label leading-none font-extrabold / 확인했습니다 13 text-label leading-none) + OQ #4 (노란 경고 #f59e0b + rgba(245,158,11,0.1) + rgba(245,158,11,0.25) → text-warning + bg-warning-bg + border-warning/25 / 숫자 원 rgba(59,130,246,0.15) + #3b82f6 → bg-accent/15 + text-accent / text #e6edf3 → text-text-primary / text #8b949e → text-text-secondary / 확인했습니다 버튼 동일 토큰) + OQ #5 (Android ⋮ U+22EE 인라인 span fontSize:16 / iOS ⎋ U+238B 인라인 span fontSize:16 + verticalAlign:'middle' 보존 / Lucide 교체 X / emoji negative grep ⋮ ⎋ 예외 anchor) verbatim 인용 1 fence
    - **★ 특수 글리프 anchor 박스 (OQ #5 LOCKED):**
      ```
      ⋮ (U+22EE Vertical Ellipsis, Android Chrome 메뉴 시그니처, line 122) — inline <span style="font-size:16px;">⋮</span>
      ⎋ (U+238B Broken Circle With Northwest Arrow, iOS Safari 공유 아이콘 시그니처, line 164) — inline <span style="font-size:16px;vertical-align:middle;">⎋</span>
      Lucide react import 추가 X (글꼴 의미 anchor — Android Chrome / iOS Safari 시스템 글꼴 그대로 표시)
      emoji negative grep 시 [😀-🛿🤀-🧿🩰-🩴✅✊👍👎❌⛔🔥💥💧⚠️📛] 패턴에서 ⋮ ⎋ 2 글리프는 매칭되지 않음 (U+22EE + U+238B 은 emoji 블록 외부)
      ```
    - 토큰 매핑: 노란 경고 background `rgba(245,158,11,0.1) → bg-warning-bg` / border `rgba(245,158,11,0.25) → border-warning/25` / text `#f59e0b → text-warning` / 숫자 원 background `rgba(59,130,246,0.15) → bg-accent/15` / text `#3b82f6 → text-accent` / 확인했습니다 버튼 동일 / 인라인 유지: 카드 border / boxShadow / 확인했습니다 버튼 border `1px solid rgba(59,130,246,0.3)`
    - negative gate 자체매칭 회피: fence escape

    **commit 메시지:**
    `docs(quick-260522-2q6): redesign/28-splash W4 install-guides sketch (3 frame Android+iOS 가이드, 노란 경고 + 숫자 원 + 3-step + 특수 글리프 ⋮ ⎋ 보존 + 확인했습니다)`

    **commit 명령:**
    `git add cha-bio-safety/docs/redesign-context/28-splash/sketch-wave-4-install-guides.html && git commit -m "docs(quick-260522-2q6): redesign/28-splash W4 install-guides sketch (3 frame Android+iOS 가이드, 노란 경고 + 숫자 원 + 3-step + 특수 글리프 ⋮ ⎋ 보존 + 확인했습니다)"`
  </action>
  <verify>
    <automated>
test -f cha-bio-safety/docs/redesign-context/28-splash/sketch-wave-4-install-guides.html \
  && wc -l cha-bio-safety/docs/redesign-context/28-splash/sketch-wave-4-install-guides.html | awk '{ if ($1 < 250) { print "FAIL: too short"; exit 1 } else print "PASS lines:", $1 }' \
  && grep -c 'data-theme="dark"' cha-bio-safety/docs/redesign-context/28-splash/sketch-wave-4-install-guides.html | awk '{ if ($1 < 2) { print "FAIL: dark frame <2 (Android + iOS)"; exit 1 } else print "PASS dark frame:", $1 }' \
  && grep -c 'data-theme="light"' cha-bio-safety/docs/redesign-context/28-splash/sketch-wave-4-install-guides.html | awk '{ if ($1 < 1) { print "FAIL: light frame <1"; exit 1 } else print "PASS light frame:", $1 }' \
  && grep -c 'text-warning' cha-bio-safety/docs/redesign-context/28-splash/sketch-wave-4-install-guides.html | awk '{ if ($1 < 2) { print "FAIL: text-warning <2 (Android 노란 경고 frame 1/3, OQ #4 LOCKED)"; exit 1 } else print "PASS text-warning:", $1 }' \
  && grep -c 'bg-warning-bg' cha-bio-safety/docs/redesign-context/28-splash/sketch-wave-4-install-guides.html | awk '{ if ($1 < 2) { print "FAIL: bg-warning-bg <2 (OQ #4 LOCKED)"; exit 1 } else print "PASS bg-warning-bg:", $1 }' \
  && grep -c 'border-warning/25' cha-bio-safety/docs/redesign-context/28-splash/sketch-wave-4-install-guides.html | awk '{ if ($1 < 2) { print "FAIL: border-warning/25 <2 (OQ #4 LOCKED)"; exit 1 } else print "PASS border-warning/25:", $1 }' \
  && grep -c 'bg-accent/15' cha-bio-safety/docs/redesign-context/28-splash/sketch-wave-4-install-guides.html | awk '{ if ($1 < 11) { print "FAIL: bg-accent/15 <11 (3 frame x 3 step 숫자 원 + 3 확인했습니다 버튼, OQ #4 LOCKED)"; exit 1 } else print "PASS bg-accent/15:", $1 }' \
  && grep -c 'text-accent' cha-bio-safety/docs/redesign-context/28-splash/sketch-wave-4-install-guides.html | awk '{ if ($1 < 11) { print "FAIL: text-accent <11"; exit 1 } else print "PASS text-accent:", $1 }' \
  && grep -c 'text-label' cha-bio-safety/docs/redesign-context/28-splash/sketch-wave-4-install-guides.html | awk '{ if ($1 < 9) { print "FAIL: text-label <9 (3 frame x 3 step 제목 + 확인했습니다 3, OQ #3 LOCKED)"; exit 1 } else print "PASS text-label:", $1 }' \
  && grep -c '⋮' cha-bio-safety/docs/redesign-context/28-splash/sketch-wave-4-install-guides.html | awk '{ if ($1 < 2) { print "FAIL: ⋮ U+22EE <2 (Android frame 1/3, OQ #5 LOCKED)"; exit 1 } else print "PASS ⋮ glyph:", $1 }' \
  && grep -c '⎋' cha-bio-safety/docs/redesign-context/28-splash/sketch-wave-4-install-guides.html | awk '{ if ($1 < 1) { print "FAIL: ⎋ U+238B missing (iOS frame 2, OQ #5 LOCKED)"; exit 1 } else print "PASS ⎋ glyph:", $1 }' \
  && grep -c 'vertical-align:middle\|verticalAlign' cha-bio-safety/docs/redesign-context/28-splash/sketch-wave-4-install-guides.html | awk '{ if ($1 < 1) { print "FAIL: ⎋ verticalAlign middle missing (OQ #5 LOCKED)"; exit 1 } else print "PASS verticalAlign:", $1 }' \
  && grep -c '자동 설치 창이 뜨지 않으면' cha-bio-safety/docs/redesign-context/28-splash/sketch-wave-4-install-guides.html | awk '{ if ($1 < 2) { print "FAIL: 노란 경고 카피 <2 (Android frame 1/3)"; exit 1 } else print "PASS 노란 경고 카피:", $1 }' \
  && grep -c '크롬 우상단' cha-bio-safety/docs/redesign-context/28-splash/sketch-wave-4-install-guides.html | awk '{ if ($1 < 2) { print "FAIL: 크롬 우상단 카피 <2 (Android step 1)"; exit 1 } else print "PASS 크롬 우상단:", $1 }' \
  && grep -c '앱 설치' cha-bio-safety/docs/redesign-context/28-splash/sketch-wave-4-install-guides.html | awk '{ if ($1 < 2) { print "FAIL: 앱 설치 카피 <2 (Android step 2)"; exit 1 } else print "PASS 앱 설치:", $1 }' \
  && grep -c '하단 공유 버튼 터치' cha-bio-safety/docs/redesign-context/28-splash/sketch-wave-4-install-guides.html | awk '{ if ($1 < 1) { print "FAIL: 하단 공유 버튼 터치 카피 missing (iOS step 1)"; exit 1 } else print "PASS 하단 공유:", $1 }' \
  && grep -c '홈 화면에 추가' cha-bio-safety/docs/redesign-context/28-splash/sketch-wave-4-install-guides.html | awk '{ if ($1 < 3) { print "FAIL: 홈 화면에 추가 카피 <3 (Android step 2 + iOS step 2)"; exit 1 } else print "PASS 홈 화면에 추가:", $1 }' \
  && grep -c '확인했습니다' cha-bio-safety/docs/redesign-context/28-splash/sketch-wave-4-install-guides.html | awk '{ if ($1 < 3) { print "FAIL: 확인했습니다 카피 <3 (3 frame 모두)"; exit 1 } else print "PASS 확인했습니다:", $1 }' \
  && grep -c 'U+22EE\|U+238B' cha-bio-safety/docs/redesign-context/28-splash/sketch-wave-4-install-guides.html | awk '{ if ($1 < 2) { print "FAIL: ⋮ ⎋ codepoint anchor 박제 <2 (rules 박스 OQ #5)"; exit 1 } else print "PASS U+22EE/U+238B anchor:", $1 }' \
  && grep -cE 'linear-gradient' cha-bio-safety/docs/redesign-context/28-splash/sketch-wave-4-install-guides.html | awk '{ if ($1 > 0) { print "FAIL: linear-gradient found"; exit 1 } else print "PASS no gradient" }' \
  && grep -cE '(text|bg|border)-status-(safe|fire|warning|danger|caution)' cha-bio-safety/docs/redesign-context/28-splash/sketch-wave-4-install-guides.html | awk '{ if ($1 > 0) { print "FAIL: status- prefix found"; exit 1 } else print "PASS no status- prefix" }' \
  && grep -v '^<!--' cha-bio-safety/docs/redesign-context/28-splash/sketch-wave-4-install-guides.html | grep -v '^#' | grep -cE 'font-size:[[:space:]]*(9|10|11)[^0-9]|fontSize:[[:space:]]*(9|10|11)[^0-9]' | awk '{ if ($1 > 0) { print "FAIL: 9/10/11 px found"; exit 1 } else print "PASS no 9-11px" }' \
  && grep -cE '\bw-8\b|\bh-8\b' cha-bio-safety/docs/redesign-context/28-splash/sketch-wave-4-install-guides.html | awk '{ if ($1 > 0) { print "FAIL: w-8/h-8 found"; exit 1 } else print "PASS no w-8 h-8" }' \
  && grep -cE 'OQ #3|OQ #4|OQ #5' cha-bio-safety/docs/redesign-context/28-splash/sketch-wave-4-install-guides.html | awk '{ if ($1 < 3) { print "FAIL: OQ #3/#4/#5 citation <3"; exit 1 } else print "PASS OQ citations:", $1 }' \
  && git log --oneline -1 | grep -q '260522-2q6.*W4' && echo "PASS commit W4"
    </automated>
  </verify>
  <done>
    - sketch-wave-4-install-guides.html 파일 존재 + ≥250 lines (≥380 권장)
    - data-theme="dark" ≥2 (Android + iOS) / data-theme="light" ≥1 (Android 라이트)
    - text-warning / bg-warning-bg / border-warning/25 (Android 노란 경고) 각 ≥2 (OQ #4 LOCKED)
    - bg-accent/15 / text-accent (3 frame x 3 step 숫자 원 + 확인했습니다 3) 각 ≥11 (OQ #4 LOCKED)
    - text-label (step 제목 + 확인했습니다 + 숫자 원 글자) ≥9 (OQ #3 LOCKED 13 격상)
    - **★ 특수 글리프 보존 (OQ #5 LOCKED)**: ⋮ ≥2 (Android frame 1/3) / ⎋ ≥1 (iOS frame 2) / vertical-align:middle 또는 verticalAlign ≥1
    - 카피 verbatim: '자동 설치 창이 뜨지 않으면' ≥2 / '크롬 우상단' ≥2 / '앱 설치' ≥2 / '하단 공유 버튼 터치' ≥1 / '홈 화면에 추가' ≥3 / '확인했습니다' ≥3
    - rules 박스 안 U+22EE 또는 U+238B 코드포인트 anchor ≥2 (OQ #5 LOCKED 박제)
    - linear-gradient 0건 / status- prefix 0건 / 9·10·11px fontSize 0건 / w-8 h-8 0건
    - OQ #3 + OQ #4 + OQ #5 인용 ≥3건
    - atomic commit `docs(quick-260522-2q6): ... W4 install-guides sketch ...` 1개
  </done>
</task>

<task type="auto">
  <name>Task 4 (W5): TSX 변환 verify checklist markdown 생성 (12 섹션) + 4 src 파일 변경 0 최종 확인</name>
  <files>cha-bio-safety/docs/redesign-context/28-splash/wave-5-tsx-conversion-checklist.md</files>
  <action>
    17-annual-plan/wave-5-tsx-conversion-checklist.md 패턴 mirror 로 새 markdown 파일 작성. **sketch 가 아닌 TSX 변환 wave 진입을 위한 verify checklist**. W2~W4 sketch 의 모든 Tailwind class / CSS 토큰 / OQ LOCKED 결정 / negative gate / positive gate / build gate / 자체 verify 명령을 **12 섹션**으로 박제. **★ 비즈 anchor 16건 1 byte 변경 금지 룰** 별도 박스 (§2 보존 + §10 비즈 보존 체크박스 + §11 메모리 룰).

    **frontmatter:**
    ```yaml
    ---
    title: "redesign/28-splash — wave 5 (TSX conversion checklist)"
    status: pending  # TSX 변환 wave 시작 전 사용자 컨펌 후 in-progress
    created: 2026-05-22
    quick_id: 260522-2q6
    branch: redesign/28-splash
    source_tsx_1: cha-bio-safety/src/pages/SplashScreen.tsx
    source_tsx_1_lines: 83
    source_tsx_2: cha-bio-safety/src/components/InstallPrompt.tsx
    source_tsx_2_lines: 237
    source_util_1: cha-bio-safety/src/utils/pwaInstall.ts (수정 0)
    source_util_2: cha-bio-safety/src/utils/versionCheck.ts (수정 0)
    sketch_sources:
      - cha-bio-safety/docs/redesign-context/28-splash/sketch-wave-2-splash.html
      - cha-bio-safety/docs/redesign-context/28-splash/sketch-wave-3-install-main.html
      - cha-bio-safety/docs/redesign-context/28-splash/sketch-wave-4-install-guides.html
    mirror_of: cha-bio-safety/docs/redesign-context/17-annual-plan/wave-5-tsx-conversion-checklist.md (260522-0j3)
    ---
    ```

    **§1. 변환 범위 + 산출 파일**
    - **2 파일 단일 atomic 변환**: `cha-bio-safety/src/pages/SplashScreen.tsx` (83 lines → 약 90~110 lines 예상) + `cha-bio-safety/src/components/InstallPrompt.tsx` (237 lines → 약 250~290 lines 예상). Tailwind v0.1.1 className 으로 인라인 style 치환.
    - W2 (SplashScreen 전체) + W3 (InstallPrompt 메인 view) + W4 (InstallPrompt 가이드 view — Android + iOS) 3 sketch 결정 → className 1:1 매핑.
    - **components.css 신규 생성 X** (페이지 로컬 인라인 토큰 유지)
    - **pwaInstall.ts + versionCheck.ts 변경 0** (PWA 이벤트 구독 + 버전 체크 비즈 보존)
    - **비즈 anchor 16건 (SplashScreen 8 + InstallPrompt 8) 1 byte 변경 금지** (§2 박스)
    - 비즈 로직 0 diff (state/handler/effect/hook 모두 보존, §10 체크박스)

    **§2. 보존 (비즈 anchor 16건 박스 — 1 byte 변경 금지)**

    **★ SplashScreen 8건 (line 별):**
    ```
    1. __APP_VERSION__ Vite define (line 73, `v{__APP_VERSION__} · 경기도 성남시 분당구`)
    2. @keyframes slideUp { from{opacity:0;transform:translateY(16px)} to{opacity:1;transform:translateY(0)} } (line 79)
    3. 48ms setInterval tick (line 18: setInterval(() => setPct(p => Math.min(p + 4, 100)), 48))
    4. 1600ms setTimeout (line 26: setTimeout(... , 1600))
    5. +4 increment per tick (line 18: p + 4)
    6. checkVersionAndRefresh() void 호출 (line 16)
    7. isAuthenticated ? '/dashboard' : '/login' 분기 (line 24, 34)
    8. shouldShowInstallPrompt() 분기 (line 21)
    ```

    **★ InstallPrompt 8건 (line 별):**
    ```
    1. isStandalone() (line 5~10, iOS standalone + display-mode standalone/fullscreen 3분기)
    2. isIOS() (line 12~17, iPhone/iPad/iPod + Macintosh+maxTouchPoints fallback)
    3. getDeferredInstallPrompt / subscribeInstallPrompt / showInstallPrompt import (line 2)
    4. shouldShowInstallPrompt() export (line 224~233, isStandalone() + 24h TTL)
    5. dismissInstallPrompt() export (line 235~237, localStorage.setItem)
    6. localStorage key 'pwa-install-dismissed' + TTL 24 * 60 * 60 * 1000
    7. outcome 3 분기: 'accepted' → onDismiss / 'unavailable' → setShowAndroidGuide(true) / 'dismissed' → no-op (line 40~47)
    8. 3 view state: 메인 (!showIOSGuide && !showAndroidGuide) / Android (showAndroidGuide) / iOS (else) — line 88~215
    ```

    **§3. 변환 매핑 (영역별 className/토큰/폰트 — W2/W3/W4 sketch verbatim 인용)**

    **§3.1 SplashScreen — 외곽 + 로고 + 타이틀 + 진행 바 + 버전 (line 38~82, W2 sketch 출처)**

    | 현재 (인라인 style) | 변환 후 (className + 인라인) | sketch 출처 |
    |---|---|---|
    | 외곽 div `minHeight:'100dvh', background:'#161b22'` (line 38~39) | `class="bg-surface-page"` style={{ minHeight:'100dvh', ... }} (OQ #4 LOCKED) | W2 |
    | 로고 박스 wrapper `animation:'slideUp .4s ease-out', marginBottom:56` (line 45) | 인라인 유지 (animation anchor) | W2 |
    | 로고 박스 `width:88, height:88, borderRadius:22, background:'rgba(37,99,235,0.2)', border:'1px solid rgba(59,130,246,0.3)'` (line 47~52) | `class="rounded-[22px]"` style={{ width:88, height:88, background:'rgba(37,99,235,0.2)', border:'1px solid rgba(59,130,246,0.3)' }} (OQ #4 LOCKED 인라인 유지 4건 중 일부) | W2 |
    | 타이틀 h1 `fontSize:22, fontWeight:900, color:'#e6edf3', letterSpacing:'-.02em'` (line 58) | `class="text-heading font-black tracking-tight text-text-primary"` (OQ #3 LOCKED 22→text-heading) | W2 |
    | 부제 p `fontSize:12, color:'#6e7681', letterSpacing:'.1em'` (line 59) | `class="text-caption leading-relaxed text-text-tertiary"` style={{ letterSpacing:'.1em' }} (OQ #3 LOCKED 12 유지 + leading-relaxed) | W2 |
    | 진행 바 트랙 `height:2, background:'rgba(255,255,255,0.07)', borderRadius:2` (line 65) | 인라인 유지 (OQ #4 LOCKED 4 인라인 중 1) | W2 |
    | 진행 바 fill `background:'linear-gradient(90deg,#3b82f6,#0ea5e9)'` (line 66) | `class="bg-accent"` style={{ height:'100%', borderRadius:2, width:`${pct}%`, transition:'width .05s linear' }} (OQ #2 LOCKED solid, 그라데이션 폐기) | W2 |
    | 진행 라벨 p `fontSize:11, color:'#6e7681'` (line 68) | `class="text-caption leading-none text-text-tertiary"` (OQ #3 LOCKED 11→12 격상) | W2 |
    | 버전 p `fontSize:10, color:'#3d444d'` (line 73) | `class="text-caption leading-none text-text-disabled"` (OQ #3 LOCKED 10→12 격상 + OQ #4 #3d444d → text-text-disabled) | W2 |
    | `@keyframes slideUp` (line 79) | 인라인 유지 (animation anchor) | W2 |

    **§3.2 InstallPrompt 메인 view — 오버레이 + 카드 + 아이콘 + 제목 + 설치 버튼 + 나중에 (line 57~111, W3 sketch 출처)**

    | 현재 | 변환 후 | sketch |
    |---|---|---|
    | 오버레이 `position:'fixed', inset:0, zIndex:9999, background:'rgba(0,0,0,0.85)', padding:24` (line 58~63) | 인라인 유지 (OQ #4 LOCKED 4 인라인 중 1) | W3 |
    | 카드 `background:'#1c2128', borderRadius:20, padding:'28px 24px', maxWidth:340, border:'1px solid rgba(59,130,246,0.3)', boxShadow:'0 8px 40px rgba(0,0,0,0.5)'` (line 64~70) | `class="bg-surface-raised rounded-[20px]"` style={{ padding:'28px 24px', maxWidth:340, width:'100%', textAlign:'center', border:'1px solid rgba(59,130,246,0.3)', boxShadow:'0 8px 40px rgba(0,0,0,0.5)' }} (OQ #4 LOCKED 카드 토큰 + 인라인 border/boxShadow 유지) | W3 |
    | 아이콘 박스 `width:64, height:64, borderRadius:16, background:'rgba(37,99,235,0.2)', border:'1px solid rgba(59,130,246,0.3)'` (line 72~77) | `class="rounded-lg"` style={{ width:64, height:64, ... 인라인 rgba 유지 }} | W3 |
    | 제목 h2 `fontSize:18, fontWeight:800, color:'#e6edf3'` (line 81) | `class="text-title font-extrabold text-text-primary"` style={{ margin:'0 0 8px' }} (OQ #3 LOCKED 18→text-title) | W3 |
    | 부제 p `fontSize:12, color:'#8b949e', lineHeight:1.5` (line 84) | `class="text-caption leading-relaxed text-text-secondary"` style={{ margin:'0 0 20px' }} (OQ #3 LOCKED 12 유지 + leading-relaxed + OQ #4 #8b949e → text-text-secondary) | W3 |
    | 설치 버튼 `width:'100%', height:48, borderRadius:12, background:'linear-gradient(135deg, #2563eb, #0ea5e9)', border:'none', color:'#fff', fontSize:15, fontWeight:700` (line 92~98) | `class="bg-safe-bar text-text-on-accent text-body font-bold rounded-md"` style={{ width:'100%', height:48, border:'none', cursor:'pointer' }} (OQ #1 LOCKED solid + OQ #3 LOCKED 15→16 격상) | W3 |
    | 나중에 할게요 `width:'100%', height:40, borderRadius:10, background:'transparent', border:'1px solid rgba(255,255,255,0.1)', color:'#6e7681', fontSize:12, fontWeight:600` (line 101~108) | `class="text-caption font-bold leading-none text-text-tertiary rounded-md"` style={{ width:'100%', height:40, background:'transparent', border:'1px solid rgba(255,255,255,0.1)', cursor:'pointer' }} (OQ #3 LOCKED 12 유지 + OQ #4 #6e7681 → text-text-tertiary) | W3 |

    **§3.3 InstallPrompt Android 가이드 — 노란 경고 + 3-step + 확인했습니다 (line 112~147, W4 sketch 출처)**

    | 현재 | 변환 후 | sketch |
    |---|---|---|
    | 노란 경고 `fontSize:12, color:'#f59e0b', background:'rgba(245,158,11,0.1)', border:'1px solid rgba(245,158,11,0.25)', borderRadius:8, padding:'8px 10px', marginBottom:14` (line 115) | `class="text-caption leading-relaxed text-warning bg-warning-bg border-warning/25 rounded-sm"` style={{ border:'1px solid', padding:'8px 10px', marginBottom:14 }} (OQ #4 LOCKED warning 토큰 3종) | W4 frame 1/3 |
    | 숫자 원 `width:28, height:28, borderRadius:8, background:'rgba(59,130,246,0.15)', color:'#3b82f6', fontSize:13, fontWeight:800` (line 120/127/134) | `class="bg-accent/15 text-accent text-label font-extrabold leading-none rounded-sm flex items-center justify-center"` style={{ width:28, height:28, flexShrink:0 }} (OQ #4 LOCKED + OQ #3 LOCKED 13 text-label) | W4 |
    | step 제목 `fontSize:13, fontWeight:700, color:'#e6edf3'` (line 122/130/136) | `class="text-label leading-none text-text-primary"` style={{ fontWeight:700 }} (OQ #3 LOCKED 13 text-label) | W4 |
    | step 하위 설명 `fontSize:11, color:'#8b949e', marginTop:2` (line 123/131/137) | `class="text-caption leading-relaxed text-text-secondary"` style={{ marginTop:2 }} (OQ #3 LOCKED 11→12 격상 + OQ #4 #8b949e → text-text-secondary) | W4 |
    | `<strong style={{ color: '#e6edf3' }}>` 강조 (line 130/137) | `<strong class="text-text-primary">` (OQ #4 LOCKED) | W4 |
    | **★ Android ⋮ 글리프 `<span style={{ fontSize: 16 }}>⋮</span>` (line 122)** | **인라인 100% 유지 — OQ #5 LOCKED, Lucide 교체 X** | W4 frame 1/3 |
    | 확인했습니다 버튼 `width:'100%', height:44, borderRadius:10, marginTop:18, background:'rgba(59,130,246,0.15)', border:'1px solid rgba(59,130,246,0.3)', color:'#3b82f6', fontSize:13, fontWeight:700` (line 142~143) | `class="bg-accent/15 text-accent text-label font-bold leading-none rounded-md"` style={{ width:'100%', height:44, marginTop:18, border:'1px solid rgba(59,130,246,0.3)', cursor:'pointer' }} (OQ #4 LOCKED 인라인 border 유지 + OQ #3 LOCKED 13 text-label) | W4 |

    **§3.4 InstallPrompt iOS 가이드 — 3-step + ⎋ 글리프 + 확인했습니다 (line 148~215, W4 sketch 출처)**

    | 현재 | 변환 후 | sketch |
    |---|---|---|
    | (노란 경고 없음 — Android only) | (해당 없음) | W4 frame 2 |
    | 숫자 원 동일 (line 153~158/170~175/186~191) | 동일 (Android 와 동일 className) | W4 frame 2 |
    | step 제목 동일 (line 160/177/194) | 동일 | W4 frame 2 |
    | step 하위 설명 동일 (line 163/180/197) | 동일 | W4 frame 2 |
    | **★ iOS ⎋ 글리프 `<span style={{ fontSize: 16, verticalAlign: 'middle' }}>⎋</span>` (line 164)** | **인라인 100% 유지 — OQ #5 LOCKED, verticalAlign:'middle' 보존, Lucide 교체 X** | W4 frame 2 |
    | 확인했습니다 버튼 (line 204~210) | 동일 (Android 와 동일 className) | W4 frame 2 |

    **§4. OQ LOCKED 5건 변환 결과 반영 (W1 §7 verbatim + 코드 적용 line 범위)**

    ```
    OQ #1 (InstallPrompt 설치 버튼 bg-safe-bar solid + 그라데이션 완전 폐기):
      - line 92~98 (설치 버튼): background 'linear-gradient(135deg, #2563eb, #0ea5e9)' → className "bg-safe-bar text-text-on-accent"
      - linear-gradient 0건 강제 (grep gate)
      - 영향: InstallPrompt.tsx line 92~98

    OQ #2 (SplashScreen 진행 바 bg-accent solid + 그라데이션 완전 폐기):
      - line 66 (진행 바 fill): background 'linear-gradient(90deg,#3b82f6,#0ea5e9)' → className "bg-accent"
      - line 65 (진행 바 트랙): background 'rgba(255,255,255,0.07)' 인라인 유지
      - linear-gradient 0건 강제 (grep gate)
      - 영향: SplashScreen.tsx line 65~66

    OQ #3 (폰트 격상 verbatim):
      - SplashScreen line 58 타이틀 22 → "text-heading font-black tracking-tight"
      - SplashScreen line 59 부제 12 → "text-caption leading-relaxed" (유지)
      - SplashScreen line 68 진행 라벨 11 → "text-caption leading-none" (11→12 격상)
      - SplashScreen line 73 버전 10 → "text-caption leading-none" (10→12 격상)
      - InstallPrompt line 81 카드 제목 18 → "text-title font-extrabold"
      - InstallPrompt line 84 카드 부제 12 → "text-caption leading-relaxed" (유지)
      - InstallPrompt line 96 설치 버튼 15 → "text-body font-bold" (15→16 격상)
      - InstallPrompt line 106 나중에 할게요 12 → "text-caption font-bold leading-none" (유지)
      - InstallPrompt line 115 노란 경고 12 → "text-caption leading-relaxed"
      - InstallPrompt line 120/127/134/154/171/187 숫자 원 13 → "text-label font-extrabold leading-none"
      - InstallPrompt line 122/130/136/160/177/194 step 제목 13 → "text-label leading-none"
      - InstallPrompt line 123/131/137/163/180/197 하위 설명 11 → "text-caption leading-relaxed" (11→12 격상)
      - InstallPrompt line 143/209 확인했습니다 버튼 13 → "text-label font-bold leading-none"
      - InstallPrompt line 122/164 글리프 인라인 fontSize:16 유지 (OQ #5)

    OQ #4 (토큰 치환 + 인라인 유지 4건):
      - 페이지 background #161b22 → "bg-surface-page" (SplashScreen line 39)
      - 카드 background #1c2128 → "bg-surface-raised" (InstallPrompt line 65)
      - text #e6edf3 → "text-text-primary" (다수)
      - text #8b949e → "text-text-secondary" (다수)
      - text #6e7681 → "text-text-tertiary" (다수)
      - text #3d444d → "text-text-disabled" (SplashScreen line 73)
      - 노란 경고 3종 → "text-warning bg-warning-bg border-warning/25" (InstallPrompt line 115)
      - 숫자 원 + 확인했습니다 → "bg-accent/15 text-accent" (InstallPrompt 다수)
      - **인라인 유지 4건**:
        * SplashScreen line 65 진행 바 트랙 rgba(255,255,255,0.07)
        * InstallPrompt line 64~68 카드 border rgba(59,130,246,0.3)
        * InstallPrompt line 59 오버레이 rgba(0,0,0,0.85)
        * InstallPrompt line 69 boxShadow '0 8px 40px rgba(0,0,0,0.5)'

    OQ #5 (iOS ⋮ ⎋ 글리프 인라인 보존):
      - InstallPrompt line 122 `<span style={{ fontSize: 16 }}>⋮</span>` 100% 유지 (Android Chrome 메뉴 시그니처)
      - InstallPrompt line 164 `<span style={{ fontSize: 16, verticalAlign: 'middle' }}>⎋</span>` 100% 유지 (iOS Safari 공유 시그니처)
      - Lucide 교체 금지 (글꼴 의미 anchor)
      - emoji negative grep 시 [😀-🛿🤀-🧿🩰-🩴✅✊👍👎❌⛔🔥💥💧⚠️📛] 패턴은 ⋮ ⎋ 매칭 X (emoji 블록 외부)
    ```

    **§5. negative gate (TSX 변환 후 2 파일 모두 통과해야 할 grep gate)**

    ```bash
    # 1. 이모지 0건 — 단, ⋮ (U+22EE) + ⎋ (U+238B) 예외 anchor
    grep -v '^#' cha-bio-safety/src/pages/SplashScreen.tsx | grep -cE '[😀-🛿🤀-🧿🩰-🩴✅✊👍👎❌⛔🔥💥💧⚠️📛]' == 0
    grep -v '^#' cha-bio-safety/src/components/InstallPrompt.tsx | grep -cE '[😀-🛿🤀-🧿🩰-🩴✅✊👍👎❌⛔🔥💥💧⚠️📛]' == 0

    # 2. linear-gradient 0건 (OQ #1 + OQ #2 LOCKED — 설치 버튼 + 진행 바 그라데이션 폐기)
    grep -c 'linear-gradient' cha-bio-safety/src/pages/SplashScreen.tsx == 0
    grep -c 'linear-gradient' cha-bio-safety/src/components/InstallPrompt.tsx == 0

    # 3. 9·10·11px 0건 (OQ #3 LOCKED 격상 결과 — 단 fontSize:16 글리프 OQ #5 예외)
    grep -v '^\s*//' cha-bio-safety/src/pages/SplashScreen.tsx | grep -cE "fontSize:\s*(9|10|11)[^0-9px]|font-size:\s*(9|10|11)[^0-9px]" == 0
    grep -v '^\s*//' cha-bio-safety/src/components/InstallPrompt.tsx | grep -cE "fontSize:\s*(9|10|11)[^0-9px]|font-size:\s*(9|10|11)[^0-9px]" == 0

    # 4. status- prefix 0건
    grep -cE '(text|bg|border)-status-(safe|fire|warning|danger|caution)' cha-bio-safety/src/pages/SplashScreen.tsx == 0
    grep -cE '(text|bg|border)-status-(safe|fire|warning|danger|caution)' cha-bio-safety/src/components/InstallPrompt.tsx == 0

    # 5. w-8 / h-8 0건 (w-8=48 함정)
    grep -cE '\bw-8\b|\bh-8\b' cha-bio-safety/src/pages/SplashScreen.tsx == 0
    grep -cE '\bw-8\b|\bh-8\b' cha-bio-safety/src/components/InstallPrompt.tsx == 0

    # 6. 옛 alias 토큰 0건
    grep -cE 'var\(--(bg|bg2|bg3|bd|bd2|t1|t2|t3|acl|accent)\)' cha-bio-safety/src/pages/SplashScreen.tsx == 0
    grep -cE 'var\(--(bg|bg2|bg3|bd|bd2|t1|t2|t3|acl|accent)\)' cha-bio-safety/src/components/InstallPrompt.tsx == 0

    # 7. 비즈 anchor 16건 보존 (★ 1 byte 변경 금지)
    grep -c '__APP_VERSION__' cha-bio-safety/src/pages/SplashScreen.tsx >= 1
    grep -c '@keyframes slideUp' cha-bio-safety/src/pages/SplashScreen.tsx >= 1
    grep -c ', 48' cha-bio-safety/src/pages/SplashScreen.tsx >= 1   # setInterval 48ms
    grep -c '1600' cha-bio-safety/src/pages/SplashScreen.tsx >= 1   # setTimeout
    grep -c 'p + 4' cha-bio-safety/src/pages/SplashScreen.tsx >= 1  # +4 increment
    grep -c 'checkVersionAndRefresh' cha-bio-safety/src/pages/SplashScreen.tsx >= 1
    grep -c 'shouldShowInstallPrompt' cha-bio-safety/src/pages/SplashScreen.tsx >= 1
    grep -c "isAuthenticated ? '/dashboard' : '/login'" cha-bio-safety/src/pages/SplashScreen.tsx >= 2

    grep -c 'isStandalone' cha-bio-safety/src/components/InstallPrompt.tsx >= 2  # 정의 + 사용
    grep -c 'isIOS' cha-bio-safety/src/components/InstallPrompt.tsx >= 2
    grep -c 'subscribeInstallPrompt\|getDeferredInstallPrompt\|showInstallPrompt' cha-bio-safety/src/components/InstallPrompt.tsx >= 3
    grep -c 'shouldShowInstallPrompt' cha-bio-safety/src/components/InstallPrompt.tsx >= 1
    grep -c 'dismissInstallPrompt' cha-bio-safety/src/components/InstallPrompt.tsx >= 1
    grep -c "'pwa-install-dismissed'" cha-bio-safety/src/components/InstallPrompt.tsx >= 2
    grep -c '24 \* 60 \* 60 \* 1000' cha-bio-safety/src/components/InstallPrompt.tsx >= 1  # 24h TTL
    grep -c "'accepted'\|'unavailable'\|'dismissed'" cha-bio-safety/src/components/InstallPrompt.tsx >= 2

    # 8. 특수 글리프 보존 (★ OQ #5 LOCKED, 1 byte 변경 금지)
    grep -c '⋮' cha-bio-safety/src/components/InstallPrompt.tsx >= 1  # Android Chrome 메뉴
    grep -c '⎋' cha-bio-safety/src/components/InstallPrompt.tsx >= 1  # iOS Safari 공유
    grep -c "verticalAlign: 'middle'" cha-bio-safety/src/components/InstallPrompt.tsx >= 1  # iOS ⎋ 글리프
    ```

    **§6. positive gate (변환 후 등장해야 할 패턴)**

    ```bash
    # 1. v0.1.1 토큰 class 카운트
    grep -c 'bg-surface-page' cha-bio-safety/src/pages/SplashScreen.tsx >= 1
    grep -c 'bg-surface-raised' cha-bio-safety/src/components/InstallPrompt.tsx >= 1
    grep -c 'bg-safe-bar' cha-bio-safety/src/components/InstallPrompt.tsx >= 1  # 설치 버튼 OQ #1
    grep -c 'text-text-on-accent' cha-bio-safety/src/components/InstallPrompt.tsx >= 1
    grep -c 'bg-accent' cha-bio-safety/src/pages/SplashScreen.tsx >= 1  # 진행 바 fill OQ #2
    grep -c 'bg-accent/15' cha-bio-safety/src/components/InstallPrompt.tsx >= 4  # 숫자 원 6 + 확인했습니다 2
    grep -c 'text-accent' cha-bio-safety/src/components/InstallPrompt.tsx >= 4
    grep -c 'text-warning' cha-bio-safety/src/components/InstallPrompt.tsx >= 1  # Android 노란 경고
    grep -c 'bg-warning-bg' cha-bio-safety/src/components/InstallPrompt.tsx >= 1
    grep -c 'border-warning/25' cha-bio-safety/src/components/InstallPrompt.tsx >= 1
    grep -c 'text-text-primary' cha-bio-safety/src/components/InstallPrompt.tsx >= 3
    grep -c 'text-text-secondary' cha-bio-safety/src/components/InstallPrompt.tsx >= 3
    grep -c 'text-text-tertiary' cha-bio-safety/src/components/InstallPrompt.tsx >= 1
    grep -c 'text-text-tertiary' cha-bio-safety/src/pages/SplashScreen.tsx >= 1  # 진행 라벨
    grep -c 'text-text-disabled' cha-bio-safety/src/pages/SplashScreen.tsx >= 1  # 버전

    # 2. 폰트 토큰 (OQ #3 LOCKED 격상)
    grep -c 'text-heading' cha-bio-safety/src/pages/SplashScreen.tsx >= 1   # 타이틀 22
    grep -c 'text-title' cha-bio-safety/src/components/InstallPrompt.tsx >= 1   # 카드 제목 18
    grep -c 'text-body' cha-bio-safety/src/components/InstallPrompt.tsx >= 1   # 설치 버튼 15→16
    grep -c 'text-label' cha-bio-safety/src/components/InstallPrompt.tsx >= 6  # step 제목 + 숫자 원 + 확인했습니다
    grep -c 'text-caption' cha-bio-safety/src/pages/SplashScreen.tsx >= 3  # 부제 + 진행 라벨 + 버전
    grep -c 'text-caption' cha-bio-safety/src/components/InstallPrompt.tsx >= 6  # 부제 + 나중에 할게요 + 노란 경고 + step 하위 설명 6
    grep -c 'leading-none' cha-bio-safety/src/pages/SplashScreen.tsx >= 2
    grep -c 'leading-relaxed' cha-bio-safety/src/components/InstallPrompt.tsx >= 1
    grep -c 'font-black' cha-bio-safety/src/pages/SplashScreen.tsx >= 1  # 타이틀
    grep -c 'tracking-tight' cha-bio-safety/src/pages/SplashScreen.tsx >= 1  # 타이틀
    grep -c 'font-extrabold' cha-bio-safety/src/components/InstallPrompt.tsx >= 1  # 카드 제목

    # 3. 인라인 유지 4건 (OQ #4 LOCKED)
    grep -c 'rgba(255,255,255,0.07)' cha-bio-safety/src/pages/SplashScreen.tsx >= 1  # 진행 바 트랙
    grep -c 'rgba(59,130,246,0.3)' cha-bio-safety/src/components/InstallPrompt.tsx >= 2  # 카드 border + 아이콘 박스 + 확인했습니다 버튼
    grep -c 'rgba(0,0,0,0.85)' cha-bio-safety/src/components/InstallPrompt.tsx >= 1  # 오버레이
    grep -c 'boxShadow.*rgba(0,0,0,0.5)\|0 8px 40px rgba(0,0,0,0.5)' cha-bio-safety/src/components/InstallPrompt.tsx >= 1

    # 4. radius 토큰
    grep -c 'rounded-\[22px\]' cha-bio-safety/src/pages/SplashScreen.tsx >= 1  # 로고 박스
    grep -c 'rounded-\[20px\]' cha-bio-safety/src/components/InstallPrompt.tsx >= 1  # 카드
    grep -c 'rounded-lg' cha-bio-safety/src/components/InstallPrompt.tsx >= 1  # 아이콘 박스 16
    grep -c 'rounded-md' cha-bio-safety/src/components/InstallPrompt.tsx >= 3  # 설치 버튼 + 나중에 할게요 + 확인했습니다 2
    grep -c 'rounded-sm' cha-bio-safety/src/components/InstallPrompt.tsx >= 7  # 노란 경고 + 숫자 원 6

    # 5. 카피 verbatim
    grep -c 'CBC 방재' cha-bio-safety/src/pages/SplashScreen.tsx >= 1
    grep -c 'CBC 방재' cha-bio-safety/src/components/InstallPrompt.tsx >= 1
    grep -c '소방안전 통합관리 시스템' cha-bio-safety/src/pages/SplashScreen.tsx >= 1
    grep -c '시스템 초기화 중\|데이터 불러오는 중\|준비 완료' cha-bio-safety/src/pages/SplashScreen.tsx >= 3
    grep -c '경기도 성남시 분당구' cha-bio-safety/src/pages/SplashScreen.tsx >= 1
    grep -c '홈 화면에 앱을 설치하면' cha-bio-safety/src/components/InstallPrompt.tsx >= 1
    grep -c '홈 화면에 설치\|설치 방법 보기' cha-bio-safety/src/components/InstallPrompt.tsx >= 2
    grep -c '나중에 할게요' cha-bio-safety/src/components/InstallPrompt.tsx >= 1
    grep -c '자동 설치 창이 뜨지 않으면' cha-bio-safety/src/components/InstallPrompt.tsx >= 1
    grep -c '크롬 우상단' cha-bio-safety/src/components/InstallPrompt.tsx >= 1
    grep -c '하단 공유 버튼 터치' cha-bio-safety/src/components/InstallPrompt.tsx >= 1
    grep -c '홈 화면에 추가' cha-bio-safety/src/components/InstallPrompt.tsx >= 3
    grep -c '확인했습니다' cha-bio-safety/src/components/InstallPrompt.tsx >= 2
    ```

    **§7. build / tsc gate**

    ```bash
    cd cha-bio-safety && npx tsc --noEmit                # 0 error
    cd cha-bio-safety && npm run build                   # exit 0
    # Splash/Install chunk size 확인
    ls -la cha-bio-safety/dist/assets/SplashScreen-*.js
    ls -la cha-bio-safety/dist/assets/InstallPrompt-*.js  # 또는 main 번들 안 포함
    ```

    **§8. 자체 verify 명령 (TSX 변환 wave 진입 시점 + 완료 시점 양쪽 실행)**

    각 gate 의 grep/wc/git 명령을 fence 안에 verbatim 박제.

    ```bash
    # 진입 시점: sketch 3 파일 모두 존재 확인
    ls cha-bio-safety/docs/redesign-context/28-splash/sketch-wave-{2-splash,3-install-main,4-install-guides}.html

    # 완료 시점: §5 negative + §6 positive + §7 build 모두 PASS

    # pwaInstall.ts + versionCheck.ts 변경 0 확인 (TSX 변환 wave 끝나도 유지)
    git diff HEAD -- cha-bio-safety/src/utils/pwaInstall.ts | wc -l == 0
    git diff HEAD -- cha-bio-safety/src/utils/versionCheck.ts | wc -l == 0
    ```

    **§9. Tailwind cheatsheet (v0.1.1 토큰 → utility class 매핑 표 — context 의 cheatsheet 동일 mirror)**

    | v0.1.1 토큰 | Tailwind utility | 28-splash 적용 위치 |
    |---|---|---|
    | `--surface-page` | `bg-surface-page` | SplashScreen 외곽 |
    | `--surface-raised` | `bg-surface-raised` | InstallPrompt 카드 |
    | `--text-primary` | `text-text-primary` | 모든 제목 / step 본문 |
    | `--text-secondary` | `text-text-secondary` | 부제 / step 하위 설명 |
    | `--text-tertiary` | `text-text-tertiary` | "나중에 할게요" 버튼 / 진행 라벨 |
    | `--text-disabled` | `text-text-disabled` | SplashScreen 버전 문자열 |
    | `--safe-bar` | `bg-safe-bar` + `text-text-on-accent` | InstallPrompt 설치 버튼 (OQ #1) |
    | `--accent` | `bg-accent` / `bg-accent/15` / `text-accent` | 진행 바 fill (OQ #2) / 숫자 원 / 확인했습니다 버튼 |
    | `--warning` | `text-warning` / `bg-warning-bg` / `border-warning/25` | Android 노란 경고 (OQ #4) |
    | (radius 8) | `rounded-sm` | 모든 step 카드 / 숫자 원 / 노란 경고 |
    | (radius 10) | `rounded-md` | 나중에 할게요 / 확인했습니다 |
    | (radius 12) | `rounded-md` | 설치 버튼 |
    | (radius 16) | `rounded-lg` | InstallPrompt 아이콘 박스 |
    | (radius 20) | `rounded-[20px]` | InstallPrompt 카드 (토큰 없음) |
    | (radius 22) | `rounded-[22px]` | SplashScreen 로고 박스 (토큰 없음) |
    | (인라인 유지 OQ #4) | `style={{ background:'rgba(255,255,255,0.07)' }}` | 진행 바 트랙 |
    | (인라인 유지 OQ #4) | `style={{ border:'1px solid rgba(59,130,246,0.3)' }}` | 카드 border / 아이콘 박스 / 확인했습니다 버튼 |
    | (인라인 유지 OQ #4) | `style={{ background:'rgba(0,0,0,0.85)' }}` | 오버레이 |
    | (인라인 유지 OQ #4) | `style={{ boxShadow:'0 8px 40px rgba(0,0,0,0.5)' }}` | 카드 shadow |
    | (인라인 유지 OQ #5) | `<span style={{ fontSize:16 }}>⋮</span>` | Android Chrome 메뉴 글리프 |
    | (인라인 유지 OQ #5) | `<span style={{ fontSize:16, verticalAlign:'middle' }}>⎋</span>` | iOS Safari 공유 글리프 |

    **§10. 비즈 보존 체크박스 (TSX 변환 wave 완료 후 직접 체크)**

    **SplashScreen 8건:**
    - [ ] `__APP_VERSION__` Vite define line 73 그대로 (1 byte 변경 금지)
    - [ ] `@keyframes slideUp { from{opacity:0;transform:translateY(16px)} to{opacity:1;transform:translateY(0)} }` line 79 그대로
    - [ ] `setInterval(() => setPct(p => Math.min(p + 4, 100)), 48)` line 18 그대로 (48ms tick + +4 increment)
    - [ ] `setTimeout(... , 1600)` line 26 그대로 (1600ms)
    - [ ] `checkVersionAndRefresh()` void 호출 line 16 그대로
    - [ ] `shouldShowInstallPrompt()` 분기 line 21 그대로
    - [ ] `isAuthenticated ? '/dashboard' : '/login'` 분기 line 24, 34 양쪽 그대로
    - [ ] `useNavigate` / `useAuthStore` / `useState` (pct, showInstall) hook 보존

    **InstallPrompt 8건:**
    - [ ] `isStandalone()` 함수 line 5~10 그대로 (iOS standalone + display-mode standalone/fullscreen 3 분기)
    - [ ] `isIOS()` 함수 line 12~17 그대로 (iPhone/iPad/iPod + Macintosh+maxTouchPoints fallback)
    - [ ] `getDeferredInstallPrompt` / `subscribeInstallPrompt` / `showInstallPrompt` import line 2 그대로
    - [ ] `shouldShowInstallPrompt()` export line 224~233 그대로 (isStandalone + 24h TTL 분기)
    - [ ] `dismissInstallPrompt()` export line 235~237 그대로 (localStorage.setItem)
    - [ ] localStorage key `'pwa-install-dismissed'` + TTL `24 * 60 * 60 * 1000` 그대로
    - [ ] outcome 3 분기 `'accepted'` / `'unavailable'` / `'dismissed'` line 40~47 그대로
    - [ ] 3 view state 구조 line 88~215 그대로 (메인 / Android / iOS)

    **★ 특수 글리프 OQ #5 (2건):**
    - [ ] InstallPrompt line 122 `<span style={{ fontSize: 16 }}>⋮</span>` (Android Chrome 메뉴) 1 byte 변경 금지
    - [ ] InstallPrompt line 164 `<span style={{ fontSize: 16, verticalAlign: 'middle' }}>⎋</span>` (iOS Safari 공유) 1 byte 변경 금지

    **★ pwaInstall.ts + versionCheck.ts:**
    - [ ] `pwaInstall.ts` export 0 byte 변경 (beforeinstallprompt 이벤트 구독)
    - [ ] `versionCheck.ts` export 0 byte 변경

    **§11. 메모리 룰 inline 인용 (W1 mirror, 12+ 룰 — 28-splash 특화 룰 2건 포함)**

    각 룰 작용 케이스 한 줄 — `feedback_*.md` 파일명 + How (28-splash 컨텍스트).

    1. `feedback_design_sketch_first` — spacing/sizing 변경 시 sketch 먼저 컨펌. T1~T3 에서 진행 바 width / 카드 크기 / step gap 조정 시 sketch 먼저.
    2. `feedback_redesign_sketch_rule_enforcement` — 설치 버튼 = CTA → `bg-safe-bar` solid (의미 anchor, OQ #1). `bg-status-safe-bg` 위험 색 사용 금지. 진행 바 fill = neutral → `bg-accent` (OQ #2). 노란 경고 = warning → `bg-warning-bg` + `text-warning` (OQ #4).
    3. `feedback_sketch_realistic_data` — 카피 verbatim (CBC 방재 / 소방안전 통합관리 시스템 / 시스템 초기화 중 / 데이터 불러오는 중 / 준비 완료 / 경기도 성남시 분당구 / 홈 화면에 앱을 설치하면 더 빠르고 편리하게 / 홈 화면에 설치 / 설치 방법 보기 / 나중에 할게요 / 자동 설치 창이 뜨지 않으면 / 크롬 우상단 ⋮ 메뉴 / 앱 설치 / 홈 화면에 추가 / 설치 확인 / 하단 공유 버튼 터치 / Safari 하단의 ⎋ 공유 아이콘 / 추가 / 확인했습니다). 시안에서 변경 금지.
    4. `feedback_planner_prompt_sketch_verbatim` — TSX 변환 진입 시 W2~W4 sketch 의 Tailwind class / CSS 토큰 grep 으로 추출 → 본 §3 verbatim 인용. 특히 ⋮ ⎋ 글리프 fontSize:16 / 카드 boxShadow 0 8px 40px / 진행 바 트랙 rgba(255,255,255,0.07) / 카드 border rgba(59,130,246,0.3) 모두 추측 X — sketch verbatim.
    5. `feedback_tailwind_token_class_pattern` — 설치 버튼 그라데이션 → `bg-safe-bar` solid (OQ #1). `bg-status-safe-bar` (status- prefix) 사용 시 W5 verify FAIL. 진행 바 fill → `bg-accent` (OQ #2). 노란 경고 → `text-warning bg-warning-bg border-warning/25` (`status-warning` X). 숫자 원 → `bg-accent/15 text-accent`.
    6. `feedback_tailwind_w8_h8_is_48px` — InstallPrompt 숫자 원 28x28 (line 120 etc.) → `w-8 h-8` 사용 시 48x48 (1.7배 확대 사고). `style={{ width:28, height:28 }}` 인라인 필수. SplashScreen 로고 88x88 도 동일 — 인라인 유지.
    7. `feedback_text_caption_leading_none` — SplashScreen 진행 라벨 11px h≈18, 버전 10px h≈16 (작은 컨테이너) → `text-caption leading-none` (12 + leading-none). InstallPrompt 부제 12px lineHeight:1.5 (가독성 우선) → `text-caption leading-relaxed` (12 유지 + leading-relaxed). 노란 경고 12px padding 8px (작은 칩) → `text-caption leading-relaxed` (텍스트 wrap 가능성). step 제목 13px h≈18 (작은 컨테이너) → `text-label leading-none`. step 하위 설명 11→12 → `text-caption leading-relaxed`.
    8. `feedback_tsx_wave_emoji_dot_gap` — sketch + TSX 본문 이모지 0건 강제. **단, ⋮ (U+22EE) + ⎋ (U+238B) 2 글리프는 OQ #5 LOCKED 예외 anchor — emoji 블록 외부 codepoint 이므로 [😀-🛿🤀-🧿🩰-🩴✅✊👍👎❌⛔🔥💥💧⚠️📛] grep 패턴 매칭 안 됨**. 카드 부제의 `<br/>` (line 85) 는 콘텐츠 줄바꿈, 이모지 아님.
    9. `feedback_tsx_wave_stat_card_drift` — SplashScreen + InstallPrompt 에 Stat Card 28px display 숫자 없음 → §3.5 "미적용" 메타 명시. 단, sketch 새 패턴 (진행률 매트릭스 + 가이드 3-step + iOS 글리프) 은 verbatim 인용해 W5 checklist 박제. source 의 `linear-gradient(135deg, #2563eb, #0ea5e9)` (line 94) + `linear-gradient(90deg,#3b82f6,#0ea5e9)` (line 66) 두 그라데이션이 sketch 의 `bg-safe-bar` + `bg-accent` 토큰 패턴을 덮어쓰지 않도록 명시.
    10. `feedback_avoid_premature_confirmation` — TSX 변환 후 "approved 거의 일치" 자체 판단 금지. 결과 보여주고 사용자 판단. 특히 ⋮ ⎋ 글리프 시각 결과 (모바일 iOS Safari + Android Chrome 실기 확인) 는 사용자 판단 영역.
    11. ★ `feedback_pwa_cache_invalidation` (28-splash 특화 — 캐시 갱신 anchor) — SplashScreen 의 `checkVersionAndRefresh()` (line 16) 는 PWA 캐시 무효화 핵심 시그니처. `__APP_VERSION__` 미스매치 시 `location.reload()` 호출. TSX 변환 시 이 호출 1 byte 변경 시 PWA 캐시가 배포 무시 (메모리 룰). 변경 금지.
    12. ★ `feedback_ios_pwa_push_silent_drop` (28-splash 특화 — PWA 설치 anchor) — InstallPrompt 의 `isStandalone()` (line 5~10) + `shouldShowInstallPrompt()` (line 224~233) 는 PWA 미설치 시 푸시 silent drop 검출 첫 단추. 24h TTL (line 230) 도 미설치 신고 우선순위 anchor. 변경 금지.
    13. `feedback_check_branch_before_edit` — TSX 변환 wave 진입 전 현재 branch 가 `redesign/28-splash` 인지 확인 (main 단일-trunk 운영, dirty 면 사용자 컨펌 먼저).

    **§12. 다음 단계**

    1. 본 wave (W5 checklist) 작성 완료 → 4 commit + push (orchestrator cherry-pick 으로 부모 머지).
    2. 사용자 시각 검수 — cbc7119-preview 배포 사이클 1회 (main 머지 후 자동) — sketch 3 HTML 직접 열어 frame 진행률 매트릭스 + 가이드 ⋮ ⎋ 글리프 시각 확인 (Android Chrome + iOS Safari 양쪽 실기 확인 권장).
    3. 사용자 컨펌 받으면 **다음 quick task 시작** (`/clear` 권장 — memory `feedback_gsd_workflow_strict`) — TSX 변환 wave 진입.
       - 새 quick id (예: 260523-XX) 생성
       - PLAN: 1 task (W6 — 2 파일 단일 atomic 변환), action = §3 매핑 표 verbatim + §5/§6/§7/§8 verify gate 그대로 적용
       - 산출: SplashScreen.tsx + InstallPrompt.tsx 2 파일 atomic 수정 + atomic commit 1개 (`feat(28-splash): W6 TSX 변환 ...`)
    4. TSX 변환 wave 완료 → 사용자 시각 검수 → main 머지 → cbc7119-preview 배포 → 직원도메인 별도 배포 (메모리 `feedback_deploy_test` 룰 — design 작업은 사용자 명시 컨펌 후만).
    5. 28-splash 완결 status 메모 (memory `project_redesign_*_status` 패턴).

    **§ 자체 verify (본 W5 checklist 가 통과해야 할 gate)**
    - 4 src 파일 (SplashScreen.tsx + InstallPrompt.tsx + pwaInstall.ts + versionCheck.ts) 모두 변경 0 (`git diff HEAD~4 HEAD -- {4 paths}` empty)
    - 12 섹션 헤더 ≥1 each (§1~§12)
    - verify 명령 fence ≥4 (§5/§6/§7/§8)
    - sketch 출처 frontmatter sketch_sources 3개 (W2/W3/W4) 모두 존재 확인 (test -f)
    - OQ #1~#5 인용 ≥5건
    - 메모리 룰 ≥12 인용 (10 기본 + 28-splash 특화 PWA cache + iOS PWA push 2건)
    - 비즈 anchor 16건 (SplashScreen 8 + InstallPrompt 8) 모두 verbatim 박제
    - 특수 글리프 ⋮ ⎋ 2건 박제 (U+22EE + U+238B + verticalAlign 인용)

    **commit 메시지:**
    `docs(quick-260522-2q6): redesign/28-splash W5 TSX conversion checklist (12 sections, sketch sources 3 + verify gates + 비즈 anchor 16건 박제 + ⋮ ⎋ 글리프 anchor + 4 src 파일 변경 0)`

    **commit 명령:**
    `git add cha-bio-safety/docs/redesign-context/28-splash/wave-5-tsx-conversion-checklist.md && git commit -m "docs(quick-260522-2q6): redesign/28-splash W5 TSX conversion checklist (12 sections, sketch sources 3 + verify gates + 비즈 anchor 16건 박제 + ⋮ ⎋ 글리프 anchor + 4 src 파일 변경 0)"`

    **마지막 단계 (commit 후 4 파일 변경 0 최종 확인):**
    `git diff --name-only HEAD~4 HEAD -- cha-bio-safety/src/pages/SplashScreen.tsx cha-bio-safety/src/components/InstallPrompt.tsx cha-bio-safety/src/utils/pwaInstall.ts cha-bio-safety/src/utils/versionCheck.ts` 의 결과가 빈 줄이어야 함 (최근 4 commit 동안 4 파일 모두 변경 0).
  </action>
  <verify>
    <automated>
test -f cha-bio-safety/docs/redesign-context/28-splash/wave-5-tsx-conversion-checklist.md \
  && wc -l cha-bio-safety/docs/redesign-context/28-splash/wave-5-tsx-conversion-checklist.md | awk '{ if ($1 < 320) { print "FAIL: too short"; exit 1 } else print "PASS lines:", $1 }' \
  && grep -cE '^# §1[0-2]?\b|^## §1[0-2]?\b|^### §1[0-2]?\b|^\*\*§1[0-2]?\b|^# §[1-9]\b|^## §[1-9]\b|^### §[1-9]\b|^\*\*§[1-9]\b' cha-bio-safety/docs/redesign-context/28-splash/wave-5-tsx-conversion-checklist.md | awk '{ if ($1 < 12) { print "FAIL: §1~§12 헤더 <12"; exit 1 } else print "PASS sections:", $1 }' \
  && grep -c '^```' cha-bio-safety/docs/redesign-context/28-splash/wave-5-tsx-conversion-checklist.md | awk '{ if ($1 < 8) { print "FAIL: fence <8 (4 verify section x 2 open/close each)"; exit 1 } else print "PASS fences:", $1 }' \
  && grep -cE 'OQ #1|OQ #2|OQ #3|OQ #4|OQ #5' cha-bio-safety/docs/redesign-context/28-splash/wave-5-tsx-conversion-checklist.md | awk '{ if ($1 < 5) { print "FAIL: OQ #1~#5 citations <5"; exit 1 } else print "PASS OQ citations:", $1 }' \
  && grep -c 'SplashScreen\.tsx' cha-bio-safety/docs/redesign-context/28-splash/wave-5-tsx-conversion-checklist.md | awk '{ if ($1 < 8) { print "FAIL: SplashScreen.tsx ref <8"; exit 1 } else print "PASS SplashScreen refs:", $1 }' \
  && grep -c 'InstallPrompt\.tsx' cha-bio-safety/docs/redesign-context/28-splash/wave-5-tsx-conversion-checklist.md | awk '{ if ($1 < 8) { print "FAIL: InstallPrompt.tsx ref <8"; exit 1 } else print "PASS InstallPrompt refs:", $1 }' \
  && grep -c 'pwaInstall\.ts\|versionCheck\.ts' cha-bio-safety/docs/redesign-context/28-splash/wave-5-tsx-conversion-checklist.md | awk '{ if ($1 < 2) { print "FAIL: pwaInstall.ts + versionCheck.ts ref <2"; exit 1 } else print "PASS utils refs:", $1 }' \
  && grep -c 'bg-safe-bar' cha-bio-safety/docs/redesign-context/28-splash/wave-5-tsx-conversion-checklist.md | awk '{ if ($1 < 2) { print "FAIL: bg-safe-bar citation <2 (OQ #1)"; exit 1 } else print "PASS bg-safe-bar:", $1 }' \
  && grep -c 'bg-accent' cha-bio-safety/docs/redesign-context/28-splash/wave-5-tsx-conversion-checklist.md | awk '{ if ($1 < 4) { print "FAIL: bg-accent citation <4 (OQ #2 + #4)"; exit 1 } else print "PASS bg-accent:", $1 }' \
  && grep -c 'text-warning\|bg-warning-bg\|border-warning' cha-bio-safety/docs/redesign-context/28-splash/wave-5-tsx-conversion-checklist.md | awk '{ if ($1 < 3) { print "FAIL: warning 토큰 citation <3 (OQ #4)"; exit 1 } else print "PASS warning tokens:", $1 }' \
  && grep -c '__APP_VERSION__' cha-bio-safety/docs/redesign-context/28-splash/wave-5-tsx-conversion-checklist.md | awk '{ if ($1 < 2) { print "FAIL: __APP_VERSION__ 비즈 anchor 박제 <2"; exit 1 } else print "PASS __APP_VERSION__:", $1 }' \
  && grep -c '@keyframes slideUp' cha-bio-safety/docs/redesign-context/28-splash/wave-5-tsx-conversion-checklist.md | awk '{ if ($1 < 2) { print "FAIL: slideUp keyframes 박제 <2"; exit 1 } else print "PASS slideUp:", $1 }' \
  && grep -c '48ms\|, 48' cha-bio-safety/docs/redesign-context/28-splash/wave-5-tsx-conversion-checklist.md | awk '{ if ($1 < 2) { print "FAIL: 48ms tick 박제 <2"; exit 1 } else print "PASS 48ms:", $1 }' \
  && grep -c '1600' cha-bio-safety/docs/redesign-context/28-splash/wave-5-tsx-conversion-checklist.md | awk '{ if ($1 < 2) { print "FAIL: 1600ms timeout 박제 <2"; exit 1 } else print "PASS 1600ms:", $1 }' \
  && grep -c 'isStandalone' cha-bio-safety/docs/redesign-context/28-splash/wave-5-tsx-conversion-checklist.md | awk '{ if ($1 < 2) { print "FAIL: isStandalone 비즈 anchor 박제 <2"; exit 1 } else print "PASS isStandalone:", $1 }' \
  && grep -c 'isIOS' cha-bio-safety/docs/redesign-context/28-splash/wave-5-tsx-conversion-checklist.md | awk '{ if ($1 < 2) { print "FAIL: isIOS 박제 <2"; exit 1 } else print "PASS isIOS:", $1 }' \
  && grep -c 'shouldShowInstallPrompt' cha-bio-safety/docs/redesign-context/28-splash/wave-5-tsx-conversion-checklist.md | awk '{ if ($1 < 2) { print "FAIL: shouldShowInstallPrompt 박제 <2"; exit 1 } else print "PASS shouldShowInstallPrompt:", $1 }' \
  && grep -c 'dismissInstallPrompt' cha-bio-safety/docs/redesign-context/28-splash/wave-5-tsx-conversion-checklist.md | awk '{ if ($1 < 2) { print "FAIL: dismissInstallPrompt 박제 <2"; exit 1 } else print "PASS dismissInstallPrompt:", $1 }' \
  && grep -c 'pwa-install-dismissed' cha-bio-safety/docs/redesign-context/28-splash/wave-5-tsx-conversion-checklist.md | awk '{ if ($1 < 2) { print "FAIL: pwa-install-dismissed key 박제 <2"; exit 1 } else print "PASS pwa-install-dismissed:", $1 }' \
  && grep -c '24 \* 60 \* 60 \* 1000\|24h TTL' cha-bio-safety/docs/redesign-context/28-splash/wave-5-tsx-conversion-checklist.md | awk '{ if ($1 < 2) { print "FAIL: 24h TTL 박제 <2"; exit 1 } else print "PASS 24h TTL:", $1 }' \
  && grep -c '⋮' cha-bio-safety/docs/redesign-context/28-splash/wave-5-tsx-conversion-checklist.md | awk '{ if ($1 < 2) { print "FAIL: ⋮ U+22EE 박제 <2 (OQ #5)"; exit 1 } else print "PASS ⋮ anchor:", $1 }' \
  && grep -c '⎋' cha-bio-safety/docs/redesign-context/28-splash/wave-5-tsx-conversion-checklist.md | awk '{ if ($1 < 2) { print "FAIL: ⎋ U+238B 박제 <2 (OQ #5)"; exit 1 } else print "PASS ⎋ anchor:", $1 }' \
  && grep -c 'U+22EE\|U+238B' cha-bio-safety/docs/redesign-context/28-splash/wave-5-tsx-conversion-checklist.md | awk '{ if ($1 < 2) { print "FAIL: ⋮ ⎋ codepoint anchor <2"; exit 1 } else print "PASS U+22EE/U+238B anchor:", $1 }' \
  && grep -c 'verticalAlign\|vertical-align' cha-bio-safety/docs/redesign-context/28-splash/wave-5-tsx-conversion-checklist.md | awk '{ if ($1 < 1) { print "FAIL: verticalAlign middle 박제 missing"; exit 1 } else print "PASS verticalAlign:", $1 }' \
  && grep -c 'sketch-wave-2-splash' cha-bio-safety/docs/redesign-context/28-splash/wave-5-tsx-conversion-checklist.md | awk '{ if ($1 < 1) { print "FAIL: sketch W2 ref missing"; exit 1 } else print "PASS W2 ref:", $1 }' \
  && grep -c 'sketch-wave-3-install-main' cha-bio-safety/docs/redesign-context/28-splash/wave-5-tsx-conversion-checklist.md | awk '{ if ($1 < 1) { print "FAIL: sketch W3 ref missing"; exit 1 } else print "PASS W3 ref:", $1 }' \
  && grep -c 'sketch-wave-4-install-guides' cha-bio-safety/docs/redesign-context/28-splash/wave-5-tsx-conversion-checklist.md | awk '{ if ($1 < 1) { print "FAIL: sketch W4 ref missing"; exit 1 } else print "PASS W4 ref:", $1 }' \
  && grep -oE 'feedback_[a-z_]+|project_redesign_[a-z0-9_]+' cha-bio-safety/docs/redesign-context/28-splash/wave-5-tsx-conversion-checklist.md | sort -u | wc -l | awk '{ if ($1 < 12) { print "FAIL: memory rules unique <12"; exit 1 } else print "PASS memory rules:", $1 }' \
  && (git diff --name-only HEAD~4 HEAD -- cha-bio-safety/src/pages/SplashScreen.tsx | wc -l | awk '{ if ($1 != 0) { print "FAIL: SplashScreen.tsx changed in last 4 commits"; exit 1 } else print "PASS SplashScreen.tsx untouched" }') \
  && (git diff --name-only HEAD~4 HEAD -- cha-bio-safety/src/components/InstallPrompt.tsx | wc -l | awk '{ if ($1 != 0) { print "FAIL: InstallPrompt.tsx changed in last 4 commits"; exit 1 } else print "PASS InstallPrompt.tsx untouched" }') \
  && (git diff --name-only HEAD~4 HEAD -- cha-bio-safety/src/utils/pwaInstall.ts | wc -l | awk '{ if ($1 != 0) { print "FAIL: pwaInstall.ts changed"; exit 1 } else print "PASS pwaInstall.ts untouched" }') \
  && (git diff --name-only HEAD~4 HEAD -- cha-bio-safety/src/utils/versionCheck.ts | wc -l | awk '{ if ($1 != 0) { print "FAIL: versionCheck.ts changed"; exit 1 } else print "PASS versionCheck.ts untouched" }') \
  && (ls cha-bio-safety/docs/redesign-context/28-splash/sketch-wave-{2-splash,3-install-main,4-install-guides}.html 2>/dev/null | wc -l | awk '{ if ($1 != 3) { print "FAIL: 3 sketch files missing"; exit 1 } else print "PASS all 3 sketches present" }') \
  && git log --oneline -1 | grep -q '260522-2q6.*W5' && echo "PASS commit W5"
    </automated>
  </verify>
  <done>
    - wave-5-tsx-conversion-checklist.md 파일 존재 + ≥320 lines
    - §1~§12 12 섹션 헤더 모두 존재
    - 자체 verify fence ≥8 (§5/§6/§7/§8 각 명령 fence open+close)
    - OQ #1~#5 5건 모두 인용
    - SplashScreen.tsx ref ≥8 / InstallPrompt.tsx ref ≥8 / pwaInstall.ts + versionCheck.ts ref ≥2
    - bg-safe-bar ≥2 (OQ #1) / bg-accent ≥4 (OQ #2 + #4) / warning 토큰 ≥3 (OQ #4)
    - ★ 비즈 anchor 16건 박제: SplashScreen 8 (__APP_VERSION__ ≥2 / @keyframes slideUp ≥2 / 48ms ≥2 / 1600ms ≥2 / +4 / checkVersionAndRefresh / shouldShowInstallPrompt / isAuthenticated) + InstallPrompt 8 (isStandalone ≥2 / isIOS ≥2 / shouldShowInstallPrompt ≥2 / dismissInstallPrompt ≥2 / pwa-install-dismissed ≥2 / 24h TTL ≥2 / outcome accepted/unavailable/dismissed / 3 view state)
    - ★ 특수 글리프 OQ #5 박제: ⋮ ≥2 / ⎋ ≥2 / U+22EE 또는 U+238B ≥2 / verticalAlign ≥1
    - sketch W2/W3/W4 ref 각 ≥1
    - 메모리 룰 (feedback_*.md + project_redesign_*) unique ≥12
    - **4 src 파일 (SplashScreen.tsx + InstallPrompt.tsx + pwaInstall.ts + versionCheck.ts) 모두 변경 0** (git diff HEAD~4 HEAD 결과 empty)
    - 3 sketch HTML 파일 (W2/W3/W4) 모두 존재 확인
    - atomic commit `docs(quick-260522-2q6): ... W5 TSX conversion checklist ...` 1개
  </done>
</task>

</tasks>

<verification>
**전체 4 task 종합 verify (executor 가 마지막 task 4 종료 직후 실행):**

```bash
# 1. 4 산출 파일 모두 존재
test -f cha-bio-safety/docs/redesign-context/28-splash/sketch-wave-2-splash.html
test -f cha-bio-safety/docs/redesign-context/28-splash/sketch-wave-3-install-main.html
test -f cha-bio-safety/docs/redesign-context/28-splash/sketch-wave-4-install-guides.html
test -f cha-bio-safety/docs/redesign-context/28-splash/wave-5-tsx-conversion-checklist.md

# 2. 4 src 파일 모두 변경 0 (가장 중요)
[[ -z "$(git diff --name-only HEAD~4 HEAD -- cha-bio-safety/src/pages/SplashScreen.tsx)" ]] || echo "FAIL: SplashScreen.tsx changed"
[[ -z "$(git diff --name-only HEAD~4 HEAD -- cha-bio-safety/src/components/InstallPrompt.tsx)" ]] || echo "FAIL: InstallPrompt.tsx changed"
[[ -z "$(git diff --name-only HEAD~4 HEAD -- cha-bio-safety/src/utils/pwaInstall.ts)" ]] || echo "FAIL: pwaInstall.ts changed"
[[ -z "$(git diff --name-only HEAD~4 HEAD -- cha-bio-safety/src/utils/versionCheck.ts)" ]] || echo "FAIL: versionCheck.ts changed"

# 3. 평면 폴더 패턴 — sketch/ 서브폴더 없음
test ! -d cha-bio-safety/docs/redesign-context/28-splash/sketch/

# 4. 4 atomic commit 확인
git log --oneline HEAD~4..HEAD | grep -c '260522-2q6' | awk '{ if ($1 != 4) { print "FAIL: 4 commit 아님"; exit 1 } else print "PASS 4 commits" }'

# 5. 28-splash/ 외 변경 0 (다른 페이지 영향 없음)
git diff --name-only HEAD~4 HEAD | grep -v '^cha-bio-safety/docs/redesign-context/28-splash/' | wc -l | awk '{ if ($1 != 0) { print "FAIL: 다른 페이지 변경 감지"; exit 1 } else print "PASS only 28-splash/ changed" }'

# 6. ⋮ ⎋ 글리프 박제 — W4 sketch + W5 checklist 양쪽
grep -c '⋮' cha-bio-safety/docs/redesign-context/28-splash/sketch-wave-4-install-guides.html | awk '{ if ($1 < 2) { print "FAIL: ⋮ W4 sketch <2"; exit 1 } else print "PASS ⋮ W4:", $1 }'
grep -c '⎋' cha-bio-safety/docs/redesign-context/28-splash/sketch-wave-4-install-guides.html | awk '{ if ($1 < 1) { print "FAIL: ⎋ W4 sketch missing"; exit 1 } else print "PASS ⎋ W4:", $1 }'
grep -c '⋮' cha-bio-safety/docs/redesign-context/28-splash/wave-5-tsx-conversion-checklist.md | awk '{ if ($1 < 2) { print "FAIL: ⋮ W5 checklist <2"; exit 1 } else print "PASS ⋮ W5:", $1 }'
grep -c '⎋' cha-bio-safety/docs/redesign-context/28-splash/wave-5-tsx-conversion-checklist.md | awk '{ if ($1 < 2) { print "FAIL: ⎋ W5 checklist <2"; exit 1 } else print "PASS ⎋ W5:", $1 }'
```
</verification>

<success_criteria>
- W2~W4 sketch HTML 3개 + W5 checklist markdown 1개 = 총 4 파일 모두 `cha-bio-safety/docs/redesign-context/28-splash/` 직속 평면 배치 (sketch/ 서브폴더 X)
- SplashScreen.tsx (83 lines) + InstallPrompt.tsx (237 lines) + pwaInstall.ts + versionCheck.ts 4 src 파일 모두 변경 0 byte
- OQ #1~#5 LOCKED 결정 5건 모두 4 파일 안 verbatim 반영:
  - OQ #1: 설치 버튼 bg-safe-bar solid + 그라데이션 완전 폐기 (W3 + W5)
  - OQ #2: 진행 바 bg-accent solid + 그라데이션 완전 폐기 (W2 + W5)
  - OQ #3: 폰트 격상 매핑 10→12 / 11→12 / 12 유지 / 13 / 15→16 / 16 인라인 / 18→text-title / 22→text-heading (W2~W5 모두)
  - OQ #4: 토큰 치환 (page / 카드 / 4 text / warning / accent/15) + 인라인 유지 4건 (트랙 / 카드 border / 오버레이 / boxShadow) (W2~W5 모두)
  - OQ #5: iOS ⋮ U+22EE + ⎋ U+238B 글리프 fontSize:16 + verticalAlign middle 보존 (W4 + W5)
- ★ 비즈 anchor 16건 (SplashScreen 8 + InstallPrompt 8) 모두 W2~W5 sketch + checklist verbatim 박제
- ★ 특수 글리프 ⋮ ⎋ 2건 모두 W4 sketch + W5 checklist 양쪽 verbatim 박제 (Lucide 교체 X anchor)
- negative gate (이모지 단 ⋮ ⎋ 예외 / linear-gradient / 9·10·11px / status- prefix / w-8 h-8 / 옛 alias 토큰) 4 파일 모두 0건
- 각 task atomic commit 1개씩 = 총 4 commit (`docs(quick-260522-2q6): ...`)
- 28-splash/ 외 변경 0 (다른 페이지 영향 없음)
</success_criteria>

<output>
- W2~W4 sketch HTML 3개 + W5 checklist markdown 1개 = 총 4 파일이 `cha-bio-safety/docs/redesign-context/28-splash/` 직속 평면 배치로 생성됨
- 4 atomic commit (W2 / W3 / W4 / W5) 모두 worktree 안에서 실행됨 — orchestrator 가 cherry-pick 으로 부모 브랜치 머지 (executor commit 자동 머지 안 됨, executor cherry-pick 사고 4회 precedent 회피)
- `.planning/quick/260522-2q6-redesign-28-splash-sketch-waves-w2-w5-4-/260522-2q6-SUMMARY.md` 생성 (다음 wave 인수인계 — sketch sources 3 + checklist + 비즈 anchor 16건 박제 + ⋮ ⎋ 글리프 anchor + 사용자 시각 검수 안내)
- 사용자 시각 검수 후 W6 (TSX 변환) wave 진입 가능 — `/clear` + 새 quick id (260523-XX) + PLAN 1 task (action = §3 매핑 verbatim, verify = §5/§6/§7/§8 그대로) + 2 파일 단일 atomic 변환
</output>
