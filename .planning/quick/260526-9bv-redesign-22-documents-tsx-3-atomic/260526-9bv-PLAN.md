---
phase: quick-260526-9bv
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cha-bio-safety/src/pages/DocumentsPage.tsx
  - cha-bio-safety/src/components/DocumentSection.tsx
  - cha-bio-safety/src/components/DocumentUploadForm.tsx
  - cha-bio-safety/src/styles/components.css
autonomous: true
requirements:
  - REDESIGN-22-W7
quick_id: 260526-9bv
branch: redesign/22-documents
mirror_of:
  - .planning/quick/260525-uou-redesign-18-worklog-tsx-worklogpage-1216 (slq W7 단일 atomic)
  - 19-legal (6if) / 20-legal-findings (bbz) / 21-legal-finding-detail (lft) / 23-education (4i9) / 28-splash (0803d2f)
must_haves:
  truths:
    - "DocumentsPage 모바일 탭 + 데스크톱 좌우 2단 + upload sheet/modal shell 모두 정상 렌더"
    - "DocumentSection 4 state (loading/error/empty/data) + Hero card + 과거 이력 + admin Trash2 + Loader2 spin 모두 동작"
    - "DocumentUploadForm year/title/file + Progress bar + Error block + abort/retry + beforeunload guard 동작"
    - "documentsApi.list/remove + runMultipartUpload 호출 시그니처 1 byte 변경 0"
    - "★ delete confirm full string '정말 삭제하시겠습니까? 이 작업은 되돌릴 수 없습니다.' verbatim 보존"
    - "★ beforeunload '업로드 중입니다. 페이지를 나가면 전송이 중단됩니다.' verbatim 보존"
    - "★ abort confirm '업로드를 취소하시겠습니까? 지금까지 전송된 데이터는 저장되지 않습니다.' verbatim 보존"
    - "★ 403 카피 '관리자만 업로드할 수 있습니다.' verbatim 보존"
    - "components.css 신규 ≥15 class (.docs-*) + .btn-primary-gradient 추가"
    - "tsc --noEmit 0 errors + npm run build PASS"
  artifacts:
    - path: "cha-bio-safety/src/pages/DocumentsPage.tsx"
      provides: "DocumentsPage TSX (Tailwind class + components.css inherit)"
    - path: "cha-bio-safety/src/components/DocumentSection.tsx"
      provides: "DocumentSection TSX (4 state + Hero + 과거 + admin)"
    - path: "cha-bio-safety/src/components/DocumentUploadForm.tsx"
      provides: "DocumentUploadForm TSX (year/title/file + Progress + abort/retry)"
    - path: "cha-bio-safety/src/styles/components.css"
      provides: "신규 .docs-* class ≥15 + .btn-primary-gradient (OQ #2 lin-grad)"
  key_links:
    - from: "src/pages/DocumentsPage.tsx"
      to: "src/components/DocumentSection.tsx + DocumentUploadForm.tsx"
      via: "import default + props (type/onUploadClick/onClose)"
      pattern: "import DocumentSection from '../components/DocumentSection'"
    - from: "src/components/DocumentSection.tsx"
      to: "utils/api (documentsApi) + utils/downloadBlob + utils/multipartUpload + stores/authStore + hooks/useIsDesktop"
      via: "import — 외부 의존 수정 0"
      pattern: "from '../utils/api'"
    - from: "src/components/DocumentUploadForm.tsx"
      to: "utils/multipartUpload (runMultipartUpload/formatBytes/formatEta/ProgressState) + utils/api (ApiError)"
      via: "import — 외부 의존 수정 0"
      pattern: "from '../utils/multipartUpload'"
---

<objective>
redesign/22-documents 의 **W7 TSX 변환 wave 단일 진입점**.
3 파일 (DocumentsPage 162 + DocumentSection 517 + DocumentUploadForm 402 = 1081 lines) + components.css 신규 17 class 추가 = 4 파일 **단일 atomic commit**.

W6 checklist (wave-6-tsx-conversion-checklist.md 501 lines) §1~§12 verbatim 적용 — 비즈 anchor 9 그룹 + OQ LOCKED 5 + sketch class fence 36 unique + negative 17 + verify 22 모두 박제.

Purpose: 19-legal (6if) / 20-legal-findings (bbz) / 21-legal-finding-detail (lft) / 23-education (4i9) / 28-splash (0803d2f) 5 mirror 의 multi-file scope 확장 적용. 22-documents 만 3 파일 동시 변환 (sketch HTML 4 wave 합산 36 class).

Output:
- DocumentsPage.tsx 변환 (var(--*) alias → Tailwind token class + components.css inherit)
- DocumentSection.tsx 변환 (4 state + Hero + 과거 + admin)
- DocumentUploadForm.tsx 변환 (year/title/file + Progress + abort/retry)
- components.css 신규 .docs-* class ≥15 + .btn-primary-gradient
- 단일 atomic commit 1 + SUMMARY commit 1 = 2 commit
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@cha-bio-safety/docs/redesign-context/22-documents/wave-6-tsx-conversion-checklist.md
@cha-bio-safety/docs/redesign-context/22-documents/DocumentsPage.tsx
@cha-bio-safety/docs/redesign-context/22-documents/DocumentSection.tsx
@cha-bio-safety/docs/redesign-context/22-documents/DocumentUploadForm.tsx
@cha-bio-safety/src/styles/components.css
@cha-bio-safety/docs/redesign-context/00-design-context/design-system.md
@cha-bio-safety/docs/redesign-context/00-design-context/tokens.css

<interfaces>
<!-- 외부 의존 (수정 0) -->

utils/api.ts 의 documentsApi (시그니처 1 byte 변경 0):
- `documentsApi.list(type: 'plan' | 'drill') => Promise<DocumentListItem[]>`
- `documentsApi.remove(id: number) => Promise<void>`
- `ApiError` 클래스 (status 403 분기용)
- type `DocumentListItem`: { id, type, year, title, filename, size, uploaded_by_name, uploaded_at }

utils/multipartUpload.ts (수정 0):
- `runMultipartUpload({ file, type, year, title, contentType, signal, onProgress }) => Promise<void>`
- `formatBytes(bytes: number) => string`
- `formatEta(seconds: number) => string`
- type `ProgressState`: { percent, loadedBytes, totalBytes, speedBps, etaSeconds }

utils/downloadBlob.ts (수정 0):
- `downloadDocument(id: number, filename: string) => Promise<void>`

stores/authStore.ts (수정 0):
- `useAuthStore((s) => s.staff?.role === 'admin') => boolean`

hooks/useIsDesktop.ts (수정 0):
- `useIsDesktop() => boolean` (≥1024px 분기)

App.tsx (수정 0):
- /documents 라우트 + MOBILE_NO_NAV_PATHS + DESKTOP_HEADER_HIDE_PATHS + PAGE_TITLES 미수정
</interfaces>

<sketch_class_fence>
<!-- W6 §6 sketch grep fence 36 unique class verbatim (≥8 마지노선 4.5배) -->
<!-- 위치: wave-6-tsx-conversion-checklist.md line 244~282 -->

핵심 토큰 패턴 (변환 시 그대로 적용):
- bg-accent / border-accent / text-text-on-accent (OQ #1)
- bg-surface-raised / bg-surface-sunken / bg-surface-page (OQ #5)
- text-text-primary / text-text-secondary / text-text-tertiary
- text-danger (status- prefix 0 — feedback_tailwind_token_class_pattern)
- text-heading / text-body / text-body-sm / text-caption (4 단계)
- text-caption + leading-none (작은 컨테이너 강제 — feedback_text_caption_leading_none)
- font-bold / font-semibold / font-medium / font-normal
- border border-border-default / border-dashed / border-dashed border-border-default
- rounded-sm / rounded-md / rounded-lg / rounded-xl / rounded-full

sketch 인프라 클래스 (TSX 변환 시 모두 제거):
- frame / frame-mobile / frame-desktop / frame-label / sketch-wrap / placeholder
- rules-box / dialog / dialog-overlay / star
</sketch_class_fence>

<biz_anchor_9_groups>
<!-- W6 §4 9 그룹 verbatim 박제 (1 byte 변경 0) -->

GROUP 1 — documentsApi 2종 (utils/api 외부 의존 미수정):
  - documentsApi.list(type) / documentsApi.remove(id) 시그니처 그대로
  - DocumentListItem type 보존 (id/type/year/title/filename/size/uploaded_by_name/uploaded_at)

GROUP 2 — useQuery + invalidateQueries:
  - useQuery({ queryKey: ['documents', type], queryFn: () => documentsApi.list(type), staleTime: 60_000 })
  - queryClient.invalidateQueries({ queryKey: ['documents', type] }) (Section handleDelete + Form handleSubmit 2곳)

GROUP 3 — ALLOWED 6 ext + EXT_TO_MIME + MAX_SIZE 500MB:
  - ALLOWED 6 ext: .pdf .xlsx .docx .pptx .hwp .zip (Form line 23~30 verbatim)
  - EXT_TO_MIME 매핑 (line 32~39) / MAX_SIZE = 500 * 1024 * 1024 (line 41)

GROUP 4 — empty MIME fallback (iOS Safari HWP/ZIP):
  - 조건 `if (f.type && !(entry.mimes as readonly string[]).includes(f.type))` (Form line 96 verbatim)
  - HWP/ZIP empty MIME 통과 허용

GROUP 5 — multipart upload runMultipartUpload:
  - { file, type, year, title, contentType, signal, onProgress } (Form line 122~130)
  - contentType fallback: file.type || (entry ? EXT_TO_MIME[entry.ext] : 'application/octet-stream') (line 112)

GROUP 6 — abort/retry:
  - abortRef = useRef<AbortController|null>(null) (line 59)
  - ctrl = new AbortController() + abortRef.current = ctrl + finally abortRef.current = null (line 118~152)
  - AbortError 분기: err.name === 'AbortError' → '업로드가 취소되었습니다.' + isAbort=true → toast 미발송 (line 137~138)
  - handleCancel: isUploading 시 window.confirm → ok → abortRef.current?.abort() (line 155~166)
  - handleRetry: setError(null) + void handleSubmit() (line 168~171)
  - unmount cleanup useEffect: return () => abortRef.current?.abort() (line 75~79)

GROUP 7 — beforeunload guard:
  - useEffect (line 64~72) — isUploading 동안만 listener 등록
  - returnValue verbatim '업로드 중입니다. 페이지를 나가면 전송이 중단됩니다.' (1 byte 변경 0)

GROUP 8 — admin 권한 isAdmin:
  - isAdmin = useAuthStore((s) => s.staff?.role === 'admin') (Section line 40)
  - uploadBtn 분기 / Trash2 분기 (Hero + 과거)
  - 403 → '관리자만 업로드할 수 있습니다.' (Form line 141) — ApiError status 분기

GROUP 9 — ★ delete confirm gate (memory project_legal_findings_delete_incident_260520):
  - Section handleDelete (line 87~89) window.confirm full string verbatim:
    `"${item.title}\n(${item.filename})\n\n정말 삭제하시겠습니까? 이 작업은 되돌릴 수 없습니다."`
  - 1 byte 변경 0 (2026-05-20 22행 삭제 사고 후 가드 강화 룰 일반화)
  - 추가 가드: deletingIds Set state per-item 잠금 / Loader2 spin / aria-disabled
  - 성공 후 toast.success('문서를 삭제했습니다') + invalidateQueries (2 step)
</biz_anchor_9_groups>

<oq_locked_5>
<!-- W6 §5.1~§5.5 LOCKED default 5 anchor verbatim -->

OQ #1 — chrome 강조색 토큰 통일 (LOCKED bg-accent):
  - 7곳 hex `#2f81f7` → `bg-accent` / `border-accent` 치환
  - fallback (tokens.css mismatch 시): `bg-[#2f81f7]` arbitrary (16-workshift 사례)

OQ #2 — submit button 그라데이션 (LOCKED lin-grad):
  - submit button: `background: 'linear-gradient(135deg, #1d4ed8, #0ea5e9)'` 또는 `.btn-primary-gradient` 신규 class
  - canSubmit false 시 `bg-surface-sunken text-text-tertiary cursor-not-allowed` 분기 보존

OQ #3 — 최신 pill 11 → 12 격상 (LOCKED 12 + leading-none):
  - 최신 pill: `text-caption font-bold leading-none` (12px, 노안 마지노선)
  - 파일 보조 'PDF, XLSX, ... 최대 500MB': `text-caption leading-none`

OQ #4 — 빈/오류/Progress 시각 일관성 (LOCKED 격상):
  - 빈 FileText size={48} 유지
  - 모달 title `${label} 업로드` text-heading (22, 20 → 22 격상)
  - Error 카피 text-body-sm font-medium (14, 13 → 14 격상)
  - Progress meta 16/600 % + 14/400 speedBps 유지

OQ #5 — var(--bg4) → bg-surface-sunken 통일 (LOCKED):
  - inputBaseStyle bg + progress bar bg + Year tile bg + dashed file button bg = `bg-surface-sunken` (4곳)
  - var(--bg3) + var(--bg4) 모두 동일 sunken 매핑
</oq_locked_5>

<components_css_inherit>
<!-- W6 §9 재사용 3 + 신규 17 = 20 class -->

재사용 ≥3 (기존):
  - .btn-primary → Section error '다시 시도' + Section uploadBtn 데스크톱 pill + Form Error '다시 시도'
  - .btn-secondary → Form cancel button
  - .empty-state → Section empty block wrapper

신규 ≥15 (.docs-* + .btn-primary-gradient):
  1. .docs-tab-bar (sticky top-0 z-10 flex bg-surface-page border-b border-border-default mb-4)
  2. .docs-tab-btn + .docs-tab-btn-active (flex-1 h-11 / active border-b-2 border-accent)
  3. .docs-section-header (flex justify-between items-center min-h-10)
  4. .docs-hero-card (relative w-full min-h-24 p-6 bg-surface-raised border-border-default rounded-xl)
  5. .docs-year-tile (w-16 h-16 bg-surface-sunken rounded-lg font-mono text-display font-semibold)
  6. .docs-latest-pill (absolute top-3 right-3 text-caption font-semibold leading-none bg-accent rounded-full)
  7. .docs-history-row (w-full min-h-14 p-4 bg-surface-raised border-border-default — first:rounded-t-lg + last:rounded-b-lg)
  8. .docs-trash-btn (w-[32px] h-[32px] text-danger border border-border-default rounded-lg — w-8 함정 회피)
  9. .docs-upload-btn-mobile (w-[40px] h-[40px] text-text-primary border border-border-default rounded-lg)
  10. .docs-upload-sheet + .docs-upload-sheet-body (fixed inset-0 bg-black/55 z-[1000] flex items-end / body rounded-t-2xl animate-slide-up)
  11. .docs-upload-modal + .docs-upload-modal-body (fixed inset-0 bg-black/55 z-[1000] flex items-center justify-center animate-fade-in)
  12. .docs-sheet-handle (w-10 h-1 bg-border-default rounded-sm mx-auto mb-4)
  13. .docs-input (w-full h-11 px-3 bg-surface-sunken border-border-default rounded-lg text-body-sm — OQ #5)
  14. .docs-file-btn + .docs-file-btn-filled (w-full h-11 bg-surface-sunken border border-dashed border-border-default rounded-lg)
  15. .docs-progress-bar + .docs-progress-fill (h-2 bg-surface-sunken / fill bg-accent transition-[width] duration-[240ms] ease-linear)
  16. .docs-error-block (flex items-center justify-between p-3 bg-surface-sunken border-border-default rounded-lg)
  17. .btn-primary-gradient (OQ #2 LOCKED lin-grad 135deg #1d4ed8 #0ea5e9 + disabled:bg-surface-sunken)

@keyframes 3종 components.css 안 정의:
  - docs-slide-up (DocumentsPage 인라인 → components.css 이관)
  - docs-fade-in (DocumentsPage 인라인 → components.css 이관)
  - docsec-spin (DocumentSection 인라인 → components.css 이관)
</components_css_inherit>

<deviation_abbreviations>
<!-- mbr deviation 2건 mirror -->
- "warning glyph" — AlertTriangle 약어 (Section error block 옵션 — OQ #4 LOCKED 추가 옵션 = 현재 미사용)
- "lin-grad" — linear-gradient 약어 (OQ #2 LOCKED submit button + .btn-primary-gradient anchor만 verbatim 등장)
</deviation_abbreviations>
</context>

<tasks>

<task type="auto">
  <name>T1: 4 파일 단일 atomic commit — DocumentsPage + DocumentSection + DocumentUploadForm + components.css</name>
  <files>
    cha-bio-safety/src/pages/DocumentsPage.tsx,
    cha-bio-safety/src/components/DocumentSection.tsx,
    cha-bio-safety/src/components/DocumentUploadForm.tsx,
    cha-bio-safety/src/styles/components.css
  </files>
  <action>
W6 checklist (cha-bio-safety/docs/redesign-context/22-documents/wave-6-tsx-conversion-checklist.md) §1~§12 verbatim 적용.

## 작업 순서 (4 파일 한 commit)

### Step 1 — components.css 신규 17 class 추가 (먼저, depends 우선)

cha-bio-safety/src/styles/components.css 의 @layer components { ... } 블록 끝(line ~500 직전)에 22-documents 전용 신규 섹션 추가:

```css
  /* ============================================================
   * 22-documents W7 — sections §1~§17 (W6 checklist §9 verbatim)
   * Source: wave-6-tsx-conversion-checklist.md §9 (재사용 3 + 신규 17 = 20 class)
   * 메모리 룰:
   * - feedback_planner_prompt_sketch_verbatim (W6 §6 fence 36 unique 박제)
   * - feedback_tailwind_token_class_pattern (status- prefix 0 + Lucide size prop)
   * - feedback_tailwind_w8_h8_is_48px (.docs-trash-btn 32 / .docs-upload-btn-mobile 40 명시 px)
   * - feedback_text_caption_leading_none (.docs-latest-pill 12 + leading-none)
   * - project_legal_findings_delete_incident_260520 (★ delete confirm 가드)
   * ============================================================ */

  /* §1. 모바일 탭 chrome (DocumentsPage W6 §3) */
  .docs-tab-bar { position: sticky; top: 0; z-index: 10; display: flex; background: var(--surface-page); border-bottom: 1px solid var(--border-default); margin-bottom: 16px; }
  .docs-tab-btn { flex: 1; height: 44px; background: transparent; color: var(--text-secondary); font-size: 16px; font-weight: 400; border: none; border-bottom: 2px solid transparent; cursor: pointer; line-height: 1; }
  .docs-tab-btn-active { color: var(--text-primary); font-weight: 600; border-bottom: 2px solid var(--accent); }  /* OQ #1 LOCKED */

  /* §2. Section 헤더 (DocumentSection W6 §3) */
  .docs-section-header { display: flex; justify-content: space-between; align-items: center; min-height: 40px; }

  /* §3. Hero card (DocumentSection latest W6 §3) */
  .docs-hero-card { position: relative; width: 100%; min-height: 96px; padding: 24px; background: var(--surface-raised); border: 1px solid var(--border-default); border-radius: 12px; display: flex; align-items: center; gap: 16px; cursor: pointer; text-align: left; }
  .docs-hero-card--busy { cursor: wait; opacity: 0.7; }
  .docs-year-tile { width: 64px; height: 64px; background: var(--surface-sunken); border-radius: 8px; display: flex; align-items: center; justify-content: center; flex-shrink: 0; font-family: 'JetBrains Mono', monospace; font-size: 28px; font-weight: 600; color: var(--text-primary); }  /* OQ #5 sunken */
  .docs-latest-pill { position: absolute; top: 12px; right: 12px; font-size: 12px; font-weight: 600; line-height: 1; color: var(--text-on-accent); background: var(--accent); padding: 4px 8px; border-radius: 9999px; }  /* OQ #3 11→12 격상 + leading-none + OQ #1 accent */
  .docs-hero-title { font-size: 16px; font-weight: 600; color: var(--text-primary); overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
  .docs-hero-meta { font-size: 14px; font-weight: 400; color: var(--text-secondary); overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }

  /* §4. 과거 이력 row (DocumentSection history W6 §3) */
  .docs-history-row { width: 100%; min-height: 56px; padding: 16px; background: var(--surface-raised); border: 1px solid var(--border-default); display: flex; align-items: center; gap: 12px; cursor: pointer; text-align: left; }
  .docs-history-row--first { border-top-left-radius: 8px; border-top-right-radius: 8px; }
  .docs-history-row--last { border-bottom-left-radius: 8px; border-bottom-right-radius: 8px; }
  .docs-history-row--not-first { border-top: none; }
  .docs-history-row--busy { cursor: wait; opacity: 0.7; }
  .docs-history-title { font-size: 14px; font-weight: 600; color: var(--text-primary); overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
  .docs-history-meta { font-size: 14px; font-weight: 400; color: var(--text-secondary); overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
  .docs-history-section-label { font-size: 16px; font-weight: 600; color: var(--text-primary); margin-top: 12px; margin-bottom: 12px; }

  /* §5. admin Trash2 + uploadBtn (w-8 함정 회피 — feedback_tailwind_w8_h8_is_48px) */
  .docs-trash-btn { width: 32px; height: 32px; display: inline-flex; align-items: center; justify-content: center; background: transparent; color: var(--danger); border: 1px solid var(--border-default); border-radius: 8px; cursor: pointer; flex-shrink: 0; }
  .docs-trash-btn--hero { position: absolute; bottom: 12px; right: 12px; }
  .docs-trash-btn:disabled { cursor: wait; }
  .docs-upload-btn-mobile { width: 40px; height: 40px; display: inline-flex; align-items: center; justify-content: center; background: transparent; color: var(--text-primary); border: 1px solid var(--border-default); border-radius: 8px; cursor: pointer; }
  .docs-upload-btn-desktop { height: 40px; padding: 0 16px; display: inline-flex; align-items: center; gap: 6px; background: var(--accent); color: var(--text-on-accent); font-size: 14px; font-weight: 600; border: none; border-radius: 9999px; cursor: pointer; line-height: 1; }  /* OQ #1 LOCKED */

  /* §6. upload sheet/modal shell (DocumentsPage W6 §3) */
  .docs-upload-sheet { position: fixed; inset: 0; background: rgba(0,0,0,0.55); z-index: 1000; display: flex; align-items: flex-end; justify-content: center; }
  .docs-upload-sheet-body { width: 100%; max-height: 85vh; overflow-y: auto; background: var(--surface-raised); border-top-left-radius: 16px; border-top-right-radius: 16px; padding: 24px; animation: docs-slide-up 240ms ease-out; }
  .docs-upload-modal { position: fixed; inset: 0; background: rgba(0,0,0,0.55); z-index: 1000; display: flex; align-items: center; justify-content: center; animation: docs-fade-in 180ms ease-out; }
  .docs-upload-modal-body { width: min(480px, 92vw); max-height: 85vh; overflow-y: auto; background: var(--surface-raised); border: 1px solid var(--border-default); border-radius: 12px; padding: 24px; }
  .docs-sheet-handle { width: 40px; height: 4px; background: var(--border-default); border-radius: 2px; margin: 0 auto 16px auto; }

  /* §7. Form input/select/file button (OQ #5 sunken) */
  .docs-input { width: 100%; height: 44px; padding: 0 12px; background: var(--surface-sunken); color: var(--text-primary); border: 1px solid var(--border-default); border-radius: 8px; font-size: 14px; outline: none; box-sizing: border-box; }
  .docs-input-label { display: block; font-size: 14px; font-weight: 600; color: var(--text-primary); margin-bottom: 8px; }
  .docs-file-btn { width: 100%; height: 44px; padding: 0 12px; background: var(--surface-sunken); color: var(--text-secondary); border: 1px dashed var(--border-default); border-radius: 8px; font-size: 14px; text-align: left; cursor: pointer; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
  .docs-file-btn--filled { color: var(--text-primary); }
  .docs-file-btn:disabled { cursor: not-allowed; }
  .docs-file-hint { font-size: 12px; font-weight: 400; color: var(--text-secondary); margin-top: 8px; line-height: 1; }  /* OQ #3 12 + leading-none */

  /* §8. Progress bar + meta (OQ #5 sunken + OQ #1 accent) */
  .docs-progress-bar { height: 8px; background: var(--surface-sunken); border-radius: 4px; overflow: hidden; }
  .docs-progress-fill { height: 100%; background: var(--accent); border-radius: 4px; transition: width 240ms linear; }  /* OQ #1 LOCKED */
  .docs-progress-meta { display: flex; justify-content: space-between; align-items: center; gap: 12px; font-family: 'JetBrains Mono', monospace; }
  .docs-progress-percent { font-size: 16px; font-weight: 600; color: var(--text-primary); }
  .docs-progress-speed { font-size: 14px; font-weight: 400; color: var(--text-secondary); text-align: right; }

  /* §9. Error block (OQ #4 14 격상 + OQ #5 sunken) */
  .docs-error-block { display: flex; align-items: center; justify-content: space-between; gap: 12px; padding: 12px; background: var(--surface-sunken); border: 1px solid var(--border-default); border-radius: 8px; }
  .docs-error-msg { color: var(--danger); font-size: 14px; font-weight: 500; flex: 1; }
  .docs-error-retry { height: 36px; padding: 0 14px; background: var(--accent); color: var(--text-on-accent); font-size: 14px; font-weight: 600; border: none; border-radius: 8px; cursor: pointer; flex-shrink: 0; }

  /* §10. submit button gradient (OQ #2 LOCKED lin-grad 135deg #1d4ed8 #0ea5e9) */
  .btn-primary-gradient { flex: 1; height: 44px; background: linear-gradient(135deg, #1d4ed8, #0ea5e9); color: #ffffff; font-size: 16px; font-weight: 600; border: none; border-radius: 8px; cursor: pointer; }
  .btn-primary-gradient:disabled { background: var(--surface-sunken); color: var(--text-tertiary); cursor: not-allowed; }

  /* §11. cancel button (Form cancel — .btn-secondary 재사용 또는 별도) */
  .docs-cancel-btn { flex: 1; height: 44px; background: transparent; color: var(--text-primary); font-size: 16px; font-weight: 600; border: 1px solid var(--border-default); border-radius: 8px; cursor: pointer; }

  /* §12. Section state blocks (재사용 .empty-state 보강) */
  .docs-loading-skeleton-hero { height: 96px; background: var(--surface-raised); border-radius: 12px; border: 1px solid var(--border-default); }
  .docs-loading-skeleton-row { height: 56px; background: var(--surface-raised); border-radius: 8px; }
  .docs-error-state { padding: 24px; background: var(--surface-raised); border: 1px solid var(--border-default); border-radius: 12px; display: flex; flex-direction: column; align-items: center; gap: 12px; }
  .docs-error-state-msg { color: var(--danger); font-size: 16px; font-weight: 600; }
  .docs-empty-state { padding: 48px 24px; background: var(--surface-raised); border: 1px solid var(--border-default); border-radius: 12px; display: flex; flex-direction: column; align-items: center; gap: 12px; text-align: center; }
  .docs-empty-title { font-size: 16px; font-weight: 600; color: var(--text-primary); }
  .docs-empty-hint { font-size: 14px; font-weight: 400; color: var(--text-secondary); }

  /* §13. @keyframes 3종 (인라인 → components.css 이관) */
  @keyframes docs-slide-up { from { transform: translateY(100%); } to { transform: translateY(0); } }
  @keyframes docs-fade-in { from { opacity: 0; } to { opacity: 1; } }
  @keyframes docsec-spin { to { transform: rotate(360deg); } }

}  /* @layer components 종료 */
```

(상기 신규 class 17+ 추가 / .btn-primary-gradient 신규 / @keyframes 3종 이관)

### Step 2 — DocumentsPage.tsx 변환

cha-bio-safety/src/pages/DocumentsPage.tsx 162 lines 전체 교체:

- W6 §1 imports 블록 verbatim (line 10~13 그대로 유지)
- W6 §2 시그니처 보존 (DocType / activeTab / uploadFor / tabBtnStyle / isDesktop)
- W6 §3 JSX 변환:
  - 인라인 @keyframes 제거 (components.css 이관)
  - 모바일 탭 → `.docs-tab-bar` + `.docs-tab-btn` / `.docs-tab-btn-active` 클래스
  - tabBtnStyle 헬퍼는 분기만 (className 적용) 또는 제거 후 className 분기로 대체
  - 데스크톱 좌우 2단 → arbitrary `max-w-[1200px] mx-auto p-6 flex gap-12` (또는 className)
  - 모바일 upload sheet → `.docs-upload-sheet` + `.docs-upload-sheet-body` + `.docs-sheet-handle`
  - 데스크톱 upload modal → `.docs-upload-modal` + `.docs-upload-modal-body`
  - backdrop NO-OP 코멘트 verbatim 유지 ('backdrop no-op — user must use 취소 button so confirm guard fires')
- 외부 의존 0 변경 (useIsDesktop / DocumentSection / DocumentUploadForm import 그대로)

### Step 3 — DocumentSection.tsx 변환

cha-bio-safety/src/components/DocumentSection.tsx 517 lines 전체 교체:

- W6 §1 imports verbatim (line 7~15)
- W6 §2 시그니처 보존 (Props / formatDate / typeLabel / isAdmin / deletingIds / useQuery key)
- W6 §3 JSX 변환:
  - 인라인 @keyframes docsec-spin 제거 (components.css 이관)
  - 헤더 행 → `.docs-section-header` + h2 text-body font-semibold
  - uploadBtn 모바일 → `.docs-upload-btn-mobile` + Plus size={20}
  - uploadBtn 데스크톱 → `.docs-upload-btn-desktop` (OQ #1 accent pill) + Plus size={16}
  - Loading → `.docs-loading-skeleton-hero` + `.docs-loading-skeleton-row` 3행
  - Error → `.docs-error-state` + `.docs-error-state-msg` + `.btn-primary` 재사용 ('다시 시도' button)
  - Empty → `.docs-empty-state` + FileText size={48} text-text-tertiary + `.docs-empty-title` + `.docs-empty-hint` (admin/non-admin 분기 카피 verbatim)
  - Hero card → `.docs-hero-card` (+ --busy 분기) + `.docs-year-tile` + `.docs-latest-pill` (OQ #3 12 + leading-none + OQ #1 accent) + `.docs-hero-title` + `.docs-hero-meta` + Loader2 size={16} animate (docsec-spin) + admin `.docs-trash-btn .docs-trash-btn--hero`
  - 과거 이력 → `.docs-history-section-label` + map → `.docs-history-row` (+ --first/--last/--not-first/--busy 분기) + `.docs-history-title` + `.docs-history-meta` + Loader2 + admin `.docs-trash-btn`
- 비즈 anchor verbatim:
  - useQuery({ queryKey: ['documents', type], queryFn: () => documentsApi.list(type), staleTime: 60_000 }) (GROUP 2)
  - handleDelete window.confirm full string verbatim (GROUP 9) `"${item.title}\n(${item.filename})\n\n정말 삭제하시겠습니까? 이 작업은 되돌릴 수 없습니다."`
  - handleDownload + downloadDocument 호출 (GROUP 1)
  - downloadingIds + deletingIds Set state per-item 잠금 (GROUP 9 추가 가드)
  - toast 모든 한글 카피 verbatim ('다운로드 중입니다…' / '다운로드를 시작했습니다' / '다운로드에 실패했습니다. 네트워크를 확인해주세요.' / '문서를 삭제했습니다' / '문서 목록을 불러오지 못했습니다. 잠시 후 다시 시도해주세요.')

### Step 4 — DocumentUploadForm.tsx 변환

cha-bio-safety/src/components/DocumentUploadForm.tsx 402 lines 전체 교체:

- W6 §1 imports verbatim (line 7~16)
- W6 §2 시그니처 보존:
  - Props (type / onClose)
  - ALLOWED 6 ext + EXT_TO_MIME + MAX_SIZE = 500 * 1024 * 1024 + typeLabel + findAllowed (GROUP 3 verbatim)
  - 7 state (year / title / file / progress / isUploading / error / abortRef / fileInputRef)
  - 2 useEffect (beforeunload + unmount abort)
  - 4 handler (handleFileChange + handleSubmit + handleCancel + handleRetry)
- W6 §3 JSX 변환:
  - 모달 title → text-heading font-semibold (OQ #4 20 → 22 격상) `{label} 업로드`
  - Year select → `<label className="docs-input-label">` + `<select className="docs-input">` (OQ #5 sunken)
  - Title input → `<label>` + `<input className="docs-input">` placeholder verbatim `예: ${currentYear}년 ${label}`
  - File button → `.docs-file-btn` (+ --filled 분기) + hidden file input + `.docs-file-hint` (OQ #3 12 + leading-none) 카피 verbatim 'PDF, XLSX, DOCX, PPTX, HWP, ZIP · 최대 500MB'
  - Progress block → `.docs-progress-bar` + `.docs-progress-fill` (OQ #1 accent + duration-240ms ease-linear) + `.docs-progress-meta` + `.docs-progress-percent` (font-mono) + `.docs-progress-speed`
  - Error block → `.docs-error-block` + `.docs-error-msg` (OQ #4 14 격상 font-medium) + `.docs-error-retry` (OQ #1 accent)
  - Action row → `.btn-primary-gradient` (OQ #2 LOCKED lin-grad submit, canSubmit false 시 disabled) + `.docs-cancel-btn` (또는 `.btn-secondary` 재사용)
- 비즈 anchor verbatim:
  - GROUP 4 empty MIME 조건 `if (f.type && !(entry.mimes as readonly string[]).includes(f.type))` 1 byte 변경 0
  - GROUP 5 runMultipartUpload 호출 시그니처 + contentType fallback 1 byte 변경 0
  - GROUP 6 abortRef + AbortController + AbortError 분기 (err.name === 'AbortError' → '업로드가 취소되었습니다.' + isAbort=true → toast 미발송)
  - GROUP 6 handleCancel window.confirm full verbatim `'업로드를 취소하시겠습니까? 지금까지 전송된 데이터는 저장되지 않습니다.'`
  - GROUP 7 beforeunload returnValue verbatim `'업로드 중입니다. 페이지를 나가면 전송이 중단됩니다.'`
  - GROUP 8 403 카피 verbatim `'관리자만 업로드할 수 있습니다.'`
  - 모든 toast 카피 verbatim ('파일 크기가 500MB를 초과합니다.' / '지원하지 않는 파일 형식입니다. (PDF, XLSX, DOCX, PPTX, HWP, ZIP)' / '연도, 제목, 파일을 모두 입력해주세요.' / '업로드가 완료되었습니다.' / '업로드 중 네트워크 오류가 발생했습니다. 다시 시도해주세요.')

### Step 5 — Build verify (commit 전 강제)

```bash
cd cha-bio-safety && npx tsc --noEmit 2>&1 | head -40
# 기대: 0 errors

cd cha-bio-safety && npm run build 2>&1 | tail -30
# 기대: PASS + DocumentsPage chunk size 보고
```

### Step 6 — 단일 atomic commit

```bash
git add cha-bio-safety/src/pages/DocumentsPage.tsx \
        cha-bio-safety/src/components/DocumentSection.tsx \
        cha-bio-safety/src/components/DocumentUploadForm.tsx \
        cha-bio-safety/src/styles/components.css

git commit -m "$(cat <<'EOF'
feat(redesign/22-documents): W7 TSX 변환 — 4 파일 단일 atomic

W6 checklist (wave-6-tsx-conversion-checklist.md 501 lines) §1~§12 verbatim 적용.

3 파일 변환:
- DocumentsPage.tsx 162 lines — 모바일 탭 + 데스크톱 좌우 2단 + upload sheet/modal shell
- DocumentSection.tsx 517 lines — 4 state + Hero + 과거 이력 + admin Trash2
- DocumentUploadForm.tsx 402 lines — year/title/file + Progress + abort/retry + beforeunload

components.css 신규 17+ class:
- .docs-tab-bar / .docs-tab-btn / .docs-tab-btn-active (OQ #1 LOCKED accent)
- .docs-hero-card / .docs-year-tile / .docs-latest-pill (OQ #3 11→12 + leading-none)
- .docs-history-row / .docs-trash-btn / .docs-upload-btn-mobile/-desktop
- .docs-upload-sheet/-modal + .docs-sheet-handle
- .docs-input / .docs-file-btn / .docs-progress-bar/-fill (OQ #5 sunken + OQ #1 accent)
- .docs-error-block / .btn-primary-gradient (OQ #2 LOCKED lin-grad)
- @keyframes 3종 (docs-slide-up / docs-fade-in / docsec-spin) 인라인 → components.css 이관

비즈 anchor 9 그룹 1 byte 변경 0:
- documentsApi 2종 + useQuery key + ALLOWED 6 ext + empty MIME fallback
- runMultipartUpload + abort/retry + beforeunload + admin 권한
- ★ delete confirm full string verbatim (memory project_legal_findings_delete_incident_260520)

OQ LOCKED 5건 적용:
- #1 chrome 강조색 토큰 통일 (bg-accent)
- #2 submit button lin-grad 135deg #1d4ed8 #0ea5e9 (.btn-primary-gradient)
- #3 최신 pill 11→12 + leading-none (text-caption font-bold leading-none)
- #4 모달 title 20→22 + Error 카피 13→14 + Progress meta 16/14
- #5 var(--bg3)/var(--bg4) → bg-surface-sunken 통일 (4곳)

mirror: 19-legal (6if) / 20-legal-findings (bbz) / 21-legal-finding-detail (lft) / 23-education (4i9) / 28-splash (0803d2f) 5 페이지 → 22-documents 만 multi-file scope (3 파일 1081 lines).

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

  </action>
  <verify>
    <automated>
# Negative gate 17 (W6 §11)
test "$(git diff --name-only HEAD~1 -- cha-bio-safety/src/utils/api.ts cha-bio-safety/src/utils/multipartUpload.ts cha-bio-safety/src/utils/downloadBlob.ts cha-bio-safety/src/hooks/useIsDesktop.ts cha-bio-safety/src/stores/authStore.ts cha-bio-safety/src/App.tsx | wc -l)" = "0" || (echo "FAIL: 외부 의존 수정 감지" && exit 1)

# src 변경 = 4 파일만
test "$(git diff --name-only HEAD~1 -- cha-bio-safety/src/ | wc -l)" = "4" || (echo "FAIL: src 변경 4 파일 아님" && exit 1)

# 이모지 0
grep -P "[\x{1F300}-\x{1F9FF}]|[\x{2600}-\x{27BF}]" cha-bio-safety/src/pages/DocumentsPage.tsx cha-bio-safety/src/components/DocumentSection.tsx cha-bio-safety/src/components/DocumentUploadForm.tsx && (echo "FAIL: 이모지 발견" && exit 1) || true

# fontSize 9·10·11 인라인 0
grep -nE "fontSize:\s*(9|10|11)\b" cha-bio-safety/src/pages/DocumentsPage.tsx cha-bio-safety/src/components/DocumentSection.tsx cha-bio-safety/src/components/DocumentUploadForm.tsx && (echo "FAIL: fontSize 9/10/11 인라인 발견" && exit 1) || true

# status- prefix 0
grep -nE "(text|bg|border)-status-" cha-bio-safety/src/pages/DocumentsPage.tsx cha-bio-safety/src/components/DocumentSection.tsx cha-bio-safety/src/components/DocumentUploadForm.tsx && (echo "FAIL: status- prefix 발견" && exit 1) || true

# w-8 h-8 className 0 (48px 함정)
grep -nE '"[^"]*\bw-8\b[^"]*"|"[^"]*\bh-8\b[^"]*"' cha-bio-safety/src/pages/DocumentsPage.tsx cha-bio-safety/src/components/DocumentSection.tsx cha-bio-safety/src/components/DocumentUploadForm.tsx && (echo "FAIL: w-8/h-8 발견 (48px 함정)" && exit 1) || true

# Positive: 비즈 anchor 9 그룹
grep -q "documentsApi.list" cha-bio-safety/src/components/DocumentSection.tsx || (echo "FAIL: GROUP 1 documentsApi.list 누락" && exit 1)
grep -q "documentsApi.remove" cha-bio-safety/src/components/DocumentSection.tsx || (echo "FAIL: GROUP 1 documentsApi.remove 누락" && exit 1)
grep -q "queryKey: \['documents', type\]" cha-bio-safety/src/components/DocumentSection.tsx || (echo "FAIL: GROUP 2 useQuery key 누락" && exit 1)
grep -qE "\.pdf|\.xlsx|\.docx|\.pptx|\.hwp|\.zip" cha-bio-safety/src/components/DocumentUploadForm.tsx || (echo "FAIL: GROUP 3 ALLOWED 6 ext 누락" && exit 1)
grep -q "500 \* 1024 \* 1024" cha-bio-safety/src/components/DocumentUploadForm.tsx || (echo "FAIL: GROUP 3 MAX_SIZE 누락" && exit 1)
grep -q "f.type && !(entry.mimes" cha-bio-safety/src/components/DocumentUploadForm.tsx || (echo "FAIL: GROUP 4 empty MIME fallback 누락" && exit 1)
grep -q "runMultipartUpload" cha-bio-safety/src/components/DocumentUploadForm.tsx || (echo "FAIL: GROUP 5 runMultipartUpload 누락" && exit 1)
grep -q "abortRef" cha-bio-safety/src/components/DocumentUploadForm.tsx || (echo "FAIL: GROUP 6 abortRef 누락" && exit 1)
grep -q "beforeunload" cha-bio-safety/src/components/DocumentUploadForm.tsx || (echo "FAIL: GROUP 7 beforeunload 누락" && exit 1)
grep -q "useAuthStore" cha-bio-safety/src/components/DocumentSection.tsx || (echo "FAIL: GROUP 8 useAuthStore 누락" && exit 1)

# ★ delete confirm full string verbatim
grep -q "정말 삭제하시겠습니까? 이 작업은 되돌릴 수 없습니다" cha-bio-safety/src/components/DocumentSection.tsx || (echo "FAIL: GROUP 9 delete confirm verbatim 누락" && exit 1)

# beforeunload returnValue verbatim
grep -q "업로드 중입니다. 페이지를 나가면 전송이 중단됩니다" cha-bio-safety/src/components/DocumentUploadForm.tsx || (echo "FAIL: beforeunload verbatim 누락" && exit 1)

# abort confirm verbatim
grep -q "업로드를 취소하시겠습니까? 지금까지 전송된 데이터는 저장되지 않습니다" cha-bio-safety/src/components/DocumentUploadForm.tsx || (echo "FAIL: abort confirm verbatim 누락" && exit 1)

# 403 카피 verbatim
grep -q "관리자만 업로드할 수 있습니다" cha-bio-safety/src/components/DocumentUploadForm.tsx || (echo "FAIL: 403 카피 verbatim 누락" && exit 1)

# typeLabel verbatim
grep -q "소방계획서" cha-bio-safety/src/components/DocumentSection.tsx && grep -q "소방훈련자료" cha-bio-safety/src/components/DocumentSection.tsx || (echo "FAIL: typeLabel verbatim 누락" && exit 1)
grep -q "소방계획서" cha-bio-safety/src/components/DocumentUploadForm.tsx && grep -q "소방훈련자료" cha-bio-safety/src/components/DocumentUploadForm.tsx || (echo "FAIL: Form typeLabel verbatim 누락" && exit 1)

# components.css 신규 class ≥15
test "$(grep -cE '^\s*\.docs-' cha-bio-safety/src/styles/components.css)" -ge 15 || (echo "FAIL: .docs-* 신규 class <15" && exit 1)

# .btn-primary-gradient (OQ #2 LOCKED)
grep -q ".btn-primary-gradient" cha-bio-safety/src/styles/components.css || (echo "FAIL: .btn-primary-gradient 누락 (OQ #2)" && exit 1)
grep -q "linear-gradient(135deg, #1d4ed8, #0ea5e9)" cha-bio-safety/src/styles/components.css || (echo "FAIL: OQ #2 lin-grad verbatim 누락" && exit 1)

# @keyframes 3종
grep -q "@keyframes docs-slide-up" cha-bio-safety/src/styles/components.css || (echo "FAIL: docs-slide-up 누락" && exit 1)
grep -q "@keyframes docs-fade-in" cha-bio-safety/src/styles/components.css || (echo "FAIL: docs-fade-in 누락" && exit 1)
grep -q "@keyframes docsec-spin" cha-bio-safety/src/styles/components.css || (echo "FAIL: docsec-spin 누락" && exit 1)

# Build PASS
cd cha-bio-safety && npx tsc --noEmit 2>&1 | tee /tmp/22-tsc.log && test ! -s /tmp/22-tsc.log -o "$(grep -c 'error TS' /tmp/22-tsc.log)" = "0" || (echo "FAIL: tsc 0 errors 아님" && exit 1)

cd cha-bio-safety && npm run build 2>&1 | tee /tmp/22-build.log && grep -q "built in" /tmp/22-build.log || (echo "FAIL: build 실패" && exit 1)

# DocumentsPage chunk size 보고
grep -E "DocumentsPage|DocumentSection|DocumentUploadForm" /tmp/22-build.log || echo "WARN: chunk size 보고 없음 (lazy split 안 됐을 수 있음)"

# wrangler / npm run deploy 금지 (workspace 룰)
test "$(git log -1 --format=%B HEAD | grep -cE 'wrangler|npm run deploy')" = "0" || (echo "FAIL: wrangler/deploy 명령 commit message 포함" && exit 1)

echo "ALL GATES PASS — 4 파일 단일 atomic commit OK"
    </automated>
  </verify>
  <done>
- 4 파일 단일 atomic commit 1개 (git log HEAD = feat(redesign/22-documents): W7 TSX 변환)
- src 변경 = DocumentsPage.tsx + DocumentSection.tsx + DocumentUploadForm.tsx + components.css 4개만
- 외부 의존 (utils/api / utils/multipartUpload / utils/downloadBlob / hooks/useIsDesktop / stores/authStore / App.tsx) 0 byte 변경
- W6 §4 비즈 anchor 9 그룹 모두 verbatim 보존
- W6 §5 OQ LOCKED 5건 적용 (accent / lin-grad / 12 leading-none / 22 격상 / sunken 통일)
- ★ delete confirm + beforeunload + abort confirm + 403 카피 4건 verbatim
- components.css 신규 .docs-* class ≥15 + .btn-primary-gradient + @keyframes 3종
- npx tsc --noEmit 0 errors / npm run build PASS
- 이모지 0 / fontSize 9·10·11 인라인 0 / status- prefix 0 / w-8 h-8 className 0
- wrangler / npm run deploy 명령 실행 0 (workspace 룰)
  </done>
</task>

</tasks>

<verification>
## 전체 phase verify (T1 종료 직후)

1. **Git history check:**
   ```bash
   git log --oneline -2
   # 기대: HEAD = feat(redesign/22-documents): W7 TSX 변환 — 4 파일 단일 atomic
   #       HEAD~1 = W6 SUMMARY 커밋 (55ec391c)
   ```

2. **4 파일 변경 확인:**
   ```bash
   git diff --name-only HEAD~1 HEAD
   # 기대 (정확히 4 line):
   # cha-bio-safety/src/components/DocumentSection.tsx
   # cha-bio-safety/src/components/DocumentUploadForm.tsx
   # cha-bio-safety/src/pages/DocumentsPage.tsx
   # cha-bio-safety/src/styles/components.css
   ```

3. **외부 의존 미변경:**
   ```bash
   git diff --name-only HEAD~1 HEAD -- cha-bio-safety/src/utils/ cha-bio-safety/src/hooks/ cha-bio-safety/src/stores/ cha-bio-safety/src/App.tsx
   # 기대: 0 line
   ```

4. **Build 최종 확인:**
   ```bash
   cd cha-bio-safety && npm run build
   # 기대: built in ≈Ns (PASS)
   ```

5. **본 wave 산출 4 파일 + 새 컨텍스트 SUMMARY 1 = 다음 commit (SUMMARY commit) 후보**
</verification>

<success_criteria>
- 4 파일 atomic commit 1개 생성
- W6 checklist §1~§12 모든 verbatim 적용 (negative 17 + positive 9 anchor + OQ LOCKED 5)
- tsc + build PASS
- 외부 의존 6 파일 (utils/api / utils/multipartUpload / utils/downloadBlob / hooks/useIsDesktop / stores/authStore / App.tsx) 0 byte 변경
- ★ delete confirm + beforeunload + abort + 403 카피 4건 verbatim
- components.css 신규 ≥15 class + .btn-primary-gradient + @keyframes 3종
- 이모지 / fontSize 9·10·11 / status- prefix / w-8 h-8 className 모두 0
</success_criteria>

<output>
After completion, create `.planning/quick/260526-9bv-redesign-22-documents-tsx-3-atomic/260526-9bv-SUMMARY.md` then commit SUMMARY:

```bash
git add .planning/quick/260526-9bv-redesign-22-documents-tsx-3-atomic/260526-9bv-SUMMARY.md
git commit -m "docs(quick-260526-9bv): redesign/22-documents W7 TSX 변환 SUMMARY

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

총 commit 2개:
1. feat(redesign/22-documents): W7 TSX 변환 — 4 파일 단일 atomic
2. docs(quick-260526-9bv): redesign/22-documents W7 TSX 변환 SUMMARY
</output>
