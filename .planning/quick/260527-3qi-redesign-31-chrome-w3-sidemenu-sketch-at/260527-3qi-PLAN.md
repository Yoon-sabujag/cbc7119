---
phase: quick-260527-3qi
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cha-bio-safety/docs/redesign-context/31-chrome/sketch-wave-3-side-menu.html
autonomous: true
requirements:
  - REDESIGN-31-CHROME-W3
  - SIDE-MENU-DARK-LIGHT-DUAL
  - SIDE-MENU-NOAN-PARTIAL-UPGRADE
  - SIDE-MENU-BIZ-ANCHOR-PRESERVE
  - SIDE-MENU-AVATAR-GRADIENT-KEEP
  - SIDE-MENU-PANEL-DIMENSION-LOCKED
  - SIDE-MENU-CLOSE-X-LUCIDE-DEFAULT
  - SIDE-MENU-UNRESOLVED-FIRE-BAR
tags:
  - redesign
  - chrome
  - side-menu
  - sketch
  - 31-chrome
  - dark-light

must_haves:
  truths:
    - "단일 sketch HTML 파일 1개 (sketch-wave-3-side-menu.html) 가 cha-bio-safety/docs/redesign-context/31-chrome/ 평면에 존재 (sketch/ 서브폴더 아님)"
    - "다크 + 라이트 2 변형 모두 sketch 안에 시연 (OQ #1 LOCKED) — :root 다크 + [data-theme=light] 라이트 토큰 정의 + body 안 4~6 frame 중 최소 2 frame 이 data-theme=light"
    - "비즈 anchor 박스 (sketch HTML 머리 주석 안) 가 W3 핵심 식별자 20+ 박제 — Props 인터페이스 / NAV_H / MENU 상수 5섹션 17 아이템 (label/path/role/desktopOnly verbatim) / ITEM_META / RAW_TO_LABEL 4매핑 / todayShiftLabel + getMonthlySchedule / body scroll lock useEffect + #side-menu-panel hook / go(path) / 오버레이 0.28s + panel 0.3s cubic-bezier / panel width 82% maxWidth 300 / borderRadius '0 16px 16px 0' / divider+item+soon+badge 4 분기 / badgeCount /remediation 분기 / 아바타 그라데이션 / staff.name 이니셜 fallback"
    - "OQ #2 LOCKED 부분 격상 — 9·10·11px 위반 7건 각각의 격상 결정이 sketch 머리 주석 격상 표에 명시 (부제 9.5→12 / divider 9→11 / soon 10→12 / item 12.5→16 / badge 11→13~14 / 사용자 이름 11.5→14~16 / 사용자 부제 9.5→12)"
    - "body markup 안 9·10·11px 실사용 0건 (단 :root 정의 / 비즈 anchor 박스 / 격상 표 / 토큰 매핑 표 doc context 안 인용은 예외)"
    - "OQ #3 LOCKED 사용자 아바타 그라데이션 linear-gradient(135deg,#1d4ed8,#0ea5e9) 보존 (line 189 verbatim 매치, §6.4 매치 ✓)"
    - "W3 추가 OQ 2건 default 답 + LOCKED placeholder 머리 주석 명시 — ✕ 텍스트 → Lucide X 교체 (default) / 미조치 badge → --status-fire-bar 주황 (memory feedback_inspection_unresolved_color)"
    - "panel width 82% maxWidth 300 + borderRadius '0 16px 16px 0' 변경 0 (OQ #7 LOCKED)"
    - "MENU 상수 5섹션 17 아이템 1 byte 변경 0 (label/path/badge/soon/role/desktopOnly verbatim — MenuSettingsSection 의 source)"
    - "legacy alias --bg2/--bg3/--bg4/--bd/--t1/--t2/--t3/--danger 가 body markup 안에서 0건 (모두 v0.1.1 의미 토큰으로 교체 — surface-raised/sunken/active/border-default/text-primary/secondary/tertiary/status-fire-bar). 단 :root 호환 alias 블록 안 정의 + 비즈 anchor 박스 + 토큰 매핑 표 doc context 안 인용은 예외"
    - "이모지 0건 (✕ U+2715 글리프도 negative — default Lucide X 교체)"
    - "status-* 클래스 prefix 0건 (memory feedback_tailwind_token_class_pattern — bg-fire-bar O / bg-status-fire-bar X). 단 var(--status-...) CSS 변수 이름은 OK (토큰 정의)"
    - "linear-gradient 1건만 — 사용자 아바타 OQ #3 보존 (body markup 안). 기타 그라데이션 0건"
    - "src/components/SideMenu.tsx + App.tsx + tokens.css + typography.css + design-system.md + tailwind.config.js + 31-chrome/wave-1-index.md + 31-chrome 7 스냅샷 + 31-chrome/sketch-wave-2-global-header.html + 00-design-context/ 모두 1 byte 변경 0"
    - "atomic 단일 commit — sketch HTML 1 파일 추가만 stage / commit"
    - "wrangler 명령 + npm run deploy 둘 다 호출 0"

  artifacts:
    - path: "cha-bio-safety/docs/redesign-context/31-chrome/sketch-wave-3-side-menu.html"
      provides: "SideMenu (햄버거 슬라이드 드로어 본체, 201 라인) 의 W3 sub-wave sketch — 다크/라이트 + 4~6 frame + 비즈 anchor 박스 + 격상 표 + 토큰 매핑 + W3 추가 OQ 2건"
      min_lines: 600

  key_links:
    - from: "sketch-wave-3-side-menu.html"
      to: "wave-1-index.md §1.3 [SideMenu.tsx] 박스 + §2.3 W3 행 + §7 OQ #1/2/3/5/7 LOCKED 답"
      via: "비즈 anchor 박스 verbatim 인용 + 격상 표 7건 + W3 추가 OQ default 답"
      pattern: "head comment 안 OQ # 매트릭스 박스"
    - from: "sketch-wave-3-side-menu.html"
      to: "sketch-wave-2-global-header.html (W2 precedent)"
      via: "동일 31-chrome 트랙, 동일 :root + [data-theme=light] 정의 패턴 + frame-mobile 480 wrapper + decision-summary/biz-anchors/negative-gate/upgrade-table 4 박스 패턴 mirror"
      pattern: "<style> :root + [data-theme=\"light\"] + .frame-mobile + .decision-summary 클래스"
    - from: "sketch-wave-3-side-menu.html 사용자 아바타"
      to: "src/components/SideMenu.tsx line 189"
      via: "linear-gradient(135deg,#1d4ed8,#0ea5e9) 1 byte 일치 (OQ #3 LOCKED)"
      pattern: "linear-gradient\\(135deg,#1d4ed8,#0ea5e9\\)"
    - from: "sketch-wave-3-side-menu.html MENU 상수 박제"
      to: "src/components/SideMenu.tsx line 19~54"
      via: "5섹션 17 아이템 label/path/role/desktopOnly verbatim — MenuSettingsSection 의 import source"
      pattern: "section: '주요 기능'"
---

<objective>
redesign/31-chrome 의 W3 sub-wave — SideMenu (햄버거 슬라이드 드로어 본체, src/components/SideMenu.tsx 201 라인) 단일 컴포넌트 sketch HTML 1 파일 atomic 1-commit 산출.

**왜:** chrome 4 컴포넌트 통합 1558 라인 중 SideMenu (201 라인) 는 W2 GlobalHeader (45) 다음으로 작고 chrome 통일 정의 페이지 — 사용자가 매일 보는 햄버거 드로어. 30 페이지 모바일 모두에 영향. wave-1-index.md §7 OQ #1/#2/#3/#5/#7 LOCKED 5건 + W3 추가 OQ 2건 (✕ Lucide 교체 / 미조치 badge fire 색) 의 첫 시각 검증 산출물.

**Output:** `cha-bio-safety/docs/redesign-context/31-chrome/sketch-wave-3-side-menu.html` 단일 파일 (예상 600~900 라인).

**비즈 anchor 보존 절대 룰:** sketch HTML 머리 주석 안 박제만 — src/components/SideMenu.tsx 1 byte 변경 0. MENU 상수 5섹션 17 아이템 / Props 인터페이스 / NAV_H 상수 / RAW_TO_LABEL 4매핑 / body scroll lock useEffect / panel id="side-menu-panel" / 오버레이 0.28s + panel 0.3s cubic-bezier 등 W7 TSX 변환에서 1 byte 변경 0 강제 anchor 가 sketch 머리 주석에 verbatim 박제되어야 W7 verify gate 통과.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md
@./CLAUDE.local.md

@cha-bio-safety/docs/redesign-context/31-chrome/wave-1-index.md
@cha-bio-safety/docs/redesign-context/31-chrome/SideMenu.tsx
@cha-bio-safety/docs/redesign-context/31-chrome/sketch-wave-2-global-header.html
@cha-bio-safety/docs/redesign-context/31-chrome/design-system.md
@cha-bio-safety/docs/redesign-context/31-chrome/tokens.css
@cha-bio-safety/docs/redesign-context/31-chrome/typography.css
@cha-bio-safety/docs/redesign-context/30-not-found/sketch.html
@cha-bio-safety/src/App.tsx
@cha-bio-safety/src/utils/api.ts
@cha-bio-safety/tailwind.config.js

<interfaces>
<!-- W3 변환 시 1 byte 변경 0 강제 anchor — sketch HTML 머리 주석 박제 필수 -->

From cha-bio-safety/docs/redesign-context/31-chrome/SideMenu.tsx (201 lines):

```typescript
// line 1~7 — imports
import { useEffect, useMemo, useState } from 'react'
import { useNavigate } from 'react-router-dom'
import { useQuery } from '@tanstack/react-query'
import { useAuthStore } from '../stores/authStore'
import { getMonthlySchedule } from '../utils/shiftCalc'
import { useStaffList } from '../hooks/useStaffList'
import { settingsApi, type SideMenuEntry, type MenuConfig } from '../utils/api'

// line 9 — NAV_H 상수 (BottomNav 위 panel bottom 계산용)
const NAV_H = 'calc(54px + var(--sab, 0px))'

// line 11~15 — Props interface
interface Props {
  open: boolean
  onClose: () => void
  unresolvedCount?: number
}

// line 17 — MenuItem type
export type MenuItem = { label: string; path: string; badge: number; soon: boolean; role?: 'admin' | 'assistant'; desktopOnly?: boolean }

// line 19~54 — MENU 상수 5섹션 17 아이템 (label/path/role/desktopOnly verbatim — MenuSettingsSection import source)
export const MENU: { section: string; items: MenuItem[] }[] = [
  { section: '주요 기능', items: [
    { label: '대시보드',    path: '/dashboard',      badge: 0, soon: false },
    { label: '일반 점검',   path: '/inspection',     badge: 0, soon: false },
    { label: 'QR 스캔',    path: '/inspection/qr',  badge: 0, soon: false },
    { label: '조치 관리',   path: '/remediation',    badge: 0, soon: false },
    { label: '승강기 관리', path: '/elevator',       badge: 0, soon: false },
  ]},
  { section: '시설 관리', items: [
    { label: 'DIV 압력 관리',   path: '/div',           badge: 0, soon: false },
    { label: '소화기 관리',      path: '/extinguishers', badge: 0, soon: false, desktopOnly: true },
    { label: 'CCTV 현황',       path: '/cctv',          badge: 0, soon: false, desktopOnly: true },
    { label: '소방 시설 도면',   path: '/floorplan',     badge: 0, soon: false },
    { label: '소방 점검 관리',   path: '/legal',         badge: 0, soon: false },
    { label: '소방 시설 추가',  path: '/checkpoints',   badge: 0, soon: false, role: 'admin' },
  ]},
  { section: '문서 관리', items: [
    { label: '일일 업무 일지',   path: '/daily-report',  badge: 0, soon: false },
    { label: '업무 수행 기록표', path: '/worklog', badge: 0, soon: false, role: 'admin' },
    { label: '월간 점검 계획', path: '/schedule',      badge: 0, soon: false },
    { label: '월간 출근부',   path: '/workshift',      badge: 0, soon: false },
    { label: '연간 업무 추진 계획', path: '/annual-plan', badge: 0, soon: false },
    { label: '소방계획서/훈련자료', path: '/documents', badge: 0, soon: false },
    { label: '점검 일지 출력', path: '/reports',        badge: 0, soon: false },
    { label: 'QR 코드 출력',  path: '/qr-print',      badge: 0, soon: false },
  ]},
  { section: '근무·복지', items: [
    { label: '연차 및 식사', path: '/staff-service',  badge: 0, soon: false },
    { label: '보수교육',    path: '/education',      badge: 0, soon: false },
  ]},
  { section: '시스템', items: [
    { label: '직원 관리', path: '/staff-manage', badge: 0, soon: false, role: 'admin' },
  ]},
]

// line 56~57 — ITEM_META (path → MenuItem lookup)
const ITEM_META: Record<string, MenuItem> = {}
MENU.forEach(s => s.items.forEach(i => { ITEM_META[i.path] = i }))

// line 59 — export
export function SideMenu({ open, onClose, unresolvedCount = 0 }: Props) {

// line 63 — queryKey 'menu-config' (MenuSettingsSection 공유 — qc.invalidateQueries 동기화)
const { data: menuConfig } = useQuery({ queryKey: ['menu-config'], queryFn: () => settingsApi.getMenu(), staleTime: 300_000 })

// line 66~70 — appliedEntries (Phase 18 D-05 평면 SideMenuEntry[])
const appliedEntries: SideMenuEntry[] = useMemo(() => {
  const cfg = menuConfig as MenuConfig | undefined
  const entries = cfg?.sideMenu ?? []
  return entries.length > 0 ? entries : []
}, [menuConfig])

// line 72 — RAW_TO_LABEL (당직/비번/주간/연차 매핑)
const RAW_TO_LABEL: Record<string, string> = { '당':'당직', '비':'비번', '주':'주간', '휴':'연차' }

// line 73 — todayShiftLabel state
const [todayShiftLabel, setTodayShiftLabel] = useState('평일주간고정')

// line 74~86 — todayShiftLabel useEffect (8:30am 분기, getMonthlySchedule 의존)
// line 89~103 — body scroll lock useEffect (touchmove preventDefault, #side-menu-panel 외 only)
// line 105 — go(path) handler
const go = (path: string) => { navigate(path); onClose() }

// line 110~118 — 오버레이 div (position fixed inset 0 zIndex 190 rgba(0,0,0,0.65) opacity transition 0.28s)
// line 122~133 — panel div (id="side-menu-panel", width 82% maxWidth 300, transform translateX cubic-bezier(.4,0,.2,1) 0.3s, borderRadius '0 16px 16px 0')
// line 135~142 — 헤더 (로고 30×30 radius 8 /icons/icon-192.png + 타이틀 "차바이오컴플렉스" 13/700 + 부제 "소방안전 통합관리" 9.5/var(--t3) + ✕ 닫기 28×28 fontSize 15)
// line 145~184 — 메뉴 리스트 render (divider 9 letterSpacing .08em uppercase / item 12.5/500 / soon 배지 10 / badgeCount danger 11 JetBrains Mono 99+ cap)
// line 187~197 — 사용자 카드 (아바타 28×28 linear-gradient(135deg,#1d4ed8,#0ea5e9) + 이름 11.5/700 + 직책+shift 9.5)
```

From cha-bio-safety/src/App.tsx (mount + unresolvedCount source):

```typescript
// line 8 — import
import { SideMenu } from './components/SideMenu'

// line 120 — sideOpen state
const [sideOpen, setSideOpen] = useState(false)

// line 130 — unresolvedCount = dashData?.stats?.unresolved ?? 0
const unresolvedCount = dashData?.stats?.unresolved ?? 0

// line 217~220 — SideMenu mount (모바일 전용)
{/* 모바일 전용: SideMenu 드로어 */}
{!isDesktop && showNav && (
  <SideMenu open={sideOpen} onClose={() => setSideOpen(false)} unresolvedCount={unresolvedCount} />
)}
```

From cha-bio-safety/docs/redesign-context/31-chrome/tokens.css (W3 적용 토큰):

```css
/* 다크 (기본) */
:root, [data-theme="dark"] {
  --surface-raised:  #1a1f27;   /* panel bg */
  --surface-sunken:  #232a33;   /* 닫기 btn bg / 사용자 카드 bg / soon 배지 bg */
  --surface-active:  #2c333d;   /* item hover bg */
  --surface-overlay: rgba(0, 0, 0, 0.6);   /* 모달 dimmer */

  --text-primary:    #e6edf3;   /* item label / 헤더 타이틀 / 사용자 이름 */
  --text-secondary:  #adb6c0;   /* 닫기 btn 색 */
  --text-tertiary:   #8b949e;   /* divider / soon / 부제 / 사용자 직책+shift */
  --text-on-accent:  #ffffff;   /* 아바타 텍스트 / badge 텍스트 */

  --border-default:  rgba(255, 255, 255, 0.14);   /* 헤더 + 사용자 카드 영역 borderTop */

  --status-fire-bar:    #f97316;   /* 미조치 unresolvedCount badge (W3 추가 OQ default) */
  --status-danger-bar:  #ef4444;   /* alternative — 사용자 결정 sub-OQ */

  --radius-sm:   8px;   /* 로고 + 닫기 + item + soon */
  --radius-md:   12px;
  --radius-lg:   16px;  /* panel borderRadius '0 16px 16px 0' */
}

/* 라이트 (OQ #1 LOCKED 양쪽 지원) */
[data-theme="light"] {
  --surface-raised:  #f6f8fa;
  --surface-sunken:  #ebeef1;
  --surface-active:  #d8dee4;
  --surface-overlay: rgba(0, 0, 0, 0.5);   /* 라이트 오버레이도 어두운 색 유지 */

  --text-primary:    #1f2328;
  --text-secondary:  #4d5562;
  --text-tertiary:   #656d76;

  --border-default:  rgba(0, 0, 0, 0.14);

  --status-fire-bar:    #c2410c;   /* 라이트 fire 채도 진한 톤 */
  --status-danger-bar:  #b91c1c;
}
```

From cha-bio-safety/src/utils/api.ts (type 시그니처):

```typescript
export type SideMenuEntry =
  | { type: 'divider'; id: string; title: string }
  | { type: 'item'; path: string; visible: boolean }

export interface MenuConfig {
  sideMenu: SideMenuEntry[]
}

export interface NotificationPreferences { ... }

export const settingsApi = {
  getMenu: async (): Promise<MenuConfig> => req<MenuConfig>('/settings/menu'),
  saveMenu: async (config: MenuConfig): Promise<void> => ...
}

export const DEFAULT_SIDE_MENU: SideMenuEntry[] = [ /* 백업 fallback */ ]
```
</interfaces>

</context>

<tasks>

<task type="auto">
  <name>Task 1: SideMenu W3 sub-wave sketch HTML atomic 1 파일 작성 + 1-commit</name>
  <files>cha-bio-safety/docs/redesign-context/31-chrome/sketch-wave-3-side-menu.html</files>
  <action>
**산출:** `cha-bio-safety/docs/redesign-context/31-chrome/sketch-wave-3-side-menu.html` 단일 파일 (예상 600~900 라인).

**파일 작성 룰 — Write tool 만 사용 (heredoc/cat 금지)**

---

## §1. 머리 주석 (HTML `<!-- -->`) — 100~250 라인

W2 sketch (`sketch-wave-2-global-header.html` line 7~145) 의 머리 주석 구조를 정확히 mirror. 다음 6 박스 포함:

### 박스 A — 메타 (제목/작성/범위/out-of-scope/참조)

```
sketch-wave-3-side-menu.html — redesign/31-chrome W3 sub-wave
작성: 2026-05-27 / quick 260527-3qi
범위: SideMenu.tsx (201 라인) 단일 컴포넌트 sketch
      다크 + 라이트 2 변형 + 패널 닫힘/열림/사용자 카드 줌/badge 시연 = 4~6 frame 매트릭스
out of scope:
  - src/components/SideMenu.tsx 수정 (W7 책임 — 1 byte 변경 0 강제)
  - App.tsx / tokens.css / typography.css / design-system.md / tailwind.config.js 1 byte 변경 0
  - GlobalHeader / SettingsPanel / MenuSettingsSection sketch (W2 완결 / W4/W5 책임)
  - 31-chrome/wave-1-index.md 갱신 (W1 완결됨)
  - 31-chrome/ 7 스냅샷 / sketch-wave-2-global-header.html / 00-design-context/ 1 byte 변경 0
  - wrangler 명령 / npm run deploy (CLAUDE.local.md + memory feedback_cbc7119_design_never_wrangler)
참조: 31-chrome/sketch-wave-2-global-header.html (W2 동일 트랙 precedent — :root + [data-theme=light] + .frame-mobile + .decision-summary 4 박스 패턴 mirror)
      30-not-found/sketch.html (다크/라이트 양쪽 precedent)
      design-system.md v0.1.1 §1.1/§1.2/§1.3/§6.4/§7.1
      tokens.css / typography.css / wave-1-index.md §7 OQ LOCKED 8건 + §1.3 [SideMenu.tsx] 박스
```

### 박스 B — 비즈 anchor 22건 (★ 핵심 — W7 1 byte 변경 0 강제)

`<interfaces>` 섹션의 SideMenu.tsx 발췌를 verbatim 인용 (이 섹션이 sketch HTML 머리 주석에 그대로 들어가야 함). 다음 22건 모두 포함 (Markdown numbered list 형식):

```
1. Props interface { open: boolean; onClose: () => void; unresolvedCount?: number } (line 11~15)
2. NAV_H = 'calc(54px + var(--sab, 0px))' 상수 (line 9) — BottomNav 위 panel bottom 계산
3. MENU 상수 5섹션 17 아이템 verbatim (line 19~54) — label/path/badge/soon/role/desktopOnly,
   MenuSettingsSection.tsx line 11 에서 import — 변경 시 양방향 깨짐
   - 주요 기능 5: 대시보드 /dashboard, 일반 점검 /inspection, QR 스캔 /inspection/qr,
     조치 관리 /remediation, 승강기 관리 /elevator
   - 시설 관리 6: DIV 압력 관리 /div, 소화기 관리 /extinguishers desktopOnly,
     CCTV 현황 /cctv desktopOnly, 소방 시설 도면 /floorplan, 소방 점검 관리 /legal,
     소방 시설 추가 /checkpoints admin
   - 문서 관리 8: 일일 업무 일지 /daily-report, 업무 수행 기록표 /worklog admin,
     월간 점검 계획 /schedule, 월간 출근부 /workshift, 연간 업무 추진 계획 /annual-plan,
     소방계획서/훈련자료 /documents, 점검 일지 출력 /reports, QR 코드 출력 /qr-print
   - 근무·복지 2: 연차 및 식사 /staff-service, 보수교육 /education
   - 시스템 1: 직원 관리 /staff-manage admin
4. ITEM_META: Record<string, MenuItem> (line 56~57) — MENU.forEach 평면 path lookup
5. export type MenuItem = { label / path / badge / soon / role? / desktopOnly? } (line 17)
6. useNavigate / useAuthStore / useStaffList / useQuery import (line 1~7) — 변경 금지
7. useQuery({ queryKey: ['menu-config'], queryFn: settingsApi.getMenu, staleTime: 300_000 })
   (line 63) — MenuSettingsSection 와 동일 queryKey 공유
8. appliedEntries: SideMenuEntry[] useMemo (line 66~70) — cfg?.sideMenu ?? []
9. RAW_TO_LABEL: { '당':'당직', '비':'비번', '주':'주간', '휴':'연차' } (line 72)
10. todayShiftLabel state 'default 평일주간고정' (line 73)
11. todayShiftLabel useEffect (line 74~86) — getMonthlySchedule(year, month, staffForCalc),
    8:30am 분기 (now.getHours() < 8 || (==8 && minutes < 30) → 어제 ref)
12. body scroll lock useEffect (line 89~103) —
    document.body.style.overflow = 'hidden' + touchmove preventDefault,
    panel = document.getElementById('side-menu-panel') 외부만 차단,
    cleanup return — overflow 복원 + removeEventListener
    (memory feedback_body_scroll_lock_safe_area — body position:fixed 안 함)
13. go(path) handler (line 105) — navigate(path); onClose()
14. 오버레이: position fixed inset 0 zIndex 190 background rgba(0,0,0,0.65)
    opacity open ? 1 : 0 pointerEvents open ? 'all' : 'none'
    transition 'opacity 0.28s' (line 110~118)
15. panel id="side-menu-panel" (line 123) — touchmove 차단 hook 식별자
16. panel position fixed top var(--sat, 0px) bottom calc(54px + var(--sab, 0px) - var(--sat, 0px))
    left 0 zIndex 200, width 82% maxWidth 300, background var(--bg2)
    transform open ? translateX(0) : translateX(-100%)
    transition 'transform 0.3s cubic-bezier(.4,0,.2,1)'
    display flex flexDirection column overflow hidden
    borderRadius '0 16px 16px 0' (line 121~133) — OQ #7 LOCKED 보존
17. 헤더: padding '12px 15px' borderBottom 1px var(--bd) flexShrink 0,
    로고 /icons/icon-192.png 30×30 radius 8,
    타이틀 "차바이오컴플렉스" fontSize 13 fontWeight 700,
    부제 "소방안전 통합관리" fontSize 9.5 var(--t3) marginTop 1 (위반 → 격상),
    ✕ 닫기 28×28 radius 7 var(--bg3) var(--t2) fontSize 15 (line 135~142)
18. divider render: padding '9px 13px 2px' fontSize 9 fontWeight 700 var(--t3)
    letterSpacing '.08em' textTransform uppercase (line 149) — 위반 → 격상
19. item render: padding '9px 13px' margin '1px 7px' borderRadius 8 fontSize 12.5 fontWeight 500
    color var(--t1) hover bg var(--bg4) transition 'background 0.13s' (line 163/175)
20. soon 배지: opacity 0.5 cursor default pointerEvents none,
    label 12.5, 배지 fontSize 10 var(--t3) var(--bg3) radius 6 padding '2px 7px' "준비중" (line 164)
21. badgeCount = path === '/remediation' ? unresolvedCount : meta.badge,
    badgeCount > 0 → bg var(--danger) color #fff fontSize 11 fontWeight 700
    fontFamily 'JetBrains Mono' padding '2px 4px' borderRadius 9 minWidth 16 textAlign center,
    99+ cap (line 176~180) — W3 추가 OQ #2: --danger → --status-fire-bar (default)
22. 사용자 카드 (line 187~197):
    padding '9px 11px' borderTop 1px var(--bd) flexShrink 0,
    안쪽 row padding '8px 10px' bg var(--bg3) borderRadius 9 gap 9,
    아바타 28×28 radius 50% background 'linear-gradient(135deg,#1d4ed8,#0ea5e9)'
    fontSize 11 fontWeight 700 color #fff,
    이름 fontSize 11.5 fontWeight 700 — fallback {staff?.name?.[0] ?? '?'},
    직책+shift "{staff?.title} · {todayShiftLabel}" fontSize 9.5 var(--t3) — 모두 위반 → 격상
```

### 박스 C — OQ LOCKED 8건 매트릭스 (wave-1-index.md §7) + W3 추가 OQ 2건

W2 sketch line 54~94 의 매트릭스 패턴 mirror. 각 OQ:
- `default 답:` (인덱스 §7 verbatim)
- `LOCKED 답:` (W1 사용자 결정 — 인덱스에는 LOCKED placeholder, 본 plan scope 의 LOCKED 답 적용)
- `본 W3 적용:` 또는 `본 W3 무관:` 표기

```
OQ #1: 다크 + 라이트 양쪽 지원
  default: 다크 단일 유지
  LOCKED: 다크 + 라이트 양쪽 지원
  본 W3 적용: :root 다크 + [data-theme="light"] 라이트 둘 다 정의.
              4~6 frame 중 최소 2 frame data-theme="light" 부착.

OQ #2: 9·10·11 위반 — 일괄 12 격상 vs 부분 절충
  default: 부분 절충
  LOCKED: 부분 절충 (SideMenu 의 노안 마지노선 16 격상은 본 W3 핵심)
  본 W3 적용 격상 표 7건 (박스 D 참조):
    부제 9.5 → 12 (caption)
    divider 9 → 11 (uppercase letterSpacing 유지)
    soon 10 → 12 (caption)
    item 12.5 → 16 (text-body, 노안 마지노선)
    badge 11 → 13 (label) 또는 14 (body-sm) — sketch 선택
    사용자 이름 11.5 → 14 (body-sm) 또는 16 (text-body) — sketch 선택
    사용자 부제 9.5 → 12 (caption)
  단 panel 세로 overflow 발생 시 dense layout 회귀 sub-OQ 가능 — sketch 결과 본 후
  사용자 결정.

OQ #3: SideMenu 사용자 카드 아바타 그라데이션 유지
  default: 유지
  LOCKED: 유지 (§6.4 매치 OK — 저장/CTA 색 #1d4ed8 → #0ea5e9 정확 매치)
  본 W3 적용: 사용자 아바타 28×28 background linear-gradient(135deg,#1d4ed8,#0ea5e9)
              verbatim 보존. body markup 안 1건 (sketch negative gate
              "linear-gradient 0건" 예외).

OQ #4: SettingsPanel 프로필 그라데이션 폐기
  LOCKED: 폐기 → bg-accent solid
  본 W3 무관 (SettingsPanel W4 책임)

OQ #5: 인라인 svg ↔ Lucide 일관성
  LOCKED: 인라인 svg 유지 (header 햄버거 / back / settings cog)
  본 W3 부분 적용 — SideMenu 의 ✕ U+2715 글리프는 svg 아님 (텍스트 글리프) →
  W3 추가 OQ A (아래) 별도 결정.

OQ #6: admin 테스트 푸시 이모지 제거
  LOCKED: 제거 → Lucide Send + Loader2
  본 W3 무관 (SettingsPanel W4 책임)

OQ #7: 햄버거 panel width 300 + SettingsPanel width 320 보존
  LOCKED: 보존
  본 W3 적용: panel width 82% maxWidth 300 + borderRadius '0 16px 16px 0' 변경 0.

OQ #8: W7 TSX 변환 4 분할 (sub-wave)
  LOCKED: 4 분할
  본 W3 적용: SideMenu 단일 sketch 가 W7 의 두 번째 sub-wave 대상.

────────────────────────────────────────────
W3 추가 OQ 2건 (sketch 머리 주석 명시 + default 답)
────────────────────────────────────────────

W3-OQ #A: ✕ 텍스트 글리프 (U+2715, line 141) → Lucide X 교체 vs 유지?
  default 답: Lucide X 교체 (size={16}, var(--text-secondary))
  근거:
    - memory feedback_tsx_wave_emoji_dot_gap — 글리프도 negative 룰
    - design-system §7.1 Lucide 사용, 28-splash/27-login/SettingsPanel 닫기 패턴 mirror
    - ariaLabel "메뉴 닫기" 보존 강제
  대안: 콘텐츠 글리프 (U+2715) 유지 — OQ #5 인라인 svg 유지 룰과 align
  ★ LOCKED placeholder — 사용자 결정. 본 sketch 는 Lucide X 가정으로 시각화.

W3-OQ #B: 미조치 badge 색 — var(--danger) (line 177) → --status-danger vs --status-fire-bar?
  default 답: --status-fire-bar (주황 #f97316 / 라이트 #c2410c)
  근거:
    - memory feedback_inspection_unresolved_color — "미조치 색 = status-fire (주황).
      사용자 인지 = 칩의 fire."
    - 메인 칩이 fire 인데 SideMenu badge 만 danger 면 inconsistent
  대안: --status-danger-bar (빨강 #ef4444 / 라이트 #b91c1c) — 현 source verbatim 호환
  단 SideMenu badge 는 minWidth 16 작은 영역 — fire(주황) vs danger(빨강) 시각 차이
  검수 필요. 두 대안 모두 동일 frame 안 ★ side-by-side 시연 권장.
  ★ LOCKED placeholder — 사용자 결정. 본 sketch 는 --status-fire-bar 기본 +
  --status-danger-bar 보조 frame.
```

### 박스 D — 노안 룰 §1.1 격상 표

W2 sketch line 96~112 mirror. 다음 7건 표 형식:

```
source (SideMenu.tsx line)              ->  sketch (W3)               비고
---------------------------------------     ----------------------    ----------------------------
부제 "소방안전 통합관리" 9.5 (line 139)  ->  12 (text-caption)         caption — 헤더 부제
✕ 닫기 fontSize 15 (line 141)            ->  Lucide X size 16          W3-OQ #A default
divider 9 (line 149)                     ->  11 (uppercase 유지)        section 헤더 — letterSpacing .08em 유지
divider letterSpacing '.08em'            ->  유지                        uppercase 시각 유지
item 12.5 (line 163, 175)                ->  16 (text-body)             ★ 노안 마지노선 — 메뉴 라벨
item fontWeight 500                      ->  500 (유지)                  medium 유지
soon 배지 10 (line 164)                   ->  12 (text-caption)         배지 작은 컨테이너 leading-none 명시
soon 배지 padding '2px 7px'              ->  '4px 8px' (8pt 그리드)
badge 11 monospace (line 177)            ->  13 (text-label) 또는 14    숫자 — 본 sketch 13 채택
badge 99+ cap                            ->  유지                        '99+' 텍스트 그대로
사용자 이름 11.5 (line 193)               ->  14 (text-body-sm)          14 vs 16 sub-OQ — 14 채택
사용자 직책+shift 9.5 (line 194)          ->  12 (text-caption)         caption + leading-none
panel width 82% maxWidth 300              ->  유지                        OQ #7 LOCKED
panel borderRadius '0 16px 16px 0'        ->  유지                        OQ #7 LOCKED
오버레이 rgba(0,0,0,0.65)                  ->  var(--surface-overlay)    토큰 매핑 (다크 0.6 / 라이트 0.5 자동)
panel bg var(--bg2)                       ->  var(--surface-raised)     토큰 매핑
hover bg var(--bg4)                       ->  var(--surface-active)     토큰 매핑
divider/soon/부제 color var(--t3)          ->  var(--text-tertiary)      토큰 매핑
닫기 color var(--t2)                       ->  var(--text-secondary)     토큰 매핑
item color var(--t1)                       ->  var(--text-primary)       토큰 매핑
badge bg var(--danger)                     ->  var(--status-fire-bar)    W3-OQ #B default
badge color #fff                           ->  var(--text-on-accent)     토큰 매핑
사용자 아바타 그라데이션                    ->  유지 (OQ #3 LOCKED)        §6.4 매치
사용자 아바타 #fff                          ->  var(--text-on-accent)     토큰 매핑
borderRadius 7 (닫기) / 8 (로고) /         ->  --radius-sm (8)            tokens.css §radius 일관
borderRadius 9 (사용자 row)               ->  --radius-sm (8) 또는       8 또는 12 — sketch 결정
                                             --radius-md (12)
사용자 카드 bg var(--bg3)                  ->  var(--surface-sunken)     토큰 매핑
borderTop 1px var(--bd)                   ->  1px var(--border-default)  토큰 매핑
```

### 박스 E — 토큰 매핑 표 (legacy alias → v0.1.1 의미 토큰)

W2 sketch line 114~132 mirror. 박스 D 와 중복되는 항목은 간략화. 핵심 룰: legacy alias는 :root 호환 블록 안에서만 허용, body markup 안 0건 (verify gate).

### 박스 F — Negative gate (sketch 본문 verify 룰)

```
- 9 / 10 / 11 / 11.5 / 12.5 / 9.5 px fontSize body markup 안 0건
  (단 :root 정의 + 비즈 anchor 박스 + 격상 표 + 토큰 매핑 표 doc context 안 인용은 예외)
- linear-gradient body markup 안 1건만 (사용자 아바타 OQ #3 보존, line 189 verbatim)
- 이모지 body markup 안 0건 (✕ U+2715 텍스트 글리프도 negative — Lucide X 교체 default)
- status-* class prefix 0건 (memory feedback_tailwind_token_class_pattern)
- legacy alias --bg/--bg2/--bg3/--bg4/--bd/--t1/--t2/--t3/--danger body markup 안 0건
- raw hex body markup 안 0건 (단 :root + [data-theme="light"] + 호환 alias 정의 블록 안 +
  OQ #3 그라데이션 보존 예외)
- MENU 상수 17 items label/path 변경 0 (sketch 안 박제 시 verbatim, MenuSettingsSection 의 source)
- w-8/h-8 함정 의식 (memory feedback_tailwind_w8_h8_is_48px — w-8=48px, 32 아님)
  → 28×28 = w-7 h-7 (= 32) 잘못 / w-[28px] h-[28px] 인라인 정확
- panel width 82% maxWidth 300 변경 0 (OQ #7 LOCKED)
- borderRadius '0 16px 16px 0' 변경 0 (OQ #7 LOCKED)
- src/components/SideMenu.tsx 1 byte 변경 0
- App.tsx 1 byte 변경 0
- tokens.css / typography.css / design-system.md / tailwind.config.js 1 byte 변경 0
- 31-chrome/ 다른 8 파일 (W2 sketch + wave-1-index.md + 7 스냅샷) 1 byte 변경 0
- 00-design-context/ 1 byte 변경 0
- wrangler 명령 호출 0 (CLAUDE.local.md + memory feedback_cbc7119_design_never_wrangler)
- npm run deploy 호출 0
```

---

## §2. `<head><style>` — v0.1.1 토큰 정의 + 외곽 chrome 스타일

W2 sketch line 148~399 mirror. 다음 블록:

### §2.1 `:root` (다크 기본) + `[data-theme="light"]` (OQ #1 LOCKED)

W2 sketch line 157~200 패턴 그대로. tokens.css 의 §1 다크 + §2 라이트 verbatim. SideMenu W3 가 사용하는 토큰 모두 포함:
- `--surface-raised` (panel bg)
- `--surface-sunken` (닫기 btn / 사용자 카드 bg / soon 배지 bg)
- `--surface-active` (item hover bg)
- `--surface-overlay` (오버레이 dimmer — 다크 0.6 / 라이트 0.5)
- `--text-primary` (item label / 헤더 / 사용자 이름)
- `--text-secondary` (닫기 btn 색 / Lucide X 색)
- `--text-tertiary` (divider / soon / 부제 / 사용자 직책+shift)
- `--text-on-accent` (아바타 / badge 텍스트)
- `--border-default` (헤더 + 사용자 카드 borderTop)
- `--status-fire-bar` (W3-OQ #B default badge bg)
- `--status-danger-bar` (W3-OQ #B alternative badge bg — 보조 frame)
- `--accent` (h2 박스용)
- `--radius-sm` (8 — 로고 + 닫기 + item + soon)
- `--radius-md` (12 — 사용자 row)
- `--radius-lg` (16 — panel borderRadius)
- `--space-3` (12) / `--space-4` (16)
- `--font-sans`

### §2.2 외곽 chrome (sketch body wrapper)

W2 sketch line 202~265 패턴 그대로:
- `body` padding 32px background #0f1218 (raw hex 예외) font-family var(--font-sans)
- `h1` / `h1 + p` / `h2` (sketch 외곽 시각 chrome)
- `.vp-desc` (viewport 설명 caption)
- `.row` (frame 좌우 배치) / `.caption`

### §2.3 `.frame-mobile` (480px 모바일 시뮬레이션)

W2 sketch line 268~276 패턴. width 480 (모바일 시뮬레이션) + var(--surface-page) bg + border var(--border-default) + radius var(--radius-lg) overflow hidden.

단, SideMenu 는 panel 이 오버레이 위 슬라이드 — 별도 `.frame-mobile-tall` (높이 720 정도, 모바일 viewport 시뮬레이션) 도 정의 권장. panel 이 left 0 슬라이드 시 frame 안 width 82% maxWidth 300 = 약 246px 패널 보임.

### §2.4 SideMenu 내부 마크업 클래스 (W7 가 TSX 로 변환할 대상)

다음 클래스 정의 (W2 의 `.global-header` `.gh-icon-btn` `.gh-title` mirror — sketch 안에서 TSX 가 사용할 className 와 1:1 매칭):

```css
/* 오버레이 (overlay) */
.side-overlay {
  position: absolute; inset: 0; z-index: 190;
  background: var(--surface-overlay);
  /* sketch frame 안에서는 absolute, 실제 TSX 는 fixed */
}

/* panel — id="side-menu-panel" 일관 */
.side-panel {
  position: absolute; top: 0; bottom: 54px; left: 0; z-index: 200;
  width: 82%; max-width: 300px;
  background: var(--surface-raised);
  display: flex; flex-direction: column; overflow: hidden;
  border-radius: 0 var(--radius-lg) var(--radius-lg) 0;
  transform: translateX(0);
  transition: transform 0.3s cubic-bezier(.4,0,.2,1);
}
.side-panel--closed { transform: translateX(-100%); }

/* 헤더 */
.side-header {
  display: flex; align-items: center; gap: 10px;
  padding: 12px 15px;
  border-bottom: 1px solid var(--border-default);
  flex-shrink: 0;
}
.side-header-logo {
  width: 30px; height: 30px;
  border-radius: var(--radius-sm);
  flex-shrink: 0;
}
.side-header-title {
  font-size: 13px;   /* 13 = label 마지노선 유지 — 브랜드명 */
  font-weight: 700;
  line-height: 1.4;
  color: var(--text-primary);
}
.side-header-sub {
  font-size: 12px;   /* 9.5 -> 12 격상 (caption + leading-none) */
  line-height: 1;
  color: var(--text-tertiary);
  margin-top: 2px;
}
.side-close-btn {
  margin-left: auto;
  width: 28px; height: 28px;    /* w-8/h-8 함정 회피 — inline 28 */
  border-radius: var(--radius-sm);
  background: var(--surface-sunken);
  border: none;
  color: var(--text-secondary);
  cursor: pointer;
  display: flex; align-items: center; justify-content: center;
  flex-shrink: 0;
}

/* 메뉴 리스트 영역 */
.side-list {
  overflow-y: auto;
  flex: 1;
  padding: 5px 0;
}

/* divider (섹션 헤더) */
.side-divider {
  padding: 12px 13px 4px;
  font-size: 11px;          /* 9 -> 11 격상 */
  font-weight: 700;
  color: var(--text-tertiary);
  letter-spacing: .08em;
  text-transform: uppercase;
  line-height: 1;
}

/* item */
.side-item {
  display: flex; align-items: center; gap: 10px;
  padding: 11px 13px;
  margin: 1px 7px;
  border-radius: var(--radius-sm);
  cursor: pointer;
  color: var(--text-primary);
  transition: background 0.13s;
}
.side-item:hover { background: var(--surface-active); }
.side-item-label {
  font-size: 16px;          /* 12.5 -> 16 격상 ★ 노안 마지노선 */
  font-weight: 500;
  flex: 1;
  line-height: 1.4;
}

/* soon 배지 */
.side-item--soon {
  opacity: 0.5;
  cursor: default;
  pointer-events: none;
  color: var(--text-tertiary);
}
.side-soon-badge {
  font-size: 12px;          /* 10 -> 12 격상 */
  line-height: 1;
  color: var(--text-tertiary);
  background: var(--surface-sunken);
  border-radius: var(--radius-sm);
  padding: 4px 8px;
}

/* badge (미조치 카운트) */
.side-badge {
  background: var(--status-fire-bar);   /* W3-OQ #B default */
  color: var(--text-on-accent);
  font-size: 13px;                       /* 11 -> 13 격상 */
  line-height: 1;
  font-weight: 700;
  font-family: 'JetBrains Mono', ui-monospace, monospace;
  padding: 3px 6px;
  border-radius: 9px;
  min-width: 20px;
  text-align: center;
}
.side-badge--danger {
  background: var(--status-danger-bar);   /* W3-OQ #B alternative */
}

/* 사용자 카드 */
.side-user-area {
  padding: 11px 11px;
  border-top: 1px solid var(--border-default);
  flex-shrink: 0;
}
.side-user-card {
  display: flex; align-items: center; gap: 10px;
  padding: 10px 12px;
  background: var(--surface-sunken);
  border-radius: var(--radius-md);    /* 9 -> 12 */
}
.side-user-avatar {
  width: 28px; height: 28px;
  border-radius: 50%;
  background: linear-gradient(135deg, #1d4ed8, #0ea5e9);  /* OQ #3 LOCKED — raw hex 예외 */
  display: flex; align-items: center; justify-content: center;
  font-size: 14px;           /* 11 -> 14 */
  font-weight: 700;
  color: var(--text-on-accent);
  flex-shrink: 0;
  line-height: 1;
}
.side-user-info { flex: 1; min-width: 0; }
.side-user-name {
  font-size: 14px;           /* 11.5 -> 14 (body-sm) */
  font-weight: 700;
  line-height: 1.4;
  color: var(--text-primary);
}
.side-user-meta {
  font-size: 12px;           /* 9.5 -> 12 (caption + leading-none) */
  line-height: 1;
  color: var(--text-tertiary);
  margin-top: 2px;
}
```

### §2.5 디자인 결정 / 비즈 anchor / negative gate 박스 (sketch 외곽 문서)

W2 sketch line 328~399 mirror — `.decision-summary`, `.biz-anchors`, `.negative-gate`, `.upgrade-table` 4 박스. 폰트 14~16 (노안 룰 부합) — sketch 외곽 doc 은 본문 16 마지노선.

---

## §3. `<body>` — 4~6 frame 매트릭스

### Frame 1 (h2 "Frame 1 — 다크 / 패널 열림 + 오버레이"): 다크 mobile 전체 view

`<div class="frame-mobile-tall">` 안:
- `<div class="side-overlay">` (오버레이 보임 — opacity 1)
- `<div class="side-panel" id="side-menu-panel">`:
  - `.side-header` — 로고 img / 타이틀 / 부제 / Lucide X 닫기 (svg inline 또는 path "M18 6 6 18M6 6l12 12")
  - `.side-list` — MENU 상수 일부 (주요 기능 5 + 시설 관리 6 모바일 노출 4 [DIV/도면/소방 점검/소방 시설 추가 admin] = 9 item) + 1 divider (주요 기능) + 1 divider (시설 관리)
    - 일부 item 에 `.side-badge` 표시 (조치 관리 path /remediation 에 unresolvedCount 12 같은 카운트)
    - 일부 item soon 처리 (실제 source 의 soon: false 이지만 sketch demo 용 1개 정도)
  - `.side-user-area` — 사용자 카드 (이니셜 "윤" + 이름 "윤종엽" + meta "팀원 · 주간")
- 그 옆 `.caption` — "다크 / 패널 열림. 오버레이 var(--surface-overlay) 와 panel 슬라이드 표시. 메뉴 라벨 16px 노안 마지노선. badge 12 (조치 관리) fire-bar 주황."

### Frame 2 (h2 "Frame 2 — 다크 / 사용자 카드 zoom + divider+item+soon+badge 4 분기 시각화")

작은 mobile frame 또는 별도 zoom container:
- 사용자 카드 1개 (zoom)
- 별도 박스: divider 1 + item 1 (normal) + item 1 (soon, "준비중" 배지) + item 1 (badge "12" fire-bar) + item 1 (badge "99+" cap 시연)
- `.caption` — "사용자 카드 zoom. 직책+shift 12px caption. divider 11px uppercase. soon 12px. badge 13px monospace fire-bar 주황."

### Frame 3 (h2 "Frame 3 — 라이트 / 패널 열림 + 오버레이")

`<div class="frame-mobile-tall" data-theme="light">` — Frame 1 의 markup 동일, theme 분기로 자동 라이트 토큰 적용. 오버레이도 라이트 surface-overlay (rgba(0,0,0,0.5)) 자동.

`.caption` — "라이트 / 동일 markup, [data-theme=\"light\"] 만 부착. surface/text/border 모두 라이트 토큰 자동. 오버레이 0.5 (다크 0.6)."

### Frame 4 (h2 "Frame 4 — 라이트 / 미조치 badge fire-bar vs danger-bar 비교 시연")

`<div class="frame-mobile" data-theme="light">` 작은 frame 2개 좌우 배치:
- 좌: `.side-badge` (fire-bar 주황, W3-OQ #B default) — 조치 관리 item 에 "12"
- 우: `.side-badge--danger` (danger-bar 빨강, W3-OQ #B alternative) — 동일 item "12"
- `.caption` — "W3-OQ #B 시각 비교. fire-bar (#c2410c 라이트 / #f97316 다크) vs danger-bar (#b91c1c 라이트 / #ef4444 다크). 사용자 결정 — default 답 = fire-bar (memory feedback_inspection_unresolved_color: 메인 칩 fire 와 일관)."

### Frame 5 (선택, h2 "Frame 5 — 다크 / 패널 닫힘 transition") — sketch 결정

panel `--closed` modifier — `transform translateX(-100%)` 시각화 (왼쪽 절반 잘림). `.caption` — "transition 'transform 0.3s cubic-bezier(.4,0,.2,1)' — 닫힘 시 왼쪽 슬라이드. 오버레이 opacity 0 + pointerEvents none."

### Frame 6 (선택, h2 "Frame 6 — admin 사용자 메뉴 — 직원 관리 노출")

다크 frame, MENU 의 admin 전용 메뉴 (소방 시설 추가 /checkpoints / 업무 수행 기록표 /worklog / 직원 관리 /staff-manage) 표시. `.caption` — "admin role 사용자 view — staff.role === 'admin' 분기로 노출. assistant 사용자는 3 메뉴 모두 숨김 (line 158)."

---

## §4. 박스 본문 (4 박스)

W2 sketch line 562~718 패턴 mirror.

### `.decision-summary` (디자인 결정 요약)

ul li 8건:
- ★ 다크 + 라이트 양쪽 (OQ #1 LOCKED) — :root + [data-theme="light"] 토큰 둘 다 정의
- ★ §1.1 부분 격상 (OQ #2 LOCKED) — 7건 격상 (부제 12 / divider 11 / item 16 / badge 13 / 사용자 이름 14 / 사용자 부제 12 / Lucide X 16)
- ★ 사용자 아바타 그라데이션 유지 (OQ #3 LOCKED) — `linear-gradient(135deg, #1d4ed8, #0ea5e9)` body markup 안 1건 예외
- ★ panel width 82% maxWidth 300 + borderRadius '0 16px 16px 0' 보존 (OQ #7 LOCKED)
- W3-OQ #A: ✕ → Lucide X 교체 (default, sketch 적용)
- W3-OQ #B: badge fire-bar 주황 (default, sketch 적용) — memory feedback_inspection_unresolved_color
- 오버레이 var(--surface-overlay) 토큰 사용 (다크 0.6 / 라이트 0.5 자동)
- W7 TSX 변환 시 SideMenu.tsx 1 byte 변경 0 + MENU 상수 / Props / NAV_H / RAW_TO_LABEL / body scroll lock useEffect / panel id / 오버레이 0.28s + panel 0.3s cubic-bezier / borderRadius '0 16px 16px 0' / 아바타 그라데이션 모두 verbatim

### `.biz-anchors` (비즈 anchor 22건 — 박스 B 재 표시)

박스 B 의 22건 ul li 형식으로 시각 박제 (sketch 외곽 doc 으로도 한 번 더).

### `.negative-gate` (sketch 본문 검증 룰 — 박스 F 재 표시)

박스 F 의 룰 ul li 형식.

### `.upgrade-table` (격상 표 — 박스 D 재 표시)

박스 D 의 표를 `<table>` 형식으로 (W2 sketch `.upgrade-table table th td` 스타일 mirror).

---

## §5. 자체 verify (작성 직후 grep 으로 확인)

작업 완료 후 sketch HTML 에 대해 다음 grep 명령 직접 실행하고 결과 확인. 실패 시 sketch 수정 후 재실행 (사용자에게 컨펌 받기 전 self-verify).

```bash
SK="cha-bio-safety/docs/redesign-context/31-chrome/sketch-wave-3-side-menu.html"

# (1) 9·10·11px body markup 사용 0 — head comment / table / :root 정의 안 인용은 제외
# body markup 안에서 font-size: Npx 형태로 9/10/11/9.5/11.5/12.5 사용 0건 검증
grep -nE 'font-size:\s*(9|10|11|9\.5|10\.5|11\.5|12\.5)px' "$SK" | grep -v '^[0-9]*:\s*//' | grep -v '\s/\*'
# 기대: 0건. (단 :root 정의나 .upgrade-table td 안 텍스트 인용은 px 가 아닌 일반 텍스트라 매치 X)

# (2) linear-gradient body markup 안 1건만 (OQ #3 아바타)
grep -c 'linear-gradient(135deg, #1d4ed8' "$SK"
# 기대: 박스 B/C/D 의 doc 인용 포함 ≥3, body markup 안에 .side-user-avatar 정의 1건 + 박스 인용 ≥2

# (3) status-* class prefix 0
grep -nE 'class="[^"]*status-' "$SK"
# 기대: 0건

# (4) :root + [data-theme="light"] 둘 다 정의
grep -c '\[data-theme="light"\]' "$SK"
# 기대: ≥1

# (5) frame data-theme="light" 부착 ≥2
grep -c 'data-theme="light"' "$SK"
# 기대: ≥3 (style block 1 + body frame ≥2)

# (6) 핵심 anchor verbatim 박제
grep -c 'linear-gradient(135deg, #1d4ed8, #0ea5e9)' "$SK"   # ≥3 (doc + css)
grep -c "calc(54px + var(--sab, 0px))" "$SK"                  # ≥1 (NAV_H 박스)
grep -c '0 16px 16px 0' "$SK"                                 # ≥2 (OQ #7 보존 박스 + .side-panel)
grep -c 'side-menu-panel' "$SK"                               # ≥2 (박스 + id)
grep -c "queryKey: \\['menu-config'\\]" "$SK"                  # ≥1 (박스 B)
grep -c "cubic-bezier(.4,0,.2,1)" "$SK"                       # ≥2 (박스 + .side-panel transition)
grep -c "RAW_TO_LABEL" "$SK"                                  # ≥1
grep -c "ITEM_META" "$SK"                                     # ≥1

# (7) MENU 5섹션 17 아이템 verbatim 박제
grep -c "section: '주요 기능'" "$SK"                          # ≥1
grep -c "/inspection/qr" "$SK"                                # ≥1
grep -c "desktopOnly: true" "$SK"                             # ≥2 (소화기 + CCTV)
grep -c "role: 'admin'" "$SK"                                 # ≥3 (소방 시설 추가 + 업무 수행 기록표 + 직원 관리)

# (8) 보호 파일 변경 0
git status --porcelain | grep -E '\.(tsx|css|md|js|json|toml)$' | grep -vE 'sketch-wave-3-side-menu\.html$'
# 기대: 0줄 (sketch HTML 1 파일만 새로 추가)

git diff --name-only HEAD -- cha-bio-safety/src/components/SideMenu.tsx
# 기대: 0줄
git diff --name-only HEAD -- cha-bio-safety/src/App.tsx
# 기대: 0줄
git diff --name-only HEAD -- cha-bio-safety/docs/redesign-context/31-chrome/wave-1-index.md \
                              cha-bio-safety/docs/redesign-context/31-chrome/SideMenu.tsx \
                              cha-bio-safety/docs/redesign-context/31-chrome/GlobalHeader.tsx \
                              cha-bio-safety/docs/redesign-context/31-chrome/SettingsPanel.tsx \
                              cha-bio-safety/docs/redesign-context/31-chrome/MenuSettingsSection.tsx \
                              cha-bio-safety/docs/redesign-context/31-chrome/design-system.md \
                              cha-bio-safety/docs/redesign-context/31-chrome/tokens.css \
                              cha-bio-safety/docs/redesign-context/31-chrome/typography.css \
                              cha-bio-safety/docs/redesign-context/31-chrome/sketch-wave-2-global-header.html
# 기대: 0줄 (모두 변경 0)
git diff --name-only HEAD -- cha-bio-safety/docs/redesign-context/00-design-context/
# 기대: 0줄
git diff --name-only HEAD -- cha-bio-safety/tailwind.config.js
# 기대: 0줄

# (9) 라인 수 적정 (600~900)
wc -l "$SK"
# 기대: ≥600 (머리 주석 + style + body 4~6 frame + 4 박스)
```

---

## §6. atomic commit

```bash
git status --short
# 기대: ?? cha-bio-safety/docs/redesign-context/31-chrome/sketch-wave-3-side-menu.html
#       (단일 파일 untracked, 다른 변경 0)

git add cha-bio-safety/docs/redesign-context/31-chrome/sketch-wave-3-side-menu.html

git commit -m "docs(quick-260527-3qi): redesign/31-chrome W3 sketch — SideMenu (햄버거 슬라이드 드로어, 다크+라이트, OQ#2 부분 격상 7건 + W3-OQ#A Lucide X + W3-OQ#B fire-bar)"

git log -1 --stat
# 기대: 1 파일 (sketch-wave-3-side-menu.html) 추가, 다른 변경 0
```

**wrangler 명령 절대 금지** (CLAUDE.local.md + memory feedback_cbc7119_design_never_wrangler).
**npm run deploy 절대 금지**. main push 도 본 task scope 밖 — 사용자 컨펌 후만.

---

## §7. 작업 종료 보고 (사용자에게 컨펌 요청)

memory `feedback_avoid_premature_confirmation` 룰 — "거의 일치" 자신감 표현 금지. 다음 형식으로 보고:

```
W3 sketch 작성 완료. cha-bio-safety/docs/redesign-context/31-chrome/sketch-wave-3-side-menu.html
산출 (N 라인). 4~6 frame 매트릭스 (다크 1~2 + 라이트 1~2 + zoom 1 + 옵션 1~2).

OQ LOCKED 5건 (#1/#2/#3/#5 partial/#7) + W3 추가 OQ 2건 (#A ✕→Lucide X default,
#B badge fire-bar default) 모두 sketch 머리 주석 매트릭스 박제 + body frame 시각화.

비즈 anchor 22건 머리 주석 박제 — W7 TSX 변환 1 byte 변경 0 강제. self-verify
9건 (9·10·11 0 / linear-gradient 1 / status-* 0 / [data-theme=light] 정의 + frame /
NAV_H / panel borderRadius / MENU 5섹션 17 / 보호 파일 변경 0 / 라인 수 ≥600) 모두 PASS.

컨펌 사항:
1. W3-OQ #A (✕ → Lucide X 교체) default 답 OK?
   대안: U+2715 글리프 유지 (OQ #5 인라인 svg 유지 룰과 align).

2. W3-OQ #B (미조치 badge fire-bar 주황) default 답 OK?
   대안: --status-danger-bar 빨강 (현 source 호환). Frame 4 에서 좌우 비교 시연.

3. OQ #2 부분 격상 7건 — item 12.5 → 16, divider 9 → 11, 사용자 이름 11.5 → 14 등.
   panel 세로 overflow 발생 시 dense layout 회귀 가능. sketch frame 본 후 결정.

OK 받으면 main 머지 / W4 SettingsPanel sketch 진입.
```
  </action>
  <verify>
    <automated>
SK="cha-bio-safety/docs/redesign-context/31-chrome/sketch-wave-3-side-menu.html" && \
test -f "$SK" && \
test $(wc -l < "$SK") -ge 600 && \
[ $(grep -c '\[data-theme="light"\]' "$SK") -ge 1 ] && \
[ $(grep -c 'data-theme="light"' "$SK") -ge 3 ] && \
[ $(grep -c 'linear-gradient(135deg, #1d4ed8, #0ea5e9)' "$SK") -ge 3 ] && \
[ $(grep -c "calc(54px + var(--sab, 0px))" "$SK") -ge 1 ] && \
[ $(grep -c '0 16px 16px 0' "$SK") -ge 2 ] && \
[ $(grep -c 'side-menu-panel' "$SK") -ge 2 ] && \
[ $(grep -c "cubic-bezier(.4,0,.2,1)" "$SK") -ge 2 ] && \
[ $(grep -c "section: '주요 기능'" "$SK") -ge 1 ] && \
[ $(grep -c "/inspection/qr" "$SK") -ge 1 ] && \
[ $(grep -c "desktopOnly: true" "$SK") -ge 2 ] && \
[ $(grep -c "role: 'admin'" "$SK") -ge 3 ] && \
[ $(grep -cE 'class="[^"]*status-' "$SK") -eq 0 ] && \
[ -z "$(git diff --name-only HEAD -- cha-bio-safety/src/components/SideMenu.tsx cha-bio-safety/src/App.tsx cha-bio-safety/tailwind.config.js cha-bio-safety/docs/redesign-context/31-chrome/wave-1-index.md cha-bio-safety/docs/redesign-context/31-chrome/SideMenu.tsx cha-bio-safety/docs/redesign-context/31-chrome/GlobalHeader.tsx cha-bio-safety/docs/redesign-context/31-chrome/SettingsPanel.tsx cha-bio-safety/docs/redesign-context/31-chrome/MenuSettingsSection.tsx cha-bio-safety/docs/redesign-context/31-chrome/design-system.md cha-bio-safety/docs/redesign-context/31-chrome/tokens.css cha-bio-safety/docs/redesign-context/31-chrome/typography.css cha-bio-safety/docs/redesign-context/31-chrome/sketch-wave-2-global-header.html cha-bio-safety/docs/redesign-context/00-design-context/)" ] && \
git log -1 --name-only | grep -q "sketch-wave-3-side-menu.html" && \
echo "ALL VERIFY GATES PASS"
    </automated>
  </verify>
  <done>
- cha-bio-safety/docs/redesign-context/31-chrome/sketch-wave-3-side-menu.html 파일 존재 (600~900 라인)
- sketch HTML 머리 주석 6 박스 모두 작성 (메타 + 비즈 anchor 22 + OQ 매트릭스 10 + 격상 표 7 + 토큰 매핑 + negative gate)
- :root 다크 + [data-theme="light"] 라이트 토큰 둘 다 정의 (OQ #1 LOCKED)
- body 안 4~6 frame 중 최소 2 frame data-theme="light" 부착 (OQ #1 LOCKED)
- 노안 격상 7건 모두 body markup 안 적용 (item 12.5→16 / divider 9→11 / 사용자 이름 11.5→14 / 사용자 부제 9.5→12 / 부제 9.5→12 / soon 10→12 / badge 11→13)
- body markup 안 9·10·11px 실사용 0건 (verify gate (1) PASS — :root 정의/박스 인용 제외)
- 사용자 아바타 그라데이션 linear-gradient(135deg, #1d4ed8, #0ea5e9) verbatim 보존 (OQ #3 LOCKED, body markup 안 1건 예외 + 박스 인용)
- panel width 82% maxWidth 300 + borderRadius '0 16px 16px 0' 보존 (OQ #7 LOCKED)
- W3-OQ #A (✕→Lucide X) + W3-OQ #B (badge fire-bar) default 답 sketch 머리 주석 매트릭스 + body Frame 시각화
- MENU 상수 5섹션 17 아이템 label/path/role/desktopOnly verbatim 박제 (verify gate (7) PASS)
- status-* class prefix body 안 0건 (memory feedback_tailwind_token_class_pattern)
- src/components/SideMenu.tsx + App.tsx + tokens.css + typography.css + design-system.md + tailwind.config.js + 31-chrome/wave-1-index.md + 31-chrome 7 스냅샷 + 31-chrome/sketch-wave-2-global-header.html + 00-design-context/ 모두 git diff 0 (verify gate (8) PASS)
- atomic 1-commit (sketch HTML 1 파일 추가만)
- 작업 종료 보고 형식대로 컨펌 사항 3건 (W3-OQ #A, W3-OQ #B, OQ #2 격상 panel 세로 overflow) 사용자에게 명시
- wrangler / npm run deploy 호출 0 (CLAUDE.local.md + memory feedback_cbc7119_design_never_wrangler)
  </done>
</task>

</tasks>

<verification>
**Phase 검증 — 본 plan 1 task 완료 후 사용자가 시각/구조 검증:**

1. 파일 존재 + 라인 수: `wc -l cha-bio-safety/docs/redesign-context/31-chrome/sketch-wave-3-side-menu.html` ≥600
2. 브라우저 열기: `open cha-bio-safety/docs/redesign-context/31-chrome/sketch-wave-3-side-menu.html`
3. 시각 확인:
   - Frame 1 (다크 패널 열림): 오버레이 + 슬라이드 panel + 헤더 + 메뉴 리스트 + 사용자 카드
   - Frame 2 (사용자 카드 zoom + 4 분기 시각화): divider / item / soon / badge 분기 명확
   - Frame 3 (라이트 패널 열림): Frame 1 의 라이트 변형
   - Frame 4 (badge fire-bar vs danger-bar 비교): W3-OQ #B 시각 결정 도움
   - Frame 5/6 (선택): 닫힘 transition / admin view
4. 노안 격상 검수: 메뉴 라벨 16px 부합 확인 / divider 11px uppercase / 사용자 이름 14px / 부제 12px
5. 그라데이션 보존 검수: 사용자 아바타 28×28 #1d4ed8 → #0ea5e9 (OQ #3)
6. panel 치수 보존 검수: width 82% maxWidth 300 + borderRadius '0 16px 16px 0' (OQ #7)
7. body markup grep — 9·10·11px / linear-gradient (1건만) / status-* / legacy alias 모두 negative gate 부합
8. 보호 파일 git diff 0 (verify gate (8))
9. atomic commit 단일 (sketch HTML 1 파일만)
10. wrangler / npm run deploy 호출 기록 0 (CLAUDE.local.md 룰)
</verification>

<success_criteria>
**최종 산출 1건:**
- `cha-bio-safety/docs/redesign-context/31-chrome/sketch-wave-3-side-menu.html` (600~900 라인)

**검증 PASS:**
- 머리 주석 6 박스 (메타 + 비즈 anchor 22 + OQ 매트릭스 10 + 격상 표 7 + 토큰 매핑 + negative gate)
- 다크 + 라이트 양쪽 (OQ #1 LOCKED — `:root` + `[data-theme="light"]` + frame ≥2개에 부착)
- 부분 격상 7건 (OQ #2 LOCKED — body markup 안 9·10·11px 0건)
- 사용자 아바타 그라데이션 보존 (OQ #3 LOCKED — `linear-gradient(135deg, #1d4ed8, #0ea5e9)` body markup 안 1건)
- panel 치수 보존 (OQ #7 LOCKED — width 82% maxWidth 300 + borderRadius '0 16px 16px 0')
- W3 추가 OQ #A (✕ Lucide X default) + W3 추가 OQ #B (badge fire-bar default) 머리 주석 매트릭스 + body Frame 시각화
- MENU 상수 5섹션 17 아이템 verbatim 박제 (W7 TSX 변환 anchor)
- status-* class prefix body 안 0건
- atomic 1-commit (sketch HTML 1 파일만)
- 보호 파일 (src/components + App.tsx + tokens.css + typography.css + design-system.md + tailwind.config.js + 31-chrome/wave-1-index.md + 31-chrome 7 스냅샷 + sketch-wave-2-global-header.html + 00-design-context/) git diff 0
- wrangler / npm run deploy 호출 0
- 작업 종료 보고 형식 — "approved 주세요" 자신감 표현 금지 (memory feedback_avoid_premature_confirmation), 컨펌 사항 3건 명시

**다음 단계 (본 plan scope 밖):**
- 사용자 컨펌 (W3-OQ #A + W3-OQ #B + OQ #2 격상 panel 세로 overflow 결정) 후 main 머지
- W4 SettingsPanel sketch (894 라인, 가장 큰 sub-wave) 또는 W5 MenuSettingsSection sketch (418 라인) 진입
- W6 TSX 변환 verify checklist markdown 작성 후 W7 TSX 변환 4 분할 진입
</success_criteria>

<output>
After completion, create `.planning/quick/260527-3qi-redesign-31-chrome-w3-sidemenu-sketch-at/260527-3qi-SUMMARY.md` summarizing:
- 산출 파일 (sketch-wave-3-side-menu.html, N 라인)
- 4~6 frame 매트릭스 (다크 N + 라이트 N + zoom + 옵션)
- OQ LOCKED 5건 + W3 추가 OQ 2건 default 답 + 격상 7건 적용 결과
- 비즈 anchor 22건 박제 + verify gate 9건 결과
- 보호 파일 git diff 0 확인
- atomic 1-commit 해시
- 사용자 컨펌 사항 3건 (W3-OQ #A, W3-OQ #B, OQ #2 격상 panel 세로 overflow)
- 다음 단계 안내 (main 머지 컨펌 후 W4 또는 W5 진입)
</output>
