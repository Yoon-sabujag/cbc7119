---
quick_id: 260515-jp3
slug: redesign-07-elevator-tsx-wave-4-repairne
type: execute
wave: 1
depends_on: []
files_modified:
  - cha-bio-safety/src/pages/ElevatorPage.tsx
autonomous: true
requirements:
  - REDESIGN-07-ELEVATOR-WAVE-4-REPAIR-NEW-MODAL
tags:
  - redesign
  - elevator
  - tsx-conversion
  - design-tokens-v0.1.1
  - tailwind-only
  - repairnewmodal
  - wave4

must_haves:
  truths:
    - "RepairNewModal 비즈니스 로직 100% 보존 (state/handleSubmit/canSubmit/isEscalator 자동 useEffect)"
    - "edit 모드 분기 보존 (isEdit / editData / 제목 '수리 기록 수정' / 페이로드 / API.update)"
    - "EvSelector 호출 시그니처 보존 (groups=EV_GROUPS_ANNUAL, esNodes=ES_NODES_ANNUAL, evKind/elevatorId/setEvKind/setElevatorId props)"
    - "elevatorRepairApi.create / elevatorRepairApi.update 호출 한 줄도 변경되지 않음"
    - "qc.invalidateQueries(['elev-repairs']), qc.invalidateQueries(['elevator_repairs_all']) 보존"
    - "toast.success / toast.error 한글 메시지 보존 (수리 기록 저장/수정 완료, 저장/수정 실패)"
    - "수리 대상 4 옵션 (카/홀/기계실/피트) — 선택 시 bg-warning-bg + outline-warning + text-warning, 비선택 시 bg-surface-sunken + text-text-tertiary (2C 시안 권위)"
    - "에스컬레이터일 때 (!isEscalator) 수리 대상 영역 자체 숨김 분기 보존"
    - "홀 층 칩 (repairTarget==='hall' && floors.length>0) — 수리 대상과 같은 warning 토큰 통일"
    - "4 단계 사진 업로드 (부품 입고 / 파손 부품 / 수리 중 / 수리 완료) 라벨 + props 시그니처 (label/keys/setKeys) 보존"
    - "CTA = accent 단색 (그라디언트 X). disabled 시 opacity 0.5 (화이트리스트)"
    - "RepairNewModal 변환 영역 내 인라인 style 금지 키 0건 (화이트리스트: opacity 동적값만 허용)"
    - "RepairNewModal 변환 영역 내 9-11px 폰트 0건 (text-caption 12px 이상)"
    - "RepairNewModal 변환 영역 내 이모지 0건"
    - "보존 영역 sentinel 0 변경: ModalWrap / Field / MultiPhotoUpload / EvSelector / EsBtn / EsNodeMap / FaultNewModal / FaultNewFullscreen / FaultResolveModal / InspectModal / EvDetailModal / icons.tsx / tailwind.config.js"
    - "inputSt / primaryBtnSt 글로벌 상수 본체 수정 0건 (다른 모달이 공유)"
  artifacts:
    - path: "cha-bio-safety/src/pages/ElevatorPage.tsx"
      provides: "RepairNewModal 변환 후 (line ~3082~3230)"
      contains: "function RepairNewModal"
  key_links:
    - from: "RepairNewModal 수리 대상 버튼"
      to: "tailwind bg-warning-bg / text-warning / outline-warning"
      via: "className 조건부 (repairTarget === opt.key)"
      pattern: "bg-warning-bg.*text-warning|outline-warning"
    - from: "RepairNewModal CTA 저장 버튼"
      to: "tailwind bg-accent 단색"
      via: "className + disabled + opacity"
      pattern: "bg-accent.*disabled:opacity"
    - from: "RepairNewModal EvSelector 호출"
      to: "Wave 2 EvSelector 컴포넌트"
      via: "groups={EV_GROUPS_ANNUAL} esNodes={ES_NODES_ANNUAL}"
      pattern: "EV_GROUPS_ANNUAL.*ES_NODES_ANNUAL"
    - from: "RepairNewModal handleSubmit"
      to: "elevatorRepairApi.create / update"
      via: "if(isEdit) update else create"
      pattern: "elevatorRepairApi\\.(create|update)"
---

<objective>
ElevatorPage.tsx 의 RepairNewModal (line ~3082~, 약 150줄) 을 v0.1.1 디자인 토큰 + Tailwind utility 만으로 변환.

Purpose:
- 2C 시안 권위 (60515-g61 결정) 1:1 매핑 — 수리 대상/홀 층 칩 = warning 토큰
- 인라인 style 금지 키 0건 (opacity 화이트리스트만) + 9-11px 폰트 0건 + 이모지 0건
- 노안 룰 (본문 16px, 보조 12-14px) 준수
- 비즈니스 로직 100% 보존 — edit 모드 / API / qc / toast / 4 단계 사진

Output:
- cha-bio-safety/src/pages/ElevatorPage.tsx 의 RepairNewModal 함수 본체만 변환
- Wave 1+2+3 + 2A~2D sketch 와 일관된 Tailwind only 코드
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/STATE.md
@cha-bio-safety/src/pages/ElevatorPage.tsx
@cha-bio-safety/docs/redesign-context/07-elevator/sketch/input-modals-sketch.html
@.planning/quick/260515-g61-redesign-07-elevator-2c-sketch-inspectmo/260515-g61-SUMMARY.md
@.planning/quick/260515-iz1-redesign-07-elevator-tsx-wave-3-fault-3-/260515-iz1-SUMMARY.md
@.planning/quick/260515-ia6-redesign-07-elevator-tsx-wave-2-evselect/260515-ia6-SUMMARY.md
@cha-bio-safety/src/components/ui/icons.tsx

<interfaces>
<!-- Wave 2/3 에서 확정된 시그니처. 본 wave 에서 호출만 하고 본체 수정 금지. -->

EvSelector (Wave 2 변환 완료):
```typescript
<EvSelector
  elevators={elevators}
  evKind={evKind}
  setEvKind={v => { setEvKind(v); setElevatorId(''); setRepairTarget('') }}
  elevatorId={elevatorId}
  setElevatorId={setElevatorId}
  groups={EV_GROUPS_ANNUAL}     // 5 그룹 variant (저층/중고층/장애인/덤웨이터/연결동)
  esNodes={ES_NODES_ANNUAL}      // 6 노선 variant
/>
```

Field (Wave 1 유지):
```typescript
<Field label="수리일">
  <input type="date" value={repairDate} onChange={...} className="..." />
</Field>
```

MultiPhotoUpload (Wave 3 패턴):
```typescript
<MultiPhotoUpload label="부품 입고 사진" keys={partsPhotos} setKeys={setPartsPhotos} />
```

ModalWrap (Wave 1 유지):
```typescript
<ModalWrap title={isEdit ? "수리 기록 수정" : "수리 기록 입력"} onClose={onClose}>
  {/* 본문 */}
</ModalWrap>
```

elevatorRepairApi (보존 — 한 줄도 변경 X):
```typescript
elevatorRepairApi.create(body)
elevatorRepairApi.update(editData.sourceId, body)
```

전역 상수 (참조만 — 본체 수정 금지):
- inputSt: 다른 모달 (InspectModal Wave 6) 공유 — 본체 수정 X. RepairNewModal 안에서만 Tailwind 로 직접 교체.
- primaryBtnSt: 마찬가지로 공유. RepairNewModal CTA 만 Tailwind 직접 교체.
- EV_FLOORS / EV_GROUPS_ANNUAL / ES_NODES_ANNUAL: 데이터 상수, 참조만.

Tailwind 토큰 (검증 완료):
- 색: text-warning / bg-warning-bg / outline-warning (status-warning 계열)
- 색: bg-surface-sunken / text-text-tertiary (비선택)
- 색: bg-accent / hover:bg-accent-hover (CTA)
- 색: text-text-primary / text-text-secondary / text-text-tertiary
- 색: bg-surface-base / border-border-default / border-border-strong
- 사이즈: text-body (16) / text-body-sm (14) / text-caption (12) / text-label (13)
- 라운드: rounded-sm(8) / rounded-md(12) / rounded-lg(16) / rounded-pill
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: RepairNewModal TSX 변환 (수리 대상/홀 층 칩 + 4 단계 사진 + input/textarea/CTA)</name>
  <files>cha-bio-safety/src/pages/ElevatorPage.tsx</files>
  <action>
RepairNewModal 함수 본체 (line ~3082 시작, `function RepairNewModal({ elevators, selected, onClose, editData }: ...)` 부터 닫는 `}` 까지) 를 2C 시안 권위로 1:1 변환.

**변환 범위 (RepairNewModal 함수 본체만)**:

1) **본문 wrapper**:
   - 변경 전: `<div style={{ display:'flex', flexDirection:'column', gap:14 }}>`
   - 변경 후: `<div className="flex flex-col gap-[14px]">`

2) **수리일 date input** (Field 안):
   - 변경 전: `<input type="date" ... style={inputSt} />`
   - 변경 후: `<input type="date" value={repairDate} onChange={e => setRepairDate(e.target.value)} className="w-full px-3 py-[10px] bg-surface-sunken border border-border-default rounded-md text-text-primary text-body-sm font-medium outline-none appearance-none box-border min-w-0" />`

3) **수리 대상 토글 (2C 권위 — 핵심 변환)**:
   - `!isEscalator &&` 분기 보존
   - 라벨 `<div>`: 변경 전 `style={{ fontSize:11, fontWeight:700, color:'var(--t2)', marginBottom:7 }}` → 변경 후 `className="text-label font-bold text-text-secondary mb-[7px]"` (11→13px 노안 룰)
   - 컨테이너: 변경 전 `style={{ display:'flex', gap:7 }}` → 변경 후 `className="flex gap-[7px]"`
   - 4 옵션 버튼 (.map 안): 변경 전 인라인 style yellow `#eab308` 직접색
     ```
     style={{
       flex:1, padding:'11px 0', borderRadius:10, border:'none', cursor:'pointer', fontSize:11, fontWeight:700,
       background: repairTarget === opt.key ? '#eab30822' : 'var(--bg3)',
       color: repairTarget === opt.key ? '#eab308' : 'var(--t3)',
       outline: repairTarget === opt.key ? '2px solid #eab308' : 'none',
     }}
     ```
   - 변경 후:
     ```tsx
     <button
       key={opt.key}
       onClick={() => { setRepairTarget(opt.key); setHallFloor('') }}
       className={`flex-1 py-[11px] rounded-md border-none cursor-pointer text-label font-bold transition-all hover:-translate-y-px ${
         repairTarget === opt.key
           ? 'bg-warning-bg text-warning outline outline-2 outline-warning outline-offset-1'
           : 'bg-surface-sunken text-text-tertiary'
       }`}
     >
       {opt.label}
     </button>
     ```
   - (text-label = 13px — 노안 룰. yellow 직접색 → warning 토큰 — 2C 결정 권위)

4) **홀 층 칩 (repairTarget==='hall' && floors.length>0)**:
   - 라벨: 위와 동일 `className="text-label font-bold text-text-secondary mb-[7px]"`
   - 컨테이너: 변경 전 `style={{ display:'flex', gap:5, flexWrap:'wrap' }}` → 변경 후 `className="flex gap-[6px] flex-wrap"`
   - 층 버튼 (.map 안):
     ```tsx
     <button
       key={f}
       onClick={() => setHallFloor(f)}
       className={`px-3 py-2 rounded-md border-none cursor-pointer text-label font-bold transition-all hover:-translate-y-px ${
         hallFloor === f
           ? 'bg-warning-bg text-warning outline outline-2 outline-warning outline-offset-1'
           : 'bg-surface-sunken text-text-tertiary'
       }`}
     >
       {f}
     </button>
     ```

5) **수리 항목 input** (Field 안, 필수):
   - 변경 전: `<input ... style={inputSt} />`
   - 변경 후: `<input value={repairItem} onChange={e => setRepairItem(e.target.value)} placeholder="수리 부품/항목명" className="w-full px-3 py-[10px] bg-surface-sunken border border-border-default rounded-md text-text-primary text-body-sm outline-none appearance-none box-border min-w-0 placeholder:text-text-tertiary" />`

6) **수리 내용 textarea** (Field 안, 선택, 3 row):
   - 변경 전: `style={{ ...inputSt, resize:'none' }}`
   - 변경 후: `className="w-full px-3 py-[10px] bg-surface-sunken border border-border-default rounded-md text-text-primary text-body-sm outline-none box-border min-w-0 resize-none placeholder:text-text-tertiary font-[inherit]"` (resize-none 보존)

7) **수리 업체 input** (Field 안, 선택):
   - 수리 항목 input 과 동일 패턴

8) **4 단계 사진 업로드** — `<MultiPhotoUpload label="..." keys={...} setKeys={...} />` 4 줄 그대로 보존 (라벨 + props 시그니처 100% 보존). 본체 수정 X.

9) **CTA 저장 버튼**:
   - 변경 전:
     ```
     <button onClick={handleSubmit} disabled={!canSubmit} style={{ ...primaryBtnSt, opacity: canSubmit ? 1 : 0.5 }}>
       {saving ? '저장 중...' : '수리 기록 저장'}
     </button>
     ```
   - 변경 후 (accent 단색 — 그라디언트 X):
     ```tsx
     <button
       onClick={handleSubmit}
       disabled={!canSubmit}
       style={{ opacity: canSubmit ? 1 : 0.5 }}
       className="w-full py-[13px] rounded-md border-none bg-accent hover:bg-accent-hover text-white text-body-sm font-bold cursor-pointer transition-colors disabled:cursor-not-allowed"
     >
       {saving ? '저장 중...' : (isEdit ? '수정 완료' : '수리 기록 저장')}
     </button>
     ```
   - **edit 모드 라벨 분기 추가**: 시안 권위 "수정 완료" (edit 모드 — 헤더 "수리 기록 수정" 과 일관). 현 코드는 edit 모드에서도 "수리 기록 저장" 출력 → 시안 권위로 정정.
   - opacity 는 동적 값이므로 인라인 style 화이트리스트 유지.

**비즈니스 로직 100% 보존 (한 줄도 건드리지 마)**:
- 모든 useState 초기값 분기 (editData?. ... ?? selected? ... ?? '')
- selectedEv = elevators.find(...)
- isEscalator / floors 계산
- isEscalator 자동 useEffect
- handleSubmit body 객체 구성 (elevatorId/repairDate/repairTarget/hallFloor 조건부/repairItem.trim()/repairDetail.trim() || undefined / repairCompany.trim() || undefined / 4 단계 photos.join(',') 조건부)
- isEdit 분기 → elevatorRepairApi.update(editData.sourceId, body) vs elevatorRepairApi.create(body)
- qc.invalidateQueries({ queryKey: ['elev-repairs'] })
- qc.invalidateQueries({ queryKey: ['elevator_repairs_all'] })
- toast.success(isEdit ? '수리 기록 수정 완료' : '수리 기록 저장 완료')
- toast.error(isEdit ? '수정 실패' : '저장 실패')
- canSubmit = elevatorId && repairTarget && repairItem.trim() && !saving

**ModalWrap 헤더**: `title={isEdit ? "수리 기록 수정" : "수리 기록 입력"}` 보존

**EvSelector 호출**: 변경 전과 동일 props 시그니처 (groups={EV_GROUPS_ANNUAL} esNodes={ES_NODES_ANNUAL}) 보존

**보존 영역 (이번 wave 수정 금지)**:
- function RepairNewModal 바깥의 모든 코드 (다른 모달, 헬퍼, 메인 페이지, 상수, primaryBtnSt/inputSt 본체)
- icons.tsx, tailwind.config.js, 다른 페이지
- MultiPhotoUpload props 호출 시그니처 (label/keys/setKeys 그대로)
- Field 호출 패턴

**lucide-react import**: RepairNewModal 내부에 이모지가 없고 새 아이콘도 불필요 → import 변경 없음.

**커밋 메시지**: `feat(redesign-07-elevator): wave 4 — RepairNewModal v0.1.1 토큰 + Tailwind 변환 (2C 권위)`
  </action>
  <verify>
    <automated>
cd /Users/jykevin/Documents/cbc7119-design/cha-bio-safety && npm run build 2>&1 | tail -25
    </automated>
  </verify>
  <done>
    - RepairNewModal 함수 본체가 Tailwind only 로 변환됨 (수리 대상/홀 층 칩 = warning 토큰)
    - npm run build 성공 (TS 오류 0건)
    - 비즈니스 로직 한 줄도 변경되지 않음 (state/handleSubmit/canSubmit/useEffect/API/qc/toast)
    - edit 모드 분기 보존 + CTA 라벨 "수정 완료" 추가
  </done>
</task>

<task type="auto">
  <name>Task 2: 회귀 검증 — grep gate + 보존 영역 sentinel + 라인 카운트</name>
  <files>cha-bio-safety/src/pages/ElevatorPage.tsx</files>
  <action>
RepairNewModal 변환 영역에 한해 grep gate 통과를 확증. 보존 영역 (다른 모달/헬퍼/메인 페이지/전역 상수) 의 변경 0건을 sentinel grep 으로 검증.

**Step A. RepairNewModal 함수 범위 확정**:
```bash
cd /Users/jykevin/Documents/cbc7119-design/cha-bio-safety

# RepairNewModal 시작 라인
START=$(grep -n "^function RepairNewModal" src/pages/ElevatorPage.tsx | head -1 | cut -d: -f1)
echo "RepairNewModal 시작: $START"

# 다음 function 또는 const 시작 (RepairNewModal 종료)
END=$(awk -v s=$START 'NR>s && /^(function |const |\/\/ ── )/ { print NR; exit }' src/pages/ElevatorPage.tsx)
echo "RepairNewModal 종료: $END"

# 변환 영역 추출
awk -v s=$START -v e=$END 'NR>=s && NR<e' src/pages/ElevatorPage.tsx > /tmp/repair-new-modal.tsx
echo "변환 영역 라인 수: $(wc -l < /tmp/repair-new-modal.tsx)"
```

**Step B. grep gate (RepairNewModal 변환 영역 — /tmp/repair-new-modal.tsx)**:

```bash
# (1) 인라인 style 금지 키 — opacity 화이트리스트만 허용
#     주석 제외 후 카운트
grep -v '^\s*//' /tmp/repair-new-modal.tsx | grep -c "style={{" || true
# 기대: 1 (CTA opacity 1줄만)

# 화이트리스트 검증: style={{ ... 안에 opacity 외 다른 키 없는지
grep -v '^\s*//' /tmp/repair-new-modal.tsx | grep -E "style=\{\{" || true
# 기대: opacity: canSubmit ? 1 : 0.5 한 줄만 출력

# (2) yellow 직접색 0건 (2C 권위 — warning 토큰으로 교체됨)
grep -v '^\s*//' /tmp/repair-new-modal.tsx | grep -c "#eab308" || true
# 기대: 0

# (3) inputSt / primaryBtnSt 사용 0건 (Tailwind 로 직접 교체됨)
grep -v '^\s*//' /tmp/repair-new-modal.tsx | grep -cE "(inputSt|primaryBtnSt)" || true
# 기대: 0

# (4) 9-11px 폰트 0건 (텍스트 토큰 + 12px+ 만)
#     fontSize:9, fontSize:10, fontSize:11 / fontSize:'9..11..' 패턴
grep -v '^\s*//' /tmp/repair-new-modal.tsx | grep -cE "fontSize:\s*['\"]?(9|10|11)['\"]?[,\s\}]" || true
# 기대: 0

# (5) 이모지 0건 (변환 영역)
grep -v '^\s*//' /tmp/repair-new-modal.tsx | grep -cP "[\x{1F300}-\x{1FAFF}\x{2600}-\x{27BF}]" || true
# 기대: 0

# (6) warning 토큰 사용 검증 (수리 대상 + 홀 층 칩 합쳐 4 회 이상 — 선택 시 outline-warning 2회 + bg-warning-bg 2회)
grep -c "bg-warning-bg" /tmp/repair-new-modal.tsx || true
# 기대: 2 이상 (수리 대상 + 홀 층 칩)
grep -c "outline-warning" /tmp/repair-new-modal.tsx || true
# 기대: 2 이상

# (7) accent CTA 검증 (그라디언트 X)
grep -c "bg-accent" /tmp/repair-new-modal.tsx || true
# 기대: 1 이상 (CTA)
grep -c "linear-gradient" /tmp/repair-new-modal.tsx || true
# 기대: 0
```

**Step C. 비즈니스 로직 sentinel (RepairNewModal — /tmp/repair-new-modal.tsx)**:
```bash
# state 13개
grep -c "useState" /tmp/repair-new-modal.tsx
# 기대: 13 (evKind/elevatorId/repairDate/repairTarget/hallFloor/repairItem/repairDetail/repairCompany/partsPhotos/damagedPhotos/duringPhotos/completedPhotos/saving)

# API 호출
grep -c "elevatorRepairApi\.create\|elevatorRepairApi\.update" /tmp/repair-new-modal.tsx
# 기대: 2

# qc.invalidateQueries
grep -c "elev-repairs\|elevator_repairs_all" /tmp/repair-new-modal.tsx
# 기대: 2

# isEscalator useEffect
grep -c "isEscalator.*useEffect\|useEffect.*isEscalator" /tmp/repair-new-modal.tsx
# 기대: 0 (useEffect 본체 안에 isEscalator 사용 — 한 줄 grep 으로 안 잡힘)
grep -c "useEffect" /tmp/repair-new-modal.tsx
# 기대: 1

# canSubmit
grep -c "canSubmit = " /tmp/repair-new-modal.tsx
# 기대: 1

# toast 메시지 4종
grep -c "수리 기록 저장 완료\|수리 기록 수정 완료\|저장 실패\|수정 실패" /tmp/repair-new-modal.tsx
# 기대: 4 (toast.success 2 + toast.error 2)

# EvSelector 호출 시그니처
grep -c "EV_GROUPS_ANNUAL" /tmp/repair-new-modal.tsx
# 기대: 1
grep -c "ES_NODES_ANNUAL" /tmp/repair-new-modal.tsx
# 기대: 1

# 4 단계 MultiPhotoUpload
grep -c "MultiPhotoUpload" /tmp/repair-new-modal.tsx
# 기대: 4 (부품 입고/파손 부품/수리 중/수리 완료)
grep -c "부품 입고 사진\|파손 부품 사진\|수리 중 사진\|수리 완료 사진" /tmp/repair-new-modal.tsx
# 기대: 4
```

**Step D. 보존 영역 sentinel (ElevatorPage.tsx 전체 — 다른 영역 변경 0)**:
```bash
# inputSt / primaryBtnSt 전역 정의 본체 보존 (line 2830~2842)
grep -c "^const primaryBtnSt\|^const inputSt" src/pages/ElevatorPage.tsx
# 기대: 2

# 다른 모달 함수 존재 (Wave 1+2+3 결과)
grep -c "^function FaultNewModal\|^function FaultNewFullscreen\|^function FaultResolveModal" src/pages/ElevatorPage.tsx
# 기대: 3

grep -c "^function InspectModal\|^function EvDetailModal\|^function ModalWrap\|^function Field\|^function MultiPhotoUpload\|^function EvSelector" src/pages/ElevatorPage.tsx
# 기대: 6

# RepairNewModal 호출 사이트 4 곳 보존 (line 1044, 1046, 1671, 2987)
grep -c "<RepairNewModal " src/pages/ElevatorPage.tsx
# 기대: 4
```

**Step E. 시안 권위 — 다른 페이지 / 설정 파일 변경 0건**:
```bash
cd /Users/jykevin/Documents/cbc7119-design
git status --porcelain
# 기대: cha-bio-safety/src/pages/ElevatorPage.tsx 만 modified
```

**Step F. 최종 빌드 + 라인 카운트**:
```bash
cd /Users/jykevin/Documents/cbc7119-design/cha-bio-safety
npm run build 2>&1 | tail -10
wc -l src/pages/ElevatorPage.tsx
# 기대: 빌드 성공, 라인 수 ~3350~3400 (이전 Wave 3: 3345줄, RepairNewModal 변환으로 ~30~60줄 증가)
```

모든 grep gate 통과 + 빌드 성공 + git status 1 파일만 modified 면 PASS. 실패 항목 있으면 Task 1 으로 돌아가 수정.

**커밋 메시지** (Task 1 commit 에 통합 — 별도 commit X. verify 만 수행):
verify 결과를 SUMMARY 에 기록.
  </action>
  <verify>
    <automated>
cd /Users/jykevin/Documents/cbc7119-design/cha-bio-safety && START=$(grep -n "^function RepairNewModal" src/pages/ElevatorPage.tsx | head -1 | cut -d: -f1) && END=$(awk -v s=$START 'NR>s && /^(function |const |\/\/ ── )/ { print NR; exit }' src/pages/ElevatorPage.tsx) && awk -v s=$START -v e=$END 'NR>=s && NR<e' src/pages/ElevatorPage.tsx > /tmp/repair-new-modal.tsx && echo "=== 변환 영역 라인 ===" && wc -l /tmp/repair-new-modal.tsx && echo "=== grep gate ===" && echo "inline style (1=opacity만 통과):" && grep -v '^\s*//' /tmp/repair-new-modal.tsx | grep -c "style={{" && echo "yellow #eab308:" && grep -v '^\s*//' /tmp/repair-new-modal.tsx | grep -c "#eab308" && echo "inputSt/primaryBtnSt 사용:" && grep -v '^\s*//' /tmp/repair-new-modal.tsx | grep -cE "(inputSt|primaryBtnSt)" && echo "9-11px:" && grep -v '^\s*//' /tmp/repair-new-modal.tsx | grep -cE "fontSize:\s*['\"]?(9|10|11)['\"]?[,\s\}]" && echo "bg-warning-bg:" && grep -c "bg-warning-bg" /tmp/repair-new-modal.tsx && echo "bg-accent:" && grep -c "bg-accent" /tmp/repair-new-modal.tsx && echo "linear-gradient:" && grep -c "linear-gradient" /tmp/repair-new-modal.tsx && echo "=== business sentinel ===" && echo "useState:" && grep -c "useState" /tmp/repair-new-modal.tsx && echo "elevatorRepairApi:" && grep -c "elevatorRepairApi\.create\|elevatorRepairApi\.update" /tmp/repair-new-modal.tsx && echo "qc invalidate:" && grep -c "elev-repairs\|elevator_repairs_all" /tmp/repair-new-modal.tsx && echo "MultiPhotoUpload:" && grep -c "MultiPhotoUpload" /tmp/repair-new-modal.tsx && echo "EV_GROUPS_ANNUAL:" && grep -c "EV_GROUPS_ANNUAL" /tmp/repair-new-modal.tsx && echo "=== 보존 영역 ===" && grep -c "^function FaultNewModal\|^function FaultNewFullscreen\|^function FaultResolveModal" src/pages/ElevatorPage.tsx && grep -c "<RepairNewModal " src/pages/ElevatorPage.tsx && echo "=== build ===" && npm run build 2>&1 | tail -5
    </automated>
  </verify>
  <done>
    - 변환 영역 grep gate 모두 통과:
      - 인라인 style={{ 1 (opacity 만)
      - #eab308 0
      - inputSt/primaryBtnSt 0
      - 9-11px 0
      - bg-warning-bg >= 2 (수리 대상 + 홀 층 칩)
      - bg-accent >= 1, linear-gradient 0
    - 비즈니스 sentinel 보존: useState 13, elevatorRepairApi 호출 2, qc invalidate 2, MultiPhotoUpload 4, EV_GROUPS_ANNUAL 1
    - 보존 영역 sentinel: Fault 모달 3종 보존, RepairNewModal 호출 사이트 4 곳 보존
    - npm run build 성공
    - git status: ElevatorPage.tsx 1 파일만 modified
  </done>
</task>

</tasks>

<verification>
- npm run build TS 오류 0
- RepairNewModal 변환 영역 grep gate 통과 (Task 2 자동 verify 결과 기반)
- 비즈니스 로직 sentinel 통과 (useState 13, API 2, qc 2, MultiPhotoUpload 4, EV_GROUPS_ANNUAL 1)
- 보존 영역 sentinel 통과 (Fault 3종, RepairNewModal 호출 4)
- git status: 1 파일만 modified
</verification>

<success_criteria>
- RepairNewModal 함수 본체가 v0.1.1 토큰 + Tailwind only 로 변환됨
- 2C 시안 권위 1:1 매핑 (수리 대상/홀 층 칩 = warning, CTA = accent 단색)
- 인라인 style 1줄 (opacity 화이트리스트), 9-11px 0건, 이모지 0건, yellow 직접색 0건
- edit 모드 분기 + CTA 라벨 "수정 완료" 추가 (시안 권위 정정)
- 비즈니스 로직 100% 보존 (state/handleSubmit/canSubmit/useEffect/API/qc/toast)
- 보존 영역 (다른 모달/헬퍼/메인/전역 상수/다른 페이지/설정) 변경 0건
- npm run build 성공, 1 commit
</success_criteria>

<output>
After completion, create `.planning/quick/260515-jp3-redesign-07-elevator-tsx-wave-4-repairne/260515-jp3-SUMMARY.md` documenting:
- 변환 전/후 라인 범위 + 라인 수 증감
- 2C 시안 권위 매핑 표 (수리 대상/홀 층 칩/CTA)
- grep gate 결과 표
- 비즈니스 로직 보존 sentinel 결과
- edit 모드 라벨 정정 ("수정 완료") 결정 근거
- 다음 wave (Wave 5: EvDetailModal? Wave 6: InspectModal? planner 후속 결정) 후보
</output>
