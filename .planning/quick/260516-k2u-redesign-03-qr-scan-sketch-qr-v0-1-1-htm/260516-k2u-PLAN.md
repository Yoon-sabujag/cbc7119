---
quick_id: 260516-k2u
slug: redesign-03-qr-scan-sketch-qr-v0-1-1-htm
date: 2026-05-16
branch: redesign/03-qr-scan
type: quick
wave: 1
depends_on: []
files_modified:
  - cha-bio-safety/docs/redesign-context/03-qr-scan/sketch/qr-scan-sketch.html
autonomous: true
tags: [sketch, redesign, qr-scan, scan-stage, manual-stage, camera-error, cp-error, header-portal, design-tokens, lucide, v0.1.1]

# ───────────────────────────────────────────────────────────
# redesign/03-qr-scan — QR 스캔 페이지 v0.1.1 시안 HTML
# ───────────────────────────────────────────────────────────
# 07-elevator 옵션 B 3C sketch (safety-mgr-sketch.html, 260515-wvq, cf538a3) 1:1 mirror.
# 인프라(tokens/typography/viewport-frame/meta-label) + verify gate A~G 모두 동일.
# 다음 wave: 3A/3B/3C 와 동일 패턴으로 TSX 변환 (별도 quick task) 예정.

must_haves:
  truths:
    - "단일 HTML 파일 — qr-scan-sketch.html"
    - "[data-theme] 컨테이너 ≥4 (모바일 다크 + 모바일 라이트 + 데스크톱 다크 + 데스크톱 라이트)"
    - "viewport 라벨 ≥4 (📱 모바일 / 🖥️ 데스크톱 × 다크/라이트 명시)"
    - "Scan stage 정상 본문 시각화 — 자체 헤더(좌: 페이지 타이틀 'QR 스캔', 우: '수동입력' ghost 버튼 text-text-secondary) + QR reader 박스 placeholder (max-w-[320px], aspect-square, bg-black, rounded-2xl, ring-1 ring-border-strong, 내부 240×240 코너 4 마커 점선 가이드) + 안내문 'QR 코드를 카메라에 비춰주세요' (text-caption text-text-secondary text-center)"
    - "카메라 에러 카드 시각화 — bg-danger-bg border border-danger-bar/40 rounded-xl p-4 + Camera lucide size=28 text-danger 가운데 + 메시지 (text-caption text-text-secondary whitespace-pre-line leading-relaxed — '카메라 권한이 필요합니다.\\n설정에서 카메라 접근을 허용해주세요.') + 2-col row: 다시 시도(primary) / 수동 입력(ghost)"
    - "cpError 카드 시각화 — bg-danger-bg border border-danger-bar/40 rounded-lg p-2.5 + text-caption text-danger text-center whitespace-pre-line ('QR 코드를 찾을 수 없습니다.\\n(QR-3F-OFF-001)' — 한 줄 'QR-3F-OFF-001' 은 QR 코드 값 예시 — 호기 ID(EV-NN/ES-NN) 패턴과 무관)"
    - "Manual stage 본문 시각화 — 자체 헤더(우: '카메라' ghost text-accent) + 큰 ScanLine lucide size=40 text-accent 가운데 + 라벨 'QR 코드 값' (text-caption bold text-text-secondary) + input (bg-surface-raised border border-border-default text-text-primary rounded-lg px-3 py-2.5 text-body-sm w-full max-w-[320px], placeholder '예: QR-3F-OFF-001' text-text-tertiary) + primary 버튼 '체크포인트 조회' (bg-accent text-on-accent rounded-xl py-3 text-body-sm bold w-full max-w-[320px])"
    - "primary 버튼 3 variant 시각화 — 활성(bg-accent text-on-accent) / 비활성(opacity-50 cursor-not-allowed) / 로딩('조회 중...' + Loader2 lucide size=16 animate-spin)"
    - "Loader2 spinner 4 size 카탈로그 — VP4 영역 3 에 16/20/24/28 4 단계 시각화 (animate-spin Tailwind 내장 — 옛 CSS keyframes 'spin' 0.7s linear infinite 대체)"
    - "이모지 → lucide 매핑 표 — VP4 영역 4 에 3종 매핑 행 시각화 (📷 → Camera / 🔍 → ScanLine / Spinner CSS keyframes → Loader2 animate-spin)"
    - "본문 이모지 0건 — 모두 lucide 매핑 (📷 → Camera / 🔍 → ScanLine / Spinner → Loader2). 옛 fontSize 28/40 의 이모지 글자 시각화는 lucide SVG size 28/40 으로 1:1 교체. viewport 라벨 📱/🖥️ 한정 허용"
    - "9·10·11px 폰트 0건 — 옛 코드 fontSize 11 (헤더 토글/라벨/cpError/카메라 에러 메시지) / fontSize 12 (안내문/카메라 에러 텍스트/ghost btn) / fontSize 13 (input/primary btn) 모두 12px+ 격상 (12 caption / 13 body-sm / 14 body / 16 body 마지노선)"
    - "인라인 style 속성 0건 — 모든 var()/색/spacing/border-radius/padding/width 인라인 → Tailwind className. 동적 값/외부 라이브러리 호환 제약 없음(sketch 단계 — TSX 변환 wave 에서 html5-qrcode 호환 width:100% 한 줄만 인라인 화이트리스트 검토)"
    - "옛 토큰(--bg2/bd/bd2/t1/t2/t3/bg3/acl) 인라인 0건 — 모두 v0.1.1 시각 토큰 (bg-surface-raised / bg-surface-sunken / border-border-default / text-text-primary / text-text-secondary / text-text-tertiary / text-accent / bg-accent) 으로 매핑"
    - "color/border 결정 시각화 — 카메라 에러 카드 / cpError: bg-danger-bg + border-danger-bar/40 + text-danger (옛 rgba(239,68,68,.08~.25) → v0.1.1 danger 페어). 헤더 토글 scan→manual='수동입력' text-text-secondary / manual→scan='카메라' text-accent. 안내문 text-text-secondary. QR reader bg-black + ring-1 ring-border-strong."
    - "primary 버튼 배경 결정 — 'bg-accent text-on-accent' 채택 (3C sketch 와 일관, 단색). 옛 'linear-gradient(135deg,#1d4ed8,#0ea5e9)' 그라디언트는 sketch 본문에서 사용 X. 단 VP4 영역 2 비교 카탈로그에 옛 그라디언트 1행 + 새 bg-accent 1행 시각화로 변환 결정 근거 명시 가능 (옵션)"
    - "데이터 보존: Stage 'scan'|'manual' 분기 / HEADER_PORTAL_ID portal slot / Html5Qrcode startCamera/stopCamera / scannedRef / scannerRef / lookupCheckpoint API / navigate('/inspection', state) / camError/cpError/loading/scanning state / ultra wide deviceId + zoom 0.5x 안전망 / Spinner 서브 컴포넌트 — 모두 시각만 다듬고 라벨/분기 100% 보존"
    - "자체 헤더 (좌: 'QR 스캔' 타이틀, 우: stage 토글 ghost 버튼) — 3C sister sketch 의 미해결칩+6탭 패턴 X, QR 스캔은 'GlobalHeader portal slot' 패턴이라 헤더 우측 1 버튼만 노출"
  artifacts:
    - path: "cha-bio-safety/docs/redesign-context/03-qr-scan/sketch/qr-scan-sketch.html"
      provides: "QR 스캔 페이지 v0.1.1 sketch — Scan stage 정상/카메라 에러/cpError + Manual stage + primary 버튼 3 variant + Loader2 4 size + 이모지→lucide 매핑표 4 viewport 시각화"
      min_lines: 800
      max_lines: 2500
  key_links:
    - from: "qr-scan-sketch.html"
      to: "safety-mgr-sketch.html (3C, cf538a3)"
      via: "tokens.css 다크/라이트 + viewport-frame + meta-label + typography 7단계 100% 재사용 (인프라 source)"
      pattern: "\\[data-theme=\"(dark|light)\"\\]"
    - from: "qr-scan-sketch.html"
      to: "safety-mgr-sketch.html (3C, cf538a3)"
      via: "state-label 박스 패턴 (Scan 정상/카메라 에러/cpError/Manual/Loading 5 상태 라벨 박스로 응용) + 이모지→lucide 매핑표 패턴"
      pattern: "state-label|icon-mapping"
    - from: "qr-scan-sketch.html"
      to: "cha-bio-safety/src/pages/QRScanPage.tsx (line 1~297)"
      via: "비즈니스 로직 100% 보존 시각 권위 — 다음 wave TSX 변환 1:1 매핑 source"
      pattern: "Stage|HEADER_PORTAL_ID|Html5Qrcode"

verify_before_commit:
  - id: 1
    name: 라인 수
    target: "800-2500"
    cmd: "wc -l cha-bio-safety/docs/redesign-context/03-qr-scan/sketch/qr-scan-sketch.html"
  - id: 2
    name: 9·10·11px 폰트
    target: "0건"
    cmd: "grep -cE 'font-size:\\s*(9|10|11)px|fontSize:(9|10|11)\\b' cha-bio-safety/docs/redesign-context/03-qr-scan/sketch/qr-scan-sketch.html"
  - id: 3
    name: 본문 이모지 (viewport 라벨 한정 📱/🖥️ 만 허용)
    target: "0건"
    cmd: "grep -cP '[\\x{1F300}-\\x{1F9FF}]|📷|🔍|✓|✗' cha-bio-safety/docs/redesign-context/03-qr-scan/sketch/qr-scan-sketch.html | grep -v 'viewport.*📱\\|viewport.*🖥️'"
  - id: 4
    name: 인라인 style 속성
    target: "0건"
    cmd: "grep -c 'style=\"' cha-bio-safety/docs/redesign-context/03-qr-scan/sketch/qr-scan-sketch.html"
  - id: 5
    name: viewport [data-theme] >=4
    target: ">=4"
    cmd: "grep -c '\\[data-theme=' cha-bio-safety/docs/redesign-context/03-qr-scan/sketch/qr-scan-sketch.html"
  - id: 6
    name: 옛 토큰 인라인 0건 (--bg2/bd/bd2/t1/t2/t3/bg3/acl)
    target: "0건"
    cmd: "grep -cE 'var\\(--(bg2|bd|bd2|t1|t2|t3|bg3|acl)\\)' cha-bio-safety/docs/redesign-context/03-qr-scan/sketch/qr-scan-sketch.html"
  - id: 7
    name: 코드 변경 0건 (docs/redesign-context + .planning/ 외)
    target: "0건"
    cmd: "git diff --name-only HEAD | grep -v 'docs/redesign-context' | grep -v '.planning/' | wc -l"
---

<objective>
redesign/03-qr-scan — QR 스캔 페이지 (`src/pages/QRScanPage.tsx`, 297라인) 의 v0.1.1 시안 HTML 작성. 07-elevator 옵션 B 3C sketch (safety-mgr-sketch.html, cf538a3, 260515-wvq) 의 인프라/tokens/typography/viewport/카드 패턴 100% mirror. 코드 변경 0건. 다음 wave (별도 quick task) 에서 TSX 변환 1:1 매핑 source 로 사용.

Purpose: 디자인 토큰 v0.1.1 일관성 확보. 옛 QRScanPage.tsx 는 var(--bg/bg2/bg3/bd/bd2/t1/t2/t3/acl) 인라인 다수 + 이모지 📷🔍 + fontSize 11/12/13 + 그라디언트 primary 버튼 + Spinner CSS keyframes 등 옛 패턴. 본 sketch 권위로 시각 잡고 변환 wave 진입.

Output: 단일 sketch HTML — 4 viewport × Scan stage (정상/카메라 에러/cpError) + Manual stage + primary 버튼 3 variant + Loader2 spinner 4 size + 이모지→lucide 매핑표. 코드 변경 0건. 다음 wave 의 변환 source.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/quick/260516-k2u-redesign-03-qr-scan-sketch-qr-v0-1-1-htm/

# 메모리 권위
# - project_redesign_workflow.md → redesign/NN 브랜치 + 즉시 푸시 / 시안 HTML 먼저 → TSX 변환 / Claude Design 불필요
# - feedback_redesign_sketch_rule_enforcement.md → §6.1 색 토큰 분리 + verify gate 4중 강화
# - feedback_sketch_realistic_data.md → 표시 분기/라벨 코드 그대로, 시각만 손봄
# - project_design_tokens_branch.md → v0.1.1 시각 토큰 룰 (bg-surface-raised / text-text-primary 등)
# - feedback_design_sketch_first.md → spacing/sizing 도 sketch HTML 로 시안 보여주고 승인 받은 후 인라인 적용

# 패턴 참고 (재사용 — 인프라 source, 1:1 mirror)
# - cha-bio-safety/docs/redesign-context/07-elevator/sketch/safety-mgr-sketch.html (3C — cf538a3, 1596라인)
#   · <style> tokens.css 다크/라이트 변수 정의 (v0.1.1)
#   · viewport-frame class (모바일 393×852 / 데스크톱 1280×720)
#   · meta-label class (viewport 라벨 + 설명, 📱/🖥️ 이모지 메타 한정 허용)
#   · typography 7단계 (text-caption / text-body-sm / text-body / text-title / text-heading 등)
#   · state-label class (5 상태 라벨 박스 패턴)
#   · lucide stroke svg 패턴 (User/BookOpen/Building2/ElevatorIcon/MoveDiagonal/CheckCircle2/X 등 임베드 source)

# 코드 source (Read 검증 완료 — 2026-05-16)
# - cha-bio-safety/src/pages/QRScanPage.tsx (297라인)
#   · Stage 'scan'|'manual' 분기 (line 9, 17)
#   · HEADER_PORTAL_ID = 'qr-header-portal-slot' (line 12) — createPortal 로 GlobalHeader 토글 버튼 주입
#   · headerToggleBtn (line 171~186): scan='수동입력' var(--t2) / manual='카메라' var(--acl). 32 height, padding 0 10, radius 7, bg var(--bg3), border 1px var(--bd), fontSize 11, fontWeight 600
#   · Scan stage 본문 (line 199~231):
#     · QR reader 박스 (line 201~208): width 100% maxWidth 320, borderRadius 20, overflow hidden, bg #000, boxShadow '0 0 0 1px var(--bd2)', position relative. 내부 div#qr-reader-region width 100%. loading overlay rgba(0,0,0,0.6) absolute inset 0 + Spinner
#     · 카메라 에러 카드 (line 211~218): bg rgba(239,68,68,.08) border 1px rgba(239,68,68,.2) radius 12 pad 14 16. 📷 fontSize 28 marginBottom 8. 메시지 fontSize 12 var(--t2) lineHeight 1.6 whiteSpace pre-line. 2 버튼 row (다시 시도 primary + 수동 입력 ghost)
#     · 정상 안내 (line 219~223): fontSize 12 var(--t2) textAlign center 'QR 코드를 카메라에 비춰주세요'
#     · cpError (line 225~229): bg rgba(239,68,68,.1) border 1px rgba(239,68,68,.25) radius 10 pad 10 13 fontSize 11 var(--danger) textAlign center whiteSpace pre-line
#   · Manual stage 본문 (line 234~261):
#     · 🔍 (line 236): fontSize 40 textAlign center
#     · 라벨 'QR 코드 값' (line 238): fontSize 11 fontWeight 700 var(--t2)
#     · input (line 239~246): inputSt - width 100% pad 11 13 radius 10 bg var(--bg2) border 1px var(--bd2) color var(--t1) fontSize 13. placeholder '예: QR-3F-OFF-001'. autoFocus + onKeyDown Enter handleManualSearch
#     · primary 버튼 (line 248~254): primaryBtnSt - width 100% pad 13 0 radius 12 background 'linear-gradient(135deg,#1d4ed8,#0ea5e9)' color #fff fontSize 13 fontWeight 700 boxShadow '0 4px 14px rgba(37,99,235,0.35)'. disabled 시 opacity 0.5. loading 시 텍스트 '조회 중...'
#   · Spinner 서브 컴포넌트 (line 270~276): 28×28 border 2px rgba(255,255,255,.2) borderTopColor var(--acl) borderRadius 50% animation 'spin .7s linear infinite' + keyframes inline
#   · primaryBtnSt / ghostBtnSt / inputSt (line 279~297): 공통 스타일 객체

# 색 매핑 (사용자 의도 — 메모리 + 코드 권위)
# - 카메라 에러 / cpError: rgba(239,68,68,*) → v0.1.1 danger 페어 (bg-danger-bg + border-danger-bar/40 + text-danger). text-text-secondary 메시지 색.
# - 헤더 토글 scan='수동입력' var(--t2) → text-text-secondary / manual='카메라' var(--acl) accent → text-accent.
# - 안내문 var(--t2) → text-text-secondary.
# - QR reader 검정 → bg-black. boxShadow '0 0 0 1px var(--bd2)' → ring-1 ring-border-strong (또는 outline outline-1 outline-border-default).
# - primary 버튼: 옛 그라디언트 'linear-gradient(135deg,#1d4ed8,#0ea5e9)' → bg-accent text-on-accent (3C 일관, 단순). VP4 영역 2 에 옛 그라디언트 1행 비교 시각화 옵션.
# - ghost 버튼: var(--bg2)/var(--bd2)/var(--t2) → bg-surface-raised border border-border-default text-text-secondary.
# - input: var(--bg2)/var(--bd2)/var(--t1) → bg-surface-raised border border-border-default text-text-primary.
# - Spinner: var(--acl) → text-accent. CSS keyframes → Loader2 lucide animate-spin.

# 이모지 → lucide (3종 매핑)
# - 📷 (카메라 에러 fontSize 28) → Camera lucide size=28 text-danger
# - 🔍 (manual stage fontSize 40) → ScanLine lucide size=40 text-accent (QR 컨텍스트 시각적 적합)
# - Spinner CSS keyframes 'spin .7s linear infinite' → Loader2 lucide animate-spin (Tailwind 내장)

@cha-bio-safety/src/pages/QRScanPage.tsx
@cha-bio-safety/docs/redesign-context/03-qr-scan/03-qr-scan.md
@cha-bio-safety/docs/redesign-context/03-qr-scan/design-system.md
@cha-bio-safety/docs/redesign-context/03-qr-scan/tokens.css
@cha-bio-safety/docs/redesign-context/07-elevator/sketch/safety-mgr-sketch.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: qr-scan-sketch.html 작성</name>
  <files>cha-bio-safety/docs/redesign-context/03-qr-scan/sketch/qr-scan-sketch.html</files>
  <action>
sketch 디렉토리 신규 생성 필요 — `cha-bio-safety/docs/redesign-context/03-qr-scan/sketch/` 가 아직 없음. 파일 작성 시 부모 디렉토리 자동 생성 (Write 도구는 부모 생성 안 함 — 필요 시 `mkdir -p` 선행).

직전 sister sketch (safety-mgr-sketch.html — 3C, cf538a3, 1596라인 mirror source) 의 tokens.css + viewport-frame + meta-label + typography + state-label 인프라 100% 재사용. 자체 헤더 패턴은 QR 스캔용으로 단순화 (좌 타이틀 'QR 스캔' + 우 stage 토글 ghost 버튼 1개). 본문에 4 viewport 영역 작성.

### viewport 구성 (4개 — 3C 와 동일 구조)

**VP1 모바일·다크 — Scan stage 정상 (헤더 토글 + reader + 안내)**

- 자체 헤더 (flex items-center justify-between px-4 py-3 border-b border-border-subtle bg-surface-page):
  - 좌측: 'QR 스캔' (text-title bold text-text-primary)
  - 우측: '수동입력' ghost 버튼 (bg-surface-raised border border-border-default text-text-secondary text-caption bold rounded-md px-2.5 h-8 inline-flex items-center)
- 본문 (flex flex-col items-center px-4 py-5 gap-4):
  - QR reader 박스 (w-full max-w-[320px] aspect-square bg-black rounded-2xl ring-1 ring-border-strong relative overflow-hidden):
    - 내부 가운데 240×240 코너 4 마커 점선 가이드 (인라인 style 0건 — CSS class `.qr-corner-tl/tr/bl/br` 정의 또는 absolute Tailwind 클래스로 4개 div positioning)
    - placeholder 텍스트 'html5-qrcode 라이브러리가 video 삽입' (text-caption text-white/60 가운데, 시안 전용 안내)
  - 안내문 'QR 코드를 카메라에 비춰주세요' (text-caption text-text-secondary text-center)
  - state-label 박스: '상태: scan / camError=null / cpError=null (정상 카메라 활성)'

**VP2 모바일·라이트 — Scan stage 에러 (카메라 에러 + cpError)**

- 자체 헤더 (좌: 'QR 스캔', 우: '수동입력' ghost) — VP1 과 동일 패턴, 라이트 토큰 자동 분기
- 영역 1: 카메라 에러 카드 (w-full max-w-[320px] bg-danger-bg border border-danger-bar/40 rounded-xl p-4 text-center):
  - Camera lucide size=28 text-danger 가운데 (mb-2)
  - 메시지 (text-caption text-text-secondary leading-relaxed whitespace-pre-line mb-3): '카메라 권한이 필요합니다.\n설정에서 카메라 접근을 허용해주세요.'
  - 2-col row (flex gap-2):
    - 다시 시도 (primary — bg-accent text-on-accent text-body-sm bold rounded-xl py-3 flex-1)
    - 수동 입력 (ghost — bg-surface-raised border border-border-default text-text-secondary text-caption bold rounded-xl py-3 flex-1)
  - state-label 박스: '상태: scan / camError=값 있음'
- 영역 2: cpError 카드 (w-full max-w-[320px] bg-danger-bg border border-danger-bar/40 rounded-lg p-2.5):
  - text-caption text-danger text-center whitespace-pre-line leading-relaxed:
    'QR 코드를 찾을 수 없습니다.\n(QR-3F-OFF-001)'
  - state-label 박스: '상태: cpError=값 있음 (조회 실패 또는 미발견)'

**VP3 데스크톱·다크 — Manual stage + primary 버튼 3 variant**

- 자체 헤더 (좌: 'QR 스캔', 우: '카메라' ghost text-accent — stage='manual' 일 때 토글)
- 본문 가운데 정렬 (flex flex-col items-center justify-center px-6 py-12 gap-4):
  - ScanLine lucide size=40 text-accent 가운데
  - 라벨 'QR 코드 값' (text-caption bold text-text-secondary self-start max-w-[320px] w-full mb-1.5)
  - input (w-full max-w-[320px] bg-surface-raised border border-border-default text-text-primary rounded-lg px-3 py-2.5 text-body-sm focus:outline-none focus:ring-1 focus:ring-accent placeholder:text-text-tertiary, placeholder '예: QR-3F-OFF-001')
  - primary 버튼 3 variant 가로 row (flex gap-2 w-full max-w-[640px] mt-2 — 시안에서 3 variant 한눈 비교):
    - 활성: '체크포인트 조회' (bg-accent text-on-accent text-body-sm bold rounded-xl py-3 flex-1)
    - 비활성: '체크포인트 조회' (bg-accent text-on-accent text-body-sm bold rounded-xl py-3 flex-1 opacity-50 cursor-not-allowed)
    - 로딩: '조회 중...' + Loader2 size=16 animate-spin (bg-accent text-on-accent text-body-sm bold rounded-xl py-3 flex-1 inline-flex items-center justify-center gap-1.5)
  - state-label 박스 3개 — 활성('manualQr 입력됨, loading=false') / 비활성('manualQr=공백 또는 loading=true') / 로딩('lookupCheckpoint 진행 중')

**VP4 데스크톱·라이트 — 카탈로그 + 변환 매핑표**

- 영역 1 (상단 ~25%): Scan stage 4 상태 카탈로그 row (가로 비교):
  - 헤더 (text-body bold text-text-primary): 'Scan stage 상태 카탈로그'
  - 4-col grid gap-3:
    - col 1: '정상' — text-caption text-text-secondary '안내문' + state-label 'scan/정상'
    - col 2: '카메라 에러' — bg-danger-bg + Camera + 짧은 메시지 + state-label 'camError 활성'
    - col 3: 'cpError' — bg-danger-bg + 짧은 메시지 + state-label 'cpError 활성'
    - col 4: '로딩' — bg-black rounded + Loader2 size=24 + state-label 'loading 오버레이'
- 영역 2 (상단 ~25%): primary 버튼 패턴 카탈로그 (가로 비교):
  - 헤더 'primary/ghost/accent ghost 버튼 카탈로그'
  - 4-col grid gap-3 — 각 col 에 버튼 + 라벨 (text-caption text-text-tertiary mt-1):
    - col 1: primary 활성 (bg-accent text-on-accent) + 'primary 활성'
    - col 2: primary 비활성 (opacity-50) + 'primary 비활성 (disabled)'
    - col 3: primary 로딩 (Loader2) + 'primary 로딩'
    - col 4: ghost (bg-surface-raised border) + 'ghost (수동입력/수동 입력 fallback/카메라)'
  - 옵션 row 추가 — 옛 그라디언트 비교 (text-caption text-text-tertiary): 'OLD: linear-gradient(135deg,#1d4ed8,#0ea5e9) — 코드 source 보존 / NEW: bg-accent text-on-accent (3C 일관)'
- 영역 3 (하단 ~25%): Loader2 spinner 4 size 카탈로그:
  - 헤더 'Loader2 spinner size 카탈로그 (animate-spin)'
  - 4-col grid gap-3 — 각 col 에 Loader2 + 라벨:
    - col 1: size=16 + 'size 16 (버튼 inline)'
    - col 2: size=20 + 'size 20'
    - col 3: size=24 + 'size 24'
    - col 4: size=28 + 'size 28 (옛 Spinner 28×28 1:1)'
- 영역 4 (하단 ~25%): 이모지 → lucide 매핑표:
  - 헤더 '이모지 → lucide 매핑 (변환 룰)'
  - 3 row (3-col grid 또는 표 형태 — 옛 / 새 / 비고):
    - row 1: '📷 fontSize 28 (카메라 에러)' → 'Camera size=28 text-danger' — 비고 'lucide-react Camera'
    - row 2: '🔍 fontSize 40 (manual stage)' → 'ScanLine size=40 text-accent' — 비고 'QR 컨텍스트 시각적 적합 (Search 대신)'
    - row 3: 'Spinner CSS keyframes spin .7s' → 'Loader2 animate-spin' — 비고 'Tailwind 내장, JS 불필요'

### 디자인 룰 (verify gate 강제)

- 폰트: 9·10·11px 0건. 최소 12px (text-caption). 13px = body-sm (input/primary btn). 14px = body. 16px = body 마지노선. 18px = title (페이지 헤더). text-title = 18px (3C 사용 패턴).
- 색: 카메라 에러 / cpError → bg-danger-bg + border-danger-bar/40 + text-danger. 헤더 토글 scan→manual='수동입력' text-text-secondary / manual→scan='카메라' text-accent. 안내문 text-text-secondary. QR reader bg-black + ring-1 ring-border-strong. primary bg-accent text-on-accent. ghost bg-surface-raised border-border-default text-text-secondary. input bg-surface-raised border-border-default text-text-primary.
- 아이콘: lucide 사용 — Camera (📷 1:1) / ScanLine (🔍 1:1) / Loader2 (Spinner 1:1 + animate-spin). lucide-react 의 SVG path 그대로 inline SVG 임베드 (3C 패턴 — User/BookOpen/Building2 svg path 임베드 한 방식 그대로).
- 인라인 style 속성 0건. 모두 CSS class 또는 Tailwind. 가이드 코너 4 마커는 CSS class 정의 또는 Tailwind absolute + border 클래스.
- 다크/라이트 둘 다 동일 컨텐츠. tokens.css [data-theme] 분기 사용. [data-theme="dark"]/[data-theme="light"] selector ≥4.
- viewport 라벨: 화면 좌상단 모바일/데스크톱 + 다크/라이트 명시 (📱/🖥️ 이모지는 viewport 라벨 한정 허용 — 본문 0건 규칙은 그대로).

### 인프라 재사용 (safety-mgr-sketch.html — 3C 에서 그대로, 1:1 mirror)

- <style> 안의 tokens.css 다크/라이트 변수 정의 (v0.1.1) — `--surface-page` / `--surface-raised` / `--surface-sunken` / `--text-primary/secondary/tertiary` / `--status-danger/warning/safe/info/fire` (foreground/bar/bg 페어) / `--accent` / `--text-on-accent` / `--border-default/strong/subtle` / `--radius-sm/md/lg` 등
- viewport-frame class (모바일 393×852 / 데스크톱 1280×720)
- meta-label class (viewport 라벨 + 설명, 📱/🖥️ 이모지 메타 한정 허용)
- typography 7단계 (text-caption 12 / text-label 13 / text-body-sm 14 / text-body 16 / text-title 18 / text-heading 22 / text-display 28)
- state-label class (3C 의 5 상태 라벨 박스 패턴 — VP1/VP2/VP3 state-label 5종 + VP4 카탈로그 라벨 적용)
- lucide icon stroke svg — 신규 매핑 3종 임베드: Camera / ScanLine / Loader2 (lucide-react v0.454.0 의 SVG path 그대로)
- 카드 좌측 색바 ::before pseudo 패턴은 본 sketch 에는 불필요 (QR 스캔 카드는 색바 없는 평면 카드)

### 보존 항목 (절대 건드리지 말 것)

- QRScanPage.tsx 본체 코드 (단 한 줄도 수정 X — Stage 분기 / HEADER_PORTAL_ID portal / Html5Qrcode startCamera/stopCamera / scannedRef / scannerRef / lookupCheckpoint API / navigate 호출 / camError/cpError/loading/scanning state / ultra wide deviceId + zoom 0.5x 안전망 / Spinner 서브 컴포넌트 / primaryBtnSt/ghostBtnSt/inputSt 객체 모두 보존)
- GlobalHeader.tsx / icons.tsx / tailwind.config.js / 다른 sketch HTML (07-elevator 3A/3B/3C 포함 0건 수정)
- 다른 페이지 / 컴포넌트

### 작업 순서 (안전)

1. `mkdir -p cha-bio-safety/docs/redesign-context/03-qr-scan/sketch/` 로 sketch 디렉토리 생성.
2. safety-mgr-sketch.html (cf538a3) 의 <head>+<style>+<body> 인프라 + viewport-frame + meta-label + typography + state-label 클래스 + lucide SVG 임베드 패턴 복사 → qr-scan-sketch.html 새 파일.
3. <style> 에 lucide 신규 icon stroke 3종 추가: Camera, ScanLine, Loader2 (svg path 임베드 — lucide-react v0.454.0 source).
4. 자체 헤더 패턴 QR 스캔용으로 단순화 — 미해결 칩/6탭 X, 좌 'QR 스캔' 타이틀 + 우 stage 토글 ghost 1개.
5. <style> 또는 Tailwind 에 QR reader 코너 4 마커 가이드 클래스 정의 (인라인 style 회피).
6. VP1 영역 작성 (Scan stage 정상 — reader + 안내 + state-label).
7. VP2 영역 작성 (Scan stage 에러 — 카메라 에러 카드 + cpError 카드 + state-label).
8. VP3 영역 작성 (Manual stage — ScanLine 헤더 + input + primary 3 variant + state-label).
9. VP4 영역 작성 (Scan 4 상태 카탈로그 + 버튼 카탈로그 + Loader2 size 카탈로그 + 이모지→lucide 매핑표).
10. verify gate 7개 모두 PASS 확인 (특히 Section 2 9·10·11px 0건 — 옛 코드 fontSize 11 다수 격상 필요 + Section 4 인라인 style 0건).
11. commit (Task 2 에서 처리).
  </action>
  <verify>
    <automated>
cd /Users/jykevin/Documents/cbc7119-design && \
  echo "=== Section A: 라인 수 ===" && \
  wc -l cha-bio-safety/docs/redesign-context/03-qr-scan/sketch/qr-scan-sketch.html && \
  test $(wc -l < cha-bio-safety/docs/redesign-context/03-qr-scan/sketch/qr-scan-sketch.html) -ge 800 && \
  test $(wc -l < cha-bio-safety/docs/redesign-context/03-qr-scan/sketch/qr-scan-sketch.html) -le 2500 && \
  echo "" && \
  echo "=== Section B: 9·10·11px 폰트 0건 ===" && \
  test $(grep -cE 'font-size:\s*(9|10|11)px|fontSize:(9|10|11)\b' cha-bio-safety/docs/redesign-context/03-qr-scan/sketch/qr-scan-sketch.html) -eq 0 && \
  echo "" && \
  echo "=== Section C: 인라인 style 속성 0건 ===" && \
  test $(grep -c 'style="' cha-bio-safety/docs/redesign-context/03-qr-scan/sketch/qr-scan-sketch.html) -eq 0 && \
  echo "" && \
  echo "=== Section D: viewport [data-theme] >=4 ===" && \
  test $(grep -c '\[data-theme=' cha-bio-safety/docs/redesign-context/03-qr-scan/sketch/qr-scan-sketch.html) -ge 4 && \
  echo "" && \
  echo "=== Section E: 옛 토큰 인라인 0건 (--bg2/bd/bd2/t1/t2/t3/bg3/acl) ===" && \
  test $(grep -cE 'var\(--(bg2|bd|bd2|t1|t2|t3|bg3|acl)\)' cha-bio-safety/docs/redesign-context/03-qr-scan/sketch/qr-scan-sketch.html) -eq 0 && \
  echo "" && \
  echo "=== Section F: 본문 이모지 0건 (viewport 라벨 📱/🖥️ 한정 허용) ===" && \
  EMOJI_COUNT=$(grep -oP '📷|🔍|✓|✗' cha-bio-safety/docs/redesign-context/03-qr-scan/sketch/qr-scan-sketch.html | wc -l) && \
  test $EMOJI_COUNT -eq 0 && \
  echo "" && \
  echo "=== Section G: 코드 변경 0건 (docs/redesign-context + .planning/ 외) ===" && \
  test $(git diff --name-only HEAD | grep -v 'docs/redesign-context' | grep -v '.planning/' | wc -l) -eq 0 && \
  echo "" && \
  echo "=== ALL VERIFY GATES PASS ==="
    </automated>
  </verify>
  <done>
- qr-scan-sketch.html 작성 완료 (800~2500 라인)
- verify gate Section A~G 모두 PASS
- 코드 0건 변경 (단일 sketch HTML 만)
- QRScanPage.tsx / GlobalHeader.tsx / icons.tsx / tailwind.config.js / 다른 컴포넌트 / 다른 sketch HTML (07-elevator 3A/3B/3C 포함) 0건 수정
  </done>
</task>

<task type="auto">
  <name>Task 2: SUMMARY 작성 + commit + push</name>
  <files>.planning/quick/260516-k2u-redesign-03-qr-scan-sketch-qr-v0-1-1-htm/260516-k2u-SUMMARY.md</files>
  <action>
SUMMARY 작성 후 single commit + push.

### SUMMARY 필수 섹션

- **What changed**: 단일 HTML sketch (qr-scan-sketch.html) 작성. 4 viewport × Scan stage (정상 + 카메라 에러 + cpError) + Manual stage (ScanLine + input + primary 3 variant) + Scan 4 상태 카탈로그 + 버튼 카탈로그 + Loader2 size 카탈로그 + 이모지→lucide 매핑표 본문 시각화.
- **Why**: redesign/03-qr-scan 브랜치 v0.1.1 시안 권위 확보. 07-elevator 옵션 B 3C sketch (cf538a3) 1:1 mirror. 다음 wave (별도 quick task) TSX 변환 1:1 매핑 source.
- **Design decisions**:
  - 색 매핑: 카메라 에러 / cpError → bg-danger-bg + border-danger-bar/40 + text-danger (rgba(239,68,68,.08~.25) 그라디언트 패턴 → v0.1.1 danger 페어). 헤더 토글 scan='수동입력' text-text-secondary / manual='카메라' text-accent. primary 버튼 'bg-accent text-on-accent' 채택 (3C 일관, 단순. 옛 그라디언트는 비교 카탈로그에만 한 줄 시각화).
  - 폰트 격상: 옛 fontSize 11 (헤더 토글 / 라벨 / cpError / 카메라 에러 메시지) / 12 (안내문 / ghost btn) / 13 (input / primary btn) → 12px+ 격상 (12 caption / 13 body-sm / 14 body). 9·10·11px 0건.
  - 이모지 → lucide (3종): 📷 → Camera size=28 text-danger / 🔍 → ScanLine size=40 text-accent (QR 컨텍스트 시각적 적합) / Spinner CSS keyframes → Loader2 animate-spin (Tailwind 내장).
  - 인라인 style 0건: 옛 inputSt/primaryBtnSt/ghostBtnSt 객체 + 30+ 인라인 var() → 모두 Tailwind className.
  - 가이드 코너 4 마커: CSS class 또는 Tailwind absolute + border 로 인라인 회피.
- **Preserved**: 모든 코드 0건 변경. QRScanPage.tsx / GlobalHeader.tsx / icons.tsx / tailwind.config.js / 07-elevator 3A·3B·3C sketch 0건 수정. Stage 분기 / HEADER_PORTAL_ID portal / Html5Qrcode startCamera/stopCamera / scannedRef/scannerRef / lookupCheckpoint API / navigate / camError/cpError/loading/scanning state / ultra wide deviceId + zoom 0.5x 안전망 / Spinner 서브 컴포넌트 모두 보존.
- **Verification**: verify gate Section A~G 7/7 PASS. npm build 무관 (HTML sketch).
- **Out of scope**: TSX 변환 (다음 quick task — 별도 wave). html5-qrcode 라이브러리 호환 (TSX 변환 wave 에서 `#qr-reader-region` width:100% 인라인 한 줄 화이트리스트 검토). 데스크톱 분기 호환성 검증 (모바일 주 사용 환경 — `03-qr-scan.md` 섹션 2 권위).
- **Next**: 사용자 검수 → TSX 변환 wave (별도 quick task) 진입.

### Commit + Push

```bash
cd /Users/jykevin/Documents/cbc7119-design
git add cha-bio-safety/docs/redesign-context/03-qr-scan/sketch/qr-scan-sketch.html
git commit -m "feat(260516-k2u): redesign/03-qr-scan sketch — QR 스캔 v0.1.1 시안 HTML"
git push origin redesign/03-qr-scan
```

배포 X. redesign/03-qr-scan 브랜치 push 만. SUMMARY + PLAN 은 orchestrator 가 별도 docs commit 처리.
  </action>
  <verify>
    <automated>
cd /Users/jykevin/Documents/cbc7119-design && \
  ls .planning/quick/260516-k2u-redesign-03-qr-scan-sketch-qr-v0-1-1-htm/260516-k2u-SUMMARY.md && \
  git log --oneline -1 | grep -q '260516-k2u'
    </automated>
  </verify>
  <done>
- SUMMARY.md 작성 완료
- single commit (sketch HTML 만)
- redesign/03-qr-scan push (main 머지 X, 배포 X)
  </done>
</task>

</tasks>

<success_criteria>
- [ ] qr-scan-sketch.html 800~2500 라인
- [ ] [data-theme] 컨테이너 ≥4
- [ ] 본문 이모지 0건 (viewport 라벨 📱/🖥️ 한정 허용)
- [ ] 9·10·11px 0건
- [ ] 인라인 style 속성 0건
- [ ] var(--bg2/bd/bd2/t1/t2/t3/bg3/acl) 인라인 0건
- [ ] 코드 0건 변경
- [ ] SUMMARY + single commit + push (redesign/03-qr-scan, main 머지 X, 배포 X)
</success_criteria>

<output>
After completion, ensure `.planning/quick/260516-k2u-redesign-03-qr-scan-sketch-qr-v0-1-1-htm/260516-k2u-SUMMARY.md` exists.
</output>
</content>
</invoke>