---
phase: quick-260522-209
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cha-bio-safety/docs/redesign-context/28-splash/wave-1-index.md
autonomous: true
requirements:
  - REDESIGN-28-WAVE1
must_haves:
  truths:
    - "wave-1-index.md 파일이 cha-bio-safety/docs/redesign-context/28-splash/ 직속에 생성됨 (sketch/ 서브폴더 X — 13-schedule + 14-reports + 27-login + 16-workshift + 17-annual-plan 평면 패턴 mirror)"
    - "7개 필수 섹션(§1~§7) 모두 채워짐"
    - "인벤토리 표가 SplashScreen.tsx (83 lines) + InstallPrompt.tsx (237 lines) 두 파일을 모두 포함 — 5 영역 (Splash 외곽+로고+타이틀, Splash 진행 바+버전, InstallPrompt 오버레이+카드+메인 view, InstallPrompt Android 가이드 view, InstallPrompt iOS 가이드 view) + 비즈 시그니처 박스 분리"
    - "4 sub-wave 분배 표가 W2~W5 행을 모두 포함 (SplashScreen + InstallPrompt 통합 320 lines → 4 sub-wave: W2=Splash, W3=InstallPrompt 메인, W4=InstallPrompt 가이드 (Android+iOS), W5=markdown TSX checklist)"
    - "design-system.md §1.1/§1.2/§1.3/§6/§7/§7.1 인용이 fence 안 verbatim 으로 포함 (§6/§7 은 미적용 1줄 메타 동반)"
    - "메모리 룰 12개가 inline 인용 (10건 + 28-splash 특화 2건 — feedback_tsx_wave_emoji_dot_gap 의 특수 글리프 ⋮ ⎋ 예외 결정 룰 + project_redesign_15_daily_report_status precedent 패턴)"
    - "negative rule 섹션이 sketch HTML 금지 / 코드 수정 금지 (SplashScreen + InstallPrompt + pwaInstall + versionCheck 4파일) / wrangler 금지 / npm run deploy 금지 / 평면 폴더 / App.tsx 미수정 / __APP_VERSION__ 인젝션 보존 / @keyframes slideUp 보존 / 비즈 시그니처 변경 금지 (7건) 9건 포함"
    - "OQ 5건이 §7 에 정리됨 (InstallPrompt 설치 버튼 그라데이션 / SplashScreen 진행 바 그라데이션 / 폰트 격상 / 외곽 hex 토큰 치환 / 특수 글리프 ⋮ ⎋ Lucide vs 콘텐츠 유지)"
    - "SplashScreen.tsx + InstallPrompt.tsx + pwaInstall.ts + versionCheck.ts 코드 변경 0"
    - "/ (splash route) 가 App.tsx 실측 — MOBILE_NO_NAV_PATHS + DESKTOP_NO_NAV_PATHS 양쪽 등재 (line 71/74) + DESKTOP_HEADER_HIDE_PATHS 미등재 (line 77 — 단 PAGE_TITLES 미등재라 글로벌 헤더 타이틀 미렌더) + Route line 265 = SplashScreen 결과 §4 에 박제 — 인증 전 스플래시 → chrome 룰 미적용"
  artifacts:
    - path: "cha-bio-safety/docs/redesign-context/28-splash/wave-1-index.md"
      provides: "W2~W5 진입을 위한 단일 진입점 인덱스 + 룰 verbatim 인용 + sub-wave 분배 매핑 (SplashScreen 83 lines + InstallPrompt 237 lines 통합 320 lines 인증 전 페이지용 4 sub-wave 분배)"
      contains: "§1. SplashScreen + InstallPrompt 인벤토리, §2. 4 sub-wave 분배, §3. design-system verbatim, §4. chrome 통일 룰 (인증 전 스플래시 = chrome 룰 직접 적용 X + App.tsx BottomNav/Header 실측 박제), §5. 메모리 룰 12개 inline, §6. negative rule, §7. open questions"
  key_links:
    - from: "wave-1-index.md"
      to: "cha-bio-safety/src/pages/SplashScreen.tsx"
      via: "§1 인벤토리에 line 범위 인용 + §2 sub-wave 분배 표의 element/line 매핑"
      pattern: "line [0-9]+"
    - from: "wave-1-index.md"
      to: "cha-bio-safety/src/components/InstallPrompt.tsx"
      via: "§1 인벤토리에 line 범위 인용 + §2 sub-wave 분배 표의 element/line 매핑 + 비즈 시그니처 박스"
      pattern: "line [0-9]+"
    - from: "wave-1-index.md"
      to: "cha-bio-safety/docs/redesign-context/28-splash/design-system.md"
      via: "§3 fence verbatim 인용 (§1.1/§1.2/§1.3 본문 박제)"
      pattern: "design-system.md §"
    - from: "wave-1-index.md"
      to: "cha-bio-safety/docs/redesign-context/00-design-context/inspection-modal-chrome-rules.md"
      via: "§4 chrome 룰 (인증 전 스플래시 → chrome 룰 직접 적용 X + 패턴 mirror 검토 1단락)"
      pattern: "inspection-modal-chrome-rules"
    - from: "wave-1-index.md"
      to: "cha-bio-safety/src/App.tsx"
      via: "§4 BottomNav/AppHeader 실측 — MOBILE_NO_NAV_PATHS (line 71 '/' 등재) + DESKTOP_NO_NAV_PATHS (line 74 '/' 등재) + DESKTOP_HEADER_HIDE_PATHS (line 77 '/' 미등재 단 PAGE_TITLES 미등재로 무의미) + Route line 265 = SplashScreen"
      pattern: "MOBILE_NO_NAV_PATHS|DESKTOP_NO_NAV_PATHS|DESKTOP_HEADER_HIDE_PATHS|PAGE_TITLES|SplashScreen"
    - from: "wave-1-index.md"
      to: "cha-bio-safety/src/utils/pwaInstall.ts"
      via: "§1 인벤토리 비즈 로직 보존 — getDeferredInstallPrompt / subscribeInstallPrompt / showInstallPrompt export 시그니처 박제"
      pattern: "getDeferredInstallPrompt|subscribeInstallPrompt|showInstallPrompt"
    - from: "wave-1-index.md"
      to: "cha-bio-safety/src/utils/versionCheck.ts"
      via: "§1 인벤토리 비즈 로직 보존 — checkVersionAndRefresh export 시그니처 박제"
      pattern: "checkVersionAndRefresh"
---

<objective>
redesign/28-splash sketch 작업의 wave 1 — 후속 wave(W2~W5) 의 단일 진입점이 되는 인덱스/룰 정리 문서 1개만 작성한다.

Purpose: SplashScreen.tsx (83 라인 — 인증 전 `/` route 스플래쉬) + InstallPrompt.tsx (237 라인 — PWA 설치 안내 팝업) 2개 파일 통합 320 라인의 모든 element 를 **4 sub-wave** 로 분배 (17-annual-plan + 27-login + 16-workshift W1 패턴 mirror), 그리고 design-system.md 룰과 메모리 룰 12개 (10 기본 + 28-splash 특화 2건 — 특수 글리프 ⋮ ⎋ 처리 룰 + 15-daily-report precedent 일반화) 를 verbatim 박제해서 후속 sketch wave 작업자가 이 인덱스만 보면 일관되게 작업할 수 있도록 한다.

Output: `cha-bio-safety/docs/redesign-context/28-splash/wave-1-index.md` 단 1개 파일. 코드 변경 0건. sketch HTML 생성 0건 (그건 W2 부터).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@./CLAUDE.local.md

# 17-annual-plan W1 + 27-login W1 + 16-workshift W1 precedent (이번 wave 가 mirror 할 정확한 7 섹션 + 4 sub-wave 구조)
@.planning/quick/260521-wmq-redesign-17-annual-plan-sketch-wave-1-an/260521-wmq-PLAN.md
@.planning/quick/260521-c6p-redesign-27-login-sketch-wave-1-loginpag/260521-c6p-PLAN.md
@.planning/quick/260521-sjj-redesign-16-workshift-sketch-wave-1-work/260521-sjj-PLAN.md

# 17-annual-plan W1 산출물 (가장 최근 7섹션 mirror + 12 메모리 룰 패턴 가장 최근 origin)
@cha-bio-safety/docs/redesign-context/17-annual-plan/wave-1-index.md

# Source files (이 wave 의 분석 대상, 수정 0)
@cha-bio-safety/src/pages/SplashScreen.tsx
@cha-bio-safety/src/components/InstallPrompt.tsx
@cha-bio-safety/src/utils/pwaInstall.ts
@cha-bio-safety/src/utils/versionCheck.ts

# Redesign context (이 wave 가 산출할 인덱스가 인용/참조하는 문서들)
@cha-bio-safety/docs/redesign-context/28-splash/28-splash.md
@cha-bio-safety/docs/redesign-context/28-splash/design-system.md
@cha-bio-safety/docs/redesign-context/00-design-context/inspection-modal-chrome-rules.md

# App.tsx 실측 (chrome 등록 여부)
@cha-bio-safety/src/App.tsx

# 13-schedule + 14-reports + 27-login + 16-workshift + 17-annual-plan 모두 평면 sibling 패턴 (sketch/ 서브폴더 없음). 본 wave 도 동일.
</context>

<interfaces>
<!-- 후속 wave 가 산출할 sketch 파일 명명 규칙 (이 인덱스가 §2 표에서 인용) -->
<!-- 13-schedule + 14-reports + 27-login + 16-workshift + 17-annual-plan 평면 패턴 일관 — 28-splash/ 직속에 위치 -->

W2 → cha-bio-safety/docs/redesign-context/28-splash/sketch-wave-2-splash.html
W3 → cha-bio-safety/docs/redesign-context/28-splash/sketch-wave-3-install-main.html
W4 → cha-bio-safety/docs/redesign-context/28-splash/sketch-wave-4-install-guide.html
W5 → cha-bio-safety/docs/redesign-context/28-splash/wave-5-tsx-conversion-checklist.md (markdown, sketch 아님)

(주의: 28-splash 는 단일 페이지가 아닌 2개 파일 통합 (SplashScreen + InstallPrompt). 그러나 InstallPrompt 의 3 view state (메인 / Android 가이드 / iOS 가이드) 중 메인 view 와 가이드 view 는 시각 디자인이 충분히 달라서 분리 권장. 평면 패턴 4-wave 개수와 sketch-wave-N-{slug}.html (W2~W4) + wave-5-tsx-conversion-checklist.md (W5) 유지. W5 TSX 변환은 2개 파일 합쳐서 단일 atomic 변환 권장 — 두 파일이 같은 흐름에 묶여 있어서 (SplashScreen → InstallPrompt 트리거) 분할 시 컨텍스트 낭비.)

# 비즈 로직 시그니처 (W5 TSX 보존 checklist 의 anchor — 이 인덱스가 §1 + §6 에서 인용)

## SplashScreen.tsx (line 1~83)
- export default function SplashScreen()
- useState<number> pct (0~100, 4씩 증가, 48ms tick)
- useState<boolean> showInstall
- useNavigate() / useAuthStore().isAuthenticated
- useEffect: checkVersionAndRefresh() (await 안 함) + setInterval tick 48ms + setTimeout 1600ms — shouldShowInstallPrompt() ? setShowInstall(true) : navigate(isAuthenticated ? '/dashboard' : '/login', { replace: true })
- handleDismiss(): dismissInstallPrompt() + setShowInstall(false) + navigate(...)
- `__APP_VERSION__` 빌드 시점 인젝션 — Vite define (line 73)
- `@keyframes slideUp` 인라인 (line 79~80) — 로고 fade+translateY 16px

## InstallPrompt.tsx (line 1~237)
- isStandalone(): boolean — iOS navigator.standalone || display-mode standalone/fullscreen
- isIOS(): boolean — userAgent /iPhone|iPad|iPod/ || Macintosh + maxTouchPoints>1 (iPadOS 13+)
- export function InstallPrompt({ onDismiss }: { onDismiss: () => void })
- useState<boolean> hasPrompt = !!getDeferredInstallPrompt()
- useState<boolean> showIOSGuide / showAndroidGuide
- useEffect: subscribeInstallPrompt(p => setHasPrompt(!!p))
- handleInstallAndroid(): await showInstallPrompt() → 'accepted' onDismiss() / 'unavailable' setShowAndroidGuide(true) / 'dismissed' noop
- handleInstallIOS(): setShowIOSGuide(true)
- export function shouldShowInstallPrompt(): boolean — !isStandalone() && (24h dismissed check)
- export function dismissInstallPrompt(): localStorage.setItem('pwa-install-dismissed', String(Date.now()))
- localStorage key 'pwa-install-dismissed' (line 227/236) — 24h TTL 룰

## pwaInstall.ts / versionCheck.ts (수정 0)
- getDeferredInstallPrompt(): BeforeInstallPromptEvent | null
- subscribeInstallPrompt(callback): unsubscribe
- showInstallPrompt(): Promise<'accepted' | 'dismissed' | 'unavailable'>
- checkVersionAndRefresh(): Promise<void> — version mismatch 시 location.reload()
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: wave-1-index.md 작성</name>
  <files>cha-bio-safety/docs/redesign-context/28-splash/wave-1-index.md</files>
  <action>
SplashScreen.tsx (83 라인) + InstallPrompt.tsx (237 라인) + pwaInstall.ts (참조) + versionCheck.ts (참조) + 28-splash.md + design-system.md + inspection-modal-chrome-rules.md 를 모두 끝까지 읽은 뒤 아래 7개 섹션을 가진 단일 markdown 파일을 작성한다. 파일은 Write 도구로 생성한다 (heredoc/cat 금지).

---

# 파일 헤더

상단에 다음 1블록:
- 제목: `# redesign/28-splash — sketch wave 1 (index)`
- 1-2줄 설명: 본 문서는 W2~W5 진입의 단일 진입점이며, 이 인덱스만 봐도 후속 wave 가 디자인 룰 / 메모리 룰 / sub-wave 분배 / OQ 를 알 수 있도록 한다.
- 산출일자: 2026-05-22 / Quick ID 260522-209 / branch redesign/28-splash
- 1줄 메타: "17-annual-plan W1 (260521-wmq) + 27-login W1 (260521-c6p) + 16-workshift W1 (260521-sjj) 의 7 섹션 + 4 sub-wave 구조를 정확히 mirror. SplashScreen 83 lines + InstallPrompt 237 lines 통합 320 lines 인증 전 페이지 (`/` route, chrome 미적용). InstallPrompt 의 3 view state (메인 / Android 가이드 / iOS 가이드) 중 메인 (W3) + 가이드 (W4) 분리 — 시각 디자인 차이 크고 특수 글리프 ⋮ ⎋ 결정 분리 필요."

---

# §1. SplashScreen.tsx + InstallPrompt.tsx 인벤토리

5 영역으로 나눠 표로 정리. 각 행은 (영역 / element / source file:line 범위 / 역할 / 비즈 로직 연결 / 후속 wave 매핑) 6 컬럼.

**28-splash 의 구조 특이성** (인벤토리 머리말로 1단락):
- **2개 파일 통합** (SplashScreen + InstallPrompt) — SplashScreen 이 `<InstallPrompt onDismiss={handleDismiss} />` 컴포넌트를 1600ms tick 후 `shouldShowInstallPrompt()` 결과에 따라 렌더 (line 76)
- **인증 전 페이지** — `/` route (App.tsx line 265), `MOBILE_NO_NAV_PATHS` + `DESKTOP_NO_NAV_PATHS` 양쪽 등재 (line 71/74) → BottomNav 모바일/데스크톱 모두 숨김. `DESKTOP_HEADER_HIDE_PATHS` 미등재 + `PAGE_TITLES` 미등재 → 글로벌 AppHeader 타이틀 미렌더 (showNav 가 false 라 헤더 자체 미표시). chrome 룰 직접 적용 X.
- **특수 글리프 2개** — InstallPrompt Android 가이드 line 122 `⋮` (U+22EE, "크롬 우상단 메뉴") + iOS 가이드 line 164 `⎋` (U+238B, "Safari 하단 공유 버튼"). 사용자 OS UI 표기 일치 → 콘텐츠 글리프 유지가 default (memory `feedback_tsx_wave_emoji_dot_gap` 예외 결정 룰, 27-login OQ #4 ☎ 와 동일).
- **`__APP_VERSION__` Vite define 인젝션** (SplashScreen line 73) — 빌드 시점 치환, 보존 필수
- **localStorage 키 `pwa-install-dismissed`** (InstallPrompt line 227/236) — 24h TTL, 변경 금지
- **외부 모듈 의존** — pwaInstall.ts (getDeferredInstallPrompt / subscribeInstallPrompt / showInstallPrompt) + versionCheck.ts (checkVersionAndRefresh) — 수정 0
- **모바일 전용** — 데스크톱 분기 없음 (`/` 가 PC 1920x1080 에도 모바일 레이아웃으로 동일 표시). 단 InstallPrompt 의 isIOS / Android 분기는 디바이스 사용자에이전트 기반.

**영역 1: SplashScreen 외곽 + 로고 + 타이틀** (SplashScreen.tsx line 1~61)
- imports + state (line 1~11): useState pct / showInstall / useNavigate / useAuthStore / InstallPrompt / shouldShowInstallPrompt / dismissInstallPrompt / checkVersionAndRefresh
- useEffect (line 13~29): checkVersionAndRefresh (await 안 함) + setInterval tick 48ms +4 → setTimeout 1600ms → shouldShowInstallPrompt() ? setShowInstall(true) : navigate(isAuthenticated ? '/dashboard' : '/login', { replace: true })
- handleDismiss (line 31~35): dismissInstallPrompt() + setShowInstall(false) + navigate
- 외곽 wrapper (line 37~43): minHeight 100dvh, background `#161b22`, flex column center, gap 0
- 로고 wrapper (line 45~56): animation `slideUp .4s ease-out`, flex column center, gap 20, marginBottom 56
  - 로고 박스 (line 47~55): 88x88, radius 22, background `rgba(37,99,235,0.2)`, border `1px solid rgba(59,130,246,0.3)`, flex center, overflow hidden
  - 로고 img (line 54): `/icons/icon-192.png`, 64x64, radius 14
- 타이틀 div (line 57~60): textAlign center
  - h1 (line 58): fontSize 22, fontWeight 900, color `#e6edf3`, letterSpacing `-.02em` — "CBC 방재"
  - p (line 59): fontSize 12, color `#6e7681`, letterSpacing `.1em`, marginTop 6 — "소방안전 통합관리 시스템"
- 비즈: pct setInterval / showInstall state / navigate / handleDismiss / `@keyframes slideUp`
- 후속 wave: **W2** (Splash 전체 묶음 — 로고 + 타이틀 + 진행 바 + 버전 합쳐서 단일 sketch)

**영역 2: SplashScreen 진행 바 + 하단 버전 + animation** (SplashScreen.tsx line 63~83)
- 진행 바 wrapper (line 64): width 160
- 진행 바 트랙 (line 65~67): height 2, background `rgba(255,255,255,0.07)`, radius 2, overflow hidden
  - 진행 바 fill (line 66): height 100%, background `linear-gradient(90deg,#3b82f6,#0ea5e9)`, radius 2, width `${pct}%`, transition `width .05s linear`
- 진행 텍스트 p (line 68~70): fontSize 11, color `#6e7681`, textAlign center, marginTop 10 — 분기: pct<40 '시스템 초기화 중...' / pct<80 '데이터 불러오는 중...' / '준비 완료'
- 하단 버전 p (line 73): position absolute, bottom 20, fontSize 10, color `#3d444d` — `v${__APP_VERSION__} · 경기도 성남시 분당구`
- InstallPrompt 렌더 (line 76): `{showInstall && <InstallPrompt onDismiss={handleDismiss} />}`
- `@keyframes slideUp` (line 78~80): from opacity 0 translateY 16px / to opacity 1 translateY 0
- 비즈: pct (진행 바 width + 텍스트 분기) / `__APP_VERSION__` 인젝션 / showInstall 분기 / handleDismiss
- 후속 wave: **W2** (진행 바 + 버전 모두 Splash 묶음)

**영역 3: InstallPrompt 오버레이 + 카드 + 헤더 (아이콘+제목)** (InstallPrompt.tsx line 1~92)
- imports + isStandalone + isIOS helper (line 1~17)
- InstallPrompt 함수 본체 시작 + state (line 25~35): useState hasPrompt = !!getDeferredInstallPrompt() / showIOSGuide / showAndroidGuide / useEffect subscribeInstallPrompt 구독
- handleInstallAndroid (line 38~48): await showInstallPrompt() → 'accepted' onDismiss / 'unavailable' setShowAndroidGuide(true) / 'dismissed' noop
- handleInstallIOS (line 51~53): setShowIOSGuide(true)
- const ios = isIOS() (line 55)
- 오버레이 (line 58~63): position fixed, inset 0, zIndex 9999, background `rgba(0,0,0,0.85)`, flex center, padding 24
- 카드 (line 64~70): background `#1c2128`, radius 20, padding `28px 24px`, maxWidth 340 width 100%, textAlign center, border `1px solid rgba(59,130,246,0.3)`, boxShadow `0 8px 40px rgba(0,0,0,0.5)`
- 아이콘 박스 (line 72~78): 64x64, radius 16, margin `0 auto 16px`, background `rgba(37,99,235,0.2)`, border `1px solid rgba(59,130,246,0.3)`, flex center
  - img (line 78): `/icons/icon-192.png`, 48x48, radius 12
- h2 (line 81~83): fontSize 18, fontWeight 800, color `#e6edf3`, margin `0 0 8px` — "CBC 방재"
- p 부제 (line 84~86): fontSize 12, color `#8b949e`, margin `0 0 20px`, lineHeight 1.5 — "홈 화면에 앱을 설치하면<br/>더 빠르고 편리하게 사용할 수 있습니다"
- 비즈: hasPrompt / showIOSGuide / showAndroidGuide / handleInstallAndroid / handleInstallIOS / isIOS / 오버레이 = 모든 view 공통
- 후속 wave: **W3** (InstallPrompt 메인 view 묶음 — 오버레이 + 카드 + 헤더 + 메인 view 버튼 영역. 가이드 view 는 W4)

**영역 4: InstallPrompt 메인 view (설치 버튼 + 나중에 버튼)** (InstallPrompt.tsx line 88~111)
- 분기 시작 (line 88): `!showIOSGuide && !showAndroidGuide ? (...메인 view...) : showAndroidGuide ? (...Android 가이드...) : (...iOS 가이드...)`
- 메인 view wrapper (line 89): flex column gap 10
- **설치 버튼** (line 90~100):
  - onClick: ios ? handleInstallIOS : handleInstallAndroid
  - style: width 100%, height 48, radius 12, background `linear-gradient(135deg, #2563eb, #0ea5e9)`, border none, color `#fff`, fontSize 15, fontWeight 700, cursor pointer
  - label 분기 (line 99): ios ? '설치 방법 보기' : (hasPrompt ? '홈 화면에 설치' : '설치 방법 보기')
- **나중에 버튼** (line 101~110):
  - onClick: onDismiss
  - style: width 100%, height 40, radius 10, background transparent, border `1px solid rgba(255,255,255,0.1)`, color `#6e7681`, fontSize 12, fontWeight 600, cursor pointer
  - 카피: '나중에 할게요'
- 비즈: ios / hasPrompt / handleInstallAndroid / handleInstallIOS / onDismiss
- 후속 wave: **W3** (InstallPrompt 메인 view — 영역 3 카드 wrapper 안에 nested)

**영역 5: InstallPrompt Android + iOS 가이드 view** (InstallPrompt.tsx line 112~215)

**5a. Android 가이드** (line 112~147):
- wrapper (line 113~114): textAlign left
- 노란 경고 박스 (line 115~117): fontSize 12, color `#f59e0b`, background `rgba(245,158,11,0.1)`, border `1px solid rgba(245,158,11,0.25)`, radius 8, padding `8px 10px`, marginBottom 14 — '자동 설치 창이 뜨지 않으면 아래 순서로 설치해 주세요.'
- 3-step list wrapper (line 118): flex column gap 14
- **Step 1** (line 119~125):
  - 숫자 원 (line 120): 28x28, radius 8, flexShrink 0, background `rgba(59,130,246,0.15)`, color `#3b82f6`, flex center, fontSize 13, fontWeight 800 — '1'
  - 텍스트 wrapper (line 121~124):
    - 타이틀 div (line 122): fontSize 13, fontWeight 700, color `#e6edf3` — `크롬 우상단 <span style={{ fontSize: 16 }}>⋮</span> 메뉴`
    - 서브 div (line 123): fontSize 11, color `#8b949e`, marginTop 2 — '주소창 오른쪽 점 세 개 메뉴를 누르세요'
- **Step 2** (line 126~132): 동일 구조, 타이틀 "'앱 설치' 또는 '홈 화면에 추가' 선택", 서브 "메뉴에서 <strong>앱 설치</strong>(또는 <strong>홈 화면에 추가</strong>)를 누르세요"
- **Step 3** (line 133~139): 동일 구조, 타이틀 "'설치' 확인", 서브 "팝업에서 <strong>설치</strong>를 누르면 완료"
- **확인 버튼** (line 141~146): width 100%, height 44, radius 10, marginTop 18, background `rgba(59,130,246,0.15)`, border `1px solid rgba(59,130,246,0.3)`, color `#3b82f6`, fontSize 13, fontWeight 700, cursor pointer — '확인했습니다'

**5b. iOS 가이드** (line 148~214):
- wrapper (line 150): textAlign left
- 3-step list wrapper (line 151): flex column gap 14
- **Step 1** (line 152~167): 숫자 원 동일, 타이틀 '하단 공유 버튼 터치', 서브 `Safari 하단의 <span style={{ fontSize: 16, verticalAlign: 'middle' }}>⎋</span> 공유 아이콘을 누르세요`
- **Step 2** (line 169~184): 타이틀 "'홈 화면에 추가' 선택", 서브 "스크롤해서 <strong>홈 화면에 추가</strong>를 찾아 누르세요"
- **Step 3** (line 186~201): 타이틀 "'추가' 터치", 서브 "오른쪽 상단 <strong>추가</strong> 버튼을 누르면 완료!"
- **확인 버튼** (line 204~213): Android 동일

- 비즈: showIOSGuide / showAndroidGuide 분기 / onDismiss (확인 버튼)
- 후속 wave: **W4** (Android + iOS 가이드 묶음 — 3-step 패턴 + 노란 경고 박스 (Android만) + 특수 글리프 ⋮ ⎋)

**비즈 시그니처 보존 박스 (별도 박스):**
```
[SplashScreen.tsx]
- useState<number> pct (0~100, +4 per 48ms tick)
- useState<boolean> showInstall
- useEffect: checkVersionAndRefresh() (void) + setInterval(48ms) + setTimeout(1600ms) → shouldShowInstallPrompt() ? setShowInstall(true) : navigate(isAuthenticated ? '/dashboard' : '/login', { replace: true })
- handleDismiss(): dismissInstallPrompt() + setShowInstall(false) + navigate
- __APP_VERSION__ Vite define 인젝션 (변경 금지)
- @keyframes slideUp (변경 금지)
- background hex `#161b22` (page bg, 토큰 치환 OK — OQ #4)

[InstallPrompt.tsx]
- isStandalone(): boolean (변경 금지)
- isIOS(): boolean (변경 금지)
- export function InstallPrompt({ onDismiss }: { onDismiss: () => void })
- useState hasPrompt / showIOSGuide / showAndroidGuide
- useEffect: subscribeInstallPrompt(p => setHasPrompt(!!p))
- handleInstallAndroid(): await showInstallPrompt() → 'accepted'/'unavailable'/'dismissed' (변경 금지)
- handleInstallIOS(): setShowIOSGuide(true)
- export function shouldShowInstallPrompt(): boolean — !isStandalone() && 24h dismissed (변경 금지)
- export function dismissInstallPrompt(): localStorage.setItem('pwa-install-dismissed', ...) (변경 금지)
- localStorage key 'pwa-install-dismissed' (변경 금지)
- 카드 background hex `#1c2128` (토큰 치환 OK — OQ #4)

[pwaInstall.ts] (수정 0)
- getDeferredInstallPrompt / subscribeInstallPrompt / showInstallPrompt 시그니처 보존

[versionCheck.ts] (수정 0)
- checkVersionAndRefresh 시그니처 보존
```
위 모든 식별자/값은 W5 TSX 변환 시 1 byte 변경 금지 (15-daily-report SW3 precedent 동일 적용).

---

# §2. 4 sub-wave 분배 plan

다음 표 그대로 박제 (단, 파일명은 위 <interfaces> 의 통일된 `sketch-wave-N-{slug}.html` 패턴 사용):

| Wave | scope | 대상 element | 산출 파일 |
|---|---|---|---|
| W2 | SplashScreen 전체 (외곽 + 로고 + 타이틀 + 진행 바 + 하단 버전 + slideUp animation) | 영역 1 + 영역 2 — `/` route 인증 전 스플래시 단일 화면. background `#161b22` / 로고 88x88 radius 22 / "CBC 방재" 22px + "소방안전 통합관리 시스템" 12px / 진행 바 width 160 + 진행 텍스트 3 분기 / 하단 버전 v{__APP_VERSION__} · 경기도 성남시 분당구 / @keyframes slideUp. tick 4 state (0% / 40% / 80% / 100%) 매트릭스 권장. | sketch-wave-2-splash.html |
| W3 | InstallPrompt 메인 view (오버레이 + 카드 + 헤더 + 설치 버튼 + 나중에 버튼) | 영역 3 + 영역 4 — 오버레이 `rgba(0,0,0,0.85)` + 카드 `#1c2128` maxWidth 340 + 아이콘 64x64 + "CBC 방재" 18px + 부제 12px + 설치 버튼 그라데이션 `linear-gradient(135deg, #2563eb, #0ea5e9)` + 나중에 버튼 transparent. 3 label 분기 (iOS / Android hasPrompt / Android !hasPrompt) 매트릭스 권장. | sketch-wave-3-install-main.html |
| W4 | InstallPrompt Android + iOS 가이드 view (3-step + 노란 경고 박스 + 특수 글리프 ⋮ ⎋) | 영역 5 — Android 노란 경고 박스 (color/bg/border #f59e0b 알파) + Android 3-step (⋮ 글리프 포함) + iOS 3-step (⎋ 글리프 포함) + 확인 버튼 (둘 다 rgba(59,130,246,0.15) variant). 메인 view 외곽 (영역 3 카드 wrapper) 은 W3 에서 정의된 것 그대로 nested 사용. | sketch-wave-4-install-guide.html |
| W5 | TSX 변환 verify checklist (sketch 아님, markdown) | W2~W4 sketch + SplashScreen + InstallPrompt 비즈 로직 보존 룰 + 비즈 시그니처 1 byte 변경 금지 checklist + Tailwind cheatsheet + 2개 파일 atomic 단일 변환 권장 메모 | wave-5-tsx-conversion-checklist.md |

각 wave 행 아래에 boldface "보존 / 토큰 / 폰트" 3 미니 섹션:
- **보존**: 변환 후 보존해야 할 비즈 로직 호출 —
  - W2: pct setInterval 48ms +4 / setTimeout 1600ms / shouldShowInstallPrompt() / navigate(isAuthenticated ? '/dashboard' : '/login', { replace: true }) / checkVersionAndRefresh() void / `__APP_VERSION__` Vite define / `@keyframes slideUp` / 진행 텍스트 3 분기 카피 verbatim ('시스템 초기화 중...' / '데이터 불러오는 중...' / '준비 완료') / 하단 버전 카피 'v{__APP_VERSION__} · 경기도 성남시 분당구' verbatim / "CBC 방재" + "소방안전 통합관리 시스템" verbatim
  - W3: isStandalone / isIOS / hasPrompt = !!getDeferredInstallPrompt() / subscribeInstallPrompt 구독 / handleInstallAndroid await showInstallPrompt() 3 outcome 분기 / handleInstallIOS / onDismiss prop / 설치 버튼 label 3 분기 (iOS / Android hasPrompt / Android !hasPrompt) verbatim / "CBC 방재" + "홈 화면에 앱을 설치하면<br/>더 빠르고 편리하게 사용할 수 있습니다" verbatim / "나중에 할게요" verbatim
  - W4: showIOSGuide / showAndroidGuide / setShowIOSGuide / setShowAndroidGuide / 노란 경고 박스 카피 '자동 설치 창이 뜨지 않으면 아래 순서로 설치해 주세요.' verbatim / Android 3 step 카피 ('크롬 우상단 ⋮ 메뉴' / "'앱 설치' 또는 '홈 화면에 추가' 선택" / "'설치' 확인" + 서브 텍스트 verbatim) / iOS 3 step 카피 ('하단 공유 버튼 터치' / "'홈 화면에 추가' 선택" / "'추가' 터치" + 서브 텍스트 verbatim) / 확인 버튼 카피 '확인했습니다' verbatim / **특수 글리프 ⋮ (U+22EE) + ⎋ (U+238B) 콘텐츠 유지** (OQ #5 default — 27-login OQ #4 ☎ 와 동일 룰)
  - W5: 위 모든 항목의 TSX 보존 checklist + 비즈 시그니처 무변 verify (grep gate) + localStorage key 'pwa-install-dismissed' 보존 + `__APP_VERSION__` 보존
- **토큰**: 적용할 디자인 토큰 —
  - W2: bg-surface-page (page bg `#161b22`) / text-text-primary (`#e6edf3`) / text-text-tertiary (`#6e7681`) / text-text-disabled (`#3d444d` 버전) / border 무 / 로고 박스 `rgba(37,99,235,0.2)` + border `rgba(59,130,246,0.3)` 인라인 유지 (브랜드 강조, 단일 사용처) / 진행 바 fill `linear-gradient(90deg,#3b82f6,#0ea5e9)` → solid `bg-accent` 치환 (OQ #2 default)
  - W3: bg-surface-raised (카드 `#1c2128`) / overlay `rgba(0,0,0,0.85)` 인라인 유지 / 설치 버튼 그라데이션 `linear-gradient(135deg, #2563eb, #0ea5e9)` → solid `bg-safe-bar` 치환 (OQ #1 default — 27-login + 14-reports + 16-workshift + 17-annual-plan 일관) / 나중에 버튼 border `rgba(255,255,255,0.1)` 인라인 유지 (semi-transparent ghost border)
  - W4: 노란 경고 박스 — color `#f59e0b` + bg `rgba(245,158,11,0.1)` + border `rgba(245,158,11,0.25)` → `text-status-caution` + `bg-status-caution-bg` 토큰화 검토 (status- prefix 룰 위반 가능성 → memory `feedback_tailwind_token_class_pattern` 확인 — 현재 토큰 alias 가 prefix-less 면 `bg-caution-bg` / `text-caution`). 숫자 원 `rgba(59,130,246,0.15)` + color `#3b82f6` → `bg-accent/15` + `text-accent` 토큰화 OK / 확인 버튼 동일 토큰. **특수 글리프 ⋮ ⎋ 는 텍스트 inline span fontSize 16 유지** (Lucide 교체 X — OQ #5 default).
  - **status- prefix 없음** (memory `feedback_tailwind_token_class_pattern`).
- **폰트**: 모바일/데스크톱 동일 폰트 룰 (design-system §1.3). 본문 16px 마지노선, 9~11px 금지 (§1.1). 현재 SplashScreen + InstallPrompt 의 fontSize:
  - **10** (SplashScreen 하단 버전 line 73) — §1.1 위반 강도 최대 → text-caption(12) leading-none 격상 검토 (OQ #3)
  - **11** (SplashScreen 진행 텍스트 line 68, InstallPrompt 서브 텍스트 step line 123 등 다수) — §1.1 위반 → text-caption(12) leading-none 격상
  - **12** (SplashScreen 부제 line 59, InstallPrompt 부제 line 84, InstallPrompt 노란 경고 line 115, 나중에 버튼 line 106) — §1.1 마지노선 12px → text-caption leading-none 유지
  - **13** (Step 타이틀 line 122 등, 숫자 원 line 120 등, 확인 버튼 line 143) — text-label
  - **15** (InstallPrompt 설치 버튼 line 96) — text-body-sm vs text-body(16) 격상 후보 (CTA 강조 — OQ #3 default 격상 권장)
  - **16** (특수 글리프 ⋮ ⎋ inline span line 122, 164) — 콘텐츠 글리프, fontSize 명시 유지
  - **18** (InstallPrompt h2 line 81) — text-title 유지
  - **22** (SplashScreen h1 line 58 "CBC 방재") — text-heading 유지

---

# §3. design-system.md 인용 (verbatim 발췌, fence 안)

design-system.md (cha-bio-safety/docs/redesign-context/28-splash/design-system.md) 를 읽고 아래 항목을 각각 별도의 ```fence 블록``` 안에 **원문 그대로** 박제 (불가피한 줄바꿈 제외 정확히 일치):

- §1.1 노안 친화 (본문 16px, 9~11px 금지, 터치 44px)
- §1.2 정보 인지 > 미적 정제
- §1.3 모바일/데스크톱 동일 폰트
- §6 Progress Color Rule → fence 인용 후 1줄 메타: "§6 미적용 — 인증 전 스플래시 + PWA 설치 안내에는 진척률 도넛/카테고리 카드 없음."
- §7 Stat Card → fence 인용 후 1줄 메타: "§7 미적용 — 인증 전 스플래시 + PWA 설치 안내에는 통계 숫자 카드 없음."
- §7.1 Iconography (Lucide) — fence 인용 후 1줄 메타: "§7.1 — Lucide 사용 가능. 단 InstallPrompt 의 특수 글리프 ⋮ (Android 가이드 line 122) + ⎋ (iOS 가이드 line 164) 는 사용자 OS UI 표기 일치 목적의 **콘텐츠 글리프** 이므로 Lucide MoreVertical / Share 교체 X (OQ #5 default — 27-login OQ #4 ☎ 와 동일 룰). 다른 아이콘 없음 (다운로드 / 뒤로 가기 등) — InstallPrompt 의 인포 일러스트는 `<img src=/icons/icon-192.png>` PNG 그대로 사용."

만약 design-system.md 안 섹션 번호/제목이 실제 다르면 실제 파일 기준 §번호 맞춰 인용하고 1줄 메타에서 차이 명시.

추가:
- 기존 디자인 = InstallPrompt 설치 버튼 `linear-gradient(135deg, #2563eb, #0ea5e9)` (line 94) → `bg-safe-bar` solid 통일 검토 — **default OK**. 근거: 27-login W1 OQ #1 default OK (그라데이션 → solid) + 14-reports W1 OQ #1/#3 default OK + 16-workshift W1 OQ #1 default OK + 17-annual-plan W1 OQ #1 default OK 일관 + design-system §6.4 CTA solid 룰 + memory `feedback_design_sketch_first` + `feedback_tailwind_token_class_pattern`. 이 결정은 §7 OQ #1 에서 사용자 컨펌 받음.
- SplashScreen 진행 바 `linear-gradient(90deg,#3b82f6,#0ea5e9)` (line 66) — 시각적 진행 표현이 핵심. solid `bg-accent` 치환 default 권장. §7 OQ #2 에서 컨펌.

---

# §4. 02+06 chrome 통일 룰 적용 여부

`inspection-modal-chrome-rules.md` 를 읽고 1~2 단락으로 작성:

- 28-splash 페이지 (`/` route) 는 **인증 전 스플래시 + PWA 설치 안내** → chrome 룰 자체는 **직접 적용 X**
- 단, 다음 패턴은 mirror 검토 안 됨 (인증 전 페이지 특성):
  - **헤더 없음** — SplashScreen 은 헤더 X (전체 화면 중앙 정렬). InstallPrompt 는 오버레이 모달 안 카드 (헤더 = 아이콘 + 제목 + 부제) — chrome 룰 §2.1 의 `bg-surface-page` 통일 룰 적용 안 됨 (모달 카드 = `bg-surface-raised` 패턴 — 단 modal-content padding 패턴은 적용 후보).
  - **back button 없음** — 인증 전 페이지 → 모바일 자체 헤더의 back button 패턴 (chrome 룰 §7.2 `w-8 h-8 bg-surface-sunken`) 적용 안 됨.
  - **BottomNav 숨김 / AppHeader 숨김** — App.tsx 실측: `/` 가 `MOBILE_NO_NAV_PATHS` (line 71) + `DESKTOP_NO_NAV_PATHS` (line 74) 양쪽 등재 → 모바일/데스크톱 BottomNav 둘 다 **숨김** (showNav false). `DESKTOP_HEADER_HIDE_PATHS` (line 77) 에는 **미등재** 이지만 showNav 가 false 이므로 글로벌 AppHeader 자체가 미렌더 (App.tsx line 227 `{isDesktop && showNav && !DESKTOP_HEADER_HIDE_PATHS.includes(location.pathname) && (...)}` — showNav 가 첫번째 조건이라 단락). `PAGE_TITLES` (line 79~) 에는 `/` 미등재 → 헤더 표시되더라도 타이틀 무.

**실측 결과 (App.tsx 본문 grep):**
```
line 16: const SplashScreen = lazy(() => import('./pages/SplashScreen'))
line 71: MOBILE_NO_NAV_PATHS = ['/', '/login', '/schedule', ...]  // '/' 등재
line 74: DESKTOP_NO_NAV_PATHS = ['/', '/login']  // '/' 등재 — 데스크톱 BottomNav 숨김
line 77: DESKTOP_HEADER_HIDE_PATHS = ['/elevator', '/div', '/floorplan', '/workshift']  // '/' 미등재 (단 showNav false 라 무의미)
line 79~: PAGE_TITLES — '/' 미등재
line 227: {isDesktop && showNav && !DESKTOP_HEADER_HIDE_PATHS.includes(...) && (...)}  // showNav 단락
line 265: <Route path="/" element={<SplashScreen />} />  // 인증 가드 없음 — Auth wrapper 없이 노출
```

핵심 시사점:
- 모바일/데스크톱 모두 — BottomNav 숨김, 글로벌 AppHeader 숨김 (showNav false 단락) → sketch 시 nav/header placeholder 그릴 필요 없음
- SplashScreen 은 **전체 화면 중앙 정렬** (minHeight 100dvh, flex column center) — chrome 룰 미적용, design-system 만 적용
- InstallPrompt 는 **오버레이 모달** (position fixed, inset 0, zIndex 9999) — chrome 룰의 modal 패턴은 inspection 점검 모달 룰과 다른 변형 (인증 전 풀스크린 dimmer) → 패턴 mirror 검토 안 됨

---

# §5. 메모리 룰 inline 인용 (verbatim)

아래 12개 룰을 **각각 별도 미니 카드**로 박제. 각 카드 포맷 (boldface 라벨):

```
### 룰 N — {룰 슬러그}
- **요약**: 1줄
- **Why**: 1줄
- **How to apply (28-splash)**: 1줄
```

12개 룰 (17-annual-plan W1 의 10개 기본 + 28-splash 특화 2개):
1. `feedback_design_sketch_first` — spacing/sizing 도 sketch HTML 시안 먼저
2. `feedback_redesign_sketch_rule_enforcement` — §6.2 negative rule / §6.3 §7.1 일관성
3. `feedback_sketch_realistic_data` — 표시 분기/라벨 룰 verbatim, 시각 디자인만
4. `feedback_planner_prompt_sketch_verbatim` — sketch CSS grep 추출, 추측 X
5. `feedback_tailwind_token_class_pattern` — status- prefix 없음
6. `feedback_tailwind_w8_h8_is_48px` — w-8=48 / w-7=32 함정
7. `feedback_text_caption_leading_none` — 작은 컨테이너 leading-none
8. `feedback_tsx_wave_emoji_dot_gap` — ★ 28-splash 특화 — 이모지 제거 + dot span 추가 + **특수 글리프 ⋮ ⎋ 예외 결정** (콘텐츠 글리프 = 사용자 OS UI 표기 일치 → 유지 허용. 27-login OQ #4 ☎ 와 동일 룰. 일반 장식 이모지는 여전히 제거.)
9. `feedback_tsx_wave_stat_card_drift` — Stat Card §6.3 룰 verbatim
10. `feedback_avoid_premature_confirmation` — "거의 일치" 자신감 표현 금지
11. `feedback_planner_prompt_sketch_verbatim` (재인용/강조) — ★ 28-splash 특화 — 320 라인 2-file 통합 — sketch CSS grep 추출 강도 최대 (line 별 정확한 hex/사이즈/카피 verbatim). 토큰 추측 시 deviation 유발.
12. `project_redesign_15_daily_report_status` — ★ 28-splash 특화 — 15-daily-report SW3 precedent 일반화 — 비즈 시그니처 1 byte 변경 금지 룰. SplashScreen `__APP_VERSION__` Vite define + `@keyframes slideUp` + InstallPrompt isStandalone/isIOS/shouldShowInstallPrompt/dismissInstallPrompt + localStorage key 'pwa-install-dismissed' 모두 1 byte 변경 금지. yearPos 좌표 시스템과 유사한 "비즈 anchor" 보존 패턴.

각 룰의 Why/How 는 28-splash 의 실제 element/상황으로 구체화. 예시 (각 룰 작성 시 참고, 그대로 박제 가능):
- **룰 1** (sketch first) — "InstallPrompt 카드 패딩 (현재 `28px 24px`) + 설치 버튼 height (현재 48) + 노란 경고 박스 padding (`8px 10px`) 같은 spacing 도 변경 시 sketch 먼저 보여주고 컨펌."
- **룰 2** (negative rule) — "노란 경고 박스 `#f59e0b` + `rgba(245,158,11,0.1)` + `rgba(245,158,11,0.25)` 은 status 임계치 색이 아니라 **시스템 안내** (Android 자동 설치 fallback 안내) — `bg-caution-bg` + `text-caution` 알리아스 토큰화 OK. 단 위험 임계치 의미 부여 (예: bad/danger) 금지."
- **룰 3** (realistic data) — "카피 verbatim — 'CBC 방재' (h1 + h2), '소방안전 통합관리 시스템', '시스템 초기화 중...' / '데이터 불러오는 중...' / '준비 완료' (진행 텍스트 3 분기), 'v{__APP_VERSION__} · 경기도 성남시 분당구' (버전), '홈 화면에 앱을 설치하면\\n더 빠르고 편리하게 사용할 수 있습니다' (부제 <br/> 보존), 설치 버튼 label 3 분기 ('홈 화면에 설치' / '설치 방법 보기'), '나중에 할게요', '자동 설치 창이 뜨지 않으면 아래 순서로 설치해 주세요.', Android 3-step + iOS 3-step 모든 타이틀/서브 텍스트 + <strong> 강조 위치 verbatim, '확인했습니다'. 시안에서 변경 금지."
- **룰 4** (verbatim grep) — "W5 TSX 변환 wave 진입 시 sketch HTML 의 CSS 정의 (예: 카드 `background:#1c2128 border:1px solid rgba(59,130,246,0.3) boxShadow:0 8px 40px rgba(0,0,0,0.5)`, 설치 버튼 `linear-gradient(135deg, #2563eb, #0ea5e9)`, 진행 바 `linear-gradient(90deg,#3b82f6,#0ea5e9)`, 노란 경고 박스 `color:#f59e0b background:rgba(245,158,11,0.1)`, 숫자 원 `28x28 radius:8 bg:rgba(59,130,246,0.15) color:#3b82f6 fontSize:13 fontWeight:800`) 를 grep 으로 추출해 그대로 인용. 추측 토큰명 사용 시 deviation 유발."
- **룰 5** (token class pattern) — "설치 버튼 그라데이션 → `bg-safe-bar` solid 치환 / 진행 바 → `bg-accent` solid 치환 / 노란 경고 → `bg-caution-bg` + `text-caution` 알리아스. `bg-status-safe-bar` 같은 status- prefix 사용 시 W5 verify FAIL."
- **룰 6** (w-8=48px) — "InstallPrompt 숫자 원 28x28 (line 120) → w-7=32 함정 (현재 28). 강제 격상 시 w-[28px] 명시 또는 w-7(32) 검토. 설치 버튼 height 48 → h-12(48) 정상 매핑. 나중에 버튼 height 40 → h-10(40) 정상 매핑. 확인 버튼 height 44 → h-11(44) 정상 매핑."
- **룰 7** (leading-none) — "InstallPrompt 서브 텍스트 fontSize 11 (line 123 등 다수) / SplashScreen 진행 텍스트 fontSize 11 (line 68) — 작은 컨테이너 안에서 시각 패딩 유발. leading-none 명시 검토. SplashScreen 부제 12px (line 59) + InstallPrompt 부제 12px (line 84) 도 leading-none 후보 (단 lineHeight:1.5 명시된 곳은 유지)."
- **룰 8** (dot gap + 특수 글리프) — "★ 28-splash 핵심 룰 — 일반 장식 이모지는 sketch 시 제거 + dot span 추가. 단 InstallPrompt 의 `⋮` (U+22EE, Android 메뉴 표기, line 122) + `⎋` (U+238B, iOS 공유 버튼 표기, line 164) 은 **콘텐츠 글리프** — 사용자가 실제 OS 에서 볼 UI 와 1:1 매칭 → 유지 허용 (27-login OQ #4 ☎ 와 동일 룰 — 전화번호 표기). W2~W4 sketch 진입 시 emoji-0 grep 자동 verify gate 에서 ⋮ ⎋ 는 예외로 처리 (또는 grep 패턴이 emoji range 만 잡고 U+22EE/U+238B 는 anchor)."
- **룰 9** (Stat Card drift) — "28-splash 페이지에 Stat Card 없음 → 미적용. 단 sketch 새 패턴 (예: SplashScreen tick 진행 4 state 매트릭스 / InstallPrompt 3 view state 매트릭스 / 설치 버튼 label 3 분기 매트릭스) 은 W5 진입 시 verbatim 인용 필수."
- **룰 10** (premature confirmation) — "W2~W4 sketch 산출 후 '거의 일치 / 잘 됐다' 자신감 표현 금지. 결과 보여주고 사용자 판단."
- **룰 11** (sketch verbatim grep 강조) — "2-file 통합 320 lines — sketch CSS verbatim grep 강도 최대. 특히 카드 boxShadow `0 8px 40px rgba(0,0,0,0.5)` / 오버레이 alpha `rgba(0,0,0,0.85)` / 카드 border `rgba(59,130,246,0.3)` / 노란 경고 박스 3 색 알파 분기 (`0.1` bg / `0.25` border / `#f59e0b` text) / 숫자 원 패턴 (`rgba(59,130,246,0.15)` bg + `#3b82f6` text) — 1 byte 어긋나면 W5 verify FAIL."
- **룰 12** (15-daily-report 좌표 시스템 precedent 일반화) — "★ 28-splash 특화 — 15-daily-report SW3 의 portraitPos % 좌표 보존 룰 일반화. 28-splash 비즈 anchor: `__APP_VERSION__` Vite define 인젝션 (line 73) / `@keyframes slideUp .4s ease-out` (line 79~80) / localStorage key `'pwa-install-dismissed'` 24h TTL (line 227/236) / setInterval 48ms tick (line 18) / setTimeout 1600ms (line 20) / pct +4 per tick (line 18) — 모두 1 byte/숫자 변경 금지."

---

# §6. negative rule (이 wave 에서 금지된 것)

bullet list:
- sketch HTML 생성 금지 (wave 2 부터)
- SplashScreen.tsx + InstallPrompt.tsx + pwaInstall.ts + versionCheck.ts 4개 파일 모두 코드 수정 금지 (이 wave 의 산출물은 markdown 1개)
- 비즈 로직 시그니처 변경 금지 (7건) — SplashScreen.useEffect / handleDismiss / InstallPrompt 자체 export / isStandalone / isIOS / shouldShowInstallPrompt / dismissInstallPrompt 모두 import/export 동일하게 유지. pwaInstall.ts 의 getDeferredInstallPrompt / subscribeInstallPrompt / showInstallPrompt 호출 시그니처 동일. versionCheck.ts 의 checkVersionAndRefresh 호출 동일.
- 다른 페이지 (13-schedule, 14-reports, 27-login, 16-workshift, 17-annual-plan 등) 영향 금지
- **wrangler 명령 금지** (CLAUDE.local.md 룰 — `.claude/settings.local.json` deny 강제, memory `feedback_cbc7119_design_never_wrangler`)
- **`npm run deploy` 금지** (CLAUDE.local.md 룰 — 직원 도메인 경로)
- 13-schedule + 14-reports + 27-login + 16-workshift + 17-annual-plan 의 평면 sketch-wave-*.html 패턴과 다른 폴더 구조 도입 금지 (sketch/ 서브폴더 X — 28-splash/ 직속에 평면 배치)
- App.tsx 미수정 — MOBILE_NO_NAV_PATHS / DESKTOP_NO_NAV_PATHS / DESKTOP_HEADER_HIDE_PATHS / PAGE_TITLES / Route 등재 상태 유지
- **`__APP_VERSION__` Vite define 인젝션 보존** — SplashScreen line 73 의 `v${__APP_VERSION__} · 경기도 성남시 분당구` 카피에서 식별자 변경 금지 (vite.config.ts 의 define 파라미터 연결)
- **`@keyframes slideUp .4s ease-out` 보존** — SplashScreen line 79~80 의 from/to 키프레임 (opacity 0→1, translateY 16px→0) 변경 금지
- **localStorage key `'pwa-install-dismissed'` 24h TTL 보존** — InstallPrompt line 227/236 의 키 문자열 + 24h 윈도우 (24 * 60 * 60 * 1000) 변경 금지
- **특수 글리프 ⋮ (U+22EE) + ⎋ (U+238B) 제거 금지** — InstallPrompt line 122 + 164. Lucide MoreVertical / Share 교체 금지 (콘텐츠 글리프, OQ #5 default).
- 외부 PNG 자산 경로 `/icons/icon-192.png` 변경 금지 (SplashScreen line 54 + InstallPrompt line 78)

---

# §7. open questions (W2 진입 직전 사용자 컨펌)

bullet list 5건:

- **OQ #1**: InstallPrompt 설치 버튼 `linear-gradient(135deg, #2563eb, #0ea5e9)` (line 94) → `bg-safe-bar` solid 통일 OK?
  - default 답: **OK** (27-login W1 OQ #1 + 14-reports W1 OQ #1/#3 + 16-workshift W1 OQ #1 + 17-annual-plan W1 OQ #1 default OK 일관 + design-system §6.4 CTA solid 룰)

- **OQ #2**: SplashScreen 진행 바 fill `linear-gradient(90deg,#3b82f6,#0ea5e9)` (line 66) → `bg-accent` solid 치환 vs 그라데이션 유지?
  - default 답: **solid `bg-accent` 치환 OK** — 진행 바는 시각적 진행 표현이 핵심 (width 변화로 진행 표시) → 색상 그라데이션 없어도 시각 메시지 손상 없음. solid 통일 시 design-system 일관성 + 토큰 정의 효율. 그라데이션 유지 시 인라인만 가능 (토큰 비용 vs 단일 사용처 trade-off, 16-workshift W1 OQ #4 SHIFT_COLOR hex+22 인라인 유지 패턴과 일관 — 다만 진행 바는 단일 element 라 토큰화 default 우세).

- **OQ #3**: 폰트 격상 매핑 — 현재 fontSize:10 (SplashScreen 하단 버전 line 73) / 11 (SplashScreen 진행 텍스트 line 68, InstallPrompt 서브 텍스트 다수) / 12 (SplashScreen 부제 line 59, InstallPrompt 부제 line 84, 노란 경고 line 115, 나중에 버튼 line 106) / 13 (Step 타이틀 + 숫자 원 + 확인 버튼) / 15 (InstallPrompt 설치 버튼 line 96) / 16 (특수 글리프 inline) / 18 (h2) / 22 (h1). §1.1 10~11px 위반. 어디까지 격상?
  - default 답: **부분 절충** — fontSize:10 (버전) → text-caption(12) leading-none 격상 / fontSize:11 (진행 텍스트 + 서브 텍스트) → text-caption(12) leading-none 격상 / fontSize:12 (부제 + 노란 경고 + 나중에 버튼) → text-caption leading-none 유지 (dense 영역 + lineHeight:1.5 명시는 유지) / fontSize:13 (Step 타이틀 + 확인 버튼) → text-label 유지 / fontSize:15 (설치 버튼) → text-body(16) 격상 (CTA 강조 + 노안 친화) / fontSize:16 (특수 글리프 inline) → 콘텐츠 글리프 유지 / fontSize:18 (h2) → text-title / fontSize:22 (h1 splash) → text-heading. 17-annual-plan W1 OQ #3 부분 절충 패턴 mirror.

- **OQ #4**: 외곽 hex 인라인 — SplashScreen `#161b22` (page bg, line 39) / InstallPrompt 카드 `#1c2128` (line 65) / 텍스트 hex `#e6edf3` (primary) + `#8b949e` (secondary) + `#6e7681` (tertiary) + `#3d444d` (disabled) — 디자인 토큰 (bg-surface-page / bg-surface-raised / text-text-primary / -secondary / -tertiary / -disabled) 치환 OK?
  - default 답: **토큰 치환 OK** — 옛 alias 토큰 0건 룰 (memory `feedback_tailwind_token_class_pattern`). 인라인 hex 8개 (page bg 1, card bg 1, text 4, 진행 바 트랙 `rgba(255,255,255,0.07)` 1, 보더 misc 1) 중 토큰 대응 있는 6개 치환, 인라인 잔존 2개 (진행 바 트랙 alpha + 카드 border alpha rgba(59,130,246,0.3)) 는 단일 사용처라 인라인 유지. 카드 boxShadow `0 8px 40px rgba(0,0,0,0.5)` 도 단일 사용처 인라인 유지.

- **OQ #5**: 특수 글리프 ⋮ (Android 가이드 line 122, U+22EE) / ⎋ (iOS 가이드 line 164, U+238B) — Lucide MoreVertical / Share 교체 vs 콘텐츠 글리프 유지?
  - default 답: **콘텐츠 글리프 유지** — 27-login OQ #4 ☎ 와 동일 룰 (전화번호 표기 콘텐츠). 사용자가 실제 Android Chrome / iOS Safari UI 에서 볼 글리프와 1:1 매칭 → 교체 시 인지 부조화. fontSize:16 inline span 형태 유지 (line 122 verticalAlign 무 / line 164 verticalAlign middle). 단 W5 verify gate 에서 emoji-0 grep 검사 시 ⋮ ⎋ 는 예외 처리 (U+22EE / U+238B anchor).

각 OQ 아래에 "default 답" 1줄 (사용자가 별 의견 없으면 이 답으로 진행할 것이라는 reasonable call). 단, "approved" 받기 전까지 W2 진입 금지.

---

작성 완료 후 자체 verify:
1. 7개 섹션 모두 존재 (§1~§7) — grep `^# §[1-7]` 카운트 = 7
2. 메모리 룰 12개 인용 — 각 룰 슬러그 `feedback_*` 10건 + `project_*` 1건 본문 등장, unique grep 카운트 ≥ 10
3. sub-wave 분배 표가 W2~W5 4행 — 표 안에 `| W[2-5] |` 카운트 정확히 4
4. design-system 인용 fence 가 최소 6개 (§1.1 / §1.2 / §1.3 / §6 / §7 / §7.1 6 fence × 2 = 12 open+close 사실상 ≥ 6 라인의 ```)
5. OQ 5건 — `OQ #` 카운트 ≥ 5
6. SplashScreen.tsx + InstallPrompt.tsx + pwaInstall.ts + versionCheck.ts 변경 0 — `git diff --name-only HEAD -- {파일들}` 가 빈 출력
  </action>
  <verify>
    <automated>test -f cha-bio-safety/docs/redesign-context/28-splash/wave-1-index.md && \
echo "--- section (=7) ---" && \
grep -c '^# §[1-7]' cha-bio-safety/docs/redesign-context/28-splash/wave-1-index.md && \
echo "--- subwave (=4) ---" && \
grep -E '^\| W[2-5] \|' cha-bio-safety/docs/redesign-context/28-splash/wave-1-index.md | wc -l && \
echo "--- memory rules (>=10) ---" && \
grep -oE 'feedback_[a-z_]+' cha-bio-safety/docs/redesign-context/28-splash/wave-1-index.md | sort -u | wc -l && \
echo "--- OQ (>=5) ---" && \
grep -cE 'OQ #[1-5]' cha-bio-safety/docs/redesign-context/28-splash/wave-1-index.md && \
echo "--- fence (>=6) ---" && \
grep -c '^```' cha-bio-safety/docs/redesign-context/28-splash/wave-1-index.md && \
echo "--- src unchanged ---" && \
git diff --name-only HEAD -- cha-bio-safety/src/pages/SplashScreen.tsx cha-bio-safety/src/components/InstallPrompt.tsx cha-bio-safety/src/utils/pwaInstall.ts cha-bio-safety/src/utils/versionCheck.ts | wc -l</automated>
  </verify>
  <done>
- `cha-bio-safety/docs/redesign-context/28-splash/wave-1-index.md` 파일 존재 (sketch/ 서브폴더 X — 28-splash/ 직속)
- 7개 섹션 (§1~§7) 모두 존재, grep 결과 = 7
- §2 sub-wave 표가 W2~W5 4행 모두 포함 (정확히 4)
- §1 인벤토리에 5 영역 (Splash 외곽+로고+타이틀, Splash 진행 바+버전+animation, InstallPrompt 오버레이+카드+헤더, InstallPrompt 메인 view 버튼, InstallPrompt Android+iOS 가이드 view) 모두 포함 + 비즈 시그니처 보존 박스 별도 박스 포함
- 메모리 룰 12개 (`feedback_*` 10건 + 28-splash 특화 2건 — feedback_tsx_wave_emoji_dot_gap 특수 글리프 예외 결정 강조 + project_redesign_15_daily_report_status 비즈 anchor 보존 일반화) 모두 inline 인용, unique feedback_ + project_ count ≥ 10
- 28-splash 특화 룰 2건 (특수 글리프 ⋮ ⎋ 콘텐츠 글리프 처리 + 15-daily-report 비즈 anchor 1 byte 변경 금지 일반화) 명시 포함
- design-system.md 인용 fence 최소 6개 (§1.1, §1.2, §1.3, §6, §7, §7.1 모두 fence)
- §4 에 MOBILE_NO_NAV_PATHS ('/' 등재 line 71) + DESKTOP_NO_NAV_PATHS ('/' 등재 line 74) + DESKTOP_HEADER_HIDE_PATHS ('/' 미등재 line 77 — 단 showNav false 라 무의미) + PAGE_TITLES ('/' 미등재) + Route line 265 SplashScreen 실측 결과 박제
- §6 negative rule 안 `wrangler` 와 `npm run deploy` 키워드 모두 등장 + `__APP_VERSION__` Vite define 보존 + `@keyframes slideUp` 보존 + localStorage key `'pwa-install-dismissed'` 24h TTL 보존 + 특수 글리프 ⋮ ⎋ 제거 금지 + 외부 PNG 자산 경로 `/icons/icon-192.png` 보존 + 비즈 시그니처 7건 변경 금지 8건 명시
- §7 OQ 5건 모두 정리, 각각 default 답 1줄 포함 (InstallPrompt 설치 버튼 그라데이션 / SplashScreen 진행 바 그라데이션 / 폰트 격상 부분 절충 / 외곽 hex 토큰 치환 / 특수 글리프 ⋮ ⎋ 콘텐츠 글리프 유지)
- SplashScreen.tsx + InstallPrompt.tsx + pwaInstall.ts + versionCheck.ts 코드 변경 0 — `git diff --name-only` 모두 빈 출력
- 어떠한 sketch-wave-*.html 도 생성하지 않음 (W2 부터)
  </done>
</task>

</tasks>

<verification>
이 wave 의 verify 는 task 1 의 automated block 으로 충분. 추가 phase-level 검증 없음.

자체 검수 흐름:
1. `git status` — wave-1-index.md 1개 신규, 그 외 변경 0
2. `wc -l cha-bio-safety/docs/redesign-context/28-splash/wave-1-index.md` — 합리적 길이 (대략 350~450 줄 예상, SplashScreen 83 + InstallPrompt 237 = 320 lines 2-file 통합 + 비즈 시그니처 박스 + 특수 글리프 룰 박제로 17-annual-plan W1 와 유사 또는 약간 김)
3. 사용자에게 "wave-1-index.md 작성 완료 / §7 OQ 5건 답변 필요" 보고 후 컨펌 대기 (W2 자동 진입 금지, memory `feedback_avoid_premature_confirmation`)
</verification>

<success_criteria>
- wave-1-index.md 7개 섹션 모두 채워짐 (§1 인벤토리 5영역 + 비즈 시그니처 박스 / §2 sub-wave 분배 4행 + 보존/토큰/폰트 미니섹션 / §3 design-system verbatim 6 항목 / §4 chrome 룰 (인증 전 스플래시 = chrome 룰 직접 적용 X + showNav false 단락 실측 박제) / §5 메모리 룰 12개 (10 + 28-splash 특화 2) / §6 negative rule 8건+ / §7 OQ 5건)
- 코드 변경 0건 (SplashScreen.tsx / InstallPrompt.tsx / pwaInstall.ts / versionCheck.ts untouched)
- sketch HTML 0건 생성 (W2 부터)
- 13-schedule + 14-reports + 27-login + 16-workshift + 17-annual-plan 의 평면 sketch-wave-*.html 패턴 mirror — sketch/ 서브폴더 안 만듦
- 사용자 컨펌 받을 OQ 5건 정리됨 (설치 버튼 그라데이션 / 진행 바 그라데이션 / 폰트 격상 / 외곽 hex 토큰 / 특수 글리프 ⋮ ⎋ 처리)
- automated verify 명령이 PASS (section=7 / subwave=4 / rules≥10 / OQ≥5 / fence≥6 / src 변경=0)
</success_criteria>

<output>
After completion, return summary to user:
- 생성 파일: `cha-bio-safety/docs/redesign-context/28-splash/wave-1-index.md`
- §7 OQ 5건 사용자 컨펌 대기
- W2 진입 = OQ #1 답변 후 (`/clear` + 새 `/gsd:quick` 시작 권장 — memory `feedback_gsd_workflow_strict`)
- 다음 wave 파일명 권장: `sketch-wave-2-splash.html` (SplashScreen 전체 — 로고 + 타이틀 + 진행 바 + 버전 + slideUp)
</output>
</task>
</content>
</invoke>