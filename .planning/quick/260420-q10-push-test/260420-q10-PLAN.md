---
phase: 260420-q10-push-test
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cha-bio-safety/functions/api/push/test.ts
  - cha-bio-safety/src/components/SettingsPanel.tsx
  - cha-bio-safety/README.md
autonomous: true
requirements: [Q10-01]
must_haves:
  truths:
    - "admin 계정이 설정 → 알림 섹션에서 '테스트 푸시 보내기' 버튼을 볼 수 있다"
    - "푸시 알림 구독 + 권한 허용 상태일 때만 버튼이 활성화된다"
    - "버튼 클릭 시 현재 로그인한 admin 본인의 모든 구독 엔드포인트에 '테스트 푸시 / 수신 확인용' 알림이 발송된다"
    - "비관리자(assistant)는 POST /api/push/test 호출 시 403을 받는다"
    - "만료된 구독(410/404)은 자동 삭제되고, 성공/실패 건수가 토스트로 표시된다"
    - "VAPID 키가 Pages 환경에 없으면 500과 함께 명확한 에러 메시지를 반환한다"
  artifacts:
    - path: "cha-bio-safety/functions/api/push/test.ts"
      provides: "admin 전용 push test 엔드포인트"
      exports: ["onRequestPost"]
    - path: "cha-bio-safety/src/components/SettingsPanel.tsx"
      provides: "알림 섹션 내 admin 전용 '테스트 푸시 보내기' 버튼"
      contains: "/api/push/test"
    - path: "cha-bio-safety/README.md"
      provides: "푸시 테스트 방법 및 VAPID 키 설정 가이드"
      contains: "푸시 알림 테스트"
  key_links:
    - from: "SettingsPanel.tsx (테스트 버튼 onClick)"
      to: "POST /api/push/test"
      via: "fetch with Bearer token"
      pattern: "/api/push/test"
    - from: "functions/api/push/test.ts"
      to: "push_subscriptions 테이블 + 브라우저 push endpoint"
      via: "D1 SELECT → buildPushPayload → fetch(endpoint)"
      pattern: "buildPushPayload"
    - from: "functions/api/push/test.ts"
      to: "admin 권한 게이트"
      via: "ctx.data.role 검사"
      pattern: "role !== 'admin'"
---

<objective>
관리자(admin) 계정이 자신의 브라우저/기기에 실제로 푸시 알림이 잘 도달하는지 즉시 확인할 수 있도록 "테스트 푸시 발송" 엔드포인트와 SettingsPanel UI 버튼을 추가한다.

Purpose: 현재 푸시 인프라(VAPID, cbc-cron-worker의 sendPush)는 구축되어 있지만 수동으로 수신 여부를 검증할 방법이 없다. admin이 설정에서 버튼 한 번으로 본인 구독에 테스트 페이로드를 쏴서, 실제 iOS/Android/PC 기기에 알림이 뜨는지 확인할 수 있어야 한다.

Output:
- 신규 엔드포인트: `POST /api/push/test` (admin only, 본인 구독 대상)
- SettingsPanel 알림 섹션에 admin 한정 "🔔 테스트 푸시 보내기" 버튼
- README에 테스트 절차 + VAPID 키 Pages 등록 방법 문서화
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@cha-bio-safety/functions/_middleware.ts
@cha-bio-safety/functions/api/push/subscribe.ts
@cha-bio-safety/src/components/SettingsPanel.tsx

<interfaces>
<!-- 실제 코드 기반 핵심 계약. 실행자는 추가 탐색 불필요. -->

From `cha-bio-safety/functions/_middleware.ts`:
```ts
export interface Env {
  DB: D1Database
  STORAGE: R2Bucket
  JWT_SECRET: string
  ENVIRONMENT: string
  // NOTE: VAPID_PUBLIC_KEY / VAPID_PRIVATE_KEY는 Env에 아직 미선언.
  //       Task 1에서 선언 추가 필요(선택 필드로).
}
// middleware가 ctx.data에 주입하는 shape (실제 코드):
//   { staffId: string, staffName: string, role: string }
//   - implementation_hints의 { sub, role, name, title }는 JWT payload 원형이며,
//     실제 ctx.data는 위 shape. Task 1은 반드시 ctx.data.staffId / ctx.data.role 사용.
```

From `cha-bio-safety/functions/api/push/subscribe.ts` (push_subscriptions 테이블 스키마 근거):
```ts
// 테이블 컬럼: id, staff_id, endpoint, p256dh, auth, notification_preferences,
//             created_at, updated_at (JSON preferences 포함).
// INSERT 시 ON CONFLICT(staff_id, endpoint)로 upsert. (id, staff_id, endpoint, p256dh, auth)가 테스트 엔드포인트에 필요한 필드.
```

From `cha-bio-safety/src/components/SettingsPanel.tsx`:
```ts
// authStore 사용 패턴 (실제 코드):
import { useAuthStore } from '../stores/authStore'
const { staff, logout, updateStaff } = useAuthStore()   // 컴포넌트 내부
const token = useAuthStore.getState().token              // 비render 컨텍스트
// staff.role === 'admin' | 'assistant' — admin 체크는 staff?.role === 'admin'
// API 호출 패턴 (handleDbBackup 참조):
const base = import.meta.env.VITE_API_BASE_URL || '/api'
const res = await fetch(`${base}/database/backup`, { headers: { Authorization: `Bearer ${token}` } })
// 토스트:
import toast from 'react-hot-toast'
toast.success('...') / toast.error('...')
```

From `@block65/webcrypto-web-push` (Pages package.json ^1.0.2 확인됨):
```ts
buildPushPayload(
  message: { data: string },
  subscription: { endpoint: string; expirationTime: null; keys: { p256dh: string; auth: string } },
  vapid: { subject: string; publicKey: string; privateKey: string }
): Promise<{ method: string; headers: Record<string,string>; body: BodyInit }>
```
</interfaces>

<corrections_vs_hints>
Implementation hints에서 ctx.data를 `{ sub, role, name, title }`로 표기했지만 실제 `_middleware.ts`는 `{ staffId, staffName, role }`로 주입한다. **Task 1 구현은 반드시 `ctx.data.staffId`와 `ctx.data.role`을 사용**해야 하며, `auth.sub` 같은 표기를 그대로 옮기면 안 된다.
</corrections_vs_hints>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 백엔드 admin push test 엔드포인트 작성</name>
  <files>cha-bio-safety/functions/api/push/test.ts, cha-bio-safety/functions/_middleware.ts</files>
  <action>
1. `cha-bio-safety/functions/_middleware.ts`의 `Env` interface에 아래 두 필드가 없으면 추가한다(있으면 건너뜀):
   ```ts
   VAPID_PUBLIC_KEY?: string
   VAPID_PRIVATE_KEY?: string
   ```
   나머지 middleware 로직은 건드리지 않는다.

2. 신규 파일 `cha-bio-safety/functions/api/push/test.ts`를 생성하고 `onRequestPost`를 export한다. 동작:
   - `ctx.data`를 `{ staffId: string; role: string }` 형태로 캐스팅해서 읽는다 (JWT payload 원형이 아닌 middleware가 주입한 ctx.data 기준, `subscribe.ts`와 동일 패턴 `(ctx as any).data?.staffId`).
   - `role !== 'admin'` 이면 `{ success: false, error: '관리자 전용' }` 403 반환.
   - `ctx.env.VAPID_PUBLIC_KEY` 또는 `ctx.env.VAPID_PRIVATE_KEY`가 falsy면 `{ success: false, error: 'VAPID 키가 서버에 설정되지 않았습니다' }` 500 반환 (운영자가 누락을 즉시 진단할 수 있어야 함).
   - `push_subscriptions`에서 `SELECT id, staff_id, endpoint, p256dh, auth WHERE staff_id = ?` 로 본인 구독만 조회. 결과 0건이면 `{ success: false, error: '등록된 푸시 구독이 없습니다. 먼저 구독을 켜주세요.' }` 400.
   - 페이로드: `{ title: '테스트 푸시', body: '수신 확인용', type: 'test' }`.
   - 각 구독에 대해 `@block65/webcrypto-web-push`의 `buildPushPayload`로 암호화한 뒤 endpoint에 `fetch`. 결과:
     - 200-299: 성공 집계
     - 410 / 404: `DELETE FROM push_subscriptions WHERE id = ?` 실행 후 '구독 만료 — 삭제됨' 기록
     - 그 외: `status {code}` 기록
     - try-catch로 개별 구독 실패가 전체를 중단시키지 않도록 함.
   - 반환: `{ success: successCount > 0, data: { sent, total, results }, error?: '모든 구독에 발송 실패' }`
   - VAPID subject는 implementation_hints와 동일하게 `mailto:admin@chabio.com` 사용.
   - import 경로: `import type { Env } from '../../_middleware'`, `import { buildPushPayload } from '@block65/webcrypto-web-push'`.

3. **주의사항**:
   - `sw.ts`를 수정하지 않는다.
   - `cbc-cron-worker/**`는 일절 건드리지 않는다 (별도 배포 대상).
   - `console.error`는 catch 블록에서만 사용 (subscribe.ts 톤과 맞춤).
   - 중간 파일 생성 금지, README 업데이트는 Task 3에서.
  </action>
  <verify>
    <automated>ls cha-bio-safety/functions/api/push/test.ts &amp;&amp; grep -c "onRequestPost" cha-bio-safety/functions/api/push/test.ts &amp;&amp; grep -c "buildPushPayload" cha-bio-safety/functions/api/push/test.ts &amp;&amp; grep -c "role !== 'admin'" cha-bio-safety/functions/api/push/test.ts &amp;&amp; grep -c "VAPID 키가 서버에 설정되지 않았습니다" cha-bio-safety/functions/api/push/test.ts &amp;&amp; cd cha-bio-safety &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <done>
- `cha-bio-safety/functions/api/push/test.ts` 존재, `onRequestPost` export.
- `buildPushPayload`, `role !== 'admin'`, VAPID 누락 에러 문구가 모두 포함됨.
- `Env`에 VAPID 키 필드 선언(없었다면) 추가됨.
- `npx tsc --noEmit` 0 errors.
- `sw.ts`, `cbc-cron-worker/**`, 기타 push 엔드포인트 파일 변경 없음.
  </done>
</task>

<task type="auto">
  <name>Task 2: SettingsPanel에 admin 전용 '테스트 푸시 보내기' 버튼 추가</name>
  <files>cha-bio-safety/src/components/SettingsPanel.tsx</files>
  <action>
1. 기존 알림 섹션 (line 641 근처 `{/* 알림 */}` 블록) 내, `!notifCollapsed && (<div ...>{...})` 내부 박스 **최상단**(점검 그룹 `<div>점검</div>` 바로 위)에 admin 전용 버튼을 추가한다.

2. 버튼 노출/활성 조건:
   - 컴포넌트가 이미 가지고 있는 authStore state를 사용한다. 기존 패턴 확인:
     - 파일 상단 기존 `useAuthStore()` 호출 라인에서 `staff`가 이미 destructure되어 있으면 그대로 활용.
     - 없으면 기존 `useAuthStore()` 호출 근처에 `staff`를 추가로 꺼낸다 (중복 호출 금지, 기존 코드 재사용).
   - 조건식: `staff?.role === 'admin' && subscribed && permState === 'granted'` 일 때만 버튼 렌더. (`subscribed`, `permState`는 해당 섹션에서 이미 사용 중인 변수.)

3. 버튼 UI:
   - 라벨: `🔔 테스트 푸시 보내기`
   - 스타일: 섹션 내 다른 보조 버튼 톤과 맞춤 (`background: 'var(--bg4)'`, `color: 'var(--t1)'`, `border: '1px solid var(--bd2)'`, `borderRadius: 8`, `padding: '7px 10px'`, `fontSize: 11`, `fontWeight: 600`, `width: '100%'`, `marginBottom: 8`, `cursor: 'pointer'`). 전송 중에는 `disabled` 처리 + 라벨을 `⏳ 전송 중...`으로 교체하기 위한 `useState<boolean>` 하나 추가 (`const [testSending, setTestSending] = useState(false)` — 기존 state 선언 블록 근처에 둘 것).

4. onClick 동작 (비render 컨텍스트라 `useAuthStore.getState().token` 사용, 기존 handleDbBackup 패턴과 동일):
   ```ts
   const token = useAuthStore.getState().token
   const base = import.meta.env.VITE_API_BASE_URL || '/api'
   setTestSending(true)
   try {
     const res = await fetch(`${base}/push/test`, {
       method: 'POST',
       headers: { Authorization: `Bearer ${token}`, 'Content-Type': 'application/json' },
     })
     const json = await res.json() as { success: boolean; data?: { sent: number; total: number }; error?: string }
     if (json.success) {
       toast.success(`테스트 푸시 발송: ${json.data?.sent ?? 0}/${json.data?.total ?? 0} 성공`)
     } else {
       toast.error(`실패: ${json.error ?? '알 수 없는 오류'}`)
     }
   } catch {
     toast.error('요청 실패: 네트워크 확인')
   } finally {
     setTestSending(false)
   }
   ```

5. **건드리지 말 것**:
   - 다른 섹션(화면/계정/DB 백업/등) 로직 전부.
   - `notifCollapsed` 기본 동작.
   - 점검/일정 토글 Row들의 순서/내용.
  </action>
  <verify>
    <automated>grep -c "테스트 푸시 보내기" cha-bio-safety/src/components/SettingsPanel.tsx &amp;&amp; grep -c "/push/test" cha-bio-safety/src/components/SettingsPanel.tsx &amp;&amp; grep -c "staff?.role === 'admin'" cha-bio-safety/src/components/SettingsPanel.tsx &amp;&amp; cd cha-bio-safety &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <done>
- SettingsPanel.tsx에 "테스트 푸시 보내기" 문자열 + `/push/test` 경로 + `staff?.role === 'admin'` 가드 모두 존재.
- 전송 중 상태(`testSending`) 관리로 중복 클릭 방지.
- `npx tsc --noEmit` 0 errors.
- 다른 SettingsPanel 로직에 의도치 않은 diff 없음(점검/일정 토글, DB 백업 등 무변경).
  </done>
</task>

<task type="auto">
  <name>Task 3: README에 푸시 테스트 & VAPID 키 등록 가이드 작성</name>
  <files>cha-bio-safety/README.md</files>
  <action>
`cha-bio-safety/README.md`는 현재 존재하지 않음 (확인됨). 신규 생성한다. 내용:

```markdown
# CHA Bio Complex Fire Safety Management (cha-bio-safety)

차바이오컴플렉스 방재팀 소방안전 통합관리 PWA (Cloudflare Pages + D1 + R2).

## 푸시 알림 테스트

관리자(admin) 계정으로 본인 브라우저/기기에 테스트 푸시가 실제로 도달하는지 확인할 수 있다.

1. admin 계정으로 로그인한다.
2. 설정(우상단 ⚙️) → 알림 → "푸시 알림" 토글을 ON.
3. 브라우저가 알림 권한을 묻는다 → 허용.
4. 같은 알림 섹션 내 "🔔 테스트 푸시 보내기" 버튼을 클릭.
5. 기기에 **제목: 테스트 푸시 / 본문: 수신 확인용** 알림이 뜨면 OK.
6. 성공/실패 건수가 토스트로 표시된다 (예: `테스트 푸시 발송: 1/1 성공`).

만료된 구독(HTTP 410/404)은 자동으로 DB에서 삭제된다.

### VAPID 키 설정 (최초 1회)

Worker와 Pages **양쪽에 동일한 VAPID 공개/비밀 키** 쌍을 등록해야 한다.

```bash
# Worker (cbc-cron-worker) — 기존에 이미 등록되어 있을 수 있음. `npx wrangler secret list`로 확인.
cd cbc-cron-worker
npx wrangler secret put VAPID_PUBLIC_KEY
npx wrangler secret put VAPID_PRIVATE_KEY

# Pages (cha-bio-safety) — 위와 동일한 값을 사용할 것
cd ../cha-bio-safety
npx wrangler pages secret put VAPID_PUBLIC_KEY --project-name cbc7119
npx wrangler pages secret put VAPID_PRIVATE_KEY --project-name cbc7119
```

`VAPID_PUBLIC_KEY` / `VAPID_PRIVATE_KEY`가 Pages에 없으면 테스트 버튼 클릭 시 500
`VAPID 키가 서버에 설정되지 않았습니다` 에러가 반환된다 — 등록 누락 진단용 신호.

### 권한 정책

- `POST /api/push/test`는 admin 전용 (assistant는 403).
- 발송 대상은 **호출자 본인의 구독만** (다른 사용자 기기로 발송하지 않는다).

## 배포

```bash
cd cha-bio-safety
npm run deploy          # 프로덕션에 배포하려면 반드시 --branch production 사용하는 script인지 확인
```

※ 직접 `wrangler pages deploy`를 쓸 경우 **`--branch production` 누락 시 Preview 환경으로 배포된다.**
```

- 루트 프로젝트 `/Users/jykevin/Documents/20260328/README.md`는 변경 금지(존재하지 않거나 타 맥락).
- 오직 `cha-bio-safety/README.md`만 신규 생성.
  </action>
  <verify>
    <automated>ls cha-bio-safety/README.md &amp;&amp; grep -c "푸시 알림 테스트" cha-bio-safety/README.md &amp;&amp; grep -c "VAPID_PUBLIC_KEY" cha-bio-safety/README.md &amp;&amp; grep -c "cbc7119" cha-bio-safety/README.md</automated>
  </verify>
  <done>
- `cha-bio-safety/README.md`가 존재하고 "푸시 알림 테스트", "VAPID 키 설정", `cbc7119` 키워드가 모두 포함됨.
- 빌드에는 영향 없음 (docs only).
  </done>
</task>

</tasks>

<verification>
전체 플랜 완료 후 한 번에 확인:

```bash
# 1. 파일 존재
ls cha-bio-safety/functions/api/push/test.ts
ls cha-bio-safety/README.md

# 2. 핵심 가드 모두 걸려있는지
grep -c "role !== 'admin'" cha-bio-safety/functions/api/push/test.ts            # ≥1
grep -c "buildPushPayload"  cha-bio-safety/functions/api/push/test.ts            # ≥1
grep -c "VAPID 키가 서버에 설정되지 않았습니다" cha-bio-safety/functions/api/push/test.ts   # ≥1
grep -c "staff?.role === 'admin'" cha-bio-safety/src/components/SettingsPanel.tsx  # ≥1
grep -c "/push/test" cha-bio-safety/src/components/SettingsPanel.tsx             # ≥1
grep -c "테스트 푸시 보내기" cha-bio-safety/src/components/SettingsPanel.tsx     # ≥1

# 3. 타입체크
cd cha-bio-safety && npx tsc --noEmit   # 0 errors

# 4. 건드리지 않은 범위 검증
git diff --name-only | grep -E "(sw\.ts|cbc-cron-worker/)" | wc -l   # must be 0
```

배포 전 수동 확인(체크리스트, 배포자용):
- [ ] `cd cha-bio-safety && npx wrangler pages secret list --project-name cbc7119` 에 `VAPID_PUBLIC_KEY`, `VAPID_PRIVATE_KEY` 존재
- [ ] 존재하지 않으면 Worker secret과 동일한 값으로 등록
- [ ] `npm run deploy` 시 production 브랜치로 배포되는지 확인 (CLAUDE.md 피드백 항목)
- [ ] 프로덕션에서 admin 로그인 → 구독 ON → 버튼 클릭 → 기기 알림 수신 확인 (로컬 서버 테스트 X, 반드시 프로덕션)
</verification>

<success_criteria>
- POST `/api/push/test` 엔드포인트가 admin에게만 동작하고, 본인 구독 endpoint로 실제 Web Push를 발송한다.
- SettingsPanel 알림 섹션에 admin 전용 "🔔 테스트 푸시 보내기" 버튼이 표시되고, 클릭 시 토스트로 성공/실패 건수를 알려준다.
- 만료 구독(410/404)은 자동 정리된다.
- VAPID 키 누락 시 500과 함께 명확한 에러 메시지가 반환된다.
- `cha-bio-safety/README.md`에 테스트 절차와 VAPID 키 등록 명령이 문서화된다.
- `sw.ts`, `cbc-cron-worker/**`에 diff 없음. `npx tsc --noEmit` 0 errors.
</success_criteria>

<output>
완료 후 `.planning/quick/260420-q10-push-test/260420-q10-SUMMARY.md`를 생성.
</output>
