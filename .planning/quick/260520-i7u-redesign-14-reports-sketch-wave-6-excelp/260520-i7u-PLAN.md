---
phase: quick-260520-i7u
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cha-bio-safety/docs/redesign-context/14-reports/sketch-wave-6-desktop-preview-wrapper.html
autonomous: true
requirements:
  - REDESIGN-14-W6
must_haves:
  truths:
    - "sketch-wave-6-desktop-preview-wrapper.html 1개 파일이 14-reports/ 평면 sibling 위치에 존재한다"
    - "1280px 데스크톱 frame 4개 (다크 div-early / 다크 자탐 / 라이트 div-early / 라이트 자탐) 모두 렌더링된다"
    - "각 frame 의 우측 .preview-wrapper 안에 .a4-preview 가 흰배경 (#ffffff) + A4 비율 (210/297) 로 표시된다"
    - "4 frame 모두에서 좌측은 W5 책임 dashed sidelist placeholder, 상단은 W4 책임 dashed toolbar placeholder 로 노출된다"
    - "F1/F3 의 A4 안에 '월초 유수검지 장치 점검표' verbatim 텍스트가, F2/F4 의 A4 안에 '자동화재탐지설비 점검일지' verbatim 텍스트가 표시된다"
    - "§6 verify gate ≥15 모두 PASS — 이모지/그라데이션/status- prefix/가운뎃점 본문/w-8h-8/9·10·11px 본문 0건"
    - "ReportsPage.tsx / ExcelPreview.tsx / 다른 페이지 / src 어디도 수정되지 않는다"
  artifacts:
    - path: "cha-bio-safety/docs/redesign-context/14-reports/sketch-wave-6-desktop-preview-wrapper.html"
      provides: "W6 우측 ExcelPreview 래퍼 영역 시안 (4 frame matrix)"
      contains: ".preview-wrapper, .a4-preview, aspect-ratio: 210"
      min_lines: 400
  key_links:
    - from: "sketch-wave-6-desktop-preview-wrapper.html"
      to: ".a4-preview 흰배경 고정"
      via: "background: #ffffff 다크/라이트 모두 동일"
      pattern: "background:\\s*#ffffff"
    - from: "sketch-wave-6-desktop-preview-wrapper.html"
      to: "ReportsPage.tsx line 298~300 wrapper source"
      via: ".preview-wrapper CSS = flex 1 + overflow hidden + bg surface-page"
      pattern: "\\.preview-wrapper\\s*\\{"
    - from: ".a4-preview"
      to: "A4 210mm 비율"
      via: "aspect-ratio: 210 / 297 + max-width: 595px"
      pattern: "aspect-ratio:\\s*210"
---

<objective>
redesign/14-reports sketch wave 6 — 데스크톱 1280px frame 우측 ExcelPreview 래퍼 영역의 시안. ReportsPage.tsx line 297~300 의 `<div style={{ flex: 1, overflow: 'hidden', background: 'var(--bg)' }}><ExcelPreview ... /></div>` wrapper 를 sketch 로 변환하여, 우측 panel 안에 A4 흰배경 (다크/라이트 무관 고정) 미리보기 영역을 dashed grid placeholder 로 시연한다.

Purpose: W7 TSX 변환 wave 가 사용할 `.preview-wrapper` + `.a4-preview` CSS class 정의를 박제하고, 사용자에게 "다크 모드에서도 A4 영역은 흰배경 유지" 룰을 시각 컨펌 받는 wave. ExcelPreview.tsx 본체는 손대지 않으며, 본 wave 는 wrapper layout + A4 영역 시각화에 한정한다.

Output: `cha-bio-safety/docs/redesign-context/14-reports/sketch-wave-6-desktop-preview-wrapper.html` 1개 파일 (평면 sibling, atomic 1-commit).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.local.md
@cha-bio-safety/docs/redesign-context/14-reports/wave-1-index.md
@cha-bio-safety/docs/redesign-context/14-reports/sketch-wave-5-desktop-sidelist.html
@cha-bio-safety/docs/redesign-context/14-reports/sketch-wave-4-desktop-toolbar.html
@cha-bio-safety/docs/redesign-context/14-reports/tokens.css
@cha-bio-safety/src/pages/ReportsPage.tsx

<interfaces>
<!-- W6 wrapper source (ReportsPage.tsx line 297~300) — verbatim 인용 -->

```tsx
{/* 우측 이미지 미리보기 + 데이터 오버레이 */}
<div style={{ flex: 1, overflow: 'hidden', background: 'var(--bg)' }}>
  <ExcelPreview reportType={selectedType} year={year} month={month} />
</div>
```

ExcelPreview props 시그니처 (ExcelPreview.tsx line 6~14):
- `reportType: ReportType | null` (10종: div-early | div-late | 소화전 | 청정소화약제 | 비상콘센트 | 피난방화 | 방화셔터 | 제연 | 자탐 | 소방펌프)
- `year: number`
- `month?: number`

본 wave 는 ExcelPreview 내부에 들어가지 않음. wrapper layout + A4 placeholder 시각만.

W5 sketch 의 .placeholder-preview (line 446~460) CSS — 본 wave 에서 .preview-wrapper + .a4-preview 로 격상 대체:
```css
.placeholder-preview {
  flex: 1;
  background: var(--surface-page);
  border: 1px dashed var(--border-default);
  margin: 16px;
  ...
}
```

W6 신규 CSS (task_scope verbatim):
```css
.preview-wrapper {
  flex: 1;
  overflow: hidden;
  background: var(--surface-page);
  display: flex;
  align-items: flex-start;
  justify-content: center;
  padding: 32px;
}

.a4-preview {
  width: 100%;
  max-width: 595px;
  aspect-ratio: 210 / 297;
  background: #ffffff;                /* 다크/라이트 무관 고정 */
  border: 1px solid var(--border-default);
  box-shadow: 0 4px 20px rgba(0,0,0,0.08);
  border-radius: 4px;
  overflow: hidden;
  display: flex;
  flex-direction: column;
  padding: 32px 28px;
}

.a4-preview-title {
  font-size: 18px;
  font-weight: 700;
  color: #1f2328;                    /* A4 흰배경 위 검정 텍스트 고정 */
  text-align: center;
  margin-bottom: 8px;
  letter-spacing: -0.02em;
}

.a4-preview-meta {
  font-size: 12px;
  color: #656d76;
  text-align: center;
  margin-bottom: 24px;
  letter-spacing: 0.05em;
}

.a4-preview-table-placeholder {
  flex: 1;
  display: grid;
  grid-template-columns: 1.4fr repeat(12, 1fr);
  gap: 0;
  border: 1px dashed #d0d7de;
  border-radius: 2px;
  font-size: 12px;
  color: #656d76;
  align-items: center;
  justify-items: center;
  text-align: center;
  overflow: hidden;
}

.a4-preview-cell {
  padding: 8px 4px;
  border: 1px dashed #d0d7de;
  font-size: 12px;
  color: #656d76;
  height: 100%;
  display: flex;
  align-items: center;
  justify-content: center;
}

.a4-preview-cell--header {
  background: #f6f8fa;
  font-weight: 700;
  color: #1f2328;
}
```

W5 sketch 의 frame chrome — 본 wave 가 그대로 mirror:
- `.frame-desktop` (1280px max-width, surface-page bg, border-default 1px, radius 18, overflow hidden)
- `.global-header-placeholder` (h-48, rgba(0,0,0,0.4), '/reports 자체 헤더 페이지' 라벨)
- `.placeholder-toolbar` (h-48, dashed border-bottom, surface-raised bg, 'W4 책임 — 상단 toolbar (연도 / 일괄 / spacer / 선택 타이틀 / 개별)' 라벨)
- `.below-toolbar` (display: flex, height: 520px)
- `.sidelist` placeholder (W5 책임) — 본 wave 에서는 dashed bar 로 단순화하여 표현. width: 260px / border-right default / W5 책임 라벨 + selected row hint 1줄 텍스트만.
- `.variant-label` (12px monospace, frame 외부 chrome)
- `.frame-stack` (gap 32px, max-width 1280px)
- 다크/라이트 :root + [data-theme="light"] 토큰 (tokens.css line 16~119 verbatim)
- Spacing / Radius / Typography 토큰 (W5 mirror)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: W6 sketch HTML 작성 (.preview-wrapper + .a4-preview 4 frame matrix)</name>
  <files>cha-bio-safety/docs/redesign-context/14-reports/sketch-wave-6-desktop-preview-wrapper.html</files>
  <action>
    W5 sketch (sketch-wave-5-desktop-sidelist.html) 를 base 로 4 frame matrix HTML 작성. 다음 사항을 verbatim 으로 반영:

    1. **상단 주석 블록**: W5 헤더 주석 패턴 mirror. 다음 항목 포함:
       - 파일명 / 작성일 (2026-05-20) / Quick ID 260520-i7u / 범위 / out of scope
       - W1 LOCKED 5건 적용 매트릭스 (W6 관점: OQ #5 dot span 본 wave 에는 sub 텍스트 없으므로 미해당, OQ #2 sidelist 260px 는 W5 책임이지만 본 wave 의 below-toolbar layout 에서 mirror 유지)
       - W5 → W6 진입 시 LOCKED OQ 3건 verbatim:
         * OQ #1 LOCKED — ExcelPreview 영역 = 단순 미리보기 자리 (내부 그리드 오버레이는 ExcelPreview.tsx 본체 책임. W6 wrapper layout 만)
         * OQ #2 LOCKED — A4 흰배경 고정 (다크/라이트 자동 전환 X) — 본 wave 의 핵심 룰. .a4-preview 의 background: #ffffff 고정.
         * OQ #3 LOCKED — 빈 상태 UI 불필요 (sidelist 항상 default selected, useState&lt;ReportType&gt;('div-early') 초기값)
       - 메모리 룰 10건 박제 (W5 의 10건 mirror — feedback_design_sketch_first / feedback_sketch_realistic_data / feedback_planner_prompt_sketch_verbatim / feedback_tailwind_token_class_pattern / feedback_tailwind_w8_h8_is_48px / feedback_text_caption_leading_none / feedback_tsx_wave_emoji_dot_gap / feedback_avoid_premature_confirmation / feedback_cbc7119_design_never_wrangler / feedback_tsx_wave_stat_card_drift). 각 룰 = 요약 1줄 + W6 컨텍스트 1~2줄.
       - W7 진입 전 OQ 2건 default 답 명시:
         * OQ #1 default: (a) @layer components 안에 정의 — src/styles/components.css 에 박제, ReportsPage.tsx 는 className 사용. 근거: feedback_planner_prompt_sketch_verbatim 룰.
         * OQ #2 default: 3 sub-wave (CSS layer 정의 + 모바일 컴포넌트 + 데스크톱 컴포넌트).

    2. **CSS** (style 블록 안):
       - 다크/라이트 토큰 (W5 line 96~198 verbatim)
       - Spacing (W5 line 203~238 verbatim) / Radius (W5 line 243~248 verbatim) / Typography (W5 line 253~302 verbatim) / leading-none helper (W5 line 305)
       - Sketch chrome 헬퍼: page-bg-dark / page-bg-light / frame-desktop / global-header-placeholder (W5 line 309~338 verbatim)
       - W5 mirror — placeholder-toolbar (W5 line 425~437) + below-toolbar (W5 line 440~443) verbatim. 본 wave 에서 toolbar 는 여전히 W4 책임 placeholder dashed.
       - **W6 신규**: .preview-wrapper / .a4-preview / .a4-preview-title / .a4-preview-meta / .a4-preview-table-placeholder / .a4-preview-cell / .a4-preview-cell--header (task_scope §source verbatim CSS, interfaces 섹션 참조)
       - W5 sidelist 의 .sidelist / .sidelist-row 등 본 wave 에서 미사용 — 대신 .placeholder-sidelist 라는 단순화된 dashed placeholder 정의 (W5 책임 명시용 라벨 컨테이너). CSS: `flex-shrink: 0; width: 260px; border-right: 1px solid var(--border-default); padding: 16px; display: flex; flex-direction: column; gap: 8px; background: var(--surface-raised); color: var(--text-tertiary); font-family: monospace; font-size: 12px; letter-spacing: 0.05em; line-height: 1.6;`
       - .placeholder-sidelist-row (selected row hint): `padding: 8px 12px; border-left: 3px solid var(--accent); background: var(--surface-sunken); color: var(--text-secondary); border-radius: 4px;`
       - .variant-label (W5 line 479~486 verbatim) / .frame-stack (W5 line 489~496 verbatim)

    3. **Tailwind config script** (W5 line 498~540 verbatim mirror — surface/text/border/accent/safe/danger/warning/info/fire 토큰 매핑 + borderRadius 토큰)

    4. **Body — frame matrix 4건**:

       각 frame 구조 (1280px outer):
       ```
       <div class="frame-desktop" data-theme="dark|light">
         <div class="global-header-placeholder">App GlobalHeader ...</div>
         <div class="placeholder-toolbar">W4 책임 — 상단 toolbar ...</div>
         <div class="below-toolbar">
           <div class="placeholder-sidelist">
             [W5 책임 라벨 + selected row hint 1줄: '월초 유수검지 장치 점검표 (selected)' or '자동화재탐지설비 점검일지 (selected)']
           </div>
           <div class="preview-wrapper">
             <div class="a4-preview">
               <div class="a4-preview-title">[F1/F3: '월초 유수검지 장치 점검표' / F2/F4: '자동화재탐지설비 점검일지']</div>
               <div class="a4-preview-meta">[F1/F3: '2026년도 / 월별 점검 결과' / F2/F4: '2026년도 / 자탐 카테고리 점검']</div>
               <div class="a4-preview-table-placeholder">
                 [헤더 row: 항목 / 1월 / 2월 / ... / 12월 = 13개 .a4-preview-cell--header]
                 [본문 row × 6개 (F1/F3: DIV-1 ~ DIV-6 / F2/F4: 소방용전원공급반 / 연기감지기 / 열감지기 / 발신기 / 수신기 / 경종): 각 row = 항목 cell + 12 빈 cell]
               </div>
             </div>
           </div>
         </div>
       </div>
       ```

       Frame matrix:
       | F | Theme | Selected | A4 title | A4 meta | placeholder-sidelist-row hint |
       |---|---|---|---|---|---|
       | 1 | dark | div-early | '월초 유수검지 장치 점검표' | '2026년도 / 월별 점검 결과' | '월초 유수검지 장치 점검표 (selected)' |
       | 2 | dark | 자탐 | '자동화재탐지설비 점검일지' | '2026년도 / 자탐 카테고리 점검' | '자동화재탐지설비 점검일지 (selected)' |
       | 3 | light | div-early mirror | '월초 유수검지 장치 점검표' | '2026년도 / 월별 점검 결과' | '월초 유수검지 장치 점검표 (selected)' |
       | 4 | light | 자탐 mirror | '자동화재탐지설비 점검일지' | '2026년도 / 자탐 카테고리 점검' | '자동화재탐지설비 점검일지 (selected)' |

       각 frame 전에 `.variant-label` 1줄 (예: 'Frame 1 / 다크 div-early selected + A4 흰배경 (1280px 데스크톱)').

       A4 안 body row 의 placeholder 텍스트 — 시각만 위한 최소 표현 (실제 데이터 시연 X):
       - F1/F3: DIV-1, DIV-2, DIV-3, DIV-4, DIV-5, DIV-6 (실제 ReportsPage 데이터 흉내 X)
       - F2/F4: 소방용전원공급반, 연기감지기, 열감지기, 발신기, 수신기, 경종 (자탐 카테고리)

       각 본문 row 12 빈 cell 은 `<div class="a4-preview-cell"></div>` 로 비워두기 (체크 표시 X — 실제 데이터 시연 아님). 항목 cell 만 텍스트 채움.

    5. **Notes block** (frame 외부, max-width 1280px):
       - "W6 검토 포인트" h2 + 6~8 bullet (A4 흰배경 고정 / aspect-ratio 210:297 / preview-wrapper padding 32 / placeholder-sidelist 는 W5 책임 더미 / placeholder-toolbar 는 W4 책임 더미 / ExcelPreview.tsx 본체 손대지 않음 / W5 → W6 LOCKED OQ 3건 적용 결과 / 그라데이션 0건)
       - "W7 진입 전 OQ 2건 (default 답 채택 시 별 의견 없는 것으로 진행)" h2 + 2 bullet (CSS layer 위치 = (a) @layer components / sub-wave 분할 = 3 sub-wave)
       - "W7 변환 wave 가 인용할 CSS class verbatim 인덱스" h2 + 7 bullet (.preview-wrapper / .a4-preview / .a4-preview-title / .a4-preview-meta / .a4-preview-table-placeholder / .a4-preview-cell / .a4-preview-cell--header)
       - "Frame matrix 4건 정리" h2 + 4 bullet (각 frame 의 theme/selected/A4 title)

    6. **Negative gate 자체 검증** (작성 후 grep 결과 주석으로 박제):
       작성자가 본인 verify 결과를 HTML 끝 주석에 박제. 단순 라벨 외 검증 명령 결과 (예: '`grep -c "background:\s*#ffffff" output.html` = 4 PASS') 6~8건.

    중요 룰:
    - **이모지 0** (⬇ ✅ ❌ ⚠️ 📄 등)
    - **linear-gradient 0** (전체 파일)
    - **text-status-* / bg-status-* prefix 0** (status- 없는 패턴만)
    - **본문 가운뎃점 ' · ' 0** (dot-meta span 도 본 wave 에서 sub 텍스트 자체가 없으므로 미사용)
    - **w-8 h-8 utility class 0** (Tailwind class. 인라인 px 값은 OK)
    - **9·10·11px 본문 0** (variant-label / placeholder 라벨의 12px monospace 는 chrome — OK)
    - **빈 상태 UI 0** ('선택된 보고서 없음' 같은 텍스트 표시 X — W5 OQ #3 LOCKED)
    - **그리드 오버레이 실제 데이터 채움 0** (placeholder dashed line + 비어있는 cell 만 OK)
    - **ExcelPreview.tsx 손대지 않음**
    - **ReportsPage.tsx / App.tsx / 다른 페이지 / src 손대지 않음**
    - **wrangler / npm run deploy 0건** (CLAUDE.local.md 룰)
  </action>
  <verify>
    <automated>
      F=cha-bio-safety/docs/redesign-context/14-reports/sketch-wave-6-desktop-preview-wrapper.html;
      test -f "$F" || (echo "FAIL: file missing"; exit 1);
      # Negative gates (≥6)
      G1=$(grep -cE '⬇|📄|✅|❌|⚠️|🎯' "$F"); echo "G1 emoji = $G1 (expect 0)"; test "$G1" -eq 0 || (echo "FAIL G1"; exit 1);
      G2=$(grep -c 'linear-gradient' "$F"); echo "G2 gradient = $G2 (expect 0)"; test "$G2" -eq 0 || (echo "FAIL G2"; exit 1);
      G3=$(grep -cE '(text|bg)-status-' "$F"); echo "G3 status-prefix = $G3 (expect 0)"; test "$G3" -eq 0 || (echo "FAIL G3"; exit 1);
      G4=$(grep -cE 'class="[^"]*\bw-8\b' "$F"); G4b=$(grep -cE 'class="[^"]*\bh-8\b' "$F"); echo "G4 w-8=$G4 h-8=$G4b (expect 0/0)"; test "$G4" -eq 0 && test "$G4b" -eq 0 || (echo "FAIL G4"; exit 1);
      G5=$(grep -v '^[[:space:]]*\*\|^[[:space:]]*//' "$F" | grep -cE 'font-size:\s*(9|10|11)px'); echo "G5 9-11px = $G5 (expect 0)"; test "$G5" -eq 0 || (echo "FAIL G5"; exit 1);
      # Positive gates (≥10)
      P1=$(grep -cE '\.preview-wrapper\s*\{' "$F"); echo "P1 preview-wrapper CSS = $P1 (expect >=1)"; test "$P1" -ge 1 || (echo "FAIL P1"; exit 1);
      P2=$(grep -cE '\.a4-preview\s*\{' "$F"); echo "P2 a4-preview CSS = $P2 (expect >=1)"; test "$P2" -ge 1 || (echo "FAIL P2"; exit 1);
      P3=$(grep -cE 'aspect-ratio:\s*210' "$F"); echo "P3 aspect-ratio 210 = $P3 (expect >=1)"; test "$P3" -ge 1 || (echo "FAIL P3"; exit 1);
      P4=$(grep -cE 'background:\s*#ffffff' "$F"); echo "P4 #ffffff bg = $P4 (expect >=4)"; test "$P4" -ge 4 || (echo "FAIL P4"; exit 1);
      P5=$(grep -c '월초 유수검지 장치 점검표' "$F"); echo "P5 div-early title = $P5 (expect >=2)"; test "$P5" -ge 2 || (echo "FAIL P5"; exit 1);
      P6=$(grep -c '자동화재탐지설비 점검일지' "$F"); echo "P6 자탐 title = $P6 (expect >=2)"; test "$P6" -ge 2 || (echo "FAIL P6"; exit 1);
      P7=$(grep -oE 'feedback_[a-z0-9_]+\.md' "$F" | sort -u | wc -l); echo "P7 memory rules unique = $P7 (expect >=7)"; test "$P7" -ge 7 || (echo "FAIL P7"; exit 1);
      P8=$(grep -c 'variant-label' "$F"); echo "P8 variant-label uses = $P8 (expect >=4 — class def + 4 uses = >=5)"; test "$P8" -ge 4 || (echo "FAIL P8"; exit 1);
      P9=$(grep -c 'W4 책임' "$F"); echo "P9 W4 책임 placeholder = $P9 (expect >=4)"; test "$P9" -ge 4 || (echo "FAIL P9"; exit 1);
      P10=$(grep -c 'W5 책임' "$F"); echo "P10 W5 책임 placeholder = $P10 (expect >=4)"; test "$P10" -ge 4 || (echo "FAIL P10"; exit 1);
      P11=$(grep -c '선택된 보고서 없음' "$F"); echo "P11 empty state text = $P11 (expect 0)"; test "$P11" -eq 0 || (echo "FAIL P11 (W5 OQ #3 LOCKED)"; exit 1);
      # No src modifications
      SRC_CHANGES=$(git diff --name-only HEAD 2>/dev/null | grep -cE '^cha-bio-safety/src/' || true); echo "SRC changes = $SRC_CHANGES (expect 0)"; test "$SRC_CHANGES" -eq 0 || (echo "FAIL src modified"; exit 1);
      echo "ALL VERIFY PASS"
    </automated>
  </verify>
  <done>
    `sketch-wave-6-desktop-preview-wrapper.html` 1개 파일 작성 완료. 다크/라이트 × div-early/자탐 = 4 frame matrix, 각 frame 안에 W4 placeholder toolbar + W5 placeholder sidelist (260px) + W6 본격 .preview-wrapper + .a4-preview (흰배경 + aspect-ratio 210:297) 가 시각화된다. 자체 verify (negative 6 + positive 11) 모두 PASS. src/ 어디도 수정되지 않음.
  </done>
</task>

</tasks>

<verification>
1. 단일 파일 산출: `cha-bio-safety/docs/redesign-context/14-reports/sketch-wave-6-desktop-preview-wrapper.html` 존재.
2. §6 verify gate ≥15 모두 PASS (위 Task 1 verify 블록 17건).
3. ReportsPage.tsx / ExcelPreview.tsx / App.tsx / 기타 src 0 변경: `git diff --name-only HEAD -- cha-bio-safety/src` 결과 0 라인.
4. atomic 1-commit: `git log --oneline -1` 1줄 = 본 sketch wave 6 커밋.
5. 커밋 메시지: `docs(14-reports): sketch wave 6 — 우측 ExcelPreview 래퍼 영역 (A4 흰배경 고정, 다크/라이트 1280px)`
6. wrangler / `npm run deploy` 명령 0건 실행 (디자인 워크트리 룰 / CLAUDE.local.md).
</verification>

<success_criteria>
- sketch-wave-6-desktop-preview-wrapper.html 1개 파일 작성
- 4 frame matrix (다크 div-early / 다크 자탐 / 라이트 div-early / 라이트 자탐) 모두 1280px 렌더링
- 각 frame 의 .a4-preview 가 흰배경 (#ffffff) + aspect-ratio 210/297 + max-width 595px
- W4 placeholder toolbar + W5 placeholder sidelist 가 frame 안에 명시되어 책임 분리 가시화
- W7 진입 전 OQ 2건 default 답 (CSS layer = @layer components / sub-wave 분할 = 3 sub-wave) 명시
- §6 verify gate (negative 6 + positive 11) 모두 PASS
- src/ 코드 0 변경 + wrangler/npm run deploy 0건
- atomic 1-commit 으로 머지
</success_criteria>

<output>
완료 후 `.planning/quick/260520-i7u-redesign-14-reports-sketch-wave-6-excelp/260520-i7u-SUMMARY.md` 작성:
- Frame matrix 4건 정리 (theme / selected / A4 title / A4 meta)
- .preview-wrapper + .a4-preview CSS 정의 verbatim 인용
- §6 verify gate 결과 (negative 6 + positive 11) PASS 여부
- W7 진입 전 OQ 2건 default 답 명시 (CSS layer 위치 / sub-wave 분할)
- W5 → W6 LOCKED OQ 3건 적용 결과 정리
- 메모리 룰 박제 결과 (10건 unique 인용 여부)
</output>
