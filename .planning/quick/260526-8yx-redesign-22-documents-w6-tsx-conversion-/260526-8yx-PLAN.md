---
phase: quick-260526-8yx
plan: 01
type: execute
wave: 1
depends_on: []
quick_id: 260526-8yx
branch: redesign/22-documents (HEAD 7f03993 — W2~W5 SUMMARY 직후, 85a 4 atomic commit 직후)
date: 2026-05-26
autonomous: true
files_modified:
  - cha-bio-safety/docs/redesign-context/22-documents/wave-6-tsx-conversion-checklist.md
requirements:
  - redesign-22-documents-w6-tsx-conversion-checklist
must_haves:
  truths:
    - "wave-6-tsx-conversion-checklist.md 1개가 22-documents/ 직속 평면 배치로 생성된다 (sketch/ 서브폴더 절대 X — 18-worklog slq + 19-legal 6if + 20-legal-findings bbz + 21-legal-finding-detail lft + 23-education 4i9 + 28-splash 0803d2f 평면 패턴 mirror)"
    - "src 3 파일 (DocumentsPage.tsx 162 + DocumentSection.tsx 517 + DocumentUploadForm.tsx 402 = 1081 lines) + 외부 의존 6 파일 (utils/api documentsApi + utils/multipartUpload runMultipartUpload/formatBytes/formatEta + utils/downloadBlob downloadDocument + hooks/useIsDesktop + stores/authStore + components.css + App.tsx) 모두 코드 변경 0 byte — 본 wave 는 markdown 1개만 산출, TSX 변환 실행은 다음 wave"
    - "12 섹션 (§1~§12) 모두 존재 — slq mirror 패턴 (§1 imports / §2 메인 함수 / §3 JSX render / §4 비즈 anchor / §5 OQ LOCKED / §6 sketch fence verbatim / §7 폰트 격상 / §8 Lucide / §9 components.css / §10 Tailwind cheatsheet / §11 negative / §12 verify gate)"
    - "비즈 anchor 9 그룹 (documentsApi list+upload+remove 2종 / useQuery + invalidateQueries / ALLOWED 6 ext / empty MIME fallback / multipart upload runMultipartUpload / abort/retry / beforeunload guard / admin 권한 isAdmin / ★ delete confirm gate full string verbatim) 모두 §4 안 1 byte 변경 0 + grep anchor 박제"
    - "OQ #1~#5 LOCKED 5건 default 모두 §5 안 verbatim 인용 — #1 chrome 강조색 토큰 통일 (bg-accent / border-accent) / #2 submit button 그라데이션 (linear-gradient 135deg #1d4ed8 #0ea5e9) / #3 최신 pill 12 격상 leading-none / #4 모달 title 22 격상 + Error 카피 14 격상 + 빈 FileText 48 유지 / #5 var(--bg4) → bg-surface-sunken 통일"
    - "negative rule 17건 + verify gate 22건 (slq mirror — 18-worklog W7 패턴 일관). 메모리 룰 unique slug ≥10. components.css inherit (재사용 ≥3 + 신규 ≥15 — 3 파일 분)"
    - "★ delete confirm 카피 verbatim 박제 (memory project_legal_findings_delete_incident_260520 1 byte 변경 0) + beforeunload + abort confirm + 403 카피 + toast 12종 + typeLabel + 빈/오류 카피 + ALLOWED 6 ext 보조 모두 verbatim"
  artifacts:
    - path: "cha-bio-safety/docs/redesign-context/22-documents/wave-6-tsx-conversion-checklist.md"
      provides: "W6 TSX 변환 verify checklist markdown — 3 파일 (DocumentsPage 162 + DocumentSection 517 + DocumentUploadForm 402 = 1081 lines) 변환 시 executor 가 단일 진입점으로 참조. 12 섹션 (§1 imports 매핑 3 파일 영역별 / §2 메인 함수 시그니처 보존 / §3 JSX render 영역 매핑 / §4 비즈 anchor 9 그룹 verbatim 박스 / §5 OQ LOCKED 5건 verbatim / §6 sketch grep fence 박제 ≥8 / §7 폰트 격상 9·10·11 → 12/14/16/18 line 별 매핑 / §8 Lucide ≥5종 size prop 매핑 / §9 components.css inherit 재사용 ≥3 + 신규 ≥15 / §10 Tailwind cheatsheet / §11 negative 17건 / §12 verify gate 22건). slq mirror 12 섹션 구조 일관."
      contains: "12 H1 헤더 `# §1`~`# §12` / 3 파일 imports 매핑 표 (DocumentsPage / DocumentSection / DocumentUploadForm) / useState + useEffect + useRef + useQuery + useQueryClient + useAuthStore + useIsDesktop hook 보존 / documentsApi.list + documentsApi.remove (++ upload via runMultipartUpload) 비즈 시그니처 anchor / queryKey ['documents', type] + staleTime 60_000 + invalidateQueries 박제 / ALLOWED 6 ext (.pdf/.xlsx/.docx/.pptx/.hwp/.zip) + EXT_TO_MIME 매핑 + MAX_SIZE 500MB 박제 / empty MIME fallback `f.type && !mimes.includes(f.type)` verbatim / runMultipartUpload({ file, type, year, title, contentType, signal, onProgress }) 시그니처 verbatim / AbortController abortRef + abort/retry 패턴 / beforeunload returnValue '업로드 중입니다. 페이지를 나가면 전송이 중단됩니다.' verbatim / abort confirm '업로드를 취소하시겠습니까? 지금까지 전송된 데이터는 저장되지 않습니다.' verbatim / ★ delete confirm full string `\"${item.title}\\n(${item.filename})\\n\\n정말 삭제하시겠습니까? 이 작업은 되돌릴 수 없습니다.\"` verbatim (memory project_legal_findings_delete_incident_260520 1 byte 변경 0) / admin 권한 anchor `useAuthStore((s) => s.staff?.role === 'admin')` verbatim / 403 → '관리자만 업로드할 수 있습니다.' verbatim / OQ #1~#5 LOCKED 5건 verbatim 인용 / W2~W5 sketch grep fence ≥8 (executor 가 `grep -hoE 'class=\"[^\"]+\"' sketch-wave-*.html | sort -u` 실행 후 verbatim 박제) / 폰트 격상 매핑 (최신 pill 11 → text-caption 12 leading-none / 파일 보조 12 leading-none / Error 카피 13 → text-body-sm 14 / 모달 title 20 → text-heading 22 / Hero meta 14 / 과거 row 14 / h2 16 / Progress % 16/600 / Year tile 28/600 JetBrains Mono) / Lucide ≥5종 (FileText 48 빈 상태 / Plus 16 데스크톱 또는 20 모바일 uploadBtn / Loader2 16 spin / Trash2 16 admin / Upload + X + AlertTriangle 옵션 — 7종 매핑) / components.css inherit (.btn-primary + .btn-secondary + .empty-state 재사용 3 + 신규 ≥15: .docs-tab-bar / .docs-tab-btn / .docs-tab-btn-active / .docs-section-header / .docs-hero-card / .docs-year-tile / .docs-latest-pill / .docs-history-row / .docs-trash-btn / .docs-upload-btn-mobile / .docs-upload-sheet / .docs-upload-sheet-body / .docs-upload-modal / .docs-upload-modal-body / .docs-sheet-handle / .docs-input / .docs-file-btn / .docs-progress-bar / .docs-progress-fill / .docs-error-block) / Tailwind cheatsheet (w-7=32 / w-11=44 / w-10=40 / w-[32px]+w-[40px] arbitrary 활용 / w-8=48 함정 회피 / text-caption=12 + leading-none / text-body-sm=14 / text-body=16 / text-heading=22 / text-display=28) / negative 17건 (src/** 변경 0 / sketch HTML 추가 0 / wrangler 0 / npm run deploy 0 / 이모지 0 / lin-grad T4 만 anchor / status- prefix 0 / w-8 h-8 0 / 9·10·11px 0 / 옛 alias 토큰 0 / sketch/ 서브폴더 0 / app.tsx 변경 0 / utils/api 변경 0 / utils/multipartUpload 변경 0 / utils/downloadBlob 변경 0 / hooks/useIsDesktop 변경 0 / stores/authStore 변경 0) / verify gate 22건 (12 헤더 grep / 비즈 anchor ≥9 / OQ LOCKED 5 / sketch grep fence ≥8 / 메모리 룰 unique slug ≥10 / negative ≥15 / verify ≥18 / 3 파일 line 수 실측 / components.css 재사용 ≥3 + 신규 ≥15 / Tailwind cheatsheet 12 마지노선 / Lucide ≥5종 / @keyframes 3종 docs-slide-up + docs-fade-in + docsec-spin / typeLabel verbatim / delete confirm verbatim / beforeunload verbatim / abort confirm verbatim / 403 카피 verbatim / admin 권한 anchor verbatim / OQ #2 lin-grad anchor ≥3 / OQ #5 bg-surface-sunken 통일 / Hero card minHeight 96 / 과거 row minHeight 56 — 22 gate)"
      min_lines: 380
  key_links:
    - from: "85a SUMMARY 직후 W2~W5 sketch HTML 4개 (sketch-wave-2-chrome.html + sketch-wave-3-document-section.html + sketch-wave-4-upload-form-inline.html + sketch-wave-5-modal-confirm.html)"
      to: "wave-6-tsx-conversion-checklist.md §6 sketch grep fence verbatim"
      via: "executor 가 PLAN.md 안 명시된 grep 명령 `grep -hoE 'class=\"[^\"]+\"' cha-bio-safety/docs/redesign-context/22-documents/sketch-wave-*.html | sort -u` 실행 → 결과 verbatim 박제 (≥8 unique class). planner 가 추측 X — executor 실시간 grep 결과 박제 (slq mirror 패턴)."
      pattern: "grep -hoE 'class=\"\\[\\^\"\\]\\+\"'"
    - from: "wave-1-index.md §7 OQ #1~#5 LOCKED 5건 default"
      to: "wave-6-tsx-conversion-checklist.md §5"
      via: "OQ #1 chrome 강조색 토큰 통일 (bg-accent / border-accent) / OQ #2 submit button 그라데이션 (linear-gradient 135deg #1d4ed8 #0ea5e9) / OQ #3 최신 pill 12 격상 + leading-none / OQ #4 모달 title 22 격상 + Error 14 격상 + 빈 FileText 48 유지 / OQ #5 var(--bg4) → bg-surface-sunken 통일 — 5건 verbatim 인용."
      pattern: "OQ #[1-5]"
    - from: "DocumentSection.tsx handleDelete window.confirm 카피 (line 87~89 verbatim)"
      to: "wave-6-tsx-conversion-checklist.md §4 비즈 anchor 박스 + §12 verify gate"
      via: "★ delete confirm full string `\"${item.title}\\n(${item.filename})\\n\\n정말 삭제하시겠습니까? 이 작업은 되돌릴 수 없습니다.\"` 1 byte 변경 0 (memory project_legal_findings_delete_incident_260520 2026-05-20 22행 삭제 사고 후 가드 강화 룰 일반화). verify gate `grep -c '정말 삭제하시겠습니까' wave-6-tsx-conversion-checklist.md` ≥1."
      pattern: "정말 삭제하시겠습니까|되돌릴 수 없습니다"
    - from: "DocumentUploadForm.tsx beforeunload + handleCancel confirm 카피 (line 67~68 + 157~159 verbatim)"
      to: "wave-6-tsx-conversion-checklist.md §4 비즈 anchor 박스 + §12 verify gate"
      via: "beforeunload returnValue '업로드 중입니다. 페이지를 나가면 전송이 중단됩니다.' + abort confirm '업로드를 취소하시겠습니까? 지금까지 전송된 데이터는 저장되지 않습니다.' 모두 1 byte 변경 0. verify gate `grep -c '업로드 중입니다' wave-6-tsx-conversion-checklist.md` ≥1 + `grep -c '업로드를 취소하시겠습니까' wave-6-tsx-conversion-checklist.md` ≥1."
      pattern: "업로드 중입니다|업로드를 취소하시겠습니까"
    - from: "메모리 룰 12 unique slug (feedback_* 10 + project_* 2)"
      to: "wave-6-tsx-conversion-checklist.md §11 negative + §12 verify gate"
      via: "feedback_tailwind_token_class_pattern (status- prefix 0 + Lucide size prop) / feedback_tailwind_w8_h8_is_48px (w-7 = 32, w-8 = 48 함정) / feedback_text_caption_leading_none (12 마지노선 leading-none) / feedback_design_changes_ask_first / feedback_design_sketch_first / feedback_avoid_premature_confirmation / feedback_sketch_realistic_data / feedback_redesign_sketch_rule_enforcement / feedback_check_branch_before_edit / feedback_cbc7119_design_never_wrangler / project_redesign_19_legal (multi-file 외부 의존 미수정 패턴) / project_legal_findings_delete_incident_260520 (delete confirm 가드). verify gate `grep -oE 'feedback_[a-z_]+' wave-6-tsx-conversion-checklist.md | sort -u | wc -l` ≥10."
      pattern: "feedback_[a-z_]+|project_[a-z0-9_]+"
  scope_negatives:
    - "markdown 1개 만 산출 (.planning 폴더 + cha-bio-safety/docs/redesign-context/22-documents/ 안 wave-6-tsx-conversion-checklist.md). TSX 변환 실행은 다음 wave (본 PLAN 범위 외 — slq 와 동일하게 checklist 만 생성, TSX 변환은 W7 또는 별도 quick)"
    - "src 3 파일 (DocumentsPage.tsx + DocumentSection.tsx + DocumentUploadForm.tsx) + utils/api + utils/multipartUpload + utils/downloadBlob + hooks/useIsDesktop + stores/authStore + components.css + App.tsx 모두 0 byte 변경"
    - "sketch HTML 추가/수정 0 (W2~W5 4 파일 완료 직후 본 wave 는 markdown 만 생성)"
    - "wrangler 명령 절대 X / npm run deploy 절대 X (CLAUDE.local.md + memory `feedback_cbc7119_design_never_wrangler`)"
    - "옛 alias 토큰 (var(--bg/bg2/bg3/bg4/bd/bd2/t1/t2/t3/acl/accent/safe/warn/danger)) checklist 본문 안 0 (sketch fence 인용 안 예외)"
    - "status- prefix 0 (bg-status-safe / text-status-danger 등 사용 시 verify FAIL, memory feedback_tailwind_token_class_pattern)"
    - "이모지 0 (checklist 본문) — Lucide 아이콘 anchor 만 텍스트 인용 (FileText / Plus / Loader2 / Trash2 / Upload / X / AlertTriangle)"
    - "w-8 / h-8 className 0 (48px 함정, memory feedback_tailwind_w8_h8_is_48px). 32x32 = w-[32px] h-[32px] arbitrary 또는 w-7 h-7. 40x40 = w-[40px] h-[40px]. 44 = w-11 h-11"
    - "9·10·11 fontSize 인라인 0 (text-caption 12 leading-none 마지노선, OQ #3 LOCKED)"
    - "linear-gradient — 본문 약어 'lin-grad' 사용 / §4 비즈 anchor 박스 안 §5 OQ #2 LOCKED 인용 1회 만 anchor (T4 sketch fence 안 verbatim 등장 OK)"
    - "본 워크트리 = cbc7119-design (cbc7119-preview.pages.dev 만). 직원 도메인 cbc7119.pages.dev 절대 손대지 않음 (20260328 워크트리 책임)"
    - "sketch/ 서브폴더 절대 금지 — checklist 파일도 22-documents/ 직속 평면 배치 (18/19/20/21/23/28 6 페이지 일관)"
---

<objective>
redesign/22-documents 의 W2~W5 sketch 4 파일 완료 + 85a SUMMARY 박제 직후 (HEAD 7f03993) 상황에서,
**W6 TSX 변환 verify checklist markdown 1개** 만 산출한다 — slq (18-worklog W7) mirror.

- **T1:** `wave-6-tsx-conversion-checklist.md` 1 파일 생성 (12 섹션 §1~§12, ≥380 lines).
  - §1 imports 매핑 (3 파일 영역별) / §2 메인 함수 시그니처 / §3 JSX render 영역 매핑
  - §4 비즈 anchor 9 그룹 verbatim 박스 / §5 OQ LOCKED 5건 verbatim 인용
  - §6 sketch grep fence verbatim 박제 (executor 가 `grep -hoE 'class="[^"]+"' sketch-wave-*.html | sort -u` 실행 → 결과 verbatim 박제 ≥8 unique class)
  - §7 폰트 격상 9·10·11 → 12/14/16/18 line 별 매핑 (3 파일 line 별)
  - §8 Lucide ≥5종 size prop 매핑 (FileText / Plus / Loader2 / Trash2 + Upload / X / AlertTriangle 옵션)
  - §9 components.css inherit (재사용 ≥3: .btn-primary / .btn-secondary / .empty-state + 신규 ≥15)
  - §10 Tailwind cheatsheet (12 마지노선 + w-7/w-10/w-11 + arbitrary 활용)
  - §11 negative 17건 + §12 verify gate 22건

**Purpose:** 18-worklog slq / 19-legal 6if / 20-legal-findings bbz / 21-legal-finding-detail lft / 23-education 4i9 / 28-splash 0803d2f 등 다수 mirror 와 동일한 checklist 단독 wave 패턴. 22-documents 3 파일 통합 (1081 lines) TSX 변환 시 executor 가 단일 진입점 markdown 참조. 본 PLAN 은 checklist 생성만, TSX 변환 실행은 다음 wave.

**Output:** `cha-bio-safety/docs/redesign-context/22-documents/wave-6-tsx-conversion-checklist.md` 1 파일. `DocumentsPage.tsx` (162) + `DocumentSection.tsx` (517) + `DocumentUploadForm.tsx` (402) 합계 1081 lines + 외부 의존 6 파일 모두 코드 변경 0 byte.

**slq mirror:** 본 PLAN 은 18-worklog slq PLAN 의 정확한 mirror. mirror 와 차이 1건:
- **multi-file scope** — 18-worklog 단일 1216 lines (단일 파일 변환) → 22-documents 3 파일 1081 lines (multi-file 변환). §1 imports 매핑 / §2 메인 함수 / §3 JSX render 모두 3 파일 영역별 박제 필요. components.css 신규 클래스 ≥10 → ≥15 격상 (3 파일 분).

**중요 — executor cherry-pick 사고 회피 (memory project_redesign_28_splash 6회 사례):** executor worktree commit 후 명시적 보고 필수 (commit hash + message 1줄) + orchestrator 가 필요 시 자동 cherry-pick 으로 복구. 본 PLAN T1 task 의 commit 명령은 명시 박제.

**mbr deviation 2건 mirror (negative gate self-collision 회피):** "warning glyph" / "lin-grad" 약어 패턴 사용 — 본문 안 `linear-gradient` 단어 자체는 §5 OQ #2 LOCKED 인용 + §4 anchor 박스 안에만 등장. 그 외 본문은 약어 또는 fence escape 사용. (mbr/slq verify pattern 일관)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jykevin/Documents/cbc7119-design/CLAUDE.md
@/Users/jykevin/Documents/cbc7119-design/CLAUDE.local.md
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/22-documents/wave-1-index.md
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/22-documents/DocumentsPage.tsx
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/22-documents/DocumentSection.tsx
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/22-documents/DocumentUploadForm.tsx
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/22-documents/sketch-wave-2-chrome.html
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/22-documents/sketch-wave-3-document-section.html
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/22-documents/sketch-wave-4-upload-form-inline.html
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/22-documents/sketch-wave-5-modal-confirm.html
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/22-documents/tokens.css
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/22-documents/typography.css
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/22-documents/design-system.md
</context>

<tasks>

<task type="auto">
  <name>T1: wave-6-tsx-conversion-checklist.md 생성 (12 섹션 §1~§12 multi-file 3 파일 박제)</name>
  <files>cha-bio-safety/docs/redesign-context/22-documents/wave-6-tsx-conversion-checklist.md</files>
  <action>
22-documents/ 직속 평면 배치로 `wave-6-tsx-conversion-checklist.md` markdown 1개 생성. slq (18-worklog W7 / 6if / bbz / lft / 4i9 / 0803d2f) mirror 12 섹션 구조 일관. multi-file scope (3 파일: DocumentsPage 162 + DocumentSection 517 + DocumentUploadForm 402 = 1081 lines).

**필독 순서 (executor):**
1. wave-1-index.md (564 lines) — §1.3 비즈 anchor 박스 + §4 components.css inherit + §5 메모리 룰 12 + §6 negative 16 + §7 OQ 5건 LOCKED + §8 verify gate 8 (단일 진입점)
2. DocumentsPage.tsx (162) + DocumentSection.tsx (517) + DocumentUploadForm.tsx (402) — 비즈 시그니처 + 카피 verbatim + state 패턴 + @keyframes 3종 docs-slide-up + docs-fade-in + docsec-spin
3. sketch-wave-2-chrome.html + sketch-wave-3-document-section.html + sketch-wave-4-upload-form-inline.html + sketch-wave-5-modal-confirm.html (85a 산출 4 파일, 7f03993 직전 commit) — class 패턴 grep 추출 필수
4. design-system.md (v0.1.1) — §1.1 노안 마지노선 12 / §1.3 모바일/데스크톱 같은 폰트 / §6.4 그라데이션 / §7.1 Lucide size prop
5. tokens.css + typography.css — 토큰 매핑 검증

**산출 markdown 구조 (12 섹션 §1~§12, ≥380 lines):**

```
---
title: "redesign/22-documents — W6 TSX 변환 verify checklist"
status: ready_for_w7
created: 2026-05-26
quick_id: 260526-8yx
branch: redesign/22-documents
source_files:
  - cha-bio-safety/src/pages/DocumentsPage.tsx (162 lines, 변환 대상)
  - cha-bio-safety/src/components/DocumentSection.tsx (517 lines, 변환 대상)
  - cha-bio-safety/src/components/DocumentUploadForm.tsx (402 lines, 변환 대상)
  - 합계 1081 lines / 3 파일 multi-file scope
mirror_of:
  - cha-bio-safety/docs/redesign-context/18-worklog/wave-7-tsx-conversion-checklist.md (slq, 단일 1216)
  - 19-legal / 20-legal-findings / 21-legal-finding-detail / 23-education / 28-splash 6 페이지 checklist 패턴
oq_locked: 5 (#1~#5, wave-1-index.md §7)
biz_anchor_groups: 9
sketch_grep_classes: ≥8
memory_rules: ≥10 unique slug
components_css_reuse: ≥3 / new: ≥15
negative_count: 17
verify_gate_count: 22
---

본 문서는 redesign/22-documents 의 **W7 TSX 변환 wave 단일 진입점** 이다. 18-worklog slq W7 / 6if / bbz / lft / 4i9 / 0803d2f 6 mirror 의 정확한 박제. 12 섹션:

────────────────────────────────────────

# §1. imports 매핑 (3 파일 영역별)

[DocumentsPage.tsx 변환 후 import 블록 (verbatim, line 10~13 기반)]
- import { useState } from 'react'
- import { useIsDesktop } from '../hooks/useIsDesktop'
- import DocumentSection from '../components/DocumentSection'
- import DocumentUploadForm from '../components/DocumentUploadForm'
+ (Lucide 미사용 — 본 파일은 chrome 만)

[DocumentSection.tsx 변환 후 import 블록 (verbatim, line 7~15 기반)]
- import { useEffect, useState } from 'react'
- import { useQuery, useQueryClient } from '@tanstack/react-query'
- import toast from 'react-hot-toast'
- import { FileText, Plus, Loader2, Trash2 } from 'lucide-react'
- import { documentsApi, type DocumentListItem } from '../utils/api'
- import { downloadDocument } from '../utils/downloadBlob'
- import { formatBytes } from '../utils/multipartUpload'
- import { useAuthStore } from '../stores/authStore'
- import { useIsDesktop } from '../hooks/useIsDesktop'

[DocumentUploadForm.tsx 변환 후 import 블록 (verbatim, line 7~16 기반)]
- import { useEffect, useRef, useState } from 'react'
- import { useQueryClient } from '@tanstack/react-query'
- import toast from 'react-hot-toast'
- import { runMultipartUpload, formatBytes, formatEta, type ProgressState } from '../utils/multipartUpload'
- import { ApiError } from '../utils/api'

verify: 3 import 블록 모두 1 byte 변경 0 (변환 후 alias rename / 경로 수정 0).

────────────────────────────────────────

# §2. 메인 함수 시그니처 보존

[DocumentsPage]
- `export default function DocumentsPage()` — props 없음, useState<DocType> activeTab + useState<DocType|null> uploadFor + tabBtnStyle 헬퍼 + isDesktop 분기

[DocumentSection]
- `export default function DocumentSection({ type, onUploadClick }: Props)` — Props 인터페이스 `type: 'plan' | 'drill'; onUploadClick: () => void` 보존
- `formatDate(iso, mode 'full'|'date-only')` 헬퍼 보존
- `typeLabel(t: 'plan' | 'drill')` 헬퍼 verbatim '소방계획서/소방훈련자료'

[DocumentUploadForm]
- `export default function DocumentUploadForm({ type, onClose }: Props)` — Props 인터페이스 `type: 'plan' | 'drill'; onClose: () => void` 보존
- `ALLOWED` 6 ext + `EXT_TO_MIME` + `MAX_SIZE` 500MB + `typeLabel` + `findAllowed` 헬퍼 모두 verbatim
- 7 state (year + title + file + progress + isUploading + error + abortRef + fileInputRef) verbatim
- 2 useEffect (beforeunload + unmount abort) verbatim
- 4 handler (handleFileChange + handleSubmit + handleCancel + handleRetry) verbatim

verify: 3 메인 함수 모두 시그니처 + state 인터페이스 1 byte 변경 0.

────────────────────────────────────────

# §3. JSX render 영역 매핑 (3 파일 영역별)

[DocumentsPage render (line 34~162 기반)]
- 모바일 분기 (line 41~64) — div sticky top-0 z-10 flex bg-surface-page border-b border-border-default mb-4 → `.docs-tab-bar` + activeTab 별 tabBtnStyle 함수 → `.docs-tab-btn` / `.docs-tab-btn-active` (border-accent — OQ #1)
- 데스크톱 분기 (line 66~83) — 좌우 2단 maxWidth 1200 gap 48 padding 24 → flex gap-12 max-w-[1200px] mx-auto p-6 (또는 arbitrary 1200/48/24)
- upload shell 모바일 (line 86~126) — BottomSheet + handle 40x4 + slideUp 240ms + backdrop NO-OP 코멘트 verbatim 유지
- upload shell 데스크톱 (line 128~159) — Modal + fade-in 180ms + min(480px,92vw) + backdrop NO-OP 코멘트 verbatim 유지
- @keyframes docs-slide-up + docs-fade-in 인라인 정의 (line 36~39) 보존

[DocumentSection render (line 165~516 기반)]
- 헤더 행 (line 170~180) — flex justify-between items-center min-h-10 + h2 16/600 + uploadBtn 분기 (admin 40x40 모바일 / 40 pill 데스크톱)
- 4 state 매트릭스:
  - loading (line 183~196) — 96+56+56 스켈레톤 3행 bg-surface-raised
  - error (line 199~233) — '문서 목록을 불러오지 못했습니다.' + 다시 시도 button bg-accent
  - empty (line 236~260) — FileText size={48} + admin/non-admin 카피 분기 (typeLabel 사용)
  - data (line 263~514) — Hero card (latest) + 과거 이력 list (history)
- Hero card (line 263~394) — 최신 pill bg-accent text-caption font-bold leading-none + Year tile 64x64 bg-surface-sunken JetBrains Mono 28/600 + Meta verbatim 포맷 + Loader2 spin + admin Trash2 32x32
- 과거 이력 list (line 397~514) — 연결된 borderRadius isFirst/isLast + Year+Title 14/600 + meta 14/400 + admin Trash2 32x32
- @keyframes docsec-spin (line 167) 보존

[DocumentUploadForm render (line 201~401 기반)]
- 모달 title (line 204) — '${label} 업로드' text-heading 22/600 (OQ #4 격상)
- year select (line 207~221) — yearOptions descending currentYear+1 → 2020
- title input (line 223~234) — placeholder verbatim '예: ${currentYear}년 ${label}'
- file dashed button (line 236~272) — bg-surface-sunken border-dashed border-border-default + 보조 'PDF, XLSX, DOCX, PPTX, HWP, ZIP · 최대 500MB' text-caption leading-none 12 (OQ #3)
- Progress block (line 274~322) — height-2 bg-surface-sunken + fill bg-accent transition-[width] duration-[240ms] ease-linear + meta (% 16/600 + speedBps + ETA 14/400)
- Error block (line 324~360) — bg-surface-sunken text-danger text-body-sm 14 font-medium (OQ #4 14 격상) + 다시 시도 button bg-accent
- Action row (line 362~399) — submit (lin-grad OQ #2 LOCKED) + cancel (border-border-default)

────────────────────────────────────────

# §4. 비즈 anchor 박스 (9 그룹, 1 byte 변경 0)

```
[GROUP 1 — documentsApi 2종 (utils/api 외부 의존 미수정)]
- documentsApi.list(type: 'plan' | 'drill') → Promise<DocumentListItem[]> (시그니처 변경 금지)
- documentsApi.remove(id: number) → Promise<void> (시그니처 변경 금지)
- documentsApi.upload — runMultipartUpload 내부 호출 (직접 호출 0)
- DocumentListItem type (id / type / year / title / filename / size / uploaded_by_name / uploaded_at) 보존

[GROUP 2 — useQuery + invalidateQueries (react-query 시그니처)]
- useQuery({ queryKey: ['documents', type], queryFn: () => documentsApi.list(type), staleTime: 60_000 })
- queryClient.invalidateQueries({ queryKey: ['documents', type] }) (DocumentSection handleDelete + DocumentUploadForm handleSubmit 2곳)
- query key 1 byte 변경 0 (변경 시 cache mismatch → silent stale)

[GROUP 3 — ALLOWED 6 ext + EXT_TO_MIME + MAX_SIZE 500MB]
- ALLOWED 6 ext: .pdf .xlsx .docx .pptx .hwp .zip (verbatim, line 23~30)
- EXT_TO_MIME 매핑 (verbatim, line 32~39) — fallback contentType
- MAX_SIZE = 500 * 1024 * 1024 (500MB) (verbatim, line 41)

[GROUP 4 — empty MIME fallback (HWP/ZIP iOS Safari)]
- 조건 `if (f.type && !(entry.mimes as readonly string[]).includes(f.type))` verbatim (line 96)
- HWP/ZIP empty MIME 통과 허용 (iOS Safari file.type 비어있는 케이스 대응)

[GROUP 5 — multipart upload runMultipartUpload]
- runMultipartUpload({ file, type, year, title, contentType, signal, onProgress }) (verbatim, line 122~130)
- contentType fallback: file.type || (entry ? EXT_TO_MIME[entry.ext] : 'application/octet-stream') (verbatim, line 112)
- onProgress: (p: ProgressState) => setProgress(p) — ProgressState 타입 보존

[GROUP 6 — abort/retry]
- AbortController abortRef = useRef<AbortController | null>(null) (line 59)
- ctrl = new AbortController() + abortRef.current = ctrl + try { runMultipartUpload({ ..., signal: ctrl.signal, ... }) } finally { abortRef.current = null } (line 118~152)
- AbortError 분기: err.name === 'AbortError' → message '업로드가 취소되었습니다.' + isAbort=true → toast 미발송 (line 137~138)
- handleCancel: isUploading 시 window.confirm → ok → abortRef.current?.abort() (line 155~166)
- handleRetry: setError(null) + void handleSubmit() (line 168~171)
- unmount cleanup useEffect: return () => abortRef.current?.abort() (line 75~79)

[GROUP 7 — beforeunload guard]
- useEffect (line 64~72) — isUploading 동안만 listener 등록
- handler returnValue verbatim '업로드 중입니다. 페이지를 나가면 전송이 중단됩니다.' (1 byte 변경 0)

[GROUP 8 — admin 권한 isAdmin]
- isAdmin = useAuthStore((s) => s.staff?.role === 'admin') (verbatim, DocumentSection line 40)
- uploadBtn 분기 — isAdmin false 시 null 반환 (line 118 + 163)
- Trash2 button 분기 — isAdmin true 만 (Hero line 364 + 과거 line 482)
- 403 → '관리자만 업로드할 수 있습니다.' (DocumentUploadForm line 141, 1 byte 변경 0)

[GROUP 9 — ★ delete confirm gate full string (memory project_legal_findings_delete_incident_260520)]
- DocumentSection handleDelete (line 87~89) window.confirm full string verbatim:
  `"${item.title}"\n(${item.filename})\n\n정말 삭제하시겠습니까? 이 작업은 되돌릴 수 없습니다.`
- 1 byte 변경 0 (2026-05-20 22행 삭제 사고 후 가드 강화 룰 일반화)
- 위치 보존 (Hero 카드 우하단 absolute + 과거 row 우측 행 안)
- 추가 가드: deletingIds Set state per-item 잠금 / Loader2 spin during delete / aria-disabled
```

verify: 9 그룹 모두 anchor verbatim 박제. grep gate 9 (그룹 별 ≥1 anchor).

────────────────────────────────────────

# §5. OQ LOCKED 5건 verbatim 인용 (wave-1-index.md §7)

## §5.1 OQ #1 — chrome 강조색 토큰 통일 (LOCKED default)

> 모바일 탭 active borderBottom `2px solid #2f81f7` + 최신 pill bg `#2f81f7` + uploadBtn 데스크톱 pill bg `#2f81f7` + submit button bg `#2f81f7` + progress fill bg `#2f81f7` + Error 다시 시도 button bg `#2f81f7` + DocumentSection error 다시 시도 button bg `#2f81f7` — 모두 동일 hex `#2f81f7` 인데 → `bg-accent` / `border-accent` 토큰 통일.
> default 답: 토큰 통일 (`bg-accent` / `border-accent`). 16-workshift / 17-annual-plan / 23-education / 28-splash 4 페이지 일관 패턴.

22-documents 적용: 7곳 모두 `bg-accent` / `border-accent` 치환. fallback (tokens.css mismatch 시): `bg-[#2f81f7]` arbitrary (16-workshift 사례).

## §5.2 OQ #2 — submit button 그라데이션 (LOCKED default)

> submit button 현재 solid `#2f81f7` (line 371) → design-system §6.4 그라데이션 `linear-gradient(135deg, #1d4ed8, #0ea5e9)` 통일.
> default 답: 그라데이션 OK (lin-grad 채택). 14-reports / 16-workshift / 17-annual-plan / 23-education W1 OQ #3 그라데이션 default 일관.

22-documents 적용: DocumentUploadForm line 371 submit button `background: 'linear-gradient(135deg, #1d4ed8, #0ea5e9)'` 인라인 또는 components.css `.btn-primary-gradient` 신규 클래스. canSubmit false 시 `bg-surface-sunken text-text-tertiary cursor-not-allowed` 분기 보존. T2/T3 sketch 본문 lin-grad 0건 — T4 + checklist §4/§5 만 anchor.

## §5.3 OQ #3 — 최신 pill 11 → 12 격상 (LOCKED default)

> 최신 pill 현재 `fontSize 11/600` (line 292~298, 노안 마지노선 12 위반) → 12/600 격상.
> default 답: 12 격상 OK. design-system §1.1 노안 마지노선 12 / 9·10·11px 사용 금지. memory `feedback_text_caption_leading_none` 일관.

22-documents 적용: 최신 pill `text-caption font-bold leading-none` (12px). 파일 보조 'PDF, XLSX, DOCX, PPTX, HWP, ZIP · 최대 500MB' (line 269) 도 `text-caption leading-none` 유지.

## §5.4 OQ #4 — 빈/오류/Progress 시각 일관성 (LOCKED default)

> 빈 상태 FileText 48 유지. 오류 상태는 카피만 (아이콘 추가 옵션). Progress 메타 14 → text-body-sm 유지. 모달 title 20 → text-heading 22 격상. Error 카피 13 → text-body-sm 14 격상.

22-documents 적용:
- 빈 FileText size={48} 유지 (DocumentSection line 250)
- 모달 title `${label} 업로드` text-heading 22/600 (DocumentUploadForm line 204, 20 → 22 격상)
- Error 카피 14 font-medium (DocumentUploadForm line 338, 13 → 14 격상 — 13px 사용 금지)
- Progress meta 16/600 % + 14/400 speedBps 유지

## §5.5 OQ #5 — var(--bg4) → bg-surface-sunken 통일 (LOCKED default)

> var(--bg4) 토큰 (DocumentUploadForm inputBaseStyle bg, line 184 + progress bar 배경 line 284) → bg-surface-sunken 통일.
> default 답: bg-surface-sunken 통일. 새 토큰 신설 안 함.

22-documents 적용: inputBaseStyle bg + progress bar bg + Year tile bg + dashed file button bg 모두 `bg-surface-sunken` 통일 (4곳). var(--bg3) + var(--bg4) 모두 동일 sunken 매핑.

verify: §5.1~§5.5 5건 헤더 grep `grep -cE '^## §5\\.[1-5]' wave-6-tsx-conversion-checklist.md` = 5.

────────────────────────────────────────

# §6. sketch grep fence verbatim 박제 (≥8 unique class)

executor 실행 명령:
```bash
grep -hoE 'class="[^"]+"' cha-bio-safety/docs/redesign-context/22-documents/sketch-wave-*.html | sort -u
```

위 명령 결과 verbatim 박제 (planner 가 추측 X — executor 실시간 grep 결과만):

```
[T1 — executor 실시간 결과 박제 fence (≥8 unique class)]
... (executor 가 직접 grep 후 본 fence 안 ≥8 unique class 패턴 박제)
... (예상 패턴 — 단 verbatim 박제는 executor 책임):
class="docs-tab-bar"
class="docs-tab-btn"
class="docs-tab-btn-active"
class="docs-section-header"
class="docs-hero-card"
class="docs-year-tile"
class="docs-latest-pill"
class="docs-history-row"
class="docs-trash-btn"
class="docs-upload-btn-mobile"
class="docs-upload-sheet"
class="docs-upload-modal"
class="docs-input"
class="docs-file-btn"
class="docs-progress-bar"
class="docs-error-block"
... (이외 sketch 안 실제 등장한 모든 class verbatim 박제)
```

verify: §6 fence ≥1 + 본문 안 grep 명령 verbatim 1회 + 결과 line ≥8.

────────────────────────────────────────

# §7. 폰트 격상 매핑 (9·10·11 → 12/14/16/18 line 별)

[DocumentsPage]
- tabBtnStyle fontSize: 16 (line 27) → text-body (마지노선) ✓
- handle bar / @keyframes — 폰트 없음

[DocumentSection]
- h2 헤더 (line 178): fontSize: 16, fontWeight: 600 → text-body font-semibold
- uploadBtn 데스크톱 pill (line 132): fontSize: 14, fontWeight: 600 → text-body-sm font-semibold
- error 카피 (line 212): fontSize: 16, fontWeight: 600 → text-body font-semibold
- error '다시 시도' button (line 223): fontSize: 14, fontWeight: 600 → text-body-sm font-semibold
- empty '아직 업로드된...' (line 251): fontSize: 16, fontWeight: 600 → text-body font-semibold
- empty admin/non-admin 카피 (line 254): fontSize: 14, fontWeight: 400 → text-body-sm font-normal
- 최신 pill (line 292): **fontSize: 11, fontWeight: 600 → text-caption font-bold leading-none (12 격상, OQ #3 LOCKED)** ★
- Year tile (line 319): fontSize: 28, fontWeight: 600 fontFamily 'JetBrains Mono' → font-mono text-display font-semibold
- Hero 이름 (line 332): fontSize: 16, fontWeight: 600 → text-body font-semibold
- Hero meta (line 344): fontSize: 14, fontWeight: 400 → text-body-sm font-normal
- 과거 이력 라벨 (line 401): fontSize: 16, fontWeight: 600 → text-body font-semibold
- 과거 row Year+Title (line 448): fontSize: 14, fontWeight: 600 → text-body-sm font-semibold
- 과거 row meta (line 460): fontSize: 14, fontWeight: 400 → text-body-sm font-normal

[DocumentUploadForm]
- 모달 title (line 204): **fontSize: 20, fontWeight: 600 → text-heading font-semibold (22 격상, OQ #4 LOCKED)** ★
- labelStyle (line 195): fontSize: 14, fontWeight: 600 → text-body-sm font-semibold
- inputBaseStyle (line 188): fontSize: 14 → text-body-sm
- file button (line 259): fontSize: 14 → text-body-sm
- 파일 보조 (line 269): **fontSize: 12, fontWeight: 400 → text-caption leading-none font-normal (OQ #3 LOCKED leading-none 강제)** ★
- Progress % (line 308): fontSize: 16, fontWeight: 600 fontFamily 'JetBrains Mono' → font-mono text-body font-semibold
- Progress meta (line 311): fontSize: 14, fontWeight: 400 → text-body-sm font-normal
- Error 카피 (line 338): **fontSize: 14, fontWeight: 500 → text-body-sm font-medium (13 → 14 격상, OQ #4 LOCKED)** ★
- Error '다시 시도' button (line 348): fontSize: 14, fontWeight: 600 → text-body-sm font-semibold
- submit button (line 374): fontSize: 15, fontWeight: 600 → text-body font-semibold (15 → 16 ↓ 0)
- cancel button (line 390): fontSize: 15, fontWeight: 600 → text-body font-semibold

verify: 9·10·11 fontSize 인라인 0 (최신 pill 11 → 12 격상 + 보조 12 leading-none 강제). text-caption 12 leading-none 마지노선.

────────────────────────────────────────

# §8. Lucide ≥5종 size prop 매핑 (size prop 룰)

| icon | size | 위치 | className |
|---|---|---|---|
| FileText | 48 | DocumentSection 빈 상태 (line 250) | text-text-tertiary |
| Plus | 16 | DocumentSection uploadBtn 데스크톱 (line 139) | text-on-accent |
| Plus | 20 | DocumentSection uploadBtn 모바일 (line 160) | text-text-primary |
| Loader2 | 16 | Hero card spin (line 358) + 과거 row spin (line 473) + Trash2 spin (line 387 + 503) | text-text-secondary + animate-spin |
| Trash2 | 16 | Hero admin (line 389) + 과거 admin (line 505) | text-danger |
| Upload | 16/20 | DocumentUploadForm file dashed button 옵션 (현재 미사용 — text label 만) | text-text-secondary |
| X | 16 | DocumentUploadForm 모달 닫기 옵션 (현재 미사용 — handleCancel button 만) | text-text-primary |
| AlertTriangle | 16/20 | DocumentSection error block 옵션 (현재 미사용 — 카피만, OQ #4 LOCKED 추가 옵션) | text-danger |

룰 (memory feedback_tailwind_token_class_pattern):
- size prop 사용 (`size={N}` O, `w-N h-N` className X)
- 색은 토큰 className (`text-danger` O, `color="var(--danger)"` 인라인 X)
- status- prefix 0 (`text-danger` O, `text-status-danger` X)
- stroke-width: 24 이하 default 2, 48 이상 1.5 검토 (FileText 48 → strokeWidth 1.5 옵션)

verify: ≥5종 Lucide 박제 + size prop 사용 100% + w-N/h-N className 0.

────────────────────────────────────────

# §9. components.css inherit (재사용 ≥3 + 신규 ≥15)

[재사용 ≥3 — 기존 components.css 클래스 그대로]

`.btn-primary` (기존 — 14-reports / 16-workshift / 17-annual-plan / 23-education 일관)
→ DocumentSection error '다시 시도' button + DocumentSection uploadBtn 데스크톱 pill + DocumentUploadForm Error '다시 시도' button
→ bg-accent + h-11 (또는 h-10) + px-4 + text-body-sm + font-semibold + rounded-lg
→ submit button 은 OQ #2 LOCKED lin-grad → 신규 `.btn-primary-gradient` 별도 생성

`.btn-secondary` (기존 — 14-reports / 16-workshift / 17-annual-plan / 23-education 일관)
→ DocumentUploadForm cancel button
→ transparent + border-border-default + text-text-primary + h-11 + text-body font-semibold + rounded-lg

`.empty-state` (기존 — 14-reports / 23-education 일관)
→ DocumentSection empty block wrapper
→ flex column items-center gap-3 px-6 py-12 + bg-surface-raised + rounded-xl + border-border-default

[신규 ≥15 — 22-documents 전용 새 클래스]

1. `.docs-tab-bar` — DocumentsPage 모바일 탭 sticky top-0 z-10 flex bg-surface-page border-b border-border-default mb-4
2. `.docs-tab-btn` + `.docs-tab-btn-active` — flex-1 h-11 bg-transparent text-body / active text-text-primary font-semibold border-b-2 border-accent
3. `.docs-section-header` — flex justify-between items-center min-h-10 (DocumentSection 헤더 행)
4. `.docs-hero-card` — relative w-full min-h-24 p-6 bg-surface-raised border-border-default rounded-xl flex items-center gap-4 cursor-pointer
5. `.docs-year-tile` — w-16 h-16 bg-surface-sunken rounded-lg flex items-center justify-center font-mono text-display font-semibold text-text-primary flex-shrink-0
6. `.docs-latest-pill` — absolute top-3 right-3 text-caption font-semibold leading-none text-on-accent bg-accent px-2 py-1 rounded-full (OQ #3 12 격상)
7. `.docs-history-row` — w-full min-h-14 p-4 bg-surface-raised border-border-default flex items-center gap-3 cursor-pointer (first:rounded-t-lg + last:rounded-b-lg)
8. `.docs-trash-btn` — w-[32px] h-[32px] inline-flex items-center justify-center bg-transparent text-danger border border-border-default rounded-lg (w-8 = 48 함정 회피)
9. `.docs-upload-btn-mobile` — w-[40px] h-[40px] inline-flex items-center justify-center bg-transparent text-text-primary border border-border-default rounded-lg
10. `.docs-upload-sheet` + `.docs-upload-sheet-body` — fixed inset-0 bg-black/55 z-[1000] flex items-end / body w-full max-h-[85vh] overflow-y-auto bg-surface-raised rounded-t-2xl p-6 animate-slide-up
11. `.docs-upload-modal` + `.docs-upload-modal-body` — fixed inset-0 bg-black/55 z-[1000] flex items-center justify-center animate-fade-in / body w-[min(480px,92vw)] max-h-[85vh] overflow-y-auto bg-surface-raised border-border-default rounded-xl p-6
12. `.docs-sheet-handle` — w-10 h-1 bg-border-default rounded-sm mx-auto mb-4 (40x4)
13. `.docs-input` — w-full h-11 px-3 bg-surface-sunken text-text-primary border-border-default rounded-lg text-body-sm (OQ #5 var(--bg4) → bg-surface-sunken)
14. `.docs-file-btn` + `.docs-file-btn-filled` — w-full h-11 px-3 bg-surface-sunken text-text-secondary border border-dashed border-border-default rounded-lg text-body-sm text-left truncate / filled text-text-primary
15. `.docs-progress-bar` + `.docs-progress-fill` — h-2 bg-surface-sunken rounded-sm overflow-hidden / fill h-full bg-accent rounded-sm transition-[width] duration-[240ms] ease-linear
16. `.docs-error-block` — flex items-center justify-between gap-3 p-3 bg-surface-sunken border-border-default rounded-lg / 본문 text-danger text-body-sm font-medium flex-1
17. `.btn-primary-gradient` — bg gradient (OQ #2 LOCKED lin-grad 135deg #1d4ed8 #0ea5e9) + h-11 + text-body + font-semibold + rounded-lg + disabled:bg-surface-sunken disabled:text-text-tertiary disabled:cursor-not-allowed

(재사용 3 + 신규 17 = 20개 ≥ 18 ≥ 신규 ≥15 + 재사용 ≥3 양쪽 만족)

verify: components.css inherit 표 grep `^[0-9]+\\. \\.docs-` ≥15 + `^\\.btn-` (재사용) ≥2.

────────────────────────────────────────

# §10. Tailwind cheatsheet (12 마지노선 + w-N 함정 회피)

| 영역 | Tailwind class | 비고 |
|---|---|---|
| 32x32 (admin Trash2) | `w-[32px] h-[32px]` 또는 `w-7 h-7` | ★ `w-8 h-8` = 48px 함정 (memory feedback_tailwind_w8_h8_is_48px) |
| 40x40 (모바일 uploadBtn) | `w-[40px] h-[40px]` | arbitrary 권장 |
| 44 (input + button height) | `h-11` | tailwind.config spacing override |
| 40 (데스크톱 uploadBtn pill) | `h-10` | |
| 56 (과거 row min-h) | `min-h-14` | |
| 96 (Hero min-h) | `min-h-24` | |
| 64 (Year tile) | `w-16 h-16` | |
| 11px → 12px | `text-caption` (12) + `leading-none` | ★ 9·10·11 사용 금지 (OQ #3 LOCKED + memory feedback_text_caption_leading_none) |
| 12px | `text-caption` + `leading-none` (작은 컨테이너) | |
| 14px | `text-body-sm` | |
| 16px | `text-body` | 마지노선 ✓ |
| 18px | `text-body` 또는 `text-heading` | |
| 20px → 22px | `text-heading` (22) | ★ OQ #4 LOCKED 모달 title 격상 |
| 28px | `text-display` | Year tile JetBrains Mono |
| Lucide 16/20/24/48 | `size={N}` prop | className 금지 (memory feedback_tailwind_token_class_pattern) |
| @keyframes 3종 | `animate-slide-up` / `animate-fade-in` / `animate-spin` | docs-slide-up + docs-fade-in + docsec-spin 인라인 정의 components.css 또는 tailwind.config 등록 |
| 색 — 강조 | `bg-accent` / `border-accent` / `text-on-accent` | OQ #1 LOCKED, fallback `bg-[#2f81f7]` (16-workshift 사례) |
| 색 — danger | `text-danger` | status- prefix 0 (memory feedback_tailwind_token_class_pattern) |
| 그라데이션 (submit) | lin-grad 135deg #1d4ed8 #0ea5e9 | OQ #2 LOCKED — 인라인 style 또는 신규 `.btn-primary-gradient` |
| backdrop | `bg-black/55` (= rgba 0,0,0,0.55) | docs-slide-up + docs-fade-in 외곽 |

────────────────────────────────────────

# §11. negative rule (W6 본 wave + W7 TSX 변환 직전 — 17건)

1. **src 3 파일 (DocumentsPage.tsx + DocumentSection.tsx + DocumentUploadForm.tsx) 변경 0** — 본 wave (markdown 만) + W7 변환 전 0 byte 변경
2. **sketch HTML 4 파일 변경/추가 0** — W2~W5 완료 직후 W6 는 markdown 만 (sketch/ 서브폴더 절대 X)
3. **wrangler 명령 절대 X** (CLAUDE.local.md + memory `feedback_cbc7119_design_never_wrangler`)
4. **npm run deploy 절대 X** (직원 도메인 경로 — 본 워크트리 cbc7119-design 금지)
5. **이모지 0** (Lucide 텍스트 이름 anchor 만 인용 — FileText/Plus/Loader2/Trash2/Upload/X/AlertTriangle)
6. **linear-gradient 본문 약어 'lin-grad' 사용** — §4/§5 OQ #2 LOCKED 인용 anchor 만 verbatim 등장 / 그 외 본문 약어 (mbr deviation 2건 mirror)
7. **status- prefix 0** (`bg-status-safe / text-status-danger` 등 사용 시 verify FAIL, memory feedback_tailwind_token_class_pattern)
8. **w-8 / h-8 className 0** (48px 함정, memory feedback_tailwind_w8_h8_is_48px). 32x32 = w-[32px] h-[32px] arbitrary 또는 w-7 h-7
9. **9·10·11 fontSize 인라인 0** (text-caption 12 leading-none 마지노선, OQ #3 LOCKED)
10. **옛 alias 토큰 (var(--bg/bg2/bg3/bg4/bd/bd2/t1/t2/t3/acl/accent/safe/warn/danger)) 본문 안 0** — sketch fence 인용 안 예외
11. **utils/api 수정 0** — documentsApi 시그니처 미수정 (외부 의존 보존, memory project_redesign_19_legal multi-file 패턴)
12. **utils/multipartUpload 수정 0** — runMultipartUpload + formatBytes + formatEta + ProgressState 모두 미수정
13. **utils/downloadBlob 수정 0** — downloadDocument 미수정
14. **hooks/useIsDesktop 수정 0** — ≥1024px 분기 default 미수정
15. **stores/authStore 수정 0** — useAuthStore + Staff role 미수정
16. **App.tsx 수정 0** — /documents 라우트 + MOBILE_NO_NAV_PATHS + DESKTOP_HEADER_HIDE_PATHS + PAGE_TITLES 모두 미수정
17. **components.css 수정 — 신규 클래스 추가 OK (§9 ≥15)** 단 기존 `.btn-primary / .btn-secondary / .empty-state` 수정 0

(verify gate `grep -c '^[0-9]+\\.' wave-6-tsx-conversion-checklist.md` 17 보장. wrangler ≥1 + npm run deploy ≥1 + status- ≥1 + w-8 ≥1 negative anchor 박제)

────────────────────────────────────────

# §12. verify gate (22건 — slq mirror)

| # | 명령 | 기대값 |
|---|---|---|
| 1 | `grep -c '^# §' wave-6-tsx-conversion-checklist.md` | =12 (§1~§12) |
| 2 | `grep -c '^\\[GROUP [1-9]' wave-6-tsx-conversion-checklist.md` | ≥9 (비즈 anchor 9 그룹) |
| 3 | `grep -cE '^## §5\\.[1-5]' wave-6-tsx-conversion-checklist.md` | =5 (OQ #1~#5 LOCKED) |
| 4 | `grep -hoE 'class="[^"]+"' cha-bio-safety/docs/redesign-context/22-documents/sketch-wave-*.html \| sort -u \| wc -l` 결과 ≥8 + checklist §6 fence 안 박제 ≥8 | sketch grep fence ≥8 unique class |
| 5 | `grep -oE 'feedback_[a-z_]+\|project_[a-z0-9_]+' wave-6-tsx-conversion-checklist.md \| sort -u \| wc -l` | ≥10 unique slug |
| 6 | `grep -c '^[0-9]\+\\.' wave-6-tsx-conversion-checklist.md` (§11 negative count) | ≥15 (17 박제) |
| 7 | `grep -c '^\| [0-9]\+ \|' wave-6-tsx-conversion-checklist.md` (§12 verify table row) | ≥18 (22 박제) |
| 8 | `wc -l cha-bio-safety/docs/redesign-context/22-documents/DocumentsPage.tsx` | =162 |
| 9 | `wc -l cha-bio-safety/docs/redesign-context/22-documents/DocumentSection.tsx` | =517 |
| 10 | `wc -l cha-bio-safety/docs/redesign-context/22-documents/DocumentUploadForm.tsx` | =402 |
| 11 | `grep -cE '^[0-9]+\\. \\.(docs|btn)-' wave-6-tsx-conversion-checklist.md` (§9 components.css inherit) | 재사용 ≥2 + 신규 ≥15 = 합계 ≥17 |
| 12 | `grep -c 'text-caption' wave-6-tsx-conversion-checklist.md` (§10 Tailwind cheatsheet 12 마지노선) | ≥1 |
| 13 | `grep -cE 'FileText\|Plus\|Loader2\|Trash2\|Upload\|X\|AlertTriangle' wave-6-tsx-conversion-checklist.md` | ≥5종 |
| 14 | `grep -cE 'docs-slide-up\|docs-fade-in\|docsec-spin' wave-6-tsx-conversion-checklist.md` | ≥3 (@keyframes 3종) |
| 15 | `grep -c '소방계획서\|소방훈련자료' wave-6-tsx-conversion-checklist.md` (typeLabel verbatim) | ≥2 |
| 16 | `grep -c '정말 삭제하시겠습니까' wave-6-tsx-conversion-checklist.md` (★ delete confirm verbatim) | ≥1 |
| 17 | `grep -c '업로드 중입니다' wave-6-tsx-conversion-checklist.md` (beforeunload verbatim) | ≥1 |
| 18 | `grep -c '업로드를 취소하시겠습니까' wave-6-tsx-conversion-checklist.md` (abort confirm verbatim) | ≥1 |
| 19 | `grep -c '관리자만 업로드할 수 있습니다' wave-6-tsx-conversion-checklist.md` (403 카피 verbatim) | ≥1 |
| 20 | `grep -c "useAuthStore" wave-6-tsx-conversion-checklist.md` (admin 권한 anchor verbatim) | ≥1 |
| 21 | `grep -cE '#1d4ed8.*#0ea5e9\|linear-gradient\\(135deg' wave-6-tsx-conversion-checklist.md` (OQ #2 lin-grad anchor) | ≥3 (§4/§5/§9 등) |
| 22 | `grep -c 'bg-surface-sunken' wave-6-tsx-conversion-checklist.md` (OQ #5 통일) | ≥3 (§3/§9/§10 등) |
| + | `git diff --name-only HEAD -- cha-bio-safety/src/pages/DocumentsPage.tsx cha-bio-safety/src/components/DocumentSection.tsx cha-bio-safety/src/components/DocumentUploadForm.tsx cha-bio-safety/src/utils/api.ts cha-bio-safety/src/utils/multipartUpload.ts cha-bio-safety/src/utils/downloadBlob.ts cha-bio-safety/src/hooks/useIsDesktop.ts cha-bio-safety/src/stores/authStore.ts cha-bio-safety/src/App.tsx cha-bio-safety/src/styles/components.css` | 0 lines (src 변경 0) |
| + | `git diff --name-only HEAD -- cha-bio-safety/docs/redesign-context/22-documents/sketch-wave-*.html` | 0 lines (sketch HTML 변경 0) |
| + | `git diff --name-only HEAD -- cha-bio-safety/docs/redesign-context/22-documents/wave-6-tsx-conversion-checklist.md` | 1 line (본 wave 산출 1개만) |

22 verify gate + 3 git diff gate 모두 PASS 시 본 checklist 가 W7 TSX 변환 wave 단일 진입점 자격을 갖춘 것으로 본다.

────────────────────────────────────────
```

**작성 후 verify (executor 자체):**
1. `wc -l cha-bio-safety/docs/redesign-context/22-documents/wave-6-tsx-conversion-checklist.md` ≥380
2. §12 verify gate 22 + 3 모두 본인이 직접 실행 + 결과 SUMMARY 박제
3. `git diff --name-only HEAD` 결과 `cha-bio-safety/docs/redesign-context/22-documents/wave-6-tsx-conversion-checklist.md` 1줄만

**commit 명령 verbatim (executor cherry-pick 사고 6회 precedent 회피용):**
```
git add cha-bio-safety/docs/redesign-context/22-documents/wave-6-tsx-conversion-checklist.md
git commit -m "$(cat <<'EOF'
docs(quick-260526-8yx): redesign/22-documents W6 TSX 변환 verify checklist (12 섹션 + multi-file 3 파일 박제)

slq (18-worklog W7) / 6if / bbz / lft / 4i9 / 0803d2f 6 mirror 일관 — 12 섹션 (§1 imports / §2 메인 함수 / §3 JSX render / §4 비즈 anchor 9 그룹 / §5 OQ LOCKED 5건 / §6 sketch grep fence ≥8 / §7 폰트 격상 / §8 Lucide ≥5종 / §9 components.css 재사용 ≥3 + 신규 ≥15 / §10 Tailwind cheatsheet / §11 negative 17 / §12 verify 22).

multi-file scope — DocumentsPage 162 + DocumentSection 517 + DocumentUploadForm 402 = 1081 lines. ★ delete confirm full string verbatim (memory project_legal_findings_delete_incident_260520 1 byte 변경 0) + beforeunload + abort confirm + 403 카피 + admin 권한 + multipart upload + ALLOWED 6 ext + empty MIME fallback + abort/retry 9 그룹 비즈 anchor 박제.

OQ #1~#5 LOCKED verbatim (chrome 토큰 통일 / submit button lin-grad / 최신 pill 12 격상 / 모달 title 22 격상 + Error 14 격상 + 빈 FileText 48 유지 / var(--bg4) → bg-surface-sunken 통일).

다음 wave: W7 TSX 변환 실행 (본 checklist 단일 진입점 참조).
EOF
)"
git log --oneline -1
```

executor 는 commit 직후 `git log --oneline -1` 결과 (hash + message 1줄) 명시적 보고 필수.
  </action>
  <verify>
    <automated>cd /Users/jykevin/Documents/cbc7119-design && wc -l cha-bio-safety/docs/redesign-context/22-documents/wave-6-tsx-conversion-checklist.md | awk '{ if ($1 >= 380) print "PASS lines="$1; else { print "FAIL lines="$1; exit 1 } }' && grep -c '^# §' cha-bio-safety/docs/redesign-context/22-documents/wave-6-tsx-conversion-checklist.md | awk '{ if ($1 == 12) print "PASS §1~§12"; else { print "FAIL §count="$1; exit 1 } }' && grep -c '^\[GROUP [1-9]' cha-bio-safety/docs/redesign-context/22-documents/wave-6-tsx-conversion-checklist.md | awk '{ if ($1 >= 9) print "PASS GROUP="$1; else { print "FAIL GROUP="$1; exit 1 } }' && grep -cE '^## §5\.[1-5]' cha-bio-safety/docs/redesign-context/22-documents/wave-6-tsx-conversion-checklist.md | awk '{ if ($1 == 5) print "PASS OQ#1-5"; else { print "FAIL OQ="$1; exit 1 } }' && SLUG_COUNT=$(grep -oE 'feedback_[a-z_]+|project_[a-z0-9_]+' cha-bio-safety/docs/redesign-context/22-documents/wave-6-tsx-conversion-checklist.md | sort -u | wc -l) && [ "$SLUG_COUNT" -ge 10 ] && echo "PASS slug=$SLUG_COUNT" && grep -c '정말 삭제하시겠습니까' cha-bio-safety/docs/redesign-context/22-documents/wave-6-tsx-conversion-checklist.md | awk '{ if ($1 >= 1) print "PASS delete confirm"; else { print "FAIL delete confirm"; exit 1 } }' && grep -c '업로드 중입니다' cha-bio-safety/docs/redesign-context/22-documents/wave-6-tsx-conversion-checklist.md | awk '{ if ($1 >= 1) print "PASS beforeunload"; else { print "FAIL beforeunload"; exit 1 } }' && grep -c '업로드를 취소하시겠습니까' cha-bio-safety/docs/redesign-context/22-documents/wave-6-tsx-conversion-checklist.md | awk '{ if ($1 >= 1) print "PASS abort confirm"; else { print "FAIL abort confirm"; exit 1 } }' && grep -c '관리자만 업로드할 수 있습니다' cha-bio-safety/docs/redesign-context/22-documents/wave-6-tsx-conversion-checklist.md | awk '{ if ($1 >= 1) print "PASS 403 카피"; else { print "FAIL 403"; exit 1 } }' && grep -c 'useAuthStore' cha-bio-safety/docs/redesign-context/22-documents/wave-6-tsx-conversion-checklist.md | awk '{ if ($1 >= 1) print "PASS admin anchor"; else { print "FAIL admin"; exit 1 } }' && SRC_DIFF=$(git diff --name-only HEAD -- cha-bio-safety/src/pages/DocumentsPage.tsx cha-bio-safety/src/components/DocumentSection.tsx cha-bio-safety/src/components/DocumentUploadForm.tsx cha-bio-safety/src/utils/api.ts cha-bio-safety/src/utils/multipartUpload.ts cha-bio-safety/src/utils/downloadBlob.ts cha-bio-safety/src/hooks/useIsDesktop.ts cha-bio-safety/src/stores/authStore.ts cha-bio-safety/src/App.tsx cha-bio-safety/src/styles/components.css | wc -l) && [ "$SRC_DIFF" -eq 0 ] && echo "PASS src 변경 0" && SKETCH_DIFF=$(git diff --name-only HEAD -- cha-bio-safety/docs/redesign-context/22-documents/sketch-wave-2-chrome.html cha-bio-safety/docs/redesign-context/22-documents/sketch-wave-3-document-section.html cha-bio-safety/docs/redesign-context/22-documents/sketch-wave-4-upload-form-inline.html cha-bio-safety/docs/redesign-context/22-documents/sketch-wave-5-modal-confirm.html | wc -l) && [ "$SKETCH_DIFF" -eq 0 ] && echo "PASS sketch 변경 0" && echo "ALL VERIFY PASS"</automated>
  </verify>
  <done>
    `cha-bio-safety/docs/redesign-context/22-documents/wave-6-tsx-conversion-checklist.md` 1개 생성 — 12 섹션 (§1~§12) + ≥380 lines + 9 비즈 anchor 그룹 + 5 OQ LOCKED + ≥8 sketch grep fence + ≥10 메모리 룰 unique slug + 17 negative + 22 verify gate + ★ delete confirm + beforeunload + abort confirm + 403 + admin 권한 + lin-grad anchor + bg-surface-sunken 통일 모두 verbatim 박제. src 3 파일 + utils 3개 + hooks + stores + App.tsx + components.css 모두 0 byte 변경. sketch HTML 4 파일 0 byte 변경. git commit 1개 (verbatim 메시지) + executor 가 `git log --oneline -1` 결과 명시적 보고.
  </done>
</task>

</tasks>

<verification>
- §1~§12 12 헤더 모두 존재 (grep `^# §` = 12)
- 비즈 anchor 9 그룹 모두 박제 (grep `^\[GROUP [1-9]` ≥9)
- OQ #1~#5 5건 모두 verbatim 인용 (grep `^## §5\.[1-5]` = 5)
- sketch grep fence ≥8 unique class (executor 실시간 grep 결과 박제)
- 메모리 룰 unique slug ≥10 (feedback_* 10 + project_* 2)
- ★ delete confirm + beforeunload + abort confirm + 403 + admin 권한 모두 verbatim
- src 3 파일 + utils/api + utils/multipartUpload + utils/downloadBlob + hooks/useIsDesktop + stores/authStore + App.tsx + components.css 모두 0 byte 변경
- sketch HTML 4 파일 0 byte 변경
- git commit 1개 (markdown 1개 + commit 1 + SUMMARY 1 = 총 2 commit on branch)
- executor 가 `git log --oneline -1` 결과 명시적 보고 (cherry-pick 사고 6회 precedent 회피)
</verification>

<success_criteria>
- `wave-6-tsx-conversion-checklist.md` 1 파일 ≥380 lines + 12 섹션 + 9 비즈 anchor 그룹 + 5 OQ LOCKED + ≥8 sketch grep fence + ≥10 메모리 룰 unique slug + 17 negative + 22 verify gate 모두 만족
- ★ delete confirm full string verbatim 1 byte 변경 0 (memory project_legal_findings_delete_incident_260520 일반화)
- OQ #1~#5 LOCKED default 5건 모두 §5 안 verbatim 인용
- src 3 파일 + 외부 의존 6 파일 모두 0 byte 변경
- atomic 1 commit (verbatim 메시지 박제) — executor 가 `git log --oneline -1` hash + message 1줄 명시 보고
- 다음 wave (W7 TSX 변환 실행) 단일 진입점으로 본 checklist 자격 충족
</success_criteria>

<output>
After completion: SUMMARY 생성 `.planning/quick/260526-8yx-redesign-22-documents-w6-tsx-conversion-/260526-8yx-SUMMARY.md` (commit hash + message + 22 verify gate 결과 + W7 다음 단계 안내).
</output>
