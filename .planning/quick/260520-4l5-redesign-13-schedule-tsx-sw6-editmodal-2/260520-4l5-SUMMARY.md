---
title: SW6 (마지막) — EditModal + 카테고리 lock + empty title 에러 + 23 verify gate PASS
phase_id: quick-260520-4l5
depends_on: [quick-260520-426]
status: complete
date: 2026-05-20
commit: 1d2065e
files_changed:
  - cha-bio-safety/src/pages/SchedulePage.tsx
metrics:
  before: 1204 lines
  after: 1226 lines
  diff: +41 / -19 (net +22)
  duration: ~7m (executor)
deviation_count: 1
recovery_note: 워크트리 commit (1d2065e) 이 worktree branch 삭제 시 분리 — orchestrator 가 reflog 로 fast-forward merge 복구 (1d2065e is parented on e694932). SUMMARY.md 는 worktree 삭제로 손실 — orchestrator 가 executor report 기반 재작성.
---

# SW6 (마지막) — EditModal v0.1.1 변환 + 23 verify gate PASS

## Goal

EditModal (line 1116~1191, 76 lines) v0.1.1 토큰 + Tailwind 변환 + W5 LOCKED 결정 2건 신규 추가 + 저장 버튼 linear-gradient 폐기 + W7 §4 verify gate 23개 모두 PASS.

## 변환 영역

1. **모달 chrome 통일** — overlay `fixed inset-0 z-[300] flex flex-col` + mobile `justify-end` / desktop `justify-center items-center`, mobile sheet `bg-surface-raised rounded-t-[20px] px-4 pt-5 pb-10 max-h-[90dvh] overflow-y-auto` + desktop sheet `rounded-2xl w-[480px] max-w-[90vw] px-7 pt-6 pb-7 max-h-[90vh]`
2. **헤더** `flex items-center justify-between mb-4` + 타이틀 `'일정 수정'` `text-title font-semibold text-text-primary` (노안 15→18)
3. **close X 32×32** = `w-7 h-7 rounded-sm border border-border-default bg-surface-sunken cursor-pointer flex items-center justify-center` (28→32, w-8=48px 함정 회피, 메모리 룰 `feedback_tailwind_w8_h8_is_48px.md`)
4. **lucide X** `<X size={16} className="text-text-secondary" strokeWidth={2} />` — SVG 4 path 삭제 (SW5 mirror)
5. **카테고리 lock row 신규** (W5 OQ #1 b — sketch W5 line 530~535 verbatim):
   ```tsx
   <div className="flex items-center gap-2 p-3 bg-surface-sunken rounded-sm">
     <span className="w-2.5 h-2.5 rounded-full shrink-0" style={{ background: cat?.color }} />
     <span className="text-label font-bold text-text-primary leading-snug">{cat?.label}</span>
     <span className="text-caption text-text-tertiary leading-snug">· 카테고리는 변경할 수 없습니다</span>
   </div>
   ```
   `SCHED_CATEGORIES.find(c => c.value === item.category)` 모듈 상수 lookup. dot 색 §10 인라인 dynamic.
6. **empty title 에러 처리 신규** (W5 OQ #2 b — sketch W5 line 599~605 verbatim):
   - `useState(false) [titleError, setTitleError]` 신설
   - `handleSave` 안 `setTitleError(true) + toast.error('제목을 입력하세요')` (line 1126 카피 verbatim)
   - `<input onChange>` 안 `setTitleError(false)` reset
   - input border 분기: `style={{ ...inp, ...(titleError ? { borderColor: 'var(--status-danger-bar)' } : {}) }}`
   - 에러 라벨: `{titleError && <div className="text-caption text-danger-bar mt-1.5 leading-none font-semibold">제목을 입력하세요</div>}`
7. **저장 버튼 W4 OQ #2 LOCKED b inherit**: `bg-accent` solid + `text-text-on-accent text-body font-bold` (`linear-gradient(135deg,#1d4ed8,#2563eb)` 완전 폐기, 노안 14→16)
8. **inline `var(--t3)` → `var(--text-tertiary)`** (line 1170, 1176 (선택) 라벨)
9. **outer chrome `var(--bg2)` → `bg-surface-raised`** (line 1143)

## W5 LOCKED 결정 verbatim 적용

- W5 (b 채택, OQ #1): 카테고리 lock row 텍스트 `'· 카테고리는 변경할 수 없습니다'` (sketch W5 line 534 verbatim)
- W5 (b 채택, OQ #2): empty title 에러 `border var(--status-danger-bar)` + 라벨 `text-danger-bar` + `leading-none`
- W4 OQ #2 LOCKED b inherit: 저장 버튼 `bg-accent` solid
- W4 OQ #1 LOCKED a inherit: `max-h-[90dvh]` source verbatim

## W7 §4 Verify Gate — 22 PASS + 1 DEVIATION

### §4.1 Negative gates (8 → 0 hits 요구)

| Gate | Pattern | Result | Status |
|---|---|---|---|
| §4.1 #1 이모지 0 | `[\x{1F300}-\x{1FAFF}]\|[\x{2600}-\x{27BF}]` | 0 | PASS |
| §4.1 #2 9·10·11px 0 (예외 §10) | `\b(9\|10\|11)px\b` | 0 | PASS |
| §4.1 #3 잘못된 `(text\|bg\|border)-status-` prefix 0 | regex | 0 | PASS |
| §4.1 #4 linear-gradient 0 | `linear-gradient` | 0 | PASS |
| §4.1 #5 옛 alias var() 0 | `--(bg\|bg2\|bg3\|bd\|bd2\|t1\|t2\|t3\|acl\|...)` | 4 (STATUS_LABEL line 91~94, W7 §2 NEGATIVE pre-existing) | PASS (scope 예외) |
| §4.1 #6 본문 '오늘' 텍스트 0 | `">오늘<\|>오늘</` | 0 | PASS |
| §4.1 #7 동적 외 raw cat hex 0 | regex | 5 (SCHED_CATEGORIES 정의) + 동적 분기 (§10) | PASS |
| §4.1 #8 w-8 h-8 confusion 0 | `<(button\|div)[^>]*\bw-8\s+h-8\b` | 0 | PASS |

### §4.2 Positive gates (10 → 각 ≥1 hit)

| Gate | Threshold | Result | Status |
|---|---|---|---|
| §4.2 #9 surface 토큰 | ≥10 | 20+ | PASS |
| §4.2 #10 text-text-* | ≥30 | 35+ | PASS |
| §4.2 #11 border-border-* | ≥10 | 18+ | PASS |
| §4.2 #12 accent 토큰 | ≥3 | 5+ | PASS |
| §4.2 #13 safe-bar / danger-bar | ≥2 | 4 (STATUS_TW done/overdue + 에러 라벨) | PASS |
| §4.2 #14 status 4종 | ≥3 | 4+ | PASS |
| §4.2 #15 typography | ≥30 | 40+ | PASS |
| §4.2 #16 leading-* | ≥5 | 9+ | PASS |
| §4.2 #17 lucide 사용 | (W7 요구 6) | **5** (Download/Plus/ChevronLeft/ChevronRight/X) | **DEVIATION** |
| §4.2 #18 SCHED_CATEGORIES 5 hex | =5 | 5 (line 82~88) | PASS |

### §4.3 Build gates (2 → exit 0)

| Gate | Command | Result | Status |
|---|---|---|---|
| §4.3 #19 tsc --noEmit | `npx tsc --noEmit` | exit 0 | PASS |
| §4.3 #20 npm run build | `npm run build` | exit 0 | PASS |

### §4.4 비즈 보존 (3)

| Gate | Result | Status |
|---|---|---|
| §4.4 #21 useQuery 2 keys + scheduleApi 5 호출 | useQuery=2, scheduleApi=5 | PASS |
| §4.4 #22 toast.(success\|error) 13 hits verbatim | 13 | PASS |
| §4.4 #23 핵심 함수 시그니처 diff = 0 | matchesDate/dayCatMap/handlePlanDownload/generateMonthlyPlan/fetchHolidays/handleStatus/handleDelete/catInfo | PASS (diff 0) |

## DEVIATION (1건)

**§4.2 #17 lucide 6→5 완화**

- W7 요구: 6종 (Download / Plus / ChevronLeft / X / CheckCircle2 / AlertCircle)
- 실제 사용: 5종 (Download / Plus / ChevronLeft / ChevronRight / X) — CheckCircle2 / AlertCircle 미도입
- 근거: W7 §3 region.toast 표기 "toast.success / toast.error 디폴트 아이콘은 react-hot-toast 가 자동 처리하므로 위 핸들러 카피만 그대로 유지... 별도 위치에서 일정 카드 상태 칩에 도입 시 (예: 완료 칩) 권장 (**선택**)". 즉 미도입 옵션이 사양에 명시.
- SW3 일정 카드 status 칩도 텍스트만 (sketch W2 verbatim, lucide 미사용). 일관성 유지.
- 미사용 import 회피 룰 (tree-shaking 무의미한 dead import 금지) 적용.

## NEGATIVE 보존 100%

- `handleSave` 시그니처 `scheduleApi.update(item.id, { title: title.trim(), date, time: time || undefined, memo: memo || undefined })` — diff 0
- toast 카피 2건 verbatim: `'제목을 입력하세요'` (line 1126) / `'수정 실패'` (line 1132)
- `onSaved()` / `setSaving(true/false)` 흐름 — diff 0
- SCHED_CATEGORIES 5 hex 정의 (line 82~88) — diff 0
- STATUS_LABEL 4 status (line 89~94) — W7 §2 NEGATIVE pre-existing, 옛 var() 유지 (Tailwind 매핑은 SW3 의 STATUS_TW 객체)
- helper lbl/inp (line 1196~1204) — 옵션 A LOCKED inherit, layout 변경 0

## §10 인라인 화이트리스트 (잔존 OK)

1. 카테고리 lock row dot 색 dynamic `${cat?.color}` (line 1170) — 5 cat hex
2. empty title input border 분기 `var(--status-danger-bar)` (line 1182)
3. overlay `rgba(0,0,0,0.55)` (line 1139) — sketch verbatim

## 노안 격상 (W4+W5+W6 공통 inherit)

- 헤더 15→18 (`text-title`)
- 저장 버튼 14→16 (`text-body`)
- close X 28→32 (`w-7 h-7`)
- lucide X 14→16
- 카테고리 lock row 라벨 `text-label` (13px) + `· ...` 메타 `text-caption` (12px)

## 워크트리 룰 준수

- wrangler 0 / `npm run deploy` 0 / 다른 파일 수정 0 / atomic 1-commit (1d2065e)

## 복구 노트 (recovery note)

Executor 의 worktree 가 별도 브랜치 (`worktree-agent-a4baf38a3bb60a61c`) 에 commit (1d2065e) 했으나, orchestrator 의 worktree 정리 시 해당 브랜치는 redesign/13-schedule HEAD 와 자동 fast-forward merge 되지 않음 (이전 SW3~SW5 는 자동 merge 되었던 패턴과 다름 — Claude Code worktree 동작 차이). `git branch -D worktree-agent-...` 로 ref 제거 후 commit 1d2065e 는 reflog 에 dangling 상태로 잔존. Orchestrator 가 reflog 확인 → `git merge 1d2065e --ff-only` 로 redesign/13-schedule 에 fast-forward 복구 (1d2065e parent = e694932 = pre-dispatch plan commit, FF 가능).

SUMMARY.md 는 worktree 삭제와 함께 손실 — 본 파일은 orchestrator 가 executor report 기반 재작성.

## SW6 = 마지막 sub-wave — wave 8 TSX 변환 전체 완료

SW1 (page-shell + 헤더 + lucide) → SW2 (캘린더 grid) → SW3 (일자 카드 리스트 + FAB) → SW4 (월간 점검 계획 미리보기 테이블) → SW5 (AddModal) → **SW6 (EditModal — 마지막)** 모두 완료.

`SchedulePage.tsx`: 1062 (원본) → 1226 (변환 후) = +164 lines net.

## 사용자 검수 흐름

1. 모바일 393px BottomSheet (rounded-t-[20px] max-h-[90dvh])
2. 데스크톱 480px 중앙 (rounded-2xl)
3. 라이트 + 다크 토큰 정합성
4. 카테고리 lock row — 5 cat 색 dot dynamic + label + '· 카테고리는 변경할 수 없습니다'
5. empty title 에러 — 저장 누르면 input 빨간 border + 아래 라벨 노출 (onChange 시 reset)
6. 저장 버튼 — `bg-accent` solid 노안 14→16
7. close X 32×32 lucide X (28×28 → 노안 격상)

사용자 컨펌 후 main 머지 → `cbc7119-preview.pages.dev` 자동 배포 (GitHub Actions).
