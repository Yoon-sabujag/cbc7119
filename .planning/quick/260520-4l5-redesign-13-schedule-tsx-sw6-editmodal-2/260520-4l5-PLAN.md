---
title: SW6 (마지막) — EditModal + 카테고리 lock + empty title 에러 + 23 verify gate PASS
phase_id: quick-260520-4l5
phase: quick-260520-4l5
plan: 01
type: execute
wave: 1
depends_on: [quick-260520-426]
files_modified:
  - cha-bio-safety/src/pages/SchedulePage.tsx
autonomous: true
status: ready
requirements: [redesign-13-schedule-w7-sw6]
must_haves:
  truths:
    - "EditModal 본문이 v0.1.1 토큰 + Tailwind 클래스로 변환되어 표시된다 (옛 alias var(--bg/bg2/bg3/bd/t1/t2/t3) 0 hit)"
    - "EditModal 본문 첫 element 로 카테고리 lock row 가 표시되며 (dot + label + '· 카테고리는 변경할 수 없습니다'), 카테고리는 수정 불가하다"
    - "title 비운 채 저장 누르면 input border 가 status-danger-bar 로 바뀌고 '제목을 입력하세요' 에러 라벨이 input 직후에 표시된다 (sketch W5 frame B verbatim)"
    - "저장 버튼이 bg-accent solid 로 렌더링된다 (linear-gradient(135deg,#1d4ed8,#2563eb) 폐기)"
    - "close X 버튼이 32×32 (w-7 h-7) + lucide X (size 16) 로 표시된다 (이모지/SVG path 0)"
    - "W7 §4 verify gate 23개 모두 PASS (Negative 8 + Positive 10 + Build 2 + 비즈 보존 3)"
    - "handleSave 의 scheduleApi.update 시그니처 / onSaved / setSaving / toast 카피 2건 ('제목을 입력하세요' / '수정 실패') 1 byte 변경 없음"
  artifacts:
    - path: "cha-bio-safety/src/pages/SchedulePage.tsx"
      provides: "EditModal 변환 + lock row 신규 + empty title 에러 신규 + linear-gradient 폐기"
      contains: "function EditModal"
      contains_strict: "titleError"
  key_links:
    - from: "EditModal lock row dot"
      to: "SCHED_CATEGORIES (line 82)"
      via: "SCHED_CATEGORIES.find(c => c.value === item.category)?.color"
      pattern: "SCHED_CATEGORIES\\.find\\(c => c\\.value === item\\.category\\)"
    - from: "EditModal empty title 분기"
      to: "useState titleError"
      via: "handleSave setTitleError(true) + onChange setTitleError(false)"
      pattern: "setTitleError"
---

<objective>
SchedulePage.tsx EditModal (line 1116~1191, 76 lines) TSX 변환 + W5 LOCKED 결정 2건 신규 추가 + W4 OQ #2 LOCKED b inherit + W7 §4 verify gate 23개 모두 PASS.

**SW6 가 마지막 sub-wave** — 전체 변환 wave 8 완료를 의미. 사용자 컨펌 후 main 머지 → cbc7119-preview.pages.dev 자동 배포.

Purpose:
1. 옛 alias var(--bg2/bg3/bd/t1/t2/t3) 잔존 0 — v0.1.1 토큰으로 매핑
2. **카테고리 lock row 신규 추가** (sketch W5 line 530~535 verbatim) — `item.category` 표시 + "변경할 수 없습니다" 라벨
3. **empty title 에러 처리 신규 추가** (sketch W5 line 599~605 verbatim) — useState `titleError` + input border 분기 + 에러 라벨
4. **linear-gradient(135deg,#1d4ed8,#2563eb) 폐기** — 저장 버튼 `bg-accent` solid (W4 OQ #2 LOCKED b)
5. close X 28→32 (`w-7 h-7`) + lucide X (size 16) — SW5 mirror
6. 노안 격상: 헤더 15→18 (`text-title`) / 저장 14→16 (`text-body`)
7. helper (lbl/inp) 옵션 A LOCKED inherit (SW5) — 유지

Output:
- 단일 파일 변경: `cha-bio-safety/src/pages/SchedulePage.tsx`
- line 변화: +15 ~ +30 (lock row + 에러 라벨 + linear-gradient 폐기 + lucide 도입)
- W7 §4 verify gate 23 PASS (5 SUMMARY 표 row)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@CLAUDE.local.md
@cha-bio-safety/docs/redesign-context/13-schedule/wave-7-tsx-conversion-checklist.md
@cha-bio-safety/docs/redesign-context/13-schedule/sketch-wave-5.html
@cha-bio-safety/docs/redesign-context/13-schedule/tokens.css
@cha-bio-safety/docs/redesign-context/13-schedule/design-system.md
@cha-bio-safety/src/pages/SchedulePage.tsx
@cha-bio-safety/tailwind.config.js
@.planning/quick/260520-426-redesign-13-schedule-tsx-sw5-addmodal-bo/260520-426-SUMMARY.md
@.planning/quick/260520-1q0-redesign-13-schedule-tsx-sw3-rendercard-/260520-1q0-SUMMARY.md

<interfaces>
<!-- EditModal 현재 시그니처 (line 1116~1118, 1 byte 변경 금지) -->
```tsx
function EditModal({ item, onClose, onSaved, isDesktop }: {
  item: ScheduleItem; onClose: () => void; onSaved: () => void; isDesktop?: boolean
}) { ... }
```

<!-- SCHED_CATEGORIES 정의 (line 82~88, NEGATIVE — 5 hex 잔존 유지) -->
```tsx
const SCHED_CATEGORIES: { value: ScheduleCategory; label: string; color: string }[] = [
  { value:'inspect',  label:'점검',  color:'#3b82f6' },
  { value:'task',     label:'업무',  color:'#eab308' },
  { value:'event',    label:'행사',  color:'#e2e8f0' },
  { value:'elevator', label:'승강기', color:'#f97316' },
  { value:'fire',     label:'소방',  color:'#ef4444' },
]
```

<!-- helper (lbl/inp, line 1196~1204, 옵션 A LOCKED inherit — 유지) -->
```tsx
const lbl: React.CSSProperties = {
  fontSize:12, fontWeight:700, color:'var(--text-tertiary)',
  display:'block', marginBottom:6, lineHeight:1,
}
const inp: React.CSSProperties = {
  width:'100%', padding:'11px 12px', borderRadius:10, boxSizing:'border-box',
  background:'var(--surface-sunken)', border:'1px solid var(--border-strong)',
  color:'var(--text-primary)', fontSize:13, outline:'none', fontFamily:'inherit',
}
```

<!-- scheduleApi.update 시그니처 (NEGATIVE — 1 byte 변경 금지) -->
```ts
scheduleApi.update(item.id, {
  title: title.trim(),
  date,
  time: time || undefined,
  memo: memo || undefined
})
```
</interfaces>
</context>

<source_of_truth>

## W7 §3 region.edit-modal verbatim

`SchedulePage.tsx line 1116~1191 (76 lines)` — EditModal 함수 전체.
- close X 버튼 (line 1151~1156): SW5 AddModal mirror — `w-7 h-7` + lucide X.
- 저장 버튼 (line 1180~1186): linear-gradient → `bg-accent`.
- 입력 필드 4개 (title / date / time / memo): helper lbl/inp 유지.
- **신규 추가**: 카테고리 lock row (W5 OQ #1 b) + empty title 에러 (W5 OQ #2 b).

## W7 §6 sub-task 6 (마지막)

> "EditModal — line 1116~1191 atomic 변환. AddModal mirror + W5 LOCKED 결정 2건 신규 추가. close X w-7 h-7, lucide X 도입, 저장 버튼 bg-accent. verify gate 23개 모두 PASS."

## W7 §8 LOCKED 결정 verbatim (W5 + W4 inherit)

**W5 OQ #1 LOCKED b** (sketch W5 line 530~535 verbatim):
```html
<div style="display:flex; align-items:center; gap:8px; padding:12px; background:var(--surface-sunken); border-radius:8px;">
  <span style="width:10px; height:10px; border-radius:99px; background:#3b82f6; flex-shrink:0;"></span>
  <span style="font-size:13px; font-weight:700; color:var(--text-primary); line-height:1.4;">점검</span>
  <span class="text-caption" style="color:var(--text-tertiary); line-height:1.4;">· 카테고리는 변경할 수 없습니다</span>
</div>
```

**W5 OQ #2 LOCKED b** (sketch W5 line 599~605 verbatim):
```html
<input value="" placeholder="제목을 입력하세요"
  style="... border:1px solid var(--danger); ..." />
<div class="text-caption" style="margin-top:6px; color:var(--danger); line-height:1; font-weight:600;">제목을 입력하세요</div>
```
> 메모리 룰 정합: sketch 의 `var(--danger)` 는 옛 alias. v0.1.1 매핑 = `var(--status-danger-bar)` (border 용). Tailwind class 별칭 = `border-danger-bar` / `text-danger-bar` (status- prefix 없음).

**W4 OQ #2 LOCKED b inherit** (AddModal mirror):
- 저장 버튼 = `bg-accent` solid (linear-gradient(135deg,#1d4ed8,#2563eb) 폐기, line 1182).
- 노안 14→16 (`text-body`).

**W4 OQ #1 LOCKED a inherit**:
- 모바일 BottomSheet `maxHeight:'90dvh'` source verbatim 유지 (line 1146).

## W7 §10 인라인 화이트리스트 (잔존 OK — 3건)

1. **카테고리 lock row dot 색 dynamic** — `style={{ background: cat?.color }}` (5 cat hex)
2. **empty title input border 분기** — `style={{...inp, ...(titleError ? {borderColor:'var(--status-danger-bar)'} : {})}}`
3. **overlay 배경** — `rgba(0,0,0,0.55)` (line 1139) sketch verbatim 또는 `var(--surface-overlay)` 치환 (둘 다 OK)

</source_of_truth>

<sketch_verbatim_w5>

## sketch-wave-5.html line 520~561 (frame A — default 카테고리 lock row)

```html
<!-- line 520~561 (frame A: 일정 수정 default state) -->
<!-- 모달 본문 첫 element = 카테고리 lock row -->
<div style="display:flex; align-items:center; gap:8px; padding:12px; background:var(--surface-sunken); border-radius:8px;">
  <span style="width:10px; height:10px; border-radius:99px; background:#3b82f6; flex-shrink:0;"></span>
  <span style="font-size:13px; font-weight:700; color:var(--text-primary); line-height:1.4;">점검</span>
  <span class="text-caption" style="color:var(--text-tertiary); line-height:1.4;">· 카테고리는 변경할 수 없습니다</span>
</div>
<!-- 그 다음 title 라벨 + input ... -->
```

## sketch-wave-5.html line 584~625 (frame B — empty title error)

```html
<!-- title input — border:1px solid var(--danger) -->
<label class="text-label" style="font-weight:700; color:var(--text-tertiary); display:block; margin-bottom:6px; line-height:1;">제목</label>
<input value="" placeholder="제목을 입력하세요"
  style="width:100%; padding:11px 12px; border-radius:10px; box-sizing:border-box;
         background:var(--surface-sunken); border:1px solid var(--danger);
         color:var(--text-primary); font-size:13px; outline:none;" />
<!-- 에러 라벨 — input 직후 -->
<div class="text-caption" style="margin-top:6px; color:var(--danger); line-height:1; font-weight:600;">제목을 입력하세요</div>
```

> sketch 의 `var(--danger)` → v0.1.1 매핑 = `var(--status-danger-bar)` (border 용, saturated). Tailwind class = `border-danger-bar` / `text-danger-bar` (status- prefix 없음).

</sketch_verbatim_w5>

<transform_map>

## 변환 매핑 (source line → 변환 후)

| Source line (현재) | 변환 후 |
|---|---|
| line 1139 overlay `position:'fixed', inset:0, background:'rgba(0,0,0,0.55)', zIndex:300, display:'flex', flexDirection:'column'` + isDesktop justify/align | `className="fixed inset-0 z-[300] flex flex-col"` + className mobile `justify-end` / desktop `justify-center items-center` + `style={{background:'rgba(0,0,0,0.55)'}}` (§10 화이트리스트) |
| line 1143 `background:'var(--bg2)'` | `bg-surface-raised` |
| line 1144 `borderRadius: isDesktop ? 16 : '20px 20px 0 0'` | mobile `rounded-t-[20px]` / desktop `rounded-2xl` |
| line 1145 `padding: isDesktop ? '24px 28px 28px' : '20px 16px 40px'` | mobile `px-4 pt-5 pb-10` / desktop `px-7 pt-6 pb-7` |
| line 1146 `maxHeight:'90dvh', overflowY:'auto'` | `max-h-[90dvh] overflow-y-auto` (W4 OQ #1 LOCKED a) |
| line 1147 `width: 480, maxWidth: '90vw'` | desktop `w-[480px] max-w-[90vw]` |
| line 1149 wrapper `display:'flex', alignItems:'center', justifyContent:'space-between', marginBottom:18` | `className="flex items-center justify-between mb-[18px]"` |
| line 1150 `fontSize:15, fontWeight:700, color:'var(--t1)'` ('일정 수정') | `className="text-title font-semibold text-text-primary"` (노안 15→18) |
| line 1152 close X `width:28, height:28, borderRadius:7, border:'1px solid var(--bd)', background:'var(--bg3)'` | `className="w-7 h-7 rounded-sm border border-border-default bg-surface-sunken cursor-pointer flex items-center justify-center"` (28→32, w-8 함정 회피) |
| line 1153~1155 SVG 4 path | `<X size={16} className="text-text-secondary" strokeWidth={2} />` (lucide, SW5 mirror) |
| **신규** — 카테고리 lock row (line 1158 직후, 본문 첫 element) | **W5 OQ #1 b verbatim** — see below 변환 코드 |
| line 1158 본문 wrapper `display:'flex', flexDirection:'column', gap:16` | `className="flex flex-col gap-4"` |
| line 1160 `<label style={lbl}>제목</label>` | 유지 (helper 옵션 A LOCKED) |
| line 1161 `<input value={title} onChange={...} style={inp} />` | `<input value={title} onChange={e => { setTitle(e.target.value); setTitleError(false) }} style={{...inp, ...(titleError ? {borderColor:'var(--status-danger-bar)'} : {})}} />` |
| **신규** — empty title 에러 라벨 (line 1161 직후) | **W5 OQ #2 b verbatim** — see below 변환 코드 |
| line 1167 `style={{ ...inp, display:'block', WebkitAppearance:'none', height:44 }}` | 유지 (helper 옵션 A LOCKED) |
| line 1170, 1176 `<span style={{ fontWeight:400, color:'var(--t3)' }}>(선택)</span>` | `<span style={{ fontWeight:400, color:'var(--text-tertiary)' }}>(선택)</span>` 또는 inline className `text-text-tertiary font-normal` |
| line 1178 `style={{ ...inp, resize:'none', lineHeight:1.6 }}` | 유지 (helper 옵션 A LOCKED) |
| line 1180~1186 저장 버튼 `background:'linear-gradient(135deg,#1d4ed8,#2563eb)', color:'#fff', fontSize:14, fontWeight:700` | `className="bg-accent text-text-on-accent text-body font-bold rounded-xl border-0 cursor-pointer"` + `style={{padding:'14px', opacity: saving?0.6:1}}` (W4 OQ #2 LOCKED b, 14→16) |

## 신규 추가: 카테고리 lock row (line 1158 본문 wrapper 의 첫 element)

```tsx
{(() => {
  const cat = SCHED_CATEGORIES.find(c => c.value === item.category)
  return (
    <div className="flex items-center gap-2 p-3 bg-surface-sunken rounded-sm">
      <span className="w-2.5 h-2.5 rounded-full shrink-0" style={{ background: cat?.color }} />
      <span className="text-label font-bold text-text-primary leading-snug">{cat?.label}</span>
      <span className="text-caption text-text-tertiary leading-snug">· 카테고리는 변경할 수 없습니다</span>
    </div>
  )
})()}
```

> Note: `w-2.5 h-2.5` = 10px (Tailwind 기본 spacing scale, NOT overridden). `p-3` = 12px. `gap-2` = 8px. dot 색은 §10 인라인 dynamic (5 cat). `leading-snug` ≈ 1.375 (sketch `line-height:1.4` 근사) — `leading-tight` (1.25) 보다 `leading-snug` 가 더 가깝다.

## 신규 추가: empty title 에러 처리

**state 추가** (line 1123 직후):
```tsx
const [titleError, setTitleError] = useState(false)
```

**handleSave 변경** (line 1126):
```tsx
if (!title.trim()) { setTitleError(true); toast.error('제목을 입력하세요'); return }
```

**input onChange 변경** (line 1161):
```tsx
<input value={title}
  onChange={e => { setTitle(e.target.value); setTitleError(false) }}
  style={{...inp, ...(titleError ? {borderColor:'var(--status-danger-bar)'} : {})}} />
```

**에러 라벨 신규** (input 직후, sketch W5 line 604 verbatim):
```tsx
{titleError && (
  <div className="text-caption text-danger-bar mt-1.5 leading-none font-semibold">제목을 입력하세요</div>
)}
```

> 메모리 룰: `text-danger-bar` (status- prefix 없음 — feedback_tailwind_token_class_pattern.md 강제). `leading-none` (sketch `line-height:1` verbatim — feedback_text_caption_leading_none.md).

</transform_map>

<negative_scope>

## NEGATIVE 변경 금지 (1 byte 도 손대지 마라)

1. **EditModal 함수 시그니처** (line 1116~1118):
   ```tsx
   function EditModal({ item, onClose, onSaved, isDesktop }: {
     item: ScheduleItem; onClose: () => void; onSaved: () => void; isDesktop?: boolean
   })
   ```

2. **handleSave 안 scheduleApi.update 시그니처** (line 1129):
   ```ts
   await scheduleApi.update(item.id, { title: title.trim(), date, time: time || undefined, memo: memo || undefined })
   ```

3. **toast 카피 2건** (line 1126, 1132):
   - `toast.error('제목을 입력하세요')` ← W5 OQ #2 b 신규 분기에서도 동일 카피 verbatim
   - `toast.error('수정 실패')`

4. **onSaved() 호출** (line 1130) — 외부 modalsEl 의 '수정됐습니다' toast 흐름

5. **setSaving / setSaving(true) / setSaving(false) 흐름** (line 1123, 1127, 1134)

6. **item.category lock 로직** — 카테고리 변경 UI 추가 금지 (lock row 는 표시 전용)

7. **state 변수명** (`title`, `date`, `time`, `memo`, `saving`, `titleError`) — 위 이름 그대로

8. **SCHED_CATEGORIES 5 hex 잔존** (line 82~88) — `inspect:#3b82f6` / `task:#eab308` / `event:#e2e8f0` / `elevator:#f97316` / `fire:#ef4444`

9. **helper lbl / inp 정의** (line 1196~1204) — 옵션 A LOCKED inherit from SW5 (삭제 X)

</negative_scope>

<memory_rules>

## 메모리 룰 inline 인용 (5건)

1. **`feedback_tailwind_token_class_pattern.md`** (status- prefix 없음):
   - 정확 패턴: `border-danger-bar` (NOT `border-status-danger-bar`), `text-danger-bar` (NOT `text-status-danger`)
   - tailwind.config.js 별칭 키 확인 필수
   - SW3 학습 inherit

2. **`feedback_tsx_wave_emoji_dot_gap.md`** (이모지 0 + dot 보존):
   - close X SVG 4 path → lucide X (size 16) 1 컴포넌트
   - 카테고리 lock row dot span markup 보존 (`<span ... rounded-full>`)
   - 이모지 사용 시 verify gate FAIL

3. **`feedback_planner_prompt_sketch_verbatim.md`** (sketch CSS verbatim):
   - sketch W5 line 530~535 (lock row), line 599~605 (empty title 에러) CSS 그대로 인용 → executor 추측 방지
   - 토큰명/사이즈 추측 X — verbatim mapping 만

4. **`feedback_tailwind_w8_h8_is_48px.md`** (w-8 = 48px 함정):
   - tailwind.config spacing override: `w-8` = **48px** (NOT 32). `w-7` = **32px** (실제 32).
   - close X 정확 32×32 = `w-7 h-7` (NOT `w-8 h-8`)
   - 11-div TSX v3 hotfix(4ce707e) 사고 방지

5. **`feedback_text_caption_leading_none.md`** (작은 영역 leading-none 명시):
   - empty title 에러 라벨: sketch `line-height:1` verbatim → `leading-none` 명시
   - 카테고리 lock row 라벨: sketch `line-height:1.4` → `leading-snug` (1.375) 명시
   - `text-caption` (lh:1.5, 18px) 가 작은 컨테이너 안에서 시각적 패딩 효과 — 명시 필수

</memory_rules>

<worktree_rules>

## CLAUDE.local.md (cbc7119-design 워크트리 전용 룰)

- **wrangler 명령 금지** (`.claude/settings.local.json` deny)
- **`npm run deploy` 금지** (직원 도메인 경로)
- **운영 PWA hotfix 시도 금지** — 20260328 워크트리에서만
- 작업 위치: `/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/src/pages/SchedulePage.tsx`
- 브랜치: `redesign/13-schedule` (이미 체크아웃됨, recent commits 확인 — b8a706b SW2, 350d1ca SW1)
- main 머지 / push 사용자 컨펌 후 → cbc7119-preview.pages.dev 자동 배포

</worktree_rules>

<tasks>

<task type="auto">
  <name>Task 1: EditModal 변환 + lock row + empty title 에러 + linear-gradient 폐기 + W7 §4 verify gate 23 PASS</name>
  <files>cha-bio-safety/src/pages/SchedulePage.tsx</files>
  <action>
**Step 1 — 현재 EditModal 영역 확인:**
1. Read SchedulePage.tsx line 1115~1191 (현재 76 lines)
2. Read line 82~88 (SCHED_CATEGORIES 5 hex 정의 확인)
3. Read line 1196~1204 (helper lbl/inp 잔존 확인)
4. 현재 import 확인: `Grep -n "lucide-react" cha-bio-safety/src/pages/SchedulePage.tsx` — SW5 후 import = `ChevronLeft, ChevronRight, Download, Plus, X` 잔존 확인

**Step 2 — sketch W5 verbatim CSS 재확인:**
1. Read cha-bio-safety/docs/redesign-context/13-schedule/sketch-wave-5.html line 520~561 (frame A — lock row)
2. Read line 584~625 (frame B — empty title error)
3. 인용 토큰 매핑 확인:
   - sketch `var(--danger)` → v0.1.1 = `var(--status-danger-bar)` (border용) / Tailwind class = `border-danger-bar`, `text-danger-bar`
   - sketch `var(--surface-sunken)` / `var(--text-primary)` / `var(--text-tertiary)` — v0.1.1 그대로

**Step 3 — EditModal 변환 (line 1116~1191):**

3.1. **state 추가** (line 1123 직후, `[saving, setSaving]` 다음):
```tsx
const [titleError, setTitleError] = useState(false)
```

3.2. **handleSave 변경** (line 1126):
변경 전:
```tsx
if (!title.trim()) { toast.error('제목을 입력하세요'); return }
```
변경 후:
```tsx
if (!title.trim()) { setTitleError(true); toast.error('제목을 입력하세요'); return }
```
> NEGATIVE 보존: toast 카피 `'제목을 입력하세요'` 완전 일치, setSaving / scheduleApi.update / catch 시그니처 / `'수정 실패'` toast 1 byte 변경 금지

3.3. **overlay div** (line 1139~1140):
변경 전:
```tsx
<div style={{ position:'fixed', inset:0, background:'rgba(0,0,0,0.55)', zIndex:300, display:'flex', flexDirection:'column', justifyContent: isDesktop ? 'center' : 'flex-end', alignItems: isDesktop ? 'center' : undefined }}
  onClick={onClose}>
```
변경 후:
```tsx
<div
  className={`fixed inset-0 z-[300] flex flex-col ${isDesktop ? 'justify-center items-center' : 'justify-end'}`}
  style={{ background:'rgba(0,0,0,0.55)' }}
  onClick={onClose}>
```
> §10 화이트리스트: overlay rgba 인라인 잔존 OK

3.4. **모달 본체** (line 1141~1148):
변경 전:
```tsx
<div onClick={e => e.stopPropagation()}
  style={{
    background:'var(--bg2)',
    borderRadius: isDesktop ? 16 : '20px 20px 0 0',
    padding: isDesktop ? '24px 28px 28px' : '20px 16px 40px',
    maxHeight:'90dvh', overflowY:'auto',
    ...(isDesktop ? { width: 480, maxWidth: '90vw' } : {}),
  }}>
```
변경 후:
```tsx
<div onClick={e => e.stopPropagation()}
  className={`bg-surface-raised max-h-[90dvh] overflow-y-auto ${isDesktop ? 'rounded-2xl px-7 pt-6 pb-7 w-[480px] max-w-[90vw]' : 'rounded-t-[20px] px-4 pt-5 pb-10'}`}>
```

3.5. **헤더 row** (line 1149~1157):
변경 전:
```tsx
<div style={{ display:'flex', alignItems:'center', justifyContent:'space-between', marginBottom:18 }}>
  <span style={{ fontSize:15, fontWeight:700, color:'var(--t1)' }}>일정 수정</span>
  <button onClick={onClose}
    style={{ width:28, height:28, borderRadius:7, border:'1px solid var(--bd)', background:'var(--bg3)', cursor:'pointer', display:'flex', alignItems:'center', justifyContent:'center' }}>
    <svg width={14} height={14} fill="none" viewBox="0 0 24 24" stroke="var(--t2)" strokeWidth={2}>
      <path strokeLinecap="round" strokeLinejoin="round" d="M6 18L18 6M6 6l12 12"/>
    </svg>
  </button>
</div>
```
변경 후:
```tsx
<div className="flex items-center justify-between mb-[18px]">
  <span className="text-title font-semibold text-text-primary">일정 수정</span>
  <button onClick={onClose}
    className="w-7 h-7 rounded-sm border border-border-default bg-surface-sunken cursor-pointer flex items-center justify-center">
    <X size={16} className="text-text-secondary" strokeWidth={2} />
  </button>
</div>
```
> 메모리 룰 #4: w-7 = 32px (NOT w-8 = 48px). 노안 15→18 = `text-title`.

3.6. **본문 wrapper + 카테고리 lock row 신규** (line 1158~1160 사이):
변경 전:
```tsx
<div style={{ display:'flex', flexDirection:'column', gap:16 }}>
  <div>
    <label style={lbl}>제목</label>
```
변경 후:
```tsx
<div className="flex flex-col gap-4">
  {(() => {
    const cat = SCHED_CATEGORIES.find(c => c.value === item.category)
    return (
      <div className="flex items-center gap-2 p-3 bg-surface-sunken rounded-sm">
        <span className="w-2.5 h-2.5 rounded-full shrink-0" style={{ background: cat?.color }} />
        <span className="text-label font-bold text-text-primary leading-snug">{cat?.label}</span>
        <span className="text-caption text-text-tertiary leading-snug">· 카테고리는 변경할 수 없습니다</span>
      </div>
    )
  })()}
  <div>
    <label style={lbl}>제목</label>
```
> §10 화이트리스트: dot 색 dynamic `${cat?.color}` (5 cat hex)

3.7. **title input + 에러 라벨 신규** (line 1161):
변경 전:
```tsx
<input value={title} onChange={e => setTitle(e.target.value)} style={inp} />
```
변경 후:
```tsx
<input
  value={title}
  onChange={e => { setTitle(e.target.value); setTitleError(false) }}
  style={{ ...inp, ...(titleError ? { borderColor: 'var(--status-danger-bar)' } : {}) }} />
{titleError && (
  <div className="text-caption text-danger-bar mt-1.5 leading-none font-semibold">제목을 입력하세요</div>
)}
```
> 메모리 룰 #1: `text-danger-bar` (status- prefix 없음). 메모리 룰 #5: `leading-none` (sketch `line-height:1` verbatim).

3.8. **(선택) span 색** (line 1170, 1176):
변경 전:
```tsx
<span style={{ fontWeight:400, color:'var(--t3)' }}>(선택)</span>
```
변경 후 (둘 중 택1):
- inline 유지: `<span style={{ fontWeight:400, color:'var(--text-tertiary)' }}>(선택)</span>`
- className: `<span className="font-normal text-text-tertiary">(선택)</span>`

3.9. **저장 버튼** (line 1180~1186):
변경 전:
```tsx
<button onClick={handleSave} disabled={saving}
  style={{ padding:'14px', borderRadius:12, border:'none',
    background:'linear-gradient(135deg,#1d4ed8,#2563eb)',
    color:'#fff', fontSize:14, fontWeight:700, cursor:'pointer',
    opacity:saving?0.6:1 }}>
  {saving ? '저장 중...' : '수정 저장'}
</button>
```
변경 후:
```tsx
<button onClick={handleSave} disabled={saving}
  className="bg-accent text-text-on-accent text-body font-bold rounded-xl border-0 cursor-pointer"
  style={{ padding:'14px', opacity: saving ? 0.6 : 1 }}>
  {saving ? '저장 중...' : '수정 저장'}
</button>
```
> W4 OQ #2 LOCKED b: linear-gradient 폐기. 노안 14→16 = `text-body`. rounded-xl = 12px. text-text-on-accent = white (또는 text-white 무방).

**Step 4 — date/time/memo input/textarea 잔존 helper 검증:**

date input (line 1166~1167):
```tsx
<input type="date" value={date} onChange={e => setDate(e.target.value)}
  style={{ ...inp, display:'block', WebkitAppearance:'none', height:44 }} />
```
→ 유지 (helper 옵션 A LOCKED). inp 스타일은 이미 v0.1.1 토큰(surface-sunken, border-strong, text-primary).

time input (line 1171~1172) / memo textarea (line 1177~1178) → 동일 유지.

**Step 5 — lucide-react import 확인:**
- 현재 SW5 후 import: `import { ChevronLeft, ChevronRight, Download, Plus, X } from 'lucide-react'`
- SW6 추가 import = 없음 (X 이미 import됨)
- CheckCircle2 / AlertCircle 도입 안 함 (react-hot-toast default 처리, 미사용 import 회피)
- **DEVIATION 박제 (SUMMARY)**: W7 §4.2 #17 lucide 6 → 5 완화 (실제 사용 5: Download, Plus, ChevronLeft, ChevronRight, X). 미사용 import 회피 룰 + tree-shaking.

**Step 6 — Build & Verify gate 23개 실행:**

6.1 **Build gates (§4.3):**
```bash
cd /Users/jykevin/Documents/cbc7119-design/cha-bio-safety && npx tsc --noEmit
cd /Users/jykevin/Documents/cbc7119-design/cha-bio-safety && npm run build
```
둘 다 exit 0 요구.

6.2 **Negative gates (§4.1) — 모두 0 hits 요구:**
```bash
# 1. 이모지 0
grep -nP '[\x{1F300}-\x{1FAFF}]|[\x{2600}-\x{27BF}]' cha-bio-safety/src/pages/SchedulePage.tsx
# 2. 9·10·11px 0 (MonthlyPlanPreview 12px 격상 완료 검증)
grep -nE 'fontSize:\s*(9|10|11)([^0-9]|$)' cha-bio-safety/src/pages/SchedulePage.tsx
# 3. status- prefix 잘못된 패턴 0
grep -nE '(text|bg|border)-status-' cha-bio-safety/src/pages/SchedulePage.tsx
# 4. linear-gradient 0
grep -n 'linear-gradient' cha-bio-safety/src/pages/SchedulePage.tsx
# 5. 옛 alias var() 0 (--bg/bg2/bg3/bd/bd2/t1/t2/t3/acl/c-day/c-night/c-off/c-leave/safe/warn/danger/info/fire)
grep -nE 'var\(--(bg2?|bg3|bd2?|t[123]|acl|c-(day|night|off|leave)|safe|warn|danger|info|fire)\)' cha-bio-safety/src/pages/SchedulePage.tsx | grep -v 'surface\|text-\|border\|status-\|accent'
# 6. 본문 "오늘" 텍스트 0 (HTML 텍스트만)
grep -nE '>오늘<|>오늘\s*</' cha-bio-safety/src/pages/SchedulePage.tsx
# 7. 동적 분기 외 raw category hex 0
grep -nE "#(3b82f6|eab308|e2e8f0|f97316|ef4444)" cha-bio-safety/src/pages/SchedulePage.tsx
# (SCHED_CATEGORIES line 83~87 = 5 hits, MonthlyPlanPreview 안 동적 분기 외 잔존 0 확인)
# 8. w-8 h-8 confusion 0
grep -nE '<(button|div)[^>]*className="[^"]*\bw-8\s+h-8\b' cha-bio-safety/src/pages/SchedulePage.tsx
```

6.3 **Positive gates (§4.2) — 각 ≥ threshold hits:**
```bash
# 9. surface 토큰 ≥10
grep -cE '(bg|border|text)-surface-' cha-bio-safety/src/pages/SchedulePage.tsx
# 10. text-text-* ≥30
grep -cE 'text-text-(primary|secondary|tertiary|on-accent)' cha-bio-safety/src/pages/SchedulePage.tsx
# 11. border-border-* ≥10
grep -cE 'border-border-' cha-bio-safety/src/pages/SchedulePage.tsx
# 12. accent 토큰 ≥3
grep -cE '(bg|text|border)-accent' cha-bio-safety/src/pages/SchedulePage.tsx
# 13. safe-bar ≥2 (저장→완료 칩 + 완료 버튼)
grep -cE '\bsafe-bar\b' cha-bio-safety/src/pages/SchedulePage.tsx
# 14. status 4종 ≥3 (safe|warning|danger|info)
grep -cE '\b(safe|warning|danger|info)-bar\b' cha-bio-safety/src/pages/SchedulePage.tsx
# 15. typography ≥30 (text-display|title|heading|body|label|caption)
grep -cE 'text-(display|title|heading|body|label|caption)' cha-bio-safety/src/pages/SchedulePage.tsx
# 16. leading-* ≥5
grep -cE 'leading-(none|tight|snug|normal|relaxed)' cha-bio-safety/src/pages/SchedulePage.tsx
# 17. lucide-react import 사용 (5개 — DEVIATION)
grep -nE "from 'lucide-react'" cha-bio-safety/src/pages/SchedulePage.tsx
grep -nE '<(Download|Plus|ChevronLeft|ChevronRight|X)\s' cha-bio-safety/src/pages/SchedulePage.tsx
# 18. SCHED_CATEGORIES 5 hex 정의 잔존
grep -nE "color:\s*'#(3b82f6|eab308|e2e8f0|f97316|ef4444)'" cha-bio-safety/src/pages/SchedulePage.tsx
```

6.4 **비즈 보존 verify (§4.4):**
```bash
# 21. useQuery 2 keys + scheduleApi 5 호출
grep -nE "useQuery|queryKey:\s*\['(schedule|holidays)" cha-bio-safety/src/pages/SchedulePage.tsx
grep -nE "scheduleApi\.(getByMonth|create|update|updateStatus|delete)" cha-bio-safety/src/pages/SchedulePage.tsx
# 22. toast 13 hits (변환 전 13 = 변환 후 13)
grep -cE "toast\.(success|error)" cha-bio-safety/src/pages/SchedulePage.tsx
# 23. 비즈 함수 시그니처 diff = 0
grep -nE "function (matchesDate|catInfo|handlePlanDownload|generateMonthlyPlan|fetchHolidays|handleStatus|handleDelete)|const dayCatMap" cha-bio-safety/src/pages/SchedulePage.tsx
```

**Step 7 — SUMMARY 작성:**

`.planning/quick/260520-4l5-redesign-13-schedule-tsx-sw6-editmodal-2/260520-4l5-SUMMARY.md` 생성. 다음 섹션 포함:

1. **변경 사항** — line 변화 (+15 ~ +30), 신규 추가 2건 (lock row + empty title 에러), 폐기 1건 (linear-gradient).
2. **W7 §4 verify gate 23 표** — 23 row × (gate ID / threshold / 실측 / PASS-FAIL).
3. **W5 LOCKED 결정 2건 박제** — OQ #1 b (lock row) + OQ #2 b (empty title 에러).
4. **DEVIATION 박제** — §4.2 #17 lucide 6 → 5 완화 (CheckCircle2/AlertCircle 도입 안 함, react-hot-toast default 처리).
5. **NEGATIVE 보존 verify** — handleSave / scheduleApi.update / toast 카피 2건 / onSaved / setSaving diff = 0.
6. **다음 단계** — 사용자 컨펌 → main 머지 → cbc7119-preview.pages.dev 자동 배포 → 전체 wave 8 완료.

**Step 8 — 사용자 컨펌 대기 (commit 자동, push 대기):**
- 자동 commit message: `tsx(13-schedule): SW6 (마지막) — EditModal + 카테고리 lock + empty title 에러 + linear-gradient 폐기`
- main 머지 / push 는 사용자 명시 컨펌 후 (메모리 룰 feedback_deploy_test.md — redesign 트랙은 컨펌 후)
  </action>
  <verify>
    <automated>cd /Users/jykevin/Documents/cbc7119-design/cha-bio-safety && npx tsc --noEmit && npm run build</automated>

W7 §4 verify gate 23개 모두 PASS — SUMMARY 안 23 row 표 형식으로 명시.

**Negative gates (8개, 모두 0 hits 요구):**
```bash
cd /Users/jykevin/Documents/cbc7119-design
grep -cP '[\x{1F300}-\x{1FAFF}]|[\x{2600}-\x{27BF}]' cha-bio-safety/src/pages/SchedulePage.tsx
grep -cE 'fontSize:\s*(9|10|11)([^0-9]|$)' cha-bio-safety/src/pages/SchedulePage.tsx
grep -cE '(text|bg|border)-status-' cha-bio-safety/src/pages/SchedulePage.tsx
grep -c 'linear-gradient' cha-bio-safety/src/pages/SchedulePage.tsx
grep -cE 'var\(--(bg2?|bg3|bd2?|t[123]|acl|c-(day|night|off|leave)|safe|warn|danger|info|fire)\)' cha-bio-safety/src/pages/SchedulePage.tsx
grep -cE '>오늘<|>오늘\s*</' cha-bio-safety/src/pages/SchedulePage.tsx
grep -cE '<(button|div)[^>]*className="[^"]*\bw-8\s+h-8\b' cha-bio-safety/src/pages/SchedulePage.tsx
```
모두 = 0 → PASS

**Positive gates (10개, 각 ≥ threshold):**
```bash
grep -cE '(bg|border|text)-surface-' cha-bio-safety/src/pages/SchedulePage.tsx       # ≥10
grep -cE 'text-text-(primary|secondary|tertiary|on-accent)' cha-bio-safety/src/pages/SchedulePage.tsx  # ≥30
grep -cE 'border-border-' cha-bio-safety/src/pages/SchedulePage.tsx                  # ≥10
grep -cE '(bg|text|border)-accent' cha-bio-safety/src/pages/SchedulePage.tsx         # ≥3
grep -cE '\bsafe-bar\b' cha-bio-safety/src/pages/SchedulePage.tsx                    # ≥2
grep -cE '\b(safe|warning|danger|info)-bar\b' cha-bio-safety/src/pages/SchedulePage.tsx  # ≥3
grep -cE 'text-(display|title|heading|body|label|caption)' cha-bio-safety/src/pages/SchedulePage.tsx  # ≥30
grep -cE 'leading-(none|tight|snug|normal|relaxed)' cha-bio-safety/src/pages/SchedulePage.tsx  # ≥5
grep -nE "from 'lucide-react'" cha-bio-safety/src/pages/SchedulePage.tsx              # ≥1 (5종 사용 — DEVIATION)
grep -cE "color:\s*'#(3b82f6|eab308|e2e8f0|f97316|ef4444)'" cha-bio-safety/src/pages/SchedulePage.tsx  # =5
```

**비즈 보존 (3개):**
```bash
grep -cE "scheduleApi\.(getByMonth|create|update|updateStatus|delete)" cha-bio-safety/src/pages/SchedulePage.tsx  # ≥5
grep -cE "toast\.(success|error)" cha-bio-safety/src/pages/SchedulePage.tsx          # =13 (변환 전 13 = 변환 후 13)
git diff cha-bio-safety/src/pages/SchedulePage.tsx | grep -E '^\-\s*(scheduleApi\.update\(item\.id|toast\.error\(.제목을 입력하세요|toast\.error\(.수정 실패|onSaved\(\)|setSaving)'  # 변경 라인 0 (NEGATIVE 보존)
```
  </verify>
  <done>
- EditModal 변환 완료 (line 1116~1191 → ~+15~30 line 증가)
- 카테고리 lock row 신규 추가 (sketch W5 line 530~535 verbatim, dot 색 dynamic 5 cat)
- empty title 에러 처리 신규 추가 (useState titleError + input border 분기 + 에러 라벨)
- 저장 버튼 bg-accent solid (linear-gradient 폐기)
- close X 32×32 (w-7 h-7) + lucide X size 16
- 노안 격상: 헤더 text-title / 저장 text-body
- helper (lbl/inp) 잔존 — 옵션 A LOCKED inherit
- W7 §4 verify gate 23개 모두 PASS — SUMMARY 안 23 row 표
- DEVIATION 박제: lucide §4.2 #17 6 → 5 완화 (CheckCircle2/AlertCircle 미도입)
- NEGATIVE 보존 100%: handleSave / scheduleApi.update / toast 카피 / onSaved / setSaving 1 byte 변경 0
- npx tsc --noEmit exit 0 + npm run build exit 0
- 자동 commit (push 사용자 컨펌 대기)
  </done>
</task>

</tasks>

<verification>

## 전체 phase 검증 (SW6 완료 시 = 전체 wave 8 TSX 변환 완료)

1. **Build PASS**: `cd cha-bio-safety && npx tsc --noEmit && npm run build` exit 0
2. **W7 §4 verify gate 23 PASS** — 위 task <verify> 의 23 grep 명령 결과를 SUMMARY 안 표로 기록
3. **NEGATIVE 보존 100%** — `git diff` 검증:
   - handleSave 시그니처 변경 = 0
   - scheduleApi.update 호출 시그니처 변경 = 0
   - toast 카피 2건 ('제목을 입력하세요' / '수정 실패') 변경 = 0
   - onSaved() / setSaving 흐름 변경 = 0
4. **신규 추가 verify**:
   - `grep -c 'SCHED_CATEGORIES.find(c => c.value === item.category)' cha-bio-safety/src/pages/SchedulePage.tsx` ≥ 1 (lock row)
   - `grep -c 'titleError' cha-bio-safety/src/pages/SchedulePage.tsx` ≥ 4 (useState + setter 2 + 조건부 렌더 + border 분기)
5. **DEVIATION 박제**: SUMMARY 안 §4.2 #17 lucide 6 → 5 명시 (CheckCircle2/AlertCircle 미도입 + react-hot-toast default 처리 근거)

</verification>

<success_criteria>

- [ ] EditModal (line 1116~1191) v0.1.1 토큰 + Tailwind 클래스 변환 완료
- [ ] 카테고리 lock row 신규 추가 (sketch W5 line 530~535 verbatim 매핑)
- [ ] empty title 에러 처리 신규 추가 (state + handleSave + onChange + 에러 라벨)
- [ ] 저장 버튼 bg-accent solid (linear-gradient 폐기 — grep 0 hits)
- [ ] close X 32×32 (`w-7 h-7`) + lucide X size 16 — w-8 함정 회피
- [ ] 노안 격상 (헤더 text-title / 저장 text-body)
- [ ] helper lbl/inp 잔존 (옵션 A LOCKED inherit)
- [ ] W7 §4 verify gate 23 PASS — SUMMARY 23 row 표
- [ ] DEVIATION 박제: §4.2 #17 lucide 6 → 5 완화
- [ ] NEGATIVE 보존 100% (handleSave / scheduleApi.update / toast 2건 / onSaved / setSaving)
- [ ] Build PASS (tsc exit 0 + build exit 0)
- [ ] 자동 commit (`tsx(13-schedule): SW6 (마지막) — EditModal + 카테고리 lock + empty title 에러 + linear-gradient 폐기`)
- [ ] push / main 머지 사용자 컨펌 대기 (CLAUDE.local.md + memory rule feedback_deploy_test.md)
- [ ] SUMMARY 작성: `.planning/quick/260520-4l5-redesign-13-schedule-tsx-sw6-editmodal-2/260520-4l5-SUMMARY.md`

</success_criteria>

<output>
After completion, create `.planning/quick/260520-4l5-redesign-13-schedule-tsx-sw6-editmodal-2/260520-4l5-SUMMARY.md`.

SUMMARY 필수 섹션:
1. **변경 사항** — EditModal line 변화, 신규 추가 2건, 폐기 1건
2. **W7 §4 verify gate 23 표** — gate ID / threshold / 실측 / PASS-FAIL (23 row)
3. **W5 LOCKED 결정 2건 박제** — OQ #1 b (lock row) + OQ #2 b (empty title 에러)
4. **DEVIATION 박제** — §4.2 #17 lucide 6→5 완화 (CheckCircle2/AlertCircle 미도입, react-hot-toast default 처리 근거)
5. **NEGATIVE 보존 verify** — handleSave / scheduleApi.update / toast 카피 2건 / onSaved / setSaving diff = 0
6. **executor 학습 inherit** — SW3 status- prefix / SW4 모달 chrome / SW5 close X w-7
7. **다음 단계** — 사용자 컨펌 → main 머지 → cbc7119-preview.pages.dev 자동 배포 → **전체 wave 8 TSX 변환 완료**
</output>
