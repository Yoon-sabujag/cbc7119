---
phase: 260522-4i9
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cha-bio-safety/src/pages/SplashScreen.tsx
  - cha-bio-safety/src/components/InstallPrompt.tsx
autonomous: true
requirements:
  - W5-checklist-§1-scope-2files
  - W5-checklist-§2-biz-anchor-16
  - W5-checklist-§3-region-mapping-6
  - W5-checklist-§4-OQ-LOCKED-5
  - W5-checklist-§5-negative-gate-8
  - W5-checklist-§6-positive-gate
  - W5-checklist-§7-build-gate
  - W5-checklist-§10-biz-checkbox
  - W5-checklist-biz-anchor-splash-8
  - W5-checklist-biz-anchor-install-8
tags:
  - redesign
  - 28-splash
  - tsx-conversion
  - v0.1.1
  - quick
  - 2-file-atomic
  - pwa-install
  - special-glyph-preserve

must_haves:
  truths:
    - "SplashScreen.tsx + InstallPrompt.tsx **2 파일 단일 atomic** in-place 수정. `git diff --name-only HEAD~ HEAD` 에 'cha-bio-safety/src/pages/SplashScreen.tsx' + 'cha-bio-safety/src/components/InstallPrompt.tsx' + 본 PLAN.md/SUMMARY.md 만 등장 (다른 파일 0). 27-login/16-workshift/17-annual-plan 은 단일 파일이었으나 28-splash 만 2 파일 atomic."
    - "pwaInstall.ts + versionCheck.ts + App.tsx + tailwind.config.js + tokens.css + functions/ + templates/ + migrations/ + public/ 모두 변경 0 byte."
    - "변환 후 SplashScreen.tsx 라인 수 83 ± 25 (원본 83, 변환 후 80~110 예상)."
    - "변환 후 InstallPrompt.tsx 라인 수 237 ± 50 (원본 237, 변환 후 220~290 예상)."
    - "★ SplashScreen 비즈 anchor 8건 1 byte 변경 0: __APP_VERSION__ Vite define (line 73) / @keyframes slideUp (line 79) / setInterval 48ms tick (line 18) / setTimeout 1600ms (line 26) / +4 increment per tick (line 18) / checkVersionAndRefresh() 호출 (line 16) / shouldShowInstallPrompt() 분기 (line 21) / isAuthenticated ? '/dashboard' : '/login' 분기 (line 24, 34) — 모두 verbatim 보존."
    - "★ InstallPrompt 비즈 anchor 8건 1 byte 변경 0: isStandalone() (line 5~10) / isIOS() (line 12~17) / 3 PWA imports getDeferredInstallPrompt/subscribeInstallPrompt/showInstallPrompt (line 2) / shouldShowInstallPrompt() export + 24h TTL Date.now()-ts<24*60*60*1000 (line 224~233) / dismissInstallPrompt() export + localStorage 'pwa-install-dismissed' (line 235~237) / outcome 3 분기 'accepted'/'unavailable'/'dismissed' (line 40~47) / 3 view state !showIOSGuide && !showAndroidGuide / showAndroidGuide / iOS (line 88, 112, 148) / setHasPrompt(!!p) useState + subscribeInstallPrompt useEffect (line 28, 32~35) — 모두 verbatim 보존."
    - "비즈 로직 0 diff (SplashScreen): imports 5개 (useState/useEffect from react / useNavigate / useAuthStore / InstallPrompt+shouldShow+dismiss / checkVersionAndRefresh) line 1~5 그대로."
    - "비즈 로직 0 diff (InstallPrompt): imports 2개 (useState/useEffect from react / 3 PWA helpers) line 1~2 그대로."
    - "비즈 로직 0 diff (SplashScreen): useState 2종 (pct=0 / showInstall=false) line 8~9 + useNavigate + useAuthStore line 10~11 + useEffect (tick + nav cleanup) line 13~29 + handleDismiss line 31~35 그대로."
    - "비즈 로직 0 diff (InstallPrompt): useState 3종 (hasPrompt=!!getDeferredInstallPrompt() / showIOSGuide=false / showAndroidGuide=false) line 28~30 + useEffect (subscribeInstallPrompt cleanup) line 32~35 + handleInstallAndroid async + handleInstallIOS + ios=isIOS() line 37~55 그대로."
    - "카피 verbatim 보존 (SplashScreen): 'CBC 방재' / '소방안전 통합관리 시스템' / '시스템 초기화 중...' / '데이터 불러오는 중...' / '준비 완료' / 'v{__APP_VERSION__} · 경기도 성남시 분당구'."
    - "카피 verbatim 보존 (InstallPrompt): 'CBC 방재' / '홈 화면에 앱을 설치하면<br/>더 빠르고 편리하게 사용할 수 있습니다' / '설치 방법 보기' / '홈 화면에 설치' / '나중에 할게요' / '자동 설치 창이 뜨지 않으면 아래 순서로 설치해 주세요.' / '크롬 우상단 ⋮ 메뉴' / '앱 설치' / '홈 화면에 추가' / '설치' / '하단 공유 버튼 터치' / 'Safari 하단의 ⎋ 공유 아이콘을 누르세요' / '추가' / '확인했습니다' / 3 step 본문 카피."
    - "OQ #1 LOCKED 적용 (InstallPrompt 설치 버튼): `bg-safe-bar text-text-on-accent` solid. `linear-gradient(135deg, #2563eb, #0ea5e9)` 완전 폐기 (grep gate 0건 강제)."
    - "OQ #2 LOCKED 적용 (SplashScreen 진행 바): fill = `bg-accent` solid. `linear-gradient(90deg,#3b82f6,#0ea5e9)` 완전 폐기 (grep gate 0건 강제). 트랙 = 인라인 `background:'rgba(255,255,255,0.07)'` 유지 (alpha 정밀도 인라인 4건 중 1)."
    - "OQ #3 LOCKED 적용 (폰트 격상 verbatim): SplashScreen — 22 → text-heading font-black tracking-tight / 12 부제 → text-caption leading-relaxed (유지) / 11 진행 라벨 → text-caption leading-none (11→12 격상) / 10 버전 → text-caption leading-none (10→12 격상). InstallPrompt — 18 → text-title font-extrabold / 12 카드 부제 → text-caption leading-relaxed (유지) / 15 설치 버튼 → text-body font-bold (15→16 격상) / 12 나중에 → text-caption font-bold leading-none (유지) / 12 노란 경고 → text-caption leading-relaxed (유지) / 13 숫자 원 → text-label font-extrabold leading-none / 13 step 제목 → text-label leading-none / 11 하위 설명 → text-caption leading-relaxed (11→12 격상) / 13 확인했습니다 → text-label font-bold leading-none. 인라인 fontSize:16 글리프 ⋮ ⎋ (OQ #5 예외) 유지."
    - "OQ #4 LOCKED 적용 (토큰 치환 + 인라인 유지 4건): 페이지 #161b22 → bg-surface-page (SplashScreen 외곽). 카드 #1c2128 → bg-surface-raised (InstallPrompt 카드). text #e6edf3 → text-text-primary / #8b949e → text-text-secondary / #6e7681 → text-text-tertiary / #3d444d → text-text-disabled. 노란 경고 #f59e0b + rgba(245,158,11,0.1) + rgba(245,158,11,0.25) → text-warning bg-warning-bg border-warning/25 (status- prefix 없음). 숫자 원 + 확인했습니다 rgba(59,130,246,0.15) + #3b82f6 → bg-accent/15 text-accent. **인라인 유지 4건** = SplashScreen 진행 바 트랙 rgba(255,255,255,0.07) + InstallPrompt 카드 border rgba(59,130,246,0.3) + InstallPrompt 오버레이 rgba(0,0,0,0.85) + InstallPrompt 카드 boxShadow 0 8px 40px rgba(0,0,0,0.5)."
    - "OQ #5 LOCKED 적용 (특수 글리프 인라인 보존): InstallPrompt line 122 `<span style={{ fontSize: 16 }}>⋮</span>` U+22EE (Android Chrome 메뉴) **1 byte 변경 0**. InstallPrompt line 164 `<span style={{ fontSize: 16, verticalAlign: 'middle' }}>⎋</span>` U+238B (iOS Safari 공유) **1 byte 변경 0, verticalAlign 'middle' 포함**. Lucide 교체 절대 금지. emoji negative grep range `[😀-🛿🤀-🧿]` 는 U+22EE / U+238B 매칭 X (Unicode 블록 외부 codepoint)."
    - "Negative gate 모두 PASS (2 파일 각각): 이모지 0 (⋮ ⎋ 는 emoji range 외부 OK) / linear-gradient 0 / fontSize 9·10·11 인라인 0 (fontSize:16 글리프 OQ #5 예외) / status- prefix 0 / w-8 h-8 0 / 옛 alias var(--bg|bg2|bg3|bd|bd2|t1|t2|t3|acl|accent) 0."
    - "Positive gate 모두 PASS: v0.1.1 토큰 className SplashScreen ≥3 / InstallPrompt ≥5 / bg-safe-bar ≥1 (InstallPrompt 설치 버튼, OQ #1) / bg-accent ≥1 (SplashScreen 진행 바, OQ #2) / bg-accent/15 ≥4 + text-accent ≥4 (숫자 원 6 + 확인했습니다 2) / text-warning ≥1 + bg-warning-bg ≥1 + border-warning ≥1 (Android 노란 경고, OQ #4) / text-text-primary/secondary/tertiary/disabled 다수 / ⋮ ≥1 (Android line 122) + ⎋ ≥1 (iOS line 164) / @keyframes slideUp ≥1 (SplashScreen 인라인) / __APP_VERSION__ ≥1 (SplashScreen 버전)."
    - "★ 비즈 anchor 16건 보존 grep gate PASS: SplashScreen 8건 (__APP_VERSION__ / @keyframes slideUp / setInterval 48ms / setTimeout 1600ms / +4 increment / checkVersionAndRefresh / shouldShowInstallPrompt / isAuthenticated) + InstallPrompt 8건 (isStandalone / isIOS / 3 PWA imports / shouldShowInstallPrompt export / dismissInstallPrompt export + 'pwa-install-dismissed' / outcome 3 분기 / 3 view state showIOSGuide+showAndroidGuide / 24*60*60*1000 TTL)."
    - "Build gate: `cd cha-bio-safety && npm run build` exit 0 (tsc --noEmit 0 errors + Vite build PASS). SplashScreen + InstallPrompt chunk size 보고."
    - "Atomic 1-commit (2 파일 묶음): `tsx(quick-260522-4i9): redesign/28-splash SplashScreen + InstallPrompt v0.1.1 토큰 변환 (2 파일 atomic + W2/W3/W4 sketch verbatim + OQ 5건 LOCKED + 비즈 anchor 16건 + ⋮ ⎋ 글리프 보존)`."
  artifacts:
    - path: cha-bio-safety/src/pages/SplashScreen.tsx
      provides: v0.1.1 토큰 className 적용 (외곽 / 로고 / 타이틀 / 진행 바 / 버전) + OQ #2 진행 바 bg-accent solid + OQ #3 폰트 격상 + OQ #4 토큰 치환 (페이지 + 텍스트) + 비즈 anchor 8건 (__APP_VERSION__/@keyframes slideUp/48ms/1600ms/+4/checkVersionAndRefresh/shouldShowInstallPrompt/isAuthenticated) 1 byte 변경 0
      contains: "bg-surface-page / bg-accent / text-heading / text-title / text-caption / text-text-primary / text-text-tertiary / text-text-disabled / font-black / tracking-tight / leading-none / leading-relaxed / @keyframes slideUp / __APP_VERSION__ / checkVersionAndRefresh / shouldShowInstallPrompt / setInterval / setTimeout / 1600 / p + 4 / isAuthenticated"
      min_lines: 78
    - path: cha-bio-safety/src/components/InstallPrompt.tsx
      provides: v0.1.1 토큰 className 적용 (오버레이 / 카드 / 아이콘 / 제목 / 설치 버튼 / 나중에 / Android 가이드 / iOS 가이드 / 확인했습니다) + OQ #1 설치 버튼 bg-safe-bar solid + OQ #3 폰트 격상 + OQ #4 토큰 치환 (카드 + 텍스트 + 노란 경고 + bg-accent/15) + OQ #5 ⋮ ⎋ 글리프 인라인 보존 + 비즈 anchor 8건 (isStandalone/isIOS/3 PWA imports/shouldShowInstallPrompt export/dismissInstallPrompt/outcome/3 view state) 1 byte 변경 0
      contains: "bg-surface-raised / bg-safe-bar / bg-warning-bg / bg-accent/15 / border-warning / border-warning/25 / text-text-on-accent / text-text-primary / text-text-secondary / text-text-tertiary / text-warning / text-accent / text-title / text-body / text-label / text-caption / font-extrabold / font-bold / leading-none / leading-relaxed / rounded-lg / rounded-md / rounded-sm / rounded-[20px] / ⋮ / ⎋ / isStandalone / isIOS / getDeferredInstallPrompt / subscribeInstallPrompt / showInstallPrompt / shouldShowInstallPrompt / dismissInstallPrompt / pwa-install-dismissed / 'accepted' / 'unavailable' / 'dismissed' / showIOSGuide / showAndroidGuide"
      min_lines: 220
  key_links:
    - from: "W5 §3.1 SplashScreen 외곽 + 로고 (line 38~60)"
      to: SplashScreen.tsx line 38~60
      via: "bg-surface-page + style minHeight 100dvh + 로고 박스 인라인 rgba(37,99,235,0.2) + rgba(59,130,246,0.3) 유지 + 로고 wrapper animation slideUp 인라인 anchor + 타이틀 text-heading font-black tracking-tight text-text-primary + 부제 text-caption leading-relaxed text-text-tertiary (letterSpacing .1em 인라인)"
      pattern: "bg-surface-page|text-heading.*font-black"
    - from: "W5 §3.2 SplashScreen 진행 바 + 하단 버전 (line 61~73)"
      to: SplashScreen.tsx line 61~73
      via: "진행 바 트랙 인라인 background rgba(255,255,255,0.07) 유지 (인라인 1/4) + 진행 바 fill bg-accent solid (OQ #2 LOCKED, linear-gradient 폐기) + 진행 라벨 text-caption leading-none text-text-tertiary (11→12 격상) + 버전 text-caption leading-none text-text-disabled (10→12 격상) + __APP_VERSION__ Vite define anchor + @keyframes slideUp 인라인 anchor"
      pattern: "bg-accent[^/-]|__APP_VERSION__"
    - from: "W5 §3.3 InstallPrompt 오버레이 + 카드 + 아이콘 + 제목 (line 57~84)"
      to: InstallPrompt.tsx line 57~84
      via: "오버레이 인라인 rgba(0,0,0,0.85) 유지 (인라인 2/4) + 카드 bg-surface-raised rounded-[20px] + 카드 border 인라인 rgba(59,130,246,0.3) 유지 (인라인 3/4) + 카드 boxShadow 인라인 0 8px 40px rgba(0,0,0,0.5) 유지 (인라인 4/4) + 아이콘 박스 rounded-lg + 제목 text-title font-extrabold text-text-primary (18) + 부제 text-caption leading-relaxed text-text-secondary (12, lineHeight 1.5)"
      pattern: "bg-surface-raised|rounded-\\[20px\\]"
    - from: "W5 §3.4 InstallPrompt 메인 view — 설치 + 나중에 (line 88~111)"
      to: InstallPrompt.tsx line 88~111
      via: "메인 view 분기 !showIOSGuide && !showAndroidGuide + 설치 버튼 bg-safe-bar text-text-on-accent text-body font-bold rounded-md (OQ #1 LOCKED solid, 15→16 격상, linear-gradient 폐기) + 나중에 버튼 text-caption font-bold leading-none text-text-tertiary rounded-md (background transparent + border rgba(255,255,255,0.1) 인라인 유지)"
      pattern: "bg-safe-bar.*text-text-on-accent"
    - from: "W5 §3.5 InstallPrompt Android 가이드 view (line 112~147)"
      to: InstallPrompt.tsx line 112~147
      via: "노란 경고 text-warning bg-warning-bg border-warning/25 rounded-sm (OQ #4 LOCKED 3 토큰, status- prefix 없음, 12 text-caption leading-relaxed) + 숫자 원 bg-accent/15 text-accent text-label font-extrabold leading-none rounded-sm (13 text-label, OQ #3/#4) + step 제목 text-label leading-none text-text-primary (13) + step 하위 설명 text-caption leading-relaxed text-text-secondary (11→12 격상) + Android ⋮ U+22EE `<span style={{ fontSize: 16 }}>⋮</span>` 인라인 1 byte 변경 0 (OQ #5) + 확인했습니다 bg-accent/15 text-accent text-label font-bold leading-none rounded-md (border 인라인 rgba(59,130,246,0.3) 유지)"
      pattern: "text-warning|bg-warning-bg|⋮"
    - from: "W5 §3.6 InstallPrompt iOS 가이드 view (line 148~215)"
      to: InstallPrompt.tsx line 148~215
      via: "iOS 가이드 view 분기 else (showIOSGuide) + 3-step 동일 토큰 (Android 와 동일 className/style 패턴) + iOS ⎋ U+238B `<span style={{ fontSize: 16, verticalAlign: 'middle' }}>⎋</span>` 인라인 1 byte 변경 0 (OQ #5, verticalAlign 'middle' 포함) + 확인했습니다 Android 와 동일 토큰"
      pattern: "⎋|verticalAlign.*middle"
---

<objective>
redesign/28-splash TSX 변환 — SplashScreen.tsx (83 lines) + InstallPrompt.tsx (237 lines) **2 파일 단일 atomic in-place 수정**.

W2 (SplashScreen) + W3 (InstallPrompt 메인) + W4 (InstallPrompt Android+iOS 가이드) 3 sketch + W5 12 섹션 checklist + W1 OQ 5건 LOCKED 결정을 1-commit 으로 적용. **★ 비즈 anchor 16건 (SplashScreen 8 + InstallPrompt 8) 1 byte 변경 0**. **★ 특수 글리프 ⋮ U+22EE (Android Chrome 메뉴) + ⎋ U+238B (iOS Safari 공유) 인라인 fontSize:16 1 byte 변경 0** (OQ #5 LOCKED). 27-login (gox) / 16-workshift (u5n) / 17-annual-plan (1hj) 단일 파일 mirror 패턴이지만 28-splash 만 2 파일 atomic.

Purpose:
- v0.1.1 토큰 className 으로 옛 인라인 `#161b22 / #1c2128 / #e6edf3 / #8b949e / #6e7681 / #3d444d / #f59e0b / rgba(245,158,11,0.1) / rgba(245,158,11,0.25) / #3b82f6 / rgba(59,130,246,0.15)` 치환
- **OQ #1 LOCKED**: InstallPrompt 설치 버튼 → `bg-safe-bar text-text-on-accent` solid + `linear-gradient(135deg, #2563eb, #0ea5e9)` 완전 폐기
- **OQ #2 LOCKED**: SplashScreen 진행 바 fill → `bg-accent` solid + `linear-gradient(90deg,#3b82f6,#0ea5e9)` 완전 폐기. 트랙 인라인 rgba(255,255,255,0.07) 유지
- **OQ #3 LOCKED**: 폰트 격상 매핑 verbatim — 10·11 → text-caption leading-none (12 격상) / 12 유지 / 13 → text-label / 15 → text-body (16 격상) / 18 → text-title / 22 → text-heading. fontSize:16 글리프 ⋮ ⎋ 인라인 유지 (OQ #5 예외).
- **OQ #4 LOCKED**: 토큰 치환 + 인라인 유지 4건 (진행 바 트랙 + 카드 border + 오버레이 + boxShadow). 노란 경고 3종 → text-warning bg-warning-bg border-warning/25. 숫자 원 + 확인했습니다 → bg-accent/15 text-accent
- **OQ #5 LOCKED**: ⋮ U+22EE (InstallPrompt line 122) + ⎋ U+238B (InstallPrompt line 164) 인라인 `fontSize: 16` 100% 유지. iOS ⎋ `verticalAlign: 'middle'` 보존. Lucide 교체 절대 금지 (글꼴 의미 anchor — Android Chrome ⋮ 메뉴 / iOS Safari ⎋ 공유 시그니처)
- ★ 비즈 anchor 16건 (SplashScreen 8 + InstallPrompt 8) **1 byte 변경 0** (§4 박제)
- 비즈 로직 0 diff (state/handler/effect/hook/카피/PWA 이벤트 구독 모두 보존)

Output:
- cha-bio-safety/src/pages/SplashScreen.tsx 단일 파일 in-place 수정 (83 → 80~110 lines 예상)
- cha-bio-safety/src/components/InstallPrompt.tsx 단일 파일 in-place 수정 (237 → 220~290 lines 예상)
- pwaInstall.ts + versionCheck.ts 0 byte 변경
- 다른 파일 변경 0건 (App.tsx / tailwind.config.js / tokens.css / hooks/ / functions/ / templates/ / migrations/ / public/ 모두 무영향)
- `npm run build` PASS
- atomic 1-commit (2 파일 묶음)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jykevin/Documents/cbc7119-design/CLAUDE.md
@/Users/jykevin/Documents/cbc7119-design/CLAUDE.local.md
@/Users/jykevin/Documents/cbc7119-design/.planning/STATE.md

# W5 TSX 변환 checklist (478 lines, SOURCE OF TRUTH — §3 매핑 표 verbatim)
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/28-splash/wave-5-tsx-conversion-checklist.md

# W1 OQ LOCKED 결정 (5건) + 메모리 룰
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/28-splash/wave-1-index.md

# W2~W4 sketch (변환 매핑 시각 검증용)
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/28-splash/sketch-wave-2-splash.html
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/28-splash/sketch-wave-3-install-main.html
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/28-splash/sketch-wave-4-install-guides.html

# 토큰 / 타이포 source
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/28-splash/tokens.css
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/tailwind.config.js

# Precedent — 17-annual-plan 1hj + 27-login gox + 16-workshift u5n PLAN.md (정확한 mirror, 단일 atomic 1-commit / W5 checklist verbatim)
@/Users/jykevin/Documents/cbc7119-design/.planning/quick/260522-1hj-redesign-17-annual-plan-tsx-annualplanpa/260522-1hj-PLAN.md
@/Users/jykevin/Documents/cbc7119-design/.planning/quick/260522-1hj-redesign-17-annual-plan-tsx-annualplanpa/260522-1hj-SUMMARY.md
@/Users/jykevin/Documents/cbc7119-design/.planning/quick/260521-gox-redesign-27-login-tsx-loginpage-tsx-220-/260521-gox-PLAN.md
@/Users/jykevin/Documents/cbc7119-design/.planning/quick/260521-u5n-redesign-16-workshift-tsx-workshiftpage-/260521-u5n-PLAN.md

# 변환 대상 (in-place 수정 2 파일)
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/src/pages/SplashScreen.tsx
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/src/components/InstallPrompt.tsx

# 변환 무영향 — 0 byte 변경 (PWA 이벤트 구독 + 버전 체크 비즈)
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/src/utils/pwaInstall.ts
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/src/utils/versionCheck.ts

<interfaces>
<!-- SplashScreen + InstallPrompt 안의 비즈 로직 시그니처 — 모두 보존 (변환 시 1 byte 도 손대지 않음) -->

```typescript
// ============================================================
// SplashScreen.tsx — imports + state + effects (line 1~35)
// ============================================================

// imports (line 1~5) — 1 byte 변경 0
import { useEffect, useState } from 'react'
import { useNavigate } from 'react-router-dom'
import { useAuthStore } from '../stores/authStore'
import { InstallPrompt, shouldShowInstallPrompt, dismissInstallPrompt } from '../components/InstallPrompt'
import { checkVersionAndRefresh } from '../utils/versionCheck'

// 시그니처 (line 7~11) — 그대로
export default function SplashScreen() {
  const [pct, setPct] = useState(0)                  // line 8
  const [showInstall, setShowInstall] = useState(false)  // line 9
  const navigate = useNavigate()                      // line 10
  const { isAuthenticated } = useAuthStore()          // line 11

  // useEffect (line 13~29) — ★ 비즈 anchor 4건 포함, 1 byte 변경 0
  useEffect(() => {
    void checkVersionAndRefresh()                                                   // line 16 anchor
    const tick = setInterval(() => setPct(p => Math.min(p + 4, 100)), 48)            // line 18 anchor (48ms / +4)
    const nav = setTimeout(() => {                                                  // line 20
      if (shouldShowInstallPrompt()) {                                              // line 21 anchor
        setShowInstall(true)
      } else {
        navigate(isAuthenticated ? '/dashboard' : '/login', { replace: true })      // line 24 anchor
      }
    }, 1600)                                                                        // line 26 anchor
    return () => { clearInterval(tick); clearTimeout(nav) }
  }, [isAuthenticated, navigate])

  // handleDismiss (line 31~35) — 1 byte 변경 0
  function handleDismiss() {
    dismissInstallPrompt()
    setShowInstall(false)
    navigate(isAuthenticated ? '/dashboard' : '/login', { replace: true })          // line 34 anchor (isAuthenticated 분기 2번째)
  }

  // ... render (line 37~82) — className 변환 대상 ...

  // ★ line 73 anchor — Vite define
  // <p ...>v{__APP_VERSION__} · 경기도 성남시 분당구</p>

  // ★ line 78~80 anchor — animation keyframe
  // <style>{`@keyframes slideUp { from{opacity:0;transform:translateY(16px)} to{opacity:1;transform:translateY(0)} }`}</style>
}

// ============================================================
// InstallPrompt.tsx — imports + helpers + state + effects (line 1~55)
// ============================================================

// imports (line 1~2) — ★ 비즈 anchor (3 PWA helpers), 1 byte 변경 0
import { useState, useEffect } from 'react'
import { getDeferredInstallPrompt, subscribeInstallPrompt, showInstallPrompt } from '../utils/pwaInstall'

// ★ isStandalone() — 1 byte 변경 0 (line 5~10, PWA 검출 anchor)
function isStandalone(): boolean {
  if ((window.navigator as any).standalone === true) return true  // iOS
  if (window.matchMedia('(display-mode: standalone)').matches) return true  // Android
  if (window.matchMedia('(display-mode: fullscreen)').matches) return true
  return false
}

// ★ isIOS() — 1 byte 변경 0 (line 12~17, iOS 검출 anchor)
function isIOS(): boolean {
  if (/iPhone|iPad|iPod/.test(navigator.userAgent)) return true
  if (navigator.userAgent.includes('Macintosh') && navigator.maxTouchPoints > 1) return true
  return false
}

// 시그니처 (line 25~35) — 그대로
export function InstallPrompt({ onDismiss }: { onDismiss: () => void }) {
  const [hasPrompt, setHasPrompt] = useState<boolean>(() => !!getDeferredInstallPrompt())  // line 28
  const [showIOSGuide, setShowIOSGuide] = useState(false)                                  // line 29
  const [showAndroidGuide, setShowAndroidGuide] = useState(false)                          // line 30

  useEffect(() => {                                                                        // line 32~35
    const unsub = subscribeInstallPrompt(p => setHasPrompt(!!p))
    return unsub
  }, [])

  // ★ outcome 3 분기 (line 38~48) — 1 byte 변경 0
  async function handleInstallAndroid() {
    const outcome = await showInstallPrompt()
    if (outcome === 'accepted') {        // anchor
      onDismiss()
    } else if (outcome === 'unavailable') {  // anchor
      setShowAndroidGuide(true)
    }
    // 'dismissed' anchor — no-op
  }

  function handleInstallIOS() {
    setShowIOSGuide(true)
  }

  const ios = isIOS()

  // ... render (line 57~217) — className 변환 대상, 3 view state 분기 ...
}

// ★ shouldShowInstallPrompt() — 1 byte 변경 0 (line 224~233, 24h TTL anchor)
export function shouldShowInstallPrompt(): boolean {
  if (isStandalone()) return false
  const dismissed = localStorage.getItem('pwa-install-dismissed')
  if (dismissed) {
    const ts = parseInt(dismissed, 10)
    if (Date.now() - ts < 24 * 60 * 60 * 1000) return false   // anchor 24h TTL
  }
  return true
}

// ★ dismissInstallPrompt() — 1 byte 변경 0 (line 235~237, localStorage key anchor)
export function dismissInstallPrompt() {
  localStorage.setItem('pwa-install-dismissed', String(Date.now()))
}
```
</interfaces>

<biz_anchor_lockdown>
<!-- 17-annual-plan SW3 precedent — 비즈 anchor 시그니처 보존 = PWA 이벤트/버전 캐시/iOS PWA 검출 무손실 변환 조건 -->

**SplashScreen 8건 — 모두 1 byte 변경 금지:**

1. **`__APP_VERSION__` Vite define** (line 73) — `v{__APP_VERSION__} · 경기도 성남시 분당구`
   - vite.config.ts 의 `define: { __APP_VERSION__: JSON.stringify(...) }` 가 빌드 시 인라인 치환. 변환 시 변수명 / 문자열 위치 변경 시 빌드 실패.

2. **`@keyframes slideUp`** (line 79) — inline `<style>` 블록 안 keyframe
   - `from{opacity:0;transform:translateY(16px)} to{opacity:1;transform:translateY(0)}` — 로고 등장 애니메이션 anchor. tailwind animate 토큰으로 교체 X.

3. **`48ms` setInterval tick** (line 18) — `setInterval(() => setPct(p => Math.min(p + 4, 100)), 48)`
   - 1600ms ÷ 48ms ≈ 33 tick × +4% = 132% (cap 100%). 변경 시 진행 바 속도 어긋남.

4. **`1600ms` setTimeout** (line 26) — `setTimeout(..., 1600)`
   - 스플래시 표시 시간. 변경 시 dashboard / login 분기 타이밍 어긋남.

5. **`+4` increment** (line 18, `p + 4`) — 진행 바 step.

6. **`checkVersionAndRefresh()` 호출** (line 16) — `void checkVersionAndRefresh()`
   - PWA 캐시 invalidation 트리거. memory `feedback_pwa_cache_invalidation` "여전히 똑같다" 반복 안 됨 anchor.

7. **`shouldShowInstallPrompt()` 분기** (line 21) — `if (shouldShowInstallPrompt())`
   - PWA 미설치 시 설치 팝업 우선 표시 분기.

8. **`isAuthenticated ? '/dashboard' : '/login'` 분기** (line 24, 34) — 2번 등장
   - 로그인 / 비로그인 navigate 분기. memory `project_inspection_completion_rule` 단일 룰 anchor 와 동일 패턴.

**InstallPrompt 8건 — 모두 1 byte 변경 금지:**

1. **`isStandalone()`** (line 5~10) — iOS standalone + display-mode standalone + display-mode fullscreen 3 분기
   - memory `feedback_ios_pwa_push_silent_drop` 1순위 의심 anchor.

2. **`isIOS()`** (line 12~17) — iPhone/iPad/iPod + Macintosh+maxTouchPoints fallback
   - iPadOS 13+ Macintosh 위장 검출 anchor.

3. **3 PWA imports** (line 2) — `getDeferredInstallPrompt`, `subscribeInstallPrompt`, `showInstallPrompt`
   - pwaInstall.ts 모듈 로드 시점 beforeinstallprompt 이벤트 캡처. import path / 함수명 변경 시 PWA 설치 분기 깨짐.

4. **`shouldShowInstallPrompt()` export + 24h TTL** (line 224~233) — `Date.now() - ts < 24 * 60 * 60 * 1000`
   - 24h dismiss 정책 anchor. 변경 시 사용자 거부 후 재표시 시점 어긋남.

5. **`dismissInstallPrompt()` export + localStorage key** (line 235~237) — `'pwa-install-dismissed'`
   - localStorage key 변경 시 기존 dismiss 기록 무효화. 사용자 경험 저하.

6. **`outcome` 3 분기** (line 40~47) — `'accepted'` / `'unavailable'` / `'dismissed'`
   - beforeinstallprompt prompt() 결과 분기. 'unavailable' 분기 (브라우저가 이벤트 미발사) → Android 가이드 표시 anchor.

7. **3 view state** (line 88, 112, 148) — `!showIOSGuide && !showAndroidGuide` 메인 / `showAndroidGuide` / iOS (else)
   - 3 분기 ternary 패턴. 변경 시 가이드 표시 로직 깨짐.

8. **`setHasPrompt(!!p)` + `subscribeInstallPrompt`** (line 28, 32~35) — useState initial + useEffect 구독
   - 이벤트 미발사 시점에 mount 시 hasPrompt false → 이후 이벤트 도착 시 setHasPrompt(true) 갱신 anchor.

**★ 특수 글리프 anchor (OQ #5 LOCKED, 별도 박제):**

9. **⋮ U+22EE** (InstallPrompt line 122) — `<span style={{ fontSize: 16 }}>⋮</span>`
   - Android Chrome 메뉴 시그니처 글리프. Lucide MoreVertical 교체 X (사용자가 글꼴로 인지하는 시그니처).

10. **⎋ U+238B** (InstallPrompt line 164) — `<span style={{ fontSize: 16, verticalAlign: 'middle' }}>⎋</span>`
    - iOS Safari 공유 버튼 시그니처 글리프. `verticalAlign: 'middle'` 도 포함 1 byte 변경 X. Lucide Share 교체 X.

**확인 grep gate (verify automated 안에 박제):**
```bash
# SplashScreen 8
grep -c '__APP_VERSION__' SplashScreen.tsx                              # >= 1
grep -c '@keyframes slideUp' SplashScreen.tsx                           # >= 1
grep -c ', 48' SplashScreen.tsx                                          # >= 1  (setInterval 48ms)
grep -c '1600' SplashScreen.tsx                                          # >= 1  (setTimeout 1600ms)
grep -c 'p + 4' SplashScreen.tsx                                         # >= 1  (+4 increment)
grep -c 'checkVersionAndRefresh' SplashScreen.tsx                        # >= 1
grep -c 'shouldShowInstallPrompt' SplashScreen.tsx                       # >= 1
grep -c "isAuthenticated ? '/dashboard' : '/login'" SplashScreen.tsx     # >= 2

# InstallPrompt 8
grep -c 'isStandalone' InstallPrompt.tsx                                 # >= 2  (정의 + 사용)
grep -c 'isIOS' InstallPrompt.tsx                                        # >= 2
grep -cE 'getDeferredInstallPrompt|subscribeInstallPrompt|showInstallPrompt' InstallPrompt.tsx  # >= 3
grep -c "'pwa-install-dismissed'" InstallPrompt.tsx                      # >= 2
grep -c '24 \* 60 \* 60 \* 1000' InstallPrompt.tsx                        # >= 1
grep -cE "'accepted'|'unavailable'|'dismissed'" InstallPrompt.tsx       # >= 2
grep -c 'showIOSGuide' InstallPrompt.tsx                                 # >= 3
grep -c 'showAndroidGuide' InstallPrompt.tsx                             # >= 3

# 특수 글리프 OQ #5
grep -c '⋮' InstallPrompt.tsx                                            # >= 1  (Android U+22EE)
grep -c '⎋' InstallPrompt.tsx                                            # >= 1  (iOS U+238B)
grep -c "verticalAlign: 'middle'" InstallPrompt.tsx                     # >= 1  (iOS ⎋)
```
</biz_anchor_lockdown>

<tailwind_token_cheatsheet>
<!-- W5 §9 verbatim 인용 — 옛 인라인 → v0.1.1 className 1:1 매핑 (28-splash 적용 위치) -->

| 옛 인라인 / 색                                                | v0.1.1 className                            | 28-splash 적용 위치                                |
|---------------------------------------------------------------|---------------------------------------------|----------------------------------------------------|
| `#161b22` (외곽 background)                                   | `bg-surface-page`                           | SplashScreen 외곽                                  |
| `#1c2128` (카드 background)                                   | `bg-surface-raised`                         | InstallPrompt 카드                                 |
| `#e6edf3` (text)                                              | `text-text-primary`                         | SplashScreen 타이틀 / InstallPrompt 카드 제목 + step 제목 + strong |
| `#8b949e` (text)                                              | `text-text-secondary`                       | InstallPrompt 카드 부제 + step 하위 설명           |
| `#6e7681` (text)                                              | `text-text-tertiary`                        | SplashScreen 부제 + 진행 라벨 + InstallPrompt 나중에 |
| `#3d444d` (text)                                              | `text-text-disabled`                        | SplashScreen 버전                                  |
| `#fff` (CTA 글자)                                             | `text-text-on-accent`                       | InstallPrompt 설치 버튼 (OQ #1)                    |
| `linear-gradient(135deg, #2563eb, #0ea5e9)` (설치 버튼)       | `bg-safe-bar` (OQ #1 LOCKED solid)          | InstallPrompt 설치 버튼 — 그라데이션 폐기          |
| `linear-gradient(90deg,#3b82f6,#0ea5e9)` (진행 바 fill)       | `bg-accent` (OQ #2 LOCKED solid)            | SplashScreen 진행 바 fill — 그라데이션 폐기        |
| `rgba(255,255,255,0.07)` (진행 바 트랙)                       | **인라인 유지** (OQ #4 인라인 1/4)           | SplashScreen 진행 바 트랙 (alpha 정밀도)           |
| `rgba(59,130,246,0.3)` (카드 border / 아이콘 박스 / 확인했습니다 border) | **인라인 유지** (OQ #4 인라인 2/4)  | InstallPrompt 카드 border + 아이콘 박스 + 확인했습니다 border |
| `rgba(0,0,0,0.85)` (오버레이)                                 | **인라인 유지** (OQ #4 인라인 3/4)           | InstallPrompt 오버레이                             |
| `0 8px 40px rgba(0,0,0,0.5)` (카드 boxShadow)                 | **인라인 유지** (OQ #4 인라인 4/4)           | InstallPrompt 카드 그림자                          |
| `rgba(37,99,235,0.2)` (로고 박스 + 아이콘 박스 bg)            | **인라인 유지** (희박한 케이스, 토큰 비용 회피) | SplashScreen 로고 박스 + InstallPrompt 아이콘 박스 |
| `rgba(255,255,255,0.1)` (나중에 border)                       | **인라인 유지** (alpha 정밀도)               | InstallPrompt 나중에 버튼 border                   |
| `#f59e0b` (text) + `rgba(245,158,11,0.1)` (bg) + `rgba(245,158,11,0.25)` (border) | `text-warning bg-warning-bg border-warning/25` (OQ #4) | Android 노란 경고 3종 |
| `#3b82f6` (text) + `rgba(59,130,246,0.15)` (bg)               | `text-accent bg-accent/15`                  | 숫자 원 6 + 확인했습니다 2 (OQ #4)                 |
| `fontSize: 22` (타이틀)                                       | `text-heading font-black tracking-tight`    | SplashScreen 타이틀 (OQ #3)                        |
| `fontSize: 18` (카드 제목)                                    | `text-title font-extrabold`                 | InstallPrompt 카드 제목 (OQ #3)                    |
| `fontSize: 15` (설치 버튼)                                    | `text-body font-bold` (15→16 격상)          | InstallPrompt 설치 버튼 (OQ #3)                    |
| `fontSize: 14` (모바일 헤더 / 다운로드 — 28에는 없음)         | -                                           | (28-splash 에 14 없음)                             |
| `fontSize: 13` (숫자 원 / step 제목 / 확인했습니다)            | `text-label` (13 유지)                       | InstallPrompt 숫자 원 + step 제목 + 확인했습니다 (OQ #3) |
| `fontSize: 12` (부제 / 진행 라벨 격상 전 / 나중에 / 노란 경고 / 카드 부제) | `text-caption` (12 유지)             | InstallPrompt 카드 부제 + 나중에 + 노란 경고 + SplashScreen 부제 (OQ #3) |
| `fontSize: 11` (진행 라벨 / step 하위 설명)                   | `text-caption` (11→12 격상)                  | SplashScreen 진행 라벨 + InstallPrompt step 하위 설명 (OQ #3) |
| `fontSize: 10` (버전)                                          | `text-caption` (10→12 격상)                  | SplashScreen 버전 (OQ #3)                          |
| `fontSize: 16` (특수 글리프 ⋮ ⎋)                              | **인라인 유지** (OQ #5 LOCKED 예외)          | Android ⋮ + iOS ⎋                                  |
| `borderRadius: 22` (로고 박스)                                | `rounded-[22px]` (토큰 없음 arbitrary)       | SplashScreen 로고 박스                             |
| `borderRadius: 20` (카드)                                     | `rounded-[20px]` (토큰 없음 arbitrary)       | InstallPrompt 카드                                 |
| `borderRadius: 16` (아이콘 박스)                              | `rounded-lg`                                | InstallPrompt 아이콘 박스                          |
| `borderRadius: 12` (설치 버튼)                                | `rounded-md`                                | InstallPrompt 설치 버튼                            |
| `borderRadius: 10` (나중에 / 확인했습니다)                    | `rounded-md`                                | InstallPrompt 나중에 + 확인했습니다 2건            |
| `borderRadius: 8` (노란 경고 / 숫자 원)                        | `rounded-sm`                                | InstallPrompt 노란 경고 + 숫자 원 6건              |
| `borderRadius: 2` (진행 바 트랙 + fill)                       | **인라인 유지** (희박한 small radius)        | SplashScreen 진행 바                               |

**status- class prefix 0건** (memory `feedback_tailwind_token_class_pattern`):
- 정확 = `text-warning` / `bg-warning-bg` / `border-warning/25` / `text-accent` / `bg-accent/15` / `bg-safe-bar` / `bg-accent`
- 잘못 = `text-status-warning` / `bg-status-safe-bar` ← 11-div TSX hotfix(4ce707e) 사고 회피

**w-8 / h-8 = 48px 함정** (memory `feedback_tailwind_w8_h8_is_48px`):
- 28-splash 에 width:8 / height:8 사용 사례 없음. width:28 (숫자 원) / width:48 (icon img) / width:64 (아이콘 박스) / width:88 (로고 박스) 등. 모두 인라인 유지.

**작은 컨테이너 text-caption → leading-none** (memory `feedback_text_caption_leading_none`):
- 진행 라벨 / 버전 / 나중에 / 노란 경고 / step 하위 설명 / 카드 부제 모두 `leading-none` 명시 (카드 부제 + 노란 경고 + step 하위 설명 + 카드 부제 = `leading-relaxed` 예외 = 줄바꿈 2 행)

**linear-gradient 폐기 (OQ #1 + OQ #2 LOCKED):**
- `linear-gradient(135deg, #2563eb, #0ea5e9)` (InstallPrompt line 94 설치 버튼) → `bg-safe-bar` solid
- `linear-gradient(90deg,#3b82f6,#0ea5e9)` (SplashScreen line 66 진행 바 fill) → `bg-accent` solid
- 2 파일 모두 grep gate `linear-gradient == 0`

**특수 글리프 (OQ #5 LOCKED) — Lucide 교체 절대 금지:**
- ⋮ U+22EE (Android Chrome 메뉴 시그니처) — Lucide MoreVertical 으로 바꾸지 X
- ⎋ U+238B (iOS Safari 공유 시그니처) — Lucide Share 으로 바꾸지 X
- 이유: 사용자가 글꼴 형태로 인지하는 OS 시그니처. Lucide outline icon 으로는 의미 전달 불가.
- emoji negative grep range `[😀-🛿🤀-🧿]` 는 U+22EE / U+238B 모두 매칭 X (Unicode 블록 외부)
</tailwind_token_cheatsheet>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SplashScreen.tsx + InstallPrompt.tsx 2 파일 atomic v0.1.1 토큰 변환 (in-place, 비즈 anchor 16건 + ⋮ ⎋ 글리프 보존, atomic 1-commit)</name>
  <files>cha-bio-safety/src/pages/SplashScreen.tsx, cha-bio-safety/src/components/InstallPrompt.tsx</files>
  <action>

SplashScreen.tsx 83 lines + InstallPrompt.tsx 237 lines 를 **단일 atomic commit** 으로 in-place 수정하여 v0.1.1 토큰 className 으로 치환한다. **27-login (gox) / 16-workshift (u5n) / 17-annual-plan (1hj) mirror 패턴이지만 2 파일 묶음 atomic 인 점만 다름.** 비즈 로직(useState/useEffect/handler/카피/PWA 이벤트 구독 + 24h TTL + 3 view state) 1 byte 변경 0. **★ 비즈 anchor 16건 (SplashScreen 8 + InstallPrompt 8) + 특수 글리프 ⋮ U+22EE + ⎋ U+238B 인라인 1 byte 변경 금지**.

## 0. 사전 확인 (필수)

```bash
git branch --show-current   # 기대: redesign/28-splash-tsx
git status --short          # 기대: 빈 출력 (clean)
wc -l cha-bio-safety/src/pages/SplashScreen.tsx       # 기대: 83
wc -l cha-bio-safety/src/components/InstallPrompt.tsx  # 기대: 237

# pwaInstall.ts + versionCheck.ts 현 상태 (변환 후 0 diff 확인용 baseline)
wc -l cha-bio-safety/src/utils/pwaInstall.ts
wc -l cha-bio-safety/src/utils/versionCheck.ts
```

## 1. 변환 매핑 (W5 §3.1~§3.6 verbatim — 추측 0건)

### §3.1 SplashScreen — 외곽 + 로고 + 타이틀 + 부제 (line 38~60, W2 sketch)

| 현재 (인라인 style, line) | 변환 후 (className + 인라인) | sketch |
|---|---|---|
| 외곽 div line 38~43: `style={{ minHeight:'100dvh', background:'#161b22', display:'flex', flexDirection:'column', alignItems:'center', justifyContent:'center', gap:0 }}` | `<div className="bg-surface-page" style={{ minHeight:'100dvh', display:'flex', flexDirection:'column', alignItems:'center', justifyContent:'center', gap:0 }}>` (OQ #4 LOCKED 페이지 토큰) | W2 |
| 로고 wrapper line 45: `style={{ animation:'slideUp .4s ease-out', display:'flex', flexDirection:'column', alignItems:'center', gap:20, marginBottom:56 }}` | **인라인 100% 유지** (★ animation slideUp anchor) | W2 |
| 로고 박스 line 47~52: `style={{ width:88, height:88, borderRadius:22, background:'rgba(37,99,235,0.2)', border:'1px solid rgba(59,130,246,0.3)', display:'flex', alignItems:'center', justifyContent:'center', overflow:'hidden' }}` | `<div className="rounded-[22px]" style={{ width:88, height:88, background:'rgba(37,99,235,0.2)', border:'1px solid rgba(59,130,246,0.3)', display:'flex', alignItems:'center', justifyContent:'center', overflow:'hidden' }}>` (rounded-[22px] arbitrary + rgba 인라인 유지) | W2 |
| 로고 img line 54: `<img src="/icons/icon-192.png" alt="" style={{ width: 64, height: 64, borderRadius: 14 }} />` | **인라인 유지** (희박한 radius 14 토큰 비용 회피) | W2 |
| 타이틀 wrapper line 57: `style={{ textAlign:'center' }}` | **인라인 유지** | W2 |
| 타이틀 h1 line 58: `style={{ fontSize:22, fontWeight:900, color:'#e6edf3', margin:0, letterSpacing:'-.02em' }}` | `<h1 className="text-heading font-black tracking-tight text-text-primary" style={{ margin:0 }}>` (OQ #3 LOCKED 22→text-heading + OQ #4 #e6edf3→text-text-primary) | W2 |
| 부제 p line 59: `style={{ fontSize:12, color:'#6e7681', margin:'6px 0 0', letterSpacing:'.1em' }}` | `<p className="text-caption leading-relaxed text-text-tertiary" style={{ margin:'6px 0 0', letterSpacing:'.1em' }}>` (OQ #3 LOCKED 12 유지 + leading-relaxed + OQ #4 #6e7681→text-text-tertiary) | W2 |

### §3.2 SplashScreen — 진행 바 + 진행 라벨 + 버전 + InstallPrompt 마운트 + style 블록 (line 63~80, W2 sketch + OQ #2)

| 현재 | 변환 후 | sketch |
|---|---|---|
| 진행 바 wrapper line 64: `style={{ width:160 }}` | **인라인 유지** | W2 |
| 진행 바 트랙 line 65: `style={{ height:2, background:'rgba(255,255,255,0.07)', borderRadius:2, overflow:'hidden' }}` | **인라인 100% 유지** (★ OQ #4 인라인 유지 1/4 — alpha 정밀도 + radius 2) | W2 |
| 진행 바 fill line 66: `style={{ height:'100%', background:'linear-gradient(90deg,#3b82f6,#0ea5e9)', borderRadius:2, width:\`${pct}%\`, transition:'width .05s linear' }}` | `<div className="bg-accent" style={{ height:'100%', borderRadius:2, width:\`${pct}%\`, transition:'width .05s linear' }} />` (★ OQ #2 LOCKED solid, linear-gradient 폐기) | W2 |
| 진행 라벨 p line 68: `style={{ fontSize:11, color:'#6e7681', textAlign:'center', marginTop:10 }}` | `<p className="text-caption leading-none text-text-tertiary" style={{ textAlign:'center', marginTop:10 }}>` (OQ #3 LOCKED 11→12 격상 + leading-none + OQ #4 #6e7681→text-text-tertiary) | W2 |
| 버전 p line 73: `style={{ position:'absolute', bottom:20, fontSize:10, color:'#3d444d' }}` 안 `v{__APP_VERSION__} · 경기도 성남시 분당구` | `<p className="text-caption leading-none text-text-disabled" style={{ position:'absolute', bottom:20 }}>v{__APP_VERSION__} · 경기도 성남시 분당구</p>` (OQ #3 LOCKED 10→12 격상 + leading-none + OQ #4 #3d444d→text-text-disabled, ★ __APP_VERSION__ 1 byte 변경 0) | W2 |
| InstallPrompt 마운트 line 76: `{showInstall && <InstallPrompt onDismiss={handleDismiss} />}` | **변경 0** | W2 |
| style 블록 line 78~80: `<style>{\`@keyframes slideUp { from{opacity:0;transform:translateY(16px)} to{opacity:1;transform:translateY(0)} }\`}</style>` | **변경 0** (★ animation anchor) | W2 |

### §3.3 InstallPrompt — 오버레이 + 카드 + 아이콘 + 제목 + 부제 (line 57~86, W3 sketch + OQ #4)

| 현재 | 변환 후 | sketch |
|---|---|---|
| 오버레이 line 58~63: `style={{ position:'fixed', inset:0, zIndex:9999, background:'rgba(0,0,0,0.85)', display:'flex', alignItems:'center', justifyContent:'center', padding:24 }}` | **인라인 100% 유지** (★ OQ #4 인라인 유지 2/4 — overlay alpha 정밀도) | W3 |
| 카드 line 64~70: `style={{ background:'#1c2128', borderRadius:20, padding:'28px 24px', maxWidth:340, width:'100%', textAlign:'center', border:'1px solid rgba(59,130,246,0.3)', boxShadow:'0 8px 40px rgba(0,0,0,0.5)' }}` | `<div className="bg-surface-raised rounded-[20px]" style={{ padding:'28px 24px', maxWidth:340, width:'100%', textAlign:'center', border:'1px solid rgba(59,130,246,0.3)', boxShadow:'0 8px 40px rgba(0,0,0,0.5)' }}>` (OQ #4 카드 토큰 + ★ 인라인 유지 3/4 border + ★ 인라인 유지 4/4 boxShadow) | W3 |
| 아이콘 박스 line 72~77: `style={{ width:64, height:64, borderRadius:16, margin:'0 auto 16px', background:'rgba(37,99,235,0.2)', border:'1px solid rgba(59,130,246,0.3)', display:'flex', alignItems:'center', justifyContent:'center' }}` | `<div className="rounded-lg" style={{ width:64, height:64, margin:'0 auto 16px', background:'rgba(37,99,235,0.2)', border:'1px solid rgba(59,130,246,0.3)', display:'flex', alignItems:'center', justifyContent:'center' }}>` (rounded-lg 16 + rgba 인라인 유지) | W3 |
| 아이콘 img line 78: `<img src="/icons/icon-192.png" alt="" style={{ width: 48, height: 48, borderRadius: 12 }} />` | **인라인 유지** | W3 |
| 카드 제목 h2 line 81: `style={{ fontSize:18, fontWeight:800, color:'#e6edf3', margin:'0 0 8px' }}` | `<h2 className="text-title font-extrabold text-text-primary" style={{ margin:'0 0 8px' }}>` (OQ #3 LOCKED 18→text-title + OQ #4 #e6edf3→text-text-primary) | W3 |
| 카드 부제 p line 84: `style={{ fontSize:12, color:'#8b949e', margin:'0 0 20px', lineHeight:1.5 }}` 안 `홈 화면에 앱을 설치하면<br/>더 빠르고 편리하게 사용할 수 있습니다` | `<p className="text-caption leading-relaxed text-text-secondary" style={{ margin:'0 0 20px' }}>` (OQ #3 LOCKED 12 + leading-relaxed [`<br/>` 줄바꿈] + OQ #4 #8b949e→text-text-secondary, lineHeight:1.5 제거 — leading-relaxed 가 처리) | W3 |

### §3.4 InstallPrompt — 메인 view (설치 + 나중에) (line 88~111, W3 sketch + OQ #1)

```jsx
// 분기 시작 (line 88) — 변경 0
{!showIOSGuide && !showAndroidGuide ? (
  <div style={{ display: 'flex', flexDirection: 'column', gap: 10 }}>  // 인라인 유지
    {/* 설치 버튼 */}
    <button
      onClick={ios ? handleInstallIOS : handleInstallAndroid}  // 변경 0
      // 옛 style (line 92~98): { width:'100%', height:48, borderRadius:12, background:'linear-gradient(135deg, #2563eb, #0ea5e9)', border:'none', color:'#fff', fontSize:15, fontWeight:700, cursor:'pointer' }
      className="bg-safe-bar text-text-on-accent text-body font-bold rounded-md"  // ★ OQ #1 LOCKED solid + OQ #3 LOCKED 15→16 격상 text-body
      style={{ width:'100%', height:48, border:'none', cursor:'pointer' }}
    >
      {ios ? '설치 방법 보기' : hasPrompt ? '홈 화면에 설치' : '설치 방법 보기'}  // 변경 0 (★ hasPrompt 분기 + 카피)
    </button>
    {/* 나중에 할게요 */}
    <button
      onClick={onDismiss}  // 변경 0
      // 옛 style (line 103~107): { width:'100%', height:40, borderRadius:10, background:'transparent', border:'1px solid rgba(255,255,255,0.1)', color:'#6e7681', fontSize:12, fontWeight:600, cursor:'pointer' }
      className="text-caption font-bold leading-none text-text-tertiary rounded-md"  // OQ #3 LOCKED 12 유지 + leading-none + OQ #4 #6e7681→text-text-tertiary
      style={{ width:'100%', height:40, background:'transparent', border:'1px solid rgba(255,255,255,0.1)', cursor:'pointer' }}  // 희박 border alpha 인라인 유지
    >
      나중에 할게요  // 변경 0
    </button>
  </div>
) : showAndroidGuide ? (
  // ... §3.5 ...
) : (
  // ... §3.6 ...
)}
```

### §3.5 InstallPrompt — Android 가이드 view (line 112~147, W4 sketch + OQ #4 + OQ #5)

```jsx
) : showAndroidGuide ? (  // 변경 0 (★ 3 view state 분기)
  <div style={{ textAlign:'left' }}>  // 인라인 유지
    {/* 노란 경고 — OQ #4 LOCKED text-warning + bg-warning-bg + border-warning/25 */}
    {/* 옛 style (line 115): { fontSize:12, color:'#f59e0b', background:'rgba(245,158,11,0.1)', border:'1px solid rgba(245,158,11,0.25)', borderRadius:8, padding:'8px 10px', marginBottom:14 } */}
    <div
      className="text-caption leading-relaxed text-warning bg-warning-bg border border-warning/25 rounded-sm"
      style={{ padding:'8px 10px', marginBottom:14 }}
    >
      자동 설치 창이 뜨지 않으면 아래 순서로 설치해 주세요.  // 변경 0
    </div>
    {/* 3-step list */}
    <div style={{ display:'flex', flexDirection:'column', gap:14 }}>  // 인라인 유지
      {/* step 1 — ★ Android ⋮ U+22EE 글리프 포함 */}
      <div style={{ display:'flex', alignItems:'flex-start', gap:12 }}>  // 인라인 유지
        {/* 숫자 원 — 옛 style: { width:28, height:28, borderRadius:8, flexShrink:0, background:'rgba(59,130,246,0.15)', color:'#3b82f6', display:'flex', alignItems:'center', justifyContent:'center', fontSize:13, fontWeight:800 } */}
        <div
          className="bg-accent/15 text-accent text-label font-extrabold leading-none rounded-sm"
          style={{ width:28, height:28, flexShrink:0, display:'flex', alignItems:'center', justifyContent:'center' }}
        >1</div>
        <div>
          {/* step 제목 — 옛 style: { fontSize:13, fontWeight:700, color:'#e6edf3' } */}
          <div className="text-label leading-none text-text-primary" style={{ fontWeight:700 }}>
            크롬 우상단 <span style={{ fontSize: 16 }}>⋮</span> 메뉴   {/* ★ OQ #5 LOCKED — `<span style={{ fontSize: 16 }}>⋮</span>` 1 byte 변경 0 */}
          </div>
          {/* step 하위 설명 — 옛 style: { fontSize:11, color:'#8b949e', marginTop:2 } */}
          <div className="text-caption leading-relaxed text-text-secondary" style={{ marginTop:2 }}>
            주소창 오른쪽 점 세 개 메뉴를 누르세요
          </div>
        </div>
      </div>
      {/* step 2 — 동일 패턴 */}
      <div style={{ display:'flex', alignItems:'flex-start', gap:12 }}>
        <div className="bg-accent/15 text-accent text-label font-extrabold leading-none rounded-sm" style={{ width:28, height:28, flexShrink:0, display:'flex', alignItems:'center', justifyContent:'center' }}>2</div>
        <div>
          <div className="text-label leading-none text-text-primary" style={{ fontWeight:700 }}>
            '앱 설치' 또는 '홈 화면에 추가' 선택
          </div>
          <div className="text-caption leading-relaxed text-text-secondary" style={{ marginTop:2 }}>
            메뉴에서 <strong className="text-text-primary">앱 설치</strong>(또는 <strong className="text-text-primary">홈 화면에 추가</strong>)를 누르세요
          </div>
        </div>
      </div>
      {/* step 3 — 동일 패턴 */}
      <div style={{ display:'flex', alignItems:'flex-start', gap:12 }}>
        <div className="bg-accent/15 text-accent text-label font-extrabold leading-none rounded-sm" style={{ width:28, height:28, flexShrink:0, display:'flex', alignItems:'center', justifyContent:'center' }}>3</div>
        <div>
          <div className="text-label leading-none text-text-primary" style={{ fontWeight:700 }}>'설치' 확인</div>
          <div className="text-caption leading-relaxed text-text-secondary" style={{ marginTop:2 }}>
            팝업에서 <strong className="text-text-primary">설치</strong>를 누르면 완료
          </div>
        </div>
      </div>
    </div>
    {/* 확인했습니다 — 옛 style (line 142~143): { width:'100%', height:44, borderRadius:10, marginTop:18, background:'rgba(59,130,246,0.15)', border:'1px solid rgba(59,130,246,0.3)', color:'#3b82f6', fontSize:13, fontWeight:700, cursor:'pointer' } */}
    <button
      onClick={onDismiss}  // 변경 0
      className="bg-accent/15 text-accent text-label font-bold leading-none rounded-md"
      style={{ width:'100%', height:44, marginTop:18, border:'1px solid rgba(59,130,246,0.3)', cursor:'pointer' }}  // 인라인 border rgba 유지
    >
      확인했습니다  // 변경 0
    </button>
  </div>
)
```

### §3.6 InstallPrompt — iOS 가이드 view (line 148~215, W4 sketch + OQ #5)

```jsx
: (  // 변경 0 (★ 3 view state 분기 — else)
  <div style={{ textAlign:'left' }}>  // 인라인 유지
    <div style={{ display:'flex', flexDirection:'column', gap:14 }}>  // 인라인 유지
      {/* step 1 — ★ iOS ⎋ U+238B 글리프 포함, verticalAlign 'middle' */}
      <div style={{ display:'flex', alignItems:'flex-start', gap:12 }}>
        <div className="bg-accent/15 text-accent text-label font-extrabold leading-none rounded-sm" style={{ width:28, height:28, flexShrink:0, display:'flex', alignItems:'center', justifyContent:'center' }}>1</div>
        <div>
          <div className="text-label leading-none text-text-primary" style={{ fontWeight:700 }}>
            하단 공유 버튼 터치
          </div>
          <div className="text-caption leading-relaxed text-text-secondary" style={{ marginTop:2 }}>
            Safari 하단의 <span style={{ fontSize: 16, verticalAlign: 'middle' }}>⎋</span> 공유 아이콘을 누르세요   {/* ★ OQ #5 LOCKED — `<span style={{ fontSize: 16, verticalAlign: 'middle' }}>⎋</span>` 1 byte 변경 0 */}
          </div>
        </div>
      </div>
      {/* step 2 */}
      <div style={{ display:'flex', alignItems:'flex-start', gap:12 }}>
        <div className="bg-accent/15 text-accent text-label font-extrabold leading-none rounded-sm" style={{ width:28, height:28, flexShrink:0, display:'flex', alignItems:'center', justifyContent:'center' }}>2</div>
        <div>
          <div className="text-label leading-none text-text-primary" style={{ fontWeight:700 }}>
            '홈 화면에 추가' 선택
          </div>
          <div className="text-caption leading-relaxed text-text-secondary" style={{ marginTop:2 }}>
            스크롤해서 <strong className="text-text-primary">홈 화면에 추가</strong>를 찾아 누르세요
          </div>
        </div>
      </div>
      {/* step 3 */}
      <div style={{ display:'flex', alignItems:'flex-start', gap:12 }}>
        <div className="bg-accent/15 text-accent text-label font-extrabold leading-none rounded-sm" style={{ width:28, height:28, flexShrink:0, display:'flex', alignItems:'center', justifyContent:'center' }}>3</div>
        <div>
          <div className="text-label leading-none text-text-primary" style={{ fontWeight:700 }}>
            '추가' 터치
          </div>
          <div className="text-caption leading-relaxed text-text-secondary" style={{ marginTop:2 }}>
            오른쪽 상단 <strong className="text-text-primary">추가</strong> 버튼을 누르면 완료!
          </div>
        </div>
      </div>
    </div>
    {/* 확인했습니다 — Android 와 동일 */}
    <button
      onClick={onDismiss}
      className="bg-accent/15 text-accent text-label font-bold leading-none rounded-md"
      style={{ width:'100%', height:44, marginTop:18, border:'1px solid rgba(59,130,246,0.3)', cursor:'pointer' }}
    >
      확인했습니다
    </button>
  </div>
)
```

## 2. OQ 5건 LOCKED 결정 (W1 §7 + W5 §4 verbatim — 위반 0건)

### OQ #1 LOCKED — InstallPrompt 설치 버튼 `bg-safe-bar` solid + 그라데이션 완전 폐기
- line 92~98 (설치 버튼): `background:'linear-gradient(135deg, #2563eb, #0ea5e9)'` 인라인 완전 제거 → className `bg-safe-bar text-text-on-accent text-body font-bold rounded-md`
- `color:'#fff'` 제거 → `text-text-on-accent`
- `fontSize:15` 제거 → `text-body font-bold` (15→16 격상)
- `borderRadius:12` 제거 → `rounded-md`
- `border:'none'`, `cursor:'pointer'`, `width:'100%'`, `height:48` 인라인 유지
- grep gate `grep -c 'linear-gradient' InstallPrompt.tsx == 0`

### OQ #2 LOCKED — SplashScreen 진행 바 `bg-accent` solid + 그라데이션 완전 폐기
- line 66 (진행 바 fill): `background:'linear-gradient(90deg,#3b82f6,#0ea5e9)'` 인라인 완전 제거 → className `bg-accent`
- `height:'100%'`, `borderRadius:2`, `width:\`${pct}%\``, `transition:'width .05s linear'` 인라인 유지
- line 65 트랙 `background:'rgba(255,255,255,0.07)'` 인라인 100% 유지 (★ 인라인 유지 1/4)
- grep gate `grep -c 'linear-gradient' SplashScreen.tsx == 0`

### OQ #3 LOCKED — 폰트 격상 매핑 verbatim (W5 §4)
- SplashScreen line 58 타이틀 22 → `text-heading font-black tracking-tight`
- SplashScreen line 59 부제 12 → `text-caption leading-relaxed` (유지)
- SplashScreen line 68 진행 라벨 11 → `text-caption leading-none` (11→12 격상)
- SplashScreen line 73 버전 10 → `text-caption leading-none` (10→12 격상)
- InstallPrompt line 81 카드 제목 18 → `text-title font-extrabold`
- InstallPrompt line 84 카드 부제 12 → `text-caption leading-relaxed` (유지, lineHeight:1.5 제거)
- InstallPrompt line 96 설치 버튼 15 → `text-body font-bold` (15→16 격상)
- InstallPrompt line 106 나중에 12 → `text-caption font-bold leading-none` (유지)
- InstallPrompt line 115 노란 경고 12 → `text-caption leading-relaxed`
- InstallPrompt line 120/127/134/154/171/187 숫자 원 13 → `text-label font-extrabold leading-none` (총 6개)
- InstallPrompt line 122/130/136/160/177/194 step 제목 13 → `text-label leading-none` (총 6개)
- InstallPrompt line 123/131/137/163/180/197 하위 설명 11 → `text-caption leading-relaxed` (11→12 격상, 총 6개)
- InstallPrompt line 143/209 확인했습니다 13 → `text-label font-bold leading-none` (총 2개)
- InstallPrompt line 122/164 글리프 인라인 fontSize:16 유지 (★ OQ #5 LOCKED 예외)

### OQ #4 LOCKED — 토큰 치환 + 인라인 유지 4건
- 페이지 #161b22 → `bg-surface-page` (SplashScreen line 39)
- 카드 #1c2128 → `bg-surface-raised` (InstallPrompt line 65)
- text #e6edf3 → `text-text-primary` (SplashScreen 타이틀 + InstallPrompt 카드 제목 + step 제목 + strong 다수)
- text #8b949e → `text-text-secondary` (InstallPrompt 카드 부제 + step 하위 설명 다수)
- text #6e7681 → `text-text-tertiary` (SplashScreen 부제 + 진행 라벨 + InstallPrompt 나중에)
- text #3d444d → `text-text-disabled` (SplashScreen 버전)
- 노란 경고 #f59e0b + rgba(245,158,11,0.1) + rgba(245,158,11,0.25) → `text-warning bg-warning-bg border-warning/25` (InstallPrompt line 115 3 토큰 status- prefix 없음)
- 숫자 원 + 확인했습니다 rgba(59,130,246,0.15) + #3b82f6 → `bg-accent/15 text-accent` (InstallPrompt 숫자 원 6 + 확인했습니다 2)
- **인라인 유지 4건**:
  - SplashScreen line 65 진행 바 트랙 `rgba(255,255,255,0.07)`
  - InstallPrompt line 68 카드 border `rgba(59,130,246,0.3)`
  - InstallPrompt line 59 오버레이 `rgba(0,0,0,0.85)`
  - InstallPrompt line 69 boxShadow `'0 8px 40px rgba(0,0,0,0.5)'`

### OQ #5 LOCKED — ⋮ U+22EE + ⎋ U+238B 인라인 fontSize:16 보존
- InstallPrompt line 122 `<span style={{ fontSize: 16 }}>⋮</span>` **1 byte 변경 0** (Android Chrome 메뉴 시그니처, U+22EE)
- InstallPrompt line 164 `<span style={{ fontSize: 16, verticalAlign: 'middle' }}>⎋</span>` **1 byte 변경 0** (iOS Safari 공유 시그니처, U+238B, `verticalAlign: 'middle'` 포함)
- Lucide 교체 절대 금지 (글꼴 의미 anchor, OS 시그니처)
- emoji negative grep range `[😀-🛿🤀-🧿]` 는 U+22EE / U+238B 매칭 X (Unicode 블록 외부)

## 3. ★ 비즈 anchor 16건 보존 (17-annual-plan SW3 precedent — 1 byte 변경 금지)

다음은 **변경 0** — 인라인 style 제거 외 logic / 상수 / 값 라인 손대지 않음:

### SplashScreen 8건
1. **line 73**: `v{__APP_VERSION__} · 경기도 성남시 분당구` — Vite define 인라인 치환 anchor
2. **line 79**: `@keyframes slideUp { from{opacity:0;transform:translateY(16px)} to{opacity:1;transform:translateY(0)} }` — 로고 등장 animation anchor
3. **line 18**: `setInterval(() => setPct(p => Math.min(p + 4, 100)), 48)` — 48ms tick + +4 increment 동시 anchor
4. **line 26**: `setTimeout(... , 1600)` — 1600ms 스플래시 표시 anchor
5. **line 18**: `p + 4` (+4 increment per tick) — anchor #3 안에 포함
6. **line 16**: `void checkVersionAndRefresh()` — PWA 캐시 invalidation 트리거 (memory `feedback_pwa_cache_invalidation`)
7. **line 21**: `if (shouldShowInstallPrompt())` — PWA 미설치 분기
8. **line 24, 34**: `navigate(isAuthenticated ? '/dashboard' : '/login', { replace: true })` — 2번 등장 (useEffect + handleDismiss)

### InstallPrompt 8건
1. **line 5~10**: `function isStandalone(): boolean` — iOS standalone + display-mode 3 분기 (memory `feedback_ios_pwa_push_silent_drop` 1순위 anchor)
2. **line 12~17**: `function isIOS(): boolean` — iPhone/iPad/iPod + Macintosh+maxTouchPoints fallback
3. **line 2**: `import { getDeferredInstallPrompt, subscribeInstallPrompt, showInstallPrompt } from '../utils/pwaInstall'`
4. **line 224~233**: `export function shouldShowInstallPrompt(): boolean` — isStandalone() + 24h TTL (`Date.now() - ts < 24 * 60 * 60 * 1000`)
5. **line 235~237**: `export function dismissInstallPrompt()` — `localStorage.setItem('pwa-install-dismissed', String(Date.now()))`
6. **line 40~47**: `outcome` 3 분기 (`'accepted'` → `onDismiss()` / `'unavailable'` → `setShowAndroidGuide(true)` / `'dismissed'` → no-op)
7. **line 88, 112, 148**: 3 view state ternary — `!showIOSGuide && !showAndroidGuide` 메인 / `showAndroidGuide` / iOS (else)
8. **line 28, 32~35**: `useState<boolean>(() => !!getDeferredInstallPrompt())` initial + `useEffect(() => { const unsub = subscribeInstallPrompt(p => setHasPrompt(!!p)); return unsub }, [])` 구독 anchor

## 4. 비즈 로직 0 diff 보존 (W5 §10 sampling)

다음은 **변경 0** — 인라인 style 제거 외 logic 라인 손대지 않음:

### SplashScreen
- line 1~5 imports 5개 (useEffect/useState / useNavigate / useAuthStore / InstallPrompt+shouldShow+dismiss / checkVersionAndRefresh)
- line 7 `export default function SplashScreen()`
- line 8~11 useState (pct=0 / showInstall=false) + useNavigate + useAuthStore
- line 13~29 useEffect (tick + nav cleanup) — biz anchor 4건 포함
- line 31~35 handleDismiss
- line 76 `{showInstall && <InstallPrompt onDismiss={handleDismiss} />}`

### InstallPrompt
- line 1~2 imports 2개 (useState/useEffect / 3 PWA helpers)
- line 5~17 isStandalone() + isIOS() biz anchor 2건
- line 25 `export function InstallPrompt({ onDismiss }: { onDismiss: () => void })`
- line 28~30 useState 3종 (hasPrompt / showIOSGuide / showAndroidGuide)
- line 32~35 useEffect (subscribeInstallPrompt cleanup)
- line 37~48 handleInstallAndroid async + outcome 3 분기
- line 50~53 handleInstallIOS
- line 55 `const ios = isIOS()`
- line 91, 102, 142, 205 onClick 바인딩 (ios ? handleInstallIOS : handleInstallAndroid / onDismiss × 3)
- line 224~237 shouldShowInstallPrompt + dismissInstallPrompt export

### 카피 verbatim
- SplashScreen: 'CBC 방재' / '소방안전 통합관리 시스템' / '시스템 초기화 중...' / '데이터 불러오는 중...' / '준비 완료' / 'v{__APP_VERSION__} · 경기도 성남시 분당구'
- InstallPrompt: 'CBC 방재' / '홈 화면에 앱을 설치하면<br/>더 빠르고 편리하게 사용할 수 있습니다' / '설치 방법 보기' / '홈 화면에 설치' / '나중에 할게요' / '자동 설치 창이 뜨지 않으면 아래 순서로 설치해 주세요.' / '크롬 우상단 ⋮ 메뉴' / '주소창 오른쪽 점 세 개 메뉴를 누르세요' / "'앱 설치' 또는 '홈 화면에 추가' 선택" / '앱 설치' / '홈 화면에 추가' (3회) / "'설치' 확인" / '설치' / '팝업에서 ... 누르면 완료' / '확인했습니다' (2회) / '하단 공유 버튼 터치' / 'Safari 하단의 ⎋ 공유 아이콘을 누르세요' / "'홈 화면에 추가' 선택" / '스크롤해서 ... 찾아 누르세요' / "'추가' 터치" / '오른쪽 상단 추가 버튼을 누르면 완료!'

## 5. 작업 순서

1. `wc -l` 으로 양쪽 라인 수 확인 (SplashScreen 83 + InstallPrompt 237)
2. SplashScreen.tsx 한 번 Read 로 전체 83 라인 읽기
3. InstallPrompt.tsx 한 번 Read 로 전체 237 라인 읽기
4. Edit / Write 로 in-place 수정 — 순서:
   - **SplashScreen.tsx** 먼저 (더 작음, 비즈 anchor 8건 박제)
     - §3.1 외곽 + 로고 + 타이틀 + 부제 (line 38~60)
     - §3.2 진행 바 + 진행 라벨 + 버전 + InstallPrompt 마운트 + style 블록 (line 63~80)
   - **InstallPrompt.tsx** 다음 (더 큼, ⋮ ⎋ 글리프 anchor + 비즈 anchor 8건)
     - §3.3 오버레이 + 카드 + 아이콘 + 제목 + 부제 (line 57~86)
     - §3.4 메인 view 설치 + 나중에 (line 88~111)
     - §3.5 Android 가이드 view (line 112~147) — ★ ⋮ U+22EE line 122
     - §3.6 iOS 가이드 view (line 148~215) — ★ ⎋ U+238B line 164
5. `cd cha-bio-safety && npx tsc --noEmit` 실행 → 0 errors 확인
6. `cd cha-bio-safety && npm run build` 실행 → tsc 0 + Vite build PASS 확인
7. §5 negative gate + §6 positive gate + §비즈 anchor 16건 + §scope 모두 PASS 확인
8. atomic 1-commit (2 파일 묶음):
   ```
   git add cha-bio-safety/src/pages/SplashScreen.tsx cha-bio-safety/src/components/InstallPrompt.tsx .planning/quick/260522-4i9-redesign-28-splash-tsx-splashscreen-inst/
   git commit -m "tsx(quick-260522-4i9): redesign/28-splash SplashScreen + InstallPrompt v0.1.1 토큰 변환 (2 파일 atomic + W2/W3/W4 sketch verbatim + OQ 5건 LOCKED + 비즈 anchor 16건 + ⋮ ⎋ 글리프 보존)"
   ```

## 6. 금지 사항

- wrangler 명령 0건 (CLAUDE.local.md 강제, 디자인 워크트리)
- npm run deploy 0건
- SplashScreen.tsx + InstallPrompt.tsx 외 파일 수정 0건 (pwaInstall.ts / versionCheck.ts / App.tsx / tailwind.config.js / tokens.css / typography.css / hooks/ / functions/ / templates/ / migrations/ / public/ 모두 무영향)
- `bg-status-safe-bar` / `text-status-warning` / `text-status-accent` 등 status- prefix className 0건 (memory `feedback_tailwind_token_class_pattern`)
- `w-8` / `h-8` 토큰 사용 0건 (=48px 함정, memory `feedback_tailwind_w8_h8_is_48px`)
- `var(--bg|bg2|bg3|bd|bd2|t1|t2|t3|acl|accent)` 잔존 0건 (이미 28-splash 원본에 alias 없음, 가드)
- `linear-gradient(135deg, #2563eb, #0ea5e9)` (InstallPrompt 설치 버튼) 0건 (OQ #1 LOCKED 완전 폐기)
- `linear-gradient(90deg,#3b82f6,#0ea5e9)` (SplashScreen 진행 바) 0건 (OQ #2 LOCKED 완전 폐기)
- `linear-gradient` 일반 0건 (2 파일 모두)
- fontSize 9·10·11 인라인 0건 (OQ #3 LOCKED 격상, 단 fontSize:16 ⋮ ⎋ 글리프는 OQ #5 LOCKED 예외)
- Lucide MoreVertical / Share / 기타 icon 으로 ⋮ ⎋ 교체 0건 (OQ #5 LOCKED 강제)
- 이모지 0건 — `[😀-🛿🤀-🧿]` 범위 (sketch + 본문 모두 이모지 없음, ⋮ ⎋ 는 emoji range 외부 OK)
- 비즈 로직 (state/handler/effect/hook/카피/PWA 이벤트 구독 + 24h TTL + 3 view state) 변경 0건
- **★ 비즈 anchor 16건 (SplashScreen 8 + InstallPrompt 8) 1 byte 변경 0건** — 17-annual-plan SW3 precedent
- **★ 특수 글리프 ⋮ U+22EE + ⎋ U+238B 인라인 fontSize:16 1 byte 변경 0건** — OQ #5 LOCKED
- pwaInstall.ts + versionCheck.ts 0 byte 변경 (final verify gate)

  </action>
  <verify>
    <automated>
# Path 변수
S="cha-bio-safety/src/pages/SplashScreen.tsx"
I="cha-bio-safety/src/components/InstallPrompt.tsx"

# ─── Negative gate (2 파일 각각, W5 §5 verbatim) ───
echo "=== Negative gate (SplashScreen) ==="
test "$(grep -oP '[\x{1F300}-\x{1FAFF}\x{2600}-\x{26FF}]' "$S" 2>/dev/null | wc -l | tr -d ' ')" = "0" && echo "neg-S1 (emoji) PASS" || echo "neg-S1 FAIL"
test "$(grep -c 'linear-gradient' "$S")" = "0" && echo "neg-S2 (no linear-gradient, OQ #2) PASS" || echo "neg-S2 FAIL"
test "$(grep -v '^\s*//' "$S" | grep -cE 'fontSize:\s*(9|10|11)[^0-9px]|font-size:\s*(9|10|11)[^0-9px]')" = "0" && echo "neg-S3 (no 9·10·11 fontSize) PASS" || echo "neg-S3 FAIL"
test "$(grep -cE '\b(text|bg|border)-status-(safe|fire|warning|danger|caution|accent)' "$S")" = "0" && echo "neg-S4 (no status- prefix) PASS" || echo "neg-S4 FAIL"
test "$(grep -cE '\bw-8\b|\bh-8\b' "$S")" = "0" && echo "neg-S5 (no w-8/h-8) PASS" || echo "neg-S5 FAIL"
test "$(grep -cE 'var\(--(bg|bg2|bg3|bd|bd2|t1|t2|t3|acl|accent)\)' "$S")" = "0" && echo "neg-S6 (no old alias) PASS" || echo "neg-S6 FAIL"

echo "=== Negative gate (InstallPrompt) ==="
# ⋮ U+22EE + ⎋ U+238B 는 emoji range 외부이므로 매칭 X
test "$(grep -oP '[\x{1F300}-\x{1FAFF}\x{2600}-\x{26FF}]' "$I" 2>/dev/null | wc -l | tr -d ' ')" = "0" && echo "neg-I1 (emoji, ⋮ ⎋ excluded by range) PASS" || echo "neg-I1 FAIL"
test "$(grep -c 'linear-gradient' "$I")" = "0" && echo "neg-I2 (no linear-gradient, OQ #1) PASS" || echo "neg-I2 FAIL"
# fontSize:16 글리프 ⋮ ⎋ 는 OQ #5 예외 — 9·10·11 만 검사
test "$(grep -v '^\s*//' "$I" | grep -cE 'fontSize:\s*(9|10|11)[^0-9px]|font-size:\s*(9|10|11)[^0-9px]')" = "0" && echo "neg-I3 (no 9·10·11 fontSize) PASS" || echo "neg-I3 FAIL"
test "$(grep -cE '\b(text|bg|border)-status-(safe|fire|warning|danger|caution|accent)' "$I")" = "0" && echo "neg-I4 (no status- prefix) PASS" || echo "neg-I4 FAIL"
test "$(grep -cE '\bw-8\b|\bh-8\b' "$I")" = "0" && echo "neg-I5 (no w-8/h-8) PASS" || echo "neg-I5 FAIL"
test "$(grep -cE 'var\(--(bg|bg2|bg3|bd|bd2|t1|t2|t3|acl|accent)\)' "$I")" = "0" && echo "neg-I6 (no old alias) PASS" || echo "neg-I6 FAIL"
# OQ #5 — Lucide 교체 금지 검사 (MoreVertical / Share 등 import 0건)
test "$(grep -cE 'from .lucide-react.' "$I")" = "0" && echo "neg-I7 (no lucide-react import, OQ #5 글리프 보존) PASS" || echo "neg-I7 FAIL"

# ─── Positive gate (W5 §6 verbatim) ───
echo "=== Positive gate (SplashScreen) ==="
test "$(grep -cE 'bg-surface-(page|raised|sunken)|border-border-(default|strong)|text-text-(primary|secondary|tertiary|disabled|on-accent)' "$S")" -ge 3 && echo "pos-S1 (v0.1.1 tokens ≥3) PASS" || echo "pos-S1 FAIL"
test "$(grep -c 'bg-surface-page' "$S")" -ge 1 && echo "pos-S2 (bg-surface-page) PASS" || echo "pos-S2 FAIL"
test "$(grep -cE '\bbg-accent\b' "$S")" -ge 1 && echo "pos-S3 (bg-accent 진행 바, OQ #2) PASS" || echo "pos-S3 FAIL"
test "$(grep -c 'text-heading' "$S")" -ge 1 && echo "pos-S4 (text-heading 타이틀, OQ #3) PASS" || echo "pos-S4 FAIL"
test "$(grep -c 'text-caption' "$S")" -ge 3 && echo "pos-S5 (text-caption ≥3 부제+진행라벨+버전) PASS" || echo "pos-S5 FAIL"
test "$(grep -c 'text-text-primary' "$S")" -ge 1 && echo "pos-S6 (text-text-primary 타이틀) PASS" || echo "pos-S6 FAIL"
test "$(grep -c 'text-text-tertiary' "$S")" -ge 2 && echo "pos-S7 (text-text-tertiary 부제+진행라벨) PASS" || echo "pos-S7 FAIL"
test "$(grep -c 'text-text-disabled' "$S")" -ge 1 && echo "pos-S8 (text-text-disabled 버전) PASS" || echo "pos-S8 FAIL"
test "$(grep -c 'font-black' "$S")" -ge 1 && echo "pos-S9 (font-black 타이틀) PASS" || echo "pos-S9 FAIL"
test "$(grep -c 'tracking-tight' "$S")" -ge 1 && echo "pos-S10 (tracking-tight 타이틀) PASS" || echo "pos-S10 FAIL"
test "$(grep -c 'leading-none' "$S")" -ge 2 && echo "pos-S11 (leading-none ≥2 진행라벨+버전) PASS" || echo "pos-S11 FAIL"
test "$(grep -c 'rounded-\[22px\]' "$S")" -ge 1 && echo "pos-S12 (rounded-[22px] 로고 박스) PASS" || echo "pos-S12 FAIL"
test "$(grep -c 'rgba(255,255,255,0.07)' "$S")" -ge 1 && echo "pos-S13 (인라인 1/4 진행 바 트랙) PASS" || echo "pos-S13 FAIL"

echo "=== Positive gate (InstallPrompt) ==="
test "$(grep -cE 'bg-surface-(page|raised|sunken)|border-border-(default|strong)|text-text-(primary|secondary|tertiary|disabled|on-accent)' "$I")" -ge 5 && echo "pos-I1 (v0.1.1 tokens ≥5) PASS" || echo "pos-I1 FAIL"
test "$(grep -c 'bg-surface-raised' "$I")" -ge 1 && echo "pos-I2 (bg-surface-raised 카드) PASS" || echo "pos-I2 FAIL"
test "$(grep -c 'bg-safe-bar' "$I")" -ge 1 && echo "pos-I3 (bg-safe-bar 설치 버튼, OQ #1) PASS" || echo "pos-I3 FAIL"
test "$(grep -c 'text-text-on-accent' "$I")" -ge 1 && echo "pos-I4 (text-text-on-accent 설치 버튼) PASS" || echo "pos-I4 FAIL"
test "$(grep -c 'bg-accent/15' "$I")" -ge 4 && echo "pos-I5 (bg-accent/15 ≥4 숫자 원 6 + 확인했습니다 2 = 일부 grep 카운트) PASS" || echo "pos-I5 FAIL"
test "$(grep -c 'text-accent' "$I")" -ge 4 && echo "pos-I6 (text-accent ≥4) PASS" || echo "pos-I6 FAIL"
test "$(grep -c 'text-warning' "$I")" -ge 1 && echo "pos-I7 (text-warning 노란 경고, OQ #4) PASS" || echo "pos-I7 FAIL"
test "$(grep -c 'bg-warning-bg' "$I")" -ge 1 && echo "pos-I8 (bg-warning-bg) PASS" || echo "pos-I8 FAIL"
test "$(grep -c 'border-warning' "$I")" -ge 1 && echo "pos-I9 (border-warning) PASS" || echo "pos-I9 FAIL"
test "$(grep -c 'text-text-primary' "$I")" -ge 3 && echo "pos-I10 (text-text-primary ≥3 카드 제목+step 제목+strong) PASS" || echo "pos-I10 FAIL"
test "$(grep -c 'text-text-secondary' "$I")" -ge 3 && echo "pos-I11 (text-text-secondary ≥3 카드 부제+step 하위) PASS" || echo "pos-I11 FAIL"
test "$(grep -c 'text-text-tertiary' "$I")" -ge 1 && echo "pos-I12 (text-text-tertiary 나중에) PASS" || echo "pos-I12 FAIL"
test "$(grep -c 'text-title' "$I")" -ge 1 && echo "pos-I13 (text-title 카드 제목, OQ #3) PASS" || echo "pos-I13 FAIL"
test "$(grep -cE '\btext-body\b' "$I")" -ge 1 && echo "pos-I14 (text-body 설치 버튼, OQ #3) PASS" || echo "pos-I14 FAIL"
test "$(grep -c 'text-label' "$I")" -ge 6 && echo "pos-I15 (text-label ≥6 step 제목+숫자 원+확인했습니다) PASS" || echo "pos-I15 FAIL"
test "$(grep -c 'text-caption' "$I")" -ge 6 && echo "pos-I16 (text-caption ≥6 카드 부제+나중에+노란 경고+step 하위) PASS" || echo "pos-I16 FAIL"
test "$(grep -c 'leading-none' "$I")" -ge 5 && echo "pos-I17 (leading-none ≥5) PASS" || echo "pos-I17 FAIL"
test "$(grep -c 'leading-relaxed' "$I")" -ge 1 && echo "pos-I18 (leading-relaxed ≥1 카드 부제) PASS" || echo "pos-I18 FAIL"
test "$(grep -c 'font-extrabold' "$I")" -ge 1 && echo "pos-I19 (font-extrabold ≥1) PASS" || echo "pos-I19 FAIL"
test "$(grep -c 'rounded-\[20px\]' "$I")" -ge 1 && echo "pos-I20 (rounded-[20px] 카드) PASS" || echo "pos-I20 FAIL"
test "$(grep -c 'rounded-lg' "$I")" -ge 1 && echo "pos-I21 (rounded-lg 아이콘 박스) PASS" || echo "pos-I21 FAIL"
test "$(grep -c 'rounded-md' "$I")" -ge 3 && echo "pos-I22 (rounded-md ≥3 설치+나중에+확인했습니다×2) PASS" || echo "pos-I22 FAIL"
test "$(grep -c 'rounded-sm' "$I")" -ge 7 && echo "pos-I23 (rounded-sm ≥7 노란 경고+숫자 원 6) PASS" || echo "pos-I23 FAIL"
test "$(grep -c 'rgba(59,130,246,0.3)' "$I")" -ge 2 && echo "pos-I24 (인라인 2/4 카드 border + 아이콘 + 확인했습니다) PASS" || echo "pos-I24 FAIL"
test "$(grep -c 'rgba(0,0,0,0.85)' "$I")" -ge 1 && echo "pos-I25 (인라인 3/4 오버레이) PASS" || echo "pos-I25 FAIL"
test "$(grep -cE 'boxShadow.*rgba\(0,0,0,0\.5\)|0 8px 40px rgba\(0,0,0,0\.5\)' "$I")" -ge 1 && echo "pos-I26 (인라인 4/4 boxShadow) PASS" || echo "pos-I26 FAIL"

# ─── ★ 비즈 anchor 16건 보존 ───
echo "=== Biz anchor (SplashScreen 8) ==="
test "$(grep -c '__APP_VERSION__' "$S")" -ge 1 && echo "anchor-S1 (__APP_VERSION__) PASS" || echo "anchor-S1 FAIL"
test "$(grep -c '@keyframes slideUp' "$S")" -ge 1 && echo "anchor-S2 (@keyframes slideUp) PASS" || echo "anchor-S2 FAIL"
test "$(grep -c ', 48)' "$S")" -ge 1 && echo "anchor-S3 (setInterval 48ms) PASS" || echo "anchor-S3 FAIL"
test "$(grep -c ', 1600)' "$S")" -ge 1 && echo "anchor-S4 (setTimeout 1600ms) PASS" || echo "anchor-S4 FAIL"
test "$(grep -c 'p + 4' "$S")" -ge 1 && echo "anchor-S5 (+4 increment per tick) PASS" || echo "anchor-S5 FAIL"
test "$(grep -c 'checkVersionAndRefresh' "$S")" -ge 1 && echo "anchor-S6 (checkVersionAndRefresh) PASS" || echo "anchor-S6 FAIL"
test "$(grep -c 'shouldShowInstallPrompt' "$S")" -ge 1 && echo "anchor-S7 (shouldShowInstallPrompt) PASS" || echo "anchor-S7 FAIL"
test "$(grep -c \"isAuthenticated ? '/dashboard' : '/login'\" "$S")" -ge 2 && echo "anchor-S8 (isAuthenticated 분기 2건) PASS" || echo "anchor-S8 FAIL"

echo "=== Biz anchor (InstallPrompt 8) ==="
test "$(grep -c 'isStandalone' "$I")" -ge 2 && echo "anchor-I1 (isStandalone 정의+사용) PASS" || echo "anchor-I1 FAIL"
test "$(grep -c 'isIOS' "$I")" -ge 2 && echo "anchor-I2 (isIOS 정의+사용) PASS" || echo "anchor-I2 FAIL"
test "$(grep -cE 'getDeferredInstallPrompt|subscribeInstallPrompt|showInstallPrompt' "$I")" -ge 3 && echo "anchor-I3 (3 PWA imports + 사용) PASS" || echo "anchor-I3 FAIL"
test "$(grep -c \"'pwa-install-dismissed'\" "$I")" -ge 2 && echo "anchor-I4 (localStorage key 2건) PASS" || echo "anchor-I4 FAIL"
test "$(grep -c '24 \* 60 \* 60 \* 1000' "$I")" -ge 1 && echo "anchor-I5 (24h TTL) PASS" || echo "anchor-I5 FAIL"
test "$(grep -cE \"'accepted'|'unavailable'|'dismissed'\" "$I")" -ge 2 && echo "anchor-I6 (outcome 3 분기) PASS" || echo "anchor-I6 FAIL"
test "$(grep -c 'showIOSGuide' "$I")" -ge 3 && echo "anchor-I7 (showIOSGuide ≥3 state+분기+toggle) PASS" || echo "anchor-I7 FAIL"
test "$(grep -c 'showAndroidGuide' "$I")" -ge 3 && echo "anchor-I8 (showAndroidGuide ≥3) PASS" || echo "anchor-I8 FAIL"

# ─── ★ 특수 글리프 (OQ #5 LOCKED) ───
echo "=== Special glyph (OQ #5 LOCKED) ==="
test "$(grep -c '⋮' "$I")" -ge 1 && echo "glyph-1 (⋮ U+22EE Android Chrome 메뉴) PASS" || echo "glyph-1 FAIL"
test "$(grep -c '⎋' "$I")" -ge 1 && echo "glyph-2 (⎋ U+238B iOS Safari 공유) PASS" || echo "glyph-2 FAIL"
test "$(grep -c \"verticalAlign: 'middle'\" "$I")" -ge 1 && echo "glyph-3 (⎋ verticalAlign middle 보존) PASS" || echo "glyph-3 FAIL"
test "$(grep -cE 'fontSize: ?16' "$I")" -ge 2 && echo "glyph-4 (글리프 fontSize:16 2건 ⋮+⎋) PASS" || echo "glyph-4 FAIL"

# ─── 카피 verbatim (SplashScreen) ───
echo "=== Copy verbatim (SplashScreen) ==="
test "$(grep -c 'CBC 방재' "$S")" -ge 1 && echo "copy-S1 (CBC 방재) PASS" || echo "copy-S1 FAIL"
test "$(grep -c '소방안전 통합관리 시스템' "$S")" -ge 1 && echo "copy-S2 (소방안전 통합관리 시스템) PASS" || echo "copy-S2 FAIL"
test "$(grep -cE '시스템 초기화 중|데이터 불러오는 중|준비 완료' "$S")" -ge 3 && echo "copy-S3 (진행 3 단계 카피) PASS" || echo "copy-S3 FAIL"
test "$(grep -c '경기도 성남시 분당구' "$S")" -ge 1 && echo "copy-S4 (경기도 성남시 분당구) PASS" || echo "copy-S4 FAIL"

# ─── 카피 verbatim (InstallPrompt) ───
echo "=== Copy verbatim (InstallPrompt) ==="
test "$(grep -c 'CBC 방재' "$I")" -ge 1 && echo "copy-I1 (CBC 방재) PASS" || echo "copy-I1 FAIL"
test "$(grep -c '홈 화면에 앱을 설치하면' "$I")" -ge 1 && echo "copy-I2 (홈 화면에 앱을 설치하면) PASS" || echo "copy-I2 FAIL"
test "$(grep -c '설치 방법 보기' "$I")" -ge 1 && echo "copy-I3 (설치 방법 보기) PASS" || echo "copy-I3 FAIL"
test "$(grep -c '홈 화면에 설치' "$I")" -ge 1 && echo "copy-I4 (홈 화면에 설치) PASS" || echo "copy-I4 FAIL"
test "$(grep -c '나중에 할게요' "$I")" -ge 1 && echo "copy-I5 (나중에 할게요) PASS" || echo "copy-I5 FAIL"
test "$(grep -c '자동 설치 창이 뜨지 않으면' "$I")" -ge 1 && echo "copy-I6 (Android 노란 경고) PASS" || echo "copy-I6 FAIL"
test "$(grep -c '크롬 우상단' "$I")" -ge 1 && echo "copy-I7 (Android step 1) PASS" || echo "copy-I7 FAIL"
test "$(grep -c '하단 공유 버튼 터치' "$I")" -ge 1 && echo "copy-I8 (iOS step 1) PASS" || echo "copy-I8 FAIL"
test "$(grep -c '홈 화면에 추가' "$I")" -ge 3 && echo "copy-I9 (홈 화면에 추가 ≥3 step 2+strong 2) PASS" || echo "copy-I9 FAIL"
test "$(grep -c '확인했습니다' "$I")" -ge 2 && echo "copy-I10 (확인했습니다 Android+iOS) PASS" || echo "copy-I10 FAIL"

# ─── pwaInstall.ts + versionCheck.ts 0 byte 변경 ───
echo "=== Utils preserve ==="
test "$(git diff HEAD~ HEAD -- cha-bio-safety/src/utils/pwaInstall.ts 2>/dev/null | wc -l | tr -d ' ')" = "0" && echo "utils-1 (pwaInstall.ts 0 diff) PASS" || echo "utils-1 FAIL"
test "$(git diff HEAD~ HEAD -- cha-bio-safety/src/utils/versionCheck.ts 2>/dev/null | wc -l | tr -d ' ')" = "0" && echo "utils-2 (versionCheck.ts 0 diff) PASS" || echo "utils-2 FAIL"

# ─── Scope (정확히 2 파일 + PLAN.md 만) ───
echo "=== Scope ==="
EXTRA=$(git diff --name-only HEAD~ HEAD 2>/dev/null | grep -vE 'cha-bio-safety/src/pages/SplashScreen\.tsx|cha-bio-safety/src/components/InstallPrompt\.tsx|^\.planning/quick/260522-4i9-')
test -z "$EXTRA" && echo "scope (only 2 tsx files + PLAN.md) PASS" || echo "scope FAIL — extra: $EXTRA"

# ─── Line count sanity ───
echo "=== Line count ==="
LCS=$(wc -l < "$S" | tr -d ' ')
LCI=$(wc -l < "$I" | tr -d ' ')
test "$LCS" -ge 58 -a "$LCS" -le 110 && echo "lc-S ($LCS lines, 83 ± 25) PASS" || echo "lc-S FAIL ($LCS lines)"
test "$LCI" -ge 187 -a "$LCI" -le 290 && echo "lc-I ($LCI lines, 237 ± 50) PASS" || echo "lc-I FAIL ($LCI lines)"

# ─── Build gate ───
echo "=== Build ==="
(cd cha-bio-safety && npm run build 2>&1 | tail -5)
echo "=== SplashScreen + InstallPrompt chunk size ==="
ls -la cha-bio-safety/dist/assets/SplashScreen-*.js 2>/dev/null || echo "(SplashScreen chunk — main 번들 포함 가능)"
ls -la cha-bio-safety/dist/assets/InstallPrompt-*.js 2>/dev/null || echo "(InstallPrompt chunk — main 번들 포함 가능)"
    </automated>
  </verify>
  <done>
- SplashScreen.tsx + InstallPrompt.tsx 2 파일 모두 in-place 수정 완료. SplashScreen 라인 수 83 ± 25 / InstallPrompt 237 ± 50.
- Negative gate (SplashScreen 6건 + InstallPrompt 7건) 모두 PASS: 이모지·linear-gradient·9·10·11 fontSize·status- prefix·w-8·h-8·var(--alias)·lucide-react import 0건 (lucide-react import 0 = OQ #5 글리프 보존 증거)
- Positive gate (SplashScreen 13건 + InstallPrompt 26건) 모두 PASS — v0.1.1 토큰 카운트 + 폰트 토큰 + radius 토큰 + 인라인 유지 4건 + Lucide 미사용
- ★ 비즈 anchor 16건 (SplashScreen 8 + InstallPrompt 8) 모두 PASS: __APP_VERSION__ / @keyframes slideUp / 48ms / 1600ms / +4 / checkVersionAndRefresh / shouldShowInstallPrompt / isAuthenticated×2 / isStandalone / isIOS / 3 PWA imports / pwa-install-dismissed×2 / 24h TTL / outcome 3 분기 / showIOSGuide / showAndroidGuide
- ★ 특수 글리프 4건 PASS: ⋮ U+22EE 1건 / ⎋ U+238B 1건 / verticalAlign 'middle' 1건 / fontSize:16 2건
- 카피 verbatim (SplashScreen 4건 + InstallPrompt 10건) 모두 PASS
- pwaInstall.ts + versionCheck.ts 0 byte 변경 (utils-1 + utils-2 PASS)
- Scope: cha-bio-safety/src/pages/SplashScreen.tsx + cha-bio-safety/src/components/InstallPrompt.tsx + .planning/quick/260522-4i9-*/ 외 파일 변경 0건
- `cd cha-bio-safety && npm run build` exit 0 (tsc 0 errors + Vite PASS), SplashScreen + InstallPrompt chunk size 보고
- Atomic 1-commit (2 파일 묶음): `tsx(quick-260522-4i9): redesign/28-splash SplashScreen + InstallPrompt v0.1.1 토큰 변환 (2 파일 atomic + W2/W3/W4 sketch verbatim + OQ 5건 LOCKED + 비즈 anchor 16건 + ⋮ ⎋ 글리프 보존)`
  </done>
</task>

</tasks>

<verification>
## 전체 PLAN 통과 조건

1. **Negative gate (2 파일 각각)** — 이모지 0 / linear-gradient 0 / 9·10·11 fontSize 0 / status- prefix 0 / w-8·h-8 0 / 옛 alias 0 / InstallPrompt lucide-react import 0
2. **Positive gate (SplashScreen 13 + InstallPrompt 26)** — v0.1.1 토큰 + Lucide 미사용 (글리프 anchor) + 폰트 토큰 + radius 토큰 + 인라인 유지 4건
3. **★ 비즈 anchor 16건** — SplashScreen 8 (__APP_VERSION__ / @keyframes slideUp / 48ms / 1600ms / +4 / checkVersionAndRefresh / shouldShowInstallPrompt / isAuthenticated×2) + InstallPrompt 8 (isStandalone / isIOS / 3 PWA imports / pwa-install-dismissed×2 / 24h TTL / outcome 3 분기 / showIOSGuide / showAndroidGuide)
4. **★ 특수 글리프 4건** — ⋮ U+22EE (1) / ⎋ U+238B (1) / verticalAlign 'middle' (1) / fontSize:16 (2건)
5. **카피 verbatim** — SplashScreen 4건 (CBC 방재 / 소방안전 통합관리 시스템 / 진행 3 단계 / 경기도 성남시 분당구) + InstallPrompt 10건
6. **pwaInstall.ts + versionCheck.ts 0 byte 변경** — git diff 0 lines
7. **Scope** — 2 파일 변경 (SplashScreen.tsx + InstallPrompt.tsx) + PLAN.md/SUMMARY.md
8. **Build** — `npm run build` exit 0
9. **Atomic 1-commit** — 2 파일 묶음 단일 commit

## 검수 시각 (cbc7119-preview 자동 배포 후)

- 모바일 viewport: SplashScreen 외곽 = `bg-surface-page` (#161b22 톤) + 진행 바 fill = `bg-accent` solid (그라데이션 없음) + 타이틀 "CBC 방재" `text-heading font-black tracking-tight` + 부제 "소방안전 통합관리 시스템" `text-caption leading-relaxed text-text-tertiary` + 진행 라벨 격상 (10→12) + 버전 격상 (10→12)
- InstallPrompt 모달 표시 (PWA 미설치 시):
  - 오버레이 `rgba(0,0,0,0.85)` 인라인 (어두운 반투명 배경)
  - 카드 `bg-surface-raised rounded-[20px]` (#1c2128 톤 + radius 20)
  - 카드 제목 "CBC 방재" `text-title font-extrabold` (18)
  - 카드 부제 `text-caption leading-relaxed text-text-secondary` (12 + 줄바꿈 2 행)
  - 설치 버튼 default = `bg-safe-bar text-text-on-accent` (solid 파랑, 그라데이션 없음) + `text-body font-bold` (16 격상)
  - 나중에 버튼 `text-text-tertiary text-caption font-bold leading-none rounded-md`
- Android 가이드 분기 (showAndroidGuide true):
  - 노란 경고 = `text-warning bg-warning-bg border-warning/25` (status- prefix 없는 토큰 3종)
  - 숫자 원 = `bg-accent/15 text-accent text-label font-extrabold leading-none rounded-sm` 6건
  - step 제목 = `text-label leading-none text-text-primary`
  - step 하위 설명 = `text-caption leading-relaxed text-text-secondary` (11→12 격상)
  - ★ step 1 안 ⋮ U+22EE 글리프 `fontSize:16` (Lucide 교체 X)
  - 확인했습니다 = `bg-accent/15 text-accent text-label font-bold leading-none rounded-md`
- iOS 가이드 분기 (else):
  - 동일 토큰 패턴 (노란 경고 없음)
  - ★ step 1 안 ⎋ U+238B 글리프 `fontSize:16 + verticalAlign middle` (Lucide 교체 X)
- 새로고침 후 24h TTL dismiss 동작 정상 (localStorage 'pwa-install-dismissed')
- isAuthenticated 분기 시 navigate '/dashboard' / '/login' 정상
- __APP_VERSION__ Vite define 빌드 시 인라인 치환 (예: `v1.2.3 · 경기도 성남시 분당구`)
- @keyframes slideUp 로고 등장 애니메이션 정상

## 사용자 컨펌 후 다음 단계

- `main` 머지 (사용자 명시 컨펌 후, memory `feedback_deploy_test`)
- GitHub Actions → cbc7119-preview 자동 배포
- 직원 도메인 (cbc7119) 배포는 별도 worktree (20260328) 담당 (이 워크트리는 절대 다루지 않음, CLAUDE.local.md)
- `project_redesign_28_splash_status` 메모리 박제 (신규)
</verification>

<success_criteria>
- [ ] SplashScreen.tsx 83 ± 25 lines, in-place 수정
- [ ] InstallPrompt.tsx 237 ± 50 lines, in-place 수정
- [ ] Negative gate (SplashScreen 6 + InstallPrompt 7) 모두 PASS (이모지 / linear-gradient / 9·10·11 fontSize / status- prefix / w-8·h-8 / 옛 alias / lucide-react import 0)
- [ ] Positive gate (SplashScreen 13 + InstallPrompt 26) 모두 임계치 이상
- [ ] ★ 비즈 anchor 16건 (SplashScreen 8 + InstallPrompt 8) 모두 PASS
- [ ] ★ 특수 글리프 4건 (⋮ / ⎋ / verticalAlign middle / fontSize:16 2건) 모두 PASS
- [ ] 카피 verbatim (SplashScreen 4 + InstallPrompt 10) 모두 PASS
- [ ] pwaInstall.ts + versionCheck.ts 0 byte 변경
- [ ] 다른 파일 변경 0건
- [ ] `npm run build` exit 0
- [ ] Atomic 1-commit (2 파일 묶음)
- [ ] OQ #1: InstallPrompt bg-safe-bar ≥1 + linear-gradient 0
- [ ] OQ #2: SplashScreen bg-accent ≥1 + linear-gradient 0
- [ ] OQ #3: fontSize 9·10·11 인라인 0 (양쪽) + text-heading/title/body/label/caption 토큰 카운트 충족
- [ ] OQ #4: 토큰 치환 다수 + 인라인 유지 4건 (트랙 / 카드 border / 오버레이 / boxShadow) 모두 grep PASS
- [ ] OQ #5: ⋮ + ⎋ + verticalAlign middle + fontSize:16 모두 보존 + lucide-react import 0
</success_criteria>

<output>
After completion, create `.planning/quick/260522-4i9-redesign-28-splash-tsx-splashscreen-inst/260522-4i9-SUMMARY.md` documenting:

- 변환 전후 SplashScreen.tsx + InstallPrompt.tsx wc -l 양쪽
- 적용된 v0.1.1 토큰 className 목록 (grep 카운트 2 파일 각각)
- OQ #1~#5 LOCKED 각 적용 결과 확인
- ★ 비즈 anchor 16건 (SplashScreen 8 + InstallPrompt 8) 1 byte 변경 0 검증 결과 (17-annual-plan SW3 precedent mirror)
- ★ 특수 글리프 ⋮ U+22EE + ⎋ U+238B + verticalAlign 'middle' + fontSize:16 보존 결과
- Negative gate (S6 + I7) + Positive gate (S13 + I26) + Biz anchor 16 + Glyph 4 + Copy (S4+I10) + Utils 2 + Scope + LC 2 PASS 결과 (verify automated 출력 첨부)
- `npm run build` 결과 (SplashScreen + InstallPrompt chunk size 포함)
- atomic commit hash + main 머지 대기 상태 명시
- 다음 단계 (사용자 컨펌 → main 머지 → cbc7119-preview 자동 배포 → `project_redesign_28_splash_status` 메모리 박제)
</output>
</content>
</invoke>