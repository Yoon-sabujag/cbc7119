---
quick_id: 260520-u3b
mode: quick
phase: quick
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - cha-bio-safety/docs/redesign-context/14-reports/wave-7-tsx-conversion-checklist.md
autonomous: true
requirements:
  - redesign/14-reports W7 — TSX 변환 verify checklist markdown 단일 산출
user_setup: []

must_haves:
  truths:
    - "wave-7-tsx-conversion-checklist.md 1개 파일이 14-reports/ 평면 sibling 으로 생성된다."
    - "13-schedule W7 mirror 구조 — §1~§12 12 sections 모두 채움 (빈 section 0)."
    - "W2~W6 sketch 의 모든 CSS class 정의 ≥30개가 verbatim fence 로 인용된다."
    - "W7 OQ #1 (components.css 파일 위치), #2 (SELECT_STYLE 처리), #3 (iconBtn/navBtn), #4 (a4 placeholder), #5 (chunk size impact) ≥4건 default 명시 후 박제."
    - "Tailwind cheatsheet 표 ≥28 row (surface 5 + text 6 + border 3 + status 15 + typography 7 + radius 4 + lucide 4)."
    - "비즈 로직 보존 체크리스트 ≥15 행 — downloadReport / generateReportBlob / downloadAllAsZip / handleDownload / handleDownloadAll / ASSISTANTS / REPORT_CARDS / MATRIX_CONFIG / DESKTOP_SECTIONS / api.get / zipSync 모두 명시."
    - "3 Sub-wave (SW1 = components.css 신규 / SW2 = MobileReportsPage / SW3 = DesktopReportsPage) 분할 + 각 sub-wave 별 verify gate (SW1 8 + SW2 10 + SW3 10 = 28+) 박제."
    - "메모리 룰 unique feedback_*.md ≥12 inline 인용 (W1 10건 inherit + check_branch + tsx_wave_emoji_dot_gap 추가 강조)."
    - "src/** + migrations/** + scripts/** 코드 수정 0 (markdown 1 파일만 변경)."
    - "ExcelPreview.tsx 변경 0 — W6 OQ #1 LOCKED 일관, W7 checklist 안에 '무변경' 명시."
    - "wrangler 명령 0 / `npm run deploy` 0 (워크트리 룰 박제)."
    - "git diff --stat HEAD~..HEAD = 1 file changed (md 1개) + 1 commit (atomic)."
  artifacts:
    - path: "cha-bio-safety/docs/redesign-context/14-reports/wave-7-tsx-conversion-checklist.md"
      provides: "TSX 변환 wave (SW1~SW3) executor 의 단일 진입점 markdown"
      min_lines: 500
      contains: "§1. 개요 / §2. W1~W6 LOCKED 인용표 / §3. CSS class verbatim / §4. Tailwind cheatsheet / §5. NEGATIVE scope / §6. 3 Sub-wave 분할 / §7. source line ref / §8. 메모리 룰 / §9. 인라인 style 화이트리스트 / §10. 비즈 로직 보존 verify / §11. verify gate per sub-wave / §12. OQ"
    - path: ".planning/quick/260520-u3b-redesign-14-reports-sketch-wave-7-tsx-ve/260520-u3b-SUMMARY.md"
      provides: "12 sections 체크 + verify gate 결과 + W7 LOCKED 2건 박제 + TSX SW1 진입 전 OQ 5건 default"
      min_lines: 60
  key_links:
    - from: "wave-7-tsx-conversion-checklist.md §3"
      to: "sketch-wave-2~6.html 의 .class { ... } 정의"
      via: "verbatim fence (CSS 본문 그대로)"
      pattern: "\\.(report-card|toolbar|sidelist|preview-wrapper|a4-preview)"
    - from: "wave-7-tsx-conversion-checklist.md §6 SW1"
      to: "cha-bio-safety/src/styles/ 신규 components.css + main.tsx 또는 index.css import"
      via: "src/index.css 의 @import 추가 (현재 tokens.css + typography.css 2건 → components.css 3건)"
      pattern: "@import './styles/components\\.css'"
    - from: "wave-7-tsx-conversion-checklist.md §10"
      to: "ReportsPage.tsx 비즈 로직 시그니처 (downloadReport / generateReportBlob / downloadAllAsZip / handleDownload / handleDownloadAll)"
      via: "line ref 표 + grep 명령"
      pattern: "(downloadReport|generateReportBlob|downloadAllAsZip|handleDownload|handleDownloadAll)\\("
    - from: "wave-7-tsx-conversion-checklist.md §8"
      to: "feedback_*.md 12건 (메모리 룰 박제)"
      via: "inline 인용 + Why + How to apply (14-reports 컨텍스트)"
      pattern: "feedback_[a-z_]+\\.md"

threat_model_enabled: false
---

<objective>
redesign/14-reports 시리즈의 **마지막 sketch wave (W7)** — TSX 변환 wave (SW1~SW3) executor 가 1-pass 로 적용할 verify gate + region mapping + LOCKED 룰 박제 markdown 1개 파일 작성. 13-schedule W7 mirror 구조 (12 sections, 500~550 줄) 그대로 14-reports 컨텍스트로 재작성.

Purpose: W2~W6 sketch 의 모든 CSS class / 토큰 / LOCKED 결정 / 메모리 룰 / 비즈 로직 보존 룰을 한 곳에 박제해, TSX 변환 wave 가 sketch 6개 + ReportsPage.tsx + 본 checklist 만 input 으로 atomic 변환 가능하도록.

Output: `cha-bio-safety/docs/redesign-context/14-reports/wave-7-tsx-conversion-checklist.md` (≥500 lines, 12 sections) + `.planning/quick/260520-u3b-.../SUMMARY.md` (≥60 lines).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.local.md
@cha-bio-safety/docs/redesign-context/14-reports/wave-1-index.md
@cha-bio-safety/docs/redesign-context/13-schedule/wave-7-tsx-conversion-checklist.md

<!--
주: 본 wave 는 markdown 1개 산출. sketch 생성 0 / src 수정 0.
- W7 mirror 소스 = 13-schedule/wave-7-tsx-conversion-checklist.md (520줄, 12 sections, 16 LOCKED, 23 verify gates) — 본 wave 의 구조 reference.
- W1 인덱스 = 14-reports/wave-1-index.md (339줄) — LOCKED 5건 + 메모리 룰 10건 + §6 negative + §7 OQ 5건. 본 wave 의 룰 inherit source.
- sketch-wave-2~6.html = W2~W6 sketch — 본 wave 의 CSS class 추출 source. wc -l 합계 = 4627 줄.
- ReportsPage.tsx = 405 줄 source — 본 wave 의 line ref 박제 대상 (수정 0).
- ExcelPreview.tsx = 535 줄 — wrapper layout 만 손대고 내부 무변경 명시 source (수정 0).
- 워크트리 = cbc7119-design — wrangler 금지 + `npm run deploy` 금지 (워크트리 룰).
-->

<interfaces>
<!-- W7 산출물은 markdown 만. 코드 인터페이스 정의 0. -->
<!-- 단, TSX 변환 wave (SW1~SW3) 가 적용할 핵심 패턴 박제: -->

새 CSS class @layer 패턴 (SW1 신규 components.css):
```css
@layer components {
  /* W2~W6 sketch 의 .class 정의 verbatim — var(--surface-*) 토큰 사용 */
  .report-card { background: var(--surface-raised); border-radius: 12px; ... }
  .toolbar { background: var(--surface-raised); ... }
  .sidelist { width: 260px; ... }
  .preview-wrapper { background: var(--surface-page); ... }
  .a4-preview { background: #ffffff; ... }
  /* 등 ≥30 class */
}
```

main.tsx → src/index.css 의 @import 추가 (현재 tokens.css + typography.css 2건 → 3건):
```css
@import './styles/tokens.css';
@import './styles/typography.css';
@import './styles/components.css';  /* SW1 신규 */
```

ReportsPage.tsx 인터페이스 시그니처 (변경 0):
```typescript
function downloadReport(type: ReportType, year: number): Promise<void>
function generateReportBlob(type: ReportType, year: number): Promise<{ blob: Blob; filename: string } | null>
function downloadAllAsZip(year: number, month: number, onProgress: (msg: string) => void): Promise<void>
function handleDownload(type: ReportType): Promise<void>  // Desktop line 157~164 + Mobile line 320~327 동형
function handleDownloadAll(): Promise<void>  // Desktop line 166~173 만
type ReportType = 'div-early' | 'div-late' | '소화전' | '청정소화약제' | '비상콘센트'
                | '피난방화' | '방화셔터' | '제연' | '자탐' | '소방펌프'
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: wave-7-tsx-conversion-checklist.md (markdown 1 파일, 12 sections, ≥500 lines)</name>
  <files>cha-bio-safety/docs/redesign-context/14-reports/wave-7-tsx-conversion-checklist.md</files>

  <action>
    `cha-bio-safety/docs/redesign-context/14-reports/wave-7-tsx-conversion-checklist.md` 1개 파일을 새로 작성한다. **13-schedule/wave-7-tsx-conversion-checklist.md (520줄, 12 sections) 의 구조를 mirror** 하되 14-reports 컨텍스트로 재작성. 500~600 줄 target. sketch HTML 아님 — markdown 만.

    ---

    ## frontmatter

    ```yaml
    ---
    title: "14-reports — W7 TSX 변환 verify checklist"
    status: draft
    created: 2026-05-20
    quick_id: 260520-u3b
    branch: redesign/14-reports
    source_tsx: cha-bio-safety/src/pages/ReportsPage.tsx
    source_tsx_lines: 405
    source_preview: cha-bio-safety/src/components/ExcelPreview.tsx
    source_preview_lines: 535
    sketches_referenced: [W1, W2, W3, W4, W5, W6]
    locked_decisions: "W1: 5 / W2~W6 (각 OQ 0~3건 inherit) / W7 (본 wave): 2"
    sub_wave_count: 3
    verify_gate_count: "≥28 (SW1 8 + SW2 10 + SW3 10)"
    mirror_of: cha-bio-safety/docs/redesign-context/13-schedule/wave-7-tsx-conversion-checklist.md
    consumed_by: 14-reports TSX 변환 wave (SW1 = components.css 신규 / SW2 = MobileReportsPage / SW3 = DesktopReportsPage) executor
    ---
    ```

    ---

    ## §1. 개요 (Purpose / Scope / Status)

    - **Purpose**: TSX 변환 wave 진입 전 W2~W6 sketch 의 모든 룰 (CSS class / 토큰 / LOCKED 결정 / 메모리 룰) 을 한 곳에 박제. 13-schedule W7 mirror 구조 (12 sections) 그대로 14-reports 컨텍스트로 재작성.
    - **Scope**: `cha-bio-safety/src/pages/ReportsPage.tsx` (405 lines) v0.1.1 Tailwind + components.css @layer 변환 + `cha-bio-safety/src/components/ExcelPreview.tsx` (535 lines) 무변경.
    - **Status**: sketch W2~W6 완료 + 머지 + cbc7119-preview 배포 완료. TSX SW1 진입 전 마지막 sketch wave.
    - **본 checklist 자체 룰 (self-applied negative gate)**:
      - 이모지 0건 (U+1F300~1FAFF / 2600~27BF 범위 글리프 0)
      - ≥500 lines (13-schedule W7 520줄 mirror)
      - 12 sections (§1~§12) 모두 채움 — 빈 section 0
      - W1~W6 LOCKED 결정 verbatim 인용 ≥15 row
      - CSS class verbatim fence ≥30
      - 메모리 룰 unique feedback_*.md ≥12

    ---

    ## §2. W1~W7 LOCKED 인용표 (표 ≥15 row)

    표 컬럼: `| Wave | OQ | LOCKED 결정 | 본 wave 적용 |`

    포함 행 (작성 시 task_scope 의 §2 표 그대로 + 본 wave LOCKED 추가):
    - W1 #1: gradient → `bg-status-safe-bar` solid (모바일/데스크톱 일괄 다운로드)
    - W1 #2: 데스크톱 좌측 = 260px 유지 (sidelist width)
    - W1 #3: 일괄 다운로드 색 = `bg-status-safe-bar` (데스크톱 toolbar)
    - W1 #4: 모바일 footer = 유지 (MobileReportsPage 마지막 div)
    - W1 #5: sub line 가운뎃점 → dot span (카드 sub + 섹션 B 라벨)
    - W3 #1/#2/#3: 모바일 = 데스크톱 placeholder/칩/hover 미배치 (모바일 sketch X)
    - W4 #1/#2/#3: 데스크톱 toolbar sticky/footer inline/2열 그리드 처리 룰
    - W5 #1/#2/#3: confirm 모달 미적용 / 개별 버튼 = surface-sunken (보조 위계) / zip 완료 toast 미적용
    - W6 #1/#2/#3: ExcelPreview 무변경 / A4 흰배경 고정 / 빈 상태 UI 불필요
    - **W7 #1 (본 wave LOCKED)**: CSS class 처리 = @layer components 신규 파일 (src/styles/components.css 추가)
    - **W7 #2 (본 wave LOCKED)**: sub-wave 분할 = 3 sub-wave (SW1 = components.css 신규 / SW2 = MobileReportsPage / SW3 = DesktopReportsPage), 각 atomic 1-commit

    **W1~W7 LOCKED 인용 누적 ≥15 row**. status- prefix 룰 (memory) 적용 — `bg-safe-bar` (X) / `bg-status-safe-bar` (O) 명시.

    ---

    ## §3. W2~W6 CSS class 정의 verbatim 추출 (≥30 class fence)

    W2~W6 sketch HTML 의 `<style>` 블록에서 grep 으로 추출한 .class 정의를 fence 안에 verbatim 인용. 각 class 마다 source 파일 + line range 표기.

    **§3.1 W2/W3 공통 — 모바일 헤더 + 카드** (sketch-wave-2.html line 283~461, sketch-wave-3.html line 289~467 verbatim):

    포함 class (각 ` * { ... }` 본문 fence):
    - `.dot-meta` (W2 line 283~290 / W3 line 289~296) — width: 4px / height: 4px / border-radius: 9999px / background: var(--text-tertiary) / flex-shrink: 0
    - `.frame-shell` — 393px width frame
    - `.global-header-placeholder` — h-48 placeholder
    - `.page-header` (W2 line 326~334) — padding 8px 12px 9px / background var(--surface-raised) / border-bottom var(--border-default)
    - `.back-btn` (W2 line 337~349) — w/h 34px / border-radius 8px / background var(--surface-sunken) / border var(--border-default) / color var(--text-secondary)
    - `.page-title` (W2 line 351~357) — flex: 1 / font-size 18px / font-weight 700 / color var(--text-primary) / line-height 1.4
    - `.year-pager` / `.year-pager-slot` / `.year-nav-btn` (W2 line 359~386) — 28x28 / border-radius 7px / line-height 1
    - `.year-label` (W2 line 388~395) — width 44px / font-size 14px / line-height 1
    - `.page-body` (W2 line 400~403) — padding 12px 16px / background var(--surface-page)
    - `.report-card` (W2 line 410~416) — background var(--surface-raised) / border-radius 12px / border var(--border-default) / padding 14px / margin-bottom 10px
    - `.report-card-head` / `.report-card-title` / `.report-card-sub` (W2 line 418~437) — sub: margin-top 4px / font-size 12px / flex + gap 6px / line-height 1
    - `.report-card-btn` (W2 line 440~454) — width 100% / padding 12px / border-radius 8px / background var(--status-safe-bar) / color var(--text-on-accent) / font-size 16px / font-weight 700 / flex + gap 6px
    - `.report-card--loading .report-card-btn` (W2 line 457~461) — loading variant: background var(--surface-sunken) / color var(--text-tertiary) / cursor default
    - `.page-footer-note` (W3 line 461~467) — text-align center / padding 8px 16px 20px / font-size 12px / line-height 1.6 / color var(--text-tertiary)

    **§3.2 W4 — 데스크톱 toolbar** (sketch-wave-4.html line 337~426 verbatim):

    - `.toolbar` (line 337~345) — display flex / align-items center / gap 12px / padding 8px 16px / border-bottom var(--border-default) / background var(--surface-raised) / flex-shrink 0
    - `.toolbar-year-label` (line 348~352) — font-size 12px / color var(--text-secondary) / line-height 1
    - `.toolbar-select` (line 355~365) — background var(--surface-sunken) / color var(--text-primary) / border var(--border-strong) / border-radius 4px / padding 4px 8px / font-size 12px / line-height 1
    - `.toolbar-batch-btn` (line 369~384) — height 32px / padding 0 14px / background var(--status-safe-bar) / border 0 / border-radius 6px / color var(--text-on-accent) / font-size 12px / font-weight 700 / flex + gap 6px / line-height 1
    - `.toolbar-batch-btn--loading` (line 387~391) — background var(--surface-sunken) / color var(--text-tertiary) / cursor default
    - `.toolbar-spacer` (line 394) — flex: 1
    - `.toolbar-selected-title` (line 397~402) — font-size 14px / font-weight 700 / color var(--text-primary) / line-height 1
    - `.toolbar-individual-btn` (line 406~421) — height 32px / padding 0 14px / background var(--surface-sunken) / border var(--border-strong) / border-radius 6px / color var(--text-primary) / font-size 12px / font-weight 700 / flex + gap 6px / line-height 1
    - `.toolbar-individual-btn--loading` (line 423~426) — opacity 0.5 / cursor default

    **§3.3 W5 — 데스크톱 sidelist** (sketch-wave-5.html line 346~406 verbatim):

    - `.sidelist` (line 346~352) — width 260px / flex-shrink 0 / border-right var(--border-default) / overflow-y auto / background var(--surface-raised)
    - `.sidelist-section-header` (line 356~363) — padding 8px 16px 4px / font-size 12px / font-weight 700 / color var(--text-tertiary) / letter-spacing 0.05em / line-height 1
    - `.sidelist-row` (line 367~372) — padding 8px 16px / cursor pointer / background transparent / border-left 3px solid transparent
    - `.sidelist-row--selected` (line 375~378) — background var(--surface-sunken) / border-left 3px solid var(--accent)
    - `.sidelist-row--hover` (line 381~384) — background var(--surface-sunken) / border-left 3px solid transparent
    - `.sidelist-row-title` (line 388~392) — font-size 14px / color var(--text-primary) / font-weight 400
    - `.sidelist-row-title--selected` (line 395~398) — color var(--accent) / font-weight 700
    - `.sidelist-row-sub` (line 402~406) — font-size 12px / color var(--text-tertiary) / margin-top 1px

    **§3.4 W6 — 데스크톱 preview wrapper** (sketch-wave-6.html line 450~528 verbatim):

    - `.preview-wrapper` (line 450~458) — flex 1 / overflow hidden / background var(--surface-page) / display flex / align-items flex-start / justify-content center / padding 32px
    - `.a4-preview` (line 462~474) — width 100% / max-width 595px / aspect-ratio 210/297 / background #ffffff (다크/라이트 무관 고정) / border var(--border-default) / box-shadow 0 4px 20px rgba(0,0,0,0.08) / border-radius 4px / overflow hidden / display flex / flex-direction column / padding 32px 28px
    - `.a4-preview-title` (line 477~484) — font-size 18px / font-weight 700 / color #1f2328 / text-align center / margin-bottom 8px / letter-spacing -0.02em
    - `.a4-preview-meta` (line 487~493) — font-size 12px / color #656d76 / text-align center / margin-bottom 24px / letter-spacing 0.05em
    - `.a4-preview-table-placeholder` (line 496~509) — flex 1 / display grid / grid-template-columns 1.4fr repeat(12, 1fr) / border 1px dashed #d0d7de / border-radius 2px / font-size 12px / color #656d76
    - `.a4-preview-cell` (line 512~521) — padding 8px 4px / border 1px dashed #d0d7de / font-size 12px / color #656d76 / height 100% / display flex / align-items center / justify-content center
    - `.a4-preview-cell--header` (line 524~528) — background #f6f8fa / font-weight 700 / color #1f2328

    각 class 마다 ` * { ... }` 형식 fence + source 파일 (sketch-wave-N.html) + line range + 핵심 token mapping 1줄 코멘트. 총 unique class 정의 ≥30 (W2/W3 17 + W4 9 + W5 8 + W6 7 = 41).

    ---

    ## §4. Tailwind cheatsheet (잘못된 표기 박제 예방) — 표 ≥28 row

    표 컬럼: `| 옛 토큰 / hex | v0.1.1 Tailwind class | 토큰 CSS 변수 |`

    행 ≥28:
    - **surface 5종** (5 row): page / raised / sunken / active / overlay
    - **text 6종** (6 row): primary / secondary / tertiary / disabled / on-accent / link
    - **border 3종** (3 row): default / strong / accent
    - **status 15종** (15 row, 5 status × 3 페어): safe-bar / safe-soft / safe-on / warning-bar / warning-soft / warning-on / danger-bar / danger-soft / danger-on / info-bar / info-soft / info-on / fire-bar / fire-soft / fire-on
    - **typography 7종** (7 row): caption (12px) / label (13px) / body-sm (14px) / body (16px) / title (18px) / heading (22px) / display (28px)
    - **radius 4종** (4 row): sm (8) / md (12) / lg (16) / pill (9999)
    - **lucide 4종** (4 row): Download size=14 (모바일 카드 버튼) / ChevronLeft size=15 (모바일 뒤로) / ChevronLeft size=20 (옵션) / ChevronRight size=20 (옵션, 미사용)

    **§4.X status- prefix 룰** (메모리 룰 박제):
    - 정확 패턴 = `text-status-{safe|warning|danger|info|fire}-bar` (v0.1.1)
    - 잘못된 가정 = `text-safe-bar` (status- 없음) — TSX 변환 시 class 안 먹음
    - 11-div TSX v3 hotfix(4ce707e) 사고 + W3 SW3 실제 사고 사례 (`text-status-safe-bar` → `text-safe-bar` 정정 미적용 위험) 인용
    - 단, W2~W6 sketch 의 `<style>` 안에서는 `var(--status-safe-bar)` 토큰을 직접 사용했으므로 components.css 신규 파일 안에서도 토큰 사용. Tailwind class 형태로는 `bg-status-safe-bar` (prefix 포함) 가 정확.

    ---

    ## §5. NEGATIVE scope (변경 절대 금지) — ≥6 sub-section

    **§5.1 비즈 로직 보존 (시그니처 diff 0)** — task_scope §5.1 표 그대로:
    - `downloadReport(type, year)` (line 39~71) — verbatim, return type Promise<void>
    - `generateReportBlob(type, year)` (line 74~109) — return type Promise<{blob: Blob; filename: string} | null>
    - `downloadAllAsZip(year, month, onProgress)` (line 112~139) — fflate zipSync 호출 + URL.createObjectURL pattern
    - `handleDownload(type)` (Desktop line 157~164 + Mobile line 320~327) — 동형
    - `handleDownloadAll()` (Desktop line 166~173) — Desktop 만

    **§5.2 API call signature**:
    - `api.get<any[]>('/reports/div?year=...&timing=...')`
    - `api.get<any[]>('/reports/check-monthly?year=...&category=...')` (encodeURIComponent 적용 위치 동일)

    **§5.3 외부 라이브러리 호출**:
    - `generateDivExcel(year, data, timing, asBlob?)` — utils/generateExcel.ts
    - `generateCheckExcel(year, data, type, asBlob?)`
    - `generateMatrixExcel(year, data, sheetIndex, itemCount, name, inspectorRow?, asBlob?)`
    - `generatePumpExcel(year, data, asBlob?)`
    - `zipSync(files, { level: 6 })` — fflate
    - `URL.createObjectURL` + `a.click()` + `URL.revokeObjectURL` pattern + `setTimeout(..., 1000)`

    **§5.4 상수 + 데이터 구조** (변경 0):
    - `REPORT_CARDS` (line 12~23) — 10 entry { type, title, sub } verbatim
    - `MATRIX_CONFIG` (line 25~30) — 4 entry { category, sheetIndex, itemCount, name, inspectorRow }
    - `DESKTOP_SECTIONS` (line 142~147) — 4 entry { label, types[] }
    - `ANNUAL_TYPES` (line 36) — Set<ReportType> 4종
    - `CURRENT_YEAR` / `MIN_YEAR` (line 32~33) — 2026 / 2023
    - `ASSISTANTS = ['석현민', '김병조', '박보융']` (line 50, 87) — 자탐/방화셔터/제연 한정 랜덤 채움

    **§5.5 React hooks + state**:
    - `useIsDesktop()` 분기 (line 402)
    - useState 5종 (year, selectedType, loading, zipLoading, hoverType) — initial value + setter signature 보존

    **§5.6 ASSISTANTS 랜덤 채움 룰 verbatim** (line 49~57 = downloadReport / line 86~93 = generateReportBlob):
    ```typescript
    if (['자탐', '방화셔터', '제연'].includes(type) && data.length > 0) {
      const ASSISTANTS = ['석현민', '김병조', '박보융']
      for (const cp of data) {
        for (const m of Object.keys(cp.months ?? {})) {
          if (!cp.months[m].inspector) {
            cp.months[m].inspector = ASSISTANTS[Math.floor(Math.random() * ASSISTANTS.length)]
          }
        }
      }
    }
    ```
    분기 조건 + array literal 변경 0.

    **§5.7 ExcelPreview.tsx 무변경**:
    - 0 line 수정
    - W6 OQ #1 LOCKED b) 일관 — wrapper layout (line 297~300) 만 변환, 내부 ExcelPreview 컴포넌트는 손대지 않음
    - 단, `<ExcelPreview reportType={selectedType} year={year} month={month} />` props 시그니처 verbatim 유지

    **§5.8 외부 컴포넌트 import** (line 1~7 영역 — verbatim 보존 + 확장만 OK):
    - `import { Download } from 'lucide-react'` (line 3) — SW2 에서 `ChevronLeft` 추가 확장
    - `import { useNavigate } from 'react-router-dom'` (line 2) — Mobile 만 사용
    - `import { api } from '../utils/api'` (line 4)
    - `import { generateDivExcel, generateCheckExcel, generateMatrixExcel, generatePumpExcel } from '../utils/generateExcel'` (line 5)
    - `import { ExcelPreview } from '../components/ExcelPreview'` (line 6)
    - `import { useIsDesktop } from '../hooks/useIsDesktop'` (line 7)

    ---

    ## §6. 3 Sub-wave 분할 (W7 OQ #2 LOCKED) — task_scope §6 verbatim 박제

    **SW1 — CSS @layer 정의 + tokens.css 확인** (atomic commit 1):
    - 신규 파일: `cha-bio-safety/src/styles/components.css` (W7 OQ #1 default a 단일 파일)
    - 30+ CSS class @layer components 안에 정의 (W2~W6 sketch 의 ` * { ... }` 블록 verbatim 인용, var(--token) 사용)
    - `cha-bio-safety/src/index.css` 에 `@import './styles/components.css';` 추가 (현재 tokens.css + typography.css 2건 → 3건)
    - npm run build PASS / tsc --noEmit PASS
    - ReportsPage.tsx / ExcelPreview.tsx 0 line 수정 (SW1 은 CSS 신규만)
    - 커밋: `style(14-reports): SW1 — components.css @layer 신규 (30+ class verbatim from sketch W2~W6)`

    **SW2 — MobileReportsPage 변환** (atomic commit 2):
    - 대상: ReportsPage.tsx line 316~385 영역 — 자체 헤더 + 카드 그리드 10종 + footer
    - 인라인 style → className= (위 components.css class 사용)
    - `iconBtn` / `navBtn` 상수 (line 387~391, 393~398) → CSS class 대체 (W7 OQ #3 default a) 또는 유지 결정
    - lucide `ChevronLeft` 추가 import (Download 는 이미 line 3)
    - 카드 sub "DIV · 34개소 · {year}년도" → `<span class="dot-meta" />` 변환 (W1 OQ #5 LOCKED)
    - ⬇ 이모지 (line 374 U+2B07) 제거 — lucide `<Download size={14} />` 로 치환 (메모리 룰 8: tsx_wave_emoji_dot_gap)
    - 비즈 로직 보존: handleDownload (line 320~327) signature 동일, downloadReport 호출 동일
    - npm run build PASS / tsc --noEmit PASS
    - 커밋: `tsx(14-reports): SW2 — MobileReportsPage Tailwind 변환 (이모지 제거, dot span 도입, 그라데이션 폐기)`

    **SW3 — DesktopReportsPage 변환** (atomic commit 3):
    - 대상: ReportsPage.tsx line 149~304 영역 — 상단 toolbar + 좌측 sidelist + 우측 preview-wrapper
    - 인라인 style → className= (components.css)
    - DESKTOP_SECTIONS 4 섹션 변환 — 섹션 B 라벨 "소화전 · 가스 · 비상콘센트" 의 가운뎃점 → dot span 변환 (W1 OQ #5 LOCKED, sidelist-section-header 안)
    - SELECT_STYLE 처리 = W7 OQ #2 default a — `.toolbar-select` CSS class 로 대체 (line 306~313 상수 폐기)
    - selectedType / hoverType state + handleDownload + handleDownloadAll 시그니처 동일 (line 157~173)
    - 비즈 로직 보존: ASSISTANTS 랜덤 채움 룰 동일 (line 49~57 / 86~93)
    - npm run build PASS / tsc --noEmit PASS
    - 커밋: `tsx(14-reports): SW3 — DesktopReportsPage Tailwind 변환 (toolbar/sidelist/preview-wrapper, dot span, 그라데이션 폐기)`

    각 SW 마다 atomic 1-commit. 커밋 메시지 prefix: `style/tsx(14-reports): SW1/SW2/SW3 — ...`.

    ---

    ## §7. source line ref 인용표 (≥15 row)

    표 컬럼: `| 영역 | 파일 | line range | 핵심 내용 |`

    포함 row (task_scope §7 + ExcelPreview verbatim):
    - Imports: ReportsPage.tsx line 1~7
    - ReportType union: line 9~10
    - REPORT_CARDS: line 12~23
    - MATRIX_CONFIG: line 25~30
    - CURRENT_YEAR/MIN_YEAR: line 32~33
    - ANNUAL_TYPES: line 36
    - downloadReport: line 39~71
    - generateReportBlob: line 74~109 (생성 함수, blob+filename return)
    - downloadAllAsZip: line 112~139 (zipSync + URL.createObjectURL pattern)
    - DESKTOP_SECTIONS: line 142~147
    - DesktopReportsPage: line 149~304 (toolbar 180~243 + sidelist 248~295 + preview-wrapper 297~300)
    - SELECT_STYLE 상수: line 306~313
    - MobileReportsPage: line 316~385 (header 331~353 + cards 355~377 + footer 379~381)
    - iconBtn 상수: line 387~391
    - navBtn 상수: line 393~398
    - Default export (isDesktop 분기): line 401~405
    - ExcelPreview.tsx: line 1~535 (무변경 명시)

    총 ≥15 row.

    ---

    ## §8. 메모리 룰 inline 인용 (≥12건)

    각 룰: 파일명 + 1줄 요약 + Why + How to apply (14-reports 컨텍스트).

    포함 (W1 인덱스 §5 의 10건 inherit + 본 wave 추가 2건):

    1. `feedback_design_sketch_first.md` — sketch 시안 먼저, 변경 후 확인 X. (적용: TSX 변환은 sketch W2~W6 합의 그대로, 재논의 X)
    2. `feedback_redesign_sketch_rule_enforcement.md` — §6.2 negative rule (위험 임계치 아닌 카드 status 색 금지). (적용: 다운로드 버튼은 정상 CTA → safe 색 OK, 카드 배경 status 색 금지)
    3. `feedback_sketch_realistic_data.md` — 표시 분기/라벨 룰은 코드 그대로. (적용: REPORT_CARDS 10 entry / ASSISTANTS 3명 / footer 카피 verbatim)
    4. `feedback_planner_prompt_sketch_verbatim.md` — sketch CSS verbatim 인용 필수. (적용: §3 의 30+ class 본문 fence)
    5. `feedback_tailwind_token_class_pattern.md` — status- prefix 룰 (text-status-safe-bar). (적용: §4 cheatsheet 박제 + components.css 안 var() 토큰 사용)
    6. `feedback_tailwind_w8_h8_is_48px.md` — w-8 = 48px 함정. (적용: 뒤로 버튼 34x34 verbatim 또는 w-[34px] 명시, w-8 사용 금지)
    7. `feedback_text_caption_leading_none.md` — 작은 컨테이너 안 text-caption → leading-none 명시. (적용: year-label / year-nav-btn / toolbar-year-label / sidelist-section-header 모두 line-height 1 명시)
    8. `feedback_tsx_wave_emoji_dot_gap.md` — sketch negative gate (이모지 0) + dot span 추가 markup verify. (적용: ⬇ 이모지 line 374 제거 + dot span 카드 sub + 섹션 B 라벨 적용)
    9. `feedback_tsx_wave_stat_card_drift.md` — sketch 새 패턴 누락 위험. (적용: 14-reports 는 Stat Card 없음 → W7 checklist 에 "Stat Card 룰 14-reports 미적용" 명시)
    10. `feedback_avoid_premature_confirmation.md` — "거의 일치" 자신감 표현 금지. (적용: TSX 변환 후 사용자 컨펌 명시 받은 후 main 머지)
    11. **추가 1: `feedback_check_branch_before_edit.md`** — 작업 시작 전 브랜치 확인 필수. (적용: TSX 변환 wave 진입 시 `git branch --show-current = redesign/14-reports` 확인 후 SW1 시작)
    12. **추가 2: `feedback_cbc7119_design_never_wrangler.md`** — 디자인 wave 중 wrangler 절대 X. (적용: SW1~SW3 모두 wrangler 명령 0건, `npm run deploy` 0건. cbc7119-preview 자동 배포만)

    총 unique feedback_*.md ≥12.

    ---

    ## §9. 인라인 style 화이트리스트 (TSX 변환 후 잔존 OK) — ≥3 항목

    Tailwind/CSS class 으로 표현 안 되거나 안 하는 게 나은 case. 각 인스턴스는 line ref + 사유 inline.

    표 컬럼: `| 케이스 | Source line | 사유 |`

    포함 row ≥3:
    - **ExcelPreview.tsx 내부 인라인 style** (W6 OQ #1 LOCKED 일관, 본 변환 scope 외)
    - **동적 색 (14-reports 는 0건)** — ReportsPage.tsx 의 카드 색은 모두 정적 토큰 (safe/surface/text), 카테고리 hex 동적 분기 0
    - **React 컴포넌트 props 동적 값** — `opacity: loading === selectedType ? 0.5 : 1` (line 237) — `.toolbar-individual-btn--loading` class 조건부 적용으로 회피 권장, 단 인라인 잔존도 OK
    - **DesktopReportsPage line 178 outer wrapper** — `style={{ display: 'flex', flexDirection: 'column', height: '100%', overflow: 'hidden' }}` 는 layout primitive → `className="flex flex-col h-full overflow-hidden"` 변환 권장 (인라인 잔존도 OK)

    **0회 강제 (linear-gradient 완전 폐기)**:
    - `linear-gradient` 0 hit — W1 OQ #1 LOCKED + W1 OQ #3 LOCKED 양쪽 다 토큰 solid
    - source line 202 (데스크톱 일괄) + line 370 (모바일 카드) 양쪽 제거

    ---

    ## §10. 비즈 로직 보존 verify 체크리스트 (≥15 행)

    체크박스 ≥15 행:
    - [ ] `downloadReport(type, year)` 시그니그 diff 0 (line 39~71)
    - [ ] `generateReportBlob(type, year)` 시그니처 diff 0 (line 74~109)
    - [ ] `downloadAllAsZip(year, month, onProgress)` 시그니처 diff 0 (line 112~139)
    - [ ] `handleDownload` state update 동일 (Desktop line 157~164 + Mobile line 320~327)
    - [ ] `handleDownloadAll` setZipLoading 호출 동일 (line 166~173)
    - [ ] `selectedType` useState initial 'div-early' 유지 (line 151)
    - [ ] `hoverType` state setter 호출 위치 동일 (line 154, 276~277)
    - [ ] ASSISTANTS 랜덤 채움 분기 (자탐/방화셔터/제연만) 유지 (line 49~57, 86~93)
    - [ ] REPORT_CARDS array entries 10개 변경 0 (line 12~23)
    - [ ] MATRIX_CONFIG record entries 4개 변경 0 (line 25~30)
    - [ ] DESKTOP_SECTIONS array entries 4개 변경 0 (line 142~147)
    - [ ] `api.get<any[]>('/reports/div?year=...&timing=...')` 호출 URL pattern 동일
    - [ ] `api.get<any[]>('/reports/check-monthly?year=...&category=...')` 호출 URL pattern 동일
    - [ ] `zipSync(files, { level: 6 })` 호출 동일 (line 129)
    - [ ] `URL.createObjectURL` → `a.click()` → `URL.revokeObjectURL` pattern + `setTimeout(..., 1000)` 동일 (line 131~138)
    - [ ] `useIsDesktop()` hook 분기 유지 (line 402)
    - [ ] `ExcelPreview` props 시그니처 verbatim — `reportType / year / month` 3 prop (line 299)

    ---

    ## §11. verify gate per sub-wave (≥28 항목 total)

    **SW1 gate (≥8)** — components.css 신규:
    1. `ls cha-bio-safety/src/styles/components.css` — 존재
    2. `grep -c '@layer components' cha-bio-safety/src/styles/components.css` ≥ 1
    3. `.report-card / .toolbar / .sidelist / .preview-wrapper / .a4-preview` 5 컴포넌트군 모두 정의됨
    4. CSS class 정의 ≥30 — `grep -cE '^[[:space:]]*\.[a-zA-Z0-9_-]+[[:space:]]*\{' cha-bio-safety/src/styles/components.css` ≥ 30
    5. `var(--surface-raised) / var(--text-primary) / var(--accent) / var(--border-default)` 등 토큰 사용 ≥10 — `grep -cE 'var\(--' cha-bio-safety/src/styles/components.css` ≥ 10
    6. linear-gradient 0건 — `grep -c 'linear-gradient' cha-bio-safety/src/styles/components.css` = 0
    7. font-size 9·10·11px 0건 — `grep -nE 'font-size:[[:space:]]*(9|10|11)px' cha-bio-safety/src/styles/components.css` 0 hits
    8. `src/index.css` 에 `@import './styles/components.css'` 추가 + `npm run build` PASS + `tsc --noEmit` PASS

    **SW2 gate (≥10)** — MobileReportsPage:
    1. MobileReportsPage line 316~385 영역 className= 사용 — `grep -cE 'className=' cha-bio-safety/src/pages/ReportsPage.tsx` line 316~385 범위 ≥ 10
    2. 인라인 style 카운트 감소 — 변환 전 ≥30 인라인 (line 330~398 범위) → 변환 후 ≤5
    3. lucide import 확장 — `grep -nE "from 'lucide-react'" cha-bio-safety/src/pages/ReportsPage.tsx` 안에 `ChevronLeft` + `Download` 둘 다 포함
    4. ⬇ 이모지 0건 — `grep -nP '[\x{2B00}-\x{2BFF}]|[\x{1F300}-\x{1FAFF}]' cha-bio-safety/src/pages/ReportsPage.tsx` = 0
    5. ' · ' 가운뎃점 텍스트 본문 0건 (dot span 만) — `grep -nP '·' cha-bio-safety/src/pages/ReportsPage.tsx` = 0 (dot 표현은 `.dot-meta` class 만)
    6. `bg-status-safe-bar` 패턴 (status- prefix 정확) — `grep -nE 'bg-status-safe-bar' cha-bio-safety/src/pages/ReportsPage.tsx` ≥ 1 (모바일 카드 버튼)
    7. handleDownload 시그니처 동일 — `grep -nE 'const handleDownload = async \(type: ReportType\) =>' cha-bio-safety/src/pages/ReportsPage.tsx` ≥ 1
    8. REPORT_CARDS 변경 0 — `git diff cha-bio-safety/src/pages/ReportsPage.tsx` line 12~23 영역 = 0 byte 변경
    9. `npm run build` PASS / `tsc --noEmit` PASS
    10. chunk size 변동 ±10% 이내 (예: 33KB → 30~36KB)

    **SW3 gate (≥10)** — DesktopReportsPage:
    1. DesktopReportsPage line 149~304 영역 className= 사용 — ≥ 30
    2. 인라인 style 카운트 감소 — 변환 전 ≥50 인라인 → 변환 후 ≤8
    3. lucide Download import 유지 (line 3)
    4. linear-gradient 0건 — `grep -c 'linear-gradient' cha-bio-safety/src/pages/ReportsPage.tsx` = 0 (line 202 그라데이션 폐기)
    5. DESKTOP_SECTIONS 4 섹션 변환 — 섹션 B 라벨 "소화전 · 가스 · 비상콘센트" 의 가운뎃점 → dot span (`.dot-meta` 2회 사이)
    6. SELECT_STYLE 처리 — W7 OQ #2 default a) `.toolbar-select` class 대체 + line 306~313 상수 삭제. `grep -nE 'SELECT_STYLE' cha-bio-safety/src/pages/ReportsPage.tsx` = 0
    7. iconBtn / navBtn 상수 — W7 OQ #3 default a) CSS class 대체 + line 387~391, 393~398 상수 삭제. `grep -nE '(iconBtn|navBtn)' cha-bio-safety/src/pages/ReportsPage.tsx` = 0
    8. ASSISTANTS 랜덤 채움 룰 line 49~57 / 86~93 동일 — `grep -nE "ASSISTANTS = \['석현민'" cha-bio-safety/src/pages/ReportsPage.tsx` = 2 hits
    9. `npm run build` PASS / `tsc --noEmit` PASS
    10. handleDownloadAll signature 동일 — `grep -nE 'const handleDownloadAll = async \(\) =>' cha-bio-safety/src/pages/ReportsPage.tsx` ≥ 1

    Sub-wave 별 verify gate 총합 = 8 + 10 + 10 = 28.

    ---

    ## §12. OQ (TSX SW1 진입 전 ≥4건, default 명시)

    - **OQ #1**: components.css 파일 위치 — (a) `cha-bio-safety/src/styles/components.css` 단일 (장기적으로 다른 페이지 재디자인도 합쳐 운영, 12-staff/13-schedule TSX 변환 시 정해진 패턴 있으면 mirror) / (b) `cha-bio-safety/src/styles/reports.css` 페이지별 분리. **default: (a) 단일** — TSX SW1 진입 시 12-staff/13-schedule 변환 후 패턴 확인 후 결정.
    - **OQ #2**: SELECT_STYLE 처리 — (a) `.toolbar-select` CSS class 로 대체 (인라인 0) / (b) 유지 + 토큰 alias 만 v0.1.1 으로 정합. **default: (a) 대체**.
    - **OQ #3**: iconBtn / navBtn 상수 처리 — (a) CSS class 로 대체 (`.back-btn` / `.year-nav-btn` 사용) / (b) 유지. **default: (a) 대체** — components.css 의 .back-btn / .year-nav-btn 이 verbatim 매핑.
    - **OQ #4**: A4 미리보기 영역에 `.a4-preview-table-placeholder` 추가 — (a) 실제 wrapper TSX 에 빈 div placeholder 추가 / (b) ExcelPreview.tsx 내부에서 자체 placeholder 처리 / (c) wrapper layout 만 변환 (placeholder 0, ExcelPreview 본체 그대로). **default: (c)** — sketch 의 dashed grid placeholder 는 sketch only, 실제 컴포넌트는 ExcelPreview.tsx 가 처리 (W6 OQ #1 LOCKED 일관).
    - **OQ #5** (선택): TSX 변환 후 chunk size impact — **default: 의도된 증가 ±5KB OK**. components.css 신규 (~3KB) + Tailwind className 길이 증가는 자연스러움.

    ---

    **End of W7 checklist.**

    > TSX 변환 wave (SW1~SW3) executor 는 본 checklist + sketch-wave-2~6.html + ReportsPage.tsx (405 lines) + ExcelPreview.tsx (535 lines, 무변경) + design-system.md + tokens.css + typography.css 만 input 으로 atomic 변환 가능.

    ---

    ## 작성 시 핵심 룰

    1. **§3 의 CSS class 본문 fence 는 sketch HTML 의 `<style>` 블록을 verbatim 그대로 복사**. 토큰명 (예: var(--surface-raised)) 절대 추측 X. 본 PLAN.md 의 §3.1~§3.4 인용 내용을 그대로 옮김.
    2. **§2 LOCKED 표 ≥15 row + §4 cheatsheet ≥28 row + §7 source line ref ≥15 row + §10 비즈 로직 체크 ≥15 row + §11 verify gate 합계 ≥28 + §12 OQ ≥4 + §8 메모리 룰 unique ≥12** — 각 카운트 모두 충족.
    3. **이모지 0건** — 본 markdown 안 어디에도 이모지 사용 금지 (체크박스 - [ ] 는 ASCII OK).
    4. **메타 verify gate (markdown 자체)**:
       - `^## ` 헤더 ≥12 sections
       - CSS class verbatim fence ≥30 — `grep -cE '\.(report-card|toolbar|sidelist|preview-wrapper|a4-preview|page-header|back-btn|year|dot-meta|page-body|page-footer|sidelist-row)' wave-7-tsx-conversion-checklist.md` ≥ 30
       - W1~W7 LOCKED 표 행 ≥15
       - Tailwind cheatsheet 표 행 ≥28
       - source line ref 표 행 ≥15
       - sub-section ≥6 (§5.1~§5.8)
       - sub-wave 정의 SW1/SW2/SW3 = 3
       - verify gate per sub-wave 항목 ≥28
       - 메모리 룰 unique feedback_* ≥12 — `grep -oE 'feedback_[a-z_0-9]+\.md' wave-7-tsx-conversion-checklist.md | sort -u | wc -l` ≥ 12
       - OQ ≥4 — `grep -cE '^- \*\*OQ #' wave-7-tsx-conversion-checklist.md` ≥ 4
       - file size 500~600 줄 — `wc -l wave-7-tsx-conversion-checklist.md` 500~600
       - sketch HTML 생성 0 (markdown 만)
       - src 코드 수정 0 — `git diff --name-only HEAD -- cha-bio-safety/src migrations scripts` 0 lines
       - wrangler 0 / `npm run deploy` 0
  </action>

  <verify>
    <automated>
      bash -c '
        set -e
        FILE=cha-bio-safety/docs/redesign-context/14-reports/wave-7-tsx-conversion-checklist.md
        test -f "$FILE" || { echo "FAIL: file not found"; exit 1; }
        LINES=$(wc -l < "$FILE")
        [ "$LINES" -ge 500 ] && [ "$LINES" -le 700 ] || { echo "FAIL: lines=$LINES not in 500~700"; exit 1; }
        HDR=$(grep -cE "^## §" "$FILE")
        [ "$HDR" -ge 12 ] || { echo "FAIL: section headers=$HDR < 12"; exit 1; }
        CLASS=$(grep -cE "\.(report-card|toolbar|sidelist|preview-wrapper|a4-preview|page-header|back-btn|year|dot-meta|page-body|page-footer|sidelist-row|toolbar-)" "$FILE")
        [ "$CLASS" -ge 30 ] || { echo "FAIL: class refs=$CLASS < 30"; exit 1; }
        MEM=$(grep -oE "feedback_[a-z_0-9]+\.md" "$FILE" | sort -u | wc -l | tr -d " ")
        [ "$MEM" -ge 12 ] || { echo "FAIL: memory rules unique=$MEM < 12"; exit 1; }
        OQ=$(grep -cE "^- \*\*OQ #" "$FILE")
        [ "$OQ" -ge 4 ] || { echo "FAIL: OQ=$OQ < 4"; exit 1; }
        EMOJI=$(grep -cP "[\x{1F300}-\x{1FAFF}]|[\x{2600}-\x{26FF}]|[\x{2700}-\x{27BF}]" "$FILE" || true)
        [ "$EMOJI" -eq 0 ] || { echo "FAIL: emoji=$EMOJI > 0"; exit 1; }
        SW=$(grep -cE "^\*\*SW[123]" "$FILE")
        [ "$SW" -ge 3 ] || { echo "FAIL: SW1/SW2/SW3 markers=$SW < 3"; exit 1; }
        SRC_DIFF=$(git diff --name-only HEAD -- cha-bio-safety/src migrations scripts 2>/dev/null | wc -l | tr -d " ")
        [ "$SRC_DIFF" -eq 0 ] || { echo "FAIL: src/migrations/scripts modified=$SRC_DIFF > 0"; exit 1; }
        WRANGLER=$(grep -cE "wrangler " "$FILE" || true)
        DEPLOY=$(grep -cE "npm run deploy" "$FILE" || true)
        [ "$WRANGLER" -ge 1 ] && [ "$DEPLOY" -ge 1 ] || { echo "WARN: wrangler/deploy mentions wrangler=$WRANGLER deploy=$DEPLOY (negative scope 인용 필요)"; }
        echo "PASS lines=$LINES headers=$HDR classes=$CLASS memory=$MEM OQ=$OQ SW=$SW src_diff=$SRC_DIFF"
      '
    </automated>
  </verify>

  <done>
    - `cha-bio-safety/docs/redesign-context/14-reports/wave-7-tsx-conversion-checklist.md` 존재
    - 500~700 lines
    - ≥12 sections (## §1 ~ ## §12)
    - ≥30 CSS class refs (report-card / toolbar / sidelist / preview-wrapper / a4-preview / page-header / back-btn / year-* / dot-meta / page-body / page-footer / sidelist-row / toolbar-* 등)
    - ≥12 unique feedback_*.md 메모리 룰
    - ≥4 OQ (default 명시)
    - ≥3 sub-wave (SW1/SW2/SW3) 정의
    - 이모지 0건
    - src/migrations/scripts 코드 수정 0
    - wrangler / npm run deploy 인용 ≥1 (negative scope 박제)
  </done>
</task>

</tasks>

<verification>
- Task 1 의 `<verify>` automated gate 모두 PASS
- atomic 1-commit: `docs(14-reports): sketch wave 7 — TSX 변환 verify checklist (12 sections, 30+ CSS class verbatim 박제)`
- 워크트리 룰 준수: wrangler 0 / `npm run deploy` 0 / branch = redesign/14-reports
- SUMMARY.md 작성: 12 sections 체크 / verify gate 결과 / W7 LOCKED 2건 박제 / TSX SW1 진입 전 OQ 5건 default
</verification>

<success_criteria>
- W7 (마지막 sketch wave) 산출물 = markdown 1개 파일, 500~600 줄, 12 sections, 30+ CSS class verbatim, 메모리 룰 12+, OQ 4+, sub-wave 3 정의
- TSX 변환 wave (SW1~SW3) executor 가 본 checklist 1개 + sketch W2~W6 5개 + ReportsPage.tsx 1개 + ExcelPreview.tsx (참조만) 만 input 으로 atomic 변환 가능
- sketch HTML 생성 0 / src 코드 수정 0 / wrangler 0 / `npm run deploy` 0
- 사용자에게 W7 완결 보고 + TSX SW1 진입 OQ 5건 default 컨펌 요청
</success_criteria>

<output>
After completion, create `.planning/quick/260520-u3b-redesign-14-reports-sketch-wave-7-tsx-ve/260520-u3b-SUMMARY.md` containing:
- 12 sections 체크 결과 (각 section line count 또는 표 row 수)
- verify gate 자동 명령 결과 (lines / headers / classes / memory / OQ / SW / src_diff 카운트)
- W7 LOCKED 2건 박제 (#1 components.css 신규 / #2 3 sub-wave 분할)
- TSX SW1 진입 전 OQ 5건 default 명시 (OQ #1 single file / #2 .toolbar-select 대체 / #3 .back-btn+.year-nav-btn 대체 / #4 placeholder 없이 wrapper 만 / #5 chunk size ±5KB OK)
- 다음 액션: 사용자 OQ 5건 컨펌 후 TSX SW1 진입
</output>