---
phase: 260525-6if
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cha-bio-safety/src/pages/LegalFindingDetailPage.tsx
autonomous: true
requirements:
  - W5-checklist-§1-scope-single-atomic
  - W5-checklist-§2-biz-anchor-11
  - W5-checklist-§3-region-mapping-3
  - W5-checklist-§4-OQ-LOCKED-5
  - W5-checklist-§5-negative-gate
  - W5-checklist-§6-positive-gate
  - W5-checklist-§7-build-gate
  - W5-checklist-§8-self-verify-grep
  - W5-checklist-§9-tailwind-cheatsheet
  - W5-checklist-§10-biz-checkbox
  - W5-checklist-§11-memory-rules-12
  - W5-checklist-§12-next-step
tags:
  - redesign
  - 21-legal-finding-detail
  - tsx-conversion
  - v0.1.x
  - quick
  - single-file-atomic
  - lucide-chevron-left
  - lucide-loader2
  - lucide-download
  - lucide-camera
  - back-button-44x44
  - spinner-discarded
  - cta-gradient-anchor

must_haves:
  truths:
    - "LegalFindingDetailPage.tsx **단일 파일 atomic** in-place 수정. `git diff --name-only HEAD~ HEAD` 에 'cha-bio-safety/src/pages/LegalFindingDetailPage.tsx' + 본 PLAN.md/SUMMARY.md 만 등장 (다른 파일 0). 23-education (r22) / 19-legal (lft) / 20-legal-findings (bbz) / 17-annual-plan (1hj) / 27-login (gox) / 16-workshift (u5n) / 28-splash (4i9) 와 동일한 단일 파일 패턴. 본 21-legal-finding-detail 는 279 lines 단일 export (LegalFindingDetailPage default) + 3 영역 (imports/포맷터/컴포넌트 / 메인 함수 / JSX render) 통합 변환 — 19-legal lft 571 lines (3 컴포넌트 + 5 helper) 보다 단순 + 20-legal-findings bbz 378 lines (3 영역) 와 동일 영역 분할."
    - "App.tsx + tailwind.config.js + tokens.css + typography.css + hooks/useMultiPhotoUpload.ts + hooks/useIsDesktop.ts + stores/authStore.ts + utils/api.ts + utils/findingDownload.ts + components/PhotoGrid.tsx + components/PhotoSourceModal.tsx + pages/LegalPage.tsx + pages/LegalFindingsPage.tsx + types/ + functions/ + templates/ + migrations/ + public/ 모두 변경 0 byte. **9 src 파일 + tailwind.config.js 변경 0 byte 가드 = final verify gate**."
    - "변환 후 LegalFindingDetailPage.tsx 라인 수 279 ± 30 (250~310 예상). Spinner 함수 line 41~48 (8 lines) 폐기 + Lucide Loader2 직접 사용 + 인라인 SVG ChevronLeft path line 156 1 line + 인라인 SVG Download path line 161 1 line + 인라인 spin @keyframes line 147 1 line + Spinner 안 @keyframes line 45 1 line → Lucide 4종 import 1 line 추가 + Loader2 직접 mount + back 44x44 격상 + admin 다운로드 button 44x44 격상 → 총 라인 수 거의 동일."
    - "★ LegalFindingDetailPage 비즈 anchor 11건 1 byte 변경 0: (1) `legalApi.getFinding(roundId, findingId)` (line 63) + `legalApi.resolveFinding(roundId, findingId, { resolution_memo, resolution_photo_keys })` snake_case payload (line 70~73) — 본 페이지 2종, 19-legal LegalPage 5종 (list/get/getFindings/updateResult/deleteFinding) + 20-legal-findings 4종 과 다름. (2) useQuery `['legal-finding', id, fid]` (line 61~65) + enabled `!!id && !!fid` (3) useMutation resolveMutation uploadAll 선행 (line 67~74) + invalidateQueries 4 키 onSuccess (legal-finding → legal-findings → legal-rounds → legal-round, line 76~79) + reset + navigate(-1) 순서 (4) useMultiPhotoUpload **직접 호출** (line 59) — 19-legal/20-legal-findings 의 FindingFormSheet 우회와 다름 (19/20 차이 2). 직접 사용 멤버: slots/canAdd/openPicker/closePicker/pickCamera/pickAlbum/handleFiles/removeSlot/uploadAll/reset/isUploading/cameraRef/albumRef/showPicker (5) handleResolve memo.trim() validation + `toast.error('조치 내용을 입력하세요')` (line 132~138) (6) `finding.status === 'open'` Section 3 form / `'resolved'` Section 4 결과 **mutually exclusive** 분기 (line 218 + 253) (7) `staff = useAuthStore(s => s.staff)` **selector 패턴** (line 58) — 20-legal-findings getState() 와 다름. `staff?.role === 'admin' && finding` 모바일 (line 159) + 데스크톱 (line 171) 양쪽 admin 다운로드 분기 (8) handleDownload (line 89~130) fflate dynamic import + zipSync + Promise.allSettled photoKeys + resolutionPhotoKeys + buildMetaTxt(finding) + iOS PWA `<a download>` createElement+appendChild+click+removeChild+setTimeout revoke 패턴 verbatim (9) ZIP 파일명 **location 기반** `지적사항_${name}.zip` where `name = (finding.location ?? '위치없음').replace(/[\/\\\\:*?\"<>|]/g, '_')` (line 96, 119) — 20-legal-findings round.title 기반 + fid 기반 아님 (19/20 차이 4) (10) toast 카피 5종 verbatim (success 2: '조치 완료' line 80 + '다운로드 완료' line 124, error 3: '조치 처리 실패' line 85 + '조치 내용을 입력하세요' line 134 + '다운로드 실패' line 126) (11) 카피 verbatim — 헤더 '지적 상세' (정적, 20-legal-findings 동적 분기와 다름) + SectionHeader 4종 '지적 정보'/'지적 사진'/'조치 내용'/'조치 결과' + KVRow label 7종 '지적 내용'/'위치'/'등록일'/'등록자'/'조치일시'/'조치자'/'조치 내용' + 위치 fallback '-' (20-legal-findings '위치 미지정' 과 다름) + '사진 없음' + '조치 사진 (최대 5장)' + textarea placeholder '조치 내용을 입력하세요' + '사진 첨부' + 슬롯 제거 '✕' + 업로드 중 '업로드 중' + CTA '조치 완료'/'처리 중...' + admin '다운로드'/'다운로드 중...' + 오류 단일 문장 '항목을 불러오지 못했습니다. 뒤로 가서 다시 시도하세요.'"
    - "비즈 로직 0 diff: imports (useState/useParams+useNavigate/useQuery+useMutation+useQueryClient/toast/legalApi/useIsDesktop/useMultiPhotoUpload/PhotoGrid/PhotoSourceModal/useAuthStore/buildMetaTxt/types LegalFinding) 그대로 + **추가 1줄**: `import { ChevronLeft, Download, Loader2, Camera } from 'lucide-react'` (OQ #5 LOCKED 4종). **Spinner 함수 line 41~48 폐기** → `<Loader2 className=\"animate-spin\" size={24} />` 직접 사용. **@keyframes spin 중복 정의 2건 폐기** — line 45 (Spinner 안) + line 147 (JSX 외곽) 양쪽 모두 제거."
    - "카피 verbatim 보존 (5건 toast + 라벨 다수): success 2 — '조치 완료' (line 80) + '다운로드 완료' (line 124). error 3 — '조치 처리 실패' (line 85) + '조치 내용을 입력하세요' (line 134) + '다운로드 실패' (line 126). 헤더 '지적 상세' (line 158/170, 정적 — 20-legal-findings 동적 종합정밀/작동기능 분기와 다름). SectionHeader 4종 (line 198/209/220/255). KVRow label 7종 (line 200~204/257~259). 위치 fallback '-' (line 201). 등록자 fallback `??` (line 203). '사진 없음' (line 213). '조치 사진 (최대 5장)' (line 223). textarea placeholder '조치 내용을 입력하세요' (line 221). '사진 첨부' (line 237, OQ #3 격상 11→12). 슬롯 제거 '✕' (line 231, OQ #3 격상 11→12). '업로드 중' (line 232, OQ #3 격상 10→12). CTA '조치 완료'/'처리 중...' (line 246/273). admin '다운로드'/'다운로드 중...' (line 173). 오류 '항목을 불러오지 못했습니다. 뒤로 가서 다시 시도하세요.' (line 185)."
    - "OQ #1 LOCKED 적용 (모바일 자체 헤더): `bg-surface-raised border-b border-border-default`. 옛 인라인 `background:'rgba(22,27,34,0.97)', borderBottom:'1px solid var(--bd)'` (line 152) 완전 폐기. 타이틀 '지적 상세' 정중앙 정적 className `text-body font-bold text-text-primary` (line 158, 20-legal-findings 동적 분기와 다름)."
    - "OQ #2 LOCKED 적용 (finding 상태 칩 본 페이지 없음 + KVRow value 좌측 강조 없음): 본 페이지는 status 표시 = **화면 모드 자체** (`finding.status === 'open'` Section 3 form / `'resolved'` Section 4 결과 mutually exclusive 분기). **finding 상태 칩 자체가 없음** (20-legal-findings border-l-2 + 칩 패턴과 다름 — 카드 list 가 아닌 상세 페이지). **KVRow value 좌측 강조 없음** (borderLeft 등 인라인 0건). status- prefix 없음 (memory `feedback_tailwind_token_class_pattern`)."
    - "OQ #3 LOCKED 적용 (9·10·11 fontSize → text-caption 12 + leading-none 격상): 슬롯 제거 button '✕' fontSize 11 (line 231) → `text-caption font-bold leading-none`. 업로드 중 overlay fontSize 10 (line 232) → `text-caption leading-none`. 사진 첨부 button '사진 첨부' fontSize 11 (line 236~237) → `text-caption font-bold leading-none`. fontSize 9·10·11 인라인 0건 (verify negative gate)."
    - "OQ #4 LOCKED 적용 (조치 완료 CTA 인라인 그라데이션 ≥2 anchor + admin 다운로드 button = 작은 도구 solid 유지 + 빈/오류 카피 verbatim): **★ 데스크톱 inline 조치 완료 CTA (line 244~248) ★** + **★ 모바일 고정 하단 조치 완료 CTA (line 270~275) ★** 양쪽 모두 옛 `background:'var(--acl)'` 완전 제거 → 인라인 `background: 'linear-gradient(135deg, #1d4ed8, #0ea5e9)'` 추가 (★ OQ #4 LOCKED **T3 만 예외 anchor ≥2 박제**, 19-legal lft 1건 + 20-legal-findings bbz 1건 패턴 mirror). **admin 다운로드 button** (모바일 line 159~163 ghost + 데스크톱 line 171~175 solid `var(--bg3)` border `var(--bd2)`) → 옛 토큰만 className 치환 (`bg-surface-sunken border-border-strong text-text-primary` 또는 className 만 — 작은 도구는 그라데이션 아님, executor 판단으로 admin 다운로드 도 그라데이션 적용 가능 — 단, lft/bbz 일관 CTA only 권장). 빈/오류 카피 verbatim '항목을 불러오지 못했습니다. 뒤로 가서 다시 시도하세요.' (line 185) + '사진 없음' (line 213)."
    - "OQ #5 LOCKED 적용 (Lucide 4종 + back 44x44 격상 + admin 다운로드 44x44 격상 + Spinner 폐기 + Camera 22 교체): `import { ChevronLeft, Download, Loader2, Camera } from 'lucide-react'` 추가 (line 14 또는 imports 끝). (a) 모바일 헤더 back button 인라인 SVG path `d=\"M15 19l-7-7 7-7\"` (line 156) 완전 제거 → `<ChevronLeft size={20} />`. width:36 height:36 → **width:44 height:44 격상** (`w-11 h-11` 권장, 또는 인라인 명시. w-8 함정 회피). (b) 모바일 헤더 admin 다운로드 button 인라인 SVG path `d=\"M12 5v14m0 0l-6-6m6 6l6-6M5 19h14\"` (line 161) 완전 제거 → `<Download size={18} />`. width:36 height:36 → **width:44 height:44 격상**. (c) Spinner 함수 line 41~48 완전 폐기 → `<Loader2 className=\"animate-spin text-accent\" size={24} />` 직접 mount (line 180 호출처). (d) 카메라 첨부 button 안 이모지 '📷' (line 237) 완전 제거 → `<Camera size={22} />`. **이모지 0건** (canAdd '사진 첨부' button 안 이모지 폐기). grep gate `M15 19l-7-7 7-7 = 0` + `M12 5v14m0 0l-6-6m6 6l6-6M5 19h14 = 0` + `📷 = 0` + `function Spinner = 0` + `const Spinner = 0` + `@keyframes spin = 0`."
    - "Negative gate 모두 PASS: 이모지 0 / linear-gradient — **CTA ≥2 (OQ #4 anchor 모바일 고정 + 데스크톱 inline 양쪽)** + 그 외 anchor 카운트 일치 / fontSize 9·10·11 인라인 0 / status- prefix 0 / w-8·h-8 0 (memory `feedback_tailwind_w8_h8_is_48px`) / 옛 alias var(--bg|bg2|bg3|bg4|bd|bd2|t1|t2|t3|acl|safe|warn|danger) 0 / 인라인 SVG path (M15 19l-7-7 7-7 + M12 5v14m0 0l-6-6m6 6l6-6M5 19h14) 0 / function Spinner / const Spinner 0 / @keyframes spin 0."
    - "Positive gate 모두 PASS: bg-surface-raised border-b border-border-default ≥1 (OQ #1) / 화면 모드 mutually exclusive 분기 `finding.status === 'open'` ≥1 + `finding.status === 'resolved'` ≥1 (OQ #2 — 칩 없음, 화면 자체 status 표시) / text-caption ≥5 + leading-none ≥5 (OQ #3 격상 — 슬롯 제거 + 업로드 중 + 사진 첨부 + KVRow label + SectionHeader) / linear-gradient(135deg, #1d4ed8, #0ea5e9) ≥2 (OQ #4 CTA 모바일 + 데스크톱 양쪽) / import ChevronLeft, Download, Loader2, Camera from lucide-react ≥1 + <ChevronLeft size={20} ≥1 + <Download size={18} ≥1 + <Loader2 ≥1 + <Camera size={22} ≥1 (OQ #5)."
    - "★ 비즈 anchor 11건 보존 grep gate PASS: legalApi 2종 (getFinding + resolveFinding) + useQuery 1 (legal-finding queryKey) + useMutation 1 (resolveMutation) + invalidateQueries 4 키 (legal-finding/legal-findings/legal-rounds/legal-round 순서) + useMultiPhotoUpload **직접 호출** + buildMetaTxt + fflate zipSync + ZIP location 기반 (지적사항_${name}.zip) + handleZipDownload iOS PWA createElement+appendChild+click+removeChild+revokeObjectURL + handleResolve memo.trim() validation + status open/resolved mutually exclusive 분기 + admin selector 패턴 (s => s.staff) + toast 5종 + KVRow/SectionHeader/fmtDate 컴포넌트 + 카피 verbatim."
    - "9 src 파일 + tailwind.config.js 변경 0 byte: `git diff --name-only HEAD~ HEAD -- cha-bio-safety/src/App.tsx cha-bio-safety/src/components/PhotoGrid.tsx cha-bio-safety/src/components/PhotoSourceModal.tsx cha-bio-safety/src/hooks/useMultiPhotoUpload.ts cha-bio-safety/src/hooks/useIsDesktop.ts cha-bio-safety/src/utils/findingDownload.ts cha-bio-safety/src/utils/api.ts cha-bio-safety/src/stores/authStore.ts cha-bio-safety/src/pages/LegalPage.tsx cha-bio-safety/src/pages/LegalFindingsPage.tsx cha-bio-safety/tailwind.config.js` 빈 출력."
    - "Build gate: `cd cha-bio-safety && npx tsc --noEmit` 0 errors + `cd cha-bio-safety && npm run build` exit 0 (Vite build PASS). LegalFindingDetailPage chunk size 보고."
    - "Atomic 1-commit (단일 파일): `feat(quick-260525-6if): redesign/21-legal-finding-detail TSX 변환 (LegalFindingDetailPage.tsx 279→{N} lines 단일 atomic + v0.1.x 토큰 className + 비즈 anchor 11 보존 + Lucide ChevronLeft+Download+Loader2+Camera + Spinner 폐기 + back 44x44 + admin 다운로드 44x44 + CTA 그라데이션 ≥2 + OQ LOCKED 5건 + W5 §1 매핑 verbatim)`. SUMMARY 별도 commit."

  artifacts:
    - path: cha-bio-safety/src/pages/LegalFindingDetailPage.tsx
      provides: v0.1.x 토큰 className 적용 (외곽 / 모바일 자체 헤더 / 데스크톱 타이틀 / 로딩 Loader2 / 에러 단일 문장 / 콘텐츠 외곽 / Section 1 지적 정보 + KVRow 4행 / Section 2 지적 사진 + PhotoGrid + '사진 없음' / Section 3 조치 내용 + textarea + 사진 5장 슬롯 PhotoSourceModal + canAdd Camera + 데스크톱 inline CTA / Section 4 조치 결과 + KVRow 3행 + PhotoGrid / 모바일 고정 하단 CTA) + Lucide 4종 교체 (ChevronLeft 모바일 헤더 back + Download 모바일 헤더 admin + Loader2 spinner + Camera canAdd) + back button 44x44 격상 + admin 다운로드 button 44x44 격상 + Spinner 함수 line 41~48 폐기 + @keyframes spin 2건 (line 45 + 147) 폐기 + 인라인 SVG path 2종 (M15 19l-7-7 7-7 line 156 + M12 5v14m0 0l-6-6m6 6l6-6M5 19h14 line 161) 폐기 + 이모지 '📷' line 237 폐기 + OQ #1 모바일 헤더 bg-surface-raised border-b border-border-default + OQ #2 화면 모드 mutually exclusive (칩 없음 + KVRow value 좌측 강조 없음) + OQ #3 9·10·11 → text-caption + leading-none 격상 + OQ #4 CTA 인라인 그라데이션 ≥2 (모바일 고정 + 데스크톱 inline) + admin 다운로드 solid + 빈/오류 verbatim + OQ #5 Lucide 4종 + 비즈 anchor 11건 (legalApi 2 + useQuery 1 + useMutation 1 + invalidateQueries 4 키 + useMultiPhotoUpload 직접 + handleResolve memo.trim + handleDownload fflate + iOS PWA + ZIP location 기반 + status open/resolved 분기 + admin selector 패턴 + toast 5 + 카피 verbatim + KVRow/SectionHeader/fmtDate) 1 byte 변경 0
      contains: "bg-surface-page / bg-surface-raised / bg-surface-sunken / border-border-default / border-border-strong / text-text-primary / text-text-secondary / text-text-tertiary / text-text-on-accent / bg-danger / text-text-on-accent / bg-accent / text-body / text-body-sm / text-label / text-caption / font-bold / font-extrabold / font-medium / leading-none / leading-relaxed / rounded-full / rounded-md / rounded-sm / ChevronLeft / Download / Loader2 / Camera / size={20} / size={18} / size={24} / size={22} / animate-spin / from 'lucide-react' / w-11 / h-11 / legal-finding / legal-findings / legal-rounds / legal-round / legalApi / getFinding / resolveFinding / resolution_memo / resolution_photo_keys / useMultiPhotoUpload / buildMetaTxt / fflate / zipSync / 지적사항_ / 지적 상세 / 지적 정보 / 지적 사진 / 조치 내용 / 조치 결과 / 조치 완료 / 조치 처리 실패 / 조치 내용을 입력하세요 / 다운로드 완료 / 다운로드 실패 / 사진 없음 / 사진 첨부 / 업로드 중 / 처리 중... / 다운로드 / 다운로드 중... / 항목을 불러오지 못했습니다 / linear-gradient(135deg, #1d4ed8, #0ea5e9) / staff?.role === 'admin' / s => s.staff"
      min_lines: 250

  key_links:
    - from: "W5 §1 영역 1~3 매핑 verbatim (490 lines, 12 섹션)"
      to: cha-bio-safety/src/pages/LegalFindingDetailPage.tsx line 1~279
      via: "영역 1 (line 1~48) imports + Lucide ChevronLeft+Download+Loader2+Camera 추가 + fmtDate (분까지 표시) className 사용처 후속 영역 + KVRow (label 12 width 64 + children 14 flex 1) className + SectionHeader (12/700 var(--t3) marginBottom 10) className + Spinner 함수 line 41~48 폐기 + @keyframes spin line 45 폐기. 영역 2 (line 51~143) 메인 함수 useState memo/downloading + useAuthStore staff selector + useMultiPhotoUpload 직접 + useQuery legal-finding + useMutation resolveMutation uploadAll+invalidate 4 키+reset+navigate(-1) + handleDownload fflate+iOS PWA+ZIP location 기반+buildMetaTxt + handleResolve memo.trim validation. 영역 3 (line 145~278) JSX render 외곽 (flex 1 column overflow hidden + @keyframes spin line 147 폐기) + 모바일 헤더 OQ #1 raised+border-b + back 44x44 ChevronLeft size 20 + admin 다운로드 44x44 Download size 18 + 데스크톱 타이틀 chrome 0 anchor + 로딩 Loader2 + 에러 단일 문장 + Section 1+2 KVRow+SectionHeader+PhotoGrid+'사진 없음' + Section 3 status open form textarea+사진 5장 슬롯+canAdd Camera 22+데스크톱 inline CTA 그라데이션 + Section 4 status resolved KVRow 3+PhotoGrid + 모바일 고정 하단 CTA 그라데이션 (OQ #4 anchor ≥2)."
      pattern: "영역 1|영역 2|영역 3|imports|메인 함수|JSX render"

    - from: "W5 checklist + W1 §7 OQ LOCKED 5건 + 비즈 anchor 11 (490 lines, sketch checklist)"
      to: cha-bio-safety/docs/redesign-context/21-legal-finding-detail/wave-5-tsx-conversion-checklist.md
      via: "OQ #1 모바일 헤더 raised + border-b + 타이틀 정적 '지적 상세' + back/admin 44x44 / OQ #2 finding 상태 칩 본 페이지 없음 + 화면 모드 mutually exclusive (open Section 3 / resolved Section 4) + KVRow value 좌측 강조 없음 / OQ #3 9·10·11 → text-caption 12 leading-none (슬롯 제거 11 + 업로드 중 10 + 사진 첨부 11 모두 격상) / OQ #4 조치 완료 CTA 인라인 그라데이션 ≥2 (모바일 고정 + 데스크톱 inline 양쪽 박제) + admin 다운로드 solid + 빈/오류 verbatim 아이콘 X / OQ #5 Lucide 4종 (ChevronLeft 20 back 44x44 + Download 18 admin 44x44 + Loader2 24 spinner + Camera 22 canAdd) + Spinner 함수 line 41~48 폐기 + @keyframes spin 2건 폐기 + 이모지 '📷' 폐기. 비즈 anchor 11 — legalApi 2 + useQuery 1 + useMutation 1 + invalidateQueries 4 키 + useMultiPhotoUpload 직접 + handleResolve memo.trim + handleDownload fflate + iOS PWA + ZIP location 기반 + status open/resolved 분기 + admin selector 패턴 + toast 5 + 카피 verbatim."
      pattern: "OQ #1|OQ #2|OQ #3|OQ #4|OQ #5|LOCKED|location 기반|selector 패턴"

    - from: "W2~W4 sketch (sketch HTML 시각 검증, read-only reference)"
      to: cha-bio-safety/docs/redesign-context/21-legal-finding-detail/ sketch-wave-{2,3,4}-*.html
      via: "변환 매핑 시각 검증 — chrome / finding-info / resolve-form sketch HTML. 변경 0 byte (read-only)."
      pattern: "sketch-wave-(2|3|4)"

    - from: "토큰 / Tailwind config (utility class 검증)"
      to: cha-bio-safety/src/styles/tokens.css + cha-bio-safety/tailwind.config.js
      via: "v0.1.x 토큰 확인 — --surface-page/raised/sunken / --text-primary/secondary/tertiary/on-accent / --border-default/strong / --accent / --danger. tailwind.config 매핑 bg-surface-* / border-border-* / text-text-* / bg-accent / bg-danger. **status- prefix 없음** (memory `feedback_tailwind_token_class_pattern`). w-11 h-11 = 44px (memory `feedback_tailwind_w8_h8_is_48px` w-8=48 함정 회피)."
      pattern: "surface-page|surface-raised|surface-sunken|border-default|border-strong|w-11|h-11"

    - from: "legalApi (utils/api.ts, 0 byte 변경 = 가드)"
      to: cha-bio-safety/src/utils/api.ts
      via: "legalApi 2종 시그니처 — getFinding(roundId, findingId) + resolveFinding(roundId, findingId, { resolution_memo, resolution_photo_keys }) snake_case payload verbatim. 본 페이지 2종만 사용 — list/get/getFindings/updateResult/deleteFinding 5종은 19-legal LegalPage + 20-legal-findings LegalFindingsPage 가 각각 사용. 본 페이지 변환 후에도 utils/api.ts 0 byte 변경."
      pattern: "legalApi|getFinding|resolveFinding|resolution_memo|resolution_photo_keys"

    - from: "useMultiPhotoUpload (hooks/useMultiPhotoUpload.ts, 0 byte 변경 = 가드)"
      to: cha-bio-safety/src/hooks/useMultiPhotoUpload.ts
      via: "본 페이지 line 59 `useMultiPhotoUpload()` **직접 호출** — 19-legal LegalPage/20-legal-findings LegalFindingsPage 의 FindingFormSheet 우회 패턴과 다름 (19/20/21 차이 2). 직접 사용 멤버 14종: slots/canAdd/openPicker/closePicker/pickCamera/pickAlbum/handleFiles/removeSlot/uploadAll/reset/isUploading/cameraRef/albumRef/showPicker. 본 페이지 변환 후에도 0 byte 변경 = 가드."
      pattern: "useMultiPhotoUpload|resolutionPhotos|slots|canAdd|uploadAll|reset|cameraRef|albumRef|showPicker"

  scope_negatives:
    - "1 파일만 수정 — cha-bio-safety/src/pages/LegalFindingDetailPage.tsx 외 src/ 트리 변경 0건"
    - "App.tsx 변경 0 byte (final verify gate)"
    - "9 src 파일 변경 0 byte: components/PhotoGrid.tsx / components/PhotoSourceModal.tsx / hooks/useMultiPhotoUpload.ts / hooks/useIsDesktop.ts / utils/findingDownload.ts / utils/api.ts / stores/authStore.ts / pages/LegalPage.tsx (조부모) / pages/LegalFindingsPage.tsx (부모)"
    - "tailwind.config.js / tokens.css / typography.css / types/ / functions/ / templates/ / migrations/ / public/ 모두 0 byte 변경"
    - "sketch HTML (W2 chrome / W3 finding-info / W4 resolve-form) + W5 checklist (490) + W1 index 모두 0 byte 변경 (read-only reference)"
    - "wrangler 명령 0건 (CLAUDE.local.md 디자인 워크트리 강제)"
    - "npm run deploy 0건 (직원 도메인 cbc7119 경로)"
---

<objective>
redesign/21-legal-finding-detail TSX 변환 — LegalFindingDetailPage.tsx (279 lines) **단일 파일 atomic in-place 수정**.

W2 (chrome) + W3 (finding-info) + W4 (resolve-form) sketch + W5 12 섹션 checklist (490) + W1 OQ 5건 LOCKED 결정 을 1-commit 으로 적용. **★ 비즈 anchor 11건 1 byte 변경 0**. **★ Lucide 4종 교체** (ChevronLeft 모바일 헤더 back + Download 모바일 헤더 admin + Loader2 spinner + Camera canAdd, Spinner 함수 line 41~48 + @keyframes spin 2건 + 인라인 SVG path 2종 + 이모지 '📷' 완전 제거, OQ #5 LOCKED). **★ back button + admin 다운로드 button 양쪽 44x44 격상** (옛 36x36, w-8 함정 회피, `w-11 h-11` 권장).

20-legal-findings (bbz) 378 lines mirror 패턴 — 단, 본 21-legal-finding-detail 는 `/legal/:id/finding/:fid` 풀 페이지 단일 export (LegalFindingDetailPage default) + 3 영역 (imports+포맷터+컴포넌트 / 메인 함수+비즈 로직 / JSX render) 통합 변환. bbz (378 lines, 단일 list export) 보다 단순 + lft (571 lines, 3 컴포넌트 + 5 helper) 보다 단순. **★ ZIP 파일명 location 기반 (지적사항_${finding.location}.zip) ★** + **★ useMultiPhotoUpload 직접 호출 ★** 은 본 페이지 고유 anchor (19/20/21 차이 2 + 4).

Purpose:
- v0.1.x 토큰 className 으로 옛 인라인 `var(--bg) / var(--bg2) / var(--bg3) / var(--bd) / var(--bd2) / var(--t1) / var(--t2) / var(--t3) / var(--acl) / var(--danger)` 치환
- **OQ #1 LOCKED**: 모바일 자체 헤더 → `bg-surface-raised border-b border-border-default`. 옛 `background:rgba(22,27,34,0.97), borderBottom:1px solid var(--bd)` (line 152) 완전 폐기. 타이틀 '지적 상세' 정적 (line 158, 20-legal-findings 동적 분기와 다름)
- **OQ #2 LOCKED**: finding 상태 칩 본 페이지 없음 + KVRow value 좌측 강조 없음. status 표시는 **화면 모드 자체** (`finding.status === 'open'` Section 3 form / `'resolved'` Section 4 결과 **mutually exclusive** 분기, line 218 + 253). status- prefix 없음
- **OQ #3 LOCKED**: 9·10·11 fontSize → `text-caption font-bold leading-none` (12 격상). 슬롯 제거 button '✕' 11 (line 231) + 업로드 중 overlay 10 (line 232) + 사진 첨부 button '사진 첨부' 11 (line 236~237) 모두 격상. 인라인 0건
- **OQ #4 LOCKED**: **★ 조치 완료 CTA 인라인 그라데이션 ≥2 박제** `linear-gradient(135deg, #1d4ed8, #0ea5e9)` (★ T3 만 예외 anchor — 데스크톱 inline CTA line 244~248 + 모바일 고정 하단 CTA line 270~275 양쪽 박제, lft 1건 + bbz 1건 패턴 mirror) + **admin 다운로드 button 작은 도구 solid** (모바일 ghost line 159~163 + 데스크톱 solid line 171~175 — `bg-surface-sunken border-border-strong` 또는 ghost className 만, executor 판단으로 그라데이션 적용 가능 — 단, lft/bbz 일관 CTA only 권장). 빈/오류 카피 verbatim. 아이콘 추가 X
- **OQ #5 LOCKED**: Lucide 4종 교체 — `import { ChevronLeft, Download, Loader2, Camera } from 'lucide-react'` 추가 1줄. (a) 모바일 헤더 back button 인라인 SVG path `d="M15 19l-7-7 7-7"` (line 156) 완전 제거 → `<ChevronLeft size={20} />` + back button 36x36 → **44x44 격상**. (b) 모바일 헤더 admin 다운로드 button 인라인 SVG path `d="M12 5v14m0 0l-6-6m6 6l6-6M5 19h14"` (line 161) 완전 제거 → `<Download size={18} />` + admin button 36x36 → **44x44 격상**. (c) **Spinner 함수 line 41~48 정의 완전 폐기** + **@keyframes spin 2건 (line 45 + 147) 모두 폐기** → `<Loader2 className="animate-spin text-accent" size={24} />` 직접 mount (line 180 호출처). (d) **카메라 첨부 button 이모지 '📷' (line 237) 완전 제거** → `<Camera size={22} />`. 이모지 0건
- ★ 비즈 anchor 11건 **1 byte 변경 0** (§3 박제)
- 비즈 로직 0 diff (state/handler/effect/hook/queryKey/mutation/legalApi 2종/snake_case payload/role admin selector/handleZipDownload iOS PWA/ZIP location 기반/handleResolve memo.trim/status open·resolved 분기/useMultiPhotoUpload 직접/buildMetaTxt/fflate zipSync 모두 보존)

Output:
- cha-bio-safety/src/pages/LegalFindingDetailPage.tsx 단일 파일 in-place 수정 (279 → 250~310 lines 예상)
- App.tsx + 9 src 파일 + tailwind.config.js + tokens.css + typography.css + types/ + functions/ + templates/ + migrations/ + public/ 모두 0 byte 변경
- `npm run build` PASS
- atomic 1-commit (단일 파일) + SUMMARY 별도 commit
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jykevin/Documents/cbc7119-design/CLAUDE.md
@/Users/jykevin/Documents/cbc7119-design/CLAUDE.local.md
@/Users/jykevin/Documents/cbc7119-design/.planning/STATE.md

# W5 TSX 변환 checklist (SOURCE OF TRUTH — §1 매핑 verbatim, 12 섹션, 490 lines)
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/21-legal-finding-detail/wave-5-tsx-conversion-checklist.md

# W1 sketch index (OQ LOCKED 5건 + biz anchor 11 + 19/20/21 차이 원본)
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/21-legal-finding-detail/wave-1-index.md

# 토큰 / Tailwind config (utility class 검증, read-only)
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/src/styles/tokens.css
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/tailwind.config.js

# Precedent — 20-legal-findings bbz PLAN.md (직전 사례, mirror skeleton)
@/Users/jykevin/Documents/cbc7119-design/.planning/quick/260524-bbz-redesign-20-legal-findings-tsx-legalfind/260524-bbz-PLAN.md

# 변환 대상 (in-place 수정 단일 파일, executor 가 Read 로 직접 확인)
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/src/pages/LegalFindingDetailPage.tsx

# 변환 무영향 — 0 byte 변경 (final verify gate)
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/src/App.tsx
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/src/components/PhotoGrid.tsx
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/src/components/PhotoSourceModal.tsx
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/src/hooks/useMultiPhotoUpload.ts
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/src/hooks/useIsDesktop.ts
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/src/utils/findingDownload.ts
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/src/utils/api.ts
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/src/stores/authStore.ts
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/src/pages/LegalPage.tsx
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/src/pages/LegalFindingsPage.tsx

</context>

<tasks>

<task type="auto">
  <name>Task 1: LegalFindingDetailPage.tsx 단일 파일 atomic v0.1.x 토큰 변환 (in-place, 비즈 anchor 11건 + Lucide 4종 교체 + Spinner 함수 폐기 + back+admin 44x44 격상 + CTA 그라데이션 ≥2 + OQ LOCKED 5건, atomic 1-commit)</name>
  <files>cha-bio-safety/src/pages/LegalFindingDetailPage.tsx</files>
  <action>

LegalFindingDetailPage.tsx 279 lines 를 **단일 atomic commit** 으로 in-place 수정하여 v0.1.x 토큰 className 으로 치환한다. **23-education (r22) / 19-legal (lft) / 20-legal-findings (bbz) / 17-annual-plan (1hj) / 27-login (gox) / 16-workshift (u5n) / 28-splash (4i9) mirror 패턴 — 단일 파일 atomic**. 본 279 lines 는 단일 export (LegalFindingDetailPage default) + 3 영역 (imports+포맷터+컴포넌트 / 메인 함수+비즈 로직 / JSX render) 통합 변환. 비즈 로직(useState memo+downloading/useParams id+fid/useNavigate/useQueryClient/useQuery legal-finding/useMutation resolveMutation uploadAll+invalidate 4 키+reset+navigate(-1)/legalApi 2종/snake_case payload/handleDownload fflate+iOS PWA+ZIP location 기반/handleResolve memo.trim validation/staff selector 패턴/useMultiPhotoUpload 직접/buildMetaTxt) 1 byte 변경 0. **★ 비즈 anchor 11건 + Lucide 4종 교체 (ChevronLeft+Download+Loader2+Camera) + Spinner 함수 폐기 + @keyframes spin 2건 폐기 + 인라인 SVG path 2종 폐기 + 이모지 '📷' 폐기 + back/admin 44x44 격상 + CTA 그라데이션 ≥2 박제 모두 grep PASS**.

## 0. 사전 확인 (필수)

```bash
# 0-1. branch 확인 (worktree-aware)
git branch --show-current   # 기대: redesign/21-legal-finding-detail
git status --short          # 기대: 빈 출력 (clean) 또는 PLAN.md 만

# 0-2. branch base 검증
EXPECTED_BASE="origin/main"
git merge-base HEAD "$EXPECTED_BASE" >/dev/null 2>&1 && echo "branch base OK" || echo "branch base FAIL"

# 0-3. 변환 대상 파일 라인 수 baseline
wc -l cha-bio-safety/src/pages/LegalFindingDetailPage.tsx   # 기대: 279

# 0-4. 9 src 파일 + tailwind.config.js baseline (0 byte 변경 verify gate)
for f in cha-bio-safety/src/App.tsx \
         cha-bio-safety/src/components/PhotoGrid.tsx \
         cha-bio-safety/src/components/PhotoSourceModal.tsx \
         cha-bio-safety/src/hooks/useMultiPhotoUpload.ts \
         cha-bio-safety/src/hooks/useIsDesktop.ts \
         cha-bio-safety/src/utils/findingDownload.ts \
         cha-bio-safety/src/utils/api.ts \
         cha-bio-safety/src/stores/authStore.ts \
         cha-bio-safety/src/pages/LegalPage.tsx \
         cha-bio-safety/src/pages/LegalFindingsPage.tsx \
         cha-bio-safety/tailwind.config.js; do
  git rev-parse "HEAD:$f" 2>/dev/null | head -c 12; echo "  $f"
done

# 0-5. 인라인 alias / 이모지 / 인라인 SVG / Spinner 함수 / @keyframes spin 현 상태 (negative gate baseline)
grep -c "var(--" cha-bio-safety/src/pages/LegalFindingDetailPage.tsx           # baseline: 다수 (변환 후 0)
grep -cE "function Spinner|const Spinner" cha-bio-safety/src/pages/LegalFindingDetailPage.tsx  # baseline: 1 (변환 후 0)
grep -c "M15 19l-7-7 7-7" cha-bio-safety/src/pages/LegalFindingDetailPage.tsx  # baseline: 1 (변환 후 0)
grep -c "M12 5v14m0 0l-6-6m6 6l6-6M5 19h14" cha-bio-safety/src/pages/LegalFindingDetailPage.tsx  # baseline: 1 (변환 후 0)
grep -c "@keyframes spin" cha-bio-safety/src/pages/LegalFindingDetailPage.tsx  # baseline: 2 (변환 후 0)
grep -c "📷" cha-bio-safety/src/pages/LegalFindingDetailPage.tsx               # baseline: 1 (변환 후 0)
grep -c "linear-gradient" cha-bio-safety/src/pages/LegalFindingDetailPage.tsx  # baseline: 0 (변환 후 ≥2 = OQ #4 CTA 양쪽)
```

## 1. 변환 매핑 (W5 §1 verbatim — 3 영역, 추측 0건)

> **executor 는 LegalFindingDetailPage.tsx 한 번 Read 로 279 lines 전체 확인 후, 아래 영역 분할에 따라 in-place Edit 적용.**
> **W5 checklist (490 lines) 가 SOURCE OF TRUTH — 본 PLAN.md 와 충돌 시 W5 checklist 우선.**

### §1.1 영역 1 — Imports + 포맷터 + 컴포넌트 (line 1~48, W5 §1 영역 1)

| 현재 (인라인, line) | 변환 후 (className) | sketch / OQ |
|---|---|---|
| imports (line 1~12) | **그대로** + **추가 1줄**: `import { ChevronLeft, Download, Loader2, Camera } from 'lucide-react'` (OQ #5 LOCKED 4종) | OQ #5 |
| `fmtDate` 포맷터 (line 15~19) | **그대로** (★ 비즈 anchor — 분까지 표시 verbatim) — 사용처 className 만 후속 영역에서 격상 (KVRow children 안에서 호출) | - |
| `KVRow({ label, children })` (line 22~29) | className 치환 — `<div className="flex gap-3 items-start">` + `<span className="text-caption text-text-tertiary leading-none flex-shrink-0" style={{ width: 64 }}>{label}</span>` + `<span className="text-body-sm text-text-primary flex-1 leading-relaxed">{children}</span>`. 옛 인라인 fontSize 12 / 14 + var(--t3) / var(--t1) + lineHeight 1.5 완전 폐기. width 64 인라인 유지 (Tailwind w-16 = 64 대안 가능) | OQ #3 |
| `SectionHeader({ children })` (line 32~38) | className 치환 — `<div className="text-caption font-bold text-text-tertiary leading-none" style={{ marginBottom: 10 }}>{children}</div>`. 옛 인라인 fontSize 12 / fontWeight 700 / var(--t3) / marginBottom 10 완전 폐기 (marginBottom 인라인 유지 또는 mb-2.5 대안) | OQ #3 |
| **★ Spinner 함수 (line 41~48) ★★★** | **완전 폐기 (OQ #5 LOCKED)** — `function Spinner() { return <div style={{ flex:1, display:'flex', alignItems:'center', justifyContent:'center' }}><div style={{ width:28, height:28, border:'2px solid var(--bd2)', borderTopColor:'var(--acl)', borderRadius:'50%', animation:'spin .7s linear infinite' }} /><style>{'@keyframes spin{to{transform:rotate(360deg)}}'}</style></div>; }` 정의 8 lines 완전 제거. 호출처 (line 180 `<Spinner />`) → `<div className="flex-1 flex items-center justify-center"><Loader2 className="animate-spin text-accent" size={24} /></div>` 직접 mount | OQ #5 |

### §1.2 영역 2 — 메인 함수 LegalFindingDetailPage (line 51~143, W5 §1 영역 2)

★ **0 byte 변경** — 모든 비즈 anchor 11건 verbatim 보존:

| 현재 (인라인 / 비즈 로직, line) | 변환 후 (className + 인라인) | sketch / OQ |
|---|---|---|
| **★ useParams (id, fid 추출) + useNavigate + useQueryClient (line 52~54)** | **그대로** (★ 비즈 anchor) — `useParams<{ id: string; fid: string }>()` 시그니처 1 byte 변경 0 | - |
| **★ useState memo + downloading (line 56~57)** | **그대로** — 비즈 state | - |
| **★ staff = useAuthStore(s => s.staff) (line 58)** | **그대로** (★ 비즈 anchor 7 selector 패턴) — 20-legal-findings getState() 와 다름 | - |
| **★ resolutionPhotos = useMultiPhotoUpload() (line 59)** | **그대로** (★ 비즈 anchor 4) — **직접 호출**, 19-legal/20-legal-findings FindingFormSheet 우회와 다름 (19/20/21 차이 2) | - |
| **★ useQuery (line 61~65)** | **그대로** (★ 비즈 anchor 2) — `queryKey: ['legal-finding', id, fid]` + `enabled: !!id && !!fid` 1 byte 변경 0 | - |
| **★ useMutation resolveMutation (line 67~87)** | **그대로** (★ 비즈 anchor 3) — uploadAll 선행 + snake_case payload (resolution_memo / resolution_photo_keys) + invalidateQueries 4 키 onSuccess (legal-finding → legal-findings → legal-rounds → legal-round 순서) + toast '조치 완료' + resolutionPhotos.reset() + navigate(-1) 순서 1 byte 변경 0 | - |
| **★ handleDownload (line 89~130)** | **그대로** (★ 비즈 anchor 8 + 9) — fflate dynamic import + zipSync + buildMetaTxt(finding) + Promise.allSettled photoKeys + resolutionPhotoKeys + **ZIP 파일명 location 기반** `(finding.location ?? '위치없음').replace(/[\/\\\\:*?\"<>|]/g, '_')` (line 96) + `지적사항_${name}.zip` (line 119, 19/20 차이 4) + **iOS PWA `<a download>` 패턴** createElement + body.appendChild + click + removeChild + setTimeout URL.revokeObjectURL 3000 + toast '다운로드 완료' / '다운로드 실패' 1 byte 변경 0 | - |
| **★ handleResolve (line 132~138)** | **그대로** (★ 비즈 anchor 5) — `memo.trim()` 빈 값 → `toast.error('조치 내용을 입력하세요')` validation + resolveMutation.mutate() 1 byte 변경 0 | - |
| isSubmitting / isDesktop / sectionPad (line 140~143) | **그대로** — `useIsDesktop()` hook 호출 + `sectionPad = isDesktop ? '20px 32px' : '20px 16px'` 1 byte 변경 0 | - |

### §1.3 영역 3 — JSX render (line 145~278, W5 §1 영역 3, OQ #1+#2+#3+#4+#5 통합 적용)

| 현재 (인라인 style, line) | 변환 후 (className + 인라인) | sketch / OQ |
|---|---|---|
| **외곽 (line 146)** | 옛 `style={{ flex:1, display:'flex', flexDirection:'column', background:'var(--bg)', height:'100%', overflow:'hidden' }}` → className `flex-1 flex flex-col bg-surface-page h-full overflow-hidden`. 옛 var(--bg) 완전 폐기 | - |
| **★ @keyframes spin (line 147) ★★★** | **완전 제거 (OQ #5 LOCKED)** — `<style>{'@keyframes spin{to{transform:rotate(360deg)}}'}</style>` 1 line 폐기 (Lucide Loader2 animate-spin 사용) | OQ #5 |
| **★ 모바일 자체 헤더 (line 149~165) ★★★ (OQ #1 + OQ #5)** | 외곽: 옛 `style={{ height:48, background:'rgba(22,27,34,0.97)', borderBottom:'1px solid var(--bd)', display:'flex', alignItems:'center', justifyContent:'center', position:'relative', flexShrink:0 }}` → className `bg-surface-raised border-b border-border-default flex items-center justify-center relative flex-shrink-0` + 인라인 `height:48`. **★ back button (line 155~157) OQ #5 ★★★**: 옛 인라인 SVG `<svg ...><path strokeLinecap="round" strokeLinejoin="round" d="M15 19l-7-7 7-7" /></svg>` 완전 제거 → `<ChevronLeft size={20} />`. width:36 height:36 → **width:44 height:44 격상** (w-8 함정 회피 — className `w-11 h-11` 권장, memory `feedback_tailwind_w8_h8_is_48px`) + 인라인 `position:absolute, left:8, background:'none', border:'none', cursor:'pointer', display:'flex', alignItems:'center', justifyContent:'center', color:'var(--text-primary)'` 또는 className `text-text-primary`. **★ 타이틀 (line 158) 정적 '지적 상세'** className `text-body font-bold text-text-primary` (verbatim, 20-legal-findings 동적 분기와 다름). **★ admin 다운로드 button (line 159~163) OQ #5 ★★★**: `staff?.role === 'admin' && finding` 분기 (★ 비즈 anchor 7 보존). 옛 인라인 SVG `<svg ...><path strokeLinecap="round" strokeLinejoin="round" d="M12 5v14m0 0l-6-6m6 6l6-6M5 19h14" /></svg>` 완전 제거 → `<Download size={18} />`. width:36 height:36 → **width:44 height:44 격상** (w-11 h-11 권장) + 인라인 `position:absolute, right:8, background:'none', border:'none', cursor: downloading ? 'not-allowed' : 'pointer', display:'flex', alignItems:'center', justifyContent:'center', opacity: downloading ? 0.5 : 1` + className `text-text-primary` | OQ #1 + OQ #5 |
| **데스크톱 타이틀 (line 168~177)** | 외곽 className `flex items-center justify-between flex-shrink-0` + 인라인 `padding:'24px 32px 12px'` (글로벌 chrome 0 anchor — App.tsx line 117 정규식 `!location.pathname.match(/^\\/legal\\/.+/)` showNav=false 보존). 타이틀 className `text-text-primary` + 인라인 `fontSize:22, fontWeight:800` 또는 className `text-2xl font-extrabold text-text-primary`. admin 다운로드 button className `bg-surface-sunken border border-border-strong text-text-primary text-caption font-bold leading-none rounded-sm` + 인라인 `height:36, padding:'0 16px', cursor: downloading ? 'not-allowed' : 'pointer', opacity: downloading ? 0.5 : 1`. var(--bg3) / var(--bd2) / var(--t1) 옛 alias 완전 폐기 | - |
| **★ 로딩 (line 180) Spinner 호출 ★★★** | 옛 `{isLoading && <Spinner />}` → 직접 mount: `{isLoading && <div className="flex-1 flex items-center justify-center"><Loader2 className="animate-spin text-accent" size={24} /></div>}` (Spinner 함수 영역 1 에서 폐기) | OQ #5 |
| **에러 단일 문장 (line 183~187)** | 외곽 className `flex-1 flex items-center justify-center text-center text-body-sm text-text-secondary` + 인라인 `padding:'0 24px'`. 카피 verbatim '항목을 불러오지 못했습니다. 뒤로 가서 다시 시도하세요.' | OQ #4 |
| **콘텐츠 외곽 (line 190~195)** | className `flex-1 overflow-y-auto` + 인라인 `paddingBottom: finding.status === 'open' ? (isDesktop ? 24 : 'calc(72px + var(--sab, 0px))') : 24, maxWidth: isDesktop ? 700 : undefined` 유지 (비즈 anchor — status 분기 + iOS safe-area 보존) | - |
| **Section 1: 지적 정보 (line 197~205)** | 외곽 className `border-b border-border-default` + 인라인 `padding: sectionPad`. SectionHeader '지적 정보' verbatim. KVRow 4행: '지적 내용' (whiteSpace:pre-wrap) / '위치' (fallback '-') / '등록일' (fmtDate) / '등록자' (?? 패턴) verbatim. (★ OQ #2 — KVRow value 좌측 강조 없음, borderLeft 인라인 0건) | OQ #2 |
| **Section 2: 지적 사진 (line 207~215)** | 외곽 className `border-b border-border-default` + 인라인 `padding: sectionPad`. SectionHeader '지적 사진' verbatim. PhotoGrid 분기: `finding.photoKeys && finding.photoKeys.length > 0` → `<div style={{ marginTop: 8 }}><PhotoGrid photoUrls={finding.photoKeys.map(k => '/api/uploads/' + k)} /></div>` 그대로. 빈: '사진 없음' className `text-body-sm text-text-tertiary` + 인라인 `marginTop:8` (옛 var(--t3) + fontSize 13 폐기). | - |
| **★ Section 3 status open form (line 218~250) ★★★ (OQ #3 + OQ #4 + OQ #5)** | 외곽 분기 `{finding.status === 'open' && (` (★ 비즈 anchor 6 — mutually exclusive 분기 보존). 외곽 className `border-b border-border-default` + 인라인 `padding: sectionPad`. SectionHeader '조치 내용' verbatim. textarea (line 221) className `bg-surface-sunken border border-border-strong text-text-primary text-body-sm rounded-sm` + 인라인 `width:'100%', padding:'10px 12px', boxSizing:'border-box', fontFamily:'inherit', lineHeight:1.5, resize:'vertical', outline:'none'` (옛 var(--bg3) / var(--bd2) / var(--t1) / fontSize 13 폐기, borderRadius 9 → rounded-sm 또는 인라인 유지). placeholder '조치 내용을 입력하세요' verbatim. **조치 사진 (최대 5장) 라벨 (line 223)** className `text-caption font-bold text-text-tertiary leading-none` + 인라인 `marginBottom:6` (옛 fontSize 12 / fontWeight 700 / var(--t3) 폐기). input cameraRef / albumRef (line 224~225) 그대로. PhotoSourceModal (line 226) 그대로. 슬롯 컨테이너 (line 227) className `flex gap-2 overflow-x-auto` + 인라인 `paddingBottom:4`. **슬롯 img (line 230)** className `border border-border-default` + 인라인 `width:72, height:72, objectFit:'cover', borderRadius:10, display:'block'` (rounded utility 또는 인라인 유지). **★ 슬롯 제거 button '✕' (line 231) OQ #3 격상 ★** className `bg-danger rounded-full border-0 text-text-on-accent text-caption font-bold leading-none cursor-pointer flex items-center justify-center` + 인라인 `position:'absolute', top:-6, right:-6, width:20, height:20`. 옛 var(--danger) + fontSize 11 + fontWeight 700 폐기. ★ **fontSize 11 → text-caption 12 leading-none 격상** (OQ #3 LOCKED). **★ slot.uploading overlay '업로드 중' (line 232) OQ #3 격상 ★** className `absolute inset-0 rounded-md flex items-center justify-center text-caption leading-none text-text-on-accent` + 인라인 `background:'rgba(0,0,0,0.4)'`. ★ **fontSize 10 → text-caption 12 leading-none 격상** (OQ #3 LOCKED). **★ canAdd 사진 첨부 button (line 235~239) OQ #3 + OQ #5 ★★★** className `bg-surface-sunken border border-dashed border-border-strong text-text-tertiary text-caption font-bold leading-none cursor-pointer flex flex-col items-center justify-center flex-shrink-0 rounded-md gap-1` + 인라인 `width:72, height:72`. ★ **이모지 '📷' 완전 제거 (line 237) → `<Camera size={22} />`** (OQ #5 LOCKED). 라벨 '사진 첨부' verbatim, ★ **fontSize 11 → text-caption 12 leading-none 격상** (OQ #3 LOCKED). **★ 데스크톱 inline 조치 완료 CTA (line 244~248) OQ #4 LOCKED 그라데이션 anchor 1 ★★★** `{isDesktop && (` 분기 안 button: 옛 `style={{ marginTop:16, width:'100%', height:48, background:'var(--acl)', color:'#fff', fontSize:14, fontWeight:700, border:'none', borderRadius:12, cursor: isSubmitting ? 'not-allowed' : 'pointer', opacity: isSubmitting ? 0.5 : 1 }}` → className `w-full text-text-on-accent text-body font-bold border-0 rounded-md cursor-pointer` + 인라인 `marginTop:16, height:48, cursor: isSubmitting ? 'not-allowed' : 'pointer', opacity: isSubmitting ? 0.5 : 1, **background: 'linear-gradient(135deg, #1d4ed8, #0ea5e9)'**` (★ OQ #4 LOCKED 예외 anchor 1, var(--acl) 완전 폐기). 라벨 '처리 중...' / '조치 완료' verbatim | OQ #2 + OQ #3 + OQ #4 + OQ #5 |
| **★ Section 4 status resolved (line 253~265) ★★★** | 외곽 분기 `{finding.status === 'resolved' && (` (★ 비즈 anchor 6 — mutually exclusive 분기 보존). 외곽 className `border-b border-border-default` + 인라인 `padding: sectionPad`. SectionHeader '조치 결과' verbatim. KVRow 3행: '조치일시' (fmtDate finding.resolvedAt) / '조치자' (resolvedByName ?? resolvedBy ?? '-') / '조치 내용' (whiteSpace:pre-wrap, ?? '-') verbatim. PhotoGrid 분기: `finding.resolutionPhotoKeys && finding.resolutionPhotoKeys.length > 0` → `<div style={{ marginTop: 12 }}><PhotoGrid photoUrls={finding.resolutionPhotoKeys.map(k => '/api/uploads/' + k)} /></div>` 그대로 | OQ #2 |
| **★ 모바일 고정 하단 CTA (line 270~275) OQ #4 LOCKED 그라데이션 anchor 2 ★★★** | 외곽 분기 `{!isDesktop && !isLoading && !error && finding && finding.status === 'open' && (` 그대로. 외곽 className `bg-surface-page border-t border-border-default` + 인라인 `position:'fixed', bottom:0, left:0, right:0, padding:'12px 16px', paddingBottom:'calc(12px + var(--sab, 0px))'` (옛 var(--bg) / var(--bd) 폐기, position fixed + iOS safe-area 보존). **button**: 옛 `style={{ width:'100%', height:48, background:'var(--acl)', color:'#fff', fontSize:14, fontWeight:700, border:'none', borderRadius:12, cursor: isSubmitting ? 'not-allowed' : 'pointer', opacity: isSubmitting ? 0.5 : 1, transition:'opacity 0.15s' }}` → className `w-full text-text-on-accent text-body font-bold border-0 rounded-md cursor-pointer` + 인라인 `height:48, cursor: isSubmitting ? 'not-allowed' : 'pointer', opacity: isSubmitting ? 0.5 : 1, transition:'opacity 0.15s', **background: 'linear-gradient(135deg, #1d4ed8, #0ea5e9)'**` (★ OQ #4 LOCKED 예외 anchor 2, var(--acl) 완전 폐기). 라벨 '처리 중...' / '조치 완료' verbatim | OQ #4 |

> **★ executor 주의**: W5 checklist (490 lines) 가 SOURCE OF TRUTH. 위 표는 본 페이지 279 lines 패턴 + 19-legal/20-legal-findings 차이 (selector / 직접 호출 / location 기반 ZIP) 반영 요약. **충돌 시 W5 checklist 우선**, executor 가 Read 후 line 번호 / 정확한 매핑 확정.

## 2. OQ 5건 LOCKED 결정 (W1 §7 + W5 §4 verbatim — 위반 0건)

### OQ #1 LOCKED — 모바일 자체 헤더 `bg-surface-raised border-b border-border-default` + 타이틀 정적 '지적 상세' + back/admin 44x44 격상
- 모바일 헤더 외곽 (line 149~154): `background:'rgba(22,27,34,0.97)', borderBottom:'1px solid var(--bd)'` 인라인 완전 제거 → className `bg-surface-raised border-b border-border-default flex items-center justify-center relative flex-shrink-0` + 인라인 `height:48`
- 타이틀 (line 158): '지적 상세' 정적 (20-legal-findings 동적 분기와 다름) className `text-body font-bold text-text-primary`
- back button (line 155) 44x44 격상 (OQ #5 통합)
- admin 다운로드 button (line 159~163) 44x44 격상 (OQ #5 통합)
- grep gate `bg-surface-raised border-b border-border-default >= 1` + `'지적 상세' >= 2 (모바일 + 데스크톱)`

### OQ #2 LOCKED — finding 상태 칩 본 페이지 없음 + 화면 모드 mutually exclusive + KVRow value 좌측 강조 없음 (status- prefix 없음)
- **★ finding 상태 칩 자체 없음** ★ — 20-legal-findings (bbz) border-l-2 + 칩 패턴과 다름 (카드 list 가 아닌 상세 페이지)
- status 표시 = **화면 모드 자체** (`finding.status === 'open'` Section 3 form / `'resolved'` Section 4 결과 mutually exclusive 분기, line 218 + 253)
- **KVRow value 좌측 강조 없음** (borderLeft 등 인라인 0건 — KVRow 컴포넌트 정의 line 22~29 보존)
- **status- prefix 없음** (memory `feedback_tailwind_token_class_pattern` — 정확 = `bg-danger`, `text-danger` / 잘못 = `bg-status-danger`, `text-status-danger`)
- grep gate `finding.status === 'open' >= 1` + `finding.status === 'resolved' >= 1` + KVRow borderLeft 패턴 0건 검사

### OQ #3 LOCKED — 9·10·11 fontSize → text-caption 12 leading-none 격상
- 슬롯 제거 button '✕' (line 231) fontSize 11 → `text-caption font-bold leading-none`
- 업로드 중 overlay (line 232) fontSize 10 → `text-caption leading-none`
- canAdd 사진 첨부 button (line 236) fontSize 11 → `text-caption font-bold leading-none`
- KVRow label (line 25) fontSize 12 → `text-caption leading-none`
- SectionHeader (line 34) fontSize 12 fontWeight 700 → `text-caption font-bold leading-none`
- '조치 사진 (최대 5장)' 라벨 (line 223) fontSize 12 fontWeight 700 → `text-caption font-bold leading-none`
- fontSize 9·10·11 인라인 0건 (negative gate)
- grep gate `text-caption >= 5` + `leading-none >= 5`

### OQ #4 LOCKED — 조치 완료 CTA 인라인 그라데이션 ≥2 박제 + admin 다운로드 solid + 빈/오류 verbatim + 아이콘 X
- **★ 데스크톱 inline 조치 완료 CTA (line 244~248)** ★ 옛 `background:'var(--acl)'` 완전 제거 → 인라인 `background: 'linear-gradient(135deg, #1d4ed8, #0ea5e9)'` 추가 (★ OQ #4 LOCKED 예외 anchor 1)
- **★ 모바일 고정 하단 조치 완료 CTA (line 270~275)** ★ 옛 `background:'var(--acl)'` 완전 제거 → 인라인 `background: 'linear-gradient(135deg, #1d4ed8, #0ea5e9)'` 추가 (★ OQ #4 LOCKED 예외 anchor 2)
- **admin 다운로드 button (모바일 ghost + 데스크톱 solid)** 작은 도구 solid 유지 (var(--bg3) → bg-surface-sunken, var(--bd2) → border-border-strong, var(--t1) → text-text-primary). 모바일 ghost button (line 159~163) 은 background:'none' 유지. **executor 판단으로 admin 다운로드 도 그라데이션 적용 가능 — 단, lft/bbz 일관 CTA only 권장**
- **빈/오류 카피 verbatim** — '항목을 불러오지 못했습니다. 뒤로 가서 다시 시도하세요.' (line 185) + '사진 없음' (line 213) + toast 5종 (성공 2 + 실패 3) + 헤더 '지적 상세' 정적 + SectionHeader 4종 + KVRow label 7종 + 위치 fallback '-' + 등록자 fallback `??` + textarea placeholder + '사진 첨부' + '✕' + '업로드 중' + '조치 완료' / '처리 중...' / '다운로드' / '다운로드 중...'
- 아이콘 / lucide / SVG 추가 X (back / admin 다운로드 / spinner / Camera 외 빈/오류 영역 아이콘 무)
- grep gate `linear-gradient(135deg, #1d4ed8, #0ea5e9) >= 2`

### OQ #5 LOCKED — Lucide 4종 + back/admin 44x44 격상 + Spinner 폐기 + 이모지 폐기
- **추가**: `import { ChevronLeft, Download, Loader2, Camera } from 'lucide-react'` (line 14 또는 imports 끝)
- **(a) 모바일 헤더 back button ChevronLeft 교체 + 44x44 격상**: 옛 인라인 SVG `<svg ...><path strokeLinecap="round" strokeLinejoin="round" d="M15 19l-7-7 7-7" /></svg>` (line 156) 완전 제거 → `<ChevronLeft size={20} />`. width:36 height:36 → **width:44 height:44 격상** (`w-11 h-11` 권장, w-8 함정 회피)
- **(b) 모바일 헤더 admin 다운로드 button Download 교체 + 44x44 격상**: 옛 인라인 SVG `<svg ...><path strokeLinecap="round" strokeLinejoin="round" d="M12 5v14m0 0l-6-6m6 6l6-6M5 19h14" /></svg>` (line 161) 완전 제거 → `<Download size={18} />`. width:36 height:36 → **width:44 height:44 격상** (`w-11 h-11` 권장)
- **(c) Spinner 함수 line 41~48 정의 완전 폐기 + @keyframes spin 2건 (line 45 + 147) 모두 폐기 + Loader2 직접 사용**: 옛 `function Spinner() { return <div style={{ ... animation:'spin .7s linear infinite' }} /><style>{'@keyframes spin{...}'}</style> }` 8 lines 완전 제거. line 147 외곽 `<style>{'@keyframes spin{to{transform:rotate(360deg)}}'}</style>` 완전 제거. 호출처 (line 180 `<Spinner />`) → `<div className="flex-1 flex items-center justify-center"><Loader2 className="animate-spin text-accent" size={24} /></div>` 직접 mount
- **(d) 카메라 첨부 button 이모지 '📷' 완전 제거 + Camera 22 교체**: 옛 `<span style={{ fontSize: 22 }}>📷</span>` (line 237) 완전 제거 → `<Camera size={22} />`. **이모지 0건** (canAdd '사진 첨부' button 안 이모지 폐기)
- grep gate `import { ChevronLeft, Download, Loader2, Camera } from 'lucide-react' >= 1` + `<ChevronLeft size={20} >= 1` + `<Download size={18} >= 1` + `<Loader2 >= 1` + `<Camera size={22} >= 1` + `function Spinner = 0` + `const Spinner = 0` + `M15 19l-7-7 7-7 = 0` + `M12 5v14m0 0l-6-6m6 6l6-6M5 19h14 = 0` + `📷 = 0` + `@keyframes spin = 0` + back/admin 44x44 `w-11 h-11 >= 2` 또는 인라인 `width:44, height:44 >= 2`

## 3. ★ 비즈 anchor 11건 보존 (19-legal lft / 20-legal-findings bbz / 23-education r22 precedent — 1 byte 변경 금지)

다음은 **변경 0** — className 추가 + 인라인 alias 토큰 제거 외 logic / 상수 / 값 / 카피 / 시그니처 라인 손대지 않음:

1. **legalApi 2종 시그니처** — `legalApi.getFinding(id!, fid!)` (line 63) + `legalApi.resolveFinding(id!, fid!, { resolution_memo: memo.trim(), resolution_photo_keys: photoKeys.length > 0 ? photoKeys : undefined })` (line 70~73). **snake_case payload** verbatim. utils/api.ts 0 byte 변경 = 가드. 본 페이지 2종만 — 19-legal LegalPage 5종 (list/get/getFindings/updateResult/deleteFinding) + 20-legal-findings 4종 과 다름.

2. **useQuery × 1** — `queryKey: ['legal-finding', id, fid]` + `queryFn: () => legalApi.getFinding(id!, fid!)` + `enabled: !!id && !!fid` (line 61~65). 캐시 키 + 조건부 fetch anchor.

3. **useMutation resolveMutation + invalidateQueries 4 키 onSuccess + reset + navigate(-1)** — mutationFn uploadAll 선행 (line 69) + snake_case payload (line 70~73). onSuccess (line 75~82): invalidateQueries 4 키 **순서 보존** (legal-finding → legal-findings → legal-rounds → legal-round) + `toast.success('조치 완료')` + `resolutionPhotos.reset()` + `navigate(-1)`. onError: `toast.error('조치 처리 실패')`. lft/bbz 비즈 anchor 2~4 와 일치, 본 페이지 4 키 (lft 3 키 와 다름).

4. **useMultiPhotoUpload 직접 호출** (line 59) — `const resolutionPhotos = useMultiPhotoUpload()`. **19-legal/20-legal-findings 의 FindingFormSheet 우회와 다름 (19/20/21 차이 2)**. 직접 사용 멤버 14종: slots / canAdd / openPicker / closePicker / pickCamera / pickAlbum / handleFiles / removeSlot / uploadAll / reset / isUploading / cameraRef / albumRef / showPicker.

5. **handleResolve memo.trim() validation** (line 132~138) — `if (!memo.trim()) { toast.error('조치 내용을 입력하세요'); return; } resolveMutation.mutate()`. 빈 값 가드 + toast 검증.

6. **finding.status === 'open' / 'resolved' mutually exclusive 분기** (line 218 + 253) — Section 3 form (open) / Section 4 결과 (resolved) 동시 표시 불가. 화면 모드 자체 status 표시 (OQ #2 LOCKED — 칩 없음).

7. **staff = useAuthStore(s => s.staff) selector 패턴** (line 58) — **20-legal-findings getState() 와 다름**. `staff?.role === 'admin' && finding` 모바일 (line 159) + 데스크톱 (line 171) 양쪽 admin 다운로드 분기.

8. **handleDownload (line 89~130) fflate dynamic import + zipSync + Promise.allSettled + buildMetaTxt + iOS PWA `<a download>` 패턴** — `const { zipSync } = await import('fflate')` (line 93) + `files['내용.txt'] = encoder.encode(buildMetaTxt(finding))` (line 98) + `Promise.allSettled(finding.photoKeys.map(k => fetch('/api/uploads/' + k).then(r => r.arrayBuffer())))` (line 100, 107) + `zipSync(files, { level: 6 })` (line 114) + `document.createElement('a') + a.href + a.download + body.appendChild + a.click() + body.removeChild + setTimeout(() => URL.revokeObjectURL(url), 3000)` (line 117~123). iOS PWA 호환 패턴 verbatim (memory `feedback_ios_pwa_push_silent_drop` 일반화).

9. **ZIP 파일명 location 기반** (line 96, 119) — `const name = (finding.location ?? '위치없음').replace(/[\/\\\\:*?"<>|]/g, '_')` (line 96) + `a.download = \`지적사항_${name}.zip\`` (line 119). **20-legal-findings round.title 기반 + 19-legal 패턴과 다름 (19/20/21 차이 4)**.

10. **toast 카피 5종 verbatim** — success 2: '조치 완료' (line 80) + '다운로드 완료' (line 124). error 3: '조치 처리 실패' (line 85) + '조치 내용을 입력하세요' (line 134) + '다운로드 실패' (line 126).

11. **카피 verbatim 전체 + KVRow/SectionHeader/fmtDate 컴포넌트 구조** — 헤더 '지적 상세' (line 158, 정적) + SectionHeader 4종 ('지적 정보' / '지적 사진' / '조치 내용' / '조치 결과' line 198/209/220/255) + KVRow label 7종 ('지적 내용' / '위치' / '등록일' / '등록자' / '조치일시' / '조치자' / '조치 내용' line 200~204 + 257~259) + 위치 fallback '-' (line 201) + 등록자 fallback `??` (line 203) + '사진 없음' (line 213) + '조치 사진 (최대 5장)' (line 223) + textarea placeholder '조치 내용을 입력하세요' (line 221) + '사진 첨부' (line 237) + '✕' (line 231) + '업로드 중' (line 232) + '조치 완료' / '처리 중...' (line 246, 273) + '다운로드' / '다운로드 중...' (line 173) + 오류 '항목을 불러오지 못했습니다. 뒤로 가서 다시 시도하세요.' (line 185) + KVRow/SectionHeader/fmtDate 컴포넌트 구조 line 15~38 보존.

## 4. 비즈 로직 0 diff 보존

### 영역 1 (line 1~48): imports + 포맷터 + 컴포넌트
- imports 13개 + 추가 1줄 lucide-react 4종
- fmtDate (분까지 표시) verbatim
- KVRow / SectionHeader 컴포넌트 구조 verbatim
- Spinner 함수 폐기 (OQ #5 LOCKED)

### 영역 2 (line 51~143): 메인 함수 LegalFindingDetailPage
- useParams id/fid + useNavigate + useQueryClient + useState memo/downloading + staff selector + useMultiPhotoUpload 직접
- useQuery 1 + useMutation 1 + invalidateQueries 4 키
- handleDownload fflate + iOS PWA + ZIP location 기반
- handleResolve memo.trim() validation
- isSubmitting / isDesktop / sectionPad

### 영역 3 (line 145~278): JSX render
- 외곽 + @keyframes spin 폐기
- 모바일 자체 헤더 + back 44x44 ChevronLeft + admin 44x44 Download
- 데스크톱 타이틀 + admin solid 다운로드
- 로딩 Loader2 직접 mount
- 에러 단일 문장 verbatim
- 콘텐츠 외곽 + paddingBottom 분기 보존
- Section 1+2 KVRow + SectionHeader + PhotoGrid + '사진 없음'
- Section 3 status open form + textarea + 사진 5장 슬롯 + canAdd Camera + 데스크톱 inline CTA 그라데이션
- Section 4 status resolved + KVRow 3 + PhotoGrid
- 모바일 고정 하단 CTA 그라데이션

### 카피 verbatim 전체 — toast 5 + 헤더/SectionHeader/KVRow label/fallback/placeholder/슬롯/CTA/admin/오류

## 5. 작업 순서

1. wc -l 으로 279 baseline 확인
2. LegalFindingDetailPage.tsx 한 번 Read (279 lines 전체)
3. W5 checklist Read (490 lines) — SOURCE OF TRUTH 매핑 확정
4. App.tsx + 9 src 파일 + tailwind.config.js baseline hash 저장
5. Edit/Write in-place 수정 순서: §1.1 (imports + Lucide 4종 추가 + Spinner 폐기) → §1.2 (메인 함수 verbatim, 비즈 anchor 11 0 byte) → §1.3 (JSX render, 외곽 @keyframes spin 폐기 + 모바일 헤더 OQ #1+#5 + 데스크톱 + 로딩 Loader2 + 에러 + 콘텐츠 + Section 1+2 + Section 3 OQ #3+#4+#5 + Section 4 + 모바일 고정 CTA OQ #4)
6. tsc --noEmit 0 errors 확인
7. build PASS 확인
8. negative gate + positive gate + biz anchor 11 모두 PASS 확인
9. atomic 1-commit:
   ```bash
   git add cha-bio-safety/src/pages/LegalFindingDetailPage.tsx .planning/quick/260525-6if-redesign-21-legal-finding-detail-tsx-leg/260525-6if-PLAN.md
   git commit -m "feat(quick-260525-6if): redesign/21-legal-finding-detail TSX 변환 (LegalFindingDetailPage.tsx 279→{N} lines 단일 atomic + v0.1.x 토큰 className + 비즈 anchor 11 보존 + Lucide ChevronLeft+Download+Loader2+Camera + Spinner 폐기 + back 44x44 + admin 다운로드 44x44 + CTA 그라데이션 ≥2 + OQ LOCKED 5건 + W5 §1 매핑 verbatim)"
   ```
10. SUMMARY 별도 commit:
    ```bash
    git add .planning/quick/260525-6if-redesign-21-legal-finding-detail-tsx-leg/260525-6if-SUMMARY.md
    git commit -m "docs(quick-260525-6if): redesign/21-legal-finding-detail TSX 변환 SUMMARY"
    ```

## 6. 금지 사항

- wrangler 명령 0건 (CLAUDE.local.md 강제, 디자인 워크트리)
- npm run deploy 0건 (직원 도메인 cbc7119 경로)
- LegalFindingDetailPage.tsx 외 src 파일 수정 0건 (App.tsx + 9 src 파일 + tailwind.config.js + tokens.css + typography.css + types/ + functions/ + templates/ + migrations/ + public/ 모두 무영향)
- **★ App.tsx + 9 src 파일 + tailwind.config.js 변경 0 byte** — final verify gate
- status- prefix className 0건 (memory `feedback_tailwind_token_class_pattern`)
- w-8 / h-8 토큰 사용 0건 (=48px 함정, memory `feedback_tailwind_w8_h8_is_48px`) — back/admin button 은 `w-11 h-11` (=44px) 또는 인라인 `width:44, height:44`
- var(--bg|bg2|bg3|bg4|bd|bd2|t1|t2|t3|acl|safe|warn|danger) 잔존 0건
- linear-gradient 일반 0건. **예외 anchor: linear-gradient(135deg, #1d4ed8, #0ea5e9) (조치 완료 CTA, OQ #4 LOCKED) ≥2** (데스크톱 inline + 모바일 고정 양쪽 박제)
- fontSize 9·10·11 인라인 0건 (OQ #3 LOCKED 격상)
- **★ 이모지 0건** ('📷' line 237 완전 제거, OQ #5 LOCKED Camera 22 교체)
- 인라인 SVG path `d="M15 19l-7-7 7-7"` 0건 (OQ #5 LOCKED ChevronLeft 교체)
- 인라인 SVG path `d="M12 5v14m0 0l-6-6m6 6l6-6M5 19h14"` 0건 (OQ #5 LOCKED Download 교체)
- **★ function Spinner / const Spinner 정의 0건** (OQ #5 LOCKED Loader2 직접 사용)
- **★ @keyframes spin 0건** (line 45 + 147 양쪽 모두 폐기, OQ #5 LOCKED Loader2 animate-spin 사용)
- IconChevronLeft 0건
- polyline 0건
- 비즈 로직 변경 0건 (state/handler/useMutation/useQuery 1/legalApi 2종/snake_case payload/staff selector 패턴/useMultiPhotoUpload 직접/handleZipDownload iOS PWA/ZIP location 기반/handleResolve memo.trim/status open·resolved 분기)
- **★ 비즈 anchor 11건 1 byte 변경 0건** — lft/bbz/r22 precedent
- **★ ZIP 파일명 location 기반 유지** (`지적사항_${finding.location}.zip`, 19/20/21 차이 4 — 20-legal-findings round.title 기반 패턴 복사 금지)
- **★ useMultiPhotoUpload 직접 호출 유지** (19/20/21 차이 2 — FindingFormSheet 우회 패턴 복사 금지)
- App.tsx + 9 src 파일 + tailwind.config.js 0 byte 변경 (final verify gate)

  </action>
  <verify>
    <automated>
# Path 변수
L="cha-bio-safety/src/pages/LegalFindingDetailPage.tsx"
APP="cha-bio-safety/src/App.tsx"
PG="cha-bio-safety/src/components/PhotoGrid.tsx"
PSM="cha-bio-safety/src/components/PhotoSourceModal.tsx"
UMP="cha-bio-safety/src/hooks/useMultiPhotoUpload.ts"
UID="cha-bio-safety/src/hooks/useIsDesktop.ts"
FDL="cha-bio-safety/src/utils/findingDownload.ts"
API="cha-bio-safety/src/utils/api.ts"
AUTH="cha-bio-safety/src/stores/authStore.ts"
LP="cha-bio-safety/src/pages/LegalPage.tsx"
LFP="cha-bio-safety/src/pages/LegalFindingsPage.tsx"
TW="cha-bio-safety/tailwind.config.js"

# ─── Negative gate ───
echo "=== Negative gate (LegalFindingDetailPage) ==="

# emoji range 검사 (이모지 0 — '📷' 폐기 OQ #5)
test "$(grep -oP '[\x{1F300}-\x{1FAFF}\x{2600}-\x{26FF}]' "$L" 2>/dev/null | wc -l | tr -d ' ')" = "0" && echo "neg-L1 (emoji 0) PASS" || echo "neg-L1 FAIL"

# linear-gradient = 정확히 OQ #4 anchor 카운트 (총 카운트 = CTA 그라데이션 anchor 카운트 ≥2)
LG_TOTAL=$(grep -c "linear-gradient" "$L")
LG_ANCHOR=$(grep -c "linear-gradient(135deg, #1d4ed8, #0ea5e9)" "$L")
test "$LG_TOTAL" = "$LG_ANCHOR" && test "$LG_ANCHOR" -ge 2 && echo "neg-L2 (linear-gradient only OQ #4 anchor ≥2, count=$LG_ANCHOR) PASS" || echo "neg-L2 FAIL (total=$LG_TOTAL, anchor=$LG_ANCHOR)"

# fontSize 9·10·11 인라인 0
test "$(grep -v "^\s*//" "$L" | grep -cE "fontSize:\s*(9|10|11)[^0-9px]|font-size:\s*(9|10|11)[^0-9px]")" = "0" && echo "neg-L3 (no 9·10·11 fontSize) PASS" || echo "neg-L3 FAIL"

# status- prefix 0
test "$(grep -cE "\b(text|bg|border)-status-(safe|fire|warning|danger|caution|accent)" "$L")" = "0" && echo "neg-L4 (no status- prefix) PASS" || echo "neg-L4 FAIL"

# w-8 / h-8 0 (=48px 함정)
test "$(grep -cE "\bw-8\b|\bh-8\b" "$L")" = "0" && echo "neg-L5 (no w-8/h-8) PASS" || echo "neg-L5 FAIL"

# 옛 alias var(--bg|bg2|bg3|bg4|bd|bd2|t1|t2|t3|acl|safe|warn|danger) 0
test "$(grep -cE "var\(--(bg|bg2|bg3|bg4|bd|bd2|t1|t2|t3|acl|safe|warn|danger)\)" "$L")" = "0" && echo "neg-L6 (no legacy var alias) PASS" || echo "neg-L6 FAIL"

# 인라인 SVG path "M15 19l-7-7 7-7" 0
test "$(grep -c 'M15 19l-7-7 7-7' "$L")" = "0" && echo "neg-L7-chevron (no inline SVG ChevronLeft path) PASS" || echo "neg-L7-chevron FAIL"

# 인라인 SVG path "M12 5v14m0 0l-6-6m6 6l6-6M5 19h14" 0
test "$(grep -c 'M12 5v14m0 0l-6-6m6 6l6-6M5 19h14' "$L")" = "0" && echo "neg-L7-download (no inline SVG Download path) PASS" || echo "neg-L7-download FAIL"

# function Spinner / const Spinner 정의 0
test "$(grep -cE "(function|const)\s+Spinner" "$L")" = "0" && echo "neg-L8 (Spinner function/const defined 0) PASS" || echo "neg-L8 FAIL"

# @keyframes spin 0 (line 45 + 147 양쪽 모두 폐기)
test "$(grep -c "@keyframes spin" "$L")" = "0" && echo "neg-L9 (@keyframes spin 0) PASS" || echo "neg-L9 FAIL"

# IconChevronLeft / polyline 0
test "$(grep -cE "IconChevronLeft|polyline" "$L")" = "0" && echo "neg-L10 (IconChevronLeft/polyline 0) PASS" || echo "neg-L10 FAIL"

# 이모지 '📷' 0 (canAdd 폐기)
test "$(grep -c '📷' "$L")" = "0" && echo "neg-L11 (📷 emoji 0) PASS" || echo "neg-L11 FAIL"

# ─── Positive gate ───
echo "=== Positive gate (LegalFindingDetailPage) ==="

# OQ #1 — 모바일 헤더 raised + border-b
test "$(grep -c 'bg-surface-raised border-b border-border-default' "$L")" -ge 1 && echo "pos-L1 (OQ #1 mobile header) PASS" || echo "pos-L1 FAIL"

# OQ #2 — 화면 모드 mutually exclusive (칩 없음, 화면 자체 status)
test "$(grep -c "finding\.status === 'open'" "$L")" -ge 1 && echo "pos-L2-open (status open 분기) PASS" || echo "pos-L2-open FAIL"
test "$(grep -c "finding\.status === 'resolved'" "$L")" -ge 1 && echo "pos-L2-resolved (status resolved 분기) PASS" || echo "pos-L2-resolved FAIL"

# OQ #3 — text-caption / leading-none ≥5
test "$(grep -c 'text-caption' "$L")" -ge 5 && echo "pos-L3-caption (text-caption ≥5) PASS" || echo "pos-L3-caption FAIL"
test "$(grep -c 'leading-none' "$L")" -ge 5 && echo "pos-L3-leading (leading-none ≥5) PASS" || echo "pos-L3-leading FAIL"

# OQ #4 — CTA 그라데이션 ≥2 (데스크톱 inline + 모바일 고정 양쪽)
test "$(grep -c 'linear-gradient(135deg, #1d4ed8, #0ea5e9)' "$L")" -ge 2 && echo "pos-L4-gradient (OQ #4 CTA ≥2) PASS" || echo "pos-L4-gradient FAIL"

# OQ #5 — Lucide imports 4종 + ChevronLeft + Download + Loader2 + Camera
test "$(grep -cE "from ['\"]lucide-react['\"]" "$L")" -ge 1 && echo "pos-L5-import (lucide-react import) PASS" || echo "pos-L5-import FAIL"
test "$(grep -c 'ChevronLeft' "$L")" -ge 1 && echo "pos-L5-chevron PASS" || echo "pos-L5-chevron FAIL"
test "$(grep -c 'Download' "$L")" -ge 1 && echo "pos-L5-download PASS" || echo "pos-L5-download FAIL"
test "$(grep -c '<Loader2' "$L")" -ge 1 && echo "pos-L5-loader PASS" || echo "pos-L5-loader FAIL"
test "$(grep -c 'Camera' "$L")" -ge 1 && echo "pos-L5-camera PASS" || echo "pos-L5-camera FAIL"
test "$(grep -c '<ChevronLeft size={20}' "$L")" -ge 1 && echo "pos-L5-chevron-size20 PASS" || echo "pos-L5-chevron-size20 FAIL"
test "$(grep -c '<Download size={18}' "$L")" -ge 1 && echo "pos-L5-download-size18 PASS" || echo "pos-L5-download-size18 FAIL"
test "$(grep -c '<Camera size={22}' "$L")" -ge 1 && echo "pos-L5-camera-size22 PASS" || echo "pos-L5-camera-size22 FAIL"

# back/admin 44x44 격상 ≥2 — w-11 h-11 또는 width:44 height:44 (양쪽 모두)
W11_COUNT=$(grep -cE 'w-11 h-11|h-11 w-11' "$L")
WIDTH44_COUNT=$(grep -cE 'width:\s*44.*height:\s*44|height:\s*44.*width:\s*44' "$L")
TOTAL_44=$((W11_COUNT + WIDTH44_COUNT))
test "$TOTAL_44" -ge 2 && echo "pos-L5-back-admin-44x44 (back+admin 44x44 ≥2) PASS (w-11 h-11=$W11_COUNT, inline=$WIDTH44_COUNT)" || echo "pos-L5-back-admin-44x44 FAIL (total=$TOTAL_44)"

# 헤더 '지적 상세' 정적 ≥2 (모바일 line 158 + 데스크톱 line 170)
test "$(grep -c '지적 상세' "$L")" -ge 2 && echo "pos-L1-title (지적 상세 정적 ≥2) PASS" || echo "pos-L1-title FAIL"

# ─── ★ 비즈 anchor 11 보존 grep gate ───
echo "=== ★ 비즈 anchor 11 grep gate ==="
test "$(grep -cE "legalApi\.(getFinding|resolveFinding)" "$L")" -ge 2 && echo "biz-A1 (legalApi 2종) PASS" || echo "biz-A1 FAIL"
test "$(grep -cE "queryKey:\s*\[['\"]legal-finding['\"]" "$L")" -ge 1 && echo "biz-A2 (useQuery legal-finding) PASS" || echo "biz-A2 FAIL"
test "$(grep -cE "invalidateQueries.*\[['\"]legal-(finding|findings|rounds?)['\"]" "$L")" -ge 4 && echo "biz-A3 (invalidateQueries 4 키) PASS" || echo "biz-A3 FAIL"
test "$(grep -cE "resolution_memo|resolution_photo_keys" "$L")" -ge 2 && echo "biz-A4 (snake_case payload) PASS" || echo "biz-A4 FAIL"
test "$(grep -cE "useMultiPhotoUpload\(\)" "$L")" -ge 1 && echo "biz-A5 (useMultiPhotoUpload 직접 호출, 19/20/21 차이 2) PASS" || echo "biz-A5 FAIL"
test "$(grep -cE "memo\.trim\(\)" "$L")" -ge 1 && echo "biz-A6-trim (handleResolve memo.trim) PASS" || echo "biz-A6-trim FAIL"
test "$(grep -c '조치 내용을 입력하세요' "$L")" -ge 1 && echo "biz-A6-copy (조치 내용을 입력하세요) PASS" || echo "biz-A6-copy FAIL"
test "$(grep -cE "useAuthStore\(s => s\.staff\)|useAuthStore\(s=>s\.staff\)" "$L")" -ge 1 && echo "biz-A7-selector (staff selector 패턴) PASS" || echo "biz-A7-selector FAIL"
test "$(grep -cE "staff\?\.role === 'admin'" "$L")" -ge 2 && echo "biz-A7-admin (admin role 분기 ≥2 모바일+데스크톱) PASS" || echo "biz-A7-admin FAIL"
test "$(grep -cE "import\(['\"]fflate['\"]\)|zipSync" "$L")" -ge 2 && echo "biz-A8-fflate (fflate dynamic + zipSync) PASS" || echo "biz-A8-fflate FAIL"
test "$(grep -cE "createElement\(['\"]a['\"]\)|URL\.revokeObjectURL" "$L")" -ge 2 && echo "biz-A8-iospwa (iOS PWA <a download>) PASS" || echo "biz-A8-iospwa FAIL"
test "$(grep -cE "지적사항_.*\\.zip|finding\.location" "$L")" -ge 2 && echo "biz-A9-zip-location (ZIP location 기반, 19/20/21 차이 4) PASS" || echo "biz-A9-zip-location FAIL"
test "$(grep -cE "toast\.(success|error)" "$L")" -ge 5 && echo "biz-A10 (toast ≥5) PASS" || echo "biz-A10 FAIL"
test "$(grep -c '조치 완료' "$L")" -ge 2 && echo "biz-A10-cta (조치 완료 ≥2 toast+button) PASS" || echo "biz-A10-cta FAIL"
test "$(grep -c '다운로드 완료' "$L")" -ge 1 && echo "biz-A10-download (다운로드 완료) PASS" || echo "biz-A10-download FAIL"
test "$(grep -cE "function KVRow|function SectionHeader" "$L")" -ge 2 && echo "biz-A11-comp (KVRow + SectionHeader 컴포넌트) PASS" || echo "biz-A11-comp FAIL"
test "$(grep -cE "fmtDate" "$L")" -ge 3 && echo "biz-A11-fmtdate (fmtDate 호출 ≥3) PASS" || echo "biz-A11-fmtdate FAIL"

# ─── 카피 verbatim 검증 ───
echo "=== 카피 verbatim 검증 ==="
for COPY in '지적 정보' '지적 사진' '조치 내용' '조치 결과' '지적 내용' '위치' '등록일' '등록자' '조치일시' '조치자' '사진 없음' '사진 첨부' '업로드 중' '처리 중\.\.\.' '다운로드 중\.\.\.' '항목을 불러오지 못했습니다' '조치 사진' '조치 처리 실패' '다운로드 실패'; do
  test "$(grep -c "$COPY" "$L")" -ge 1 && echo "copy-OK: $COPY" || echo "copy-FAIL: $COPY"
done

# ─── App.tsx + 9 src 파일 + tailwind.config.js 0 byte 변경 (final verify gate) ───
echo "=== App.tsx + 9 src 파일 + tailwind.config.js 0 byte 변경 verify ==="
CHANGED=$(git diff --name-only HEAD~ HEAD -- "$APP" "$PG" "$PSM" "$UMP" "$UID" "$FDL" "$API" "$AUTH" "$LP" "$LFP" "$TW" 2>/dev/null)
test -z "$CHANGED" && echo "external-0byte PASS (no external file changed)" || echo "external-0byte FAIL (changed: $CHANGED)"

# ─── Build gate ───
echo "=== Build gate ==="
( cd cha-bio-safety && npx tsc --noEmit ) && echo "tsc PASS" || echo "tsc FAIL"
( cd cha-bio-safety && npm run build ) && echo "build PASS" || echo "build FAIL"

# ─── Final line count ───
wc -l cha-bio-safety/src/pages/LegalFindingDetailPage.tsx
    </automated>
  </verify>
  <done>
- LegalFindingDetailPage.tsx 279 → 250~310 lines 단일 파일 in-place 수정 완료
- v0.1.x 토큰 className 적용 (외곽 / 모바일 헤더 / 데스크톱 타이틀 / 로딩 Loader2 / 에러 / 콘텐츠 / Section 1+2 KVRow + PhotoGrid / Section 3 status open form + 슬롯 + Camera + 데스크톱 inline CTA 그라데이션 / Section 4 status resolved KVRow + PhotoGrid / 모바일 고정 하단 CTA 그라데이션)
- Lucide 4종 교체 완료 — ChevronLeft (back 44x44 격상) + Download (admin 44x44 격상) + Loader2 (Spinner 함수 폐기) + Camera (이모지 '📷' 폐기)
- Spinner 함수 line 41~48 폐기 + @keyframes spin 2건 (line 45 + 147) 모두 폐기 + 인라인 SVG path 2종 폐기 + 이모지 '📷' 폐기
- back button + admin 다운로드 button 양쪽 44x44 격상 완료
- ★ CTA 그라데이션 ≥2 박제 (데스크톱 inline line 244~248 + 모바일 고정 line 270~275)
- OQ #1~#5 LOCKED 모두 적용 + Negative gate 11건 + Positive gate 13건 모두 PASS
- ★ 비즈 anchor 11건 1 byte 변경 0 grep PASS (A1~A11 + sub-anchors)
- ★ ZIP 파일명 location 기반 유지 (19/20/21 차이 4)
- ★ useMultiPhotoUpload 직접 호출 유지 (19/20/21 차이 2)
- ★ staff selector 패턴 유지 (20-legal-findings getState() 와 다름)
- 카피 verbatim 19종 검증 PASS
- App.tsx + 9 src 파일 + tailwind.config.js 변경 0 byte 확인 (git diff --name-only 빈 출력)
- tsc --noEmit 0 errors + npm run build PASS + LegalFindingDetailPage chunk size 보고
- atomic 1-commit (LegalFindingDetailPage.tsx + PLAN.md) + SUMMARY 별도 commit 완료
- 다음 단계: redesign/21-legal-finding-detail 브랜치 push → main 머지 → GitHub Actions cbc7119-preview 자동 배포 → 사용자 컨펌
  </done>
</task>

</tasks>

<verification>
- LegalFindingDetailPage.tsx 단일 파일 in-place 수정 (외 src 파일 0건)
- W5 checklist (490 lines, SOURCE OF TRUTH) 12 섹션 + W1 index OQ LOCKED 5건 + 비즈 anchor 11 모두 적용
- ★ CTA 그라데이션 ≥2 박제 (데스크톱 inline + 모바일 고정 양쪽, lft 1건 + bbz 1건 패턴 mirror)
- ★ ZIP 파일명 location 기반 (19/20/21 차이 4)
- ★ useMultiPhotoUpload 직접 호출 (19/20/21 차이 2)
- ★ staff selector 패턴 (s => s.staff)
- App.tsx + 9 src 파일 (PhotoGrid + PhotoSourceModal + useMultiPhotoUpload + useIsDesktop + findingDownload + api + authStore + LegalPage + LegalFindingsPage) + tailwind.config.js 0 byte 변경
- tsc + build PASS
- atomic 1-commit + SUMMARY 별도 commit
</verification>

<success_criteria>
- Negative gate 11건 모두 PASS (emoji 0 / linear-gradient ≥2 anchor 카운트 일치 / fontSize 9·10·11 0 / status- prefix 0 / w-8 h-8 0 / 옛 alias 0 / 인라인 SVG ChevronLeft path 0 / 인라인 SVG Download path 0 / Spinner 함수 정의 0 / @keyframes spin 0 / IconChevronLeft+polyline 0 / 📷 0)
- Positive gate 13건 모두 PASS (OQ #1 모바일 헤더 + 타이틀 '지적 상세' ≥2 / OQ #2 status open + resolved 분기 / OQ #3 text-caption + leading-none ≥5 / OQ #4 CTA 그라데이션 ≥2 / OQ #5 lucide imports + ChevronLeft size 20 + Download size 18 + Loader2 + Camera size 22 + back/admin 44x44 ≥2)
- ★ 비즈 anchor 11 grep gate 모두 PASS (A1~A11 + sub-anchors)
- 카피 verbatim 19종 PASS
- App.tsx + 9 src 파일 + tailwind.config.js 변경 0 byte
- LegalFindingDetailPage chunk size 보고
- atomic 1-commit (LegalFindingDetailPage.tsx + PLAN.md) + SUMMARY 별도 commit
</success_criteria>

<output>
After completion, create `.planning/quick/260525-6if-redesign-21-legal-finding-detail-tsx-leg/260525-6if-SUMMARY.md` covering:
- 변환 전후 라인 수 (279 → N)
- 적용된 OQ LOCKED 5건 결정 + 위반 0건 확인
- 비즈 anchor 11건 보존 grep 결과 (legalApi 2 + useQuery 1 + useMutation 1 + invalidateQueries 4 키 + useMultiPhotoUpload 직접 + handleResolve memo.trim + handleDownload fflate + iOS PWA + ZIP location 기반 + status open/resolved 분기 + staff selector 패턴 + toast 5 + 카피 verbatim 19)
- Lucide 4종 교체 + Spinner 함수 폐기 + @keyframes spin 2건 폐기 + 인라인 SVG path 2종 폐기 + 이모지 '📷' 폐기 박제
- back button + admin 다운로드 button 양쪽 44x44 격상 박제
- CTA 그라데이션 ≥2 박제 (데스크톱 inline + 모바일 고정 양쪽, lft 1건 + bbz 1건 mirror)
- ZIP 파일명 location 기반 anchor (19/20/21 차이 4)
- useMultiPhotoUpload 직접 호출 anchor (19/20/21 차이 2)
- staff selector 패턴 anchor (20-legal-findings getState() 와 다름)
- App.tsx + 9 src 파일 + tailwind.config.js 0 byte 변경 verify 결과
- Build gate (tsc + npm run build) 결과 + LegalFindingDetailPage chunk size
- 다음 단계 (브랜치 push + main 머지 + cbc7119-preview 자동 배포 + 사용자 컨펌)
- 메모리 박제 후보 (단일 파일 atomic 8번째 도달 / LegalFinding 시리즈 19/20/21 3 페이지 redesign 완료 / location 기반 ZIP + selector 패턴 + useMultiPhotoUpload 직접 anchor 3종 박제 / CTA 그라데이션 ≥2 박제 패턴 첫 사례)
</output>