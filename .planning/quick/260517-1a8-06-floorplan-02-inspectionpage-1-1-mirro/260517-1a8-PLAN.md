---
phase: 260517-1a8
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cha-bio-safety/src/pages/FloorPlanPage.tsx
autonomous: true
requirements:
  - REDESIGN-06-CHROME-MIRROR
must_haves:
  truths:
    - "06 FloorPlanPage mobile 헤더의 padding/gap/타이틀 폰트가 02 InspectionPage 카테고리 모달 헤더와 1:1 동일"
    - "06 도면 타입 탭(4개)의 컨테이너/버튼/선택색이 02 Zone 탭과 1:1 동일 (단 flex-1 균등분배는 보존)"
    - "06 층 칩(13개)의 컨테이너/버튼/선택색이 02 층 칩과 1:1 동일"
    - "데스크톱 분기(isDesktop) 코드 경로는 손대지 않음"
    - "뒤로가기/마커 편집/축소보기 버튼 핸들러·로직·존재 자체는 보존, height 만 h-input 으로 통일"
    - "준비중 배지 dead code 보존"
    - "`text-on-accent` (Tailwind color key) → `text-text-on-accent` (text color utility) 로 정정"
    - "npm run build 통과"
  artifacts:
    - path: "cha-bio-safety/src/pages/FloorPlanPage.tsx"
      provides: "FloorPlanPage chrome (헤더 + 도면 타입 탭 + 층 칩) — 02 InspectionPage 모달 chrome 과 통일된 스펙"
      contains: "px-4 py-2.5 gap-2.5"
  key_links:
    - from: "FloorPlanPage.tsx line 947 (header)"
      to: "InspectionPage.tsx line 3822 (header)"
      via: "className verbatim mirror — mobile 분기만"
      pattern: "px-4 py-2.5.*gap-2.5"
    - from: "FloorPlanPage.tsx line 974-985 (도면 타입 탭)"
      to: "InspectionPage.tsx line 3840-3858 (Zone 탭)"
      via: "컨테이너 px-3.5 py-2 + 버튼 px-2 py-1 text-caption font-bold"
      pattern: "px-3\\.5 py-2"
    - from: "FloorPlanPage.tsx line 988-998 (층 칩)"
      to: "InspectionPage.tsx line 3863-3883 (층 칩)"
      via: "컨테이너 px-3.5 py-2 + 버튼 shrink-0 px-3 py-1 text-caption font-bold"
      pattern: "shrink-0 px-3 py-1 rounded-sm text-caption font-bold"
---

<objective>
06 FloorPlanPage 의 chrome (자체 헤더 + 도면 타입 탭 + 13층 칩) 의 className 을 02 InspectionPage 카테고리 모달 chrome (line 3822, 3840, 3863) 의 토큰과 1:1 mirror.

Purpose: 02 와 06 간 페이지 chrome 의 시각적 통일성 확보. paired-precedent 룰 — sketch CSS 추측이 아닌, 검증된 02 InspectionPage 의 className 을 reference 로 채택.

Output: cha-bio-safety/src/pages/FloorPlanPage.tsx lines 942-998 의 mobile 분기 className 갱신. 데스크톱 분기/이벤트 핸들러/레이아웃 구조/비즈니스 로직 보존. 단일 commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md

<interfaces>
Tailwind config (cha-bio-safety/tailwind.config.js) 에서 확인된 토큰 정의:

```js
fontSize: {
  caption:   ['12px', { lineHeight: '1.5', fontWeight: '400' }],  // text-caption
  label:     ['13px', { lineHeight: '1.5', fontWeight: '400' }],  // text-label (06 현재 — 교체 대상)
  'body-sm': ['14px', { lineHeight: '1.6', fontWeight: '400' }],  // text-body-sm (06 mobile 헤더 타이틀 — 교체 대상)
  body:      ['16px', { lineHeight: '1.7', fontWeight: '400' }],  // text-body (02 reference, 채용)
}

colors: {
  'text-on-accent': 'var(--text-on-accent)',  // → Tailwind utility = text-text-on-accent
}

height: {
  input:  'var(--input-height)',  // h-input (02 reference, 헤더 우측 버튼)
}

borderRadius: {
  sm: '8px',   // rounded-sm
}
```

**중요한 utility 명명 규칙:**
- 색 utility 는 `text-` prefix + color key. color key 가 `text-on-accent` 이므로 text-color utility 는 `text-text-on-accent` (이중 text-).
- 06 현재 코드의 `text-on-accent` (한 번만) 는 Tailwind 가 처리하지 않는 잘못된 표기 — 02 의 `text-text-on-accent` 로 정정 필요.
</interfaces>

<reference_blocks>
**02 InspectionPage (cha-bio-safety/src/pages/InspectionPage.tsx) — verbatim reference:**

```tsx
// 헤더 (line 3822) — 02 가 보여주는 정확한 className
<div className="shrink-0 bg-surface-raised border-b border-border-default px-4 py-2.5 flex items-center gap-2.5">
  ...
  <div className="text-body font-bold text-text-primary truncate">{...}</div>
  ...
  <button className="h-input px-3 rounded-sm bg-surface-sunken border border-border-default text-text-secondary text-caption font-semibold cursor-pointer hover:bg-surface-active transition-colors">
    ...
  </button>
</div>

// Zone 탭 (line 3840)
<div className="px-3.5 py-2 bg-surface-raised border-b border-border-default shrink-0">
  <div className="flex gap-1.5">
    <button className={`flex-1 flex items-center justify-center gap-1.5 px-2 py-1 rounded-sm text-caption font-bold cursor-pointer transition-colors ${
      isSel
        ? 'border-[1.5px] border-accent bg-accent text-text-on-accent'
        : 'border border-border-strong bg-surface-page text-text-secondary hover:bg-surface-active'
    }`}>
      ...
    </button>
  </div>
</div>

// 층 칩 (line 3863)
<div className="px-3.5 py-2 bg-surface-raised border-b border-border-default shrink-0">
  <div className="flex gap-1 overflow-x-auto pb-0.5 [scrollbar-width:none] [&::-webkit-scrollbar]:hidden">
    <button className={`shrink-0 px-3 py-1 rounded-sm text-caption font-bold cursor-pointer transition-colors ${
      isSel
        ? 'border-[1.5px] border-accent bg-accent text-text-on-accent'
        : 'border border-border-strong bg-surface-page text-text-secondary hover:bg-surface-active'
    }`}>
      ...
    </button>
  </div>
</div>
```

**06 FloorPlanPage 현재 (cha-bio-safety/src/pages/FloorPlanPage.tsx lines 942-998) — 변경 대상:**

verified at edit time via Read tool; lines 947 (header), 956 (title span), 960 (마커 편집 button), 967 (축소보기 button), 974/979 (도면 타입 탭 컨테이너/버튼), 988/993 (층 칩 컨테이너/버튼).
</reference_blocks>
</context>

<tasks>

<task type="auto">
  <name>Task 1: FloorPlanPage chrome className 을 02 InspectionPage 토큰과 1:1 mirror</name>
  <files>cha-bio-safety/src/pages/FloorPlanPage.tsx</files>

  <action>
**Edit 범위: lines 942-998 (mobile 분기 className 만). 데스크톱 분기 / 이벤트 핸들러 / 로직 / 레이아웃 구조 보존.**

**변경 1 — 헤더 (line 947):**
- 현재: `<header className={\`flex-shrink-0 flex items-center bg-surface-raised border-b border-border-default ${isDesktop ? 'h-[54px] px-5 gap-2.5' : 'px-3 py-2 gap-2'}\`}>`
- 변경: mobile 분기를 `'px-3 py-2 gap-2'` → `'px-4 py-2.5 gap-2.5'` 로 교체.
- 결과: `<header className={\`flex-shrink-0 flex items-center bg-surface-raised border-b border-border-default ${isDesktop ? 'h-[54px] px-5 gap-2.5' : 'px-4 py-2.5 gap-2.5'}\`}>`

**변경 2 — 헤더 타이틀 span (line 956):**
- 현재: `<span className={\`flex-1 font-bold text-text-primary truncate ${isDesktop ? 'text-body' : 'text-body-sm'}\`}>소방 시설 도면</span>`
- 변경: mobile 분기도 `text-body` 로 통일.
- 결과: `<span className="flex-1 font-bold text-text-primary truncate text-body">소방 시설 도면</span>` (분기 제거, isDesktop ternary 삭제)

**변경 3 — 마커 편집 버튼 (line 960):**
- 현재: `className={\`h-8 px-3 rounded-sm text-caption font-semibold leading-none cursor-pointer inline-flex items-center gap-1 transition-[background,border-color] duration-[130ms] ${editMode ? 'bg-accent border border-accent text-on-accent' : 'bg-surface-sunken border border-border-default text-text-secondary'}\`}`
- 변경: `h-8` → `h-input`, `leading-none` 제거 (02 reference 에 없음), `text-on-accent` → `text-text-on-accent` (정확한 Tailwind utility 명).
- 결과: `className={\`h-input px-3 rounded-sm text-caption font-semibold cursor-pointer inline-flex items-center gap-1 transition-[background,border-color] duration-[130ms] ${editMode ? 'bg-accent border border-accent text-text-on-accent' : 'bg-surface-sunken border border-border-default text-text-secondary'}\`}`

**변경 4 — 축소보기 버튼 (line 967):**
- 현재: `className="h-8 px-3 rounded-sm bg-surface-sunken border border-border-default text-text-secondary text-caption font-semibold leading-none cursor-pointer inline-flex items-center gap-1 transition-[background,border-color] duration-[130ms]"`
- 변경: `h-8` → `h-input`, `leading-none` 제거.
- 결과: `className="h-input px-3 rounded-sm bg-surface-sunken border border-border-default text-text-secondary text-caption font-semibold cursor-pointer inline-flex items-center gap-1 transition-[background,border-color] duration-[130ms]"`

**변경 5 — 도면 타입 탭 컨테이너 (line 974):**
- 현재: `<div className="flex-shrink-0 flex gap-1 px-3 py-2 bg-surface-raised border-b border-border-default">`
- 변경: `gap-1` → `gap-1.5`, `px-3` → `px-3.5` (02 Zone 탭 컨테이너 mirror).
- 결과: `<div className="flex-shrink-0 flex gap-1.5 px-3.5 py-2 bg-surface-raised border-b border-border-default">`

**변경 6 — 도면 타입 탭 버튼 (line 979):**
- 현재: `className={\`flex-1 h-9 rounded-sm text-label font-semibold transition-[background] duration-[130ms] inline-flex items-center justify-center relative ${p.ready ? 'cursor-pointer' : 'cursor-default opacity-40'} ${planType === p.key ? 'bg-accent border border-accent text-on-accent' : 'bg-surface-sunken border border-border-default text-text-tertiary'}\`}`
- 변경:
  - `h-9` 제거 (intrinsic height — 02 Zone 탭은 height 강제 없음).
  - `text-label font-semibold` → `text-caption font-bold` (02 mirror).
  - `inline-flex items-center justify-center` → `flex items-center justify-center` (02 reference 는 flex 직접 사용; 동작 동일).
  - 선택색: `bg-accent border border-accent text-on-accent` → `border-[1.5px] border-accent bg-accent text-text-on-accent` (02 mirror; `border-[1.5px]` 채택).
  - 비선택색: `bg-surface-sunken border border-border-default text-text-tertiary` → `border border-border-strong bg-surface-page text-text-secondary hover:bg-surface-active` (02 mirror).
  - `transition-[background] duration-[130ms]` → `transition-colors` (02 reference 패턴).
  - `flex-1` 보존 (4개 탭 균등분배 필요), `relative` 보존 (준비중 배지 absolute 기준), `p.ready` 분기 보존.
- 결과: `className={\`flex-1 flex items-center justify-center px-2 py-1 rounded-sm text-caption font-bold transition-colors relative ${p.ready ? 'cursor-pointer' : 'cursor-default opacity-40'} ${planType === p.key ? 'border-[1.5px] border-accent bg-accent text-text-on-accent' : 'border border-border-strong bg-surface-page text-text-secondary hover:bg-surface-active'}\`}`

**변경 7 — 준비중 배지 (line 982):**
- 보존. dead code 이지만 PLAN_TYPES 의 ready=false 가능성 대비 유지. className 변경 없음.

**변경 8 — 층 칩 컨테이너 (line 988):**
- 현재: `<div className="flex-shrink-0 flex gap-1 overflow-x-auto px-3 py-2 bg-surface-raised border-b border-border-default">`
- 변경: 02 reference 는 컨테이너 wrapper(`px-3.5 py-2 bg-surface-raised border-b border-border-default shrink-0`) + 안쪽 scroll wrapper(`flex gap-1 overflow-x-auto pb-0.5 [scrollbar-width:none] [&::-webkit-scrollbar]:hidden`) 의 2-level 구조.
  - 06 의 단일 div 를 02 의 2-level 구조로 분리.
  - 외곽 div: `<div className="flex-shrink-0 px-3.5 py-2 bg-surface-raised border-b border-border-default">`
  - 안쪽 div (scroll wrapper): `<div className="flex gap-1 overflow-x-auto pb-0.5 [scrollbar-width:none] [&::-webkit-scrollbar]:hidden">`
  - `flex-shrink-0` 는 02 의 `shrink-0` 와 동일 의미 (alias). 02 와 일관성을 위해 `shrink-0` 로 통일.
- 결과 (외곽): `<div className="shrink-0 px-3.5 py-2 bg-surface-raised border-b border-border-default">`
- 결과 (안쪽 새 wrapper): `<div className="flex gap-1 overflow-x-auto pb-0.5 [scrollbar-width:none] [&::-webkit-scrollbar]:hidden">` — FLOORS.map 을 이 안쪽 div 로 감쌀 것. 닫는 `</div>` 추가.

**변경 9 — 층 칩 버튼 (line 993):**
- 현재: `className={\`flex-shrink-0 h-8 px-3 rounded-sm text-caption font-semibold leading-none cursor-pointer inline-flex items-center justify-center transition-[background] duration-[130ms] ${floor === f ? 'bg-accent border border-accent text-on-accent' : 'bg-surface-sunken border border-border-default text-text-secondary'}\`}`
- 변경:
  - `flex-shrink-0` → `shrink-0` (02 alias 통일).
  - `h-8` 제거, `leading-none` 제거 (02 reference: `px-3 py-1` intrinsic).
  - `font-semibold` → `font-bold` (02 mirror).
  - `inline-flex items-center justify-center` 제거 (02 reference 는 button 기본 표시 사용 — text 단일).
  - 선택색: `bg-accent border border-accent text-on-accent` → `border-[1.5px] border-accent bg-accent text-text-on-accent`.
  - 비선택색: `bg-surface-sunken border border-border-default text-text-secondary` → `border border-border-strong bg-surface-page text-text-secondary hover:bg-surface-active`.
  - `transition-[background] duration-[130ms]` → `transition-colors`.
- 결과: `className={\`shrink-0 px-3 py-1 rounded-sm text-caption font-bold cursor-pointer transition-colors ${floor === f ? 'border-[1.5px] border-accent bg-accent text-text-on-accent' : 'border border-border-strong bg-surface-page text-text-secondary hover:bg-surface-active'}\`}`

**주의 사항:**
- `text-on-accent` (Tailwind utility 로는 무효) 가 6곳에 있음 (line 960, 979, 993; 추가로 다른 자취 가능). 이 plan 의 범위 (lines 942-998) 내 3 occurrence 만 `text-text-on-accent` 로 정정. 밖 영역은 별도 task 필요시 추후 처리.
- 라벨 (`구역 선택` / `층 선택` 텍스트) 절대 추가 금지 — 06 은 페이지 chrome 으로 라벨 없는 게 의도된 디자인.
- `relative` (line 979) 보존 — 준비중 배지 absolute positioning 기준점.
  </action>

  <verify>
    <automated>cd /Users/jykevin/Documents/cbc7119-design/cha-bio-safety && npm run build 2>&1 | tail -20</automated>
    <grep>
      # 헤더: mobile 분기에 02 의 px-4 py-2.5 gap-2.5 존재
      grep -n "px-4 py-2.5 gap-2.5" cha-bio-safety/src/pages/FloorPlanPage.tsx | grep -v "^#"
      # 헤더 타이틀: text-body (text-body-sm 분기 제거)
      grep -n "text-body-sm.*소방 시설 도면\|소방 시설 도면.*text-body-sm" cha-bio-safety/src/pages/FloorPlanPage.tsx
      # → 결과 0건이어야 함
      # 도면 타입 탭 컨테이너: px-3.5 py-2 + gap-1.5
      grep -n "flex gap-1.5 px-3.5 py-2 bg-surface-raised" cha-bio-safety/src/pages/FloorPlanPage.tsx
      # 층 칩 외곽: shrink-0 px-3.5 py-2 (flex 없음)
      grep -n "shrink-0 px-3.5 py-2 bg-surface-raised" cha-bio-safety/src/pages/FloorPlanPage.tsx
      # 층 칩 scroll wrapper
      grep -n "flex gap-1 overflow-x-auto pb-0.5" cha-bio-safety/src/pages/FloorPlanPage.tsx
      # 선택색 02 mirror
      grep -c "border-\[1.5px\] border-accent bg-accent text-text-on-accent" cha-bio-safety/src/pages/FloorPlanPage.tsx
      # → 3 이상 (도면 타입 탭 + 층 칩, 가능하면 마커 편집 버튼)
      # text-on-accent (잘못된 표기) 가 변경 범위에서 0건
      sed -n '942,1000p' cha-bio-safety/src/pages/FloorPlanPage.tsx | grep -v "^#" | grep -c "text-on-accent\"" || true
      # h-8 / h-9 가 변경 범위 안에 없어야 함 (chrome 만; 캔버스/마커 영역은 별도)
      sed -n '942,1000p' cha-bio-safety/src/pages/FloorPlanPage.tsx | grep -v "^#" | grep -c "\\bh-[89]\\b" || true
      # → 0
    </grep>
  </verify>

  <done>
    - npm run build 성공 (TypeScript / Vite 에러 없음).
    - 헤더 (line ~947): mobile 분기 padding 이 `px-4 py-2.5 gap-2.5`.
    - 헤더 타이틀 (line ~956): `text-body` 만 사용, `text-body-sm` 분기 제거.
    - 헤더 우측 버튼 (마커 편집 / 축소보기): `h-input`, `leading-none` 제거.
    - 도면 타입 탭 컨테이너: `flex gap-1.5 px-3.5 py-2 ...`, 4개 버튼 균등분배 `flex-1` 보존.
    - 도면 타입 탭 버튼: `flex-1 flex items-center justify-center px-2 py-1 rounded-sm text-caption font-bold transition-colors relative`, `h-9` / `text-label` 제거.
    - 층 칩 컨테이너: 외곽 `shrink-0 px-3.5 py-2 bg-surface-raised border-b border-border-default` + 안쪽 `flex gap-1 overflow-x-auto pb-0.5 [scrollbar-width:none] [&::-webkit-scrollbar]:hidden` 2-level.
    - 층 칩 버튼: `shrink-0 px-3 py-1 rounded-sm text-caption font-bold cursor-pointer transition-colors`, `h-8` / `leading-none` / `inline-flex` 제거.
    - 선택색 (모든 탭 + 칩): `border-[1.5px] border-accent bg-accent text-text-on-accent`.
    - 비선택색: `border border-border-strong bg-surface-page text-text-secondary hover:bg-surface-active`.
    - `text-on-accent` (잘못된 표기) 가 lines 942-998 범위 내에서 0회.
    - 데스크톱 분기 (`isDesktop ? 'h-[54px] px-5 gap-2.5' : ...`) 보존.
    - 뒤로가기 / 마커 편집 / 축소보기 버튼 onClick 핸들러 보존.
    - 준비중 배지 코드 보존 (`{!p.ready && <span ...>준비중</span>}`).
  </done>
</task>

</tasks>

<verification>
**Build gate:**
- `npm run build` 통과 (cha-bio-safety/ 안에서). Vite + TypeScript 컴파일 에러 0.

**Token mirror gate (grep):**
- 02 의 핵심 패턴이 06 에 모두 존재: `px-4 py-2.5 gap-2.5`, `px-3.5 py-2`, `text-caption font-bold`, `border-[1.5px] border-accent bg-accent text-text-on-accent`, `border-border-strong bg-surface-page text-text-secondary hover:bg-surface-active`.
- 06 의 deprecated 토큰 (`text-body-sm` 헤더 타이틀 분기, `h-8` chrome 버튼, `h-9` 도면 타입 탭, `text-label` chrome, `text-on-accent` chrome, `leading-none` chrome) 이 lines 942-998 범위에서 0건.

**Preserve gate (sed-range grep):**
- `isDesktop ? 'h-[54px] px-5 gap-2.5'` 존재 (데스크톱 분기 보존).
- `onClick={() => navigate(-1)}`, `onClick={() => { setEditMode(!editMode); setSelected(null) }}`, `onClick={() => { setScale(1); setTranslate({ x: 0, y: 0 }) }}` 3개 onClick 보존.
- `{!p.ready && <span` 보존.
- `relative` 보존 (도면 타입 탭 버튼).
</verification>

<success_criteria>
- 단일 commit (atomic).
- cha-bio-safety/src/pages/FloorPlanPage.tsx 만 변경.
- npm run build 통과.
- 위의 verification grep 8 가지 모두 expected 결과.
- mobile 시뮬레이션 시 06 페이지 chrome 의 헤더/탭/칩 세로폭과 02 카테고리 모달 chrome 의 그것이 픽셀 단위로 일치 (시각 검증은 SUMMARY 후 사용자 confirm 단계에서).
</success_criteria>

<output>
After completion, create `.planning/quick/260517-1a8-06-floorplan-02-inspectionpage-1-1-mirro/260517-1a8-SUMMARY.md` covering:
- Lines edited (942-998 범위 + 2-level 층 칩 구조 분리로 라인 수 증가 가능).
- Before/after table for each className change (9 changes).
- Verification grep 결과.
- 사용자 시각 검증 대기 (paired-precedent 룰: 02 와 06 의 mobile chrome 이 통일성 있는지 확인).
</output>
