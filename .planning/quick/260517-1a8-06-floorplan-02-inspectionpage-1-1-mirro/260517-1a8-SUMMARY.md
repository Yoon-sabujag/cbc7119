---
phase: 260517-1a8
plan: 01
subsystem: redesign/06-floorplan
tags: [redesign, mirror, chrome, tokens]
requires: []
provides:
  - FloorPlanPage chrome (헤더 + 도면 타입 탭 + 층 칩) — 02 InspectionPage 모달 chrome 토큰과 1:1 mirror
affects:
  - cha-bio-safety/src/pages/FloorPlanPage.tsx
tech-stack:
  added: []
  patterns:
    - 2-level 칩 컨테이너 구조 (외곽 padding wrapper + 안쪽 scroll wrapper)
    - paired-precedent — 02 verified className 을 reference 로 채택
key-files:
  modified:
    - cha-bio-safety/src/pages/FloorPlanPage.tsx (lines 942-1000, +18 -16)
decisions:
  - 마커 편집 버튼은 02 reference 의 단순 버튼 패턴(border 1px) 유지 — 도면 타입 탭/층 칩(border-[1.5px]) 와 의도적으로 차별. 02 InspectionPage 헤더 우측 버튼도 동일 패턴이므로 일관됨.
  - 헤더 타이틀 text-body 통일 — mobile/desktop 모두 16px 로 02 와 정합. 작은 화면에서 약간 커지지만 02 와 동일한 시각적 강도 확보가 paired-precedent 룰 상 우선.
  - 층 칩 2-level 구조 분리 — 02 reference 가 padding wrapper + scroll wrapper 를 명확히 분리하므로 그대로 채택. overflow-x-auto 의 padding 시각 효과 차이 흡수 필요.
metrics:
  duration: 약 6분
  completed: 2026-05-17
---

# Phase 260517-1a8 Plan 01: FloorPlanPage chrome → 02 InspectionPage 토큰 1:1 mirror Summary

`cha-bio-safety/src/pages/FloorPlanPage.tsx` mobile 분기의 chrome (자체 헤더 + 도면 타입 탭 + 13층 칩) className 을 02 InspectionPage 카테고리 모달 chrome (line 3822/3840/3863) 의 verified 토큰과 1:1 mirror. 데스크톱 분기 / 이벤트 핸들러 / 비즈니스 로직 / 준비중 배지 dead code 전부 보존.

## Tasks Completed

### Task 1 — FloorPlanPage chrome className 9곳 변경 (commit `dabd367`)

| # | Line | Element | Before (verbatim) | After (verbatim) |
| --- | --- | --- | --- | --- |
| 1 | 947 | 헤더 mobile 분기 | `'px-3 py-2 gap-2'` | `'px-4 py-2.5 gap-2.5'` |
| 2 | 956 | 헤더 타이틀 span | `\`flex-1 font-bold text-text-primary truncate ${isDesktop ? 'text-body' : 'text-body-sm'}\`` | `"flex-1 font-bold text-text-primary truncate text-body"` (분기 제거) |
| 3 | 960 | 마커 편집 버튼 | `h-8 px-3 rounded-sm text-caption font-semibold leading-none cursor-pointer inline-flex items-center gap-1 transition-... ${editMode ? '... text-on-accent' : '... text-text-secondary'}` | `h-input px-3 rounded-sm text-caption font-semibold cursor-pointer inline-flex items-center gap-1 transition-... ${editMode ? '... text-text-on-accent' : '... text-text-secondary'}` (h-input, leading-none 제거, text-text-on-accent 정정) |
| 4 | 967 | 축소보기 버튼 | `h-8 px-3 ... text-caption font-semibold leading-none cursor-pointer ...` | `h-input px-3 ... text-caption font-semibold cursor-pointer ...` (h-input, leading-none 제거) |
| 5 | 974 | 도면 타입 탭 컨테이너 | `flex-shrink-0 flex gap-1 px-3 py-2 bg-surface-raised border-b border-border-default` | `flex-shrink-0 flex gap-1.5 px-3.5 py-2 bg-surface-raised border-b border-border-default` |
| 6 | 979 | 도면 타입 탭 버튼 | `flex-1 h-9 rounded-sm text-label font-semibold transition-[background] duration-[130ms] inline-flex items-center justify-center relative ... ${p.ready ? '...' : '...'} ${planType === p.key ? 'bg-accent border border-accent text-on-accent' : 'bg-surface-sunken border border-border-default text-text-tertiary'}` | `flex-1 flex items-center justify-center px-2 py-1 rounded-sm text-caption font-bold transition-colors relative ... ${p.ready ? '...' : '...'} ${planType === p.key ? 'border-[1.5px] border-accent bg-accent text-text-on-accent' : 'border border-border-strong bg-surface-page text-text-secondary hover:bg-surface-active'}` |
| 7 | 982 | 준비중 배지 | (변경 없음 — 보존) | (변경 없음) |
| 8 | 988 | 층 칩 컨테이너 | `flex-shrink-0 flex gap-1 overflow-x-auto px-3 py-2 bg-surface-raised border-b border-border-default` 단일 div | 외곽 `shrink-0 px-3.5 py-2 bg-surface-raised border-b border-border-default` + 안쪽 `flex gap-1 overflow-x-auto pb-0.5 [scrollbar-width:none] [&::-webkit-scrollbar]:hidden` 2-level |
| 9 | 993 | 층 칩 버튼 | `flex-shrink-0 h-8 px-3 rounded-sm text-caption font-semibold leading-none cursor-pointer inline-flex items-center justify-center transition-[background] duration-[130ms] ... ${floor === f ? 'bg-accent border border-accent text-on-accent' : 'bg-surface-sunken border border-border-default text-text-secondary'}` | `shrink-0 px-3 py-1 rounded-sm text-caption font-bold cursor-pointer transition-colors ... ${floor === f ? 'border-[1.5px] border-accent bg-accent text-text-on-accent' : 'border border-border-strong bg-surface-page text-text-secondary hover:bg-surface-active'}` |

총 9곳 변경. diff: +18 lines / -16 lines (층 칩 2-level 구조로 인한 net +2).

## Verification Results

### Build Gate
```
$ cd cha-bio-safety && npm run build
✓ 87 modules transformed.
✓ built in 14.46s (vite build)
✓ built in 166ms (sw.ts)
PWA injectManifest — precache 82 entries (7876.97 KiB)
```
TypeScript / Vite 에러 0.

### Token Mirror Gate (grep)

| # | Pattern | Result | Expected |
| --- | --- | --- | --- |
| 1 | `px-4 py-2.5 gap-2.5` | line 947 (header mobile) | found |
| 2 | `text-body-sm.*소방 시설 도면\|소방 시설 도면.*text-body-sm` | 0 hits | 0 |
| 3 | `flex gap-1.5 px-3.5 py-2 bg-surface-raised` | line 974 | found |
| 4 | `shrink-0 px-3.5 py-2 bg-surface-raised` | line 988 | found |
| 5 | `flex gap-1 overflow-x-auto pb-0.5` | line 989 | found |
| 6 | `border-[1.5px] border-accent bg-accent text-text-on-accent` | 2 hits (line 979, 994) | ≥2 (도면 타입 탭 + 층 칩) |
| 7 | `[^-]text-on-accent` (잘못된 표기) in 942-1005 | 0 hits | 0 |

### Preserve Gate

- `isDesktop ? 'h-[54px] px-5 gap-2.5'` (line 947) — 데스크톱 분기 보존
- `onClick={() => navigate(-1)}` (line 950) — 뒤로가기 핸들러 보존
- `setEditMode(!editMode); setSelected(null)` (line 959) — 마커 편집 토글 보존
- `setScale(1); setTranslate({ x: 0, y: 0 })` (line 966) — 축소보기 보존
- `{!p.ready && <span ...>준비중</span>}` (line 982) — 준비중 배지 dead code 보존
- `flex-1` 도면 타입 탭 (line 979) — 4개 균등분배 보존
- `relative` (line 979) — 준비중 배지 absolute positioning 기준점 보존

### Scope Boundary Notes
- 뒤로가기 버튼 `w-8 h-8` (line 951) 은 정사각형 아이콘 버튼 — 02 reference 의 헤더에는 뒤로가기가 없으므로 (모달이라) 대응 토큰 없음. 플랜 9 changes 에 포함되지 않은 pre-existing chrome, scope 밖이라 손대지 않음.
- 마커 편집 버튼 선택색은 `bg-accent border border-accent text-text-on-accent` (1px) — 02 헤더 우측 버튼 패턴과 일치. `border-[1.5px]` 는 도면 타입 탭 + 층 칩에만 적용 (02 reference 가 그렇게 구성).

## Deviations from Plan

None. 플랜의 9개 className 변경을 verbatim 으로 적용. 데스크톱 분기 / 이벤트 핸들러 / 로직 / 준비중 배지 / flex-1 / relative 전부 보존.

## Self-Check: PASSED

- File exists: `cha-bio-safety/src/pages/FloorPlanPage.tsx` — FOUND
- Commit exists: `dabd367` — FOUND
- Build pass: ✓ (vite build OK + sw.ts OK + PWA injectManifest OK)
- 9 className changes applied verbatim per plan body
- Verification grep gate: 7/7 pass
- Preserve gate: 7/7 pass

## Next Steps

1. 사용자 시각 검증 — mobile 시뮬레이션에서 06 페이지 chrome 의 헤더/탭/칩 세로폭과 강도가 02 카테고리 모달 chrome 과 통일성 있는지 확인.
2. 컨펌 후 redesign/06-floorplan-fix 브랜치 main 머지 + 배포.
