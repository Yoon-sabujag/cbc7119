---
phase: quick-260526-9nd
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cha-bio-safety/docs/redesign-context/25-qr-print/wave-1-index.md
autonomous: true
requirements: [Q-25-W1]
must_haves:
  truths:
    - "wave-1-index.md 파일이 단일 진입점으로 존재한다 (cha-bio-safety/docs/redesign-context/25-qr-print/wave-1-index.md)"
    - "§1~§8 8-section 헤더가 모두 존재한다 (인벤토리 / sub-wave / design-system fence / components.css inherit / 메모리 룰 / negative / OQ / verify)"
    - "§1 인벤토리 표가 QRPrintPage.tsx 330 lines 4 영역(헤더 / 안내 박스 / 카테고리 목록 + 카드 / 다운로드 버튼 + PDF 생성) line 범위 박제"
    - "§2 sub-wave 분배표 3~4 sub-wave (W2~W4 또는 W2~W5) 권장"
    - "§3 design-system v0.1.1 fence verbatim 7건 (§1.1 / §1.2 / §1.3 / §6.4 / §6.5 / §6.8 / §7.1)"
    - "§5 메모리 룰 inline 인용 ≥10 unique slug"
    - "§6 negative rule ≥8건 (src/** 0 / wrangler 0 / npm run deploy 0 / sketch HTML 0 / 비즈 anchor 변경 0 등)"
    - "§7 OQ ≥5 (다운로드 버튼 그라데이션 / 안내 박스 info 알리아스 / 카테고리 카드 hover / 외곽 hex 토큰 치환 / 비즈 anchor 보존 확인)"
    - "§8 verify gate 8개 (§1~§8 헤더 grep / sub-wave row / unique slug / OQ / src/** 0 / sketch HTML 0 / wrangler 0 / npm run deploy 0)"
  artifacts:
    - path: "cha-bio-safety/docs/redesign-context/25-qr-print/wave-1-index.md"
      provides: "redesign/25-qr-print W2~W4 진입을 위한 단일 인덱스 (8-section + 비즈 anchor + OQ)"
      min_lines: 350
  key_links:
    - from: "wave-1-index.md §1"
      to: "QRPrintPage.tsx 330 lines"
      via: "line 범위 박제 + 비즈 anchor 보존"
      pattern: "line.*[0-9]+~[0-9]+"
    - from: "wave-1-index.md §3"
      to: "design-system.md v0.1.1"
      via: "fence verbatim 7건"
      pattern: "design-system.md.*§"
    - from: "wave-1-index.md §5"
      to: "MEMORY.md feedback_*.md slug"
      via: "inline 인용 ≥10 unique"
      pattern: "feedback_|project_|reference_"
---

<objective>
redesign/25-qr-print W1 — `QRPrintPage.tsx` (330 lines, 단일 파일) 의 element 인벤토리 + 비즈 anchor 박제 + design-system v0.1.1 fence verbatim + 메모리 룰 inline + negative rule + OQ + verify gate 를 통합한 **단일 진입점 마크다운** 1개 (`wave-1-index.md`) 를 생성한다. W2~W4 후속 sketch wave 작업자(자기 자신이든 다른 세션이든)는 이 파일 하나만 읽으면 모든 컨텍스트를 확보한다.

Purpose: 28-splash / 23-education / 10-cctv-info / 27-login 등 단일/2파일 페이지의 W1 인덱스 mirror 패턴을 25-qr-print 에도 적용. 다음 wave (W2~W4 sketch + W5 TSX) 의 컨텍스트 낭비 차단 + 비즈 anchor (jsPDF / qrcode / canvas 렌더링 / PDF 카드 사이즈 mm / 7 카테고리 hasPublic 분기) 1 byte 변경 사고 사전 예방.

Output: `cha-bio-safety/docs/redesign-context/25-qr-print/wave-1-index.md` 단일 파일 (350~450 lines 예상).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@cha-bio-safety/docs/redesign-context/25-qr-print/25-qr-print.md
@cha-bio-safety/docs/redesign-context/25-qr-print/design-system.md
@cha-bio-safety/src/pages/QRPrintPage.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: wave-1-index.md 단일 atomic 생성 (8-section + 4 영역 인벤토리 + 비즈 anchor + 메모리 룰 inline + OQ + verify gate)</name>
  <files>cha-bio-safety/docs/redesign-context/25-qr-print/wave-1-index.md</files>
  <action>
다음 8-section 구조로 단일 markdown 1개를 생성한다. 28-splash/23-education/10-cctv-info W1 mirror 패턴이되 **단일 파일 (QRPrintPage.tsx 330 lines)** 이므로 §1 인벤토리는 1 영역(파일) 4 sub-area (헤더 / 안내 박스 / 카테고리 목록+카드 / 다운로드 버튼+PDF 생성 비즈) 로 정리. sub-wave 는 3~4개 권장 (단순 페이지 — W2 외곽+헤더 / W3 안내 박스+카테고리 카드 / W4 다운로드 버튼+canvas 미리보기 / TSX 는 W5 또는 W4 통합).

---

**YAML frontmatter** (top):

```yaml
---
title: "redesign/25-qr-print — sketch wave 1 (index)"
status: ready_for_oq
created: 2026-05-26
quick_id: 260526-9nd
branch: redesign/25-qr-print
source_tsx: cha-bio-safety/src/pages/QRPrintPage.tsx
source_tsx_lines: 330
design_system: cha-bio-safety/docs/redesign-context/25-qr-print/design-system.md (v0.1.1, c8bfa86)
chrome_rules: cha-bio-safety/docs/redesign-context/00-design-context/inspection-modal-chrome-rules.md (25-qr-print = `MOBILE_NO_NAV_PATHS` 자체 헤더 사용 — chrome 룰 직접 적용 X. 자체 헤더 토큰화만)
mirror_of: cha-bio-safety/docs/redesign-context/28-splash/wave-1-index.md + 10-cctv-info/wave-1-index.md + 23-education/wave-1-index.md (8-section + sub-wave 3~4 단일 파일 패턴)
calibration_precedent: cha-bio-safety/docs/redesign-context/15-daily-report/wave-1-index.md SW3 (비즈 anchor 1 byte 변경 금지 일반화)
sub_wave_count: 3 (W2~W4) + TSX W5
memory_rules_inline: 12
open_questions: 5
---
```

---

**Title + 머리말 (5~8줄):**

```
# redesign/25-qr-print — sketch wave 1 (index)

본 문서는 W2~W5 후속 wave 의 **단일 진입점**이다. 이 인덱스 1개 파일만 읽으면 후속 wave 작업자(자기 자신이든 다른 세션이든)는 다음을 알 수 있다:

- QRPrintPage.tsx (330 lines) 의 element 인벤토리 → 4 sub-area + 3~4 sub-wave 분배 + **비즈 시그니처** 보존 anchor (jsPDF / QRCode.toCanvas / 7 카테고리 CATEGORIES + hasPublic / PDF 카드 사이즈 30×38mm + 70×90mm / scale 3 / margin 10mm / gap 1mm / cardW 0.65 ratio / `/api/checkpoints?category=...` / `${baseUrl}/e/${cp.id}` 공개 URL / `dlBtnStyle` / `busy` state / toast 카피)
- design-system.md v0.1.1 §1.1 / §1.2 / §1.3 / §6.4 / §6.5 / §6.8 / §7.1 의 verbatim 룰 박제
- 02+06 chrome 통일 룰 (`inspection-modal-chrome-rules.md`) 의 25-qr-print 적용 여부 (25-qr-print = 자체 헤더 사용 페이지, `MOBILE_NO_NAV_PATHS` 등재 — chrome 룰 직접 적용 X, 자체 헤더 토큰화만 진행)
- 메모리 룰 12건 inline 인용 — 25-qr-print 특화 룰 2건 (PDF 카드 mm 비즈 사이즈 변경 금지 + canvas 렌더링 파이프라인 보존) 포함
- §6 negative rule (이 wave 에서 금지된 것)
- §7 open questions 5건 — W2 진입 직전 사용자 컨펌
```

---

# §1. QRPrintPage.tsx 인벤토리

머리말 (4~6줄): 단일 파일 330 lines, 4 sub-area (1. 헤더 / 2. 안내 박스 / 3. 카테고리 목록 + 카드 / 4. 다운로드 버튼 + canvas+PDF 생성 비즈 로직) 로 정리. line 범위는 실측 (Read 검증).

**25-qr-print 의 구조 특이성** (머리말 박스):

- **단일 파일** — QRPrintPage.tsx 330 lines, 외부 컴포넌트 import 없음 (jsPDF / QRCode 라이브러리 직접 사용)
- **자체 헤더 페이지** — `/qr-print` ∈ `MOBILE_NO_NAV_PATHS` (App.tsx — 25-qr-print.md §2 라인 138 확인). BottomNav 모바일 숨김. 자체 헤더 (line 256~263) 사용. chrome 룰 직접 적용 X (점검 시리즈 아님).
- **canvas → jsPDF 파이프라인 비즈 로직** — `renderCardCanvas()` (line 29~119) + `generatePdf()` (line 122~219) 는 종이 인쇄용 비즈 로직, **변경 금지**. scale 3 / cardW mm / qrSize ratio 0.65 / margin 10mm / gap 1mm / FONT (Apple SD Gothic Neo) 모두 1 byte 변경 금지.
- **7 카테고리 CATEGORIES 배열 비즈 데이터** (line 17~25) — `소화기` / `소화전` / `DIV` / `청정소화약제` / `완강기` / `전실제연댐퍼` / `방화셔터` 7건, `hasPublic: true` 는 소화기만 (점검확인용 공개 QR URL `${baseUrl}/e/${cp.id}` 분기). 카테고리 추가/순서 변경 금지.
- **`useAuthStore().token` 직접 사용** — raw fetch (`/api/checkpoints?category=...`) Authorization Bearer 직접 인젝션 (line 233~235). 변경 금지.
- **`dlBtnStyle()` 인라인 헬퍼 함수** (line 312~330) — loading 3 상태 (loading / public / 기본) 그라데이션 분기. W5 TSX 변환 시 토큰화 (OQ #1) 또는 함수 형태 유지.
- **모바일/데스크톱 분기 없음** — 단일 컬럼 레이아웃 (PC 1920x1080 에도 모바일 레이아웃 동일 표시).

## §1.1 sub-area 인벤토리 표

| sub-area | element | line 범위 | 역할 | 비즈 로직 연결 | 후속 wave |
|---|---|---|---|---|---|
| 1. 외곽 + 헤더 | imports (useState, useNavigate, jsPDF, QRCode, useAuthStore, toast) | 1~6 | 정적 import | jsPDF / QRCode 라이브러리 / authStore / react-hot-toast | 보존만 |
| 1. 외곽 + 헤더 | CheckPoint interface | 8~15 | 타입 정의 | API 응답 shape (id/locationNo?/location/floor/category/description?) | 보존만 |
| 1. 외곽 + 헤더 | CATEGORIES 배열 7건 | 17~25 | 카테고리 비즈 데이터 | hasPublic: 소화기만 true | 보존만 (W3 카드 렌더 데이터 source) |
| 1. 외곽 + 헤더 | 외곽 wrapper (`height:100%` / flex column / overflow hidden / bg `var(--bg)`) | 253 | 페이지 wrapper | bg `--bg` → `--surface-page` 토큰 치환 OK | W2 |
| 1. 외곽 + 헤더 | 자체 헤더 (`padding:8px 12px` / bg `var(--bg2)` / borderBottom `1px solid var(--bd)` / flex / gap 10) | 256 | 헤더 wrapper | bg `--bg2` → `--surface-raised` 치환 OK | W2 |
| 1. 외곽 + 헤더 | 뒤로가기 버튼 (`34x34 / radius:8 / bg:var(--bg3) / border:var(--bd)` + ChevronLeft SVG 15x15 stroke `var(--t2)` strokeWidth 2) | 257~261 | navigate(-1) | onClick navigate(-1) / SVG path verbatim | W2 (Lucide ChevronLeft 치환 검토 — OQ #3) |
| 1. 외곽 + 헤더 | 헤더 타이틀 ("QR 코드 출력", fontSize 15 / fontWeight 700 / `var(--t1)`) | 262 | 페이지 제목 | 정적 카피 verbatim | W2 |
| 2. 안내 박스 | 안내 박스 wrapper (`padding:10px 14px 4px`) | 266 | 안내 박스 wrapper | flexShrink 0 | W3 |
| 2. 안내 박스 | 안내 박스 (`bg:rgba(14,165,233,.08) / border:rgba(14,165,233,.25) / radius:10 / padding:10px 13px / fontSize:12 / color:var(--t2) / lineHeight:1.6`) | 267 | info 색 안내 (status-info 알리아스 검토) | 정적 wrapper + 색 알파 | W3 (OQ #2 status-info-bg 치환) |
| 2. 안내 박스 | 안내 카피 verbatim ("항목별 QR 코드를 PDF 파일로 다운로드합니다.\n소화기는 <b>점검용</b>(3×3 cm)과 <b>점검확인용</b>(7×9 cm)을 별도 파일로 다운로드할 수 있습니다.") | 268~269 | 사용자 안내 | 카피 + `<b>` 강조 위치 verbatim | W3 |
| 3. 카테고리 목록 + 카드 | 카테고리 목록 wrapper (`flex:1 / overflowY:auto / padding:10px 14px 20px`) | 274 | scroll 영역 | flex 1 채움 | W3 |
| 3. 카테고리 목록 + 카드 | 카테고리 column wrapper (`flex column / gap:10`) | 275 | 카드 column | 정적 | W3 |
| 3. 카테고리 목록 + 카드 | CATEGORIES.map 카드 (`bg:var(--bg2) / border:var(--bd) / radius:13 / padding:13px 14px`) | 280 | 7건 카테고리 카드 | key cat.value / bg `--bg2` → `--surface-raised` | W3 |
| 3. 카테고리 목록 + 카드 | 카드 타이틀 (`fontSize:14 / fontWeight:700 / var(--t1) / marginBottom:10`) | 281 | 카테고리명 | cat.label verbatim | W3 |
| 3. 카테고리 목록 + 카드 | 버튼 row (`flex / gap:8 / flexWrap:wrap`) | 282 | 버튼 묶음 | 정적 wrapper | W3 |
| 4. 다운로드 버튼 + PDF 생성 비즈 | 점검용 버튼 (onClick handleDownload(cat.value,'inspect') / disabled busy / `dlBtnStyle(busy===inspKey, false)` / label "점검용 QR PDF" 또는 "생성 중...") | 284~290 | inspect 다운로드 트리거 | busy === inspKey / handleDownload / 카피 verbatim | W4 |
| 4. 다운로드 버튼 + PDF 생성 비즈 | 점검확인용 버튼 (cat.hasPublic 분기 → 소화기만 / `dlBtnStyle(busy===publicKey, true)` / label "점검확인용 QR PDF" 또는 "생성 중...") | 293~300 | public 다운로드 트리거 (소화기만) | cat.hasPublic / busy === publicKey / 카피 verbatim | W4 |
| 4. 다운로드 버튼 + PDF 생성 비즈 | dlBtnStyle 헬퍼 함수 (loading/public/기본 3 상태 그라데이션) | 312~330 | 인라인 스타일 헬퍼 | `linear-gradient(135deg,#16a34a,#22c55e)` public / `linear-gradient(135deg,#1d4ed8,#0ea5e9)` 기본 / `var(--bd2)` loading bg | W4 (OQ #1 토큰 치환 검토) |
| 4. 다운로드 버튼 + PDF 생성 비즈 | handleDownload async (setBusy + fetch `/api/checkpoints?category=...` + generatePdf + toast) | 229~250 | 다운로드 트리거 비즈 로직 | useAuthStore.token 직접 / fetch raw / json shape 체크 / catLabel 검색 / generatePdf 호출 / toast.success/error 카피 verbatim | W4/W5 보존 |
| 4. 다운로드 버튼 + PDF 생성 비즈 | renderCardCanvas async (canvas 2d / scale 3 / FONT `-apple-system,...` / 상하/QR/하단 레이아웃 계산) | 29~119 | 카드 캔버스 렌더링 비즈 로직 | scale 3 / PAD 3*S / qrSize 폭 0.85 max / QR margin 1 / textBaseline top / fillStyle 색 verbatim | W5 보존 (변경 금지) |
| 4. 다운로드 버튼 + PDF 생성 비즈 | generatePdf async (jsPDF a4 / margin 10mm / cardW inspect 30 / public 70 / cardH inspect 38 / public 90 / gap 1 / 격자 계산 / 카드 흰배경+테두리 / 이미지 추가 / save) | 122~219 | PDF 비즈 로직 | PAGE_W/H portrait/landscape / cols/rows 계산 / offsetX/Y 중앙정렬 / setFillColor/setDrawColor/setLineWidth verbatim / `${categoryLabel}_${typeLabel}_QR.pdf` 파일명 verbatim | W5 보존 (변경 금지) |

## §1.2 line 수 실측 확인

```
$ wc -l cha-bio-safety/src/pages/QRPrintPage.tsx
     330 cha-bio-safety/src/pages/QRPrintPage.tsx
```

PLAN 추정치 + 25-qr-print.md §2 메타 일치, drift 없음.

## §1.3 비즈 시그니처 보존 anchor (별도 박스)

W5 TSX 변환 wave 에서 다음 식별자/값은 **1 byte 변경 금지** (15-daily-report SW3 portraitPos 좌표 시스템 보존 룰 일반화, memory `project_redesign_15_daily_report_status`):

```
[QRPrintPage.tsx 비즈 로직]
- import { jsPDF } from 'jspdf'
- import QRCode from 'qrcode'
- import { useAuthStore } from '../stores/authStore'
- import toast from 'react-hot-toast'

- interface CheckPoint { id / locationNo? / location / floor / category / description? } (line 8~15, 변경 금지)

- CATEGORIES 배열 7건 (line 17~25, 순서/value/label/hasPublic 변경 금지):
    { value: '소화기',      label: '소화기',      hasPublic: true  }
    { value: '소화전',      label: '소화전',      hasPublic: false }
    { value: 'DIV',         label: 'DIV',         hasPublic: false }
    { value: '청정소화약제', label: '청정소화약제', hasPublic: false }
    { value: '완강기',      label: '완강기',      hasPublic: false }
    { value: '전실제연댐퍼', label: '전실제연댐퍼', hasPublic: false }
    { value: '방화셔터',    label: '방화셔터',    hasPublic: false }

- renderCardCanvas (line 29~119, 변경 금지):
    scale 기본 3
    PAD = 3 * S (상하 여백)
    FONT = `-apple-system, "Apple SD Gothic Neo", "Malgun Gothic", "Noto Sans KR", sans-serif`
    topFontSize 기본 12 / bottomFontSize 기본 13
    topH 계산 (topLines.length * (fs + 2 * S) + 2 * S)
    bottomH 계산 (첫 줄 +1, 나머지 -1 fontSize)
    availH = H - PAD * 2 - topH - bottomH - 4 * S
    maxQrW = Math.floor(W * 0.85)
    qrDrawSize = Math.min(Math.max(availH, 20 * S), maxQrW)
    QRCode.toCanvas(qrCanvas, qrValue, { width: qrDrawSize, margin: 1 })
    ctx.fillStyle = '#ffffff' (배경)
    ctx.fillStyle = '#222222' (상단 텍스트)
    ctx.fillStyle = '#333333' (하단 텍스트)
    ctx.textAlign = 'center' / ctx.textBaseline = 'top'
    첫 하단 라인은 'bold ' 추가
    return canvas.toDataURL('image/png')

- generatePdf (line 122~219, 변경 금지):
    if (points.length === 0) toast.error('체크포인트가 없습니다')
    isLandscape = type === 'public'
    jsPDF unit:'mm' / format:'a4' / orientation: portrait | landscape
    PAGE_W portrait 210 / landscape 297
    PAGE_H portrait 297 / landscape 210
    MARGIN = 10
    cardW inspect 30 / public 70
    cardH inspect 38 / public 90
    gap = 1
    cols = Math.floor((usableW + gap) / (cardW + gap))
    rows = Math.floor((usableH + gap) / (cardH + gap))
    offsetX/Y 중앙정렬
    setFillColor(255,255,255) / rect 'FD' (테두리+채움)
    setDrawColor(150,150,150) / setLineWidth(0.2) / rect 외곽
    inspect: qrValue = cp.id / qrSize = Math.floor(cardW * 0.65) / bottomLines [locationNo??id, location, floor] / bottomFontSize 3
    public:  qrValue = `${baseUrl}/e/${cp.id}` / qrSize 동일 / topLines 3 verbatim ('본 소화기는 QR코드로 관리되며,' / '아래 QR코드로' / '점검 내역 확인 가능합니다.') / topFontSize 3 / bottomLines 동일 / bottomFontSize 3
    addImage 'PNG' / setFillColor(0,0,0) + setLineWidth(0.3) + rect 'S' (테두리 재 그리기)
    doc.save(`${categoryLabel}_${typeLabel}_QR.pdf`) — typeLabel inspect '점검용' / public '점검확인용'

- handleDownload (line 229~250, 변경 금지):
    key = `${category}-${type}`
    setBusy(key)
    fetch(`/api/checkpoints?category=${encodeURIComponent(category)}`, headers: { Authorization: `Bearer ${token}` })
    if (!json.success || !json.data?.length) toast.error('체크포인트가 없습니다')
    catLabel = CATEGORIES.find(c => c.value === category)?.label ?? category
    await generatePdf(json.data, type, baseUrl, catLabel)
    toast.success('PDF 다운로드 완료')
    catch: toast.error('PDF 생성 오류') / console.error
    finally: setBusy(null)

- useAuthStore().token 직접 사용 (raw fetch 패턴, 변경 금지)
- baseUrl = window.location.origin (공개 URL prefix)

[카피 verbatim]
- "QR 코드 출력" (헤더 타이틀, line 262)
- "항목별 QR 코드를 PDF 파일로 다운로드합니다.\n소화기는 <b>점검용</b>(3×3 cm)과 <b>점검확인용</b>(7×9 cm)을 별도 파일로 다운로드할 수 있습니다." (안내, line 268~269)
- "점검용 QR PDF" / "생성 중..." (inspect 버튼, line 289)
- "점검확인용 QR PDF" / "생성 중..." (public 버튼, line 299)
- "체크포인트가 없습니다" (toast.error, line 128 + line 238)
- "PDF 다운로드 완료" (toast.success, line 243)
- "PDF 생성 오류" (toast.error, line 245)
- "본 소화기는 QR코드로 관리되며,\n아래 QR코드로\n점검 내역 확인 가능합니다." (renderCardCanvas topLines, line 194~196)
- 파일명 `${categoryLabel}_${typeLabel}_QR.pdf` (generatePdf save, line 218)
- '점검용' / '점검확인용' typeLabel (line 217)

[외곽 hex / 색 토큰화 검토 영역]
- 외곽 wrapper bg `var(--bg)` → `var(--surface-page)` 치환 OK (OQ #4)
- 헤더 bg `var(--bg2)` → `var(--surface-raised)` 치환 OK
- 카드 bg `var(--bg2)` / border `var(--bd)` → `--surface-raised` / `--border-default` 치환 OK
- 안내 박스 색 `rgba(14,165,233,.08)` / `rgba(14,165,233,.25)` → status-info 알리아스 검토 (OQ #2)
- 다운로드 버튼 그라데이션 (`linear-gradient(135deg,#1d4ed8,#0ea5e9)` 기본 / `linear-gradient(135deg,#16a34a,#22c55e)` public) → §6.4 의 "저장/CTA 버튼" 그라데이션 룰 (단 #1d4ed8→#0ea5e9 만 정식 사용 — public 의 #16a34a→#22c55e 그라데이션은 §6.4 위반 후보) — OQ #1
- 뒤로가기 SVG path → Lucide ChevronLeft 치환 검토 (OQ #3, 자체 헤더 통일 영역)
```

위 모든 식별자/값은 §6 negative rule + §5 룰 11/12 + §7 OQ #1/#2/#3/#4/#5 default 답에서 재확인. 1 byte 변경 시 W5 verify FAIL.

---

# §2. sub-wave 분배 + TSX checklist

## §2.1 sub-wave 표 (3개 권장)

| sub-wave | 슬러그 | 영역 | 산출 sketch | 라인 추정 | 비즈 anchor 포함? |
|---|---|---|---|---|---|
| W2 | `sketch-wave-2-frame-header.html` | 외곽 + 자체 헤더 (뒤로가기 / 타이틀) | sketch HTML | 80~120 | 카피 "QR 코드 출력" / 뒤로가기 navigate(-1) |
| W3 | `sketch-wave-3-info-categories.html` | 안내 박스 + 7 카테고리 목록 + 카드 골격 | sketch HTML | 150~220 | CATEGORIES 7건 / 안내 카피 verbatim |
| W4 | `sketch-wave-4-download-buttons.html` | 다운로드 버튼 (inspect + 소화기 public 분기) + loading 상태 + dlBtnStyle 토큰화 | sketch HTML | 100~160 | busy 3 state 매트릭스 / 카피 verbatim / 그라데이션 OQ #1 |

> 단순 페이지로 3 sub-wave 가 자연스럽다. 4-sub-wave 분할은 안내 박스 단독 분리할 정도의 디자인 결정 부담 없음 — W3 에 통합.

## §2.2 TSX 변환 (W5) checklist

| 항목 | 룰 |
|---|---|
| 비즈 anchor §1.3 전체 | 1 byte 변경 금지 (CATEGORIES 7건 / renderCardCanvas 전체 / generatePdf 전체 / handleDownload / toast 카피) |
| 인라인 `style={{...}}` 제거 | Tailwind utility 치환 (25-qr-print.md §4 요구사항) |
| `lg:*` prefix | 사용 금지 (모바일/데스크톱 동일 레이아웃, 토큰 자동 분기) |
| 자체 헤더 chrome | 02+06 chrome 룰 직접 적용 X. 단, 뒤로가기 = Lucide ChevronLeft 치환 검토 (OQ #3) |
| 외곽 hex 변경 | `var(--bg)/--bg2/--bg3/--bd` → `--surface-page/--raised/--sunken/--border-default` 치환 OK |
| 폰트 격상 | fontSize 12 → text-caption / fontSize 13 → text-label / fontSize 14 → text-body-sm / fontSize 15 → text-body / fontSize 11/10 사용 금지 (§1.1 노안 친화) — 단 PDF 내부 (renderCardCanvas/generatePdf) 의 px 값은 종이 인쇄용이므로 변경 금지 |
| 그라데이션 | 다운로드 버튼 inspect (#1d4ed8→#0ea5e9) 는 §6.4 룰 인정. public (#16a34a→#22c55e) 은 §6.4 위반 가능 — OQ #1 (solid `--status-safe-bar` 치환 또는 inspect 와 동일 그라데이션 사용 또는 그라데이션 유지) |
| 안내 박스 | `rgba(14,165,233,...)` → `bg-status-info-bg` + `border-status-info-bar` 치환 검토 (OQ #2) |
| Lucide 치환 | 뒤로가기 SVG path → `<ChevronLeft size={16} />` 또는 `size={20}` (OQ #3) |
| toast | react-hot-toast 그대로 사용 (메모리 `feedback_*` 변경 룰 없음) |
| useAuthStore | raw fetch + Authorization Bearer 보존 (변경 금지) |
| 비즈 카피 | §1.3 카피 verbatim 9건 + 안내 박스 카피 verbatim 1건 모두 보존 |

---

# §3. design-system.md v0.1.1 fence verbatim 7건

design-system.md (v0.1.1, c8bfa86) 의 다음 7 fence 를 verbatim 인용 (변형 금지, header 포함 그대로):

## §3.1 §1.1 노안 친화가 모든 결정보다 우선

```
### 1.1 노안 친화가 모든 결정보다 우선
- 본문 폰트 최소 16px. 9·10·11px 사용 금지.
- 보조 텍스트 명도 대비 AAA(7:1) 도달.
- 터치 타겟 모바일 44px, 데스크톱 40px.
- 1-2px 단위 미세 차이는 의미 없다 — 토큰은 4의 배수로만.
```

## §3.2 §1.2 정보 인지 > 미적 정제

```
### 1.2 정보 인지 > 미적 정제
방재 시스템은 매일 보는 업무 도구다. 트렌디함은 가치가 없다.
- 정보 위계는 폰트 크기/굵기/색이 분명하게 차별화한다.
- 카드 경계는 항상 명확하게 (다크는 명도, 라이트는 보더).
- 인지 부하를 늘리는 장식은 빼고, 빠른 식별을 돕는 색·아이콘을 살린다.
```

## §3.3 §1.3 모바일/데스크톱은 같은 시스템, 다른 밀도

```
### 1.3 모바일/데스크톱은 같은 시스템, 다른 밀도
- 폰트는 양쪽 동일 — 노안 대응 절대 룰.
- Radius도 양쪽 동일.
- Spacing만 분기 (모바일 14px → 데스크톱 10px 등).
- 데스크톱이 빽빽한 건 spacing보다 **레이아웃**(사이드바, 좌우 분할, 그리드 컬럼 수)이 책임진다.
```

## §3.4 §6.4 Backgrounds & Gradients

```
### 6.4 Backgrounds & Gradients

- 단색 surface 계층 — 이미지 배경 없음, 풀블리드 없음
- **유일한 그라디언트 2종:**
  - "오늘 점검 대상" 배너: `linear-gradient(135deg, rgba(37,99,235,.10), rgba(14,165,233,.05))`
  - 저장/CTA 버튼: `linear-gradient(135deg, #1d4ed8, #0ea5e9)`
- 그 외 모든 배경은 surface 토큰 단색
```

(메타) — `dlBtnStyle` public 분기 (#16a34a→#22c55e) 는 위 "유일한 그라디언트 2종" 에 없음 → OQ #1 결정 영역.

## §3.5 §6.5 Hover & Press States

```
### 6.5 Hover & Press States

- **hover**: `border-color` 강화 (`default` → `strong`) + `translateY(-1px)` 미세 상승, **또는** background 한 단계 진하게 (`raised` → `sunken`)
- **press/active**: 별도 스타일 없음 (웹 기반 PWA, 네이티브 제스처 의존)
- **링크**: 별도 hover 없음, `--text-link` 색만
```

## §3.6 §6.8 Layout Rules

```
### 6.8 Layout Rules

- **모바일**: 단일 컬럼, 그리드 기반 (2열 또는 4열 통계)
- **데스크톱**: 좌/우 분할 (flex, 우측 고정폭 340px), 또는 좌(50%)/우(50%)
- **페이지 패딩**: 모바일 16px, 데스크톱 24px (자동 분기, `--page-padding`)
- **네비게이션**: 모바일 BottomNav, 데스크톱 사이드바
- **자체 헤더 페이지 다수** (App.tsx 헤더 숨김 패턴) — 상세 페이지/도면/DIV/일정/일지/법정점검 등
```

(메타) — 25-qr-print 는 "자체 헤더 페이지" 군에 포함 (line 256~263 의 자체 헤더).

## §3.7 §7.1 Icon System: Lucide

```
### 7.1 Icon System: Lucide

- **`lucide-react`** 사용 (MIT, stroke 기반, 24×24 viewBox)
- 사이즈: **16 / 20 / 24 px** 세 종류만
- 색상: 본 문서의 status / accent 토큰만 사용
- 이모지 사용 금지 (대시보드 빠른 도구 카드 + 카테고리 카드 모두 Lucide로 통일)
```

(메타) — 25-qr-print 의 뒤로가기 SVG path 는 W5 에서 Lucide `ChevronLeft` 치환 검토 (OQ #3). 그 외 카테고리 카드는 아이콘 없이 카피만 표시 — 7.2 카테고리 매핑 적용 검토는 별도 OQ 영역 (default: 미적용, 단순 카드 유지).

---

# §4. 14-reports SW1 components.css inherit 매핑

25-qr-print 는 components.css 신규 추가/수정 0 (단순 페이지, 14-reports 의 sketch CSS 재사용 패턴이면 충분). 재사용 ≥3건 + 신규 추가 0 확인.

## §4.1 재사용 (기존 components.css 그대로 사용)

| 컴포넌트 | 출처 | 25-qr-print 사용처 |
|---|---|---|
| `.btn` / `.btn-primary` (기본 버튼) | 14-reports components.css | sketch 머리말 / 다운로드 버튼 (W4 sketch 에서만) |
| `.card` (raised 카드) | 14-reports components.css | 카테고리 카드 (W3) |
| `.page-header` / `.header-back` (자체 헤더) | 02-inspection chrome / 14-reports components.css | W2 자체 헤더 (모달 chrome 룰 직접 적용 X — 자체 헤더 토큰만 inherit) |
| `.text-caption` / `.text-label` / `.text-body-sm` | typography.css | 안내 박스 / 카드 타이틀 / 버튼 카피 |
| `.bg-surface-raised` / `.border-border-default` | tokens.css | 카드 / 헤더 |

## §4.2 신규 추가 (이 wave 에서 새로 정의)

| 컴포넌트 | 정의 위치 | 사유 |
|---|---|---|
| **없음** | — | 단순 페이지 — 신규 utility 0. 모두 기존 토큰/components.css 로 충분. |

> 14-reports SW1 의 신규 추가 ≥10 패턴은 14-reports 가 표/PDF 미리보기/그래프 등 다수 신규 컴포넌트 필요했기 때문. 25-qr-print 는 카드 7개 + 버튼 14개 + 안내 박스 1개 = 신규 정의 불필요.

---

# §5. 메모리 룰 inline 인용 (≥10 unique slug)

다음 12 메모리 룰 slug 를 inline 으로 박제 (변경 시 W5 verify FAIL):

1. **`feedback_design_sketch_first`** — spacing/sizing 도 sketch HTML 로 시안 보여주고 승인 받은 후 인라인 적용. 25-qr-print 의 W2/W3/W4 sketch 는 사용자 컨펌 필수.
2. **`feedback_design_changes_ask_first`** — 버그 수정이라도 레이아웃 구조/표시 방식 변경은 사용자와 상의 후. 25-qr-print 의 안내 박스 색 변경 / 그라데이션 변경 / Lucide 치환 모두 OQ 거쳐 컨펌.
3. **`feedback_redesign_sketch_rule_enforcement`** — §6.2/§6.3/§7.1 negative rule + 일관성 + executor 프롬프트 + verify gate + 자체 검수 4중 강화. 25-qr-print 도 동일 룰 강제.
4. **`feedback_sketch_realistic_data`** — 표시 분기/라벨 룰 (없음/N개/X-Y/완료) 은 코드 그대로. 시안은 시각 디자인만. 25-qr-print 의 7 카테고리 / hasPublic 분기 / busy 3 state 모두 코드 그대로.
5. **`feedback_tsx_wave_stat_card_drift`** — executor 가 source outline 패턴 보존, sketch 새 패턴 누락 가능. W5 plan 에 sketch CSS verbatim 인용 + verify gate 권장.
6. **`feedback_planner_prompt_sketch_verbatim`** — 변환 wave 시 sketch CSS 정의 grep 추출 verbatim 인용. 추측한 토큰명/사이즈는 deviation 유발 (03-qr-scan 6건 사례).
7. **`feedback_tailwind_token_class_pattern`** — `status-` prefix 없음 (`text-fire-bar` O / `text-status-fire-bar` X) + lucide `size={N}` prop. 25-qr-print W5 시 동일 패턴 강제.
8. **`feedback_tailwind_w8_h8_is_48px`** — tailwind.config spacing override: `w-8` = 48 (기본 32 아님), `w-7` = 32. 25-qr-print 의 뒤로가기 34x34 (line 257) 는 `w-[34px] h-[34px]` arbitrary 또는 가장 가까운 utility 사용.
9. **`feedback_text_caption_leading_none`** — 작은 컨테이너 안 `text-caption` lh:1.5 (18px) 가 `h-8` (32px) 안에서도 시각적 패딩. 헤더 타이틀 / 카드 타이틀 / 버튼 카피 모두 `leading-none` 명시 검토.
10. **`feedback_avoid_premature_confirmation`** — 변경 후 "approved 주세요" 자제. 시각 작업에서 "거의 일치" 같은 자신감 표현 금지. 25-qr-print W2/W3/W4 sketch 결과는 보여주고 사용자 판단.
11. **`project_redesign_15_daily_report_status`** — 캘리브 좌표 시스템 100% 보존 패턴 일반화 → 25-qr-print 의 비즈 anchor (renderCardCanvas + generatePdf 의 모든 px/mm/scale/색/카피) 1 byte 변경 금지.
12. **`feedback_cbc7119_design_never_wrangler`** — 디자인 wave 중 wrangler 절대 X. 25-qr-print W1/W2/W3/W4/W5 모두 wrangler 금지. main push 자동 cbc7119-preview 만.

---

# §6. negative rule (이 wave 에서 금지된 것)

이 wave 의 산출은 **단일 markdown 1개** 뿐이다. 다음은 금지된다:

1. **`src/**` 수정 0** — 이 wave 는 sketch/문서 wave. `cha-bio-safety/src/pages/QRPrintPage.tsx` 또는 다른 `src/**` 파일 수정 시 W1 verify FAIL.
2. **sketch HTML 생성 0** — sketch HTML 은 W2/W3/W4 에서 생성. 이 wave 는 인덱스 markdown 1개만.
3. **components.css 변경 0** — 14-reports 의 components.css 재사용만, 신규 추가 0.
4. **App.tsx / `MOBILE_NO_NAV_PATHS` 등 라우팅 변경 0** — 25-qr-print 의 `/qr-print` 는 이미 `MOBILE_NO_NAV_PATHS` 등재. 변경 불필요.
5. **wrangler 명령 0** — `wrangler pages deploy` / `wrangler d1` 모두 금지 (`.claude/settings.local.json` deny 강제). memory `feedback_cbc7119_design_never_wrangler`.
6. **`npm run deploy` 0** — 직원 도메인 경로. CLAUDE.local.md 룰. main push 만으로 cbc7119-preview 자동 배포.
7. **CATEGORIES 7건 / hasPublic 분기 변경 0** — 비즈 데이터, 이 wave 에서 결정/변경 불가.
8. **renderCardCanvas / generatePdf 비즈 로직 변경 0** — W5 에서도 변경 금지. 이 wave 는 박제만.
9. **평면(flat) 폴더 룰 위반 0** — sketch HTML 은 (다음 wave 에서) `cha-bio-safety/docs/redesign-context/25-qr-print/sketch-wave-N-{slug}.html` 평면 배치. `sketch/` 서브폴더 만들기 금지. 28-splash / 23-education / 10-cctv-info 동일 패턴.
10. **이 인덱스에 시각 디자인 결정 박제 0** — 결정은 W2/W3/W4 sketch + W5 TSX 에서. 이 인덱스는 박제 + OQ + 룰 인용만.

---

# §7. open questions (W2 진입 직전 사용자 컨펌)

다음 5건은 W2 진입 직전 사용자 컨펌 필수. default 답을 명시 (사용자가 변경 요청 시에만 변경):

## OQ #1 — 다운로드 버튼 그라데이션 처리

**문제:** `dlBtnStyle` 의 public 버튼 그라데이션 `linear-gradient(135deg,#16a34a,#22c55e)` 는 §6.4 "유일한 그라디언트 2종" 에 없는 색 조합. inspect 버튼 `linear-gradient(135deg,#1d4ed8,#0ea5e9)` 은 §6.4 의 "저장/CTA 버튼" 그라데이션과 일치.

**옵션:**
- (a) public 그라데이션을 §6.4 위반으로 간주 → solid `bg-status-safe-bar` (#22c55e) 치환
- (b) public 그라데이션을 `linear-gradient(135deg,#16a34a,#22c55e)` solid 유지 (예외 인정)
- (c) public + inspect 모두 동일 그라데이션 (`#1d4ed8→#0ea5e9`) 으로 통일 + public 은 좌측 색바 또는 아이콘으로 구별

**Default:** (a) — §6.4 룰 엄격 적용, solid 치환. 사용자 컨펌 후 W4 sketch 반영.

## OQ #2 — 안내 박스 색 토큰화

**문제:** 안내 박스 `bg:rgba(14,165,233,.08) / border:rgba(14,165,233,.25) / color:var(--t2)` 는 status-info (`#0ea5e9` 계열) 와 동일 hue. tokens.css 의 `--status-info-bg` / `--status-info-bar` 알리아스로 치환 가능.

**옵션:**
- (a) `bg-status-info-bg` + `border-status-info-bar` + `text-text-secondary` 치환
- (b) 인라인 `rgba(14,165,233,.08/.25)` 유지

**Default:** (a) — 알리아스 치환. 25-qr-print.md §4 요구사항 ("페이지 화면만 토큰화") 일치.

## OQ #3 — 뒤로가기 Lucide ChevronLeft 치환

**문제:** 뒤로가기 SVG path (line 258~260) `<path d="M15 19l-7-7 7-7"/>` 는 ChevronLeft 와 동일. §7.1 룰 ("이모지 사용 금지" + Lucide 통일) 적용 시 `<ChevronLeft size={16} />` 또는 `size={20}` 치환.

**옵션:**
- (a) `<ChevronLeft size={16} strokeWidth={2} className="text-text-secondary" />` 치환
- (b) `<ChevronLeft size={20} ... />` 치환 (헤더 34x34 컨테이너 안 시각 균형)
- (c) SVG path 유지

**Default:** (a) — Lucide 16px 치환 (`size={16}`, line 258 기존 `width={15}` 와 가장 근접). 02+06 chrome 룰의 자체 헤더 통일 영역.

## OQ #4 — 외곽 hex 토큰 치환 범위

**문제:** `var(--bg)/--bg2/--bg3/--bd/--bd2/--t1/--t2/--t3` raw 변수는 design-system.md §4.1 마이그레이션 룰에 따라 새 토큰으로 치환 가능.

**옵션:**
- (a) 모두 새 토큰 치환 (`--bg`→`--surface-page` 등) — design-system.md §4.1 표 그대로
- (b) 기존 raw 변수 유지 (워크트리 변환 부담 차단)

**Default:** (a) — design-system.md §4.1 마이그레이션 표 그대로 적용. 25-qr-print.md §4 요구사항 일치.

## OQ #5 — 비즈 anchor 보존 확인

**문제:** §1.3 박스의 모든 식별자/값 (CATEGORIES 7건 / renderCardCanvas / generatePdf / handleDownload / 카피 verbatim 9건 / 종이 인쇄 px 값 / mm 값 / scale 3 등) 은 W5 TSX 변환에서 1 byte 변경 금지가 default. 사용자가 특정 anchor 의 변경/조정을 원하는지 사전 확인.

**옵션:**
- (a) §1.3 전체 1 byte 변경 금지 (default)
- (b) 사용자가 특정 anchor 변경 요청 시 그 부분만 W5 plan 에서 별도 처리

**Default:** (a). 사용자가 OQ #1~#4 외 추가 변경 요청 시 (b) 로 처리.

---

# §8. verify gate

W1 산출 (`wave-1-index.md`) commit 직전 다음 8개 verify 실행. 모두 PASS 시 W1 종결, FAIL 시 재작성.

## 8.1 §1~§8 헤더 grep

```bash
grep -c "^# §[1-8]\." cha-bio-safety/docs/redesign-context/25-qr-print/wave-1-index.md
# 기대: 8 (정확)
```

## 8.2 sub-wave row ≥3

```bash
grep -E "^\| W[2-4] \|" cha-bio-safety/docs/redesign-context/25-qr-print/wave-1-index.md | grep -v '^#' | wc -l
# 기대: ≥3 (sub-wave 표의 W2/W3/W4 행 3개)
```

## 8.3 unique 메모리 룰 slug ≥10

```bash
grep -oE "(feedback|project|reference)_[a-z0-9_]+" cha-bio-safety/docs/redesign-context/25-qr-print/wave-1-index.md | sort -u | wc -l
# 기대: ≥10
```

## 8.4 OQ ≥5

```bash
grep -c "^## OQ #" cha-bio-safety/docs/redesign-context/25-qr-print/wave-1-index.md
# 기대: ≥5
```

## 8.5 src/** 변경 0

```bash
git status --porcelain cha-bio-safety/src/ 2>&1 | wc -l
# 기대: 0 (src/** 미수정)
```

## 8.6 sketch HTML 추가 0

```bash
find cha-bio-safety/docs/redesign-context/25-qr-print -name "sketch-wave-*.html" -newer cha-bio-safety/docs/redesign-context/25-qr-print/25-qr-print.md 2>/dev/null | wc -l
# 기대: 0 (sketch HTML 은 W2~W4 에서 생성, W1 에서는 0)
```

## 8.7 wrangler 명령 사용 0

```bash
# wrangler 명령 자체 실행 0 (.claude/settings.local.json deny 강제)
# verify: 본 wave 의 commit log 에 wrangler 흔적 없음
git log -1 --name-only | grep -c "wrangler" || true
# 기대: 0
```

## 8.8 `npm run deploy` 0

```bash
# CLAUDE.local.md 룰 — 디자인 작업은 npm run deploy 금지
# verify: 본 wave 진행 중 npm run deploy 미실행 (수동 점검 또는 shell history grep)
# 자동화 verify 어려움 — 본 룰은 atomic commit 직전 self-check 만으로 PASS 인정
echo "manual: npm run deploy 미실행 확인"
# 기대: 수동 PASS
```

---

# §9. 변경 이력

- **v1** (2026-05-26, quick `260526-9nd`) — redesign/25-qr-print W1 인덱스 최초 생성. 28-splash / 23-education / 10-cctv-info / 27-login W1 mirror (8-section + sub-wave 3 + 비즈 anchor + 메모리 룰 12 + OQ 5 + verify 8). 단일 파일 (QRPrintPage.tsx 330 lines) 4 sub-area 패턴.
  </action>
  <verify>
    <automated>
# §1~§8 헤더 grep == 8 (정확)
test "$(grep -c '^# §[1-8]\.' cha-bio-safety/docs/redesign-context/25-qr-print/wave-1-index.md)" -eq 8 \
  && echo "[8.1 PASS] §1~§8 헤더 8개" \
  || (echo "[8.1 FAIL]" && exit 1)

# sub-wave row ≥3
test "$(grep -E '^\| W[2-4] \|' cha-bio-safety/docs/redesign-context/25-qr-print/wave-1-index.md | grep -v '^#' | wc -l)" -ge 3 \
  && echo "[8.2 PASS] sub-wave row ≥3" \
  || (echo "[8.2 FAIL]" && exit 1)

# unique 메모리 룰 slug ≥10
test "$(grep -oE '(feedback|project|reference)_[a-z0-9_]+' cha-bio-safety/docs/redesign-context/25-qr-print/wave-1-index.md | sort -u | wc -l)" -ge 10 \
  && echo "[8.3 PASS] unique slug ≥10" \
  || (echo "[8.3 FAIL]" && exit 1)

# OQ ≥5
test "$(grep -c '^## OQ #' cha-bio-safety/docs/redesign-context/25-qr-print/wave-1-index.md)" -ge 5 \
  && echo "[8.4 PASS] OQ ≥5" \
  || (echo "[8.4 FAIL]" && exit 1)

# src/** 변경 0
test "$(git status --porcelain cha-bio-safety/src/ 2>&1 | wc -l)" -eq 0 \
  && echo "[8.5 PASS] src/** 변경 0" \
  || (echo "[8.5 FAIL]" && exit 1)

# sketch HTML 추가 0
test "$(find cha-bio-safety/docs/redesign-context/25-qr-print -name 'sketch-wave-*.html' 2>/dev/null | wc -l)" -eq 0 \
  && echo "[8.6 PASS] sketch HTML 0" \
  || (echo "[8.6 FAIL]" && exit 1)

# §1.3 비즈 anchor 박스 존재
grep -q "비즈 시그니처 보존 anchor" cha-bio-safety/docs/redesign-context/25-qr-print/wave-1-index.md \
  && echo "[BIZ ANCHOR PASS]" \
  || (echo "[BIZ ANCHOR FAIL]" && exit 1)

# design-system fence verbatim 7건 (§3.1~§3.7)
test "$(grep -c '^## §3\.' cha-bio-safety/docs/redesign-context/25-qr-print/wave-1-index.md)" -ge 7 \
  && echo "[FENCE 7 PASS]" \
  || (echo "[FENCE 7 FAIL]" && exit 1)

# negative rule ≥8건
test "$(grep -cE '^[0-9]+\.\s+\*\*' cha-bio-safety/docs/redesign-context/25-qr-print/wave-1-index.md)" -ge 8 \
  && echo "[NEGATIVE ≥8 PASS]" \
  || (echo "[NEGATIVE ≥8 FAIL]" && exit 1)
    </automated>
  </verify>
  <done>
- `cha-bio-safety/docs/redesign-context/25-qr-print/wave-1-index.md` 단일 파일 생성 (350~450 lines)
- §1~§8 8-section 헤더 모두 존재
- §1.3 비즈 anchor 박스 박제 (CATEGORIES 7건 / renderCardCanvas 전체 / generatePdf 전체 / handleDownload / 카피 verbatim 9건)
- §2 sub-wave 3개 (W2 외곽+헤더 / W3 안내+카테고리 / W4 다운로드 버튼)
- §3 design-system v0.1.1 fence verbatim 7건 (§1.1 / §1.2 / §1.3 / §6.4 / §6.5 / §6.8 / §7.1)
- §5 메모리 룰 inline ≥10 unique slug
- §6 negative rule ≥8건
- §7 OQ 5건 (default 답 명시)
- §8 verify gate 8개 (자동화 + 수동 1)
- `src/**` 수정 0
- sketch HTML 추가 0
- wrangler / npm run deploy 0
- atomic commit (한글 OK): `docs(25-qr-print): wave 1 index — QRPrintPage 330l 4-area 인벤토리 + 비즈 anchor + design-system fence 7 + 메모리 룰 12 + OQ 5 + verify 8`
  </done>
</task>

</tasks>

<verification>
- W1 산출 파일 1개 (`wave-1-index.md`) 만 존재. sketch HTML / TSX / components.css / src/** 변경 0.
- §1~§8 8-section 구조 완비, §1.3 비즈 anchor 박스 박제, §3 fence verbatim 7건, §5 메모리 룰 ≥10 slug, §6 negative ≥8, §7 OQ 5, §8 verify gate 8.
- atomic commit 1개 (한글 메시지 OK).
- W2 진입 가능 상태 (OQ 5건 사용자 컨펌 대기).
</verification>

<success_criteria>
- W2~W4 후속 wave 작업자가 이 단일 인덱스 파일 1개만 읽고 다음을 즉시 알 수 있다:
  - QRPrintPage.tsx 330 lines 의 4 sub-area + line 범위
  - 비즈 anchor (CATEGORIES 7건 / renderCardCanvas / generatePdf / handleDownload / 카피 9건) 1 byte 변경 금지 룰
  - design-system v0.1.1 7 fence verbatim
  - 메모리 룰 12건 (이 wave 에서 강제되는 모든 룰)
  - negative rule (이 wave 에서 금지된 것)
  - OQ 5건 (W2 진입 직전 사용자 컨펌 영역)
  - verify gate 8개 (W1 종결 기준)
- 8 verify 모두 PASS.
- atomic commit 1개 머지 (한글 메시지 OK).
</success_criteria>

<output>
After completion, atomic commit:

```
docs(25-qr-print): wave 1 index — QRPrintPage 330l 4-area 인벤토리 + 비즈 anchor + design-system fence 7 + 메모리 룰 12 + OQ 5 + verify 8

- cha-bio-safety/docs/redesign-context/25-qr-print/wave-1-index.md (단일 파일)
- 28-splash / 23-education / 10-cctv-info W1 mirror (8-section + sub-wave 3 단일 파일 패턴)
- W2~W4 sketch + W5 TSX 진입 가능
```

별도 SUMMARY 작성 불필요 (quick mode).
</output>
