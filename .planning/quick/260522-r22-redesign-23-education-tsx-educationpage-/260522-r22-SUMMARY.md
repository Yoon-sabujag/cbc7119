---
phase: quick-260522-r22
plan: 01
subsystem: redesign/23-education
tags: [redesign, 23-education, tsx-conversion, single-file-atomic, lucide-chevron-left, dday-status-token, v0.1.1]
status: complete
mirror_of: 28-splash 4i9 (precedent)
key_files:
  modified:
    - cha-bio-safety/src/pages/EducationPage.tsx (591→586 lines)
  unchanged:
    - cha-bio-safety/src/App.tsx (0 byte)
    - cha-bio-safety/tailwind.config.js (0 byte)
metrics:
  completed_date: 2026-05-22
  tasks_completed: 1
  files_modified: 1
  diff_stat: "+128/-133"
  commit_count: 1
---

# quick-260522-r22 SUMMARY

W5 checklist 12 섹션 verbatim, OQ 5건 LOCKED 모두 적용, 비즈 anchor 12건 1 byte 변경 0. **단일 파일 atomic** (17/27/16/23 TSX 패턴 동일 — 단일 파일 안 4 내부 컴포넌트 통합 변환).

## Commit

- `3aced26` — `feat(quick-260522-r22): redesign/23-education TSX 변환 (EducationPage.tsx 단일 atomic + v0.1.1 토큰 className 매핑 + 비즈 anchor 12건 보존 + Lucide ChevronLeft 교체 + OQ LOCKED 5건 반영 + W5 §3 매핑 verbatim)`
- 1 file changed: 128 insertions, 133 deletions
- Line count: 591 → 586 lines

## Verify ALL PASS

**OQ #1 PASS** — 모바일 자체 헤더 `bg-surface-raised border-b border-border-default` = 1
**OQ #2 PASS** — DdayBadge 3 임계치: `bg-safe-bg text-safe` = 1, `bg-warning-bg text-warning` = 1, `bg-danger-bg text-danger` = 1
**OQ #3 PASS** — submit 그라데이션 `linear-gradient(135deg, #1d4ed8, #0ea5e9)` = 1, `bg-surface-sunken text-text-tertiary cursor-not-allowed` = 1, `border-2 border-accent` = 1
**OQ #4 PASS** — 빈/오류 카피 verbatim: '교육 이력 없음' = 1, '이수일을 기록하면...' = 1, '교육 현황을 불러오지 못했습니다...' = 1
**OQ #5 PASS** — `import { ChevronLeft } from 'lucide-react'` = 1, `<ChevronLeft size={20}` = 1, `IconChevronLeft` = 0, `polyline` = 0

**Negative gate ALL PASS:**
- status- prefix = 0
- `\b(w|h)-8\b` = 0
- fontSize 9/10/11 인라인 = 0
- 이모지 = 0
- 옛 alias 토큰 (var(--bg|bg2|bg3|bd|bd2|t1|t2|t3|acl|safe|warn|danger)) = 0
- linear-gradient = 1 (OQ #3 예외 anchor만, 정확히 1건)
- IconChevronLeft = 0
- polyline = 0

**비즈 anchor 12건 1 byte 변경 0 PASS:**
1. calcNextDeadline + addMonths(6) + addYears(2) verbatim
2. differenceInCalendarDays(d, new Date()) verbatim
3. TITLE_ORDER {'주임':0,'대리':1,'기사':2} + titleRank fallback 99 verbatim
4. D-day 분기 임계치 30/0 + 라벨 'D-${dday}' / 'D+${Math.abs(dday)} 초과' verbatim
5. useQuery queryKey ['education'] + queryFn educationApi.list verbatim
6. useMutation × 3 + invalidateQueries ['education'] + toast 카피 5종 verbatim
7. educationApi.list/create/update/delete snake_case payload verbatim
8. useIsDesktop() verbatim
9. canEdit currentStaff?.role === 'admin' || currentStaff.id === cardStaffId verbatim
10. @keyframes blink + @keyframes slideUp (animation anchor) verbatim
11. toast 카피 5종 verbatim
12. 빈/오류/fallback 카피 verbatim

**App.tsx 0 byte gate PASS** — `git diff --name-only HEAD~1 HEAD -- cha-bio-safety/src/App.tsx` empty

**Build gate PASS** — tsc 0 errors + npm run build exit 0 (EducationPage chunk: 11.63 kB gzip 3.72 kB)

## OQ LOCKED 5건 적용

- #1 모바일 자체 헤더 → `bg-surface-raised border-b border-border-default` (옛 `background:'var(--bg2)', borderBottom:'1px solid var(--bd)'` 폐기)
- #2 DdayBadge → 3 임계치 Tailwind 토큰 (옛 rgba 인라인 + var(--safe)/var(--warn)/var(--danger) 폐기)
- #3 submit → `linear-gradient(135deg, #1d4ed8, #0ea5e9)` 인라인 (OQ #3 예외 anchor) + select disabled `bg-surface-sunken text-text-tertiary cursor-not-allowed` + selected card `border-2 border-accent`
- #4 빈/오류 카피 verbatim 유지, 아이콘 추가 X
- #5 Lucide ChevronLeft 교체 — IconChevronLeft 함수 정의 (line 12~18) + polyline SVG 완전 제거 + `import { ChevronLeft } from 'lucide-react'` 추가 + `<ChevronLeft size={20} />` 교체

## Scope

정확히 1 파일 (EducationPage.tsx), App.tsx + tailwind.config.js + tokens.css + typography.css + hooks/ + stores/ + utils/ + types/ + functions/ 무영향
