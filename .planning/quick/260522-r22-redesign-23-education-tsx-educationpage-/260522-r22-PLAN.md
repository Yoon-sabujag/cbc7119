---
phase: 260522-r22
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cha-bio-safety/src/pages/EducationPage.tsx
autonomous: true
requirements:
  - W5-checklist-§1-scope-single-file
  - W5-checklist-§2-biz-anchor-12
  - W5-checklist-§3-region-mapping-6
  - W5-checklist-§4-OQ-LOCKED-5
  - W5-checklist-§5-negative-gate
  - W5-checklist-§6-positive-gate
  - W5-checklist-§7-build-gate
  - W5-checklist-§8-self-verify-grep
  - W5-checklist-§9-tailwind-cheatsheet
  - W5-checklist-§10-biz-checkbox
  - W5-checklist-§11-memory-rules-12
  - W5-checklist-§12-next-step
tags:
  - redesign
  - 23-education
  - tsx-conversion
  - v0.1.1
  - quick
  - single-file-atomic
  - lucide-chevron-left
  - dday-status-token

must_haves:
  truths:
    - "EducationPage.tsx **단일 파일 atomic** in-place 수정. `git diff --name-only HEAD~ HEAD` 에 'cha-bio-safety/src/pages/EducationPage.tsx' + 본 PLAN.md/SUMMARY.md 만 등장 (다른 파일 0). 17-annual-plan (1hj) / 27-login (gox) / 16-workshift (u5n) 와 동일한 단일 파일 패턴. 28-splash (4i9) 는 2 파일이었음 — 본 23-education 은 단일 파일이지만 내부에 4 컴포넌트 (DdayBadge + StaffEducationCard + EducationEditPanel + EducationBottomSheet + 메인 EducationPage) 통합 변환."
    - "App.tsx + tailwind.config.js + tokens.css + typography.css + hooks/useIsDesktop.ts + stores/authStore.ts + utils/api.ts + types/ + functions/ + templates/ + migrations/ + public/ 모두 변경 0 byte. **App.tsx 변경 0 byte 가드 = final verify gate**."
    - "변환 후 EducationPage.tsx 라인 수 591 ± 30 (원본 591, 변환 후 580~620 예상). IconChevronLeft 함수 정의 7 lines 제거 + ChevronLeft import 1 line 추가 → 6 line 감소. className 추가 + style 인라인 축소 평형 → 총 라인 수 거의 동일."
    - "★ EducationPage 비즈 anchor 12건 1 byte 변경 0: (1) calcNextDeadline 함수 시그니처 + addMonths(parseISO(appointedAt), 6) + addYears(parseISO(sorted[0].completedAt), 2) (line 21~33) / (2) differenceInCalendarDays(d, new Date()) (line 29, 32) / (3) TITLE_ORDER {'주임':0,'대리':1,'기사':2} + titleRank fallback 99 (line 49~50) / (4) D-day 분기 매트릭스 임계치 30/0 + 라벨 'D-${dday}' / 'D+${Math.abs(dday)} 초과' (line 66~78) / (5) useQuery queryKey ['education'] + queryFn educationApi.list (line 427~430) / (6) useMutation × 3 + invalidateQueries ['education'] + toast 카피 5종 (line 212~244) / (7) educationApi.list/create/update/delete snake_case payload (education_type/completed_at) + positional id / (8) useIsDesktop() (line 422) / (9) canEdit currentStaff?.role === 'admin' || currentStaff.id === cardStaffId (line 432~435) / (10) @keyframes blink + @keyframes slideUp / (11) toast 카피 5종 verbatim / (12) 빈/오류/fallback 카피 verbatim — 모두 verbatim 보존."
    - "비즈 로직 0 diff: imports 9개 (useState/useNavigate/useQuery+useMutation+useQueryClient/date-fns 4종/toast/educationApi/useAuthStore/useIsDesktop/types) line 1~9 그대로 + **추가 1줄**: `import { ChevronLeft } from 'lucide-react'`."
    - "비즈 로직 0 diff: useState 2종 (selectedItem / EditPanel 내 completedAt+educationType+editingRecord) + useQuery + useMutation × 3 + useQueryClient + useNavigate + useAuthStore + useIsDesktop + canEdit + adminList + assistantList + renderCards + renderGroupedList + 데스크톱/모바일 분기 모두 verbatim."
    - "카피 verbatim 보존 (12건): '교육 이력 없음' / '이수일을 기록하면 다음 교육 마감일이 자동으로 계산됩니다.' / '교육 현황을 불러오지 못했습니다. 화면을 당겨서 새로고침하세요.' / '좌측에서 직원을 선택하세요' / '보수교육' (모바일 타이틀) / '소방안전관리자' / '소방안전관리 보조자' / '이수일이 기록되었습니다.' / '이수일이 수정되었습니다.' / '이수 기록이 삭제되었습니다.' / '이수 기록 저장에 실패했습니다.' / 'e?.message ?? 삭제에 실패했습니다.'"
    - "OQ #1 LOCKED 적용 (모바일 자체 헤더): `bg-surface-raised border-b border-border-default` (line 541~565). 옛 인라인 `background:'var(--bg2)', borderBottom:'1px solid var(--bd)'` 완전 폐기."
    - "OQ #2 LOCKED 적용 (DdayBadge 3 임계치): `bg-safe-bg text-safe` (>30) / `bg-warning-bg text-warning` (0~30) / `bg-danger-bg text-danger` (<0). 옛 rgba(34,197,94,0.12) + rgba(245,158,11,0.15) + rgba(239,68,68,0.15) + var(--safe) + var(--warn) + var(--danger) 인라인 모두 완전 폐기. **status- prefix 없음** (memory `feedback_tailwind_token_class_pattern`)."
    - "OQ #3 LOCKED 적용 (submit + disabled + selected): submit 인라인 그라데이션 `linear-gradient(135deg, #1d4ed8, #0ea5e9)` 유지 (W4 만 예외 anchor, ≥1) + 옛 `background:'var(--acl)'` 폐기. disabled select `bg-surface-sunken text-text-tertiary cursor-not-allowed` (OQ #3 토큰). selected card `border-2 border-accent` (데스크톱만, 1.5→2 격상 + var(--acl)→border-accent)."
    - "OQ #4 LOCKED 적용 (빈/오류 카피 verbatim + 아이콘 추가 X): '교육 이력 없음' + '이수일을 기록하면 다음 교육 마감일이 자동으로 계산됩니다.' + '교육 현황을 불러오지 못했습니다. 화면을 당겨서 새로고침하세요.' 모두 1 byte 변경 0. 아이콘 / lucide / SVG 추가 X (back button ChevronLeft 외 빈/오류 영역 아이콘 무)."
    - "OQ #5 LOCKED 적용 (Lucide ChevronLeft): `import { ChevronLeft } from 'lucide-react'` 추가 (line 11 또는 imports 끝) + `<ChevronLeft size={20} />` 모바일 back button 안 (line 559 대체) + IconChevronLeft 함수 정의 (line 12~18) **완전 제거** + polyline SVG (line 15) **완전 제거** + IconChevronLeft 사용처 (line 559) **교체**. grep gate `IconChevronLeft = 0` + `polyline = 0`."
    - "Negative gate 모두 PASS: 이모지 0 (TSX 본문) / linear-gradient — submit `linear-gradient(135deg, #1d4ed8, #0ea5e9)` ≥1 만 허용 (OQ #3 예외) / 그 외 0 / fontSize 9·10·11 인라인 0 / status- prefix 0 / w-8·h-8 0 / 옛 alias var(--bg|bg2|bg3|bd|bd2|t1|t2|t3|acl|safe|warn|danger) 0 (TSX 본문 안 인라인 style 안에서 단계적 제거 완료) / IconChevronLeft 0 / polyline 0."
    - "Positive gate 모두 PASS: bg-surface-raised border-b border-border-default ≥1 (OQ #1) / bg-safe-bg text-safe ≥1 + bg-warning-bg text-warning ≥1 + bg-danger-bg text-danger ≥1 (OQ #2 3 임계치) / linear-gradient(135deg, #1d4ed8, #0ea5e9) ≥1 + bg-surface-sunken text-text-tertiary cursor-not-allowed ≥1 + border-2 border-accent ≥1 (OQ #3 3 토큰) / 빈+오류 카피 3건 모두 ≥1 (OQ #4) / import ChevronLeft from lucide-react ≥1 + <ChevronLeft size={20} ≥1 + IconChevronLeft 0 + polyline 0 (OQ #5)."
    - "★ 비즈 anchor 12건 보존 grep gate PASS: calcNextDeadline / addMonths / addYears / differenceInCalendarDays / TITLE_ORDER / titleRank / D-day 임계치 30·0 + 라벨 D-${dday} + D+${Math.abs(dday)} 초과 / useQuery ['education'] / educationApi.create+update+delete / canEdit / @keyframes blink + slideUp / toast 카피 5종 / 빈+오류 카피 verbatim."
    - "App.tsx 변경 0 byte: `git diff --name-only HEAD~ HEAD -- cha-bio-safety/src/App.tsx` 빈 출력."
    - "Build gate: `cd cha-bio-safety && npx tsc --noEmit` 0 errors + `cd cha-bio-safety && npm run build` exit 0 (Vite build PASS). EducationPage chunk size 보고."
    - "Atomic 1-commit (단일 파일): `feat(quick-260522-r22): redesign/23-education TSX 변환 (EducationPage.tsx 단일 atomic + v0.1.1 토큰 className 매핑 + 비즈 anchor 12건 보존 + Lucide ChevronLeft 교체 + OQ LOCKED 5건 반영 + W5 §3 매핑 verbatim)`."
  artifacts:
    - path: cha-bio-safety/src/pages/EducationPage.tsx
      provides: v0.1.1 토큰 className 적용 (외곽 / 모바일 헤더 / 데스크톱 분할 / DdayBadge 3 임계치 / 직원 카드 / EditPanel 5 영역 / 바텀시트) + Lucide ChevronLeft 교체 (IconChevronLeft 함수 + polyline SVG 완전 제거) + OQ #1 모바일 헤더 bg-surface-raised border-b border-border-default + OQ #2 DdayBadge 3 토큰 + OQ #3 submit 그라데이션 인라인 + disabled 토큰 + selected border-2 border-accent + OQ #4 빈/오류 카피 verbatim + OQ #5 Lucide ChevronLeft + 비즈 anchor 12건 (calcNextDeadline / addMonths / addYears / differenceInCalendarDays / TITLE_ORDER / titleRank / D-day 임계치 / useQuery / useMutation×3 + educationApi / useIsDesktop / canEdit / @keyframes blink+slideUp / toast 5종 / 빈/오류 12건) 1 byte 변경 0
      contains: "bg-surface-page / bg-surface-raised / bg-surface-sunken / border-border-default / border-border-strong / border-2 / border-accent / text-text-primary / text-text-secondary / text-text-tertiary / text-text-on-accent / text-danger / bg-safe-bg / text-safe / bg-warning-bg / text-warning / bg-danger-bg / text-danger / text-heading / text-title / text-body / text-body-sm / text-label / text-caption / font-extrabold / font-bold / leading-none / leading-relaxed / rounded-full / rounded-md / rounded-sm / rounded-t-lg / ChevronLeft / size={20} / from 'lucide-react' / calcNextDeadline / addMonths / addYears / differenceInCalendarDays / TITLE_ORDER / titleRank / D-${dday} / D+ / 초과 / queryKey: ['education'] / educationApi.list / educationApi.create / educationApi.update / educationApi.delete / education_type / completed_at / useIsDesktop / canEdit / @keyframes blink / slideUp / 이수일이 기록되었습니다 / 이수일이 수정되었습니다 / 이수 기록이 삭제되었습니다 / 이수 기록 저장에 실패했습니다 / 삭제에 실패했습니다 / 교육 이력 없음 / 이수일을 기록하면 다음 교육 마감일이 자동으로 계산됩니다 / 교육 현황을 불러오지 못했습니다 / 좌측에서 직원을 선택하세요 / 보수교육 / 소방안전관리자 / 소방안전관리 보조자 / linear-gradient(135deg, #1d4ed8, #0ea5e9)"
      min_lines: 565
  key_links:
    - from: "W5 §3.1 상단 유틸 + 상수 + 스켈레톤 (line 1~58)"
      to: EducationPage.tsx line 1~58
      via: "imports 9개 + Lucide ChevronLeft 추가 1줄 (line 11 또는 imports 끝) + IconChevronLeft 함수 정의 line 12~18 완전 제거 + calcNextDeadline + fmtDate + dateToYmd + TITLE_ORDER + titleRank 그대로 (★ 비즈 anchor 1+2+3) + SKELETON_STYLE 인라인 React.CSSProperties 유지 (height 88 + animation 'blink 2s ease-in-out infinite') 또는 className 분리 옵션 `bg-surface-sunken rounded-md` + 인라인 height + animation"
      pattern: "import \\{ ChevronLeft \\} from 'lucide-react'|calcNextDeadline|TITLE_ORDER|titleRank|@keyframes blink"
    - from: "W5 §3.2 DdayBadge 3 임계치 (line 61~93, OQ #2 LOCKED ★)"
      to: EducationPage.tsx line 61~93
      via: "분기 매트릭스 — dday > 30 → className `bg-safe-bg text-safe` + label `D-${dday}` / dday >= 0 → className `bg-warning-bg text-warning` + label `D-${dday}` / dday < 0 → className `bg-danger-bg text-danger` + label `D+${Math.abs(dday)} 초과` (OQ #2 LOCKED ★, status- prefix 없음). 외곽 div className `text-caption font-bold leading-none rounded-sm` + 인라인 `padding: '2px 8px', flexShrink: 0` (memory feedback_text_caption_leading_none — leading-none 강제, 12px 작은 컨테이너)"
      pattern: "bg-safe-bg text-safe|bg-warning-bg text-warning|bg-danger-bg text-danger|D-\\$\\{dday\\}|D\\+\\$\\{Math\\.abs\\(dday\\)\\} 초과"
    - from: "W5 §3.3 StaffEducationCard (line 96~187, OQ #3 LOCKED selected ★)"
      to: EducationPage.tsx line 96~187
      via: "외곽 div className `bg-surface-raised rounded-md ${selected ? 'border-2 border-accent' : 'border border-border-default'}` (★ OQ #3 LOCKED 1.5→2 + var(--acl)→border-accent, 데스크톱만 selected) + 인라인 padding 16 + minHeight 80 + cursor + WebkitTapHighlightColor + userSelect 유지. 아바타 32x32 div className `bg-surface-sunken text-text-secondary text-body-sm font-bold rounded-full` + 인라인 width:32 + height:32 (w-8=48px 함정 회피, 인라인 명시) + flex center. 이름 div className `text-body font-bold text-text-primary` + 인라인 lineHeight 1.3. 직책 div className `text-label text-text-secondary` + 인라인 marginTop 2. 메타 div className `text-caption leading-relaxed text-text-tertiary` 또는 `text-text-secondary`"
      pattern: "bg-surface-raised rounded-md|border-2 border-accent|border border-border-default|bg-surface-sunken text-text-secondary text-body-sm font-bold rounded-full"
    - from: "W5 §3.4 EducationEditPanel (line 190~390, OQ #3 LOCKED submit + disabled ★)"
      to: EducationPage.tsx line 190~390
      via: "프로필 헤더 아바타 40x40 className `bg-surface-sunken text-text-secondary text-body font-bold rounded-full` + 인라인 width 40 + height 40 + 이름 className `text-title font-extrabold text-text-primary` (line 285) + 직책 className `text-label text-text-secondary` (line 286) + DdayBadge 영역 2 동일. 마감 정보 박스 className `bg-surface-sunken rounded-md` + 인라인 padding '12px 16px' (line 293) + 라벨 `text-caption leading-none text-text-tertiary` + 일자 `text-body-sm font-bold text-text-primary` + 보조 `text-caption leading-none text-text-tertiary`. 이수 이력 row className `bg-surface-sunken rounded-sm` + 인라인 padding '8px 12px' (line 307~310) + 액션 버튼 `bg-surface-raised border border-border-strong text-label leading-none rounded-sm` + 인라인 padding '4px 10px' + cursor. 폼 borderTop `border-t border-border-default` (line 347) + 인라인 paddingTop 16. 폼 라벨 `text-label font-bold text-text-secondary` (line 348) + `text-caption leading-none font-bold text-text-tertiary` (line 353, 357). input/select inputStyle className `bg-surface-sunken border border-border-strong text-label text-text-primary rounded-md` + 인라인 width 100% + padding '10px 12px' + outline none + boxSizing + fontFamily inherit + appearance + minWidth. select disabled 분기 className `${(!hasRecords && !isEditMode) ? 'bg-surface-sunken text-text-tertiary cursor-not-allowed' : 'bg-surface-sunken text-text-primary cursor-pointer'}` (★ OQ #3 LOCKED disabled 토큰). **★ submit button** (line 374~384) className `text-text-on-accent text-body font-bold rounded-md` + 인라인 background 'linear-gradient(135deg, #1d4ed8, #0ea5e9)' (★ OQ #3 LOCKED 인라인 그라데이션 유지, var(--acl) 완전 폐기) + width 100% + height 44 + border none + cursor + opacity + marginTop 4"
      pattern: "linear-gradient\\(135deg, #1d4ed8, #0ea5e9\\)|bg-surface-sunken text-text-tertiary cursor-not-allowed|text-text-on-accent text-body font-bold|border-t border-border-default"
    - from: "W5 §3.5 EducationBottomSheet (line 393~417)"
      to: EducationPage.tsx line 393~417
      via: "오버레이 div 인라인 100% 유지 (alpha 정밀도 rgba(0,0,0,0.6) — OQ #4 정신 일관 인라인 anchor). 시트 div className `bg-surface-raised rounded-t-lg` + 인라인 animation 'slideUp 0.28s ease-out both' (★ slideUp 인라인 anchor 보존) + maxHeight '90vh' + overflowY auto + padding '16px 16px 32px'. handle 32x4 className `bg-border-strong rounded-sm` + 인라인 width 32 + height 4"
      pattern: "bg-surface-raised rounded-t-lg|slideUp 0\\.28s ease-out both|bg-border-strong rounded-sm"
    - from: "W5 §3.6 EducationPage 메인 + 데스크톱/모바일 분기 (line 419~590, OQ #1 + OQ #4 + OQ #5 LOCKED ★)"
      to: EducationPage.tsx line 419~590
      via: "useNavigate / useIsDesktop / useAuthStore / useState selectedItem / useQuery education / canEdit / adminList / assistantList / renderCards 모두 verbatim. sectionLabelStyle className `text-label font-bold text-text-secondary` + 인라인 marginBottom 8 + marginTop 4 + isDesktop 분기 fontSize 15/13 인라인 유지 또는 className 분기. 로딩 SKELETON 4개 className `bg-surface-sunken rounded-md` + 인라인 height 88 + animation 'blink 2s ease-in-out infinite'. 오류 className `text-body-sm text-danger` + 인라인 padding 24 ('교육 현황을 불러오지 못했습니다. 화면을 당겨서 새로고침하세요.' verbatim, OQ #4). 빈 제목 className `text-body font-bold text-text-secondary` ('교육 이력 없음' verbatim) + 빈 보조 className `text-body-sm text-text-tertiary` ('이수일을 기록하면 다음 교육 마감일이 자동으로 계산됩니다.' verbatim, OQ #4) + 인라인 padding 24 + gap 8. 데스크톱 외곽 className `bg-surface-page` + 인라인 display flex + height 100%. 데스크톱 좌측 className `border-r border-border-default` + 인라인 flex 1 + display flex. 데스크톱 우측 fallback className `text-body-sm text-text-tertiary` + 인라인 display flex center + height 100% ('좌측에서 직원을 선택하세요' verbatim). 모바일 외곽 className `bg-surface-page` + 인라인 display flex column + height 100%. **★ 모바일 자체 헤더** (line 541~548) className `bg-surface-raised border-b border-border-default` (★ OQ #1 LOCKED) + 인라인 height 48 + flexShrink 0. **★ 모바일 back button** (line 549~560) className `text-text-secondary` + 인라인 width 44 + height 44 + background none + border none + cursor pointer + display flex center + `<ChevronLeft size={20} />` (★ OQ #5 LOCKED Lucide 교체, IconChevronLeft 폐기). 모바일 타이틀 className `text-body font-bold text-text-primary` + 인라인 flex 1 + textAlign center ('보수교육' verbatim). right spacer width 44 인라인 유지. 스크롤 영역 flex 1 + overflowY auto + padding 16 인라인 유지. @keyframes blink 인라인 <style> 블록 (line 583~588) 100% 유지 (★ animation anchor)"
      pattern: "bg-surface-raised border-b border-border-default|<ChevronLeft size=\\{20\\}|@keyframes blink"
---

<objective>
redesign/23-education TSX 변환 — EducationPage.tsx (591 lines) **단일 파일 atomic in-place 수정**.

W2 (chrome+empty) + W3 (staff-card) + W4 (edit-modal) 3 sketch + W5 12 섹션 checklist + W1 OQ 5건 LOCKED 결정을 1-commit 으로 적용. **★ 비즈 anchor 12건 1 byte 변경 0**. **★ Lucide ChevronLeft 교체** (IconChevronLeft 인라인 SVG 함수 정의 + polyline 완전 제거 + lucide-react import 1줄 추가, OQ #5 LOCKED).

17-annual-plan (1hj) / 27-login (gox) / 16-workshift (u5n) 와 동일한 단일 파일 atomic 패턴 mirror. 28-splash (4i9) 는 2 파일이었으나 23-education 은 단일 591 lines 안 4 컴포넌트 (DdayBadge + StaffEducationCard + EducationEditPanel + EducationBottomSheet + 메인 EducationPage) 통합 변환.

Purpose:
- v0.1.1 토큰 className 으로 옛 인라인 `var(--bg) / var(--bg2) / var(--bg3) / var(--bd) / var(--bd2) / var(--t1) / var(--t2) / var(--t3) / var(--acl) / var(--safe) / var(--warn) / var(--danger)` 치환
- **OQ #1 LOCKED**: 모바일 자체 헤더 → `bg-surface-raised border-b border-border-default` (line 541~565). 옛 `background:'var(--bg2)', borderBottom:'1px solid var(--bd)'` 완전 폐기
- **OQ #2 LOCKED**: DdayBadge 3 임계치 → `bg-safe-bg text-safe` (>30) / `bg-warning-bg text-warning` (0~30) / `bg-danger-bg text-danger` (<0). 옛 rgba(34,197,94,0.12) + rgba(245,158,11,0.15) + rgba(239,68,68,0.15) + var(--safe) + var(--warn) + var(--danger) 완전 폐기. status- prefix 없음. 임계치 30·0 + 라벨 verbatim
- **OQ #3 LOCKED**: submit 인라인 그라데이션 `linear-gradient(135deg, #1d4ed8, #0ea5e9)` 유지 (W4 만 예외 anchor) + 옛 `background:'var(--acl)'` 완전 폐기. select disabled `bg-surface-sunken text-text-tertiary cursor-not-allowed`. selected card `border-2 border-accent` (1.5→2 격상, 데스크톱만)
- **OQ #4 LOCKED**: 빈/오류 카피 verbatim 유지 + 아이콘 추가 X. '교육 이력 없음' + '이수일을 기록하면 다음 교육 마감일이 자동으로 계산됩니다.' + '교육 현황을 불러오지 못했습니다. 화면을 당겨서 새로고침하세요.' + '좌측에서 직원을 선택하세요' 모두 1 byte 변경 0
- **OQ #5 LOCKED**: IconChevronLeft 인라인 SVG 함수 정의 (line 12~18) + polyline SVG (line 15) + 사용처 (line 559) 모두 제거 → `import { ChevronLeft } from 'lucide-react'` 추가 (line 11 또는 imports 끝) + `<ChevronLeft size={20} />` 모바일 back button 안 (line 559 교체)
- ★ 비즈 anchor 12건 **1 byte 변경 0** (§3 박제)
- 비즈 로직 0 diff (state/handler/effect/hook/카피/queryKey/mutation/D-day 분기/toast/canEdit/timezone 모두 보존)

Output:
- cha-bio-safety/src/pages/EducationPage.tsx 단일 파일 in-place 수정 (591 → 580~620 lines 예상)
- App.tsx + tailwind.config.js + tokens.css + typography.css + hooks/ + stores/ + utils/api.ts + types/ + functions/ + templates/ + migrations/ + public/ 모두 0 byte 변경
- `npm run build` PASS
- atomic 1-commit (단일 파일 + PLAN.md/SUMMARY.md)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jykevin/Documents/cbc7119-design/CLAUDE.md
@/Users/jykevin/Documents/cbc7119-design/CLAUDE.local.md
@/Users/jykevin/Documents/cbc7119-design/.planning/STATE.md

# W5 TSX 변환 checklist (SOURCE OF TRUTH — §3 매핑 표 verbatim)
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/23-education/wave-5-tsx-conversion-checklist.md

# W2~W4 sketch (변환 매핑 시각 검증용)
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/23-education/sketch-wave-2-chrome-empty.html
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/23-education/sketch-wave-3-staff-card.html
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/23-education/sketch-wave-4-edit-modal.html

# 토큰 / Tailwind config (utility class 검증용)
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/tailwind.config.js

# Precedent — 28-splash 4i9 + 17-annual-plan 1hj + 27-login gox PLAN.md mirror
@/Users/jykevin/Documents/cbc7119-design/.planning/quick/260522-4i9-redesign-28-splash-tsx-splashscreen-inst/260522-4i9-PLAN.md
@/Users/jykevin/Documents/cbc7119-design/.planning/quick/260522-4i9-redesign-28-splash-tsx-splashscreen-inst/260522-4i9-SUMMARY.md
@/Users/jykevin/Documents/cbc7119-design/.planning/quick/260522-1hj-redesign-17-annual-plan-tsx-annualplanpa/260522-1hj-PLAN.md

# 변환 대상 (in-place 수정 단일 파일)
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/src/pages/EducationPage.tsx

# 변환 무영향 — 0 byte 변경 (final verify gate)
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/src/App.tsx

<interfaces>
<!-- EducationPage.tsx 안의 비즈 로직 시그니처 — 모두 보존 (변환 시 1 byte 도 손대지 않음) -->

```typescript
// ============================================================
// EducationPage.tsx — imports + helpers + 상수 (line 1~58)
// ============================================================

// imports 9개 (line 1~9) — 1 byte 변경 0 (단, 추가 1줄 lucide-react)
import { useState } from 'react'
import { useNavigate } from 'react-router-dom'
import { useQuery, useMutation, useQueryClient } from '@tanstack/react-query'
import { addMonths, addYears, differenceInCalendarDays, parseISO } from 'date-fns'
import toast from 'react-hot-toast'
import { educationApi } from '../utils/api'
import { useAuthStore } from '../stores/authStore'
import { useIsDesktop } from '../hooks/useIsDesktop'
import type { EducationRecord, StaffEducation } from '../types'

// ★ 추가 1줄 (OQ #5 LOCKED) — Lucide ChevronLeft
import { ChevronLeft } from 'lucide-react'

// ★ IconChevronLeft 인라인 SVG 함수 정의 (line 12~18) — 완전 제거 (OQ #5 LOCKED)
// function IconChevronLeft({ size = 20, color = 'currentColor' }: { size?: number; color?: string }) {
//   return (
//     <svg width={size} height={size} viewBox="0 0 24 24" fill="none" stroke={color} strokeWidth={2} strokeLinecap="round" strokeLinejoin="round">
//       <polyline points="15 18 9 12 15 6" />   ← polyline SVG 완전 제거
//     </svg>
//   )
// }

// ★ calcNextDeadline (line 21~33) — 1 byte 변경 0 (★ 비즈 anchor 1)
function calcNextDeadline(
  appointedAt: string | null,
  records: EducationRecord[]
): { deadline: Date | null; dday: number | null; label: string } {
  if (!appointedAt) return { deadline: null, dday: null, label: '선임일 미등록' }
  const sorted = [...records].sort((a, b) => b.completedAt.localeCompare(a.completedAt))
  if (sorted.length === 0) {
    const d = addMonths(parseISO(appointedAt), 6)                                        // ★ addMonths(6) anchor
    return { deadline: d, dday: differenceInCalendarDays(d, new Date()), label: '첫 실무교육' }   // ★ differenceInCalendarDays(d, new Date()) anchor
  }
  const d = addYears(parseISO(sorted[0].completedAt), 2)                                  // ★ addYears(2) anchor
  return { deadline: d, dday: differenceInCalendarDays(d, new Date()), label: '보수교육' }
}

// fmtDate + dateToYmd (line 35~46) — 1 byte 변경 0
function fmtDate(iso: string) {
  const [y, m, d] = iso.split('-')
  return `${y}-${m}-${d}`
}
function dateToYmd(d: Date) {
  const y = d.getFullYear()
  const m = String(d.getMonth() + 1).padStart(2, '0')
  const day = String(d.getDate()).padStart(2, '0')
  return `${y}-${m}-${day}`
}

// ★ TITLE_ORDER + titleRank (line 49~50) — 1 byte 변경 0 (★ 비즈 anchor 3)
const TITLE_ORDER: Record<string, number> = { '주임': 0, '대리': 1, '기사': 2 }
function titleRank(title: string) { return TITLE_ORDER[title] ?? 99 }

// SKELETON_STYLE (line 53~58) — 인라인 React.CSSProperties 유지 또는 className 분리 옵션
// 옵션 A (인라인 유지): const SKELETON_STYLE: React.CSSProperties = { background: 'var(--bg3)', borderRadius: 12, height: 88, animation: 'blink 2s ease-in-out infinite' }
// 옵션 B (className 분리, 권장): SKELETON 컴포넌트 분리 `<div className="bg-surface-sunken rounded-md" style={{ height: 88, animation: 'blink 2s ease-in-out infinite' }} />`

// ============================================================
// DdayBadge 함수 (line 61~93) — ★ OQ #2 LOCKED 3 임계치 매트릭스 anchor
// ============================================================

// ★ D-day 분기 매트릭스 임계치 30·0 + 라벨 verbatim (★ 비즈 anchor 4)
function DdayBadge({ dday }: { dday: number }) {
  // 옛 (1 byte 변경 0):
  if (dday > 30) {
    // 옛: bg = 'rgba(34,197,94,0.12)'; color = 'var(--safe)'; label = `D-${dday}`
    // 변환 후: className `bg-safe-bg text-safe`, label = `D-${dday}`
  } else if (dday >= 0) {
    // 옛: bg = 'rgba(245,158,11,0.15)'; color = 'var(--warn)'; label = `D-${dday}`
    // 변환 후: className `bg-warning-bg text-warning`, label = `D-${dday}`
  } else {
    // 옛: bg = 'rgba(239,68,68,0.15)'; color = 'var(--danger)'; label = `D+${Math.abs(dday)} 초과`
    // 변환 후: className `bg-danger-bg text-danger`, label = `D+${Math.abs(dday)} 초과`
  }
  // 외곽 div (line 80~91):
  // 옛 style: { background: bg, color, fontSize: 12, fontWeight: 700, padding: '2px 8px', borderRadius: 8, flexShrink: 0 }
  // 변환 후: className `${tokenClass} text-caption font-bold leading-none rounded-sm`, style `padding: '2px 8px', flexShrink: 0`
}

// ============================================================
// StaffEducationCard 함수 (line 96~187) — ★ OQ #3 LOCKED selected anchor
// ============================================================

// EducationPage 메인 (line 419~590)
export default function EducationPage() {
  const navigate = useNavigate()
  const isDesktop = useIsDesktop()                                          // ★ 비즈 anchor 8 — useIsDesktop()
  const { staff: currentStaff } = useAuthStore()
  const [selectedItem, setSelectedItem] = useState<StaffEducation | null>(null)

  // ★ useQuery (line 427~430) — ★ 비즈 anchor 5
  const { data, isLoading, isError } = useQuery({
    queryKey: ['education'],
    queryFn: educationApi.list,
  })

  // ★ canEdit (line 432~435) — ★ 비즈 anchor 9
  function canEdit(cardStaffId: string): boolean {
    if (!currentStaff) return false
    return currentStaff.role === 'admin' || currentStaff.id === cardStaffId
  }

  // ★ adminList + assistantList sort (line 438~439) — titleRank 활용
  const adminList = (data ?? []).filter(i => i.staff.role === 'admin').sort((a, b) => titleRank(a.staff.title) - titleRank(b.staff.title))
  const assistantList = (data ?? []).filter(i => i.staff.role !== 'admin').sort((a, b) => titleRank(a.staff.title) - titleRank(b.staff.title))

  // ... renderCards / renderGroupedList / 데스크톱 분기 / 모바일 분기 ...

  // ★ 모바일 back button (line 549~560) — ★ OQ #5 LOCKED Lucide 교체
  // 옛: <IconChevronLeft size={20} color="var(--t2)" />
  // 변환 후: <ChevronLeft size={20} />   (className `text-text-secondary` 가 색 처리)

  // ★ @keyframes blink (line 583~588) — ★ 비즈 anchor 10
  // <style>{`@keyframes blink { 0%, 100% { opacity: 1; } 50% { opacity: 0.4; } }`}</style>
}

// ============================================================
// EducationEditPanel 안 useMutation × 3 (line 212~244) — ★ 비즈 anchor 6 + 11
// ============================================================

const createMutation = useMutation({
  mutationFn: () => educationApi.create({                                  // ★ educationApi.create anchor
    staffId: staff.id,
    education_type: educationType,                                          // ★ snake_case payload anchor
    completed_at: completedAt,
  }),
  onSuccess: () => {
    queryClient.invalidateQueries({ queryKey: ['education'] })              // ★ invalidateQueries anchor
    toast.success('이수일이 기록되었습니다.')                                  // ★ toast 카피 5종 anchor #1
    onSaved()
  },
  onError: () => toast.error('이수 기록 저장에 실패했습니다.'),                  // ★ toast 카피 #4
})

const updateMutation = useMutation({
  mutationFn: () => educationApi.update(editingRecord!.id, { completed_at: completedAt }),  // ★ positional id + snake_case
  onSuccess: () => {
    queryClient.invalidateQueries({ queryKey: ['education'] })
    toast.success('이수일이 수정되었습니다.')                                  // ★ toast 카피 #2
    onSaved()
  },
  onError: () => toast.error('이수 기록 저장에 실패했습니다.'),                  // ★ toast 카피 #4
})

const deleteMutation = useMutation({
  mutationFn: (id: string) => educationApi.delete(id),                      // ★ positional id
  onSuccess: () => {
    queryClient.invalidateQueries({ queryKey: ['education'] })
    toast.success('이수 기록이 삭제되었습니다.')                                // ★ toast 카피 #3
    onSaved()
  },
  onError: (e: any) => toast.error(e?.message ?? '삭제에 실패했습니다.'),       // ★ toast 카피 #5
})

// EducationBottomSheet (line 393~417) — slideUp 인라인 anchor
// <div style={{ background: 'var(--bg2)', borderRadius: '16px 16px 0 0',
//               animation: 'slideUp 0.28s ease-out both',                  // ★ slideUp anchor
//               maxHeight: '90vh', overflowY: 'auto', padding: '16px 16px 32px' }}>
//   → className `bg-surface-raised rounded-t-lg` + 인라인 animation + maxHeight + overflowY + padding 유지
```
</interfaces>

<biz_anchor_lockdown>
<!-- 17-annual-plan SW3 precedent + 28-splash 4i9 mirror — 비즈 anchor 시그니처 보존 = 비즈 로직 / API 계약 / D-day 계산 / 권한 분기 / toast 카피 무손실 변환 조건 -->

**EducationPage 12건 — 모두 1 byte 변경 금지:**

1. **calcNextDeadline 함수 시그니처** (line 21~33) — `addMonths(parseISO(appointedAt), 6)` (첫 실무, 6개월) + `addYears(parseISO(sorted[0].completedAt), 2)` (보수, 2년)
   - 첫 실무교육 마감일 = 선임일 + 6개월 / 보수교육 마감일 = 가장 최근 이수일 + 2년. 변경 시 마감 계산 어긋남.

2. **`differenceInCalendarDays(d, new Date())`** (line 29, 32) — D-day 산출
   - `new Date()` 현재 시점 기준 캘린더 일수 차이. timezone 영향 받음. memory `feedback_korean_holidays_library_gap` 단일 fallback 패턴 동일 정신.

3. **TITLE_ORDER 객체 + titleRank fallback 99** (line 49~50) — `{ '주임': 0, '대리': 1, '기사': 2 }` + `TITLE_ORDER[title] ?? 99`
   - 직급 정렬 키. 변경 시 직원 카드 정렬 순서 어긋남. fallback 99 = 미정의 직급은 끝으로.

4. **D-day 분기 매트릭스 + 라벨** (line 66~78) — 임계치 30 / 0 + 라벨 `D-${dday}` (safe + warning) / `D+${Math.abs(dday)} 초과` (danger)
   - >30 → safe / 0~30 → warning / <0 → danger 3 분기. 라벨 verbatim ('D-' 이지만 음수일 때 'D+' 표기 + ' 초과' 명시). 변경 시 사용자 인지 어긋남. OQ #2 LOCKED.

5. **useQuery queryKey ['education']** (line 427~430) — `queryKey: ['education'], queryFn: educationApi.list`
   - React Query cache 키 anchor. 3 mutation 모두 동일 key 로 invalidate. 변경 시 캐시 갱신 어긋남.

6. **useMutation × 3 + invalidateQueries** (line 212~244) — create / update / delete 3개 모두 동일 invalidate 패턴
   - create: educationApi.create({ staffId, education_type, completed_at }) + invalidateQueries ['education'] + toast.success '이수일이 기록되었습니다.' + onSaved
   - update: educationApi.update(editingRecord!.id, { completed_at }) + invalidateQueries + toast.success '이수일이 수정되었습니다.'
   - delete: educationApi.delete(id) + invalidateQueries + toast.success '이수 기록이 삭제되었습니다.' + onError 'e?.message ?? 삭제에 실패했습니다.'

7. **educationApi 호출 시그니처** — snake_case payload (`education_type` / `completed_at`) + positional id (update / delete)
   - API 계약 anchor. camelCase 로 바꿀 시 D1 컬럼명 미스매치 → 500 error.

8. **useIsDesktop()** (line 422) — 데스크톱 분기 hook. 변경 시 데스크톱/모바일 분기 깨짐.

9. **canEdit** (line 432~435) — `currentStaff?.role === 'admin' || currentStaff.id === cardStaffId`
   - admin 은 모든 카드 edit / assistant 는 본인 카드만 edit. memory `project_inspection_completion_rule` 단일 룰 anchor 동일 패턴.

10. **@keyframes blink + @keyframes slideUp** (line 583~588 + line 406) — 인라인 <style> 블록 + EducationBottomSheet animation 인라인
    - blink: 0%/100% opacity 1 + 50% opacity 0.4 (스켈레톤 깜빡임). slideUp: EducationBottomSheet 등장 (0.28s ease-out both). 변경 시 시각 어긋남.

11. **toast 카피 5종 verbatim** (line 220/223/230/233/240/243):
    - create.success '이수일이 기록되었습니다.'
    - update.success '이수일이 수정되었습니다.'
    - delete.success '이수 기록이 삭제되었습니다.'
    - create+update.error '이수 기록 저장에 실패했습니다.' (line 223, 233 동일)
    - delete.error 'e?.message ?? 삭제에 실패했습니다.'

12. **빈/오류/fallback 카피 verbatim** (line 472/477/478/528 + line 562):
    - 오류 '교육 현황을 불러오지 못했습니다. 화면을 당겨서 새로고침하세요.' (line 472)
    - 빈 제목 '교육 이력 없음' (line 477)
    - 빈 보조 '이수일을 기록하면 다음 교육 마감일이 자동으로 계산됩니다.' (line 478)
    - 데스크톱 우측 fallback '좌측에서 직원을 선택하세요' (line 528)
    - 모바일 타이틀 '보수교육' (line 562)
    - role 그룹 라벨 '소방안전관리자' (line 486) + '소방안전관리 보조자' (line 492)
    - select option '실무교육 (최초)' (line 369) + '보수교육' (line 370)
    - submit text 'isSubmitting ? "저장 중..." : (isEditMode ? "수정 완료" : "이수일 기록")' (line 383)

**확인 grep gate (verify automated 안에 박제):**
```bash
# 비즈 anchor 12건
grep -c 'calcNextDeadline' E                                                     # >= 2 (정의 + 사용 ≥1)
grep -c 'addMonths(parseISO(appointedAt), 6)' E                                  # >= 1
grep -c 'addYears(parseISO(sorted\[0\]\.completedAt), 2)' E                      # >= 1
grep -c 'differenceInCalendarDays(d, new Date())' E                              # >= 2 (첫 실무 + 보수 2건)
grep -cE "'주임': 0, '대리': 1, '기사': 2" E                                       # >= 1 (TITLE_ORDER 단일 객체)
grep -c 'TITLE_ORDER\[title\] ?? 99' E                                           # >= 1 (titleRank fallback)
grep -c 'dday > 30' E                                                             # >= 1 (D-day 임계치 1)
grep -c 'dday >= 0' E                                                             # >= 1 (D-day 임계치 2)
grep -c 'D-${dday}' E                                                             # >= 2 (safe + warning 라벨)
grep -c 'D+${Math.abs(dday)} 초과' E                                              # >= 1 (danger 라벨)
grep -c "queryKey: \['education'\]" E                                            # >= 4 (useQuery + 3 invalidate)
grep -c 'educationApi.create' E                                                   # >= 1
grep -c 'educationApi.update' E                                                   # >= 1
grep -c 'educationApi.delete' E                                                   # >= 1
grep -c 'education_type:' E                                                       # >= 1
grep -c 'completed_at:' E                                                         # >= 2 (create + update)
grep -c 'useIsDesktop()' E                                                        # >= 1
grep -c "currentStaff?.role === 'admin' || currentStaff.id === cardStaffId" E    # >= 1
grep -c '@keyframes blink' E                                                      # >= 1
grep -c 'slideUp 0.28s ease-out both' E                                           # >= 1
grep -c '이수일이 기록되었습니다' E                                                  # >= 1
grep -c '이수일이 수정되었습니다' E                                                  # >= 1
grep -c '이수 기록이 삭제되었습니다' E                                                # >= 1
grep -c '이수 기록 저장에 실패했습니다' E                                              # >= 2
grep -c 'e?.message ?? .삭제에 실패했습니다' E                                       # >= 1  (e?.message ?? '삭제에 실패했습니다.')
grep -c '교육 이력 없음' E                                                          # >= 1
grep -c '이수일을 기록하면 다음 교육 마감일이 자동으로 계산됩니다' E                      # >= 1
grep -c '교육 현황을 불러오지 못했습니다. 화면을 당겨서 새로고침하세요' E                 # >= 1
grep -c '좌측에서 직원을 선택하세요' E                                                # >= 1
grep -c '보수교육' E                                                                # >= 3 (모바일 타이틀 + select option + 라벨)
grep -c '소방안전관리자' E                                                          # >= 1
grep -c '소방안전관리 보조자' E                                                      # >= 1
grep -c '실무교육 (최초)' E                                                          # >= 1
```
</biz_anchor_lockdown>

<tailwind_token_cheatsheet>
<!-- W5 §9 verbatim 인용 — 옛 var(--alias) → v0.1.1 className 1:1 매핑 (23-education 적용 위치) -->

| 옛 인라인 / 색                                                | v0.1.1 className                            | 23-education 적용 위치                              |
|---------------------------------------------------------------|---------------------------------------------|----------------------------------------------------|
| `var(--bg)` (외곽 background)                                  | `bg-surface-page`                           | EducationPage 외곽 (모바일 line 538 + 데스크톱 line 503) |
| `var(--bg2)` (카드 + 모바일 헤더 + 바텀시트)                    | `bg-surface-raised`                         | StaffEducationCard (line 117) + 모바일 헤더 (line 543, OQ #1) + 바텀시트 (line 405) + 이수 이력 액션 버튼 (line 319) |
| `var(--bg3)` (아바타 + 마감 박스 + input + 이수 이력 row + SKELETON) | `bg-surface-sunken`                         | 아바타 32 (line 134) + 아바타 40 (line 278) + 마감 박스 (line 293) + 이수 이력 row (line 309) + input/select (line 267) + SKELETON_STYLE (line 54) + select disabled (OQ #3) |
| `var(--bd)` (보더)                                              | `border-border-default`                     | 모바일 헤더 borderBottom (line 544, OQ #1) + 카드 평시 border (line 120 평시) + 폼 borderTop (line 347) + 데스크톱 좌측 borderRight (line 506) |
| `var(--bd2)` (강한 보더)                                        | `border-border-strong`                      | input/select border (line 268) + 이수 이력 액션 버튼 border (line 320, 330) + 바텀시트 handle (line 411) |
| `var(--t1)` (text)                                              | `text-text-primary`                         | 이름 (line 148) + 모바일 타이틀 (line 561) + 다음 마감 일자 (line 295) + input color (line 268) + 이름 18 (line 285) |
| `var(--t2)` (text)                                              | `text-text-secondary`                       | 직책 (line 151) + back button color (line 556) + sectionLabelStyle (line 456) + 폼 라벨 (line 348) + 이수 이력 row (line 311) + 액션 버튼 수정 (line 321) + 빈 제목 (line 477) + 아바타 32 color (line 135) |
| `var(--t3)` (text)                                              | `text-text-tertiary`                        | 메타 (line 165, 171, 179) + 데스크톱 우측 fallback (line 526) + 빈 보조 (line 478) + 액션 버튼 삭제 (line 331) + 마감 박스 라벨 (line 294, 296) + 폼 라벨 (line 353, 357) + select disabled (line 364, OQ #3) |
| `var(--acl)` (CTA 배경, 옛 submit + selected card)               | submit: 인라인 `linear-gradient(135deg, #1d4ed8, #0ea5e9)` (W4 만 예외 anchor, OQ #3) / selected: `border-2 border-accent` (OQ #3, 데스크톱만, 1.5→2) | submit 버튼 (line 377, var(--acl) 완전 폐기 → 인라인 그라데이션 OQ #3 LOCKED) + selected card border (line 120 selected 분기 OQ #3 LOCKED) |
| `var(--safe)` + `rgba(34,197,94,0.12)` (D-day safe)              | `bg-safe-bg text-safe` (OQ #2 LOCKED)        | DdayBadge safe (>30, line 66~69) — rgba + var(--safe) 인라인 완전 폐기 |
| `var(--warn)` + `rgba(245,158,11,0.15)` (D-day warning)          | `bg-warning-bg text-warning` (OQ #2 LOCKED)  | DdayBadge warning (0~30, line 70~73) — rgba + var(--warn) 인라인 완전 폐기 |
| `var(--danger)` + `rgba(239,68,68,0.15)` (D-day danger)          | `bg-danger-bg text-danger` (OQ #2 LOCKED)    | DdayBadge danger (<0, line 74~77) — rgba + var(--danger) 인라인 완전 폐기 |
| `var(--danger)` (오류 카피)                                     | `text-danger`                                | 오류 (line 471) |
| `var(--bd2)` (handle)                                           | `bg-border-strong`                          | 바텀시트 handle 32x4 (line 411) |
| `#fff` (CTA 글자)                                                | `text-text-on-accent`                       | submit 버튼 (line 378, OQ #3) |
| `fontSize: 18` (이름 18)                                        | `text-title font-extrabold`                  | 프로필 헤더 이름 (line 285) |
| `fontSize: 16` (이름 + 모바일 타이틀 + 빈 제목)                  | `text-body font-bold`                       | StaffEducationCard 이름 (line 148) + 모바일 타이틀 (line 561) + 빈 제목 (line 477) |
| `fontSize: 14` (다음 마감 일자 + 오류 + 빈 보조 + 데스크톱 fallback + submit) | `text-body-sm`                       | 마감 박스 일자 (line 295, font-bold) + 오류 (line 471, text-danger) + 빈 보조 (line 478, text-text-tertiary) + 데스크톱 fallback (line 526, text-text-tertiary) + submit text (line 378, text-body font-bold = 14→16 격상 옵션) |
| `fontSize: 13` (직책 + 폼 라벨 + 이수 이력 + 아바타 32 + sectionLabel 모바일) | `text-label`                       | StaffEducationCard 직책 (line 151) + 폼 라벨 (line 348, font-bold) + 이수 이력 (line 311) + sectionLabelStyle 모바일 (line 454, font-bold) + 아바타 32 (line 139) |
| `fontSize: 12` (메타 + DdayBadge + 폼 라벨 + 액션 버튼 + 마감 박스 라벨 + 빈 보조 옵션) | `text-caption` + `leading-none` (작은 컨테이너) | DdayBadge (line 84) + 메타 (line 165, 171, 176, 179) + 폼 라벨 (line 353, 357 font-bold leading-none) + 액션 버튼 (line 321, 331 leading-none) + 마감 박스 라벨 (line 294, 296 leading-none) |
| `borderRadius: 12` (카드 + SKELETON)                            | `rounded-md`                                | StaffEducationCard (line 118) + SKELETON_STYLE (line 55) |
| `borderRadius: 10` (마감 박스 + submit + EducationEditPanel)    | `rounded-md`                                | 마감 박스 (line 293) + submit (line 377) |
| `borderRadius: 9` (input/select)                                | `rounded-md` 또는 `rounded-[9px]` arbitrary  | input/select (line 267) |
| `borderRadius: 8` (DdayBadge + 이수 이력 row + 액션 버튼)        | `rounded-sm`                                | DdayBadge (line 87) + 이수 이력 row (line 309) + 액션 버튼 (line 320, 330) |
| `borderRadius: 6` (액션 버튼 별 옵션)                            | `rounded-sm`                                | 액션 버튼 (line 320, 330, 옛 6 → rounded-sm 8 통합) |
| `borderRadius: '50%'` (아바타 32 + 40)                          | `rounded-full`                              | 아바타 32 (line 133) + 아바타 40 (line 277) |
| `borderRadius: '16px 16px 0 0'` (바텀시트)                      | `rounded-t-lg` (= 16px) 또는 `rounded-t-[16px]` arbitrary | EducationBottomSheet (line 405) |
| `borderRadius: 2` (바텀시트 handle)                              | `rounded-sm`                                | 바텀시트 handle (line 411) |

**status- class prefix 0건** (memory `feedback_tailwind_token_class_pattern`):
- 정확 = `bg-safe-bg` / `text-safe` / `bg-warning-bg` / `text-warning` / `bg-danger-bg` / `text-danger` / `border-accent` / `text-text-on-accent`
- 잘못 = `text-status-safe` / `bg-status-warning-bg` / `text-status-danger` ← 11-div TSX hotfix(4ce707e) 사고 회피

**w-8 / h-8 = 48px 함정** (memory `feedback_tailwind_w8_h8_is_48px`):
- 23-education 안 width:8 / height:8 토큰 사용 사례 없음. width:32 (아바타 32) / width:40 (아바타 40) / width:44 (back button + spacer + submit/확인했습니다) / width:48 (모바일 헤더 height) / width:88 (... 없음) 등 모두 인라인 명시.
- 특히 back button 44x44 = `style={{ width:44, height:44 }}` 인라인 명시 (tailwind w-11 = 44px 도 정확하지만 일관성으로 인라인 권장)
- 아바타 32x32 / 40x40 도 인라인 명시

**작은 컨테이너 text-caption → leading-none** (memory `feedback_text_caption_leading_none`):
- DdayBadge 12px (line 84) — leading-none 강제 (text-caption lh:1.5 = 18px 가 12px 컨테이너 안에서 시각 패딩)
- 메타 12px (line 165, 171, 179) — leading-relaxed 가 줄바꿈 다행 OK (선임일 미등록 + 다음 마감 + label) — 단일 행은 leading-none, 다행은 leading-relaxed 분기
- 액션 버튼 12px (line 321, 331) — leading-none
- 마감 박스 라벨 12px (line 294, 296) — leading-none
- 폼 라벨 12px (line 353, 357) — leading-none

**linear-gradient 폐기 (OQ #3 LOCKED 예외 anchor):**
- submit 인라인 `linear-gradient(135deg, #1d4ed8, #0ea5e9)` (line 377) **유지** (W4 만 예외 anchor, ≥1 회 grep gate)
- 그 외 linear-gradient 인스턴스 **0건** (옛 var(--acl) → background 'var(--acl)' 폐기, 인라인 그라데이션 OQ #3 LOCKED submit 만 유지)
- 23-education 에는 SplashScreen 같은 그라데이션 anchor 없음. submit 1건이 유일.

**Lucide ChevronLeft 교체 (OQ #5 LOCKED):**
- IconChevronLeft 인라인 SVG 함수 정의 (line 12~18) **완전 제거** → import { ChevronLeft } from 'lucide-react' 추가 1줄
- polyline points="15 18 9 12 15 6" SVG (line 15) **완전 제거**
- 사용처 `<IconChevronLeft size={20} color="var(--t2)" />` (line 559) → `<ChevronLeft size={20} />` 교체 (color 는 wrapper button 의 className `text-text-secondary` 가 처리)
- grep gate `IconChevronLeft = 0` (정의 + 사용 모두 제거) + `polyline = 0` (SVG 제거) + `import { ChevronLeft } from 'lucide-react' ≥ 1` (추가) + `<ChevronLeft size={20} ≥ 1` (사용)
</tailwind_token_cheatsheet>
</context>

<tasks>

<task type="auto">
  <name>Task 1: EducationPage.tsx 단일 파일 atomic v0.1.1 토큰 변환 (in-place, 비즈 anchor 12건 + Lucide ChevronLeft 교체 + OQ LOCKED 5건, atomic 1-commit)</name>
  <files>cha-bio-safety/src/pages/EducationPage.tsx</files>
  <action>

EducationPage.tsx 591 lines 를 **단일 atomic commit** 으로 in-place 수정하여 v0.1.1 토큰 className 으로 치환한다. **17-annual-plan (1hj) / 27-login (gox) / 16-workshift (u5n) mirror 패턴 — 단일 파일 atomic**. 단, 28-splash (4i9) 2 파일과 달리 591 lines 안 4 컴포넌트 통합 변환이라는 점이 다름. 비즈 로직(useState/useEffect/useMutation/useQuery/canEdit/calcNextDeadline/D-day 분기/toast/snake_case payload) 1 byte 변경 0. **★ 비즈 anchor 12건 + Lucide ChevronLeft 교체 (IconChevronLeft 함수 + polyline 완전 제거) 모두 grep PASS**.

## 0. 사전 확인 (필수)

```bash
# 0-1. branch 확인 (worktree-aware)
git branch --show-current   # 기대: redesign/23-education
git status --short          # 기대: 빈 출력 (clean)

# 0-2. branch base 검증 (use_worktrees=true 가드)
EXPECTED_BASE="origin/main"
git merge-base HEAD "$EXPECTED_BASE" >/dev/null 2>&1 && echo "branch base OK" || echo "branch base FAIL — origin/main 에서 분기 안 됨"

# 0-3. 변환 대상 파일 라인 수 baseline
wc -l cha-bio-safety/src/pages/EducationPage.tsx   # 기대: 591

# 0-4. App.tsx baseline (0 byte 변경 verify gate)
git rev-parse HEAD:cha-bio-safety/src/App.tsx > /tmp/app_tsx_hash_before.txt
cat /tmp/app_tsx_hash_before.txt

# 0-5. 인라인 alias / Icon / linear-gradient 현 상태 (변환 후 negative gate baseline)
grep -c 'var(--' cha-bio-safety/src/pages/EducationPage.tsx        # baseline: 다수 (변환 후 0)
grep -c 'IconChevronLeft' cha-bio-safety/src/pages/EducationPage.tsx  # baseline: 2 (정의 + 사용, 변환 후 0)
grep -c 'polyline' cha-bio-safety/src/pages/EducationPage.tsx      # baseline: 1 (변환 후 0)
grep -c 'linear-gradient' cha-bio-safety/src/pages/EducationPage.tsx  # baseline: 0 (변환 후 1 = OQ #3 submit 추가)
```

## 1. 변환 매핑 (W5 §3.1~§3.6 verbatim — 추측 0건)

### §3.1 영역 1 — 상단 유틸 + 상수 + 스켈레톤 (line 1~58, W2 sketch)

| 현재 (인라인 style, line) | 변환 후 (className + 인라인) | sketch / OQ |
|---|---|---|
| imports 9개 (line 1~9) | **그대로** + **추가**: `import { ChevronLeft } from 'lucide-react'` (line 10 또는 imports 끝, OQ #5 LOCKED) | W2 + OQ #5 |
| IconChevronLeft 함수 정의 (line 12~18): `function IconChevronLeft({ size = 20, color = 'currentColor' }: ...) { return <svg ... ><polyline points="15 18 9 12 15 6" /></svg> }` | **완전 제거** (OQ #5 LOCKED) — 함수 정의 + polyline SVG 모두 삭제 | OQ #5 |
| calcNextDeadline (line 21~33) | **그대로** (★ 비즈 anchor 1+2) — addMonths(parseISO(appointedAt), 6) + addYears(parseISO(sorted[0].completedAt), 2) + differenceInCalendarDays(d, new Date()) | - |
| fmtDate + dateToYmd (line 35~46) | **그대로** | - |
| TITLE_ORDER + titleRank (line 49~50) | **그대로** (★ 비즈 anchor 3) — `{ '주임': 0, '대리': 1, '기사': 2 }` + `TITLE_ORDER[title] ?? 99` | - |
| SKELETON_STYLE (line 53~58): `background: 'var(--bg3)', borderRadius: 12, height: 88, animation: 'blink 2s ease-in-out infinite'` | **옵션 A (인라인 유지)**: var(--bg3) → tokens.css 정의된 surface-sunken 으로 치환 시도. 단, var(--bg3) 가 tokens.css 에 존재 시 인라인 유지 OK. **옵션 B (className 분리, 권장)**: SKELETON_STYLE 상수 제거 + 사용처 line 464~467 4개 모두 `<div className="bg-surface-sunken rounded-md" style={{ height:88, animation:'blink 2s ease-in-out infinite' }} />` 인라인. ★ height 88 + animation 보존 anchor | W2 |

### §3.2 영역 2 — DdayBadge (line 61~93, OQ #2 LOCKED ★)

```jsx
// 옛 (line 61~93):
function DdayBadge({ dday }: { dday: number }) {
  let bg: string; let color: string; let label: string;
  if (dday > 30) {
    bg = 'rgba(34,197,94,0.12)'; color = 'var(--safe)'; label = `D-${dday}`
  } else if (dday >= 0) {
    bg = 'rgba(245,158,11,0.15)'; color = 'var(--warn)'; label = `D-${dday}`
  } else {
    bg = 'rgba(239,68,68,0.15)'; color = 'var(--danger)'; label = `D+${Math.abs(dday)} 초과`
  }
  return (
    <div style={{ background: bg, color, fontSize: 12, fontWeight: 700, padding: '2px 8px', borderRadius: 8, flexShrink: 0 }}>
      {label}
    </div>
  )
}

// 변환 후 — OQ #2 LOCKED 3 토큰 매트릭스 (status- prefix 없음, ★ 임계치 30·0 + 라벨 verbatim):
function DdayBadge({ dday }: { dday: number }) {
  let tokenClass: string; let label: string;
  if (dday > 30) {
    tokenClass = 'bg-safe-bg text-safe'              // OQ #2 LOCKED safe
    label = `D-${dday}`                                // ★ verbatim
  } else if (dday >= 0) {
    tokenClass = 'bg-warning-bg text-warning'         // OQ #2 LOCKED warning
    label = `D-${dday}`                                // ★ verbatim
  } else {
    tokenClass = 'bg-danger-bg text-danger'           // OQ #2 LOCKED danger
    label = `D+${Math.abs(dday)} 초과`                  // ★ verbatim
  }
  return (
    <div
      className={`${tokenClass} text-caption font-bold leading-none rounded-sm`}   // OQ #2 + memory feedback_text_caption_leading_none
      style={{ padding: '2px 8px', flexShrink: 0 }}
    >
      {label}
    </div>
  )
}
```

### §3.3 영역 3 — StaffEducationCard (line 96~187, OQ #3 LOCKED selected ★)

| 현재 | 변환 후 | sketch / OQ |
|---|---|---|
| 외곽 div (line 114~125): `background: 'var(--bg2)', borderRadius: 12, padding: 16, border: selected ? '1.5px solid var(--acl)' : '1px solid var(--bd)', cursor: ..., minHeight: 80, WebkitTapHighlightColor: 'transparent', userSelect: 'none'` | `<div onClick={canEdit ? onTap : undefined} className={\`bg-surface-raised rounded-md ${selected ? 'border-2 border-accent' : 'border border-border-default'}\`} style={{ padding:16, minHeight:80, cursor: canEdit ? 'pointer' : 'default', WebkitTapHighlightColor:'transparent', userSelect:'none' }}>` (★ OQ #3 LOCKED 1.5→2 + var(--acl)→border-accent, 데스크톱만 selected) | W3 + OQ #3 |
| 아바타 32x32 (line 130~144): `width:32, height:32, borderRadius:'50%', background:'var(--bg3)', color:'var(--t2)', fontSize:14, fontWeight:700, flexShrink:0` + 인라인 flex center | `<div className="bg-surface-sunken text-text-secondary text-body-sm font-bold rounded-full" style={{ width:32, height:32, display:'flex', alignItems:'center', justifyContent:'center', flexShrink:0 }}>` (w-8 함정 회피 — 32px 인라인 명시, fontSize 14 → text-body-sm = 14) | W3 |
| 이름 (line 148): `fontSize:16, fontWeight:700, color:'var(--t1)', lineHeight:1.3` | `<div className="text-body font-bold text-text-primary" style={{ lineHeight:1.3 }}>` | W3 |
| 직책 (line 151): `fontSize:13, fontWeight:400, color:'var(--t2)', marginTop:2` | `<div className="text-label text-text-secondary" style={{ marginTop:2 }}>` | W3 |
| 메타 (line 165, 171, 176, 179) 4건: `fontSize:12, fontWeight:400, color:'var(--t3)'` 또는 `color:'var(--t2)'` | 다음 4 가지 분기:<br>- line 165 마지막 이수 (var(--t3)): `<div className="text-caption leading-relaxed text-text-tertiary" style={{ marginBottom:2 }}>`<br>- line 171 선임일 미등록 (var(--t3)): `<div className="text-caption leading-relaxed text-text-tertiary">`<br>- line 176 다음 마감 일자 (var(--t2)): `<span className="text-caption leading-relaxed text-text-secondary">`<br>- line 179 label (var(--t3)): `<span className="text-caption leading-relaxed text-text-tertiary">` | W3 |

### §3.4 영역 4 — EducationEditPanel (line 190~390, OQ #3 LOCKED submit + disabled ★)

| 현재 | 변환 후 | sketch / OQ |
|---|---|---|
| 프로필 헤더 아바타 40x40 (line 277~282): `width:40, height:40, borderRadius:'50%', background:'var(--bg3)', color:'var(--t2)', fontSize:16, fontWeight:700, flexShrink:0` + flex center | `<div className="bg-surface-sunken text-text-secondary text-body font-bold rounded-full" style={{ width:40, height:40, display:'flex', alignItems:'center', justifyContent:'center', flexShrink:0 }}>` | W4 |
| 이름 (line 285): `fontSize:18, fontWeight:700, color:'var(--t1)'` | `<div className="text-title font-extrabold text-text-primary">` | W4 |
| 직책 (line 286): `fontSize:13, color:'var(--t2)', marginTop:2` | `<div className="text-label text-text-secondary" style={{ marginTop:2 }}>` | W4 |
| 마감 박스 (line 293): `background:'var(--bg3)', borderRadius:10, padding:'12px 16px'` | `<div className="bg-surface-sunken rounded-md" style={{ padding:'12px 16px' }}>` | W4 |
| 마감 박스 라벨 '다음 마감' (line 294): `fontSize:12, fontWeight:400, color:'var(--t3)', marginBottom:4` | `<div className="text-caption leading-none text-text-tertiary" style={{ marginBottom:4 }}>` | W4 |
| 마감 박스 일자 (line 295): `fontSize:14, fontWeight:600, color:'var(--t1)'` | `<div className="text-body-sm font-bold text-text-primary">` | W4 |
| 마감 박스 (label) 보조 (line 296): `fontSize:12, fontWeight:400, color:'var(--t3)'` | `<span className="text-caption leading-none text-text-tertiary">` | W4 |
| 이수 이력 제목 (line 304): `fontSize:13, fontWeight:700, color:'var(--t2)', marginBottom:8` | `<div className="text-label font-bold text-text-secondary" style={{ marginBottom:8 }}>` | W4 |
| 이수 이력 row (line 307~310): `background:'var(--bg3)', borderRadius:8, padding:'8px 12px'` + flex space-between | `<div className="bg-surface-sunken rounded-sm" style={{ display:'flex', alignItems:'center', justifyContent:'space-between', padding:'8px 12px' }}>` | W4 |
| 이수 이력 date (line 311): `fontSize:13, color:'var(--t2)'` | `<span className="text-label text-text-secondary">` | W4 |
| 액션 버튼 수정/취소 (line 318~322): `background: editingRecord?.id === rec.id ? 'var(--bg4)' : 'var(--bg2)', border:'1px solid var(--bd2)', borderRadius:6, padding:'4px 10px', fontSize:12, color:'var(--t2)', cursor:'pointer'` | `<button className={\`${editingRecord?.id === rec.id ? 'bg-surface-raised' : 'bg-surface-raised'} border border-border-strong text-text-secondary text-caption leading-none rounded-sm\`} style={{ padding:'4px 10px', cursor:'pointer' }}>` (var(--bg4) 가 tokens.css 없음 → bg-surface-raised 통합, 또는 인라인 var(--bg4) 유지 — 단, 변환 wave 마무리 시 0 목표) | W4 |
| 액션 버튼 삭제 (line 328~333): `background:'var(--bg2)', border:'1px solid var(--bd2)', borderRadius:6, padding:'4px 10px', fontSize:12, color:'var(--t3)', cursor: isSubmitting ? 'not-allowed' : 'pointer'` + disabled | `<button className="bg-surface-raised border border-border-strong text-text-tertiary text-caption leading-none rounded-sm" style={{ padding:'4px 10px', cursor: isSubmitting ? 'not-allowed' : 'pointer' }} disabled={isSubmitting}>` | W4 |
| 폼 borderTop (line 347): `borderTop:'1px solid var(--bd)', paddingTop:16` | `<div className="border-t border-border-default" style={{ paddingTop:16 }}>` | W4 |
| 폼 제목 (line 348): `fontSize:13, fontWeight:700, color:'var(--t2)', marginBottom:10` | `<div className="text-label font-bold text-text-secondary" style={{ marginBottom:10 }}>` | W4 |
| 폼 라벨 이수일 (line 353): `fontSize:12, fontWeight:700, color:'var(--t3)', marginBottom:6` | `<div className="text-caption leading-none font-bold text-text-tertiary" style={{ marginBottom:6 }}>` | W4 |
| 폼 라벨 교육 유형 (line 357): `fontSize:12, fontWeight:700, color:'var(--t3)', marginBottom:6` | `<div className="text-caption leading-none font-bold text-text-tertiary" style={{ marginBottom:6 }}>` | W4 |
| inputStyle (line 266~271): `width:'100%', background:'var(--bg3)', borderRadius:9, padding:'10px 12px', border:'1px solid var(--bd2)', color:'var(--t1)', fontSize:13, boxSizing:'border-box', outline:'none', fontFamily:'inherit', minWidth:0, WebkitAppearance:'none', appearance:'none'` | inputStyle 상수 제거 또는 className 분리:<br>`<input type="date" value={completedAt} onChange={...} className="bg-surface-sunken border border-border-strong text-label text-text-primary rounded-md" style={{ width:'100%', padding:'10px 12px', boxSizing:'border-box', outline:'none', fontFamily:'inherit', minWidth:0, WebkitAppearance:'none', appearance:'none' }} />` (radius 9 → rounded-md 12, 미세 차이 허용 또는 rounded-[9px] arbitrary) | W4 |
| select disabled 분기 (line 358~371): `color: (!hasRecords && !isEditMode) ? 'var(--t3)' : 'var(--t1)', cursor: 'default', pointerEvents: 'none'` (옛 인라인 분기) | className 동적 분기:<br>`<select disabled={!hasRecords && !isEditMode} className={\`${(!hasRecords && !isEditMode) ? 'bg-surface-sunken text-text-tertiary cursor-not-allowed' : 'bg-surface-sunken text-text-primary cursor-pointer'} border border-border-strong text-label rounded-md\`} style={{ width:'100%', padding:'10px 12px', ..., pointerEvents: (!hasRecords && !isEditMode) ? 'none' : 'auto', appearance:'none' }}>` (★ OQ #3 LOCKED disabled 토큰) | W4 + OQ #3 |
| **★ submit button** (line 374~384): `width:'100%', height:44, background:'var(--acl)', borderRadius:10, border:'none', color:'#fff', fontWeight:700, fontSize:14, cursor: ..., opacity: ..., marginTop:4` | `<button onClick={handleSubmit} disabled={isSubmitting \|\| !completedAt} className="text-text-on-accent text-body font-bold rounded-md" style={{ width:'100%', height:44, border:'none', cursor: isSubmitting ? 'default' : 'pointer', opacity: isSubmitting ? 0.6 : 1, marginTop:4, background: 'linear-gradient(135deg, #1d4ed8, #0ea5e9)' }}>` (★ OQ #3 LOCKED 인라인 그라데이션 유지, var(--acl) 완전 폐기. fontSize 14 → text-body = 16 격상, 또는 text-body-sm = 14 유지 — W5 §9 cheatsheet 에 14 → text-body-sm 명시이나 OQ #3 LOCKED 가 submit 격상 명시 X — submit 은 fontSize 14 유지 또는 text-body-sm. **결정: text-body (16 격상)** — submit CTA 격상으로 visual hierarchy 정합) | W4 + OQ #3 |
| submit text (line 383): `{isSubmitting ? '저장 중...' : (isEditMode ? '수정 완료' : '이수일 기록')}` | **1 byte 변경 0** (★ verbatim) | - |
| select option (line 369~370): `<option value="initial">실무교육 (최초)</option><option value="refresher">보수교육</option>` | **1 byte 변경 0** (★ verbatim) | - |

### §3.5 영역 5 — EducationBottomSheet (line 393~417, W4 sketch)

| 현재 | 변환 후 | sketch |
|---|---|---|
| 오버레이 (line 397~400): `position:'fixed', inset:0, background:'rgba(0,0,0,0.6)', display:'flex', flexDirection:'column', justifyContent:'flex-end', zIndex:50` | **인라인 100% 유지** (alpha 정밀도, OQ #4 정신 일관 — 인라인 anchor 1) | W4 |
| 시트 (line 402~408): `background:'var(--bg2)', borderRadius:'16px 16px 0 0', animation:'slideUp 0.28s ease-out both', maxHeight:'90vh', overflowY:'auto', padding:'16px 16px 32px'` | `<div onClick={e => e.stopPropagation()} className="bg-surface-raised rounded-t-lg" style={{ animation:'slideUp 0.28s ease-out both', maxHeight:'90vh', overflowY:'auto', padding:'16px 16px 32px' }}>` (rounded-t-lg = 16px 또는 rounded-t-[16px] arbitrary, ★ slideUp 인라인 anchor 보존) | W4 |
| handle 32x4 (line 411): `width:32, height:4, background:'var(--bd2)', borderRadius:2` | `<div className="bg-border-strong rounded-sm" style={{ width:32, height:4 }} />` | W4 |

### §3.6 영역 6 — EducationPage 메인 + 데스크톱/모바일 분기 (line 419~590, OQ #1 + OQ #4 + OQ #5 LOCKED ★)

```jsx
// 옛 line 420~430 (1 byte 변경 0):
export default function EducationPage() {
  const navigate = useNavigate()
  const isDesktop = useIsDesktop()                                     // ★ 비즈 anchor 8
  const { staff: currentStaff } = useAuthStore()
  const [selectedItem, setSelectedItem] = useState<StaffEducation | null>(null)
  const { data, isLoading, isError } = useQuery({                       // ★ 비즈 anchor 5
    queryKey: ['education'],
    queryFn: educationApi.list,
  })
  // ... canEdit + adminList + assistantList + renderCards 그대로

  // sectionLabelStyle (line 453~459): { fontSize: isDesktop ? 15 : 13, fontWeight:700, color:'var(--t2)', marginBottom:8, marginTop:4 }
  // 변환 후 옵션 A (className): `text-label font-bold text-text-secondary` + 인라인 marginBottom + marginTop (15→13 통일 = text-label, 데스크톱만 약간 작아지나 허용 범위)
  // 변환 후 옵션 B (분기 className): `${isDesktop ? 'text-body-sm font-bold' : 'text-label font-bold'} text-text-secondary` + 인라인 marginBottom + marginTop
  // 결정: 옵션 B (isDesktop 분기, 데스크톱 15→14 격하 미세 차이 허용)

  // 로딩 SKELETON (line 462~468):
  if (isLoading) return (
    <>
      <div className="bg-surface-sunken rounded-md" style={{ height:88, animation:'blink 2s ease-in-out infinite' }} />   {/* × 4 */}
      ...
    </>
  )

  // 오류 (line 471~473):
  if (isError) return (
    <div className="text-body-sm text-danger"
         style={{ display:'flex', alignItems:'center', justifyContent:'center', flex:1, textAlign:'center', padding:24 }}>
      교육 현황을 불러오지 못했습니다. 화면을 당겨서 새로고침하세요.   {/* ★ OQ #4 verbatim */}
    </div>
  )

  // 빈 (line 475~480):
  if (!data || data.length === 0) return (
    <div style={{ display:'flex', flexDirection:'column', alignItems:'center', justifyContent:'center', flex:1, textAlign:'center', padding:24, gap:8 }}>
      <div className="text-body font-bold text-text-secondary">교육 이력 없음</div>                                            {/* ★ OQ #4 verbatim */}
      <div className="text-body-sm text-text-tertiary">이수일을 기록하면 다음 교육 마감일이 자동으로 계산됩니다.</div>           {/* ★ OQ #4 verbatim */}
    </div>
  )

  // ... role 그룹핑 + renderCards ...

  // 데스크톱 분기 (line 501~534):
  if (isDesktop) {
    return (
      <div className="bg-surface-page" style={{ display:'flex', height:'100%' }}>
        {/* 좌측 */}
        <div className="border-r border-border-default" style={{ flex:1, display:'flex', flexDirection:'column', height:'100%' }}>
          <div style={{ flex:1, overflowY:'auto', padding:'24px' }}>
            {renderGroupedList()}
          </div>
        </div>
        {/* 우측 */}
        <div style={{ flex:1, overflowY:'auto', padding:'24px 32px' }}>
          {selectedItem ? (
            <EducationEditPanel ... />
          ) : (
            <div className="text-body-sm text-text-tertiary"
                 style={{ display:'flex', alignItems:'center', justifyContent:'center', height:'100%' }}>
              좌측에서 직원을 선택하세요   {/* ★ OQ #4 verbatim */}
            </div>
          )}
        </div>
      </div>
    )
  }

  // 모바일 분기 (line 537~590):
  return (
    <div className="bg-surface-page" style={{ display:'flex', flexDirection:'column', height:'100%' }}>

      {/* ★ 모바일 자체 헤더 (line 541~565) — OQ #1 LOCKED */}
      <div className="bg-surface-raised border-b border-border-default"
           style={{ height:48, display:'flex', alignItems:'center', flexShrink:0 }}>
        <button onClick={() => navigate(-1)}
                className="text-text-secondary"
                style={{ width:44, height:44, background:'none', border:'none', cursor:'pointer',
                         display:'flex', alignItems:'center', justifyContent:'center' }}>
          <ChevronLeft size={20} />   {/* ★ OQ #5 LOCKED Lucide 교체, IconChevronLeft 완전 폐기 */}
        </button>
        <span className="text-body font-bold text-text-primary"
              style={{ flex:1, textAlign:'center' }}>
          보수교육   {/* ★ verbatim */}
        </span>
        <div style={{ width:44 }} />   {/* spacer */}
      </div>

      {/* 스크롤 영역 (line 568) */}
      <div style={{ flex:1, overflowY:'auto', padding:16 }}>
        {renderGroupedList()}
      </div>

      {/* 모바일 바텀시트 (line 572~580) — 변경 0 */}
      {selectedItem && (
        <EducationBottomSheet ... />
      )}

      {/* ★ @keyframes blink (line 583~588) — 1 byte 변경 0 */}
      <style>{`
        @keyframes blink {
          0%, 100% { opacity: 1; }
          50% { opacity: 0.4; }
        }
      `}</style>
    </div>
  )
}
```

## 2. OQ 5건 LOCKED 결정 (W1 + W5 §4 verbatim — 위반 0건)

### OQ #1 LOCKED — 모바일 자체 헤더 `bg-surface-raised border-b border-border-default`
- line 541~548 (모바일 헤더): `background:'var(--bg2)', borderBottom:'1px solid var(--bd)'` 인라인 완전 제거 → className `bg-surface-raised border-b border-border-default`
- `height:48`, `display:'flex'`, `alignItems:'center'`, `flexShrink:0` 인라인 유지
- grep gate `bg-surface-raised border-b border-border-default >= 1`

### OQ #2 LOCKED — DdayBadge 3 임계치 토큰
- line 66~78 분기 매트릭스: `bg = 'rgba(...)' + color = 'var(--safe/warn/danger)'` 인라인 완전 제거 → className 3 분기 `bg-safe-bg text-safe` / `bg-warning-bg text-warning` / `bg-danger-bg text-danger`
- 임계치 30 / 0 + 라벨 `D-${dday}` (safe + warning) / `D+${Math.abs(dday)} 초과` (danger) **1 byte 변경 0**
- 외곽 div className `${tokenClass} text-caption font-bold leading-none rounded-sm` + 인라인 `padding: '2px 8px', flexShrink: 0`
- **status- prefix 없음** (memory `feedback_tailwind_token_class_pattern`)
- grep gate `bg-safe-bg text-safe >= 1` + `bg-warning-bg text-warning >= 1` + `bg-danger-bg text-danger >= 1`

### OQ #3 LOCKED — submit + disabled + selected (3 anchor)
- **submit** (line 374~384): `background:'var(--acl)'` 인라인 완전 제거 → 인라인 `background: 'linear-gradient(135deg, #1d4ed8, #0ea5e9)'` 추가 (W4 만 예외 anchor) + className `text-text-on-accent text-body font-bold rounded-md`
- **select disabled** (line 358~371): `color: (!hasRecords && !isEditMode) ? 'var(--t3)' : 'var(--t1)'` 인라인 분기 → className 동적 분기 `${disabled ? 'bg-surface-sunken text-text-tertiary cursor-not-allowed' : 'bg-surface-sunken text-text-primary cursor-pointer'}`
- **selected card** (line 114~125, StaffEducationCard): `border: selected ? '1.5px solid var(--acl)' : '1px solid var(--bd)'` 인라인 → className 분기 `${selected ? 'border-2 border-accent' : 'border border-border-default'}` (1.5→2 격상, var(--acl)→border-accent, 데스크톱만 selected)
- grep gate `linear-gradient(135deg, #1d4ed8, #0ea5e9) >= 1` + `bg-surface-sunken text-text-tertiary cursor-not-allowed >= 1` + `border-2 border-accent >= 1`

### OQ #4 LOCKED — 빈/오류 카피 verbatim + 아이콘 추가 X
- 오류 카피 (line 472): '교육 현황을 불러오지 못했습니다. 화면을 당겨서 새로고침하세요.' **1 byte 변경 0**
- 빈 제목 (line 477): '교육 이력 없음' **1 byte 변경 0**
- 빈 보조 (line 478): '이수일을 기록하면 다음 교육 마감일이 자동으로 계산됩니다.' **1 byte 변경 0**
- 데스크톱 fallback (line 528): '좌측에서 직원을 선택하세요' **1 byte 변경 0**
- 아이콘 / lucide / SVG 추가 X (back button ChevronLeft 외 빈/오류 영역 아이콘 무)
- grep gate 4건 모두 `>= 1`

### OQ #5 LOCKED — Lucide ChevronLeft 교체
- **추가**: `import { ChevronLeft } from 'lucide-react'` (line 10 또는 imports 끝, 단 imports 그룹 안)
- **완전 제거**: IconChevronLeft 함수 정의 (line 12~18, 7 lines) + polyline SVG (line 15)
- **교체**: line 559 `<IconChevronLeft size={20} color="var(--t2)" />` → `<ChevronLeft size={20} />` (color 는 wrapper button className `text-text-secondary` 가 처리)
- grep gate `import { ChevronLeft } from 'lucide-react' >= 1` + `<ChevronLeft size={20} >= 1` + `IconChevronLeft = 0` + `polyline = 0`

## 3. ★ 비즈 anchor 12건 보존 (17-annual-plan SW3 precedent + 28-splash 4i9 mirror — 1 byte 변경 금지)

다음은 **변경 0** — className 추가 + 인라인 alias 토큰 제거 외 logic / 상수 / 값 / 카피 / 시그니처 라인 손대지 않음:

1. **calcNextDeadline 함수 시그니처** (line 21~33) — `function calcNextDeadline(appointedAt: string | null, records: EducationRecord[]): { deadline: Date | null; dday: number | null; label: string }` + `addMonths(parseISO(appointedAt), 6)` (line 28) + `addYears(parseISO(sorted[0].completedAt), 2)` (line 31)
   - 첫 실무교육 = 선임일 + 6개월. 보수교육 = 가장 최근 이수일 + 2년. 변경 시 마감 계산 어긋남.

2. **`differenceInCalendarDays(d, new Date())`** (line 29, 32) — 2번 등장
   - timezone-aware 캘린더 일수 차이. memory `feedback_korean_holidays_library_gap` 단일 fallback 패턴 동일 정신.

3. **TITLE_ORDER + titleRank** (line 49~50) — `{ '주임': 0, '대리': 1, '기사': 2 }` + `TITLE_ORDER[title] ?? 99`
   - 직급 정렬 키 + fallback 99. 변경 시 adminList / assistantList 정렬 깨짐.

4. **D-day 분기 매트릭스 + 라벨** (line 66~78) — OQ #2 LOCKED ★ 임계치 30 / 0 + 라벨 `D-${dday}` (safe + warning) / `D+${Math.abs(dday)} 초과` (danger)
   - >30 → safe / 0~30 → warning / <0 → danger. 라벨 verbatim. memory `feedback_inspection_unresolved_color` 일반화.

5. **useQuery queryKey ['education']** (line 427~430) — `queryKey: ['education'], queryFn: educationApi.list`
   - React Query cache 키. 3 mutation 모두 동일 key 로 invalidate (line 219, 229, 239). 변경 시 캐시 갱신 어긋남.

6. **useMutation × 3 + invalidateQueries** (line 212~244):
   - createMutation: `educationApi.create({ staffId: staff.id, education_type: educationType, completed_at: completedAt })` + invalidateQueries + toast.success '이수일이 기록되었습니다.' + onSaved + onError '이수 기록 저장에 실패했습니다.'
   - updateMutation: `educationApi.update(editingRecord!.id, { completed_at: completedAt })` + invalidateQueries + toast.success '이수일이 수정되었습니다.' + onError '이수 기록 저장에 실패했습니다.'
   - deleteMutation: `educationApi.delete(id)` + invalidateQueries + toast.success '이수 기록이 삭제되었습니다.' + onError 'e?.message ?? 삭제에 실패했습니다.'

7. **educationApi 호출 시그니처** — snake_case payload (`education_type` / `completed_at`) + positional id (update / delete)
   - API 계약 anchor. camelCase 변경 시 D1 컬럼명 미스매치.

8. **`useIsDesktop()`** (line 422) — `const isDesktop = useIsDesktop()`
   - >=768px 분기 hook. memory `project_desktop_conversion_plan`.

9. **canEdit** (line 432~435) — `function canEdit(cardStaffId: string): boolean { if (!currentStaff) return false; return currentStaff.role === 'admin' || currentStaff.id === cardStaffId }`
   - 권한 분기. admin 모든 카드 / assistant 본인만. memory `project_inspection_completion_rule` 동일 패턴.

10. **@keyframes blink + @keyframes slideUp** (line 583~588 + line 406)
    - blink (인라인 <style>): 0%/100% opacity 1 + 50% opacity 0.4 (스켈레톤 깜빡임).
    - slideUp (EducationBottomSheet 인라인): 'slideUp 0.28s ease-out both' (시트 등장 애니메이션).
    - 글로벌 @keyframes slideUp 은 별도 정의 파일이 있음 (App.tsx 또는 index.css), animation 호출만 anchor.

11. **toast 카피 5종 verbatim**:
    - '이수일이 기록되었습니다.' (line 220, createMutation.success)
    - '이수일이 수정되었습니다.' (line 230, updateMutation.success)
    - '이수 기록이 삭제되었습니다.' (line 240, deleteMutation.success)
    - '이수 기록 저장에 실패했습니다.' (line 223, 233, create+update.error 2번 등장)
    - 'e?.message ?? 삭제에 실패했습니다.' (line 243, deleteMutation.error)

12. **빈/오류/fallback 카피 verbatim**:
    - 오류 (line 472): '교육 현황을 불러오지 못했습니다. 화면을 당겨서 새로고침하세요.'
    - 빈 제목 (line 477): '교육 이력 없음'
    - 빈 보조 (line 478): '이수일을 기록하면 다음 교육 마감일이 자동으로 계산됩니다.'
    - 데스크톱 fallback (line 528): '좌측에서 직원을 선택하세요'
    - 모바일 타이틀 (line 562): '보수교육'
    - role 그룹 라벨 (line 486, 492): '소방안전관리자' / '소방안전관리 보조자'
    - select option (line 369, 370): '실무교육 (최초)' / '보수교육'
    - submit text (line 383): isSubmitting ? '저장 중...' : (isEditMode ? '수정 완료' : '이수일 기록')

## 4. 비즈 로직 0 diff 보존 (W5 §10 sampling)

다음은 **변경 0** — className 추가 + 인라인 alias 토큰 제거 외 logic 라인 손대지 않음:

### EducationPage 메인 (line 419~590)
- line 1~9 imports 9개 + **추가** line 10 또는 imports 끝 `import { ChevronLeft } from 'lucide-react'`
- line 20 `export default function EducationPage()` 위쪽 helper 함수 5개 (calcNextDeadline / fmtDate / dateToYmd / titleRank / SKELETON_STYLE)
- line 420~451 메인 state + useQuery + canEdit + adminList + assistantList + renderCards
- line 461~498 renderGroupedList (로딩 / 오류 / 빈 / 그룹 렌더)
- line 501~534 데스크톱 분기 (2 분할 레이아웃)
- line 537~590 모바일 분기 (자체 헤더 + 스크롤 영역 + 바텀시트 + style)

### EducationEditPanel (line 190~390)
- line 196~210 state (completedAt / educationType / editingRecord) + useQueryClient
- line 212~244 useMutation × 3 (★ 비즈 anchor 6)
- line 246~264 handler 3개 (handleStartEdit / handleCancelEdit / handleSubmit)
- line 266~271 inputStyle 상수 (className 분리 시 제거 옵션)
- line 273~389 render

### StaffEducationCard (line 96~187) + DdayBadge (line 61~93) + EducationBottomSheet (line 393~417)
- 모두 props 시그니처 + 내부 로직 verbatim

### 카피 verbatim (전체)
- '교육 이력 없음' / '이수일을 기록하면 다음 교육 마감일이 자동으로 계산됩니다.' / '교육 현황을 불러오지 못했습니다. 화면을 당겨서 새로고침하세요.' / '좌측에서 직원을 선택하세요' / '보수교육' / '소방안전관리자' / '소방안전관리 보조자' / '이수일이 기록되었습니다.' / '이수일이 수정되었습니다.' / '이수 기록이 삭제되었습니다.' / '이수 기록 저장에 실패했습니다.' / '삭제에 실패했습니다.' / '실무교육 (최초)' / '선임일 미등록' / '첫 실무교육' / '다음 마감' / '이수 이력' / '이수일' / '교육 유형' / '이수일 기록' / '수정 완료' / '저장 중...' / '수정' / '취소' / '삭제'

## 5. 작업 순서

1. `wc -l cha-bio-safety/src/pages/EducationPage.tsx` 으로 라인 수 591 확인
2. EducationPage.tsx 한 번 Read 로 전체 591 라인 읽기 (필요 시 offset/limit 분할 — 591 lines 는 한 번에 가능)
3. App.tsx baseline hash 저장 (`git rev-parse HEAD:cha-bio-safety/src/App.tsx > /tmp/app_tsx_hash_before.txt`)
4. Edit / Write 로 in-place 수정 — 순서:
   - **§3.1 영역 1 (line 1~58)** — imports 추가 (lucide-react) + IconChevronLeft 함수 정의 line 12~18 제거 + SKELETON_STYLE 결정 (옵션 A 유지 또는 옵션 B 제거)
   - **§3.2 영역 2 (DdayBadge, line 61~93)** — OQ #2 LOCKED 3 토큰 매트릭스 매핑 (★ 임계치 30·0 + 라벨 verbatim)
   - **§3.3 영역 3 (StaffEducationCard, line 96~187)** — OQ #3 LOCKED selected border-2 + 4개 메타 분기
   - **§3.4 영역 4 (EducationEditPanel, line 190~390)** — OQ #3 LOCKED submit 그라데이션 + disabled 토큰 + 14 영역 매핑
   - **§3.5 영역 5 (EducationBottomSheet, line 393~417)** — bg-surface-raised rounded-t-lg + slideUp 인라인 anchor
   - **§3.6 영역 6 (EducationPage 메인, line 419~590)** — OQ #1 LOCKED 모바일 헤더 + OQ #4 LOCKED 빈/오류 카피 + OQ #5 LOCKED Lucide ChevronLeft 교체 + 데스크톱/모바일 분기
5. `cd cha-bio-safety && npx tsc --noEmit` 실행 → 0 errors 확인
6. `cd cha-bio-safety && npm run build` 실행 → tsc 0 + Vite build PASS 확인
7. §5 negative gate + §6 positive gate + §3 비즈 anchor 12건 + §scope 모두 PASS 확인
8. atomic 1-commit (단일 파일 + PLAN.md/SUMMARY.md):
   ```bash
   git add cha-bio-safety/src/pages/EducationPage.tsx .planning/quick/260522-r22-redesign-23-education-tsx-educationpage-/
   git commit -m "$(cat <<'EOF'
feat(quick-260522-r22): redesign/23-education TSX 변환 (EducationPage.tsx 단일 atomic + v0.1.1 토큰 className 매핑 + 비즈 anchor 12건 보존 + Lucide ChevronLeft 교체 + OQ LOCKED 5건 반영 + W5 §3 매핑 verbatim)
EOF
)"
   ```

## 6. 금지 사항

- wrangler 명령 0건 (CLAUDE.local.md 강제, 디자인 워크트리)
- npm run deploy 0건 (직원 도메인 cbc7119 경로)
- EducationPage.tsx 외 src 파일 수정 0건 (App.tsx / tailwind.config.js / tokens.css / typography.css / hooks/useIsDesktop.ts / stores/authStore.ts / utils/api.ts / types/ / functions/ / templates/ / migrations/ / public/ 모두 무영향)
- **★ App.tsx 변경 0 byte** — `git diff --name-only HEAD~ HEAD -- cha-bio-safety/src/App.tsx` 빈 출력 (final verify gate)
- `bg-status-safe-bg` / `text-status-warning` / `text-status-danger` 등 status- prefix className 0건 (memory `feedback_tailwind_token_class_pattern`)
- `w-8` / `h-8` 토큰 사용 0건 (=48px 함정, memory `feedback_tailwind_w8_h8_is_48px`)
- `var(--bg|bg2|bg3|bd|bd2|t1|t2|t3|acl|safe|warn|danger)` 잔존 0건 (TSX 본문 안 인라인 style 안에서 단계적 제거 마무리)
- `linear-gradient` 일반 0건 **단**, `linear-gradient(135deg, #1d4ed8, #0ea5e9)` (submit, OQ #3 LOCKED) ≥1 예외 anchor
- fontSize 9·10·11 인라인 0건 (OQ #3 LOCKED 격상)
- 이모지 0건 — TSX 본문 emoji range `[😀-🛿🤀-🧿]` (sketch + 본문 모두 이모지 없음)
- IconChevronLeft 함수 정의 + 사용처 0건 (OQ #5 LOCKED 완전 제거)
- polyline SVG 0건 (OQ #5 LOCKED IconChevronLeft 제거 부수효과)
- 비즈 로직 (state/handler/useMutation/useQuery/useEffect/D-day 분기/canEdit/toast/snake_case payload/timezone) 변경 0건
- **★ 비즈 anchor 12건 1 byte 변경 0건** — 17-annual-plan SW3 + 28-splash 4i9 precedent
- App.tsx 0 byte 변경 (final verify gate)

  </action>
  <verify>
    <automated>
# Path 변수
E="cha-bio-safety/src/pages/EducationPage.tsx"
APP="cha-bio-safety/src/App.tsx"

# ─── Negative gate (W5 §5 verbatim) ───
echo "=== Negative gate (EducationPage) ==="
# emoji range 검사 (⋮ ⎋ 같은 OS 시그니처는 23-education 에 없음)
test "$(grep -oP '[\x{1F300}-\x{1FAFF}\x{2600}-\x{26FF}]' "$E" 2>/dev/null | wc -l | tr -d ' ')" = "0" && echo "neg-E1 (emoji) PASS" || echo "neg-E1 FAIL"
# linear-gradient = 정확히 1 (OQ #3 submit 예외)
LG_TOTAL=$(grep -c 'linear-gradient' "$E")
LG_SUBMIT=$(grep -c 'linear-gradient(135deg, #1d4ed8, #0ea5e9)' "$E")
test "$LG_TOTAL" = "$LG_SUBMIT" && test "$LG_SUBMIT" -ge 1 && echo "neg-E2 (linear-gradient only OQ #3 submit, count=$LG_SUBMIT) PASS" || echo "neg-E2 FAIL (total=$LG_TOTAL, submit=$LG_SUBMIT)"
# fontSize 9·10·11 인라인 0
test "$(grep -v '^\s*//' "$E" | grep -cE 'fontSize:\s*(9|10|11)[^0-9px]|font-size:\s*(9|10|11)[^0-9px]')" = "0" && echo "neg-E3 (no 9·10·11 fontSize) PASS" || echo "neg-E3 FAIL"
# status- prefix 0
test "$(grep -cE '\b(text|bg|border)-status-(safe|fire|warning|danger|caution|accent)' "$E")" = "0" && echo "neg-E4 (no status- prefix) PASS" || echo "neg-E4 FAIL"
# w-8 / h-8 0 (=48px 함정)
test "$(grep -cE '\bw-8\b|\bh-8\b' "$E")" = "0" && echo "neg-E5 (no w-8/h-8) PASS" || echo "neg-E5 FAIL"
# 옛 alias var(--bg|bg2|bg3|bd|bd2|t1|t2|t3|acl|safe|warn|danger) 0
test "$(grep -cE 'var\(--(bg|bg2|bg3|bg4|bd|bd2|t1|t2|t3|acl|safe|warn|danger)\)' "$E")" = "0" && echo "neg-E6 (no old alias) PASS" || echo "neg-E6 FAIL"
# IconChevronLeft 0 (OQ #5 완전 제거)
test "$(grep -c 'IconChevronLeft' "$E")" = "0" && echo "neg-E7 (no IconChevronLeft, OQ #5) PASS" || echo "neg-E7 FAIL"
# polyline 0 (OQ #5 SVG 제거)
test "$(grep -c 'polyline' "$E")" = "0" && echo "neg-E8 (no polyline SVG, OQ #5) PASS" || echo "neg-E8 FAIL"

# ─── Positive gate (W5 §6 verbatim) ───
echo "=== Positive gate (EducationPage) ==="
# v0.1.1 토큰 다수
test "$(grep -cE 'bg-surface-(page|raised|sunken)|border-border-(default|strong)|text-text-(primary|secondary|tertiary|disabled|on-accent)' "$E")" -ge 10 && echo "pos-E1 (v0.1.1 tokens ≥10) PASS" || echo "pos-E1 FAIL"
# OQ #1 — 모바일 헤더
test "$(grep -c 'bg-surface-raised border-b border-border-default' "$E")" -ge 1 && echo "pos-E2 (OQ #1 모바일 헤더) PASS" || echo "pos-E2 FAIL"
# OQ #2 — DdayBadge 3 토큰
test "$(grep -c 'bg-safe-bg text-safe' "$E")" -ge 1 && echo "pos-E3 (OQ #2 safe) PASS" || echo "pos-E3 FAIL"
test "$(grep -c 'bg-warning-bg text-warning' "$E")" -ge 1 && echo "pos-E4 (OQ #2 warning) PASS" || echo "pos-E4 FAIL"
test "$(grep -c 'bg-danger-bg text-danger' "$E")" -ge 1 && echo "pos-E5 (OQ #2 danger) PASS" || echo "pos-E5 FAIL"
# OQ #3 — submit + disabled + selected
test "$(grep -c 'linear-gradient(135deg, #1d4ed8, #0ea5e9)' "$E")" -ge 1 && echo "pos-E6 (OQ #3 submit 그라데이션) PASS" || echo "pos-E6 FAIL"
test "$(grep -c 'bg-surface-sunken text-text-tertiary cursor-not-allowed' "$E")" -ge 1 && echo "pos-E7 (OQ #3 select disabled) PASS" || echo "pos-E7 FAIL"
test "$(grep -c 'border-2 border-accent' "$E")" -ge 1 && echo "pos-E8 (OQ #3 selected card) PASS" || echo "pos-E8 FAIL"
# OQ #4 — 빈/오류 카피 4건 verbatim
test "$(grep -c '교육 이력 없음' "$E")" -ge 1 && echo "pos-E9 (OQ #4 빈 제목) PASS" || echo "pos-E9 FAIL"
test "$(grep -c '이수일을 기록하면 다음 교육 마감일이 자동으로 계산됩니다' "$E")" -ge 1 && echo "pos-E10 (OQ #4 빈 보조) PASS" || echo "pos-E10 FAIL"
test "$(grep -c '교육 현황을 불러오지 못했습니다. 화면을 당겨서 새로고침하세요' "$E")" -ge 1 && echo "pos-E11 (OQ #4 오류) PASS" || echo "pos-E11 FAIL"
test "$(grep -c '좌측에서 직원을 선택하세요' "$E")" -ge 1 && echo "pos-E12 (OQ #4 데스크톱 fallback) PASS" || echo "pos-E12 FAIL"
# OQ #5 — Lucide ChevronLeft
test "$(grep -c "import { ChevronLeft } from 'lucide-react'" "$E")" -ge 1 && echo "pos-E13 (OQ #5 lucide import) PASS" || echo "pos-E13 FAIL"
test "$(grep -c '<ChevronLeft size={20}' "$E")" -ge 1 && echo "pos-E14 (OQ #5 ChevronLeft usage) PASS" || echo "pos-E14 FAIL"

# v0.1.1 토큰 카운트 (W5 §6 sampling)
test "$(grep -c 'bg-surface-page' "$E")" -ge 2 && echo "pos-E15 (bg-surface-page 모바일+데스크톱 외곽) PASS" || echo "pos-E15 FAIL"
test "$(grep -c 'bg-surface-raised' "$E")" -ge 2 && echo "pos-E16 (bg-surface-raised 카드+헤더+바텀시트) PASS" || echo "pos-E16 FAIL"
test "$(grep -c 'bg-surface-sunken' "$E")" -ge 4 && echo "pos-E17 (bg-surface-sunken 아바타+마감박스+input+이수이력+SKELETON) PASS" || echo "pos-E17 FAIL"
test "$(grep -c 'text-text-primary' "$E")" -ge 3 && echo "pos-E18 (text-text-primary 이름+타이틀+일자) PASS" || echo "pos-E18 FAIL"
test "$(grep -c 'text-text-secondary' "$E")" -ge 5 && echo "pos-E19 (text-text-secondary 직책+back+sectionLabel+폼라벨+빈제목) PASS" || echo "pos-E19 FAIL"
test "$(grep -c 'text-text-tertiary' "$E")" -ge 5 && echo "pos-E20 (text-text-tertiary 메타+fallback+빈보조+삭제+마감박스+폼라벨) PASS" || echo "pos-E20 FAIL"
test "$(grep -c 'text-caption' "$E")" -ge 5 && echo "pos-E21 (text-caption ≥5 DdayBadge+메타+폼라벨+액션버튼+마감박스) PASS" || echo "pos-E21 FAIL"
test "$(grep -c 'text-label' "$E")" -ge 3 && echo "pos-E22 (text-label ≥3 직책+폼제목+이수이력) PASS" || echo "pos-E22 FAIL"
test "$(grep -c 'leading-none' "$E")" -ge 3 && echo "pos-E23 (leading-none ≥3 DdayBadge+폼라벨+액션버튼) PASS" || echo "pos-E23 FAIL"
test "$(grep -c 'rounded-full' "$E")" -ge 2 && echo "pos-E24 (rounded-full 아바타 32+40) PASS" || echo "pos-E24 FAIL"
test "$(grep -c 'rounded-md' "$E")" -ge 3 && echo "pos-E25 (rounded-md 카드+마감박스+input+submit) PASS" || echo "pos-E25 FAIL"
test "$(grep -c 'rounded-sm' "$E")" -ge 3 && echo "pos-E26 (rounded-sm DdayBadge+이수이력row+액션버튼+handle) PASS" || echo "pos-E26 FAIL"
test "$(grep -cE 'rounded-t-(lg|\[16px\])' "$E")" -ge 1 && echo "pos-E27 (rounded-t 바텀시트) PASS" || echo "pos-E27 FAIL"

# ─── ★ 비즈 anchor 12건 보존 ───
echo "=== Biz anchor (EducationPage 12) ==="
test "$(grep -c 'calcNextDeadline' "$E")" -ge 2 && echo "anchor-E1 (calcNextDeadline 정의+사용) PASS" || echo "anchor-E1 FAIL"
test "$(grep -c 'addMonths(parseISO(appointedAt), 6)' "$E")" -ge 1 && echo "anchor-E2 (addMonths 첫 실무 6개월) PASS" || echo "anchor-E2 FAIL"
test "$(grep -c 'addYears(parseISO(sorted\[0\].completedAt), 2)' "$E")" -ge 1 && echo "anchor-E3 (addYears 보수 2년) PASS" || echo "anchor-E3 FAIL"
test "$(grep -c 'differenceInCalendarDays(d, new Date())' "$E")" -ge 2 && echo "anchor-E4 (differenceInCalendarDays 2번) PASS" || echo "anchor-E4 FAIL"
test "$(grep -cE "'주임':\s*0,\s*'대리':\s*1,\s*'기사':\s*2" "$E")" -ge 1 && echo "anchor-E5 (TITLE_ORDER) PASS" || echo "anchor-E5 FAIL"
test "$(grep -c 'TITLE_ORDER\[title\] ?? 99' "$E")" -ge 1 && echo "anchor-E6 (titleRank fallback 99) PASS" || echo "anchor-E6 FAIL"
test "$(grep -c 'dday > 30' "$E")" -ge 1 && echo "anchor-E7 (D-day 임계치 30) PASS" || echo "anchor-E7 FAIL"
test "$(grep -c 'dday >= 0' "$E")" -ge 1 && echo "anchor-E8 (D-day 임계치 0) PASS" || echo "anchor-E8 FAIL"
test "$(grep -c 'D-${dday}' "$E")" -ge 2 && echo "anchor-E9 (D-day 라벨 safe+warning) PASS" || echo "anchor-E9 FAIL"
test "$(grep -c 'D+${Math.abs(dday)} 초과' "$E")" -ge 1 && echo "anchor-E10 (D-day 라벨 danger) PASS" || echo "anchor-E10 FAIL"
test "$(grep -c "queryKey: \['education'\]" "$E")" -ge 4 && echo "anchor-E11 (useQuery + 3 invalidate) PASS" || echo "anchor-E11 FAIL"
test "$(grep -c 'educationApi.create' "$E")" -ge 1 && echo "anchor-E12 (educationApi.create) PASS" || echo "anchor-E12 FAIL"
test "$(grep -c 'educationApi.update' "$E")" -ge 1 && echo "anchor-E13 (educationApi.update) PASS" || echo "anchor-E13 FAIL"
test "$(grep -c 'educationApi.delete' "$E")" -ge 1 && echo "anchor-E14 (educationApi.delete) PASS" || echo "anchor-E14 FAIL"
test "$(grep -c 'education_type:' "$E")" -ge 1 && echo "anchor-E15 (snake_case education_type) PASS" || echo "anchor-E15 FAIL"
test "$(grep -c 'completed_at:' "$E")" -ge 2 && echo "anchor-E16 (snake_case completed_at create+update) PASS" || echo "anchor-E16 FAIL"
test "$(grep -c 'useIsDesktop()' "$E")" -ge 1 && echo "anchor-E17 (useIsDesktop) PASS" || echo "anchor-E17 FAIL"
test "$(grep -c "currentStaff?.role === 'admin' || currentStaff.id === cardStaffId" "$E")" -ge 1 && echo "anchor-E18 (canEdit 시그니처) PASS" || echo "anchor-E18 FAIL"
test "$(grep -c '@keyframes blink' "$E")" -ge 1 && echo "anchor-E19 (@keyframes blink) PASS" || echo "anchor-E19 FAIL"
test "$(grep -c 'slideUp 0.28s ease-out both' "$E")" -ge 1 && echo "anchor-E20 (slideUp animation) PASS" || echo "anchor-E20 FAIL"
test "$(grep -c '이수일이 기록되었습니다' "$E")" -ge 1 && echo "anchor-E21 (toast create.success) PASS" || echo "anchor-E21 FAIL"
test "$(grep -c '이수일이 수정되었습니다' "$E")" -ge 1 && echo "anchor-E22 (toast update.success) PASS" || echo "anchor-E22 FAIL"
test "$(grep -c '이수 기록이 삭제되었습니다' "$E")" -ge 1 && echo "anchor-E23 (toast delete.success) PASS" || echo "anchor-E23 FAIL"
test "$(grep -c '이수 기록 저장에 실패했습니다' "$E")" -ge 2 && echo "anchor-E24 (toast create+update.error 2번) PASS" || echo "anchor-E24 FAIL"
test "$(grep -cE "e\?\.message \?\? '삭제에 실패했습니다\.'" "$E")" -ge 1 && echo "anchor-E25 (toast delete.error) PASS" || echo "anchor-E25 FAIL"

# ─── 카피 verbatim (그룹 라벨 + 메인 카피) ───
echo "=== Copy verbatim (EducationPage) ==="
test "$(grep -c '교육 이력 없음' "$E")" -ge 1 && echo "copy-E1 (빈 제목) PASS" || echo "copy-E1 FAIL"
test "$(grep -c '이수일을 기록하면 다음 교육 마감일이 자동으로 계산됩니다' "$E")" -ge 1 && echo "copy-E2 (빈 보조) PASS" || echo "copy-E2 FAIL"
test "$(grep -c '교육 현황을 불러오지 못했습니다. 화면을 당겨서 새로고침하세요' "$E")" -ge 1 && echo "copy-E3 (오류) PASS" || echo "copy-E3 FAIL"
test "$(grep -c '좌측에서 직원을 선택하세요' "$E")" -ge 1 && echo "copy-E4 (데스크톱 fallback) PASS" || echo "copy-E4 FAIL"
test "$(grep -c '보수교육' "$E")" -ge 2 && echo "copy-E5 (보수교육 모바일타이틀+select option) PASS" || echo "copy-E5 FAIL"
test "$(grep -c '소방안전관리자' "$E")" -ge 1 && echo "copy-E6 (소방안전관리자 그룹) PASS" || echo "copy-E6 FAIL"
test "$(grep -c '소방안전관리 보조자' "$E")" -ge 1 && echo "copy-E7 (소방안전관리 보조자 그룹) PASS" || echo "copy-E7 FAIL"
test "$(grep -c '실무교육 (최초)' "$E")" -ge 1 && echo "copy-E8 (실무교육 (최초) select option) PASS" || echo "copy-E8 FAIL"

# ─── App.tsx 0 byte 변경 (final verify gate) ───
echo "=== App.tsx preserve ==="
# 미커밋 시점: working tree 비교 (git diff HEAD)
test "$(git diff HEAD -- "$APP" 2>/dev/null | wc -l | tr -d ' ')" = "0" && echo "app-1 (App.tsx working tree 0 diff) PASS" || echo "app-1 FAIL"
# 커밋 후 시점: HEAD~ HEAD 비교 (재실행 시점 가드)
test "$(git diff HEAD~ HEAD -- "$APP" 2>/dev/null | wc -l | tr -d ' ')" = "0" && echo "app-2 (App.tsx HEAD~ HEAD 0 diff) PASS" || echo "app-2 (커밋 전 — skip)"

# ─── Scope (정확히 1 파일 + PLAN/SUMMARY 만) ───
echo "=== Scope ==="
EXTRA=$(git diff --name-only HEAD~ HEAD 2>/dev/null | grep -vE 'cha-bio-safety/src/pages/EducationPage\.tsx|^\.planning/quick/260522-r22-')
test -z "$EXTRA" && echo "scope (only EducationPage.tsx + PLAN/SUMMARY) PASS" || echo "scope FAIL — extra: $EXTRA"

# ─── Line count sanity ───
echo "=== Line count ==="
LCE=$(wc -l < "$E" | tr -d ' ')
test "$LCE" -ge 560 -a "$LCE" -le 620 && echo "lc-E ($LCE lines, 591 ± 30) PASS" || echo "lc-E FAIL ($LCE lines)"

# ─── Build gate ───
echo "=== Build ==="
(cd cha-bio-safety && npx tsc --noEmit 2>&1 | tail -5)
(cd cha-bio-safety && npm run build 2>&1 | tail -5)
echo "=== EducationPage chunk size ==="
ls -la cha-bio-safety/dist/assets/EducationPage-*.js 2>/dev/null || echo "(EducationPage chunk — lazy chunk 보고)"
    </automated>
  </verify>
  <done>
- EducationPage.tsx 단일 파일 in-place 수정 완료. 라인 수 591 ± 30 (560~620).
- Negative gate 8건 모두 PASS: emoji 0 / linear-gradient = 1 (OQ #3 submit 만) / 9·10·11 fontSize 0 / status- prefix 0 / w-8·h-8 0 / 옛 alias 0 / IconChevronLeft 0 / polyline 0
- Positive gate 27건 모두 PASS — v0.1.1 토큰 다수 + OQ #1 모바일 헤더 + OQ #2 3 토큰 + OQ #3 submit 그라데이션 + disabled + selected + OQ #4 빈/오류 4건 카피 + OQ #5 Lucide ChevronLeft import + 사용 + 폰트 토큰 + radius 토큰
- ★ 비즈 anchor 25건 (Anchor 12개 카테고리 × 평균 2 grep 분해 = 25건) 모두 PASS: calcNextDeadline / addMonths(6) / addYears(2) / differenceInCalendarDays×2 / TITLE_ORDER / titleRank / D-day 임계치 30+0 / D-day 라벨 verbatim×2 / useQuery ['education']×4 / educationApi.create+update+delete / snake_case education_type+completed_at / useIsDesktop / canEdit / @keyframes blink / slideUp / toast 카피 5종×6 grep
- 카피 verbatim 8건 모두 PASS: 빈 제목 / 빈 보조 / 오류 / 데스크톱 fallback / 보수교육×2 / 소방안전관리자 / 소방안전관리 보조자 / 실무교육 (최초)
- App.tsx 0 byte 변경 (app-1 PASS working tree, app-2 PASS 커밋 후)
- Scope: cha-bio-safety/src/pages/EducationPage.tsx + .planning/quick/260522-r22-*/ 외 파일 변경 0건
- `cd cha-bio-safety && npx tsc --noEmit` 0 errors, `cd cha-bio-safety && npm run build` exit 0, EducationPage chunk size 보고
- Atomic 1-commit (단일 파일 + PLAN/SUMMARY 묶음): `feat(quick-260522-r22): redesign/23-education TSX 변환 (EducationPage.tsx 단일 atomic + v0.1.1 토큰 className 매핑 + 비즈 anchor 12건 보존 + Lucide ChevronLeft 교체 + OQ LOCKED 5건 반영 + W5 §3 매핑 verbatim)`
  </done>
</task>

</tasks>

<verification>
## 전체 PLAN 통과 조건

1. **Negative gate (8건)** — emoji 0 / linear-gradient = 정확히 1 (OQ #3 submit 만, total == submit) / 9·10·11 fontSize 0 / status- prefix 0 / w-8·h-8 0 / 옛 alias var(--bg|bg2|bg3|bg4|bd|bd2|t1|t2|t3|acl|safe|warn|danger) 0 / IconChevronLeft 0 / polyline 0
2. **Positive gate (27건)** — v0.1.1 토큰 ≥10 / bg-surface-page+raised+sunken / text-text-primary+secondary+tertiary / text-caption+text-label / leading-none / rounded-full+md+sm+t-lg / OQ #1 bg-surface-raised border-b border-border-default / OQ #2 bg-safe-bg+text-safe + bg-warning-bg+text-warning + bg-danger-bg+text-danger / OQ #3 linear-gradient(135deg, #1d4ed8, #0ea5e9) + bg-surface-sunken text-text-tertiary cursor-not-allowed + border-2 border-accent / OQ #4 빈/오류 4건 카피 / OQ #5 import ChevronLeft from lucide-react + <ChevronLeft size={20}
3. **★ 비즈 anchor 12 카테고리 (25 grep 분해)** — calcNextDeadline / addMonths(6) + addYears(2) + differenceInCalendarDays / TITLE_ORDER + titleRank / D-day 임계치 30+0 + 라벨 / useQuery ['education']×4 / useMutation × 3 + educationApi 3종 + snake_case payload / useIsDesktop / canEdit / @keyframes blink + slideUp / toast 카피 5종 / 빈/오류/fallback 카피 verbatim
4. **카피 verbatim (8건)** — '교육 이력 없음' / '이수일을 기록하면 다음 교육 마감일이 자동으로 계산됩니다.' / '교육 현황을 불러오지 못했습니다. 화면을 당겨서 새로고침하세요.' / '좌측에서 직원을 선택하세요' / '보수교육' (모바일 타이틀 + select option) / '소방안전관리자' / '소방안전관리 보조자' / '실무교육 (최초)'
5. **App.tsx 0 byte 변경** (final verify gate) — git diff 0 lines (working tree + 커밋 후 모두)
6. **Scope** — 1 파일 변경 (EducationPage.tsx) + PLAN.md/SUMMARY.md
7. **Build** — `npx tsc --noEmit` 0 errors + `npm run build` exit 0
8. **Atomic 1-commit** — 단일 파일 + PLAN/SUMMARY 묶음 단일 commit
9. **Line count** — 591 ± 30 (560~620)

## 검수 시각 (cbc7119-preview 자동 배포 후, 사용자 컨펌 필수)

- **모바일 viewport (375x812)**:
  - 외곽 `bg-surface-page` (#0d1117 톤)
  - **★ 모바일 자체 헤더 (OQ #1)** = `bg-surface-raised border-b border-border-default` (#1c2128 + #30363d 보더) + height 48 + back button `<ChevronLeft size={20} />` text-text-secondary + 타이틀 "보수교육" `text-body font-bold text-text-primary` center + spacer 44
  - 빈 상태 (OQ #4): "교육 이력 없음" `text-body font-bold text-text-secondary` + "이수일을 기록하면 다음 교육 마감일이 자동으로 계산됩니다." `text-body-sm text-text-tertiary` (아이콘 무)
  - 오류 상태 (OQ #4): "교육 현황을 불러오지 못했습니다. 화면을 당겨서 새로고침하세요." `text-body-sm text-danger`
  - 로딩 SKELETON 4개: `bg-surface-sunken rounded-md` + height 88 + animation blink 깜빡임
  - 그룹 라벨: "소방안전관리자" / "소방안전관리 보조자" `text-label font-bold text-text-secondary`
  - **★ StaffEducationCard**: `bg-surface-raised rounded-md border border-border-default` + 아바타 32x32 `bg-surface-sunken text-text-secondary rounded-full` + 이름 `text-body font-bold text-text-primary` + 직책 `text-label text-text-secondary` + DdayBadge
  - **★ DdayBadge (OQ #2)**: 3 임계치 → safe `bg-safe-bg text-safe` (>30, 'D-365' 같은 안전 라벨) / warning `bg-warning-bg text-warning` (0~30, 'D-15' 같은 임박 라벨) / danger `bg-danger-bg text-danger` (<0, 'D+5 초과' 같은 초과 라벨) + `text-caption font-bold leading-none rounded-sm`
- **모바일 바텀시트 (직원 카드 tap, canEdit true)**:
  - 오버레이 `rgba(0,0,0,0.6)` 인라인
  - 시트 `bg-surface-raised rounded-t-lg` + animation slideUp 0.28s 등장 + maxHeight 90vh + handle 32x4 `bg-border-strong rounded-sm`
  - 프로필 헤더: 아바타 40x40 + 이름 18 `text-title font-extrabold` + 직책 13 `text-label` + DdayBadge
  - 마감 정보 박스: `bg-surface-sunken rounded-md` + 라벨 "다음 마감" 12 `text-caption leading-none text-text-tertiary` + 일자 14 `text-body-sm font-bold text-text-primary` + (label) 12 `text-caption leading-none text-text-tertiary`
  - 이수 이력: row `bg-surface-sunken rounded-sm` + 액션 버튼 "수정"/"삭제" `bg-surface-raised border border-border-strong text-caption leading-none rounded-sm`
  - 폼: borderTop `border-t border-border-default` + 라벨 "이수일"/"교육 유형" `text-caption leading-none font-bold text-text-tertiary` + input/select `bg-surface-sunken border border-border-strong text-label text-text-primary rounded-md`
  - **★ select disabled (OQ #3)**: hasRecords && !isEditMode == false 분기 → `bg-surface-sunken text-text-tertiary cursor-not-allowed`
  - **★ submit (OQ #3)**: `text-text-on-accent text-body font-bold rounded-md` + 인라인 `background: 'linear-gradient(135deg, #1d4ed8, #0ea5e9)'` + height 44 + opacity isSubmitting 분기
- **데스크톱 viewport (1280+)**:
  - 외곽 `bg-surface-page` + display flex
  - 좌측 `border-r border-border-default` + 카드 그리드 + **★ selected card (OQ #3)**: `border-2 border-accent` (1.5→2 격상, 데스크톱만)
  - 우측 fallback: "좌측에서 직원을 선택하세요" `text-body-sm text-text-tertiary`
  - 우측 EditPanel (item selected): 바텀시트와 동일 레이아웃 (모달 X, inline 표시)
- **공통**:
  - admin 권한: 모든 카드 cursor pointer + 클릭 시 바텀시트/EditPanel 열림
  - assistant 권한: 본인 카드만 cursor pointer + 본인 외 카드 cursor default + 클릭 시 EditPanel 열리되 폼 영역 표시 X (canEdit false)
  - D-day 산출: 윤종엽 (assistant, 본인 = canEdit true) appointedAt + 최근 이수일 기반
  - toast 5종 동작: 등록 → '이수일이 기록되었습니다.' / 수정 → '이수일이 수정되었습니다.' / 삭제 → '이수 기록이 삭제되었습니다.' / 저장 오류 → '이수 기록 저장에 실패했습니다.' / 삭제 오류 → '삭제에 실패했습니다.'

## 사용자 컨펌 후 다음 단계

- `main` 머지 (사용자 명시 컨펌 후, memory `feedback_deploy_test` — redesign/feature 브랜치는 사용자 컨펌 후 main 머지+배포)
- GitHub Actions → cbc7119-preview 자동 배포 (메모리 `project_cbc7119_design_repo` + `reference_cbc7119_domain` + `feedback_cbc7119_design_never_wrangler`)
- 직원 도메인 (cbc7119) 배포는 별도 worktree (20260328) 담당 (이 워크트리는 절대 다루지 않음, CLAUDE.local.md)
- `project_redesign_23_education_status` 메모리 박제 (신규) — W1 인덱스 + W2~W5 통합 + R22 TSX 변환 모두 main 머지+배포 완결 status
</verification>

<success_criteria>
- [ ] EducationPage.tsx 591 ± 30 lines (560~620), in-place 수정
- [ ] Negative gate 8건 모두 PASS (emoji / linear-gradient OQ #3 예외 / 9·10·11 fontSize / status- prefix / w-8·h-8 / 옛 alias / IconChevronLeft / polyline 0건)
- [ ] Positive gate 27건 모두 임계치 이상 (v0.1.1 토큰 + OQ #1~#5 grep + 폰트 + radius)
- [ ] ★ 비즈 anchor 12 카테고리 (25 grep 분해) 모두 PASS
- [ ] 카피 verbatim 8건 모두 PASS
- [ ] App.tsx 0 byte 변경 (working tree + 커밋 후 모두)
- [ ] 다른 파일 변경 0건 (tailwind.config.js / tokens.css / typography.css / hooks/ / stores/ / utils/api.ts / types/ / functions/ / templates/ / migrations/ / public/ 모두 무영향)
- [ ] `cd cha-bio-safety && npx tsc --noEmit` 0 errors
- [ ] `cd cha-bio-safety && npm run build` exit 0
- [ ] Atomic 1-commit (단일 파일 + PLAN/SUMMARY 묶음)
- [ ] OQ #1: `bg-surface-raised border-b border-border-default` ≥1
- [ ] OQ #2: `bg-safe-bg text-safe` ≥1 + `bg-warning-bg text-warning` ≥1 + `bg-danger-bg text-danger` ≥1
- [ ] OQ #3: `linear-gradient(135deg, #1d4ed8, #0ea5e9)` ≥1 + `bg-surface-sunken text-text-tertiary cursor-not-allowed` ≥1 + `border-2 border-accent` ≥1
- [ ] OQ #4: 빈/오류 카피 4건 모두 verbatim grep ≥1
- [ ] OQ #5: `import { ChevronLeft } from 'lucide-react'` ≥1 + `<ChevronLeft size={20}` ≥1 + `IconChevronLeft` 0 + `polyline` 0
</success_criteria>

<output>
After completion, create `.planning/quick/260522-r22-redesign-23-education-tsx-educationpage-/260522-r22-SUMMARY.md` documenting:

- 변환 전후 EducationPage.tsx wc -l (591 → 변환 후 라인 수)
- 적용된 v0.1.1 토큰 className 목록 (grep 카운트 — bg-surface-*/border-border-*/text-text-*/text-caption+label+body+title+heading/font-*/rounded-*/leading-*)
- OQ #1~#5 LOCKED 각 적용 결과 확인:
  - OQ #1: 모바일 헤더 bg-surface-raised border-b border-border-default ≥1
  - OQ #2: DdayBadge 3 토큰 매트릭스 (safe/warning/danger) + 임계치 30·0 + 라벨 verbatim
  - OQ #3: submit 그라데이션 + disabled 토큰 + selected border-2 grep PASS
  - OQ #4: 빈/오류 카피 4건 verbatim grep PASS
  - OQ #5: Lucide ChevronLeft 교체 + IconChevronLeft 0 + polyline 0 검증
- ★ 비즈 anchor 12 카테고리 (25 grep 분해) 1 byte 변경 0 검증 결과 (17-annual-plan SW3 + 28-splash 4i9 precedent mirror)
- Negative gate (8) + Positive gate (27) + Biz anchor (25) + Copy (8) + App preserve (2) + Scope + LC + Build PASS 결과 (verify automated 출력 첨부)
- `npm run build` 결과 (EducationPage chunk size 포함)
- atomic commit hash + main 머지 대기 상태 명시
- 다음 단계 (사용자 컨펌 → main 머지 → cbc7119-preview 자동 배포 → `project_redesign_23_education_status` 메모리 박제 신규)
</output>
</content>
</invoke>