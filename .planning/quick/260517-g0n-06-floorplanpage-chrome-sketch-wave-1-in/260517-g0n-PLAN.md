---
phase: 260517-g0n
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cha-bio-safety/docs/redesign-context/06-floorplan/sketch/floorplan-modals-sketch.html
autonomous: true
requirements:
  - 06-modal-sketch-wave-1
must_haves:
  truths:
    - "floorplan-modals-sketch.html 파일이 06-floorplan/sketch/ 하위에 존재한다"
    - "3 모달(마커 선택 정보 popup / inspectModal / resolveModal) 모두 시각화된다"
    - "v0.1.1 토큰만 사용하고 옛 alias(--bg2/--bg3/--bd/--bd2/--t1~3/--acl/--warn/--danger) 0건"
    - "linear-gradient 0건 — CTA 는 단색 bg-accent"
    - "이모지 ✕ 폐기 — lucide X 아이콘 사용"
    - "점검 결과 3택은 02 inspection / 05 remediation 패턴 mirror (border-[1.5px] + status 토큰 페어 + lucide CheckCircle2/AlertTriangle/XCircle)"
    - "모달 wrapper 일관성: bg-surface-raised border border-border-default rounded-md p-5"
  artifacts:
    - path: "cha-bio-safety/docs/redesign-context/06-floorplan/sketch/floorplan-modals-sketch.html"
      provides: "06 FloorPlanPage Wave 1 모달 chrome 통일 시안 (3 모달 × 2 viewport 최소)"
      contains: "bg-surface-raised, border border-border-default, border-[1.5px] border-accent, border-[1.5px] border-safe, border-[1.5px] border-warning, border-[1.5px] border-danger, bg-accent text-text-on-accent, data-lucide=\"x\", data-lucide=\"check-circle\", data-lucide=\"alert-triangle\", data-lucide=\"x-circle\""
      min_lines: 800
  key_links:
    - from: "floorplan-modals-sketch.html"
      to: "inspection-modal-chrome-rules.md §6.2 (input/textarea bg-surface-raised), §5 (선택/완료/비선택 상태색)"
      via: "verbatim className 인용 (룰 mirror)"
      pattern: "bg-surface-raised border border-border-default"
    - from: "floorplan-modals-sketch.html"
      to: "FloorPlanPage.tsx line 1300~2160 (3 모달 원본 위치)"
      via: "mock 데이터로 시각만 재현 (로직 변경 X)"
      pattern: "점검 기록 입력|조치 입력|마커"
    - from: "floorplan-modals-sketch.html"
      to: "floorplan-chrome-sketch.html (06 chrome sketch — head/style/tokens/viewport-frame 블록)"
      via: "verbatim 복사"
      pattern: "viewport-mobile|viewport-desktop|tailwind.config"
---

<objective>
06 FloorPlanPage 의 Wave 1 모달 3종(마커 선택 정보 popup / inspectModal / resolveModal)의 chrome 통일 시안을 단일 HTML 파일로 작성. TSX 변환은 별도 quick task — 이 작업은 **sketch HTML 작성 한정**.

Purpose: redesign/06-floorplan-v2 진입 전 사용자 시각 검토용. 02 inspection-unification 패턴 + 05 remediation 단색 CTA 결정을 06 모달에 mirror 하기 위한 디자인 기준선.

Output: `cha-bio-safety/docs/redesign-context/06-floorplan/sketch/floorplan-modals-sketch.html` (단일 HTML, 3 모달 × 2 viewport 이상, 룰 박스 포함, ~800-1000 lines)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@cha-bio-safety/docs/redesign-context/00-design-context/inspection-modal-chrome-rules.md
@cha-bio-safety/docs/redesign-context/02-inspection/sketch/inspection-sketch-main.html
@cha-bio-safety/docs/redesign-context/02-inspection/sketch/inspection-unification-sketch.html
@cha-bio-safety/docs/redesign-context/06-floorplan/sketch/floorplan-chrome-sketch.html
@cha-bio-safety/src/pages/FloorPlanPage.tsx
@cha-bio-safety/src/pages/InspectionPage.tsx
@cha-bio-safety/src/pages/RemediationDetailPage.tsx

<interfaces>
<!-- 06 chrome sketch (이미 존재) 의 head/style/tokens/viewport CSS 블록 verbatim 복사 대상 -->
<!-- floorplan-chrome-sketch.html 의 1-244 라인 (head + style 블록 전체) 을 새 파일에 그대로 복사 -->
<!-- 변경 포인트: title, header 텍스트, main grid (4 viewport → 6+ viewport), rule-box 내용 -->

From inspection-modal-chrome-rules.md §5 — 상태 색 페어:
```
선택:   border-[1.5px] border-accent bg-accent text-text-on-accent
완료:   border-[1.5px] border-safe bg-safe-bg text-safe
비선택: border border-border-strong bg-surface-page text-text-secondary
```

From inspection-modal-chrome-rules.md §6.2 — input/textarea:
```
'w-full box-border px-3 py-2.5 rounded-sm border border-border-default bg-surface-raised text-text-primary text-label outline-none'
```
- bg-surface-raised (raised) — bg-surface-page 금지
- focus:border-border-focus

From 02 InspectionPage 점검 결과 3택 패턴 (mirror):
- normal 선택: border-[1.5px] border-safe bg-safe-bg text-safe + CheckCircle2
- caution 선택: border-[1.5px] border-warning bg-warning-bg text-warning + AlertTriangle
- bad 선택: border-[1.5px] border-danger bg-danger-bg text-danger + XCircle
- 비선택: border border-border-default bg-surface-page text-text-secondary

From 05 RemediationDetailPage CTA 패턴 (mirror):
- 단색 accent (그라디언트 폐기): bg-accent text-text-on-accent
- 취소: bg-surface-sunken border border-border-default text-text-secondary
- height: h-input (또는 h-10 허용)

From 메모리 룰 feedback_inspection_unresolved_color:
- 미조치 = fire (주황) / 완료 = safe (녹) — 마커 정보 popup statusLabel 색은 status 의미에 따라:
  * normal → safe
  * caution → warning
  * bad/unresolved → fire (주황) 또는 danger (사용자 인지 = fire 가 표준)
  * resolved → safe
  * fault → danger
  * uninspected → text-tertiary
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: floorplan-modals-sketch.html 작성 (3 모달 × 6 viewport + 룰 박스)</name>
  <files>cha-bio-safety/docs/redesign-context/06-floorplan/sketch/floorplan-modals-sketch.html</files>

  <action>
**작성 절차:**

1. **base 복사**: `cha-bio-safety/docs/redesign-context/06-floorplan/sketch/floorplan-chrome-sketch.html` 의 1-244 라인 (`<!doctype html>` ~ `</style></head>`) 을 그대로 복사. title 만 "06 FloorPlanPage 모달 통일 시안 v0.1 (Wave 1 — 마커 popup / inspectModal / resolveModal)" 로 교체. style 블록 내 `.viewport-frame .modal-backdrop` 보조 클래스 추가:
```css
.modal-backdrop {
  position: absolute; inset: 0;
  background: rgba(0,0,0,0.6);
  display: flex; align-items: center; justify-content: center;
  z-index: 50;
}
.modal-box {
  position: relative;
  width: 90%; max-width: 320px;
  border-radius: 12px;
  padding: 20px;
  max-height: 86%; overflow-y: auto;
}
.balloon-arrow-top    { width: 0; height: 0; border-left: 8px solid transparent; border-right: 8px solid transparent; }
.balloon-arrow-bottom { width: 0; height: 0; border-left: 8px solid transparent; border-right: 8px solid transparent; }
```

2. **body 구조** (`<body class="p-10">`):
   - 헤더 (`<header class="mb-8">`): meta-h1 "06 FloorPlanPage 모달 통일 시안 v0.1 (Wave 1)" + meta-p 룰 reference + Wave 1 범위 명시 ("외부 popup 보류: InspectionRevisitPopup / AccessBlockedPopup")
   - main grid: 6 viewport 를 섹션별로 묶음 (`<main class="flex flex-col gap-12">`):
     - **Section A: 마커 선택 정보 popup** — meta-h2 + 3 viewport (mobile dark sheet 1 / mobile dark sheet 2 / desktop dark balloon) `grid grid-cols-3 gap-8`
     - **Section B: inspectModal** — meta-h2 + 2 viewport (mobile dark / desktop dark) `grid grid-cols-2 gap-10`
     - **Section C: resolveModal** — meta-h2 + 2 viewport (mobile dark / desktop dark) `grid grid-cols-2 gap-10`
   - 마지막에 rule-box (적용 룰 요약)
   - 끝에 `<script>lucide.createIcons()</script>`

3. **공통 모달 wrapper className** (3 모달 모두):
```
modal-backdrop + modal-box + class="bg-surface-raised border border-border-default"
```

4. **Section A — 마커 선택 정보 popup**:

   **A1. Mobile dark sheet — status=normal/safe** (variant A):
   - viewport-mobile data-theme="dark"
   - 안에 dummy chrome (헤더 stub + 도면 placeholder bg-surface-page)
   - 백드롭 + 모바일 sheet: `<div class="absolute bottom-0 left-0 right-0 bg-surface-raised border-t border-border-default rounded-t-lg px-4 pt-3 pb-5 z-50">`
     - grab handle: `<div class="w-9 h-1 rounded-pill bg-border-default mx-auto mb-3.5"></div>`
     - 컨텐츠 row (`<div class="flex items-start gap-3 mb-4">`):
       * 아이콘 박스 40x40: `<div class="w-10 h-10 rounded-sm flex-shrink-0 flex items-center justify-center bg-safe-bg border-[1.5px] border-safe">` + `<i data-lucide="zap" style="width:22px;height:22px" class="text-safe"></i>` (유도등 placeholder)
       * 라벨 + meta (`<div class="flex-1 min-w-0">`):
         - 라벨: `<div class="text-label font-bold text-text-primary mb-1">M2 비상등 1번</div>` (label 13px → 14-15px 효과 위해 mb-1 + leading-snug 가능)
         - meta row: `<div class="flex flex-wrap gap-2 items-center">` + 4 span (text-caption text-text-tertiary leading-none):
           - 3F
           - ID CP-3-1-GL
           - statusLabel: `<span class="text-caption font-bold text-safe leading-none">정상</span>`
           - 최근 2026-05-15
       * ✕ close: `<button class="text-text-tertiary -mt-1 p-1"><i data-lucide="x" style="width:16px;height:16px"></i></button>`
     - action buttons row (`<div class="flex gap-2">`): "점검 기록 입력" `flex-1 h-input rounded-sm bg-accent text-text-on-accent text-label font-bold` + "조치 입력" 비활성 또는 normal 이라 단일 액션만

   **A2. Mobile dark sheet — status=bad/fire (미조치)** (variant B):
   - 동일 sheet 구조, 다만:
     - 아이콘 박스: `bg-fire-bg border-[1.5px] border-fire` + lucide 아이콘 색 `text-fire`
     - 라벨: "M5 소화기 분말 3.3kg"
     - meta: 5F / ID CP-5-2-FE / statusLabel `text-fire` "불량" / 최근 2026-05-10
     - actions: "점검 기록 입력" (단색 accent) + "조치 입력" (단색 accent, 두 번째 액션) — 2 버튼 flex-1

   **A3. Desktop dark balloon — status=uninspected** (variant C):
   - viewport-desktop data-theme="dark"
   - 안에 dummy chrome (06 chrome sketch desktop 패턴 stub — 헤더 + 도면 종류 탭 + 층 칩 mini 버전 OR 그냥 도면 placeholder)
   - 백드롭 (`absolute inset-0` 인데 desktop 은 백드롭 없이 balloon 만 — 실제 코드 mirror): balloon 만 absolute 위치
   - balloon (위치 데스크톱 중앙 mock): `<div class="absolute top-[180px] left-[480px] w-[320px] bg-surface-raised border border-border-default rounded-md p-4 z-30" style="box-shadow:0 8px 32px rgba(0,0,0,0.45);">`
     - 화살표 (상단 향함, arrow-bottom = balloon 이 위에 있고 마커는 아래): `<div class="absolute -top-2 left-[100px] balloon-arrow-bottom" style="border-bottom:8px solid var(--surface-raised);"></div>` (border 토큰 매칭 추가 wrapper 가능)
     - 컨텐츠 row: 아이콘 박스 36x36 `bg-surface-sunken border border-border-default` (uninspected = 회색 톤) + lucide 아이콘 `text-text-tertiary` "circle-help" 또는 "alert-circle"
     - 라벨: "M3 감지기"
     - meta: 1F (ID 없음) / statusLabel `text-text-tertiary` "미점검"
     - ✕ close
     - actions: "점검 기록 입력" (단색 accent flex-1) — 단일 버튼

5. **Section B — inspectModal "점검 기록 입력"**:

   **B1. Mobile dark — 소화기 카테고리, 결과=normal 선택**:
   - viewport-mobile data-theme="dark"
   - dummy chrome stub (헤더 + 도면 종류 탭 (소화기·소화전 선택) + 층 칩 (5F 선택))
   - 백드롭 + modal-box:
     - 헤더 영역:
       * `<div class="text-body font-bold text-text-primary mb-1">점검 기록 입력</div>`
       * `<div class="text-caption text-text-tertiary mb-4">B1F 방재센터 · 5F</div>` (실제 코드 selected.cp_location || label 패턴, 시안 mock)
     - **소화기 KV grid** (소화기 카테고리만):
       * wrapper: `<div class="bg-surface-page border border-border-default rounded-sm p-3 mb-2">`
       * `<div class="grid grid-cols-2 gap-x-3 gap-y-1 text-caption">` 8 KV 페어:
         - 위치: 방재센터 외 / 제조업체: 한국소방 / 제조년월: 2024-03 / 형식승인: 한24-1234
         - 접두문자: AB / 증지번호: 25-7-1 / 제조번호: SN-2024-001 / ID: CP-5-2-FE
         - 각 페어: `<div><span class="text-text-tertiary">위치 </span><span class="text-text-primary font-semibold">방재센터 외</span></div>`
       * 정보수정 + 소화기분리 버튼 row:
         - `<div class="flex gap-1.5 mb-3.5">`
         - 정보수정: `flex-1 h-8 rounded-sm bg-surface-sunken border border-border-default text-text-secondary text-caption font-semibold leading-none`
         - 소화기분리: `flex-1 h-8 rounded-sm bg-danger-bg border border-danger-bar/40 text-danger text-caption font-semibold leading-none`
     - **점검 결과 3택**:
       * 라벨: `<div class="text-caption font-semibold text-text-tertiary mb-1.5 tracking-wider">점검 결과</div>`
       * row: `<div class="flex gap-1.5 mb-3.5">`
       * normal (선택): `flex-1 inline-flex items-center justify-center gap-1.5 px-2 py-2.5 rounded-sm text-label font-bold border-[1.5px] border-safe bg-safe-bg text-safe` + `<i data-lucide="check-circle" style="width:14px;height:14px"></i>` + "정상"
       * caution (비선택): `flex-1 inline-flex items-center justify-center gap-1.5 px-2 py-2.5 rounded-sm text-label font-bold border border-border-default bg-surface-page text-text-secondary` + `<i data-lucide="alert-triangle" style="width:14px;height:14px"></i>` + "주의"
       * bad (비선택): 동일 비선택 패턴 + `<i data-lucide="x-circle" style="width:14px;height:14px"></i>` + "불량"
     - **특이사항 + 사진**:
       * label row: `<div class="flex items-center justify-between mb-1.5"><label class="text-caption font-semibold text-text-tertiary tracking-wider">특이사항 (선택)</label><span class="text-caption text-text-tertiary">점검 사진 (선택)</span></div>`
       * row: `<div class="flex gap-2 items-start mb-3.5">`
       * textarea: `flex-1 h-18 rounded-sm bg-surface-raised border border-border-default text-text-primary text-label leading-relaxed p-2.5 outline-none box-border resize-none` placeholder "특이사항을 입력하세요"
         - **주의**: 모달 wrapper 가 이미 bg-surface-raised 이므로 textarea bg 는 페이지 톤(bg-surface-page) 또는 sunken 으로 분리해야 시각 구분. 룰 §6.2 는 본문이 surface-page 일 때 raised 라는 전제. 모달 안에서는 modal wrapper(raised) → textarea(page or sunken) 분기 필요. **결정**: textarea bg = `bg-surface-page border border-border-default` (modal 내부 컨테이너 패턴)
       * PhotoButton placeholder: `<div class="w-18 h-18 rounded-sm bg-surface-page border border-dashed border-border-default flex flex-col items-center justify-center gap-1 text-text-tertiary"><i data-lucide="camera" style="width:18px;height:18px"></i><span class="text-caption leading-none">촬영</span></div>`
     - **하단 액션**:
       * `<div class="flex gap-2">`
       * 취소: `flex-1 h-input rounded-sm bg-surface-sunken border border-border-default text-text-secondary text-label font-semibold`
       * 저장: `flex-1 h-input rounded-sm bg-accent text-text-on-accent text-label font-bold` "저장"

   **B2. Desktop dark — 소화전 카테고리, paired BC 포함, 결과=caution 선택**:
   - viewport-desktop data-theme="dark"
   - dummy chrome stub (06 chrome desktop 헤더)
   - 백드롭 + modal-box (`max-width: 400px` 또는 더 넓게 — 시안용으로 동일 320 유지 가능)
   - 헤더: "점검 기록 입력" + "B1F 옥내소화전 1번 · B1F"
   - 점검 결과 3택: caution 선택 패턴 (`border-[1.5px] border-warning bg-warning-bg text-warning` + AlertTriangle)
   - 특이사항 textarea + PhotoButton placeholder
   - **paired BC 섹션** (별도 카드):
     * divider: `<div class="h-px bg-border-default my-3"></div>`
     * BC info 카드: `<div class="bg-surface-page border border-border-default rounded-sm px-3 py-2 mb-2.5">`
       - `<div class="text-caption text-text-tertiary leading-none mb-0.5">비상콘센트</div>`
       - `<div class="text-label font-bold text-text-primary">B1F 옥내소화전 1번 BC</div>`
     * BC 점검 결과 라벨 + 3택 (normal 선택 — safe 토큰)
     * BC 특이사항 textarea + PhotoButton placeholder
   - 하단 액션 (취소 + 저장)

6. **Section C — resolveModal "조치 입력"**:

   **C1. Mobile dark — 유도등 카테고리, "본체 교체" 선택, 자재 입력 + 지적 메모 배지**:
   - viewport-mobile data-theme="dark"
   - dummy chrome (유도등 plan 선택, 3F)
   - 백드롭 + modal-box:
     - 헤더: "조치 입력" + "M2 비상등 1번 · 3F"
     - **지적 메모 배지**: `<div class="bg-warning-bg border border-warning-bar/40 rounded-sm px-2.5 py-1.5 text-caption text-warning mb-3">지적: 예비전원 점멸</div>`
     - **조치 피커 3택** (증상 피커와 동일 패턴):
       * 라벨 없이 바로 3택 row (실제 코드도 라벨 없음)
       * "본체 교체" (선택): `flex-1 px-2 py-2 rounded-sm text-label font-bold border-[1.5px] border-accent bg-accent text-text-on-accent`
       * "예비전원 교체" (비선택): `flex-1 px-2 py-2 rounded-sm text-label font-bold border border-border-default bg-surface-page text-text-secondary`
       * "직접 입력" (비선택): 동일 비선택 패턴
       * row: `<div class="flex gap-1.5 mb-3.5">`
     - **소모 자재 라벨**:
       * `<div class="flex items-center justify-between mb-1.5"><label class="text-caption font-semibold text-text-tertiary tracking-wider">소모 자재</label><span class="text-caption text-text-tertiary">조치 사진 (선택)</span></div>`
     - **자재명 + 개수 + PhotoButton row** (`flex gap-2 items-start mb-3.5`):
       * 좌측 column (`flex-1 flex flex-col gap-1`):
         - 자재명 input: `w-full h-9 rounded-sm bg-surface-page border border-border-default text-text-primary text-label px-3 outline-none box-border` placeholder "자재명" value="유도등 본체"
         - 개수 input wrapper (relative): `<div class="relative">`
           - input: `w-full h-9 rounded-sm bg-surface-page border border-border-default text-text-primary text-label px-3 pr-7 outline-none box-border` placeholder="0" value="1"
           - suffix: `<span class="absolute right-2.5 top-1/2 -translate-y-1/2 text-caption text-text-tertiary pointer-events-none leading-none">ea</span>`
       * PhotoButton placeholder (B1 과 동일)
     - **하단 액션**:
       * 취소: `flex-1 h-input rounded-sm bg-surface-sunken border border-border-default text-text-secondary text-label font-semibold`
       * 조치 완료: `flex-1 h-input rounded-sm bg-accent text-text-on-accent text-label font-bold` (linear-gradient 폐기)

   **C2. Desktop dark — 감지기 카테고리, memo textarea (자재 input 없음)**:
   - viewport-desktop data-theme="dark"
   - dummy chrome (감지기 plan, 2F)
   - 백드롭 + modal-box:
     - 헤더: "조치 입력" + "M7 감지기 · 2F"
     - 지적 메모 배지: "지적: 감지부 손상"
     - **memo 영역 (planType !== 'guidelamp' 분기 시뮬)**:
       * label row + textarea + PhotoButton placeholder
       * textarea: `bg-surface-page border border-border-default` 패턴
       * label: "조치 내용 (필수)" + "조치 사진 (선택)"
     - 하단 액션 (취소 + 조치 완료, 단색 accent)

7. **마지막에 rule-box 박스** (`<section class="mt-12">`):
   - meta-h2 "적용 룰 요약 (chrome 통일)"
   - `<div class="rule-box">` 안에 h2 + ul/li:
     - "v0.1.1 토큰만 — alias 0건"
     - "이모지 ✕ → lucide X / lucide CheckCircle2 / AlertTriangle / XCircle / Camera"
     - "linear-gradient 0건 — CTA 단색 bg-accent"
     - "모달 wrapper: bg-surface-raised border border-border-default rounded-md p-5 (max-width 320 모바일 / 400 데스크톱 옵션)"
     - "점검 결과 3택: 02 InspectionPage 패턴 mirror — border-[1.5px] + status 토큰 페어"
     - "조치/증상 피커: 선택 1.5px accent / 비선택 1px border-default page"
     - "input/textarea bg: 모달 wrapper(raised) 안에서는 surface-page 로 분리해 시각 구분 (룰 §6.2 본문 raised 패턴의 모달 내 변형)"
     - "마커 정보 popup: 상태 색 아이콘 박스 (status 토큰 페어) + statusLabel 색 일치"
     - "메모리 룰 feedback_inspection_unresolved_color: 미조치 = fire (주황) 톤, 완료 = safe (녹)"
   - 작은 footer: "Wave 1 범위 한정. InspectionRevisitPopup / AccessBlockedPopup 외부 컴포넌트는 별도 Wave 보류."

8. **lucide 초기화** body 맨 끝:
```html
<script>lucide.createIcons();</script>
```

**금지 사항** (작성 중 자체 검수):
- 옛 var() alias (`var(--bg2)`, `var(--bg3)`, `var(--bd)`, `var(--bd2)`, `var(--t1)`, `var(--t2)`, `var(--t3)`, `var(--acl)`, `var(--warn)`, `var(--danger)`) — 0건
- `linear-gradient(` — 0건
- 이모지 ✕ (U+2715, `✕`) — 0건 (lucide x icon 만)
- raw hex 색 (`#22c55e`, `#eab308`, `#ef4444`, `#f59e0b`, `#3b82f6` 등) — 0건 (status 토큰 페어만)
- `style={{` (React 인라인) — sketch 는 순수 HTML 이므로 React 문법 금지

**메모리 룰 준수**:
- `feedback_planner_prompt_sketch_verbatim`: 06 chrome sketch 의 head/style/tokens/viewport CSS verbatim 복사 (위 1번 참조)
- `feedback_design_sketch_first`: 비즈니스 로직 (소화기 KV / paired BC / 직접입력 분기) 시각 표현만, 코드 로직 변경 X
- `feedback_redesign_sketch_rule_enforcement`: 룰 문서 className verbatim 사용
- `feedback_inspection_unresolved_color`: 미조치 = fire (주황) 톤
- `feedback_text_caption_leading_none`: `h-8` 정보수정/소화기분리 버튼 내 text-caption 은 `leading-none` 명시 (이미 위에 반영)
  </action>

  <verify>
    <automated>F=/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/06-floorplan/sketch/floorplan-modals-sketch.html; test -f "$F" && \
LINES=$(wc -l < "$F") && [ "$LINES" -ge 800 ] && echo "lines:$LINES OK" && \
VP=$(grep -c 'viewport-frame ' "$F") && [ "$VP" -ge 6 ] && echo "viewports:$VP OK" && \
MW=$(grep -c 'bg-surface-raised border border-border-default' "$F") && [ "$MW" -ge 6 ] && echo "modal-wrappers:$MW OK" && \
N=$(grep -c 'border-\[1\.5px\] border-safe bg-safe-bg text-safe' "$F") && [ "$N" -ge 1 ] && echo "normal-state:$N OK" && \
C=$(grep -c 'border-\[1\.5px\] border-warning bg-warning-bg text-warning' "$F") && [ "$C" -ge 1 ] && echo "caution-state:$C OK" && \
B=$(grep -c 'border-\[1\.5px\] border-danger bg-danger-bg text-danger' "$F") && [ "$B" -ge 1 ] && echo "bad-state:$B OK" && \
CTA=$(grep -c 'bg-accent text-text-on-accent' "$F") && [ "$CTA" -ge 2 ] && echo "accent-cta:$CTA OK" && \
ALIAS=$(grep -E 'var\(--bg2\)|var\(--bg3\)|var\(--bd\)|var\(--bd2\)|var\(--t1\)|var\(--t2\)|var\(--t3\)|var\(--acl\)|var\(--warn\)|var\(--danger\)' "$F" | grep -v '^[[:space:]]*\*' | wc -l | tr -d ' ') && [ "$ALIAS" -eq 0 ] && echo "alias:$ALIAS OK" && \
GRAD=$(grep -c 'linear-gradient' "$F") && [ "$GRAD" -eq 0 ] && echo "gradient:$GRAD OK" && \
EMOJI=$(grep -c '✕' "$F") && [ "$EMOJI" -eq 0 ] && echo "emoji-x:$EMOJI OK" && \
LX=$(grep -c 'data-lucide="x"' "$F") && [ "$LX" -ge 2 ] && echo "lucide-x:$LX OK" && \
LCC=$(grep -c 'data-lucide="check-circle"' "$F") && [ "$LCC" -ge 1 ] && echo "lucide-check:$LCC OK" && \
LAT=$(grep -c 'data-lucide="alert-triangle"' "$F") && [ "$LAT" -ge 1 ] && echo "lucide-alert:$LAT OK" && \
LXC=$(grep -c 'data-lucide="x-circle"' "$F") && [ "$LXC" -ge 1 ] && echo "lucide-xcircle:$LXC OK" && \
HEX=$(grep -E '#22c55e|#eab308|#ef4444|#f59e0b' "$F" | grep -v '^[[:space:]]*--status' | grep -v '^[[:space:]]*\*' | wc -l | tr -d ' ') && [ "$HEX" -eq 0 ] && echo "raw-hex:$HEX OK" && \
echo "ALL CHECKS PASS"</automated>
  </verify>

  <done>
- `cha-bio-safety/docs/redesign-context/06-floorplan/sketch/floorplan-modals-sketch.html` 파일이 존재
- 라인 수 800+ (3 모달 × 6 viewport + chrome stub + rule-box)
- viewport-frame 6개 (mobile dark sheet × 2 + desktop dark balloon × 1 + inspectModal mobile dark + desktop dark + resolveModal mobile dark + desktop dark = 7 viewport, 최소 6)
- bg-surface-raised + border-border-default 모달 wrapper 6+ 매칭
- 점검 결과 3택 패턴 (safe/warning/danger 각 1.5px border + bg 페어) 각 1+ 인스턴스
- 단색 accent CTA 2+ (저장 + 조치 완료)
- 옛 alias var() 0건, linear-gradient 0건, 이모지 ✕ 0건, raw hex (#22c55e/#eab308/#ef4444/#f59e0b status 색) 0건
- lucide x / check-circle / alert-triangle / x-circle / camera 아이콘 모두 사용
- 적용 룰 요약 rule-box 박스 포함
  </done>
</task>

</tasks>

<verification>
**Grep gate (verify 블록의 automated 명령 그대로):**
- 파일 존재 + 라인 수 ≥ 800
- viewport-frame 6+
- 모달 wrapper 패턴 6+
- 점검 결과 3택 패턴 normal/caution/bad 각 1+
- 단색 accent CTA 2+
- 옛 alias 0건, linear-gradient 0건, 이모지 ✕ 0건, raw hex 0건
- lucide 아이콘 패턴 각 1+ (x ≥ 2 / check-circle ≥ 1 / alert-triangle ≥ 1 / x-circle ≥ 1)

**사용자 시각 검토 (자체 verify 후 별도):**
- 브라우저에서 sketch 열어 3 모달 chrome 일관성 확인
- 02 inspection-unification-sketch.html / 05 RemediationDetailPage 와 옆에 두고 비교 (CTA / 점검 결과 / textarea 패턴)
- 마커 정보 popup statusLabel 색이 status 의미와 일치하는지 (normal=safe, bad=fire, uninspected=tertiary)
</verification>

<success_criteria>
- 단일 HTML 파일 `floorplan-modals-sketch.html` 작성 완료
- 자동 grep gate 모두 PASS ("ALL CHECKS PASS" 출력)
- 사용자 시각 검토 후 TSX 변환 quick task 로 분리 가능한 상태
- TSX 변환 시 sketch className verbatim 인용 가능한 명확한 구조
</success_criteria>

<output>
After completion, create `.planning/quick/260517-g0n-06-floorplanpage-chrome-sketch-wave-1-in/260517-g0n-01-SUMMARY.md`
요약 내용:
- 작성한 sketch 파일 경로
- 6 viewport 구성 (Section A 3개 + Section B 2개 + Section C 2개 = 7 또는 6)
- 적용된 핵심 룰 (모달 wrapper, 상태색 페어, 단색 accent, textarea bg-page 분기)
- 사용자 시각 검토 대기 + 다음 quick (TSX 변환) 트리거 시점
</output>
