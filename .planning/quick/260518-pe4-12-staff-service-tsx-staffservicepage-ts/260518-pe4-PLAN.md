---
phase: 260518-pe4
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cha-bio-safety/src/pages/StaffServicePage.tsx
autonomous: true
requirements:
  - REDESIGN-12-TSX-CONVERSION
tags:
  - redesign
  - tsx-conversion
  - tailwind-v0.1.1
  - staff-service

must_haves:
  truths:
    - "StaffServicePage.tsx v0.1.1 Tailwind 변환 완료 — 모든 alias 토큰 제거 / 정규화 카테고리 hex 적용 / lucide Check 도입"
    - "모바일 BottomSheet + 데스크톱 3-panel + 달력 grid + 식단 + dropzone + PDF preview 모두 W1~W9 sketch 1:1 매핑"
    - "비즈니스 로직 0 변경 — useQuery/useMutation/toast 카피/lp 좌표/상수 8종/calc 함수 모두 보존"
    - "Build PASS — tsc --noEmit 0 errors / npm run build exit 0"
    - "negative gate 모두 0 hits / positive gate 최소 hits 모두 통과"
  artifacts:
    - path: "cha-bio-safety/src/pages/StaffServicePage.tsx"
      provides: "v0.1.1 Tailwind 으로 재작성된 StaffServicePage (target ~1200~1400 lines, source 1552)"
      contains: "import { Check } from 'lucide-react'"
  key_links:
    - from: "StaffServicePage.tsx"
      to: "tailwind.config.js (v0.1.1 토큰)"
      via: "Tailwind class (bg-surface-*, text-text-*, border-border-*, bg-duty-*, text-safe|warning|danger|info)"
      pattern: "bg-surface-(page|raised|sunken)|text-text-(primary|secondary|tertiary)"
    - from: "StaffServicePage.tsx"
      to: "src/styles/typography.css"
      via: "text-(caption|label|body-sm|body|title|heading|display) class"
      pattern: "text-(caption|label|body-sm|body|title)"
    - from: "StaffServicePage.tsx"
      to: "lucide-react"
      via: "import { Check }"
      pattern: "from 'lucide-react'"
---

<objective>
StaffServicePage.tsx (1552 lines) 의 legacy 인라인 스타일 + alias 토큰 (`var(--bg)`, `var(--t1)` 등) 을 v0.1.1 Tailwind class (`bg-surface-page`, `text-text-primary` 등) 로 재작성한다. W1~W9 sketch HTML 을 source-of-truth, W10 checklist 를 변환 가이드로 사용해 모바일 (BottomSheet) + 데스크톱 (3-panel) 양쪽 모두 1:1 매핑한다.

Purpose: redesign/12-staff-service 트랙의 최종 wave — design-tokens v0.1.1 시스템 통합 + 노안 룰 (≥12px) + 정규화 카테고리 hex 적용으로 cbc7119-preview 시각 일관성 확보.

Output: Modified `cha-bio-safety/src/pages/StaffServicePage.tsx` (target ~1200~1400 lines). Atomic single-commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jykevin/Documents/cbc7119-design/CLAUDE.md
@/Users/jykevin/Documents/cbc7119-design/CLAUDE.local.md

# PRIMARY SPEC — read FIRST
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/12-staff-service/sketch/10-tsx-conversion-checklist.md

# Design contract
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/12-staff-service/UI-SPEC.md

# Source TSX — 변환 대상
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/src/pages/StaffServicePage.tsx

# Sketch HTML — CSS verbatim source per region
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/12-staff-service/sketch/01-calendar-cell-rules.html
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/12-staff-service/sketch/02-calendar-grid.html
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/12-staff-service/sketch/03-legend-summary.html
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/12-staff-service/sketch/04-menu-upload.html
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/12-staff-service/sketch/05-mobile-bottomsheet.html
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/12-staff-service/sketch/06-desktop-3panel.html
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/12-staff-service/sketch/07-desktop-center-form.html
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/12-staff-service/sketch/08-desktop-pdf-preview.html
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/12-staff-service/sketch/09-toast-states.html

# v0.1.1 토큰 매핑 — verbatim source (메모리 룰: status-fire 토큰 prefix 금지 / spacing 8=48px 함정)
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/tailwind.config.js
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/src/styles/tokens.css
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/src/styles/typography.css
</context>

<interfaces>
<!-- Key tokens/contracts the executor will use. Extracted from tailwind.config + memory rules. -->

## v0.1.1 Tailwind class set (verbatim)

### Surface
- `bg-surface-page` (= source `var(--bg)`)
- `bg-surface-raised` (= source `var(--bg2)`)
- `bg-surface-sunken` (= source `var(--bg3)`)

### Border
- `border-border-default` (= source `var(--bd)`)
- `border-border-strong` (= source `var(--bd2)`)

### Text
- `text-text-primary` (= source `var(--t1)`)
- `text-text-secondary` (= source `var(--t2)`)
- `text-text-tertiary` (= source `var(--t3)`)
- `text-text-on-accent` (인버스 — accent 위 텍스트)
- `text-text-disabled`

### Accent
- `bg-accent` / `text-accent` / `border-accent` (= source `var(--acl)`)

### Duty
- `bg-duty-day` (= source `var(--c-day)`)
- `bg-duty-night` (= source `var(--c-night)`)
- `bg-duty-off` (= source `var(--c-off)`)
- `bg-duty-leave` (= source `var(--c-leave)`)

### Status (정확한 prefix — `status-` 사용 금지!)
- 텍스트: `text-safe`, `text-warning`, `text-danger`, `text-info`
- bar bg: `bg-safe-bar`, `bg-warning-bar`, `bg-danger-bar`, `bg-info-bar`
- 패널 bg: `bg-safe-bg`, `bg-warning-bg`, `bg-danger-bg`, `bg-info-bg`
- border: `border-safe-bar`, `border-warning-bar`, `border-danger-bar`, `border-info-bar`
- **금지 패턴**: `text-status-safe`, `bg-status-warning-bar`, `text-fire`, `bg-fire-bar`, `status-fire-*`

### Typography (size — leading 별도)
- `text-caption` (12px / lh 1.5 = 18px)
- `text-label` (13px)
- `text-body-sm` (14px)
- `text-body` (16px)
- `text-title` (18px)
- `text-heading`
- `text-display` (28px)
- 9·10·11px 모두 12px+ (text-caption) 로 격상 — 노안 룰

### Leading (작은 컨테이너 안에서 필수)
- `leading-none` (= 1) — h-7/h-8/배지/칩 안 텍스트
- `leading-relaxed` (= 1.625) — 메뉴 본문 등
- `leading-snug` (= 1.375)

### Spacing override (메모리 룰 — feedback_tailwind_w8_h8_is_48px)
- `w-1 = 4px` / `w-2 = 8px` / `w-3 = 12px` / `w-4 = 16px` / `w-5 = 20px` / `w-6 = 24px` / `w-7 = 32px` / `w-8 = 48px`
- `h-*` 동일
- **함정**: source 의 `32` (햄버거/백버튼 sized) → `w-7 h-7` (NOT `w-8`)

### Radius / Padding tokens (v0.1.1)
- `rounded-sm` (= 4px)
- `rounded-md` (= 6~8px — config 확인)
- `p-card` (= 카드 padding 토큰)
- `p-modal` (= 모달 padding 토큰)

## 정규화 카테고리 hex (W2 결정)
- 연차: `#42d778` (rgb `66, 215, 120`)
- 공가: `#8f42d7` (rgb `143, 66, 215`)
- 경조: `#d78042` (rgb `215, 128, 66`)
- 병가: `#d74242` (rgb `215, 66, 66`)
- 보건: `#d7428c` (rgb `215, 66, 140`)
- 기타: `#4244d7` (rgb `66, 68, 215`)
- 식단 cyan: `#06b6d4` (그대로)

## Lucide import
```tsx
import { Check } from 'lucide-react';
```
사용처: 휴가 종류 grid 의 선택/등록 표시 (✓ U+2713 대체).
크기: `<Check size={16} />` (= text-body-sm 매칭).

## 인라인 style 화이트리스트 (잔존 OK)
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/12-staff-service/sketch/10-tsx-conversion-checklist.md §10
(12 case: lp 좌표 17 / LEAVE_BG 셀 / 반차 gradient / boxShadow / animation jsx style / 식단 카드 alpha bg / 셀 텍스트 동적 color / `#facc15` 노랑 / 주말식대 보라 / 셀 border 동적 / 체크박스 12×12 / PDF overlay 동적 좌표)
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: StaffServicePage.tsx v0.1.1 Tailwind 재작성 (W1~W10 1:1 매핑, atomic single-commit)</name>
  <files>cha-bio-safety/src/pages/StaffServicePage.tsx</files>

  <behavior>
    이 task 는 production code 변환이지만 비즈니스 로직 0 변경 (useQuery / useMutation / toast / 상수 / lp 좌표 / calc 함수 보존)
    이므로 단위 테스트 추가 없이 grep 기반 verify gate + tsc + build 로 회귀 방지한다 (tdd=false).
    behavior 명세는 W10 checklist + §B region 매핑 + §C verify gate (negative/positive) 로 정의된다.
  </behavior>

  <action>
## STEP 0 — Mandatory 사전 read

다음 파일을 순서대로 읽는다 (1회씩, 재독 금지):
1. `cha-bio-safety/docs/redesign-context/12-staff-service/sketch/10-tsx-conversion-checklist.md` (PRIMARY SPEC — 436 lines)
2. `cha-bio-safety/src/pages/StaffServicePage.tsx` (1552 lines source — 큰 파일이므로 offset/limit 으로 region 별 read 권장)
3. `cha-bio-safety/tailwind.config.js` (토큰 매핑 verbatim)
4. `cha-bio-safety/src/styles/typography.css` (text-* class 정의)
5. (필요시) W1~W9 sketch HTML 의 region 별 CSS

읽기 전략:
- W10 checklist 의 §3 (region map) + §5 (cheatsheet) + §10 (인라인 style 예외) 을 핵심 reference 로 활용
- StaffServicePage.tsx 는 region 별로 한 번씩만 read — line 1~250 (hooks/헬퍼/상수), 251~680 (이벤트 핸들러/effect/PDF), 681~824 (calendarGrid + legendRow), 826~1018 (summaryCards + menuSection + uploadSection), 1020~1278 (데스크톱 분기), 1280~1549 (모바일 BottomSheet)

## STEP 1 — Imports 변경

- 기존 imports 보존
- 추가: `import { Check } from 'lucide-react';` (✓ U+2713 대체용)

## STEP 2 — 상수 정규화 update (B-2 / A-5)

`LEAVE_TYPES` 배열의 `rgb` 필드만 정규화 좌표로 교체:
- `'34,197,94'` → `'66, 215, 120'` (연차)
- `'168,85,247'` → `'143, 66, 215'` (공가)
- `'249,115,22'` → `'215, 128, 66'` (경조)
- `'239,68,68'` → `'215, 66, 66'` (병가)
- `'236,72,153'` → `'215, 66, 140'` (보건)
- `'99,102,241'` → `'66, 68, 215'` (기타)

`LEAVE_BG`/`LEAVE_LABEL` 도 옛 hex 사용 시 §A-5 정규화 hex set 으로 교체. 단 식단 cyan `#06b6d4` 는 그대로.

`SHIFT_BG`/`SHIFT_COLOR` 의 alias 색은 `var(--c-day)` → `var(--duty-day)` (또는 tailwind class 사용 가능한 곳은 class 로) 으로 update — alias 0건 gate 통과 필수.

다른 상수 (DOC_LEAVE_GRID, DOC_TO_API_TYPE, ANNUAL_TYPES, HOLIDAYS_FALLBACK, HALF_TYPES, lp[0..16] 17개 좌표) 는 변경 0.

## STEP 3 — Region 별 JSX 변환

W10 checklist §3 의 region 매핑 + planning_context §B 의 region 별 가이드를 따라 다음 순서로 처리:

### B-3. 달력 grid `calendarGrid` (line 681~798)
- W2 sketch 1:1
- 컨테이너 `padding: '0 8px'` → `className="px-2"`
- chevron 버튼: `bg-surface-sunken text-text-secondary rounded-md px-2 py-1 text-label font-bold`
- year/month 라벨: `text-title font-extrabold text-text-primary text-center min-w-[50px]`
- 요일 row: `grid grid-cols-7 mb-0.5` + 각 요일 `text-center text-caption font-bold py-1 leading-none {dow0?text-danger : dow6?text-info : text-text-tertiary}`
- 셀 grid: `grid grid-cols-7 gap-[3px] lg:gap-[2px]` (또는 `gap-1` 단일)
- 셀: `aspect-square lg:aspect-[1.2] rounded-sm p-0.5`
- 셀 안 텍스트: `text-caption font-bold leading-none` (source 6/7/8/10/11px → 12px 격상)
- "오늘" 칩: `bg-accent text-text-on-accent text-caption font-extrabold rounded-md ...` (source `#3b82f6` 인라인 → `bg-accent` 토큰)
- spinner: `w-6 h-6 border-2 border-border-default border-t-accent rounded-full animate-[spin_0.7s_linear_infinite]` (arbitrary animate 가 tailwind config 미설정으로 실패하면 inline `<style>` jsx 잔존 — `<style>{'@keyframes spin{to{transform:rotate(360deg)}}'}` 그대로)
- 셀 dynamic bg/border/text color 는 §E 화이트리스트 #2/#7/#10 에 따라 `style={{}}` 잔존 OK

### B-4. 범례 `legendRow` (line 800~824)
- W3 sketch 1:1
- 컨테이너: `flex flex-wrap items-center gap-2 lg:gap-1.5 px-3 pt-1.5`
- dot: `inline-block w-3 h-3 lg:w-2.5 lg:h-2.5 rounded-full` + dynamic bg (duty 토큰 `bg-duty-*` 또는 카테고리 정규화 hex inline)
- 라벨: `text-caption text-text-tertiary leading-none`
- 반차 dot: linear-gradient inline style 잔존 (§E #3)

### B-5. 요약 카드 `summaryCards` (line 826~840)
- W3 sketch 1:1
- 컨테이너: `flex gap-2 px-3 pt-3.5 overflow-x-auto` (Webkit scrolling 그대로)
- 카드: `flex-1 min-w-[72px] bg-surface-raised border border-border-default rounded-md p-card text-center`
- 좌측 3px 색바: `style={{ boxShadow: 'inset 3px 0 0 {hex}' }}` (§E #4)
- 라벨: `text-caption font-semibold text-text-tertiary leading-none mb-1`
- 값: `text-title font-extrabold leading-none whitespace-nowrap` + dynamic color (정규화 hex inline 또는 status 토큰)
- 색: 연차 `#42d778` / 제공식수 `#06b6d4` / 미사용식수 `#d7428c` / 주말식대 `#8f42d7`
- 연차 잔여 임계치 (W3): >= 3일 `#42d778` / < 3 `text-warning` / < 1 `text-danger`

### B-6. 식단 + dropzone (line 842~1018)
- W4 sketch 1:1
- `menuSection` 컨테이너: `grid grid-cols-2 gap-2 px-3 pt-2`
- 카드 3종 (한식/특식/석식): `border rounded-md p-3` + `style={{ background: 'rgba(R,G,B,0.08)', borderColor: 'rgba(R,G,B,0.2)' }}` (정규화 RGB §A-5 / §E #6)
- 코너 라벨: `text-caption font-bold leading-none mb-1.5` + `style={{ color: 'hex' }}` (식단 cyan / 보건 / 경조)
- 메뉴 본문: `text-body-sm text-text-secondary leading-relaxed whitespace-pre-line`
- 석식 카드: `col-span-2`
- `uploadSection` 컨테이너: `px-3 py-3.5`
- dropzone label: `flex items-center justify-center cursor-pointer transition-colors bg-surface-raised border border-border-default text-text-secondary text-caption font-semibold leading-none rounded-md py-3 lg:border-2 lg:border-dashed lg:py-12`
- hover/dragover dynamic: source 의 onDragOver/onDragLeave handler 유지 — borderColor 동적은 `style={{}}` 잔존 또는 state 기반 class toggle

### B-7. 모바일 BottomSheet (line 1280~1549)
- W5 sketch 1:1
- overlay: `style={{ position: 'absolute', inset: 0, background: 'rgba(0,0,0,0.45)', zIndex: 90, animation: 'fadeIn .2s ease' }}` (§E #1/#5 잔존 — animation jsx style 함께)
- sheet 컨테이너: `absolute bottom-0 left-0 right-0 z-100 bg-surface-raised rounded-t-[20px] p-4 pb-6 max-h-[65vh] overflow-y-auto shadow-[0_-4px_24px_rgba(0,0,0,0.2)]` + `style={{ animation: 'slideUp .25s ease' }}` (jsx style 잔존)
- drag handle: `flex justify-center mb-3` 컨테이너 안 `<div className="w-9 h-1 rounded-sm bg-border-default" />`
- 헤더 row:
  - 날짜: `text-body font-bold text-text-primary leading-none`
  - shift 칩: `text-caption font-bold text-text-on-accent bg-duty-{type} rounded-sm px-2 py-0.5 leading-none`
- 식사 미사용 끼수 버튼: dynamic (skipped>0 → `bg-warning-bg text-warning border-warning-bar` pair / 0 → `bg-surface-sunken text-text-tertiary border-border-default`)
- 휴가 섹션:
  - 차단 카피: `text-label text-text-tertiary px-3 py-2 bg-surface-sunken rounded-sm`
  - 점검일 경고: `text-label font-semibold bg-warning-bg text-warning px-2 py-1 rounded-sm`
  - 기간 input grid: `grid grid-cols-2 gap-2` + input `bg-surface-page border border-border-default text-text-primary rounded-sm px-3 py-2 text-body-sm`
  - 일수 표시: `text-body-sm font-bold leading-none` + `style={{ color: '#facc15' }}` (§E #8)
- 휴가 종류 버튼 그리드 (DOC_LEAVE_GRID 7행):
  - 일반: `bg-surface-sunken text-text-secondary border border-border-default rounded-sm py-2 text-label font-semibold leading-none`
  - 선택중: `bg-accent text-text-on-accent border-accent` + `<Check size={16} className="ml-1 inline" />` (✓ 대체)
  - 등록됨: `bg-safe-bg text-safe border-safe-bar` + `<Check size={16} className="ml-1 inline" />`
- 사유 textarea: `w-full bg-surface-page border border-border-default text-text-primary rounded-sm px-3 py-2 text-body-sm leading-relaxed resize-none`
- 기타종류 input: 동일 input styling
- CTA 2종:
  - 휴가 신청: 활성 `bg-safe text-text-on-accent` / 비활성 `bg-surface-sunken text-text-tertiary` — `rounded-md py-3 text-body font-bold leading-none`
  - PDF 다운로드: `bg-accent text-text-on-accent rounded-md py-3 text-body font-bold leading-none`
- 팀원 연차 칩: 연차잔여 `text-info` / 사용중 `text-safe` 분기
- 주말식대 안내: `style={{ background: 'rgba(143, 66, 215, 0.08)', border: '1px solid rgba(143, 66, 215, 0.2)' }}` + `style={{ color: '#8f42d7' }}` (§E #6/#9)
- 닫기 버튼: `bg-surface-sunken border border-border-default text-body-sm font-bold text-text-secondary rounded-md p-3`

### B-8. 데스크톱 분기 (line 1020~1278)
- W6/W7/W8 sketch 1:1
- 페이지 컨테이너 (line 1023): `h-full flex flex-col overflow-hidden bg-surface-page`
- 3-panel body (line 1027): `flex-1 flex overflow-hidden`
- 좌측 패널: `flex-1 min-w-0 border-r border-border-default overflow-y-auto pb-2` — calendarGrid + legendRow + summaryCards + divider + menuSection + uploadSection 재사용 (모바일과 동일 컴포넌트 변수)
- 중앙 패널 (line 1041): `w-[280px] flex-shrink-0 border-r border-border-default overflow-y-auto p-modal flex flex-col gap-3`
  - W7 sketch 1:1
  - 타이틀: `text-body font-bold text-text-primary leading-none mb-2`
  - 날짜표시 + shift 칩: 모바일 헤더와 동일 패턴
  - 기간 input grid: 모바일과 동일
  - DOC_LEAVE_GRID: 모바일과 동일 (단 desktop 사이즈)
  - 사유 / 기타종류 input: 모바일과 동일
  - 휴가신청 CTA: 모바일과 동일
  - PDF 다운로드 CTA: 모바일과 동일
  - 인쇄 CTA: `bg-info text-text-on-accent rounded-md py-3 text-body font-bold leading-none`
- 우측 패널 (line 1226): `flex-1 min-w-0 overflow-y-auto bg-surface-raised flex items-start justify-center p-modal relative`
- A4 컨테이너 (line 1227): `relative w-full max-w-[595px]`
- PDF preview img: `w-full block rounded-sm shadow-md` (source boxShadow 동적 — Tailwind shadow-md 매핑)
- lp[0..16] overlay (17 instances): `style={{ position: 'absolute', left: \`${p.x}%\`, top: \`${p.y}%\`, transform: 'translate(-50%,-50%)', fontSize: 12, fontWeight: 700, color: '#111', whiteSpace: 'nowrap', fontFamily: "'Noto Sans KR', sans-serif" }}` (§E #12 잔존 — source 10px → 12px 격상만 적용)
- 체크박스 (cp.x/cp.y 좌표): `style={{ position: 'absolute', left: \`${cp.x}%\`, top: \`${cp.y}%\`, transform: 'translate(-50%,-50%)', width: 12, height: 12, background: '#000' }}` (§E #11 그대로)

## STEP 4 — Animation `<style>` jsx 처리

source 의 `@keyframes spin/fadeIn/slideUp/shimmer` 는 `<style>{'@keyframes ...'}</style>` jsx 안에 그대로 잔존 (W10 OQ d default = 잔존). tailwind.config animation 추가하지 않음 (다른 페이지 broken 위험).

## STEP 5 — Verify gates (commit 전 모두 PASS 필요)

cha-bio-safety 디렉토리에서 다음 명령을 순서대로 실행:

### Negative (모두 0 hits 필요)
```bash
cd /Users/jykevin/Documents/cbc7119-design/cha-bio-safety

# 1. alias 토큰 0건
grep -cE 'var\(--(bg|bg2|bg3|bd|bd2|t1|t2|t3|acl|c-day|c-night|c-off|c-leave)\)' src/pages/StaffServicePage.tsx

# 2. 이모지/dingbat 0건 (✓ U+2713 포함 — lucide 대체 확인)
grep -cE '😀|😁|😂|🎯|✨|🔥|⚠️|✅|❌' src/pages/StaffServicePage.tsx
LC_ALL=C grep -cE $'[\xE2\x9C-\xE2\x9D]' src/pages/StaffServicePage.tsx  # U+2713 check

# 3. lg: spacing 0건 (layout 분기만 허용, gap 미세 분기는 arbitrary 사용)
grep -cE 'lg:px-|lg:py-|lg:p-[0-9]|lg:space-' src/pages/StaffServicePage.tsx

# 4. 인라인 fontSize 0건 (단 §E #12 PDF overlay 의 fontSize: 12 는 동적 객체 안이므로 제외 — grep 'fontSize:\s*[0-9]' 가 잡으면 lp 좌표 17 instance + 체크박스 0 — confirmed 잔존)
# NOTE: §E #12 화이트리스트에 명시된 PDF overlay text fontSize: 12 (×17) + 동적 lp 좌표 안의 fontSize 는 잔존 허용.
# 단 calendarGrid / summaryCards / menuSection 등 일반 JSX 안의 fontSize: NN 은 모두 className 으로 치환되어야 함.
# 검증: 잔존 fontSize 카운트 ≤ 17 (PDF overlay) 이면 PASS
grep -cE 'fontSize:\s*[0-9]+' src/pages/StaffServicePage.tsx

# 5. 9·10·11px 인라인 0건 (노안 룰)
grep -cE '\b(9|10|11)px\b' src/pages/StaffServicePage.tsx

# 6. text-fire / bg-fire / status-fire 0건 (UI-SPEC §3.4.1)
grep -cE 'text-fire|bg-fire|status-fire' src/pages/StaffServicePage.tsx

# 7. 옛 카테고리 / status hex 0건
grep -cE '#22c55e|#a855f7|#f97316|#ef4444|#ec4899|#6366f1|#f59e0b|#2563eb' src/pages/StaffServicePage.tsx
```

### Positive (모두 최소 hits 통과 필요)
```bash
# 1. bg-surface-* ≥ 10
grep -cE 'bg-surface-(page|raised|sunken)' src/pages/StaffServicePage.tsx

# 2. text-text-* ≥ 15
grep -cE 'text-text-(primary|secondary|tertiary|disabled|on-accent)' src/pages/StaffServicePage.tsx

# 3. border-border-* ≥ 5
grep -cE 'border-border-(default|strong)' src/pages/StaffServicePage.tsx

# 4. text-safe/warning/danger/info ≥ 4
grep -cE '\btext-(safe|warning|danger|info)\b' src/pages/StaffServicePage.tsx

# 5. bg-*-bar ≥ 3
grep -cE 'bg-(safe|warning|danger|info)-bar' src/pages/StaffServicePage.tsx

# 6. bg-*-bg ≥ 3
grep -cE 'bg-(safe|warning|danger|info)-bg' src/pages/StaffServicePage.tsx

# 7. bg-duty-* ≥ 4
grep -cE 'bg-duty-(day|night|off|leave)' src/pages/StaffServicePage.tsx

# 8. 정규화 hex ≥ 6
grep -cE '#42d778|#8f42d7|#d78042|#d74242|#d7428c|#4244d7|#06b6d4' src/pages/StaffServicePage.tsx

# 9. lucide-react import ≥ 1
grep -cE "from 'lucide-react'" src/pages/StaffServicePage.tsx

# 10. leading-* ≥ 5
grep -cE 'leading-(none|relaxed|snug)' src/pages/StaffServicePage.tsx

# 11. text-(caption|label|body-sm|body|title|heading|display) ≥ 15
grep -cE 'text-(caption|label|body-sm|body|title|heading|display)\b' src/pages/StaffServicePage.tsx
```

### Build
```bash
cd /Users/jykevin/Documents/cbc7119-design/cha-bio-safety
npx tsc --noEmit  # exit 0 (errors 0)
npm run build     # exit 0
```

## STEP 6 — Commit

모든 verify gate PASS 후:
```bash
cd /Users/jykevin/Documents/cbc7119-design
git add cha-bio-safety/src/pages/StaffServicePage.tsx
git commit -m "feat(260518-pe4-01): StaffServicePage.tsx v0.1.1 Tailwind 재작성 (W1~W10 1:1 매핑)"
```

(주의: wrangler 명령 절대 금지 — CLAUDE.local.md. main push 시 cbc7119-preview 자동 배포만.)

## STEP 7 — Summary

`.planning/quick/260518-pe4-12-staff-service-tsx-staffservicepage-ts/260518-pe4-01-SUMMARY.md` 작성:
- 변경 line 수 (source 1552 → target X)
- 모든 verify gate 결과 (negative 0 hits / positive ≥ N hits)
- tsc / build 결과
- 인라인 style 잔존 case 개수 (목표 ~30-50, source ~120)
- 비즈니스 로직 보존 확인 체크리스트 (useQuery 6 / useMutation N / toast 18 / lp 17 / 상수 8 — §D)
- 다음 단계 (main 머지 → cbc7119-preview 배포)
  </action>

  <verify>
    <automated>cd /Users/jykevin/Documents/cbc7119-design/cha-bio-safety &amp;&amp; ALIAS=$(grep -cE 'var\(--(bg|bg2|bg3|bd|bd2|t1|t2|t3|acl|c-day|c-night|c-off|c-leave)\)' src/pages/StaffServicePage.tsx) &amp;&amp; OLD_HEX=$(grep -cE '#22c55e|#a855f7|#f97316|#ef4444|#ec4899|#6366f1|#f59e0b|#2563eb' src/pages/StaffServicePage.tsx) &amp;&amp; FIRE=$(grep -cE 'text-fire|bg-fire|status-fire' src/pages/StaffServicePage.tsx) &amp;&amp; SMALL_PX=$(grep -cE '\b(9|10|11)px\b' src/pages/StaffServicePage.tsx) &amp;&amp; LG_SPACING=$(grep -cE 'lg:px-|lg:py-|lg:p-[0-9]|lg:space-' src/pages/StaffServicePage.tsx) &amp;&amp; SURFACE=$(grep -cE 'bg-surface-(page|raised|sunken)' src/pages/StaffServicePage.tsx) &amp;&amp; TEXT_TEXT=$(grep -cE 'text-text-(primary|secondary|tertiary|disabled|on-accent)' src/pages/StaffServicePage.tsx) &amp;&amp; LUCIDE=$(grep -cE "from 'lucide-react'" src/pages/StaffServicePage.tsx) &amp;&amp; echo "alias=$ALIAS old_hex=$OLD_HEX fire=$FIRE small_px=$SMALL_PX lg_spacing=$LG_SPACING surface=$SURFACE text_text=$TEXT_TEXT lucide=$LUCIDE" &amp;&amp; [ "$ALIAS" = "0" ] &amp;&amp; [ "$OLD_HEX" = "0" ] &amp;&amp; [ "$FIRE" = "0" ] &amp;&amp; [ "$SMALL_PX" = "0" ] &amp;&amp; [ "$LG_SPACING" = "0" ] &amp;&amp; [ "$SURFACE" -ge 10 ] &amp;&amp; [ "$TEXT_TEXT" -ge 15 ] &amp;&amp; [ "$LUCIDE" -ge 1 ] &amp;&amp; npx tsc --noEmit &amp;&amp; npm run build</automated>
  </verify>

  <done>
    - `cha-bio-safety/src/pages/StaffServicePage.tsx` 가 v0.1.1 Tailwind class 로 재작성됨 (~1200~1400 lines)
    - W1~W9 sketch 1:1 매핑 — 달력 grid / 범례 / 요약 카드 / 식단 / dropzone / 모바일 BottomSheet / 데스크톱 3-panel / 중앙 폼 / PDF preview 모두 변환
    - 비즈니스 로직 0 변경 — useQuery 6 / useMutation / toast 18 / lp 17 / 상수 8 / calc 함수 모두 보존
    - 모든 negative gate 0 hits PASS (alias 0 / 옛 hex 0 / fire 0 / 9-11px 0 / lg-spacing 0)
    - 모든 positive gate 최소 hits PASS (surface ≥ 10 / text-text ≥ 15 / border-border ≥ 5 / status text ≥ 4 / bar ≥ 3 / bg ≥ 3 / duty ≥ 4 / 정규화 hex ≥ 6 / lucide ≥ 1 / leading ≥ 5 / typography ≥ 15)
    - `npx tsc --noEmit` exit 0
    - `npm run build` exit 0
    - lucide-react `Check` import 추가 + ✓ U+2713 모두 대체
    - LEAVE_TYPES rgb 정규화 + SHIFT_BG/SHIFT_COLOR alias 0 update
    - 인라인 style 잔존 ~30-50 case (W10 §10 화이트리스트 한정 — lp/LEAVE_BG/gradient/boxShadow/animation/식단 alpha/동적 color/노랑/보라/border/체크박스/PDF overlay)
    - atomic single commit: `feat(260518-pe4-01): StaffServicePage.tsx v0.1.1 Tailwind 재작성 (W1~W10 1:1 매핑)`
    - SUMMARY.md 작성 완료
    - wrangler 명령 0회 실행 (CLAUDE.local.md 룰 준수)
  </done>
</task>

</tasks>

<verification>
**Plan-level 검증 (executor 가 task 1 완료 후):**

1. **Negative gates 모두 0 hits**
   - alias 토큰 `var(--bg|bg2|bg3|bd|bd2|t1|t2|t3|acl|c-day|c-night|c-off|c-leave)` → 0
   - 옛 카테고리 hex `#22c55e|#a855f7|#f97316|#ef4444|#ec4899|#6366f1|#f59e0b|#2563eb` → 0
   - `text-fire|bg-fire|status-fire` → 0
   - 9·10·11px 인라인 → 0
   - `lg:px-|lg:py-|lg:p-N|lg:space-` → 0
   - 이모지/dingbat (✓ U+2713 포함) → 0

2. **Positive gates 최소 hits 통과**
   - `bg-surface-*` ≥ 10
   - `text-text-*` ≥ 15
   - `border-border-*` ≥ 5
   - `text-(safe|warning|danger|info)` ≥ 4
   - `bg-*-bar` ≥ 3
   - `bg-*-bg` ≥ 3
   - `bg-duty-*` ≥ 4
   - 정규화 hex (`#42d778|#8f42d7|#d78042|#d74242|#d7428c|#4244d7|#06b6d4`) ≥ 6
   - `from 'lucide-react'` ≥ 1
   - `leading-(none|relaxed|snug)` ≥ 5
   - `text-(caption|label|body-sm|body|title|heading|display)` ≥ 15

3. **Build PASS**
   - `cd cha-bio-safety && npx tsc --noEmit` exit 0
   - `cd cha-bio-safety && npm run build` exit 0

4. **비즈니스 로직 보존**
   - useQuery 6개 + useMutation 호출 보존
   - toast 카피 18개 한글 verbatim 보존
   - lp[0..16] 17 좌표 0 변경
   - 상수 8종 (DOC_LEAVE_GRID/DOC_TO_API_TYPE/ANNUAL_TYPES/HOLIDAYS_FALLBACK/SHIFT_BG/SHIFT_COLOR/LEAVE_BG/LEAVE_LABEL/HALF_TYPES) 보존 (LEAVE_TYPES rgb 정규화만 허용)
   - handleMenuUpload / generateLeaveRequest / printLeaveRequest / calcLeaveQuota / calcWeekendAllowance / calcProvidedMeals 보존
   - isBlocked / isDesktop 분기 룰 보존

5. **인라인 style 잔존 화이트리스트 한정**
   - lp 17 + LEAVE_BG 셀 + 반차 gradient + boxShadow + animation jsx + 식단 alpha + 동적 color + 노랑 #facc15 + 보라 #8f42d7 + 셀 border 동적 + 체크박스 12×12 + PDF overlay 동적 좌표 — 약 30-50 case (source ~120 → 50% 이상 감소)

6. **CLAUDE.local.md 룰 준수**
   - wrangler 명령 0회
   - npm run deploy 0회
   - src/pages/StaffServicePage.tsx 외 다른 파일 미수정
</verification>

<success_criteria>
- StaffServicePage.tsx 가 v0.1.1 Tailwind 으로 재작성되어 cbc7119-preview 에 자동 배포되었을 때 W1~W9 sketch 와 시각적으로 일치 (모바일 + 데스크톱)
- 비즈니스 로직 0 변경 — 기존 데이터 fetch / mutation / PDF 생성 / toast 카피 모두 동일하게 동작
- tsc + build PASS
- main 머지 시 cbc7119-preview 자동 배포 트리거 (wrangler 수동 명령 0회)
- 인라인 style 함수형 감소 (source ~120 → target ~30-50, W10 §10 화이트리스트 한정)
</success_criteria>

<output>
After completion, create `.planning/quick/260518-pe4-12-staff-service-tsx-staffservicepage-ts/260518-pe4-01-SUMMARY.md` documenting:
- Line count diff (1552 → target)
- All negative/positive gate results
- tsc + build result
- 인라인 style 잔존 case 개수 + 화이트리스트 비교
- 비즈니스 로직 보존 체크리스트
- Commit SHA
- Next step: main 머지 → cbc7119-preview 자동 배포 → 사용자 시각 확인
</output>
