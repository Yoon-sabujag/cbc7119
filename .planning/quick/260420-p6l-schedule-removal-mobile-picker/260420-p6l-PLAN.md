---
phase: 260420-p6l
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cha-bio-safety/src/pages/ElevatorPage.tsx
autonomous: true
requirements:
  - P6L-01  # "검사 일정 등록" UI 완전 제거 (데스크톱/모바일 safety 탭)
  - P6L-02  # 모바일 "검사 기록"(annual) 탭 — 17대 전체 리스트 뷰

must_haves:
  truths:
    - "데스크톱 안전관리자(safety) 탭에 '🔍 검사 일정 등록' 카드가 더 이상 보이지 않는다"
    - "모바일 안전관리자(safety) 탭에도 '🔍 검사 일정 등록' 카드가 더 이상 보이지 않는다"
    - "모바일 '검사 기록'(annual) 탭에 진입하면 호기를 선택하지 않아도 cert_no 보유 호기 전체의 공단 검사이력이 세로 스크롤로 보인다"
    - "각 호기 카드는 'EV-01 · 장애/전망용' 형태 헤더 + 기존 KoelsaHistorySection UI(펼침 포함)로 렌더된다"
    - "TypeScript 컴파일 에러 0"
  artifacts:
    - path: "cha-bio-safety/src/pages/ElevatorPage.tsx"
      provides: "ElevatorPage 단일 파일 수정 — InspectionLookupInput/InspectKeyEntry/inspectKeysQuery/minwon24Query/inspectKeyList/koelsaHistoryMobile 제거, MobileAnnualRow 신규 도입"
      contains: "function MobileAnnualRow"
  key_links:
    - from: "모바일 annual 탭 (tab === 'annual' 분기)"
      to: "KoelsaHistorySection (호기별)"
      via: "MobileAnnualRow 래퍼 — 호기별 useQuery + KoelsaHistorySection 렌더"
      pattern: "elevators\\.filter\\(.*cert_no\\).*map.*MobileAnnualRow"
    - from: "MobileAnnualRow"
      to: "fetchInspectHistory (/api/elevators/inspect-history)"
      via: "useQuery queryKey ['elevator_inspect_history', cert_no], staleTime 6h"
      pattern: "queryKey:\\s*\\['elevator_inspect_history'"
---

<objective>
차바이오 PWA 승강기 페이지(`ElevatorPage.tsx`) 2건 정리:

1. **UI 제거:** 안전관리자 탭에 있던 "🔍 검사 일정 등록" 카드(고객안내번호 13자리 + 접수번호 17자리 수동 입력 폼)를 데스크톱·모바일 모두에서 완전히 제거한다. 소비자가 더 이상 없는 `minwon24Query`·`inspectKeysQuery`·`inspectKeyList` state·`InspectionLookupInput` 컴포넌트·`InspectKeyEntry` 타입도 같이 제거한다. 단, 서버 엔드포인트(`/api/elevators/inspect-keys`)와 DB 테이블(`inspect_keys`)은 롤백용으로 유지한다.
2. **모바일 검사 기록 탭 전면 리스트화:** 현재 모바일 `tab === 'annual'`은 `selectedEv?.cert_no` 하나만 보여주므로 호기를 선택하지 않으면 "공단 고유번호 없음" 빈 상태가 뜬다. 이를 점검 기록 탭처럼 cert_no 보유 호기(17대) 전체를 세로 리스트로 렌더하도록 교체한다.

Purpose: (1) 실제로 아무도 쓰지 않는 dead path 제거로 코드/상태 단순화. (2) 모바일에서 안전관리자가 "검사 기록"을 열면 호기 선택 없이 바로 전체 이력을 훑을 수 있도록 UX 개선.

Output: `cha-bio-safety/src/pages/ElevatorPage.tsx` 단일 파일 수정.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@CLAUDE.md
@cha-bio-safety/src/pages/ElevatorPage.tsx
@cha-bio-safety/src/components/KoelsaHistorySection.tsx
@cha-bio-safety/src/utils/inspectHistory.ts

<interfaces>
재탐색 없이 바로 쓸 수 있도록 기존 코드에서 추출한 핵심 계약.

Elevator 타입 (ElevatorPage.tsx line 20-50, 수정 금지):

```ts
interface Elevator {
  id: string
  number: number
  type: 'passenger' | 'cargo' | 'dumbwaiter' | 'escalator'
  status: 'normal' | 'fault' | 'maintenance' | 'out_of_service'
  cert_no?: string        // 승강기고유번호 (예: 2114-971) — 이 필드 보유 호기만 모바일 annual 리스트에 포함
  classification?: string // 분류 (장애/전망용, 전망용, 승객용, 화물용, 에스컬레이터 등)
  // ...
}
```

KoelsaHistorySection props (src/components/KoelsaHistorySection.tsx line 7-13, 수정 금지):

```ts
interface Props {
  certNo: string | null | undefined
  data: InspectHistoryResponse | null | undefined
  isLoading: boolean
  isError: boolean
  isMobile?: boolean
}
```

fetchInspectHistory (src/utils/inspectHistory.ts line 44-47, 수정 금지):

```ts
// 서버에서 min_age=21600(6h) TTL 적용 → 6시간 내 재호출은 DB 캐시만 반환.
// 17개 동시 호출해도 공단 API에는 전파되지 않음.
export async function fetchInspectHistory(certNo: string): Promise<InspectHistoryResponse>
```

기존 모바일 annual 탭 블록 (ElevatorPage.tsx line 1278-1289, 교체 대상):

```tsx
{tab === 'annual' && (
  <div style={{ marginBottom: 10 }}>
    <KoelsaHistorySection
      certNo={selectedEv?.cert_no}
      data={koelsaHistoryMobile.data}
      isLoading={koelsaHistoryMobile.isLoading}
      isError={koelsaHistoryMobile.isError}
      isMobile
    />
  </div>
)}
```

기존 koelsaHistoryMobile useQuery (line 511-519, 제거 대상):

```ts
const mobileCertNo = selectedEv?.cert_no ?? null
const koelsaHistoryMobile = useQuery({
  queryKey: ['elevator_inspect_history', mobileCertNo],
  queryFn: () => fetchInspectHistory(mobileCertNo!),
  enabled: !!mobileCertNo && tab === 'annual',
  staleTime: 6 * 60 * 60 * 1000,
  refetchOnWindowFocus: false,
})
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 검사 일정 등록 UI 및 dead 상태/쿼리 완전 제거</name>
  <files>cha-bio-safety/src/pages/ElevatorPage.tsx</files>
  <action>
ElevatorPage.tsx에서 다음 코드를 모두 제거한다 (라인 번호는 현재 스냅샷 기준. 제거하면 밀리므로 문자열/정규식 매칭으로 작업).

1. **데스크톱 safety 탭 렌더 (line 989-990 부근):**
   `{/* 검사 일정 등록 */}` 주석 + 바로 다음 줄의 `<InspectionLookupInput onSave={entries => setInspectKeyList(entries)} initialKeys={inspectKeyList} />` 라인을 함께 삭제. 바로 위 `</div>` (line 987)와 감싸는 Fragment는 건드리지 않는다.

2. **모바일 safety 탭 렌더 (line 1408-1409 부근):**
   `{/* 검사 일정 등록 */}` 주석 + `<InspectionLookupInput ... />` 라인 삭제. 바로 위 `</div>` (line 1406)와 이후의 `</>` Fragment 종료(line 1410)는 유지.

3. **`inspectKeysQuery` useQuery (line 453-474) 전체 제거.** 바로 위 주석 `// 민원24 검사조회 키 — DB에서 로드 ...` 포함.

4. **`inspectKeyList` state + 초기화 useEffect (line 475-480) 전체 제거.**

5. **`minwon24Query` useQuery (line 482-498) 전체 제거.** 바로 위 `// NOTE(260420): ...` 및 `// TODO:` 주석 2줄도 함께 제거.

6. **`InspectionLookupInput` 컴포넌트 + `InspectKeyEntry` 타입 (line 2004-2091) 전체 제거.**
   `// ── 검사 일정 등록 입력폼 ...` 주석부터 컴포넌트 닫는 `}` (line 2091)까지. 바로 다음 `// ── 점검 기록 모달 ──` 블록(line 2093-)은 보존.

7. **Import 정리:** `useEffect`는 다른 곳에서도 사용되므로 유지. React import 목록은 원본 유지. 다른 제거 심볼은 전부 파일 내부 선언이었으므로 별도 import 제거 작업 없음.

8. **서버/DB는 절대 건드리지 않는다:** `/api/elevators/inspect-keys`, `/api/elevators/koelsa-inspect` 엔드포인트, `inspect_keys` D1 테이블, 관련 마이그레이션 모두 롤백용으로 유지. functions/api/elevators/* 하위 파일은 수정 금지.

**수정 후 자기점검:**
- `InspectionLookupInput`, `InspectKeyEntry`, `inspectKeyList`, `inspectKeysQuery`, `minwon24Query` 다섯 심볼이 파일에서 전부 사라졌는지 grep으로 확인
- 남은 useQuery/useEffect/useState 블록의 중괄호·세미콜론 짝이 정상인지 확인
  </action>
  <verify>
    <automated>cd /Users/jykevin/Documents/20260328/cha-bio-safety && grep -cE "InspectionLookupInput|InspectKeyEntry|inspectKeyList|inspectKeysQuery|minwon24Query|검사 일정 등록|검사일정 등록" src/pages/ElevatorPage.tsx</automated>
  </verify>
  <done>
- 데스크톱/모바일 safety 탭 어디에도 🔍 검사 일정 등록 카드가 렌더되지 않음
- `InspectionLookupInput`, `InspectKeyEntry`, `inspectKeyList`, `inspectKeysQuery`, `minwon24Query` 5개 심볼이 ElevatorPage.tsx에서 모두 사라짐
- `/api/elevators/inspect-keys` 엔드포인트 코드는 수정되지 않음 (롤백용 유지)
- 위 grep 결과 0. 다른 코드 블록의 문법 온전.
- Task 2의 변경이 함께 적용되기 전 `tsc --noEmit`는 MobileAnnualRow 누락으로 실패할 수 있으므로, Task 1 단독 검증은 grep 결과만 확인.
  </done>
</task>

<task type="auto">
  <name>Task 2: 모바일 annual 탭을 17대 전체 리스트(MobileAnnualRow)로 교체</name>
  <files>cha-bio-safety/src/pages/ElevatorPage.tsx</files>
  <action>
ElevatorPage.tsx에 다음 변경을 적용한다.

1. **기존 `koelsaHistoryMobile` useQuery 블록 제거 (line 511-519 부근):**
   `const mobileCertNo = selectedEv?.cert_no ?? null` 부터 useQuery 블록 전체 삭제. 주석 `// 공단 공식 검사이력 (모바일 annual 탭) — selectedEv.cert_no 기준` 포함. `mobileCertNo` 지역 변수도 함께 제거.

2. **기존 모바일 annual 렌더 블록 교체 (line 1278-1289 부근):**
   기존 `{tab === 'annual' && (<div style={{ marginBottom: 10 }}> <KoelsaHistorySection ... isMobile /> </div>)}` 전체를 다음으로 교체:

   ```tsx
   {/* ── 검사 기록 (17대 전체 리스트) ── */}
   {tab === 'annual' && (
     <div style={{ display:'flex', flexDirection:'column', gap:12, marginBottom: 10 }}>
       {elevators.filter(e => e.cert_no).map(ev => (
         <MobileAnnualRow key={ev.id} elevator={ev} />
       ))}
       {elevators.filter(e => e.cert_no).length === 0 && (
         <div style={{ textAlign:'center', padding:'40px 0', color:'var(--t3)', fontSize:12 }}>
           공단 고유번호가 등록된 호기가 없습니다
         </div>
       )}
     </div>
   )}
   ```

3. **`MobileAnnualRow` 컴포넌트 신규 추가:**
   - 위치: Task 1에서 `InspectionLookupInput`이 제거된 자리(기존 line 2004~2091 영역). 바로 아래 `// ── 점검 기록 모달 ──` 블록(기존 line 2093)의 **직전**에 삽입.
   - 구현:

   ```tsx
   // ── 모바일 annual 탭 — 호기별 공단 검사이력 Row ──
   function MobileAnnualRow({ elevator }: { elevator: Elevator }) {
     const q = useQuery({
       queryKey: ['elevator_inspect_history', elevator.cert_no],
       queryFn: () => fetchInspectHistory(elevator.cert_no!),
       enabled: !!elevator.cert_no,
       staleTime: 6 * 60 * 60 * 1000,
       refetchOnWindowFocus: false,
     })
     const prefix = elevator.type === 'escalator' ? 'ES' : 'EV'
     const numStr = String(elevator.number).padStart(2, '0')
     return (
       <div>
         <div style={{ fontSize:11, fontWeight:700, color:'var(--t3)', marginBottom:6, letterSpacing:'.04em' }}>
           {prefix}-{numStr}{elevator.classification ? ` · ${elevator.classification}` : ''}
         </div>
         <KoelsaHistorySection
           certNo={elevator.cert_no}
           data={q.data}
           isLoading={q.isLoading}
           isError={q.isError}
           isMobile
         />
       </div>
     )
   }
   ```

   - `useQuery` (line 3), `KoelsaHistorySection` (line 14), `fetchInspectHistory` (line 15), `Elevator` 타입 (line 20)은 이미 파일 상단에 선언/임포트되어 있음. 추가 import 불필요.

4. **데스크톱 annual 탭은 건드리지 않는다.** 데스크톱 annual 렌더 경로는 이 Task의 변경 범위 밖. 해당 분기의 파일/로직 수정 금지.

5. **React Hooks 규칙 준수:** `MobileAnnualRow`는 모듈 스코프 함수 컴포넌트이고 내부에서 `useQuery` 1회만 호출. 부모는 `elevators.filter(...).map(ev => <MobileAnnualRow key={ev.id} ... />)` — map 콜백 내부에서 직접 `useQuery`를 부르지 않으므로 Hooks 규칙 위반 없음.

6. **동시 fetch 부하 체크:** cert_no 보유 호기 17개 → 최초 진입 시 17개 요청이 `/api/elevators/inspect-history`로 병렬 발사되지만, 서버는 이미 6h TTL 캐시(min_age=21600) 적용 중이라 공단 API 호출 없이 D1에서만 반환. React Query staleTime 6h로 같은 cert_no 재방문은 네트워크 스킵.
  </action>
  <verify>
    <automated>cd /Users/jykevin/Documents/20260328/cha-bio-safety && A=$(grep -cE "MobileAnnualRow" src/pages/ElevatorPage.tsx) && B=$(grep -cE "koelsaHistoryMobile|mobileCertNo" src/pages/ElevatorPage.tsx) && echo "MobileAnnualRow=$A koelsaHistoryMobile_leftover=$B" && [ "$A" -ge 2 ] && [ "$B" -eq 0 ] && npx tsc --noEmit</automated>
  </verify>
  <done>
- `MobileAnnualRow` 컴포넌트가 정의되고, 모바일 annual 탭에서 `elevators.filter(e => e.cert_no).map`으로 렌더됨
- `koelsaHistoryMobile`·`mobileCertNo` 식별자가 파일에서 완전히 사라짐
- 데스크톱 annual 탭 코드 경로 변경 없음 (grep diff로 해당 구간 수정 0줄 확인)
- `npx tsc --noEmit` 결과 에러 0
- 모바일 검사 기록 탭 진입 시 호기 선택 없이 17대(cert_no 보유 호기) 전체 공단 이력 카드가 세로 스크롤로 나열됨
  </done>
</task>

</tasks>

<verification>
**전체 변경 검증 (Task 1 + 2 완료 후):**

1. `cd cha-bio-safety && grep -cE "InspectionLookupInput|InspectKeyEntry|inspectKeyList|inspectKeysQuery|minwon24Query|koelsaHistoryMobile|mobileCertNo" src/pages/ElevatorPage.tsx` → **0**
2. `cd cha-bio-safety && grep -cE "MobileAnnualRow" src/pages/ElevatorPage.tsx` → **2 이상** (정의 + 렌더)
3. `cd cha-bio-safety && npx tsc --noEmit` → **에러 0**
4. `cd cha-bio-safety && npm run build` → **성공** (Vite 프로덕션 빌드)
5. **서버 영역 무변경 확인:** `git diff --stat functions/api/elevators/` → **비어있음**

**배포 후 수동 점검 (CLAUDE.md 정책: 프로덕션 배포 후 테스트):**
- 데스크톱 safety 탭: 🔍 검사 일정 등록 카드 없음 확인
- 모바일 safety 탭: 🔍 검사 일정 등록 카드 없음 확인
- 모바일 annual 탭: 호기 선택 없이 진입 → cert_no 보유 호기 전체 카드 리스트가 세로로 보이는지 + 각 카드 펼침 동작 정상
- 데스크톱 annual 탭: 기존 동작 그대로 (회귀 없음)
</verification>

<success_criteria>
- [ ] `InspectionLookupInput`, `InspectKeyEntry`, `inspectKeyList`, `inspectKeysQuery`, `minwon24Query`, `koelsaHistoryMobile`, `mobileCertNo` 7개 식별자가 ElevatorPage.tsx에서 완전히 제거됨
- [ ] `MobileAnnualRow` 컴포넌트가 추가되고 모바일 annual 탭에서 cert_no 보유 호기별로 렌더됨
- [ ] `npx tsc --noEmit` 통과
- [ ] 서버/DB 코드(functions/api/elevators/*, migrations/*) 변경 없음
- [ ] 데스크톱 annual 탭 코드 경로 무변경
- [ ] `npm run build` 성공
- [ ] wrangler deploy `--branch production`으로 프로덕션 배포 후 데스크톱/모바일 safety·annual 탭 수동 확인 완료
</success_criteria>

<output>
작업 완료 후 다음 파일 경로의 SUMMARY를 생성한다:

`/Users/jykevin/Documents/20260328/.planning/quick/260420-p6l-schedule-removal-mobile-picker/260420-p6l-01-SUMMARY.md`

SUMMARY 포함 내용:
- 제거된 심볼 목록(7개)과 제거 라인 수
- 신규 `MobileAnnualRow` 위치(라인 번호)와 코드 요약
- 데스크톱 annual 탭 무변경 확인 결과
- `npx tsc --noEmit` 및 `npm run build` 실행 결과
- 배포 여부 및 수동 검증 결과 (또는 배포 미완료 시 다음 액션)
</output>
