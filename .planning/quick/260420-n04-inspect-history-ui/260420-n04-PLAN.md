---
phase: 260420-n04-inspect-history-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cha-bio-safety/functions/api/elevators/inspect-history.ts
  - cha-bio-safety/src/utils/inspectHistory.ts
  - cha-bio-safety/src/components/KoelsaHistorySection.tsx
  - cha-bio-safety/src/pages/ElevatorPage.tsx
autonomous: true
requirements:
  - QUICK-260420-n04
must_haves:
  truths:
    - "승강기 annual(검사 기록) 탭 진입 시, cert_no가 있는 호기는 공단 공식 검사이력이 자동 표시된다"
    - "같은 cert_no에 대해 6시간 내 재진입 시 공단 API를 다시 호출하지 않고 DB 캐시만 반환한다(cached=true, lastFetchedAt 포함)"
    - "cert_no가 없는 호기는 '공단 고유번호 없음 — 관리자 등록 필요' 빈 상태를 보여준다"
    - "각 검사이력 항목은 날짜 DESC 순서로 펼침 가능하며, 펼치면 fails[] 부적합 내역이 표기된다"
    - "기존 수동 업로드 PDF 섹션은 공단 공식 검사이력 카드 아래에 그대로 유지된다(회귀 없음)"
    - "모바일(tab='annual')과 데스크톱(desktopRightTab='annual') 양쪽에서 동일한 공단 공식 검사이력 섹션이 렌더된다"
  artifacts:
    - path: "cha-bio-safety/functions/api/elevators/inspect-history.ts"
      provides: "GET /api/elevators/inspect-history에 min_age(초, 기본 21600) 파라미터 추가 — DB 최근 fetched_at이 신선하면 공단 API 호출 skip + cached=true + lastFetchedAt 반환"
      contains: "min_age"
    - path: "cha-bio-safety/src/utils/inspectHistory.ts"
      provides: "fetchInspectHistory(certNo) 클라이언트 함수 + InspectHistoryItem/InspectFailItem 타입"
      exports: ["fetchInspectHistory", "InspectHistoryItem", "InspectFailItem"]
    - path: "cha-bio-safety/src/components/KoelsaHistorySection.tsx"
      provides: "공단 공식 검사이력 카드 섹션 컴포넌트 — 헤더/리스트/펼침 상세/빈상태/로딩/판정배지 색상"
      exports: ["KoelsaHistorySection"]
    - path: "cha-bio-safety/src/pages/ElevatorPage.tsx"
      provides: "annual 탭 상단에 KoelsaHistorySection 삽입 (데스크톱 line 861 블록, 모바일 line 1445 블록) + React Query 연동"
      contains: "elevator_inspect_history"
  key_links:
    - from: "cha-bio-safety/src/components/KoelsaHistorySection.tsx"
      to: "cha-bio-safety/src/utils/inspectHistory.ts"
      via: "fetchInspectHistory import (또는 상위에서 useQuery로 주입)"
      pattern: "fetchInspectHistory"
    - from: "cha-bio-safety/src/pages/ElevatorPage.tsx"
      to: "/api/elevators/inspect-history"
      via: "useQuery(queryKey:['elevator_inspect_history', cert_no]) + fetchInspectHistory"
      pattern: "elevator_inspect_history"
    - from: "cha-bio-safety/functions/api/elevators/inspect-history.ts"
      to: "elevator_inspect_history / elevator_inspect_fails (D1)"
      via: "DB SELECT으로 lastFetchedAt 판정 + INSERT/UPSERT"
      pattern: "min_age"
---

<objective>
승강기 페이지 "검사 기록(annual)" 탭에 공단 공식 API(ElevatorInspectsafeService) 기반 검사이력을 자동 표시한다. 260420-mk6에서 완성된 `inspect-history` endpoint에 6시간 TTL을 서버쪽에 추가하고, 프론트에서는 탭 마운트 시 React Query로 자동 조회한다. 기존 수동 업로드 PDF 섹션은 그대로 두고 상단에 "공단 공식 검사이력" 카드를 추가한다.

Purpose: 사용자가 검사 기록 탭만 열어도 공단 최신 검사 데이터가 즉시 보이게 하고, 6시간 내 재진입 시 불필요한 외부 API 호출을 방지해 응답 속도와 공단 서버 부하 양쪽을 줄인다.

Output:
- 서버: `inspect-history.ts`에 `min_age` 파라미터 + cached 응답 필드
- 프론트 유틸: `src/utils/inspectHistory.ts` (타입 + fetcher)
- UI 컴포넌트: `src/components/KoelsaHistorySection.tsx`
- `ElevatorPage.tsx` 모바일/데스크톱 annual 탭에 삽입
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/quick/260420-mk6-inspect-history/260420-mk6-SUMMARY.md
@cha-bio-safety/functions/api/elevators/inspect-history.ts
@cha-bio-safety/functions/api/elevators/_inspectsafe.ts
@cha-bio-safety/src/utils/api.ts
@cha-bio-safety/migrations/0067_inspect_history.sql

<interfaces>
<!-- 기존 inspect-history.ts response shape (260420-mk6 기준) -->
<!-- 단일 호기 응답 (cert_no 쿼리):
{
  success: true,
  data: {
    elevatorNo: string,      // 7자리 숫자 (하이픈 제거됨)
    certNo: string,          // 원본 (하이픈 포함)
    history: Array<{
      failCd: string,
      inspectDate: string | null,       // YYYY-MM-DD
      inspectKind: string | null,
      inspectInstitution: string | null,
      companyName: string | null,
      dispWords: string | null,         // 판정 문자열 (예: '합격', '보완후합격')
      validStart: string | null,
      validEnd: string | null,
      ratedSpeed: string | null,
      ratedCap: number | null,
      floorCount: number | null,
      buildingName: string | null,
      address: string | null,
      fails: Array<{
        failDesc: string | null,
        failDescInspector: string | null,
        standardArticle: string | null,
        standardTitle: string | null,
      }>,
    }>,
    historyCount: number,
    failCount: number,
    lastInspectDate: string | null,
    cached: boolean,
  }
}
-->

<!-- ElevatorPage.tsx 핵심 앵커 (이미 존재) -->
<!--
  Line 90:   type Tab = 'list' | 'fault' | 'repair' | 'inspect' | 'annual' | 'safety'
  Line 322:  const [desktopRightTab, setDesktopRightTab] = useState<...'annual'...>('fault')
  Line 339-342: 기존 useQuery 선언 위치 (이 근처에 새 useQuery 추가)
  Line 541:  const selectedDesktopEv = detailEv ?? evs[0] ?? ess[0] ?? null
  Line 547:  evAnnuals 정의
  Line 861:  {desktopRightTab === 'annual' && (() => { ... })()}  ← 데스크톱 annual 블록
  Line 1445: {tab === 'annual' && (() => { ... })()}              ← 모바일 annual 블록
-->

<!-- api.ts 패턴 (참고) -->
<!--
  export class ApiError extends Error { constructor(public status: number, message: string) ... }
  async function req<T>(path, init) — Authorization Bearer + 401 auto-logout
  api.get<T>(path) = req<T>(path)
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: inspect-history 서버 핸들러에 min_age TTL 추가</name>
  <files>cha-bio-safety/functions/api/elevators/inspect-history.ts</files>
  <action>
GET 핸들러의 단일 호기 분기(syncAll=false)에 `min_age` 파라미터 처리 로직을 추가한다.

구체 변경:
1. `url.searchParams.get('min_age')` 파싱. 기본값 `21600` (6시간, 초 단위). 숫자 아니면 기본값. 음수는 0으로 clamp.
2. `refresh !== false` 이면서 `min_age > 0` 이면, 공단 API 호출 전에 DB 최신성 체크:
   ```ts
   const latestFetch = await env.DB.prepare(
     'SELECT fetched_at FROM elevator_inspect_history WHERE elevator_no=? ORDER BY fetched_at DESC LIMIT 1'
   ).bind(elevatorNo).first<{ fetched_at: string }>()
   ```
   - `latestFetch?.fetched_at` 가 존재하고, `Date.now() - new Date(latestFetch.fetched_at).getTime() < min_age * 1000` 이면:
     - `syncOne()` 호출 SKIP
     - `loadFromDb()` 로만 조회
     - response `cached: true`, `lastFetchedAt: latestFetch.fetched_at` 반환
3. 위 조건 미충족(DB에 데이터 없음 또는 stale)이면 기존대로 `syncOne()` 실행 후 `loadFromDb()`. 이 경로의 response에 `cached: false`, `lastFetchedAt: new Date().toISOString()` 반환.
4. 기존 `refresh=0` (캐시 only) 경로: `lastFetchedAt`도 함께 포함하도록 확장 — `SELECT MAX(fetched_at)` 조회 추가.
5. `sync_all=1` 분기는 `min_age` 완전 무시 (기존 동작 유지 — 관리자 초기 동기화용). 주석 한 줄 추가: `// min_age ignored for sync_all mode`.
6. 타입 안전성: `as any` 금지. 기존 `HistoryRow`/`FailRow` 인터페이스 패턴 유지. 새 `lastFetchedAt` 은 `string | null`.
7. 에러 로깅: 기존 `console.error('inspect-history error:', msg)` 유지. DB 조회 실패는 기존 try-catch에서 502로 흘러가도록 그대로 둠.

건들지 말 것: `_inspectsafe.ts`, `sync_all` 로직, `syncOne()` 내부, UPSERT 쿼리, fail API 병렬 청크 로직.
  </action>
  <verify>
    <automated>cd /Users/jykevin/Documents/20260328/cha-bio-safety && grep -c "min_age" functions/api/elevators/inspect-history.ts && grep -c "lastFetchedAt" functions/api/elevators/inspect-history.ts && npx tsc --noEmit -p .</automated>
  </verify>
  <done>
`min_age` grep 결과 ≥ 2 (파싱 + 주석/사용), `lastFetchedAt` grep ≥ 3 (3개 경로 모두), `npx tsc --noEmit` 에러 0. 기존 3개 경로(sync_all / refresh=0 / refresh=1) 모두 `lastFetchedAt` 필드 포함.
  </done>
</task>

<task type="auto">
  <name>Task 2: 프론트 API 유틸 + 타입 (inspectHistory.ts)</name>
  <files>cha-bio-safety/src/utils/inspectHistory.ts</files>
  <action>
신규 파일 생성. 기존 `api.ts`의 `req<T>` / `ApiError` 패턴을 **그대로 재사용**한다 (이미 Authorization Bearer + 401 auto-logout 처리됨).

파일 구조:
```ts
import { api } from './api'

export interface InspectFailItem {
  failDesc: string | null
  failDescInspector: string | null
  standardArticle: string | null
  standardTitle: string | null
}

export interface InspectHistoryItem {
  failCd: string
  inspectDate: string | null       // YYYY-MM-DD
  inspectKind: string | null
  inspectInstitution: string | null
  companyName: string | null
  dispWords: string | null         // '합격' | '보완후합격' | '보완' | '불합격' | ...
  validStart: string | null
  validEnd: string | null
  ratedSpeed: string | null
  ratedCap: number | null
  floorCount: number | null
  buildingName: string | null
  address: string | null
  fails: InspectFailItem[]
}

export interface InspectHistoryResponse {
  elevatorNo: string
  certNo: string
  history: InspectHistoryItem[]
  historyCount: number
  failCount: number
  lastInspectDate: string | null
  cached: boolean
  lastFetchedAt: string | null
}

/**
 * 공단 공식 승강기 검사이력 조회.
 * - 서버에서 min_age=21600(6h) TTL 적용 — 6시간 내 재호출은 DB 캐시만 반환.
 * - cert_no 하이픈 포함/미포함 모두 허용 (서버에서 내부 정규화).
 * - 401 시 api.ts 레이어에서 자동 로그아웃 + /login 리다이렉트.
 */
export async function fetchInspectHistory(certNo: string): Promise<InspectHistoryResponse> {
  const q = encodeURIComponent(certNo)
  return api.get<InspectHistoryResponse>(`/elevators/inspect-history?cert_no=${q}`)
}
```

주의:
- `min_age` 파라미터는 **명시하지 않음** — 서버 기본값(21600) 사용. 프론트가 바꿀 일 없음.
- 기존 `api.ts`의 `req<T>`는 `{ success, data }` 래퍼를 언랩하므로 `data` 내용만 반환됨 — 위 타입은 언랩 후 shape.
- 이 파일은 `ElevatorPage.tsx`가 직접 import, 또는 Task 3의 `KoelsaHistorySection.tsx` 에서 간접 사용.
- 전역 상태/fetch client 직접 호출 금지. 반드시 `api.get`만 사용.

건들지 말 것: `src/utils/api.ts` (확장 불필요, 독립 파일로 분리).
  </action>
  <verify>
    <automated>cd /Users/jykevin/Documents/20260328/cha-bio-safety && ls src/utils/inspectHistory.ts && grep -c "fetchInspectHistory" src/utils/inspectHistory.ts && grep -c "InspectHistoryItem" src/utils/inspectHistory.ts && npx tsc --noEmit -p .</automated>
  </verify>
  <done>
`src/utils/inspectHistory.ts` 존재, `fetchInspectHistory` export ≥ 1회, `InspectHistoryItem` 타입 export ≥ 1회, `npx tsc --noEmit` 에러 0.
  </done>
</task>

<task type="auto">
  <name>Task 3: KoelsaHistorySection 컴포넌트 + ElevatorPage annual 탭 연동</name>
  <files>cha-bio-safety/src/components/KoelsaHistorySection.tsx, cha-bio-safety/src/pages/ElevatorPage.tsx</files>
  <action>
### 3-1. 신규 컴포넌트: `src/components/KoelsaHistorySection.tsx`

Props:
```ts
interface Props {
  certNo: string | null | undefined
  data: InspectHistoryResponse | null | undefined
  isLoading: boolean
  isError: boolean
  isMobile?: boolean  // 모바일에서 padding/폰트 축소
}
```

렌더 로직 (위에서 아래로):
1. **cert_no 없음** → 박스 안에 "공단 고유번호 없음 — 관리자 등록 필요" (회색 톤). 그 외 UI 미표시.
2. **isLoading && !data** → 기존 페이지의 skeleton 패턴(회색 rounded div 2~3개) 사용. Tailwind 도입 금지 — 인라인 스타일 + CSS 변수(`var(--bg)`, `var(--t1)`) 사용.
3. **isError** → "공단 API 일시 오류 — 잠시 후 다시 시도해주세요" (회색 박스).
4. **data 존재**:
   - **헤더 영역**:
     - 좌측: `공단 공식 검사이력 · 총 {data.historyCount}건`
     - 우측 작은 글씨: `lastFetchedAt` 이 있으면 `date-fns` + `date-fns/locale/ko`로 `formatDistanceToNow(new Date(lastFetchedAt), { addSuffix: true, locale: ko })` → "3시간 전 동기화" 형태. 없으면 숨김.
   - **빈 리스트**(`historyCount === 0`): "공단에 등록된 검사이력이 없습니다" 메시지.
   - **리스트**: `data.history` 그대로(서버가 이미 inspectDate DESC 정렬). 각 항목:
     ```
     [클릭 행]
       2024-02-15 · 정기검사 · [판정배지 합격]
       유효기간 2024-02-15 ~ 2025-02-14
       성남지사 · 티케이엘리베이터(주)
     [펼침 시 — fails[].length > 0]
       부적합 N건
       ▸ {standardArticle} {standardTitle}
          {failDesc}  ({failDescInspector})
     ```
   - **판정 배지 색상** (기존 `minwon24` 섹션 스타일 재사용):
     - `dispWords`가 `합격` 포함 + `보완`/`불합격` 미포함 → `var(--safe)` (초록)
     - `보완후합격` 또는 `조건부` 포함 → `var(--warn)` (노랑)
     - `보완` 또는 `불합격` 포함 → `var(--danger)` (빨강)
     - 그 외 → `var(--t3)` 또는 회색
   - **펼침 상태**: `useState<string | null>(null)` — 클릭 시 `failCd` 기준 토글. 기존 `ElevatorPage`의 `expandedMw` 패턴 참고 (컴포넌트 내부 자체 상태로 독립).

스타일:
- 인라인 스타일 + CSS 변수만 사용 (Tailwind 도입 금지).
- 카드 border/padding: `border: '1px solid var(--b)'`, `borderRadius: 12`, `padding: 16`, 모바일은 `padding: 12`.
- 폰트 계층: 헤더 16px bold, 날짜 14px, 보조 정보 12px var(--t3).

파일 상단 import:
```ts
import { useState } from 'react'
import { formatDistanceToNow } from 'date-fns'
import { ko } from 'date-fns/locale'
import type { InspectHistoryResponse } from '../utils/inspectHistory'
```

주석: 컴포넌트 상단에 한 줄: `// 공단 공식 검사이력 (ElevatorInspectsafeService) — annual 탭 상단 카드`

### 3-2. `ElevatorPage.tsx` 연동

(A) **import 추가** (파일 상단 기존 import 블록에):
```ts
import { KoelsaHistorySection } from '../components/KoelsaHistorySection'
import { fetchInspectHistory } from '../utils/inspectHistory'
```

(B) **useQuery 추가** — line 339~342 근처 기존 useQuery 선언 블록 바로 아래.

데스크톱과 모바일이 서로 다른 선택 호기(`selectedDesktopEv` vs `selectedEv`)를 쓰므로, 한 화면에 둘 다 동시에 필요한 일은 없지만 **서로 독립적인 두 useQuery** 대신 **현재 활성 호기 하나만 조회**하도록 통합한다:

```ts
// 검사 기록 탭이 활성일 때만 사용할 호기 결정 (데스크톱이면 selectedDesktopEv, 모바일이면 selectedEv)
// selectedDesktopEv 는 아래(line ~541)에서 계산되므로 useQuery에서 그 시점 값을 쓸 수 없음.
// → 대신 useQuery enabled는 certNo만 의존하고, certNo 계산을 각 탭 내부에서 하향 전달한다.
```

위 통합이 코드 복잡도를 키우면, **단순히 두 useQuery로 분리**하되 queryKey를 공유해 중복 호출 방지:

```ts
// 데스크톱용
const desktopCertNo = selectedDesktopEv?.cert_no ?? null
const koelsaHistoryDesktop = useQuery({
  queryKey: ['elevator_inspect_history', desktopCertNo],
  queryFn: () => fetchInspectHistory(desktopCertNo!),
  enabled: !!desktopCertNo && desktopRightTab === 'annual',
  staleTime: 6 * 60 * 60 * 1000,
  refetchOnWindowFocus: false,
})

// 모바일용
const mobileCertNo = selectedEv?.cert_no ?? null
const koelsaHistoryMobile = useQuery({
  queryKey: ['elevator_inspect_history', mobileCertNo],
  queryFn: () => fetchInspectHistory(mobileCertNo!),
  enabled: !!mobileCertNo && tab === 'annual',
  staleTime: 6 * 60 * 60 * 1000,
  refetchOnWindowFocus: false,
})
```

주의: `selectedDesktopEv` 는 line ~541 이후에 계산되므로 위 useQuery 블록은 **그 이후** 위치에 두거나, `useMemo` 로 호기 계산을 useQuery 이전으로 끌어올린다. **가장 안전한 배치**: 기존 desktopRightTab 블록 바로 위(line ~860 근처) 또는 함수 최상단에서 `selectedDesktopEv` 재계산을 한 번 더 하지 않도록 기존 line 541 위쪽으로 `detailEv`/`evs`/`ess` 계산을 당기는 리팩터는 **금지**. 대신, useQuery 자체를 line ~550~555 근처(selectedDesktopEv 계산 직후, JSX 시작 전) 위치에 배치한다.

(C) **데스크톱 annual 블록 삽입** — line 861 `{desktopRightTab === 'annual' && (() => {` 블록 내부 **맨 위** (IIFE 함수 본문 return JSX 최상단에):
```tsx
<KoelsaHistorySection
  certNo={selectedDesktopEv?.cert_no}
  data={koelsaHistoryDesktop.data}
  isLoading={koelsaHistoryDesktop.isLoading}
  isError={koelsaHistoryDesktop.isError}
/>
{/* ─ 아래는 기존 민원24/PDF 업로드 섹션 그대로 유지 ─ */}
```

(D) **모바일 annual 블록 삽입** — line 1445 `{tab === 'annual' && (() => {` 블록 내부 **맨 위**:
```tsx
<KoelsaHistorySection
  certNo={selectedEv?.cert_no}
  data={koelsaHistoryMobile.data}
  isLoading={koelsaHistoryMobile.isLoading}
  isError={koelsaHistoryMobile.isError}
  isMobile
/>
{/* ─ 아래는 기존 민원24/PDF 업로드 섹션 그대로 유지 ─ */}
```

(E) **건들지 말 것**:
- 기존 `annuals`/`mwList`/`evAnnuals` 로직 전부
- 기존 `expandedAnnual` / `expandedMw` 상태
- 기존 annual 탭 하단의 민원24 카드 + PDF 업로드/파싱 섹션
- 다른 탭 (`list`/`fault`/`repair`/`inspect`/`safety`) 로직 전부
- `koelsa-inspect.ts` / `koelsa.ts` 엔드포인트

### 3-3. 배포 주의사항 (실행 후 별도 메시지로 출력만)

- 서버 코드 변경(Task 1) 포함 → Cloudflare Pages 재배포 필수:
  ```
  cd cha-bio-safety && npm run deploy -- --branch production
  ```
- migration 0067 적용 여부 사전 확인 (미적용 시 500):
  ```
  wrangler d1 execute cha-bio-db --remote --command "SELECT name FROM sqlite_master WHERE type='table' AND name='elevator_inspect_history'"
  ```
- 17대 초기 동기화는 사용자가 admin 계정 로그인 후 브라우저에서 `/api/elevators/inspect-history?sync_all=1` 한 번 호출 (또는 별도 관리자 UI — 이 플랜 범위 밖).
- **로컬 dev 서버 테스트 금지** (CLAUDE.md 정책) — 반드시 프로덕션 배포 후 브라우저에서 확인.
  </action>
  <verify>
    <automated>cd /Users/jykevin/Documents/20260328/cha-bio-safety && ls src/components/KoelsaHistorySection.tsx && grep -c "KoelsaHistorySection" src/pages/ElevatorPage.tsx && grep -c "elevator_inspect_history" src/pages/ElevatorPage.tsx && grep -c "공단 공식 검사이력" src/components/KoelsaHistorySection.tsx && npx tsc --noEmit -p .</automated>
  </verify>
  <done>
`KoelsaHistorySection.tsx` 존재, `ElevatorPage.tsx`에서 KoelsaHistorySection import+사용 ≥ 2회(데스크톱+모바일), queryKey `elevator_inspect_history` 사용 ≥ 2회, 컴포넌트에 "공단 공식 검사이력" 헤더 텍스트 존재, `npx tsc --noEmit` 에러 0. 모바일/데스크톱 annual 탭 블록 모두 KoelsaHistorySection이 기존 섹션 **위**에 배치됨.
  </done>
</task>

</tasks>

<verification>
전체 TypeScript 컴파일 성공:
```
cd /Users/jykevin/Documents/20260328/cha-bio-safety && npx tsc --noEmit -p .
```
→ 0 errors.

기능 확인(배포 후):
1. 아무 승강기 선택 → "검사 기록" 탭 클릭
2. cert_no 있는 호기 → "공단 공식 검사이력 · 총 N건" 카드 자동 표시
3. 6시간 내 다시 열기 → 동일 데이터, "M시간 전 동기화" 표시 (공단 API 호출 없음은 Cloudflare 로그로 확인)
4. cert_no 없는 호기 선택 → "공단 고유번호 없음" 빈 상태
5. 각 검사건 클릭 → fails 펼침 확인
6. 기존 PDF 업로드 섹션이 카드 아래 그대로 보이는지 확인 (회귀 없음)
</verification>

<success_criteria>
- `npx tsc --noEmit -p .` 0 errors
- `inspect-history.ts` 에 `min_age` 파라미터 + `cached` / `lastFetchedAt` 응답 필드 추가
- `src/utils/inspectHistory.ts` 생성 및 `fetchInspectHistory` export
- `src/components/KoelsaHistorySection.tsx` 생성, props 시그니처 일치
- `ElevatorPage.tsx` 데스크톱(line 861) + 모바일(line 1445) 양쪽 annual 탭 상단에 컴포넌트 배치
- 기존 민원24 카드 + PDF 업로드 섹션 회귀 없음 (해당 코드 라인 미변경)
- React Query staleTime 6h, refetchOnWindowFocus=false
- 401 처리는 기존 `api.ts`의 `req<T>` 경유로 자동 처리 (별도 핸들링 없음)
</success_criteria>

<output>
작업 완료 후 작성: `.planning/quick/260420-n04-inspect-history-ui/260420-n04-SUMMARY.md`

Summary에 포함할 것:
- 변경된 4개 파일 경로
- `min_age` 기본값 21600(6h) 명시
- 배포 명령어: `npm run deploy -- --branch production`
- migration 0067 적용 여부 확인 명령어
- 17대 sync_all=1 초기 동기화 방법 (admin 전용)
- 다음 단계: 프로덕션 배포 후 사용자가 실제 호기에서 검사 기록 탭 검증
</output>
