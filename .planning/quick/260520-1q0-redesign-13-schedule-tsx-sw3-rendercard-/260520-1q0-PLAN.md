---
title: SW3 — 일자 카드 리스트 (renderCard + scheduleListEl + FAB)
phase_id: quick-260520-1q0
phase: quick-260520-1q0
plan: 01
type: execute
wave: 1
depends_on: []
status: ready
files_modified:
  - cha-bio-safety/src/pages/SchedulePage.tsx
autonomous: true
requirements:
  - redesign/13-schedule W7 §3 region.day-cards
  - redesign/13-schedule W7 §6 sub-task SW3
  - W2 OQ #1 LOCKED a (상태 칩 색 매핑 verbatim)
  - W2 OQ #2 LOCKED c (FAB + 모바일 헤더 inline 추가 제거)
  - W2 OQ #3 LOCKED b (멀티데이 시간 자리 텍스트)
must_haves:
  truths:
    - "renderCard 안 모든 옛 alias (var(--bg2/--bg3/--bd/--bd2/--t1/--t2/--t3/--safe/--info/--acl/--danger)) 가 0건"
    - "scheduleListEl 안 모든 옛 alias 가 0건"
    - "모바일 chrome header (line 527~533) 의 '+ 추가' 버튼이 제거됨 + 데스크톱 액션 바 (현 line 485 영역) 의 '+ 추가' 는 유지"
    - "모바일 view 에 FAB 1개 추가 — 우하단 fixed 56px (w-14 h-14) 원형 accent bg + lucide Plus size={24}"
    - "renderCard 카드 1개당 시계 글리프 🕐 0건 (이모지 0 negative gate)"
    - "상태 칩 색이 STATUS_TW 매핑 객체로 Tailwind class 화 — pending=text-text-tertiary / in_progress=text-accent / done=text-status-safe-bar / overdue=text-status-danger-bar"
    - "멀티데이 시간 자리 텍스트 '{startMD} ~ {endMD} ({n}일)' 가 시간 row 안에 표시 (제목 옆 칩 / 메타 row 칩 X)"
    - "tsc --noEmit / npm run build 모두 PASS"
  artifacts:
    - path: "cha-bio-safety/src/pages/SchedulePage.tsx"
      provides: "SW3 변환 완료된 renderCard + scheduleListEl + FAB"
      contains: "STATUS_TW"
  key_links:
    - from: "renderCard"
      to: "handleStatus / setEditItem / handleDelete"
      via: "버튼 onClick — 시그니처 verbatim 보존"
      pattern: "handleStatus\\(item,'done'\\)|setEditItem\\(item\\)|handleDelete\\(item\\)"
    - from: "FAB"
      to: "setShowAdd(true)"
      via: "onClick → modalsEl AddModal trigger"
      pattern: "setShowAdd\\(true\\)"
---

<objective>
SW3 — `SchedulePage.tsx` 의 일자 카드 리스트 영역을 v0.1.1 토큰 + Tailwind 으로 일괄 변환.

**작업 범위:**
1. `renderCard(item, grouped?)` (현재 line 334~375) — 인라인 스타일 → Tailwind class + 동적 hex (cat color) 인라인 유지
2. `scheduleListEl` (현재 line 377~435) — 섹션 헤더 / 로딩 / 빈 상태 / 모바일 리스트 / 데스크톱 카테고리 컬럼 변환
3. 모바일 view 에 FAB 추가 (`!isDesktop` 한정, modalsEl 직전 또는 mobile return 의 mobile root 안)
4. 모바일 chrome header (현재 line 527~533) 의 "+ 추가" 인라인 버튼 제거 → FAB 로 치환
5. 시계 글리프 `🕐` (현재 line 355) 제거 — sketch verbatim (시간 row 단순 텍스트)
6. STATUS_LABEL 정의 (line 90~94) 의 `color: var(--...)` 옛 alias 는 `STATUS_TW` Tailwind class 매핑 객체로 신규 추가 — label 텍스트는 그대로 활용

**Purpose:** W7 checklist §3 region.day-cards / §6 SW3 verbatim 산출. 머지 시 cbc7119-preview.pages.dev 자동 배포.

**Output:** SchedulePage.tsx SW3 영역 변환 + verify gate 23종 중 SW3 관련 14종 PASS.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@CLAUDE.md
@CLAUDE.local.md
@cha-bio-safety/docs/redesign-context/13-schedule/wave-7-tsx-conversion-checklist.md
@cha-bio-safety/docs/redesign-context/13-schedule/sketch-wave-2.html
@cha-bio-safety/docs/redesign-context/13-schedule/tokens.css
@cha-bio-safety/docs/redesign-context/13-schedule/design-system.md
@cha-bio-safety/src/pages/SchedulePage.tsx
@cha-bio-safety/tailwind.config.js

<source-of-truth>
## W7 checklist §3 region.day-cards (verbatim)
SchedulePage.tsx line 333~435 (renderCard + scheduleListEl) 의 인라인 스타일을
v0.1.1 토큰 + Tailwind class 로 변환한다. 시계 글리프는 lucide `<Clock size={10} />`
또는 텍스트 prefix 로 치환 (executor 판단 — sketch W2 는 시계 글리프 없는 단순 텍스트 → 제거 택1).

## W7 §6 SW3 (verbatim)
- renderCard / scheduleListEl 전 영역 변환
- 모바일 chrome header 의 "+ 추가" 버튼 제거 (FAB 로 치환)
- 데스크톱 액션바 헤더의 "+ 추가" 는 유지
- FAB = 우하단 fixed 56px 원형 accent bg, lucide Plus size={24}, mobile only
- STATUS_LABEL → STATUS_TW 매핑 객체 신규 추가 (Tailwind class 화)
- 멀티데이 시간 자리 텍스트 = `{startMD} ~ {endMD} ({n}일)` (제목/메타 row 칩 X, 시간 row 안 텍스트)

## W7 §8 W2 OQ 1/2/3 LOCKED (verbatim)
- **OQ #1 LOCKED a** — 상태 칩 색 = SchedulePage.tsx line 89~94 verbatim
  - 예정(pending) → text-text-tertiary
  - 진행중(in_progress) → text-accent
  - 완료(done) → text-status-safe-bar
  - 지연(overdue) → text-status-danger-bar
- **OQ #2 LOCKED c** — add CTA = FAB (우하단 fixed 56px 원형 accent bg). 헤더 inline + 풀폭 버튼 제거. 데스크톱 헤더 "+ 추가" 만 유지.
- **OQ #3 LOCKED b** — 멀티데이 = 시간 자리 텍스트 "5/12 ~ 5/15 (4일)". 제목 옆 칩 / 메타 row 칩 제거. 시간 row 안에 텍스트 표시.
</source-of-truth>

<sketch-verbatim>
## 카드 외곽 (W2 line 357~358)
```html
<div class="bg-surface-raised border border-border-default rounded-md"
     style="padding:12px 14px;">
```
- `rounded-md-token` → Tailwind `rounded-md` (tailwind.config.js line 92~98: borderRadius.md = 12px)
- `padding:12px 14px` → 인라인 유지 또는 `px-3.5 py-3` (px-3.5 = 14px / py-3 = 12px)

## 카드 row1 chip row (W2 line 359, 369, 384, 397~398)
```html
<div class="flex items-center flex-wrap" style="gap:6px; margin-bottom:6px;">
```
- `gap-1.5 mb-1.5` (gap:6 = 1.5 / mb:6 = 1.5)

## 카테고리 칩 — 동적 hex (W2 line 360~362, 386~388, 399~401)
```html
<span class="text-caption font-semibold rounded-sm"
      style="color:#${cat.color}; background:rgba(${rgb},0.13); padding:2px 8px; line-height:1.4;">
  {cat?.label}
</span>
```
- TSX 변환: `style={{ color: cat?.color, background: \`${cat?.color}22\`, padding:'2px 8px', lineHeight:1.4 }}` — `${cat?.color}22` 동적 hex 인라인 유지 (W7 §10 화이트리스트). 0x22 = 0.133 ≈ 0.13.
- class: `text-caption font-semibold rounded-sm`

## sub-category 칩 inspectionCategory (W2 line 402~404)
```html
<span class="text-caption font-medium rounded-sm"
      style="color:var(--status-info); background:var(--status-info-bg); padding:2px 8px; line-height:1.4;">
  {item.inspectionCategory}
</span>
```
- TSX class: `text-caption font-medium rounded-sm`
- 토큰: tokens.css line 47 (`--status-info`) + line 61 (`--status-info-bg`) 존재 확인. tailwind.config.js line 67 (`text-status-info`) + line 82 (`bg-status-info-bg`) 존재 확인.
- 인라인 잔존 OK: `style={{ padding:'2px 8px', lineHeight:1.4 }}` — 또는 class `px-2 py-0.5 leading-snug` (py:2 = 0.5 = 2px / leading-snug 약 1.375 ≠ 1.4 → 인라인 유지가 정확)

## 상태 칩 (W2 line 363, 389, 405)
```html
<span class="text-caption font-medium" style="margin-left:auto; color:var(--text-tertiary|accent|status-safe|status-danger); line-height:1;">
  {st.label}
</span>
```
- 매핑 객체 신규:
```ts
const STATUS_TW: Record<string, string> = {
  pending:     'text-text-tertiary',
  in_progress: 'text-accent',
  done:        'text-status-safe-bar',
  overdue:     'text-status-danger-bar',
}
```
- class: `text-caption font-medium ml-auto leading-none ${STATUS_TW[item.status] ?? STATUS_TW.pending}`
- **leading-none** 명시 — sketch `line-height:1` = leading-none (feedback_text_caption_leading_none.md 룰)

## 제목 (W2 line 366, 391, 407)
```html
<div class="text-body font-semibold text-text-primary" style="margin-bottom:4px;">
  {item.title}
</div>
```
- class: `text-body font-semibold text-text-primary mb-1` (mb-1 = 4px)
- 단, sketch 는 항상 mb-1 이지만 source line 352 는 memo/time 유무에 따라 조건부 — sketch 우선 (단순 mb-1) OR source 조건부 유지 (executor 판단, mb-1 단순화 권장 — UX 영향 미미)

## 메모 (W2 line 367, 411~414)
- 단일 라인 (W2 line 367 카드 1): `text-caption text-text-secondary` + `style="white-space:pre-line; line-height:1.5"`
- 3-line clamp (W2 line 411~414 카드 3): `text-caption text-text-secondary` + `style="white-space:pre-line; line-height:1.5; display:-webkit-box; -webkit-line-clamp:3; -webkit-box-orient:vertical; overflow:hidden;"`
- TSX: source line 354 의 조건부 grouped (clamp:2 vs clamp:3) 보존. class `text-caption text-text-secondary` + 인라인 `style={{ whiteSpace:'pre-line', lineHeight:1.5, display:'-webkit-box', WebkitLineClamp: grouped ? 2 : 3, WebkitBoxOrient:'vertical', overflow:'hidden' }}`

## 시간 (W2 line 392, 415)
```html
<div class="text-caption text-text-tertiary" style="line-height:1.4; margin-top:4px;">
  09:00
</div>
```
- TSX: class `text-caption text-text-tertiary leading-snug mt-1` (leading-snug ≈ 1.375, sketch 는 1.4 — 인라인 잔존 OK: `style={{ lineHeight:1.4 }}` + class `mt-1`)
- **시계 글리프 🕐 제거** — sketch 에는 없음 (단순 텍스트 "09:00")
- 멀티데이 = 같은 자리에 텍스트 (`item.endDate && item.endDate !== item.date` 조건). 시간이 있는 경우 우선순위는 멀티데이 표시 (현재 source 는 시간만 표시 — 멀티데이 표시 로직은 SW3 신규).
  - 멀티데이 텍스트: ``${item.date.slice(5).replace('-','/')} ~ ${item.endDate.slice(5).replace('-','/')} (${diffDays}일)``
  - 시간 + 멀티데이 둘 다 있으면? — sketch W2 line 442 는 멀티데이 only (시간 미표시). 보수적 선택: 멀티데이가 있으면 멀티데이 텍스트만 + 그렇지 않으면 시간. (UX 결정 권한 executor — 안전 선택)

## 액션 row (W2 line 369~376, 416~424)
```html
<div class="flex" style="gap:6px; margin-top:8px;">
  <!-- 완료 (status !== 'done') -->
  <button class="text-caption font-semibold rounded-sm"
          style="border:1px solid var(--status-safe); background:var(--status-safe-bg); color:var(--status-safe); padding:4px 10px; line-height:1.4;">
    완료
  </button>
  <!-- 수정 -->
  <button class="text-caption font-medium rounded-sm border border-border-default bg-surface-sunken text-text-secondary"
          style="padding:4px 10px; line-height:1.4;">수정</button>
  <!-- 삭제 -->
  <button class="text-caption font-medium rounded-sm border border-border-default bg-surface-sunken text-text-tertiary"
          style="padding:4px 10px; line-height:1.4;">삭제</button>
</div>
```
- class: `flex gap-1.5 mt-2`
- 완료 버튼:
  - tokens.css line 44 (`--status-safe`), line 58 (`--status-safe-bg`) 존재. tailwind.config.js line 65 (`text-safe` = var(--status-safe)) — **`text-safe` Tailwind 패턴 사용 가능** (status- prefix 없는 별칭).
  - 그러나 `text-status-safe-bar` 패턴 룰 (feedback_tailwind_token_class_pattern.md) 와 정합 — `text-safe` 와 `text-status-safe-bar` 둘 다 존재. **sketch verbatim 따라 `var(--status-safe)` (line 44, lighter green) 사용**.
  - 옵션 A (class): `border border-safe bg-safe-bg text-safe` (tailwind.config.js line 65~82: `safe`, `safe-bg` 단축 별칭)
  - 옵션 B (인라인 유지): `style={{ border:'1px solid var(--status-safe)', background:'var(--status-safe-bg)', color:'var(--status-safe)', padding:'4px 10px', lineHeight:1.4 }}`
  - **권장 옵션 A** + 잔존 인라인 `style={{ padding:'4px 10px', lineHeight:1.4 }}` — verify gate §4.1 #5 옛 alias 0건 통과 (var(--status-safe) 는 v0.1.1 토큰이므로 OK 이지만 class 통일이 안전).
  - class: `text-caption font-semibold rounded-sm border border-safe bg-safe-bg text-safe`
- 수정 / 삭제: class `text-caption font-medium rounded-sm border border-border-default bg-surface-sunken text-text-secondary` (수정) / `text-text-tertiary` (삭제) + `style={{ padding:'4px 10px', lineHeight:1.4 }}`

## 섹션 C 헤더 (W2 line 348~351 + scheduleListEl line 379~390)
```html
<div class="flex items-center" style="margin:0 16px 8px; gap:8px;">
  <span class="text-caption font-semibold text-text-secondary" style="line-height:1;">5/5 일정</span>
  <span class="text-caption text-text-tertiary" style="line-height:1;">3건</span>
</div>
```
- sketch margin 16px 는 외곽 컨테이너 (mobile body line 537 `px-4`) 와 중복 — sketch margin 무시, 외곽 padding 만.
- TSX: `<div className="flex items-center gap-2 mb-2">` (gap:8 = 2 / mb:8 = 2)
- 라벨: `text-caption font-semibold text-text-secondary leading-none`
- 건수: `text-caption text-text-tertiary leading-none`
- 우측 데스크톱 "+ 추가" 버튼 (source line 385~390) 유지 — `isDesktop && (...)` 분기 + spacer `<span className="flex-1" />` 또는 `ml-auto`
- 데스크톱 + 추가 버튼 변환: source line 386~390 인라인 → class `ml-auto px-3 py-1 rounded-sm bg-accent text-text-on-accent text-caption font-bold cursor-pointer` (or `text-body-sm` 위 SW1 mobile chrome `text-body-sm font-bold` 패턴 mirror).
  - 권장 mirror SW1 chrome (line 527~533 will-be-deleted) 패턴: `flex items-center gap-1 px-3 py-1 rounded-sm bg-accent text-text-on-accent text-body-sm font-bold cursor-pointer` + lucide `<Plus size={12} />` + "추가"
  - **단, 데스크톱 헤더이므로 "+ 추가" 텍스트 그대로도 OK** (sketch 는 lucide 강제 X). 보수적: text-only "+ 추가" 유지.

## 섹션 H Empty state (W2 line 549~555)
```html
<div class="border border-border-default bg-surface-raised rounded-md text-center"
     style="margin:0 16px; padding:32px 16px;">
  <div class="text-body text-text-tertiary" style="margin-bottom:12px;">등록된 일정이 없습니다</div>
  <button class="text-label font-semibold rounded-sm border border-border-strong bg-surface-sunken text-text-secondary"
          style="padding:10px 20px;">
    + 일정 추가
  </button>
</div>
```
- TSX 컨테이너 (외곽 px-4 와 중복 → margin 무시): `border border-border-default bg-surface-raised rounded-md text-center px-4 py-8`
- 본문: `text-body text-text-tertiary mb-3` (mb:12 = 3)
- 버튼: source line 398~401 인라인 → class `text-label font-semibold rounded-sm border border-border-strong bg-surface-sunken text-text-secondary px-5 py-2.5 cursor-pointer` (py-2.5 = 10px / px-5 = 20px)
- 텍스트 카피 "등록된 일정이 없습니다" + "+ 일정 추가" verbatim (NEGATIVE — §2)

## 섹션 I Loading state (W2 line 561~566)
```html
<div class="text-center text-caption text-text-tertiary"
     style="margin:0 16px; padding:32px 16px; background:var(--surface-raised); border:1px solid var(--border-default); border-radius:12px;">
  불러오는 중...
</div>
```
- TSX: `<div className="text-center text-caption text-text-tertiary px-4 py-8 bg-surface-raised border border-border-default rounded-md">불러오는 중...</div>`
- 텍스트 카피 "불러오는 중..." verbatim (NEGATIVE — §2)

## 섹션 J FAB (W2 line 568~574)
```html
<span style="position:absolute; right:16px; bottom:16px; width:56px; height:56px; border-radius:50%; background:var(--accent); color:#fff; font-size:24px; font-weight:600; line-height:1; display:flex; align-items:center; justify-content:center; box-shadow:0 4px 12px rgba(59,130,246,0.4);">
  +
</span>
```
- TSX (mobile only, !isDesktop):
```tsx
{!isDesktop && (
  <button
    onClick={() => setShowAdd(true)}
    aria-label="일정 추가"
    className="fixed right-4 bottom-4 w-14 h-14 rounded-pill flex items-center justify-center bg-accent text-text-on-accent shadow-lg cursor-pointer z-20"
  >
    <Plus size={24} strokeWidth={2.5} />
  </button>
)}
```
- **w-14 h-14 = 56px** (Tailwind 기본 — config override 영향 없음, 14 는 미 override. feedback_tailwind_w8_h8_is_48px.md 함정 회피)
- `rounded-pill` (radius-pill 99px) 또는 `rounded-full` 둘 다 원형. tailwind.config.js line 97 = `pill: '99px'` → `rounded-pill` 사용.
- `shadow-lg` — Tailwind 기본 shadow. sketch 박스섀도우 `0 4px 12px rgba(59,130,246,0.4)` 와 정확 일치 X 이지만 시각적 근사 OK. 정확 매칭 원하면 `style={{ boxShadow:'0 4px 12px rgba(59,130,246,0.4)' }}` 인라인 잔존.
- lucide `Plus size={24}` — size prop 사용, w-N h-N className 금지 (feedback_tailwind_token_class_pattern.md)
- 배치 위치: mobile return (line 503) 의 root div 안 — modalsEl 호출 직후 또는 직전. `<div className="w-full h-full flex flex-col overflow-hidden bg-surface-page">` 안에 fixed 로 두면 viewport 기준이 아닌 root 기준이지만 root 가 `h-full` 이므로 사실상 viewport. 안전선택: modalsEl 의 fragment 처럼 mobile return 의 마지막 sibling 으로 두기.
</sketch-verbatim>

<negative-scope>
## §2 NEGATIVE — 1 byte 도 변경 금지

- `handleStatus(item, 'done')` 호출 시그니처 (renderCard 안 완료 버튼 onClick)
- `setEditItem(item)` 호출 (수정 버튼 onClick)
- `handleDelete(item)` 호출 (삭제 버튼 onClick) — try/catch toast 카피 변경 금지
- `STATUS_LABEL` label 텍스트 (`예정 / 진행중 / 완료 / 지연`) — color 만 STATUS_TW 로 분리, label 은 그대로 사용
- `item.inspectionCategory` 표시 분기 조건 (`item.inspectionCategory && ...`)
- `catInfo(item.category)` 호출
- `STATUS_LABEL[item.status] ?? STATUS_LABEL.pending` 패턴 (STATUS_TW 도 동일 패턴 적용)
- 빈 상태 카피 "등록된 일정이 없습니다" + "+ 일정 추가"
- 로딩 상태 카피 "불러오는 중..."
- 데스크톱 카테고리별 컬럼 그룹 로직 (line 405~427) — 변환만, 비즈 동일 (groups 객체 / Object.entries / map)
- `dayItems.length` / `isLoading` / `isDesktop` 분기 조건
- 카드 외곽 `grouped` prop 동작 (height:130 / flex-direction:column 등 grouped 전용 스타일은 인라인 유지 또는 className 분기로 보존)
- 데스크톱 액션바 헤더 "+ 추가" 버튼 (source line 385~390) — 변환은 OK, 제거 X
- mobile chrome header (line 507~534) 의 다른 버튼 (뒤로가기 / 엑셀 다운로드) — line 527~533 의 "+ 추가" 만 제거
</negative-scope>

<tasks>

<task type="auto">
  <name>Task 1: renderCard + scheduleListEl + FAB 일괄 변환 + 시계 글리프 / 모바일 헤더 + 추가 제거</name>
  <files>cha-bio-safety/src/pages/SchedulePage.tsx</files>
  <behavior>
    - 변환 후 grep:
      - `grep -nE "var\(--bg2\)|var\(--bg3\)|var\(--bd[^-]|var\(--bd2\)|var\(--t1\)|var\(--t2\)|var\(--t3\)|var\(--safe\)|var\(--info\)|var\(--acl\)|var\(--danger\)" SchedulePage.tsx | grep -E "^[3-4][0-9][0-9]:"` → 0 hit (renderCard + scheduleListEl 영역)
      - `grep -nE "🕐|🕑|🕒|🕓" SchedulePage.tsx` → 0 hit
      - `grep -nE "w-14 h-14|rounded-pill|<Plus size=\{24\}" SchedulePage.tsx` → 1 hit (FAB)
      - `grep -nE "STATUS_TW" SchedulePage.tsx` → 매핑 객체 정의 + 사용처 최소 2 hit
      - `grep -nE "+ 추가" SchedulePage.tsx` → mobile chrome header (line 527~533) 영역 제거 → 데스크톱 헤더 (scheduleListEl 내부) 1 hit 잔존 + 빈 상태 "일정 추가" 1 hit
    - tsc --noEmit PASS
    - npm run build PASS
  </behavior>
  <action>
**작업 위치:** `cha-bio-safety/src/pages/SchedulePage.tsx`

**Step 1 — STATUS_TW 매핑 객체 추가 (STATUS_LABEL 직후, line 95 영역):**

```ts
// W2 OQ #1 LOCKED a — 상태 칩 Tailwind class 매핑
const STATUS_TW: Record<string, string> = {
  pending:     'text-text-tertiary',
  in_progress: 'text-accent',
  done:        'text-status-safe-bar',
  overdue:     'text-status-danger-bar',
}
```

STATUS_LABEL 정의 자체는 label 만 사용 (color 필드는 더 이상 참조 X — 그러나 NEGATIVE 룰 따라 정의는 그대로 보존, 사용처만 STATUS_TW 로 갈음).

**Step 2 — renderCard 변환 (line 334~375 전체 교체):**

```tsx
const renderCard = (item: ScheduleItem, grouped?: boolean) => {
  const cat = catInfo(item.category)
  const st  = STATUS_LABEL[item.status] ?? STATUS_LABEL.pending
  const stTw = STATUS_TW[item.status] ?? STATUS_TW.pending

  // 멀티데이 텍스트 (시간 자리에 표시)
  const isMultiDay = !!item.endDate && item.endDate !== item.date
  let multiDayText: string | null = null
  if (isMultiDay) {
    const startMD = item.date.slice(5).replace('-', '/')
    const endMD   = item.endDate!.slice(5).replace('-', '/')
    const sd = new Date(item.date + 'T00:00:00')
    const ed = new Date(item.endDate! + 'T00:00:00')
    const diffDays = Math.round((ed.getTime() - sd.getTime()) / 86400000) + 1
    multiDayText = `${startMD} ~ ${endMD} (${diffDays}일)`
  }

  return (
    <div
      key={item.id}
      className="bg-surface-raised border border-border-default rounded-md"
      style={{
        padding: '12px 14px',
        ...(grouped ? { height: 130, display: 'flex', flexDirection: 'column' as const } : {}),
      }}
    >
      {/* row1 — chip row */}
      <div className="flex items-center flex-wrap gap-1.5 mb-1.5">
        {!grouped && (
          <span
            className="text-caption font-semibold rounded-sm"
            style={{ color: cat?.color, background: `${cat?.color}22`, padding: '2px 8px', lineHeight: 1.4 }}
          >
            {cat?.label}
          </span>
        )}
        {item.inspectionCategory && (
          <span
            className="text-caption font-medium rounded-sm text-status-info bg-status-info-bg"
            style={{ padding: '2px 8px', lineHeight: 1.4 }}
          >
            {item.inspectionCategory}
          </span>
        )}
        <span className={`text-caption font-medium ml-auto leading-none ${stTw}`}>{st.label}</span>
      </div>

      {/* 제목 */}
      <div className="text-body font-semibold text-text-primary" style={{ marginBottom: (item.memo || item.time || multiDayText) ? 4 : 0 }}>
        {item.title}
      </div>

      {/* 메모 + 시간/멀티데이 */}
      <div style={{ flex: grouped ? 1 : undefined, minHeight: 0, overflow: 'hidden' }}>
        {item.memo && (
          <div
            className="text-caption text-text-secondary"
            style={{
              whiteSpace: 'pre-line',
              lineHeight: 1.5,
              display: '-webkit-box',
              WebkitLineClamp: grouped ? 2 : 3,
              WebkitBoxOrient: 'vertical' as any,
              overflow: 'hidden',
              marginBottom: (item.time || multiDayText) ? 4 : 0,
            }}
          >
            {item.memo}
          </div>
        )}
        {multiDayText ? (
          <div className="text-caption text-text-tertiary" style={{ lineHeight: 1.4 }}>{multiDayText}</div>
        ) : item.time ? (
          <div className="text-caption text-text-tertiary" style={{ lineHeight: 1.4 }}>{item.time}</div>
        ) : null}
      </div>

      {/* 액션 row */}
      <div className="flex gap-1.5" style={{ marginTop: grouped ? 'auto' : 8, paddingTop: grouped ? 4 : 0 }}>
        {item.status !== 'done' && (
          <button
            onClick={() => handleStatus(item, 'done')}
            className="text-caption font-semibold rounded-sm border border-safe bg-safe-bg text-safe cursor-pointer"
            style={{ padding: '4px 10px', lineHeight: 1.4 }}
          >
            완료
          </button>
        )}
        <button
          onClick={() => setEditItem(item)}
          className="text-caption font-medium rounded-sm border border-border-default bg-surface-sunken text-text-secondary cursor-pointer"
          style={{ padding: '4px 10px', lineHeight: 1.4 }}
        >
          수정
        </button>
        <button
          onClick={() => handleDelete(item)}
          className="text-caption font-medium rounded-sm border border-border-default bg-surface-sunken text-text-tertiary cursor-pointer"
          style={{ padding: '4px 10px', lineHeight: 1.4 }}
        >
          삭제
        </button>
      </div>
    </div>
  )
}
```

**주의:** `🕐` 글리프 제거 — sketch verbatim (단순 텍스트 "09:00").
**주의:** 멀티데이 표시는 W2 OQ #3 LOCKED b verbatim — 시간 자리 텍스트만 (제목 옆 칩 / 메타 row 칩 X). 시간과 멀티데이 둘 다 있으면 멀티데이 우선 (sketch 카드 4 = 멀티데이 only).

**Step 3 — scheduleListEl 변환 (line 377~435 전체 교체):**

```tsx
const scheduleListEl = (
  <>
    {/* 섹션 C — 선택 일자 헤더 */}
    <div className="flex items-center gap-2 mb-2">
      <span className="text-caption font-semibold text-text-secondary leading-none">
        {selDate === today ? '오늘' : `${selDate.slice(5).replace('-', '/')}`} 일정
      </span>
      <span className="text-caption text-text-tertiary leading-none">{dayItems.length}건</span>
      <span className="flex-1" />
      {isDesktop && (
        <button
          onClick={() => setShowAdd(true)}
          className="px-3 py-1 rounded-sm bg-accent text-text-on-accent text-caption font-bold cursor-pointer leading-none"
        >
          + 추가
        </button>
      )}
    </div>

    {isLoading ? (
      <div className="text-center text-caption text-text-tertiary px-4 py-8 bg-surface-raised border border-border-default rounded-md">
        불러오는 중...
      </div>
    ) : dayItems.length === 0 ? (
      <div className="border border-border-default bg-surface-raised rounded-md text-center px-4 py-8">
        <div className="text-body text-text-tertiary mb-3">등록된 일정이 없습니다</div>
        <button
          onClick={() => setShowAdd(true)}
          className="text-label font-semibold rounded-sm border border-border-strong bg-surface-sunken text-text-secondary cursor-pointer"
          style={{ padding: '10px 20px' }}
        >
          + 일정 추가
        </button>
      </div>
    ) : isDesktop ? (
      /* 데스크톱: 카테고리별 컬럼 */
      <div className="flex flex-wrap gap-3 items-start">
        {(() => {
          const groups: Record<string, ScheduleItem[]> = {}
          for (const item of dayItems) {
            const key = item.category
            if (!groups[key]) groups[key] = []
            groups[key].push(item)
          }
          return Object.entries(groups).map(([catKey, items]) => {
            const cat = catInfo(catKey)
            return (
              <div key={catKey} style={{ flex: '0 0 auto', width: 300, minWidth: 0 }}>
                <div
                  className="text-caption font-bold mb-1.5"
                  style={{
                    color: cat?.color,
                    padding: '3px 0',
                    borderBottom: `2px solid ${cat?.color}44`,
                  }}
                >
                  {cat?.label} ({items.length})
                </div>
                <div className="flex flex-col gap-1.5">
                  {items.map(item => renderCard(item, true))}
                </div>
              </div>
            )
          })
        })()}
      </div>
    ) : (
      <div className="flex flex-col gap-2">
        {dayItems.map(item => renderCard(item))}
      </div>
    )}
  </>
)
```

**Step 4 — 모바일 chrome header "+ 추가" 버튼 제거 (현재 line 527~533):**

다음 블록을 통째 삭제:

```tsx
<button
  onClick={() => setShowAdd(true)}
  className="flex items-center gap-1 px-3.5 py-1.5 rounded-sm bg-accent text-text-on-accent text-body-sm font-bold cursor-pointer"
>
  <Plus size={14} />
  추가
</button>
```

`<Plus />` import 는 FAB 에서도 사용하므로 import 라인 (SW1 에서 lucide 도입 완료) 은 그대로 유지.

**Step 5 — FAB 추가 (mobile return 안, modalsEl 직전 또는 직후):**

mobile return (현재 line 504~544) 의 root `<div className="w-full h-full flex flex-col overflow-hidden bg-surface-page">` 안, `{modalsEl}` 직전에 추가:

```tsx
{/* FAB — W2 OQ #2 LOCKED c, mobile only */}
<button
  onClick={() => setShowAdd(true)}
  aria-label="일정 추가"
  className="fixed right-4 bottom-4 w-14 h-14 rounded-pill flex items-center justify-center bg-accent text-text-on-accent cursor-pointer z-20"
  style={{ boxShadow: '0 4px 12px rgba(59,130,246,0.4)' }}
>
  <Plus size={24} strokeWidth={2.5} />
</button>
```

- mobile return 안에 있으므로 `isDesktop` 조건 불필요 (이미 mobile 분기). 단 BottomNav 와 겹치지 않는지 확인 필요 — BottomNav 미존재 시 bottom-4 OK. (SchedulePage 는 BottomNav 미적용 페이지 — App.tsx 라우팅 확인 후 결정. BottomNav 있으면 `bottom-20` 정도로 조정).
- z-20: modalsEl 보다 낮게 (modal overlay 가 더 위). modalsEl 가 fixed z-50 사용한다면 FAB z-20 안전.

**Step 6 — Step 1~5 완료 후 verify gate 실행 (verify 섹션 참조).**
  </action>
  <verify>
    <automated>cd cha-bio-safety && npx tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <done>
- SchedulePage.tsx line 334~435 영역에서 모든 옛 alias `var(--bg2|bg3|bd|bd2|t1|t2|t3|safe|info|acl|danger)` 가 0건
- `🕐` 글리프 0건
- FAB 1개 추가됨 (`w-14 h-14 rounded-pill ... bg-accent ... <Plus size={24}`)
- 모바일 chrome header (구 line 527~533) 의 "+ 추가" 버튼 제거됨
- 데스크톱 헤더 (scheduleListEl 안 isDesktop 분기) 의 "+ 추가" 버튼 유지됨
- STATUS_TW 매핑 객체 정의 + renderCard 안 사용
- npx tsc --noEmit PASS
- npm run build PASS
  </done>
</task>

<task type="auto">
  <name>Task 2: verify gate 실행 + git commit</name>
  <files>cha-bio-safety/src/pages/SchedulePage.tsx</files>
  <action>
**Step 1 — verify gate § 4 grep 명령 (W7 checklist verbatim, SW3 영역 = line 333~445 + mobile return FAB):**

작업 디렉토리는 워크트리 루트 (`/Users/jykevin/Documents/cbc7119-design`). 모든 grep 은 `cha-bio-safety/src/pages/SchedulePage.tsx` 대상.

```bash
# §4.1 #1 — 이모지 0 (시계 글리프 제거 검증)
grep -nP "[\x{1F300}-\x{1FAFF}]|[\x{2600}-\x{27BF}]" cha-bio-safety/src/pages/SchedulePage.tsx | grep -v '^\s*//\|^\s*#'
# 기대: 0 hit (또는 SW1/SW2 잔존이 있다면 SW3 영역 line 333~445 + mobile return FAB 자리 = 0 hit)

# §4.1 #3 — 잘못된 status- prefix 0 (text-status-fire 같은 패턴)
grep -nE "text-status-(fire|safe|warning|info|danger)[^-]" cha-bio-safety/src/pages/SchedulePage.tsx | grep -v '^#'
# 기대: 0 hit (정확 패턴은 -bar 또는 -bg 접미사 필수)

# §4.1 #4 — linear-gradient 0
grep -nE "linear-gradient" cha-bio-safety/src/pages/SchedulePage.tsx | grep -v '^#' | wc -l
# 기대: 0

# §4.1 #5 — 옛 alias 0건 (SW3 영역 한정)
sed -n '333,445p' cha-bio-safety/src/pages/SchedulePage.tsx | grep -E "var\(--(bg2|bg3|bd|bd2|t1|t2|t3|safe|info|acl|danger)[^-]" | wc -l
# 기대: 0

# §4.1 #8 — w-8 h-8 confusion 0 (FAB 는 w-14 h-14)
grep -nE "w-8 h-8" cha-bio-safety/src/pages/SchedulePage.tsx | grep -v '^#' | wc -l
# 기대: 0

# §4.2 #9 — surface 토큰 ≥10 hits (전체 파일)
grep -vE "^\s*//|^\s*\*|^\s*#" cha-bio-safety/src/pages/SchedulePage.tsx | grep -cE "(surface-raised|surface-sunken|surface-page|surface-active)"
# 기대: ≥10

# §4.2 #10 — text 토큰 ≥30 hits
grep -vE "^\s*//|^\s*\*|^\s*#" cha-bio-safety/src/pages/SchedulePage.tsx | grep -cE "text-text-(primary|secondary|tertiary|on-accent)"
# 기대: ≥30

# §4.2 #11 — border 토큰 ≥10 hits
grep -vE "^\s*//|^\s*\*|^\s*#" cha-bio-safety/src/pages/SchedulePage.tsx | grep -cE "border-(border-default|border-strong)"
# 기대: ≥10

# §4.2 #12 — accent 토큰 ≥3 (FAB + 데스크톱 헤더 + 상태 칩)
grep -vE "^\s*//|^\s*\*|^\s*#" cha-bio-safety/src/pages/SchedulePage.tsx | grep -cE "(bg-accent|text-accent)"
# 기대: ≥3

# §4.2 #13 — status-safe-bar ≥2 (완료 칩 + 완료 버튼은 text-safe 별칭이지만 STATUS_TW done 매핑은 status-safe-bar)
grep -vE "^\s*//|^\s*\*|^\s*#" cha-bio-safety/src/pages/SchedulePage.tsx | grep -cE "status-safe-bar"
# 기대: ≥1 (STATUS_TW done — status-danger-bar 도 동일 패턴)

# §4.2 #16 — leading-none/tight ≥5
grep -vE "^\s*//|^\s*\*|^\s*#" cha-bio-safety/src/pages/SchedulePage.tsx | grep -cE "leading-(none|tight)"
# 기대: ≥5

# §4.2 #17 — lucide Plus 사용 (FAB 안)
grep -nE "<Plus size=\{24\}" cha-bio-safety/src/pages/SchedulePage.tsx
# 기대: 1 hit (FAB)

# Additional — STATUS_TW 정의 존재
grep -nE "const STATUS_TW" cha-bio-safety/src/pages/SchedulePage.tsx
# 기대: 1 hit

# Additional — 시계 글리프 0
grep -nE "🕐|🕑|🕒|🕓|🕔" cha-bio-safety/src/pages/SchedulePage.tsx | wc -l
# 기대: 0

# Additional — 모바일 chrome header "+ 추가" 버튼 제거 검증
# mobile header 영역 (현재 line 507~534) 안에 setShowAdd 가 없어야 함
awk 'NR>=505 && NR<=540' cha-bio-safety/src/pages/SchedulePage.tsx | grep -E "setShowAdd|추가" | wc -l
# 기대: 0 (라인 범위는 변환 후 실제 위치 기준 조정 필요. 정확히는: "월간 점검 계획" title 직후 mobile header 영역에 setShowAdd 0)
```

**Step 2 — §4.3 #19 + #20 빌드 검증:**

```bash
cd cha-bio-safety && npx tsc --noEmit 2>&1 | tail -30
# 기대: error 0

cd cha-bio-safety && npm run build 2>&1 | tail -30
# 기대: vite build success
```

**Step 3 — git status 확인 후 commit (사용자 컨펌 받은 후):**

```bash
git status
git diff cha-bio-safety/src/pages/SchedulePage.tsx | head -200
```

사용자가 시각 검수 OK 라면 commit:

```bash
git add cha-bio-safety/src/pages/SchedulePage.tsx
git commit -m "$(cat <<'EOF'
tsx(13-schedule): SW3 — 일자 카드 리스트 + FAB

- renderCard / scheduleListEl 인라인 → v0.1.1 토큰 + Tailwind 변환
- STATUS_TW 매핑 객체 추가 (W2 OQ #1 LOCKED a verbatim)
- 모바일 FAB 추가 (W2 OQ #2 LOCKED c, w-14 h-14 rounded-pill bg-accent)
- 모바일 chrome header "+ 추가" 버튼 제거 (FAB 로 치환)
- 멀티데이 시간 자리 텍스트 (W2 OQ #3 LOCKED b verbatim)
- 시계 글리프 🕐 제거 (sketch verbatim)
- 데스크톱 헤더 "+ 추가" 버튼 유지

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

**NEVER 룰 reminder:**
- wrangler 명령 절대 X
- npm run deploy X
- main 머지 → cbc7119-preview.pages.dev 자동 (사용자가 머지 시점 결정, executor 가 머지 자율 X)
  </action>
  <verify>
    <automated>cd cha-bio-safety && npx tsc --noEmit 2>&1 | tail -5 && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
- §4.1 #1/#3/#4/#5/#8 negative gate 모두 0
- §4.2 #9~#17 quantitative gate 모두 ≥ threshold
- §4.3 #19 tsc --noEmit PASS
- §4.3 #20 npm run build PASS
- 모바일 chrome header "+ 추가" 제거 확인
- FAB 1개 추가 확인
- 시계 글리프 0건 확인
- 변경사항 git status 로 사용자에게 노출 (commit 은 사용자 컨펌 후)
  </done>
</task>

</tasks>

<memory-rules-quoted>
## 강제 적용 메모리 룰 5건 (inline 인용)

### 1. feedback_planner_prompt_sketch_verbatim.md
> 변환 wave 시 sketch CSS 정의를 grep 으로 추출해 그대로 인용. 추측한 토큰명/사이즈는 deviation 유발 (03-qr-scan 6건 사례)

**적용:** 본 PLAN 의 `<sketch-verbatim>` 섹션은 W2 sketch (line 354~574) 의 CSS 정의를 그대로 인용. executor 는 추측 X — 인용한 패턴 그대로 사용.

### 2. feedback_tsx_wave_emoji_dot_gap.md
> alias sed-replace 만 하지 말고 sketch negative gate (이모지 0) + dot span 추가 markup 도 verify.

**적용:** §4.1 #1 이모지 0 gate + 시계 글리프 `🕐` 제거 명시 + verify gate 의 `🕐|🕑|🕒|🕓|🕔` grep 검증.

### 3. feedback_tailwind_token_class_pattern.md
> status- prefix 없음 (text-fire-bar O / text-status-fire-bar X) + lucide size={N} prop. 11-div TSX v3 hotfix(4ce707e) 사고 방지.

**적용:** STATUS_TW 매핑은 `text-status-safe-bar` / `text-status-danger-bar` (v0.1.1 정확 패턴). lucide `<Plus size={24}>` size prop 사용, w-N h-N className 금지 (FAB 컨테이너 자체는 w-14 h-14 = 56px, 아이콘은 size prop).
**비고:** tailwind.config.js 에 `safe` / `safe-bar` / `safe-bg` 별칭 모두 존재 — 완료 버튼은 `border-safe bg-safe-bg text-safe` 별칭 사용 (sketch verbatim `var(--status-safe)` 와 동일). 상태 칩은 STATUS_TW 매핑이 `text-status-safe-bar` 명시 (verify gate §4.2 #13 검증 대상).

### 4. feedback_tailwind_w8_h8_is_48px.md
> tailwind.config spacing override: w-8=48 (기본 32 아님), w-7=32. 11-div 백버튼 1.5배 사고(54a1c8d) 직후 박제.

**적용:** FAB 56px = `w-14 h-14`. tailwind.config.js spacing override 는 `'7':'32px','8':'48px'` 만 — `14` 는 Tailwind 기본 (3.5rem = 56px) 유지. w-8 h-8 사용 금지 (48px 함정).

### 5. feedback_text_caption_leading_none.md
> text-caption lh:1.5 (18px) 가 h-8(32px) 컨테이너 안에서도 시각적 패딩. 헤더 토글/배지/칩 같은 작은 영역은 leading-none 명시.

**적용:** 상태 칩 (`leading-none`) + 데스크톱 헤더 "+ 추가" 버튼 (`leading-none`) + 섹션 C 라벨/건수 (`leading-none`) — verify gate §4.2 #16 ≥5 hits.
</memory-rules-quoted>

<threat_model>
N/A — UI 변환 작업. 백엔드/auth/네트워크 영향 0.
</threat_model>

<verification>
## 최종 verify 체크리스트

### Negative gates (§4.1 — 0 이어야 함)
- [ ] §4.1 #1 이모지 0 (시계 글리프 🕐 제거 검증)
- [ ] §4.1 #3 잘못된 status- prefix 0
- [ ] §4.1 #4 linear-gradient 0
- [ ] §4.1 #5 SW3 영역 (line 333~445) 옛 alias 0
- [ ] §4.1 #8 w-8 h-8 confusion 0

### Quantitative gates (§4.2 — 임계치 이상)
- [ ] §4.2 #9 surface 토큰 ≥10
- [ ] §4.2 #10 text 토큰 ≥30
- [ ] §4.2 #11 border 토큰 ≥10
- [ ] §4.2 #12 accent 토큰 ≥3
- [ ] §4.2 #13 status-safe-bar ≥1 (STATUS_TW done 매핑)
- [ ] §4.2 #16 leading-none/tight ≥5
- [ ] §4.2 #17 lucide Plus 1 hit (FAB)

### Build gates (§4.3)
- [ ] §4.3 #19 npx tsc --noEmit PASS
- [ ] §4.3 #20 npm run build PASS

### SW3 전용 검증
- [ ] STATUS_TW 매핑 정의 존재
- [ ] FAB 1개 추가 (`w-14 h-14 rounded-pill bg-accent`)
- [ ] 모바일 chrome header "+ 추가" 버튼 제거
- [ ] 데스크톱 헤더 "+ 추가" 버튼 유지
- [ ] 멀티데이 시간 자리 텍스트 표시 (제목/메타 row 칩 X)
- [ ] handleStatus / setEditItem / handleDelete 시그니처 보존
</verification>

<success_criteria>
SW3 완료 = SchedulePage.tsx line 333~445 영역 + mobile return 의 FAB + 모바일 chrome header "+ 추가" 제거가 변환되어:
1. 옛 alias 0
2. 시계 글리프 0
3. 이모지 0
4. FAB 정상 동작 (setShowAdd(true) trigger)
5. 모든 verify gate 14종 PASS
6. tsc + build PASS
7. 사용자가 시각 검수 후 commit OK 신호 송신
</success_criteria>

<output>
After completion, create `.planning/quick/260520-1q0-redesign-13-schedule-tsx-sw3-rendercard-/260520-1q0-SUMMARY.md` documenting:
- 변환된 영역 (renderCard / scheduleListEl / mobile return FAB / mobile chrome header 정리)
- verify gate 결과 (모두 PASS / 일부 미달 시 상세)
- 변경된 코드 라인 범위 (변환 전 line 333~435 + 527~533 → 변환 후 line 범위)
- 발견된 OQ 또는 deviation (없으면 명시)
</output>
