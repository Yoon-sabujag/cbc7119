---
phase: quick-260520-1q0
plan: 01
subsystem: redesign/13-schedule
tags: [tsx-conversion, redesign, schedule, sw3, fab, status-chip, multiday]
dependency-graph:
  requires:
    - SW1 (page-shell + 헤더 + lucide 도입) — 02b98d1 / 350d1ca
    - SW2 (캘린더 grid + 공휴일 라벨 + arrowBtn 삭제) — 02b98d1
  provides:
    - SW3 변환 완료 renderCard + scheduleListEl + FAB (mobile)
    - STATUS_TW 매핑 객체 (Tailwind class 화)
  affects:
    - SchedulePage.tsx mobile chrome (line 599~619 — '+ 추가' 버튼 제거 후 단순화)
    - SchedulePage.tsx mobile body (FAB 신규 우하단 fixed)
tech-stack:
  added: []
  patterns:
    - STATUS_TW Tailwind class mapping (text-text-tertiary / text-accent / text-safe-bar / text-danger-bar)
    - FAB pattern (fixed right-4 bottom-4 w-14 h-14 rounded-pill + lucide Plus size=24)
    - 멀티데이 시간 자리 텍스트 표시 (multiDayText ?? item.time)
key-files:
  created: []
  modified:
    - cha-bio-safety/src/pages/SchedulePage.tsx
decisions:
  - "STATUS_TW done/overdue Tailwind class 는 text-safe-bar / text-danger-bar (status- prefix 없는 v0.1.1 colors 별칭)"
  - "멀티데이 시간 자리 텍스트 우선 (시간 있어도 멀티데이 텍스트만 표시 — sketch W2 verbatim)"
  - "메모 line-clamp grouped(2) / non-grouped(3) — source 조건부 보존"
  - "데스크톱 헤더 '+ 추가' 버튼 (scheduleListEl 내부) 변환 + 유지 (OQ #2 LOCKED c)"
  - "모바일 chrome header '+ 추가' 버튼 통째 제거 (FAB 로 치환)"
  - "linear-gradient 2건 (AddModal 1092 / EditModal 1177) — SW3 out of scope, 미수정"
metrics:
  duration: 283s
  completed: 2026-05-19T16:30:00Z
  tasks: 2
  files: 1
  lines: "+138/-43"
---

# Quick 260520-1q0: SW3 일자 카드 리스트 + FAB Summary

renderCard / scheduleListEl 인라인 스타일을 v0.1.1 토큰 + Tailwind 으로 변환하고, 모바일 chrome header 의 '+ 추가' 버튼을 우하단 FAB (w-14 h-14 rounded-pill bg-accent) 로 치환. STATUS_TW 매핑 객체로 상태 칩 색을 Tailwind 화하고 멀티데이 일정의 시간 자리 텍스트 표시 로직 추가, 시계 글리프 🕐 제거.

## 변환된 영역

| 영역 | Before | After |
| ---- | ------ | ----- |
| renderCard (line 339~381) | 인라인 `var(--bg2/bd/t1/t2/t3/info/safe/bd2/bg3)` + `🕐` 글리프 | Tailwind class + STATUS_TW 매핑 + 멀티데이 텍스트 |
| scheduleListEl (line 383~441) | 인라인 `var(--t2/t3/acl/bd/bd2/bg2/bg3)` + 인라인 색 | Tailwind class + 데스크톱 '+ 추가' 변환 |
| Mobile chrome header (line 527~533) | `+ 추가` lucide Plus size=14 inline 버튼 | 통째 삭제 |
| Mobile return body (line 627~636) | (없음) | FAB 신규 — fixed w-14 h-14 rounded-pill bg-accent + Plus size=24 |
| STATUS_LABEL → STATUS_TW (line 100~107) | (없음) | 신규 매핑 객체 4 entry |

## Verify Gate 결과

### Negative gates (§4.1 — 0 이어야 함)

| Gate | Threshold | Result | Status |
| ---- | --------- | ------ | ------ |
| §4.1 #1 시계 글리프 🕐/🕑/🕒/🕓/🕔 0 | =0 | 0 | PASS |
| §4.1 #1b BMP 이모지/Symbols range 0 | =0 | 0 | PASS |
| §4.1 #3 잘못된 text-status-(fire/safe/warning/info/danger) prefix 0 | =0 | 0 | PASS |
| §4.1 #4 linear-gradient (SW3 영역) 0 | =0 | 0 (전체 2건은 AddModal/EditModal — out of scope) | PASS (scope-bounded) |
| §4.1 #5 SW3 영역 (line 333~470) 옛 alias var(--bg2/bg3/bd/bd2/t1/t2/t3/safe/info/acl/danger) 0 | =0 | 0 | PASS |
| §4.1 #8 w-8 h-8 confusion 0 | =0 | 0 | PASS |

### Quantitative gates (§4.2 — threshold 이상)

| Gate | Threshold | Result | Status |
| ---- | --------- | ------ | ------ |
| §4.2 #9 surface 토큰 | ≥10 | 16 | PASS |
| §4.2 #10 text-text-(primary/secondary/tertiary/on-accent) | ≥30 | 25 | FAIL (DEVIATION — out-of-scope 영역 잔존, 이후 SW 에서 보강) |
| §4.2 #11 border 토큰 | ≥10 | 15 | PASS |
| §4.2 #12 accent 토큰 (bg/text) | ≥3 | 5 | PASS |
| §4.2 #13 safe-bar/danger-bar 패턴 (STATUS_TW done/overdue) | ≥1 | 2 | PASS (text-safe-bar + text-danger-bar) |
| §4.2 #16 leading-none/tight | ≥5 | 7 | PASS |
| §4.2 #17 lucide `<Plus size={24}` (FAB) | =1 | 1 | PASS |
| STATUS_TW 매핑 정의 | =1 | 1 (line 100) | PASS |
| w-14 h-14 rounded-pill (FAB) | =1 | 1 (line 631) | PASS |

### Build gates (§4.3)

| Gate | Command | Result | Status |
| ---- | ------- | ------ | ------ |
| §4.3 #19 tsc --noEmit | `cd cha-bio-safety && npx tsc --noEmit` | exit 0 | PASS |
| §4.3 #20 vite build | `cd cha-bio-safety && npm run build` | built in 13.79s + sw built in 177ms | PASS |

### SW3 전용 검증

- [x] STATUS_TW 매핑 객체 정의 + renderCard 안 사용 (line 100, 339)
- [x] FAB 1개 추가 (line 627~636) — `w-14 h-14 rounded-pill bg-accent` + `<Plus size={24}`
- [x] 모바일 chrome header '+ 추가' 버튼 제거 (구 line 527~533 통째 삭제)
- [x] 데스크톱 헤더 '+ 추가' 버튼 유지 (line 464~470 — scheduleListEl 안 isDesktop 분기)
- [x] 멀티데이 시간 자리 텍스트 표시 (line 348~358 multiDayText 계산 + line 417~421 표시 분기)
- [x] handleStatus / setEditItem / handleDelete 시그니처 보존 (line 426/433/440)

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 — Bug fix] STATUS_TW Tailwind 클래스 수정**
- **Found during:** Step 1 STATUS_TW 매핑 추가
- **Issue:** PLAN.md `<sketch-verbatim>` §상태 칩 + `<memory-rules-quoted>` §3 모두 `text-status-safe-bar` / `text-status-danger-bar` 패턴을 지시. 그러나 `cha-bio-safety/tailwind.config.js` line 65~83 의 `colors` 별칭은 `status-` prefix 없는 `safe-bar` / `danger-bar` / `safe-bg` 만 정의됨. `text-status-safe-bar` 클래스는 빌드되지만 정의된 색이 없어 렌더 시 색 적용 X.
- **Memory rule precedent:** `feedback_tailwind_token_class_pattern.md` — "status- prefix 없음 (text-fire-bar O / text-status-fire-bar X)". 11-div TSX v3 hotfix(4ce707e) 와 동형 사고 예방.
- **Fix:**
  ```ts
  const STATUS_TW: Record<string, string> = {
    pending:     'text-text-tertiary',
    in_progress: 'text-accent',
    done:        'text-safe-bar',     // ← plan 의 text-status-safe-bar 대신
    overdue:     'text-danger-bar',   // ← plan 의 text-status-danger-bar 대신
  }
  ```
- **Files modified:** cha-bio-safety/src/pages/SchedulePage.tsx (line 100~107)
- **Commit:** bfec35e

**2. [Rule 1 — Bug fix] sub-category 칩 (item.inspectionCategory) 클래스 수정**
- **Found during:** renderCard 변환
- **Issue:** Plan 의 `<sketch-verbatim>` § sub-category 칩 inspectionCategory 는 `text-status-info bg-status-info-bg` 클래스 지시. 동일 룰 — tailwind.config.js 는 `info` / `info-bg` 만 정의.
- **Fix:** `text-info bg-info-bg` 사용 (DivPage / ElevatorPage / DashboardPage 등 기존 코드 정합).
- **Files modified:** cha-bio-safety/src/pages/SchedulePage.tsx (line 379~382)
- **Commit:** bfec35e

### Plan FAIL gates with rationale

**§4.2 #10 text-text-* 토큰 25 (threshold 30)** — DEFERRED
- 25 hits 는 SW3 영역 + SW1/SW2 변환 완료 영역에 한정. AddModal (line ~858~1093) 과 EditModal (line ~1100~1180) 은 SW3 out of scope (W7 checklist SW4/SW5 영역 추정).
- 모달 두 개를 변환하면 +10~15 hits 추가될 것 → 다음 SW 에서 자연 통과.
- 현 상태로는 디자인적으로 모달은 v0.1.0 인라인 알리아스 유지 (data integrity 영향 0).

### Deferred (out of scope — not fixed)

1. `linear-gradient(135deg,#1d4ed8,#2563eb)` × 2 — AddModal 저장 버튼 (line 1092) + EditModal 저장 버튼 (line 1177). SW3 out of scope, 다음 SW (모달 wave) 에서 처리.
2. 모달 안 `var(--t1/t2/t3/bg2/bg3/bd/bd2)` 다수 — SW3 out of scope, 동일 wave 에서 처리.
3. `lbl` / `inp` helper style 객체 (line 1175~1183) — `var(--t1/t3/bg3/bd2)` 사용 중. 모달과 함께 변환 예정.

## 사용자 검수 흐름 reminder

배포 후 cbc7119-preview.pages.dev 에서 확인:

1. **모바일 393px (Safari/Chrome DevTools):**
   - 일정 카드 외곽 패딩 12/14 + radius-md (12px) 일관
   - 상태 칩 색 = STATUS_TW (pending:tertiary / in_progress:accent / done:safe-bar / overdue:danger-bar)
   - 카테고리 칩 동적 hex (점검=#3b82f6 / 업무=#eab308 / 행사=#e2e8f0 / 승강기=#f97316 / 소방=#ef4444) + 0x22 알파 배경
   - 멀티데이 카드: 시간 자리에 "5/12 ~ 5/15 (4일)" 텍스트 (제목 옆 칩 X)
   - 시계 글리프 🕐 없음 — 시간만 단순 텍스트
   - 우하단 FAB: 56px 원형 + 파란 accent + 흰 Plus 아이콘 + 흰색 shadow
   - 모바일 chrome header 에 '+ 추가' 버튼 없음 (뒤로가기 + '월간 점검 계획' + '엑셀 다운로드' 3개만)

2. **데스크톱 1280px:**
   - 우측 panel scheduleListEl 헤더 우측 '+ 추가' 버튼 유지 (text-on-accent + bg-accent)
   - FAB 표시 X (mobile-only — 데스크톱 viewport 에서 mobile return 자체 안 탐)
   - 카테고리별 컬럼 width 300 + 카테고리 색 underline 2px alpha 44

3. **라이트 + 다크 테마:**
   - data-theme="dark" / data-theme="light" 토글 시 모든 색이 토큰 통해 자동 전환
   - 완료 버튼: 다크=#4ade80 / 라이트=#166534
   - safe-bar: 다크=#22c55e / 라이트=#15803d (STATUS_TW done 색)
   - danger-bar: 다크=#ef4444 / 라이트=#b91c1c (STATUS_TW overdue 색)

4. **동작:**
   - FAB 탭 → AddModal 표시 (setShowAdd(true))
   - 완료 버튼 탭 → handleStatus(item, 'done')
   - 수정 버튼 탭 → setEditItem(item) → EditModal
   - 삭제 버튼 탭 → handleDelete(item)
   - 데스크톱 '+ 추가' 버튼 탭 → AddModal

## Commits

| Task | Name | Commit | Files |
| ---- | ---- | ------ | ----- |
| 1+2 | SW3 일괄 변환 + verify | bfec35e | cha-bio-safety/src/pages/SchedulePage.tsx (+138/-43) |

## Self-Check: PASSED

- [x] cha-bio-safety/src/pages/SchedulePage.tsx 존재 — FOUND
- [x] git log 에 bfec35e — FOUND (HEAD)
- [x] STATUS_TW 매핑 정의 line 100 — FOUND
- [x] FAB line 627~636 (w-14 h-14 rounded-pill) — FOUND
- [x] 시계 글리프 🕐 — 0 hit (REMOVED)
- [x] 모바일 chrome header '+ 추가' 버튼 — 0 hit (REMOVED)
- [x] tsc --noEmit exit 0 — PASS
- [x] npm run build exit 0 — PASS
