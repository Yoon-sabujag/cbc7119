---
phase: quick
plan: 01
type: execute
quick_id: 260517-e9t
depends_on: [260517-e9c, 260517-e9d, 260517-e9r, 260517-e9e, 260517-e9k, 260517-e9s]
files_modified:
  - cha-bio-safety/src/pages/ExtinguishersListPage.tsx
must_haves:
  truths:
    - "옛 alias (--bg/bg2/bg3/bg4/bd/bd2/t1/t2/t3/acl/info/warn/danger 단독) 0건"
    - "raw hex / rgba 0건"
    - "fontSize 9/10/11 0건 (마지노선 12)"
    - "비즈니스 로직 (state machine / mutation / fromMarker / 4종 confirm / replaceFilter / gridCols) 0 변경"
    - "TypeScript build PASS"
---

# 토큰 매핑 테이블 (sketch wave 1~6 verbatim)

## Alias 일괄 치환 (Edit replace_all)

| 옛 | 신 |
|---|---|
| `'var(--bg)'` | `'var(--surface-page)'` |
| `'var(--bg2)'` | `'var(--surface-raised)'` |
| `'var(--bg3)'` | `'var(--surface-sunken)'` |
| `'var(--bg4)'` | `'var(--surface-active)'` |
| `'var(--bd)'` | `'var(--border-default)'` |
| `'var(--bd2)'` | `'var(--border-strong)'` |
| `'var(--t1)'` | `'var(--text-primary)'` |
| `'var(--t2)'` | `'var(--text-secondary)'` |
| `'var(--t3)'` | `'var(--text-tertiary)'` |
| `'var(--acl)'` | `'var(--accent)'` |
| `'var(--info)'` | `'var(--status-info)'` |
| `'var(--warn)'` | `'var(--status-warning)'` |
| `'var(--danger)'` | `'var(--status-danger)'` |

## Raw hex / rgba 치환 (호출 컨텍스트 정확)

| 옛 | 신 | 컨텍스트 |
|---|---|---|
| `'#fff'` | `'var(--accent-fg)'` | 등록/저장/스왑/폐기 primary 버튼 텍스트 |
| `'#a16207'` | `'var(--status-warning)'` | warn chip 텍스트 (chrome) |
| `'#c2410c'` | `'var(--status-fire)'` | imminent chip 텍스트 (chrome) |
| `'#b91c1c'` | `'var(--status-danger)'` | danger chip 텍스트 (chrome) |
| `'rgba(59,130,246,.15)'` | `'var(--accent-bg)'` | 배치됨 badge bg |
| `'rgba(239,68,68,.15)'` | `'var(--status-danger-bg)'` | 미배치 badge bg, warning chip danger bg |
| `'rgba(239,68,68,.08)'` | `'var(--status-danger-bg)'` | action danger 버튼 bg |
| `'rgba(239,68,68,.3)'` | `'var(--status-danger-bar)'` | danger 보더 |
| `'rgba(245,158,11,.15)'` | `'var(--status-warning-bg)'` | 폐기 badge bg, warning chip bg |
| `'rgba(245,158,11,.3)'` | `'var(--status-warning-bar)'` | warning chip 보더 |
| `'rgba(234,179,8,.08)'` | `'var(--status-warning-bg)'` | chrome chip warn inactive |
| `'rgba(234,179,8,.12)'` | `'var(--status-warning-bg)'` | sketch 통일 |
| `'rgba(234,179,8,.25)'` | `'var(--status-warning-bg)'` | chrome chip warn active |
| `'rgba(234,179,8,.5)'` | `'var(--status-warning-bar)'` | active border |
| `'rgba(249,115,22,.08)'` | `'var(--status-fire-bg)'` | imminent chip inactive |
| `'rgba(249,115,22,.25)'` | `'var(--status-fire-bg)'` | imminent chip active |
| `'rgba(249,115,22,.5)'` | `'var(--status-fire-bar)'` | imminent active border |
| `'rgba(14,165,233,.10)'` | `'var(--status-info-bg)'` | 마커 배너 bg |
| `'rgba(14,165,233,.25)'` | `'var(--status-info-bar)'` | 마커 배너 border |
| `'rgba(14,165,233,.35)'` | `'var(--status-info-bar)'` | 마커 배너 취소 버튼 border |
| `'rgba(0,0,0,0.6)'` | `'var(--surface-overlay)'` | ModalBackdrop bg |

## fontSize 10/11 → 12 (마지노선)

- chrome chip fontSize 11 → 12
- 카드 badge fontSize 11 → 12
- location/warning chip fontSize 11 → 12
- 카드 footer fontSize 10 → 12
- DetailField fontSize 11 → 12
- counter chip fontSize 11 → 12
- microcopy fontSize 11 → 12
- modal info banner fontSize 11 → 12
- FieldLabel fontSize 11 → 12
- "폐기된 자산입니다" fontSize 11 → 12

## borderRadius 5/6/7/10/12/16 → 토큰

- radius 5 (badge / chip) → `'var(--radius-sm)'`
- radius 6 (chrome chip / 취소 버튼 border) → `'var(--radius-sm)'`
- radius 7 (헤더 portal 등록 버튼 / port table) → `'var(--radius-sm)'`
- radius 8 (input / select / action button) → `'var(--radius-sm)'`
- radius 10 (confirm 버튼) → `'var(--radius-md)'`
- radius 12 (카드 / "다시 시도" 버튼 margin) → `'var(--radius-md)'`
- radius 16 (모달 wrapper) → `'var(--radius-lg)'`

## SKELETON_STYLE 특수 처리

```ts
// 변환 전:
const SKELETON_STYLE: React.CSSProperties = {
  background: 'var(--bg3)',
  borderRadius: 12,
  height: 88,
  animation: 'blink 2s ease-in-out infinite',
}
// 변환 후:
const SKELETON_STYLE: React.CSSProperties = {
  background: 'var(--surface-sunken)',
  borderRadius: 'var(--radius-md)',
  height: 88,
  animation: 'blink 2s ease-in-out infinite',
}
```

## counterChipStyle 특수 처리 (EditModal)

```ts
// 변환 전:
background: changedCount === 0 ? 'var(--bg3)' : changedCount <= 3 ? 'rgba(59,130,246,.15)' : 'rgba(239,68,68,.15)',
color:      changedCount === 0 ? 'var(--t3)' : changedCount <= 3 ? 'var(--acl)' : 'var(--danger)',

// 변환 후:
background: changedCount === 0 ? 'var(--surface-sunken)' : changedCount <= 3 ? 'var(--accent-bg)' : 'var(--status-danger-bg)',
color:      changedCount === 0 ? 'var(--text-tertiary)' : changedCount <= 3 ? 'var(--accent)' : 'var(--status-danger)',
```

## borderForField 함수 특수 처리

```ts
// 변환 전:
const borderForField = (original, current) =>
  norm(current) !== norm(original) ? 'var(--acl)' : 'var(--bd2)'

// 변환 후:
const borderForField = (original, current) =>
  norm(current) !== norm(original) ? 'var(--accent)' : 'var(--border-strong)'
```

## ConfirmModal primary 톤 분기

```ts
// 변환 전:
background: primaryStyle === 'acl' ? 'var(--acl)' : 'var(--danger)',
color: '#fff',

// 변환 후:
background: primaryStyle === 'acl' ? 'var(--accent)' : 'var(--status-danger-bar)',
color: 'var(--accent-fg)',
```

## EditModal disabled 톤 (saveDisabled)

```ts
// 변환 전:
{ ...primaryBtnStyle, background: 'var(--bd2)', color: 'var(--t3)', cursor: 'not-allowed', border: '1px solid rgba(239,68,68,.3)' }

// 변환 후:
{ ...primaryBtnStyle, background: 'var(--border-strong)', color: 'var(--text-tertiary)', cursor: 'not-allowed', border: '1px solid var(--status-danger-bar)' }
```

## RegisterModal disabled 톤 (submitting)

```ts
// 변환 전:
{ ...primaryBtnStyle, background: 'var(--bd2)', color: 'var(--t3)', cursor: 'not-allowed' }
// 변환 후:
{ ...primaryBtnStyle, background: 'var(--border-strong)', color: 'var(--text-tertiary)', cursor: 'not-allowed' }
```

## 비즈니스 로직 보존 (0 변경)

- import / hooks / state / useQuery / mutations
- handleRegister / handleAssignClick / dismissMarkerContext
- getMappingState / getReplaceWarning
- replaceFilter / replaceCounts
- norm() / changedCount useMemo / saveDisabled / borderForField
- ConfirmModalProps / 4 호출처
- gridCols 반응형
- localStorage lastReg
- toast / navigate / setSearchParams

## Verify gate (변환 후)

1. `grep -E "var\(--(bg2?|bg3|bg4|bd2?|t1|t2|t3|acl|safe\)|warn\)|danger\)|info\))" ExtinguishersListPage.tsx` → empty
2. `grep -E "#[0-9a-fA-F]{3,6}\b" ExtinguishersListPage.tsx` → empty
3. `grep -E "rgba\(" ExtinguishersListPage.tsx` → empty
4. `grep -E "fontSize: (9|10|11)\b" ExtinguishersListPage.tsx` → empty
5. `npx tsc --noEmit` → PASS
