---
phase: quick-260519-4iu
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cha-bio-safety/docs/redesign-context/13-schedule/sketch-wave-1.html
autonomous: true
requirements:
  - REDESIGN-13-W1
must_haves:
  truths:
    - "사용자는 단일 .html 파일을 브라우저로 열어 393px 모바일 뷰포트 + 다크 모드 기본 화면을 즉시 본다"
    - "사용자는 우상단 토글을 눌러 다크/라이트 모드를 전환하며, 두 모드 모두에서 모든 색이 tokens.css 변수로 분기된다"
    - "사용자는 자체 헤더에서 페이지 제목 '월간 점검 계획'을 본다 (App.tsx 헤더 숨김 패턴 적용 — 자체 헤더 통일 룰)"
    - "사용자는 월/연도 네비게이션 (‹ / › 화살표 + 'YYYY년 M월' 라벨 + 오늘 버튼)을 본다"
    - "사용자는 7×6 캘린더 그리드를 본다 (요일 헤더 일~토, 일자 셀, 카테고리 dot 5종, 공휴일 빨간색, 오늘/선택 표식, 멀티데이 범위 띠)"
    - "캘린더 카테고리 dot 5종 색은 SchedulePage.tsx line 81~87 SCHED_CATEGORIES verbatim — inspect #3b82f6 / task #eab308 / event #e2e8f0 / elevator #f97316 / fire #ef4444"
    - "데스크톱(lg: ≥768px) 분기 시 페이지 패딩이 16→24px 로 자동 분기되는 것을 확인할 수 있다 (tokens.css media query 동작 verbatim)"
  artifacts:
    - path: "cha-bio-safety/docs/redesign-context/13-schedule/sketch-wave-1.html"
      provides: "Schedule page 헤더 + 월 네비 + 캘린더 grid v0.1.1 정적 시안"
      min_lines: 350
  key_links:
    - from: "sketch-wave-1.html <head>"
      to: "tokens.css line 16~197 + typography.css line 28~95"
      via: "inline <style> 블록에 verbatim 복사 (12-staff sketch 패턴)"
      pattern: "--surface-page.*#0a0d12"
    - from: "sketch-wave-1.html category dots"
      to: "SchedulePage.tsx line 82~86 SCHED_CATEGORIES"
      via: "dot color hex 5종 verbatim"
      pattern: "#3b82f6.*#eab308.*#e2e8f0.*#f97316.*#ef4444"
    - from: "sketch-wave-1.html 다크/라이트 토글 버튼"
      to: "document.documentElement.dataset.theme"
      via: "vanilla JS onclick 한 줄"
      pattern: "data-theme"
---

<objective>
redesign/13-schedule 의 첫 시안 wave. 페이지 상단 chrome (자체 헤더 + 월/연도 네비게이션 + 캘린더 그리드) 만 정적 HTML 로 출력. 사용자가 브라우저로 열어 시각 컨펌 후 다음 wave (캘린더 아래 영역들) 로 진행.

Purpose: 12-staff-service 와 동일한 영역별 sketch → 사용자 컨펌 → TSX 변환 워크플로우 (메모리 룰 project_redesign_workflow.md / feedback_design_sketch_first.md) 진입. 이번 wave 에서 캘린더 grid + 카테고리 dot 체계 + 공휴일 색 + 다크/라이트 모드를 한 번에 확정.

Output: `cha-bio-safety/docs/redesign-context/13-schedule/sketch-wave-1.html` (자급 단일 파일)

Out of scope (다음 wave): 일자 선택 시 하단 일정 카드 / 미리보기 (MonthlyPlanPreview) / AddModal / EditModal / 자동 생성 버튼 / 데스크톱 우측 컬럼.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@cha-bio-safety/docs/redesign-context/13-schedule/13-schedule.md
@cha-bio-safety/docs/redesign-context/13-schedule/design-system.md
@cha-bio-safety/docs/redesign-context/13-schedule/SchedulePage.tsx
@cha-bio-safety/docs/redesign-context/13-schedule/tokens.css
@cha-bio-safety/docs/redesign-context/13-schedule/typography.css

<interfaces>
<!-- SchedulePage.tsx 에서 추출한 비즈니스 상수 + 핵심 분기 룰. sketch 는 이 패턴을 시각적으로 mirror 한다. -->

### A. SCHED_CATEGORIES (SchedulePage.tsx line 81~87, verbatim)
```ts
const SCHED_CATEGORIES: { value: ScheduleCategory; label: string; color: string }[] = [
  { value:'inspect',  label:'점검',  color:'#3b82f6' },  // 파랑
  { value:'task',     label:'업무',  color:'#eab308' },  // 노랑
  { value:'event',    label:'행사',  color:'#e2e8f0' },  // 흰회 (다크 모드용. 라이트 모드는 회색 처리 필요 — open question)
  { value:'elevator', label:'승강기', color:'#f97316' },  // 주황
  { value:'fire',     label:'소방',  color:'#ef4444' },  // 빨강
]
```

### B. WEEK_DAYS (line 96)
```ts
const WEEK_DAYS = ['일','월','화','수','목','금','토']
```
- 일요일 dow===0 → `#ef4444` (빨강)
- 토요일 dow===6 → `var(--acl)` = accent 파랑
- 그 외 평일 → `var(--t3)` = text-tertiary

### C. matchesDate / 멀티데이 룰 (line 154~162, verbatim)
```ts
// 범위 일정: 시작일~종료일 사이 날짜에 표시
// 단일 일자(end_date 없음)는 사용자가 명시적으로 그 날에 잡은 거라 주말/공휴일이라도 표시.
// 멀티데이 범위는 주말·공휴일을 자동 제외 — DB 는 단일 항목이지만 표시만 스킵.
const matchesDate = (item, d) => {
  if (d < item.date || d > (item.endDate ?? item.date)) return false
  if (!item.endDate || item.endDate === item.date) return true
  const dow = new Date(d + 'T00:00:00').getDay()
  if (dow === 0 || dow === 6) return false
  if (holidays[d]) return false
  return true
}
```

### D. 일자 셀 markup (line 257~290, verbatim 추출)
- 셀 wrapper: `display:flex flex-col items-center gap-2 padding:5px 0 7px cursor:pointer`
- 선택 상태: `borderRadius:8 background:rgba(59,130,246,0.15) border:2px solid #3b82f6`
- 일자 숫자: 12px / fontWeight 700 if today|sel else 400, color 분기 (sel→accent / today→#fff / red(일요일·공휴일)→#ef4444 / 토요일→accent / else→text-primary)
- 오늘 마커: 24×24 원형 배경 accent
- 카테고리 dot: 4×4 원, 최대 3개 (slice(0,3)), gap 2, 컨테이너 height 4 (고정)

### E. 공휴일 라벨 (line 295~299, verbatim)
```tsx
{holidays[selDate] && (
  <div style={{ marginBottom:8, fontSize:11, color:'#ef4444', fontWeight:600 }}>
    {holidays[selDate]}
  </div>
)}
```
→ sketch v0.1.1 격상: fontSize 11 → **12 (text-caption)**

### F. 노안 친화 격상 (메모리 룰)
- 요일 헤더 source 10px → 12px (text-caption)
- 일자 숫자 source 12px → **12px 유지** (text-caption) — `leading-none` 명시 (feedback_text_caption_leading_none.md)
- 공휴일 라벨 source 11px → 12px (text-caption)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: sketch-wave-1.html 작성 — 헤더 + 월 네비 + 캘린더 그리드 (다크/라이트 토글 + 393px 모바일 frame + lg: 데스크톱 분기 표시)</name>
  <files>cha-bio-safety/docs/redesign-context/13-schedule/sketch-wave-1.html</files>
  <action>
**단일 자급 HTML 파일을 작성한다.** 12-staff-service sketch (특히 02-calendar-grid-sketch.html) 의 구조 패턴을 따른다.

### 1. 파일 구조 (skeleton)
```html
<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>13 월간 점검 계획 — sketch W1 헤더+월네비+캘린더 grid</title>
  <link href="https://cdn.jsdelivr.net/gh/orioncactus/pretendard@v1.3.9/dist/web/variable/pretendardvariable.min.css" rel="stylesheet" />
  <script src="https://cdn.tailwindcss.com"></script>
  <style>
    /* tokens.css line 16~197 verbatim 복사 (다크 기본 + [data-theme="light"] override + media (min-width:768px)) */
    /* typography.css line 13~95 verbatim 복사 (.text-caption ~ .text-display + .text-mono 등) */
    /* tailwind 토큰 매핑은 design-system §3.2 + 11-div hotfix 룰 — class 직접 매핑 X, var() 만 사용 */
  </style>
  <script>
    // tailwind config: surface/text/status/duty 토큰 매핑 (12-staff sketch 패턴 verbatim)
    tailwind.config = {
      theme: {
        extend: {
          colors: {
            'surface-page':    'var(--surface-page)',
            'surface-raised':  'var(--surface-raised)',
            'surface-sunken':  'var(--surface-sunken)',
            'surface-active':  'var(--surface-active)',
            'text-primary':    'var(--text-primary)',
            'text-secondary':  'var(--text-secondary)',
            'text-tertiary':   'var(--text-tertiary)',
            'accent':          'var(--accent)',
            'safe':            'var(--status-safe)',
            'safe-bar':        'var(--status-safe-bar)',
            'danger':          'var(--status-danger)',
            'danger-bar':      'var(--status-danger-bar)',
            'warning':         'var(--status-warning)',
            'warning-bar':     'var(--status-warning-bar)',
            'info':            'var(--status-info)',
            'info-bar':        'var(--status-info-bar)',
            'fire':            'var(--status-fire)',
            'fire-bar':        'var(--status-fire-bar)',
          },
          borderRadius: { 'sm-token': '8px', 'md-token': '12px', 'lg-token': '16px', 'pill': '99px' },
        },
      },
    }
  </script>
</head>
<body class="bg-zinc-900 min-h-screen p-6">
  <!-- 우상단 다크/라이트 토글 -->
  <!-- 시안 본체: 다크 frame + 라이트 frame side-by-side (12-staff W1 패턴) -->
</body>
</html>
```

### 2. 시안 frame 레이아웃
- **모바일 frame 2개** (다크 + 라이트, side-by-side, flex gap-8 flex-wrap)
- 각 frame: width 393px, border, rounded-lg, overflow-hidden
- frame 안 background = surface-page (data-theme 분기로 자동)
- **데스크톱 frame 1개** (다크만, lg:block 으로 화면 폭 ≥1024px 일 때만 표시) — width 1280px, viewport 분기 시각 확인용
  - 동일 캘린더 컴포넌트지만 `--page-padding` 가 24px 로 자동 분기되는 것을 시각적으로 확인 가능해야 함
  - **데스크톱 frame 은 좌측 캘린더 컬럼만 그린다 (우측 컬럼은 다음 wave). placeholder dashed callout 으로 우측 영역 비워둘 것**

### 3. Frame 내부 구조 (위→아래, 모바일 393px 기준)

#### 3.1 자체 헤더 (h-12, 통일 룰)
- 높이 48px, bg-surface-page, border-b border-border-default
- 좌측: 백 버튼 자리 placeholder (32×32 rounded bg-surface-raised, ChevronLeft SVG 15px — lucide stroke 1.5)
- 중앙: 페이지 제목 "월간 점검 계획" (text-title 18px font-medium text-text-primary)
- 우측: 액션 자리 (이번 wave 는 빈 32×32 placeholder; "자동 생성" 버튼은 다음 wave)
- 자체 헤더 통일 룰 메모리: project_redesign_self_header_chrome.md verbatim 적용 — bg-surface-raised + w-8 h-8 rounded-[7px] (w-8/h-8 은 tailwind config 미override 시 기본 32px 인데, 본 sketch 는 config override 없으므로 w-8=32px OK; tokens override 함정 메모리 feedback_tailwind_w8_h8_is_48px.md 는 *프로덕션 tailwind.config* 한정. sketch 는 CDN 기본값 사용 → w-8=32px 정상)
- **잠재 함정**: sketch CDN tailwind 는 spacing override 없음 → w-8=32px. TSX 변환 wave 에서는 config override 로 w-8=48px 이 되므로 별도 px 명시 필요. **이번 sketch 는 명시적 `w-[32px] h-[32px]` 또는 `style="width:32px;height:32px"` 사용 권장** (혼동 방지)

#### 3.2 월/연도 네비게이션 (h-auto, padding 12 16)
- flex items-center justify-between
- 좌: ‹ 버튼 — 36×36 rounded-md border border-border-default bg-surface-raised text-text-primary text-xl
- 중: "2026년 5월" — text-title font-semibold text-text-primary
  - + 우측 작은 "오늘로" 버튼 (h-7 rounded-sm-token border border-border-default bg-surface-raised text-caption text-text-secondary px-2.5, leading-none 적용)
  - 또는 "오늘" 칩 (text-caption text-accent border border-accent rounded-pill px-2.5 py-0.5)
- 우: › 버튼 — 동일
- **세부 결정**: "오늘" 버튼은 중앙 라벨 옆이 아닌 우측 끝 (› 버튼 안쪽) 에 둘지 사용자 컨펌 필요 → 시안에는 *중앙 라벨 우측에 작은 칩* 으로 그린다 (사용자가 위치 컨펌 후 다음 wave 또는 수정)

#### 3.3 캘린더 본체 (mx-page-padding, mt-3)
- 카드 wrapper: bg-surface-raised rounded-md-token border border-border-default overflow-hidden
- **요일 헤더** (h-9, grid grid-cols-7 border-b border-border-default)
  - 7 cell, text-caption font-semibold text-center leading-none, padding-y 8px
  - 일(0번): `text-danger` (var(--status-danger))
  - 토(6번): `text-accent`
  - 평일(1~5): `text-text-tertiary`
- **일자 그리드** (grid grid-cols-7)
  - 6주 × 7일 = 42 cell, aspect-square (정사각형 — 12-staff W2 룰 일관성)
  - cell wrapper: flex flex-col items-center justify-start gap-1 pt-1.5 pb-2 cursor-pointer relative
  - 일자 숫자: text-caption (12px) **leading-none** (1자릿수 패딩 함정 회피 — feedback_text_caption_leading_none.md), font-weight: 400 (today/sel 만 700), color 분기:
    - 선택 셀: text-accent + font-bold
    - 오늘 (선택 아님): 흰색 (#fff) on accent 원형 배경 (w-6 h-6 rounded-full bg-accent flex items-center justify-center, 즉 일자 span 을 24×24 원으로 감싸기)
    - 일요일 또는 공휴일: text-danger
    - 토요일: text-accent
    - 평일 기본: text-text-primary
    - 이전/다음 달 회색 셀: text-text-disabled (또는 빈 cell)
  - 선택 셀 윤곽: 셀 내부에 rounded-sm-token border-2 border-accent bg-accent/15 (rgba(59,130,246,0.15)) — 셀 전체가 아닌 일자+dot 영역 박스. **셀 자체 aspect-square 유지** (12-staff W2 패치 룰 mirror)
  - **카테고리 dot row** (mt-auto, height 6px, flex gap-1 items-center)
    - 최대 3개 (source slice(0,3)) — 4×4 원 (rounded-full)
    - 색은 SCHED_CATEGORIES verbatim hex 5종 (#3b82f6 / #eab308 / #e2e8f0 / #f97316 / #ef4444)
    - **라이트 모드 호환**: event(#e2e8f0) 는 라이트 배경에서 거의 안 보임 → 라이트 모드 한정 darker variant 정의 필요. **sketch 에 두 가지 hex 모두 표시** (다크/라이트 frame 분리되어 있으므로 시각 확인 가능) — open question 으로 README 식 주석에 명기:
      ```
      /* OQ #1 (사용자 컨펌 대기): event 카테고리 dot color
         다크: #e2e8f0 (소스 verbatim)
         라이트: ??? (#e2e8f0 은 흰 배경에서 안 보임)
         후보 a: 라이트 한정 #94a3b8 (slate-400)
         후보 b: 다크/라이트 공통 var(--text-secondary)
         후보 c: 카테고리 의미 변경 — event 색 자체 재선정
      */
      ```
    - 다크 frame 에는 #e2e8f0 그대로, 라이트 frame 에는 임시로 #94a3b8 로 표시 + 상단에 OQ 주석 carded.

#### 3.4 캘린더 상단 우측 — 카테고리 범례 (이번 wave 포함 여부)
- **포함하지 않음**. 범례는 캘린더 *아래* placement (12-staff W3 mirror) → 다음 wave 에서 처리.
- 단, **캘린더 하단에 dashed dotted "다음 wave 영역" placeholder** 1개 만 (12-staff W1 패턴): `border-2 border-dashed border-border-default rounded-md p-4 text-center text-caption text-text-tertiary` 안에 "↓ 다음 wave: 일자별 일정 카드 + 미리보기 + 범례 + CTA"

#### 3.5 공휴일 라벨 (선택 셀이 공휴일일 때)
- mock data 에서 선택 셀을 5월 5일 (어린이날) 로 잡아 라벨 렌더링 검증
- 캘린더 카드 아래 (또는 캘린더 내부 마지막 row 아래) 에 표시: text-caption text-danger font-semibold padding-left 2px margin-bottom 2

#### 3.6 멀티데이 범위 표시
- mock item 1개 추가: "소방 종합정밀점검 5/12~5/15" — 평일 4일 연속, fire 카테고리 (#ef4444)
- 각 일자에 동일 카테고리 dot 표시 (matchesDate 룰 시각 mirror)
- **범위 띠** (band) 를 일자 셀 하단에 가로로 깔지는 *시안에서 판단* — 소스 코드는 dot 만 사용. **시안에는 dot 만 표시** + OQ #2 에 "멀티데이 일정을 범위 띠로 시각화할지" 사용자 컨펌 항목 적기.

### 4. Realistic Mock Data (2026년 5월 기준)
- curMonth: "2026-05"
- 오늘: 2026-05-19 (현재 날짜)
- selDate: 2026-05-05 (어린이날 — 공휴일 라벨 검증용)
- holidays (HOLIDAYS_FALLBACK 시뮬레이션):
  - "2026-05-01": "근로자의 날"
  - "2026-05-05": "어린이날"
  - "2026-05-25": "부처님 오신 날" (5월 25일 일요일 가정 — 실제 확인 불필요, mock)
- dots (dotMap mock):
  - 5/4 월: [inspect, task]
  - 5/5 화: [event] (어린이날이지만 단일 일자 schedule 은 표시되는 룰 mirror)
  - 5/12~5/15 (화수목금): [fire] 4일 연속 (멀티데이)
  - 5/18 월: [elevator]
  - 5/19 화 (오늘): [inspect, fire, task] (3개 max)
  - 5/20 수: [inspect, task, event, elevator] → slice(0,3) 으로 [inspect, task, event] 만 표시 (4번째 잘림 시각 검증)
  - 5/25 월: 공휴일 + [event] (단일 일자라 표시되는 룰 검증)
  - 5/27 수: [task]
- 5월 1일은 금요일 (Date.UTC 검증: 2026-05-01 = Fri). 5월은 31일까지 → grid 6주 × 7일 = 42 cell 안에 들어감 (앞 4 cell empty 또는 4월 잔여, 마지막 4 cell empty 또는 6월 시작).

### 5. 다크/라이트 토글
- 우상단 fixed 버튼 (top-4 right-4)
- 클릭 시 `document.documentElement.dataset.theme = 'light'|'dark'` 토글 + 양쪽 frame 의 wrapper 도 동일 dataset 분기
- **모바일 frame 2개는 항상 다크+라이트 side-by-side 로 동시 표시** (토글과 무관) — 12-staff W1 패턴
- 토글은 페이지 전체 배경 (body bg-zinc-900 vs bg-zinc-100) + 데스크톱 frame 한정 영향
- 토글 버튼 자체: text-caption rounded-pill border px-3 py-1 — "다크 / 라이트" 라벨

### 6. 강제 검증 (작성 후 self-check, executor 가 직접 grep)
```bash
F=cha-bio-safety/docs/redesign-context/13-schedule/sketch-wave-1.html

# A. 이모지 0건 (feedback_tsx_wave_emoji_dot_gap.md)
# 한글/숫자/기호 외 일반 유니코드 픽토그램 차단. ‹›·× 같은 punctuation 은 OK.
# 아래 grep 패턴은 흔히 쓰이는 이모지 범위 차단:
[[ $(grep -cE '[\U0001F300-\U0001F9FF\U0001FA00-\U0001FAFF\U00002600-\U000027BF]' "$F") -eq 0 ]] || echo "FAIL: emoji detected"
# (한국어 환경에서 위 grep 가 작동 안 하면 git grep 또는 python: python3 -c "import re,sys;t=open('$F').read();print(len(re.findall(r'[\U0001F300-\U0001F9FF\U0001FA00-\U0001FAFF\U00002600-\U000027BF]',t)))")

# B. 9·10·11px 0건 (단, font-size 한정. opacity/line-height 등 다른 속성은 허용)
[[ $(grep -nE 'font-size:\s*(9|10|11)px|fontSize:\s*(9|10|11)\b|text-\[1[01]px\]|text-\[9px\]' "$F" | grep -v '^\s*/\*' | grep -v '^\s*\*' | grep -cE 'font-size|fontSize|text-') -eq 0 ]] || echo "FAIL: 9/10/11px font-size detected"

# C. status- prefix className 0건 (feedback_tailwind_token_class_pattern.md)
[[ $(grep -nE 'text-status-|bg-status-|border-status-' "$F" | grep -v '^\s*/\*' | grep -v '^\s*\*' | wc -l) -eq 0 ]] || echo "FAIL: status- prefix className"

# D. SCHED_CATEGORIES 5 hex 모두 포함
for h in '#3b82f6' '#eab308' '#e2e8f0' '#f97316' '#ef4444'; do
  [[ $(grep -c "$h" "$F") -gt 0 ]] || echo "FAIL: missing category hex $h"
done

# E. WEEK_DAYS 7개 한글 모두 포함
for d in '일' '월' '화' '수' '목' '금' '토'; do
  [[ $(grep -c "$d" "$F") -gt 0 ]] || echo "FAIL: missing week day $d"
done

# F. data-theme 토글 동작
grep -q "data-theme" "$F" || echo "FAIL: no data-theme toggle"

# G. 페이지 제목 verbatim
grep -q "월간 점검 계획" "$F" || echo "FAIL: page title missing"

# H. 다크 + 라이트 frame 동시 존재
[[ $(grep -c 'data-theme="dark"' "$F") -ge 2 ]] || echo "WARN: dark frame count low (root + frame)"
[[ $(grep -c 'data-theme="light"' "$F") -ge 1 ]] || echo "FAIL: light frame missing"

# I. tokens.css 표식 (verbatim copy 검증)
grep -q -- '--surface-page' "$F" || echo "FAIL: tokens.css not embedded"
grep -q -- '--status-fire-bar' "$F" || echo "FAIL: status-fire-bar var missing"

# J. typography.css 표식
grep -q '.text-caption' "$F" || echo "FAIL: typography.css not embedded"

# K. 멀티데이 mock (fire 카테고리 4일 연속) 의도 주석 또는 데이터 존재
grep -qE '5/12|소방 종합정밀점검|멀티데이' "$F" || echo "WARN: multiday mock missing"

# L. OQ (open question) 명시
grep -qE 'OQ\s*#?[0-9]|open question|사용자 컨펌' "$F" || echo "WARN: no OQ note"

# M. SchedulePage.tsx line 81~87 SCHED_CATEGORIES label 5종 모두 노출
for l in '점검' '업무' '행사' '승강기' '소방'; do
  [[ $(grep -c "$l" "$F") -gt 0 ]] || echo "WARN: category label missing $l"
done
```

### 7. 시안 내부 주석 (HTML <!-- --> 또는 <style> /* */)
- 파일 최상단에 다음 메타 주석:
  ```html
  <!--
    sketch-wave-1.html — redesign/13-schedule W1
    작성: 2026-05-19 / quick 260519-4iu
    범위: 자체 헤더 + 월/연도 네비 + 캘린더 grid (요일 헤더 + 일자 셀 + 카테고리 dot + 공휴일 + 선택/오늘)
    out of scope: 일정 카드 / 미리보기 / AddModal / 데스크톱 우측 컬럼
    참조: design-system.md v0.1.1 §1~§7, SchedulePage.tsx line 81~302 verbatim
    open questions: ① event(#e2e8f0) 라이트 모드 색 / ② 멀티데이 범위 띠 표시 여부 / ③ "오늘" 버튼 위치
  -->
  ```

### 8. 비즈니스 로직 변경 0 — 표시 분기 룰 (멀티데이 weekend/holiday skip, dot slice(0,3), 공휴일 라벨, today/sel 색 분기) 는 시각으로만 mirror. 코드는 다음 wave (TSX 변환) 에서 처리.

### 9. NEGATIVE gate (작성 금지 항목)
- 이모지 (📅 🔥 ⚠ 등) — 모든 영역
- `text-status-fire`, `bg-status-warning-bar` 등 status- prefix className (tailwind 인식 X)
- inline `style="font-size:11px"` 등 9·10·11px font-size (메모리 룰)
- `lucide-react` import (sketch 는 HTML 단일 파일 — 아이콘은 inline SVG verbatim 또는 ChevronLeft/ChevronRight 만 사용, ChevronLeft path: `<svg ... viewBox="0 0 24 24"><polyline points="15 18 9 12 15 6"/></svg>` stroke 1.5)
- `<Stat Card>` 사용 (이번 wave 범위 X — 만일 어쩔 수 없이 카드 형태 통계가 들어가면 §6.3 룰 (좌측 3px bar + 위험 임계치 조건부 danger) 강제 적용)
- 카테고리 카드 (§6.2 negative rule — 이번 wave 는 dot 만, 카드 X)
  </action>
  <verify>
    <automated>
# 1. 파일 존재 + 최소 라인
F=cha-bio-safety/docs/redesign-context/13-schedule/sketch-wave-1.html
[[ -f "$F" ]] && [[ $(wc -l < "$F") -ge 350 ]] && echo "PASS: file + min_lines" || echo "FAIL: file missing or under 350 lines"

# 2. 이모지 0 (Python 사용 — 한국어 환경 grep utf-8 안정성)
python3 -c "
import re, sys
t = open('$F', encoding='utf-8').read()
emoji = re.findall(r'[\U0001F300-\U0001F9FF\U0001FA00-\U0001FAFF\U00002600-\U000027BF]', t)
sys.exit(0 if len(emoji) == 0 else 1)
" && echo "PASS: emoji 0" || echo "FAIL: emoji detected"

# 3. 9·10·11px font-size 0 (주석 라인 제외)
COUNT=$(grep -nE 'font-size:\s*(9|10|11)px|fontSize:\s*(9|10|11)\b|text-\[(9|10|11)px\]' "$F" | grep -v '^\s*[0-9]*:\s*/\*' | grep -v '^\s*[0-9]*:\s*\*' | wc -l)
[[ $COUNT -eq 0 ]] && echo "PASS: no 9/10/11px font-size" || (echo "FAIL: 9/10/11px detected ($COUNT)" && grep -nE 'font-size:\s*(9|10|11)px|fontSize:\s*(9|10|11)\b' "$F")

# 4. status- prefix className 0
COUNT=$(grep -nE 'class[^=]*=[^>]*\b(text|bg|border)-status-' "$F" | wc -l)
[[ $COUNT -eq 0 ]] && echo "PASS: no status- prefix" || (echo "FAIL: status- prefix found ($COUNT)" && grep -nE 'class[^=]*=[^>]*\b(text|bg|border)-status-' "$F")

# 5. SCHED_CATEGORIES 5 hex 모두 포함
MISS=""
for h in '#3b82f6' '#eab308' '#e2e8f0' '#f97316' '#ef4444'; do
  grep -q "$h" "$F" || MISS="$MISS $h"
done
[[ -z "$MISS" ]] && echo "PASS: all 5 category hex present" || echo "FAIL: missing hex:$MISS"

# 6. WEEK_DAYS 7 한글 모두 포함
MISS=""
for d in '일' '월' '화' '수' '목' '금' '토'; do
  grep -q "$d" "$F" || MISS="$MISS $d"
done
[[ -z "$MISS" ]] && echo "PASS: all 7 week days present" || echo "FAIL: missing days:$MISS"

# 7. data-theme 토글 동작
grep -q "data-theme" "$F" && echo "PASS: data-theme toggle present" || echo "FAIL: no data-theme"

# 8. 페이지 제목
grep -q "월간 점검 계획" "$F" && echo "PASS: page title present" || echo "FAIL: page title missing"

# 9. 라이트 frame 존재
grep -q 'data-theme="light"' "$F" && echo "PASS: light frame present" || echo "FAIL: light frame missing"

# 10. tokens.css verbatim 표식
grep -q -- '--surface-page' "$F" && grep -q -- '--status-fire-bar' "$F" && echo "PASS: tokens.css embedded" || echo "FAIL: tokens.css missing"

# 11. typography.css 표식
grep -q '.text-caption' "$F" && grep -q '.text-display' "$F" && echo "PASS: typography.css embedded" || echo "FAIL: typography.css missing"

# 12. category 5 label 모두 노출
MISS=""
for l in '점검' '업무' '행사' '승강기' '소방'; do
  grep -q "$l" "$F" || MISS="$MISS $l"
done
[[ -z "$MISS" ]] && echo "PASS: all 5 category labels present" || echo "FAIL: missing labels:$MISS"

# 13. open question 명시
grep -qE 'OQ|open question|사용자 컨펌' "$F" && echo "PASS: OQ notes present" || echo "FAIL: no OQ notes"

# 14. 멀티데이 mock
grep -qE '5/12|5-12|소방 종합정밀점검|멀티데이' "$F" && echo "PASS: multiday mock present" || echo "WARN: multiday mock note missing"

# Final: 위 14 grep 모두 PASS 면 시안 완성
echo "---"
echo "Verify gate run complete. 모든 PASS 면 사용자 컨펌 단계 진입."
    </automated>
  </verify>
  <done>
1. `cha-bio-safety/docs/redesign-context/13-schedule/sketch-wave-1.html` 파일 존재 (≥350 라인)
2. 14개 verify gate 모두 PASS
3. 다크 + 라이트 모바일 frame 2개가 side-by-side 표시되고, 라이트 모드에서 색 분기가 모두 정상 (text-text-primary, status-danger, accent 등 var() 정상 전환)
4. 자체 헤더 + 월/연도 네비 + 캘린더 grid (7×6) + 요일 헤더 + 일자 셀 + 카테고리 dot + 공휴일 라벨 + 선택/오늘 표식 모두 시각적으로 식별 가능
5. 5월 5일 (어린이날) 선택 상태로 "어린이날" 라벨이 빨간색 캡션으로 표시됨
6. 5/12~5/15 멀티데이 일정의 fire dot 4일 연속 표시
7. 5/20 cell 에 4개 카테고리 dot 중 3개만 잘려 표시됨 (slice(0,3) 시각 검증)
8. README 식 OQ 주석 3건 명시: event 라이트 색 / 멀티데이 범위 띠 / "오늘" 버튼 위치
9. 비즈니스 로직 변경 0 — SchedulePage.tsx 코드 1줄도 수정하지 않음
10. 다음 단계: 사용자가 브라우저로 파일 열어 시각 컨펌 → OQ 답변 → wave 2 (캘린더 아래 영역) 진행
  </done>
</task>

</tasks>

<verification>
sketch wave 1 완료 기준:

1. **시각 컨펌 (사용자 단계)**: 사용자가 브라우저로 `sketch-wave-1.html` 을 열어 모바일 다크/라이트 frame + 데스크톱 frame placeholder 를 보고 시각적으로 OK 판단
2. **자동 grep gate**: 14개 verify gate 모두 PASS
3. **메모리 룰 준수**: 이모지 0 / 9·10·11px font-size 0 / status- prefix 0 / 카테고리 hex verbatim / leading-none 일자 셀 / aspect-square 셀
4. **비즈니스 로직 변경 0**: SchedulePage.tsx / SchedulePage 관련 어떤 코드 파일도 수정하지 않음

verify gate FAIL 시: 해당 항목 fix → 재실행 → 모두 PASS 후 git add + 커밋.
</verification>

<success_criteria>
- [ ] `sketch-wave-1.html` 작성 완료 (≥350 라인, 14 verify gate PASS)
- [ ] 사용자가 브라우저로 열어 시각 컨펌 (OQ 3건 답변 포함)
- [ ] git commit 메시지: `sketch(13-schedule): wave 1 — 헤더 + 월 네비 + 캘린더 grid`
- [ ] 다음 wave (캘린더 아래 영역: 일정 카드 / 미리보기 / 범례 / CTA) 로 진행 가능 상태
- [ ] 비즈니스 로직 / TSX 코드 변경 0
- [ ] cbc7119-preview / cbc7119(직원) 어떤 페이지에도 영향 없음 (docs/redesign-context 폴더 단독 변경)
</success_criteria>

<output>
실행 완료 후 `.planning/quick/260519-4iu-redesign-13-schedule-sketch-wave-1-heade/260519-4iu-SUMMARY.md` 생성:
- 작성된 시안 위치 + 라인 수 + verify gate 결과 (14/14)
- 시각 컨펌 항목 (모바일 다크/라이트, 데스크톱 placeholder, 카테고리 dot 5종, 공휴일 라벨, 멀티데이 표시)
- OQ (open question) 3건 + 사용자 답변 대기 표시
- 다음 wave 진입 조건 (사용자 컨펌 후)
- 메모리 룰 준수 체크리스트 (이모지 0 / 9-11px 0 / status- prefix 0 / aspect-square / leading-none)
</output>
