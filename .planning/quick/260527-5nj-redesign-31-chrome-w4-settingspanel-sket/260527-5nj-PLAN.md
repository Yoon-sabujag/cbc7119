---
phase: quick-260527-5nj
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cha-bio-safety/docs/redesign-context/31-chrome/sketch-wave-4-settings-panel.html
autonomous: true
requirements:
  - REDESIGN-31-CHROME-W4
must_haves:
  truths:
    - "redesign/31-chrome 의 W4 sub-wave sketch 단일 파일이 디스크에 atomic 1-commit 으로 산출된다"
    - "8 OQ LOCKED 결정 + W3-OQ #A (✕ Lucide X) pattern 이 sketch 머리 주석 + body markup 양쪽에 모두 반영된다"
    - "SettingsPanel 894 라인 source 의 비즈 anchor 30+ 식별자 (Props + 8 내부 컴포넌트 + JSZip 백업 + push prefs 6 키 + 8 필드 폼) 가 머리 주석에 verbatim 박제되어 W7 TSX 변환 wave 1 byte 변경 0 강제가 보장된다"
    - "다크 + 라이트 (`[data-theme=\"light\"]`) 2 변형이 모두 시연되고, OQ #2 LOCKED 부분 격상 22건(9·10·11px 위반) 의 신/구 대조가 노안 격상 표로 박제된다"
    - "Negative gate (이모지 0 / ✕ 텍스트 글리프 0 / linear-gradient 0 / status-* prefix 0 / 9·10·11px body markup 0 / raw hex body 0 / legacy alias body 0) 가 sketch 안 grep 으로 검증된다"
  artifacts:
    - path: "cha-bio-safety/docs/redesign-context/31-chrome/sketch-wave-4-settings-panel.html"
      provides: "W4 SettingsPanel sketch — 머리 주석 (OQ 8건 + 비즈 anchor 30+ + 노안 격상 표 + 토큰 매핑 + negative gate + 메모리 룰) + :root 다크 + [data-theme=light] 라이트 + 5~8 frame body"
      min_lines: 1500
  key_links:
    - from: "sketch-wave-4-settings-panel.html 머리 주석"
      to: "wave-1-index.md §7 OQ LOCKED + §1.3 [SettingsPanel.tsx] 박스"
      via: "OQ #1/#2/#4/#5/#6/#7 + 비즈 anchor 30+ 식별자 명시적 인용"
      pattern: "OQ #[0-9]"
    - from: "sketch-wave-4-settings-panel.html body markup"
      to: "tokens.css v0.1.1 semantic 토큰"
      via: "var(--surface-*) / var(--text-*) / var(--border-*) / var(--accent*) / var(--status-*) 만 사용 (legacy alias --bg/--bg2/--bg3/--bg4/--bd/--bd2/--t1/--t2/--t3/--acl/--danger/--safe body markup 안 0)"
      pattern: "var\\(--surface-|var\\(--text-|var\\(--border-|var\\(--accent|var\\(--status-"
    - from: "sketch-wave-4-settings-panel.html 노안 격상 표"
      to: "source SettingsPanel.tsx 9·10·11px 22 위반 (실측 line 박제)"
      via: "fontSize 9 5건 (line 17/124/225/726/738) + fontSize 10 11건 (line 89/101/228/232/236/240/244/251/260/264/685/797/826 — 13건 중 OQ #2 격상 후보) + fontSize 11 6건 (line 133/717/760/765 + sketch 작성 시 추가 grep) → 격상 11/12/13 대조표"
      pattern: "fontSize: (9|10|11)"
    - from: "sketch-wave-4-settings-panel.html"
      to: "src/components/SettingsPanel.tsx (W7 TSX 변환 시 참조 target)"
      via: "비즈 anchor 30+ verbatim 박제로 W7 변환 wave 1 byte 변경 0 강제"
      pattern: "비즈 anchor"
---

<objective>
redesign/31-chrome W4 sub-wave 의 단일 산출물 — `cha-bio-safety/docs/redesign-context/31-chrome/sketch-wave-4-settings-panel.html` (894 라인 SettingsPanel 의 sketch HTML) atomic 1 파일 + 1 commit.

Purpose: chrome 4 컴포넌트 중 가장 큰 SettingsPanel (894 라인, 8 내부 컴포넌트 + JSZip 백업 + push prefs + 8 필드 폼 + 6 collapsible 섹션) 의 디자인 시안. v0.1.1 토큰 (다크/라이트) + OQ LOCKED 8건 적용. W7 TSX 변환 wave 이전에 디자인 검토/사용자 컨펌 단계를 위한 sketch.

Output:
- 단일 파일 `cha-bio-safety/docs/redesign-context/31-chrome/sketch-wave-4-settings-panel.html` (예상 1500~2500 라인, W3 1621 mirror)
- W3 sketch HTML 구조 패턴 mirror (머리 주석 박스 + :root 다크 + [data-theme=light] 라이트 + .frame-mobile + .decision-summary)
- 6~8 frame body 시연 (W3 보다 frame 수 많음 — 더 큰 컴포넌트 + 폼 변형 시연 필요)

Out of scope (보호 파일):
- src/ 전체 (특히 src/components/SettingsPanel.tsx / App.tsx) — W7 TSX 변환 책임
- tokens.css / typography.css / design-system.md / tailwind.config.js / wave-1-index.md / 7 스냅샷 / sketch-wave-2-global-header.html / sketch-wave-3-side-menu.html / 00-design-context/ — 1 byte 변경 0
- MenuSettingsSection sketch — W5 책임 (단 SettingsPanel 안 `<MenuSettingsSection />` import 시그니처 박제는 필수)
- W7 TSX 변환 — 별도 wave (이번 W4 는 sketch 단계)
- wrangler 명령 / npm run deploy (CLAUDE.local.md + memory `feedback_cbc7119_design_never_wrangler`)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md
@./CLAUDE.local.md

# W1 OQ LOCKED + W4 직접 영향 박스
@cha-bio-safety/docs/redesign-context/31-chrome/wave-1-index.md

# source 894 라인 (전체 끝까지 — 5 내부 컴포넌트 + 3 폼 + JSZip 백업 + push prefs + 6 섹션 + 로그아웃)
@cha-bio-safety/docs/redesign-context/31-chrome/SettingsPanel.tsx

# 디자인 시스템 v0.1.1 (§1.1 토큰 / §6.4 그라데이션 폐기 / §7.1 일관성)
@cha-bio-safety/docs/redesign-context/31-chrome/design-system.md
@cha-bio-safety/docs/redesign-context/31-chrome/tokens.css
@cha-bio-safety/docs/redesign-context/31-chrome/typography.css

# W2 / W3 precedent — sketch 구조 패턴 (다크 + 라이트 + 머리 주석 박스 + frame mirror)
@cha-bio-safety/docs/redesign-context/31-chrome/sketch-wave-2-global-header.html
@cha-bio-safety/docs/redesign-context/31-chrome/sketch-wave-3-side-menu.html

# mount 박제 + isDesktop 분기 박제
@cha-bio-safety/src/App.tsx

# settingsApi / R2 / push API 시그니처 — 비즈 anchor 박제 (handlePrefToggle, handleTestPush, handleDbBackup 등 변경 금지)
@cha-bio-safety/src/utils/api.ts

# tailwind config — w-8/h-8 = 48px 함정 의식 (memory feedback_tailwind_w8_h8_is_48px)
@cha-bio-safety/tailwind.config.js

<memory_rules>
이 sketch 작성 시 직접 영향:

1. feedback_design_sketch_first — sketch HTML 먼저, TSX 변환 W7 에서. 본 W4 = sketch only.
2. feedback_redesign_sketch_rule_enforcement — §6.2 negative rule (위험 임계치 아닌 카드는 status 색 금지)
   + §6.3/§7.1 일관성 verify gate. SettingsPanel 의 PermBadge granted/denied/default 3 분기 = status semantic 일관 적용 OK.
3. feedback_sketch_realistic_data — 표시 분기/라벨 룰은 코드 그대로. 시각 디자인만 손볼 것.
4. feedback_tsx_wave_stat_card_drift — sketch 의 새 패턴 + source 의 비즈 anchor 양쪽 머리 주석 verbatim 인용 필수.
5. feedback_planner_prompt_sketch_verbatim — 본 plan 안에 SettingsPanel CSS/props/handler 시그니처 grep 으로 추출해 verbatim 인용 (이미 scope 에 포함).
6. feedback_tailwind_token_class_pattern — status-* prefix 없음 (text-fire-bar O / text-status-fire-bar X).
7. feedback_tailwind_w8_h8_is_48px — w-8/h-8 = 48px 함정. SettingsPanel 의 width 38 / height 21 (Toggle) / 28x28 (✕ 버튼) / 44x44 (avatar) 다 임의 size 라 함정 영향 0 — 단, sketch 머리 주석에 의식적 박제.
8. feedback_tsx_wave_emoji_dot_gap — 이모지 negative gate 강제 (⏳🔔 OQ #6 LOCKED 제거).
9. feedback_avoid_premature_confirmation — sketch 결과는 사용자가 본 후 판단. "approved 주세요" 자제.
10. project_cbc7119_design_repo + feedback_cbc7119_design_never_wrangler — 디자인 격리 리포. wrangler 금지. main push 만 cbc7119-preview 자동 배포.
11. feedback_check_branch_before_edit — main 단일-trunk. 작업 시작 전 branch 확인 + dirty 면 사용자 컨펌.
12. project_redesign_workflow — redesign/NN 브랜치 + 즉시 푸시. /gsd:quick 으로 진행 (본 plan).
</memory_rules>

<interfaces>
<!-- W7 TSX 변환 wave 의 1 byte 변경 0 강제를 위해, sketch 머리 주석에 박제할 source 시그니처. -->
<!-- 모두 SettingsPanel.tsx 894 라인 source 에서 verbatim 추출. 추측 0. -->

From cha-bio-safety/docs/redesign-context/31-chrome/SettingsPanel.tsx:

```typescript
// === Props (line 50~54) ===
interface Props {
  open: boolean
  onClose: () => void
  isDesktop?: boolean
}

// === 내부 컴포넌트 5종 + 폼 3종 시그니처 ===

// 1. SectionHeader (line 5~27) — collapsible 토글, ChevronRight rotate 90
function SectionHeader({ label, collapsed, onToggle }: {
  label: string; collapsed: boolean; onToggle: () => void
})

// 2. usePersistedCollapse (line 29~41) — localStorage 영속 hook
function usePersistedCollapse(key: string, defaultCollapsed = true):
  [boolean, (v: boolean | ((c: boolean) => boolean)) => void]

// 3. Toggle (line 57~77) — on/off switch, 38x21px, background on=#2563eb (OQ-매핑: --accent-active) / off=var(--bg4) → --surface-active
function Toggle({ on, onChange, disabled }: {
  on: boolean; onChange?: (v: boolean) => void; disabled?: boolean
})

// 4. PermBadge (line 80~93) — NotificationPermission 3 분기
function PermBadge({ perm }: { perm: NotificationPermission })
//   granted: var(--safe, #22c55e) → --status-safe-bar
//   denied:  var(--danger, #ef4444) → --status-danger-bar
//   default: var(--t3, #6e7681) → --text-tertiary
//   현재 fontSize: 10 → OQ #2 격상 12 (caption)

// 5. Row (line 96~106) — label + sub + children + onClick
function Row({ label, sub, children, onClick }: {
  label: string; sub?: string; children?: React.ReactNode; onClick?: () => void
})
//   label fontSize: 12 (유지) / sub fontSize: 10 → OQ #2 격상 12

// 6. ChangePasswordForm (line 109~145) — current / next / confirm + validation + mutation
function ChangePasswordForm({ onDone }: { onDone: () => void })
//   sub-header fontSize: 9 (line 124) → OQ #2 격상 11 또는 12 (uppercase letterSpacing 유지)
//   danger 텍스트 fontSize: 11 (line 133) → OQ #2 격상 13 (label)

// 7. NameEditModal (line 148~180) — 이름 변경 modal
function NameEditModal({ currentName, onClose, onSave }: {
  currentName: string; onClose: () => void; onSave: (name: string) => void
})

// 8. ProfileEditForm (line 183~277) — 8 필드 폼
function ProfileEditForm({ onDone }: { onDone: () => void })
//   sub-header fontSize: 9 (line 225) → OQ #2 격상
//   라벨 fontSize: 10 (line 228/232/236/240/244/251/260/264 = 8건) → OQ #2 격상 12
//   8 필드: 이름 / 사번 / 직책 / 역할 / 입사일 / 생년월일 / 연락처 / 이메일

// === SettingsPanel 메인 export (line 280) ===
export function SettingsPanel({ open, onClose, isDesktop = false }: Props)

// === 6 collapsible 섹션 ===
// 1. 알림 (notifCollapsed, line 285) — usePersistedCollapse('settings.notif.collapsed', true)
// 2. 화면 (displayCollapsed, line 286) — usePersistedCollapse('settings.display.collapsed', true)
// 3. 계정 (accountCollapsed, line 287) — usePersistedCollapse('settings.account.collapsed', true)
// 4. 데이터베이스 (dbCollapsed, line 288) — usePersistedCollapse('settings.db.collapsed', true) — admin only
// 5. 앱 정보 (appInfoCollapsed, line 289) — usePersistedCollapse('settings.appinfo.collapsed', true)
// 6. 메뉴 설정 (MenuSettingsSection — W5 책임, 본 sketch 에선 import 시그니처만 박제)

// === Push notification 비즈 (line 304~407) ===
const [permState, setPermState] = useState<NotificationPermission>(
  typeof Notification !== 'undefined' ? Notification.permission : 'denied'
)
const [subscribed, setSubscribed] = useState(false)
const [prefs, setPrefs] = useState<NotificationPreferences>({
  daily_schedule: true, incomplete_schedule: true,
  unresolved_issue: true, education_reminder: true,
  event_15min: true, event_5min: true,
})
// → 6 prefs 키: daily_schedule / incomplete_schedule / unresolved_issue / education_reminder / event_15min / event_5min

async function handleSubscribe()    // line 332 — pushApi.getVapidKey + pushManager.subscribe
async function handleUnsubscribe()  // line 361
async function handleTestPush()     // line 376 — POST /api/push/test (admin only)
async function handlePrefToggle(key: keyof NotificationPreferences)  // line 398

// === JSZip 백업 비즈 (안전 직결 — line 444~625) ===
import JSZip from 'jszip'  // line 3
async function handleDbBackup()    // line 444 — fetch /database/backup → .sql 다운로드
async function handleDbRestore()   // line 467 — input file .sql → POST /database/restore
async function handleR2Backup()    // line 497 — r2-list / r2-download / JSZip / cronZips + deltaKeys 분기
async function handleR2Restore()   // line 584 — JSZip.loadAsync + 10개 batch upload

// === Account ===
async function handleClearCache()  // line 420 — caches.delete + serviceWorker.unregister
function handleLogout()            // line 627 — logout() + navigate('/login') + onClose()
function handleNameSaved(newName)  // line 633

// === Display 정보 ===
const displayName = staff?.name ?? ''
const displayRole = staff?.role === 'admin' ? '관리자' : '보조자'
const displayTitle = staff?.title ?? ''
const avatarChar = displayName ? displayName.charAt(0) : '?'
// __APP_VERSION__ / __BUILD_TIME__ (line 864)

// === Layout (line 638~893) ===
// Backdrop: position fixed, inset 0, zIndex 190, rgba(0,0,0,0.65), opacity transition 0.28s
// Panel: position fixed,
//        top: isDesktop ? 0 : 'var(--sat, 0px)',
//        bottom: isDesktop ? 0 : 'calc(54px + var(--sab, 0px) - var(--sat, 0px))',
//        right 0, zIndex 200,
//        width: '88%', maxWidth: 320,  ← OQ #7 LOCKED 보존
//        background: var(--bg2) → --surface-raised
//        transform: open translateX(0) / closed translateX(100%)
//        transition 0.3s cubic-bezier(.4,0,.2,1)
//        borderRadius '16px 0 0 16px'
//        overflowY auto

// === 헤더 (line 663~670) ===
// padding '12px 15px', borderBottom 1px solid var(--bd) → --border-default
// 인라인 svg gear 아이콘 (16x16, OQ #5 LOCKED 인라인 svg 유지)
// 설정 텍스트 fontSize: 13.5 fontWeight: 700
// ✕ 텍스트 글리프 (line 669) → W3-OQ #A pattern: Lucide X 교체 (28-splash/27-login/SideMenu mirror)
//   28x28 ratio + 7 borderRadius + var(--bg3) → --surface-sunken + var(--t2) → --text-secondary

// === 프로필 (line 673~688) ===
// padding '14px 13px 8px'
// avatar 44x44 borderRadius 50%
// background: 'linear-gradient(135deg, #2563eb 0%, #7c3aed 100%)' (line 677)
//   → OQ #4 LOCKED 폐기: var(--accent-active) solid (또는 var(--accent))
// avatarChar fontSize: 18 fontWeight: 700 color: '#fff' → --text-on-accent
// displayName fontSize: 14 fontWeight: 700 color: var(--t1) → --text-primary
// displayTitle · displayRole fontSize: 10 color: var(--t3) → OQ #2 격상 12 / --text-tertiary

// === 알림 섹션 (line 691~750) ===
// padding '12px 13px 5px'
// SectionHeader "알림"
// Row "푸시 알림" + PermBadge + Toggle
// admin 전용 테스트 푸시 button (line 710~723):
//   '⏳ 전송 중...' : '🔔 테스트 푸시 보내기' (line 722)
//   → OQ #6 LOCKED 이모지 제거 + Lucide Loader2 / Send 교체
//   fontSize: 11 → OQ #2 격상 13 (label)
// 점검 sub-header "점검" fontSize: 9 (line 726) → 격상
// 일정 sub-header "일정" fontSize: 9 (line 738) → 격상
// 6 prefs Row × Toggle 시연

// === 메뉴 설정 섹션 (line 753) ===
// <MenuSettingsSection />  ← W5 책임, sketch 에선 placeholder 자리만 명시

// === 화면 섹션 (line 756~771) ===
// SectionHeader "화면"
// Row "테마" + select (다크/라이트/시스템) — select fontSize: 11 (line 760) → OQ #2 격상 13
// Row "주간 현황 기준" + select — fontSize: 11 (line 765) → 격상
// Row "결과 즉시 저장" + Toggle on=true 시연

// === 계정 섹션 (line 774~790) ===
// 분기: showPwChange ? ChangePasswordForm : showProfileEdit ? ProfileEditForm : 메뉴 Row 2개
// "개인정보 수정" / "비밀번호 변경" Row + ChevronRight svg (13x13 인라인)

// === 데이터베이스 섹션 (line 793~857) — admin only ===
// SectionHeader "데이터베이스"
// sub fontSize: 10 (line 797 "DB ..." / line 826 "파일 ...") → OQ #2 격상 12
// DB 백업/업로드 버튼 2 (40x40, var(--bg3) / var(--bd))
// R2 백업/업로드 버튼 2 (백업 시 r2BackupProgress 라이브 텍스트)
// 인라인 svg 4 아이콘 (download / upload — OQ #5 LOCKED 유지)

// === 앱 정보 섹션 (line 860~875) ===
// SectionHeader "앱 정보"
// Row "버전" sub=`v${__APP_VERSION__} (${__BUILD_TIME__})`
// Row "캐시 초기화" sub="최신 리소스로 새로고침" onClick=handleClearCache
// Row "차바이오컴플렉스 방재" sub="경기도 성남시 분당구 판교로 335"

// === 로그아웃 (line 878~889) ===
// padding '12px 13px'
// 40 height, raw rgba(220,38,38,0.12) background, raw #dc2626 color, raw rgba(220,38,38,0.25) border
//   → OQ 매핑: var(--status-danger-bg) / var(--status-danger) / var(--status-danger) 25% alpha
// fontSize: 12 fontWeight: 700
```

From cha-bio-safety/docs/redesign-context/31-chrome/tokens.css (v0.1.1 semantic 토큰):
```css
/* 다크 (:root, [data-theme="dark"]) */
--surface-page    #0a0d12   (← --bg)
--surface-raised  #1a1f27   (← --bg2)  ← Panel background
--surface-sunken  #232a33   (← --bg3)  ← Row background, ✕ button bg
--surface-active  #2c333d   (← --bg4)  ← Toggle off, 취소 button
--text-primary    #e6edf3   (← --t1)
--text-secondary  #adb6c0   (← --t2)
--text-tertiary   #8b949e   (← --t3)
--border-default  rgba(255,255,255,0.14)  (← --bd)
--border-strong   rgba(255,255,255,0.22)  (← --bd2)
--accent          #3b82f6   (← --acl)
--accent-active   #2563eb   ← Toggle on background + 프로필 아바타 (OQ #4 폐기 후 solid)
--status-safe     #4ade80
--status-safe-bar #22c55e   (← --safe, 현 fallback)
--status-danger   #f87171
--status-danger-bar #ef4444 (← --danger, 현 fallback)
--status-danger-bg  rgba(239,68,68,0.16) ← 로그아웃 button bg

/* 라이트 ([data-theme="light"]) — accent 진해짐 / status 채도 진한 톤 */
--surface-page    #ffffff
--surface-raised  #f6f8fa
--surface-sunken  #ebeef1
--surface-active  #d8dee4
--text-primary    #1f2328
--text-secondary  #4d5562
--text-tertiary   #656d76
--border-default  rgba(0,0,0,0.14)
--accent-active   #0a52c4
--status-safe-bar #15803d
--status-danger-bar #b91c1c
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SettingsPanel sketch HTML 단일 atomic 파일 작성 + commit</name>
  <files>cha-bio-safety/docs/redesign-context/31-chrome/sketch-wave-4-settings-panel.html</files>

  <action>
**필수 작업 시작 전 확인:**

1. 현재 브랜치 확인. 만약 `main` 단일-trunk 가 아니거나 dirty 면 사용자에게 컨펌 (memory `feedback_check_branch_before_edit`).
2. `cha-bio-safety/docs/redesign-context/31-chrome/sketch-wave-4-settings-panel.html` 가 이미 존재하면 사용자에게 확인 후 진행. 없으면 새 파일.

**SettingsPanel.tsx 894 라인 source 재확인 grep (verbatim 박제 정확도 보장 — sketch 작성 시 한 번 더 실측):**

```bash
# fontSize 9·10·11 위반 22건 정확 count
grep -nE "fontSize: (9|10|11)[^0-9]" cha-bio-safety/docs/redesign-context/31-chrome/SettingsPanel.tsx | head -40

# linear-gradient 1건 (OQ #4 LOCKED 폐기)
grep -n "linear-gradient" cha-bio-safety/docs/redesign-context/31-chrome/SettingsPanel.tsx

# 이모지 (⏳🔔) 위치
grep -nE "⏳|🔔" cha-bio-safety/docs/redesign-context/31-chrome/SettingsPanel.tsx

# ✕ 텍스트 글리프
grep -n "✕" cha-bio-safety/docs/redesign-context/31-chrome/SettingsPanel.tsx

# raw hex (다크 모드 fallback / 로그아웃 destructive)
grep -nE "#[0-9a-fA-F]{6}|#[0-9a-fA-F]{3}[^0-9a-fA-F]" cha-bio-safety/docs/redesign-context/31-chrome/SettingsPanel.tsx | head -30

# JSZip / push prefs / 6 collapsible 키 박제
grep -nE "JSZip|usePersistedCollapse|NotificationPreferences|daily_schedule|incomplete_schedule|unresolved_issue|education_reminder|event_15min|event_5min" cha-bio-safety/docs/redesign-context/31-chrome/SettingsPanel.tsx | head -20
```

grep 결과를 sketch 머리 주석 박스에 verbatim 인용 (추측 0 — memory `feedback_planner_prompt_sketch_verbatim`).

**파일 생성 — `cha-bio-safety/docs/redesign-context/31-chrome/sketch-wave-4-settings-panel.html`:**

### 구조 1. 머리 주석 (300~500 라인 — W3 1621 머리 200+ 라인 mirror, W4 는 컴포넌트 더 크니 박스 더 많음)

다음 박스를 모두 포함:

**[메타 박스]**
- 파일 / quick_id (260527-5nj) / branch (redesign/31-chrome 의 W4 sub-wave) / source TSX 라인 (894) / 작성 일자 (2026-05-27)
- 범위: SettingsPanel.tsx 단일 컴포넌트 sketch — 다크 + 라이트 + 6~8 frame
- out of scope: 위 `<objective>` 섹션 보호 파일 목록 1:1 복제

**[OQ LOCKED 8건 매트릭스 박스] — W4 직접 영향 6건 강조 표시**

```
OQ #1 LOCKED 다크+라이트 양쪽 지원   → W4 적용 ★ (frame 5+ 라이트 변형)
OQ #2 LOCKED 부분 격상 22건           → W4 적용 ★ (9·10·11 px → 11/12/13 격상)
OQ #3 LOCKED SideMenu 그라데이션      → W4 무관 (W3 완료)
OQ #4 LOCKED 프로필 그라데이션 폐기   → W4 적용 ★ (line 677 linear-gradient → --accent-active solid)
OQ #5 LOCKED 인라인 svg 유지           → W4 적용 ★ (gear/ChevronRight/svg path 유지 — 단 ✕ 글리프만 Lucide X)
OQ #6 LOCKED 이모지 제거               → W4 적용 ★ (line 722 ⏳🔔 → Lucide Loader2/Send + 텍스트)
OQ #7 LOCKED panel width 보존          → W4 적용 ★ (width 88% maxWidth 320 박제 — 위 `<interfaces>` 참조)
W3-OQ #A pattern (참고)                → W4 mirror: ✕ 글리프 (line 669) → Lucide X 교체
```

**[비즈 anchor 30+ 박스]** — `<interfaces>` 섹션의 source 시그니처 전체를 머리 주석에 1:1 인용. 5 내부 컴포넌트 + 3 폼 + 6 collapsible + 4 JSZip 핸들러 + 4 push 핸들러 + 8 ProfileEditForm 필드 + 6 prefs 키 + Layout (Backdrop + Panel + 헤더 + 7 섹션 + 로그아웃) 모두 박제.

**[노안 격상 표 박스]** — OQ #2 LOCKED 22건 위반 매핑 (위 grep 결과 + 아래 표 mirror):

```
[fontSize 9 위반 5건 — 모두 sub-header uppercase, letterSpacing .08em 유지]
  line 17  SectionHeader span               9 → 11 (또는 12) ★
  line 124 ChangePasswordForm sub-header    9 → 11 (또는 12)
  line 225 ProfileEditForm sub-header       9 → 11 (또는 12)
  line 726 push prefs "점검" sub-header     9 → 11 (또는 12)
  line 738 push prefs "일정" sub-header     9 → 11 (또는 12)

[fontSize 10 위반 13건 — 본문 sub / 폼 라벨]
  line 89  PermBadge text                  10 → 12 (caption)
  line 101 Row sub                          10 → 12 (caption)
  line 228 ProfileEditForm 이름 라벨        10 → 12
  line 232 사번 라벨                        10 → 12
  line 236 직책 라벨                        10 → 12
  line 240 역할 라벨                        10 → 12
  line 244 입사일 라벨                      10 → 12
  line 251 생년월일 라벨                    10 → 12
  line 260 연락처 라벨                      10 → 12
  line 264 이메일 라벨                      10 → 12
  line 685 displayTitle · displayRole       10 → 12 (caption)
  line 797 DB sub "점검기록, 직원, 설정"   10 → 12
  line 826 파일 sub "점검 사진 등"          10 → 12

[fontSize 11 위반 6건 — 폼 검증 텍스트 / button / select]
  line 133 ChangePasswordForm danger       11 → 13 (label) — 변형 시 색 = --status-danger
  line 717 admin 테스트 푸시 button         11 → 13 (label)
  line 760 select "테마"                   11 → 13 (label)
  line 765 select "주간 현황 기준"          11 → 13 (label)
  (sketch 작성 시 grep 으로 추가 11 위반 발견 시 표에 보강)
```

격상 결정 메모: SectionHeader 9 → **11** (W3 divider 11 와 일관) 또는 12 둘 다 sketch 에서 시연해 두 변형 frame 으로 보여주고 사용자 결정. 본문 sub 10 → 12. 폼 라벨 10 → 12. button + select 11 → 13. sketch 머리 주석에 "sketch 결과 본 후 사용자 결정" 명시.

**[토큰 매핑 표 박스]** — 14 alias → 14 semantic 매핑:

```
var(--bg)      → --surface-page       (헤더 분리선 인접 / overflow 영역)
var(--bg2)     → --surface-raised     (Panel background)
var(--bg3)     → --surface-sunken     (Row / 인풋 / ✕ button / DB 백업 button bg)
var(--bg4)     → --surface-active     (Toggle off / 취소 button bg)
var(--bd)      → --border-default     (Panel 헤더 분리선 / input border)
var(--bd2)     → --border-strong      (admin 테스트 button border / select border)
var(--t1)      → --text-primary       (displayName / 입력값)
var(--t2)      → --text-secondary     (✕ button color / 취소 button color)
var(--t3)      → --text-tertiary      (SectionHeader uppercase / sub / 라벨 / 입력 placeholder readonly)
var(--acl)     → --accent             (대안 적용: --accent or --accent-active)
var(--safe)    → --status-safe-bar    (PermBadge granted)
var(--danger)  → --status-danger-bar  (PermBadge denied / ChangePasswordForm 검증)
raw #2563eb (Toggle on / 그라데이션 폐기 후 avatar) → --accent-active
raw #22c55e fallback   → --status-safe-bar
raw #ef4444 fallback   → --status-danger-bar
raw #6e7681 fallback   → --text-tertiary
raw #dc2626 (로그아웃) → --status-danger (또는 --status-danger-bar)
raw rgba(220,38,38,0.12) → --status-danger-bg
raw rgba(220,38,38,0.25) → --status-danger border 25% alpha (또는 --status-danger-bg 진하게)
linear-gradient (line 677) → 폐기, --accent-active solid (OQ #4 LOCKED)
border-radius 7/8/9/10/11/14/16 다중 → --radius-sm (8) / --radius-md (12) / --radius-lg (16)
```

**[Negative gate 박스]** — sketch HTML 안 grep verify 항목 (sketch 자체 작성 후 self-verify 에서 사용):

```
1. fontSize: (9|10|11)[^0-9] body markup 안 0 — 단 머리 주석 박스 안 인용은 예외
2. linear-gradient body markup 안 0 (OQ #4 LOCKED 폐기 — 1건 제거)
3. 이모지 (⏳🔔) body markup 안 0 (OQ #6 LOCKED 제거)
4. ✕ 텍스트 글리프 body markup 안 0 (W3-OQ #A pattern, Lucide X 교체)
5. status-* prefix class 안 0 (memory feedback_tailwind_token_class_pattern — text-status-fire-bar X)
6. legacy alias body markup 안 0: --bg / --bg2 / --bg3 / --bg4 / --bd / --bd2 / --t1 / --t2 / --t3 / --acl / --danger / --safe / --warn / --info / --fire — 모두 v0.1.1 명 교체
7. raw hex body markup 안 0 — 단 :root + [data-theme="light"] 정의 블록 안 + 머리 주석 박스 안 인용은 예외
8. MENU 상수 import 변경 0 (MenuSettingsSection W5 의존 — placeholder 표기만)
```

**[메모리 룰 inline 박스]** — 12 unique slug:

```
이 sketch 작성 시 직접 영향 박제:
1. feedback_design_sketch_first
2. feedback_redesign_sketch_rule_enforcement (§6.2/§6.3/§7.1)
3. feedback_sketch_realistic_data
4. feedback_tsx_wave_stat_card_drift
5. feedback_planner_prompt_sketch_verbatim
6. feedback_tailwind_token_class_pattern
7. feedback_tailwind_w8_h8_is_48px
8. feedback_tsx_wave_emoji_dot_gap
9. feedback_avoid_premature_confirmation
10. project_cbc7119_design_repo
11. feedback_cbc7119_design_never_wrangler
12. project_redesign_workflow
```

### 구조 2. `<style>` 정의 블록 (200~300 라인)

W3 sketch precedent (sketch-wave-3-side-menu.html) mirror:

- `:root, [data-theme="dark"]` — tokens.css v0.1.1 다크 정의 verbatim 복사 (surface/text/border/accent/status/radius 모두)
- `[data-theme="light"]` — tokens.css v0.1.1 라이트 정의 verbatim 복사
- 공통 reset + body font (Noto Sans KR / system-ui)
- `.canvas` (frame 컨테이너), `.frame-mobile` (375x812 ratio iPhone 14 Pro 기준 — 또는 W3 와 동일 ratio)
- `.frame-label` (frame 위 라벨 — "다크 / 전체 패널" 등)
- `.decision-summary` 4 박스 (OQ 결정 / 노안 격상 / 토큰 매핑 / negative gate — Frame 위 또는 옆에 시각 박스)
- 컴포넌트 클래스: `.panel`, `.panel-header`, `.panel-section-header`, `.row`, `.toggle`, `.toggle-on`, `.perm-badge`, `.input`, `.form-button-primary`, `.form-button-secondary`, `.avatar`, `.db-button`, `.logout-button` 등
- 각 클래스에 비교 표시용 신/구 두 버전을 시연하려면 modifier (예: `.row--legacy` vs `.row--v011`) 변형 정의

### 구조 3. `<body>` frame 6~8 (~1000~1500 라인 영역)

각 frame 의 위에 `decision-summary` 박스 또는 `frame-label` 로 의도 명시.

**Frame 1: 다크 / 전체 패널 (메인 시연)**
- Backdrop + Panel (88% width / 320 maxWidth / 16px borderRadius / right edge)
- 헤더: gear svg 아이콘 + "설정" + Lucide X (✕ 글리프 폐기 후)
- 프로필: avatar 44x44 — `--accent-active` solid (그라데이션 폐기) — `displayName` "윤종엽" / `displayTitle · displayRole` "팀장 · 관리자" (격상 12)
- 알림 섹션 (collapsed=false): SectionHeader (격상 11) + Row "푸시 알림" + PermBadge granted (격상 12) + Toggle on
- 메뉴 설정 섹션 placeholder (W5 책임 — `<MenuSettingsSection />` 자리 표시, 회색 outline 박스 + "MenuSettingsSection — W5 책임" 텍스트)
- 화면 섹션 (collapsed=true) SectionHeader 만
- 계정 섹션 (collapsed=true)
- 데이터베이스 섹션 (collapsed=true) — admin
- 앱 정보 섹션 (collapsed=true)
- 로그아웃 button (`--status-danger-bg` background / `--status-danger` color)

**Frame 2: 다크 / 알림 권한 + 테스트 푸시 button 시연**
- 알림 섹션 expanded
- admin 전용 button "Loader2 회전 + 전송 중..." 변형 + 정적 "Send + 테스트 푸시 보내기" 변형 (Lucide 인라인 svg path 박제)
- PermBadge 3 변형: granted / denied / default 가로 비교
- 폰트 격상 13 (button) + 12 (PermBadge) 시연

**Frame 3: 다크 / Push prefs 6 토글 시연**
- 점검 sub-header (격상 11 또는 12, uppercase letterSpacing .08em 유지)
- daily_schedule / incomplete_schedule / unresolved_issue Row × Toggle
- 일정 sub-header
- event_15min / event_5min / education_reminder Row × Toggle
- Toggle on/off 양쪽 변형 가시화

**Frame 4: 다크 / 데이터베이스 백업/복원 카드 시연 — admin**
- DB sub (격상 12) + DB 백업/업로드 button 2
- 파일 sub (격상 12) + R2 백업/업로드 button 2 (R2 백업 진행중 텍스트 "신규 파일 다운로드 (3/8)..." 시연)
- 인라인 svg 4 아이콘 (OQ #5 LOCKED 유지)

**Frame 5: 라이트 / 전체 패널 (`data-theme="light"` mirror)**
- Frame 1 과 동일 구조, [data-theme="light"] selector 적용
- avatar `--accent-active` (#0a52c4) / Row `--surface-sunken` (#ebeef1) / 헤더 분리선 (#e6edf3 rgba)

**Frame 6: 라이트 / 비밀번호 변경 폼 시연**
- ChangePasswordForm: 현재 / 새 / 확인 (3 input — 마지막은 일치하지 않음 상태)
- 검증 메시지 "비밀번호가 일치하지 않습니다" (격상 13 / `--status-danger`)
- 취소 + 변경 button (`--surface-active` / `--accent`)
- sub-header "비밀번호 변경" (격상 11 또는 12, uppercase)

**Frame 7: 라이트 / 개인정보 수정 폼 시연**
- ProfileEditForm 8 필드 모두 시연:
  - 이름 (readonly, `--surface-page` background — 인풋 readonly 색)
  - 사번 (readonly)
  - 직책 (readonly)
  - 역할 (readonly)
  - 입사일 (readonly)
  - 생년월일 (date input, 활성)
  - 연락처 (tel input, "010-0000-0000" placeholder)
  - 이메일 (email input, "email@example.com" placeholder)
- 8 라벨 모두 격상 12 시연
- sub-header "개인정보 수정" (격상)

**Frame 8 (선택, 시간 허용 시): 다크 / SectionHeader collapse/expand 토글 4 상태 매트릭스**
- 알림 collapsed / expanded
- 화면 collapsed / expanded
- ChevronRight rotate 0 / 90 시연

각 frame 마다 frame-label 에 의도 명시 (예: "Frame 2 — 다크 / 알림 권한 expanded + admin 테스트 푸시 button + PermBadge 3 분기 (OQ #2 격상 12 + OQ #6 이모지 폐기 + Lucide Send icon)").

### 구조 4. Footer 주석 (50~100 라인)

- W4 sketch 완결 표시
- 다음 wave (W5 MenuSettingsSection — 별도 quick / W7 SettingsPanel TSX 변환 — 별도 quick)
- 비즈 anchor verify 다음 wave 강제 — "본 sketch 의 비즈 anchor 박스 30+ 식별자 + 노안 격상 표 + 토큰 매핑 표 → W7 TSX 변환 시 1 byte 변경 0 강제"

### 작성 검증 (sketch 자체 grep self-verify — Task 안 강제)

파일 작성 직후 다음 grep 으로 self-verify (모두 0 또는 의도 예외만):

```bash
SKETCH=cha-bio-safety/docs/redesign-context/31-chrome/sketch-wave-4-settings-panel.html

# 1. body markup 영역만 추출 (머리 주석 + :root + [data-theme] 정의 제외)
#    <body> ~ </body> 영역에서 다음 모두 0:
awk '/<body/,/<\/body>/' "$SKETCH" | grep -cE 'fontSize: (9|10|11)[^0-9]'   # 기대 0
awk '/<body/,/<\/body>/' "$SKETCH" | grep -c 'linear-gradient'              # 기대 0
awk '/<body/,/<\/body>/' "$SKETCH" | grep -cE '⏳|🔔'                        # 기대 0
awk '/<body/,/<\/body>/' "$SKETCH" | grep -c '✕'                            # 기대 0
awk '/<body/,/<\/body>/' "$SKETCH" | grep -cE 'class="[^"]*status-(safe|danger|warning|info|fire)[^"]*"'  # 기대 0

# 2. legacy alias body 안 0 — 단 :root 정의 + 머리 주석 인용 제외
awk '/<body/,/<\/body>/' "$SKETCH" | grep -cE 'var\(--(bg|bg2|bg3|bg4|bd|bd2|t1|t2|t3|acl|danger|safe|warn|info|fire|c-day|c-night|c-off|c-leave)[^a-zA-Z-]'  # 기대 0

# 3. raw hex body 안 — 단 :root + [data-theme="light"] 정의 + 머리 주석 인용 제외
#    (즉 body markup style="" attribute 에 raw hex 0)
awk '/<body/,/<\/body>/' "$SKETCH" | grep -cE 'style="[^"]*#[0-9a-fA-F]{3,6}'  # 기대 0

# 4. semantic token 만 사용 — positive 검증
awk '/<body/,/<\/body>/' "$SKETCH" | grep -cE 'var\(--(surface|text|border|accent|status|radius|space|duty)'  # 기대 양의 다수

# 5. 비즈 anchor verbatim 박제 (머리 주석에 30+ 항목 존재 — line 수 풍성)
grep -c 'usePersistedCollapse\|NotificationPreferences\|JSZip\|handleDbBackup\|handleR2Backup\|handleTestPush\|handlePrefToggle\|ProfileEditForm\|ChangePasswordForm\|SectionHeader' "$SKETCH"  # 기대 >= 20

# 6. frame 6+ 카운트 (다크 5 + 라이트 2~3)
grep -c 'class="frame-mobile\|frame-label' "$SKETCH"  # 기대 frame 수의 2배 정도

# 7. OQ LOCKED 8건 명시 인용
grep -cE 'OQ #[1-7]\s+LOCKED|W3-OQ #A' "$SKETCH"  # 기대 >= 8

# 8. 라이트 모드 selector 사용 검증
grep -c 'data-theme="light"' "$SKETCH"  # 기대 >= 2 (정의 1 + 라이트 frame 사용 1+)

# 9. Lucide X / Send / Loader2 svg path 사용 검증 (OQ #6 + W3-OQ #A pattern)
grep -cE 'lucide|class="[^"]*lucide' "$SKETCH"  # 기대 >= 3 (X, Send, Loader2 인라인 svg path 적어도)
```

self-verify 실패 시 (예: legacy alias body 잔존 / 이모지 잔존 / linear-gradient 잔존) 즉시 수정 후 재검증.

**파일 작성 완료 후 commit:**

```bash
cd /Users/jykevin/Documents/cbc7119-design

git add cha-bio-safety/docs/redesign-context/31-chrome/sketch-wave-4-settings-panel.html

git status  # 다른 파일 변경 0 확인 (보호 파일 영향 0 — src/ / tokens.css / typography.css / wave-1-index.md / 기존 7 스냅샷 / W2 W3 sketch 1 byte 변경 0)

git commit -m "$(cat <<'EOF'
docs(redesign/31-chrome): SettingsPanel sketch W4 atomic — 8 컴포넌트 + JSZip 백업 + push prefs 6키 + 8 필드 폼

OQ LOCKED 8건 적용 (W4 직접 영향 6건):
- #1 다크+라이트 양쪽 frame
- #2 부분 격상 22건 (9·10·11 px → 11/12/13)
- #4 프로필 그라데이션 폐기 → --accent-active solid
- #5 인라인 svg 유지 (단 ✕ 글리프만 Lucide X, W3-OQ #A pattern)
- #6 이모지 ⏳🔔 제거 → Lucide Loader2 / Send
- #7 panel width 88% maxWidth 320 보존

비즈 anchor 30+ 박제 (W7 TSX 변환 1 byte 변경 0 강제):
- Props { open, onClose, isDesktop } + 8 컴포넌트 시그니처
- JSZip import + 4 백업/복원 핸들러
- NotificationPreferences 6 prefs 키 + 4 push 핸들러
- 6 collapsible 섹션 localStorage 키 + 8 필드 ProfileEditForm

Frame 6~8: 다크 전체 + 알림 expanded + push prefs + DB/R2 백업 + 라이트 전체 + 비밀번호 변경 + 개인정보 8 필드 (+ 옵션 collapse 매트릭스)

quick: 260527-5nj
EOF
)"

git log -1 --stat  # commit 확인
```

**보호 파일 영향 0 검증 — git status 안에 다음 파일 0 등장:**

```
src/                                   (전체)
cha-bio-safety/src/                    (전체 — 특히 App.tsx, SettingsPanel.tsx)
docs/redesign-context/31-chrome/wave-1-index.md
docs/redesign-context/31-chrome/SettingsPanel.tsx  (스냅샷)
docs/redesign-context/31-chrome/SideMenu.tsx
docs/redesign-context/31-chrome/GlobalHeader.tsx
docs/redesign-context/31-chrome/MenuSettingsSection.tsx
docs/redesign-context/31-chrome/design-system.md
docs/redesign-context/31-chrome/tokens.css
docs/redesign-context/31-chrome/typography.css
docs/redesign-context/31-chrome/sketch-wave-2-global-header.html
docs/redesign-context/31-chrome/sketch-wave-3-side-menu.html
docs/redesign-context/00-design-context/
tailwind.config.js
```

위 보호 파일이 git status 에 등장하면 즉시 중단 + 사용자에게 보고.

**금지 동작 명시:**
- `wrangler` 명령 일체 금지 (CLAUDE.local.md `.claude/settings.local.json` deny 강제됨)
- `npm run deploy` 금지 (직원 도메인 경로 — memory `feedback_cbc7119_design_never_wrangler`)
- W5 MenuSettingsSection sketch 작성 금지 (별도 quick)
- W7 TSX 변환 금지 (`cha-bio-safety/src/components/SettingsPanel.tsx` 변경 0)
- 31-chrome/ 안 기존 10 파일 변경 0 (sketch-wave-4-* 만 신규 작성)
- 평면 폴더 유지 (sketch/ 서브폴더 만들지 말 것)
  </action>

  <verify>
    <automated>
SKETCH=cha-bio-safety/docs/redesign-context/31-chrome/sketch-wave-4-settings-panel.html

# 산출물 존재 + 최소 라인 수 (W3 1621 mirror — 최소 1500 강제)
test -f "$SKETCH" && echo "EXISTS" || echo "MISSING"
LINES=$(wc -l < "$SKETCH"); echo "LINES=$LINES"; [ "$LINES" -ge 1500 ] && echo "LINES OK" || echo "LINES TOO FEW"

# Negative gate — body markup 영역만 검증
awk '/<body/,/<\/body>/' "$SKETCH" > /tmp/sketch-body.html
echo "FONT-9-10-11 body : $(grep -cE 'fontSize: (9|10|11)[^0-9]' /tmp/sketch-body.html) (expect 0)"
echo "LINEAR-GRADIENT body : $(grep -c 'linear-gradient' /tmp/sketch-body.html) (expect 0)"
echo "EMOJI body : $(grep -cE '⏳|🔔' /tmp/sketch-body.html) (expect 0)"
echo "✕ GLYPH body : $(grep -c '✕' /tmp/sketch-body.html) (expect 0)"
echo "STATUS- CLASS body : $(grep -cE 'class="[^"]*status-(safe|danger|warning|info|fire)[^"]*"' /tmp/sketch-body.html) (expect 0)"
echo "LEGACY ALIAS body : $(grep -cE 'var\(--(bg|bg2|bg3|bg4|bd|bd2|t1|t2|t3|acl|danger|safe|warn|info|fire|c-day|c-night|c-off|c-leave)[^a-zA-Z-]' /tmp/sketch-body.html) (expect 0)"
echo "RAW HEX style attr body : $(grep -cE 'style="[^"]*#[0-9a-fA-F]{3,6}' /tmp/sketch-body.html) (expect 0)"

# Positive — semantic token 사용 + 비즈 anchor 박제 + frame 수 + OQ LOCKED 인용
echo "SEMANTIC TOKEN body : $(grep -cE 'var\(--(surface|text|border|accent|status|radius|space|duty)' /tmp/sketch-body.html) (expect >> 50)"
echo "BIZ ANCHOR : $(grep -c 'usePersistedCollapse\|NotificationPreferences\|JSZip\|handleDbBackup\|handleR2Backup\|handleTestPush\|handlePrefToggle\|ProfileEditForm\|ChangePasswordForm\|SectionHeader' "$SKETCH") (expect >= 20)"
echo "FRAME COUNT : $(grep -c 'class="frame-mobile\|class="frame-label' "$SKETCH") (expect >= 6)"
echo "OQ LOCKED REF : $(grep -cE 'OQ #[1-7][^0-9]\s+LOCKED|W3-OQ #A' "$SKETCH") (expect >= 8)"
echo "LIGHT THEME REF : $(grep -c 'data-theme="light"' "$SKETCH") (expect >= 2)"
echo "LUCIDE ICON REF : $(grep -cE 'lucide|class="[^"]*lucide' "$SKETCH") (expect >= 3)"

# 메모리 룰 12 slug 인용
echo "MEMORY SLUG : $(grep -cE 'feedback_design_sketch_first|feedback_redesign_sketch_rule_enforcement|feedback_sketch_realistic_data|feedback_tsx_wave_stat_card_drift|feedback_planner_prompt_sketch_verbatim|feedback_tailwind_token_class_pattern|feedback_tailwind_w8_h8_is_48px|feedback_tsx_wave_emoji_dot_gap|feedback_avoid_premature_confirmation|project_cbc7119_design_repo|feedback_cbc7119_design_never_wrangler|project_redesign_workflow' "$SKETCH") (expect >= 12)"

# 보호 파일 영향 0 — git status 안 산출물 1건만
echo "==== GIT STATUS ===="
git -C /Users/jykevin/Documents/cbc7119-design status --short
echo "==== 산출물 외 변경 없으면 'sketch-wave-4-settings-panel.html' 1줄만 보여야 함 ===="

# commit log 확인
echo "==== LAST COMMIT ===="
git -C /Users/jykevin/Documents/cbc7119-design log -1 --oneline --stat | head -10
    </automated>
  </verify>

  <done>
- `cha-bio-safety/docs/redesign-context/31-chrome/sketch-wave-4-settings-panel.html` 신규 파일 1500+ 라인.
- 모든 negative gate 통과 (위 verify 의 expect 0 항목 모두 0).
- positive 검증: semantic token 다수 + 비즈 anchor 20+ + frame 6+ + OQ LOCKED 8+ + 라이트 frame 2+ + Lucide icon 3+ + 메모리 슬러그 12.
- atomic 1-commit (sketch-wave-4-settings-panel.html 단 1 파일 변경).
- 보호 파일 1 byte 변경 0 (src/ + W1 index + 7 스냅샷 + W2/W3 sketch + tokens/typography/design-system + tailwind.config + 00-design-context/).
- `wrangler` / `npm run deploy` 호출 0.
  </done>
</task>

</tasks>

<verification>
**산출물 단일 검증:**
- 파일 존재 + 1500+ 라인 + atomic 1-commit (verify 의 `git log -1 --stat` 안 `sketch-wave-4-settings-panel.html` 1 파일만 표시)
- 보호 파일 11종 (src/ + App.tsx + tokens.css + typography.css + design-system.md + tailwind.config.js + wave-1-index.md + 7 스냅샷 + W2 W3 sketch + 00-design-context/) 1 byte 변경 0

**Negative gate 통과 (sketch body 안):**
- fontSize: 9·10·11 px markup 0 (단 머리 주석 인용 OK)
- linear-gradient 0 (OQ #4 LOCKED 폐기)
- ⏳🔔 이모지 0 (OQ #6 LOCKED 폐기)
- ✕ 텍스트 글리프 0 (W3-OQ #A pattern, Lucide X 교체)
- status-* class prefix 0 (memory `feedback_tailwind_token_class_pattern`)
- legacy alias (--bg/--bg2/--bg3/--bg4/--bd/--bd2/--t1/--t2/--t3/--acl/--danger/--safe/--warn/--info/--fire/--c-*) body 안 0
- raw hex (style="" attribute 안) body 안 0 — 단 :root + [data-theme="light"] 정의 + 머리 주석 인용은 예외

**Positive 검증 (sketch 충실도):**
- semantic 토큰 var(--surface|text|border|accent|status|radius|space|duty) body 50+ 회 사용
- 비즈 anchor 30+ 식별자 머리 주석 박제 (usePersistedCollapse / NotificationPreferences / JSZip / handleDbBackup / handleR2Backup / handleTestPush / handlePrefToggle / ProfileEditForm / ChangePasswordForm / SectionHeader / Toggle / PermBadge / Row / NameEditModal — 10+ unique)
- frame 6~8 (다크 5+ / 라이트 2~3)
- OQ LOCKED 8건 + W3-OQ #A pattern 머리 주석 인용
- 메모리 룰 12 unique slug 모두 인용
- Lucide X / Send / Loader2 인라인 svg path 사용 (OQ #6 + W3-OQ #A)

**노안 격상 표 충실도:**
- fontSize 9 위반 5건 (line 17/124/225/726/738) 격상 11 또는 12
- fontSize 10 위반 13건 (line 89/101/228/232/236/240/244/251/260/264/685/797/826) 격상 12
- fontSize 11 위반 6건+ (line 133/717/760/765) 격상 13
- sketch 머리 주석 표에 신/구 대조 + "sketch 결과 본 후 사용자 결정" 메모

**토큰 매핑 표 충실도:**
- 14 legacy alias → 14 v0.1.1 semantic 매핑 + linear-gradient 폐기 결정 + raw hex 대안 명시
</verification>

<success_criteria>
- [ ] `cha-bio-safety/docs/redesign-context/31-chrome/sketch-wave-4-settings-panel.html` 신규 파일 1500+ 라인 산출 (실측)
- [ ] sketch body 의 negative gate 8개 항목 모두 0
- [ ] sketch 머리 주석 + body 의 positive 검증 모두 통과 (semantic token 50+, 비즈 anchor 20+, frame 6+, OQ LOCKED 8+, 라이트 frame 2+, Lucide icon 3+, 메모리 슬러그 12)
- [ ] atomic 1-commit + 보호 파일 11종 1 byte 변경 0
- [ ] `wrangler` / `npm run deploy` 호출 0
</success_criteria>

<output>
After completion, create `.planning/quick/260527-5nj-redesign-31-chrome-w4-settingspanel-sket/260527-5nj-SUMMARY.md` with:
- sketch 산출물 path + 라인 수 (실측)
- frame 개수 + 다크/라이트 분배
- 머리 주석 박스 개수 + 비즈 anchor 식별자 개수 (실측)
- OQ LOCKED 적용 8건 status
- negative gate 8개 grep 결과 (모두 0 입증)
- 다음 wave 후보 (W5 MenuSettingsSection sketch 별도 quick / W6 chrome 통합 sketch / W7 TSX 변환 별도 quick)
- commit hash + push 안내 (auto-push hook 가동 — memory `feedback_push_proactive`)
</output>
