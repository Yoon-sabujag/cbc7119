---
phase: 260519-kq7-redesign-13-schedule-sketch-wave-4-addmo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cha-bio-safety/docs/redesign-context/13-schedule/sketch-wave-4.html
autonomous: true
requirements:
  - W4-ADDMO-01-modal-chrome-overlay-sheet
  - W4-ADDMO-02-header-title-close
  - W4-ADDMO-03-category-grid-5
  - W4-ADDMO-04-cat-variant-matrix-5
  - W4-ADDMO-05-date-time-grid
  - W4-ADDMO-06-n-day-preview
  - W4-ADDMO-07-cta-grid-3
  - W4-ADDMO-08-state-frames-7
  - W4-ADDMO-09-light-mirror-2
  - W4-ADDMO-10-verify-gate-16
must_haves:
  truths:
    - "sketch-wave-4.html 이 600 라인 이상의 단일 정적 HTML 시안으로 존재한다"
    - "AddModal 의 5 cat variant matrix (inspect / task / event / elevator / fire) 가 모두 시각 시연된다"
    - "INSP_CATEGORIES 19종 / ELEV_SUBCATS 3종 / FIRE_SUBCATS 4종 라벨이 SchedulePage.tsx 원본과 1글자도 다르지 않게 등장한다"
    - "SCHED_CATEGORIES 5 hex (#3b82f6 / #eab308 / #e2e8f0 / #f97316 / #ef4444) 가 모두 등장한다"
    - "라이트 모드 event dot override #94a3b8 가 W1+W2+W3 mirror 로 보존된다"
    - "9·10·11px font-size 가 0건이다 (노안 격상 룰)"
    - "이모지가 0건이다"
    - "status- prefix className 이 0건이다"
    - "data-theme 다크/라이트 두 모드를 모두 노출한다 (1번 빈폼 + 6번 N일 미리보기 라이트 mirror)"
    - "7개 state frame (빈폼/inspect-sub/task/elev-sub/fire-sub/N일/saving) 이 모바일 393px + 데스크톱 480px 중앙 모달 페어로 노출된다"
    - "tokens.css line 16~197 verbatim + typography.css line 28~95 verbatim 임베드된다"
    - "비즈니스 로직 변경 0 — SchedulePage.tsx 파일은 1줄도 수정하지 않는다"
  artifacts:
    - path: "cha-bio-safety/docs/redesign-context/13-schedule/sketch-wave-4.html"
      provides: "AddModal 시안 — 7 state frame + 다크/라이트 mirror"
      min_lines: 600
  key_links:
    - from: "sketch-wave-4.html"
      to: "SchedulePage.tsx line 647~963 (AddModal verbatim)"
      via: "라벨/구조/색상/inline style 패턴 mirror"
      pattern: "INSP_CATEGORIES.*소화기.*소화전.*방화문|ELEV_SUBCATS.*승강기 정기 점검|FIRE_SUBCATS.*소방"
    - from: "sketch-wave-4.html"
      to: "inspection-modal-chrome-rules.md §1~§6"
      via: "overlay rgba(0,0,0,0.55) z-300 + sheet bg-surface-raised + radius-lg 16 desktop / borderTopRadius 20 mobile"
      pattern: "rgba\\(0,0,0,0\\.55\\).*z-?(?:index)?:?\\s*300"
    - from: "sketch-wave-4.html"
      to: "sketch-wave-1.html LOCKED + sketch-wave-2.html LOCKED + sketch-wave-3.html LOCKED"
      via: "카테고리 5 hex set / event #94a3b8 라이트 / 이모지 0 / 노안 12px 마지노선"
      pattern: "#3b82f6.*#eab308.*#e2e8f0.*#f97316.*#ef4444"
---

<objective>
redesign/13-schedule 의 4번째 sketch wave. SchedulePage.tsx 의 AddModal (line 647~963) 을
정적 HTML 시안 1장으로 표현한다. TSX 변환은 별도 wave — 이 wave 는 디자인 검수용 시안 only.

Purpose: 사용자가 5 cat variant (inspect/task/event/elevator/fire) × 7 state frame 을
한 페이지에서 다크/라이트 mirror 로 시각 검토 후 컨펌하면, 그 컨펌된 시안 위에서 다음 wave 에서 TSX 변환.

Output: cha-bio-safety/docs/redesign-context/13-schedule/sketch-wave-4.html (≥600 lines, self-contained)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@CLAUDE.local.md
@cha-bio-safety/docs/redesign-context/13-schedule/13-schedule.md
@cha-bio-safety/docs/redesign-context/13-schedule/design-system.md
@cha-bio-safety/docs/redesign-context/13-schedule/tokens.css
@cha-bio-safety/docs/redesign-context/13-schedule/typography.css
@cha-bio-safety/docs/redesign-context/00-design-context/inspection-modal-chrome-rules.md
@cha-bio-safety/docs/redesign-context/13-schedule/sketch-wave-1.html
@cha-bio-safety/docs/redesign-context/13-schedule/sketch-wave-2.html
@cha-bio-safety/docs/redesign-context/13-schedule/sketch-wave-3.html
@cha-bio-safety/docs/redesign-context/13-schedule/SchedulePage.tsx
</context>

<source_verbatim>

다음 source 상수/라벨은 **SchedulePage.tsx 원본에서 grep 추출한 verbatim** 입니다.
시안 작성 시 1글자 다르면 verify gate 실패. 그대로 복사하세요.

### SchedulePage.tsx line 35~39 — INSP_CATEGORIES (19종, 순서 그대로)
```ts
const INSP_CATEGORIES = [
  '소화기','소화전','방화문','특별피난계단','유도등','방화셔터','DIV','컴프레셔',
  '비상콘센트','배연창','주차장비','완강기','전실제연댐퍼',
  '청정소화약제','연결송수관','소방용전원공급반','회전문','소방펌프','CCTV',
]
```

### SchedulePage.tsx line 63 — ELEV_SUBCATS (3종, source verbatim)
```ts
const ELEV_SUBCATS = ['승강기 정기 점검', '승강기 수리', '승강기 법정 검사'] as const
```

> ⚠ **task_scope 본문에는 "자체 점검 / 안전관리자 교육" 으로 적혀 있지만, SchedulePage.tsx 원본 line 63 은**
> `'승강기 정기 점검', '승강기 수리', '승강기 법정 검사'` **입니다. 비즈니스 로직 변경 0 룰 + source verbatim 룰에 따라 source 를 따릅니다.**
> task_scope 의 라벨이 사용자가 의도한 변경이라면 별도 wave 에서 SchedulePage.tsx 자체를 패치한 뒤 sketch 재작성해야 합니다 — 이번 wave 는 시안 only 이므로 source 라벨 그대로 사용합니다.

### SchedulePage.tsx line 64 — FIRE_SUBCATS (4종, source verbatim)
```ts
const FIRE_SUBCATS = ['소방 상반기 종합정밀점검', '소방 하반기 작동기능점검', '소방 시설물 공사', '소방 관공서 불시 점검'] as const
```

> ⚠ **task_scope 본문의 "소방 하반기 종합정밀점검 / 소방 작동기능점검 / 소방안전관리자 보수교육" 도 source 와 다릅니다.**
> 같은 이유로 source 라벨 (`소방 상반기 종합정밀점검 / 소방 하반기 작동기능점검 / 소방 시설물 공사 / 소방 관공서 불시 점검`) 을 따릅니다.

### SchedulePage.tsx line 69~73 — ELEV_AGENCY (매핑 verbatim)
```ts
const ELEV_AGENCY: Record<ElevSubCat, string> = {
  '승강기 정기 점검': 'TKE',
  '승강기 수리':     'TKE',
  '승강기 법정 검사': '한국승강기안전공단',
}
```

### SchedulePage.tsx line 74~79 — FIRE_AGENCY (매핑 verbatim)
```ts
const FIRE_AGENCY: Record<FireSubCat, string> = {
  '소방 상반기 종합정밀점검': '동양소방',
  '소방 하반기 작동기능점검': '동양소방',
  '소방 시설물 공사':       '동양소방',
  '소방 관공서 불시 점검':   '성남소방서',
}
```

### SchedulePage.tsx line 81~87 — SCHED_CATEGORIES (5종 hex verbatim)
```ts
const SCHED_CATEGORIES: { value: ScheduleCategory; label: string; color: string }[] = [
  { value:'inspect',  label:'점검',  color:'#3b82f6' },
  { value:'task',     label:'업무',  color:'#eab308' },
  { value:'event',    label:'행사',  color:'#e2e8f0' },
  { value:'elevator', label:'승강기', color:'#f97316' },
  { value:'fire',     label:'소방',  color:'#ef4444' },
]
```

### SchedulePage.tsx line 41~61 — INSP_DEFAULTS (시안 시연용 5개만 노출)
시안에서는 화면 분량 절약을 위해 inspect-sub 선택 frame 에서 `소화기` 케이스 1개만 풀 노출하면 충분.
나머지 18개는 `<select>` option 리스트로 verbatim 노출 (19개 option 모두).

| key | title verbatim | memo 1줄 verbatim 미리보기 (생략 표시 OK) |
|---|---|---|
| 소화기 | 전층 소화기 점검 | "   - 소화기 압력상태 점검\n   - 안전핀 체결상태 확인\n..." |
| 소화전 | 전층 소화전 점검 | (생략 — option 리스트만 노출) |
| 방화문 | 전층 방화문 동작상태 점검 | (생략) |
| ... | ... | (생략) |

### AddModal 핵심 inline style 패턴 (line 647~963 verbatim)
- overlay: `position:fixed; inset:0; background:rgba(0,0,0,0.55); zIndex:300; display:flex; flexDirection:column; justifyContent: isDesktop ? 'center' : 'flex-end'; alignItems: isDesktop ? 'center' : undefined`
- sheet: `background:var(--bg2); borderRadius: isDesktop ? 16 : '20px 20px 0 0'; padding: isDesktop ? '24px 28px 28px' : '20px 16px 40px'; maxHeight:'90dvh'; overflowY:'auto'; (desktop) width:480, maxWidth:'90vw'`
- 헤더 close X: `width:28, height:28, borderRadius:7, border:'1px solid var(--bd)', background:'var(--bg3)'` + `<svg width=14 height=14 stroke=var(--t2) strokeWidth=2 X path>`
- 카테고리 grid: `display:grid; gridTemplateColumns:'repeat(5,1fr)'; gap:5` / 버튼 `padding:'8px 0', borderRadius:8, border:1px solid (selected? c.color : --bd), background:(selected? c.color+'22' : var(--bg3)), color:(selected? c.color : var(--t2)), fontSize:11, fontWeight:700`
- inspect select: `<select style={inp}>` + 19 option verbatim
- elevator grid: `display:grid; gridTemplateColumns:'repeat(3,1fr)'; gap:6` / 버튼 `padding:'9px 0', borderRadius:9, fontSize:12, fontWeight:700, border:1px solid (selected? '#f97316' : --bd), background:(selected? 'rgba(249,115,22,0.15)' : var(--bg3)), color:(selected? '#f97316' : var(--t2))`
- fire grid: `display:grid; gridTemplateColumns:'repeat(4,1fr)'; gap:6` / 버튼 `padding:'8px 4px', borderRadius:9, fontSize:10, fontWeight:700, lineHeight:1.4, border:1px solid (selected? '#ef4444' : --bd), background:(selected? 'rgba(239,68,68,0.13)' : var(--bg3)), color:(selected? '#ef4444' : var(--t2))`
- N일 미리보기 (line 930~937): `fontSize:12, color:'var(--acl)', fontWeight:600, textAlign:'center', marginTop:-8` + 문구 `${rangeDays}일 범위로 1건 추가됩니다 (주말·공휴일 ${skippedCount}일은 표시에서 제외)`
- CTA grid (line 939~959): `gridTemplateColumns:'1fr 3fr 1fr', gap:6, marginTop:4` / 좌우 `padding:'14px 0', borderRadius:12, border:'1px solid var(--bd2)', background:'var(--bg3)', color:'var(--t2)', fontSize:18, lineHeight:1` / 중앙 `padding:'14px', borderRadius:12, border:'none', background:'linear-gradient(135deg,#1d4ed8,#2563eb)', color:'#fff', fontSize:14, fontWeight:700, opacity:saving?0.6:1` + label `saving ? '저장 중...' : '저장'`
- label/input 토큰 (line 1055~1062):
  - `const lbl: { fontSize:11, fontWeight:700, color:'var(--t3)', display:'block', marginBottom:6 }`
  - `const inp: { width:'100%', padding:'11px 12px', borderRadius:10, boxSizing:'border-box', background:'var(--bg3)', border:'1px solid var(--bd2)', color:'var(--t1)', fontSize:13, outline:'none', fontFamily:'inherit' }`
  - 노안 격상: lbl 11 → 12 (text-caption + leading-none — 작은 label 영역 룰 mirror — feedback_text_caption_leading_none.md)

</source_verbatim>

<noan_upgrade_table>

source 의 9·10·11px 폰트는 모두 12px (text-caption) 이상으로 격상. 룰: 노안 친화 12px 마지노선.

| 위치 | source px | 격상 px | 클래스 매핑 |
|---|---|---|---|
| 카테고리 grid 버튼 라벨 (점검/업무/행사/승강기/소방) | 11 | 12 | text-caption + leading-none |
| label `lbl` (구분/제목/내용/시작일/시작시간/종료일/종료시간/업체 및 기관/점검 분류) | 11 | 12 | text-caption + leading-none |
| 헤더 타이틀 "일정 추가" | 15 | 18 | text-title font-bold (text-title 18 = 시안 모달 헤더 마지노선, design-system.md §2.7) |
| fire grid 4열 라벨 (상반기/하반기/시설물/관공서) | 10 | 12 | text-caption + leading-snug (4글자 wrap 허용 — `lineHeight:1.4` source mirror) |
| inp 입력 (`fontSize:13`) | 13 | 13 | text-label (변경 없음 — 13 은 OK) |
| N일 미리보기 | 12 | 12~13 | text-caption verbatim 유지 OK (또는 text-label 13 격상 검토 — 사용자 검토 frame 으로 노출) |
| CTA 저장 버튼 | 14 | 16 | text-body (mobile button 마지노선 16) |
| CTA 좌우 화살표 ‹/› | 18 | 18 | 변경 없음 (terminal icon은 시각 인지 OK) |
| close X svg | 14 | 14 | 변경 없음 (svg viewBox 24, 시각 인지 OK) |

</noan_upgrade_table>

<state_frame_matrix>

총 7 frame, 각 모바일 393px BottomSheet + 데스크톱 480px 중앙 모달 페어. 다크 모드 메인.
라이트 mirror 는 frame #1 + frame #6 두 개만 (검수 부담 감축).

| # | frame 라벨 | state | 노출 UI |
|---|---|---|---|
| 1 | 빈폼 / inspect 기본 | `cat='inspect', insCat='', date='2026-05-12'` | 카테고리 grid (점검 selected) + 점검 분류 select (값 "-- 선택하세요 --") + 시작일/시간 + 종료일/시간 + CTA |
| 2 | inspect sub 선택 | `cat='inspect', insCat='소화기', inspTitle='전층 소화기 점검', memo=INSP_DEFAULTS['소화기'].memo` | + 제목 input + 내용 textarea (5 row, memo verbatim) |
| 3 | task 업무 입력 | `cat='task', title='당직 인계 정리', memo='야간 당직 인계 사항 정리'` | 카테고리 grid (업무 selected) + 제목 input + 내용 textarea (3 row, 선택 라벨) |
| 4 | elevator sub 선택 | `cat='elevator', elevSub='승강기 정기 점검', elevAgency='TKE'` | 카테고리 grid (승강기 selected) + 제목 grid 3 (정기 점검 selected #f97316 alpha 0.15) + 업체 input ('TKE' 자동) + 내용 textarea |
| 5 | fire sub 선택 | `cat='fire', fireSub='소방 상반기 종합정밀점검', fireAgency='동양소방'` | 카테고리 grid (소방 selected) + 제목 grid 4 (상반기 selected #ef4444 alpha 0.13) + 업체 input ('동양소방' 자동) + 내용 textarea |
| 6 | N일 미리보기 (2 sub-frame) | (a) `date='2026-05-12', endDate='2026-05-15', rangeDays=4, skippedCount=0` → "4일 범위로 1건 추가됩니다" / (b) `date='2026-05-12', endDate='2026-05-19', rangeDays=8, skippedCount=2` → "8일 범위로 1건 추가됩니다 (주말·공휴일 2일은 표시에서 제외)" | 시작일/시간 + 종료일/시간 + N일 미리보기 문구 + CTA. 두 sub-frame 한 페이지에 좌우로 나란히. |
| 7 | saving 저장 중 | `cat='inspect', insCat='소화기', saving=true` | CTA 저장 버튼 label "저장 중..." + opacity 0.6 (좌우 화살표도 disabled) |

라이트 mirror (다크 frame 옆에 같은 너비로):
- frame #1 라이트 — 빈폼 + event #94a3b8 hardcode override 시연 (chrome consistency)
- frame #6a 라이트 — N일 미리보기 (skippedCount=0)

</state_frame_matrix>

<tasks>

<task type="auto">
  <name>Task 1: sketch-wave-4.html 작성 (AddModal 7 state frame 전체)</name>
  <files>cha-bio-safety/docs/redesign-context/13-schedule/sketch-wave-4.html</files>
  <action>
SchedulePage.tsx line 647~963 (AddModal) 를 정적 HTML 시안으로 작성한다. **TSX 변환 절대 금지** — SchedulePage.tsx 파일은 1줄도 수정하지 않는다.

### 1. 파일 헤더 (HTML 코멘트)

W3 sketch-wave-3.html line 1~42 패턴 mirror. 다음 항목 포함:
- 파일명, 작성일 (2026-05-19), quick ID (260519-kq7)
- 범위: AddModal (SchedulePage.tsx line 647~963 verbatim)
- out of scope: W1/W2/W3 영역, EditModal, MonthlyPlanPreview, TSX 변환
- 참조: design-system.md v0.1.1, SchedulePage.tsx 라인 번호, sketch-wave-1/2/3.html LOCKED 결정, inspection-modal-chrome-rules.md §1~§6
- 메모리 룰 (이모지 0, 9·10·11px 0, status- prefix 0, SCHED_CATEGORIES 5 hex verbatim, 라이트 event #94a3b8 override, tokens.css/typography.css verbatim 임베드, W1+W2+W3 LOCKED mirror, 비즈 로직 0 변경)
- open questions 3개 (아래 §8 참조)
- LOCKED 항목 (source verbatim 라벨 차이 — ELEV_SUBCATS / FIRE_SUBCATS 는 task_scope 본문 아닌 SchedulePage.tsx 원본 따름)

### 2. tokens.css + typography.css verbatim 임베드

W3 sketch-wave-3.html line 49~252 패턴 그대로 복사:
- `:root, [data-theme="dark"]` — tokens.css line 16~69 verbatim
- `[data-theme="light"]` — tokens.css line 74~119 verbatim
- spacing tokens + media query (768px) — tokens.css line 122~162 verbatim
- radius — tokens.css line 165~172 verbatim
- typography (.text-caption ~ .text-display + .text-mono + .font-medium/semibold) — typography.css line 28~95 verbatim

### 3. 추가 헬퍼 CSS

- `.frame-shell` (W3 line 257~264 mirror — outer border + radius 18)
- `.frame-mobile { width: 393px; min-height: 720px; }` 모바일 BottomSheet 시안 컨테이너 (모달 sheet 가 하단에 붙음)
- `.frame-desktop { width: 1280px; min-height: 720px; position: relative; }` + 중앙 모달 sheet 480px
- `.modal-overlay { position: absolute; inset: 0; background: rgba(0,0,0,0.55); display: flex; justify-content: center; align-items: center; }` (desktop)
- `.modal-overlay-mobile { ... justify-content: flex-end; align-items: stretch; }` (mobile — BottomSheet 하단 정렬)
- `.modal-sheet-desktop { width: 480px; max-width: 90vw; background: var(--surface-raised); border-radius: 16px; padding: 24px 28px 28px; max-height: 90vh; overflow-y: auto; }`
- `.modal-sheet-mobile { width: 100%; background: var(--surface-raised); border-radius: 20px 20px 0 0; padding: 20px 16px 40px; max-height: 90vh; overflow-y: auto; }`
- `.cat-btn` / `.cat-btn-selected` (5종 hex 별 alpha bg + border)
- `[data-theme="light"] .event-dot-themed { background: #94a3b8 !important; }` (W1+W2+W3 LOCKED mirror — frame #1 라이트 시연에서 사용)

### 4. body 구조 (frame-grid 단일 컬럼)

```
body
├── 페이지 헤더 (h1 "13 월간 점검 계획 — sketch W4 AddModal" + LOCKED 결정 요약 메모)
├── frame-row #1: 빈폼 / inspect 기본 (모바일 + 데스크톱 다크 + 라이트 mirror)
├── frame-row #2: inspect sub 선택 — 소화기 (모바일 + 데스크톱 다크)
├── frame-row #3: task 업무 입력 (모바일 + 데스크톱 다크)
├── frame-row #4: elevator sub 선택 (모바일 + 데스크톱 다크)
├── frame-row #5: fire sub 선택 (모바일 + 데스크톱 다크)
├── frame-row #6: N일 미리보기 — 2 sub-frame (a: skipped=0 다크 + 라이트 / b: skipped=2 다크)
├── frame-row #7: saving 저장 중 (모바일 + 데스크톱 다크)
└── 푸터 OQ #1~#3 사용자 답변 요청 박스
```

각 frame 위에 label `<h3 class="text-label" style="color:var(--text-tertiary)">Frame N — {라벨}</h3>` 표시.

### 5. 각 frame 안의 AddModal 구조 (verbatim mirror)

#### 5.1 모달 sheet (모바일 / 데스크톱 분기)

모바일: `<div class="modal-overlay-mobile" data-theme="dark"><div class="modal-sheet-mobile">...</div></div>`
데스크톱: `<div class="modal-overlay" data-theme="dark"><div class="modal-sheet-desktop">...</div></div>`

#### 5.2 헤더 (line 770~779 verbatim 구조, 노안 격상)

```html
<div style="display:flex; align-items:center; justify-content:space-between; margin-bottom:18px;">
  <span class="text-title font-bold" style="color:var(--text-primary);">일정 추가</span>
  <button style="width:32px; height:32px; border-radius:8px; border:1px solid var(--border-default); background:var(--surface-sunken); cursor:pointer; display:flex; align-items:center; justify-content:center;">
    <svg width="16" height="16" fill="none" viewBox="0 0 24 24" stroke="var(--text-secondary)" stroke-width="2">
      <path stroke-linecap="round" stroke-linejoin="round" d="M6 18L18 6M6 6l12 12"/>
    </svg>
  </button>
</div>
```

> 노안 격상: source 15 → 18 (text-title font-bold), close 28→32 + radius 7→8 + svg 14→16.

#### 5.3 본문 컨테이너

`<div style="display:flex; flex-direction:column; gap:16px;">` (line 781 verbatim)

#### 5.4 카테고리 grid (line 783~798 — 모든 frame 공통)

```html
<div>
  <label class="text-caption" style="font-weight:700; color:var(--text-tertiary); display:block; margin-bottom:6px; line-height:1;">구분</label>
  <div style="display:grid; grid-template-columns:repeat(5,1fr); gap:5px;">
    <!-- frame 별로 selected 다름 -->
    <button class="cat-btn cat-btn-selected" data-cat-color="#3b82f6" style="padding:10px 0; border-radius:8px; border:1px solid #3b82f6; background:#3b82f622; color:#3b82f6; font-size:12px; font-weight:700; line-height:1; cursor:pointer;">점검</button>
    <button class="cat-btn" style="padding:10px 0; border-radius:8px; border:1px solid var(--border-default); background:var(--surface-sunken); color:var(--text-secondary); font-size:12px; font-weight:700; line-height:1; cursor:pointer;">업무</button>
    <button class="cat-btn" style="...">행사</button>
    <button class="cat-btn" style="...">승강기</button>
    <button class="cat-btn" style="...">소방</button>
  </div>
</div>
```

> 노안 격상: padding 8 → 10, fontSize 11 → 12, lineHeight 1 (작은 컨테이너 룰).

#### 5.5 cat variant 5 (frame 별로 다름)

**inspect frame #1 (insCat='')**:
```html
<div>
  <label class="text-caption" style="...">점검 분류</label>
  <select style="{inp 토큰 — width:100%; padding:11px 12px; border-radius:10px; box-sizing:border-box; background:var(--surface-sunken); border:1px solid var(--border-default); color:var(--text-primary); font-size:13px; outline:none;}">
    <option value="">-- 선택하세요 --</option>
    <option>소화기</option>
    <option>소화전</option>
    <option>방화문</option>
    <option>특별피난계단</option>
    <option>유도등</option>
    <option>방화셔터</option>
    <option>DIV</option>
    <option>컴프레셔</option>
    <option>비상콘센트</option>
    <option>배연창</option>
    <option>주차장비</option>
    <option>완강기</option>
    <option>전실제연댐퍼</option>
    <option>청정소화약제</option>
    <option>연결송수관</option>
    <option>소방용전원공급반</option>
    <option>회전문</option>
    <option>소방펌프</option>
    <option>CCTV</option>
  </select>
</div>
```

> 19 option verbatim.

**inspect frame #2 (insCat='소화기')**: select 위에 동일하게 두고, 아래에 제목 input + 내용 textarea (memo verbatim) 추가:
```html
<div>
  <label class="text-caption" style="...">제목</label>
  <input value="전층 소화기 점검" style="{inp}" />
</div>
<div>
  <label class="text-caption" style="...">내용</label>
  <textarea rows="5" style="{inp; resize:none; line-height:1.6;}">   - 소화기 압력상태 점검
   - 안전핀 체결상태 확인
   - 위치표시 스티커 부착상태 점검
   - 받침대 파손여부 점검</textarea>
</div>
```

**task / event frame #3** (line 826~840 mirror):
```html
<div>
  <label class="text-caption" style="...">제목</label>
  <input value="당직 인계 정리" placeholder="일정 제목" style="{inp}" />
</div>
<div>
  <label class="text-caption" style="...">내용 <span style="font-weight:400; color:var(--text-tertiary);">(선택)</span></label>
  <textarea rows="3" placeholder="상세 내용을 입력하세요" style="{inp; resize:none; line-height:1.5;}">야간 당직 인계 사항 정리</textarea>
</div>
```

**elevator frame #4** (line 842~870 mirror — ELEV_SUBCATS 3종 verbatim):
```html
<div>
  <label class="text-caption" style="...">제목</label>
  <div style="display:grid; grid-template-columns:repeat(3,1fr); gap:6px;">
    <button style="padding:9px 0; border-radius:9px; font-size:12px; font-weight:700; cursor:pointer; line-height:1.4; border:1px solid #f97316; background:rgba(249,115,22,0.15); color:#f97316;">승강기 정기 점검</button>
    <button style="padding:9px 0; border-radius:9px; font-size:12px; font-weight:700; cursor:pointer; line-height:1.4; border:1px solid var(--border-default); background:var(--surface-sunken); color:var(--text-secondary);">승강기 수리</button>
    <button style="...">승강기 법정 검사</button>
  </div>
</div>
<div>
  <label class="text-caption" style="...">업체 및 기관</label>
  <input value="TKE" style="{inp}" />
</div>
<div>
  <label class="text-caption" style="...">내용 <span style="...">(선택)</span></label>
  <textarea rows="3" placeholder="상세 내용을 입력하세요" style="{inp; resize:none; line-height:1.5;}"></textarea>
</div>
```

**fire frame #5** (line 872~899 mirror — FIRE_SUBCATS 4종 verbatim, 노안 격상 10→12):
```html
<div>
  <label class="text-caption" style="...">제목</label>
  <div style="display:grid; grid-template-columns:repeat(4,1fr); gap:6px;">
    <button style="padding:8px 4px; border-radius:9px; font-size:12px; font-weight:700; cursor:pointer; line-height:1.3; border:1px solid #ef4444; background:rgba(239,68,68,0.13); color:#ef4444;">소방 상반기 종합정밀점검</button>
    <button style="padding:8px 4px; border-radius:9px; font-size:12px; font-weight:700; cursor:pointer; line-height:1.3; border:1px solid var(--border-default); background:var(--surface-sunken); color:var(--text-secondary);">소방 하반기 작동기능점검</button>
    <button style="...">소방 시설물 공사</button>
    <button style="...">소방 관공서 불시 점검</button>
  </div>
</div>
<div>
  <label class="text-caption" style="...">업체 및 기관</label>
  <input value="동양소방" style="{inp}" />
</div>
<div>
  <label class="text-caption" style="...">내용 <span style="...">(선택)</span></label>
  <textarea rows="3" placeholder="상세 내용을 입력하세요" style="{inp; resize:none; line-height:1.5;}"></textarea>
</div>
```

> ⚠ 노안 격상: fire grid fontSize **10 → 12** + lineHeight 1.4 → 1.3 (text-caption + leading-snug). 4글자 wrap 허용 (`소방 상반기 종합정밀점검` 은 두 줄 wrap).

#### 5.6 시작일/시간 + 종료일/시간 (line 902~928 verbatim, 모든 frame 공통)

```html
<div style="display:flex; gap:10px;">
  <div style="flex:0 0 calc(50% - 5px); min-width:0; overflow:hidden;">
    <label class="text-caption" style="...">시작일</label>
    <input type="date" value="2026-05-12" style="{inp; display:block; height:44px;}" />
  </div>
  <div style="flex:0 0 calc(50% - 5px); min-width:0; overflow:hidden;">
    <label class="text-caption" style="...">시작시간 <span style="...">(선택)</span></label>
    <input type="time" value="" style="{inp; display:block; height:44px;}" />
  </div>
</div>
<div style="display:flex; gap:10px;">
  <div style="...">
    <label class="text-caption" style="...">종료일 <span style="...">(선택)</span></label>
    <input type="date" value="2026-05-12" min="2026-05-12" style="{inp; display:block; height:44px;}" />
  </div>
  <div style="...">
    <label class="text-caption" style="...">종료시간 <span style="...">(선택)</span></label>
    <input type="time" value="" style="{inp; display:block; height:44px;}" />
  </div>
</div>
```

frame #6a: `endDate="2026-05-15"` (rangeDays=4, skipped=0)
frame #6b: `endDate="2026-05-19"` (rangeDays=8, skipped=2 — 5/16 + 5/17 주말)

#### 5.7 N일 미리보기 (line 930~937, frame #6 에서만 노출)

```html
<div style="font-size:12px; color:var(--accent); font-weight:600; text-align:center; margin-top:-8px; line-height:1.5;">
  4일 범위로 1건 추가됩니다
</div>
<!-- 또는 frame #6b: -->
<div style="...">
  8일 범위로 1건 추가됩니다 (주말·공휴일 2일은 표시에서 제외)
</div>
```

> 노안 격상: source 12 verbatim 유지 OK.

#### 5.8 CTA grid (line 939~959 verbatim, 노안 격상 14→16)

```html
<div style="display:grid; grid-template-columns:1fr 3fr 1fr; gap:6px; margin-top:4px;">
  <button style="padding:14px 0; border-radius:12px; border:1px solid var(--border-default); background:var(--surface-sunken); color:var(--text-secondary); font-size:18px; line-height:1; cursor:pointer; display:flex; align-items:center; justify-content:center;">‹</button>
  <button style="padding:14px; border-radius:12px; border:none; background:linear-gradient(135deg,#1d4ed8,#2563eb); color:#fff; font-size:16px; font-weight:700; cursor:pointer;">저장</button>
  <button style="padding:14px 0; ...">›</button>
</div>
```

frame #7 (saving=true): 저장 버튼 label "저장 중..." + `opacity:0.6` + cursor:default. 좌우 화살표도 opacity 0.6 + cursor:default.

### 6. 라이트 mirror (frame #1 + frame #6a)

frame-row 안에 다크 옆에 라이트 column 추가:
```html
<div class="frame-row" style="display:flex; gap:24px; flex-wrap:wrap; margin-bottom:32px;">
  <div class="frame-mobile frame-shell" data-theme="dark">...</div>
  <div class="frame-desktop frame-shell" data-theme="dark">...</div>
  <div class="frame-mobile frame-shell" data-theme="light" style="background:#ffffff;">...</div>  <!-- 라이트 mirror -->
  <div class="frame-desktop frame-shell" data-theme="light" style="background:#ffffff;">...</div>
</div>
```

frame #1 라이트: 카테고리 grid 의 event 칩 (행사 #e2e8f0) 위에 `.event-dot-themed` span 1개 추가 시각 (라이트 override #94a3b8 시연용 — chrome consistency).

### 7. inline style 예외 사유

본 시안은 5 cat variant × 7 state frame × 다크/라이트 mirror 의 dynamic 색상 매트릭스라
Tailwind utility 로 모든 조합 (예: `cat selected hex alpha bg`, `fire sub selected alpha 0.13`) 을 표현하기 어렵다.
**예외 사유 명시**: 카테고리/sub 선택 alpha bg + border 색은 dynamic — class 매핑 불가능.
나머지 chrome (overlay, sheet, input, label) 은 가능한 한 토큰 var() 사용.

### 8. 푸터 — Open Questions 3건

```html
<div style="margin-top:48px; padding:24px; border:1px solid var(--border-default); border-radius:12px; background:var(--surface-raised);">
  <h3 class="text-title" style="margin:0 0 16px;">Open Questions (사용자 답변 요청)</h3>

  <p><strong>OQ #1</strong> — 모바일 BottomSheet maxHeight</p>
  <ul>
    <li>a) source 90dvh verbatim 유지</li>
    <li>b) 80vh 축소 (검수 편의)</li>
  </ul>

  <p><strong>OQ #2</strong> — 저장 버튼 색</p>
  <ul>
    <li>a) source linear-gradient(135deg, #1d4ed8, #2563eb) verbatim 유지</li>
    <li>b) accent solid (디자인 시스템 토큰 일관)</li>
  </ul>

  <p><strong>OQ #3</strong> — INSP_CATEGORIES 19종 입력 형식</p>
  <ul>
    <li>a) source &lt;select&gt; native 그대로 (현재 시안)</li>
    <li>b) grid 3-4열 칩 (검수 편의 + 시각 정합)</li>
  </ul>

  <p><strong>LOCKED 사전 결정 (사용자 답변 불필요)</strong></p>
  <ul>
    <li>ELEV_SUBCATS / FIRE_SUBCATS 라벨은 SchedulePage.tsx 원본 (line 63~64) verbatim. task_scope 본문의 다른 라벨 (자체 점검 / 안전관리자 교육 / 보수교육 등) 은 별도 wave 에서 SchedulePage.tsx 자체 패치 후 sketch 재작성 필요.</li>
    <li>카테고리 5 hex (#3b82f6 / #eab308 / #e2e8f0 / #f97316 / #ef4444) — W1+W2+W3 LOCKED mirror</li>
    <li>라이트 event #94a3b8 hardcode override — W1+W2+W3 LOCKED mirror</li>
    <li>이모지 0 — feedback_tsx_wave_emoji_dot_gap.md</li>
    <li>노안 격상: 9·10·11px → 12 이상 (각 위치별 노안 격상 표 참조)</li>
  </ul>
</div>
```

### 9. 작성 후 self-check (Bash grep — verify gate 16건 모두 PASS 확인)

작성 직후 아래 16개 grep 을 직접 실행해서 모두 PASS 한 다음에야 사용자에게 결과 보고.
실패 1건이라도 있으면 sketch 재작성 (Write tool 으로 재기록).
  </action>
  <verify>
    <automated>
SKETCH=cha-bio-safety/docs/redesign-context/13-schedule/sketch-wave-4.html

# 1. 파일 ≥ 600 라인
LINES=$(wc -l < "$SKETCH")
echo "1. 라인 수: $LINES (≥600)"
test "$LINES" -ge 600

# 2. 이모지 0 (Python regex — emoji + symbol ranges)
EMOJI=$(python3 -c "import re,sys; t=open('$SKETCH',encoding='utf-8').read(); print(len(re.findall(r'[\U0001F000-\U0001FFFF\U00002600-\U000027BF\U0001F300-\U0001F9FF\U0001F600-\U0001F64F\U0001F680-\U0001F6FF\U0001F700-\U0001F77F\U0001F780-\U0001F7FF\U0001F800-\U0001F8FF\U0001F900-\U0001F9FF\U0001FA00-\U0001FA6F\U0001FA70-\U0001FAFF]', t)))")
echo "2. 이모지: $EMOJI (=0)"
test "$EMOJI" = "0"

# 3. 9·10·11px font-size 0
# (HTML 코멘트 안의 `source 9 → 12` 같은 텍스트는 검사에서 제외 — 실제 CSS 값만)
NOAN=$(grep -v '^[[:space:]]*<!--\|^[[:space:]]*\*\|source.*→\|source.*->\|노안 격상\|→ 12\|→ 13\|→ 16\|→ 18' "$SKETCH" | grep -cE 'font-size:[[:space:]]*(9|10|11)px|fontSize:[[:space:]]*(9|10|11)[^0-9]')
echo "3. 9/10/11px: $NOAN (=0)"
test "$NOAN" = "0"

# 4. status- prefix className 0 (class 속성 안의 status- 로 시작하는 token, var(--status-*) 은 OK)
STATUS_PREFIX=$(grep -oE 'class="[^"]*"' "$SKETCH" | grep -cE 'status-(fire|safe|danger|warning|info)-(bar|bg)') 2>/dev/null
echo "4. status- prefix className: ${STATUS_PREFIX:-0} (=0)"
test "${STATUS_PREFIX:-0}" = "0"

# 5. 카테고리 5 hex 모두
for HEX in "#3b82f6" "#eab308" "#e2e8f0" "#f97316" "#ef4444"; do
  COUNT=$(grep -c "$HEX" "$SKETCH")
  echo "5. $HEX: $COUNT (≥1)"
  test "$COUNT" -ge 1
done

# 6. 라이트 event #94a3b8 hardcode
LIGHT_EVENT=$(grep -c "#94a3b8" "$SKETCH")
echo "6. #94a3b8 라이트 event: $LIGHT_EVENT (≥1)"
test "$LIGHT_EVENT" -ge 1

# 7. INSP_CATEGORIES 19개 모두 (option 안 또는 다른 곳)
for CAT in "소화기" "소화전" "방화문" "특별피난계단" "유도등" "방화셔터" "DIV" "컴프레셔" "비상콘센트" "배연창" "주차장비" "완강기" "전실제연댐퍼" "청정소화약제" "연결송수관" "소방용전원공급반" "회전문" "소방펌프" "CCTV"; do
  COUNT=$(grep -c "$CAT" "$SKETCH")
  echo "7. INSP_CATEGORIES '$CAT': $COUNT (≥1)"
  test "$COUNT" -ge 1
done

# 8. ELEV_SUBCATS 3개 verbatim (SchedulePage.tsx line 63 원본)
for SUB in "승강기 정기 점검" "승강기 수리" "승강기 법정 검사"; do
  COUNT=$(grep -c "$SUB" "$SKETCH")
  echo "8. ELEV_SUBCATS '$SUB': $COUNT (≥1)"
  test "$COUNT" -ge 1
done

# 9. FIRE_SUBCATS 4개 verbatim (SchedulePage.tsx line 64 원본)
for SUB in "소방 상반기 종합정밀점검" "소방 하반기 작동기능점검" "소방 시설물 공사" "소방 관공서 불시 점검"; do
  COUNT=$(grep -c "$SUB" "$SKETCH")
  echo "9. FIRE_SUBCATS '$SUB': $COUNT (≥1)"
  test "$COUNT" -ge 1
done

# 10. tokens.css verbatim 임베드 (--surface-page + --duty-day + light --status-fire-bar)
TOKENS_MARKER=$(grep -c -E "(--surface-page:[[:space:]]*#0a0d12|--duty-day:[[:space:]]*#f59e0b)" "$SKETCH")
echo "10. tokens.css 임베드 마커: $TOKENS_MARKER (≥2)"
test "$TOKENS_MARKER" -ge 2

# 11. typography.css verbatim 임베드 (.text-caption + .text-title 정의)
TYPO_MARKER=$(grep -cE "\.text-(caption|title|body)" "$SKETCH")
echo "11. typography 클래스 정의: $TYPO_MARKER (≥3)"
test "$TYPO_MARKER" -ge 3

# 12. data-theme 다크/라이트 토글 둘 다
DARK=$(grep -c 'data-theme="dark"' "$SKETCH")
LIGHT=$(grep -c 'data-theme="light"' "$SKETCH")
echo "12. data-theme dark: $DARK, light: $LIGHT (각 ≥1)"
test "$DARK" -ge 1 && test "$LIGHT" -ge 1

# 13. "일정 추가" 헤더 타이틀
HEAD=$(grep -c "일정 추가" "$SKETCH")
echo "13. '일정 추가' 헤더: $HEAD (≥7 — 7 state frame)"
test "$HEAD" -ge 7

# 14. N일 미리보기 문구 (frame #6 의 두 변형)
PREVIEW_A=$(grep -c "4일 범위로 1건 추가됩니다" "$SKETCH")
PREVIEW_B=$(grep -c "8일 범위로 1건 추가됩니다 (주말·공휴일 2일은 표시에서 제외)" "$SKETCH")
echo "14. N일 미리보기 a: $PREVIEW_A (≥1), b: $PREVIEW_B (≥1)"
test "$PREVIEW_A" -ge 1 && test "$PREVIEW_B" -ge 1

# 15. saving "저장 중..." 라벨
SAVING=$(grep -c "저장 중\.\.\." "$SKETCH")
echo "15. '저장 중...': $SAVING (≥1)"
test "$SAVING" -ge 1

# 16. 모바일 393 + 데스크톱 480 frame 둘 다 노출
MOBILE_W=$(grep -cE "393(px|[[:space:]]|;)" "$SKETCH")
DESKTOP_W=$(grep -cE "(width:[[:space:]]*480|480px)" "$SKETCH")
echo "16. 모바일 393: $MOBILE_W (≥1), 데스크톱 480: $DESKTOP_W (≥1)"
test "$MOBILE_W" -ge 1 && test "$DESKTOP_W" -ge 1

echo ""
echo "16/16 verify gate PASS"
    </automated>
  </verify>
  <done>
- sketch-wave-4.html 파일이 존재한다
- 16개 verify gate 모두 PASS
- 7 state frame (빈폼 / inspect-sub 소화기 / task / elev-sub / fire-sub / N일 미리보기 2 sub-frame / saving) 모두 다크 모드로 노출
- frame #1 + frame #6a 는 라이트 mirror 도 함께 노출
- SCHED_CATEGORIES 5 hex / INSP_CATEGORIES 19 / ELEV_SUBCATS 3 / FIRE_SUBCATS 4 라벨 verbatim
- 노안 격상 (9·10·11px → 12+, 헤더 15 → 18, CTA 14 → 16) 적용
- W1+W2+W3 LOCKED 결정 mirror (event #94a3b8, 카테고리 hex set, 이모지 0, "오늘" 칩 0)
- inspection-modal-chrome-rules.md §1~§6 mirror (overlay rgba(0,0,0,0.55) z-300 + sheet bg-surface-raised + radius-lg 16 desktop / 20 0 0 0 mobile)
- 푸터 OQ #1~#3 + LOCKED 사전 결정 노출
- SchedulePage.tsx 파일 0건 수정 (`git diff --stat -- cha-bio-safety/src/pages/SchedulePage.tsx` 가 비어있어야 함)
  </done>
</task>

</tasks>

<verification>

작업 완료 후 다음 모두 PASS 확인:

1. **파일 존재 + 라인 수**: `wc -l cha-bio-safety/docs/redesign-context/13-schedule/sketch-wave-4.html` ≥ 600
2. **16-grep verify gate**: 위 `<verify>` 의 16개 grep 모두 PASS
3. **SchedulePage.tsx 0건 수정**: `git status -- cha-bio-safety/src/pages/SchedulePage.tsx` 빈 결과
4. **시각 검수**: 사용자에게 결과 보고 후 시안 컨펌 대기

</verification>

<success_criteria>

- [ ] `cha-bio-safety/docs/redesign-context/13-schedule/sketch-wave-4.html` 존재 (≥600 라인)
- [ ] 16개 verify gate 모두 PASS
- [ ] AddModal 5 cat variant 모두 시연 (inspect / task / event / elevator / fire)
- [ ] 7 state frame 모두 노출 + frame #1 + #6a 라이트 mirror
- [ ] INSP_CATEGORIES 19 / ELEV_SUBCATS 3 / FIRE_SUBCATS 4 라벨 verbatim
- [ ] tokens.css + typography.css verbatim 임베드
- [ ] 이모지 0, 9·10·11px 0, status- prefix 0
- [ ] W1+W2+W3 LOCKED 결정 mirror
- [ ] SchedulePage.tsx 0건 수정 (시안 only — 비즈 로직 변경 0)
- [ ] 푸터 OQ #1~#3 + LOCKED 사전 결정 시연

</success_criteria>

<output>
After completion, create `.planning/quick/260519-kq7-redesign-13-schedule-sketch-wave-4-addmo/260519-kq7-SUMMARY.md`
</output>
