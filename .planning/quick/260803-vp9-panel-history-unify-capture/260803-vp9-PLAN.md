---
phase: quick-260803-vp9
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cha-bio-safety/src/utils/panelEvents.ts
  - cha-bio-safety/src/components/PanelEventRow.tsx
  - cha-bio-safety/src/components/PanelEventDetailModal.tsx
  - cha-bio-safety/src/pages/FireAlarmHistoryPage.tsx
  - cha-bio-safety/src/pages/InspectionPage.tsx
autonomous: true
requirements: [VP9-1, VP9-2, VP9-3]
must_haves:
  truths:
    - "줌 뷰어(모바일·데스크톱)에 라이브/경보시점 토글이 없고 라이브만 표시된다 (VP9-1)"
    - "확정 기록과 연결된 자동 경보가 이력에서 단일 행으로 병합된다 (VP9-2)"
    - "병합된 기록 행이 경보 시점 캡처(snapshotKey)를 갖는다 (VP9-2)"
    - "자동감지/수동 출처 칩이 행에서 제거된다 (VP9-2)"
    - "이벤트 행 탭 → 세부 열람 모달이 3곳 공통으로 열린다 (VP9-3)"
    - "세부 모달: 확정 기록은 구분·발생일시·장소·원인·조치·캡처, 미확정 경보는 종류·시각·해제사유·캡처 (VP9-3)"
  artifacts:
    - path: "cha-bio-safety/src/utils/panelEvents.ts"
      provides: "PanelEventItem 확장 + 사건단위 dedup mergePanelEvents"
      contains: "draftRecordId"
    - path: "cha-bio-safety/src/components/PanelEventDetailModal.tsx"
      provides: "3곳 공용 세부 열람 모달 (신규)"
      min_lines: 40
    - path: "cha-bio-safety/src/components/PanelEventRow.tsx"
      provides: "출처 칩 제거 + 탭 가능 행"
  key_links:
    - from: "cha-bio-safety/src/components/PanelEventRow.tsx"
      to: "PanelEventDetailModal"
      via: "onSelect prop → host selected state"
      pattern: "onSelect"
    - from: "cha-bio-safety/src/utils/panelEvents.ts"
      to: "panel_alarms ↔ fire_alarm_records join"
      via: "alarm.draftRecordId === record.id"
      pattern: "draftRecordId"
    - from: "cha-bio-safety/src/components/PanelEventDetailModal.tsx"
      to: "/api/public/panel/{snapshotKey}.jpg"
      via: "캡처 img (공개 라우트)"
      pattern: "public/panel"
---

<objective>
수신반 이벤트 이력을 사건 단위로 통합하고 캡처 열람을 붙인다. 260803-tan 직후 사용자 피드백 3건을 반영:

1. 줌 뷰어의 라이브↔경보시점 토글 제거 → 라이브 전용 복귀 (VP9-1)
2. 확정 기록과 연결된 자동 경보 이벤트를 이력에서 중복 제거(사건 단위) + 출처 칩 제거 (VP9-2)
3. 이벤트 행 탭 → 세부 열람 모달(3곳 공통, 캡처 이미지 포함) (VP9-3)

Purpose: 경보 1건이 이력에 두 줄(자동 이벤트 + 확정 기록)로 뜨는 중복을 없애고, 캡처는 라이브 자리가 아니라 기록·이력 열람에 붙여 원래 취지대로 되돌린다.
Output: panelEvents.ts(병합 dedup) + PanelEventRow.tsx(칩 제거·탭) + PanelEventDetailModal.tsx(신규 공용 모달) + FireAlarmHistoryPage.tsx(모달 배선·출처필터 제거) + InspectionPage.tsx(줌 토글 제거·최근카드 모달 배선).

배경 확인 완료(서버 무변경):
- functions/_lib/alarm.ts `mapAlarm` 이 이미 `snapshotKey`·`snapshotUrl`·`draftRecordId`·`clearedReason`·`detectedAt` 노출.
- functions/api/alarm/events.ts 가 `mapAlarm` 을 그대로 사용 → events API 에 draftRecordId·snapshotKey 이미 포함. **events/mapAlarm 서버 변경 불필요.**
- functions/api/fire-alarm/index.ts PUT(초안 확정)이 `created_by = staffId` 로 갱신(line 109) → 확정 기록은 `created_by !== 'panel-agent'` 라서 `normalizeManual` 을 통과해 수동 리스트에 들어오고, 동시에 draftRecordId 로 연결된 자동 경보도 auto 리스트에 남아 **현재 2행 중복**이 뜬다. 이것이 없앨 대상.
- 미확정 초안(created_by='panel-agent')은 `normalizeManual` 이 계속 필터 → 자동 경보 행만 잔존(요구 ② 단서 유지).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@CLAUDE.md

# 병합·행·이력 핵심 (수정 대상)
@cha-bio-safety/src/utils/panelEvents.ts
@cha-bio-safety/src/components/PanelEventRow.tsx
@cha-bio-safety/src/pages/FireAlarmHistoryPage.tsx

# 서버 계약 확인용 (변경 금지 — 이미 필드 노출)
@cha-bio-safety/functions/_lib/alarm.ts
@cha-bio-safety/functions/api/fire-alarm/index.ts

# InspectionPage 는 619KB 대용량 — 아래 라인 범위만 편집. 전체 재읽기 금지.
# 모바일 FireAlarmModal 줌토글 상태: 4473 / 4483 / 4487-4488
# 모바일 줌 오버레이 토글: 4780 / 4794-4801 / 4811-4813
# 데스크톱 pane 줌토글 상태: 5225 / 5235 / 5239-5240 / 5264
# 데스크톱 줌 오버레이 토글: 5941 / 5954-5961 / 5969-5971
# 모바일 최근 이벤트 카드: 4642-4657 (PanelEventRow map)
# 데스크톱 in-pane 이력: 5527-5541 (FireAlarmHistoryView thumb)
</context>

<tasks>

<task type="auto">
  <name>Task 1: panelEvents.ts — PanelEventItem 확장 + 사건단위 dedup 병합</name>
  <files>cha-bio-safety/src/utils/panelEvents.ts</files>
  <action>
`PanelEventItem` 인터페이스에 세부 모달·dedup 용 필드를 추가한다(기존 필드 유지): `snapshotKey: string | null`, `detectedAt: string | null`(경보 감지 시각), `action: string | null`(수동 기록 조치), `clearedReason: string | null`(자동 해제 사유), `recordType: 'fire' | 'non_fire' | null`(확정 기록의 화재보/비화재보 구분), `status: string | null`(자동 경보 상태), `draftRecordId: string | null`(dedup 조인 키).

`normalizeAuto` 매핑에 신규 필드 반영: `snapshotKey: a.snapshotKey ?? null`, `detectedAt: a.detectedAt ?? null`, `action: null`, `clearedReason: a.clearedReason ?? null`, `recordType: null`, `status: a.status ?? null`, `draftRecordId: a.draftRecordId ?? null`. 기존 `snapshotUrl: a.snapshotUrl ?? null` 유지.

`normalizeManual` 매핑에 신규 필드 반영: `snapshotKey: null`, `detectedAt: null`, `action: r.action ?? null`, `clearedReason: null`, `recordType: r.type === 'non_fire' ? 'non_fire' : 'fire'`, `status: null`, `draftRecordId: null`. **`created_by !== 'panel-agent'` 필터·`kind: 'fire'` 고정은 그대로 유지**(미확정 초안 제외 규칙 보존).

`mergePanelEvents(auto, manual)` 를 사건 단위 dedup 으로 재작성:
- `const autoItems = normalizeAuto(auto)`, `const manualItems = normalizeManual(manual)`.
- `const manualIds = new Set(manualItems.map(m => m.id))` — 현재 병합 대상 수동 기록 id 집합(이미 panel-agent 제외됨).
- 확정 기록에 연결된 자동 경보를 recordId 로 인덱싱: `manualIds.has(a.draftRecordId)` 인 auto 만 `Map<recordId, autoItem>` 에 담는다.
- 수동 행에 캡처 부착: 각 manualItem 에 대해 연결 auto 가 있으면 `{ ...m, snapshotKey: linked.snapshotKey, snapshotUrl: linked.snapshotUrl, detectedAt: linked.detectedAt }` 로 교체(없으면 원본).
- 연결된 auto 행 숨김: `autoItems.filter(a => !(a.draftRecordId && manualIds.has(a.draftRecordId)))`.
- `[...shownAuto, ...mergedManual].sort((a, b) => b.time.localeCompare(a.time))` 반환.

한쪽 창에만 있는 상대(연도/시간창 불일치)는 매칭 실패로 그대로 표시 — 정합 유지. 다중 auto 가 한 record 를 가리키면 전부 숨기고 record 는 마지막 linked 캡처를 받음(실사용 1:1). `useRecentPanelEvents` 는 mergePanelEvents 호출 그대로라 dedup 자동 적용 — 수정 불필요(단, 반환 필터 `e.time >= cutoff` 유지 확인).
  </action>
  <verify>
    <automated>cd cha-bio-safety && npx tsc --noEmit</automated>
  </verify>
  <done>PanelEventItem 에 7개 신규 필드 존재, mergePanelEvents 가 draftRecordId===record.id 로 auto 숨김·manual 에 캡처 부착, tsc 통과. `grep -c draftRecordId cha-bio-safety/src/utils/panelEvents.ts` ≥ 3.</done>
</task>

<task type="auto">
  <name>Task 2: PanelEventRow 출처칩 제거·탭 + 공용 PanelEventDetailModal 신규 + 3곳 배선</name>
  <files>cha-bio-safety/src/components/PanelEventRow.tsx, cha-bio-safety/src/components/PanelEventDetailModal.tsx, cha-bio-safety/src/pages/FireAlarmHistoryPage.tsx, cha-bio-safety/src/pages/InspectionPage.tsx</files>
  <action>
**(a) PanelEventRow.tsx** — 출처 칩 제거 + 탭 가능화:
- 좌측 세로 스택(lc)에서 `{item.source === 'auto' ? 자동감지 : 수동}` 칩 블록 전체 삭제. 종류칩(badge)만 남긴다.
- 썸네일 placeholder 폴백 텍스트 `{item.source === 'manual' ? '수기' : '미연결'}` → 출처 구분 제거 취지에 맞춰 중립 `'캡처 없음'` 로 교체(snapshotUrl 있으면 이미지, 없으면 이 문구).
- `onSelect?: (item: PanelEventItem) => void` prop 추가. 최상위 래퍼 `<div>` 를 클릭 가능하게: `onClick={() => onSelect?.(item)}`, `role="button"`, `className` 에 `cursor-pointer` 추가(onSelect 있을 때). 기존 토큰·레이아웃·padding 유지, 신규 디자인 언어 금지.

**(b) src/components/PanelEventDetailModal.tsx (신규)** — 3곳 공용 세부 열람 모달. 기존 FireAlarmModal/PanelEventRow 토큰만 사용, 신규 디자인 언어 금지:
- `export function PanelEventDetailModal({ item, onClose }: { item: PanelEventItem; onClose: () => void })`.
- 오버레이: 화면 중앙 카드(기존 모달 chrome — `bg-surface-raised border border-border-default rounded-md`, 딤 배경 `bg-black/50`, X 닫기 버튼 lucide `X`). 헤더에 구분 칩:
  - 확정 기록(`item.source === 'manual'`): recordType 로 `화재보`(text-danger bg-danger-bg) / `비화재보`(중립 text-text-secondary bg-surface-sunken — negative 룰: 비위험은 알람색 금지).
  - 미확정 경보(`item.source === 'auto'`): `KIND_BADGE[item.kind]`(화재/설비/고장) 칩 재사용.
- 본문 필드(라벨-값 행, text-caption 라벨 + text-body 값):
  - 확정 기록: 발생일시(item.time) · 발생장소(item.location ?? '수신반 확인 필요') · 원인(item.cause) · 조치(item.action). 값 없으면 '-'.
  - 미확정 경보: 종류(KIND_BADGE 라벨) · 시각(item.time) · 해제 사유(item.clearedReason ?? '-').
- 경보 시점 캡처: `item.snapshotKey` 있을 때만 섹션 노출. 라벨 '경보 시점 캡처'. `<img src={`/api/public/panel/${item.snapshotKey}.jpg`}>` (공개 라우트, 인증 불필요 — snapshotUrl 과 동일). 이미지 탭 → 전체화면 확대 오버레이(로컬 state `zoom` boolean): 기존 줌 오버레이 스타일 재사용 `bg-[#05070a]` 풀스크린 + `object-contain` + X 닫기. pinch 훅은 불필요(단순 확대 오버레이). LivePanelImage 는 InspectionPage 지역 컴포넌트라 import 하지 말 것 — plain `<img>` 사용.
- import: `KIND_BADGE, type PanelEventItem` from '../utils/panelEvents', `X` from 'lucide-react'.

**(c) FireAlarmHistoryPage.tsx** — 모달 배선 + 출처 필터 제거(요구 ② '출처 구분 필요 없다' 직접 귀결):
- `FireAlarmHistoryView` 안에 `const [sel, setSel] = useState<PanelEventItem | null>(null)` 추가, 각 `<PanelEventRow ... onSelect={setSel} />`, 리스트 하단에 `{sel && <PanelEventDetailModal item={sel} onClose={() => setSel(null)} />}` 렌더.
- **출처 Segment 필터 제거**: `SrcFilter` 타입·`srcF` state·`.filter(e => srcF === 'all' || e.source === srcF)`·출처 Segment UI(라벨 '출처' 행) 삭제. 월 스테퍼는 그 행에서 유지(레이아웃 재배치 최소화 — 출처 Segment 자리를 비우고 스테퍼 유지). **종류(KindFilter) 필터는 유지.**
- import 에 `PanelEventDetailModal`, `type PanelEventItem` 추가.

**(d) InspectionPage.tsx (모바일 최근 이벤트 카드만, ~4642-4657)** — 세부 모달 배선:
- FireAlarmModal 스코프에 `const [evtSel, setEvtSel] = useState<PanelEventItem | null>(null)` 추가.
- `mergedEvents.map(ev => <PanelEventRow key={ev.id} item={ev} onSelect={setEvtSel} />)`.
- FireAlarmModal 반환부(오버레이 계열과 같은 레벨)에 `{evtSel && <PanelEventDetailModal item={evtSel} onClose={() => setEvtSel(null)} />}` 추가.
- import 에 `PanelEventDetailModal`, `PanelEventItem` 타입 추가. **데스크톱 in-pane 이력(5541)은 FireAlarmHistoryView 를 쓰므로 (c)로 자동 커버 — 별도 배선 불필요.**

3곳(모바일 최근카드 / 전체 이력 페이지 / 데스크톱 in-pane)이 단일 `PanelEventDetailModal` 를 공유 — 모달 중복 구현 금지 준수. Task 3 과 같은 파일(InspectionPage)을 편집하므로 순차 실행.
  </action>
  <verify>
    <automated>cd cha-bio-safety && npx tsc --noEmit && grep -c "자동감지" src/components/PanelEventRow.tsx</automated>
  </verify>
  <done>PanelEventRow 에 자동감지/수동 칩 없음(grep 결과 0), onSelect 로 탭 가능. PanelEventDetailModal.tsx 존재(확정=구분·일시·장소·원인·조치·캡처 / 미확정=종류·시각·해제사유·캡처, 캡처 탭 확대). FireAlarmHistoryView·FireAlarmModal 최근카드 모두 모달 배선, 출처 Segment 필터 제거·종류 필터 유지. tsc 통과.</done>
</task>

<task type="auto">
  <name>Task 3: InspectionPage 줌 뷰어 라이브↔경보시점 토글 제거 (라이브 전용 복귀)</name>
  <files>cha-bio-safety/src/pages/InspectionPage.tsx</files>
  <action>
260803-tan(b2821a67/035a8d26)이 줌 뷰어에 넣은 라이브/경보시점 토글을 걷어내고 라이브 전용으로 되돌린다. 관련 state 는 전부 줌 토글 전용임을 grep 으로 확인 완료 — 안전 제거.

**모바일 FireAlarmModal:**
- 상태 제거: line 4473 `const [draftSnapKey, setDraftSnapKey] = useState...`, line 4483 `setDraftSnapKey(draft.snapshotKey ?? null)`(loadDraft 내), line 4487 `const [zoomSnap, setZoomSnap] = useState(false)`, line 4488 `const zoomSnapKey = ...` — 전부 삭제.
- line 4780 닫기 핸들러 `onClick={() => { setZoomOpen(false); setZoomSnap(false) }}` → `onClick={() => setZoomOpen(false)}`.
- line 4794-4801 토글 블록 `{zoomSnapKey && ( <span>...라이브/경보 시점 버튼...</span> )}` 전체 삭제.
- line 4811-4813 프레임 렌더 `{zoomSnap && zoomSnapKey ? <LivePanelImage snapshotKey=.../> : <LivePanelImage frameUpdatedAt=.../>}` → `<LivePanelImage frameUpdatedAt={status?.frameUpdatedAt} objectClass="object-contain" />` 만 남김.

**데스크톱 pane:**
- 상태 제거: line 5225 `const [panelDraftSnapKey, setPanelDraftSnapKey] = useState...`, line 5235 `setPanelDraftSnapKey(draft.snapshotKey ?? null)`, line 5264 `setPanelDraftSnapKey(null)`(pane 이탈 리셋), line 5239 `const [panelZoomSnap, setPanelZoomSnap] = useState(false)`, line 5240 `const panelZoomSnapKey = ...` — 전부 삭제.
- line 5941 닫기 핸들러 `... setPanelZoomSnap(false); panelZoom.reset() ...` → `setPanelZoomSnap(false)` 만 제거(`setPanelZoomOpen(false); panelZoom.reset()` 유지).
- line 5954-5961 토글 블록 `{panelZoomSnapKey && ( ... )}` 전체 삭제.
- line 5969-5971 프레임 렌더 `{panelZoomSnap && panelZoomSnapKey ? <LivePanelImage snapshotKey=.../> : <LivePanelImage frameUpdatedAt=.../>}` → `<LivePanelImage frameUpdatedAt={panelStatus?.frameUpdatedAt} objectClass="object-contain" />` 만 남김.

줌 뱃지의 `mode === 'alarm' ? '화재' : 'LIVE'` 표기·blink 는 유지(라이브 상태 표기이지 캡처 토글 아님). 다른 tan 산출물(자동 ack·초안 카드·OCR prefill·푸시 문구)은 무변경.
  </action>
  <verify>
    <automated>cd cha-bio-safety && grep -c "zoomSnap\|panelZoomSnap\|draftSnapKey\|panelDraftSnapKey" src/pages/InspectionPage.tsx; npx tsc --noEmit && npm run build</automated>
  </verify>
  <done>grep 결과 zoomSnap/panelZoomSnap/draftSnapKey/panelDraftSnapKey 심볼 0. 줌 뷰어(모바일·데스크톱)에 라이브/경보시점 버튼 없음·라이브만 렌더. tsc + build 통과.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→R2 공개 캡처 | `/api/public/panel/{key}.jpg` 는 기존 공개 라우트(무인증). 이번 변경은 새 열람 지점을 추가할 뿐 신규 경계 없음 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-vp9-01 | Information Disclosure | 세부 모달 캡처 img → /api/public/panel/{key}.jpg | accept | 기존 라이브 뷰어·썸네일이 이미 동일 공개 라우트로 캡처 노출 중. 4인 내부망 PWA·수신반 화면(개인정보 없음). 신규 노출면 아님 |
| T-vp9-02 | Tampering | npm 신규 패키지 | accept | 신규 의존성 0(lucide-react·react-query 기존). 설치 태스크 없음 |
| T-vp9-03 | Tampering | dedup 병합 오조인 | mitigate | 조인 키는 서버가 채운 draftRecordId(불변 id)와 record.id 정확 일치만. 부분/추정 매칭 없음. 한쪽 창 부재 시 그대로 표시(데이터 손실 없음) |
</threat_model>

<verification>
빌드 게이트(각 태스크 <verify>):
- `cd cha-bio-safety && npx tsc --noEmit` — 타입 통과
- `npm run build` — 프로덕션 번들 성공(Task 3)
- grep 게이트: PanelEventRow 출처칩 0 / InspectionPage 줌토글 심볼 0

실데이터 육안 검증(배포 후, 사용자):
- 확정 기록 FA-zZOVmVA2wi ↔ 경보 PA-Zz0QWdxnus 가 이력에 **1행만** 표시(중복 제거).
- 그 1행 탭 → 세부 모달에 구분·발생일시·장소·원인·조치 + 경보 시점 캡처 이미지 노출, 캡처 탭 시 확대.
- 줌 뷰어(모바일 FireAlarmModal + 데스크톱 pane) 열어 라이브/경보시점 토글 버튼이 **없음** 확인.
- 미확정 경보 행(기록 없는 건) 탭 → 종류·시각·해제사유 + 캡처(있으면)만 표시.
</verification>

<success_criteria>
- [ ] VP9-1: 모바일·데스크톱 줌 뷰어에서 라이브↔경보시점 토글 제거, 라이브만 표시 (grep 심볼 0 + 육안)
- [ ] VP9-2: 확정 기록과 연결 자동 경보가 단일 행으로 병합(dedup), 병합 행이 캡처 보유, 출처 칩 제거, 출처 필터 제거·종류 필터 유지
- [ ] VP9-3: 3곳(모바일 최근카드/전체 이력/데스크톱 in-pane) 공통 단일 세부 모달 — 확정=구분·일시·장소·원인·조치·캡처, 미확정=종류·시각·해제사유·캡처, 캡처 탭 확대
- [ ] 서버(functions/) 무변경 — events/mapAlarm/fire-alarm 손대지 않음
- [ ] 260803-tan 다른 산출물(자동 ack·초안 카드·OCR prefill·푸시 문구) 무변경
- [ ] `npx tsc --noEmit` + `npm run build` 통과 (cha-bio-safety/)
- [ ] production 브랜치 직접 작업, worktree 미사용
</success_criteria>

<output>
Create `.planning/quick/260803-vp9-panel-history-unify-capture/260803-vp9-SUMMARY.md` when done
</output>
