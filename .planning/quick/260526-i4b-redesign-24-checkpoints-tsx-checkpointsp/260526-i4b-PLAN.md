---
phase: 260526-i4b
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cha-bio-safety/src/pages/CheckpointsPage.tsx
autonomous: true
requirements:
  - REDESIGN-24-W6-TSX-CONVERSION-CHECKPOINTS
must_haves:
  truths:
    - "CheckpointsPage.tsx 의 외곽 chrome 4 영역 (W2 frame-guard / W3 header-filters / W4 list-fab / W5 modal-form) 이 W6 §3 sketch class 매핑 verbatim 으로 변환되어 cbc7119-preview 에서 W2~W5 sketch 와 시각적으로 동일하다"
    - "비즈 anchor 25건 (CATEGORIES_FALLBACK 19종 / ZONE_LABEL / ZONE_FLOORS / FLOOR_ORDER 20건 / MARKER_TYPE_LABEL 6건 / FLOOR_CODE 8건 / admin 가드 useEffect+early return / queryKey 6 / useMutation 3 / catCheckPoints 자동 채우기 / isGuidelamp+guidelampAsCp+FPM- / isExtCategory+extinguisherApi+zoneMap / 'basement'='common' eq / 카피 8 / placeholder 9 / toast 7 / select option ≥10 / SKELETON_STYLE / FAB safe-area / ModalWrapper+categoryOptions / BottomSheet+DesktopModal 인라인) 이 1 byte 변경 0 으로 grep PASS 한다"
    - "OQ LOCKED 6건 적용 — #1 BottomSheet/DesktopModal 동 파일 인라인 보존 (Modal.tsx/Sheet.tsx 신규 생성 0) / #2 단색 var(--acl) 유지 (linear-gradient 0) / #3 zone toggle button row 유지 + fontSize 11→12 격상 / #4 외곽 hex 새 토큰 전체 치환 / #5 9·10·11 → 12 일률 격상 + leading-none / #6 Lucide Plus+ChevronDown 치환 + 기존 size 보존 (ChevronDown 14→16 §7.1 위반 수정)"
    - "Lucide 2종 import: import { Plus, ChevronDown } from 'lucide-react' 신규 + 인라인 SVG 함수 IconPlus / IconChevronDown 함수 전체 제거"
    - "fontSize 9·10·11 인라인 0 — W6 §7 폰트 매트릭스 따라 모두 text-caption (12px) + leading-none 격상 (line 279 zone btn / line 408 mobile cat 배지 / line 567+574 mobile filter / line 578 mobile count / line 632 desktop 테이블 cat 배지 / line 641 desktop status)"
    - "linear-gradient 0 / status- prefix 0 / w-8 h-8 0 / 이모지 0 (CheckpointsPage.tsx 본문)"
    - "Build PASS — cd cha-bio-safety && npm run build 성공 (tsc --noEmit 0 errors + vite build success + PWA injectManifest PASS). Build 실패 시 commit 보류"
    - "components.css 0 byte diff (이번 wave 신규 추가 0 — OQ #1 default + W6 §9 박제) + App.tsx 0 byte diff + src/** 변경 = CheckpointsPage.tsx 1 파일만"
  artifacts:
    - path: "cha-bio-safety/src/pages/CheckpointsPage.tsx"
      provides: "CheckpointsPage TSX 변환본 (외곽 chrome sketch class + 비즈 로직 0 byte + Lucide 치환 + OQ LOCKED 6)"
      min_lines: 600
      contains: "import { Plus, ChevronDown } from 'lucide-react'"
  key_links:
    - from: "cha-bio-safety/src/pages/CheckpointsPage.tsx (line 1~109)"
      to: "wave-6-tsx-conversion-checklist.md §1 imports + 상수 보존"
      via: "Lucide 추가 + IconPlus/IconChevronDown 제거 + 상수 19+5종 verbatim"
      pattern: "import \\{ Plus, ChevronDown \\} from 'lucide-react'"
    - from: "cha-bio-safety/src/pages/CheckpointsPage.tsx (line 111~398, 428~692)"
      to: "wave-6-tsx-conversion-checklist.md §2 메인 함수 + 모달"
      via: "hooks/state/handlers 1:1 verbatim — 비즈 로직 0 byte"
      pattern: "createMutation|updateMutation|deactivateMutation|catCheckPoints"
    - from: "CheckpointsPage.tsx JSX render 4 영역"
      to: "sketch-wave-2~5.html 4 파일 class fence (W6 §6 grep 결과 verbatim)"
      via: "sketch class 매핑 — page-wrapper / desktop-header / data-table / sheet-panel 등"
      pattern: "(page-wrapper|desktop-header|mobile-header|data-table|cp-card|sheet-panel|modal-panel)"
    - from: "비즈 anchor 25건"
      to: "wave-6-tsx-conversion-checklist.md §4 보존 박스 + wave-1-index §1.3"
      via: "1 byte 변경 금지 grep gate"
      pattern: "CATEGORIES_FALLBACK|ZONE_FLOORS|FLOOR_ORDER|MARKER_TYPE_LABEL|FLOOR_CODE"
    - from: "OQ LOCKED 6"
      to: "wave-6-tsx-conversion-checklist.md §5 + wave-1-index §7"
      via: "default (a) x6 — inline / 단색 / zone row / 토큰 / 12 격상 / Lucide"
      pattern: "OQ #[1-6]"
    - from: "verify gate 22 (Build PASS)"
      to: "wave-6-tsx-conversion-checklist.md §11 negative + §12 verify (TSX 전용 22 추출)"
      via: "bash gate 22건 + npm run build 성공 후에만 commit"
      pattern: "npm run build"
---

<objective>
redesign/24-checkpoints TSX 변환 wave — CheckpointsPage.tsx (693 lines, admin 전용 BottomSheet/DesktopModal) **단일 파일 atomic 1-commit** 변환.

W6 (`cha-bio-safety/docs/redesign-context/24-checkpoints/wave-6-tsx-conversion-checklist.md`, 560 lines) §1~§12 verbatim 적용 — 외곽 chrome 4 영역 (W2 frame-guard / W3 header-filters / W4 list-fab / W5 modal-form) 만 sketch class 로 변환, BottomSheet+DesktopModal+ModalWrapper+메인 hook/state/handler 본문 + 비즈 anchor 25건은 1 byte 변경 0.

`feedback_gsd_workflow_strict` — 다음 작업부터 GSD 워크플로 강제. 이 plan 은 minimal brief 단일 atomic 패턴 (uou 333 lines mirror).

Purpose: 24-checkpoints 종결 → 4차 모니터링 (19/20/21 legal) 진입 가능. 14-reports / 15-daily-report / 18-worklog / 23-education / 25-qr-print / 28-splash 의 단일 파일 atomic 패턴 자동 도달.
Output: CheckpointsPage.tsx 변환본 + Build PASS + atomic 1-commit (변환) + SUMMARY 1-commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@cha-bio-safety/docs/redesign-context/24-checkpoints/wave-6-tsx-conversion-checklist.md
@cha-bio-safety/docs/redesign-context/24-checkpoints/wave-1-index.md
@cha-bio-safety/docs/redesign-context/24-checkpoints/sketch-wave-2-frame-guard.html
@cha-bio-safety/docs/redesign-context/24-checkpoints/sketch-wave-3-header-filters.html
@cha-bio-safety/docs/redesign-context/24-checkpoints/sketch-wave-4-list-fab.html
@cha-bio-safety/docs/redesign-context/24-checkpoints/sketch-wave-5-modal-form.html
@cha-bio-safety/src/pages/CheckpointsPage.tsx
@cha-bio-safety/src/styles/tokens.css
@cha-bio-safety/src/styles/typography.css
@cha-bio-safety/src/styles/components.css

<interfaces>
CheckpointsPage.tsx 변환 영역 line range 3구역:
- §1 imports (line 1~109): hooks/router/react-query/toast/store/api/hook/type 8 import 보존 + Lucide 2종 추가 + 인라인 SVG 함수 2개 (IconPlus/IconChevronDown line 11~25) 전체 제거 + 상수 19+5 (CATEGORIES_FALLBACK / ZONE_LABEL / BottomSheet 함수 / DesktopModal 함수 / INPUT_STYLE / LABEL_STYLE / CpFormState / EMPTY_CP_FORM / ZONE_FLOORS / ExtState / EMPTY_EXT) verbatim 보존
- §2 메인 함수 + 모달 (line 111~398, 428~504): CheckPointModalContent hook/mutation 3종/handler / CheckpointsPage hook 4 / admin 가드 useEffect / useQuery 3 / guidelampAsCp 변환 / cpListRaw / cpList filter (eq 헬퍼) / availableFloors / early return / ModalWrapper / categoryOptions — **본문 0 byte**
- §3 JSX render (line 505~692): 외곽 wrapper + style 태그 (W2) / 데스크톱+모바일 헤더 (W3) / 콘텐츠 4상태 분기 (skeleton/empty/error/data) + 데스크톱 테이블 + 모바일 카드 (CheckPointCard) + FAB (W4) / 모달 ModalWrapper 호출 (W5) — sketch class verbatim 적용

비즈 anchor 25건 (W6 §4 verbatim, 1 byte 변경 0 강제):
1. CATEGORIES_FALLBACK 19종 (line 29~34)
2. ZONE_LABEL (line 35~37) — legacy 'common' 포함
3. ZONE_FLOORS (line 94~98) — 'common' 키 8/8/6층
4. FLOOR_ORDER 20건 (line 492)
5. MARKER_TYPE_LABEL 6건 (line 462~464)
6. FLOOR_CODE 8건 (line 465)
7. admin 가드 useEffect (line 438~440) + early return (line 501)
8. queryKey 6건 (['check-point-categories'] / ['check-points', selectedCategory] / ['check-points', form.category] / ['floorplan-markers-all', 'guidelamp'] + invalidate 5건)
9. catCheckPoints useQuery (line 124~130) — staleTime 30_000, enabled: mode==='add' && form.category!==''
10. 자동 채우기 useEffect (line 138~160) — filtered→lastNo→numMatch→nextNo / '{floor} {category} {N+1}번'
11. createMutation (line 165~200) — isExtCategory 분기 + zoneMap {research:'연',office:'사',common:'공'}
12. updateMutation (line 206~227) — isMarker 'FPM-' 분기
13. deactivateMutation (line 229~247) — isMarker FloorPlanMarker.delete vs isActive:0
14. canSave (line 249~252) — 3 조건
15. 'basement'='common' eq 헬퍼 (line 481~484, 494~497)
16. 유도등 분기: isGuidelamp (line 448) + guidelampAsCp (line 466~477) + isLoading/cpListRaw (line 478~479)
17. ModalWrapper = isDesktop ? DesktopModal : BottomSheet (line 503) + categoryOptions (line 504)
18. BottomSheet 함수 (line 40~57) — overlay rgba(0,0,0,0.6) / bg var(--bg2) / radius 16/16/0/0 / slideUp 0.28s / maxHeight 90vh / handle 32x4 / backdrop click close
19. DesktopModal 함수 (line 60~74) — overlay rgba(0,0,0,0.5) / radius 12 / width 440 / maxHeight 85vh / boxShadow
20. SKELETON_STYLE (line 422~425)
21. 모바일 FAB safe-area: paddingBottom: 'calc(16px + var(--sab))' (line 676)
22. 카피 verbatim 8건 (W6 §4 row 22)
23. placeholder verbatim 9건 (W6 §4 row 23)
24. toast 7건 (W6 §4 row 24)
25. select option verbatim ≥10건 (W6 §4 row 25)

Lucide 2종 import:
```typescript
import { Plus, ChevronDown } from 'lucide-react'
```
- Plus size={16} (line 548 데스크톱 '개소 추가' button)
- Plus size={18} (line 679 모바일 FAB — **18 보존**, OQ #6 default)
- ChevronDown size={16} (line 528 데스크톱 cat select — 14→**16 격상**, §7.1 위반 수정)
- ChevronDown size={16} (line 561 모바일 cat select — 16 보존)

OQ LOCKED 6건 적용 위치 (W6 §5 verbatim):
- OQ #1: BottomSheet (line 40~57) + DesktopModal (line 60~74) 동 파일 인라인 보존 — src/components/ui/Modal.tsx 또는 Sheet.tsx 신규 생성 0 byte (StaffManagePage 공통화는 별도 task)
- OQ #2: 저장 버튼 (line 371) / 데스크톱 '개소 추가' (line 547) / 모바일 FAB (line 678) 3건 모두 단색 var(--acl) / var(--accent-primary) — linear-gradient 신규 도입 0
- OQ #3: zone toggle button row (line 275~284) 유지 + line 279 fontSize 11 → text-caption font-bold (12 격상)
- OQ #4: 외곽 hex 새 토큰 전체 치환 (design-system §4.1 마이그레이션 표): var(--bg)→--surface-page / --bg2→--surface-raised / --bg3→--surface-sunken / --bg4→--surface-active / --bd→--border-default / --bd2→--border-strong / --t1→--text-primary / --t2→--text-secondary / --t3→--text-tertiary / --acl→--accent-primary / --safe→--status-safe-bar / --danger→--status-danger
- OQ #5: 9·10·11 → 12 일률 격상 + leading-none — line 279 (zone btn 11) / line 408 (모바일 cat 배지 9) / line 567+574 (모바일 filter 11) / line 578 (모바일 count 11) / line 632 (데스크톱 테이블 cat 배지 10) / line 641 (데스크톱 status 11)
- OQ #6: Lucide 치환 + size 보존 — Plus 16/18 + ChevronDown 14→16(격상)/16. IconPlus / IconChevronDown 함수 제거

components.css 0 byte (W6 §9 박제 — 신규 추가 0, OQ #1 default + 단일 파일 인라인 패턴):
- 재사용만: `.bg-surface-page` / `.bg-surface-raised` / `.text-text-primary` / `.text-status-safe-bar` / `.text-caption` / `.text-label` / `.rounded-md` 등 tokens.css + typography.css 기존 class
- 신규 추가 없음 — BottomSheet/DesktopModal/CheckPointCard/SKELETON 모두 동 파일 인라인 유지

카피 verbatim 8건 + placeholder 9건 + toast 7건 + select option ≥10건:
- 카피 8: '카테고리를 선택하면 개소 목록이 표시됩니다' / '데이터를 불러오지 못했습니다. 잠시 후 다시 시도해 주세요' / '해당 카테고리에 개소가 없습니다' / '개소 추가 버튼을 눌러 점검 개소를 등록하세요' / '개소 추가' / '개소 수정' / '저장' / '취소' / '비활성화' / '이 개소를 비활성화합니다. 기존 점검 기록은 보존됩니다.'
- placeholder 9: '예: 한울방재' / '예: 2024-04 (YYYY-MM)' / '예: 수소10-19-11' / '예: BEQV' / '예: 72605' / '예: 68605' / '1층 로비 소화기' / '001 (선택)' / '메모 (선택)'
- toast 7: '소화기 등록 완료 ({mgmtNo})' / '개소가 추가되었습니다' / '저장에 실패했습니다. 입력값을 확인해 주세요' / '개소 정보가 수정되었습니다' / '마커가 삭제되었습니다' / '개소가 비활성화되었습니다' / '비활성화에 실패했습니다'
- select option ≥10: '전체 (카테고리 선택)' / '전체 구역' / '전체 층' / '사무동' / '연구동' / '지하' / '카테고리 선택' / '종류 선택' / '층 선택' / '분말 20kg' / '분말 3.3kg' / '할로겐' / 'K급'
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>T1: CheckpointsPage.tsx 단일 atomic 변환 + Build PASS</name>
  <files>cha-bio-safety/src/pages/CheckpointsPage.tsx</files>
  <action>
W6 markdown (`cha-bio-safety/docs/redesign-context/24-checkpoints/wave-6-tsx-conversion-checklist.md`) §1~§12 verbatim 적용. 단일 파일 in-place 교체, 1 atomic commit.

**Step 1: §1 imports (line 1~109) 변환**
- line 1~8 기존 8 import 그대로 유지.
- line 9 (또는 새 라인): `import { Plus, ChevronDown } from 'lucide-react'` 신규 추가.
- line 11~25 IconPlus / IconChevronDown 인라인 SVG 함수 2개 **전체 제거** (Lucide 치환 후 미사용).
- line 27~34 CATEGORIES_FALLBACK 19종 — verbatim 보존.
- line 35~37 ZONE_LABEL — verbatim 보존 (legacy 'common' 키 포함).
- line 40~57 BottomSheet / line 60~74 DesktopModal — **인라인 보존 (OQ #1 default)**. src/components/ui/Modal.tsx 또는 Sheet.tsx 신규 생성 0.
- line 77~84 INPUT_STYLE / LABEL_STYLE — 보존 (Tailwind 치환 검토 가능, 단 폼 필드 1:1 매핑 안정성 위해 보존 우선).
- line 87~109 CpFormState / EMPTY_CP_FORM / ZONE_FLOORS / ExtState / EMPTY_EXT — verbatim 보존.

**Step 2: §2 메인 함수 + 모달 (line 111~398, 428~504) — 본문 0 byte**
- CheckPointModalContent (line 111~398): useState / isExtCategory / catCheckPoints useQuery / handleCategoryChange / 자동 채우기 useEffect / setField / createMutation / isMarker / updateMutation / deactivateMutation / canSave / isBusy / handleSave — **1 byte 변경 0**.
- CheckpointsPage 메인 (line 428~504): hook 4 / admin 가드 useEffect / useQuery 3 / MARKER_TYPE_LABEL / FLOOR_CODE / guidelampAsCp / isLoading / cpListRaw / cpList filter (eq 헬퍼) / FLOOR_ORDER / availableFloors / early return / ModalWrapper / categoryOptions — **1 byte 변경 0**.
- 단, INPUT_STYLE / LABEL_STYLE 함수 본문은 Tailwind 클래스로 치환하더라도 시그니처 보존. 안전 가드: 본문 변경 시 grep 카운트 동일성 검증 (queryKey 6 / useMutation 3 / isAdmin 가드 등).

**Step 3: §3 JSX render (line 505~692) — sketch class 4 영역 적용**

§3.1 W2 외곽 wrapper + style 태그 (line 505~513):
- `<div style={{ display:'flex', flexDirection:'column', height:'100%', overflow:'hidden', background:'var(--bg)' }}>` → `<div className="flex flex-col h-full overflow-hidden bg-surface-page">` (OQ #4 토큰 치환).
- `<style>` 태그 (line 508~513) — keyframes blink + slideUp + focus border-color 그대로 유지 (또는 tailwind.config.extend.keyframes 이관). BottomSheet slideUp 0.28s 의존, **0 byte 변경**.

§3.2 W3 헤더 (데스크톱+모바일 분기) (line 516~582):
- 데스크톱 헤더 (line 516~551): `hidden lg:flex items-center gap-3 px-6 py-3 border-b border-border-default`. 카테고리 select 220px (`hdr-select`), filterZone/filterFloor (`filter-select`), 카운트 (`count-label`), '개소 추가' button (`add-btn`).
- 카테고리 select ChevronDown (line 528): `<IconChevronDown size={14} ...>` → `<ChevronDown size={16} color="var(--text-secondary)" />` (§7.1 위반 14→**16 격상**).
- 데스크톱 '개소 추가' button (line 546~550): `<button className="flex items-center gap-2 px-3 py-2 rounded-lg bg-accent-primary text-white text-label font-bold">` + `<Plus size={16} color="#fff" />` + '개소 추가' 카피 verbatim. OQ #2 — linear-gradient 0, 단색.
- 모바일 헤더 (line 552~582): `flex flex-col lg:hidden px-4 py-3 gap-2`. 카테고리 select 풀폭 (`mob-cat-select`) + ChevronDown size={16} (line 561). 조건부 필터 row (`mob-filter-row`) — filterZone/filterFloor select. 카운트 (`mob-count`).
- 폰트 격상: line 567+574 (filterZone/filterFloor fontSize 11) → `text-caption`. line 578 (카운트 fontSize 11) → `text-caption leading-none`. (OQ #5)

§3.3 W4 콘텐츠 (목록+FAB) (line 585~683):
- 콘텐츠 wrapper (line 585): `<div className="flex-1 overflow-auto min-h-0">`.
- 4 상태 분기:
  - empty 카테고리 미선택 (line 586~590): `<div className="state-empty flex items-center justify-center h-full text-text-secondary text-body-sm">` + 카피 '카테고리를 선택하면 개소 목록이 표시됩니다' verbatim.
  - skeleton 3개 (line 591~597): SKELETON_STYLE (line 422~425) 인라인 보존 — bg var(--bg3)→bg-surface-sunken / radius 12 / height 64 / blink 2s — **시각 변경 0**.
  - error (line 598~602): `state-error` + 카피 '데이터를 불러오지 못했습니다. 잠시 후 다시 시도해 주세요' verbatim.
  - data: 데스크톱 테이블 / 모바일 카드 리스트.
- 데스크톱 테이블 (line 604~656): `data-table` / `desktop-table-wrap`. 7 컬럼: 개소명/카테고리/구역/층/위치번호/상태/액션.
  - 테이블 thead (line 610~615 fontSize 12 fw700): `text-caption font-bold`.
  - 카테고리 배지 (line 632 fontSize 10 fw700): `text-caption leading-none` (OQ #5, 10→12).
  - 위치번호 (line 638 fontSize 12 JetBrains Mono): `text-caption font-mono`.
  - status (line 641 fontSize 11 fw600): `text-caption leading-none` (OQ #5, 11→12).
  - 액션 '수정' (line 649 fontSize 12 fw700): `text-caption font-bold`.
  - 테이블 empty (line 621): `text-body-sm` + 카피 '해당 카테고리에 개소가 없습니다' verbatim.
- CheckPointCard (line 401~419 모바일 카드) — JSX 영역에서 호출 시 sketch class 적용:
  - 외곽 (line 401): `cp-card` + inactive 시 `cp-card inactive` (`opacity-[0.45]`).
  - 8x8 dot (line 404): `<span className="cp-dot w-[8px] h-[8px] rounded-full" style={{ background: isActive ? 'var(--status-safe-bar)' : 'var(--text-tertiary)' }} />` (**w-8 = 48px 함정 회피**, `feedback_tailwind_w8_h8_is_48px`).
  - 개소명 (line 407 fontSize 14 fw700): `text-body-sm font-bold`.
  - 카테고리 배지 (line 408 fontSize 9 fw700): `text-caption leading-none` (OQ #5, 9→12).
  - 메타 zone·floor (line 412 fontSize 12): `text-caption leading-none`.
  - '수정 ▸' (line 416 fontSize 12 fw700): `text-caption leading-none` (텍스트 보존, ChevronRight 교체 별도 OQ).
- 모바일 empty (line 660~665): `text-body font-bold` + 카피 '해당 카테고리에 개소가 없습니다' / '개소 추가 버튼을 눌러 점검 개소를 등록하세요' verbatim.
- 모바일 FAB (line 674~683):
  - 외곽 (line 675~676): `<div className="mobile-fab-wrap" style={{ position:'sticky', bottom:0, padding:'0 16px', paddingBottom:'calc(16px + var(--sab))', background:'var(--bg)' }}>` — **safe-area `calc(16px + var(--sab))` 1 byte 변경 0** (iOS PWA safe-area-inset-bottom, `feedback_viewport_units_per_platform`).
  - button (line 677~681): `<button className="mobile-fab-btn w-full h-[52px] bg-accent-primary text-white rounded-xl flex items-center justify-center gap-2 text-body-sm font-bold">` + `<Plus size={18} color="#fff" />` + '개소 추가' 카피.
  - OQ #2: linear-gradient 0 (단색 var(--acl)).
  - OQ #6: size={18} **보존** (18→20 격상은 별도 OQ).

§3.4 W5 모달 호출 (line 685~690):
- `{modal.open && <ModalWrapper onClose={...} title={modal.mode === 'add' ? '개소 추가' : '개소 수정'}><CheckPointModalContent mode={modal.mode} cp={modal.target} onClose={...} /></ModalWrapper>}` — **1 byte 변경 0**.
- BottomSheet / DesktopModal 인라인 함수 (line 40~74) 변경 0 — OQ #1.
- 폼 필드 (CheckPointModalContent line 264~395) — `form-body` / `form-body-desktop` / `form-label` / `form-input` / `required-star` / `zone-row` / `zone-btn active` / `zone-btn inactive` / `action-row` / `action-btns` / `btn-cancel` / `btn-save` / `btn-save disabled` / `btn-deactivate` / `btn-deactivate-confirm` / `deactivate-confirm-box`. INPUT_STYLE / LABEL_STYLE 의존 부분은 보존 또는 Tailwind 치환 (`text-body-sm` / `text-label font-bold`).
  - zone toggle button row (line 275~284): height 36 유지 + line 279 fontSize 11 → `text-caption font-bold` (OQ #3, OQ #5).
  - 소화기 추가 폼 (line 296~347): isExtCategory 시만 표시 — placeholder 6건 verbatim ('예: 한울방재' / '예: 2024-04 (YYYY-MM)' / '예: 수소10-19-11' / '예: BEQV' / '예: 72605' / '예: 68605').
  - 액션 row (line 366~395): 취소 (line 369 14fw700) / 저장 (line 371 14fw700, **bg var(--acl) 단색 — linear-gradient 0**) / 비활성화 (line 377 12) / 비활성화 안내 (line 384 12, 카피 '이 개소를 비활성화합니다. 기존 점검 기록은 보존됩니다.' verbatim) / confirmDeactivate (line 388/390).
  - 카피 verbatim: '저장' / '취소' / '비활성화'.
  - toast 7건 (line 193~226, 243~246): verbatim 보존 — '소화기 등록 완료 ({mgmtNo})' / '개소가 추가되었습니다' / '저장에 실패했습니다. 입력값을 확인해 주세요' / '개소 정보가 수정되었습니다' / '마커가 삭제되었습니다' / '개소가 비활성화되었습니다' / '비활성화에 실패했습니다'.

**Step 4: 검증 — npm run build (Build 실패 시 commit 보류)**
- `cd cha-bio-safety && npx tsc --noEmit` — 0 errors.
- `cd cha-bio-safety && npm run build` — PASS (vite build success + PWA injectManifest PASS). CheckpointsPage chunk size 보고.
- Build 실패 시: 오류 수정 후 최대 2회 재시도. 2회 실패 시 deviation 보고 + commit 보류 (`feedback_check_branch_before_edit`).

**Step 5: atomic commit (1 commit, 1 파일)**
```bash
git add cha-bio-safety/src/pages/CheckpointsPage.tsx
git commit -m "feat(quick-260526-i4b): redesign/24-checkpoints TSX 변환 (CheckpointsPage 693→XXX + v0.1.1 토큰 className + 비즈 anchor 25 보존 + Lucide Plus+ChevronDown + OQ LOCKED 6 + W6 §1~§12 verbatim)"
```

메모리 룰 inline 13 slug (W6 §메모리 박제, plan 본문 echo 금지):
feedback_planner_prompt_sketch_verbatim / feedback_redesign_sketch_rule_enforcement / feedback_sketch_realistic_data / feedback_tsx_wave_emoji_dot_gap / feedback_tsx_wave_stat_card_drift / feedback_text_caption_leading_none / feedback_tailwind_token_class_pattern / feedback_tailwind_w8_h8_is_48px / feedback_cbc7119_design_never_wrangler / feedback_design_changes_ask_first / feedback_design_sketch_first / feedback_avoid_premature_confirmation / project_redesign_15_daily_report_status.
  </action>
  <verify>
    <automated>
# ── TSX 전용 22 gate (W6 §11 negative + §12 verify 추출) ──

# (1) CheckpointsPage.tsx 존재 + min_lines 600
test -f cha-bio-safety/src/pages/CheckpointsPage.tsx
test "$(wc -l < cha-bio-safety/src/pages/CheckpointsPage.tsx)" -ge "600"

# (2) cd cha-bio-safety && npm run build PASS (Build 가 없으면 commit 금지)
(cd cha-bio-safety && npm run build)

# (3) tsc --noEmit 0 errors (별도 안전 확인)
(cd cha-bio-safety && npx tsc --noEmit)

# (4) Lucide import grep PASS
grep -E "import \{[^}]*Plus[^}]*ChevronDown[^}]*\} from 'lucide-react'|import \{[^}]*ChevronDown[^}]*Plus[^}]*\} from 'lucide-react'" cha-bio-safety/src/pages/CheckpointsPage.tsx

# (5) linear-gradient 0 (OQ #2)
test "$(grep -c "linear-gradient" cha-bio-safety/src/pages/CheckpointsPage.tsx)" = "0"

# (6) fontSize 9/10/11 인라인 0 — JSX render 영역 (line 505~692)
grep -nE "fontSize:\s*(9|10|11)(?![0-9])" cha-bio-safety/src/pages/CheckpointsPage.tsx | awk -F: '{ if ($1 >= 505 && $1 <= 692) { print "VIOLATION at line " $1; exit 1 } } END { print "OK — JSX render 영역 fontSize 9/10/11 인라인 0" }'

# (7) font-size 9/10/11px (CSS-in-JS) 0 — JSX render 영역
grep -nE "font-size:\s*(9|10|11)px" cha-bio-safety/src/pages/CheckpointsPage.tsx | awk -F: '{ if ($1 >= 505 && $1 <= 692) { print "VIOLATION at line " $1; exit 1 } } END { print "OK" }'

# (8) status- prefix 0 (className Tailwind utility — feedback_tailwind_token_class_pattern)
test "$(grep -cE 'className=[^>]*"[^"]*(bg-status-|text-status-|border-status-)' cha-bio-safety/src/pages/CheckpointsPage.tsx)" = "0"

# (9) w-8 / h-8 (48px 사고 방지) 0 — Tailwind config spacing override
test "$(grep -cE 'className=[^>]*"[^"]*\b(w-8|h-8)\b' cha-bio-safety/src/pages/CheckpointsPage.tsx)" = "0"

# (10) 이모지 0
test "$(LC_ALL=C grep -cP '[\x{1F300}-\x{1FAFF}]' cha-bio-safety/src/pages/CheckpointsPage.tsx)" = "0"

# (11) CATEGORIES_FALLBACK 19종 verbatim grep PASS — 4 라벨 sample
grep -q "CATEGORIES_FALLBACK" cha-bio-safety/src/pages/CheckpointsPage.tsx
grep -q "소화기.*소화전.*스프링클러" cha-bio-safety/src/pages/CheckpointsPage.tsx
grep -q "자동화재탐지설비.*유도등.*방화셔터" cha-bio-safety/src/pages/CheckpointsPage.tsx
grep -q "DIV.*CCTV.*주차장비.*회전문" cha-bio-safety/src/pages/CheckpointsPage.tsx

# (12) ZONE_LABEL + ZONE_FLOORS + FLOOR_ORDER + MARKER_TYPE_LABEL + FLOOR_CODE 5종 grep PASS
grep -q "ZONE_LABEL" cha-bio-safety/src/pages/CheckpointsPage.tsx
grep -q "ZONE_FLOORS" cha-bio-safety/src/pages/CheckpointsPage.tsx
grep -q "FLOOR_ORDER" cha-bio-safety/src/pages/CheckpointsPage.tsx
grep -q "MARKER_TYPE_LABEL" cha-bio-safety/src/pages/CheckpointsPage.tsx
grep -q "FLOOR_CODE" cha-bio-safety/src/pages/CheckpointsPage.tsx

# (13) admin 가드 useEffect + early return grep PASS
test "$(grep -c "me?.role !== 'admin'" cha-bio-safety/src/pages/CheckpointsPage.tsx)" -ge "2"

# (14) queryKey 6건 PASS — 4 key sample (check-point-categories / check-points / floorplan-markers-all 등)
test "$(grep -cE "queryKey:|invalidateQueries" cha-bio-safety/src/pages/CheckpointsPage.tsx)" -ge "6"
grep -q "'check-point-categories'" cha-bio-safety/src/pages/CheckpointsPage.tsx
grep -q "'check-points'" cha-bio-safety/src/pages/CheckpointsPage.tsx
grep -q "'floorplan-markers-all'.*'guidelamp'" cha-bio-safety/src/pages/CheckpointsPage.tsx

# (15) useMutation 3건 PASS (createMutation + updateMutation + deactivateMutation)
test "$(grep -c "useMutation" cha-bio-safety/src/pages/CheckpointsPage.tsx)" -ge "3"
grep -q "createMutation" cha-bio-safety/src/pages/CheckpointsPage.tsx
grep -q "updateMutation" cha-bio-safety/src/pages/CheckpointsPage.tsx
grep -q "deactivateMutation" cha-bio-safety/src/pages/CheckpointsPage.tsx

# (16) 유도등 분기 PASS — isGuidelamp + guidelampAsCp + FPM-
grep -q "isGuidelamp" cha-bio-safety/src/pages/CheckpointsPage.tsx
grep -q "guidelampAsCp" cha-bio-safety/src/pages/CheckpointsPage.tsx
grep -q "FPM-" cha-bio-safety/src/pages/CheckpointsPage.tsx

# (17) 소화기 분기 PASS — isExtCategory + extinguisherApi + zoneMap
grep -q "isExtCategory" cha-bio-safety/src/pages/CheckpointsPage.tsx
grep -q "extinguisherApi" cha-bio-safety/src/pages/CheckpointsPage.tsx
grep -qE "zoneMap|research.*'연'|office.*'사'" cha-bio-safety/src/pages/CheckpointsPage.tsx

# (18) 'basement'='common' eq 헬퍼 grep PASS
grep -qE "'basement'.*'common'|'common'.*'basement'" cha-bio-safety/src/pages/CheckpointsPage.tsx

# (19) 카피 verbatim 8건 grep PASS
test "$(grep -cE "카테고리를 선택하면|데이터를 불러오지 못했습니다|해당 카테고리에 개소가 없습니다|개소 추가 버튼을 눌러|개소 추가|개소 수정|이 개소를 비활성화합니다|기존 점검 기록은 보존됩니다" cha-bio-safety/src/pages/CheckpointsPage.tsx)" -ge "8"

# (20) placeholder verbatim 9건 grep PASS
test "$(grep -cE "예: 한울방재|예: 2024-04|예: 수소10-19-11|예: BEQV|예: 72605|예: 68605|1층 로비 소화기|001 \(선택\)|메모 \(선택\)" cha-bio-safety/src/pages/CheckpointsPage.tsx)" -ge "9"

# (21) components.css 0 byte diff (이 wave 신규 추가 0 — OQ #1 default + W6 §9)
test "$(git diff origin/main..HEAD -- cha-bio-safety/src/styles/components.css | wc -l)" = "0"

# (22) App.tsx 0 byte diff + src/** 변경 = CheckpointsPage.tsx 1 파일만
test "$(git diff origin/main..HEAD --name-only -- cha-bio-safety/src/App.tsx | wc -l)" = "0"
test "$(git diff origin/main..HEAD --name-only -- cha-bio-safety/src/ | wc -l)" = "1"
git diff origin/main..HEAD --name-only -- cha-bio-safety/src/ | grep -q "^cha-bio-safety/src/pages/CheckpointsPage.tsx$"

# ── 추가 positive gate (워크트리 룰) ──
# wrangler 0 / npm run deploy 0
test "$(grep -cE "wrangler|npm run deploy" cha-bio-safety/src/pages/CheckpointsPage.tsx)" = "0"
# IconPlus / IconChevronDown 인라인 SVG 함수 제거 확인
test "$(grep -cE "^function IconPlus|^function IconChevronDown" cha-bio-safety/src/pages/CheckpointsPage.tsx)" = "0"
# Lucide 컴포넌트 사용 ≥4 (Plus 2 + ChevronDown 2)
test "$(grep -cE "<Plus |<ChevronDown " cha-bio-safety/src/pages/CheckpointsPage.tsx)" -ge "4"
# BottomSheet / DesktopModal / ModalWrapper 함수 보존
grep -q "function BottomSheet" cha-bio-safety/src/pages/CheckpointsPage.tsx
grep -q "function DesktopModal" cha-bio-safety/src/pages/CheckpointsPage.tsx
grep -q "ModalWrapper" cha-bio-safety/src/pages/CheckpointsPage.tsx
# 모바일 FAB safe-area 1 byte 0
grep -q "calc(16px + var(--sab))" cha-bio-safety/src/pages/CheckpointsPage.tsx

echo "── ALL 22 gate PASS ──"
echo "CheckpointsPage chunk size:"
ls -lh cha-bio-safety/dist/assets/ 2>/dev/null | grep -i "checkpoints\|index" || true
    </automated>
  </verify>
  <done>
- CheckpointsPage.tsx 1 파일 atomic commit 완료 (커밋 메시지: "feat(quick-260526-i4b): redesign/24-checkpoints TSX 변환 (CheckpointsPage 693→XXX + v0.1.1 토큰 className + 비즈 anchor 25 보존 + Lucide Plus+ChevronDown + OQ LOCKED 6 + W6 §1~§12 verbatim)")
- TSX 전용 22 gate PASS — Build PASS + Lucide 2종 import + linear-gradient 0 + status- prefix 0 + w-8/h-8 0 + 이모지 0 + 비즈 anchor 25 grep PASS + components.css 0 byte + App.tsx 0 byte + src/** 변경 1 파일만
- npm run build PASS (vite + PWA injectManifest) — Build 실패 시 commit 보류
- OQ LOCKED 6 적용 — #1 BottomSheet/DesktopModal 동 파일 인라인 / #2 단색 var(--acl) / #3 zone toggle row 유지 + 11→12 / #4 외곽 hex 새 토큰 / #5 9·10·11→12 일률 격상 / #6 Lucide 치환 + size 보존 (ChevronDown 14→16 격상)
- 비즈 anchor 25건 1 byte 변경 0 (CATEGORIES_FALLBACK 19종 / ZONE_LABEL / ZONE_FLOORS / FLOOR_ORDER 20건 / MARKER_TYPE_LABEL / FLOOR_CODE / admin 가드 / queryKey 6 / useMutation 3 / catCheckPoints / 자동 채우기 / canSave / eq 헬퍼 / 유도등 분기 / 소화기 분기 / ModalWrapper / BottomSheet / DesktopModal / SKELETON_STYLE / FAB safe-area / 카피 8 / placeholder 9 / toast 7 / select option ≥10)
  </done>
</task>

</tasks>

<verification>
- TSX 전용 22 gate (위 task verify) 모두 PASS — Build PASS 포함, components.css 0 byte / App.tsx 0 byte / src/** 변경 1 파일만 / 이모지 0 / fontSize 9·10·11 인라인 0 / linear-gradient 0 / status- prefix 0 / w-8 h-8 0 / wrangler 0 / npm run deploy 0
- Build gate (필수) — `cd cha-bio-safety && npm run build` PASS + chunk size 보고. Build 실패 시 최대 2회 재시도 후 deviation 보고 + commit 보류.
- 비즈 anchor 25건 grep PASS + Lucide 2종 (Plus + ChevronDown) import + 인라인 SVG 함수 제거.
- 작업 종료 누적: `git log --oneline origin/main..HEAD` ≈ 2 commit (T1 atomic 변환 1 + SUMMARY 1).
- 디자인 작업 룰: wrangler 명령 / npm run deploy 절대 금지 (이 워크트리 CLAUDE.local.md deny). main 머지 시 GitHub Actions 자동 cbc7119-preview 배포.
- 메모리 룰 13 slug 적용 — `feedback_planner_prompt_sketch_verbatim` / `feedback_tsx_wave_stat_card_drift` / `feedback_text_caption_leading_none` / `feedback_tailwind_token_class_pattern` / `feedback_tailwind_w8_h8_is_48px` / `feedback_cbc7119_design_never_wrangler` / `feedback_design_changes_ask_first` / `feedback_design_sketch_first` / `feedback_avoid_premature_confirmation` / `feedback_redesign_sketch_rule_enforcement` / `feedback_sketch_realistic_data` / `feedback_tsx_wave_emoji_dot_gap` / `project_redesign_15_daily_report_status`.
</verification>

<success_criteria>
- CheckpointsPage.tsx 의 외곽 chrome 4 영역 (W2 frame-guard 외곽 wrapper + style 태그 / W3 데스크톱+모바일 헤더 + 카테고리/필터/카운트/개소추가 / W4 콘텐츠 4상태 분기 + 데스크톱 테이블 + 모바일 CheckPointCard + FAB / W5 ModalWrapper 호출 + 폼 필드) 이 W6 §3 sketch class verbatim 으로 변환
- BottomSheet (line 40~57) + DesktopModal (line 60~74) + ModalWrapper (line 503) + 메인 hook/state/handler / useQuery 3 / useMutation 3 / 자동 채우기 useEffect / canSave / handleSave / guidelampAsCp / cpList filter eq 헬퍼 — **1 byte 변경 0** (OQ #1 default)
- 비즈 anchor 25건 grep PASS (CATEGORIES_FALLBACK 19종 / ZONE_LABEL / ZONE_FLOORS / FLOOR_ORDER 20건 / MARKER_TYPE_LABEL 6건 / FLOOR_CODE 8건 / admin 가드 / queryKey 6 / useMutation 3 / catCheckPoints / 자동 채우기 / canSave / eq / 유도등 / 소화기 / ModalWrapper / BottomSheet+DesktopModal / SKELETON_STYLE / FAB safe-area / 카피 8 / placeholder 9 / toast 7 / select option ≥10)
- Lucide 2종 (Plus + ChevronDown) import + 인라인 SVG 함수 IconPlus / IconChevronDown 제거 + 사용 ≥4 위치 (데스크톱 cat select / 데스크톱 '개소 추가' / 모바일 cat select / 모바일 FAB)
- OQ LOCKED 6 적용 — #1 BottomSheet/DesktopModal 동 파일 인라인 / #2 단색 (linear-gradient 0) / #3 zone toggle row 유지 + 11→12 / #4 외곽 hex 새 토큰 전체 치환 / #5 9·10·11 → 12 일률 격상 + leading-none / #6 Lucide 치환 + size 보존 (ChevronDown 14→16 격상)
- components.css 0 byte diff (이 wave 신규 추가 0 — OQ #1 default + 단일 파일 인라인 패턴)
- App.tsx 0 byte diff + src/** 변경 = CheckpointsPage.tsx 1 파일만
- `npm run build` PASS + CheckpointsPage chunk size 보고
- atomic 2 commit (T1 변환 + SUMMARY) — git push 시 GitHub Actions cbc7119-preview 자동 배포 트리거 + 부모 워크트리 자동 도달 (executor commit propagation 패턴)
</success_criteria>

<output>
After completion, create `.planning/quick/260526-i4b-redesign-24-checkpoints-tsx-checkpointsp/260526-i4b-SUMMARY.md` covering:
- 변환 line range 3구역 (imports / 메인 함수+모달 / JSX render 4 영역) 적용 결과 + 최종 line 수 (693 → N)
- 비즈 anchor 25종 보존 확인 (grep 결과 카운트)
- Lucide 2종 매핑 위치 (Plus size=16/18 / ChevronDown size=16/16, 데스크톱 cat select 14→16 격상)
- OQ LOCKED 6 적용 결과 + 폰트 격상 매트릭스 적용 위치 (line 279/408/567/574/578/632/641)
- BottomSheet / DesktopModal 인라인 보존 확인 (StaffManagePage 공통화는 별도 task 박제)
- npm run build PASS + chunk size + tsc --noEmit 0 errors
- components.css 0 byte + App.tsx 0 byte + src/** 1 파일 변경 확인
- 누적 commit 수 + cbc7119-preview 배포 트리거 확인 + 부모 워크트리 자동 도달
- 메모리 박제 후보 (deviation / 신규 패턴 / 추가 룰 발견 시)
- 다음 단계: 24-checkpoints 완결 → 4차 모니터링 (19/20/21 legal 시리즈) 진입 / 또는 26-staff-manage (BottomSheet/DesktopModal 공통화 task) / 다음 페이지 후보
</output>
