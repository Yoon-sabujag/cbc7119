---
phase: quick-260612-5tn
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cha-bio-safety/src/pages/LegalFindingsPage.tsx
autonomous: true
requirements:
  - QUICK-260612-5tn
must_haves:
  truths:
    - "보고서 PDF 업로드 실패 시 토스트가 '보고서 업로드 실패'로 표시된다"
    - "LegalFindingsPage.tsx 에 '사진 업로드 실패' 문구가 남아있지 않다"
    - "사진 경로 미스라벨 가드(usePhotoUpload/useMultiPhotoUpload 등 8개 파일)는 무변경이다"
  artifacts:
    - path: "cha-bio-safety/src/pages/LegalFindingsPage.tsx"
      provides: "보고서 업로드 실패 catch 블록 토스트 정확 라벨"
      contains: "보고서 업로드 실패"
  key_links:
    - from: "cha-bio-safety/src/pages/LegalFindingsPage.tsx (catch 블록 L102-103)"
      to: "react-hot-toast"
      via: "toast.error('보고서 업로드 실패')"
      pattern: "toast\\.error\\('보고서 업로드 실패'\\)"
---

<objective>
법정점검 보고서 PDF 업로드 실패 경로의 토스트 미스라벨을 수정한다.

`LegalFindingsPage.tsx` L103 의 catch 블록은 보고서 PDF 업로드(`legalApi.updateResult`로 `report_file_key` 저장) 실패 경로인데, `toast.error('사진 업로드 실패')`라는 사진 문구를 쓰는 기존 미스라벨이 있다. 보고서 업로드와 무관한 "사진" 단어 때문에 실패 원인이 오인된다.

Purpose: 사용자가 보고서 업로드 실패 시 정확한 메시지를 보고 올바르게 조치하도록 한다. 260612-3lt 사진 UX 1단계에서 범위 밖 backlog 로 분류된 별건.
Output: LegalFindingsPage.tsx L103 토스트 문구 1줄 변경 (`사진 업로드 실패` → `보고서 업로드 실패`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md
@cha-bio-safety/src/pages/LegalFindingsPage.tsx

# 변경 대상 catch 블록 (L97-106, 현재 상태)
#   if (!json.success || !json.data?.key) throw new Error('upload failed')
#   await legalApi.updateResult(id, { report_file_key: json.data.key })
#   queryClient.invalidateQueries({ queryKey: ['legal-round', id] })
#   toast.success('보고서가 업로드되었습니다.')
# } catch {
#   toast.error('사진 업로드 실패')   ← L103, 이 한 줄만 변경
# } finally {
#   setUploadingReport(false)
# }
</context>

<tasks>

<task type="auto">
  <name>Task 1: 보고서 업로드 실패 catch 토스트 라벨 정정</name>
  <files>cha-bio-safety/src/pages/LegalFindingsPage.tsx</files>
  <action>
LegalFindingsPage.tsx L103 의 `toast.error('사진 업로드 실패')` 를 `toast.error('보고서 업로드 실패')` 로 변경한다. 이 catch 블록은 보고서 PDF 업로드(`legalApi.updateResult`로 `report_file_key` 저장) 실패 경로이므로 "사진" 문구는 오라벨이다.

오직 이 한 줄(L103)만 변경한다. `git diff` 는 +1/-1 단일 hunk 여야 한다. catch 의 위치/구조, 위 try 블록(fetch /api/uploads, json 파싱, updateResult, invalidateQueries, toast.success), finally(setUploadingReport(false)), 그리고 파일 내 다른 모든 로직은 무변경.

절대 건드리지 말 것 — 다른 파일의 정상적인 '사진 업로드 실패' 문구(어제 260612-3lt 배포 가드): usePhotoUpload.ts 의 photoUploadFailMsg 헬퍼, useMultiPhotoUpload.ts 의 PhotoUploadFailedError, FindingFormSheet/RemediationDetailPage/InspectionPage/LegalFindingDetailPage/LegalPage/FloorPlanPage 의 사진 가드. 이번 변경 범위는 LegalFindingsPage.tsx 단일 파일 한 줄로 한정한다.

이 파일은 직원 production 운영 소스다. 순수 클라이언트 문구 1줄 변경 — 데이터/스키마/API 무관, staging 불필요.
  </action>
  <verify>
    <automated>grep -c "보고서 업로드 실패" cha-bio-safety/src/pages/LegalFindingsPage.tsx | grep -qx 1 && grep -c "사진 업로드 실패" cha-bio-safety/src/pages/LegalFindingsPage.tsx | grep -qx 0 && echo OK</automated>
    <automated>test "$(git diff --numstat cha-bio-safety/src/pages/LegalFindingsPage.tsx | awk '{print $1"/"$2}')" = "1/1" && test "$(git diff --name-only | wc -l | tr -d ' ')" = "1" && echo "single-file +1/-1 OK"</automated>
    <automated>cd cha-bio-safety && npx tsc --noEmit && echo "tsc OK"</automated>
    <automated>cd cha-bio-safety && npm run build && echo "build OK"</automated>
  </verify>
  <done>
LegalFindingsPage.tsx L103 토스트가 '보고서 업로드 실패'로 표시된다. 파일 내 '사진 업로드 실패' 문구 0건. git diff 는 단일 파일 +1/-1. tsc 0 errors, build PASS. 다른 8개 사진-경로 파일 무변경.
  </done>
</task>

</tasks>

<verification>
- `grep -n "보고서 업로드 실패" cha-bio-safety/src/pages/LegalFindingsPage.tsx` → 1건 (L103)
- `grep -n "사진 업로드 실패" cha-bio-safety/src/pages/LegalFindingsPage.tsx` → 0건
- `git diff --numstat` → `cha-bio-safety/src/pages/LegalFindingsPage.tsx` 만 1/1 (+1/-1)
- 보호 대상 8개 파일(usePhotoUpload.ts / useMultiPhotoUpload.ts / FindingFormSheet.tsx / RemediationDetailPage.tsx / InspectionPage.tsx / LegalFindingDetailPage.tsx / LegalPage.tsx / FloorPlanPage.tsx) `git diff` 무변경
- `cd cha-bio-safety && npx tsc --noEmit` → 0 errors
- `cd cha-bio-safety && npm run build` → PASS
</verification>

<success_criteria>
보고서 PDF 업로드 실패 시 사용자가 '보고서 업로드 실패' 토스트를 본다. LegalFindingsPage.tsx 단일 파일 한 줄(+1/-1) 변경, tsc/build PASS, 사진-경로 가드 8개 파일 무변경.
</success_criteria>

<output>
Create `.planning/quick/260612-5tn-legalfindingspage/260612-5tn-SUMMARY.md` when done.

배포는 메인 Claude(오케스트레이터)가 직접 수행한다. executor/서브에이전트는 wrangler 배포 금지(메모리 룰). 이 plan 에 배포 task 없음.
</output>
