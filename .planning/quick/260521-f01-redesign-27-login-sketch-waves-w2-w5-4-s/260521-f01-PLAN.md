---
phase: quick-260521-f01
plan: 01
type: execute
wave: 1
depends_on: []
quick_id: 260521-f01
branch: redesign/27-login
date: 2026-05-21
autonomous: true
files_modified:
  - cha-bio-safety/docs/redesign-context/27-login/sketch-wave-2-mobile-shell.html
  - cha-bio-safety/docs/redesign-context/27-login/sketch-wave-3-staff-card-grid.html
  - cha-bio-safety/docs/redesign-context/27-login/sketch-wave-4-login-form.html
  - cha-bio-safety/docs/redesign-context/27-login/wave-5-tsx-conversion-checklist.md
requirements:
  - redesign-27-login-w2-mobile-shell
  - redesign-27-login-w3-staff-card-grid
  - redesign-27-login-w4-login-form
  - redesign-27-login-w5-tsx-checklist
must_haves:
  truths:
    - "W2~W4 sketch HTML 3개 + W5 checklist markdown 1개, 총 4 파일이 27-login/ 직속 평면 배치로 생성된다"
    - "LoginPage.tsx (220 lines) 코드 변경 0건 — sketch + checklist 만 산출"
    - "negative gate 4 sketch + 1 checklist 모두 통과 (이모지 0 / linear-gradient 0 / 9·10·11px 0 / status- prefix 0 / w-8 h-8 0 / 옛 alias 토큰 0)"
    - "OQ #1~#5 LOCKED 결정 5건 모두 4 파일 안에 verbatim 반영 (그라데이션 폐기 / CARD_COLORS 6 rgba / 폰트 격상 / show-hide 텍스트 + ☎ 유지 / 인라인 px 사이즈)"
    - "각 task 마다 atomic git commit 1개씩 — 총 4 commit"
  artifacts:
    - path: "cha-bio-safety/docs/redesign-context/27-login/sketch-wave-2-mobile-shell.html"
      provides: "모바일 헤더 + 데스크톱 wrapper sketch (영역 2 + 영역 6)"
      contains: "data-theme dark+light 4 frame / w-[38px] h-[38px] rounded-[11px] / w-[28px] h-[28px] rounded-[7px] / bg-surface-page / bg-surface-raised"
      min_lines: 400
    - path: "cha-bio-safety/docs/redesign-context/27-login/sketch-wave-3-staff-card-grid.html"
      provides: "직원 카드 그리드 sketch (영역 3, CARD_COLORS 6 rgba cycle + isSelected)"
      contains: "rgba(245,158,11,0.15) / rgba(34,197,94,0.1) / rgba(59,130,246,0.1) / rgba(139,92,246,0.1) / rgba(236,72,153,0.1) / rgba(20,184,166,0.1) / w-[34px] h-[34px] rounded-[10px] / 담당자 선택 / text-caption font-bold uppercase tracking-wider"
      min_lines: 500
    - path: "cha-bio-safety/docs/redesign-context/27-login/sketch-wave-4-login-form.html"
      provides: "로그인 폼 + footer 안내문 sketch (영역 4 + 영역 5)"
      contains: "bg-safe-bar / text-text-on-accent / bg-surface-sunken / 표시 / 숨김 / ☎ 031-881-7119 / 사번 10자리 / 비밀번호 입력 / text-label font-bold leading-none / text-caption leading-relaxed"
      min_lines: 600
    - path: "cha-bio-safety/docs/redesign-context/27-login/wave-5-tsx-conversion-checklist.md"
      provides: "TSX 변환 verify checklist (markdown, 8 섹션)"
      contains: "§1 변환 범위 / §2 보존 / §3 변환 매핑 / §4 OQ LOCKED 5건 / §5 negative gate / §6 positive gate / §7 build tsc gate / §8 자체 verify 명령"
      min_lines: 250
  key_links:
    - from: "wave-1-index.md §7 OQ #1 LOCKED"
      to: "sketch-wave-4-login-form.html + wave-5-tsx-conversion-checklist.md"
      via: "로그인 버튼 bg-safe-bar solid + text-text-on-accent (그라데이션 폐기)"
      pattern: "bg-safe-bar"
    - from: "wave-1-index.md §7 OQ #2 LOCKED"
      to: "sketch-wave-3-staff-card-grid.html"
      via: "CARD_COLORS 6 rgba 인라인 verbatim + isSelected accent #2563eb or bg-accent variant"
      pattern: "rgba\\(245,158,11"
    - from: "wave-1-index.md §7 OQ #3 LOCKED"
      to: "sketch-wave-3 + sketch-wave-4 + wave-5-checklist"
      via: "폰트 격상 — footer 12px text-caption / label 13px text-label / 담당자 선택 12px text-caption / role-title 12px text-caption"
      pattern: "text-caption|text-label"
    - from: "wave-1-index.md §7 OQ #4 LOCKED"
      to: "sketch-wave-4-login-form.html"
      via: "show/hide 토글 텍스트 표시/숨김 유지 + footer ☎ U+260E 유지 (Lucide 교체 X)"
      pattern: "표시|숨김|☎"
    - from: "wave-1-index.md §7 OQ #5 LOCKED"
      to: "sketch-wave-2 + sketch-wave-3"
      via: "로고 38×38 / icon 28×28 / staff initial 34×34 모두 인라인 px (w-[Npx])"
      pattern: "w-\\[38px\\]|w-\\[28px\\]|w-\\[34px\\]"
---

<objective>
redesign/27-login 의 W1 인덱스(`wave-1-index.md`, 343 lines, OQ 5건 LOCKED 완료)를 기반으로
W2~W5 4 sub-wave 산출물을 **단일 quick task** 안에서 **atomic 4 commit** 으로 한 번에 만든다.

- **W2 (T1):** 모바일 shell + 데스크톱 wrapper + 공통 헤더 sketch (영역 2 + 영역 6)
- **W3 (T2):** 직원 카드 그리드 sketch (영역 3, CARD_COLORS 6 rgba cycle + isSelected variant)
- **W4 (T3):** 로그인 폼 + footer 안내문 sketch (영역 4 + 영역 5)
- **W5 (T4):** TSX 변환 verify checklist markdown (sketch 아님, W2~W4 grep 추출 검증 룰)

**Purpose:** 14-reports / 13-schedule 와 동일한 평면(flat sibling) 패턴으로 27-login 의 sketch 단계를
4 wave 분량을 한 turn 에 처리하여 cbc7119-preview 자동 배포 사이클 1회로 사용자 시각 검수 가능하게 함.

**Output:** `cha-bio-safety/docs/redesign-context/27-login/` 직속 4 파일 (sketch HTML 3개 + checklist md 1개).
`LoginPage.tsx` 코드 변경 0건 (verify gate 마지막에 확인).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jykevin/Documents/cbc7119-design/CLAUDE.md
@/Users/jykevin/Documents/cbc7119-design/CLAUDE.local.md

# W1 인덱스 (단일 진입점, OQ 5건 LOCKED 완료)
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/27-login/wave-1-index.md

# precedent — 14-reports W2~W6 sketch 패턴 mirror
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/14-reports/sketch-wave-2-mobile-header-card.html
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/14-reports/sketch-wave-3-mobile-card-list.html
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/14-reports/sketch-wave-4-desktop-toolbar.html
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/14-reports/sketch-wave-6-desktop-preview-wrapper.html
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/14-reports/wave-7-tsx-conversion-checklist.md

# Source TSX (sketch 대상, 변경 0)
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/src/pages/LoginPage.tsx

# 토큰/타이포 source
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/27-login/27-login.md
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/27-login/design-system.md
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/27-login/tokens.css
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/27-login/typography.css

<rules_verbatim>
### OQ LOCKED 5건 (wave-1-index.md §7 verbatim)

**OQ #1 (로그인 버튼 그라데이션 폐기):**
> LOCKED (2026-05-21): 로그인 버튼 = `bg-safe-bar` solid + `text-text-on-accent`. 그라데이션 완전 폐기. disabled 시 = `bg-surface-sunken`. W4 sketch + TSX 변환 양쪽 동일 적용.

**OQ #2 (CARD_COLORS 6 rgba 인라인 유지):**
> LOCKED (2026-05-21): `CARD_COLORS` 6 rgba 인라인 상수 그대로 보존. variant matrix 도 인라인 rgba 로 표현. isSelected accent 만 `#2563eb` → `bg-accent` 토큰화 검토 (W3 sketch 단계 결정).

**OQ #3 (폰트 격상 verbatim):**
> LOCKED (2026-05-21): 폰트 격상 매핑 (sketch + TSX 변환 양쪽):
> - footer 안내문 11px → `text-caption` (12px) `leading-relaxed text-text-tertiary`
> - 사번/비밀번호 label 11px → `text-label` (13px) `font-bold leading-none text-text-secondary`
> - "담당자 선택" 라벨 10px → `text-caption` (12px) `font-bold uppercase tracking-wider text-text-tertiary leading-none`
> - 카드 안 role/title 10px → `text-caption` (12px) `leading-none text-text-tertiary`
> - §1.1 16px 마지노선 위반은 §3 머리말에 1줄 메타로 명시 (현 footer/label/sub 의 보조 정보 위계상 절충)

**OQ #4 (show/hide 텍스트 + ☎ 유지):**
> LOCKED (2026-05-21): show/hide 토글 = 텍스트 "표시"/"숨김" 유지 (노안 친화 우선). footer ☎ (U+260E) 글리프 = 콘텐츠 글리프로 유지 (이모지 제거 룰 8 예외 케이스 — 전화 affordance). W4 sketch 의 비밀번호 input 우측 토글 ghost button 은 텍스트 그대로.

**OQ #5 (인라인 px 사이즈 유지):**
> LOCKED (2026-05-21): 로고 박스 = `w-[38px] h-[38px] rounded-[11px]` + 내부 img = `w-[28px] h-[28px] rounded-[7px]` 인라인. 모바일/데스크톱 둘 다 동일. W2 sketch + TSX 변환 양쪽 동일 적용. 동시에 staff initial 박스 34×34 도 `w-[34px] h-[34px] rounded-[10px]` 인라인 (W3, OQ 항목 아니지만 동일 인라인 명시 룰 적용).

### 디자인 토큰 패턴 (wave-1-index.md §3 + planning constraints verbatim)
- `bg-surface-page` (var(--bg) → --surface-page) 페이지 외곽
- `bg-surface-raised` (var(--bg2) → --surface-raised) 카드/wrapper
- `bg-surface-sunken` (var(--bg3) → --surface-sunken) input/disabled
- `border-border-default` (var(--bd) → --border-default)
- `border-border-strong` (var(--bd2) → --border-strong)
- `text-text-primary` (var(--t1) → --text-primary)
- `text-text-secondary` (var(--t2) → --text-secondary)
- `text-text-tertiary` (var(--t3) → --text-tertiary)
- `text-text-on-accent` (액센트 버튼 위 흰 글자)
- `bg-safe-bar` / `bg-accent` (status- prefix 없음 — memory feedback_tailwind_token_class_pattern)
- radius: `rounded-sm` 8 / `rounded-md` 12 / `rounded-lg` 16 / 토큰 없는 값은 `rounded-[Npx]` 인라인

### Negative rule (모든 task verify gate)
- 이모지 0건 (단 W4 footer ☎ U+260E 는 OQ #4 LOCKED 예외 — 콘텐츠 글리프)
- `linear-gradient` 0건
- 9·10·11px fontSize 0건 (OQ #3 격상 결과)
- `status-` prefix class 0건 (`text-status-safe` X, `bg-status-fire` X)
- `w-8` / `h-8` 0건 (tailwind config w-8=48 함정)
- 옛 alias 토큰 (`var(--bg)` `var(--bg2)` `var(--bg3)` `var(--bd)` `var(--bd2)` `var(--t1)` `var(--t2)` `var(--t3)` `var(--acl)` `var(--accent)`) 0건 in sketch (단, tokens.css 정의 블록 인용 시 fence 안 예외)

### 평면 폴더 패턴 (13-schedule + 14-reports 일관)
- 27-login/ 직속에 4 파일 배치
- `sketch/` 서브폴더 절대 금지
</rules_verbatim>
</context>

<tasks>

<task type="auto">
  <name>Task 1 (W2): 모바일 shell + 데스크톱 wrapper + 공통 헤더 sketch 생성</name>
  <files>cha-bio-safety/docs/redesign-context/27-login/sketch-wave-2-mobile-shell.html</files>
  <action>
    14-reports/sketch-wave-2-mobile-header-card.html 패턴 mirror 로 새 sketch 파일 작성. 영역 2 (모바일 헤더, LoginPage.tsx line 204~213) + 영역 6 (데스크톱 wrapper + 카드 헤더, line 175~194) 시각화.

    **구조 (14-reports W2 mirror):**
    - `<!DOCTYPE html>` + `<html lang="ko">` + `<head><meta charset="utf-8"><title>redesign/27-login — W2 모바일 shell + 데스크톱 wrapper</title>` + `<style>` 블록
    - `<style>` 안: v0.1.1 tokens.css 의 dark+light 양쪽 정의 verbatim 인용 (`[data-theme="dark"]` + `[data-theme="light"]` selector), typography.css 의 `text-caption` `text-label` `text-body` `font-bold` 정의 verbatim 인용
    - `<body>` 안 4 frame side-by-side:
      | frame | viewport | 시각화 |
      |---|---|---|
      | 1 | 모바일 다크 393×800 | `data-theme="dark"` + 모바일 헤더 (로고 박스 + 타이틀 + 서브) + body 영역 placeholder ("{inner} — W3/W4 에서 채움") |
      | 2 | 모바일 라이트 393×800 | `data-theme="light"` + 모바일 헤더 동일 |
      | 3 | 데스크톱 다크 1280×800 | `data-theme="dark"` + 외곽 (minHeight:100dvh + flex center + padding 20 + bg-surface-page) + 카드 (maxWidth:420 + rounded-[20px] + bg-surface-raised + border border-border-default + boxShadow 0 8px 32px rgba(0,0,0,0.4) + overflow-hidden) + 카드 헤더 (padding:24px 24px 20px + border-b border-border-default) + 카드 바디 placeholder |
      | 4 | 데스크톱 라이트 1280×800 | `data-theme="light"` + 위 동일 (boxShadow 0 8px 32px rgba(0,0,0,0.4) 는 라이트에서도 동일 또는 약화 — 14-reports 패턴 확인 후 결정) |

    **헤더 마크업 룰 (4 frame 공통 — OQ #5 LOCKED + OQ #3 LOCKED 반영):**
    ```html
    <div class="bg-surface-raised border-b border-border-default" style="padding:16px 20px 24px;">
      <div class="flex items-center" style="gap:12px;margin-top:16px;">
        <div class="w-[38px] h-[38px] rounded-[11px]" style="background:rgba(37,99,235,0.2);border:1px solid rgba(59,130,246,0.3);display:flex;align-items:center;justify-content:center;overflow:hidden;">
          <img src="/icons/icon-192.png" alt="" class="w-[28px] h-[28px] rounded-[7px]" />
        </div>
        <div>
          <div class="text-body font-bold text-text-primary">차바이오컴플렉스 방재팀</div>
          <div class="text-caption text-text-tertiary leading-none" style="margin-top:2px;">소방안전 통합관리 시스템</div>
        </div>
      </div>
    </div>
    ```
    (서브 11px → OQ #3 LOCKED `text-caption` 12px + `leading-none` 적용. 타이틀 16px → `text-body font-bold`.)

    **데스크톱 카드 마크업 룰 (frame 3, 4):**
    ```html
    <div style="min-height:100dvh;background:var(--surface-page);display:flex;align-items:center;justify-content:center;padding:20px;">
      <div class="rounded-[20px] bg-surface-raised border border-border-default" style="max-width:420px;width:100%;box-shadow:0 8px 32px rgba(0,0,0,0.4);overflow:hidden;">
        <div class="bg-surface-raised border-b border-border-default" style="padding:24px 24px 20px;">
          <!-- 카드 헤더: 모바일 헤더 markup 재사용 -->
        </div>
        <div style="padding:16px 16px 24px;">
          <!-- 카드 바디 (W3/W4 inner placeholder) -->
          <div class="text-caption text-text-tertiary leading-none" style="opacity:.5;text-align:center;padding:32px 0;">
            {inner} placeholder — W3 직원 카드 그리드 + W4 로그인 폼 + footer
          </div>
        </div>
      </div>
    </div>
    ```

    **rules 박스 (sketch 하단, viewport-label 외 메타):**
    - LOCKED 결정: OQ #5 (로고 인라인 px) + OQ #3 (서브 12px text-caption) verbatim 인용 1 fence
    - 토큰 매핑: `var(--bg) → --surface-page` / `var(--bg2) → --surface-raised` / `var(--bd) → --border-default` / `var(--t1) → --text-primary` / `var(--t3) → --text-tertiary` / radius:20 → `rounded-[20px]` 인라인 (토큰 없음) / radius:11 → `rounded-[11px]` 인라인 / radius:7 → `rounded-[7px]` 인라인
    - negative gate 자체매칭 회피 표현: "이모지 0 / linear-gradient 0 / 9·10·11px 0 / status- prefix 0 / w-8 h-8 0 / 옛 alias 토큰 0" (이 줄 자체가 gate grep 의 대상이 되지 않도록 `<code>` fence 안 인용 또는 별도 escape — `w-8`/`h-8` 같은 문자열은 sketch 본문에 등장하지 않도록 fence 처리)

    **commit 메시지:**
    `docs(quick-260521-f01): redesign/27-login W2 mobile shell + desktop wrapper sketch (4 frame)`

    **commit 명령:**
    `git add cha-bio-safety/docs/redesign-context/27-login/sketch-wave-2-mobile-shell.html && git commit -m "docs(quick-260521-f01): redesign/27-login W2 mobile shell + desktop wrapper sketch (4 frame)"`
  </action>
  <verify>
    <automated>
test -f cha-bio-safety/docs/redesign-context/27-login/sketch-wave-2-mobile-shell.html \
  && wc -l cha-bio-safety/docs/redesign-context/27-login/sketch-wave-2-mobile-shell.html | awk '{ if ($1 < 200) { print "FAIL: too short"; exit 1 } else print "PASS lines:", $1 }' \
  && grep -c 'data-theme="dark"' cha-bio-safety/docs/redesign-context/27-login/sketch-wave-2-mobile-shell.html | awk '{ if ($1 < 2) { print "FAIL: dark frame <2"; exit 1 } else print "PASS dark frame:", $1 }' \
  && grep -c 'data-theme="light"' cha-bio-safety/docs/redesign-context/27-login/sketch-wave-2-mobile-shell.html | awk '{ if ($1 < 2) { print "FAIL: light frame <2"; exit 1 } else print "PASS light frame:", $1 }' \
  && grep -c 'w-\[38px\]' cha-bio-safety/docs/redesign-context/27-login/sketch-wave-2-mobile-shell.html | awk '{ if ($1 < 4) { print "FAIL: w-[38px] <4 (4 frame x 1 each)"; exit 1 } else print "PASS w-[38px]:", $1 }' \
  && grep -c 'w-\[28px\]' cha-bio-safety/docs/redesign-context/27-login/sketch-wave-2-mobile-shell.html | awk '{ if ($1 < 4) { print "FAIL: w-[28px] <4"; exit 1 } else print "PASS w-[28px]:", $1 }' \
  && grep -c 'rounded-\[11px\]' cha-bio-safety/docs/redesign-context/27-login/sketch-wave-2-mobile-shell.html | awk '{ if ($1 < 4) { print "FAIL: rounded-[11px] <4"; exit 1 } else print "PASS rounded-[11px]:", $1 }' \
  && grep -c 'rounded-\[7px\]' cha-bio-safety/docs/redesign-context/27-login/sketch-wave-2-mobile-shell.html | awk '{ if ($1 < 4) { print "FAIL: rounded-[7px] <4"; exit 1 } else print "PASS rounded-[7px]:", $1 }' \
  && grep -c '차바이오컴플렉스 방재팀' cha-bio-safety/docs/redesign-context/27-login/sketch-wave-2-mobile-shell.html | awk '{ if ($1 < 4) { print "FAIL: 타이틀 <4"; exit 1 } else print "PASS 타이틀:", $1 }' \
  && grep -c '소방안전 통합관리 시스템' cha-bio-safety/docs/redesign-context/27-login/sketch-wave-2-mobile-shell.html | awk '{ if ($1 < 4) { print "FAIL: 서브 <4"; exit 1 } else print "PASS 서브:", $1 }' \
  && grep -cE 'linear-gradient' cha-bio-safety/docs/redesign-context/27-login/sketch-wave-2-mobile-shell.html | awk '{ if ($1 > 0) { print "FAIL: linear-gradient found"; exit 1 } else print "PASS no gradient" }' \
  && grep -cE 'status-(safe|fire|warning|danger|caution)' cha-bio-safety/docs/redesign-context/27-login/sketch-wave-2-mobile-shell.html | awk '{ if ($1 > 0) { print "FAIL: status- prefix found"; exit 1 } else print "PASS no status- prefix" }' \
  && grep -cE 'fontSize:\s*(9|10|11)[^0-9]' cha-bio-safety/docs/redesign-context/27-login/sketch-wave-2-mobile-shell.html | awk '{ if ($1 > 0) { print "FAIL: 9/10/11 px found"; exit 1 } else print "PASS no 9-11px" }' \
  && grep -cE 'OQ #5|OQ #3' cha-bio-safety/docs/redesign-context/27-login/sketch-wave-2-mobile-shell.html | awk '{ if ($1 < 2) { print "FAIL: OQ #5/#3 citation <2"; exit 1 } else print "PASS OQ citations:", $1 }' \
  && git log --oneline -1 | grep -q '260521-f01.*W2' && echo "PASS commit W2"
    </automated>
  </verify>
  <done>
    - sketch-wave-2-mobile-shell.html 파일 존재 + ≥200 lines
    - data-theme="dark" / data-theme="light" 각 ≥2 frame (총 4 frame)
    - w-[38px] / w-[28px] / rounded-[11px] / rounded-[7px] 각 ≥4 (4 frame x 1 each)
    - "차바이오컴플렉스 방재팀" / "소방안전 통합관리 시스템" 각 ≥4 frame
    - linear-gradient 0건 / status- prefix 0건 / 9·10·11px fontSize 0건
    - OQ #5 + OQ #3 인용 ≥2건
    - atomic commit `docs(quick-260521-f01): ... W2 mobile shell + desktop wrapper sketch (4 frame)` 1개
  </done>
</task>

<task type="auto">
  <name>Task 2 (W3): 직원 카드 그리드 sketch 생성 (CARD_COLORS 6 rgba + isSelected variant)</name>
  <files>cha-bio-safety/docs/redesign-context/27-login/sketch-wave-3-staff-card-grid.html</files>
  <action>
    14-reports/sketch-wave-3-mobile-card-list.html 패턴 mirror 로 새 sketch 파일 작성. 영역 3 (직원 카드 그리드, LoginPage.tsx line 85~115) 시각화. CARD_COLORS 6종 cycle + isSelected variant matrix 모두 표현.

    **구조 (14-reports W3 mirror):**
    - `<!DOCTYPE html>` + `<head><title>redesign/27-login — W3 직원 카드 그리드</title>` + `<style>` 블록 (W2 와 동일 tokens + typography 인용)
    - `<body>` 안 4 frame side-by-side:
      | frame | viewport | 시각화 |
      |---|---|---|
      | 1 | 모바일 다크 393×900 | `data-theme="dark"` + 카드 그리드 컨테이너 (bg-surface-raised + border border-border-default + rounded-lg + padding 16) + 라벨 "담당자 선택" + 6 staff full grid (CARD_COLORS 6종 cycle, isSelected 없음) |
      | 2 | 모바일 다크 393×900 isSelected variant | 동일 카드 그리드 + 2번째 카드 (홍길동 등) isSelected = true (border `rgba(59,130,246,0.6)` / bg `rgba(59,130,246,0.12)` / initial `#2563eb` 또는 `bg-accent` 토큰 양쪽 시각화) |
      | 3 | 모바일 라이트 393×900 | `data-theme="light"` + 6 staff full grid (라이트 모드 명도 차 확인) |
      | 4 | 데스크톱 다크 1280×900 centered 420px wrapper 안 grid | `data-theme="dark"` + 카드 (maxWidth:420) 안 카드 그리드 (모바일과 동일 마크업) |

    **카드 그리드 마크업 룰 (각 frame 공통 — OQ #2 + OQ #3 + OQ #5 LOCKED 반영):**
    ```html
    <div class="bg-surface-raised border border-border-default rounded-lg" style="padding:16px;margin-bottom:12px;">
      <p class="text-caption font-bold uppercase tracking-wider text-text-tertiary leading-none" style="margin-bottom:12px;">담당자 선택</p>
      <div style="display:grid;grid-template-columns:1fr 1fr;gap:8px;">
        <!-- staff card 반복, 6 staff x 2 column -->
        <button style="display:flex;align-items:center;gap:10px;padding:10px;border-radius:12px;border:1px solid rgba(245,158,11,0.35);background:rgba(245,158,11,0.15);cursor:pointer;text-align:left;transition:all .13s;">
          <div class="w-[34px] h-[34px] rounded-[10px]" style="background:rgba(245,158,11,0.35);display:flex;align-items:center;justify-content:center;color:#fff;flex-shrink:0;">
            <span class="text-body font-bold" style="line-height:1;">윤</span>
          </div>
          <div>
            <div class="text-label font-bold text-text-primary leading-none" style="margin-bottom:4px;">윤종엽</div>
            <div class="text-caption text-text-tertiary leading-none">관리자</div>
          </div>
        </button>
        <!-- staff 2: green -->
        <!-- staff 3: blue -->
        <!-- staff 4: violet -->
        <!-- staff 5: pink -->
        <!-- staff 6: teal -->
      </div>
    </div>
    ```

    **CARD_COLORS 6 rgba verbatim (LoginPage.tsx line 21~28):**
    ```
    [0] amber:  bg rgba(245,158,11,0.15) / border rgba(245,158,11,0.35)
    [1] green:  bg rgba(34,197,94,0.1)   / border rgba(34,197,94,0.3)
    [2] blue:   bg rgba(59,130,246,0.1)  / border rgba(59,130,246,0.3)
    [3] violet: bg rgba(139,92,246,0.1)  / border rgba(139,92,246,0.3)
    [4] pink:   bg rgba(236,72,153,0.1)  / border rgba(236,72,153,0.3)
    [5] teal:   bg rgba(20,184,166,0.1)  / border rgba(20,184,166,0.3)
    ```
    각 카드의 initial 박스 bg 도 동일 border 색 (CARD_COLORS[i].border) 적용. 6 staff 전부 다른 색.

    **isSelected variant (frame 2 안 2번째 카드):**
    - border = `rgba(59,130,246,0.6)` (verbatim, line 99)
    - background = `rgba(59,130,246,0.12)` (verbatim, line 100)
    - initial bg = `#2563eb` 인라인 + 옆에 `bg-accent` 토큰 variant 동시 시각화 (사용자 컨펌 안 받았으니 두 옵션 다 보여줌, OQ #2 LOCKED 의 "W3 sketch 단계 결정" 반영)
    - 시각화 방식: frame 2 안 같은 카드 2 row — 윗 row 는 `#2563eb` 인라인, 아래 row 는 `bg-accent` 토큰 라벨링 또는 별도 인접 frame 추가

    **샘플 staff 6명 (sketch 만, 실 데이터 아님 — memory feedback_sketch_realistic_data 비례 룰):**
    이름은 1글자 initial 이 다 다른 한글 이름 예시 — "윤종엽 / 김민준 / 박서연 / 이지호 / 최예린 / 정시우" 또는 "방재팀1~6" 같이 형식 일관. role/title 도 동일 — "관리자 / 책임 / 주임 / 사원 / 사원 / 사원" 등. **실 staff 데이터 가져오지 말 것** — sketch 는 디자인만, 실제 카피는 API.

    **rules 박스:**
    - LOCKED 결정: OQ #2 (CARD_COLORS 인라인 rgba 유지) + OQ #3 (담당자 선택 12px text-caption / role-title 12px text-caption) + OQ #5 (staff initial w-[34px] h-[34px] rounded-[10px] 인라인) verbatim 인용 1 fence
    - 토큰 매핑: 컨테이너 `bg-surface-raised border border-border-default rounded-lg` / 카드 border-radius:12px 인라인 (rounded-md 토큰 12 와 동일 — 토큰화 가능하나 인라인 명시도 OK) / staff initial w-[34px] h-[34px] rounded-[10px] 인라인 (토큰 없음)

    **commit 메시지:**
    `docs(quick-260521-f01): redesign/27-login W3 staff card grid sketch (CARD_COLORS 6 rgba + isSelected)`

    **commit 명령:**
    `git add cha-bio-safety/docs/redesign-context/27-login/sketch-wave-3-staff-card-grid.html && git commit -m "docs(quick-260521-f01): redesign/27-login W3 staff card grid sketch (CARD_COLORS 6 rgba + isSelected)"`
  </action>
  <verify>
    <automated>
test -f cha-bio-safety/docs/redesign-context/27-login/sketch-wave-3-staff-card-grid.html \
  && wc -l cha-bio-safety/docs/redesign-context/27-login/sketch-wave-3-staff-card-grid.html | awk '{ if ($1 < 300) { print "FAIL: too short"; exit 1 } else print "PASS lines:", $1 }' \
  && grep -c 'rgba(245,158,11' cha-bio-safety/docs/redesign-context/27-login/sketch-wave-3-staff-card-grid.html | awk '{ if ($1 < 1) { print "FAIL: amber missing"; exit 1 } else print "PASS amber:", $1 }' \
  && grep -c 'rgba(34,197,94' cha-bio-safety/docs/redesign-context/27-login/sketch-wave-3-staff-card-grid.html | awk '{ if ($1 < 1) { print "FAIL: green missing"; exit 1 } else print "PASS green:", $1 }' \
  && grep -c 'rgba(59,130,246,0\.[13]' cha-bio-safety/docs/redesign-context/27-login/sketch-wave-3-staff-card-grid.html | awk '{ if ($1 < 1) { print "FAIL: blue missing"; exit 1 } else print "PASS blue:", $1 }' \
  && grep -c 'rgba(139,92,246' cha-bio-safety/docs/redesign-context/27-login/sketch-wave-3-staff-card-grid.html | awk '{ if ($1 < 1) { print "FAIL: violet missing"; exit 1 } else print "PASS violet:", $1 }' \
  && grep -c 'rgba(236,72,153' cha-bio-safety/docs/redesign-context/27-login/sketch-wave-3-staff-card-grid.html | awk '{ if ($1 < 1) { print "FAIL: pink missing"; exit 1 } else print "PASS pink:", $1 }' \
  && grep -c 'rgba(20,184,166' cha-bio-safety/docs/redesign-context/27-login/sketch-wave-3-staff-card-grid.html | awk '{ if ($1 < 1) { print "FAIL: teal missing"; exit 1 } else print "PASS teal:", $1 }' \
  && grep -c 'rgba(59,130,246,0\.6\|rgba(59,130,246,0\.12' cha-bio-safety/docs/redesign-context/27-login/sketch-wave-3-staff-card-grid.html | awk '{ if ($1 < 1) { print "FAIL: isSelected variant missing"; exit 1 } else print "PASS isSelected:", $1 }' \
  && grep -c '담당자 선택' cha-bio-safety/docs/redesign-context/27-login/sketch-wave-3-staff-card-grid.html | awk '{ if ($1 < 4) { print "FAIL: 담당자 선택 <4"; exit 1 } else print "PASS 담당자 선택:", $1 }' \
  && grep -c 'w-\[34px\]' cha-bio-safety/docs/redesign-context/27-login/sketch-wave-3-staff-card-grid.html | awk '{ if ($1 < 4) { print "FAIL: staff initial w-[34px] <4"; exit 1 } else print "PASS w-[34px]:", $1 }' \
  && grep -c 'rounded-\[10px\]' cha-bio-safety/docs/redesign-context/27-login/sketch-wave-3-staff-card-grid.html | awk '{ if ($1 < 4) { print "FAIL: rounded-[10px] <4"; exit 1 } else print "PASS rounded-[10px]:", $1 }' \
  && grep -c 'text-caption font-bold uppercase tracking-wider' cha-bio-safety/docs/redesign-context/27-login/sketch-wave-3-staff-card-grid.html | awk '{ if ($1 < 4) { print "FAIL: 담당자 선택 라벨 class <4"; exit 1 } else print "PASS 라벨 class:", $1 }' \
  && grep -cE 'linear-gradient' cha-bio-safety/docs/redesign-context/27-login/sketch-wave-3-staff-card-grid.html | awk '{ if ($1 > 0) { print "FAIL: linear-gradient found"; exit 1 } else print "PASS no gradient" }' \
  && grep -cE 'status-(safe|fire|warning|danger|caution)' cha-bio-safety/docs/redesign-context/27-login/sketch-wave-3-staff-card-grid.html | awk '{ if ($1 > 0) { print "FAIL: status- prefix found"; exit 1 } else print "PASS no status- prefix" }' \
  && grep -cE 'fontSize:\s*(9|10|11)[^0-9]' cha-bio-safety/docs/redesign-context/27-login/sketch-wave-3-staff-card-grid.html | awk '{ if ($1 > 0) { print "FAIL: 9/10/11 px found"; exit 1 } else print "PASS no 9-11px" }' \
  && grep -cE 'OQ #2|OQ #3|OQ #5' cha-bio-safety/docs/redesign-context/27-login/sketch-wave-3-staff-card-grid.html | awk '{ if ($1 < 3) { print "FAIL: OQ citations <3"; exit 1 } else print "PASS OQ citations:", $1 }' \
  && git log --oneline -1 | grep -q '260521-f01.*W3' && echo "PASS commit W3"
    </automated>
  </verify>
  <done>
    - sketch-wave-3-staff-card-grid.html 파일 존재 + ≥300 lines
    - CARD_COLORS 6 rgba 모두 등장 (amber/green/blue/violet/pink/teal)
    - isSelected variant (rgba(59,130,246,0.6) border + rgba(59,130,246,0.12) bg) 1+ 등장
    - "담당자 선택" 라벨 ≥4 (4 frame x 1 each)
    - w-[34px] / rounded-[10px] (staff initial 인라인 px) ≥4
    - `text-caption font-bold uppercase tracking-wider` 라벨 class ≥4
    - linear-gradient 0건 / status- prefix 0건 / 9·10·11px fontSize 0건
    - OQ #2 + #3 + #5 인용 ≥3건
    - atomic commit `docs(quick-260521-f01): ... W3 staff card grid sketch (CARD_COLORS 6 rgba + isSelected)` 1개
  </done>
</task>

<task type="auto">
  <name>Task 3 (W4): 로그인 폼 + footer 안내문 sketch 생성 (bg-safe-bar solid + 표시/숨김 + ☎)</name>
  <files>cha-bio-safety/docs/redesign-context/27-login/sketch-wave-4-login-form.html</files>
  <action>
    14-reports/sketch-wave-4-desktop-toolbar.html 패턴 mirror 로 새 sketch 파일 작성. 영역 4 (로그인 폼, line 118~165) + 영역 5 (footer 안내문, line 167~170) 시각화. **OQ #1 LOCKED 결정 (그라데이션 폐기 → bg-safe-bar solid)** 가 본 wave 의 핵심.

    **구조 (14-reports W4 mirror):**
    - `<!DOCTYPE html>` + `<head><title>redesign/27-login — W4 로그인 폼 + footer 안내문</title>` + `<style>` 블록 (W2/W3 와 동일 tokens + typography 인용)
    - `<body>` 안 5 frame side-by-side:
      | frame | viewport | 시각화 |
      |---|---|---|
      | 1 | 모바일 다크 393×800 default empty | `data-theme="dark"` + 폼 컨테이너 (bg-surface-raised + border-border-default + rounded-lg + padding 16) + 사번 input empty + 비밀번호 input empty + 로그인 버튼 default (bg-safe-bar solid) + footer 안내문 |
      | 2 | 모바일 다크 393×800 입력 중 + show pw | 사번 "2022051052" 입력됨 + 비밀번호 "********" 입력됨 + show pw 토글 = "숨김" (showPw=true 상태) + 비밀번호 input type=text 시각화 + 로그인 버튼 default |
      | 3 | 모바일 다크 393×800 loading | 사번 + 비밀번호 입력됨 + 로그인 버튼 disabled (bg-surface-sunken) + "로그인 중..." 카피 |
      | 4 | 모바일 라이트 393×800 default | `data-theme="light"` + frame 1 mirror |
      | 5 | 데스크톱 다크 1280×800 centered 420px | `data-theme="dark"` + 카드 wrapper (maxWidth:420) 안 폼 + footer (W2 wrapper 와 일관) |

    **로그인 폼 마크업 룰 (frame 1 base, OQ #1 + OQ #3 + OQ #4 LOCKED 반영):**
    ```html
    <div class="bg-surface-raised border border-border-default rounded-lg" style="padding:16px;">
      <form style="display:flex;flex-direction:column;gap:12px;">
        <!-- 사번 -->
        <div>
          <label class="text-label font-bold leading-none text-text-secondary" style="display:block;margin-bottom:6px;">사번</label>
          <input type="text" inputmode="numeric" placeholder="사번 10자리"
            class="w-full bg-surface-sunken border border-border-strong rounded-sm text-text-primary"
            style="padding:12px 14px;font-size:14px;outline:none;transition:border-color .15s;" />
        </div>
        <!-- 비밀번호 -->
        <div>
          <label class="text-label font-bold leading-none text-text-secondary" style="display:block;margin-bottom:6px;">비밀번호</label>
          <div style="position:relative;">
            <input type="password" placeholder="비밀번호 입력"
              class="w-full bg-surface-sunken border border-border-strong rounded-sm text-text-primary"
              style="padding:12px 14px;padding-right:44px;font-size:14px;outline:none;transition:border-color .15s;" />
            <button type="button"
              class="text-text-tertiary"
              style="position:absolute;right:12px;top:50%;transform:translateY(-50%);background:none;border:none;cursor:pointer;font-size:13px;">
              표시
            </button>
          </div>
        </div>
        <!-- 로그인 버튼 (OQ #1 LOCKED: bg-safe-bar solid + text-text-on-accent, 그라데이션 완전 폐기) -->
        <button type="submit"
          class="bg-safe-bar text-text-on-accent font-bold"
          style="padding:14px;border-radius:12px;border:none;font-size:14px;cursor:pointer;transition:all .13s;margin-top:4px;">
          로그인
        </button>
      </form>
    </div>
    <!-- footer 안내문 (OQ #3 LOCKED: text-caption 12px leading-relaxed, OQ #4 LOCKED: ☎ U+260E 유지) -->
    <p class="text-caption leading-relaxed text-text-tertiary" style="text-align:center;margin-top:20px;">
      초기 비밀번호: 사번 뒤 4자리<br/>
      문의: 방재팀 내선 ☎ 031-881-7119
    </p>
    ```

    **disabled (loading) 변형 (frame 3):**
    ```html
    <button type="submit" disabled
      class="bg-surface-sunken text-text-tertiary font-bold"
      style="padding:14px;border-radius:12px;border:none;font-size:14px;cursor:not-allowed;transition:all .13s;margin-top:4px;">
      로그인 중...
    </button>
    ```

    **show pw 활성 변형 (frame 2):**
    - `<input type="text" placeholder="비밀번호 입력" value="********"` 시각화
    - 토글 button 카피 = "숨김" (OQ #4 LOCKED: 텍스트 유지)

    **rules 박스:**
    - LOCKED 결정: OQ #1 (로그인 버튼 bg-safe-bar solid, 그라데이션 폐기) + OQ #3 (label 13px text-label / footer 12px text-caption) + OQ #4 (show/hide 텍스트 + footer ☎ 유지) verbatim 인용 1 fence
    - 토큰 매핑: 폼 컨테이너 `bg-surface-raised border border-border-default rounded-lg` / input `bg-surface-sunken border border-border-strong rounded-sm` / 로그인 버튼 `bg-safe-bar text-text-on-accent` (OQ #1) / disabled 시 `bg-surface-sunken text-text-tertiary` / footer `text-caption leading-relaxed text-text-tertiary`
    - 명시 메타: "linear-gradient 0건 — 로그인 버튼은 bg-safe-bar solid, OQ #1 LOCKED" (이 메타가 verify gate grep 의 false positive 가 되지 않도록 fence 안 또는 escape 처리 — 예: `linear-gradient` 라는 문자열 자체를 sketch 본문에 등장시키지 않거나, `<code>` fence 안에서만 인용)

    **footer ☎ 글리프 (U+260E):**
    - "문의: 방재팀 내선 ☎ 031-881-7119" 정확히 인용 (LoginPage.tsx line 169 verbatim)
    - ☎ 는 OQ #4 LOCKED 예외 — 콘텐츠 글리프, 이모지 제거 룰 적용 안 함
    - 단, 다른 모든 이모지 (🎯⬇✓ 등) 는 0건

    **show/hide 토글 카피:**
    - "표시" / "숨김" 텍스트 그대로 (OQ #4 LOCKED)
    - Lucide `Eye` / `EyeOff` 아이콘 X (텍스트 유지)

    **commit 메시지:**
    `docs(quick-260521-f01): redesign/27-login W4 login form + footer sketch (bg-safe-bar solid + 표시/숨김 + ☎)`

    **commit 명령:**
    `git add cha-bio-safety/docs/redesign-context/27-login/sketch-wave-4-login-form.html && git commit -m "docs(quick-260521-f01): redesign/27-login W4 login form + footer sketch (bg-safe-bar solid + 표시/숨김 + ☎)"`
  </action>
  <verify>
    <automated>
test -f cha-bio-safety/docs/redesign-context/27-login/sketch-wave-4-login-form.html \
  && wc -l cha-bio-safety/docs/redesign-context/27-login/sketch-wave-4-login-form.html | awk '{ if ($1 < 400) { print "FAIL: too short"; exit 1 } else print "PASS lines:", $1 }' \
  && grep -c 'bg-safe-bar' cha-bio-safety/docs/redesign-context/27-login/sketch-wave-4-login-form.html | awk '{ if ($1 < 4) { print "FAIL: bg-safe-bar <4 (default + 4 frame x 1 each)"; exit 1 } else print "PASS bg-safe-bar:", $1 }' \
  && grep -c 'text-text-on-accent' cha-bio-safety/docs/redesign-context/27-login/sketch-wave-4-login-form.html | awk '{ if ($1 < 4) { print "FAIL: text-text-on-accent <4"; exit 1 } else print "PASS text-text-on-accent:", $1 }' \
  && grep -c '표시' cha-bio-safety/docs/redesign-context/27-login/sketch-wave-4-login-form.html | awk '{ if ($1 < 2) { print "FAIL: 표시 토글 <2"; exit 1 } else print "PASS 표시:", $1 }' \
  && grep -c '숨김' cha-bio-safety/docs/redesign-context/27-login/sketch-wave-4-login-form.html | awk '{ if ($1 < 1) { print "FAIL: 숨김 토글 <1 (frame 2 활성)"; exit 1 } else print "PASS 숨김:", $1 }' \
  && grep -c '☎' cha-bio-safety/docs/redesign-context/27-login/sketch-wave-4-login-form.html | awk '{ if ($1 < 5) { print "FAIL: ☎ <5 (5 frame x 1 each)"; exit 1 } else print "PASS ☎:", $1 }' \
  && grep -c '031-881-7119' cha-bio-safety/docs/redesign-context/27-login/sketch-wave-4-login-form.html | awk '{ if ($1 < 5) { print "FAIL: 전화번호 <5"; exit 1 } else print "PASS 전화번호:", $1 }' \
  && grep -c '초기 비밀번호: 사번 뒤 4자리' cha-bio-safety/docs/redesign-context/27-login/sketch-wave-4-login-form.html | awk '{ if ($1 < 5) { print "FAIL: 초기 비밀번호 안내 <5"; exit 1 } else print "PASS 초기 비밀번호:", $1 }' \
  && grep -c '사번 10자리' cha-bio-safety/docs/redesign-context/27-login/sketch-wave-4-login-form.html | awk '{ if ($1 < 5) { print "FAIL: placeholder 사번 <5"; exit 1 } else print "PASS placeholder 사번:", $1 }' \
  && grep -c '비밀번호 입력' cha-bio-safety/docs/redesign-context/27-login/sketch-wave-4-login-form.html | awk '{ if ($1 < 5) { print "FAIL: placeholder 비밀번호 <5"; exit 1 } else print "PASS placeholder 비밀번호:", $1 }' \
  && grep -c '로그인 중\.\.\.' cha-bio-safety/docs/redesign-context/27-login/sketch-wave-4-login-form.html | awk '{ if ($1 < 1) { print "FAIL: 로그인 중... <1 (frame 3 loading)"; exit 1 } else print "PASS 로그인 중...:", $1 }' \
  && grep -c 'text-label font-bold leading-none' cha-bio-safety/docs/redesign-context/27-login/sketch-wave-4-login-form.html | awk '{ if ($1 < 10) { print "FAIL: text-label font-bold leading-none <10 (5 frame x 2 label each)"; exit 1 } else print "PASS text-label class:", $1 }' \
  && grep -c 'text-caption leading-relaxed' cha-bio-safety/docs/redesign-context/27-login/sketch-wave-4-login-form.html | awk '{ if ($1 < 5) { print "FAIL: footer text-caption leading-relaxed <5"; exit 1 } else print "PASS footer class:", $1 }' \
  && grep -c 'bg-surface-sunken' cha-bio-safety/docs/redesign-context/27-login/sketch-wave-4-login-form.html | awk '{ if ($1 < 1) { print "FAIL: bg-surface-sunken disabled <1"; exit 1 } else print "PASS bg-surface-sunken:", $1 }' \
  && grep -cE 'linear-gradient' cha-bio-safety/docs/redesign-context/27-login/sketch-wave-4-login-form.html | awk '{ if ($1 > 0) { print "FAIL: linear-gradient found (그라데이션 폐기 룰 위반)"; exit 1 } else print "PASS no gradient" }' \
  && grep -cE 'status-(safe|fire|warning|danger|caution)' cha-bio-safety/docs/redesign-context/27-login/sketch-wave-4-login-form.html | awk '{ if ($1 > 0) { print "FAIL: status- prefix found"; exit 1 } else print "PASS no status- prefix" }' \
  && grep -cE 'fontSize:\s*(9|10|11)[^0-9]' cha-bio-safety/docs/redesign-context/27-login/sketch-wave-4-login-form.html | awk '{ if ($1 > 0) { print "FAIL: 9/10/11 px found"; exit 1 } else print "PASS no 9-11px" }' \
  && grep -cE 'OQ #1|OQ #3|OQ #4' cha-bio-safety/docs/redesign-context/27-login/sketch-wave-4-login-form.html | awk '{ if ($1 < 3) { print "FAIL: OQ citations <3"; exit 1 } else print "PASS OQ citations:", $1 }' \
  && git log --oneline -1 | grep -q '260521-f01.*W4' && echo "PASS commit W4"
    </automated>
  </verify>
  <done>
    - sketch-wave-4-login-form.html 파일 존재 + ≥400 lines
    - bg-safe-bar / text-text-on-accent (로그인 버튼 default) ≥4 (5 frame 중 disabled 제외 4개)
    - "표시" ≥2 / "숨김" ≥1 (frame 2 활성) / ☎ ≥5 / "031-881-7119" ≥5
    - "초기 비밀번호: 사번 뒤 4자리" / "사번 10자리" / "비밀번호 입력" / "로그인 중..." 각 frame 별 등장
    - `text-label font-bold leading-none` (label class) ≥10 (5 frame x 2 label)
    - `text-caption leading-relaxed` (footer) ≥5
    - `bg-surface-sunken` (disabled) ≥1 (frame 3 loading)
    - linear-gradient 0건 / status- prefix 0건 / 9·10·11px fontSize 0건
    - OQ #1 + #3 + #4 인용 ≥3건
    - atomic commit `docs(quick-260521-f01): ... W4 login form + footer sketch ...` 1개
  </done>
</task>

<task type="auto">
  <name>Task 4 (W5): TSX 변환 verify checklist markdown 생성 + LoginPage.tsx 변경 0 최종 확인</name>
  <files>cha-bio-safety/docs/redesign-context/27-login/wave-5-tsx-conversion-checklist.md</files>
  <action>
    14-reports/wave-7-tsx-conversion-checklist.md 패턴 mirror 로 새 markdown 파일 작성. **sketch 가 아닌 TSX 변환 wave 진입을 위한 verify checklist**. W2~W4 sketch 의 모든 Tailwind class / CSS 토큰 / OQ LOCKED 결정 / negative gate / positive gate / build gate / 자체 verify 명령을 8 섹션으로 박제.

    **frontmatter:**
    ```yaml
    ---
    title: "redesign/27-login — wave 5 (TSX conversion checklist)"
    status: pending  # TSX 변환 wave 시작 전 사용자 컨펌 후 in-progress
    created: 2026-05-21
    quick_id: 260521-f01
    branch: redesign/27-login
    source_tsx: cha-bio-safety/src/pages/LoginPage.tsx
    source_tsx_lines: 220
    sketch_sources:
      - cha-bio-safety/docs/redesign-context/27-login/sketch-wave-2-mobile-shell.html
      - cha-bio-safety/docs/redesign-context/27-login/sketch-wave-3-staff-card-grid.html
      - cha-bio-safety/docs/redesign-context/27-login/sketch-wave-4-login-form.html
    mirror_of: cha-bio-safety/docs/redesign-context/14-reports/wave-7-tsx-conversion-checklist.md
    ---
    ```

    **§1. 변환 범위 + 산출 파일**
    - 단일 파일 in-place 수정: `cha-bio-safety/src/pages/LoginPage.tsx` (220 lines → 약 220~250 lines 예상, Tailwind v0.1.1 className 으로 인라인 style 치환)
    - W2 (모바일 헤더 + 데스크톱 wrapper + 카드 헤더) + W3 (직원 카드 그리드 + isSelected variant) + W4 (로그인 폼 + footer) 3 sketch 결정 → className 1:1 매핑
    - 비즈 로직 0 diff (state/handler/effect/hook 모두 보존)

    **§2. 보존 (LoginPage.tsx 비즈 로직 100% 보존 목록)**
    | line | 항목 | 보존 방식 |
    |---|---|---|
    | 8~19 | `useMediaQuery` hook (window.matchMedia + setMatches + addEventListener/removeEventListener cleanup) | 그대로 |
    | 21~28 | `CARD_COLORS` 6 rgba 상수 | 그대로 (인라인 rgba 유지, OQ #2 LOCKED) |
    | 31~36 | state 6종 (staffId / password / showPw / loading / selected / staffList) | 그대로 |
    | 37 | `pwRef = useRef<HTMLInputElement>(null)` | 그대로 |
    | 38 | `useNavigate()` | 그대로 |
    | 39 | `useAuthStore().login` | 그대로 |
    | 40 | `useMediaQuery('(min-width: 768px)')` 분기 | 그대로 |
    | 42~46 | `useEffect` — fetch `/api/public/staff-list` + setStaffList | 그대로 |
    | 48~53 | `selectStaff` handler — setSelected + setStaffId + setPassword('') + setTimeout pwRef.focus 80ms | 그대로 |
    | 55~72 | `handleSubmit` handler — authApi.login + login(token, staff) + navigate('/dashboard') + toast + catch ApiError + finally setLoading(false) | 그대로 |
    | 88~111 | `staffList.map((s, i) => ...)` map + `s.role === 'admin' ? '관리자' : s.title` 분기 + `s.name.charAt(0)` initial | 그대로 |
    | 139 | `onKeyDown={e => e.key === 'Enter' && handleSubmit()}` | 그대로 |
    | 145 | `setShowPw(v => !v)` | 그대로 |
    | 154 | `disabled={loading}` | 그대로 |

    **§3. 변환 매핑 (영역별 className/토큰/폰트 변환 표 — W2/W3/W4 sketch verbatim 인용)**

    표 1: 영역 2 (모바일 헤더, line 204~213)
    | 현재 (LoginPage.tsx 인라인 style) | 변환 후 (className + 인라인) | sketch 출처 |
    |---|---|---|
    | `<div style={{ background:'var(--bg2)', padding:'16px 20px 24px', borderBottom:'1px solid var(--bd)' }}>` | `<div class="bg-surface-raised border-b border-border-default" style={{ padding:'16px 20px 24px' }}>` | W2 frame 1 |
    | 로고 박스 width:38, height:38, borderRadius:11 | `class="w-[38px] h-[38px] rounded-[11px]"` (OQ #5 LOCKED) | W2 frame 1, 2 |
    | img width:28, height:28, borderRadius:7 | `class="w-[28px] h-[28px] rounded-[7px]"` (OQ #5 LOCKED) | W2 frame 1, 2 |
    | 타이틀 fontSize:16, fontWeight:700, color:'var(--t1)' | `class="text-body font-bold text-text-primary"` | W2 |
    | 서브 fontSize:11, color:'var(--t3)', marginTop:2 | `class="text-caption leading-none text-text-tertiary"` style={{ marginTop:2 }} (OQ #3 LOCKED 11→12) | W2 |

    표 2: 영역 3 (직원 카드 그리드, line 85~115)
    | 현재 | 변환 후 | sketch |
    |---|---|---|
    | 컨테이너 bg:var(--bg2), borderRadius:16, padding:16, marginBottom:12, border 1px var(--bd) | `class="bg-surface-raised border border-border-default rounded-lg" style={{ padding:16, marginBottom:12 }}` | W3 |
    | 라벨 fontSize:10, fontWeight:700, var(--t3), letterSpacing:.06em, uppercase, marginBottom:12 | `class="text-caption font-bold uppercase tracking-wider text-text-tertiary leading-none" style={{ marginBottom:12 }}` (OQ #3 LOCKED 10→12) | W3 |
    | 카드 button 인라인 (반복) — gap:10, padding:10, borderRadius:12, border CARD_COLORS rgba, bg CARD_COLORS rgba | 인라인 유지 (OQ #2 LOCKED) — `style={{ display:'flex', alignItems:'center', gap:10, padding:10, borderRadius:12, border:'1px solid ' + (isSelected ? 'rgba(59,130,246,0.6)' : c.border), background: isSelected ? 'rgba(59,130,246,0.12)' : c.color, cursor:'pointer', textAlign:'left', transition:'all .13s' }}` | W3 |
    | initial 박스 width:34, height:34, borderRadius:10, bg:isSelected?'#2563eb':c.border | `class="w-[34px] h-[34px] rounded-[10px]" style={{ background: isSelected ? '#2563eb' : c.border, ... }}` (OQ #5 LOCKED extension) | W3 |
    | 이름 fontSize:13, fontWeight:700, var(--t1) | `class="text-label font-bold text-text-primary leading-none"` | W3 |
    | role/title fontSize:10, var(--t3) | `class="text-caption text-text-tertiary leading-none"` (OQ #3 LOCKED 10→12) | W3 |

    표 3: 영역 4 (로그인 폼, line 118~165)
    | 현재 | 변환 후 | sketch |
    |---|---|---|
    | 폼 컨테이너 bg:var(--bg2), borderRadius:16, padding:16, border 1px var(--bd) | `class="bg-surface-raised border border-border-default rounded-lg" style={{ padding:16 }}` | W4 |
    | 사번/비밀번호 label fontSize:11, fontWeight:600, var(--t3), display:block, marginBottom:6 | `class="text-label font-bold leading-none text-text-secondary" style={{ display:'block', marginBottom:6 }}` (OQ #3 LOCKED 11→13) | W4 |
    | input inline style (width:100%, padding:12px 14px, borderRadius:12, border 1px var(--bd2), bg var(--bg3), color var(--t1), fontSize:14, outline:none, transition:'border-color .15s') | `class="w-full bg-surface-sunken border border-border-strong rounded-sm text-text-primary" style={{ padding:'12px 14px', fontSize:14, outline:'none', transition:'border-color .15s' }}` (inputStyle 객체 분해) | W4 |
    | show/hide 토글 fontSize:13, color:'var(--t3)', position absolute right:12 top:50% transform translateY(-50%) | `class="text-text-tertiary" style={{ position:'absolute', right:12, top:'50%', transform:'translateY(-50%)', background:'none', border:'none', cursor:'pointer', fontSize:13 }}` + 카피 "표시"/"숨김" (OQ #4 LOCKED) | W4 |
    | 로그인 버튼 background: loading ? 'var(--bg3)' : 'linear-gradient(135deg,#1d4ed8,#2563eb)', color:'#fff', fontSize:14, fontWeight:700, padding:14, borderRadius:12 | `class={loading ? 'bg-surface-sunken text-text-tertiary' : 'bg-safe-bar text-text-on-accent'} className+="font-bold" style={{ padding:14, borderRadius:12, border:'none', fontSize:14, cursor:loading?'not-allowed':'pointer', transition:'all .13s', marginTop:4 }}` (OQ #1 LOCKED — 그라데이션 완전 폐기) | W4 |

    표 4: 영역 5 (footer 안내문, line 167~170)
    | 현재 | 변환 후 | sketch |
    |---|---|---|
    | `<p style={{ textAlign:'center', fontSize:11, color:'var(--t3)', marginTop:20, lineHeight:1.6 }}>` + "초기 비밀번호..." + `<br/>` + "문의: ☎ 031-881-7119" | `<p class="text-caption leading-relaxed text-text-tertiary" style={{ textAlign:'center', marginTop:20 }}>` 카피 그대로 (OQ #3 LOCKED 11→12, OQ #4 LOCKED ☎ 유지) | W4 |

    표 5: 영역 6 (데스크톱 wrapper, line 175~196)
    | 현재 | 변환 후 | sketch |
    |---|---|---|
    | 외곽 minHeight:100dvh, flex center, padding 20, bg:var(--bg) | `class="bg-surface-page" style={{ minHeight:'100dvh', display:'flex', alignItems:'center', justifyContent:'center', padding:'20px' }}` | W2 frame 3, 4 |
    | 카드 maxWidth:420, borderRadius:20, bg:var(--bg2), border, boxShadow 0 8px 32px rgba(0,0,0,0.4), overflow:hidden | `class="bg-surface-raised border border-border-default rounded-[20px]" style={{ maxWidth:420, width:'100%', boxShadow:'0 8px 32px rgba(0,0,0,0.4)', overflow:'hidden' }}` | W2 frame 3, 4 |
    | 카드 헤더 padding:'24px 24px 20px', borderBottom 1px var(--bd) | `class="bg-surface-raised border-b border-border-default" style={{ padding:'24px 24px 20px' }}` | W2 frame 3, 4 |

    **§4. OQ LOCKED 5건 변환 결과 반영 (W1 §7 verbatim + 코드 적용 위치 line 범위)**

    ```
    OQ #1 (그라데이션 폐기 → bg-safe-bar solid):
      - line 157: background: 'linear-gradient(135deg,#1d4ed8,#2563eb)' → className "bg-safe-bar text-text-on-accent"
      - disabled 시 line 157: background:'var(--bg3)' → className "bg-surface-sunken text-text-tertiary"
      - 영향 line: 152~163

    OQ #2 (CARD_COLORS 6 rgba 인라인 유지):
      - line 21~28: CARD_COLORS 상수 그대로
      - line 99~100: border / background 인라인 rgba 유지
      - isSelected accent line 104: '#2563eb' → 인라인 유지 또는 'bg-accent' 토큰 (W3 sketch 단계 시각 결정 → checklist 작성 시점 결과 반영)

    OQ #3 (폰트 격상 verbatim):
      - line 86: fontSize:10 → className "text-caption font-bold uppercase tracking-wider text-text-tertiary leading-none"
      - line 108: fontSize:13 → className "text-label font-bold text-text-primary leading-none"
      - line 109: fontSize:10 → className "text-caption text-text-tertiary leading-none"
      - line 121, 132: fontSize:11 (label) → className "text-label font-bold leading-none text-text-secondary"
      - line 167: fontSize:11 (footer) → className "text-caption leading-relaxed text-text-tertiary"
      - line 187, 211 서브: fontSize:11 → className "text-caption leading-none text-text-tertiary"

    OQ #4 (show/hide 텍스트 + ☎ 유지):
      - line 148: {showPw ? '숨김' : '표시'} 그대로 (Lucide Eye/EyeOff 도입 X)
      - line 169: '문의: 방재팀 내선 ☎ 031-881-7119' ☎ U+260E 유지

    OQ #5 (인라인 px 사이즈 유지):
      - line 182, 206 로고 박스: width:38, height:38, borderRadius:11 → className "w-[38px] h-[38px] rounded-[11px]"
      - line 183, 207 img: width:28, height:28, borderRadius:7 → className "w-[28px] h-[28px] rounded-[7px]"
      - line 104 staff initial 박스: width:34, height:34, borderRadius:10 → className "w-[34px] h-[34px] rounded-[10px]" (OQ #5 LOCKED extension)
    ```

    **§5. negative gate (TSX 변환 후 LoginPage.tsx 가 통과해야 할 grep gate)**

    ```
    # 1. 이모지 0건 (단 ☎ U+260E 1개는 OQ #4 LOCKED 예외, line 169)
    grep -cE '[😀-🛿🤀-🧿🩰-🩴✅✊👍👎❌⛔🔥💥💧⚠️📛]' cha-bio-safety/src/pages/LoginPage.tsx == 0

    # 2. linear-gradient 0건 (OQ #1 LOCKED)
    grep -c 'linear-gradient' cha-bio-safety/src/pages/LoginPage.tsx == 0

    # 3. 9·10·11px 0건 (OQ #3 LOCKED 격상 결과)
    grep -cE 'fontSize:\s*(9|10|11)[^0-9]' cha-bio-safety/src/pages/LoginPage.tsx == 0

    # 4. status- prefix 0건 (memory feedback_tailwind_token_class_pattern)
    grep -cE 'status-(safe|fire|warning|danger|caution)' cha-bio-safety/src/pages/LoginPage.tsx == 0

    # 5. w-8 / h-8 0건 (w-8=48 함정, memory feedback_tailwind_w8_h8_is_48px)
    grep -cE '\bw-8\b|\bh-8\b' cha-bio-safety/src/pages/LoginPage.tsx == 0

    # 6. 옛 alias 토큰 0건 (var(--bg)/var(--bg2)/var(--bg3)/var(--bd)/var(--bd2)/var(--t1)/var(--t2)/var(--t3)/var(--acl)/var(--accent))
    grep -cE 'var\(--(bg|bg2|bg3|bd|bd2|t1|t2|t3|acl|accent)\)' cha-bio-safety/src/pages/LoginPage.tsx == 0

    # 7. 비즈 로직 diff 0 (handler/state/effect 보존)
    git diff HEAD~N cha-bio-safety/src/pages/LoginPage.tsx | grep -E '^[+-]\s*(useState|useEffect|useRef|useNavigate|useAuthStore|authApi|fetch|setStaffId|setPassword|setShowPw|setLoading|setSelected|setStaffList|handleSubmit|selectStaff)' == 0
    # (logic line 의 - 추가 / + 제거 또는 변경이 없어야 함)
    ```

    **§6. positive gate (변환 후 등장해야 할 패턴)**

    ```
    # 1. lucide-react import — 없음 (show/hide 텍스트 유지, 로고는 PNG)
    grep -c "from 'lucide-react'" cha-bio-safety/src/pages/LoginPage.tsx == 0

    # 2. v0.1.1 토큰 class 카운트
    grep -c 'bg-surface-raised' cha-bio-safety/src/pages/LoginPage.tsx >= 3   # 모바일 헤더 + 데스크톱 카드 + 폼 + 카드그리드
    grep -c 'bg-surface-page' cha-bio-safety/src/pages/LoginPage.tsx >= 1
    grep -c 'bg-surface-sunken' cha-bio-safety/src/pages/LoginPage.tsx >= 1   # input
    grep -c 'text-text-primary' cha-bio-safety/src/pages/LoginPage.tsx >= 1
    grep -c 'text-text-tertiary' cha-bio-safety/src/pages/LoginPage.tsx >= 4  # 서브 + 라벨 + role + footer
    grep -c 'border-border-default' cha-bio-safety/src/pages/LoginPage.tsx >= 3
    grep -c 'bg-safe-bar' cha-bio-safety/src/pages/LoginPage.tsx >= 1         # 로그인 버튼 default

    # 3. 폰트 토큰 (OQ #3 LOCKED 격상)
    grep -c 'text-caption' cha-bio-safety/src/pages/LoginPage.tsx >= 4       # 서브 + 라벨 + role/title + footer
    grep -c 'text-label' cha-bio-safety/src/pages/LoginPage.tsx >= 3         # 사번/비밀번호 label + 카드 이름
    grep -c 'text-body' cha-bio-safety/src/pages/LoginPage.tsx >= 2          # 모바일 + 데스크톱 타이틀

    # 4. 인라인 px 사이즈 (OQ #5 LOCKED)
    grep -c 'w-\[38px\]' cha-bio-safety/src/pages/LoginPage.tsx >= 2         # 모바일 + 데스크톱 로고
    grep -c 'w-\[28px\]' cha-bio-safety/src/pages/LoginPage.tsx >= 2         # 모바일 + 데스크톱 img
    grep -c 'w-\[34px\]' cha-bio-safety/src/pages/LoginPage.tsx >= 1         # staff initial
    grep -c 'rounded-\[11px\]' cha-bio-safety/src/pages/LoginPage.tsx >= 2
    grep -c 'rounded-\[7px\]' cha-bio-safety/src/pages/LoginPage.tsx >= 2
    grep -c 'rounded-\[10px\]' cha-bio-safety/src/pages/LoginPage.tsx >= 1
    grep -c 'rounded-\[20px\]' cha-bio-safety/src/pages/LoginPage.tsx >= 1   # 데스크톱 카드

    # 5. ☎ glyph 유지 (OQ #4 LOCKED)
    grep -c '☎' cha-bio-safety/src/pages/LoginPage.tsx >= 1                  # footer line 169
    grep -c '표시' cha-bio-safety/src/pages/LoginPage.tsx >= 1                # show/hide 토글
    grep -c '숨김' cha-bio-safety/src/pages/LoginPage.tsx >= 1
    ```

    **§7. build / tsc gate**

    ```
    cd cha-bio-safety && npx tsc --noEmit                                     # 0 error
    cd cha-bio-safety && npm run build                                        # exit 0
    ```

    **§8. 자체 verify 명령 (TSX 변환 wave 진입 시점 + 완료 시점 양쪽 실행)**

    각 gate 의 grep/wc/git 명령을 fence 안에 verbatim 박제. 8 섹션 별 verify 명령 모두 묶음으로 cat/exit code 체크.

    **본 W5 checklist 가 통과해야 할 자체 gate (W5 markdown 작성 시점):**
    - LoginPage.tsx 변경 0 (`git diff --name-only HEAD -- cha-bio-safety/src/pages/LoginPage.tsx` = empty)
    - 8 섹션 헤더 ≥1 each
    - verify 명령 fence ≥4 (§5, §6, §7, §8)
    - sketch 출처 frontmatter sketch_sources 3개 (W2/W3/W4) 모두 존재 확인 (test -f)

    **commit 메시지:**
    `docs(quick-260521-f01): redesign/27-login W5 TSX conversion checklist (8 sections, sketch sources 3 + verify gates)`

    **commit 명령:**
    `git add cha-bio-safety/docs/redesign-context/27-login/wave-5-tsx-conversion-checklist.md && git commit -m "docs(quick-260521-f01): redesign/27-login W5 TSX conversion checklist (8 sections, sketch sources 3 + verify gates)"`

    **마지막 단계 (commit 후 LoginPage.tsx 변경 0 최종 확인):**
    `git diff --name-only HEAD~4 HEAD -- cha-bio-safety/src/pages/LoginPage.tsx` 의 결과가 빈 줄이어야 함 (최근 4 commit 동안 LoginPage.tsx 변경 0건).
  </action>
  <verify>
    <automated>
test -f cha-bio-safety/docs/redesign-context/27-login/wave-5-tsx-conversion-checklist.md \
  && wc -l cha-bio-safety/docs/redesign-context/27-login/wave-5-tsx-conversion-checklist.md | awk '{ if ($1 < 250) { print "FAIL: too short"; exit 1 } else print "PASS lines:", $1 }' \
  && grep -cE '^# §[1-8]\b|^## §[1-8]\b|^### §[1-8]\b' cha-bio-safety/docs/redesign-context/27-login/wave-5-tsx-conversion-checklist.md | awk '{ if ($1 < 8) { print "FAIL: §1~§8 헤더 <8"; exit 1 } else print "PASS sections:", $1 }' \
  && grep -c '^```' cha-bio-safety/docs/redesign-context/27-login/wave-5-tsx-conversion-checklist.md | awk '{ if ($1 < 8) { print "FAIL: fence <8 (4 verify section x 2 open/close each)"; exit 1 } else print "PASS fences:", $1 }' \
  && grep -cE 'OQ #1|OQ #2|OQ #3|OQ #4|OQ #5' cha-bio-safety/docs/redesign-context/27-login/wave-5-tsx-conversion-checklist.md | awk '{ if ($1 < 5) { print "FAIL: OQ #1~#5 citations <5"; exit 1 } else print "PASS OQ citations:", $1 }' \
  && grep -c 'LoginPage\.tsx' cha-bio-safety/docs/redesign-context/27-login/wave-5-tsx-conversion-checklist.md | awk '{ if ($1 < 10) { print "FAIL: LoginPage.tsx ref <10"; exit 1 } else print "PASS LoginPage refs:", $1 }' \
  && grep -c 'bg-safe-bar' cha-bio-safety/docs/redesign-context/27-login/wave-5-tsx-conversion-checklist.md | awk '{ if ($1 < 2) { print "FAIL: bg-safe-bar citation <2"; exit 1 } else print "PASS bg-safe-bar:", $1 }' \
  && grep -c 'rgba(245,158,11' cha-bio-safety/docs/redesign-context/27-login/wave-5-tsx-conversion-checklist.md | awk '{ if ($1 < 1) { print "FAIL: CARD_COLORS amber missing"; exit 1 } else print "PASS amber:", $1 }' \
  && grep -c 'w-\[38px\]' cha-bio-safety/docs/redesign-context/27-login/wave-5-tsx-conversion-checklist.md | awk '{ if ($1 < 1) { print "FAIL: w-[38px] missing"; exit 1 } else print "PASS w-[38px]:", $1 }' \
  && grep -c 'sketch-wave-2-mobile-shell' cha-bio-safety/docs/redesign-context/27-login/wave-5-tsx-conversion-checklist.md | awk '{ if ($1 < 1) { print "FAIL: sketch W2 ref missing"; exit 1 } else print "PASS W2 ref:", $1 }' \
  && grep -c 'sketch-wave-3-staff-card-grid' cha-bio-safety/docs/redesign-context/27-login/wave-5-tsx-conversion-checklist.md | awk '{ if ($1 < 1) { print "FAIL: sketch W3 ref missing"; exit 1 } else print "PASS W3 ref:", $1 }' \
  && grep -c 'sketch-wave-4-login-form' cha-bio-safety/docs/redesign-context/27-login/wave-5-tsx-conversion-checklist.md | awk '{ if ($1 < 1) { print "FAIL: sketch W4 ref missing"; exit 1 } else print "PASS W4 ref:", $1 }' \
  && (git diff --name-only HEAD~4 HEAD -- cha-bio-safety/src/pages/LoginPage.tsx | wc -l | awk '{ if ($1 != 0) { print "FAIL: LoginPage.tsx changed in last 4 commits"; exit 1 } else print "PASS LoginPage.tsx untouched" }') \
  && (ls cha-bio-safety/docs/redesign-context/27-login/sketch-wave-{2-mobile-shell,3-staff-card-grid,4-login-form}.html 2>/dev/null | wc -l | awk '{ if ($1 != 3) { print "FAIL: 3 sketch files missing"; exit 1 } else print "PASS all 3 sketches present" }') \
  && git log --oneline -1 | grep -q '260521-f01.*W5' && echo "PASS commit W5"
    </automated>
  </verify>
  <done>
    - wave-5-tsx-conversion-checklist.md 파일 존재 + ≥250 lines
    - §1~§8 8 섹션 헤더 모두 존재
    - 자체 verify fence ≥8 (§5/§6/§7/§8 각 명령 fence)
    - OQ #1~#5 5건 모두 인용
    - LoginPage.tsx ref ≥10건 (line 범위 + grep 명령에서 참조)
    - bg-safe-bar / CARD_COLORS amber rgba / w-[38px] / sketch W2/W3/W4 ref 각 ≥1
    - LoginPage.tsx 변경 0 (git diff HEAD~4 HEAD 결과 empty — 4 commit 동안 LoginPage.tsx untouched)
    - 3 sketch HTML 파일 (W2/W3/W4) 모두 존재 확인
    - atomic commit `docs(quick-260521-f01): ... W5 TSX conversion checklist ...` 1개
  </done>
</task>

</tasks>

<verification>
**전체 4 task 종합 verify (executor 가 마지막 task 4 종료 직후 실행):**

```bash
# 1. 4 산출 파일 모두 존재
test -f cha-bio-safety/docs/redesign-context/27-login/sketch-wave-2-mobile-shell.html
test -f cha-bio-safety/docs/redesign-context/27-login/sketch-wave-3-staff-card-grid.html
test -f cha-bio-safety/docs/redesign-context/27-login/sketch-wave-4-login-form.html
test -f cha-bio-safety/docs/redesign-context/27-login/wave-5-tsx-conversion-checklist.md

# 2. LoginPage.tsx 변경 0 (가장 중요)
[[ -z "$(git diff --name-only HEAD~4 HEAD -- cha-bio-safety/src/pages/LoginPage.tsx)" ]] || echo "FAIL: LoginPage.tsx changed"

# 3. 평면 폴더 패턴 — sketch/ 서브폴더 없음
test ! -d cha-bio-safety/docs/redesign-context/27-login/sketch/

# 4. 4 atomic commit 확인
git log --oneline HEAD~4..HEAD | grep -c '260521-f01' | awk '{ if ($1 != 4) { print "FAIL: 4 commit 아님"; exit 1 } else print "PASS 4 commits" }'

# 5. 27-login/ 외 변경 0 (다른 페이지 영향 없음)
[[ -z "$(git diff --name-only HEAD~4 HEAD -- ':!cha-bio-safety/docs/redesign-context/27-login/')" ]] || echo "FAIL: 다른 페이지 변경 있음"
```

각 task 의 자체 verify 가 PASS 한 후 위 종합 verify 가 추가 PASS 해야 quick task 종료. 시각 검수는 cbc7119-preview 자동 배포 후 사용자 별도 컨펌 단계.
</verification>

<success_criteria>
- [ ] 4 산출 파일 모두 27-login/ 직속 평면 배치 (sketch-wave-2/3/4.html + wave-5-tsx-conversion-checklist.md)
- [ ] LoginPage.tsx 코드 변경 0건 (4 commit 동안 git diff empty)
- [ ] OQ #1~#5 LOCKED 결정 5건 모두 4 파일 안에 verbatim 반영
- [ ] 4 negative gate (이모지 except ☎ / linear-gradient / 9·10·11px / status- prefix / w-8 h-8 / 옛 alias) 모두 PASS
- [ ] atomic commit 4개 (`docs(quick-260521-f01): ... W2/W3/W4/W5 ...`)
- [ ] cbc7119-preview 자동 배포 트리거 (main push 시점은 quick task 종료 후 사용자 컨펌 단계)
</success_criteria>

<output>
After completion, create `.planning/quick/260521-f01-redesign-27-login-sketch-waves-w2-w5-4-s/260521-f01-SUMMARY.md` (14-reports / 13-schedule 의 quick SUMMARY 패턴 mirror — 4 task 결과 + verify gate 결과 + 다음 wave 안내).
</output>
