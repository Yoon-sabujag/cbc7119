---
phase: quick
plan: 260403-jg3
type: execute
wave: 1
depends_on: []
files_modified:
  - cha-bio-safety/migrations/0039_cctv_checkpoints.sql
  - cha-bio-safety/src/pages/InspectionPage.tsx
autonomous: true
must_haves:
  truths:
    - "InspectionPage 카테고리 그리드에 CCTV(📹) 항목이 표시된다"
    - "CCTV 선택 시 층/구역 선택 없이 바로 CctvModal이 열린다"
    - "CctvModal에서 DVR 1~12를 좌열(1~6)/우열(7~12) 2열 그리드로 볼 수 있다"
    - "각 DVR별 정상/주의/불량 판정을 선택할 수 있다"
    - "메모+사진 입력 후 저장하면 12개 check_record가 한번에 생성된다"
  artifacts:
    - path: "cha-bio-safety/migrations/0039_cctv_checkpoints.sql"
      provides: "CCTV DVR 1~12 check_points INSERT"
    - path: "cha-bio-safety/src/pages/InspectionPage.tsx"
      provides: "CCTV 카테고리 그룹 + CctvModal 컴포넌트"
  key_links:
    - from: "CctvModal"
      to: "handleSave (page-level)"
      via: "onSave prop — loops 12 DVR checkpoints"
      pattern: "for.*cp.*of.*cctvCPs.*onSave"
---

<objective>
CCTV DVR 점검 기능을 InspectionPage에 추가한다.

Purpose: 방재팀이 B1F 방재센터의 DVR 12대를 한 화면에서 일괄 점검하고 기록할 수 있도록 한다.
Output: DB에 CCTV 체크포인트 12개 + InspectionPage에 CCTV 카테고리 및 CctvModal
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@cha-bio-safety/src/pages/InspectionPage.tsx
@cha-bio-safety/src/types/index.ts

<interfaces>
<!-- InspectionPage의 핵심 패턴 — CctvModal은 이들을 그대로 따른다 -->

CATEGORY_GROUPS 배열 (line ~19): { labels:string[]; icon:string; color:string; border:string; categories:string[] }[]
CCTV 엔트리를 이 배열에 추가한다.

모달 선택 분기 (line ~3345): selectedGroup.categories.includes('CCTV') 조건으로 CctvModal 라우팅

StairwellModal 패턴 (line ~160): CctvModal의 참조 구현
- props: { group, allCheckpoints, records, onClose, onSave }
- onSave 시그니처: (cpId: string, result: CheckResult, memo: string, photoKey?: string) => Promise<void>
- floorResults: Record<string, CheckResult> — checkpoint ID를 key로 결과 매핑
- resultBtnStyle 함수로 정상/주의/불량 버튼 스타일링
- INSPECT_RESULT_OPTIONS 배열로 3개 버튼 렌더
- usePhotoUpload() + PhotoButton 컴포넌트로 사진 처리
- 2열 그리드: gridTemplateColumns:'1fr 1fr'

handleSave (line ~3066):
```typescript
const handleSave = async (cpId: string, result: CheckResult, memo: string, photoKey?: string) => {
  const sid = await ensureSession()
  await inspectionApi.submitRecord(sid, { checkpointId: cpId, result, memo: memo.trim() || undefined, photoKey })
  setRecords(prev => ({ ...prev, [cpId]: result }))
  loadTodayRecords()
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CCTV DVR 체크포인트 마이그레이션</name>
  <files>cha-bio-safety/migrations/0039_cctv_checkpoints.sql</files>
  <action>
check_points 테이블에 CCTV DVR 1~12번을 INSERT하는 마이그레이션을 생성한다.

각 행의 값:
- id: nanoid 형식 16자리 랜덤 hex (lower(hex(randomblob(8))))
- qr_code: 같은 방식의 랜덤 hex
- floor: 'B1'
- zone: 'common'
- category: 'CCTV'
- location: DVR 번호 + 커버리지 (예: 'DVR 1번 (8F, 7F)')
- location_no: 'DVR-01' ~ 'DVR-12' (zero-padded)
- description: 채널 수 (예: '16ch')
- is_active: 1

DVR 데이터:
| DVR | ch | coverage |
|---|---|---|
| 1 | 16ch | 8F, 7F |
| 2 | 16ch | 6F, 5F |
| 3 | 16ch | 5F, 2F |
| 4 | 16ch | 3F |
| 5 | 14ch | 3F, 2F |
| 6 | 15ch | 1F, B1F |
| 7 | 15ch | B1F, B2F |
| 8 | 14ch | B2F~B4F |
| 9 | 14ch | B3F (주차장) |
| 10 | 15ch | B4F (주차장) |
| 11 | 15ch | B5F (주차장) |
| 12 | 8ch | 리서치프라자, 서버실 |

마이그레이션 적용: `npx wrangler d1 execute cha-bio-safety-db --remote --file=cha-bio-safety/migrations/0039_cctv_checkpoints.sql`
  </action>
  <verify>
    <automated>cd /Users/jykevin/Documents/20260328 && npx wrangler d1 execute cha-bio-safety-db --remote --command="SELECT id, location, location_no, description FROM check_points WHERE category='CCTV' ORDER BY location_no" 2>&1 | head -30</automated>
  </verify>
  <done>check_points에 category='CCTV'인 DVR 12개 행이 존재하며 location_no가 DVR-01~DVR-12</done>
</task>

<task type="auto">
  <name>Task 2: InspectionPage에 CCTV 카테고리 + CctvModal 추가</name>
  <files>cha-bio-safety/src/pages/InspectionPage.tsx</files>
  <action>
1. CATEGORY_GROUPS 배열(line ~19)에 CCTV 엔트리 추가:
   ```
   { labels:['CCTV'], icon:'📹', color:'rgba(71,85,105,.12)', border:'rgba(71,85,105,.3)', categories:['CCTV'] }
   ```
   위치: 배연창과 완강기 사이 또는 배열 마지막 — 사용자가 찾기 편한 곳에 배치.

2. CctvModal 함수 컴포넌트 생성 (StairwellModal 패턴 참조):

   Props: `{ allCheckpoints, records, onClose, onSave }` — group prop 불필요 (CCTV 단일 카테고리)

   DVR 정의 상수 (모달 위 또는 안에):
   ```typescript
   const CCTV_DVRS = [
     { no: 'DVR-01', label: 'DVR 1', desc: '8F, 7F' },
     { no: 'DVR-02', label: 'DVR 2', desc: '6F, 5F' },
     { no: 'DVR-03', label: 'DVR 3', desc: '5F, 2F' },
     { no: 'DVR-04', label: 'DVR 4', desc: '3F' },
     { no: 'DVR-05', label: 'DVR 5', desc: '3F, 2F' },
     { no: 'DVR-06', label: 'DVR 6', desc: '1F, B1F' },
     { no: 'DVR-07', label: 'DVR 7', desc: 'B1F, B2F' },
     { no: 'DVR-08', label: 'DVR 8', desc: 'B2F~B4F' },
     { no: 'DVR-09', label: 'DVR 9', desc: 'B3F (주차장)' },
     { no: 'DVR-10', label: 'DVR 10', desc: 'B4F (주차장)' },
     { no: 'DVR-11', label: 'DVR 11', desc: 'B5F (주차장)' },
     { no: 'DVR-12', label: 'DVR 12', desc: '리서치프라자, 서버실' },
   ]
   ```

   State:
   - `dvrResults: Record<string, CheckResult>` — key는 checkpoint ID, 초기값 기존 records에서 로드 또는 'normal'
   - `memo`, `submitting`, `justSaved`, `submitError`, `visible` — StairwellModal과 동일 패턴
   - `usePhotoUpload()` 훅

   초기화 (useEffect): allCheckpoints에서 category='CCTV' 필터링 → CCTV_DVRS 순서로 매칭 (locationNo 기준) → dvrResults 초기화 (기존 records 있으면 로드, 없으면 'normal')

   렌더:
   - 풀스크린 모달 (position:fixed, top:var(--sat), bottom:NAV_BOTTOM)
   - 헤더: 📹 CCTV 점검
   - 스크롤 영역:
     - 2열 그리드 (gridTemplateColumns:'1fr 1fr', gap:8)
     - 왼쪽 열: DVR-01 ~ DVR-06 (CCTV_DVRS[0..5])
     - 오른쪽 열: DVR-07 ~ DVR-12 (CCTV_DVRS[6..11])
     - 각 DVR 카드: DVR 번호(볼드) + 커버리지(서브텍스트) + 정상/주의/불량 3버튼
     - 버튼 스타일: resultBtnStyle 패턴 복사 (StairwellModal line ~226)
   - 메모 + 사진: StairwellModal 패턴 (textarea + PhotoButton)
   - 에러/성공 메시지
   - 하단 바: 닫기 + "CCTV 점검 저장" 버튼

   저장 로직 (handleSave):
   - cctvCPs 배열(allCheckpoints에서 category='CCTV') 순회
   - 각 CP에 대해 onSave(cp.id, dvrResults[cp.id] ?? 'normal', memo, photoKey) 호출
   - 사진은 첫 번째 CP에만 첨부하거나 모든 CP에 동일하게 첨부 (StairwellModal 패턴 따름 — 모든 CP에 동일 memo+photo)

3. 모달 선택 분기 추가 (line ~3345 영역):
   selectedGroup.categories.includes('CCTV') 조건을 기존 분기 체인에 추가:
   ```
   selectedGroup.categories.includes('CCTV') ? (
     <CctvModal
       allCheckpoints={allCheckpoints}
       records={records}
       onClose={() => setSelectedGroupIdx(null)}
       onSave={handleSave}
     />
   ) :
   ```

CCTV는 계단실 선택 단계가 없으므로 StairwellModal보다 단순 — 모달 열리면 바로 12개 DVR이 2열로 표시됨.
  </action>
  <verify>
    <automated>cd /Users/jykevin/Documents/20260328/cha-bio-safety && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>InspectionPage에 CCTV 카테고리 카드가 표시되고, 선택 시 CctvModal이 열리며, DVR 12개가 2열 그리드로 표시되고 점검 결과 저장이 가능하다. TypeScript 컴파일 에러 없음.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — TypeScript 에러 없음
2. D1 remote에서 `SELECT count(*) FROM check_points WHERE category='CCTV'` → 12
3. 로컬 dev 서버에서 점검 페이지 → CCTV 카드 표시 확인 → 클릭 시 CctvModal 열림 → DVR 2열 그리드 표시 → 저장 동작
</verification>

<success_criteria>
- check_points에 CCTV DVR 12개 체크포인트 존재
- InspectionPage 카테고리 그리드에 CCTV(📹) 표시
- CCTV 클릭 → 바로 CctvModal 표시 (층/구역 선택 건너뜀)
- DVR 1~6 좌열 / DVR 7~12 우열 2열 그리드
- 각 DVR별 정상/주의/불량 선택 가능
- 메모+사진 입력 후 저장 → 12개 check_record 생성
</success_criteria>

<output>
After completion, create `.planning/quick/260403-jg3-cctv-dvr/260403-jg3-SUMMARY.md`
</output>
