---
phase: quick
plan: 01
type: execute
wave: 4
quick_id: 260517-e9e
depends_on: [260517-e9c, 260517-e9d, 260517-e9r]
files_modified:
  - cha-bio-safety/docs/redesign-context/09-extinguishers/sketch/edit-modal-sketch.html
---

<objective>
EditModal (l.938-1061) 의 v0.1.1 토큰 sketch. wave 3 의 공통 modal styles 그대로 재사용 + EditModal 고유 요소 (counter chip / borderForField / microcopy / disabled red-bordered).

Edit 만의 변형 요소:
- 헤더에 counter chip "변경: N / 3" 노출
- 각 필드 borderColor 분기: 원본과 다르면 accent / 같으면 border-strong
- 종류 grid 의 isChanged 표시: 1.5px accent border (active 이고 원본과 다른 경우)
- changedCount > 3 시 microcopy (danger 텍스트)
- save disabled 톤 (border-strong bg + text-tertiary + danger-bar 보더)
- saving=true 라벨 "저장 중…"
</objective>

<context>

# Counter chip 3 톤 (l.961-967)

| changedCount | bg | color |
|---|---|---|
| 0 | `--surface-sunken` | `--text-tertiary` |
| 1~3 | `--accent-bg` | `--accent` |
| ≥4 | `--status-danger-bg` | `--status-danger` |

- padding 2/8 + radius-sm + fontSize 12 + fontWeight 700

# borderForField (l.984-985)

```js
borderForField(original, current) = current !== original ? '--accent' : '--border-strong'
```

- 변경된 필드: 1px accent
- 동일: 1px border-strong (default 모달 input)

# 종류 grid 변경 표시 (l.1010-1014)

- isActive + 원본과 동일: bg accent + accent-fg + border none
- isActive + 원본과 다름 (변경): bg accent + accent-fg + 1.5px accent border (강조)
- inactive: bg surface-sunken + text-secondary + 1px border-default

# Microcopy (l.997-1001, changedCount > 3 시 노출)

- fontSize 12 / color status-danger / marginBottom 12
- 텍스트: "4개 이상 변경하려면 「폐기 후 재등록」을 사용하세요."

# Save disabled (saveDisabled = changedCount > 3 || === 0)

- bg `var(--bd2)` → `--border-strong`
- color `var(--t3)` → `--text-tertiary`
- border `rgba(239,68,68,.3)` → `--status-danger-bar` 1px (위험성 시각 강조)
- cursor not-allowed

# saving 분기

- saving=true: 라벨 "저장 중…" (saveDisabled 톤 유지)
- saving=false: 라벨 "저장"

# Viewport 매트릭스 (5개)

| ID | 상태 | 핵심 |
|---|---|---|
| V1 | 모바일 — 변경 0 | counter "0 / 3" 회색 + save disabled (변경 없음) |
| V2 | 모바일 — 변경 2 (제조업체, 제조년월) | counter "2 / 3" accent + 변경 필드 accent border + save enabled |
| V3 | 모바일 — 변경 4 (over) | counter "4 / 3" danger + microcopy 노출 + save disabled (red-bordered) |
| V4 | 모바일 — 저장 중 (saving=true, 변경 2) | counter accent + save disabled + "저장 중…" 라벨 |
| V5 | 데스크톱 — 변경 2 | 모달 사이즈 동일, backdrop overlay 강조 |

</context>

<tasks>
<task type="auto">
  <name>Task 1: edit-modal-sketch.html 작성</name>
  <files>cha-bio-safety/docs/redesign-context/09-extinguishers/sketch/edit-modal-sketch.html</files>
  <action>
1. :root 정의 — wave 3 와 동일 토큰 set (이미 surface-overlay / radius-lg 정의됨)
2. 5 viewport 모두 EditModal 마크업 + counter chip + borderForField + microcopy
3. rule 박스 마지막 (적용 룰 + 금지 룰 + 비즈니스 로직 + viewport 매트릭스)
  </action>
</task>
</tasks>
