---
phase: quick-260428-id1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [DATA-RECOVER-CP-B1-6-BC]
must_haves:
  truths:
    - "B1 비상콘센트 CP-B1-6-BC 의 4월 정상 점검 기록이 production D1 에 1건 존재한다"
    - "그 기록의 session_id / staff_id / result / checked_at 가 짝꿍 소화전 CP-B1-6-SH (2026-04-27 14:44:43, 박보융 2023071752, normal) 와 동일하다"
    - "INSERT 후 cha-bio-db 에서 (소화전 + 비상콘센트) 4월 strict 완료 카운트가 정확히 154 이다"
    - "기존 SH 기록은 변경되지 않았다 (read-only 검증만)"
    - "DB 외 다른 영역(코드, R2, telemetry) 은 건드리지 않았다 — 데이터 회복 단일 책임"
  artifacts:
    - path: ".planning/quick/260428-id1-recover-cp-b1-6-bc-april/RESULT.md"
      provides: "INSERT 전/후 SELECT 결과 + 새 record id 기록"
      contains: "CP-B1-6-BC"
  key_links:
    - from: "check_records (CP-B1-6-BC, 2026-04-27)"
      to: "check_records (CP-B1-6-SH, 2026-04-27 14:44:43)"
      via: "동일 session_id + staff_id + result + checked_at"
      pattern: "session_id 동일 매칭"
    - from: "InspectionPage 카드 카운트"
      to: "computeCardCompletion(monthRecordDates)"
      via: "BC 4월 normal 기록이 monthRecords 에 포함되어 153→154"
      pattern: "src/utils/inspectionProgress.ts"
---

<objective>
B1 비상콘센트 `CP-B1-6-BC` 의 4월 점검 기록 누락(153/154 표기) 을 production D1 직접 INSERT 로 회복한다.

Purpose:
- 디버그 세션 (`.planning/debug/hydrant-count-mismatch.md`) 에서 진짜 미점검 1건이 `CP-B1-6-BC` 임을 확정.
- 사용자 명시 승인: "비상콘센트도 소화전이랑 같은 시점에 점검한 걸로 해줘" — 짝꿍 SH (2026-04-27 14:44:43, 박보융 2023071752, normal) 와 동일 (session_id, staff_id, result, checked_at) 로 BC 1건 INSERT.
- CLAUDE.md 데이터 무결성 원칙: 점검 기록 삭제 불가. 본 작업은 **INSERT only** — 수정/삭제 없음.

Output:
- `cha-bio-db.check_records` 에 CP-B1-6-BC 신규 row 1건
- `.planning/quick/260428-id1-recover-cp-b1-6-bc-april/RESULT.md` 에 전/후 SELECT 결과 기록
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/debug/hydrant-count-mismatch.md
@CLAUDE.md

<interfaces>
<!-- check_records 스키마 (migrations 0001 + 0012 + 0013 + records.ts 핸들러로부터 추출) -->

테이블: check_records
필수 컬럼:
- id            TEXT PRIMARY KEY                                   -- nanoid(21) 형식, prefix 없음
- session_id    TEXT NOT NULL → inspection_sessions(id)
- checkpoint_id TEXT NOT NULL → check_points(id)                   -- ⚠️ check_point_id 아님
- staff_id      TEXT NOT NULL → staff(id)
- result        TEXT NOT NULL CHECK IN ('normal','caution','bad','unresolved','missing')
- memo          TEXT (nullable)
- photo_key     TEXT (nullable)                                    -- R2 object key
- checked_at    TEXT NOT NULL DEFAULT datetime('now')
- created_at    TEXT NOT NULL DEFAULT datetime('now')

추가 컬럼 (later migrations):
- status              TEXT NOT NULL DEFAULT 'open'                  -- 0012
- resolution_memo     TEXT                                          -- 0012
- resolved_at         TEXT                                          -- 0012
- resolved_by         TEXT                                          -- 0012
- resolution_photo_key TEXT                                         -- 0013
- guide_light_type    TEXT                                          -- 유도등 전용
- floor_plan_marker_id TEXT                                         -- 평면도 마커 전용

⚠️ 주의 사항 (사용자 task spec 의 오해 정정):
- check_records 에 `is_active` 컬럼은 **존재하지 않는다** (is_active 는 check_points 테이블에만 있음).
  → 멱등성 SELECT 에서 `is_active=1` 조건 사용 금지.
- record id 에 `record-` prefix 는 없다. 핸들러는 `nanoid(21)` 만 사용 (대소문자 영숫자 21자).

핸들러 INSERT 패턴 (functions/api/inspections/[sessionId]/records.ts:42-45):
```sql
INSERT INTO check_records
  (id, session_id, checkpoint_id, staff_id, result, memo, photo_key,
   guide_light_type, floor_plan_marker_id, checked_at, created_at)
VALUES (?,?,?,?,?,?,?,?,?,
        datetime('now','+9 hours'), datetime('now','+9 hours'));
```
→ 본 작업은 사용자 승인대로 SH 와 **동일 timestamp** 사용. `datetime('now',...)` 대신 SH 의 checked_at 리터럴을 그대로 박는다.

BC 분기: CP-B1-6-BC 는 비상콘센트 (유도등 X, 평면도 마커 X — debug evidence 14:12).
→ guide_light_type=NULL, floor_plan_marker_id=NULL.

CP-B1-6-SH 짝꿍 사실 (debug evidence 14:08, 14:10):
- session_id: B3cQQD0JNBgcozbWQminL
- staff_id:   2023071752 (박보융)
- result:     normal
- checked_at: 2026-04-27 14:44:43
- (정확값은 Task 1 SELECT 결과로 재확인 후 Task 3 에서 그대로 박을 것)
</interfaces>

도구:
- D1 production 실행: `cd /Users/jykevin/Documents/20260328/cha-bio-safety && npx wrangler d1 execute cha-bio-db --remote --command "..."`
- DB 이름: cha-bio-db
- DB id: b12b88e7-fc41-4186-8f35-ee9cbaf994c7

원칙:
- INSERT only — 기존 row 수정/삭제 금지.
- 코드 변경 없음. 배포 없음. 빌드 없음.
- Task 1, 2 결과를 사용자에게 보여주고 사용자 승인(checkpoint) 후 Task 3 INSERT.
- CLAUDE.md "디자인/데이터 변경 전 상의" 규칙 적용 — INSERT 직전 사용자 명시 승인 재확인.
</context>

<tasks>

<task type="auto">
  <name>Task 1: SH 짝꿍 정확값 SELECT + BC 부재(멱등성) 검증</name>
  <files>.planning/quick/260428-id1-recover-cp-b1-6-bc-april/RESULT.md</files>
  <action>
    Production D1 에 read-only 두 SELECT 를 순서대로 실행한다. 결과를 RESULT.md 에 timestamp 와 함께 기록한다.

    1) SH 정확값 조회 (Task 3 에서 그대로 박을 베이스):
       ```bash
       cd /Users/jykevin/Documents/20260328/cha-bio-safety && \
       npx wrangler d1 execute cha-bio-db --remote --command "SELECT id, session_id, checkpoint_id, staff_id, result, checked_at, photo_key, memo FROM check_records WHERE checkpoint_id = 'CP-B1-6-SH' AND checked_at LIKE '2026-04-27%' ORDER BY checked_at DESC;"
       ```
       기대: 정확히 1행. session_id / staff_id / result / checked_at 메모.
       만약 0행 또는 2행 이상이면 즉시 abort 하고 사용자에게 보고 (사전 가정과 다름).

    2) BC 4월 부재 확인 (멱등성 — 이미 INSERT 됐다면 재실행 금지):
       ```bash
       cd /Users/jykevin/Documents/20260328/cha-bio-safety && \
       npx wrangler d1 execute cha-bio-db --remote --command "SELECT COUNT(*) AS cnt FROM check_records WHERE checkpoint_id = 'CP-B1-6-BC' AND substr(checked_at, 1, 7) = '2026-04';"
       ```
       기대: cnt=0. cnt>=1 이면 즉시 abort 하고 RESULT.md 에 "이미 회복됨" 기록.

    3) 보너스 — 현재 4월 카운트 baseline 기록 (Task 4 에서 비교용):
       ```bash
       cd /Users/jykevin/Documents/20260328/cha-bio-safety && \
       npx wrangler d1 execute cha-bio-db --remote --command "SELECT (SELECT COUNT(*) FROM check_records cr JOIN check_points cp ON cr.checkpoint_id=cp.id WHERE cp.category IN ('소화전','비상콘센트') AND substr(cr.checked_at,1,7)='2026-04' AND cr.result IN ('normal','caution')) AS strict_complete, (SELECT COUNT(*) FROM check_points WHERE category IN ('소화전','비상콘센트') AND is_active=1) AS total_cps;"
       ```
       기대 baseline: strict_complete=153, total_cps=154 (debug 세션 기준).

    RESULT.md 에 세 SELECT 의 raw 결과 + 추출된 SH 의 (session_id, staff_id, result, checked_at) 를 명시 기록.
  </action>
  <verify>
    <automated>test -f .planning/quick/260428-id1-recover-cp-b1-6-bc-april/RESULT.md &amp;&amp; grep -q "CP-B1-6-SH" .planning/quick/260428-id1-recover-cp-b1-6-bc-april/RESULT.md &amp;&amp; grep -qE "cnt[^0-9]*0|count.*0" .planning/quick/260428-id1-recover-cp-b1-6-bc-april/RESULT.md</automated>
  </verify>
  <done>
    RESULT.md 에 SH 1행 정확값 + BC 4월 cnt=0 + baseline 카운트 153/154 모두 기록됨. 다음 Task 진행 가능 상태.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Task 1 SELECT 결과 (RESULT.md). INSERT 전 사용자 최종 확인.
  </what-built>
  <how-to-verify>
    RESULT.md 를 사용자에게 보여주고 다음 3가지 명시 확인:

    1. SH 정확값이 디버그 세션 가설과 일치하는가?
       - checkpoint_id='CP-B1-6-SH'
       - checked_at='2026-04-27 14:44:43' (또는 근접한 14:44:xx)
       - staff_id='2023071752' (박보융)
       - result='normal'

    2. BC 4월 cnt=0 인가? (cnt>=1 이면 INSERT 절대 금지 — 이미 회복된 상태)

    3. INSERT 진행해도 되는가? CLAUDE.md "디자인/데이터 변경 전 상의" 규칙에 따라 데이터 변경 직전 명시 승인 필요.

    사용자 답변 옵션:
    - "INSERT 진행" → Task 3 실행
    - "중단" → 작업 종료, RESULT.md 에 사유 기록
    - "값 수정 필요" → 사용자 지시대로 Task 3 의 INSERT 값 조정
  </how-to-verify>
  <resume-signal>"INSERT 진행" 또는 수정 지시 또는 "중단"</resume-signal>
</task>

<task type="auto">
  <name>Task 3: CP-B1-6-BC INSERT + 직후 검증 SELECT</name>
  <files>.planning/quick/260428-id1-recover-cp-b1-6-bc-april/RESULT.md</files>
  <action>
    Task 1 에서 추출한 SH 의 정확값(`<SH_SESSION>`, `<SH_STAFF>`, `<SH_RESULT>`, `<SH_CHECKED_AT>`) 을 그대로 사용해 BC 1건 INSERT.

    사전 준비:
    - 새 record id 생성: `node -e "const c='ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789'; const a=require('crypto').getRandomValues(new Uint8Array(21)); console.log(Array.from(a,b=>c[b%c.length]).join(''))"`
      → 21자 nanoid 출력. RESULT.md 에 `<NEW_BC_ID>` 로 기록.
      (참고: functions/api/inspections/[sessionId]/records.ts:3 의 nanoid 와 동일 알고리즘. prefix 없음.)

    INSERT 실행:
    ```bash
    cd /Users/jykevin/Documents/20260328/cha-bio-safety && \
    npx wrangler d1 execute cha-bio-db --remote --command "INSERT INTO check_records (id, session_id, checkpoint_id, staff_id, result, memo, photo_key, guide_light_type, floor_plan_marker_id, checked_at, created_at) VALUES ('<NEW_BC_ID>', '<SH_SESSION>', 'CP-B1-6-BC', '<SH_STAFF>', '<SH_RESULT>', NULL, NULL, NULL, NULL, '<SH_CHECKED_AT>', '<SH_CHECKED_AT>');"
    ```
    주의:
    - `checkpoint_id` (under-score 한 단어!) — `check_point_id` 아님.
    - photo_key, memo, guide_light_type, floor_plan_marker_id 모두 NULL.
    - status / resolution_* / resolved_* 컬럼은 INSERT 명시 안 함 → DEFAULT 적용 (status='open' 등).
    - checked_at = created_at = SH 의 checked_at 그대로 (사용자 승인: "같은 시점에 점검한 걸로").
    - is_active 컬럼은 check_records 에 없음 → INSERT 컬럼 리스트에 포함 금지.

    INSERT 직후 즉시 단건 검증 SELECT:
    ```bash
    cd /Users/jykevin/Documents/20260328/cha-bio-safety && \
    npx wrangler d1 execute cha-bio-db --remote --command "SELECT id, session_id, checkpoint_id, staff_id, result, checked_at FROM check_records WHERE checkpoint_id='CP-B1-6-BC' AND substr(checked_at,1,7)='2026-04';"
    ```
    기대: 1행. id=`<NEW_BC_ID>`, 다른 4개 필드 SH 와 동일.

    INSERT 가 D1 error 로 실패하면:
    - "no such column" → 컬럼명 오타 (가장 가능성 큰 원인: check_point_id vs checkpoint_id)
    - "FOREIGN KEY constraint failed" → session_id/staff_id 가 잘못 박힘 → Task 1 결과 재확인
    - "CHECK constraint failed" → result 가 enum 외 값 → 정확히 'normal' 인지 확인

    실패 시 RESULT.md 에 에러 메시지 그대로 기록 후 사용자에게 보고 (재시도 금지).
  </action>
  <verify>
    <automated>cd /Users/jykevin/Documents/20260328/cha-bio-safety &amp;&amp; npx wrangler d1 execute cha-bio-db --remote --command "SELECT COUNT(*) AS cnt FROM check_records WHERE checkpoint_id='CP-B1-6-BC' AND substr(checked_at,1,7)='2026-04';" 2>&amp;1 | grep -qE "cnt[^0-9]*1|\\| 1 \\|"</automated>
  </verify>
  <done>
    BC 4월 행 1건 존재. RESULT.md 에 INSERT 명령 전체 + 새 id + 검증 SELECT 결과 기록.
  </done>
</task>

<task type="auto">
  <name>Task 4: 카드 카운트 회복 검증 (154/154)</name>
  <files>.planning/quick/260428-id1-recover-cp-b1-6-bc-april/RESULT.md</files>
  <action>
    카드 카운트가 정확히 154/154 로 회복됐는지 production D1 에서 최종 검증한다.

    1) 그룹 strict 완료 카운트:
       ```bash
       cd /Users/jykevin/Documents/20260328/cha-bio-safety && \
       npx wrangler d1 execute cha-bio-db --remote --command "SELECT (SELECT COUNT(*) FROM check_records cr JOIN check_points cp ON cr.checkpoint_id=cp.id WHERE cp.category IN ('소화전','비상콘센트') AND substr(cr.checked_at,1,7)='2026-04' AND cr.result IN ('normal','caution')) AS strict_complete, (SELECT COUNT(*) FROM check_points WHERE category IN ('소화전','비상콘센트') AND is_active=1) AS total_cps;"
       ```
       기대: strict_complete=154, total_cps=154.

    2) 짝꿍 페어 무결성 — B1F-6 SH/BC 둘 다 4월 normal 인지:
       ```bash
       cd /Users/jykevin/Documents/20260328/cha-bio-safety && \
       npx wrangler d1 execute cha-bio-db --remote --command "SELECT checkpoint_id, staff_id, result, checked_at FROM check_records WHERE checkpoint_id IN ('CP-B1-6-SH','CP-B1-6-BC') AND substr(checked_at,1,7)='2026-04' ORDER BY checkpoint_id;"
       ```
       기대: 2행. SH 와 BC 모두 staff_id=2023071752, result='normal', checked_at 동일.

    3) RESULT.md 최종 정리:
       - "## 결과" 섹션 추가
       - baseline (153) → after (154) 비교표
       - INSERT 된 row id 명시
       - 다음 사용자 액션: "PWA 앱 reload (또는 splash 버전 캐시 무효화 후 재진입) → 일반점검 페이지에서 소화전·비상콘센트 카드가 154/154 인지 시각 확인"
       - CLAUDE.md `feedback_pwa_cache_invalidation.md` 메모리 적용: SW 캐시로 인해 즉시 안 보일 수 있음 → 앱 재설치 또는 강제 새로고침 안내.

    실패 시 (strict_complete!=154 또는 페어가 2행이 아님) RESULT.md 에 raw 결과 그대로 기록 후 사용자에게 보고. 추가 INSERT/UPDATE 시도 금지.
  </action>
  <verify>
    <automated>cd /Users/jykevin/Documents/20260328/cha-bio-safety &amp;&amp; npx wrangler d1 execute cha-bio-db --remote --command "SELECT COUNT(*) AS c FROM check_records cr JOIN check_points cp ON cr.checkpoint_id=cp.id WHERE cp.category IN ('소화전','비상콘센트') AND substr(cr.checked_at,1,7)='2026-04' AND cr.result IN ('normal','caution');" 2>&amp;1 | grep -qE "c[^0-9]*154|\\| 154 \\|"</automated>
  </verify>
  <done>
    Production D1 에서 4월 (소화전+비상콘센트) strict 완료 = 154 = 전체 CP 수. RESULT.md 에 baseline/after 비교 + 사용자 시각 확인 안내 기록.
  </done>
</task>

</tasks>

<verification>
- BC 4월 row 1건 존재 (Task 3 verify)
- 그룹 strict 완료 카운트 정확히 154 (Task 4 verify)
- B1F-6 SH/BC 페어가 동일 (session_id, staff_id, result, checked_at) 로 묶임
- RESULT.md 에 전 과정 SQL/결과/새 id 모두 기록됨
- 코드/배포/스키마 변경 0건 (데이터만 회복)
</verification>

<success_criteria>
- [x] Task 1 SH 정확값 + BC 4월 cnt=0 확인
- [x] 사용자 INSERT 승인 (Task 2 checkpoint)
- [x] Task 3 INSERT 성공 + 단건 검증 SELECT 1행
- [x] Task 4 카운트 154/154 + SH/BC 페어 무결성 확인
- [x] RESULT.md 에 baseline/after 기록 + PWA 캐시 안내
- [x] FloorPlanPage paired BC 갭 (debug 세션 결정 root_cause #1) 은 본 quick 의 책임 아님 — 별도 후속 작업으로 남김
</success_criteria>

<output>
완료 후 `.planning/quick/260428-id1-recover-cp-b1-6-bc-april/RESULT.md` 가 다음을 포함해야 한다:

```markdown
# RESULT: CP-B1-6-BC 4월 점검 기록 회복

## Baseline (Task 1)
- SH 정확값 SELECT 결과 (raw)
- BC 4월 cnt SELECT 결과 (raw)
- 그룹 카운트 baseline: 153/154

## INSERT (Task 3)
- 새 record id: <NEW_BC_ID>
- 실행 SQL (전체)
- 단건 검증 SELECT 결과

## After (Task 4)
- 그룹 카운트: 154/154
- SH/BC 페어 SELECT 결과
- 비교표 (전/후)

## 사용자 액션
- PWA 앱 reload 또는 재설치 (SW 캐시 무효화)
- 일반점검 페이지에서 소화전·비상콘센트 카드 154/154 시각 확인

## 후속 (out-of-scope, 별도 quick)
- FloorPlanPage paired BC 저장 로직 추가 (debug 세션 fix B)
- InspectionPage picker 가시성 개선 (debug 세션 fix C)
```

별도 SUMMARY.md 는 quick 모드라서 생성 안 함 — RESULT.md 가 산출물.
</output>
