---
phase: quick-260601-n1s
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cha-bio-safety/scripts/check-emoji.cjs
  - cha-bio-safety/src/pages/HandoverPage.tsx
  - cha-bio-safety/src/pages/WorkListPage.tsx
autonomous: true
requirements: [DESKTOP-HANDOVER, DESKTOP-WORKLIST]

must_haves:
  truths:
    - "데스크톱(≥1024px)에서 인수 인계장이 카드 그리드 + 중앙 상세 모달로 표시된다"
    - "데스크톱(≥1024px)에서 업무 관련 리스트가 탭/검색/마스킹 토글 툴바 + 테이블로 표시된다"
    - "모바일(<1024px) 두 페이지의 렌더 결과는 변경 전과 100% 동일하다"
    - "데스크톱은 ComposeModal/HistoryModal/WorkListHistoryModal, 기존 useQuery/useMutation, 본인가드, 마스킹, 소프트삭제, 타임머신을 그대로 재사용한다"
    - "두 파일 모두 lucide 아이콘만 쓰며 렌더 이모지가 0개다"
    - "tsc 통과 + vite build 통과"
  artifacts:
    - path: "cha-bio-safety/scripts/check-emoji.cjs"
      provides: "렌더 이모지/딩벳 검사 도구 (한글·주석 화살표 제외)"
    - path: "cha-bio-safety/src/pages/HandoverPage.tsx"
      provides: "if(isDesktop) 데스크톱 분기 + DesktopHandover 카드 그리드/상세 모달"
      contains: "useIsDesktop"
    - path: "cha-bio-safety/src/pages/WorkListPage.tsx"
      provides: "if(isDesktop) 데스크톱 분기 + DesktopWorkList 테이블"
      contains: "useIsDesktop"
  key_links:
    - from: "HandoverPage 데스크톱 카드 클릭"
      to: "중앙 상세 모달"
      via: "selectedId 상태"
      pattern: "DesktopHandover|selected"
    - from: "데스크톱 작성/수정"
      to: "기존 ComposeModal"
      via: "composeOpen / editingId 재사용"
      pattern: "ComposeModal"
    - from: "WorkList 비밀번호 셀"
      to: "마스킹 표시"
      via: "showValue = revealAll || revealed || tab==='contact'"
      pattern: "revealAll"
---

<objective>
인수 인계장(HandoverPage)과 업무 관련 리스트(WorkListPage)에 데스크톱(≥1024px) 레이아웃 분기를 신규 추가한다. 사용자가 컨펌한 시안(`/tmp/desktop-handover-worklist-sketch.html`)을 두 가지 수정 반영(인계장 상세 모달에 별도 제목칸 없음 / 모든 아이콘 lucide 그대로)하여 구현한다.

Purpose: PC(1920x1080) 사용 시 두 페이지가 모바일 단일 컬럼으로만 보여 화면을 비효율적으로 쓴다. 데스크톱 전용 그리드/테이블 레이아웃으로 한눈에 보이게 한다.
Output: 두 페이지 파일에 `if (isDesktop) return <데스크톱>` 분기 추가. 모바일 무변경. 새 API/쿼리키/mutation/모달 0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md

<!-- 데스크톱 분기 훅 (1024px) -->
@cha-bio-safety/src/hooks/useIsDesktop.ts

<!-- 데스크톱 카드 그리드 + 마스터/디테일 참고 (패턴만 차용, 복붙 금지) -->
@cha-bio-safety/src/pages/InspectionPage.tsx
@cha-bio-safety/src/pages/ElevatorPage.tsx

<!-- 수정 대상 (현재 모바일 단일 레이아웃) -->
@cha-bio-safety/src/pages/HandoverPage.tsx
@cha-bio-safety/src/pages/WorkListPage.tsx

<interfaces>
<!-- 확인된 사실 (executor 는 추가 탐색 없이 사용) -->

데스크톱 패턴 참고 위치:
- InspectionPage.tsx line 5753~: `DesktopInspectionView`. 카드 그리드 =
  `grid grid-cols-[repeat(auto-fill,minmax(170px,1fr))] gap-2.5`, 카드 hover =
  `hover:border-border-strong hover:-translate-y-px`, 좌측 상태바 =
  `absolute left-0 top-0 bottom-0 w-[3px] {barClass}`, 컨테이너 = `bg-surface-raised border rounded-md`.
- ElevatorPage.tsx line 527: `if (isDesktop) { return (...) }` 패턴.
  line 603: 데스크톱 헤더 `h-14 bg-surface-raised border-b border-border-default px-5 flex items-center gap-2.5`.
  line 629~: 본문 50/50 (`flex-1 min-h-0 flex overflow-hidden`).

App.tsx 라우팅/헤더 (변경 금지):
- 라우트 `/handovers`→HandoverPage, `/work-list`→WorkListPage 이미 존재.
- `DESKTOP_HEADER_HIDE_PATHS = ['/elevator', '/div', '/floorplan', '/workshift']`
  → 두 경로는 여기 **없음** = 데스크톱에서 App 의 54px 헤더(제목 '인수 인계장'/'업무 관련 리스트')
    + 280px 사이드바가 이미 렌더됨.
  → DESKTOP_HEADER_HIDE_PATHS 에 두 경로 **추가 금지** (이중 헤더 방지).
  → 데스크톱 페이지 본문은 "페이지 제목 헤더"를 다시 그리지 말 것.
    본문 안 툴바는 검색/탭/추가/마스킹토글 컨트롤만 (타이틀 텍스트 X).
- App `<main>` 이 `flex flex-col overflow:hidden` 컨테이너를 제공하므로
  데스크톱 루트는 `flex-1 min-h-0 flex flex-col` 류로 그 안을 채운다.

tailwind.config.js spacing override (w-8/h-8 함정):
- `w-8/h-8 = 48px`, `w-7/h-7 = 32px`. 버튼/아이콘 박스는 의도한 px 확인 후 사용.
- 사용 가능 semantic color 토큰(다크·라이트 양립): bg-surface-page / bg-surface-raised /
  bg-surface-sunken / bg-surface-active / border-border-default / border-border-strong /
  text-text-primary / text-text-secondary / text-text-tertiary / bg-accent / text-text-on-accent /
  bg-safe-bar / bg-warning-bar / text-status-danger.

HandoverPage 재사용 대상 (현 파일 내 정의, signature 그대로):
- `useQuery(['handovers', tab, searchQ, showDeleted])` → `items: HandoverItem[]`
- `HandoverItem`: { id, title, body, status:'waiting'|'done', pinned, staffId, staffName,
  createdAt, updatedAt, deletedAt, attachmentCount }
- 상세 fetch: `useQuery(['handover-detail', id], ()=>handoverApi.get(id))` → `HandoverDetail.attachments[]`
  ({ id, storageKey, contentType, filename }). URL = `/api/uploads/${a.storageKey}`.
- mutation (HandoverCard 내 정의): updateMutation = handoverApi.update(id, patch) —
  patch 로 `{ pinned }`, `{ status:'done'|'waiting' }`, `{ title, body }`. deleteMutation = handoverApi.delete.
- `ComposeModal({ onClose, onSaved })` — 새 글 작성. 인라인 수정은 `editingId` 패턴.
- `HistoryModal({ handoverId, onClose })` — 타임머신(이력 + 복원 RotateCcw).
- 본인가드: `isMine = !!me && h.staffId === me.id` (me = useAuthStore(s=>s.staff)).
- 고정 비활성 룰: `disabled={h.status==='done' && !h.pinned}`.
- 상태 표시: done → 초록 '✓ 완료' / waiting → accent '대기'.
  **데스크톱에선 이모지 ✓(U+2713) 대신 lucide `<Check />` + '완료' 텍스트로.**
- `fmtTime(iso)` 헬퍼(파일 상단) 재사용.
- import 아이콘: Pin, Check, RotateCcw, Search, Plus, Trash2, History, X, Paperclip, ImageIcon.

WorkListPage 재사용 대상:
- `useQuery(['work-list', tab, searchQ, showDeleted])` → `items: WorkListItem[]`
  - tab: 'password' | 'contact'
  - 필드 의미: label=항목명, affiliation=(pw)아이디/(contact)이름, value=(pw)비번/(contact)전화,
    extra=(contact)직책, memo=메모, createdByName, createdBy, updatedAt, updatedByName, deletedAt.
- 마스킹 룰(그대로): `showValue = revealAll || revealed || tab === 'contact'`. 가릴 땐 '••••••••'.
  비밀번호 monospace: `fontFamily:'JetBrains Mono, monospace'` (또는 `font-mono`).
- `revealAll` 전체 토글(Eye/EyeOff), 행별 `revealed`(Eye/EyeOff).
- mutation: updateMutation / deleteMutation(WorkListCard 내), `ComposeModal({ type, onClose, onSaved })`,
  `WorkListHistoryModal({ itemId, onClose })`.
- 본인가드: `isMine = !!me && item.createdBy === me.id`.
- import 아이콘: Search, Plus, Trash2, History, X, RotateCcw, Edit3, Eye, EyeOff.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 0: 이모지 검사 스크립트 생성 (verify 도구 선행)</name>
  <files>cha-bio-safety/scripts/check-emoji.cjs</files>
  <action>
    이후 Task 1~3 의 "이모지 0" 게이트가 사용할 검사기를 먼저 만든다. (Korean Hangul 은 절대 flag 하지 말 것 —
    `node -e` 인라인으로 0x2600~0x1FB00 범위를 잡으면 한글이 전부 걸리는 함정이 있으므로 전용 스크립트로 분리.)

    `cha-bio-safety/scripts/check-emoji.cjs` 를 아래 내용으로 생성한다. 인자로 받은 파일들에 실제 이모지/픽토그램/
    딩벳 체크마크(✓ U+2713 포함)가 있으면 EMOJI-FOUND + 코드포인트 + 종료코드 1, 없으면 EMOJI-0 + 종료코드 0.
    **화살표 U+2190~U+21FF 는 기존 주석(`→ 글 생성` 등)에 존재하므로 검사 대상에서 제외** — 렌더 이모지만 본다.

    ```js
    // scripts/check-emoji.cjs — 렌더되는 이모지/픽토그램/딩벳 검출 (한글·주석화살표 제외)
    const files = process.argv.slice(2);
    const isEmoji = cp => (
      (cp >= 0x1F000 && cp <= 0x1FAFF) ||   // pictographs / supplemental symbols
      (cp >= 0x2600 && cp <= 0x27BF)   ||   // misc symbols + dingbats (체크마크 0x2713)
      (cp >= 0x2300 && cp <= 0x23FF)   ||   // misc technical
      cp === 0x2B50 || cp === 0xFE0F        // star + variation selector
    );
    let bad = [];
    for (const f of files) {
      const s = require('fs').readFileSync(f, 'utf8');
      const hits = [...s].map(c => c.codePointAt(0)).filter(isEmoji);
      if (hits.length) bad.push(f + ': ' + hits.map(c => 'U+' + c.toString(16)).join(' '));
    }
    if (bad.length) { console.log('EMOJI-FOUND\n' + bad.join('\n')); process.exit(1); }
    console.log('EMOJI-0'); process.exit(0);
    ```

    생성 직후 현재 파일들로 동작 확인: HandoverPage 는 현재 ✓(U+2713)가 있어 EMOJI-FOUND 가 나오는 게 정상
    (Task 1 에서 ✓ → `<Check/>` 로 치환하면 EMOJI-0 이 됨). WorkListPage 는 지금도 EMOJI-0 이어야 함.
  </action>
  <verify>
    <automated>cd cha-bio-safety && test -f scripts/check-emoji.cjs && node scripts/check-emoji.cjs src/pages/WorkListPage.tsx</automated>
  </verify>
  <done>scripts/check-emoji.cjs 존재. WorkListPage 검사 시 EMOJI-0(종료코드 0). HandoverPage 는 현재 ✓ 때문에 EMOJI-FOUND(Task 1 에서 해소).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 1: HandoverPage 데스크톱 분기 (카드 그리드 + 중앙 상세 모달)</name>
  <files>cha-bio-safety/src/pages/HandoverPage.tsx</files>
  <behavior>
    데스크톱(≥1024px)에서:
    - 본문 상단 툴바(타이틀 없음): 검색 input(Search + X 클리어), 탭(전체/대기/완료), 새 글(Plus),
      searchQ 시 삭제포함 체크박스 — 모두 모바일과 동일 상태/핸들러 재사용.
    - 카드 그리드: `repeat(auto-fill, minmax(290px,1fr))`. 카드 = 좌측 상태바(대기 bg-warning-bar /
      완료 bg-safe-bar), Pin(고정) 상단 정렬, 제목 2줄 클램프 + 본문 3줄 클램프, 하단 상태배지
      (대기 warning / 완료 safe, lucide Check) + 첨부수(Paperclip + N, attachmentCount>0) + 작성자·시각(fmtTime).
    - 카드 클릭 → selectedId 상태로 중앙 상세 모달:
        · **별도 제목칸 없음**: detail.title?.trim() 있으면 본문 영역 최상단에 굵게 표기, 없으면 생략.
        · 작성자·시각 + 본문 전체(pre-wrap) + 첨부 미리보기(ImageIcon/Paperclip, /api/uploads/${storageKey}).
        · 액션: 완료(Check, status 토글) / 고정(Pin, done&&!pinned 비활성) / 수정 / 삭제(Trash2, confirm) /
          이력(History → HistoryModal). 복원은 HistoryModal 내 RotateCcw 그대로.
    - 모바일 return 경로 0 byte 변경.
  </behavior>
  <action>
    HandoverPage.tsx 를 연다. 파일 상단(컴포넌트 밖)에 `import { useIsDesktop } from '../hooks/useIsDesktop'` 추가.

    HandoverPage() 본문에서 기존 모든 state/useQuery/qc/me 는 그대로 두고, **모바일 `return (...)` 직전**에
    `const isDesktop = useIsDesktop()` 호출 + `if (isDesktop) return <DesktopHandover ... />` 분기를 삽입한다.
    hooks 규칙 준수: useIsDesktop 는 다른 모든 hook 호출 뒤·조건 return 전에 호출. 모바일 `return (...)` 블록은
    단 한 글자도 수정하지 말 것 — 분기 추가만.

    데스크톱은 새 내부 컴포넌트 `function DesktopHandover(...)` 로 추출한다(상세 모달 selectedId 상태가 필요).
    DesktopHandover 안에서 동일 useQuery(['handovers', tab, searchQ, showDeleted]) + 동일 state(tab/searchInput/
    searchQ/showDeleted/composeOpen/historyId/selectedId) 를 구성하거나 props 로 받는다 — 가독성상 깔끔한 쪽 선택.
    **새 쿼리키/새 mutation/새 API 생성 금지** — 키 'handovers', 'handover-detail', 'handover-revisions' 만 사용.

    카드/모달 마크업은 `<interfaces>` 의 InspectionPage `DesktopInspectionView`(line 5753~) 패턴
    (grid-cols-[repeat(auto-fill,minmax(290px,1fr))], hover:-translate-y-px, bg-surface-raised, border-border-default)
    을 따른다. semantic Tailwind class 사용, 하드코딩 hex 지양. 상태바 = `absolute left-0 top-0 bottom-0 w-[3px]
    bg-warning-bar|bg-safe-bar`. 제목 2줄/본문 3줄 클램프 = `line-clamp-2`/`line-clamp-3`.

    상세 모달:
    - 카드 클릭 시 selectedId 저장 → `useQuery(['handover-detail', selectedId])` 로 첨부 포함 상세 fetch(키 재사용).
    - 모달 최상단에 별도 제목 input/라벨 절대 만들지 말 것. detail.title?.trim() 있으면 본문 박스 맨 위에 굵게, 없으면 생략.
    - 액션 버튼은 모바일 HandoverCard 의 onClick 로직(updateMutation { pinned } / { status }, deleteMutation,
      onShowHistory)을 동일 의미로 호출. 고정 비활성 `disabled={status==='done' && !pinned}` 유지.
    - 본인 아닌 글: 수정/삭제 버튼 숨김(isMine 가드).
    - 작성/수정/이력 모달은 기존 ComposeModal / HistoryModal 재사용. 상세 모달 자체 마크업은 신규여도 됨
      (상세 표시는 모바일에 없던 화면). 단 새 useMutation·새 API 추가 금지.

    아이콘: 모바일 import 목록(Pin, Check, RotateCcw, Search, Plus, Trash2, History, X, Paperclip, ImageIcon)만 사용.
    이모지 절대 추가 금지 — 모바일 상태배지의 '✓ 완료' 이모지(✓ U+2713)는 데스크톱에선 `<Check size=.. />`+'완료'.
    (※ Task 0 의 check-emoji 가 데스크톱 추가분에 이모지가 있으면 즉시 잡아냄. 단 모바일 return 블록의 기존 ✓ 는
    건드리지 않으므로, 이 게이트는 데스크톱 추가분에 이모지 0 을 보장하되 기존 모바일 ✓ 가 남아있으면 여전히
    EMOJI-FOUND 가 뜬다 → 모바일 무변경 원칙상 기존 ✓ 는 그대로 두고, 게이트 통과 기준은 "데스크톱 추가분에
    새 이모지 0" 으로 해석. 확실히 하려면 데스크톱 상태배지에 ✓ 대신 lucide Check 를 쓰는 것만 지키면 됨.)
    w-8/h-8(48px) 함정 주의.
  </action>
  <verify>
    <automated>cd cha-bio-safety && grep -q "useIsDesktop" src/pages/HandoverPage.tsx && grep -q "if (isDesktop)" src/pages/HandoverPage.tsx && echo OK-branch</automated>
    <automated>cd cha-bio-safety && grep -c "DesktopHandover" src/pages/HandoverPage.tsx</automated>
  </verify>
  <done>HandoverPage.tsx 에 useIsDesktop import + `if (isDesktop) return <DesktopHandover/>` 분기 존재. 데스크톱 카드 그리드(minmax 290px) + 상세 모달(제목칸 없음, title 있으면 본문 위, lucide Check 사용) 구현. 데스크톱 추가분 이모지 0. 모바일 return 블록 무변경. ComposeModal/HistoryModal 재사용.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: WorkListPage 데스크톱 분기 (탭/검색/마스킹 툴바 + 테이블)</name>
  <files>cha-bio-safety/src/pages/WorkListPage.tsx</files>
  <behavior>
    데스크톱(≥1024px)에서:
    - 본문 상단 툴바(타이틀 없음): 탭(비밀번호/연락처), 검색(Search + X), 전체 마스킹 토글(Eye/EyeOff,
      password 탭만), 추가(Plus), searchQ 시 삭제포함 체크박스 — 모두 모바일 동일 상태/핸들러 재사용.
    - 테이블 뷰:
        · thead sticky, tbody 행 hover, 비밀번호 monospace, 메모 ellipsis.
        · 비밀번호 탭 컬럼: 항목명(label)/아이디(affiliation)/비밀번호(value, 행별 Eye·EyeOff +
          헤더 전체토글 revealAll)/메모(memo)/작성자(createdByName)/액션(Edit3·Trash2 본인만, History).
        · 연락처 탭 컬럼: 항목명(label)/이름(affiliation)/직책(extra)/전화번호(value, 마스킹 없음)/메모/작성자/액션.
        · 마스킹 룰 `showValue = revealAll || revealed || tab==='contact'` 그대로.
    - 추가/수정 = 기존 ComposeModal 재사용, 수정이력 = WorkListHistoryModal 재사용.
    - 모바일 return 경로 0 byte 변경.
  </behavior>
  <action>
    WorkListPage.tsx 를 연다. 파일 상단에 `import { useIsDesktop } from '../hooks/useIsDesktop'` 추가.

    WorkListPage() 본문에서 기존 state/useQuery/qc/me 는 그대로 두고, **모바일 `return (...)` 직전**에
    `const isDesktop = useIsDesktop()` + `if (isDesktop) return <DesktopWorkList ... />` 분기를 삽입.
    hooks 규칙 준수(모든 hook 뒤·조건 return 전). 모바일 `return (...)` 블록 0 byte 변경.

    데스크톱은 새 내부 컴포넌트 `function DesktopWorkList(...)` 로 추출. 안에서 동일
    useQuery(['work-list', tab, searchQ, showDeleted]) + 동일 state(tab/searchInput/searchQ/showDeleted/
    composeOpen/editingId/historyId/revealAll) 구성하거나 props 로 받는다.
    **새 쿼리키/mutation/API 생성 금지** — 키 'work-list', 'work-list-revisions' 만 사용.

    테이블 마크업:
    - 컨테이너 `flex-1 min-h-0 flex flex-col`. 툴바는 본문 상단 컨트롤 줄(검색/탭/마스킹/추가)로. 타이틀 텍스트 금지.
    - `<table class="w-full">` + `thead` 는 `sticky top-0 bg-surface-raised`(스크롤 시 헤더 고정),
      `tbody tr` 는 `hover:bg-surface-active border-b border-border-default`.
    - 컬럼은 tab 별로 분기(위 behavior 컬럼 목록 그대로). 비밀번호 셀:
      `<span class="font-mono">{showValue ? item.value : '••••••••'}</span>` + 행별 Eye/EyeOff 토글(revealed),
      헤더 비밀번호 컬럼에 전체토글(revealAll, Eye/EyeOff). `showValue = revealAll || revealed || tab==='contact'`
      룰 변경 금지. 연락처 전화번호는 마스킹 없이 그대로.
    - 메모 셀 ellipsis: `max-w-[..] truncate` 또는 인라인 overflow ellipsis.
    - 작성자 = createdByName. 액션 컬럼: History(모두), Edit3·Trash2 는 `isMine`(item.createdBy===me.id)일 때만.
      삭제는 모바일과 동일 confirm 후 deleteMutation. 수정은 ComposeModal 재사용 또는 인라인 editing 패턴 중 깔끔한 쪽.

    아이콘: 모바일 import 목록(Search, Plus, Trash2, History, X, RotateCcw, Edit3, Eye, EyeOff)만 사용. 이모지 0.
    semantic 토큰 사용, 하드코딩 hex 지양. w-8/h-8(48px) 함정 주의.
  </action>
  <verify>
    <automated>cd cha-bio-safety && grep -q "useIsDesktop" src/pages/WorkListPage.tsx && grep -q "if (isDesktop)" src/pages/WorkListPage.tsx && echo OK-branch</automated>
    <automated>cd cha-bio-safety && grep -q "revealAll || revealed || tab" src/pages/WorkListPage.tsx && echo OK-mask-rule</automated>
    <automated>cd cha-bio-safety && node scripts/check-emoji.cjs src/pages/WorkListPage.tsx</automated>
  </verify>
  <done>WorkListPage.tsx 에 useIsDesktop import + `if (isDesktop) return <DesktopWorkList/>` 분기 존재. 데스크톱 테이블(thead sticky, hover, password monospace, 메모 ellipsis, 탭별 컬럼) 구현. 마스킹 룰 `revealAll || revealed || tab` 보존(grep 통과). check-emoji EMOJI-0. 모바일 return 블록 무변경. ComposeModal/WorkListHistoryModal 재사용.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: 정적 검증 + 모바일 무회귀 + tsc/build</name>
  <files>cha-bio-safety/src/pages/HandoverPage.tsx, cha-bio-safety/src/pages/WorkListPage.tsx</files>
  <action>
    아래 정적 검증을 모두 통과시킨다. 실패 항목이 있으면 Task 1/2 로 돌아가 수정 후 재검증.

    1) 모바일 무회귀 — 두 파일의 **모바일 return 블록**에는 변경이 없어야 한다. git diff 로 추가된 라인이
       데스크톱 분기(useIsDesktop, if(isDesktop), DesktopHandover/DesktopWorkList 정의)와 import 외에
       기존 모바일 JSX/핸들러/상수를 건드리지 않았는지 확인:
         `git -C cha-bio-safety diff -- src/pages/HandoverPage.tsx src/pages/WorkListPage.tsx`
       기존 HandoverCard/WorkListCard/ComposeModal/HistoryModal/WorkListHistoryModal/Modal/스타일 상수(INPUT_S 등)
       본문이 변경되지 않았는지 육안 확인(재사용을 위한 export/공유 추출은 허용, 로직 변경은 불가).

    2) DESKTOP_HEADER_HIDE_PATHS 미변경:
         `git -C cha-bio-safety diff -- src/App.tsx` → 출력이 비어야 한다(App.tsx 무변경).

    3) 새 API/쿼리키/mutation 0:
         `cd cha-bio-safety && grep -nE "useQuery\\(\\[|useMutation" src/pages/HandoverPage.tsx src/pages/WorkListPage.tsx`
       쿼리키가 기존 집합(handovers / handover-detail / handover-revisions / work-list / work-list-revisions)
       을 벗어나지 않는지, 새 mutation 함수가 추가되지 않았는지 확인. 새 API 파일이 생기지 않았는지
       `git -C cha-bio-safety status --porcelain functions/` 가 비어있는지 확인.

    4) 이모지(데스크톱 추가분) 0:
         WorkListPage 는 `node scripts/check-emoji.cjs src/pages/WorkListPage.tsx` → EMOJI-0.
         HandoverPage 는 모바일 기존 ✓ 를 제거하지 않으므로 EMOJI-FOUND 가 ✓(U+2713) 1개만(모바일 라인) 나와야 함 —
         그 외 코드포인트가 추가로 나오면(데스크톱 추가분 이모지) 실패. 즉 데스크톱 상태배지는 반드시 lucide Check.

    5) tsc + build:
         `cd cha-bio-safety && npx tsc --noEmit` → 에러 0.
         `cd cha-bio-safety && npm run build` → 성공.
  </action>
  <verify>
    <automated>cd cha-bio-safety && git diff --quiet -- src/App.tsx && echo "APP-UNCHANGED"</automated>
    <automated>cd cha-bio-safety && test -z "$(git status --porcelain functions/)" && echo "NO-NEW-API"</automated>
    <automated>cd cha-bio-safety && grep -nE "useQuery\(\[|useMutation" src/pages/HandoverPage.tsx src/pages/WorkListPage.tsx</automated>
    <automated>cd cha-bio-safety && npx tsc --noEmit && echo "TSC-PASS"</automated>
    <automated>cd cha-bio-safety && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>App.tsx 무변경(git diff 비어있음) + functions/ 새 파일 0. 쿼리키/mutation 기존 집합만 사용. WorkListPage check-emoji EMOJI-0, HandoverPage 데스크톱 추가분 이모지 0(✓ 는 모바일 라인만). `npx tsc --noEmit` 에러 0 + `npm run build` 성공. 배포는 범위 밖 — 코드+tsc+build 까지만.</done>
</task>

</tasks>

<verification>
- 데스크톱(≥1024px): /handovers 카드 그리드 + 카드 클릭 시 중앙 상세 모달(제목칸 없음, title 있으면 본문 위).
- 데스크톱: /work-list 탭/검색/마스킹토글 툴바 + 테이블(thead sticky, hover, password monospace).
- 모바일(<1024px): 두 페이지 변경 전과 픽셀 동일(모바일 return 블록 git diff 0).
- App.tsx 무변경 (DESKTOP_HEADER_HIDE_PATHS 그대로) → 이중 헤더 없음.
- 새 API 파일 0 / 새 useQuery·useMutation 키 0 / 데스크톱 추가분 이모지 0.
- tsc PASS + build PASS.
</verification>

<success_criteria>
- 두 페이지 모두 `if (isDesktop) return <데스크톱>` 분기로 데스크톱 레이아웃 제공, 모바일 무변경.
- ComposeModal/HistoryModal/WorkListHistoryModal, 기존 useQuery/useMutation, 본인가드, 마스킹, 소프트삭제, 타임머신 100% 재사용 (새 모달·API·쿼리키·mutation 0).
- 시안 2개 수정 반영: 인계장 상세 모달 별도 제목칸 없음 / 아이콘 lucide 그대로(데스크톱 추가분 이모지 0).
- semantic 토큰 사용(다크·라이트 양립), w-8/h-8 함정 회피.
- tsc + build 통과. (배포는 범위 밖)
</success_criteria>

<output>
After completion, create `.planning/quick/260601-n1s-desktop-layout-for-handover-and-worklist/260601-n1s-SUMMARY.md`
</output>
