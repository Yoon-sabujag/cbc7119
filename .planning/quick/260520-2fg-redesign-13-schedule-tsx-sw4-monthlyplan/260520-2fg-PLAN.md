---
title: SW4 — 월간 점검 계획 미리보기 테이블 (MonthlyPlanPreview)
phase_id: quick-260520-2fg
phase: quick-260520-2fg
plan: 01
type: execute
wave: 1
status: ready
depends_on: [quick-260520-1q0]
files_modified:
  - cha-bio-safety/src/pages/SchedulePage.tsx
autonomous: true
requirements: [SCHED-SW4]
tags: [tsx-conversion, redesign, schedule, sw4, monthly-plan-preview, desktop-only, tokens]
subsystem: redesign/13-schedule

must_haves:
  truths:
    - "MonthlyPlanPreview 컴포넌트 (line 643~787) 의 옛 alias var() 토큰이 v0.1.1 semantic 토큰으로 1:1 치환됨"
    - "cell fontSize 9/10/11 모두 12 로 노안 격상됨 (sketch W3 line 286/302/304/309 verbatim)"
    - "31×21 본문 cell + 31×2 헤더 행 dynamic 분기 (sun/hol/sat/today/safe/today-last) 인라인 잔존 (W7 §10 화이트리스트)"
    - "today border 4 위치 (date row top + date row side + weekday row side + body left/right + body last-row bottom) `var(--accent)` 토큰화"
    - "PLAN_PREVIEW_ROWS / dayCatMap / Array.from 패턴 / row 분기 로직 / 타이틀+헤더 카피 1 byte 변경 0"
    - "desktop-only — 호출 site 분기 (`isDesktop && <MonthlyPlanPreview ... />`) 유지, 컴포넌트 안 early return 추가 안 함"
    - "이모지 0 / linear-gradient (SW4 영역) 0 / `text-status-*` 잘못된 prefix 0"
    - "tsc --noEmit + npm run build PASS"
  artifacts:
    - path: "cha-bio-safety/src/pages/SchedulePage.tsx"
      provides: "MonthlyPlanPreview v0.1.1 토큰화 + 노안 격상"
      contains: "var(--surface-sunken), var(--text-primary), var(--text-tertiary), var(--border-default), var(--accent), fontSize: 12"
  key_links:
    - from: "MonthlyPlanPreview cellStyle / headCell"
      to: "tokens.css v0.1.1 semantic 토큰"
      via: "var(--surface-sunken) / var(--text-primary) / var(--border-default) 직접 참조"
      pattern: "var\\(--(surface|text|border|accent)-"
    - from: "MonthlyPlanPreview 31×21 cell 동적 분기"
      to: "W7 §10 인라인 화이트리스트"
      via: "잔존 OK — 정적 Tailwind 불가 (alpha 0.06/0.08/0.1/0.18 보정 + 8중 분기)"
      pattern: "(rgba\\(239,68,68|rgba\\(59,130,246|rgba\\(34,197,94)"
---

<objective>
SchedulePage.tsx 의 `MonthlyPlanPreview` 컴포넌트 (현재 line 643~787, 약 145 lines) 의 옛 alias var() 토큰을 v0.1.1 semantic 토큰으로 1:1 치환하고, cell fontSize 9/10/11 을 모두 12 로 노안 격상한다.

**전략:** W7 §10 인라인 화이트리스트 채택 (옵션 A). 31×21 본문 cell + 31×2 헤더 행의 dynamic 분기 (sun/sat/hol/today/safe/today-last + alpha 0.06/0.08/0.1/0.18) 는 정적 Tailwind 로 표현 불가 → 인라인 style 잔존. `cellStyle` / `headCell` base 객체만 토큰 치환.

**Source-of-truth verbatim 인용:**

W7 §3 region.preview-table (wave-7-tsx-conversion-checklist.md line 105):
> `region.preview-table` (월간 점검 계획, desktop-only) | line 498~643 | W3 | W3 OQ #1 LOCKED 모바일 미구현 — 데스크톱 전용 (`isDesktop && <MonthlyPlanPreview ... />`). W3 OQ #2 LOCKED b) FAB 표시 안 함. W3 OQ #3 LOCKED a) 1280px cramped 그대로 (31일 폰트 12px). 31×21 cell — `cell-bg-(sun|sat|hol|safe|today|today-last)` 6 클래스 환원 또는 §10 인라인 잔존. today border `var(--accent)` 2px 잔존 OK (§10).

W7 §6 sub-task 4 (line 326):
> **SW4 — 미리보기 테이블 (line 498~643, desktop-only):** MonthlyPlanPreview 컴포넌트. W3 OQ #1 LOCKED 모바일 미구현, W3 OQ #2 LOCKED b) FAB 없음, W3 OQ #3 LOCKED a) cramped 그대로 (31일 폰트 12px). 31×21 cell — `cell-bg-(sun|sat|hol|safe|today|today-last)` 6 클래스 환원 또는 §10 인라인 잔존.

W7 §10 인라인 화이트리스트 (line 472~474):
> | 미리보기 테이블 31×21 cell 동적 분기 | line 622~630 | 8중 분기 + alpha 0.06/0.1 — 정적 Tailwind 불가 또는 `cell-bg-*` 6 클래스 환원 (§7.6) |
> | 미리보기 테이블 today border | line 627~629 | 동적 border 분기 — Tailwind 가능하지만 인라인 잔존도 OK |
> | 미리보기 테이블 header today bg | line 557 | alpha 0.18 보정 |

**채택:** §10 인라인 잔존 (옵션 A). 옵션 B (scoped `<style>`) 비표준, 옵션 C (tailwind.config 6 backgroundColor + 5 borderColor 추가) overkill.

Purpose: redesign/13-schedule SW4 — 데스크톱 전용 월간 점검 계획 미리보기 테이블의 토큰 정합성 확보. 시각 변경은 노안 격상 (cell fontSize 9/10/11 → 12) 만, 색·구조·로직 0 변경.

Output: SchedulePage.tsx 단일 파일 수정. line 수 변화 거의 없음 (+0~+5 lines, alias 치환만).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@CLAUDE.local.md
@cha-bio-safety/docs/redesign-context/13-schedule/wave-7-tsx-conversion-checklist.md
@cha-bio-safety/docs/redesign-context/13-schedule/sketch-wave-3.html
@cha-bio-safety/docs/redesign-context/13-schedule/tokens.css
@cha-bio-safety/src/pages/SchedulePage.tsx
@.planning/quick/260520-1q0-redesign-13-schedule-tsx-sw3-rendercard-/260520-1q0-SUMMARY.md

<interfaces>
<!-- Sketch CSS verbatim (sketch-wave-3.html line 276~339) — 변환 매핑의 single source -->

`.preview-table` base (line 276~291):
```css
.preview-table {
  width: 100%;
  border-collapse: collapse;
  table-layout: fixed;
}
.preview-table th,
.preview-table td {
  border: 1px solid var(--border-default);
  padding: 3px 1px;
  text-align: center;
  font-size: 12px; /* 노안 격상: source 11 → 12 */
  line-height: 1.3;
  overflow: hidden;
  white-space: nowrap;
  color: var(--text-primary);
}
.preview-table th {
  font-weight: 700;
  background: var(--surface-sunken);
  color: var(--text-primary);
}
.preview-table .row-weekday th { font-weight: 600; }
```

`.preview-table` 헤더/바디 column 정의 (line 298~309):
```css
.preview-table .col-no  { width: 2%; }
.preview-table .col-lbl { width: 20%; text-align: left; padding-left: 6px; }
.preview-table .col-note { width: 6%; }
.preview-table td.cell-no  { font-weight: 600; }
.preview-table td.cell-lbl { text-align: left; padding-left: 6px; font-size: 12px; /* 격상 10 → 12 */ }
.preview-table td.cell-note {
  font-size: 12px; /* 격상 9 → 12 */
  color: var(--text-tertiary);
  white-space: normal;
  line-height: 1.2;
}
.preview-table td.cell-day { font-size: 12px; /* 격상 10 → 12 */ }
```

`.preview-table` 요일 색상 + today 배경/border (line 312~325):
```css
.preview-table th.sun, .preview-table th.hol { color: #ef4444; }
.preview-table th.sat                        { color: #3b82f6; }
.preview-table th.bg-sun, .preview-table th.bg-hol { background: rgba(239,68,68,0.08); }
.preview-table th.bg-sat                            { background: rgba(59,130,246,0.08); }
.preview-table th.bg-today                          { background: rgba(59,130,246,0.18); }
.preview-table th.brd-today {
  border-left:  2px solid var(--accent);
  border-right: 2px solid var(--accent);
  border-top:   2px solid var(--accent);
}
.preview-table th.brd-today-side {
  border-left:  2px solid var(--accent);
  border-right: 2px solid var(--accent);
}
```

`.preview-table` tbody 셀 분기 (line 327~339):
```css
.preview-table td.cell-bg-sun, .preview-table td.cell-bg-hol { background: rgba(239,68,68,0.06); }
.preview-table td.cell-bg-sat                                { background: rgba(59,130,246,0.06); }
.preview-table td.cell-bg-safe                               { background: rgba(34,197,94,0.10); }
.preview-table td.cell-today {
  border-left:  2px solid var(--accent);
  border-right: 2px solid var(--accent);
}
.preview-table td.cell-today-last {
  border-left:   2px solid var(--accent);
  border-right:  2px solid var(--accent);
  border-bottom: 2px solid var(--accent);
}
.preview-table td.cell-empty { color: transparent; }
```

<!-- tokens.css v0.1.1 alias 매핑 (이미 정의됨, 추가 작업 불필요) -->

옛 alias → v0.1.1 매핑 (tokens.css line 176~197):
```
--bg2  → --surface-raised
--bg3  → --surface-sunken
--bd   → --border-default
--t1   → --text-primary
--t3   → --text-tertiary
--acl  → --accent
```

<!-- SW3 학습: status- prefix 없음 (cha-bio-safety/tailwind.config.js — text-fire / text-safe-bar / text-danger-bar / bg-info-bg, NOT text-status-*) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: MonthlyPlanPreview cellStyle/headCell base + outer container + 헤더 2행 + 본문 21 row 토큰화 + 노안 격상</name>
  <files>cha-bio-safety/src/pages/SchedulePage.tsx</files>
  <action>
    cha-bio-safety/src/pages/SchedulePage.tsx 의 `MonthlyPlanPreview` 컴포넌트 (현재 line 643~787) 의 옛 alias var() 토큰을 v0.1.1 semantic 토큰으로 1:1 치환하고, cell fontSize 9/10/11 모두 12 로 노안 격상한다. **동적 분기 인라인 style 은 §10 화이트리스트로 잔존.**

    ===== 변환 매핑 표 (verbatim) =====

    **A. base 객체 (line 673~677):**
    ```diff
    - const cellStyle: React.CSSProperties = {
    -   border: '1px solid var(--bd)', padding: '3px 1px', textAlign: 'center',
    -   fontSize: 11, lineHeight: 1.3, overflow: 'hidden', whiteSpace: 'nowrap', color: 'var(--t1)',
    - }
    - const headCell: React.CSSProperties = { ...cellStyle, fontWeight: 700, background: 'var(--bg3)', color: 'var(--t1)' }
    + const cellStyle: React.CSSProperties = {
    +   border: '1px solid var(--border-default)', padding: '3px 1px', textAlign: 'center',
    +   fontSize: 12, lineHeight: 1.3, overflow: 'hidden', whiteSpace: 'nowrap', color: 'var(--text-primary)',
    + }
    + const headCell: React.CSSProperties = { ...cellStyle, fontWeight: 700, background: 'var(--surface-sunken)', color: 'var(--text-primary)' }
    ```
    노안 격상: fontSize 11 → 12 (sketch W3 line 286 verbatim).

    **B. outer container (line 680):**
    ```diff
    - <div style={{ width: '100%', padding: '12px 20px 8px', background: 'var(--bg2)' }}>
    + <div style={{ width: '100%', padding: '12px 20px 8px', background: 'var(--surface-raised)' }}>
    ```
    padding 12/20/8 비표준 → 인라인 잔존 OK.

    **C. 타이틀 (line 682):**
    ```diff
    - <div style={{ textAlign: 'center', fontSize: 14, fontWeight: 700, marginBottom: 8, color: 'var(--t1)' }}>
    + <div style={{ textAlign: 'center', fontSize: 14, fontWeight: 700, marginBottom: 8, color: 'var(--text-primary)' }}>
    ```
    fontSize 14 유지 (cramped 일관성 — 키우면 헤더 깨짐). 타이틀 카피 `{mo}월 중요업무추진계획(방재)` 변경 금지.

    **D. 날짜 행 31 cell (line 692~709):**
    ```diff
    - color: dow === 0 || isHol ? '#ef4444' : dow === 6 ? '#3b82f6' : 'var(--t1)',
    - background: isTdy ? 'rgba(59,130,246,0.18)' : dow === 0 || isHol ? 'rgba(239,68,68,0.08)' : dow === 6 ? 'rgba(59,130,246,0.08)' : 'var(--bg3)',
    - borderLeft: isTdy ? '2px solid var(--acl)' : undefined,
    - borderRight: isTdy ? '2px solid var(--acl)' : undefined,
    - borderTop: isTdy ? '2px solid var(--acl)' : undefined,
    + color: dow === 0 || isHol ? '#ef4444' : dow === 6 ? '#3b82f6' : 'var(--text-primary)',
    + background: isTdy ? 'rgba(59,130,246,0.18)' : dow === 0 || isHol ? 'rgba(239,68,68,0.08)' : dow === 6 ? 'rgba(59,130,246,0.08)' : 'var(--surface-sunken)',
    + borderLeft: isTdy ? '2px solid var(--accent)' : undefined,
    + borderRight: isTdy ? '2px solid var(--accent)' : undefined,
    + borderTop: isTdy ? '2px solid var(--accent)' : undefined,
    ```
    raw hex `#ef4444` / `#3b82f6` 잔존 OK (§4.1 #7 / §10 화이트리스트 — sketch line 312~313 의 `.sun.hol` / `.sat` color hex 와 정합, JS 변수 동적 hex 아님).
    alpha 보정 `rgba(*,0.18/0.08)` 인라인 잔존 OK (§10 — 정적 Tailwind 불가).

    **E. 요일 행 31 cell (line 716~733):**
    ```diff
    - color: dow === 0 || isHol ? '#ef4444' : dow === 6 ? '#3b82f6' : 'var(--t1)',
    - borderLeft: isTdy ? '2px solid var(--acl)' : undefined,
    - borderRight: isTdy ? '2px solid var(--acl)' : undefined,
    + color: dow === 0 || isHol ? '#ef4444' : dow === 6 ? '#3b82f6' : 'var(--text-primary)',
    + borderLeft: isTdy ? '2px solid var(--accent)' : undefined,
    + borderRight: isTdy ? '2px solid var(--accent)' : undefined,
    ```
    fontWeight: 600 유지 (sketch line 297 `.row-weekday th { font-weight: 600 }` verbatim).

    **F. 본문 row × 31 day cells (line 735~782):**

    F-1. NO. 셀 (line 738) — `{ ...cellStyle, fontWeight: 600 }` (cell-no `font-weight: 600` sketch line 301 verbatim) — **변경 없음**.

    F-2. label 셀 (line 739):
    ```diff
    - <td style={{ ...cellStyle, textAlign: 'left', paddingLeft: 6, fontSize: 10 }}>{row.label}</td>
    + <td style={{ ...cellStyle, textAlign: 'left', paddingLeft: 6, fontSize: 12 }}>{row.label}</td>
    ```
    노안 격상 (sketch line 302 `.cell-lbl { font-size: 12px /* 격상 10 → 12 */ }` verbatim).

    F-3. day 셀 31개 (line 765~777):
    ```diff
    - <td key={i} style={{
    -   ...cellStyle, fontSize: 10,
    -   color: text ? 'var(--t1)' : 'transparent',
    -   background: isWeekend ? (dow === 0 || isHol ? 'rgba(239,68,68,0.06)' : 'rgba(59,130,246,0.06)')
    -     : text && !row.daily ? 'rgba(34,197,94,0.1)' : 'transparent',
    -   borderLeft: isTdy ? '2px solid var(--acl)' : undefined,
    -   borderRight: isTdy ? '2px solid var(--acl)' : undefined,
    -   borderBottom: isTdy && isLastRow ? '2px solid var(--acl)' : undefined,
    - }}>
    + <td key={i} style={{
    +   ...cellStyle, fontSize: 12,
    +   color: text ? 'var(--text-primary)' : 'transparent',
    +   background: isWeekend ? (dow === 0 || isHol ? 'rgba(239,68,68,0.06)' : 'rgba(59,130,246,0.06)')
    +     : text && !row.daily ? 'rgba(34,197,94,0.1)' : 'transparent',
    +   borderLeft: isTdy ? '2px solid var(--accent)' : undefined,
    +   borderRight: isTdy ? '2px solid var(--accent)' : undefined,
    +   borderBottom: isTdy && isLastRow ? '2px solid var(--accent)' : undefined,
    + }}>
    ```
    노안 격상 fontSize 10 → 12 (sketch line 309 `.cell-day { font-size: 12px /* 격상 10 → 12 */ }` verbatim).
    `text || '.'` dot 표시 (line 776) **변경 금지** — sketch dot fallback 보존 룰.
    alpha 보정 `rgba(*,0.06/0.1)` 인라인 잔존 OK (§10 — 8중 분기 정적 Tailwind 불가).

    F-4. note 셀 (line 779~781):
    ```diff
    - <td style={{ ...cellStyle, fontSize: 9, color: 'var(--t3)', whiteSpace: 'normal', lineHeight: 1.2 }}>
    + <td style={{ ...cellStyle, fontSize: 12, color: 'var(--text-tertiary)', whiteSpace: 'normal', lineHeight: 1.2 }}>
    ```
    노안 격상 fontSize 9 → 12 (sketch line 304 `.cell-note { font-size: 12px /* 격상 9 → 12 */ }` verbatim).

    ===== NEGATIVE 변경 금지 항목 (1 byte 도 변경 금지) =====

    1. `PLAN_PREVIEW_ROWS` 21 row 정의 (line 99~121) — 0 byte.
    2. `dayCatMap` useMemo 로직 (line 652~672) — 멀티데이 주말/공휴일 제외 룰 0 byte.
    3. `daysInMonth` / `firstDow` / `DOW` 7종 상수 (line 647~649) — 0 byte.
    4. `Array.from({ length: daysInMonth }, (_, i) => ...)` 31일 map 패턴 3회 (line 692, 716, 740) — 0 byte.
    5. row 분기 로직 `row.daily ? '점검' : row.cats? dayCats.has(cat) → row.cl?.[cat] ?? '점검'` (line 749~761) — 0 byte.
    6. `text || '.'` 빈 셀 dot 표시 (line 775) — 0 byte.
    7. `row.note ?? ''` (line 780) — 0 byte.
    8. 타이틀 카피 `{mo}월 중요업무추진계획(방재)` (line 683) — 0 byte.
    9. 헤더 카피 `'시행일자'` (line 691) / `'NO.'` (line 714) / `'내   용'` (line 715 — `내 &nbsp; 용` HTML entity 포함) / `'비고'` (line 710) — 0 byte.
    10. 호출 site 분기 `isDesktop && <MonthlyPlanPreview ... />` (SchedulePage 모바일 분기 line 575 근처) — 0 byte. W3 OQ #1 LOCKED 모바일 미구현 verbatim. 컴포넌트 안 early return 추가 안 함.

    ===== 메모리 룰 (executor 강제 준수) =====

    1. **`feedback_tailwind_token_class_pattern.md`** — status- prefix 없음. SW3 (260520-1q0) executor Rule 1 정정 적용: 모든 `text-status-*` / `bg-status-*` 패턴 X. **단 본 SW4 는 Tailwind class 사용 안 함 — 100% 인라인 CSS variable 만 사용.** 신경 쓸 것은 verify gate §4.1 #3 (오탈자 방지) 만.

    2. **`feedback_tsx_wave_emoji_dot_gap.md`** — 이모지 0 강제 + sketch 의 dot 표시 보존. line 775 `text || '.'` 변경 금지.

    3. **`feedback_planner_prompt_sketch_verbatim.md`** — 본 plan 의 sketch CSS verbatim 인용 (위 변환 매핑 표) 그대로 따를 것. 추측한 토큰명/사이즈 사용 금지.

    4. **`feedback_text_caption_leading_none.md`** — text-caption (12px) cell 안 `lineHeight: 1.3` 명시 유지 (sketch line 287 verbatim). 인라인 `lineHeight: 1.3` 본문 cell + `lineHeight: 1.2` note cell 모두 명시적으로 유지.

    5. **`feedback_tailwind_w8_h8_is_48px.md`** — width % (2/20/6 fractional) 은 spacing 충돌 없음, 인라인 잔존 그대로 (`width: '2%' / '20%' / '6%'`).

    ===== executor 주의사항 =====

    - **§10 인라인 화이트리스트 OK**: 31×3=93 헤더 cell + 21×31=651 본문 cell 의 인라인 style 잔존 OK (옵션 A 채택).
    - **status- prefix 오탈자 방지**: 본 SW4 는 Tailwind class 0 사용. 만약 부주의로 추가하면 §4.1 #3 violate.
    - **옛 alias 잔존 0**: SW4 영역 (line 643~787) 내부 `var(--bg2/--bg3/--bd/--t1/--t3/--acl)` grep hit = 0. SW4 영역 외 잔존은 out of scope.
    - **raw hex 잔존 OK**: `#ef4444` (sun/hol text) / `#3b82f6` (sat text) 잔존 OK (§10 — sketch line 312~313 verbatim).
  </action>
  <verify>
    <automated>
      # 작업 위치 = /Users/jykevin/Documents/cbc7119-design/cha-bio-safety/

      # 1. tsc 타입체크 PASS
      cd /Users/jykevin/Documents/cbc7119-design/cha-bio-safety && npx tsc --noEmit

      # 2. build PASS
      cd /Users/jykevin/Documents/cbc7119-design/cha-bio-safety && npm run build

      # 3. SW4 영역 옛 alias 0 (line 643~787)
      sed -n '643,787p' /Users/jykevin/Documents/cbc7119-design/cha-bio-safety/src/pages/SchedulePage.tsx | grep -cE 'var\(--(bg2|bg3|bd|bd2|t1|t2|t3|acl|safe|info|warn|danger|fire)\)'
      # expected: 0

      # 4. SW4 영역 v0.1.1 토큰 ≥ N hits
      sed -n '643,787p' /Users/jykevin/Documents/cbc7119-design/cha-bio-safety/src/pages/SchedulePage.tsx | grep -cE 'var\(--(surface-sunken|surface-raised|text-primary|text-tertiary|border-default|accent)\)'
      # expected: ≥ 10

      # 5. fontSize 9/10/11 0 (SW4 영역)
      sed -n '643,787p' /Users/jykevin/Documents/cbc7119-design/cha-bio-safety/src/pages/SchedulePage.tsx | grep -cE 'fontSize:\s*(9|10|11)\b'
      # expected: 0

      # 6. fontSize: 12 hits ≥ 4 (cellStyle base + label + day + note)
      sed -n '643,787p' /Users/jykevin/Documents/cbc7119-design/cha-bio-safety/src/pages/SchedulePage.tsx | grep -cE 'fontSize:\s*12\b'
      # expected: ≥ 4 (cellStyle base 1 + label 1 + day 1 + note 1)

      # 7. 이모지/시계 글리프 0 (SW4 영역)
      sed -n '643,787p' /Users/jykevin/Documents/cbc7119-design/cha-bio-safety/src/pages/SchedulePage.tsx | grep -cE '🕐|🕑|🕒|🕓|🕔|🕕|🕖|🕗|🕘|🕙|🕚|🕛'
      # expected: 0

      # 8. linear-gradient 0 (SW4 영역)
      sed -n '643,787p' /Users/jykevin/Documents/cbc7119-design/cha-bio-safety/src/pages/SchedulePage.tsx | grep -c 'linear-gradient'
      # expected: 0

      # 9. text-status-* / bg-status-* 잘못된 prefix 0 (SW4 영역)
      sed -n '643,787p' /Users/jykevin/Documents/cbc7119-design/cha-bio-safety/src/pages/SchedulePage.tsx | grep -cE '(text|bg)-status-(fire|safe|warning|info|danger)'
      # expected: 0

      # 10. NEGATIVE — PLAN_PREVIEW_ROWS / dayCatMap / Array.from / row.daily / row.cats / row.cl / text || '.' / row.note 시그니처 diff = 0
      grep -nE '(PLAN_PREVIEW_ROWS|dayCatMap|Array\.from|row\.daily|row\.cats|row\.cl|row\.note)' /Users/jykevin/Documents/cbc7119-design/cha-bio-safety/src/pages/SchedulePage.tsx
      # expected: ≥ 12 hits (PLAN_PREVIEW_ROWS def + 3 maps + row branch + note)

      # 11. NEGATIVE — 타이틀 카피 + 헤더 카피 변경 0
      grep -c '중요업무추진계획(방재)' /Users/jykevin/Documents/cbc7119-design/cha-bio-safety/src/pages/SchedulePage.tsx
      # expected: 1
      grep -cE '(시행일자|NO\.|비고)' /Users/jykevin/Documents/cbc7119-design/cha-bio-safety/src/pages/SchedulePage.tsx
      # expected: ≥ 3

      # 12. NEGATIVE — text || '.' dot 표시 보존
      grep -c "text || '\.'" /Users/jykevin/Documents/cbc7119-design/cha-bio-safety/src/pages/SchedulePage.tsx
      # expected: 1

      # 13. accent border 12 hits (date row top/L/R + weekday row L/R + body L/R + last-row bottom — 모두 isTdy 분기)
      sed -n '643,787p' /Users/jykevin/Documents/cbc7119-design/cha-bio-safety/src/pages/SchedulePage.tsx | grep -cE "'2px solid var\(--accent\)'"
      # expected: ≥ 6 (date 3 + weekday 2 + body 3 = 8 차이 OK)

      # 14. raw hex #ef4444 / #3b82f6 잔존 (§10 화이트리스트) — sun/sat text color
      sed -n '643,787p' /Users/jykevin/Documents/cbc7119-design/cha-bio-safety/src/pages/SchedulePage.tsx | grep -cE "#(ef4444|3b82f6)"
      # expected: ≥ 2 (date row 1 + weekday row 1, 동일 표현식)

      # 15. alpha rgba 인라인 잔존 (§10 화이트리스트)
      sed -n '643,787p' /Users/jykevin/Documents/cbc7119-design/cha-bio-safety/src/pages/SchedulePage.tsx | grep -cE 'rgba\((239,68,68|59,130,246|34,197,94)'
      # expected: ≥ 6 (date row 3 + body row 3)
    </automated>
  </verify>
  <done>
    - tsc --noEmit PASS (0 errors)
    - npm run build PASS (0 errors)
    - SW4 영역 (line 643~787) 옛 alias var() 0 hit
    - SW4 영역 v0.1.1 토큰 ≥10 hits (surface/text/border/accent)
    - SW4 영역 fontSize 9/10/11 = 0, fontSize 12 ≥4 hits
    - 이모지 0 / linear-gradient 0 / `text-status-*` 0
    - PLAN_PREVIEW_ROWS / dayCatMap / Array.from / row 분기 / 타이틀+헤더 카피 / dot 표시 모두 verbatim 유지
    - desktop-only 호출 site 분기 변경 0 (컴포넌트 안 early return 추가 안 함)
  </done>
</task>

</tasks>

<verification>
**verify gate (W7 §4 — SW4 관련 grep 명령 verbatim):**

```bash
cd /Users/jykevin/Documents/cbc7119-design/cha-bio-safety

# §4.1 negative gates (0 이어야 함)
# #1 이모지 0 (SW4 영역)
sed -n '643,787p' src/pages/SchedulePage.tsx | grep -cE '🕐|🕑|🕒|🕓|🕔|🕕|🕖|🕗|🕘|🕙|🕚|🕛'
# → 0

# #3 text-status-* / bg-status-* 잘못된 prefix 0
sed -n '643,787p' src/pages/SchedulePage.tsx | grep -cE '(text|bg)-status-(fire|safe|warning|info|danger)'
# → 0

# #4 linear-gradient 0 (SW4 영역)
sed -n '643,787p' src/pages/SchedulePage.tsx | grep -c 'linear-gradient'
# → 0

# #5 옛 alias var() 0 (SW4 영역)
sed -n '643,787p' src/pages/SchedulePage.tsx | grep -cE 'var\(--(bg2|bg3|bd|bd2|t1|t2|t3|acl|safe|info|warn|danger|fire)\)'
# → 0

# #6 본문 "오늘" 텍스트 0 (SW4 영역 isTdy 분기는 bg/border 만)
sed -n '643,787p' src/pages/SchedulePage.tsx | grep -c '오늘'
# → 0

# #7 raw hex 잔존 OK (§10 화이트리스트 — sketch line 312~313 verbatim)
# #ef4444 / #3b82f6 잔존 OK, 그 외 0
sed -n '643,787p' src/pages/SchedulePage.tsx | grep -oE '#[0-9a-fA-F]{6}' | sort -u
# → only #3b82f6, #ef4444 (각 sun/sat text color)

# §4.2 quantitative gates (≥ threshold)
# #9 surface 토큰 ≥ 2
sed -n '643,787p' src/pages/SchedulePage.tsx | grep -cE 'var\(--surface-(raised|sunken)\)'
# → ≥ 2

# #10 text 토큰 ≥ 3
sed -n '643,787p' src/pages/SchedulePage.tsx | grep -cE 'var\(--text-(primary|tertiary)\)'
# → ≥ 3

# #11 border 토큰 ≥ 1
sed -n '643,787p' src/pages/SchedulePage.tsx | grep -cE 'var\(--border-default\)'
# → ≥ 1

# #12 accent 토큰 ≥ 6 (today border 위치)
sed -n '643,787p' src/pages/SchedulePage.tsx | grep -cE 'var\(--accent\)'
# → ≥ 6

# §4.3 build gates
# #19 tsc PASS
npx tsc --noEmit
# → exit 0

# #20 build PASS
npm run build
# → exit 0
```

**NEGATIVE — diff zero 확인:**

```bash
# PLAN_PREVIEW_ROWS / dayCatMap / Array.from 패턴 / row 분기 / 타이틀+헤더 카피 / dot 표시
grep -nE '(PLAN_PREVIEW_ROWS|dayCatMap|중요업무추진계획|시행일자|"NO\\.|"비고")' src/pages/SchedulePage.tsx
# → 모두 그대로
grep -c "text || '\.'" src/pages/SchedulePage.tsx
# → 1
```
</verification>

<success_criteria>
- `cha-bio-safety/src/pages/SchedulePage.tsx` 단일 파일 수정.
- MonthlyPlanPreview 컴포넌트 (line 643~787) 의 옛 alias var() 모두 v0.1.1 semantic 토큰으로 치환.
- cell fontSize 9/10/11 모두 12 로 노안 격상 (cellStyle base 11→12, label 10→12, day 10→12, note 9→12).
- 31×21 본문 cell + 31×2 헤더 행 dynamic 분기 인라인 잔존 (§10 화이트리스트 — alpha 0.06/0.08/0.1/0.18 / 8중 분기 / today border).
- raw hex `#ef4444` / `#3b82f6` 잔존 OK (§10 — sketch line 312~313 verbatim, sun/sat text color).
- PLAN_PREVIEW_ROWS 21 row / dayCatMap 로직 / Array.from 31일 map / row 분기 / `text || '.'` dot 표시 / 타이틀+헤더 카피 모두 1 byte 변경 0.
- desktop-only 호출 site 분기 (`isDesktop && <MonthlyPlanPreview ... />`) 변경 0. 컴포넌트 안 early return 추가 안 함 (W3 OQ #1 LOCKED 모바일 미구현 verbatim).
- 이모지 0 / linear-gradient (SW4 영역) 0 / `text-status-*` 잘못된 prefix 0.
- `npx tsc --noEmit` PASS / `npm run build` PASS.
- SchedulePage.tsx 라인 수 변화 거의 없음 (+0~+5 lines, alias 치환만).
- **NEVER 룰 준수**: wrangler 명령 X / `npm run deploy` X / 운영 PWA 영향 0.
- commit message: `tsx(13-schedule): SW4 — 월간 점검 계획 미리보기 테이블`
</success_criteria>

<output>
After completion, create `.planning/quick/260520-2fg-redesign-13-schedule-tsx-sw4-monthlyplan/260520-2fg-SUMMARY.md`.

SUMMARY 에 포함:
- 변환된 영역 표 (Before / After)
- verify gate 결과 표 (§4.1 negative / §4.2 quantitative / §4.3 build)
- 다음 SW (SW5/SW6 — AddModal/EditModal/외부 함수 등) 예고
- main 머지 + push 사용자 컨펌 대기 상태로 마무리
</output>
