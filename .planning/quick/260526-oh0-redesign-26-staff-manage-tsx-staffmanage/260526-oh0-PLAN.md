---
phase: 260526-oh0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cha-bio-safety/src/pages/StaffManagePage.tsx
autonomous: true
requirements:
  - REDESIGN-26-W5-TSX-CONVERSION-STAFFMANAGE
must_haves:
  truths:
    - "StaffManagePage.tsx 의 외곽 chrome 3 영역 (W2 frame-guard-header / W3 staff-list+FAB / W4 modal-form-confirm) 이 W5 §3 sketch class 매핑 verbatim 으로 변환되어 cbc7119-preview 에서 W2~W4 sketch 와 시각적으로 동일하다"
    - "비즈 anchor 26건 (admin 가드 useEffect+early return / staffApi 4종 (list+create+update+resetPassword) / queryKey ['staff-list'] + invalidate 4 / useMutation 4 (create+update+resetPw+deactivate) / staffList sort+rankOfTitle / 사번 10자리 정규식 /^\\d{10}$/ / appointedAt 사번 앞 8자리 자동 채움 / shiftOffset 0~3+shiftFixed 폼 UI 미노출 / ReplaceModalContent 2× staffApi.update 시퀀스 / candidates 필터 / shiftLabel 분기 3종 / window.__openReplaceModal 훅+cleanup+100ms setTimeout / role 옵션 (['admin','assistant'] as Role[]) / 비밀번호 초기화 confirm 카피 / ModalWrapper 분기 / BottomSheet+DesktopModal 인라인 / SKELETON_STYLE / FAB safe-area calc(16px + var(--sab)) / 카피 verbatim 18+ / placeholder 7 / toast StaffModal 8 + ReplaceModal 2) 이 1 byte 변경 0 으로 grep PASS"
    - "OQ LOCKED 6건 적용 — #1 BottomSheet/DesktopModal/ReplaceModalContent/confirmReset/confirmDeactivate 동 파일 인라인 보존 (Modal.tsx/Sheet.tsx 신규 생성 0) / #2 단색 var(--accent) 유지 (linear-gradient 0) / #3 shift 폼 미노출 유지 + role toggle height 36→40(데스크톱)/44(모바일) 격상 / #4 외곽 hex 새 토큰 전체 치환 / #5 9·10·11 → 12 일률 격상 + leading-none / #6 Lucide UserPlus 16/18 치환 + IconUserPlus 함수 제거"
    - "Lucide 1종 import (X/ChevronDown 미사용 — source TSX 부재): import { UserPlus } from 'lucide-react' 신규 + 인라인 SVG 함수 IconUserPlus (line 11~20) 전체 제거"
    - "fontSize 9·10·11 인라인 0 — W5 §7 폰트 매트릭스 따라 모두 text-caption (12px) + leading-none 격상 (line 121 ReplaceModal 보조 / line 224+236 입사일/생년월일 보조 / line 327 StaffCard role 배지 / line 461 데스크톱 테이블 role 배지 / line 471 데스크톱 테이블 status)"
    - "linear-gradient 0 / status- prefix 0 (`bg-status-fire-bar` 형태 NG) / w-8 h-8 0 (8x8 dot → `w-[8px] h-[8px]` / 6x6 dot → `w-[6px] h-[6px]` / handle bar 32x4 → `w-[32px] h-[4px]`) / 이모지 0"
    - "Build PASS — cd cha-bio-safety && npm run build 성공 (tsc --noEmit 0 errors + vite build success + PWA injectManifest PASS). Build 실패 시 commit 보류 (최대 2회 재시도)"
    - "components.css 0 byte diff (이 wave 신규 추가 0 — OQ #1 default + W5 §9 박제) + App.tsx 0 byte diff + src/** 변경 = StaffManagePage.tsx 1 파일만"
    - "bg-accent 실측 키명 사용 (i4b deviation 박제) — tailwind.config theme.extend.colors 의 `accent` 키 grep 확인. `bg-accent-primary` 추측 0 hits / `bg-accent` (또는 `bg-accent-hover` / `bg-accent-active`) 만 적용"
  artifacts:
    - path: "cha-bio-safety/src/pages/StaffManagePage.tsx"
      provides: "StaffManagePage TSX 변환본 (외곽 chrome sketch class + 비즈 로직 0 byte + Lucide 치환 + OQ LOCKED 6)"
      min_lines: 500
      contains: "import { UserPlus } from 'lucide-react'"
  key_links:
    - from: "cha-bio-safety/src/pages/StaffManagePage.tsx (line 1~78)"
      to: "wave-5-tsx-conversion-checklist.md §1 imports + 상수 보존"
      via: "Lucide UserPlus 추가 + IconUserPlus 제거 + BottomSheet/DesktopModal/INPUT_STYLE/LABEL_STYLE/StaffFormState/EMPTY_STAFF_FORM 보존"
      pattern: "import \\{ UserPlus \\} from 'lucide-react'"
    - from: "cha-bio-safety/src/pages/StaffManagePage.tsx (line 81~315, 357~388)"
      to: "wave-5-tsx-conversion-checklist.md §2 메인 함수 + 모달 + ReplaceModal"
      via: "hooks/state/handlers 1:1 verbatim — 비즈 로직 0 byte (useMutation 4 / staffApi 4 / window 훅 / sort)"
      pattern: "createMutation|updateMutation|resetPwMutation|deactivateMutation|__openReplaceModal"
    - from: "StaffManagePage.tsx JSX render 3 영역 (line 389~528)"
      to: "sketch-wave-2~4.html 3 파일 class fence (W5 §6 grep 결과 verbatim)"
      via: "sketch class 매핑 — desktop-frame / desktop-header / data-table / staff-card / sheet-panel / modal-panel / role-toggle / replace-info-box"
      pattern: "(desktop-header|mobile-header|data-table|staff-card|sheet-panel|modal-panel|role-toggle|btn-replace|confirm-reset-box)"
    - from: "비즈 anchor 26건"
      to: "wave-5-tsx-conversion-checklist.md §4 보존 박스 + wave-1-index §1.3"
      via: "1 byte 변경 금지 grep gate"
      pattern: "admin|staffApi\\.|useMutation|rankOfTitle|__openReplaceModal|d\\{10\\}|appointedAt|shiftOffset|shiftFixed|ReplaceModalContent|candidates|shiftLabel"
    - from: "OQ LOCKED 6"
      to: "wave-5-tsx-conversion-checklist.md §5 + wave-1-index §7"
      via: "default (a) x6 — inline / 단색 var(--accent) / shift 미노출+role toggle 40/44 / 토큰 / 12 격상 / Lucide UserPlus"
      pattern: "OQ #[1-6]"
    - from: "verify gate 26 (Build PASS)"
      to: "wave-5-tsx-conversion-checklist.md §11 negative + §12 verify (TSX 전용 20+ 추출)"
      via: "bash gate 20+ + npm run build 성공 후에만 commit. i4b deviation 박제 (bg-accent 실측)"
      pattern: "npm run build"
---

<objective>
redesign/26-staff-manage TSX 변환 wave — StaffManagePage.tsx (530 lines, admin 전용 BottomSheet/DesktopModal+ReplaceModalContent+confirmReset+confirmDeactivate) **단일 파일 atomic 1-commit** 변환.

W5 (`cha-bio-safety/docs/redesign-context/26-staff-manage/wave-5-tsx-conversion-checklist.md`, 660 lines) §1~§12 verbatim 적용 — 외곽 chrome 3 영역 (W2 frame-guard-header / W3 staff-list+FAB / W4 modal-form-confirm) 만 sketch class 로 변환, BottomSheet+DesktopModal+ReplaceModalContent+StaffModalContent+StaffCard+SKELETON_STYLE+rankOfTitle+메인 hook/state/handler 본문 + 비즈 anchor 26건은 1 byte 변경 0.

`feedback_gsd_workflow_strict` — GSD 워크플로 강제. 이 plan 은 minimal brief 단일 atomic 패턴 (i4b 385 mirror, 26 단순하므로 280~360 target).

`i4b deviation 박제`: tailwind.config.js 실측 결과 `theme.extend.colors.accent` 키 — `bg-accent` 만 유효, `bg-accent-primary` 는 존재 X. 추측 금지.

Purpose: 26-staff-manage 종결 → 4차 모니터링 (19/20/21 legal) 진입 가능. 14-reports / 15-daily-report / 18-worklog / 23-education / 25-qr-print / 28-splash / 24-checkpoints 의 단일 파일 atomic 패턴 자동 도달 8번째.
Output: StaffManagePage.tsx 변환본 + Build PASS + atomic 1-commit (변환) + SUMMARY 1-commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@cha-bio-safety/docs/redesign-context/26-staff-manage/wave-5-tsx-conversion-checklist.md
@cha-bio-safety/docs/redesign-context/26-staff-manage/wave-1-index.md
@cha-bio-safety/docs/redesign-context/26-staff-manage/sketch-wave-2-frame-guard-header.html
@cha-bio-safety/docs/redesign-context/26-staff-manage/sketch-wave-3-staff-list.html
@cha-bio-safety/docs/redesign-context/26-staff-manage/sketch-wave-4-modal-form-confirm.html
@cha-bio-safety/src/pages/StaffManagePage.tsx
@cha-bio-safety/tailwind.config.js
@cha-bio-safety/src/styles/tokens.css
@cha-bio-safety/src/styles/typography.css
@cha-bio-safety/src/styles/components.css

<interfaces>
StaffManagePage.tsx 변환 영역 line range 3구역:
- §1 imports + 상수 (line 1~78): hooks/router/react-query/toast/store/api/hook/type 8 import 보존 + Lucide UserPlus 1종 추가 + 인라인 SVG 함수 IconUserPlus (line 11~20) 전체 제거 + BottomSheet (line 22~40) / DesktopModal (line 42~57) / INPUT_STYLE+LABEL_STYLE (line 60~67) / StaffFormState+EMPTY_STAFF_FORM (line 70~78) verbatim 보존
- §2 메인 함수 + 모달 (line 81~315, 357~388): ReplaceModalContent (line 81~149) / StaffModalContent hook+state+5 mutation handler (line 151~315) / StaffCard (line 318~339) / SKELETON_STYLE+rankOfTitle (line 342~354) / StaffManagePage 메인 hook 4 + window 훅 + admin 가드 useEffect + useQuery + sort + early return + ModalWrapper — **본문 0 byte**
- §3 JSX render (line 389~528): 외곽 wrapper + style 태그 (W2) / 데스크톱+모바일 헤더 (W2) / 콘텐츠 4상태 분기 (skeleton/error/empty/data) + 데스크톱 테이블 + 모바일 StaffCard 리스트 + FAB (W3) / 모달 ModalWrapper 호출 2종 + StaffModalContent 폼 + confirmReset + confirmDeactivate + 액션 row (W4) — sketch class verbatim 적용

비즈 anchor 26건 (W5 §4 verbatim, 1 byte 변경 0 강제):
1. admin 가드 useEffect (line 371~373) + early return (line 385)
2. queryKey ['staff-list'] 단일 (line 87 ReplaceModal, line 376 메인) + invalidate 4건 (line 106, 168, 174, 186)
3. staffList sort: rankOfTitle 차 → 0이면 a.id.localeCompare(b.id) (line 380~383)
4. createMutation (line 166~170) — staffApi.create + invalidate+toast+onClose
5. updateMutation (line 172~176) — staffApi.update(staff!.id, data: StaffUpdatePayload)
6. resetPwMutation (line 178~182) — staffApi.resetPassword + '비밀번호가 초기화되었습니다 (사번 뒷 4자리)'
7. deactivateMutation (line 184~188) — staffApi.update({ active: 0 })
8. canSave (line 190): form.name.trim() !== '' && form.id.trim() !== ''
9. 사번 10자리 정규식 (line 194): /^\d{10}$/.test(form.id)
10. appointedAt 자동 채움 (line 227~230): p=form.id.slice(0,8) + /^[0-9]{8}$/.test(p) + yyyy-mm-dd
11. shiftOffset 0~3 + shiftFixed 'day'/null (line 76~78, 157) — 폼 UI 미노출, ReplaceModal 시퀀스에서만 변경
12. ReplaceModalContent 2× staffApi.update 시퀀스 (line 93~114): 1. selectedId 에 shift 이전 / 2. oldStaff active:0+phone:''+email:''+shift:null
13. candidates 필터 (line 88): active===1 && id!==oldStaff.id && shiftOffset===null && shiftFixed===null
14. shiftLabel 분기 (line 90~91): '평일 주간 고정' / `3교대 (오프셋 ${N})` / '미설정'
15. window.__openReplaceModal 훅 (line 365~368) + line 292 setTimeout 100ms
16. rankOfTitle (line 347~354): 대리=0, 주임=1, 기사=2, 기타=3
17. role 옵션 (line 246): (['admin', 'assistant'] as Role[]) + 라벨 '관리자'/'보조자'
18. ModalWrapper (line 387) + 모달 title 분기 '직원 추가'/'직원 수정' (line 517) + '직원 교체' (line 524)
19. BottomSheet (line 22~40) — overlay rgba(0,0,0,0.6) / bg var(--bg2) / radius 16/16/0/0 / slideUp 0.28s / maxHeight 90vh / handle 32x4
20. DesktopModal (line 42~57) — overlay rgba(0,0,0,0.5) / radius 12 / width 440 / maxHeight 85vh / boxShadow
21. SKELETON_STYLE (line 342~345) — bg var(--bg3) / radius 12 / height 64 / blink 2s
22. 모바일 FAB safe-area (line 506): `paddingBottom: 'calc(16px + var(--sab))'`
23. 카피 verbatim 18+: '직원 관리' / '{N}명' / '직원 추가' / '직원 수정' / '직원 교체' / '데이터를 불러오지 못했습니다. 잠시 후 다시 시도해 주세요' / '등록된 직원이 없습니다' / '직원 추가 버튼을 눌러 첫 번째 직원을 등록하세요' / '저장'/'취소' / '비활성화' / '교체' / '이 직원을 비활성화합니다. 점검 기록은 보존됩니다.' / '비밀번호 초기화' / '사번 뒷 4자리로 비밀번호를 초기화합니다. 계속하시겠습니까?' / '초기화' / '관리자'/'보조자' / '활성'/'비활성' / '수정 ▸' / '수정' / '(사번 앞 8자리에서 자동)' / '(휴가신청서 자동 채움)'
24. placeholder verbatim 7: '홍길동' / '0000000000' / '010-0000-0000' / 'email@example.com' / '사번 입력 시 자동 채워짐' / '소방안전관리자' / '선택하세요'
25. toast StaffModal 8: '직원이 추가되었습니다' / '직원 정보가 수정되었습니다' / '저장에 실패했습니다. 입력값을 확인해 주세요' x2 / '비밀번호가 초기화되었습니다 (사번 뒷 4자리)' / '비밀번호 초기화에 실패했습니다' / '직원이 비활성화되었습니다' / '비활성화에 실패했습니다' / '사번은 10자리 숫자여야 합니다'
26. toast ReplaceModal 2: `${oldStaff.name} → ${newStaff.name} 교체 완료` (line 107) / `e.message || '교체 실패'` (line 110)

Lucide 1종 import (X / ChevronDown 미사용 — source TSX 부재 확인):
```typescript
import { UserPlus } from 'lucide-react'
```
- UserPlus size={16} (line 405 데스크톱 헤더 '직원 추가' button)
- UserPlus size={18} (line 509 모바일 FAB — **18 보존**, OQ #6 default)

OQ LOCKED 6건 적용 위치 (W5 §5 verbatim):
- OQ #1: BottomSheet (line 22~40) + DesktopModal (line 42~57) + ReplaceModalContent (line 81~149) + StaffModalContent confirmReset/confirmDeactivate (line 255~312) 동 파일 인라인 보존 — src/components/ui/Modal.tsx 또는 Sheet.tsx 신규 생성 0
- OQ #2: 데스크톱 '직원 추가' (line 403) / 모바일 FAB (line 507) / 저장 (line 281) 3건 모두 단색 `var(--accent)` — linear-gradient 0. 교체 버튼은 warning 톤 (`#f59e0b` / `rgba(245,158,11,.1)`) 유지
- OQ #3: shift 폼 UI 미노출 (admin 자동 채움) + role toggle button row (line 245~253) height 36 → **40(데스크톱)/44(모바일) 격상** + line 248 fontSize 12 → `text-caption font-bold`
- OQ #4: 외곽 hex 새 토큰 전체 치환 (design-system §4.1 마이그레이션 표): var(--bg)→`bg-surface-page` / --bg2→`bg-surface-raised` / --bg3→`bg-surface-sunken` / --bg4→`bg-surface-active` / --bd→`border-border-default` / --bd2→`border-border-strong` / --t1→`text-text-primary` / --t2→`text-text-secondary` / --t3→`text-text-tertiary` / --acl→`bg-accent`/`text-accent` (i4b 실측) / --safe→`bg-safe-bar`/`text-safe-bar` / --danger→`bg-danger-bar` / --warn→`bg-warning-bar`
- OQ #5: 9·10·11 → 12 일률 격상 + leading-none — line 121 (ReplaceModal 보조 11) / line 224 (입사일 보조 10) / line 236 (생년월일 보조 10) / line 327 (StaffCard role 배지 9) / line 461 (테이블 role 배지 10) / line 471 (테이블 status 11)
- OQ #6: Lucide UserPlus 치환 + size 보존 — size={16}/size={18}. IconUserPlus 함수 (line 11~20) 제거

components.css 0 byte (W5 §9 박제 — 신규 추가 0, OQ #1 default + 단일 파일 인라인 패턴):
- 재사용만: tokens.css + typography.css 기존 class (`.bg-surface-page` / `.text-text-primary` / `.text-safe-bar` / `.text-caption` / `.text-label` / `.rounded-md` 등)
- 신규 추가 없음 — BottomSheet/DesktopModal/StaffModalContent/ReplaceModalContent/StaffCard/SKELETON 모두 동 파일 인라인 유지

**tailwind.config 실측 (i4b deviation 박제 — 필수)**:
- `theme.extend.colors.accent` = 'var(--accent)' (line 60) — `bg-accent` / `text-accent` 만 유효
- `bg-accent-primary` NG — 키 부재
- status- prefix 0: `bg-safe-bar` (O) / `bg-status-safe-bar` (X — 키 부재)
- 8x8 dot → `w-[8px] h-[8px]` (w-8 = 48px spacing override, `feedback_tailwind_w8_h8_is_48px`)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>T1: StaffManagePage.tsx 단일 atomic 변환 + Build PASS</name>
  <files>cha-bio-safety/src/pages/StaffManagePage.tsx</files>
  <action>
W5 markdown (`cha-bio-safety/docs/redesign-context/26-staff-manage/wave-5-tsx-conversion-checklist.md`) §1~§12 verbatim 적용. 단일 파일 in-place 교체, 1 atomic commit.

**Step 0: tailwind.config 실측 (i4b deviation 박제)**
- `grep -nE 'accent|surface-page|safe-bar|danger-bar|warning-bar' cha-bio-safety/tailwind.config.js` 로 키명 사전 확인. 결과: `accent` (단일 키), `accent-hover`, `accent-active`. `accent-primary` 부재 — 추측 금지.

**Step 1: §1 imports + 상수 (line 1~78) 변환**
- line 1~8 기존 8 import 그대로 유지.
- line 9 (또는 새 라인): `import { UserPlus } from 'lucide-react'` 신규 추가.
- line 11~20 IconUserPlus 인라인 SVG 함수 **전체 제거** (Lucide 치환 후 미사용).
- line 22~40 BottomSheet — **인라인 보존 (OQ #1 default)**. src/components/ui/Modal.tsx 또는 Sheet.tsx 신규 생성 0.
- line 42~57 DesktopModal — **인라인 보존 (OQ #1 default)**.
- line 60~67 INPUT_STYLE / LABEL_STYLE — 보존 (폼 필드 1:1 매핑 안정성 우선).
- line 70~78 StaffFormState / EMPTY_STAFF_FORM — verbatim 보존 (10 필드, shiftOffset+shiftFixed 포함, 폼 UI 미노출).

**Step 2: §2 메인 함수 + 모달 (line 81~388) — 본문 0 byte**
- ReplaceModalContent (line 81~149): useState 2 / allStaff useQuery / candidates 필터 / shiftLabel 분기 3종 / handleReplace 2× staffApi.update 시퀀스 / toast 2 — **1 byte 변경 0**.
- StaffModalContent (line 151~315): useState 3 (form/confirmReset/confirmDeactivate) / setField / useMutation 4 (create/update/resetPw/deactivate) / canSave / handleSave 정규식 / isBusy — **1 byte 변경 0**.
- StaffCard (line 318~339): props + 8x8 dot + name+title+role 배지+id+'수정 ▸' — JSX 영역에서 sketch class 적용 (Step 3 §3.3).
- SKELETON_STYLE (line 342~345) + rankOfTitle (line 347~354) — **1 byte 변경 0**.
- StaffManagePage 메인 (line 357~388): hook 4 / window.__openReplaceModal 훅 useEffect+cleanup / admin 가드 useEffect / useQuery+sort / early return / ModalWrapper — **1 byte 변경 0**.

**Step 3: §3 JSX render (line 389~528) — sketch class 3 영역 적용**

§3.1 W2 외곽 wrapper + style 태그 + 헤더 (line 389~413):
- 외곽 wrapper (line 390): `style={{ display:'flex', flexDirection:'column', background:'var(--bg)', height:'100%', overflow:'hidden' }}` → `className="flex flex-col h-full overflow-hidden bg-surface-page"` (OQ #4 토큰 치환).
- `<style>` 태그 (line 391~395) — keyframes blink + slideUp + focus 그대로 유지 (BottomSheet slideUp 0.28s 의존, **0 byte 변경**).
- 데스크톱 헤더 (line 398~408): `hidden lg:flex items-center px-6 py-3 border-b border-border-default`. 타이틀 '직원 관리' fontSize 14 fw700 → `text-body-sm font-bold text-text-primary` + 카운트 '{N}명' fontSize 12 → `text-caption text-text-tertiary ml-2`. '직원 추가' button (line 403~407): height 36 → OQ #3 default 40 격상: `flex items-center gap-2 h-10 px-3 rounded-sm bg-accent text-white text-label font-bold` + `<UserPlus size={16} color="#fff" />` (line 405, Lucide 치환).
- 모바일 헤더 (line 409~413): `flex lg:hidden items-center px-4 py-2`. 카운트 fontSize 12 → `text-caption text-text-tertiary`.

§3.2 W3 콘텐츠 (목록+FAB) (line 416~513):
- 콘텐츠 wrapper (line 416): `flex-1 overflow-auto min-h-0`.
- 4 상태 분기:
  - skeleton 3개 (line 417~422): `SKELETON_STYLE` 인라인 보존 — bg var(--bg3)→`bg-surface-sunken` / radius 12 / height 64 / blink 2s — **시각 변경 0**.
  - error (line 424~427): `text-center py-10 px-4 text-text-secondary text-body-sm` + 카피 '데이터를 불러오지 못했습니다. 잠시 후 다시 시도해 주세요' verbatim.
  - 데스크톱 empty (line 446~448): colSpan 7 + `text-center py-10 px-4 text-text-tertiary text-body-sm` + 카피 '등록된 직원이 없습니다' verbatim.
  - 모바일 empty (line 491~496): `class="mobile-empty"` + title fontSize 16 fw700 → `text-body font-bold text-text-primary` + desc fontSize 12 → `text-caption text-text-secondary`.
- 데스크톱 테이블 (line 431~486): `class="data-table"` / `class="table-wrap"` / `class="desktop-content"`. 7 컬럼: 이름/사번/직책/역할/연락처/상태/액션.
  - thead (line 436~441 fontSize 12 fw700): `text-caption font-bold text-text-secondary`.
  - 사번 (line 457 JetBrains Mono fontSize 12): `text-caption font-mono`.
  - role 배지 (line 460~466 fontSize 10 fw700): `text-caption leading-none px-1.5 py-0.5 rounded` (OQ #5, 10→12). 배지 색은 admin/assistant 분기 인라인 RGBA 보존.
  - status (line 470~476 fontSize 11 fw600): `text-caption leading-none inline-flex items-center gap-1` (OQ #5, 11→12). 6x6 dot → `w-[6px] h-[6px] rounded-full` (**w-8 = 48px 함정 회피**).
  - 액션 '수정' (line 479 fontSize 12 fw700): `text-caption font-bold text-accent` (i4b 실측 — `bg-accent-primary` NG).
  - row hover (line 453~454): `hover:bg-surface-sunken` 또는 onMouseEnter/Leave 보존.
- StaffCard 모바일 (line 318~339, 호출 line 497~499): `class="staff-card"` / inactive 시 `opacity-[0.45]`.
  - 8x8 dot (line 321): `<div className="w-[8px] h-[8px] rounded-full" style={{ background: staff.active !== 0 ? 'var(--status-safe-bar)' : 'var(--text-tertiary)', flexShrink: 0 }} />` (**w-8 함정 회피**).
  - name (line 324 fontSize 16 fw700): `text-body font-bold text-text-primary`.
  - title (line 325 fontSize 12): `text-caption text-text-secondary`.
  - role 배지 (line 326~332 fontSize 9 fw700): `text-caption leading-none px-1.5 py-0.5 rounded` (OQ #5, 9→12). 배경 RGBA 인라인 보존 (admin: rgba(59,130,246,.13) / assistant: rgba(110,118,129,.15)).
  - id (line 334 fontSize 12 JetBrains Mono): `text-caption font-mono text-text-tertiary`.
  - '수정 ▸' (line 336 fontSize 12 fw700): `text-caption leading-none text-accent font-bold` (`feedback_text_caption_leading_none`).
- 모바일 FAB (line 505~513):
  - 외곽 (line 506): `style={{ position:'sticky', bottom:0, padding:'0 16px', paddingBottom:'calc(16px + var(--sab))', background:'var(--bg)' }}` — **safe-area `calc(16px + var(--sab))` 1 byte 변경 0** (iOS PWA, `feedback_viewport_units_per_platform`).
  - button (line 507~511): `w-full h-[52px] bg-accent text-white rounded-md flex items-center justify-center gap-2 text-body-sm font-bold` + `<UserPlus size={18} color="#fff" />` + '직원 추가' 카피.
  - OQ #2: linear-gradient 0 (단색 `var(--accent)`).
  - OQ #6: size={18} **보존**.

§3.3 W4 모달 호출 + 폼 + confirm 3종 (line 515~527, + StaffModalContent line 204~313 + ReplaceModalContent line 116~148):
- `{modal.open && <ModalWrapper onClose={...} title={modal.mode === 'add' ? '직원 추가' : '직원 수정'}>...</ModalWrapper>}` (line 516~520) — **1 byte 변경 0**.
- `{replaceModal.open && replaceModal.target && <ModalWrapper onClose={...} title="직원 교체">...</ModalWrapper>}` (line 523~527) — **1 byte 변경 0**.
- BottomSheet / DesktopModal 인라인 보존 (line 22~57) — OQ #1.
- 폼 필드 (StaffModalContent line 207~253): `form-field` / `form-label` / `form-required` (빨간 *) / `form-input` / `form-input mono` (사번) / `form-input disabled` (edit 시 사번/입사일).
  - 이름/사번/연락처/이메일/입사일/생년월일/직책 — INPUT_STYLE / LABEL_STYLE 보존 + className 보강.
  - 입사일·생년월일 보조 라벨 (line 224, 236 fontSize 10) → `text-caption text-text-tertiary` (OQ #5, 10→12).
  - 역할 toggle (line 243~253): `class="role-toggle"` border + radius 8 overflow hidden. button height 36 → **OQ #3 격상 `h-10 lg:h-10` (데스크톱 40) / 모바일 `h-11` (44)** + fontSize 12 fw700 → `text-caption font-bold`. selected: `bg-accent text-white` / unselected: `bg-surface-active text-text-tertiary`. `(['admin', 'assistant'] as Role[])` 라벨 '관리자'/'보조자' verbatim.
- confirmReset (line 255~274): `class="confirm-reset-link"` text-warning-bar underline / `class="confirm-reset-box"` `bg-warning-bg` (또는 rgba(245,158,11,.08) 인라인 보존) / `class="confirm-reset-text"`. 카피 verbatim '비밀번호 초기화' / '사번 뒷 4자리로 비밀번호를 초기화합니다. 계속하시겠습니까?' / '취소' / '초기화'. small-btn height 32 보존.
- 액션 row (line 277~312):
  - 정상 row (line 278~298): 취소 (line 280 14fw700 h-44) / 저장 (line 281 14fw700 h-44, **bg var(--accent) 단색 — linear-gradient 0**). edit + active!==0 시: 비활성화 (line 288, height 40, bg-danger-bg + text-danger) / 교체 (line 292, height 40, bg `rgba(245,158,11,.1)` + text `#d97706` 또는 `text-warning-bar` 단색 보존). line 292 `setTimeout(() => (window as any).__openReplaceModal?.(staff), 100)` 1 byte 변경 0.
  - confirmDeactivate row (line 300~310): `class="confirm-deactivate-box"` bg-danger-bg + '이 직원을 비활성화합니다. 점검 기록은 보존됩니다.' verbatim / 취소 + 비활성화 (bg-danger-bar text-white).
- ReplaceModalContent (line 116~148):
  - `class="replace-info-box"` `rgba(59,130,246,.08)` 인라인 보존 (또는 `bg-info-bg`) + info sub fontSize 11 → `text-caption` (OQ #5).
  - candidates.length === 0: `class="replace-no-candidates"` `bg-surface-sunken text-caption text-text-tertiary`.
  - select (line 131~136): INPUT_STYLE 보존 + cursor pointer.
  - 액션 row (line 140~145): 취소 (bg-surface-active text-text-secondary) / 교체 (bg `#f59e0b` text-white, submitting/disabled 시 opacity 0.4) — `'처리 중...'` / `'교체'` 카피 verbatim.
- 카피/placeholder/toast verbatim 보존 (위 §interfaces row 23~26 전체).

**Step 4: 검증 — npm run build (Build 실패 시 commit 보류)**
- `cd cha-bio-safety && npx tsc --noEmit` — 0 errors.
- `cd cha-bio-safety && npm run build` — PASS (vite build success + PWA injectManifest PASS). StaffManagePage chunk size 보고.
- Build 실패 시: 오류 수정 후 최대 2회 재시도. 2회 실패 시 deviation 보고 + commit 보류 (`feedback_check_branch_before_edit`).

**Step 5: atomic commit (1 commit, 1 파일)**
```bash
git add cha-bio-safety/src/pages/StaffManagePage.tsx
git commit -m "feat(quick-260526-oh0): redesign/26-staff-manage TSX 변환 (StaffManagePage 530→XXX + v0.1.1 토큰 className + 비즈 anchor 26 보존 + Lucide UserPlus + OQ LOCKED 6 + W5 §1~§12 verbatim)"
```
(XXX 는 실제 line 수)

메모리 룰 inline 15 slug (W5 §메모리 박제, plan 본문 echo 금지):
feedback_planner_prompt_sketch_verbatim / feedback_redesign_sketch_rule_enforcement / feedback_sketch_realistic_data / feedback_tsx_wave_emoji_dot_gap / feedback_tsx_wave_stat_card_drift / feedback_text_caption_leading_none / feedback_tailwind_token_class_pattern / feedback_tailwind_w8_h8_is_48px / feedback_cbc7119_design_never_wrangler / feedback_design_changes_ask_first / feedback_design_sketch_first / feedback_avoid_premature_confirmation / project_redesign_15_daily_report_status / project_div_compressor_pair / project_meal_calc_rules.
  </action>
  <verify>
    <automated>
# ── TSX 전용 26 gate (W5 §11 negative + §12 verify 추출) ──

# (1) StaffManagePage.tsx 존재 + min_lines 500
test -f cha-bio-safety/src/pages/StaffManagePage.tsx
test "$(wc -l < cha-bio-safety/src/pages/StaffManagePage.tsx)" -ge "500"

# (2) cd cha-bio-safety && npm run build PASS (Build 가 없으면 commit 금지)
(cd cha-bio-safety && npm run build)

# (3) tsc --noEmit 0 errors (별도 안전 확인)
(cd cha-bio-safety && npx tsc --noEmit)

# (4) Lucide UserPlus import grep PASS
grep -E "import \{[^}]*UserPlus[^}]*\} from 'lucide-react'" cha-bio-safety/src/pages/StaffManagePage.tsx

# (5) linear-gradient 0 (OQ #2)
test "$(grep -c "linear-gradient" cha-bio-safety/src/pages/StaffManagePage.tsx)" = "0"

# (6) fontSize 9/10/11 인라인 0 — JSX render 영역 (line 389~528) 및 모달 내부 (line 121, 224, 236, 327, 461, 471)
# 6개 위반 라인 모두 text-caption 격상 후 fontSize 숫자 인라인 잔존 0
test "$(grep -cE "fontSize:\s*(9|10|11)([^0-9]|$)" cha-bio-safety/src/pages/StaffManagePage.tsx)" = "0"

# (7) font-size 9/10/11px (CSS-in-JS) 0
test "$(grep -cE "font-size:\s*(9|10|11)px" cha-bio-safety/src/pages/StaffManagePage.tsx)" = "0"

# (8) status- prefix 0 (className Tailwind utility — feedback_tailwind_token_class_pattern)
test "$(grep -cE 'className=[^>]*"[^"]*(bg-status-|text-status-|border-status-)' cha-bio-safety/src/pages/StaffManagePage.tsx)" = "0"

# (9) w-8 / h-8 (48px 사고 방지) 0 — Tailwind config spacing override
test "$(grep -cE 'className=[^>]*"[^"]*\b(w-8|h-8)\b' cha-bio-safety/src/pages/StaffManagePage.tsx)" = "0"

# (10) 이모지 0
test "$(LC_ALL=C grep -cP '[\x{1F300}-\x{1FAFF}]' cha-bio-safety/src/pages/StaffManagePage.tsx)" = "0"

# (11) admin 가드 useEffect + early return grep PASS (2 hits 이상)
test "$(grep -c "me?.role !== 'admin'" cha-bio-safety/src/pages/StaffManagePage.tsx)" -ge "2"

# (12) staffApi 4종 호출 grep PASS
grep -q "staffApi.list" cha-bio-safety/src/pages/StaffManagePage.tsx
grep -q "staffApi.create" cha-bio-safety/src/pages/StaffManagePage.tsx
grep -q "staffApi.update" cha-bio-safety/src/pages/StaffManagePage.tsx
grep -q "staffApi.resetPassword" cha-bio-safety/src/pages/StaffManagePage.tsx

# (13) useMutation 4건 PASS (create + update + resetPw + deactivate)
test "$(grep -c "useMutation" cha-bio-safety/src/pages/StaffManagePage.tsx)" -ge "4"
grep -q "createMutation" cha-bio-safety/src/pages/StaffManagePage.tsx
grep -q "updateMutation" cha-bio-safety/src/pages/StaffManagePage.tsx
grep -q "resetPwMutation" cha-bio-safety/src/pages/StaffManagePage.tsx
grep -q "deactivateMutation" cha-bio-safety/src/pages/StaffManagePage.tsx

# (14) staff 카드 필드 8종 grep (name/staffId/role/phone/email/birthDate/shiftOffset/shiftFixed)
grep -q "form.name" cha-bio-safety/src/pages/StaffManagePage.tsx
grep -q "form.id" cha-bio-safety/src/pages/StaffManagePage.tsx
grep -q "form.role" cha-bio-safety/src/pages/StaffManagePage.tsx
grep -q "form.phone" cha-bio-safety/src/pages/StaffManagePage.tsx
grep -q "form.email" cha-bio-safety/src/pages/StaffManagePage.tsx
grep -q "form.birthDate" cha-bio-safety/src/pages/StaffManagePage.tsx
grep -qE "shiftOffset" cha-bio-safety/src/pages/StaffManagePage.tsx
grep -qE "shiftFixed" cha-bio-safety/src/pages/StaffManagePage.tsx

# (15) role admin/assistant 옵션 grep PASS
grep -qE "\\['admin',\\s*'assistant'\\]\\s+as\\s+Role\\[\\]" cha-bio-safety/src/pages/StaffManagePage.tsx
grep -q "'관리자'" cha-bio-safety/src/pages/StaffManagePage.tsx
grep -q "'보조자'" cha-bio-safety/src/pages/StaffManagePage.tsx

# (16) shiftFixed 'day' + shiftOffset null 처리 grep PASS
grep -q "shiftFixed === 'day'" cha-bio-safety/src/pages/StaffManagePage.tsx
grep -q "shiftOffset === null" cha-bio-safety/src/pages/StaffManagePage.tsx

# (17) duty 토큰 (필요 시) 또는 shiftLabel 분기 verbatim grep PASS — '평일 주간 고정' / '3교대 (오프셋' / '미설정'
grep -q "'평일 주간 고정'" cha-bio-safety/src/pages/StaffManagePage.tsx
grep -qE "3교대 \(오프셋" cha-bio-safety/src/pages/StaffManagePage.tsx
grep -q "'미설정'" cha-bio-safety/src/pages/StaffManagePage.tsx

# (18) 비밀번호 초기화 confirm 카피 verbatim grep PASS
grep -q "비밀번호 초기화" cha-bio-safety/src/pages/StaffManagePage.tsx
grep -q "사번 뒷 4자리로 비밀번호를 초기화합니다" cha-bio-safety/src/pages/StaffManagePage.tsx
grep -q "비밀번호가 초기화되었습니다 (사번 뒷 4자리)" cha-bio-safety/src/pages/StaffManagePage.tsx

# (19) ReplaceModalContent 2× staffApi.update 시퀀스 grep PASS — handleReplace 함수 내부
grep -q "ReplaceModalContent" cha-bio-safety/src/pages/StaffManagePage.tsx
# update 호출 ≥3 (resetPw 1 + update 2 in ReplaceModal + update 2 in StaffModal(updateMutation + deactivateMutation) — 총 ≥5)
test "$(grep -c "staffApi.update" cha-bio-safety/src/pages/StaffManagePage.tsx)" -ge "5"
grep -qE "active:\\s*0,\\s*phone:\\s*'',\\s*email:\\s*''" cha-bio-safety/src/pages/StaffManagePage.tsx

# (20) window.__openReplaceModal 훅 grep PASS (등록 + cleanup + 호출 3 hits)
test "$(grep -c "__openReplaceModal" cha-bio-safety/src/pages/StaffManagePage.tsx)" -ge "3"

# (21) rankOfTitle 정렬 grep PASS
grep -q "function rankOfTitle" cha-bio-safety/src/pages/StaffManagePage.tsx
grep -q "rankOfTitle(a.title)" cha-bio-safety/src/pages/StaffManagePage.tsx

# (22) 사번 10자리 정규식 grep PASS
grep -qE "\\^\\\\d\\{10\\}\\\$" cha-bio-safety/src/pages/StaffManagePage.tsx
grep -q "사번은 10자리 숫자여야 합니다" cha-bio-safety/src/pages/StaffManagePage.tsx

# (23) appointedAt 자동 채움 grep PASS — slice(0,8) + /^[0-9]{8}$/
grep -q "form.id" cha-bio-safety/src/pages/StaffManagePage.tsx
grep -qE "slice\\(0,\\s*8\\)" cha-bio-safety/src/pages/StaffManagePage.tsx
grep -qE "\\^\\[0-9\\]\\{8\\}\\\$" cha-bio-safety/src/pages/StaffManagePage.tsx

# (24) bg-accent (i4b deviation 박제 — 실측 키명) grep PASS / bg-accent-primary 0 hits
test "$(grep -cE 'className=[^>]*"[^"]*\bbg-accent\b' cha-bio-safety/src/pages/StaffManagePage.tsx)" -ge "1"
test "$(grep -c "bg-accent-primary" cha-bio-safety/src/pages/StaffManagePage.tsx)" = "0"

# (25) components.css 0 byte diff
test "$(git diff origin/main..HEAD -- cha-bio-safety/src/styles/components.css | wc -l)" = "0"

# (26) App.tsx 0 byte diff + src/** 변경 = StaffManagePage.tsx 1 파일만
test "$(git diff origin/main..HEAD --name-only -- cha-bio-safety/src/App.tsx | wc -l)" = "0"
test "$(git diff origin/main..HEAD --name-only -- cha-bio-safety/src/ | wc -l)" = "1"
git diff origin/main..HEAD --name-only -- cha-bio-safety/src/ | grep -q "^cha-bio-safety/src/pages/StaffManagePage.tsx$"

# ── 추가 positive gate (워크트리 룰) ──
# wrangler 0 / npm run deploy 0
test "$(grep -cE "wrangler|npm run deploy" cha-bio-safety/src/pages/StaffManagePage.tsx)" = "0"
# IconUserPlus 인라인 SVG 함수 제거 확인
test "$(grep -cE "^function IconUserPlus" cha-bio-safety/src/pages/StaffManagePage.tsx)" = "0"
# Lucide UserPlus 사용 ≥2 (데스크톱 헤더 + 모바일 FAB)
test "$(grep -c "<UserPlus " cha-bio-safety/src/pages/StaffManagePage.tsx)" -ge "2"
# BottomSheet / DesktopModal / ReplaceModalContent / StaffModalContent / StaffCard 함수 보존
grep -q "function BottomSheet" cha-bio-safety/src/pages/StaffManagePage.tsx
grep -q "function DesktopModal" cha-bio-safety/src/pages/StaffManagePage.tsx
grep -q "function ReplaceModalContent" cha-bio-safety/src/pages/StaffManagePage.tsx
grep -q "function StaffModalContent" cha-bio-safety/src/pages/StaffManagePage.tsx
grep -q "function StaffCard" cha-bio-safety/src/pages/StaffManagePage.tsx
# ModalWrapper 분기 + 타이틀 카피 verbatim
grep -q "isDesktop ? DesktopModal : BottomSheet" cha-bio-safety/src/pages/StaffManagePage.tsx
grep -q "'직원 추가'" cha-bio-safety/src/pages/StaffManagePage.tsx
grep -q "'직원 수정'" cha-bio-safety/src/pages/StaffManagePage.tsx
grep -q "직원 교체" cha-bio-safety/src/pages/StaffManagePage.tsx
# 모바일 FAB safe-area 1 byte 0
grep -q "calc(16px + var(--sab))" cha-bio-safety/src/pages/StaffManagePage.tsx

echo "── ALL 26 gate PASS ──"
echo "StaffManagePage chunk size:"
ls -lh cha-bio-safety/dist/assets/ 2>/dev/null | grep -i "staff\|manage\|index" || true
    </automated>
  </verify>
  <done>
- StaffManagePage.tsx 1 파일 atomic commit 완료 (커밋 메시지: "feat(quick-260526-oh0): redesign/26-staff-manage TSX 변환 (StaffManagePage 530→XXX + v0.1.1 토큰 className + 비즈 anchor 26 보존 + Lucide UserPlus + OQ LOCKED 6 + W5 §1~§12 verbatim)")
- TSX 전용 26 gate PASS — Build PASS + Lucide UserPlus import + linear-gradient 0 + status- prefix 0 + w-8/h-8 0 + 이모지 0 + 비즈 anchor 26 grep PASS + components.css 0 byte + App.tsx 0 byte + src/** 변경 1 파일만 + bg-accent (실측, NOT bg-accent-primary)
- npm run build PASS (vite + PWA injectManifest) — Build 실패 시 commit 보류
- OQ LOCKED 6 적용 — #1 BottomSheet/DesktopModal/ReplaceModal/confirmReset/confirmDeactivate 동 파일 인라인 / #2 단색 var(--accent) / #3 shift 폼 미노출 + role toggle 36→40/44 / #4 외곽 hex 새 토큰 / #5 9·10·11→12 일률 격상 + leading-none / #6 Lucide UserPlus 16/18 치환
- 비즈 anchor 26건 1 byte 변경 0 (admin 가드 / staffApi 4 / queryKey ['staff-list']+invalidate 4 / useMutation 4 / sort+rankOfTitle / 정규식 / appointedAt / shift / ReplaceModal 시퀀스 / candidates / shiftLabel / window 훅 / role 옵션 / ModalWrapper / BottomSheet / DesktopModal / SKELETON / FAB safe-area / 카피 18+ / placeholder 7 / toast 8+2)
  </done>
</task>

</tasks>

<verification>
- TSX 전용 26 gate (위 task verify) 모두 PASS — Build PASS 포함, components.css 0 byte / App.tsx 0 byte / src/** 변경 1 파일만 / 이모지 0 / fontSize 9·10·11 인라인 0 / linear-gradient 0 / status- prefix 0 / w-8 h-8 0 / wrangler 0 / npm run deploy 0 / bg-accent-primary 0 hits / bg-accent (실측) ≥1 hit
- Build gate (필수) — `cd cha-bio-safety && npm run build` PASS + chunk size 보고. Build 실패 시 최대 2회 재시도 후 deviation 보고 + commit 보류.
- 비즈 anchor 26건 grep PASS + Lucide UserPlus import + 인라인 SVG 함수 IconUserPlus 제거.
- 작업 종료 누적: `git log --oneline origin/main..HEAD` ≈ 2 commit (T1 atomic 변환 1 + SUMMARY 1).
- 디자인 작업 룰: wrangler 명령 / npm run deploy 절대 금지 (이 워크트리 CLAUDE.local.md deny). main 머지 시 GitHub Actions 자동 cbc7119-preview 배포.
- i4b deviation 박제 — tailwind.config 실측: `theme.extend.colors.accent` 키만 유효. `bg-accent-primary` 추측 사고 재발 방지.
- 메모리 룰 15 slug 적용 — `feedback_planner_prompt_sketch_verbatim` / `feedback_redesign_sketch_rule_enforcement` / `feedback_sketch_realistic_data` / `feedback_tsx_wave_emoji_dot_gap` / `feedback_tsx_wave_stat_card_drift` / `feedback_text_caption_leading_none` / `feedback_tailwind_token_class_pattern` / `feedback_tailwind_w8_h8_is_48px` / `feedback_cbc7119_design_never_wrangler` / `feedback_design_changes_ask_first` / `feedback_design_sketch_first` / `feedback_avoid_premature_confirmation` / `project_redesign_15_daily_report_status` / `project_div_compressor_pair` / `project_meal_calc_rules`.
</verification>

<success_criteria>
- StaffManagePage.tsx 의 외곽 chrome 3 영역 (W2 frame-guard-header 외곽 wrapper + style 태그 + 데스크톱+모바일 헤더 / W3 콘텐츠 4상태 분기 + 데스크톱 테이블 + 모바일 StaffCard 리스트 + FAB / W4 ModalWrapper 호출 2종 + StaffModalContent 폼 + confirmReset + confirmDeactivate + 액션 row + ReplaceModalContent) 이 W5 §3 sketch class verbatim 으로 변환
- BottomSheet (line 22~40) + DesktopModal (line 42~57) + ReplaceModalContent (line 81~149) + StaffModalContent (line 151~315) + StaffCard (line 318~339) + SKELETON_STYLE (line 342~345) + rankOfTitle (line 347~354) + 메인 hook/state/handler / window 훅 / admin 가드 / useQuery+sort — **1 byte 변경 0** (OQ #1 default)
- 비즈 anchor 26건 grep PASS (admin 가드 / staffApi 4 / queryKey ['staff-list']+invalidate 4 / useMutation 4 / sort+rankOfTitle / canSave / 사번 10자리 정규식 / appointedAt slice(0,8) / shiftOffset+shiftFixed / ReplaceModal 2× update 시퀀스 / candidates 필터 / shiftLabel 3종 / window 훅+setTimeout 100ms / role 옵션 / ModalWrapper / BottomSheet+DesktopModal / SKELETON / FAB safe-area / 카피 18+ / placeholder 7 / toast 8+2)
- Lucide UserPlus 1종 import + 인라인 SVG 함수 IconUserPlus 제거 + 사용 ≥2 위치 (데스크톱 헤더 size=16 / 모바일 FAB size=18)
- OQ LOCKED 6 적용 — #1 BottomSheet/DesktopModal/ReplaceModal/confirmReset/confirmDeactivate 동 파일 인라인 / #2 단색 (linear-gradient 0) / #3 shift 폼 미노출 + role toggle 36→40(데스크톱)/44(모바일) / #4 외곽 hex 새 토큰 전체 치환 / #5 9·10·11 → 12 일률 격상 + leading-none / #6 Lucide UserPlus 16/18 치환
- bg-accent 실측 사용 (i4b deviation 박제) — `bg-accent-primary` 추측 0 hits / `bg-accent` ≥1 hit
- components.css 0 byte diff (이 wave 신규 추가 0 — OQ #1 default + 단일 파일 인라인 패턴)
- App.tsx 0 byte diff + src/** 변경 = StaffManagePage.tsx 1 파일만
- `npm run build` PASS + StaffManagePage chunk size 보고
- atomic 2 commit (T1 변환 + SUMMARY) — git push 시 GitHub Actions cbc7119-preview 자동 배포 트리거 + 부모 워크트리 자동 도달 (executor commit propagation 패턴)
</success_criteria>

<output>
After completion, create `.planning/quick/260526-oh0-redesign-26-staff-manage-tsx-staffmanage/260526-oh0-SUMMARY.md` covering:
- 변환 line range 3구역 (imports+상수 / 메인 함수+모달 / JSX render 3 영역) 적용 결과 + 최종 line 수 (530 → N)
- 비즈 anchor 26종 보존 확인 (grep 결과 카운트)
- Lucide UserPlus 매핑 위치 (size=16 데스크톱 헤더 / size=18 모바일 FAB)
- OQ LOCKED 6 적용 결과 + 폰트 격상 매트릭스 적용 위치 (line 121/224/236/327/461/471) + role toggle 36→40/44 격상
- BottomSheet / DesktopModal / ReplaceModalContent / StaffModalContent / StaffCard 인라인 보존 확인 (CheckpointsPage 24 공통화는 별도 task 박제)
- npm run build PASS + chunk size + tsc --noEmit 0 errors
- components.css 0 byte + App.tsx 0 byte + src/** 1 파일 변경 확인
- bg-accent 실측 적용 확인 (i4b deviation 박제 — tailwind.config accent 키)
- 누적 commit 수 + cbc7119-preview 배포 트리거 확인 + 부모 워크트리 자동 도달
- 메모리 박제 후보 (deviation / 신규 패턴 / 추가 룰 발견 시)
- 다음 단계: 26-staff-manage 완결 → 4차 모니터링 (19/20/21 legal 시리즈) 진입 / 또는 다음 페이지 후보
</output>
