---
phase: 260517-ev2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cha-bio-safety/src/pages/FloorPlanPage.tsx
autonomous: true
requirements:
  - QUICK-260517-ev2-chrome-tsx
tags: [redesign, chrome-unification, tsx-conversion, floorplan, design-tokens, 06-floorplan]

must_haves:
  truths:
    - "FloorPlanPage chrome (헤더 + 도면 종류 탭 + 층 칩) 가 sketch HTML 과 1:1 매핑된 Tailwind className 으로 렌더된다"
    - "변환 영역 (940~1020 라인 기존 위치) 안에 옛 var() alias 토큰 (--bg/--bg2/--bg3/--bd/--t1/--t2/--t3/--acl) 이 0건이다"
    - "데스크톱 분기 (isDesktop) / 핀치줌 / 마커/모달/팝업/canEditMarker 권한 분기 등 모든 비즈니스 로직이 100% 보존된다"
    - "선택/비선택 상태 색이 sketch 와 동일하다 (선택 1.5px accent + bg-accent / 비선택 1px strong + bg-surface-page)"
    - "데스크톱에서 뒤로가기 버튼이 더 이상 렌더되지 않는다 (모바일만 렌더)"
    - "데스크톱 헤더 wrapper 가 h-[54px] px-5 sketch-mirror 이고 모바일은 px-4 py-2.5 sketch-mirror 다"
    - "`npm run build` 가 PASS 하고 `tsc` 0 에러다"
  artifacts:
    - path: "cha-bio-safety/src/pages/FloorPlanPage.tsx"
      provides: "Tailwind 전환된 chrome (헤더 + 도면 종류 탭 + 층 칩) JSX"
      contains: "bg-surface-page border-b border-border-default flex-shrink-0"
  key_links:
    - from: "chrome 헤더 우측 액션 (마커 편집)"
      to: "editMode state + setEditMode/setSelected 핸들러"
      via: "onClick + active className 분기"
      pattern: "editMode \\?.*bg-accent.*border-accent.*text-text-on-accent"
    - from: "chrome 도면 종류 탭 4개"
      to: "PLAN_TYPES.map + planType state + setPlanType 핸들러"
      via: "p.ready && setPlanType + active className 분기"
      pattern: "PLAN_TYPES\\.map.*setPlanType"
    - from: "chrome 층 칩 13개"
      to: "FLOORS.map + floor state + setFloor 핸들러"
      via: "onClick={() => setFloor(f)} + active className 분기"
      pattern: "FLOORS\\.map.*setFloor"
---

<objective>
06 FloorPlanPage.tsx 의 chrome 영역 (헤더 + 도면 종류 탭 + 층 칩, 기존 940~1020 라인) 을 inline style + var() alias 에서 Tailwind utility + v0.1.1 토큰으로 1:1 변환한다.

Source of truth: 사용자가 확인완료한 sketch HTML
(`cha-bio-safety/docs/redesign-context/06-floorplan/sketch/floorplan-chrome-sketch.html`)
의 4 viewport 중 mobile · dark / desktop · dark 의 className 을 verbatim 매핑.

Purpose:
- 02 inspection 통일 룰 (`inspection-modal-chrome-rules.md` §7.2) 의 chrome 일관성을 06 도면 페이지에도 강제 적용.
- v0.1.1 디자인 토큰 단일화 (옛 --bg/--bg2/--bg3/--bd/--t1/--t2/--t3/--acl chrome 영역 0건).
- 비즈니스 로직은 단 한 줄도 건드리지 않으면서 시각적으로 sketch 와 동일하게 만드는 것.

Output:
- `FloorPlanPage.tsx` chrome JSX 만 변환 (940~1020 라인 위치). 본문 (도면 캔버스/마커/모달/팝업/툴팁) 은 out-of-scope, 그대로 보존.
- 옛 var() chrome 영역 0건 + `npm run build` PASS + `tsc` 0 에러.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@cha-bio-safety/docs/redesign-context/06-floorplan/sketch/floorplan-chrome-sketch.html
@cha-bio-safety/docs/redesign-context/00-design-context/inspection-modal-chrome-rules.md
@.planning/quick/260517-e8v-06-floorplanpage-chrome/260517-e8v-SUMMARY.md
@cha-bio-safety/src/pages/FloorPlanPage.tsx

<interfaces>
<!-- 변환 대상 chrome 영역의 현재 상태 (FloorPlanPage.tsx 940~1020 — 인라인 style 기반). -->
<!-- 이 코드를 sketch 의 Tailwind className 으로 1:1 교체하는 것이 본 task 의 핵심. -->

현재 chrome (변환 전, FloorPlanPage.tsx 940~1020):
```tsx
return (
  <div style={{ height: '100%', display: 'flex', flexDirection: 'column', overflow: 'hidden', background: 'var(--bg)', position: 'relative' }}>

    {/* ── 헤더 ─────────────────────────────────────── */}
    <header style={{
      flexShrink: 0,
      ...(isDesktop
        ? { height: 54, padding: '0 20px' }
        : { padding: '8px 12px' }),
      background: 'var(--bg2)', borderBottom: '1px solid var(--bd)',
      display: 'flex', alignItems: 'center', gap: isDesktop ? 10 : 8,
    }}>
      {!isDesktop && (
        <button onClick={() => navigate(-1)} style={{ width: 34, height: 34, borderRadius: 8, background: 'var(--bg3)', border: '1px solid var(--bd)', cursor: 'pointer', display: 'flex', alignItems: 'center', justifyContent: 'center' }}>
          <svg width={15} height={15} fill="none" viewBox="0 0 24 24" stroke="var(--t2)" strokeWidth={2}><path strokeLinecap="round" strokeLinejoin="round" d="M15 19l-7-7 7-7"/></svg>
        </button>
      )}
      <span style={{ flex: 1, fontSize: isDesktop ? 16 : 14, fontWeight: 700, color: 'var(--t1)' }}>소방 시설 도면</span>
      {canEditMarker && (
        <button
          onClick={() => { setEditMode(!editMode); setSelected(null) }}
          style={{
            height: 30, padding: '0 10px', borderRadius: 7,
            background: editMode ? 'var(--acl)' : 'var(--bg3)',
            border: editMode ? 'none' : '1px solid var(--bd)',
            color: editMode ? '#fff' : 'var(--t2)',
            fontSize: 11, fontWeight: 600, cursor: 'pointer',
          }}
        >
          {editMode ? '편집 완료' : '마커 편집'}
        </button>
      )}
      <button
        onClick={() => { setScale(1); setTranslate({ x: 0, y: 0 }) }}
        style={{ height: 30, padding: '0 10px', borderRadius: 7, background: 'var(--bg3)', border: '1px solid var(--bd)', color: 'var(--t2)', fontSize: 11, fontWeight: 600, cursor: 'pointer' }}
      >
        축소보기
      </button>
    </header>

    {/* ── 도면 종류 선택 ───────────────────────────── */}
    <div style={{ flexShrink: 0, display: 'flex', gap: 4, padding: '7px 10px', background: 'var(--bg2)', borderBottom: '1px solid var(--bd)' }}>
      {PLAN_TYPES.map(p => (
        <button
          key={p.key}
          onClick={() => p.ready && setPlanType(p.key)}
          style={{
            flex: 1, padding: '6px 4px', borderRadius: 7, fontSize: 11, fontWeight: 600,
            cursor: p.ready ? 'pointer' : 'default',
            background: planType === p.key ? 'var(--acl)' : 'var(--bg3)',
            color: planType === p.key ? '#fff' : p.ready ? 'var(--t2)' : 'var(--t3)',
            border: planType === p.key ? 'none' : '1px solid var(--bd)',
            opacity: p.ready ? 1 : 0.4,
            position: 'relative',
          }}
        >
          {p.label}
          {!p.ready && <span style={{ position: 'absolute', top: -6, right: -2, fontSize: 8, background: 'var(--bg3)', color: 'var(--t3)', padding: '1px 4px', borderRadius: 4, border: '1px solid var(--bd)' }}>준비중</span>}
        </button>
      ))}
    </div>

    {/* ── 층 선택 탭 ───────────────────────────────── */}
    <div style={{ flexShrink: 0, overflowX: 'auto', display: 'flex', gap: 4, padding: '7px 10px', background: 'var(--bg2)', borderBottom: '1px solid var(--bd)' }}>
      {FLOORS.map(f => (
        <button
          key={f}
          onClick={() => setFloor(f)}
          style={{
            flexShrink: 0, padding: '4px 10px', borderRadius: 7, fontSize: 11, fontWeight: 600, cursor: 'pointer',
            background: floor === f ? 'var(--acl)' : 'var(--bg3)',
            color: floor === f ? '#fff' : 'var(--t2)',
            border: floor === f ? 'none' : '1px solid var(--bd)',
          }}
        >
          {f}
        </button>
      ))}
    </div>

    {/* ── 도면 캔버스 (핀치줌) ──────────────────────── */}
    {/* ↑↑↑ 여기부터 OUT-OF-SCOPE — 절대 건드리지 말 것 */}
```

Sketch verbatim className (source of truth, sketch HTML 267~327 / 410~458 라인):

* 외부 wrapper (`<div>` 컨테이너):
  - 기존 인라인 style 의 height/display/flexDirection/overflow/position 은 보존 필수 (본문 핀치줌 컨테이너의 형제로서 작동해야 함).
  - background `var(--bg)` 만 Tailwind `bg-surface-page` 로 교체. 다른 inline style key 는 그대로 유지.
  - 권장: `className="h-full flex flex-col overflow-hidden bg-surface-page relative"` 로 inline style → className 통째 교체.

* 헤더 wrapper (모바일):
  `className="flex items-center gap-2.5 px-4 py-2.5 bg-surface-page border-b border-border-default flex-shrink-0"`

* 헤더 wrapper (데스크톱):
  `className="flex items-center gap-2.5 h-[54px] px-5 bg-surface-page border-b border-border-default flex-shrink-0"`

* 뒤로가기 (모바일 only — `!isDesktop && (...)` 분기 유지):
  `<button className="w-8 h-8 rounded-sm bg-surface-sunken border border-border-default text-text-secondary inline-flex items-center justify-center" onClick={() => navigate(-1)}>`
  `  <ChevronLeft size={15} />`
  `</button>`

* 타이틀:
  `<div className="flex-1 min-w-0">`
  `  <div className="text-body font-bold text-text-primary truncate">소방 시설 도면</div>`
  `</div>`
  (기존 `<span>` 1개에서 2-layer `<div>` 로 바뀜 — flex-1 min-w-0 가 truncate 동작에 필요. sketch verbatim.)

* 마커 편집 (active=editMode):
  비-active: `className="h-8 px-3 rounded-sm bg-surface-sunken border border-border-default text-text-secondary text-caption font-semibold"`
  active:   `className="h-8 px-3 rounded-sm bg-accent border border-accent text-text-on-accent text-caption font-semibold"`
  분기 권장: `className={\`h-8 px-3 rounded-sm border text-caption font-semibold ${editMode ? 'bg-accent border-accent text-text-on-accent' : 'bg-surface-sunken border-border-default text-text-secondary'}\`}`
  텍스트: `{editMode ? '편집 완료' : '마커 편집'}` (기존 분기 보존)

* 축소보기 (항상 비-active):
  `className="h-8 px-3 rounded-sm bg-surface-sunken border border-border-default text-text-secondary text-caption font-semibold"`
  텍스트: `축소 보기` (sketch verbatim. 기존 `축소보기` 공백 없는 표기에서 공백 추가 — sketch mirror)
  onClick: 기존 `{ setScale(1); setTranslate({ x: 0, y: 0 }) }` 보존.

* 도면 종류 wrapper:
  `<div className="bg-surface-raised border-b border-border-default px-3.5 py-2 flex-shrink-0">`
  `  <div className="text-caption font-semibold text-text-tertiary mb-1.5 tracking-wider">도면 종류</div>`
  `  <div className="flex gap-1.5">`
  `    {PLAN_TYPES.map(p => (...))}`
  `  </div>`
  `</div>`

* 도면 종류 탭 버튼 (4종):
  비-active: `className="flex-1 basis-0 min-w-0 inline-flex items-center justify-center gap-1.5 px-2 py-2 rounded-sm text-label font-bold whitespace-nowrap cursor-pointer transition-colors border border-border-strong bg-surface-page text-text-secondary"`
  active:   `className="flex-1 basis-0 min-w-0 inline-flex items-center justify-center gap-1.5 px-2 py-2 rounded-sm text-label font-bold whitespace-nowrap cursor-pointer transition-colors border-[1.5px] border-accent bg-accent text-text-on-accent"`
  분기 권장: base 부분 const + active 부분 ternary.
  텍스트: `{p.label}` 그대로 (PLAN_TYPES 상수 유지).
  **아이콘 제외** (사용자 결정 — sketch 도 className 만 verbatim, child icon 은 없음).
  **준비중 배지 dead code 보존**: PLAN_TYPES 의 모든 항목이 `ready: true` 라 실제 렌더 안 되지만, `!p.ready && <span>준비중</span>` 분기는 dead-code 로 그대로 유지. 단 인라인 style 도 Tailwind 로 교체: `<span className="absolute -top-1.5 -right-0.5 text-[8px] bg-surface-sunken text-text-tertiary px-1 py-0.5 rounded-sm border border-border-default">준비중</span>` (현재 안 보이는 영역이므로 정확한 토큰 매핑만 신경, sketch 에는 미정의 — 합리적 추론).
  **cursor 분기 보존**: `cursor: p.ready ? 'pointer' : 'default'` 는 className 'cursor-pointer' 가 base 에 있으므로 dead-code 분기에서만 `cursor-default` 가 필요. 인라인 style 로 추가하거나 className conditional 로 처리: `${!p.ready ? 'cursor-default opacity-40' : ''}` 추가. ready=true 100% 이므로 사실상 무동작.

* 층 칩 wrapper:
  `<div className="bg-surface-raised border-b border-border-default px-3.5 py-2 flex-shrink-0">`
  `  <div className="text-caption font-semibold text-text-tertiary mb-1.5 tracking-wider">층 선택</div>`
  `  <div className="flex gap-1 overflow-x-auto [scrollbar-width:none] [&::-webkit-scrollbar]:hidden">`
  `    {FLOORS.map(f => (...))}`
  `  </div>`
  `</div>`
  (sketch 의 `.no-scrollbar` 헬퍼 → Tailwind arbitrary variant 로 전환)

* 층 칩 버튼 (13개):
  비-active: `className="flex-shrink-0 px-3.5 py-1.5 rounded-sm text-label font-bold whitespace-nowrap cursor-pointer transition-colors border border-border-strong bg-surface-page text-text-secondary"`
  active:   `className="flex-shrink-0 px-3.5 py-1.5 rounded-sm text-label font-bold whitespace-nowrap cursor-pointer transition-colors border-[1.5px] border-accent bg-accent text-text-on-accent"`
  분기 권장: base 부분 const + active 부분 ternary.
  텍스트: `{f}` 그대로 (FLOORS 상수 유지: '8-1F','8F','7F','6F','5F','3F','2F','1F','B1','B2','B3','B4','B5').

* 데스크톱 wrapper 분기 처리:
  현재 inline style 의 `...(isDesktop ? {...} : {...})` 패턴을 className conditional 로 전환:
  `className={isDesktop
    ? "flex items-center gap-2.5 h-[54px] px-5 bg-surface-page border-b border-border-default flex-shrink-0"
    : "flex items-center gap-2.5 px-4 py-2.5 bg-surface-page border-b border-border-default flex-shrink-0"}`
  (또는 `<header>` 를 분기로 둘로 나눠도 OK — 가독성 선택. **반드시 모바일/데스크톱 두 클래스셋 모두 정확히 sketch verbatim**)

* lucide-react import:
  기존 FloorPlanPage.tsx 1~14 라인 import 블록 확인 — lucide-react import 없음. 추가 필요:
  `import { ChevronLeft } from 'lucide-react'`
  (기존 SVG path `M15 19l-7-7 7-7` 는 lucide-react ChevronLeft 와 시각 동일. size={15} 그대로 — sketch verbatim)
</interfaces>

<critical_rules_for_this_conversion>
1. **chrome 만 변환**: 940~1020 라인 영역 (헤더 wrapper + 도면 종류 wrapper + 층 칩 wrapper). 1021 라인 이후 (`도면 캔버스 (핀치줌)` 컨테이너부터 끝까지) 는 절대 건드리지 말 것. 사용자가 별도 작업으로 처리.

2. **비즈니스 로직 100% 보존**:
   - state: editMode, selected, planType, floor, scale, translate
   - 핸들러: setEditMode/setSelected, setPlanType, setFloor, setScale, setTranslate
   - 권한 분기: canEditMarker 그대로
   - 데스크톱 분기: isDesktop 그대로
   - 상수: PLAN_TYPES, FLOORS 그대로 (4종 도면 타입, 13층 순서)
   - 준비중 배지 dead code: `!p.ready && <span>준비중</span>` 보존 (Tailwind 로만 교체)
   - 텍스트: `{editMode ? '편집 완료' : '마커 편집'}` 분기 보존. `소방 시설 도면` 타이틀 보존. `축소보기` → `축소 보기` 로만 공백 추가 (sketch mirror).

3. **외부 wrapper `<div>` 의 inline style 변환**:
   - background 'var(--bg)' → `bg-surface-page` className
   - 나머지 (height/display/flexDirection/overflow/position) 도 Tailwind 로 통째 교체: `className="h-full flex flex-col overflow-hidden bg-surface-page relative"`
   - 핀치줌 컨테이너 (1022 라인 이후) 가 형제 요소이므로 외부 wrapper 의 flex-col + overflow-hidden 이 유지되어야 함 — 시각 회귀 가드.

4. **sketch className verbatim**:
   - 옛 alias 토큰 (--bg/--bg2/--bg3/--bd/--t1/--t2/--t3/--acl) 940~1020 영역 0건이 필수.
   - **다른 라인 (1021 이후) 의 var() 토큰은 그대로 둘 것** — 본 task 범위 밖.
   - className 의 spacing/sizing 토큰 (px-3.5, py-2, py-1.5, gap-1, gap-1.5, gap-2.5, h-8, h-[54px], rounded-sm) 은 sketch 그대로. 추측 금지.

5. **lucide-react import 추가**:
   - 기존 import 블록 (1~14 라인) 에 lucide-react 가 없으면 추가: `import { ChevronLeft } from 'lucide-react'`
   - 기존 SVG `<svg>` 는 ChevronLeft 컴포넌트로 교체 (size={15}).
   - 다른 SVG (마커 아이콘 등 본문) 는 손대지 말 것.

6. **데스크톱/모바일 두 클래스셋 정확히**:
   - 데스크톱 헤더: `h-[54px] px-5` (sketch verbatim, h-input 가 아니라 h-8 우측 액션 + h-[54px] wrapper)
   - 모바일 헤더: `px-4 py-2.5`
   - 사용자 결정 (commit 2594aa5): 우측 액션 (마커 편집/축소 보기) = `h-8` 통일 (뒤로가기 `w-8 h-8` 와 동일 32px).

7. **검증 명령**:
   - chrome 영역 옛 var() 0건: `awk 'NR>=940 && NR<=1100' cha-bio-safety/src/pages/FloorPlanPage.tsx | grep -cE "var\\(--(bg|bg2|bg3|bd|t1|t2|t3|acl)\\)"` (변환 후 라인이 줄어들 수 있으므로 범위 940~1100 까지 안전마진 확보)
   - 전체 빌드: `cd cha-bio-safety && npm run build` (혹은 `npx tsc --noEmit`)
   - sketch verbatim grep 11종 (Task 1 verify gate)

8. **시각 회귀 가드**:
   - 변환 후에도 외부 wrapper 가 `flex flex-col overflow-hidden relative` 를 유지하여 도면 캔버스 (1022 라인 이후) 가 본문 영역에서 정상 렌더되어야 함.
   - 헤더/도면 종류/층 칩 3개 모두 `flex-shrink-0` 유지 — 캔버스가 flex:1 로 차지하는 패턴 유지.

9. **Out-of-scope (절대 건드리지 말 것)**:
   - 도면 캔버스 컨테이너 (`containerRef` 의 `<div>` 1022 라인부터)
   - 마커 렌더링 (SVG, 좌표 계산, dragId, onCanvasMouseDown)
   - 모달 (addModal, AccessBlockedPopup, InspectionRevisitPopup)
   - 팝업/툴팁
   - `canEditMarker` 권한 로직 자체
   - 1021 라인 이후의 모든 var() 토큰
   - PLAN_TYPES / FLOORS 상수 자체 (참조하는 className 만 변환)
   - 그 외 모든 비-chrome JSX
</critical_rules_for_this_conversion>
</context>

<tasks>

<task type="auto">
  <name>Task 1: FloorPlanPage chrome 영역을 sketch verbatim Tailwind 로 변환</name>
  <files>cha-bio-safety/src/pages/FloorPlanPage.tsx</files>
  <action>
1. **사전 확인**:
   - `git branch --show-current` 가 `redesign/06-floorplan-v2` 인지 확인 (다른 브랜치면 사용자에게 컨펌 요청).
   - `git status` 깨끗한지 확인 (dirty 면 사용자에게 컨펌).

2. **lucide-react import 추가** (1~14 라인):
   - 기존 import 블록에 lucide-react 가 없으면 추가:
     ```tsx
     import { ChevronLeft } from 'lucide-react'
     ```
   - 위치: 다른 import 들과 함께 (예: `react-hot-toast` 다음 줄). 기존 import 순서/스타일 보존.

3. **chrome JSX 변환** (940~1020 라인):

   **3-1. 외부 wrapper `<div>` (940~941 라인 영역)** 의 inline style 을 className 으로 통째 교체:
   ```tsx
   <div className="h-full flex flex-col overflow-hidden bg-surface-page relative">
   ```
   기존 style prop 제거.

   **3-2. 헤더 wrapper (`<header>` 940~952 라인)** — 데스크톱/모바일 분기를 className conditional 로:
   ```tsx
   <header className={isDesktop
     ? "flex items-center gap-2.5 h-[54px] px-5 bg-surface-page border-b border-border-default flex-shrink-0"
     : "flex items-center gap-2.5 px-4 py-2.5 bg-surface-page border-b border-border-default flex-shrink-0"}>
   ```
   기존 style prop 완전 제거.

   **3-3. 뒤로가기 버튼 (953~957 라인)** — `!isDesktop &&` 분기 유지하고 내부 교체:
   ```tsx
   {!isDesktop && (
     <button
       onClick={() => navigate(-1)}
       className="w-8 h-8 rounded-sm bg-surface-sunken border border-border-default text-text-secondary inline-flex items-center justify-center"
     >
       <ChevronLeft size={15} />
     </button>
   )}
   ```
   기존 SVG path 완전 제거 (lucide-react ChevronLeft 로 교체).

   **3-4. 타이틀 (958 라인)** — 단일 `<span>` → 2-layer `<div>` (sketch verbatim):
   ```tsx
   <div className="flex-1 min-w-0">
     <div className="text-body font-bold text-text-primary truncate">소방 시설 도면</div>
   </div>
   ```

   **3-5. 마커 편집 버튼 (959~972 라인)** — `canEditMarker &&` 분기 유지, editMode active 분기:
   ```tsx
   {canEditMarker && (
     <button
       onClick={() => { setEditMode(!editMode); setSelected(null) }}
       className={editMode
         ? "h-8 px-3 rounded-sm bg-accent border border-accent text-text-on-accent text-caption font-semibold"
         : "h-8 px-3 rounded-sm bg-surface-sunken border border-border-default text-text-secondary text-caption font-semibold"}
     >
       {editMode ? '편집 완료' : '마커 편집'}
     </button>
   )}
   ```

   **3-6. 축소 보기 버튼 (973~978 라인)** — 항상 비-active, 텍스트는 sketch mirror `축소 보기` (공백 추가):
   ```tsx
   <button
     onClick={() => { setScale(1); setTranslate({ x: 0, y: 0 }) }}
     className="h-8 px-3 rounded-sm bg-surface-sunken border border-border-default text-text-secondary text-caption font-semibold"
   >
     축소 보기
   </button>
   ```

   **3-7. 도면 종류 wrapper (981~1001 라인)** — wrapper + 라벨 + 4 버튼:
   ```tsx
   {/* ── 도면 종류 선택 ───────────────────────────── */}
   <div className="bg-surface-raised border-b border-border-default px-3.5 py-2 flex-shrink-0">
     <div className="text-caption font-semibold text-text-tertiary mb-1.5 tracking-wider">도면 종류</div>
     <div className="flex gap-1.5">
       {PLAN_TYPES.map(p => (
         <button
           key={p.key}
           onClick={() => p.ready && setPlanType(p.key)}
           className={planType === p.key
             ? "relative flex-1 basis-0 min-w-0 inline-flex items-center justify-center gap-1.5 px-2 py-2 rounded-sm text-label font-bold whitespace-nowrap cursor-pointer transition-colors border-[1.5px] border-accent bg-accent text-text-on-accent"
             : `relative flex-1 basis-0 min-w-0 inline-flex items-center justify-center gap-1.5 px-2 py-2 rounded-sm text-label font-bold whitespace-nowrap transition-colors border border-border-strong bg-surface-page text-text-secondary ${p.ready ? 'cursor-pointer' : 'cursor-default opacity-40'}`}
         >
           {p.label}
           {!p.ready && (
             <span className="absolute -top-1.5 -right-0.5 text-[8px] bg-surface-sunken text-text-tertiary px-1 py-0.5 rounded-sm border border-border-default">
               준비중
             </span>
           )}
         </button>
       ))}
     </div>
   </div>
   ```
   - `relative` 는 active/비-active 양쪽에 모두 필요 (준비중 배지 absolute 좌표 기준).
   - dead-code 준비중 배지: 현재 PLAN_TYPES 모두 `ready: true` 이므로 안 보이지만 분기는 보존.
   - 아이콘 child 없음 (사용자 결정 — sketch verbatim).

   **3-8. 층 칩 wrapper (1003~1019 라인)** — wrapper + 라벨 + 13 칩 + 가로 스크롤:
   ```tsx
   {/* ── 층 선택 탭 ───────────────────────────────── */}
   <div className="bg-surface-raised border-b border-border-default px-3.5 py-2 flex-shrink-0">
     <div className="text-caption font-semibold text-text-tertiary mb-1.5 tracking-wider">층 선택</div>
     <div className="flex gap-1 overflow-x-auto [scrollbar-width:none] [&::-webkit-scrollbar]:hidden">
       {FLOORS.map(f => (
         <button
           key={f}
           onClick={() => setFloor(f)}
           className={floor === f
             ? "flex-shrink-0 px-3.5 py-1.5 rounded-sm text-label font-bold whitespace-nowrap cursor-pointer transition-colors border-[1.5px] border-accent bg-accent text-text-on-accent"
             : "flex-shrink-0 px-3.5 py-1.5 rounded-sm text-label font-bold whitespace-nowrap cursor-pointer transition-colors border border-border-strong bg-surface-page text-text-secondary"}
         >
           {f}
         </button>
       ))}
     </div>
   </div>
   ```

4. **변환 후 검증** (반드시 자동 명령으로):

   4-1. **chrome 영역 옛 var() 0건**:
   ```bash
   awk 'NR>=940 && NR<=1100' cha-bio-safety/src/pages/FloorPlanPage.tsx | grep -cE "var\\(--(bg|bg2|bg3|bd|t1|t2|t3|acl)\\)"
   ```
   결과: `0` 필수. (변환 후 라인 수가 줄 수 있으므로 940~1100 안전 마진. 1100 이후 도면 캔버스 영역의 var() 는 OK)

   4-2. **sketch verbatim 11종 grep gate** (chrome 영역만 검색):
   ```bash
   F=cha-bio-safety/src/pages/FloorPlanPage.tsx
   echo "G1 header-page:";       grep -c "bg-surface-page border-b border-border-default flex-shrink-0" $F     # ≥1 (헤더 wrapper)
   echo "G2 zone-floor-raised:"; grep -c "bg-surface-raised border-b border-border-default px-3.5 py-2 flex-shrink-0" $F  # ≥2 (도면종류+층)
   echo "G3 label-token:";       grep -c "text-caption font-semibold text-text-tertiary mb-1.5 tracking-wider" $F  # ≥2 (도면종류/층 라벨)
   echo "G4 tab-button:";        grep -c "flex-1 basis-0 min-w-0 inline-flex items-center justify-center gap-1.5 px-2 py-2 rounded-sm text-label font-bold whitespace-nowrap" $F  # ≥1 (도면종류 버튼 className 기반)
   echo "G5 chip-button:";       grep -c "flex-shrink-0 px-3.5 py-1.5 rounded-sm text-label font-bold whitespace-nowrap" $F  # ≥1 (층 칩 className 기반)
   echo "G6 selected-state:";    grep -c "border-\\[1.5px\\] border-accent bg-accent text-text-on-accent" $F  # ≥2 (탭+칩 active)
   echo "G7 unselected-state:";  grep -c "border border-border-strong bg-surface-page text-text-secondary" $F  # ≥2
   echo "G8 title-token:";       grep -c "text-body font-bold text-text-primary truncate" $F  # ≥1
   echo "G9 action-button:";     grep -c "h-8 px-3 rounded-sm bg-surface-sunken border border-border-default text-text-secondary text-caption font-semibold" $F  # ≥1 (축소 보기, 모바일 마커편집 비-active 등)
   echo "G10 back-button:";      grep -c "w-8 h-8 rounded-sm bg-surface-sunken border border-border-default text-text-secondary inline-flex items-center justify-center" $F  # ≥1
   echo "G11 lucide-import:";    grep -c "import { ChevronLeft } from 'lucide-react'" $F  # 1
   ```
   각 gate 결과를 SUMMARY 에 기록.

   4-3. **TypeScript 0 에러**:
   ```bash
   cd cha-bio-safety && npx tsc --noEmit 2>&1 | grep -E "error TS" | wc -l
   ```
   결과: `0` 필수.

   4-4. **빌드 PASS**:
   ```bash
   cd cha-bio-safety && npm run build 2>&1 | tail -20
   ```
   결과: 빌드 완료 메시지 (`✓ built in` 등). 실패 시 에러 보고.

5. **변환되지 않은 항목 보존 확인** (회귀 가드):
   - PLAN_TYPES / FLOORS 상수 변경 없음: `grep -n "const PLAN_TYPES\\|const FLOORS" cha-bio-safety/src/pages/FloorPlanPage.tsx` 결과가 변환 전과 동일.
   - 핀치줌 컨테이너 (`containerRef`) 영역 그대로: `grep -n "containerRef" cha-bio-safety/src/pages/FloorPlanPage.tsx | wc -l` 결과가 변환 전과 동일.
   - canEditMarker 그대로: `grep -n "const canEditMarker" cha-bio-safety/src/pages/FloorPlanPage.tsx` 결과 동일.
   - isDesktop 사용처 (chrome 외): `grep -cn "isDesktop" cha-bio-safety/src/pages/FloorPlanPage.tsx` 결과가 변환 전과 동일 또는 증감 ±2 이내 (헤더 분기 1건 변경됨).

6. **commit**:
   ```bash
   git add cha-bio-safety/src/pages/FloorPlanPage.tsx
   git commit -m "feat(260517-ev2): 06 FloorPlanPage chrome — sketch 1:1 TSX 변환

- 헤더/도면 종류/층 칩 inline style → Tailwind utility (sketch verbatim)
- 옛 alias 토큰 (--bg/--bg2/--bg3/--bd/--t1/--t2/--t3/--acl) chrome 영역 0건
- lucide-react ChevronLeft import 추가, 모바일 뒤로가기 SVG 교체
- 데스크톱 헤더 h-[54px] px-5, 모바일 헤더 px-4 py-2.5, 우측 액션 h-8 통일
- 도면 종류 라벨/탭 4종 + 층 선택 라벨/칩 13종 + 선택 1.5px accent / 비선택 1px strong
- 본문 (도면 캔버스/마커/모달/팝업) 보존, 비즈니스 로직 100% 보존"
   ```

7. **결과 보고**:
   - 11종 grep gate 결과 표
   - 변환 전후 chrome 영역 라인 수 비교
   - 옛 var() chrome 영역 카운트 (목표 0)
   - tsc / build 결과
   - 보존 확인 항목 (PLAN_TYPES/FLOORS/containerRef/canEditMarker)
  </action>
  <verify>
    <automated>F=cha-bio-safety/src/pages/FloorPlanPage.tsx; CHROME_VAR=$(awk 'NR>=940 && NR<=1100' $F | grep -cE "var\(--(bg|bg2|bg3|bd|t1|t2|t3|acl)\)"); G1=$(grep -c "bg-surface-page border-b border-border-default flex-shrink-0" $F); G2=$(grep -c "bg-surface-raised border-b border-border-default px-3.5 py-2 flex-shrink-0" $F); G3=$(grep -c "text-caption font-semibold text-text-tertiary mb-1.5 tracking-wider" $F); G4=$(grep -c "flex-1 basis-0 min-w-0 inline-flex items-center justify-center gap-1.5 px-2 py-2 rounded-sm text-label font-bold whitespace-nowrap" $F); G5=$(grep -c "flex-shrink-0 px-3.5 py-1.5 rounded-sm text-label font-bold whitespace-nowrap" $F); G6=$(grep -c "border-\[1.5px\] border-accent bg-accent text-text-on-accent" $F); G7=$(grep -c "border border-border-strong bg-surface-page text-text-secondary" $F); G8=$(grep -c "text-body font-bold text-text-primary truncate" $F); G9=$(grep -c "h-8 px-3 rounded-sm bg-surface-sunken border border-border-default text-text-secondary text-caption font-semibold" $F); G10=$(grep -c "w-8 h-8 rounded-sm bg-surface-sunken border border-border-default text-text-secondary inline-flex items-center justify-center" $F); G11=$(grep -c "import { ChevronLeft } from 'lucide-react'" $F); echo "CHROME_VAR=$CHROME_VAR (need 0) G1=$G1(>=1) G2=$G2(>=2) G3=$G3(>=2) G4=$G4(>=1) G5=$G5(>=1) G6=$G6(>=2) G7=$G7(>=2) G8=$G8(>=1) G9=$G9(>=1) G10=$G10(>=1) G11=$G11(==1)"; [ $CHROME_VAR -eq 0 ] && [ $G1 -ge 1 ] && [ $G2 -ge 2 ] && [ $G3 -ge 2 ] && [ $G4 -ge 1 ] && [ $G5 -ge 1 ] && [ $G6 -ge 2 ] && [ $G7 -ge 2 ] && [ $G8 -ge 1 ] && [ $G9 -ge 1 ] && [ $G10 -ge 1 ] && [ $G11 -eq 1 ] && (cd cha-bio-safety && npx tsc --noEmit 2>&1 | grep -E "error TS" | wc -l | grep -q "^[[:space:]]*0$") && (cd cha-bio-safety && npm run build > /tmp/build.log 2>&1) && echo "ALL GATES PASS" || echo "GATE FAIL"</automated>
  </verify>
  <done>
- FloorPlanPage.tsx chrome 영역 (헤더 + 도면 종류 wrapper + 층 칩 wrapper) 이 sketch HTML 의 className 과 1:1 매핑된 Tailwind utility 로 변환됨.
- chrome 영역 (940~1100 라인 안전 마진) 안에 옛 var() alias 토큰 0건.
- 11종 grep gate 모두 PASS (G1~G11 각 임계치 충족).
- `npx tsc --noEmit` 결과 0 에러.
- `npm run build` PASS.
- 비즈니스 로직 (PLAN_TYPES/FLOORS 상수, planType/floor/editMode state, canEditMarker 권한, isDesktop 분기, 핀치줌 컨테이너) 100% 보존.
- lucide-react `ChevronLeft` import 추가 (모바일 뒤로가기 SVG path 교체).
- 도면 캔버스/마커/모달/팝업 (1022 라인 이후) 영역 완전 보존.
- commit 완료, branch `redesign/06-floorplan-v2` 위.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 사용자 시각 검토 (브라우저 확인)</name>
  <what-built>FloorPlanPage chrome (헤더 + 도면 종류 탭 + 층 칩) 의 Tailwind 변환. sketch HTML 의 4 viewport 와 1:1 매핑된 상태.</what-built>
  <how-to-verify>
1. dev 서버 실행 (이미 떠 있으면 hot-reload):
   ```bash
   cd cha-bio-safety && npm run dev
   ```
2. 브라우저에서 `/floor-plan` (또는 도면 페이지 경로) 열기.
3. **모바일 viewport (375px 폭)**:
   - 헤더: 뒤로가기 (32×32 sunken) + "소방 시설 도면" (body bold) + 마커 편집 + 축소 보기 (모두 h-8 sunken)
   - 헤더 wrapper: `bg-surface-page border-b` (3-layer 중 가장 어두운/밝은 레이어)
   - 도면 종류 wrapper: `bg-surface-raised` + 라벨 "도면 종류" + 4 탭 균등 분배
   - 층 칩 wrapper: `bg-surface-raised` + 라벨 "층 선택" + 13 칩 가로 스크롤
   - 현재 선택된 도면 종류 (예: 유도등) = 1.5px accent border + bg-accent + 흰 글자
   - 비선택 도면 종류 3개 = 1px strong border + page bg + secondary text
   - 현재 선택된 층 (예: 1F 또는 기본값) = 1.5px accent border + bg-accent + 흰 글자
   - 비선택 층 12개 = 1px strong border + page bg + secondary text
4. **데스크톱 viewport (1280px+)**:
   - 헤더 높이 = 54px, 뒤로가기 **없음** (사이드바 nav 가정)
   - 그 외 항목 동일
5. **인터랙션**:
   - 도면 종류 탭 클릭 → planType 변경 + active 색 이동
   - 층 칩 클릭 → floor 변경 + active 색 이동
   - 마커 편집 클릭 → editMode 토글 + active 시 bg-accent
   - 축소 보기 클릭 → scale/translate reset (캔버스 zoom 초기화)
   - 모바일 뒤로가기 클릭 → 이전 페이지 (navigate(-1))
6. **다크/라이트 테마 토글** (있다면):
   - 두 테마 모두에서 sketch 와 시각 동일한지 확인
7. **02 inspection 통일 화면과 시각 일관성 확인** (헤더/탭/칩 디자인 동일감)

**resume-signal:** "approved" 또는 수정 코멘트.
  </how-to-verify>
  <resume-signal>"approved" 입력 또는 구체 수정 항목 코멘트</resume-signal>
</task>

</tasks>

<verification>
- chrome 영역 (940~1100 라인) 안에 옛 var() alias 토큰 0건 (CHROME_VAR=0).
- 11종 grep gate (G1~G11) 모두 임계치 통과.
- `npx tsc --noEmit` 0 에러.
- `npm run build` PASS.
- PLAN_TYPES / FLOORS 상수 변경 없음.
- containerRef / canEditMarker / isDesktop / editMode / planType / floor state 변경 없음.
- 도면 캔버스 (1022 라인 이후) 영역 완전 보존.
- 사용자 시각 검토 (checkpoint Task 2) approved.
</verification>

<success_criteria>
- FloorPlanPage chrome 이 sketch 와 시각 동일.
- 옛 alias 토큰 chrome 영역 완전 제거.
- 빌드/타입 검사 PASS.
- 비즈니스 로직 100% 보존 (사용자 인터랙션/상태 변경 없음).
- 사용자 approved.
</success_criteria>

<output>
After completion, create `.planning/quick/260517-ev2-06-floorplanpage-chrome-tsx-sketch-1-1/260517-ev2-SUMMARY.md` 에:
- 변환된 chrome 영역 라인 수 (전후 비교)
- 11종 grep gate 결과 표
- chrome 영역 옛 var() 카운트
- tsc / build 결과
- 보존 확인 (PLAN_TYPES/FLOORS/containerRef/canEditMarker)
- commit hash
- 사용자 컨펌 상태
- 다음 단계 (사용자 컨펌 시 main 머지 + 배포 후보 / 잔존 cleanup 항목 식별)
</output>
