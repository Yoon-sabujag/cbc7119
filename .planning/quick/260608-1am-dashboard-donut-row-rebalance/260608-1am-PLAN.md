---
phase: quick-260608-1am
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [cha-bio-safety/src/pages/DashboardPage.tsx]
autonomous: true
requirements: [QUICK-260608-1am]
must_haves:
  truths:
    - "데스크톱 '이번 달 점검 현황' 박스에서 19개 도넛이 6·7·6 으로 3줄 배치된다"
    - "세로 행 간격이 32px(gap-8) → 16px(gap-4) 로 줄어 도넛이 박스 상·하단에서 잘리지 않는다"
    - "모바일 월간 도넛 블록(가로 스크롤, size=44)은 단 1바이트도 바뀌지 않는다"
  artifacts:
    - path: "cha-bio-safety/src/pages/DashboardPage.tsx"
      provides: "데스크톱 월간 도넛 6·7·6 분배 + gap-4 행 간격"
      contains: "const counts = new Array(numRows).fill(base)"
  key_links:
    - from: "DashboardPage.tsx 데스크톱 IIFE"
      to: "rows.map 렌더링 JSX"
      via: "counts[] 기반 슬라이스로 rows 생성"
      pattern: "rows\\.push\\(monthly\\.slice\\(off"
---

<objective>
대시보드 데스크톱 "이번 달 점검 현황" 박스의 월간 도넛 배치를 7·7·5 greedy 분배에서 6·7·6 (가운데 줄부터 바깥쪽으로 나머지 배분) 으로 바꾸고, 세로 행 간격을 `gap-8`(32px) → `gap-4`(16px) 으로 줄여 도넛이 박스 상·하단에서 잘리는 문제를 해소한다.

Purpose: 사용자 브라우저 시안 승인 완료(LOCKED). n=19 기준 6·7·6 균형 배치 + 행 간격 축소로 76px 도넛이 박스 안에 온전히 들어오게 한다.
Output: DashboardPage.tsx 한 파일, 정확히 2곳 Edit. 모바일 블록 무변경.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

# 대상 파일 (데스크톱 블록 L306–353, 모바일 블록 L651–695)
@cha-bio-safety/src/pages/DashboardPage.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: 데스크톱 월간 도넛 6·7·6 분배 + 행 간격 gap-4 적용 (2곳 Edit)</name>
  <files>cha-bio-safety/src/pages/DashboardPage.tsx</files>
  <action>
DashboardPage.tsx 데스크톱 "이번 달 점검 현황" 박스에 정확히 2곳 Edit 을 적용한다. 추측/창작 금지 — 아래 OLD→NEW 문자열을 그대로 사용한다. 두 OLD 문자열 모두 파일 내 unique 임을 grep 으로 사전 확인했다(`gap-8` L314 1곳, `const perRow = Math.ceil` L321 1곳).

Edit 1 — 행 간격 (현재 L314): 컨테이너 className 에서 `gap-8` 토큰 하나만 `gap-4` 로 교체한다. OLD = `<div className="px-6 py-5 flex flex-col gap-8 flex-1 justify-center overflow-y-auto">` → NEW = `<div className="px-6 py-5 flex flex-col gap-4 flex-1 justify-center overflow-y-auto">`. `px-6 py-5`, `flex flex-col`, `flex-1`, `justify-center`, `overflow-y-auto` 는 전부 그대로 보존한다.

Edit 2 — 분배 알고리즘 (현재 L316–324, IIFE 내부 greedy perRow 블록): 기존 greedy `perRow = Math.ceil(n / numRows)` 블록을 "나머지를 가운데 줄부터 바깥쪽으로 배분" 하는 balanced 블록으로 교체한다. `MonthlyItem` 타입명, `cols = 7`, `numRows = Math.max(1, Math.ceil(n / cols))` 시그니처는 그대로 유지하고, `base`/`counts`/`rem`/`mid`/`order`/`off` 로직으로 rows 를 채운다. 바로 다음 줄 `return rows.map((row, ri) => (` 이하 전체 렌더링 JSX(`justify-evenly gap-2`, Donut size={76}, doubleCycle 분기, label/count markup)는 단 1바이트도 건드리지 않는다.

  OLD 블록:
  주석 `// 한 줄당 최대 7개 — 2줄에 균등 분배 ...` 부터 `for` 루프 닫는 `}` 까지 (perRow + slice 루프).

  NEW 블록 (정확히 이 순서/내용):
  주석 = `// 한 줄당 최대 7개 — 행을 균등 분배하되 나머지는 가운데 줄부터 바깥쪽으로 배치 (n=19 → 6·7·6)`,
  `const rows: MonthlyItem[][] = []`,
  `const n = monthly.length`,
  `const cols = 7`,
  `const numRows = Math.max(1, Math.ceil(n / cols))`,
  `const base = Math.floor(n / numRows)`,
  `const counts = new Array(numRows).fill(base)`,
  `const rem = n - base * numRows`,
  `const mid = (numRows - 1) / 2`,
  `const order = [...counts.keys()].sort((a, b) => Math.abs(a - mid) - Math.abs(b - mid) || a - b)`,
  `for (let k = 0; k < rem; k++) counts[order[k]]++`,
  `let off = 0`,
  그리고 `for (let i = 0; i < numRows; i++) { rows.push(monthly.slice(off, off + counts[i])); off += counts[i] }`.

알고리즘 검증(근거): n=19 → numRows=ceil(19/7)=3, base=6, counts=[6,6,6], rem=1, mid=1, order=[1,0,2], counts[1]++ → [6,7,6]. 슬라이스 6/7/6. (일반화: n=16→5·6·5, n=22→5·6·6·5.)

절대 금지: 모바일 블록(L651–695, `flex-nowrap` 가로 스크롤, Donut `size={44}`)은 절대 건드리지 않는다 — 메모리 룰 "월간 도넛은 가로 스크롤". `justify-center`/`overflow-y-auto`/Donut size 76/라벨·카운트 markup/doubleCycle 분기 전부 보존. gap 값은 `gap-4` 외 다른 값(gap-3/5/6) 금지.
  </action>
  <verify>
    <automated>cd cha-bio-safety && test "$(grep -c 'gap-8' src/pages/DashboardPage.tsx)" -eq 0 && grep -q 'flex flex-col gap-4 flex-1 justify-center overflow-y-auto' src/pages/DashboardPage.tsx && echo PASS-GAP</automated>
    <automated>cd cha-bio-safety && test "$(grep -c 'const perRow = Math.ceil' src/pages/DashboardPage.tsx)" -eq 0 && grep -q 'const counts = new Array(numRows).fill(base)' src/pages/DashboardPage.tsx && grep -q 'rows.push(monthly.slice(off, off + counts\[i\]))' src/pages/DashboardPage.tsx && echo PASS-ALGO</automated>
    <automated>cd cha-bio-safety && git diff src/pages/DashboardPage.tsx | grep -E '^[-+]' | grep -E 'size=\{44\}|flex-nowrap' && echo FAIL-MOBILE-TOUCHED || echo PASS-MOBILE-UNTOUCHED</automated>
    <automated>cd cha-bio-safety && npx tsc --noEmit && echo PASS-TSC</automated>
    <automated>cd cha-bio-safety && npm run build && echo PASS-BUILD</automated>
  </verify>
  <done>
- 데스크톱 박스 컨테이너가 `gap-4` 이고 `gap-8` 은 파일 내 0건
- greedy `const perRow = Math.ceil(n / numRows)` 0건, balanced `const counts = new Array(numRows).fill(base)` + `rows.push(monthly.slice(off, off + counts[i]))` 존재
- git diff 에 `size={44}` / `flex-nowrap` 라인 미등장 (모바일 무변경)
- `npx tsc --noEmit` 및 `npm run build` PASS
- atomic commit 1건 생성 (배포는 하지 않음)
  </done>
</task>

</tasks>

<verification>
- 데스크톱 분배: 코드상 n=19 → counts=[6,7,6] 산출 (rows.map 렌더링 JSX 무변경으로 6·7·6 3줄 렌더)
- 행 간격: `gap-8`(32px) → `gap-4`(16px), 76px 도넛이 박스 상·하단에서 잘리지 않음 (시안 승인 기준)
- 모바일 격리: `flex-nowrap`/`size={44}` 라인이 diff 에 등장하지 않음
- 타입/빌드 무결성: tsc + vite build PASS
</verification>

<success_criteria>
- DashboardPage.tsx 정확히 2곳 Edit 만 반영, 모바일 블록 0 변경
- 모든 verify automated 항목 PASS (PASS-GAP / PASS-ALGO / PASS-MOBILE-UNTOUCHED / PASS-TSC / PASS-BUILD)
- atomic commit 1건 (배포 미실행)
</success_criteria>

<out_of_scope>
- 배포 금지: 이 콘솔은 production 전용. executor 는 소스 Edit + atomic commit 까지만. `wrangler` / `npm run deploy` 실행 금지 (build 는 검증용 OK).
- 브랜치 체크아웃 변경 금지: 현재 `production` 브랜치 메인 트리에서 직접 작업, 워크트리 미사용.
- production-sync.md / STATE.md 갱신과 실제 배포는 오케스트레이터(메인)가 executor 이후 처리.
</out_of_scope>

<output>
Create `.planning/quick/260608-1am-dashboard-donut-row-rebalance/260608-1am-SUMMARY.md` when done
</output>
