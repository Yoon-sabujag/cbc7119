---
quick_id: 260426-f54
slug: completion-rule-bad-resolved
status: complete
date: 2026-04-26
commits:
  - task_1_hash: 4749bcd
  - task_2_hash: b7e4c71
files_changed:
  - cha-bio-safety/src/pages/InspectionPage.tsx
  - cha-bio-safety/src/utils/inspectionProgress.ts
  - cha-bio-safety/functions/api/dashboard/stats.ts
build: passed
deployed: true
deploy_url: https://ed498e33.cbc7119.pages.dev
verified_by_user: 2026-04-26
---

# 260426-f54 — 일반 점검 "완료" 정의 통일 (bad + resolved 인정)

## 무엇을 고쳤나

카드 / 대시보드 월간 통계 / InspectionPage 층별 화면이 서로 다른 "완료" 정의를 쓰던 모순을 단일 룰로 통일했다. 대표 사례는 CP-2F-4-SH (홍보관 옆 소화전): 4/15 bad → 14:59 status='resolved' 인데 카드는 미점검 (150/154), 층별 화면은 6/6 으로 표시되던 불일치. 이제 세 화면 모두 같은 룰을 사용한다.

**프론트엔드 (Task 1, 4749bcd):**
- `InspectionPage.tsx` 모듈 레벨에 `isCpCompleted(entry)` 헬퍼 추가
- 라인 4014 `monthDatesMap` 빌드: `bad + resolved` 도 날짜 인덱스에 포함
- 라인 3216 (수정 후 3225) 층별 fDone: `monthRecords[cp.id]` truthy 체크 → `isCpCompleted(monthRecords[cp.id])` 호출로 변경
- `inspectionProgress.ts` 헤더 주석 갱신 (코드 변경 없음 — 호출자가 새 룰로 데이터를 채워주므로 자동 정합)

**백엔드 (Task 2, b7e4c71):**
- `dashboard/stats.ts` 7곳 SQL 확장 (라인 98 / 107 / 188 / 313 / 334 / 417 / 427)
- 패턴: `AND cr.result IN ('normal','caution')` → `AND (cr.result IN ('normal','caution') OR (cr.result='bad' AND cr.status='resolved'))`
- 괄호 그루핑으로 우선순위 보존, 각 사이트에 `260426-f54` 주석 첨부

## 새 룰 정확한 정의

```
완료 = result IN ('normal','caution') OR (result='bad' AND status='resolved')
```

| 케이스 | 완료? |
|---|---|
| normal | ✓ |
| caution | ✓ |
| bad + status='resolved' | ✓ (NEW — 카드/대시보드 인정) |
| bad + status='open' | ✗ (조치 대기) |
| 기록 없음 | ✗ (미점검) |

## 손대지 않은 것 (의도적)

- **InspectionPage.tsx 라인 2935 / 2975 / 3060** (구 라인 2927 / 2967 / 3052) `monthRecords[cp.id]` 사용처 — `firstPending` 후보 계산 / 자동 다음 cp 이동 흐름. 의미가 "이미 점검 시도 있음" 이라 새 룰을 적용하면 자동 흐름이 깨진다.
- **stats.ts 라인 206** `WHERE result IN ('bad','caution')` — 불량/주의 통계 (대시보드 "미조치 결함" 카운트). 완료 판정과 무관한 별도 목적이라 그대로.
- **DB 데이터 변경 없음.** 'bad' 기록은 그대로 두고 `status='resolved'` 컬럼에 의존.

## 회귀 mental walkthrough

- CP-2F-4-SH (4/15 bad+resolved) → 카드 +1 (T2 SQL), 층별 화면 동일 (T1 isCpCompleted) ✓
- bad+open 인 cp → 카드/층별 모두 미완료 (status 가드) ✓
- normal/caution 만 있는 cp → 변동 없음 (기존 룰 일부) ✓
- 기록 없음 → 미완료 (모든 화면) ✓
- `inspectionProgress.computeCardCompletion` 함수 본문 무변경 — 호출자가 보내주는 `monthRecordDates` 가 이미 새 룰로 채워져 자동 정합 ✓

## 빌드 결과

```
cd cha-bio-safety && npm run build
✓ built in 10.90s   (Task 1 후)
✓ built in 0.17s    (Task 2 후 — SQL 문자열 변경이라 모듈 재생성 거의 없음)
```

PWA service worker / precache 67 entries / 5999.38 KiB — 이상 없음.

## 다음 (orchestrator + 사용자)

1. orchestrator: 워크트리 머지
2. orchestrator: `cd cha-bio-safety && npm run deploy -- --branch production --commit-message "fix-completion-rule-bad-resolved"`
3. 사용자 시각 검증 (PWA 캐시 주의 — 새로고침/재설치):
   - 일반 점검 → **소화전 카드 151/154** (이전 150/154) — CP-2F-4-SH 가 자동 +1
   - 소화전 → 연구동 2층 → **6/6 그대로** (이미 6/6 이었음 — 룰 통일로 의미만 정확해짐)
   - 대시보드 월간 도넛 (소화전) → 카드와 동일 비율 보정 반영
   - bad+open 케이스가 있으면 그대로 미완료 유지 (회귀 없음)
