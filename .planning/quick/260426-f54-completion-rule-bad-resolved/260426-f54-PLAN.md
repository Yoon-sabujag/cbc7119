---
phase: quick-260426-f54
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cha-bio-safety/src/pages/InspectionPage.tsx
  - cha-bio-safety/src/utils/inspectionProgress.ts
  - cha-bio-safety/functions/api/dashboard/stats.ts
autonomous: false
requirements:
  - QUICK-260426-f54
must_haves:
  truths:
    - "일반 점검 카드, 대시보드 월간 통계, InspectionPage 층별 화면 모두 동일한 '완료' 정의를 사용한다 (normal | caution | bad+resolved)"
    - "CP-2F-4-SH 같은 bad+status='resolved' 점검 개소가 카드에서도 완료로 카운트된다"
    - "bad+status='open' (조치 대기) 점검 개소는 모든 화면에서 미완료로 표시된다 (회귀 없음)"
    - "InspectionPage 층별 화면의 fDone 카운트가 '점검 시도 있음' 이 아니라 '확정 완료' 를 의미한다"
    - "프로덕션 배포 후 소화전 카드가 151/154 (이전 150/154) 로 표시된다"
  artifacts:
    - path: "cha-bio-safety/src/pages/InspectionPage.tsx"
      provides: "isCpCompleted 헬퍼 + 라인 4014 monthDatesMap 빌드 + 라인 3216 fDone 카운트 룰 통일"
      contains: "isCpCompleted"
    - path: "cha-bio-safety/functions/api/dashboard/stats.ts"
      provides: "7곳의 cr.result IN ('normal','caution') → (normal|caution|bad+resolved) 룰 확장"
      contains: "cr.result='bad' AND cr.status='resolved'"
    - path: "cha-bio-safety/src/utils/inspectionProgress.ts"
      provides: "주석 갱신 (코드 변경 없음) — monthRecordDates 의 의미가 '완료' 로 정확히 매핑됨"
      contains: "bad-resolved"
  key_links:
    - from: "InspectionPage.tsx (라인 4014, monthData 처리 루프)"
      to: "monthDatesMap → setMonthRecordDates → computeCardCompletion (카드 카운트)"
      via: "rawResult='bad' && entry.status='resolved' 조건 추가"
      pattern: "rawResult === 'bad'"
    - from: "InspectionPage.tsx (라인 3216, 층별 fDone)"
      to: "isCpCompleted(monthRecords[cp.id])"
      via: "헬퍼 함수 호출로 '점검 시도 있음' 대신 '확정 완료' 로 의미 통일"
      pattern: "isCpCompleted"
    - from: "stats.ts SQL (라인 98/107/188/313/334/417/427)"
      to: "대시보드 inspDone, 월간 도넛 done 카운트, schedule_items 자동 done 동기화"
      via: "(cr.result IN ('normal','caution') OR (cr.result='bad' AND cr.status='resolved'))"
      pattern: "cr.result='bad' AND cr.status='resolved'"
---

<objective>
일반 점검의 "완료" 정의를 모든 표시 지점에서 통일한다.

**현재 상태 (불일치):**
- 카드 + 대시보드: `result IN ('normal','caution')` 만 완료로 카운트 — bad+resolved 인 cp 가 미점검으로 잘못 잡힘
- InspectionPage 층별 화면 (line 3216): `monthRecords[cp.id]` truthy 만 봄 — bad+open 도 완료로 잘못 카운트
- 모순 사례: CP-2F-4-SH (홍보관 옆 소화전), 4/15 bad → 14:59 status='resolved'. 카드는 미점검 (150/154), 층별 화면은 완료 (2층 6/6)

**확정된 룰 (사용자 합의):**
"완료" = `result IN ('normal','caution') OR (result='bad' AND status='resolved')`

| 케이스 | 완료? |
|---|---|
| normal | ✓ |
| caution | ✓ |
| bad + status='resolved' | ✓ (NEW — 카드/대시보드에서 인정) |
| bad + status='open' | ✗ (조치 대기) |
| 기록 없음 | ✗ (미점검) |

**Purpose:** 운영 관찰 모드의 정확도 보강 — 화면 간 모순 제거. 5월 법정점검 실전 검증 신뢰도 확보.

**Output:** Frontend 1 commit + Backend SQL 1 commit + 프로덕션 배포 검증.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md

<!-- 운영 관찰 모드 컨텍스트 -->
<!-- - 신규 기능 X, 버그 수정/정확도 보강만 허용 -->
<!-- - 디자인 변경 X (숫자만 정확해짐, 카드/레이아웃 구조 동일) -->
<!-- - 항상 프로덕션 배포 후 테스트 (로컬 서버 X) -->
<!-- - 배포: `cd cha-bio-safety && npm run deploy -- --branch production --commit-message "ascii"` -->

<interfaces>
<!-- 핵심 타입 — 추가 탐색 불필요 -->

From cha-bio-safety/src/hooks/useInspectionRevisitPopup.ts (imported in InspectionPage.tsx line 15):
```typescript
export type MonthRecordEntry = {
  result:    CheckResult                 // 'normal' | 'caution' | 'bad'
  checkedAt: string
  staffName?: string
  recordId:  string
  status:    'open' | 'resolved'
}
```

From cha-bio-safety/src/types/index.ts:
```typescript
export type CheckResult = 'normal' | 'caution' | 'bad'
```

From cha-bio-safety/src/utils/inspectionProgress.ts (호출자가 보내주는 데이터에 의존, 인터페이스 변경 없음):
```typescript
export function computeCardCompletion(options: {
  cps: CheckPoint[]
  monthRecordDates: Record<string, string[]>   // ← 여기 들어가는 데이터를 정확히 채워야 함
}): number
```
</interfaces>

<existing_code>
<!-- 정확한 수정 위치 — 추가 grep 불필요 -->

InspectionPage.tsx 라인 4014 (현재):
```typescript
// 대시보드와 동일한 완료 기준(normal/caution) 으로 날짜 인덱스 구축
if (rawResult === 'normal' || rawResult === 'caution') {
  const checkedAt = entry.checkedAt
  if (checkedAt) {
    if (!monthDatesMap[cpId]) monthDatesMap[cpId] = []
    monthDatesMap[cpId].push(checkedAt.slice(0, 10))
  }
}
```

InspectionPage.tsx 라인 3216 (현재):
```typescript
const fDone = fCPs.filter(cp => monthRecords[cp.id]).length
```

stats.ts 7곳 (라인 98, 107, 188, 313, 334, 417, 427) — 모두 동일 패턴:
```sql
AND cr.result IN ('normal','caution')
```
</existing_code>

<scope_boundary>
<!-- 손대지 말 것 (다른 룰을 가진 사용처) -->
- InspectionPage.tsx 라인 2927 / 2967 / 3052 의 `monthRecords` 사용처 — 의미가 "이미 점검 시도 있음"
  (firstPending 후보 / 자동 다음 cp 이동 흐름). 손대면 자동 흐름이 깨진다.
- stats.ts 라인 206 의 `WHERE result IN ('bad','caution')` — 불량/주의 통계 (별도 목적, 완료 정의와 무관).
- DB 데이터 변경 없음. 'bad' 기록은 그대로 두고 status='resolved' 컬럼에 의존.
</scope_boundary>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Frontend — InspectionPage 완료 룰 통일 (헬퍼 + 두 위치 수정 + 주석 갱신)</name>
  <files>
    cha-bio-safety/src/pages/InspectionPage.tsx
    cha-bio-safety/src/utils/inspectionProgress.ts
  </files>
  <action>
**1. InspectionPage.tsx — 모듈 레벨 헬퍼 추가 (imports 직후, 라인 17~18 근처):**

`import { computeCardCompletion } from '../utils/inspectionProgress'` 다음 빈 줄에 추가:

```typescript
// 완료 정의 통일 — 카드/대시보드/층별 화면 모두 동일 룰을 사용한다.
// "완료" = normal | caution | (bad + status='resolved')
// bad+open (조치 대기) 와 기록 없음은 미완료. (260426-f54)
const isCpCompleted = (entry: MonthRecordEntry | undefined): boolean =>
  entry?.result === 'normal' ||
  entry?.result === 'caution' ||
  (entry?.result === 'bad' && entry?.status === 'resolved')
```

타입 import 는 이미 라인 15 의 `import { useInspectionRevisitPopup, type MonthRecordEntry } from '../hooks/useInspectionRevisitPopup'` 으로 충족됨.

**2. InspectionPage.tsx 라인 4014 — monthDatesMap 빌드 조건 확장:**

현재:
```typescript
if (rawResult === 'normal' || rawResult === 'caution') {
```

변경:
```typescript
// 카드 완료 기준 (260426-f54): normal | caution | (bad + status='resolved')
if (rawResult === 'normal' || rawResult === 'caution' || (rawResult === 'bad' && entry.status === 'resolved')) {
```

`entry` 는 같은 블록 라인 4001 의 MonthRecordEntry 객체. `entry.status` 는 라인 4006 에서 'open'|'resolved' 로 보장됨.

주석 라인 4013 ("대시보드와 동일한 완료 기준(normal/caution) 으로 날짜 인덱스 구축") 도 위 의미에 맞게 갱신: "대시보드와 동일한 완료 기준(normal/caution/bad-resolved) 으로 날짜 인덱스 구축".

**3. InspectionPage.tsx 라인 3216 — 층별 fDone 카운트:**

현재:
```typescript
const fDone = fCPs.filter(cp => monthRecords[cp.id]).length
```

변경:
```typescript
// 260426-f54: '점검 시도 있음' 이 아니라 '확정 완료' 를 카운트
const fDone = fCPs.filter(cp => isCpCompleted(monthRecords[cp.id])).length
```

**4. inspectionProgress.ts — 주석만 갱신 (코드 변경 없음):**

라인 3~6 의 주석:
```typescript
// 대시보드 월간 카드와 동일 기준의 완료 개수 계산 (모바일 일반점검 카드용).
// - cp.defaultResult 또는 description '[접근불가]' 는 자동 완료
// - 그 외에는 당월 normal/caution 기록이 하나라도 있으면 완료
// - monthRecordDates: cp.id → 당월 normal/caution 기록들의 YYYY-MM-DD 배열
```

으로 갱신:
```typescript
// 대시보드 월간 카드와 동일 기준의 완료 개수 계산 (모바일 일반점검 카드용).
// - cp.defaultResult 또는 description '[접근불가]' 는 자동 완료
// - 그 외에는 당월 완료된 점검(normal | caution | bad+resolved) 기록이 하나라도 있으면 완료
// - monthRecordDates: cp.id → 당월 완료 기록들의 YYYY-MM-DD 배열
//   (호출자가 InspectionPage 라인 4014 에서 같은 룰로 채워야 정합) (260426-f54)
```

함수 본문은 그대로 (인터페이스 기반 호출이라 호출자가 보내주는 데이터가 새 룰을 따르면 자동으로 정확).

**손대지 말 것:**
- 라인 2927 / 2967 / 3052 의 `monthRecords` 사용처 (firstPending / 자동 다음 cp 이동) — "점검 시도 있음" 의미라 다른 룰
  </action>
  <verify>
    <automated>cd /Users/jykevin/Documents/20260328/cha-bio-safety && npm run build 2>&1 | tail -20 && grep -n "isCpCompleted" src/pages/InspectionPage.tsx && grep -n "rawResult === 'bad' && entry.status === 'resolved'" src/pages/InspectionPage.tsx && grep -n "bad-resolved\|bad+resolved" src/utils/inspectionProgress.ts</automated>
  </verify>
  <done>
- `cd cha-bio-safety && npm run build` 통과 (TypeScript 에러 0)
- `grep "isCpCompleted" InspectionPage.tsx` → 헬퍼 정의 1줄 + 사용 1줄 (총 2 매치)
- `grep "rawResult === 'bad' && entry.status === 'resolved'" InspectionPage.tsx` → 1 매치 (라인 4014)
- `grep "bad-resolved\|bad+resolved" inspectionProgress.ts` → 1 매치 (주석)
- 라인 2927/2967/3052 의 `monthRecords` 사용처는 변경 없음 (회귀 방지)
- atomic git commit 1개: `fix(inspection): completion rule bad-resolved (frontend)` (한글 본문 OK, 제목 ASCII)
  </done>
</task>

<task type="auto">
  <name>Task 2: Backend — stats.ts SQL 7곳 완료 룰 확장</name>
  <files>
    cha-bio-safety/functions/api/dashboard/stats.ts
  </files>
  <action>
**라인 98, 107, 188, 313, 334, 417, 427 — 모두 같은 패턴 변환:**

현재 (7곳 동일):
```sql
AND cr.result IN ('normal','caution')
```

변경 (7곳 동일):
```sql
AND (cr.result IN ('normal','caution') OR (cr.result='bad' AND cr.status='resolved'))
```

**괄호 그루핑 매우 중요** — 주변 AND 와 우선순위가 안 깨져야 한다. 7곳 모두 정확히 같은 형태로.

**컨텍스트별 위치 (참고):**
- 라인 98, 107: `calcStreakDays` — 연속 점검일 카운트
- 라인 188: 오늘 inspect 일정의 완료 개소 카운트 (대시보드 inspDone)
- 라인 313: check_points 가 없는 카테고리 (CCTV/회전문/주차장비) 의 hasRecord 판정
- 라인 334: 월간 도넛 카테고리별 done 카운트
- 라인 417, 427: schedule_items 자동 done 동기화 (다음 일정 전까지 / open-ended)

각 변경 옆에 한 줄 주석 첨부 권장:
```sql
-- 260426-f54: 완료 = normal | caution | (bad + resolved)
AND (cr.result IN ('normal','caution') OR (cr.result='bad' AND cr.status='resolved'))
```

**손대지 말 것:**
- 라인 206 의 `WHERE result IN ('bad','caution')` (불량/주의 통계 — 별도 목적). 절대 같은 패턴으로 인식해서 잘못 수정하지 말 것.
  </action>
  <verify>
    <automated>cd /Users/jykevin/Documents/20260328/cha-bio-safety && grep -nE "cr\.result IN \('normal','caution'\)" functions/api/dashboard/stats.ts | grep -v "OR (cr.result='bad'" ; echo "---expected: 0 lines (모두 확장 적용)---" ; grep -nE "cr\.result IN \('normal','caution'\) OR \(cr\.result='bad' AND cr\.status='resolved'\)" functions/api/dashboard/stats.ts | wc -l ; echo "---expected: 7---" ; grep -n "WHERE result IN ('bad','caution')" functions/api/dashboard/stats.ts ; echo "---expected: 1 line (라인 206 변경 없음)---" ; npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>
- `grep "cr.result IN ('normal','caution')"` 중 OR 절 없는 라인 0개 (모두 확장됨)
- `grep "cr.result IN ('normal','caution') OR (cr.result='bad' AND cr.status='resolved')"` → 정확히 7 매치
- 라인 206 의 `WHERE result IN ('bad','caution')` 그대로 (1 매치)
- `npm run build` 통과
- atomic git commit 1개: `fix(dashboard): completion rule bad-resolved (sql)` (한글 본문 OK, 제목 ASCII)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 프로덕션 배포 + 사용자 시각 검증</name>
  <what-built>
- Frontend: InspectionPage isCpCompleted 헬퍼 + monthDatesMap 빌드 룰 확장 + 층별 fDone 카운트 통일 + inspectionProgress 주석
- Backend: stats.ts SQL 7곳 (cr.result IN ('normal','caution') OR (cr.result='bad' AND cr.status='resolved')) 확장
  </what-built>
  <how-to-verify>
**1. 프로덕션 배포 (Claude 가 자동 실행):**
```bash
cd /Users/jykevin/Documents/20260328/cha-bio-safety
npm run deploy -- --branch production --commit-message "fix-completion-rule-bad-resolved"
```
배포 성공 로그 확인 (Deployment URL 출력).

**2. 사용자 검증 (PWA 캐시 주의 — 새로고침 또는 재설치 필요):**

a. **소화전 카드 카운트:**
   - 일반 점검 → 소화전 카드
   - 기대: **151/154** (이전 150/154)
   - 즉, 4/15 에 bad → resolved 처리된 CP-2F-4-SH 가 자동으로 +1

b. **InspectionPage 층별 화면:**
   - 소화전 → 연구동 2층 선택
   - 기대: **6/6** 그대로 (이미 6/6 였음 — 룰 통일로 의미만 정확해짐, 숫자 변동 없음)
   - 회귀 체크: 다른 층의 fDone 카운트가 비합리적으로 줄지 않는지 확인

c. **대시보드 월간 통계:**
   - 대시보드 → 월간 도넛 (소화전)
   - 기대: 카드 카운트와 동일 비율로 보정 반영

d. **회귀 — bad+open 케이스:**
   - 만약 현재 bad+status='open' (조치 대기 중) 인 cp 가 있다면 그대로 미완료로 표시되는지 확인
   - 카드 카운트가 그 cp 만큼 추가로 오르면 안 됨

e. **PWA 캐시:**
   - "여전히 똑같다" 면 SW 캐시 의심. 앱 강제 재설치 또는 Splash 버전 체크 동작 확인.
  </how-to-verify>
  <resume-signal>
"approved" — 모든 화면 일관, 카드 151/154 확인, 회귀 없음
또는 이슈 발견 시 구체 증상 (어느 화면/어느 카테고리/기대 vs 실제 숫자)
  </resume-signal>
</task>

</tasks>

<verification>
**자동 검증 (Task 1, 2):**
- `npm run build` 통과
- grep 카운트 매치 (Task 1: 헬퍼 2회 + 라인 4014 1회 + 주석 1회 / Task 2: SQL 7회 확장 + 라인 206 보존)

**수동 검증 (Task 3):**
- 프로덕션 배포 후 PWA 새로고침
- 소화전 카드 151/154 (이전 150/154) — CP-2F-4-SH 가 bad+resolved 로 완료 카운트
- 연구동 2층 6/6 그대로 — 룰 통일로 의미 정합
- bad+open 케이스 (있다면) 미완료 유지
</verification>

<success_criteria>
- 카드 / 대시보드 월간 / 층별 화면 모두 동일한 "완료" 정의 사용
- CP-2F-4-SH (bad+resolved) 가 카드에서 완료로 카운트 (소화전 151/154)
- 연구동 2층 fDone=6/6 그대로 유지
- bad+open 점검은 모든 화면에서 미완료
- TypeScript 빌드 통과
- 2개 atomic commit (frontend / backend) + 프로덕션 배포 1회
</success_criteria>

<output>
After completion, create `.planning/quick/260426-f54-completion-rule-bad-resolved/260426-f54-SUMMARY.md` with:
- Final commit hashes (frontend, backend)
- Production deployment URL
- Verified card counts (소화전 카드, 층별 화면, 대시보드 월간) — 사용자 확인 결과
- Any regressions found and fixed
- STATE.md "Quick Tasks Completed" 표에 1행 추가 권장
</output>
