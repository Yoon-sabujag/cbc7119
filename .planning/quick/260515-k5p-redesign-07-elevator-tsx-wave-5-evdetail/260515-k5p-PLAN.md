---
quick_id: 260515-k5p
slug: redesign-07-elevator-tsx-wave-5-evdetail
type: execute
wave: 1
depends_on:
  - 260515-hbv  # 2D EvDetailModal sketch (1799줄, KIND_STYLE 색 결정 권위)
  - 260515-jp3  # Wave 4 RepairNewModal — warning 토큰 + ev-btn-selected 패턴
  - 260515-iz1  # Wave 3 — lucide X import 확보, ModalWrap/Field 패턴
  - 260515-ia6  # Wave 2 — EvSelector ev-btn-selected 패턴
  - 260515-3mc  # Wave 1 — TYPE_ICON_COMPONENT 매퍼
files_modified:
  - cha-bio-safety/src/pages/ElevatorPage.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "EvDetailModal 비즈니스 로직 100% 보존 (useState 3종 periodIdx/checkFilter/histTab, body scroll lock useEffect, useQuery['ev_history', ev.id, from, to], floor stats 가공, filtered/sort 로직)"
    - "props 시그니처 보존 ({ ev, onClose }: { ev:Elevator; onClose:()=>void })"
    - "ElevatorInfoCard 호출 변경 0건 (`<ElevatorInfoCard ev={ev} compact={!isDesktop} />` 라인 그대로 — 본체 미수정)"
    - "TYPE_ICON_COMPONENT 매퍼 적용 (헤더 호기 아이콘 — 이모지 {TYPE_ICON[ev.type]} 제거)"
    - "lucide X 닫기 버튼 적용 (헤더 ✕ 문자 제거)"
    - "KIND_STYLE 색 결정 2D 권위 적용: fault=text-fire/bg-fire (var(--status-fire-bar)), repair=text-safe (var(--status-safe-bar)) 유지, inspect=text-info (var(--status-info-bar)) 유지, annual=text-text-tertiary 회색"
    - "KIND_STYLE icon lucide 매핑: fault=AlertOctagon, repair=Wrench, inspect=ClipboardCheck, annual=FileSearch"
    - "층별 누적 이력 lucide 매핑: 미해결=AlertOctagon+text-fire, 이력있음=AlertTriangle+text-warning, 이상없음=CheckCircle2+text-safe"
    - "이력 빈 상태 FileSearch + '해당 이력이 없어요' (시안 line 1397~1399 권위)"
    - "층별 빈 상태 CheckCircle2 + '해당 기간 이상 없음' (이모지 ✅ 제거)"
    - "기간 선택/점검필터/이력탭 = ev-btn-selected 패턴 (bg-accent + text-text-on-accent 선택 / bg-surface-sunken + text-text-tertiary 비선택) — Wave 2/4 패턴과 일관"
    - "변환 영역 이모지 0건 (🛗📦🔲↕️ + ✕ + 🔴⚠️✅ + 🔧📋🔍 모두 제거)"
    - "변환 영역 9-11px 폰트 0건 (text-caption 12px+ / text-label 13px / text-body-sm 14px / text-body 16px)"
    - "변환 영역 옛 토큰 var(--bg/bg2/bg3/bd/bd2/t1/t2/t3/danger/warn/info/safe/acl) 0건"
    - "변환 영역 인라인 style 화이트리스트 외 0건 — 화이트리스트: position:'fixed' / inset / zIndex / top/left/right/bottom (모달 동적 위치) / transform (데스크톱 가운데) / maxHeight (calc 동적) / boxShadow (§6.7 모달 예외) / WebkitOverflowScrolling:'touch' / overscrollBehavior:'contain'"
    - "보존 영역 0건 수정: ModalWrap/Field/MultiPhotoUpload/EvSelector/EsBtn/EsNodeMap/FaultNewModal/FaultNewFullscreen/FaultResolveModal/RepairNewModal/InspectModal/ElevatorInfoCard/KoelsaHistorySection/RepairListSection/icons.tsx/tailwind.config.js"
    - "ev_detail 모달 call site (line 1672 + line 1045) 보존 (2건)"
    - "isDesktop 분기 보존 (모바일 bottom-sheet / 데스크톱 중앙 모달)"
    - "ev.type !== 'escalator' 분기 보존 (층별 통계 + 점검항목 필터 숨김)"
    - "HISTORY_TABS / PERIOD_OPTIONS / CHECK_ITEM_LABELS 상수 그대로 (라인 1681~1694)"
    - "npm run build PASS (TypeScript 0 에러)"
  artifacts:
    - path: cha-bio-safety/src/pages/ElevatorPage.tsx
      provides: "EvDetailModal v0.1.1 토큰 + Tailwind 변환 (line ~1696~1893, +확장 라인)"
      contains: "function EvDetailModal"
  key_links:
    - from: cha-bio-safety/src/pages/ElevatorPage.tsx
      to: cha-bio-safety/tailwind.config.js
      via: "fire/safe/warning/info/accent + bar/bg variants + text-text-on-accent/text-text-tertiary 토큰"
      pattern: "bg-fire-bar|text-fire|text-safe|text-warning|text-info|text-text-tertiary|bg-accent|text-text-on-accent"
    - from: cha-bio-safety/src/pages/ElevatorPage.tsx
      to: lucide-react
      via: "AlertOctagon, ClipboardCheck, FileSearch, CheckCircle2 신규 import; AlertTriangle/Wrench/X 재사용"
      pattern: "from 'lucide-react'"
    - from: cha-bio-safety/src/pages/ElevatorPage.tsx
      to: cha-bio-safety/src/components/ui/icons.tsx
      via: "ElevatorIcon (TYPE_ICON_COMPONENT 매퍼 경유 — 이미 line 17 import 중)"
      pattern: "TYPE_ICON_COMPONENT\\[ev\\.type\\]"
---

<objective>
ElevatorPage.tsx 의 **EvDetailModal** (line ~1696~1893, 198줄) 함수 본체를 v0.1.1 디자인 토큰 + Tailwind utility 전용으로 변환한다. 2D 시안(260515-hbv) 권위 1:1 매핑으로 6 영역(헤더/기간선택/InfoCard 호출 보존/층별통계/점검필터/이력 리스트)을 모두 변환하되, 비즈니스 로직(state 3종/useEffect/useQuery/floor 가공/filtered)과 호출 시그니처를 100% 보존한다.

Purpose: 07-elevator 재디자인 wave 5 — 호기 상세 모달(detail) v0.1.1 디자인 토큰 룰 + Tailwind only 룰 적용. KIND_STYLE 색 통일 (fault=fire / annual=회색) 으로 메모리 룰 "fire=호기 고장 미수리" 와 list/FaultNew CTA 와 일관성 확보. 이모지 0건 + 9-11px 0건 + 옛 토큰 0건 룰 강제.

Output: ElevatorPage.tsx 단일 파일 수정 (EvDetailModal 함수 본체 + lucide-react import 라인 보강).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/STATE.md

# 2D 시안 권위 (KIND_STYLE 색 + 6 영역 매트릭스 최종 결정)
@cha-bio-safety/docs/redesign-context/07-elevator/sketch/evdetail-modal-sketch.html

# 변환 대상 (line 1696~1893 — EvDetailModal 본체)
@cha-bio-safety/src/pages/ElevatorPage.tsx

# Wave 1~4 누적 패턴
@.planning/quick/260515-3mc-redesign-07-elevator-tsx-wave-1-list/260515-3mc-SUMMARY.md
@.planning/quick/260515-ia6-redesign-07-elevator-tsx-wave-2-evselect/260515-ia6-SUMMARY.md
@.planning/quick/260515-iz1-redesign-07-elevator-tsx-wave-3-fault-3-/260515-iz1-SUMMARY.md
@.planning/quick/260515-jp3-redesign-07-elevator-tsx-wave-4-repairne/260515-jp3-SUMMARY.md

# 2D 시안 결정 PLAN
@.planning/quick/260515-hbv-redesign-07-elevator-2d-sketch-evdetailm/260515-hbv-PLAN.md
@.planning/quick/260515-hbv-redesign-07-elevator-2d-sketch-evdetailm/260515-hbv-SUMMARY.md

# Tailwind 토큰 카탈로그 + 아이콘 컴포넌트
@cha-bio-safety/tailwind.config.js
@cha-bio-safety/src/components/ui/icons.tsx

<interfaces>
<!-- TYPE_ICON_COMPONENT 매퍼 (ElevatorPage.tsx line 197~202) — Wave 1 신설 -->
```typescript
const TYPE_ICON_COMPONENT: Record<string, React.ComponentType<{ size?: number | string; className?: string; strokeWidth?: number | string }>> = {
  passenger:  ElevatorIcon,
  cargo:      Package,
  dumbwaiter: UtensilsCrossed,
  escalator:  MoveDiagonal,
}
```
사용 패턴 (Wave 1+3 검증):
```tsx
const TypeIcon = TYPE_ICON_COMPONENT[ev.type]
<TypeIcon size={20} className="text-text-tertiary" />
```

<!-- 현 lucide-react import (line 16) — Wave 3 까지 누적 -->
```typescript
import { Package, UtensilsCrossed, MoveDiagonal, ChevronRight, ChevronUp, ChevronDown, AlertTriangle, Wrench, X } from 'lucide-react'
```
본 wave 추가 필요: **AlertOctagon, ClipboardCheck, FileSearch, CheckCircle2** (4종 신규)

<!-- HISTORY_TABS / PERIOD_OPTIONS (line 1681~1694) — 상수 그대로 -->
```typescript
const PERIOD_OPTIONS = [
  { label:'1개월', months:1 }, { label:'3개월', months:3 },
  { label:'6개월', months:6 }, { label:'1년',   months:12 },
]
type HistoryTab = 'all' | 'fault' | 'repair' | 'inspect' | 'annual'
const HISTORY_TABS: { key:HistoryTab; label:string }[] = [
  { key:'all', label:'전체' }, { key:'fault', label:'고장' },
  { key:'repair', label:'수리' }, { key:'inspect', label:'점검' },
  { key:'annual', label:'검사' },
]
```

<!-- EvDetailModal props 시그니처 -->
```typescript
function EvDetailModal({ ev, onClose }: { ev:Elevator; onClose:()=>void })
```
호출 사이트 (2건 보존):
- line 1045: 데스크톱 분기
- line 1672: 모바일 분기 (모달 단일 라우터)

<!-- KIND_STYLE 색 결정 매트릭스 (2D 시안 권위 — 260515-hbv) -->
| kind    | 색 토큰 (text)        | 좌측 3px 색바 토큰          | lucide 아이콘     |
|---------|----------------------|-----------------------------|-------------------|
| fault   | text-fire            | bg-fire-bar (var(--status-fire-bar))    | AlertOctagon      |
| repair  | text-safe            | bg-safe-bar (var(--status-safe-bar))    | Wrench            |
| inspect | text-info            | bg-info-bar (var(--status-info-bar))    | ClipboardCheck    |
| annual  | text-text-tertiary   | bg-text-tertiary (var(--text-tertiary)) | FileSearch        |

<!-- 층별 누적 이력 legend + row 우측 아이콘 매핑 -->
| 상태       | lucide              | 색 토큰          |
|------------|---------------------|------------------|
| 미해결     | AlertOctagon        | text-fire        |
| 이력있음   | AlertTriangle       | text-warning     |
| 이상없음   | CheckCircle2        | text-safe        |
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: lucide-react import 확장 + EvDetailModal 헤더/기간선택/스크롤body 컨테이너 변환</name>
  <files>cha-bio-safety/src/pages/ElevatorPage.tsx</files>
  <action>
**Step 1.1: lucide-react import 라인 16 확장**

현재 (Wave 3 누적):
```typescript
import { Package, UtensilsCrossed, MoveDiagonal, ChevronRight, ChevronUp, ChevronDown, AlertTriangle, Wrench, X } from 'lucide-react'
```

변경 후 (추가 4종):
```typescript
import { Package, UtensilsCrossed, MoveDiagonal, ChevronRight, ChevronUp, ChevronDown, AlertTriangle, Wrench, X, AlertOctagon, ClipboardCheck, FileSearch, CheckCircle2 } from 'lucide-react'
```

**Step 1.2: EvDetailModal 헤더 변환 (line ~1761~1771)**

변환 전:
```tsx
{/* 헤더 */}
<div style={{ flexShrink:0, padding:'14px 16px 12px', borderBottom:'1px solid var(--bd)', display:'flex', alignItems:'center', gap:10 }}>
  <div style={{ width:36, height:36, borderRadius:9, background:'var(--bg3)', display:'flex', alignItems:'center', justifyContent:'center', fontSize:20, flexShrink:0 }}>
    {TYPE_ICON[ev.type]}
  </div>
  <div style={{ flex:1, minWidth:0 }}>
    <div style={{ fontSize:14, fontWeight:700, color:'var(--t1)' }}>{ev.number}호기</div>
    <div style={{ fontSize:10, color:'var(--t3)' }}>{ev.location}</div>
  </div>
  <button onClick={onClose} style={{ background:'none', border:'none', color:'var(--t3)', cursor:'pointer', fontSize:18 }}>✕</button>
</div>
```

변환 후 (TypeIcon 매퍼 + lucide X + text-* 토큰):
```tsx
{(() => {
  const TypeIcon = TYPE_ICON_COMPONENT[ev.type]
  return (
    <div className="flex-shrink-0 px-4 pt-[14px] pb-3 border-b border-border-default flex items-center gap-2.5">
      <div className="w-9 h-9 rounded-lg bg-surface-sunken flex items-center justify-center flex-shrink-0">
        <TypeIcon size={20} className="text-text-secondary" />
      </div>
      <div className="flex-1 min-w-0">
        <div className="text-body-sm font-bold text-text-primary">{ev.number}호기</div>
        <div className="text-caption text-text-tertiary">{ev.location}</div>
      </div>
      <button onClick={onClose} className="bg-transparent border-0 text-text-tertiary cursor-pointer flex items-center justify-center p-1">
        <X size={20} />
      </button>
    </div>
  )
})()}
```

**Step 1.3: 기간 선택 변환 (line ~1773~1786)**

변환 후:
```tsx
{/* 기간 선택 */}
<div className="flex-shrink-0 px-4 pt-2.5 pb-2 border-b border-border-default">
  <div className="text-caption font-bold text-text-tertiary mb-1.5">조회 기간</div>
  <div className="flex gap-1.5">
    {PERIOD_OPTIONS.map((p,i) => (
      <button
        key={i}
        onClick={() => setPeriodIdx(i)}
        className={
          'flex-1 py-1.5 rounded-md border-0 cursor-pointer text-label font-bold ' +
          (periodIdx===i
            ? 'bg-accent text-text-on-accent'
            : 'bg-surface-sunken text-text-tertiary')
        }
      >
        {p.label}
      </button>
    ))}
  </div>
  <div className="text-caption text-text-tertiary mt-1.5 text-right font-mono">{from} ~ {to}</div>
</div>
```

**Step 1.4: 모달 컨테이너 인라인 화이트리스트 보존 + 단색 surface 토큰 적용 (line 1752~1759)**

변환 후 (overlay + isDesktop 분기 — 동적 값/position/transform/maxHeight/boxShadow 화이트리스트만 인라인, 색은 Tailwind):
```tsx
return (
  <>
    <div
      onClick={onClose}
      className="bg-overlay"
      style={{ position:'fixed', inset:0, zIndex:90 }}
    />
    <div
      className={
        isDesktop
          ? 'fixed z-[100] bg-surface-raised rounded-2xl w-[720px] max-w-[92vw] max-h-[88vh] flex flex-col overflow-x-hidden border border-border-default'
          : 'fixed z-[100] left-0 right-0 bg-surface-raised rounded-t-[20px] flex flex-col overflow-x-hidden'
      }
      style={
        isDesktop
          ? { top:'50%', left:'50%', transform:'translate(-50%, -50%)', boxShadow:'0 20px 60px rgba(0,0,0,.5)' }
          : { bottom:NAV_H, maxHeight:'calc(100dvh - var(--sat, 44px) - var(--sab, 0px) - 54px)' }
      }
    >
```

**주의 (`bg-overlay` 토큰 확인)**: tailwind.config.js 에 `bg-overlay` 없으면 `bg-black/60` 으로 폴백. 사전 확인:
```bash
grep -nE "overlay" cha-bio-safety/tailwind.config.js
```
- 있으면 `bg-overlay` 사용
- 없으면 `bg-black/60` 사용 (Tailwind 기본 + alpha)

**Step 1.5: 스크롤 body 컨테이너 변환 (line 1789)**

변환 전:
```tsx
<div style={{ flex:1, minHeight:0, overflowY:'auto', WebkitOverflowScrolling:'touch', overscrollBehavior:'contain', padding:'12px 16px', display:'flex', flexDirection:'column', gap:14 } as React.CSSProperties}>
```

변환 후 (`WebkitOverflowScrolling` + `overscrollBehavior` 만 인라인 보존):
```tsx
<div
  className="flex-1 min-h-0 overflow-y-auto px-4 py-3 flex flex-col gap-[14px]"
  style={{ WebkitOverflowScrolling:'touch', overscrollBehavior:'contain' } as React.CSSProperties}
>
```

**Step 1.6: 로딩 텍스트 변환 (line 1795)**

변환 전:
```tsx
<div style={{ textAlign:'center', padding:'30px 0', color:'var(--t3)', fontSize:13 }}>불러오는 중...</div>
```

변환 후:
```tsx
<div className="text-center py-[30px] text-text-tertiary text-label">불러오는 중...</div>
```

**Step 1.7: ElevatorInfoCard 호출 라인 (line 1792) — 수정 0건**
```tsx
<ElevatorInfoCard ev={ev} compact={!isDesktop} />
```
이 라인은 **그대로** 보존. props/className 추가 X.

**Step 1.8: 사전 검증 (작업 전 grep — 컨텍스트 확보)**

```bash
sed -n '1696,1893p' cha-bio-safety/src/pages/ElevatorPage.tsx | wc -l   # 변환 전 라인 수 기록
grep -nE "^import .* from 'lucide-react'" cha-bio-safety/src/pages/ElevatorPage.tsx
grep -nE "function EvDetailModal" cha-bio-safety/src/pages/ElevatorPage.tsx
```

**보존 룰 (Task 1 — 헤더/기간/컨테이너):**
- useState 3종 + useEffect body lock + useQuery + floor 가공 + filtered 미수정 (Task 1 범위 밖)
- ElevatorInfoCard 호출 그대로
- ✕ → lucide X, {TYPE_ICON[ev.type]} → TYPE_ICON_COMPONENT 매퍼
- 9-11px → text-caption(12)/text-label(13)/text-body-sm(14) 격상
- 옛 토큰 var(--bg3/bd/t1/t2/t3/acl) 0건 (Task 1 영역)
- 인라인 화이트리스트 외 정적 인라인 0건
  </action>
  <verify>
    <automated>
# Step 1.8 사전 컨텍스트 확보 후 변환 적용. 이후 임시 verify:
sed -n '1752,1800p' cha-bio-safety/src/pages/ElevatorPage.tsx | grep -E "var\(--(bg3|bd|t1|t2|t3|acl)\)"  # 0건 (Task 1 영역)
sed -n '1752,1800p' cha-bio-safety/src/pages/ElevatorPage.tsx | grep -P "✕"                                    # 0건
grep -nE "AlertOctagon|ClipboardCheck|FileSearch|CheckCircle2" cha-bio-safety/src/pages/ElevatorPage.tsx | head -5  # import 라인 1건 이상
grep -E "<ElevatorInfoCard ev=\{ev\} compact=\{!isDesktop\}" cha-bio-safety/src/pages/ElevatorPage.tsx           # 1건 (호출 보존)
    </automated>
  </verify>
  <done>
- lucide-react import 라인에 AlertOctagon/ClipboardCheck/FileSearch/CheckCircle2 4종 추가됨
- EvDetailModal 헤더 영역: TYPE_ICON_COMPONENT 매퍼 + lucide X 적용, 이모지 0건, 9-11px 0건
- 기간 선택 영역: ev-btn-selected 패턴 (bg-accent/text-text-on-accent 선택), 옛 토큰 0건
- 모달 컨테이너: 단색 surface 토큰 + 인라인 화이트리스트(position/inset/zIndex/top/left/right/bottom/transform/maxHeight/boxShadow) 만 보존
- 스크롤 body: WebkitOverflowScrolling + overscrollBehavior 만 인라인 보존
- ElevatorInfoCard 호출 라인 한 글자도 수정되지 않음
  </done>
</task>

<task type="auto">
  <name>Task 2: 층별 누적 이력 + 점검항목 필터 변환 (엘리베이터 분기 영역)</name>
  <files>cha-bio-safety/src/pages/ElevatorPage.tsx</files>
  <action>
**Step 2.1: 층별 누적 이력 변환 (line ~1799~1832)**

변환 전:
```tsx
{/* 층별 누적 이력 */}
{ev.type !== 'escalator' && (
  <div>
    <div style={{ display:'flex', alignItems:'center', marginBottom:8 }}>
      <div style={{ fontSize:11, fontWeight:700, color:'var(--t2)' }}>층별 누적 이력</div>
      <div style={{ marginLeft:'auto', display:'flex', gap:8, fontSize:10, color:'var(--t3)' }}>
        <span>🔴 미해결</span>
        <span>⚠️ 이력있음</span>
        <span>✅ 이상없음</span>
      </div>
    </div>
    {floorStats.length === 0 ? (
      <div style={{ fontSize:12, color:'var(--t3)', padding:'10px 0' }}>해당 기간 이상 없음 ✅</div>
    ) : (
      floorStats.map(s => (
        <div key={s.floor} style={{ display:'flex', alignItems:'center', gap:8, padding:'7px 10px', background:'var(--bg3)', borderRadius:9, marginBottom:5 }}>
          <span style={{ fontSize:12, fontWeight:700, color:'var(--t1)', width:56, flexShrink:0 }}>{s.floor}</span>
          <div style={{ flex:1, display:'flex', gap:10, flexWrap:'wrap' }}>
            {s.fault_total > 0 && (
              <span style={{ fontSize:11, color:'var(--danger)' }}>
                고장 {s.fault_total}회{s.fault_unresolved > 0 ? ` (미해결 ${s.fault_unresolved})` : ''}
              </span>
            )}
            {s.action_count > 0 && (
              <span style={{ fontSize:11, color:'var(--warn)' }}>조치지적 {s.action_count}회</span>
            )}
          </div>
          <span style={{ fontSize:16 }}>
            {s.fault_unresolved > 0 ? '🔴' : s.fault_total > 0 || s.action_count > 0 ? '⚠️' : '✅'}
          </span>
        </div>
      ))
    )}
  </div>
)}
```

변환 후 (lucide AlertOctagon/AlertTriangle/CheckCircle2 적용, 색 토큰 fire/warning/safe + danger):
```tsx
{/* 층별 누적 이력 */}
{ev.type !== 'escalator' && (
  <div>
    <div className="flex items-center mb-2">
      <div className="text-label font-bold text-text-secondary">층별 누적 이력</div>
      <div className="ml-auto flex gap-2 text-caption text-text-tertiary">
        <span className="inline-flex items-center gap-1 text-fire"><AlertOctagon size={12} />미해결</span>
        <span className="inline-flex items-center gap-1 text-warning"><AlertTriangle size={12} />이력있음</span>
        <span className="inline-flex items-center gap-1 text-safe"><CheckCircle2 size={12} />이상없음</span>
      </div>
    </div>
    {floorStats.length === 0 ? (
      <div className="inline-flex items-center gap-1.5 text-caption text-text-tertiary py-2.5">
        <CheckCircle2 size={14} className="text-safe" />
        해당 기간 이상 없음
      </div>
    ) : (
      floorStats.map(s => (
        <div key={s.floor} className="flex items-center gap-2 px-2.5 py-1.5 bg-surface-sunken rounded-lg mb-1.5">
          <span className="text-caption font-bold text-text-primary w-14 flex-shrink-0">{s.floor}</span>
          <div className="flex-1 flex gap-2.5 flex-wrap">
            {s.fault_total > 0 && (
              <span className="text-label text-danger">
                고장 {s.fault_total}회{s.fault_unresolved > 0 ? ` (미해결 ${s.fault_unresolved})` : ''}
              </span>
            )}
            {s.action_count > 0 && (
              <span className="text-label text-warning">조치지적 {s.action_count}회</span>
            )}
          </div>
          <span className="flex items-center">
            {s.fault_unresolved > 0
              ? <AlertOctagon size={16} className="text-fire" />
              : (s.fault_total > 0 || s.action_count > 0)
                ? <AlertTriangle size={16} className="text-warning" />
                : <CheckCircle2 size={16} className="text-safe" />}
          </span>
        </div>
      ))
    )}
  </div>
)}
```

**주의 색 토큰 검증**:
- "고장 N회" 텍스트는 점검 항목 불량(danger)의 의미 — `text-danger` 사용 (v0.1.1 룰 §6.1: danger = 점검 항목 불량 / 미해결 카운트 텍스트)
- 우측 상태 아이콘은 fire(미수리) — 메모리 룰
- 조치지적은 warning (점검 시 발견된 조치 필요 사항)

**Step 2.2: 점검항목 필터 변환 (line ~1835~1848)**

변환 전:
```tsx
{/* 점검항목 필터 */}
{ev.type !== 'escalator' && (
  <div>
    <div style={{ fontSize:11, fontWeight:700, color:'var(--t2)', marginBottom:8 }}>점검항목 필터</div>
    <div style={{ display:'flex', gap:5, flexWrap:'wrap' }}>
      {['all','brake','door','safety_device','lighting','emergency_call'].map(k => (
        <button key={k} onClick={() => setCheckFilter(k)} style={{
          padding:'5px 10px', borderRadius:20, border:'none', cursor:'pointer', fontSize:10, fontWeight:700,
          background: checkFilter===k ? 'var(--acl)' : 'var(--bg3)',
          color:      checkFilter===k ? '#fff'       : 'var(--t3)',
        }}>{k==='all' ? '전체' : CHECK_ITEM_LABELS[k]}</button>
      ))}
    </div>
  </div>
)}
```

변환 후 (ev-btn-selected 패턴 일관):
```tsx
{/* 점검항목 필터 */}
{ev.type !== 'escalator' && (
  <div>
    <div className="text-label font-bold text-text-secondary mb-2">점검항목 필터</div>
    <div className="flex gap-1.5 flex-wrap">
      {['all','brake','door','safety_device','lighting','emergency_call'].map(k => (
        <button
          key={k}
          onClick={() => setCheckFilter(k)}
          className={
            'px-2.5 py-1 rounded-full border-0 cursor-pointer text-caption font-bold ' +
            (checkFilter===k
              ? 'bg-accent text-text-on-accent'
              : 'bg-surface-sunken text-text-tertiary')
          }
        >
          {k==='all' ? '전체' : CHECK_ITEM_LABELS[k]}
        </button>
      ))}
    </div>
  </div>
)}
```

**보존 룰 (Task 2 — 층별/점검필터):**
- `ev.type !== 'escalator'` 분기 그대로 (에스컬레이터 숨김)
- `floorStats.map` / `s.fault_unresolved` / `s.fault_total` / `s.action_count` 데이터 가공 미수정
- CHECK_ITEM_LABELS 상수 그대로
- setCheckFilter / checkFilter state 그대로
- 이모지 🔴⚠️✅ 모두 lucide 교체
- 옛 토큰 var(--bg3/danger/warn/acl/t1/t2/t3) → fire/warning/safe/danger/accent/text-* 토큰
- 9-11px 0건
  </action>
  <verify>
    <automated>
# Task 2 영역 (대략 1799~1850, sed 범위는 변환 후 적용 — 라인 이동 가능성)
grep -nE "층별 누적 이력|점검항목 필터" cha-bio-safety/src/pages/ElevatorPage.tsx | head -2
# 이모지 0건 확인 (전체 EvDetailModal 영역)
sed -n '1696,1900p' cha-bio-safety/src/pages/ElevatorPage.tsx | grep -P "🔴|⚠️|✅"   # 0건
# lucide 아이콘 사용 확인
grep -cE "<AlertOctagon|<AlertTriangle|<CheckCircle2" cha-bio-safety/src/pages/ElevatorPage.tsx   # 6건 이상 (legend 3 + row 3종)
    </automated>
  </verify>
  <done>
- 층별 누적 이력 legend 3 아이콘 (AlertOctagon/AlertTriangle/CheckCircle2) 적용
- floorStats.map row 우측 상태 아이콘 3분기 lucide 교체
- 빈 상태 "해당 기간 이상 없음" + CheckCircle2 (이모지 ✅ 제거)
- 점검항목 필터 ev-btn-selected 패턴 적용
- ev.type !== 'escalator' 분기 그대로
- CHECK_ITEM_LABELS / setCheckFilter / checkFilter state 미수정
- Task 2 영역 9-11px 0건, 옛 토큰 0건, 이모지 0건
  </done>
</task>

<task type="auto">
  <name>Task 3: 이력 리스트 + KIND_STYLE 색 결정 적용 + 회귀 검증 + npm build</name>
  <files>cha-bio-safety/src/pages/ElevatorPage.tsx</files>
  <action>
**Step 3.1: KIND_STYLE 객체 재구성 (line ~1745~1750)**

변환 전:
```tsx
const KIND_STYLE: Record<string,{color:string;label:string;icon:string}> = {
  fault:   { color:'var(--danger)', label:'고장',  icon:'🔴' },
  repair:  { color:'var(--safe)',   label:'수리',  icon:'🔧' },
  inspect: { color:'var(--info)',   label:'점검',  icon:'📋' },
  annual:  { color:'var(--warn)',   label:'검사',  icon:'🔍' },
}
```

변환 후 (이모지 제거 + Tailwind className enum + lucide component 매퍼):
```tsx
const KIND_STYLE: Record<string, {
  textCls: string;       // 텍스트 색 className
  barCls:  string;       // 좌측 3px 색바 background className
  label:   string;
  Icon:    React.ComponentType<{ size?: number | string; className?: string }>;
}> = {
  fault:   { textCls:'text-fire',           barCls:'bg-fire-bar',           label:'고장',  Icon: AlertOctagon },
  repair:  { textCls:'text-safe',           barCls:'bg-safe-bar',           label:'수리',  Icon: Wrench },
  inspect: { textCls:'text-info',           barCls:'bg-info-bar',           label:'점검',  Icon: ClipboardCheck },
  annual:  { textCls:'text-text-tertiary',  barCls:'bg-text-tertiary',      label:'검사',  Icon: FileSearch },
}
```

**주의 — `bg-text-tertiary` 토큰 검증**: tailwind.config.js 의 `text-tertiary` 는 color 키. `bg-text-tertiary` 가 자동 매핑되는지 확인:
```bash
grep -nE "'text-tertiary'" cha-bio-safety/tailwind.config.js
```
Tailwind colors 객체에 정의되면 `bg-text-tertiary`/`text-text-tertiary` 모두 사용 가능 (tailwind v3 표준). 만약 미작동 시 fallback: 좌측 색바를 인라인 `style={{ borderLeftColor: 'var(--text-tertiary)' }}` 화이트리스트 — 단 1차로 className 시도 후 build 결과 확인.

**Step 3.2: 이력 리스트 영역 변환 (line ~1851~1884)**

변환 전:
```tsx
{/* 이력 리스트 */}
<div>
  <div style={{ fontSize:11, fontWeight:700, color:'var(--t2)', marginBottom:8 }}>이력</div>
  {/* 이력 탭 */}
  <div style={{ display:'flex', gap:5, marginBottom:10, overflowX:'auto' }}>
    {HISTORY_TABS.map(t => (
      <button key={t.key} onClick={() => setHistTab(t.key)} style={{
        padding:'5px 12px', borderRadius:20, border:'none', cursor:'pointer', fontSize:11, fontWeight:700, whiteSpace:'nowrap', flexShrink:0,
        background: histTab===t.key ? 'var(--acl)' : 'var(--bg3)',
        color:      histTab===t.key ? '#fff'       : 'var(--t3)',
      }}>{t.label}</button>
    ))}
  </div>

  {filtered.length === 0 ? (
    <div style={{ textAlign:'center', padding:'20px 0', color:'var(--t3)', fontSize:12 }}>해당 이력이 없어요</div>
  ) : (
    filtered.map(h => {
      const ks = KIND_STYLE[h.kind] ?? KIND_STYLE.inspect
      return (
        <div key={h.id} style={{ padding:'9px 11px', background:'var(--bg3)', borderRadius:10, marginBottom:6, borderLeft:`3px solid ${ks.color}` }}>
          <div style={{ display:'flex', alignItems:'center', gap:6, marginBottom:4 }}>
            <span style={{ fontSize:11 }}>{ks.icon}</span>
            <span style={{ fontSize:11, fontWeight:700, color:ks.color }}>{ks.label}</span>
            {h.floor && <span style={{ fontSize:10, fontWeight:700, color:'var(--info)', background:'rgba(14,165,233,.12)', padding:'1px 6px', borderRadius:6 }}>{h.floor}</span>}
            {h.check_item && <span style={{ fontSize:10, color:'var(--t3)', background:'var(--bg4)', padding:'1px 6px', borderRadius:6 }}>{CHECK_ITEM_LABELS[h.check_item] ?? h.check_item}</span>}
            <span style={{ marginLeft:'auto', fontSize:10, color:'var(--t3)', fontFamily:'JetBrains Mono, monospace' }}>{h.date.slice(0,10)}</span>
          </div>
          <div style={{ fontSize:12, color:'var(--t1)', fontWeight:600 }}>{h.summary}</div>
          {h.detail && <div style={{ fontSize:11, color:'var(--t3)', marginTop:3 }}>{h.detail}</div>}
        </div>
      )
    })
  )}
</div>
```

변환 후 (KIND_STYLE className enum + 좌측 색바 동적 background className + FileSearch 빈 상태 + 발생층 info-bg 칩):
```tsx
{/* 이력 리스트 */}
<div>
  <div className="text-label font-bold text-text-secondary mb-2">이력</div>
  {/* 이력 탭 */}
  <div className="flex gap-1.5 mb-2.5 overflow-x-auto">
    {HISTORY_TABS.map(t => (
      <button
        key={t.key}
        onClick={() => setHistTab(t.key)}
        className={
          'px-3 py-1 rounded-full border-0 cursor-pointer text-label font-bold whitespace-nowrap flex-shrink-0 ' +
          (histTab===t.key
            ? 'bg-accent text-text-on-accent'
            : 'bg-surface-sunken text-text-tertiary')
        }
      >
        {t.label}
      </button>
    ))}
  </div>

  {filtered.length === 0 ? (
    <div className="flex flex-col items-center justify-center py-5 gap-2">
      <FileSearch size={28} className="text-text-tertiary" />
      <span className="text-caption text-text-tertiary">해당 이력이 없어요</span>
    </div>
  ) : (
    filtered.map(h => {
      const ks = KIND_STYLE[h.kind] ?? KIND_STYLE.inspect
      const KsIcon = ks.Icon
      return (
        <div key={h.id} className="relative pl-3 pr-2.5 py-2 bg-surface-sunken rounded-xl mb-1.5 overflow-hidden">
          <span className={'absolute left-0 top-0 bottom-0 w-[3px] ' + ks.barCls} />
          <div className="flex items-center gap-1.5 mb-1 flex-wrap">
            <KsIcon size={12} className={ks.textCls} />
            <span className={'text-caption font-bold ' + ks.textCls}>{ks.label}</span>
            {h.floor && (
              <span className="text-caption font-bold text-info bg-info-bg px-1.5 py-0.5 rounded-md">{h.floor}</span>
            )}
            {h.check_item && (
              <span className="text-caption text-text-tertiary bg-surface-raised px-1.5 py-0.5 rounded-md">
                {CHECK_ITEM_LABELS[h.check_item] ?? h.check_item}
              </span>
            )}
            <span className="ml-auto text-caption text-text-tertiary font-mono">{h.date.slice(0,10)}</span>
          </div>
          <div className="text-caption text-text-primary font-semibold">{h.summary}</div>
          {h.detail && <div className="text-caption text-text-tertiary mt-1">{h.detail}</div>}
        </div>
      )
    })
  )}
</div>
```

**주의 — 색바 배치 변경 근거**:
- 변환 전: `borderLeft:`3px solid ${ks.color}`` 동적 색 — Tailwind 만으로는 동적 색 className 못 함 (PurgeCSS 충돌)
- 변환 후: `<span className={...} />` 으로 absolute 좌측 3px overlay + `barCls` 의 4종 className (bg-fire-bar/bg-safe-bar/bg-info-bar/bg-text-tertiary) — 모두 정적 className 이므로 Tailwind safelist 불필요. 카드 본체에 `relative pl-3 overflow-hidden` 으로 색바 좌측 3px 공간 확보.
- 효과는 시안과 동일 (좌측 3px 색바 + 본문 padding)

**Step 3.3: 변환 영역 grep gate (sed 범위는 변환 후 라인 — 적용 후 측정)**

```bash
# (1) EvDetailModal 함수 범위 확인 (라인 변동 가능성)
grep -nE "^function EvDetailModal\(" cha-bio-safety/src/pages/ElevatorPage.tsx
# 출력으로 시작 라인 확인. 다음 function 정의까지가 변환 영역
grep -nE "^function [A-Z]" cha-bio-safety/src/pages/ElevatorPage.tsx | head -20
# 변환 후 라인 범위를 START~END 로 sed 적용
```

**(2) 변환 영역 grep gate 7종**:
```bash
START=1696   # EvDetailModal 시작 라인 (실제 변환 후 라인으로 보정)
END=$(grep -nE "^// ── 에스컬 노선도 버튼|^function EsBtn" cha-bio-safety/src/pages/ElevatorPage.tsx | head -1 | cut -d: -f1)
# END 직전 한 줄(}) 까지가 EvDetailModal 끝

# 1. 이모지 0건
sed -n "${START},${END}p" cha-bio-safety/src/pages/ElevatorPage.tsx | grep -P "🛗|📦|🔲|↕️|🔴|⚠️|✅|🔧|📋|🔍|✕"
# 기대: 출력 없음

# 2. 9-11px 폰트 0건
sed -n "${START},${END}p" cha-bio-safety/src/pages/ElevatorPage.tsx | grep -E "fontSize:\s*(9|10|11)\b|text-\[(9|10|11)px\]"
# 기대: 출력 없음

# 3. 옛 토큰 0건
sed -n "${START},${END}p" cha-bio-safety/src/pages/ElevatorPage.tsx | grep -E "var\(--(bg|bg2|bg3|bg4|bd|bd2|t1|t2|t3|danger|warn|info|safe|acl)\)"
# 기대: 출력 없음

# 4. 인라인 화이트리스트 외 정적 인라인 0건 (style={{ 만, 화이트리스트 키는 제외)
sed -n "${START},${END}p" cha-bio-safety/src/pages/ElevatorPage.tsx | grep -vE "(position:'fixed'|inset:0|zIndex:|top:|left:|right:|bottom:|transform:|maxHeight:|boxShadow:|WebkitOverflowScrolling|overscrollBehavior)" | grep -E "style=\{\{"
# 기대: 출력 없음 (화이트리스트 외 인라인 0건)

# 5. lucide 아이콘 신규 4종 import 확인
grep -E "^import \{[^}]*AlertOctagon[^}]*\} from 'lucide-react'" cha-bio-safety/src/pages/ElevatorPage.tsx | head -1
grep -E "^import \{[^}]*ClipboardCheck[^}]*\} from 'lucide-react'" cha-bio-safety/src/pages/ElevatorPage.tsx | head -1
grep -E "^import \{[^}]*FileSearch[^}]*\} from 'lucide-react'" cha-bio-safety/src/pages/ElevatorPage.tsx | head -1
grep -E "^import \{[^}]*CheckCircle2[^}]*\} from 'lucide-react'" cha-bio-safety/src/pages/ElevatorPage.tsx | head -1
# 기대: 각 1건

# 6. ev-btn-selected 패턴 일관성 (기간/점검필터/이력탭 3 영역)
sed -n "${START},${END}p" cha-bio-safety/src/pages/ElevatorPage.tsx | grep -cE "bg-accent text-text-on-accent"
# 기대: 3 (기간 / 점검필터 / 이력탭)
sed -n "${START},${END}p" cha-bio-safety/src/pages/ElevatorPage.tsx | grep -cE "bg-surface-sunken text-text-tertiary"
# 기대: 3 이상 (3 비선택 + 그 외 surface-sunken 사용처 — 카드 등)

# 7. KIND_STYLE className 정의 검증
grep -nE "textCls:'text-fire'|textCls:'text-safe'|textCls:'text-info'|textCls:'text-text-tertiary'" cha-bio-safety/src/pages/ElevatorPage.tsx
# 기대: 4건 (fault/repair/inspect/annual)
grep -nE "Icon: AlertOctagon|Icon: Wrench|Icon: ClipboardCheck|Icon: FileSearch" cha-bio-safety/src/pages/ElevatorPage.tsx
# 기대: 4건 (KIND_STYLE 매핑)
```

**Step 3.4: 비즈니스 로직 보존 sentinel**

```bash
# useState 3종
grep -cE "(setPeriodIdx|setCheckFilter|setHistTab)" cha-bio-safety/src/pages/ElevatorPage.tsx
# 기대: ≥6 (선언 3 + 사용 3)

# useEffect body scroll lock 보존
grep -nE "document\.body\.style\.overflow = 'hidden'" cha-bio-safety/src/pages/ElevatorPage.tsx
# 기대: 1건 (EvDetailModal 내부)

# useQuery ev_history 보존
grep -nE "queryKey: \['ev_history'" cha-bio-safety/src/pages/ElevatorPage.tsx
# 기대: 1건

# floorStats 가공 그대로
grep -nE "floorStats\.map\(s =>" cha-bio-safety/src/pages/ElevatorPage.tsx
# 기대: 1건

# filtered.map 그대로
grep -nE "filtered\.map\(h =>" cha-bio-safety/src/pages/ElevatorPage.tsx
# 기대: 1건

# ElevatorInfoCard 호출 1건 그대로
grep -E "<ElevatorInfoCard ev=\{ev\} compact=\{!isDesktop\} />" cha-bio-safety/src/pages/ElevatorPage.tsx
# 기대: 1건
```

**Step 3.5: 보존 영역 sentinel (다른 모달/컴포넌트 한 글자도 수정 X)**

```bash
# Wave 2/3/4 함수 정의 그대로
grep -cE "^function (EvSelector|EsBtn|EsNodeMap|FaultNewModal|FaultNewFullscreen|FaultResolveModal|RepairNewModal|InspectModal|ModalWrap|Field|MultiPhotoUpload|ElevatorInfoCard|KoelsaHistorySection|RepairListSection)\(" cha-bio-safety/src/pages/ElevatorPage.tsx
# 기대: 14 (각 1건)

# ev_detail 호출 사이트 2건
grep -cE "modal === 'ev_detail'" cha-bio-safety/src/pages/ElevatorPage.tsx
# 기대: 2

# git status — 단일 파일만 변경
git status --porcelain | wc -l
# 기대: 1 (cha-bio-safety/src/pages/ElevatorPage.tsx 만)

# icons.tsx / tailwind.config.js / 다른 파일 미수정
git diff --name-only | grep -E "(icons\.tsx|tailwind\.config\.js|tokens\.css)"
# 기대: 출력 없음
```

**Step 3.6: npm build PASS 확인**

```bash
cd cha-bio-safety && npm run build 2>&1 | tail -20
```

기대:
- TypeScript 0 에러
- `✓ built in <N>s`
- 새 ElevatorPage chunk 해시 (Wave 4: `nNW3YsCg` 또는 이후 변경분에서 변동)

**Step 3.7: commit 단일**

```bash
git add cha-bio-safety/src/pages/ElevatorPage.tsx
git commit -m "$(cat <<'EOF'
feat(260515-k5p): Wave 5 — EvDetailModal v0.1.1 토큰 + Tailwind 변환

- 헤더: TYPE_ICON_COMPONENT 매퍼 + lucide X (이모지 ✕ 제거)
- 기간/점검필터/이력탭: ev-btn-selected 패턴 (bg-accent/text-text-on-accent)
- 층별 누적 이력: lucide AlertOctagon/AlertTriangle/CheckCircle2 (이모지 🔴⚠️✅ 제거)
- KIND_STYLE 색 결정 (2D 시안 권위): fault=text-fire / repair=text-safe /
  inspect=text-info / annual=text-text-tertiary 회색
- KIND_STYLE icon: fault=AlertOctagon / repair=Wrench / inspect=ClipboardCheck /
  annual=FileSearch (이모지 🔧📋🔍 제거)
- 이력 빈 상태: FileSearch + "해당 이력이 없어요"
- 좌측 3px 색바: borderLeft 동적 색 → absolute span + barCls className enum
- 9-11px 폰트 0건, 옛 토큰 0건, 화이트리스트 외 인라인 0건
- 비즈니스 로직 100% 보존 (state 3종/useEffect/useQuery/floor 가공/filtered)
- ElevatorInfoCard 호출 + 다른 모달/컴포넌트 한 글자도 수정 X

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

**보존 룰 (Task 3 — 이력 리스트 + 회귀):**
- HISTORY_TABS 상수 그대로 (라인 1687~1694)
- KIND_STYLE 객체는 외형만 변경 (color → textCls/barCls, icon → Icon component) — kind key 4종 동일
- 발생층 칩: 기존 info 색 토큰 사용 (text-info + bg-info-bg)
- 점검항목 칩: 기존 회색 (text-text-tertiary + bg-surface-raised)
- 날짜 monospace: font-mono className (또는 fontFamily 인라인 — 화이트리스트 외이므로 className 우선)
- filtered/sort/kindOk/checkOk 로직 미수정
- ev_detail 모달 call site (2건) 보존
- icons.tsx / tailwind.config.js 미수정
  </action>
  <verify>
    <automated>
# (1) Task 3 핵심: 변환 영역 grep gate 7종 + 비즈니스 로직 sentinel + 보존 영역 sentinel + npm build
# 변환 후 라인 범위 동적 측정
START=$(grep -nE "^function EvDetailModal\(" cha-bio-safety/src/pages/ElevatorPage.tsx | head -1 | cut -d: -f1)
END=$(awk -v s="$START" 'NR>s && /^function [A-Z]/ {print NR-1; exit}' cha-bio-safety/src/pages/ElevatorPage.tsx)
echo "EvDetailModal range: $START~$END"

# 이모지 0건
sed -n "${START},${END}p" cha-bio-safety/src/pages/ElevatorPage.tsx | grep -cP "🛗|📦|🔲|↕️|🔴|⚠️|✅|🔧|📋|🔍|✕"
# 기대: 0

# 9-11px 폰트 0건
sed -n "${START},${END}p" cha-bio-safety/src/pages/ElevatorPage.tsx | grep -cE "fontSize:\s*(9|10|11)\b|text-\[(9|10|11)px\]"
# 기대: 0

# 옛 토큰 0건
sed -n "${START},${END}p" cha-bio-safety/src/pages/ElevatorPage.tsx | grep -cE "var\(--(bg|bg2|bg3|bg4|bd|bd2|t1|t2|t3|danger|warn|info|safe|acl)\)"
# 기대: 0

# lucide 4종 신규 import
grep -cE "AlertOctagon|ClipboardCheck|FileSearch|CheckCircle2" cha-bio-safety/src/pages/ElevatorPage.tsx
# 기대: ≥8 (import 4 + 사용 다수)

# ev-btn-selected 패턴 3 영역
sed -n "${START},${END}p" cha-bio-safety/src/pages/ElevatorPage.tsx | grep -cE "bg-accent text-text-on-accent"
# 기대: 3 (기간/점검필터/이력탭)

# KIND_STYLE 4종 매핑
grep -cE "Icon: AlertOctagon|Icon: Wrench|Icon: ClipboardCheck|Icon: FileSearch" cha-bio-safety/src/pages/ElevatorPage.tsx
# 기대: 4

# 비즈니스 로직 sentinel
grep -cE "(setPeriodIdx|setCheckFilter|setHistTab)" cha-bio-safety/src/pages/ElevatorPage.tsx   # ≥6
grep -cE "document\.body\.style\.overflow = 'hidden'" cha-bio-safety/src/pages/ElevatorPage.tsx # 1
grep -cE "queryKey: \['ev_history'" cha-bio-safety/src/pages/ElevatorPage.tsx                   # 1
grep -cE "<ElevatorInfoCard ev=\{ev\} compact=\{!isDesktop\}" cha-bio-safety/src/pages/ElevatorPage.tsx  # 1
grep -cE "filtered\.map\(h =>" cha-bio-safety/src/pages/ElevatorPage.tsx                        # 1

# 보존 영역 sentinel
grep -cE "^function (EvSelector|EsBtn|EsNodeMap|FaultNewModal|FaultNewFullscreen|FaultResolveModal|RepairNewModal|InspectModal|ModalWrap|Field|MultiPhotoUpload|ElevatorInfoCard|KoelsaHistorySection|RepairListSection)\(" cha-bio-safety/src/pages/ElevatorPage.tsx
# 기대: 14
grep -cE "modal === 'ev_detail'" cha-bio-safety/src/pages/ElevatorPage.tsx  # 기대: 2

# git status — 단일 파일만
git status --porcelain | wc -l   # 기대: 1
git diff --name-only | grep -cE "(icons\.tsx|tailwind\.config\.js|tokens\.css)"  # 기대: 0

# npm build
cd cha-bio-safety && npm run build 2>&1 | tail -5 | grep -E "built in|error"
# 기대: "✓ built in <N>s" 1건, "error" 0건
    </automated>
  </verify>
  <done>
- KIND_STYLE 색 결정 적용: fault=text-fire/bg-fire-bar / repair=text-safe/bg-safe-bar / inspect=text-info/bg-info-bar / annual=text-text-tertiary/bg-text-tertiary
- KIND_STYLE lucide 매핑: AlertOctagon/Wrench/ClipboardCheck/FileSearch
- 이력 탭 5개 ev-btn-selected 패턴
- 좌측 3px 색바 absolute span + className enum (정적 — PurgeCSS 안전)
- 빈 상태 FileSearch + "해당 이력이 없어요"
- 발생층 칩 info 토큰 / 점검항목 칩 회색 토큰 / 날짜 monospace
- 변환 영역 이모지/9-11px/옛 토큰 0건, 인라인 화이트리스트 외 0건
- 비즈니스 로직 sentinel 5종 모두 PASS (state/effect/useQuery/floorStats/filtered)
- 보존 영역 sentinel 14 함수 정의 + ev_detail call site 2건 + 단일 파일 git status
- npm run build PASS (TypeScript 0 에러)
- 단일 commit 생성
  </done>
</task>

</tasks>

<verification>

## Wave 5 종합 검증 — EvDetailModal 변환

### A. 변환 영역 grep gate (variable START/END)
```bash
START=$(grep -nE "^function EvDetailModal\(" cha-bio-safety/src/pages/ElevatorPage.tsx | head -1 | cut -d: -f1)
END=$(awk -v s="$START" 'NR>s && /^function [A-Z]/ {print NR-1; exit}' cha-bio-safety/src/pages/ElevatorPage.tsx)
# 1. 이모지 0건
sed -n "${START},${END}p" ElevatorPage.tsx | grep -P "🛗|📦|🔲|↕️|🔴|⚠️|✅|🔧|📋|🔍|✕"
# 2. 9-11px 0건
sed -n "${START},${END}p" ElevatorPage.tsx | grep -E "fontSize:\s*(9|10|11)\b|text-\[(9|10|11)px\]"
# 3. 옛 토큰 0건
sed -n "${START},${END}p" ElevatorPage.tsx | grep -E "var\(--(bg|bg2|bg3|bg4|bd|bd2|t1|t2|t3|danger|warn|info|safe|acl)\)"
# 4. 인라인 화이트리스트 외 0건
sed -n "${START},${END}p" ElevatorPage.tsx | grep -vE "(position:'fixed'|inset:0|zIndex:|top:|left:|right:|bottom:|transform:|maxHeight:|boxShadow:|WebkitOverflowScrolling|overscrollBehavior)" | grep -E "style=\{\{"
```

### B. lucide 4종 신규 import 확인
```bash
grep -E "^import \{[^}]*\b(AlertOctagon|ClipboardCheck|FileSearch|CheckCircle2)\b[^}]*\} from 'lucide-react'" cha-bio-safety/src/pages/ElevatorPage.tsx
```

### C. KIND_STYLE 색 결정 검증
```bash
grep -E "textCls:'text-(fire|safe|info|text-tertiary)'" cha-bio-safety/src/pages/ElevatorPage.tsx
grep -E "Icon: (AlertOctagon|Wrench|ClipboardCheck|FileSearch)" cha-bio-safety/src/pages/ElevatorPage.tsx
```

### D. ev-btn-selected 패턴 일관성 (3 영역)
```bash
sed -n "${START},${END}p" cha-bio-safety/src/pages/ElevatorPage.tsx | grep -c "bg-accent text-text-on-accent"
# 기대: 3 (기간 / 점검필터 / 이력탭)
```

### E. 비즈니스 로직 sentinel
```bash
grep -cE "(setPeriodIdx|setCheckFilter|setHistTab)" cha-bio-safety/src/pages/ElevatorPage.tsx     # ≥6
grep -E "document\.body\.style\.overflow = 'hidden'" cha-bio-safety/src/pages/ElevatorPage.tsx    # 1
grep -E "queryKey: \['ev_history'" cha-bio-safety/src/pages/ElevatorPage.tsx                       # 1
grep -E "<ElevatorInfoCard ev=\{ev\} compact=\{!isDesktop\}" cha-bio-safety/src/pages/ElevatorPage.tsx  # 1
```

### F. 보존 영역 sentinel
```bash
grep -cE "^function (EvSelector|EsBtn|EsNodeMap|FaultNewModal|FaultNewFullscreen|FaultResolveModal|RepairNewModal|InspectModal|ModalWrap|Field|MultiPhotoUpload|ElevatorInfoCard|KoelsaHistorySection|RepairListSection)\(" cha-bio-safety/src/pages/ElevatorPage.tsx
# 기대: 14
grep -cE "modal === 'ev_detail'" cha-bio-safety/src/pages/ElevatorPage.tsx
# 기대: 2
git diff --name-only | grep -cE "(icons\.tsx|tailwind\.config\.js)"   # 기대: 0
git status --porcelain | wc -l   # 기대: 1
```

### G. npm build
```bash
cd cha-bio-safety && npm run build
# 기대: ✓ built in <N>s, TypeScript 0 에러
```

</verification>

<success_criteria>

- [ ] EvDetailModal 변환 영역 이모지 0건 (🛗📦🔲↕️🔴⚠️✅🔧📋🔍✕)
- [ ] EvDetailModal 변환 영역 9-11px 폰트 0건
- [ ] EvDetailModal 변환 영역 옛 토큰 var(--bg/bg2/bg3/bg4/bd/bd2/t1/t2/t3/danger/warn/info/safe/acl) 0건
- [ ] EvDetailModal 변환 영역 인라인 style 화이트리스트 외 0건
- [ ] lucide 4종 신규 import (AlertOctagon/ClipboardCheck/FileSearch/CheckCircle2) 라인 16
- [ ] KIND_STYLE 4종 색 결정 적용 (fault=text-fire / repair=text-safe / inspect=text-info / annual=text-text-tertiary)
- [ ] KIND_STYLE 4종 lucide 매핑 (AlertOctagon / Wrench / ClipboardCheck / FileSearch)
- [ ] 층별 누적 이력 3 아이콘 매핑 (legend + row 우측: AlertOctagon/AlertTriangle/CheckCircle2)
- [ ] 이력 빈 상태 FileSearch + 텍스트
- [ ] 층별 빈 상태 CheckCircle2 (이모지 ✅ 제거)
- [ ] ev-btn-selected 패턴 3 영역 (기간/점검필터/이력탭) bg-accent + text-text-on-accent
- [ ] TYPE_ICON_COMPONENT 매퍼 헤더 적용
- [ ] 좌측 3px 색바 = absolute span + barCls className enum (정적, PurgeCSS 안전)
- [ ] ElevatorInfoCard 호출 라인 한 글자도 수정 X
- [ ] 비즈니스 로직 100% 보존 (state 3종 / useEffect / useQuery / floorStats / filtered)
- [ ] 보존 영역 14 함수 정의 그대로 + ev_detail call site 2건 그대로
- [ ] git diff: ElevatorPage.tsx 단일 파일만 변경
- [ ] npm run build PASS (TypeScript 0 에러)
- [ ] 단일 commit 생성

</success_criteria>

<output>
완료 후: `.planning/quick/260515-k5p-redesign-07-elevator-tsx-wave-5-evdetail/260515-k5p-SUMMARY.md` 작성.

SUMMARY 필수 섹션:
- 변환 전/후 라인 범위 (EvDetailModal 시작~종료)
- 2D 시안 권위 매핑 표 (6 영역 × 변환 전/후 className)
- KIND_STYLE 색 결정 4종 표 (kind / textCls / barCls / Icon)
- 이모지 → lucide 매핑 표
- 인라인 화이트리스트 enumeration (position/inset/zIndex/top/left/right/bottom/transform/maxHeight/boxShadow/WebkitOverflowScrolling/overscrollBehavior)
- grep gate 결과 표 (이모지/9-11px/옛 토큰/인라인/lucide import/ev-btn-selected/KIND_STYLE)
- 비즈니스 로직 보존 sentinel 표
- 보존 영역 sentinel 표 (14 함수 정의 + ev_detail call site)
- npm run build 결과
- Self-Check PASSED 체크박스
</output>
