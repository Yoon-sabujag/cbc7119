---
phase: quick-260503-2t5
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cha-bio-safety/migrations/0082_extinguisher_type_powder_3-3kg.sql
  - cha-bio-safety/src/pages/ExtinguishersListPage.tsx
  - cha-bio-safety/functions/api/remediation/[recordId].ts
  - cha-bio-safety/src/types/index.ts
  - cha-bio-safety/src/pages/InspectionPage.tsx
  - cha-bio-safety/src/pages/RemediationDetailPage.tsx
autonomous: false
requirements:
  - QUICK-260503-2t5
must_haves:
  truths:
    - "분말 소화기 표기가 모든 화면에서 '분말 3.3kg'로 통일된다"
    - "소화기 점검에서 normal 외 결과 선택 시 증상 피커(받침 파손/연한 만료/직접 입력)가 노출된다"
    - "조치 화면에서 record.memo 증상에 따라 actionPick이 자동 선택된다 ('받침 파손'→'받침 교체', '연한 만료'→'소화기 교체')"
    - "actionPick 변경 시 소모 자재가 자동 채워진다 ('받침 교체'→'소화기 받침/1', '소화기 교체'→'{ext_type} 소화기/1')"
    - "프로덕션 D1 의 extinguishers.type='분말' 행 416개가 '분말 3.3kg'로 갱신된다"
  artifacts:
    - path: "cha-bio-safety/migrations/0082_extinguisher_type_powder_3-3kg.sql"
      provides: "extinguishers.type '분말' → '분말 3.3kg' UPDATE 마이그레이션"
      contains: "UPDATE extinguishers"
    - path: "cha-bio-safety/functions/api/remediation/[recordId].ts"
      provides: "조치 상세 응답에 extinguisherType 필드 포함"
      contains: "extinguisherType"
    - path: "cha-bio-safety/src/pages/InspectionPage.tsx"
      provides: "소화기 증상 피커 (받침 파손/연한 만료/직접 입력) + 저장 분기"
      contains: "받침 파손"
    - path: "cha-bio-safety/src/pages/RemediationDetailPage.tsx"
      provides: "소화기 조치 피커 (본체 교체/예비전원 교체/받침 교체/소화기 교체/직접 입력) + 자동 선택/자재 채움"
      contains: "받침 교체"
  key_links:
    - from: "InspectionPage.tsx (소화기 증상 피커)"
      to: "check_records.memo"
      via: "handleSave finalMemo (symptomPick 또는 직접 입력)"
      pattern: "isExtinguisher.*result.*!==.*normal"
    - from: "RemediationDetailPage.tsx (조치 피커)"
      to: "record.memo (저장된 증상)"
      via: "useEffect actionPick 자동 선택"
      pattern: "받침 파손|연한 만료"
    - from: "remediation/[recordId].ts (LEFT JOIN extinguishers)"
      to: "RemediationDetailPage extinguisherType"
      via: "ext.type AS ext_type 별칭 → 응답 매핑"
      pattern: "LEFT JOIN extinguishers"
---

<objective>
사용자 요청 1) 분말 → 분말 3.3kg 표기 통일 (DB + UI 상수). 2) 소화기 점검·조치 워크플로 자동화를 유도등 패턴(`isGuideLight` / `symptomPick` / `actionPick`) 그대로 따라 구현하여 UX 일관성 확보.

Purpose: 5월 법정점검 현장 사용 중 발견된 반복 입력 부담 제거. 점검 시 증상을 피커로 빠르게 선택하면 조치 화면에서 자동 매핑되어 자재 입력까지 감소.

Output:
- migrations/0082_extinguisher_type_powder_3-3kg.sql (D1 production 적용)
- 소화기 점검(InspectionPage) / 조치(RemediationDetailPage) 자동화
- API 응답 확장 + 타입 추가
- production 배포 후 사용자 검증
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md

<interfaces>
<!-- 유도등 패턴 — 정확히 그대로 따라가기 -->

InspectionPage.tsx:2789  (onSave 시그니처 확장 — 변경 X)
```ts
onSave: (cpId: string, result: CheckResult, memo: string, photoKey?: string,
        extra?: { guide_light_type?: string; floor_plan_marker_id?: string }) => Promise<void>
```

InspectionPage.tsx:2813  (유도등 state — 소화기에 동일 의미로 재사용 가능, 단 `isGuideLight` 로직과 충돌 방지 위해 `isExtinguisher` 분기 추가)
```ts
const [symptomPick,   setSymptomPick]   = useState<string>('점등 이상')
const [symptomCustom, setSymptomCustom] = useState('')
```

InspectionPage.tsx:3196-3208  (저장 시 증상 분기)
```ts
if (isGuideLight && selectedCP.id.startsWith('MARKER:')) {
  ...
  if (result !== 'normal' && glTypeFromMarker !== 'audience_passage') {
    finalMemo = symptomPick === '직접 입력' ? memo.trim() : symptomPick
  } else {
    finalMemo = memo.trim()
  }
}
```

InspectionPage.tsx:3430-3445  (증상 피커 UI)
```tsx
{isGuideLight && result !== 'normal' && (selectedCP as any).locationNo !== 'audience_passage' && (
  <div style={{ marginTop:10 }}>
    <div style={{ fontSize:10, fontWeight:600, color:'var(--t3)', ... }}>증상</div>
    <div style={{ display:'flex', gap:5 }}>
      {['점등 이상','예비전원 이상','직접 입력'].map(s => (
        <button key={s} onClick={() => setSymptomPick(s)} ...>{s}</button>
      ))}
    </div>
  </div>
)}
```

RemediationDetailPage.tsx:36  (actionPick 타입 — 소화기 옵션으로 확장)
```ts
const [actionPick, setActionPick] = useState<'본체 교체' | '예비전원 교체' | '직접 입력'>('본체 교체')
```

RemediationDetailPage.tsx:43-50  (GL_TYPE_LABEL — 그대로 유지)

RemediationDetailPage.tsx:58  (isGuideLight 분기)
```ts
const isGuideLight = record?.category === '유도등'
```

RemediationDetailPage.tsx:61-67  (증상→조치 자동선택 useEffect — 소화기에 동일 패턴 적용)
```ts
useEffect(() => {
  if (!isGuideLight || !record) return
  const sym = record.memo ?? ''
  if (sym === '점등 이상') setActionPick('본체 교체')
  else if (sym === '예비전원 이상') setActionPick('예비전원 교체')
  else setActionPick('직접 입력')
}, [isGuideLight, record?.id])
```

RemediationDetailPage.tsx:69-81  (조치→자재 자동채움 useEffect)
```ts
useEffect(() => {
  if (!isGuideLight) return
  if (actionPick === '본체 교체') {
    setMaterialName(GL_TYPE_LABEL[record?.guideLightType ?? ''] ?? '')
    setMaterialCount('1')
  } else if (actionPick === '예비전원 교체') {
    setMaterialName('예비전원 4.8V'); setMaterialCount('1')
  } else { setMaterialName(''); setMaterialCount('') }
}, [actionPick, isGuideLight, record?.guideLightType])
```

RemediationDetailPage.tsx:110-119  (handleResolve memo 분기)
```ts
let finalMemo = ''
if (isGuideLight) {
  finalMemo = actionPick === '직접 입력' ? memo.trim() : actionPick
} else { finalMemo = memo.trim() }
```

RemediationDetailPage.tsx:303-315  (조치 피커 UI)
```tsx
{isGuideLight && (
  <div style={{ display:'flex', gap:5, marginBottom:10 }}>
    {(['본체 교체', '예비전원 교체', '직접 입력'] as const).map(opt => (
      <button key={opt} onClick={() => setActionPick(opt)} ...>{opt}</button>
    ))}
  </div>
)}
```

RemediationDetailPage.tsx:318  (textarea 표시 조건)
```tsx
{(!isGuideLight || actionPick === '직접 입력') && (
  <textarea ... />
)}
```

remediation/[recordId].ts (현재 SELECT — extinguisher 정보 없음. 확장 필요)
```ts
SELECT r.id, r.result, r.memo, r.photo_key,
       COALESCE(r.status, 'open') as status,
       r.resolution_memo, ..., r.guide_light_type, r.floor_plan_marker_id, ...,
       cp.category, cp.location, cp.floor, cp.zone,
       s.name AS staff_name, ...,
       fpm.label AS marker_label
FROM check_records r
JOIN check_points cp ON cp.id = r.checkpoint_id
LEFT JOIN staff s ON s.id = r.staff_id
LEFT JOIN staff rs ON rs.id = r.resolved_by
LEFT JOIN floor_plan_markers fpm ON fpm.id = r.floor_plan_marker_id
WHERE r.id = ?
```

types/index.ts:55-76  (RemediationRecord — `extinguisherType?: string | null` 추가 위치)

ExtinguishersListPage.tsx:22  (상수)
```ts
const EXTINGUISHER_TYPES = ['분말', '분말 20kg', '이산화탄소', '할로겐', '강화액', 'K급']
```

migrations/0079_extinguisher_asset_split.sql  (`check_records.extinguisher_id INTEGER` 컬럼 + idx_check_records_ext 인덱스 — JOIN 기반)
migrations/0081_zone_common_to_basement.sql  (직전 마이그레이션 번호: 0081 → 신규는 0082)
</interfaces>

<patterns>
- migration 파일명 규칙: `NNNN_<snake_case_description>.sql` (NNNN = zero-padded)
- migration 헤더 주석에 의도/검증 SQL 포함 (0079, 0081 참고)
- 한국어 toast: react-hot-toast
- inline style + CSS variable (`var(--bd)`, `var(--acl)`, `rgba(59,130,246,.12)`) — 신규 색 추가 금지
- 배포: `npm run deploy` (wrangler pages deploy + `--branch production` + ASCII `--commit-message`)
- D1 마이그레이션: `npx wrangler d1 execute cha-bio-db --remote --file=migrations/<file>.sql` (--remote 필수)
</patterns>
</context>

<tasks>

<task type="auto">
  <name>Task 1: D1 마이그레이션 0082 작성 + ExtinguishersListPage 상수 갱신</name>
  <files>cha-bio-safety/migrations/0082_extinguisher_type_powder_3-3kg.sql, cha-bio-safety/src/pages/ExtinguishersListPage.tsx</files>
  <action>
1) `cha-bio-safety/migrations/0082_extinguisher_type_powder_3-3kg.sql` 신규 파일 생성. 0079/0081 헤더 스타일 따라가기:

```sql
-- 0082: extinguishers.type '분말' → '분말 3.3kg' 표기 통일
--
-- 배경: 기존 '분말'은 모두 ABC 분말 3.3kg 소화기. 추후 5kg 등 변형 도입 가능성 고려해
--       명시적 용량 표기로 정규화. 약 416행 영향 (2026-05-02 기준).
--
-- 검증 SQL (적용 후):
--   SELECT type, COUNT(*) FROM extinguishers GROUP BY type ORDER BY type;
--   → '분말' 0 / '분말 3.3kg' 416

UPDATE extinguishers
SET type = '분말 3.3kg'
WHERE type = '분말';
```

2) `cha-bio-safety/src/pages/ExtinguishersListPage.tsx:22` 의 EXTINGUISHER_TYPES 배열에서 `'분말'` 을 `'분말 3.3kg'` 으로 교체:

Before: `const EXTINGUISHER_TYPES = ['분말', '분말 20kg', '이산화탄소', '할로겐', '강화액', 'K급']`
After:  `const EXTINGUISHER_TYPES = ['분말 3.3kg', '분말 20kg', '이산화탄소', '할로겐', '강화액', 'K급']`

3) 다른 곳에 `'분말'` 하드코딩이 있는지 grep 으로 확인:
   `grep -rn "'분말'" cha-bio-safety/src cha-bio-safety/functions | grep -v node_modules`
   결과가 추가로 있으면 동일하게 `'분말 3.3kg'` 로 치환 (UI 상수/필터/토스트 메시지에 한정 — 사용자 입력 자유 텍스트는 건드리지 말 것).

D1 적용은 Task 5 (배포) 에서 수행.
  </action>
  <verify>
    <automated>cd /Users/jykevin/Documents/20260328/cha-bio-safety && grep -c "'분말 3.3kg'" src/pages/ExtinguishersListPage.tsx | grep -q "^1$" && grep -q "UPDATE extinguishers" migrations/0082_extinguisher_type_powder_3-3kg.sql && echo OK</automated>
  </verify>
  <done>migrations/0082_*.sql 파일 존재 + UPDATE 문 포함. ExtinguishersListPage.tsx 의 EXTINGUISHER_TYPES 첫 원소가 '분말 3.3kg'. 추가 하드코딩 잔재 없음.</done>
</task>

<task type="auto">
  <name>Task 2: API + 타입 확장 (extinguisherType 노출)</name>
  <files>cha-bio-safety/functions/api/remediation/[recordId].ts, cha-bio-safety/src/types/index.ts</files>
  <action>
1) `cha-bio-safety/functions/api/remediation/[recordId].ts` 의 SELECT 문에 `LEFT JOIN extinguishers ext ON ext.id = r.extinguisher_id` 추가하고 `ext.type AS ext_type` 컬럼 SELECT (예약어 충돌 회피 위해 별칭 필수). 응답 `data` 객체에 `extinguisherType: record.ext_type` 추가.

수정 후 SELECT (참고용):
```ts
SELECT r.id, r.result, r.memo, r.photo_key,
       COALESCE(r.status, 'open') as status,
       r.resolution_memo, r.resolution_photo_key, r.materials_used,
       r.guide_light_type, r.floor_plan_marker_id, r.location_detail,
       r.resolved_at, r.resolved_by, r.checked_at, r.staff_id,
       cp.category, cp.location, cp.floor, cp.zone,
       s.name AS staff_name,
       rs.name AS resolved_by_name,
       fpm.label AS marker_label,
       ext.type AS ext_type
FROM check_records r
JOIN check_points cp ON cp.id = r.checkpoint_id
LEFT JOIN staff s ON s.id = r.staff_id
LEFT JOIN staff rs ON rs.id = r.resolved_by
LEFT JOIN floor_plan_markers fpm ON fpm.id = r.floor_plan_marker_id
LEFT JOIN extinguishers ext ON ext.id = r.extinguisher_id
WHERE r.id = ?
```

응답 매핑에:
```ts
extinguisherType:     record.ext_type,
```
추가 (markerLabel 인근에 자연스럽게 배치).

2) `cha-bio-safety/src/types/index.ts` 의 `RemediationRecord` 인터페이스 (line 55-76) 에 다음 필드 추가:
```ts
extinguisherType: string | null
```
markerLabel 옆/아래에 자연스럽게 추가. 기존 필드 순서는 건드리지 말 것.
  </action>
  <verify>
    <automated>cd /Users/jykevin/Documents/20260328/cha-bio-safety && grep -q "LEFT JOIN extinguishers" functions/api/remediation/\[recordId\].ts && grep -q "ext.type AS ext_type" functions/api/remediation/\[recordId\].ts && grep -q "extinguisherType" src/types/index.ts && npx tsc --noEmit -p . 2>&1 | tail -5</automated>
  </verify>
  <done>API 응답에 extinguisherType 포함. RemediationRecord 타입에 필드 존재. tsc --noEmit 통과 (또는 기존 에러 외 신규 에러 0).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: InspectionPage — 소화기 증상 피커 + 저장 분기</name>
  <files>cha-bio-safety/src/pages/InspectionPage.tsx</files>
  <behavior>
- record.category === '소화기' 이고 result !== 'normal' 일 때만 증상 피커 노출
- 옵션: ['받침 파손', '연한 만료', '직접 입력']
- '직접 입력' 선택 시 textarea 가 증상상세 placeholder로 변경됨 (유도등 line 3451 패턴)
- handleSave 에서 isExtinguisher && result !== 'normal' 분기로 finalMemo 채움
  </behavior>
  <action>
유도등 패턴(InspectionPage.tsx 인용)을 그대로 따라가되 별도 state 로 분리하여 카테고리 충돌 방지.

1) state 추가 (line 2813 부근, symptomPick 선언 바로 아래):
```ts
const isExtinguisher = group.categories.includes('소화기')
const [extSymptomPick, setExtSymptomPick] = useState<string>('받침 파손')
```
(`isExtinguisher` 가 이미 다른 위치에 선언돼 있으면 재선언 금지 — grep 으로 먼저 확인하고 기존 변수 재사용)

2) 증상 피커 UI 추가 (line 3445 직후, 유도등 피커 블록 다음 줄):
```tsx
{isExtinguisher && result !== 'normal' && (
  <div style={{ marginTop:10 }}>
    <div style={{ fontSize:10, fontWeight:600, color:'var(--t3)', marginBottom:6, letterSpacing:'0.05em' }}>증상</div>
    <div style={{ display:'flex', gap:5 }}>
      {['받침 파손','연한 만료','직접 입력'].map(s => (
        <button key={s} onClick={() => setExtSymptomPick(s)} style={{
          flex:1, padding:'8px 4px', borderRadius:10, cursor:'pointer',
          border: extSymptomPick===s ? '2px solid var(--acl)' : '1px solid var(--bd)',
          background: extSymptomPick===s ? 'rgba(59,130,246,.12)' : 'var(--bg2)',
          fontSize:11, fontWeight:700, color: extSymptomPick===s ? 'var(--acl)' : 'var(--t2)',
        }}>{s}</button>
      ))}
    </div>
  </div>
)}
```

3) 특이사항 label (line 3451) 의 조건에 소화기 직접입력 케이스 추가:
Before: `{isGuideLight && result !== 'normal' && (selectedCP as any).locationNo !== 'audience_passage' && symptomPick === '직접 입력' ? '증상 상세 및 특이사항 (선택)' : '특이사항 (선택)'}`
After:  유도등 케이스 OR `(isExtinguisher && result !== 'normal' && extSymptomPick === '직접 입력')` 에서도 '증상 상세 및 특이사항 (선택)' 라벨로.

4) handleSave (line 3187 부근) 에서 유도등 분기 다음에 소화기 분기 추가:
```ts
} else if (isExtinguisher && result !== 'normal') {
  finalMemo = extSymptomPick === '직접 입력' ? memo.trim() : extSymptomPick
}
```
(유도등 if 블록 종료 후, await onSave 직전에 삽입. else if 체이닝 위치는 컴포넌트 구조 보존하도록 신중히.)

5) result 가 normal 로 돌아올 때 extSymptomPick 리셋은 필요 없음 — 피커가 unmount 되므로 다음 표시 시 기본값 그대로 유지하면 OK.

주의: `pairedBC` 분기(line 3211)는 비상콘센트 전용이므로 소화기 그룹과는 무관 — 건드리지 말 것.
  </action>
  <verify>
    <automated>cd /Users/jykevin/Documents/20260328/cha-bio-safety && grep -c "extSymptomPick" src/pages/InspectionPage.tsx | awk '$1 >= 4 {print "OK"; exit 0} {exit 1}' && grep -q "받침 파손" src/pages/InspectionPage.tsx && grep -q "연한 만료" src/pages/InspectionPage.tsx && npx tsc --noEmit -p . 2>&1 | grep -E "InspectionPage|error TS" | head -5</automated>
  </verify>
  <done>소화기 그룹 점검 모달에서 result≠normal 일 때 증상 피커 노출. 저장 시 extSymptomPick 값(또는 직접입력 memo)이 finalMemo 로 저장됨. tsc 신규 에러 0.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: RemediationDetailPage — 소화기 조치 자동화</name>
  <files>cha-bio-safety/src/pages/RemediationDetailPage.tsx</files>
  <behavior>
- record.category === '소화기' 일 때 actionPick 옵션이 ['받침 교체', '소화기 교체', '직접 입력']
- record.memo 가 '받침 파손' → actionPick 자동 '받침 교체'
- record.memo 가 '연한 만료' → actionPick 자동 '소화기 교체'
- 그 외 → '직접 입력'
- '받침 교체' → ('소화기 받침', '1')
- '소화기 교체' → (`${record.extinguisherType ?? ''} 소화기`.trim(), '1')
- handleResolve memo: 직접입력이면 memo.trim(), 아니면 actionPick
  </behavior>
  <action>
1) actionPick 타입 확장 (line 36):
```ts
const [actionPick, setActionPick] = useState<
  '본체 교체' | '예비전원 교체' | '받침 교체' | '소화기 교체' | '직접 입력'
>('본체 교체')
```

2) isExtinguisher 분기 추가 (line 58 직후, isGuideLight 옆):
```ts
const isExtinguisher = record?.category === '소화기'
```

3) 증상→조치 자동선택 useEffect 추가 (line 67 직후, 유도등 useEffect 와 동일 구조):
```ts
useEffect(() => {
  if (!isExtinguisher || !record) return
  const sym = record.memo ?? ''
  if (sym === '받침 파손') setActionPick('받침 교체')
  else if (sym === '연한 만료') setActionPick('소화기 교체')
  else setActionPick('직접 입력')
}, [isExtinguisher, record?.id])
```

4) 자재 자동채움 useEffect 확장 — 기존 line 69-81 useEffect 안에서 isExtinguisher 분기 추가. 단, 유도등 케이스(`if (!isGuideLight) return`) 와 충돌 방지 위해 별도 useEffect 로 분리:

```ts
useEffect(() => {
  if (!isExtinguisher) return
  if (actionPick === '받침 교체') {
    setMaterialName('소화기 받침'); setMaterialCount('1')
  } else if (actionPick === '소화기 교체') {
    const t = (record?.extinguisherType ?? '').trim()
    setMaterialName(t ? `${t} 소화기` : '소화기')
    setMaterialCount('1')
  } else {
    setMaterialName(''); setMaterialCount('')
  }
}, [actionPick, isExtinguisher, record?.extinguisherType])
```

5) handleResolve memo 분기 (line 110-119) 에 소화기 케이스 추가:
```ts
if (isGuideLight) {
  finalMemo = actionPick === '직접 입력' ? memo.trim() : actionPick
} else if (isExtinguisher) {
  finalMemo = actionPick === '직접 입력' ? memo.trim() : actionPick
} else {
  finalMemo = memo.trim()
}
```

6) 조치 피커 UI (line 304-315) 분기 확장 — 유도등 블록은 그대로 유지하고 그 아래에 소화기 블록 추가:
```tsx
{isExtinguisher && (
  <div style={{ display: 'flex', gap: 5, marginBottom: 10 }}>
    {(['받침 교체', '소화기 교체', '직접 입력'] as const).map(opt => (
      <button key={opt} onClick={() => setActionPick(opt)} style={{
        flex: 1, padding: '10px 4px', borderRadius: 10, cursor: 'pointer',
        border: actionPick === opt ? '2px solid var(--acl)' : '1px solid var(--bd)',
        background: actionPick === opt ? 'rgba(59,130,246,.12)' : 'var(--bg2)',
        fontSize: 12, fontWeight: 700, color: actionPick === opt ? 'var(--acl)' : 'var(--t2)',
      }}>{opt}</button>
    ))}
  </div>
)}
```

7) textarea 표시 조건 (line 318) 갱신:
Before: `{(!isGuideLight || actionPick === '직접 입력') && (`
After:  `{((!isGuideLight && !isExtinguisher) || actionPick === '직접 입력') && (`

(유도등 OR 소화기 카테고리에서 직접 입력일 때만 textarea, 그 외 카테고리는 항상 textarea — 기존 동작 보존.)

주의: GL_TYPE_LABEL (line 43-50) 은 유도등 전용 — 건드리지 말 것.
  </action>
  <verify>
    <automated>cd /Users/jykevin/Documents/20260328/cha-bio-safety && grep -q "isExtinguisher" src/pages/RemediationDetailPage.tsx && grep -q "받침 교체" src/pages/RemediationDetailPage.tsx && grep -q "소화기 교체" src/pages/RemediationDetailPage.tsx && grep -q "extinguisherType" src/pages/RemediationDetailPage.tsx && npx tsc --noEmit -p . 2>&1 | grep -E "RemediationDetailPage|error TS" | head -5</automated>
  </verify>
  <done>소화기 조치 화면에서 피커 노출, 증상 따라 자동 선택, 자재 자동 채움. 직접입력 외에는 textarea 비표시. tsc 신규 에러 0.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: 빌드 + Production 배포 + D1 마이그레이션 + 사용자 검증</name>
  <what-built>
    - migrations/0082 (분말 → 분말 3.3kg)
    - InspectionPage 소화기 증상 피커
    - RemediationDetailPage 소화기 조치 자동화
    - API extinguisherType 노출
  </what-built>
  <how-to-verify>
1) 빌드 확인:
```bash
cd /Users/jykevin/Documents/20260328/cha-bio-safety
npm run build
```
빌드 성공 확인 (에러 0).

2) D1 production 마이그레이션 적용 (--remote 필수):
```bash
cd /Users/jykevin/Documents/20260328/cha-bio-safety
npx wrangler d1 execute cha-bio-db --remote --file=migrations/0082_extinguisher_type_powder_3-3kg.sql
```
출력에서 `Executed 1 query` 와 영향받은 행 수(약 416) 확인.

검증 쿼리:
```bash
npx wrangler d1 execute cha-bio-db --remote --command="SELECT type, COUNT(*) AS n FROM extinguishers GROUP BY type ORDER BY type;"
```
→ '분말' 0행, '분말 3.3kg' 약 416행 표시되어야 함.

3) Pages production 배포 (--branch production + ASCII commit msg 필수):
```bash
cd /Users/jykevin/Documents/20260328/cha-bio-safety
npm run deploy -- --branch production --commit-message "extinguisher: powder 3.3kg unify + auto remediation pattern"
```
또는 wrangler 직접:
```bash
npx wrangler pages deploy dist --project-name cha-bio-safety --branch production --commit-message "extinguisher: powder 3.3kg unify + auto remediation pattern"
```

4) 모바일 PWA 에서 앱 재실행 (PWA 캐시 무효화는 SW 자동 처리). 다음 플로우를 사용자가 직접 검증:

  a) 소화기 목록 화면 → 필터/등록 폼의 종류 옵션이 '분말 3.3kg'로 표기되는지
  b) 소화기 점검 모달 → 결과 '주의' 또는 '불량' 선택 시 '받침 파손/연한 만료/직접 입력' 피커가 나타나는지
  c) '받침 파손' 선택 후 저장 → 미조치 목록에서 해당 기록의 memo 가 '받침 파손'으로 표시되는지
  d) 조치 화면 진입 → actionPick 이 자동으로 '받침 교체'로 선택되고, 소모 자재가 '소화기 받침 1ea'로 자동 채워지는지
  e) '연한 만료' → actionPick 이 '소화기 교체', 자재가 '분말 3.3kg 소화기 1ea' (또는 ext.type 에 따라)로 채워지는지
  f) '직접 입력' 선택 시 textarea 가 다시 노출되는지
  g) 유도등 점검·조치 워크플로가 회귀 없이 그대로 동작하는지 (regression check)

5) 이상 있으면 어떤 항목이 안되는지 알려주세요. 모두 OK 면 "approved" 회신.
  </how-to-verify>
  <resume-signal>"approved" 또는 발견된 이슈 설명</resume-signal>
</task>

</tasks>

<verification>
- migrations/0082 파일 존재, UPDATE 문 포함
- production D1: extinguishers WHERE type='분말' 카운트 0
- ExtinguishersListPage EXTINGUISHER_TYPES 첫 원소 '분말 3.3kg'
- API GET /api/remediation/{recordId} 응답에 extinguisherType 키 존재
- RemediationRecord 타입에 extinguisherType: string | null
- InspectionPage 소화기 그룹 + result≠normal 시 증상 피커 노출 (받침 파손/연한 만료/직접 입력)
- RemediationDetailPage 소화기 조치 시 5옵션 피커 + 증상→조치 자동 매핑 + 자재 자동 채움
- 유도등 워크플로 회귀 없음
- 사용자 production 검증 통과
</verification>

<success_criteria>
- 사용자가 모바일 PWA 에서 소화기 점검 → 증상 피커 → 조치 화면에서 자동 채움까지 한번에 시연 가능
- 분말 표기 통일 (DB + UI 모두 '분말 3.3kg')
- 유도등 패턴(증상→조치→자재 자동) 과 동일한 UX 제공
- production 배포 완료 + D1 적용 완료
</success_criteria>

<output>
After completion, create `.planning/quick/260503-2t5-3-3kg/260503-2t5-SUMMARY.md`
</output>
