---
phase: 260527-6o0
plan: 01
wave: 1
type: execute
completed: 2026-05-27
commit: 8441ca5
files_created:
  - cha-bio-safety/docs/redesign-context/31-chrome/sketch-wave-5-menu-settings-section.html
files_modified: []
tasks_completed: 1
tasks_total: 1
deviations: 0
---

# Phase 260527-6o0 Plan 01: redesign/31-chrome W5 — MenuSettingsSection sketch Summary

**One-liner:** Authored a single 2193-line atomic sketch HTML for MenuSettingsSection.tsx (418-line source) with 6 frame matrix (4 dark + 2 light), 8 noan upgrades (9·10 → 12), 4 sub-component dimensions visualized, 25+ biz anchors + Phase 21 `migrateLegacyMenuConfig` forward-merge anchor verbatim — sketch track 종결.

## File path produced

- `cha-bio-safety/docs/redesign-context/31-chrome/sketch-wave-5-menu-settings-section.html` (new, 2193 lines)

## Line count

| Metric         | Value             |
| -------------- | ----------------- |
| Total lines    | 2193              |
| Allowed range  | [1500, 2200]      |
| Margin to cap  | 7 lines headroom  |

## Noan upgrade applications (OQ #2 LOCKED, 8건)

| Source line | Original | Context                              | New size            |
| ----------- | -------- | ------------------------------------ | ------------------- |
| L173        | 9        | "메뉴 설정" SectionHeader uppercase  | 12 (text-caption)   |
| L214        | 9        | divider title uppercase              | 12                   |
| L263        | 9        | "숨김" 라벨 (item hidden)            | 12                   |
| L294        | 10       | "기본 배치로 되돌릴까요?" prompt       | 12                   |
| L297        | 10       | 취소 button (reset)                  | 12                   |
| L301        | 10       | 초기화 button (reset danger)          | 12                   |
| L307        | 10       | "기본값으로 초기화" 링크 button         | 12                   |
| L407        | 10       | "삭제할까요?" prompt                  | 12                   |

All 8 upgrades unified to 12 (text-caption) so every uppercase label inside MenuSettingsSection
aligns to the same noan-compliant size, and inline confirm rows match the column rhythm.

## OQ LOCKED matrix — application notes

| OQ           | Decision                                                                  | W5 applied | Notes                                                              |
| ------------ | ------------------------------------------------------------------------- | ---------- | ------------------------------------------------------------------ |
| OQ #1        | 다크+라이트 양쪽 지원                                                      | YES        | Frame 5 + Frame 6 carry `data-theme="light"` (≥ 2 light frames)    |
| OQ #2        | 부분 격상 8건                                                              | YES        | Above noan table — all 9·10 → 12, no fontSize 11 hits in source    |
| OQ #3        | SideMenu 그라데이션 유지                                                   | N/A        | MenuSettingsSection has zero gradients                              |
| OQ #4        | 프로필 그라데이션 폐기                                                     | N/A        | Same — zero gradients                                                |
| OQ #5        | Lucide 인라인 svg 유지                                                     | YES        | ChevronUp / ChevronDown / ChevronRight / Trash2 canonical paths      |
| OQ #6        | 이모지 제거                                                                | N/A        | Zero emoji; `+` in "+ 구분선 추가" is ASCII 0x2B literal             |
| OQ #7        | panel width 88% maxWidth 320 보존                                           | N/A        | Renders inside SettingsPanel column                                  |
| W3-OQ #A     | ✕ 글리프 → Lucide X                                                       | N/A        | No close glyph; Trash2 already Lucide                                |
| **W5-OQ #B** | **Toggle accent = `var(--accent-active)`**                                | **YES**    | Raw `#2563eb` retired; knob raw `#fff` → `var(--text-on-accent)`     |

## Verification gate results (all PASS)

### Pre-commit body grep gates (extracted to `/tmp/w5-body.html`)

| Gate                                                                          | Count       | Required   | Status |
| ----------------------------------------------------------------------------- | ----------- | ---------- | ------ |
| `font-size:\s*(9\|10\|11)px` in body                                          | 0           | 0          | PASS   |
| `linear-gradient` in body                                                     | 0           | 0          | PASS   |
| `✕` (U+2715) literal in body                                                  | 0           | 0          | PASS   |
| legacy alias `--t1/--t2/--t3/--bg3/--bg4/--bd2/--danger/--acl` in body        | 0           | 0          | PASS   |
| raw hex `#2563eb` / `#fff[non-hex]` in body                                   | 0           | 0          | PASS   |
| emoji `[\u{1F300}-\u{1FAFF}]` / `[\u{2600}-\u{27BF}]` in body                  | 0           | 0          | PASS   |

### Positive coverage gates (against full file)

| Gate                                                                                                                                | Result      | Required   | Status |
| ------------------------------------------------------------------------------------------------------------------------------------ | ----------- | ---------- | ------ |
| `data-theme="light"` count                                                                                                          | 13          | ≥ 2        | PASS   |
| `class="frame"` count                                                                                                                | 6           | ≥ 6        | PASS   |
| `MenuSettingsSection` references                                                                                                    | 35          | ≥ 1        | PASS   |
| `migrateLegacyMenuConfig` references                                                                                                | 8           | ≥ 1        | PASS   |
| `Phase 21` references                                                                                                                | 12          | ≥ 1        | PASS   |
| `DEFAULT_SIDE_MENU` references                                                                                                      | 14          | ≥ 1        | PASS   |
| `SideMenuEntry` references                                                                                                          | 6           | ≥ 1        | PASS   |
| `circular` references                                                                                                                | 4           | ≥ 1        | PASS   |
| `menuSettings.collapsed` references                                                                                                 | 2           | ≥ 1        | PASS   |

### 17 verbatim Korean strings + 2 toast messages

All present (each grep returned ≥ 1):

- '메뉴 설정' · '구분선 삭제' · '위로 이동' · '아래로 이동' · '+ 구분선 추가' ·
  '기본 배치로 되돌릴까요?' · '취소' · '초기화' · '기본값으로 초기화' · '저장 중…' ·
  '설정 저장' · '삭제할까요?' · '삭제' · '숨김' · '새 구분선' · '구분선 제목' · `${label} 표시`
- toast.success: '메뉴 설정이 저장되었습니다'
- toast.error: '저장에 실패했습니다. 다시 시도해주세요.'

### 5 semantic tokens in body

| Token                       | Count |
| --------------------------- | ----- |
| `var(--surface-sunken)`     | 9     |
| `var(--accent-active)`      | 9     |
| `var(--status-danger)`      | 13    |
| `var(--text-on-accent)`     | 12    |
| `var(--border-strong)`      | 5     |

### Protected-file invariance

```
git status --porcelain → only the sketch file appears as ??
PASS — no other repo file touched
```

## Deviations from Plan

None — plan executed exactly as written.

Note on tooling artifact: the plan's `<automated>` verify chain uses
`echo "$BODY" | grep -c "pattern" | grep -qx 0`, which exits non-zero on macOS BSD grep
when there are zero matches (grep -c with zero matches outputs blank, not "0", and exits 1).
This is a known shell-pipe artifact present in W4's verify chain as well — the file-based
verify in the plan's `<verification>` block (writing to `/tmp/w5-body.html`) returns the
intended "0" count for every negative gate, confirming compliance. All gate intents satisfied.

## OQ LOCKED matrix application notes (W5 sub-OQ #B Toggle accent)

W5 sub-OQ #B LOCKED — Toggle ON state accent color decision:
- Source uses raw `#2563eb` (line 364 inline style) for Toggle ON background.
- W5 sketch maps to `var(--accent-active)` semantic token (dark `#2563eb`, light `#0a52c4`).
- Source uses raw `#fff` (line 370 inline style) for knob background.
- W5 sketch maps to `var(--text-on-accent)` semantic token.
- W4 mirror — same decision applied to SettingsPanel.Toggle (W4 sketch).
- Frame 6 showcase verifies Toggle 4-state (ON/OFF × active/disabled) renders correctly under
  `data-theme="light"`, confirming the cross-theme variable swap is lossless.

## Phase 21 forward-merge anchor

`migrateLegacyMenuConfig` (`cha-bio-safety/src/utils/api.ts` line 449) is anchored in the head
comment as biz-anchor #48 with the slug `[Phase 21 forward-merge]`. The W7 TSX conversion must
preserve the `DEFAULT_SIDE_MENU` import (`MenuSettingsSection.tsx` line 9) and never replace it
with a hand-rolled constant — that would break server-side legacy config auto-merge of new
menu items (e.g. `/documents`).

## Hand-off note to W6/W7

**redesign/31-chrome sketch track is now complete.** W2 (GlobalHeader) → W3 (SideMenu) →
W4 (SettingsPanel) → W5 (MenuSettingsSection) all sketched, totaling 4 sketch files.

Next steps for downstream waves:

- **W6 — TSX 변환 verify checklist (markdown).** Combine W2~W5 biz anchors + Tailwind cheatsheet
  + circular-import (MenuSettingsSection ↔ SideMenu.MENU) warning + JSZip / Notification API
  / pushManager preservation rules + Phase 21 `migrateLegacyMenuConfig` preservation rule.
- **W7 — TSX 변환 atomic single wave.** Convert all 4 components (GlobalHeader 45 +
  SideMenu 201 + SettingsPanel 894 + MenuSettingsSection 418 = 1558 lines) in one atomic commit.
  Enforce **1-byte-change-0** against the 47 anchors + Phase 21 anchor #48 from this W5 sketch
  (and the analogous anchor sets from W2/W3/W4).

After W7 main merge, cbc7119-preview auto-deploys via GitHub Actions. Per
`CLAUDE.local.md` + memory `feedback_cbc7119_design_never_wrangler`, no wrangler invocation
is permitted from this worktree — only main push triggers preview deployment.

## Self-Check: PASSED

- File exists: `cha-bio-safety/docs/redesign-context/31-chrome/sketch-wave-5-menu-settings-section.html` (2193 lines) FOUND
- Commit exists: `8441ca5` FOUND in git log (`git log --oneline | grep 8441ca5`)
- All negative gates: 0 hits in body (verified via `/tmp/w5-body.html` file-based grep)
- All positive gates: ≥ minimum thresholds (verified via full-file grep)
- Protected-file invariance: only the sketch file appears in `git status --porcelain`
