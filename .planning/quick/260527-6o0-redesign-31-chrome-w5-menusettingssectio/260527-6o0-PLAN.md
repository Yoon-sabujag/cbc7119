---
phase: 260527-6o0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cha-bio-safety/docs/redesign-context/31-chrome/sketch-wave-5-menu-settings-section.html
autonomous: true
requirements:
  - redesign-31-chrome-w5
must_haves:
  truths:
    - "sketch HTML file exists at the exact path with 6 frames (4 dark + 2 light)"
    - "All 8 noan violations (9·10·11px) from MenuSettingsSection.tsx body are upgraded — 0 occurrences of font-size:9/10/11px in body markup"
    - "All 25+ business anchors (Korean copy + DOM structure + state semantics) are preserved verbatim as quoted strings in the head comment"
    - "OQ LOCKED matrix #1 (light theme), #2 (partial noan upgrade), #5 (Lucide retained), #6 (no emoji), and W5 sub-OQ (Toggle accent = --accent-active) are documented and applied"
    - "legacy alias tokens (--t1/--t2/--t3/--bg3/--bg4/--bd2/--danger/--acl) and raw hex (#2563eb, #fff) are 0 inside <body>; only allowed inside :root / [data-theme] CSS definitions"
    - "All 4 subcomponent shapes (ArrowButton 32×32, ToggleSmall 38×21 with knob 17×17, DividerTitleInput maxLength 20, DeleteConfirmInline) are visualized with exact dimensions"
    - "Phase 21 forward-merge rule (migrateLegacyMenuConfig) and DEFAULT_SIDE_MENU dependency are anchored in head comment"
  artifacts:
    - path: "cha-bio-safety/docs/redesign-context/31-chrome/sketch-wave-5-menu-settings-section.html"
      provides: "W5 chrome sub-wave sketch — MenuSettingsSection 6-frame visual reference"
      min_lines: 1500
      contains: "sketch-wave-5-menu-settings-section"
  key_links:
    - from: "sketch-wave-5-menu-settings-section.html"
      to: "MenuSettingsSection.tsx (snapshot)"
      via: "head comment biz anchor table — 25+ verbatim identifiers"
      pattern: "MenuSettingsSection|ArrowButton|ToggleSmall|DividerTitleInput|DeleteConfirmInline"
    - from: "sketch-wave-5-menu-settings-section.html"
      to: "tokens.css v0.1.0 semantic layer"
      via: ":root and [data-theme=\"light\"] override blocks"
      pattern: "--surface-sunken|--surface-active|--text-primary|--text-secondary|--text-tertiary|--accent-active|--status-danger|--text-on-accent"
    - from: "sketch-wave-5-menu-settings-section.html"
      to: "sketch-wave-4-settings-panel.html (W4 mirror)"
      via: "light/dark frame pattern + noan upgrade matrix"
      pattern: "data-theme=\"light\"|frame"
---

<objective>
Produce a single sketch HTML file documenting the redesign of `MenuSettingsSection.tsx` (418 lines) — the menu settings section rendered inside the SettingsPanel. This is the **fifth and final sub-wave (W5) of redesign/31-chrome sketch series** and the **last sketch artifact before TSX conversion** for the chrome track.

Purpose:
- Lock the visual + token decisions for the MenuSettingsSection before TSX conversion (W7).
- Honor all OQ LOCKED decisions from W1 index (mirrored from W4 precedent) and the W5 sub-OQ on the Toggle accent color.
- Upgrade the 8 noan violations (9/10/11px) per OQ #2 partial-upgrade decision.
- Replace legacy alias tokens and raw hex with v0.1.0 semantic tokens.
- Anchor 25+ business identifiers verbatim so the W7 TSX conversion can be a 1-byte-change-0 atomic operation against the snapshot at `docs/redesign-context/31-chrome/MenuSettingsSection.tsx`.
- Document the Phase 21 forward-merge rule (`migrateLegacyMenuConfig` in `src/utils/api.ts`) so the converter cannot drop the DEFAULT_SIDE_MENU dependency.

Output: ONE sketch HTML file at the exact path below. atomic 1-commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@CLAUDE.local.md
@cha-bio-safety/docs/redesign-context/31-chrome/wave-1-index.md
@cha-bio-safety/docs/redesign-context/31-chrome/MenuSettingsSection.tsx
@cha-bio-safety/docs/redesign-context/31-chrome/SideMenu.tsx
@cha-bio-safety/docs/redesign-context/31-chrome/SettingsPanel.tsx
@cha-bio-safety/docs/redesign-context/31-chrome/design-system.md
@cha-bio-safety/docs/redesign-context/31-chrome/tokens.css
@cha-bio-safety/docs/redesign-context/31-chrome/typography.css
@cha-bio-safety/docs/redesign-context/31-chrome/sketch-wave-2-global-header.html
@cha-bio-safety/docs/redesign-context/31-chrome/sketch-wave-3-side-menu.html
@cha-bio-safety/docs/redesign-context/31-chrome/sketch-wave-4-settings-panel.html

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from snapshots and src/utils/api.ts. -->

From cha-bio-safety/src/utils/api.ts (lines 408–471):
```typescript
export type SideMenuEntry =
  | { type: 'item'; path: string; visible: boolean }
  | { type: 'divider'; id: string; title: string }

export interface MenuConfig {
  sideMenu: SideMenuEntry[]
}

export const DEFAULT_SIDE_MENU: SideMenuEntry[] = [ /* item + divider entries */ ]

// Phase 21 forward-merge: ensures new DEFAULT_SIDE_MENU items (e.g. /documents)
// are added even when server has older config. NEVER bypass on TSX conversion.
export function migrateLegacyMenuConfig(raw: MenuConfig | { sideMenu?: SideMenuEntry[] } | null | undefined): MenuConfig
```

From cha-bio-safety/docs/redesign-context/31-chrome/MenuSettingsSection.tsx (lines 1–48 module level):
```typescript
import { useState, useEffect, useMemo, useRef } from 'react'
import { useQuery, useQueryClient, useMutation } from '@tanstack/react-query'
import toast from 'react-hot-toast'
import { ChevronUp, ChevronDown, ChevronRight, Trash2 } from 'lucide-react'
import { settingsApi, type SideMenuEntry, type MenuConfig, DEFAULT_SIDE_MENU } from '../utils/api'
import { MENU } from './SideMenu'   // ← circular-safe: SideMenu does NOT import MenuSettingsSection
import { useAuthStore } from '../stores/authStore'

const PATH_LABEL: Record<string, string>      // MENU.forEach lookup
const DESKTOP_ONLY_PATHS: Set<string>          // filter — mobile sidebar only
const ADMIN_PATHS: Set<string>                 // filter — admin-only paths
function newDividerId(): string                // `d-${Date.now().toString(36)}-${Math.random().toString(36).slice(2, 7)}`
function entriesEqual(a: SideMenuEntry[], b: SideMenuEntry[]): boolean
```

From cha-bio-safety/docs/redesign-context/31-chrome/tokens.css (semantic layer — use these in body markup):
- `--surface-sunken` (was `--bg3`) — item/divider row background
- `--surface-active` (was `--bg4`) — cancel button bg / Toggle off bg
- `--text-primary` (was `--t1`) — item label
- `--text-secondary` (was `--t2`) — secondary text (cancel button, confirm prompt)
- `--text-tertiary` (was `--t3`) — section header / "숨김" label / "기본값으로 초기화"
- `--border-strong` (was `--bd2`) — divider borderLeft + "+ 구분선 추가" dashed border
- `--accent` (was `--acl`) — DividerTitleInput border + "설정 저장" button bg
- `--accent-active` (was raw `#2563eb`) — Toggle ON state (W5 sub-OQ LOCKED)
- `--status-danger` (was `--danger`) — Trash2 icon + 초기화 + 삭제 buttons
- `--text-on-accent` (was raw `#fff`) — Toggle knob + button text on accent/danger
- `--radius-sm` 8px — input, button, sub-card
- `--radius-md` 12px — standard card
- Toggle border-radius 11 (half of 21 height) — raw value retained as raw 11 OR `border-radius: 99px` (`--radius-pill`); pick `--radius-pill` for consistency

From cha-bio-safety/docs/redesign-context/31-chrome/typography.css (noan-compliant scale):
- `.text-caption` — 12px / 1.5 / 400 — smallest allowed body text
- `.text-label` — 13px / 1.5 / 400
- `.text-body-sm` — 14px / 1.6 / 400
- `.text-body` — 16px (절대 마지노선)
- 9·10·11px are FORBIDDEN in body markup
</interfaces>

<oq-locked-matrix>
<!-- W1 index OQ decisions, applied to W5. -->
1. **OQ #1 LOCKED — 다크+라이트 양쪽**: Include ≥2 frames with `[data-theme="light"]`.
2. **OQ #2 LOCKED — 부분 격상**: Upgrade the 8 noan violations in MenuSettingsSection.tsx:
   - L173 fontSize:9 (SectionHeader "메뉴 설정" uppercase) → **11→`text-caption` 12px** (W3 divider mirror)
   - L214 fontSize:9 (divider title uppercase) → **11→12 (`text-caption`)**
   - L263 fontSize:9 ("숨김" label) → **12 (`text-caption`)**
   - L294 fontSize:10 ("기본 배치로 되돌릴까요?") → **12**
   - L297 fontSize:10 (취소 button) → **12** (or 13/`text-label` for button text — note both as options in the head comment; pick **12** as default for inline confirm row)
   - L301 fontSize:10 (초기화 button) → **12**
   - L307 fontSize:10 ("기본값으로 초기화" link button) → **12**
   - L407 fontSize:10 ("삭제할까요?") → **12**
3. **OQ #3 / #4** — gradient-related; W5 has no gradient → N/A, document as N/A in head comment.
4. **OQ #5 LOCKED — Lucide 유지**: W5 already uses Lucide (ChevronUp, ChevronDown, ChevronRight, Trash2). No inline svg involved.
5. **OQ #6 LOCKED — 이모지 제거**: W5 has no emoji. Note: `"+ 구분선 추가"` literal `+` is a text glyph, not an emoji — retain verbatim.
6. **OQ #7** — panel width; W5 renders inside SettingsPanel column → N/A.
7. **W5 sub-OQ LOCKED — Toggle ON accent = `--accent-active`** (mirror W4 LOCKED accent token). Knob `#fff` → `--text-on-accent`.
</oq-locked-matrix>

<biz-anchors>
<!-- 25+ verbatim identifiers / Korean copy that the W7 TSX conversion MUST preserve byte-for-byte. -->

Imports + module-level:
1. `import { ChevronUp, ChevronDown, ChevronRight, Trash2 } from 'lucide-react'`
2. `import { settingsApi, type SideMenuEntry, type MenuConfig, DEFAULT_SIDE_MENU } from '../utils/api'`
3. `import { MENU } from './SideMenu'` — circular-safe direction
4. `import { useAuthStore } from '../stores/authStore'`
5. `PATH_LABEL: Record<string, string>` (MENU.forEach lookup)
6. `DESKTOP_ONLY_PATHS: Set<string>` (mobile sidebar filter)
7. `ADMIN_PATHS: Set<string>` (admin-only filter)
8. `newDividerId()` — `d-${Date.now().toString(36)}-${Math.random().toString(36).slice(2, 7)}`
9. `entriesEqual(a, b)` — array deep compare

Main hook + state:
10. `useQueryClient` + `useAuthStore(s => s.staff)` + `useQuery({queryKey:['menu-config'], queryFn:()=>settingsApi.getMenu(), staleTime: 300_000})`
11. `isAdmin = staff?.role === 'admin'`
12. `draft: SideMenuEntry[]` state (initialized from serverConfig.sideMenu OR DEFAULT_SIDE_MENU)
13. `editingDividerIdx: number | null`
14. `confirmDeleteIdx: number | null`
15. `confirmReset: boolean`
16. `confirmTimerRef: useRef<number | null>` (5s auto-dismiss timer)
17. `collapsed: boolean` — localStorage key `'menuSettings.collapsed'` (default `true`; value `'false'` means expanded)
18. `dirty` useMemo (`entriesEqual` vs serverConfig)
19. `saveMutation` useMutation:
    - onSuccess: `toast.success('메뉴 설정이 저장되었습니다')` + `qc.invalidateQueries({queryKey:['menu-config']})`
    - onError: `toast.error('저장에 실패했습니다. 다시 시도해주세요.')`

Mutators:
20. `moveUp(idx)` / `moveDown(idx)` — adjacent swap (no-op at boundary)
21. `toggleVisible(idx)` — item.visible toggle (skip non-item)
22. `renameDivider(idx, title)` — `.trim().slice(0, 20)`; empty → silent revert (do not commit)
23. `deleteDivider(idx)` — filter + clear timer
24. `addDivider()` — push `{ type:'divider', id:newDividerId(), title:'새 구분선' }` then `setTimeout(() => setEditingDividerIdx(next.length - 1), 0)`
25. `resetToDefaults()` — `DEFAULT_SIDE_MENU.map(e => ({...e}))` + `setConfirmReset(false)`

Effects:
26. 5s auto-dismiss useEffect on `confirmDeleteIdx` (`window.setTimeout(..., 5000)`)

Render branches:
27. Collapsed/expanded header — ChevronRight rotate 0deg/90deg (size 14, color `--text-tertiary`)
28. `draft.map()` admin filter — `!isAdmin && entry.type==='item' && ADMIN_PATHS.has(entry.path)` → null
29. divider — isEditing branch → `DividerTitleInput`; isConfirmingDelete branch → `DeleteConfirmInline`; default → up/down arrows + Trash2 button
30. item — skip if `!(entry.path in PATH_LABEL)` OR `DESKTOP_ONLY_PATHS.has(entry.path)`
31. item visible/hidden — opacity 1 vs 0.4 + "숨김" label (only when hidden)

Bottom buttons:
32. `"+ 구분선 추가"` — dashed border (1px dashed `--border-strong`), height 36, marginTop 10, radius 8, fontSize 12
33. confirmReset branch (inline): "기본 배치로 되돌릴까요?" + 취소(취소 button, surface-active) + 초기화(status-danger button)
34. Default "기본값으로 초기화" — link-style button, fontSize 12, color `--text-tertiary`
35. "설정 저장" — height 40, marginTop 8, bg `--accent`, color `--text-on-accent`, fontSize 12, fontWeight 700, radius 9, opacity 0.4 when disabled; label `'저장 중…'` (single-char ellipsis 0x2026) while pending, else `'설정 저장'`

Subcomponents (exact dimensions):
36. `ArrowButton({ dir, disabled, onClick })` — 32×32, color `--text-secondary`, Icon size 14, opacity 0.25 when disabled, pointerEvents none, aria-label `'위로 이동'` / `'아래로 이동'`
37. `ToggleSmall({ on, onChange, ariaLabel })` — 38×21 outer (radius 11 raw OR `--radius-pill`), knob 17×17 (radius 50%), knob top:2 left:2, translateX(17px) when on; bg `--accent-active` (on) / `--surface-active` (off); knob `--text-on-accent`; transition 0.18s
38. `DividerTitleInput({ initial, onCommit })` — height 40, full width, bg `--surface-sunken`, border 1px solid `--accent`, radius 8, padding 0 12px, fontSize 13 (text-label), color `--text-primary`, outline none, `maxLength={20}`, `aria-label="구분선 제목"`, focus+select on mount, Enter → blur (commit), Escape → revert+commit(initial)
39. `DeleteConfirmInline({ onCancel, onConfirm })` — flex gap 6; "삭제할까요?" fontSize 12 `--text-secondary`; 취소(`--surface-active` bg, `--text-secondary` text, fontSize 12, fontWeight 700, radius 6); 삭제(`--status-danger` bg, `--text-on-accent` text, fontSize 12, fontWeight 700, radius 6)

Verbatim Korean copy (DO NOT translate, DO NOT alter punctuation/ellipsis):
- `'메뉴 설정'`
- `'구분선 삭제'` (aria-label)
- `'위로 이동'` / `'아래로 이동'` (aria-label)
- `'+ 구분선 추가'` (button text — `+` is a literal character)
- `'기본 배치로 되돌릴까요?'`
- `'취소'`
- `'초기화'`
- `'기본값으로 초기화'`
- `'저장 중…'` (single Unicode ellipsis U+2026)
- `'설정 저장'`
- `'삭제할까요?'`
- `'삭제'`
- `'숨김'`
- `'새 구분선'` (default title for new divider)
- `'구분선 제목'` (aria-label on input)
- ``${label} 표시`` (template literal — aria-label on ToggleSmall)
- `'메뉴 설정이 저장되었습니다'` (toast.success)
- `'저장에 실패했습니다. 다시 시도해주세요.'` (toast.error)

Container outer:
40. Root `<div style={{ padding: '12px 13px 5px' }}>` — verbatim padding
41. Entry list — `display:flex, flexDirection:column, gap:5`
42. Divider/item row padding `'10px 12px'`, gap 8, radius 9, leftPad 16 placeholder div

Phase 21 forward-merge:
43. DEFAULT_SIDE_MENU dependency MUST be preserved — `migrateLegacyMenuConfig` (api.ts L449) appends missing items so e.g. `/documents` shows up even on legacy server config. The W7 TSX conversion cannot drop this import or replace it with a hand-rolled DEFAULT_SIDE_MENU constant. Anchor inline in head comment with the slug `[Phase 21 forward-merge]`.
</biz-anchors>

<noan-upgrade-table>
| Source line | Original size | Context | New size | Rationale |
|---|---|---|---|---|
| L173 | 9 | "메뉴 설정" SectionHeader uppercase | **12** (text-caption) | uppercase smallcap header; matches W3 SideMenu divider treatment after upgrade |
| L214 | 9 | divider title uppercase | **12** | same family as L173; uppercase preserved |
| L263 | 9 | "숨김" label (hidden item) | **12** | inline meta label — smallest noan-safe size |
| L294 | 10 | "기본 배치로 되돌릴까요?" prompt | **12** | inline confirm row |
| L297 | 10 | 취소 button | **12** | inline confirm action |
| L301 | 10 | 초기화 button | **12** | inline confirm action (danger) |
| L307 | 10 | "기본값으로 초기화" link button | **12** | tertiary link button — minimum noan |
| L407 | 10 | "삭제할까요?" prompt | **12** | inline confirm row |
</noan-upgrade-table>

<token-mapping>
| Source raw / alias | W5 sketch token |
|---|---|
| `var(--t1)` | `var(--text-primary)` |
| `var(--t2)` | `var(--text-secondary)` |
| `var(--t3)` | `var(--text-tertiary)` |
| `var(--bg3)` | `var(--surface-sunken)` |
| `var(--bg4)` | `var(--surface-active)` |
| `var(--bd2)` | `var(--border-strong)` |
| `var(--danger)` | `var(--status-danger)` |
| `var(--acl)` (border + save bg) | `var(--accent)` |
| raw `#2563eb` (Toggle on) | `var(--accent-active)` |
| raw `#fff` (knob + button text on accent/danger) | `var(--text-on-accent)` |
| Toggle outer radius 11 | retain raw 11 OR `var(--radius-pill)` (pick `--radius-pill`) |
| 6/8/9 radii | `--radius-sm` (8) / retain raw for in-between values |
</token-mapping>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author sketch-wave-5-menu-settings-section.html (single atomic artifact)</name>
  <files>cha-bio-safety/docs/redesign-context/31-chrome/sketch-wave-5-menu-settings-section.html</files>

  <action>
Create the single sketch HTML file at the exact path:
`cha-bio-safety/docs/redesign-context/31-chrome/sketch-wave-5-menu-settings-section.html`

The file must follow the precedent established in `sketch-wave-2-global-header.html`, `sketch-wave-3-side-menu.html`, and `sketch-wave-4-settings-panel.html` — same head-comment style, same :root token block, same multi-frame layout.

## A. Head comment (150–250 lines, fenced as `<!-- … -->`)

Include these sections, in order:

1. **Meta header** — file name, wave (W5), parent track (redesign/31-chrome), date 2026-05-27, source snapshot path (`cha-bio-safety/docs/redesign-context/31-chrome/MenuSettingsSection.tsx`, 418 lines).

2. **OQ LOCKED matrix** — quote all 7 OQ items + W5 sub-OQ from `<oq-locked-matrix>` above. Each item is one line: `OQ #N — LOCKED: <decision>`. Items #3/#4/#7 are explicitly marked `N/A (no gradient / inside SettingsPanel column)`.

3. **Noan upgrade table** — verbatim copy of `<noan-upgrade-table>` from this plan. 8 rows mapping source line → new size → rationale.

4. **Biz anchors block (25+ items)** — verbatim copy of `<biz-anchors>` (items 1–43). Use slug `[Phase 21 forward-merge]` next to anchor #43. State explicitly: "W7 TSX 변환 시 이 블록의 모든 anchor 는 1 byte 변경 0."

5. **Verbatim Korean copy list** — every Korean string (17 entries from biz-anchors). Use a one-per-line list.

6. **Token mapping table** — verbatim copy of `<token-mapping>`.

7. **Negative gate (sketch self-check)** — list the grep checks the verifier will run:
   - `font-size:\s*(9|10|11)px` in `<body>` → 0
   - `linear-gradient` → 0
   - emoji ranges `[\u{1F300}-\u{1FAFF}]|[\u{2600}-\u{27BF}]` → 0
   - text glyph `✕` → 0 (per W3 close-button precedent: use SVG/Lucide, not literal glyph)
   - `status-(safe|warning|danger|info|fire)(?!-)` (i.e. `text-status-fire` without `-bar`/`-bg` suffix) → 0
   - legacy alias names inside `<body>` — `--t1`, `--t2`, `--t3`, `--bg3`, `--bg4`, `--bd2`, `--danger`, `--acl` → 0 occurrences
   - raw hex inside `<body>` — `#2563eb`, `#fff(?!.*:root)`, any 6-digit `#[0-9a-f]{6}` → 0 outside `:root`/`[data-theme]` blocks
   - `MENU` import path or signature changes → 0 (note: this is verified at W7 TSX conversion; sketch only documents the dependency)

8. **Memory rule slugs** — inline 12 unique slugs (one-line headings), each prefixed `// [memory:]`:
   - `[memory:feedback_design_changes_ask_first]`
   - `[memory:feedback_redesign_sketch_rule_enforcement]`
   - `[memory:feedback_sketch_realistic_data]`
   - `[memory:feedback_design_sketch_first]`
   - `[memory:feedback_planner_prompt_sketch_verbatim]`
   - `[memory:feedback_tsx_wave_emoji_dot_gap]`
   - `[memory:feedback_tsx_wave_stat_card_drift]`
   - `[memory:feedback_tailwind_token_class_pattern]`
   - `[memory:feedback_text_caption_leading_none]` (relevant to inline small text inside narrow rows)
   - `[memory:feedback_cbc7119_design_never_wrangler]`
   - `[memory:project_cbc7119_design_repo]`
   - `[memory:feedback_check_branch_before_edit]`

## B. `<style>` block

Include:

1. `<meta charset="utf-8">`, `<title>W5 — MenuSettingsSection sketch</title>`.
2. Import the Pretendard CDN font URL from `typography.css` line 11 (verbatim).
3. `:root` and `[data-theme="dark"]` block with the semantic dark tokens from `tokens.css` (verbatim subset — at minimum the 10 tokens used by W5 from `<token-mapping>`).
4. `[data-theme="light"]` block with the light overrides (verbatim subset).
5. Body font: `font-family: 'Pretendard Variable', 'Pretendard', system-ui, sans-serif; font-size: 16px;`.
6. `.frame` container — match W4 sketch frame style: light shadow, radius 12, padding 24, max-width 320 (mobile column width), background `var(--surface-page)`, color `var(--text-primary)`. Stack frames vertically with gap 32. Add a small frame title (text-label, color text-tertiary) above each.
7. Inline class helpers `.text-caption`, `.text-label`, `.text-body-sm`, `.text-body`, `.text-title` matching `typography.css`.
8. Make sure no `--bg3`/`--bg4`/etc. aliases appear in body — but the `:root` block MAY keep the v0.1.0 alias bridge from tokens.css if needed for completeness (this is allowed because aliases live in `:root`, not `<body>`).

## C. `<body>` — 6 frames

All 6 frames render the MenuSettingsSection at the column width it occupies inside SettingsPanel (use 312px content width as the column — matches W4). Each frame is a self-contained `<div class="frame" data-theme="...">`. Set `data-theme` directly on the frame `div` so light/dark coexist on the same HTML page.

**Frame 1 — Dark / Expanded default (collapsed === false)**
- SectionHeader row: "메뉴 설정" (text-caption uppercase, color text-tertiary, letter-spacing .08em) + ChevronRight (size 14, color text-tertiary, rotate 90deg via inline `style="transform: rotate(90deg)"`).
- Show **8 entries** in a flex column gap 5 — alternate dividers and items to demonstrate both row types. Suggested seed (do NOT call mutators — pure HTML):
  1. divider: id `d-seed-a`, title `'주요 기능'`
  2. item: path `/dashboard`, label `'대시보드'`, visible:true
  3. item: path `/inspection`, label `'일반 점검'`, visible:true
  4. divider: id `d-seed-b`, title `'시설 관리'`
  5. item: path `/div`, label `'DIV 압력 관리'`, visible:true
  6. item: path `/floorplan`, label `'소방 시설 도면'`, visible:false (demonstrates opacity 0.4 + '숨김' label)
  7. divider: id `d-seed-c`, title `'문서 관리'`
  8. item: path `/daily-report`, label `'일일 업무 일지'`, visible:true
- Each divider row: padding 10px 12px, bg surface-sunken, borderLeft 2px solid border-strong, radius 9. Inside: 16px spacer + title span (text-caption uppercase, color text-secondary, letter-spacing .08em) + ArrowButton up + ArrowButton down + Trash2 button (32×32, color status-danger, Lucide-style svg inlined).
- Each item row: padding 10px 12px, bg surface-sunken, radius 9, opacity 1 (or 0.4 for hidden). Inside: 16px spacer + label span (text-caption, color text-primary) + (if hidden) '숨김' span (text-caption, color text-tertiary, leading-none) + ArrowButton up + ArrowButton down + ToggleSmall.
- First row's up arrow disabled, last row's down arrow disabled (opacity 0.25, pointer-events none).
- Bottom buttons (verbatim):
  - "+ 구분선 추가" — dashed 1px border-strong, marginTop 10, height 36, radius 8, fontSize 12, color text-secondary
  - "기본값으로 초기화" right-aligned link button (fontSize 12, color text-tertiary, no border)
  - "설정 저장" — height 40, marginTop 8, bg `--accent`, color `--text-on-accent`, fontSize 12, fontWeight 700, radius 9 (enabled state — full opacity)
- Inline Lucide SVGs: for each ChevronUp/ChevronDown/ChevronRight/Trash2, paste the actual Lucide path `<svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">…</svg>`. Use the icons' canonical paths:
  - ChevronUp: `<polyline points="18 15 12 9 6 15" />`
  - ChevronDown: `<polyline points="6 9 12 15 18 9" />`
  - ChevronRight: `<polyline points="9 18 15 12 9 6" />`
  - Trash2: standard 5-path Lucide trash2 set (lid + can body + 3 vertical lines). Use 5 elements: `<polyline points="3 6 5 6 21 6"/><path d="M19 6l-2 14a2 2 0 0 1-2 2H9a2 2 0 0 1-2-2L5 6"/><path d="M10 11v6"/><path d="M14 11v6"/><path d="M9 6V4a2 2 0 0 1 2-2h2a2 2 0 0 1 2 2v2"/>`

**Frame 2 — Dark / Divider editing mode**
- Render the same 8-entry list but with `editingDividerIdx = 3` (the "시설 관리" divider). Replace the title span with the DividerTitleInput markup:
  - `<input type="text" value="시설 관리" maxlength="20" aria-label="구분선 제목" style="height:40px;width:100%;box-sizing:border-box;background:var(--surface-sunken);border:1px solid var(--accent);border-radius:8px;padding:0 12px;font-size:13px;color:var(--text-primary);outline:none;">` — focus+select is described as behavior in a comment but obviously cannot be visually shown — instead show a thin caret marker after the text.
- Frame title says "다크 / 구분선 편집 (focus + select on mount)".

**Frame 3 — Dark / Delete confirm inline (5s timer)**
- Render the same 8-entry list with `confirmDeleteIdx = 0` (the first divider "주요 기능"). Replace the up/down/trash trio with `DeleteConfirmInline`:
  - "삭제할까요?" span (fontSize 12, color text-secondary) + 취소 button (bg surface-active, color text-secondary, fontSize 12, fw 700, radius 6, padding 4px 8px) + 삭제 button (bg status-danger, color text-on-accent, fontSize 12, fw 700, radius 6, padding 4px 8px).
- Add a 1-line tooltip/caption below the row (text-caption, color text-tertiary): "5초 후 자동 취소" — this is a sketch annotation only, not a code requirement.

**Frame 4 — Dark / Reset confirm inline**
- Same 8-entry list (no row-level mutation), but the bottom area shows the `confirmReset === true` branch:
  - "기본 배치로 되돌릴까요?" (fontSize 12, color text-secondary) + 취소 button (bg surface-active, color text-secondary, fontSize 12, fw 700, radius 6, padding 4px 8px) + 초기화 button (bg status-danger, color text-on-accent, fontSize 12, fw 700, radius 6, padding 4px 8px).
- "+ 구분선 추가" still visible above. "설정 저장" still visible below.

**Frame 5 — Light / Expanded default**
- Add `data-theme="light"` on the frame `div`. Render identically to Frame 1 — verifies that all tokens flip cleanly.
- Frame title: "라이트 / 전체 (data-theme=\"light\")".

**Frame 6 — Light / item visible OFF + ToggleSmall on/off comparison**
- Add `data-theme="light"` on the frame `div`.
- Render a reduced 4-entry list focusing on visible/hidden contrast:
  1. divider "주요 기능"
  2. item `/dashboard`, label `'대시보드'`, visible:true (Toggle ON — bg accent-active, knob translated)
  3. item `/inspection`, label `'일반 점검'`, visible:false (opacity 0.4 + '숨김' label; Toggle OFF — bg surface-active)
  4. item `/floorplan`, label `'소방 시설 도면'`, visible:true (Toggle ON)
- Below the list, add a "Toggle states" annotation row showing 4 ToggleSmall components side by side: ON-enabled, OFF-enabled, ON-disabled (opacity 0.25), OFF-disabled. Add a one-line caption per state (text-caption, text-tertiary).
- Also show two ArrowButton states side by side: enabled (color text-secondary, opacity 1) vs disabled (color text-secondary, opacity 0.25, pointer-events none).

## D. Inline structural rules (apply to ALL frames)

- Never use legacy alias names (`--t1`, `--t2`, `--t3`, `--bg3`, `--bg4`, `--bd2`, `--danger`, `--acl`) anywhere in the body. Only use v0.1.0 semantic tokens listed in `<token-mapping>`.
- Never use raw hex (`#2563eb`, `#fff`, etc.) anywhere in the body. Only inside `:root` / `[data-theme]` CSS blocks.
- Never use font-size 9px / 10px / 11px in body markup.
- Toggle outer pill: `border-radius: var(--radius-pill)` (preferred) OR `border-radius: 11px` (acceptable since it is the geometric half-height). Pick `--radius-pill` for consistency.
- For each frame, set the frame's `data-theme` attribute (`"dark"` or `"light"`) on the wrapping `div`, NOT on `<body>` — because we need both themes to coexist on the page.
- ChevronRight rotation for collapsed/expanded must be visible in Frame 1 (rotated 90deg = expanded).
- The "메뉴 설정" header letter-spacing is `.08em`; uppercase via `text-transform: uppercase`. Keep these.

## E. File-level conventions

- Plain HTML (no React, no JSX, no scripts).
- No external JS dependencies. No `<script>` tags at all.
- Maintain 2-space indentation.
- Keep the file under ~2200 lines (precedent W4 is 2370; W5 should be ~1500–2000 since it's a smaller component).
- File must be UTF-8 with Korean rendering verified by visual inspection.

## F. Hand-off note for W7 TSX conversion

At the very bottom of the head comment, add a final block titled `## W7 변환 노트` that contains:
- "이 sketch 는 디자인 lock 만 정의한다. 변환 시 source `MenuSettingsSection.tsx` (418 lines) 의 비즈 로직 (mutators, effects, queryKeys, toast 문구) 은 1 byte 변경 0."
- "Phase 21 forward-merge — `migrateLegacyMenuConfig` (src/utils/api.ts L449) 의존성 보존."
- "circular import 주의: `MenuSettingsSection` → `SideMenu.MENU`. `SideMenu` 는 `MenuSettingsSection` 을 import 하지 않는다 (verified in snapshot)."

## G. Writing technique

Use the **Write** tool — never `Bash(cat << 'EOF')`.

Authoring approach: start by writing the head comment (sections 1–8 + F) in one Write call. Then append the `<style>` block in a follow-up Edit call. Then append each frame (`<div class="frame">…</div>`) in successive Edit calls. This keeps each Write/Edit call small and reviewable.
  </action>

  <verify>
    <automated>
test -f cha-bio-safety/docs/redesign-context/31-chrome/sketch-wave-5-menu-settings-section.html \
  && LINES=$(wc -l < cha-bio-safety/docs/redesign-context/31-chrome/sketch-wave-5-menu-settings-section.html) \
  && echo "lines=$LINES" \
  && test "$LINES" -ge 1500 \
  && test "$LINES" -le 2200 \
  && BODY_START=$(grep -n '^<body' cha-bio-safety/docs/redesign-context/31-chrome/sketch-wave-5-menu-settings-section.html | head -1 | cut -d: -f1) \
  && BODY_END=$(grep -n '^</body>' cha-bio-safety/docs/redesign-context/31-chrome/sketch-wave-5-menu-settings-section.html | head -1 | cut -d: -f1) \
  && BODY=$(sed -n "${BODY_START},${BODY_END}p" cha-bio-safety/docs/redesign-context/31-chrome/sketch-wave-5-menu-settings-section.html) \
  && echo "$BODY" | grep -E "font-size:\s*(9|10|11)px" -c | grep -qx 0 \
  && echo "$BODY" | grep -c "linear-gradient" | grep -qx 0 \
  && echo "$BODY" | grep -cE "✕" | grep -qx 0 \
  && echo "$BODY" | grep -cE "var\(--t1\)|var\(--t2\)|var\(--t3\)|var\(--bg3\)|var\(--bg4\)|var\(--bd2\)|var\(--danger\)|var\(--acl\)" | grep -qx 0 \
  && echo "$BODY" | grep -cE "#2563eb|#fff([^a-fA-F0-9]|$)" | grep -qx 0 \
  && grep -cE 'data-theme="light"' cha-bio-safety/docs/redesign-context/31-chrome/sketch-wave-5-menu-settings-section.html | awk '{ exit ($1 < 2) }' \
  && grep -cE 'frame' cha-bio-safety/docs/redesign-context/31-chrome/sketch-wave-5-menu-settings-section.html | awk '{ exit ($1 < 6) }' \
  && grep -q 'MenuSettingsSection' cha-bio-safety/docs/redesign-context/31-chrome/sketch-wave-5-menu-settings-section.html \
  && grep -q 'migrateLegacyMenuConfig\|Phase 21' cha-bio-safety/docs/redesign-context/31-chrome/sketch-wave-5-menu-settings-section.html \
  && grep -q 'DEFAULT_SIDE_MENU' cha-bio-safety/docs/redesign-context/31-chrome/sketch-wave-5-menu-settings-section.html \
  && grep -q 'SideMenuEntry' cha-bio-safety/docs/redesign-context/31-chrome/sketch-wave-5-menu-settings-section.html \
  && grep -q "메뉴 설정이 저장되었습니다" cha-bio-safety/docs/redesign-context/31-chrome/sketch-wave-5-menu-settings-section.html \
  && grep -q "저장에 실패했습니다" cha-bio-safety/docs/redesign-context/31-chrome/sketch-wave-5-menu-settings-section.html \
  && grep -q "메뉴 설정" cha-bio-safety/docs/redesign-context/31-chrome/sketch-wave-5-menu-settings-section.html \
  && grep -q "기본값으로 초기화" cha-bio-safety/docs/redesign-context/31-chrome/sketch-wave-5-menu-settings-section.html \
  && grep -q "설정 저장" cha-bio-safety/docs/redesign-context/31-chrome/sketch-wave-5-menu-settings-section.html \
  && grep -q "삭제할까요" cha-bio-safety/docs/redesign-context/31-chrome/sketch-wave-5-menu-settings-section.html \
  && grep -q "기본 배치로 되돌릴까요" cha-bio-safety/docs/redesign-context/31-chrome/sketch-wave-5-menu-settings-section.html \
  && grep -q "+ 구분선 추가" cha-bio-safety/docs/redesign-context/31-chrome/sketch-wave-5-menu-settings-section.html \
  && grep -q "새 구분선" cha-bio-safety/docs/redesign-context/31-chrome/sketch-wave-5-menu-settings-section.html \
  && grep -q "구분선 제목" cha-bio-safety/docs/redesign-context/31-chrome/sketch-wave-5-menu-settings-section.html \
  && grep -q "menuSettings.collapsed" cha-bio-safety/docs/redesign-context/31-chrome/sketch-wave-5-menu-settings-section.html \
  && grep -q "var(--surface-sunken)" cha-bio-safety/docs/redesign-context/31-chrome/sketch-wave-5-menu-settings-section.html \
  && grep -q "var(--accent-active)" cha-bio-safety/docs/redesign-context/31-chrome/sketch-wave-5-menu-settings-section.html \
  && grep -q "var(--status-danger)" cha-bio-safety/docs/redesign-context/31-chrome/sketch-wave-5-menu-settings-section.html \
  && grep -q "var(--text-on-accent)" cha-bio-safety/docs/redesign-context/31-chrome/sketch-wave-5-menu-settings-section.html \
  && grep -q "var(--border-strong)" cha-bio-safety/docs/redesign-context/31-chrome/sketch-wave-5-menu-settings-section.html \
  && grep -q "circular" cha-bio-safety/docs/redesign-context/31-chrome/sketch-wave-5-menu-settings-section.html \
  && echo "ALL GREP GATES PASS"
    </automated>
  </verify>

  <done>
- File exists at the exact path `cha-bio-safety/docs/redesign-context/31-chrome/sketch-wave-5-menu-settings-section.html`.
- Line count is between 1500 and 2200 (inclusive).
- Body markup contains 0 occurrences of `font-size:9/10/11px`, `linear-gradient`, literal `✕`, legacy alias tokens, raw hex.
- ≥2 frames carry `data-theme="light"` (Frames 5 + 6).
- ≥6 frames total (Frames 1–6).
- Head comment cites: `MenuSettingsSection`, `migrateLegacyMenuConfig` or `Phase 21`, `DEFAULT_SIDE_MENU`, `SideMenuEntry`, `circular`.
- All 17 verbatim Korean copy strings appear at least once.
- All 5 semantic tokens (`--surface-sunken`, `--accent-active`, `--status-danger`, `--text-on-accent`, `--border-strong`) appear in body markup.
- No changes to any other file (src/, App.tsx, tokens.css, typography.css, design-system.md, tailwind.config.js, existing 31-chrome files including 4 snapshots + W1 index + W2/W3/W4 sketches, or 00-design-context/).
- 1 atomic commit covering only this sketch file.
  </done>
</task>

</tasks>

<verification>
Run these from the repository root after the task completes:

```bash
# 1. Path + line count gates
test -f cha-bio-safety/docs/redesign-context/31-chrome/sketch-wave-5-menu-settings-section.html
LINES=$(wc -l < cha-bio-safety/docs/redesign-context/31-chrome/sketch-wave-5-menu-settings-section.html); echo "$LINES"
test "$LINES" -ge 1500 -a "$LINES" -le 2200

# 2. Negative grep gates (BODY only — exclude :root/[data-theme] blocks)
BODY_START=$(grep -n '^<body' cha-bio-safety/docs/redesign-context/31-chrome/sketch-wave-5-menu-settings-section.html | head -1 | cut -d: -f1)
BODY_END=$(grep -n '^</body>' cha-bio-safety/docs/redesign-context/31-chrome/sketch-wave-5-menu-settings-section.html | head -1 | cut -d: -f1)
sed -n "${BODY_START},${BODY_END}p" cha-bio-safety/docs/redesign-context/31-chrome/sketch-wave-5-menu-settings-section.html > /tmp/w5-body.html

grep -cE "font-size:\s*(9|10|11)px" /tmp/w5-body.html       # → 0
grep -c "linear-gradient" /tmp/w5-body.html                  # → 0
grep -cE "✕" /tmp/w5-body.html                                # → 0
grep -cE "var\(--t1\)|var\(--t2\)|var\(--t3\)|var\(--bg3\)|var\(--bg4\)|var\(--bd2\)|var\(--danger\)|var\(--acl\)" /tmp/w5-body.html  # → 0
grep -cE "#2563eb|#fff([^a-fA-F0-9]|$)" /tmp/w5-body.html    # → 0

# 3. Positive coverage gates
grep -cE 'data-theme="light"' cha-bio-safety/docs/redesign-context/31-chrome/sketch-wave-5-menu-settings-section.html  # ≥ 2
grep -cE 'class="frame"|<div class="frame"' cha-bio-safety/docs/redesign-context/31-chrome/sketch-wave-5-menu-settings-section.html  # ≥ 6
for needle in 'MenuSettingsSection' 'DEFAULT_SIDE_MENU' 'SideMenuEntry' 'migrateLegacyMenuConfig\|Phase 21' 'circular' \
              '메뉴 설정' '메뉴 설정이 저장되었습니다' '저장에 실패했습니다' '기본값으로 초기화' '설정 저장' \
              '삭제할까요' '기본 배치로 되돌릴까요' '+ 구분선 추가' '새 구분선' '구분선 제목' \
              'menuSettings.collapsed' \
              'var(--surface-sunken)' 'var(--accent-active)' 'var(--status-danger)' 'var(--text-on-accent)' 'var(--border-strong)'
do
  grep -qE "$needle" cha-bio-safety/docs/redesign-context/31-chrome/sketch-wave-5-menu-settings-section.html || { echo "MISSING: $needle"; exit 1; }
done
echo "ALL VERIFICATION GATES PASS"

# 4. Protected-file invariance — only the sketch file should be new
git status --porcelain | grep -v "^?? .planning/quick/260527-6o0-redesign-31-chrome-w5-menusettingssectio/" \
  | grep -v "cha-bio-safety/docs/redesign-context/31-chrome/sketch-wave-5-menu-settings-section.html" \
  | (! grep -q .)
```
</verification>

<success_criteria>
- The single file `cha-bio-safety/docs/redesign-context/31-chrome/sketch-wave-5-menu-settings-section.html` exists with line count in [1500, 2200].
- All negative gates (noan upgrade, no gradient, no ✕ glyph, no legacy alias tokens in body, no raw hex in body) return 0.
- All positive coverage gates (≥2 light frames, ≥6 frames total, 17 verbatim Korean strings, 5 semantic tokens, 5 head-comment anchor terms) pass.
- No other file is created or modified. wrangler not invoked. `npm run deploy` not invoked.
- 1 atomic git commit with message referencing `redesign/31-chrome W5 — MenuSettingsSection sketch`.
</success_criteria>

<output>
After completion, create `.planning/quick/260527-6o0-redesign-31-chrome-w5-menusettingssectio/260527-6o0-SUMMARY.md` with:
- File path produced
- Line count
- 8 noan upgrade applications (line → new size)
- OQ LOCKED matrix application notes (incl. W5 sub-OQ Toggle accent)
- Verification gate results (all PASS)
- Hand-off note to W6/W7 (chrome sketch series complete; next step = TSX conversion W7 with biz-anchor 1-byte-change-0 invariant)
</output>
