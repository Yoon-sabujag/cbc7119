---
phase: 260514-sp7-redesign-02-inspection-tsx-wave-2-5
plan: 01
type: execute
wave: 2
depends_on:
  - 260514-i4r-redesign-02-inspection-tsx          # Wave 1 본체 변환 (WAVE2-PRESERVE 마커 도입)
  - 260514-pnr-wave-1-fix-photobutton-revisitpopup-acce  # Wave 1 fix (외부 컴포넌트 3종 lucide + v0.1.1)
files_modified:
  - cha-bio-safety/src/pages/InspectionPage.tsx
autonomous: true
requirements:
  - REDESIGN-02-INSPECTION-TSX-WAVE2
tags:
  - redesign
  - inspection
  - tsx-conversion
  - design-tokens-v0.1.1
  - tailwind-only
  - symptom-pickers
  - wave2

must_haves:
  truths:
    - "InspectionPage.tsx 의 [WAVE2-PRESERVE-START] ... [WAVE2-PRESERVE-END] 마커 사이 5 증상 피커 JSX 가 v0.1.1 토큰 + Tailwind utility 로 교체되고 마커 주석 자체도 제거된다 (Wave 2 완료 시그널)"
    - "5 증상 피커의 비즈니스 로직(조건부 표시 분기 / state setter / 5 옵션 라벨 / '직접 입력' memo 라벨 분기) 이 한 줄도 변경되지 않는다 — symptomPick / extSymptomPick / hydrantSymptomPick / shutterSymptomPick / damperSymptomPick state 의 set 호출과 비교(=== 일치) 로직 모두 보존"
    - "5 피커 모두 시안 권위 spec(inspection-sketch-symptom-pickers.html .symptom-picker 클래스 라인 169~192) 와 1:1 매칭: gap-1.5(6px) flex-wrap, button = bg-surface-raised + 1.5px border border-default + text-text-secondary + text-label(13px) + font-semibold, active = border-accent + bg-[rgba(59,130,246,.12)] + text-accent"
    - "9 / 10 / 11px 폰트 사이즈 사용이 0건 (Wave 2 변환 영역 한정) — 모두 text-label(13px) 통일 (시안의 노안 친화 룰 §1.1)"
    - "Wave 2 변환 영역의 인라인 style={{...}} 의 금지 키(color / background / border / padding / margin / fontSize / fontWeight / borderRadius / display / flex* / gap) 사용이 0건"
    - "소화전 증상 피커는 옵션 4개라 시안의 flex-wrap 룰이 그대로 적용 (소화기/방화셔터/유도등/전실제연댐퍼는 옵션 3개로 1행)"
    - "TypeScript 컴파일 에러 0개, npm run build 통과"
    - "/inspection 라우트 라이트/다크 모두 정상 렌더 — 5 카테고리(유도등/소화기/소화전/방화셔터/전실제연댐퍼)에서 result !== 'normal' 일 때 증상 피커 표시되며 active button 누르면 v0.1.1 accent 색 강조"
  artifacts:
    - path: "cha-bio-safety/src/pages/InspectionPage.tsx"
      provides: "Wave 2 변환 완료된 5 증상 피커 JSX. WAVE2-PRESERVE 마커 제거. 비즈니스 로직 100% 보존. Wave 3~6 (5 특수 모달 + 미정 영역) 은 후속 트랙."
      min_lines: 5500
      contains: "symptomPick, extSymptomPick, hydrantSymptomPick, shutterSymptomPick, damperSymptomPick, 점등 이상, 받침 파손, 경종 파손, 방화셔터 라인 표시 필요, 기판 조작 불량, 직접 입력"
  key_links:
    - from: "InspectionPage.tsx 5 증상 피커 블록"
      to: "tailwind.config.js (text-label, border-border-default, bg-surface-raised, text-text-secondary, border-accent, text-accent)"
      via: "Tailwind utility 클래스"
      pattern: "text-label|bg-surface-raised|border-border-default|text-text-secondary|border-accent|text-accent"
    - from: "InspectionPage.tsx 5 증상 피커 블록"
      to: "5 state setter — 비즈니스 로직 보존"
      via: "onClick 핸들러"
      pattern: "set(Symptom|ExtSymptom|HydrantSymptom|ShutterSymptom|DamperSymptom)Pick"
---

<objective>
InspectionPage TSX 변환 **Wave 2** — Wave 1 (`260514-i4r`) 에서 인라인 style 그대로 보존했던 `[WAVE2-PRESERVE-START] ... [WAVE2-PRESERVE-END]` 마커 사이 85줄(라인 3573~3658) 의 **5 증상 피커 JSX 블록**(유도등 / 소화기 / 소화전 / 방화셔터 / 전실제연댐퍼) 를 5차 시안(`inspection-sketch-symptom-pickers.html` line 169~192 `.symptom-picker` 클래스) 권위 spec 에 맞춰 v0.1.1 토큰 + Tailwind only 로 교체한다.

**Purpose:**
- 디자인 시스템 v0.1.1 의 §1.1 노안 친화(13px+ 강제) 룰을 5 피커에 적용 — 기존 10/11px 폰트 0건화
- 시안의 active 상태 표현(`border-accent + tinted bg + accent 색`) 적용 — Wave 1 결과 버튼 3종(§7.1) 과 시각 일관성 정렬
- WAVE2-PRESERVE 마커 제거 (Wave 2 완료 시그널) — Wave 3 이후 분리 트랙 명확화
- 5 피커 모두 동일 패턴이라 단일 task 일괄 변환이 가장 안전 (이질 변환 분산 방지)

**Output:**
- 비즈니스 로직 100% 보존하면서 5 피커 마크업/스타일이 시안과 1:1 매칭된 새 `InspectionPage.tsx`
- WAVE2-PRESERVE 마커 주석 2 줄 제거
- 5 카테고리 detection 분기(`isGuideLight && result !== 'normal' && (selectedCP as any).locationNo !== 'audience_passage'`, `isExtinguisher && result !== 'normal'`, `selectedCP?.category === '소화전' && result !== 'normal'`, `selectedCP?.category === '방화셔터' && result !== 'normal'`, `selectedCP?.category === '전실제연댐퍼' && result !== 'normal'`) 한 줄도 변경 없음
- 5 state setter 호출(`setSymptomPick / setExtSymptomPick / setHydrantSymptomPick / setShutterSymptomPick / setDamperSymptomPick`) 한 줄도 변경 없음
- 5 옵션 라벨 배열 그대로 (`['점등 이상','예비전원 이상','직접 입력']` 등)
- '직접 입력' 모드 memo 라벨 분기(라인 3664~3669) 는 Wave 1 에서 이미 Tailwind 화되어 있으므로 손대지 않음

**Out of scope (Wave 3~ follow-up):**
- 5 특수 모달 본문 (`StairwellModal` / `CctvModal` / `BaeyeonModal` / `DivModal` / `CompressorModal` / `PowerPanelModal` / `ParkingGateModal` / `DamperModal`) — 별도 quick 트랙
- InspectionPage.tsx 의 Wave 1 변환 영역(메인 page render / DesktopInspectionView / InspectionModal 셸 / Resolution* / PhotoViewer / FireAlarmModal) — 이미 변환됨, 한 줄도 건드리지 않음
- `CATEGORY_GROUPS` / `INSPECT_RESULT_OPTIONS` / 헬퍼 상수 — 수정 금지
- API / hooks / utils / 외부 컴포넌트 — 수정 금지
- 다른 페이지 (.tsx) — 수정 금지
- 5 피커 의 비즈니스 로직 (state default / 분기 / 라벨 / '직접 입력' 분기) — 수정 금지
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/STATE.md

# Wave 1 본체 (변환 패턴 + must_haves 형식 단일 진실 소스)
@.planning/quick/260514-i4r-redesign-02-inspection-tsx/260514-i4r-PLAN.md
@.planning/quick/260514-i4r-redesign-02-inspection-tsx/260514-i4r-SUMMARY.md

# Wave 1 fix (외부 컴포넌트 3종 v0.1.1 화 — 토큰 매핑 표 재사용)
@.planning/quick/260514-pnr-wave-1-fix-photobutton-revisitpopup-acce/260514-pnr-PLAN.md

# 디자인 시스템 v0.1.1 — §1.1 노안 친화(13px+) / §7.1 결과 입력 일관성 / §7.3 result picker
@cha-bio-safety/docs/design-system.md

# 5차 시안 HTML — Wave 2 의 마크업/색/구조 단일 진실 소스 (.symptom-picker 클래스 line 169~192)
@cha-bio-safety/docs/redesign-context/02-inspection/sketch/inspection-sketch-symptom-pickers.html
@cha-bio-safety/docs/redesign-context/02-inspection/sketch/inspection-audit-design-system.html

# 변환 대상 — WAVE2-PRESERVE 마커 사이 85줄 (라인 3573~3658)
@cha-bio-safety/src/pages/InspectionPage.tsx

# 토큰 / Tailwind 카탈로그
@cha-bio-safety/src/styles/tokens.css
@cha-bio-safety/tailwind.config.js
</context>

<interfaces>
<!-- 이 변환이 의존하는 InspectionPage 내부 시그니처. 변경 금지. -->
<!-- Executor 는 아래 정확히 보존 (한 줄도 변경 금지). -->

# 1. 5 피커 detection 분기 (라인 3575 / 3592 / 3609 / 3626 / 3643) — 그대로 보존
isGuideLight && result !== 'normal' && (selectedCP as any).locationNo !== 'audience_passage'
isExtinguisher && result !== 'normal'
selectedCP?.category === '소화전' && result !== 'normal'
selectedCP?.category === '방화셔터' && result !== 'normal'
selectedCP?.category === '전실제연댐퍼' && result !== 'normal'

# 2. 5 state setter (라인 2907~2912 정의, 라인 3580 / 3597 / 3614 / 3631 / 3648 호출) — 그대로 보존
const [symptomPick,        setSymptomPick]        = useState<string>('점등 이상')
const [extSymptomPick,     setExtSymptomPick]     = useState<string>('받침 파손')
const [hydrantSymptomPick, setHydrantSymptomPick] = useState<string>('경종 파손')
const [shutterSymptomPick, setShutterSymptomPick] = useState<string>('방화셔터 라인 표시 필요')
const [damperSymptomPick,  setDamperSymptomPick]  = useState<string>('기판 조작 불량')

# 3. 5 옵션 라벨 배열 — 한 글자도 변경 금지
유도등:        ['점등 이상','예비전원 이상','직접 입력']                          // 3개
소화기:        ['받침 파손','연한 만료','직접 입력']                              // 3개
소화전:        ['경종 파손','위치표시등 점등 이상','호스걸이 파손','직접 입력']    // 4개 → flex-wrap 필요
방화셔터:      ['방화셔터 라인 표시 필요','연동제어기 기판 작동 불','직접 입력']  // 3개
전실제연댐퍼:  ['기판 조작 불량','모터 기능 이상','직접 입력']                    // 3개

# 4. '직접 입력' 모드 memo 라벨 분기 (라인 3663~3669) — Wave 1 에서 이미 Tailwind 화. 한 줄도 변경 금지.
{(isGuideLight && result !== 'normal' && (selectedCP as any).locationNo !== 'audience_passage' && symptomPick === '직접 입력')
  || (isExtinguisher && result !== 'normal' && extSymptomPick === '직접 입력')
  || (selectedCP?.category === '소화전' && result !== 'normal' && hydrantSymptomPick === '직접 입력')
  || (selectedCP?.category === '방화셔터' && result !== 'normal' && shutterSymptomPick === '직접 입력')
  || (selectedCP?.category === '전실제연댐퍼' && result !== 'normal' && damperSymptomPick === '직접 입력')
  ? '증상 상세 및 특이사항 (선택)' : '특이사항 (선택)'}

# 5. 저장 시 memo 분기 (라인 3303~3314) — 한 줄도 변경 금지
finalMemo = symptomPick === '직접 입력' ? memo.trim() : symptomPick
finalMemo = extSymptomPick === '직접 입력' ? memo.trim() : extSymptomPick
finalMemo = hydrantSymptomPick === '직접 입력' ? memo.trim() : hydrantSymptomPick
finalMemo = shutterSymptomPick === '직접 입력' ? memo.trim() : shutterSymptomPick
finalMemo = damperSymptomPick === '직접 입력' ? memo.trim() : damperSymptomPick

# 6. WAVE2-PRESERVE 마커 (라인 3573 / 3658) — Wave 2 변환 후 두 줄 모두 제거
{/* [WAVE2-PRESERVE-START] 5 증상 피커 — 인라인 style 보존 (다음 quick 트랙에서 변환) */}
{/* [WAVE2-PRESERVE-END] 5 증상 피커 끝 */}

# 7. 시안 권위 spec (inspection-sketch-symptom-pickers.html line 169~192 `.symptom-picker`)
.symptom-picker {
  display:flex; gap:6px; flex-wrap:wrap;
}
.symptom-picker button {
  flex:1 1 0; min-width:0;
  padding:9px 8px;
  border-radius:10px;
  border:1.5px solid var(--bd);                     // 1.5px (Wave 1 의 1px 보다 굵음)
  background:var(--bg2);                            // = --surface-raised
  color:var(--t2);                                  // = --text-secondary
  font-size:13px;                                   // 노안 친화 §1.1
  font-weight:600;
  cursor:pointer;
  text-align:center;
  transition:all .12s;
  line-height:1.3;
}
.symptom-picker button.is-active {
  border-color:var(--acl);                          // = --accent
  background:var(--status-accent-bg);               // = rgba(59,130,246,0.13) dark / rgba(31,111,235,0.10) light
  color:var(--acl);                                 // = --accent
}

# 8. 증상 라벨(피커 위 작은 헤더) — 변환 후 Tailwind 화
{/* 라인 3577 / 3594 / 3611 / 3628 / 3645 */}
<div style={{ fontSize:10, fontWeight:600, color:'var(--t3)', marginBottom:6, letterSpacing:'0.05em' }}>증상</div>
// → <div className="text-caption font-semibold text-text-tertiary mb-1.5 tracking-wider">증상</div>
// (시안의 .bar-label 패턴 — Wave 1 결과 라벨 "점검 결과" 와 동일 클래스)

# 9. 인라인 style 허용 키 (이 변환 영역 한정)
- 1.5px 보더 → Tailwind 미정의(border-2 는 2px) — `border-[1.5px]` arbitrary value 또는 인라인 style 의 `borderWidth:'1.5px'` 만 허용
- active 배경 `rgba(59,130,246,.12)` → `bg-[rgba(59,130,246,0.12)]` arbitrary value 권장. 또는 라이트 모드 분기 위해 인라인 var(--status-accent-bg) 사용 가능 (단, tokens.css 에 정의 안 됨 → arbitrary value 가 안전)
- 그 외 모든 정적 색/배경/패딩/폰트/radius/flex/gap 는 인라인 금지 (Tailwind utility 만)
</interfaces>

<conversion_strategy>
**Wave 1 변환 패턴 그대로 (i4r/5xl 패턴) — 5 피커 일괄 동일 변환:**

1. **Read 1회 → Edit 5회(또는 Read+Write 1회):**
   - 피커 5개가 거의 동일한 형태(옵션 개수만 다름)이므로 Edit 5번 (각 피커 블록을 시안 spec 으로 교체)
   - WAVE2-PRESERVE-START / END 마커 주석 2 줄 별도 Edit 로 제거
   - 또는 Read 전체 → Write 전체 1회 (단, 다른 영역 한 줄도 안 건드린다는 룰 엄수 — 변경 diff 가 5 피커 + 마커 2줄 = 정확히 약 90줄로 한정)

2. **각 피커 블록 변환 패턴 (5번 동일 적용):**

   변환 전:
   ```tsx
   <div style={{ marginTop:10 }}>
     <div style={{ fontSize:10, fontWeight:600, color:'var(--t3)', marginBottom:6, letterSpacing:'0.05em' }}>증상</div>
     <div style={{ display:'flex', gap:5 }}>
       {['옵션A','옵션B','직접 입력'].map(s => (
         <button key={s} onClick={() => setXSymptomPick(s)} style={{
           flex:1, padding:'8px 4px', borderRadius:10, cursor:'pointer',
           border: xSymptomPick===s ? '2px solid var(--acl)' : '1px solid var(--bd)',
           background: xSymptomPick===s ? 'rgba(59,130,246,.12)' : 'var(--bg2)',
           fontSize:11, fontWeight:700, color: xSymptomPick===s ? 'var(--acl)' : 'var(--t2)',
         }}>{s}</button>
       ))}
     </div>
   </div>
   ```

   변환 후 (시안 .symptom-picker 1:1):
   ```tsx
   <div className="mt-2.5">
     <div className="text-caption font-semibold text-text-tertiary mb-1.5 tracking-wider">증상</div>
     <div className="flex flex-wrap gap-1.5">
       {['옵션A','옵션B','직접 입력'].map(s => {
         const active = xSymptomPick === s
         return (
           <button key={s} onClick={() => setXSymptomPick(s)}
             className={`flex-1 basis-0 min-w-0 px-2 py-2 rounded-md cursor-pointer text-label font-semibold text-center leading-tight transition-colors ${
               active
                 ? 'border-[1.5px] border-accent bg-[rgba(59,130,246,0.12)] text-accent'
                 : 'border-[1.5px] border-border-default bg-surface-raised text-text-secondary'
             }`}>
             {s}
           </button>
         )
       })}
     </div>
   </div>
   ```

   **매핑 표:**
   | 옛 (Wave 1 보존) | 새 (Wave 2 변환) |
   |---|---|
   | `marginTop:10` | `mt-2.5` (10px) |
   | `fontSize:10, fontWeight:600, color:'var(--t3)', marginBottom:6, letterSpacing:'0.05em'` | `text-caption font-semibold text-text-tertiary mb-1.5 tracking-wider` |
   | `display:'flex', gap:5` | `flex flex-wrap gap-1.5` (gap 6px — 시안 spec) |
   | `flex:1, padding:'8px 4px', borderRadius:10` | `flex-1 basis-0 min-w-0 px-2 py-2 rounded-md` |
   | `border: '2px solid var(--acl)'` (active) | `border-[1.5px] border-accent` |
   | `border: '1px solid var(--bd)'` (inactive) | `border-[1.5px] border-border-default` |
   | `background: 'rgba(59,130,246,.12)'` (active) | `bg-[rgba(59,130,246,0.12)]` |
   | `background: 'var(--bg2)'` (inactive) | `bg-surface-raised` |
   | `fontSize:10 또는 11, fontWeight:700` | `text-label font-semibold` (13px / 600 — 시안 spec) |
   | `color: 'var(--acl)'` (active) | `text-accent` |
   | `color: 'var(--t2)'` (inactive) | `text-secondary` (→ `text-text-secondary`) |

   **시안 spec 룰:**
   - 시안은 active/inactive 모두 1.5px 보더 (Wave 1 옛 코드는 active=2px, inactive=1px 비대칭 — 시안 권위 따라 1.5px 통일)
   - 시안 font-weight 는 600 (Wave 1 옛 코드는 700 — 시안 권위 따라 600/font-semibold 통일)
   - 시안 padding 은 9px 8px — Tailwind `px-2 py-2` (8px) 또는 `py-[9px] px-2` arbitrary. 본 plan 은 `px-2 py-2` 권장 (4의 배수 룰 §1.1)
   - 소화전(4 옵션)은 flex-wrap 자동 적용 (3개+1개 또는 2개+2개 자연 줄바꿈)

3. **WAVE2-PRESERVE 마커 제거 (변환 종료 시 마지막 Edit):**
   - 라인 3573: `{/* [WAVE2-PRESERVE-START] 5 증상 피커 — 인라인 style 보존 (다음 quick 트랙에서 변환) */}` 한 줄 삭제
   - 라인 3658: `{/* [WAVE2-PRESERVE-END] 5 증상 피커 끝 */}` 한 줄 삭제
   - 두 마커 사이의 5 피커 블록 사이사이의 빈 줄/주석(`{/* 유도등: 증상 피커 ... */}` 등 라인 3574 / 3591 / 3608 / 3625 / 3642) 은 유지 권장 (가독성)

4. **인라인 style 잔존 방지 자기 검증:**
   ```bash
   sed -n '3570,3680p' cha-bio-safety/src/pages/InspectionPage.tsx | grep -c "var(--bg2\|var(--bd\|var(--acl\|var(--t2\|var(--t3)\|fontSize:10\|fontSize:11"
   # → 0 이어야 함
   sed -n '3570,3680p' cha-bio-safety/src/pages/InspectionPage.tsx | grep -c "WAVE2-PRESERVE"
   # → 0 이어야 함 (마커 제거됨)
   ```

5. **build/tsc 검증:**
   - `cd cha-bio-safety && npx tsc --noEmit` → 0 에러
   - `cd cha-bio-safety && npm run build` → ✓ built

6. **시각 검증(autonomous 권장 — 단, 자동 검증으로 충분):**
   - 시안 권위 spec 매칭은 위 grep gate + 시각 1회 spot check 로 충분
   - 라이트/다크 양쪽 자동 — tokens.css 분기 작동 (--surface-raised / --text-secondary / --accent / --border-default 모두 모드별 정의됨)
   - 단 `rgba(59,130,246,0.12)` arbitrary value 는 라이트 모드에서 시안의 `rgba(31,111,235,0.10)` 과 미세 차이 — Wave 1 i4r 의 동일 패턴(라인 3583 등) 그대로 유지 (시안의 dark 톤 기준). 라이트 모드에서 약간 진하게 보이는 건 시안 의도된 허용 범위
</conversion_strategy>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 5 증상 피커 일괄 변환 + WAVE2-PRESERVE 마커 제거</name>
  <files>cha-bio-safety/src/pages/InspectionPage.tsx</files>
  <behavior>
변환 후 InspectionPage.tsx 의 (이전) 라인 3573~3658 영역이 다음을 만족해야 한다:

- WAVE2-PRESERVE-START / END 마커 주석 2 줄 모두 제거됨 (`grep WAVE2-PRESERVE InspectionPage.tsx` → 0건)
- 5 증상 피커(유도등 / 소화기 / 소화전 / 방화셔터 / 전실제연댐퍼) 모두 시안 권위 spec (`.symptom-picker` 클래스 라인 169~192) 와 1:1 매칭:
  - 컨테이너: `flex flex-wrap gap-1.5`
  - 각 button: `flex-1 basis-0 min-w-0 px-2 py-2 rounded-md cursor-pointer text-label font-semibold text-center leading-tight`
  - inactive: `border-[1.5px] border-border-default bg-surface-raised text-text-secondary`
  - active: `border-[1.5px] border-accent bg-[rgba(59,130,246,0.12)] text-accent`
  - 증상 라벨: `text-caption font-semibold text-text-tertiary mb-1.5 tracking-wider`
  - 피커 컨테이너 외곽 wrapper: `mt-2.5`
- 비즈니스 로직 100% 보존:
  - 5 detection 분기 한 글자도 변경 없음
  - 5 setter 호출(`setSymptomPick / setExtSymptomPick / setHydrantSymptomPick / setShutterSymptomPick / setDamperSymptomPick`) 한 글자도 변경 없음
  - 5 옵션 라벨 배열 한 글자도 변경 없음 (`'점등 이상' / '예비전원 이상' / '받침 파손' / '연한 만료' / '경종 파손' / '위치표시등 점등 이상' / '호스걸이 파손' / '방화셔터 라인 표시 필요' / '연동제어기 기판 작동 불' / '기판 조작 불량' / '모터 기능 이상' / '직접 입력'`)
  - `===` 비교 active 분기 그대로
- 인라인 style 금지 키 0건 (라인 3570~3680 범위 한정):
  - `style={{` 등장 횟수 = 0 (단, 5 피커 외 다른 영역의 인라인 style 은 손대지 않음 — diff 범위 한정)
- 9 / 10 / 11px 폰트 사이즈 0건 (이 영역 한정) — 모두 `text-caption(12px)` 또는 `text-label(13px)`
- '직접 입력' 모드 memo 라벨 분기(라인 3663~3669) 는 한 줄도 변경 없음 (이미 Wave 1 에서 Tailwind 화됨)
- TypeScript 컴파일 에러 0개, npm run build 통과
- /inspection 라우트 라이트/다크 양쪽에서 5 카테고리 result !== 'normal' 진입 시 시안 spec 대로 렌더
  </behavior>
  <action>
**Step 1.1 — Read 로 변환 영역 정확 위치 확보:**

```
Read /Users/jykevin/Documents/cbc7119-design/cha-bio-safety/src/pages/InspectionPage.tsx offset=3570 limit=120
```

확인 사항:
- 라인 3573: `{/* [WAVE2-PRESERVE-START] ... */}` 마커 존재
- 라인 3658: `{/* [WAVE2-PRESERVE-END] ... */}` 마커 존재
- 5 피커 블록(유도등/소화기/소화전/방화셔터/전실제연댐퍼) 순서 확인
- 5 피커 사이의 카테고리 주석(`{/* 유도등: 증상 피커 ... */}` 등) 유지

**Step 1.2 — 5 피커 Edit (각 피커 블록을 시안 spec 으로 교체):**

5번의 Edit 호출로 각 피커 블록 일괄 교체. 각 피커는 옵션 라벨과 setter 만 다르고 마크업 구조는 동일.

**1.2.1 유도등 피커 (라인 3575~3589 영역):**

`old_string` (Edit 의 old_string — 정확 매칭 필요. 들여쓰기 12-space 가정):
```
            {/* 유도등: 증상 피커 (점검 결과 아래, 특이사항 위) */}
            {isGuideLight && result !== 'normal' && (selectedCP as any).locationNo !== 'audience_passage' && (
              <div style={{ marginTop:10 }}>
                <div style={{ fontSize:10, fontWeight:600, color:'var(--t3)', marginBottom:6, letterSpacing:'0.05em' }}>증상</div>
                <div style={{ display:'flex', gap:5 }}>
                  {['점등 이상','예비전원 이상','직접 입력'].map(s => (
                    <button key={s} onClick={() => setSymptomPick(s)} style={{
                      flex:1, padding:'8px 4px', borderRadius:10, cursor:'pointer',
                      border: symptomPick===s ? '2px solid var(--acl)' : '1px solid var(--bd)',
                      background: symptomPick===s ? 'rgba(59,130,246,.12)' : 'var(--bg2)',
                      fontSize:11, fontWeight:700, color: symptomPick===s ? 'var(--acl)' : 'var(--t2)',
                    }}>{s}</button>
                  ))}
                </div>
              </div>
            )}
```

`new_string`:
```
            {/* 유도등: 증상 피커 (점검 결과 아래, 특이사항 위) */}
            {isGuideLight && result !== 'normal' && (selectedCP as any).locationNo !== 'audience_passage' && (
              <div className="mt-2.5">
                <div className="text-caption font-semibold text-text-tertiary mb-1.5 tracking-wider">증상</div>
                <div className="flex flex-wrap gap-1.5">
                  {['점등 이상','예비전원 이상','직접 입력'].map(s => {
                    const active = symptomPick === s
                    return (
                      <button key={s} onClick={() => setSymptomPick(s)}
                        className={`flex-1 basis-0 min-w-0 px-2 py-2 rounded-md cursor-pointer text-label font-semibold text-center leading-tight transition-colors ${
                          active
                            ? 'border-[1.5px] border-accent bg-[rgba(59,130,246,0.12)] text-accent'
                            : 'border-[1.5px] border-border-default bg-surface-raised text-text-secondary'
                        }`}>
                        {s}
                      </button>
                    )
                  })}
                </div>
              </div>
            )}
```

**1.2.2 소화기 피커** — 동일 패턴, 단:
- detection: `{isExtinguisher && result !== 'normal' && (`
- options: `['받침 파손','연한 만료','직접 입력']`
- setter: `setExtSymptomPick`
- active 변수명: `const active = extSymptomPick === s`

**1.2.3 소화전 피커** — 동일 패턴, 단:
- detection: `{selectedCP?.category === '소화전' && result !== 'normal' && (`
- options: `['경종 파손','위치표시등 점등 이상','호스걸이 파손','직접 입력']` (4개 — flex-wrap 자동 적용)
- setter: `setHydrantSymptomPick`
- active 변수명: `const active = hydrantSymptomPick === s`
- 옛 코드의 `padding:'8px 2px'` → 동일하게 `px-2 py-2` (시안 spec 통일)

**1.2.4 방화셔터 피커** — 동일 패턴, 단:
- detection: `{selectedCP?.category === '방화셔터' && result !== 'normal' && (`
- options: `['방화셔터 라인 표시 필요','연동제어기 기판 작동 불','직접 입력']`
- setter: `setShutterSymptomPick`
- active 변수명: `const active = shutterSymptomPick === s`

**1.2.5 전실제연댐퍼 피커** — 동일 패턴, 단:
- detection: `{selectedCP?.category === '전실제연댐퍼' && result !== 'normal' && (`
- options: `['기판 조작 불량','모터 기능 이상','직접 입력']`
- setter: `setDamperSymptomPick`
- active 변수명: `const active = damperSymptomPick === s`

**Step 1.3 — WAVE2-PRESERVE 마커 2 줄 Edit 제거:**

마지막 두 Edit:

Edit 1 (START 마커 제거):
```
old_string:
            {/* [WAVE2-PRESERVE-START] 5 증상 피커 — 인라인 style 보존 (다음 quick 트랙에서 변환) */}
            {/* 유도등: 증상 피커 (점검 결과 아래, 특이사항 위) */}

new_string:
            {/* 유도등: 증상 피커 (점검 결과 아래, 특이사항 위) */}
```

Edit 2 (END 마커 제거):
```
old_string:
            )}
            {/* [WAVE2-PRESERVE-END] 5 증상 피커 끝 */}

new_string:
            )}
```
(END 마커 바로 위 줄 `)}` 는 전실제연댐퍼 피커의 닫는 괄호 — 정확 매칭 위해 그 줄까지 포함)

**Step 1.4 — Grep gate (자기 검증):**

```bash
cd /Users/jykevin/Documents/cbc7119-design

# WAVE2-PRESERVE 마커 0건 확인
grep -c "WAVE2-PRESERVE" cha-bio-safety/src/pages/InspectionPage.tsx
# → 0

# 5 피커 영역의 옛 토큰 잔존 0건 확인 (라인 3570~3680 한정 — sed range 로 좁히기)
sed -n '3570,3680p' cha-bio-safety/src/pages/InspectionPage.tsx | grep -cE "var\(--bg2\)|var\(--bd\)|var\(--acl\)|var\(--t2\)|var\(--t3\)|fontSize:10\b|fontSize:11\b|fontWeight:700"
# → 0 (위 그렙은 정확히 옛 인라인 스타일 패턴만 매칭. 5 피커 외부의 다른 var(--) 사용은 매칭 안 됨)

# 5 setter 호출 5건 그대로 보존 확인
grep -cE "set(Symptom|ExtSymptom|HydrantSymptom|ShutterSymptom|DamperSymptom)Pick\(s\)" cha-bio-safety/src/pages/InspectionPage.tsx
# → 5

# 5 옵션 라벨 배열 그대로 보존
grep -c "'점등 이상'" cha-bio-safety/src/pages/InspectionPage.tsx          # → 2 (정의 라인 2907 + 피커 라인)
grep -c "'받침 파손'" cha-bio-safety/src/pages/InspectionPage.tsx          # → 2
grep -c "'경종 파손'" cha-bio-safety/src/pages/InspectionPage.tsx          # → 2
grep -c "'방화셔터 라인 표시 필요'" cha-bio-safety/src/pages/InspectionPage.tsx  # → 2
grep -c "'기판 조작 불량'" cha-bio-safety/src/pages/InspectionPage.tsx     # → 2
```

**Step 1.5 — Build 검증:**

```bash
cd /Users/jykevin/Documents/cbc7119-design/cha-bio-safety
npx tsc --noEmit
npm run build
```

둘 다 통과해야 함.

**Step 1.6 — Diff 자기 검증 (변경 범위 한정 확인):**

```bash
cd /Users/jykevin/Documents/cbc7119-design
git diff --stat cha-bio-safety/src/pages/InspectionPage.tsx
# → 1 file changed, 약 80~100 insertions(+), 약 90 deletions(-) 예상
# (5 피커 × 16줄 변환 + 마커 2줄 제거 = 약 80~100줄 diff)
```

다른 영역(라인 1~3572, 3681~끝) diff 가 발생하면 안 됨. 만약 우발적 변경이 있으면 그 영역만 `git checkout -p` 로 복원.
  </action>
  <verify>
    <automated>
cd /Users/jykevin/Documents/cbc7119-design && \
echo "=== 1. WAVE2-PRESERVE marker removed ===" && \
[ "$(grep -c 'WAVE2-PRESERVE' cha-bio-safety/src/pages/InspectionPage.tsx)" = "0" ] && echo "PASS" || (echo "FAIL: WAVE2-PRESERVE still present"; exit 1) && \
echo "=== 2. Old inline style tokens in symptom picker region (line 3570-3680) ===" && \
[ "$(sed -n '3570,3680p' cha-bio-safety/src/pages/InspectionPage.tsx | grep -cE 'var\(--bg2\)|var\(--bd\)|var\(--acl\)|var\(--t2\)|var\(--t3\)|fontSize:10|fontSize:11|fontWeight:700')" = "0" ] && echo "PASS" || (echo "FAIL: legacy tokens still in picker region"; exit 1) && \
echo "=== 3. 5 setters preserved ===" && \
[ "$(grep -cE 'set(Symptom|ExtSymptom|HydrantSymptom|ShutterSymptom|DamperSymptom)Pick\(s\)' cha-bio-safety/src/pages/InspectionPage.tsx)" = "5" ] && echo "PASS" || (echo "FAIL: setter count mismatch"; exit 1) && \
echo "=== 4. 5 option labels preserved (≥2 occurrences each — state default + picker) ===" && \
for label in "점등 이상" "받침 파손" "경종 파손" "방화셔터 라인 표시 필요" "기판 조작 불량"; do \
  cnt=$(grep -c "'$label'" cha-bio-safety/src/pages/InspectionPage.tsx); \
  [ "$cnt" -ge "2" ] || (echo "FAIL: '$label' count=$cnt"; exit 1); \
done && echo "PASS" && \
echo "=== 5. TypeScript compile ===" && \
(cd cha-bio-safety && npx tsc --noEmit) && echo "PASS" && \
echo "=== 6. Build ===" && \
(cd cha-bio-safety && npm run build > /tmp/sp7-build.log 2>&1) && echo "PASS" || (tail -40 /tmp/sp7-build.log; exit 1)
    </automated>
  </verify>
  <done>
- 5 증상 피커 모두 시안 spec 매칭 + 비즈니스 로직 100% 보존
- WAVE2-PRESERVE 마커 2줄 제거 (Wave 2 완료 시그널)
- 9/10/11px 폰트 0건 (이 영역 한정)
- 인라인 style 금지 키 0건 (이 영역 한정)
- tsc + build 통과
- atomic commit: `feat(260514-sp7): redesign/02-inspection Wave 2 — 5 증상 피커 v0.1.1 토큰 + Tailwind 화 (WAVE2-PRESERVE 마커 제거)`
  </done>
</task>

</tasks>

<verification>
**자동 검증 (Task 1 verify 블록):**
1. WAVE2-PRESERVE 마커 0건 (Wave 2 완료 시그널)
2. 5 피커 영역(sed 3570~3680)의 옛 인라인 토큰(`var(--bg2|bd|acl|t2|t3)`, `fontSize:10|11`, `fontWeight:700`) 0건
3. 5 setter 호출 5건 그대로
4. 5 카테고리 옵션 라벨 ≥2건씩 (state default + 피커)
5. tsc 0 에러
6. npm run build 통과

**수동 검증 (Wave 1 fix pnr 와 동일하게 사용자 시각 확인 — 단 plan 자체는 autonomous):**
- /inspection 진입 → 5 카테고리(유도등 / 소화기 / 소화전 / 방화셔터 / 전실제연댐퍼) 중 임의 1개 선택 → result 를 '주의' 또는 '불량' 으로 → 증상 피커 노출 → button 누르면 active 상태(border-accent + tinted bg + accent 색) 표시 → 라이트/다크 양쪽 spot check
- 사용자가 빠른 시각 확인 후 OK 면 main 머지 + 배포 (디자인 브랜치 룰: 사용자 컨펌 후 머지)
</verification>

<success_criteria>
- InspectionPage.tsx 의 WAVE2-PRESERVE 마커 사이 5 피커 영역이 시안 권위 spec 과 1:1 매칭된 새 마크업으로 교체됨
- WAVE2-PRESERVE 마커 주석 2줄 제거됨
- 5 피커의 비즈니스 로직(detection / setter / 옵션 라벨 / 직접 입력 분기) 한 줄도 변경 없음
- 5 피커 영역의 인라인 style 금지 키 + 9/10/11px 폰트 0건
- TypeScript 컴파일 + npm run build 통과
- 5 카테고리 라이트/다크 양쪽 정상 렌더 (active 상태 시안 spec 대로 강조)
- atomic git commit 1건 (Wave 2 트랙 마커)
</success_criteria>

<output>
After completion, create `.planning/quick/260514-sp7-redesign-02-inspection-tsx-wave-2-5/260514-sp7-SUMMARY.md` documenting:
- 변경 줄 수 (diff stat: 1 file, ~80-100 +/-90 -)
- 5 피커 모두 시안 spec 매칭 확인 (border-[1.5px] / bg-[rgba(59,130,246,0.12)] / text-accent / text-label 등)
- 비즈니스 로직 보존 grep gate 결과
- Wave 3 ~ 후속 트랙 안내 (5 특수 모달 본문)
</output>
