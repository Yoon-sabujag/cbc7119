---
phase: 260515-iz1-redesign-07-elevator-tsx-wave-3-fault-3-
plan: 01
type: execute
wave: 1
depends_on:
  - 260515-3mc-redesign-07-elevator-tsx-wave-1-list        # Wave 1 — TYPE_ICON_COMPONENT 매퍼 + ElevatorIcon icons.tsx 신설 + lucide import 블록 + 인라인 화이트리스트 패턴(position/zIndex 풀스크린) 권위
  - 260515-ia6-redesign-07-elevator-tsx-wave-2-evselect    # Wave 2 — EvSelector / EsBtn / EsNodeMap 변환 완료. 본 Wave 의 FaultNewModal / FaultNewFullscreen 안 EvSelector 호출이 자동으로 신규 디자인 반영. lucide import (ChevronUp/ChevronDown/MoveDiagonal/AlertTriangle/...) 재사용 권위
  - 260515-bgg-redesign-07-elevator-3-sketch-fault-faul    # 2B sketch — Fault 흐름 3 모달 단일 진실 소스 (fault-modals-sketch.html 1541줄). 4 viewport 모두 1:1 매핑 + 디자인 의사결정 코멘트 블록 권위
  - 260514-i4r-redesign-02-inspection-tsx                  # InspectionPage Wave 1 — must_haves carrier 패턴 + 영역 한정 변환 권위
  - 260515-2r5-redesign-02-inspection-tsx-wave-6-powerp    # 02-inspection Wave 6 — task별 1 모달 분리 패턴 + verify grep gate 패턴 권위
files_modified:
  - cha-bio-safety/src/pages/ElevatorPage.tsx
autonomous: true
requirements:
  - REDESIGN-07-ELEVATOR-TSX-WAVE3-FAULT-3-MODALS
tags:
  - redesign
  - elevator
  - tsx-conversion
  - design-tokens-v0.1.1
  - tailwind-only
  - faultnewmodal
  - faultnewfullscreen
  - faultresolvemodal
  - wave3

must_haves:
  truths:
    - "FaultNewModal / FaultNewFullscreen / FaultResolveModal 3 함수의 props 시그니처(elevators / selected / onClose / onSubmit / loading — FaultNewModal · elevators / onClose / onSubmit / loading — FaultNewFullscreen · fault / onClose / onSubmit / loading — FaultResolveModal)가 한 글자도 변경되지 않는다. 함수명 / 매개변수 순서 / 타입 / 기본값 / 반환 구조 모두 보존"
    - "FaultNewModal 비즈니스 로직 100% 보존: initKind = selected ? (selected.type==='escalator'?'escalator':'elevator') : '' / useState 7개 (evKind / elevatorId / faultAt / faultFloor / hasPassenger / symptoms / photoKeys) / isElev / floors = EV_FLOORS[elevatorId] / handleSelectEv(setElevatorId + setFaultAt(nowKstLocal()) + setFaultFloor('')) / handleSubmit(floorPart + passPart + symptoms 합성 / faultAt+':00' / isResolved:false) / EvSelector props (elevators, evKind, setEvKind+setElevatorId('')+setFaultFloor('') 콜백, elevatorId, setElevatorId=handleSelectEv, groups=EV_GROUPS_FAULT, esNodes=ES_NODES_FAULT) — 라인 shift / 마크업 wrap 변경만 허용"
    - "FaultNewFullscreen 비즈니스 로직 100% 보존: useState 7개 (evKind 초기값 '' / elevatorId / faultAt / faultFloor / hasPassenger / symptoms / photoKeys) / isElev / floors / handleSelectEv / handleSubmit 모두 FaultNewModal 와 동일 로직 / EvSelector props 동일 / fullscreen 오버레이 컨테이너 + 자체 헤더 + 본문 스크롤 + 하단 고정 CTA 영역 3분할 구조 보존"
    - "FaultResolveModal 비즈니스 로직 100% 보존: useState 4개 (repairCompany 초기값 'TKE' / repairedAt 초기값 nowKstLocal() / repairDetail / repairPhotoKeys) / pureSymptoms = fault.symptoms.replace(/^\\[[^\\]]+\\]\\s*/, '').replace(/\\[승객탑승\\]\\s*/, '') 정규식 두 번 적용 / onSubmit 페이로드 (id:fault.id / repairCompany / repairedAt:repairedAt+':00' / repairDetail / repairPhotoKeys) / disabled = !repairDetail.trim()||loading"
    - "FaultNewModal 의 ModalWrap 호출 (`<ModalWrap title=\"고장 접수\" onClose={onClose}>`) 과 FaultResolveModal 의 ModalWrap 호출 (`<ModalWrap title=\"수리 완료 처리\" onClose={onClose}>`) 시그니처 그대로 보존 — ModalWrap 본체 (라인 2528~2546) 한 줄도 수정 X (Wave 후속에서 처리)"
    - "Field 컴포넌트 (라인 2547~2554) 본체 한 줄도 수정 X. Field 호출만 그대로 유지 (`<Field label=\"발생 일시\">`, `<Field label=\"발생 층\" style={{ flex:1 }}>`, `<Field label=\"증상\">`, `<Field label=\"수리 업체\">`, `<Field label=\"수리 완료 일시\">`, `<Field label=\"수리 내용\">`). style={{ flex:1 }} prop 전달은 화이트리스트 — Field 시그니처 보존을 위해 보존"
    - "MultiPhotoUpload 호출 (`<MultiPhotoUpload label=\"증상 사진\" keys={photoKeys} setKeys={setPhotoKeys} />` 2회 + `<MultiPhotoUpload label=\"수리 사진\" keys={repairPhotoKeys} setKeys={setRepairPhotoKeys} />` 1회) 시그니처 그대로 보존. MultiPhotoUpload 본체 (라인 2988~) 한 줄도 수정 X"
    - "EvSelector 호출 시그니처 보존 — FaultNewModal / FaultNewFullscreen 안의 `<EvSelector elevators={...} evKind={...} setEvKind={...} elevatorId={...} setElevatorId={handleSelectEv} groups={EV_GROUPS_FAULT} esNodes={ES_NODES_FAULT} />` props 한 줄도 변경 금지. EvSelector 본체 (Wave 2 변환 완료, 라인 1941~) 수정 X"
    - "TKE 자동 다이얼 보존 — FaultNewModal CTA (`<a href={TKE_TEL} style={{ textDecoration:'none', display:'block' }}><button onClick={handleSubmit} ...>`) 와 FaultNewFullscreen CTA (`<a href={TKE_TEL} ...><button ...>`) 둘 다 `<a href={TKE_TEL}>` 래핑 패턴 보존. `TKE_TEL = 'tel:18999070'` 상수 변경 X. 텍스트(`고장 접수 (TKE 자동 연결)` / `🚨 고장 접수 (TKE 자동 연결)` — Fullscreen 만 `🚨` 이모지 제거하여 `고장 접수 (TKE 자동 연결)` 통일) 시각 룰만 변경"
    - "submitFault.mutate / resolveFault.mutate 사용처 호출 (메인 페이지 render 내 `<FaultNewModal ... onSubmit={b => submitFault.mutate(b)} />`, `<FaultNewFullscreen ... />`, `<FaultResolveModal ... onSubmit={b => resolveFault.mutate(b)} />`) 한 줄도 변경 0건. mutation 정의 (useMutation) / 핸들러 / API 호출 모두 보존"
    - "변환 영역(라인 2022~2253, 정확히 3 함수 본체 — FaultNewModal + FaultNewFullscreen + FaultResolveModal) 안에 인라인 style={{...}} 의 금지 키(color / background / backgroundColor / padding / paddingLeft|Right|Top|Bottom / margin / marginLeft|Right|Top|Bottom / fontSize / fontWeight / borderRadius / display / flex / flexDirection / justifyContent / alignItems / grid / gridTemplateColumns / gap / width / height / textAlign / lineHeight / cursor / border / borderTop|... — 정적값) 사용이 0개이다. 화이트리스트 4개만 예외: (a) position / top / left / right / bottom — FaultNewFullscreen 오버레이 동적값 (`bottom: NAV_H` 등) (b) zIndex: 100 — 풀스크린 컨테이너 (Wave 1 데스크톱 헤더 패턴 동일) (c) paddingTop: 'var(--sat, 44px)' — iOS safe-area-inset-top 동적값 (d) opacity (CTA disabled 동적값 = `(!조건||loading)?0.5:1`) 만 인라인 보존 가능. 그 외 모든 정적값 → Tailwind utility"
    - "변환 영역 안에 9 / 10 / 11px 폰트 사이즈 사용이 0개이다 — 현재 Fullscreen 헤더 부제 fontSize:10 / Field label fontSize:11 (Field 본체 내부) / 승객 토글 라벨 fontSize:11 / FaultResolve 카드 fontSize:11 모두 변환 후 12+ (Tailwind text-caption=12 / text-label=13 / text-body-sm=14 / text-body=16). Field 본체는 수정 X 이므로 Field 의 11px 라벨은 본 Wave 변환 영역 밖. 변환 영역 안의 인라인 fontSize 만 격상 대상"
    - "변환 영역 안에 옛 토큰(var(--bg) / var(--bg2) / var(--bg3) / var(--bd) / var(--bd2) / var(--acl) / var(--t1) / var(--t2) / var(--t3) / var(--safe) / var(--warn) / var(--danger) / var(--fire) / var(--info)) 사용이 0개이다 — 모두 Tailwind utility 로 교체 (bg-surface-base / bg-surface-raised / bg-surface-sunken / bg-fire-bg / bg-danger-bg / text-text-primary / text-text-secondary / text-text-tertiary / text-fire / text-danger / text-text-on-accent / border-border-default 등). 단, FaultNewFullscreen 오버레이 컨테이너의 `background:'var(--bg)'` 는 Tailwind utility(`bg-surface-base`) 로 교체"
    - "변환 영역 안에 이모지(🚨 / ✕ / 🛗 / 📦 / 🔲 / ↕️ / ⚠️) 본문 사용이 0개이다. 모두 lucide-react + 커스텀 icons.tsx 매핑: FaultNewFullscreen 헤더 아이콘 박스 = `<AlertTriangle size={20} className=\"text-danger\" />` (2B sketch VP3 권위 — danger-bg 아이콘 박스 + AlertTriangle text-danger) / FaultNewFullscreen 헤더 ✕ = `<X size={20} />` lucide / FaultNewFullscreen CTA 텍스트의 `🚨 고장 접수 (TKE 자동 연결)` → `고장 접수 (TKE 자동 연결)` (이모지 제거, 헤더 아이콘 박스가 위험 알림 역할 — 2B sketch §5 권위) / FaultNewModal 의 승객 탑승 ON 텍스트 `탑승 🚨` → `<AlertTriangle size={14} /> 탑승` (lucide 아이콘 + 텍스트 조합, danger 색) / FaultResolveModal 의 고장 정보 카드 `{TYPE_ICON[fault.elevator_type]}` (이모지 객체 lookup) → `TYPE_ICON_COMPONENT[fault.elevator_type]` 매퍼 사용 (Wave 1 신설 — passenger=ElevatorIcon / cargo=Package / dumbwaiter=UtensilsCrossed / escalator=MoveDiagonal — 회색 통일 text-text-secondary / size 20~24)"
    - "lucide-react import 블록(현재 Wave 2: Package, UtensilsCrossed, MoveDiagonal, ChevronRight, ChevronUp, ChevronDown, AlertTriangle, Wrench)에 **X 만 추가**된다. AlertTriangle / MoveDiagonal / Package / UtensilsCrossed 는 Wave 1+2 에서 이미 import (재사용). Siren 은 사용하지 않음 (2B sketch VP3 권위 — 헤더 아이콘 박스는 AlertTriangle 사용). 신규 import = X (1개)"
    - "TYPE_ICON_COMPONENT 매퍼 (Wave 1 신설, 라인 197) 가 FaultResolveModal 의 고장 정보 카드에서 재사용된다 — `const TypeIcon = TYPE_ICON_COMPONENT[fault.elevator_type]; <TypeIcon size={20} className=\"text-text-secondary\" />` 패턴 (Wave 1 list 탭 카드 패턴 동일, 라인 576). TYPE_ICON_COMPONENT 정의 (라인 197~) 본체 수정 X. 회색 통일(text-text-secondary 또는 text-text-primary, sketch VP4 권위)"
    - "FaultNewModal CTA = fire 그라디언트 보존 — 현재 코드 `style={{ ...primaryBtnSt, opacity:..., width:'100%' }}` 는 primaryBtnSt(기본 accent 색)을 base 로 쓰는데, 2B sketch VP1 결정에 따라 fire 그라디언트(`linear-gradient(135deg, #991b1b, #ef4444)`)로 변경. Wave 1 데스크톱 헤더 CTA `fire 그라디언트` 패턴과 동일 — §6.4 그라디언트 화이트리스트(fire 위험 의미만 예외) 적용. 인라인 `background: 'linear-gradient(135deg, #991b1b, #ef4444)'` 보존은 §6.4 그라디언트 화이트리스트 예외. 또는 Tailwind arbitrary value (`bg-[linear-gradient(135deg,#991b1b,#ef4444)]`) 사용 가능"
    - "FaultNewFullscreen CTA = fire 그라디언트 보존 — 현재 코드 이미 `background:'linear-gradient(135deg,#991b1b,#ef4444)'` 인라인 적용 중 (라인 2205). §6.4 그라디언트 화이트리스트 예외로 보존. 텍스트만 이모지 제거: `🚨 고장 접수 (TKE 자동 연결)` → `고장 접수 (TKE 자동 연결)` (FaultNewModal 과 통일, 2B sketch §5 권위)"
    - "FaultResolveModal CTA = accent 단색 (그라디언트 X) — 현재 코드 `style={{ ...primaryBtnSt, opacity:..., }}` 의 primaryBtnSt 가 accent 단색이라면 그대로, 아니라면 `bg-accent text-text-on-accent` Tailwind utility 로 명시 교체. 2B sketch VP2/VP4 권위 — `.cta-fault-resolve` 클래스가 bg-accent 단색. §6.4 그라디언트 정책 (FaultResolve 는 위험 의미 아님 — 단색)"
    - "**승객 탑승 토글 시각 룰** (FaultNewModal + FaultNewFullscreen 동일): ON 상태 = `bg-danger-bg text-danger outline outline-2 outline-danger` + `<AlertTriangle size={14} />` 아이콘 + `탑승` 텍스트 (이모지 🚨 제거, 2B sketch §3 권위). OFF 상태 = `bg-surface-sunken text-text-tertiary` + `미탑승` 텍스트 (outline 없음). 터치 타겟 height 44px (모바일) / 40px (데스크톱, Fullscreen 은 데스크톱 우선). gap-1 또는 gap-2 (아이콘 + 텍스트 사이)"
    - "**FaultNewFullscreen 풀스크린 컨테이너 시각 룰** (2B sketch VP3 권위): 외곽 컨테이너 = `fixed inset-0 z-[100] bg-surface-base flex flex-col` (인라인 화이트리스트 = `top:0, left:0, right:0, bottom: NAV_H`(동적) + `zIndex:100`+ `paddingTop:'var(--sat, 44px)'` + `boxSizing:'border-box'` 만 보존 — 그 외 background/display/flexDirection 은 Tailwind utility 로 교체). 자체 헤더 = `flex-shrink-0 bg-surface-raised border-b border-border-default flex items-center gap-2.5 px-4 pt-3.5 pb-3` (인라인 padding/background/border 제거 → Tailwind). 본문 스크롤 = `flex-1 min-h-0 overflow-y-auto overflow-x-hidden p-4`. 하단 CTA 영역 = `flex-shrink-0 p-3 px-4 bg-surface-raised border-t border-border-default`"
    - "**FaultNewFullscreen 자체 헤더 시각 룰** (2B sketch VP3 §6 권위): 아이콘 박스 = `w-9 h-9 rounded-md bg-danger-bg flex items-center justify-center` + `<AlertTriangle size={20} className=\"text-danger\" />` (현 코드 🚨 이모지 + bg `rgba(239,68,68,.15)` 제거). 타이틀 = `flex-1` 안 `text-body font-bold text-text-primary` (현 fontSize:15 → 16, text-body) + 부제 `text-caption text-text-tertiary` (현 fontSize:10 → 12, text-caption, 노안 친화). 닫기 버튼 = `bg-none border-none text-text-tertiary cursor-pointer p-1 hover:text-text-primary hover:bg-surface-sunken rounded-md transition` + `<X size={20} />` (현 ✕ 문자 → lucide)"
    - "**FaultResolveModal 고장 정보 카드 시각 룰** (2B sketch VP2/VP4 §7 권위): 카드 = `bg-surface-sunken rounded-lg p-3` (현 padding `10px 13px` + `borderRadius:10` 토큰화). 호기 라벨 행 = `flex items-center gap-2` + `<TypeIcon size={20} className=\"text-text-secondary\" />` + `text-body-sm font-bold text-text-primary` (현 fontSize:11 → 14, text-body-sm 노안 친화) — 'N호기' 라벨 그대로 (EV-NN 본문 노출 0건). pureSymptoms = `text-label text-text-secondary mt-1.5` (현 fontSize:11 인라인 + `<br/>` + `<span>` → 13px text-label, br 제거 + 줄바꿈 mt 처리)"
    - "**EmptyState 컴포넌트 (라인 2555~2562) 본체 수정 X** — 본 Wave 변환 영역 밖. EmptyState 호출도 Fault 모달 3종 내에는 없음"
    - "**inputSt / primaryBtnSt 상수 (라인 2786~) 본체 수정 X** — 본 Wave 변환 영역 밖. 단, 변환 영역 안의 `style={inputSt}` 사용 패턴은 Tailwind utility 로 교체 가능 (예: `<input type=\"datetime-local\" className=\"w-full h-[42px] px-3 bg-surface-sunken border border-border-default rounded-md text-text-primary text-body-sm focus:border-border-strong outline-none transition\" />`). 또는 inputSt 가 inline 으로 동일 효과를 내고 있다면 inputSt 인라인 적용을 Tailwind 교체본으로 명시 변경. 결정: **inputSt 사용처 모두 Tailwind utility 로 교체** — Tailwind 일관성 + 변환 영역 인라인 금지 키 0건 강제. inputSt 상수 자체는 보존 (다른 미변환 영역에서 사용 중)"
    - "**ModalWrap / Field / MultiPhotoUpload / EvSelector / EmptyState 컴포넌트 본체 수정 0건** — 본 Wave 의 호출처 변환만. ModalWrap 의 헤더 ✕ 문자 / borderBottom 인라인 / Field 의 11px 라벨 / MultiPhotoUpload 의 사진 슬롯 시각은 모두 본 Wave out of scope (후속 Wave 에서 처리)"
    - "tailwind.config.js / tokens.css / 다른 페이지 / 다른 컴포넌트 / hooks / utils / API / 라우팅 / D1 스키마 / icons.tsx / DesignPage / App.tsx / BottomNav / DesktopSidebar / GlobalHeader / Wave 1 변환 영역(list 탭/자체 헤더/데스크톱 좌측 컬럼) / Wave 2 변환 영역(EsBtn/EsNodeMap/EvSelector 3 헬퍼) 수정 0건. **Wave 3 변환 영역 = 라인 2022~2253, 정확히 3 함수 본체(FaultNewModal + FaultNewFullscreen + FaultResolveModal) + lucide-react import 블록(X 추가)** 만 변경"
    - "InspectModal / RepairNewModal / EvDetailModal 본문 + 다른 탭 본문(fault/repair/inspect/annual/safety) + 데스크톱 우측 컬럼(desktopRightTab 영역) + KoelsaHistorySection 호출 + RepairListSection 호출 + ElevatorInfoCard 컴포넌트 + ModalWrap / Field / EmptyState / MultiPhotoUpload / CertSummary / CertBlock / FindingsPanel / FindingCountBadge / CertViewerModal 본체는 한 줄도 변경 없이 보존된다 — Wave 4+ 후속"
    - "TypeScript 컴파일 0 에러, `npm run build` 통과. cbc7119-preview.pages.dev 자동 배포 대상이지만 본 Wave 의 verify 는 빌드 통과까지 — 시각 검수는 사용자가 push 후 별도 진행"
  artifacts:
    - path: "cha-bio-safety/src/pages/ElevatorPage.tsx"
      provides: "Wave 3 변환 완료 — 라인 2022~2253 (FaultNewModal + FaultNewFullscreen + FaultResolveModal 3 함수 본체) 가 v0.1.1 토큰 + Tailwind only. lucide-react import 블록에 X 추가. 그 외 영역(Wave 1+2 변환 영역 / InspectModal / RepairNewModal / EvDetailModal / 다른 탭 본문 / 데스크톱 우측 컬럼 / 다른 헬퍼 / 데이터 상수 / 비즈니스 로직 / 사용처 mutation 호출) 은 한 줄도 변경 없이 보존"
      min_lines: 3200
      contains: "function FaultNewModal, function FaultNewFullscreen, function FaultResolveModal, AlertTriangle, X, TYPE_ICON_COMPONENT, bg-danger-bg, text-danger, outline-danger, bg-surface-base, bg-surface-raised, bg-surface-sunken, bg-fire-bg, text-text-on-accent, TKE_TEL, EV_GROUPS_FAULT, ES_NODES_FAULT"
  key_links:
    - from: "cha-bio-safety/src/pages/ElevatorPage.tsx (FaultNewModal / FaultNewFullscreen / FaultResolveModal)"
      to: "tailwind.config.js color/spacing/fontSize tokens"
      via: "Tailwind utility 클래스 (bg-surface-base / bg-surface-raised / bg-surface-sunken / bg-danger-bg / bg-fire-bg / text-text-primary / text-text-secondary / text-text-tertiary / text-danger / text-fire / text-text-on-accent / bg-accent / outline-danger / border-border-default / border-border-strong / text-caption / text-label / text-body-sm / text-body / font-bold)"
      pattern: "bg-(surface-(base|raised|sunken)|danger-bg|fire-bg|accent)|text-(text-(primary|secondary|tertiary|on-accent)|danger|fire|caption|label|body-sm|body)|outline-danger|border-border-(default|strong)"
    - from: "cha-bio-safety/src/pages/ElevatorPage.tsx (FaultNewFullscreen 헤더 아이콘 + 닫기 + 승객 토글 + FaultResolveModal 카드 아이콘)"
      to: "lucide-react (X 신규 + AlertTriangle / MoveDiagonal / Package / UtensilsCrossed 재사용) + icons.tsx ElevatorIcon (재사용)"
      via: "이모지(🚨 / ✕ / 🛗 / 📦 / 🔲 / ↕️) 제거 → lucide+커스텀 아이콘 컴포넌트. import 블록에 X 추가. AlertTriangle = Fullscreen 헤더 아이콘 박스 + 승객 탑승 ON 토글 / X = Fullscreen 닫기 / TYPE_ICON_COMPONENT 매퍼(Wave 1) = FaultResolveModal 고장 정보 카드"
      pattern: "<(AlertTriangle|X|MoveDiagonal|Package|UtensilsCrossed|ElevatorIcon|TypeIcon)\\s+(size|className)"
    - from: "cha-bio-safety/src/pages/ElevatorPage.tsx (FaultResolveModal 고장 정보 카드)"
      to: "TYPE_ICON_COMPONENT 매퍼 (라인 197, Wave 1 신설)"
      via: "`const TypeIcon = TYPE_ICON_COMPONENT[fault.elevator_type]; <TypeIcon size={20} className=\"text-text-secondary\" />` 패턴 — Wave 1 list 탭 카드 (라인 576) 동일. passenger=ElevatorIcon / cargo=Package / dumbwaiter=UtensilsCrossed / escalator=MoveDiagonal (회색 통일)"
      pattern: "TYPE_ICON_COMPONENT\\[fault\\.elevator_type\\]|const\\s+TypeIcon\\s*=\\s*TYPE_ICON_COMPONENT"
    - from: "cha-bio-safety/src/pages/ElevatorPage.tsx (FaultNewModal + FaultNewFullscreen EvSelector 호출)"
      to: "EvSelector 함수 본체 (라인 1941~, Wave 2 변환 완료)"
      via: "`<EvSelector elevators={...} evKind={...} setEvKind={v => { setEvKind(v); setElevatorId(''); setFaultFloor('') }} elevatorId={...} setElevatorId={handleSelectEv} groups={EV_GROUPS_FAULT} esNodes={ES_NODES_FAULT} />` 호출 — 한 줄도 변경 금지. Wave 2 토큰 자동 반영"
      pattern: "<EvSelector\\s+(elevators|evKind|setEvKind|elevatorId|setElevatorId|groups|esNodes)"
    - from: "cha-bio-safety/src/pages/ElevatorPage.tsx (FaultNewModal + FaultNewFullscreen CTA)"
      to: "TKE_TEL 상수 (라인 227) + 사용처 mutation submitFault.mutate"
      via: "`<a href={TKE_TEL} style={{ textDecoration:'none', display:'block' }}><button onClick={handleSubmit} disabled={...} ...>` 래핑 보존 — TKE 자동 다이얼. textDecoration / display 화이트리스트 (Tailwind no-underline block 으로 교체 가능). onSubmit 페이로드 변경 0건"
      pattern: "<a\\s+href=\\{TKE_TEL\\}|TKE_TEL"
    - from: "cha-bio-safety/src/pages/ElevatorPage.tsx (FaultNewFullscreen 풀스크린 컨테이너)"
      to: "NAV_H 상수 (라인 19) + iOS safe-area-inset-top"
      via: "`bottom: NAV_H` (동적 — BottomNav 위까지) + `paddingTop: 'var(--sat, 44px)'` (iOS safe-area) + `zIndex: 100` (Wave 1 데스크톱 헤더 동일 패턴). 인라인 style 화이트리스트 (5xl Dashboard 풀스크린 패턴 동일)"
      pattern: "NAV_H|bottom:\\s*NAV_H|zIndex:\\s*100|paddingTop:\\s*'var\\(--sat"
    - from: "cha-bio-safety/src/pages/ElevatorPage.tsx (Wave 3 변환 영역)"
      to: "design-system.md v0.1.1 §6.1 / §6.3 / §6.4 / §6.5 / §6.7 / §7.1 + 2B sketch §1~7 디자인 의사결정"
      via: "§6.1 색 분리(fire=호기 고장 / danger=승객 즉시 위험·Fullscreen 헤더 / accent=FaultResolve CTA) / §6.3 회색 통일 / §6.4 CTA 그라디언트 1개(FaultNew fire) + 단색(FaultResolve accent) / §6.5 hover(border-strong + translateY) / §6.7 그림자 X(모달 본체 shadow 예외) / §7.1 이모지 0건 + 노안(본문 16px, 보조 12-14px, 9-11px 0건) + 터치 타겟(모바일 44px / 데스크톱 40px)"
      pattern: "(bg-fire-bg|text-fire|bg-danger-bg|text-danger|outline-danger|bg-accent|text-text-on-accent)|(border-border-(default|strong))|(text-(caption|label|body-sm|body))"
    - from: "cha-bio-safety/src/pages/ElevatorPage.tsx (Wave 3 호출처 보존)"
      to: "메인 페이지 render 내 `<FaultNewModal ... onSubmit={b => submitFault.mutate(b)} />`, `<FaultNewFullscreen ... onSubmit={...} />`, `<FaultResolveModal ... onSubmit={b => resolveFault.mutate(b)} />`"
      via: "props 시그니처 보존 → 사용처 호출 한 줄도 변경 0건. submitFault / resolveFault mutation 정의 + 핸들러 + API 호출 모두 보존"
      pattern: "<(FaultNewModal|FaultNewFullscreen|FaultResolveModal)\\s+|submitFault\\.mutate|resolveFault\\.mutate"
---

<objective>
ElevatorPage.tsx 의 **Fault 모달 3종**(FaultNewModal / FaultNewFullscreen / FaultResolveModal) 본체를 v0.1.1 디자인 토큰 + Tailwind utility only 로 변환한다. 2B sketch (`fault-modals-sketch.html` 1541줄) 4 viewport 단일 진실 소스를 1:1 매핑하여 시각 룰을 적용한다.

**Purpose:** redesign/07-elevator Wave 3 — 본 Wave 는 사용자가 호기 고장을 접수하고 수리 완료를 처리하는 핵심 입력 흐름이다. §6.1 색 의미 분리(fire=호기 고장 / danger=승객 즉시 위험 / accent=수리 완료)를 시각적으로 분명히 하고, FaultNewFullscreen 의 자체 헤더 아이콘 박스로 위험 의미를 강조한다. Wave 1+2 에서 확립한 패턴(TYPE_ICON_COMPONENT 매퍼 / EvSelector 헬퍼 / lucide import 블록 / 인라인 화이트리스트)을 그대로 확장하여 일관성을 유지한다.

**Output:** ElevatorPage.tsx 라인 2022~2253 (3 함수 본체) Tailwind 교체본 + lucide-react import 블록 X 추가. 비즈니스 로직 100% 보존, props 시그니처 보존, 사용처 호출(submitFault.mutate / resolveFault.mutate / `<FaultNewModal />` / `<FaultNewFullscreen />` / `<FaultResolveModal />`) 변경 0건. ModalWrap / Field / MultiPhotoUpload / EvSelector / EmptyState / TYPE_ICON_COMPONENT 컴포넌트 본체 수정 0건.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/STATE.md
@.planning/quick/260515-iz1-redesign-07-elevator-tsx-wave-3-fault-3-/

# Wave 3 source files
@cha-bio-safety/src/pages/ElevatorPage.tsx
@cha-bio-safety/docs/redesign-context/07-elevator/sketch/fault-modals-sketch.html
@cha-bio-safety/src/components/ui/icons.tsx
@cha-bio-safety/tailwind.config.js

# Wave 1+2 SUMMARY (커밋 + grep gate 패턴 + lucide import 권위)
@.planning/quick/260515-3mc-redesign-07-elevator-tsx-wave-1-list/260515-3mc-SUMMARY.md
@.planning/quick/260515-ia6-redesign-07-elevator-tsx-wave-2-evselect/260515-ia6-SUMMARY.md

# 2B sketch SUMMARY (시각 결정 권위)
@.planning/quick/260515-bgg-redesign-07-elevator-3-sketch-fault-faul/260515-bgg-SUMMARY.md
@.planning/quick/260515-bgg-redesign-07-elevator-3-sketch-fault-faul/260515-bgg-PLAN.md

# 02-inspection Wave 1+6 패턴 권위
@.planning/quick/260514-i4r-redesign-02-inspection-tsx/260514-i4r-PLAN.md
@.planning/quick/260515-2r5-redesign-02-inspection-tsx-wave-6-powerp/

<interfaces>
<!-- Wave 3 변환 영역의 props 시그니처 + 비즈니스 로직 + 헬퍼 시그니처 — 보존 필수 -->

From cha-bio-safety/src/pages/ElevatorPage.tsx (Wave 3 변환 대상 함수 시그니처):
```typescript
function FaultNewModal({ elevators, selected, onClose, onSubmit, loading }: {
  elevators: Elevator[]
  selected: Elevator | null
  onClose: () => void
  onSubmit: (b: any) => void
  loading: boolean
})

function FaultNewFullscreen({ elevators, onClose, onSubmit, loading }: {
  elevators: Elevator[]
  onClose: () => void
  onSubmit: (b: any) => void
  loading: boolean
})

function FaultResolveModal({ fault, onClose, onSubmit, loading }: {
  fault: ElevatorFault
  onClose: () => void
  onSubmit: (b: any) => void
  loading: boolean
})
```

From cha-bio-safety/src/pages/ElevatorPage.tsx (Wave 1+2 helpers — 본 Wave 가 의존, 수정 X):
```typescript
// 라인 17 — Wave 1 icons.tsx 신설
import { ElevatorIcon } from '../components/ui/icons'

// 라인 16 — Wave 2 lucide import (X 만 추가)
import { Package, UtensilsCrossed, MoveDiagonal, ChevronRight, ChevronUp, ChevronDown, AlertTriangle, Wrench } from 'lucide-react'
// 변경 후:
// import { Package, UtensilsCrossed, MoveDiagonal, ChevronRight, ChevronUp, ChevronDown, AlertTriangle, Wrench, X } from 'lucide-react'

// 라인 19 — 풀스크린 컨테이너 bottom 동적값
const NAV_H = 'calc(54px + env(safe-area-inset-bottom, 20px))'

// 라인 197 — Wave 1 신설 TYPE_ICON_COMPONENT 매퍼 (재사용)
const TYPE_ICON_COMPONENT: Record<string, React.ComponentType<{ size?: number | string; className?: string; strokeWidth?: number | string }>> = {
  passenger: ElevatorIcon,
  cargo: Package,
  dumbwaiter: UtensilsCrossed,
  escalator: MoveDiagonal,
}

// 라인 227 — TKE 자동 다이얼 상수
const TKE_TEL = 'tel:18999070'

// 라인 1941~ — Wave 2 변환 완료 EvSelector (호출 시그니처 보존)
function EvSelector({ elevators, evKind, setEvKind, elevatorId, setElevatorId, groups, esNodes })

// 라인 2528~2546 — ModalWrap (헤더 ✕ 문자 보존, Wave 후속)
function ModalWrap({ title, onClose, children }: { title:string; onClose:()=>void; children:React.ReactNode })

// 라인 2547~2554 — Field (11px 라벨 보존, Wave 후속)
function Field({ label, children, style }: { label:string; children:React.ReactNode; style?:React.CSSProperties })

// 라인 2988~ — MultiPhotoUpload (사진 슬롯 시각 보존, Wave 후속)
function MultiPhotoUpload({ label, keys, setKeys, max = 5 })
```

From 2B sketch (fault-modals-sketch.html 디자인 의사결정 코멘트 블록):
```text
1. ModalWrap shell — 헤더 16px 700 + lucide X (X 문자 → 아이콘)
2. Field — 라벨 13px 700 (Wave 후속, 본 Wave 변환 영역 밖)
3. 승객 탑승 토글 — ON: bg-danger-bg + text-danger + outline-2 outline-danger + AlertTriangle 14px / OFF: bg-sunken + text-t3
4. MultiPhotoUpload — Wave 후속, 본 Wave 변환 영역 밖
5. CTA §6.4 그라디언트 정책 — FaultNew: fire 그라디언트(135deg #991b1b #ef4444) 예외 / FaultResolve: bg-accent 단색
6. FaultNewFullscreen 자체 헤더 — 아이콘 박스 36x36 rounded-md bg-danger-bg + AlertTriangle 20px text-danger / 타이틀 16px 700 / 부제 12px text-t3 / 닫기 lucide X 20px
7. FaultResolve 고장 정보 카드 — bg-sunken rounded-lg p-3 / TypeIcon 매퍼 / 호기 라벨 14px 700 / pureSymptoms 13px text-t2
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: FaultNewModal 변환 (라인 2022~2109, ModalWrap 안 모바일/데스크톱 공용)</name>
  <files>cha-bio-safety/src/pages/ElevatorPage.tsx</files>
  <action>
**범위**: FaultNewModal 함수 본체 (라인 2022~2109) 만 변경. 함수 시그니처 / state / 핸들러 / EvSelector 호출 / TKE_TEL `<a>` 래핑 / props 전달 / onSubmit 페이로드 한 줄도 변경 0건.

**lucide-react import 블록 (라인 16)**: 현재 `Package, UtensilsCrossed, MoveDiagonal, ChevronRight, ChevronUp, ChevronDown, AlertTriangle, Wrench` 에 **`X` 만 추가** → `Package, UtensilsCrossed, MoveDiagonal, ChevronRight, ChevronUp, ChevronDown, AlertTriangle, Wrench, X`. (Task 2/3 에서도 사용하므로 본 Task 에서 한 번에 추가)

**ModalWrap 호출 보존**: `<ModalWrap title="고장 접수" onClose={onClose}>` 그대로. ModalWrap 본체 (라인 2528) 수정 X.

**본문 컨테이너 (라인 2051)**: `<div style={{ display:'flex', flexDirection:'column', gap:14 }}>` → `<div className="flex flex-col gap-[14px]">` (또는 gap-3.5).

**EvSelector 호출 보존 (라인 2052~2057)**: 한 줄도 변경 X. Wave 2 토큰 자동 반영.

**조건부 본문 (`{elevatorId && (...)}` 라인 2059)**: 그대로 보존. 안쪽 `<>` Fragment 보존.

**발생 일시 Field (라인 2061~2063)**:
- `<Field label="발생 일시">` 그대로 보존 (Field 본체 수정 X)
- 안의 `<input type="datetime-local" value={faultAt} onChange={e => setFaultAt(e.target.value)} style={inputSt} />` 의 `style={inputSt}` 인라인 → Tailwind utility 로 교체: `className="w-full h-[42px] px-3 bg-surface-sunken border border-border-default rounded-md text-text-primary text-body-sm focus:border-border-strong outline-none transition"` (inputSt 상수 자체는 보존, 본 Wave 변환 영역 안의 인라인 적용만 제거)

**발생 층 + 승객 탑승 (라인 2066~2087)**: `{isElev && (...)}` 조건부 그대로 보존.
- 외곽 `<div style={{ display:'flex', gap:8, alignItems:'flex-end' }}>` → `<div className="flex gap-2 items-end">`
- `<Field label="발생 층" style={{ flex:1 }}>` 의 `style={{ flex:1 }}` 는 Field 의 prop 시그니처 (style?:React.CSSProperties) 이므로 보존 — Field 본체에 전달되는 wrapper style. (또는 Field 시그니처 보존 위해 wrapper `<div className="flex-1"><Field label="발생 층">` 패턴으로 교체 가능. 결정: **`style={{ flex:1 }}` 보존** — Field 시그니처 한 글자도 변경 X 룰)
- `<select value={faultFloor} onChange={e => setFaultFloor(e.target.value)} style={inputSt}>` → `style={inputSt}` 제거 + Tailwind className 적용 (input 과 동일 utility)
- 승객 탑승 토글 (라인 2074~2086):
  - 외곽 `<div style={{ flexShrink:0 }}>` → `<div className="flex-shrink-0">`
  - 라벨 `<div style={{ fontSize:11, fontWeight:700, color:'var(--t2)', marginBottom:5 }}>승객 탑승</div>` → `<div className="text-caption font-bold text-text-secondary mb-1.5">승객 탑승</div>` (fontSize 11 → text-caption 12 격상)
  - 토글 button: 현재 인라인 style 객체 전체 제거 → Tailwind utility 분기:
    - ON 상태 (hasPassenger=true): `className="h-[42px] px-4 rounded-md border-none cursor-pointer text-label font-bold bg-danger-bg text-danger outline outline-2 outline-danger whitespace-nowrap flex items-center gap-1 transition hover:-translate-y-px"`
    - OFF 상태 (hasPassenger=false): `className="h-[42px] px-4 rounded-md border-none cursor-pointer text-label font-bold bg-surface-sunken text-text-tertiary whitespace-nowrap flex items-center justify-center transition hover:-translate-y-px"`
    - className 분기는 템플릿 리터럴 또는 조건부 표현으로: `` className={`h-[42px] px-4 rounded-md border-none cursor-pointer text-label font-bold whitespace-nowrap flex items-center gap-1 transition hover:-translate-y-px ${hasPassenger ? 'bg-danger-bg text-danger outline outline-2 outline-danger' : 'bg-surface-sunken text-text-tertiary justify-center'}`} ``
  - 텍스트 내용:
    - ON: `<><AlertTriangle size={14} /> 탑승</>` (이모지 🚨 제거, lucide AlertTriangle 아이콘 + 텍스트)
    - OFF: `미탑승` (텍스트만)
    - 조건부: `{hasPassenger ? <><AlertTriangle size={14} /> 탑승</> : '미탑승'}`

**증상 textarea (라인 2089~2091)**:
- `<Field label="증상">` 보존
- `<textarea value={symptoms} onChange={e => setSymptoms(e.target.value)} rows={3} placeholder="고장 증상을 입력하세요" style={{ ...inputSt, resize:'none' }} />` 의 `style={{ ...inputSt, resize:'none' }}` 제거 → Tailwind: `className="w-full px-3 py-2.5 bg-surface-sunken border border-border-default rounded-md text-text-primary text-body-sm focus:border-border-strong outline-none transition resize-none"`

**MultiPhotoUpload 호출 보존 (라인 2093)**: `<MultiPhotoUpload label="증상 사진" keys={photoKeys} setKeys={setPhotoKeys} />` 그대로.

**CTA (라인 2095~2103)**:
- `<a href={TKE_TEL} style={{ textDecoration:'none', display:'block' }}>` 의 `style` → `className="no-underline block"` (TKE_TEL 보존)
- `<button onClick={handleSubmit} disabled={!symptoms.trim() || loading} style={{ ...primaryBtnSt, opacity:(!symptoms.trim()||loading)?0.5:1, width:'100%' }}>` 의 인라인 style:
  - `...primaryBtnSt` 전개 제거 → Tailwind utility 명시 + **fire 그라디언트 적용** (2B sketch §5 권위, §6.4 화이트리스트 예외):
  - `` className={`w-full h-[48px] rounded-md border-none cursor-pointer text-body font-bold text-text-on-accent bg-[linear-gradient(135deg,#991b1b,#ef4444)] transition hover:-translate-y-px disabled:opacity-50 disabled:cursor-not-allowed`} ``
  - 또는 `style={{ background:'linear-gradient(135deg,#991b1b,#ef4444)' }}` 인라인 + 나머지 Tailwind (그라디언트 화이트리스트 — §6.4 fire 위험 의미)
  - `opacity` 인라인 `(!symptoms.trim()||loading)?0.5:1` 은 disabled 분기로 처리 또는 `style={{ opacity: ... }}` 인라인 화이트리스트 (동적값)
- 텍스트 보존: `{loading ? '접수 중...' : '고장 접수 (TKE 자동 연결)'}`

**금지사항**:
- 비즈니스 로직 변경 0건
- EvSelector 호출 변경 0건
- props 시그니처 변경 0건
- 이모지(🚨 / ✕ / 🛗 / 📦 / 🔲 / ↕️ / ⚠️) 변환 영역 안 0건
- 9-11px 폰트 변환 영역 안 0건
- 옛 토큰 (var(--bg) / var(--bg2) / var(--bg3) / var(--bd) / var(--bd2) / var(--acl) / var(--t1) / var(--t2) / var(--t3) / var(--safe) / var(--warn) / var(--danger) / var(--fire) / var(--info)) 변환 영역 안 0건
- ModalWrap / Field / MultiPhotoUpload / EvSelector / TYPE_ICON_COMPONENT 본체 수정 0건
- tailwind.config.js / icons.tsx / 다른 페이지 수정 0건

**커밋 후**: `git status` + `git log -1 --stat` 으로 변경 라인 범위 확인 (라인 16 lucide import + 라인 2022~2109 FaultNewModal 본체 만).
  </action>
  <verify>
    <automated>
cd cha-bio-safety && npx tsc --noEmit 2>&1 | tail -20 && \
echo "--- FaultNewModal 변환 영역 grep gate (라인 2022~2109) ---" && \
sed -n '2022,2109p' src/pages/ElevatorPage.tsx > /tmp/wave3-task1-region.txt && \
echo "  - 인라인 금지 키 (color/background/padding/margin/fontSize/fontWeight/borderRadius/display/flex/grid/gap/width/height — 정적값):" && \
grep -nE "(color|background(Color)?|padding(Left|Right|Top|Bottom)?|margin(Left|Right|Top|Bottom)?|fontSize|fontWeight|borderRadius|display|flex(Direction)?|justifyContent|alignItems|gridTemplateColumns|gap|width|height|textAlign|lineHeight):\s*('[^']*'|\"[^\"]*\"|[0-9]+|var\()" /tmp/wave3-task1-region.txt | grep -vE "position|top|left|right|bottom|transform|zIndex|opacity|background:\s*'linear-gradient" | wc -l && \
echo "  - 9-11px 폰트:" && grep -cE "fontSize:\s*(9|10|11)|text-\[(9|10|11)px\]" /tmp/wave3-task1-region.txt && \
echo "  - 이모지 (🚨/✕/🛗/📦/🔲/↕️/⚠️):" && grep -cE "🚨|✕|🛗|📦|🔲|↕️|⚠️" /tmp/wave3-task1-region.txt && \
echo "  - 옛 토큰 (var(--xxx)):" && grep -cE "var\(--(bg|bg2|bg3|bd|bd2|acl|t1|t2|t3|safe|warn|danger|fire|info)\)" /tmp/wave3-task1-region.txt && \
echo "  - lucide X import 확인:" && grep -nE "^import\s*\{[^}]*X[^}]*\}\s*from\s*'lucide-react'" src/pages/ElevatorPage.tsx && \
echo "  - AlertTriangle 호출 (승객 탑승 ON):" && grep -nE "<AlertTriangle\s+size=\{14\}" /tmp/wave3-task1-region.txt && \
echo "  - EvSelector 호출 보존:" && grep -nE "<EvSelector" /tmp/wave3-task1-region.txt && \
echo "  - TKE_TEL 보존:" && grep -nE "TKE_TEL|tel:18999070" /tmp/wave3-task1-region.txt && \
echo "  - fire 그라디언트 (FaultNew CTA):" && grep -nE "linear-gradient\(135deg.*991b1b.*ef4444\)" /tmp/wave3-task1-region.txt && \
echo "  - 함수 시그니처 보존:" && grep -nE "^function FaultNewModal\(\{\s*elevators,\s*selected,\s*onClose,\s*onSubmit,\s*loading\s*\}" /tmp/wave3-task1-region.txt
    </automated>
  </verify>
  <done>
- TypeScript 컴파일 0 에러
- 변환 영역(2022~2109) 인라인 금지 키 0건 (화이트리스트: position/top/left/right/bottom/transform/zIndex/opacity/background:linear-gradient 제외)
- 변환 영역 9-11px 폰트 0건
- 변환 영역 이모지 0건
- 변환 영역 옛 토큰 0건
- lucide-react import 블록에 X 추가됨 (Task 2/3 가 재사용)
- AlertTriangle (승객 탑승 ON) lucide 아이콘 등장
- EvSelector 호출 1건 보존
- TKE_TEL 보존 (`<a href={TKE_TEL}>` 패턴)
- FaultNew CTA fire 그라디언트 (`linear-gradient(135deg, #991b1b, #ef4444)`) 적용
- 함수 시그니처(FaultNewModal({ elevators, selected, onClose, onSubmit, loading })) 보존
- git commit (예: `feat(260515-iz1): task 1 — FaultNewModal v0.1.1 토큰 + Tailwind 변환`)
  </done>
</task>

<task type="auto">
  <name>Task 2: FaultNewFullscreen 변환 (라인 2112~2213, 풀스크린 + 자체 헤더 + 본문 + 하단 CTA)</name>
  <files>cha-bio-safety/src/pages/ElevatorPage.tsx</files>
  <action>
**범위**: FaultNewFullscreen 함수 본체 (라인 2112~2213) 만 변경. 함수 시그니처 / state / 핸들러 / EvSelector 호출 / TKE_TEL `<a>` 래핑 / 3분할 구조(헤더 / 본문 스크롤 / 하단 고정 CTA) 보존.

**풀스크린 외곽 컨테이너 (라인 2138)**:
- 현재: `<div style={{ position:'fixed', top:0, left:0, right:0, bottom:NAV_H, zIndex:100, background:'var(--bg)', display:'flex', flexDirection:'column', paddingTop:'var(--sat, 44px)', boxSizing:'border-box' }}>`
- 변경: 화이트리스트 인라인(position/top/left/right/bottom 동적 NAV_H/zIndex 100/paddingTop 동적 var(--sat))만 보존 + 나머지 Tailwind:
  - `<div className="fixed flex flex-col bg-surface-base box-border" style={{ top: 0, left: 0, right: 0, bottom: NAV_H, zIndex: 100, paddingTop: 'var(--sat, 44px)' }}>`
  - 또는 Tailwind arbitrary value: `<div className="fixed top-0 left-0 right-0 z-[100] flex flex-col bg-surface-base box-border pt-[var(--sat,44px)]" style={{ bottom: NAV_H }}>` (NAV_H 만 동적 인라인 보존, 나머지 모두 Tailwind. **권장 — NAV_H 만 인라인**)

**자체 헤더 (라인 2140~2147)**:
- 외곽: `<div style={{ flexShrink:0, background:'var(--bg2)', borderBottom:'1px solid var(--bd)', padding:'14px 16px 12px', display:'flex', alignItems:'center', gap:10 }}>` → `<div className="flex-shrink-0 bg-surface-raised border-b border-border-default pt-3.5 pb-3 px-4 flex items-center gap-2.5">`
- 아이콘 박스 (라인 2141): `<div style={{ width:36, height:36, borderRadius:9, background:'rgba(239,68,68,.15)', display:'flex', alignItems:'center', justifyContent:'center', fontSize:20 }}>🚨</div>` →
  - `<div className="w-9 h-9 rounded-md bg-danger-bg flex items-center justify-center"><AlertTriangle size={20} className="text-danger" /></div>`
  - (2B sketch VP3 §6 권위 — danger-bg + AlertTriangle text-danger)
- 타이틀 + 부제 (라인 2142~2145):
  - 외곽 `<div style={{ flex:1 }}>` → `<div className="flex-1">`
  - 타이틀: `<div style={{ fontSize:15, fontWeight:700, color:'var(--t1)' }}>고장 접수</div>` → `<div className="text-body font-bold text-text-primary">고장 접수</div>` (fontSize 15 → 16, text-body)
  - 부제: `<div style={{ fontSize:10, color:'var(--t3)' }}>접수 후 TKE(1899-9070) 자동 연결</div>` → `<div className="text-caption text-text-tertiary">접수 후 TKE(1899-9070) 자동 연결</div>` (fontSize 10 → 12, text-caption 노안)
- 닫기 button (라인 2146): `<button onClick={onClose} style={{ background:'none', border:'none', color:'var(--t3)', cursor:'pointer', fontSize:18 }}>✕</button>` →
  - `<button onClick={onClose} className="bg-none border-none text-text-tertiary cursor-pointer p-1 rounded-md hover:text-text-primary hover:bg-surface-sunken transition"><X size={20} /></button>`
  - (이모지 ✕ → lucide X, hover §6.5 적용)

**본문 스크롤 영역 (라인 2150~2196)**:
- 외곽: `<div style={{ flex:1, minHeight:0, overflowY:'auto', overflowX:'hidden', padding:'16px' }}>` → `<div className="flex-1 min-h-0 overflow-y-auto overflow-x-hidden p-4">`
- 안쪽 column: `<div style={{ display:'flex', flexDirection:'column', gap:16 }}>` → `<div className="flex flex-col gap-4">`
- **EvSelector 호출 보존 (라인 2153~2158)**: 한 줄도 변경 X. Wave 2 토큰 자동 반영.
- 조건부 본문 (`{elevatorId && (...)}` 라인 2160) + `<>` Fragment 보존.
- 발생 일시 Field (라인 2162~2164): Task 1 과 동일 패턴 (Field 호출 보존 + `style={inputSt}` → Tailwind className)
- 발생 층 + 승객 탑승 (라인 2166~2187): Task 1 과 동일 패턴
  - 외곽 `<div style={{ display:'flex', gap:8, alignItems:'flex-end' }}>` → `<div className="flex gap-2 items-end">`
  - `<Field label="발생 층" style={{ flex:1 }}>` 보존
  - select `style={inputSt}` 제거 → Tailwind className
  - 승객 탑승 토글: Task 1 의 패턴 그대로 (ON: bg-danger-bg + AlertTriangle / OFF: bg-surface-sunken + 미탑승)
- 증상 textarea (라인 2189~2191): Task 1 패턴 (rows={4} 만 다름, 보존)
- MultiPhotoUpload 호출 (라인 2193): `<MultiPhotoUpload label="증상 사진" keys={photoKeys} setKeys={setPhotoKeys} />` 그대로

**하단 고정 CTA 영역 (라인 2199~2210)**:
- 외곽: `<div style={{ flexShrink:0, padding:'12px 16px', background:'var(--bg2)', borderTop:'1px solid var(--bd)' }}>` → `<div className="flex-shrink-0 p-3 px-4 bg-surface-raised border-t border-border-default">`
- `<a href={TKE_TEL} style={{ textDecoration:'none', display:'block' }}>` → `<a href={TKE_TEL} className="no-underline block">` (TKE_TEL 보존)
- `<button onClick={handleSubmit} disabled={...} style={{ ...primaryBtnSt, background:'linear-gradient(135deg,#991b1b,#ef4444)', opacity:..., width:'100%' }}>` →
  - `...primaryBtnSt` 전개 제거 → Tailwind utility 명시 + fire 그라디언트 보존:
  - `<button onClick={handleSubmit} disabled={!elevatorId || !symptoms.trim() || loading} className="w-full h-[48px] rounded-md border-none cursor-pointer text-body font-bold text-text-on-accent transition hover:-translate-y-px disabled:opacity-50 disabled:cursor-not-allowed" style={{ background: 'linear-gradient(135deg,#991b1b,#ef4444)' }}>` (배경 그라디언트 인라인 화이트리스트 — §6.4 fire)
- **텍스트 변경**: `{loading ? '접수 중...' : '🚨 고장 접수 (TKE 자동 연결)'}` → `{loading ? '접수 중...' : '고장 접수 (TKE 자동 연결)'}` (이모지 🚨 제거 — 2B sketch §5 권위, 헤더 아이콘 박스가 위험 알림 역할)

**금지사항** (Task 1 과 동일):
- 함수 시그니처 / state / 핸들러 / EvSelector 호출 / TKE_TEL `<a>` 래핑 변경 0건
- 이모지(🚨 / ✕ / 🛗 / 📦 / 🔲 / ↕️ / ⚠️) 변환 영역 안 0건
- 9-11px 폰트 변환 영역 안 0건
- 옛 토큰 변환 영역 안 0건
- 인라인 금지 키 변환 영역 안 0건 (화이트리스트: position/top/left/right/bottom/transform/zIndex/opacity/paddingTop:var(--sat)/background:linear-gradient 만 예외)
- ModalWrap / Field / MultiPhotoUpload / EvSelector 본체 수정 0건
- tailwind.config.js / icons.tsx / 다른 페이지 수정 0건

**커밋 후**: `git log -1 --stat` 확인 (라인 2112~2213 만 변경).
  </action>
  <verify>
    <automated>
cd cha-bio-safety && npx tsc --noEmit 2>&1 | tail -20 && \
echo "--- FaultNewFullscreen 변환 영역 grep gate (라인 2112~2213) ---" && \
sed -n '2112,2213p' src/pages/ElevatorPage.tsx > /tmp/wave3-task2-region.txt && \
echo "  - 인라인 금지 키 (화이트리스트: position/top/left/right/bottom/transform/zIndex/opacity/paddingTop:var(--sat)/background:linear-gradient 제외):" && \
grep -nE "(color|background(Color)?|padding(Left|Right|Top|Bottom)?|margin(Left|Right|Top|Bottom)?|fontSize|fontWeight|borderRadius|display|flex(Direction)?|justifyContent|alignItems|gridTemplateColumns|gap|width|height|textAlign|lineHeight):\s*('[^']*'|\"[^\"]*\"|[0-9]+|var\()" /tmp/wave3-task2-region.txt | grep -vE "position|top:\s*0|left:\s*0|right:\s*0|bottom:\s*NAV_H|transform|zIndex:\s*100|opacity|paddingTop:\s*'var\(--sat|background:\s*'linear-gradient" | wc -l && \
echo "  - 9-11px 폰트:" && grep -cE "fontSize:\s*(9|10|11)|text-\[(9|10|11)px\]" /tmp/wave3-task2-region.txt && \
echo "  - 이모지 (🚨/✕/🛗/📦/🔲/↕️/⚠️):" && grep -cE "🚨|✕|🛗|📦|🔲|↕️|⚠️" /tmp/wave3-task2-region.txt && \
echo "  - 옛 토큰 (var(--xxx)):" && grep -cE "var\(--(bg|bg2|bg3|bd|bd2|acl|t1|t2|t3|safe|warn|danger|fire|info)\)" /tmp/wave3-task2-region.txt | head -1 && \
echo "  - AlertTriangle size=20 (헤더 아이콘):" && grep -nE "<AlertTriangle\s+size=\{20\}" /tmp/wave3-task2-region.txt && \
echo "  - X size=20 (닫기):" && grep -nE "<X\s+size=\{20\}" /tmp/wave3-task2-region.txt && \
echo "  - 풀스크린 NAV_H 인라인 화이트리스트:" && grep -nE "bottom:\s*NAV_H|zIndex:\s*100" /tmp/wave3-task2-region.txt && \
echo "  - EvSelector 호출 보존:" && grep -nE "<EvSelector" /tmp/wave3-task2-region.txt && \
echo "  - TKE_TEL 보존:" && grep -nE "TKE_TEL|tel:18999070" /tmp/wave3-task2-region.txt && \
echo "  - fire 그라디언트 (FaultNew CTA):" && grep -nE "linear-gradient\(135deg.*991b1b.*ef4444\)" /tmp/wave3-task2-region.txt && \
echo "  - CTA 텍스트 이모지 제거 ('🚨 고장 접수' 등장 0건):" && grep -cE "🚨\s*고장 접수" /tmp/wave3-task2-region.txt && \
echo "  - 함수 시그니처 보존:" && grep -nE "^function FaultNewFullscreen\(\{\s*elevators,\s*onClose,\s*onSubmit,\s*loading\s*\}" /tmp/wave3-task2-region.txt
    </automated>
  </verify>
  <done>
- TypeScript 컴파일 0 에러
- 변환 영역(2112~2213) 인라인 금지 키 0건 (화이트리스트 6개만 허용)
- 변환 영역 9-11px 폰트 0건
- 변환 영역 이모지 0건 (🚨 헤더 + 🚨 CTA 텍스트 + ✕ 닫기 모두 제거)
- 변환 영역 옛 토큰 0건
- AlertTriangle size=20 (헤더 아이콘 박스) 적용
- X size=20 (닫기 버튼) 적용
- bottom: NAV_H + zIndex: 100 화이트리스트 인라인 보존 (풀스크린 패턴)
- EvSelector 호출 1건 보존
- TKE_TEL 보존
- FaultNew CTA fire 그라디언트 보존 (`linear-gradient(135deg,#991b1b,#ef4444)`)
- CTA 텍스트 '🚨 고장 접수 (TKE 자동 연결)' → '고장 접수 (TKE 자동 연결)' 변경 (이모지 0건)
- 함수 시그니처(FaultNewFullscreen({ elevators, onClose, onSubmit, loading })) 보존
- git commit (예: `feat(260515-iz1): task 2 — FaultNewFullscreen v0.1.1 토큰 + Tailwind 변환`)
  </done>
</task>

<task type="auto">
  <name>Task 3: FaultResolveModal 변환 (라인 2216~2253, ModalWrap 안 수리 완료 처리)</name>
  <files>cha-bio-safety/src/pages/ElevatorPage.tsx</files>
  <action>
**범위**: FaultResolveModal 함수 본체 (라인 2216~2253) 만 변경. 함수 시그니처 / state / pureSymptoms 정규식 / onSubmit 페이로드 / `<MultiPhotoUpload>` 호출 / ModalWrap 호출 보존.

**ModalWrap 호출 보존 (라인 2227)**: `<ModalWrap title="수리 완료 처리" onClose={onClose}>` 그대로.

**본문 컨테이너 (라인 2228)**: `<div style={{ display:'flex', flexDirection:'column', gap:12 }}>` → `<div className="flex flex-col gap-3">`

**고장 정보 카드 (라인 2229~2232)** — 2B sketch VP2/VP4 §7 권위:
- 현재: `<div style={{ background:'var(--bg3)', borderRadius:10, padding:'10px 13px', fontSize:11, color:'var(--t2)' }}>{TYPE_ICON[fault.elevator_type]} {fault.elevator_number}호기<br/><span style={{ color:'var(--t1)' }}>{pureSymptoms}</span></div>`
- 변경:
```tsx
{(() => {
  const TypeIcon = TYPE_ICON_COMPONENT[fault.elevator_type]
  return (
    <div className="bg-surface-sunken rounded-lg p-3">
      <div className="flex items-center gap-2">
        {TypeIcon && <TypeIcon size={20} className="text-text-secondary" />}
        <span className="text-body-sm font-bold text-text-primary">{fault.elevator_number}호기</span>
      </div>
      <div className="text-label text-text-secondary mt-1.5">{pureSymptoms}</div>
    </div>
  )
})()}
```
또는 더 단순하게 const 를 함수 본체 윗단에 빼고 카드만 마크업:
```tsx
const TypeIcon = TYPE_ICON_COMPONENT[fault.elevator_type]
// ... return 안에:
<div className="bg-surface-sunken rounded-lg p-3">
  <div className="flex items-center gap-2">
    {TypeIcon && <TypeIcon size={20} className="text-text-secondary" />}
    <span className="text-body-sm font-bold text-text-primary">{fault.elevator_number}호기</span>
  </div>
  <div className="text-label text-text-secondary mt-1.5">{pureSymptoms}</div>
</div>
```
- TYPE_ICON 이모지 객체 lookup 제거 → TYPE_ICON_COMPONENT 매퍼 사용 (Wave 1 신설, 라인 197). passenger=ElevatorIcon / cargo=Package / dumbwaiter=UtensilsCrossed / escalator=MoveDiagonal — 회색 통일 text-text-secondary (sketch VP4 권위)
- fontSize 11 → 14 (text-body-sm) 호기 라벨 / 13 (text-label) pureSymptoms — 노안 친화
- `<br/>` 제거 → mt-1.5 (6px) 줄바꿈
- `<span style={{ color:'var(--t1)' }}>` → 별도 `<div className="text-label text-text-secondary mt-1.5">` 로 분리

**Field 호출들 (라인 2233~2241)** 보존:
- `<Field label="수리 업체">` / `<Field label="수리 완료 일시">` / `<Field label="수리 내용">` 모두 보존
- 안의 input/textarea 의 `style={inputSt}` / `style={{ ...inputSt, resize:'none' }}` 만 Tailwind className 으로 교체 (Task 1 패턴 동일):
  - 수리 업체 input: `className="w-full h-[42px] px-3 bg-surface-sunken border border-border-default rounded-md text-text-primary text-body-sm focus:border-border-strong outline-none transition"`
  - 수리 완료 일시 input: 동일 className (datetime-local)
  - 수리 내용 textarea: `className="w-full px-3 py-2.5 bg-surface-sunken border border-border-default rounded-md text-text-primary text-body-sm focus:border-border-strong outline-none transition resize-none"` (rows={3} 보존)

**MultiPhotoUpload 호출 보존 (라인 2242)**: `<MultiPhotoUpload label="수리 사진" keys={repairPhotoKeys} setKeys={setRepairPhotoKeys} />` 그대로.

**CTA "수리 완료" (라인 2243~2249)** — 2B sketch §5 권위 = accent 단색 (그라디언트 X):
- 현재: `<button onClick={...} disabled={!repairDetail.trim()||loading} style={{ ...primaryBtnSt, opacity:(!repairDetail.trim()||loading)?0.5:1 }}>`
- 변경: `<button onClick={() => onSubmit({ id:fault.id, repairCompany, repairedAt:repairedAt+':00', repairDetail, repairPhotoKeys })} disabled={!repairDetail.trim()||loading} className="w-full h-[48px] rounded-md border-none cursor-pointer text-body font-bold bg-accent text-text-on-accent transition hover:-translate-y-px disabled:opacity-50 disabled:cursor-not-allowed">`
- 텍스트 보존: `{loading ? '처리 중...' : '수리 완료'}`
- onSubmit 페이로드(id/repairCompany/repairedAt/repairDetail/repairPhotoKeys) 한 글자도 변경 X

**금지사항** (Task 1/2 와 동일):
- 함수 시그니처 / state / pureSymptoms 정규식 / onSubmit 페이로드 변경 0건
- TYPE_ICON_COMPONENT 매퍼 본체 수정 X (Wave 1 신설, 라인 197 사용만)
- 이모지(🛗 / 📦 / 🔲 / ↕️ / ⚠️ / ✕) 변환 영역 안 0건
- 9-11px 폰트 변환 영역 안 0건
- 옛 토큰 변환 영역 안 0건
- 인라인 금지 키 변환 영역 안 0건 (화이트리스트: opacity 동적값 만)
- ModalWrap / Field / MultiPhotoUpload / TYPE_ICON_COMPONENT 본체 수정 0건
- FaultResolve CTA = accent 단색 (그라디언트 X — §6.4 기본 정책)
- tailwind.config.js / icons.tsx / 다른 페이지 수정 0건

**커밋 후**: `git log -1 --stat` 확인 (라인 2216~2253 만 변경).
  </action>
  <verify>
    <automated>
cd cha-bio-safety && npx tsc --noEmit 2>&1 | tail -20 && \
echo "--- FaultResolveModal 변환 영역 grep gate (라인 2216~2253 전후 확장 — 2216~2270) ---" && \
sed -n '2216,2270p' src/pages/ElevatorPage.tsx > /tmp/wave3-task3-region.txt && \
echo "  - 인라인 금지 키 (화이트리스트: opacity 동적값만):" && \
grep -nE "(color|background(Color)?|padding(Left|Right|Top|Bottom)?|margin(Left|Right|Top|Bottom)?|fontSize|fontWeight|borderRadius|display|flex(Direction)?|justifyContent|alignItems|gridTemplateColumns|gap|width|height|textAlign|lineHeight):\s*('[^']*'|\"[^\"]*\"|[0-9]+|var\()" /tmp/wave3-task3-region.txt | grep -vE "opacity|^\s*//" | wc -l && \
echo "  - 9-11px 폰트:" && grep -cE "fontSize:\s*(9|10|11)|text-\[(9|10|11)px\]" /tmp/wave3-task3-region.txt && \
echo "  - 이모지 (🛗/📦/🔲/↕️/⚠️/✕):" && grep -cE "🛗|📦|🔲|↕️|⚠️|✕" /tmp/wave3-task3-region.txt && \
echo "  - 옛 토큰 (var(--xxx)):" && grep -cE "var\(--(bg|bg2|bg3|bd|bd2|acl|t1|t2|t3|safe|warn|danger|fire|info)\)" /tmp/wave3-task3-region.txt && \
echo "  - TYPE_ICON_COMPONENT 매퍼 사용 (FaultResolve 고장 정보 카드):" && grep -nE "TYPE_ICON_COMPONENT\[fault\.elevator_type\]" /tmp/wave3-task3-region.txt && \
echo "  - TYPE_ICON 이모지 객체 lookup 제거 ([fault.elevator_type] 패턴이 TYPE_ICON_COMPONENT 만 매치):" && grep -cE "TYPE_ICON\[fault\.elevator_type\]" /tmp/wave3-task3-region.txt && \
echo "  - ModalWrap 호출 보존:" && grep -nE "<ModalWrap title=\"수리 완료 처리\"" /tmp/wave3-task3-region.txt && \
echo "  - MultiPhotoUpload 호출 보존:" && grep -nE "<MultiPhotoUpload label=\"수리 사진\"" /tmp/wave3-task3-region.txt && \
echo "  - FaultResolve CTA accent (그라디언트 0건):" && grep -nE "bg-accent\s+text-text-on-accent" /tmp/wave3-task3-region.txt && \
echo "  - FaultResolve CTA fire 그라디언트 0건 (오염 없음):" && grep -cE "linear-gradient\(135deg.*991b1b" /tmp/wave3-task3-region.txt && \
echo "  - pureSymptoms 정규식 보존:" && grep -nE "fault\.symptoms\.replace\(/\^\\\\\[" /tmp/wave3-task3-region.txt && \
echo "  - onSubmit 페이로드 보존:" && grep -nE "id:fault\.id,\s*repairCompany,\s*repairedAt:repairedAt\+':00',\s*repairDetail,\s*repairPhotoKeys" /tmp/wave3-task3-region.txt && \
echo "  - 함수 시그니처 보존:" && grep -nE "^function FaultResolveModal\(\{\s*fault,\s*onClose,\s*onSubmit,\s*loading\s*\}" /tmp/wave3-task3-region.txt
    </automated>
  </verify>
  <done>
- TypeScript 컴파일 0 에러
- 변환 영역(2216~2253) 인라인 금지 키 0건 (화이트리스트: opacity 동적값만 허용)
- 변환 영역 9-11px 폰트 0건
- 변환 영역 이모지 0건
- 변환 영역 옛 토큰 0건
- TYPE_ICON_COMPONENT 매퍼 사용 (Wave 1 매퍼 재사용)
- TYPE_ICON[fault.elevator_type] 이모지 lookup 0건 (TYPE_ICON_COMPONENT 로 교체됨)
- ModalWrap title="수리 완료 처리" 호출 보존
- MultiPhotoUpload label="수리 사진" 호출 보존
- FaultResolve CTA = `bg-accent text-text-on-accent` (단색, 그라디언트 0건 — §6.4 기본 정책)
- pureSymptoms 정규식 (`/^\[[^\]]+\]\s*/` + `/\[승객탑승\]\s*/`) 보존
- onSubmit 페이로드 (id/repairCompany/repairedAt:repairedAt+':00'/repairDetail/repairPhotoKeys) 보존
- 함수 시그니처(FaultResolveModal({ fault, onClose, onSubmit, loading })) 보존
- git commit (예: `feat(260515-iz1): task 3 — FaultResolveModal v0.1.1 토큰 + Tailwind 변환`)
  </done>
</task>

<task type="auto">
  <name>Task 4: 회귀 검증 — npm build + 변환 영역 grep gate + 보존 영역 sentinel + SUMMARY.md</name>
  <files>cha-bio-safety/src/pages/ElevatorPage.tsx, .planning/quick/260515-iz1-redesign-07-elevator-tsx-wave-3-fault-3-/260515-iz1-SUMMARY.md</files>
  <action>
**범위**: 코드 변경 0건. Task 1~3 의 누적 검증만 수행한 후 SUMMARY.md 작성.

**1. npm build 검증**:
```
cd cha-bio-safety && npm run build 2>&1 | tail -20
```
- Exit code 0 / `ElevatorPage-*.js` chunk 등장 / 에러 0건 확인

**2. Wave 3 변환 영역 전체 누적 grep gate** (라인 2022~2253 = FaultNewModal + FaultNewFullscreen + FaultResolveModal 합):
```
sed -n '2022,2253p' src/pages/ElevatorPage.tsx > /tmp/wave3-all.txt
```

검증 항목 (각 0건 또는 명시 카운트):
- 인라인 금지 키 (색/배경/패딩/마진/폰트/디스플레이/플렉스/그리드/사이즈/텍스트 정렬 정적값) → 화이트리스트(position/top/left/right/bottom/transform/zIndex/opacity/paddingTop:var(--sat)/background:linear-gradient/textDecoration) 제외하고 **0건**
- 9-11px 폰트 (`fontSize: 9|10|11` 또는 `text-[9|10|11px]`) → **0건**
- 본문 이모지 (🚨/✕/🛗/📦/🔲/↕️/⚠️) → **0건**
- 옛 토큰 (var(--bg/bg2/bg3/bd/bd2/acl/t1/t2/t3/safe/warn/danger/fire/info)) → **0건**

**3. 보존 영역 sentinel** (Wave 3 변환 영역 밖, 한 줄도 변경 0건 확인):
- ModalWrap 함수 정의(라인 2528) 보존: `grep -n "^function ModalWrap" src/pages/ElevatorPage.tsx` → 1건 (라인 2528 부근)
- Field 함수 정의(라인 2547) 보존: `grep -n "^function Field" src/pages/ElevatorPage.tsx` → 1건
- EmptyState 함수 정의 보존: `grep -n "^function EmptyState" src/pages/ElevatorPage.tsx` → 1건
- MultiPhotoUpload 함수 정의 보존: `grep -n "^function MultiPhotoUpload" src/pages/ElevatorPage.tsx` → 1건
- EvSelector 함수 정의 보존 (Wave 2): `grep -n "^function EvSelector" src/pages/ElevatorPage.tsx` → 1건
- EsBtn 함수 정의 보존 (Wave 2): `grep -n "^function EsBtn" src/pages/ElevatorPage.tsx` → 1건
- EsNodeMap 함수 정의 보존 (Wave 2): `grep -n "^function EsNodeMap" src/pages/ElevatorPage.tsx` → 1건
- InspectModal 함수 정의 보존 (Wave 4+): `grep -n "^function InspectModal" src/pages/ElevatorPage.tsx` → 1건
- RepairNewModal 함수 정의 보존 (Wave 4+): `grep -n "^function RepairNewModal" src/pages/ElevatorPage.tsx` → 1건
- EvDetailModal 함수 정의 보존 (Wave 4+): `grep -n "^function EvDetailModal" src/pages/ElevatorPage.tsx` → 1건
- TYPE_ICON_COMPONENT 매퍼 정의 보존 (Wave 1): `grep -n "TYPE_ICON_COMPONENT.*Record" src/pages/ElevatorPage.tsx` → 1건
- TKE_TEL 상수 보존: `grep -n "const TKE_TEL" src/pages/ElevatorPage.tsx` → 1건
- EV_GROUPS_FAULT / ES_NODES_FAULT 데이터 상수 보존: `grep -cE "(EV_GROUPS_FAULT|ES_NODES_FAULT)" src/pages/ElevatorPage.tsx` → 4건 이상 (정의 + Fault 모달 2회 + 기타)
- 사용처 호출 보존: `grep -nE "<FaultNewModal\s+|<FaultNewFullscreen\s+|<FaultResolveModal\s+" src/pages/ElevatorPage.tsx` → 메인 render 안 3건 등장
- mutation 호출 보존: `grep -cE "submitFault\.mutate|resolveFault\.mutate" src/pages/ElevatorPage.tsx` → 2건 이상

**4. 다른 파일 변경 0건**:
- `git diff --stat HEAD~3 HEAD` → cha-bio-safety/src/pages/ElevatorPage.tsx 1 파일만 변경 확인
- icons.tsx / tailwind.config.js / 다른 페이지 / API / hooks / utils 변경 0건

**5. SUMMARY.md 작성**:
경로: `.planning/quick/260515-iz1-redesign-07-elevator-tsx-wave-3-fault-3-/260515-iz1-SUMMARY.md`

포함 내용:
- frontmatter (quick_id / slug / status: complete / date / commit / tasks_completed: 4 / tasks_total: 4 / files_modified / tags)
- One-liner
- Wave 3 변환 영역 (라인 범위 + 변경 라인 수)
- 추가된 lucide import (X)
- v0.1.1 토큰 매핑 표 (영역별 옛 토큰 → 새 Tailwind utility)
- 2B sketch 결정 반영 표
- grep gate 결과 표 (변환 영역 한정)
- 보존 영역 sentinel 결과 표
- npm run build 결과 (built in N초 / ElevatorPage-*.js chunk 사이즈)
- 커밋 이력 표 (Task 1~3 각 커밋)
- Deviations from Plan (None 또는 명시)
- Known Stubs (None)
- Threat Flags (None — UI 렌더링 전용)
- 다음 Wave 권장 (Wave 4 = InspectModal cleanup 또는 RepairNewModal 변환, 2C sketch 권위)
- Self-Check (5+ 항목 PASS 확인)

**6. SUMMARY 커밋**:
```
git add .planning/quick/260515-iz1-redesign-07-elevator-tsx-wave-3-fault-3-/260515-iz1-SUMMARY.md
git commit -m "docs(260515-iz1): Wave 3 SUMMARY — FaultNewModal/Fullscreen/Resolve 변환 완료"
```
  </action>
  <verify>
    <automated>
cd cha-bio-safety && npm run build 2>&1 | tail -10 && \
echo "--- 전체 변환 영역 누적 grep gate (라인 2022~2253) ---" && \
sed -n '2022,2253p' src/pages/ElevatorPage.tsx > /tmp/wave3-all.txt && \
echo "  - 인라인 금지 키 (화이트리스트 9개 제외):" && \
grep -nE "(color|background(Color)?|padding(Left|Right|Top|Bottom)?|margin(Left|Right|Top|Bottom)?|fontSize|fontWeight|borderRadius|display|flex(Direction)?|justifyContent|alignItems|gridTemplateColumns|gap|width|height|textAlign|lineHeight):\s*('[^']*'|\"[^\"]*\"|[0-9]+|var\()" /tmp/wave3-all.txt | grep -vE "position|top:\s*0|left:\s*0|right:\s*0|bottom:\s*NAV_H|transform|zIndex:\s*100|opacity|paddingTop:\s*'var\(--sat|background:\s*'linear-gradient|textDecoration" | wc -l && \
echo "  - 9-11px 폰트:" && grep -cE "fontSize:\s*(9|10|11)|text-\[(9|10|11)px\]" /tmp/wave3-all.txt && \
echo "  - 본문 이모지 (🚨/✕/🛗/📦/🔲/↕️/⚠️):" && grep -cE "🚨|✕|🛗|📦|🔲|↕️|⚠️" /tmp/wave3-all.txt && \
echo "  - 옛 토큰:" && grep -cE "var\(--(bg|bg2|bg3|bd|bd2|acl|t1|t2|t3|safe|warn|danger|fire|info)\)" /tmp/wave3-all.txt && \
echo "--- 보존 영역 sentinel ---" && \
echo "  - 3 모달 함수 정의(FaultNewModal/FaultNewFullscreen/FaultResolveModal):" && grep -nE "^function (FaultNewModal|FaultNewFullscreen|FaultResolveModal)" src/pages/ElevatorPage.tsx | wc -l && \
echo "  - 헬퍼 함수 정의(ModalWrap/Field/EmptyState/MultiPhotoUpload) 보존:" && grep -nE "^function (ModalWrap|Field|EmptyState|MultiPhotoUpload)" src/pages/ElevatorPage.tsx | wc -l && \
echo "  - Wave 1+2 헬퍼(EvSelector/EsBtn/EsNodeMap) 보존:" && grep -nE "^function (EvSelector|EsBtn|EsNodeMap)" src/pages/ElevatorPage.tsx | wc -l && \
echo "  - Wave 4+ 모달(InspectModal/RepairNewModal/EvDetailModal) 보존:" && grep -nE "^function (InspectModal|RepairNewModal|EvDetailModal)" src/pages/ElevatorPage.tsx | wc -l && \
echo "  - TYPE_ICON_COMPONENT 매퍼 정의 보존:" && grep -cE "^const TYPE_ICON_COMPONENT" src/pages/ElevatorPage.tsx && \
echo "  - TKE_TEL 상수 보존:" && grep -cE "^const TKE_TEL" src/pages/ElevatorPage.tsx && \
echo "  - 사용처 호출 보존(메인 render):" && grep -cE "<FaultNewModal\s+|<FaultNewFullscreen\s+|<FaultResolveModal\s+" src/pages/ElevatorPage.tsx && \
echo "  - mutation 호출 보존:" && grep -cE "submitFault\.mutate|resolveFault\.mutate" src/pages/ElevatorPage.tsx && \
echo "--- 변경 파일 검증 ---" && \
cd .. && git diff --stat HEAD~3 HEAD 2>&1 | tail -10 && \
echo "--- SUMMARY.md 존재 확인 ---" && \
ls -la .planning/quick/260515-iz1-redesign-07-elevator-tsx-wave-3-fault-3-/260515-iz1-SUMMARY.md 2>&1
    </automated>
  </verify>
  <done>
- npm run build PASS (built in N초, ElevatorPage-*.js chunk 사이즈 reasonable)
- 변환 영역(2022~2253) 인라인 금지 키 0건 (화이트리스트 9개 제외)
- 변환 영역 9-11px 폰트 0건
- 변환 영역 본문 이모지 0건 (🚨/✕/🛗/📦/🔲/↕️/⚠️ 모두 변환됨)
- 변환 영역 옛 토큰 0건
- 3 모달 함수 정의 보존 (FaultNewModal/FaultNewFullscreen/FaultResolveModal 각 1건)
- 헬퍼 함수 정의 4건 보존 (ModalWrap/Field/EmptyState/MultiPhotoUpload)
- Wave 1+2 헬퍼 3건 보존 (EvSelector/EsBtn/EsNodeMap)
- Wave 4+ 모달 3건 보존 (InspectModal/RepairNewModal/EvDetailModal)
- TYPE_ICON_COMPONENT 매퍼 + TKE_TEL 상수 보존
- 사용처 호출(메인 render `<FaultNewModal />`, `<FaultNewFullscreen />`, `<FaultResolveModal />`) 3건 보존
- submitFault.mutate / resolveFault.mutate mutation 호출 2건 이상 보존
- 변경 파일 = `cha-bio-safety/src/pages/ElevatorPage.tsx` 1건 만 (icons.tsx / tailwind.config.js / 다른 페이지 변경 0건)
- SUMMARY.md 작성 완료
- 최종 커밋 (예: `docs(260515-iz1): Wave 3 SUMMARY — Fault 모달 3종 변환 완료`)
  </done>
</task>

</tasks>

<verification>
**Phase-level 회귀 검증** (Task 4 가 누적 수행, 본 섹션은 요약):

1. **TypeScript 컴파일**: `npx tsc --noEmit` 0 에러
2. **빌드**: `npm run build` PASS + ElevatorPage chunk 생성
3. **변환 영역 grep gate** (라인 2022~2253):
   - 인라인 금지 키 0건 (화이트리스트 9개 — position/top/left/right/bottom/transform/zIndex/opacity/paddingTop:var(--sat)/background:linear-gradient/textDecoration 만 예외)
   - 9-11px 폰트 0건 (Tailwind text-caption=12 이상)
   - 본문 이모지(🚨/✕/🛗/📦/🔲/↕️/⚠️) 0건
   - 옛 토큰(var(--bg|bg2|bg3|bd|bd2|acl|t1|t2|t3|safe|warn|danger|fire|info)) 0건
4. **보존 영역 sentinel**:
   - 3 모달 함수 정의 보존
   - 헬퍼 함수 (ModalWrap/Field/EmptyState/MultiPhotoUpload) 본체 0줄 수정
   - Wave 1+2 헬퍼 (EvSelector/EsBtn/EsNodeMap) 0줄 수정
   - Wave 4+ 모달 (InspectModal/RepairNewModal/EvDetailModal) 0줄 수정
   - TYPE_ICON_COMPONENT / TKE_TEL / EV_GROUPS_FAULT / ES_NODES_FAULT 상수 보존
   - 사용처 호출 (`<FaultNewModal />`, `<FaultNewFullscreen />`, `<FaultResolveModal />` + submitFault.mutate / resolveFault.mutate) 변경 0건
5. **변경 파일**: cha-bio-safety/src/pages/ElevatorPage.tsx 1 파일만 변경 (icons.tsx / tailwind.config.js / 다른 페이지 / hooks / utils / API / D1 변경 0건)
</verification>

<success_criteria>
- 3 모달 본체(라인 2022~2253) v0.1.1 토큰 + Tailwind only 변환 완료
- 비즈니스 로직 100% 보존 (state / 핸들러 / EvSelector 호출 / pureSymptoms 정규식 / onSubmit 페이로드 / TKE_TEL 자동 다이얼)
- props 시그니처 보존 (3 함수 시그니처 한 글자도 변경 0건)
- 사용처 호출 보존 (`<FaultNewModal />`, `<FaultNewFullscreen />`, `<FaultResolveModal />` + submitFault.mutate / resolveFault.mutate 변경 0건)
- 보존 영역 (ModalWrap / Field / MultiPhotoUpload / EvSelector / EmptyState / TYPE_ICON_COMPONENT / Wave 4+ 모달) 본체 0줄 수정
- 변환 영역 grep gate: 인라인 금지 키 0건 / 9-11px 폰트 0건 / 본문 이모지 0건 / 옛 토큰 0건
- lucide-react import 블록에 X 추가 (Wave 1+2 의 8개 + X = 9개 import)
- FaultNewModal CTA = fire 그라디언트 (`linear-gradient(135deg,#991b1b,#ef4444)`)
- FaultNewFullscreen CTA = fire 그라디언트 + 텍스트 이모지 🚨 제거
- FaultResolveModal CTA = accent 단색 (그라디언트 X)
- 승객 탑승 토글 = `<AlertTriangle size={14} />` + 텍스트 (이모지 🚨 제거)
- FaultNewFullscreen 자체 헤더 = `<AlertTriangle size={20} className="text-danger" />` 박스 + `<X size={20} />` 닫기
- FaultResolveModal 고장 정보 카드 = TYPE_ICON_COMPONENT 매퍼 사용 (passenger/cargo/dumbwaiter/escalator 4타입)
- TypeScript 0 에러 / npm run build PASS
- 변경 파일 = cha-bio-safety/src/pages/ElevatorPage.tsx 1건만
- SUMMARY.md 작성 + 커밋 완료
</success_criteria>

<output>
After completion, create `.planning/quick/260515-iz1-redesign-07-elevator-tsx-wave-3-fault-3-/260515-iz1-SUMMARY.md` (Task 4 가 수행).

SUMMARY 포함:
- frontmatter (quick_id / slug / status: complete / date / commit / tasks_completed: 4 / tasks_total: 4 / files_modified / tags)
- One-liner: "Fault 모달 3종(FaultNewModal + FaultNewFullscreen + FaultResolveModal) v0.1.1 토큰 + Tailwind only 변환 — 이모지 7종 lucide 교체, fire/danger 색 의미 분리, CTA 그라디언트 정책(FaultNew=fire / FaultResolve=accent) 시각화, 9-11px 폰트 0건."
- Wave 3 변환 영역 (라인 2022~2253 + lucide X import)
- v0.1.1 토큰 매핑 표 / 2B sketch 결정 반영 표
- grep gate 결과 표 (변환 영역 한정 + 보존 영역 sentinel 결과)
- npm run build 결과
- 커밋 이력 표 (Task 1~3 + SUMMARY 4 commits)
- Deviations / Known Stubs / Threat Flags
- 다음 Wave 권장 (Wave 4 = RepairNewModal 변환 또는 InspectModal cleanup, 2C sketch 권위)
- Self-Check PASSED
</output>
