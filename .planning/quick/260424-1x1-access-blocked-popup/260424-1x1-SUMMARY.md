---
quick: 260424-1x1
title: 접근불가 개소 자동 스킵 대신 팝업 노출
date: 2026-04-23
worktree: agent-a5aff193
base: f4cd81bb
commit: 44463c4
status: completed
---

# Quick 260424-1x1: 접근불가 개소 자동 스킵 대신 팝업 노출

## 배경

사용자 요청:
> "접근 불가 개소를 자동 스킵하지 말고, 개소명 카드 아래로 '접근 불가 개소입니다'라는
> 팝업을 띄우고 확인이나 이전/이후 개소로 이동 버튼을 누르거나 스와이프하면
> 스킵이 되게 해야 할 것 같음."

## 적용 카테고리 (DB 조사 결과)

`cp.description` 패턴별로 현재 접근불가 CP 분포:

| 패턴                       | 카테고리 | CP 개수 | defaultResult | 기존 처리                     |
| -------------------------- | -------- | ------- | ------------- | ----------------------------- |
| `description = '접근불가'` | 완강기   | 9개     | 없음          | 모달 open 시 자동 normal 제출 |
| `description ~ '[접근불가]'` | 유도등   | 2개     | 'normal'      | defaultResult 뱃지 + 피커 제외 |

신규 팝업 UX 는 주로 **완강기 접근불가 9개** 에 적용된다. 유도등 `[접근불가]`
은 이미 `cp.defaultResult` 분기로 처리 중이며 pendingCPs 필터에서 `defaultResult`
제외 조건이 그대로 남아 있어 거기에는 변화 없음.

## 변경 사항

### 신규 파일: `src/components/AccessBlockedPopup.tsx`

`InspectionRevisitPopup` 과 동일한 부분 오버레이 스타일(position:absolute;
inset:0; zIndex:10) 로 아이콘 🚫 + "접근 불가 개소입니다. 점검 기록 없이
다음 개소로 이동합니다." + "확인" 버튼. 부모 박스는 반드시 position:relative.

### `src/pages/InspectionPage.tsx`

1. **Import 추가**: `AccessBlockedPopup`.
2. **pendingCPs 필터 (line ~2896+)**: `if (cp.description?.includes('접근불가')) return false`
   제거. 접근불가 cp 도 피커에 포함.
3. **초기 포커스 `firstPending` (line ~2918+)**: `!cp.description?.includes('접근불가')`
   조건 추가 — 실제 점검 대상 먼저 보여주기 위함.
4. **`isAccessBlocked` 플래그 신설**: `!!currentSelCP?.description?.includes('접근불가')`.
5. **`useInspectionRevisitPopup` 호출부 (line ~2931+)**: `checkpointId: isAccessBlocked ? null : ...`.
   접근불가 cp 는 AccessBlockedPopup 이 우선하므로 재진입 훅 비활성화.
6. **`advanceToNextPending(skipCpId?)` 헬퍼 신설**: 저장 후 auto-advance 로직을
   추출. AccessBlockedPopup '확인' 액션에서도 재사용. 접근불가/defaultResult/이미
   완료된 cp 는 건너뜀.
7. **렌더 순서 변경 (line ~3239+)**: `{isAccessBlocked ? <AccessBlockedPopup> : popupState && <InspectionRevisitPopup>}`.
   접근불가 팝업이 재진입 팝업보다 최우선.
8. **저장 버튼 (line ~3360+)**: `isAccessBlocked` 추가 조건으로 disabled + 라벨
   '접근 불가 개소' 로 변경. 이중 방어.

## 자동 스킵 vs 새 팝업 동작 표

| 시나리오                          | 기존 동작                             | 새 동작                                                     |
| --------------------------------- | ------------------------------------- | ----------------------------------------------------------- |
| 완강기 카테고리 모달 open         | 9개 자동 normal 제출 (useEffect)      | **동일** — useEffect 계속 동작, records/doneCount 유지      |
| 피커에 접근불가 cp 표시?          | ❌ 필터링 되어 안 보임                | ✅ 피커에 포함                                              |
| 초기 포커스                       | 첫 미완료 (자연히 non-접근불가)       | 첫 미완료 & non-접근불가 (명시적으로 배제)                  |
| 접근불가 cp 에 화살표/스와이프 도달 | 불가능                                | 가능 → AccessBlockedPopup 오버레이 노출                     |
| 팝업 상태에서 재진입 팝업도 뜸?   | n/a                                   | ❌ hook 비활성화 (checkpointId=null)                        |
| '확인' 버튼                       | n/a                                   | `advanceToNextPending(cp.id)` 호출 → 다음 non-접근불가로 이동 |
| 이전/이후 화살표·스와이프         | n/a                                   | 기존 피커 nav 그대로 동작 (팝업 위 헤더 카드)               |
| 저장 버튼                         | n/a                                   | disabled + '접근 불가 개소' 라벨                            |
| doneCount 집계                    | `cp.description?.includes('[접근불가]')` 포함 | **동일** (완강기는 auto-submit 된 records 로 자동 집계)     |

## 엣지 케이스

1. **층/구역 전체가 접근불가인 경우**: `firstPending` 은 -1 반환 → 인덱스 0 으로
   폴백 → 인덱스 0 이 접근불가 cp 라면 즉시 팝업 노출. 사용자는 ‹/›/스와이프/닫기
   로 탐색 가능. 수용 가능한 UX.
2. **모든 잔여 cp 가 접근불가인 상태에서 '확인' 클릭**: `advanceToNextPending` 이
   이동 대상을 못 찾고 no-op → 현재 cp 유지 → 팝업 계속 표시. 사용자가 수동으로
   "닫기" 버튼(모달 헤더 하단) 또는 이전 화살표로 빠져나감.
3. **유도등 (isGuideLight)**: `advanceToNextPending` 은 가드로 early return
   (기존 auto-advance 동작과 일치). 유도등 개소는 [접근불가] + defaultResult 조합
   이므로 `pendingCPs` 에서 이미 제외됨 → 신규 UX 영향 없음.
4. **완강기 auto-submit useEffect 타이밍**: useEffect 가 비동기라 모달 open 직후
   0~수백 ms 동안은 records 가 비어있을 수 있음. 이 타이밍에 사용자가 접근불가
   cp 를 탐색하면 팝업은 뜨지만 records 는 아직 미반영. useEffect 완료 후
   `loadTodayRecords` 가 돌아 records 가 채워짐. UX 영향 없음 (팝업의 본 기능은
   "안내" 이므로 기록 여부와 무관).
5. **QR 스캔 경로**: `initialCpId` 로 바로 특정 cp 에 포커스 가는 경로는 변경 없음.
   해당 cp 가 접근불가 cp 면 스캔 직후 팝업 노출 — 사용자가 QR 스캔 했는데 접근
   불가였음을 즉시 알 수 있어 정보 제공 측면에서 오히려 개선.

## 수정 파일 리스트

- `cha-bio-safety/src/components/AccessBlockedPopup.tsx` (신규, 31 lines)
- `cha-bio-safety/src/pages/InspectionPage.tsx` (+47 / -31)

## 검증

- `tsc --noEmit`: 0 errors
- `npm run build`: success

## 회귀 리스크

- **완강기 auto-submit useEffect 유지**: 기존 records/doneCount 집계 동작 그대로.
  새 UX 는 "피커 탐색 시 접근불가 cp 에서 뭔가 보여주기" 만 추가.
- **pendingCPs 의 개수 증가**: 피커 개수 `(idx/N)` 표기에서 N 이 커짐. 완강기
  카테고리 모달 기준 N 이 +9 (접근불가 9개 포함). 사용자가 혼란스러울 수 있으나,
  doneCount/totalCount 에도 동일하게 반영돼 "9개 이미 완료" 로 표기되므로
  의미적으로 정합.
- **auto-advance 헬퍼 리팩토링**: 기존 setTimeout 내 인라인 로직을 외부 함수로
  추출했을 뿐 로직은 완전 동일. 차이는 `isIncomplete` 내부의 `justSavedId` →
  `skipCpId` 파라미터 이름만 변경.
- **저장 버튼 disabled 조건 추가**: `|| isAccessBlocked` 만 추가. 접근불가가 아닌
  경우 동작 변동 없음.

## 커밋

- `44463c4` feat(260424-1x1): 접근불가 개소 자동 스킵 대신 팝업 노출
