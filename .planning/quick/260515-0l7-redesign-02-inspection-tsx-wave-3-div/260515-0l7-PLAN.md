---
phase: 260515-0l7-redesign-02-inspection-tsx-wave-3-div
plan: 01
type: execute
wave: 3
depends_on:
  - 260514-i4r-redesign-02-inspection-tsx                      # Wave 1 — InspectionPage 메인 셸 변환 패턴 권위
  - 260514-pnr-wave-1-fix-photobutton-revisitpopup-acce        # Wave 1 fix — 외부 컴포넌트 lucide/토큰 정합
  - 260514-sp7-redesign-02-inspection-tsx-wave-2-5             # Wave 2 — 5종 증상 피커 변환 패턴
  - 260514-tbj-redesign-02-inspection-tsx-wave-4               # Wave 4 — Baeyeon/Damper 변환 패턴 (인접 wave 직전)
files_modified:
  - cha-bio-safety/src/pages/InspectionPage.tsx
autonomous: true
requirements:
  - REDESIGN-02-INSPECTION-TSX-WAVE3
tags:
  - redesign
  - inspection
  - tsx-conversion
  - design-tokens-v0.1.1
  - tailwind-only
  - div-modal
  - compressor-modal
  - div-trend-subview
  - div-under-picker
  - double-cycle
  - wave3

must_haves:
  truths:
    - "DivUnderPicker / DivTrendSubview / DivModal / CompressorModal 4 컴포넌트의 비즈니스 로직(props / state / useRef / useMemo / useEffect / 핸들러 / detectDivTrend / parseRow / diffTag / handleDigit / handleDigitKey / handleSave / resetForm / DIV_PT_CP·COMP_PT_CP 매핑 / useInspectionRevisitPopup 호출 / fetch /api/div/pressure · /api/div/comp-inspection · /api/div/logs · /api/div/comp-inspection?location= 호출 / mode='from-div' 분기 / setShowCompressor / setShowTrend / showTrend / done / saving / underPending / underPickIdx / line / lineIdx / zone / timing / digits / drain / tankDrain / oil / result / memo / autoReason / prevRecords / prevInspections / lastDrain / drainDPlus)이 100% 보존된다 (변경 전후 동작 동일)"
    - "DIV doubleCycle 룰(월초/월말 timing 분기 + new Date().getDate() <= 15 기본값 + early/late 두 버튼)이 시안 권위 1:1 보존된다"
    - "DIV/컴프 짝꿍 매핑(CP-DIV-X-Y ↔ CP-COMP-X-Y) — DIV_PT_CP[currentPt.id] · COMP_PT_CP[currentPt.id] 호출 + onSaveRecord 시그니처 한 줄도 변경 없음"
    - "DivModal 의 showCompressor 서브 컴포넌트 렌더(from-div mode) + DivTrendSubview overlay(showTrend) 두 오버레이 호출이 보존된다"
    - "DivModal/CompressorModal 본문(JSX) 안에 인라인 style={{...}} 의 금지 키(color / background / padding / margin / fontSize / fontWeight / borderRadius / display / flex* / grid* / gap / width / height — 정적값) 사용이 0개이다 (i4r/sp7/tbj 화이트리스트와 동일 — position / transform / transition / animation / cubic-bezier / NAV_BOTTOM 동적값 / 동적 색 var() / §6.4 그라디언트 CTA / border-[1.5px] / bg-[rgba()] 만 허용)"
    - "DivUnderPicker / DivTrendSubview / DivModal / CompressorModal 4 영역에서 9 / 10 / 11px 폰트 사이즈 사용이 0개이다 (text-caption(12) ~ text-display(28) 또는 text-[Npx] arbitrary 12 이상만)"
    - "옛 토큰(var(--bg) / var(--bg2) / var(--bg3) / var(--bd) / var(--bd2) / var(--acl) / var(--t1) / var(--t2) / var(--t3) / var(--safe) / var(--warn) / var(--danger) / var(--fire)) 사용이 변환 영역(라인 948~2039 부근) 내 0개이다"
    - "결과 picker (정상 / 주의 / 불량) 3 버튼 — §7.1 일관성 (pill + lucide CheckCircle2 / AlertTriangle / XCircle 16x16 + status outline+tinted bg) 시각화. tbj BaeyeonModal/DamperModal 결과 picker 와 동일 톤"
    - "증상 피커 도입 X — DIV/컴프는 자동화 5종(유도등/소화기/소화전/방화셔터/전실제연댐퍼)이 아니므로 symptomPick 신설 금지. 메모는 result !== 'normal' 시 그대로 placeholder='특이사항 (선택)' 유지"
    - "TypeScript 컴파일 0 에러, npm run build 통과 (sw.js 재생성 포함)"
    - "Wave 5~6 출구 — PowerPanelModal / ParkingGateModal 은 이번 변환에서 인라인 style 그대로 보존 (후속 quick 트랙)"
  artifacts:
    - path: "cha-bio-safety/src/pages/InspectionPage.tsx"
      provides: "Wave 3 변환 완료된 DivUnderPicker + DivTrendSubview + DivModal + CompressorModal. doubleCycle / DIV·컴프 짝꿍 / DivTrendSubview overlay / from-div mode / DIV_PT_CP·COMP_PT_CP 매핑 100% 보존. 인라인 style 금지 키 0건 + 9/10/11px 0건 + 옛 토큰 0건 + 증상 피커 미도입."
      min_lines: 5500
      contains: "DivUnderPicker, DivTrendSubview, DivModal, CompressorModal, detectDivTrend, DIV_PTS, DIV_LINE_SEQ, DIV_UNDER_SEQ, DIV_PT_CP, COMP_PT_CP, useInspectionRevisitPopup"
  key_links:
    - from: "cha-bio-safety/src/pages/InspectionPage.tsx (DivModal/CompressorModal)"
      to: "tailwind.config.js fontSize tokens + colors (status / text / surface / border / accent)"
      via: "Tailwind utility 클래스 (text-caption / bg-surface-raised / text-text-primary / border-border-default / bg-{safe|warning|danger|fire}{-bar|-bg}? / text-{safe|warning|danger|fire})"
      pattern: "text-(caption|label|body-sm|body|title|heading|display)|bg-surface-(page|raised|sunken|active)|text-text-(primary|secondary|tertiary|on-accent)|border-border-(default|strong|focus)|bg-(safe|warning|danger|info|fire)(-bar|-bg)?|text-(safe|warning|danger|info|fire)"
    - from: "cha-bio-safety/src/pages/InspectionPage.tsx (DivModal/CompressorModal)"
      to: "fetch /api/div/pressure, /api/div/comp-inspection, /api/div/logs"
      via: "기존 호출 한 줄도 변경 없음 — body/headers/method 그대로"
      pattern: "fetch\\(['\"]\\/api\\/div\\/(pressure|comp-inspection|logs)"
    - from: "cha-bio-safety/src/pages/InspectionPage.tsx (DivModal)"
      to: "DIV_PT_CP[currentPt.id] / onSaveRecord(cpId, result, memo, photoKey)"
      via: "DIV→점검기록 연동 (Wave 1/i4r InspectionPage 의 onSaveRecord 시그니처 그대로)"
      pattern: "DIV_PT_CP\\[currentPt\\.id\\]|onSaveRecord\\(cpId, result, memo"
    - from: "cha-bio-safety/src/pages/InspectionPage.tsx (CompressorModal)"
      to: "COMP_PT_CP[currentPt.id] / onSaveRecord(cpId, result, memo, photoKey)"
      via: "컴프→점검기록 연동 + DIV/컴프 1:1 짝꿍 매핑(CP-DIV-X-Y ↔ CP-COMP-X-Y) 메모리 룰"
      pattern: "COMP_PT_CP\\[currentPt\\.id\\]|onSaveRecord\\(cpId, result, memo"
    - from: "cha-bio-safety/src/pages/InspectionPage.tsx (DivModal/CompressorModal)"
      to: "lucide-react (X / ChevronLeft / ChevronRight / BarChart3 / Wind / Camera / CheckCircle2 / AlertTriangle / XCircle / Flame / TrendingUp / Droplets)"
      via: "이모지(📊/💨/✅/✕/‹/›/📈/🚨/⚠️) 제거 + lucide 통일 — i4r 의 Wave 1 import 블록에 필요 시 신규 아이콘 추가"
      pattern: "import.*from\\s+['\"]lucide-react['\"]"
---

<objective>
InspectionPage TSX 변환 Wave 3 — 3차 시안(`inspection-sketch-div-comp-cycle.html` 1390줄) 을 단일 진실 소스로 삼아 `cha-bio-safety/src/pages/InspectionPage.tsx` 의 **DivUnderPicker(948~1004) + DivTrendSubview(1005~1136) + DivModal(1137~1708) + CompressorModal(1709~2039)** 4 영역(총 ~1110줄)을 v0.1.1 디자인 토큰 + Tailwind only 로 교체한다.

**Purpose:**
- Wave 1 (i4r) / Wave 2 (sp7) / Wave 4 (tbj) 의 변환 패턴(인라인 금지 키 0건 + 9/10/11px 0건 + lucide 통일 + tokens.css 자동 라이트/다크 분기 + §7.1 결과 picker 일관성 + §6.4 그라디언트 CTA) 을 DIV/컴프 doubleCycle 영역에도 정착
- DIV/컴프는 자동화 5종(유도등/소화기/소화전/방화셔터/전실제연댐퍼) 카테고리가 **아니므로 증상 피커는 도입하지 않는다** (메모리 룰 — "자동화 5종 카테고리 룰")
- doubleCycle 1주차/2주차 동시 입력 패턴(timing early/late 분기 + new Date().getDate() <= 15 기본값) 시각화는 시안 권위 1:1 매핑
- Wave 5~6 (PowerPanelModal / ParkingGateModal) 후속 트랙의 fixed reference point 확보

**Output:**
- 비즈니스 로직 100% 보존하면서 Wave 3 변환 영역(라인 948~2039 부근) 의 마크업/스타일이 3차 시안과 1:1 매칭된 새 `InspectionPage.tsx`
- PowerPanelModal / ParkingGateModal 본문은 **한 줄도 변경 없이 그대로 보존** (Wave 5~6 별도 quick 트랙)

**Out of scope (Wave 5~6 follow-up):**
- `PowerPanelModal` (라인 ~2034부터) — 소방용전원공급반 (별도 sketch / Wave 5)
- `ParkingGateModal` — 주차장비/회전문 (별도 sketch / Wave 6)
- 자동화 5종 카테고리(유도등/소화기/소화전/방화셔터/전실제연댐퍼) — Wave 2 (sp7) / Wave 4 (tbj) 에서 이미 완료
- BaeyeonModal / DamperModal / StairwellModal / CctvModal — 이전 Wave 에서 완료
- DivModal 의 detectDivTrend 함수(라인 24~/별도 헬퍼) — 본문 한 줄도 변경 금지 (자동 판단 로직)
- DIV_PTS / DIV_LINE_SEQ / DIV_UNDER_SEQ / DIV_PT_CP / COMP_PT_CP 상수 / 타입 변경 금지
- 외부 컴포넌트(PhotoButton / InspectionRevisitPopup / AccessBlockedPopup) 본체 수정 금지 — pnr 에서 이미 토큰 정합
- API / hooks / utils 수정 금지
- 다른 페이지 (.tsx) 수정 금지

**핵심 메모리 룰 강제(작업 전 재확인):**
- **"DIV/컴프 동시 점검"** — 컴프는 항상 DIV와 같이 점검. CP 1:1 매핑(`CP-DIV-X-Y` ↔ `CP-COMP-X-Y`). cycle 룰 동일. → DivModal 의 `setShowCompressor(true)` 진입점 + CompressorModal mode='from-div' 분기 시각/동작 보존 필수
- **"자동화 5종 카테고리 룰"** — DIV/컴프는 5종에 포함 안 됨. **증상 피커(symptomPick) 신설 금지**. 메모는 `placeholder='특이사항 (선택)'` 그대로
- **"디자인 변경 전 상의 필수"** — Wave 1/2/4 의 변환 패턴 외 새로운 디자인 결정이 필요하면 시안과 일치 확인. 시안이 권위
- **"디자인 조정은 시안 먼저"** — 시안 inspection-sketch-div-comp-cycle.html 1390줄이 마크업/색/구조 단일 진실 소스
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/STATE.md

# Wave 1 (i4r) — 변환 패턴 권위, 5xl Dashboard 패턴 InspectionPage 정착
@.planning/quick/260514-i4r-redesign-02-inspection-tsx/260514-i4r-PLAN.md

# Wave 2 (sp7) — 5 증상 피커 변환 패턴 (DIV/컴프는 미도입 룰 reference)
@.planning/quick/260514-sp7-redesign-02-inspection-tsx-wave-2-5/260514-sp7-PLAN.md

# Wave 4 (tbj) — Baeyeon/Damper 변환 패턴 (인접 직전 wave)
@.planning/quick/260514-tbj-redesign-02-inspection-tsx-wave-4/260514-tbj-PLAN.md
@.planning/quick/260514-tbj-redesign-02-inspection-tsx-wave-4/260514-tbj-SUMMARY.md

# 3차 sketch HTML — Wave 3 의 마크업/색/구조 단일 진실 소스 (1390줄, doubleCycle 룰 명시)
@cha-bio-safety/docs/redesign-context/02-inspection/sketch/inspection-sketch-div-comp-cycle.html
@cha-bio-safety/docs/redesign-context/02-inspection/sketch/inspection-audit-design-system.html

# 디자인 시스템 v0.1.1 — §6.1 Progress / §6.4 그라디언트 / §7.1 결과 picker 일관성 / §7.3 결과 아이콘
@cha-bio-safety/docs/design-system.md

# 변환 대상 — 비즈니스 로직 100% 보존, 5716줄 (Wave 1/2/4 이후)
@cha-bio-safety/src/pages/InspectionPage.tsx

# 토큰 / Tailwind 카탈로그
@cha-bio-safety/src/styles/tokens.css
@cha-bio-safety/tailwind.config.js

# 공통 UI / 외부 컴포넌트 — 그대로 사용 (수정 금지)
@cha-bio-safety/src/components/ui/icons.tsx
@cha-bio-safety/src/components/PhotoButton.tsx
@cha-bio-safety/src/components/InspectionRevisitPopup.tsx
</context>

<interfaces>
<!-- 이 영역이 의존하는 외부/내부 시그니처. 변경 금지. -->
<!-- Executor 는 아래 인터페이스를 직접 사용 (codebase 추가 탐색 불필요). -->

# 1. DivUnderPicker — props (변경 금지)
function DivUnderPicker({ items, activeIdx, onChange }: {
  items: { id: string; label: string }[]
  activeIdx: number
  onChange: (idx: number) => void
})
// 내부 상수: ITEM_H = 44 / VISIBLE = 3 — 변경 금지
// useRef 3종: scrollRef / timerRef / prevIdsRef — 그대로
// 동작: scrollSnapType:'y mandatory' + 100ms debounce snapTo + opacity 분기(거리 0→1, 1→0.48, 2+→0.15)
// 시각: dist===0 일 때 폰트 13px/700, 그 외 11px/400 → text-label/font-bold vs text-caption/font-normal (11px 금지)

# 2. DivTrendSubview — props (변경 금지)
function DivTrendSubview({ point, records, onClose }: {
  point:   DivPt
  records: any[]   // oldest → newest
  onClose: () => void
})
// SVG 차트(폴리라인 3종 — 1차압 #3b82f6 / 2차압 #f97316 / 세팅압 #22c55e dashed)
// **SVG 안 fontSize 11 / 9 / 12 — SVG attribute 는 폰트 룰 적용 안 함(차트 좌표)**
// 단, div text(.section-title / table cell / value) 은 v0.1.1 토큰 매핑 필수
// 동적 색 P_COLORS / dashed / strokeWidth 등은 인라인 SVG 속성 그대로 (Tailwind 영역 외)
// **detectDivTrend (별도 헬퍼) / hist 계산 / labels 계산 한 줄도 변경 금지**

# 3. DivModal — props (변경 금지)
function DivModal({ onClose, onSaveRecord, initialLocationNo, monthRecords, scheduleItems }: {
  onClose: () => void
  onSaveRecord: (cpId: string, result: CheckResult, memo: string, photoKey?: string) => Promise<void>
  initialLocationNo?: string
  monthRecords:  Record<string, MonthRecordEntry>
  scheduleItems: ScheduleItem[]
})
// state 17개 — 한 줄도 변경 금지:
//   timing / zone / line / lineIdx / underPending / underPickIdx / digits(9 slots) / drain / result / memo /
//   photo (usePhotoUpload) / showCompressor / prevRecords / autoReason / saving / done / showTrend
// useRef 9개 (dRef0~dRef8) + useMemo dRefs — 그대로
// useEffect 3개 (currentPt / prevRecords fetch / autoReason 자동 판단 — detectDivTrend 호출) — 한 줄도 변경 금지
// fetch /api/div/pressure + /api/div/logs (drain=yes) — body/headers/method 그대로
// onSaveRecord 호출: const cpId = DIV_PT_CP[currentPt.id]; if (cpId) await onSaveRecord(cpId, result, memo || '', photoKey ?? undefined).catch(() => {})
// 트렌드 overlay: showTrend && currentPt → <DivTrendSubview ... />
// 컴프 overlay: showCompressor && currentPt → <CompressorModal mode="from-div" ... />

# 4. CompressorModal — props (변경 금지)
function CompressorModal({ onClose, onSaveRecord, initialLocationNo, mode = 'standalone', monthRecords, scheduleItems }: {
  onClose: () => void
  onSaveRecord: (cpId: string, result: CheckResult, memo: string, photoKey?: string) => Promise<void>
  initialLocationNo?: string
  mode?: 'standalone' | 'from-div'
  monthRecords:  Record<string, MonthRecordEntry>
  scheduleItems: ScheduleItem[]
})
// state 11개 — 한 줄도 변경 금지:
//   zone / line / lineIdx / underPending / underPickIdx / tankDrain / oil / result / memo / saving / done
//   + lastDrain / prevInspections (useState<...|null>/useState<any[]>) + photo (usePhotoUpload)
// useEffect 2개 (lastDrain fetch /api/div/logs?type=comp_drain / prevInspections fetch /api/div/comp-inspection?location=) — 한 줄도 변경 금지
// drainDPlus useMemo / resetForm useCallback — 그대로
// fetch /api/div/comp-inspection (POST) + /api/div/logs (tank_drain / refill) — body/headers/method 그대로
// onSaveRecord 호출: const cpId = COMP_PT_CP[currentPt.id]; if (cpId) await onSaveRecord(cpId, result, memo || '', photoKey ?? undefined).catch(() => {})
// mode === 'from-div' 분기 — zIndex 99 → 120, 헤더 step 표시 안 함, 구역/라인 선택 카드 표시 안 함, 저장 후 onClose() 즉시 호출
// **DIV/컴프 짝꿍 메모리 룰** — DivModal 안에서 호출될 때 mode='from-div', initialLocationNo=currentPt.id 그대로

# 5. 외부 헬퍼 / 상수 — 변경 금지 (라인 ~23부터)
NAV_BOTTOM = 'calc(54px + env(safe-area-inset-bottom, 20px))'
detectDivTrend(series: number[], isP1: boolean): { level: 'normal'|'caution'|'bad', cumulative: number }
DIV_PTS: DivPt[]   // 각 측정점 { id, pos, floor, floorLabel, loc }
DIV_LINE_SEQ: Record<number, number[]>   // 1: [...], 2: [...], 3: [...]
DIV_UNDER_SEQ: string[]
DIV_PT_CP: Record<string, string>        // CP-DIV-X-Y 매핑
COMP_PT_CP: Record<string, string>       // CP-COMP-X-Y 매핑 (DIV 짝꿍)
DivZone = 'research' | 'office' | 'underground'
DivPt = { id: string, pos: number, floor: number, floorLabel: string, loc: string }

# 6. 훅 — 변경 금지
useAuthStore() → { staff, token, ... }
usePhotoUpload() → { blob, preview, key, uploadFromFile, pick, reset, upload(), ... }
useInspectionRevisitPopup({ checkpointId, category, monthRecords, scheduleItems })
  → { popupState: { variant, checkedAt, inspectorName, recordId? } | null, dismiss(): void }
useNavigate / useState / useRef / useMemo / useCallback / useEffect

# 7. 외부 컴포넌트 — 그대로 사용 (수정 금지)
PhotoButton({ hook })                                    // src/components/PhotoButton.tsx (pnr 에서 Camera 아이콘 + 토큰 정합 완료)
InspectionRevisitPopup({ variant, checkedAt, inspectorName, recordId?, onClose, onGoToRemediation? })   // pnr 정합 완료, 부모 박스 position:relative 필수

# 8. lucide 아이콘 매핑 (Wave 3 신규 — Wave 1/4 import 블록 확장)
- 💨 (컴프 헤더 / 모듈명) → Wind (lucide, 18x18, text-text-secondary)   // §7.2 컴프레셔 카탈로그
- 📊 (DIV 헤더 / 모듈명) → BarChart3 (lucide, 18x18, text-text-secondary)   // §7.2 DIV 카탈로그
- 📈 트렌드 (DivModal 압력 입력 헤더의 토글 버튼) → TrendingUp (lucide, 14x14)
- ✕ (DivTrendSubview close) → X (lucide, 16x16)
- ‹ / › (currentPt 네비 이전/다음) → ChevronLeft / ChevronRight (lucide, 20x20)
- ✅ (done 완료 화면) → CheckCircle2 (lucide, 48x48 또는 36x36, text-safe)
- 🚨 / ⚠️ (autoReason 배너) → Flame / AlertTriangle (lucide, 14x14, status-fire/warning)
- 결과 picker 3종 (정상/주의/불량) → CheckCircle2 / AlertTriangle / XCircle (lucide, 16x16) — Wave 4 tbj 와 동일 톤

# 9. 결과 picker 시각화 룰 (§7.1 일관성 — Wave 4 tbj 와 동일)
- 컨테이너: `flex gap-2`
- 버튼 기본: `flex-1 px-3 py-2.5 rounded-pill border-[1.5px] inline-flex items-center justify-center gap-1.5 text-label font-semibold transition-colors`
- 비활성: `border-border-default bg-surface-sunken text-text-secondary`
- 활성(정상): `border-safe-bar bg-safe-bg text-safe`
- 활성(주의): `border-warning-bar bg-warning-bg text-warning`
- 활성(불량): `border-danger-bar bg-danger-bg text-danger`
- lucide outline 16x16 (CheckCircle2 / AlertTriangle / XCircle), color="currentColor"

# 10. 인라인 style 허용 화이트리스트 (i4r/sp7/tbj 전례 그대로)
- position / top / left / right / bottom (NAV_BOTTOM 등 동적 calc)
- transform (slide-up / translateY 동적)
- transition (cubic-bezier / .26s 등 — Tailwind 미정의 cubic-bezier)
- animation (keyframe 미정의)
- 동적 색 var() 직참조 (예: P_COLORS[idx] / diff.color / resultColor[r] 같은 인덱스 분기) — 단 가능하면 Tailwind 분기로 옮기기 권장
- §6.4 그라디언트 CTA: `background: 'linear-gradient(135deg,#1d4ed8,#0ea5e9)'` (저장 버튼만, Tailwind arbitrary `bg-[linear-gradient(...)]` 도 OK)
- bg-[rgba(...)] arbitrary value (예: `bg-[rgba(59,130,246,.18)]` 같은 tinted bg — Tailwind utility 로 표현 가능하면 우선)
- SVG attribute (fontSize / fontFamily / stroke / fill 등) — DivTrendSubview SVG 차트는 attribute 그대로 유지
- scrollbarWidth: 'none' / scrollSnapType: 'y mandatory' / scrollSnapAlign: 'center' (Tailwind 미정의)
- maxHeight / minHeight 동적 calc

# 11. 옛 토큰 → v0.1.1 매핑 (4.1 마이그레이션 룰 그대로)
| 기존 | 새 토큰 |
|---|---|
| `var(--bg)` | `var(--surface-page)` 또는 `bg-surface-page` |
| `var(--bg2)` | `var(--surface-raised)` 또는 `bg-surface-raised` |
| `var(--bg3)` | `var(--surface-sunken)` 또는 `bg-surface-sunken` |
| `var(--bd)` | `var(--border-default)` 또는 `border-border-default` |
| `var(--bd2)` | `var(--border-strong)` 또는 `border-border-strong` |
| `var(--acl)` | `var(--accent)` 또는 `bg-accent` / `text-accent` |
| `var(--t1)` | `text-text-primary` |
| `var(--t2)` | `text-text-secondary` |
| `var(--t3)` | `text-text-tertiary` |
| `var(--safe)` | `text-safe` 또는 `bg-safe-bar` 또는 `bg-safe-bg` |
| `var(--warn)` | `text-warning` / `bg-warning-bar` / `bg-warning-bg` |
| `var(--danger)` | `text-danger` / `bg-danger-bar` / `bg-danger-bg` |
| `var(--fire)` | `text-fire` / `bg-fire-bar` / `bg-fire-bg` (조치 대기 의미 고정) |
</interfaces>

<design_rules_wave3>
**시안 매핑 (3차 sketch — inspection-sketch-div-comp-cycle.html, 1390줄):**

1. **헤더 (각 모달):**
   - DivModal: `<Wind|BarChart3 size={18} className="text-text-secondary" />` + `<span className="text-title text-text-primary">DIV 점검</span>` + 우측 step (`text-caption text-text-tertiary`)
   - CompressorModal: 동일 패턴 with `Wind` 18x18

2. **timing(월초/월말 doubleCycle) 2 버튼 + 구역(연구동/사무동/지하) 3 버튼 + 라인(DIV #1/#2/#3) segmented:**
   - 컨테이너: `flex gap-2`
   - 버튼 기본: `flex-1 basis-0 px-2 py-2.5 rounded-md text-label font-bold transition-colors`
   - 활성: `border-[1.5px] border-accent bg-accent text-text-on-accent`
   - 비활성: `border border-border-strong bg-surface-page text-text-secondary`
   - 섹션 라벨: `text-caption font-semibold text-text-tertiary mb-2`

3. **현재 개소 네비 카드 (Prev/Next 화살표 + 중앙 정보 + 좌우 스와이프):**
   - 컨테이너: `bg-surface-raised border border-border-default rounded-md px-3 py-2.5 flex items-center gap-2.5 [touch-action:pan-y]`
   - 좌우 버튼: `w-9 h-9 rounded-sm border border-border-default bg-surface-page inline-flex items-center justify-center flex-shrink-0` + `text-text-primary` 활성 / `text-text-tertiary opacity-30` 비활성 + lucide `ChevronLeft|ChevronRight size={20}`
   - 중앙: `flex-1 text-center` — 상단 `text-caption text-text-tertiary font-semibold`, 메인 `text-body-sm text-text-primary font-bold`, 보조 `text-caption text-text-secondary`
   - 스와이프 touch handler 본문 한 줄도 변경 금지

4. **압력 입력 카드 (DivModal):**
   - 외곽: `bg-surface-raised rounded-md p-3.5 border border-border-default`
   - 섹션 헤더: `flex items-center mb-3` + `text-caption font-semibold text-text-tertiary flex-1` + 트렌드 버튼 `px-2.5 py-1 rounded-sm border border-border-default bg-surface-page text-text-tertiary text-caption font-semibold inline-flex items-center gap-1` + `<TrendingUp size={12} />`
   - 컬럼 헤더 row: 동일 flex + `text-caption font-semibold text-text-tertiary text-center`
   - 압력 행 [라벨42 / 직전 flex:1 / 변화 flex:1 / 입력 flex:1] — 라벨 `w-[42px] flex-shrink-0 text-caption font-semibold text-text-tertiary`
   - 직전값 폰트 20/700 → `text-[20px] font-bold font-mono text-text-tertiary` (text-display 28 너무 큼, arbitrary 20 허용 — 12+ 만족)
   - 변화량 폰트 12/800 → `text-caption font-extrabold font-mono` + 동적 색 인라인 (diff.color 분기는 보존, var(--warn)/var(--safe) → status 토큰)
   - 입력 박스: `w-[34px] h-[42px] text-center text-[20px] font-bold rounded-sm bg-surface-page outline-none flex-shrink-0` + border-2 + 동적 color (P_COLORS[idx] 인라인 색 분기 OK — 차트 색 일관)
   - 소수점 dot: `text-[18px] font-bold` + 동적 color

5. **배수 / 컴프 점검 (DivModal) — 2 칸 grid:**
   - 컨테이너: `flex gap-2.5`
   - 각 칸: `flex-1`
   - 배수 segmented: 없음/있음 — 없음 활성=accent ring, 있음 활성=`bg-[rgba(59,130,246,.18)] text-accent` ring-2 ring-accent (Wave 4 tbj 의 segmented 룰 적용 가능 — 활성=`border-[1.5px] border-accent bg-accent text-text-on-accent`)
   - 컴프 점검 CTA 버튼: `w-full px-2.5 py-2.5 rounded-sm bg-surface-active text-text-primary text-label font-bold border border-border-default inline-flex items-center justify-center gap-1.5` + `<Wind size={14} />` + "컴프레셔 점검 →"

6. **탱크 배수 / 컴프 오일 (CompressorModal) — 2 칸 grid:**
   - 동일 segmented 패턴
   - 탱크 배수 D+N 배지: drainDPlus > 60 → `text-warning bg-warning-bg`, 그 외 → `text-text-tertiary bg-surface-sunken`, 패딩 `px-1.5 py-0` rounded-[4px] text-caption font-bold

7. **점검 결과 picker (§7.1, §7.3 일관성 — Wave 4 tbj 패턴 1:1):**
   - 컨테이너: `flex gap-2`
   - 3 버튼 (정상/주의/불량) — pill + lucide + status outline+tinted bg (interfaces §9 룰 그대로)
   - 활성 색은 `result === r` 분기

8. **autoReason 배너 (DivModal — bad/caution 자동 판단 시):**
   - bad: `mt-2 px-3 py-2 rounded-sm bg-fire-bg border border-fire-bar text-fire text-label font-semibold inline-flex items-center gap-1.5` + `<Flame size={14} />`
   - caution: `mt-2 px-3 py-2 rounded-sm bg-warning-bg border border-warning-bar text-warning text-label font-semibold inline-flex items-center gap-1.5` + `<AlertTriangle size={14} />`

9. **특이사항 + 사진 (textarea + PhotoButton):**
   - 컨테이너: `flex gap-2.5 items-start`
   - textarea: `flex-1 h-[72px] px-3 py-2.5 rounded-md bg-surface-raised border border-border-default text-text-primary text-label resize-none outline-none box-border placeholder:text-text-tertiary font-sans`
   - **placeholder='특이사항 (선택)' 그대로 — 증상 피커 미도입 룰**

10. **하단 버튼 바 (footer):**
    - 컨테이너: `flex gap-2 px-3.5 pt-2.5 pb-3 bg-surface-raised border-t border-border-default flex-shrink-0`
    - 닫기: `px-4 py-3 rounded-md bg-surface-page border border-border-strong text-text-secondary text-caption font-semibold`
    - 저장 CTA(§6.4 그라디언트 인라인 화이트리스트): `flex-1 py-3.5 rounded-md text-text-on-accent text-body font-bold` + style 인라인 `{ background: saving ? 'var(--border-default)' : 'linear-gradient(135deg,#1d4ed8,#0ea5e9)' }`

11. **DivTrendSubview overlay:**
    - 외곽 컨테이너: `fixed left-0 right-0 z-[99] bg-surface-page flex flex-col` + 인라인 `style={{ top:'var(--sat, 0px)', bottom:NAV_BOTTOM }}`
    - 헤더: `flex items-center px-4 py-3 border-b border-border-default gap-2.5 flex-shrink-0`
    - 닫기 버튼: `w-8 h-8 rounded-sm border border-border-default bg-surface-raised cursor-pointer inline-flex items-center justify-center` + `<X size={16} />`
    - 본문 차트 + 테이블 — SVG attribute 는 그대로, div 텍스트만 토큰화
    - 테이블: `mt-3.5 rounded-md border border-border-default overflow-hidden` + 헤더 `grid grid-cols-[60px_1fr_1fr_1fr] bg-surface-sunken px-2.5 py-1.5` + 셀 `text-caption font-bold text-text-tertiary text-center`

12. **DivUnderPicker:**
    - 외곽: `relative h-[132px] rounded-md overflow-hidden bg-surface-raised border border-border-default` (containerH = ITEM_H * VISIBLE = 132 동적)
    - 가운데 selection highlight: `absolute top-1/2 left-0 right-0 h-[44px] bg-[rgba(14,165,233,.08)] border-y border-[rgba(14,165,233,.22)] pointer-events-none z-[2]` + 인라인 transform `{ transform:'translateY(-50%)' }`
    - 위/아래 그라디언트 마스크: 동적 색 `linear-gradient(to bottom, var(--surface-raised) 30%, transparent)` 인라인 — 동적 var() 화이트리스트
    - 스크롤 컨테이너: `h-full overflow-y-auto box-border [scrollbar-width:none]` + 인라인 `{ scrollSnapType:'y mandatory', paddingTop:pad, paddingBottom:pad }` (pad 동적값 화이트리스트)
    - 항목: `h-[44px] flex items-center px-3.5 cursor-pointer transition-opacity duration-100` + 인라인 `{ scrollSnapAlign:'center', opacity: dist===0 ? 1 : dist===1 ? 0.48 : 0.15 }` (opacity 동적값 화이트리스트)
    - **active 폰트: dist===0 → `text-label font-bold text-text-primary`, 그 외 → `text-caption font-normal text-text-primary`** (기존 11px → 12 = text-caption 으로 올림. 노안 친화 룰 §1.1 만족)

13. **완료 화면 (done state):**
    - 컨테이너: `fixed left-0 right-0 z-[99] bg-surface-page flex flex-col items-center justify-center gap-4` + 인라인 `{ top:'var(--sat, 0px)', bottom:NAV_BOTTOM }`
    - 아이콘: `<CheckCircle2 size={48} className="text-safe" />` (✅ 이모지 제거)
    - 텍스트: `text-title font-bold text-text-primary` "점검 완료"
    - 닫기 버튼: `mt-2 px-8 py-3 rounded-md bg-accent text-text-on-accent text-body font-bold` (또는 §6.4 그라디언트 OK)
    - CompressorModal 완료 시 mode='from-div' 면 zIndex 120 — 인라인 화이트리스트
</design_rules_wave3>

<conversion_strategy>
**시안 → TSX 변환 (i4r/sp7/tbj 패턴 그대로, Wave 3 영역 한정):**

1. **파일 영역 한정 작업 (tbj 패턴 그대로):**
   - InspectionPage.tsx 5716 줄 중 Wave 3 변환 영역은 라인 948~2039 부근 (총 ~1110줄)
   - **다른 영역(라인 1~947 / 라인 2040~끝) 은 한 줄도 건드리지 않는다**
   - Edit 단위로 영역별 정밀 교체 — Write 전체 덮어쓰기는 금지(다른 wave 변환 영역 안전 보장)
   - PowerPanelModal (라인 ~2034부터) 시작 부분(`function PowerPanelModal({ group, allCheckpoints ...`) 직전까지가 Wave 3 영역 경계

2. **모든 `style={{...}}` 의 금지 키 제거 → Tailwind 또는 토큰 변수 클래스로:**
   - 색: `color: 'var(--t1)'` → `className="text-text-primary"`
   - 배경: `background: 'var(--bg2)'` → `className="bg-surface-raised"`
   - 테두리: `border: '1px solid var(--bd)'` → `className="border border-border-default"`
   - 패딩: `padding: '10px 12px'` → `className="px-3 py-2.5"`
   - 폰트: `fontSize: 13, fontWeight: 700` → `className="text-label font-bold"`
   - 11px → 12px (text-caption) — 노안 친화 §1.1
   - 보존: position / top / left / right / bottom (동적 calc) / transform / transition / cubic-bezier / animation / SVG attribute / 동적 색 var() / §6.4 그라디언트 CTA / scrollSnapType / scrollSnapAlign / scrollbarWidth / opacity 동적값 / paddingTop+paddingBottom 동적값 / w/h 동적값

3. **lucide 통일:**
   - 📊 → BarChart3 (이미 Wave 1 i4r 에서 import 됨)
   - 💨 → Wind (Wave 1 i4r import 확인 — 없으면 추가)
   - 📈 → TrendingUp (신규 import)
   - ✕ → X (신규 import 또는 기존)
   - ‹ / › → ChevronLeft / ChevronRight (Wave 1 i4r import 확인)
   - ✅ → CheckCircle2 (Wave 1 i4r import 됨)
   - 🚨 → Flame (Wave 1 i4r import 됨)
   - ⚠️ → AlertTriangle (Wave 1 i4r import 됨)
   - 결과 picker → CheckCircle2 / AlertTriangle / XCircle (Wave 4 tbj 와 동일)

4. **비즈니스 로직 보존 (가장 중요):**
   - state / useRef / useMemo / useEffect / useCallback 본문 한 줄도 변경 금지
   - handleSave / handleDigit / handleDigitKey / resetForm / parseRow / diffTag 본문 한 줄도 변경 금지
   - **detectDivTrend 함수 한 줄도 변경 금지** (autoReason 자동 판단 로직)
   - fetch 호출 (body/headers/method/url) 한 줄도 변경 금지
   - onSaveRecord(cpId, result, memo, photoKey) 시그니처 그대로
   - DIV_PT_CP / COMP_PT_CP 매핑 호출 한 줄도 변경 금지 (DIV/컴프 짝꿍 메모리 룰)
   - showCompressor / showTrend / mode='from-div' 분기 한 줄도 변경 금지
   - done / saving 분기 그대로
   - 스와이프 onTouchStart/onTouchEnd 핸들러 그대로

5. **증상 피커 미도입 (메모리 룰 강제):**
   - DivModal / CompressorModal 에 symptomPick state 신설 금지
   - 메모 placeholder='특이사항 (선택)' 그대로 (자동화 5종 카테고리 아님)
</conversion_strategy>

<tasks>

<task type="auto">
  <name>Task 1: DivUnderPicker + DivTrendSubview 변환 (서브 컴포넌트, 라인 948~1136, ~190줄)</name>
  <files>cha-bio-safety/src/pages/InspectionPage.tsx</files>
  <action>
**범위:** `function DivUnderPicker` (라인 948~1004) + `function DivTrendSubview` (라인 1005~1136) 두 함수 본문. DivModal/CompressorModal 은 Task 2/3 에서 처리 — 이 Task 에서 절대 건드리지 않는다.

**시안:** `cha-bio-safety/docs/redesign-context/02-inspection/sketch/inspection-sketch-div-comp-cycle.html` 의 DivUnderPicker 영역 (오버레이 picker) + DivTrendSubview 영역 (트렌드 차트 풀스크린 오버레이) — 권위.

**lucide import 보강 (파일 상단 import 블록):**
- 이미 import 된 것 확인 후 누락만 추가: `X`, `TrendingUp` (Wave 1 i4r 에서 BarChart3 / Wind / CheckCircle2 / AlertTriangle / XCircle / Flame / Camera / ChevronLeft / ChevronRight 등 대부분 추가됨 — `grep -n "from 'lucide-react'" InspectionPage.tsx` 로 기존 import 1줄 확인 후 누락만 보강)

**DivUnderPicker 변환 (라인 948~1002):**
- 상수 ITEM_H=44 / VISIBLE=3, useRef 3종(scrollRef/timerRef/prevIdsRef), useEffect, snapTo useCallback, handleScroll useCallback **한 줄도 변경 금지**
- 외곽 div (985 부근): `relative rounded-md overflow-hidden bg-surface-raised border border-border-default` + 인라인 `style={{ height: containerH }}` (containerH 동적 화이트리스트)
- 중앙 selection highlight (986 부근): `absolute left-0 right-0 z-[2] pointer-events-none bg-[rgba(14,165,233,.08)] border-y border-[rgba(14,165,233,.22)]` + 인라인 `style={{ top:'50%', height: ITEM_H, transform:'translateY(-50%)' }}` (top/height/transform 화이트리스트)
- 상단 그라디언트 마스크 (987 부근): `absolute top-0 left-0 right-0 z-[3] pointer-events-none` + 인라인 `style={{ height: pad, background:'linear-gradient(to bottom, var(--surface-raised) 30%, transparent)' }}` (동적 색 화이트리스트)
- 하단 그라디언트 마스크 (988 부근): 동일 패턴 `bottom-0` + `linear-gradient(to top, var(--surface-raised) 30%, transparent)`
- 스크롤 컨테이너 (989-990 부근): `ref={scrollRef} onScroll={handleScroll} className="h-full overflow-y-auto box-border [scrollbar-width:none] [&::-webkit-scrollbar]:hidden"` + 인라인 `style={{ scrollSnapType:'y mandatory', paddingTop: pad, paddingBottom: pad }}`
- 항목 div (991-996 부근): `className="flex items-center px-3.5 cursor-pointer transition-opacity duration-100"` + 인라인 `style={{ height: ITEM_H, scrollSnapAlign:'center', opacity: dist===0 ? 1 : dist===1 ? 0.48 : 0.15 }}`
- span 라벨 (995 부근): **dist===0 → `className="text-label font-bold text-text-primary"`, 그 외 → `className="text-caption font-normal text-text-primary"`** (11px → 12px(text-caption) 노안 친화)

**DivTrendSubview 변환 (라인 1005~1135):**
- 외곽 fixed 컨테이너 (1025 부근): `fixed left-0 right-0 z-[99] flex flex-col bg-surface-page` + 인라인 `style={{ top:'var(--sat, 0px)', bottom: NAV_BOTTOM }}`
- 헤더 (1027-1033 부근): `flex items-center px-4 py-3 border-b border-border-default gap-2.5 flex-shrink-0`
- 닫기 버튼 (1028 부근): `w-8 h-8 rounded-sm border border-border-default bg-surface-raised cursor-pointer inline-flex items-center justify-center` + 자식 `<X size={16} className="text-text-secondary" />` (✕ 텍스트 삭제)
- 헤더 텍스트 div (1030-1031 부근):
  - `<div className="text-body-sm font-bold text-text-primary">{point.floorLabel} — {point.loc}</div>`
  - `<div className="text-caption text-text-tertiary mt-0.5">DIV #{point.pos} · {point.id}</div>` (10 → 12 = text-caption)
- 차트+테이블 본문 div (1035 부근): `flex-1 overflow-y-auto px-4 pt-4 pb-10`
- "이전 기록 없음" empty (1037 부근): `text-center text-text-tertiary py-10 text-label`
- 폴리라인 컨테이너 (1040 부근): `flex flex-col gap-2.5`
- 폴리라인 label div (1057 부근): `text-caption font-bold mb-1` + 인라인 동적 색 `style={{ color }}` (P_COLORS 인라인 화이트리스트 — 차트 색 일관)
- SVG 영역 (1058-1107 부근): **모든 SVG attribute(fontSize / fontFamily / fill / stroke 등) 그대로 보존**. SVG 안 fontSize 11 / 9 / 12 는 SVG attribute (Tailwind 룰 적용 안 함)
- 동적 color 폴리라인 (circle / text 의 fill="rgba(...)" 등) — SVG attribute 그대로
- 단, `circle fill={isLate ? color : 'var(--bg2)'}` → `circle fill={isLate ? color : 'var(--surface-raised)'}` (옛 토큰 var(--bg2) → 새 토큰)
- 수치 테이블 컨테이너 (1113 부근): `mt-3.5 rounded-md border border-border-default overflow-hidden`
- 테이블 헤더 grid (1114-1117 부근): `grid bg-surface-sunken px-2.5 py-1.5` + 인라인 `style={{ gridTemplateColumns:'60px 1fr 1fr 1fr' }}` (grid-template-columns 동적 화이트리스트 또는 Tailwind `grid-cols-[60px_1fr_1fr_1fr]` 사용 가능 — arbitrary value 권장)
  - 셀: `text-caption font-bold text-text-tertiary text-center` (9 → 12 = text-caption 노안 친화)
- 데이터 행 (1119-1128 부근): `grid grid-cols-[60px_1fr_1fr_1fr] px-2.5 py-1.5 border-t border-border-default`
  - 월 셀: `text-caption text-text-tertiary text-center font-mono` (11 → 12)
  - 값 셀: `text-caption font-bold text-center font-mono` + 인라인 `style={{ color: ['#3b82f6','#f97316','#22c55e'][i] }}` (차트 색 일관 — 동적 색 화이트리스트)

**보존 룰 (한 줄도 변경 금지):**
- `function DivUnderPicker({ items, activeIdx, onChange })` props
- `const ITEM_H = 44 / VISIBLE = 3`
- useRef 3종 / useEffect (prevIds 비교 + scrollTop 설정) / snapTo useCallback / handleScroll useCallback
- pad / containerH 계산
- `function DivTrendSubview({ point, records, onClose })` props
- W / hist / endY / endM / startDate / n 계산
- SVG 좌표 계산 (sMinV / sMaxV / sH / sPadL / sPadR / sPadT / sPadB / sCW / sCH / spx / spy / sTicks / labels) — 한 줄도 변경 금지
- polyline / circle / text SVG attribute 그대로

**Out of scope (이 Task 에서 건드리지 않음):**
- DivModal (라인 1137 시작) → Task 2
- CompressorModal (라인 1708 시작) → Task 3
- detectDivTrend 함수 (외부 헬퍼) — 호출만 있고 변경 금지

**커밋:** `feat(260515-0l7): task 1 — DivUnderPicker + DivTrendSubview v0.1.1 토큰 + Tailwind 변환`
  </action>
  <verify>
    <automated>cd cha-bio-safety && npx tsc --noEmit 2>&1 | grep -E "error TS" | head -20; echo "---"; npm run build 2>&1 | tail -5; echo "---"; sed -n '948,1136p' src/pages/InspectionPage.tsx | grep -vE '^\s*//' | grep -cE 'var\(--bg\)|var\(--bg2\)|var\(--bg3\)|var\(--bd\)|var\(--bd2\)|var\(--acl\)|var\(--t1\)|var\(--t2\)|var\(--t3\)|var\(--safe\)|var\(--warn\)|var\(--danger\)|var\(--fire\)' | xargs -I{} test {} -le 2 && echo "OLD_TOKENS: ≤2 (SVG/색 인라인 화이트리스트만)" || echo "OLD_TOKENS: FAIL"; echo "---"; sed -n '948,1136p' src/pages/InspectionPage.tsx | grep -vE '^\s*//|fontSize="(11|10|9)"' | grep -cE 'fontSize:\s*(9|10|11)[^0-9]|text-\[(9|10|11)px\]' | xargs -I{} test {} -eq 0 && echo "9_10_11PX_DIV_ONLY: 0 (SVG attribute 제외)" || echo "9_10_11PX_DIV_ONLY: FAIL"</automated>
  </verify>
  <done>
    - DivUnderPicker (948~1004) + DivTrendSubview (1005~1136) 본문 마크업/스타일이 v0.1.1 토큰 + Tailwind 로 교체
    - 옛 토큰 var(--bg/bd/acl/t1~3/safe/warn/danger/fire) 사용이 div 영역 내 0건 (SVG attribute 의 fill/stroke 동적 색은 화이트리스트)
    - 9/10/11px 폰트가 div text 영역 내 0건 (SVG attribute fontSize 는 화이트리스트)
    - lucide X / TrendingUp import 확인
    - DivUnderPicker props / state / useRef / useEffect / snapTo / handleScroll / pad / containerH 시그니처 한 줄도 변경 없음
    - DivTrendSubview props / W / hist 계산 / SVG 좌표 계산 / polyline/circle/text attribute 한 줄도 변경 없음
    - tsc 0 에러, npm run build 통과
    - 단일 파일 변경 (cha-bio-safety/src/pages/InspectionPage.tsx)
    - 커밋 hash 1개
  </done>
</task>

<task type="auto">
  <name>Task 2: DivModal 변환 (메인 모달, 라인 1137~1707, ~570줄, doubleCycle/짝꿍/트렌드/컴프 오버레이 보존)</name>
  <files>cha-bio-safety/src/pages/InspectionPage.tsx</files>
  <action>
**범위:** `function DivModal({ onClose, onSaveRecord, initialLocationNo, monthRecords, scheduleItems })` 본문 (라인 1137~1707). CompressorModal 은 Task 3 — 이 Task 에서 절대 건드리지 않는다.

**시안:** `inspection-sketch-div-comp-cycle.html` 의 DivModal 메인 점검 모달 영역 — 권위. doubleCycle 룰(월초/월말) + 구역/라인 segmented + 압력 입력 카드 + 배수/컴프 점검 + 결과 picker + autoReason 배너 + 메모/사진 + footer 저장 CTA.

**비즈니스 보존 (한 줄도 변경 금지) — 절대 룰:**
- props 시그니처 그대로
- state 17개 (timing / zone / line / lineIdx / underPending / underPickIdx / digits / drain / result / memo / photo / showCompressor / prevRecords / autoReason / saving / done / showTrend) 그대로
- useRef 9개 (dRef0~dRef8) + useMemo dRefs 그대로
- initPt / initIsUnder / initZone / initLine / initLineIdx / initUnderIdx 도출 그대로
- useEffect 3개 (currentPt useMemo + prevRecords fetch + autoReason 자동 판단) 한 줄도 변경 없음
- detectDivTrend 호출 그대로 — **detectDivTrend 함수 본체는 외부 헬퍼, 손대지 않음**
- handleSave / resetForm / parseRow / diffTag / handleDigit / handleDigitKey 본문 그대로
- fetch /api/div/pressure (body 전체 — location_no/floor/position/year/month/day/timing/pressure_1/2/set/result/drain/memo/photo_key/inspector) 그대로
- fetch /api/div/logs (drain=yes) 그대로
- **DIV_PT_CP[currentPt.id] / onSaveRecord(cpId, result, memo || '', photoKey ?? undefined) 호출 그대로 (DIV/컴프 짝꿍 메모리 룰)**
- showTrend overlay 렌더 (1680-1691) 그대로 — `<DivTrendSubview point records onClose />` 그대로
- showCompressor overlay 렌더 (1694-1703) 그대로 — `<CompressorModal mode="from-div" initialLocationNo={currentPt.id} ... />` 그대로
- useInspectionRevisitPopup({ checkpointId: currentCpId, category: 'DIV', monthRecords, scheduleItems }) 그대로

**시각 변환 (v0.1.1 토큰 + Tailwind):**

1. **done 완료 화면 (1408-1414):**
   - 외곽: `fixed left-0 right-0 z-[99] flex flex-col items-center justify-center gap-4 bg-surface-page` + 인라인 `style={{ top:'var(--sat, 0px)', bottom: NAV_BOTTOM }}`
   - ✅ 이모지 (1410) → `<CheckCircle2 size={48} className="text-safe" />`
   - 텍스트 (1411): `text-title font-bold text-text-primary` "점검 완료"
   - 닫기 버튼 (1412): `mt-2 px-8 py-3 rounded-md bg-accent text-text-on-accent text-body font-bold cursor-pointer border-0`

2. **외곽 모달 컨테이너 (1417):**
   - `fixed left-0 right-0 z-[99] flex flex-col overflow-hidden bg-surface-page` + 인라인 `style={{ top:'var(--sat, 0px)', bottom: NAV_BOTTOM }}`

3. **헤더 (1419-1427):**
   - `flex items-center px-4 py-3 border-b border-border-default gap-2 flex-shrink-0`
   - 📊 (1420) → `<BarChart3 size={18} className="text-text-secondary" />` + `<span className="text-title font-bold text-text-primary">DIV 점검</span>`
   - step 표시 (1421-1426): `ml-auto text-caption font-semibold text-text-tertiary`

4. **본문 영역 (1430):**
   - `relative flex-1 overflow-y-auto p-4 flex flex-col gap-3.5`
   - InspectionRevisitPopup 호출 (1431-1440) 그대로 — position:relative 부모 확보됨

5. **점검 구분 doubleCycle (1443-1457) — 월초/월말:**
   - 섹션 라벨 (1444): `text-caption font-semibold text-text-tertiary mb-2` "점검 구분"
   - 버튼 컨테이너 (1445): `flex gap-2`
   - 각 버튼 (1449-1454): `flex-1 px-2 py-2.5 rounded-md text-label font-bold transition-colors cursor-pointer`
     - sel 활성: `border-[1.5px] border-accent bg-accent text-text-on-accent`
     - sel 비활성: `border border-border-strong bg-surface-page text-text-secondary`

6. **구역 선택 (1460-1474) — 연구동/사무동/지하:** 동일 segmented 패턴 (5 와 동일 클래스)

7. **라인 선택 (1477-1493) — DIV #1/#2/#3:** 동일 segmented 패턴

8. **현재 개소 네비 카드 (1500-1540):**
   - 외곽 (1521): `bg-surface-raised rounded-md px-3 py-2.5 border border-border-default flex items-center gap-2.5 [touch-action:pan-y]` + onTouchStart/End 핸들러 그대로
   - btnStyle (1514-1518) 인라인 헬퍼 → **Tailwind 분기 인라인으로 교체**:
     - `w-9 h-9 rounded-sm border border-border-default bg-surface-page inline-flex items-center justify-center flex-shrink-0 cursor-pointer transition-opacity` + 인라인 분기 `${enabled ? 'text-text-primary opacity-100' : 'text-text-tertiary opacity-30 cursor-default'}` — 또는 별도 헬퍼 함수 `navBtnCls(enabled)` 도입 (가독성)
   - ‹ / › 텍스트 (1531, 1537) → `<ChevronLeft size={20} />` / `<ChevronRight size={20} />`
   - 중앙 텍스트 (1532-1536):
     - `flex-1 text-center`
     - "현재 개소" (1533): `text-caption text-text-tertiary font-semibold`
     - 메인 (1534): `text-body-sm font-bold text-text-primary mt-0.5`
     - 보조 (1535): `text-caption text-text-secondary mt-0.5`

9. **압력 입력 카드 (1542-1615):**
   - 외곽 (1554): `bg-surface-raised rounded-md p-3.5 border border-border-default`
   - 섹션 헤더 (1556): `flex items-center mb-3`
     - "압력 입력" (1557): `text-caption font-semibold text-text-tertiary flex-1`
     - 트렌드 버튼 (1559-1563): `px-2.5 py-1 rounded-sm border border-border-default bg-surface-page text-text-tertiary text-caption font-semibold cursor-pointer inline-flex items-center gap-1` + 📈 → `<TrendingUp size={12} />` + "트렌드"
   - 컬럼 헤더 row (1566-1571): `flex items-center gap-1 mb-1`
     - 빈 라벨 (1567): `w-[42px] flex-shrink-0`
     - "직전" / "현재" 셀: `flex-1 text-center text-caption font-semibold text-text-tertiary` (10 → 12 = text-caption 노안 친화)
     - 중간 빈 셀: `flex-1`
   - 압력 행 (1573-1612) — 라벨/직전/변화/입력 4 칸:
     - 행 컨테이너 (1574): `flex items-center gap-1 mb-2.5`
     - 라벨 (1576): `w-[42px] flex-shrink-0 text-caption font-semibold text-text-tertiary`
     - 직전값 컨테이너 (1578): `flex-1 flex justify-center items-baseline`
       - 숫자 (1581, 1583): `inline-block w-4 font-mono text-[20px] font-bold text-text-tertiary text-right` / `text-left` (20 → arbitrary `text-[20px]` 12+ 만족)
       - 점 (1582): `font-mono text-[20px] font-bold text-text-tertiary`
       - "—" (1585): `text-[20px] font-bold text-text-tertiary`
     - 변화량 컨테이너 (1588): `flex-1 flex justify-center items-baseline`
       - 숫자 (1591, 1593): `inline-block w-5 font-mono text-caption font-extrabold text-right` / `inline-block w-[9px] font-mono text-caption font-extrabold text-left` + 인라인 `style={{ color: diff.color }}` (동적 색 화이트리스트 — 단 var(--warn)/var(--safe) → status 토큰으로 매핑 필요)
       - 점 (1592): `font-mono text-caption font-extrabold` + 인라인 색
       - **diffTag 함수 (1371-1377)**: 반환 객체 `color: 'var(--t3)'` / `'var(--warn)'` / `'var(--safe)'` → `'var(--text-tertiary)'` / `'var(--status-warning)'` / `'var(--status-safe)'` (옛 토큰 → 새 토큰)
       - "—" (1595): `text-caption text-text-tertiary`
     - 입력 박스 컨테이너 (1598): `flex-1 flex justify-center items-center gap-0.5`
     - 정수 input (1600-1603): `w-[34px] h-[42px] text-center text-[20px] font-bold rounded-sm bg-surface-page outline-none flex-shrink-0` + 인라인 `style={{ border: '2px solid ' + (digits[dIdx + i] ? color : 'var(--border-default)'), color }}` (border-2 동적 색 화이트리스트)
     - 소수점 (1605): `text-[18px] font-bold flex-shrink-0` + 인라인 `style={{ color }}`
     - 소수 input (1606-1609): 동일 패턴

10. **배수 / 컴프 점검 (1617-1635) — 2 칸 grid:**
    - 컨테이너 (1618): `flex gap-2.5`
    - 좌 칸 (1619): `flex-1`
      - 라벨 (1620): `text-caption font-semibold text-text-tertiary mb-2` "배수"
      - 버튼 row (1621): `flex gap-1.5`
      - 없음 (1622-1623): segmented 패턴 (Wave 4 tbj 와 동일)
        - 비활성: `flex-1 px-2 py-2 rounded-sm text-label font-bold cursor-pointer border border-border-strong bg-surface-sunken text-text-tertiary`
        - 활성('none'): `flex-1 px-2 py-2 rounded-sm text-label font-bold cursor-pointer border-[1.5px] border-accent bg-surface-page text-text-primary`
      - 있음 (1624-1625):
        - 활성('yes'): `flex-1 px-2 py-2 rounded-sm text-label font-bold cursor-pointer border-[1.5px] border-accent bg-[rgba(59,130,246,.18)] text-accent` (기존 시각 톤 유지 — bg-[rgba()] 화이트리스트)
        - 비활성: 동일 비활성 패턴
    - 우 칸 (1628): `flex-1`
      - 라벨 (1629): "컴프 점검" 동일
      - CTA 버튼 (1630-1633): `w-full px-2.5 py-2 rounded-sm text-label font-bold cursor-pointer border border-border-default bg-surface-active text-text-primary inline-flex items-center justify-center gap-1.5` + 💨 → `<Wind size={14} className="text-text-secondary" />` + "컴프레셔 점검 →"

11. **점검 결과 picker (1637-1653):**
    - 섹션 라벨 (1639): `text-caption font-semibold text-text-tertiary mb-2` "점검 결과"
    - 버튼 컨테이너 (1640): `flex gap-2`
    - 3 버튼 (1641-1646) — Wave 4 tbj §7.1 일관성 1:1:
      - 비활성: `flex-1 px-3 py-2.5 rounded-pill border-[1.5px] border-border-default bg-surface-sunken text-text-secondary text-label font-semibold cursor-pointer inline-flex items-center justify-center gap-1.5`
      - 활성(normal): `flex-1 px-3 py-2.5 rounded-pill border-[1.5px] border-safe-bar bg-safe-bg text-safe text-label font-semibold cursor-pointer inline-flex items-center justify-center gap-1.5`
      - 활성(caution): `border-warning-bar bg-warning-bg text-warning`
      - 활성(bad): `border-danger-bar bg-danger-bg text-danger`
      - lucide: `r==='normal'` → `<CheckCircle2 size={16} />`, `caution` → `<AlertTriangle size={16} />`, `bad` → `<XCircle size={16} />` (color="currentColor")
      - 헬퍼 도입 권장 (tbj 패턴): `resultIcon(r)` / `resultPickerCls(active, r)`
    - autoReason 배너 (1648-1652):
      - 외곽: `mt-2 px-3 py-2 rounded-sm border text-label font-semibold inline-flex items-center gap-1.5`
      - bad 분기: `bg-fire-bg border-fire-bar text-fire` + `<Flame size={14} />` + `{autoReason}` (🚨 제거)
      - caution 분기: `bg-warning-bg border-warning-bar text-warning` + `<AlertTriangle size={14} />` + `{autoReason}` (⚠️ 제거)
    - **resultColor / resultLabel 헬퍼 (1395-1396)**: 옛 var(--safe/warn/danger) → status 토큰. 단 resultColor 인라인 사용처는 위 §7.1 클래스 분기로 대체되었으므로 헬퍼 자체는 디ad code 가 될 수도 있음. 사용처 다 잡아내고 unused 면 제거 OK (TS 경고 회피).

12. **특이사항 + 사진 (1655-1659):**
    - 컨테이너 (1656): `flex gap-2.5 items-start`
    - textarea (1657-1658): `flex-1 h-[72px] px-3 py-2.5 rounded-md bg-surface-raised border border-border-default text-text-primary text-label resize-none outline-none box-border font-sans placeholder:text-text-tertiary` (placeholder='특이사항 (선택)' 그대로 — **증상 피커 미도입 룰**)
    - PhotoButton 호출 그대로

13. **하단 버튼 바 (1666-1677):**
    - 컨테이너 (1666): `flex gap-2 px-3.5 pt-2.5 pb-3 bg-surface-raised border-t border-border-default flex-shrink-0`
    - 닫기 (1667): `px-4 py-3 rounded-md bg-surface-page border border-border-strong text-text-secondary text-caption font-semibold cursor-pointer`
    - 저장 CTA (1669-1675): `flex-1 py-3.5 rounded-md text-text-on-accent text-body font-bold cursor-pointer border-0` + 인라인 §6.4 그라디언트:
      ```tsx
      style={{
        background: (saving || digits.slice(0, totalDigitSlots).some(d=>d==='')) ? 'var(--border-default)' : 'linear-gradient(135deg,#1d4ed8,#0ea5e9)',
        cursor: saving ? 'default' : 'pointer'
      }}
      ```

14. **showTrend overlay (1680-1691) / showCompressor overlay (1694-1703):** 그대로 (Task 1 의 DivTrendSubview 변환 + Task 3 의 CompressorModal 변환에서 처리)

**Out of scope (이 Task 에서 건드리지 않음):**
- DivUnderPicker (948~1004) — Task 1 완료
- DivTrendSubview (1005~1136) — Task 1 완료
- CompressorModal (1708~2039) — Task 3
- detectDivTrend 함수 — 외부 헬퍼, 손대지 않음
- diffTag 함수 본문은 변경 — 단 옛 토큰 var(--t3)/var(--warn)/var(--safe) → 새 토큰 var(--text-tertiary)/var(--status-warning)/var(--status-safe) 만 매핑 (반환 객체 color 필드)
- **증상 피커 신설 절대 금지**

**커밋:** `feat(260515-0l7): task 2 — DivModal v0.1.1 토큰 + Tailwind 변환 (doubleCycle/짝꿍/트렌드 overlay 보존, 증상 피커 미도입)`
  </action>
  <verify>
    <automated>cd cha-bio-safety && npx tsc --noEmit 2>&1 | grep -E "error TS" | head -20; echo "---"; npm run build 2>&1 | tail -5; echo "---"; sed -n '1137,1707p' src/pages/InspectionPage.tsx | grep -vE '^\s*//' | grep -cE 'var\(--bg\)|var\(--bg2\)|var\(--bg3\)|var\(--bd\)|var\(--bd2\)|var\(--acl\)|var\(--t1\)|var\(--t2\)|var\(--t3\)|var\(--safe\)|var\(--warn\)|var\(--danger\)|var\(--fire\)' | xargs -I{} test {} -le 2 && echo "OLD_TOKENS: ≤2 (동적 색 화이트리스트)" || echo "OLD_TOKENS: FAIL"; echo "---"; sed -n '1137,1707p' src/pages/InspectionPage.tsx | grep -vE '^\s*//' | grep -cE 'fontSize:\s*(9|10|11)[^0-9]|text-\[(9|10|11)px\]' | xargs -I{} test {} -eq 0 && echo "9_10_11PX_DIV: 0" || echo "9_10_11PX_DIV: FAIL"; echo "---"; sed -n '1137,1707p' src/pages/InspectionPage.tsx | grep -cE 'symptomPick|setSymptomPick|setDamperSymptomPick' | xargs -I{} test {} -eq 0 && echo "NO_SYMPTOM_PICKER: OK" || echo "NO_SYMPTOM_PICKER: FAIL (증상 피커 신설 금지 룰 위반)"; echo "---"; sed -n '1137,1707p' src/pages/InspectionPage.tsx | grep -cE 'DIV_PT_CP\[currentPt\.id\]' | xargs -I{} test {} -eq 1 && echo "DIV_PT_CP_PAIR: OK" || echo "DIV_PT_CP_PAIR: FAIL (짝꿍 매핑 변경)"; echo "---"; sed -n '1137,1707p' src/pages/InspectionPage.tsx | grep -cE 'detectDivTrend\(' | xargs -I{} test {} -eq 2 && echo "DETECTDIVTREND_CALLS: 2 (p1+p2)" || echo "DETECTDIVTREND_CALLS: FAIL (자동 판단 변경)"; echo "---"; sed -n '1137,1707p' src/pages/InspectionPage.tsx | grep -cE '/api/div/(pressure|logs)' | xargs -I{} test {} -ge 2 && echo "API_CALLS_OK: ≥2" || echo "API_CALLS: FAIL (fetch 시그니처 변경)"; echo "---"; sed -n '1137,1707p' src/pages/InspectionPage.tsx | grep -cE 'CompressorModal[^\}]*mode="from-div"' | xargs -I{} test {} -eq 1 && echo "FROM_DIV_OVERLAY: OK" || echo "FROM_DIV_OVERLAY: FAIL (짝꿍 컴프 오버레이 누락)"</automated>
  </verify>
  <done>
    - DivModal (1137~1707) 본문 마크업/스타일이 v0.1.1 토큰 + Tailwind 로 교체
    - timing(월초/월말 doubleCycle), 구역(연구동/사무동/지하), 라인(DIV #1/#2/#3) segmented 시각화 시안 1:1
    - 압력 입력 카드 (1차/2차/세팅압 — 직전/변화/입력 4 칸) 시각화 시안 1:1, 동적 색(P_COLORS/diff.color/border 동적) 인라인 화이트리스트 보존
    - 결과 picker 3 버튼 §7.1 일관성 (pill + lucide CheckCircle2/AlertTriangle/XCircle + status outline+tinted bg)
    - autoReason 배너 lucide 통일 (Flame/AlertTriangle)
    - 헤더 BarChart3 / 트렌드 TrendingUp / 컴프 CTA Wind / 네비 ChevronLeft/Right / done CheckCircle2 — 이모지 0건
    - **DIV_PT_CP[currentPt.id] 호출 + onSaveRecord 시그니처 한 줄도 변경 없음** (DIV/컴프 짝꿍 메모리 룰)
    - **detectDivTrend 호출 2건(p1+p2) 보존** (자동 판단 로직 한 줄도 변경 없음)
    - **fetch /api/div/pressure + /api/div/logs body/headers/method 한 줄도 변경 없음**
    - showTrend overlay + showCompressor mode="from-div" overlay 호출 그대로
    - useInspectionRevisitPopup 호출 그대로
    - **증상 피커 신설 0건** (DIV/컴프는 자동화 5종 카테고리 아님)
    - 옛 토큰 var(--bg/bd/acl/t1~3/safe/warn/danger/fire) 변환 영역 내 ≤2 (동적 색 인라인 화이트리스트만)
    - 9/10/11px 폰트가 변환 영역 내 0건
    - tsc 0 에러, npm run build 통과
    - 단일 파일 변경
    - 커밋 hash 1개
  </done>
</task>

<task type="auto">
  <name>Task 3: CompressorModal 변환 (짝꿍 모달, 라인 1708~2039, ~330줄, mode='from-div' 분기 + 짝꿍 매핑 보존)</name>
  <files>cha-bio-safety/src/pages/InspectionPage.tsx</files>
  <action>
**범위:** `function CompressorModal({ onClose, onSaveRecord, initialLocationNo, mode = 'standalone', monthRecords, scheduleItems })` 본문 (라인 1708~2039). PowerPanelModal (라인 ~2034부터 시작 — `function PowerPanelModal({ group, allCheckpoints ...`) 는 Wave 5 — 이 Task 에서 절대 건드리지 않는다.

**시안:** `inspection-sketch-div-comp-cycle.html` 의 CompressorModal 짝꿍 모달 영역 — 권위. mode='from-div' / 'standalone' 두 분기 시각화. 탱크 배수 D+N 배지 + 컴프 오일 + 결과 picker + 메모/사진 + footer.

**비즈니스 보존 (한 줄도 변경 금지) — 절대 룰:**
- props 시그니처 그대로 (mode?: 'standalone' | 'from-div' 분기)
- state 11개 + lastDrain + prevInspections (총 13개) 그대로
- initPt / initIsUnder / initZone / initLine / initLineIdx / initUnderIdx 도출 그대로
- useEffect 2개 (lastDrain fetch /api/div/logs?type=comp_drain&divId= / prevInspections fetch /api/div/comp-inspection?location=) 한 줄도 변경 없음
- currentPt useMemo / drainDPlus useMemo / resetForm useCallback 그대로
- useInspectionRevisitPopup({ checkpointId: currentCpId, category: '컴프레셔', monthRecords, scheduleItems }) 그대로
- handleSave 본문 그대로:
  - fetch /api/div/comp-inspection (POST body 전체) 그대로
  - fetch /api/div/logs (tank_drain=yes / oil=refill 2 분기) 그대로
  - **COMP_PT_CP[currentPt.id] / onSaveRecord(cpId, result, memo || '', photoKey ?? undefined) 그대로 (DIV/컴프 짝꿍 메모리 룰)**
  - mode === 'from-div' 분기 — 저장 후 onClose() 즉시 호출 그대로
  - underground/지상 다음 개소 진행 로직 그대로
- done state 분기 그대로

**시각 변환 (v0.1.1 토큰 + Tailwind):**

1. **done 완료 화면 (1848-1854):**
   - 외곽: `fixed left-0 right-0 flex flex-col items-center justify-center gap-4 bg-surface-page` + 인라인 `style={{ top:'var(--sat, 0px)', bottom: NAV_BOTTOM, zIndex: mode === 'from-div' ? 120 : 99 }}` (zIndex 동적 화이트리스트)
   - ✅ → `<CheckCircle2 size={48} className="text-safe" />`
   - 텍스트: `text-title font-bold text-text-primary` "점검 완료"
   - 닫기: `mt-2 px-8 py-3 rounded-md bg-accent text-text-on-accent text-body font-bold cursor-pointer border-0`

2. **외곽 모달 컨테이너 (1857):**
   - `fixed left-0 right-0 flex flex-col overflow-hidden bg-surface-page` + 인라인 `style={{ top:'var(--sat, 0px)', bottom: NAV_BOTTOM, zIndex: mode === 'from-div' ? 120 : 99 }}`

3. **헤더 (1859-1867):**
   - `flex items-center px-4 py-3 border-b border-border-default gap-2 flex-shrink-0`
   - 💨 (1860) → `<Wind size={18} className="text-text-secondary" />` + `<span className="text-title font-bold text-text-primary">컴프레셔 점검</span>`
   - step 표시 (1861-1866 — mode !== 'from-div' 분기 그대로): `ml-auto text-caption font-semibold text-text-tertiary`

4. **본문 (1870):**
   - `flex-1 overflow-y-auto p-4 flex flex-col gap-3.5`

5. **mode !== 'from-div' 구역/라인 선택 (1872-1910):**
   - 구역 (1875-1889): segmented 패턴 (Task 2 와 동일 — `flex-1 px-2 py-2.5 rounded-md text-label font-bold transition-colors` + 활성/비활성 분기)
   - 라인 (1892-1908): 동일 segmented

6. **점검 폼 영역 (1913-2013):**
   - 컨테이너 (1914): `flex flex-col gap-3.5`

7. **현재 개소 네비 카드 — mode !== 'from-div' (1916-1942):**
   - 외곽 (1929): `bg-surface-raised rounded-md px-3 py-2.5 border border-border-default flex items-center gap-2.5 [touch-action:pan-y]` + onTouchStart/End 그대로
   - navBtnStyle 인라인 헬퍼 (1922-1926) → Tailwind 분기 인라인 또는 별도 `navBtnCls(enabled)` 헬퍼 (Task 2 와 동일 패턴)
   - ChevronLeft / ChevronRight 동일
   - 중앙 텍스트 (1934-1938): `flex-1 text-center` + `text-caption text-text-tertiary font-semibold` / `text-body-sm font-bold text-text-primary mt-0.5` / `text-caption text-text-secondary mt-0.5`

8. **현재 개소 정보 — mode === 'from-div' (1945-1951):**
   - 외곽 (1946): `bg-surface-raised rounded-md px-3 py-2.5 border border-border-default text-center`
   - 텍스트 3 줄: 동일 패턴 (Task 2 의 중앙 텍스트 클래스 그대로)

9. **입력 폼 서브 컨테이너 — position:relative (1956):**
   - `relative flex flex-col gap-3.5`
   - 주석 (1953-1955) 보존 또는 코드 정리 시 살짝 축약 OK — "260423-htx Task 5: 팝업이 ... 입력 폼만 덮도록" 의도 보존
   - InspectionRevisitPopup 호출 (1957-1966) 그대로

10. **탱크 배수 / 컴프 오일 (1968-1990) — 2 칸 grid:**
    - 컨테이너 (1969): `flex gap-2.5`
    - 좌 칸 — 탱크 배수 (1970-1982):
      - 라벨 row (1971-1977): `text-caption font-semibold text-text-tertiary mb-2 flex items-center gap-1.5`
        - "탱크 배수"
        - drainDPlus !== null && (1973): D+N 배지 — drainDPlus > 60 → `text-caption font-bold text-warning bg-warning-bg px-1.5 py-0 rounded-[4px]`, 그 외 → `text-caption font-bold text-text-tertiary bg-surface-sunken px-1.5 py-0 rounded-[4px]` (10 → 12 노안 친화)
        - drainDPlus === null && (1976): `text-caption text-text-tertiary opacity-50` "기록 없음"
      - 버튼 row (1978-1981): segmented 패턴 (Task 2 의 배수 와 동일 — 없음/있음 활성 분기)
    - 우 칸 — 컴프 오일 (1983-1989):
      - 라벨 (1984): "컴프 오일" 동일
      - 버튼 row (1985-1988): segmented — 충분함 활성=accent ring, 보충함 활성=`bg-[rgba(245,158,11,.18)] text-warning border-warning-bar` (또는 `bg-warning-bg text-warning border-[1.5px] border-warning-bar` 권장)

11. **점검 결과 picker (1992-2003):**
    - Task 2 의 §7.1 일관성 패턴 1:1 (pill + lucide + status outline+tinted bg)
    - **autoReason 배너 없음** (CompressorModal 은 자동 판단 X)

12. **특이사항 + 사진 (2005-2010):**
    - Task 2 와 동일 패턴 — textarea placeholder='특이사항 (선택)' 그대로 + PhotoButton

13. **하단 버튼 바 (2016-2029):**
    - Task 2 와 동일 패턴 — 닫기 + 저장 CTA §6.4 그라디언트
    - 저장 CTA 라벨 분기 (2022-2027 그대로 — `mode === 'from-div'` 면 '저장 후 닫기', underground/지상 분기)
    - **boxShadow: '0 4px 14px rgba(37,99,235,0.35)' (2021)** 보존 (그라디언트 CTA 의 시그니처 그림자 — §6.4 화이트리스트로 허용)

**Out of scope (이 Task 에서 건드리지 않음):**
- DivUnderPicker / DivTrendSubview / DivModal — Task 1/2 완료
- PowerPanelModal (라인 ~2034부터) — Wave 5 별도 quick 트랙
- ParkingGateModal — Wave 6 별도
- **증상 피커 신설 절대 금지**

**커밋:** `feat(260515-0l7): task 3 — CompressorModal v0.1.1 토큰 + Tailwind 변환 (from-div mode + 짝꿍 매핑 보존, 증상 피커 미도입)`
  </action>
  <verify>
    <automated>cd cha-bio-safety && npx tsc --noEmit 2>&1 | grep -E "error TS" | head -20; echo "---"; npm run build 2>&1 | tail -5; echo "---"; sed -n '1708,2030p' src/pages/InspectionPage.tsx | grep -vE '^\s*//' | grep -cE 'var\(--bg\)|var\(--bg2\)|var\(--bg3\)|var\(--bd\)|var\(--bd2\)|var\(--acl\)|var\(--t1\)|var\(--t2\)|var\(--t3\)|var\(--safe\)|var\(--warn\)|var\(--danger\)|var\(--fire\)' | xargs -I{} test {} -le 2 && echo "OLD_TOKENS: ≤2 (동적 색)" || echo "OLD_TOKENS: FAIL"; echo "---"; sed -n '1708,2030p' src/pages/InspectionPage.tsx | grep -vE '^\s*//' | grep -cE 'fontSize:\s*(9|10|11)[^0-9]|text-\[(9|10|11)px\]' | xargs -I{} test {} -eq 0 && echo "9_10_11PX_COMP: 0" || echo "9_10_11PX_COMP: FAIL"; echo "---"; sed -n '1708,2030p' src/pages/InspectionPage.tsx | grep -cE 'symptomPick|setSymptomPick' | xargs -I{} test {} -eq 0 && echo "NO_SYMPTOM_PICKER: OK" || echo "NO_SYMPTOM_PICKER: FAIL"; echo "---"; sed -n '1708,2030p' src/pages/InspectionPage.tsx | grep -cE 'COMP_PT_CP\[currentPt\.id\]' | xargs -I{} test {} -eq 1 && echo "COMP_PT_CP_PAIR: OK" || echo "COMP_PT_CP_PAIR: FAIL (짝꿍 매핑 변경)"; echo "---"; sed -n '1708,2030p' src/pages/InspectionPage.tsx | grep -cE '/api/div/(comp-inspection|logs)' | xargs -I{} test {} -ge 4 && echo "COMP_API_CALLS: ≥4 (fetch comp-inspection POST + logs 2종 + comp-inspection?location=)" || echo "COMP_API_CALLS: FAIL"; echo "---"; sed -n '1708,2030p' src/pages/InspectionPage.tsx | grep -cE "mode === 'from-div'|mode !== 'from-div'" | xargs -I{} test {} -ge 5 && echo "FROM_DIV_BRANCHES: ≥5 분기 보존" || echo "FROM_DIV_BRANCHES: FAIL"; echo "---"; grep -n "function PowerPanelModal" src/pages/InspectionPage.tsx | head -1 && echo "PowerPanelModal 시작 라인 확인 (이 라인 이후는 Wave 3 영역 아님)"</automated>
  </verify>
  <done>
    - CompressorModal (1708~2039) 본문 마크업/스타일이 v0.1.1 토큰 + Tailwind 로 교체
    - 헤더 Wind / 네비 ChevronLeft/Right / done CheckCircle2 — 이모지 0건
    - 탱크 배수 D+N 배지 (drainDPlus > 60 분기) 시안 1:1
    - 컴프 오일 segmented (충분함/보충함) 시안 1:1
    - 결과 picker §7.1 일관성 (pill + lucide + status outline+tinted bg)
    - mode === 'from-div' 분기 (zIndex 120 / 헤더 step 숨김 / 구역·라인 카드 숨김 / 저장 후 onClose 즉시) 한 줄도 변경 없음
    - **COMP_PT_CP[currentPt.id] 호출 + onSaveRecord 시그니처 한 줄도 변경 없음** (DIV/컴프 짝꿍 메모리 룰)
    - **fetch /api/div/comp-inspection POST + /api/div/logs (comp_drain / compressor 2종) body/headers/method 한 줄도 변경 없음**
    - useInspectionRevisitPopup 호출 그대로 (category: '컴프레셔')
    - InspectionRevisitPopup 부모 박스 position:relative 보존 (260423-htx 의도 — 팝업이 입력 폼만 덮음)
    - **증상 피커 신설 0건** (DIV/컴프는 자동화 5종 카테고리 아님)
    - 옛 토큰 변환 영역 내 ≤2 (동적 색 화이트리스트만)
    - 9/10/11px 폰트가 변환 영역 내 0건
    - PowerPanelModal 시작 라인 그대로 (Wave 5 영역 — 한 줄도 변경 없음)
    - tsc 0 에러, npm run build 통과
    - 단일 파일 변경
    - 커밋 hash 1개
  </done>
</task>

</tasks>

<verification>
**Plan 차원 종합 검증 (3 task 완료 후 자체 점검):**

| Gate | Expected | Method |
|------|----------|--------|
| Wave 3 변환 영역(948~2030 부근) 옛 토큰 사용 | ≤6 (동적 색 인라인 화이트리스트만 — 차트 색/border 동적/그라디언트 마스크 var()) | `sed -n '948,2030p' ... \| grep -cE 'var\(--bg\)\|var\(--bd\)\|var\(--acl\)\|var\(--t[123]\)\|var\(--(safe\|warn\|danger\|fire)\)'` |
| Wave 3 변환 영역 9/10/11px (div text, SVG attribute 제외) | 0 | `sed -n '948,2030p' ... \| grep -cE 'fontSize:\s*(9\|10\|11)[^0-9]\|text-\[(9\|10\|11)px\]'` |
| 이모지(📊/💨/✕/‹/›/✅/🚨/⚠️/📈) | 0 (lucide 전환) | `sed -n '948,2030p' ... \| grep -cE '📊\|💨\|✕\|‹\|›\|✅\|🚨\|⚠️\|📈'` |
| 증상 피커 신설(DIV/컴프 영역 내) | 0 | `sed -n '948,2030p' ... \| grep -cE 'symptomPick\|setSymptomPick\|setExtSymptomPick\|setHydrantSymptomPick\|setShutterSymptomPick\|setDamperSymptomPick'` |
| DIV_PT_CP 짝꿍 매핑 호출(DivModal) | 1 | `sed -n '1137,1707p' ... \| grep -cE 'DIV_PT_CP\[currentPt\.id\]'` |
| COMP_PT_CP 짝꿍 매핑 호출(CompressorModal) | 1 | `sed -n '1708,2030p' ... \| grep -cE 'COMP_PT_CP\[currentPt\.id\]'` |
| detectDivTrend 호출(DivModal autoReason) | 2 (p1+p2) | `sed -n '1137,1707p' ... \| grep -cE 'detectDivTrend\('` |
| timing(early/late) doubleCycle 분기 | 보존 | `grep -cE "useState<'early'\|'late'>" ...` ≥ 1 |
| mode === 'from-div' / mode !== 'from-div' 분기(CompressorModal) | ≥5 | sed range grep |
| InspectionRevisitPopup 호출 2건 (DivModal + CompressorModal) | 2 | `sed -n '948,2030p' ... \| grep -cE '<InspectionRevisitPopup'` |
| PowerPanelModal 시작 라인 변경 없음 | 보존 (시작 라인 = `function PowerPanelModal({ group, allCheckpoints ...`) | `grep -n "function PowerPanelModal" ...` |
| TypeScript compile | 0 에러 | `npx tsc --noEmit` |
| Production build | 통과 | `npm run build` 마지막 5줄 |
| Diff scope | 단일 파일 | `git diff --stat HEAD` |

**비즈니스 보존 sanity check (DIV/컴프 짝꿍 메모리 룰 강제):**
- DivModal 안 `await onSaveRecord(cpId, result, memo || '', photoKey ?? undefined).catch(() => {})` 호출 보존
- CompressorModal 안 `await onSaveRecord(cpId, result, memo || '', photoKey ?? undefined).catch(() => {})` 호출 보존
- DivModal 의 `showCompressor` overlay 안 `<CompressorModal mode="from-div" initialLocationNo={currentPt.id} ... />` 호출 보존
- detectDivTrend(series, true) / detectDivTrend(series, false) 2 호출 보존 (autoReason 자동 판단)
- DivModal 의 fetch /api/div/pressure body 17 필드 (location_no/floor/position/year/month/day/timing/pressure_1/2/set/result/drain/memo/photo_key/inspector) 그대로
- CompressorModal 의 fetch /api/div/comp-inspection body 12 필드 그대로
- fetch /api/div/logs (drain=yes / comp_drain / compressor 3종 type) 호출 모두 보존

**중요: chunk hash 자동 변경:**
- InspectionPage.tsx 본체 변경이므로 Vite 가 새 chunk hash 생성 → 배포 시 캐시 무효화 자동
- 디자인 리포 cbc7119 한정 (운영 PWA cha-bio-safety 영향 0)
</verification>

<success_criteria>
- 3 task 모두 완료 (커밋 3개)
- DivUnderPicker + DivTrendSubview + DivModal + CompressorModal 4 컴포넌트 v0.1.1 토큰 + Tailwind 변환 완료
- doubleCycle 룰(월초/월말) + DIV/컴프 짝꿍 매핑(DIV_PT_CP/COMP_PT_CP) + detectDivTrend autoReason + mode='from-div' 분기 + InspectionRevisitPopup 호출 + 모든 fetch API + state/useEffect 시그니처 **한 줄도 변경 없음**
- 옛 토큰 사용 변환 영역 내 ≤6 (동적 색 화이트리스트만)
- 9/10/11px 폰트 변환 영역 내 div text 0건 (SVG attribute 제외)
- 이모지(📊/💨/✕/‹/›/✅/🚨/⚠️/📈) 변환 영역 내 0건 (lucide 통일)
- 증상 피커 신설 0건 (DIV/컴프 자동화 5종 카테고리 아님 — 메모리 룰)
- PowerPanelModal / ParkingGateModal 본문 한 줄도 변경 없음 (Wave 5~6 별도)
- tsc 0 에러, npm run build 통과
- 단일 파일 변경 (cha-bio-safety/src/pages/InspectionPage.tsx)
- 다음 wave (Wave 5 — PowerPanelModal) 의 fixed reference point 확보
</success_criteria>

<output>
After completion, create `.planning/quick/260515-0l7-redesign-02-inspection-tsx-wave-3-div/260515-0l7-SUMMARY.md` using the standard summary template (tbj/sp7 reference). Include:

- Frontmatter: dependency_graph (requires i4r/pnr/sp7/tbj, provides Wave 3 변환), tech_stack (재사용 패턴), key_files (modified: InspectionPage.tsx), decisions (헬퍼 도입 여부 / 동적 색 화이트리스트 선택 / 짝꿍 매핑 보존 확인), metrics (duration / tasks_completed / line_count_before/after).
- One-liner (메모리 룰 — DIV/컴프 짝꿍 + doubleCycle + 증상 피커 미도입 강조)
- What was done (Task 1/2/3 각 변환 요약 + 핵심 lucide 매핑)
- Verification table (모든 grep gate + tsc + build)
- 비즈니스 로직 보존 체크 (변환 전후 비교 — DivModal/CompressorModal props/state/useEffect/handlers/fetch/onSaveRecord/오버레이 호출)
- Out of scope / 후속 트랙 (Wave 5 PowerPanelModal / Wave 6 ParkingGateModal)
- Self-Check PASSED 표
</output>
