---
phase: 260518-9zw
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cha-bio-safety/docs/redesign-context/12-staff-service/sketch/01-mobile-shell-sketch.html
autonomous: true
requirements:
  - W1-SHELL
  - W1-CHROME
  - W1-REGION-STACK
  - W1-BOTTOMNAV

must_haves:
  truths:
    - "사용자는 다크/라이트 모드 393px 모바일 frame 2개를 side-by-side 로 본다"
    - "각 frame 안에 App GlobalHeader (48px, 햄버거 + 타이틀 '연차 및 식사' + 설정톱니) 가 보인다"
    - "헤더 아래 7개 region placeholder (요약/달력/범례/식단3종/주말식대/식단PDF/휴가FAB) 가 UI-SPEC §4.2 순서로 세로 stack 한다"
    - "각 region 은 dashed border + caption 라벨만 있는 빈 박스 (실제 컴포넌트는 W2+ 에서 채움)"
    - "frame 하단에 App BottomNav (5탭: 대시보드/점검/QR/조치/승강기) 가 fixed-position 흉내로 보인다. QR 은 가운데 큰 button"
    - "page-padding 16px 가 헤더와 첫 region 사이 + region들 좌우에 명백히 보인다"
    - "section-gap 24px 가 region 사이에 명백히 보인다"
    - "이모지 0건 / status-fire 토큰 0건 / 9·10·11px 폰트 0건 검증 grep 통과"
  artifacts:
    - path: "cha-bio-safety/docs/redesign-context/12-staff-service/sketch/01-mobile-shell-sketch.html"
      provides: "W1 모바일 shell sketch — chrome + region placeholder + BottomNav, dark+light side-by-side"
      contains: ":root, [data-theme='light'], frame-mobile, mock-header, mock-bottomnav, region-placeholder"
  key_links:
    - from: ":root + [data-theme='light']"
      to: "verbatim tokens.css 값 (surface 5단계 / text 5단계 / border 3단계 / accent 3단계 / status 5×3단계 / duty 4종 / space-1~8 / radius-{sm,md,lg,pill})"
      via: "두 모드 block 두 번 정의, 각 frame 에 data-theme attribute 로 분기"
      pattern: "(?s):root\\s*\\{[^}]*--surface-page.*--surface-raised.*--surface-sunken"
    - from: ".mock-header"
      to: "GlobalHeader.tsx 시각 매칭 (height 48 / padding 0 12 / 32×32 surface-sunken icon-btn / title 13px bold + text-primary 단, 헤더 배경은 chrome-rules §1 룰에 따라 var(--surface-page) 로 변경)"
      via: "redesign 의도 — chrome 3-layer 룰 (헤더=page, wrapper=raised, 본문=page) 적용 시 헤더 톤 변경 검증"
      pattern: "background:\\s*var\\(--surface-page\\).*border-bottom.*var\\(--border-default\\)"
    - from: "7개 region placeholder"
      to: "UI-SPEC §4.2 모바일 stack 순서 (summary-row → calendar → legend → menu-cards → weekend-allowance → pdf-upload → bottomsheet-trigger)"
      via: "세로 flex column + gap var(--space-6)"
      pattern: "region\\.summary-row.*region\\.calendar.*region\\.legend.*region\\.menu-cards.*region\\.weekend-allowance.*region\\.pdf-upload.*region\\.bottomsheet-trigger"
---

<objective>
12-staff-service (StaffServicePage) 재디자인 sketch wave **W1** 을 단일 HTML 파일로 작성한다.

Purpose: UI-SPEC §12 의 W1 검증 포인트 (layout 흐름, surface 계층, padding) 를 사용자에게 시각으로 보여주기. 모바일 단일컬럼 외곽 + App GlobalHeader/BottomNav 자리 + region placeholder 세로 stack 만 표시. 구체 컴포넌트 (달력 grid / 카드 시안 / BottomSheet) 는 W2+ 에서 별도 wave 로 진행.

Output: `cha-bio-safety/docs/redesign-context/12-staff-service/sketch/01-mobile-shell-sketch.html` — 다크/라이트 393px frame side-by-side 단일 HTML.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@cha-bio-safety/docs/redesign-context/12-staff-service/UI-SPEC.md
@cha-bio-safety/docs/redesign-context/12-staff-service/tokens.css
@cha-bio-safety/docs/redesign-context/12-staff-service/typography.css
@cha-bio-safety/docs/redesign-context/00-design-context/inspection-modal-chrome-rules.md
@cha-bio-safety/docs/redesign-context/09-extinguishers/sketch/chrome-sketch.html
@cha-bio-safety/docs/redesign-context/11-div/sketch/01-header-sketch.html

<interfaces>
<!-- 이 sketch 가 시각화하는 App chrome 의 verbatim 사양. 추측 금지, 아래 값을 그대로 사용. -->

**App GlobalHeader (src/components/GlobalHeader.tsx — 본 sketch chrome 3-layer 룰로 헤더 배경만 page 로 교체):**
```
height: 48
padding: 0 12px
background: var(--bg2)  ← 본 sketch 에서는 var(--surface-page) 로 교체 (chrome-rules §1 룰)
border-bottom: 1px solid var(--bd)  ← 본 sketch 에서는 1px solid var(--border-default)
flex-shrink: 0
display: flex
align-items: center

leftSlot (햄버거 버튼):
  width: 32, height: 32, border-radius: 7px
  background: var(--bg3)  ← var(--surface-sunken)
  border: none
  color: var(--t2)  ← var(--text-secondary)
  display: flex, items center, justify center
  cursor: pointer
  svg 15×15 stroke-width 2 stroke var(--t2) viewBox 0 0 24 24
  path: M4 6h16M4 12h16M4 18h16  (3줄 햄버거)

title (가운데 span):
  flex: 1
  text-align: center (rightSlot 없을 때) / left (있을 때)
  font-size: 13, font-weight: 700, color: var(--t1)  ← var(--text-primary)
  margin-left: 0 (rightSlot 없을 때) / 8 (있을 때)
  본 sketch 텍스트: "연차 및 식사"

rightSlot (이 페이지는 톱니 사용):
  width: 32, height: 32, border-radius: 7px
  background: var(--surface-sunken)
  border: none, color: var(--text-secondary)
  svg 15×15 stroke-width 1.8 (톱니는 1.8) viewBox 0 0 24 24
  path: 09 chrome-sketch.html line 188-191 의 톱니 svg 그대로 인용 가능
```

**App BottomNav (src/components/BottomNav.tsx — IS_ANDROID 분기 무시, 다크모드 베이스 값만 사용):**
```
position: fixed (sketch 에서는 frame 안 absolute 흉내)
bottom: 0, left: 0, right: 0
height: calc(54px + var(--sab, 0px))  ← sketch 에서 54px 고정
background: rgba(22,27,34,0.97)  ← sketch 에서는 var(--surface-raised) 로 표현 (다크 frame)
border-top: 1px solid var(--bd)  ← var(--border-default)
display: flex
justify-content: space-around
align-items: center

탭 5개 (key | label | path):
1. dashboard | 대시보드 | /dashboard
2. inspection | 점검 | /inspection
3. qr | QR 스캔 | /inspection/qr   ← 가운데 특수 버튼 (큰 그라디언트 원)
4. remediation | 조치 | /remediation
5. elevator | 승강기 | /elevator

일반 탭 (1,2,4,5):
  display flex, flex-direction column, items center, gap 2px
  padding 3px 0, border none, background none
  color: 비활성 var(--text-tertiary), 활성 var(--accent)
  icon container: width 21, height 21
  label: font-size 12 (sketch 에서는 9.5 → 12 로 상향, UI-SPEC §1.1 노안 마지노선 12 룰)
        font-weight 500 (비활성) / 700 (활성)
        color: 비활성 var(--text-tertiary), 활성 var(--accent)

QR 가운데 탭:
  margin-top: -14 (위로 튀어나옴)
  inner div: width 50, height 50, border-radius 14
  background: linear-gradient(135deg, #1d4ed8, #0ea5e9)
  box-shadow: 0 4px 16px rgba(37,99,235,0.55)
  inner svg: 22×22 stroke white
  label "QR 스캔" font-size 12 (역시 9.5 → 12 상향) color var(--accent) font-weight 700

본 sketch 의 활성 탭: 이 페이지(/staff-service)는 BottomNav 5탭 어디에도 매칭되지 않는다 (라우트는 /staff-service, ITEMS 에 staff-service 없음). 따라서 활성 탭 없음 — 전부 비활성 회색으로 표시한다.
```

**UI-SPEC §4.2 모바일 layout regions (verbatim 순서, 위→아래):**
```
1. region.summary-row             — 요약 카드 row (4종)
2. region.calendar                — 7×6 달력 grid
3. region.legend                  — 범례 row (duty 4종 / 휴가 카테고리 / 공휴일 / 토일)
4. region.menu-cards              — 식단 카드 3종 (중식 A / 중식 B / 석식)
5. region.weekend-allowance       — 주말식대 stat card
6. region.pdf-upload              — 식단 PDF 업로드 dropzone
7. region.bottomsheet-trigger     — 휴가 등록 FAB / trigger 위치 표시
```

**Spacing 토큰 (UI-SPEC §2.2 모바일 값):**
```
--page-padding: 16px   ← 페이지 좌우 padding (region 들 좌우)
--section-gap: 24px    ← region 사이 세로 간격
--card-padding: 14px   ← (W2+ 에서 사용, W1 에서는 placeholder 안쪽 14px 시뮬레이션)
```

**Token 인용 룰 (tokens.css 에서 verbatim 카피):**
- 다크 모드 (`:root, [data-theme="dark"]`) → tokens.css line 16~69 전체 카피
- 라이트 모드 (`[data-theme="light"]`) → tokens.css line 74~119 전체 카피
- Spacing primitives + component (테마 무관) → tokens.css line 124~146
- Radius → tokens.css line 167~172
- **alias 토큰 (line 177~197) 은 카피 금지** — `--bg/--bg2/--bg3/--bd/--t1/2/3/--acl/--c-day` 등은 본 sketch 에서 사용 금지 (UI-SPEC §0.1 "옛 var() 토큰 0건" 룰)

**Typography 룰 (typography.css):**
- 본 sketch 는 Pretendard CDN import 한 줄: `@import url('https://cdn.jsdelivr.net/gh/orioncactus/pretendard@v1.3.9/dist/web/variable/pretendardvariable.min.css');` (또는 link rel="stylesheet")
- W1 단계에서는 region label 만 표시 → `.text-caption` (12px line-height 1.5 weight 400) 만 정의해도 충분. 다른 7단계는 W2+ 에서 추가.
- 9·10·11px 사용 금지 (UI-SPEC §1.1 노안 룰)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: W1 모바일 shell sketch HTML 단일 파일 작성</name>
  <files>cha-bio-safety/docs/redesign-context/12-staff-service/sketch/01-mobile-shell-sketch.html</files>
  <action>
sketch 디렉토리는 이미 존재 (`ls cha-bio-safety/docs/redesign-context/12-staff-service/sketch/`). 단일 HTML5 파일을 새로 작성한다.

**HTML 구조 (의무):**

```
<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>12 연차 및 식사 — sketch W1 모바일 shell</title>
  <style>
    /* 1) tokens.css verbatim 카피 (line 16~69 다크 + line 74~119 라이트 + line 124~146 spacing + line 167~172 radius) */
    /* 2) 본 sketch 전용 frame / mock-header / mock-bottomnav / region-placeholder / .text-caption */
  </style>
</head>
<body>
  <h1>12 · 연차 및 식사 (StaffServicePage) — sketch W1 모바일 shell</h1>
  <p class="lead">W1 검증 포인트 (UI-SPEC §12): layout 흐름 / surface 계층 / padding. region 7개 placeholder 세로 stack + App chrome (GlobalHeader + BottomNav) 자리 표시. 실제 컴포넌트는 W2~W5 에서 채움.</p>

  <div class="frame-row">
    <!-- VP1 다크 -->
    <div>
      <div class="vp-label">VP1 · 모바일 다크 · 393×844</div>
      <div class="frame-mobile" data-theme="dark">
        <header class="mock-header">…햄버거 + "연차 및 식사" 타이틀 + 톱니…</header>
        <main class="page-stack">
          <div class="region-placeholder"><span class="region-label">region.summary-row</span><span class="region-hint">요약 카드 row (4종) — W3 sketch</span></div>
          <div class="region-placeholder"><span class="region-label">region.calendar</span><span class="region-hint">7×6 달력 grid — W2 sketch</span></div>
          <div class="region-placeholder"><span class="region-label">region.legend</span><span class="region-hint">범례 row — W3 sketch</span></div>
          <div class="region-placeholder"><span class="region-label">region.menu-cards</span><span class="region-hint">식단 카드 3종 — W4 sketch</span></div>
          <div class="region-placeholder"><span class="region-label">region.weekend-allowance</span><span class="region-hint">주말식대 stat card — W4 sketch</span></div>
          <div class="region-placeholder"><span class="region-label">region.pdf-upload</span><span class="region-hint">식단 PDF 업로드 dropzone — W4 sketch</span></div>
          <div class="region-placeholder"><span class="region-label">region.bottomsheet-trigger</span><span class="region-hint">휴가 등록 FAB / trigger — W5 sketch (BottomSheet)</span></div>
        </main>
        <nav class="mock-bottomnav">…5개 탭 (대시보드/점검/QR 가운데/조치/승강기), 전부 비활성…</nav>
      </div>
    </div>

    <!-- VP2 라이트 (동일 구조, data-theme="light") -->
    <div>
      <div class="vp-label">VP2 · 모바일 라이트 · 393×844</div>
      <div class="frame-mobile" data-theme="light"> … 동일 구조 … </div>
    </div>
  </div>

  <div class="rules"> … 적용 룰 + 금지 룰 + 검증 포인트 … </div>
</body>
</html>
```

**구체 사양 (각 파트):**

A) `:root` token block — tokens.css 의 line 16~69 (다크), line 74~119 (라이트), line 124~146 (spacing primitives + component spacing — 테마 무관), line 167~172 (radius) 를 **verbatim** 카피. line 177~197 의 alias 토큰은 카피 금지. spacing 의 데스크톱 분기 `@media (min-width: 768px)` block (tokens.css line 149~162) 은 카피 필요 — 본 sketch 가 데스크톱 뷰포트로 열릴 때도 토큰이 일관되게 동작해야 함.

B) Body 외곽 (raw hex 예외 영역):
```
body {
  margin: 0;
  padding: 32px;
  background: #0f1218;  /* raw hex 예외 — 페이지 외곽 만 */
  color: var(--text-secondary);  /* 이후 본문은 var(--...) 만 사용 */
  font-family: 'Pretendard Variable', 'Pretendard', system-ui, -apple-system, sans-serif;
  line-height: 1.55;
}
* { box-sizing: border-box; }
```
Pretendard CDN: `<link href="https://cdn.jsdelivr.net/gh/orioncactus/pretendard@v1.3.9/dist/web/variable/pretendardvariable.min.css" rel="stylesheet" />` (head 안).

C) `.frame-row` — 두 frame side-by-side:
```
.frame-row {
  display: flex;
  flex-wrap: wrap;
  gap: 32px;
  align-items: flex-start;
}
.vp-label {
  font-size: 13px;
  color: var(--text-tertiary);
  margin-bottom: 8px;
}
```

D) `.frame-mobile` (393px frame, raw hex 예외 #2a2f3a 보더만):
```
.frame-mobile {
  width: 393px;
  background: var(--surface-page);
  color: var(--text-primary);
  border: 1px solid #2a2f3a;  /* raw hex 예외 — frame border 만 */
  border-radius: 12px;
  overflow: hidden;
  display: flex;
  flex-direction: column;
  /* 높이는 내용에 맞춤 — 7 region + chrome 합산 */
}
```

E) `.mock-header` (UI-SPEC chrome-rules §1+§2 적용 — 헤더는 page 톤):
```
.mock-header {
  display: flex;
  align-items: center;
  height: 48px;
  padding: 0 12px;
  background: var(--surface-page);  /* chrome-rules §1: 헤더 = page (NOT raised) */
  border-bottom: 1px solid var(--border-default);
  flex-shrink: 0;
}
.mock-header .icon-btn {
  width: 32px; height: 32px;
  flex-shrink: 0;
  border-radius: var(--radius-sm);  /* 8px — 본 sketch 는 8px 통일 (App 의 7px 는 미세차이) */
  background: var(--surface-sunken);
  border: none;
  color: var(--text-secondary);
  display: inline-flex; align-items: center; justify-content: center;
  cursor: pointer;
}
.mock-header .icon-btn svg {
  width: 15px; height: 15px;
  stroke: var(--text-secondary);
  fill: none;
  stroke-linecap: round;
  stroke-linejoin: round;
  /* stroke-width: 햄버거=2, 톱니=1.8 — 인라인 attribute 로 설정 */
}
.mock-header .title {
  flex: 1;
  margin: 0 8px;
  text-align: center;
  font-size: 13px;
  font-weight: 700;
  color: var(--text-primary);
  line-height: 1;
}
```

`.mock-header` 안 내용:
- 왼쪽: `<button class="icon-btn" aria-label="메뉴 열기"><svg viewBox="0 0 24 24" stroke-width="2"><path d="M4 6h16M4 12h16M4 18h16"/></svg></button>` (햄버거 3줄, GlobalHeader.tsx line 30-32 verbatim)
- 가운데: `<span class="title">연차 및 식사</span>`
- 오른쪽: `<button class="icon-btn" aria-label="설정"><svg viewBox="0 0 24 24" stroke-width="1.8">…톱니 svg…</svg></button>` (톱니 svg 는 09 chrome-sketch.html line 188-191 의 settings svg path 를 그대로 인용. circle cx=12 cy=12 r=3 + path M19.4 15… )

F) `.page-stack` (region 7개를 세로 stack — 본문 영역):
```
.page-stack {
  flex: 1;
  display: flex;
  flex-direction: column;
  gap: var(--section-gap);  /* 모바일 24px */
  padding: var(--page-padding);  /* 모바일 16px — 좌우+상하 */
  background: var(--surface-page);  /* chrome-rules §1: 본문 = page */
  overflow-y: auto;
  /* BottomNav 하단 갭 — height 54 + 약간의 여유 */
  padding-bottom: calc(var(--page-padding) + 54px + 8px);
}
```

G) `.region-placeholder` (7개 빈 박스 — dashed 보더 + caption 라벨):
```
.region-placeholder {
  /* placeholder 임을 시각으로 알리는 점선 dashed 보더 */
  border: 1.5px dashed var(--border-strong);
  border-radius: var(--radius-md);
  padding: var(--card-padding);  /* 모바일 14px */
  min-height: 72px;
  display: flex;
  flex-direction: column;
  justify-content: center;
  gap: 4px;
  background: transparent;  /* surface 톤 분리는 W2+ 에서 raised 카드로 채우기 */
}
.region-label {
  font-size: 12px;  /* text-caption */
  font-weight: 600;
  color: var(--text-secondary);
  font-family: monospace;  /* 코드 자리표시 felt — region.foo 같은 자리표시 */
  line-height: 1.5;
}
.region-hint {
  font-size: 12px;
  font-weight: 400;
  color: var(--text-tertiary);
  line-height: 1.5;
}
```

**region 7개 순서 (UI-SPEC §4.2 verbatim, 절대 변경 금지):**
1. `region.summary-row` — 힌트: "요약 카드 row (4종) — W3 sketch"
2. `region.calendar` — 힌트: "7×6 달력 grid — W2 sketch"
3. `region.legend` — 힌트: "범례 row — W3 sketch"
4. `region.menu-cards` — 힌트: "식단 카드 3종 — W4 sketch"
5. `region.weekend-allowance` — 힌트: "주말식대 stat card — W4 sketch"
6. `region.pdf-upload` — 힌트: "식단 PDF 업로드 dropzone — W4 sketch"
7. `region.bottomsheet-trigger` — 힌트: "휴가 등록 FAB / trigger — W5 sketch (BottomSheet)"

각 region 의 hint 텍스트는 **위 그대로 사용** (이모지 0건). region 6 "bottomsheet-trigger" 만 `border: 1.5px dashed var(--accent);` 로 살짝 강조 — 사용자가 BottomSheet 진입점 위치 인지를 돕는다.

H) `.mock-bottomnav` (5탭, BottomNav.tsx 시각 매칭 — frame 안에 absolute 흉내):
```
.mock-bottomnav {
  position: absolute;
  bottom: 0; left: 0; right: 0;
  height: 54px;
  background: var(--surface-raised);  /* 다크 frame 의 rgba(22,27,34,0.97) 시각 근사 — sketch 단순화 */
  border-top: 1px solid var(--border-default);
  display: flex;
  justify-content: space-around;
  align-items: center;
  box-sizing: border-box;
}
```
`.frame-mobile` 에 `position: relative` 추가 필요 (mock-bottomnav 가 frame 안에서 absolute 동작).

탭 5개 (verbatim BottomNav.tsx ITEMS):
1. 대시보드 (svg 집 + 1.8 stroke) — 비활성
2. 점검 (svg 클립보드 + 체크) — 비활성
3. **QR 스캔** — 가운데 큰 그라디언트 원:
   ```
   margin-top: -14px;  (위로 튀어나옴)
   inner div: 50×50 border-radius 14
   background: linear-gradient(135deg, #1d4ed8, #0ea5e9)  /* raw hex 예외 — BottomNav.tsx 의 그라디언트 verbatim 보존 */
   box-shadow: 0 4px 16px rgba(37,99,235,0.55)
   inner svg 22×22 stroke white  (QR 4사각형)
   label "QR 스캔" font-size 12 weight 700 color var(--accent)
   ```
   **주의:** 이 페이지의 라우트는 `/staff-service` 인데 BottomNav.ITEMS 에 staff-service 가 없음 → 활성 탭 없음. QR 도 비활성 상태로 표시 (그라디언트 자체는 항상 활성 색 — 그게 BottomNav 의 컨벤션). 텍스트 라벨 색은 비활성일 때도 `var(--accent)` (BottomNav.tsx line 81 verbatim).
4. 조치 (svg 렌치) — 비활성
5. 승강기 (svg 엘리베이터) — 비활성

일반 탭 (1,2,4,5) 마크업:
```
<button class="bn-tab">
  <div class="bn-icon"><svg …></svg></div>
  <span class="bn-label">대시보드</span>
</button>
```
스타일:
```
.bn-tab {
  display: flex; flex-direction: column; align-items: center; gap: 2px;
  padding: 3px 0; border: none; background: none; cursor: pointer;
  color: var(--text-tertiary);  /* 비활성 */
}
.bn-icon { width: 21px; height: 21px; }
.bn-icon svg { width: 21px; height: 21px; stroke: var(--text-tertiary); fill: none; stroke-width: 1.8; stroke-linecap: round; stroke-linejoin: round; }
.bn-label {
  font-size: 12px;  /* UI-SPEC §1.1: BottomNav 의 9.5px → 12px 노안 마지노선 상향 */
  font-weight: 500;
  color: var(--text-tertiary);
  line-height: 1;
}
```

각 탭의 svg path (BottomNav.tsx 에서 verbatim 추출):
- 대시보드: `<path d="M3 12l2-2m0 0l7-7 7 7M5 10v10a1 1 0 001 1h3m10-11l2 2m-2-2v10a1 1 0 01-1 1h-3m-6 0a1 1 0 001-1v-4a1 1 0 011-1h2a1 1 0 011 1v4a1 1 0 001 1m-6 0h6"/>`
- 점검: `<path d="M9 5H7a2 2 0 00-2 2v12a2 2 0 002 2h10a2 2 0 002-2V7a2 2 0 00-2-2h-2M9 5a2 2 0 002 2h2a2 2 0 002-2M9 5a2 2 0 012-2h2a2 2 0 012 2m-6 9l2 2 4-4"/>`
- 조치 (렌치): `<path d="M14.7 6.3a1 1 0 000 1.4l1.6 1.6a1 1 0 001.4 0l3.77-3.77a6 6 0 01-7.94 7.94l-6.91 6.91a2.12 2.12 0 01-3-3l6.91-6.91a6 6 0 017.94-7.94l-3.76 3.76z"/>`
- 승강기: `<rect x="5" y="2" width="14" height="20" rx="2" /><path d="M9 9l3-3 3 3M9 15l3 3 3-3"/>`

I) Rule box (frame 들 아래) — self-documentation:
```
<div class="rules">
  <h2>적용 룰 — W1 모바일 shell</h2>
  <p>UI-SPEC §3 surface 계층 + §4.2 모바일 region stack + chrome-rules §1 3-layer 규칙 단일 source.</p>
  <h3>chrome 3-layer 룰 (헤더 = page, NOT raised)</h3>
  <ul>
    <li>헤더 배경: var(--surface-page) — 본 sketch 에서 변경. App GlobalHeader.tsx 의 var(--bg2)=raised 와 의도적으로 다름. chrome-rules §1 적용으로 헤더+본문 = page / wrapper만 = raised 3계층 정착.</li>
    <li>본문 (page-stack): var(--surface-page) — 헤더와 동일 톤. region placeholder 안쪽이 raised 카드로 채워질 때 (W2+) 비로소 layer 분리 가시.</li>
    <li>BottomNav: var(--surface-raised) — 본문 위 떠 있는 chrome 으로서 한 단계 raised.</li>
  </ul>
  <h3>spacing 룰</h3>
  <ul>
    <li>page-padding 16px (var(--page-padding)) — 헤더와 첫 region 사이 + region 좌우.</li>
    <li>section-gap 24px (var(--section-gap)) — region 사이 세로 간격.</li>
    <li>BottomNav 가림 회피: page-stack padding-bottom = page-padding + 54 + 8.</li>
  </ul>
  <h3>region stack 순서 (UI-SPEC §4.2)</h3>
  <ol>
    <li>region.summary-row</li>
    <li>region.calendar</li>
    <li>region.legend</li>
    <li>region.menu-cards</li>
    <li>region.weekend-allowance</li>
    <li>region.pdf-upload</li>
    <li>region.bottomsheet-trigger (accent 보더로 강조 — BottomSheet 진입점)</li>
  </ol>
  <h3>금지 룰 (verify gate)</h3>
  <ul>
    <li>이모지 0건 — 모든 region 라벨/힌트/룰 박스 텍스트에서 일체 사용 금지</li>
    <li>옛 alias 토큰 (--bg, --bg2, --bg3, --bg4, --bd, --bd2, --t1, --t2, --t3, --acl, --info, --safe, --warn, --danger, --fire, --c-day, --c-night, --c-off, --c-leave) 0건</li>
    <li>status-fire 토큰 0건 — 이 페이지 (StaffServicePage) 는 §3.4.1 룰로 fire 미사용</li>
    <li>9·10·11px 폰트 0건 — typography.css §노안 마지노선 12 룰</li>
    <li>raw hex / rgba 0건 — :root 토큰 정의 + body 외곽 (#0f1218) + frame border (#2a2f3a) + QR 그라디언트 (#1d4ed8/#0ea5e9 + box-shadow rgba) 만 예외</li>
    <li>그라디언트 일체 — QR 가운데 버튼만 예외 (BottomNav.tsx verbatim)</li>
    <li>그림자 / blur / backdrop-filter — QR 가운데 box-shadow 만 예외</li>
    <li>lg:* / @media (max-width:…) 모바일 분기 — W1 은 모바일 frame 만 (393px 고정). 데스크톱 frame 은 W6 sketch.</li>
  </ul>
  <h3>다음 wave</h3>
  <ul>
    <li>W2 — 02-calendar-grid-sketch.html (달력 헤더 + 7×6 grid + 셀 모든 상태)</li>
    <li>W3 — 03-legend-summary-sketch.html (범례 + 4종 요약 stat card)</li>
    <li>W4 — 04-menu-cards-sketch.html (식단 3종 + PDF dropzone + 주말식대)</li>
    <li>W5 — 05-bottomsheet-sketch.html (BottomSheet 전체 — 휴가 등록 trigger 의 실제 모습)</li>
  </ul>
</div>
```
rule box 스타일:
```
.rules { margin-top: 40px; padding: 16px 20px; background: rgba(255,255,255,0.03); border: 1px solid var(--border-default); border-radius: 8px; max-width: 860px; }
.rules h2 { font-size: 16px; margin: 0 0 6px 0; color: var(--text-primary); }
.rules h3 { font-size: 14px; margin: 16px 0 6px 0; color: var(--text-primary); }
.rules p { font-size: 13px; color: var(--text-tertiary); margin: 0 0 10px 0; }
.rules ul, .rules ol { margin: 0; padding-left: 20px; font-size: 13px; color: var(--text-secondary); }
.rules li { margin-bottom: 4px; }
```

**작업 절차:**

1. Read tokens.css (이미 plan 의 `<interfaces>` 에 사양 있음) 으로 line 16~69, 74~119, 124~146, 149~162 (데스크톱 분기), 167~172 를 :root + [data-theme="light"] + spacing + radius 블록으로 verbatim 카피.
2. Write `cha-bio-safety/docs/redesign-context/12-staff-service/sketch/01-mobile-shell-sketch.html` — 위 구조 단일 파일.
3. **자체 검수 (Write 직후, executor 가 반드시 실행):**
   ```
   grep -nE '😀|😁|😂|🎯|✨|🔥|⚠|✅|❌|⭐|💡|📌|📝|📂|📁|🗂|📋|🔧|🔨|🔍|🚨|🛠' cha-bio-safety/docs/redesign-context/12-staff-service/sketch/01-mobile-shell-sketch.html
   ```
   → 0 lines. (한글 한자 「」 같은 punctuation 은 이모지 아님 — OK.)
   ```
   grep -nE 'text-fire|bg-fire|status-fire' cha-bio-safety/docs/redesign-context/12-staff-service/sketch/01-mobile-shell-sketch.html
   ```
   → 0 lines.
   ```
   grep -nE 'font-size:\s*(9|10|11)px' cha-bio-safety/docs/redesign-context/12-staff-service/sketch/01-mobile-shell-sketch.html
   ```
   → 0 lines.
   ```
   grep -nE -- '--bg\b|--bg2|--bg3|--bg4|--bd\b|--bd2|--t1\b|--t2\b|--t3\b|--acl\b|--c-day|--c-night|--c-off|--c-leave' cha-bio-safety/docs/redesign-context/12-staff-service/sketch/01-mobile-shell-sketch.html
   ```
   → 0 lines.
   ```
   grep -cE 'region\.(summary-row|calendar|legend|menu-cards|weekend-allowance|pdf-upload|bottomsheet-trigger)' cha-bio-safety/docs/redesign-context/12-staff-service/sketch/01-mobile-shell-sketch.html
   ```
   → 최소 14 (7 region × 2 frame = 14 — 다크/라이트 frame 모두 region 명 등장).
   ```
   grep -nE '#[0-9a-fA-F]{6}' cha-bio-safety/docs/redesign-context/12-staff-service/sketch/01-mobile-shell-sketch.html | grep -vE ':root|\[data-theme=|/\*|body\s*\{|frame-mobile|linear-gradient|box-shadow'
   ```
   → 0 lines (예외 영역 외 raw hex 없음).
4. 모든 grep PASS 면 task done.

**금지 (deviation 사고 방지):**
- 이모지 도입 일체 금지 — region 라벨에 "📅 달력" 같은 표기 절대 X.
- region 순서 변경 금지 — UI-SPEC §4.2 의 모바일 stack 순서 그대로.
- region placeholder 안에 실제 컴포넌트 (달력 grid / 카드 시안 / 식단표) 그리기 금지 — W1 은 외곽만, 안쪽은 빈 dashed 박스.
- 데스크톱 frame (1280px) 추가 금지 — W6 sketch 의 책임.
- BottomNav 활성 탭 도입 금지 — 이 페이지는 BottomNav.ITEMS 에 없음. 전부 비활성 회색 (QR 가운데 그라디언트는 컨벤션상 항상 강조).
- 헤더 우측 슬롯에 톱니 외 다른 버튼 (「+ 새로 등록」 같은) 도입 금지 — StaffServicePage 의 GlobalHeader 는 leftSlot=햄버거 + title 만, rightSlot 없음. 그러나 sketch 에서 chrome 의 전형 모양 보여주기 위해 톱니 추가는 OK (App 의 settings 진입점이 다른 페이지에 존재하므로 chrome 컨벤션 표시 목적). 단, 사용자가 W1 컨펌 시 "톱니 빼라" 요청하면 W1.1 으로 수정.
- inline `style="..."` 사용은 OK (sketch HTML 은 Tailwind 없음). 단, **inline style 안의 색은 반드시 var(--…)** 사용. raw hex 는 위 예외 5개 영역 (body bg #0f1218, frame border #2a2f3a, QR 그라디언트 #1d4ed8/#0ea5e9, QR box-shadow rgba(37,99,235,0.55)) 만.
  </action>
  <verify>
    <automated>cd /Users/jykevin/Documents/cbc7119-design && test -f cha-bio-safety/docs/redesign-context/12-staff-service/sketch/01-mobile-shell-sketch.html && grep -cE 'region\.(summary-row|calendar|legend|menu-cards|weekend-allowance|pdf-upload|bottomsheet-trigger)' cha-bio-safety/docs/redesign-context/12-staff-service/sketch/01-mobile-shell-sketch.html | awk '$1 >= 14 {exit 0} {exit 1}' && ! grep -qE 'text-fire|bg-fire|status-fire' cha-bio-safety/docs/redesign-context/12-staff-service/sketch/01-mobile-shell-sketch.html && ! grep -qE 'font-size:\s*(9|10|11)px' cha-bio-safety/docs/redesign-context/12-staff-service/sketch/01-mobile-shell-sketch.html && ! grep -qE -- '--bg2|--bg3|--bd\b|--t1\b|--t2\b|--t3\b|--acl\b|--c-day|--c-night|--c-off|--c-leave' cha-bio-safety/docs/redesign-context/12-staff-service/sketch/01-mobile-shell-sketch.html && ! grep -qE '😀|😁|😂|🎯|✨|🔥|⚠|✅|❌|⭐|💡|📌|📝|📂|📁|🗂|📋|🔧|🔨|🔍|🚨|🛠' cha-bio-safety/docs/redesign-context/12-staff-service/sketch/01-mobile-shell-sketch.html && echo PASS</automated>
  </verify>
  <done>
    파일 `cha-bio-safety/docs/redesign-context/12-staff-service/sketch/01-mobile-shell-sketch.html` 존재. 7개 region label 14건 이상 (다크+라이트). 이모지/status-fire/9·10·11px/옛 alias 토큰 0건. 다크/라이트 frame 2개 side-by-side 로 렌더 가능 (HTML5 valid). 검증 grep 모두 PASS.
  </done>
</task>

</tasks>

<verification>
브라우저에서 `01-mobile-shell-sketch.html` 을 열면:
1. 두 모바일 frame (393px) 가 side-by-side (다크 좌 + 라이트 우).
2. 각 frame 상단에 48px GlobalHeader — 햄버거 + "연차 및 식사" + 톱니.
3. 헤더 아래 page-padding 16px 의 본문 영역.
4. 7개 region placeholder (dashed border) 가 section-gap 24px 간격으로 세로 stack — UI-SPEC §4.2 순서:
   summary-row → calendar → legend → menu-cards → weekend-allowance → pdf-upload → bottomsheet-trigger.
5. region 7 (bottomsheet-trigger) 만 accent 점선 보더로 강조.
6. frame 하단에 54px BottomNav — 5탭 (대시보드/점검/QR 가운데 그라디언트/조치/승강기), 모두 비활성 회색 (QR 컨벤션 강조 제외).
7. 라이트 모드 frame 에서 모든 토큰이 light theme 값으로 자동 전환 (배경 흰색 / 텍스트 어두운 회색 / surface raised 가 미세한 회색 등).
8. frame 들 아래 rule 박스에 chrome 3-layer / spacing / region 순서 / 금지 룰 / 다음 wave 목록.
</verification>

<success_criteria>
- 단일 HTML 파일 1개 생성 (`cha-bio-safety/docs/redesign-context/12-staff-service/sketch/01-mobile-shell-sketch.html`)
- HTML5 valid (브라우저 콘솔 에러 없이 렌더)
- 다크/라이트 frame 양쪽에서 chrome + 7 region + BottomNav 가 시각으로 분명
- 모든 검증 grep PASS (이모지 0, status-fire 0, 9·10·11px 0, alias 토큰 0, region 14+ 매치, raw hex 예외 영역 외 0)
- 사용자가 컨펌 시 W2 (달력 grid) 로 이행 가능
</success_criteria>

<output>
완료 후 `.planning/quick/260518-9zw-12-staff-service-sketch-wave-1-mobile-ch/260518-9zw-SUMMARY.md` 작성:
- 생성 파일 경로
- 적용한 chrome 3-layer 결정 (헤더 = page, App 기본값 raised 와 의도적 차이) 사용자에게 confirm 필요 여부 명시
- BottomNav 활성 탭 없음 결정 (이 페이지는 ITEMS 에 없음)
- region 7개 순서와 hint 매핑
- grep 검증 결과
- 다음 wave (W2 — 02-calendar-grid-sketch.html) 진입 가이드
</output>
