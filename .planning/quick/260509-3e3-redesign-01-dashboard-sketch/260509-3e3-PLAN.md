---
phase: 260509-3e3-redesign-01-dashboard-sketch
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cha-bio-safety/docs/redesign-context/01-dashboard/sketch/dashboard-sketch.html
autonomous: false
requirements:
  - REDESIGN-01-DASHBOARD-SKETCH
tags:
  - redesign
  - dashboard
  - design-tokens-v0.1.1
  - sketch-html

must_haves:
  truths:
    - "단일 HTML 파일을 브라우저에서 열면 모바일(375px)/데스크톱(1280px) 두 뷰포트가 동시에 보인다"
    - "라이트/다크 두 테마가 동시에 보이거나 토글 가능하다 (4-grid: mobile-light, mobile-dark, desktop-light, desktop-dark 권장)"
    - "DashboardPage.tsx의 모든 섹션(근무자 칩, 통계 4종, 배너 2종, 빠른도구 4종, 오늘 일정, 월간 도넛, 데스크톱 미니 캘린더+일정 패널)이 시안에 1:1로 등장한다"
    - "Progress Color Rule(도넛/카테고리 색바), Stat Card 좌측 3px 색바 + 위험 임계치 danger 분기, 카테고리 아이콘 회색 통일, 0% 카드 opacity 0.6 룰이 시각적으로 확인된다"
    - "월간 도넛은 가로 스크롤(overflow-x:auto)로 표현되며 flex-wrap 으로 줄바꿈하지 않는다"
    - "doubleCycle two-lap 표시가 DIV/Comp 카드에서 시각화된다 (1주차/2주차 레이어 구분)"
    - "loading / error / empty 상태가 각 섹션별로 시각적으로 표현된다 (현재 페이지에 없는 명시적 에러 UI는 신규 제안)"
    - "사용자가 시안을 열고 디자인을 확인한 뒤 approve / 수정요청 결정을 내릴 수 있다"
  artifacts:
    - path: "cha-bio-safety/docs/redesign-context/01-dashboard/sketch/dashboard-sketch.html"
      provides: "self-contained dashboard redesign sketch with mobile+desktop, light+dark, all sections"
      min_lines: 600
  key_links:
    - from: "dashboard-sketch.html"
      to: "design-system.md v0.1.1 §6 Progress Color Rule"
      via: "도넛/카테고리 진척률 색바 매핑 (0-25 danger / 25-50 warning / 50-75 info / 75-100 safe)"
      pattern: "data-progress|--status-(safe|warning|danger|info)-bar"
    - from: "dashboard-sketch.html"
      to: "design-system.md v0.1.1 §7 Stat Card"
      via: "좌측 3px 색바 + 위험 임계치 조건부 danger 적용"
      pattern: "border-left.*3px|stat-card"
    - from: "dashboard-sketch.html"
      to: "tokens.css 다크/라이트 두 모드"
      via: "토큰 인라인 + data-theme=dark / data-theme=light 두 컨테이너"
      pattern: "data-theme=|--surface-page"
    - from: "dashboard-sketch.html"
      to: "DashboardPage.tsx 모든 섹션"
      via: "코드 구조 1:1 매핑 (mock 데이터로 표현)"
      pattern: "근무자|통계|빠른도구|오늘 일정|이번 달 점검 현황|미니 캘린더"
---

<objective>
DashboardPage 재디자인 1단계 — 사용자 검토용 단일 HTML 시안을 작성한다.

**Purpose:** TSX 변환 전에 디자인 시스템 v0.1.1 룰이 실제 페이지에 어떻게 적용되는지 시각적으로 확정한다. 사용자가 모바일/데스크톱 × 라이트/다크 4가지 조합을 한 화면에서 비교하며 디자인을 승인하거나 수정 요청을 줄 수 있어야 한다.

**Output:** `cha-bio-safety/docs/redesign-context/01-dashboard/sketch/dashboard-sketch.html` — Tailwind CDN + tokens.css 인라인 + lucide CDN(또는 inline SVG) 사용한 자기-완결적 한 파일. 브라우저로 더블클릭하면 바로 보여야 한다.

**Out of scope:** TSX 변환, src/ 어떤 파일도 수정 금지, 새 공통 컴포넌트 추가 금지. 비즈니스 로직 분석은 reference 용도로만 사용.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/STATE.md

# 권위 있는 디자인 스펙 — v0.1.1 (Progress Color Rule, Stat Card, Iconography 포함)
@cha-bio-safety/docs/redesign-context/01-dashboard/design-system.md

# 토큰 카탈로그 (라이트/다크 두 모드)
@cha-bio-safety/docs/redesign-context/01-dashboard/tokens.css

# 타이포 클래스 정의
@cha-bio-safety/docs/redesign-context/01-dashboard/typography.css

# 시안에서 1:1 매핑해야 하는 현재 페이지 (697줄, 모든 섹션/상태/Android 분기 포함)
@cha-bio-safety/docs/redesign-context/01-dashboard/DashboardPage.tsx

# 공통 UI 컴포넌트 (DutyChip, RoleLabel, Donut, StatusBadge, CatBar) — 시안에서 동일 시각으로 재현
@cha-bio-safety/docs/redesign-context/01-dashboard/ui-index.tsx

# 작업 컨텍스트 (분석 + 진행 방식)
@cha-bio-safety/docs/redesign-context/01-dashboard/01-dashboard.md

<interfaces>
<!-- 시안에서 표현해야 하는 데이터 모양 (DashboardPage.tsx 에서 추출). mock 으로 채우되 누락 금지. -->

# DutyChip 룰 (ui-index.tsx)
DutyChip props: { name: string; role: 'admin'|'assistant'; duty: 'day'|'night'|'off'|'leave'|'holiday' }
- 색: --duty-day(주간) / --duty-night(당직) / --duty-off(비번) / --duty-leave(휴무)
- 연차/공가는 별도 leave-type 라벨

# Donut 룰 (ui-index.tsx)
Donut props: { progress: number(0-100); doubleCycle?: boolean; firstCyclePct?: number }
- 색: design-system v0.1.1 §6 Progress Color Rule (0-25 danger / 25-50 warning / 50-75 info / 75-100 safe)
- doubleCycle=true 면 outer ring(2주차) + inner ring(1주차) two-lap 표시

# Stat Card 4종 (DashboardPage.tsx 통계 영역)
1) 점검 미완료 — 평소 info, 임계치 초과 시 danger 색바
2) 미조치 항목 — fire 색 (조치 대기) — 1개 이상이면 danger 색바
3) 오늘 일정 — info 색바
4) 승강기 고장 — fire 색 (1개 이상이면 danger)

# 빠른 도구 카드 4종
1) 도면 점검 (FileText icon)
2) DIV 트렌드 (TrendingUp)
3) 고장 접수 (AlertTriangle, fire 색 강조)
4) 직원 서비스 (Users)

# 오늘 일정 리스트 항목 모양
{ id, title, time?: string, category?: 'inspect'|'meeting'|'training', status: 'pending'|'completed' }
- 시간 확정 / 시간 미정 분리 표시
- 항목 우측 체크 버튼으로 수동 완료 처리 (시안에서는 스타일만)

# 월간 도넛 카테고리 (대략, DashboardPage.tsx 참조)
- 자탐, 스프링클러, 옥내소화전, 소화기, 유도등, 방화셔터, 전실제연댐퍼, DIV, 컴프, 승강기, 비상콘센트, 특별피난계단, CCTV
- DIV/컴프는 doubleCycle two-lap

# 데스크톱 우측 패널
- 미니 캘린더(현재 월, 공휴일 dot 빨강, 일정 dot 파랑, 오늘 강조)
- 우측 일정 패널(오늘 일정 리스트 동일 데이터)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: dashboard-sketch.html 작성</name>
  <files>cha-bio-safety/docs/redesign-context/01-dashboard/sketch/dashboard-sketch.html</files>
  <action>
새 디렉토리 `cha-bio-safety/docs/redesign-context/01-dashboard/sketch/` 를 생성하고 그 안에 `dashboard-sketch.html` 한 파일만 작성한다.

**파일 구조:**

```
<!doctype html>
<html>
  <head>
    - meta viewport, UTF-8, title "대시보드 재디자인 시안 v0.1.1"
    - <script src="https://cdn.tailwindcss.com"></script>
    - <link href="https://fonts.googleapis.com/css2?family=Noto+Sans+KR:wght@400;500;600&display=swap" rel="stylesheet">
    - <script src="https://unpkg.com/lucide@latest"></script> (또는 inline SVG)
    - <style>
        @tokens.css 내용을 그대로 복붙 (tokens.css 파일 그대로 인라인)
        @typography.css 내용도 인라인
        body { font-family: 'Noto Sans KR', sans-serif; background: #2a2a2a; }
        .viewport-frame { border-radius: 24px; box-shadow: 0 20px 60px rgba(0,0,0,0.4); overflow: hidden; }
        .viewport-mobile { width: 375px; }
        .viewport-desktop { width: 1280px; max-width: 100%; }
        /* data-theme=light 컨테이너 안에서는 tokens.css :root 대신 [data-theme="light"] override */
      </style>
  </head>
  <body class="p-8 space-y-8">

    <!-- Header — 시안 메타 -->
    <header class="text-white max-w-[1400px] mx-auto">
      <h1 class="text-3xl font-semibold">CHA Bio Complex — 대시보드 재디자인 시안</h1>
      <p class="text-sm opacity-70">디자인 시스템 v0.1.1 · 모바일 375px / 데스크톱 1280px · 라이트/다크 동시 비교</p>
    </header>

    <!-- 4-grid: 모바일 다크 | 모바일 라이트 | 데스크톱 다크 | 데스크톱 라이트 -->
    <!-- 또는 2x2 grid (위: 모바일, 아래: 데스크톱; 좌: 다크, 우: 라이트) -->

    <section class="grid grid-cols-1 lg:grid-cols-2 gap-8 max-w-[1400px] mx-auto">

      <!-- 모바일 다크 -->
      <div class="viewport-frame viewport-mobile" data-theme="dark">
        [페이지 마크업 — 아래 마크업 명세 참조]
      </div>

      <!-- 모바일 라이트 -->
      <div class="viewport-frame viewport-mobile" data-theme="light">
        [동일 마크업, data-theme 만 변경]
      </div>

      <!-- 데스크톱 다크 — 두 컬럼 가로폭 -->
      <div class="viewport-frame viewport-desktop col-span-1 lg:col-span-2" data-theme="dark">
        [데스크톱 레이아웃 — 좌(점검현황+빠른도구) / 우(미니 캘린더+일정 sticky panel)]
      </div>

      <!-- 데스크톱 라이트 -->
      <div class="viewport-frame viewport-desktop col-span-1 lg:col-span-2" data-theme="light">
        [동일]
      </div>

    </section>

  </body>
</html>
```

**모바일 뷰포트 마크업 명세 (375px, 5행 그리드 단일 컬럼 1fr — 메모리 룰):**

순서대로:

1. **상단 헤더 바** — 좌: 햄버거 + "차바이오컴플렉스 방재팀" 로고/타이틀, 우: 알림 벨(unread dot) + 프로필 아바타
2. **오늘 근무자 칩 영역** — DutyChip 컴포넌트 시각화. 4명 표시 (주간 1 / 당직 1 / 비번 1 / 휴무 1) + 연차자 1명, 공가자 1명. ui-index.tsx 의 DutyChip 시각 그대로 (캡슐, role 라벨 포함). 탭하면 전화/문자 액션시트가 뜬다는 힌트(탭 가능 표시).
3. **배너 영역** (2개):
   - "오늘 점검 대상" 배너 (info 색 배경, 카테고리 배지들 + "점검하러 가기 →")
   - "수신반 이력 48시간" 배너 (warning 색 배경, "최근 24시간 내 자탐 이벤트 3건" 같은 mock)
4. **통계 카드 4종** (2x2 grid, gap=--card-gap):
   - 점검 미완료 (info / threshold 시 danger) — 좌측 3px 색바, 큰 숫자(text-display), 라벨(text-label)
   - 미조치 항목 (fire bar) — 위험 임계치 적용
   - 오늘 일정 (info bar)
   - 승강기 고장 (fire bar — 1대 이상이면 danger)
   - **시안에 두 변형 표시:** 정상치 4개 + danger 임계치 초과 4개를 toggle 또는 좌우 비교로 보여주기 (적어도 코멘트로 "임계치 초과 시" 변형 1개는 노출)
5. **빠른 도구 카드 4종** (2x2 grid):
   - 각 카드: 큰 lucide 아이콘(grey 통일, design-system §8 룰), 라벨, 보조 설명, 우측 화살표
   - 도면 점검(FileText) / DIV 트렌드(TrendingUp) / 고장 접수(AlertTriangle, fire 강조) / 직원 서비스(Users)
6. **오늘 일정 섹션:**
   - "시간 확정" 섹션 헤더 + 항목 카드 3개 (08:00, 14:00, 16:30 mock)
   - "시간 미정" 섹션 헤더 + 항목 카드 2개
   - 각 항목 우측 체크 버튼 (pending / completed 두 변형 표시)
   - 빈 상태 변형도 코멘트 또는 작은 영역으로 표시: "오늘 일정 없음"
7. **이번 달 점검 현황 도넛 (가로 스크롤)** — overflow-x:auto, flex(no-wrap):
   - 13개 카테고리 카드를 한 줄로
   - 각 카드: Donut 시각화 + 카테고리명 + n/m 분수 + 진척률 텍스트 + 좌측 색바(progress color rule)
   - DIV / 컴프는 doubleCycle two-lap 시각화 (outer + inner 두 ring)
   - 0% 카드는 opacity 0.6 (design-system v0.1.1 §6)
   - 빈 상태 변형도 표시 ("이번 달 점검 일정 없음" 코멘트 또는 작은 별도 블록)
8. **하단 BottomNav** — 5개 탭 (대시보드, 점검, 평면도, 직원, 더보기). height + paddingBottom 증가 패턴 (메모리 룰: bottom 또는 버튼 압축 금지). 활성 탭 강조.

**데스크톱 뷰포트 마크업 명세 (1280px, 좌:우 = 2:1):**

- 상단 헤더 바: 모바일과 동일 구조이되 가로로 펼침. SideMenu(고정 사이드바) 옵션도 시각화 가능.
- 좌측(2/3): 통계 카드 4종(가로 4-col), 빠른 도구 4종(가로 4-col), 이번 달 점검 현황 도넛(이 영역도 데스크톱은 grid-3col 또는 가로 스크롤 둘 다 시각화 — 코멘트로 "메모리: 모바일은 무조건 가로 스크롤" 명시)
- 우측 sticky panel (1/3, top: 88px): 미니 캘린더 + 오늘 일정 리스트
- 미니 캘린더: 7-col grid, 현재 월(2026-05), 토요일/일요일 색 분기, 공휴일 dot(빨강), 일정 있는 날 dot(파랑), 오늘 강조 ring

**상태 시각화 (각 섹션 옆 또는 별도 "States" 섹션):**

- **Loading:** 통계 카드 skeleton 1세트 + 도넛 skeleton 1세트 (shimmer 또는 pulse)
- **Error (NEW — 현재 페이지에 명시적 에러 없음, 신규 제안):** 통계 영역 + 도넛 영역에 "데이터를 불러올 수 없어요. [다시 시도]" 카드. 색은 danger-bg + danger-foreground.
- **Empty:** 오늘 일정 빈 / 월간 도넛 빈 두 케이스

**디자인 시스템 v0.1.1 §6~§9 룰 체크리스트 (시안에서 시각 확인 가능해야 함):**

- §6 Progress Color Rule — 도넛/색바 색이 progress 구간별로 다름 (0-25 danger / 25-50 warning / 50-75 info / 75-100 safe). 시안 카드에 다양한 progress 값 4개 이상 분포 (예: 0%, 28%, 62%, 95%).
- §7 Stat Card — 좌측 3px 색바 + 위험 임계치 조건부 danger. 두 변형(정상/임계치) 모두 노출.
- §8 카테고리 카드 — 아이콘 모두 회색 통일, 진척률은 좌측 색바로만 차별화, 0% opacity 0.6.
- §9 Iconography — Lucide 표준 아이콘 + 커스텀 SVG 6종(StairsIcon, ShutterIcon, ExitSignIcon, SmokeVentIcon, HoseReelIcon, FireExtinguisherCustom)은 카테고리에 매핑. 대시보드 직접 사용은 없으나 빠른 도구/카테고리 카드에 lucide 사용 시각 확인.

**메모리 룰 시각 코멘트 (HTML 주석으로 표시):**

- `<!-- 메모리: Dashboard 그리드 1fr 의도. minmax(0,1fr) 금지 -->`
- `<!-- 메모리: 월간 도넛 가로 스크롤 의도. flex-wrap 금지 -->`
- `<!-- 메모리: BottomNav 하단 갭은 height + paddingBottom 증가로 처리 -->`
- `<!-- 메모리: iOS PWA viewport — Android 100svh / iOS Safari 100dvh / iOS PWA 100vh -->`

**비즈니스 로직 보존 — 시각적으로:**

DashboardPage.tsx 의 모든 분기를 시안에서 노출:
- 근무자 4명 + 연차/공가 합성
- 통계 카드 4종 (정상/임계치 두 변형)
- 두 배너 (오늘 점검 / 수신반 48h)
- 빠른 도구 4종
- 오늘 일정 시간 확정/미정 분리 + completed 상태
- 월간 도넛 13 카테고리 + DIV/컴프 doubleCycle
- 데스크톱 미니 캘린더 + 우측 패널
- loading / error(신규) / empty 상태

**제약:**

- 자기-완결적 1 파일. 외부 CSS/JS 파일 금지(CDN tailwind/lucide/Google Fonts 만 허용).
- mock 데이터 한국어로(현장 컨텍스트). 직원 이름은 가공명(예: 김주간, 이당직, 박비번, 최휴무, 정연차, 한공가).
- 모든 인라인 style 은 토큰 변수 사용 (`color: var(--text-primary)` 등). hex 직접 입력 최소화 — 토큰이 부족할 때만.
- 폰트 크기는 typography.css 클래스(text-caption ~ text-display) 사용. 9/10/11px 절대 금지(메모리: 노안 친화).

**산출물 검증 시점에 자동으로 확인할 수 있는 grep gate** (verify 단계용):
- 파일 존재 + 라인 수 600+ (자기-완결 + 모든 섹션 포함하면 600 line 이상 됨이 자연스러움)
- `data-theme="dark"` 와 `data-theme="light"` 모두 등장
- viewport 4종(mobile dark, mobile light, desktop dark, desktop light) 등장
- doubleCycle / two-lap 표현 등장
- 카테고리 13종 라벨 등장(자탐, 스프링클러, 옥내소화전, 소화기, 유도등, 방화셔터, 전실제연댐퍼, DIV, 컴프, 승강기, 비상콘센트, 특별피난계단, CCTV)
- DutyChip 시각 4종(day/night/off/leave) + leave/holiday 변형 등장
- BottomNav 5탭 등장
- 미니 캘린더 등장
- loading / error / empty 변형 등장
  </action>
  <verify>
    <automated>
test -f cha-bio-safety/docs/redesign-context/01-dashboard/sketch/dashboard-sketch.html && \
LINES=$(wc -l < cha-bio-safety/docs/redesign-context/01-dashboard/sketch/dashboard-sketch.html) && \
[ "$LINES" -ge 600 ] && \
grep -q 'data-theme="dark"' cha-bio-safety/docs/redesign-context/01-dashboard/sketch/dashboard-sketch.html && \
grep -q 'data-theme="light"' cha-bio-safety/docs/redesign-context/01-dashboard/sketch/dashboard-sketch.html && \
grep -q -i 'doubleCycle\|two-lap\|outer-ring\|inner-ring\|2주차' cha-bio-safety/docs/redesign-context/01-dashboard/sketch/dashboard-sketch.html && \
grep -qE '자탐|스프링클러|옥내소화전|소화기|유도등|방화셔터|전실제연댐퍼' cha-bio-safety/docs/redesign-context/01-dashboard/sketch/dashboard-sketch.html && \
grep -qE 'DIV|컴프|승강기|비상콘센트|특별피난계단|CCTV' cha-bio-safety/docs/redesign-context/01-dashboard/sketch/dashboard-sketch.html && \
grep -qE 'duty-day|주간' cha-bio-safety/docs/redesign-context/01-dashboard/sketch/dashboard-sketch.html && \
grep -qE 'duty-night|당직' cha-bio-safety/docs/redesign-context/01-dashboard/sketch/dashboard-sketch.html && \
grep -qE 'duty-off|비번' cha-bio-safety/docs/redesign-context/01-dashboard/sketch/dashboard-sketch.html && \
grep -qE 'duty-leave|휴무' cha-bio-safety/docs/redesign-context/01-dashboard/sketch/dashboard-sketch.html && \
grep -qE '연차|공가' cha-bio-safety/docs/redesign-context/01-dashboard/sketch/dashboard-sketch.html && \
grep -qE 'BottomNav|bottom-nav|하단 네비|nav-bar' cha-bio-safety/docs/redesign-context/01-dashboard/sketch/dashboard-sketch.html && \
grep -qiE 'loading|skeleton|로딩' cha-bio-safety/docs/redesign-context/01-dashboard/sketch/dashboard-sketch.html && \
grep -qiE 'error|다시 시도|에러|실패' cha-bio-safety/docs/redesign-context/01-dashboard/sketch/dashboard-sketch.html && \
grep -qiE '없음|empty|empty-state' cha-bio-safety/docs/redesign-context/01-dashboard/sketch/dashboard-sketch.html && \
grep -qE '미니 캘린더|mini-calendar|calendar' cha-bio-safety/docs/redesign-context/01-dashboard/sketch/dashboard-sketch.html && \
grep -qE '오늘 점검|수신반|48' cha-bio-safety/docs/redesign-context/01-dashboard/sketch/dashboard-sketch.html && \
grep -qE '도면 점검|DIV 트렌드|고장 접수|직원 서비스' cha-bio-safety/docs/redesign-context/01-dashboard/sketch/dashboard-sketch.html && \
echo "OK lines=$LINES"
    </automated>
  </verify>
  <done>
- `cha-bio-safety/docs/redesign-context/01-dashboard/sketch/dashboard-sketch.html` 파일이 600+ 라인으로 존재
- 브라우저로 파일을 열면 모바일/데스크톱 × 다크/라이트 4가지 뷰포트가 한 화면에 보인다
- DashboardPage.tsx 의 모든 섹션이 1:1 매핑되어 시각화됨
- v0.1.1 §6~§9 룰(Progress Color, Stat Card, Iconography) 모두 시각 확인 가능
- loading / error(신규) / empty 상태 시각화 포함
- 메모리 룰(1fr 그리드, 가로 스크롤, BottomNav 갭, viewport 분기) HTML 주석으로 명시
- automated grep gate 통과
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 사용자 시안 검토 (브라우저 시각 확인)</name>
  <what-built>
DashboardPage 재디자인 v0.1.1 시안 HTML — 모바일/데스크톱 × 라이트/다크 4뷰포트, 13 카테고리 도넛(DIV/컴프 two-lap 포함), 4종 통계 카드(정상/임계치), 빠른 도구 4종, 오늘 일정(시간 확정/미정/완료 변형), 데스크톱 미니 캘린더+sticky 일정 패널, loading/error(신규)/empty 상태 시각화.
  </what-built>
  <how-to-verify>
1. 파일 열기:
   ```
   open cha-bio-safety/docs/redesign-context/01-dashboard/sketch/dashboard-sketch.html
   ```
   (또는 Finder 더블클릭)

2. 시각적으로 확인:
   - 모바일(375px) 다크 / 라이트 두 프레임이 한 화면에 보인다
   - 데스크톱(1280px) 다크 / 라이트 두 프레임이 한 화면에 보인다
   - DashboardPage 의 모든 섹션이 빠짐없이 시안에 등장:
     a. 헤더(햄버거 + 알림 + 프로필)
     b. 근무자 칩 4명 + 연차/공가 (DutyChip 시각)
     c. 두 배너 (오늘 점검 / 수신반 48h)
     d. 통계 카드 4종 (정상 변형 + 임계치 변형)
     e. 빠른 도구 4종
     f. 오늘 일정 (시간 확정 / 시간 미정 분리 + completed 상태)
     g. 월간 도넛 13 카테고리 (가로 스크롤, DIV/컴프 two-lap)
     h. BottomNav 5탭
     i. 데스크톱 우측 미니 캘린더 + 일정 패널
     j. loading / error / empty 상태

3. 디자인 시스템 v0.1.1 룰 시각 확인:
   - Progress Color Rule: 도넛 색이 진척률 구간별로 다른가?
   - Stat Card: 좌측 3px 색바 + 임계치 시 danger 변형 보이는가?
   - 카테고리 카드: 아이콘 회색 통일 + 0% 카드 opacity 0.6 적용?
   - Lucide 아이콘 매핑이 빠른 도구/카테고리에 적용?

4. 메모리 룰 위반 없는가:
   - 9/10/11px 폰트 사용 없음
   - 월간 도넛 가로 스크롤(flex-wrap 없음)
   - 폰트는 모바일/데스크톱 동일

5. 라이트/다크 모두 가독성 OK인가? 색 대비 어색한 곳 없는가?
  </how-to-verify>
  <resume-signal>
- "approved" → 2단계(TSX 변환) quick task 별도 생성으로 진행
- "수정: [구체 항목]" → 시안 보강 (이 quick task 내에서 task 1 다시)
- "전면 재시작" → 디자인 방향 자체 재논의
  </resume-signal>
</task>

</tasks>

<verification>
- 시안 HTML 파일 존재 + 600+ lines
- 4 viewports (mobile×2 + desktop×2) 모두 등장
- DashboardPage.tsx 모든 섹션 1:1 매핑 확인 (grep gate)
- v0.1.1 §6~§9 룰 시각 확인
- loading / error / empty 상태 모두 표현
- 사용자 브라우저 검토 후 approve 또는 수정요청
</verification>

<success_criteria>
- 사용자가 브라우저에서 시안 1 파일만 열어 디자인 승인/거절 결정을 즉시 내릴 수 있다
- TSX 변환 시 시안을 그대로 따라 작성하면 디자인 변형이 발생하지 않는다 (시안 = 단일 진실 소스)
- 시안 HTML 자체가 self-contained — 디렉토리 이동/공유에도 깨지지 않는다
- 비즈니스 로직 누락 없음 — DashboardPage.tsx 의 모든 섹션/상태/분기가 시각화됨
- src/ 어떤 파일도 수정되지 않음 (시안 단계)
</success_criteria>

<output>
After completion, create `.planning/quick/260509-3e3-redesign-01-dashboard-sketch/260509-3e3-SUMMARY.md` with:
- 시안 파일 경로 + 라인 수
- 사용자 검토 결과 (approved / 수정요청 + 항목)
- 다음 quick task 트리거 정보 (TSX 변환 시작점, 이번 시안에서 결정된 디자인 합의 사항)
</output>
