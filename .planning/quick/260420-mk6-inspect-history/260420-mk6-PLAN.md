---
phase: 260420-mk6
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cha-bio-safety/migrations/0067_inspect_history.sql
  - cha-bio-safety/functions/api/elevators/_inspectsafe.ts
  - cha-bio-safety/functions/api/elevators/inspect-history.ts
autonomous: true
requirements:
  - QUICK-260420-MK6
must_haves:
  truths:
    - "GET /api/elevators/inspect-history?cert_no=2114-971 호출 시 해당 호기의 공단 공식 검사이력이 반환된다 (historyCount ≥ 10)"
    - "검사이력은 elevator_inspect_history 테이블에 저장되고, 동일 fail_cd 재호출 시 UPSERT로 최신 데이터가 덮어써진다"
    - "각 검사건의 failCd 기준 부적합 상세(failDesc, standardArticle 등)가 elevator_inspect_fails 테이블에 저장된다"
    - "sync_all=1 쿼리 시 elevators 테이블의 cert_no 보유 17대 전체가 순차 동기화된다"
    - "기존 koelsa-inspect.ts, koelsa.ts는 변경되지 않는다 (동작 유지)"
  artifacts:
    - path: "cha-bio-safety/migrations/0067_inspect_history.sql"
      provides: "elevator_inspect_history, elevator_inspect_fails 테이블 스키마"
      contains: "CREATE TABLE IF NOT EXISTS elevator_inspect_history"
    - path: "cha-bio-safety/functions/api/elevators/_inspectsafe.ts"
      provides: "공단 ElevatorInspectsafeService XML fetch + parse 공용 유틸"
      exports: ["fetchInspectHistory", "fetchFailDetails"]
    - path: "cha-bio-safety/functions/api/elevators/inspect-history.ts"
      provides: "GET /api/elevators/inspect-history 엔드포인트 (단일/전체 동기화)"
      exports: ["onRequestGet"]
  key_links:
    - from: "cha-bio-safety/functions/api/elevators/inspect-history.ts"
      to: "cha-bio-safety/functions/api/elevators/_inspectsafe.ts"
      via: "import { fetchInspectHistory, fetchFailDetails }"
      pattern: "from ['\"]\\./_inspectsafe['\"]"
    - from: "cha-bio-safety/functions/api/elevators/inspect-history.ts"
      to: "D1: elevator_inspect_history"
      via: "INSERT ... ON CONFLICT(fail_cd) DO UPDATE"
      pattern: "INSERT INTO elevator_inspect_history"
    - from: "cha-bio-safety/functions/api/elevators/inspect-history.ts"
      to: "D1: elevator_inspect_fails"
      via: "DELETE + INSERT by fail_cd"
      pattern: "(INSERT INTO|DELETE FROM) elevator_inspect_fails"
---

<objective>
공단 공식 API(ElevatorInspectsafeService)를 호출해 승강기 공식 검사이력(검사일/판정/유효기간/부적합사유)을 D1에 캐싱하고 조회하는 API/DB 레이어를 추가한다.

Purpose: 현재 민원24(koelsa-inspect.ts)는 특정 접수번호 건만 조회 가능하여 전체 검사이력 조회 불가. 공단 공식 API로 호기별 전체 검사이력 + 부적합 상세를 확보해 데스크톱 검사이력 뷰의 데이터 소스를 마련한다.

Output:
- 마이그레이션 1개 (0067)
- 공용 서비스 모듈 1개 (_inspectsafe.ts)
- 새 엔드포인트 1개 (inspect-history.ts)
- UI 연동과 마이그레이션 실행은 이 plan 범위 밖 (사용자 수동)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md

# 참조 (읽기 전용, 패턴 복제 대상)
@cha-bio-safety/functions/api/elevators/koelsa.ts
@cha-bio-safety/functions/api/elevators/koelsa-inspect.ts
@cha-bio-safety/functions/api/elevators/index.ts
@cha-bio-safety/migrations/0054_elevator_public_info.sql
@cha-bio-safety/migrations/0061_koelsa_cache.sql
@cha-bio-safety/migrations/0064_minwon_findings.sql

<interfaces>
<!-- 재사용 대상: koelsa.ts의 XML 파서 패턴 -->

From cha-bio-safety/functions/api/elevators/koelsa.ts:
```typescript
// 재사용할 XML 파싱 패턴
const SERVICE_KEY = 'bb8deaf60d1322e149801cc367cb94a2aa6ffa700a2d0635e8399c8a3a9f0b00'

function parseXml(xml: string): Item[] {
  const items: Item[] = []
  const itemRegex = /<item>([\s\S]*?)<\/item>/g
  let match
  while ((match = itemRegex.exec(xml)) !== null) {
    const block = match[1]
    const get = (tag: string) => {
      const m = block.match(new RegExp(`<${tag}>([^<]*)</${tag}>`))
      return m ? m[1] : ''
    }
    // ... field 추출
  }
  return items
}

function parseTotalCount(xml: string): number {
  const m = xml.match(/<totalCount>(\d+)<\/totalCount>/)
  return m ? parseInt(m[1], 10) : 0
}

// 에러 감지
if (xml.includes('Unexpected errors') || xml.includes('SERVICE_KEY_IS_NOT_REGISTERED')) {
  throw new Error('API 호출 실패')
}
```

From cha-bio-safety/functions/_middleware.ts (JWT 이미 처리됨):
```typescript
// /api/* 요청은 자동으로 JWT 검증됨 (public routes 제외)
// ctx.data.staffId, ctx.data.role 으로 접근 가능
// 별도 인증 코드 작성 불필요
```

From cha-bio-safety/functions/api/elevators/index.ts (D1 쿼리 패턴):
```typescript
import type { Env } from '../../_middleware'
export const onRequestGet: PagesFunction<Env> = async ({ request, env }) => {
  // env.DB.prepare(sql).bind(...).all<Row>()
  // env.DB.prepare(sql).bind(...).first()
  // env.DB.prepare(sql).bind(...).run()
}
```

From cha-bio-safety/migrations/0054_elevator_public_info.sql (cert_no 정보):
```sql
-- elevators 테이블에는 이미 cert_no 컬럼 존재
-- 예: '2114-971' (하이픈 포함 8자리). 공단 API는 하이픈 제거 7자리 필요: '2114971'
-- cert_no IS NOT NULL 인 elevator 가 17대 (EV-01~11, ES-01~06)
```
</interfaces>

## 확정 API 스펙

**Base URL:** `https://apis.data.go.kr/B553664/ElevatorInspectsafeService`
**SERVICE_KEY:** `bb8deaf60d1322e149801cc367cb94a2aa6ffa700a2d0635e8399c8a3a9f0b00` (기존 재사용)

### Operation 1: `getInspectsafeList`
- GET 파라미터: `serviceKey`, `pageNo`, `numOfRows`(권장 100), `elevator_no` (7자리, 하이픈 제거)
- Response: XML, `<items><item>...</item></items>`, `<totalCount>`
- item 필드 (이번 plan에서 DB로 저장할 것만):
  - `elevatorNo` (7자리), `elvtrAsignNo` (호기)
  - `buldNm` (building_name), `address1`+`address2` (address)
  - `inspctDe` (YYYYMMDD → inspect_date), `inspctKindNm` (inspect_kind)
  - `inspctInsttNm` (inspect_institution), `companyNm` (company_name)
  - `dispWords` (disp_words: 합격/보완/보완후합격/불합격)
  - `applcBeDt`, `applcEnDt` (YYYYMMDD → valid_start/valid_end)
  - `failCd` (부적합코드 — 고유 키, Operation 2에서 사용)
  - `ratedSpeed`, `ratedCap`(숫자), `shuttleFloorCnt`(숫자)

### Operation 2: `getInspectFailList`
- GET 파라미터: `serviceKey`, `pageNo`, `numOfRows`, `fail_cd` (Operation 1 응답 failCd)
- Response: XML, `<items><item>...</item></items>`
- item 필드: `failDesc`, `failDescInspector`, `standardArticle`, `standardTitle1`(standard_title 로 저장)
</context>

<tasks>

<task type="auto">
  <name>Task 1: DB 마이그레이션 — elevator_inspect_history + elevator_inspect_fails</name>
  <files>cha-bio-safety/migrations/0067_inspect_history.sql</files>
  <action>
Create file `cha-bio-safety/migrations/0067_inspect_history.sql` with EXACT contents below (UTF-8, no BOM). 번호 0067이 맞다 — 최신은 0066.

```sql
-- ─── 공단 공식 검사이력 (ElevatorInspectsafeService / getInspectsafeList) ───
-- 호기(elevator_no)별 검사건 단위로 저장. 한 호기당 과거 10~20건 내외.
-- 자체점검(0061 koelsa_self_checks)와 별개이며, 민원24(0061 koelsa_inspections)와도 구분됨.
CREATE TABLE IF NOT EXISTS elevator_inspect_history (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  elevator_no TEXT NOT NULL,           -- cert_no 하이픈 제거, 7자리 (예: '2114971')
  fail_cd TEXT UNIQUE NOT NULL,        -- 공단 부적합코드(검사건 고유ID). 부적합 없어도 발급됨
  inspect_date TEXT,                   -- YYYY-MM-DD (inspctDe 변환)
  inspect_kind TEXT,                   -- inspctKindNm (설치/정기/수시/정밀)
  inspect_institution TEXT,            -- inspctInsttNm
  company_name TEXT,                   -- companyNm (유지관리업체)
  disp_words TEXT,                     -- 판정 (합격/보완/보완후합격/불합격)
  valid_start TEXT,                    -- YYYY-MM-DD (applcBeDt)
  valid_end TEXT,                      -- YYYY-MM-DD (applcEnDt)
  rated_speed TEXT,                    -- 정격속도 (원본 문자열 유지)
  rated_cap INTEGER,                   -- 정격용량 kg
  floor_count INTEGER,                 -- shuttleFloorCnt
  building_name TEXT,                  -- buldNm
  address TEXT,                        -- address1 + ' ' + address2
  raw_json TEXT,                       -- 전체 item 원본 JSON (향후 확장/디버그)
  fetched_at TEXT NOT NULL             -- ISO8601
);

CREATE INDEX IF NOT EXISTS idx_inspect_hist_elev
  ON elevator_inspect_history(elevator_no, inspect_date DESC);

-- ─── 부적합 상세 (ElevatorInspectsafeService / getInspectFailList) ───
-- elevator_inspect_history.fail_cd 기준 한 검사건당 0~N건.
-- 동기화 시 해당 fail_cd 에 대해 DELETE + 전체 INSERT (원자성 위해 배치).
CREATE TABLE IF NOT EXISTS elevator_inspect_fails (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  fail_cd TEXT NOT NULL,               -- elevator_inspect_history.fail_cd 참조
  fail_desc TEXT,                      -- 부적합 내용 요약
  fail_desc_inspector TEXT,            -- 검사자 상세 메모
  standard_article TEXT,               -- 규정 조항 번호 (예: '5.2.1.1')
  standard_title TEXT,                 -- 조항 제목 (standardTitle1)
  FOREIGN KEY(fail_cd) REFERENCES elevator_inspect_history(fail_cd)
);

CREATE INDEX IF NOT EXISTS idx_inspect_fails_code
  ON elevator_inspect_fails(fail_cd);
```

주의:
- 반드시 `IF NOT EXISTS` 사용 (중복 실행 안전).
- 외래키는 선언하지만 D1은 기본 외래키 enforce 안 함(정상). `ON DELETE CASCADE` 는 붙이지 않음 — `elevator_inspect_fails` 는 동기화 시마다 DELETE + INSERT 로 관리.
- 파일 끝에 주석/공백 이외 SQL 넣지 말 것.
- 파일 인코딩 UTF-8, 줄끝 LF.
  </action>
  <verify>
    <automated>test -f /Users/jykevin/Documents/20260328/cha-bio-safety/migrations/0067_inspect_history.sql && [ "$(grep -c 'CREATE TABLE IF NOT EXISTS' /Users/jykevin/Documents/20260328/cha-bio-safety/migrations/0067_inspect_history.sql)" = "2" ] && [ "$(grep -c 'CREATE INDEX IF NOT EXISTS' /Users/jykevin/Documents/20260328/cha-bio-safety/migrations/0067_inspect_history.sql)" = "2" ] && grep -q 'elevator_inspect_history' /Users/jykevin/Documents/20260328/cha-bio-safety/migrations/0067_inspect_history.sql && grep -q 'elevator_inspect_fails' /Users/jykevin/Documents/20260328/cha-bio-safety/migrations/0067_inspect_history.sql && grep -q 'fail_cd TEXT UNIQUE NOT NULL' /Users/jykevin/Documents/20260328/cha-bio-safety/migrations/0067_inspect_history.sql</automated>
  </verify>
  <done>
마이그레이션 파일 0067_inspect_history.sql 존재. CREATE TABLE 2개(elevator_inspect_history, elevator_inspect_fails), CREATE INDEX 2개. elevator_inspect_history.fail_cd 는 UNIQUE NOT NULL. 실제 D1 실행은 수동으로 사용자가 진행 (plan 밖).
  </done>
</task>

<task type="auto">
  <name>Task 2: 공용 서비스 모듈 _inspectsafe.ts — fetchInspectHistory / fetchFailDetails</name>
  <files>cha-bio-safety/functions/api/elevators/_inspectsafe.ts</files>
  <action>
Create `cha-bio-safety/functions/api/elevators/_inspectsafe.ts`. Cloudflare Workers 런타임이므로 `fs`, `path`, `xml2js` 같은 Node 모듈 사용 불가 — `fetch` + 정규식 파서만 사용한다. `koelsa.ts` 의 `parseXml` 패턴을 그대로 복제해 적용.

파일 상단에 언더스코어 prefix 설명:
```
// functions/api/elevators/_inspectsafe.ts
// 공단 ElevatorInspectsafeService 공용 유틸. 언더스코어 prefix 로 라우트 노출 제외(Pages Functions 규칙).
```

정확히 아래 exports 만 제공:

```typescript
export const INSPECTSAFE_SERVICE_KEY = 'bb8deaf60d1322e149801cc367cb94a2aa6ffa700a2d0635e8399c8a3a9f0b00'
const BASE = 'https://apis.data.go.kr/B553664/ElevatorInspectsafeService'

export interface InspectHistoryItem {
  elevatorNo: string
  elvtrAsignNo: string
  buldNm: string
  address1: string
  address2: string
  inspctDe: string          // YYYYMMDD (raw)
  inspctKindNm: string
  inspctInsttNm: string
  companyNm: string
  dispWords: string
  applcBeDt: string         // YYYYMMDD (raw)
  applcEnDt: string         // YYYYMMDD (raw)
  failCd: string
  ratedSpeed: string
  ratedCap: string          // numeric string
  shuttleFloorCnt: string   // numeric string
  raw: Record<string, string>  // 전체 필드 (DB raw_json 저장용)
}

export interface FailItem {
  failDesc: string
  failDescInspector: string
  standardArticle: string
  standardTitle: string    // standardTitle1 매핑
}

export async function fetchInspectHistory(elevatorNo: string): Promise<InspectHistoryItem[]>
export async function fetchFailDetails(failCd: string): Promise<FailItem[]>

// 유틸 (named export — 호출자에서 재사용)
export function yyyymmdd_to_iso(s: string): string  // '20140210' -> '2014-02-10', '' -> ''
```

구현 상세:

1. **`parseTag(block: string, tag: string): string`** — `new RegExp('<' + tag + '>([^<]*)</' + tag + '>')` 매치. 없으면 ''. (koelsa.ts 의 `get` 헬퍼와 동일.)

2. **`parseItems(xml: string): Array<Record<string,string>>`**
   - `/<item>([\s\S]*?)<\/item>/g` 로 순회
   - 각 블록에 대해 내부 태그를 모두 추출해 객체로 반환 (이번엔 알려진 필드만 하드코딩하지 말고, 블록 내 모든 `<tagName>value</tagName>` 을 순회)
   - 정규식: `/<([a-zA-Z0-9_]+)>([^<]*)<\/\1>/g`

3. **`parseTotalCount(xml)`** — koelsa.ts 동일.

4. **`checkError(xml)`** — `'Unexpected errors'`, `'SERVICE_KEY_IS_NOT_REGISTERED'`, `'API not found'`, `'SERVICE ERROR'` 중 하나라도 포함되면 `throw new Error('Inspectsafe API 오류: ' + snippet)` (snippet = xml의 앞 200자).

5. **`fetchInspectHistory(elevatorNo: string)`** — 입력 검증: 7자리 숫자 아니면 throw `'elevator_no 는 7자리 숫자여야 합니다: ' + elevatorNo`. 첫 페이지 호출 → checkError → totalCount 파싱. `numOfRows=100`. 전체 페이지 순회 (`Math.ceil(total/100)`). 각 아이템을 `InspectHistoryItem` 형태로 매핑 (raw 에는 원본 필드 dict 전체 저장).

6. **`fetchFailDetails(failCd: string)`** — failCd 빈 문자열이면 `return []`. `numOfRows=100`. 응답 파싱해 FailItem 배열 반환. checkError 필수. `standardTitle1` → `standardTitle` 로 리매핑.

7. **`yyyymmdd_to_iso`** — `s` 가 8자리 숫자 아니면 빈 문자열 리턴. 아니면 `s.slice(0,4) + '-' + s.slice(4,6) + '-' + s.slice(6,8)`.

URL 빌드 예:
```
`${BASE}/getInspectsafeList?serviceKey=${INSPECTSAFE_SERVICE_KEY}&pageNo=${pageNo}&numOfRows=100&elevator_no=${elevatorNo}`
`${BASE}/getInspectFailList?serviceKey=${INSPECTSAFE_SERVICE_KEY}&pageNo=1&numOfRows=100&fail_cd=${encodeURIComponent(failCd)}`
```

금지: `as any`, `Node` 모듈 import, `JSON.parse(xmlString)`. export 는 위 리스트에 한정 — 추가 export 금지.
  </action>
  <verify>
    <automated>test -f /Users/jykevin/Documents/20260328/cha-bio-safety/functions/api/elevators/_inspectsafe.ts && grep -q 'export async function fetchInspectHistory' /Users/jykevin/Documents/20260328/cha-bio-safety/functions/api/elevators/_inspectsafe.ts && grep -q 'export async function fetchFailDetails' /Users/jykevin/Documents/20260328/cha-bio-safety/functions/api/elevators/_inspectsafe.ts && grep -q 'export function yyyymmdd_to_iso' /Users/jykevin/Documents/20260328/cha-bio-safety/functions/api/elevators/_inspectsafe.ts && grep -q 'INSPECTSAFE_SERVICE_KEY' /Users/jykevin/Documents/20260328/cha-bio-safety/functions/api/elevators/_inspectsafe.ts && grep -q "B553664/ElevatorInspectsafeService" /Users/jykevin/Documents/20260328/cha-bio-safety/functions/api/elevators/_inspectsafe.ts && cd /Users/jykevin/Documents/20260328/cha-bio-safety && npx -y tsc --noEmit functions/api/elevators/_inspectsafe.ts --target es2022 --module esnext --moduleResolution bundler --skipLibCheck --types @cloudflare/workers-types 2>&1 | grep -v "^$" | head -20 || true</automated>
  </verify>
  <done>
_inspectsafe.ts 존재. `fetchInspectHistory`, `fetchFailDetails`, `yyyymmdd_to_iso`, `INSPECTSAFE_SERVICE_KEY` 모두 export. tsc 타입 검사 에러 없음(경고 무방). koelsa.ts 와 유사한 XML 파서 패턴 사용, Node 모듈 import 없음.
  </done>
</task>

<task type="auto">
  <name>Task 3: inspect-history.ts 엔드포인트 + UPSERT 캐시 로직</name>
  <files>cha-bio-safety/functions/api/elevators/inspect-history.ts</files>
  <action>
Create `cha-bio-safety/functions/api/elevators/inspect-history.ts`. 엔드포인트:

```
GET /api/elevators/inspect-history?cert_no=2114-971              → 단일 호기 동기화 + 반환
GET /api/elevators/inspect-history?cert_no=2114-971&refresh=0    → 캐시 반환(동기화 안 함)
GET /api/elevators/inspect-history?sync_all=1                    → elevators 중 cert_no IS NOT NULL 전체 순차 동기화 (admin only)
```

구현:

```typescript
// functions/api/elevators/inspect-history.ts
// 공단 공식 API 기반 승강기 검사이력 동기화/조회
import type { Env } from '../../_middleware'
import {
  fetchInspectHistory,
  fetchFailDetails,
  yyyymmdd_to_iso,
  type InspectHistoryItem,
  type FailItem,
} from './_inspectsafe'
```

1. **입력 파싱**
   - `url.searchParams`: `cert_no`, `sync_all`, `refresh` (기본 `'1'` — 즉 refresh 안 넘기면 항상 동기화).
   - `sync_all === '1'` 이면 admin 체크: `ctx.data.role !== 'admin'` 이면 `{ success:false, error:'admin 권한 필요' }` 403.
   - `cert_no` 없고 `sync_all` 도 없으면 400 `'cert_no 또는 sync_all 필수'`.

2. **cert_no → elevator_no 변환**
   - 함수 `certToElevatorNo(certNo: string): string` — 하이픈/공백 제거, 7자리 숫자 아니면 throw.

3. **단일 호기 동기화 루틴** (재사용 가능한 내부 async 함수 `syncOne(env, elevatorNo)`)
   - `items = await fetchInspectHistory(elevatorNo)`
   - 각 item 에 대해 `env.DB.prepare(...).bind(...).run()` UPSERT:
     ```sql
     INSERT INTO elevator_inspect_history
       (elevator_no, fail_cd, inspect_date, inspect_kind, inspect_institution,
        company_name, disp_words, valid_start, valid_end,
        rated_speed, rated_cap, floor_count,
        building_name, address, raw_json, fetched_at)
     VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)
     ON CONFLICT(fail_cd) DO UPDATE SET
       elevator_no=excluded.elevator_no,
       inspect_date=excluded.inspect_date,
       inspect_kind=excluded.inspect_kind,
       inspect_institution=excluded.inspect_institution,
       company_name=excluded.company_name,
       disp_words=excluded.disp_words,
       valid_start=excluded.valid_start,
       valid_end=excluded.valid_end,
       rated_speed=excluded.rated_speed,
       rated_cap=excluded.rated_cap,
       floor_count=excluded.floor_count,
       building_name=excluded.building_name,
       address=excluded.address,
       raw_json=excluded.raw_json,
       fetched_at=excluded.fetched_at
     ```
     - `inspect_date = yyyymmdd_to_iso(item.inspctDe) || null`
     - `valid_start = yyyymmdd_to_iso(item.applcBeDt) || null`, `valid_end` 동일
     - `rated_cap = parseInt(item.ratedCap) || null`
     - `floor_count = parseInt(item.shuttleFloorCnt) || null`
     - `address = (item.address1 + ' ' + item.address2).trim() || null`
     - `raw_json = JSON.stringify(item.raw)`
     - `fetched_at = new Date().toISOString()`

   - failCd 처리: `failCds = items.map(i => i.failCd).filter(Boolean)`
   - 청크 5개씩: `for` loop 로 `for (let i=0; i<failCds.length; i+=5) { const batch = failCds.slice(i, i+5); const results = await Promise.all(batch.map(fc => fetchFailDetails(fc).then(fails => ({fc, fails})).catch(e => ({fc, fails: [], err: e.message})))); ... }`
   - 각 fc 에 대해:
     ```sql
     DELETE FROM elevator_inspect_fails WHERE fail_cd = ?
     ```
     그다음 fails 배열 순회하며
     ```sql
     INSERT INTO elevator_inspect_fails (fail_cd, fail_desc, fail_desc_inspector, standard_article, standard_title)
     VALUES (?, ?, ?, ?, ?)
     ```
   - return `{ elevatorNo, historyCount: items.length, failCount: <실제 삽입된 fails 총합>, lastInspectDate: <가장 최근 inspect_date ISO or null> }`

4. **메인 handler**
   ```typescript
   export const onRequestGet: PagesFunction<Env> = async (ctx) => {
     const { request, env } = ctx
     const url = new URL(request.url)
     const certNo = url.searchParams.get('cert_no')
     const syncAll = url.searchParams.get('sync_all') === '1'
     const refresh = url.searchParams.get('refresh') !== '0'
     // ...
   }
   ```

   - `sync_all=1`: admin 체크 → `env.DB.prepare("SELECT id, cert_no FROM elevators WHERE cert_no IS NOT NULL ORDER BY public_no ASC").all()` → 각각 `syncOne` 순차 실행 (Promise.all 아님 — 공단 API 부하 고려). 실패해도 계속 진행, 결과 배열에 `{cert_no, ok, ...data, error?}`. 최종 `{ success:true, data:{ results, totalOk, totalFail } }`.

   - 단일: `refresh=false` 이면 DB 에서만 조회 (`SELECT * FROM elevator_inspect_history WHERE elevator_no=? ORDER BY inspect_date DESC`, 각 row 의 fail_cd 로 `elevator_inspect_fails` 조회), `cached:true`. 그 외는 `syncOne` 호출 후 DB 재조회해서 반환.
   - 반환 shape (단일):
     ```
     { success:true, data:{ elevatorNo, certNo, historyCount, failCount, lastInspectDate, history: [...], cached:boolean } }
     ```
     `history` 배열 각 원소 shape:
     ```
     { failCd, inspectDate, inspectKind, inspectInstitution, companyName, dispWords,
       validStart, validEnd, ratedSpeed, ratedCap, floorCount, buildingName, address,
       fails: [{ failDesc, failDescInspector, standardArticle, standardTitle }] }
     ```

5. **에러 처리**
   - try/catch 로 전체 감싸고 `{success:false, error: e.message ?? '검사이력 조회 실패'}`, 502 반환.
   - 개별 호기 동기화 실패는 sync_all 모드에서만 partial failure 로 처리.

6. **주의사항 (파일 상단 JSDoc 주석)**
   - 최초 1회는 sync_all=1 로 전체 캐싱 권장 (17대 × 10~20건 × fail API 호출 = 시간 소요)
   - fail_cd 가 빈 검사건은 elevator_inspect_fails 에 아무것도 안 들어감 (정상)
   - 민원24(koelsa-inspect.ts) 와는 독립적 — 서로 데이터 섞지 말 것

**금지**:
- `koelsa-inspect.ts`, `koelsa.ts` 수정 금지.
- 프론트엔드(`src/**`) 수정 금지.
- `elevator_inspect_history` 에 `elevator_id` 컬럼 사용 금지 (스키마에 없음 — `elevator_no` 만 사용).
- 마이그레이션 실행 (`wrangler d1 execute`) 금지 — 사용자 수동.
  </action>
  <verify>
    <automated>test -f /Users/jykevin/Documents/20260328/cha-bio-safety/functions/api/elevators/inspect-history.ts && grep -q 'export const onRequestGet' /Users/jykevin/Documents/20260328/cha-bio-safety/functions/api/elevators/inspect-history.ts && grep -q "sync_all" /Users/jykevin/Documents/20260328/cha-bio-safety/functions/api/elevators/inspect-history.ts && grep -q "from './_inspectsafe'" /Users/jykevin/Documents/20260328/cha-bio-safety/functions/api/elevators/inspect-history.ts && grep -q "INSERT INTO elevator_inspect_history" /Users/jykevin/Documents/20260328/cha-bio-safety/functions/api/elevators/inspect-history.ts && grep -q "ON CONFLICT(fail_cd)" /Users/jykevin/Documents/20260328/cha-bio-safety/functions/api/elevators/inspect-history.ts && grep -q "DELETE FROM elevator_inspect_fails" /Users/jykevin/Documents/20260328/cha-bio-safety/functions/api/elevators/inspect-history.ts && grep -q "INSERT INTO elevator_inspect_fails" /Users/jykevin/Documents/20260328/cha-bio-safety/functions/api/elevators/inspect-history.ts && ! grep -q "koelsa-inspect" /Users/jykevin/Documents/20260328/cha-bio-safety/functions/api/elevators/inspect-history.ts && cd /Users/jykevin/Documents/20260328/cha-bio-safety && npx -y tsc --noEmit functions/api/elevators/inspect-history.ts functions/api/elevators/_inspectsafe.ts --target es2022 --module esnext --moduleResolution bundler --skipLibCheck --types @cloudflare/workers-types 2>&1 | grep -E "error TS" | head -5 || true</automated>
  </verify>
  <done>
- inspect-history.ts 존재, onRequestGet export.
- `sync_all` 처리 + admin 체크.
- _inspectsafe 모듈만 import (koelsa-inspect.ts, koelsa.ts 건드리지 않음).
- UPSERT on fail_cd, fails 는 DELETE+INSERT.
- tsc 타입 검사 에러 없음.
- 배포/마이그레이션은 미수행 (사용자가 수동).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → Pages Functions | JWT 보유 사용자만 (기존 _middleware.ts 에서 검증). `/api/elevators/*` 는 public 아님 |
| Pages Functions → 공단 API | 외부 공공데이터 API, SERVICE_KEY 로 식별 |
| Pages Functions → D1 | 환경 바인딩, 내부 신뢰 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-mk6-01 | Spoofing | inspect-history.ts (`sync_all`) | mitigate | `ctx.data.role !== 'admin'` 체크 후 403. 일반 assistant 는 전체 동기화 불가. |
| T-mk6-02 | Tampering | inspect-history.ts SQL 파라미터 | mitigate | 모든 D1 쿼리는 `.bind(...)` prepared statement. 문자열 연결 금지. cert_no 는 `certToElevatorNo` 에서 숫자 외 문자 제거 후 7자리 검증. |
| T-mk6-03 | Information Disclosure | _inspectsafe.ts SERVICE_KEY | accept | 기존 `koelsa.ts` 와 동일한 키를 서버 사이드 상수로 보관. Cloudflare Functions 내에서만 참조(번들에 포함되나 `/functions/*` 는 클라이언트로 노출 안 됨). |
| T-mk6-04 | DoS | fetchInspectHistory (페이지네이션) | mitigate | `numOfRows=100` 고정, totalCount 기반 정확한 페이지 수만 호출. sync_all 은 순차(Promise.all 금지) — 공단 API rate limit 준수. |
| T-mk6-05 | DoS | client 가 `sync_all=1` 반복 호출 | mitigate | admin 전용(T-mk6-01) + fetch 중 로그 없음(공단 측 부하는 키 단위 rate limit 로 자동 조절). 추가 throttle 은 deferred. |
| T-mk6-06 | Repudiation | inspect history 수정 내역 | accept | 내부 4인 팀 전용, raw_json 원본 보관으로 재동기화 시 변경점 추적 가능. 감사로그 미도입. |
| T-mk6-07 | Elevation of Privilege | /api/elevators/inspect-history 전체 경로 | mitigate | 기존 `_middleware.ts` JWT 검증 자동 적용 (public route 아님). |
</threat_model>

<verification>
플랜 전체 검증:

1. 파일 3개 존재 및 타입 검사:
```
test -f cha-bio-safety/migrations/0067_inspect_history.sql
test -f cha-bio-safety/functions/api/elevators/_inspectsafe.ts
test -f cha-bio-safety/functions/api/elevators/inspect-history.ts
cd cha-bio-safety && npx tsc --noEmit -p . 2>&1 | grep -E "error TS" | grep -E "_inspectsafe|inspect-history" || echo "no errors"
```

2. 기존 파일 무변경 확인 (git):
```
git diff --name-only | grep -E "koelsa\.ts|koelsa-inspect\.ts|src/" && echo "ERROR: 건들면 안 되는 파일이 수정됨" || echo "ok"
```

3. 수동 테스트 (plan 밖 — 사용자가 수행):
```
# a. 마이그레이션 적용
cd cha-bio-safety
npx wrangler d1 execute cha-bio-db --local  --file=migrations/0067_inspect_history.sql
npx wrangler d1 execute cha-bio-db --remote --file=migrations/0067_inspect_history.sql

# b. 빌드 + 배포
npm run deploy   # (--branch production 확인)

# c. JWT 로그인 후 토큰 획득, 단일 호기 테스트
curl -s "https://cbc7119.pages.dev/api/elevators/inspect-history?cert_no=2114-971" \
  -H "Authorization: Bearer <JWT>" | jq '.data | {historyCount, failCount, lastInspectDate}'
# 기대: historyCount >= 10
```
</verification>

<success_criteria>
- migrations/0067_inspect_history.sql 존재, CREATE TABLE 2개 + CREATE INDEX 2개, elevator_inspect_history.fail_cd UNIQUE.
- _inspectsafe.ts 가 `fetchInspectHistory`, `fetchFailDetails`, `yyyymmdd_to_iso`, `INSPECTSAFE_SERVICE_KEY` export, Cloudflare Workers 호환(fs/path 안 씀).
- inspect-history.ts 가 `GET /api/elevators/inspect-history` 핸들러 제공 + cert_no 단일 / sync_all 전체 / refresh=0 캐시-only 세 가지 모드 처리.
- `sync_all=1` 은 admin 권한으로만 동작.
- UPSERT 는 fail_cd 기준, fails 는 DELETE+INSERT. 모든 D1 쿼리 prepared statement(bind).
- 기존 koelsa.ts, koelsa-inspect.ts, src/** 미변경.
- tsc 타입 검사 에러 없음.
- 마이그레이션 실행/배포/프론트 연동은 이 plan 밖 (사용자 수동).
</success_criteria>

<output>
After completion, create `.planning/quick/260420-mk6-inspect-history/260420-mk6-SUMMARY.md` summarizing:
- 생성된 파일 3개 경로 + 각 파일의 핵심 export/스키마
- 사용자가 수동으로 수행해야 할 작업 (마이그레이션 2회, 배포, curl 테스트)
- 다음 quick 에서 다룰 것 (데스크톱 검사이력 UI 연동)
</output>
