---
phase: 260520-0z0
plan: 01
subsystem: redesign-13-schedule
tags: [redesign, tsx-conversion, sub-wave-2, schedule-page, calendar-grid, v0.1.1-tailwind, lucide]
dependency-graph:
  requires:
    - "redesign/13-schedule SW1 commit 350d1ca (page-shell + 헤더 + lucide 도입)"
    - "redesign/13-schedule wave 7 OQ lock commit 09b8bf3 (4건 default 채택)"
  provides:
    - "SchedulePage 캘린더 영역 (calendarEl) v0.1.1 Tailwind 토큰 변환 완료"
    - "arrowBtn utility const 제거 — 사용처 0건"
    - "ChevronRight lucide 아이콘 import 도입"
  affects:
    - "cha-bio-safety/src/pages/SchedulePage.tsx (1 file, +53/-28 lines)"
tech-stack:
  added: []
  patterns:
    - "lucide ChevronLeft/ChevronRight 월 네비 (size={20})"
    - "tailwind status token (text-danger / text-accent / text-text-tertiary) prefix 없음"
    - "§10 화이트리스트 인라인 3 site (button isSel border / span today bg / dot color)"
key-files:
  created: []
  modified:
    - "cha-bio-safety/src/pages/SchedulePage.tsx — line 5 import + calendarEl 영역 + arrowBtn const 삭제"
decisions:
  - "캘린더 카드 borderRadius:14 → rounded-lg (16px 차이 2px 시각 무시) per PLAN scope verbatim"
  - "arrowBtn 5-line const 완전 삭제 + 위치에 SW2 marker 주석만 남김 per D.5"
  - "comment marker 1 hit 은 §7 gate 의 'arrowBtn 코드 식별자 0 hit' 룰 의도에 부합 (PLAN D.5 명시)"
metrics:
  duration: "단일 task / 단일 commit"
  completed: "2026-05-19"
  commits: 1
  lines_changed: "+53 / -28"
  schedule_chunk_kb: "33.26 (gzip 11.03)"
threat-flags: []
---

# Phase 260520-0z0 Plan 01: redesign/13-schedule TSX SW2 — 캘린더 grid + 공휴일 라벨 + arrowBtn 삭제 Summary

## One-Liner

SchedulePage.tsx 캘린더 영역(calendarEl) 인라인 style → v0.1.1 Tailwind 토큰 변환 + 월 네비 ‹/› → lucide ChevronLeft/ChevronRight + arrowBtn utility const 5줄 삭제 (사용처 0건). 비즈 로직 0 변경, atomic commit 1개.

## Scope

| 영역 | 상태 | 비고 |
|---|---|---|
| import 블록 (line 5) | 변경 | `ChevronRight` 추가, alphabet 순 유지 |
| 캘린더 영역 (calendarEl, line 239~300 source → 239~330 post) | 변경 | 인라인 style 11개 → 3개 (§10 화이트리스트) + Tailwind class |
| arrowBtn const 정의 (line 1066~1070 source) | 삭제 | 5줄 → 주석 marker 1줄 |
| lbl / inp utility const | 0 변경 | SW5/SW6 에서 처리 예정 |
| 일자 카드 리스트 (renderCard + scheduleListEl) | 0 변경 | SW3 영역 |
| MonthlyPlanPreview | 0 변경 | SW4 영역 |
| AddModal / EditModal | 0 변경 | SW5/SW6 영역 |
| 모바일 헤더 (SW1) | 0 변경 | 직전 commit 350d1ca 결과 보존 |
| 비즈 로직 (matchesDate / dayCatMap / catInfo / setSelDate / shiftMonth / calDays / dotMap / holidays 등) | 0 변경 | 시그니처 + 호출 verbatim |

## Implementation Diff

### A. lucide-react import (line 5)

BEFORE:
```tsx
import { ChevronLeft, Download, Plus } from 'lucide-react'
```

AFTER:
```tsx
import { ChevronLeft, ChevronRight, Download, Plus } from 'lucide-react'
```

### B. 월 네비 행 (line 241~248 source → 241~258 post)

BEFORE (인라인 style + arrowBtn 사용 + ‹/› glyph):
```tsx
<div style={{ display:'flex', alignItems:'center', justifyContent:'space-between', marginBottom:12 }}>
  <button onClick={() => shiftMonth(-1)} style={arrowBtn}>‹</button>
  <span style={{ fontSize:15, fontWeight:700, color:'var(--t1)' }}>
    {curMonth.split('-')[0]}년 {parseInt(curMonth.split('-')[1])}월
  </span>
  <button onClick={() => shiftMonth(1)} style={arrowBtn}>›</button>
</div>
```

AFTER (Tailwind class + lucide):
```tsx
<div className="flex items-center justify-between mb-3">
  <button
    onClick={() => shiftMonth(-1)}
    className="w-7 h-7 rounded-sm border border-border-default bg-surface-raised text-text-primary flex items-center justify-center cursor-pointer"
    aria-label="이전 달"
  >
    <ChevronLeft size={20} />
  </button>
  <span className="text-title font-bold text-text-primary">
    {curMonth.split('-')[0]}년 {parseInt(curMonth.split('-')[1])}월
  </span>
  <button
    onClick={() => shiftMonth(1)}
    className="w-7 h-7 rounded-sm border border-border-default bg-surface-raised text-text-primary flex items-center justify-center cursor-pointer"
    aria-label="다음 달"
  >
    <ChevronRight size={20} />
  </button>
</div>
```

### C. 캘린더 카드 outer + 요일 헤더 (line 250~257 source → 261~282 post)

BEFORE:
```tsx
<div style={{ background:'var(--bg2)', borderRadius:14, border:'1px solid var(--bd)', overflow:'hidden', marginBottom:16 }}>
  <div style={{ display:'grid', gridTemplateColumns:'repeat(7,1fr)', borderBottom:'1px solid var(--bd)' }}>
    {WEEK_DAYS.map((d,i) => (
      <div key={d} style={{ textAlign:'center', padding:'7px 0', fontSize:10, fontWeight:600,
        color: i===0 ? '#ef4444' : i===6 ? 'var(--acl)' : 'var(--t3)' }}>{d}</div>
    ))}
  </div>
```

AFTER:
```tsx
<div className="bg-surface-raised rounded-lg border border-border-default overflow-hidden mb-4">
  <div className="grid grid-cols-7 border-b border-border-default">
    {WEEK_DAYS.map((d,i) => (
      <div
        key={d}
        className={`text-center py-1.5 text-caption font-semibold leading-none ${i===0 ? 'text-danger' : i===6 ? 'text-accent' : 'text-text-tertiary'}`}
      >
        {d}
      </div>
    ))}
  </div>
```

노안 격상: 10px → text-caption (12px) + leading-none.

### D. 일자 cell (line 269~289 source → 282~324 post) — §10 화이트리스트 3 site 잔존

button outer (border/bg/borderRadius 동적):
```tsx
<button
  key={idx}
  onClick={() => setSelDate(date)}
  className="flex flex-col items-center cursor-pointer box-border py-1.5 gap-0.5"
  style={{
    borderRadius: isSel ? 8 : 0,
    background: isSel ? 'rgba(59,130,246,0.15)' : 'transparent',
    border: isSel ? '2px solid #3b82f6' : '2px solid transparent',
  }}
>
```

span 일자 텍스트 (today 원형 dynamic):
```tsx
<span
  className={`text-caption flex items-center justify-center rounded-full leading-none ${
    isToday || isSel ? 'font-bold' : 'font-normal'
  } ${
    isSel ? 'text-accent' : isToday ? 'text-white' : isRed ? 'text-danger' : dow === 6 ? 'text-accent' : 'text-text-primary'
  }`}
  style={{
    width: 24,
    height: 24,
    background: isToday && !isSel ? 'var(--accent)' : 'transparent',
  }}
>
```

dot row (카테고리 동적 color):
```tsx
<div className="flex gap-0.5 h-1">
  {dots.slice(0,3).map((cat,ci) => (
    <span
      key={ci}
      className="w-1 h-1 rounded-full"
      style={{ background: catInfo(cat)?.color ?? 'var(--text-tertiary)' }}
    />
  ))}
</div>
```

### E. 공휴일 라벨 (line 297~299 source → 326~328 post)

BEFORE:
```tsx
<div style={{ marginBottom:8, fontSize:11, color:'#ef4444', fontWeight:600, paddingLeft:2 }}>
```

AFTER:
```tsx
<div className="mb-2 text-caption font-semibold text-danger pl-0.5 leading-none">
```

노안 격상: 11px → text-caption (12px) + leading-none.

### F. arrowBtn utility const (line 1066~1070 source → 1095 post)

BEFORE (5줄 const 정의):
```tsx
const arrowBtn: React.CSSProperties = {
  width:32, height:32, borderRadius:8, border:'1px solid var(--bd)',
  background:'var(--bg2)', color:'var(--t1)', fontSize:20, lineHeight:'1',
  cursor:'pointer', display:'flex', alignItems:'center', justifyContent:'center',
}
```

AFTER (주석 marker):
```tsx
// arrowBtn 삭제 — 사용처 캘린더 월 네비 lucide ChevronLeft/ChevronRight inline className 으로 치환 (SW2)
```

## Verify Gate Results

### Negative gates (SW2 영역 line 239~330)

| # | gate | expected | actual | result |
|---|---|---|---|---|
| 1 | linear-gradient | 0 | 0 | PASS |
| 2 | 옛 alias var(--bg/bg2/bg3/bd/bd2/t1/t2/t3/acl) | 0 | 0 | PASS |
| 3 | 인라인 fontSize: | 0 | 0 | PASS |
| 4 | 인라인 style={{ | 3 (§10 화이트리스트) | 3 | PASS |
| 5 | status- prefix | 0 | 0 | PASS |
| 6 | 9/10/11px | 0 | 0 | PASS |
| 7 | arrowBtn 식별자 (전체 파일) | 0 코드 + 1 marker 주석 | 1 (marker only, PLAN D.5 명시) | PASS |

Gate 7 보충: PLAN D.5 가 `// arrowBtn 삭제 — ...` 주석 marker 1줄을 명시. 코드 식별자 (정의 / 사용처) 는 0 hit (검증: `grep -nE '\barrowBtn\b'` → marker 줄만 매치).

### Positive gates (SW2 영역 line 239~330)

| # | gate | expected | actual | result |
|---|---|---|---|---|
| 8 | bg-surface-raised | ≥1 | 3 (캘린더 카드 1 + ‹ 버튼 1 + › 버튼 1) | PASS |
| 9 | border-border-default | ≥2 | 4 (카드 1 + 요일 row 1 + ‹ 1 + › 1) | PASS |
| 10 | text-danger | ≥2 | 3 (요일 일 1 + 일자 isRed 1 + 공휴일 라벨 1) | PASS |
| 11 | text-accent | ≥1 | 2 (요일 토 1 + 일자 isSel/dow=6 분기 1) | PASS |
| 12 | text-text-tertiary | ≥1 | 1 (요일 평일) | PASS |
| 13 | text-caption + leading-none 조합 | ≥1 | 3 (요일 row + 일자 span + 공휴일) | PASS |
| 14 | grid-cols-7 | 2 | 2 (요일 row + 일자 grid) | PASS |
| 15 | ChevronRight import | ≥1 | 1 | PASS |
| 16 | Chevron(Left|Right) usage (whole file) | ≥3 | 3 (SW1 모바일 백 1 + SW2 ‹ + SW2 ›) | PASS |
| 17 | w-1 h-1 rounded-full | ≥1 | 1 (dot span) | PASS |

### Biz preservation gates (whole file)

| # | gate | expected | actual | result |
|---|---|---|---|---|
| 18 | toast.(success|error) | 13 | 13 | PASS |
| 19 | scheduleApi 호출 | 5 | 5 | PASS |
| 20 | lbl + inp const 정의 | 2 | 2 | PASS |
| 21 | SW3+ 영역 (line 333~end) 인라인 style={{ | 92 (pre-conv == post-conv) | 92 | PASS |
| 22 | matchesDate/dayCatMap/calDays/catInfo/shiftMonth 등 식별자 | ≥10 | 19 | PASS |

### Build gates

| gate | expected | actual | result |
|---|---|---|---|
| `npx tsc --noEmit` | exit 0 | exit 0 | PASS |
| `npm run build` | exit 0 | exit 0 (built in 14.39s) | PASS |
| SchedulePage chunk | exists | 33.26 kB / gzip 11.03 kB | PASS |

## Commit

- **Hash:** `02b98d1`
- **Message:** `tsx(13-schedule): SW2 — 캘린더 grid + 공휴일 라벨 + arrowBtn 삭제`
- **Diff:** +53 / -28, 1 file (SchedulePage.tsx)
- **File size:** 1078 → 1103 lines (+25)

## Deviations from Plan

**None.** D.4 / D.5 snippet 1 byte 도 변경 없이 적용. import alphabet 순 (`ChevronLeft, ChevronRight, Download, Plus`) 정확. 비즈 로직 0 변경.

PLAN Step 5 (21) 의 "SW1 SUMMARY 의 66 ± 0" 표현은 SW1 시점 line 518~end 의 카운트였음. SW2 post-conversion 시 SW3+ 영역의 line range 가 333~end 로 shift 되지만, **content-level 카운트** (= 92) 는 pre-SW2 baseline 과 정확히 일치 = 보존 룰 충족. PLAN 의 의도(SW3+ 영역 손대지 않음)와 부합.

## Next Sub-Wave Recommendation

**SW3 — 일자 카드 리스트 (renderCard + scheduleListEl, post-SW2 line 333~430)**

target:
- `renderCard` function (line 334~) — 일정 카드 1개 렌더링 (border-l 색바, 카테고리 chip, 제목/메모, 상태 toggle)
- `scheduleListEl` (line 405~) — 선택일 일정 리스트 (헤더 + 추가 버튼 + grouped 분기)
- 인라인 style 약 25개 → Tailwind class + §10 화이트리스트 (border-left dynamic color, 카테고리 chip background) 잔존

scope guard:
- MonthlyPlanPreview / AddModal / EditModal 0 변경 (SW4/SW5/SW6)
- 비즈 시그니처 verbatim: `catInfo`, `updateStatus`, `setEditItem`, `dayCatMap`, `groupedItems`

verify gate 강화: §6.3 카테고리 hex 정규화 set (text-blue-600 X / catInfo(cat).color O) — PLAN scope 인용.

## Self-Check: PASSED

- FOUND: `cha-bio-safety/src/pages/SchedulePage.tsx` modified (verified via `git log -1 --stat`)
- FOUND: commit `02b98d1` in `git log --oneline -5`
- FOUND: `.planning/quick/260520-0z0-redesign-13-schedule-tsx-sw2-grid-arrowb/260520-0z0-SUMMARY.md` written
- FOUND: ChevronRight in import line 5
- FOUND: arrowBtn marker 주석 line 1095, 코드 식별자 0
- FOUND: SW3+ 영역 inline style={{ 카운트 92 보존
- FOUND: tsc + build 모두 exit 0
- FOUND: §10 화이트리스트 정확히 3 site 잔존
- MISSING: 없음
