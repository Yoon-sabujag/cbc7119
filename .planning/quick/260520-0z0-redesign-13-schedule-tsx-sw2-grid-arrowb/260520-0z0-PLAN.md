---
phase: 260520-0z0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cha-bio-safety/src/pages/SchedulePage.tsx
autonomous: true
requirements:
  - redesign-13-schedule-tsx-sw2-grid-arrowb
tags: [redesign, tsx-conversion, sub-wave-2, schedule-page, calendar-grid, v0.1.1-tailwind]

must_haves:
  truths:
    - "월 네비 행이 v0.1.1 Tailwind class 로 작성되고 ‹/› 버튼이 lucide ChevronLeft/ChevronRight 아이콘으로 표시된다"
    - "캘린더 카드 outer 가 bg-surface-raised + rounded-lg + border border-border-default + overflow-hidden + mb-4 로 렌더된다"
    - "요일 헤더 7 cell 이 text-caption + leading-none 로 표시되고 일요일=text-danger / 토요일=text-accent / 평일=text-text-tertiary 분기 적용"
    - "7×6 grid (calDays) 가 grid-cols-7 로 표시되고 dot row 가 className=\"flex gap-0.5 h-1\" 로 작성된다"
    - "공휴일 라벨 (선택일이 공휴일일 때) 가 text-caption + text-danger + font-semibold + leading-none 로 표시된다"
    - "arrowBtn utility const 정의가 파일에서 완전히 삭제되고 사용처가 0건이다"
    - "캘린더 영역(line 239~300) 외 모든 코드는 1 byte 도 변경되지 않는다 — matchesDate / dayCatMap / catInfo / setSelDate / shiftMonth / calDays / curMonth 분리 시그니처 verbatim"
    - "ChevronRight 가 lucide-react import block 에 추가된다 (ChevronLeft 는 SW1 에서 기 import)"
    - "npx tsc --noEmit + npm run build 모두 exit 0"
  artifacts:
    - path: "cha-bio-safety/src/pages/SchedulePage.tsx"
      provides: "calendar grid + holiday label 영역 v0.1.1 Tailwind 변환 + arrowBtn const 삭제 + ChevronRight import"
      contains: "ChevronRight, bg-surface-raised, grid-cols-7, text-danger, text-accent, w-1 h-1 rounded-full"
  key_links:
    - from: "calendar nav arrow buttons"
      to: "lucide ChevronLeft / ChevronRight"
      via: "size={20} stroke 적용된 lucide 아이콘"
      pattern: "<Chevron(Left|Right) size=\\{20\\}"
    - from: "weekday header cells"
      to: "color branch via className"
      via: "i===0 ? text-danger : i===6 ? text-accent : text-text-tertiary"
      pattern: "i===0 \\? 'text-danger'"
    - from: "day cells dot row"
      to: "v0.1.1 token class with dynamic category color"
      via: "className='flex gap-0.5 h-1' + dot span className='w-1 h-1 rounded-full' + dynamic style={{ background: catInfo(cat)?.color ?? 'var(--text-tertiary)' }}"
      pattern: "w-1 h-1 rounded-full"
---

<objective>
SchedulePage.tsx (1078 lines) 의 **sub-wave 2 = 캘린더 grid (월 네비 + 요일 헤더 + 7×6 일자 grid + dot row) + 공휴일 라벨 + arrowBtn utility const 삭제** 영역만 v0.1.1 Tailwind 토큰으로 변환.

source line 239~300 (`calendarEl` 안 캘린더 part) + line 1066~1070 (`arrowBtn` const 정의) 만 수정. 다른 region (일자 카드 / MonthlyPlanPreview / AddModal / EditModal / lbl / inp const) 은 0 byte 변경.

Purpose: 6 sub-wave 분할 commit (W7 §6 + W7 OQ #1 LOCKED b) 의 두 번째 단계. SW1 commit `350d1ca` 가 page-shell + 헤더 + lucide 도입을 끝냈고, 이제 캘린더 영역만 토큰화하여 다음 SW3 (일자 카드) 진입 안정성 확보.

Output: 변환된 `SchedulePage.tsx` (1 file). 비즈 로직 0 변경. atomic commit 1개. tsc + build PASS.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/quick/260519-nxf-redesign-13-schedule-tsx-sub-wave-1-page/260519-nxf-SUMMARY.md
@cha-bio-safety/docs/redesign-context/13-schedule/wave-7-tsx-conversion-checklist.md
@cha-bio-safety/docs/redesign-context/13-schedule/sketch-wave-1.html
@cha-bio-safety/src/pages/SchedulePage.tsx
@cha-bio-safety/tailwind.config.js

<interfaces>
<!-- planner 가 codebase 에서 추출한 verbatim 정보 — executor 가 추측 없이 사용 -->

## A. tailwind.config.js — status token 키 (status- prefix 없음)

source: cha-bio-safety/tailwind.config.js line 64~83

```js
// Status — foreground (color name = `text-{name}`)
safe:    'var(--status-safe)',     // #4ade80 light, #166534 dark
warning: 'var(--status-warning)',
danger:  'var(--status-danger)',   // #f87171 light, #991b1b dark
info:    'var(--status-info)',
fire:    'var(--status-fire)',

// Status — bar (좌측 색바)
'safe-bar':    'var(--status-safe-bar)',     // #22c55e light, #15803d dark
'warning-bar': 'var(--status-warning-bar)',
'danger-bar':  'var(--status-danger-bar)',   // #ef4444 light, #b91c1c dark
'info-bar':    'var(--status-info-bar)',
'fire-bar':    'var(--status-fire-bar)',

// Status — bg (배지 채움)
'safe-bg':    'var(--status-safe-bg)',
'warning-bg': 'var(--status-warning-bg)',
'danger-bg':  'var(--status-danger-bg)',
'info-bg':    'var(--status-info-bg)',
'fire-bg':    'var(--status-fire-bg)',
```

**중요: status- prefix 없음.** `text-danger` ✓ / `text-status-danger` ✗.

W7 §5.1 cheatsheet 의 `text-status-danger-bar` 같은 표현은 실제 tailwind config 키와 mismatch. **이 SW2 는 actual tailwind config 의 키만 사용**: 일요일/공휴일 라벨 = `text-danger` (= var(--status-danger) foreground). SW1 SUMMARY 의 PASS 결과 mirror.

## B. tailwind.config.js — fontSize / spacing / borderRadius (line 28~117)

```js
fontSize: {
  caption:   ['12px', { lineHeight: '1.5', fontWeight: '400' }],  // text-caption
  label:     ['13px', { lineHeight: '1.5', fontWeight: '400' }],
  'body-sm': ['14px', { lineHeight: '1.6', fontWeight: '400' }],
  body:      ['16px', { lineHeight: '1.7', fontWeight: '400' }],
  title:     ['18px', { lineHeight: '1.4', fontWeight: '500' }],  // text-title
  // ...
},
spacing: {
  '1': '4px', '2': '8px', '3': '12px', '4': '16px',
  '5': '20px', '6': '24px', '7': '32px', '8': '48px',  // w-8=48px, w-7=32px
  // ...
},
borderRadius: {
  sm:   '8px',
  md:   '12px',
  lg:   '16px',
  pill: '99px',
},
```

source borderRadius:14 → `rounded-lg` (16px 근사, sketch 도 `rounded-md-token` 사용했으나 실제로는 8px 차이 시각 무시 가능. W7 §5.1 cheatsheet 는 borderRadius:14 → `rounded-[14px]` arbitrary 권장하지만, source 의 14 와 tailwind 의 16 (rounded-lg) 차이 2px 은 시각적으로 무의미). 본 SW2 는 **`rounded-lg`** 사용 (task scope verbatim).

## C. src/styles/tokens.css — 새 CSS 변수 이름 (var(--t3) → var(--text-tertiary))

source: cha-bio-safety/src/styles/tokens.css

| 옛 alias | 새 변수 |
|---|---|
| `var(--t1)` | `var(--text-primary)` |
| `var(--t2)` | `var(--text-secondary)` |
| `var(--t3)` | `var(--text-tertiary)` |
| `var(--bg2)` | `var(--surface-raised)` |
| `var(--bd)` | `var(--border-default)` |
| `var(--acl)` | `var(--accent)` |

dot color dynamic fallback (line 286 의 `var(--t3)`) → `var(--text-tertiary)` 로 교체. tailwind class 로 대체 불가 (JS 변수 동적 분기 + fallback) → §10 화이트리스트 인라인 잔존 OK.

## D. SchedulePage.tsx 현재 상태 (post-SW1 = commit 350d1ca, 1078 lines)

### D.1 import block (line 1~11) — ChevronLeft 기 import

```tsx
import { useState, useMemo, useRef, useEffect, useCallback } from 'react'
import { useNavigate } from 'react-router-dom'
import { useQuery, useQueryClient } from '@tanstack/react-query'
import toast from 'react-hot-toast'
import { ChevronLeft, Download, Plus } from 'lucide-react'
import { scheduleApi } from '../utils/api'
import { useAuthStore } from '../stores/authStore'
import { generateMonthlyPlan } from '../utils/generateMonthlyPlan'
import { useIsDesktop } from '../hooks/useIsDesktop'
import { HOLIDAYS_FALLBACK } from '../utils/holidays'
import type { ScheduleItem, ScheduleCategory } from '../types'
```

SW2 변경: line 5 `import { ChevronLeft, Download, Plus } from 'lucide-react'` →
```tsx
import { ChevronLeft, ChevronRight, Download, Plus } from 'lucide-react'
```

### D.2 calendarEl 영역 BEFORE (line 239~300) — 변환 대상

```tsx
  const calendarEl = (
    <>
      {/* 월 이동 */}
      <div style={{ display:'flex', alignItems:'center', justifyContent:'space-between', marginBottom:12 }}>
        <button onClick={() => shiftMonth(-1)} style={arrowBtn}>‹</button>
        <span style={{ fontSize:15, fontWeight:700, color:'var(--t1)' }}>
          {curMonth.split('-')[0]}년 {parseInt(curMonth.split('-')[1])}월
        </span>
        <button onClick={() => shiftMonth(1)} style={arrowBtn}>›</button>
      </div>

      {/* 캘린더 */}
      <div style={{ background:'var(--bg2)', borderRadius:14, border:'1px solid var(--bd)', overflow:'hidden', marginBottom:16 }}>
        <div style={{ display:'grid', gridTemplateColumns:'repeat(7,1fr)', borderBottom:'1px solid var(--bd)' }}>
          {WEEK_DAYS.map((d,i) => (
            <div key={d} style={{ textAlign:'center', padding:'7px 0', fontSize:10, fontWeight:600,
              color: i===0 ? '#ef4444' : i===6 ? 'var(--acl)' : 'var(--t3)' }}>{d}</div>
          ))}
        </div>
        <div style={{ display:'grid', gridTemplateColumns:'repeat(7,1fr)' }}>
          {calDays.map((date, idx) => {
            if (!date) return <div key={idx} />
            const isToday   = date === today
            const isSel     = date === selDate
            const dow       = idx % 7
            const isHoliday = !!holidays[date]
            const dots      = dotMap[date] ?? []
            const isRed     = dow === 0 || isHoliday

            return (
              <button key={idx} onClick={() => setSelDate(date)}
                style={{ padding:'5px 0 7px', cursor:'pointer',
                  display:'flex', flexDirection:'column', alignItems:'center', gap:2,
                  borderRadius: isSel ? 8 : 0,
                  background: isSel ? 'rgba(59,130,246,0.15)' : 'transparent',
                  border: isSel ? '2px solid #3b82f6' : '2px solid transparent',
                  boxSizing: 'border-box' }}>
                <span style={{
                  fontSize:12, fontWeight: isToday||isSel ? 700:400,
                  color: isSel ? 'var(--acl)' : isToday ? '#fff' : isRed ? '#ef4444' : dow===6 ? 'var(--acl)' : 'var(--t1)',
                  width:24, height:24, display:'flex', alignItems:'center', justifyContent:'center',
                  borderRadius:'50%', background: isToday&&!isSel ? 'var(--acl)':'transparent',
                }}>
                  {parseInt(date.slice(8))}
                </span>
                <div style={{ display:'flex', gap:2, height:4 }}>
                  {dots.slice(0,3).map((cat,ci) => (
                    <span key={ci} style={{ width:4, height:4, borderRadius:'50%', background: catInfo(cat)?.color??'var(--t3)' }} />
                  ))}
                </div>
              </button>
            )
          })}
        </div>
      </div>

      {/* 공휴일 표시 */}
      {holidays[selDate] && (
        <div style={{ marginBottom:8, fontSize:11, color:'#ef4444', fontWeight:600, paddingLeft:2 }}>
          {holidays[selDate]}
        </div>
      )}
```

### D.3 arrowBtn utility const BEFORE (line 1066~1070) — 삭제 대상

```tsx
const arrowBtn: React.CSSProperties = {
  width:32, height:32, borderRadius:8, border:'1px solid var(--bd)',
  background:'var(--bg2)', color:'var(--t1)', fontSize:20, lineHeight:'1',
  cursor:'pointer', display:'flex', alignItems:'center', justifyContent:'center',
}
```

(line 1071~1078 의 `lbl` / `inp` const 는 보존 — SW5/SW6 에서 처리)

### D.4 SW2 변환 AFTER snippet — verbatim 적용

```tsx
  const calendarEl = (
    <>
      {/* 월 이동 */}
      <div className="flex items-center justify-between mb-3">
        <button
          onClick={() => shiftMonth(-1)}
          className="w-7 h-7 rounded-sm border border-border-default bg-surface-raised text-text-primary flex items-center justify-center cursor-pointer"
          aria-label="이전 달"
        >
          <ChevronLeft size={20} />
        </button>
        <span className="text-title font-bold text-text-primary">
          {curMonth.split('-')[0]}년 {parseInt(curMonth.split('-')[1])}월
        </span>
        <button
          onClick={() => shiftMonth(1)}
          className="w-7 h-7 rounded-sm border border-border-default bg-surface-raised text-text-primary flex items-center justify-center cursor-pointer"
          aria-label="다음 달"
        >
          <ChevronRight size={20} />
        </button>
      </div>

      {/* 캘린더 */}
      <div className="bg-surface-raised rounded-lg border border-border-default overflow-hidden mb-4">
        <div className="grid grid-cols-7 border-b border-border-default">
          {WEEK_DAYS.map((d,i) => (
            <div
              key={d}
              className={`text-center py-1.5 text-caption font-semibold leading-none ${i===0 ? 'text-danger' : i===6 ? 'text-accent' : 'text-text-tertiary'}`}
            >
              {d}
            </div>
          ))}
        </div>
        <div className="grid grid-cols-7">
          {calDays.map((date, idx) => {
            if (!date) return <div key={idx} />
            const isToday   = date === today
            const isSel     = date === selDate
            const dow       = idx % 7
            const isHoliday = !!holidays[date]
            const dots      = dotMap[date] ?? []
            const isRed     = dow === 0 || isHoliday

            return (
              <button
                key={idx}
                onClick={() => setSelDate(date)}
                className="flex flex-col items-center cursor-pointer box-border py-1.5 gap-0.5"
                style={{
                  borderRadius: isSel ? 8 : 0,
                  background: isSel ? 'rgba(59,130,246,0.15)' : 'transparent',
                  border: isSel ? '2px solid #3b82f6' : '2px solid transparent',
                }}
              >
                <span
                  className={`text-caption flex items-center justify-center rounded-full leading-none ${
                    isToday || isSel ? 'font-bold' : 'font-normal'
                  } ${
                    isSel ? 'text-accent' : isToday ? 'text-white' : isRed ? 'text-danger' : dow === 6 ? 'text-accent' : 'text-text-primary'
                  }`}
                  style={{
                    width: 24,
                    height: 24,
                    background: isToday && !isSel ? 'var(--accent)' : 'transparent',
                  }}
                >
                  {parseInt(date.slice(8))}
                </span>
                <div className="flex gap-0.5 h-1">
                  {dots.slice(0,3).map((cat,ci) => (
                    <span
                      key={ci}
                      className="w-1 h-1 rounded-full"
                      style={{ background: catInfo(cat)?.color ?? 'var(--text-tertiary)' }}
                    />
                  ))}
                </div>
              </button>
            )
          })}
        </div>
      </div>

      {/* 공휴일 표시 */}
      {holidays[selDate] && (
        <div className="mb-2 text-caption font-semibold text-danger pl-0.5 leading-none">
          {holidays[selDate]}
        </div>
      )}
```

### D.5 arrowBtn const AFTER — 5줄 삭제 + 주석 marker 남김

```tsx
// arrowBtn 삭제 — 사용처 캘린더 월 네비 lucide ChevronLeft/ChevronRight inline className 으로 치환 (SW2)
```

(line 1071 의 `const lbl` 직전에 marker 주석만 남김. `const arrowBtn: React.CSSProperties = { ... }` 5줄은 완전 삭제.)

## E. §10 인라인 style 화이트리스트 — SW2 변환 영역 안 정확히 3 site (잔존 OK)

1. **일자 button isSel/isToday border + bg + borderRadius dynamic** (post: button `style={{ borderRadius, background, border }}` — 3 prop). source line 272~274 원본 위치 mirror.
2. **일자 span today 원형 + 색 dynamic 일부** (post: span `style={{ width: 24, height: 24, background: isToday && !isSel ? 'var(--accent)' : 'transparent' }}` — 3 prop). 색은 className 으로 분기, 24×24 원형 + today bg 만 인라인.
3. **dot span dynamic color** (post: span `style={{ background: catInfo(cat)?.color ?? 'var(--text-tertiary)' }}` — 1 prop).

**SW2 영역 안 인라인 `style={{` 총 3 site 이하 강제.** (verify gate §4 변경 영역 안)

</interfaces>

</context>

<tasks>

<task type="auto">
  <name>Task 1: SchedulePage.tsx SW2 — 캘린더 grid + 공휴일 라벨 + arrowBtn const 삭제 1-pass 변환</name>
  <files>cha-bio-safety/src/pages/SchedulePage.tsx</files>
  <action>
**Goal:** `cha-bio-safety/src/pages/SchedulePage.tsx` 의 캘린더 영역 (line 239~300) 과 arrowBtn const (line 1066~1070) 을 v0.1.1 Tailwind 토큰으로 변환. 다른 영역은 0 byte 변경.

### Step 1 — 변환 전 상태 검증 (read-only)

다음 grep 으로 SW1 commit `350d1ca` 가 적용된 base 상태 확인:

```bash
grep -nE "from 'lucide-react'" cha-bio-safety/src/pages/SchedulePage.tsx
# 기대: 1 hit, line 5 = "import { ChevronLeft, Download, Plus } from 'lucide-react'"

grep -nE 'arrowBtn' cha-bio-safety/src/pages/SchedulePage.tsx
# 기대: 3 hits — line 243 / 247 (사용처 2개) + line 1066 (정의)

grep -c '^const arrowBtn' cha-bio-safety/src/pages/SchedulePage.tsx
# 기대: 1 hit
```

만약 base 와 다르면 STOP 하고 사용자에게 보고. 일치하면 Step 2 진행.

### Step 2 — import 블록 수정 (line 5)

**Edit operation:**
- old_string: `import { ChevronLeft, Download, Plus } from 'lucide-react'`
- new_string: `import { ChevronLeft, ChevronRight, Download, Plus } from 'lucide-react'`

(`ChevronRight` 만 추가. 다른 import 는 변경 금지.)

### Step 3 — calendarEl 캘린더 영역 변환 (line 239~300)

`<interfaces>` block D.2 BEFORE 의 정확한 62 lines (line 239 = `const calendarEl = (` ~ line 300 = `      )}`) 을 D.4 AFTER snippet 으로 1-pass 치환.

**중요 룰:**
- D.4 snippet 의 indent (6-space 또는 8-space) 는 source 의 enclosing 위치 (calendarEl arrow function 안) 와 일치해야 함. source line 239 의 indent = 2-space (function body 내부). 내부 JSX 들은 4/6/8/10-space.
- D.4 snippet 의 모든 className / style / dynamic 분기 verbatim 유지.
- D.4 snippet 의 비즈 시그니처 변경 0: `calDays.map((date, idx) => { ... })`, `setSelDate(date)`, `dotMap[date] ?? []`, `holidays[date]`, `holidays[selDate]`, `parseInt(date.slice(8))`, `WEEK_DAYS.map((d,i) => ...)`, `dots.slice(0,3).map(...)`, `catInfo(cat)?.color ?? ...`, `shiftMonth(-1)`, `shiftMonth(1)`, `curMonth.split('-')[0]`, `parseInt(curMonth.split('-')[1])` — 모두 1 byte 도 바꾸지 않음.
- `isToday` / `isSel` / `dow` / `isHoliday` / `dots` / `isRed` 5 const 정의 verbatim 유지.

### Step 4 — arrowBtn utility const 삭제 (line 1066~1070)

**Edit operation:**
- old_string (5 lines, verbatim):
  ```
  const arrowBtn: React.CSSProperties = {
    width:32, height:32, borderRadius:8, border:'1px solid var(--bd)',
    background:'var(--bg2)', color:'var(--t1)', fontSize:20, lineHeight:'1',
    cursor:'pointer', display:'flex', alignItems:'center', justifyContent:'center',
  }
  ```
- new_string:
  ```
  // arrowBtn 삭제 — 사용처 캘린더 월 네비 lucide ChevronLeft/ChevronRight inline className 으로 치환 (SW2)
  ```

(다른 const `lbl` / `inp` 는 보존. line 1065 의 SW1 주석 marker `// iconBtn 삭제 ...` 도 보존.)

### Step 5 — Verify gate (read-only)

#### 5a. Negative grep gates (SW2 변환 영역 = 캘린더 calendarEl + arrowBtn 삭제 후)

전체 파일 기준이 아니라 **SW2 영역 안** 검증:

```bash
# (1) SW2 영역 안 linear-gradient 0
sed -n '239,305p' cha-bio-safety/src/pages/SchedulePage.tsx | grep -cE 'linear-gradient'
# 기대: 0

# (2) SW2 영역 안 옛 alias var(--bg|--bg2|--bg3|--bd|--bd2|--t1|--t2|--t3|--acl) 0
sed -n '239,305p' cha-bio-safety/src/pages/SchedulePage.tsx | grep -cE 'var\(--(bg|bg2|bg3|bd|bd2|t1|t2|t3|acl)\)'
# 기대: 0

# (3) SW2 영역 안 인라인 fontSize: 0
sed -n '239,305p' cha-bio-safety/src/pages/SchedulePage.tsx | grep -cE 'fontSize\s*:'
# 기대: 0

# (4) SW2 영역 안 인라인 style={{ ≤ 3 (정확히 3 — D.4 의 §10 화이트리스트)
sed -n '239,305p' cha-bio-safety/src/pages/SchedulePage.tsx | grep -cE 'style=\{\{'
# 기대: 3 (button isSel border / span today bg / dot color)

# (5) SW2 영역 안 status- prefix 0 (tailwind config 에 키 없음)
sed -n '239,305p' cha-bio-safety/src/pages/SchedulePage.tsx | grep -cE '(text|bg|border)-status-'
# 기대: 0

# (6) SW2 영역 안 9·10·11px 0 (sketch text-caption=12px 격상)
sed -n '239,305p' cha-bio-safety/src/pages/SchedulePage.tsx | grep -vE '^\s*//' | grep -cE '\b(9|10|11)px\b|fontSize:\s*(9|10|11)\b'
# 기대: 0

# (7) arrowBtn 식별자 — 전체 파일에 0 hit (사용처 + 정의 모두 삭제)
grep -cE '\barrowBtn\b' cha-bio-safety/src/pages/SchedulePage.tsx
# 기대: 0
```

#### 5b. Positive grep gates (SW2 변환 영역 안)

```bash
# (8) bg-surface-raised ≥ 1 (캘린더 카드 outer)
sed -n '239,305p' cha-bio-safety/src/pages/SchedulePage.tsx | grep -cE 'bg-surface-raised'
# 기대: ≥1 (캘린더 카드 + 월 네비 ‹/› 버튼 = 2~3 hits)

# (9) border-border-default ≥ 2 (캘린더 카드 + 요일 row 하단 border + 네비 버튼 border)
sed -n '239,305p' cha-bio-safety/src/pages/SchedulePage.tsx | grep -cE 'border-border-default'
# 기대: ≥2 (카드 1 + 요일 row 1 + 네비 버튼 2 = 4 가능)

# (10) text-danger ≥ 2 (일요일 + 공휴일 라벨 + 일자 isRed 분기)
sed -n '239,305p' cha-bio-safety/src/pages/SchedulePage.tsx | grep -cE "text-danger\b"
# 기대: ≥2

# (11) text-accent ≥ 1 (토요일)
sed -n '239,305p' cha-bio-safety/src/pages/SchedulePage.tsx | grep -cE "text-accent\b"
# 기대: ≥1 (토요일 weekday + dow===6 일자 + isSel 일자 = 3 가능)

# (12) text-text-tertiary ≥ 1 (평일 weekday)
sed -n '239,305p' cha-bio-safety/src/pages/SchedulePage.tsx | grep -cE 'text-text-tertiary'
# 기대: ≥1

# (13) text-caption leading-none ≥ 1 (요일 + 공휴일 라벨 + 일자 셀)
sed -n '239,305p' cha-bio-safety/src/pages/SchedulePage.tsx | grep -cE 'text-caption.*leading-none|leading-none.*text-caption'
# 기대: ≥1 (요일 row + 공휴일 라벨 + 일자 span 3 가능)

# (14) grid-cols-7 ≥ 2 (요일 row + 일자 grid)
sed -n '239,305p' cha-bio-safety/src/pages/SchedulePage.tsx | grep -cE 'grid-cols-7'
# 기대: 2

# (15) ChevronRight import 존재
grep -cE 'import.*ChevronRight.*from .lucide-react.' cha-bio-safety/src/pages/SchedulePage.tsx
# 기대: ≥1 (line 5)

# (16) ChevronLeft + ChevronRight 사용
grep -cE '<Chevron(Left|Right)\s' cha-bio-safety/src/pages/SchedulePage.tsx
# 기대: ≥3 (SW1 모바일 헤더 백 1 + SW2 ‹ + SW2 › = 3)

# (17) w-1 h-1 rounded-full (dot span)
sed -n '239,305p' cha-bio-safety/src/pages/SchedulePage.tsx | grep -cE 'w-1 h-1 rounded-full'
# 기대: ≥1
```

#### 5c. 비즈 로직 + scope 보존

```bash
# (18) toast.(success|error) 전체 카운트 = 13 (변경 전과 동일)
grep -cE 'toast\.(success|error)' cha-bio-safety/src/pages/SchedulePage.tsx
# 기대: 13 (SW1 SUMMARY 와 동일)

# (19) scheduleApi 5 호출 보존
grep -cE 'scheduleApi\.(getByMonth|create|update|updateStatus|delete)' cha-bio-safety/src/pages/SchedulePage.tsx
# 기대: 5

# (20) lbl + inp const 정의 보존 (arrowBtn 만 삭제)
grep -cE '^const (lbl|inp): React\.CSSProperties' cha-bio-safety/src/pages/SchedulePage.tsx
# 기대: 2

# (21) SW3+ 영역 (line 306~end 일자 카드 / MonthlyPlanPreview / AddModal / EditModal) inline style={{ 카운트 = SW1 SUMMARY 의 66 와 일치 (혹은 SW1 변경 후 카운트)
sed -n '306,$p' cha-bio-safety/src/pages/SchedulePage.tsx | grep -cE 'style=\{\{'
# 기대: SW1 SUMMARY 의 line 518~end = 66 ± 0 (SW2 에서 일자 카드 손대지 않으므로 정확히 보존)
# (SW1 후 line range 518 가 SW2 변환으로 약간 shift 됐지만 카운트는 일치)

# (22) calDays / matchesDate / dayCatMap / catInfo / shiftMonth 시그니처 보존
grep -cE '(matchesDate|dayCatMap|calDays|shiftMonth|catInfo|fetchHolidays|HOLIDAYS_FALLBACK)' cha-bio-safety/src/pages/SchedulePage.tsx
# 기대: ≥10 hits (정의 + 호출)
```

#### 5d. Build gate

```bash
cd cha-bio-safety && npx tsc --noEmit
# 기대: exit 0

cd cha-bio-safety && npm run build
# 기대: exit 0
```

### Step 6 — atomic commit

verify gate 모두 PASS 후:

```bash
git add cha-bio-safety/src/pages/SchedulePage.tsx
git commit -m "$(cat <<'EOF'
tsx(13-schedule): SW2 — 캘린더 grid + 공휴일 라벨 + arrowBtn 삭제

- 캘린더 영역 (calendarEl) line 239~300 v0.1.1 Tailwind 변환:
  - 월 네비 ‹/› → lucide ChevronLeft/ChevronRight (size={20})
  - 요일 header text-caption + leading-none + 일/토/평 색 분기
  - 7×6 grid grid-cols-7 + 일자 cell Tailwind class + 동적 인라인 3 site
  - 공휴일 라벨 text-caption + text-danger + font-semibold + leading-none
- arrowBtn utility const 5 line 삭제 (사용처 0건 확인 후)
- ChevronRight lucide-react import 추가
- 비즈 로직 0 변경 (matchesDate / dayCatMap / catInfo / setSelDate / shiftMonth verbatim)

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"

git status
```

(commit message 카피는 verbatim 유지. Co-Authored-By 라인 포함.)
  </action>
  <verify>
    <automated>cd cha-bio-safety && npx tsc --noEmit && npm run build</automated>
  </verify>
  <done>
- `cha-bio-safety/src/pages/SchedulePage.tsx` 변경됨 (1 file 만)
- line 5 `ChevronRight` import 추가
- 캘린더 영역 (calendarEl) line 239~300 v0.1.1 Tailwind 변환 완료
- arrowBtn const 5 line 삭제 + 주석 marker 남김
- arrowBtn 식별자 전체 파일에 0 hit
- Step 5 의 verify gate 모두 PASS (negative 7 + positive 10 + 비즈 4)
- npx tsc --noEmit exit 0, npm run build exit 0
- atomic commit 1개 (hash 확인) — git status clean
- SW3+ 영역 (line 306~end) inline `style={{` 카운트 = SW1 SUMMARY 의 66 ± 0 (line range shift 만 발생, 카운트 보존)
  </done>
</task>

</tasks>

<verification>

### Pre-change baseline
- Branch: `redesign/13-schedule` (or wherever SW1 was committed)
- HEAD before SW2: commit `350d1ca` 또는 그 후속 (SW1 SUMMARY 박제 후)
- SchedulePage.tsx: 1078 lines, lucide import 3종 (ChevronLeft/Download/Plus)

### Post-change expected state
- SchedulePage.tsx: 약 1050~1075 lines (캘린더 영역 +20 lines 추가 / arrowBtn const -5 lines / 기타 변환 ±)
- import 4종 (ChevronLeft/ChevronRight/Download/Plus)
- arrowBtn 식별자 0 hit
- SW2 영역 안 `style={{` 정확히 3 hit
- SW3+ 영역 (line 306~end) `style={{` 카운트 변경 없음

### Negative gates (전체 verify Step 5a 참조)
변환 영역 안:
- linear-gradient 0
- 옛 alias var(--bg|bg2|...) 0
- 인라인 fontSize: 0
- status- prefix 0 (tailwind config 키 없음)
- 9·10·11px 0
- arrowBtn 식별자 0 (전체 파일)

### Positive gates (Step 5b 참조)
변환 영역 안:
- bg-surface-raised ≥1
- border-border-default ≥2
- text-danger ≥2
- text-accent ≥1
- text-text-tertiary ≥1
- text-caption + leading-none 조합 ≥1
- grid-cols-7 = 2 hits
- w-1 h-1 rounded-full ≥1

### Build
- `cd cha-bio-safety && npx tsc --noEmit` exit 0
- `cd cha-bio-safety && npm run build` exit 0

</verification>

<success_criteria>

1. **변환 완료**: D.2 BEFORE → D.4 AFTER snippet 의 정확한 1-pass 치환. 캘린더 영역 (line 239~300) 의 모든 인라인 `style` 중 §10 화이트리스트 (3 site) 외 모두 Tailwind class 로 교체.
2. **arrowBtn 삭제**: 정의 + 사용처 모두 0 hit. lbl / inp 는 보존.
3. **ChevronRight import**: line 5 에서 ChevronLeft 와 함께 import.
4. **비즈 로직 보존**: matchesDate / dayCatMap / catInfo / setSelDate / shiftMonth / calDays / curMonth / dotMap / holidays / dots / isRed / isHoliday / isSel / isToday / dow 모두 시그니처 / 호출 verbatim. toast 13 hits / scheduleApi 5 hits 변경 0.
5. **Scope 보존**: SW3+ 영역 (일자 카드 / 미리보기 / AddModal / EditModal) inline `style={{` 카운트 변경 없음 (= SW1 SUMMARY 의 66).
6. **Build PASS**: `npx tsc --noEmit` + `npm run build` 모두 exit 0.
7. **Atomic commit**: 1개 commit (`tsx(13-schedule): SW2 — 캘린더 grid + 공휴일 라벨 + arrowBtn 삭제`).
8. **No deviations**: D.4 snippet 1 byte 도 변경 없이 적용. import 4종 (ChevronLeft/ChevronRight/Download/Plus) 정확 alphabet 순.

</success_criteria>

<output>
After completion, create `.planning/quick/260520-0z0-redesign-13-schedule-tsx-sw2-grid-arrowb/260520-0z0-SUMMARY.md`.

Summary should include:
- Frontmatter (phase / plan / subsystem / tags / dependency-graph / tech-stack / key-files / decisions / metrics / threat-flags)
- One-Liner
- Scope (변경된 영역 + 0 변경 영역 — 표 형식)
- Implementation Diff (각 영역별 BEFORE/AFTER 핵심 lines)
- Verify Gate Results (Step 5 의 negative + positive + biz + build 모두 표)
- Build chunk size (dist/assets/SchedulePage-*.js 크기 / gzip)
- Commit hash + message
- Deviations from Plan (없으면 "None")
- Next Sub-Wave Recommendation: **SW3 — 일자 카드 리스트 (renderCard + scheduleListEl, line 306~430)**
- Self-Check: PASSED / FOUND / MISSING
</output>
