---
quick_id: 260527-37t
phase: quick
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cha-bio-safety/docs/redesign-context/31-chrome/sketch-wave-2-global-header.html
autonomous: true
requirements:
  - W2-GLOBALHEADER-SKETCH
must_haves:
  truths:
    - "단일 sketch HTML 파일 1개 생성 (sketch-wave-2-global-header.html)"
    - "다크 + 라이트 두 변형 모두 단일 파일 안에 시연 (data-theme 분기)"
    - "GlobalHeader 비즈 anchor 7건 머리 주석에 박스로 명시"
    - "OQ LOCKED 8건 매트릭스 머리 주석에 박스로 명시"
    - "햄버거 svg path \"M4 6h16M4 12h16M4 18h16\" verbatim 보존"
    - "노안 룰 §1.1 격상 — title fontSize 13 → 18 (text-title), 햄버거 button 32→48 (h-12)"
    - "토큰 매핑 v0.1.1 — legacy alias --bg/--bg2/--bg3/--bd/--t1/--t2 body markup 안 0건"
    - "보호 파일 1 byte 변경 0건 (src/components/GlobalHeader.tsx + 6 보호 파일)"
  artifacts:
    - path: "cha-bio-safety/docs/redesign-context/31-chrome/sketch-wave-2-global-header.html"
      provides: "GlobalHeader 단일 컴포넌트 sketch (45 라인 source 의 W7 TSX 변환 시안)"
      min_lines: 300
      contains:
        - "<!DOCTYPE html>"
        - "data-theme=\"light\""
        - "M4 6h16M4 12h16M4 18h16"
        - "var(--surface-raised)"
        - "var(--border-default)"
        - "var(--text-primary)"
        - "비즈 anchor"
        - "OQ LOCKED"
  key_links:
    - from: "sketch-wave-2-global-header.html 머리 주석"
      to: "GlobalHeader.tsx 비즈 anchor 7건"
      via: "verbatim 박스 인용 (W7 TSX 변환 1 byte 변경 0 강제)"
    - from: "sketch HTML :root + [data-theme=\"light\"]"
      to: "tokens.css v0.1.1 의미 토큰"
      via: "var(--surface-raised/border-default/surface-sunken/text-secondary/text-primary) 매핑"
---

<objective>
redesign/31-chrome 의 W2 sub-wave 단일 atomic — GlobalHeader (45 라인) sketch HTML 1 파일 산출.

Purpose: chrome 4 컴포넌트 중 가장 작은 GlobalHeader 의 v0.1.1 토큰 매핑 + 노안 격상 + 다크/라이트 양쪽 시각 시연. W7 TSX 변환 wave 진입 전 사용자가 양쪽 변형을 한 파일에서 검수할 수 있는 단일 sketch.

Output: `cha-bio-safety/docs/redesign-context/31-chrome/sketch-wave-2-global-header.html` 1 파일 (atomic 1-commit).

Out of scope:
- TSX 변환 (W7 책임)
- src/components/GlobalHeader.tsx 수정 (1 byte 변경 0)
- tokens.css / typography.css / design-system.md / tailwind.config.js / App.tsx 수정 (1 byte 변경 0)
- 31-chrome/wave-1-index.md 갱신 (W1 완결됨)
- 31-chrome/ 7 스냅샷 + 00-design-context/ 변경 (1 byte 변경 0)
- SideMenu / SettingsPanel / MenuSettingsSection sketch (W3/W4/W5 책임)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@CLAUDE.local.md
@cha-bio-safety/docs/redesign-context/31-chrome/wave-1-index.md
@cha-bio-safety/docs/redesign-context/31-chrome/GlobalHeader.tsx
@cha-bio-safety/docs/redesign-context/31-chrome/design-system.md
@cha-bio-safety/docs/redesign-context/31-chrome/tokens.css
@cha-bio-safety/docs/redesign-context/31-chrome/typography.css
@cha-bio-safety/docs/redesign-context/30-not-found/sketch.html
@cha-bio-safety/docs/redesign-context/28-splash/sketch-wave-2-splash-static-light.html
@cha-bio-safety/docs/redesign-context/10-cctv-info/sketch/cctv-info-sketch.html
@cha-bio-safety/src/App.tsx
@cha-bio-safety/tailwind.config.js

<interfaces>
<!-- GlobalHeader.tsx 비즈 anchor 7건 (W7 TSX 변환 1 byte 변경 0 강제) -->
<!-- W2 sketch HTML 머리 주석에 이 박스 verbatim 으로 인용. -->

From cha-bio-safety/docs/redesign-context/31-chrome/GlobalHeader.tsx (45 라인):

```tsx
interface GlobalHeaderProps {
  title: string
  onMenuOpen: () => void
  rightSlot?: React.ReactNode
  leftSlot?: React.ReactNode  // 햄버거 대신 표시 (예: 뒤로가기 버튼)
}

export function GlobalHeader({ title, onMenuOpen, rightSlot, leftSlot }: GlobalHeaderProps) {
  return (
    <header style={{
      display: 'flex',
      alignItems: 'center',
      height: 48,
      padding: '0 12px',
      background: 'var(--bg2)',
      borderBottom: '1px solid var(--bd)',
      flexShrink: 0,
    }}>
      {leftSlot ?? (
        <button
          onClick={onMenuOpen}
          aria-label="메뉴 열기"
          style={{
            width: 32, height: 32, borderRadius: 7,
            background: 'var(--bg3)', border: 'none',
            color: 'var(--t2)', cursor: 'pointer',
            display: 'flex', alignItems: 'center', justifyContent: 'center',
          }}
        >
          <svg width={15} height={15} fill="none" viewBox="0 0 24 24" stroke="var(--t2)" strokeWidth={2}>
            <path strokeLinecap="round" strokeLinejoin="round" d="M4 6h16M4 12h16M4 18h16"/>
          </svg>
        </button>
      )}
      <span style={{
        flex: 1, textAlign: rightSlot ? 'left' : 'center',
        fontSize: 13, fontWeight: 700, color: 'var(--t1)',
        marginLeft: rightSlot ? 8 : 0,
      }}>
        {title}
      </span>
      {rightSlot || <div style={{ width: 32 }} />}
    </header>
  )
}
```

From tokens.css (v0.1.1 의미 토큰 매핑):
- `--bg2` (alias) → `--surface-raised` (다크 #1a1f27, 라이트 #f6f8fa)
- `--bg3` (alias) → `--surface-sunken` (다크 #232a33, 라이트 #ebeef1)
- `--bd` (alias) → `--border-default` (다크 rgba(255,255,255,0.14), 라이트 rgba(0,0,0,0.14))
- `--t1` (alias) → `--text-primary` (다크 #e6edf3, 라이트 #1f2328)
- `--t2` (alias) → `--text-secondary` (다크 #adb6c0, 라이트 #4d5562)

From App.tsx (GlobalHeader mount 변형 — leftSlot/rightSlot 사용 패턴):
- line 196~215: `<GlobalHeader title onMenuOpen leftSlot rightSlot />` — 모바일 전용
- line 146~156: backBtn (svg path "M15 19l-7-7 7-7") — leftSlot 후보
- line 159~170: settingsGearBtn (svg cog path) — rightSlot 후보
- 사용 분기: isDashboard / isQrScan / isExtinguishers / isCctv 4 케이스 — sketch frame 2 + 4 에 시연
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: sketch-wave-2-global-header.html 단일 atomic 작성</name>
  <files>cha-bio-safety/docs/redesign-context/31-chrome/sketch-wave-2-global-header.html</files>
  <action>
GlobalHeader (45 라인 모바일 메인 헤더) sketch HTML 1 파일을 단일 atomic 으로 작성한다. 다크 + 라이트 양쪽 4~6 frame, 비즈 anchor 7건 박스, OQ LOCKED 8건 매트릭스, 노안 룰 §1.1 격상, v0.1.1 의미 토큰 매핑을 모두 한 파일에 담는다.

## 1. 파일 구조 (300~500 라인 예상)

```
<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>31 GlobalHeader · v0.1.1 재디자인 시안 (W2 sub-wave)</title>
  <!--
    [머리 주석 박스 4개 — 아래 §2~§5 verbatim]
  -->
  <link rel="preconnect" href="https://cdn.jsdelivr.net" />
  <style>
    [§6 :root 다크 + [data-theme="light"] 라이트 토큰 정의]
    [§7 sketch 외곽 chrome 스타일 — body / h1 / h2 / .vp-desc / .row / .caption]
    [§8 .frame-mobile + .global-header + .gh-* 컴포넌트 내부 스타일]
    [§9 .decision-summary / .biz-anchors / .negative-gate 요약 박스]
  </style>
</head>
<body>
  <h1>31 GlobalHeader · v0.1.1 재디자인 시안 (W2 sub-wave)</h1>
  <p>[메타 설명 1단락]</p>

  <h2>1. 다크 + 라이트 양쪽 4 frame</h2>
  <div class="row">
    [Frame 1] 다크 / 햄버거 default
    [Frame 2] 다크 / leftSlot=backBtn + rightSlot=settingsGearBtn
    [Frame 3] 라이트 / 햄버거 default
    [Frame 4] 라이트 / leftSlot=backBtn + rightSlot=settingsGearBtn
  </div>

  <h2>2. (선택) 햄버거 touch active state 시연</h2>
  [Frame 5/6 — 다크/라이트 active hover]

  <h2>3. 비즈 anchor 7건 박스</h2>
  [§3 박스 — anchor 1~7]

  <h2>4. OQ LOCKED 8건 매트릭스</h2>
  [§4 박스 — OQ #1~#8]

  <h2>5. 노안 룰 §1.1 격상 표</h2>
  [§5 박스 — fontSize / 터치 타겟 / svg size 격상]

  <h2>6. Negative gate (sketch 자체 verify)</h2>
  [§6 박스 — 9/10/11 0건 / linear-gradient 0건 / 이모지 0건 / legacy alias body 안 0건]
</body>
</html>
```

## 2. 머리 주석 §2 — 메타 (50~80 라인)

```html
<!--
  sketch-wave-2-global-header.html — redesign/31-chrome W2 sub-wave
  작성: 2026-05-27 / quick 260527-37t
  범위: GlobalHeader.tsx (45 라인) 단일 컴포넌트 sketch
        다크 + 라이트 2 변형 + 햄버거/back+gear 슬롯 변형 = 4 frame 매트릭스
  out of scope:
    - src/components/GlobalHeader.tsx 수정 (W7 책임 — 1 byte 변경 0 강제)
    - App.tsx / tokens.css / typography.css / design-system.md / tailwind.config.js 1 byte 변경 0
    - SideMenu / SettingsPanel / MenuSettingsSection sketch (W3/W4/W5 책임)
    - 31-chrome/wave-1-index.md 갱신 (W1 완결됨)
    - 31-chrome/ 7 스냅샷 / 00-design-context/ 1 byte 변경 0
  참조: 30-not-found/sketch.html (다크/라이트 양쪽 precedent)
        28-splash/sketch-wave-2-splash-static-light.html (단일 컴포넌트 atomic precedent)
        10-cctv-info/sketch/cctv-info-sketch.html (작은 page atomic precedent)
        design-system.md v0.1.1 / tokens.css / typography.css / wave-1-index.md §7 OQ LOCKED
-->
```

## 3. 머리 주석 §3 — 비즈 anchor 7건 박스 (verbatim)

```
==========================================================
비즈 anchor 7건 (GlobalHeader) — W7 TSX 변환 wave 1 byte 변경 0 강제
==========================================================

  1. GlobalHeaderProps interface { title, onMenuOpen, rightSlot, leftSlot } (line 1~6)
     - title: string
     - onMenuOpen: () => void
     - rightSlot?: React.ReactNode
     - leftSlot?: React.ReactNode

  2. export function GlobalHeader({ title, onMenuOpen, rightSlot, leftSlot }: ...) 시그니처 (line 8)

  3. <header> + display:flex + alignItems:center + flex-shrink:0 (line 10~18)
     - 스크롤 안 됨 (flex-shrink:0 = 0)
     - height 48 (h-12 Tailwind = 48 매치 ✓)
     - padding '0 12px' (px-3 매치 ✓)

  4. leftSlot ?? <button onClick={onMenuOpen} aria-label="메뉴 열기"> JSX 분기 (line 19~33)
     - 옵셔널 fallback ?? (||  와 다름 — 0/false/'' 도 leftSlot 으로 인정)

  5. svg path "M4 6h16M4 12h16M4 18h16" verbatim 보존 (line 30~32)
     - viewBox "0 0 24 24" / strokeWidth 2 / strokeLinecap/Linejoin "round"
     - W2 sketch 에서 Lucide Menu 교체 X (OQ #5 LOCKED — 인라인 svg 유지)

  6. title <span> + flex:1 + textAlign 분기 + marginLeft 분기 (line 35~41)
     - textAlign rightSlot ? 'left' : 'center'
     - marginLeft rightSlot ? 8 : 0

  7. rightSlot || <div style={{ width: 32 }} /> 옵셔널 fallback (line 42)
     - || 사용 (?? 아님) — placeholder 로 symmetry 보존 의도
```

## 4. 머리 주석 §4 — OQ LOCKED 8건 매트릭스

```
==========================================================
OQ LOCKED 8건 매트릭스 (wave-1-index.md §7 사용자 답변 반영)
==========================================================

  OQ #1: 다크 + 라이트 양쪽 지원
    - default 답: 다크 단일 유지
    - LOCKED 답: 다크 + 라이트 양쪽 지원 (사용자 명시 변경)
    - 본 W2 적용: sketch HTML 에 :root 다크 + [data-theme="light"] 라이트 둘 다 정의.
                  4 frame 중 2 frame data-theme="light" 부착.

  OQ #2: §1.1 9·10·11 위반 — 일괄 12 격상 vs 부분 절충
    - default 답: 부분 절충
    - LOCKED 답: 부분 절충 (GlobalHeader 의 fontSize 13 → 노안 룰 마지노선 16 격상 필요)
    - 본 W2 적용: title fontSize 13 → 18 (text-title — 페이지 헤더 제목)
                  햄버거 button 32×32 → 48×48 (h-12 w-12 = 48 = touch target 룰 — w-8 함정 회피)
                  svg 15×15 → 20 (Lucide size 표준)
                  right placeholder <div width:32> → <div width:48> (symmetry 보존)

  OQ #3: SideMenu 사용자 카드 아바타 그라데이션 유지
    - LOCKED 답: 유지 (§6.4 매치 ✓)
    - 본 W2 무관 (SideMenu 는 W3 책임)

  OQ #4: SettingsPanel 프로필 그라데이션 폐기
    - LOCKED 답: 폐기 → bg-accent solid
    - 본 W2 무관 (SettingsPanel 은 W4 책임)

  OQ #5: 인라인 svg ↔ Lucide 일관성
    - LOCKED 답: 인라인 svg 유지 (path "M4 6h16M4 12h16M4 18h16" verbatim)
    - 본 W2 적용: 햄버거 svg verbatim 보존. Lucide Menu 교체 X.

  OQ #6: admin 테스트 푸시 이모지 ⏳ 🔔 제거
    - LOCKED 답: 제거 → Lucide Send + Loader2
    - 본 W2 무관 (SettingsPanel 책임)

  OQ #7: 햄버거 panel width 300 + SettingsPanel width 320 보존
    - LOCKED 답: 보존
    - 본 W2 무관 (panel 은 W3/W4 책임)

  OQ #8: W7 TSX 변환 4 분할 (sub-wave)
    - LOCKED 답: 4 분할 (atomic 단일 변환 X)
    - 본 W2 적용: GlobalHeader 단일 sketch 가 W7 의 첫 sub-wave 대상.
```

## 5. 머리 주석 §5 — 노안 룰 §1.1 격상 표

```
==========================================================
노안 룰 §1.1 격상 표 (본 W2 적용)
==========================================================

  source (GlobalHeader.tsx)           →  sketch (W2)              비고
  ──────────────────────────────────     ──────────────────────   ──────────────────────────
  title fontSize 13                   →  18 (text-title)          페이지 헤더 = 카드 제목급
  title fontWeight 700                →  600 (font-semibold)      v0.1.1 룰 (700 무거움)
  햄버거 button 32×32                  →  48×48 (h-12 w-12)        터치 타겟 44 마지노선 + 8pt
  햄버거 button borderRadius 7         →  8 (var(--radius-sm))     v0.1.1 §radius (8/12/16/99)
  svg 15×15                           →  20 (size={20})           Lucide 표준 / 인라인 path 유지
  svg strokeWidth 2                   →  2 (유지)                  굵기 동일
  right placeholder <div width:32>    →  <div width:48>           symmetry 보존 — 햄버거와 동일
  header height 48                    →  48 (h-12)                현 source 부합 — 유지
  header padding '0 12px'             →  px-3 (12px)              유지
  legacy alias --bg2 / --bg3 / --bd /  →  v0.1.1 의미 토큰         §6 토큰 매핑 박스 참조
  --t1 / --t2 (다크 hex)
```

## 6. 머리 주석 §6 — 토큰 매핑 박스

```
==========================================================
토큰 매핑 (본 W2 적용 — design-system v0.1.1 §2)
==========================================================

  source (legacy alias)        →   sketch (v0.1.1 의미 토큰)
  ─────────────────────────       ──────────────────────────────
  background var(--bg2)        →   var(--surface-raised)
  borderBottom var(--bd)       →   var(--border-default)
  햄버거 background var(--bg3)  →   var(--surface-sunken)
  햄버거 color var(--t2)        →   var(--text-secondary)
  svg stroke var(--t2)         →   var(--text-secondary)
  title color var(--t1)        →   var(--text-primary)
  borderRadius 7               →   var(--radius-sm) (8px)
  height 48                    →   var(--space-8) (48 — 매치)
  padding '0 12px'             →   var(--space-3) horizontal

  ※ legacy alias --bg/--bg2/--bg3/--bd/--t1/--t2 는 :root/[data-theme] 정의 블록 안에서만 허용
     (tokens.css line 175~197 의 호환 alias 와 동일 패턴).
     body markup 안에서는 0건 — verify gate 강제.
```

## 7. 머리 주석 §7 — Negative gate (sketch 자체 verify)

```
==========================================================
Negative gate (sketch 본문 — W7 변환 시 의식할 룰)
==========================================================

  - 9 / 10 / 11 px fontSize 0건 (노안 §1.1 마지노선 16 룰)
  - linear-gradient 0건 (§6.4 매치 X — GlobalHeader 는 그라데이션 사용 X)
  - 이모지 0건 (memory feedback_tsx_wave_emoji_dot_gap)
  - status-* 색 0건 (chrome 은 status 아님 — accent / surface / text 만)
  - legacy alias --bg/--bg2/--bg3/--bd/--t1/--t2 body markup 안 0건
    (단 :root + [data-theme="light"] + 기존 코드 호환 alias 정의 블록 안만 허용)
  - svg path "M4 6h16M4 12h16M4 18h16" verbatim 보존 (Lucide Menu 교체 X)
```

## 8. <style> 블록 (§6~§9 인용)

§6 :root + [data-theme="light"] 토큰 정의 (30-not-found/sketch.html line 53~111 패턴 mirror):

```css
:root {
  /* Surface */
  --surface-raised:  #1a1f27;
  --surface-sunken:  #232a33;
  --surface-page:    #0a0d12;
  --surface-active:  #2c333d;

  /* Text */
  --text-primary:    #e6edf3;
  --text-secondary:  #adb6c0;
  --text-tertiary:   #8b949e;

  /* Border */
  --border-default:  rgba(255, 255, 255, 0.14);

  /* Radius */
  --radius-sm:       8px;
  --radius-md:       12px;
  --radius-lg:       16px;

  /* Spacing */
  --space-3:         12px;
  --space-8:         48px;

  /* Font */
  --font-sans:       'Pretendard Variable', 'Pretendard', system-ui, -apple-system, sans-serif;
}

[data-theme="light"] {
  --surface-raised:  #f6f8fa;
  --surface-sunken:  #ebeef1;
  --surface-page:    #ffffff;
  --surface-active:  #d8dee4;

  --text-primary:    #1f2328;
  --text-secondary:  #4d5562;
  --text-tertiary:   #656d76;

  --border-default:  rgba(0, 0, 0, 0.14);
}
```

§7 sketch 외곽 chrome (body, h1, h2, .vp-desc, .row, .caption — 30-not-found 의 line 113~168 mirror).

§8 .frame-mobile + .global-header (GlobalHeader 자체 시각화):

```css
.frame-mobile {
  width: 480px;
  background: var(--surface-page);
  color: var(--text-primary);
  border: 1px solid var(--border-default);
  border-radius: var(--radius-lg);
  overflow: hidden;
  font-family: var(--font-sans);
}

.global-header {
  display: flex;
  align-items: center;
  height: 48px;             /* h-12 — source 부합 */
  padding: 0 var(--space-3);
  background: var(--surface-raised);
  border-bottom: 1px solid var(--border-default);
  /* flex-shrink: 0 의도 — frame 안에서는 컨테이너가 컬럼 1개라 생략 가능 */
}

.gh-icon-btn {
  width: 48px; height: 48px;            /* 32→48 격상 */
  border-radius: var(--radius-sm);      /* 7→8 */
  background: var(--surface-sunken);
  border: none;
  color: var(--text-secondary);
  cursor: pointer;
  display: flex; align-items: center; justify-content: center;
}
.gh-icon-btn:hover { background: var(--surface-active); }

.gh-title {
  flex: 1;
  font-size: 18px;          /* 13→18 (text-title) */
  font-weight: 600;         /* 700→600 */
  line-height: 1.4;
  color: var(--text-primary);
  /* textAlign / marginLeft 분기는 .gh-title--center / .gh-title--left modifier 로 표현 */
}
.gh-title--center { text-align: center; margin-left: 0; }
.gh-title--left   { text-align: left; margin-left: 8px; }

.gh-right-spacer { width: 48px; height: 48px; }   /* placeholder symmetry */
```

§9 .decision-summary / .biz-anchors / .negative-gate (30-not-found line 220~278 mirror).

## 9. <body> 변형 4 frame (필수) + 선택 2 frame

각 frame 은 .frame-mobile 컨테이너 + .global-header 마크업.

Frame 1 (다크 / 햄버거 default):
```html
<div class="frame-mobile">
  <header class="global-header">
    <button class="gh-icon-btn" aria-label="메뉴 열기">
      <svg width="20" height="20" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
        <path stroke-linecap="round" stroke-linejoin="round" d="M4 6h16M4 12h16M4 18h16"/>
      </svg>
    </button>
    <span class="gh-title gh-title--center">대시보드</span>
    <div class="gh-right-spacer"></div>
  </header>
</div>
<p class="caption">다크 · 햄버거 default · title <code>대시보드</code> · rightSlot 없음 → center align + placeholder</p>
```

Frame 2 (다크 / leftSlot=back + rightSlot=settings):
```html
<div class="frame-mobile">
  <header class="global-header">
    <button class="gh-icon-btn" aria-label="뒤로가기">
      <svg width="20" height="20" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
        <path stroke-linecap="round" stroke-linejoin="round" d="M15 19l-7-7 7-7"/>
      </svg>
    </button>
    <span class="gh-title gh-title--left">소화기 점검</span>
    <button class="gh-icon-btn" aria-label="설정">
      <svg width="20" height="20" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.8">
        <path stroke-linecap="round" stroke-linejoin="round" d="M10.325 4.317c.426-1.756 2.924-1.756 3.35 0a1.724 1.724 0 002.573 1.066c1.543-.94 3.31.826 2.37 2.37a1.724 1.724 0 001.065 2.572c1.756.426 1.756 2.924 0 3.35a1.724 1.724 0 00-1.066 2.573c.94 1.543-.826 3.31-2.37 2.37a1.724 1.724 0 00-2.572 1.065c-.426 1.756-2.924 1.756-3.35 0a1.724 1.724 0 00-2.573-1.066c-1.543.94-3.31-.826-2.37-2.37a1.724 1.724 0 00-1.065-2.572c-1.756-.426-1.756-2.924 0-3.35a1.724 1.724 0 001.066-2.573c-.94-1.543.826-3.31 2.37-2.37.996.608 2.296.07 2.572-1.065z"/>
        <circle cx="12" cy="12" r="3"/>
      </svg>
    </button>
  </header>
</div>
<p class="caption">다크 · leftSlot=뒤로가기 (App.tsx line 146 backBtn) + rightSlot=설정 (line 159 settingsGearBtn) → left align + ml 8</p>
```

Frame 3 (라이트 / 햄버거 default — 다크 frame 1 mirror + data-theme="light"):
```html
<div class="frame-mobile" data-theme="light">
  [동일 .global-header 햄버거 default]
</div>
<p class="caption">라이트 · 햄버거 default · OQ #1 LOCKED 사용자 명시 변경 반영</p>
```

Frame 4 (라이트 / leftSlot+rightSlot — 다크 frame 2 mirror + data-theme="light"):
```html
<div class="frame-mobile" data-theme="light">
  [동일 .global-header back + settings]
</div>
<p class="caption">라이트 · leftSlot+rightSlot · 라이트 토큰 hex 검수용</p>
```

(선택) Frame 5/6 — touch active state (다크/라이트 .gh-icon-btn:active background var(--surface-active) 시연). 시간 여유 없으면 생략 가능.

## 10. <h2> 박스 — 비즈 anchor / OQ LOCKED / 노안 격상 / Negative gate

머리 주석 §3~§7 박스를 body 안에서 시각적으로 다시 보여준다 (사용자가 frame 옆에서 박스를 동시에 볼 수 있어야 함). 30-not-found/sketch.html 의 .decision-summary / .biz-anchors / .negative-gate 패턴 mirror.

```html
<h2>3. 비즈 anchor 7건 (W7 TSX 변환 1 byte 변경 0 강제)</h2>
<div class="biz-anchors">
  <ul>
    <li><strong>1. GlobalHeaderProps</strong> interface { title, onMenuOpen, rightSlot, leftSlot } (line 1~6)</li>
    <li><strong>2. export function</strong> GlobalHeader({ title, onMenuOpen, rightSlot, leftSlot }) (line 8)</li>
    <li><strong>3. &lt;header&gt;</strong> + flex-shrink:0 (스크롤 안 됨) (line 10~18)</li>
    <li><strong>4. leftSlot ??</strong> &lt;button onClick={onMenuOpen} aria-label="메뉴 열기"&gt; (?? 사용, || 와 다름) (line 19~33)</li>
    <li><strong>5. svg path</strong> <code>M4 6h16M4 12h16M4 18h16</code> verbatim (line 30~32)</li>
    <li><strong>6. title &lt;span&gt;</strong> flex:1 + textAlign 분기 + marginLeft 분기 (line 35~41)</li>
    <li><strong>7. rightSlot ||</strong> &lt;div style={{ width: 32 }} /&gt; (|| 사용, ?? 와 다름) (line 42)</li>
  </ul>
</div>

<h2>4. OQ LOCKED 8건 매트릭스</h2>
<div class="decision-summary">
  <ul>
    [OQ #1~#8 — §4 매트릭스 mirror]
  </ul>
</div>

<h2>5. 노안 룰 §1.1 격상 표</h2>
<div class="decision-summary">
  [표 — §5 격상 표 mirror]
</div>

<h2>6. Negative gate</h2>
<div class="negative-gate">
  <ul>
    <li>9 / 10 / 11 px fontSize 0건 (노안 §1.1 마지노선 16 룰)</li>
    <li>linear-gradient 0건 (chrome 그라데이션 사용 X)</li>
    <li>이모지 0건</li>
    <li>status-* 색 0건 (chrome 은 status 아님)</li>
    <li>legacy alias --bg/--bg2/--bg3/--bd/--t1/--t2 body markup 안 0건 (정의 블록 안만 허용)</li>
    <li>svg path "M4 6h16M4 12h16M4 18h16" verbatim 보존 (Lucide Menu 교체 X)</li>
  </ul>
</div>
```

## 11. 작성 가이드 (실행자에게)

- 300~500 라인 단일 atomic — sub-wave 분할 X
- 30-not-found/sketch.html 의 라이트/다크 row 패턴을 그대로 mirror (이미 검증된 precedent)
- 머리 주석 박스는 §2~§7 7건 모두 verbatim 포함 (생략 금지)
- :root + [data-theme="light"] 정의 블록 외에는 raw hex 0 (body markup 안)
- legacy alias 사용 0 (body markup 안)
- svg path "M4 6h16M4 12h16M4 18h16" 한 글자도 바꾸지 않는다
- App.tsx backBtn (line 146~156) + settingsGearBtn (line 159~170) 의 인라인 svg path 를 Frame 2/4 에 verbatim 보여준다
- font-family: Pretendard Variable CDN import 1회 (외부 리소스 1건만)
- 사용자가 cha-bio-safety/docs/redesign-context/31-chrome/sketch-wave-2-global-header.html 을 브라우저로 열어 다크/라이트 4 frame 을 한 화면에서 동시 검수 가능해야 함
- 보호 파일 1 byte 변경 0: src/components/GlobalHeader.tsx + tokens.css + typography.css + design-system.md + tailwind.config.js + App.tsx + 31-chrome/wave-1-index.md + 31-chrome/ 7 스냅샷 + 00-design-context/

작성 완료 후 verify 섹션의 grep 검증을 실행하여 모든 negative gate 0건 + 필수 토큰 ≥1건 확인.
  </action>
  <verify>
    <automated>cd /Users/jykevin/Documents/cbc7119-design && bash -c '
set -e
F=cha-bio-safety/docs/redesign-context/31-chrome/sketch-wave-2-global-header.html

echo "=== 1. 파일 존재 ==="
test -f "$F"

echo "=== 2. DOCTYPE + meta ==="
grep -q "<!DOCTYPE html>" "$F"
grep -q "lang=\"ko\"" "$F"

echo "=== 3. 4 frame 최소 보장 (다크 2 + 라이트 2) ==="
FRAMES=$(grep -c "class=\"frame-mobile\"" "$F")
test "$FRAMES" -ge 4 || { echo "frame-mobile count: $FRAMES (need >=4)"; exit 1; }

echo "=== 4. 라이트 변형 ≥2 ==="
LIGHT=$(grep -c "data-theme=\"light\"" "$F")
test "$LIGHT" -ge 2 || { echo "data-theme light count: $LIGHT (need >=2)"; exit 1; }

echo "=== 5. 햄버거 svg path verbatim 보존 ==="
grep -q "M4 6h16M4 12h16M4 18h16" "$F"

echo "=== 6. backBtn svg path (Frame 2/4) ==="
grep -q "M15 19l-7-7 7-7" "$F"

echo "=== 7. v0.1.1 의미 토큰 5종 ≥1건 each ==="
grep -q "var(--surface-raised)" "$F"
grep -q "var(--surface-sunken)" "$F"
grep -q "var(--border-default)" "$F"
grep -q "var(--text-primary)" "$F"
grep -q "var(--text-secondary)" "$F"

echo "=== 8. 비즈 anchor 박스 ==="
grep -q "비즈 anchor" "$F"
grep -q "GlobalHeaderProps" "$F"

echo "=== 9. OQ LOCKED 매트릭스 ==="
grep -q "OQ LOCKED" "$F" || grep -q "OQ #1" "$F"

echo "=== 10. 노안 룰 §1.1 ==="
grep -q "§1.1" "$F" || grep -q "노안" "$F"

echo "=== 11. NEGATIVE: 9/10/11 px fontSize body 안 0건 ==="
# style 블록 + :root 정의는 px 사용 가능 — 단 fontSize/font-size: 9px|10px|11px 패턴 0
BAD_FS=$(grep -E "font-size:\s*(9|10|11)px[^0-9]" "$F" | grep -v "^[[:space:]]*\*" | wc -l | tr -d " ")
test "$BAD_FS" = "0" || { echo "BAD font-size 9/10/11px count: $BAD_FS"; grep -n -E "font-size:\s*(9|10|11)px[^0-9]" "$F"; exit 1; }

echo "=== 12. NEGATIVE: linear-gradient 0건 (정의/주석 제외) ==="
LG=$(grep -v "^[[:space:]]*[*/-]" "$F" | grep -c "linear-gradient" || true)
test "$LG" = "0" || { echo "linear-gradient count: $LG"; grep -n "linear-gradient" "$F"; exit 1; }

echo "=== 13. NEGATIVE: legacy alias body markup 안 0건 ==="
# :root 블록 안 (line 50~200 추정) 외에서 var(--bg2) 등 사용 시 fail
# 간단 검증: body 태그 이후 legacy alias 등장 0
BODY_LINE=$(grep -n "<body>" "$F" | head -1 | cut -d: -f1)
test -n "$BODY_LINE"
LEGACY_IN_BODY=$(tail -n +"$BODY_LINE" "$F" | grep -c -E "var\(--(bg|bg2|bg3|bg4|bd|bd2|t1|t2|t3)\)" || true)
test "$LEGACY_IN_BODY" = "0" || { echo "legacy alias in body count: $LEGACY_IN_BODY"; tail -n +"$BODY_LINE" "$F" | grep -n -E "var\(--(bg|bg2|bg3|bg4|bd|bd2|t1|t2|t3)\)"; exit 1; }

echo "=== 14. NEGATIVE: status-* 색 0건 ==="
ST=$(grep -c -E "var\(--status-" "$F" || true)
test "$ST" = "0" || { echo "status-* count: $ST"; exit 1; }

echo "=== 15. NEGATIVE: 이모지 0건 (한글/영문/숫자/기호 외 멀티바이트 글리프) ==="
# 보수적 검증: 흔한 이모지 후보 0
EMOJI=$(grep -c -E "(⏳|🔔|✅|❌|⚠|🚀|🔧|📦|💡|🎉)" "$F" || true)
test "$EMOJI" = "0" || { echo "emoji count: $EMOJI"; exit 1; }

echo "=== 16. 라인 수 300~700 ==="
LC=$(wc -l < "$F" | tr -d " ")
test "$LC" -ge 250 || { echo "line count: $LC (need >=250)"; exit 1; }
test "$LC" -le 800 || { echo "line count: $LC (need <=800)"; exit 1; }

echo "=== 17. 보호 파일 1 byte 변경 0 ==="
git diff --quiet HEAD -- cha-bio-safety/src/components/GlobalHeader.tsx cha-bio-safety/src/App.tsx cha-bio-safety/docs/redesign-context/31-chrome/GlobalHeader.tsx cha-bio-safety/docs/redesign-context/31-chrome/tokens.css cha-bio-safety/docs/redesign-context/31-chrome/typography.css cha-bio-safety/docs/redesign-context/31-chrome/design-system.md cha-bio-safety/docs/redesign-context/31-chrome/wave-1-index.md cha-bio-safety/tailwind.config.js 2>/dev/null || { echo "보호 파일 변경 감지!"; git diff --stat HEAD -- cha-bio-safety/src/components/GlobalHeader.tsx cha-bio-safety/src/App.tsx cha-bio-safety/docs/redesign-context/31-chrome/; exit 1; }

# 31-chrome/ 7 스냅샷 (W2 산출 신규 파일 외 변경 0)
CHANGED_IN_31=$(git status --porcelain cha-bio-safety/docs/redesign-context/31-chrome/ | grep -v "sketch-wave-2-global-header.html" | wc -l | tr -d " ")
test "$CHANGED_IN_31" = "0" || { echo "31-chrome 다른 파일 변경 감지!"; git status cha-bio-safety/docs/redesign-context/31-chrome/; exit 1; }

# 00-design-context 변경 0
git diff --quiet HEAD -- cha-bio-safety/docs/redesign-context/00-design-context/ 2>/dev/null || { echo "00-design-context 변경 감지!"; exit 1; }

echo ""
echo "✓ ALL 17 VERIFY GATES PASS"
'
    </automated>
  </verify>
  <done>
sketch-wave-2-global-header.html 1 파일이 단일 atomic 으로 작성되어:
- 다크 frame 2개 + 라이트 frame 2개 (총 ≥4 frame)
- 비즈 anchor 7건 박스 (머리 주석 + body 안 둘 다)
- OQ LOCKED 8건 매트릭스 박스
- 노안 룰 §1.1 격상 표
- Negative gate 박스
- v0.1.1 의미 토큰 5종 (--surface-raised, --surface-sunken, --border-default, --text-primary, --text-secondary) 모두 사용
- 햄버거 svg path "M4 6h16M4 12h16M4 18h16" verbatim 보존
- backBtn svg path "M15 19l-7-7 7-7" frame 2/4 에 verbatim 보여줌
- 9/10/11 px fontSize 0건 / linear-gradient 0건 / 이모지 0건 / legacy alias body 안 0건 / status-* 0건
- 보호 파일 (GlobalHeader.tsx + App.tsx + tokens.css + typography.css + design-system.md + wave-1-index.md + tailwind.config.js + 7 스냅샷 + 00-design-context) 1 byte 변경 0
verify 17 게이트 PASS.
  </done>
</task>

</tasks>

<verification>
**파일 단일 산출:**
- `cha-bio-safety/docs/redesign-context/31-chrome/sketch-wave-2-global-header.html` 1 파일 (250~800 라인)

**시각 검수 (사용자):**
- 브라우저로 파일 열어 다크 2 frame + 라이트 2 frame 한 화면에서 동시 확인
- 햄버거 button 48×48 (32 → 48 격상) 시각 적절성 판단
- title fontSize 18 (13 → 18 격상) 페이지 헤더 가독성 판단
- 라이트 모드 토큰 hex 시각 적절성 판단 (#f6f8fa / #ebeef1 / #1f2328 / #4d5562)
- 4 frame symmetry (햄버거 default / back+settings 변형) 검수

**Negative gate (자동):**
- 9/10/11 px fontSize 0건
- linear-gradient 0건
- 이모지 0건
- status-* 색 0건
- legacy alias --bg/--bg2/--bg3/--bd/--t1/--t2 body markup 안 0건

**비즈 anchor 보존 (자동):**
- svg path "M4 6h16M4 12h16M4 18h16" verbatim (햄버거)
- svg path "M15 19l-7-7 7-7" verbatim (backBtn frame 2/4)
- GlobalHeaderProps interface 인용
- export function GlobalHeader 시그니처 인용

**보호 파일 (자동):**
- src/components/GlobalHeader.tsx 변경 0
- App.tsx / tokens.css / typography.css / design-system.md / tailwind.config.js 변경 0
- 31-chrome/wave-1-index.md + 7 스냅샷 변경 0
- 00-design-context/ 변경 0
</verification>

<success_criteria>
1. sketch-wave-2-global-header.html 1 파일 생성 (atomic 1-commit)
2. verify 17 게이트 모두 PASS
3. 사용자가 브라우저로 열어 다크/라이트 4 frame 시각 검수 가능
4. W7 TSX 변환 wave 진입 시 비즈 anchor 7건 박스 + svg path verbatim 으로 1 byte 변경 0 강제 가능
5. 보호 파일 변경 0 byte (git diff 검증)
</success_criteria>

<output>
After completion, create `.planning/quick/260527-37t-redesign-31-chrome-w2-globalheader-sketc/260527-37t-SUMMARY.md`
</output>
