---
phase: quick
plan: 01
type: execute
wave: 6
quick_id: 260517-e9s
depends_on: [260517-e9c, 260517-e9d, 260517-e9r, 260517-e9e, 260517-e9k]
files_modified:
  - cha-bio-safety/docs/redesign-context/09-extinguishers/sketch/states-sketch.html
---

<objective>
Card grid 의 3 상태 (Loading skeleton / Error / Empty) v0.1.1 토큰 sketch. 이 wave 가 09 sketch 시리즈 마지막 — 완료 후 TSX 변환 wave 진입.

영역:
- SKELETON_STYLE (l.15-20) — 카드 자리 차지 skeleton (blink animation)
- Loading 블록 (l.493-499)
- Error 블록 (l.502-521) — 타이틀 + 안내 + "다시 시도" 버튼
- Empty 블록 (l.524-535) — 타이틀 + 안내 텍스트
- gridCols 반응형 (모바일 1열 / 768+ 2열 / 데스크톱 3열)
</objective>

<context>

# 토큰 매핑

## SKELETON_STYLE (l.15-20)
- bg `var(--bg3)` → `--surface-sunken`
- borderRadius 12 → `--radius-md`
- height 88 그대로
- animation `blink 2s ease-in-out infinite` 그대로
- @keyframes blink: 0%/100% opacity 0.6, 50% opacity 0.3

## Error wrapper (l.503-507)
- flex column, gap 8, padding 60 16
- 타이틀: fontSize 16 / fontWeight 700 / `var(--t1)` → `--text-primary`
- sub: fontSize 12 / `var(--t2)` → `--text-secondary` / textAlign center
- "다시 시도" 버튼:
  - height 36 / padding 0 16 / borderRadius 8 → `--radius-sm`
  - bg `var(--bg3)` → `--surface-sunken`
  - border `1px solid var(--bd2)` → `--border-strong`
  - color `var(--t1)` → `--text-primary`
  - fontSize 12 / fontWeight 700 / cursor pointer
  - marginTop 8

## Empty wrapper (l.525-535)
- flex column, gap 8, padding 60 16
- 타이틀: fontSize 16 / fontWeight 700 / `var(--t1)` → `--text-primary`
- 안내 텍스트: fontSize 12 / `var(--t2)` → `--text-secondary` / textAlign center / lineHeight 1.6
- 텍스트: "필터를 조정하거나 우상단 「+ 새로 등록」 으로 새 자산을 추가해 주세요."

# Viewport 매트릭스

| ID | 상태 | 핵심 |
|---|---|---|
| V1 | Loading 모바일 1열 | 3 skeleton 카드 + blink animation |
| V2 | Loading 데스크톱 3열 | 3 skeleton 카드 (1열에 3개) |
| V3 | Error 모바일 | "목록을 불러오지 못했습니다" + 재시도 버튼 |
| V4 | Empty 모바일 | "해당하는 소화기가 없습니다" + 「+ 새로 등록」 안내 |

</context>

<tasks>
<task type="auto">
  <name>Task 1: states-sketch.html 작성</name>
  <files>cha-bio-safety/docs/redesign-context/09-extinguishers/sketch/states-sketch.html</files>
  <action>
1. :root — 이전 wave 토큰 set
2. @keyframes blink 정의 (0/100% opacity 0.6, 50% opacity 0.3)
3. 4 viewport — 카드 grid wrapper 안 3 상태 + grid 반응형 1/3열
4. rule 박스 마지막 + "다음 단계: TSX 변환 wave" 명시
  </action>
</task>
</tasks>
