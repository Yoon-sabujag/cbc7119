---
phase: 260518-lr5
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cha-bio-safety/docs/redesign-context/12-staff-service/sketch/06-desktop-3panel-sketch.html
autonomous: true
requirements:
  - W6-DESKTOP-3PANEL
  - W6-LEFT-PANEL-REUSE
  - W6-CENTER-PLACEHOLDER
  - W6-RIGHT-PLACEHOLDER
  - W6-TOKEN-CONTRACT
  - W6-OQ5-LOCKED

must_haves:
  truths:
    - "1280px wide desktop frame in BOTH dark and light themes shows a 3-panel layout (left=flex / center=280px fixed / right=flex) with vertical borders between panels"
    - "Left panel inside each frame reuses W2 calendar (desktop variant: aspectRatio 1.2, 7×6, all cell states), W3 legend (11 dots) + 4 stat cards (요약), divider, W4 menu cards (중식 A/B/석식) + 데스크톱 dropzone (48px dashed)"
    - "Center panel shows '휴가신청서' title + a Wave 7 placeholder callout (dashed border) with text 'Wave 7 sketch 예정'"
    - "Right panel shows centered Wave 8 placeholder callout (dashed border) with text 'Wave 8 sketch 예정' referencing A4 595px max-w and lp[0..16] overlay"
    - "GlobalHeader placeholder strip (54px, 햄버거 32 + title '연차 및 식사' + 톱니) sits above the 3-panel body in each frame"
    - "OQ #5 locked decision is visually enforced: NO selCell detail inside the left panel — selection detail belongs to the center panel (휴가신청서)"
  artifacts:
    - path: "cha-bio-safety/docs/redesign-context/12-staff-service/sketch/06-desktop-3panel-sketch.html"
      provides: "Wave 6 desktop 3-panel skeleton sketch (dark + light, 1280px each)"
      contains: ":root, [data-theme=\"light\"], width: 280px, border-right, Wave 7 sketch 예정, Wave 8 sketch 예정"
  key_links:
    - from: "06-desktop-3panel-sketch.html"
      to: "tokens.css :root + [data-theme=\"light\"] block"
      via: "verbatim :root copy (NO alias tokens used in markup)"
      pattern: "--surface-page|--border-default|--text-primary|--text-tertiary|--accent"
    - from: "06-desktop-3panel-sketch.html"
      to: "StaffServicePage.tsx lines 1020~1040"
      via: "exact 3-panel skeleton (left flex 1 minWidth 0 borderRight, center 280 flexShrink 0 borderRight, right flex 1) and left panel ordering (calendar → legend → summary → divider → menu wrapper → upload)"
      pattern: "width: 280px|width:280px|flex-shrink: 0|border-right.*var\\(--border-default\\)"
    - from: "06-desktop-3panel-sketch.html (left panel)"
      to: "W2/W3/W4 sketch decisions"
      via: "desktop sizing reuse — calendar 18px header / 13 weekday / aspectRatio 1.2 / dots 10×10 / stat 4 cards / menu grid 2col / dropzone 48px dashed"
      pattern: "aspect-ratio:1.2|aspectRatio: 1.2|dropzone|width:10px;height:10px"
    - from: "06-desktop-3panel-sketch.html (categorical colors)"
      to: "정규화 카테고리 hex set"
      via: "left panel duty cells, legend dots, stat cards, menu cards all use the 7 normalized hex set"
      pattern: "#42d778|#8f42d7|#d78042|#d74242|#d7428c|#4244d7|#06b6d4"
---

<objective>
Produce Wave 6 desktop 3-panel skeleton sketch for the StaffServicePage redesign (page #12, redesign/12-staff-service).

Purpose:
- Lock the desktop 3-panel skeleton (좌 flex / 중앙 280 fixed / 우 flex) AND visualize the fully-populated LEFT panel (달력 + 범례 + 요약 + 식단 + 업로드 — W2~W4 reuse at desktop sizing).
- Center + right panels render as **placeholders** with explicit "Wave 7 sketch 예정" / "Wave 8 sketch 예정" labels.
- Enforce UI-SPEC §14 OQ #5 locked decision: **NO selCell detail in the left panel** — selection detail is owned by the center panel (휴가신청서).

Output:
- Single static HTML sketch file at `cha-bio-safety/docs/redesign-context/12-staff-service/sketch/06-desktop-3panel-sketch.html`.
- 2 frames stacked vertically: one dark, one light, each 1280px wide.
- This is a **sketch only** — do NOT touch `src/`, do NOT touch other sketch files (01~05), do NOT run wrangler.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@cha-bio-safety/docs/redesign-context/12-staff-service/UI-SPEC.md
@cha-bio-safety/docs/redesign-context/12-staff-service/tokens.css
@cha-bio-safety/docs/redesign-context/12-staff-service/typography.css
@cha-bio-safety/docs/redesign-context/12-staff-service/StaffServicePage.tsx
@cha-bio-safety/docs/redesign-context/12-staff-service/sketch/02-calendar-grid-sketch.html
@cha-bio-safety/docs/redesign-context/12-staff-service/sketch/03-legend-summary-sketch.html
@cha-bio-safety/docs/redesign-context/12-staff-service/sketch/04-menu-cards-sketch.html
@cha-bio-safety/docs/redesign-context/12-staff-service/sketch/05-bottomsheet-sketch.html
@CLAUDE.md
@CLAUDE.local.md

<interfaces>
<!--
Extracted from tokens.css (lines 16~69 dark, 74~119 light, 124~146 primitive spacing, 148~162 desktop @media branch, 167~172 radius).
The sketch MUST embed :root + [data-theme="light"] verbatim, NO alias block.
The sketch MUST inline the desktop spacing values directly (or wrap in @media (min-width: 768px) — recommended since the sketch frame is 1280 wide so the @media branch is always active anyway. Either embed @media block verbatim OR inline desktop values directly in markup. Choose ONE consistent strategy.)
-->

Dark theme tokens (must appear verbatim — `:root, [data-theme="dark"] { ... }`):
```
--surface-page: #0a0d12;
--surface-raised: #1a1f27;
--surface-sunken: #232a33;
--surface-active: #2c333d;
--surface-overlay: rgba(0, 0, 0, 0.6);
--text-primary: #e6edf3;
--text-secondary: #adb6c0;
--text-tertiary: #8b949e;
--text-disabled: #5d646e;
--text-on-accent: #ffffff;
--text-link: #58a6ff;
--border-default: rgba(255, 255, 255, 0.14);
--border-strong: rgba(255, 255, 255, 0.22);
--border-focus: #3b82f6;
--accent: #3b82f6;
--accent-hover: #60a5fa;
--accent-active: #2563eb;
--status-safe: #4ade80;
--status-warning: #fbbf24;
--status-danger: #f87171;
--status-info: #38bdf8;
--status-fire: #fb923c;
--status-safe-bar: #22c55e;
--status-warning-bar: #f59e0b;
--status-danger-bar: #ef4444;
--status-info-bar: #0ea5e9;
--status-fire-bar: #f97316;
--status-safe-bg: rgba(34, 197, 94, 0.16);
--status-warning-bg: rgba(245, 158, 11, 0.16);
--status-danger-bg: rgba(239, 68, 68, 0.16);
--status-info-bg: rgba(14, 165, 233, 0.16);
--status-fire-bg: rgba(249, 115, 22, 0.16);
--duty-day: #f59e0b;
--duty-night: #ef4444;
--duty-off: #3b82f6;
--duty-leave: #6b7280;
```

Light theme tokens (must appear verbatim — `[data-theme="light"] { ... }`):
```
--surface-page: #ffffff;
--surface-raised: #f6f8fa;
--surface-sunken: #ebeef1;
--surface-active: #d8dee4;
--surface-overlay: rgba(0, 0, 0, 0.5);
--text-primary: #1f2328;
--text-secondary: #4d5562;
--text-tertiary: #656d76;
--text-disabled: #afb8c1;
--text-on-accent: #ffffff;
--text-link: #0969da;
--border-default: rgba(0, 0, 0, 0.14);
--border-strong: rgba(0, 0, 0, 0.22);
--border-focus: #1f6feb;
--accent: #1f6feb;
--accent-hover: #218bff;
--accent-active: #0a52c4;
--status-safe: #166534;
--status-warning: #854d0e;
--status-danger: #991b1b;
--status-info: #075985;
--status-fire: #9a3412;
--status-safe-bar: #15803d;
--status-warning-bar: #b45309;
--status-danger-bar: #b91c1c;
--status-info-bar: #0369a1;
--status-fire-bar: #c2410c;
--status-safe-bg: #dcfce7;
--status-warning-bg: #fef3c7;
--status-danger-bg: #fee2e2;
--status-info-bg: #e0f2fe;
--status-fire-bg: #ffedd5;
--duty-day: #b45309;
--duty-night: #b91c1c;
--duty-off: #1f6feb;
--duty-leave: #6b7280;
```

Primitive spacing + radius (theme-independent block — `:root { --space-1..--space-8; --card-padding..--button-padding-x; --radius-sm..--radius-pill; }`) — copy verbatim from tokens.css lines 124~146 + 167~172. Desktop @media branch (lines 148~162) MUST also be included verbatim so the 1280px sketch frame is always in desktop mode.

DO NOT include the alias block (lines 177~197). NO `--bg`, `--bg2`, `--bd`, `--t1/2/3`, `--c-day` etc anywhere in markup.

Source skeleton extract (StaffServicePage.tsx lines 1020~1041) — the exact 3-panel pattern this sketch must visualize:
```tsx
<div style={{ height: '100%', display: 'flex', flexDirection: 'column', overflow: 'hidden', background: 'var(--bg)' }}>
  {/* 페이지 제목은 App.tsx 헤더에서 표시 */}
  <div style={{ flex: 1, display: 'flex', overflow: 'hidden' }}>
    {/* 좌측 */}
    <div style={{ flex: 1, minWidth: 0, borderRight: '1px solid var(--bd)', overflowY: 'auto', padding: '0 0 8px' }}>
      {calendarGrid}
      {legendRow}
      {summaryCards}
      <div style={{ borderTop: '1px solid var(--bd)', margin: '4px 12px 0' }} />
      <div style={{ padding: '8px 4px 0' }}>
        {menuSection}
      </div>
      {uploadSection}
    </div>
    {/* 중앙 */}
    <div style={{ width: 280, flexShrink: 0, borderRight: '1px solid var(--bd)', overflowY: 'auto', padding: 16, display: 'flex', flexDirection: 'column', gap: 10 }}>
      ...
    </div>
    {/* 우측 */}
    ...
  </div>
</div>
```
Token rewrite for sketch markup: `var(--bg)` → `var(--surface-page)`, `var(--bd)` → `var(--border-default)`, padding 16 → `var(--modal-padding)` (=24 desktop), gap 10 → `var(--space-3)` (=12).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write 06-desktop-3panel-sketch.html (dark + light, 1280px × 2 frames, 3-panel skeleton + populated left panel + center/right placeholders)</name>
  <files>cha-bio-safety/docs/redesign-context/12-staff-service/sketch/06-desktop-3panel-sketch.html</files>
  <action>
Create a single static HTML5 file at the path above. The page renders **2 stacked frames** (dark theme on top, light theme below) so the user can review both modes side-by-side scrolling vertically.

STRUCTURE OVERVIEW
==================

```
<!doctype html>
<html lang="ko">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=1280" />
  <title>W6 — 12 staff-service desktop 3-panel sketch</title>
  <style>
    /* :root + [data-theme="light"] verbatim from tokens.css (dark 16~69, light 74~119). NO alias block. */
    /* Primitive spacing + radius blocks verbatim (lines 124~146 + 148~162 desktop @media + 167~172). */
    /* Page-level reset + Pretendard font stack + 1280px frame layout */
  </style>
</head>
<body>
  <!-- Outer page bg: neutral page color so frames "float" -->
  <div class="page-bg">
    <div class="frame-label">Dark theme — 1280px</div>
    <div class="desk-frame" data-theme="dark"> ... </div>
    <div class="frame-label">Light theme — 1280px</div>
    <div class="desk-frame" data-theme="light"> ... </div>
  </div>
</body>
</html>
```

PAGE CHROME / FRAME WRAPPER
===========================

- `body { margin:0; font-family: 'Pretendard Variable', Pretendard, system-ui, -apple-system, sans-serif; background: #1a1d24; color: #e6edf3; }` (dark outer page bg, NOT a token — this is sketch chrome only)
- `.page-bg { padding: 32px 24px 64px; display: flex; flex-direction: column; gap: 24px; align-items: center; min-height: 100vh; }`
- `.frame-label { color: #adb6c0; font-size: 13px; font-weight: 600; align-self: flex-start; max-width: 1280px; width: 100%; margin: 16px auto 0; }`
- `.desk-frame { width: 1280px; height: 760px; border-radius: 12px; overflow: hidden; box-shadow: 0 8px 24px rgba(0,0,0,0.3); }`
  - **Dark frame** outer border: `border: 1px solid #2a2f3a`
  - **Light frame** outer border: `border: 1px solid #d0d7de`
  - Inside the frame, the rendering goes through `[data-theme="dark"]` / `[data-theme="light"]` token scope.
- Each `.desk-frame` interior:
  ```
  <div class="page-root" style="height:100%; display:flex; flex-direction:column; overflow:hidden; background: var(--surface-page);">
    <!-- GlobalHeader placeholder strip 54px -->
    <!-- 3-panel body flex:1 -->
  </div>
  ```

GLOBAL HEADER PLACEHOLDER (54px) — top of each frame
=====================================================

`<header class="gh">`:
- `height: 54px; display: flex; align-items: center; justify-content: space-between; padding: 0 16px; background: var(--surface-page); border-bottom: 1px solid var(--border-default); flex-shrink: 0;`
- Left side: a hamburger placeholder — `<div>` 32×32 (`width:32px; height:32px; display:flex; align-items:center; justify-content:center; border-radius: var(--radius-sm); color: var(--text-secondary);`) with 3 horizontal bars rendered as 3 stacked `<div>`s (each 16×2, gap 4, background `currentColor`). DO NOT use lucide / emoji.
- Center: `<div style="font-size:15px; font-weight:700; color: var(--text-primary); line-height:1;">연차 및 식사</div>` (text-body size 15 from typography.css; if unsure, fall back to inline `font-size:15px`).
- Right side: a settings (톱니) placeholder — same 32×32 button shell, render as an inline SVG cog. Use a minimal inline SVG with `stroke="currentColor"` color `var(--text-secondary)` size 18×18 (8 teeth cog OR a simpler 6-spoke gear is acceptable — must be a real SVG, NOT emoji, NOT text).

3-PANEL BODY (flex:1)
=====================

```html
<div class="body3" style="flex:1; display:flex; overflow:hidden;">
  <section class="left"  style="flex:1; min-width:0; border-right:1px solid var(--border-default); overflow-y:auto; padding:0 0 8px;"> ... </section>
  <section class="center" style="width:280px; flex-shrink:0; border-right:1px solid var(--border-default); overflow-y:auto; padding: var(--modal-padding); display:flex; flex-direction:column; gap: var(--space-3);"> ... </section>
  <section class="right" style="flex:1; min-width:0; overflow-y:auto; padding: var(--modal-padding); display:flex; align-items:center; justify-content:center;"> ... </section>
</div>
```

The inline `width:280px` literal MUST appear in the markup of BOTH frames (so the grep gate `width: 280px|width:280px` matches ≥2). Likewise the `border-right:1px solid var(--border-default)` must appear in left + center of BOTH frames (4 total matches).

LEFT PANEL CONTENT (W2 + W3 + W4 reuse, desktop sizing)
========================================================

Order — match source line 1029~1037 exactly:

1. **Calendar (W2 desktop variant)** — wrapper `<div style="padding: 0 8px;">`
   - **Header row**: a 7-column grid `display:grid; grid-template-columns: repeat(7, 1fr); align-items:center; padding: 8px 0;`
     - col 1: prev-year `<` chevron button — 28×28, `background: var(--surface-sunken); color: var(--text-secondary); border-radius: var(--radius-sm); border:none; display:flex; align-items:center; justify-content:center; font-size:14px;`
     - cols 2~3: month label (e.g. "2026년 5월") — `grid-column: 2 / 7; text-align:center; font-size:18px; font-weight:800; color: var(--text-primary); line-height:1;`
     - col 7: next-year `>` chevron — same shell as col 1.
     - (Use 2026년 5월 as the displayed month — current date is 2026-05-18.)
   - **Weekday row**: `display:grid; grid-template-columns: repeat(7, 1fr); gap: 2px; padding: 2px 0;`
     - 7 cells: 일, 월, 화, 수, 목, 금, 토. Each: `font-size:13px; font-weight:700; text-align:center; line-height:1; padding: 4px 0;`
     - 일 → `color: var(--status-danger);` (raw red for Sunday)
     - 토 → `color: var(--status-info);` (raw blue for Saturday)
     - 월~금 → `color: var(--text-tertiary);`
   - **6×7 day grid**: `display:grid; grid-template-columns: repeat(7, 1fr); gap: 2px;`
     - 42 cells covering May 2026 (May starts Friday — fill prev/next month cells as dimmed). The actual day numbers: prev month 4월 26·27·28·29·30 in row 1 cols 1~5; then 1·2·3 (5월); week 2: 4~10; week 3: 11~17; week 4: 18·19·20·21·22·23·24; week 5: 25·26·27·28·29·30·31; row 6: 6월 1~6.
     - Each cell: `aspect-ratio: 1.2; padding: 2px; border-radius: var(--radius-sm); display:flex; flex-direction:column; position:relative; overflow:hidden;`
     - Cell shows: top-left day number (`font-size:12px; font-weight:700; line-height:1;`), bottom-right info text (`font-size:10px` is forbidden → use `11px` is forbidden → use `12px font-weight:700; line-height:1;` is OK; per negative gates **no 9·10·11 anywhere** — bottom info text is `12px`).
     - **Cell states** (lay out at least one of each across the 42 cells, freely chosen for a sketch):
       - **Today (2026-05-18)**: small blue chip "오늘" `background: var(--accent); color: var(--text-on-accent); font-size:12px; padding: 0 4px; border-radius: var(--radius-sm); line-height:1;` rendered as an inline badge below the day number AND give cell `outline: 2px solid var(--warning-bar)` — actually wait: 오늘 칩 = accent, 선택 셀 보더 = `#facc15` (lock decision). So 2026-05-18 = today chip; pick another cell (e.g., 2026-05-20) as "selected" — that cell gets `outline: 2px solid #facc15; outline-offset: -2px;` (raw #facc15 is the locked exception color for selection).
       - **Sunday cells**: day number `color: var(--status-danger);` (e.g. 5/3, 5/10, 5/17, 5/24, 5/31).
       - **Saturday cells**: day number `color: var(--status-info);` (e.g. 5/2, 5/9, 5/16, 5/23, 5/30).
       - **공휴일 (red name)**: 5/5 어린이날 — under the day number add `<div style="font-size:12px; color: var(--status-danger); line-height:1; font-weight:600;">어린이날</div>`. 5/25 부처님오신날 — same pattern.
       - **Duty cells** (4 종): pick 4 cells across the month and use the normalized hex set for the **cell background** AND a small bottom-right text indicating duty:
         - Day duty (주): cell `background: rgba(215, 128, 66, 0.18);` (≈ #d78042 @ 18%) with day number color staying default — wait: locked decision is duty-day = #f59e0b in tokens. For the **카테고리 정규화 hex set** (#42d778 / #8f42d7 / #d78042 / #d74242 / #d7428c / #4244d7), these are the LEAVE categories. The duty cell backgrounds should use **duty tokens** (`var(--duty-day)` orange, `var(--duty-night)` red, `var(--duty-off)` blue, `var(--duty-leave)` gray) NOT the categorical hex.
         - Use translucent versions for cell backgrounds so day number stays readable: `background: rgba(245, 158, 11, 0.20);` (duty-day @ ~20%), `rgba(239, 68, 68, 0.20);` (duty-night), `rgba(59, 130, 246, 0.20);` (duty-off), `rgba(107, 114, 128, 0.20);` (duty-leave).
       - **Leave category cells (5종 — normalized hex set used here)**: pick 5 cells and apply solid category background. Use:
         - 1 cell `background: #42d778;` (연차, 초록 정규화)
         - 1 cell `background: #8f42d7;` (공가, 보라 정규화)
         - 1 cell `background: #d78042;` (경조, 오렌지 정규화) — note also reused in 석식; that's fine, hex used ≥1
         - 1 cell `background: #d74242;` (병가, 빨강 정규화)
         - 1 cell `background: #d7428c;` (보건, 핑크 정규화)
         - 1 cell with `background: linear-gradient(135deg, #42d778 50%, rgba(245,158,11,0.20) 50%);` (오전반차 = 연차 hex + duty-day 셀 그라디언트)
         - On these category cells the day number gets `color: #ffffff; text-shadow: 0 0 2px rgba(0,0,0,0.5);` for contrast in dark; in the light frame use the same since hex is saturated enough.
         - Add small "성+코드" text bottom-right `font-size:12px; color: rgba(255,255,255,0.92); line-height:1; font-weight:700;` e.g. "윤연", "김공", "이경", "박병", "최보".
       - **점검일 (소검/승검) cell**: pick a non-category cell, add a small bottom-right text `font-size:12px; color: var(--status-warning); line-height:1; font-weight:700;` reading "소검" (or "승검" on another cell).
       - **Blocked (이미 점검일에 휴가 등록 시도 X)**: pick 1 cell and overlay a diagonal stripe — `background: repeating-linear-gradient(45deg, transparent, transparent 4px, rgba(245,158,11,0.18) 4px, rgba(245,158,11,0.18) 8px);`
       - **미N (미사용 식수)** cell: pick a cell and add bottom-right `<div style="font-size:12px; color: var(--status-warning); font-weight:700; line-height:1;">미2</div>`
       - **팀원 leave** cell: add small inline chip `font-size:12px; background: rgba(105, 168, 247, 0.18); color: var(--status-info); border-radius: var(--radius-sm); padding: 0 4px; line-height:1;` reading e.g. "팀 김".
       - **Prev/next month cells**: `opacity: 0.35;` on day number, no background.

2. **Legend row (W3)** — wrapper `<div style="padding: 6px 12px 0; display:flex; flex-wrap:wrap; gap:6px;">`
   - 11 chips total. Each chip: `display:inline-flex; align-items:center; gap:4px; font-size:12px; line-height:1; color: var(--text-secondary);`
   - Inside each chip: a 10×10 dot `<span style="width:10px; height:10px; border-radius:50%; background:{color}; flex-shrink:0;"></span>` + a label.
   - The 11 chips (in order):
     1. 주간 — dot `var(--duty-day)` — label "주(주간)"
     2. 당직 — dot `var(--duty-night)` — label "당(당직)"
     3. 비번 — dot `var(--duty-off)` — label "비(비번)"
     4. 휴무 — dot `var(--duty-leave)` — label "휴(휴무)"
     5. 연차 — dot `#42d778` — label "연차"
     6. 공가 — dot `#8f42d7` — label "공가"
     7. 경조 — dot `#d78042` — label "경조"
     8. 병가 — dot `#d74242` — label "병가"
     9. 보건 — dot `#d7428c` — label "보건"
     10. 기타 — dot `#4244d7` — label "기타특별"
     11. 반차 — dot `<span style="width:10px; height:10px; border-radius:50%; background: linear-gradient(135deg, #42d778 50%, var(--duty-day) 50%); flex-shrink:0;"></span>` — label "반차"

3. **Summary cards (4 stat cards, W3 §6.3)** — wrapper `<div style="display:flex; gap:8px; padding: 14px 12px 0;">`
   - Each card (4 total): `flex:1; min-width:0; background: var(--surface-raised); border-radius: var(--radius-md); padding: var(--card-padding); box-shadow: inset 3px 0 0 {barColor}; display:flex; flex-direction:column; gap:6px;`
   - Card 1: barColor `#42d778`, label "연차 잔여" `<div style="font-size:12px; font-weight:600; color: var(--text-tertiary); line-height:1;">연차 잔여</div>`, value `<div style="font-size:18px; font-weight:800; color: var(--text-primary); line-height:1; white-space:nowrap;">12<span style="font-size:12px; color: var(--text-tertiary); font-weight:600;"> 일</span></div>`
   - Card 2: barColor `#06b6d4`, label "제공 식수", value "48 끼"
   - Card 3: barColor `#d7428c`, label "미사용 식수", value "3 끼"
   - Card 4: barColor `#8f42d7`, label "주말 식대", value "22,000 원"

4. **Divider** — `<div style="border-top:1px solid var(--border-default); margin: 4px 12px 0;"></div>` (verbatim source line 1033 token-rewritten)

5. **Menu section (W4)** — wrapper `<div style="padding: 8px 4px 0;">` (verbatim source line 1034 padding) containing inside another wrapper `<div style="padding: 14px 12px 0;">` then a grid:
   - `<div style="display:grid; grid-template-columns: 1fr 1fr; gap: var(--card-gap);">`
   - Card A (중식 A) — `grid-column: 1 / 2; background: rgba(6, 182, 212, 0.08); border:1px solid rgba(6, 182, 212, 0.20); border-radius: var(--radius-md); padding: 10px 12px;`:
     - label `<div style="font-size:12px; font-weight:700; color: #06b6d4; line-height:1; margin-bottom:6px;">중식 A코너</div>`
     - body `<div style="font-size:14px; color: var(--text-secondary); line-height:1.625;">잡곡밥 · 미역국<br/>제육볶음 · 콩나물무침<br/>김치 · 요거트</div>`
   - Card B (중식 B) — `grid-column: 2 / 3; background: rgba(215, 66, 140, 0.08); border:1px solid rgba(215, 66, 140, 0.20);` (#d7428c) — label color `#d7428c`, label "중식 B코너", body "현미밥 · 된장국<br/>고등어구이 · 시금치무침<br/>김치 · 과일"
   - Card 석식 — `grid-column: 1 / 3; background: rgba(215, 128, 66, 0.08); border:1px solid rgba(215, 128, 66, 0.20);` (#d78042) — label color `#d78042`, label "석식", body "흰쌀밥 · 김치찌개<br/>닭갈비 · 무생채<br/>김치 · 요거트"

6. **PDF upload dropzone (W4 desktop variant)** — outer wrapper `<div style="padding: 14px 12px;">` containing a dashed dropzone label:
   ```html
   <div style="padding: 48px 0; border: 2px dashed var(--border-default); background: var(--surface-raised); border-radius: var(--radius-md); text-align:center;">
     <div style="font-size:12px; font-weight:600; color: var(--text-secondary); line-height:1;">식단표 PDF 드래그앤드롭 또는 클릭하여 업로드</div>
   </div>
   ```

CENTER PANEL CONTENT (280px, placeholder)
==========================================

```html
<section class="center" style="...">
  <div style="font-size:18px; font-weight:700; color: var(--text-primary); line-height:1;">휴가신청서</div>
  <div style="background: var(--surface-sunken); border: 1px dashed var(--border-default); border-radius: var(--radius-md); padding: 32px 16px; display:flex; flex-direction:column; gap:8px; align-items:center; justify-content:center; text-align:center;">
    <div style="font-size:13px; font-weight:600; color: var(--text-tertiary); line-height:1.5;">Wave 7 sketch 예정</div>
    <div style="font-size:12px; color: var(--text-tertiary); line-height:1.5;">(중앙 휴가 신청서 폼 전체)</div>
  </div>
</section>
```

RIGHT PANEL CONTENT (flex 1, placeholder)
==========================================

```html
<section class="right" style="...">
  <div style="background: var(--surface-raised); border: 1px dashed var(--border-default); border-radius: var(--radius-md); padding: 48px 32px; max-width: 595px; width:100%; display:flex; flex-direction:column; gap:12px; align-items:center; justify-content:center; text-align:center;">
    <div style="font-size:13px; font-weight:600; color: var(--text-tertiary); line-height:1.5;">Wave 8 sketch 예정</div>
    <div style="font-size:12px; color: var(--text-tertiary); line-height:1.5;">(우측 휴가 신청서 PDF 미리보기<br/>A4 595px max-w · lp[0..16] 오버레이)</div>
  </div>
</section>
```

LIGHT FRAME
===========

Render a second `.desk-frame` identical in structure but with `data-theme="light"` and `border: 1px solid #d0d7de`. Inside content is the SAME markup as the dark frame. The translucent rgba duty backgrounds + category solid hex backgrounds + dashed borders all read against light surfaces — that's expected.

IMPORTANT LOCKED RULES (OQ #5)
================================

The left panel MUST NOT contain a selCell detail row, a 선택된 날짜 label, or any leave-form input. Selection detail is owned by the CENTER panel (휴가신청서). The center panel in W6 is a placeholder; W7 will populate it.

WHAT THE SKETCH SHOULD NOT CONTAIN
====================================

- No `style="font-size:9px"`, `10px`, `11px` ANYWHERE — every text size is 12px or larger. (10×10 dot `width`/`height` is fine — those are pixel dimensions on a `<span>` block, not font-size.)
- No emoji (📅 🔥 ⚠ ✅ etc).
- No `status-fire`, `text-fire`, `bg-fire`, `fire-bar`, `fire-bg` — none of these tokens or class names. (The :root block does contain `--status-fire*` — that's fine; the grep gates check for status-fire **outside** the :root block. Use string match including `var(--status-fire` and reject it in markup.) **Actionable: do NOT write `var(--status-fire` or `var(--status-fire-bar` or `var(--status-fire-bg` anywhere in the markup body. The :root definitions themselves stay.**
- No alias tokens — no `var(--bg)`, `var(--bg2)`, `var(--bg3)`, `var(--bd)`, `var(--bd2)`, `var(--t1)`, `var(--t2)`, `var(--t3)`, `var(--acl)`, `var(--info)`, `var(--safe)`, `var(--warn)`, `var(--danger)`, `var(--fire)`, `var(--c-day)`, `var(--c-night)`, `var(--c-off)`, `var(--c-leave)`. Also do NOT include the alias `:root { --bg: ... }` block from tokens.css lines 177~197. Skip it entirely.
- No old categorical hex outside the :root token blocks. Old hex like `#22c55e` (was 연차), `#a855f7` (was 공가), `#f97316` (was 경조), `#ef4444` (was 병가), `#ec4899` (was 보건), `#6366f1` (was 기타), `#facc15` (selection border) — these ALL appear inside the verbatim :root token blocks (which is fine and expected). Outside those blocks, in the markup body:
  - **Allowed**: the 7 normalized hex set `#42d778`, `#8f42d7`, `#d78042`, `#d74242`, `#d7428c`, `#4244d7`, `#06b6d4`.
  - **Allowed exceptions**: `#facc15` for the selected cell border (locked W2 decision); frame chrome hex `#2a2f3a` (dark frame border), `#1a1d24` (sketch page bg), `#e6edf3` / `#adb6c0` (sketch outer text), `#d0d7de` (light frame border), `#ffffff` (light bg).
  - **Forbidden** outside :root: `#22c55e`, `#a855f7`, `#f97316`, `#ef4444`, `#ec4899`, `#6366f1`, `#fb923c`, `#fbbf24`, `#f87171`, `#38bdf8`, `#4ade80`, `#3b82f6`, `#1f6feb`, `#9a3412`, `#c2410c`, etc. — use tokens (`var(--status-danger)`, `var(--accent)`, `var(--duty-day)`) instead. The duty cell rgba translucent backgrounds use raw rgba (acceptable — they are translucent overlays, not categorical hex).

GREP VERIFY GATES (executor self-check before declaring done)
===============================================================

Run from `/Users/jykevin/Documents/cbc7119-design`:

```bash
F=cha-bio-safety/docs/redesign-context/12-staff-service/sketch/06-desktop-3panel-sketch.html

# Gate 1: file exists, valid HTML5
test -f "$F" && head -1 "$F" | grep -q '<!doctype html>' && echo "G1 PASS" || echo "G1 FAIL"

# Gate 2: NO 9·10·11 px font-size (10×10 pixel dot dimensions are fine; only font-size:NNpx counts)
test "$(grep -cE 'font-size:[[:space:]]*(9|10|11)px' "$F")" = "0" && echo "G2 PASS" || echo "G2 FAIL"

# Gate 3: NO emoji (heuristic check — these specific scripts should be 0). Adjust the character class if needed.
test "$(grep -cP '[\x{1F300}-\x{1FAFF}\x{2600}-\x{27BF}]' "$F")" = "0" && echo "G3 PASS" || echo "G3 FAIL"

# Gate 4: NO status-fire / fire-bar / fire-bg / text-fire / bg-fire in markup body. Strip the :root token block first.
# Easier: count `var(--status-fire` references in the file (should be 0).
test "$(grep -c 'var(--status-fire' "$F")" = "0" && echo "G4 PASS" || echo "G4 FAIL"

# Gate 5: NO alias tokens in markup
test "$(grep -cE 'var\(--(bg|bg2|bg3|bg4|bd|bd2|t1|t2|t3|acl|info|safe|warn|danger|fire|c-day|c-night|c-off|c-leave)\)' "$F")" = "0" && echo "G5 PASS" || echo "G5 FAIL"

# Gate 6: 7 normalized hex each appears ≥1
for H in '#42d778' '#8f42d7' '#d78042' '#d74242' '#d7428c' '#4244d7' '#06b6d4'; do
  C=$(grep -c "$H" "$F"); test "$C" -ge 1 && echo "G6:$H PASS ($C)" || echo "G6:$H FAIL"
done

# Gate 7: 1280px frame ≥2 (dark + light) — match width:1280 in CSS or HTML
test "$(grep -cE '1280px|width:[[:space:]]*1280' "$F")" -ge 2 && echo "G7 PASS" || echo "G7 FAIL"

# Gate 8: width 280px (center panel) matches ≥2 (dark + light frame)
test "$(grep -cE 'width:[[:space:]]*280px' "$F")" -ge 2 && echo "G8 PASS" || echo "G8 FAIL"

# Gate 9: border-right with --border-default appears 4× (2 frames × 2 panels each: left + center)
test "$(grep -cE 'border-right:[[:space:]]*1px[[:space:]]+solid[[:space:]]+var\(--border-default\)' "$F")" -ge 4 && echo "G9 PASS" || echo "G9 FAIL"

# Gate 10: Wave 7 + Wave 8 placeholder text each ≥1 (markup body), counting across both frames ≥2 each is even better.
test "$(grep -c 'Wave 7 sketch 예정' "$F")" -ge 1 && echo "G10a PASS" || echo "G10a FAIL"
test "$(grep -c 'Wave 8 sketch 예정' "$F")" -ge 1 && echo "G10b PASS" || echo "G10b FAIL"

# Gate 11: GlobalHeader '연차 및 식사' title appears ≥1
test "$(grep -c '연차 및 식사' "$F")" -ge 1 && echo "G11 PASS" || echo "G11 FAIL"

# Gate 12: token contract — :root and [data-theme="light"] both present
grep -q ':root' "$F" && grep -q '\[data-theme="light"\]' "$F" && echo "G12 PASS" || echo "G12 FAIL"

# Gate 13: OQ #5 negative — left panel must NOT contain '선택된 날짜' or 'selCell' detail row or '휴가기간' label
test "$(grep -cE '(선택된 날짜|휴가기간|docStartDate|docEndDate)' "$F")" = "0" && echo "G13 PASS" || echo "G13 FAIL"
```

All gates must report PASS. If any FAIL, fix the markup before declaring done.

REFERENCE FILES TO MIRROR (visual consistency)
================================================

- Token block structure: `cha-bio-safety/docs/redesign-context/12-staff-service/sketch/04-menu-cards-sketch.html` and `05-bottomsheet-sketch.html` (same verbatim :root pattern).
- Calendar desktop sizing: `02-calendar-grid-sketch.html` lines for desktop frame (header 18px, weekday 13, aspectRatio 1.2).
- Legend + stat cards desktop sizing: `03-legend-summary-sketch.html`.
- Menu + dropzone desktop sizing: `04-menu-cards-sketch.html`.

Build the file in **one Write call**.
  </action>
  <verify>
    <automated>cd /Users/jykevin/Documents/cbc7119-design &amp;&amp; F=cha-bio-safety/docs/redesign-context/12-staff-service/sketch/06-desktop-3panel-sketch.html; test -f "$F" &amp;&amp; head -1 "$F" | grep -q '&lt;!doctype html&gt;' &amp;&amp; test "$(grep -cE 'font-size:[[:space:]]*(9|10|11)px' "$F")" = "0" &amp;&amp; test "$(grep -cP '[\x{1F300}-\x{1FAFF}\x{2600}-\x{27BF}]' "$F" 2&gt;/dev/null || echo 0)" = "0" &amp;&amp; test "$(grep -c 'var(--status-fire' "$F")" = "0" &amp;&amp; test "$(grep -cE 'var\(--(bg|bg2|bg3|bg4|bd|bd2|t1|t2|t3|acl|info|safe|warn|danger|fire|c-day|c-night|c-off|c-leave)\)' "$F")" = "0" &amp;&amp; test "$(grep -cE 'width:[[:space:]]*280px' "$F")" -ge 2 &amp;&amp; test "$(grep -cE 'border-right:[[:space:]]*1px[[:space:]]+solid[[:space:]]+var\(--border-default\)' "$F")" -ge 4 &amp;&amp; test "$(grep -c 'Wave 7 sketch 예정' "$F")" -ge 1 &amp;&amp; test "$(grep -c 'Wave 8 sketch 예정' "$F")" -ge 1 &amp;&amp; test "$(grep -c '연차 및 식사' "$F")" -ge 1 &amp;&amp; grep -q ':root' "$F" &amp;&amp; grep -q '\[data-theme="light"\]' "$F" &amp;&amp; test "$(grep -cE '(선택된 날짜|휴가기간|docStartDate|docEndDate)' "$F")" = "0" &amp;&amp; for H in '#42d778' '#8f42d7' '#d78042' '#d74242' '#d7428c' '#4244d7' '#06b6d4'; do test "$(grep -c "$H" "$F")" -ge 1 || { echo "missing $H"; exit 1; }; done &amp;&amp; echo "ALL GATES PASS"</automated>
  </verify>
  <done>
    - File `cha-bio-safety/docs/redesign-context/12-staff-service/sketch/06-desktop-3panel-sketch.html` exists.
    - Opens in a browser and renders 2 stacked 1280px frames (dark on top, light below).
    - Each frame: GlobalHeader placeholder (54px) on top, then 3-panel body (left flex / center 280 / right flex) with vertical borders between panels.
    - Left panel inside both frames displays: 5월 2026 calendar (desktop sizing 18/13/1.2 aspect, with today/selected/Sun/Sat/공휴일/duty/leave-category/점검일/blocked/미N/팀원 states all shown), 11-chip legend row, 4 stat cards (연차 잔여 #42d778 / 제공식수 #06b6d4 / 미사용식수 #d7428c / 주말식대 #8f42d7), divider, 식단 grid (중식 A #06b6d4 / 중식 B #d7428c / 석식 #d78042 — grid-column 1/3), PDF dropzone (48px dashed).
    - Center panel: "휴가신청서" title + Wave 7 placeholder callout (dashed border, surface-sunken bg).
    - Right panel: Wave 8 placeholder callout centered (dashed border, surface-raised bg, max-w 595px) referencing A4 595px and lp[0..16].
    - NO selCell detail / 선택된 날짜 row / 휴가기간 inputs anywhere in the left panel (OQ #5 locked).
    - All grep gates pass (one-liner verify above prints "ALL GATES PASS").
    - File does not modify any other sketch (01~05) or any source under `src/`.
  </done>
</task>

</tasks>

<verification>
- File exists at the exact path specified in `files_modified`.
- All 13 grep gates pass (combined into one verify command above).
- Visual check (browser): the dark frame renders the 3-panel skeleton with a fully populated left panel matching W2 (달력 desktop variant) + W3 (범례 + 4 stat cards) + W4 (식단 + dropzone) reuse, plus placeholder center and right panels with explicit "Wave 7 sketch 예정" / "Wave 8 sketch 예정" labels. The light frame renders the same content under the light token scope.
- The sketch contains the 7 normalized categorical hex (each ≥1) and zero alias tokens, zero status-fire references in markup, zero 9/10/11px font-sizes, zero emoji.
</verification>

<success_criteria>
- The single sketch file `06-desktop-3panel-sketch.html` is committed-ready (sketch only, no src/ touched, no wrangler).
- Locked decisions A~E are all visually represented:
  - **A**: 3-panel skeleton with `width: 280px flex-shrink: 0` center and `border-right: 1px solid var(--border-default)` on left + center (4 matches across 2 frames).
  - **B**: Left panel ordering exactly matches source line 1029~1037 (calendar → legend → summary → divider → menu wrapper `padding: 8px 4px 0` → dropzone).
  - **C**: Center has "휴가신청서" title + Wave 7 placeholder callout; right has Wave 8 placeholder callout.
  - **D**: OQ #5 — left panel has zero selCell detail (grep gate G13 passes).
  - **E**: 2 frames (dark + light), both at 1280px wide.
- Token contract: :root dark + [data-theme="light"] verbatim from tokens.css, primitive spacing block, desktop @media branch (lines 148~162), radius block. NO alias block.
- Negative gates: 9/10/11 = 0, emoji = 0, status-fire in markup = 0, alias tokens = 0, forbidden old categorical hex outside :root = 0.
- Positive gates: 7 normalized hex each ≥1, 1280px ≥2, width 280px ≥2, border-right with --border-default ≥4, Wave 7 / Wave 8 placeholders ≥1 each, GlobalHeader title "연차 및 식사" ≥1.
</success_criteria>

<output>
After completion, do NOT auto-create a SUMMARY for this quick task. The user will review the rendered sketch in browser and either request fixes or move to Wave 7. If a SUMMARY is requested later, write it to `.planning/quick/260518-lr5-12-staff-service-sketch-wave-6-desktop-3/260518-lr5-01-SUMMARY.md` noting:
- OQ #5 locked: left panel has no selCell detail; selection detail belongs to center panel (W7).
- Center + right are placeholders; W7 and W8 will populate.
</output>
