---
phase: 260515-ia6-redesign-07-elevator-tsx-wave-2-evselect
plan: 01
type: execute
wave: 1
depends_on:
  - 260515-3mc-redesign-07-elevator-tsx-wave-1-list       # 직전 Wave — ElevatorIcon icons.tsx 추가 + lucide import 블록(Package/UtensilsCrossed/MoveDiagonal/ChevronRight/AlertTriangle/Wrench) + tokens 화이트리스트 + grep gate 패턴 권위
  - 260515-4zh-redesign-07-elevator-2-sketch-evselector   # 2A sketch — EvSelector / EsBtn / EsNodeMap 시안 단일 진실 소스 (`evselector-sketch.html` 1557줄, fix 적용 후 최종 — EV-NN→N호기 / EsBtn accent fill 통일 / 방향=Chevron 아이콘만)
  - 260514-i4r-redesign-02-inspection-tsx                 # InspectionPage Wave 1 — must_haves carrier 패턴 권위 (페이지 전체가 아닌 영역 한정 변환)
  - 260515-2r5-redesign-02-inspection-tsx-wave-6-powerp   # PowerPanel/Parking Wave 6 — task별 1 컴포넌트/영역 분리 패턴 + verify grep gate 패턴
files_modified:
  - cha-bio-safety/src/pages/ElevatorPage.tsx
autonomous: true
requirements:
  - REDESIGN-07-ELEVATOR-TSX-WAVE2-EVSELECT
tags:
  - redesign
  - elevator
  - tsx-conversion
  - design-tokens-v0.1.1
  - tailwind-only
  - evselector
  - esbtn
  - esnodemap
  - wave2

must_haves:
  truths:
    - "EsBtn / EsNodeMap / EvSelector 의 props 시그니처(id/label/dir/isDown/selected/onSelect — EsBtn / nodes/elevatorId/onSelect — EsNodeMap / elevators/evKind/setEvKind/elevatorId/setElevatorId/groups/esNodes — EvSelector) 가 한 글자도 변경되지 않는다. 함수명 / 매개변수 순서 / 타입 / 기본값 / 반환 구조 모두 보존"
    - "EvSelector 의 비즈니스 로직(evList = elevators.filter(e => e.type !== 'escalator') / evKind === 'elevator' 분기 / evKind === 'escalator' 분기 / groups.map / groupEvs = group.ids.map(...).filter(Boolean) / !groupEvs.length 조기 리턴 / setEvKind 호출 시 setElevatorId('') 동시 실행 / setElevatorId(ev.id) 콜백 / ev.number 라벨 / ev.status === 'fault' 분기) 가 100% 보존된다 (라인 shift / 마크업 wrap 변경만 허용)"
    - "EsNodeMap 의 비즈니스 로직(nodes.map(idx) / node.floor 조건부 렌더 / node.left && node.right 행 조건부 렌더 / node.isBottom 사용 / EsBtn isDown=true(좌측 = 하행) / isDown=false(우측 = 상행) / elevatorId === node.left.id|right.id 비교) 가 100% 보존된다"
    - "EsBtn 의 비즈니스 로직(onClick={() => onSelect(id)} / selected boolean 분기 / id/label/dir 텍스트 표시) 이 100% 보존된다. 단, **시각 룰만** 2A 결정에 따라 변경: 선택 시 색 통일(direction 무관 accent fill), 방향은 lucide Chevron 모양으로만 구분"
    - "EvSelector 사용처 4곳 (line 2028 FaultNewModal / line 2129 FaultNewFullscreen / line 2257 InspectModal / line 3074 RepairNewModal) 의 `<EvSelector ... />` 호출이 한 줄도 변경되지 않는다 — props 전달 그대로 (elevators / evKind / setEvKind / elevatorId / setElevatorId / groups / esNodes)"
    - "변환 영역(라인 1893~1995, EsBtn + EsNodeMap + EvSelector 3 함수 본체) 안에 인라인 style={{...}} 의 금지 키(color / background / backgroundColor / padding / paddingLeft|Right|Top|Bottom / margin / marginLeft|Right|Top|Bottom / fontSize / fontWeight / borderRadius / display / flex / flexDirection / justifyContent / alignItems / grid / gridTemplateColumns / gap / width / height / textAlign / lineHeight / cursor / border / borderTop|... — 정적값) 사용이 0개이다. 화이트리스트(position / top/left/right/bottom — 동적 / transform — 동적 / transition — 동적) 만 예외. **단, EvSelector 의 호기 그리드 gridTemplateColumns 는 동적값(`repeat(${groupEvs.length}, 1fr)`) 이므로 화이트리스트 — Tailwind arbitrary value(`grid-cols-[repeat(N,1fr)]`)는 React 동적 표현식 한계로 인라인 style 보존 허용** (5xl Dashboard 화이트리스트 패턴 동일)"
    - "변환 영역 안에 9 / 10 / 11px 폰트 사이즈 사용이 0개이다 (Tailwind text-caption(12) / text-label(13) / text-body-sm(14) / text-body(16) 또는 12 이상 arbitrary value 만). 현재 인라인 fontSize 10 (그룹 라벨) / 11 (종류 선택 라벨 + 호기 선택 라벨) 는 모두 12+ 로 격상 (노안 친화 + 2A sketch 권위)"
    - "변환 영역 안에 옛 토큰(var(--bg) / var(--bg2) / var(--bg3) / var(--bd) / var(--bd2) / var(--acl) / var(--t1) / var(--t2) / var(--t3) / var(--safe) / var(--warn) / var(--danger) / var(--fire) / var(--info)) 사용이 0개이다 — 모두 Tailwind utility (bg-surface-raised / bg-surface-sunken / text-text-primary / text-text-secondary / text-text-tertiary / text-text-on-accent / bg-accent / outline-accent / bg-fire-bg / text-fire / border-border-default) 로 교체"
    - "변환 영역 안에 이모지(🛗 / ↕️ / ▼ / ▲ / ⚠️) 사용이 0개이다. 모두 lucide-react + 커스텀 icons.tsx 매핑: 종류 토글 = ElevatorIcon(엘리베이터) / MoveDiagonal(에스컬) — Wave 1 import 그대로 재사용 / EsBtn 방향 = ChevronDown(하행, isDown=true) / ChevronUp(상행, isDown=false) — lucide-react import 블록에 ChevronUp/ChevronDown 추가 / 호기 fault 표시 = Siren 또는 AlertTriangle(lucide, 기존 Wave 1 import 재사용) — 2A sketch 의 호기 카드 fault 아이콘과 동일"
    - "**.ev-btn-selected 패턴 적용** — 호기 버튼 선택 시 `bg-accent text-text-on-accent outline outline-2 outline-accent` (또는 동등 Tailwind utility 조합), 비선택 시 `bg-surface-sunken text-text-primary`, fault 시 `bg-fire-bg text-fire`. **.es-btn-selected 패턴 적용** — EsBtn 선택 시 `bg-accent text-text-on-accent outline outline-2 outline-accent`, 비선택 시 `bg-surface-sunken text-text-primary`. 2A sketch (4zh-SUMMARY fix_commits 권위) 의 accent 단일 선택색 결정 1:1 반영"
    - "ElevatorIcon (Wave 1 에서 icons.tsx 에 추가 export 된 커스텀 SVG) 가 EvSelector 의 종류 토글 엘리베이터 옵션 아이콘으로 재사용된다 — icons.tsx 본체는 한 줄도 변경되지 않는다. 신규 import 추가도 불필요(Wave 1 에서 이미 `import { ElevatorIcon } from '../components/ui/icons'` 존재)"
    - "lucide-react import 블록(현재: Package, UtensilsCrossed, MoveDiagonal, ChevronRight, AlertTriangle, Wrench)에 **ChevronUp, ChevronDown 만 추가** 된다 (MoveDiagonal / AlertTriangle 은 Wave 1 에서 이미 import 됨 — 재사용). Siren 사용 시에는 Siren 도 추가 — 단, Wave 2 sketch 와 일관성 위해 fault 표시는 AlertTriangle 재사용 권장 (별도 import 최소화)"
    - "EsNodeMap 의 중앙 1px 라인 (라인 1915 `<div style={{ position:'absolute', left:'50%', top:20, bottom:20, width:1, background:'var(--bd2)', transform:'translateX(-50%)' }} />`) 는 화이트리스트 키(position / left / top / bottom / width=1 — 정적이지만 1px 라인은 Tailwind 표현 한계로 예외)는 Tailwind utility (`absolute left-1/2 top-5 bottom-5 w-px bg-border-default -translate-x-1/2`) 로 완전 교체. left-1/2 = 50% / w-px = 1px / -translate-x-1/2 = transform:translateX(-50%)"
    - "EsNodeMap 컨테이너 (라인 1914 `<div style={{ background:'var(--bg3)', borderRadius:12, padding:'14px 10px', position:'relative' }}>`) 는 `bg-surface-sunken rounded-xl px-[10px] py-[14px] relative` (또는 `bg-surface-sunken rounded-[12px] py-[14px] px-[10px] relative`) Tailwind utility 로 교체. **bg-surface-sunken** 토큰 사용 (현재 bg3 = sunken 매핑, 2A sketch 권위)"
    - "EsNodeMap 의 층 레이블 (라인 1920 `<div style={{ textAlign:'center', fontSize:12, fontWeight:700, color:'var(--t2)', marginBottom:8, position:'relative', zIndex:1 }}>`) 는 `text-center text-caption font-bold text-text-secondary mb-2 relative z-10` (또는 동등 utility) 로 교체. **zIndex:1 → z-10 (또는 z-[1])** 보존 — 중앙 라인 위에 표시되어야 함"
    - "EsNodeMap 의 버튼 행 컨테이너 (라인 1926 `<div style={{ display:'flex', gap:8, marginBottom:10 }}>`) 는 `flex gap-2 mb-2.5` 로 교체. mb-10px 는 `mb-[10px]` arbitrary value 허용 (또는 `mb-2.5` = 10px)"
    - "EvSelector 의 종류 선택 라벨/호기 선택 라벨 (라인 1948, 1962, 1989 `fontSize:11, fontWeight:700, color:'var(--t2)', marginBottom:7|8`) 모두 `text-caption font-bold text-text-secondary mb-2` (또는 `mb-[7px]`) 로 교체. **fontSize 11 → text-caption(12) 격상** (노안 친화)"
    - "EvSelector 의 종류 토글 버튼 (라인 1949~1957) 는 종류 토글 옵션 배열에서 이모지 라벨(`🛗 엘리베이터` / `↕️ 에스컬레이터`) 제거 → 아이콘 컴포넌트(ElevatorIcon / MoveDiagonal) + 텍스트(`엘리베이터` / `에스컬레이터`) 의 children 형태로 재구성. 활성 시 `bg-accent text-text-on-accent`, 비활성 시 `bg-surface-sunken text-text-secondary`. flex-1 + gap-2 + padding(14~16px) + 터치타겟 44px 이상"
    - "EvSelector 의 그룹 라벨 (라인 1968 `fontSize:10, fontWeight:700, color:'var(--t3)', marginBottom:6`) 는 `text-caption font-bold text-text-tertiary mb-1.5` (또는 `mb-[6px]`) 로 교체. **fontSize 10 → text-caption(12) 격상** (노안 친화 + 2A sketch 권위 + §6.3 회색 통일)"
    - "EvSelector 의 호기 버튼 (라인 1971~1978) 는 §6.4 단색 surface — 그라디언트 0건 — fontSize 14 (text-body-sm) 또는 16 (text-body), fontWeight 700 (font-bold), padding y-3 (12px) 이상, borderRadius rounded-[10px] 또는 rounded-lg. `{ev.number}호기` 라벨 그대로 보존 (이미 N호기 형태 — 변경 없음). 우측에 ' ⚠️' suffix 텍스트 제거 → fault 시 `<AlertTriangle size={14} />` lucide 아이콘으로 교체 또는 fault 시 아이콘+라벨 조합 (2A sketch 의 호기 카드 패턴과 일관)"
    - "tailwind.config.js / tokens.css / 다른 페이지 / 다른 컴포넌트 / hooks / utils / API / 라우팅 / D1 스키마 / icons.tsx / DesignPage / App.tsx / BottomNav / DesktopSidebar / GlobalHeader / wave 1 변환 영역(list 탭/자체 헤더/데스크톱 좌측 컬럼) 수정 0건. **Wave 2 변환 영역 = 라인 1893~1995, 정확히 3 함수 본체(EsBtn / EsNodeMap / EvSelector) + lucide-react import 블록(ChevronUp/ChevronDown 추가)** 만 변경"
    - "FaultNewModal / FaultNewFullscreen / FaultResolveModal / InspectModal / RepairNewModal / EvDetailModal 본문 + 다른 탭 본문(fault/repair/inspect/annual/safety) + 데스크톱 우측 컬럼(desktopRightTab 영역) + KoelsaHistorySection 호출 + RepairListSection 호출 + ElevatorInfoCard 컴포넌트 + ModalWrap / Field / EmptyState / CertSummary / CertBlock / FindingsPanel / FindingCountBadge / CertViewerModal 본체는 한 줄도 변경 없이 보존된다 — Wave 3~5 후속"
    - "**InspectModal cleanup 은 본 Wave out of scope** — InspectModal 함수 본체 / 'inspect_new' Modal type / submitInspect mutation / EvSelector 의 InspectModal 호출(line 2257) 모두 보존만. 별도 cleanup wave 에서 dead code 제거 (사용자 메모리 `project_elevator_page_tabs.md` 확인 완료)"
    - "TypeScript 컴파일 0 에러, `npm run build` 통과. cbc7119-preview.pages.dev 자동 배포 대상이지만 본 Wave 의 verify 는 빌드 통과까지 — 시각 검수는 사용자가 push 후 별도 진행"
  artifacts:
    - path: "cha-bio-safety/src/pages/ElevatorPage.tsx"
      provides: "Wave 2 변환 완료 — 라인 1893~1995 (EsBtn + EsNodeMap + EvSelector 3 함수 본체) 가 v0.1.1 토큰 + Tailwind only. lucide-react import 블록에 ChevronUp / ChevronDown 추가. 그 외 영역(Wave 1 변환 영역 / 5 모달 / 다른 탭 본문 / 데스크톱 우측 컬럼 / 다른 헬퍼 / 데이터 상수 / 비즈니스 로직 / EvSelector 사용처 4곳) 은 한 줄도 변경 없이 보존"
      min_lines: 3200
      contains: "function EsBtn, function EsNodeMap, function EvSelector, ChevronUp, ChevronDown, ElevatorIcon, MoveDiagonal, AlertTriangle, bg-accent, text-text-on-accent, outline-accent, bg-surface-sunken, bg-fire-bg, text-fire"
  key_links:
    - from: "cha-bio-safety/src/pages/ElevatorPage.tsx (EsBtn / EsNodeMap / EvSelector)"
      to: "tailwind.config.js color/spacing/fontSize tokens"
      via: "Tailwind utility 클래스 (bg-surface-sunken / bg-accent / text-text-on-accent / outline-accent / bg-fire-bg / text-fire / text-text-primary / text-text-secondary / text-text-tertiary / border-border-default / text-caption / text-body-sm / text-body / font-bold)"
      pattern: "bg-(surface-(sunken|raised)|accent|fire-bg)|text-(text-(primary|secondary|tertiary|on-accent)|fire|caption|body-sm|body)|outline-accent|border-border-default"
    - from: "cha-bio-safety/src/pages/ElevatorPage.tsx (EvSelector 종류 토글)"
      to: "cha-bio-safety/src/components/ui/icons.tsx (ElevatorIcon — Wave 1 추가)"
      via: "named import 재사용 (`import { ElevatorIcon } from '../components/ui/icons'` 이미 라인 17 존재). EvSelector 종류 토글에서 `<ElevatorIcon size={18} />` 사용"
      pattern: "<ElevatorIcon\\s+(size|className)"
    - from: "cha-bio-safety/src/pages/ElevatorPage.tsx (EvSelector + EsBtn lucide 아이콘)"
      to: "lucide-react (ChevronUp / ChevronDown 신규 + MoveDiagonal / AlertTriangle 재사용)"
      via: "이모지(🛗↕️▼▲⚠️) 제거 → lucide+커스텀 컴포넌트로 교체. import 블록에 ChevronUp, ChevronDown 추가. 종류 토글 에스컬=MoveDiagonal(재사용) / EsBtn 방향=ChevronDown(하행)/ChevronUp(상행) / 호기 fault=AlertTriangle(재사용)"
      pattern: "<(ChevronUp|ChevronDown|MoveDiagonal|AlertTriangle|ElevatorIcon)\\s+(size|className)"
    - from: "cha-bio-safety/src/pages/ElevatorPage.tsx (EvSelector 사용처 4곳)"
      to: "FaultNewModal(line 2028) / FaultNewFullscreen(line 2129) / InspectModal(line 2257) / RepairNewModal(line 3074)"
      via: "`<EvSelector elevators={...} evKind={...} setEvKind={...} elevatorId={...} setElevatorId={...} groups={...} esNodes={...} />` 호출 — 한 줄도 변경 금지"
      pattern: "<EvSelector\\s+(elevators|evKind|setEvKind|elevatorId|setElevatorId|groups|esNodes)"
    - from: "cha-bio-safety/src/pages/ElevatorPage.tsx (EvSelector 호기 그리드)"
      to: "동적 gridTemplateColumns (`repeat(${groupEvs.length}, 1fr)`)"
      via: "그룹별 호기 수(3/3/3/2/1) 가 런타임 결정되므로 인라인 style 화이트리스트 보존 (5xl Dashboard 동적값 패턴 동일). 화이트리스트 키: gridTemplateColumns(동적값)"
      pattern: "gridTemplateColumns:\\s*`repeat\\(\\$\\{groupEvs\\.length\\}"
    - from: "cha-bio-safety/src/pages/ElevatorPage.tsx (Wave 2 변환 영역)"
      to: "design-system.md v0.1.1 §6.1 / §6.3 / §6.4 / §6.5 / §6.7 / §7.1"
      via: "§6.1 색 = accent 선택(단일, 방향 무관) / §6.3 회색 통일 = 그룹 라벨+비활성 토글 text-tertiary~secondary / §6.4 단색 surface = 그라디언트 0건 / §6.5 hover = border-strong+translateY / §6.7 그림자 0건 / §7.1 이모지 0건"
      pattern: "bg-accent\\s+text-text-on-accent\\s+outline-(2\\s+)?outline-accent|bg-surface-sunken\\s+text-text-(primary|secondary)|bg-fire-bg\\s+text-fire"
---

<objective>
ElevatorPage TSX 변환 **Wave 2** — 2A sketch (`evselector-sketch.html` 1557줄, 4zh quick) 를 단일 진실 소스로 삼아 `cha-bio-safety/src/pages/ElevatorPage.tsx` 의 **EvSelector / EsBtn / EsNodeMap 3 헬퍼 컴포넌트(라인 1893~1995, 약 102줄)** 를 v0.1.1 디자인 토큰 + Tailwind only 로 교체한다.

**Purpose:**
- 4 모달(FaultNewModal / FaultNewFullscreen / InspectModal / RepairNewModal)이 공통으로 사용하는 호기 선택 헬퍼 3종을 먼저 변환 — 후속 Wave 3~5 의 모달 본체 변환 시 EvSelector 가 이미 토큰화되어 있어서 spawned 색/spacing/typography 흔들림 방지
- **2A 결정 (4zh-SUMMARY fix_commits)** 1:1 반영:
  - EsBtn 색 통일: `.es-btn-down`(danger) / `.es-btn-up`(safe) 폐기 → `.es-btn-selected`(accent 단일). 방향은 ChevronDown / ChevronUp 모양으로만 구분 (색 차별 X)
  - 호기 ID(EV-NN/ES-NN) 본문 노출 0건 — 코드는 이미 `{ev.number}호기` 형태로 N호기 표시. 변환 후에도 그대로
  - §6.1 색 의미 분리: fire(주황) = 호기 고장 미수리 / accent(파) = 선택 상태 (호기·EsBtn 공통, 단일 색)
- 이모지 5종(🛗 / ↕️ / ▼ / ▲ / ⚠️) → lucide+커스텀 매핑 통일. Wave 1 의 이모지 0건 룰을 헬퍼 컴포넌트까지 확장
- 옛 토큰(var(--bg2/3) / var(--bd2) / var(--t1/2/3) / var(--acl) / var(--danger) / var(--safe)) → v0.1.1 토큰 (surface-sunken / border-default / text-text-* / accent / fire-bg / text-fire) 교체
- 9-11px 폰트 (현재 그룹 라벨 10 / 호기 선택 라벨 11 / EsBtn dir 라벨 10) → 12px+ 격상 (노안 친화)

**Output:**
- 비즈니스 로직 100% 보존하면서 라인 1893~1995 의 마크업/스타일이 2A sketch 와 1:1 매칭된 새 `ElevatorPage.tsx`
- lucide-react import 블록에 **ChevronUp, ChevronDown 만 추가** (MoveDiagonal / AlertTriangle / ElevatorIcon 은 Wave 1 에서 이미 import)
- 4 모달의 EvSelector 사용처(line 2028 / 2129 / 2257 / 3074) 호출 한 줄도 변경 없음

**Out of scope (후속 Wave / 별도 quick):**
- FaultNewModal / FaultNewFullscreen / FaultResolveModal 본체 → Wave 3 (2B sketch 기반)
- RepairNewModal 본체 → Wave 4 (2C sketch 기반)
- EvDetailModal → Wave 5 (2D sketch 기반)
- InspectModal cleanup (dead code 제거) → Wave 6 또는 별도 quick
- 다른 탭 본문 (fault / repair / inspect / annual / safety) / 데스크톱 우측 컬럼 / KoelsaHistorySection / RepairListSection / ElevatorInfoCard / 다른 헬퍼 — 별도 wave
- 다른 페이지 / utils / hooks / API / icons.tsx / tailwind.config.js / tokens.css — 수정 0건
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@cha-bio-safety/src/pages/ElevatorPage.tsx
@cha-bio-safety/docs/redesign-context/07-elevator/sketch/evselector-sketch.html
@.planning/quick/260515-4zh-redesign-07-elevator-2-sketch-evselector/260515-4zh-SUMMARY.md
@cha-bio-safety/src/components/ui/icons.tsx
@cha-bio-safety/tailwind.config.js
@.planning/quick/260515-3mc-redesign-07-elevator-tsx-wave-1-list/260515-3mc-PLAN.md
@.planning/quick/260515-3mc-redesign-07-elevator-tsx-wave-1-list/260515-3mc-SUMMARY.md

<interfaces>
<!-- Wave 2 변환 영역 = 라인 1893~1995, 3 함수 본체. props 시그니처 한 글자도 변경 금지. -->

EsBtn (line 1894~1909):
```typescript
function EsBtn({ id, label, dir, isDown, selected, onSelect }: {
  id:string; label:string; dir:string; isDown:boolean; selected:boolean; onSelect:(id:string)=>void
}) { ... }
```

EsNodeMap (line 1912~1935):
```typescript
function EsNodeMap({ nodes, elevatorId, onSelect }: { nodes:EsNode[]; elevatorId:string; onSelect:(id:string)=>void }) { ... }
```

EvSelector (line 1938~1995):
```typescript
function EvSelector({ elevators, evKind, setEvKind, elevatorId, setElevatorId, groups, esNodes }: {
  elevators: Elevator[]; evKind: EvKind; setEvKind:(v:EvKind)=>void
  elevatorId: string; setElevatorId:(v:string)=>void
  groups: { title:string; ids:string[] }[]; esNodes: EsNode[]
}) { ... }
```

EsNode type (line 131~138, 변경 금지):
```typescript
interface EsNodeBtn { id:string; label:string; dir:string }
interface EsNode {
  floor?: string
  isBottom?: boolean
  left?: EsNodeBtn
  right?: EsNodeBtn
}
```

EvKind type (별도 정의 위치):
```typescript
type EvKind = '' | 'elevator' | 'escalator'
```

Elevator type (별도, 변경 금지):
```typescript
interface Elevator { id: string; number: number; type: string; status: 'normal'|'fault'|'maintenance'|'out_of_service'; ... }
```

Wave 1 에서 추가된 lucide-react import 블록 (line 16):
```typescript
import { Package, UtensilsCrossed, MoveDiagonal, ChevronRight, AlertTriangle, Wrench } from 'lucide-react'
```
→ Wave 2 에서 ChevronUp, ChevronDown 만 추가

Wave 1 에서 추가된 ElevatorIcon import (line 17, 재사용):
```typescript
import { ElevatorIcon } from '../components/ui/icons'
```

EvSelector 사용처 4곳 (한 줄도 변경 금지):
- line 2028: FaultNewModal
- line 2129: FaultNewFullscreen
- line 2257: InspectModal (dead code — 보존만, 별도 cleanup wave)
- line 3074: RepairNewModal
</interfaces>

<tokens>
<!-- v0.1.1 디자인 토큰 → Tailwind utility 매핑 (tailwind.config.js 권위) -->

색 토큰:
- accent (선택 상태) = bg-accent / text-text-on-accent / outline-accent
- surface-sunken (비선택 배경) = bg-surface-sunken
- surface-raised (대안) = bg-surface-raised
- fire 상태 (호기 고장) = bg-fire-bg / text-fire
- text 계층: text-text-primary (호기 라벨) / text-text-secondary (종류 라벨) / text-text-tertiary (그룹 라벨)
- border: border-border-default (1px 라인)

폰트 토큰:
- text-caption (12px) — 그룹 라벨 / 종류 라벨 / 층 레이블 / EsBtn dir 라벨
- text-label (13px) — 종류 토글 텍스트 / EsBtn label
- text-body-sm (14px) — 호기 라벨 (선택사항)
- text-body (16px) — 호기 라벨 (노안 친화 권장)

옛 토큰 → v0.1.1 매핑:
- var(--bg2) → bg-surface-raised
- var(--bg3) → bg-surface-sunken
- var(--bd2) → border-border-default (또는 bg-border-default for 1px line)
- var(--t1) → text-text-primary
- var(--t2) → text-text-secondary
- var(--t3) → text-text-tertiary
- var(--acl) → bg-accent
- '#fff' (selected text) → text-text-on-accent
- var(--danger) → (EsBtn 폐기 — accent 사용)
- var(--safe) → (EsBtn 폐기 — accent 사용)
- rgba(239,68,68,.15) (호기 fault bg) → bg-fire-bg
- var(--danger) (호기 fault text) → text-fire
</tokens>
</context>

<tasks>

<task type="auto">
  <name>Task 1: EsBtn 함수 변환 (라인 1893~1909, 1 컴포넌트 ~18줄)</name>
  <files>cha-bio-safety/src/pages/ElevatorPage.tsx</files>
  <action>
**스코프**: 라인 1893~1909, function EsBtn 본체만 교체. EsNodeMap / EvSelector / 다른 코드 한 줄도 건드리지 말 것.

**변경 단계**:

1. **lucide-react import 블록 보강** (라인 16):
   - 현재: `import { Package, UtensilsCrossed, MoveDiagonal, ChevronRight, AlertTriangle, Wrench } from 'lucide-react'`
   - 변경: `ChevronUp, ChevronDown` 추가 → `import { Package, UtensilsCrossed, MoveDiagonal, ChevronRight, ChevronUp, ChevronDown, AlertTriangle, Wrench } from 'lucide-react'`
   - 알파벳 순서 유지 권장 (Chevron* 묶음)

2. **EsBtn 함수 본체 재작성** (라인 1894~1909):
   - **props 시그니처 한 글자도 변경 금지**: `{ id, label, dir, isDown, selected, onSelect }` 그대로
   - **삭제할 라인**: 라인 1897 `const color = isDown ? 'var(--danger)' : 'var(--safe)'` (2A 결정으로 방향 색 차별 폐기)
   - **삭제할 키**: 인라인 style 의 모든 정적 키 (flex / padding / borderRadius / border / cursor / background / outline / display / flexDirection / alignItems / gap / fontSize / fontWeight / color) → Tailwind utility
   - **삭제할 이모지**: 라인 1906 `{isDown?'▼':'▲'}` → `{isDown ? <ChevronDown size={12} className="inline-block" /> : <ChevronUp size={12} className="inline-block" />}` (또는 svg icon span 형태)

   **변환 후 마크업 (예시 구조 — 정확한 className 조합은 executor 판단)**:
   ```tsx
   function EsBtn({ id, label, dir, isDown, selected, onSelect }: {
     id:string; label:string; dir:string; isDown:boolean; selected:boolean; onSelect:(id:string)=>void
   }) {
     return (
       <button
         onClick={() => onSelect(id)}
         className={`flex-1 px-1.5 py-2.5 rounded-[9px] border-0 cursor-pointer flex flex-col items-center gap-[3px] ${
           selected
             ? 'bg-accent text-text-on-accent outline outline-2 outline-accent'
             : 'bg-surface-sunken text-text-primary'
         }`}
       >
         <span className="text-label font-bold">{label}</span>
         <span className="text-caption font-semibold inline-flex items-center gap-1">
           {isDown ? <ChevronDown size={12} /> : <ChevronUp size={12} />} {dir}
         </span>
       </button>
     )
   }
   ```

   **핵심 결정**:
   - 선택 시: `bg-accent + text-text-on-accent + outline outline-2 outline-accent` (2A `.es-btn-selected` 패턴)
   - 비선택 시: `bg-surface-sunken + text-text-primary` (방향 무관, 색 차별 X)
   - 방향 표시: ChevronDown(하행) / ChevronUp(상행) lucide 아이콘 — 색은 부모(selected/text-text-primary) 상속
   - dir 텍스트(`하행` / `상행`) 라벨은 그대로 유지 — 시각 + 접근성 양쪽 보장
   - fontSize 10 → text-caption(12) 격상 (노안 친화)
   - fontSize 13 → text-label(13) 유지

3. **TypeScript 검증**: EsBtn 의 props 타입 그대로 보존. JSX 변경만.

**보존해야 할 것 (변경 0건)**:
- 함수명 `EsBtn` / props 시그니처 / 매개변수 순서 / 타입
- `onClick={() => onSelect(id)}` 콜백
- `selected` 분기 로직
- `label` / `dir` 텍스트 표시
- `isDown` boolean 분기 (방향 = 아이콘 모양 결정)
- EsBtn 호출처(EsNodeMap line 1927, 1928) — 다음 task 에서 처리하지만 props 전달 형태는 동일
  </action>
  <verify>
<automated>cd cha-bio-safety && grep -nE "function EsBtn" src/pages/ElevatorPage.tsx | head -2 && grep -c "ChevronUp\|ChevronDown" src/pages/ElevatorPage.tsx && grep -c "▼\|▲" src/pages/ElevatorPage.tsx && npx tsc -p tsconfig.json --noEmit 2>&1 | grep -E "EsBtn|error TS" | head -20</automated>

수동 체크:
- EsBtn 함수 본체에 `var(--danger) / var(--safe) / var(--bg2)` 잔재 0건
- 이모지 ▼ / ▲ 0건 (`grep -c '▼\|▲' src/pages/ElevatorPage.tsx` → EsBtn 라인 범위에서 0)
- `<ChevronDown` / `<ChevronUp` lucide 아이콘 사용 확인
- props 시그니처 변경 0건 (`grep -n "function EsBtn" src/pages/ElevatorPage.tsx` 한 번만 매치)
- `npm run build` (또는 tsc --noEmit) 통과
  </verify>
  <done>
- function EsBtn (라인 ~1893~) 본체가 Tailwind only + v0.1.1 토큰 + lucide 아이콘으로 재작성됨
- props 시그니처 `{ id, label, dir, isDown, selected, onSelect }` 그대로
- 선택 시 accent fill / 비선택 시 surface-sunken / 방향은 ChevronDown/ChevronUp 으로만 구분
- 이모지 ▼ / ▲ EsBtn 안에서 0건
- lucide-react import 블록에 ChevronUp, ChevronDown 추가
- TypeScript 0 에러
  </done>
</task>

<task type="auto">
  <name>Task 2: EsNodeMap + EvSelector 변환 (라인 1911~1995, 2 컴포넌트 묶음 ~85줄)</name>
  <files>cha-bio-safety/src/pages/ElevatorPage.tsx</files>
  <action>
**스코프**: 라인 1911~1995, function EsNodeMap + function EvSelector 본체만 교체. EsBtn(이미 task 1 변환됨) / 다른 코드 한 줄도 건드리지 말 것.

**변경 단계 — EsNodeMap (라인 1911~1935)**:

1. **컨테이너** (라인 1914):
   - 현재: `<div style={{ background:'var(--bg3)', borderRadius:12, padding:'14px 10px', position:'relative' }}>`
   - 변경: `<div className="bg-surface-sunken rounded-xl px-[10px] py-[14px] relative">`
   - rounded-xl = 12px / px-[10px] py-[14px] 또는 `p-[14px_10px]` arbitrary value

2. **중앙 1px 라인** (라인 1915):
   - 현재: `<div style={{ position:'absolute', left:'50%', top:20, bottom:20, width:1, background:'var(--bd2)', transform:'translateX(-50%)' }} />`
   - 변경: `<div className="absolute left-1/2 top-5 bottom-5 w-px bg-border-default -translate-x-1/2" />`
   - left-1/2 = 50% / top-5 = 20px / bottom-5 = 20px / w-px = 1px / -translate-x-1/2 = translateX(-50%)
   - 인라인 style 0건

3. **층 레이블** (라인 1920):
   - 현재: `<div style={{ textAlign:'center', fontSize:12, fontWeight:700, color:'var(--t2)', marginBottom:8, position:'relative', zIndex:1 }}>`
   - 변경: `<div className="text-center text-caption font-bold text-text-secondary mb-2 relative z-10">`
   - z-10 = z-index:10 (z-[1] arbitrary 도 가능, 중앙 라인 위에 표시되어야 하므로 1+ 충분)

4. **버튼 행 컨테이너** (라인 1926):
   - 현재: `<div style={{ display:'flex', gap:8, marginBottom:10 }}>`
   - 변경: `<div className="flex gap-2 mb-2.5">`
   - gap-2 = 8px / mb-2.5 = 10px (또는 mb-[10px] arbitrary)

5. **EsBtn 호출 (라인 1927, 1928)**: props 전달 한 글자도 변경 금지. JSX 그대로 유지.

**변경 단계 — EvSelector (라인 1938~1995)**:

1. **외부 Fragment + 종류 선택 블록** (라인 1947~1958):

   1-A. **종류 선택 라벨** (라인 1948):
   - 현재: `<div style={{ fontSize:11, fontWeight:700, color:'var(--t2)', marginBottom:7 }}>종류 선택</div>`
   - 변경: `<div className="text-caption font-bold text-text-secondary mb-[7px]">종류 선택</div>`
   - fontSize 11 → text-caption(12) 격상

   1-B. **종류 선택 컨테이너** (라인 1949):
   - 현재: `<div style={{ display:'flex', gap:8 }}>`
   - 변경: `<div className="flex gap-2">`

   1-C. **종류 토글 옵션 배열 + 버튼 마크업** (라인 1950~1956):
   - **삭제할 이모지**: `'🛗 엘리베이터'` / `'↕️ 에스컬레이터'` → 옵션 배열 데이터에서 이모지 제거
   - 옵션 배열 재구성: `[{ val:'elevator', label:'엘리베이터', Icon: ElevatorIcon }, { val:'escalator', label:'에스컬레이터', Icon: MoveDiagonal }]`
   - 버튼 마크업 재작성:
     ```tsx
     {[
       { val:'elevator',  label:'엘리베이터',   Icon: ElevatorIcon },
       { val:'escalator', label:'에스컬레이터', Icon: MoveDiagonal },
     ].map(opt => {
       const Icon = opt.Icon
       const active = evKind === opt.val
       return (
         <button
           key={opt.val}
           onClick={() => { setEvKind(opt.val as EvKind); setElevatorId('') }}
           className={`flex-1 py-3 rounded-[10px] border-0 cursor-pointer text-label font-bold inline-flex items-center justify-center gap-2 ${
             active ? 'bg-accent text-text-on-accent' : 'bg-surface-sunken text-text-secondary'
           }`}
         >
           <Icon size={18} />
           {opt.label}
         </button>
       )
     })}
     ```
   - padding y-3 = 12px (현재 padding:'12px 0' 와 동일)
   - text-label(13) 유지 (현재 fontSize:13 와 동일)
   - **활성**: bg-accent + text-text-on-accent (`.ev-btn-selected` 와 동일 패턴, 2A 권위)
   - **비활성**: bg-surface-sunken + text-text-secondary
   - 비즈니스 로직 보존: `onClick={() => { setEvKind(opt.val as EvKind); setElevatorId('') }}` 그대로

2. **엘리베이터 분기 — `evKind === 'elevator'`** (라인 1960~1985):

   2-A. **호기 선택 라벨** (라인 1962):
   - 현재: `<div style={{ fontSize:11, fontWeight:700, color:'var(--t2)', marginBottom:8 }}>호기 선택</div>`
   - 변경: `<div className="text-caption font-bold text-text-secondary mb-2">호기 선택</div>`
   - fontSize 11 → text-caption(12) 격상

   2-B. **groups.map** (라인 1963~1983): 비즈니스 로직 한 글자도 변경 금지. 매핑/필터/조기 리턴 그대로.

   2-C. **그룹 컨테이너** (라인 1967):
   - 현재: `<div key={group.title} style={{ marginBottom:12 }}>`
   - 변경: `<div key={group.title} className="mb-3">`

   2-D. **그룹 라벨** (라인 1968):
   - 현재: `<div style={{ fontSize:10, fontWeight:700, color:'var(--t3)', marginBottom:6 }}>{group.title}</div>`
   - 변경: `<div className="text-caption font-bold text-text-tertiary mb-1.5">{group.title}</div>`
   - fontSize 10 → text-caption(12) 격상 (노안 친화)

   2-E. **호기 그리드 컨테이너** (라인 1969):
   - 현재: `<div style={{ display:'grid', gridTemplateColumns:`repeat(${groupEvs.length}, 1fr)`, gap:8 }}>`
   - 변경: `<div className="grid gap-2" style={{ gridTemplateColumns: `repeat(${groupEvs.length}, 1fr)` }}>`
   - **gridTemplateColumns 는 동적값(`repeat(${groupEvs.length}, 1fr)`)이므로 인라인 style 화이트리스트 보존** (Tailwind arbitrary value 는 React 동적 표현식 한계로 불가). 다른 정적 키(display:grid / gap)는 Tailwind 로 이동.

   2-F. **호기 버튼** (라인 1971~1978):
   - 현재 (이모지 ⚠️ + 색 인라인):
     ```tsx
     <button key={ev.id} onClick={() => setElevatorId(ev.id)} style={{
       padding:'12px 0', borderRadius:10, border:'none', cursor:'pointer', fontSize:14, fontWeight:700,
       background: elevatorId === ev.id ? 'var(--acl)' : ev.status === 'fault' ? 'rgba(239,68,68,.15)' : 'var(--bg3)',
       color:      elevatorId === ev.id ? '#fff'       : ev.status === 'fault' ? 'var(--danger)'        : 'var(--t1)',
       outline:    elevatorId === ev.id ? '2px solid var(--acl)' : 'none',
     }}>
       {ev.number}호기{ev.status === 'fault' ? ' ⚠️' : ''}
     </button>
     ```
   - 변경 (3상태 className 분기 + AlertTriangle 아이콘):
     ```tsx
     {groupEvs.map(ev => {
       const isSel = elevatorId === ev.id
       const isFault = ev.status === 'fault'
       const stateClass = isSel
         ? 'bg-accent text-text-on-accent outline outline-2 outline-accent'
         : isFault
           ? 'bg-fire-bg text-fire'
           : 'bg-surface-sunken text-text-primary'
       return (
         <button
           key={ev.id}
           onClick={() => setElevatorId(ev.id)}
           className={`py-3 rounded-[10px] border-0 cursor-pointer text-body-sm font-bold inline-flex items-center justify-center gap-1 ${stateClass}`}
         >
           {ev.number}호기
           {isFault && !isSel && <AlertTriangle size={14} />}
         </button>
       )
     })}
     ```
   - 핵심 결정:
     - **3 상태** (.ev-btn-selected / .ev-btn-fault / .ev-btn-default, 2A sketch 권위):
       - 선택: bg-accent + text-text-on-accent + outline-2 outline-accent
       - 고장 (선택 안 됨): bg-fire-bg + text-fire + AlertTriangle 아이콘
       - 기본: bg-surface-sunken + text-text-primary
     - **이모지 ⚠️ 제거** → `<AlertTriangle size={14} />` lucide 아이콘
     - **선택+고장 동시인 경우** = 선택 표시 우선 (accent), 아이콘 표시 안 함 (사용자 인지 = "이미 골랐어요"). 2A sketch 의 결정 일관 (또는 아이콘 항상 표시도 가능 — executor 판단)
     - fontSize 14 → text-body-sm 유지 (현재와 동일, 노안 친화 OK)
     - `{ev.number}호기` 라벨 그대로

3. **에스컬레이터 분기 — `evKind === 'escalator'`** (라인 1987~1992):

   3-A. **호기 선택 라벨** (라인 1989):
   - 현재: `<div style={{ fontSize:11, fontWeight:700, color:'var(--t2)', marginBottom:8 }}>호기 선택</div>`
   - 변경: `<div className="text-caption font-bold text-text-secondary mb-2">호기 선택</div>`

   3-B. **EsNodeMap 호출 (라인 1990)**: 한 글자도 변경 금지. props 그대로.

**보존해야 할 것 (변경 0건)**:
- 함수명 / props 시그니처 / 매개변수 순서 / 타입
- `evList = elevators.filter(e => e.type !== 'escalator')` 비즈니스 로직
- `evKind === 'elevator'` / `evKind === 'escalator'` 분기
- `groups.map / groupEvs / !groupEvs.length 조기 리턴`
- `setEvKind(opt.val as EvKind); setElevatorId('')` 콜백
- `setElevatorId(ev.id)` 콜백
- `{ev.number}호기` 라벨 (이미 N호기 형태, 변경 금지)
- `ev.status === 'fault'` 분기
- EsNodeMap 호출 props (nodes / elevatorId / onSelect)
- EvSelector 사용처 4곳 (line 2028 / 2129 / 2257 / 3074) — 한 줄도 변경 금지

**예외 — 인라인 style 화이트리스트 유지**:
- EvSelector 의 그리드 `style={{ gridTemplateColumns: \`repeat(\${groupEvs.length}, 1fr)\` }}` (동적값 — 5xl Dashboard 화이트리스트 패턴 동일)
  </action>
  <verify>
<automated>cd cha-bio-safety && grep -nE "function (EsNodeMap|EvSelector)" src/pages/ElevatorPage.tsx | head -4 && grep -nc "🛗\|↕️\|⚠️" src/pages/ElevatorPage.tsx && grep -c "<EvSelector" src/pages/ElevatorPage.tsx && grep -c "var(--bg3)\|var(--t1)\|var(--t2)\|var(--t3)\|var(--acl)\|var(--bd2)" src/pages/ElevatorPage.tsx && npx tsc -p tsconfig.json --noEmit 2>&1 | grep -E "EsNodeMap|EvSelector|error TS" | head -20</automated>

수동 체크:
- EsNodeMap / EvSelector 본체에 옛 토큰(var(--bg3) / var(--t1) / var(--t2) / var(--t3) / var(--acl) / var(--bd2)) 0건 — 단, 페이지 전체에는 다른 영역(다른 탭/모달) 잔재 있을 수 있음 (Wave 2 out of scope)
- 이모지 🛗 / ↕️ / ⚠️ Wave 2 변환 영역에서 0건
- `<EvSelector` 매치 5건 유지 (정의 1 + 사용처 4)
- `<AlertTriangle` 아이콘 사용 확인
- `<ElevatorIcon size={` + `<MoveDiagonal size={` 종류 토글에서 사용 확인
- `npm run build` 통과
  </verify>
  <done>
- function EsNodeMap (라인 ~1911~) + function EvSelector (라인 ~1938~) 본체가 Tailwind only + v0.1.1 토큰 + lucide+커스텀 아이콘으로 재작성됨
- props 시그니처 / 비즈니스 로직 100% 보존
- EsNodeMap 중앙 1px 라인 = `absolute left-1/2 top-5 bottom-5 w-px bg-border-default -translate-x-1/2` Tailwind utility
- EvSelector 종류 토글 = ElevatorIcon / MoveDiagonal + 텍스트 (이모지 0건)
- EvSelector 호기 버튼 3상태 = selected(accent) / fault(fire-bg+text-fire+AlertTriangle) / default(surface-sunken)
- EvSelector 호기 그리드 gridTemplateColumns 만 동적값 인라인 style 보존 (화이트리스트)
- 9-11px 폰트 Wave 2 영역에서 0건 (모두 12+)
- 이모지 🛗 / ↕️ / ⚠️ Wave 2 영역에서 0건
- TypeScript 0 에러
  </done>
</task>

<task type="auto">
  <name>Task 3: 회귀 검증 + grep gate + npm run build PASS + 보존 영역 sentinel</name>
  <files>cha-bio-safety/src/pages/ElevatorPage.tsx</files>
  <action>
**스코프**: 코드 변경 없음. Wave 2 변환 영역(라인 1893~1995) 한정 검증 + 보존 영역(나머지 코드) sentinel 확인.

**검증 순서**:

1. **변환 영역 라인 범위 식별** — Wave 2 변환 후 라인 shift 가능. EsBtn / EsNodeMap / EvSelector 3 함수 본체 정확 라인 추출:
   ```bash
   grep -n "^function \(EsBtn\|EsNodeMap\|EvSelector\)" cha-bio-safety/src/pages/ElevatorPage.tsx
   ```
   → 예: EsBtn ~1893 / EsNodeMap ~1910 / EvSelector ~1935 / FaultNewModal(다음 함수) ~1995

2. **변환 영역 한정 grep gate** (sed 로 라인 범위 추출 후 grep):

   2-A. **이모지 0건** (변환 영역):
   ```bash
   sed -n '1893,1995p' cha-bio-safety/src/pages/ElevatorPage.tsx | grep -nE "🛗|↕️|▼|▲|⚠️"
   ```
   → 0건 PASS / 1+건 FAIL

   2-B. **옛 토큰 0건** (변환 영역):
   ```bash
   sed -n '1893,1995p' cha-bio-safety/src/pages/ElevatorPage.tsx | grep -nE "var\(--bg2\)|var\(--bg3\)|var\(--bd2\)|var\(--bd\)|var\(--t1\)|var\(--t2\)|var\(--t3\)|var\(--acl\)|var\(--danger\)|var\(--safe\)|var\(--warn\)|var\(--fire\)|var\(--info\)"
   ```
   → 0건 PASS

   2-C. **9-11px 폰트 0건** (변환 영역):
   ```bash
   sed -n '1893,1995p' cha-bio-safety/src/pages/ElevatorPage.tsx | grep -nE "fontSize:\s*(9|10|11)\b|fontSize=['\"]?(9|10|11)\b|text-\[(9|10|11)px\]"
   ```
   → 0건 PASS

   2-D. **인라인 style 금지 키 0건** (변환 영역, 정적값 한정):
   ```bash
   sed -n '1893,1995p' cha-bio-safety/src/pages/ElevatorPage.tsx | grep -nE "style=\{\{[^}]*\b(color|background|backgroundColor|padding|paddingLeft|paddingRight|paddingTop|paddingBottom|margin|marginLeft|marginRight|marginTop|marginBottom|fontSize|fontWeight|borderRadius|borderTopLeftRadius|borderTopRightRadius|borderBottomLeftRadius|borderBottomRightRadius|display|flex|flexDirection|justifyContent|alignItems|gap|width|height|textAlign|lineHeight|cursor|border|borderTop|borderBottom|borderLeft|borderRight|opacity|outline)\b"
   ```
   → **예외 1건만 허용**: EvSelector 호기 그리드 `gridTemplateColumns: \`repeat(\${groupEvs.length}, 1fr)\`` 동적값 화이트리스트 (5xl Dashboard 패턴 동일).
   → 그 외 0건 PASS

   2-E. **새 토큰 사용 확인** (변환 영역):
   ```bash
   sed -n '1893,1995p' cha-bio-safety/src/pages/ElevatorPage.tsx | grep -cE "bg-accent|text-text-on-accent|outline-accent|bg-surface-sunken|bg-fire-bg|text-fire|text-text-(primary|secondary|tertiary)|text-caption|text-label|text-body-sm|font-bold"
   ```
   → 10+ 매치 (대략 — EsBtn 4~6 / EsNodeMap 4~6 / EvSelector 10+)

3. **보존 영역 sentinel** — 변환 영역 밖은 한 줄도 변경되지 말아야 함:

   3-A. **EvSelector 사용처 4곳 호출 변경 0건**:
   ```bash
   grep -nB1 -A1 "<EvSelector" cha-bio-safety/src/pages/ElevatorPage.tsx
   ```
   → 5건 매치 (정의 1 + 사용처 4: line ~2028 / 2129 / 2257 / 3074, 라인 shift 가능). 각 호출의 props 전달이 `elevators={...} evKind={...} setEvKind={...} elevatorId={...} setElevatorId={...} groups={...} esNodes={...}` 형태 그대로

   3-B. **Wave 1 변환 영역 / 다른 함수 / 데이터 상수 보존**:
   - `EV_GROUPS_FAULT` / `EV_GROUPS_ANNUAL` / `ES_NODES_FAULT` / `ES_NODES_ANNUAL` 상수 본체 변경 0건:
     ```bash
     grep -n "EV_GROUPS_FAULT\|EV_GROUPS_ANNUAL\|ES_NODES_FAULT\|ES_NODES_ANNUAL" cha-bio-safety/src/pages/ElevatorPage.tsx | head -10
     ```
   - `TYPE_ICON_COMPONENT` / `STATUS_STYLE` / `OVERALL_STYLE` / `INSPECT_TYPE_LABEL` / `RESULT_STYLE` / `EV_FLOORS` / `CHECK_ITEMS_EV` / `CHECK_ITEMS_ES` 보존:
     ```bash
     grep -n "TYPE_ICON_COMPONENT\|STATUS_STYLE\|EV_FLOORS\|CHECK_ITEMS_EV\|CHECK_ITEMS_ES" cha-bio-safety/src/pages/ElevatorPage.tsx | head -10
     ```
   - 5 모달 함수(FaultNewModal / FaultNewFullscreen / FaultResolveModal / InspectModal / RepairNewModal / EvDetailModal) 정의 보존:
     ```bash
     grep -nE "^function (FaultNewModal|FaultNewFullscreen|FaultResolveModal|InspectModal|RepairNewModal|EvDetailModal)\b" cha-bio-safety/src/pages/ElevatorPage.tsx
     ```
     → 6건 매치

4. **icons.tsx 한 줄도 변경 안 됨**:
   ```bash
   git status cha-bio-safety/src/components/ui/icons.tsx
   ```
   → "nothing to commit" 또는 untracked 아님 (Wave 1 변경 이후 그대로)

5. **tailwind.config.js / tokens.css / 다른 페이지 변경 0건**:
   ```bash
   git status cha-bio-safety/tailwind.config.js cha-bio-safety/src/styles/tokens.css cha-bio-safety/src/pages/ 2>&1 | grep -E "modified|new"
   ```
   → 오직 `cha-bio-safety/src/pages/ElevatorPage.tsx` 만 modified. 다른 파일 0건

6. **TypeScript 컴파일 + 빌드 통과**:
   ```bash
   cd cha-bio-safety && npx tsc -p tsconfig.json --noEmit 2>&1 | head -30
   cd cha-bio-safety && npm run build 2>&1 | tail -40
   ```
   → 0 에러 / "built in ..." 메시지

7. **변환 영역 라인 수 확인** — 함수 본체가 너무 짧아지지 않았는지:
   ```bash
   awk '/^function EsBtn/,/^function EsNodeMap/' cha-bio-safety/src/pages/ElevatorPage.tsx | wc -l
   awk '/^function EsNodeMap/,/^function EvSelector/' cha-bio-safety/src/pages/ElevatorPage.tsx | wc -l
   awk '/^function EvSelector/,/^function FaultNewModal/' cha-bio-safety/src/pages/ElevatorPage.tsx | wc -l
   ```
   → EsBtn 15~25줄 / EsNodeMap 20~30줄 / EvSelector 55~80줄 (마크업 expansion 으로 약간 늘어남)

**verify_before_commit gate (배포 전 필수)**:
- 2-A ~ 2-E grep 모두 PASS (또는 예외 1건만)
- 3-A: 5건 매치
- 3-B: 데이터 상수 + 5 모달 함수 보존
- 4: icons.tsx 변경 0건
- 5: 다른 파일 변경 0건
- 6: TypeScript 0 에러 + npm run build PASS
- 7: 라인 수 합리적 범위
  </action>
  <verify>
<automated>cd cha-bio-safety && bash -c 'EL_LINE_START=$(grep -n "^function EsBtn" src/pages/ElevatorPage.tsx | head -1 | cut -d: -f1); EL_LINE_END=$(grep -n "^function FaultNewModal" src/pages/ElevatorPage.tsx | head -1 | cut -d: -f1); echo "Wave 2 range: $EL_LINE_START~$EL_LINE_END"; echo "--- Emoji check (Wave 2 region) ---"; sed -n "${EL_LINE_START},${EL_LINE_END}p" src/pages/ElevatorPage.tsx | grep -nE "🛗|↕️|▼|▲|⚠️" | head -5 || echo "PASS: emoji 0"; echo "--- Legacy tokens (Wave 2 region) ---"; sed -n "${EL_LINE_START},${EL_LINE_END}p" src/pages/ElevatorPage.tsx | grep -nE "var\(--(bg2|bg3|bd2|t1|t2|t3|acl|danger|safe|warn|fire|info)\)" | head -5 || echo "PASS: legacy tokens 0"; echo "--- 9-11px fonts (Wave 2 region) ---"; sed -n "${EL_LINE_START},${EL_LINE_END}p" src/pages/ElevatorPage.tsx | grep -nE "fontSize:\s*(9|10|11)\b|text-\[(9|10|11)px\]" | head -5 || echo "PASS: 9-11px 0"; echo "--- EvSelector callsites ---"; grep -c "<EvSelector" src/pages/ElevatorPage.tsx; echo "--- 5 modal sentinels ---"; grep -cE "^function (FaultNewModal|FaultNewFullscreen|FaultResolveModal|InspectModal|RepairNewModal|EvDetailModal)\b" src/pages/ElevatorPage.tsx; echo "--- Data constants ---"; grep -cE "EV_GROUPS_(FAULT|ANNUAL)|ES_NODES_(FAULT|ANNUAL)" src/pages/ElevatorPage.tsx; echo "--- icons.tsx untouched ---"; git diff --stat src/components/ui/icons.tsx 2>&1 | tail -3' && cd cha-bio-safety && npx tsc -p tsconfig.json --noEmit 2>&1 | grep -E "error TS" | head -20 && cd cha-bio-safety && npm run build 2>&1 | tail -25</automated>

**예상 결과**:
- Wave 2 range 라인 범위 표시 (예: 1893~1995)
- Emoji check: PASS: emoji 0 (또는 매치 0)
- Legacy tokens: PASS: legacy tokens 0
- 9-11px fonts: PASS: 9-11px 0
- EvSelector callsites: 5 (정의 1 + 사용처 4)
- 5 modal sentinels: 6
- Data constants: 4
- icons.tsx untouched: stat 0 또는 출력 없음
- TypeScript: 0 에러
- npm run build: "built in ..." 메시지
  </verify>
  <done>
- Wave 2 변환 영역(EsBtn / EsNodeMap / EvSelector 3 함수 본체) 한정 grep gate 모두 PASS:
  - 이모지 🛗 / ↕️ / ▼ / ▲ / ⚠️ 0건
  - 옛 토큰 (var(--bg2/3) / var(--bd2) / var(--t1/2/3) / var(--acl) / var(--danger) / var(--safe) 등) 0건
  - 9-11px 폰트 0건
  - 인라인 style 금지 키 0건 (gridTemplateColumns 동적값 화이트리스트 1건만 예외)
- 보존 영역 sentinel PASS:
  - EvSelector 사용처 4곳 호출 변경 0건 (`<EvSelector` 5건 매치 — 정의 1 + 사용처 4)
  - 5 모달 함수 정의(6건) 보존
  - 데이터 상수 4건 보존
  - icons.tsx / tailwind.config.js / tokens.css / 다른 페이지 변경 0건
- TypeScript 컴파일 0 에러
- `npm run build` PASS (cbc7119-preview.pages.dev 자동 배포 대상 — verify_before_commit 단계는 빌드 통과까지)
- **시각 검수 + push + 사용자 확인은 별도 단계** (메모리 룰: 디자인 작업은 컨펌 대기, redesign/* 브랜치 직접 main 머지 + 배포 금지)
  </done>
</task>

</tasks>

<verification>
**Wave 2 phase-level 검증 (모든 task 완료 후)**:

1. **변환 영역 grep gate** (task 3 verify 와 동일):
   - 이모지 0건 (Wave 2 영역)
   - 옛 토큰 0건 (Wave 2 영역)
   - 9-11px 폰트 0건 (Wave 2 영역)
   - 인라인 style 금지 키 0건 (gridTemplateColumns 동적값 예외 1건)

2. **보존 영역 sentinel**:
   - EvSelector 사용처 4곳 (line ~2028 / 2129 / 2257 / 3074, 라인 shift 후) 호출 변경 0건
   - 5 모달 함수(FaultNewModal / FaultNewFullscreen / FaultResolveModal / InspectModal / RepairNewModal / EvDetailModal) 본체 변경 0건
   - 데이터 상수(EV_GROUPS_* / ES_NODES_* / EV_FLOORS / CHECK_ITEMS_*) 변경 0건
   - 다른 헬퍼(TYPE_ICON_COMPONENT / STATUS_STYLE / OVERALL_STYLE / 등) 변경 0건
   - 다른 탭 본문 / 데스크톱 우측 컬럼 / KoelsaHistorySection / RepairListSection / ElevatorInfoCard 변경 0건
   - icons.tsx / tailwind.config.js / tokens.css / 다른 페이지 변경 0건

3. **TypeScript 컴파일 + 빌드**:
   - `npx tsc -p tsconfig.json --noEmit` → 0 에러
   - `npm run build` → "built in ..." 메시지

4. **시각 검수 (사용자)**: cbc7119-preview.pages.dev 자동 배포 후 사용자가 별도 시각 확인 (디자인 작업 컨펌 룰).
</verification>

<success_criteria>
- EsBtn / EsNodeMap / EvSelector 3 함수 본체가 v0.1.1 토큰 + Tailwind only + lucide+커스텀 아이콘으로 재작성됨
- props 시그니처 / 비즈니스 로직 / EvSelector 사용처 4곳 호출 / 데이터 상수 / 5 모달 함수 본체 / 다른 페이지 모두 한 줄도 변경되지 않음
- lucide-react import 블록에 ChevronUp, ChevronDown 만 추가 (Wave 1 import 재사용)
- TypeScript 0 에러, npm run build PASS
- 변환 영역 한정 grep gate: 이모지 0 / 옛 토큰 0 / 9-11px 0 / 인라인 style 금지 키 0 (gridTemplateColumns 동적값 1건 예외)
- 2A sketch (`evselector-sketch.html` + 4zh-SUMMARY fix_commits) 결정 1:1 반영: EsBtn accent fill 통일, 방향=Chevron 모양, 호기 ID 노출 0건
</success_criteria>

<output>
After completion, create `.planning/quick/260515-ia6-redesign-07-elevator-tsx-wave-2-evselect/260515-ia6-SUMMARY.md` with:
- Wave 2 변환 영역 라인 범위 (변환 전/후)
- 추가된 lucide import (ChevronUp / ChevronDown)
- 적용된 v0.1.1 토큰 매핑 표 (옛 → 새)
- grep gate 결과 (이모지 / 옛 토큰 / 9-11px / 인라인 style 금지 키 각 0건 PASS)
- 보존 영역 sentinel 결과 (사용처 4곳 / 5 모달 / 데이터 상수 / icons.tsx 변경 0건)
- npm run build 결과 + commit hash
- 다음 Wave 권장 (Wave 3 = 2B sketch 기반 FaultNewModal 변환)
</output>
