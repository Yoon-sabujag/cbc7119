---
phase: quick
plan: 01
type: execute
wave: 3
quick_id: 260517-e9r
depends_on: [260517-e9c, 260517-e9d]
files_modified:
  - cha-bio-safety/docs/redesign-context/09-extinguishers/sketch/register-modal-sketch.html
autonomous: true
---

<objective>
RegisterModal (l.823-927) + ModalBackdrop (l.1107-1120) + FieldLabel (l.1124-1130) + 공통 modal styles (l.1134-1168) 의 v0.1.1 토큰 sketch.

영역:
- ModalBackdrop: position fixed full / surface-overlay
- modalWrapperStyle: surface-raised + border-strong + radius-lg + maxHeight 80vh
- 모달 타이틀 "소화기 등록"
- infoBannerStyle: status-info 페어 (안내 문구)
- 마커 동행 안내 배너 (`hasMarkerContext`)
- 종류 3-col grid (6 EXTINGUISHER_TYPES, accent active / surface-sunken inactive)
- 6 필드 (제조업체/형식승인번호/제조년월/제조번호/접두문자/증지번호)
  - FieldLabel: fontSize 12 / text-tertiary
  - inputStyle: surface-sunken + border-strong + radius-sm + text-primary
- Action row 2 button (cancelBtnStyle / primaryBtnStyle)

비즈니스 로직 (lastReg localStorage load / handleSubmit validation / hasMarkerContext primary 라벨 분기) 0 변경.
</objective>

<context>

# 토큰 매핑

## modalWrapperStyle (l.1134-1138)
- `var(--bg2)` → `--surface-raised`
- `var(--bd2)` → `--border-strong`
- borderRadius 16 → `--radius-lg`
- maxWidth 360 / maxHeight 80vh / padding 20 그대로

## infoBannerStyle (l.1140-1146)
- `rgba(14,165,233,.10)` → `--status-info-bg`
- `rgba(14,165,233,.25)` → `--status-info-bar` 1px
- `var(--info)` → `--status-info`
- fontSize 11→12 (마지노선)
- borderRadius 8 → `--radius-sm`

## inputStyle (l.1148-1154)
- `var(--bg3)` → `--surface-sunken`
- `var(--bd2)` → `--border-strong`
- borderRadius 8 → `--radius-sm`
- `var(--t1)` → `--text-primary`
- fontSize 13 / height 40 그대로

## cancelBtnStyle (l.1156-1161)
- `var(--bg)` → `--surface-page` (모달 안 가장 어두운 톤으로 위계)
- `var(--t2)` → `--text-secondary`
- `var(--bd2)` → `--border-strong`
- borderRadius 12 → `--radius-md`
- fontSize 13 그대로

## primaryBtnStyle (l.1163-1168)
- `var(--acl)` → `--accent`
- `#fff` → `--accent-fg`
- borderRadius 12 → `--radius-md`
- fontSize 13 그대로

## 종류 3-col grid 버튼 (l.884-889)
- active: bg `var(--acl)` → `--accent` / color `#fff` → `--accent-fg` / border none
- inactive: bg `var(--bg3)` → `--surface-sunken` / color `var(--t2)` → `--text-secondary` / border `var(--bd)` → `--border-default` 1px
- radius 8 → `--radius-sm`
- fontSize 12 / fontWeight 700 / padding 8px 0

## 타이틀 (l.864-866)
- fontSize 16 / fontWeight 700 / color `var(--t1)` → `--text-primary` / marginBottom 16

## FieldLabel (l.1126)
- fontSize 11→12 / fontWeight 500 / `var(--t3)` → `--text-tertiary` / marginBottom 6

## ModalBackdrop (l.1109-1114)
- position fixed inset 0 / zIndex 40
- `rgba(0,0,0,0.6)` → `--surface-overlay`

## 비활성화 등록 버튼 (l.918)
- background `var(--bd2)` → `--border-strong`
- color `var(--t3)` → `--text-tertiary`
- cursor not-allowed

# Viewport 매트릭스

| ID | 상태 | 핵심 |
|---|---|---|
| V1 | 모바일 — 마커 동행 OFF, 종류 "분말 3.3kg" active | info banner "최대 3개 필드 수정" + 6 필드 + 등록 버튼 |
| V2 | 모바일 — 마커 동행 ON | info banner + 마커 배너 "「연구동 3F」 자동 배치" + 등록 버튼 "등록 후 배치" |
| V3 | 모바일 — 등록 중 (submit disabled) | submit 버튼 disabled 톤 (border-strong bg, text-tertiary) + "등록 중…" 라벨 |
| V4 | 데스크톱 — 마커 동행 OFF (모달은 maxWidth 360 고정이라 사이즈 동일, backdrop 넓게) | overlay 비주얼 강조 |

</context>

<tasks>

<task type="auto">
  <name>Task 1: register-modal-sketch.html 작성</name>
  <files>cha-bio-safety/docs/redesign-context/09-extinguishers/sketch/register-modal-sketch.html</files>
  <action>
1. :root 정의: chrome+card wave 의 토큰 + surface-overlay + radius-lg (16px)
2. 4 viewport (V1~V4) 모두 실제 모달 마크업
3. ModalBackdrop overlay 톤 노출 (mock frame 안에 backdrop 흉내)
4. 카드 wave 와 동일한 dot / 룰 / 한글 풀이 패턴
5. rule 박스 마지막 (적용 룰 / 금지 룰 / 비즈니스 로직 / viewport 매트릭스)
  </action>
</task>

</tasks>
