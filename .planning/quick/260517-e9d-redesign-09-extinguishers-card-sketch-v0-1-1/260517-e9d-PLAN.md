---
phase: quick
plan: 01
type: execute
wave: 2
quick_id: 260517-e9d
depends_on: [260517-e9c]
files_modified:
  - cha-bio-safety/docs/redesign-context/09-extinguishers/sketch/card-sketch.html
autonomous: true
must_haves:
  truths:
    - "단일 HTML 파일이 v0.1.1 토큰만 사용 (옛 alias 0건, 카드 내부 raw hex 0건)"
    - "8 viewport — 미배치 collapsed / 배치 collapsed + warning / expanded 3 state (unmapped-clean / unmapped-inspected / mapped) / 폐기 / danger 교체 초과 강조 / 데스크톱 그리드"
    - "상태 badge 3종 (배치됨=accent / 미배치=danger / 폐기=warning) 페어 사용"
    - "교체 warning chip 2색 (warn+imminent=warning / danger=danger) — 원본 코드 그대로"
    - "Action button 3종 (정보수정/소화기배치 = surface-sunken / 삭제·폐기·분리 = danger 페어)"
    - "이모지 0건 (📍 ⚠️ 모두 텍스트 또는 dot 대체)"
---

<objective>
ExtinguishersListPage.tsx 의 ExtinguisherCard (l.646-784) + DetailField (l.799-811) + Card grid (l.487-557) 의 시각 디자인을 v0.1.1 토큰으로 sketch.

영역:
- 카드 grid wrapper (모바일 1열 / 데스크톱 2~3열)
- 카드 wrapper 3 변형 (일반 / expanded / disposed)
- Row 1: type 이름 (text-primary) + 상태 badge (배치됨 / 미배치 / 폐기)
- Row 2: 제조번호 · 증지번호 (mono, text-secondary)
- Row 3: location (text-tertiary) + 교체 warning chip (warn/imminent/danger)
- Expanded: DetailField grid 2열 + Action button row (3 state 별 분기)
- Disposed: opacity 0.6 + "폐기된 자산입니다." 라벨

비즈니스 로직 (state machine `getMappingState` / warning 분기 / action 버튼 state 별 노출) 0 변경.
</objective>

<context>

# 토큰 매핑 (현재 → v0.1.1)

## 카드 wrapper (l.665-676)
- 일반: bg2 → `--surface-raised`, border bd → `--border-default` 1px
- expanded: border `1.5px solid var(--acl)` → `--accent`
- disposed: bg3 → `--surface-sunken`, opacity 0.6 그대로
- borderRadius 12 → `--radius-md`
- padding 12 그대로

## Row 1 — type + badge (l.679-691)
- type: fontSize 14 / fontWeight 700 / color t1 → `--text-primary` / flex 1
- badge: fontSize 11→12 / fontWeight 700 / padding 2/8 / borderRadius 5 → `--radius-sm`
  - 폐기 (disposed): bg `rgba(245,158,11,.15)` + color `var(--warn)` → `--status-warning-bg` + `--status-warning`
  - 배치됨 (cp_id): bg `rgba(59,130,246,.15)` + color `var(--acl)` → `--accent` (accent-bg 별도 정의 필요 / 또는 `rgba(59,130,246,.15)` 를 sketch 의 :root 에 `--accent-bg` 로 정의)
  - 미배치 (else): bg `rgba(239,68,68,.15)` + color `var(--danger)` → `--status-danger-bg` + `--status-danger`
  - 모든 배지 좌측에 dot 추가 (이모지 회피 + 시각 일관성)

## Row 2 — 제조/증지번호 (l.694-705)
- fontSize 12 / fontWeight 500 / color t2 → `--text-secondary`
- fontFamily mono 그대로

## Row 3 — location + warning chip (l.708-730)
- location 텍스트:
  - fontSize 11→12 / fontWeight 500 / color t3 → `--text-tertiary`
  - "📍" 이모지 → 좌측 dot (text-tertiary)
- warning chip:
  - fontSize 11→12 / fontWeight 700 / padding 2/8 / borderRadius 5 → `--radius-sm`
  - warn/imminent (코드: warning !== 'danger'): bg yellow (warning-bg) + color (warning) + border (warning-bar)
  - danger: bg red (danger-bg) + color (danger) + border (danger-bar)
  - "⚠️" 이모지 → 좌측 dot (currentColor)
  - 텍스트: "교체 도래 / 임박 / 초과"

## Expanded block (l.733-774)
- borderTop bd → `--border-default`
- DetailField grid 2열 / gap 3px 12px / fontSize 11→12 / paddingTop 8 / marginTop 4
- DetailField label: t3 → `--text-tertiary` / fontWeight 400
- DetailField value: t1 → `--text-primary` / fontWeight 700 / mono 옵션
- Action row: gap 8 / marginTop 4
  - actionBtnStyle: flex 1 / height 36 / radius 8 → `--radius-sm` / fontSize 12 / fontWeight 700 / bg `var(--bg3)` → `--surface-sunken` / color t1 → `--text-primary` / border bd2 → `--border-strong`
  - dangerBtnStyle: color danger → `--status-danger` / bg `rgba(239,68,68,.08)` → `--status-danger-bg` / border `rgba(239,68,68,.3)` → `--status-danger-bar`
  - 3 state 별 버튼:
    - unmapped-clean: 정보수정 / 소화기배치 / 삭제(danger)
    - unmapped-inspected: 정보수정 / 소화기배치 / 폐기(danger)
    - mapped: 정보수정 / 소화기분리(danger)

## Disposed 카드 (l.776-781)
- "폐기된 자산입니다." fontSize 11→12 / color t3 → `--text-tertiary` / textAlign center / padding 4 0

# Viewport 매트릭스 (8개)

| ID | 상태 | 카드 변형 | 핵심 검증 |
|---|---|---|---|
| V1 | 모바일 collapsed 미배치 | type "분말 3.3kg" / 미배치 badge danger / 위치 미지정 / warning 없음 | 미배치 danger badge + dot |
| V2 | 모바일 collapsed 배치됨 + 교체 도래 | "분말 20kg" / 배치됨 badge accent / "연구동 5F · 복도" / 교체 도래 warning (yellow) | accent badge dot + warning chip dot |
| V3 | 모바일 expanded mapped (배치됨 action) | V2 펼침 + DetailField grid + 2 action (정보수정 / 분리 danger) + accent 1.5px border | accent border-stronger / detail grid / action btn |
| V4 | 모바일 expanded unmapped-clean | type + 미배치 badge + DetailField 3 action (정보수정 / 배치 / 삭제 danger) | 3 action 분기 |
| V5 | 모바일 expanded unmapped-inspected | type + 미배치 badge + DetailField 3 action (정보수정 / 배치 / 폐기 danger) | "삭제" vs "폐기" 라벨 분기 |
| V6 | 모바일 disposed | type "이산화탄소" / 폐기 badge warning / "폐기된 자산입니다." 라벨 / opacity 0.6 | disposed surface-sunken + opacity |
| V7 | 모바일 danger 교체 초과 | "할로겐" / 배치됨 / 교체 초과 danger chip (red) | danger chip 톤 |
| V8 | 데스크톱 그리드 2열 | V2 + V6 (배치 + 폐기) 2 카드 | 데스크톱 grid gap |

# 비즈니스 로직 보존

- `getMappingState(item)` → unmapped-clean / unmapped-inspected / mapped / disposed 분기
- `getReplaceWarning(item.type, item.manufactured_at)` → warn / imminent / danger / null
- badge label: 폐기 / 배치됨 / 미배치
- 카드 wrapper onClick: !isDisposed 만 onToggle
- Action row 의 onClick e.stopPropagation()

</context>

<tasks>

<task type="auto">
  <name>Task 1: card-sketch.html 작성</name>
  <files>cha-bio-safety/docs/redesign-context/09-extinguishers/sketch/card-sketch.html</files>
  <action>
1. :root 정의 — chrome-sketch.html 의 토큰 페어 그대로 인용 + accent-bg 추가 (`rgba(59,130,246,0.16)`)
2. 8 viewport (V1~V8) 모두 실제 마크업
3. 카드 내부 모든 색은 var(--...) 만
4. 이모지 (📍 ⚠️) 0 — 모두 dot (CSS) 또는 텍스트
5. rule 박스 마지막에 — 한글 풀이 사용 (금지 리터럴 박지 않기)
  </action>
  <verify>
    <automated>F=cha-bio-safety/docs/redesign-context/09-extinguishers/sketch/card-sketch.html && test -f $F && [ $(wc -l < $F) -ge 400 ] && [ $(grep -c 'data-viewport="V[1-8]"' $F) -ge 8 ]</automated>
  </verify>
  <done>파일 >= 400 lines. 8 viewport.</done>
</task>

<task type="auto">
  <name>Task 2: 12 verify gate</name>
  <files>cha-bio-safety/docs/redesign-context/09-extinguishers/sketch/card-sketch.html</files>
  <action>
1. viewport ≥ 8
2. status-danger-bg / status-warning-bg 각 ≥ 1
3. accent (배치됨 badge + expanded border) ≥ 1
4. surface-raised (카드 wrapper) ≥ 6
5. surface-sunken (disposed + action btn) ≥ 2
6. 옛 alias 0
7. linear-gradient 0
8. raw hex/rgba 카드 영역 0
9. 이모지/shadow/blur 0
10. text-primary / text-secondary / text-tertiary 각 ≥ 1
11. border-default / border-strong 각 ≥ 1
12. radius-md / radius-sm 각 ≥ 1
  </action>
  <verify>
    <automated>F=cha-bio-safety/docs/redesign-context/09-extinguishers/sketch/card-sketch.html && [ $(grep -c 'data-viewport="V[1-8]"' $F) -ge 8 ] && [ $(grep -c 'var(--status-danger-bg)' $F) -ge 1 ] && [ $(grep -c 'var(--status-warning-bg)' $F) -ge 1 ] && [ $(grep -c 'var(--surface-raised)' $F) -ge 6 ] && [ -z "$(grep -E 'var\(--(bg2?|bg3|bg4|bd2?|t1|t2|t3|acl|safe\)|warn\)|danger\)|info\)|fire\))' $F)" ] && [ $(grep -c 'linear-gradient' $F) -eq 0 ] && [ -z "$(grep -E '[✓✕★⚠🔧📋]|box-shadow|filter:\s*blur|backdrop-filter' $F)" ]</automated>
  </verify>
  <done>12 gate PASS.</done>
</task>

</tasks>

<output>
완료 후 `.planning/quick/260517-e9d-.../260517-e9d-SUMMARY.md` 작성.
</output>
