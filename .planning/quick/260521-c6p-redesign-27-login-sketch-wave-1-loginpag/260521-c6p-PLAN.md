---
phase: quick-260521-c6p
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cha-bio-safety/docs/redesign-context/27-login/wave-1-index.md
autonomous: true
requirements:
  - REDESIGN-27-WAVE1
must_haves:
  truths:
    - "wave-1-index.md 파일이 cha-bio-safety/docs/redesign-context/27-login/ 직속에 생성됨 (sketch/ 서브폴더 X — 13-schedule + 14-reports 평면 패턴 mirror)"
    - "7개 필수 섹션(§1~§7) 모두 채워짐"
    - "LoginPage.tsx 인벤토리 표가 (1) 모바일 전용 헤더 (2) 공통 inner 블록 (3) 데스크톱 wrapper 3 영역 모두 포함"
    - "4 sub-wave 분배 표가 W2~W5 행을 모두 포함 (14-reports 의 6 sub-wave 보다 줄임 — LoginPage 가 220 lines 단순 페이지)"
    - "design-system.md v0.1.1 §1.1/§1.2/§1.3/§6/§7/§10 인용이 fence 안 verbatim 으로 포함 (§6/§7/§10 은 미적용 1줄 메타 동반)"
    - "메모리 룰 10개가 inline 인용 (각 1줄 요약 + Why + How — How 는 27-login LoginPage element 기준 구체적)"
    - "negative rule 섹션이 sketch HTML 금지 / 코드 수정 금지 / wrangler 금지 / npm run deploy 금지 / 폴더 구조 일관 5건 포함"
    - "OQ 5건이 §7 에 정리됨 (LoginPage 의 실제 결정 포인트 — 그라데이션 / CARD_COLORS / footer / show-hide / logo box 사이즈)"
    - "LoginPage.tsx 파일은 변경되지 않음 (코드 변경 0)"
    - "/login 이 MOBILE_NO_NAV_PATHS ∩ DESKTOP_NO_NAV_PATHS 양쪽에 등재된 사실이 §4 에 1줄로 박제됨"
  artifacts:
    - path: "cha-bio-safety/docs/redesign-context/27-login/wave-1-index.md"
      provides: "W2~W5 진입을 위한 단일 진입점 인덱스 + 룰 verbatim 인용 + sub-wave 분배 매핑 (LoginPage 단순 페이지용 4 sub-wave 분배)"
      contains: "§1. LoginPage.tsx 인벤토리, §2. 4 sub-wave 분배, §3. design-system v0.1.1 verbatim, §4. chrome 통일 룰 적용 여부 (인증 전 페이지 - 미적용 + BottomNav 숨김 확인), §5. 메모리 룰 10개 inline, §6. negative rule, §7. open questions"
  key_links:
    - from: "wave-1-index.md"
      to: "cha-bio-safety/src/pages/LoginPage.tsx"
      via: "§1 인벤토리에 line 범위 인용 + §2 sub-wave 분배 표의 element/line 매핑"
      pattern: "line [0-9]+"
    - from: "wave-1-index.md"
      to: "cha-bio-safety/docs/redesign-context/27-login/design-system.md"
      via: "§3 fence verbatim 인용 (§1.1/§1.2/§1.3 본문 박제)"
      pattern: "design-system.md §"
    - from: "wave-1-index.md"
      to: "cha-bio-safety/docs/redesign-context/00-design-context/inspection-modal-chrome-rules.md"
      via: "§4 chrome 룰 인증 전 페이지 미적용 명시"
      pattern: "inspection-modal-chrome-rules"
    - from: "wave-1-index.md"
      to: "cha-bio-safety/src/App.tsx"
      via: "§4 BottomNav 숨김 확인 — MOBILE_NO_NAV_PATHS (line 71) + DESKTOP_NO_NAV_PATHS (line 74) 양쪽 등재"
      pattern: "MOBILE_NO_NAV_PATHS|DESKTOP_NO_NAV_PATHS"
---

<objective>
redesign/27-login sketch 작업의 wave 1 — 후속 wave(W2~W5) 의 단일 진입점이 되는 인덱스/룰 정리 문서 1개만 작성한다.

Purpose: LoginPage.tsx (220 라인 — 14-reports 의 ReportsPage.tsx 405 라인보다 단순) 의 모든 element 를 **4 sub-wave** 로 분배 (14-reports W1 의 6 sub-wave 보다 줄임, 단순 페이지 특성 반영), 그리고 design-system.md v0.1.1 룰과 메모리 룰 10개를 verbatim 박제해서 후속 sketch wave 작업자가 이 인덱스만 보면 일관되게 작업할 수 있도록 한다.

Output: `cha-bio-safety/docs/redesign-context/27-login/wave-1-index.md` 단 1개 파일. 코드 변경 0건. sketch HTML 생성 0건 (그건 W2 부터).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@./CLAUDE.local.md

# 14-reports W1 precedent (이번 wave 가 mirror 할 정확한 7 섹션 구조)
@.planning/quick/260520-ep5-redesign-14-reports-sketch-wave-1-design/260520-ep5-PLAN.md
@.planning/quick/260520-ep5-redesign-14-reports-sketch-wave-1-design/260520-ep5-SUMMARY.md

# Source file (이 wave 가 분석하는 대상 — 수정하지 않음)
@cha-bio-safety/src/pages/LoginPage.tsx

# Redesign context (이 wave 가 산출할 인덱스가 인용/참조하는 문서들)
@cha-bio-safety/docs/redesign-context/27-login/27-login.md
@cha-bio-safety/docs/redesign-context/27-login/design-system.md
@cha-bio-safety/docs/redesign-context/00-design-context/inspection-modal-chrome-rules.md

# 14-reports 직전 페이지 W1 산출물 실측 — 폴더 평면 구조 + 산출 markdown 형태 mirror
# 실제 경로: cha-bio-safety/docs/redesign-context/14-reports/wave-1-index.md
# 13-schedule + 14-reports 모두 평면 sibling 패턴 (sketch/ 서브폴더 없음). 본 wave 도 동일.
</context>

<interfaces>
<!-- 후속 wave 가 산출할 sketch 파일 명명 규칙 (이 인덱스가 §2 표에서 인용) -->
<!-- 13-schedule + 14-reports 평면 패턴 일관 — 27-login/ 직속에 위치 -->

W2 → cha-bio-safety/docs/redesign-context/27-login/sketch-wave-2-mobile-shell.html
W3 → cha-bio-safety/docs/redesign-context/27-login/sketch-wave-3-staff-card-grid.html
W4 → cha-bio-safety/docs/redesign-context/27-login/sketch-wave-4-login-form.html
W5 → cha-bio-safety/docs/redesign-context/27-login/wave-5-tsx-conversion-checklist.md (markdown, sketch 아님)

(주의: planner 가 인벤토리 실측 후 가장 합리적인 4 sub-wave 분배로 결정. 위 label 은 권장안이며, 본 wave 작성 시 LoginPage.tsx element 매핑 결과에 맞춰 §2 표에서 확정. label 이 바뀌더라도 4-wave 개수와 sketch-wave-N-{slug}.html (W2~W4) + wave-5-tsx-conversion-checklist.md (W5) 평면 패턴은 유지.)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: wave-1-index.md 작성</name>
  <files>cha-bio-safety/docs/redesign-context/27-login/wave-1-index.md</files>
  <action>
LoginPage.tsx (220 라인) 와 27-login.md / design-system.md / inspection-modal-chrome-rules.md 를 모두 끝까지 읽은 뒤 아래 7개 섹션을 가진 단일 markdown 파일을 작성한다. 파일은 Write 도구로 생성한다 (heredoc/cat 금지).

---

# 파일 헤더

상단에 다음 1블록:
- 제목: `# redesign/27-login — sketch wave 1 (index)`
- 1-2줄 설명: 본 문서는 W2~W5 진입의 단일 진입점이며, 이 인덱스만 봐도 후속 wave 가 디자인 룰 / 메모리 룰 / sub-wave 분배 / OQ 를 알 수 있도록 한다.
- 산출일자: 2026-05-21 / Quick ID 260521-c6p / branch redesign/27-login
- 1줄 메타: "14-reports W1 (260520-ep5) 의 7 섹션 구조를 mirror 하되, LoginPage 가 220 lines 단순 페이지라 sub-wave 는 6 → 4 로 축소 (W2~W5)."

---

# §1. LoginPage.tsx 인벤토리

LoginPage.tsx 의 element 를 (1) 모바일 전용 헤더 / (2) 공통 inner 블록 / (3) 데스크톱 wrapper 3 영역으로 나눠 표로 정리. 각 행은 (영역 / element / source line 범위 / 역할 / 비즈 로직 연결 / 후속 wave 매핑) 6 컬럼.

**LoginPage 의 구조 특이성** (인벤토리 머리말로 1단락):
- `const inner = (...)` (line ~82~172) 는 모바일/데스크톱 양쪽에서 공통 사용
- 모바일 (line ~201~219) = 상단 헤더 + inner 을 flex column 으로 감쌈
- 데스크톱 (line ~175~198) = inner 을 max-width:420px centered card 안에 wrap, card 헤더 한번 더
- useMediaQuery('(min-width: 768px)') 로 분기 (line 40, hook line 8~19)
- 직원 카드 색상 `CARD_COLORS` 6종 (line 21~28) — status/duty 와 별개 카테고리 색

**영역 1: 공통 hook + state + handler** (line 8~80)
- `useMediaQuery` 커스텀 hook (line 8~19) — sketch 무관, 보존만
- state: staffId / password / showPw / loading / selected / staffList (line 31~36)
- effect: fetch `/api/public/staff-list` (line 42~46)
- handler: selectStaff / handleSubmit (line 48~72)
- inputStyle 객체 (line 74~79) — sketch 시 토큰 클래스로 치환 검토
- 후속 wave: **무관** (비즈 로직 보존, W5 checklist 에서 확인)

**영역 2: 모바일 전용 헤더** (line ~204~213)
- 로고 박스 38x38 radius 11 + 내부 icon-192.png 28x28 radius 7
- 타이틀 "차바이오컴플렉스 방재팀" (fontSize:16, fontWeight:700, color:var(--t1))
- 서브 "소방안전 통합관리 시스템" (fontSize:11, color:var(--t3))
- 비즈: 정적 텍스트 + 아이콘 이미지
- 후속 wave: **W2** (모바일 shell 의 일부)

**영역 3: 공통 inner — 직원 카드 그리드** (line ~85~115)
- 컨테이너: var(--bg2) bg, radius:16, padding:16, border 1px var(--bd)
- 라벨 "담당자 선택" (fontSize:10, fontWeight:700, var(--t3), letterSpacing .06em, uppercase)
- 그리드: 2-column, gap:8
- 카드 (반복): 34x34 initial 박스 + 이름(13/700) + role/title(10/var(--t3))
  - CARD_COLORS 6종 cycle (i % 6)
  - isSelected 상태 — border rgba(59,130,246,0.6) + bg rgba(59,130,246,0.12) + initial bg #2563eb
- 비즈: setSelected / setStaffId / pwRef.focus
- 후속 wave: **W3** (staff card grid 단독)

**영역 4: 공통 inner — 로그인 폼** (line ~118~165)
- 컨테이너: var(--bg2) bg, radius:16, padding:16, border
- label "사번" + input (inputMode numeric, placeholder "사번 10자리")
- label "비밀번호" + input wrapper (showPw 토글 — 텍스트 "표시/숨김" line 148)
- 로그인 버튼 (line 152~163) — `linear-gradient(135deg,#1d4ed8,#2563eb)` 그라데이션, fontSize:14 weight:700
- 비즈: handleSubmit / setShowPw / setPassword / Enter key
- 후속 wave: **W4** (login form 단독)

**영역 5: 공통 inner — footer 안내문** (line ~167~170)
- "초기 비밀번호: 사번 뒤 4자리"
- "문의: 방재팀 내선 ☎ 031-881-7119"
- 스타일: textAlign:center, fontSize:11, color:var(--t3), marginTop:20, lineHeight:1.6
- 비즈: 정적 텍스트
- 후속 wave: **W4** (login form 과 묶음 — footer 도 폼 카드 직후 등장)

**영역 6: 데스크톱 wrapper** (line ~175~198)
- 외곽: minHeight 100dvh, flex center, padding 20
- 카드: maxWidth 420, radius 20, var(--bg2), border, boxShadow 0 8px 32px rgba(0,0,0,0.4), overflow hidden
- 카드 헤더 (line 180~190) — 모바일 헤더와 동일 구조 (로고 38x38 + 타이틀/서브)
- 카드 바디 (line 192~194) — padding 16 16 24, 그 안에 {inner}
- 후속 wave: **W2** (mobile shell 과 같이 묶음 — 헤더 공통 + wrapper 차이만)

인벤토리 작성 시 line 추정치는 실측 우선. 220 lines 가 task_scope 가정과 일치하면 1줄 명시, 불일치하면 차이 보고.

---

# §2. 4 sub-wave 분배 plan

다음 표 그대로 박제 (단, 파일명은 위 <interfaces> 의 통일된 `sketch-wave-N-{slug}.html` 패턴 사용. label 은 planner 가 인벤토리 실측 후 결정한 결과로 §2 에서 확정):

| Wave | scope | 대상 element | 산출 파일 |
|---|---|---|---|
| W2 | 모바일 shell + 데스크톱 wrapper + 공통 헤더 (로고+타이틀+시스템명) | 영역 2 (모바일 헤더) + 영역 6 (데스크톱 wrapper + 카드 헤더). useMediaQuery 분기는 inner 공통이라 chrome 만 sketch 1매로 묶음. | sketch-wave-2-mobile-shell.html |
| W3 | 직원 카드 그리드 (2열 × 6 staff, CARD_COLORS 6종 cycle) | 영역 3 — 카드 컨테이너 + 라벨 + 6 카드 variant matrix + isSelected 상태 | sketch-wave-3-staff-card-grid.html |
| W4 | 로그인 폼 + footer 안내문 (input + show/hide 토글 + CTA + 안내) | 영역 4 + 영역 5 — 사번/비밀번호 input + 토글 + 로그인 버튼 + footer 2줄 | sketch-wave-4-login-form.html |
| W5 | TSX 변환 verify checklist (sketch 아님, markdown) | W2~W4 sketch + LoginPage.tsx 비즈 로직 보존 룰 + Tailwind cheatsheet | wave-5-tsx-conversion-checklist.md |

각 wave 행 아래에 boldface "보존 / 토큰 / 폰트" 3 미니 섹션:
- **보존**: 변환 후 보존해야 할 비즈 로직 호출 —
  - W2: useMediaQuery 분기 / 로고 src "/icons/icon-192.png" / 타이틀 카피 verbatim
  - W3: setSelected / setStaffId / pwRef.focus / CARD_COLORS 6종 rgba (status/duty 와 별개)
  - W4: handleSubmit / setShowPw / setPassword / Enter key submit / placeholder verbatim / disabled+loading 분기 / footer 카피 verbatim ("초기 비밀번호: 사번 뒤 4자리", "문의: 방재팀 내선 ☎ 031-881-7119")
  - W5: 위 모든 항목의 TSX 보존 checklist
- **토큰**: 적용할 디자인 토큰 — bg-surface-raised / bg-surface-sunken (input) / text-text-primary / text-text-secondary / text-text-tertiary / border-border-default / bg-safe-bar (그라데이션 폐기 OQ #1 default) / radius-md (12) / radius-lg (16). status- prefix 없음 (memory feedback_tailwind_token_class_pattern).
- **폰트**: 모바일/데스크톱 동일 폰트 룰 (design-system §1.3). 본문 16px 마지노선, 9~11px 금지 (§1.1). 단 현재 LoginPage 의 11px footer / 10px label / 11px 라벨은 §1.1 위반 — OQ #3 에서 결정.

---

# §3. design-system.md v0.1.1 인용 (verbatim 발췌, fence 안)

design-system.md (cha-bio-safety/docs/redesign-context/27-login/design-system.md) 를 읽고 아래 항목을 각각 별도의 ```fence 블록``` 안에 **원문 그대로** 박제 (불가피한 줄바꿈 제외 정확히 일치):

- §1.1 노안 친화 (본문 16px, 9~11px 금지, 터치 44px)
- §1.2 정보 인지 > 미적 정제
- §1.3 모바일/데스크톱 동일 폰트
- §6 Progress Color Rule → fence 인용 후 1줄 메타: "§6 미적용 — 로그인 페이지에는 진척률 도넛/카테고리 카드 없음."
- §7 Stat Card → fence 인용 후 1줄 메타: "§7 미적용 — 로그인 페이지에는 통계 숫자 카드 없음."
- §10 Iconography → fence 인용 후 1줄 메타: "§10 — Lucide 사용 가능 (예: Eye/EyeOff for show/hide 토글). 커스텀 SVG 6종 미사용. 로고 아이콘은 외부 이미지 /icons/icon-192.png (Lucide 가 아님 — PNG 자산 유지)."

만약 design-system.md 안 섹션 번호/제목이 task_scope 가정과 다르면 실제 파일 기준으로 §번호를 맞춰 인용하고, 1줄 메타에서 그 차이를 명시.

추가:
- 기존 디자인 = `linear-gradient(135deg,#1d4ed8,#2563eb)` 로그인 버튼 → `bg-safe-bar` solid 로 치환. 근거: **13-schedule W6 LOCKED b + 14-reports W1 OQ #1/#3 default OK 일관** + design-system §6.4 CTA 그라데이션 폐기 룰 + memory `feedback_design_sketch_first` + `feedback_tailwind_token_class_pattern`. 이 결정은 §7 OQ #1 에서 사용자 컨펌 받음 (단, 직전 페이지 일관 정책상 OQ default = solid OK).

---

# §4. 02+06 chrome 통일 룰 적용 여부

`inspection-modal-chrome-rules.md` 를 읽고 1~2 단락으로 작성:

- 27-login 페이지는 점검 페이지 시리즈가 아닌 **인증 전 (pre-auth) 페이지** → chrome 룰 자체는 **미적용** (back button / SideMenu / BottomNav 모두 비노출)
- 단, 다음 3가지 패턴은 mirror:
  1. **헤더 폰트 크기** — 모바일/데스크톱 양쪽 헤더 타이틀 16px → 18px (text-title) 통일 검토. 단, 14-reports + 13-schedule 와 달리 로그인은 인증 전이라 페이지 타이틀 위계가 낮음 — 16px 유지도 합리적. §7 OQ 후보 아님 (planner 판단 default 16px 유지).
  2. **back button 패턴** — 인증 전 페이지 → back button 자체 없음. mirror 무관.
  3. **BottomNav 숨김** — `MOBILE_NO_NAV_PATHS` (App.tsx line 71) + `DESKTOP_NO_NAV_PATHS` (App.tsx line 74) **양쪽** 에 `/login` 등재됨 (실측 확인). 모바일/데스크톱 모두 BottomNav 숨김 — sketch 시 nav 그릴 필요 없음. 1줄 박제.

**실측 결과 (App.tsx 본문 grep):**
```
line 71: MOBILE_NO_NAV_PATHS = ['/', '/login', ...]
line 74: DESKTOP_NO_NAV_PATHS = ['/', '/login']
```
양쪽 등재 확인 — W2 sketch 진입 시 BottomNav placeholder/예약 공간 그릴 필요 없음.

---

# §5. 메모리 룰 inline 인용 (verbatim)

아래 10개 룰을 **각각 별도 미니 카드**로 박제. 각 카드 포맷 (boldface 라벨):

```
### 룰 N — {룰 슬러그}
- **요약**: 1줄
- **Why**: 1줄
- **How to apply (27-login)**: 1줄
```

10개 룰 (14-reports W1 와 정확히 동일 10개 슬러그):
1. `feedback_design_sketch_first` — spacing/sizing 도 sketch HTML 시안 먼저
2. `feedback_redesign_sketch_rule_enforcement` — §6.2 negative rule / §6.3 §7.1 일관성
3. `feedback_sketch_realistic_data` — 표시 분기/라벨 룰 verbatim, 시각 디자인만
4. `feedback_planner_prompt_sketch_verbatim` — sketch CSS grep 추출, 추측 X
5. `feedback_tailwind_token_class_pattern` — status- prefix 없음
6. `feedback_tailwind_w8_h8_is_48px` — w-8=48 / w-7=32 함정
7. `feedback_text_caption_leading_none` — 작은 컨테이너 leading-none
8. `feedback_tsx_wave_emoji_dot_gap` — 이모지 제거 + dot span 추가
9. `feedback_tsx_wave_stat_card_drift` — Stat Card §6.3 룰 verbatim
10. `feedback_avoid_premature_confirmation` — "거의 일치" 자신감 표현 금지

각 룰의 Why/How 는 LoginPage 의 실제 element/상황으로 구체화 (포괄적 추상적 X). 예시 (각 룰 작성 시 참고, 그대로 박제 가능):
- **룰 1** (sketch first) — "W3 staff card grid 의 카드 크기 (현재 34x34 initial 박스 / padding 10 / gap 8 / 2-column) 조정도 spacing 손볼 거 있으면 sketch 먼저 보여주고 컨펌."
- **룰 2** (negative rule) — "직원 카드 색상 (CARD_COLORS 6종) 은 status 와 무관 — `bg-status-safe-bg` 같은 위험 임계치 색 사용 금지 (각 카드는 카테고리 색일 뿐)."
- **룰 3** (realistic data) — "footer 카피 '초기 비밀번호: 사번 뒤 4자리' / '문의: 방재팀 내선 ☎ 031-881-7119' 는 verbatim. 표시 분기/카피 변경 금지."
- **룰 4** (verbatim grep) — "W5 TSX 변환 wave 진입 시 sketch HTML 의 CSS 정의 (예: 직원 카드 isSelected border `rgba(59,130,246,0.6)`) 를 grep 으로 추출해 그대로 인용. 추측 토큰명 사용 시 deviation 유발."
- **룰 5** (token class pattern) — "직원 카드 hover 색은 `bg-surface-active` 또는 카드별 카테고리 색 유지. `bg-status-safe-bg` (status- prefix) 사용 시 W5 verify FAIL."
- **룰 6** (w-8=48px) — "직원 카드 initial 박스 현재 34x34. tailwind config spacing override 로 w-8=48 (1.5배). w-[34px] 명시 또는 design-system 토큰 (w-9=44? 검증 필요) 사용."
- **룰 7** (leading-none) — "label '사번' / '비밀번호' text-label(13px lh:1.5=19.5) 가 marginBottom:6 컨테이너 안에서 시각 패딩 유발. leading-none 명시 검토."
- **룰 8** (dot gap) — "footer 의 '☎ 031-881-7119' 안 ☎ 이모지 — 메모리 룰 8 의 '이모지 제거' 대상 검토. 단, ☎ 는 전화 affordance 로 유지 가능 → OQ #4 의견 받음 (default 유지)."
- **룰 9** (Stat Card drift) — "로그인 페이지에 Stat Card 없음 → 미적용. 단, sketch 새 패턴 (예: 직원 카드 색상 variant matrix) 은 W5 진입 시 verbatim 인용."
- **룰 10** (premature confirmation) — "W2~W4 sketch 산출 후 '거의 일치 / 잘 됐다' 자신감 표현 금지. 결과 보여주고 사용자 판단."

---

# §6. negative rule (이 wave 에서 금지된 것)

bullet list:
- sketch HTML 생성 금지 (wave 2 부터)
- LoginPage.tsx 코드 수정 금지 (이 wave 의 산출물은 markdown 1개)
- 비즈 로직 (handleSubmit / selectStaff / useAuthStore.login / fetch /api/public/staff-list / useMediaQuery) 무관 — 이 wave 는 markdown 1개만
- 다른 페이지 (13-schedule, 14-reports 등) 영향 금지
- **wrangler 명령 금지** (CLAUDE.local.md 룰 — `.claude/settings.local.json` deny 강제, memory `feedback_cbc7119_design_never_wrangler`)
- **`npm run deploy` 금지** (CLAUDE.local.md 룰 — 직원 도메인 경로)
- 13-schedule + 14-reports 의 평면 sketch-wave-*.html 패턴과 다른 폴더 구조 도입 금지 (sketch/ 서브폴더 X — 27-login/ 직속에 평면 배치)

---

# §7. open questions (W2 진입 직전 사용자 컨펌)

bullet list 5건:

- **OQ #1**: 로그인 버튼 그라데이션 (`linear-gradient(135deg,#1d4ed8,#2563eb)`, line 157) → `bg-safe-bar` solid 통일 OK?
  - default 답: **OK** (13-schedule W6 LOCKED b + 14-reports W1 OQ #1/#3 일관 + design-system §6.4 CTA 그라데이션 폐기 룰)

- **OQ #2**: 직원 카드 CARD_COLORS 6종 (rgba amber/green/blue/violet/pink/teal) — 디자인 토큰화 vs 인라인 rgba 유지?
  - default 답: **인라인 유지** (27-login.md 섹션 4 명시 "직원 카드 그리드 색상은 status/duty 와 별개 카테고리 색 — 그대로 유지")

- **OQ #3**: 안내 footer 안내문 (현재 11px, line 167) — text-body(16px) 노안 친화 완전 준수 vs text-label(13px) 절충 + leading-none vs 현재 11px 유지?
  - default 답: **text-label(13px) 절충** (§1.1 마지노선 16px 위반이지만 footer 보조 정보 위계상 13px 합리적. 14-reports W1 footer 도 동일 절충 적용)

- **OQ #4**: 비밀번호 show/hide 토글 (현재 텍스트 "표시/숨김", line 148) → Lucide `Eye`/`EyeOff` 아이콘 교체?
  - default 답: **사용자 컨펌 필요** (텍스트는 노안 친화 / 아이콘은 공간 절약. 14-reports 의 Download 아이콘 채택 패턴과 일관성 검토)

- **OQ #5**: 로고 (icon-192.png) 박스 — 현재 38x38 + radius 11 + 내부 28x28 (모바일 line 206, 데스크톱 line 182) → 토큰화 (w-9 h-9 등) vs 인라인 [38px] 유지?
  - default 답: **인라인 [38px] 유지** (tailwind config w-8=48 / w-9=44 — memory 룰 6 함정, 38 은 토큰에 없는 1.5px deviation. 명시 사이즈가 안전)

각 OQ 아래에 "default 답" 1줄 (사용자가 별 의견 없으면 이 답으로 진행할 것이라는 reasonable call). 단, "approved" 받기 전까지 W2 진입 금지.

---

작성 완료 후 자체 verify:
1. 7개 섹션 모두 존재 (§1~§7) — grep `^# §[1-7]` 카운트 = 7
2. 메모리 룰 10개 인용 — 각 룰 슬러그 `feedback_*` 가 본문에 등장하는지 unique grep 카운트 ≥ 10
3. sub-wave 분배 표가 W2~W5 4행 — 표 안에 `| W[2-5] |` 카운트 ≥ 4
4. design-system 인용 fence 가 최소 3개 (§1.1 / §1.2 / §1.3 필수) — fence 카운트 ≥ 6 (open+close)
5. negative rule 안 `wrangler` + `npm run deploy` 키워드 모두 등장 (≥1 each)
6. OQ 5건 — `OQ #` 카운트 ≥ 5
7. LoginPage.tsx 변경 0 — `git diff --name-only HEAD -- cha-bio-safety/src/pages/LoginPage.tsx` 가 빈 출력
  </action>
  <verify>
    <automated>test -f cha-bio-safety/docs/redesign-context/27-login/wave-1-index.md && \
echo "--- section count (expect 7) ---" && \
grep -c '^# §[1-7]' cha-bio-safety/docs/redesign-context/27-login/wave-1-index.md && \
echo "--- subwave rows (expect >=4) ---" && \
grep -E '^\| W[2-5] \|' cha-bio-safety/docs/redesign-context/27-login/wave-1-index.md | wc -l && \
echo "--- memory rules unique (expect >=10) ---" && \
grep -oE 'feedback_[a-z_]+' cha-bio-safety/docs/redesign-context/27-login/wave-1-index.md | sort -u | wc -l && \
echo "--- wrangler keyword in negative rule (expect >=1) ---" && \
grep -c 'wrangler' cha-bio-safety/docs/redesign-context/27-login/wave-1-index.md && \
echo "--- npm run deploy keyword (expect >=1) ---" && \
grep -c 'npm run deploy' cha-bio-safety/docs/redesign-context/27-login/wave-1-index.md && \
echo "--- OQ items (expect >=5) ---" && \
grep -cE 'OQ #[1-5]' cha-bio-safety/docs/redesign-context/27-login/wave-1-index.md && \
echo "--- fence count (expect >=6) ---" && \
grep -c '^```' cha-bio-safety/docs/redesign-context/27-login/wave-1-index.md && \
echo "--- code untouched (expect 0) ---" && \
git diff --name-only HEAD -- cha-bio-safety/src/pages/LoginPage.tsx | wc -l</automated>
  </verify>
  <done>
- `cha-bio-safety/docs/redesign-context/27-login/wave-1-index.md` 파일 존재 (sketch/ 서브폴더 X — 27-login/ 직속)
- 7개 섹션 (§1~§7) 모두 존재, grep 결과 = 7
- §2 sub-wave 표가 W2~W5 4행 모두 포함
- §1 인벤토리에 모바일 헤더 / 공통 inner (staff card grid + login form + footer) / 데스크톱 wrapper 3 영역 모두 포함
- 메모리 룰 10개 (`feedback_*` 슬러그) 모두 inline 인용, unique count ≥ 10
- design-system.md 인용 fence 최소 3개 (§1.1, §1.2, §1.3 필수)
- §4 에 MOBILE_NO_NAV_PATHS + DESKTOP_NO_NAV_PATHS 양쪽 `/login` 등재 실측 결과 1줄 박제
- §6 negative rule 안 `wrangler` 와 `npm run deploy` 키워드 모두 등장
- §7 OQ 5건 모두 정리, 각각 default 답 1줄 포함
- LoginPage.tsx 코드 변경 0 — `git diff --name-only` 결과 빈 출력
- 어떠한 sketch-wave-*.html 도 생성하지 않음 (W2 부터)
  </done>
</task>

</tasks>

<verification>
이 wave 의 verify 는 task 1 의 automated block 으로 충분. 추가 phase-level 검증 없음.

자체 검수 흐름:
1. `git status` — wave-1-index.md 1개 신규, 그 외 변경 0
2. `wc -l cha-bio-safety/docs/redesign-context/27-login/wave-1-index.md` — 합리적 길이 (대략 200~350 줄 예상, LoginPage 가 220 lines 단순 페이지라 14-reports W1 의 338 lines 보다 약간 짧을 수 있음. 너무 짧으면 섹션 누락 가능성)
3. 사용자에게 "wave-1-index.md 작성 완료 / §7 OQ 5건 답변 필요" 보고 후 컨펌 대기 (W2 자동 진입 금지, memory `feedback_avoid_premature_confirmation`)
</verification>

<success_criteria>
- wave-1-index.md 7개 섹션 모두 채워짐 (§1 인벤토리 / §2 sub-wave 분배 / §3 design-system verbatim / §4 chrome 룰 (인증 전 미적용 + BottomNav 양쪽 숨김 실측) / §5 메모리 룰 10개 / §6 negative / §7 OQ)
- 코드 변경 0건 (LoginPage.tsx untouched)
- sketch HTML 0건 생성 (W2 부터)
- 13-schedule + 14-reports 의 평면 sketch-wave-*.html 패턴 mirror — sketch/ 서브폴더 안 만듦
- 사용자 컨펌 받을 OQ 5건 정리됨 (그라데이션 / CARD_COLORS / footer 폰트 / show-hide / 로고 박스)
- automated verify 명령이 PASS (section=7 / subwave≥4 / rules≥10 / wrangler≥1 / deploy≥1 / OQ≥5 / fence≥6 / src 변경=0)
</success_criteria>

<output>
After completion, return summary to user:
- 생성 파일: `cha-bio-safety/docs/redesign-context/27-login/wave-1-index.md`
- §7 OQ 5건 사용자 컨펌 대기
- W2 진입 = OQ #1 답변 후 (`/clear` + 새 `/gsd:quick` 시작 권장 — memory `feedback_gsd_workflow_strict`)
- 다음 wave 파일명 권장: `sketch-wave-2-mobile-shell.html`
</output>
