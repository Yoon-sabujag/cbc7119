---
phase: 260518-mb9
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cha-bio-safety/docs/redesign-context/12-staff-service/sketch/07-desktop-form-sketch.html
autonomous: true
requirements:
  - W7-DESKTOP-CENTER-FORM
  - W7-DOC-LEAVE-GRID-7ROW
  - W7-BUTTON-STATE-MATRIX
  - W7-CTA-3-LABELS

must_haves:
  truths:
    - "Sketch file 07-desktop-form-sketch.html exists alongside W1~W6 in /sketch/"
    - "6 viewport frames render (5 dark + 1 light) showing 280px-wide desktop center panel"
    - "7-row DOC_LEAVE_GRID is visible in at least one VP with 1-col vs 3-col branching clear"
    - "3 button states (normal / selected / registered) are each visible in at least one VP"
    - "CTA labels '휴가 신청', 'PDF 다운로드', '인쇄' each appear at least once verbatim"
    - "Form contents use v0.1.1 tokens (NO alias --bg3/--t1/--acl/--safe; NO raw OLD hex outside :root + 노랑 #facc15 + frame border)"
    - "Reason textarea is shown in VP3/VP4/VP6 (non-annual types); hidden in VP1/VP2/VP5"
    - "기타특별휴가 종류 input is shown only when docLeaveType === 'other_special' (VP4)"
    - "Shift chip in 선택된 날짜 row uses duty-* tokens + short labels (당직 / 비번 / 주간 / 휴무)"
  artifacts:
    - path: "cha-bio-safety/docs/redesign-context/12-staff-service/sketch/07-desktop-form-sketch.html"
      provides: "W7 desktop center panel sketch — 휴가신청서 폼 + 버튼 상태 매트릭스"
      min_lines: 350
      contains: "DOC_LEAVE_GRID"
  key_links:
    - from: "07-desktop-form-sketch.html"
      to: "tokens.css :root block"
      via: "verbatim CSS variable definitions in :root + .light-frame"
      pattern: "--surface-sunken|--accent|--status-safe|--duty-night"
    - from: "07-desktop-form-sketch.html"
      to: "UI-SPEC §9.2 CTA copy"
      via: "button label text"
      pattern: "휴가 신청|PDF 다운로드|인쇄"
    - from: "07-desktop-form-sketch.html"
      to: "DOC_LEAVE_GRID source line 88~104"
      via: "11 leave-type labels rendered across 7 rows"
      pattern: "연차|오전반차|오후반차|경조휴가|병가\\(공상\\)|병가\\(사상\\)|보건휴가|공가|오전공가|오후공가|기타특별휴가"
---

<objective>
W7 of the 12-staff-service redesign series — desktop 중앙 패널 (휴가신청서 폼 전체).

Build a single static HTML sketch showing the 280px-wide desktop center panel
that fills the placeholder left by W6 (06-desktop-3panel-sketch.html). The
sketch renders the entire 휴가신청서 form: title, selected-date row with
shift chip, date-range inputs + days display, DOC_LEAVE_GRID 7-row button
grid, conditional reason textarea, conditional 기타특별휴가 종류 input,
and the 3 action buttons (휴가 신청 / PDF 다운로드 / 인쇄).

Purpose: Lock the desktop-form visual contract before TSX wave. Show the
3-state button matrix (normal / selected / registered) and the conditional
panels (reason textarea, 기타특별 type input) across 6 viewport frames so
the TSX executor has a verbatim visual reference.

Output: cha-bio-safety/docs/redesign-context/12-staff-service/sketch/07-desktop-form-sketch.html
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@/Users/jykevin/Documents/cbc7119-design/CLAUDE.md
@/Users/jykevin/Documents/cbc7119-design/CLAUDE.local.md
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/12-staff-service/UI-SPEC.md
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/12-staff-service/tokens.css
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/12-staff-service/typography.css
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/12-staff-service/StaffServicePage.tsx
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/12-staff-service/sketch/06-desktop-3panel-sketch.html
@/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/12-staff-service/sketch/05-bottomsheet-sketch.html

<interfaces>
<!-- v0.1.1 tokens (from tokens.css :root, dark theme) — use verbatim in :root block -->
<!-- Surface / text / border -->
--surface-sunken: #232a33;
--text-on-accent: #ffffff;
--border-default: rgba(255, 255, 255, 0.14);
--text-primary / --text-secondary / --text-tertiary (from typography/tokens.css :root)

<!-- Accent (PDF 다운로드 + selected leave button) -->
--accent: #3b82f6;

<!-- Status safe (registered leave + 휴가 신청 활성 버튼) -->
--status-safe:     #4ade80;     /* text (light: #166534) */
--status-safe-bar: #22c55e;     /* border (light: #15803d) */
--status-safe-bg:  rgba(34, 197, 94, 0.16);  /* bg (light: #dcfce7) */

<!-- Duty (shift chip in 선택된 날짜) -->
--duty-day:    #f59e0b;
--duty-night:  #ef4444;
--duty-off:    #3b82f6;
--duty-leave:  #6b7280;

<!-- Spacing / radius -->
--modal-padding: 24px (desktop);
--radius-sm: 8px;
--space-3: 12px (desktop spacing scale);

<!-- DOC_LEAVE_GRID structure (StaffServicePage.tsx lines 88~104) -->
Row 1 (3-col): annual=연차 | half_am=오전반차 | half_pm=오후반차
Row 2 (1-col, cols:3 → span 3 = full width): condolence=경조휴가
Row 3 (1-col, cols:3): sick_work=병가(공상)
Row 4 (1-col, cols:3): sick_personal=병가(사상)
Row 5 (1-col, cols:3): health=보건휴가
Row 6 (3-col): official=공가 | official_half_am=오전공가 | official_half_pm=오후공가
Row 7 (1-col, cols:3): other_special=기타특별휴가

ANNUAL_TYPES = {'annual', 'half_am', 'half_pm'}  → 사유 textarea hidden
docLeaveType === 'other_special' → 기타특별휴가 종류 input shown

<!-- CTA copy (UI-SPEC §9.2 verbatim) -->
- 휴가 신청 (data submit)
- PDF 다운로드 (desktop short label — mobile BottomSheet uses "휴가신청서 다운로드")
- 인쇄
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build 07-desktop-form-sketch.html with 6-viewport state matrix</name>
  <files>cha-bio-safety/docs/redesign-context/12-staff-service/sketch/07-desktop-form-sketch.html</files>

  <action>
Create a single static HTML5 file mirroring the structural shell of
`sketch/06-desktop-3panel-sketch.html` (page background, frame border,
caption styling, dark default + .light-frame override pattern). Do NOT
copy 06's 3-panel layout — W7 isolates ONLY the 280px center panel for
visual clarity; the left calendar / right sidebar are out of scope here.

**Page frame:**
- body bg `#0f1419` (matches 06), body text `#e6edf3`, padding 24
- caption above each viewport: `text-caption` 12 / color `#7d8a9c`,
  e.g. "VP1 · 초기 (빈 폼)", "VP2 · 연차 선택", etc.
- 6 viewport frames arranged in a horizontal/wrapping flex grid
- Each frame:
  - width 280px (fixed) × auto height
  - border 1px `#2a2f3a` (dark) / `#d0d7de` (light, on `.light-frame`)
  - radius `--radius-sm` 8px
  - overflow hidden
  - bg `var(--surface)` from :root (panel surface, same as W6 center column)

**Embed tokens.css :root block VERBATIM** (dark theme + light theme override
via `.light-frame :root`-equivalent scoping). Use the W4/W5/W6 pattern: a
single `<style>` block at top with `:root { ...dark... }` and
`.light-frame { ...light overrides... }`. **Do NOT use alias tokens**
(`--bg3`, `--t1`, `--t2`, `--t3`, `--acl`, `--safe`, `--c-day`, etc.).
Use the canonical v0.1.1 names only: `--surface`, `--surface-sunken`,
`--text-primary`, `--text-secondary`, `--text-tertiary`,
`--text-on-accent`, `--border-default`, `--accent`, `--status-safe`,
`--status-safe-bar`, `--status-safe-bg`, `--duty-day`, `--duty-night`,
`--duty-off`, `--duty-leave`, `--radius-sm`, `--modal-padding`.

**Typography classes** (inline `<style>` matching typography.css):
- `.text-title` → 18 / 700 / lh 1.2 (W7 grew title from source 14 → 18)
- `.text-body` → 16 / 400 / lh 1.5
- `.text-body-sm` → 14 / 400 / lh 1.5
- `.text-caption` → 12 / 400 / lh 1.5
- Use `leading-none` (`line-height: 1`) inline on labels/CTAs inside
  small containers per `feedback_text_caption_leading_none.md`.

**Center-panel container (per VP, locked decision A):**
- width 280, flex-shrink 0
- display flex, flex-direction column, gap 12 (`--space-3` desktop)
- padding `var(--modal-padding)` (24)
- border-right 1px `--border-default` (visually hint at 3-panel context)
- overflow-y visible (no scrolling needed in sketch)

**Form component stack (verbatim from locked decisions B-1 through B-10):**

**B-1 Title** (every VP):
- text "휴가신청서", `.text-title` 18 / font-weight 700 /
  color `--text-primary` / `line-height: 1`

**B-2 Selected date row** (VP2~VP6; HIDE in VP1):
- container: bg `--surface-sunken`, radius 8, padding `6px 10px`,
  display flex, align-items center, gap 8
- date label: `text-body-sm` 14 / 700 / `--text-primary` / leading-none.
  Sample text per VP:
  - VP2: `5/18 (월)`  shift=주간   → bg `--duty-day`,  label "주간"
  - VP3: `5/18 (월)`  shift=주간   → bg `--duty-day`,  label "주간"
  - VP4: `5/19 (화)`  shift=당직   → bg `--duty-night`, label "당직"
  - VP5: `5/20 (수)`  shift=비번   → bg `--duty-off`,  label "비번"
  - VP6: `5/18 (월)`  shift=주간   → bg `--duty-day`,  label "주간"
- shift chip: `text-caption` 12 / 700 / color `--text-on-accent` /
  bg from duty token / radius 4 / padding `2px 6px` / leading-none.

**B-3 Date-range row:**
- label row: flex align-items baseline gap 6
  - "휴가기간" `.text-caption` 12 / 600 / `--text-secondary`
  - inline help "(달력에서 클릭)" `.text-caption` 12 / 400 / `--text-tertiary`
- input row: display flex, gap 4, align-items center
  - start input: flex 1, padding `8px 10px`, radius 8,
    border 1px `--border-default`, bg `--surface-sunken`,
    color `--text-primary`, `.text-body-sm` 14
    - VP1: placeholder "달력에서 선택", color `--text-tertiary`
    - VP2: value "2026-05-18"
    - VP3: value "2026-05-18"
    - VP4: value "2026-05-19"
    - VP5: value "2026-05-20"
    - VP6: value "2026-05-18"
  - "~" separator: color `--text-tertiary`, 13px
  - end input: same spec
    - VP1: empty placeholder
    - VP2: "2026-05-18"
    - VP3: "2026-05-20"  (3-day span for sample)
    - VP4: "2026-05-19"
    - VP5: "2026-05-20"
    - VP6: "2026-05-20"
- days display (HIDE in VP1; SHOW elsewhere):
  - `.text-body-sm` 14 / 700 / color `#facc15` (raw hex — only allowed
    exception per token contract) / text-align right / margin-top 4 /
    leading-none.
  - VP2: "1일간"
  - VP3: "3일간"
  - VP4: "1일간"
  - VP5: "1일간"
  - VP6: "3일간"

**B-4 DOC_LEAVE_GRID 7-row button grid** (every VP):
- outer container: display flex flex-direction column gap 4
- each row:
  - if row.length === 1 AND item.cols === 3:
    `display: grid; grid-template-columns: 1fr; gap: 4`
  - else (3-col rows):
    `display: grid; grid-template-columns: repeat(3, 1fr); gap: 4`
- button common: padding `8px 4px`, radius `--radius-sm`,
  `.text-caption` 12 / 600 / leading-none, text-align center,
  cursor pointer, transition none.
- 3 visual states (use inline style classes, e.g. `.btn-normal`,
  `.btn-selected`, `.btn-registered`):
  - **normal**: bg `--surface-sunken` / color `--text-secondary` /
    border 1px `--border-default`
  - **selected**: bg `--accent` / color `--text-on-accent` /
    border 1px `--accent`
  - **registered**: bg `--status-safe-bg` / color `--status-safe` /
    border 2px `--status-safe-bar` / label suffix " ✓"

**Render the 11 labels in 7 rows verbatim** (use VP-specific state map below):
```
Row 1 (3-col): 연차          | 오전반차      | 오후반차
Row 2 (1-col): 경조휴가     (full-width)
Row 3 (1-col): 병가(공상)
Row 4 (1-col): 병가(사상)
Row 5 (1-col): 보건휴가
Row 6 (3-col): 공가          | 오전공가      | 오후공가
Row 7 (1-col): 기타특별휴가
```

State assignment per VP:
- **VP1** (초기): ALL 11 buttons → normal
- **VP2** (연차 선택): 연차 → selected; rest → normal
- **VP3** (경조 선택): 경조휴가 → selected; rest → normal
- **VP4** (기타특별 선택): 기타특별휴가 → selected; rest → normal
- **VP5** (registered 연차): 연차 → registered (with " ✓" suffix in label
  text); 오전반차 → selected (user picking a different one);
  rest → normal. This single VP demonstrates registered + selected
  + normal simultaneously.
- **VP6** (라이트, VP3 mirror): 경조휴가 → selected; rest → normal.
  Apply `.light-frame` class on this frame's outer container so the
  light-theme overrides cascade.

**B-5 Reason textarea** (SHOW only in VP3, VP4, VP6; HIDE in VP1, VP2, VP5):
- label "사유" `.text-caption` 12 / 600 / `--text-secondary` / leading-none
- textarea: width 100%, min-height 56, padding `8px 10px`, radius 8,
  border 1px `--border-default`, bg `--surface-sunken`,
  color `--text-primary`, `.text-body-sm` 14, resize vertical.
  Placeholder: "사유를 입력하세요"
  - VP3: empty (placeholder showing)
  - VP4: sample text "가족돌봄 — 자녀 어린이집 휴원"
  - VP6: empty

**B-6 기타특별휴가 종류 input** (SHOW only in VP4):
- label "기타특별휴가 종류" `.text-caption` 12 / 600 / `--text-secondary` / leading-none
- input: width 100%, padding `8px 10px`, radius 8,
  border 1px `--border-default`, bg `--surface-sunken`,
  color `--text-primary`, `.text-body-sm` 14.
  Placeholder: "예: 가족돌봄, 난임치료 등"
  - VP4: value "가족돌봄"

**B-7 휴가 신청 button** (every VP):
- width 100%, padding `14px 0`, radius 8, border none,
  `.text-body` 16 / 700 / leading-none, cursor pointer
- 2 states:
  - active (VP2, VP3, VP4, VP5, VP6 — selCell + leave selected):
    bg `--status-safe` / color `--text-on-accent`
  - disabled (VP1): bg `--surface-sunken` / color `--text-tertiary`
- label: `휴가 신청`

**B-8 Flex spacer:** `<div style="flex: 1; min-height: 8"></div>`
between B-7 and B-9 (matches source line 1200 pattern; sketch
gives it min-height so spacing is visible without scrolling).

**B-9 PDF 다운로드 button** (every VP):
- width 100%, padding `14px 0`, radius 8, border none,
  `.text-body` 16 / 700 / leading-none, cursor pointer
- bg `--accent` / color `--text-on-accent`
- label: `PDF 다운로드`

**B-10 인쇄 button** (every VP):
- width 100%, padding `14px 0`, radius 8,
  border 1px `--border-default`, `.text-body` 16 / 700 / leading-none,
  cursor pointer
- bg `--surface-sunken` / color `--text-secondary`
- label: `인쇄`

**Frame caption metadata:**
Under each frame, render a small `.text-caption` 12 / `#7d8a9c` line
naming the VP and its state, e.g.:
- "VP1 · 초기 — 빈 폼, 신청 비활성"
- "VP2 · 연차 선택 — 사유 hidden (ANNUAL_TYPES)"
- "VP3 · 경조 선택 — 사유 textarea 표시"
- "VP4 · 기타특별 선택 — 사유 + 종류 input 동시 표시"
- "VP5 · 연차 registered — ✓ + 신규 선택(오전반차) 공존"
- "VP6 · 라이트 — VP3 미러"

**Negative rules — these MUST NOT appear (grep gates enforce):**
- NO 9px, 10px, 11px font sizes anywhere
- NO emoji (✅, 🔥, 🚨, 🟢, etc.) — the registered " ✓" check mark
  is the U+2713 character which is NOT classified as emoji by the
  grep pattern (project precedent: W5 uses it). Verify gate below
  treats checkmark as allowed.
- NO `status-fire` / `text-fire` / `bg-fire` references
- NO alias tokens: `--bg3`, `--t1`, `--t2`, `--t3`, `--acl`, `--safe`,
  `--bd`, `--c-day`, `--c-night`, `--c-off`, `--c-leave`
- NO OLD categorical hex in body markup outside the :root token blocks
  and the three documented exceptions (`#facc15` yellow for days
  display, `#2a2f3a` dark frame border, `#d0d7de` light frame border,
  `#0f1419` body bg, `#e6edf3` body text, `#7d8a9c` caption color —
  these are page-chrome only, not component tokens)

**Positive gates — these MUST appear:**
- 6 frame containers (one per VP)
- 1 instance of `.light-frame` class (VP6)
- All 11 leave-type labels rendered in their 7-row structure:
  연차, 오전반차, 오후반차, 경조휴가, 병가(공상), 병가(사상),
  보건휴가, 공가, 오전공가, 오후공가, 기타특별휴가
- 3 button state class names referenced
  (`btn-normal`, `btn-selected`, `btn-registered`)
- CTA text exactly 3 times each: `휴가 신청`, `PDF 다운로드`, `인쇄`
  (once per VP × 6 = 6 of each — minimum 3 enforced by grep)
- All 4 duty tokens used (one per shift chip variant across VPs):
  `--duty-day`, `--duty-night`, `--duty-off` (VP5), plus
  `--duty-leave` reserved for a comment-only mention if needed.
  At minimum 3 must appear in actual rendered chips
  (VP2/VP3/VP6 = day, VP4 = night, VP5 = off).

Implementation notes:
- Write inline styles where simpler; use `<style>` block for repeated
  patterns (.text-*, .btn-*, .vp-frame, .light-frame).
- Frame width 280 fixed; total page width allows 2-3 frames per row
  (use flex-wrap and gap 32).
- Use semantic HTML where reasonable (`<button>`, `<input>`, `<textarea>`,
  `<label>`) but keep all inputs `readonly` or non-functional —
  this is a static visual sketch.
- File should be self-contained (no external CSS, no JS).
  </action>

  <verify>
    <automated>
SKETCH=/Users/jykevin/Documents/cbc7119-design/cha-bio-safety/docs/redesign-context/12-staff-service/sketch/07-desktop-form-sketch.html
test -f "$SKETCH" || { echo "FAIL: file missing"; exit 1; }
# HTML5 doctype
grep -q '<!DOCTYPE html>' "$SKETCH" || { echo "FAIL: missing doctype"; exit 1; }
# Body-content negative gates (exclude :root token block + chrome-allowlist)
BODY=$(sed -n '/<body/,$p' "$SKETCH")
# 9/10/11px font sizes — disallowed
echo "$BODY" | grep -E 'font-size:\s*(9|10|11)px' && { echo "FAIL: 9/10/11px font found"; exit 1; }
# Emoji — disallowed (allow ✓ U+2713 explicitly)
echo "$BODY" | grep -E '[🔥✅🚨🟢🟡🔴⚠️🛑]' && { echo "FAIL: emoji found"; exit 1; }
# status-fire / text-fire / bg-fire — disallowed
grep -E '(status-fire|text-fire|bg-fire)' "$SKETCH" && { echo "FAIL: fire token found"; exit 1; }
# Alias tokens — disallowed
grep -E '(--bg3|--t1|--t2|--t3|--acl|--safe|--bd|--c-day|--c-night|--c-off|--c-leave)\b' "$SKETCH" && { echo "FAIL: alias token found"; exit 1; }
# Positive: 11 leave-type labels
for LABEL in '연차' '오전반차' '오후반차' '경조휴가' '병가(공상)' '병가(사상)' '보건휴가' '공가' '오전공가' '오후공가' '기타특별휴가'; do
  grep -F "$LABEL" "$SKETCH" >/dev/null || { echo "FAIL: missing label $LABEL"; exit 1; }
done
# Positive: CTA labels appear at least 3 times each
for CTA in '휴가 신청' 'PDF 다운로드' '인쇄'; do
  COUNT=$(grep -F -o "$CTA" "$SKETCH" | wc -l | tr -d ' ')
  [ "$COUNT" -ge 3 ] || { echo "FAIL: CTA '$CTA' count=$COUNT < 3"; exit 1; }
done
# Positive: 6 viewport frames (count vp-frame class)
VP_COUNT=$(grep -o 'class="[^"]*vp-frame' "$SKETCH" | wc -l | tr -d ' ')
[ "$VP_COUNT" -ge 6 ] || { echo "FAIL: only $VP_COUNT vp-frames (need 6)"; exit 1; }
# Positive: light-frame present (VP6)
grep -q 'light-frame' "$SKETCH" || { echo "FAIL: no light-frame"; exit 1; }
# Positive: 3 button state classes
for STATE in 'btn-normal' 'btn-selected' 'btn-registered'; do
  grep -q "$STATE" "$SKETCH" || { echo "FAIL: missing state class $STATE"; exit 1; }
done
# Positive: duty tokens (at least 3 of the 4 actually used in rendered chips)
DUTY_USED=0
for DUTY in '--duty-day' '--duty-night' '--duty-off' '--duty-leave'; do
  grep -q "$DUTY" "$SKETCH" && DUTY_USED=$((DUTY_USED+1))
done
[ "$DUTY_USED" -ge 3 ] || { echo "FAIL: only $DUTY_USED duty tokens used"; exit 1; }
# Positive: status-safe family appears
grep -q -- '--status-safe' "$SKETCH" || { echo "FAIL: --status-safe missing"; exit 1; }
# Positive: accent token used
grep -q -- '--accent' "$SKETCH" || { echo "FAIL: --accent missing"; exit 1; }
echo "OK: 07-desktop-form-sketch.html passes all grep gates"
    </automated>
  </verify>

  <done>
- File `cha-bio-safety/docs/redesign-context/12-staff-service/sketch/07-desktop-form-sketch.html` exists
- All grep gates pass (negative: 9/10/11px, emoji, fire, alias = 0; positive: 11 labels, 3 CTAs ×3+, 6 VPs, light-frame, 3 states, ≥3 duty tokens, accent, status-safe)
- Visual inspection in browser shows 6 frames in a wrapping grid, each 280px wide, displaying the desktop center-panel form with the documented state matrix
- DOC_LEAVE_GRID 7-row branching (1-col vs 3-col) is visually clear
- 3 button states (normal, selected, registered) are each visible somewhere in the matrix (VP5 shows all 3 simultaneously)
- VP6 renders in light theme via `.light-frame` class
  </done>
</task>

</tasks>

<verification>
After Task 1 completes, run the embedded grep gate script. All gates must
return OK without any FAIL output. Then open the file in a browser to
visually confirm:
1. 6 frames render in a wrapping flex grid
2. Each frame is 280px wide
3. Title "휴가신청서" appears at top of each frame
4. Selected-date row hidden in VP1, shown in VP2~VP6 with correct shift chips
5. Date inputs render with VP-specific values
6. Days display "N일간" in yellow (#facc15) where applicable
7. 7-row DOC_LEAVE_GRID with 11 labels visible
8. 3 button states distinguishable: normal (gray), selected (blue accent), registered (green outline + ✓)
9. Reason textarea visible in VP3/VP4/VP6 only
10. 기타특별휴가 종류 input visible in VP4 only
11. 휴가 신청 button green/active in VP2~VP6, gray/disabled in VP1
12. PDF 다운로드 (blue accent) and 인쇄 (sunken) buttons in every frame
13. VP6 renders in light theme (lighter background, different border)
</verification>

<success_criteria>
- 07-desktop-form-sketch.html committed alongside W1~W6 in /sketch/
- Grep verification script passes 100%
- Sketch visually demonstrates the desktop form layout + 3-button-state matrix + conditional reason/종류 panels
- Token contract: only v0.1.1 canonical tokens (NO alias), raw hex limited to documented chrome-allowlist + #facc15 yellow
- Ready for user review → TSX conversion wave (future) can pull verbatim CSS from this sketch
</success_criteria>

<output>
After completion, the file path is:
`cha-bio-safety/docs/redesign-context/12-staff-service/sketch/07-desktop-form-sketch.html`

No SUMMARY.md needed (single-task quick mode sketch wave — matches W1~W6 pattern).
</output>
