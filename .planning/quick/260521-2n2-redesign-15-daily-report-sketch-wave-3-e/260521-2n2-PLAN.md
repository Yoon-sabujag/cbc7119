---
phase: quick-260521-2n2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cha-bio-safety/docs/redesign-context/15-daily-report/sketch-wave-3-editable-cards-personnel.html
autonomous: true
requirements:
  - REDESIGN-15-W3
must_haves:
  truths:
    - "단일 sketch HTML 1개가 다크/라이트 393px 2 frame 가로 나란히로 렌더된다 (data-theme 분기 = data-theme=dark + data-theme=light 두 .frame 컨테이너)"
    - "각 frame 안 카드 4장이 stack 순서 verbatim 으로 박제된다 — 금일업무 EditableCard(rows=10) → 명일업무 EditableCard(rows=5) → 특이사항 EditableCard(rows=4, placeholder='오늘 특이사항을 입력하세요') → 인원현황 SummaryCard"
    - "EditableCard 의 컴포넌트 시그니처 9개 prop (label/field/value/onChange/onSave/onReset/saving/rows/placeholder) 이 sketch 헤더 주석 안에 verbatim 명시된다"
    - "fontSize 9·10·11 px 사용 0건 — design-system §1.1 노안 친화 절대 룰. (라벨 13→16 / smallBtn 10→12 / textarea 12→14~16 / 인원현황 11→14 일괄 상향)"
    - "신규 CSS class ≥10건 sketch 본문 안에 size/padding/radius/font 명시 — .editable-card / .editable-card-head / .editable-card-label / .editable-card-actions / .editable-card-btn--reset / .editable-card-btn--save / .editable-card-textarea / .summary-card / .summary-card-label / .summary-card-body (W7 변환 wave 에서 components.css 추가 시 grep verbatim 인용 가능)"
    - "메모리 룰 verbatim slug ≥7건 sketch 헤더 주석 안에 박제 (feedback_design_sketch_first / feedback_redesign_sketch_rule_enforcement / feedback_sketch_realistic_data / feedback_planner_prompt_sketch_verbatim / feedback_tailwind_token_class_pattern / feedback_tailwind_w8_h8_is_48px / feedback_text_caption_leading_none / feedback_tsx_wave_emoji_dot_gap / feedback_tsx_wave_stat_card_drift / feedback_avoid_premature_confirmation 10건 + 보너스 feedback_cbc7119_design_never_wrangler)"
    - "인원현황 카드 가운뎃점 `·` 구분자가 모두 <span class=\"dot-meta\"></span> 4×4 회색 dot span 으로 치환된다 (W3 데이터 줄 안 dot 6건 이상 — 현재원·비번·연차·반차·교육훈련·주간근무자·당직근무자 사이 = 최소 6 dot)"
    - "인원현황 카드 본문에 3 상태 분기가 모두 박제된다 — 로딩(blink skeleton bar) / 에러('데이터 불러오기 실패 — 다시 시도해 주세요') / 데이터 없음('해당 날짜 데이터 없음') 모두 verbatim"
    - "sketch 안에 §6.1 미적용 + §6.2 미적용 주석이 명시되어 W7 변환 executor 가 Stat Card / Progress drift 를 잡지 않도록 박제됨 (memory feedback_tsx_wave_stat_card_drift)"
    - "sketch 안에 W3 OQ ≥3건 + 각 default 답 1줄 박제 — W4 진입 전 사용자 컨펌용"
    - "자체 verify gate ≥8건 sketch 안에 grep 명령 + 기대값 명시 (메모리 룰 unique ≥7 / 신규 class ≥10 / fontSize 9·10·11 0건 / 이모지 0건 / status- prefix 0건 / w-8 w-h-8 0건 / 카드 4장 × 2 frame = 8건 / dot-meta span ≥6건)"
  artifacts:
    - path: "cha-bio-safety/docs/redesign-context/15-daily-report/sketch-wave-3-editable-cards-personnel.html"
      provides: "redesign/15-daily-report W3 sketch — EditableCard 3종 + 인원현황 카드 (다크/라이트 393px × 2 frame)"
      contains: "data-theme=dark frame + data-theme=light frame, 4 cards × 2 = 8 카드 박제, ≥10 신규 class 정의, ≥7 메모리 룰 slug, ≥3 OQ + default, ≥8 verify gate"
  key_links:
    - from: "sketch-wave-3-editable-cards-personnel.html"
      to: "DailyReportPage.tsx line 270~318 (formContent EditableCard × 3 + 인원현황) + line 810~840 (EditableCard 컴포넌트) + line 43~58 (card/textareaStyle/smallBtn 상수)"
      via: "verbatim 인용 (라벨/버튼 카피/rows prop/placeholder/3 상태 분기/시그니처 9 prop)"
      pattern: "label=\"금일업무\".*rows=\\{10\\}"
    - from: "sketch-wave-3-editable-cards-personnel.html"
      to: "sketch-wave-2-mobile-header-date-nav.html"
      via: "frame 셋업 패턴 1:1 mirror (data-theme 분기, tokens.css/typography.css link, 393px width frame, 헤더 minimal context 표시)"
      pattern: "data-theme=\"(dark|light)\""
    - from: "sketch-wave-3-editable-cards-personnel.html"
      to: "wave-1-index.md §2.1 W3 + §3.1 §3.4 §3.5 + §5 메모리 룰 + §7 OQ #2"
      via: "헤더 주석 안 verbatim 인용 (보존 항목 / 폰트 마이그레이션 / 메모리 룰 slug / §6.1 §6.2 미적용 / OQ #2 default (c))"
      pattern: "wave-1-index\\.md"
---

<objective>
redesign/15-daily-report sketch wave 3 — EditableCard 3종 (금일업무/명일업무/특이사항) + 인원현황 카드 sketch HTML 1개 작성.

다크/라이트 모드 393px frame 2개를 가로 나란히 배치하고, 각 frame 안에 카드 4장을 stack 순서로 박제한다.
DailyReportPage.tsx line 270~318 (formContent) + line 810~840 (EditableCard 컴포넌트) + line 43~58 (card/textareaStyle/smallBtn 상수) 의 모든 코드 룰(라벨/카피/rows prop/placeholder/3 상태 분기/시그니처 9 prop)을 verbatim 유지하고, 시각 디자인만 새로 그린다.

Purpose:
- design-system v0.1.1 §1.1 노안 친화 절대 룰 적용 — 9·10·11px 0건 (라벨 13→16, smallBtn 10→12, textarea 12→14~16, 인원현황 11→14)
- §6.1 Progress / §6.2 Stat Card 미적용 명시 박제 (memory feedback_tsx_wave_stat_card_drift drift 방지)
- 인원현황 카드 가운뎃점 `·` → <span class="dot-meta" /> 4×4 회색 dot span 치환 (memory feedback_tsx_wave_emoji_dot_gap)
- W7 TSX 변환 wave 가 grep 으로 verbatim 인용할 수 있도록 신규 class 10건의 size/padding/radius/font 명시 (memory feedback_planner_prompt_sketch_verbatim)
- W3 OQ ≥3건 default 답 박제 — W4 진입 전 사용자 컨펌용

Output: cha-bio-safety/docs/redesign-context/15-daily-report/sketch-wave-3-editable-cards-personnel.html (신규 1개)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@CLAUDE.local.md
@cha-bio-safety/docs/redesign-context/15-daily-report/wave-1-index.md
@cha-bio-safety/docs/redesign-context/15-daily-report/design-system.md
@cha-bio-safety/docs/redesign-context/15-daily-report/tokens.css
@cha-bio-safety/docs/redesign-context/15-daily-report/typography.css
@cha-bio-safety/docs/redesign-context/15-daily-report/sketch-wave-2-mobile-header-date-nav.html
@cha-bio-safety/src/pages/DailyReportPage.tsx
@cha-bio-safety/src/styles/components.css

<source_verbatim>
<!-- DailyReportPage.tsx line 43~58 (스타일 상수) -->
const card: React.CSSProperties = {
  background: 'var(--bg2)', borderRadius: 14, border: '1px solid var(--bd)',
  padding: 14, marginBottom: 10,
}
const textareaStyle: React.CSSProperties = {
  width: '100%', boxSizing: 'border-box',
  background: 'var(--bg3)', border: '1px solid var(--bd)', borderRadius: 9,
  color: 'var(--t1)', fontSize: 12, fontFamily: 'inherit', fontWeight: 400,
  padding: '10px 12px', resize: 'vertical', outline: 'none', lineHeight: 1.6,
}
const smallBtn: React.CSSProperties = {
  fontSize: 10, fontWeight: 700, padding: '4px 10px', borderRadius: 6,
  border: '1px solid var(--bd)', cursor: 'pointer',
}

<!-- DailyReportPage.tsx line 273~296 (EditableCard 3종 호출) -->
<EditableCard label="금일업무" field="today" ... rows={10} />
<EditableCard label="명일업무" field="tomorrow" ... rows={5} />
<EditableCard label="특이사항" field="notes" ... rows={4} placeholder="오늘 특이사항을 입력하세요" />

<!-- DailyReportPage.tsx line 298~318 (인원현황 카드 — 3 상태 분기 verbatim) -->
<div style={card}>
  <div style={{ fontSize: 13, fontWeight: 700, color: 'var(--t1)', marginBottom: 6 }}>인원현황</div>
  {queryData.isLoading ? (
    <div style={{ background: 'var(--bg4)', borderRadius: 4, height: 12, width: '70%', animation: 'blink 2s ease-in-out infinite' }}/>
  ) : queryData.isError ? (
    <div style={{ fontSize: 11, color: 'var(--danger)' }}>데이터 불러오기 실패 — 다시 시도해 주세요</div>
  ) : preview ? (
    <div style={{ fontSize: 11, color: 'var(--t2)', lineHeight: 1.6 }}>
      총원 {preview.personnel.total} · 현재원 {preview.personnel.present}
      {preview.personnel.offDuty ? ` · 비번 ${preview.personnel.offDuty}` : ''}
      {preview.personnel.onLeave.length > 0 ? ` · 연차 ${preview.personnel.onLeave.join(', ')}` : ''}
      {preview.personnel.halfLeave.length > 0 ? ` · 반차 ${preview.personnel.halfLeave.join(', ')}` : ''}
      {preview.personnel.training.length > 0 ? ` · 교육/훈련 ${preview.personnel.training.join(', ')}` : ''}
      {preview.personnel.dayShift.length > 0 ? ` · 주간근무자 ${preview.personnel.dayShift.join(', ')}` : ''}
      {preview.personnel.onDuty ? ` · 당직근무자 ${preview.personnel.onDuty}` : ''}
    </div>
  ) : (
    <div style={{ fontSize: 11, color: 'var(--t3)' }}>해당 날짜 데이터 없음</div>
  )}
</div>

<!-- DailyReportPage.tsx line 810~840 (EditableCard 컴포넌트 — 9 prop 시그니처) -->
function EditableCard({ label, field, value, onChange, onSave, onReset, saving, rows, placeholder }: {
  label: string; field: string; value: string
  onChange: (v: string) => void; onSave: (f: string) => void; onReset: (f: string) => void
  saving?: boolean; rows: number; placeholder?: string
}) {
  return (
    <div style={card}>
      <div style={{ display: 'flex', alignItems: 'center', marginBottom: 8 }}>
        <div style={{ fontSize: 13, fontWeight: 700, color: 'var(--t1)', flex: 1 }}>{label}</div>
        <button onClick={() => onReset(field)}
          style={{ ...smallBtn, background: 'var(--bg3)', color: 'var(--t3)', marginRight: 6 }}>
          초기화
        </button>
        <button onClick={() => onSave(field)} disabled={saving}
          style={{ ...smallBtn, background: 'var(--bg4)', color: saving ? 'var(--t3)' : 'var(--t2)' }}>
          {saving ? '저장 중...' : '저장'}
        </button>
      </div>
      <textarea ... onFocus border='var(--bd2)' onBlur border='var(--bd)' />
    </div>
  )
}
</source_verbatim>

<frame_pattern>
<!-- sketch-wave-2-mobile-header-date-nav.html 패턴 (1:1 mirror) -->
- <html lang="ko" data-theme="dark"> 다크 기본
- pretendard CDN (link) + tailwind CDN (script)
- :root / [data-theme="dark"] / [data-theme="light"] 토큰 분기 — tokens.css verbatim 인라인
- typography.css 토큰 인라인 또는 link (Pretendard + JetBrains Mono)
- body { display: flex; gap: 24px; padding: 24px; background: #1a1a1a; }
- .frame { width: 393px; min-height: 자동; border: 1px solid #444; border-radius: 24px; overflow: hidden; }
- .frame[data-theme="dark"] / .frame[data-theme="light"] 분기로 토큰 override
- 헤더 .page-header (14-reports inherit) — 본 W3 는 컨텍스트로 minimal 표시 (back-btn + page-title "일일 업무 일지" + dateNav 1줄)
- 본문 .page-body (padding 12 16) — 본 W3 는 본문 카드 stack 만 강조
- semantic 토큰만 사용 (var(--text-primary) / var(--surface-raised) / var(--border-default))
- lucide 인라인 SVG 본 W3 미사용 (카피만, 아이콘 없음)
</frame_pattern>
</context>

<tasks>

<task type="auto">
  <name>Task 1: sketch-wave-3-editable-cards-personnel.html 생성 (다크/라이트 393px × 2 frame, 카드 4장, 메모리 룰 ≥7건, 신규 class ≥10건)</name>
  <files>cha-bio-safety/docs/redesign-context/15-daily-report/sketch-wave-3-editable-cards-personnel.html</files>
  <action>
신규 sketch HTML 1개 작성. sketch-wave-2-mobile-header-date-nav.html 의 frame 셋업 패턴을 1:1 mirror 하되, 본 wave 의 scope 는 본문 카드 4장.

**HTML 구조 (verbatim 박제 필수):**

1. `<!DOCTYPE html><html lang="ko" data-theme="dark">` 다크 기본
2. `<head>` 안:
   - title: "sketch-wave-3-editable-cards-personnel — redesign/15-daily-report W3"
   - Pretendard CDN link + JetBrains Mono link
   - tailwind CDN script (또는 sketch-wave-2 와 동일 패턴)
   - tokens.css 인라인 (또는 link) — :root / [data-theme="dark"] / [data-theme="light"] 분기 verbatim
   - typography.css 인라인 (또는 link)
3. `<head>` 안 `<style>` 블록 — 신규 class 10건 size/padding/radius/font 명시 (아래 신규 class 사양표 참조)
4. `<body>` 안:
   - body { display: flex; gap: 24px; padding: 24px; background: #1a1a1a; }
   - `<div class="frame" data-theme="dark">` (393px width)
     - .page-header (14-reports inherit, minimal context — back-btn + .page-title "일일 업무 일지" + .date-nav 1줄)
     - .page-body (padding 12 16) — 카드 stack 4장 (아래 카드 사양 참조)
   - `<div class="frame" data-theme="light">` (393px width)
     - 위 frame 내용 1:1 복제 — data-theme override 로 토큰만 다름

**카드 사양 (각 frame 안 stack 순서 verbatim):**

**카드 1 — 금일업무 EditableCard (rows=10)**
- `<div class="editable-card">`
  - `<div class="editable-card-head">`
    - `<div class="editable-card-label">금일업무</div>`
    - `<div class="editable-card-actions">`
      - `<button class="editable-card-btn--reset">초기화</button>`
      - `<button class="editable-card-btn--save">저장</button>`
  - `<textarea class="editable-card-textarea" rows="10" placeholder="금일업무 내용이 자동 생성됩니다"></textarea>`

**카드 2 — 명일업무 EditableCard (rows=5)**
- 카드 1 과 동일 마크업, label "명일업무" / rows 5 / placeholder "명일업무 내용이 자동 생성됩니다"

**카드 3 — 특이사항 EditableCard (rows=4, placeholder verbatim)**
- 카드 1 과 동일 마크업, label "특이사항" / rows 4 / placeholder "오늘 특이사항을 입력하세요" (verbatim, 다른 카드와 다름)

**카드 4 — 인원현황 SummaryCard (3 상태 분기 모두 박제)**
- `<div class="summary-card">`
  - `<div class="summary-card-label">인원현황</div>`
  - `<div class="summary-card-body">`
    - **데이터 상태** (default 표시): "총원 4<span class="dot-meta"></span>현재원 3<span class="dot-meta"></span>비번 김재호<span class="dot-meta"></span>연차 이영수<span class="dot-meta"></span>반차 박민지<span class="dot-meta"></span>교육/훈련 정수민<span class="dot-meta"></span>주간근무자 윤종엽<span class="dot-meta"></span>당직근무자 김재호"
    - dot-meta span 4×4 회색 (memory feedback_tsx_wave_emoji_dot_gap) — 최소 6 dot (현재원/비번/연차/반차/교육훈련/주간근무자/당직근무자 사이 = 7 dot 가능)
  - **로딩 상태** (주석으로 박제 + 별도 visible 표시):
    - `<!-- loading state -->` 주석 + `<div class="summary-card-skeleton" /></div>` (blink animation bar)
  - **에러 상태** (주석으로 박제 + 별도 visible 표시):
    - `<!-- error state -->` 주석 + `<div class="summary-card-error">데이터 불러오기 실패 — 다시 시도해 주세요</div>`
  - **데이터 없음 상태** (주석으로 박제 + 별도 visible 표시):
    - `<!-- empty state -->` 주석 + `<div class="summary-card-empty">해당 날짜 데이터 없음</div>`
  - (4 상태 모두 sketch 안에 visible 박제 — frame 내 stack 으로 추가 sub-card 또는 별도 row 로 모두 표시. 변환 wave 가 분기 markup verbatim 인용 가능하도록.)

**신규 CSS class 사양 (10건, sketch <style> 블록 안 verbatim 정의):**

EditableCard 7건:
- `.editable-card` — background: var(--surface-raised); border: 1px solid var(--border-default); border-radius: 14px; padding: 14px; margin-bottom: 10px;
- `.editable-card-head` — display: flex; align-items: center; margin-bottom: 8px;
- `.editable-card-label` — font-size: 16px (text-body, 13→16 상향, design-system §1.1); font-weight: 700; color: var(--text-primary); flex: 1; line-height: 1.5;
- `.editable-card-actions` — display: flex; gap: 6px;
- `.editable-card-btn--reset` — font-size: 12px (text-caption, 10→12 상향, leading-none); font-weight: 700; padding: 4px 10px; border-radius: 6px; border: 1px solid var(--border-default); background: var(--surface-sunken); color: var(--text-tertiary); cursor: pointer; line-height: 1;
- `.editable-card-btn--save` — font-size: 12px (text-caption); font-weight: 700; padding: 4px 10px; border-radius: 6px; border: 1px solid var(--border-default); background: var(--surface-strong); color: var(--text-secondary); cursor: pointer; line-height: 1;
- `.editable-card-textarea` — width: 100%; box-sizing: border-box; background: var(--surface-sunken); border: 1px solid var(--border-default); border-radius: 9px; color: var(--text-primary); font-size: 14px (text-body-sm, 12→14 상향, OQ #1 default 14); font-family: inherit; font-weight: 400; padding: 10px 12px; resize: vertical; outline: none; line-height: 1.6;

SummaryCard 3건:
- `.summary-card` — background: var(--surface-raised); border: 1px solid var(--border-default); border-radius: 14px; padding: 14px; margin-bottom: 10px; (§6.1/§6.2 미적용 — 단순 정보 카드)
- `.summary-card-label` — font-size: 16px (text-body, 13→16 상향); font-weight: 700; color: var(--text-primary); margin-bottom: 6px; line-height: 1.5;
- `.summary-card-body` — font-size: 14px (text-body-sm, 11→14 상향, OQ #2 default 14); color: var(--text-secondary); line-height: 1.6; (dot-meta span 안 .dot-meta 는 14-reports components.css inherit, 4×4 회색)

추가 helper (선택, gate 카운트 미포함):
- `.summary-card-skeleton` (blink animation 12px bar) / `.summary-card-error` (text-status-danger color) / `.summary-card-empty` (text-text-tertiary color)
- `.dot-meta` — 14-reports components.css line 15 inherit (4×4 회색 dot span)

**§6.1 / §6.2 미적용 명시 주석 (sketch <style> 안 또는 .summary-card 바로 위):**
```
/* §6.1 Progress Color Rule 미적용 — 인원현황은 진척률 도넛/카테고리 카드 아님 (raw 정보 줄) */
/* §6.2 Stat Card Number Color 미적용 — 인원현황은 정보 카드, 28px display 숫자 아님 (memory feedback_tsx_wave_stat_card_drift) */
```

**`<head>` 상단 주석 블록 (verbatim 박제, ≥7 메모리 룰 slug, 보존 항목, OQ ≥3 + default):**

주석 구조:
```
<!--
  sketch-wave-3-editable-cards-personnel.html — redesign/15-daily-report W3
  작성: 2026-05-21 / Quick ID 260521-2n2
  Branch: redesign/15-daily-report

  범위: EditableCard × 3 (금일업무 rows=10 / 명일업무 rows=5 / 특이사항 rows=4) +
        인원현황 SummaryCard (3 상태 분기 모두 박제) × 다크/라이트 393px = 2 frame
  out of scope: 모바일 헤더 + dateNav (W2 완결) / 다운로드 액션 (W4) /
                데스크톱 좌우 분할 (W5) / DailyPortraitPreview wrapper (W6) /
                TSX 변환 checklist (W7)

  참조: wave-1-index.md §1.4 / §1.7 / §2.1 W3 / §3.4 / §3.5 / §4.2 / §5 / §7 OQ #2,
        DailyReportPage.tsx line 43~58 (card / textareaStyle / smallBtn 상수) +
                              line 270~318 (formContent EditableCard × 3 + 인원현황) +
                              line 810~840 (EditableCard 컴포넌트) verbatim,
        sketch-wave-2-mobile-header-date-nav.html (frame 셋업 패턴 1:1 mirror),
        components.css line 15 (.dot-meta 4×4 회색 dot inherit)

  W1 LOCKED 5건 적용 매트릭스 (wave-1-index.md §7 OQ default 인용):
    OQ #1 ▶ default OK — 다운로드 버튼 그라데이션 → bg-safe-bar solid (W4 scope, W3 미해당)
    OQ #2 ▶ default (c) — 인원현황 = 단순 정보 카드 (본 W3 적용 = O,
                          `bg-surface-raised border-border-default` 만, §6.1/§6.2 미적용)
    OQ #3 ▶ default 유지 — 미래 날짜 시 › 버튼 spacer 28px (W2 완결, W3 미해당)
    OQ #4 ▶ default (a) — DailyPortraitPreview wrapper 만 (W6 scope, W3 미해당)
    OQ #5 ▶ default (a) — 모바일/데스크톱 분기 유지 (W5 scope, W3 미해당)

  ==========================================================
  보존 항목 (memory feedback_sketch_realistic_data)
  ==========================================================
  - EditableCard 시그니처 9 prop verbatim:
    { label, field, value, onChange, onSave, onReset, saving, rows, placeholder }
  - 라벨 verbatim: "금일업무" / "명일업무" / "특이사항" / "인원현황"
  - 버튼 verbatim: "초기화" / "저장" / "저장 중..."
  - rows prop verbatim: 10 / 5 / 4
  - placeholder verbatim: "오늘 특이사항을 입력하세요" (특이사항 카드만)
  - 인원현황 텍스트 verbatim:
    "총원 {N} · 현재원 {M} · 비번 {X} · 연차 {names} · 반차 {names} ·
     교육/훈련 {names} · 주간근무자 {names} · 당직근무자 {name}"
  - 가운뎃점 `·` → <span class="dot-meta" /> 4×4 회색 dot span 으로 치환
    (memory feedback_tsx_wave_emoji_dot_gap)
  - 3 상태 분기 verbatim:
    로딩 = blink 애니메이션 skeleton bar
    에러 = "데이터 불러오기 실패 — 다시 시도해 주세요"
    데이터 없음 = "해당 날짜 데이터 없음"
  - textarea onFocus / onBlur border 토글 (소스 line 835~836)
    → sketch 에서는 :focus 의사 클래스로 표현 (border-color 변경)

  ==========================================================
  폰트 상향 (design-system §1.1 노안 친화 절대 룰, 9·10·11px 0건)
  ==========================================================
  - EditableCard 라벨 13 → 16 (text-body) — source line 819 fontSize: 13
  - EditableCard 버튼 (초기화/저장) 10 → 12 (text-caption + leading-none) — source line 55
  - EditableCard textarea 12 → 14 (text-body-sm, OQ #1 default) — source line 51
  - 인원현황 라벨 13 → 16 (text-body) — source line 300
  - 인원현황 본문 11 → 14 (text-body-sm, OQ #2 default) — source line 306~316
  - 인원현황 에러/empty 11 → 14 (text-body-sm)
  → 9·10·11px 0건 (자체 verify gate #3)

  ==========================================================
  메모리 룰 박제 (wave-1-index.md §5 verbatim slug, unique ≥7)
  ==========================================================

    1. feedback_design_sketch_first.md
       spacing/sizing 도 sketch 시안 먼저, 인라인 직행 금지.
       → W3 sketch 가 W4 진입 전 카드 패턴 컨펌용. card margin/padding/radius
         모두 sketch 안 .editable-card / .summary-card 정의에 명시.

    2. feedback_redesign_sketch_rule_enforcement.md
       §6.2 negative rule (위험 임계치 아닌 카드 status 색 금지) / §6.3 §7.1 일관성.
       → EditableCard / SummaryCard 모두 정보 카드 → bg-surface-raised /
         border-border-default 만. bg-fire-bar / text-danger 같은 status 색 배경 X.
         단 에러 상태 텍스트는 var(--status-danger) 사용 OK (의미 = 에러).

    3. feedback_sketch_realistic_data.md
       표시 분기/라벨 룰은 코드 그대로, 시각 디자인만 손봄.
       → 라벨/버튼 카피/rows prop/placeholder/3 상태 분기 모두 verbatim.
         카피 임의 변경 시 W7 변환 wave 가 deviation 으로 잡음.

    4. feedback_planner_prompt_sketch_verbatim.md
       sketch CSS 토큰/사이즈는 grep 으로 추출해 W7 변환 wave 에 그대로 인용.
       → 본 sketch 의 .editable-card-* 7 class + .summary-card-* 3 class = 10 정의가
         W7 checklist 안에 verbatim 인용 예정. grep -oE 'class="[^"]+"' 결과 박제.

    5. feedback_tailwind_token_class_pattern.md
       text-fire-bar O / text-status-fire-bar X (status- prefix 없음).
       → 본 sketch 본문 status- prefix class 0건 (verify gate #5).
         semantic 토큰만 사용 (var(--text-primary) / var(--surface-raised) /
         var(--border-default) / var(--status-danger)).

    6. feedback_tailwind_w8_h8_is_48px.md
       tailwind.config spacing override — w-8=48px (기본 32 아님), w-7=32px.
       → 본 sketch 는 CSS 명시 px 만 사용 (padding 14px, margin-bottom 10px 등).
         Tailwind spacing utility (w-N / h-N) 본문 0건 (verify gate #6).

    7. feedback_text_caption_leading_none.md
       text-caption lh:1.5 가 작은 컨테이너 안에서도 시각 패딩 — leading-none 명시.
       → 초기화 / 저장 버튼 fontSize: 12 (text-caption) + line-height: 1 명시.
         라벨 / 인원현황 본문 (text-body / text-body-sm) 은 line-height 1.5~1.6 유지.

    8. feedback_tsx_wave_emoji_dot_gap.md
       sketch negative gate (이모지 0) + dot span 추가 markup 도 verify.
       → 인원현황 본문 가운뎃점 `·` → <span class="dot-meta" /> 6건 이상 치환.
         이모지 / 글리프 (⬇ ⚠ 🎯) 본문 0건. (verify gate #4)

    9. feedback_tsx_wave_stat_card_drift.md
       executor 가 source outline 패턴 보존, sketch 새 패턴 누락 가능.
       → 인원현황 카드는 §6.2 Stat Card (28px display 숫자) 아님. 정보 카드.
         sketch 안 주석으로 §6.1 / §6.2 미적용 명시. W7 변환 시 28px 숫자 패턴
         drift 방지.

   10. feedback_avoid_premature_confirmation.md
       "거의 일치" 자신감 표현 금지. 결과 보여주고 사용자 판단.
       → 본 sketch 완료 후 보고는 "W3 OQ ≥3건 + default 답 컨펌 부탁" 만.
         "W3 완벽 / W4 진입 가능" 같은 자신감 표현 금지.

   (보너스) feedback_cbc7119_design_never_wrangler.md
       디자인 wave 중 wrangler --project-name=cbc7119 절대 X.
       → 본 wave 산출 후 wrangler / npm run deploy 명령 0건. main push 자동
         cbc7119-preview 배포만.

   (unique slug ≥7건 — 1~10 + 보너스 11건. verify gate #1)

  ==========================================================
  W3 OQ ≥3건 (W4 진입 전 사용자 컨펌)
  ==========================================================
    OQ #1: textarea 폰트 사이즈 14 (text-body-sm) vs 16 (text-body) 중?
           ▶ default: 14 — 입력 영역 시각 농도 (사용자 입력 텍스트 = 가독성 + 밀도).
                       16 으로 더 키우면 textarea rows=10 이 viewport 거의 다 차지.
    OQ #2: 인원현황 카드 본문 폰트 사이즈 14 (text-body-sm) vs 16 (text-body) 중?
           ▶ default: 14 — dot 구분 정보 줄. 7건 데이터 한 줄 표시 = 16 이면 줄바꿈
                       다수 발생 → 시각 위계 무너짐. 14 가 7건 dot 인지 + 줄바꿈 균형.
    OQ #3: EditableCard 헤더 안 "초기화" 버튼 — 유지 vs 제거?
           ▶ default: 유지 — 소스 line 820~823 그대로. memory feedback_sketch_realistic_data.
                       기능 (onReset → 자동 생성 텍스트로 복귀) 도 보존 필수.
    OQ #4 (선택): textarea onFocus 시 border 색 변경 효과 — 유지 vs 제거?
           ▶ default: 유지 — 소스 line 835 verbatim. focus 시 var(--bd) → var(--bd2).
                       sketch 에서는 :focus 의사 클래스로 border-color 변경 명시.

  ==========================================================
  자체 verify gate ≥8건 (gate 표 본문 아래 별도 박제)
  ==========================================================
  (gate 본문 표 참조 — 메모리 룰 / 신규 class / 폰트 / 이모지 / status- prefix /
   w-8/h-8 / 카드 4장 × 2 frame / dot-meta span ≥6건 = 8 gate)
-->
```

**`<head>` 하단 자체 verify gate 표 (≥8 gate, sketch 안 주석 또는 visible 박제):**

```
==========================================================
자체 verify gate (≥8건 — 작성 완료 후 통과해야 함)
==========================================================

| # | gate | 검증 명령 | 기대값 |
|---|------|-----------|--------|
| 1 | unique feedback_* slug ≥7 | grep -oE 'feedback_[a-z_]+\.md' sketch-wave-3-*.html | sort -u | wc -l | ≥7 (실제 11) |
| 2 | 신규 class ≥10 정의 | grep -cE '^\.(editable-card|summary-card)' sketch-wave-3-*.html | ≥10 |
| 3 | fontSize 9·10·11px 0건 | grep -cE 'font-size:\s*(9px|10px|11px)' sketch-wave-3-*.html | =0 |
| 4 | 이모지 / ⬇ ⚠ 🎯 글리프 0건 | grep -cE '⬇|⚠|🎯|🚨|⚡|✅|❌' sketch-wave-3-*.html | =0 |
| 5 | status- prefix class 0건 | grep -cE 'class="[^"]*status-' sketch-wave-3-*.html | =0 |
| 6 | tailwind w-8 / h-8 사용 0건 | grep -cE 'class="[^"]*\bw-8\b|class="[^"]*\bh-8\b' sketch-wave-3-*.html | =0 |
| 7 | 카드 4장 × 2 frame = 8건 박제 | grep -c 'class="editable-card"' sketch-wave-3-*.html >=6 AND grep -c 'class="summary-card"' >=2 | total ≥8 |
| 8 | dot-meta span ≥6건 | grep -oE '<span class="dot-meta"' sketch-wave-3-*.html | wc -l | ≥6 (frame 1만 데이터 상태 visible 시 ≥6, 양 frame 데이터 visible 시 ≥12) |

추가 negative gate (gate 카운트 미포함):
- git diff --name-only HEAD -- cha-bio-safety/src 결과 0 lines (src/** 변경 0)
- git diff --name-only HEAD -- cha-bio-safety/src/styles/components.css 결과 0 lines (W7 변환 wave 대기)
- App.tsx 손대지 않음 (변경 0)
- /templates/preview/daily-1.png 변경 0
- wrangler 명령 / npm run deploy 사용 0건

모두 PASS 시 본 sketch 가 W4 진입 자격을 갖춘 것으로 본다.
사용자 컨펌은 §W3 OQ 3~4건 답변으로 받는다.
```

**작성 순서:**
1. `<head>` 의 상단 주석 블록 (메모리 룰 11건 + 보존 항목 + 폰트 상향 + W1 LOCKED 매트릭스 + W3 OQ ≥3건 + verify gate 표) 먼저 작성
2. tokens.css verbatim 인라인 (`:root` / `[data-theme="dark"]` / `[data-theme="light"]`)
3. typography.css 인라인 (Pretendard + JetBrains Mono 변수 정의)
4. `<style>` 블록 — 신규 class 10건 정의 (.editable-card / .editable-card-head / .editable-card-label / .editable-card-actions / .editable-card-btn--reset / .editable-card-btn--save / .editable-card-textarea / .summary-card / .summary-card-label / .summary-card-body) + helper class (.summary-card-skeleton / .summary-card-error / .summary-card-empty / .dot-meta) + §6.1/§6.2 미적용 주석
5. `<body>` 안 2 frame (data-theme=dark + data-theme=light) — 각 frame 에 minimal 헤더 (.page-header + .back-btn + .page-title "일일 업무 일지" + .date-nav 1줄) + .page-body 안 카드 stack 4장 (금일업무 / 명일업무 / 특이사항 / 인원현황 — 3 상태 분기 모두 visible)

**금지 (negative gate, 본문 0건):**
- 이모지 / 글리프 (⬇ ⚠ 🎯 🚨 ⚡ ✅ ❌) 0건
- status- prefix class (text-status-fire-bar / bg-status-safe-bar) 0건
- tailwind w-8 / h-8 사용 0건 (memory feedback_tailwind_w8_h8_is_48px — 32px 필요 시 w-7/h-7 또는 w-[34px])
- font-size: 9px / 10px / 11px 0건 (design-system §1.1)
- wrangler 명령 / npm run deploy 0건 (CLAUDE.local.md)
- src/** 변경 0 (DailyReportPage.tsx 손대지 않음)
- components.css 수정 0 (W7 TSX 변환 wave 대기)
- App.tsx 손대지 않음
- /templates/preview/daily-1.png 변경 0
- 다른 페이지 (13-schedule / 14-reports / 02 / 06 등) 영향 0
  </action>
  <verify>
    <automated>
test -f cha-bio-safety/docs/redesign-context/15-daily-report/sketch-wave-3-editable-cards-personnel.html && \
SLUG=$(grep -oE 'feedback_[a-z_]+\.md' cha-bio-safety/docs/redesign-context/15-daily-report/sketch-wave-3-editable-cards-personnel.html | sort -u | wc -l | tr -d ' ') && \
CLASS_NEW=$(grep -cE '^\.(editable-card|summary-card)' cha-bio-safety/docs/redesign-context/15-daily-report/sketch-wave-3-editable-cards-personnel.html) && \
FONT_BAD=$(grep -cE 'font-size:[[:space:]]*(9px|10px|11px)' cha-bio-safety/docs/redesign-context/15-daily-report/sketch-wave-3-editable-cards-personnel.html) && \
EMOJI=$(grep -cE '⬇|⚠|🎯|🚨|⚡|✅|❌' cha-bio-safety/docs/redesign-context/15-daily-report/sketch-wave-3-editable-cards-personnel.html) && \
STATUS_PREFIX=$(grep -cE 'class="[^"]*status-[a-z]' cha-bio-safety/docs/redesign-context/15-daily-report/sketch-wave-3-editable-cards-personnel.html) && \
W8=$(grep -cE 'class="[^"]*\b(w-8|h-8)\b' cha-bio-safety/docs/redesign-context/15-daily-report/sketch-wave-3-editable-cards-personnel.html) && \
EDITABLE_CARDS=$(grep -c 'class="editable-card"' cha-bio-safety/docs/redesign-context/15-daily-report/sketch-wave-3-editable-cards-personnel.html) && \
SUMMARY_CARDS=$(grep -c 'class="summary-card"' cha-bio-safety/docs/redesign-context/15-daily-report/sketch-wave-3-editable-cards-personnel.html) && \
DOT=$(grep -oE '&lt;span class="dot-meta"|<span class="dot-meta"' cha-bio-safety/docs/redesign-context/15-daily-report/sketch-wave-3-editable-cards-personnel.html | wc -l | tr -d ' ') && \
SRC_DIFF=$(git diff --name-only HEAD -- cha-bio-safety/src cha-bio-safety/src/styles/components.css | wc -l | tr -d ' ') && \
WRANGLER=$(grep -cE '^[[:space:]]*wrangler |npm run deploy' cha-bio-safety/docs/redesign-context/15-daily-report/sketch-wave-3-editable-cards-personnel.html) && \
echo "gate1 slug=$SLUG (≥7), gate2 class=$CLASS_NEW (≥10), gate3 font-bad=$FONT_BAD (=0), gate4 emoji=$EMOJI (=0), gate5 status-prefix=$STATUS_PREFIX (=0), gate6 w8/h8=$W8 (=0), gate7 cards=editable:$EDITABLE_CARDS+summary:$SUMMARY_CARDS (total≥8), gate8 dot-meta=$DOT (≥6), src-diff=$SRC_DIFF (=0), wrangler=$WRANGLER (=0)" && \
[ "$SLUG" -ge 7 ] && [ "$CLASS_NEW" -ge 10 ] && [ "$FONT_BAD" -eq 0 ] && [ "$EMOJI" -eq 0 ] && [ "$STATUS_PREFIX" -eq 0 ] && [ "$W8" -eq 0 ] && [ "$((EDITABLE_CARDS + SUMMARY_CARDS))" -ge 8 ] && [ "$DOT" -ge 6 ] && [ "$SRC_DIFF" -eq 0 ] && [ "$WRANGLER" -eq 0 ]
    </automated>
  </verify>
  <done>
sketch-wave-3-editable-cards-personnel.html 1개 파일이 다음을 모두 만족:
- 다크/라이트 393px 2 frame 가로 나란히 (data-theme=dark + data-theme=light)
- 각 frame 안 카드 4장 (금일업무 EditableCard rows=10 + 명일업무 rows=5 + 특이사항 rows=4 placeholder="오늘 특이사항을 입력하세요" + 인원현황 SummaryCard 3 상태 분기)
- ≥7 unique feedback_* slug 박제 (실제 11건)
- ≥10 신규 class 정의 (.editable-card-* 7 + .summary-card-* 3)
- fontSize 9·10·11px 0건 (라벨 16 / 버튼 12 / textarea 14 / 인원현황 본문 14)
- 이모지 / 글리프 0건 / status- prefix 0건 / w-8 h-8 0건
- dot-meta span ≥6건 (인원현황 본문 가운뎃점 치환)
- §6.1 / §6.2 미적용 주석 박제
- W3 OQ ≥3건 + 각 default 답 1줄
- 자체 verify gate ≥8 + 기대값 명시
- src/** 변경 0 / components.css 변경 0 / App.tsx 변경 0 / wrangler 명령 0
  </done>
</task>

</tasks>

<verification>
- 위 task 1 의 <automated> 명령이 PASS (단일 명령으로 8 gate + 5 negative gate 일괄 검증)
- 시각 확인: 작성 완료 후 사용자에게 "다크/라이트 frame 카드 4장 stack — 폰트 상향 + dot-meta 치환 + 3 상태 분기 OK 확인 부탁" 보고 (memory feedback_avoid_premature_confirmation — "거의 일치" 자신감 표현 금지)
</verification>

<success_criteria>
- sketch-wave-3-editable-cards-personnel.html 단일 파일 생성
- task 1 의 verify <automated> 명령 PASS — 8 verify gate + 5 negative gate 모두 통과
- 사용자가 W3 OQ ≥3건 default 답 컨펌 후 W4 (다운로드 액션) 진입 자격 획득
</success_criteria>

<output>
After completion, create `.planning/quick/260521-2n2-redesign-15-daily-report-sketch-wave-3-e/260521-2n2-SUMMARY.md`
</output>
