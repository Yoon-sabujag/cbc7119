---
quick_id: 260510-4x7
slug: redesign-07-elevator-1-html-6
status: complete
date: 2026-05-10
commit: 3e478d8
files_created:
  - cha-bio-safety/docs/redesign-context/07-elevator/sketch/elevator-sketch.html
files_modified: []
lines_added: 2013
---

# 260510-4x7 — redesign/07-elevator 1차 시안 HTML

## 작업 결과

ElevatorPage.tsx (3209 라인) 재디자인을 위한 **1차 시안 HTML** 단일 파일 생성.

- **파일:** `cha-bio-safety/docs/redesign-context/07-elevator/sketch/elevator-sketch.html`
- **라인 수:** 2013 (예상 1500~3500 범위 내, 01-dashboard 2526 라인과 비슷한 규모)
- **commit:** `3e478d8` — `docs(260510-4x7): elevator redesign 1차 sketch HTML — 4 viewports, container/header/grid/route`

## 시안 구성 (PLAN.md "1차 시안 범위" 그대로)

### 4 viewport (한 페이지에 세로 비교)
1. **📱 모바일 다크 · 375 × 812** — 자체 헤더 + 호기 그리드 + 노선도 + BottomNav
2. **📱 모바일 라이트 · 375 × 812** — 구조 동일, 토큰만 라이트
3. **🖥️ 데스크톱 다크 · 1280 × 800** — 사이드바 + 자체 헤더 + 좌(8/12 호기 그리드)/우(4/12 노선도)
4. **🖥️ 데스크톱 라이트 · 1280 × 800** — 구조 동일, 토큰만 라이트

### 페이지 골격 (포함)
- **자체 헤더** (App.tsx 헤더 숨김 패턴): 6탭 (목록 / 고장 / 수리 / 점검 기록 / 검사 기록 / 안전관리자) + 검색 입력 + 액션 (새로고침 / 미해결 배지)
- **상태 통계 카드 4종** (§6.2 Stat Card 좌측 3px 색바): 정상 11 / 고장 1(danger) / 점검중 2(warn) / 운행중지 3(mute)
- **호기 그리드** — 11호기 엘리베이터 + 6 에스컬레이터, 그룹별 묶음:
  - 투명 (1·2·3호기) — 1·2 정상 / 3 점검중
  - 오렌지 (4·5·6호기) — 4 고장(fire) / 5·6 정상
  - 기타 (7·8호기) — 7 운행중지 / 8 정상
  - 화물 (9·10호기) — 9 정상 / 10 점검중
  - 덤웨이터 (11호기) — 정상
  - 에스컬레이터 (E1~E6) — 노선도로 별도 표시
- **에스컬레이터 노선도** — `ES_NODES_ANNUAL` 데이터 모방. 좌/우 호기 + 층 라벨 (3층→2층→B1층→M층). 각 노드 = 점 + 호기 번호.
- **상태 변종 (행 4)** — 검색 활성("사5" 매칭 3건, 하이라이트 mark) / 검색 결과 0건 (Empty + 자주 검색 키워드) / 로딩 스켈레톤
- **placeholder 박스 2개** — 모달 5종 (2차) + KOELSA 이력 (3차)

### 제외 (1차 X — 의도된 범위 외)
- 5개 모달 (fault_new / fault_resolve / inspect_new / repair_new / ev_detail) — placeholder 텍스트만
- KoelsaHistorySection — placeholder 텍스트만
- list 외 5개 탭 본문 (탭 헤더만, 내용은 2·3차)

## 디자인 룰 준수

### v0.1.1 §6 Progress Color Rule (호기 카드 좌측 3px 색바)
- 정상 → `--status-safe-bar` (초록)
- 점검중 → `--status-warning-bar` (노랑)
- 고장 (미수리, 조치 대기) → `--status-fire-bar` (주황)
- 운행중지 → `--text-tertiary` (회색) + opacity 0.62

### v0.1.1 §6.2 Stat Card 위험 임계치
- 고장 1대 (≥1 임계치) → 숫자 색 `--status-danger`, 좌측 색바 `--status-danger-bar`
- 정상/점검중/중지 → 일반 색

### v0.1.1 §6.3 카테고리 카드 회색 통일
- 호기 아이콘 (`square` / `package` / `box` / `pause`) — 모두 `--text-secondary` 회색
- 단, 고장 호기는 §1.4 의미 우선 룰에 따라 `siren` + `--status-fire` 색 강조 (예외)

### §6.4 Backgrounds
- 단색 surface 만 사용 — 풀블리드 그라디언트 0건

### §6.5 Hover
- 호기 카드 / Stat 카드 — `border-color` 강화 + `translateY(-1px)` (CSS class `card-hover` / `ev-card`)

### §6.7 Shadows
- 그림자 0건 — 명도 차이만으로 계층 표현

### §7 Iconography (Lucide)
- 헤더: `menu` / `refresh-cw` / `search` / `alert-triangle`
- 호기: `square` (인승) / `package` (화물) / `box` (덤웨이터) / `pause` (운행중지)
- 고장: `siren` (예외 — fire 강조)
- 빈 상태: `search-x` / `x` / `layers` / `file-search-2`
- 사이드바/BottomNav: `layout-dashboard` / `clipboard-list` / `map` / `wrench` / `calendar`

### 노안 친화 (§1.1)
- 9·10·11px 폰트 0건 (verify 통과)
- 본문 16px (`text-body`), 보조 12~14px만 사용
- 터치 타겟 44px (모바일) / 40px (데스크톱)

## Verify 결과 (PLAN.md verify_before_commit 체크)

| # | 체크 | 결과 |
|---|------|------|
| 1 | 라인 수 (1500~3500 예상) | **2013** ✅ |
| 2 | 9·10·11px 폰트 (0이어야 함) | **0건** ✅ |
| 3 | `[data-theme]` 셀렉터/컨테이너 카운트 (≥4) | **4** (CSS 셀렉터 형태) / **11** (속성 포함) ✅ |
| 4 | viewport 라벨 (📱/🖥️) (≥4) | **4** ✅ |
| 5 | 인라인 `style="..."` 0건 | **0건** ✅ |
| 6 | 모달/KOELSA 마크업 0건 (placeholder 텍스트만) | placeholder 텍스트 4 + 토큰 명 1 (`--modal-padding`) — 실제 마크업 0 ✅ |

## 자체 검증 (Self-Check)

```
$ ls cha-bio-safety/docs/redesign-context/07-elevator/sketch/elevator-sketch.html
FOUND: cha-bio-safety/docs/redesign-context/07-elevator/sketch/elevator-sketch.html

$ git log --oneline | head -2
3e478d8 docs(260510-4x7): elevator redesign 1차 sketch HTML — 4 viewports, container/header/grid/route
66b02b4 docs(260510-4x7): pre-dispatch plan for redesign/07-elevator 1차 시안 HTML
```

## Self-Check: PASSED

## Deviations from Plan

**None — plan executed exactly as written.**

PLAN.md의 "1차 시안 범위" / "포함" / "제외" 항목 모두 그대로 구현. 추가 deviation 없음.

## Out-of-scope notes (다음 작업 후보)

- **2차 시안** — 5개 모달 (고장 신고 / 수리 등록 / 점검 기록 / 호기 상세 / 조치) 별도 quick task
- **3차 시안** — KOELSA 이력 카드 + 검사성적서 PDF 표시 별도 quick task
- **TSX 변환** — 모든 시안 통과 후 별도 quick task (260511-XXX 류)
