---
phase: quick-260510-4li
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cha-bio-safety/docs/redesign-context/02-inspection/sketch/inspection-sketch-main.html
autonomous: true
requirements:
  - REDESIGN-02-INSPECTION-SKETCH-MAIN
must_haves:
  truths:
    - "단일 자기-완결적 HTML 1개가 inspection-sketch-main.html 경로에 존재한다"
    - "4뷰포트(Mobile Dark/Light + Desktop Dark/Light)에서 InspectionPage 메인 화면(헤더 + Zone 탭 + 카테고리 16종 그리드 + 층 칩 + CP 리스트)이 1:1로 표현된다"
    - "16종 카테고리 모두 시안에 등장하며 §7.2 Iconography 매핑(custom SVG 6종 + lucide 다수)이 정확히 적용된다"
    - "카테고리 카드의 아이콘 색은 모두 --text-secondary 회색이다 (§6.3 — §7.2 표 색상은 카테고리 카드에서는 적용하지 않는다)"
    - "카테고리 카드 좌측 3px 색바는 §6.1 Progress Color Rule(0%=tertiary, 1-49%=warning, 50-99%=accent, 100%=safe)을 따른다"
    - "0% (미시작) 카테고리 카드는 opacity 0.6 으로 시각화된다"
    - "Row 4 모달 변형 영역에 일반 결과 입력 모달(휠피커 + 정상/주의/불량 버튼 + 사진 + 메모) + InspectionRevisitPopup(가완료/나조치대기 2-variant) + AccessBlockedPopup 3종이 모두 표현된다"
    - "9/10/11px 폰트가 시안 내 어디에서도 사용되지 않는다 (모든 텍스트 12px 이상 — meta-label 등 시안 chrome 제외)"
    - "Tailwind CDN + tokens.css 인라인 + Lucide CDN 만으로 동작한다 (외부 빌드 단계 없음)"
  artifacts:
    - path: "cha-bio-safety/docs/redesign-context/02-inspection/sketch/inspection-sketch-main.html"
      provides: "InspectionPage 메인 1차 시안 (4뷰포트 + 모달 변형 행)"
      min_lines: 800
      contains: "data-theme=\"dark\""
  key_links:
    - from: "inspection-sketch-main.html"
      to: "design-system.md §6.1 Progress Color Rule"
      via: "카테고리 카드 좌측 3px 색바 4단계 색 (회색/노랑/파랑/초록)"
      pattern: "status-(safe|warning)-bar|var\\(--accent\\)|var\\(--text-tertiary\\)"
    - from: "inspection-sketch-main.html"
      to: "design-system.md §6.3 카테고리 카드 룰"
      via: "16개 카테고리 카드 모두 아이콘 색 --text-secondary 회색 통일"
      pattern: "color:var\\(--text-secondary\\)"
    - from: "inspection-sketch-main.html"
      to: "design-system.md §7.2 Iconography 매핑"
      via: "custom SVG 6종 inline + lucide CDN data-lucide 속성"
      pattern: "data-lucide=\"(cloud|shield|car|zap|bar-chart-3|wind|arrow-down-to-line|waves|bell|video)\""
    - from: "inspection-sketch-main.html"
      to: "src/pages/InspectionPage.tsx ZONE_CONFIG"
      via: "Zone 탭 3종(연구동/사무동/지하)"
      pattern: "연구동|사무동|지하"
---

<objective>
redesign/02-inspection 1차 시안 HTML 1개 파일 생성. InspectionPage 메인 화면(상단 헤더 + Zone 탭 + 카테고리 16종 그리드 + 층 선택 칩 + CP 리스트) 과 일반 결과 입력 모달 1개 + 재진입/접근불가 팝업 2종 을 4뷰포트(모바일/데스크톱 × 라이트/다크) + 모달 변형 행 으로 1:1 매핑.

특수 모달(Stairwell/Baeyeon/CCTV/증상 피커 5종) 은 이번 시안에 포함하지 않음 — 별도 quick task 에서 2차 시안으로 처리.

Purpose: 디자인 시스템 v0.1.1 룰을 InspectionPage 메인 워크플로우에 적용한 시안을 사용자에게 시각 검증받고, 승인 후 TSX 변환 단계(다음 quick task)로 진행.

Output: `cha-bio-safety/docs/redesign-context/02-inspection/sketch/inspection-sketch-main.html` 1개 파일.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md
@cha-bio-safety/docs/redesign-context/02-inspection/02-inspection.md
@cha-bio-safety/docs/design-system.md
@cha-bio-safety/docs/redesign-context/01-dashboard/sketch/dashboard-sketch.html
@cha-bio-safety/src/components/ui/icons.tsx
@cha-bio-safety/src/components/InspectionRevisitPopup.tsx
@cha-bio-safety/src/components/AccessBlockedPopup.tsx
@cha-bio-safety/src/components/PhotoButton.tsx

<interfaces>
<!-- 시안 작성에 필요한 핵심 상수/타입 (codebase 에서 추출). 추가 탐색 불필요. -->

From src/pages/InspectionPage.tsx (lines 38~54, 75~80, 32~33, 58~63):

Categories (16종 — labels[], icon emoji 자리는 §7.2 Lucide/custom SVG 로 대체):
1. 특별피난계단                  → StairsIcon (custom)
2. 청정소화약제                  → Cloud (lucide)
3. 전실제연댐퍼·연결송수관       → Shield (lucide)
4. 주차장비·회전문               → Car (lucide)
5. 소방용전원공급반              → Zap (lucide)
6. 방화셔터                      → ShutterIcon (custom)
7. DIV                          → BarChart3 (lucide)
8. 컴프레셔                      → Wind (lucide)
9. 유도등                        → ExitSignIcon (custom)
10. 배연창                       → SmokeVentIcon (custom)
11. 완강기                       → ArrowDownToLine (lucide)
12. 소화전·비상콘센트            → HoseReelIcon (custom)
13. 소화기                       → FireExtinguisherCustom (custom)
14. 소방펌프                     → Waves (lucide)
15. 화재수신반                   → Bell (lucide)
16. CCTV                        → Video (lucide)

Zone tabs (3종):
- research (연구동)  — 🔬 → FlaskConical (lucide, §7.5)
- office   (사무동)  — 🏢 → Building2    (lucide, §7.5)
- underground (지하) — 🚇 → TrainFront   (lucide, §7.5)

Floors:
- GROUND_LIST = ['8-1F','8F','7F','6F','5F','3F','2F','1F']
- UNDER_LIST  = ['B1','M','B2','B3','B4','B5']

Result options (3종 — 정상/주의/불량):
- normal  → CheckCircle2 (lucide, §7.3) + status-safe
- caution → AlertTriangle (lucide, §7.3) + status-warning
- bad     → XCircle      (lucide, §7.3) + status-danger

InspectionRevisitPopup props (시안에서 표현):
- variant: 'completed' | 'pending-action'  (가완료 / 나조치대기 2-variant)
- categoryLabel: string
- onConfirm + onCancel

AccessBlockedPopup props (시안에서 표현):
- 단일 onConfirm 만 (자동 스킵 안내)

PhotoButton: 본 시안은 모달 안에서 "사진 첨부" 라벨 + Camera 아이콘 버튼으로 단순 표현.
</interfaces>

<reference_pattern>
<!-- 대시보드 시안 패턴 — 동일 구조 계승 -->
파일 구조 (참조: cha-bio-safety/docs/redesign-context/01-dashboard/sketch/dashboard-sketch.html):
1. <head>
   - Tailwind CDN
   - Pretendard CDN
   - Lucide CDN
   - <style> 안에:
     * [data-theme="dark"] / [data-theme="light"] 토큰 블록 (tokens.css 인라인)
     * :root spacing/radius/font 토큰
     * .text-{caption|label|body-sm|body|title|heading|display} 클래스
     * 시안 메타 스타일 (.meta-label, .viewport-frame 등)
     * skeleton/blink/card-hover 데모 애니메이션
2. <body class="p-8">:
   - <header> 시안 메타 (v0.1.1 시안 라벨, 날짜, 브랜치명, 페이지 타이틀, 설명)
   - 메모리 룰 HTML 주석 블록 (시각 디자인 source of truth)
   - 4뷰포트 grid:
     * Row 1: Mobile Dark + Mobile Light (375 × 812 가로 2)
     * Row 2: Desktop Dark (1280 × 800 전폭)
     * Row 3: Desktop Light (1280 × 800 전폭)
     * Row 4: 모달 변형 (3개 패널: 일반 모달 / Revisit / AccessBlocked)
3. <script>:
   - lucide.createIcons() 호출 (data-lucide 속성 → SVG 변환)
</reference_pattern>
</context>

<tasks>

<task type="auto">
  <name>Task 1: inspection-sketch-main.html 작성 (4뷰포트 + 모달 변형 행)</name>
  <files>cha-bio-safety/docs/redesign-context/02-inspection/sketch/inspection-sketch-main.html</files>
  <action>
대시보드 시안(`cha-bio-safety/docs/redesign-context/01-dashboard/sketch/dashboard-sketch.html`) 과 동일한 자기-완결적 HTML 구조를 채택한다. Write 도구로 단일 파일 생성. 디렉토리 `sketch/` 가 없으면 부모와 함께 생성됨 (Write 가 처리).

**A. <head> 영역 — 대시보드 시안과 동일 구성:**
1. Tailwind CDN, Pretendard CDN, Lucide CDN script 로드
2. <style> 안에 다음 그대로 인라인 (대시보드 시안에서 복사):
   - `[data-theme="dark"]` 와 `[data-theme="light"]` 토큰 블록 전체 (surface-* / text-* / status-* / accent / duty-* / border-* + 모든 alias)
   - `:root` spacing/radius/font 토큰
   - `.text-caption` ~ `.text-display` typography 클래스
   - `.meta-label`, `.viewport-frame`, `.viewport-mobile` (375×812), `.viewport-desktop` (1280×800)
   - `.viewport-modal` (모달 변형용 — 모바일과 동일 375×812 또는 desktop 분할)
   - `.skel`, `.blink`, `.blink-fast`, `.card-hover` 동일 채택
   - `.no-scrollbar` (CP 리스트가 길어질 때 모바일 스크롤용)

**B. 메모리 룰 HTML 주석 블록 — 대시보드 시안과 동일하게 본문 진입 직전에 명시:**
- "9/10/11px 폰트 사용 금지"
- "v0.1.1 §6.1 Progress Color Rule" (0/1-49/50-99/100% 색 매핑)
- "v0.1.1 §6.3 카테고리 카드 — 아이콘 모두 회색 통일, 좌측 3px 색바로 진척률만"
- "v0.1.1 §7.2 Iconography 매핑 — 카테고리 카드에서는 §7.2 표 색상 적용 안 함, --text-secondary 회색 강제"

**C. <header> 시안 메타:**
- meta-label: "v0.1.1 시안" / "2026-05-09" / "redesign/02-inspection 1차 (메인)"
- h1: "CHA Bio Complex — 일반 점검 재디자인 시안 (1차 — 메인 화면)"
- 설명 p: "InspectionPage 메인 화면 1:1 매핑 · 16개 카테고리 + Zone 탭 + 층 칩 + CP 리스트 + 일반 결과 입력 모달 · 특수 모달(Stairwell/Baeyeon/CCTV/증상 피커 5종) 은 2차 시안 별도 파일"

**D. 4뷰포트 본문 — 각 뷰포트는 다음 섹션을 포함 (모두 동일 정보 표현, 라이트/다크/모바일/데스크톱만 차이):**

각 뷰포트 안 컨테이너 = `<div class="viewport-frame viewport-mobile" data-theme="dark">` 식.

**섹션 ① — 상단 헤더:**
- 좌측: 햄버거(Menu icon) + "방재팀" 캡션 + "일반 점검" text-label (모바일) / "일반 점검" text-heading (데스크톱)
- 우측: 점검일 표시 ("2026-05-09 · 토") + 종/유저 아바타 (대시보드와 같은 패턴)
- background: var(--surface-raised), border-bottom var(--border-default)

**섹션 ② — Zone 탭 (3종 가로):**
- 각 탭 = lucide 아이콘(FlaskConical/Building2/TrainFront) + 라벨("연구동"/"사무동"/"지하") + 진척 mini-bar
- 활성 탭 = background var(--surface-active), color var(--text-primary)
- 비활성 = background transparent, color var(--text-secondary), border-bottom 2px solid 활성 시
- 모바일: 가로 3등분 (또는 chip 형태 가로 스크롤). 데스크톱: 좌측 사이드바 또는 동일 가로 탭
- "연구동" 활성 상태로 표시 (대표)

**섹션 ③ — 카테고리 16종 그리드 (페이지 핵심 — §6.3 정확 적용):**
- 모바일: 2열 그리드 (gap var(--card-gap))
- 데스크톱: 4열 그리드 (gap var(--card-gap)) — 더 빽빽
- 각 카드 구조:
  ```
  [카드]
    좌측 3px 색바 (absolute left:0 top:0 bottom:0 width:3px) — Progress Color Rule
    내부 padding-left 14px 부터 시작
    상단 줄: lucide/custom SVG 아이콘 24px (color:var(--text-secondary)) + 카테고리 라벨 (text-label, color:var(--text-primary))
    하단 줄: 진척률 텍스트 "12/24" (text-caption, color:var(--text-secondary)) + 우측 % 또는 N/M
    background: var(--surface-raised), border 1px solid var(--border-default), rounded var(--radius-md)
    padding: var(--card-padding) (좌측 색바 만큼 여유)
    0% 카드 → opacity:0.6
  ```
- 16개 카테고리 모두 등장. 진척률은 다양한 값으로 분배(시각 데모용 — 0%/15%/45%/72%/100% 골고루):
  * 100% (safe-bar 초록): 특별피난계단, 화재수신반
  * 50-99% (accent 파랑): 청정소화약제(72%), 소화전·비상콘센트(58%)
  * 1-49% (warning-bar 노랑): DIV(45%), 유도등(28%), 소화기(15%), 전실제연댐퍼·연결송수관(40%)
  * 0% (text-tertiary 회색 + opacity 0.6): 주차장비·회전문, 소방용전원공급반, 방화셔터, 컴프레셔, 배연창, 완강기, 소방펌프, CCTV
- 카테고리 라벨에 줄바꿈이 필요한 케이스(예: "전실제연댐퍼·연결송수관", "소화전·비상콘센트", "주차장비·회전문") 는 카드 내부에서 2줄로 자연스럽게 처리 (white-space:normal). 모바일 좁은 폭에서도 9/10/11px 줄이지 말 것 — text-label(13px) 유지. 폭 부족하면 카드 padding 조정 또는 폰트 weight 로 차별화.

**섹션 ④ — 층 선택 칩 (선택된 카테고리/Zone 기준 — 대표로 "연구동 + DIV" 가 활성):**
- 모바일: 가로 스크롤 chip 행 (overflow-x:auto, no-scrollbar)
- 데스크톱: 가로 풀폭 (3F·2F·1F·B1·B2·B3·B4·B5 등)
- 활성 칩 = background var(--accent), color var(--text-on-accent), rounded-pill
- 비활성 = background var(--surface-sunken), color var(--text-secondary), rounded-pill
- 완료 층은 우측에 작은 ✓ (lucide Check 14px) — 노안 친화로 색만 safe 변경, 폰트는 그대로
- "B5" 가 활성 상태로 표시

**섹션 ⑤ — CP 리스트 (선택된 카테고리+Zone+층 기준 — 대표로 DIV B5층 4개 CP):**
- 각 CP 카드:
  - 좌측 3px 색바: 점검 결과별 (정상=safe-bar / 주의=warning-bar / 불량=danger-bar / 미점검=text-tertiary)
  - 본문: CP 라벨 (예: "CP-DIV-B5-1") + locationNo (text-caption text-tertiary) + 결과 배지 (CheckCircle2/AlertTriangle/XCircle + 라벨)
  - 우측: 마지막 점검 시각 또는 "점검 시작 ›" CTA
  - 클릭 시 결과 입력 모달 진입을 시사 (`card-hover` 클래스)
- 4개 CP 중: 정상 1개, 주의 1개, 불량 1개, 미점검 1개 (모든 상태 visualized)

**섹션 ⑥ — BottomNav (모바일만):**
- 4개 탭: 대시보드(LayoutDashboard) / 점검(ClipboardList — 활성) / 일정(Calendar) / 더보기(Menu)
- height + paddingBottom 패턴 (메모리 룰 — bottom 으로 띄우지 말 것)
- 활성 탭 색 var(--accent), 비활성 var(--text-secondary)

**E. Row 4 — 모달 변형 (3개 패널 가로 정렬):**

각 패널은 "375×812 모바일 mock + dimmed overlay + slide-up 모달/팝업" 표현.

**E-1. 일반 결과 입력 모달 (대표 = 유도등 단일 CP "EL-RES-1F-12"):**
- overlay: 전 화면 var(--surface-overlay) 어둡힘
- 모달 (slide-up bottom sheet 모바일 패턴):
  - background: var(--surface-raised), rounded-t-lg
  - padding: var(--modal-padding)
  - 헤더: "유도등 점검" text-heading + 카테고리 아이콘 ExitSignIcon + 닫기 버튼(X icon)
  - CP 휠피커 (단일 선택 — 시각 데모): 3-item 가시 리스트, 가운데 항목 강조, 좌우 미세 그림자 없음, item-height 44px, scroll-snap 표현. 항목 텍스트 16px+
  - 결과 버튼 3개 가로 그리드: 정상(safe-bg/safe-bar)/주의(warning-bg/warning-bar)/불량(danger-bg/danger-bar). 각 버튼 = lucide 아이콘 + 라벨, height var(--button-height) 44px, 활성 = bar 색 border 2px + bg 강조
  - PhotoButton 영역: "사진 첨부" + Camera 아이콘 + 회색 빈 슬롯 (점선 border var(--border-default))
  - 메모 textarea: "특이사항" 라벨 + 입력 박스 (rows=3, var(--surface-sunken) 배경)
  - 하단 저장 버튼 (CTA — §6.4 그라디언트 사용 가능): linear-gradient(135deg, #1d4ed8, #0ea5e9), color #fff, rounded var(--radius-sm), height 44px, 라벨 "저장"

**E-2. InspectionRevisitPopup — 2-variant 같은 패널 안 위/아래 분할 또는 좌/우:**
- variant 1: "(가) 점검 완료" — 이미 정상/주의 점검된 CP 재진입. 아이콘 CheckCircle2 (safe), 메시지 "이 항목은 이미 점검 완료되었습니다", "점검 결과: 정상" + "다시 점검" / "취소" 버튼
- variant 2: "(나) 조치 대기" — bad 결과 + 미조치(unresolved) 상태. 아이콘 Wrench 또는 Flame (fire), 메시지 "이 항목은 조치 대기 중입니다", "조치 보기 ›" / "취소" 버튼
- 부분 오버레이 스타일 (position:absolute inset:0 zIndex:10 안내 — AccessBlockedPopup 와 동일 패턴 명시)

**E-3. AccessBlockedPopup:**
- 화면 가운데 작은 카드: 아이콘 (lucide Lock 또는 ShieldAlert), 메시지 "이 측정점은 접근이 차단된 위치입니다", description "다음 항목으로 자동 스킵합니다", 단일 "확인" 버튼

**F. <script> 끝부분:**
- `lucide.createIcons()` 호출하여 data-lucide 속성을 SVG 로 변환

**G. 추가 룰 (절대 위반 금지):**
1. **9/10/11px 폰트 사용 금지** — 모든 본문/라벨 텍스트는 12px 이상. meta-label 등 시안 chrome 도 12px 이상.
2. **§6.3 카테고리 카드 색 통일** — 16개 모두 아이콘 color:var(--text-secondary). §7.2 표 색상 적용 안 함. 좌측 색바만 진척률 색.
3. **§6.1 Progress Color Rule** — 0%=text-tertiary, 1-49%=warning-bar, 50-99%=accent, 100%=safe-bar. 다른 색 사용 금지.
4. **0% 카드 opacity 0.6** — §6.3 명시 룰. 8개 카드 모두 적용.
5. **§7.2 Iconography 매핑 정확** — custom SVG 6종은 inline <svg> 로 직접 그리거나 dashboard-sketch.html 처럼 `<i data-lucide="...">` 사용. custom SVG 는 lucide 에 없으므로 inline. 매핑 표 그대로:
   - StairsIcon (특별피난계단) — 솔리드 채움 3단 계단, inline SVG
   - ShutterIcon (방화셔터) — 프레임 + 슬랫 + 화살표, inline SVG
   - ExitSignIcon (유도등) — 정사각형 + 우향 화살표, inline SVG
   - SmokeVentIcon (배연창) — 4분할 창문, inline SVG
   - HoseReelIcon (소화전·비상콘센트) — 사각 프레임 + 코일 호스 + 노즐, inline SVG
   - FireExtinguisherCustom (소화기) — 본체 + 손잡이 + 호스, inline SVG
   (각 SVG 는 `cha-bio-safety/src/components/ui/icons.tsx` 의 SVG path 그대로 복사. stroke="currentColor" + parent 의 color:var(--text-secondary) 로 회색 통일)
6. **인라인 style 허용** — 시안 단계이므로 `style="color:var(--text-secondary)"` 같은 인라인 토큰 사용 OK. TSX 변환 단계(다음 quick) 에서 Tailwind 로 전환 예정.
7. **모바일/데스크톱 폰트 동일** — 노안 룰. lg:text-* prefix 로 폰트 크기 분기 금지.
8. **에러 상태 silent fallback** — 시안에 표시 안 함 (대시보드 시안과 동일 정책).
9. **loading 상태 1개 표현** — 카테고리 그리드 우측 상단 또는 별도 mini 패널에 skel 클래스 박스 2~3개 (skeleton 데모용).
10. **empty 상태 1개 표현** — CP 리스트 영역에서 "선택된 층에 측정점이 없습니다" 같은 안내. text-secondary + 일반 텍스트.

**사용 외 도구 / 파일 변경 금지:**
- TSX 파일/컴포넌트 수정 금지 (`src/` 어떤 파일도 손대지 말 것)
- 본 시안 외 다른 sketch HTML 생성 금지
- 2차 모달(Stairwell/Baeyeon/CCTV/증상 피커) 시안 포함 금지
  </action>
  <verify>
    <automated>test -f cha-bio-safety/docs/redesign-context/02-inspection/sketch/inspection-sketch-main.html &amp;&amp; \
      [ "$(wc -l &lt; cha-bio-safety/docs/redesign-context/02-inspection/sketch/inspection-sketch-main.html)" -ge 800 ] &amp;&amp; \
      grep -q 'data-theme="dark"' cha-bio-safety/docs/redesign-context/02-inspection/sketch/inspection-sketch-main.html &amp;&amp; \
      grep -q 'data-theme="light"' cha-bio-safety/docs/redesign-context/02-inspection/sketch/inspection-sketch-main.html &amp;&amp; \
      grep -c '특별피난계단\|청정소화약제\|전실제연댐퍼\|주차장비\|소방용전원공급반\|방화셔터\|DIV\|컴프레셔\|유도등\|배연창\|완강기\|소화전\|소화기\|소방펌프\|화재수신반\|CCTV' \
        cha-bio-safety/docs/redesign-context/02-inspection/sketch/inspection-sketch-main.html | awk '{ if ($1 &lt; 16) exit 1 }' &amp;&amp; \
      grep -q '연구동' cha-bio-safety/docs/redesign-context/02-inspection/sketch/inspection-sketch-main.html &amp;&amp; \
      grep -q '사무동' cha-bio-safety/docs/redesign-context/02-inspection/sketch/inspection-sketch-main.html &amp;&amp; \
      grep -q '지하' cha-bio-safety/docs/redesign-context/02-inspection/sketch/inspection-sketch-main.html &amp;&amp; \
      grep -q 'opacity:0.6\|opacity: 0.6' cha-bio-safety/docs/redesign-context/02-inspection/sketch/inspection-sketch-main.html &amp;&amp; \
      grep -v '^\s*//\|^\s*\*\|^\s*&lt;!--' cha-bio-safety/docs/redesign-context/02-inspection/sketch/inspection-sketch-main.html \
        | grep -E 'font-size:\s*(9|10|11)px|text-\[(9|10|11)px\]' ; \
      [ $? -ne 0 ] &amp;&amp; echo "PASS — 9/10/11px 미사용 + 16개 카테고리 + 3 zone + opacity 0.6 + 라이트/다크 모두 존재"</automated>
  </verify>
  <done>
- `cha-bio-safety/docs/redesign-context/02-inspection/sketch/inspection-sketch-main.html` 파일이 800줄 이상으로 존재
- 16개 카테고리 라벨 모두 등장 (특별피난계단/청정소화약제/전실제연댐퍼/주차장비/소방용전원공급반/방화셔터/DIV/컴프레셔/유도등/배연창/완강기/소화전/소화기/소방펌프/화재수신반/CCTV)
- 3개 Zone 탭(연구동/사무동/지하) 모두 등장
- `data-theme="dark"` 와 `data-theme="light"` 두 토큰 블록 존재
- `opacity:0.6` 또는 `opacity: 0.6` 0% 카드에 적용
- 9/10/11px 폰트 일체 미사용 (인라인 style 또는 Tailwind text-[9~11px] 패턴 grep 미발견)
- 사용자가 브라우저에서 파일 열었을 때 4뷰포트 + 모달 변형 행이 모두 렌더되며 §6.3 카테고리 카드 룰(아이콘 회색 통일) 이 시각적으로 일관 적용됨을 확인
  </done>
</task>

</tasks>

<verification>
시안 자기-완결성 verify:
- 파일 1개로 외부 빌드/번들 단계 없이 브라우저에서 직접 열림
- Tailwind CDN + Pretendard CDN + Lucide CDN 만 사용 (네트워크 차단 시 로컬 CDN 의존성만 끊김)
- tokens.css / typography.css 가 인라인되어 별도 파일 의존 없음
- §6.3 카테고리 카드 룰: 16개 카드 모두 아이콘 색이 var(--text-secondary) 회색으로 통일되었는지 시각 검증
- §6.1 Progress Color Rule: 좌측 3px 색바가 0%/1-49%/50-99%/100% 단계별로 회색/노랑/파랑/초록인지 시각 검증
- §7.2 Iconography: custom SVG 6종(StairsIcon/ShutterIcon/ExitSignIcon/SmokeVentIcon/HoseReelIcon/FireExtinguisherCustom) 정확히 매핑된 카테고리 카드에 등장. lucide 아이콘 10종(Cloud/Shield/Car/Zap/BarChart3/Wind/ArrowDownToLine/Waves/Bell/Video) 정확히 매핑
- 모달 변형 3종(일반 결과 입력 + Revisit 2-variant + AccessBlocked) 모두 표현
- 9/10/11px 폰트 일체 미사용 (메모리 룰)
</verification>

<success_criteria>
1. 사용자가 시안 파일을 브라우저에서 열어 4뷰포트 + 모달 행을 시각 검토 가능
2. 16개 카테고리 + 3 Zone 탭 + 층 칩 + CP 리스트 + 일반 모달 + 재진입/접근불가 팝업이 모두 1차 시안에 포함되어, 사용자가 §6 ~ §9 룰 위반 없는지 확인 후 approved 판정 가능
3. 사용자 승인 후 다음 quick task(특수 모달 5종 2차 시안) 또는 TSX 변환 단계로 진행 가능한 상태
</success_criteria>

<output>
완료 후 `.planning/quick/260510-4li-redesign-02-inspection-sketch-main/260510-4li-SUMMARY.md` 생성:
- 시안 파일 경로 + 라인 수
- 16개 카테고리 매핑 검증 (custom SVG 6 + lucide 10)
- §6.1 / §6.3 / §7.2 룰 적용 확인
- 모달 변형 3종 등장 확인
- 사용자 검증 대기 상태 명시
- 다음 단계 후보: (a) 2차 모달 시안 quick task, (b) TSX 변환 quick task
</output>
