# Production Sync — 직원 도메인(cbc7119) 적용 추적

> **읽기 전**: 이 파일은 `production` 브랜치에서만 갱신합니다. main 에는 없어야 정상.
>
> **목적**: 직원 도메인(cbc7119.pages.dev) 에 어떤 디자인/기능 변경이 적용되었는지 사람이 한눈에 보는 노트. main↔production 분기 시 "이 분기 commits 가 살아있는 작업인지 폐기된 작업인지" 판단의 single source of truth.

---

## 현재 상태

```
상태:           안정 (SYNCED) — 최신: 사진 업로드 실패 UX 1단계 (upload 무던짐화 + '기기에 임시저장됨' 문구 분기 16곳 + 조치상세 보관함 편입). 배포 + tsc/build/grep/라이브 번들 검증 완료
진행 작업:       없음
기준 production: `d6735ca` (feat: 사진 UX 1단계, 260612-3lt) — staging cbc7119-data 4e169fa sync. 직전 1dc8450(안정성 3종, f5bb1db sync)
마지막 동기화:   2026-06-12
마지막 배포 URL: https://5028e289.cbc7119.pages.dev (production alias = cbc7119.pages.dev)
```

**상태 의미**:
- `안정 (SYNCED)` — 직원 도메인이 표 마지막 entry 와 일치. 새 작업 들어와도 OK.
- `작업중 (IN_PROGRESS)` — cherry-pick / 배포가 진행 중이거나 중단됨. 새 작업 들어오면 STOP, 먼저 진행 중인 거 마무리.

---

## 사고 방지 룰 (작업 시작 전 필독)

1. **이 노트의 "기준 production" 과 실제 `git rev-parse origin/production` 이 다르면 STOP** — 누군가 이 노트 갱신 없이 production 을 건드림 = 미상의 변경. 사용자에게 즉시 확인.
2. **`origin/main` 에 표에 없는 source commit 이 있으면** → 사용자에게 "이거 적용 대상이냐, 폐기됐냐" 확인. 무조건 cherry-pick 금지.
3. **충돌 시 "operational fix 보존" 자동 적용 금지** — 분기 commit 정체가 표에 없으면 폐기 의심부터. (사고 사례: 2026-05-27 phase 15 진입점 버튼 부활)
4. **작업 시작 시 상태를 `작업중` 으로 바꾸고, 배포 후 새 entry 추가 + `안정` 으로 환원**.

---

## 표준 절차

| 단계 | 액션 |
|---|---|
| ① 사전 점검 | `git fetch origin` → 이 노트의 "기준 production" == `git rev-parse origin/production` 검증 |
| ② 작업 식별 | 사용자가 cherry-pick 대상 commit hash 알려주면 → 그 commit 들이 source-only(.ts/.tsx/.css/.html) 인지 확인 |
| ③ 상태 전환 | 이 노트의 "상태" 를 `작업중 (IN_PROGRESS)` 로 변경 + 무슨 작업인지 한 줄 |
| ④ cherry-pick | `git cherry-pick <hashes>` (충돌 시 위 룰 #3 적용) |
| ⑤ verify | 사용자가 알려준 grep/tsc 체크 통과 |
| ⑥ build + deploy | `cd cha-bio-safety && npm run build && npx wrangler pages deploy dist --project-name=cbc7119 --branch=production --commit-message="ASCII"` |
| ⑦ 표 갱신 | 아래 표에 새 entry 추가, "기준 production" 갱신, 상태 `안정` 환원 |
| ⑧ commit | 이 노트와 cherry-pick 모두 production 브랜치에 commit. push 는 사용자 명시 OK 시에만 |

---

## 적용 이력

| 날짜 | 작업명 | 적용된 main commits | production commits | 배포 URL | 메모 |
|---|---|---|---|---|---|
| 2026-06-12 | **사진 업로드 실패 UX 1단계 — upload 무던짐화 + 보관함 백업 문구 분기 + 조치상세 편입** (staging cbc7119-data 4e169fa → prod sync, quick 260612-3lt) | (staging 검증 `4e169fa` 5파일 +49/-23, main 무관 — 별도 staging repo) | `d6735ca` (feat) + `57c8c84` (plan/patch docs) + docs commit | https://5028e289.cbc7119.pages.dev | **배경**: f5bb1db(→prod 1dc8450) 후속. 사용자 iOS 실기기 테스트에서 기내모드 시 raw 'Load failed' 노출 확인 + "임시저장됨을 문구로 알려달라 + 조치 화면에도" 요청. 폼 입력값 통째 임시저장(2단계)은 **사용자 보류 확정**. **변경**: ① `usePhotoUpload.upload()` fetch+res.json() try/catch→null (기내모드 TypeError·CF 5xx HTML JSON파싱 실패도 무던짐 — 호출부 raw 문구 소멸. null 의미/finally uploading/동일 blob key 재사용/vid 스냅샷 보존) ② `photoUploadFailMsg(vaultBacked, failedCount?)` 헬퍼 export + 훅 `vaultBacked`(=!!vaultIdRef, handleFile/restore 가 ref 설정 후 setState 라 render 신선) 노출 → **가드 16곳 문구 분기**(InspectionPage 12 + FloorPlanPage 3 + RemediationDetailPage 1): 보관함 백업 시 '사진 [N장 ]업로드 실패 — 기기에 임시저장됨. 다시 시도해 주세요'. 각 가드는 **자기 훅 인스턴스의 vaultBacked**(bcPhoto/inspectBcPhoto 구분). `PhotoUploadFailedError(failedCount, vaultBacked)` 시그니처 확장(uploadAll 이 `failedSlots.some(s=>!!s.vaultId)` 전달, 멀티 호출부 5곳은 err.message 표시라 무수정 전파). import 방향 multi→single 단방향(순환 0) ③ RemediationDetailPage(조치 상세, f5bb1db 의도적 제외분 편입): scope `'inspection-resolution'` + 저장 성공 toast 직후 `photo.reset()`(보관함 정리 — 없으면 조치 성공마다 stale 복구 entry 잔존) + 가드 문구 통일. 복구 버튼은 PhotoButton 자동 연결. **적용**: 훅 2파일 = 4e169fa pre/post-image byte-identical(verbatim), 페이지 3파일 = patch offset CLEAN(어제 1dc8450 으로 이식한 가드 라인이 교체 대상이라 컨텍스트 일치). 5파일 +46/-25. **검증**: staging E2E(4e169fa) — 업로드 500/기내모드(abort) 모두 새 문구+저장 API 0건, 미첨부 정상 저장 회귀 무, 정상 업로드 회귀 무(저장 성공 시 보관함 비움), 조치 상세 차단→앱 재실행→'저장 못한 사진 복구(1장)'→복구→resolution_photo_key D1 저장+보관함 1→0, 다중 '2장 실패—임시저장됨', IndexedDB 불능(프라이빗 모드) fallback 문구. prod — executor verify-gate 13/13 + 오케스트레이터 독립 재검(헬퍼 분포 13/4/2, 옛 문구 잔존 0, BC 인스턴스 정합 1+1, LegalFindingsPage 무접촉) + tsc 0 + build precache 84 + **라이브 번들**(usePhotoUpload 청크에 '기기에 임시저장됨', version.json 02:52). **별건 backlog**: LegalFindingsPage 보고서 PDF 업로드 실패가 '사진 업로드 실패' 문구를 쓰는 기존 미스라벨(L103) — 이번 범위 밖, 별도 정리 후보. **스팟체크 팁**: DevTools 로 /api/uploads 를 HTTP 실패응답과 요청차단(abort) 둘 다 주입(핵심은 abort 경로) + 미첨부 정상 저장 회귀 확인. GSD quick(planner+executor worktree, 메인 Claude 직접 build+wrangler 배포). **사용자 iOS 실기기 기내모드 재테스트 완료(2026-06-12)** — f5bb1db 분 실기기 확인까지 이걸로 종결. |
| 2026-06-11 | **안정성 3종 — 사진 업로드 실패 가드+디바이스 보관함 / 월 경계 일정 클램프 / KST 세션 날짜** (staging cbc7119-data f5bb1db → prod sync, quick 260611-jo8) | (staging 검증 `f5bb1db` 14파일 +460/-94, main 무관 — 별도 staging repo) | `1dc8450` (fix) + `0b2bf53` (plan/patch docs) + `5c092c9` (worktree merge) + docs commit | https://ec20706c.cbc7119.pages.dev | **배경**: prod 안정성 감사 🔴 1순위 4건 중 ①③④ — ②(법정 지적사항 권한)는 의도된 설계로 확정·제외. **① 점검/조치 사진 조용한 누락** (정책: 저장 차단+전체 재시도+디바이스 보존): `useMultiPhotoUpload.uploadAll` 이 전 슬롯 시도 후 성공분 key 슬롯 기록(재시도 시 재업로드 skip), 하나라도 실패 시 `PhotoUploadFailedError('사진 N장 업로드 실패')` throw → 호출부 mutation 이 저장 API 호출 전 중단. 뮤텍스 체결+라이브 슬롯 identity 머지(업로드 중 ✕/추가 경합 방지). `usePhotoUpload.upload()` null 의미 유지(실패·미첨부 모두 null) + 동일 blob 재시도 시 key 재사용. **단일 호출부 가드 15곳** = InspectionPage 12(throw 10 + DIV압력/컴프레셔 toast+return 2) + FloorPlanPage 3 — `hook.hasPhoto && key===null` 일 때만 차단(**미첨부 정상 저장 보존**), 소화전 paired-BC 2곳(InspectionModal+FloorPlanPage)은 BC 업로드를 본 record 저장 **앞으로** 재배치(부분 저장 방지). **uploadAll 호출부 5곳**(FindingFormSheet/FindingEditModal×2+5장 초과 가드/LegalFindingDetailPage/LegalPage FindingDetailPanel) onError 에서 PhotoUploadFailedError 분기 toast. **photoVault.ts 신규**(IndexedDB, 절대 throw 안 함): 첨부 즉시 원본 캐싱 → 업로드 실패/이탈/앱 재실행에도 보존, 보관함 정리는 **저장 성공 시점 reset()**(업로드 성공 시점 아님 — 저장 성공 경로에 reset() 없던 3곳에 추가), claim 레지스트리로 교차 픽커 중복 저장 방지, 14일 자동 정리. PhotoSourceModal '저장 못한 사진 복구 (N장)' 버튼(RotateCcw) + PhotoButton/PhotoGrid 자동 와이어링. 훅 scope: inspection/inspection-bc/inspection-resolution/legal-finding/legal-finding-resolution. **RemediationDetailPage 무수정**(기존 올바른 가드, diff 0 게이트 확인). **③ 월 경계 일정 누락**: generateMonthlyPlan.ts + SchedulePage MonthlyPlanPreview 의 `parseInt(date.split('-')[2])` 가 5/30~6/2 같은 월경계 범위에서 sd=30>ed=2 로 양쪽 달 모두 누락 → stats.ts:468 패턴의 월 범위 YMD 클램프(monthEnd zero-pad + 클램프 후 sd>ed skip). 미리보기 주말/공휴일 제외 vs 엑셀 비제외 비대칭은 의도 — 각자 보존. 동일 결함 이 2곳뿐(API 정상, 백엔드 무수정). **④ 도면 점검 새벽 저장 전날 세션 귀속(UTC)**: FloorPlanPage 점검 모달 `new Date().toISOString().slice(0,10)` → `todayKstYmd()` + InspectionPage ensureSession 인라인 getter/FloorPlanPage todayYmd IIFE 수렴. 백엔드(body.date 그대로 INSERT)는 의도된 설계 — 무수정. **적용 방법**: prod 사전 검증 — 훅2+컴포넌트5+generateMonthlyPlan+photoVault 9파일은 staging 변경 전/후 blob byte-identical(patch verbatim), 분화 5페이지(redesign)는 patch offset 매칭 4 + LegalPage 수동 4지점(import/scope/onError/PhotoSourceModal). 호출부 전수 enumerate = staging 1:1(추가 호출부 0). **검증**: staging E2E(f5bb1db) — 3장 전체 실패→'3장 실패' toast+저장 API 0건 / 리로드 후 복구 버튼(3장)→재첨부 / 부분실패(1/3)→재시도 재업로드 1회만+photo_keys 3개 / 미첨부 정상 저장 회귀 무 / ③ 5/30~6/2 일정 → 6월 미리보기·엑셀 D14/E14 + 5월 AG14/AH14 '기' / ④ fake clock KST 03:30 → date=KST 오늘. prod — tsc 0 / build precache 84(83+1, photoVault 별도 청크) / grep 게이트 전수(가드 12+3, scope 9/1/1+1/1/1+2+3, todayKstYmd 3+2, restoreCount 6파일, RemediationDetailPage diff 0, UTC 잔존 0) / 9파일 staging post-image byte-identical / diff stat 14파일 +460/-94 staging 동일 / **라이브 번들 마커**(cbc7119.pages.dev: InspectionPage 청크 '사진 업로드 실패', PhotoSourceModal '저장 못한 사진 복구', photoVault 청크 200). **④ 과거 데이터 소급 보정 완료(사용자 승인, 동일 세션)**: `date <> DATE(created_at)` 5건/84 — 전부 KST 01:09~08:58 생성+date=전날 시그니처. 내용: 03-25→03-26 윤종엽 소화기 172건 / 04-01→04-02 석현민 빈 세션 / 04-21→04-22 윤종엽 DIV13+컴프12 + 김병조 DIV1 / 05-05→05-06 윤종엽 유도등 불량 1건(조치 완료분). `UPDATE inspection_sessions SET date=DATE(created_at)` 5행(changes=5), **check_records 무수정**(총량 3917 불변). 검증: mismatch 잔존 0 + 날짜별 기록 수 이동 예측 일치(04-21 29→3, 04-22 124→150, 05-06 22→23) + 월별 통계 무영향(전부 같은 달 내 하루 이동). 스냅샷·상세 = `.planning/quick/260611-jo8-…/260611-jo8-retro-correction.md`. CP-FE-0203 1건의 checked_at UTC 아티팩트(03-25 23:59=−9h)는 기록 무수정 원칙대로 보존(별도 결정 시에만). 4/22 동일 직원 세션 2개 공존은 조회 date JOIN 합산이라 merge 불채택. **스팟체크 주의**: ④는 낮 시간엔 재현 안 됨 — 검증 시 시계 조작 필수. ① iOS 실기기 확인은 사용자 진행 예정. **별건**: staging repo 에 functions/api/inspections 라우트 통째 부재 드리프트 → prod 본을 staging 으로 복사(동 세션 별도 처리, staging 콘솔에서 배포 필요). GSD quick(planner+executor worktree, **메인 Claude 직접 build+wrangler 배포** — 서브에이전트 prod 금지 준수). |
| 2026-06-11 | **승강기 공단 API 캐시 오염 가드** — 에러 검증 통일(4마커+resultCode 양성검증) + stale-while-error 4엔드포인트 (staging cbc7119-data 2763c65 → prod sync, 260610-koe) | (staging 검증 `2763c65`, main 무관 — 별도 staging repo) | `166a72d` (fix) + docs commit | https://0e7146b5.cbc7119.pages.dev | **배경**: prod 안정성 감사 P2 — 공단(KOELSA)이 장애/일일쿼터초과/점검중일 때 HTTP 200 + 에러 본문(또는 평문 `Unauthorized`/HTML)을 주면 코드가 '결과 0건 = 미등록/기록없음'으로 오인 → D1 캐시를 틀린 값으로 덮어씀(**안전관리자 17대 전원 미등록 표시, TTL 7일 유지** 사고). **변경 (staging 16-agent 적대리뷰 + 에러주입 실측 verbatim, 백엔드 byte-exact)**: ① `_koelsa-common.ts`(신규) — `checkError`(4마커 denylist `Unexpected errors`/`SERVICE_KEY_IS_NOT_REGISTERED`/`API not found`/`SERVICE ERROR` + **양성검증** `<resultCode>00</resultCode>` 없으면 throw → denylist 못 빠져나가는 새 포맷 차단) + `fetchKoelsaXml`(`!res.ok` throw + checkError). 공단 XML 호출 전부 경유. ② `safety-manager.ts` — 호기별 UPSERT를 루프 밖 **단일 batch 원자 기록**(일부만 fresh 굳는 2차사고 방지). 에러 시 기존 캐시 + `stale:true`+staleReason, 캐시 없으면 502. `is_registered=0` 은 공단 정상응답 0건일 때만. ③ `koelsa.ts` — 모든 페이지 checkError + 부분수신(`items<totalCount`) 캐시 저장 생략 + `partial:true` + 만료후 실패 시 직전 캐시 stale. ④ `koelsa-inspect.ts`(민원24) — JSON 파싱 가드(장애 시 200+HTML 실측) + stale 폴백. ⑤ `inspect-history.ts` — syncOne 실패 시 직전 캐시 stale-while-error(+26줄). ⑥ `_inspectsafe.ts` — 로컬 checkError 제거, `_koelsa-common` 위임. ⑦ 프런트(토스트만, 통째복사 X): ElevatorPage `fetchKoelsaInspection`(stale/partial) + `safetyMgrQuery`(stale) + `inspectHistory.ts`(stale), 고정 id `koelsa-stale` 1개만. **검증**: staging — 16-agent 적대리뷰(12제기→8확정→4반박) + 에러주입 실측(잘못된 키 배포→stale 응답 + D1 17행 완전불변 + is_registered=0 오기록 0건 / 캐시1행삭제+에러→오기록없이 0행유지, 키원복 refresh 1회 자연복구 / koelsa·민원24·inspect-history 만료캐시+에러→stale+fetched_at 불변, 캐시없음+에러→502+행미생성). **검증 중 실제 공단 게이트웨이 504 우연 발생 → 가드 실전 stale 동작 확인**. prod: byte-exact(6파일 diff 0) + tsc 0 + build precache 83 + 배포 Functions bundle + **prod D1 정상경로**(`koelsa_safety_managers` 17/17 등록, ts_groups=1 batch 원자). **에러주입은 staging 갈음**(prod 잘못된 키 배포 = 실장애 유발이라 금지 — 이후 실제 공단 장애 시 stale 토스트가 동작 증거). **의도된 동작(버그 아님)**: 공단 장애+캐시없는 첫조회는 502('정보 없음' 기존 패턴) / 장애중엔 "공단 서버 응답 지연" 토스트+직전 캐시, 정상화 후 다음 새로고침 자연회복(가드는 '틀린 덮어쓰기 방지'지 장애 해결 아님) / inspect-history 는 기존에 비마커 5xx 를 빈결과로 둔갑시켜 fresh 인 척 캐시 반환했음 — 이제 정직하게 stale 표시. 메인 Claude 직접 배포. |
| 2026-06-11 | **Track B 자동백업** — cron worker DB 일일백업(backups/db/<date>.sql, 14일 보존) 신설 + uploads backups/ 미인증 차단 (260610-bkr) | (production 직접 작업 + 별도 인프라 cbc-cron-worker, main 무관) | `5397f15` (feat) + docs commit | uploads: https://ee13bcea.cbc7119.pages.dev / cron: cbc-cron-worker.jongyupyoon.workers.dev | **배경**: prod 안정성 감사 Track B — 자동백업 생산자 부재(r2-list 가 `backups/r2/` 를 전제하나 생산자가 어디에도 없음, P3) + uploads `backups/` 미인증 노출(P2). **사용자 결정 (DB만 자동)**: R2 사진 cron 스냅샷은 제외 — (1) 같은 버킷 내 복사라 진짜 오프사이트가 아님(버킷 장애 시 무의미, 진짜 백업은 SettingsPanel 수동 로컬 다운로드로 이미 가능), (2) `r2-backup.ts`(서버 89M zip)는 死 코드였고 cron(worker 메모리 128MB)에서 빌드 시 OOM. **변경**: ① `cbc-cron-worker` — R2 바인딩(STORAGE=cha-bio-storage) + cron `32 18 * * *`(KST 03:32, 분 32 는 */5 비배수) + `handleDbBackup`: 앱 `backup.ts`(신버전 260610-bkr)와 **byte-identical 포맷**으로 D1 전체 .sql 직렬화(테이블 DROP/CREATE/INSERT + standalone 인덱스/뷰/트리거 덤프 + 100KB oversize 가드) → `STORAGE.put(backups/db/<KST date>.sql)` → 14일 넘은 백업 자동 삭제. 신버전 `restore.ts` 토크나이저로 복원 가능. 오류/oversize 시 telemetry + owner(2022051052) push. ② `uploads/[[path]].ts` — `key.startsWith('backups/')` → 404(STORAGE.get 전). 회귀 0(uploads 로 backups/ 받는 코드 없음, SettingsPanel 은 admin r2-download 사용). **검증**: 양쪽 `tsc` 0 / cha-bio `build` precache 83 / cron `wrangler dry-run` 바인딩(DB+STORAGE) 확인 / cron worker 배포(4 cron 등록) + **로컬 1회 실증**(`wrangler dev --remote --test-scheduled` → `/__scheduled?cron=32+18...` → `backups/db/2026-06-11.sql` 20.9MB 생성, telemetry `cron-backup-db-ok`, 보존정리 0건) / **uploads 차단 실측**(실제 존재 키 미인증 GET → HTTP 404, 차단 없었으면 20.9M 노출) / uploads 차단 prod 배포 ee13bcea. **D1 Time Travel 30일과 병행**(.sql 은 장기보관 + 오프사이트 + restore 입력). 메인 Claude 직접 배포(사용자 '배포 + 지금 1회 실증' 명시 승인 — prod 데이터 읽고 R2 쓰는 실행). **후속(선택)**: backups/db/ 다운로드 UI 를 SettingsPanel 에 노출(현재는 admin r2-download 로 key 지정 다운로드 또는 wrangler). |
| 2026-06-10 | **P1 백업/복원 정상화** — restore 파서 토크나이저 + 단일 트랜잭션 원자 복원 + 오류 표면화 + backup 인덱스/트리거 덤프 (staging cbc7119-data 3d66f6f → prod sync, 260610-bkr) | (staging 검증 `3d66f6f`, main 무관 — 별도 staging repo) | `e784241` (fix) + docs commit | https://cd066d5a.cbc7119.pages.dev | **배경**: prod 안정성 감사(73-agent 워크플로)에서 발견한 P1 — DB 복원이 실제로 동작하지 않으면서 항상 `success:true` 반환(재해 복구 불가). RCA 3종: ① restore 가 `split(';')` 후 `!startsWith('--')` 필터 → 백업의 `-- ── 테이블 ──`+DROP 구조에서 **모든 DROP 이 주석 청크로 흡수돼 스킵**(복원 동작 불능) ② 값 내 세미콜론에서 INSERT 가 두 조각으로 쪼개져 행 유실 ③ 실패를 errors 카운트만 하고 무조건 success:true. 추가로 backup 이 `type='table'` 만 덤프 → 복원 시 인덱스 50개 + 개소명 동기 트리거(`sync_marker_description_on_cp_update`) 전소실. **변경 (staging 2회 라운드트립 검증 verbatim, byte-exact)**: ① `restore.ts` — `splitSqlStatements()` 토크나이저(리터럴 `''`/인용식별자 `""`/`--` 주석/CREATE TRIGGER `BEGIN..END` 깊이 인식) + `[DROP→CREATE TABLE→INSERT→post]` 재정렬(알파벳순 덤프의 자식-먼저 FK 문제 회피) + 단일 `env.DB.batch([PRAGMA defer_foreign_keys, ...ordered])` 원자 트랜잭션(서브리퀘스트 1,000 한도 회피, 전부-성공/전부-롤백) + 실패 시 `success:false`+500+firstError + 구버전 백업(인덱스/트리거 없음) `success:true`+`data.warning`. ② `backup.ts` — standalone 인덱스/뷰/트리거 덤프 섹션(`type IN ('index','view','trigger')`, DROP+CREATE) + 100KB(byte) 초과 INSERT 발견 시 백업 500 차단(복원 불가능 백업 생성 방지). ③ `SettingsPanel.tsx` — restore success toast 직후 `if (json.data.warning) toast(json.data.warning,{icon:'⚠️',duration:8000})` 1줄(prod 프런트 별도 진화 → 통째 복사 X, 1줄만 맥락 삽입). **검증**: staging 라운드트립 — 구 baseline 12,001 stmt 5.7s 11,805행 일치 + 현 prod 데이터 리시드 12,506 stmt 5.9s 12,310행 일치 + 까다로운 값(세미콜론/줄바꿈/대시시작/`O''Brien`/빈문자열/NULL) byte-exact + 트리거 실동작 + 오류/구버전/빈DB(재해복구) 경로 실측 + 17-agent 적대 리뷰 + 토크나이저 8천회 퍼즈. prod: restore.ts/backup.ts diff 0(byte-exact) + tsc 0 + build precache 83 + 배포 Functions bundle 업로드 + **prod D1 SELECT 검증**(인덱스 50 · 트리거 1=`sync_marker_description_on_cp_update`, backup 덤프 대상 확인) + **prod restore POST 미실행**(라이브 덮어쓰기 금지 룰 준수 — 복원 경로 추가 확인은 staging 소방훈련으로 대체). prod 행수 기준(사용자 앱 백업 시 INSERT 행수 비교): check_records 3874 / check_points 901 / extinguishers 471 / markers 1278 / legal_findings 10 / staff 4. **의도된 동작(버그 아님)**: 100KB 초과 텍스트 행 있으면 백업 거부(행 축소 후 재시도) / 구버전 .sql 복원 시 ⚠️ 경고 toast(인덱스·트리거 소실 고지) / 복원 실패 시 "전체 롤백" 에러 표시(가짜 success 제거). **Track B(자동 백업 생산자 신설) 별건**: r2-list 가 전제하는 `backups/r2/<date>.zip` 생산자 부재(cron worker 에 R2 바인딩 없음 + r2-backup 은 다운로드 스트림만) — restore 정상화 후 cron worker 에서 별도 진행 예정 + uploads `backups/` 차단(미인증 노출 P2) 동반. 메인 Claude 직접 배포(서브에이전트 prod 금지 준수). 사용자 앱 백업 다운로드 검증 권장(DROP TABLE / CREATE INDEX 50 / CREATE TRIGGER sync_marker_description_on_cp_update + 테이블별 INSERT 행수 == 위 기준). |
| 2026-06-09 | **소화기 개소명 3-store 동기 가드** — floorplan-markers PUT + check-points PUT (staging cbc7119-data a9791e9 → prod sync, quick 260609-ekw) | (staging 검증 `a9791e9`, main 무관 — 별도 staging repo) | `8fac741` (floorplan-markers PUT) + `a09a44b` (check-points PUT) + docs commit | https://63fffa24.cbc7119.pages.dev | **배경 (사고 260608-b6f 와 동일 종류)**: 소화기 "개소명"이 D1 3곳 중복 저장 — `check_points.location`(일반점검·도면 마커 이름·QR) / `extinguishers.location`(소화기 관리 목록, CP-FE-% 행만 존재) / `floor_plan_markers.label`(마커 모달 입력칸). 두 PUT 경로가 일부만 갱신 → "한 개소가 화면마다 다른 이름". (a) `PUT /api/floorplan-markers/:id` 는 `label` 만 UPDATE, (b) `PUT /api/check-points/:id`(admin 편집기) 는 `check_points.location` 만 갱신 → `extinguishers.location` 미전파. **가드 (staging 선검증)**: cbc7119-data staging(`a9791e9`)에서 tsc+vite build + D1 propagation 테스트(CP-FE-0001 snapshot→mutate→verify→restore, 3-store 전파 확인) 통과한 코드를 **verbatim** 반영. ① `functions/api/floorplan-markers/[id].ts` onRequestPut — 기존 단일 UPDATE 를 `env.DB.batch` 분기로 교체: `newLabel`(빈값/undefined 면 null) 추출 + body 에 없으면 마커 행에서 `check_point_id` SELECT + 마커 UPDATE 를 **항상 batch 첫 statement**(동기 실패해도 라벨은 저장, 악화 방지) + `cpId.startsWith('CP-FE-')` 일 때만 `check_points.location` + `extinguishers.location` push + `stmts.length===1 ? run() : batch()`. onRequestDelete 무변경. ② `functions/api/check-points/[id].ts` onRequestPut(admin) — `if (willDeactivate && isExtCp){...}`(비활성화 cascade DELETE 2건) 끝에 `else if (isExtCp && body.location !== undefined)` 추가 → `UPDATE extinguishers SET location=?, updated_at=... WHERE check_point_id=?`. 상단 UPDATE check_points / SELECT·return 무변경. **범위**: 소화기(`CP-FE-%`) 한정 — 소화전(`-SH`)/완강기(`-WK`)/DIV(`CP-DIV-`)는 extinguishers 행 없음 + DIV 구조적 이름·컴프 페어링 룰로 제외. **검증**: 두 파일 `npx tsc --noEmit` 0 errors / grep 마커(batch·CP-FE-·UPDATE extinguishers SET location·else if·기존 DELETE cascade 보존) PASS / `git ls-files` tracked 확인(메모리 gitignore 유실 사고 전례 반영) / `npm run build` PASS(precache 83) / wrangler 배포 Functions bundle 업로드·Worker compile 성공. **prod 데이터 정리 (§5-4, 사용자 승인)**: 진단 결과 staging 과 **동일 stray 2건** — `CP-FE-0104` label `58510`(개소명 '차케어스 내부 소화전 뒤'), `CP-FE-0105` label `71958`(개소명 '남측 사무실 내부 완강기 옆'), 둘 다 6층 사구역, cp/ext location 은 정상이고 marker.label 만 인증번호 오입력. 가드 라이브 후 이 마커 무수정 저장 시 인증번호가 개소명을 덮을 위험 → 사용자 승인 후 `UPDATE floor_plan_markers SET label=(SELECT location FROM check_points WHERE id=...) WHERE check_point_id IN ('CP-FE-0104','CP-FE-0105') AND plan_type='extinguisher'`(changes=2). 재검증 stray 0건, 3-store(label=cp=ext) 정렬 확인. GSD quick(planner+executor source Edit+atomic commit, **메인 Claude 직접 build+wrangler 배포+prod D1** — 서브에이전트 prod 금지 룰 준수). **UI E2E(§5-5) 사용자 검증 완료** — 앱에서 소화기 개소명 정상 반영 확인(2026-06-09). |
| 2026-06-09 | **P1 핫픽스** — 점검 기록 조치 저장/취소/삭제 API 핸들러 복원 (clone 유실) + gitignore 가드 (quick 260609-cjx) | (production 직접 작업, 잃어버린 핸들러 복원) | `6eb6973` (핸들러 3종 복원) + `f9322f6` (gitignore anchor) + docs commit | https://f6c5bb3f.cbc7119.pages.dev | **증상**: 사용자가 어제(06-08) 유도등 불량 3건 조치 완료했는데 "조치처리실패" 로 저장 안 됨. **RCA (증상보다 큼)**: 프론트가 부르는 `POST /api/inspections/records/:id/resolve`·`/unresolve`·`DELETE /api/inspections/records/:id` 핸들러가 배포본에서 통째로 없음 → **전 카테고리 조치 저장/취소/삭제가 06-05 배포부터 깨진 상태**(유도등이 처음 걸림). 근거: resolved check_records 36건의 마지막이 정확히 `2026-06-02`, 이후 0건. 어제 유도등 3건(`CP-2F/7F-사무동-유도등`, FPM marker) 전부 `status=open`·`resolved_at=null` (조치내역 미저장). 과거 성공분은 resolution_memo/materials_used/resolved_by 정상 → 원래 동작하던 앱 엔드포인트. **근본 원인**: `cha-bio-safety/.gitignore` 의 bare `inspections/` 패턴이 `functions/api/inspections/` 까지 silently untrack → resolve/unresolve/delete 핸들러가 git 에 커밋된 적 없는 untracked 파일이었고, 이 콘솔이 최근 **새로 clone**(reflog clone) 되면서 유실 → 06-05 식단표 배포가 핸들러 빠진 functions 번들 업로드 → 깨짐. (기존 inspections 파일들은 과거 `git add -f` 로 추적됨.) **회복**: ① 핸들러 3종 신규 생성 + `git add -f` 커밋(6eb6973) — resolve(`UPDATE check_records SET status='resolved',resolution_memo,resolution_photo_key,materials_used,resolved_at=datetime('now','+9h'),resolved_by`; 전체 스태프) / unresolve(admin 403 게이트, NULL 복귀) / delete(admin 403, 데이터무결성룰). 프론트 계약 + 과거 36건 shape 로 100% 확정 재구성. ② **가드(재발방지)**: `.gitignore` `inspections/` → `/inspections/` anchor(f9322f6) — 이제 functions 핸들러 정상 추적, 최상위 docs 폴더만 무시. **검증**: utils/api.ts 전수 audit → 누락 라우트 inspections/records/:id 뿐(다른 untracked 유실 없음) / tsc PASS / build PASS(precache 83) / Worker compile 성공 + Functions bundle 업로드 / 새 파일 untrack 안 됨 확인. 메인 Claude 직접 배포. **3건 데이터 복구**: 핸들러 복원·배포로 조치 저장 부활 → 사용자가 앱에서 유도등 3건 직접 재조치(아직 open 이라 조치 목록에 그대로). 사용자 verify 대기. |
| 2026-06-08 | 지하4층 'A-2 기둥' 개소명 3-store 통일 — 마커 라벨 수정 미동기 RCA 회복 (quick 260608-b6f) | (production 직접 작업, **D1 데이터 교정만 — source 무변경, main 무관**) | docs commit (PLAN/SUMMARY + 노트) | (배포 무관 — D1 데이터, 라이브 조회) | **증상**: 지하4층 최근 개소를 'A-1 기둥'→'A-2 기둥' 바꾸려는데 마커 수정 모달엔 이미 'A-2 기둥' 으로 보임(화면마다 다름). **RCA**: 개소명이 3곳 중복 저장 — 개소 `CP-FE-0472` `check_points.location` / 소화기대장 `id=486` `extinguishers.location` / 마커 `FPM-9XjKJyuupOB0` `floor_plan_markers.label`. 생성시(06-04) 3곳 'A-1 기둥' 동기 → 오늘(06-08 01:41) 마커 수정 모달에서 개소명 'A-2 기둥' 변경 시 **`PUT /api/floorplan-markers/:id`([id].ts L21)가 `floor_plan_markers.label` 만 UPDATE**, `check_points.location`·`extinguishers.location` 미전파 → divergence. 표시 로직상 도면/마커 이름은 `cp_location||label`(A-1), 모달 입력칸은 `selected.label`(A-2). `check_records` 0건(미점검)이라 회복 부담 0, qr_code/location_no('지-B4-40') 무영향. **회복**(사용자 승인=마커 기준 'A-2 기둥' 통일): prod D1 `UPDATE check_points SET location='A-2 기둥' WHERE id='CP-FE-0472'`(changes=1) + `UPDATE extinguishers SET location='A-2 기둥', updated_at=datetime('now','+9 hours') WHERE check_point_id='CP-FE-0472'`(changes=1, id=486). 3-store SELECT 모두 'A-2 기둥' 검증. **메인 Claude 직접 prod D1 적용**(서브에이전트 prod 금지). 배포 없음(D1 라이브). **가드(재발 방지)는 staging 분리**: 마커 라벨 수정→개소/소화기대장 동기는 API 동작 변경 → 콘솔 룰상 cbc7119-data staging 에서 검증 후 반영 예정(후보: PUT marker 에서 `label`+`check_point_id LIKE 'CP-FE-%'` 시 두 테이블 atomic 동기, 또는 개소명 정식 편집을 ExtinguishersListPage 로 일원화). 사용자 화면 재확인 시 PWA 캐시면 새로고침. |
| 2026-06-08 | 대시보드 데스크톱 "이번 달 점검 현황" 도넛 6·7·6 재배치 + 행 간격 축소 (GSD quick 260608-1am) | (production 직접 작업, main 무관) | `44c4d40` (fix: donut 6-7-6 + gap-4) + docs commit | https://12223485.cbc7119.pages.dev | **요청**: 데스크톱 대시보드 "이번 달 점검 현황" 박스의 19개 도넛이 7·7·5 로 배치되고 세로 행 간격(`gap-8`=32px)이 넓어 도넛이 박스 상·하단에서 살짝 잘림 → 6·7·6 재배치 + 행 간격 축소 요청. **변경**: `DashboardPage.tsx` 데스크톱 블록(L306–353, Donut size=76) 정확히 2곳 Edit. ① 분배 알고리즘: greedy `perRow = Math.ceil(n/numRows)`(7·7·5, 마지막 줄에 나머지 몰림) → balanced `base = Math.floor(n/numRows)` + `counts = new Array(numRows).fill(base)` + 나머지를 가운데 줄부터 바깥쪽으로 배분(`mid=(numRows-1)/2`, `order = [...counts.keys()].sort((a,b)=>|a-mid|-|b-mid|||a-b)`, `counts[order[k]]++`) + offset slice. n=19 → counts=[6,7,6] (일반화: n=16→[5,6,5], n=22→[5,6,6,5] — 나머지가 항상 가운데로). ② 컨테이너 행 간격 `gap-8`(32px) → `gap-4`(16px). **모바일 블록(L651–695, `flex-nowrap` 가로 스크롤, Donut size=44) 무변경** (메모리 룰 "월간 도넛은 가로 스크롤" — git diff 격리 검증 PASS). `justify-center`/`overflow-y-auto`/Donut size 76/라벨·카운트 markup/doubleCycle 분기 전부 보존. **사전 시안 승인**: 실제 Donut 지오메트리·토큰값으로 before(7·7·5,gap32)/after(6·7·6,gap16) HTML 시안(`.planning/quick/260608-1am-dashboard-donut-row-rebalance/sketch.html`) 브라우저 표시 → 사용자가 행 간격 `gap-4`(16px) 선택·승인 후 적용 (메모리 룰 `feedback_design_sketch_first`). **검증**: PASS-GAP(gap-8 0건/gap-4 존재) / PASS-ALGO(perRow 0건/`counts`+offset slice 존재) / PASS-MOBILE-UNTOUCHED(`size={44}`·`flex-nowrap` diff 미등장) / `tsc --noEmit` PASS / `npm run build` PASS(precache 83). diff = 단일 hunk(@@ -311,17 +311,21 @@), `return rows.map` 이하 렌더링 JSX 무변경. **GSD quick** (planner+executor 서브에이전트가 source Edit+atomic commit, **메인 Claude 가 직접 build+wrangler 배포** — 서브에이전트 prod 배포 금지 룰 준수). 데이터/스키마/API 무관 순수 클라이언트 시각 변경 → staging 불필요. 사용자 데스크톱(1920×1080) 시각 검증 대기. |
| 2026-06-05 | 식단표 PDF 파서 비정상 파싱 감지 가드 (cbc7119-data staging → prod sync, GSD quick 260605-1ql) | (staging 검증 `dc981f0`, main 무관 — 별도 staging repo) | `7debf75` (feat: 가드 3종) + docs commit | https://2a26f36f.cbc7119.pages.dev | **사고 배경**: 식단(weekly_menus) 데이터가 주중 공휴일로 PDF 한 열이 비면 그 이후 요일 메뉴가 한 칸씩 밀려 엉뚱한 날짜에 저장되던 사고(6/1~6/5 6/3 선거, 5/4~5/8 5/5 어린이날). 현재 파서는 헤더 살아있는 케이스 정상 처리하나 **깨진 파싱 결과가 정상 데이터를 silent overwrite** 하는 방어선 부재. **변경**: `StaffServicePage.tsx` `handleMenuUpload` 의 menus 생성 직후·`menuApi.upsert` 직전 3종 가드 — `extracted`(빈칸 필터 전, `dow` 포함) 추출 → `menus`(빈칸 필터 + dow strip)로 분리(upsert payload 무변경). ① 비-공휴일 평일(월~금) 중식A·중식B·석식 전부 공란 → 비는 날짜 표시 throw. ② `menus.length < expectedDays`(평일·공휴일 제외) → 인식 일수 표시 throw. ③ weekdayCols x 간격 한 칸이 중앙값 ~1.8배↑ → 헤더 통째 누락 의심 throw. 셋 중 하나라도 throw → 기존 try/catch 가 `toast.error` 로 차단 + upsert 미도달. **검증**: tsc PASS + build PASS(precache 83) + pdfjs(3.11.174 legacy) 앱 파서+가드 1:1 재현 시뮬 — 정상(6/1~6/5·5/4~5/8 공휴일 빈열, 헤더 살아있음) 오발동 0 / 음성통제(6/3 평일 취급) ① 정확히 발동 / 라이브 청크 `StaffServicePage-D5J5oudl.js`(이전 CmIKWmQa) 가드 마커 존재. **confirm 우회 미포함**(습관적 클릭 사고 방지, staging 동일). **사전 점검 보정**: 노트 기준 c6f3432 ↔ HEAD 46fdb6c 차이 = 260602-3p6 ExtinguisherPublicPage width fix(source-only, 파서 무관) + 문서뿐 — 폐기작업 아님, 이번 배포에 함께 포함. 메인 Claude 직접 배포(서브에이전트 prod 배포 금지). **재업로드 데이터 교정은 별건**(사용자 직접 진행 예정). |
| 2026-06-01 | 인수인계장/업무리스트 데스크톱 디자인 룰 정렬 (디자인트랙 → production cherry-pick) | `9d73f73` + `21ccc1c` (origin/main, source-only) | `71bc10a` + `c6f3432` (cherry-pick) | https://ce4370e5.cbc7119.pages.dev | 디자인 콘솔(cbc7119-design) 핸드오프 2 commits cherry-pick. **9d73f73 → 71bc10a**: status 배지 하드코딩색(`rgba(34,197,94,.15)`/`#16a34a`/`rgba(59,130,246,.15)`/`rgba(239,68,68,.15)`) → semantic 토큰(`--status-safe-bg`/`--status-safe-bar`/`--status-warning-bg`/`--status-warning-bar`/`--status-danger-bg`/`--status-danger-bar`). **모바일 배지도 변경**(의도): 대기 파랑→주황(warning 통일), 완료 `✓`(U+2713 이모지)→`<Check size={12}/>` lucide. 데스크톱 인계장 그리드 `maxWidth:1200 + margin:0 auto` 중앙정렬(minmax 290→300, gap 2.5→3). HandoverPage 16줄 + WorkListPage 4줄(삭제됨 배지 토큰화 모바일/데스크톱). **21ccc1c → c6f3432**: WorkList 데스크톱 비밀번호 `<th>` 의 `display:inline-flex` 제거(th 자체에 flex 주면 열 정렬 깨짐) → 내부 `<span>` 으로 이동. 로직 변경 0. **cherry-pick 충돌 0**(base 동일 확인). **verify 전부 PASS**: `grep ✓`=0 / 하드코딩색(rgba34,197,94\|#16a34a)=0 / `maxWidth: 1200`=1 / `TH, display:'inline-flex'`=0 / check-emoji EMOJI-0(두 파일) / tsc PASS / build PASS(precache 83). 모바일 배지 색 변화는 직원 도메인 모바일에서도 정상 노출(모바일·데스크톱 일관성 의도). 사용자 시각 검증 대기. |
| 2026-06-01 | 인수 인계장 + 업무 관련 리스트 데스크톱 레이아웃 신규 (GSD quick 260601-n1s) | (production 직접 작업) | `9e51713` (emoji script) + `973c31a` (Handover) + `03e18cd` (WorkList) + `563cc62` (docs) | https://89720555.cbc7119.pages.dev | 두 신규 페이지(2026-05-30 신설)가 모바일 단일 컬럼만 있어 PC 에서 가운데가 휑함 → 데스크톱(≥1024px) 레이아웃 신규 추가. 시안 HTML 사용자 컨펌 후 구현(수정 2건: 인계장 상세 모달 별도 제목칸 없음→title 있으면 본문 위 / 아이콘 모바일 lucide 그대로). **인수 인계장**: `repeat(auto-fill,minmax(290px,1fr))` 카드 그리드(좌측 상태바 대기 warning-bar/완료 safe-bar, Pin 고정 상단, 제목 2줄·본문 3줄 클램프, 하단 상태배지+첨부수+작성자·시각) + 카드 클릭 시 중앙 상세 모달(본문 전체+첨부 미리보기+완료 Check/고정 Pin/수정/삭제 Trash2/이력 History). `DesktopHandover`+`HandoverEditModal`. **업무 관련 리스트**: 탭(비밀번호/연락처)+검색+전체 마스킹토글(Eye/EyeOff)+추가 툴바 + 테이블(thead sticky, hover, 비밀번호 monospace+행별 Eye, 메모 ellipsis, 탭별 컬럼). `DesktopWorkList`+`DesktopWorkListRow`, 마스킹 룰 `showValue=revealAll\|\|revealed\|\|tab==='contact'` 보존. **구현 안전성**: 각 페이지 `useIsDesktop()`(1024px) + `if(isDesktop) return <Desktop.../>` **순수 추가** — git diff 삭제 라인 0(모바일 return·기존 컴포넌트·스타일상수 바이트 단위 무변경). 새 API 0 / 새 useQuery·useMutation 키 0(기존 재사용) / App.tsx 무변경(DESKTOP_HEADER_HIDE_PATHS 그대로 → 이중 헤더 없음). 데스크톱 추가분 이모지 0(lucide 만). 신규 dev 스크립트 `scripts/check-emoji.cjs`(이모지 게이트 재사용). tsc PASS + build PASS(precache 83). **사용자 PC(1920×1080) 데스크톱 시각 검증 대기.** |
| 2026-06-01 | 데스크톱 점검 관리 카드 완료 집계 모바일 일치 (버그 수정, GSD quick 260601-ehn) | (production 직접 작업) | `64e7200` (refactor) + `637dfce` (fix) + `b15bf89` (docs) | https://3d3f37d5.cbc7119.pages.dev | **버그**: 데스크톱 `DesktopInspectionView`(InspectionPage.tsx) 의 `groupCounts` 가 완료를 고유 위치 수(`uniqueSites` = `zone\|floor\|location\|category`)로 셈 + 막대 색을 `getCatBarClass(레코드수, 고유위치수)` 처럼 단위 다른 분자/분모로 계산 → 특별피난계단 체크포인트 50개가 고유 위치 19개로 접혀 "점검완료 19" + 주황 막대(19/50≈38%, 사용자는 빨강 인지), 50개 다 점검해도 100%(초록) 불가능. DB 검증: `check_points category='특별피난계단'` cp_total=50 / DISTINCT zone\|floor\|location=19. 모바일은 체크포인트 단위(isCpCompleted)라 50/50 초록 — 화면 불일치. 메모리 룰 project_inspection_completion_rule 위반. **수정**: 모바일 그리드(L5163~5186)의 per-category total/doneCnt 계산(유도등 마커 · DIV/컴프 반월 사이클 · `computeCardCompletion`)을 모듈 최상위 순수 함수 `computeCategoryCounts(g, ctx)` 로 추출(L125) → 모바일 그리드(L5202)는 그 함수로 **무회귀** 치환, 데스크톱(L5790)은 부모가 이미 보유한 입력 5개(allCheckpoints/scheduleItems/markerRecords/monthRecordDates/glMarkerCount)를 props 로 받아(early return 이 hook 이후 → 추가 쿼리 0) 동일 함수 사용. bad/caution/open + 우측 내역/상세 패널은 기존 `remediationApi.list` 레코드 그대로 유지. `uniqueSites` 완전 제거. **검증**: tsc PASS + build PASS(precache 83) + computeCategoryCounts 호출 2곳 + uniqueSites 0 + getCatBarClass 정의 무변경 + props 전달 확인. 단일 파일 InspectionPage.tsx. **결과**: 특별피난계단 데스크톱 카드 = "점검완료 50" + 초록(bg-safe-bar), 유도등/DIV/컴프도 모바일과 동일. 사용자 데스크톱 시각 검증 대기. |
| 2026-06-01 | 승강기 관리(ElevatorPage) 디자인 클린업 4건 (디자인 콘솔 cbc7119 제안 이관) | (디자인 콘솔 제안, 운영 인라인-style 버전에 맞춰 재패치) | `f168015` | https://16c59ebf.cbc7119.pages.dev | 디자인 콘솔에서 넘어온 cosmetic 클린업. **①** 데스크톱 헤더 "수리 기록" 버튼 `linear-gradient(135deg,#854d0e,#eab308)`(노랑) → `var(--accent-active),var(--accent)`(파랑). **근거**: 같은 버튼 모바일 버전(L1753)이 이미 accent 파랑 → 데스크톱만 outlier. 도면점검 조치완료도 파랑계열(marker `#3b82f6` / 칩 `info`). 빨강 "고장 접수"(`#991b1b,#ef4444`) 4곳 무변경. **②③④** 닫기/삭제 `✕` 글리프 → lucide `<X>` (검사성적서 모달 20 / 이미지뷰어 흰색 22 / 고장삭제 14, `display:flex` 중앙정렬). **제안서 함정**: cbc7119 제안 old_string 은 `className="bg-none..."`(Tailwind) 기준인데 운영 ✕ 3개는 아직 인라인 `style={{}}` → 그대로면 ②③④ no-op. 운영 버전에 맞춰 재작성 적용. `X` import 기존재. 검증: `grep ✕`/`#854d0e` 0건 + 빨강 gradient 4곳 보존 + tsc PASS + build precache 83. 사용자 미리보기 HTML 시안 승인 후 적용. |
| 2026-05-27 | submission-ppt W1 — 점검 카드 다운로드 2버튼 | (production 직접 작업, main 무관) | `cc57711` | https://9caaecef.cbc7119.pages.dev | LegalPage 카드 하단에 결과내역서/지적조치사진 다운로드. 후자 항상 disabled (W2 이후 활성). 사용자 검증 완료. |
| 2026-05-27 | submission-ppt W2 — DB 0087 + lock guard | (production 직접 작업) | `e0659bf` | https://ace4dfdc.cbc7119.pages.dev | 4 컬럼 추가 (submission_status / ppt_file_key / submission_selected / submission_label). API GET 응답 확장 + mutation lock 가드 추가. 시각 변화 없음. prod D1 fire 카드 2건 모두 submission_status='pending' default 확인. |
| 2026-05-27 | submission-ppt W3 — 1열 토글/저장 + strip 색 단일화 | (production 직접 작업) | (push hook auto) | https://93b9530e.cbc7119.pages.dev | 1열 카드 우측에 제출 미완료/완료 토글 + 저장. admin only. lock 시 🔒 종결. strip 색 = submission_status 기반 (완료=safe, 미완료=warning). |
| 2026-05-27 | submission-ppt W4 — 2열 헤더 정리 + 내부용/제출용 탭 + 결과내역서 업로드/삭제 | (production 직접 작업) | (push hook auto) | https://4df0dedb.cbc7119.pages.dev | FindingsPanel 헤더 단순화. 탭 분리. 1열 카드 결과내역서 버튼 = 업로드/다운로드 분기 + X 삭제 (admin+!locked). PATCH null 처리. |
| 2026-05-27 | submission-ppt W5 — 제출용 탭 (체크 + 라벨 + 자동저장) + textarea 커서 fix | (production 직접 작업) | (push hook auto) | https://2ce0ba11.cbc7119.pages.dev | SubmissionTabPanel 신설. 500ms 디바운스. 사진 부족 시 저장 disabled. localLabels 유지로 커서 점프 사고 해결. |
| 2026-05-27 | submission-ppt W6 — 3열 PPT 미리보기 UI | (production 직접 작업) | (push hook auto) | https://fcb13cea.cbc7119.pages.dev | SubmissionPreviewPanel 신설. 표지 + 본문 슬라이드 + 페이지 네비. 사진은 R2 실제 미리보기 (a4 가로 비율). |
| 2026-05-27 | submission-ppt W7 — 양식 슬림화 + R2 + 서버측 PPT 생성 (표지) | (production 직접 작업) | (push hook auto) | https://89aa8a8b.cbc7119.pages.dev | 양식 PPTX 1.17MB (slide1+slide2) R2 업로드. fflate 로 서버측 생성 (JSZip 호환 안 됨). 표지 동적 ({YYYY}년 {종합정밀/작동기능}점검). 클라이언트 10초 자동저장. |
| 2026-05-27 | submission-ppt W8 — 본문 라벨 + 사진 임베딩 + 페이지 복제 + 다운로드 파일명 | (production 직접 작업) | (push hook auto) | https://1409a619.cbc7119.pages.dev | slide2 4 라벨 셀 패치. 페이지 복제 (ceil(N/2)). 사진 페이지별 R2 GET → media/image{N}.jpeg 추가 + rels redirect. rot 회전 제거. 다운로드 파일명 {YYYY.MM)차바이오... 형식. |
| 2026-05-27 | submission-ppt W9 — 결과내역서 와치독 API + ps1 패턴 | (production 직접 작업) | (push hook auto) | https://4b396dd0.cbc7119.pages.dev | 신규 API /api/legal/upload-report (admin only, 매칭 + reportFileKey 갱신). watchdog.ps1 GROUPS 에 legal_report/legal_ppt 추가. Process-LegalReportPdf = R2 업로드 + 매칭 + 폴더 이동. ps1 사용자 Windows 컴퓨터에 별도 복사 필요. |
| 2026-05-27 | submission-ppt W8c — pic geometry + right-column null guard | (production 직접 작업) | `ab6a45c` | https://aefeba6a.cbc7119.pages.dev | slide2 picture 4개 모두 셀 좌표(CELLS)로 xfrm 재작성. rot="5400000" 제거 + rId6 portrait ext (2700000×4428000) → landscape (4428492×2700000) 보정. `<a:stretch/>` → `<a:stretch><a:fillRect/></a:stretch>` 명시화로 세로사진 4귀퉁이 강제 fit. page.right=null 일 때 rId5/rId6 picture shape 제거 — 홀수 finding 마지막 페이지의 우측 템플릿 잔존 사고 차단. 사용자 검증 완료 후 origin push 완료 (`cba84ca`). |
| 2026-05-27 | tb3-03 LegalPage 디자인 sweep — emoji 8곳 → Lucide + 색 토큰 4곳 | `47b9088` | `4baf5d2` | https://6d353eb9.cbc7119.pages.dev | LegalPage.tsx 14+/14- 단일 hunk. emoji ✓/✗/🔒/💾 → Check/X/Lock/Save lucide. 저장버튼 옵션 B (bg-warning-bg text-warning outline). pill border 제거 (옵션 1). L538 string→JSX fragment (방법 A, ReactNode 자연 widen). inline style 141곳 무침범 (phase B 별도). cherry-pick 깔끔 (충돌 0). |
| 2026-05-28 | bug-fix batch — WorkShift Excel + 제출용 탭 드래그 순서 | (production 직접 작업) | `57fed69` | https://1da4f3b7.cbc7119.pages.dev | **Bug 1**: utils/generateExcel.ts 의 양식 patch — B4-B7 (title) + C4-C7 (name) 도 staffRows 순서로 패치 (이전엔 양식 하드코딩 순서 그대로 → 김병조 shift 가 박보융 행에 들어가는 사고). 양식 workbook.xml 의 `<sheet name="12월">` + definedName 도 실제 월로. **Bug 2** (W10 sketch s6 v3): migration 0088 `submission_order` 추가 + remote prod 즉시 적용 (rows_written 8). 신규 API `PUT /api/legal/:id/submission-order` batch renumber. SubmissionTabPanel 체크박스 제거 → 드래그&드롭만으로 PPT 포함/순서 변경. 4컬럼 그리드 (handle/badge/textarea/save-btn) + 빈영역 + 구분선. PPT hint textarea 우측 모서리, 인디케이터 save-btn 좌측 모서리. 미선택 카드 textarea readonly. 미선택→선택 시 자동 `#N+1` 부여, 언체크 시 자동 renumber. generate.ts `ORDER BY submission_order`. optimistic update via React Query. 8 파일 변경 + 2 신규 (migration + API endpoint). |
| 2026-05-27 | color audit tier1 — 비표준 bg-{status} 7곳 → -bar | `7031815` | `f5117b4` | (배치 끝에 deploy) | InspectionPage 3 + DivPage 2 + ElevatorPage 1 + LegalFindingDetailPage 1. 솔리드 fill 용도는 -bar 변종 (text-on-accent 흰글 fill). cherry-pick 충돌 0. |
| 2026-05-27 | color audit tier1 followup — LegalPage 결과내역서 X 빨강 | `df0f99d` | `94f31bc` | (배치 끝에 deploy) | LegalPage.tsx L1073 회색 X (bg-surface-active text-text-secondary border-strong) → 빨강 (bg-danger-bar text-text-on-accent border-0) — 같은 페이지 다른 사진제거 X 패턴 일관. cherry-pick 충돌 0. |
| 2026-05-27 | color audit tier2 — border-{status} 57곳 → border-{status}-bar | `1270ce7` | `068a7c8` | https://c92fc8e3.cbc7119.pages.dev | InspectionPage 44 + FloorPlanPage 10 + DashboardPage 2 + SchedulePage 1. perl lookahead (?![-\w]) 로 -bar/-bg variant 보호. 시각 변화 미미 (border 살짝 더 saturated). 3건 배치로 단일 deploy. cherry-pick 충돌 0. |
| 2026-05-28 | settings 테마 wire + 데드 코드 정리 | (production 직접 작업) | `12b1220` | https://75f6e539.cbc7119.pages.dev | SettingsPanel 화면 섹션: 테마 select 에 utils/theme 의 getThemePreference/setThemePreference 연결 (다크/라이트/시스템 실동작). 주간 현황 기준/결과 즉시 저장 Row 제거 (dead UI). SideMenu MenuItem.soon 필드 + 22 항목 + 준비중 배지 분기 제거. DesktopSidebar NavItem.soon prop + 분기 제거. FloorPlanPage PLAN_TYPES.ready + planReady + 준비중 배지 + 도면 준비 중 fallback 제거. AdminPage.tsx 삭제 (라우팅 없음). 5 파일 60+/94-. |
| 2026-05-28 | watchdog R2 업로드 multipart 1101 fix | (production 직접 작업 — Windows ps1 only) | `47e6f23` | (배포 영향 0 — Windows 와치독) | Process-LegalReportPdf 의 multipart 본문에 folder 필드 part 를 추가했더니 PDF 바이너리가 file part 의 본문 자리 밖에 매달려 Cloudflare Worker formData() throw → Error 1101. Menu PDF 와 동일한 단일 file part 패턴으로 복귀 + ftr 앞 CRLF 추가 (RFC 2046). 사용자 Windows 머신에 ps1 재복사 + 와치독 재시작 필요. |
| 2026-05-28 | watchdog simple-mode 경로 시도 후 revert | (production 직접 작업 — Windows ps1 only) | `0fab4b8` + `b7d8e74` | (배포 영향 0) | 0fab4b8: legal_report/legal_ppt 에 parent="소방점검조치" 추가 → 4단 깊이 (group/parent/label/year/month). b7d8e74: 사용자 요청에 따라 원복 (기존 3단 group/label/year/month). |
| 2026-05-28 | submission-ppt PPT 복구 다이얼로그 fix | (production 직접 작업) | `c6bb65d` | https://66773b14.cbc7119.pages.dev | **원인**: 양식 template.pptx 가 8-slide → 2-slide 슬림화 잔존 — presentation.xml.rels 에 rId4~rId9 (slide3~slide8 dangling, slide6~8 미존재). [Content_Types].xml 에도 slide3~8 Override. 우리 코드가 rId101+ 더 얹어 duplicate target → PowerPoint 손상 플래그. **수정**: (1) presentation.xml.rels 의 slide rels 모두 strip 후 actualSlideCount 만큼 신규 rId 재발급. (2) sldIdLst 전체 재구성. (3) Content_Types slide Override strip + 재추가. (4) 복제 슬라이드의 slide2.rels notesSlide rels 제거 (notesSlide1 ↔ slide2 bidirectional 보존). 추가: R2 의 양식 template.pptx 자체를 cleaned version 으로 교체 + template-backup-260528.pptx 백업. |
| 2026-05-28 | floorplan 소화전/완강기 cp 자동생성 + 8-1F 8호실 SQL 복구 | (production 직접 작업) | `31117bf` | https://4cb87127.cbc7119.pages.dev | **사고**: 도면점검 페이지에서 소화전 마커 추가 시 cp_id NULL 로 orphan 마커 생성 → 일반점검 미노출 + 점검하기 비활성. 사례: FPM-XboG1REDINMI (8-1F 8호실 소화전). **복구 SQL** (admin 컨펌 후): INSERT check_points CP-8-1F-1-SH (QR-CP-8-1F-1-SH, office, 8호실) + UPDATE 마커 cp_id 링크. **재발 방지**: /api/floorplan-markers POST 에서 marker_type ∈ {indoor_hydrant, descending_lifeline} 이고 cp_id 없으면 floor+category 의 location_no 끝 숫자 +1 자동 신규 cp INSERT 후 마커에 링크. CP-{floor}-{N}-SH / -WK + QR-CP-{floor}-{N}-... + location_no {floor}-{N}. DIV (페어링 별도) + 소화기 4종 (자산 분리 흐름) 은 영향 없음. |
| 2026-05-28 | floorplan 편집 모달 '소화기 배치' 버튼 가드 | (production 직접 작업) | `6d0b69a` | https://d4aee5b4.cbc7119.pages.dev | 도면점검 (소화기·소화전) 페이지의 마커 편집 모달이 `planType === 'extinguisher'` 만 검사해서 소화전/완강기/DIV 마커에서도 "소화기 배치" 버튼이 노출되던 버그. `EXT_ASSET_MARKER_TYPES.has(selected.marker_type ?? '')` 추가 가드 — 소화기 4종 (fire_extinguisher / ext_powder20 / ext_halogen / ext_kitchen_k) 일 때만 배치/분리 액션 섹션 노출. 점검 모드 미배치 모달 (L774) 과 일관성 회복. 5+/2-. |
| 2026-05-29 | **ROLLBACK** 260529-gcj CheckpointsPage 레지스트리 추출 1차 사용자 요청 취소 | (revert of `ff61eb6` + `e5d5e15` + `69f70b3` + `40643c2` + `e40b6ac`) | `62d947c` (revert commit, no force-push) | https://37ba8635.cbc7119.pages.dev | 짧게 배포되었던 (https://90cfe007.cbc7119.pages.dev) 방화셔터 registry entry + RegistryDrivenForm + AddCheckPointRouter + initialCategory prop 모두 git revert 로 원복. force-push 미사용 (history 보존, revert commit 추가 방식). 직원 도메인 = 6d0b69a 시점 동등 상태로 재배포. `checkpointRegistry.ts` 파일 삭제 / `CheckpointsPage.tsx` 원복 (696 lines, 변경 전과 동일) / `STATE.md` & `production-sync.md` 원복 / `.planning/quick/260529-gcj-*` PLAN+SUMMARY 삭제. **교훈**: 사용자가 "비주얼적으로만 보여줄거면 html로 만들어서 브라우저에 띄워주던지" 명시 — 다음 시도 시 PWA 직접 변경 전 sketch HTML 시안 단계 추가 필요 (memory `feedback_design_sketch_first`/`feedback_redesign_sketch_rule_enforcement` 패턴 적용). |
| 2026-05-29 | CheckpointsPage 완전 폐기 + cbc7119-data staging 영구화 | (production 직접 작업) | `866de44` | https://7cd4145b.cbc7119.pages.dev | **결정**: 소방 시설 추가 페이지가 19 카테고리의 룰을 하나의 generic 폼에 우겨넣은 구조라 데이터 정합성 사고(유도등 list↔create mismatch / 소화전·완강기 orphan / DIV id 패턴 위반 / canSave validation gap / next-seq 문자열 sort 함정) 다수. 레지스트리 추출 시도(260529-gcj wave) 도중 사용자가 "각 항목별 페이지에서 처리되는 게 더 자연스럽다 + 추가될 수 있는 시설이 많지 않다" 결론. 페이지 자체 폐기. **제거**: `App.tsx` lazy import + /checkpoints 라우트 + page title 3건 / `SideMenu.tsx` '소방 시설 추가' menu item (admin role) / `DesktopSidebar.tsx` '시설 관리' 섹션의 /checkpoints / `src/pages/CheckpointsPage.tsx` 파일(696 lines) 삭제. **카테고리별 추가 경로 보존 확인**: 소화기 → ExtinguishersListPage + 도면 마커, 소화전/완강기/유도등 → FloorPlanPage 도면 마커 (자동 cp 생성 흐름 31117bf+260529-kcs), DIV/컴프 → DivPage + 도면, CCTV → CctvInfoPage, 그 외 단일/희소 카테고리(자탐/소방펌프/방화셔터/청정소화약제/특별피난계단/배연창/회전문/주차장비/소방용전원공급반/전실제연댐퍼/연결송수관/비상콘센트) 는 직접 등록 일 거의 없음 — 필요 시 SQL 직접 또는 향후 별도 admin 도구. **신규 인프라 — cbc7119-data Pages project 영구화**: 직원 도메인(cbc7119) 외에 데이터 트랙 staging 영구 도메인(cbc7119-data.pages.dev) 영구 유지. 별도 D1 (cha-bio-db-staging) + R2 (cha-bio-storage-staging) + JWT_SECRET(분리). 디렉토리: `/Users/jykevin/Documents/cbc7119-data/` (별도 git repo). 이후 모든 데이터 룰/스키마 변경은 staging 먼저 검증 후 production 적용 (디자인 트랙의 cbc7119-preview 와 미러). prod D1 → staging D1 재시드 스크립트: `cbc7119-data/scripts/db-reseed-from-prod.sh` (PRAGMA foreign_keys=OFF 패치 포함, 인터랙티브 확인 필요). **빌드 + 배포**: tsc PASS, dist precache 81 entries(전 82). 사용자 staging 시각 검증 통과 후 production 적용. **memory 박제 후보**: cbc7119-data staging 인프라(D1/R2/Pages project/seed script) — 추후 데이터 트랙 작업 시 항상 참조. |
| 2026-05-30 | 인수 인계장 + 업무 관련 리스트 신규 도입 (사이드바 근무·복지 섹션) | (production 직접 작업, cbc7119-data staging 사전 검증 완료) | `1002a9e` (feat: handover + work-list) + production-sync commit | https://b2a2e0f3.cbc7119.pages.dev | **신규 페이지 2종** (`/handovers`, `/work-list`). 사용자 요청: 인수 인계장 (수시 작성/삭제 + 이력 검색) + 업무 관련 리스트 (탭으로 비밀번호/연락처 구분). **DB 0089 + 0090**: handovers + handover_revisions(타임캡슐) + handover_attachments + work_list_items + work_list_revisions 5 테이블 신설 / handovers.pinned boolean 분리 + work_list_items/revisions.extra 컬럼. **HandoverPage**: 제목 optional + 본문 + 사진/PDF 첨부 (R2 cha-bio-storage 업로드 → handover_attachments 연결) + 상태 모델 (waiting/done + pinned boolean 직교, 고정은 대기에서만 의미) + 검색 (소프트삭제 결과에 회색+'삭제됨' 라벨) + 수정 이력 모달 '타임머신' (시간순 snapshot + 비-삭제 시점 클릭 복원 — 누구나 가능, audit). **WorkListPage**: 비밀번호 탭 (항목명 / 아이디 선택 / 비밀번호 / 메모 선택, 카드에 `아이디:` `비밀번호:` 라벨 명시 + 마스킹 토글) / 연락처 탭 (항목명=회사·소속 / 이름 / 직책·직급 선택 / 전화번호 / 메모 선택). 본인 가드: 본문 수정/삭제 = staff_id 일치 (handover 상태 토글은 누구나). 모든 변경 revisions auto-snapshot. 삭제는 soft delete. **사이드바**: 모바일 SideMenu / 데스크톱 DesktopSidebar 둘 다 '근무·복지' 섹션에 업무 관련 리스트 → 인수 인계장 → 연차 및 식사 → 보수교육 순서 정렬. **DEFAULT_SIDE_MENU**: 신규 path 등록 + 기존 `/checkpoints` (이미 폐기) 제거. **migrateLegacyMenuConfig 위치 인식 forward-merge 수정**: 기존 `[...existing, ...missing]` append 가 '시스템' divider 뒤로 잘못 배치되던 버그 — DEFAULT 상 늦게 오는 첫 entry 직전에 splice 삽입하는 방식으로 변경. 기존 사용자 config 위치 보존 + 새 항목은 DEFAULT 순서대로 자연 정렬. **prod D1 migration 적용 확인**: tables=47 / handovers + handover_revisions + handover_attachments + work_list_items + work_list_revisions 5건 SELECT 통과. **build PASS** (precache 83 entries). 사용자 시나리오 검증 대기. |
| 2026-05-29 | 260529-vwc 일지/Excel/제출용 탭 층 양식 통일 자동 기입 | (production 직접 작업) | `a134d92` + `1c65bdd` (merge) | https://c0b1f587.cbc7119.pages.dev | **룰**: 지상층 = `{zone 한국어} {DB floor 코드}` (예: 연구동 3F, 사무동 8-1F), 지하층 = `B{N}F` (zone 무시), '전층' 자연어 seed 보존. **변경**: (1) `cha-bio-safety/src/utils/formatFloorLabel.ts` 신규 — (floor, zone) → 새 양식. B 패턴 정규식 분기 + ZONE_KO 매핑 4종 (office/research/common/basement) + 빈/M floor 안전 fallback. (2) `dailyReportCalc.ts` 조치완료 블록 — 기존 유도등 vs 비유도등 분리 빌더 (`[zoneKo, floor, spot]` / `[floor, loc]`) 를 헬퍼 단일 호출로 통일. ZONE_KO 로컬 정의 제거. (3) `generateExcel.ts` DIV_NAMES 35 entry 모두 새 양식 (예: `8층 연구동 공조실` → `연구동 8F 공조실`, `B3층 휀룸1` → `B3F 휀룸1`, 9-3 의 `8층 계단위 PS실` → `사무동 8-1F 계단위 PS실` — DIV_POINTS floorLabel='8-1층' 기준 보정, 2-2 의 zone 누락은 `2F PS실` 로 보존). (4) `LegalPage.tsx` labelFor — `f.location` 텍스트 시작이 `B[1-5]` 패턴이면 `B[1-5]F` 자동 변환 정규식 (`/^(B[1-5])(?![0-9F])/` → `$1F`, 이중 F 방지). 지상층은 zone 정보 부재로 자동 변환 X (사용자 컨펌). 다른 location 표시 자리 (KVRow 위치, zip 파일명) 무손상. **보존**: DB schema 0 / SchedulePage MEMO seed ("전층 X 점검", "2층 및 B1층 ...") 0 / dailyReportCalc INSPECT_FULL_CONTENT seed 0 / DIV_POINTS.floorLabel 0 / finding 생성 폼 UI 0. **deviation**: plan 의 `PS_LABELS` 명칭이 실제 코드의 `DIV_NAMES` 와 불일치 — 같은 35 entry 데이터, executor 가 정확한 이름 식별 후 적용. 사용자 시나리오 A/B/C/D 검증 대기. |
| 2026-05-29 | 260529-lzh CP-B3-7-BC 누락 record 회복 INSERT + FloorPlanPage paired BC fetch race 가드 | (production 직접 작업) | `202e3e0` + `276065d` (merge) | https://872d6981.cbc7119.pages.dev | **DB 회복**: 5월 종합정밀 사이클에서 비상콘센트 cp 중 단 1건 (CP-B3-7-BC, 지하 3층 D-7 기둥) 만 점검 누락 — 짝꿍 CP-B3-7-SH 는 오늘 13:49:09 정상 점검됨. 같은 staff 의 5월 29일 record 시간순 분석상 다른 모든 SH+BC 짝꿍은 같은 second/1초 차이로 INSERT 됐는데 B3-7 만 단독. 데이터/API/매칭 룰 모두 정상이라 클라이언트 fetch race 가 유일한 그럴듯한 원인. INSERT id `8RbM9X2kYatSgHEtZX4ay` 로 SH 짝꿍 (session `nyD6soJiiNYaOE4cZeMHw`, staff 2022051052, checked_at 2026-05-29 13:49:09, result normal, status open, floor_plan_marker_id NULL) 와 동일 mirror — 4-28 id1 패턴. 중복 가드 SELECT 후 INSERT (changes=1). 검증: 5월 비상콘센트 미완료 0건 / check_records total 3759→3762 (우리 INSERT 1 + 그 사이 사용자 점검 신규 2건). **Code 가드**: `cha-bio-safety/src/pages/FloorPlanPage.tsx` 에 `pairedBcLoading` state 추가 + paired BC fetch useEffect 의 시작 (`setPairedBcLoading(true)`) / 완료/실패/cleanup (`setPairedBcLoading(false)` 6 hits) 모두 반영 + 모달 닫힘 effect 에도 false / 저장 button disabled 조건에 `(planType==='extinguisher' && marker_type==='indoor_hydrant' && cp_id && pairedBcLoading)` 추가 / button label 분기에 '비상콘센트 확인 중...' 표시 추가. 5 Edit, 10 줄 수정. paired BC 매칭 룰 (location_no 매칭) 변경 0, isExtCascade/DELETE 등 다른 영역 무손상. tsc PASS. **재발 방지**: 모바일 + 지하 + 약한 네트워크 환경에서 fetch 가 완료되기 전 사용자가 빠르게 저장 누르면 BC 누락되는 race 차단 — 도면점검 indoor_hydrant SH 마커 진입 시 fetch 끝날 때까지 저장 disabled. 사용자 시나리오 A/B/C 검증 대기. |
| 2026-05-31 | 대시보드 doubleCycle 도넛 중앙 % 색 two-lap 룰 (디자인 트랙 검증건 이관) | (디자인 트랙 cbc7119-design 검증·승인) | `9291441` (fix) | https://66ed5109.cbc7119.pages.dev | **DIV/컴프레셔 two-lap 도넛**의 중앙 % 텍스트 색을 `progressColor` 두 바퀴 반복 룰로 패치. **룰**: `within = pct > 100 ? pct - 100 : pct` → `progressColor(within)` — 100%·200% 초록, 그 사이 주황(1~49)→파랑(50~99) 재진행, 0%(allZero)는 회색 유지. **변경 3곳 1줄씩**: (1) `src/components/ui/index.tsx` Donut doubleCycle 분기 중앙텍스트 `allZero ? 'var(--t3)' : 'var(--t2)'` → `: color` (단일 arc `zero ? 'var(--t3)' : color` 블록 무변경). (2) DashboardPage 데스크톱 doubleCycle `<Donut size={76}>` color `progressColor(m.pct)` → `progressColor(m.pct > 100 ? m.pct - 100 : m.pct)`. (3) 모바일 doubleCycle `<Donut size={44}>` 동일. 비-doubleCycle 단일 Donut (L342/L691) 무변경. 검증: `git grep "progressColor(m.pct > 100"` 정확히 2건, tsc --noEmit PASS, build precache 83. 사용자 시각 검증 완료. |
| 2026-05-30 | 그룹 B legal Tailwind 리팩터 (디자인 main → prod sync, 7 commits + fix) | (디자인 main, 7 commits cherry-pick) | `57b3728`..`6656be7` + `cfbae90` (fix) | https://eed17462.cbc7119.pages.dev | **legal 페이지 Tailwind 리팩터 그룹 B** — 데스크톱 패널 inline-style **117곳 → Tailwind class** (`7a81491`) + arbitrary value → 디자인 토큰 일괄 치환 (`16ecac2`) + 탭 inline-style 제거 (`57b3728`). **운영 비즈 로직 포팅**: 데스크톱 영역 production 버전 swap (`f018fcb`), 모바일 카드에 제출 상태 + 다운로드 2버튼 (`6e8ca35`), 제출 완료 잠금 UX (`a24eded`), 모바일 audit 후속 arbitrary 토큰화 (`6656be7`/디자인 `4fdfb9f`). **production fix** (`cfbae90`): LegalFindingsPage `sectionPadCls` 템플릿 클래스 → 인라인 `style={{ padding: sectionPad }}` 환원 (production 의 sectionPad 변수 보존) + LegalFindingDetailPage `py-[7px]` → `py-2` (4fdfb9f marker resolve). build PASS (precache 83). 4 files 업로드. 사용자 시각 검증 완료 (배포 OK). |
| 2026-05-30 | 그룹 A 헤더 통일 wave (디자인 main → prod sync, H1~H8 + 후속 정렬) | (디자인 main H1 ~ `5769257`, 15 commits cherry-pick) | `32739d5`..`d60374f` (16 commits, c298bd2 IN_PROGRESS 노트 포함) | https://10850504.cbc7119.pages.dev | **헤더 통일 그룹 A** — 32-header-unification 트랙 H1~H8 + 후속 정렬 fix. **백버튼/액션 버튼 32px 통일** (`.back-btn` 34→32, nav-btn 28→32, RemediationDetail back 36→32, 보수교육/지적사항 44→32, 소화기 관리 버튼 44/40→32, 데스크톱 헤더 h-14/48→32). radius 8→7 통일. **헤더 타이틀** text-title font-semibold (18/600 GlobalHeader 표준, 22곳). **누락 페이지** (H8): DivPage 모바일 + LegalPage 모바일 + QRPrintPage CSS 7곳. **후속 fix** (5769257 등): GlobalHeader 캐논 패턴 정렬 + 탭 inline-style 제거, 모바일 헤더 타이틀 center→left, BLOCK/FLAG 디자인 룰 4 카테고리, 타이틀 백버튼 옆 정렬. 디자인 main 에서 cherry-pick (소스 only). 빌드 dist precache fresh (HEAD 이후 빌드 확인). Functions bundle 업로드. 사용자 시각 검증 완료 (배포 OK). |
| 2026-05-30 | legal-mobile UI + 다운로드 헤더 (cbc7119-data staging → prod sync) | (production 직접 작업, staging 사전 검증) | `dc7e8f4` (feat: legal-mobile) + production-sync commit | https://c1f7fd92.cbc7119.pages.dev | **소스 4파일 sync** (`legal-mobile-for-prod.patch`, cbc7119-data staging 에서 사전 검증). **uploads `[[path]].ts`**: EXT_MIME 에 docx/pptx/zip 추가 + `?download=<파일명>` 쿼리 시 `Content-Disposition: attachment; filename*=UTF-8''...` 헤더 (iOS Safari/PWA 가 `<a download>` 무시하고 octet-stream 을 .txt 로 덧붙이는 문제 → 서버 헤더로 파일명·확장자 강제). **LegalFindingDetailPage**: 라운드 제출 종결 시 조치(신규 등록) 잠금 (legal-round 쿼리 추가). **LegalFindingsPage / LegalPage**: 모바일 법정점검 UI (129+/68-). **적용 함정 박제**: `git apply` 가 대괄호 경로(`[[path]].ts`) 패치를 조용히 skip (종료코드 0 + --check 통과인데 no-op) → BSD `patch -p1` 로 적용 + grep 마커 검증 (memory `feedback_git_apply_bracket_path_noop`). build PASS (tsc + precache 83). Functions bundle 업로드 확인 (API 변경 라이브). 사용자 시나리오 검증 대기. |
| 2026-05-29 | 260529-kcs floorplan marker DELETE cp cascade + 테스트 orphan cp 2건 cleanup | (production 직접 작업) | `9492ad8` + `9f39a8f` | https://1caef8ca.cbc7119.pages.dev | **버그**: 어제 31117bf POST 가 indoor_hydrant/descending_lifeline 마커 추가 시 cp 자동 INSERT 하도록 fix 했으나 DELETE 분기는 미대칭 — 사용자가 도면에서 '테스트' 마커 만들고 지워도 cp 만 남아 일반 점검 페이지 (CheckpointsPage) 에 orphan 노출. **Code fix (9492ad8)**: `[id].ts` 의 else 분기를 `else if (cpId)` + 안전 가드 (check_records=0 AND 다른 marker=0) → atomic batch DELETE marker + cp / 미통과 시 marker 만 DELETE (cp 보존). 소화기 분기 (isExtCascade) 무변경. console.log 로 cascade vs marker-only 진단 가능. **Hot fix (9f39a8f)**: 머지 직후 수동 SQL 검증 중 `check_records.check_point_id` 컬럼명 오류 발견 → `checkpoint_id` 로 정정 (스키마 아티팩트, 같은 파일 내 floor_plan_markers 의 `check_point_id` 와 헷갈림. index.ts GET 의 `WHERE checkpoint_id IN` 과 일관). **DB cleanup**: prod D1 에서 `CP-8-1F-2-SH` (연구동 8-1F 소화전, zone=research) + `CP-8F-9-SH` (사무동 8F 소화전, zone=office) 둘 다 location='테스트', records=0 + markers=0 확인 후 NOT IN check_records 가드로 DELETE. check_records 총 건수 작업 전/후 3759 동등 (손실 0). 사용자 시나리오 (A/B/C/D) 검증 후 완료. |

---

## 부록: 이 노트를 만든 배경

2026-05-27 main → production 머지 사고 이후 신설. 자세한 사고 기록은 `.planning/` 외부의 Claude 메모리 `project_phase_15_abandoned.md` 와 `feedback_merge_conflict_check_abandoned_work.md` 참조.

당시 production 에 폐기된 phase 15 commits 18건이 남아있었고, main 머지 시 충돌 해결로 그 진입점 버튼이 직원 도메인에 다시 노출되는 사고 발생. 근본 원인은 "production 에 뭐가 적용되어야 하는지" 의 SSOT 부재. 이 노트가 그 SSOT.
