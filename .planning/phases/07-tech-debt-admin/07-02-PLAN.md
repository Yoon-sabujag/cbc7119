---
phase: 07-tech-debt-admin
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - cha-bio-safety/src/pages/AdminPage.tsx
autonomous: false
requirements:
  - ADMIN-01
  - ADMIN-02
must_haves:
  truths:
    - "관리자가 직원 목록을 조회하고 추가/수정/비활성화할 수 있다"
    - "관리자가 직원 비밀번호를 사번 뒷 4자리로 초기화할 수 있다"
    - "관리자가 카테고리별로 개소를 조회하고 추가/수정/비활성화할 수 있다"
    - "카테고리는 조회 전용이며 CRUD 불가하다"
    - "assistant 역할은 /admin 접근 시 /dashboard로 리디렉션된다"
    - "비활성화된 직원/개소는 목록에서 흐리게(opacity) 표시된다"
  artifacts:
    - path: "cha-bio-safety/src/pages/AdminPage.tsx"
      provides: "Admin settings page with 2 tabs"
      min_lines: 200
  key_links:
    - from: "src/pages/AdminPage.tsx"
      to: "/api/staff"
      via: "staffApi in api.ts"
      pattern: "staffApi\\."
    - from: "src/pages/AdminPage.tsx"
      to: "/api/check-points"
      via: "checkPointApi in api.ts"
      pattern: "checkPointApi\\."
    - from: "src/pages/AdminPage.tsx"
      to: "src/types/index.ts"
      via: "StaffFull, CheckPointFull imports"
      pattern: "StaffFull|CheckPointFull"
---

<objective>
관리자 설정 페이지(AdminPage) 완전 구현 — 직원관리 탭 + 개소관리 탭

Purpose: ADMIN-01(직원 CRUD + 비밀번호 초기화) 및 ADMIN-02(개소 CRUD + 카테고리 조회)의 프론트엔드 UI를 구현하여 관리자가 앱에서 직원과 개소를 관리할 수 있게 한다.
Output: AdminPage.tsx 완전 구현 (2탭 + 모달 + CRUD 동작)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-tech-debt-admin/07-CONTEXT.md
@.planning/phases/07-tech-debt-admin/07-UI-SPEC.md
@.planning/phases/07-tech-debt-admin/07-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 — API client and types the executor will consume -->

From src/utils/api.ts (added by Plan 01):
```typescript
export const staffApi = {
  list: () => api.get<StaffFull[]>('/staff'),
  create: (data: StaffCreatePayload) => api.post<StaffFull>('/staff', data),
  update: (id: string, data: StaffUpdatePayload) => api.put<StaffFull>(`/staff/${id}`, data),
  resetPassword: (id: string) => api.post<void>(`/staff/${id}/reset-password`, {}),
}

export const checkPointApi = {
  list: (category?: string) => api.get<CheckPointFull[]>(`/check-points${category ? `?category=${encodeURIComponent(category)}` : ''}`),
  categories: () => api.get<string[]>('/check-points?categories=all'),
  create: (data: CheckPointCreatePayload) => api.post<CheckPointFull>('/check-points', data),
  update: (id: string, data: CheckPointUpdatePayload) => api.put<CheckPointFull>(`/check-points/${id}`, data),
}
```

From src/types/index.ts (added by Plan 01):
```typescript
export interface StaffFull {
  id: string; name: string; role: Role; title: string;
  phone: string | null; email: string | null; appointedAt: string | null;
  active: number; shiftType: string | null; createdAt: string;
}
export interface StaffCreatePayload { id: string; name: string; role: Role; title: string; phone?: string; email?: string; appointedAt?: string }
export interface StaffUpdatePayload { name?: string; role?: Role; title?: string; phone?: string; email?: string; appointedAt?: string; active?: number }

export interface CheckPointFull {
  id: string; qrCode: string; floor: string; zone: BuildingZone;
  location: string; category: string; description: string | null;
  locationNo: string | null; isActive: number; createdAt: string;
}
export interface CheckPointCreatePayload { id: string; qrCode: string; floor: string; zone: BuildingZone; location: string; category: string; description?: string; locationNo?: string }
export interface CheckPointUpdatePayload { location?: string; category?: string; description?: string; locationNo?: string; floor?: string; zone?: BuildingZone; isActive?: number }
```

From src/stores/authStore.ts:
```typescript
export const useAuthStore = create(persist(...))
// staff: { id, name, role, title }
```

Existing patterns for reference:
- RemediationPage.tsx: tab UI pattern (STATUS_TABS), card list, React Query usage
- RemediationDetailPage.tsx: self-header with back navigation, bottom sheet modal
- src/components/ui/index.tsx: DutyChip, RoleLabel, StatusBadge primitives
- index.css: slideUp keyframe animation, blink animation for skeletons
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: AdminPage 직원관리 탭 + 개소관리 탭 완전 구현</name>
  <files>
    cha-bio-safety/src/pages/AdminPage.tsx
  </files>
  <read_first>
    cha-bio-safety/src/pages/AdminPage.tsx
    cha-bio-safety/src/pages/RemediationPage.tsx
    cha-bio-safety/src/pages/RemediationDetailPage.tsx
    cha-bio-safety/src/utils/api.ts
    cha-bio-safety/src/types/index.ts
    cha-bio-safety/src/components/ui/index.tsx
    cha-bio-safety/src/index.css
    .planning/phases/07-tech-debt-admin/07-UI-SPEC.md
  </read_first>
  <action>
Replace the Plan 01 scaffold `AdminPage.tsx` with full implementation per 07-UI-SPEC.md. The page has these sections:

**1. Self-Header** (per D-12, UI-SPEC "AdminPage — Self Header"):
- Height 48px, `background: var(--bg2)`, `borderBottom: 1px solid var(--bd)`
- Left: lucide `ChevronLeft` icon (20px), tap target 44x44px, `color: var(--t2)`, onClick `navigate(-1)`
- Center: "관리자 설정" 16px weight 700 `color: var(--t1)`

**2. Tab Bar** (per D-01, UI-SPEC "AdminPage — Tab Bar"):
- 2 tabs: "직원 관리" / "개소 관리"
- `useState<'staff'|'checkpoints'>('staff')` — per D-13 internal state, no URL params
- Equal flex width, height 44px
- Active: `background: var(--bg4)`, `color: var(--t1)`, `borderBottom: 2px solid var(--acl)`
- Inactive: `background: transparent`, `color: var(--t3)`, `borderBottom: 2px solid transparent`
- Font: 12px weight 700, `transition: all 0.15s`
- Container: `background: var(--bg2)`, `borderBottom: 1px solid var(--bd)`

**3. Staff Tab Content** (per ADMIN-01, D-04, D-05, D-06):
- React Query: `useQuery({ queryKey: ['staff-list'], queryFn: staffApi.list })`
- Loading skeleton: 3 placeholder rows, `background: var(--bg3)`, borderRadius 12px, height 64px, `animation: blink 2s ease-in-out infinite`
- Empty state: "등록된 직원이 없습니다" heading + "직원 추가 버튼을 눌러 첫 번째 직원을 등록하세요" body
- Staff list item card (per UI-SPEC "Staff Tab — List Item Card"):
  - Card: `background: var(--bg3)`, borderRadius 12px, padding 12px 16px, minHeight 56px
  - Left: active dot (8px circle, `var(--safe)` if active=1, `var(--t3)` if active=0)
  - Name: 16px weight 700. Title: 12px weight 400 `var(--t2)`
  - Staff ID: JetBrains Mono 12px weight 400 `var(--t3)`
  - Role badge: admin → `background: rgba(59,130,246,.13)` `color: var(--acl)` / assistant → `background: rgba(110,118,129,.15)` `color: var(--t2)`. Font 9px weight 700, padding 2px 5px, borderRadius 4px
  - "수정 ▸" button on right
  - Inactive rows: entire card `opacity: 0.5`
  - Card gap/margin: 8px between cards
- FAB at bottom (per UI-SPEC "Staff Tab — Add Button"):
  - Sticky bottom, full-width, height 52px, `background: var(--acl)`, `color: #fff`, 14px weight 700
  - lucide `UserPlus` 18px, gap 8px, text "직원 추가"
  - `paddingBottom: calc(16px + var(--sab))`

**4. Staff Add/Edit Modal** (per UI-SPEC "Staff Tab — Add/Edit Modal"):
- Bottom sheet pattern: backdrop `rgba(0,0,0,0.6)`, sheet `background: var(--bg2)`, borderRadius `16px 16px 0 0`
- Drag handle: 4px x 32px, `background: var(--bd2)`, centered, margin-top 12px
- Title: "직원 추가" or "직원 수정" — 16px weight 700, padding 16px
- Fields (per D-06, D-07, D-08):
  - 이름 * (text, required)
  - 사번 * (text, inputmode="numeric", 10-digit validation, disabled in edit mode)
  - 연락처 (tel, optional)
  - 이메일 (email, optional)
  - 선임일자 (date, optional)
  - 직책 (text)
  - 역할 (segmented control: admin/assistant — selected `background: var(--acl)` `color: #fff`, unselected `background: var(--bg4)` `color: var(--t3)`, height 36px)
- Input style: height 44px, `background: var(--bg3)`, `border: 1px solid var(--bd)`, borderRadius 8px, padding `0 12px`, font 14px `var(--t1)`. Focus: `borderColor: var(--acl)`
- Label: 12px weight 700 `var(--t2)`, marginBottom 6px
- Button row: gap 8px, padding 16px
  - Cancel: `background: var(--bg4)`, `color: var(--t2)`, borderRadius 8px, height 44px, flex 1
  - Save: `background: var(--acl)`, `color: #fff`, borderRadius 8px, height 44px, flex 1. Disabled when required fields empty: `opacity: 0.4`, `cursor: not-allowed`
- On save (create): `useMutation` with `staffApi.create(data)`, invalidate `['staff-list']`, toast "직원이 추가되었습니다"
- On save (edit): `useMutation` with `staffApi.update(id, data)`, invalidate `['staff-list']`, toast "직원 정보가 수정되었습니다"
- Validation: staff ID must be 10 digits numeric → toast.error("사번은 10자리 숫자여야 합니다")

**5. Staff — Password Reset** (per D-04, UI-SPEC "Staff Tab — Password Reset Button"):
- Inside edit modal: text button "비밀번호 초기화", 12px `var(--warn)`, no background, underline on hover
- Click shows inline confirmation: "사번 뒷 4자리로 비밀번호를 초기화합니다. 계속하시겠습니까?" with [취소][확인]
- Confirm: `staffApi.resetPassword(id)`, toast "비밀번호가 초기화되었습니다 (사번 뒷 4자리)"

**6. Staff — Deactivate** (per D-05, UI-SPEC "Staff Tab — Deactivate Action"):
- Inside edit modal bottom: "비활성화" button, 12px `var(--danger)`, full-width, height 40px, `background: rgba(239,68,68,.08)`, borderRadius 8px
- Click shows inline confirmation (replaces button row): "이 직원을 비활성화합니다. 점검 기록은 보존됩니다." with [취소][비활성화]
- Confirm button: `background: var(--danger)` `color: #fff`
- Action: `staffApi.update(id, { active: 0 })`, invalidate, toast "직원이 비활성화되었습니다"

**7. Checkpoints Tab Content** (per ADMIN-02, D-09, D-10, D-11):
- Category dropdown at top (per UI-SPEC "Checkpoints Tab — Category Dropdown"):
  - Full-width select, height 44px, `background: var(--bg3)`, `border: 1px solid var(--bd)`, borderRadius 8px
  - Options: "전체" (default) + categories from `checkPointApi.categories()`
  - lucide `ChevronDown` 16px `var(--t2)` on right
- Category read-only section (per UI-SPEC "Category Read-Only Section"):
  - Below dropdown: section heading "카테고리 (조회 전용)" with lucide `Lock` 12px `var(--t3)` inline
  - Pill chips in wrap flex, gap 8px: each `background: var(--bg4)`, borderRadius 16px, padding 4px 10px, 12px weight 400 `var(--t2)`
- Checkpoint list (per UI-SPEC "Checkpoints Tab — Checkpoint List Item"):
  - React Query: `useQuery({ queryKey: ['check-points', selectedCategory], queryFn: () => checkPointApi.list(selectedCategory || undefined) })`
  - Empty state when no category selected: "카테고리를 선택하면 개소 목록이 표시됩니다"
  - Empty state when category has no items: "해당 카테고리에 개소가 없습니다"
  - Card: same structure as staff card — `background: var(--bg3)`, borderRadius 12px, padding 12px 16px, minHeight 48px
  - Active dot, 개소명 14px/700, zone+floor 12px `var(--t2)`, category badge, "수정 ▸"
  - Inactive: `opacity: 0.45`
- FAB: "개소 추가" button, same style as staff FAB but with lucide `Plus` icon

**8. Checkpoint Add/Edit Modal** (per D-10, UI-SPEC "Checkpoints Tab — Add/Edit Modal"):
- Same bottom sheet as staff modal
- Fields: 개소명 * (text), 카테고리 * (select from 13+ categories), 구역 (office/research/common segmented), 층 (number)
- On save: `checkPointApi.create(data)` or `checkPointApi.update(id, data)`, invalidate `['check-points', ...]`
- Toast: "개소가 추가되었습니다" / "개소 정보가 수정되었습니다"
- Deactivate: same pattern as staff — "이 개소를 비활성화합니다. 기존 점검 기록은 보존됩니다."

**9. Role Guard** (per D-02):
- `useEffect` redirect: if `staff?.role !== 'admin'`, `navigate('/dashboard', { replace: true })` silently (no toast)
- Early return `null` while redirecting
  </action>
  <verify>
    <automated>cd /Users/jykevin/Documents/20260328/cha-bio-safety && npx tsc --noEmit 2>&1 | head -20 && wc -l src/pages/AdminPage.tsx</automated>
  </verify>
  <acceptance_criteria>
    - cha-bio-safety/src/pages/AdminPage.tsx contains "직원 관리" and "개소 관리" tab labels
    - cha-bio-safety/src/pages/AdminPage.tsx contains "staffApi.list" (staff list query)
    - cha-bio-safety/src/pages/AdminPage.tsx contains "staffApi.create" (staff creation)
    - cha-bio-safety/src/pages/AdminPage.tsx contains "staffApi.update" (staff edit)
    - cha-bio-safety/src/pages/AdminPage.tsx contains "staffApi.resetPassword" (password reset)
    - cha-bio-safety/src/pages/AdminPage.tsx contains "checkPointApi.list" (checkpoint list)
    - cha-bio-safety/src/pages/AdminPage.tsx contains "checkPointApi.create" (checkpoint creation)
    - cha-bio-safety/src/pages/AdminPage.tsx contains "role !== 'admin'" (role guard)
    - cha-bio-safety/src/pages/AdminPage.tsx contains "사번은 10자리 숫자여야 합니다" (validation toast)
    - cha-bio-safety/src/pages/AdminPage.tsx contains "비밀번호가 초기화되었습니다" (reset toast)
    - cha-bio-safety/src/pages/AdminPage.tsx contains "비활성화" (deactivate action)
    - cha-bio-safety/src/pages/AdminPage.tsx contains "카테고리 (조회 전용)" (read-only label per D-09)
    - AdminPage.tsx is at least 200 lines (full implementation, not scaffold)
    - TypeScript compilation succeeds
  </acceptance_criteria>
  <done>AdminPage에서 직원관리(목록/추가/수정/비활성화/비밀번호 초기화)와 개소관리(카테고리별 조회/추가/수정/비활성화)가 모두 동작한다</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 관리자 설정 페이지 시각/기능 검증</name>
  <files>cha-bio-safety/src/pages/AdminPage.tsx</files>
  <action>
사용자에게 관리자 설정 페이지의 시각적/기능적 검증을 요청한다. Task 1에서 구현한 AdminPage의 모든 기능(직원관리 CRUD, 개소관리 CRUD, role guard, 탭 전환)이 올바르게 동작하는지 확인.
  </action>
  <what-built>
    관리자 설정 페이지 전체 — 직원관리 탭(CRUD + 비밀번호 초기화 + 비활성화)과 개소관리 탭(카테고리별 조회 + CRUD + 비활성화)
  </what-built>
  <how-to-verify>
    1. `cd cha-bio-safety && npm run dev` 로 로컬 서버 실행
    2. admin 계정(석현민, 2018042451/2451)으로 로그인
    3. 햄버거 메뉴 열기 → '시스템' 섹션에 '관리자 설정' 항목이 보이는지 확인
    4. '관리자 설정' 클릭 → /admin 페이지 진입 확인
    5. **직원 관리 탭:**
       - 4명의 직원 목록이 카드 형태로 표시되는지 확인
       - 역할 배지(admin/assistant), 사번, 직책이 올바르게 표시되는지 확인
       - '직원 추가' FAB 클릭 → 바텀시트 모달 올라오는지 확인
       - 필드 입력 후 저장 → 목록에 반영되는지 확인
       - 기존 직원 '수정' 클릭 → 수정 모달 → 비밀번호 초기화 / 비활성화 버튼 확인
    6. **개소 관리 탭:**
       - 카테고리 드롭다운에서 '소화기' 선택 → 해당 개소 목록 표시
       - '카테고리 (조회 전용)' 섹션에 pill 칩 표시 확인
       - '개소 추가' → 모달 → 저장 → 목록 반영 확인
    7. 로그아웃 후 assistant 계정(박보융, 2023071752/1752)으로 로그인
    8. 햄버거 메뉴에서 '관리자 설정' 항목이 보이지 않는지 확인
    9. 직접 /admin URL 입력 → /dashboard로 리디렉션되는지 확인
  </how-to-verify>
  <verify>
    <automated>cd /Users/jykevin/Documents/20260328/cha-bio-safety && npx tsc --noEmit 2>&1 | head -5</automated>
  </verify>
  <done>사용자가 관리자 설정 페이지의 시각적 디자인과 모든 CRUD 기능이 올바르게 동작함을 확인하고 "approved"를 입력함</done>
  <resume-signal>Type "approved" or describe any visual/functional issues</resume-signal>
</task>

</tasks>

<verification>
1. `cd cha-bio-safety && npx tsc --noEmit` — 전체 TypeScript 컴파일 성공
2. `grep "staffApi\.\|checkPointApi\." src/pages/AdminPage.tsx` — API 연결 확인
3. `wc -l src/pages/AdminPage.tsx` — 200줄 이상 (완전 구현)
4. Admin role guard: assistant 로그인 시 /admin 접근 불가
5. SideMenu: assistant에게 관리자 설정 미노출
</verification>

<success_criteria>
- ADMIN-01 완료: 직원 목록 조회, 추가, 수정, 비활성화, 비밀번호 초기화가 모두 동작
- ADMIN-02 완료: 카테고리별 개소 조회, 추가, 수정, 비활성화가 모두 동작. 카테고리는 조회 전용
- Role guard: API + 프론트 이중 가드 동작 (admin만 접근)
- UI: 07-UI-SPEC.md 디자인 계약 준수 (색상, 타이포그래피, 스페이싱)
- 사용자 visual verification 통과
</success_criteria>

<output>
After completion, create `.planning/phases/07-tech-debt-admin/07-02-SUMMARY.md`
</output>
