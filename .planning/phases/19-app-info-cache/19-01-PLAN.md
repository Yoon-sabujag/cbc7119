---
phase: 19-app-info-cache
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cha-bio-safety/vite.config.ts
  - cha-bio-safety/src/vite-env.d.ts
  - cha-bio-safety/src/components/SettingsPanel.tsx
autonomous: false
requirements: [APP-01, APP-02]

must_haves:
  truths:
    - "사용자가 설정 패널에서 현재 빌드 버전(v0.2.0 + KST 빌드 일시)을 확인할 수 있다"
    - "사용자가 캐시 초기화 버튼을 탭하면 Cache Storage 전체가 삭제되고 SW update 후 페이지가 하드 리로드된다"
    - "캐시 API 미지원 환경에서는 toast.error로 안내되고 중단된다"
    - "기존 하드코딩 v1.0.0 푸터 블록은 제거되고 주소는 새 앱 정보 섹션 안의 정적 Row로 이동했다"
    - "앱 정보 섹션은 Phase 18 SectionHeader/usePersistedCollapse 헬퍼로 접힘/펼침되며 기본 접힘 상태이다"
  artifacts:
    - path: "cha-bio-safety/vite.config.ts"
      provides: "__APP_VERSION__/__BUILD_TIME__ define injection"
      contains: "__APP_VERSION__"
    - path: "cha-bio-safety/src/vite-env.d.ts"
      provides: "Global const type declarations"
      contains: "declare const __APP_VERSION__"
    - path: "cha-bio-safety/src/components/SettingsPanel.tsx"
      provides: "앱 정보 section with version + cache clear + address rows"
      contains: "settings.appinfo.collapsed"
  key_links:
    - from: "vite.config.ts define"
      to: "SettingsPanel.tsx version Row"
      via: "compile-time constant substitution"
      pattern: "__APP_VERSION__"
    - from: "캐시 초기화 Row onClick"
      to: "caches.keys() + caches.delete + registration.update + reload"
      via: "async handler"
      pattern: "caches\\.keys"
---

<objective>
Phase 19 — APP-01(빌드 버전 표시) + APP-02(캐시 초기화) 구현. 사용자가 설정 패널에서 현재 빌드 버전을 확인하고, 한 번의 탭으로 Cache Storage 전체를 비운 뒤 SW update + 하드 리로드로 최신 리소스를 받을 수 있게 한다.

Purpose: iOS PWA에서 stale cache로 인한 막힘 상황의 탈출구. 빌드 추적용이 아닌 "지금 보는 앱이 언제 빌드된 건가" 확인용.
Output: vite define으로 빌드타임 주입된 __APP_VERSION__/__BUILD_TIME__, SettingsPanel 새 "앱 정보" 섹션, 기존 v1.0.0 푸터 블록 제거.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/19-app-info-cache/19-CONTEXT.md
@CLAUDE.md
@cha-bio-safety/vite.config.ts
@cha-bio-safety/src/components/SettingsPanel.tsx
@cha-bio-safety/package.json

<interfaces>
From cha-bio-safety/src/components/SettingsPanel.tsx (Phase 18 helpers, already in file):
```typescript
function SectionHeader({ label, collapsed, onToggle }: { label: string; collapsed: boolean; onToggle: () => void }): JSX.Element
function usePersistedCollapse(key: string, defaultCollapsed = true): [boolean, (v: boolean | ((c: boolean) => boolean)) => void]
function Row({ label, sub, children, onClick }: { label: string; sub?: string; children?: React.ReactNode; onClick?: () => void }): JSX.Element
```

From cha-bio-safety/package.json:
```
"version": "0.2.0"
```

date-fns-tz is NOT currently in package.json. Use plain `date-fns` `format()` with manual KST adjustment, OR use `Intl.DateTimeFormat('ko-KR', { timeZone: 'Asia/Seoul', ... })` directly in vite.config.ts (no extra dep needed). Choose the Intl approach to avoid adding a dependency.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Inject __APP_VERSION__ and __BUILD_TIME__ via vite define + add type declarations</name>
  <files>cha-bio-safety/vite.config.ts, cha-bio-safety/src/vite-env.d.ts</files>
  <read_first>
    - cha-bio-safety/vite.config.ts (current defineConfig structure)
    - cha-bio-safety/package.json (read version field — currently "0.2.0")
    - cha-bio-safety/tsconfig.json (confirm "include": ["src","functions"] picks up vite-env.d.ts)
  </read_first>
  <action>
**Step 1 — Edit `cha-bio-safety/vite.config.ts`:**

At the top of the file, add this import:
```ts
import { readFileSync } from 'node:fs'
import { resolve } from 'node:path'
```

Above `export default defineConfig({`, add these constants (per D-01, D-02, D-03):
```ts
const pkg = JSON.parse(readFileSync(resolve(__dirname, 'package.json'), 'utf-8'))
const APP_VERSION = pkg.version as string
// KST formatted YYYY-MM-DD HH:mm without adding date-fns-tz dependency
const BUILD_TIME = (() => {
  const fmt = new Intl.DateTimeFormat('sv-SE', {
    timeZone: 'Asia/Seoul',
    year: 'numeric', month: '2-digit', day: '2-digit',
    hour: '2-digit', minute: '2-digit', hour12: false,
  })
  // sv-SE locale yields "YYYY-MM-DD HH:mm" format natively
  return fmt.format(new Date())
})()
```

Inside the `defineConfig({ ... })` object, add a `define` key (place it as a sibling of `plugins`, `server`, `build`):
```ts
  define: {
    __APP_VERSION__: JSON.stringify(APP_VERSION),
    __BUILD_TIME__: JSON.stringify(BUILD_TIME),
  },
```

Do NOT remove or modify existing `plugins`, `server`, or `build` blocks.

**Step 2 — Create `cha-bio-safety/src/vite-env.d.ts` (file does not currently exist):**

```ts
/// <reference types="vite/client" />

declare const __APP_VERSION__: string
declare const __BUILD_TIME__: string
```

This per D-04. tsconfig.json `include: ["src","functions"]` will pick it up automatically.
  </action>
  <verify>
    <automated>cd cha-bio-safety && grep -q "__APP_VERSION__: JSON.stringify" vite.config.ts && grep -q "__BUILD_TIME__: JSON.stringify" vite.config.ts && grep -q "declare const __APP_VERSION__" src/vite-env.d.ts && grep -q "declare const __BUILD_TIME__" src/vite-env.d.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep "__APP_VERSION__: JSON.stringify(APP_VERSION)" cha-bio-safety/vite.config.ts` matches
    - `grep "__BUILD_TIME__: JSON.stringify(BUILD_TIME)" cha-bio-safety/vite.config.ts` matches
    - `grep "Asia/Seoul" cha-bio-safety/vite.config.ts` matches (KST timezone per D-03)
    - `cha-bio-safety/src/vite-env.d.ts` exists and contains both `declare const __APP_VERSION__: string` and `declare const __BUILD_TIME__: string`
    - `cd cha-bio-safety && npx tsc --noEmit` passes with zero errors
    - No new dependencies added to package.json
  </acceptance_criteria>
  <done>vite define injects compile-time version + KST build timestamp; TypeScript recognizes globals; tsc clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add 앱 정보 section to SettingsPanel + remove hardcoded v1.0.0 footer block</name>
  <files>cha-bio-safety/src/components/SettingsPanel.tsx</files>
  <read_first>
    - cha-bio-safety/src/components/SettingsPanel.tsx (entire file — note SectionHeader/usePersistedCollapse/Row at top, existing 계정/로그아웃 layout, hardcoded 앱 정보 footer at lines ~447-451)
    - .planning/phases/19-app-info-cache/19-CONTEXT.md (D-05..D-14)
  </read_first>
  <action>
Edit `cha-bio-safety/src/components/SettingsPanel.tsx`. Three changes:

**Change 1 — Add appinfo collapse state hook (per D-06).** Inside `SettingsPanel` component, alongside existing `useState` declarations (next to `accountCollapsed`), add:

```tsx
  const [appInfoCollapsed, setAppInfoCollapsed] = usePersistedCollapse('settings.appinfo.collapsed', true)
  const [cacheClearing, setCacheClearing] = useState(false)
```

**Change 2 — Add cache clear handler (per D-09..D-13).** Above the `return (` of `SettingsPanel`, add:

```tsx
  async function handleClearCache() {
    if (!('caches' in window)) {
      toast.error('이 브라우저는 캐시 초기화를 지원하지 않습니다')
      return
    }
    setCacheClearing(true)
    try {
      const names = await caches.keys()
      await Promise.all(names.map(n => caches.delete(n)))
      if ('serviceWorker' in navigator) {
        const reg = await navigator.serviceWorker.getRegistration()
        if (reg) await reg.update()
      }
      window.location.reload()
    } catch (e) {
      console.error('Cache clear error:', e)
      toast.error('캐시 초기화에 실패했습니다')
      setCacheClearing(false)
    }
  }
```

**Change 3 — Insert 앱 정보 section between 로그아웃 button and the closing div (per D-05, D-07, D-08).**

Locate the existing `{/* 로그아웃 */}` block and the existing `{/* 앱 정보 */}` hardcoded footer (currently at the bottom, contains `차바이오컴플렉스 방재 v1.0.0` and the address). Per D-05, the new 앱 정보 section goes ABOVE the 로그아웃 button. Per D-07, the existing hardcoded footer block is REMOVED entirely.

Insert the following NEW block immediately BEFORE `{/* 로그아웃 */}`:

```tsx
        {/* 앱 정보 */}
        <div style={{ padding: '12px 13px 5px' }}>
          <SectionHeader label="앱 정보" collapsed={appInfoCollapsed} onToggle={() => setAppInfoCollapsed(c => !c)} />
          {!appInfoCollapsed && (
            <>
              <Row label="버전" sub={`v${__APP_VERSION__} (${__BUILD_TIME__})`} />
              <Row
                label={cacheClearing ? '초기화 중…' : '캐시 초기화'}
                sub="최신 리소스로 새로고침"
                onClick={cacheClearing ? undefined : handleClearCache}
              >
                <svg width={13} height={13} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}><path strokeLinecap="round" strokeLinejoin="round" d="M9 5l7 7-7 7"/></svg>
              </Row>
              <Row label="차바이오컴플렉스 방재" sub="경기도 성남시 분당구 판교로 335" />
            </>
          )}
        </div>
```

Then DELETE the entire existing hardcoded footer block at the bottom (per D-07):

```tsx
        {/* 앱 정보 */}
        <div style={{ padding: 13, textAlign: 'center' }}>
          <div style={{ fontSize: 10, color: 'var(--t3)' }}>차바이오컴플렉스 방재 v1.0.0</div>
          <div style={{ fontSize: 9, color: 'var(--t3)', marginTop: 2 }}>경기도 성남시 분당구 판교로 335</div>
        </div>
```

After deletion the closing `</div>` of the panel and the `{showNameEdit && ...}` modal must still exist in their original positions.
  </action>
  <verify>
    <automated>cd cha-bio-safety && grep -q "settings.appinfo.collapsed" src/components/SettingsPanel.tsx && grep -q "__APP_VERSION__" src/components/SettingsPanel.tsx && grep -q "handleClearCache" src/components/SettingsPanel.tsx && grep -q "caches.keys" src/components/SettingsPanel.tsx && ! grep -q "v1.0.0" src/components/SettingsPanel.tsx && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep "settings.appinfo.collapsed" cha-bio-safety/src/components/SettingsPanel.tsx` matches exactly once
    - `grep "__APP_VERSION__" cha-bio-safety/src/components/SettingsPanel.tsx` matches
    - `grep "__BUILD_TIME__" cha-bio-safety/src/components/SettingsPanel.tsx` matches
    - `grep "handleClearCache" cha-bio-safety/src/components/SettingsPanel.tsx` matches
    - `grep "caches.keys" cha-bio-safety/src/components/SettingsPanel.tsx` matches
    - `grep "registration.update\|reg.update" cha-bio-safety/src/components/SettingsPanel.tsx` matches
    - `grep "window.location.reload" cha-bio-safety/src/components/SettingsPanel.tsx` matches
    - `grep "이 브라우저는 캐시 초기화를 지원하지 않습니다" cha-bio-safety/src/components/SettingsPanel.tsx` matches
    - `grep "초기화 중…" cha-bio-safety/src/components/SettingsPanel.tsx` matches
    - `grep -c "v1.0.0" cha-bio-safety/src/components/SettingsPanel.tsx` returns 0 (hardcoded footer removed)
    - `cd cha-bio-safety && npx tsc --noEmit` passes
    - The new 앱 정보 section appears textually BEFORE the `{/* 로그아웃 */}` block
  </acceptance_criteria>
  <done>SettingsPanel renders new collapsible 앱 정보 section with version + cache clear + address; old v1.0.0 footer removed; tsc clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Production build and deploy</name>
  <files>(no source edits — build artifacts only)</files>
  <read_first>
    - CLAUDE.md (deploy with --branch=production rule from MEMORY.md)
    - cha-bio-safety/package.json (build script)
  </read_first>
  <action>
Run from `cha-bio-safety/`:

```bash
cd cha-bio-safety
npm run build
npx wrangler pages deploy dist --project-name=cbc7119 --branch=production
```

The `--branch=production` flag is MANDATORY (per user feedback memory: omitting it deploys to Preview, not Production).

After deploy, capture the deployment URL from wrangler output and report it in the task summary so the human verification checkpoint can use it.
  </action>
  <verify>
    <automated>cd cha-bio-safety && npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0 and emits dist/ assets
    - `wrangler pages deploy` reports a successful deploy with `--branch=production` flag present
    - Built JS contains the literal version string (e.g., `"0.2.0"`) — confirms vite define substitution worked: `grep -r "0.2.0" cha-bio-safety/dist/assets/ | head -3` returns at least one match
    - Built JS contains a KST timestamp matching `[0-9]{4}-[0-9]{2}-[0-9]{2} [0-9]{2}:[0-9]{2}` pattern in the SettingsPanel chunk
  </acceptance_criteria>
  <done>Production deploy completes; build artifacts contain substituted version + build time strings.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human verification of 앱 정보 section + cache clear flow</name>
  <what-built>
    - vite define으로 __APP_VERSION__ / __BUILD_TIME__ 빌드타임 주입
    - SettingsPanel "앱 정보" 섹션 신설 (계정 ↓ / 로그아웃 ↑, 기본 접힘)
    - 버전 Row, 캐시 초기화 Row, 주소 Row
    - 캐시 초기화 핸들러: caches.keys → delete 전체 → SW update → reload
    - 기존 하드코딩 v1.0.0 푸터 제거
  </what-built>
  <how-to-verify>
    1. 프로덕션 URL에 PWA로 접속 → 로그인
    2. 우측 상단 설정(⚙) 패널 열기
    3. "앱 정보" 섹션이 계정 섹션 아래, 로그아웃 버튼 위에 표시되는지 확인
    4. 섹션이 기본 접혀 있는지 확인 → 헤더 클릭 시 chevron 회전 + 펼침 동작
    5. 버전 Row sub 텍스트가 `v0.2.0 (YYYY-MM-DD HH:mm)` 형식 (KST 기준)인지 확인
    6. 주소 Row가 `차바이오컴플렉스 방재 / 경기도 성남시 분당구 판교로 335` 표시 확인
    7. 패널 하단에 기존 `차바이오컴플렉스 방재 v1.0.0` 텍스트가 더 이상 없는지 확인
    8. 패널 닫고 다시 열어 → 앱 정보 섹션이 여전히 접혀 있는지 (localStorage 퍼시스트 확인)
    9. 한 번 펼친 뒤 패널 닫고 다시 열어 → 펼친 상태 유지되는지 확인
    10. "캐시 초기화" Row 탭 → 라벨이 "초기화 중…"으로 바뀌는지 확인
    11. 짧은 지연 후 페이지가 자동 하드 리로드되는지 확인
    12. 리로드 후 다시 로그인 → 정상 동작 확인
    13. (선택) DevTools Application → Cache Storage가 비워져 있다가 SW가 다시 채우는지 확인
  </how-to-verify>
  <resume-signal>"approved" 또는 발견된 이슈를 한국어로 설명</resume-signal>
</task>

</tasks>

<verification>
- tsc clean (Tasks 1, 2)
- Production build completes (Task 3)
- dist/ assets contain substituted version + KST build time literals (Task 3)
- Human visual + functional verification on production (Task 4)
</verification>

<success_criteria>
- ROADMAP Phase 19 success criteria 1 (버전 확인) ✅
- ROADMAP Phase 19 success criteria 2 (캐시 초기화 + 메시지) ✅ — "초기화 중…" 라벨 + reload이 메시지 역할
- ROADMAP Phase 19 success criteria 3 (최신 리소스 새로고침) ✅
- All 14 CONTEXT decisions D-01..D-14 implemented
</success_criteria>

<output>
After completion, create `.planning/phases/19-app-info-cache/19-01-SUMMARY.md` documenting actual version/build time format used, files changed, deploy URL, and any deviations.
</output>
