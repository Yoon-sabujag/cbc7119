---
phase: 10-legal-inspection
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - cha-bio-safety/src/pages/LegalPage.tsx
  - cha-bio-safety/src/pages/LegalFindingsPage.tsx
  - cha-bio-safety/src/pages/LegalFindingDetailPage.tsx
autonomous: false
requirements:
  - LEGAL-01
  - LEGAL-02

must_haves:
  truths:
    - "사용자가 법적 점검 회차 목록을 카드 리스트로 조회할 수 있다"
    - "관리자가 점검 회차를 등록하고 PDF를 업로드할 수 있다"
    - "사용자가 지적사항 목록을 조회하고 상태별 필터링할 수 있다"
    - "전체 직원이 지적사항을 등록하고 사진을 첨부할 수 있다"
    - "전체 직원이 지적 상세에서 조치완료 처리(메모+사진)를 할 수 있다"
    - "모든 페이지에 자체 헤더(뒤로가기 + 제목)가 표시된다"
  artifacts:
    - path: "cha-bio-safety/src/pages/LegalPage.tsx"
      provides: "점검 회차 카드 리스트 + 등록 BottomSheet"
      min_lines: 100
    - path: "cha-bio-safety/src/pages/LegalFindingsPage.tsx"
      provides: "지적사항 목록 + 상태 필터 탭 + 등록 BottomSheet"
      min_lines: 100
    - path: "cha-bio-safety/src/pages/LegalFindingDetailPage.tsx"
      provides: "지적 상세 + 조치완료 입력 + 사진 업로드"
      min_lines: 100
  key_links:
    - from: "cha-bio-safety/src/pages/LegalPage.tsx"
      to: "/api/legal"
      via: "legalApi.listInspections() in useQuery"
      pattern: "legalApi\\.listInspections"
    - from: "cha-bio-safety/src/pages/LegalFindingsPage.tsx"
      to: "/api/legal/:id/findings"
      via: "legalApi.listFindings(id) in useQuery"
      pattern: "legalApi\\.listFindings"
    - from: "cha-bio-safety/src/pages/LegalFindingDetailPage.tsx"
      to: "/api/legal/:id/findings/:fid/resolve"
      via: "legalApi.resolveFinding() in useMutation"
      pattern: "legalApi\\.resolveFinding"
    - from: "cha-bio-safety/src/pages/LegalFindingDetailPage.tsx"
      to: "/api/uploads"
      via: "usePhotoUpload hook"
      pattern: "usePhotoUpload"
---

<objective>
Phase 10 프론트엔드: 법적 점검 3개 페이지 — 회차 카드 리스트, 지적사항 목록(상태 필터), 지적 상세+조치완료

Purpose: 현장에서 법적 점검 지적사항을 모바일로 기록하고 조치를 추적하는 UI 제공
Output: LegalPage.tsx, LegalFindingsPage.tsx, LegalFindingDetailPage.tsx 3개 페이지 컴포넌트
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-legal-inspection/10-CONTEXT.md
@.planning/phases/10-legal-inspection/10-RESEARCH.md
@.planning/phases/10-legal-inspection/10-UI-SPEC.md
@.planning/phases/10-legal-inspection/10-01-SUMMARY.md

@cha-bio-safety/src/pages/RemediationPage.tsx
@cha-bio-safety/src/pages/RemediationDetailPage.tsx
@cha-bio-safety/src/pages/EducationPage.tsx
@cha-bio-safety/src/utils/api.ts
@cha-bio-safety/src/types/index.ts

<interfaces>
<!-- Types created in Plan 01 — executor should use directly -->

From src/types/index.ts (created in 10-01):
```typescript
export type InspectionType = 'comprehensive' | 'functional'
export type InspectionResult = 'pass' | 'fail' | 'conditional'
export type FindingStatus = 'open' | 'resolved'

export interface LegalInspection {
  id: string; inspectionType: InspectionType; inspectedAt: string;
  agency: string; result: InspectionResult; reportFileKey: string | null;
  memo: string | null; createdBy: string; createdAt: string;
  findingCount: number; resolvedCount: number;
}

export interface LegalFinding {
  id: string; inspectionId: string; description: string;
  location: string | null; photoKey: string | null;
  resolutionMemo: string | null; resolutionPhotoKey: string | null;
  status: FindingStatus; resolvedAt: string | null; resolvedBy: string | null;
  createdBy: string; createdAt: string;
}
```

From src/utils/api.ts (created in 10-01):
```typescript
export const legalApi = {
  listInspections: () => api.get<LegalInspection[]>('/legal'),
  createInspection: (data) => api.post<{ id: string }>('/legal', data),
  updateInspection: (id, data) => api.put<void>(`/legal/${id}`, data),
  listFindings: (inspectionId) => api.get<LegalFinding[]>(`/legal/${inspectionId}/findings`),
  createFinding: (inspectionId, data) => api.post<{ id: string }>(`/legal/${inspectionId}/findings`, data),
  getFinding: (inspectionId, fid) => api.get<LegalFinding>(`/legal/${inspectionId}/findings/${fid}`),
  updateFinding: (inspectionId, fid, data) => api.put<void>(`/legal/${inspectionId}/findings/${fid}`, data),
  resolveFinding: (inspectionId, fid, data) => api.post<void>(`/legal/${inspectionId}/findings/${fid}/resolve`, data),
}
```

From RemediationPage.tsx patterns:
- Self-header: 48px, rgba(22,27,34,0.97), ← back button, centered title
- Card: bg var(--bg3), borderRadius 12, padding 12, border 1px solid var(--bd)
- Status badge: fontSize 12, fontWeight 700, padding '2px 6px', borderRadius 5
- Skeleton: height 88, bg var(--bg3), borderRadius 12, blink animation
- Tab bar: 3 tabs, 44px height, var(--acl) underline on active

From EducationPage.tsx patterns:
- BottomSheet modal: fixed overlay rgba(0,0,0,0.6), bg2, radius 16px top, drag handle 32x4px
- usePhotoUpload hook import
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: LegalPage + LegalFindingsPage (회차 리스트 + 지적사항 목록)</name>
  <files>
    cha-bio-safety/src/pages/LegalPage.tsx,
    cha-bio-safety/src/pages/LegalFindingsPage.tsx
  </files>
  <read_first>
    cha-bio-safety/src/pages/RemediationPage.tsx,
    cha-bio-safety/src/pages/EducationPage.tsx,
    cha-bio-safety/src/utils/api.ts,
    cha-bio-safety/src/types/index.ts,
    cha-bio-safety/src/stores/authStore.ts,
    .planning/phases/10-legal-inspection/10-UI-SPEC.md,
    .planning/phases/10-legal-inspection/10-01-SUMMARY.md
  </read_first>
  <action>
**LegalPage.tsx** (/legal — 점검 회차 카드 리스트, per D-01, D-02):

1. **자체 헤더** (per D-03): 48px bar, bg rgba(22,27,34,0.97), centered title "법적 점검". NO back button on root page — navigate(-1) returns to wherever invoked. Header right slot: admin만 보이는 "+ 점검 등록" text button (color var(--acl), 14px, weight 700) per D-13.

2. **useQuery** with queryKey ['legal-inspections'], queryFn: legalApi.listInspections(). Loading: 3 skeleton cards (height 88, bg var(--bg3), borderRadius 12, blink animation). Error: centered "목록을 불러오지 못했습니다. 화면을 당겨서 다시 시도하세요." Empty: centered heading "법적 점검 기록 없음" + body "상반기 종합정밀점검 또는 하반기 작동기능점검을 등록하세요."

3. **LegalInspectionCard**: vertically stacked, bg var(--bg3), borderRadius 12, padding 12, border 1px solid var(--bd). Content:
   - Row 1: TypeBadge (종합정밀 = blue tint rgba(14,165,233,0.13) or 작동기능 = orange tint rgba(249,115,22,0.13)) + ResultBadge (적합=safe/부적합=danger/조건부=warn) per UI-SPEC badge specs
   - Row 2: agency (14px, 400, var(--t1)) + inspectedAt (13px, 400, var(--t2))
   - Row 3: "지적 N건 · N건 완료" (12px, var(--t2)) per D-08
   - Right side: PDF icon button if reportFileKey exists → window.open(`/api/uploads/${key}`, '_blank')
   - onClick: navigate(`/legal/${inspection.id}`)

4. **BottomSheet 점검 등록 모달** (admin only, per D-13, UI-SPEC form):
   - Fixed overlay rgba(0,0,0,0.6), bg var(--bg2), radius 16px top, drag handle 32x4px var(--bd2)
   - Fields per UI-SPEC: 점검 유형 2-option toggle (종합정밀점검/작동기능점검), 점검일자 input[type=date], 점검기관 text input, 결과 3-option toggle (적합/조건부적합/부적합), 비고 textarea, PDF 업로드 button (accept=".pdf,application/pdf")
   - PDF 업로드: FormData + fetch('/api/uploads', { method:'POST', body:form, headers:{Authorization} }) → key 저장
   - CTA: "점검 등록" button (full-width, 48px, bg var(--acl), white, 16px/700, rounded 10px)
   - useMutation: legalApi.createInspection → toast.success('점검 회차가 등록되었습니다') → invalidateQueries(['legal-inspections']) → close modal

**LegalFindingsPage.tsx** (/legal/:id — 지적사항 목록, per D-01, D-02):

1. **자체 헤더**: title "지적사항", ← back → navigate(`/legal`). Header right: "+ 지적 등록" visible to all staff per D-12.

2. **회차 요약 스트립** (non-scrolling, bg var(--bg2), ~56px): TypeBadge + inspectedAt + agency + ResultBadge. Fetch from legalApi.listInspections() and find by id, OR add separate detail fetch.

3. **상태 필터 탭** per UI-SPEC: 미조치 / 완료 / 전체. Tab state via URL searchParam 'tab' (RemediationPage pattern per D-11). Active tab: borderBottom 2px solid var(--acl). 44px height.

4. **useQuery** with queryKey ['legal-findings', id], queryFn: legalApi.listFindings(id). Client-side filter by tab (all/open/resolved). Default sort: open first, then by createdAt DESC.

5. **FindingCard**: bg var(--bg3), borderRadius 12, padding 12, border 1px solid var(--bd). Left border 2px: open=var(--warn), resolved=var(--safe).
   - description preview (14px, max 2 lines, overflow hidden)
   - location badge if exists (12px, var(--t3))
   - status badge (미조치 warn / 완료 safe) per UI-SPEC
   - createdAt (12px, var(--t2))
   - onClick: navigate(`/legal/${id}/finding/${finding.id}`)

6. **BottomSheet 지적 등록 모달** (all staff, per D-12, UI-SPEC):
   - Fields: 지적 내용 textarea (required, min-height 88px), 위치 정보 text input (optional, placeholder "예: B1F 전기실 동쪽"), 지적 사진 (usePhotoUpload hook per D-17)
   - Photo upload: usePhotoUpload() → upload to /api/uploads → get key
   - CTA: "지적 등록" (full-width, 48px, bg var(--acl), white, 16px/700, rounded 10px)
   - useMutation: legalApi.createFinding → toast.success('지적사항이 등록되었습니다') → invalidateQueries(['legal-findings', id]) + invalidateQueries(['legal-inspections']) → close modal

Loading/empty/error states same as LegalPage but with finding-specific copy from UI-SPEC copywriting contract.

ALL inline styles, CSS variables, no Tailwind classes on dynamic content. Match RemediationPage/EducationPage style exactly. No lucide-react — inline SVG functions only.
  </action>
  <verify>
    <automated>cd /Users/jykevin/Documents/20260328/cha-bio-safety && grep -q "legalApi" src/pages/LegalPage.tsx && grep -q "legalApi" src/pages/LegalFindingsPage.tsx && grep -q "useQuery" src/pages/LegalPage.tsx && grep -q "usePhotoUpload\|photoUpload\|photo" src/pages/LegalFindingsPage.tsx && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep "legalApi.listInspections" src/pages/LegalPage.tsx returns match
    - grep "legal-inspections" src/pages/LegalPage.tsx returns match (query key)
    - grep "role.*admin\|admin.*role" src/pages/LegalPage.tsx returns match (admin guard for create button)
    - grep "종합정밀\|comprehensive" src/pages/LegalPage.tsx returns match (type badge)
    - grep "적합\|pass" src/pages/LegalPage.tsx returns match (result badge)
    - grep "window.open\|보고서 열기" src/pages/LegalPage.tsx returns match (PDF open)
    - grep "legalApi.listFindings" src/pages/LegalFindingsPage.tsx returns match
    - grep "legal-findings" src/pages/LegalFindingsPage.tsx returns match (query key)
    - grep "searchParam\|tab" src/pages/LegalFindingsPage.tsx returns match (URL tab state)
    - grep "legalApi.createFinding" src/pages/LegalFindingsPage.tsx returns match
    - grep "usePhotoUpload\|photo" src/pages/LegalFindingsPage.tsx returns match (photo upload)
    - npm run build succeeds
  </acceptance_criteria>
  <done>LegalPage에서 회차 카드 리스트 조회 + admin 등록(PDF 포함), LegalFindingsPage에서 지적 목록 필터 + 전원 등록(사진 포함), 빌드 성공</done>
</task>

<task type="auto">
  <name>Task 2: LegalFindingDetailPage (지적 상세 + 조치완료)</name>
  <files>
    cha-bio-safety/src/pages/LegalFindingDetailPage.tsx
  </files>
  <read_first>
    cha-bio-safety/src/pages/RemediationDetailPage.tsx,
    cha-bio-safety/src/utils/api.ts,
    cha-bio-safety/src/types/index.ts,
    .planning/phases/10-legal-inspection/10-UI-SPEC.md
  </read_first>
  <action>
**LegalFindingDetailPage.tsx** (/legal/:id/finding/:fid — 지적 상세 + 조치, per D-01, D-02):

RemediationDetailPage.tsx를 직접 참고하여 동일 패턴으로 구현.

1. **자체 헤더**: title "지적 상세", ← back → navigate(`/legal/${id}`). Admin only: "수정" text link in header area (per D-13).

2. **useQuery**: queryKey ['legal-finding-detail', fid], queryFn: legalApi.getFinding(id, fid).

3. **Section 1 — 지적 내용**:
   - description (16px, fontWeight 700, var(--t1))
   - location badge if exists (12px, var(--t3), background rgba(255,255,255,0.05), padding '2px 8px', borderRadius 4)
   - Before photo if photoKey exists: `<img src={'/api/uploads/' + photoKey}` style={{ width:'100%', maxHeight:240, objectFit:'cover', borderRadius:8 }} />
   - Created info: createdBy + createdAt (12px, var(--t2))

4. **Section 2 — 조치 기록** (shown only when status === 'resolved', per D-11):
   - Divider: 1px solid var(--bd), margin 16px 0
   - "조치 기록" heading (14px, 700, var(--t1))
   - resolutionMemo (14px, 400, var(--t1))
   - After photo if resolutionPhotoKey exists: same img style as before photo
   - resolvedBy + resolvedAt (12px, var(--t2))

5. **Section 3 — 조치 입력** (shown only when status === 'open', per D-11):
   - Divider
   - "조치 내용 입력" heading (14px, 700, var(--t1))
   - resolution memo textarea (bg var(--bg3), border 1px solid var(--bd2), borderRadius 8, min-height 72px, padding 12, full-width, color var(--t1))
   - After photo upload: usePhotoUpload() hook (per D-17). Camera button to capture, shows preview.
   - Photo upload flow: usePhotoUpload().capture() → preview shown → on resolve, upload to /api/uploads, get key
   - CTA: "조치완료로 변경" primary button (full-width, 48px, bg var(--acl), white, 16px/700, rounded 10px, disabled during mutation)

6. **useMutation for resolve**: legalApi.resolveFinding(id, fid, { resolution_memo, resolution_photo_key }). On success:
   - toast.success('조치 완료로 변경되었습니다')
   - Invalidate 3 query keys (RESEARCH Pitfall 4): ['legal-finding-detail', fid], ['legal-findings', id], ['legal-inspections']
   - State resets

7. **Admin 수정 모달** (per D-13): small "수정" text link visible when role === 'admin'. Opens BottomSheet with description textarea + location input, pre-filled. useMutation: legalApi.updateFinding → invalidate queries.

8. **Loading state**: centered Loader spinner. **Error state**: "내용을 불러오지 못했습니다. 뒤로 가서 다시 시도하세요."

ALL inline styles matching project conventions. No lucide-react. useParams() for id, fid. useAuthStore for role check.
  </action>
  <verify>
    <automated>cd /Users/jykevin/Documents/20260328/cha-bio-safety && grep -q "legalApi.resolveFinding" src/pages/LegalFindingDetailPage.tsx && grep -q "usePhotoUpload\|photo" src/pages/LegalFindingDetailPage.tsx && grep -q "legal-finding-detail" src/pages/LegalFindingDetailPage.tsx && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep "legalApi.getFinding" src/pages/LegalFindingDetailPage.tsx returns match
    - grep "legal-finding-detail" src/pages/LegalFindingDetailPage.tsx returns match (query key)
    - grep "legalApi.resolveFinding" src/pages/LegalFindingDetailPage.tsx returns match
    - grep "usePhotoUpload\|photo" src/pages/LegalFindingDetailPage.tsx returns match
    - grep "조치완료로 변경" src/pages/LegalFindingDetailPage.tsx returns match (CTA text)
    - grep "legal-findings\|legal-inspections" src/pages/LegalFindingDetailPage.tsx returns match (invalidation)
    - grep "role.*admin\|admin.*role\|수정" src/pages/LegalFindingDetailPage.tsx returns match (admin edit)
    - grep "resolution_memo\|resolutionMemo" src/pages/LegalFindingDetailPage.tsx returns match
    - npm run build succeeds
  </acceptance_criteria>
  <done>지적 상세 페이지에서 지적 내용+사진 조회, 조치 입력(메모+사진)+완료 처리, admin 수정, 3개 쿼리 invalidation, 빌드 성공</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 법적 점검 전체 흐름 확인</name>
  <what-built>법적 점검 3단계 UI — 회차 리스트, 지적사항 목록, 지적 상세+조치완료</what-built>
  <action>User verifies the full legal inspection flow in the browser. No code changes in this task.</action>
  <how-to-verify>
    1. SideMenu에서 '법적 점검' 클릭 → /legal 페이지 진입 확인 (자체 헤더 "법적 점검")
    2. admin 계정으로 "+ 점검 등록" 클릭 → BottomSheet에서 유형/일자/기관/결과 입력 후 등록
    3. 등록된 회차 카드에 TypeBadge + ResultBadge + 지적건수 표시 확인
    4. 카드 탭 → 지적사항 목록 페이지 진입 (/legal/:id)
    5. "+ 지적 등록" 으로 지적 내용 + 사진 등록
    6. 지적 카드 탭 → 상세 페이지 (/legal/:id/finding/:fid)
    7. 조치 내용 입력 + 사진 첨부 → "조치완료로 변경" 클릭
    8. 뒤로가기 시 지적 목록의 상태 배지가 업데이트되었는지 확인
    9. 회차 카드의 "지적 N건 · N건 완료" 카운트가 정확한지 확인
    10. 필터 탭(미조치/완료/전체) 작동 확인
    11. assistant 계정에서 "+ 점검 등록" 버튼이 안 보이는지 확인
  </how-to-verify>
  <verify>
    <automated>cd /Users/jykevin/Documents/20260328/cha-bio-safety && npm run build 2>&1 | tail -3</automated>
  </verify>
  <done>User confirms all 11 verification steps pass — full legal inspection flow works end-to-end</done>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. `npm run build` passes
2. 3개 페이지 파일이 존재하고 각각 올바른 legalApi 호출
3. 모든 React Query 키가 올바르게 설정되고 invalidation이 3중으로 적용
4. admin/all-staff 권한 분기가 UI에 반영
5. usePhotoUpload 훅이 지적 등록 + 조치완료 양쪽에서 사용
6. 자체 헤더가 3개 페이지 모두에 적용
7. URL searchParam tab 필터가 LegalFindingsPage에서 작동
</verification>

<success_criteria>
- /legal 에서 점검 회차 목록이 카드로 표시되고 admin만 등록 가능
- /legal/:id 에서 지적사항 목록이 상태별 필터와 함께 표시되고 전원 등록 가능
- /legal/:id/finding/:fid 에서 지적 상세 조회 + 조치완료(메모+사진) 처리 가능
- 조치완료 후 상위 목록/회차 카드 카운트가 즉시 갱신
- 모든 페이지에 자체 헤더(뒤로가기 + 제목)가 작동
- npm run build 성공
</success_criteria>

<output>
After completion, create `.planning/phases/10-legal-inspection/10-02-SUMMARY.md`
</output>
