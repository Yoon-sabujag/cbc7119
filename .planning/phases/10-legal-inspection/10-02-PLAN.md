---
phase: 10-legal-inspection
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - cha-bio-safety/src/pages/LegalPage.tsx
  - cha-bio-safety/src/pages/LegalFindingsPage.tsx
  - cha-bio-safety/src/pages/LegalFindingDetailPage.tsx
autonomous: false
requirements:
  - LEGAL-01
  - LEGAL-02

must_haves:
  truths:
    - "User navigates to /legal from SideMenu and sees inspection round cards from schedule_items"
    - "User taps a round card to see /legal/:id with finding list and can register new findings"
    - "User taps a finding to see /legal/:id/finding/:fid detail with photo and resolution"
    - "Admin can set inspection result (pass/fail/conditional) and upload report PDF on findings page"
    - "Any user can resolve a finding with memo and optional photo"
    - "Status filter on LegalPage filters findings across all rounds"
  artifacts:
    - path: "cha-bio-safety/src/pages/LegalPage.tsx"
      provides: "Legal inspection round list with status filter and year filter"
      min_lines: 100
    - path: "cha-bio-safety/src/pages/LegalFindingsPage.tsx"
      provides: "Findings list + admin result panel + finding registration BottomSheet"
      min_lines: 150
    - path: "cha-bio-safety/src/pages/LegalFindingDetailPage.tsx"
      provides: "Finding detail with photo sections and resolve CTA"
      min_lines: 120
  key_links:
    - from: "cha-bio-safety/src/pages/LegalPage.tsx"
      to: "/api/legal"
      via: "legalApi.list(year) via useQuery"
      pattern: "legalApi\\.list"
    - from: "cha-bio-safety/src/pages/LegalFindingsPage.tsx"
      to: "/api/legal/:id/findings"
      via: "legalApi.getFindings via useQuery + legalApi.createFinding via useMutation"
      pattern: "legalApi\\.getFindings"
    - from: "cha-bio-safety/src/pages/LegalFindingDetailPage.tsx"
      to: "/api/legal/:id/findings/:fid/resolve"
      via: "legalApi.resolveFinding via useMutation"
      pattern: "legalApi\\.resolveFinding"
---

<objective>
Build the 3 UI pages for legal inspection: LegalPage (round list), LegalFindingsPage (findings + admin panel), LegalFindingDetailPage (detail + resolve).

Purpose: Users can browse legal inspection rounds, register and track findings, and resolve them with photos and memos -- completing LEGAL-01 and LEGAL-02.
Output: 3 fully functional page components replacing placeholder stubs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-legal-inspection/10-CONTEXT.md
@.planning/phases/10-legal-inspection/10-UI-SPEC.md
@.planning/phases/10-legal-inspection/10-01-SUMMARY.md

@cha-bio-safety/src/pages/RemediationPage.tsx
@cha-bio-safety/src/pages/RemediationDetailPage.tsx
@cha-bio-safety/src/pages/EducationPage.tsx
@cha-bio-safety/src/utils/api.ts
@cha-bio-safety/src/types/index.ts
@cha-bio-safety/src/components/PhotoButton.tsx
@cha-bio-safety/src/stores/authStore.ts

<interfaces>
<!-- Types created by Plan 01 -->

From src/types/index.ts (added by Plan 01):
```typescript
export type LegalInspectionResult = 'pass' | 'fail' | 'conditional'
export type LegalFindingStatus = 'open' | 'resolved'

export interface LegalRound {
  id: string
  title: string
  date: string
  inspectionCategory: string
  status: string
  result: LegalInspectionResult | null
  reportFileKey: string | null
  findingCount: number
  resolvedCount: number
}

export interface LegalFinding {
  id: string
  scheduleItemId: string
  description: string
  location: string | null
  photoKey: string | null
  resolutionMemo: string | null
  resolutionPhotoKey: string | null
  status: LegalFindingStatus
  resolvedAt: string | null
  resolvedBy: string | null
  resolvedByName: string | null
  createdBy: string
  createdByName: string | null
  createdAt: string
}
```

From src/utils/api.ts (added by Plan 01):
```typescript
export const legalApi = {
  list: (year?: string) => api.get<LegalRound[]>(`/legal${year ? `?year=${year}` : ''}`),
  get: (id: string) => api.get<LegalRound>(`/legal/${id}`),
  updateResult: (id: string, body: { result?: string; report_file_key?: string }) => api.patch<void>(`/legal/${id}`, body),
  getFindings: (scheduleItemId: string) => api.get<LegalFinding[]>(`/legal/${scheduleItemId}/findings`),
  createFinding: (scheduleItemId: string, body: { description: string; location?: string; photo_key?: string }) => api.post<{ id: string }>(`/legal/${scheduleItemId}/findings`, body),
  getFinding: (scheduleItemId: string, fid: string) => api.get<LegalFinding>(`/legal/${scheduleItemId}/findings/${fid}`),
  updateFinding: (scheduleItemId: string, fid: string, body: Record<string, any>) => api.put<void>(`/legal/${scheduleItemId}/findings/${fid}`, body),
  resolveFinding: (scheduleItemId: string, fid: string, body: { resolution_memo: string; resolution_photo_key?: string }) => api.post<void>(`/legal/${scheduleItemId}/findings/${fid}/resolve`, body),
}
```

From src/stores/authStore.ts:
```typescript
// useAuthStore.getState() returns { staffId, role, name, token, ... }
// role is 'admin' | 'assistant'
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: LegalPage + LegalFindingsPage</name>
  <files>
    cha-bio-safety/src/pages/LegalPage.tsx,
    cha-bio-safety/src/pages/LegalFindingsPage.tsx
  </files>
  <read_first>
    cha-bio-safety/src/pages/LegalPage.tsx,
    cha-bio-safety/src/pages/LegalFindingsPage.tsx,
    cha-bio-safety/src/pages/RemediationPage.tsx,
    cha-bio-safety/src/pages/EducationPage.tsx,
    cha-bio-safety/src/pages/RemediationDetailPage.tsx,
    cha-bio-safety/src/utils/api.ts,
    cha-bio-safety/src/types/index.ts,
    cha-bio-safety/src/stores/authStore.ts,
    cha-bio-safety/src/components/PhotoButton.tsx,
    .planning/phases/10-legal-inspection/10-UI-SPEC.md
  </read_first>
  <action>
**LegalPage.tsx** — Complete rewrite of placeholder stub. Follow RemediationPage.tsx patterns closely.

Structure (per UI-SPEC "LegalPage" section):
1. **Self-contained header** (48px): title "법적 점검", back button with inline SVG chevron-left (copy exact pattern from RemediationDetailPage.tsx header). `navigate(-1)` on click. Exact styles: `height:48, background:'rgba(22,27,34,0.97)', borderBottom:'1px solid var(--bd)'`, back button `position:'absolute', left:12, width:36, height:36`.

2. **Sticky 2-row filter bar** (copy RemediationPage tab strip pattern):
   - Row 1: 3 status tabs (`전체` / `미조치` / `완료`). `height:44, flex:1` per tab. Active: `background:'var(--bg4)', borderBottom:'2px solid var(--acl)'`. Read/write tab state from URL `?tab=` param using `useSearchParams` (per Interaction Contract — matches RemediationPage pattern).
   - Row 2: Year filter `<select>` — generate years from 2024 to current year. Default: current year (`new Date().getFullYear().toString()`). Style: `background:'var(--bg3)', border:'1px solid var(--bd2)', borderRadius:8, padding:'6px 12px', color:'var(--t1)', fontSize:13`. Use `useState` for year (not URL param, per UI-SPEC).

3. **Data fetching**: `useQuery({ queryKey: ['legal-rounds', year], queryFn: () => legalApi.list(year) })`. Import `useQuery` from `@tanstack/react-query`, `legalApi` from `../utils/api`, `LegalRound` from `../types`.

4. **Filtering logic**: 
   - If tab is `전체`: show all rounds (no client-side filter on findings status).
   - If tab is `미조치`: filter rounds where `r.findingCount > r.resolvedCount` (has unresolved findings).
   - If tab is `완료`: filter rounds where `r.findingCount > 0 && r.findingCount === r.resolvedCount` (all resolved).

5. **Round cards** (per UI-SPEC): `background:'var(--bg3)', border:'1px solid var(--bd)', borderRadius:12, padding:12`. Left accent `borderLeft:'3px solid {color}'` where color is: `pass` -> `var(--safe)` (#22c55e), `fail` -> `var(--danger)` (#ef4444), `conditional` -> `var(--warn)` (#f59e0b), `null` -> `var(--bd2)`.
   - Line 1: `title` (fontSize:14, fontWeight:700, color:var(--t1)) + result badge right-aligned. Badge: `fontSize:11, fontWeight:700, borderRadius:6, padding:'2px 8px'`. Pass: `background:'rgba(34,197,94,.13)', color:'var(--safe)'`, text "적합". Fail: `background:'rgba(239,68,68,.15)', color:'var(--danger)'`, text "부적합". Conditional: `background:'rgba(245,158,11,.15)', color:'var(--warn)'`, text "조건부적합". Null: `color:'var(--t3)'`, text "결과 미입력".
   - Line 2: date formatted as `YYYY.MM.DD` + `지적 {findingCount}건 · 완료 {resolvedCount}건` (fontSize:12, color:var(--t2)).
   - Tap navigates to `/legal/${round.id}`.

6. **Empty state**: heading "법적 점검 이력 없음", body "소방 일정 페이지에서 종합정밀 또는 작동기능 점검을 등록하면 여기에 표시됩니다." (per Copywriting Contract). Centered, padding:'60px 16px', gap:8.

7. **Loading**: 3 skeleton cards (height:72, background:var(--bg3), borderRadius:12, animation: blink).

**LegalFindingsPage.tsx** — Complete rewrite of placeholder stub. Follow RemediationPage list + RemediationDetailPage header patterns.

Structure (per UI-SPEC "LegalFindingsPage" section):
1. **Self-contained header** (48px): title dynamically built from round data. Use round's `inspectionCategory` to extract short label: `title.includes('종합정밀') ? '종합정밀' : '작동기능'` + date formatted as `YYYY.MM.` (e.g., "종합정밀 2025.06."). Back button same pattern.

2. **Admin-only sub-header** (visible when `role === 'admin'`): sticky below main header. Contains:
   - Result `<select>`: options 적합/부적합/조건부적합 + "결과 미입력" as default/null. Values: pass/fail/conditional/''. Style same as year filter select.
   - Save button next to select: "결과 저장" (fontSize:12, fontWeight:700, height:36, background:var(--acl), borderRadius:8, padding:'0 12px'). Calls `legalApi.updateResult(id, { result: selectedResult })`. Toast "점검 결과가 저장되었습니다." on success.
   - PDF upload button: "보고서 업로드" or if `round.reportFileKey` exists, "보고서 보기". Upload uses an `<input type="file" accept="application/pdf">` hidden, triggered by button click. Upload to `/api/uploads` with folder `legal/${id}/report`, then call `legalApi.updateResult(id, { report_file_key: key })`. View: `window.open('/api/uploads/' + round.reportFileKey, '_blank')`.
   - Row style: `padding:'8px 16px', background:'var(--bg2)', borderBottom:'1px solid var(--bd)', display:'flex', gap:8, alignItems:'center'`.

3. **Data fetching**: 
   - Round: `useQuery({ queryKey: ['legal-round', id], queryFn: () => legalApi.get(id!) })` where `id = useParams().id`.
   - Findings: `useQuery({ queryKey: ['legal-findings', id], queryFn: () => legalApi.getFindings(id!) })`.

4. **Finding cards** (per UI-SPEC, 4 lines matching RemediationPage card pattern):
   - `background:'var(--bg3)', border:'1px solid var(--bd)', borderRadius:12, padding:12`.
   - Left accent: `open` -> `var(--danger)`, `resolved` -> `var(--safe)`.
   - Line 1: description truncated (1 line, overflow:hidden, textOverflow:ellipsis, whiteSpace:nowrap, fontSize:14, fontWeight:500) + status badge right-aligned. Badge: `미조치` (background:'rgba(239,68,68,.15)', color:var(--danger)) / `완료` (background:'rgba(34,197,94,.13)', color:var(--safe)). fontSize:11, fontWeight:700, borderRadius:6, padding:'2px 8px'.
   - Line 2: location text (fontSize:12, color:var(--t2)). If null, show "위치 미지정".
   - Line 3: date `createdAt` formatted + `createdByName` (fontSize:11, color:var(--t3)).
   - Tap navigates to `/legal/${id}/finding/${finding.id}`.

5. **Empty state**: heading "지적사항 없음", body "현장에서 지적된 항목을 등록하려면 아래 버튼을 누르세요."

6. **Fixed bottom CTA**: `+ 지적사항 등록` button. Same fixed CTA bar pattern from RemediationDetailPage. Opens BottomSheet.

7. **BottomSheet** for finding registration (per UI-SPEC Interaction Contract):
   - Backdrop: `position:fixed, inset:0, background:'rgba(0,0,0,0.6)', justifyContent:'flex-end'`. Tap backdrop closes.
   - Sheet: `background:'var(--bg2)', borderRadius:'16px 16px 0 0'`, slideUp animation.
   - Drag handle: `width:32, height:4, background:'var(--bd2)', borderRadius:2`, centered, paddingTop:12.
   - Title: "지적사항 등록" (fontSize:16, fontWeight:700).
   - Fields: `지적 내용` (textarea, required, rows:4) + `위치` (text input, optional). Field style: `background:'var(--bg3)', borderRadius:8, padding:12, border:'1px solid var(--bd2)', width:'100%', color:'var(--t1)', fontSize:14`.
   - Optional: PhotoButton for 지적 사진 (photo_key). Use existing `usePhotoUpload` pattern if feasible; otherwise skip photo in BottomSheet and let user add it from detail page.
   - Submit button: "등록" (height:48, background:var(--acl), borderRadius:8, fontWeight:700, fontSize:14, width:'100%'). Calls `legalApi.createFinding(id, { description, location, photo_key })`. On success: toast "지적사항이 등록되었습니다.", close sheet, invalidate queries. On submitting: "처리 중..." disabled state.
   - Cancel button: "취소" (height:48, background:transparent, border:'1px solid var(--bd2)', color:var(--t2)).

8. **Loading**: spinner (width:28, height:28, border animation pattern from UI-SPEC).
  </action>
  <verify>
    <automated>cd /Users/jykevin/Documents/20260328/cha-bio-safety && grep -q "legalApi" src/pages/LegalPage.tsx && grep -q "legalApi" src/pages/LegalFindingsPage.tsx && grep -q "useQuery" src/pages/LegalPage.tsx && grep -q "법적 점검" src/pages/LegalPage.tsx && grep -q "지적사항 등록" src/pages/LegalFindingsPage.tsx && npx tsc --noEmit 2>&1 | head -5 && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - LegalPage.tsx contains `legalApi.list` call
    - LegalPage.tsx contains `useQuery` with queryKey including 'legal-rounds'
    - LegalPage.tsx contains tab state via `useSearchParams` (URL ?tab= param)
    - LegalPage.tsx contains year filter `<select>` with `useState`
    - LegalPage.tsx contains result badge text: '적합', '부적합', '조건부적합', '결과 미입력'
    - LegalPage.tsx contains empty state text "법적 점검 이력 없음"
    - LegalPage.tsx navigates to `/legal/${...id}`
    - LegalFindingsPage.tsx contains `legalApi.getFindings` call
    - LegalFindingsPage.tsx contains `legalApi.createFinding` in mutation
    - LegalFindingsPage.tsx contains admin result `<select>` with `role === 'admin'` guard
    - LegalFindingsPage.tsx contains `legalApi.updateResult` call
    - LegalFindingsPage.tsx contains BottomSheet with textarea for description
    - LegalFindingsPage.tsx contains status badges '미조치' and '완료'
    - LegalFindingsPage.tsx contains empty state text "지적사항 없음"
    - LegalFindingsPage.tsx navigates to `/legal/${id}/finding/${...fid}`
  </acceptance_criteria>
  <done>LegalPage shows round cards with status/year filter and result badges. LegalFindingsPage shows finding cards with admin result panel and finding registration BottomSheet. Navigation between pages works.</done>
</task>

<task type="auto">
  <name>Task 2: LegalFindingDetailPage</name>
  <files>
    cha-bio-safety/src/pages/LegalFindingDetailPage.tsx
  </files>
  <read_first>
    cha-bio-safety/src/pages/LegalFindingDetailPage.tsx,
    cha-bio-safety/src/pages/RemediationDetailPage.tsx,
    cha-bio-safety/src/components/PhotoButton.tsx,
    cha-bio-safety/src/utils/api.ts,
    cha-bio-safety/src/types/index.ts,
    cha-bio-safety/src/stores/authStore.ts,
    .planning/phases/10-legal-inspection/10-UI-SPEC.md
  </read_first>
  <action>
Complete rewrite of LegalFindingDetailPage.tsx. Follow RemediationDetailPage.tsx pattern very closely — same KVRow, SectionHeader, photo display, fixed CTA bar patterns.

Structure (per UI-SPEC "LegalFindingDetailPage" section):

1. **Self-contained header** (48px): title "지적 상세". Back button, same exact pattern.

2. **Params**: `const { id, fid } = useParams()` (id = scheduleItemId, fid = findingId).

3. **Data fetching**: `useQuery({ queryKey: ['legal-finding', id, fid], queryFn: () => legalApi.getFinding(id!, fid!) })`.

4. **Section 1 "지적 정보"** (SectionHeader pattern from RemediationDetailPage):
   - SectionHeader: `fontSize:12, fontWeight:700, color:'var(--t3)', marginBottom:10`, text "지적 정보"
   - KVRow rows (label:value pattern, label width:64, fontSize:12 color:var(--t3), value fontSize:14 color:var(--t1)):
     - 지적 내용: `finding.description` (may be multiline, use `whiteSpace:'pre-wrap'`)
     - 위치: `finding.location ?? '-'`
     - 등록일: `finding.createdAt` formatted as `YYYY.MM.DD HH:mm`
     - 등록자: `finding.createdByName ?? finding.createdBy`

5. **Section 2 "지적 사진"** (if `finding.photoKey` exists):
   - SectionHeader: "지적 사진"
   - Image display: `<img src={'/api/uploads/' + finding.photoKey}` with style `width:'100%', maxHeight:240, objectFit:'cover', borderRadius:10, border:'1px solid var(--bd)', marginTop:12`.
   - If finding is open AND no photo yet: show PhotoButton for before-photo upload. On upload success, call `legalApi.updateFinding(id, fid, { photo_key: uploadedKey })` and invalidate query. Use existing `usePhotoUpload` hook pattern from InspectionPage. R2 folder: `legal/${id}/findings/${fid}/before`.

6. **Section 3 "조치 내용 입력"** (visible when status === 'open'):
   - SectionHeader: "조치 내용"
   - Textarea: `rows:4, placeholder:'조치 내용을 입력하세요'`. Same field style as BottomSheet: `background:'var(--bg3)', borderRadius:8, padding:12, border:'1px solid var(--bd2)', width:'100%', color:'var(--t1)', fontSize:14`.
   - Resolution photo: PhotoButton for after-photo. R2 folder: `legal/${id}/findings/${fid}/after`. Store in local state.

7. **Section 4 "조치 완료"** (visible when status === 'resolved'):
   - SectionHeader: "조치 결과"
   - KVRow: 조치일시 (`finding.resolvedAt` formatted), 조치자 (`finding.resolvedByName ?? finding.resolvedBy`), 조치 내용 (`finding.resolutionMemo`).
   - Resolution photo: if `finding.resolutionPhotoKey`, display image same style as section 2.

8. **Fixed bottom CTA bar** (visible when status === 'open' only):
   - `position:fixed, bottom:0, left:0, right:0, background:'var(--bg)', borderTop:'1px solid var(--bd)', padding:'12px 16px', paddingBottom:'calc(12px + var(--sab, 0px))'`.
   - Button: "조치 완료" (width:'100%', height:48, background:'var(--acl)', borderRadius:12, fontSize:14, fontWeight:700, color:'#fff'). Submitting state: "처리 중..." + disabled.
   - On click: validate resolution_memo non-empty (toast "조치 내용을 입력하세요" if empty). Call `legalApi.resolveFinding(id!, fid!, { resolution_memo: memo, resolution_photo_key: photoKey })`. On success: toast "조치 완료", invalidate queries `['legal-finding', id, fid]` and `['legal-findings', id]`, navigate back or refetch.

9. **Scroll clearance**: when CTA bar visible, add `paddingBottom:'calc(72px + var(--sab, 0px))'` to scroll container.

10. **Error state**: "항목을 불러오지 못했습니다. 뒤로 가서 다시 시도하세요." centered.

11. **Loading**: spinner centered (same pattern).

**Import statements needed:**
```typescript
import { useState } from 'react'
import { useParams, useNavigate } from 'react-router-dom'
import { useQuery, useMutation, useQueryClient } from '@tanstack/react-query'
import toast from 'react-hot-toast'
import { legalApi } from '../utils/api'
import { useAuthStore } from '../stores/authStore'
import type { LegalFinding } from '../types'
import PhotoButton from '../components/PhotoButton'
```
  </action>
  <verify>
    <automated>cd /Users/jykevin/Documents/20260328/cha-bio-safety && grep -q "legalApi.getFinding" src/pages/LegalFindingDetailPage.tsx && grep -q "legalApi.resolveFinding" src/pages/LegalFindingDetailPage.tsx && grep -q "지적 상세" src/pages/LegalFindingDetailPage.tsx && grep -q "조치 완료" src/pages/LegalFindingDetailPage.tsx && npx tsc --noEmit 2>&1 | head -5 && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - LegalFindingDetailPage.tsx contains `legalApi.getFinding` call with useQuery
    - LegalFindingDetailPage.tsx contains `legalApi.resolveFinding` call with useMutation
    - LegalFindingDetailPage.tsx contains KVRow-style layout (label width:64 or similar pattern)
    - LegalFindingDetailPage.tsx contains SectionHeader "지적 정보" text
    - LegalFindingDetailPage.tsx contains conditional render for status === 'open' (resolution input)
    - LegalFindingDetailPage.tsx contains conditional render for status === 'resolved' (resolution display)
    - LegalFindingDetailPage.tsx contains fixed CTA bar with "조치 완료" button
    - LegalFindingDetailPage.tsx contains validation toast "조치 내용을 입력하세요"
    - LegalFindingDetailPage.tsx contains photo display with `objectFit:'cover'`
    - LegalFindingDetailPage.tsx contains PhotoButton import and usage
    - LegalFindingDetailPage.tsx header title is "지적 상세"
  </acceptance_criteria>
  <done>LegalFindingDetailPage shows finding info, photos, resolution input (open state) or resolution result (resolved state), with fixed CTA to resolve findings.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of legal inspection flow</name>
  <files>cha-bio-safety/src/pages/LegalPage.tsx</files>
  <action>Human verifies the complete legal inspection flow end-to-end in the running application. No code changes needed — this is a visual/functional checkpoint after Tasks 1 and 2.</action>
  <what-built>Complete legal inspection feature: 3 pages (round list, findings list, finding detail) with admin result/report panel, finding registration, photo upload, and resolution flow.</what-built>
  <how-to-verify>
    1. Open app, tap hamburger menu, tap "법적 점검" — should show round cards from schedule_items (or empty state if no fire schedule items exist)
    2. If no rounds: go to SchedulePage, create a fire schedule item with subcategory "소방 상반기 종합정밀점검", then return to /legal
    3. Tap a round card — should show findings page with "+ 지적사항 등록" button at bottom
    4. Tap "+ 지적사항 등록" — BottomSheet should appear with description textarea and location input
    5. Register a finding — should appear in list with "미조치" badge
    6. Tap the finding — should show detail page with "지적 정보" section and "조치 완료" button
    7. Type resolution memo, tap "조치 완료" — should toast "조치 완료" and status changes to resolved
    8. (Admin only) On findings page, verify result select and "보고서 업로드" button appear
    9. (Admin only) Select "적합" and tap "결과 저장" — should toast "점검 결과가 저장되었습니다."
    10. Navigate back to /legal — round card should show green left accent and "적합" badge
    11. Test status filter tabs (전체/미조치/완료) on /legal page
    12. Test year filter select
  </how-to-verify>
  <verify>Human approves the visual and functional behavior of all 3 legal inspection pages</verify>
  <done>User confirms all 12 verification steps pass: round list, finding registration, resolution flow, admin controls, and filtering all work correctly.</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
1. Navigate to /legal from SideMenu — page loads with round cards or empty state
2. Status tabs filter rounds correctly
3. Year filter changes displayed rounds
4. Finding registration BottomSheet works (description required, location optional)
5. Finding detail shows all sections with correct data
6. Resolution flow works: memo required, photo optional, status changes to resolved
7. Admin can set result and upload/view report PDF
8. Back navigation preserves tab state on /legal
9. `npm run build` passes without errors
</verification>

<success_criteria>
- 3 pages fully replace placeholder stubs
- Round list displays schedule_items with finding counts and result badges
- Findings list shows cards with status badges, sorted by open-first
- Finding detail shows info sections, photos, resolution input/display
- Admin-only result panel and PDF upload work
- Resolution flow with memo validation and toast notifications
- All navigation and back-button behavior works correctly
- Build passes without errors
</success_criteria>

<output>
After completion, create `.planning/phases/10-legal-inspection/10-02-SUMMARY.md`
</output>
