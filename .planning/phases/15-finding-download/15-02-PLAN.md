---
phase: 15-finding-download
plan: 02
type: execute
wave: 2
depends_on:
  - 15-01
files_modified:
  - cha-bio-safety/src/pages/LegalFindingsPage.tsx
autonomous: false
requirements:
  - DL-02
  - DL-03
must_haves:
  truths:
    - "관리자가 라운드 목록에서 일괄 다운로드 버튼을 클릭하면 전체 지적사항이 ZIP으로 다운로드된다"
    - "ZIP 내부 구조가 finding-001_위치/내용.txt, finding-001_위치/지적사진-1.jpg 형식이다"
    - "비관리자(assistant)에게는 일괄 다운로드 버튼이 표시되지 않는다"
    - "iOS PWA 홈 화면 모드에서 ZIP이 window.open으로 열려 공유시트가 표시된다"
    - "건별 다운로드와 일괄 다운로드 모두 프로덕션에서 동작한다"
  artifacts:
    - path: "cha-bio-safety/src/pages/LegalFindingsPage.tsx"
      provides: "admin 서브헤더에 일괄 다운로드 버튼"
      contains: "downloadFindingsZip"
  key_links:
    - from: "cha-bio-safety/src/pages/LegalFindingsPage.tsx"
      to: "fflate"
      via: "dynamic import for zipSync"
      pattern: "import.*fflate"
    - from: "cha-bio-safety/src/pages/LegalFindingsPage.tsx"
      to: "cha-bio-safety/src/utils/findingDownload.ts"
      via: "import { buildMetaTxt }"
      pattern: "buildMetaTxt"
    - from: "cha-bio-safety/src/pages/LegalFindingsPage.tsx"
      to: "/api/uploads/{key}"
      via: "fetch for photo ArrayBuffer in ZIP"
      pattern: "fetch.*api/uploads"
---

<objective>
라운드 전체 지적사항을 일괄 ZIP으로 다운로드하는 기능을 LegalFindingsPage에 추가하고, 프로덕션 배포 후 건별/일괄 다운로드를 검증한다.

Purpose: DL-02 (일괄 ZIP)을 구현하고 DL-03 (iOS PWA)을 프로덕션에서 검증. fflate.zipSync로 클라이언트사이드 ZIP 생성, window.open으로 iOS 호환.
Output: LegalFindingsPage admin 서브헤더에 일괄 다운로드 버튼 + 프로덕션 배포/검증
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-finding-download/15-CONTEXT.md
@.planning/phases/15-finding-download/15-RESEARCH.md
@.planning/phases/15-finding-download/15-01-SUMMARY.md

@cha-bio-safety/src/pages/LegalFindingsPage.tsx
@cha-bio-safety/src/pages/ReportsPage.tsx
@cha-bio-safety/src/utils/findingDownload.ts
@cha-bio-safety/src/utils/api.ts

<interfaces>
<!-- From Plan 01 output: findingDownload.ts exports -->
export async function fetchAsBase64(url: string): Promise<string | null>
export function buildReportHtml(finding: LegalFinding, photosHtml: string): string
export async function openFindingReport(finding: LegalFinding): Promise<void>
export function buildMetaTxt(finding: LegalFinding): string

<!-- From LegalFindingsPage existing admin sub-header (lines 494-593) -->
<!-- Admin sub-header already exists with: result select, save button, report upload/view -->
<!-- New ZIP download button goes into the same flex row, after report button -->

<!-- legalApi from api.ts -->
legalApi.getFindings(scheduleItemId: string): Promise<LegalFinding[]>
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add bulk ZIP download to LegalFindingsPage</name>
  <files>cha-bio-safety/src/pages/LegalFindingsPage.tsx</files>
  <read_first>
    cha-bio-safety/src/pages/LegalFindingsPage.tsx (full file - admin sub-header at lines 494-593, findings data structure, round object)
    cha-bio-safety/src/pages/ReportsPage.tsx (lines 109-137 for downloadAllAsZip pattern with fflate)
    cha-bio-safety/src/utils/findingDownload.ts (for buildMetaTxt import)
    cha-bio-safety/src/utils/api.ts (for legalApi methods)
    .planning/phases/15-finding-download/15-RESEARCH.md (ZIP pattern, pitfalls 3-5)
  </read_first>
  <action>
Modify `cha-bio-safety/src/pages/LegalFindingsPage.tsx`:

**New import (add to existing imports):**
```typescript
import { buildMetaTxt } from '../utils/findingDownload'
```

**Add state variable (inside the component, near other state):**
```typescript
const [zipLoading, setZipLoading] = useState<string | false>(false)
```
`zipLoading` is `false` when idle, or a progress message string when downloading.

**Add downloadFindingsZip function (inside the component, after existing handlers):**

```typescript
async function handleZipDownload() {
  if (!findings?.length) return
  setZipLoading('준비 중...')
  try {
    const { zipSync } = await import('fflate')
    const files: Record<string, Uint8Array> = {}
    const encoder = new TextEncoder()

    for (let i = 0; i < findings.length; i++) {
      const f = findings[i]
      const idx = String(i + 1).padStart(3, '0')
      const folderName = `finding-${idx}_${(f.location ?? '위치없음').replace(/[\/\\:*?"<>|]/g, '_')}`
      setZipLoading(`수집 중... (${i + 1}/${findings.length})`)

      // 내용.txt — always included even if no photos (Pitfall 3)
      files[`${folderName}/내용.txt`] = encoder.encode(buildMetaTxt(f))

      // 지적 사진 (parallel within finding, sequential across findings — Pitfall from research)
      const photoResults = await Promise.allSettled(
        f.photoKeys.map(k => fetch('/api/uploads/' + k).then(r => r.arrayBuffer()))
      )
      photoResults.forEach((r, j) => {
        if (r.status === 'fulfilled') {
          files[`${folderName}/지적사진-${j + 1}.jpg`] = new Uint8Array(r.value)
        }
      })

      // 조치 사진
      const resResults = await Promise.allSettled(
        f.resolutionPhotoKeys.map(k => fetch('/api/uploads/' + k).then(r => r.arrayBuffer()))
      )
      resResults.forEach((r, j) => {
        if (r.status === 'fulfilled') {
          files[`${folderName}/조치사진-${j + 1}.jpg`] = new Uint8Array(r.value)
        }
      })
    }

    setZipLoading('압축 중...')
    const zipped = zipSync(files, { level: 6 })
    const blob = new Blob([zipped.buffer as ArrayBuffer], { type: 'application/zip' })
    const url = URL.createObjectURL(blob)

    // Per D-08, D-10: window.open only, NO <a download>
    window.open(url, '_blank')
    setTimeout(() => URL.revokeObjectURL(url), 2000)
    toast.success('다운로드 완료')
  } catch (e) {
    console.error('ZIP download failed:', e)
    toast.error('다운로드에 실패했습니다')
  } finally {
    setZipLoading(false)
  }
}
```

**Add ZIP download button in admin sub-header (per D-04, D-12):**
Inside the admin sub-header div (after the report upload/view button, around line ~591, before the closing `</div>` of the admin sub-header), add:

```tsx
<button
  onClick={handleZipDownload}
  disabled={!!zipLoading || !findings?.length}
  style={{
    fontSize: 12,
    fontWeight: 700,
    height: 36,
    background: 'var(--bg3)',
    borderRadius: 8,
    padding: '0 12px',
    border: '1px solid var(--bd2)',
    color: 'var(--t1)',
    cursor: (zipLoading || !findings?.length) ? 'not-allowed' : 'pointer',
    opacity: (zipLoading || !findings?.length) ? 0.6 : 1,
    flexShrink: 0,
    whiteSpace: 'nowrap',
  }}
>
  {zipLoading || '일괄 다운로드'}
</button>
```

**ZIP filename pattern (Claude's Discretion):**
The `window.open(url, '_blank')` opens the blob URL directly. The browser will name it generically. This is acceptable for iOS PWA (공유시트 handles naming). For desktop, the blob approach with window.open still triggers download with a generic name, which is the tradeoff for iOS compatibility.

Note: `findings` is the existing variable from the component's React Query data. Verify the exact variable name by reading the file. It may be `data` or destructured differently. Adapt accordingly.
  </action>
  <verify>
    <automated>cd /Users/jykevin/Documents/20260328 && npx tsc --noEmit --project cha-bio-safety/tsconfig.json 2>&1 | head -20 && grep -c "zipSync\|handleZipDownload\|buildMetaTxt" cha-bio-safety/src/pages/LegalFindingsPage.tsx</automated>
  </verify>
  <acceptance_criteria>
    - File contains `import { buildMetaTxt } from '../utils/findingDownload'`
    - File contains `import('fflate')` (dynamic import for zipSync)
    - File contains `handleZipDownload` function definition
    - File contains `zipSync(files, { level: 6 })` call
    - File contains `window.open(url, '_blank')` for ZIP blob
    - File does NOT contain `<a download` or `.download =` or `a.click()` (D-08)
    - File contains `Promise.allSettled` for photo fetching
    - File contains `일괄 다운로드` button text
    - File contains `buildMetaTxt(f)` call for each finding
    - TypeScript compilation passes
  </acceptance_criteria>
  <done>LegalFindingsPage admin sub-header has 일괄 다운로드 button that creates ZIP with finding-NNN_위치/내용.txt + photos structure and opens via window.open</done>
</task>

<task type="auto">
  <name>Task 2: Build and deploy to production</name>
  <files>cha-bio-safety/src/pages/LegalFindingsPage.tsx</files>
  <read_first>
    cha-bio-safety/package.json (for build/deploy scripts)
  </read_first>
  <action>
1. Run `cd /Users/jykevin/Documents/20260328/cha-bio-safety && npm run build` to verify build succeeds
2. Deploy to production: `cd /Users/jykevin/Documents/20260328/cha-bio-safety && npm run deploy -- --branch production`
   (CRITICAL: must include `--branch production` per project memory — without it deploys to Preview)
3. Wait for deployment URL to appear in output
  </action>
  <verify>
    <automated>cd /Users/jykevin/Documents/20260328/cha-bio-safety && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits with code 0
    - `npm run deploy -- --branch production` completes successfully
    - Deployment URL is printed in output
  </acceptance_criteria>
  <done>Production deployment succeeds with both 건별 and 일괄 download features</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify downloads on production</name>
  <files>none</files>
  <action>Human verification of both download features on production deployment.</action>
  <what-built>
    건별 다운로드 (LegalFindingDetailPage 헤더 다운로드 아이콘) + 일괄 ZIP 다운로드 (LegalFindingsPage admin 서브헤더 일괄 다운로드 버튼). 모두 window.open() 패턴 사용 (iOS PWA 호환).
  </what-built>
  <how-to-verify>
    프로덕션 배포 완료 후:

    1. **건별 다운로드 테스트:**
       - 관리자 계정으로 로그인
       - 법적 점검 -> 라운드 선택 -> 지적사항 1건 탭
       - 헤더 우측 다운로드 아이콘 버튼 확인
       - 클릭 -> 새 탭에 HTML 보고서 열림 확인
       - 보고서에 메타데이터(항목, 위치, 상태, 날짜) + 사진 표시 확인
       - 인쇄 버튼 동작 확인

    2. **일괄 다운로드 테스트:**
       - 라운드 목록 화면에서 admin 서브헤더의 '일괄 다운로드' 버튼 확인
       - 클릭 -> 진행률 표시 후 ZIP 다운로드 확인
       - ZIP 내부에 finding-001_위치/ 폴더 구조 확인

    3. **권한 테스트:**
       - assistant 계정으로 로그인 시 두 버튼 모두 미표시 확인

    4. **iOS PWA 테스트 (가능하면):**
       - iOS 기기에서 PWA 홈 화면 모드로 건별/일괄 다운로드 동작 확인
       - 공유시트가 표시되는지 확인
  </how-to-verify>
  <verify>Human confirms downloads work on production</verify>
  <done>User approves both download features on production</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
1. TypeScript compiles: `npx tsc --noEmit` passes
2. Build succeeds: `npm run build` passes
3. No `<a download` pattern in any modified file (D-08 compliance)
4. ZIP contains finding-NNN_위치/ folder structure with 내용.txt + photos
5. Admin-only: buttons gated by `role === 'admin'`
6. Production deployment successful
</verification>

<success_criteria>
- LegalFindingsPage admin sub-header shows 일괄 다운로드 button
- Button creates ZIP with fflate.zipSync containing structured folders
- ZIP opens via window.open (iOS PWA safe)
- Both 건별 and 일괄 downloads verified on production
- No <a download> usage (D-08)
</success_criteria>

<output>
After completion, create `.planning/phases/15-finding-download/15-02-SUMMARY.md`
</output>
