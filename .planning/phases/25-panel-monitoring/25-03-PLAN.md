---
phase: 25-panel-monitoring
plan: 03
type: execute
wave: 2
depends_on: ['25-01']
files_modified:
  - cha-bio-safety/src/pages/InspectionPage.tsx
autonomous: true
requirements: [PANEL-UI-02, PANEL-UI-04]
must_haves:
  truths:
    - "Mobile 화재수신반 페이지 (expanded FireAlarmModal) shows header 점검모드 toggle + live card + 48h events + 5-field form"
    - "평상시=form default 비화재보; 경보중=비화재보 자동초안 notice+form; 점검모드=자동화 안내문 + live/events kept + form hidden"
    - "경보중 저장 calls alarmApi.resolve(activeAlarmId,...) NOT fireAlarmApi.create; 평상시 저장 calls fireAlarmApi.create + invalidateQueries(['fire-alarm-recent'])"
    - "Tapping the live card opens a fullscreen zoom viewer with double-tap/pinch (usePinchZoom) and body:fixed-free scroll-lock"
    - "/inspection?panel=fire-alarm auto-opens the expanded modal"
  artifacts:
    - path: "cha-bio-safety/src/pages/InspectionPage.tsx"
      provides: "Expanded FireAlarmModal + mobile fullscreen zoom viewer + deep-link auto-open"
      contains: "alarmApi.resolve"
  key_links:
    - from: "FireAlarmModal 경보중 save"
      to: "alarmApi.resolve"
      via: "POST /api/alarm/:id/resolve"
      pattern: "alarmApi\\.resolve"
    - from: "FireAlarmModal live card tap"
      to: "usePinchZoom"
      via: "fullscreen zoom viewer"
      pattern: "usePinchZoom"
---

<objective>
Expand the existing `FireAlarmModal` (InspectionPage.tsx:5337) in place into the MOBILE 화재수신반 페이지 (Surface 2): header 점검모드 toggle + live card (tap→zoom) + 48h events + the existing 5-field form, across 평상시 / 경보중 / 점검모드. Add the mobile fullscreen zoom viewer (Surface 4) and the `/inspection?panel=fire-alarm` deep-link auto-open. The 경보중 save calls `alarmApi.resolve` (chip 소멸), the 평상시 save keeps `fireAlarmApi.create` (+ the missing invalidate).

Purpose: Field staff monitor the panel live, review 48h auto-detected events, and complete the 비화재보 auto-draft — all from the existing inspection entry, no new route.
Output: Expanded FireAlarmModal + zoom viewer + deep-link handling in `InspectionPage.tsx`, consuming 25-01 primitives.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

**Track note:** cbc7119-design, wrangler forbidden. Endpoints undeployed → panel/alarm queries wrapped try/catch → 평상시 fallback; maint toggle optimistic. Validation = tsc + emoji-zero grep + token-class grep + (manual) cbc7119-preview per state. NO unit tests. InspectionPage carries legacy emojis elsewhere — do NOT copy them; new markup = lucide only.
</execution_context>

<context>
@.planning/phases/25-panel-monitoring/25-UI-SPEC.md
@.planning/phases/25-panel-monitoring/25-INTEGRATION-MAP.md
@.planning/phases/25-panel-monitoring/CONTEXT.md
@.planning/phases/25-panel-monitoring/25-HANDOFF.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Expand FireAlarmModal chrome — header maint toggle + live card + 48h events</name>
  <files>cha-bio-safety/src/pages/InspectionPage.tsx</files>
  <read_first>
    - cha-bio-safety/src/pages/InspectionPage.tsx — `FireAlarmModal` (:5337-~5425): KST now/date/time (:5338-5343), state (:5342-5348), `handleSave`→`fireAlarmApi.create` (:5350-5354), labelCls/inputCls (:5360-5361), fixed shell (:5364 `top-[var(--sat)] bottom-[calc(54px+safe-area)]`), header (:5367-5368 Bell + "화재수신반 기록"), body (:5372+ 구분/발생일시/장소/원인/조치). Mount `showFireAlarm` (:4687, :5329-5330). lucide import line (:6 area — `import { ... } from 'lucide-react'`).
    - 25-UI-SPEC.md Surface 2 (Header `.gh`/`.gh-maint`/`.gh-mswitch`; Body stack table; live-card `.live-screen`/`.live-ov`/`.live-fshint`/`.live-status`; evt-card `.eh`/`.evt`/`.badge2`) + Iconography (BellRing/BellOff/Maximize2/AlertTriangle/RefreshCw + dot spans) + Copywriting
    - 25-INTEGRATION-MAP.md §3 (extend not rebuild; form is layout-agnostic) + §6 (react-query polling, invalidate gap, maint 409 confirm) + §7 risk 10 (fixed overlay math)
  </read_first>
  <action>
    Convert `FireAlarmModal({ onClose })` into the rich page. Add react-query state inside the component: `status` (`['panel-status']`, try/catch->null, refetchInterval 15_000, retry:false), `activeAlarm` (`['alarm-active']`, try/catch->null), `events` (`['alarm-events']` = `alarmApi.getEvents(48)`, try/catch->[]). Derive `mode: 'normal' | 'alarm' | 'maint'` = maint.enabled ? 'maint' : (activeAlarm ? 'alarm' : 'normal'). Import `panelApi`, `alarmApi`, `type Alarm` from `../utils/api`; `LivePanelImage` from `../components/panel/LivePanelImage`; `freshnessLabel` from `../components/panel/freshness`; lucide `BellRing, BellOff, Maximize2, AlertTriangle, RefreshCw`.

    HEADER (`.gh` = `flex items-center h-12 px-3 bg-surface-page border-b border-border-default`): replace current header. Left back button `ChevronLeft` in `w-7 h-7 rounded-[7px] bg-surface-sunken text-text-secondary` (w-7/h-7 = 32px) -> onClose. Title `BellRing` size 18 + "화재수신반" in `flex-1 text-title font-semibold text-text-primary gap-[7px]`. RIGHT single control = 점검모드 toggle `.gh-maint`: `inline-flex items-center gap-1.5 bg-surface-sunken border border-border-default rounded-pill px-[7px] py-1 pl-[9px] text-[11px] font-bold leading-none text-text-tertiary` + `BellOff` size 13 + switch `.gh-mswitch` (`relative w-7 h-[18px] rounded-pill`, knob 14px translateX(14px), ON track `bg-info`). ON state adds `text-text-primary bg-[rgba(173,182,192,.16)] border-border-strong`. `role="switch" aria-checked={maintOn}`. onClick -> maint toggle handler (wired in Task 2, incl. 409 confirm).

    BODY (`.scroll > .cont` = `flex flex-col gap-2.5 px-3` inside the scroll area):
    - live-card `.card live-card` = `bg-surface-raised border border-border-default rounded-md overflow-hidden`. Inner `.live-screen relative w-full aspect-video bg-black cursor-pointer` containing `<LivePanelImage frameUpdatedAt={status?.frameUpdatedAt} imgClassName="w-full h-full object-cover" />`. onClick -> open zoom viewer (Task 3, `setZoomOpen(true)`). Overlays: `.live-ov` top-left LIVE badge (`absolute top-[7px] left-[7px] inline-flex items-center gap-1 rounded-pill px-[7px] py-0.5 text-[10px] font-extrabold text-white`, bg `rgba(34,197,94,.85)` normal / `rgba(239,68,68,.9)` alarm) with white blink DOT SPAN `.bdot`; `.live-fshint` bottom-right `Maximize2` size 12 + "탭하면 크게 보기" (`bg-black/50 rounded-sm px-[7px] py-0.5 text-[10px] pointer-events-none`). `.live-status` = `flex items-center gap-1.5 flex-wrap p-2 px-[11px] text-caption text-text-secondary tabular-nums` (mobile live-status = 12px -> `text-caption`): normal = green dot span + `text-safe font-bold` "정상" · 이상 없음 · `{freshnessLabel(status?.frameUpdatedAt).label}`; alarm = `text-danger` + `font-extrabold` "화재 발생" · `{activeAlarm.location}` · `{activeAlarm.detectedAt}`. In alarm mode add `firepulse` + `border-danger-bar bg-danger-bg` + red box-shadow on the card.
    - evt-card `.card evt-card`: header `.eh` `flex items-center justify-between p-[7px_12px] border-b border-border-default` = `sec-cap` "최근 이벤트 (자동감지)" + pill "최근 48시간" (`bg-surface-sunken rounded-pill text-[11px] text-text-tertiary px-2`); alarm mode -> red pill "감지중 {n}" (`text-danger bg-danger-bg border-[rgba(239,68,68,.4)]`). Rows: map `events` -> `.evt flex items-start gap-[9px] p-[9px_12px] border-b`: `.badge2` (fire=감지중 danger / non=비화재보 gray `text-text-secondary bg-surface-sunken` / equip=safe) + `.e-time` mono 11.5px tertiary + `.e-loc` 13px font-semibold primary + `.e-desc` caption tertiary + `.e-auto` info tag. Empty events (평상시, none) -> "최근 48시간 자동감지 이벤트 없음" muted row. Events stay visible in 점검모드 (조회 유지).

    Keep the existing fixed shell (`top:var(--sat)/bottom:calc(54px+safe-area)`) — it is correct for the mobile modal (risk 10 only bites when lifting into a pane; not here). Emoji-zero; lucide + dot spans only.
  </action>
  <verify>
    <automated>cd cha-bio-safety && npx tsc --noEmit && grep -q "gh-maint\|role=\"switch\"" src/pages/InspectionPage.tsx && grep -q "최근 이벤트 (자동감지)" src/pages/InspectionPage.tsx && grep -q "LivePanelImage" src/pages/InspectionPage.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` passes.
    - Header renders `BellRing` title + a `role="switch"` 점검모드 toggle using `w-7 h-7` (32px) back button and `h-[18px]` switch track.
    - live-card renders `<LivePanelImage>` with `aspect-video`, a dot-span LIVE badge (not a lucide dot), and `Maximize2` fshint.
    - evt-card maps `events` with fire/non/equip `badge2` variants and an empty-state row.
    - live-status uses `text-caption` (12px) not `text-body-sm`.
    - Emoji grep in the changed region matches nothing (lucide only).
  </acceptance_criteria>
  <done>FireAlarmModal chrome expanded: header maint toggle + live card (tap-to-zoom hook point) + 48h events; states scaffolded via mode.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Form states (평상/경보/점검) + save branch (resolve vs create) + maint 409 confirm</name>
  <files>cha-bio-safety/src/pages/InspectionPage.tsx</files>
  <read_first>
    - cha-bio-safety/src/pages/InspectionPage.tsx — the existing form fields (:5372-5421: 구분 toggle default `non_fire`, 발생일시 date+time, 발생장소, 발생원인 default '오작동', 조치사항 default '자동복구, 현장확인'), `handleSave` (:5350-5354), inputCls (:5361). The `mode`/`activeAlarm`/`status` state added in Task 1.
    - 25-UI-SPEC.md Surface 2 form/formbar rows + panel-notice + maint-autonote blocks; Copywriting (경보 자동감지 안내, 점검모드 자동화 안내문, Primary CTA 평상시/경보중, need/입력대기 hints, Destructive confirmation)
    - 25-HANDOFF.md §1.5 (경보 중 maint ON -> 409 active_alarm_requires_confirm -> confirmAlarm:true) + §3 resolve box; 25-INTEGRATION-MAP.md §6 (resolve vs create + invalidate keys)
  </read_first>
  <action>
    Wire the three-state body + save branch. Keep the existing 5 field states; extend defaults/decoration by mode:
    - maint-autonote block: shown ONLY in `mode==='maint'` — `flex gap-2 p-2 px-[11px] rounded-[10px] bg-surface-sunken border border-border-strong text-caption text-text-tertiary leading-normal` + `RefreshCw` size 13 + the 점검모드 자동화 안내문 copy (verbatim from Copywriting Contract). Placed above live-card per stack table.
    - panel-notice block: shown ONLY in `mode==='alarm'` — `flex gap-2 p-[9px_11px] rounded-[10px] bg-danger-bg border border-[rgba(239,68,68,.4)] text-caption text-danger` + danger blink dot span `.pdot` + the 경보 자동감지 안내 copy (verbatim). Placed above live-card.
    - form + formbar: rendered when `mode!=='maint'` (hidden in 점검모드 via conditional, the `.hide-maint` rule). Form header `.fh`: 평상시 `Plus` "수동 기록 추가"; 경보중 `AlertTriangle` `text-danger` "자동 생성 초안 — 보완 필요".
      - 구분 `.seg`: default `non_fire`. non_fire selected = `border-2 border-accent bg-[rgba(59,130,246,.13)] text-[#60a5fa]`; fire selected = `border-2 border-danger-bar bg-danger-bg text-danger`. 경보중 adds a "자동선택" info tag on 비화재보.
      - 발생일시: 경보중 -> info-tinted border + "자동" tag, prefilled from `activeAlarm.detectedAt` (split into date/time). 평상시 keeps KST-now default.
      - 발생장소: 경보중 -> `.need` (`border-danger-bar bg-danger-bg`) + label "· 확인 필요". placeholder "현장 확인 후 입력 (예: B1F-2 DIV 경비과 3F)".
      - 발생원인 (default '오작동'): 경보중 -> `.need` + "· 확인 필요". placeholder "현장 확인 후 입력 (예: 실화재 / 오작동 / 습기)".
      - 조치사항 (default '자동복구, 현장확인'): 경보중 -> info border + "· 입력 대기" (`text-info`).
    - formbar `.formbar` = `flex gap-2 p-[11px_14px] bg-surface-raised border-t border-border-default`: `.btn-close` "닫기" (`bg-surface-page border border-border-strong text-text-secondary rounded-md`) -> onClose; `.btn-save` gradient (inline `linear-gradient(...#1d4ed8, #0ea5e9)`).

    SAVE BRANCH (rewrite `handleSave`):
    - 평상시 (`mode==='normal'`): button label "점검 기록 저장" -> `await fireAlarmApi.create({ type, occurred_at: `${date} ${time}:00`, location, cause, action })` (unchanged shape) THEN `queryClient.invalidateQueries({queryKey:['fire-alarm-recent']})` (fixes the known gap) + toast + onClose.
    - 경보중 (`mode==='alarm'`): button label "조치완료 후 저장" -> `await alarmApi.resolve(activeAlarm.id, { type, occurredAt: `${date} ${time}:00`, location, cause, action })` (NOT create) THEN `queryClient.invalidateQueries({queryKey:['alarm-active']})` + `invalidateQueries({queryKey:['fire-alarm-recent']})` (dashboard chip 소멸) + toast + onClose. `activeAlarm.id` is the resolve `:id`.

    MAINT TOGGLE HANDLER (from Task 1): `try { await panelApi.setMaint({enabled:!maintOn}) } catch (e) { if (409 / error==='active_alarm_requires_confirm') { window.confirm(경보 진행 중… 계속할까요? copy) -> retry panelApi.setMaint({enabled:true, confirmAlarm:true}) } }` then invalidate `['panel-status','alarm-active']`. Optimistic UI acceptable while the mutation is in flight. Use `useQueryClient()`. Emoji-zero; all copy verbatim from the Copywriting Contract.
  </action>
  <verify>
    <automated>cd cha-bio-safety && npx tsc --noEmit && grep -q "alarmApi.resolve" src/pages/InspectionPage.tsx && grep -q "조치완료 후 저장" src/pages/InspectionPage.tsx && grep -q "fire-alarm-recent" src/pages/InspectionPage.tsx && grep -q "confirmAlarm" src/pages/InspectionPage.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` passes.
    - 경보중 save calls `alarmApi.resolve(activeAlarm.id, {...})` and NOT `fireAlarmApi.create`; 평상시 save calls `fireAlarmApi.create` + `invalidateQueries(['fire-alarm-recent'])`.
    - Maint toggle handles 409 `active_alarm_requires_confirm` via confirm popup + retry with `confirmAlarm:true`.
    - panel-notice appears only in alarm mode; maint-autonote only in maint mode; form+formbar hidden in maint mode.
    - CTA text is "점검 기록 저장" (평상시) / "조치완료 후 저장" (경보중); 비화재보 default retained.
  </acceptance_criteria>
  <done>Three form states + resolve/create save branch + maint 409 confirm wired; dashboard chip 소멸 path (resolve) correct.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Mobile fullscreen zoom viewer + /inspection?panel=fire-alarm auto-open</name>
  <files>cha-bio-safety/src/pages/InspectionPage.tsx</files>
  <read_first>
    - cha-bio-safety/src/pages/InspectionPage.tsx — FireAlarmModal live-card onClick hook point (Task 1), mount `showFireAlarm` (:4687, :5329-5330), the component's imports.
    - cha-bio-safety/src/hooks/usePinchZoom.ts (from 25-01) — `{ containerRef, scale, transform, bind, reset }`
    - cha-bio-safety/src/components/SideMenu.tsx — scroll-lock pattern (:91-105: save `body.style.overflow`, set 'hidden', add non-passive touchmove preventDefault except inside panel, restore on cleanup). NEVER `body.position:fixed`.
    - 25-UI-SPEC.md Surface 4 (`.fsv`/`.fsv-close`/`.fsv-top`/`.fsv-badge`/`.fsv-frame`/`.fsv-hint`) + Copywriting (줌 뷰어 hint mobile — KEEPS text) + Spacing exceptions (safe-area pins, IS_ANDROID?100svh:100dvh)
    - 25-INTEGRATION-MAP.md §6 (scroll-lock no body:fixed; viewport units)
  </read_first>
  <action>
    Add a mobile fullscreen zoom viewer component (local to InspectionPage, e.g. `PanelZoomViewer` inline or a nested function) opened from the live-card tap (`zoomOpen` state). Layout `.fsv` = `fixed inset-0 flex flex-col bg-[#05070a] text-white` pinned to safe-area: `top:var(--sat,0px)` / `bottom:calc(54px+var(--sab,0px))` (never bare 100vh); z above the modal.
    - `.fsv-close` top-right circle `absolute top-[11px] right-3 w-[34px] h-[34px] rounded-full bg-white/12` with `X` size 17 -> `setZoomOpen(false)` + `reset()`.
    - `.fsv-top`: `.fsv-badge` LIVE dot span (green normal / red when alarm) + `.fsv-label` "실시간 수신반 화면 · 자세히 보기" (or 화재 variant when alarm).
    - `.fsv-frame` = `aspect-video rounded-md bg-black cursor-zoom-in` using `usePinchZoom`: set `ref={containerRef}`, `style={{ touchAction:'none', transform }}`, spread `{...bind}`; inner `<LivePanelImage frameUpdatedAt={status?.frameUpdatedAt} imgClassName="w-full h-full object-cover" />`.
    - `.fsv-hint`: "화면을 더블탭(두 번 터치)하면 확대 · 다시 더블탭하면 원복" — mobile KEEPS the hint text (bold the 더블탭 spans).
    SCROLL-LOCK: when `zoomOpen` toggles true, apply the SideMenu pattern (save+set `body.style.overflow='hidden'`, add non-passive touchmove preventDefault except inside the frame, restore on cleanup) in a `useEffect` keyed on `zoomOpen`. Do NOT set `body.position:fixed`.

    DEEP-LINK AUTO-OPEN: in the InspectionPage top-level component, read the query string (`useSearchParams` or `location.search`) once on mount; if `panel === 'fire-alarm'` (and optionally `zoom==='1'`) call `setShowFireAlarm(true)`. This is the destination for the equip push deep-link (sw.ts, 25-06) and dashboard navigation (25-02). Import `usePinchZoom` from `../hooks/usePinchZoom`; lucide `X`. Emoji-zero.
  </action>
  <verify>
    <automated>cd cha-bio-safety && npx tsc --noEmit && grep -q "usePinchZoom" src/pages/InspectionPage.tsx && grep -q "더블탭" src/pages/InspectionPage.tsx && grep -q "panel=fire-alarm\|panel.*===.*'fire-alarm'\|panel') === 'fire-alarm'" src/pages/InspectionPage.tsx && ! grep -q "position:.*fixed.*body\|body.style.position" src/pages/InspectionPage.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` passes.
    - Zoom viewer uses `usePinchZoom` (containerRef + transform + bind) and keeps the mobile 더블탭 hint text.
    - Scroll-lock uses `body.style.overflow` + touchmove pattern; grep confirms NO `body.position:fixed`.
    - Viewer pins to `var(--sat)` / `calc(54px+var(--sab))`, never bare `100vh`.
    - InspectionPage auto-opens FireAlarmModal when `?panel=fire-alarm` is present.
  </acceptance_criteria>
  <done>Mobile zoom viewer (double-tap/pinch, safe-area, body:fixed-free lock) opens from live-card; deep-link param auto-opens the panel page.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| FireAlarmModal → JWT alarm/panel API | resolve/ack/maint writes + status/events reads; 401→existing hard-logout |
| live img / zoom frame → public frame proxy | same-origin no-store image |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-03a | Tampering | resolve/create save branch | mitigate | 경보중 uses alarmApi.resolve (server confirms + clears); form data React-escaped; `occurred_at` string shape preserved (risk 12) |
| T-25-03b | Elevation of Privilege | 경보 중 maint ON | mitigate | server 409 gate + explicit confirm popup + `confirmAlarm:true` retry (§1.5); no client-side bypass |
| T-25-03c | Denial of Service | undeployed endpoints | mitigate | all queries try/catch→평상시; maint toggle optimistic; UI never crashes on 404/500 |
| T-25-SC | Tampering | dependency installs | accept | no new packages; gate N/A |
</threat_model>

<verification>
- `cd cha-bio-safety && npx tsc --noEmit` clean.
- grep: `alarmApi.resolve`, `조치완료 후 저장`, `fire-alarm-recent`, `confirmAlarm`, `usePinchZoom`, `더블탭` present; no `body.position:fixed`; emoji-zero on changed lines.
- (manual) cbc7119-preview mobile: 평상시 (live placeholder + 비화재보 form) / 경보중 (red live + panel-notice + auto-draft + "조치완료 후 저장") / 점검모드 (autonote + live/events kept + form hidden). Live-card tap → zoom viewer double-tap works. `/inspection?panel=fire-alarm` auto-opens.
</verification>

<success_criteria>
Expanded mobile 화재수신반 페이지 renders all three states from §1 API with graceful fallback; 경보중 save = resolve (chip 소멸), 평상시 = create + invalidate; maint 409 confirm; fullscreen zoom viewer (usePinchZoom, safe-area, body:fixed-free lock); deep-link auto-open. tsc clean, zero emoji.
</success_criteria>

<output>
Create `.planning/phases/25-panel-monitoring/25-03-SUMMARY.md` when done.
</output>
