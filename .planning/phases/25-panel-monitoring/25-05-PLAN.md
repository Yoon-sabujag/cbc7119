---
phase: 25-panel-monitoring
plan: 05
type: execute
wave: 3
depends_on: ['25-01', '25-03']
files_modified:
  - cha-bio-safety/src/pages/InspectionPage.tsx
autonomous: true
requirements: [PANEL-UI-06, PANEL-UI-04]
must_haves:
  truths:
    - "Selecting 화재수신반 in DesktopInspectionView renders a fire-alarm detail pane: live → (경보중)draft notice+form → 48h events → (평상시)form"
    - "Detail header 점검모드 toggle replaces the 정상라이브 pill AND 전체화면 button; NO red banner below header (alarm shown via live-red + draft-notice)"
    - "Desktop 경보중 save calls alarmApi.resolve(activeAlarmId,...) NOT create; 평상시 save calls fireAlarmApi.create"
    - "Desktop alarm takeover modal (.alarm-modal) shows in dash view; desktop zoom overlay (.zoom) opens from biglive with NO '핀치 투 줌' text"
  artifacts:
    - path: "cha-bio-safety/src/pages/InspectionPage.tsx"
      provides: "DesktopInspectionView 화재수신반 3-split detail pane + takeover modal + zoom overlay"
      contains: "alarmApi.resolve"
  key_links:
    - from: "DesktopInspectionView card onClick"
      to: "화재수신반 detail pane"
      via: "CATEGORY_GROUPS[idx].categories.includes('화재수신반')"
      pattern: "includes\\('화재수신반'\\)"
    - from: "desktop biglive click"
      to: "usePinchZoom zoom overlay"
      via: ".zoom overlay 2.2x"
      pattern: "usePinchZoom"
---

<objective>
Wire the DESKTOP 일반점검 3분할 상세 pane (Surface 6). Currently `DesktopInspectionView` has no 화재수신반 special-case — clicking the card shows an empty remediation list. Branch on `CATEGORY_GROUPS[idx].categories.includes('화재수신반')` at BOTH the card onClick and the right-pane conditional to render the fire-alarm detail pane: live → (경보중) 비화재보 초안 notice+form → 48h events → (평상시) 수기폼. Add the detail-header 점검모드 toggle (removing the 정상라이브 pill + 전체화면 button), the desktop alarm takeover modal, and the desktop zoom overlay (no "핀치 투 줌" text). Reuses the mobile form logic and 25-01 primitives.

Purpose: Give the desk (1920×1080) the same panel monitoring + auto-draft resolution as mobile, in the 3-split inspection layout.
Output: 화재수신반 detail pane + takeover modal + zoom overlay in `InspectionPage.tsx` (`DesktopInspectionView`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

**Track note:** cbc7119-design, wrangler forbidden. Endpoints undeployed → queries try/catch→평상시; maint optimistic. Validation = tsc + emoji-zero grep + token-class grep + (manual) cbc7119-preview desktop per state. NO unit tests. This plan is Wave 3 because it touches the same file as 25-03 (mobile) — build on the queries/handlers 25-03 introduced where reusable; otherwise mirror the mobile logic (resolve/create branch, maint 409 confirm) in the desktop code path.
</execution_context>

<context>
@.planning/phases/25-panel-monitoring/25-UI-SPEC.md
@.planning/phases/25-panel-monitoring/25-INTEGRATION-MAP.md
@.planning/phases/25-panel-monitoring/CONTEXT.md
@.planning/phases/25-panel-monitoring/25-HANDOFF.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Branch 화재수신반 in DesktopInspectionView + id-head 점검모드 toggle + biglive + events</name>
  <files>cha-bio-safety/src/pages/InspectionPage.tsx</files>
  <read_first>
    - cha-bio-safety/src/pages/InspectionPage.tsx — `DesktopInspectionView` (:5729-6046): props (:5730-5735), left card grid (:5805), card `isSel`/onClick `setCategoryIdx(idx); setRecordId(null)` (:5826-5831), right pane `w-1/2` (:5875), conditional `recordId && detail ? (...) : categoryIdx !== null ? (list :5968) : (empty)`, category header (:5972-5974), remediation `<table>`/rows (:5876-5967, binds remediationApi — copy structure, swap fields). `CATEGORY_GROUPS` (:68, 화재수신반 = index 14).
    - The panel/alarm queries + LivePanelImage + freshnessLabel usage from 25-03 (reuse imports/patterns).
    - 25-UI-SPEC.md Surface 6 (id-head `.id-back`/`.id-title`/`.imode-switch`; id-body order table: maint-autonote / biglive / panel-notice+form / evt-card / form; biglive `.bigscreen aspect-video rounded-t-[14px]`, live-badge, live-status; evt-card `.ethumb` 84×48) + Copywriting (desktop maint chip "점검모드 · 17:30 자동복구")
    - 25-INTEGRATION-MAP.md §3 (greenfield branch, copy table structure swap fields) + §7 risk 11 (table binds remediationApi)
  </read_first>
  <action>
    Add a `isPanel = categoryIdx !== null && CATEGORY_GROUPS[categoryIdx].categories.includes('화재수신반')` derivation. At the card onClick (:5831): keep `setCategoryIdx(idx); setRecordId(null)` — the branch happens in the right pane (no special onClick needed beyond selecting the category). In the right-pane conditional (:5875), add a FIRST branch: `if (isPanel) return <fire-alarm detail pane>` (before the `recordId && detail` / list branches).

    **Deep-link auto-open (desktop — FLAG-1 fix, REQUIRED):** add `const [sp] = useSearchParams()` (import from `react-router-dom`) + a `useEffect([sp])`: when `sp.get('panel') === 'fire-alarm'`, call `setCategoryIdx(FIRE_ALARM_IDX); setRecordId(null)` where `const FIRE_ALARM_IDX = CATEGORY_GROUPS.findIndex(g => g.categories.includes('화재수신반'))` (=14). This is REQUIRED on desktop because InspectionPage early-returns `DesktopInspectionView` (:4972) BEFORE the mobile FireAlarmModal mount (:5329) — so 25-03's mobile `?panel=fire-alarm` handler does NOT open this pane on desktop. Dashboard single-click (`/inspection?panel=fire-alarm`) and the SW equip deep-link (25-06) depend on this effect. (The `zoom=1` param is consumed in Task 3.)

    Panel detail pane (`.inspect-detail` scroll column). Add its own panel/alarm queries local to DesktopInspectionView (or reuse if hoisted): `status`, `activeAlarm`, `events`, `mode` — same try/catch→평상시 pattern as 25-03. Import `panelApi`, `alarmApi`, `type Alarm`, `LivePanelImage`, `freshnessLabel`, `usePinchZoom`, lucide `BellRing, BellOff, Maximize2, AlertTriangle, RefreshCw, Flame, Settings, ChevronLeft, Plus, X` (add to existing imports; dedupe).

    id-head `flex items-center gap-[11px] p-[11px_16px] border-b bg-surface-raised`:
    - `.id-back` `ChevronLeft` in 32px sunken box (`w-7 h-7 rounded-[7px] bg-surface-sunken`) → `setCategoryIdx(null)` (back to dash/card grid).
    - `.id-title` "화재수신반" `text-[16px] font-bold` with `BellRing` size 16.
    - spacer, then 점검모드 toggle `.imode-switch` — this REPLACES the '정상 라이브' pill AND '전체화면' button (both removed / not rendered for this pane): `inline-flex items-center gap-[9px] bg-surface-sunken border border-border-default rounded-pill p-[6px_9px_6px_14px] text-label font-bold text-text-secondary` + `BellOff` + switch `.sw` 38×22. ON (maint) = gray-active. Handler = maint toggle with 409 confirm (mirror 25-03 Task 2: try setMaint({enabled:!maintOn}); on 409 active_alarm_requires_confirm → confirm popup → retry confirmAlarm:true; invalidate ['panel-status','alarm-active']).

    id-body stack `flex flex-col gap-[15px] overflow-y-auto p-[16px_16px_22px]`:
    - ① biglive: `.bigscreen aspect-video rounded-t-[14px]` with `<LivePanelImage frameUpdatedAt={status?.frameUpdatedAt} imgClassName="w-full h-full object-cover" />`; overlay live-badge LIVE/화재 dot span; `.live-status` 정상/화재 (desktop `.live-status` = 14px → `text-body-sm`); alarm mode → `biglive-card` firepulse + border-danger-bar. Click biglive → open desktop zoom overlay (Task 3, `setZoomOpen(true)`). Note per §Rules: NO red banner below header.
    - ③ evt-card (48h): rows with `.ethumb` 84×48 snapshot thumb (`<LivePanelImage snapshotKey={ev.snapshotUrl-derived key} .../>` or `<img>` of `ev.snapshotUrl`, small) + `.badge2` fire/non/equip; visible in all modes (점검모드 조회 유지). Empty → muted "최근 48시간 자동감지 이벤트 없음".
    Leave ② (panel-notice+form) and ④ (수기폼) placement anchors for Task 2 (render order: maint-autonote → biglive → panel-notice+form(경보중) → evt-card → form(평상시)). Emoji-zero.
  </action>
  <verify>
    <automated>cd cha-bio-safety && npx tsc --noEmit && grep -q "includes('화재수신반')" src/pages/InspectionPage.tsx && grep -q "imode-switch\|id-title\|bigscreen\|biglive" src/pages/InspectionPage.tsx && grep -c "LivePanelImage" src/pages/InspectionPage.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` passes.
    - `DesktopInspectionView` right pane branches on `CATEGORY_GROUPS[categoryIdx].categories.includes('화재수신반')` before the remediation branches.
    - id-head renders `BellRing` title + a 점검모드 switch and does NOT render a '정상 라이브' pill or '전체화면' button in this pane.
    - On desktop, navigating to `/inspection?panel=fire-alarm` (dashboard single-click / SW equip deep-link) auto-selects the 화재수신반 category and shows the detail pane via a `useSearchParams` effect — no manual card click needed. (grep: `useSearchParams` + `findIndex(g => g.categories.includes('화재수신반'))` present.)
    - biglive uses `LivePanelImage` + `aspect-video`; desktop live-status uses `text-body-sm` (14px).
    - evt-card renders snapshot thumbs + badge2 variants; visible in maint mode.
  </acceptance_criteria>
  <done>Desktop 화재수신반 detail pane scaffolded: header toggle (pill/fullscreen removed), biglive, 48h events; branch wired at right pane.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Desktop form states + resolve/create save branch + panel-notice + maint-autonote</name>
  <files>cha-bio-safety/src/pages/InspectionPage.tsx</files>
  <read_first>
    - cha-bio-safety/src/pages/InspectionPage.tsx — the panel detail pane from Task 1; the mobile form field markup + save branch from 25-03 (구분/발생일시/장소/원인/조치; resolve vs create). Remediation `<table>` structure (:5876-5967) for field layout reference (swap to type/occurred_at/location/cause/action).
    - 25-UI-SPEC.md Surface 6 id-body ② (panel-notice + form-card 경보중) and ④ (form-card 수기 평상시) + Copywriting (자동화 안내문, 경보 자동감지 안내, CTA 평상시/경보중, need/입력대기 hints)
    - 25-HANDOFF.md §3 resolve box; 25-INTEGRATION-MAP.md §6 (resolve/create + invalidate)
  </read_first>
  <action>
    Render the desktop form blocks in the id-body stack order:
    - maint-autonote (top of stack, 점검모드 only): `RefreshCw` + 자동화 안내문 (verbatim) as a banner ABOVE biglive (mobile style).
    - ② panel-notice + form-card (경보중 only): notice `bg-danger-bg text-danger` + danger blink dot span + 경보 자동감지 안내 (verbatim); then auto-draft form-card `.fh.alarm` = `AlertTriangle` "자동 생성 초안 — 보완 필요" + 5 fields (비화재보 자동선택 with "자동선택" tag, 발생일시 prefilled from activeAlarm.detectedAt with "자동" tag, 발생장소/원인 `.need` border + "· 확인 필요", 조치사항 info border + "· 입력 대기"); form-bar "나중에" (secondary) + `.btn-save.alarm` red gradient "조치완료 후 저장" → `alarmApi.resolve(activeAlarm.id, { type, occurredAt: `${date} ${time}:00`, location, cause, action })` (NOT create) + invalidate `['alarm-active','fire-alarm-recent']` → 경보 chip 소멸.
    - ④ form-card (수기, 평상시 only): `.fh` `Plus` "수동 기록 추가" + 5 fields (default 비화재보); form-bar "취소" (secondary) + `.btn-save` blue gradient "점검 기록 저장" → `fireAlarmApi.create({ type, occurred_at: `${date} ${time}:00`, location, cause, action })` + invalidate `['fire-alarm-recent']`.
    Reuse the same field state/inputCls as the mobile modal. Field markup is layout-agnostic (INTEGRATION-MAP §3) — lift the mobile field group into the `w-1/2` pane; re-derive any fixed positioning (none needed inside the pane). All copy verbatim. Emoji-zero.
  </action>
  <verify>
    <automated>cd cha-bio-safety && npx tsc --noEmit && grep -q "조치완료 후 저장" src/pages/InspectionPage.tsx && grep -q "자동 생성 초안" src/pages/InspectionPage.tsx && grep -q "수동 기록 추가" src/pages/InspectionPage.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` passes.
    - 경보중 desktop save calls `alarmApi.resolve(activeAlarm.id, {...})` (chip 소멸) and NOT `fireAlarmApi.create`; 평상시 save calls `fireAlarmApi.create`.
    - panel-notice + auto-draft form render only in 경보중; 수기 form only in 평상시; maint-autonote only in 점검모드; NO red banner below header.
    - need/입력대기 field decorations + auto tags present; CTA text "조치완료 후 저장" / "점검 기록 저장".
  </acceptance_criteria>
  <done>Desktop 3-state form + resolve/create branch + notices wired; chip 소멸 via resolve; no red banner.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Desktop alarm takeover modal + desktop zoom overlay</name>
  <files>cha-bio-safety/src/pages/InspectionPage.tsx</files>
  <read_first>
    - cha-bio-safety/src/pages/InspectionPage.tsx — DesktopInspectionView render root; the dash view (card grid before a category is selected); biglive click hook (Task 1 `setZoomOpen`).
    - cha-bio-safety/src/hooks/usePinchZoom.ts (25-01)
    - 25-UI-SPEC.md Surface 6 "Desktop alarm takeover modal `.alarm-modal`" (absolute inset-0 z-[90], dash view only, :not(.acked); `.am-card` 560px blur, Flame size 44 `.am-ico` faring, `.am-kind` 24px / `.am-loc` 46px / `.am-time` mono / `.am-sub`; foot `.am-ack` "확인 (경보 인지)" + `.am-go` "화재수신반 페이지"; `.am-note`) + "Desktop zoom overlay `.zoom`" (absolute inset-0 z-[95], `.zoom-badge`, `.zoom-close` X, `.zoom-frame aspect-video` click-to-zoom 2.2×, "핀치 투 줌" text REMOVED) + Copywriting (Desktop modal note, ACK button desktop)
    - 25-INTEGRATION-MAP.md §6 zoom + §4 (equip/fire nav)
  </read_first>
  <action>
    Desktop alarm takeover modal `.alarm-modal` = `absolute inset-0 z-[90]` overlay, rendered ONLY when an active alarm exists AND the desktop is on the dash view (no category selected) AND not locally acked (`!acked`): radial red bg + `.am-card` 560px blurred card: `Flame` size 44 in `.am-ico` faring ring, `.am-kind` "화재 발생" 24px, `.am-loc` `{activeAlarm.location}` 46px, `.am-time` mono, `.am-sub`. Foot: `.am-ack` white "확인 (경보 인지)" → `alarmApi.ack(activeAlarm.id)` + set local acked; `.am-go` "화재수신반 페이지" → `setCategoryIdx(화재수신반 index)` (open the detail pane, 경보중) + ack. `.am-note` "해당시간 근무자 전원에게 발송 · 미확인 시 20초 × 3회 재발송".

    Desktop zoom overlay `.zoom` = `absolute inset-0 z-[95]` opened from biglive click (`zoomOpen`): `.zoom-badge` LIVE/화재 dot span, `.zoom-close` `X` → `setZoomOpen(false)`+reset, `.zoom-frame aspect-video` using `usePinchZoom` (containerRef + `style={{touchAction:'none', transform}}` + `{...bind}`, click-to-zoom 2.2× via doubleTapScale option) wrapping `<LivePanelImage frameUpdatedAt={status?.frameUpdatedAt} imgClassName="w-full h-full object-cover" />`. IMPORTANT: do NOT render any "핀치 투 줌" hint text (behavior retained, text removed per CONTEXT/§Surface 6). Optionally a minimal click-toggle hint or none.
    **Dashboard dblclick→zoom (FLAG-1 fix, REQUIRED for SC5):** in the same `useSearchParams` effect from Task 1 (or a sibling `useEffect([sp])`), when `sp.get('panel')==='fire-alarm' && sp.get('zoom')==='1'`, call `setZoomOpen(true)` after selecting the category — so the dashboard widget double-click (`/inspection?panel=fire-alarm&zoom=1`, wired in 25-02 T2) opens the desktop zoom overlay on arrival. Without this, the dashboard dblclick→줌 오버레이 interaction (SC5) does not fire.

    Both overlays are `absolute` within the DesktopInspectionView container (desktop, no safe-area pinning needed). Emoji-zero; copy verbatim.
  </action>
  <verify>
    <automated>cd cha-bio-safety && npx tsc --noEmit && grep -q "alarm-modal\|am-ack\|확인 (경보 인지)" src/pages/InspectionPage.tsx && grep -q "zoom-frame\|zoom-close" src/pages/InspectionPage.tsx && grep -q "usePinchZoom" src/pages/InspectionPage.tsx && ! grep -q "핀치 투 줌" src/pages/InspectionPage.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` passes.
    - Desktop takeover modal renders in dash view only, with `확인 (경보 인지)` → ack and `화재수신반 페이지` → open detail pane + ack; note copy verbatim.
    - Desktop zoom overlay opens from biglive, uses `usePinchZoom` (2.2×), and renders NO "핀치 투 줌" text (grep confirms absence).
    - Arriving at `/inspection?panel=fire-alarm&zoom=1` (dashboard widget dblclick) opens the desktop zoom overlay (`setZoomOpen(true)` from the searchParams effect) — SC5 dblclick→줌 오버레이 satisfied.
    - Emoji grep matches nothing in changed lines.
  </acceptance_criteria>
  <done>Desktop takeover modal (dash-view ACK/go) + zoom overlay (usePinchZoom, no pinch text) complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| DesktopInspectionView → JWT alarm/panel API | resolve/ack/maint writes + status/events reads; 401→existing hard-logout |
| biglive/zoom/thumbs → public frame proxy | same-origin no-store images |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-05a | Tampering | desktop resolve/create branch | mitigate | 경보중 uses alarmApi.resolve (server-authoritative clear); React-escaped fields; occurred_at string shape preserved |
| T-25-05b | Elevation of Privilege | 경보 중 maint ON | mitigate | server 409 gate + confirm popup + confirmAlarm:true retry (§1.5) |
| T-25-05c | Denial of Service | undeployed endpoints | mitigate | queries try/catch→평상시; overlays guard on null activeAlarm |
| T-25-SC | Tampering | dependency installs | accept | no new packages; gate N/A |
</threat_model>

<verification>
- `cd cha-bio-safety && npx tsc --noEmit` clean.
- grep: `includes('화재수신반')`, `imode-switch`, `조치완료 후 저장`, `alarmApi.resolve`, `alarm-modal`, `확인 (경보 인지)`, `usePinchZoom` present; `핀치 투 줌` ABSENT; emoji-zero on changed lines.
- (manual) cbc7119-preview desktop: select 화재수신반 → 3-split detail pane (live → notice+form 경보중 / 수기폼 평상시 → events); header toggle replaces pill+fullscreen; no red banner; takeover modal on dash view during alarm; biglive click → zoom overlay (no pinch text).
</verification>

<success_criteria>
Desktop 화재수신반 3-split detail pane renders all 3 states from §1 API with graceful fallback; 경보중 save = resolve (chip 소멸), 평상시 = create; header toggle replaces pill+fullscreen; no red banner; takeover modal + zoom overlay (no pinch text) work. tsc clean, zero emoji.
</success_criteria>

<output>
Create `.planning/phases/25-panel-monitoring/25-05-SUMMARY.md` when done.
</output>
