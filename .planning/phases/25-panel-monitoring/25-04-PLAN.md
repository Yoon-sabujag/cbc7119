---
phase: 25-panel-monitoring
plan: 04
type: execute
wave: 2
depends_on: ['25-01']
files_modified:
  - cha-bio-safety/src/pages/FireAlarmPage.tsx
  - cha-bio-safety/src/App.tsx
autonomous: true
requirements: [PANEL-UI-03]
must_haves:
  truths:
    - "/fire-alarm route renders a fullscreen takeover (fire=red / equip=green) with no app chrome"
    - "'확인' ACK calls alarmApi.ack(id); fire ACK then routes to 화재수신반 페이지, equip ACK closes"
    - "/fire-alarm is registered in BOTH MOBILE_NO_NAV_PATHS and DESKTOP_NO_NAV_PATHS"
  artifacts:
    - path: "cha-bio-safety/src/pages/FireAlarmPage.tsx"
      provides: "Fullscreen 경보 풀스크린 takeover + ACK"
      min_lines: 60
    - path: "cha-bio-safety/src/App.tsx"
      provides: "/fire-alarm lazy route + no-nav registration (both mobile+desktop)"
      contains: "/fire-alarm"
  key_links:
    - from: "cha-bio-safety/src/pages/FireAlarmPage.tsx"
      to: "alarmApi.ack"
      via: "확인 button POST /api/alarm/:id/ack"
      pattern: "alarmApi\\.ack"
    - from: "cha-bio-safety/src/App.tsx"
      to: "FireAlarmPage"
      via: "lazy import + Route + no-nav paths"
      pattern: "FireAlarmPage"
---

<objective>
Create the MOBILE 경보 풀스크린 (Surface 3) as a new `/fire-alarm` route — the push-tap destination. Full-bleed takeover: fire (red, `Flame`, ACK stops re-push) / equip (green, `Settings`, single-shot). Register the route in App.tsx and hide ALL chrome in both mobile and desktop no-nav lists. This plan owns ALL App.tsx edits for the phase (25-03/25-05 stay InspectionPage-only) to avoid file conflicts.

Purpose: When a push arrives, tapping it lands on a lock-screen-legible takeover where any working staffer can ACK.
Output: `FireAlarmPage.tsx` + `/fire-alarm` route + no-nav registration, consuming 25-01 `alarmApi`/`LivePanelImage`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

**Track note:** cbc7119-design, wrangler forbidden. Endpoints undeployed → alarm queries wrapped try/catch; ACK optimistic. Validation = tsc + emoji-zero grep + token-class grep + (manual) cbc7119-preview. NO unit tests.
</execution_context>

<context>
@.planning/phases/25-panel-monitoring/25-UI-SPEC.md
@.planning/phases/25-panel-monitoring/25-INTEGRATION-MAP.md
@.planning/phases/25-panel-monitoring/CONTEXT.md
@.planning/phases/25-panel-monitoring/25-HANDOFF.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: FireAlarmPage.tsx — fullscreen fire/equip takeover + ACK</name>
  <files>cha-bio-safety/src/pages/FireAlarmPage.tsx</files>
  <read_first>
    - cha-bio-safety/src/App.tsx — `Auth` wrapper (:54-57), viewport shell branch `IS_ANDROID?100svh:100dvh` (:66 area), lazy import block (:16-46), Routes (:267-297)
    - cha-bio-safety/src/utils/api.ts — `alarmApi.getActive`/`alarmApi.ack` (from 25-01)
    - cha-bio-safety/src/components/panel/LivePanelImage.tsx (from 25-01)
    - 25-UI-SPEC.md Surface 3 (`.fa-bg`/`.fa-wash`/`.fa-icon`/`.fa-kind`/`.fa-loc`/`.fa-time`/`.fa-sub`/`.fa-ack`/`.fa-note`, fire vs equip table) + Typography (`text-[40px]` fa-loc / `text-[30px]` fa-kind arbitrary oversize) + Color (danger vs safe) + Copywriting (풀스크린 kind/sub/ACK/note)
    - 25-HANDOFF.md §1.3 (ack), §1.4 (push payload url:'/fire-alarm'); 25-INTEGRATION-MAP.md §4 (route) + §6 scroll-lock/viewport
  </read_first>
  <action>
    Create `src/pages/FireAlarmPage.tsx` (default export, lucide only, emoji-zero). It is the push destination — resolve the active alarm to render: query `alarmApi.getActive()` (`['alarm-active']`, try/catch->null, retry:false). Support both `/fire-alarm` (no id → use active) and an optional id via search param. Derive `kind = alarm?.type` ('fire' | 'equip'). If no active alarm (already cleared/ACKed elsewhere), render a neutral "경보 없음 · 상황 종료" state with a close→`navigate('/dashboard')`.

    Layout: full-bleed fixed stage `fixed inset-0 bg-surface-page` pinned to safe-area (`top:var(--sat,0px)` / `bottom:var(--sab,0px)` — this route hides nav so no 54px offset needed; use full inset). Scroll-lock via `overflow:hidden` + touchmove block (SideMenu pattern), NEVER `body.position:fixed`.
    - `.fa-bg`: `<LivePanelImage snapshotKey={alarm?.snapshotUrl ? undefined : undefined} frameUpdatedAt={null} .../>` behind, dimmed via inline filter — fire `brightness(.42) saturate(1.2)` / equip `brightness(.4)`. (If snapshot available, LivePanelImage can take the snapshot; else latest frame; else placeholder.)
    - `.fa-wash`: radial overlay — fire `radial-gradient(rgba(239,68,68,.5), rgba(110,8,8,.93))` with `fawash 1.3s` blink animation (inline keyframe); equip `radial-gradient(rgba(34,197,94,.32), rgba(7,38,21,.93))` static.
    - `.fa-icon` 96px round: fire `Flame` size 46 white on red bg + `faring` pulse ring; equip `Settings` size 44 white on green bg, static.
    - `.fa-kind`: fire "화재 발생" / equip "설비 동작" — `text-[30px] font-extrabold` (arbitrary oversize for lock-screen legibility) with the lucide icon prefix (no emoji).
    - `.fa-loc`: `{alarm.location ?? '장소 확인 필요'}` `text-[40px] font-extrabold`.
    - `.fa-time`: `{alarm.detectedAt}` `font-mono tabular-nums`.
    - `.fa-sub`: fire "현장 확인 및 조치 후 화재수신반 페이지에서 초안을 보완하세요" / equip "설비 동작 감지 · 정보성 알림 (단발 · 재발송 없음)".
    - `.fa-ack` "확인": white pill, fire `text-[#b91c1c]` / equip `text-[#15803d]` — onClick -> `await alarmApi.ack(alarm.id)` (optimistic; try/catch). fire: then `navigate('/inspection?panel=fire-alarm')` (화재수신반 페이지, 경보중 state). equip: then close -> `navigate('/dashboard')` (no re-push).
    - `.fa-note`: fire `AlertTriangle` + "확인을 눌러야 추가 푸시가 멈춥니다" / equip "정보성 알림 · 확인 시 닫힘 (재발송 없음)".
    Import lucide `Flame, Settings, AlertTriangle`; `alarmApi` + `type Alarm`; `LivePanelImage`; `useNavigate`. All copy verbatim from the Copywriting Contract. Color = fixed meaning (fire danger #ef4444 / equip safe #22c55e); use tokens + arbitrary hex where a token is absent.
  </action>
  <verify>
    <automated>cd cha-bio-safety && npx tsc --noEmit && test -f src/pages/FireAlarmPage.tsx && grep -q "alarmApi.ack" src/pages/FireAlarmPage.tsx && grep -q "화재 발생\|설비 동작" src/pages/FireAlarmPage.tsx && grep -q "text-\[40px\]\|text-\[30px\]" src/pages/FireAlarmPage.tsx && ! grep -nP "[\x{1F000}-\x{1FAFF}\x{2600}-\x{27BF}]" src/pages/FireAlarmPage.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` passes; FireAlarmPage.tsx has a default export.
    - fire branch uses `Flame` + danger palette; equip branch uses `Settings` + safe palette.
    - `.fa-loc` uses `text-[40px]` and `.fa-kind` `text-[30px]` (arbitrary oversize per Typography).
    - `확인` calls `alarmApi.ack(alarm.id)`; fire routes to `/inspection?panel=fire-alarm`, equip to `/dashboard`.
    - Scroll-lock present, NO `body.position:fixed`; emoji grep matches nothing.
  </acceptance_criteria>
  <done>FireAlarmPage renders fire/equip takeover with ACK; fire ACK→화재수신반 페이지, equip ACK→close; zero emoji.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Register /fire-alarm route + no-nav (both mobile + desktop)</name>
  <files>cha-bio-safety/src/App.tsx</files>
  <read_first>
    - cha-bio-safety/src/App.tsx — lazy import block (:16-46), `MOBILE_NO_NAV_PATHS` (:72), `DESKTOP_NO_NAV_PATHS` (:75), `noNavPaths`/`showNav` (:115-116), Routes (:267-297, `/inspection` at :270, `Auth` wrapper)
    - 25-INTEGRATION-MAP.md §4 (lazy import, Route mirroring /inspection, add to BOTH no-nav lists — forgetting desktop leaves the 280px sidebar+header)
    - 25-UI-SPEC.md Surface 3 (full takeover, chrome hidden)
  </read_first>
  <action>
    Three edits to App.tsx (this plan owns App.tsx exclusively):
    1. Add lazy import beside the others (:16-46): `const FireAlarmPage = lazy(() => import('./pages/FireAlarmPage'))`.
    2. Add route inside `<Routes>` (mirror `/inspection` :270 with `Auth` wrapper): `<Route path="/fire-alarm" element={<Auth><FireAlarmPage /></Auth>} />`.
    3. Add `'/fire-alarm'` to BOTH `MOBILE_NO_NAV_PATHS` (:72) AND `DESKTOP_NO_NAV_PATHS` (:75) so all chrome (mobile nav + desktop 280px sidebar/header) is hidden on this fullscreen takeover.
    Do NOT touch InspectionPage.tsx or any other file. Leave `PAGE_TITLES`/`DESKTOP_HEADER_HIDE_PATHS` unless needed (route is chrome-hidden already). No emoji.
  </action>
  <verify>
    <automated>cd cha-bio-safety && npx tsc --noEmit && grep -q "const FireAlarmPage = lazy" src/App.tsx && grep -q 'path="/fire-alarm"' src/App.tsx && grep -c "'/fire-alarm'" src/App.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` passes.
    - `FireAlarmPage` is lazy-imported and routed at `/fire-alarm` inside an `Auth` wrapper.
    - `'/fire-alarm'` appears in BOTH no-nav arrays (grep count of the quoted literal >= 2 across the two arrays).
    - No other source file is modified (git diff limited to App.tsx + new FireAlarmPage.tsx).
  </acceptance_criteria>
  <done>/fire-alarm route live and fully chrome-hidden on mobile + desktop.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| FireAlarmPage → JWT alarm API | ack write + active read; route behind `Auth` wrapper; 401→existing hard-logout |
| push tap → /fire-alarm | destination is auth-gated; unauth user redirected to login by `Auth` |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-04a | Spoofing | ACK by any staffer | accept | Contract §1.3 allows any working staff to ACK; JWT identifies acked_by server-side |
| T-25-04b | Denial of Service | undeployed alarm endpoint | mitigate | getActive try/catch→null→"상황 종료" neutral state; ack optimistic try/catch |
| T-25-SC | Tampering | dependency installs | accept | no new packages; gate N/A |
</threat_model>

<verification>
- `cd cha-bio-safety && npx tsc --noEmit` clean.
- grep: `FireAlarmPage` lazy import + `/fire-alarm` route + literal in both no-nav arrays; `alarmApi.ack` present; emoji-zero on FireAlarmPage.tsx.
- (manual) cbc7119-preview: visit `/fire-alarm` — no nav/sidebar/header; fire variant red + Flame, equip variant green + Settings; 확인 ACK routes correctly; safe-area pinned.
</verification>

<success_criteria>
`/fire-alarm` fullscreen takeover renders fire/equip from active alarm, ACK stops/closes correctly, route chrome-hidden on both platforms, graceful when endpoint undeployed. tsc clean, zero emoji, App.tsx solely owned here.
</success_criteria>

<output>
Create `.planning/phases/25-panel-monitoring/25-04-SUMMARY.md` when done.
</output>
