---
phase: 25-panel-monitoring
plan: 02
type: execute
wave: 2
depends_on: ['25-01']
files_modified:
  - cha-bio-safety/src/pages/DashboardPage.tsx
autonomous: true
requirements: [PANEL-UI-01, PANEL-UI-05]
must_haves:
  truths:
    - "Mobile dashboard shows a pure 16:9 수신반 card between the 오늘 점검 대상 banner and 오늘 현황; tap opens 화재수신반 페이지"
    - "Mobile banner right slot shows red blink alarm chip (경보중) / gray no-blink maint chip (점검모드) / empty (평상시)"
    - "Desktop right-column top shows a shrink-0 라이브 위젯; click→일반점검, dblclick→줌 오버레이"
    - "Both mobile gridTemplateRows strings have 6 tracks with exactly one 1fr"
  artifacts:
    - path: "cha-bio-safety/src/pages/DashboardPage.tsx"
      provides: "Mobile 16:9 panel card + right chip + desktop live widget + banner chip"
      contains: "LivePanelImage"
  key_links:
    - from: "cha-bio-safety/src/pages/DashboardPage.tsx"
      to: "alarmApi.getActive / panelApi.getStatus"
      via: "react-query with 평상시 fallback"
      pattern: "getActive|getStatus"
    - from: "DashboardPage mobile card"
      to: "화재수신반 페이지"
      via: "navigate('/inspection?panel=fire-alarm')"
      pattern: "panel=fire-alarm"
---

<objective>
Render the two DashboardPage surfaces: MOBILE 16:9 수신반 card + right-aligned 경보/점검 chip (Surface 1), and DESKTOP 우측 컬럼 라이브 위젯 + banner chip (Surface 5). Both are pure-image, state-guarded, and driven by the §1 API with 평상시 graceful fallback.

Purpose: Give the team an at-a-glance live panel view + alarm/maint state on the home screen without leaving the dashboard.
Output: Mobile card+chip and desktop widget+chip wired into `DashboardPage.tsx`, consuming 25-01 `panelApi`/`alarmApi`/`LivePanelImage`/`freshnessLabel`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

**Track note:** cbc7119-design, wrangler forbidden. Endpoints undeployed → wrap all panel/alarm queries in try/catch returning 평상시 defaults (activeAlarm=null, status=null, maint.enabled=false). Validation = tsc + emoji-zero grep + token-class grep + (manual) cbc7119-preview per state. NO unit tests.
</execution_context>

<context>
@.planning/phases/25-panel-monitoring/25-UI-SPEC.md
@.planning/phases/25-panel-monitoring/25-INTEGRATION-MAP.md
@.planning/phases/25-panel-monitoring/CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Panel state query + mobile 16:9 card + right chip (Surface 1)</name>
  <files>cha-bio-safety/src/pages/DashboardPage.tsx</files>
  <read_first>
    - cha-bio-safety/src/pages/DashboardPage.tsx — lucide import line (:5), `useIsDesktop` at :159, existing `fireAlarmApi.getRecent()` query `['fire-alarm-recent']` (:118-124) + `latestAlarm` (:124), MOBILE tree: gridTemplateRows inline strings (:492-496 — non-Android `'auto auto auto 1fr auto'`, Android `'auto auto auto 1fr minmax(140px, auto)'`), 오늘 점검 대상 banner (:499-519, `.flex-1` label block :504-507, existing latestAlarm right region :508-518), 오늘 현황 start (:521)
    - 25-UI-SPEC.md Surface 1 (`.recv-pure`, `.alarm-chip`, `.maint-chip`, `.ac-dot`, `.mc-dot`) + Copywriting (대시보드 경보칩 / 점검칩)
    - 25-INTEGRATION-MAP.md §1 (insertion anchors + companion grid-track edit + risks 2,7,8) and §6 (react-query graceful)
  </read_first>
  <action>
    Add a shared panel-state query near the existing dashboard queries (:118): `usePanelState()` inline via react-query — `useQuery({ queryKey:['panel-status'], queryFn: async()=>{ try { return await panelApi.getStatus() } catch { return null } }, staleTime:30_000, refetchInterval:30_000, retry:false })` and `useQuery({ queryKey:['alarm-active'], queryFn: async()=>{ try { return await alarmApi.getActive() } catch { return null } }, refetchInterval:30_000, retry:false })`. Derive: `activeAlarm` (fire priority), `maintOn = status?.maint?.enabled ?? false`, `frameUpdatedAt = status?.frameUpdatedAt ?? null`. Import `panelApi`, `alarmApi` from `../utils/api`, `LivePanelImage` from `../components/panel/LivePanelImage`, `freshnessLabel` from `../components/panel/freshness`.

    COMPANION GRID EDIT (do first, load-bearing — risk 2): add ONE `auto` track to BOTH inline strings at :493-495 so 5→6 children: non-Android → `'auto auto auto auto 1fr auto'`; Android → `'auto auto auto auto 1fr minmax(140px, auto)'`. Keep exactly ONE `1fr`.

    INSERT 16:9 card as a NEW grid child BETWEEN the banner close (:519) and 오늘 현황 (:521): wrapper `rounded-md overflow-hidden bg-black aspect-video cursor-pointer` (chrome-0). Add `min-h-[...]`/`minHeight` guard for Android intrinsic-height (risk 7 — set a sensible min such as `min-h-[180px]`). Inside render `<LivePanelImage frameUpdatedAt={frameUpdatedAt} className="w-full h-full" imgClassName="w-full h-full object-cover" />` (omit `snapshotKey` — always the latest frame; the alarm fire-frame comes from the live feed itself, no per-type key needed). onClick → `navigate('/inspection?panel=fire-alarm')`. No LIVE badge, no status line, no extra chrome (Surface 1 = pure image).

    RIGHT CHIP: add a `shrink-0` child inside the banner `flex items-center gap-2.5`, AFTER the `.flex-1` label block (:507), mirroring the existing latestAlarm block position. State-guarded:
    - 평상시 (no activeAlarm, not maintOn): render nothing.
    - 경보중 (activeAlarm): red `.alarm-chip` = `inline-flex items-center gap-2 rounded-pill px-3 py-[7px] text-caption font-extrabold leading-none text-white` with inline `style` background gradient `linear-gradient(...#dc2626, #ef4444)` + `chipblink` box-shadow animation (define keyframes inline via style or a token class if present; if absent use an arbitrary animate). Leading DOT SPAN `.ac-dot` = 7px white circle with `blink 1s steps(1)` (span, not icon). Label: `Flame` size 12 + `화재 경보 · {activeAlarm.location ?? '장소 확인'}`. onClick → `navigate('/inspection?panel=fire-alarm')`.
    - 점검모드 (maintOn, no activeAlarm): gray `.maint-chip` = `bg-surface-sunken border border-border-strong text-text-secondary text-caption font-extrabold leading-none rounded-pill px-3 py-[7px]`, NO blink. Leading gray dot span `.mc-dot` (no animation) + `BellOff` size 12 + `점검 모드 · 알림 중지`. onClick → `navigate('/inspection?panel=fire-alarm')`.
    Import lucide `Flame`, `BellOff` into the DashboardPage import line (:5). Emoji-zero. Use `leading-none` on chip text (small container rule). Colors: danger tokens for alarm, gray tokens for maint — never borrow status colors on non-threshold cards (§6.2).
  </action>
  <verify>
    <automated>cd cha-bio-safety && npx tsc --noEmit && grep -c "'auto auto auto auto 1fr" src/pages/DashboardPage.tsx && grep -q "panel=fire-alarm" src/pages/DashboardPage.tsx && grep -q "LivePanelImage" src/pages/DashboardPage.tsx && ! grep -nP "[\x{1F000}-\x{1FAFF}\x{2600}-\x{27BF}]" src/pages/DashboardPage.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` passes.
    - Both mobile gridTemplateRows strings now read `auto auto auto auto 1fr ...` (grep count == 2) and each contains exactly one `1fr` (verify: `grep -o "1fr" per string` == 1).
    - A card with `aspect-video` is inserted between the 오늘 점검 대상 banner and 오늘 현황.
    - Alarm chip uses danger/`text-white` + dot span (not a lucide dot); maint chip uses gray tokens + no blink.
    - Card + both chips navigate to `/inspection?panel=fire-alarm`.
    - Emoji grep matches nothing in DashboardPage.tsx.
  </acceptance_criteria>
  <done>Mobile 16:9 card + state-guarded right chip render across 평상/경보/점검; grid tracks balanced; navigates to panel page.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Desktop 라이브 위젯 + desktop banner chip (Surface 5)</name>
  <files>cha-bio-safety/src/pages/DashboardPage.tsx</files>
  <read_first>
    - cha-bio-safety/src/pages/DashboardPage.tsx — desktop tree: banner (:249-272, `.flex-1` :254-257, latestAlarm block :258-271), right column wrapper `w-[340px] shrink-0 flex flex-col gap-4` (:376), 미니 캘린더 child (:378, shrink-0), 오늘 일정 (:433, flex-1 min-h-0). Panel state query from Task 1 (`activeAlarm`, `maintOn`, `frameUpdatedAt`, `freshnessLabel`).
    - 25-UI-SPEC.md Surface 5 (`.live-pure`, `.live-badge` `.lb-live`/`.lb-fire`, `.live-hint`, `.live-cap` `.lc-ok`/`.lc-fire`, firepulse on `[data-state=alarm]`) + Copywriting (LIVE badge, live-status/live-cap, 대시보드 점검칩 desktop = "점검모드 · 17:30 자동복구")
    - 25-INTEGRATION-MAP.md §2 (insert AFTER :376 BEFORE :378, MUST be shrink-0) + §6 (240ms click/dblclick disambiguation)
  </read_first>
  <action>
    INSERT the live widget as a NEW child of the right column, immediately AFTER `:376` and BEFORE 미니 캘린더 (:378). Wrapper `.card` = `bg-surface-raised border border-border-default rounded-lg overflow-hidden shrink-0` (MUST be `shrink-0` — else it steals height from flex-1 오늘 일정, risk). Header row inside: `BellRing` size 16 + `sec-cap` "화재수신반 라이브" + right muted "방재실 캡처" (`text-caption text-text-tertiary`).
    Below header, `.live-pure` = `relative rounded-lg overflow-hidden bg-black aspect-video cursor-pointer border border-border-default` containing `<LivePanelImage frameUpdatedAt={frameUpdatedAt} imgClassName="w-full h-full object-cover" />`.
    - Overlay `.live-badge` top-left: 평상시 green dot span + "LIVE" (`.lb-live`); 경보중 red dot span + "화재" (`.lb-fire`). Dot = animated span, not icon.
    - Overlay `.live-hint` bottom-right: `Maximize2` size 13 + "더블클릭 확대" (`bg-black/50 rounded-sm px-[7px] py-0.5 text-[10px] pointer-events-none`).
    - 경보중: add `border-danger-bar` + firepulse (inline style keyframe or arbitrary animate) + red box-shadow on `.live-pure`.
    Below the image, `.live-cap` = `flex items-center gap-[7px] p-[9px_13px] text-label text-text-secondary font-semibold` (desktop cap = 13px/600 → `text-label`): 평상시 `.lc-ok` green dot + `text-safe font-bold` "정상" · 이상 없음 · `{freshnessLabel(frameUpdatedAt).label}`; 경보중 `.lc-fire` red "화재 발생" · `{activeAlarm.location}` · `{activeAlarm.detectedAt}`.
    Interaction (240ms disambiguation): single click → `navigate('/inspection?panel=fire-alarm')`; double click → open desktop zoom overlay. The desktop zoom overlay component itself is built in 25-05 (DesktopInspectionView); here just wire click→navigate and leave dblclick to call a local `setZoomOpen(true)` ONLY if a lightweight dashboard zoom is in scope — otherwise dblclick also navigates to the inspection panel (which owns the overlay). To keep file ownership clean, dblclick here navigates to `/inspection?panel=fire-alarm&zoom=1` and 25-05 reads `zoom=1` to auto-open its overlay. Implement the 240ms timer to distinguish single vs double.

    DESKTOP BANNER CHIP: add a `shrink-0` child inside the desktop banner `flex items-center gap-5`, AFTER `.flex-1` (:257), mirroring mobile: 경보중 red alarm chip / 점검모드 gray chip ("점검모드 · 17:30 자동복구") / 평상시 empty. Reuse the same chip markup approach as Task 1 (danger gradient + dot span / gray + BellOff). Import `BellRing`, `Maximize2` into the lucide import line. Emoji-zero.
  </action>
  <verify>
    <automated>cd cha-bio-safety && npx tsc --noEmit && grep -q "live-pure\|화재수신반 라이브\|방재실 캡처" src/pages/DashboardPage.tsx && grep -q "zoom=1\|setZoomOpen\|더블클릭 확대" src/pages/DashboardPage.tsx && ! grep -nP "[\x{1F000}-\x{1FAFF}\x{2600}-\x{27BF}]" src/pages/DashboardPage.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` passes.
    - The live widget wrapper carries `shrink-0` and sits before 미니 캘린더 in the right column.
    - LIVE/화재 badge is a dot span (grep no lucide dot icon for the badge); caption uses `text-label` (13px) not `text-body-sm`.
    - Single click navigates to `/inspection?panel=fire-alarm`; double click routes to the zoom (via `zoom=1` param or local overlay) with a 240ms disambiguation timer.
    - Desktop banner shows alarm/maint chip with correct desktop maint copy "점검모드 · 17:30 자동복구".
    - Emoji grep matches nothing.
  </acceptance_criteria>
  <done>Desktop live widget (shrink-0) + banner chip render per state; click→inspection, dblclick→zoom route; caption freshness/alarm correct.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| DashboardPage → JWT panel/alarm API | Read-only status/active polling; 401→existing hard-logout |
| DashboardPage → public frame img | Same-origin no-store image |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-02a | Denial of Service | undeployed status/active endpoints | mitigate | queries wrapped try/catch → 평상시 (null) fallback, `retry:false`; dashboard MOCK fallback (:84-88) tolerated |
| T-25-02b | Information Disclosure | live img in shared home screen | accept | same-origin panel capture, no PII, no-store |
| T-25-SC | Tampering | dependency installs | accept | no new packages (lucide/react-query present); gate N/A |
</threat_model>

<verification>
- `cd cha-bio-safety && npx tsc --noEmit` clean.
- Grid track grep == 2 balanced strings; exactly one 1fr each.
- Emoji-zero on DashboardPage.tsx; lucide icons only.
- (manual) cbc7119-preview mobile: card renders placeholder (204) + no chip 평상시; simulate activeAlarm→red blink chip; simulate maint→gray chip. Desktop: widget shrink-0 not stealing 오늘 일정 height; caption freshness updates.
</verification>

<success_criteria>
Mobile 16:9 card + state-guarded chip and desktop live widget + banner chip render across 평상/경보/점검 from §1 API with graceful fallback; grid tracks balanced; navigation to panel page + zoom route wired; tsc clean; zero emoji.
</success_criteria>

<output>
Create `.planning/phases/25-panel-monitoring/25-02-SUMMARY.md` when done.
</output>
