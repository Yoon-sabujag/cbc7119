---
phase: 25-panel-monitoring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cha-bio-safety/src/utils/api.ts
  - cha-bio-safety/src/components/panel/freshness.ts
  - cha-bio-safety/src/components/panel/LivePanelImage.tsx
  - cha-bio-safety/src/hooks/usePinchZoom.ts
autonomous: true
requirements: [PANEL-UI-07]
must_haves:
  truths:
    - "src/utils/api.ts exports panelApi and alarmApi with the §1 contract methods"
    - "alarmApi.resolve(id, body) posts to /api/alarm/:id/resolve (NOT /api/fire-alarm)"
    - "LivePanelImage renders a cache-busted live img and degrades to a gray 16:9 placeholder on 204/error without crashing"
    - "usePinchZoom exposes double-tap + pinch 1x<->2.5x without modifying FloorPlanPage.tsx"
  artifacts:
    - path: "cha-bio-safety/src/utils/api.ts"
      provides: "panelApi + alarmApi + PanelStatus/Alarm/AlarmSummary/MaintState interfaces"
      contains: "export const alarmApi"
    - path: "cha-bio-safety/src/components/panel/LivePanelImage.tsx"
      provides: "Shared live-frame image with cache-bust + 204 placeholder"
      min_lines: 30
    - path: "cha-bio-safety/src/components/panel/freshness.ts"
      provides: "freshnessLabel + watchdogLabel helpers"
    - path: "cha-bio-safety/src/hooks/usePinchZoom.ts"
      provides: "Reusable pinch/double-tap zoom hook"
      min_lines: 40
  key_links:
    - from: "cha-bio-safety/src/components/panel/LivePanelImage.tsx"
      to: "panelApi.latestFrameUrl"
      via: "img src build with ?t= cache-bust"
      pattern: "latestFrameUrl|/api/public/panel/latest.jpg"
    - from: "cha-bio-safety/src/utils/api.ts"
      to: "/api/alarm/:id/resolve"
      via: "alarmApi.resolve POST"
      pattern: "alarm/\\$\\{id\\}/resolve|/resolve"
---

<objective>
Foundation layer for Phase 25 (화재수신반 원격감시·경보 UI). Build the non-visual primitives every other plan consumes: the API namespaces (`panelApi`/`alarmApi`) with graceful-degradation contract, the shared live-frame image component (cache-bust + 204 placeholder + freshness label), and a reusable `usePinchZoom` hook modeled on FloorPlanPage. No visual surface renders here — this plan unblocks Waves 2 and 3.

Purpose: Centralize the §1 API contract and shared media primitives so DashboardPage / InspectionPage / FireAlarmPage never re-implement fetch, cache-bust, placeholder, or pinch logic.
Output: `panelApi`, `alarmApi`, TS interfaces, `LivePanelImage`, `freshness.ts`, `usePinchZoom`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

**Track note (cbc7119-design):** wrangler forbidden. `main` push → cbc7119-preview auto-deploy only. Backend `/api/panel/*` + `/api/alarm/*` endpoints are NOT deployed in this repo (separate cbc7119-data track) → every consumer of these APIs MUST degrade gracefully (204→placeholder, fetch error→평상시 fallback, optimistic toggle). Validation for this phase = `npx tsc --noEmit` clean + emoji-zero grep + token-class grep, NOT unit tests.
</execution_context>

<context>
@.planning/phases/25-panel-monitoring/25-UI-SPEC.md
@.planning/phases/25-panel-monitoring/25-INTEGRATION-MAP.md
@.planning/phases/25-panel-monitoring/CONTEXT.md
@.planning/phases/25-panel-monitoring/25-HANDOFF.md
</context>

<artifacts_this_phase_produces>
Reference list for the whole phase (this plan creates the first four; later plans consume them):

| Symbol / file | Created by | Kind |
|---------------|-----------|------|
| `panelApi` (getStatus, getMaint, setMaint, latestFrameUrl, snapshotUrl) | 25-01 | api.ts export |
| `alarmApi` (getActive, getEvents, ack, resolve) | 25-01 | api.ts export |
| `PanelStatus`, `Alarm`, `AlarmSummary`, `MaintState`, `AlarmEvent` interfaces | 25-01 | api.ts types |
| `LivePanelImage` | 25-01 | `src/components/panel/LivePanelImage.tsx` |
| `freshnessLabel`, `watchdogLabel` | 25-01 | `src/components/panel/freshness.ts` |
| `usePinchZoom` | 25-01 | `src/hooks/usePinchZoom.ts` |
| Mobile 대시보드 16:9 card + 경보/점검 chip | 25-02 | DashboardPage.tsx (mobile tree) |
| Desktop 대시보드 라이브 위젯 + banner chip | 25-02 | DashboardPage.tsx (desktop tree) |
| FireAlarmModal expanded (live card + 48h events + maint toggle + form + resolve/create branch) | 25-03 | InspectionPage.tsx |
| Mobile fullscreen zoom viewer (`.fsv`) | 25-03 | InspectionPage.tsx |
| `/inspection?panel=fire-alarm` auto-open handling | 25-03 | InspectionPage.tsx |
| `FireAlarmPage` + `/fire-alarm` route + no-nav registration | 25-04 | FireAlarmPage.tsx + App.tsx |
| DesktopInspectionView 화재수신반 3-split detail pane | 25-05 | InspectionPage.tsx |
| Desktop alarm takeover modal (`.alarm-modal`) + desktop zoom overlay (`.zoom`) | 25-05 | InspectionPage.tsx |
| SW push deep-link (data.url forward + notificationclick navigate) | 25-06 | sw.ts |
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: panelApi + alarmApi + interfaces in api.ts</name>
  <files>cha-bio-safety/src/utils/api.ts</files>
  <read_first>
    - cha-bio-safety/src/utils/api.ts — `req<T>` helper (:31-47, JWT auto-inject + `{success,data,error}` unwrap + 401 hard-logout), `api` verb helper (:70-76), `fireAlarmApi` (:158), `remediationApi` URLSearchParams pattern (:99-113), append point AFTER `workListApi` (:708, file ends :722)
    - 25-HANDOFF.md §1.1 (panel status/frame), §1.3 (alarm active/events/ack + maint), §1.6 (data shapes: panel_alarms / Alarm JSON camelCase / AlarmSummary / MaintState), §3 resolve box (POST /api/alarm/:id/resolve contract)
    - 25-INTEGRATION-MAP.md §6 (api.ts pattern, `alarmApi.resolve` semantics, live image URL)
  </read_first>
  <action>
    Append two `export const` namespaces AFTER `workListApi` (api.ts :708), matching the existing plain-object-delegating-to `api` verb helper pattern (NO factory). Colocate interfaces immediately above each namespace.

    Interfaces (camelCase per HANDOFF §1.6):
    - `AlarmSummary`: id string; type 'fire'|'equip'; detectedAt string; location string|null.
    - `MaintState`: enabled boolean; source 'auto'|'manual'; reason?: string; autoOffAt?: string|null; turnedOnAt?: string|null; turnedOnBy?: string|null.
    - `Alarm`: id string; type 'fire'|'equip'; status 'active'|'acked'|'cleared'|'suppressed'; detectedAt string; source?: 'visual'|'audio'|null; confidence?: number|null; snapshotUrl?: string|null; ackedBy?: string|null; ackedAt?: string|null; clearedReason?: string|null; draftRecordId?: string|null; location?: string|null; cause?: string|null; action?: string|null.
    - `AlarmEvent` = alias of `Alarm` (events list rows).
    - `PanelStatus`: frameUpdatedAt string|null; agentOnline boolean; lastHeartbeatAt string|null; activeAlarm AlarmSummary|null; maint MaintState.

    `panelApi`:
    - `getStatus(): Promise<PanelStatus>` → `api.get('/panel/status')`.
    - `getMaint(): Promise<MaintState>` → `api.get('/panel/maint')`.
    - `setMaint(body: { enabled: boolean; reason?: string; confirmAlarm?: boolean }): Promise<MaintState>` → `api.put('/panel/maint', body)`.
    - `latestFrameUrl(updatedAt?: string | number | null): string` → returns `` `/api/public/panel/latest.jpg?t=${encodeURIComponent(String(updatedAt ?? 0))}` ``. No fetch — this is an `<img src>` builder (public, no-store, no Authorization header).
    - `snapshotUrl(key: string): string` → `` `/api/public/panel/${key}.jpg` ``.

    `alarmApi`:
    - `getActive(): Promise<Alarm | null>` → `api.get('/alarm/active')`.
    - `getEvents(hours = 48): Promise<AlarmEvent[]>` → build query via `new URLSearchParams({ hours: String(hours) })`, `api.get('/alarm/events?'+qs)`.
    - `ack(id: string): Promise<Alarm>` → `api.post(`/alarm/${id}/ack`, {})`.
    - `resolve(id: string, body: { type: 'fire'|'non_fire'; occurredAt: string; location: string; cause: string; action: string }): Promise<Alarm>` → `api.post(`/alarm/${id}/resolve`, body)`. This is the 경보중 자동초안 '조치완료 후 저장' endpoint (in-place UPDATE + record 확정 + panel_alarm cleared_reason='record_saved' → 대시보드 칩 소멸). It is DISTINCT from `fireAlarmApi.create` (평상시 신규). Add a one-line comment noting this distinction per D-scope (INTEGRATION-MAP §6).

    Do NOT swallow errors inside the api methods — keep the normal throwing behavior. Consumers (25-02/03/04/05) handle graceful fallback via react-query try/catch returning 평상시 defaults. Match the surrounding code's URL style: verb-helper paths are relative to `/api` base (e.g. `api.get('/panel/status')` hits `/api/panel/status`).
  </action>
  <verify>
    <automated>cd cha-bio-safety && npx tsc --noEmit && grep -q "export const panelApi" src/utils/api.ts && grep -q "export const alarmApi" src/utils/api.ts && grep -Eq "alarm/\\\$\\{id\\}/resolve" src/utils/api.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` passes from cha-bio-safety.
    - `grep -c "export const panelApi" src/utils/api.ts` == 1 and same for `alarmApi`.
    - `alarmApi.resolve` builds the URL `/alarm/${id}/resolve` and NOT `/fire-alarm`.
    - `panelApi.latestFrameUrl` returns a string containing `/api/public/panel/latest.jpg?t=` and performs no fetch.
    - Interfaces `PanelStatus`, `Alarm`, `AlarmSummary`, `MaintState` are exported.
  </acceptance_criteria>
  <done>api.ts compiles with panelApi + alarmApi + interfaces; resolve/create split encoded; live-frame URL builder present.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: freshness helpers + LivePanelImage component</name>
  <files>cha-bio-safety/src/components/panel/freshness.ts, cha-bio-safety/src/components/panel/LivePanelImage.tsx</files>
  <read_first>
    - cha-bio-safety/src/utils/api.ts — the `panelApi` added in Task 1 (latestFrameUrl / snapshotUrl)
    - 25-UI-SPEC.md — "Freshness label" + "Empty — live (204)" copywriting rows; live-status tokens (Surface 2 live-card, Surface 5 live-cap)
    - 25-INTEGRATION-MAP.md §6 (live image = `<img src="/api/public/panel/latest.jpg?t=<updatedAt>">`, 204→placeholder, do NOT remount per refetch — cache-bust via frameUpdatedAt not Date.now()) and §7 risk 9
    - 25-HANDOFF.md §1.1 (frameUpdatedAt vs now → 방금/N초 전/지연; agentOnline = now-lastHeartbeatAt<180s)
  </read_first>
  <action>
    Create `src/components/panel/freshness.ts`:
    - `parseKst(s: string | null): number | null` — parse KST wall-clock `'YYYY-MM-DD HH:MM:SS'` to epoch ms (treat as local wall time; use `new Date(s.replace(' ','T'))` and guard NaN → null).
    - `freshnessLabel(frameUpdatedAt: string | null, nowMs = Date.now()): { label: string; tone: 'ok' | 'stale' }` — diff seconds = (now - parsed)/1000; null→`{label:'지연', tone:'stale'}`; diff < 10 → `{label:'방금', tone:'ok'}`; diff < 60 → `{label: `${Math.round(diff)}초 전`, tone:'ok'}`; else `{label:'지연', tone:'stale'}`.
    - `watchdogLabel(lastSeenAt: string | null, nowMs = Date.now()): string | null` — returns `` `수신반 모니터링 중단 · 마지막 신호 ${N}분 전` `` when `now - lastSeenAt > 180000` (N = round minutes), else null.

    Create `src/components/panel/LivePanelImage.tsx` (lucide only, emoji-zero):
    - Props: `{ frameUpdatedAt?: string | number | null; snapshotKey?: string | null; alt?: string; className?: string; imgClassName?: string; onClick?: () => void }`.
    - Compute `src` = `snapshotKey ? panelApi.snapshotUrl(snapshotKey) : panelApi.latestFrameUrl(frameUpdatedAt ?? 0)`. Because the `?t=` value derives from `frameUpdatedAt` (not `Date.now()`), the URL is stable across refetches that carry the same frame → no remount/flicker (risk 9).
    - Local `useState(errored=false)`; reset errored via `useEffect` keyed on `src`.
    - Render a `relative w-full aspect-video bg-black overflow-hidden` wrapper (className merges in). When not errored render `<img>` `w-full h-full object-cover` with `onError={() => setErrored(true)}` and `loading="lazy"`. When errored (covers HTTP 204 → browser fires no load but we treat broken/empty as placeholder) render a gray placeholder: `absolute inset-0 flex items-center justify-center bg-surface-sunken text-text-tertiary text-caption` with copy "수신반 화면 로딩 · 미연결" (no emoji).
    - Do NOT render LIVE badge / status text here — consumers overlay those (Surface-specific). This component is chrome-0 image + placeholder only.
  </action>
  <verify>
    <automated>cd cha-bio-safety && npx tsc --noEmit && test -f src/components/panel/LivePanelImage.tsx && test -f src/components/panel/freshness.ts && ! grep -nP "[\x{1F000}-\x{1FAFF}\x{2600}-\x{27BF}\x{2190}-\x{21FF}\x{2B00}-\x{2BFF}]" src/components/panel/LivePanelImage.tsx src/components/panel/freshness.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` passes.
    - `freshnessLabel(null)` returns tone 'stale' label '지연'; a timestamp <10s old returns '방금'.
    - LivePanelImage builds its src from `panelApi` (grep `latestFrameUrl|snapshotUrl` present) and never calls `Date.now()` inside render for the cache-bust key.
    - Emoji-zero: the emoji grep matches nothing in both new files.
    - Placeholder branch exists (grep `aspect-video` + a `bg-surface-sunken` placeholder).
  </acceptance_criteria>
  <done>freshness helpers + LivePanelImage compile, render placeholder on error, cache-bust keyed to frameUpdatedAt, zero emoji.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: usePinchZoom hook (modeled on FloorPlanPage, no FloorPlanPage edit)</name>
  <files>cha-bio-safety/src/hooks/usePinchZoom.ts</files>
  <read_first>
    - cha-bio-safety/src/pages/FloorPlanPage.tsx — zoom state/refs (:425-436), `clampTranslate` (:544), touch handlers (`onTouchStart` :554, move/end), wheel (:640-657), double-tap 300ms 1x<->2.5x toggle (:665-686), container `touchAction:'none'` + `translate3d(...)scale()` (:1013). READ ONLY — do not modify this file.
    - 25-UI-SPEC.md Surface 4 (mobile zoom 1x<->2.5x 300ms) + Surface 6 desktop zoom overlay (2.2x)
    - 25-INTEGRATION-MAP.md §6 zoom/pinch block
  </read_first>
  <action>
    Create `src/hooks/usePinchZoom.ts` exporting `usePinchZoom(options?: { maxScale?: number; doubleTapScale?: number })`. Port the FloorPlanPage pattern into a self-contained hook WITHOUT touching FloorPlanPage (the deployed floorplan zoom must stay untouched — risk of regression). The hook owns:
    - state `scale` (number, start 1) + refs `scaleRef`, `txRef`, `tyRef` mirroring FloorPlanPage :425-429.
    - `clampTranslate(tx,ty,s)` bounding function (port :544-552, using the container ref's clientWidth/Height).
    - pinch: two-touch distance/midpoint → scale between 1 and `maxScale` (default 2.5); single-touch pan when scale>1.
    - double-tap: 300ms window → toggle between 1x and `doubleTapScale` (default 2.5) centered on tap point (port :665-686).
    - wheel zoom (desktop) port :640-657.
    - `reset()` → scale 1, tx/ty 0.
    Return `{ containerRef, scale, transform }` where `transform` = `` `translate3d(${txRef.current}px, ${tyRef.current}px, 0) scale(${scale})` `` and a `bind` object `{ onTouchStart, onTouchMove, onTouchEnd, onWheel, onDoubleClick }`. Consumers set `ref={containerRef}`, `style={{ touchAction:'none', transform }}` on the zoom stage and spread `{...bind}`. Use `useCallback` for handlers; add non-passive touchmove note in a comment (React synthetic onTouchMove is passive-by-default in some browsers → consumers that need preventDefault should also register a native non-passive listener; document this but keep the hook framework-idiomatic).
    Keep it dependency-free (React only). No emoji.
  </action>
  <verify>
    <automated>cd cha-bio-safety && npx tsc --noEmit && grep -q "export function usePinchZoom\|export const usePinchZoom" src/hooks/usePinchZoom.ts && grep -q "clampTranslate\|maxScale" src/hooks/usePinchZoom.ts && git diff --quiet -- src/pages/FloorPlanPage.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` passes.
    - `src/hooks/usePinchZoom.ts` exports `usePinchZoom` returning `{ containerRef, scale, transform, bind }` (or documented equivalent).
    - `git diff --quiet -- src/pages/FloorPlanPage.tsx` — FloorPlanPage.tsx is UNCHANGED (pattern copied, not extracted-in-place).
    - Double-tap toggle 1x<->2.5x logic present (grep `doubleTapScale` or `2.5`).
  </acceptance_criteria>
  <done>usePinchZoom compiles, FloorPlanPage untouched, exposes bind+transform for the new zoom surfaces.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client → JWT API (`/api/panel/*`, `/api/alarm/*`) | Reads/writes cross here; JWT auto-injected by `req<T>`; 401 → existing hard-logout (unchanged) |
| client → public Worker proxy (`/api/public/panel/*.jpg`) | Same-origin image, no third party, no secrets, no-store |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-01 | Information Disclosure | live frame `<img>` | accept | Same-origin Worker proxy, no PII in a panel screen capture; no Authorization header needed (public) |
| T-25-02 | Denial of Service | undeployed endpoints 404/500 | mitigate | Consumers wrap queries in try/catch → 평상시 fallback; api methods never crash the tree |
| T-25-SC | Tampering | npm/pip/cargo installs | accept | NO new dependencies added this phase (lucide-react + react-query already present); no install task → package legitimacy gate N/A |
</threat_model>

<verification>
- `cd cha-bio-safety && npx tsc --noEmit` clean (whole project builds).
- All four artifact files exist; FloorPlanPage.tsx unchanged.
- Emoji-zero on new files.
- (manual, later) cbc7119-preview: no visual surface in this plan — verified indirectly by Wave-2 consumers rendering placeholders when endpoints 204.
</verification>

<success_criteria>
panelApi/alarmApi/interfaces exported with resolve≠create split; LivePanelImage degrades to placeholder on 204/error; freshness helpers return 방금/N초 전/지연; usePinchZoom reusable without FloorPlanPage regression. tsc clean.
</success_criteria>

<output>
Create `.planning/phases/25-panel-monitoring/25-01-SUMMARY.md` when done.
</output>
