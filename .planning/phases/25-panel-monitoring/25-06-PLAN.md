---
phase: 25-panel-monitoring
plan: 06
type: execute
wave: 3
depends_on: ['25-03', '25-04']
files_modified:
  - cha-bio-safety/src/sw.ts
autonomous: true
requirements: [PANEL-UI-08]
must_haves:
  truths:
    - "push handler forwards payload url (+ alarmType) into notification.data"
    - "notificationclick reads data.url and navigates (client.navigate/openWindow) instead of always opening /"
    - "fallback: fire → /fire-alarm, equip → /inspection?panel=fire-alarm"
  artifacts:
    - path: "cha-bio-safety/src/sw.ts"
      provides: "Push deep-link routing to the panel surfaces"
      contains: "data.url"
  key_links:
    - from: "cha-bio-safety/src/sw.ts notificationclick"
      to: "/fire-alarm | /inspection?panel=fire-alarm"
      via: "client.navigate(url) / openWindow(url)"
      pattern: "navigate\\(|openWindow\\("
---

<objective>
Wire the push deep-link (Surface non-visual dependency, HANDOFF §2.0b). Today `src/sw.ts` drops the payload `url` and always opens `/`. Update the push handler to forward `url` (+ `alarmType`) into `notification.data`, and update `notificationclick` to read `data.url` and actually navigate the existing/new window — with a fallback map (fire → `/fire-alarm`, equip → `/inspection?panel=fire-alarm`). Destinations were created in 25-04 (`/fire-alarm` route) and 25-03 (`?panel=fire-alarm` auto-open).

Purpose: A push tap must land on the alarm takeover (fire) or the 화재수신반 페이지 (equip), not the app root.
Output: Updated `push` + `notificationclick` handlers in `sw.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

**Track note:** cbc7119-design, wrangler forbidden. `sw.ts` is the injectManifest service worker (workbox `precacheAndRoute(self.__WB_MANIFEST)`); editing it triggers a normal build — no config change. Validation = `npx tsc --noEmit` + grep assertions. NO unit tests. Backend already sends the §1.4 superset payload `{kind,alarmType,alarmId,location,detectedAt,url}` (forward-compatible) — this plan makes the SW honor it.
</execution_context>

<context>
@.planning/phases/25-panel-monitoring/25-UI-SPEC.md
@.planning/phases/25-panel-monitoring/25-INTEGRATION-MAP.md
@.planning/phases/25-panel-monitoring/CONTEXT.md
@.planning/phases/25-panel-monitoring/25-HANDOFF.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Forward payload url in push + navigate on notificationclick</name>
  <files>cha-bio-safety/src/sw.ts</files>
  <read_first>
    - cha-bio-safety/src/sw.ts — push handler (:34-50: destructures `{title, body, type}` from `event.data.json()`, `showNotification(..., { tag: type, data: { type } })`), notificationclick (:53-64: `matchAll` → `existing.focus()` (no navigation) else `openWindow('/')`)
    - 25-HANDOFF.md §2.0b (the required change, both handlers) + §1.4 (push payload superset `{kind,alarmType,alarmId,location,detectedAt,url:'/fire-alarm'}`)
    - 25-INTEGRATION-MAP.md §8 (exact change spec: forward `url`+`alarmType` into `notification.data`; read `data.url ?? fallback`; fallback map fire→/fire-alarm, equip→화재수신반 페이지 = /inspection?panel=fire-alarm; honor backend per-type url verbatim when present)
    - CONTEXT.md "Service Worker 딥링크" section
  </read_first>
  <action>
    Edit `src/sw.ts` push handler (:37-45): destructure the superset — `const { title, body, type, url, alarmType } = event.data.json()` (all optional; existing payloads without url still work). In `showNotification` options set `data: { type, url, alarmType }` (keep `tag: type || 'default'`, icon/badge unchanged).

    Edit `notificationclick` (:53-64): after `event.notification.close()`, compute the destination:
    - `const data = event.notification.data || {}`
    - `const fallback = (data.alarmType === 'equip' || data.type === 'equip') ? '/inspection?panel=fire-alarm' : '/fire-alarm'` (default to fire takeover; equip → 화재수신반 페이지 auto-open).
    - `const url = data.url || fallback` (honor backend per-type url verbatim when present — forward-compat).
    In the `matchAll` handler: if an existing same-origin `WindowClient` is found → `existing.navigate(url).then(c => c && c.focus())` (navigate THEN focus, not just focus); else → `self.clients.openWindow(url)`. Guard `existing.navigate` existence for TS (WindowClient.navigate typing). Keep `event.waitUntil(...)`. No behavior change for pushes lacking url beyond the sensible fallback. Emoji-zero; keep the existing Korean section-divider comment style.
  </action>
  <verify>
    <automated>cd cha-bio-safety && npx tsc --noEmit && grep -q "data: { type, url, alarmType }\|url, alarmType" src/sw.ts && grep -q "data.url\|data\\.url" src/sw.ts && grep -q "/fire-alarm" src/sw.ts && grep -q "panel=fire-alarm" src/sw.ts && grep -q "\\.navigate(" src/sw.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` passes.
    - push handler forwards `url` and `alarmType` into `notification.data`.
    - notificationclick reads `data.url` with a fallback (fire → `/fire-alarm`, equip → `/inspection?panel=fire-alarm`) and calls `existing.navigate(url)` / `openWindow(url)` — no longer hardcoded `openWindow('/')`.
    - Backend-provided `data.url` is honored verbatim when present.
    - No emoji introduced; injectManifest `precacheAndRoute` line untouched.
  </acceptance_criteria>
  <done>SW push forwards url; notificationclick deep-links to /fire-alarm (fire) or 화재수신반 페이지 (equip); backend url honored.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| push payload → SW notification.data | payload originates from our own VAPID-signed backend; url is same-origin app path |
| notificationclick → window navigation | navigates to in-app routes only; both destinations are `Auth`-gated pages |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-06a | Tampering | payload `url` open-redirect | mitigate | url is used with `client.navigate`/`openWindow` for same-origin app paths; fallback is a fixed literal; do not construct cross-origin URLs. (Optional hardening: only accept url starting with '/'.) |
| T-25-06b | Denial of Service | malformed payload | mitigate | existing try/catch around `event.data.json()` retained; missing url → fallback |
| T-25-SC | Tampering | dependency installs | accept | no new packages; gate N/A |
</threat_model>

<verification>
- `cd cha-bio-safety && npx tsc --noEmit` clean.
- grep: `url, alarmType` forwarded into data; `data.url` read; `/fire-alarm` + `panel=fire-alarm` fallbacks; `.navigate(` used; no hardcoded `openWindow('/')` remains as the sole path.
- (manual, integration) With backend deployed on staging/prod: fire push tap → `/fire-alarm`; equip push tap → 화재수신반 페이지. (Not testable on cbc7119-preview without backend — note this in SUMMARY; the SW change is destination-existence-verified by 25-03/25-04.)
</verification>

<success_criteria>
push forwards url+alarmType; notificationclick navigates to the correct panel surface via data.url with fire/equip fallback; backend url honored; no open-redirect to cross-origin. tsc clean, zero emoji.
</success_criteria>

<output>
Create `.planning/phases/25-panel-monitoring/25-06-SUMMARY.md` when done.
</output>
