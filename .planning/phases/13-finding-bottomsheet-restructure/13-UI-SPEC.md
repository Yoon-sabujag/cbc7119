---
phase: 13
slug: finding-bottomsheet-restructure
status: draft
shadcn_initialized: false
preset: none
created: 2026-04-06
---

# Phase 13 — UI Design Contract

> Visual and interaction contract for the Finding BottomSheet Restructure phase.
> Generated by gsd-ui-researcher. Verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none — manual CSS variables + Tailwind utility classes |
| Preset | not applicable |
| Component library | none (custom inline-style components) |
| Icon library | lucide-react 0.454.0 + inline SVG |
| Font | Noto Sans KR (weights 300/400/500/600/700/900), JetBrains Mono (400/600) |

Source: tailwind.config.js + src/index.css — no components.json found.

---

## Spacing Scale

Declared values (multiples of 4 only):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Drag-handle internal padding, badge padding vertical |
| sm | 8px | Between chips in a row, between photo thumbnails, gap between buttons |
| md | 16px | BottomSheet horizontal padding, form field gap default |
| lg | 24px | BottomSheet title-to-form gap, section separators |
| xl | 32px | BottomSheet drag-handle top clearance |
| 2xl | 48px | CTA button height, safe-area bottom padding base |
| 3xl | 64px | Not used in this phase |

Exceptions:
- Touch targets (chips, buttons): minimum 44px tall on mobile — zone chips use `padding: 9px 0` which resolves to ~38px; floor chips use `padding: 7px 0`. These are acceptable because the containing row flex allows wider tap zones. Do not reduce further.
- BottomSheet drag handle: 4px tall × 32px wide, centered at top with 12px top padding — matches existing AdminPage pattern.
- Photo thumbnail: 72px × 72px fixed — matches Phase 12 PhotoGrid contract.

---

## Typography

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body / input text | 13px | 400 | 1.5 |
| Label / section header | 12px | 700 | 1.2 |
| BottomSheet title | 16px | 700 | 1.2 |
| CTA button | 14px | 700 | 1.0 (single line) |

Source: Extracted from LegalFindingsPage.tsx `lblStyle`, `inputStyle`, button styles, and sheet title div.

Notes:
- No Display size needed in this phase — no page-level headings.
- Korean body text: Noto Sans KR weight 400. Labels: weight 700 (not 600) — matches existing `lblStyle.fontWeight: 700` pattern in codebase.

---

## Color

All values reference existing CSS custom properties from `src/index.css`.

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `var(--bg)` = #0d1117 | Page background, overlay backdrop base |
| Secondary (30%) | `var(--bg2)` = #161b22 / `var(--bg3)` = #1c2128 | BottomSheet panel (`--bg2`), input fields + inactive chips (`--bg3`) |
| Accent (10%) | `var(--acl)` = #3b82f6 | Active chip border + background tint, active list item text, CTA button fill |
| Destructive | `var(--danger)` = #ef4444 | Required field asterisk, photo-remove button, delete action |

Accent reserved for:
- Active zone chip: `border: 1.5px solid var(--acl)`, `background: rgba(59,130,246,.12)`, `color: var(--acl)`
- Active floor chip: same pattern, smaller padding
- Active FINDING_ITEMS list row: `background: rgba(59,130,246,.1)`, `color: var(--acl)`
- Primary CTA button fill: `background: var(--acl)`
- Do NOT apply accent to cancel button, input borders, or labels.

Overlay backdrop: `rgba(0,0,0,0.6)` — fixed over full viewport, zIndex 50. Tap outside to dismiss.

Borders:
- Inactive chip border: `1px solid var(--bd2)` = `rgba(255,255,255,0.13)`
- Input / list container border: `1px solid var(--bd2)`
- Card separator line (inside list): `1px solid var(--bd)` = `rgba(255,255,255,0.07)`

---

## Component Inventory

### FindingBottomSheet (modified from existing inline component in LegalFindingsPage.tsx)

Structure (top to bottom):
1. Drag handle — 32px × 4px pill, `var(--bd2)`, centered, 12px top padding
2. Title row — "지적사항 등록", 16px/700, `var(--t1)`, padding `12px 16px 0`
3. Form area — padding `12px 16px`, `gap: 14px` between fields
4. Button area — padding `4px 16px 32px`, `gap: 8px`

Form field order (matches decisions D-01, D-02, D-03):
1. **구역** — chip row, 4 chips (연구동/사무동/브릿지/지하), `gap: 6px`, flex-wrap
2. **층** — chip row, conditionally shown when zone selected, `gap: 4px`, no wrap (horizontal scroll if needed)
3. **위치 상세** — combo dropdown (NEW — replaces free-text input). Renders as native `<select>` with preset options per floor, plus '직접입력' option. When '직접입력' selected, show text input below. See "상세위치 프리셋" section.
4. **지적 항목** — scrollable list (max-height 123px), 19 rows + '직접입력' at top. When '직접입력' selected, show text input below (existing pattern).
5. **지적 내용** — `<textarea>` rows=3, required (red asterisk on label), resize: vertical
6. **지적 사진** — horizontal scroll row, 72×72 thumbnails + add button (existing PhotoGrid inline pattern from Phase 12)

Button area (bottom):
- Primary: "등록" — full-width, 48px height, `var(--acl)` fill, white text, 700, 14px, borderRadius 10
- Secondary: "취소" — full-width, 48px height, transparent bg, `var(--bd2)` border, `var(--t2)` text, 14px, borderRadius 10

Loading state (isSubmitting):
- Both buttons: `opacity: 0.6`, `cursor: not-allowed`
- Primary button text changes to "처리 중..."

### 상세위치 프리셋 (ZONE_FLOOR_DETAILS constant — new)

Preset list applies to ALL zones and floors unless overridden. Standard presets (Claude's Discretion — based on InspectionPage location data and building structure):

| 항목 | 설명 |
|------|------|
| 직접입력 | First option — always shown, triggers text input |
| 복도 | Common in all above-ground floors |
| 계단실 | Stairwells present in all zones |
| 화장실 | Present in most floors |
| EPS | Electrical pipe shaft |
| TPS | Telecommunications pipe shaft |
| 기계실 | Mechanical room — primarily basement |
| 전기실 | Electrical room |
| 주차장 | Basement floors B1–B5 |
| 로비 | 1F/2F entries |
| 회의실 | Above-ground office/research floors |
| 실험실 | Research building floors |
| 옥상 | Roof — 8-1F |

Implementation note: Provide a flat list (same preset for all zone/floor combinations) as the first iteration per D-03 guidance. Claude's Discretion allows a unified list rather than per-floor branching to minimize complexity and match the data model (D-06: location = string concat, no structured breakdown stored).

Select element style (matches existing `select` in LegalFindingsPage admin subheader):
```
background: var(--bg3)
border: 1px solid var(--bd2)
borderRadius: 9px
padding: 10px 12px
color: var(--t1)
fontSize: 13px
width: 100%
appearance: none
WebkitAppearance: none
```

Text input shown below select when '직접입력' active:
- Same `inputStyle` as existing form inputs
- `marginTop: 8px`
- placeholder: "직접 입력"

---

## Interaction States

| State | Visual |
|-------|--------|
| Zone chip — unselected | `bg3` fill, `bd2` border, `t2` text, 13px/400 |
| Zone chip — selected | `rgba(59,130,246,.12)` fill, `acl` border 1.5px, `acl` text, 13px/700 |
| Floor chip — unselected | `bg3` fill, `bd2` border, `t2` text, 12px/400 |
| Floor chip — selected | `rgba(59,130,246,.12)` fill, `acl` border 1.5px, `acl` text, 12px/700 |
| Floor row — hidden | Rendered as `null` when no zone selected |
| 상세위치 select — '직접입력' | Text input appears below with `marginTop: 8px` |
| FINDING_ITEMS row — unselected | `bg3` fill, `t1` text, 13px/400 |
| FINDING_ITEMS row — selected | `rgba(59,130,246,.1)` fill, `acl` text, 13px/700 |
| Submit button — loading | opacity 0.6, cursor not-allowed, text "처리 중..." |
| Submit button — idle | `acl` fill, white text, cursor pointer |
| BottomSheet — enter | `slideUp 0.28s ease-out` animation from translateY(100%) to 0 |
| Backdrop — tap | dismisses sheet, calls onClose |

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| BottomSheet title | 지적사항 등록 |
| Primary CTA | 등록 |
| Loading CTA | 처리 중... |
| Cancel button | 취소 |
| Section label — 구역 | 구역 |
| Section label — 층 | 층 |
| Section label — 위치 상세 | 위치 상세 |
| Section label — 지적 항목 | 지적 항목 |
| Section label — 지적 내용 | 지적 내용 * (asterisk in `var(--danger)`) |
| Section label — 사진 | 지적 사진 (최대 5장) |
| 상세위치 select — first option | 직접입력 |
| 상세위치 text input placeholder | 직접 입력 |
| 지적 항목 text input placeholder | 점검 항목을 직접 입력하세요 |
| 지적 내용 textarea placeholder | 지적 내용을 입력하세요 |
| Validation error — empty description | 지적 내용을 입력하세요 (toast.error) |
| Success toast | 지적사항이 등록되었습니다. |
| Error toast | 등록에 실패했습니다. 다시 시도해 주세요. |
| Empty state heading (findings list) | 지적사항 없음 |
| Empty state body (findings list) | 현장에서 지적된 항목을 등록하려면 아래 버튼을 누르세요. |
| Page CTA button | + 지적사항 등록 |

Source: Existing strings in LegalFindingsPage.tsx — preserve as-is. No copy changes in this phase.

Destructive actions: None in this phase. The existing "삭제" button on finding cards is not modified.

---

## Layout Constraints

- BottomSheet max-height: 90vh — scrollable internally
- BottomSheet animation: `slideUp 0.28s ease-out both`
- Form gap: 14px between field groups (not 16px — matches existing)
- FINDING_ITEMS list max-height: 123px, overflowY auto
- Photo row: horizontal scroll (`overflowX: auto`), no wrap
- BottomSheet border-radius: `16px 16px 0 0` (top corners only)
- BottomSheet background: `var(--bg2)` — one level above page background
- Overlay z-index: 50 — above all page content and BottomNav

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none | not applicable — shadcn not initialized |
| third-party | none | not applicable |

No third-party component blocks used. All UI is custom inline-style React components matching existing codebase patterns.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
