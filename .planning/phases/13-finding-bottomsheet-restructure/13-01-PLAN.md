---
phase: 13-finding-bottomsheet-restructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cha-bio-safety/src/pages/LegalFindingsPage.tsx
autonomous: true
requirements:
  - FIND-01
  - FIND-02

must_haves:
  truths:
    - "상세위치 필드가 native <select> 드롭다운으로 렌더링된다"
    - "드롭다운에 직접입력/복도/계단실/화장실/EPS/TPS/기계실/전기실/주차장/로비/회의실/실험실/옥상 프리셋이 표시된다"
    - "직접입력 선택 시 텍스트 입력 필드가 나타난다"
    - "location 문자열이 구역 + 층 + 상세위치로 조합되어 API에 전송된다"
    - "기존 FINDING_ITEMS 스크롤 리스트 + 직접입력 기능이 변경 없이 동작한다"
  artifacts:
    - path: "cha-bio-safety/src/pages/LegalFindingsPage.tsx"
      provides: "ZONE_FLOOR_DETAILS constant + FindingBottomSheet with combo select"
      contains: "ZONE_FLOOR_DETAILS"
  key_links:
    - from: "FindingBottomSheet select element"
      to: "mutation.mutate() location assembly"
      via: "locationDetail + customLocationDetail state"
      pattern: "locationDetail === '직접입력' \\? customLocationDetail"
---

<objective>
Add structured location detail input (3rd stage dropdown) to the Finding BottomSheet, replacing the free-text input with a native select combo that offers preset location options plus a direct-input fallback.

Purpose: Enable structured 3-stage location entry (zone -> floor -> detail) for fire safety findings, reducing typos and speeding up mobile data entry (FIND-02). FIND-01 (item selection) is already fully implemented and confirmed working.
Output: Modified LegalFindingsPage.tsx with ZONE_FLOOR_DETAILS constant and combo select UI.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-finding-bottomsheet-restructure/13-CONTEXT.md
@.planning/phases/13-finding-bottomsheet-restructure/13-RESEARCH.md
@.planning/phases/13-finding-bottomsheet-restructure/13-UI-SPEC.md

<interfaces>
<!-- From cha-bio-safety/src/pages/LegalFindingsPage.tsx (current state): -->

Existing constants at module scope (lines 41-62):
```typescript
const ZONES = [
  { key: 'research', label: '연구동' },
  { key: 'office',   label: '사무동' },
  { key: 'bridge',   label: '브릿지' },
  { key: 'basement', label: '지하' },
] as const

const ZONE_FLOORS: Record<string, string[]> = {
  research: ['8-1F','8F','7F','6F','5F','3F','2F','1F'],
  office:   ['8-1F','8F','7F','6F','5F','3F','2F','1F'],
  bridge:   ['7F','6F','5F','3F'],
  basement: ['B1','M','B2','B3','B4','B5'],
}

const FINDING_ITEMS = [
  '직접입력',
  '감지기(불꽃)','감지기(열)','감지기(연기)',
  '방화문','방화셔터','비상방송설비','비상콘센트',
  'DIV','소방펌프','소화기','소화전','스프링클러','시각경보기',
  '완강기','유도등','자동화재탐지설비',
  '전실제연댐퍼','청정소화약제',
]
```

FindingBottomSheet state (lines 72-78):
```typescript
const [zone, setZone] = useState('')
const [floor, setFloor] = useState('')
const [locationDetail, setLocationDetail] = useState('')
const [inspectionItem, setInspectionItem] = useState('')
const [customItem, setCustomItem] = useState('')
const [description, setDescription] = useState('')
```

Location concat in mutationFn (lines 82-87):
```typescript
const loc = [
  ZONES.find(z => z.key === zone)?.label,
  floor,
  locationDetail.trim(),
].filter(Boolean).join(' ')
```

Existing inputStyle (lines 121-136):
```typescript
const inputStyle: React.CSSProperties = {
  background: 'var(--bg3)',
  borderRadius: 9,
  padding: '10px 12px',
  border: '1px solid var(--bd2)',
  width: '100%',
  color: 'var(--t1)',
  fontSize: 13,
  boxSizing: 'border-box',
  outline: 'none',
  fontFamily: 'inherit',
  lineHeight: 1.5,
  minWidth: 0,
  WebkitAppearance: 'none',
  appearance: 'none',
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ZONE_FLOOR_DETAILS constant and replace free-text input with combo select</name>
  <files>cha-bio-safety/src/pages/LegalFindingsPage.tsx</files>
  <read_first>
    - cha-bio-safety/src/pages/LegalFindingsPage.tsx (full file — current implementation)
    - .planning/phases/13-finding-bottomsheet-restructure/13-UI-SPEC.md (visual contract)
    - .planning/phases/13-finding-bottomsheet-restructure/13-RESEARCH.md (pitfalls and patterns)
  </read_first>
  <action>
1. Add `ZONE_FLOOR_DETAILS` constant at module scope, directly after the `FINDING_ITEMS` array (around line 62). The constant is a flat `as const` string array per D-02/D-03 and UI-SPEC:
```typescript
const ZONE_FLOOR_DETAILS = [
  '직접입력',
  '복도',
  '계단실',
  '화장실',
  'EPS',
  'TPS',
  '기계실',
  '전기실',
  '주차장',
  '로비',
  '회의실',
  '실험실',
  '옥상',
] as const
```

2. Add a new state variable `customLocationDetail` in FindingBottomSheet, after the existing `locationDetail` state (currently line 74). Change `locationDetail` default from `''` to `'직접입력'` so the combo select starts with the first option visible:
```typescript
const [locationDetail, setLocationDetail] = useState('직접입력')
const [customLocationDetail, setCustomLocationDetail] = useState('')
```

3. Replace the "위치 상세" section (currently lines 225-235) — remove the `<input type="text">` and replace with a native `<select>` + conditional text input:
```tsx
{/* 위치 상세 */}
<div>
  <div style={lblStyle}>위치 상세</div>
  <select
    value={locationDetail}
    onChange={e => { setLocationDetail(e.target.value); setCustomLocationDetail('') }}
    style={{
      background: 'var(--bg3)',
      borderRadius: 9,
      padding: '8px 12px',
      border: '1px solid var(--bd2)',
      width: '100%',
      color: 'var(--t1)',
      fontSize: 13,
      boxSizing: 'border-box',
      outline: 'none',
      fontFamily: 'inherit',
      appearance: 'none',
      WebkitAppearance: 'none',
    }}
  >
    {ZONE_FLOOR_DETAILS.map(item => (
      <option key={item} value={item}>{item}</option>
    ))}
  </select>
  {locationDetail === '직접입력' && (
    <input
      type="text"
      value={customLocationDetail}
      onChange={e => setCustomLocationDetail(e.target.value)}
      placeholder="직접 입력"
      style={{ ...inputStyle, marginTop: 8 }}
    />
  )}
</div>
```

4. Update the location assembly in mutationFn (currently lines 82-87). Replace `locationDetail.trim()` with the resolved detail value per D-06:
```typescript
const detailValue = locationDetail === '직접입력' ? customLocationDetail.trim() : locationDetail
const loc = [
  ZONES.find(z => z.key === zone)?.label,
  floor,
  detailValue || undefined,
].filter(Boolean).join(' ')
```

5. Update the primary CTA button text from '등록' to '지적사항 등록' per UI-SPEC copywriting contract (currently line 329). Also update the close button text from '취소' to '닫기' per UI-SPEC:
- Line 329: `{isSubmitting ? '처리 중...' : '등록'}` -> `{isSubmitting ? '처리 중...' : '지적사항 등록'}`
- Line 344: `취소` -> `닫기`

IMPORTANT: Do NOT modify FINDING_ITEMS, the item selection list, the photo section, or any other part of FindingBottomSheet beyond the changes above. Per D-04, the item selection is already working and confirmed.
  </action>
  <verify>
    <automated>cd /Users/jykevin/Documents/20260328/cha-bio-safety && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - LegalFindingsPage.tsx contains `const ZONE_FLOOR_DETAILS = [` at module scope (not inside a function)
    - ZONE_FLOOR_DETAILS array contains exactly these items in order: '직접입력', '복도', '계단실', '화장실', 'EPS', 'TPS', '기계실', '전기실', '주차장', '로비', '회의실', '실험실', '옥상'
    - LegalFindingsPage.tsx contains `useState('직접입력')` for locationDetail
    - LegalFindingsPage.tsx contains `useState('')` for customLocationDetail
    - LegalFindingsPage.tsx contains `<select` element with `value={locationDetail}`
    - LegalFindingsPage.tsx contains `{ZONE_FLOOR_DETAILS.map(item =>`
    - LegalFindingsPage.tsx contains `locationDetail === '직접입력' && (` with a text input below
    - LegalFindingsPage.tsx contains `const detailValue = locationDetail === '직접입력' ? customLocationDetail.trim() : locationDetail`
    - LegalFindingsPage.tsx contains `지적사항 등록` as CTA button text (not just '등록')
    - LegalFindingsPage.tsx contains `닫기` as close button text (not '취소')
    - `npm run build` exits with code 0 (no TypeScript or build errors)
  </acceptance_criteria>
  <done>ZONE_FLOOR_DETAILS constant exists at module scope with 13 preset items. FindingBottomSheet renders a native select for location detail with conditional text input for direct entry. Location string correctly assembled from zone + floor + resolved detail. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Deploy to production and verify</name>
  <files>cha-bio-safety/src/pages/LegalFindingsPage.tsx</files>
  <read_first>
    - cha-bio-safety/src/pages/LegalFindingsPage.tsx (verify Task 1 changes are present)
  </read_first>
  <action>
1. Deploy to production:
```bash
cd /Users/jykevin/Documents/20260328/cha-bio-safety && npm run deploy -- --branch production
```

2. After deploy succeeds, verify the build output includes the modified LegalFindingsPage chunk.

3. Production URL to verify: the legal findings page with an existing schedule item, open the "지적사항 등록" bottom sheet and confirm the 상세위치 field shows a native select dropdown with preset options.
  </action>
  <verify>
    <automated>cd /Users/jykevin/Documents/20260328/cha-bio-safety && npm run deploy -- --branch production 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - Deploy command exits with code 0
    - Deploy output contains "Deploying" or "Success" or "Published" indicating successful deployment
    - No build errors in deploy output
  </acceptance_criteria>
  <done>Production deployment successful. FindingBottomSheet with combo select for location detail is live.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> API | Finding creation sends location string to server — existing boundary, no change |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Tampering | ZONE_FLOOR_DETAILS constant | accept | Client-side constant for UI convenience only. Server-side validation of location string is unchanged. Attacker could submit any location string via API regardless — the select merely improves UX, not security. No PII involved. |
| T-13-02 | Spoofing | FindingBottomSheet form submission | accept | Existing JWT auth on `/api/legal/[id]/findings` endpoint unchanged. No new auth surface. |
</threat_model>

<verification>
1. `npm run build` passes with zero errors
2. LegalFindingsPage.tsx contains ZONE_FLOOR_DETAILS constant with 13 items
3. FindingBottomSheet renders native `<select>` for location detail
4. Selecting '직접입력' shows text input, selecting a preset hides it
5. Location string correctly assembled: "연구동 3F 복도" format
6. Production deploy succeeds
</verification>

<success_criteria>
- FIND-01: FINDING_ITEMS scrollable list + direct input works unchanged (already implemented, confirmed by D-04)
- FIND-02: Location input is 3-stage: zone chip -> floor chip -> detail combo select with presets + direct input option
- Build green, production deployed, no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/13-finding-bottomsheet-restructure/13-01-SUMMARY.md`
</output>
