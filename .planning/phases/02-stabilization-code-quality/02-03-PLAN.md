---
phase: 02-stabilization-code-quality
plan: 03
type: execute
wave: 2
depends_on: []
files_modified:
  - cha-bio-safety/package.json
  - cha-bio-safety/package-lock.json
  - cha-bio-safety/vite.config.ts
autonomous: true
requirements: [STAB-08]

must_haves:
  truths:
    - "xlsx-js-style가 package.json dependencies에서 제거되고 node_modules에 없다"
    - "lucide-react와 date-fns가 package.json dependencies에서 제거된다 (소스에서 미사용 확인됨)"
    - "npm run build가 미사용 의존성 제거 후 TypeScript 에러 없이 성공한다"
    - "번들 크기가 의존성 제거 전보다 감소한다 (~400KB xlsx-js-style 절감)"
  artifacts:
    - path: "cha-bio-safety/package.json"
      provides: "xlsx-js-style, lucide-react, date-fns 없는 정리된 의존성 목록"
    - path: "cha-bio-safety/dist/"
      provides: "미사용 의존성 제거 후 빌드 결과물"
  key_links:
    - from: "cha-bio-safety/package.json"
      to: "cha-bio-safety/vite.config.ts"
      via: "manualChunks 참조 정리"
      pattern: "vendor-icons.*lucide|vendor-date.*date-fns 항목 제거"
---

<objective>
미사용 의존성 xlsx-js-style, lucide-react, date-fns를 package.json에서 제거하고 빌드가 성공하는지 검증한다. 이는 테스트 플랜(02-01, 02-02)과 독립적으로 병렬 실행할 수 있는 코드 정리 작업이다.

Purpose: xlsx-js-style 제거로 약 400KB 번들 절감(D-06). lucide-react, date-fns는 Phase 1 조사에서 소스에서 미사용 확인됨(D-07). per D-05(적극적 정리).
Output: 정리된 package.json, 통과하는 빌드
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-stabilization-code-quality/02-CONTEXT.md
@.planning/codebase/STACK.md
@.planning/phases/01-deployment-infrastructure/01-01-SUMMARY.md

<interfaces>
<!-- Phase 1 Summary key findings: lucide-react and date-fns are NOT imported anywhere in src/. -->
<!-- Confirmed by grep: grep -r "lucide-react\|from 'date-fns'" src/ returns no results. -->
<!-- xlsx-js-style is in package.json but fflate is the actual Excel engine (generateExcel.ts). -->

From cha-bio-safety/vite.config.ts (manualChunks entries to clean up):
```typescript
// These entries exist but produce no output — safe to remove after pkg removal:
if (id.includes('node_modules/lucide-react/')) return 'vendor-icons'
if (id.includes('node_modules/date-fns/')) return 'vendor-date'
```

From cha-bio-safety/package.json (entries to remove):
```json
"date-fns": "^4.1.0",
"date-fns-tz": "^3.2.0",
"lucide-react": "^0.454.0",
"xlsx-js-style": "^1.2.0"
```
Note: date-fns-tz is a companion to date-fns — remove together if date-fns is unused.
Also remove: "@types/qrcode" from devDependencies if qrcode types are unused (verify first).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 미사용 의존성 제거 전 소스 전수 검사</name>
  <files>
    (읽기 전용 — 파일 수정 없음)
  </files>
  <read_first>
    - cha-bio-safety/package.json (현재 의존성 목록)
    - cha-bio-safety/src/utils/generateExcel.ts (xlsx-js-style vs fflate 사용 패턴)
    - cha-bio-safety/src/App.tsx (라우팅, 임포트 구조)
  </read_first>
  <action>
    제거 전 소스 전수 검사를 실행하여 각 패키지가 실제로 미사용인지 확인한다.

    실행할 grep 명령어:
    ```bash
    cd /Users/jykevin/Documents/20260328/cha-bio-safety

    # xlsx-js-style 사용 여부 확인
    grep -rn "xlsx-js-style\|XLSXStyle\|xlsx" src/ functions/ --include="*.ts" --include="*.tsx" 2>/dev/null

    # lucide-react 사용 여부 확인
    grep -rn "lucide-react\|from 'lucide" src/ --include="*.ts" --include="*.tsx" 2>/dev/null

    # date-fns 사용 여부 확인 (date-fns-tz 포함)
    grep -rn "from 'date-fns\|from \"date-fns" src/ --include="*.ts" --include="*.tsx" 2>/dev/null

    # @types/qrcode 실제 qrcode 사용 여부 확인
    grep -rn "from 'qrcode'\|require('qrcode')" src/ --include="*.ts" --include="*.tsx" 2>/dev/null
    ```

    결과 분석:
    - xlsx-js-style: 결과 없으면 → 제거 확정
    - lucide-react: 결과 없으면 → 제거 확정 (Phase 1 Summary에서 이미 확인됨)
    - date-fns/date-fns-tz: 결과 없으면 → 제거 확정 (Phase 1 Summary에서 이미 확인됨)
    - @types/qrcode: qrcode 패키지 자체는 QRPrintPage에서 사용 중일 수 있음 → 결과에 따라 결정

    만약 어느 패키지에서 임포트가 발견되면: 해당 패키지는 이번 Task에서 제거하지 않고 Task 2 메모에 "스킵 이유" 기록.

    **중요:** xlsx-js-style은 임포트가 발견되더라도 Phase 1 연구(STACK.md)에서 실제 Excel 엔진은 fflate임이 확인되었으므로, 임포트 발견 시에는 해당 임포트 자체가 데드코드일 가능성이 높음. 발견된 임포트를 먼저 확인할 것.
  </action>
  <verify>
    grep 명령이 완료되고 각 패키지의 사용 여부가 명확히 확인됨.
    확인 결과를 Task 2 실행 전에 콘솔에 출력.
  </verify>
  <acceptance_criteria>
    - xlsx-js-style 임포트 검색 완료
    - lucide-react 임포트 검색 완료
    - date-fns/date-fns-tz 임포트 검색 완료
    - 각 패키지 제거 여부 결정 완료
  </acceptance_criteria>
  <done>모든 대상 패키지의 소스 사용 여부가 grep으로 확인됨</done>
</task>

<task type="auto">
  <name>Task 2: package.json에서 미사용 의존성 제거 및 빌드 검증</name>
  <files>
    cha-bio-safety/package.json
    cha-bio-safety/vite.config.ts
  </files>
  <read_first>
    - cha-bio-safety/package.json (현재 상태)
    - cha-bio-safety/vite.config.ts (현재 manualChunks 설정)
    - Task 1 grep 결과 (제거 확정된 패키지 목록)
  </read_first>
  <action>
    Task 1 확인 결과를 바탕으로 미사용 패키지를 제거하고 빌드를 검증한다.

    **Step 1: npm uninstall 실행**
    ```bash
    cd /Users/jykevin/Documents/20260328/cha-bio-safety

    # Task 1에서 미사용 확인된 패키지만 제거 (확인되지 않은 것은 건드리지 않음)
    npm uninstall xlsx-js-style lucide-react date-fns date-fns-tz
    ```
    위 명령은 package.json과 package-lock.json을 자동 업데이트함.

    **Step 2: vite.config.ts에서 제거된 패키지의 manualChunks 항목 정리**
    현재 vite.config.ts에 lucide-react와 date-fns에 대한 manualChunks 항목이 있음 (Phase 1에서 추가됨).
    이 항목들은 Phase 1 SUMMARY에 따르면 출력을 생성하지 않지만(tree-shaken), 패키지 제거 후에는 주석 처리 또는 제거.

    vite.config.ts에서 다음 패턴을 찾아 제거:
    ```typescript
    // 제거할 항목들 (lucide-react, date-fns 관련 manualChunks)
    if (id.includes('node_modules/lucide-react/')) return 'vendor-icons'
    if (id.includes('node_modules/date-fns/')) return 'vendor-date'
    if (id.includes('node_modules/date-fns-tz/')) return 'vendor-date'
    ```
    (실제 vite.config.ts 읽어서 정확한 라인 확인 후 제거)

    **Step 3: 빌드 실행 및 검증**
    ```bash
    cd /Users/jykevin/Documents/20260328/cha-bio-safety
    npm run build 2>&1
    ```

    기대 결과:
    - TypeScript 컴파일 에러 없음
    - Rollup 번들링 에러 없음
    - "circular chunk" 경고 없음 (Phase 1에서 해결됨)
    - dist/ 디렉토리 생성 완료

    **Step 4: 번들 크기 비교**
    ```bash
    # 빌드 후 dist/assets/ 파일 크기 목록 출력
    ls -lh /Users/jykevin/Documents/20260328/cha-bio-safety/dist/assets/ 2>/dev/null | sort -k5 -rh | head -20
    ```
    xlsx-js-style 제거 전 대비 vendor 청크 크기가 감소했는지 확인.
    (참고: xlsx-js-style은 약 400KB이므로 vendor 청크 중 하나가 ~400KB 감소 예상)

    **빌드 실패 시 대응:**
    - 에러 메시지를 읽고 어떤 패키지/임포트가 문제인지 파악
    - 미사용이라고 판단했는데 실제로 임포트가 있다면 해당 패키지를 npm install로 재설치
    - 재설치 후 빌드 재시도
  </action>
  <verify>
    ```bash
    # 1. package.json에 제거 대상 패키지가 없는지 확인
    grep -E "xlsx-js-style|lucide-react|date-fns" /Users/jykevin/Documents/20260328/cha-bio-safety/package.json

    # 2. 빌드 성공 확인 (dist/index.html 존재)
    ls /Users/jykevin/Documents/20260328/cha-bio-safety/dist/index.html

    # 3. TypeScript 에러 없이 빌드 완료 확인 (빌드 출력 결과)
    # npm run build 출력에서 "error TS" 없음 확인
    ```
  </verify>
  <acceptance_criteria>
    - `grep "xlsx-js-style" cha-bio-safety/package.json` 결과 없음
    - `grep "lucide-react" cha-bio-safety/package.json` 결과 없음
    - `grep "date-fns" cha-bio-safety/package.json` 결과 없음
    - `npm run build` 성공 (exit code 0)
    - `dist/index.html` 파일 존재
    - 빌드 출력에 TypeScript 컴파일 에러 없음
  </acceptance_criteria>
  <done>3개(또는 Task 1 확인 결과에 따른 수량) 미사용 패키지가 제거되고 npm run build가 성공함</done>
</task>

</tasks>

<verification>
```bash
# 의존성 제거 확인
grep -E "xlsx-js-style|lucide-react|\"date-fns\"" /Users/jykevin/Documents/20260328/cha-bio-safety/package.json
# 기대: 출력 없음

# 빌드 결과 확인
ls /Users/jykevin/Documents/20260328/cha-bio-safety/dist/index.html
# 기대: 파일 존재

# 번들 크기 목록
ls -lh /Users/jykevin/Documents/20260328/cha-bio-safety/dist/assets/ | head -20
```
</verification>

<success_criteria>
- xlsx-js-style, lucide-react, date-fns, date-fns-tz가 package.json에서 제거됨
- vite.config.ts manualChunks에서 제거된 패키지의 항목 정리됨
- `npm run build` 성공 (TypeScript 에러 없음, Rollup 경고 없음)
- 번들 크기 감소 확인 (~400KB xlsx-js-style 절감)
</success_criteria>

<output>
After completion, create `.planning/phases/02-stabilization-code-quality/02-03-SUMMARY.md`
</output>
