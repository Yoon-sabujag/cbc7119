---
phase: 03-excel-reports-annual-matrix-types
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - cha-bio-safety/src/pages/ReportsPage.tsx
autonomous: false
requirements: [EXCEL-01, EXCEL-02, EXCEL-03, EXCEL-04, EXCEL-05]

must_haves:
  truths:
    - "User sees 10 report cards on /report page (5 existing + 5 new)"
    - "User can select year for matrix reports and download xlsx"
    - "User can select year AND month for pump report and download xlsx"
    - "Downloaded xlsx files open in Excel/Numbers without corruption"
    - "○ marks appear in correct cells for months with check records"
  artifacts:
    - path: "cha-bio-safety/src/pages/ReportsPage.tsx"
      provides: "Complete reports page with all 10 report types"
      min_lines: 100
  key_links:
    - from: "cha-bio-safety/src/pages/ReportsPage.tsx"
      to: "generateMatrixExcel"
      via: "import and call on button click"
      pattern: "generateMatrixExcel\\("
    - from: "cha-bio-safety/src/pages/ReportsPage.tsx"
      to: "generatePumpExcel"
      via: "import and call on button click"
      pattern: "generatePumpExcel\\("
    - from: "cha-bio-safety/src/pages/ReportsPage.tsx"
      to: "/api/reports/check-monthly"
      via: "api.get with category parameter"
      pattern: "check-monthly.*category"
---

<objective>
Extend ReportsPage.tsx with 5 new report cards (소방펌프, 자탐, 제연, 방화셔터, 피난방화) that call the generator functions from Plan 01, and verify all 5 new Excel downloads work correctly.

Purpose: Completes the user-facing UI for all 5 new report types, making them downloadable from the existing /report page.
Output: Updated ReportsPage.tsx with 10 total report cards + human verification of Excel output quality.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-excel-reports-annual-matrix-types/03-CONTEXT.md
@.planning/phases/03-excel-reports-annual-matrix-types/03-RESEARCH.md
@.planning/phases/03-excel-reports-annual-matrix-types/03-01-SUMMARY.md
@cha-bio-safety/src/pages/ReportsPage.tsx
@cha-bio-safety/src/utils/generateExcel.ts

<interfaces>
<!-- From Plan 01 output — generator function signatures -->
```typescript
// From cha-bio-safety/src/utils/generateExcel.ts (after Plan 01)
export async function generateMatrixExcel(
  year: number,
  data: any[],
  sheetIndex: number,   // 6=피난방화, 7=방화셔터, 8=제연, 9=자탐
  itemCount: number,    // 9 or 10
  reportName: string
): Promise<void>

export async function generatePumpExcel(
  year: number,
  month: number,
  data: any[]
): Promise<void>
```

<!-- Existing report type union and card array from ReportsPage.tsx -->
```typescript
type ReportType = 'div-early' | 'div-late' | '소화전' | '청정소화약제' | '비상콘센트'
// Must extend with: '피난방화' | '방화셔터' | '제연' | '자탐' | '소방펌프'
```

<!-- API call pattern -->
```typescript
const data = await api.get<any[]>(
  `/reports/check-monthly?year=${year}&category=${encodeURIComponent(categoryName)}`
)
```

<!-- DB category names for each new report -->
// EXCEL-05 피난방화 → category='특별피난계단', sheetIndex=6, itemCount=9
// EXCEL-04 방화셔터 → category='방화셔터', sheetIndex=7, itemCount=9
// EXCEL-03 제연 → category='전실제연댐퍼', sheetIndex=8, itemCount=9
// EXCEL-02 자탐 → category='자동화재탐지설비', sheetIndex=9, itemCount=10
// EXCEL-01 소방펌프 → category='소방펌프', sheetIndex=10 (uses generatePumpExcel, not generateMatrixExcel)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend ReportsPage with 5 new report cards</name>
  <files>cha-bio-safety/src/pages/ReportsPage.tsx</files>
  <action>
Modify ReportsPage.tsx to add 5 new report types (per D-03, D-05, D-06).

1. Update imports — add generateMatrixExcel and generatePumpExcel:
   ```typescript
   import { generateDivExcel, generateCheckExcel, generateMatrixExcel, generatePumpExcel } from '../utils/generateExcel'
   ```

2. Extend the ReportType union:
   ```typescript
   type ReportType = 'div-early' | 'div-late' | '소화전' | '청정소화약제' | '비상콘센트'
     | '피난방화' | '방화셔터' | '제연' | '자탐' | '소방펌프'
   ```

3. Add 5 new entries to REPORT_CARDS array (append after existing 5):
   ```typescript
   { type: '피난방화',  title: '월간 피난방화시설 점검일지',    sub: '피난방화 · 연간' },
   { type: '방화셔터',  title: '월간 방화셔터 점검일지',       sub: '방화셔터 · 연간' },
   { type: '제연',      title: '월간 제연설비 점검일지',       sub: '제연설비 · 연간' },
   { type: '자탐',      title: '자동화재탐지설비 점검일지',    sub: '자탐설비 · 연간' },
   { type: '소방펌프',  title: '월간 소방펌프 점검일지',       sub: '소방펌프 · 월간' },
   ```

4. Add month state for pump report — add `useState` for month:
   ```typescript
   const [month, setMonth] = useState(new Date().getMonth() + 1)  // 1-12
   ```

5. Update `handleDownload` to handle new types. Add new branches in the existing try block:

   For matrix types (피난방화/방화셔터/제연/자탐):
   ```typescript
   const MATRIX_CONFIG: Record<string, { category: string; sheetIndex: number; itemCount: number; name: string }> = {
     '피난방화': { category: '특별피난계단', sheetIndex: 6, itemCount: 9, name: '피난방화시설' },
     '방화셔터': { category: '방화셔터', sheetIndex: 7, itemCount: 9, name: '방화셔터' },
     '제연':     { category: '전실제연댐퍼', sheetIndex: 8, itemCount: 9, name: '제연설비' },
     '자탐':     { category: '자동화재탐지설비', sheetIndex: 9, itemCount: 10, name: '자동화재탐지설비' },
   }
   ```
   If `type in MATRIX_CONFIG`:
   - `const cfg = MATRIX_CONFIG[type]`
   - `const data = await api.get<any[]>('/reports/check-monthly?year=${year}&category=${encodeURIComponent(cfg.category)}')`
   - `await generateMatrixExcel(year, data, cfg.sheetIndex, cfg.itemCount, cfg.name)`

   For pump (소방펌프):
   - `const data = await api.get<any[]>('/reports/check-monthly?year=${year}&category=${encodeURIComponent('소방펌프')}')`
   - `await generatePumpExcel(year, month, data)`

6. For the 소방펌프 card ONLY, add a month selector UI inside the card (before the download button). Use the same arrow button style as the year selector in the header:
   - Show `{month}월` centered between left/right arrow buttons
   - Left arrow: decrease month (min 1), Right arrow: increase month (max 12)
   - Style consistent with the existing year nav buttons (navBtn style)

7. Keep all existing card rendering logic unchanged. The new cards use the same card layout, download button style, and loading state pattern.
  </action>
  <verify>
    <automated>cd /Users/jykevin/Documents/20260328/cha-bio-safety && npx tsc --noEmit 2>&1 | head -20 && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>ReportsPage.tsx shows 10 report cards. All 5 new types call the correct generator function with correct parameters. Pump card has month selector. TypeScript compiles clean. Build succeeds.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify all 5 new Excel report downloads</name>
  <action>
Human verifies that all 5 new Excel report types download correctly and produce valid xlsx files.

What was built: 5 new Excel report download buttons on /report page (피난방화시설, 방화셔터, 제연설비, 자동화재탐지설비, 소방펌프). Each downloads an xlsx file with ○ marks based on DB check records.

Steps to verify:
1. Start dev server: `cd cha-bio-safety && npm run dev` (and `npm run dev:api` in another terminal)
2. Navigate to /report page
3. Verify you see 10 report cards (5 existing + 5 new)
4. For each new report type, click the download button:
   a. 피난방화시설 — should download yearly xlsx, open in Excel/Numbers, verify E3 has year, check ○ marks in month columns
   b. 방화셔터 — same structure, verify sheet title matches
   c. 제연설비 — same structure
   d. 자동화재탐지설비 — similar but 10 item rows (row 29 also has data)
   e. 소방펌프 — select a month first, download, verify D3=year G3=month, I/AJ columns have ○ marks
5. Verify no Excel "repair" dialogs when opening files
6. Verify Korean text (item labels, headers) displays correctly
7. Verify months with no check records show blank cells (no ○)
  </action>
  <verify>Human visual inspection of downloaded Excel files</verify>
  <done>All 5 new Excel files download without errors, open in Excel/Numbers without repair dialogs, show correct ○ marks and Korean text.</done>
</task>

</tasks>

<verification>
1. `npm run build` — succeeds without errors
2. All 10 report cards visible on /report page
3. Each of the 5 new types produces a downloadable xlsx
4. Downloaded files open in Excel/Numbers without repair dialogs
5. ○ marks appear in correct month columns for months with check records
6. Empty months have blank cells
</verification>

<success_criteria>
- ReportsPage shows 10 report cards (5 existing + 5 new) per D-03
- Year selector works for all 10 types per D-05/D-06
- Pump report has additional month selector
- All 5 new xlsx files download without errors
- Files open in Excel/Numbers with correct layout (no corruption, no repair dialog)
- ○ marks appear for checked months, blanks for unchecked per D-07/D-08/D-09
- Korean text in item labels and headers displays correctly
</success_criteria>

<output>
After completion, create `.planning/phases/03-excel-reports-annual-matrix-types/03-02-SUMMARY.md`
</output>
