---
phase: 21
plan: 06
type: execute
wave: 4
depends_on: [21-04, 21-05]
files_modified: []
autonomous: false
requirements: [DOC-01, DOC-02, DOC-03, DOC-04, DOC-05, DOC-06]
must_haves:
  truths:
    - "Production deployment completes successfully via `wrangler pages deploy --branch production`"
    - "Logged-in assistant sees /documents in SideMenu (mobile) AND DesktopSidebar (desktop) — including users with existing saved menu_config"
    - "Assistant can download an already-uploaded 소방계획서 via the hero card tap → file saves with correct Korean filename"
    - "Admin can upload a small test PDF → progress reaches 100% → list refreshes without reload"
    - "Assistant does NOT see the upload button on either breakpoint"
    - "Past-history row download works for a second uploaded year"
  artifacts: []
  key_links:
    - from: "production deploy"
      to: "live /documents route"
      via: "wrangler pages deploy --branch production"
      pattern: "wrangler.*deploy"
---

<objective>
Deploy Phase 21 to production and run the smoke-test UAT checklist from RESEARCH.md §"Phase Requirements → Observable Behavior Map". Since this project has no test runner (RESEARCH confirms manual UAT only), the human gate is the validation.

Purpose: ship + verify.
Output: production deploy URL + UAT checklist results.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-documents-page-ui/21-CONTEXT.md
@.planning/phases/21-documents-page-ui/21-RESEARCH.md
@cha-bio-safety/wrangler.toml
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Build and deploy to production</name>
  <files></files>
  <read_first>
    - cha-bio-safety/wrangler.toml (confirm Pages project name and bindings)
    - .planning/phases/20-document-storage/20-03-SUMMARY.md (confirm Phase 20 deploy command pattern)
    - User memory: `wrangler deploy` MUST include `--branch production` flag — otherwise deploys to Preview
  </read_first>
  <action>
From `cha-bio-safety/` directory, run in sequence:

```bash
cd cha-bio-safety && npm run build
cd cha-bio-safety && npx wrangler pages deploy dist --project-name cha-bio-safety --branch production
```

(Adjust project-name if wrangler.toml declares a different name — read the file first.)

On success, capture the deployment URL. If build fails, do NOT deploy — fix the TypeScript error and rebuild.
  </action>
  <verify>
    <automated>cd cha-bio-safety && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0
    - `wrangler pages deploy` prints a production URL and exits 0
    - No build warnings about missing DocumentsPage module
  </acceptance_criteria>
  <done>Production URL captured; ready for human UAT.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human UAT — smoke test the full DocumentsPage feature in production</name>
  <what-built>
Phase 21 DocumentsPage UI is deployed. New route `/documents` with:
- Mobile tabs / desktop 2-column layout
- DocumentSection with hero card + 과거 이력 + empty/loading/error states
- downloadBlob authenticated download
- Admin-only upload: BottomSheet (mobile) / Modal (desktop) with year/title/file form + multipart upload (progress %, MB/s, ETA) + abort/retry
- SideMenu + DesktopSidebar + DEFAULT_SIDE_MENU menu entries
- migrateLegacyMenuConfig forward-merge fix for existing users
  </what-built>
  <how-to-verify>
Open the production deploy URL in a browser. Log in as admin and run through this checklist (mark each item on the shared notes). Then log out and repeat the assistant-side items as an assistant user.

**Menu integration (both users):**
1. Open SideMenu (mobile) — `소방계획서/훈련자료` visible under `문서 관리` section
2. Resize to desktop width ≥ 1024px — `소방계획서/훈련자료` visible in DesktopSidebar under `문서 관리`
3. Existing user check: if any user already had a saved menu_config from Phase 18, confirm the new item appears on next page load (the forward-merge in migrateLegacyMenuConfig)

**As assistant (read-only):**
4. Navigate to `/documents`. Mobile: tab bar visible with `소방계획서` active by default. Desktop: two columns `소방계획서` / `소방훈련자료`.
5. Upload button should NOT be visible in either section
6. Empty state shows `아직 업로드된 문서가 없습니다` + assistant body copy (if list is empty)

**As admin:**
7. On `/documents`, upload button visible per section
8. Tap upload in `소방계획서` section → BottomSheet (mobile) or Modal (desktop) opens
9. Form: year dropdown defaulting to 2026, title empty, file empty
10. Pick a small PDF → title auto-fills to `2026년 소방계획서`
11. Tap `업로드` → progress bar appears, percent increments, speed MB/s + ETA show after 1-2 seconds
12. Upload completes → toast success → sheet closes → list refreshes → hero card now shows the uploaded file with filename, size, uploader name, uploaded_at
13. Tap hero card → file downloads with original filename (Korean characters preserved)
14. Upload a second document with different year (e.g. 2025) → after refresh, 2026 becomes latest hero card, 2025 appears in `과거 이력` list → tap the past row → it downloads correctly
15. Repeat upload for `소방훈련자료` type to verify drill path works identically

**Cancel flow:**
16. Start an upload of a ~50MB+ file → mid-upload tap `취소` → browser confirm `업로드를 취소하시겠습니까?` → confirm → upload aborts, sheet stays open, error state visible, `다시 시도` button present
17. Tap `다시 시도` → upload restarts from part 1 → completes successfully

**Validation:**
18. Try to select a `.txt` file → immediate toast error `지원하지 않는 파일 형식입니다.` (no network request)
19. Try to select a >200MB file → immediate toast error `파일 크기가 200MB를 초과합니다.`

**Security:**
20. As assistant, try to POST to `/api/documents/multipart/create` directly (via devtools fetch) → should return 403 (already enforced by Phase 20)

Report PASS / FAIL per item. If any FAIL, describe the observed behavior and the planner will create a gap-closure plan.
  </how-to-verify>
  <resume-signal>Type `approved` if all items pass, or describe any failing items.</resume-signal>
</task>

</tasks>

<verification>
- Production build passes
- All UAT items pass
</verification>

<success_criteria>
1. Deployed to production
2. Menu integration verified for admin + assistant, new + existing users
3. Download works for all staff
4. Admin upload works end-to-end with progress + abort + retry
5. Assistant cannot upload via UI or direct API
</success_criteria>

<output>
After completion, create `.planning/phases/21-documents-page-ui/21-06-SUMMARY.md` + `.planning/phases/21-documents-page-ui/21-VERIFICATION.md` with checklist results.
</output>
</content>
</invoke>