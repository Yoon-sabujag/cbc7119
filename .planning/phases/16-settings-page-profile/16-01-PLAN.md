---
phase: 16-settings-page-profile
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cha-bio-safety/functions/api/auth/profile.ts
  - cha-bio-safety/src/utils/api.ts
  - cha-bio-safety/src/stores/authStore.ts
  - cha-bio-safety/src/pages/SettingsPage.tsx
autonomous: true
requirements: [PROF-01, PROF-02, APP-03]

must_haves:
  truths:
    - "SettingsPage renders with profile section showing current name"
    - "PUT /api/auth/profile updates staff name in DB"
    - "authStore has updateStaff action to sync name locally"
    - "authApi.updateProfile exists in api.ts"
  artifacts:
    - path: "cha-bio-safety/functions/api/auth/profile.ts"
      provides: "PUT endpoint for name update"
      exports: ["onRequestPut"]
    - path: "cha-bio-safety/src/pages/SettingsPage.tsx"
      provides: "Settings page with password change, name edit, logout"
      min_lines: 100
  key_links:
    - from: "cha-bio-safety/src/pages/SettingsPage.tsx"
      to: "/api/auth/profile"
      via: "authApi.updateProfile mutation"
      pattern: "authApi\\.updateProfile"
    - from: "cha-bio-safety/src/pages/SettingsPage.tsx"
      to: "authStore"
      via: "useAuthStore for logout and staff data"
      pattern: "useAuthStore"
---

<objective>
Create the settings page core: profile API endpoint, API client, authStore update, and the SettingsPage component with password change (migrated from SettingsPanel), name editing, and logout.

Purpose: Build all new artifacts for Phase 16 before wiring them into the app shell.
Output: SettingsPage.tsx, profile API, updated api.ts and authStore.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-settings-page-profile/16-CONTEXT.md

@cha-bio-safety/src/components/SettingsPanel.tsx
@cha-bio-safety/src/stores/authStore.ts
@cha-bio-safety/src/utils/api.ts
@cha-bio-safety/functions/api/auth/change-password.ts
@cha-bio-safety/functions/_middleware.ts
@cha-bio-safety/src/types/index.ts

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/types/index.ts:
```typescript
export type Role = 'admin' | 'assistant'
export interface Staff { id:string; name:string; role:Role; title:string; shiftType?:ShiftType; leaveType?:LeaveType }
```

From src/stores/authStore.ts:
```typescript
interface AuthStore {
  token: string | null
  staff: Staff | null
  isAuthenticated: boolean
  login: (token: string, staff: Staff) => void
  logout: () => void
}
```

From src/utils/api.ts:
```typescript
export const authApi = {
  login: (staffId: string, password: string) => api.post<{ token: string; staff: Staff }>('/auth/login', { staffId, password }),
  changePassword: (data: { currentPassword: string; newPassword: string }) => api.post<void>('/auth/change-password', data),
}
```

From functions/_middleware.ts:
```typescript
// ctx.data = { staffId: payload.sub, staffName: payload.name, role: payload.role }
export interface Env { DB: D1Database; STORAGE: R2Bucket; JWT_SECRET: string; ENVIRONMENT: string }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Profile API endpoint + api.ts + authStore</name>
  <files>cha-bio-safety/functions/api/auth/profile.ts, cha-bio-safety/src/utils/api.ts, cha-bio-safety/src/stores/authStore.ts</files>
  <read_first>
    - cha-bio-safety/functions/api/auth/change-password.ts (pattern for auth handler + Env type import)
    - cha-bio-safety/functions/_middleware.ts (Env interface, ctx.data shape)
    - cha-bio-safety/src/utils/api.ts (authApi object, api.put helper)
    - cha-bio-safety/src/stores/authStore.ts (full file, need to add updateStaff)
    - cha-bio-safety/src/types/index.ts (Staff interface)
  </read_first>
  <action>
    **1. Create `functions/api/auth/profile.ts`** (per D-05: PUT /api/auth/profile):
    - Import `Env` from `../../_middleware`
    - Export `onRequestPut: PagesFunction<Env>`
    - Extract `staffId` from `(ctx as any).data.staffId` (same pattern as change-password.ts)
    - Parse request body: `{ name: string }`
    - Validate: `name` must be non-empty string, trimmed, max 20 chars
    - SQL: `UPDATE staff SET name = ?1, updated_at = ?2 WHERE id = ?3`
    - Return `{ success: true, data: { name: trimmedName } }`
    - Error handling: try/catch returning 500 with Korean message `'서버 오류가 발생했습니다'`

    **2. Update `src/utils/api.ts`** — Add `updateProfile` to `authApi`:
    ```typescript
    updateProfile: (data: { name: string }) =>
      api.put<{ name: string }>('/auth/profile', data),
    ```

    **3. Update `src/stores/authStore.ts`** (per D-06: sync staff.name after change):
    - Add `updateStaff: (partial: Partial<Staff>) => void` to AuthStore interface
    - Implement: `updateStaff: (partial) => set((s) => ({ staff: s.staff ? { ...s.staff, ...partial } : null }))`
    - This allows SettingsPage to call `updateStaff({ name: newName })` after API success
  </action>
  <verify>
    <automated>cd /Users/jykevin/Documents/20260328/cha-bio-safety && grep -q "onRequestPut" functions/api/auth/profile.ts && grep -q "updateProfile" src/utils/api.ts && grep -q "updateStaff" src/stores/authStore.ts && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - grep "onRequestPut" functions/api/auth/profile.ts returns match
    - grep "UPDATE staff SET name" functions/api/auth/profile.ts returns match
    - grep "updateProfile" src/utils/api.ts returns match
    - grep "updateStaff" src/stores/authStore.ts returns match
    - grep "Partial<Staff>" src/stores/authStore.ts returns match
  </acceptance_criteria>
  <done>PUT /api/auth/profile handler exists, authApi.updateProfile callable, authStore.updateStaff syncs name locally</done>
</task>

<task type="auto">
  <name>Task 2: Create SettingsPage with profile, password change, and logout</name>
  <files>cha-bio-safety/src/pages/SettingsPage.tsx</files>
  <read_first>
    - cha-bio-safety/src/components/SettingsPanel.tsx (migrate ChangePasswordForm, Toggle, Row components)
    - cha-bio-safety/src/pages/AdminPage.tsx (page layout pattern reference)
    - cha-bio-safety/src/stores/authStore.ts (after Task 1 changes — updateStaff)
    - cha-bio-safety/src/utils/api.ts (after Task 1 changes — authApi.updateProfile)
  </read_first>
  <action>
    Create `src/pages/SettingsPage.tsx` as a full page (per D-01: independent /settings page).

    **Page structure** (section grouping — Claude's discretion: card-style sections):

    1. **Header** — Page title "설정" with back button (useNavigate(-1)), same style as other self-header pages

    2. **Profile section** (per D-04, D-06, PROF-02):
       - Show user avatar circle (first char of name, gradient background like SideMenu line 249)
       - Show staff name (clickable, per D-04: opens name edit modal)
       - Show staff title and role below name
       - Name edit modal: text input pre-filled with current name, "취소"/"저장" buttons
       - On save: call `authApi.updateProfile({ name })`, on success call `updateStaff({ name })` and toast.success('이름이 변경되었습니다')

    3. **Account section** (PROF-01):
       - "비밀번호 변경" row — clicking opens ChangePasswordForm
       - Migrate ChangePasswordForm from SettingsPanel.tsx exactly (same mutation, same validation, same UI)

    4. **Notification section** (per D-09: migrate from SettingsPanel):
       - "점검 미완료 알림", "미조치 항목 알림", "승강기 점검 D-7 알림" with Toggle components
       - These are placeholder toggles (Phase 17 will make them functional)
       - Migrate Toggle component from SettingsPanel.tsx

    5. **Display section** (per D-09):
       - "테마" select (다크/라이트/시스템)
       - "주간 현황 기준" select
       - "결과 즉시 저장" toggle
       - Migrate Row component from SettingsPanel.tsx

    6. **Logout button** (per D-07, D-08, APP-03):
       - Red-tinted button at bottom: "로그아웃"
       - onClick: `logout(); navigate('/login')` using authStore.logout()

    7. **App info footer** (per D-10):
       - "차바이오컴플렉스 방재 v1.0.0"
       - "경기도 성남시 분당구 판교로 335"

    **Styling:**
    - Use inline styles with CSS variables (project convention)
    - Page background: `var(--bg)`
    - Section cards: `background: var(--bg2)`, borderRadius 12, margin 12px horizontal
    - Section headers: fontSize 9, fontWeight 700, color var(--t3), uppercase, letterSpacing 0.08em
    - Input styles: same as ChangePasswordForm (height 40, bg var(--bg3), border var(--bd), borderRadius 8)

    **State management:**
    - `const { staff, logout, updateStaff } = useAuthStore()`
    - useMutation for name update
    - useState for showPwChange, showNameEdit, editName

    **Export:** `export default function SettingsPage()` (default export for lazy loading per D-02)
  </action>
  <verify>
    <automated>cd /Users/jykevin/Documents/20260328/cha-bio-safety && grep -q "export default function SettingsPage" src/pages/SettingsPage.tsx && grep -q "authApi.updateProfile" src/pages/SettingsPage.tsx && grep -q "logout" src/pages/SettingsPage.tsx && grep -q "ChangePasswordForm\|changePassword" src/pages/SettingsPage.tsx && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - grep "export default function SettingsPage" src/pages/SettingsPage.tsx returns match
    - grep "authApi.updateProfile" src/pages/SettingsPage.tsx returns match (PROF-02 name change)
    - grep "changePassword" src/pages/SettingsPage.tsx returns match (PROF-01 password change)
    - grep "logout" src/pages/SettingsPage.tsx returns match (APP-03 logout)
    - grep "updateStaff" src/pages/SettingsPage.tsx returns match (D-06 local sync)
    - grep "navigate.*login" src/pages/SettingsPage.tsx returns match (D-08 logout redirect)
    - File has more than 100 lines (wc -l)
  </acceptance_criteria>
  <done>SettingsPage renders profile card with name edit, password change form, notification/display toggles, logout button, and app info footer</done>
</task>

</tasks>

<verification>
- `functions/api/auth/profile.ts` exports onRequestPut
- `src/utils/api.ts` has authApi.updateProfile
- `src/stores/authStore.ts` has updateStaff action
- `src/pages/SettingsPage.tsx` is a complete page with all 3 requirements (PROF-01, PROF-02, APP-03)
- No TypeScript compilation errors: `npx tsc --noEmit` in cha-bio-safety/
</verification>

<success_criteria>
All new artifacts exist and are self-contained. SettingsPage can be lazy-loaded. Profile API endpoint handles name updates. authStore syncs name changes locally.
</success_criteria>

<output>
After completion, create `.planning/phases/16-settings-page-profile/16-01-SUMMARY.md`
</output>
