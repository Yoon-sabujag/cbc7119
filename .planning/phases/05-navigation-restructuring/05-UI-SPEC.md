---
phase: 5
slug: navigation-restructuring
status: draft
shadcn_initialized: false
preset: none
created: 2026-04-01
---

# Phase 5 — UI Design Contract

> Visual and interaction contract for Phase 5: Navigation Restructuring.
> Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none (manual CSS variables + Tailwind) |
| Preset | not applicable |
| Component library | none (custom inline-style components) |
| Icon library | lucide-react available; BottomNav uses inline SVG (strokeWidth 1.8) |
| Font | Noto Sans KR (primary), JetBrains Mono (code/mono) |

**Source:** `tailwind.config.js`, `src/index.css`, `src/components/BottomNav.tsx`

No `components.json` detected. Project uses an established custom design system (CSS variables + inline styles). shadcn initialization is not applicable.

---

## CSS Variable Tokens

All color and surface values reference these CSS variables from `src/index.css`.
Use these variables directly — do NOT hardcode hex values in new components.

| Variable | Value | Semantic Role |
|----------|-------|---------------|
| `--bg` | `#0d1117` | Page background (dominant 60%) |
| `--bg2` | `#161b22` | Cards, nav, sidebar (secondary 30%) |
| `--bg3` | `#1c2128` | Tertiary surfaces, hover states |
| `--bg4` | `#22272e` | Quaternary surfaces, active states |
| `--bd` | `rgba(255,255,255,0.07)` | Default border |
| `--bd2` | `rgba(255,255,255,0.13)` | Elevated border |
| `--t1` | `#e6edf3` | Primary text |
| `--t2` | `#8b949e` | Secondary text |
| `--t3` | `#6e7681` | Muted text, labels, badges |
| `--acl` | `#3b82f6` | Accent (active state, CTA) |
| `--danger` | `#ef4444` | Destructive, badge (unresolved count) |
| `--warn` | `#f59e0b` | Warning |
| `--safe` | `#22c55e` | Success / normal |
| `--fire` | `#f97316` | Fire / critical alert |

---

## Spacing Scale

Declared values (multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, inline padding |
| sm | 8px | Compact element spacing, icon button padding |
| md | 16px | Default element spacing, section padding |
| lg | 24px | Section breaks |
| xl | 32px | Layout gaps |
| 2xl | 48px | Major section breaks |
| 3xl | 64px | Page-level spacing |

Exceptions:
- BottomNav total height: `calc(54px + var(--sab, 34px))` — fixed by existing implementation, do not change
- BottomNav tab padding: `3px 0` — preserves existing touch target sizing
- QR button: `margin-top: -14px` — existing floating treatment, preserve as-is
- SideMenu item padding: `9px 13px` — existing pattern, preserve
- Safe area inset: `var(--sat)` (top), `var(--sab)` (bottom) — always include in height calculations
- GlobalHeader height: `48px` — 8-point aligned, matches ElevatorPage header pattern (`padding: 8px 12px`)
- Hamburger / icon button touch target: `32px × 32px` minimum (existing `iconBtnSt` pattern in ElevatorPage)

**Source:** `src/components/BottomNav.tsx`, `src/pages/ElevatorPage.tsx`

---

## Typography

4-size scale. Hierarchy within the same pixel size is expressed through weight, not additional sizes.

| Role | Size | Weight | Line Height | Usage |
|------|------|--------|-------------|-------|
| Nav label | 9.5px | 500 | 1.2 | BottomNav tab labels |
| Caption | 11px | 500–600 | 1.3 | SideMenu section headers (uppercase), badges, metadata |
| Body | 13px | 400–500 | 1.5 | SideMenu menu item labels, GlobalHeader title |
| Subheading | 13px | 700 | 1.4 | Section headings, modal headings (same size as Body; weight differentiates) |
| Heading | 18px | 700 | 1.2 | Page title (MorePage pattern: `fontSize: 18, fontWeight: 700`) |

**Two declared weights:** regular/medium (400–500) and bold (600–700).

**Discretion note:** GlobalHeader page title uses 13px weight 700 — matches ElevatorPage header (`fontSize:13, fontWeight:700`). This is the Subheading weight applied to the Body size; no separate 14px size is needed.

**Source:** `src/components/BottomNav.tsx` (9.5px), `src/components/SideMenu.tsx` (9px section header, 12.5px items), `src/pages/ElevatorPage.tsx` (13px header), `src/pages/MorePage.tsx` (18px heading)

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `var(--bg)` = `#0d1117` | Page background |
| Secondary (30%) | `var(--bg2)` = `#161b22` | GlobalHeader, BottomNav, SideMenu panel, cards |
| Accent (10%) | `var(--acl)` = `#3b82f6` | Active BottomNav tab (icon + label), QR button background gradient start, active SideMenu state |
| Destructive | `var(--danger)` = `#ef4444` | Unresolved-count badge on 조치 tab (Phase 6), no destructive actions in Phase 5 |

Accent reserved for: active BottomNav tab icon, active BottomNav tab label, QR button gradient, active/hover state indicator only. Never applied to passive navigation items.

**Disabled / coming-soon items:** `color: var(--t3)`, `opacity: 0.5`, cursor: default. Badge: background `var(--bg3)`, text `var(--t3)`, border-radius 6px, padding `2px 7px`, font-size 10px, content "준비중".

**Source:** `src/index.css` (token definitions), `src/components/BottomNav.tsx` (active state), `src/pages/MorePage.tsx` (준비중 badge)

---

## Component Inventory

### New Components (Phase 5)

#### `GlobalHeader`
- Location: `src/components/GlobalHeader.tsx`
- Layout: `display: flex`, `alignItems: center`, `height: 48px`, `padding: 0 12px`, `background: var(--bg2)`, `borderBottom: 1px solid var(--bd)`, `flexShrink: 0`
- Left slot: hamburger button (`32×32px`, background `var(--bg3)`, borderRadius 7px, icon: 3-line SVG `strokeWidth 2`, color `var(--t2)`)
  - Button must declare `aria-label="메뉴 열기"` as a required prop attribute for screen reader accessibility
- Center: page title text — `fontSize: 13px`, `fontWeight: 700`, `color: var(--t1)`
- Right slot: optional (reserved for future use; not rendered in Phase 5)
- Receives props: `title: string`, `onMenuOpen: () => void`
- Pattern reference: ElevatorPage header (`src/pages/ElevatorPage.tsx` lines 274–297)

#### `RemediationPlaceholder` (inline in `RemediationPage`)
- Location: `src/pages/RemediationPage.tsx`
- Layout: full-height flex column, `background: var(--bg)`, GlobalHeader on top
- Center content: icon (wrench SVG, 48×48px, color `var(--t3)`) + heading "조치 관리" (`fontSize: 16, fontWeight: 700, color: var(--t1)`) + body text "준비 중입니다" (`fontSize: 13, color: var(--t2)`)
- No action buttons in placeholder state

### Modified Components (Phase 5)

#### `BottomNav` — tab array change
- Remove: `더보기` tab (`key: 'more'`, path `/more`)
- Add: `조치` tab at position 4 (`key: 'remediation'`, path `/remediation`)
  - Label: "조치"
  - Icon: wrench/tool SVG — inline, `fill="none"`, `viewBox="0 0 24 24"`, `strokeWidth={1.8}`, `stroke="currentColor"`
  - SVG path (wrench): `M10.343 3.94c.09-.542.56-.94 1.11-.94h1.093c.55 0 1.02.398 1.11.94l.149.894c.07.424.384.764.78.93.398.164.855.142 1.205-.108l.737-.527a1.125 1.125 0 011.45.12l.773.774c.39.389.44 1.002.12 1.45l-.527.737c-.25.35-.272.806-.107 1.204.165.397.505.71.93.78l.893.15c.543.09.94.56.94 1.11v1.093c0 .55-.397 1.02-.94 1.11l-.893.149c-.425.07-.765.383-.93.78-.165.398-.143.854.107 1.204l.527.738c.32.447.269 1.06-.12 1.45l-.774.773a1.125 1.125 0 01-1.449.12l-.738-.527c-.35-.25-.806-.272-1.203-.107-.397.165-.71.505-.781.929l-.149.894c-.09.542-.56.94-1.11.94h-1.094c-.55 0-1.019-.398-1.11-.94l-.148-.894c-.071-.424-.384-.764-.781-.93-.398-.164-.854-.142-1.204.108l-.738.527c-.447.32-1.06.269-1.45-.12l-.773-.774a1.125 1.125 0 01-.12-1.45l.527-.737c.25-.35.273-.806.108-1.204-.165-.397-.505-.71-.93-.78l-.894-.15c-.542-.09-.94-.56-.94-1.11v-1.094c0-.55.398-1.019.94-1.11l.894-.148c.424-.071.765-.384.93-.781.165-.398.143-.854-.108-1.204l-.526-.738a1.125 1.125 0 01.12-1.45l.773-.773a1.125 1.125 0 011.45-.12l.737.527c.35.25.807.272 1.204.107.397-.165.71-.505.78-.929l.15-.894z M15 12a3 3 0 11-6 0 3 3 0 016 0z`
  - Badge: red dot/count badge when unresolved count > 0 — Phase 6 API connects; Phase 5: badge NOT rendered (no hardcoded value)
- Keep: `NavKey` type — change `'more'` to `'remediation'`
- Active detection: `pathname.startsWith('/remediation')` activates 조치 tab

#### `SideMenu` — 4-section restructure
Replace existing 3-section `MENU` array with:

| Section | Items |
|---------|-------|
| 주요 기능 | 대시보드 (`/dashboard`), 소방 점검 (`/inspection`), QR 스캔 (`/inspection/qr`), 조치 관리 (`/remediation`) |
| 점검 관리 | 월간 점검 계획 (`/schedule`), 점검 일지 출력 (`/reports`), 일일업무일지 (`/daily-report`), QR 코드 출력 (`/qr-print`), DIV 압력 관리 (`/div`) |
| 근무·복지 | 근무표 (`/workshift`), 연차 관리 (`/leave`), 식당 메뉴 (`/menu`, `soon: true`) |
| 시스템 | 건물 도면 (`/floorplan`), 승강기 관리 (`/elevator`), 법적 점검 (`/legal`, `soon: true`), 관리자 설정 (`/admin`, `soon: true`) |

Badge changes:
- 소방 점검 badge `3` → `0` (D-06: remove hardcoded badges)
- 미조치 항목 row → removed entirely (replaced by 조치 관리 in 주요 기능)

준비중 item rendering:
- Opacity: 0.5, cursor: default, pointer-events: none
- Badge: `<span style={{ fontSize:10, color:'var(--t3)', background:'var(--bg3)', borderRadius:6, padding:'2px 7px' }}>준비중</span>`

#### `App.tsx` — routing changes
- Add lazy import: `const RemediationPage = lazy(() => import('./pages/RemediationPage'))`
- Remove lazy import: `MorePage`
- Add route: `<Route path="/remediation" element={<Auth><RemediationPage /></Auth>} />`
- Replace route: `/more` → `<Route path="/more" element={<Navigate to="/dashboard" replace />} />`
- `NO_NAV_PATHS`: Add `/meal`, `/education`, `/admin`, `/legal-inspection`. Do NOT add `/remediation`.
- GlobalHeader + SideMenu mount: add inside `Layout`, conditionally rendered when `showNav === true` (same condition as BottomNav). SideMenu state (`sideOpen`) lives in Layout.
- Layout renders: `<GlobalHeader title={pageTitleFor(pathname)} onMenuOpen={() => setSideOpen(true)} />` above the content area when `showNav`.

#### `DashboardPage`, `ElevatorPage`, `InspectionPage` — header replacement
- Remove: individual `const [sideOpen, setSideOpen] = useState(false)` state
- Remove: `<SideMenu open={sideOpen} onClose={() => setSideOpen(false)} />` mount
- Remove: individual hamburger button from each page's own header
- `DashboardPage` and `ElevatorPage`: page's own header row becomes title-only (no hamburger). Title row is preserved if it contains non-navigation content (e.g., ElevatorPage's tab bar stays).
- `InspectionPage`: remove existing back/close button from header; rely on GlobalHeader.

---

## Interaction Contracts

### BottomNav active state
- Active tab: icon color `var(--acl)`, label color `var(--acl)`
- Inactive tab: icon color `var(--t3)`, label color `var(--t3)`
- QR tab: always uses gradient button treatment (never colored `var(--acl)`)
- Transition: none (instantaneous, matching existing implementation)

### GlobalHeader hamburger button
- Press: sets `sideOpen = true` in Layout state
- Button props: `aria-label="메뉴 열기"` — required; must be present on the `<button>` element
- Button style: `width: 32px, height: 32px, borderRadius: 7px, background: var(--bg3), border: none, color: var(--t2), cursor: pointer, display: flex, alignItems: center, justifyContent: center`
- Icon: 3-line hamburger SVG, `width={15} height={15}`, `strokeWidth={2}`, `stroke="var(--t2)"`

### SideMenu open/close
- Open: `transform: translateX(0)`, transition `0.3s cubic-bezier(.4,0,.2,1)`
- Close: `transform: translateX(-100%)`
- Overlay: `opacity: 0.65 (rgba(0,0,0,0.65))` when open, `0` when closed, `transition: opacity 0.28s`
- Close triggers: overlay click, ✕ button click, item navigation
- zIndex: overlay `190`, panel `200`

### /more redirect
- Behavior: `<Navigate to="/dashboard" replace />` — no animation, no toast
- Seamless — user arriving at /more from a bookmark lands on dashboard without error

### 조치 tab placeholder page
- No interaction except GlobalHeader hamburger (opens SideMenu)
- No buttons, no forms, no loading states
- Static content only

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| 조치 tab label | 조치 |
| GlobalHeader title — RemediationPage | 조치 관리 |
| RemediationPage placeholder heading | 조치 관리 |
| RemediationPage placeholder body | 준비 중입니다 |
| SideMenu 준비중 badge | 준비중 |
| 식당 메뉴 (준비중) | 식당 메뉴 |
| 법적 점검 (준비중) | 법적 점검 |
| 관리자 설정 (준비중) | 관리자 설정 |
| GlobalHeader title — DashboardPage | 대시보드 |
| GlobalHeader title — InspectionPage | 소방 점검 |
| GlobalHeader title — ElevatorPage | 승강기 관리 |
| GlobalHeader title — RemediationPage | 조치 관리 |

Error states: None in Phase 5 (no API calls, no forms, no mutations).

Destructive actions: None in Phase 5.

**Source:** CONTEXT.md (D-01 through D-15), MorePage.tsx (준비중 pattern), ElevatorPage.tsx (header copy)

---

## Page-Level Title Map

Used by Layout's `GlobalHeader` to resolve `title` prop from `pathname`:

| Pathname | Title |
|----------|-------|
| `/dashboard` | 대시보드 |
| `/inspection` | 소방 점검 |
| `/inspection/qr` | QR 스캔 |
| `/remediation` | 조치 관리 |
| `/elevator` | 승강기 관리 |

Pages in `NO_NAV_PATHS` (e.g. `/schedule`, `/reports`) do not receive GlobalHeader — they have their own headers.

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none | not applicable — shadcn not initialized |

No third-party registries. All new components are hand-authored inline-style React components following the project's established pattern.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
