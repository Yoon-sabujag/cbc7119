---
phase: 08-meal-records
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cha-bio-safety/migrations/0035_meal_records.sql
  - cha-bio-safety/functions/api/meal/index.ts
  - cha-bio-safety/src/utils/api.ts
  - cha-bio-safety/src/utils/mealCalc.ts
  - cha-bio-safety/src/App.tsx
  - cha-bio-safety/src/components/SideMenu.tsx
autonomous: true
requirements: [MEAL-01, MEAL-02]

must_haves:
  truths:
    - "meal_records 테이블이 D1에 존재하고 staff_id+date UNIQUE 제약이 적용된다"
    - "GET /api/meal?month=YYYY-MM 호출 시 해당 월의 미식 기록이 반환된다"
    - "POST /api/meal 호출 시 skippedMeals가 0이면 행 삭제, 양수면 upsert 된다"
    - "calcProvidedMeals 함수가 근무 유형+연차+요일에 따라 올바른 제공 식수(0/1/2)를 반환한다"
    - "calcWeekendAllowance 함수가 토요일 당직=5500, 일요일 당직=11000을 반환한다"
    - "SideMenu에서 식당 메뉴가 활성화되어 /meal로 이동 가능하다"
    - "/meal 라우트가 App.tsx에 등록되어 MealPage를 렌더링한다"
  artifacts:
    - path: "cha-bio-safety/migrations/0035_meal_records.sql"
      provides: "meal_records 테이블 DDL"
      contains: "CREATE TABLE meal_records"
    - path: "cha-bio-safety/functions/api/meal/index.ts"
      provides: "GET/POST /api/meal 핸들러"
      exports: ["onRequestGet", "onRequestPost"]
    - path: "cha-bio-safety/src/utils/mealCalc.ts"
      provides: "제공 식수 + 주말 식대 순수 계산 함수"
      exports: ["calcProvidedMeals", "calcWeekendAllowance"]
    - path: "cha-bio-safety/src/utils/api.ts"
      provides: "mealApi 네임스페이스"
      contains: "mealApi"
  key_links:
    - from: "cha-bio-safety/src/utils/api.ts"
      to: "/api/meal"
      via: "mealApi.list and mealApi.upsert"
      pattern: "mealApi"
    - from: "cha-bio-safety/src/App.tsx"
      to: "cha-bio-safety/src/pages/MealPage.tsx"
      via: "lazy import + Route"
      pattern: "MealPage.*lazy"
---

<objective>
식사 기록 기능의 백엔드(DB + API)와 유틸리티 함수(제공 식수/주말 식대 계산) 및 프론트엔드 라우팅을 구축한다.

Purpose: Plan 02의 MealPage UI가 의존하는 모든 인프라를 선행 구축
Output: meal_records 마이그레이션, /api/meal GET/POST, mealApi 클라이언트, mealCalc.ts 순수 함수, App.tsx 라우트 + SideMenu 활성화
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-meal-records/08-CONTEXT.md
@.planning/phases/08-meal-records/08-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From cha-bio-safety/src/utils/shiftCalc.ts:
```typescript
export type RawShift = '당' | '비' | '주' | '휴'
export function getMonthlySchedule(
  year: number, month: number,
  staffData?: { id: string; name: string; title: string }[]
): { daysInMonth: number; staffRows: { id: string; name: string; title: string; shifts: RawShift[] }[] }
```

From cha-bio-safety/src/utils/api.ts:
```typescript
export interface LeaveItem {
  id: string; staffId: string; staffName?: string;
  date: string; type: string; year: number; createdAt: string
}
export const leaveApi = {
  list: (year: number, month?: string) =>
    api.get<{ myLeaves: LeaveItem[]; teamLeaves: LeaveItem[] }>(
      `/leaves?year=${year}${month ? `&month=${month}` : ''}`
    ),
}
```

From cha-bio-safety/src/components/SideMenu.tsx line 34:
```typescript
{ label: '식당 메뉴', path: '/meal', badge: 0, soon: true },
```

From cha-bio-safety/src/App.tsx line 51:
```typescript
const NO_NAV_PATHS = ['/', '/login', '/schedule', '/reports', '/workshift', '/leave', '/floorplan', '/div', '/qr-print', '/daily-report', '/meal', '/education', '/admin', '/legal-inspection']
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DB 마이그레이션 + API 엔드포인트 + mealApi 클라이언트</name>
  <files>
    cha-bio-safety/migrations/0035_meal_records.sql,
    cha-bio-safety/functions/api/meal/index.ts,
    cha-bio-safety/src/utils/api.ts
  </files>
  <read_first>
    cha-bio-safety/migrations/0034_staff_fields.sql,
    cha-bio-safety/functions/api/leaves/index.ts,
    cha-bio-safety/src/utils/api.ts
  </read_first>
  <action>
1. Create `cha-bio-safety/migrations/0035_meal_records.sql` (per D-23, D-24):
```sql
CREATE TABLE meal_records (
  id           TEXT PRIMARY KEY DEFAULT (lower(hex(randomblob(8)))),
  staff_id     TEXT NOT NULL REFERENCES staff(id),
  date         TEXT NOT NULL,
  skipped_meals INTEGER NOT NULL CHECK(skipped_meals IN (0, 1, 2)) DEFAULT 0,
  created_at   TEXT NOT NULL DEFAULT (datetime('now')),
  updated_at   TEXT NOT NULL DEFAULT (datetime('now')),
  UNIQUE(staff_id, date)
);
CREATE INDEX idx_meal_records_staff_month ON meal_records(staff_id, date);
```

2. Create `cha-bio-safety/functions/api/meal/index.ts` with two handlers:

**onRequestGet:** Read `staffId` from `data` (set by _middleware.ts JWT). Parse `month` query param (format `YYYY-MM`). Query:
```sql
SELECT date, skipped_meals as skippedMeals FROM meal_records WHERE staff_id = ? AND date LIKE ? ORDER BY date ASC
```
Bind `staffId` and `${month}%`. Return `{ success: true, data: { records: result.results ?? [] } }`.

**onRequestPost:** Read `staffId` from `data`. Parse body `{ date: string, skippedMeals: number }`. Validate skippedMeals is 0, 1, or 2. If skippedMeals === 0, DELETE the row (`DELETE FROM meal_records WHERE staff_id = ? AND date = ?`). Otherwise, INSERT OR REPLACE:
```sql
INSERT INTO meal_records (staff_id, date, skipped_meals)
VALUES (?, ?, ?)
ON CONFLICT(staff_id, date) DO UPDATE SET skipped_meals = excluded.skipped_meals, updated_at = datetime('now')
```
Return `{ success: true }`.

Use the same `PagesFunction<Env>` pattern as `functions/api/leaves/index.ts`. Access DB via `env.DB`. Access staffId via `(data as any).staffId`.

3. Add `mealApi` namespace to `cha-bio-safety/src/utils/api.ts` at the end of the file, before or after existing API namespaces:
```typescript
export const mealApi = {
  list: (year: number, month: string) =>
    api.get<{ records: { date: string; skippedMeals: number }[] }>(
      `/meal?month=${month}`
    ),
  upsert: (date: string, skippedMeals: number) =>
    api.post<void>('/meal', { date, skippedMeals }),
}
```
The `month` param format is `YYYY-MM`. The `year` param is included in the signature for consistency but the API uses `month` string directly.
  </action>
  <verify>
    <automated>cd /Users/jykevin/Documents/20260328 && cat cha-bio-safety/migrations/0035_meal_records.sql | grep -c "CREATE TABLE meal_records" && grep -c "onRequestGet\|onRequestPost" cha-bio-safety/functions/api/meal/index.ts && grep -c "mealApi" cha-bio-safety/src/utils/api.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep "CREATE TABLE meal_records" cha-bio-safety/migrations/0035_meal_records.sql returns match
    - grep "UNIQUE(staff_id, date)" cha-bio-safety/migrations/0035_meal_records.sql returns match
    - grep "onRequestGet" cha-bio-safety/functions/api/meal/index.ts returns match
    - grep "onRequestPost" cha-bio-safety/functions/api/meal/index.ts returns match
    - grep "ON CONFLICT" cha-bio-safety/functions/api/meal/index.ts returns match
    - grep "mealApi" cha-bio-safety/src/utils/api.ts returns match
    - grep "skippedMeals" cha-bio-safety/functions/api/meal/index.ts returns match (camelCase mapping)
  </acceptance_criteria>
  <done>
    - meal_records 테이블 DDL이 0035 마이그레이션에 존재
    - GET /api/meal?month=YYYY-MM 핸들러가 staffId 기준 월별 조회 수행
    - POST /api/meal 핸들러가 skippedMeals=0이면 DELETE, 양수면 UPSERT 수행
    - mealApi.list()와 mealApi.upsert()가 api.ts에 export됨
  </done>
</task>

<task type="auto">
  <name>Task 2: 제공 식수/주말 식대 계산 유틸 + 라우팅 와이어링</name>
  <files>
    cha-bio-safety/src/utils/mealCalc.ts,
    cha-bio-safety/src/App.tsx,
    cha-bio-safety/src/components/SideMenu.tsx
  </files>
  <read_first>
    cha-bio-safety/src/utils/shiftCalc.ts,
    cha-bio-safety/src/App.tsx,
    cha-bio-safety/src/components/SideMenu.tsx,
    cha-bio-safety/src/pages/LeavePage.tsx
  </read_first>
  <action>
1. Create `cha-bio-safety/src/utils/mealCalc.ts` — pure functions with no side effects (per D-08 through D-17, D-20):

```typescript
import type { RawShift } from './shiftCalc'

/**
 * 일별 제공 식수 계산 (D-08 ~ D-14)
 * @param rawShift - 근무 유형 ('당'|'비'|'주'|'휴')
 * @param leaveType - 연차 유형 (undefined = 연차 없음)
 * @param dayOfWeek - 0=일, 6=토
 * @returns 0, 1, or 2
 */
export function calcProvidedMeals(
  rawShift: RawShift,
  leaveType: string | undefined,
  dayOfWeek: number
): number {
  // D-12: 전일 연차/공가 → 0
  if (leaveType === 'full' || leaveType === 'official_full') return 0
  // D-14: 비번 → 0
  if (rawShift === '비') return 0
  // D-11: 일요일 당직 → 0
  if (rawShift === '당' && dayOfWeek === 0) return 0
  // D-10: 토요일 당직 → 1
  if (rawShift === '당' && dayOfWeek === 6) return 1
  // D-09: 평일 당직 → 2
  if (rawShift === '당') return 2
  // D-13: 반차/공가 0.5일 → 1
  if (rawShift === '주' && leaveType && (
    leaveType === 'half' || leaveType === 'half_am' || leaveType === 'half_pm' ||
    leaveType === 'official_half_am' || leaveType === 'official_half_pm'
  )) return 1
  // D-08: 평일 주간 → 1
  if (rawShift === '주') return 1
  // '휴' (공휴일) → 0
  return 0
}

/**
 * 주말 식대 계산 (D-15 ~ D-17)
 * 미식 여부와 무관, 당직 여부만으로 결정
 */
export function calcWeekendAllowance(
  rawShift: RawShift,
  dayOfWeek: number
): number {
  if (rawShift === '당' && dayOfWeek === 6) return 5500   // D-15: 토요일 당직
  if (rawShift === '당' && dayOfWeek === 0) return 11000  // D-16: 일요일 당직
  return 0
}
```

Include the `'half'` legacy type in the leaveType check (Pitfall 2 from RESEARCH.md — migration 0031 CHECK includes 'half').

2. In `cha-bio-safety/src/App.tsx`:
   - Add lazy import: `const MealPage = lazy(() => import('./pages/MealPage'))` (place near other lazy imports)
   - Add route inside the `<Routes>` block: `<Route path="/meal" element={<MealPage />} />` (place near /leave route)
   - NO_NAV_PATHS already includes '/meal' — do NOT modify that line

3. In `cha-bio-safety/src/components/SideMenu.tsx` line 34:
   - Change `soon: true` to `soon: false` for the 식당 메뉴 item
   - Keep the label as '식당 메뉴' (or change to '식사 기록' — executor discretion per CONTEXT.md Claude's Discretion)
  </action>
  <verify>
    <automated>cd /Users/jykevin/Documents/20260328 && grep -c "calcProvidedMeals\|calcWeekendAllowance" cha-bio-safety/src/utils/mealCalc.ts && grep "MealPage" cha-bio-safety/src/App.tsx | head -3 && grep "meal.*soon.*false" cha-bio-safety/src/components/SideMenu.tsx</automated>
  </verify>
  <acceptance_criteria>
    - grep "export function calcProvidedMeals" cha-bio-safety/src/utils/mealCalc.ts returns match
    - grep "export function calcWeekendAllowance" cha-bio-safety/src/utils/mealCalc.ts returns match
    - grep "'half'" cha-bio-safety/src/utils/mealCalc.ts returns match (legacy leaveType handled)
    - grep "return 5500" cha-bio-safety/src/utils/mealCalc.ts returns match
    - grep "return 11000" cha-bio-safety/src/utils/mealCalc.ts returns match
    - grep "MealPage.*lazy" cha-bio-safety/src/App.tsx returns match
    - grep "path=\"/meal\"" cha-bio-safety/src/App.tsx returns match (route registered)
    - grep "soon: false" for meal item in SideMenu.tsx returns match
  </acceptance_criteria>
  <done>
    - calcProvidedMeals가 8개 케이스(D-08~D-14 + 휴)를 올바르게 반환
    - calcWeekendAllowance가 토요일 당직 5500, 일요일 당직 11000을 반환
    - /meal 라우트가 MealPage를 lazy 로딩
    - SideMenu 식당 메뉴가 soon:false로 활성화
  </done>
</task>

</tasks>

<verification>
1. `cat cha-bio-safety/migrations/0035_meal_records.sql` — DDL 존재 확인
2. `grep -n "onRequestGet\|onRequestPost" cha-bio-safety/functions/api/meal/index.ts` — 두 핸들러 모두 존재
3. `grep "mealApi" cha-bio-safety/src/utils/api.ts` — 클라이언트 네임스페이스 존재
4. `grep "calcProvidedMeals\|calcWeekendAllowance" cha-bio-safety/src/utils/mealCalc.ts` — 순수 함수 존재
5. `grep "MealPage" cha-bio-safety/src/App.tsx` — 라우트 등록
6. `grep "soon: false" cha-bio-safety/src/components/SideMenu.tsx` — SideMenu 활성화
7. `cd cha-bio-safety && npx tsc --noEmit 2>&1 | head -20` — TypeScript 컴파일 에러 없음 (MealPage.tsx 미존재는 Plan 02에서 생성)
</verification>

<success_criteria>
- meal_records 마이그레이션이 0035 번호로 존재하고 UNIQUE(staff_id, date) 제약 포함
- /api/meal GET/POST 핸들러가 정상 동작 구조
- mealApi.list/upsert가 api.ts에 export됨
- calcProvidedMeals/calcWeekendAllowance 순수 함수가 mealCalc.ts에 export됨
- /meal 라우트와 SideMenu 활성화 완료
</success_criteria>

<output>
After completion, create `.planning/phases/08-meal-records/08-01-SUMMARY.md`
</output>
