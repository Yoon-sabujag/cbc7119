---
phase: 04-completion-tracking-daily-reporting
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - cha-bio-safety/src/utils/dailyReportCalc.ts
  - cha-bio-safety/src/utils/generateExcel.ts
  - cha-bio-safety/src/pages/DailyReportPage.tsx
  - cha-bio-safety/src/utils/api.ts
autonomous: true
requirements: [EXCEL-06]

must_haves:
  truths:
    - "날짜를 선택하면 인원현황/금일업무/명일업무 미리보기가 표시된다"
    - "일별 다운로드 클릭 시 방재업무일지(dd일).xlsx 파일이 다운로드된다"
    - "월별 누적 다운로드 클릭 시 일일업무일지(mm월).xlsx 파일이 다운로드되고 오늘까지 시트만 포함된다"
    - "특이사항을 입력하고 저장하면 DB에 저장되고 다시 불러올 수 있다"
    - "공휴일/평일에 따라 순찰 시간이 다르게 기재된다"
  artifacts:
    - path: "cha-bio-safety/src/utils/dailyReportCalc.ts"
      provides: "Business logic for daily report data assembly"
      exports: ["buildDailyReportData", "DailyReportData"]
      min_lines: 80
    - path: "cha-bio-safety/src/utils/generateExcel.ts"
      provides: "generateDailyExcel function"
      contains: "generateDailyExcel"
    - path: "cha-bio-safety/src/pages/DailyReportPage.tsx"
      provides: "Daily report page UI"
      min_lines: 150
    - path: "cha-bio-safety/src/utils/api.ts"
      provides: "dailyReportApi namespace"
      contains: "dailyReportApi"
  key_links:
    - from: "cha-bio-safety/src/pages/DailyReportPage.tsx"
      to: "/api/daily-report"
      via: "dailyReportApi.getData(date) fetch"
      pattern: "dailyReportApi"
    - from: "cha-bio-safety/src/pages/DailyReportPage.tsx"
      to: "cha-bio-safety/src/utils/generateExcel.ts"
      via: "generateDailyExcel() call on download click"
      pattern: "generateDailyExcel"
    - from: "cha-bio-safety/src/utils/dailyReportCalc.ts"
      to: "cha-bio-safety/src/utils/shiftCalc.ts"
      via: "import getMonthlySchedule, getRawShift"
      pattern: "import.*shiftCalc"
    - from: "cha-bio-safety/src/utils/dailyReportCalc.ts"
      to: "@hyunbinseo/holidays-kr"
      via: "import isHoliday for patrol time determination"
      pattern: "import.*holidays-kr"
---

<objective>
Implement the complete daily operations log feature (EXCEL-06): business logic calculator, Excel generation, and full page UI with date navigation, mode toggle, notes input, preview, and download.

Purpose: Users can select a date, see a preview of the report data, save notes, and download the 방재업무일지 as an xlsx — either a single day or a monthly cumulative file.
Output: DailyReportPage.tsx, dailyReportCalc.ts, generateDailyExcel in generateExcel.ts, dailyReportApi in api.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-completion-tracking-daily-reporting/04-CONTEXT.md
@.planning/phases/04-completion-tracking-daily-reporting/04-RESEARCH.md
@.planning/phases/04-completion-tracking-daily-reporting/04-UI-SPEC.md
@.planning/phases/04-completion-tracking-daily-reporting/04-01-SUMMARY.md

<interfaces>
<!-- Contracts from Plan 01 that this plan consumes -->

From cha-bio-safety/functions/api/daily-report/index.ts (Plan 01 creates this):
```typescript
// GET /api/daily-report?date=YYYY-MM-DD
// Response: { success: true, data: { schedules: [...], leaves: [...], elevatorFaults: [...] } }
```

From cha-bio-safety/functions/api/daily-report/notes.ts (Plan 01 creates this):
```typescript
// GET /api/daily-report/notes?date=YYYY-MM-DD
// Response: { success: true, data: { id, date, content, created_by } | null }
// POST /api/daily-report/notes body: { date, content }
// Response: { success: true, data: { id, date, content } }
```

From cha-bio-safety/src/utils/shiftCalc.ts (existing):
```typescript
export function getShiftType(staffId: string, date: Date): ShiftType
export function getRawShift(staffId: string, date: Date): RawShift  // '당'|'비'|'주'|'휴'
export function getMonthlySchedule(year: number, month: number): {
  staffRows: { id: string; name: string; shifts: RawShift[] }[];
  ...
}
```

From cha-bio-safety/src/utils/generateExcel.ts (existing pattern):
```typescript
// fflate unzip-patch-rezip-download pattern
const { unzipSync, zipSync, strToU8, strFromU8 } = await import('fflate')
const res = await fetch('/templates/...xlsx')
const files = unzipSync(new Uint8Array(await res.arrayBuffer()))
// ... patch XML cells ...
// ... zipSync(newFiles) ...
// ... create blob and download link ...
```

From cha-bio-safety/src/utils/api.ts (existing API pattern):
```typescript
// Example: dashboardApi, scheduleApi, inspectionApi — all follow same namespace pattern
export const dashboardApi = {
  getStats: () => req<DashboardStatsResponse>('/dashboard/stats'),
}
```

From cha-bio-safety/src/pages/ReportsPage.tsx (existing page pattern — download button style):
```typescript
// Download button: width:'100%', padding:11, borderRadius:9, border:'none',
// background:'linear-gradient(135deg,#1d4ed8,#2563eb)', color:'#fff', fontSize:13, fontWeight:700
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: dailyReportCalc.ts + API client + generateDailyExcel</name>
  <files>
    cha-bio-safety/src/utils/dailyReportCalc.ts,
    cha-bio-safety/src/utils/generateExcel.ts,
    cha-bio-safety/src/utils/api.ts
  </files>
  <read_first>
    cha-bio-safety/src/utils/shiftCalc.ts,
    cha-bio-safety/src/utils/generateExcel.ts,
    cha-bio-safety/src/utils/api.ts,
    cha-bio-safety/src/types/index.ts,
    작업용/점검 일지 양식/일일업무일지(01월).xlsx,
    작업용/점검 일지 양식/점검 일지 작성법.md,
    .planning/phases/04-completion-tracking-daily-reporting/04-CONTEXT.md,
    .planning/phases/04-completion-tracking-daily-reporting/04-RESEARCH.md
  </read_first>
  <action>
    **CRITICAL FIRST STEP — Inspect template cell addresses:**
    Before writing any patchCell calls, unzip `public/templates/daily_report_template.xlsx` and inspect:
    1. `xl/worksheets/sheet1.xml` — identify cell addresses for each section (인원현황, 순찰, 금일업무, 명일업무, 특이사항, 날짜, 요일, 근무자 etc.)
    2. Check for `t="s"` (shared-string) cells at target addresses — if present, must use the shared-string-aware patch approach from Phase 3
    3. Read Sheet2 of the source `일일업무일지(01월).xlsx` for the cell position mapping reference
    4. Read Sheet3 (금일업무 조건 로직) and Sheet4 (명일업무 조건 로직) for business rule reference

    **Part A — Create `src/utils/dailyReportCalc.ts`** (per D-11, D-12, D-13, D-14):

    Export interfaces and main function:
    ```typescript
    export interface PersonnelStatus {
      total: number        // 총원 (always 4)
      present: number      // 현재원
      onDuty: string       // 당직자 이름
      offDuty: string      // 비번자 이름
      dayShift: string[]   // 주간근무자 이름 목록
      onLeave: string[]    // 연차자
      halfLeave: string[]  // 반차자
      training: string[]   // 교육/훈련자
      holiday: string[]    // 휴무자
      absent: number       // 결원
    }

    export interface PatrolEntry {
      type: '저녁순찰' | '야간순찰'
      time: string         // e.g., '22:00~23:00' or '01:00~02:00'
      patroller: string    // 순찰자 이름 (당직자)
    }

    export interface TaskEntry {
      number: number       // 동적 번호 (1, 2, 3, ...)
      content: string      // 업무 내용
    }

    export interface DailyReportData {
      date: string              // YYYY-MM-DD
      dayOfWeek: string         // 월, 화, ... (한글 요일)
      personnel: PersonnelStatus
      patrol: PatrolEntry[]     // 1-2 entries (저녁 and/or 야간)
      todayTasks: TaskEntry[]   // 금일업무 항목
      tomorrowTasks: TaskEntry[] // 명일업무 항목
      notes: string             // 특이사항
    }

    export function buildDailyReportData(
      date: string,
      apiData: { schedules: any[]; leaves: any[]; elevatorFaults: any[] },
      notes: string
    ): DailyReportData
    ```

    Implementation details for `buildDailyReportData`:

    1. **Personnel** (per D-13, D-14): Use `getRawShift(staffId, dateObj)` for each of the 4 staff members (STAFF array from shiftCalc.ts). Calculate:
       - 당직 = shift '당', 비번 = shift '비', 주간 = shift '주', 휴무 = shift '휴'
       - Cross-reference `leaves` from API for 연차/반차
       - Cross-reference `schedules` with category='event' and title containing '교육' or '훈련' for training
       - 현재원 = 총원 - 연차 - 결원
       - 결원 = anyone unaccounted for

    2. **Patrol** (per D-08, D-09, D-10):
       - Import `isHoliday` from `@hyunbinseo/holidays-kr` (wrap in try/catch per Pitfall 4)
       - Determine if date is holiday: Saturday/Sunday OR `isHoliday(dateObj)` returns true
       - 저녁순찰 time: 평일 22:00~23:00, 휴일 21:00~22:00
       - 야간순찰 time: always 01:00~02:00
       - Alternation logic (per D-08): Use REF_DATE pattern from shiftCalc.ts. Calculate `daysBetween(REF_DATE, date)`. Even = 저녁순찰 day, odd = 야간순찰 day (or vice versa — pick one and document).
       - Patroller = 당직자 name from personnel calculation

    3. **Today tasks / Tomorrow tasks** (per D-11, D-12):
       - Fixed items first: 일상점검 (매일 — numbers 1-3: 소방시설 외관점검, 비상구/피난통로 점검, 방재센터 장비 작동 확인)
       - Then conditional: 순찰 entry based on today's patrol assignment
       - Then from `schedules` API data: filter by category and add each as a task entry
       - Numbers are sequential starting from 1
       - Tomorrow tasks: same logic but for `date + 1 day`

    **Part B — Add `dailyReportApi` to `src/utils/api.ts`:**

    Add after existing API namespaces:
    ```typescript
    export const dailyReportApi = {
      getData: (date: string) => req<{ schedules: any[]; leaves: any[]; elevatorFaults: any[] }>(`/daily-report?date=${date}`),
      getNotes: (date: string) => req<{ id: string; date: string; content: string } | null>(`/daily-report/notes?date=${date}`),
      saveNotes: (date: string, content: string) => req<{ id: string; date: string; content: string }>('/daily-report/notes', { method: 'POST', body: JSON.stringify({ date, content }) }),
    }
    ```

    **Part C — Add `generateDailyExcel` to `src/utils/generateExcel.ts`:**

    Two modes (per D-01, D-02, D-03, D-07):

    ```typescript
    export async function generateDailyExcel(
      mode: 'daily' | 'monthly',
      year: number,
      month: number,
      dayDataMap: Record<number, DailyReportData>,  // day-of-month -> data
      todayDay: number  // for monthly: generate sheets 1..todayDay only
    )
    ```

    **Daily mode (per D-02):**
    - Fetch template, unzip, patch sheet1.xml with the single day's data
    - Use `patchCell()` (or shared-string-aware variant if template uses `t="s"`) for each cell
    - Cell addresses determined from template inspection step above
    - File name: `방재업무일지(${String(day).padStart(2,'0')}일).xlsx`

    **Monthly mode (per D-03, D-07):**
    - Fetch template, unzip, extract sheet1.xml as template
    - For each day from 1 to todayDay:
      - Clone the template XML
      - Strip printerSettings r:id (per Pitfall 1): `xml.replace(/(<pageSetup\b[^>]*?) r:id="[^"]*"/g, '$1')`
      - Patch with that day's data
      - Store as `xl/worksheets/dr${day}.xml`
    - Rebuild workbook.xml with N sheets (sheet names: "1일", "2일", ..., per D-07)
    - Rebuild workbook.xml.rels and [Content_Types].xml — follow exact pattern from `generateDivExcel` (lines ~209-237 in generateExcel.ts)
    - Copy styles.xml, sharedStrings.xml, theme1.xml unchanged
    - File name: `일일업무일지(${String(month).padStart(2,'0')}월).xlsx`

    **Cell patching function `patchDailySheet(xml, data)`:**
    Maps DailyReportData fields to specific cell addresses (determined from template inspection).
    Key sections to patch:
    - Header: date, day-of-week, year, month
    - Personnel: 총원, 현재원, 당직, 비번, 휴무, 연차, 반차, 교육, 결원, 주간근무자, 당직자
    - Patrol: type, time, patroller name
    - Today tasks: numbered list of task contents
    - Tomorrow tasks: numbered list of task contents
    - Notes: 특이사항 text
  </action>
  <verify>
    <automated>cd /Users/jykevin/Documents/20260328/cha-bio-safety && npx tsc --noEmit 2>&1 | head -20 && grep -q "buildDailyReportData" src/utils/dailyReportCalc.ts && echo "CALC OK" && grep -q "generateDailyExcel" src/utils/generateExcel.ts && echo "EXCEL OK" && grep -q "dailyReportApi" src/utils/api.ts && echo "API OK"</automated>
  </verify>
  <acceptance_criteria>
    - `src/utils/dailyReportCalc.ts` exists with `export function buildDailyReportData`
    - `src/utils/dailyReportCalc.ts` contains `import` from `shiftCalc`
    - `src/utils/dailyReportCalc.ts` contains `import` from `@hyunbinseo/holidays-kr`
    - `src/utils/dailyReportCalc.ts` contains `isHoliday` wrapped in try/catch
    - `src/utils/dailyReportCalc.ts` exports `DailyReportData` interface
    - `src/utils/generateExcel.ts` contains `export async function generateDailyExcel`
    - `src/utils/generateExcel.ts` contains `방재업무일지` in filename string
    - `src/utils/generateExcel.ts` contains `일일업무일지` in filename string
    - `src/utils/generateExcel.ts` contains printerSettings strip regex (`pageSetup.*r:id`)
    - `src/utils/api.ts` contains `dailyReportApi` with `getData`, `getNotes`, `saveNotes` methods
    - `npx tsc --noEmit` produces no errors
  </acceptance_criteria>
  <done>Business logic (dailyReportCalc.ts), Excel generation (generateDailyExcel), and API client (dailyReportApi) are complete and type-check clean</done>
</task>

<task type="auto">
  <name>Task 2: DailyReportPage.tsx full page UI</name>
  <files>
    cha-bio-safety/src/pages/DailyReportPage.tsx
  </files>
  <read_first>
    cha-bio-safety/src/pages/ReportsPage.tsx,
    cha-bio-safety/src/pages/DashboardPage.tsx,
    cha-bio-safety/src/components/ui/index.tsx,
    cha-bio-safety/src/utils/dailyReportCalc.ts,
    cha-bio-safety/src/utils/api.ts,
    cha-bio-safety/src/stores/authStore.ts,
    .planning/phases/04-completion-tracking-daily-reporting/04-UI-SPEC.md
  </read_first>
  <action>
    Create `src/pages/DailyReportPage.tsx` as a default-export functional component implementing the full UI-SPEC layout.

    **Page structure** (per D-04, UI-SPEC):

    ```typescript
    import { useState, useCallback } from 'react'
    import { useNavigate } from 'react-router-dom'
    import { useQuery, useMutation, useQueryClient } from '@tanstack/react-query'
    import toast from 'react-hot-toast'
    import { dailyReportApi } from '../utils/api'
    import { buildDailyReportData } from '../utils/dailyReportCalc'
    import { generateDailyExcel } from '../utils/generateExcel'
    ```

    **State:**
    - `date`: string (YYYY-MM-DD), default = today KST
    - `mode`: 'daily' | 'monthly', default = 'daily'
    - `notes`: string, local textarea value
    - `generating`: boolean, download in progress

    **Data fetching:**
    - `useQuery(['daily-report', date], () => dailyReportApi.getData(date))` for report data
    - `useQuery(['daily-notes', date], () => dailyReportApi.getNotes(date))` for notes
    - `useMutation` for `dailyReportApi.saveNotes(date, notes)` — on success: toast `특이사항이 저장되었습니다`, invalidate `['daily-notes', date]`

    **Header** (per UI-SPEC):
    - Back button: 34x34px, bg3, border bd, borderRadius 8, `onClick={() => navigate(-1)}`
    - Title: `일일업무일지`, fontSize 14, fontWeight 700, color var(--t1)
    - Date navigator: chevron buttons (28x28px) flanking date text (YYYY-MM-DD format, fontSize 13, fontWeight 700, width 90px fixed)
    - In 'daily' mode: chevrons navigate +-1 day, future dates disabled
    - In 'monthly' mode: chevrons navigate +-1 month, future months disabled
    - Date format displayed: 일별 = `YYYY-MM-DD`, 월별 = `YYYY년 MM월`

    **Mode toggle** (per UI-SPEC):
    - Two-segment pill: container bg3, borderRadius 10, padding 3px, display flex
    - Active segment: background var(--acl), color #fff, borderRadius 8, fontSize 13, fontWeight 700, padding 6px 16px
    - Inactive: background transparent, color var(--t2)
    - Labels: `일별` and `월별 누적`
    - Min touch width 88px per segment

    **특이사항 card** (per UI-SPEC, D-05):
    - Card: bg2, borderRadius 14, border 1px solid var(--bd), padding 14, marginBottom 10
    - Label: `특이사항`, fontSize 13, fontWeight 700
    - Textarea: bg3, border var(--bd), borderRadius 9, fontSize 13, padding 10px 12px, minHeight 80, resize none
    - Placeholder: `오늘 특이사항을 입력하세요`
    - Save button: right-aligned, fontSize 11, fontWeight 700, padding 6px 12px, borderRadius 7, bg4, color t2, border bd
    - On saving: button text = `저장 중...`, color t3
    - On notes query load: sync local `notes` state with fetched content

    **미리보기 card** (per UI-SPEC):
    - Card: same style as 특이사항 card
    - Label: `일일업무일지 미리보기`
    - Content: computed from `buildDailyReportData(date, apiData, notes)`
    - Show: 인원현황 summary line (총원 4 / 현재원 N / 당직 NAME / 비번 NAME)
    - Show: 금일업무 N건 / 명일업무 N건
    - Loading state: shimmer placeholder rows (bg4, borderRadius 4, height 12, width 60-80%, animation blink 2s ease-in-out infinite)
    - Error state: fontSize 11, color var(--danger), text `데이터 불러오기 실패 — 다시 시도해 주세요`

    **Download button** (per UI-SPEC):
    - Full width, padding 11, borderRadius 9, border none
    - Background: `linear-gradient(135deg,#1d4ed8,#2563eb)`, color #fff, fontSize 13, fontWeight 700
    - Label 일별: `다운로드 아이콘 방재업무일지(${dd}일) 다운로드`
    - Label 월별: `다운로드 아이콘 일일업무일지(${mm}월) 다운로드`
    - Loading: bg3, color t3, text `생성 중...`, disabled
    - On click: set generating=true, call generateDailyExcel with appropriate mode/data, set generating=false
    - On error: toast `엑셀 생성 중 오류가 발생했습니다`

    **Footer instruction** (per UI-SPEC):
    - fontSize 11, color t3, textAlign center, padding 8px 0 20px
    - 일별: `다운로드 후 엑셀에서 인쇄하세요`
    - 월별: `오늘까지의 날짜만 시트가 생성됩니다`

    **Style approach:** Inline styles using CSS variables (matching project convention — no Tailwind classes, no CSS modules). Follow exact patterns from ReportsPage.tsx and DashboardPage.tsx.
  </action>
  <verify>
    <automated>cd /Users/jykevin/Documents/20260328/cha-bio-safety && npx tsc --noEmit 2>&1 | head -20 && npm run build 2>&1 | tail -5 && grep -q "export default function DailyReportPage" src/pages/DailyReportPage.tsx && echo "PAGE OK"</automated>
  </verify>
  <acceptance_criteria>
    - `src/pages/DailyReportPage.tsx` contains `export default function DailyReportPage`
    - `src/pages/DailyReportPage.tsx` contains `useQuery` with key `daily-report`
    - `src/pages/DailyReportPage.tsx` contains `useMutation` for notes saving
    - `src/pages/DailyReportPage.tsx` contains `buildDailyReportData` import
    - `src/pages/DailyReportPage.tsx` contains `generateDailyExcel` import
    - `src/pages/DailyReportPage.tsx` contains `일별` and `월별 누적` toggle labels
    - `src/pages/DailyReportPage.tsx` contains `특이사항이 저장되었습니다` toast message
    - `src/pages/DailyReportPage.tsx` contains `방재업무일지` in download label
    - `src/pages/DailyReportPage.tsx` contains `linear-gradient(135deg,#1d4ed8,#2563eb)` download button style
    - `src/pages/DailyReportPage.tsx` contains `var(--bg2)` and `var(--t1)` CSS variable usage
    - `npm run build` succeeds without errors
  </acceptance_criteria>
  <done>DailyReportPage is fully functional: date navigation, mode toggle, notes input/save, data preview, and Excel download for both daily and monthly modes</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds
- `grep "buildDailyReportData" src/utils/dailyReportCalc.ts` — calc function exported
- `grep "generateDailyExcel" src/utils/generateExcel.ts` — Excel function exported
- `grep "dailyReportApi" src/utils/api.ts` — API client registered
- `grep "DailyReportPage" src/pages/DailyReportPage.tsx` — page exists
- All user-visible strings are in Korean
</verification>

<success_criteria>
EXCEL-06 is fully implemented: user can navigate to /daily-report, select a date, see a preview of report data, save notes, and download either a single-day or monthly cumulative Excel file with auto-filled personnel, patrol, tasks, and notes.
</success_criteria>

<output>
After completion, create `.planning/phases/04-completion-tracking-daily-reporting/04-02-SUMMARY.md`
</output>
