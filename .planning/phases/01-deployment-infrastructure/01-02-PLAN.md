---
phase: 01-deployment-infrastructure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/deploy.yml
autonomous: false
requirements:
  - DEPLOY-01
  - DEPLOY-02
  - DEPLOY-03

user_setup:
  - service: github
    why: "GitHub remote repository must exist for Actions CI/CD to run. Currently the repo is local-only with 89 commits."
    dashboard_config:
      - task: "Create a new GitHub repository (public or private) named 'cha-bio-safety' or similar"
        location: "https://github.com/new"
      - task: "After Claude pushes the local commits, add two repository secrets"
        location: "GitHub repo → Settings → Secrets and variables → Actions → New repository secret"
    env_vars:
      - name: CLOUDFLARE_API_TOKEN
        source: "Cloudflare Dashboard → My Profile → API Tokens → Create Token → Use 'Edit Cloudflare Workers' template, then add D1:Edit permission for database 'cha-bio-db'"
      - name: CLOUDFLARE_ACCOUNT_ID
        source: "Cloudflare Dashboard → right sidebar on any page, or Workers & Pages → Overview URL contains the account ID"

must_haves:
  truths:
    - "A GitHub remote repository exists and the local 89-commit history is pushed to it"
    - ".github/workflows/deploy.yml exists in the repo and triggers on push to main"
    - "The workflow runs: npm ci → npm run build → wrangler d1 migrations apply --remote --yes → wrangler pages deploy dist"
    - "After the first successful CI run, https://cbc7119.pages.dev reflects the latest build"
  artifacts:
    - path: ".github/workflows/deploy.yml"
      provides: "GitHub Actions CI/CD workflow for Cloudflare Pages deployment"
      contains: "cloudflare/wrangler-action@v3"
    - path: ".github/workflows/deploy.yml"
      provides: "D1 migration step"
      contains: "d1 migrations apply cha-bio-db --remote"
  key_links:
    - from: ".github/workflows/deploy.yml"
      to: "cha-bio-safety/dist/"
      via: "npm run build in CI creates dist/ which is deployed by wrangler pages deploy"
      pattern: "npm run build.*pages deploy dist"
    - from: ".github/workflows/deploy.yml"
      to: "Cloudflare Pages project 'cbc7119'"
      via: "cloudflare/wrangler-action@v3 with CLOUDFLARE_API_TOKEN secret"
      pattern: "wrangler-action@v3.*pages deploy.*cbc7119"
---

<objective>
Create the GitHub remote repository, push the existing 89-commit history, and add the GitHub Actions CI/CD workflow that automatically builds and deploys to Cloudflare Pages on every push to main.

Purpose: Per D-04/D-06/D-07, GitHub Actions CI/CD is the target pipeline. It replaces the manual deploy.sh workflow for production deploys while keeping deploy.sh for local convenience. The CI workflow also integrates D1 migration application (D-07), making every deploy automatically migrate the remote database.

Output:
- .github/workflows/deploy.yml — new GitHub Actions workflow file
- GitHub remote repository with 89 commits + deploy.yml pushed
- Two GitHub repository secrets configured (requires human action — Claude cannot set secrets)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-deployment-infrastructure/01-CONTEXT.md
@.planning/phases/01-deployment-infrastructure/01-RESEARCH.md

<interfaces>
<!-- Contracts the executor needs for CI workflow construction. -->

From cha-bio-safety/wrangler.toml:
```toml
name = "cbc7119"                   # Pages project name — used in: pages deploy dist --project-name=cbc7119
compatibility_date = "2024-09-23"
pages_build_output_dir = "dist"

[[d1_databases]]
database_name = "cha-bio-db"       # D1 DB name — used in: d1 migrations apply cha-bio-db --remote
database_id = "b12b88e7-fc41-4186-8f35-ee9cbaf994c7"
```

From cha-bio-safety/package.json:
```json
"scripts": {
  "build": "tsc && vite build",    # This is the build command CI must run
}
```
lockfile: package-lock.json present → use `npm ci` (not npm install) in CI

Project structure: monorepo layout
- Repo root: /Users/jykevin/Documents/20260328/  (contains .planning/ and cha-bio-safety/)
- cha-bio-safety/ is the app subdirectory
- wrangler.toml is inside cha-bio-safety/
- ALL wrangler-action steps MUST set: workingDirectory: cha-bio-safety

Critical pitfalls from RESEARCH.md:
- Use cloudflare/wrangler-action@v3 (NOT cloudflare/pages-action — deprecated)
- Use wrangler d1 migrations apply (NOT wrangler d1 execute — no tracking)
- Pass --yes to avoid interactive confirmation prompt hanging CI
- Set workingDirectory: cha-bio-safety on ALL wrangler-action steps
- Set working-directory: cha-bio-safety on npm ci and npm run build steps
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create GitHub Actions deploy workflow</name>
  <files>.github/workflows/deploy.yml</files>
  <read_first>
    - .planning/phases/01-deployment-infrastructure/01-RESEARCH.md — read Pattern 1 (GitHub Actions Deployment Workflow) for the complete workflow template and all pitfall notes
    - cha-bio-safety/wrangler.toml — confirm database_name ("cha-bio-db") and project name ("cbc7119") before writing workflow
    - cha-bio-safety/package.json — confirm lockfile is package-lock.json (use npm ci, not yarn)
  </read_first>
  <action>
Create the directory `.github/workflows/` (relative to repo root `/Users/jykevin/Documents/20260328/`) and write the file `.github/workflows/deploy.yml` with this exact content:

```yaml
# .github/workflows/deploy.yml
# Deploys cha-bio-safety to Cloudflare Pages on every push to main.
# Applies D1 migrations before deploying to keep remote schema in sync.
#
# Required GitHub Secrets (Settings → Secrets and variables → Actions):
#   CLOUDFLARE_API_TOKEN  — Cloudflare API token (Pages:Edit + D1:Edit permissions)
#   CLOUDFLARE_ACCOUNT_ID — Cloudflare account ID (visible in dashboard sidebar)

name: Deploy

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      deployments: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: cha-bio-safety/package-lock.json

      - name: Install dependencies
        run: npm ci
        working-directory: cha-bio-safety

      - name: Build
        run: npm run build
        working-directory: cha-bio-safety

      - name: Apply D1 migrations (remote)
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          workingDirectory: cha-bio-safety
          command: d1 migrations apply cha-bio-db --remote --yes

      - name: Deploy to Cloudflare Pages
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          workingDirectory: cha-bio-safety
          command: pages deploy dist --project-name=cbc7119
          gitHubToken: ${{ secrets.GITHUB_TOKEN }}
```

Key implementation notes:
- `cache-dependency-path: cha-bio-safety/package-lock.json` — path is relative to repo root, not working-directory
- D1 migration step uses `migrations apply` (not `execute`) so already-applied migrations are skipped
- `--yes` flag prevents interactive confirmation prompt from hanging CI
- Migration step runs BEFORE Pages deploy so schema is ready when new code is live
- `gitHubToken` on the deploy step enables GitHub Deployments status tracking
  </action>
  <verify>
    <automated>cat /Users/jykevin/Documents/20260328/.github/workflows/deploy.yml</automated>
  </verify>
  <acceptance_criteria>
    - test -f /Users/jykevin/Documents/20260328/.github/workflows/deploy.yml exits 0
    - grep "wrangler-action@v3" /Users/jykevin/Documents/20260328/.github/workflows/deploy.yml returns 2 lines (two steps use it)
    - grep "d1 migrations apply cha-bio-db --remote --yes" /Users/jykevin/Documents/20260328/.github/workflows/deploy.yml exits 0
    - grep "pages deploy dist --project-name=cbc7119" /Users/jykevin/Documents/20260328/.github/workflows/deploy.yml exits 0
    - grep "workingDirectory: cha-bio-safety" /Users/jykevin/Documents/20260328/.github/workflows/deploy.yml returns 2 lines (both wrangler steps)
    - grep "working-directory: cha-bio-safety" /Users/jykevin/Documents/20260328/.github/workflows/deploy.yml returns 2 lines (npm ci and npm run build steps)
    - grep "CLOUDFLARE_API_TOKEN" /Users/jykevin/Documents/20260328/.github/workflows/deploy.yml exits 0
    - grep "CLOUDFLARE_ACCOUNT_ID" /Users/jykevin/Documents/20260328/.github/workflows/deploy.yml exits 0
    - grep "pages-action" /Users/jykevin/Documents/20260328/.github/workflows/deploy.yml returns nothing (deprecated action must NOT be used)
  </acceptance_criteria>
  <done>.github/workflows/deploy.yml created with two-step wrangler pipeline (migrations then pages deploy), correct workingDirectory, and --yes flag</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Create GitHub remote, push commits, add secrets</name>
  <what-built>Claude created .github/workflows/deploy.yml. The CI workflow is ready but cannot run until: (1) a GitHub remote repository exists, (2) the 89 local commits are pushed, and (3) two repository secrets are set. These steps require human action.</what-built>
  <how-to-verify>
    Complete these steps in order:

    **Step 1 — Create GitHub repository:**
    1. Visit https://github.com/new
    2. Repository name: cha-bio-safety (or any name you prefer)
    3. Visibility: Private recommended (contains production config)
    4. Do NOT initialize with README, .gitignore, or license (local repo already has history)
    5. Click "Create repository"
    6. Copy the remote URL shown (e.g., https://github.com/YOUR_USERNAME/cha-bio-safety.git)

    **Step 2 — Push local commits to GitHub:**
    Run these commands from /Users/jykevin/Documents/20260328/:
    ```bash
    git remote add origin https://github.com/YOUR_USERNAME/cha-bio-safety.git
    git branch -M main
    git push -u origin main
    ```
    Expected: "89 commits pushed" or similar output. The push also triggers the first CI run.

    **Step 3 — Get Cloudflare credentials:**
    - CLOUDFLARE_ACCOUNT_ID: Visit https://dash.cloudflare.com → right sidebar shows "Account ID"
    - CLOUDFLARE_API_TOKEN: Visit https://dash.cloudflare.com/profile/api-tokens → "Create Token" → Use "Edit Cloudflare Workers" template → Add "D1:Edit" permission for database "cha-bio-db" → Create Token

    **Step 4 — Add GitHub repository secrets:**
    1. Go to your new GitHub repo → Settings → Secrets and variables → Actions
    2. Click "New repository secret"
    3. Add: Name=CLOUDFLARE_API_TOKEN, Value=(token from step 3)
    4. Add: Name=CLOUDFLARE_ACCOUNT_ID, Value=(account ID from step 3)

    **Step 5 — Verify CI ran successfully:**
    1. Go to your GitHub repo → Actions tab
    2. Find the "Deploy" workflow run triggered by the push
    3. Confirm all steps are green (Checkout → Setup Node → Install → Build → Apply D1 migrations → Deploy)
    4. Visit https://cbc7119.pages.dev to confirm the latest build is live

    Note: The first CI run may fail at "Apply D1 migrations" if the d1_migrations tracking table doesn't exist yet in the remote DB. If this happens, Plan 03 (D1 verification) will diagnose and fix it.
  </how-to-verify>
  <resume-signal>Type "ci-green" if all 5 steps completed successfully. Type "ci-failed" followed by the failing step name and error message if something went wrong (e.g., "ci-failed Apply D1 migrations — table not found").</resume-signal>
</task>

</tasks>

<verification>
After CI runs successfully:

```bash
# Verify workflow file exists and is syntactically correct
cat /Users/jykevin/Documents/20260328/.github/workflows/deploy.yml

# Verify live site responds
curl -s -o /dev/null -w "%{http_code}" https://cbc7119.pages.dev/
# Expected: 200
```
</verification>

<success_criteria>
- .github/workflows/deploy.yml exists with correct wrangler-action@v3 pipeline
- GitHub remote repository created and 89 commits pushed
- CLOUDFLARE_API_TOKEN and CLOUDFLARE_ACCOUNT_ID secrets set in GitHub repo
- First GitHub Actions "Deploy" workflow run completes green
- https://cbc7119.pages.dev responds with 200 and shows the latest build
</success_criteria>

<output>
After completion, create `.planning/phases/01-deployment-infrastructure/01-02-SUMMARY.md`
</output>
