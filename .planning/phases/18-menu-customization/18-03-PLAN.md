---
phase: 18
plan: 03
type: execute
wave: 3
depends_on: [18-02]
files_modified:
  - cha-bio-safety/src/components/SettingsPanel.tsx
autonomous: false
requirements: [MENU-01, MENU-02]
must_haves:
  truths:
    - "SettingsPanel renders <MenuSettingsSection /> below the 알림 section and above the 화면 section"
    - "Production deploy succeeds and the editor is reachable from the live settings panel"
    - "Saving the menu config persists; SideMenu reflects the new layout after reload"
    - "Default user (no saved config) sees the divider-grouped DEFAULT_SIDE_MENU layout"
  artifacts:
    - path: "cha-bio-safety/src/components/SettingsPanel.tsx"
      provides: "SettingsPanel mounting MenuSettingsSection"
  key_links:
    - from: "cha-bio-safety/src/components/SettingsPanel.tsx"
      to: "cha-bio-safety/src/components/MenuSettingsSection.tsx"
      via: "import + JSX mount"
      pattern: "MenuSettingsSection"
---

<objective>
Mount `<MenuSettingsSection />` inside SettingsPanel, deploy to production with `--branch production`, and visually verify the divider-model editor end-to-end.

Purpose: Ship Phase 18.
Output: Deployed app where users can edit SideMenu via settings panel and the changes persist + reflect in SideMenu.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-menu-customization/18-CONTEXT.md
@.planning/phases/18-menu-customization/18-UI-SPEC.md
@cha-bio-safety/src/components/SettingsPanel.tsx
@cha-bio-safety/src/components/MenuSettingsSection.tsx
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Mount MenuSettingsSection in SettingsPanel between 알림 and 화면 sections</name>
  <files>cha-bio-safety/src/components/SettingsPanel.tsx</files>
  <read_first>
    - cha-bio-safety/src/components/SettingsPanel.tsx (entire file, focus on lines 311-365 — 알림 and 화면 sections)
    - cha-bio-safety/src/components/MenuSettingsSection.tsx (Plan 02 output)
  </read_first>
  <action>
1. Add this import at the top of `cha-bio-safety/src/components/SettingsPanel.tsx`, alongside the other component imports:
   ```typescript
   import { MenuSettingsSection } from './MenuSettingsSection'
   ```

2. Locate the JSX block that ends the 알림 (notifications) section. It is the closing `</div>` immediately after the `<Row label="교육 D-30 알림" ...>` block (around line 348-349 in the current file). Right after that closing `</div>` and BEFORE the `{/* 화면 */}` comment block, insert:
   ```jsx
   {/* 메뉴 설정 (Phase 18) */}
   <MenuSettingsSection />
   ```

3. No other changes to SettingsPanel.tsx — preserve all existing sections, props, structure.
  </action>
  <verify>
    <automated>cd cha-bio-safety && grep -n "MenuSettingsSection" src/components/SettingsPanel.tsx && npx tsc --noEmit 2>&1 | grep "SettingsPanel" || echo "TS-OK"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "import.*MenuSettingsSection.*MenuSettingsSection" cha-bio-safety/src/components/SettingsPanel.tsx` returns 1
    - `grep -c "<MenuSettingsSection" cha-bio-safety/src/components/SettingsPanel.tsx` returns 1
    - The `<MenuSettingsSection />` line appears AFTER the line containing `'education_reminder'` and BEFORE the line containing `{/* 화면 */}`
    - `npx tsc --noEmit` produces no errors in SettingsPanel.tsx
  </acceptance_criteria>
  <done>
    SettingsPanel mounts MenuSettingsSection in the correct position. tsc passes.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Build + deploy to Cloudflare Pages production</name>
  <files>(no source files — deploy only)</files>
  <read_first>
    - cha-bio-safety/package.json (deploy script)
    - .planning/STATE.md (decision: deploy project name is cbc7119; --branch production required)
  </read_first>
  <action>
Run from `/Users/jykevin/Documents/20260328/cha-bio-safety`:

```bash
npm run build
npx wrangler pages deploy dist --project-name=cbc7119 --branch=production
```

Capture the deploy URL from the wrangler output. If build fails, fix tsc errors and retry. Do NOT skip the `--branch=production` flag — without it the deploy goes to Preview.
  </action>
  <verify>
    <automated>cd cha-bio-safety && npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0
    - wrangler deploy output contains "Deployment complete" and a `*.pages.dev` URL on the production branch
  </acceptance_criteria>
  <done>
    Production build deployed successfully with --branch=production.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification of divider-model menu editor on production</name>
  <what-built>
    SideMenu is now read-only (no edit button). SettingsPanel contains a new "메뉴 설정" section with the full divider-model editor: move arrows, visibility toggles, divider rename/add/delete, reset, and explicit "설정 저장" button.
  </what-built>
  <how-to-verify>
    1. Open the production URL on mobile (or PWA installed).
    2. Open the settings panel (gear icon). Confirm a new "메뉴 설정" section appears below the 알림 section and above the 화면 section.
    3. Confirm the editor lists all current SideMenu entries as a flat list with section dividers (대시보드, 일반 점검, etc.) visually distinct from item rows (left border).
    4. Tap a divider title — it should become an input. Type a new name, tap outside — title should update.
    5. Tap "+ 구분선 추가" — a new "새 구분선" appears at the bottom in edit mode.
    6. Tap up/down arrows on any entry — order should change. First entry's up arrow and last entry's down arrow should be dimmed/inactive.
    7. Tap a visibility toggle on an item — it should dim and show "숨김" badge.
    8. Tap the trash icon on a divider — inline "삭제할까요? [취소] [삭제]" appears. Tap [삭제] — divider is removed.
    9. Tap "설정 저장" — toast "메뉴 설정이 저장되었습니다" appears.
    10. Open the SideMenu (hamburger) — verify the new layout/order/visibility matches your edits, and there is NO "메뉴 편집" button at the bottom.
    11. Hard-reload the page — verify changes persist.
    12. Tap "기본값으로 초기화" → "초기화" — list snaps back to default divider layout. Save and verify SideMenu reflects defaults.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- All 12 manual verification steps pass on production
- No console errors in browser devtools while editing
- Existing settings sections (알림, 화면, 계정, 로그아웃) still work
</verification>

<success_criteria>
1. SettingsPanel mounts MenuSettingsSection in correct position
2. Production deploy succeeds with --branch=production
3. All 12 human verification steps pass
4. SideMenu has zero edit affordances after this phase
</success_criteria>

<output>
After completion, create `.planning/phases/18-menu-customization/18-03-SUMMARY.md`
</output>
