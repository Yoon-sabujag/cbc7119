---
phase: 24-extinguisher-asset-location
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cha-bio-safety/migrations/0079_extinguisher_asset_split.sql
autonomous: false
requirements:
  - EXT-04
tags:
  - migration
  - d1
  - extinguisher
must_haves:
  truths:
    - "운영 D1 의 extinguishers 테이블에 status 컬럼이 존재하고 모든 기존 행이 'active' 값을 가진다"
    - "운영 D1 의 check_records 테이블에 extinguisher_id 컬럼이 존재한다"
    - "기존 extinguishers.check_point_id NOT NULL 매핑이 마이그레이션 전후로 동일하게 보존된다 (1:1 데이터 무손실)"
    - "소화기 카테고리(checkpoint_id LIKE 'CP-FE-%') 점검 기록의 extinguisher_id NULL 비율이 5% 이하이다 (백필 정책 A 적용)"
    - "신규 인덱스 idx_extinguishers_status, idx_extinguishers_cp_active, idx_check_records_ext 가 존재한다"
  artifacts:
    - path: "cha-bio-safety/migrations/0079_extinguisher_asset_split.sql"
      provides: "ALTER TABLE 으로 status / extinguisher_id 컬럼 추가 + 인덱스 + 백필 SQL"
      contains: "ALTER TABLE extinguishers ADD COLUMN status"
    - path: ".planning/phases/24-extinguisher-asset-location/24-01-PRAGMA.md"
      provides: "운영 D1 의 PRAGMA table_info 출력 baseline (마이그레이션 전후 비교용)"
      contains: "PRAGMA table_info"
  key_links:
    - from: "migrations/0079_extinguisher_asset_split.sql"
      to: "운영 D1 (cha-bio-db)"
      via: "wrangler d1 execute --remote --file=...0079..."
      pattern: "wrangler d1 execute"
---

<objective>
Phase 24 Wave 1: 마이그레이션 0079 작성 + 적용. extinguishers 테이블에 status 컬럼, check_records 테이블에 extinguisher_id 컬럼을 추가하고, 기존 1:1 매핑을 백필로 채워 점검 기록에 자산 스냅샷이 남도록 한다.

Purpose: 5월 법정점검 시 분말 소화기 10년 연한 도래 항목들을 폐기 + 신규 등록 흐름으로 처리할 때 위치 이력이 끊기지 않도록 데이터 모델의 기반을 다진다 (EXT-04).
Output: 운영 D1 에 적용 완료된 컬럼 + 인덱스 + 백필 데이터.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/24-extinguisher-asset-location/CONTEXT.md
@.planning/phases/24-extinguisher-asset-location/24-RESEARCH.md
@.planning/phases/24-extinguisher-asset-location/24-PATTERNS.md
@.planning/phases/24-extinguisher-asset-location/24-VALIDATION.md
@cha-bio-safety/CLAUDE.md
@cha-bio-safety/migrations/0035_extinguishers.sql
@cha-bio-safety/migrations/0078_marker_description_sync.sql
@cha-bio-safety/migrations/0072_floor_plan_markers_description.sql

<interfaces>
<!-- Existing schema (already in production D1, verified via 0035): -->

extinguishers (id INTEGER PRIMARY KEY AUTOINCREMENT,
               check_point_id TEXT NOT NULL REFERENCES check_points(id),
               seq_no INTEGER, zone TEXT, floor TEXT, mgmt_no TEXT, location TEXT,
               type TEXT NOT NULL, approval_no TEXT, manufactured_at TEXT,
               manufacturer TEXT, prefix_code TEXT, seal_no TEXT, serial_no TEXT,
               note TEXT, created_at TEXT, updated_at TEXT)

check_records (id, session_id, checkpoint_id, staff_id, result, memo, photo_key,
               guide_light_type, floor_plan_marker_id, checked_at, created_at, ...)
  // 일부 컬럼은 미문서화 ALTER 로 추가됨 — 마이그레이션 작성 전 PRAGMA 확인 필수.

<!-- Soft-delete convention: status='폐기' (Korean) per CONTEXT D-decisions. -->
<!-- Hard-delete: extinguishers row 자체 DELETE (미매핑+미점검 한정 — Plan 02 가드). -->
<!-- check_records 는 어떤 분기에서도 절대 DELETE 금지 (floorplan-markers/[id].ts:48). -->
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: 운영 D1 PRAGMA 확보 (사용자 실행 + 결과 캡쳐)</name>
  <files>.planning/phases/24-extinguisher-asset-location/24-01-PRAGMA.md (new)</files>
  <read_first>
    - .planning/phases/24-extinguisher-asset-location/24-RESEARCH.md (§Pitfall 1 — 미문서화 컬럼 충돌)
    - .planning/phases/24-extinguisher-asset-location/24-VALIDATION.md (Wave 0 Requirements)
    - cha-bio-safety/migrations/0035_extinguishers.sql (현행 스키마 파일)
  </read_first>
  <what-built>본 task 는 마이그레이션 SQL 작성 *전* 운영 D1 의 실제 컬럼 상태를 확정하는 사전 점검. SQLite 가 ADD COLUMN IF NOT EXISTS 미지원이므로 이미 status / extinguisher_id 가 존재할 가능성을 사전 체크.</what-built>
  <how-to-verify>
다음 4개 명령을 cha-bio-safety/ 디렉토리에서 순서대로 실행하고, 출력을 `.planning/phases/24-extinguisher-asset-location/24-01-PRAGMA.md` 에 캡쳐:

```bash
cd cha-bio-safety
npx wrangler d1 execute cha-bio-db --remote --command "PRAGMA table_info(extinguishers);"
npx wrangler d1 execute cha-bio-db --remote --command "PRAGMA table_info(check_records);"
npx wrangler d1 execute cha-bio-db --remote --command "SELECT COUNT(*) AS total FROM extinguishers;"
npx wrangler d1 execute cha-bio-db --remote --command "SELECT COUNT(*) AS total, SUM(CASE WHEN check_point_id IS NULL THEN 1 ELSE 0 END) AS unmapped FROM extinguishers;"
```

24-01-PRAGMA.md 형식 (executor 가 작성):

```markdown
# Phase 24 Plan 01 — Pre-Migration PRAGMA Capture
**Captured at:** YYYY-MM-DD HH:MM KST

## extinguishers table
| cid | name | type | notnull | dflt_value | pk |
|...| (paste output) |

## check_records table
| ... |

## Row counts
- extinguishers total: N
- extinguishers unmapped (check_point_id IS NULL): M

## Decision
- [ ] `extinguishers.status` 컬럼 부재 확인 → ADD COLUMN 진행
- [ ] `check_records.extinguisher_id` 컬럼 부재 확인 → ADD COLUMN 진행
- (또는) [ ] 이미 존재 → ADD COLUMN 스킵, 인덱스+백필만 수행
```
  </how-to-verify>
  <resume-signal>24-01-PRAGMA.md 작성 완료 + 사용자 "approved" 또는 결과 공유</resume-signal>
  <acceptance_criteria>
    - `.planning/phases/24-extinguisher-asset-location/24-01-PRAGMA.md` 파일이 존재한다
    - PRAGMA 출력의 컬럼명 목록에서 `status` 부재(또는 존재) 가 명시되어 있다 (grep `status` 24-01-PRAGMA.md → 결정 라인 1줄 매치)
    - PRAGMA 출력의 컬럼명 목록에서 `extinguisher_id` 부재(또는 존재) 가 명시되어 있다 (grep `extinguisher_id` 24-01-PRAGMA.md → 결정 라인 1줄 매치)
    - row count 가 양수임 (extinguishers 총 행 수가 0 이 아님)
  </acceptance_criteria>
  <action>(human-action) 사용자가 cha-bio-safety/ 에서 4개 wrangler PRAGMA + COUNT 쿼리를 순서대로 실행하고, 출력을 24-01-PRAGMA.md 에 캡쳐한다. 결과에서 status / extinguisher_id 컬럼 부재(또는 존재) 를 결정 라인으로 명시.</action>
  <verify>
    <automated>test -f .planning/phases/24-extinguisher-asset-location/24-01-PRAGMA.md && grep -c "status\|extinguisher_id" .planning/phases/24-extinguisher-asset-location/24-01-PRAGMA.md</automated>
  </verify>
  <done>24-01-PRAGMA.md 가 존재하며 status / extinguisher_id 결정 라인 + row count 가 기록됨.</done>
</task>

<task type="auto">
  <name>Task 2: 마이그레이션 0079 SQL 작성</name>
  <files>cha-bio-safety/migrations/0079_extinguisher_asset_split.sql (new)</files>
  <read_first>
    - .planning/phases/24-extinguisher-asset-location/24-01-PRAGMA.md (Task 1 결과 — ADD COLUMN 필요 여부 결정)
    - .planning/phases/24-extinguisher-asset-location/24-PATTERNS.md (§ migrations/0079_extinguisher_asset_split.sql 패턴)
    - cha-bio-safety/migrations/0078_marker_description_sync.sql (헤더 + ALTER+UPDATE 단일 파일 패턴)
    - cha-bio-safety/migrations/0072_floor_plan_markers_description.sql (단순 ALTER 패턴)
    - cha-bio-safety/migrations/0035_extinguishers.sql (인덱스 네이밍 컨벤션 idx_extinguishers_*)
    - cha-bio-safety/migrations/0073_add_telemetry_events.sql (CREATE INDEX IF NOT EXISTS 패턴)
  </read_first>
  <action>
파일 `cha-bio-safety/migrations/0079_extinguisher_asset_split.sql` 을 다음과 같이 정확히 작성. **Task 1 의 PRAGMA 결과에서 status / extinguisher_id 가 이미 존재하면 해당 ALTER 라인을 주석 처리 + 이유 명시 주석 추가.** 그 외에는 그대로 작성.

```sql
-- Migration 0079: 소화기 자산-위치 분리
--
-- 1) extinguishers.status — 'active' / '폐기' (default 'active')
-- 2) check_records.extinguisher_id — 점검 시점 자산 스냅샷 (nullable)
-- 3) 인덱스 보강 (status 필터, cp+status JOIN, ext_id 역참조)
-- 4) 백필 — 정책 A: 소화기 카테고리(checkpoint_id LIKE 'CP-FE-%') 점검 기록에
--    현재 매핑된 ext.id 를 추정 채움 (1:1 시점이라 안전)
--
-- 절대 금지: check_records 는 어떤 분기에서도 DELETE 하지 않는다 (점검 기록 보존 원칙).
-- 본 마이그레이션은 ALTER + UPDATE 만 — DROP/DELETE 없음.

-- ── 1) extinguishers.status ─────────────────────────────
ALTER TABLE extinguishers ADD COLUMN status TEXT NOT NULL DEFAULT 'active';

-- ── 2) check_records.extinguisher_id ────────────────────
ALTER TABLE check_records ADD COLUMN extinguisher_id INTEGER;

-- ── 3) 인덱스 ────────────────────────────────────────────
CREATE INDEX IF NOT EXISTS idx_extinguishers_status     ON extinguishers(status);
CREATE INDEX IF NOT EXISTS idx_extinguishers_cp_active  ON extinguishers(check_point_id, status);
CREATE INDEX IF NOT EXISTS idx_check_records_ext        ON check_records(extinguisher_id);

-- ── 4) 백필 (정책 A — 현재 매핑된 ext_id 추정) ───────────
-- 1:1 매핑 시점이므로 cp_id 당 active ext.id 가 unique → 안전.
-- 소화기 카테고리만 채움 (checkpoint_id LIKE 'CP-FE-%').
UPDATE check_records
SET extinguisher_id = (
  SELECT e.id FROM extinguishers e
  WHERE e.check_point_id = check_records.checkpoint_id
  LIMIT 1
)
WHERE extinguisher_id IS NULL
  AND checkpoint_id LIKE 'CP-FE-%';
```

**주의:**
- D1 / SQLite 의 `ALTER TABLE ... ADD COLUMN` 은 IF NOT EXISTS 미지원 — Task 1 PRAGMA 가 부재 확인을 보장한 경우만 위 SQL 그대로 적용.
- 만약 PRAGMA 에서 `status` 또는 `extinguisher_id` 가 이미 있다면, 해당 ALTER 라인 1줄을 `-- SKIPPED: already exists in prod (see 24-01-PRAGMA.md)` 로 교체.
- KST 컨벤션 적용 X (DDL 만 — UPDATE 는 데이터 백필이라 timestamp 컬럼 안 건드림).
- `IF NOT EXISTS` 는 인덱스에만 사용 가능 (SQLite 지원).
  </action>
  <verify>
    <automated>cd cha-bio-safety && grep -c "ALTER TABLE extinguishers ADD COLUMN status" migrations/0079_extinguisher_asset_split.sql && grep -c "ALTER TABLE check_records ADD COLUMN extinguisher_id" migrations/0079_extinguisher_asset_split.sql && grep -c "idx_extinguishers_status" migrations/0079_extinguisher_asset_split.sql && grep -c "idx_check_records_ext" migrations/0079_extinguisher_asset_split.sql && grep -c "WHERE extinguisher_id IS NULL" migrations/0079_extinguisher_asset_split.sql</automated>
  </verify>
  <acceptance_criteria>
    - `cha-bio-safety/migrations/0079_extinguisher_asset_split.sql` 파일 존재
    - `grep -c "ALTER TABLE extinguishers ADD COLUMN status TEXT NOT NULL DEFAULT 'active'" cha-bio-safety/migrations/0079_extinguisher_asset_split.sql` ≥ 1 (또는 PRAGMA 가 이미 있다고 했으면 SKIPPED 주석)
    - `grep -c "ALTER TABLE check_records ADD COLUMN extinguisher_id INTEGER" cha-bio-safety/migrations/0079_extinguisher_asset_split.sql` ≥ 1 (또는 SKIPPED 주석)
    - 3개 신규 인덱스 라인 존재: `idx_extinguishers_status`, `idx_extinguishers_cp_active`, `idx_check_records_ext` (각 1+ 매치)
    - 백필 UPDATE 의 `WHERE checkpoint_id LIKE 'CP-FE-%'` 라인 존재
    - 파일 헤더에 "Migration 0079: 소화기 자산-위치 분리" 정확히 포함
    - DROP / DELETE 키워드 없음 (`grep -E '^[[:space:]]*(DROP|DELETE)[[:space:]]' cha-bio-safety/migrations/0079_extinguisher_asset_split.sql | grep -v '^--' | wc -l` = 0)
  </acceptance_criteria>
  <done>0079 SQL 파일이 작성되어 grep gate 통과 + PRAGMA 결과 반영됨.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: 마이그레이션 적용 + 사후 검증 (사용자 실행)</name>
  <files>(no file edits — wrangler 실행만)</files>
  <read_first>
    - cha-bio-safety/migrations/0079_extinguisher_asset_split.sql (Task 2 산출물)
    - .planning/phases/24-extinguisher-asset-location/24-01-PRAGMA.md (baseline)
    - cha-bio-safety/CLAUDE.md (배포 시 --remote 룰)
    - .planning/STATE.md ("배포 시 --branch production 필수" 메모리 룰)
  </read_first>
  <what-built>Task 2 의 SQL 을 운영 D1 에 적용. SQLite/D1 은 단일 ALTER 실패 시 파일 전체 롤백되지 않을 수 있으므로 적용 후 즉시 PRAGMA + COUNT 로 사후 검증 필수.</what-built>
  <how-to-verify>
사용자가 cha-bio-safety/ 디렉토리에서 다음 명령을 순서대로 실행:

```bash
cd cha-bio-safety

# (선택) 로컬 dry-run 먼저:
# npx wrangler d1 execute cha-bio-db --local --file=migrations/0079_extinguisher_asset_split.sql

# 운영 적용:
npx wrangler d1 execute cha-bio-db --remote --file=migrations/0079_extinguisher_asset_split.sql

# 사후 검증 (5개 SQL):
npx wrangler d1 execute cha-bio-db --remote --command "PRAGMA table_info(extinguishers);"
# → status 컬럼이 결과 목록에 있어야 함

npx wrangler d1 execute cha-bio-db --remote --command "PRAGMA table_info(check_records);"
# → extinguisher_id 컬럼이 결과 목록에 있어야 함

npx wrangler d1 execute cha-bio-db --remote --command "SELECT COUNT(*) AS total, SUM(CASE WHEN status='active' THEN 1 ELSE 0 END) AS active FROM extinguishers;"
# → total == active (모든 기존 행이 active)

npx wrangler d1 execute cha-bio-db --remote --command "SELECT COUNT(*) AS total, SUM(CASE WHEN extinguisher_id IS NULL THEN 1 ELSE 0 END) AS nulls FROM check_records WHERE checkpoint_id LIKE 'CP-FE-%';"
# → nulls / total ≤ 5%

npx wrangler d1 execute cha-bio-db --remote --command "SELECT name FROM sqlite_master WHERE type='index' AND name IN ('idx_extinguishers_status','idx_extinguishers_cp_active','idx_check_records_ext');"
# → 3개 인덱스 모두 존재
```

결과를 `.planning/phases/24-extinguisher-asset-location/24-01-PRAGMA.md` 의 끝에 "## Post-migration verification" 섹션으로 추가.

**롤백 절차** (실패 시):
- ALTER 가 부분 적용된 경우: `ALTER TABLE extinguishers DROP COLUMN status;` (SQLite 3.35+ 지원, D1 도 지원). 단, 백필 UPDATE 가 부분 적용됐어도 운영 데이터에는 무해 (NULL → 추정값 채워진 것뿐).
  </how-to-verify>
  <resume-signal>5개 검증 SQL 모두 기대대로 출력 + 24-01-PRAGMA.md 업데이트 + 사용자 "approved"</resume-signal>
  <acceptance_criteria>
    - PRAGMA 결과에 `extinguishers.status` 컬럼 존재 (검증 SQL 출력 grep `| status |`)
    - PRAGMA 결과에 `check_records.extinguisher_id` 컬럼 존재
    - 모든 기존 extinguishers 행이 `status='active'` (active count == total)
    - 소화기 카테고리 점검 기록의 NULL extinguisher_id 비율 ≤ 5%
    - 3개 인덱스 sqlite_master 에서 모두 발견
    - 24-01-PRAGMA.md 에 "Post-migration verification" 섹션 추가됨
  </acceptance_criteria>
  <action>(human-action) 사용자가 cha-bio-safety/ 에서 0079 SQL 을 --remote 로 적용하고, 5개 사후 검증 SQL 을 실행. 결과를 24-01-PRAGMA.md 의 'Post-migration verification' 섹션에 인라인 추가.</action>
  <verify>
    <automated>grep -c 'Post-migration verification' .planning/phases/24-extinguisher-asset-location/24-01-PRAGMA.md</automated>
  </verify>
  <done>0079 마이그레이션이 prod D1 에 적용되고 5개 사후 검증 SQL 이 모두 통과 (status 컬럼 / ext_id 컬럼 / active count==total / NULL 비율 5%이하 / 3개 인덱스).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer → 운영 D1 | wrangler CLI 가 운영 D1 에 직접 DDL/UPDATE 실행 — 잘못된 SQL 은 즉시 운영 영향 |
| 운영 D1 ← 미문서화 ALTER 이력 | 과거 wrangler 직접 적용된 컬럼 존재 가능 (RESEARCH §Pitfall 1) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-01 | Tampering | extinguishers.check_point_id | mitigate | 마이그레이션은 ALTER + UPDATE 만 — 기존 check_point_id 값에 손대지 않는다. 사후 검증 SQL `SELECT COUNT(*) FROM extinguishers WHERE check_point_id IS NOT NULL` 마이그레이션 전후 동일 확인. |
| T-24-02 | Denial of Service | check_records 백필 UPDATE | accept | 4인 팀 + ~수천행 규모로 D1 락 시간 무시 가능. 5월 법정점검 직전 적용은 점검 시간 외 (저녁/주말) 권고. |
| T-24-03 | Information Disclosure | PRAGMA 출력에 컬럼명 노출 | accept | 컬럼명은 코드에 이미 공개 (오픈소스 형태). 추가 위험 없음. |
| T-24-04 | Tampering | duplicate column 충돌로 ALTER 실패 시 부분 적용 | mitigate | Task 1 의 PRAGMA 사전 점검으로 차단. 그래도 실패 시 Task 2 의 SKIPPED 주석 처리 분기로 재시도. |
| T-24-05 | Repudiation | 마이그레이션 적용 시각/주체 추적 | mitigate | 24-01-PRAGMA.md 에 "Captured at" + Post-migration 결과 + 사용자 승인을 git commit 으로 보존. |
</threat_model>

<verification>
- 24-01-PRAGMA.md 가 마이그레이션 전/후 결과를 모두 담고 있다
- 운영 D1 PRAGMA 결과에 status / extinguisher_id 컬럼 존재 확인
- 소화기 점검 기록 NULL 비율 ≤ 5%
- 3개 신규 인덱스 모두 존재
- 기존 extinguishers row count 가 마이그레이션 전후 동일 (데이터 무손실)
</verification>

<success_criteria>
- `cha-bio-safety/migrations/0079_extinguisher_asset_split.sql` 가 작성되어 grep gate 통과
- 운영 D1 에 적용 완료 (PRAGMA 사후 검증 통과)
- `.planning/phases/24-extinguisher-asset-location/24-01-PRAGMA.md` 에 baseline + post-migration 모두 캡쳐됨
- Plan 02 (Backend API) 가 진행 가능한 상태 (status / extinguisher_id 컬럼 사용 가능)
</success_criteria>

<output>
After completion, create `.planning/phases/24-extinguisher-asset-location/24-01-SUMMARY.md` with:
- 최종 0079 SQL 파일 경로
- 24-01-PRAGMA.md 의 핵심 결과 요약 (행 수, NULL 비율, 인덱스 3개)
- Plan 02 가 사용할 수 있는 새 컬럼 (`extinguishers.status`, `check_records.extinguisher_id`)
- 마이그레이션 적용 시각 (KST)
</output>
