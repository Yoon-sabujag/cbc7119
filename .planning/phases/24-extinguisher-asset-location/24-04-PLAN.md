---
phase: 24-extinguisher-asset-location
plan: 04
type: execute
wave: 3
depends_on:
  - 02
files_modified:
  - cha-bio-safety/src/pages/FloorPlanPage.tsx
  - .planning/phases/24-extinguisher-asset-location/sketches/24-04-empty-marker.html
  - .planning/phases/24-extinguisher-asset-location/sketches/24-04-legend-and-modals.html
autonomous: false
requirements:
  - EXT-05
tags:
  - frontend
  - floorplan
  - marker
  - empty-marker
must_haves:
  truths:
    - "extinguisher plan type 의 마커 추가 모달이 개소명 + 구역만 입력 받고 「소화기 종류 / 점검 개소 연결」 필드를 더 이상 노출하지 않는다"
    - "extinguisher plan type 의 마커 수정 모달이 매핑된 마커일 때 「정보 수정 / 소화기 분리」, 미배치(❓) 마커일 때 「소화기 배치」 버튼을 보여주고 정확한 navigate 흐름을 호출한다"
    - "도면에 빈 마커(check_point_id NULL 또는 매핑된 ext 가 status='폐기')가 빨간 #ef4444 disk + 흰색 ❓ glyph 로 렌더된다"
    - "범례 status row 에 「미배치」 항목이 「완료」 다음에 추가되고 빨간 ❓ 13px disk + 11px t2 라벨로 표시된다"
    - "점검 모드에서 ❓ 마커 클릭 시 「소화기 미배치」 안내 모달이 뜨고 「소화기 배치하기」 버튼이 /extinguishers?fromMarker=...&zone=...&floor=... 로 이동시킨다"
    - "점검 모달의 소화기 정보 카드에 「정보 수정」 + 「소화기 분리」 sub-action row 가 추가되고 정상 동작한다"
    - "URL ?placingExtinguisher=&zone=&floor= 으로 진입 시 자동 layer 이동 + 도면이 ❓ 마커 클릭 대기 모드로 진입하고, 빈 마커 클릭 시 「여기에 배치할까요?」 confirm → assign API → 토스트 → navigate(-1)"
    - "addExtMode === 'new' 분기와 newExt 상태가 FloorPlanPage 에서 완전히 제거된다 (회귀 테스트로 호출 코드 0)"
  artifacts:
    - path: "cha-bio-safety/src/pages/FloorPlanPage.tsx"
      provides: "마커 추가/수정 모달 단순화 + ❓ 빈 마커 렌더 + 범례 미배치 항목 + 점검 모달 소화기 카드 sub-action + URL state machine"
      contains: "placingExtinguisher"
    - path: ".planning/phases/24-extinguisher-asset-location/sketches/24-04-empty-marker.html"
      provides: "빈 마커 ❓ SVG 렌더 sketch (도면 + 범례)"
      contains: "미배치"
    - path: ".planning/phases/24-extinguisher-asset-location/sketches/24-04-legend-and-modals.html"
      provides: "범례 status row 업데이트 + 마커 추가/수정 모달 + 점검 모달 소화기 카드 sub-action sketch"
      contains: "정보 수정"
  key_links:
    - from: "FloorPlanPage MarkerIcon"
      to: "❓ glyph 렌더 (placeholder true 분기)"
      via: "SVG <text> overlay"
      pattern: "placeholder|❓"
    - from: "FloorPlanPage 점검 모드 ❓ 클릭"
      to: "/extinguishers?fromMarker=&zone=&floor="
      via: "navigate"
      pattern: "fromMarker"
    - from: "FloorPlanPage URL ?placingExtinguisher="
      to: "extinguisherApi.assign"
      via: "❓ 마커 클릭 confirm → assign 호출"
      pattern: "extinguisherApi\\.assign"
    - from: "FloorPlanPage 마커 수정 모달 「소화기 분리」"
      to: "extinguisherApi.unassign"
      via: "confirm → unassign 호출"
      pattern: "extinguisherApi\\.unassign"
    - from: "FloorPlanPage 마커 수정 모달 「소화기 배치」"
      to: "/extinguishers?fromMarker=&zone=&floor="
      via: "navigate"
      pattern: "fromMarker"
---

<objective>
Phase 24 Wave 3b: FloorPlanPage 의 마커 모달 단순화(개소명 + 구역만), 빈 ❓ 마커 시각, 범례 미배치 항목, 점검 모달 소화기 카드 sub-action, URL state machine(placingExtinguisher / fromMarker) 통합. ExtinguishersListPage(Plan 03) 와 양방향 동행 동선이 끊김 없이 동작하게 한다.

Purpose: 도면이 위치(영구) 와 자산(이동/교체) 의 분리를 시각적으로 표현하고, 점검 모드에서 미배치 위치를 즉시 식별 + 매핑 유도 (EXT-05).
Output: 단일 FloorPlanPage 파일 수정 + sketches 2개.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md (메모리 룰: 디자인 변경 전 상의 필수)
@.planning/phases/24-extinguisher-asset-location/CONTEXT.md
@.planning/phases/24-extinguisher-asset-location/24-RESEARCH.md
@.planning/phases/24-extinguisher-asset-location/24-PATTERNS.md
@.planning/phases/24-extinguisher-asset-location/24-UI-SPEC.md
@.planning/phases/24-extinguisher-asset-location/24-02-SUMMARY.md
@.planning/phases/24-extinguisher-asset-location/24-03-SUMMARY.md
@cha-bio-safety/src/pages/FloorPlanPage.tsx
@cha-bio-safety/src/pages/RemediationPage.tsx
@cha-bio-safety/src/utils/api.ts

<interfaces>
<!-- Plan 02 가 제공: -->
extinguisherApi.assign(id, checkPointId)
extinguisherApi.unassign(id)
extinguisherApi.list({ status?, mapping? }) → 미배치 목록 조회 가능

<!-- Plan 03 가 제공: -->
/extinguishers?fromMarker=&zone=&floor= 라우트가 등록 모달 자동 매핑 모드로 작동

<!-- 본 Plan 04 가 정의해야 할 URL state: -->
/floorplan?planType=extinguisher&placingExtinguisher={id}&zone={zone}&floor={floor}
  → 진입 시 자동 layer (zone/floor) 이동 + 도면이 "❓ 클릭 대기" 모드
  → 빈 ❓ 클릭 시 confirm → assign(placingExtinguisher, ❓ marker.check_point_id) → toast → navigate(-1)
  → 매핑된 마커 클릭 시 swap 안내 모달 → swap(placingExtinguisher, occupant.id) → toast → navigate(-1)

<!-- 빈 마커 판단 로직 (read 시): -->
floor_plan_markers.check_point_id IS NULL  ← 마커가 cp 에 매핑된 적 없음
  OR
extinguishers WHERE check_point_id = marker.check_point_id AND status='active' 가 0건
  ← cp 에는 마커가 묶여 있지만 자산이 미배치 상태

  (FloorPlanPage 가 이미 fetch 하는 markers / extinguishers 응답에서 판단)

<!-- 빈 마커 시각 spec (UI-SPEC §빈 마커 spec — LOCKED): -->
disk fill=#ef4444, white text=❓ at 60% of marker height, no stroke,
text-shadow: 0 1px 2px rgba(0,0,0,0.45)
size 매핑: marker canvas size 20px → ❓ 12px / legend 13px → ❓ 8px

<!-- 범례 위치 LOCKED (FloorPlanPage 라인 1276-1340): -->
rowStyle, itemStyle, labelStyle 절대 변경 금지
padding '1px 12px 26px', minHeight 100 절대 변경 금지
미배치 항목은 status row 의 「완료」(resolved) 항목 다음에 삽입
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: 빈 마커 ❓ + 범례 + 모달 sketch 작성 + 사용자 승인</name>
  <files>
    .planning/phases/24-extinguisher-asset-location/sketches/24-04-empty-marker.html (new),
    .planning/phases/24-extinguisher-asset-location/sketches/24-04-legend-and-modals.html (new)
  </files>
  <read_first>
    - .planning/phases/24-extinguisher-asset-location/24-UI-SPEC.md (§빈 마커 spec, §Component Inventory 4-6, §Color)
    - .planning/STATE.md (메모리 룰 「디자인 변경 전 상의 필수」)
    - cha-bio-safety/src/pages/FloorPlanPage.tsx (MarkerIcon 라인 96-252, 범례 라인 1276-1340, 마커 추가 모달 라인 1505-1599, 마커 수정 모달 라인 1346-1444, 점검 모달 라인을 read_first 로 직접 확인)
    - .planning/phases/24-extinguisher-asset-location/24-PATTERNS.md (§ FloorPlanPage 패턴)
  </read_first>
  <what-built>
**메모리 룰** 「디자인 변경 전 상의 필수」 강제. 2개 sketch HTML 파일 작성:

1. **24-04-empty-marker.html** — SVG 도면 placeholder (미니 floorplan 영역) 위에 빈 ❓ 마커 + 매핑된 정상 마커 4종(정상/주의/불량/완료) 함께 배치하여 시각 비교. 빈 마커는 UI-SPEC §빈 마커 spec verbatim:
   - 모양: circle disk
   - fill: `#ef4444` (var(--danger))
   - glyph: 흰색 `❓` at 60% disk size, font-weight 700, text-shadow `0 1px 2px rgba(0,0,0,0.45)`
   - 두 사이즈 동시: marker (20px) + legend (13px)

2. **24-04-legend-and-modals.html** — 4개 영역을 한 페이지에 배치:
   - **A) 범례 row 2 업데이트**: 기존 `정상/주의/불량/완료` 다음에 `미배치` 항목 추가. itemStyle/labelStyle/rowStyle 정확히 LOCKED 값 사용 (padding '1px 12px 26px', minHeight 100, gap '6px 8px', flex-wrap wrap). 모바일 375px 너비에서 줄바꿈 안전 확인.
   - **B) 마커 추가 모달 (extinguisher plan type, 변경 후)**: 개소명 input + 구역 3-button group 만 (소화기 종류 / 점검 개소 연결 필드 제거됨). 기존 모달 wrapper geometry 유지.
   - **C) 마커 수정 모달 (extinguisher, 매핑됨 / 미배치 두 상태 사이드-바이-사이드)**:
     - 매핑됨: 「정보 수정」(acl) + 「소화기 분리」(danger-tinted)
     - 미배치(❓): 「소화기 배치」(acl, full-width)
   - **D) 점검 모드 ❓ 마커 클릭 시 안내 모달**: 제목 `소화기 미배치`, body `이 위치는 마커만 등록되어 있고 소화기가 배치되지 않았습니다.`, primary `소화기 배치하기` (acl), secondary `닫기`
   - **E) 점검 모달의 소화기 카드 sub-action row** (UI-SPEC §6): 정보 수정 + 소화기 분리 두 버튼

모든 색/spacing 은 UI-SPEC LOCKED 값 사용.
  </what-built>
  <how-to-verify>
사용자가 두 HTML 을 브라우저로 열어 시각 확인:
1. `open .planning/phases/24-extinguisher-asset-location/sketches/24-04-empty-marker.html`
2. `open .planning/phases/24-extinguisher-asset-location/sketches/24-04-legend-and-modals.html`

체크리스트:
- [ ] 빈 ❓ 마커 색이 정확히 `#ef4444` 빨강 (다른 마커 색과 시각 구분 명확)
- [ ] ❓ glyph 가중심에 정렬, 가독성 충분 (text-shadow 적용)
- [ ] 범례에 「미배치」 항목이 「완료」 다음 정확한 위치에 추가됨
- [ ] iPhone SE (375px) 너비에서 범례가 한 줄로 안 들어가면 자연스럽게 wrap (overflow X)
- [ ] 마커 추가 모달에 소화기 종류 / 점검 개소 연결 필드 *없음*
- [ ] 마커 수정 모달의 두 상태(매핑됨/미배치) 액션 버튼 정확
- [ ] 점검 모드 안내 모달 카피 verbatim
- [ ] 점검 모달 소화기 카드의 sub-action row 자연스러움
  </how-to-verify>
  <resume-signal>사용자 "approved" 또는 수정 요청</resume-signal>
  <acceptance_criteria>
    - 두 HTML 파일이 sketches/ 디렉토리에 존재
    - 빈 마커 색이 `#ef4444` (`grep -ci "#ef4444" sketches/24-04-empty-marker.html` ≥ 1)
    - 범례 sketch 에 `미배치` 라벨 존재
    - 모달 sketch 에 4종 모달 모두 시각화 (마커 추가 / 마커 수정 / ❓ 안내 / 점검 모달 sub-action)
    - 마커 추가 모달 sketch 에 「소화기 종류」 / 「점검 개소 연결」 텍스트 *없음* (`grep -ci "소화기 종류\|점검 개소 연결" sketches/24-04-legend-and-modals.html` = 0)
    - 사용자 승인 received
  </acceptance_criteria>
  <action>(human-verify) UI-SPEC §빈 마커 spec + §Component Inventory 4-6 + Sketch-First Mandate 적용. 2개 self-contained HTML sketch 파일 작성: empty-marker (도면 placeholder + 빈 ❓ 마커 + 정상 마커 4종 비교, 마커 사이즈 + 범례 사이즈 모두 시각화), legend-and-modals (범례 status row + 마커 추가/수정 모달 + ❓ 안내 모달 + 점검 모달 sub-action). #ef4444 fill + 흰색 ❓ + text-shadow LOCKED. 사용자 브라우저로 열어 시각 확인 후 'approved'.</action>
  <verify>
    <automated>test -f .planning/phases/24-extinguisher-asset-location/sketches/24-04-empty-marker.html && test -f .planning/phases/24-extinguisher-asset-location/sketches/24-04-legend-and-modals.html</automated>
  </verify>
  <done>두 sketch HTML 이 sketches/ 에 존재 + 사용자 승인 received.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: FloorPlanPage 마커 추가/수정 모달 단순화 + addExtMode/newExt 제거 + ❓ 렌더 + 범례 미배치 + URL state machine + 점검 카드 sub-action</name>
  <files>cha-bio-safety/src/pages/FloorPlanPage.tsx</files>
  <read_first>
    - .planning/phases/24-extinguisher-asset-location/sketches/24-04-empty-marker.html (Task 1)
    - .planning/phases/24-extinguisher-asset-location/sketches/24-04-legend-and-modals.html (Task 1)
    - .planning/phases/24-extinguisher-asset-location/24-UI-SPEC.md (§빈 마커 spec, §Component Inventory 4-6, §Navigation contract — query string)
    - .planning/phases/24-extinguisher-asset-location/24-PATTERNS.md (§ src/pages/FloorPlanPage.tsx 라인별 위치 가이드)
    - .planning/phases/24-extinguisher-asset-location/24-RESEARCH.md (§Pitfall 6 useEffect dependency 누락 + §Pattern 4 useSearchParams lazy init)
    - cha-bio-safety/src/pages/FloorPlanPage.tsx (전체 — 1978 lines. 다음 위치는 직접 read_first):
      - state 선언: addExtMode, newExt (대략 라인 279-281)
      - submit 분기: addExtMode === 'new' (대략 라인 811-845)
      - 마커 추가 모달 JSX: addExtMode === 'new' 블록 (대략 라인 1505-1599)
      - 마커 수정 모달 JSX: editMarker (라인 1346-1444), 점검 개소 연결 섹션 (라인 1403-1418)
      - 범례 status row (라인 1276-1340) — itemStyle/labelStyle/rowStyle LOCKED
      - MarkerIcon (라인 96-252) — placeholder prop 추가 대상
      - 점검 모달 (직접 grep 으로 위치 찾기 — extDetail card 영역)
    - cha-bio-safety/src/utils/api.ts (extinguisherApi.assign / unassign / list)
    - cha-bio-safety/src/pages/RemediationPage.tsx (라인 117-121 — useSearchParams pattern 검증)
  </read_first>
  <action>
**중요:** FloorPlanPage 는 1978 라인. 큰 파일이므로 변경 범위를 최소화 — 6개 핵심 영역만 수정.

═══════════════════════════════════════

**A) `addExtMode` / `newExt` 상태 + submit 분기 + JSX 블록 완전 제거**

read_first 로 정확한 라인 확인 후:
1. `addExtMode` state useState 선언 라인 삭제
2. `newExt` state useState 선언 + setter 라인 삭제
3. submit 핸들러의 `if (addExtMode === 'new') { ... }` 분기 전체 삭제
4. 마커 추가 모달 JSX 의 `addExtMode === 'new'` 분기 블록 전체 삭제 (들어가는 jsx 약 95라인)
5. 모달 안의 toggle UI (`addExtMode` 'existing'/'new' 전환 버튼) 도 삭제 — extinguisher plan type 의 마커 추가는 항상 단순 모드

**검증:** `grep -c "addExtMode" src/pages/FloorPlanPage.tsx` 값이 0이 되어야 함. `grep -c "newExt" src/pages/FloorPlanPage.tsx` 값도 0.

═══════════════════════════════════════

**B) 마커 추가 모달 단순화 (extinguisher plan type)**

남은 마커 추가 모달은:
- **입력:** 개소명(text) + 구역(3-button group `연구동`/`사무동`/`지하`) 만
- 층은 도면 페이지에서 이미 선택된 층 자동 사용 (입력 X)
- 「소화기 종류」 셀렉터 제거 (UI-SPEC §Component Inventory 4 의 caveat: 마커-종류 SVG 아이콘 picker 가 있다면 그건 유지 — read_first 로 구분. CONTEXT 에서 제거 대상은 *소화기 분말/할로겐/CO2 등 type field*)

**검증:** 마커 추가 모달 JSX 영역 (남은 부분) 에서 `소화기 종류` 텍스트 0 매치, `점검 개소 연결` 텍스트 0 매치.

═══════════════════════════════════════

**C) 마커 수정 모달의 「점검 개소 연결」 섹션 제거 + 액션 버튼 추가**

대략 라인 1403-1418 의 `<select value={editCheckpointId}>` 블록 전체 삭제. extinguisher plan type 일 때:

- **매핑된 마커 (cp_id 가 있고 활성 ext 매핑 존재):**
  ```tsx
  <button onClick={openInfoEditModal}>정보 수정</button>
  <button onClick={openUnassignConfirm}>소화기 분리</button>
  ```
- **미배치 마커 (cp_id 가 NULL 이거나 활성 ext 매핑 없음):**
  ```tsx
  <button onClick={() => navigate(`/extinguishers?fromMarker=${marker.check_point_id}&zone=${marker.zone}&floor=${marker.floor}`)}>소화기 배치</button>
  ```

button geometry 는 PATTERNS.md §FloorPlanPage 마커 수정 모달 (modified) 의 LOCKED:
`flex: 1, padding: '8px 0', borderRadius: 8, fontSize: 12, fontWeight: 700`

색상은 UI-SPEC §Color:
- 정보 수정 / 소화기 배치: acl
- 소화기 분리: danger-tinted (`bg rgba(239,68,68,.08), color var(--danger), border 1px solid rgba(239,68,68,.3)`)

「정보 수정」 모달은 Plan 03 의 ExtinguishersListPage 와 동일 컴포넌트 재사용 — *FloorPlanPage 안에 인라인 구현*. (대안: shared component 추출. 추출 시 비용 vs 코드 중복 trade-off — executor 가 판단. 인라인이 단일 파일 변경으로 안전.)

「소화기 분리」 confirm 모달은 UI-SPEC §Copywriting 카피 verbatim:
- 제목 `소화기 분리`, body `「{개소명}」 위치에서 분리합니다. 자산은 미배치 상태로 유지됩니다.`, primary `분리`(acl)
- 클릭 시 `extinguisherApi.unassign(matchedExt.id)` 호출 → toast → invalidateQueries (`['floorplan-markers']`, `['extinguishers']`).

═══════════════════════════════════════

**D) MarkerIcon 에 빈 마커 placeholder 분기 추가**

`MarkerIcon` 컴포넌트 (라인 96-252) 또는 별도 `EmptyMarkerIcon` 추가. UI-SPEC §빈 마커 spec verbatim:

```tsx
// 빈 마커: cp_id NULL 또는 매핑된 active ext 없음
const isEmpty = !marker.check_point_id || !mappedExtinguisherIsActive(marker)

if (isEmpty && marker.plan_type === 'extinguisher') {
  return (
    <g>
      <circle cx={cx} cy={cy} r={size/2} fill="#ef4444" />
      <text x={cx} y={cy + size*0.18} fontSize={size*0.6} fill="#fff"
        textAnchor="middle" fontWeight={700}
        style={{ textShadow: '0 1px 2px rgba(0,0,0,0.45)' }}>
        ?
      </text>
    </g>
  )
}
```

**참고:** SVG `<text>` 의 `❓` 글리프가 일부 환경에서 폰트 누락 시 정사각형 표시될 수 있음. 안전한 fallback 으로 `?` (ASCII) 를 사용하면 모든 폰트에서 보장. UI-SPEC 에 명시된 `❓` (emoji) 는 이상적이나 *executor 는 ASCII `?` 로 구현하고 sketch 검증 시 사용자 확인 권장*. 둘 다 sketch 에서 비교했다면 사용자 선호 따라 결정.

`mappedExtinguisherIsActive(marker)` 헬퍼는 FloorPlanPage 가 이미 fetch 하는 extinguishers 응답을 검색하는 inline 함수.

═══════════════════════════════════════

**E) 범례 status row 에 「미배치」 항목 추가**

라인 1276-1340 의 status row map 에 새 항목 추가. **rowStyle/itemStyle/labelStyle 변경 절대 금지**:

```tsx
{['normal', 'caution', 'fault', 'resolved'].map(s => (
  <div key={s} style={itemStyle}>
    <div style={{ width: 9, height: 9, borderRadius: '50%', background: STATUS_COLOR[s] }} />
    <span style={labelStyle}>{ /* 기존 */ }</span>
  </div>
))}
{planType === 'extinguisher' && (
  <div style={itemStyle}>
    <div style={{
      width: 13, height: 13, borderRadius: '50%',
      background: '#ef4444', display: 'flex', alignItems: 'center', justifyContent: 'center',
      position: 'relative',
    }}>
      <span style={{
        fontSize: 9, fontWeight: 700, color: '#fff', lineHeight: 1,
        textShadow: '0 1px 2px rgba(0,0,0,0.45)',
      }}>?</span>
    </div>
    <span style={labelStyle}>미배치</span>
  </div>
)}
{/* 기존 extinguisher 일 때 도래/임박/초과 divider + 항목들 그대로 유지 */}
```

**read_first 로 기존 코드 흐름 정확히 확인 후 「resolved 완료」 항목 다음, 기존 extinguisher divider 앞에 삽입.**

═══════════════════════════════════════

**F) URL state machine: ?placingExtinguisher= 처리 + 점검 모드 ❓ 클릭**

`useSearchParams` 임포트 + lazy init (RESEARCH §Pitfall 6 — floor 자동 이동 깜빡임 방지):

```tsx
import { useSearchParams } from 'react-router-dom'
// ...
const [searchParams] = useSearchParams()
const placingExtId = searchParams.get('placingExtinguisher')
const isPlacingMode = !!placingExtId

const [floor, setFloor] = useState<string>(() => {
  const f = searchParams.get('floor')
  return (f && FLOORS.includes(f)) ? f : '8-1F'  // 기존 default
})
const [zone, setZone] = useState<string>(() => {
  const z = searchParams.get('zone')
  return z || '연구동'  // 기존 default 확인 후 적용
})
```

**isPlacingMode 시 도면 동작 변경:**
- 도면 상단에 안내 배너: `자산 #{placingExtId} 을(를) 빈 마커에 배치할 위치를 선택하세요. 빈 ❓ 마커 또는 매핑된 마커를 클릭`
- 빈 ❓ 마커 클릭: confirm `여기에 배치할까요?` body `「{개소명}」 ({zone}/{floor}) 위치에 배치합니다.` primary `배치` → `extinguisherApi.assign(placingExtId, marker.check_point_id)` → toast `배치 완료` → `navigate(-1)`
- 매핑된 마커 클릭: confirm `위치 스왑` body `「{other_label}」 위치 소화기와 서로 바꿉니다. 양쪽 매핑이 동시에 변경됩니다.` primary `스왑` → 단, 본 흐름은 placingExtId 가 *현재 미배치* 자산이므로 swap 이 아닌 assign 충돌 → 백엔드가 409 + hint:'swap' 반환할 것 → confirm 하면 다시 swap 요청. 단순화: `extinguisherApi.assign(placingExtId, marker.check_point_id)` 시도 → 409 시 toast 로 안내 후 `extinguisherApi.swap` 으로 재시도. 또는 미배치 상태이니 swap 이 불가 (swap 은 둘 다 매핑된 상태 요구) → 결국 충돌 시 placingExtId 를 swap 으로 하려면 *기존 점유자를 unassign 하고 placing 을 assign* 해야 함. 하지만 이는 *데이터 일관성 잃음* → **MVP 정책:** 매핑된 마커 클릭 시 `이미 다른 자산이 매핑되어 있어 배치할 수 없습니다. 다른 빈 위치를 선택하세요.` 토스트만, 작업 안 함.

**점검 모드의 ❓ 마커 클릭 (isPlacingMode 가 false 일 때):**
- 안내 모달 (UI-SPEC §Copywriting 카피 verbatim): 제목 `소화기 미배치`, body `이 위치는 마커만 등록되어 있고 소화기가 배치되지 않았습니다.`, primary `소화기 배치하기` (acl) → `navigate('/extinguishers?fromMarker=' + marker.check_point_id + '&zone=' + marker.zone + '&floor=' + marker.floor)`, secondary `닫기`

═══════════════════════════════════════

**G) 점검 모달의 소화기 정보 카드에 sub-action row 추가**

read_first 로 점검 모달의 extDetail card 영역 위치 확인 (대략 라인 3318-3325 — `replaceWarning chip` 다음, `note` 앞). 다음 추가:

```tsx
{/* Phase 24: 정보 수정 + 소화기 분리 sub-action */}
{isExtinguisherCp && extDetail && extDetail.id && (
  <div style={{ display: 'flex', gap: 8, marginTop: 8 }}>
    <button onClick={() => setInfoEditOpen(extDetail)}
      style={{ flex: 1, height: 36, borderRadius: 8, fontSize: 12, fontWeight: 700,
               background: 'var(--bg3)', color: 'var(--t1)', border: '1px solid var(--bd2)', cursor: 'pointer' }}>
      정보 수정
    </button>
    <button onClick={() => setUnassignConfirm(extDetail)}
      style={{ flex: 1, height: 36, borderRadius: 8, fontSize: 12, fontWeight: 700,
               background: 'rgba(239,68,68,.08)', color: 'var(--danger)',
               border: '1px solid rgba(239,68,68,.3)', cursor: 'pointer' }}>
      소화기 분리
    </button>
  </div>
)}
```

「정보 수정」 모달 + 「분리」 confirm 은 D) 단계와 동일 — 인라인 구현 또는 shared 컴포넌트.

═══════════════════════════════════════

**검증 사항:**
- `addExtMode` / `newExt` 식별자 grep 결과 0 매치
- 마커 수정 모달에서 `editCheckpointId` 셀렉트 0 매치
- 빈 마커 분기 (`#ef4444` SVG fill) 1+ 매치
- 범례에 `미배치` 라벨 1+ 매치
- `useSearchParams` 임포트 + 사용
- `placingExtinguisher` 식별자 1+ 매치 (URL state)
- `extinguisherApi.assign` + `extinguisherApi.unassign` 모두 호출
- `소화기 미배치` 안내 모달 카피 1+ 매치
- 점검 모달 sub-action `정보 수정` + `소화기 분리` 1+ 매치
- typecheck 통과
  </action>
  <verify>
    <automated>cd cha-bio-safety && npx tsc --noEmit 2>&1 | tail -10 && (grep -c "addExtMode\|newExt" src/pages/FloorPlanPage.tsx | grep -q '^0$' && echo "OK: addExtMode/newExt removed" || echo "FAIL: still references") && (grep -c "editCheckpointId" src/pages/FloorPlanPage.tsx | grep -q '^0$' && echo "OK: editCheckpointId removed" || echo "FAIL: still references") && grep -c "placingExtinguisher" src/pages/FloorPlanPage.tsx && grep -c "fromMarker" src/pages/FloorPlanPage.tsx && grep -c "extinguisherApi\.assign\|extinguisherApi\.unassign" src/pages/FloorPlanPage.tsx && grep -c "소화기 미배치" src/pages/FloorPlanPage.tsx && grep -c "미배치" src/pages/FloorPlanPage.tsx && grep -c "소화기 분리\|소화기 배치" src/pages/FloorPlanPage.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `addExtMode` 식별자 0 매치 (`grep -c "addExtMode" src/pages/FloorPlanPage.tsx` = 0)
    - `newExt` 식별자 0 매치 (`grep -c "newExt" src/pages/FloorPlanPage.tsx` = 0)
    - `editCheckpointId` 식별자 0 매치 (마커 수정 모달의 점검 개소 연결 섹션 완전 제거)
    - `placingExtinguisher` 식별자 1+ 매치 (URL state)
    - `fromMarker` 식별자 1+ 매치 (점검 모드 ❓ 클릭 navigate)
    - `useSearchParams` 임포트 추가됨
    - `extinguisherApi.assign` 1+ 호출 (placing 흐름)
    - `extinguisherApi.unassign` 1+ 호출 (분리 흐름)
    - 빈 마커 분기 코드 존재 (`grep -c "#ef4444" src/pages/FloorPlanPage.tsx` ≥ 1 — 기존이 0 이라면)
    - 범례 status row 에 `미배치` 라벨 1+ 매치
    - 점검 모달 ❓ 안내 카피 `소화기 미배치` 1+ 매치
    - 점검 모달 sub-action 카피 `정보 수정` + `소화기 분리` 모두 매치
    - 마커 수정 모달 카피 `소화기 배치` 1+ 매치
    - confirm 카피 `「{개소명}」 위치에서 분리합니다` 또는 동등한 형태 1+ 매치 (template literal 일 수 있어 부분 매치 허용)
    - rowStyle / itemStyle / labelStyle 정의는 변경 없음 (라인 1276-1285 그대로)
    - 범례 padding `'1px 12px 26px'` 변경 없음 (`grep -c "'1px 12px 26px'" src/pages/FloorPlanPage.tsx` ≥ 1)
    - `npx tsc --noEmit` 0 errors
    - 회귀: 기존 guidelamp/sprinkler/detector plan type 의 마커 추가/수정 동작 변경 없음 (해당 분기 코드는 read_first 후 변경 없이 유지)
  </acceptance_criteria>
  <done>FloorPlanPage 가 sketch 정합 + 6개 핵심 변경 + addExtMode/newExt/editCheckpointId 완전 제거 + typecheck 통과.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL ?placingExtinguisher= | untrusted — assign 호출 전 백엔드 가드(Plan 02)가 cp+ext 검증 |
| 빈 마커 판단 (클라이언트) | 데이터 신호일 뿐 — 실제 매핑은 서버가 진실 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-22 | Tampering | URL placingExtinguisher 위변조 | mitigate | Plan 02 의 assign 가 ext.status / 매핑 상태 검증. 클라이언트는 토스트로 표시. |
| T-24-23 | Tampering | 빈 마커 클라이언트 판단 오류 (race condition) | mitigate | 서버 assign 의 occupant SELECT 가 충돌 시 409 + hint:'swap'. 클라이언트는 토스트로 안내. |
| T-24-24 | Information Disclosure | URL fromMarker 가 다른 사용자 cp_id 노출 | accept | cp_id 는 코드 수준 식별자 (CP-FE-...). 운영상 비밀 아님. 4인 팀 내부. |
| T-24-25 | Tampering | 점검 모달 sub-action 「분리」 가 진행 중인 점검 세션 데이터 손상 | mitigate | unassign 은 status 보존 (active). check_records 절대 미삭제. 진행 중인 점검 세션의 ext_id 스냅샷은 INSERT 시점 서버 SELECT 로 보호 (Plan 02 records.ts). |
</threat_model>

<verification>
- 두 sketch HTML 사용자 승인 받음
- FloorPlanPage 변경 후 다른 plan type (guidelamp/sprinkler/detector) 동작 회귀 없음
- `addExtMode` / `newExt` / `editCheckpointId` 0 grep 매치
- 점검 모드에서 ❓ 마커 클릭 시 안내 모달 → /extinguishers 동행 동선 동작
- placingExtinguisher 모드 시 빈 ❓ 클릭 → assign → navigate(-1) 동선 동작
- typecheck 통과
</verification>

<success_criteria>
- 사용자가 sketch 승인 후 인라인 적용 완료
- 도면에 빈 마커 ❓ + 범례 「미배치」 항목이 시각적으로 정확
- 양방향 동선 (도면→리스트, 리스트→도면) 끊김 없이 동작
- 회귀 없음 (typecheck + 기존 다른 plan type 변경 없음)
</success_criteria>

<output>
After completion, create `.planning/phases/24-extinguisher-asset-location/24-04-SUMMARY.md` with:
- 변경된 FloorPlanPage 라인 범위 + 제거된 코드 라인 수
- 빈 마커 SVG 분기 코드 인용
- 범례 「미배치」 항목 코드 인용
- URL state machine 흐름도 (?placingExtinguisher= 진입 → 빈 ❓ 클릭 → assign → navigate(-1))
- 점검 모드 ❓ 안내 모달 + 소화기 카드 sub-action 동작
- 회귀 테스트 결과 (다른 plan type 동작 확인)
</output>
