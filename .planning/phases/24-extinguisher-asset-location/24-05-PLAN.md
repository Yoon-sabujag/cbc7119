---
phase: 24-extinguisher-asset-location
plan: 05
type: execute
wave: 3
depends_on:
  - 02
files_modified:
  - cha-bio-safety/src/pages/InspectionPage.tsx
autonomous: true
requirements:
  - EXT-04
  - EXT-05
tags:
  - frontend
  - inspection
  - extinguisher
must_haves:
  truths:
    - "InspectionPage 의 소화기 카테고리 헤더에 + 새로 등록 버튼이 추가되고 클릭 시 /extinguishers 로 이동한다"
    - "InspectionPage 의 extDetail 소화기 정보 카드에 정보 수정 + 소화기 분리 sub-action row 가 추가되어 update / unassign mutation 을 호출한다"
    - "정보 수정 모달이 ≤3 필드 카운터 + 4개 시 저장 비활성 + microcopy 를 정확히 구현한다 (Plan 03 의 ExtinguishersListPage 와 동일 카피/색)"
    - "분리 confirm 모달이 UI-SPEC §Copywriting 의 정확한 카피 (제목 소화기 분리, body ...위치에서 분리합니다..., primary 분리 acl) 를 사용한다"
    - "+ 새로 등록 버튼 geometry 가 acl 배경 / height 32 / fontSize 12 / weight 700 / padding 0 12px / radius 8 와 일치한다"
    - "기존 점검 흐름(체크포인트 결과 입력, 사진 업로드, 메모 등) 회귀 없음 — 추가만 됨"
  artifacts:
    - path: "cha-bio-safety/src/pages/InspectionPage.tsx"
      provides: "소화기 카테고리 헤더 + 새로 등록 + extDetail 카드 sub-action (정보 수정 / 소화기 분리)"
      contains: "extinguisherApi.update"
  key_links:
    - from: "InspectionPage 헤더 + 새로 등록 버튼"
      to: "/extinguishers"
      via: "navigate"
      pattern: "navigate\\('/extinguishers'\\)"
    - from: "InspectionPage extDetail 카드 정보 수정 / 소화기 분리"
      to: "extinguisherApi.update / extinguisherApi.unassign"
      via: "useMutation"
      pattern: "extinguisherApi\\.(update|unassign)"
---

<objective>
Phase 24 Wave 3c: InspectionPage 일반점검 > 소화기 카테고리에 「+ 새로 등록」 헤더 버튼 + extDetail 카드 sub-action(정보 수정 / 분리) 추가. Plan 03/04 와 일관된 카피/색/모달 사용.

Purpose: 점검 작업 중 발견한 자산 정보 오류를 그 자리에서 수정하거나 분리할 수 있게 하여 5월 법정점검 동선 단순화 (EXT-04, EXT-05).
Output: InspectionPage.tsx 수정 (단일 파일).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/24-extinguisher-asset-location/CONTEXT.md
@.planning/phases/24-extinguisher-asset-location/24-RESEARCH.md
@.planning/phases/24-extinguisher-asset-location/24-PATTERNS.md
@.planning/phases/24-extinguisher-asset-location/24-UI-SPEC.md
@.planning/phases/24-extinguisher-asset-location/24-02-SUMMARY.md
@.planning/phases/24-extinguisher-asset-location/24-03-SUMMARY.md
@.planning/phases/24-extinguisher-asset-location/sketches/24-04-legend-and-modals.html
@cha-bio-safety/src/pages/InspectionPage.tsx
@cha-bio-safety/src/utils/api.ts

<interfaces>
Plan 02 가 제공:
- extinguisherApi.update(id, fields) → ≤3 필드 검증 (백엔드 강제)
- extinguisherApi.unassign(id) → check_point_id=NULL

Plan 03 의 sketch 가 정보 수정 모달 + 분리 confirm 모달 디자인 컨트랙트 제공:
- 24-04-legend-and-modals.html 의 점검 모달 sub-action row 시각
- 24-03-modals.html 의 정보 수정 모달 카운터 칩 + microcopy

UI-SPEC §Component Inventory #7 (LOCKED):
- 헤더 + 새로 등록 버튼: acl bg, height 32, fontSize 12, weight 700, padding 0 12px, radius 8
- sub-action row geometry: display flex, gap 8, marginTop 8, 각 버튼 height 36 / radius 8 / fontSize 12 / weight 700
- 정보 수정 button: bg var(--bg3), color var(--t1), border 1px solid var(--bd2)
- 소화기 분리 button: bg rgba(239,68,68,.08), color var(--danger), border 1px solid rgba(239,68,68,.3)

UI-SPEC §Copywriting verbatim:
- 분리 confirm: 제목 소화기 분리 / body 「{개소명}」 위치에서 분리합니다. 자산은 미배치 상태로 유지됩니다. / primary 분리 (acl)
- 정보 수정 카운터: 변경: {n} / 3
- microcopy: 4개 이상 변경하려면 「폐기 후 재등록」을 사용하세요.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: InspectionPage 헤더에 + 새로 등록 버튼 추가</name>
  <files>cha-bio-safety/src/pages/InspectionPage.tsx</files>
  <read_first>
    - .planning/phases/24-extinguisher-asset-location/24-UI-SPEC.md (§Component Inventory #7)
    - .planning/phases/24-extinguisher-asset-location/24-PATTERNS.md (§ src/pages/InspectionPage.tsx (modify) — 헤더 버튼 위치 라인 ~3196-3200)
    - cha-bio-safety/src/pages/InspectionPage.tsx (직접 read_first 로 헤더 영역 확인 — `리스트` 버튼이 있는 곳, isExtinguisher 조건 분기. grep 으로 위치 찾기: `grep -n "isExtinguisher\|setShowExtList" src/pages/InspectionPage.tsx` 후 해당 라인 ±20 읽기)
  </read_first>
  <action>
read_first 로 InspectionPage 의 헤더 영역에서 isExtinguisher 분기 안의 리스트 버튼 위치를 찾는다. 해당 버튼의 형제 위치(왼쪽이 안전)에 + 새로 등록 버튼을 추가:

다음 JSX 를 isExtinguisher 분기 안 리스트 버튼 *바로 앞*에 삽입.

button 의 인라인 스타일 정확한 키-값 (UI-SPEC #7 LOCKED):
  height: 32
  padding: '0 12px'
  borderRadius: 8
  background: 'var(--acl)'
  border: 'none'
  color: '#fff'
  fontSize: 12
  fontWeight: 700
  cursor: 'pointer'
  whiteSpace: 'nowrap'

onClick: () => navigate('/extinguishers')
라벨: + 새로 등록

중요:
- useNavigate import 가 이미 있는지 read_first 로 확인 (있을 가능성 높음). 없으면 `import { useNavigate } from 'react-router-dom'` 추가 후 컴포넌트 상단에 const navigate = useNavigate() 추가.
- 기존 리스트 버튼은 그대로 유지. 둘 다 노출.
- 헤더 행이 좁아 줄바꿈 발생할 수 있음 — 헤더 row 의 gap/wrap 은 디자인 LOCKED 이므로 변경 금지. 새 버튼만 추가.
- 안전한 방향: + 새로 등록 을 리스트 버튼 *왼쪽* 에 삽입.
  </action>
  <verify>
    <automated>cd cha-bio-safety && npx tsc --noEmit 2>&1 | tail -10 && grep -c "+ 새로 등록" src/pages/InspectionPage.tsx && grep -c "navigate('/extinguishers')" src/pages/InspectionPage.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `+ 새로 등록` 문자열 1+ 매치
    - `navigate('/extinguishers')` 호출 1+ 매치 (헤더 버튼 onClick)
    - useNavigate import 존재 (`grep -c "useNavigate" src/pages/InspectionPage.tsx` ≥ 1)
    - `npx tsc --noEmit` 0 errors
    - 기존 setShowExtList / 리스트 텍스트 보존 (변경 전후 매치 수 동일하거나 증가만)
    - 새 버튼 인라인 스타일에 `height: 32` + `background: 'var(--acl)'` + `padding: '0 12px'` + `borderRadius: 8` 모두 존재 (visual diff)
  </acceptance_criteria>
  <done>헤더 버튼이 정확한 geometry 로 추가되고 typecheck 통과.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: extDetail 소화기 정보 카드 sub-action row + 정보 수정 모달 + 분리 confirm 모달</name>
  <files>cha-bio-safety/src/pages/InspectionPage.tsx</files>
  <read_first>
    - .planning/phases/24-extinguisher-asset-location/24-UI-SPEC.md (§Component Inventory #7, §Copywriting, §Color)
    - .planning/phases/24-extinguisher-asset-location/24-PATTERNS.md (§ src/pages/InspectionPage.tsx (modify) — extDetail 카드 sub-action 라인 ~3318-3325)
    - .planning/phases/24-extinguisher-asset-location/sketches/24-04-legend-and-modals.html
    - .planning/phases/24-extinguisher-asset-location/sketches/24-03-modals.html
    - cha-bio-safety/src/pages/InspectionPage.tsx (extDetail 카드 영역 — grep 으로 위치 찾기: `grep -n "extDetail\|getReplaceWarning\|REPLACE_WARNING_STROKE" src/pages/InspectionPage.tsx` 후 ±30 라인 read)
    - cha-bio-safety/src/pages/ExtinguishersListPage.tsx (Plan 03 의 정보 수정 모달 + 분리 confirm 모달 — 동일한 컴포넌트 패턴 인용)
    - cha-bio-safety/src/utils/api.ts (extinguisherApi.update / extinguisherApi.unassign 시그니처 + ExtinguisherDetail 의 id 필드 확인)
  </read_first>
  <action>

A) extDetail 카드 sub-action row 추가

read_first 로 InspectionPage 의 extDetail 카드 위치 (replaceWarning chip 직후, note 앞) 정확히 확인. 다음 JSX 추가:

조건: extDetail 가 truthy + extDetail.id 가 truthy
- 컨테이너 div: display flex, gap 8, marginTop 8
- 좌측 button (정보 수정):
  - flex 1, height 36, borderRadius 8, fontSize 12, fontWeight 700
  - background 'var(--bg3)', color 'var(--t1)', border '1px solid var(--bd2)', cursor 'pointer'
  - onClick: setEditExtModalOpen(extDetail)
  - 라벨: 정보 수정
- 우측 button (소화기 분리):
  - flex 1, height 36, borderRadius 8, fontSize 12, fontWeight 700
  - background 'rgba(239,68,68,.08)', color 'var(--danger)', border '1px solid rgba(239,68,68,.3)', cursor 'pointer'
  - onClick: setUnassignConfirmExt(extDetail)
  - 라벨: 소화기 분리

extDetail 의 정확한 변수명/타입은 read_first 로 확인. extDetail.id 가 ext.id 인지 확인 — Plan 02 가 ExtinguisherListResponse.items 에 id 추가했으므로 ExtinguisherDetail 에도 id 필드가 있어야 함. 없으면 src/utils/api.ts 의 ExtinguisherDetail 인터페이스에 id?: number 추가 (Plan 02 Task 3 산출이라면 이미 있음).

B) 새 state + mutations

InspectionPage 컴포넌트 상단에:

- state 2개:
  - editExtModalOpen: ExtinguisherDetail | null
  - unassignConfirmExt: ExtinguisherDetail | null

- mutations 2개:
  - updateExtMutation:
    - mutationFn: ({ id, fields }) => extinguisherApi.update(id, fields)
    - onSuccess: invalidate ['extinguishers'] + 기존 ext fetch query key, toast.success('수정 완료'), setEditExtModalOpen(null)
    - onError: toast.error(e?.message ?? '요청 실패')
  - unassignExtMutation:
    - mutationFn: (id) => extinguisherApi.unassign(id)
    - onSuccess: invalidate 동일, toast.success('분리 완료'), setUnassignConfirmExt(null)
    - onError: toast.error(e?.message ?? '요청 실패')

useQueryClient 가 이미 사용 중인지 read_first 로 확인. 없으면 import 추가.

InspectionPage 가 ext detail 을 fetch 하는 정확한 query key 는 read_first 로 확인 — `grep -n "queryKey.*ext" src/pages/InspectionPage.tsx`. invalidate 대상에 그 key 포함.

C) 정보 수정 모달 (Plan 03 의 패턴 인라인 복사)

editExtModalOpen 이 null 이 아닐 때 모달 렌더. UI-SPEC §Component Inventory #3 의 모든 요소 구현:

- 모달 wrapper geometry (LOCKED):
  - position absolute / fixed, inset 0, zIndex 40, background 'rgba(0,0,0,0.6)' (백드롭)
  - 내부: width '90%', maxWidth 360, background 'var(--bg2)', borderRadius 16, padding 20, border '1px solid var(--bd2)', maxHeight '80vh', overflowY 'auto'
  - 백드롭 onClick → setEditExtModalOpen(null), 내부 onClick e.stopPropagation()

- 헤더 row (display flex, alignItems baseline, justifyContent space-between, marginBottom 16):
  - 좌측: 정보 수정 (16/700/t1)
  - 우측: 카운터 칩 (display inline-flex, padding 2px 8px, borderRadius 5, fontSize 11, fontWeight 700)
    - n=0: bg var(--bg3), color var(--t3), text 변경: 0 / 3
    - 1≤n≤3: bg rgba(59,130,246,.15), color var(--acl), text 변경: {n} / 3
    - n>3: bg rgba(239,68,68,.15), color var(--danger), text 변경: {n} / 3

- n>3 일 때 헤더 아래 microcopy 1줄: 4개 이상 변경하려면 「폐기 후 재등록」을 사용하세요. (11/var(--danger))

- 7개 필드 stack (각 label 11/500/t3 marginBottom 6 → input height 40 padding '0 12px' borderRadius 8 bg var(--bg3) border '1px solid var(--bd2)' color t1 fontSize 13 marginBottom 14):
  - 종류 (3-col 버튼 그리드 — active state acl/white)
  - 접두문자 / 증지번호 / 제조번호 / 형식승인 / 제조년월 / 제조업체

- 변경된 필드의 input border 를 var(--acl) 로 강조 (값이 norm 비교 시 원본과 다르면 acl border)

- norm 함수 (클라이언트 카운터 — 백엔드와 동일):
  norm = v => (v === '' || v === undefined) ? null : v

- 변경 카운트 계산:
  - 입력 state vs editExtModalOpen 원본 비교
  - EDITABLE_FIELDS = [type, prefix_code, seal_no, serial_no, approval_no, manufactured_at, manufacturer]
  - changed = sum(norm(input[f]) !== norm(original[f]) for f in EDITABLE_FIELDS)

- 액션 row (display flex, gap 8, marginTop 16):
  - 취소 (flex 1, height 42, radius 10, bg var(--bg3), border 1px solid var(--bd), color var(--t2), 13/600) → setEditExtModalOpen(null)
  - 저장 (flex 1, height 42, radius 10, 13/700)
    - 정상 (1≤n≤3): bg var(--acl), color #fff
    - disabled (n=0 || n>3): bg var(--bd2), color var(--t3), cursor not-allowed
    - onClick: updateExtMutation.mutate({ id: editExtModalOpen.id, fields: changedFieldsOnly })

D) 분리 confirm 모달 (UI-SPEC §Copywriting verbatim)

unassignConfirmExt 이 null 이 아닐 때 모달 렌더:

- 모달 wrapper geometry 동일 (width '90%' maxWidth 360 padding 20 ...)
- 백드롭 click-outside → setUnassignConfirmExt(null)
- 헤더: 소화기 분리 (16/700/t1, marginBottom 16)
- body 텍스트 (13/400/t2, marginBottom 16):
  「{개소명}」 위치에서 분리합니다. 자산은 미배치 상태로 유지됩니다.
  (개소명 = unassignConfirmExt.cp_location ?? unassignConfirmExt.location ?? unassignConfirmExt.mgmt_no)
- 액션 row (display flex, gap 8):
  - 취소 (flex 1, height 42, radius 10, bg var(--bg3), border, color var(--t2), 13/600)
  - 분리 (flex 1, height 42, radius 10, bg var(--acl), color #fff, 13/700)
    - onClick: unassignExtMutation.mutate(unassignConfirmExt.id)

E) typecheck 통과:

cd cha-bio-safety && npx tsc --noEmit

검증 사항:
- sub-action row 코드 추가
- 정보 수정 모달 영역에 변경:  카운터 텍스트 존재
- microcopy 4개 이상 변경하려면 존재
- 분리 confirm body 위치에서 분리합니다 verbatim
- extinguisherApi.update + extinguisherApi.unassign 모두 호출
- 회귀: 기존 점검 흐름 (체크포인트 결과 입력, 사진, 메모) 코드 변경 없음
  </action>
  <verify>
    <automated>cd cha-bio-safety && npx tsc --noEmit 2>&1 | tail -10 && grep -c "extinguisherApi\.update\|extinguisherApi\.unassign" src/pages/InspectionPage.tsx && grep -c "정보 수정" src/pages/InspectionPage.tsx && grep -c "소화기 분리" src/pages/InspectionPage.tsx && grep -c "변경: " src/pages/InspectionPage.tsx && grep -c "위치에서 분리합니다" src/pages/InspectionPage.tsx && grep -c "rgba(239,68,68,.08)\|rgba(239,68,68,.3)" src/pages/InspectionPage.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `extinguisherApi.update` + `extinguisherApi.unassign` 호출 합 ≥ 2 (각각 최소 1번)
    - `정보 수정` 1+ 매치 (헤더 또는 sub-action button label)
    - `소화기 분리` 2+ 매치 (sub-action button label + confirm 모달 제목)
    - `변경: ` 카운터 1+ 매치
    - `4개 이상 변경하려면` microcopy 1+ 매치
    - `위치에서 분리합니다` confirm body 1+ 매치
    - `자산은 미배치 상태로 유지됩니다` 1+ 매치
    - `rgba(239,68,68,.08)` 또는 `rgba(239,68,68,.3)` (분리 버튼 color) 1+ 매치
    - `useQueryClient` import + qc.invalidateQueries 호출 1+
    - `npx tsc --noEmit` 0 errors
    - 기존 InspectionPage 의 점검 흐름 코드 (체크포인트 결과 'normal'/'caution'/'bad', 사진 업로드, 메모) 변경 없음 — read_first 로 확인된 함수/JSX 들은 grep diff 검증
    - 기존 setShowExtList / ExtinguisherListOverlay 코드 보존
  </acceptance_criteria>
  <done>InspectionPage 의 sub-action row + 두 모달이 sketch + UI-SPEC 정합 + typecheck 통과 + 기존 점검 흐름 회귀 없음.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 클라이언트 ≤3 카운터 | UX 만 — 백엔드(Plan 02 [id].ts)가 final enforce |
| extDetail.id 사용 | api.ts ExtinguisherDetail 에 id 가 있어야 함 (Plan 02 Task 3 가 추가) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-26 | Tampering | 클라이언트 카운터 우회 (n>3 강제 제출) | mitigate | Plan 02 의 PUT 가 norm + diff count → 400 + 정확한 메시지. UX disabled 우회해도 서버 거부. |
| T-24-27 | Tampering | 분리 confirm 우회 (직접 unassign API 호출) | accept | unassign 은 idempotent 하고 status='active' 보존. 점검 기록 무결성 영향 없음. confirm 은 UX 안전장치. |
| T-24-28 | Information Disclosure | 점검 진행 중 ext 분리 → check_records.extinguisher_id 가 NULL 점검될 가능성 | mitigate | Plan 02 records.ts 가 INSERT 직전 서버 SELECT 로 *현재* active ext_id 사용. 사용자가 점검 중 분리해도 INSERT 시점 매핑 X 면 NULL 스냅샷 (정상 동작). |
| T-24-29 | Repudiation | 정보 수정/분리 작업의 추적성 | mitigate | extinguishers.updated_at 이 datetime('now','+9 hours') 로 갱신됨 (Plan 02 [id].ts / unassign.ts). _middleware 가 staffId 컨텍스트 보유. |
</threat_model>

<verification>
- 헤더 버튼 + sub-action row 추가됨, typecheck 통과
- update / unassign mutation 의 onSuccess 가 invalidateQueries 로 캐시 갱신
- confirm 모달 카피 verbatim
- 기존 점검 흐름 회귀 없음
</verification>

<success_criteria>
- InspectionPage 가 + 새로 등록 버튼 + extDetail 카드 sub-action 을 정확한 geometry/카피로 노출
- 정보 수정 + 분리 모두 동작 (Plan 02 의 백엔드와 통합)
- typecheck 0 errors
- 기존 점검 흐름 보존 (체크포인트 결과 입력, 사진, 메모)
</success_criteria>

<output>
After completion, create `.planning/phases/24-extinguisher-asset-location/24-05-SUMMARY.md` with:
- 변경된 라인 범위 + 추가된 코드 라인 수
- + 새로 등록 버튼 위치 + JSX 인용
- sub-action row JSX 인용
- 정보 수정 모달의 카운터 분기 코드 인용
- 분리 confirm 모달 카피 인용
- mutation onSuccess invalidate 대상 query key 목록
</output>
