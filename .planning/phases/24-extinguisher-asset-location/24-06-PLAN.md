---
phase: 24-extinguisher-asset-location
plan: 06
type: execute
wave: 4
depends_on:
  - 03
  - 04
  - 05
files_modified:
  - cha-bio-safety/package.json
  - .planning/phases/24-extinguisher-asset-location/24-06-DEPLOY-LOG.md
  - .planning/phases/24-extinguisher-asset-location/24-06-UAT-CHECKLIST.md
autonomous: false
requirements:
  - EXT-04
  - EXT-05
tags:
  - deploy
  - uat
  - pwa-cache
must_haves:
  truths:
    - "package.json 의 version 이 0.2.0 → 0.2.1 (또는 직전 버전 + 1) 로 bump 되어 PWA splash version-check 가 캐시 클리어를 트리거할 수 있다"
    - "wrangler pages deploy 가 --branch=production 플래그와 함께 실행되어 production deployment 가 됨"
    - "production 배포 후 7개 ROADMAP success criteria 가 모두 manual UAT 로 검증되고 24-06-UAT-CHECKLIST.md 에 결과가 기록된다"
    - "운영 D1 의 sanity check 4개 (PRAGMA + COUNT + 신규 점검 기록의 ext_id 스냅샷 + 1:1 데이터 무손실 비교) 가 통과한다"
    - "사용자에게 PWA 앱 재설치 / 새로고침 안내가 전달되고 24-06-DEPLOY-LOG.md 에 기록된다"
  artifacts:
    - path: "cha-bio-safety/package.json"
      provides: "version bump (0.2.0 → 0.2.1)"
      contains: "version"
    - path: ".planning/phases/24-extinguisher-asset-location/24-06-DEPLOY-LOG.md"
      provides: "배포 명령 출력 + 시각 + commit hash + PWA 안내"
      contains: "wrangler pages deploy"
    - path: ".planning/phases/24-extinguisher-asset-location/24-06-UAT-CHECKLIST.md"
      provides: "7개 ROADMAP success criteria 의 manual UAT 결과"
      contains: "ROADMAP success criteria"
  key_links:
    - from: "Plan 03/04/05 산출 코드"
      to: "production cha-bio-safety pages 배포"
      via: "wrangler pages deploy dist --branch=production"
      pattern: "--branch=production"
    - from: "사용자 PWA 클라이언트"
      to: "v0.2.1 빌드"
      via: "splash version-check + 캐시 클리어"
      pattern: "version"
---

<objective>
Phase 24 Wave 4: production 배포 + PWA 캐시 무효화 안내 + 7개 success criteria manual UAT.

Purpose: 5월 법정점검 시작 전(2026-05-01~) 운영 배포 완료 + 회귀 검증. 메모리 룰 「프로덕션 배포 후 테스트」, 「PWA 캐시 무효화」, 「배포 시 --branch production 필수」 모두 적용.
Output: 배포된 production 빌드 + 검증 로그 + UAT 체크리스트.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md (메모리 룰 모두)
@.planning/ROADMAP.md (Phase 24 7 Success Criteria)
@.planning/phases/24-extinguisher-asset-location/CONTEXT.md
@.planning/phases/24-extinguisher-asset-location/24-VALIDATION.md
@.planning/phases/24-extinguisher-asset-location/24-01-SUMMARY.md
@.planning/phases/24-extinguisher-asset-location/24-02-SUMMARY.md
@.planning/phases/24-extinguisher-asset-location/24-03-SUMMARY.md
@.planning/phases/24-extinguisher-asset-location/24-04-SUMMARY.md
@.planning/phases/24-extinguisher-asset-location/24-05-SUMMARY.md
@cha-bio-safety/CLAUDE.md
@cha-bio-safety/package.json
@cha-bio-safety/wrangler.toml

<interfaces>
배포 명령 (메모리 룰 verbatim):
- npm run build (Vite production build)
- npx wrangler pages deploy dist --branch=production --commit-message="phase 24: extinguisher asset location split"
  (한글 커밋 메시지가 wrangler 에서 거부될 수 있으므로 ASCII 별도 지정 — 메모리 룰 「wrangler가 한글 커밋 메시지 거부」)

PWA 캐시 무효화 패턴 (quick 260420-fri 검증):
- splash 화면이 마운트 시 /api/health 또는 /api/version 으로 서버 버전 가져옴
- localStorage 의 lastSeenVersion 과 비교
- 다르면 caches.delete() + window.location.reload()
- package.json version 이 build-time 으로 inject 됨 (vite define + import.meta.env.VITE_BUILD_VERSION)

ROADMAP Phase 24 Success Criteria (verbatim):
1. 소화기 리스트 페이지에서 신규 등록·수정(≤3필드)·삭제·폐기·분리 가능
2. 도면 마커 편집에서 위치 추가·개소명 수정·매핑 변경(스왑/배치/분리) 가능
3. 소화기 교체 시 폐기 보존 + 새 자산 등록·매핑 흐름이 자연스러움
4. 점검 기록에 그 시점 소화기 ID 스냅샷 저장됨 (check_records.extinguisher_id)
5. 빈 마커는 빨간 ❓ 로 도면+범례에 표시되고 점검 모드에선 매핑 유도
6. 리스트 ↔ 도면 양방향 매핑 동선이 마커 컨텍스트 동행으로 자연스러움
7. 기존 1:1 데이터가 손실 없이 새 모델로 보존됨
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 버전 bump + production build + production deploy</name>
  <files>
    cha-bio-safety/package.json,
    .planning/phases/24-extinguisher-asset-location/24-06-DEPLOY-LOG.md (new)
  </files>
  <read_first>
    - cha-bio-safety/package.json (현재 version 확인)
    - cha-bio-safety/CLAUDE.md (deploy 룰)
    - .planning/STATE.md ("배포 시 --branch production 필수" + "wrangler 한글 커밋 메시지 거부" + "PWA 캐시 무효화" 메모리 룰)
    - cha-bio-safety/vite.config.ts (VITE_BUILD_VERSION inject 패턴 — quick 260420-fri 결과)
  </read_first>
  <action>

A) 버전 bump

cha-bio-safety/package.json 의 "version" 필드를 직전 버전 + 0.0.1 로 bump:
- 예: "version": "0.2.0" → "0.2.1"
- read_first 로 현재 정확한 버전 확인 후 patch 자리 + 1.

B) 빌드

cd cha-bio-safety
npm run build

빌드 성공 확인 (exit 0). dist/ 디렉토리 생성 확인. 빌드 출력의 Vite asset 사이즈 (특히 main bundle) 를 24-06-DEPLOY-LOG.md 에 기록.

C) production 배포

cd cha-bio-safety
npx wrangler pages deploy dist --branch=production --commit-message="phase 24 extinguisher asset location split"

(한글 메시지 거부 가능성 회피 — ASCII 만 사용)

배포 명령 출력 (URL, deployment id, commit) 을 24-06-DEPLOY-LOG.md 에 기록.

D) 24-06-DEPLOY-LOG.md 작성

다음 형식으로 작성:

```markdown
# Phase 24 Plan 06 — Deploy Log

**Deployed at:** YYYY-MM-DD HH:MM KST
**Version:** 0.2.0 → 0.2.1
**Git commit:** {hash}
**Wrangler deployment ID:** {id}
**Production URL:** {url}

## Build output
{vite build output 요약 — main bundle size, chunks 등}

## Deploy output
{wrangler 명령 출력 verbatim 인용}

## PWA 무효화 안내 (사용자 전달용)

방재팀 4명에게 다음 메시지 전달:

> 소화기 관리 페이지가 추가됐습니다. 앱이 자동으로 업데이트되지만 안 보이면 다음 중 하나를 실행해 주세요:
> 1. 홈 화면의 PWA 앱 종료 후 재실행
> 2. 안 되면 사파리/크롬에서 https://{url} 접속 후 새로고침
> 3. 그래도 안 되면 PWA 앱 삭제 후 다시 홈 화면에 추가

## Sanity SQL (운영 D1)

{Plan 01 의 마이그레이션이 그대로 보존됐는지 빠른 확인}
```

E) 즉시 sanity check (4개 SQL):

cd cha-bio-safety
npx wrangler d1 execute cha-bio-db --remote --command "PRAGMA table_info(extinguishers);" | grep status
# → status 컬럼 존재 (Plan 01 적용 보존)

npx wrangler d1 execute cha-bio-db --remote --command "SELECT COUNT(*) AS total, SUM(CASE WHEN status='active' THEN 1 ELSE 0 END) AS active FROM extinguishers;"
# → total == active (배포 직후, 폐기 0)

npx wrangler d1 execute cha-bio-db --remote --command "SELECT COUNT(*) FROM extinguishers WHERE check_point_id IS NOT NULL;"
# → Plan 01 baseline 과 동일 (1:1 데이터 보존)

npx wrangler d1 execute cha-bio-db --remote --command "SELECT name FROM sqlite_master WHERE type='index' AND name LIKE 'idx_extinguishers_%' OR name LIKE 'idx_check_records_ext';"
# → 3개 신규 인덱스 존재

각 출력을 24-06-DEPLOY-LOG.md 의 "## Sanity SQL" 섹션에 인라인 인용.
  </action>
  <verify>
    <automated>cd cha-bio-safety && grep -c '"version":' package.json && test -d dist || (npm run build 2>&1 | tail -5) && test -f .planning/phases/24-extinguisher-asset-location/24-06-DEPLOY-LOG.md || echo "DEPLOY-LOG.md missing"</automated>
  </verify>
  <acceptance_criteria>
    - `cha-bio-safety/package.json` 의 version 이 직전 + 0.0.1 로 bump (`grep -E "\"version\":\s*\"0\.[0-9]+\.[0-9]+\"" cha-bio-safety/package.json` 매치 변화)
    - `cha-bio-safety/dist/` 디렉토리 존재 (빌드 성공)
    - `npm run build` 가 exit 0 (빌드 출력 마지막 줄 에러 없음)
    - `wrangler pages deploy ... --branch=production` 명령 실행됨 (24-06-DEPLOY-LOG.md 에 출력 인용 존재)
    - `--branch=production` 플래그 사용 확인 (DEPLOY-LOG.md grep 1+ 매치)
    - 24-06-DEPLOY-LOG.md 가 존재하고 production URL + deployment ID + commit hash + sanity SQL 4개 결과를 모두 포함
    - 4개 sanity SQL 모두 기대대로 통과 (status 컬럼 존재, active count == total, 매핑 카운트 baseline 일치, 인덱스 3개 모두 존재)
    - PWA 안내 문구가 DEPLOY-LOG.md 에 작성됨
  </acceptance_criteria>
  <done>버전 bump + production 배포 + sanity SQL 통과 + 배포 로그 작성.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 7개 ROADMAP success criteria UAT (사용자 실제 클릭)</name>
  <files>.planning/phases/24-extinguisher-asset-location/24-06-UAT-CHECKLIST.md (new)</files>
  <read_first>
    - .planning/ROADMAP.md (Phase 24 Success Criteria 7개 verbatim)
    - .planning/phases/24-extinguisher-asset-location/CONTEXT.md
    - .planning/phases/24-extinguisher-asset-location/24-VALIDATION.md (Manual-Only Verifications)
    - .planning/phases/24-extinguisher-asset-location/24-06-DEPLOY-LOG.md (production URL)
  </read_first>
  <what-built>
사용자가 production 빌드를 실제 사용하여 7개 success criteria 검증. 각 항목별 시나리오 클릭 + 결과 기록. 5월 법정점검 시작 전 회귀 차단.
  </what-built>
  <how-to-verify>
**검증 환경:** PWA 앱 재설치 후 진행 (메모리 룰 「PWA 캐시 무효화」). 스마트폰 + PC 둘 다 권장.

24-06-UAT-CHECKLIST.md 를 다음 형식으로 작성하면서 각 항목 PASS/FAIL 기록:

```markdown
# Phase 24 Plan 06 — UAT Checklist

**UAT date:** YYYY-MM-DD
**Tester:** {name}
**Environment:** PWA (iOS / Android / PC)
**App version:** 0.2.1

## Pre-UAT
- [ ] PWA 앱 재설치 또는 새로고침 완료
- [ ] /api/health 또는 splash version-check 정상 (서버 버전 = 0.2.1 노출)

## ROADMAP Success Criteria

### 1. 소화기 리스트 페이지에서 신규 등록·수정(≤3필드)·삭제·폐기·분리 가능
- [ ] /extinguishers 진입 — 모든 자산 카드 표시 (PASS / FAIL)
- [ ] 필터 4종 (전체/미배치/매핑/폐기) 동작
- [ ] zone/floor/type/검색 필터 동작
- [ ] + 새로 등록 → 7필드 입력 → 저장 → 미배치 카드로 표시
- [ ] 미매핑+미점검 카드의 「삭제」 → confirm → 카드 사라짐
- [ ] 미매핑+점검O 카드의 「폐기」 → confirm → 폐기 카드로 전환
- [ ] 매핑됨 카드의 「소화기 분리」 → confirm → 미배치 카드로 전환
- [ ] 정보 수정 모달: 변경 1개 → 카운터 acl 파랑 → 저장 정상
- [ ] 정보 수정 모달: 변경 4개 → 카운터 빨강 → 저장 비활성 + microcopy
- [ ] 정보 수정 모달: 변경 4개 강제 우회 시도 → 백엔드 400 + 토스트 「한 번에 최대 3개...」

### 2. 도면 마커 편집에서 위치 추가·개소명 수정·매핑 변경(스왑/배치/분리) 가능
- [ ] /floorplan?planType=extinguisher 진입
- [ ] 편집 모드 마커 추가 모달 — 개소명 + 구역 만 (소화기 종류 / 점검 개소 연결 필드 없음)
- [ ] 편집 모드 마커 수정 모달 — 매핑된 마커: 정보 수정 / 소화기 분리 버튼
- [ ] 편집 모드 마커 수정 모달 — 미배치 ❓ 마커: 소화기 배치 버튼 → /extinguishers?fromMarker=... 이동
- [ ] /extinguishers?fromMarker=... 진입 시 동행 안내 배너 표시
- [ ] 신규 등록 → 자동 매핑 → 토스트 → navigate(-1) 도면 복귀
- [ ] 도면에서 빈 ❓ 마커 시각 확인 (빨강 disk + 흰색 ❓)

### 3. 소화기 교체 시 폐기 보존 + 새 자산 등록·매핑 흐름이 자연스러움
시나리오: 분말 10년 도래 자산 A 를 폐기하고 새 자산 B 를 동일 위치에 매핑
- [ ] 자산 A 매핑됨 카드 → 분리 → 미배치 카드
- [ ] 자산 A 미배치 카드 → 폐기 → 폐기 카드 (status='폐기' 시각 표시)
- [ ] 도면에 동일 위치가 ❓ 빈 마커로 전환됨
- [ ] 빈 ❓ 마커 → 소화기 배치 → /extinguishers?fromMarker=...
- [ ] + 새로 등록 → 자산 B 입력 → 저장 → 자동 매핑 + navigate(-1)
- [ ] 도면에서 동일 위치가 정상 마커로 복원됨
- [ ] 자산 A 폐기 카드는 「폐기됨」 으로 보존됨 (조회만)

### 4. 점검 기록에 그 시점 소화기 ID 스냅샷 저장됨
- [ ] 새 점검 세션 생성 후 소화기 1개 점검 (정상 결과)
- [ ] sanity SQL: SELECT extinguisher_id, checkpoint_id FROM check_records ORDER BY checked_at DESC LIMIT 1 → ext_id 가 NULL 아님
- [ ] 그 ext_id 가 현재 매핑된 자산 ID 와 일치

### 5. 빈 마커는 빨간 ❓ 로 도면+범례에 표시되고 점검 모드에선 매핑 유도
- [ ] 도면에 ❓ 마커 시각 확인
- [ ] 범례 status row 의 「미배치」 항목 (빨강 ❓ 13px + t2 라벨) 표시
- [ ] 점검 모드 (편집 X)에서 ❓ 클릭 → 「소화기 미배치」 안내 모달
- [ ] 「소화기 배치하기」 → /extinguishers?fromMarker=... 이동

### 6. 리스트 ↔ 도면 양방향 매핑 동선이 마커 컨텍스트 동행으로 자연스러움
- [ ] 도면 → 리스트 (마커 동행): 검증 2 + 5 에서 이미 확인
- [ ] 리스트 → 도면 (placing 흐름): 미배치 카드 → 「소화기 배치」 → /floorplan?planType=extinguisher&placingExtinguisher=... 이동
- [ ] 도면에 안내 배너 + ❓ 클릭 대기 모드
- [ ] 빈 ❓ 클릭 → 「여기에 배치할까요?」 confirm → 「배치」 → 토스트 + navigate(-1) 리스트 복귀
- [ ] 리스트에서 해당 자산이 매핑됨 카드로 전환 표시

### 7. 기존 1:1 데이터가 손실 없이 새 모델로 보존됨
- [ ] sanity SQL: SELECT COUNT(*) FROM extinguishers WHERE check_point_id IS NOT NULL → Plan 01 baseline 과 동일 (또는 Plan 06 작업 중 분리/스왑/등록한 만큼 차이만)
- [ ] sanity SQL: SELECT COUNT(*) FROM extinguishers → Plan 01 baseline + 본 UAT 에서 등록한 신규 자산 수
- [ ] 기존 자산의 mgmt_no / type / 제조 정보 모두 그대로 (랜덤 5개 카드 클릭하여 시각 확인)
- [ ] 폐기된 자산이 삭제되지 않고 status='폐기' 로 보존됨 (sanity SQL: SELECT COUNT(*) FROM extinguishers WHERE status='폐기')

## 회귀 검증
- [ ] 다른 메뉴 (대시보드 / 일정 / DIV / 도면 다른 plan type 4종 / 일반점검 / 법정점검 / 승강기 / 식대 / 교육 / 문서 / 업무수행기록표 / 설정) 모두 정상 진입
- [ ] 일반점검 > 소화기 외 카테고리 (자탐 / 펌프 / SH / BC 등) 점검 흐름 정상
- [ ] 점검 일정 알림 푸시 동작 (cbc-cron-worker — 본 phase 변경 무관 확인)

## Issues found
{발견된 문제 인라인 기록 — 즉시 수정 또는 다음 quick task 생성}

## UAT result
- [ ] PASS / FAIL
- 서명 (테스터 + 일자):
```

검증 결과를 24-06-UAT-CHECKLIST.md 에 모두 기록 후 사용자 "approved" 또는 issues 보고.
  </how-to-verify>
  <resume-signal>모든 항목 PASS + 사용자 "approved" / 또는 issues 발견 시 다음 quick/gap-closure plan 생성</resume-signal>
  <acceptance_criteria>
    - 24-06-UAT-CHECKLIST.md 파일 존재
    - 7개 ROADMAP success criteria 가 모두 명시되어 있음
    - 각 criterion 의 sub-체크 항목 모두 [x] 또는 [ ] 표시 (PASS/FAIL 명확)
    - 회귀 검증 섹션이 있음
    - 4개 sanity SQL 결과 모두 인용됨 (UAT 4번 + 7번 항목)
    - 사용자 서명/승인 포함
  </acceptance_criteria>
  <action>(human-verify) production 빌드를 실제 사용 (PWA 재설치 후) 7개 ROADMAP success criteria 를 manual UAT 로 검증. 24-06-UAT-CHECKLIST.md 에 각 criterion 의 sub-체크 PASS/FAIL 기록 + 4개 sanity SQL 결과 인용 + 회귀 검증 + 사용자 서명. issues 발견 시 즉시 quick task 또는 gap-closure plan 생성.</action>
  <verify>
    <automated>test -f .planning/phases/24-extinguisher-asset-location/24-06-UAT-CHECKLIST.md && grep -c 'ROADMAP success criteria\|PASS\|FAIL' .planning/phases/24-extinguisher-asset-location/24-06-UAT-CHECKLIST.md</automated>
  </verify>
  <done>7개 success criteria 모두 PASS + UAT-CHECKLIST.md 작성 + 사용자 서명/승인.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: STATE.md 업데이트 + ROADMAP 업데이트 (Phase 24 완료 표시)</name>
  <files>.planning/STATE.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/STATE.md (전체)
    - .planning/ROADMAP.md (Phase 24 섹션)
    - .planning/phases/24-extinguisher-asset-location/24-06-UAT-CHECKLIST.md (Task 2 결과)
    - .planning/phases/24-extinguisher-asset-location/24-06-DEPLOY-LOG.md (Task 1 결과)
  </read_first>
  <action>

A) STATE.md 업데이트

Decisions / Accumulated Context / Roadmap Evolution 섹션에 Phase 24 완료 항목 추가:

- last_updated 와 last_activity 을 현재 KST 로 갱신
- "Roadmap Evolution" 에:
  ```
  - Phase 24 shipped (YYYY-MM-DD): 소화기 자산-위치 분리 — 5월 법정점검 준비 완료. extinguishers.status + check_records.extinguisher_id 추가, /extinguishers 페이지 신설, 빈 ❓ 마커 + 범례 미배치 항목, 양방향 마커 동행 동선.
  ```
- "Decisions" 에 본 phase 의 핵심 결정사항 1-2줄 추가:
  ```
  - [Phase 24]: 자산-위치 분리 — 마커는 영구 위치, ext 는 status('active'/'폐기') + check_point_id 가변 자산. ≤3 필드 변경 룰 백엔드 enforce.
  - [Phase 24]: floorplan-markers DELETE cascade 정책 변경 — ext 자산은 unassign 만, 자산 행 보존.
  ```

B) ROADMAP.md 업데이트

Phase 24 섹션의 다음 갱신:
- "Plans:" 카운트를 "0 plans" → "6 plans complete" 로
- "Plans:" 리스트에 6개 PLAN 파일 모두 [x] 로 표시:
  ```
  Plans:
  - [x] 24-01-PLAN.md — Migration 0079_extinguisher_asset_split.sql (ALTER + 백필)
  - [x] 24-02-PLAN.md — Backend API (assign/unassign/swap/dispose/PUT/DELETE) + records snapshot + cascade 변경 + extinguisherApi 확장
  - [x] 24-03-PLAN.md — ExtinguishersListPage + 메뉴/라우트 wiring + sketches
  - [x] 24-04-PLAN.md — FloorPlanPage 마커 모달 단순화 + ❓ 빈 마커 + 범례 미배치 + URL state machine + 점검 카드 sub-action
  - [x] 24-05-PLAN.md — InspectionPage + 새로 등록 + extDetail sub-action
  - [x] 24-06-PLAN.md — Production deploy + version bump + 7 criteria UAT
  ```
- ROADMAP 의 Phase 24 표 (있으면) 에 status "Complete" + 완료일 갱신
- Progress 표 (있으면) 에 Phase 24 행 추가

read_first 로 정확한 ROADMAP 구조 확인 후 동일 컨벤션으로 작성.

C) git commit

cd cha-bio-safety
git add -A
git -C .. add .planning/STATE.md .planning/ROADMAP.md .planning/phases/24-extinguisher-asset-location/

git -C .. commit -m "phase 24 ship: extinguisher asset-location split + UAT pass"

(parent dir 의 git 가 .planning/ 을 추적하므로 -C .. 또는 cwd 조정. read_first 로 git 구조 확인.)
  </action>
  <verify>
    <automated>grep -c "Phase 24 shipped\|\\[Phase 24\\]" .planning/STATE.md && grep -c "24-01-PLAN\|24-06-PLAN" .planning/ROADMAP.md && cd .. && git log --oneline -1 | grep -ci "phase 24"</automated>
  </verify>
  <acceptance_criteria>
    - STATE.md 의 "Roadmap Evolution" 또는 동등 섹션에 "Phase 24 shipped" 라인 추가됨
    - STATE.md 의 last_updated / last_activity / stopped_at 갱신됨
    - ROADMAP.md 의 Phase 24 섹션에 6개 PLAN 모두 [x] 표시됨
    - ROADMAP.md 의 Phase 24 status 가 "Complete" 또는 동등 표현으로 갱신됨
    - git log 마지막 커밋 메시지에 "phase 24" 포함 (case insensitive)
  </acceptance_criteria>
  <done>STATE + ROADMAP 갱신 + 커밋 완료. Phase 24 종료.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer → Cloudflare Pages | wrangler deploy 명령 — 잘못된 branch 시 preview 로 가서 사용자 노출 안 됨 |
| 사용자 PWA 클라이언트 → 서비스 워커 캐시 | SW 가 옛 manifest 캐시 → 신규 라우트 안 보일 가능 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-30 | Denial of Service | preview 배포로 사용자 영향 없음 | mitigate | --branch=production 플래그 acceptance_criteria 강제. DEPLOY-LOG.md 에 명시 인용. |
| T-24-31 | Information Disclosure | 한글 커밋 메시지로 wrangler 거부 → 부분 배포 | mitigate | --commit-message 로 ASCII 별도 지정 (메모리 룰). |
| T-24-32 | Denial of Service | PWA SW 캐시 미갱신 → 사용자가 옛 빌드 사용 | mitigate | package.json version bump + splash version-check (quick 260420-fri 검증). 사용자 안내 명시. |
| T-24-33 | Tampering | UAT 에서 발견된 회귀가 5월 법정점검 임박에 누적 | mitigate | UAT-CHECKLIST 에 회귀 섹션 강제. issues 발견 시 즉시 quick task 또는 gap-closure plan 생성. |
| T-24-34 | Repudiation | 배포 시각/주체 추적 | mitigate | DEPLOY-LOG.md + git commit hash + wrangler deployment ID 모두 보존. |
</threat_model>

<verification>
- production 배포 성공 + URL 접근 가능
- 4개 sanity SQL 통과
- 7개 ROADMAP success criteria UAT PASS
- STATE.md / ROADMAP.md 갱신
- git commit 보존
</verification>

<success_criteria>
- production 빌드가 cha-bio-safety pages production 환경에 배포되어 사용자 접근 가능
- 운영 D1 의 Plan 01 마이그레이션 결과 보존 + Plan 02 신규 컬럼 동작
- 4명의 방재팀이 PWA 재설치 후 신규 「소화기」 메뉴 + /extinguishers 페이지 진입 가능
- 7개 success criteria 모두 PASS (UAT-CHECKLIST.md 기록)
- 5월 법정점검 시작 전 (2026-05-01~) 모든 변경 사항 운영 환경에 적용 완료
</success_criteria>

<output>
After completion, create `.planning/phases/24-extinguisher-asset-location/24-06-SUMMARY.md` with:
- Production URL + version + 배포 시각
- 7개 success criteria UAT 결과 요약 (PASS/FAIL + issue 수)
- 4개 sanity SQL 결과 인용
- STATE.md / ROADMAP.md 갱신 라인
- git commit hash
- 발견된 issue 목록 (있으면 — 다음 quick task 또는 gap-closure 후보)
- 5월 법정점검 시작 시 baseline (자산 카운트, 매핑 카운트, 폐기 0 검증)
</output>
