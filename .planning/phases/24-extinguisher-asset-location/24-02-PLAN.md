---
phase: 24-extinguisher-asset-location
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - cha-bio-safety/functions/api/extinguishers/[id].ts
  - cha-bio-safety/functions/api/extinguishers/[id]/assign.ts
  - cha-bio-safety/functions/api/extinguishers/[id]/unassign.ts
  - cha-bio-safety/functions/api/extinguishers/[id]/swap.ts
  - cha-bio-safety/functions/api/extinguishers/[id]/dispose.ts
  - cha-bio-safety/functions/api/extinguishers/index.ts
  - cha-bio-safety/functions/api/extinguishers/create.ts
  - cha-bio-safety/functions/api/inspections/[sessionId]/records.ts
  - cha-bio-safety/functions/api/floorplan-markers/[id].ts
  - cha-bio-safety/src/utils/api.ts
autonomous: true
requirements:
  - EXT-04
  - EXT-05
tags:
  - api
  - backend
  - extinguisher
must_haves:
  truths:
    - "PUT /api/extinguishers/:id 가 4개 이상 필드 변경 시 400 응답 + '한 번에 최대 3개 필드까지만 변경 가능합니다' 메시지를 반환한다"
    - "POST /api/extinguishers/:id/assign 가 빈 위치는 200, 이미 매핑된 위치는 409 + {hint:'swap', occupantId} 를 반환한다"
    - "POST /api/extinguishers/:id/swap 가 D1 batch 로 두 자산의 check_point_id 를 atomic 하게 교환한다"
    - "POST /api/extinguishers/:id/dispose 가 status='폐기' + check_point_id=NULL 을 동시에 설정하고 idempotent 하다"
    - "DELETE /api/extinguishers/:id 가 매핑된 자산 또는 점검 기록 보유 자산은 409 로 거부한다"
    - "GET /api/extinguishers 가 ?status=, ?mapping=mapped|unmapped|disposed 필터를 지원하고 LEFT JOIN 으로 미배치 자산도 포함한다"
    - "POST /api/extinguishers/create 가 ?skip_marker=true 모드로 자산만 등록 가능하다 (check_point_id=NULL, status='active')"
    - "소화기 카테고리 점검 INSERT 시 서버가 현재 매핑된 ext_id 를 SELECT 해서 check_records.extinguisher_id 에 스냅샷 저장한다"
    - "DELETE /api/floorplan-markers/:id 가 더 이상 extinguishers 행을 cascade DELETE 하지 않고 unassign(check_point_id=NULL) 만 수행한다"
    - "src/utils/api.ts 의 extinguisherApi 가 update/assign/unassign/swap/dispose/remove 6개 메서드 + 확장된 list 시그니처를 export 한다"
  artifacts:
    - path: "cha-bio-safety/functions/api/extinguishers/[id].ts"
      provides: "PUT (≤3 필드 검증) + DELETE (가드: 미매핑+미점검) 핸들러"
      contains: "onRequestPut"
    - path: "cha-bio-safety/functions/api/extinguishers/[id]/assign.ts"
      provides: "POST {check_point_id} — 자산 매핑 (충돌 시 409 + hint:'swap')"
      contains: "onRequestPost"
    - path: "cha-bio-safety/functions/api/extinguishers/[id]/unassign.ts"
      provides: "POST — check_point_id=NULL 분리 (status 보존, idempotent)"
      contains: "onRequestPost"
    - path: "cha-bio-safety/functions/api/extinguishers/[id]/swap.ts"
      provides: "POST {other_extinguisher_id} — D1 batch atomic swap"
      contains: "env.DB.batch"
    - path: "cha-bio-safety/functions/api/extinguishers/[id]/dispose.ts"
      provides: "POST — status='폐기' + check_point_id=NULL"
      contains: "status='폐기'"
    - path: "cha-bio-safety/functions/api/extinguishers/index.ts"
      provides: "GET 확장 — LEFT JOIN + status/mapping 필터 + has_records correlated subquery (Plan 03 카드 상태 머신 source)"
      contains: "LEFT JOIN check_points"
      contains_2: "has_records"
    - path: "cha-bio-safety/functions/api/extinguishers/create.ts"
      provides: "skip_marker 모드 추가 (자산만 등록)"
      contains: "skip_marker"
    - path: "cha-bio-safety/functions/api/inspections/[sessionId]/records.ts"
      provides: "extinguisher_id 스냅샷 INSERT"
      contains: "extinguisher_id"
    - path: "cha-bio-safety/functions/api/floorplan-markers/[id].ts"
      provides: "DELETE cascade 변경 — ext 자산은 unassign 만"
      contains: "check_point_id=NULL"
    - path: "cha-bio-safety/src/utils/api.ts"
      provides: "extinguisherApi 확장 (update/assign/unassign/swap/dispose/remove)"
      contains: "extinguisherApi"
  key_links:
    - from: "src/utils/api.ts extinguisherApi"
      to: "/api/extinguishers/:id/{assign,unassign,swap,dispose}"
      via: "api.post / api.put / api.delete"
      pattern: "extinguisherApi\\.(update|assign|unassign|swap|dispose|remove)"
    - from: "functions/api/inspections/[sessionId]/records.ts"
      to: "check_records.extinguisher_id"
      via: "SELECT id FROM extinguishers WHERE check_point_id=? AND status='active'"
      pattern: "extinguisher_id"
    - from: "functions/api/extinguishers/[id]/swap.ts"
      to: "env.DB.batch"
      via: "atomic UPDATE x2"
    - from: "functions/api/extinguishers/index.ts GET response items[].has_records"
      to: "src/pages/ExtinguishersListPage.tsx 카드 상태 머신 (삭제 vs 폐기 버튼 분기)"
      via: "(SELECT COUNT(*) FROM check_records WHERE extinguisher_id=e.id) > 0 AS has_records"
      pattern: "has_records"
      pattern: "env\\.DB\\.batch"
---

<objective>
Phase 24 Wave 2: 6개 신규/확장 API 엔드포인트 + check_records 스냅샷 + floorplan-markers cascade 정책 변경 + 프론트엔드 API 클라이언트 확장. 자산-위치 분리의 모든 백엔드 동작을 구현하여 Wave 3 의 UI 작업이 즉시 호출 가능한 상태로 만든다.

Purpose: 자산 상태 머신(active/폐기 + 매핑 여부)을 백엔드가 single source of truth 로 강제. ≤3 필드 룰, 충돌 검증, atomic swap 모두 서버 레벨에서 enforce 한다 (EXT-04, EXT-05).
Output: 모든 신규 API 엔드포인트 + 확장된 extinguisherApi namespace.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/24-extinguisher-asset-location/CONTEXT.md
@.planning/phases/24-extinguisher-asset-location/24-RESEARCH.md
@.planning/phases/24-extinguisher-asset-location/24-PATTERNS.md
@.planning/phases/24-extinguisher-asset-location/24-VALIDATION.md
@.planning/phases/24-extinguisher-asset-location/24-01-SUMMARY.md
@cha-bio-safety/CLAUDE.md
@cha-bio-safety/functions/_middleware.ts
@cha-bio-safety/functions/api/extinguishers/index.ts
@cha-bio-safety/functions/api/extinguishers/create.ts
@cha-bio-safety/functions/api/extinguishers/[checkPointId].ts
@cha-bio-safety/functions/api/check-points/[id].ts
@cha-bio-safety/functions/api/floorplan-markers/[id].ts
@cha-bio-safety/functions/api/inspections/[sessionId]/records.ts
@cha-bio-safety/src/utils/api.ts

<interfaces>
<!-- Existing types in src/utils/api.ts (extend, do not redefine): -->

export interface ExtinguisherDetail {
  mgmt_no: string
  zone: string
  floor: string
  // type, prefix_code, seal_no, serial_no, approval_no, manufactured_at, manufacturer
  // (read existing file lines 200-213 for full shape)
}

export interface ExtinguisherListResponse {
  items: (ExtinguisherDetail & { seq_no: number; cp_id: string })[]
  stats: { type: string; cnt: number }[]
  zones: string[]
  floors: string[]
  total: number
}

export const extinguisherApi = {
  getDetail: (checkPointId: string) => api.get<ExtinguisherDetail | null>(`/extinguishers/${checkPointId}`),
  list: (params?) => api.get<ExtinguisherListResponse>(`/extinguishers${q}`),
  create: (data) => api.post<{ checkPointId: string; mgmtNo: string }>('/extinguishers/create', data),
}

<!-- Phase 24 additions (this plan): -->
- update(id, data) → PUT /api/extinguishers/:id  (≤3 필드 검증)
- assign(id, cpId) → POST /api/extinguishers/:id/assign
- unassign(id) → POST /api/extinguishers/:id/unassign
- swap(id, otherId) → POST /api/extinguishers/:id/swap
- dispose(id) → POST /api/extinguishers/:id/dispose
- remove(id) → DELETE /api/extinguishers/:id  (가드: 미매핑+미점검)
- list 확장: status?, mapping?: 'mapped'|'unmapped'|'disposed' 추가
- create 확장: skip_marker?: boolean — 자산만 등록 모드 (cp_id=NULL, status='active')

<!-- KST 컨벤션: 모든 신규 UPDATE 는 datetime('now','+9 hours') 사용 -->
<!-- 절대 금지: check_records 어떤 분기에서도 DELETE 금지 (floorplan-markers/[id].ts:48 코멘트 verbatim 인용) -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 액션 엔드포인트 4종 (assign / unassign / swap / dispose) 신규 작성</name>
  <files>
    cha-bio-safety/functions/api/extinguishers/[id]/assign.ts (new),
    cha-bio-safety/functions/api/extinguishers/[id]/unassign.ts (new),
    cha-bio-safety/functions/api/extinguishers/[id]/swap.ts (new),
    cha-bio-safety/functions/api/extinguishers/[id]/dispose.ts (new)
  </files>
  <read_first>
    - .planning/phases/24-extinguisher-asset-location/24-PATTERNS.md (§ assign.ts / unassign.ts / swap.ts / dispose.ts 패턴 — 약 165~283행)
    - .planning/phases/24-extinguisher-asset-location/24-RESEARCH.md (§Example 2/3/4 + §Pitfall 4 충돌 검증)
    - cha-bio-safety/functions/_middleware.ts (Env 타입, JWT 컨텍스트 — `(ctx as any).data` 로 staffId/role 접근)
    - cha-bio-safety/functions/api/floorplan-markers/[id].ts (D1 batch atomic 패턴 + check_records 보존 코멘트 verbatim 인용 대상)
    - cha-bio-safety/functions/api/check-points/[id].ts (Existence-then-update + try/catch 패턴)
  </read_first>
  <action>
4개 파일을 신규 작성. 모두 `import type { Env } from '../../../_middleware'` 사용 (3 단계 깊이).

═══════════════════════════════════════

**1) `cha-bio-safety/functions/api/extinguishers/[id]/assign.ts`**

```typescript
// POST /api/extinguishers/:id/assign — 자산을 위치(check_point_id)에 매핑
// 가드: 폐기 자산 X, 이미 매핑된 자산 X, 대상 위치에 active 자산 X (충돌 시 409 + hint:'swap')
import type { Env } from '../../../_middleware'

export const onRequestPost: PagesFunction<Env> = async ({ params, request, env }) => {
  try {
    const id = parseInt(params.id as string, 10)
    if (!Number.isFinite(id)) return Response.json({ success:false, error:'잘못된 자산 ID' }, { status:400 })

    const { check_point_id } = await request.json<{ check_point_id: string }>()
    if (!check_point_id) return Response.json({ success:false, error:'check_point_id 필수' }, { status:400 })

    const ext = await env.DB.prepare('SELECT id, check_point_id, status FROM extinguishers WHERE id=?')
      .bind(id).first<{ id:number; check_point_id:string|null; status:string }>()
    if (!ext) return Response.json({ success:false, error:'자산 없음' }, { status:404 })
    if (ext.status === '폐기') return Response.json({ success:false, error:'폐기된 자산은 매핑할 수 없습니다' }, { status:409 })
    if (ext.check_point_id) return Response.json({ success:false, error:'이미 매핑된 자산입니다 — 먼저 분리하세요' }, { status:409 })

    const occupant = await env.DB.prepare(
      "SELECT id FROM extinguishers WHERE check_point_id=? AND status='active'"
    ).bind(check_point_id).first<{ id:number }>()
    if (occupant) {
      return Response.json({
        success:false,
        error:'해당 위치에 이미 매핑된 자산이 있습니다',
        data: { occupantId: occupant.id, hint: 'swap' }
      }, { status:409 })
    }

    const cp = await env.DB.prepare('SELECT id FROM check_points WHERE id=? AND is_active=1').bind(check_point_id).first()
    if (!cp) return Response.json({ success:false, error:'점검 개소 없음' }, { status:404 })

    await env.DB.prepare("UPDATE extinguishers SET check_point_id=?, updated_at=datetime('now','+9 hours') WHERE id=?")
      .bind(check_point_id, id).run()

    return Response.json({ success:true })
  } catch (e) {
    console.error('extinguisher assign error:', e)
    return Response.json({ success:false, error:'서버 오류가 발생했습니다' }, { status:500 })
  }
}
```

═══════════════════════════════════════

**2) `cha-bio-safety/functions/api/extinguishers/[id]/unassign.ts`**

```typescript
// POST /api/extinguishers/:id/unassign — 자산을 위치에서 분리 (status='active' 유지, idempotent)
import type { Env } from '../../../_middleware'

export const onRequestPost: PagesFunction<Env> = async ({ params, env }) => {
  try {
    const id = parseInt(params.id as string, 10)
    if (!Number.isFinite(id)) return Response.json({ success:false, error:'잘못된 ID' }, { status:400 })

    const ext = await env.DB.prepare('SELECT id, check_point_id, status FROM extinguishers WHERE id=?')
      .bind(id).first<{ id:number; check_point_id:string|null; status:string }>()
    if (!ext) return Response.json({ success:false, error:'자산 없음' }, { status:404 })
    if (ext.check_point_id == null) return Response.json({ success:true, data:{ noop:true } })

    await env.DB.prepare("UPDATE extinguishers SET check_point_id=NULL, updated_at=datetime('now','+9 hours') WHERE id=?")
      .bind(id).run()
    return Response.json({ success:true })
  } catch (e) {
    console.error('extinguisher unassign error:', e)
    return Response.json({ success:false, error:'서버 오류가 발생했습니다' }, { status:500 })
  }
}
```

═══════════════════════════════════════

**3) `cha-bio-safety/functions/api/extinguishers/[id]/swap.ts`**

```typescript
// POST /api/extinguishers/:id/swap — 두 자산의 check_point_id 를 atomic 하게 교환
// body: { other_extinguisher_id: number }
// D1 batch 사용 — 한 statement 실패 시 전체 롤백 (Cloudflare 공식 트랜잭션 의미론).
import type { Env } from '../../../_middleware'

export const onRequestPost: PagesFunction<Env> = async ({ params, request, env }) => {
  try {
    const id = parseInt(params.id as string, 10)
    const { other_extinguisher_id } = await request.json<{ other_extinguisher_id: number }>()
    if (!Number.isFinite(id) || !Number.isFinite(other_extinguisher_id))
      return Response.json({ success:false, error:'잘못된 ID' }, { status:400 })
    if (id === other_extinguisher_id)
      return Response.json({ success:false, error:'동일 자산입니다' }, { status:400 })

    const a = await env.DB.prepare('SELECT id, check_point_id, status FROM extinguishers WHERE id=?')
      .bind(id).first<{ id:number; check_point_id:string|null; status:string }>()
    const b = await env.DB.prepare('SELECT id, check_point_id, status FROM extinguishers WHERE id=?')
      .bind(other_extinguisher_id).first<{ id:number; check_point_id:string|null; status:string }>()
    if (!a || !b) return Response.json({ success:false, error:'자산을 찾을 수 없음' }, { status:404 })
    if (a.status !== 'active' || b.status !== 'active')
      return Response.json({ success:false, error:'폐기된 자산은 스왑할 수 없습니다' }, { status:409 })
    if (!a.check_point_id || !b.check_point_id)
      return Response.json({ success:false, error:'두 자산 모두 매핑된 상태여야 합니다' }, { status:409 })

    await env.DB.batch([
      env.DB.prepare("UPDATE extinguishers SET check_point_id=?, updated_at=datetime('now','+9 hours') WHERE id=?")
        .bind(b.check_point_id, a.id),
      env.DB.prepare("UPDATE extinguishers SET check_point_id=?, updated_at=datetime('now','+9 hours') WHERE id=?")
        .bind(a.check_point_id, b.id),
    ])

    return Response.json({ success:true })
  } catch (e) {
    console.error('extinguisher swap error:', e)
    return Response.json({ success:false, error:'서버 오류가 발생했습니다' }, { status:500 })
  }
}
```

═══════════════════════════════════════

**4) `cha-bio-safety/functions/api/extinguishers/[id]/dispose.ts`**

```typescript
// POST /api/extinguishers/:id/dispose — 폐기 처리 (status='폐기' + check_point_id=NULL)
// idempotent: 이미 폐기 상태면 noop 응답.
// 절대 금지: check_records 는 어떤 분기에서도 DELETE 하지 않는다 (점검 기록 보존 원칙).
import type { Env } from '../../../_middleware'

export const onRequestPost: PagesFunction<Env> = async ({ params, env }) => {
  try {
    const id = parseInt(params.id as string, 10)
    if (!Number.isFinite(id)) return Response.json({ success:false, error:'잘못된 ID' }, { status:400 })

    const ext = await env.DB.prepare('SELECT id, status FROM extinguishers WHERE id=?')
      .bind(id).first<{ id:number; status:string }>()
    if (!ext) return Response.json({ success:false, error:'자산 없음' }, { status:404 })
    if (ext.status === '폐기') return Response.json({ success:true, data:{ noop:true } })

    await env.DB.prepare("UPDATE extinguishers SET status='폐기', check_point_id=NULL, updated_at=datetime('now','+9 hours') WHERE id=?")
      .bind(id).run()
    return Response.json({ success:true })
  } catch (e) {
    console.error('extinguisher dispose error:', e)
    return Response.json({ success:false, error:'서버 오류가 발생했습니다' }, { status:500 })
  }
}
```

**검증 사항:**
- 4개 파일 모두 `onRequestPost` export
- swap.ts 만 `env.DB.batch([...])` 사용 (atomic)
- dispose.ts 헤더에 "절대 금지: check_records 는 어떤 분기에서도 DELETE 하지 않는다" 코멘트 verbatim 포함
- 모든 UPDATE 는 `datetime('now','+9 hours')` (KST) — 정확히 이 문자열 사용
- 하드코딩된 한글 문자열 일치: `'폐기된 자산은 매핑할 수 없습니다'`, `'이미 매핑된 자산입니다 — 먼저 분리하세요'`, `'해당 위치에 이미 매핑된 자산이 있습니다'`, `'동일 자산입니다'`, `'폐기된 자산은 스왑할 수 없습니다'`, `'두 자산 모두 매핑된 상태여야 합니다'` (UI-SPEC §Copywriting + RESEARCH §Example 2/3 verbatim)
  </action>
  <verify>
    <automated>cd cha-bio-safety && npx tsc --noEmit 2>&1 | tail -20 && grep -l "onRequestPost" functions/api/extinguishers/[id]/assign.ts functions/api/extinguishers/[id]/unassign.ts functions/api/extinguishers/[id]/swap.ts functions/api/extinguishers/[id]/dispose.ts && grep -c "env.DB.batch" functions/api/extinguishers/[id]/swap.ts && grep -c "status='폐기'" functions/api/extinguishers/[id]/dispose.ts && grep -c "check_records 는 어떤 분기에서도 DELETE 하지 않는다" functions/api/extinguishers/[id]/dispose.ts</automated>
  </verify>
  <acceptance_criteria>
    - 4개 파일 모두 존재: assign.ts / unassign.ts / swap.ts / dispose.ts
    - 4개 파일 모두 `onRequestPost` export 함 (`grep -l "onRequestPost"` → 4 matches)
    - `swap.ts` 의 `env.DB.batch` 호출 횟수 ≥ 1
    - `swap.ts` 의 batch 내부에 정확히 2개의 prepare("UPDATE extinguishers ...") 가 존재 (`grep -c "UPDATE extinguishers SET check_point_id=" functions/api/extinguishers/[id]/swap.ts` ≥ 2)
    - `dispose.ts` 의 "status='폐기'" 문자열 포함 (단, 주석 라인 제외 카운트: `grep -v '^[[:space:]]*//' functions/api/extinguishers/[id]/dispose.ts | grep -c "status='폐기'"` ≥ 1)
    - `dispose.ts` 헤더에 "check_records 는 어떤 분기에서도 DELETE 하지 않는다" 코멘트 존재
    - `assign.ts` 의 충돌 응답에 `hint: 'swap'` 포함
    - 4개 파일에 `datetime('now','+9 hours')` 사용 (`grep -c "datetime('now','+9 hours')" functions/api/extinguishers/[id]/*.ts` ≥ 3)
    - `npx tsc --noEmit` 0 errors
  </acceptance_criteria>
  <done>4개 액션 엔드포인트가 작성되어 타입 체크 통과 + 모든 가드/코멘트 grep gate 통과.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: PUT/DELETE 핸들러 + GET 확장 + create skip_marker 모드 + records 스냅샷 + cascade 정책 변경</name>
  <files>
    cha-bio-safety/functions/api/extinguishers/[id].ts (new),
    cha-bio-safety/functions/api/extinguishers/index.ts (modify),
    cha-bio-safety/functions/api/extinguishers/create.ts (modify),
    cha-bio-safety/functions/api/inspections/[sessionId]/records.ts (modify),
    cha-bio-safety/functions/api/floorplan-markers/[id].ts (modify)
  </files>
  <read_first>
    - .planning/phases/24-extinguisher-asset-location/24-PATTERNS.md (§ extinguishers/[id].ts PUT+DELETE / extinguishers/index.ts / create.ts / inspections records.ts 패턴)
    - .planning/phases/24-extinguisher-asset-location/24-RESEARCH.md (§Pattern 3 ≤3 검증, §Example 5 list 필터, §Pitfall 5 race condition, §Open Question 4 cascade 정책)
    - cha-bio-safety/functions/api/extinguishers/index.ts (현행 GET — JOIN → LEFT JOIN 으로 변경)
    - cha-bio-safety/functions/api/extinguishers/create.ts (현행 POST — skip_marker 분기 추가)
    - cha-bio-safety/functions/api/extinguishers/[checkPointId].ts (기존 단건 조회 — 변경 없음, 패턴 참고)
    - cha-bio-safety/functions/api/check-points/[id].ts (PUT existence-check + try/catch + 동적 SET 패턴)
    - cha-bio-safety/functions/api/floorplan-markers/[id].ts (DELETE cascade 분기 — Phase 24 변경 대상)
    - cha-bio-safety/functions/api/inspections/[sessionId]/records.ts (현행 INSERT — extinguisher_id 추가 대상)
    - cha-bio-safety/migrations/0035_extinguishers.sql (extinguishers 컬럼 7종 정확한 이름)
  </read_first>
  <action>

═══════════════════════════════════════

**1) `cha-bio-safety/functions/api/extinguishers/[id].ts`** (신규 작성)

```typescript
// PUT /api/extinguishers/:id — 정보 수정 (변경 필드 ≤ 3 검증)
// DELETE /api/extinguishers/:id — hard delete (가드: 미매핑 + 미점검)
//
// ≤3 룰: 사용자가 완전히 다른 자산을 같은 행에 덮어쓰는 사고 방지.
// 4개 이상 변경은 "다른 자산" 으로 간주 → 폐기+신규 등록 동선 유도.
//
// 절대 금지: check_records 는 어떤 분기에서도 DELETE 하지 않는다 (점검 기록 보존 원칙).
import type { Env } from '../../_middleware'

const EDITABLE_FIELDS = ['type','prefix_code','seal_no','serial_no','approval_no','manufactured_at','manufacturer'] as const
type Editable = typeof EDITABLE_FIELDS[number]
const norm = (v: any) => (v === '' || v === undefined) ? null : v

export const onRequestPut: PagesFunction<Env> = async ({ params, request, env }) => {
  try {
    const id = parseInt(params.id as string, 10)
    if (!Number.isFinite(id)) return Response.json({ success:false, error:'잘못된 ID' }, { status:400 })

    const body = await request.json<Partial<Record<Editable, string|null>>>()
    const existing = await env.DB.prepare('SELECT * FROM extinguishers WHERE id=?').bind(id).first<any>()
    if (!existing) return Response.json({ success:false, error:'자산 없음' }, { status:404 })
    if (existing.status === '폐기') return Response.json({ success:false, error:'폐기된 자산은 수정할 수 없습니다' }, { status:409 })

    let changed = 0
    const sets: string[] = []
    const binds: any[] = []
    for (const f of EDITABLE_FIELDS) {
      if (!(f in body)) continue
      const newV = norm(body[f])
      const oldV = norm(existing[f])
      if (newV !== oldV) {
        changed++
        sets.push(`${f}=?`)
        binds.push(newV)
      }
    }

    if (changed > 3) {
      return Response.json({ success:false, error:'한 번에 최대 3개 필드까지만 변경 가능합니다' }, { status:400 })
    }
    if (changed === 0) {
      return Response.json({ success:true, data:{ noop:true } })
    }

    sets.push("updated_at=datetime('now','+9 hours')")
    binds.push(id)
    await env.DB.prepare(`UPDATE extinguishers SET ${sets.join(', ')} WHERE id=?`).bind(...binds).run()

    return Response.json({ success:true, data:{ changed } })
  } catch (e) {
    console.error('extinguisher update error:', e)
    return Response.json({ success:false, error:'서버 오류가 발생했습니다' }, { status:500 })
  }
}

export const onRequestDelete: PagesFunction<Env> = async ({ params, env }) => {
  try {
    const id = parseInt(params.id as string, 10)
    if (!Number.isFinite(id)) return Response.json({ success:false, error:'잘못된 ID' }, { status:400 })

    const ext = await env.DB.prepare('SELECT id, check_point_id FROM extinguishers WHERE id=?')
      .bind(id).first<{ id:number; check_point_id:string|null }>()
    if (!ext) return Response.json({ success:false, error:'자산 없음' }, { status:404 })
    if (ext.check_point_id) return Response.json({ success:false, error:'매핑된 자산은 삭제할 수 없습니다 — 먼저 분리하세요' }, { status:409 })

    const recCount = await env.DB.prepare('SELECT COUNT(*) AS c FROM check_records WHERE extinguisher_id=?')
      .bind(id).first<{ c:number }>()
    if ((recCount?.c ?? 0) > 0) {
      return Response.json({ success:false, error:'점검 기록이 있는 자산은 삭제할 수 없습니다 — 폐기를 사용하세요' }, { status:409 })
    }

    await env.DB.prepare('DELETE FROM extinguishers WHERE id=?').bind(id).run()
    return Response.json({ success:true })
  } catch (e) {
    console.error('extinguisher delete error:', e)
    return Response.json({ success:false, error:'서버 오류가 발생했습니다' }, { status:500 })
  }
}
```

═══════════════════════════════════════

**2) `cha-bio-safety/functions/api/extinguishers/index.ts`** (수정 — LEFT JOIN + status/mapping 필터 + has_records correlated subquery 추가)

기존 GET 핸들러를 다음과 같이 수정:
- `JOIN check_points cp` → `LEFT JOIN check_points cp` (미배치 ext 도 포함)
- SELECT 에 `cp.location AS cp_location, cp.floor AS cp_floor, cp.zone AS cp_zone, cp.qr_code AS cp_qr_code` 추가
- **SELECT 에 `(SELECT COUNT(*) FROM check_records cr WHERE cr.extinguisher_id = e.id) > 0 AS has_records` 추가** — Plan 03 의 카드 상태 머신이 「삭제」(hard delete) vs 「폐기」(soft delete) 버튼을 deterministic 하게 분기하기 위한 source. SQLite 는 boolean 을 0/1 로 반환하므로 클라이언트는 `Boolean(item.has_records)` 또는 `item.has_records === 1` 로 판정.
- 새 쿼리 파라미터:
  - `status` (`'active'` | `'폐기'`) — `e.status = ?`
  - `mapping` (`'mapped'` | `'unmapped'` | `'disposed'`)
    - `mapped` → `e.check_point_id IS NOT NULL AND e.status='active'`
    - `unmapped` → `e.check_point_id IS NULL AND e.status='active'`
    - `disposed` → `e.status='폐기'` (이 경우 check_point_id 는 무시)
- 응답 items 에 `status`, `cp_location`, `cp_floor`, `cp_zone`, `cp_qr_code`, `has_records` 포함
- ORDER BY 는 기존 `e.seq_no ASC` 유지
- stats / zones / floors / total 응답 shape 유지

**Why has_records:** Plan 03 의 CONTEXT card-state matrix 는 `미매핑+미점검 → 「삭제」` vs `미매핑+점검O → 「폐기」` 를 별도 버튼으로 분기. has_records 가 없으면 클라이언트가 두 버튼을 모두 보여주거나 trial-and-error 의 409 에 의존해야 함 — 두 경우 모두 CONTEXT 의 distinct-state 계약을 위반. correlated subquery 는 600 행 수준에서 충분히 빠름 (인덱스 `idx_check_records_extinguisher_id` Plan 24-01 에 추가됨).

═══════════════════════════════════════

**3) `cha-bio-safety/functions/api/extinguishers/create.ts`** (수정 — skip_marker 모드 추가)

기존 핸들러 시작부에 분기 추가:

```typescript
const body = await request.json<any>()

// Phase 24: skip_marker 모드 — 자산만 등록 (check_point_id=NULL, status='active')
if (body.skip_marker === true) {
  if (!body.type) return Response.json({ success:false, error:'종류는 필수입니다' }, { status:400 })

  const result = await env.DB.prepare(`
    INSERT INTO extinguishers (
      check_point_id, type, prefix_code, seal_no, serial_no,
      approval_no, manufactured_at, manufacturer, status,
      created_at, updated_at
    ) VALUES (NULL, ?, ?, ?, ?, ?, ?, ?, 'active',
              datetime('now','+9 hours'), datetime('now','+9 hours'))
  `).bind(
    body.type,
    body.prefix_code ?? null,
    body.seal_no ?? null,
    body.serial_no ?? null,
    body.approval_no ?? null,
    body.manufactured_at ?? null,
    body.manufacturer ?? null,
  ).run()

  const newId = result.meta?.last_row_id
  return Response.json({ success:true, data:{ extinguisherId: newId } })
}

// 기존 흐름 (마커 + 자산 동시 등록) 그대로 유지 — 도면 페이지 옛 호출용 (deprecate but kept).
// ...기존 코드 그대로...
```

기존 코드는 변경하지 않음. body.skip_marker 가 falsy 일 때 기존 분기로 fall-through.

═══════════════════════════════════════

**4) `cha-bio-safety/functions/api/inspections/[sessionId]/records.ts`** (수정 — extinguisher_id 스냅샷 추가)

INSERT (그리고 UPDATE 가 있다면 동일 로직) 직전에 다음 SELECT 추가, INSERT 컬럼 + bind 에 extinguisher_id 추가:

```typescript
// Phase 24: 소화기 카테고리면 현재 매핑된 active ext_id 를 스냅샷으로 기록.
// 클라이언트 값은 무시 — race condition 방지를 위해 서버가 직접 SELECT.
let extinguisherIdSnapshot: number | null = null
if (typeof checkpointId === 'string' && checkpointId.startsWith('CP-FE-')) {
  const currentExt = await env.DB.prepare(
    "SELECT id FROM extinguishers WHERE check_point_id=? AND status='active' LIMIT 1"
  ).bind(checkpointId).first<{ id:number }>()
  extinguisherIdSnapshot = currentExt?.id ?? null
}
```

INSERT 문에서:
- 컬럼 리스트 끝쪽에 `extinguisher_id` 추가 (기존 `floor_plan_marker_id` 다음, `checked_at` 앞)
- VALUES 자리표시자에 대응하는 `?` 추가
- `.bind(...)` 인자 끝쪽에 `extinguisherIdSnapshot` 추가

수정 전 (기존 코드):
```typescript
await env.DB.prepare(
  `INSERT INTO check_records (id,session_id,checkpoint_id,staff_id,result,memo,photo_key,guide_light_type,floor_plan_marker_id,checked_at,created_at) VALUES (?,?,?,?,?,?,?,?,?,datetime('now','+9 hours'),datetime('now','+9 hours'))`
).bind(id, sessionId, checkpointId, staffId, result, memo??null, photoKey??null, guide_light_type??null, floor_plan_marker_id??null).run()
```

수정 후:
```typescript
await env.DB.prepare(
  `INSERT INTO check_records (id,session_id,checkpoint_id,staff_id,result,memo,photo_key,guide_light_type,floor_plan_marker_id,extinguisher_id,checked_at,created_at) VALUES (?,?,?,?,?,?,?,?,?,?,datetime('now','+9 hours'),datetime('now','+9 hours'))`
).bind(id, sessionId, checkpointId, staffId, result, memo??null, photoKey??null, guide_light_type??null, floor_plan_marker_id??null, extinguisherIdSnapshot).run()
```

UPDATE 분기가 있다면 (재진입 점검 등) 동일하게 `extinguisher_id=?` 를 SET 절에 추가하고 동일 SELECT 로 채움.

**read_first 로 현재 파일을 정확히 읽고 INSERT 와 UPDATE 양쪽 모두 처리.**

═══════════════════════════════════════

**5) `cha-bio-safety/functions/api/floorplan-markers/[id].ts`** (수정 — extinguisher cascade 정책 변경)

기존 onRequestDelete 의 `isExtCascade` 분기를 다음과 같이 변경:

수정 전:
```typescript
if (isExtCascade) {
  await env.DB.batch([
    env.DB.prepare('DELETE FROM floor_plan_markers WHERE id=?').bind(id),
    env.DB.prepare('DELETE FROM extinguishers WHERE check_point_id=?').bind(cpId),
    env.DB.prepare('UPDATE check_points SET is_active=0 WHERE id=?').bind(cpId),
  ])
}
```

수정 후 (자산은 unassign 만, status='active' 보존):
```typescript
if (isExtCascade) {
  // Phase 24: 자산은 *unassign 만* (status='active' 유지). 자산 행 자체는 보존.
  // 사용자가 명시적으로 폐기를 원하면 ExtinguishersListPage 의 「폐기」 버튼 사용.
  // 절대 금지: check_records 는 어떤 분기에서도 DELETE 하지 않는다.
  await env.DB.batch([
    env.DB.prepare('DELETE FROM floor_plan_markers WHERE id=?').bind(id),
    env.DB.prepare("UPDATE extinguishers SET check_point_id=NULL, updated_at=datetime('now','+9 hours') WHERE check_point_id=?").bind(cpId),
    env.DB.prepare('UPDATE check_points SET is_active=0 WHERE id=?').bind(cpId),
  ])
}
```

기존 헤더 코멘트 (lines 42-48) 는 보존, 본문 코멘트 위와 같이 추가.

═══════════════════════════════════════

**검증 사항:**
- 5개 파일 모두 수정/생성됨
- `[id].ts` 의 EDITABLE_FIELDS 가 정확히 7개 필드 (`type,prefix_code,seal_no,serial_no,approval_no,manufactured_at,manufacturer`)
- `index.ts` 의 SQL 에 `LEFT JOIN check_points` 포함 (대문자 LEFT JOIN)
- `create.ts` 에 `body.skip_marker === true` 분기 추가
- `records.ts` 의 INSERT 에 `extinguisher_id` 컬럼 추가, 컬럼 수가 기존 + 1
- `floorplan-markers/[id].ts` 의 isExtCascade 분기에서 `DELETE FROM extinguishers` 가 제거되고 `UPDATE extinguishers SET check_point_id=NULL` 으로 대체됨
  </action>
  <verify>
    <automated>cd cha-bio-safety && npx tsc --noEmit 2>&1 | tail -10 && grep -c "한 번에 최대 3개 필드까지만 변경 가능합니다" functions/api/extinguishers/[id].ts && grep -c "점검 기록이 있는 자산은 삭제할 수 없습니다" functions/api/extinguishers/[id].ts && grep -c "LEFT JOIN check_points" functions/api/extinguishers/index.ts && grep -c "has_records" functions/api/extinguishers/index.ts && grep -c "skip_marker" functions/api/extinguishers/create.ts && grep -c "extinguisher_id" functions/api/inspections/[sessionId]/records.ts && (grep -c "DELETE FROM extinguishers WHERE check_point_id" functions/api/floorplan-markers/[id].ts | grep -q '^0$' && echo "cascade-removed-OK") && grep -c "UPDATE extinguishers SET check_point_id=NULL" functions/api/floorplan-markers/[id].ts</automated>
  </verify>
  <acceptance_criteria>
    - `functions/api/extinguishers/[id].ts` 존재 — `onRequestPut` + `onRequestDelete` 모두 export
    - PUT 의 `한 번에 최대 3개 필드까지만 변경 가능합니다` 문자열 정확히 포함 (1+ 매치)
    - DELETE 가드 메시지 2종 모두 포함: `매핑된 자산은 삭제할 수 없습니다 — 먼저 분리하세요`, `점검 기록이 있는 자산은 삭제할 수 없습니다 — 폐기를 사용하세요`
    - `EDITABLE_FIELDS` 배열에 정확히 7개 필드 존재 (`grep -A1 "EDITABLE_FIELDS = " functions/api/extinguishers/[id].ts | grep -c "type\|prefix_code\|seal_no\|serial_no\|approval_no\|manufactured_at\|manufacturer"` ≥ 7)
    - `index.ts` 에 `LEFT JOIN check_points` 1+ 매치 (대문자), `JOIN check_points cp ON e.check_point_id` 같은 INNER JOIN 0 매치 (있던 줄이 LEFT JOIN 으로 교체됨)
    - `index.ts` 에 `has_records` SELECT 컬럼 alias 1+ 매치 (correlated subquery `(SELECT COUNT(*) FROM check_records cr WHERE cr.extinguisher_id = e.id) > 0 AS has_records` 또는 동등 표현)
    - `index.ts` 응답 shape 의 items 배열 요소가 `has_records` 필드 보유 (검증: `cd cha-bio-safety && curl -s -H "Authorization: Bearer $JWT" http://127.0.0.1:8788/api/extinguishers | jq '.data.items[0] | has("has_records")'` → `true`, 또는 prod 배포 후 동일 검증)
    - `create.ts` 에 `skip_marker` 1+ 매치
    - `records.ts` INSERT 컬럼 리스트에 `extinguisher_id` 포함 (`grep -c "extinguisher_id" functions/api/inspections/[sessionId]/records.ts` ≥ 2 — INSERT + SELECT 사용)
    - `floorplan-markers/[id].ts` 의 isExtCascade 분기에서 `DELETE FROM extinguishers WHERE check_point_id` 0 매치 (cascade 제거됨)
    - `floorplan-markers/[id].ts` 에 `UPDATE extinguishers SET check_point_id=NULL` 1+ 매치
    - `npx tsc --noEmit` 0 errors
  </acceptance_criteria>
  <done>5개 파일 수정/생성 완료 + 타입 체크 통과 + 모든 grep gate 통과.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: src/utils/api.ts extinguisherApi 확장 (update/assign/unassign/swap/dispose/remove + list 시그니처 확장)</name>
  <files>cha-bio-safety/src/utils/api.ts</files>
  <read_first>
    - cha-bio-safety/src/utils/api.ts (현행 extinguisherApi 라인 ~200-241, ApiError + req<T> 라인 1-68, documentsApi 의 remove 컨벤션 라인 ~520+)
    - .planning/phases/24-extinguisher-asset-location/24-PATTERNS.md (§ src/utils/api.ts (extend extinguisherApi namespace) — 확장 컨트랙트)
    - .planning/phases/24-extinguisher-asset-location/24-RESEARCH.md (§Component Responsibilities API client 행)
  </read_first>
  <action>
`cha-bio-safety/src/utils/api.ts` 를 다음과 같이 수정:

**A) `ExtinguisherDetail` 인터페이스 확장:**

기존 인터페이스에 다음 필드 추가:
```typescript
export interface ExtinguisherDetail {
  // ...existing fields...
  status?: 'active' | '폐기'        // Phase 24
  // 매핑 정보(LEFT JOIN 결과 — 미배치 시 null):
  cp_location?: string | null
  cp_floor?: string | null
  cp_zone?: string | null
  cp_qr_code?: string | null
}
```

**B) `ExtinguisherListResponse.items` 타입에 id 추가** (Phase 24 의 update/assign/etc. 가 ext.id 사용):
```typescript
export interface ExtinguisherListResponse {
  items: (ExtinguisherDetail & { id: number; seq_no: number; cp_id: string | null })[]
  // ...rest unchanged...
}
```
(cp_id 가 null 가능 — 미배치 ext)

**C) `extinguisherApi.list` 시그니처 확장:**

```typescript
list: (params?: {
  floor?: string
  zone?: string
  type?: string
  q?: string
  status?: 'active' | '폐기'
  mapping?: 'mapped' | 'unmapped' | 'disposed'
}) => {
  const qs = new URLSearchParams()
  if (params?.floor) qs.set('floor', params.floor)
  if (params?.zone) qs.set('zone', params.zone)
  if (params?.type) qs.set('type', params.type)
  if (params?.q) qs.set('q', params.q)
  if (params?.status) qs.set('status', params.status)
  if (params?.mapping) qs.set('mapping', params.mapping)
  const q = qs.toString()
  return api.get<ExtinguisherListResponse>(`/extinguishers${q ? '?' + q : ''}`)
},
```

**D) 6개 신규 메서드 추가** (extinguisherApi 객체 내부, list 와 create 사이에 삽입):

```typescript
update:    (id: number, data: Partial<Pick<ExtinguisherDetail, 'type'|'prefix_code'|'seal_no'|'serial_no'|'approval_no'|'manufactured_at'|'manufacturer'>>) =>
             api.put<{ changed?: number; noop?: boolean }>(`/extinguishers/${id}`, data),

assign:    (id: number, checkPointId: string) =>
             api.post<void>(`/extinguishers/${id}/assign`, { check_point_id: checkPointId }),

unassign:  (id: number) =>
             api.post<{ noop?: boolean }>(`/extinguishers/${id}/unassign`, {}),

swap:      (id: number, otherId: number) =>
             api.post<void>(`/extinguishers/${id}/swap`, { other_extinguisher_id: otherId }),

dispose:   (id: number) =>
             api.post<{ noop?: boolean }>(`/extinguishers/${id}/dispose`, {}),

remove:    (id: number) =>
             api.delete<void>(`/extinguishers/${id}`),
```

**E) `create` 시그니처 확장** (skip_marker 모드):

```typescript
create: (data: {
  // 기존 필드들...
  skip_marker?: boolean
}) => api.post<
  | { checkPointId: string; mgmtNo: string }      // 기존 모드
  | { extinguisherId: number }                     // skip_marker 모드
>('/extinguishers/create', data),
```

(executor 는 기존 create 의 정확한 필드들을 read_first 로 확인 후 시그니처를 보존하면서 skip_marker 만 추가.)

**검증 사항:**
- 6개 새 메서드 (update / assign / unassign / swap / dispose / remove) 모두 export
- 시그니처가 backend 와 일치 (assign body `{check_point_id}`, swap body `{other_extinguisher_id}`)
- 기존 `getDetail`, `list`, `create` 는 호환성 유지 (제거 X)
- `delete` 가 아닌 `remove` 라는 이름 사용 (documentsApi 컨벤션 일치)
- `mapping` 파라미터 타입에 'disposed' 포함
  </action>
  <verify>
    <automated>cd cha-bio-safety && npx tsc --noEmit 2>&1 | tail -10 && grep -c "extinguisherApi" src/utils/api.ts && grep -E "(update|assign|unassign|swap|dispose|remove):\s*\(" src/utils/api.ts | grep -c -E "(update|assign|unassign|swap|dispose|remove)" && grep -c "mapping?: 'mapped' | 'unmapped' | 'disposed'" src/utils/api.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/utils/api.ts` 의 extinguisherApi 객체에 6개 새 메서드 모두 export 됨
    - update 메서드 시그니처에 `Partial<Pick<ExtinguisherDetail, ...>>` 타입 사용 (3개 필드 검증은 backend 가 enforce, 클라이언트는 모두 허용)
    - assign 의 body 가 `{ check_point_id: checkPointId }` 형태
    - swap 의 body 가 `{ other_extinguisher_id: otherId }` 형태
    - list params 에 `status?` 와 `mapping?: 'mapped' | 'unmapped' | 'disposed'` 추가됨
    - ExtinguisherDetail 에 `status?: 'active' | '폐기'` 추가
    - ExtinguisherListResponse.items 에 `id: number` 추가
    - 메서드 이름이 `delete` 가 아닌 `remove` (documentsApi 와 일치)
    - `npx tsc --noEmit` 0 errors
    - 기존 `getDetail`, `list`, `create` 함수 시그니처가 호환성 유지 (테스트: `grep -c "getDetail:" src/utils/api.ts` ≥ 1, `grep -c "list:" src/utils/api.ts` ≥ 1, `grep -c "create:" src/utils/api.ts` ≥ 1)
  </acceptance_criteria>
  <done>extinguisherApi 가 6개 신규 메서드 + 확장된 list/create 시그니처를 export 하고 타입 체크 통과.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API | 모든 신규 핸들러가 _middleware.ts 의 JWT 검증을 통과해야 진입 |
| API → D1 | parameter 바인딩만 사용 (string 보간 SQL 금지) |
| client body → server validation | ≤3 룰, 매핑 충돌, 폐기 가드 모두 서버에서 enforce |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-06 | Spoofing | 인증 누락된 요청 | mitigate | _middleware.ts 가 모든 `/api/*` 에 JWT 검증 적용 (whitelist 외). 신규 5개 파일은 자동 보호됨 — 추가 코드 불필요. |
| T-24-07 | Tampering | ≤3 룰 우회 (클라이언트가 4개 필드 PUT) | mitigate | `[id].ts` 의 `changed > 3` 검증이 SELECT 후 norm 비교 — 우회 불가. 응답 400 + 정확한 메시지. |
| T-24-08 | Tampering | hard delete 의 미점검 가드 우회 | mitigate | `[id].ts` 의 DELETE 가 `SELECT COUNT(*) FROM check_records WHERE extinguisher_id=?` 후 0 일 때만 진행. 클라이언트가 무엇을 보내든 무관. |
| T-24-09 | Tampering | check_records 삭제 시도 | mitigate | 본 plan 의 어떤 핸들러도 `DELETE FROM check_records` 미사용. `floorplan-markers/[id].ts` 도 cascade 제거됨. dispose.ts 헤더에 verbatim 코멘트로 명시. |
| T-24-10 | Tampering | swap 의 race condition (동시 두 요청) | mitigate | D1 batch 가 atomic. 4인 팀 동시성 사실상 0 — `WHERE check_point_id=?` 가드는 추가 보호 (낙관적). T-24-12 와 통합 처리. |
| T-24-11 | Tampering | assign 시 동일 위치 충돌 (자산 A,B → 같은 cp) | mitigate | assign.ts 의 occupant SELECT 가 active 자산 검출 시 409 + hint:'swap'. 클라이언트가 swap 으로 명시 호출하도록 유도. |
| T-24-12 | Information Disclosure | 점검 시점 옛 ext_id 스냅샷 | mitigate | `records.ts` 가 INSERT 직전 서버 SELECT 로 *현재* active ext_id 사용. 클라이언트 값 무시 (RESEARCH §Pitfall 5). |
| T-24-13 | Information Disclosure | error 메시지에 내부 SQL 노출 | mitigate | catch 블록에서 `console.error` 로 서버 로그만 남기고 클라이언트엔 `'서버 오류가 발생했습니다'` 일반 메시지. |
| T-24-14 | Denial of Service | 거대한 PUT body / list 쿼리 폭주 | accept | 4인 팀 트래픽 매우 낮음. _middleware.ts + Cloudflare 기본 limit 으로 충분. |
| T-24-15 | Elevation of Privilege | admin/assistant 분리 검증 누락 | accept | RESEARCH §A8: admin/assistant 동일 권한 가정 (CONTEXT 명시). assign/unassign/swap/dispose/delete/update 모두 일반 staff 가능. |
| T-24-16 | SQL Injection | 동적 SET 절 (`sets.push('${f}=?')`) | mitigate | `${f}` 는 EDITABLE_FIELDS 배열 (literal 7개) — 사용자 입력 아님. 모든 값은 `binds` 로 parameter binding. |
</threat_model>

<verification>
- 모든 신규 핸들러가 try/catch 로 wrapping 됨 (catch 블록에 console.error + 500 일반 메시지)
- swap.ts 가 `env.DB.batch([...])` 사용 (atomic)
- dispose.ts 헤더에 check_records 보존 verbatim 코멘트
- records.ts 가 소화기 카테고리(`CP-FE-`)에서만 ext_id 스냅샷 SELECT
- floorplan-markers cascade 가 더 이상 extinguishers DELETE 하지 않음
- src/utils/api.ts 가 6개 신규 메서드 + 확장된 시그니처 export
- `npx tsc --noEmit` 0 errors
</verification>

<success_criteria>
- 4개 신규 액션 엔드포인트 + [id].ts (PUT/DELETE) + index.ts/create.ts/records.ts/floorplan-markers/[id].ts 수정 완료
- src/utils/api.ts 의 extinguisherApi 가 update/assign/unassign/swap/dispose/remove 6개 메서드 + 확장 list 시그니처 export
- `cd cha-bio-safety && npx tsc --noEmit` 0 errors
- Wave 3 의 3개 plan (List/FloorPlan/Inspection) 이 즉시 호출 가능한 상태
</success_criteria>

<output>
After completion, create `.planning/phases/24-extinguisher-asset-location/24-02-SUMMARY.md` with:
- 신규/수정된 9개 파일 경로 + 각 파일에서 추가된 핵심 함수/엔드포인트
- API 엔드포인트 테이블 (path, method, body shape, 가드, 응답 코드)
- src/utils/api.ts 신규 메서드 시그니처 6종
- Wave 3 plan 들이 import 할 수 있는 정확한 인터페이스
- 검증한 grep gate 결과 요약
</output>
