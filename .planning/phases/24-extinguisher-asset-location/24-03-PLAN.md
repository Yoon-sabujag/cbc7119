---
phase: 24-extinguisher-asset-location
plan: 03
type: execute
wave: 3
depends_on:
  - 02
files_modified:
  - cha-bio-safety/src/pages/ExtinguishersListPage.tsx
  - cha-bio-safety/src/components/SideMenu.tsx
  - cha-bio-safety/src/components/DesktopSidebar.tsx
  - cha-bio-safety/src/utils/api.ts
  - cha-bio-safety/src/App.tsx
  - .planning/phases/24-extinguisher-asset-location/sketches/24-03-list-page.html
  - .planning/phases/24-extinguisher-asset-location/sketches/24-03-modals.html
autonomous: false
requirements:
  - EXT-05
tags:
  - frontend
  - page
  - extinguisher
  - list
must_haves:
  truths:
    - "/extinguishers 라우트에 진입하면 ExtinguishersListPage 가 렌더되고 모든 자산 카드가 표시된다"
    - "필터 바의 4개 매핑 상태 칩(전체/미배치/매핑/폐기) + zone/floor/type/검색 입력이 GET /api/extinguishers 쿼리에 반영된다"
    - "신규 등록 모달에서 7개 필드(종류/접두문자/증지번호/제조번호/형식승인/제조년월/제조업체) 입력 후 「등록」 시 skip_marker=true 모드로 자산만 등록되고 미배치 카드로 즉시 목록에 표시된다"
    - "?fromMarker= 쿼리스트링이 있으면 등록 후 자동으로 동행 마커에 assign API 호출되고 「{개소명} 위치에 배치되었습니다」 토스트 + navigate(-1) 실행"
    - "정보 수정 모달에서 변경 필드 수가 실시간으로 「변경: N / 3」 으로 표시되고 N>3 일 때 카운터 빨강 + 저장 비활성"
    - "카드 상태별 액션 버튼이 정확히 노출됨: 미매핑+미점검 → 정보 수정/배치/삭제 / 미매핑+점검 O → 정보 수정/배치/폐기 / 매핑됨 → 정보 수정/분리 / 폐기 → 조회만"
    - "삭제(hard) / 폐기 / 분리 confirmation 모달이 UI-SPEC §Copywriting 의 정확한 한글 카피와 일치한다"
    - "SideMenu / DesktopSidebar / DEFAULT_SIDE_MENU 에 「소화기」 항목이 추가되고 migrateLegacyMenuConfig 가 기존 사용자 설정에 forward-merge 로 자동 반영된다"
    - "App.tsx 의 Routes 에 /extinguishers 가 등록되고 PAGE_TITLES 에 '소화기 관리' 가 추가된다"
    - "BottomNav 가 표시되는 페이지로서 paddingBottom: calc(var(--sab) + 70px) 적용 (MOBILE_NO_NAV_PATHS 에 추가하지 않음)"
  artifacts:
    - path: "cha-bio-safety/src/pages/ExtinguishersListPage.tsx"
      provides: "신규 페이지 — 필터바 + 카드 그리드 + 등록/수정/배치/분리/폐기/삭제/스왑 모달"
      contains: "ExtinguishersListPage"
      min_lines: 400
    - path: "cha-bio-safety/src/App.tsx"
      provides: "/extinguishers 라우트 + PAGE_TITLES 항목"
      contains: "/extinguishers"
    - path: "cha-bio-safety/src/components/SideMenu.tsx"
      provides: "소화기 메뉴 항목 (시설 관리 섹션)"
      contains: "/extinguishers"
    - path: "cha-bio-safety/src/components/DesktopSidebar.tsx"
      provides: "소화기 데스크톱 사이드바 항목"
      contains: "/extinguishers"
    - path: ".planning/phases/24-extinguisher-asset-location/sketches/24-03-list-page.html"
      provides: "List page sketch (filter bar + card collapsed/expanded)"
      contains: "ExtinguishersListPage"
    - path: ".planning/phases/24-extinguisher-asset-location/sketches/24-03-modals.html"
      provides: "신규 등록 + 정보 수정 모달 sketch (counter chip 포함)"
      contains: "변경"
  key_links:
    - from: "ExtinguishersListPage"
      to: "extinguisherApi.list / update / assign / unassign / swap / dispose / remove / create"
      via: "useQuery + useMutation"
      pattern: "extinguisherApi\\.(list|update|assign|unassign|swap|dispose|remove|create)"
    - from: "SideMenu / DesktopSidebar / DEFAULT_SIDE_MENU"
      to: "/extinguishers"
      via: "migrateLegacyMenuConfig forward-merge"
      pattern: "/extinguishers"
    - from: "URL ?fromMarker=...&zone=...&floor=..."
      to: "ExtinguishersListPage register modal auto-assign 흐름"
      via: "useSearchParams + assign API 호출"
      pattern: "fromMarker"
---

<objective>
Phase 24 Wave 3a: 신규 ExtinguishersListPage 페이지 + 메뉴/라우트 wiring. 자산 등록·매핑·분리·스왑·폐기·삭제·정보수정의 모든 사용자 흐름을 단일 페이지에 통합. 마커 동행 컨텍스트(?fromMarker=) 처리 포함.

Purpose: 자산 관리의 진입점 페이지를 제공하여 5월 법정점검 시 분말 소화기 교체 동선이 자연스럽게 동작하게 한다 (EXT-05).
Output: `/extinguishers` 라우트 + 페이지 + 메뉴 항목 + sketches.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md (메모리 룰: 디자인 변경 전 상의 필수, 디자인 조정은 시안 먼저)
@.planning/phases/24-extinguisher-asset-location/CONTEXT.md
@.planning/phases/24-extinguisher-asset-location/24-RESEARCH.md
@.planning/phases/24-extinguisher-asset-location/24-PATTERNS.md
@.planning/phases/24-extinguisher-asset-location/24-UI-SPEC.md
@.planning/phases/24-extinguisher-asset-location/24-VALIDATION.md
@.planning/phases/24-extinguisher-asset-location/24-02-SUMMARY.md
@cha-bio-safety/src/pages/RemediationPage.tsx
@cha-bio-safety/src/pages/InspectionPage.tsx
@cha-bio-safety/src/pages/FloorPlanPage.tsx
@cha-bio-safety/src/utils/api.ts
@cha-bio-safety/src/utils/extinguisher.ts
@cha-bio-safety/src/components/SideMenu.tsx
@cha-bio-safety/src/components/DesktopSidebar.tsx
@cha-bio-safety/src/App.tsx
@cha-bio-safety/src/index.css

<interfaces>
<!-- From Plan 02 (already implemented in src/utils/api.ts): -->

extinguisherApi.list({ status?, mapping?, floor?, zone?, type?, q? }) → ExtinguisherListResponse
extinguisherApi.update(id, partial7Fields) → { changed?, noop? }
extinguisherApi.assign(id, checkPointId) → void
extinguisherApi.unassign(id) → { noop? }
extinguisherApi.swap(id, otherId) → void
extinguisherApi.dispose(id) → { noop? }
extinguisherApi.remove(id) → void
extinguisherApi.create({...fields, skip_marker?: true}) → { extinguisherId } | { checkPointId, mgmtNo }

ExtinguisherDetail = { mgmt_no, zone, floor, type, prefix_code, seal_no, serial_no,
                       approval_no, manufactured_at, manufacturer, status?,
                       cp_location?, cp_floor?, cp_zone?, cp_qr_code? }

ExtinguisherListResponse.items = (ExtinguisherDetail & { id, seq_no, cp_id })[]

<!-- React Query keys (project convention): ['extinguishers', filters] for list, -->
<!-- ['extinguisher', id] for detail. Invalidate ['extinguishers'] on every mutation. -->

<!-- CSS tokens — UI-SPEC §Design System 에서 lock: -->
<!-- --bg #0d1117, --bg2 #161b22, --bg3 #1c2128, --bg4 #22272e -->
<!-- --bd, --bd2, --t1 #e6edf3, --t2 #8b949e, --t3 #6e7681 -->
<!-- --acl #3b82f6, --safe, --warn, --danger #ef4444, --info -->
<!-- --sat, --sab (safe area) -->

<!-- Mapping-state badge contract (LOCKED — UI-SPEC §Color): -->
<!-- 미배치: bg rgba(239,68,68,.15) / color var(--danger) -->
<!-- 매핑됨: bg rgba(59,130,246,.15) / color var(--acl) -->
<!-- 폐기:   bg rgba(245,158,11,.15) / color var(--warn) -->
<!-- 모두: fontSize 11, fontWeight 700, padding '2px 8px', borderRadius 5 -->

<!-- 메뉴 forward-merge 패턴 (Phase 21 검증): -->
<!-- src/utils/api.ts 의 DEFAULT_SIDE_MENU 에 항목 추가 → migrateLegacyMenuConfig 가 자동 처리 -->
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: List page + 모달 sketch HTML 작성 + 사용자 승인</name>
  <files>
    .planning/phases/24-extinguisher-asset-location/sketches/24-03-list-page.html (new),
    .planning/phases/24-extinguisher-asset-location/sketches/24-03-modals.html (new)
  </files>
  <read_first>
    - .planning/phases/24-extinguisher-asset-location/24-UI-SPEC.md (전체 — 특히 §Component Inventory 1-3, §Color, §Spacing, §Typography, §Copywriting, §Sketch-First Mandate)
    - .planning/STATE.md (메모리 룰 「디자인 변경 전 상의 필수」 / 「디자인 조정은 시안 먼저」)
    - cha-bio-safety/src/pages/RemediationPage.tsx (filter bar pattern 211-222, card pattern 492-563, skeleton + empty 라인 107-112/471-490)
    - cha-bio-safety/src/pages/InspectionPage.tsx (ExtinguisherListOverlay 라인 3465-3637 — collapsed/expanded card)
    - cha-bio-safety/src/pages/FloorPlanPage.tsx (modal pattern 라인 1346-1444)
    - cha-bio-safety/src/index.css (CSS 변수 정의)
  </read_first>
  <what-built>
**메모리 룰** 「디자인 변경 전 상의 필수」 + 「디자인 조정은 시안 먼저」 강제 적용 — 인라인 적용 *전* 사용자 승인 필수.

2개 sketch HTML 파일을 작성:

1. **24-03-list-page.html** — 단일 자체 완결 HTML 파일 (CDN 의존 없이). UI-SPEC §Component Inventory item 1 의 ExtinguishersListPage 레이아웃을 구현:
   - Page header (모바일 48px, `소화기 관리` + `+ 새로 등록` acl 버튼)
   - 마커 동행 안내 배너 (info-banner 스타일, optional — both states 보여주기)
   - Filter bar 2 rows: row1 = `[전체][미배치][매핑][폐기]` chips, row2 = zone/floor/type selects + 검색 input
   - 카드 그리드 (모바일 1col): collapsed 카드 4개 + expanded 카드 1개 (다른 매핑 상태별로 4개 변형 모두 시각화: 미매핑+미점검, 미매핑+점검O, 매핑됨, 폐기)
   - Empty state (필터 매치 없음 카피 포함)
   - BottomNav placeholder (paddingBottom 시각화)
   - 모든 색/값은 UI-SPEC 의 LOCKED 토큰만 사용. 모든 한글 카피는 UI-SPEC §Copywriting verbatim.

2. **24-03-modals.html** — 단일 자체 완결 HTML 파일:
   - 신규 등록 모달 (Component Inventory #2): 7개 필드 + 종류 3-col 버튼 그리드 + info banner + 「등록」/「취소」 액션
   - 정보 수정 모달 (#3): 동일 7개 필드 + 헤더 우측 카운터 칩 (3 변형: n=0 / n=1~3 / n>3) + 변경 필드 acl 보더 시각화 + 비활성 저장 버튼 (n>3) + microcopy
   - Confirm 모달 4종 (분리 / 스왑 / 폐기 / 삭제) — UI-SPEC §Copywriting 의 정확한 한글 카피로
  </what-built>
  <how-to-verify>
사용자가 두 HTML 을 브라우저로 열어 시각 확인:
1. `open .planning/phases/24-extinguisher-asset-location/sketches/24-03-list-page.html` (mobile width 375px 부터 desktop 1440 까지 리사이즈해서 확인)
2. `open .planning/phases/24-extinguisher-asset-location/sketches/24-03-modals.html`

체크리스트:
- [ ] UI-SPEC §Color 의 mapping-state badge 3색 시각 확인 (미배치=danger, 매핑됨=acl, 폐기=warn)
- [ ] 빈 매치 empty-state 카피: `해당하는 소화기가 없습니다` + 보조 카피
- [ ] 카드 expanded 모드의 액션 버튼 3종 (height 36, fontSize 12, weight 700) 모두 보임 — 각 매핑 상태별로 정확한 버튼 조합
- [ ] 정보 수정 모달의 카운터 칩 색 변형 3종 모두 시각 확인 (n=0 회색 / 1~3 acl 파랑 / >3 danger 빨강)
- [ ] n>3 일 때 microcopy `4개 이상 변경하려면 「폐기 후 재등록」을 사용하세요.` 표시
- [ ] 4개 confirm 모달 카피가 UI-SPEC §Copywriting 와 정확히 일치
- [ ] BottomNav reservation (`paddingBottom: calc(var(--sab) + 70px)`) 시각 확인 — 카드가 BottomNav 영역에 가리지 않음

**디자인 조정 요청 시:** 사용자가 spacing/sizing/색상 변경을 요청하면 sketch HTML 만 수정하고 다시 확인. 인라인 적용은 승인 후에만.
  </how-to-verify>
  <resume-signal>사용자 "approved" 또는 "그대로 진행" — 또는 수정 요청 후 재검토</resume-signal>
  <acceptance_criteria>
    - 두 HTML 파일이 `.planning/phases/24-extinguisher-asset-location/sketches/` 에 존재
    - 각 파일은 외부 CDN 의존 없이 단일 파일로 동작 (CSS variables 인라인 정의)
    - list-page.html 에 정확히 4종의 카드 매핑 상태 모두 시각화됨 (`grep -c "미배치\|매핑됨\|폐기" 24-03-list-page.html` ≥ 4)
    - modals.html 에 카운터 칩 3변형 모두 시각화됨 (`grep -c "변경: " 24-03-modals.html` ≥ 3)
    - 4개 confirm 카피 모두 포함: `소화기 분리`, `위치 스왑`, `소화기 폐기`, `소화기 삭제`
    - 사용자 승인 received
  </acceptance_criteria>
  <action>(human-verify) UI-SPEC §Component Inventory 1-3 + §Sketch-First Mandate 적용. 2개 self-contained HTML sketch 파일 작성: list-page (filter bar 2 rows + card collapsed/expanded 4종 매핑 상태 + empty state + BottomNav reservation), modals (신규 등록 + 정보 수정 카운터 칩 3변형 + 4개 confirm). 모든 색/spacing 은 UI-SPEC LOCKED 토큰만 사용. 한글 카피는 §Copywriting verbatim. 사용자 브라우저로 열어 시각 확인 후 'approved' 응답 받음.</action>
  <verify>
    <automated>test -f .planning/phases/24-extinguisher-asset-location/sketches/24-03-list-page.html && test -f .planning/phases/24-extinguisher-asset-location/sketches/24-03-modals.html</automated>
  </verify>
  <done>두 sketch HTML 이 sketches/ 에 존재 + 사용자 승인 received.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: ExtinguishersListPage.tsx 구현 + 메뉴/라우트 wiring</name>
  <files>
    cha-bio-safety/src/pages/ExtinguishersListPage.tsx (new),
    cha-bio-safety/src/App.tsx,
    cha-bio-safety/src/components/SideMenu.tsx,
    cha-bio-safety/src/components/DesktopSidebar.tsx,
    cha-bio-safety/src/utils/api.ts (DEFAULT_SIDE_MENU 항목 추가)
  </files>
  <read_first>
    - .planning/phases/24-extinguisher-asset-location/sketches/24-03-list-page.html (Task 1 산출물)
    - .planning/phases/24-extinguisher-asset-location/sketches/24-03-modals.html (Task 1 산출물)
    - .planning/phases/24-extinguisher-asset-location/24-UI-SPEC.md (전체)
    - .planning/phases/24-extinguisher-asset-location/24-PATTERNS.md (§ ExtinguishersListPage / SideMenu / DesktopSidebar / DEFAULT_SIDE_MENU / App.tsx 패턴 + Shared Patterns 모달/Mutation/Toast/KST)
    - cha-bio-safety/src/pages/RemediationPage.tsx (filter bar / cards / skeleton / empty 패턴 — 라인 107-563)
    - cha-bio-safety/src/pages/InspectionPage.tsx (ExtinguisherListOverlay 라인 3465-3637 — 카드 collapsed/expanded 디테일)
    - cha-bio-safety/src/pages/FloorPlanPage.tsx (modal 라인 1346-1444 — 모달 wrapper geometry)
    - cha-bio-safety/src/utils/api.ts (extinguisherApi 확장 메서드들 — Plan 02 산출)
    - cha-bio-safety/src/utils/extinguisher.ts (getReplaceWarning + REPLACE_WARNING_STROKE 재사용)
    - cha-bio-safety/src/hooks/useIsDesktop.ts (있으면 — desktop 분기용)
    - cha-bio-safety/src/components/SideMenu.tsx (Phase 21 「문서 관리」 추가 패턴 line 39 부근)
    - cha-bio-safety/src/components/DesktopSidebar.tsx (DESKTOP_SECTIONS 라인 8-13)
    - cha-bio-safety/src/App.tsx (Routes 라인 220-249, PAGE_TITLES 라인 68-80, MOBILE_NO_NAV_PATHS 라인 63)
  </read_first>
  <action>

═══════════════════════════════════════

**A) `cha-bio-safety/src/pages/ExtinguishersListPage.tsx` (신규)**

UI-SPEC §Component Inventory 의 1/2/3 항목을 구현. sketch HTML 의 정확한 인라인 스타일 값을 인라인 적용. 다음 구조:

```typescript
import { useState, useMemo } from 'react'
import { useNavigate, useSearchParams } from 'react-router-dom'
import { useQuery, useMutation, useQueryClient } from '@tanstack/react-query'
import toast from 'react-hot-toast'
import { extinguisherApi, ExtinguisherDetail, ExtinguisherListResponse } from '../utils/api'
import { getReplaceWarning, REPLACE_WARNING_STROKE } from '../utils/extinguisher'
// import { useIsDesktop } from '../hooks/useIsDesktop' (있으면)

type MappingTab = 'all' | 'unmapped' | 'mapped' | 'disposed'
type Item = ExtinguisherListResponse['items'][number]

export default function ExtinguishersListPage() {
  const navigate = useNavigate()
  const [searchParams, setSearchParams] = useSearchParams()
  const qc = useQueryClient()

  // ── 마커 동행 컨텍스트 ──
  const fromMarker = searchParams.get('fromMarker')
  const ctxZone = searchParams.get('zone')
  const ctxFloor = searchParams.get('floor')
  const hasMarkerContext = !!fromMarker

  // ── 필터 상태 (URL state 도 함께 — RemediationPage 패턴) ──
  const [tab, setTab] = useState<MappingTab>(() => {
    const t = searchParams.get('tab')
    return (t === 'unmapped' || t === 'mapped' || t === 'disposed') ? t : 'all'
  })
  const [zone, setZone] = useState(searchParams.get('zone') ?? '')
  const [floor, setFloor] = useState(searchParams.get('floor') ?? '')
  const [type, setType] = useState('')
  const [q, setQ] = useState('')

  // ── 모달 상태 ──
  const [registerOpen, setRegisterOpen] = useState(false)
  const [editTarget, setEditTarget] = useState<Item | null>(null)
  const [confirmDelete, setConfirmDelete] = useState<Item | null>(null)
  const [confirmDispose, setConfirmDispose] = useState<Item | null>(null)
  const [confirmUnassign, setConfirmUnassign] = useState<Item | null>(null)
  const [swapTarget, setSwapTarget] = useState<{ from: Item; to: Item } | null>(null)
  const [expandedId, setExpandedId] = useState<number | null>(null)

  // ── List query ──
  const apiParams = useMemo(() => ({
    status: tab === 'disposed' ? '폐기' as const : tab === 'all' ? undefined : 'active' as const,
    mapping: tab === 'all' || tab === 'disposed' ? undefined : tab as 'mapped' | 'unmapped',
    zone: zone || undefined,
    floor: floor || undefined,
    type: type || undefined,
    q: q || undefined,
  }), [tab, zone, floor, type, q])

  const { data, isLoading, isError, refetch } = useQuery({
    queryKey: ['extinguishers', apiParams],
    queryFn: () => extinguisherApi.list(apiParams),
    staleTime: 30_000,
  })

  const items = data?.items ?? []

  // ── Mutations ──
  const updateMutation = useMutation({
    mutationFn: ({ id, fields }: { id: number; fields: Partial<ExtinguisherDetail> }) =>
      extinguisherApi.update(id, fields),
    onSuccess: () => { qc.invalidateQueries({ queryKey: ['extinguishers'] }); toast.success('수정 완료'); setEditTarget(null) },
    onError: (e: any) => toast.error(e?.message ?? '요청 실패'),
  })
  const assignMutation = useMutation({
    mutationFn: ({ id, cpId }: { id: number; cpId: string }) => extinguisherApi.assign(id, cpId),
    onSuccess: () => { qc.invalidateQueries({ queryKey: ['extinguishers'] }); qc.invalidateQueries({ queryKey: ['floorplan-markers'] }) },
    onError: (e: any) => toast.error(e?.message ?? '요청 실패'),
  })
  const unassignMutation = useMutation({
    mutationFn: (id: number) => extinguisherApi.unassign(id),
    onSuccess: () => { qc.invalidateQueries({ queryKey: ['extinguishers'] }); toast.success('분리 완료'); setConfirmUnassign(null) },
    onError: (e: any) => toast.error(e?.message ?? '요청 실패'),
  })
  const swapMutation = useMutation({
    mutationFn: ({ id, otherId }: { id: number; otherId: number }) => extinguisherApi.swap(id, otherId),
    onSuccess: () => { qc.invalidateQueries({ queryKey: ['extinguishers'] }); toast.success('스왑 완료'); setSwapTarget(null) },
    onError: (e: any) => toast.error(e?.message ?? '요청 실패'),
  })
  const disposeMutation = useMutation({
    mutationFn: (id: number) => extinguisherApi.dispose(id),
    onSuccess: () => { qc.invalidateQueries({ queryKey: ['extinguishers'] }); toast.success('폐기 완료'); setConfirmDispose(null) },
    onError: (e: any) => toast.error(e?.message ?? '요청 실패'),
  })
  const removeMutation = useMutation({
    mutationFn: (id: number) => extinguisherApi.remove(id),
    onSuccess: () => { qc.invalidateQueries({ queryKey: ['extinguishers'] }); toast.success('삭제 완료'); setConfirmDelete(null) },
    onError: (e: any) => toast.error(e?.message ?? '요청 실패'),
  })

  // ── 신규 등록 핸들러 (마커 동행 mode 처리) ──
  const handleRegister = async (fields: Partial<ExtinguisherDetail>) => {
    try {
      const created = await extinguisherApi.create({ ...fields, type: fields.type as string, skip_marker: true } as any)
      const newId = (created as any).extinguisherId as number | undefined
      if (!newId) throw new Error('등록 결과에 ID 없음')

      if (hasMarkerContext && fromMarker) {
        await assignMutation.mutateAsync({ id: newId, cpId: fromMarker })
        toast.success(`등록 완료 — 위치 자동 배치됨`)
        setRegisterOpen(false)
        navigate(-1)
      } else {
        toast.success('등록 완료')
        setRegisterOpen(false)
        qc.invalidateQueries({ queryKey: ['extinguishers'] })
      }
    } catch (e: any) {
      toast.error(e?.message ?? '등록 실패')
    }
  }

  // ── 카드 액션 라우팅 (배치 흐름) ──
  const handleAssignClick = (item: Item) => {
    // 미배치 카드 → 도면 페이지로 이동 (?placingExtinguisher=&zone=&floor=)
    const qs = new URLSearchParams()
    qs.set('planType', 'extinguisher')
    qs.set('placingExtinguisher', String(item.id))
    if (item.zone) qs.set('zone', item.zone)
    if (item.floor) qs.set('floor', item.floor)
    navigate(`/floorplan?${qs.toString()}`)
  }

  // ── 렌더 ──
  // 1) Page header: 48 (mobile) / 56 (desktop), bg var(--bg2), border-bottom var(--bd)
  // 2) 마커 동행 배너 (hasMarkerContext)
  // 3) Filter bar 2 rows
  // 4) Card grid (1/2/3 col responsive via inline grid-template-columns)
  // 5) Modals: register / edit / confirm-delete / confirm-dispose / confirm-unassign / confirm-swap
  // 6) BottomNav reservation: paddingBottom: 'calc(var(--sab) + 70px)' on outer scroll container

  return (
    <div style={{
      display: 'flex', flexDirection: 'column', minHeight: '100%',
      background: 'var(--bg)', color: 'var(--t1)',
      paddingBottom: 'calc(var(--sab) + 70px)',
    }}>
      {/* --- Page header --- */}
      {/* --- 동행 배너 --- */}
      {/* --- Filter bar (2 rows) --- */}
      {/* --- Cards / skeleton / empty / error --- */}
      {/* --- Modals --- */}
    </div>
  )
}
```

**구현 디테일 (executor must build all of these):**

1. **카드 (collapsed)** — UI-SPEC §Component Inventory 1, sketch 24-03-list-page.html 참고:
   - container `bg var(--bg2)`, `border 1px solid var(--bd)`, `borderRadius 12`, `padding 12`, cursor pointer
   - row1: 종류 14/700/t1 + mapping-state badge (UI-SPEC LOCKED 색상)
   - row2: 증지번호 · 제조번호 (JetBrains Mono 13/400/t2, ellipsis)
   - row3: 매핑됨이면 `📍 {zone} {floor} · {cp_location}` 11/500/t3, 미배치면 `위치 미지정` 11/500/t3
   - row4: `getReplaceWarning(item.manufactured_at)` 결과가 'none' 이 아니면 warning chip
   - 클릭 → `setExpandedId(expandedId === item.id ? null : item.id)`

2. **카드 (expanded)** — 펼침 시 추가 표시:
   - outline `border: 1.5px solid var(--acl)`
   - 디테일 grid (gridTemplateColumns: '1fr 1fr', gap '3px 12px', fontSize 11): 접두문자/형식승인/제조년월/제조업체
   - 액션 row (3 buttons, flex 1, height 36, radius 8, fontSize 12, weight 700) — **상태별 분기 (deterministic, server-driven)**:
     - **분기 키:** Plan 24-02 의 GET /api/extinguishers 응답 items 에 `has_records` (boolean — SQLite 에서 0/1 로 옴; 클라이언트는 `Boolean(item.has_records)` 또는 `item.has_records === 1` 로 판정) + `check_point_id` + `status` 3가지로 카드 상태를 결정.
     - 미매핑+미점검 (`!item.check_point_id && item.status==='active' && !item.has_records`): 정보 수정 / 소화기 배치 / **「삭제」(danger, hard delete)**
     - 미매핑+점검O (`!item.check_point_id && item.status==='active' && item.has_records`): 정보 수정 / 소화기 배치 / **「폐기」(danger, soft delete)**
     - 매핑됨 (`item.check_point_id && item.status==='active'`): 정보 수정 / 소화기 분리(danger)
     - 폐기 (`item.status==='폐기'`): opacity 0.6 + 11/t3 텍스트 `폐기된 자산입니다.` 표시, 액션 버튼 없음
   - **「삭제」와 「폐기」는 동시에 표시되지 않음 — has_records 가 source of truth.** CONTEXT 의 distinct-state matrix 를 직접 구현. 폴백/이중-버튼/409-trial 절대 사용 금지.
   - **백엔드 가드는 여전히 필수:** 클라이언트 카드 상태가 stale 한 경우(다른 staff 가 동시에 점검 입력) Plan 24-02 의 DELETE 핸들러가 서버측 `(SELECT COUNT(*) FROM check_records WHERE extinguisher_id=?) = 0` 가드로 409 + `점검 기록이 있는 자산은 삭제할 수 없습니다 — 폐기를 사용하세요` 토스트 → 카드 자동 invalidate (React Query) 로 서버 상태 재동기화.

3. **신규 등록 모달** — UI-SPEC #2 / sketch 24-03-modals.html:
   - 모달 wrapper geometry 정확히 적용 (width 90%, maxWidth 360, padding 20, borderRadius 16, bg var(--bg2), border 1px solid var(--bd2), maxHeight 80vh, overflowY auto)
   - 백드롭 click-outside 닫기, e.stopPropagation on 모달 내부
   - 헤더: `소화기 등록` 16/700/t1 marginBottom 16
   - info banner: `등록 후 한 번에 최대 3개 필드만 수정 가능합니다.` (rgba(14,165,233,.10) bg, var(--info) text, padding 8px 12px, fontSize 11, radius 8, marginBottom 14)
   - 마커 동행 시 추가 banner: `「{개소명}」 위치에 자동 매핑됩니다.` (개소명 = ctxZone? 또는 fromMarker → list 에서 lookup). 마커 동행 정보가 부족하면 단순히 `해당 위치에 자동 매핑됩니다.` 표시.
   - 필드 7개 (각: label 11/500/t3 marginBottom 6, input height 40 padding 0 12 borderRadius 8 bg var(--bg3) border 1px solid var(--bd2) color t1 fontSize 13, focused border-color var(--acl)):
     - 종류: 3-col 버튼 그리드 (분말/할로겐/이산화탄소 또는 기존 EXTINGUISHER_TYPES 상수 사용) — active state acl/white
     - 접두문자 / 증지번호 / 제조번호 (inputMode='numeric' 도 가능): text input
     - 형식승인 / 제조년월 (YYYY-MM) / 제조업체: text input
   - 액션 row: `취소` (secondary) + `등록` 또는 `등록 후 배치` (마커 동행 시) — UI-SPEC §Component Inventory #2 의 정확한 geometry

4. **정보 수정 모달** — UI-SPEC #3:
   - 동일 wrapper geometry, header `정보 수정`
   - **헤더 우측 카운터 칩** (display inline-flex, padding 2px 8px, radius 5, fontSize 11, weight 700):
     - n=0: bg var(--bg3), color var(--t3), text `변경: 0 / 3`
     - 1≤n≤3: bg rgba(59,130,246,.15), color var(--acl), text `변경: {n} / 3`
     - n>3: bg rgba(239,68,68,.15), color var(--danger) + microcopy below header `4개 이상 변경하려면 「폐기 후 재등록」을 사용하세요.` (11/danger)
   - n 계산: 입력값 vs 원본 (norm: '' → null) — 백엔드와 동일 로직
   - 변경된 필드의 input border 를 var(--acl) 로 강조
   - 저장 버튼 disabled when `n > 3 || n === 0`: `bg var(--bd2)`, `color var(--t3)`, `cursor: not-allowed`
   - mutation: `updateMutation.mutate({ id: editTarget.id, fields: changedOnly })`

5. **Confirmation 모달 4종** — UI-SPEC §Copywriting verbatim:
   - 분리 (unassign): 제목 `소화기 분리`, body `「{개소명}」 위치에서 분리합니다. 자산은 미배치 상태로 유지됩니다.`, primary `분리` (acl)
   - 스왑 (swap): 제목 `위치 스왑`, body `「{other_label}」 위치 소화기와 서로 바꿉니다. 양쪽 매핑이 동시에 변경됩니다.`, primary `스왑` (acl) — assign 충돌 응답 시 트리거 (occupant detail 별도 fetch)
   - 폐기 (dispose): 제목 `소화기 폐기`, body `이 자산은 폐기 처리되어 더 이상 매핑할 수 없습니다. 점검 이력은 보존됩니다.`, primary `폐기` (danger)
   - 삭제 (remove): 제목 `소화기 삭제`, body `이 자산은 점검 기록이 없어 영구 삭제됩니다. 되돌릴 수 없습니다.`, primary `삭제` (danger)

6. **빈 / 오류 / 로딩 상태** (RemediationPage 패턴 인용):
   - Loading: 3개 SKELETON_STYLE blink card
   - Empty: heading `해당하는 소화기가 없습니다`, body `필터를 조정하거나 우상단 「+ 새로 등록」 으로 새 자산을 추가해 주세요.`
   - Error: heading `목록을 불러오지 못했습니다`, body `네트워크 상태를 확인하고 다시 시도해 주세요.`, retry button `다시 시도` → `refetch()`

7. **반응형:** 1/2/3 col grid via inline `gridTemplateColumns: '1fr'` (mobile <768), `'1fr 1fr'` (tablet 768-1023), `'1fr 1fr 1fr'` (desktop ≥1024) — `useIsDesktop` 또는 `window.innerWidth + resize listener` (RemediationPage 패턴 확인 후 동일 사용).

═══════════════════════════════════════

**B) `cha-bio-safety/src/components/SideMenu.tsx` (수정)**

기존 「시설 관리」 섹션의 items 배열에 새 항목 추가 (`소방 시설 도면` 뒤, `소방 점검 관리` 앞):

```typescript
{ section: '시설 관리', items: [
  { label: 'DIV 압력 관리',   path: '/div',           badge: 0, soon: false },
  { label: '소방 시설 도면',   path: '/floorplan',     badge: 0, soon: false },
  { label: '소화기',          path: '/extinguishers', badge: 0, soon: false },  // NEW Phase 24
  { label: '소방 점검 관리',   path: '/legal',         badge: 0, soon: false },
  { label: '소방 시설 추가',   path: '/checkpoints',   badge: 0, soon: false, role: 'admin' },
]},
```

**read_first 로 정확한 현재 배열을 확인 후 위 위치에 1줄 추가.**

═══════════════════════════════════════

**C) `cha-bio-safety/src/components/DesktopSidebar.tsx` (수정)**

DESKTOP_SECTIONS 의 「시설 관리」 paths 배열에 `'/extinguishers'` 추가:

```typescript
{ label: '시설 관리', paths: ['/div', '/floorplan', '/extinguishers', '/legal', '/elevator', '/checkpoints'] },
```

**read_first 로 정확한 기존 paths 순서를 확인 후 동일 위치에 1줄 삽입.**

═══════════════════════════════════════

**D) `cha-bio-safety/src/utils/api.ts` 의 `DEFAULT_SIDE_MENU` (수정)**

「시설 관리」 divider 다음 item 들 사이에 `/extinguishers` 항목 추가:

```typescript
{ type: 'divider', id: 'd-facility', title: '시설 관리' },
{ type: 'item', path: '/div',           visible: true },
{ type: 'item', path: '/floorplan',     visible: true },
{ type: 'item', path: '/extinguishers', visible: true },  // NEW Phase 24
{ type: 'item', path: '/legal',         visible: true },
{ type: 'item', path: '/checkpoints',   visible: true },
```

**migrateLegacyMenuConfig 는 변경 불필요** — 기존 forward-merge 로직이 자동으로 누락 항목 추가 (Phase 21 검증 패턴).

═══════════════════════════════════════

**E) `cha-bio-safety/src/App.tsx` (수정)**

세 곳을 변경:

1. **Imports**: `import ExtinguishersListPage from './pages/ExtinguishersListPage'` 추가 (lazy import 가 표준이면 그 패턴 사용 — read_first 로 기존 패턴 확인).

2. **PAGE_TITLES** 객체에 추가:
```typescript
'/extinguishers': '소화기 관리',
```

3. **Routes** 블록의 `/floorplan` 라우트와 `/checkpoints` 라우트 사이에 추가:
```tsx
<Route path="/extinguishers" element={<Auth><ExtinguishersListPage /></Auth>} />
```

**MOBILE_NO_NAV_PATHS 에는 추가하지 않음** — UI-SPEC §Mobile vs Desktop 명시: BottomNav 표시 페이지.

═══════════════════════════════════════

**F) typecheck 통과:**

```bash
cd cha-bio-safety && npx tsc --noEmit
```

**검증 사항 (executor 가 직접 grep 으로 확인):**
- `ExtinguishersListPage` 파일 라인 수 ≥ 400 (모달 5종 + 카드 분기 포함)
- 6개 mutation 모두 정의됨 (`update|assign|unassign|swap|dispose|remove`)
- 카운터 칩 색 분기 3종 (n=0 / 1-3 / >3) 모두 코드에 존재
- 4개 confirm 모달 카피 verbatim
- 메뉴 3곳 (`SideMenu.tsx` / `DesktopSidebar.tsx` / `api.ts DEFAULT_SIDE_MENU`) 에 `/extinguishers` 추가
- `App.tsx` Routes 에 `/extinguishers` 추가, `PAGE_TITLES` 에 `'소화기 관리'` 추가
- `MOBILE_NO_NAV_PATHS` 에 `/extinguishers` 미포함 (`grep -c "'/extinguishers'" src/App.tsx | awk '...'` 로 확인 — Routes/PAGE_TITLES 두 군데만 매치)
  </action>
  <verify>
    <automated>cd cha-bio-safety && npx tsc --noEmit 2>&1 | tail -10 && wc -l src/pages/ExtinguishersListPage.tsx | awk '{ if($1<400) print "TOO SHORT: "$1; else print "OK: "$1 }' && grep -c "extinguisherApi.update\|extinguisherApi.assign\|extinguisherApi.unassign\|extinguisherApi.swap\|extinguisherApi.dispose\|extinguisherApi.remove" src/pages/ExtinguishersListPage.tsx && grep -c "/extinguishers" src/components/SideMenu.tsx && grep -c "/extinguishers" src/components/DesktopSidebar.tsx && grep -c "/extinguishers" src/utils/api.ts && grep -c "/extinguishers" src/App.tsx && grep -c "변경: " src/pages/ExtinguishersListPage.tsx && grep -c "소화기 관리" src/App.tsx && grep -c "has_records" src/pages/ExtinguishersListPage.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/pages/ExtinguishersListPage.tsx` 존재, 라인 수 ≥ 400
    - `extinguisherApi.update|assign|unassign|swap|dispose|remove` 6 종 모두 호출됨 (`grep -c` 합 ≥ 6)
    - `extinguisherApi.create` 도 호출됨 (`skip_marker: true` 인자 포함)
    - `useSearchParams` import + 사용
    - `useNavigate` 사용 + `navigate(-1)` (마커 동행 자동 복귀) 또는 `navigate('/floorplan?...placingExtinguisher=...')` 호출
    - `mapping-state badge` 3종 색상 모두 코드에 존재 (`grep -c "rgba(239,68,68,.15)\|rgba(59,130,246,.15)\|rgba(245,158,11,.15)" src/pages/ExtinguishersListPage.tsx` ≥ 3)
    - 카운터 텍스트 `변경: ` 코드에 존재
    - `4개 이상 변경하려면` microcopy 코드에 존재
    - 4 confirm 카피 모두 코드에 존재: `소화기 분리`, `위치 스왑`, `소화기 폐기`, `소화기 삭제`
    - **카드 상태 머신이 `item.has_records` 를 읽어서 분기:** `has_records` 식별자가 ExtinguishersListPage.tsx 에 1+ 매치 (`grep -c "has_records" src/pages/ExtinguishersListPage.tsx` ≥ 1). 해당 라인이 三항 또는 if 분기로 「삭제」/「폐기」 버튼 라벨을 결정하는 로직과 인접해야 함.
    - 카드의 「삭제」 버튼 onClick handler 가 `extinguisherApi.remove(item.id)` 호출 (hard delete) — `grep -A3 "삭제.*onClick\|onClick.*삭제" src/pages/ExtinguishersListPage.tsx | grep -c "extinguisherApi.remove"` ≥ 1
    - 카드의 「폐기」 버튼 onClick handler 가 `extinguisherApi.dispose(item.id)` 호출 (soft delete)
    - `해당하는 소화기가 없습니다` empty heading 코드에 존재
    - SideMenu.tsx 의 「시설 관리」 섹션 items 배열에 `path: '/extinguishers'` 1줄 추가됨
    - DesktopSidebar.tsx 의 「시설 관리」 paths 배열에 `'/extinguishers'` 1개 추가됨
    - src/utils/api.ts 의 DEFAULT_SIDE_MENU 에 `/extinguishers` item 추가됨 (`migrateLegacyMenuConfig` 함수 본체는 변경 X)
    - App.tsx Routes 블록에 `<Route path="/extinguishers"` 1+ 매치
    - App.tsx PAGE_TITLES 에 `'/extinguishers': '소화기 관리'` 1+ 매치
    - App.tsx MOBILE_NO_NAV_PATHS 에 `/extinguishers` 0 매치 (BottomNav 표시 정책)
    - `npx tsc --noEmit` 0 errors
  </acceptance_criteria>
  <done>ExtinguishersListPage 가 sketch 정합 + 6개 mutation 통합 + 메뉴/라우트 wiring 완료 + 타입 체크 통과.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL query string → page state | `?fromMarker=`, `?zone=`, `?floor=` 는 untrusted — assign 호출 전 백엔드가 cp 존재 확인 (Plan 02 가드) |
| 클라이언트 카운터(≤3) | UX 만 — 백엔드가 final enforce |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-17 | Tampering | URL ?fromMarker= 위변조 (없는 cp_id) | mitigate | Plan 02 의 assign 핸들러가 cp 존재 + 활성 검증. 클라이언트는 결과 토스트로 처리. |
| T-24-18 | Tampering | 클라이언트 ≤3 카운터 우회 | mitigate | Plan 02 의 PUT 가 norm + diff count → 400. 클라이언트 disabled 우회해도 서버 거부. |
| T-24-19 | Information Disclosure | 토스트 메시지에 서버 raw error | mitigate | catch 에서 `e?.message ?? '요청 실패'` — ApiError 패턴이 이미 표준 한글 메시지. |
| T-24-20 | Spoofing | 라우트 보호 누락 (인증 없이 진입) | mitigate | App.tsx 의 `<Route element={<Auth><ExtinguishersListPage /></Auth>}>` 으로 _middleware 미들웨어 패턴 강제 |
| T-24-21 | Tampering | localStorage MenuConfig 위변조로 항목 누락 | accept | migrateLegacyMenuConfig 가 forward-merge — 누락 시 자동 추가 (Phase 21 검증). |
</threat_model>

<verification>
- 두 sketch HTML 이 사용자 승인 받음
- ExtinguishersListPage 가 sketch 의 모든 시각 요소 인라인 적용
- 6개 mutation 모두 `qc.invalidateQueries({ queryKey: ['extinguishers'] })` 호출
- 메뉴 3곳 + App.tsx 4곳 모두 `/extinguishers` 추가됨
- typecheck 통과
- 기존 점검/도면 라우트 회귀 없음 (App.tsx Routes 다른 부분 변경 X)
</verification>

<success_criteria>
- 사용자가 sketch HTML 승인 후 인라인 적용 완료
- /extinguishers 라우트로 진입 시 페이지가 정상 렌더 (로컬 빌드 확인)
- ExtinguishersListPage 가 sketch 와 시각 일치
- 메뉴에 「소화기」 항목 추가됨 (forward-merge 로 기존 사용자 자동 반영)
- typecheck 0 errors
</success_criteria>

<output>
After completion, create `.planning/phases/24-extinguisher-asset-location/24-03-SUMMARY.md` with:
- 신규/수정 파일 5종 + sketches 2개
- ExtinguishersListPage 라인 수 + 모달 5종 분기 (등록/수정/분리 confirm/스왑 confirm/폐기 confirm/삭제 confirm)
- 메뉴 3곳 + App.tsx 변경 위치
- Plan 04, Plan 05 가 사용할 수 있는 navigate 헬퍼 (`/extinguishers?fromMarker=&zone=&floor=`)
- typecheck 결과
</output>
