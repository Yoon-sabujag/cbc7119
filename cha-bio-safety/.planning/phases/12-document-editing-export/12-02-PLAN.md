---
phase: 12-document-editing-export
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - src/pages/DailyReportPage.tsx
autonomous: false
requirements: [DOC-01, DOC-02, DOC-03]

must_haves:
  truths:
    - "사용자가 데스크톱에서 일일업무일지 편집 좌측 + A4 세로 미리보기 우측 2분할 레이아웃을 볼 수 있다"
    - "모바일에서 기존 DailyReportPage가 그대로 동작한다"
  artifacts:
    - path: "src/pages/DailyReportPage.tsx"
      provides: "Desktop 2-panel layout with portrait ExcelPreview"
      contains: "useIsDesktop"
  key_links:
    - from: "src/pages/DailyReportPage.tsx"
      to: "src/components/ExcelPreview.tsx"
      via: "import ExcelPreview for portrait preview"
      pattern: "ExcelPreview"
---

<objective>
Add desktop 2-panel layout to DailyReportPage (edit left, A4 portrait preview right) and visually verify the complete Phase 12 output.

Purpose: Complete the desktop document viewing experience with portrait document support (D-06, D-07, D-12), and confirm all Phase 12 layouts work correctly.
Output: DailyReportPage with desktop 2-panel wrapper, visual verification of all Phase 12 features.

Note: DOC-02 (소방계획서) and DOC-03 (소방훈련 자료) are DEFERRED per CONTEXT.md — user will discuss with manager. These requirement IDs are listed in this plan's frontmatter for traceability but NO implementation is done for them.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-document-editing-export/12-CONTEXT.md
@.planning/phases/12-document-editing-export/12-UI-SPEC.md
@.planning/phases/12-document-editing-export/12-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 output -->

From src/components/ExcelPreview.tsx (created in Plan 01):
```typescript
interface ExcelPreviewProps {
  reportType: ReportType | null
  year: number
  month?: number
}
export function ExcelPreview({ reportType, year, month }: ExcelPreviewProps): JSX.Element
```

From src/hooks/useIsDesktop.ts:
```typescript
export function useIsDesktop(): boolean
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add desktop 2-panel layout to DailyReportPage</name>
  <files>src/pages/DailyReportPage.tsx</files>
  <read_first>
    - src/pages/DailyReportPage.tsx (full file — understand existing form layout, state, data flow)
    - src/components/ExcelPreview.tsx (created in Plan 01 — understand props interface, portrait support)
    - src/hooks/useIsDesktop.ts (hook signature)
    - .planning/phases/12-document-editing-export/12-UI-SPEC.md (DesktopDailyLayout section)
  </read_first>
  <action>
**Modify `src/pages/DailyReportPage.tsx`** — per D-06, D-07, D-12:

Add imports: `import { useIsDesktop } from '../hooks/useIsDesktop'`

At top of component, add: `const isDesktop = useIsDesktop()`

For desktop rendering, wrap the existing form content in a 2-panel layout:

Desktop layout structure (per UI-SPEC DesktopDailyLayout):
- Outer: `display: flex; flexDirection: row; height: 100%; overflow: hidden`
- Left panel (existing form): `flex: 1; overflow: auto; padding: 24px 32px` — move ALL existing DailyReportPage JSX content (header, form cards, textareas, buttons) into this panel. Remove the mobile header (back button + title) when on desktop since the App.tsx desktop header already shows the page title.
- Right panel (preview): `width: 400px; flexShrink: 0; borderLeft: 1px solid var(--bd); overflow: hidden; background: var(--bg); display: flex; alignItems: center; justifyContent: center; position: relative`
  - Label at top: `position: absolute; top: 8px; left: 0; right: 0; textAlign: center; fontSize: 11px; color: var(--t2); fontWeight: 700; textTransform: uppercase` — text: "인쇄 미리보기"
  - Note: DailyReportPage doesn't use the same report types as ReportsPage. For the portrait preview, show a simple formatted text preview of the daily report content (date, duty roster, inspection summary, notes) styled as a portrait A4 page. Use the ExcelPreview's inner paper div pattern but with portrait dimensions (width: 794, height: 1123). If ExcelPreview doesn't support DailyReport data format, create a simple inline preview div with portrait A4 dimensions, white background, transform:scale() using the same ResizeObserver pattern, and render the daily report text content in a readable format.

For mobile rendering: return existing JSX unchanged (early return pattern: `if (!isDesktop) return <existing mobile JSX />`).

The mobile header with back button should remain for mobile. On desktop, hide it (the App.tsx 48px header handles page title).
  </action>
  <verify>
    <automated>cd /Users/jykevin/Documents/20260328/cha-bio-safety && grep -q "useIsDesktop" src/pages/DailyReportPage.tsx && grep -q "인쇄 미리보기" src/pages/DailyReportPage.tsx && npx tsc --noEmit 2>&1 | head -10</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "useIsDesktop" src/pages/DailyReportPage.tsx (desktop detection added)
    - grep -q "isDesktop" src/pages/DailyReportPage.tsx (conditional rendering)
    - grep -q "인쇄 미리보기" src/pages/DailyReportPage.tsx (preview panel label)
    - grep -q "width: 400" src/pages/DailyReportPage.tsx (right panel fixed width) OR grep -q "400px" src/pages/DailyReportPage.tsx
  </acceptance_criteria>
  <done>
    Desktop DailyReportPage shows 2-panel layout: editing form on left, portrait A4 preview on right with "인쇄 미리보기" label. Mobile layout unchanged.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of Phase 12 desktop layouts</name>
  <files>none</files>
  <action>
Human visually verifies all Phase 12 desktop layouts. No code changes — verification only.

Complete Phase 12 desktop document viewing:
1. ReportsPage 3-panel layout (tabs + item list + A4 landscape preview)
2. DailyReportPage 2-panel layout (edit form + A4 portrait preview)
3. @media print styles (sidebar/header hidden, only A4 content printed)
4. Excel download functionality preserved

Verification steps:
1. Open app on desktop browser (1024px+ width)
2. Navigate to "점검 일지 출력" (Reports page)
   - Verify: 3-panel layout visible — tabs at top, item list on left (280px), A4 preview on right
   - Click different tabs (유수검지, 소화전, 피난방화, 제연, 자탐, 소방펌프) — item list updates
   - Select different items — A4 preview updates with data
   - Change year/month filters — preview refreshes
   - Click "엑셀 다운로드" — Excel file downloads
   - Click "인쇄" or Ctrl+P — print preview shows only A4 content (no sidebar, no tabs, no list)
3. Navigate to "일일업무일지" (DailyReport page)
   - Verify: 2-panel layout — form on left, portrait preview on right
   - Verify: "인쇄 미리보기" label visible at top of right panel
4. Resize browser to mobile width (less than 1024px)
   - Verify: ReportsPage shows original card list layout
   - Verify: DailyReportPage shows original single-column layout
  </action>
  <verify>User types "approved" or describes issues</verify>
  <done>User confirms all Phase 12 desktop layouts render correctly, print styles work, mobile unchanged.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes
2. Desktop DailyReportPage: 2-panel layout with form + portrait preview
3. Mobile DailyReportPage: unchanged
4. All Phase 12 features working: 3-panel reports, 2-panel daily, print styles, Excel download
</verification>

<success_criteria>
- DailyReportPage desktop 2-panel layout renders correctly
- Mobile layouts for both pages completely unchanged
- All Phase 12 visual features confirmed by user
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/12-document-editing-export/12-02-SUMMARY.md`
</output>
