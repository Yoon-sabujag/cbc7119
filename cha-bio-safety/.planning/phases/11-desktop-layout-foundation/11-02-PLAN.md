---
phase: 11-desktop-layout-foundation
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - src/App.tsx
  - src/components/SettingsPanel.tsx
autonomous: false
requirements:
  - LAYOUT-01
  - LAYOUT-02
  - LAYOUT-03
  - LAYOUT-04

must_haves:
  truths:
    - "PC(1024px+)에서 좌측에 DesktopSidebar + 상단에 간소화 헤더가 보인다"
    - "PC에서 BottomNav/GlobalHeader/SideMenu 드로어가 표시되지 않는다"
    - "PC에서 main 영역이 스크롤 가능하다 (paddingBottom phantom gap 없음)"
    - "모바일(1023px-)에서 기존 GlobalHeader + BottomNav + SideMenu 드로어가 동일하게 동작한다"
    - "데스크톱에서 SettingsPanel이 화면 하단에 정상 배치된다 (BottomNav 높이 계산 없음)"
    - "데스크톱에서 NO_NAV_PATHS의 모든 경로가 사이드바 포함 레이아웃으로 표시된다"
  artifacts:
    - path: "src/App.tsx"
      provides: "isDesktop 분기 Layout 함수"
      contains: "useIsDesktop"
    - path: "src/components/SettingsPanel.tsx"
      provides: "데스크톱 대응 bottom 위치"
      contains: "isDesktop"
  key_links:
    - from: "src/App.tsx"
      to: "src/hooks/useIsDesktop.ts"
      via: "import useIsDesktop"
      pattern: "import.*useIsDesktop.*from.*hooks/useIsDesktop"
    - from: "src/App.tsx"
      to: "src/components/DesktopSidebar.tsx"
      via: "conditional render"
      pattern: "isDesktop.*&&.*showNav.*&&.*DesktopSidebar"
    - from: "src/App.tsx"
      to: "src/components/SettingsPanel.tsx"
      via: "isDesktop prop"
      pattern: "SettingsPanel.*isDesktop"
---

<objective>
App.tsx Layout 함수를 리팩터링하여 데스크톱/모바일 분기 렌더링을 구현하고, SettingsPanel의 데스크톱 위치를 수정한다.

Purpose: Plan 01에서 생성한 빌딩 블록(useIsDesktop, DesktopSidebar)을 Layout에 통합하여 데스크톱 레이아웃을 완성한다.
Output: 데스크톱에서 사이드바+헤더 레이아웃, 모바일에서 기존 레이아웃이 동작하는 완성된 셸
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-desktop-layout-foundation/11-CONTEXT.md
@.planning/phases/11-desktop-layout-foundation/11-UI-SPEC.md
@.planning/phases/11-desktop-layout-foundation/11-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs -->
From src/hooks/useIsDesktop.ts:
```typescript
export function useIsDesktop(): boolean
// Returns true when viewport >= 1024px
```

From src/components/DesktopSidebar.tsx:
```typescript
interface DesktopSidebarProps {
  unresolvedCount: number
  onSettingsOpen: () => void
}
export function DesktopSidebar(props: DesktopSidebarProps): JSX.Element
```

From src/App.tsx (current Layout function structure):
```typescript
// Current: single-path mobile layout
// NO_NAV_PATHS = ['/', '/login', '/schedule', '/reports', '/workshift', '/leave', '/floorplan', '/div', '/qr-print', '/daily-report', '/meal', '/education', '/admin', '/legal', '/elevator/findings']
// PAGE_TITLES: Record<string, string> — 6 entries
// showNav = isAuthenticated && !NO_NAV_PATHS.includes(location.pathname) && !location.pathname.match(...)
```

From src/components/SettingsPanel.tsx:
```typescript
interface Props {
  open: boolean
  onClose: () => void
}
// Current: no isDesktop prop
// bottom: 'calc(54px + var(--sab, 34px) - var(--sat, 0px))'
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: App.tsx Layout 리팩터링 + SettingsPanel 수정</name>
  <files>src/App.tsx, src/components/SettingsPanel.tsx</files>
  <read_first>
    - src/App.tsx (현재 Layout 함수 전체, NO_NAV_PATHS, PAGE_TITLES)
    - src/components/SettingsPanel.tsx (현재 Props 인터페이스, bottom 위치 스타일)
    - src/components/DesktopSidebar.tsx (Plan 01에서 생성한 props 확인)
    - src/hooks/useIsDesktop.ts (export 확인)
    - .planning/phases/11-desktop-layout-foundation/11-UI-SPEC.md (Layout Patterns 섹션)
  </read_first>
  <action>
    **1) src/App.tsx 수정:**

    **Import 추가:**
    ```typescript
    import { useIsDesktop } from './hooks/useIsDesktop'
    import { DesktopSidebar } from './components/DesktopSidebar'
    ```

    **NO_NAV_PATHS 변경:**
    데스크톱에서는 거의 모든 페이지에 사이드바를 표시해야 한다. 현재 NO_NAV_PATHS에는 schedule, reports, workshift 등 자체 헤더가 있는 페이지들이 포함되어 있는데, 데스크톱에서는 이 페이지들도 사이드바가 필요하다.

    모바일 전용 NO_NAV 목록과 데스크톱 전용 NO_NAV 목록을 분리한다:
    ```typescript
    // 모바일: 기존과 동일 — 자체 헤더가 있는 페이지는 nav 숨김
    const MOBILE_NO_NAV_PATHS = ['/', '/login', '/schedule', '/reports', '/workshift', '/leave', '/floorplan', '/div', '/qr-print', '/daily-report', '/meal', '/education', '/admin', '/legal', '/elevator/findings']

    // 데스크톱: 로그인/스플래시만 nav 숨김 — 나머지는 모두 사이드바 표시
    const DESKTOP_NO_NAV_PATHS = ['/', '/login']
    ```

    **PAGE_TITLES 확장:**
    데스크톱 헤더에 모든 페이지 타이틀을 표시해야 하므로 기존 6개에 추가:
    ```typescript
    const PAGE_TITLES: Record<string, string> = {
      '/dashboard': '대시보드',
      '/inspection': '소방 점검',
      '/inspection/qr': 'QR 스캔',
      '/remediation': '조치 관리',
      '/elevator': '승강기 관리',
      '/staff-service': '연차 및 식사',
      '/schedule': '월간 점검 계획',
      '/reports': '점검 일지 출력',
      '/daily-report': '일일업무일지',
      '/workshift': '근무표',
      '/leave': '연차 관리',
      '/floorplan': '건물 도면',
      '/div': 'DIV 압력 관리',
      '/qr-print': 'QR 코드 출력',
      '/meal': '식사 관리',
      '/education': '보수교육',
      '/admin': '관리자 설정',
      '/legal': '법적 점검',
    }
    ```

    **Layout 함수 리팩터링:**
    ```typescript
    function Layout() {
      const isDesktop = useIsDesktop()
      const { isAuthenticated } = useAuthStore()
      const location = useLocation()

      const noNavPaths = isDesktop ? DESKTOP_NO_NAV_PATHS : MOBILE_NO_NAV_PATHS
      const showNav = isAuthenticated
        && !noNavPaths.includes(location.pathname)
        && !location.pathname.match(/^\/remediation\/.+/)
        && !location.pathname.match(/^\/legal\/.+/)
        && !location.pathname.match(/^\/elevator\/findings\/.+/)

      const [sideOpen, setSideOpen] = useState(false)
      const [settingsOpen, setSettingsOpen] = useState(false)

      const { data: dashData } = useQuery({
        queryKey: ['dashboard'],
        queryFn: dashboardApi.getStats,
        staleTime: 30_000,
        refetchInterval: 30_000,
        enabled: isAuthenticated,
      })
      const unresolvedCount = dashData?.stats?.unresolved ?? 0
      const datetime = useDateTime()
      const dateOnly = datetime.split('  ')[0]
      const pageTitle = PAGE_TITLES[location.pathname] || ''
      const isDashboard = location.pathname === '/dashboard'

      // 기존 dashboardRightSlot 유지 (모바일 전용이지만 코드 삭제 불필요)
      const dashboardRightSlot = (/* 기존 코드 그대로 */)

      return (
        <div style={{
          display: 'flex',
          height: '100dvh',
          overflow: 'hidden',
        }}>
          {/* 데스크톱: 280px 고정 사이드바 (per D-03, D-06, D-08) */}
          {isDesktop && showNav && (
            <DesktopSidebar
              unresolvedCount={unresolvedCount}
              onSettingsOpen={() => setSettingsOpen(true)}
            />
          )}

          {/* 콘텐츠 영역 */}
          <div style={{
            flex: 1,
            minHeight: 0,
            display: 'flex',
            flexDirection: 'column',
            overflow: 'hidden',
            paddingTop: (!isDesktop) ? 'var(--sat, 0px)' : 0,
          }}>
            {/* 모바일 전용: GlobalHeader (per D-08) */}
            {!isDesktop && showNav && (
              <GlobalHeader
                title={isDashboard ? dateOnly : pageTitle}
                onMenuOpen={() => setSideOpen(true)}
                rightSlot={isDashboard ? dashboardRightSlot : undefined}
              />
            )}
            {/* 모바일 전용: SideMenu drawer (per D-08) */}
            {!isDesktop && showNav && (
              <SideMenu open={sideOpen} onClose={() => setSideOpen(false)} unresolvedCount={unresolvedCount} />
            )}

            {/* 데스크톱: 간소화된 헤더 48px (per D-07) */}
            {isDesktop && showNav && (
              <header style={{
                height: 48,
                display: 'flex',
                alignItems: 'center',
                padding: '0 16px',
                background: 'var(--bg2)',
                borderBottom: '1px solid var(--bd)',
                flexShrink: 0,
              }}>
                <span style={{
                  fontSize: 12,
                  fontWeight: 700,
                  color: 'var(--t1)',
                  flex: 1,
                }}>
                  {isDashboard ? '대시보드' : pageTitle}
                </span>
                <button
                  onClick={() => setSettingsOpen(true)}
                  style={{
                    width: 32, height: 32, borderRadius: 7,
                    background: 'var(--bg3)', border: 'none',
                    color: 'var(--t2)', cursor: 'pointer',
                    display: 'flex', alignItems: 'center', justifyContent: 'center',
                  }}
                >
                  <svg width={15} height={15} fill="none" viewBox="0 0 24 24" stroke="var(--t2)" strokeWidth={1.8}>
                    <path strokeLinecap="round" strokeLinejoin="round" d="M10.325 4.317c.426-1.756 2.924-1.756 3.35 0a1.724 1.724 0 002.573 1.066c1.543-.94 3.31.826 2.37 2.37a1.724 1.724 0 001.065 2.572c1.756.426 1.756 2.924 0 3.35a1.724 1.724 0 00-1.066 2.573c.94 1.543-.826 3.31-2.37 2.37a1.724 1.724 0 00-2.572 1.065c-.426 1.756-2.924 1.756-3.35 0a1.724 1.724 0 00-2.573-1.066c-1.543.94-3.31-.826-2.37-2.37a1.724 1.724 0 00-1.065-2.572c-1.756-.426-1.756-2.924 0-3.35a1.724 1.724 0 001.066-2.573c-.94-1.543.826-3.31 2.37-2.37.996.608 2.296.07 2.572-1.065z"/>
                    <circle cx="12" cy="12" r="3"/>
                  </svg>
                </button>
              </header>
            )}

            {/* 설정 패널 — 모바일/데스크톱 공통 */}
            <SettingsPanel open={settingsOpen} onClose={() => setSettingsOpen(false)} isDesktop={isDesktop && showNav} />

            {/* 페이지 콘텐츠 */}
            <main style={{
              flex: 1,
              minHeight: 0,
              overflow: 'auto',
              paddingBottom: (!isDesktop && showNav) ? 'calc(54px + var(--sab, 34px))' : 0,
            }}>
              <Suspense fallback={<Loader />}>
                <Routes>
                  {/* 기존 Route 전체 유지 — 변경 없음 */}
                </Routes>
              </Suspense>
            </main>

            {/* 모바일 전용: BottomNav (per D-08) */}
            {!isDesktop && showNav && <BottomNav unresolvedCount={unresolvedCount} />}
          </div>
        </div>
      )
    }
    ```

    핵심 변경점:
    - 최상위 div: `display: 'flex'`로 변경 (기존은 `flexDirection: 'column'`). 사이드바와 콘텐츠를 수평 배치.
    - DesktopSidebar: `isDesktop && showNav`일 때만 렌더.
    - GlobalHeader, SideMenu, BottomNav: `!isDesktop && showNav`일 때만 렌더.
    - paddingBottom: `!isDesktop && showNav`일 때만 BottomNav 높이. 데스크톱은 0.
    - paddingTop: 모바일만 safe area top. 데스크톱은 0.
    - SettingsPanel: 대시보드 조건(`isDashboard && showNav`) 제거 — 데스크톱에서 모든 페이지에서 설정 접근 가능하도록. `isDesktop` prop 전달.
    - main의 overflow: `'auto'`로 설정 (데스크톱에서 페이지 스크롤 활성화). 기존 `overflow: 'hidden'`에서 변경.

    **2) src/components/SettingsPanel.tsx 수정:**

    Props 인터페이스에 `isDesktop` 추가:
    ```typescript
    interface Props {
      open: boolean
      onClose: () => void
      isDesktop?: boolean
    }
    ```

    컴포넌트 함수 시그니처 변경:
    ```typescript
    export function SettingsPanel({ open, onClose, isDesktop = false }: Props) {
    ```

    패널 스타일에서 `bottom`과 `top` 값을 isDesktop 기반으로 분기:
    - `bottom`: `isDesktop ? 0 : 'calc(54px + var(--sab, 34px) - var(--sat, 0px))'`
    - `top`: `isDesktop ? 48 : 'var(--sat, 0px)'`

    이 변경은 SettingsPanel의 position fixed 스타일에만 적용. 패널 내부 콘텐츠/기능은 변경하지 않는다.
  </action>
  <verify>
    <automated>cd /Users/jykevin/Documents/20260328/cha-bio-safety && grep -n "useIsDesktop" src/App.tsx && grep -n "DesktopSidebar" src/App.tsx && grep -n "DESKTOP_NO_NAV_PATHS" src/App.tsx && grep -n "isDesktop.*SettingsPanel\|SettingsPanel.*isDesktop" src/App.tsx && grep -n "isDesktop" src/components/SettingsPanel.tsx | head -5 && npx tsc --noEmit 2>&1 | head -20 && npm run build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - src/App.tsx에 `import { useIsDesktop }` 존재
    - src/App.tsx에 `import { DesktopSidebar }` 존재
    - src/App.tsx에 `DESKTOP_NO_NAV_PATHS` 배열 존재 (값: `['/', '/login']`)
    - src/App.tsx에 `MOBILE_NO_NAV_PATHS` 배열 존재 (기존 NO_NAV_PATHS와 동일값)
    - src/App.tsx Layout에서 `isDesktop && showNav` 와 `DesktopSidebar` 패턴 존재
    - src/App.tsx Layout에서 `!isDesktop && showNav` 와 `BottomNav` 패턴 존재
    - src/App.tsx Layout에서 `!isDesktop && showNav` 와 `GlobalHeader` 패턴 존재
    - src/App.tsx에서 paddingBottom 조건이 `!isDesktop && showNav` 포함
    - src/components/SettingsPanel.tsx에 `isDesktop?: boolean` prop 존재
    - src/components/SettingsPanel.tsx에 `isDesktop ? 0 :` bottom 분기 존재
    - `npm run build` 성공 (exit code 0)
  </acceptance_criteria>
  <done>데스크톱에서 DesktopSidebar + 간소화 헤더 레이아웃이 렌더되고, 모바일에서 기존 레이아웃이 유지된다</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 데스크톱/모바일 레이아웃 시각 검증</name>
  <files>src/App.tsx</files>
  <action>
    사용자가 데스크톱/모바일 레이아웃을 직접 브라우저에서 확인한다. `npm run dev`로 개발 서버를 시작하고 http://localhost:5173 에서 테스트한다.
  </action>
  <verify>
    <automated>cd /Users/jykevin/Documents/20260328/cha-bio-safety && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>사용자가 데스크톱(1024px+)에서 사이드바 레이아웃, 모바일(1023px-)에서 기존 레이아웃을 확인하고 "approved" 입력</done>
  <what-built>
    데스크톱(1024px+) 레이아웃: 좌측 280px DesktopSidebar + 상단 48px 헤더 + main 스크롤 영역.
    모바일(1023px-) 레이아웃: 기존 GlobalHeader + BottomNav + SideMenu 드로어 동일 유지.
  </what-built>
  <how-to-verify>
    1. `npm run dev` 실행 후 PC 브라우저에서 http://localhost:5173 접속
    2. 로그인 후 대시보드 확인:
       - 좌측에 280px 사이드바가 보이는가? (4개 섹션: 점검 현황/문서 관리/직원 관리/시설 관리)
       - 상단에 48px 간소화 헤더가 보이는가? (페이지 타이틀 + 설정 버튼)
       - BottomNav가 보이지 않는가?
       - 메인 콘텐츠 영역이 스크롤 가능한가?
    3. 사이드바 메뉴 클릭하여 각 페이지 이동 확인 (5개 이상 테스트)
    4. 사이드바 하단에 사용자 이름 + 역할 + 로그아웃 버튼이 있는가?
    5. 섹션 라벨 클릭 시 접힘/펼침이 동작하는가?
    6. 브라우저 크기를 1023px 이하로 줄였을 때:
       - 사이드바가 사라지고 기존 GlobalHeader + BottomNav가 나타나는가?
       - SideMenu 드로어가 정상 동작하는가?
    7. 설정 패널(톱니바퀴 클릭)이 데스크톱/모바일 양쪽에서 정상 위치에 열리는가?
  </how-to-verify>
  <resume-signal>"approved" 입력 또는 발견된 문제점 설명</resume-signal>
</task>

</tasks>

<verification>
- `npm run build` 빌드 성공
- PC(1024px+): DesktopSidebar 표시, BottomNav/GlobalHeader 숨김
- 모바일(1023px-): 기존 BottomNav/GlobalHeader 표시, DesktopSidebar 숨김
- 데스크톱 main 영역 스크롤 가능
- 사이드바 메뉴 클릭으로 모든 페이지 이동 가능
- SettingsPanel이 데스크톱에서 하단 정상 배치
</verification>

<success_criteria>
- PC(1920x1080)에서 영구 사이드바로 모든 메뉴에 접근 가능 (LAYOUT-01)
- PC에서 넓은 레이아웃으로 데이터를 스크롤하여 확인 가능 -- phantom gap 없음 (LAYOUT-02)
- 멀티 패널 레이아웃 기반 구조 완성 -- DesktopSidebar + main 영역 분리 (LAYOUT-03 기반)
- 모바일(767px 이하)에서 기존 BottomNav/헤더/드로어 방식 동일 동작 (LAYOUT-04)
</success_criteria>

<output>
After completion, create `.planning/phases/11-desktop-layout-foundation/11-02-SUMMARY.md`
</output>
