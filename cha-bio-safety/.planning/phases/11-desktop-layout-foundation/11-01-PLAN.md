---
phase: 11-desktop-layout-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/index.css
  - src/hooks/useIsDesktop.ts
  - src/components/SideMenu.tsx
  - src/components/DesktopSidebar.tsx
autonomous: true
requirements:
  - LAYOUT-01
  - LAYOUT-04

must_haves:
  truths:
    - "useIsDesktop 훅이 1024px 기준으로 true/false를 반환한다"
    - "DesktopSidebar 컴포넌트가 280px 고정 너비로 4개 섹션 17개 메뉴를 표시한다"
    - "데스크톱(1024px+)에서 html overflow:hidden이 적용되지 않아 스크롤이 가능하다"
    - "모바일(1023px-)에서 html overflow:hidden이 그대로 유지된다"
  artifacts:
    - path: "src/hooks/useIsDesktop.ts"
      provides: "matchMedia 기반 데스크톱 판별 훅"
      exports: ["useIsDesktop"]
    - path: "src/components/DesktopSidebar.tsx"
      provides: "280px 영구 사이드바"
      exports: ["DesktopSidebar"]
    - path: "src/index.css"
      provides: "모바일 전용 overflow:hidden"
      contains: "@media (max-width: 1023px)"
  key_links:
    - from: "src/components/DesktopSidebar.tsx"
      to: "src/components/SideMenu.tsx"
      via: "MENU 상수 import"
      pattern: "import.*MENU.*from.*SideMenu"
    - from: "src/components/DesktopSidebar.tsx"
      to: "src/stores/authStore"
      via: "useAuthStore for staff info and logout"
      pattern: "useAuthStore"
---

<objective>
Phase 11의 기반 컴포넌트를 생성한다: CSS overflow 수정, useIsDesktop 훅, SideMenu MENU export, DesktopSidebar 컴포넌트.

Purpose: App.tsx Layout 리팩터링(Plan 02)에 앞서 필요한 모든 빌딩 블록을 준비한다.
Output: useIsDesktop.ts, DesktopSidebar.tsx 신규 파일 + index.css/SideMenu.tsx 수정
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-desktop-layout-foundation/11-CONTEXT.md
@.planning/phases/11-desktop-layout-foundation/11-UI-SPEC.md

<interfaces>
<!-- SideMenu.tsx MENU constant structure (to be exported) -->
From src/components/SideMenu.tsx:
```typescript
type MenuItem = { label: string; path: string; badge: number; soon: boolean; role?: 'admin' | 'assistant' }

const MENU: { section: string; items: MenuItem[] }[] = [
  { section: '주요 기능', items: [...] },
  { section: '점검 관리', items: [...] },
  { section: '근무·복지', items: [...] },
  { section: '시스템', items: [...] },
]
```

From src/stores/authStore (Zustand):
```typescript
useAuthStore() => { staff: { id, name, role, title } | null, isAuthenticated: boolean, logout: () => void }
```

From src/index.css (line 40-45):
```css
html {
  height: 100%;
  overflow: hidden;  /* <-- 이것을 모바일 전용으로 변경 */
  margin: 0;
  background: #161b22;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CSS overflow 수정 + useIsDesktop 훅 + SideMenu MENU export</name>
  <files>src/index.css, src/hooks/useIsDesktop.ts, src/components/SideMenu.tsx</files>
  <read_first>
    - src/index.css (현재 overflow:hidden 위치 확인 — line 40-45)
    - src/components/SideMenu.tsx (MENU 상수 현재 선언 위치 확인 — line 17)
  </read_first>
  <action>
    **1) src/index.css 수정 (per D-10):**
    Line 40-45의 `html` 블록에서 `overflow: hidden;` 줄을 제거하고, 그 아래에 별도 미디어쿼리 블록을 추가한다:

    ```css
    html {
      height: 100%;
      margin: 0;
      background: #161b22;
    }

    @media (max-width: 1023px) {
      html {
        overflow: hidden;
      }
    }
    ```

    주의: `body`의 `overscroll-behavior: none`은 그대로 유지한다 (iOS bounce 방지).

    **2) src/hooks/useIsDesktop.ts 신규 생성:**
    `src/hooks/` 디렉토리에 `useIsDesktop.ts` 파일을 생성한다:

    ```typescript
    import { useState, useEffect } from 'react'

    export function useIsDesktop(): boolean {
      const [isDesktop, setIsDesktop] = useState(
        () => typeof window !== 'undefined'
          ? window.matchMedia('(min-width: 1024px)').matches
          : false
      )
      useEffect(() => {
        const mq = window.matchMedia('(min-width: 1024px)')
        const handler = (e: MediaQueryListEvent) => setIsDesktop(e.matches)
        mq.addEventListener('change', handler)
        return () => mq.removeEventListener('change', handler)
      }, [])
      return isDesktop
    }
    ```

    **3) src/components/SideMenu.tsx 수정:**
    Line 17의 `const MENU` 선언을 `export const MENU`로 변경한다. `MenuItem` 타입도 export한다:

    ```typescript
    export type MenuItem = { label: string; path: string; badge: number; soon: boolean; role?: 'admin' | 'assistant' }

    export const MENU: { section: string; items: MenuItem[] }[] = [
    ```

    이 외에는 SideMenu.tsx의 어떤 코드도 변경하지 않는다.
  </action>
  <verify>
    <automated>cd /Users/jykevin/Documents/20260328/cha-bio-safety && grep -n "overflow: hidden" src/index.css | head -5 && grep -n "@media (max-width: 1023px)" src/index.css && grep -n "export function useIsDesktop" src/hooks/useIsDesktop.ts && grep -n "export const MENU" src/components/SideMenu.tsx && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/index.css 내 `html` 블록에 `overflow: hidden`이 없음 (미디어쿼리 안에만 존재)
    - src/index.css에 `@media (max-width: 1023px) { html { overflow: hidden; } }` 블록 존재
    - src/hooks/useIsDesktop.ts에 `export function useIsDesktop(): boolean` 존재
    - useIsDesktop.ts에 `matchMedia('(min-width: 1024px)')` 포함
    - src/components/SideMenu.tsx에 `export const MENU` 존재
    - src/components/SideMenu.tsx에 `export type MenuItem` 존재
    - `npx tsc --noEmit` 에러 없음 (또는 기존 에러만 존재, 새 에러 없음)
  </acceptance_criteria>
  <done>CSS overflow가 모바일 전용으로 제한되고, useIsDesktop 훅이 생성되고, MENU 상수가 export된다</done>
</task>

<task type="auto">
  <name>Task 2: DesktopSidebar 컴포넌트 생성</name>
  <files>src/components/DesktopSidebar.tsx</files>
  <read_first>
    - src/components/SideMenu.tsx (MENU 상수 구조 + MenuItem 타입 확인)
    - .planning/phases/11-desktop-layout-foundation/11-UI-SPEC.md (DesktopSidebar 전체 스펙 — Component Inventory 섹션)
    - src/stores/authStore.ts (staff 객체 구조, logout 함수)
  </read_first>
  <action>
    `src/components/DesktopSidebar.tsx` 신규 생성. UI-SPEC의 DesktopSidebar 정의를 정확히 구현한다.

    **Props:**
    ```typescript
    interface DesktopSidebarProps {
      unresolvedCount: number
      onSettingsOpen: () => void
    }
    ```

    **구조:**
    - 280px 고정 너비 (`width: 280, flexShrink: 0`), `height: '100dvh'`, `background: 'var(--bg2)'`, `borderRight: '1px solid var(--bd)'`
    - flex column: Logo strip(48px) + scrollable nav(flex:1) + user card(56px)

    **Logo strip (48px):**
    - `height: 48`, `background: 'var(--bg2)'`, `borderBottom: '1px solid var(--bd)'`
    - "CBC 방재" 텍스트: `fontSize: 14, fontWeight: 700, color: 'var(--t1)'`, `paddingLeft: 16`
    - display flex, alignItems center

    **Nav sections (scrollable middle):**
    - `overflow: 'auto'`, `flex: 1`
    - 데스크톱 전용 섹션 순서 (per D-04, UI-SPEC):

    ```typescript
    const DESKTOP_SECTIONS = [
      { label: '점검 현황', paths: ['/dashboard', '/inspection', '/remediation', '/floorplan'] },
      { label: '문서 관리', paths: ['/reports', '/daily-report', '/schedule', '/qr-print'] },
      { label: '직원 관리', paths: ['/workshift', '/staff-service', '/education'] },
      { label: '시설 관리', paths: ['/elevator', '/legal', '/div', '/inspection/qr', '/admin'] },
    ]
    ```

    SideMenu.tsx에서 `MENU`와 `MenuItem` 타입을 import. DESKTOP_SECTIONS의 paths 배열을 순회하며 MENU에서 해당 path의 item을 찾아 렌더링.

    - 섹션 그룹 라벨: `fontSize: 11, fontWeight: 700, color: 'var(--t2)', textTransform: 'uppercase'`, `padding: '8px 16px 4px'`
    - 네비 아이템: `height: 36, padding: '0 16px'`, `fontSize: 14, fontWeight: 400, color: 'var(--t1)'`
    - 아이템 hover: `background: 'var(--bg3)'`
    - 아이템 active (현재 경로 일치): `background: 'var(--bg4)'`, `borderLeft: '3px solid var(--acl)'`, `color: 'var(--acl)'`
    - `item.soon === true`: `color: 'var(--t3)'`, `cursor: 'default'`, `pointerEvents: 'none'`
    - 조치 관리 badge: path '/remediation'이면 unresolvedCount > 0일 때 `16px` 지름 pill, `background: 'var(--danger)'`, `color: '#fff'`, `fontSize: 11, fontWeight: 700`
    - role 기반 필터: `item.role && staff?.role !== item.role` 이면 숨김 (관리자 설정은 admin만)

    섹션 접힘/펼침 (per D-03): `useState`로 각 섹션 열림 상태 관리. 기본값: 모두 열림. 섹션 라벨 클릭 시 토글. 접힌 섹션은 아이템 숨김.

    **User card footer (56px):**
    - `height: 56`, `background: 'var(--bg2)'`, `borderTop: '1px solid var(--bd)'`, `padding: '0 16px'`
    - flex row, align center
    - 좌측: staff.name (12px, weight 700, `var(--t1)`) + role 라벨 (11px, weight 400, `var(--t2)`) 세로 스택
      - role 라벨: `staff.role === 'admin' ? '관리자' : '부관리자'`
    - 우측: LogOut 아이콘 (lucide-react, `size={16}`, `color: 'var(--danger)'`, `opacity: 0.8`, hover `opacity: 1`)
    - 로그아웃 클릭: `logout()` 호출 후 `navigate('/login')`. 확인 다이얼로그 없음.

    **useLocation import:** react-router-dom의 `useLocation`으로 현재 경로를 가져와 active 상태 판별.
    **useNavigate import:** 메뉴 클릭 시 네비게이션, 로그아웃 시 /login 이동.
  </action>
  <verify>
    <automated>cd /Users/jykevin/Documents/20260328/cha-bio-safety && grep -n "export.*DesktopSidebar" src/components/DesktopSidebar.tsx && grep -n "280" src/components/DesktopSidebar.tsx && grep -n "import.*MENU.*SideMenu" src/components/DesktopSidebar.tsx && grep -n "LogOut" src/components/DesktopSidebar.tsx && grep -n "점검 현황" src/components/DesktopSidebar.tsx && grep -n "문서 관리" src/components/DesktopSidebar.tsx && grep -n "직원 관리" src/components/DesktopSidebar.tsx && grep -n "시설 관리" src/components/DesktopSidebar.tsx && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/components/DesktopSidebar.tsx에 `export function DesktopSidebar` 또는 `export default function DesktopSidebar` 존재
    - 파일에 `width: 280` 또는 `width:'280px'` 포함
    - `import { MENU` 또는 `import { MENU, MenuItem }` from SideMenu 존재
    - lucide-react에서 `LogOut` import 존재
    - 4개 섹션 라벨 모두 존재: '점검 현황', '문서 관리', '직원 관리', '시설 관리'
    - `var(--bg2)` 배경색 사용
    - `var(--acl)` 활성 상태 색상 사용
    - `npx tsc --noEmit` 새 에러 없음
  </acceptance_criteria>
  <done>DesktopSidebar가 280px 고정 너비로 4개 섹션, 하단 사용자 카드, 로그아웃 버튼을 갖추고, SideMenu의 MENU 상수를 재사용하여 17개 메뉴를 표시한다</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` 타입 에러 없음
- `npm run build` 성공 (Vite 빌드)
- `grep -r "useIsDesktop" src/` 로 훅 존재 확인
- `grep -r "DesktopSidebar" src/` 로 컴포넌트 존재 확인
</verification>

<success_criteria>
- useIsDesktop 훅이 1024px matchMedia 기반으로 boolean 반환
- DesktopSidebar가 280px 고정, 4개 섹션, 사용자 카드, 로그아웃 포함
- CSS overflow:hidden이 모바일(1023px 이하)에서만 적용
- MENU 상수가 SideMenu에서 export되어 DesktopSidebar에서 import 가능
- 타입 체크 및 빌드 성공
</success_criteria>

<output>
After completion, create `.planning/phases/11-desktop-layout-foundation/11-01-SUMMARY.md`
</output>
