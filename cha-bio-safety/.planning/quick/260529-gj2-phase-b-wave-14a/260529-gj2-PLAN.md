---
phase: 260529-gj2-phase-b-wave-14a-remediation-mobile
plan: 01
type: execute
wave: 14a
depends_on: [260529-f2w]
files_modified:
  - src/pages/RemediationPage.tsx
autonomous: true
roadmap-wave: Tier 2 / Wave 14a (Remediation 모바일 zone — Tier 2 세 번째 wave)
---

<objective>
**Phase B Wave 14a — RemediationPage 모바일 zone.** Tier 2 세 번째 wave (Wave 12a StaffManage / 13a Checkpoints 직후).

RemediationPage (724줄, **25 total inline**):
- **모바일 zone (sweep 대상): 11 inline** = pre-L325 helpers 5 (`renderCard` + `filterBar`, 양 view 공용) + post-L537 mobile return 6
- **데스크톱 zone (skip, Wave 14b 후행): 14 inline** — `if (isDesktop) { ... }` block 안 (L341-L457)

옵션 X+P+M+색변수N + SKELETON_STYLE module const N 승계 (15번째 atomic).

### 구조 특이사항 (12a/13a 와 다름)
- **early-return 패턴**: L325 `if (isDesktop) { return (...) }` 이후 default 모바일 `return (...)` (L540~)
- **helper inline 은 양 view 공용**: `renderCard` (L167-240, L183 1건) + `filterBar` (L244-322, L247/255/280/301 4건) = pre-L325 5건은 데스크톱 view (L337/L379-L398) 와 모바일 view (간접) 모두에서 호출. **모바일 zone 으로 분류 — 양 view 동일 변환**
- **mobile-only block 없음**: `{!isDesktop && ...}` / `if (!isDesktop)` 패턴 0. early-return 이후 default return 이 모바일 view = shared 분류
- **데스크톱 zone = `if (isDesktop)` 블록 안만** (L325-L537 사이의 14건)

### Zone 분류 (style_zones.py 출력 검증 완료)

| zone | count | 위치 |
|---|---:|---|
| **shared (변환)** | 5 | pre-L325 helpers — L183 (renderCard 색바) + L247/255/280/301 (filterBar 4건) |
| **mobile-only (변환)** | 6 | post-L537 모바일 view — L548/556/581/605/629/668 |
| **모바일 zone 합계** | **11** | sweep 대상 |
| **desktop-only (보존, 14b 후행)** | 14 | `if (isDesktop)` 블록 안 L341/371/379/401/414/416/444/445/448/449/452/453/456/457 |
| **전체** | **25** | |

### 토큰 alias (Wave 11 nkv / 12a epe / 13a f2w precedent 그대로)
- `var(--surface-raised)` → `bg-surface-raised`
- `var(--surface-sunken)` → `bg-surface-sunken`
- `var(--surface-active)` → `bg-surface-active`
- `var(--surface-page)` → `bg-surface-page`
- `var(--text-primary)` → `text-text-primary`
- `var(--text-secondary)` → `text-text-secondary`
- `var(--text-tertiary)` → `text-text-tertiary`
- `var(--accent)` → `text-accent` / `bg-accent` / `border-accent`
- `var(--border-default)` → `border-border-default`
- `var(--border-strong)` → `border-border-strong`
- `var(--status-fire-bar)` → `bg-fire-bar`
- `var(--status-safe-bar)` → `bg-safe-bar`

### 위험 anchor
- 데스크톱 zone L341-L457 의 14건 (kv-table th/td 11건 + scrollbar 1 + detail-empty fontSize 1 + detail-pane padding 1) — **변경 절대 0** (Wave 14b 책임)
- module const `SKELETON_STYLE` (L109-114) — **정의 보존**, 직접 참조 (L343 데스크톱 3건 + L633/634/635 모바일 3건 = 6건 `style={SKELETON_STYLE}`) 은 이미 inline 아님 → grep 에 안 잡힘, 손대지 말 것
- 비즈 anchor (useState 3 / useQuery 2 / useNavigate 1 / useSearchParams 1 / onClick 7) IDENTICAL
- **MOBILE/DESKTOP duplicate inline** — L255 (desktop tab) 와 L556 (mobile tab) 는 동일 패턴이나 zone 다름. **반드시 L556 만 변환**, L255 는 desktop zone 으로 보존. 마찬가지로 L280↔L581, L301↔L605 모두 desktop=보존 / mobile=변환

</objective>

<context>

### 모바일 zone (sweep 대상 — 11 inline)

#### Pre-L325 helpers (shared — renderCard + filterBar) — 5건

**renderCard (L167-240) — 1건**
- **L183** 색바 background (status-based dynamic) + alignSelf
  - Before: `style={{ background: record.status === 'open' ? 'var(--status-fire-bar)' : 'var(--status-safe-bar)', alignSelf: 'stretch' }}`
  - After: **옵션 M template literal** — 기존 `className="w-1 rounded-[2px] shrink-0"` 에 `self-stretch` + 동적 bg 병합:
    - `className={` + `\`w-1 rounded-[2px] shrink-0 self-stretch ${record.status === 'open' ? 'bg-fire-bar' : 'bg-safe-bar'}\`` + `}`
  - 옵션: **M+token**

**filterBar (L244-322) — 4건**

- **L247** 외부 wrap background (정적)
  - Before: `style={{ background: 'var(--surface-raised)' }}`
  - After: 기존 `className="shrink-0 border-b border-border-default"` 에 `bg-surface-raised` 병합
  - 옵션: **token**

- **L255** STATUS_TABS 버튼 (8-prop conditional)
  - Before: `style={{ flex: 1, height: 44, border: 'none', fontSize: 13, fontWeight: 600, cursor: 'pointer', transition: 'color .13s', background: statusTab === tab.key ? 'var(--surface-active)' : 'transparent', color: statusTab === tab.key ? 'var(--text-primary)' : 'var(--text-tertiary)', borderBottom: statusTab === tab.key ? '2px solid var(--accent)' : '2px solid transparent' }}`
  - After: **옵션 M template literal** — `flex-1 h-11 border-0 text-[13px] font-semibold cursor-pointer transition-colors duration-[130ms]` + dynamic bg/text/border-b:
    - `className={` + `\`flex-1 h-11 border-0 text-[13px] font-semibold cursor-pointer transition-colors duration-[130ms] border-b-2 ${statusTab === tab.key ? 'bg-surface-active text-text-primary border-accent' : 'bg-transparent text-text-tertiary border-transparent'}\`` + `}`
  - 옵션: **M+X+token** (height:44=h-11, fontSize:13=text-[13px] arbitrary, fontWeight:600=font-semibold)
  - 함정 회피: `transition: 'color .13s'` 의 `.13s` 는 `duration-[130ms]` 로 표현 (소수점 0 prefix 안전)

- **L280** category select (9-prop)
  - Before: `style={{ flex: 1, height: 36, background: 'var(--surface-sunken)', border: '1px solid var(--border-strong)', borderRadius: 8, color: 'var(--text-primary)', fontSize: 12, padding: '0 8px', cursor: 'pointer', minWidth: 0 }}`
  - After: `className="flex-1 h-9 bg-surface-sunken border border-border-strong rounded-sm text-text-primary text-[12px] px-2 cursor-pointer min-w-0"`
  - 옵션: **X+token** (height:36=h-9 standard tailwind, borderRadius:8=rounded-sm config override, fontSize:12=text-[12px] arbitrary, padding:'0 8px'=px-2)

- **L301** PERIOD_BUTTONS 버튼 (9-prop conditional)
  - Before: `style={{ height: 32, padding: '0 12px', borderRadius: 8, border: 'none', fontSize: 12, fontWeight: 700, cursor: 'pointer', whiteSpace: 'nowrap', background: days === btn.value ? 'var(--surface-active)' : 'transparent', color: days === btn.value ? 'var(--text-primary)' : 'var(--text-tertiary)' }}`
  - After: **옵션 M** — `h-7 px-3 rounded-sm border-0 text-[12px] font-bold cursor-pointer whitespace-nowrap` + dynamic bg/text:
    - `className={` + `\`h-7 px-3 rounded-sm border-0 text-[12px] font-bold cursor-pointer whitespace-nowrap ${days === btn.value ? 'bg-surface-active text-text-primary' : 'bg-transparent text-text-tertiary'}\`` + `}`
  - 옵션: **M+X+token** (height:32=h-7 config override 32px, padding:'0 12px'=px-3, borderRadius:8=rounded-sm)

#### Post-L537 mobile-only — 6건

- **L548** mobile filter bar wrap (sticky + 4-prop)
  - Before: `style={{ background: 'var(--surface-raised)', position: 'sticky', top: 0, zIndex: 10 }}`
  - After: 기존 `className="shrink-0 border-b border-border-default"` 에 `sticky top-0 z-10 bg-surface-raised` 병합
  - 옵션: **token** (sticky/top-0/z-10 은 standard tailwind)

- **L556** mobile STATUS_TABS 버튼 (L255 와 동일 패턴, mobile 분기 안)
  - Before: 8-prop conditional (L255 와 IDENTICAL except mobile zone)
  - After: **L255 와 동일 className 변환** (옵션 M)
    - `className={` + `\`flex-1 h-11 border-0 text-[13px] font-semibold cursor-pointer transition-colors duration-[130ms] border-b-2 ${statusTab === tab.key ? 'bg-surface-active text-text-primary border-accent' : 'bg-transparent text-text-tertiary border-transparent'}\`` + `}`
  - 옵션: **M+X+token**

- **L581** mobile category select (L280 와 동일 패턴)
  - Before: 9-prop (L280 와 IDENTICAL except mobile zone)
  - After: **L280 와 동일 className 변환**
    - `className="flex-1 h-9 bg-surface-sunken border border-border-strong rounded-sm text-text-primary text-[12px] px-2 cursor-pointer min-w-0"`
  - 옵션: **X+token**

- **L605** mobile PERIOD_BUTTONS 버튼 (L301 와 거의 동일 + `transition: 'all 0.15s'` 추가 1prop)
  - Before: 10-prop conditional — L301 의 9-prop + `transition: 'all 0.15s'`
  - After: **옵션 M** — L301 className + `transition-all duration-150`
    - `className={` + `\`h-7 px-3 rounded-sm border-0 text-[12px] font-bold cursor-pointer whitespace-nowrap transition-all duration-150 ${days === btn.value ? 'bg-surface-active text-text-primary' : 'bg-transparent text-text-tertiary'}\`` + `}`
  - 옵션: **M+X+token**
  - 함정: L301 (desktop view 안의 동일 helper) 와 L605 (mobile view inline) 의 transition prop 차이 — L605 는 `transition: 'all 0.15s'`, L301 은 `transition: 'color .13s'`. 둘 다 별개 inline 으로 각자 className 매핑

- **L629** mobile list scrollbar (1-prop, non-standard property)
  - Before: `style={{ scrollbarWidth: 'none' } as React.CSSProperties}`
  - After: **옵션 N 잔존** — `scrollbarWidth` 는 Tailwind 기본 클래스 없음. inline 유지 (옵션 N — non-standard CSS property).
  - **변경 0** — 모바일 zone count 11 → 10 (이 1건 보존). 단, **데스크톱 L341 도 동일 패턴** → 본 wave 에서 L629 도 그대로 두고 옵션 N 잔존 카운트에 포함
  - 옵션: **N (잔존)**

- **L668** mobile card 색바 background (status-based — L183 와 동일 패턴)
  - Before: `style={{ background: record.status === 'open' ? 'var(--status-fire-bar)' : 'var(--status-safe-bar)', alignSelf: 'stretch' }}`
  - After: **옵션 M template literal** — 기존 `className="w-1 rounded-[2px] shrink-0"` 에 `self-stretch` + 동적 bg 병합:
    - `className={` + `\`w-1 rounded-[2px] shrink-0 self-stretch ${record.status === 'open' ? 'bg-fire-bar' : 'bg-safe-bar'}\`` + `}`
  - 옵션: **M+token**

### 데스크톱 zone — **변경 절대 0** (14곳, Wave 14b)

`if (isDesktop) { ... }` 블록 안 (L325-L537):

| Line | 영역 | inline |
|----:|---|---|
| L341 | desktop list scrollbar `style={{ scrollbarWidth: 'none' } as React.CSSProperties}` | 1 |
| L371 | detail-empty `style={{ fontSize: 13 }}` | 1 |
| L379 | detail-pane `style={{ padding: '20px 28px', boxSizing: 'border-box', scrollbarWidth: 'none' } as React.CSSProperties}` | 1 |
| L401 | kv-table `style={{ width: '100%', borderCollapse: 'collapse', marginBottom: 20 }}` | 1 |
| L414 | th (label, dynamic loop) `style={{ width: 110, padding: '8px 12px', background, border, fontSize: 12, fontWeight: 700, color, textAlign, verticalAlign, lineHeight: 1.5 }}` | 1 |
| L416 | td (value, dynamic loop) `style={{ padding, border, fontSize: 13, color, whiteSpace, verticalAlign, lineHeight: 1.6 }}` | 1 |
| L444 | th 조치일시 (resolved branch) | 1 |
| L445 | td 조치일시 | 1 |
| L448 | th 조치자 | 1 |
| L449 | td 조치자 | 1 |
| L452 | th 조치 내용 | 1 |
| L453 | td 조치 내용 | 1 |
| L456 | th 소모 자재 | 1 |
| L457 | td 소모 자재 | 1 |
| **합계** | | **14** |

> 데스크톱 zone 경계: **L325 ~ L537 (`if (isDesktop) { ... return (...) ... }` 블록)**. 본 wave diff 의 모든 hunk 가 L325 이전 또는 L538 이후만 포함하는지 검증 필수.

### 예상

| 구역 | Before | After | 변환 |
|---|---:|---:|---:|
| Pre-L325 helpers (shared) | 5 | 0 | 5 |
| Post-L537 mobile-only | 6 | 1 (L629 scrollbarWidth 옵션 N) | 5 |
| **모바일 zone subtotal** | **11** | **1** | **10** |
| Desktop-only (skip) | 14 | 14 | 0 |
| **전체 file** | **25** | **15** | **10** |

목표: **25 → ~14-16** (10건 변환, 옵션 N 1건 잔존, 데스크톱 14건 보존). 정확 예상 = 15.

### 룰 (locked)
- 옵션 X+P+M+색변수N + SKELETON_STYLE module const N
- 데스크톱 zone 절대 변경 0 (Wave 14b 책임)
- 비즈 anchor precise diff = empty
- emoji 카운트 변동 0 (Phase A 보존 — RemediationPage 는 이미 Lucide 적용 완료, emoji=0)
- 비표준 색 토큰 0
- TypeScript 0 error
- 변경 파일 = 1 .tsx 만 (off-scope = 0)
- 단일 atomic commit (`feat(260529-gj2-01): Phase B Wave 14a — Remediation 모바일 zone (11 inline) → tailwind`)

### 함정 회피
- **mobile/desktop duplicate inline** — L255/280/301 (desktop view 안의 filterBar 호출 site) ≠ L556/581/605 (mobile view 안의 inline). 둘 다 별개 inline 으로 분류되지만 **filterBar 는 helper 함수 (L244-322) 라 inline 정의는 L255/280/301 한 곳에만 존재**. desktop view 가 filterBar 를 호출 → 같은 inline 이 두 view 에 렌더링됨. **변환 후에도 동일 — 한 번 변환하면 양 view 모두 적용**. desktop zone 보존 룰을 어기는 게 아님 (inline 정의 자체가 helper 안 = pre-L325 shared zone)
  - 즉, L255/280/301 변환 = 자동으로 desktop view L337 의 `{filterBar}` 호출 site 에도 영향 → **이건 의도된 부수효과 (shared helper)**, desktop zone 보존 룰 위반 아님
  - mobile 의 L556/581/605 는 별도 인라인 (mobile view 가 filterBar 호출 안 하고 자체적으로 재정의) → **둘 다 변환**

- **L301 vs L605 의 transition 차이** — L301 = `transition: 'color .13s'`, L605 = `transition: 'all 0.15s'`. 별개 inline 으로 각자 매핑 (시각 0 byte 룰 보존). L605 에 `transition-all duration-150` 추가

- **`text-[13px]` / `text-[12px]` arbitrary** — tailwind config 의 `text-caption` (12px lh:1.5) / `text-body-sm` (13px) 대신 정확값 arbitrary 사용. 이유: text-caption 의 lh:1.5 가 컨테이너 영향 줄 수 있음, 시각 0 byte 보장 위해 arbitrary 채택. **feedback_text_caption_leading_none.md anchor 적용 — 작은 컨테이너 안에서 lh 문제 회피**

- **`scrollbarWidth: 'none'`** (L629, L341) — Tailwind 기본 지원 X. 옵션 N 잔존 (둘 다). L629 만 본 wave 범위, L341 은 desktop zone 보존

- **w-7=32 / w-8=48 spacing override** — height:32 → `h-7`, height:36 → `h-9` (standard), height:44 → `h-11`, height:40 → `h-10`

- **`bg-fire-bar` / `bg-safe-bar`** — tailwind.config L72/76 에 정의됨 (status- prefix 없음, `feedback_tailwind_token_class_pattern.md` 준수)

- **`self-stretch`** — `alignSelf: 'stretch'` 의 tailwind 표준 클래스 (`self-stretch` = `align-self: stretch`)

- **`duration-[130ms]`** — `transition: 'color .13s'` 의 130ms 표현. `transition-colors` 와 결합 (오직 color/bg/border-color 만 transition)

- **early-return 구조** — desktop view L325-537 절대 변경 0. mobile view default return (L540~) 만 변환. helper (pre-L325) 는 변환 (양 view 공용이 의도)

### 메모리 anchors
- `feedback_tailwind_w8_h8_is_48px.md` — h-7=32 (PERIOD_BUTTONS) / h-9=36 (category select) / h-11=44 (STATUS_TABS) standard
- `feedback_tailwind_token_class_pattern.md` — `bg-fire-bar` / `bg-safe-bar` / `bg-accent` / `border-accent` — status- prefix 없음
- `feedback_text_caption_leading_none.md` — text-[13px]/text-[12px] arbitrary 채택 (text-caption lh:1.5 영향 회피)
- `feedback_design_changes_ask_first.md` — 시각 0 byte 룰 100%
- Wave 11 nkv + 12a epe + 13a f2w — token alias 매핑 precedent (14번째 atomic 자동 도달)
- Wave 5 db728c0 (RemediationDetail precedent) — 같은 페이지 family Phase B 룰 일관 적용

</context>

<tasks>

<task type="auto">
  <name>Task 1: Sweep RemediationPage 모바일 zone — 11 inline (shared 5 + mobile-only 6, 옵션 N 1건 잔존)</name>
  <files>src/pages/RemediationPage.tsx</files>
  <action>

### Step 1: Before snapshot

```bash
cd /Users/jykevin/Documents/cbc7119-design/cha-bio-safety
F=src/pages/RemediationPage.tsx
echo "style before: $(grep -c 'style={{' $F)"  # expect 25

# anchor counts
{
  for ANCHOR in 'onClick=\{[^}]+\}' 'useState\(' 'useRef\(' 'useEffect\(' 'useMutation\(' 'useQuery\(' 'useNavigate\(' 'useParams\(' 'fetch\('; do
    echo "  $ANCHOR : $(grep -cE "$ANCHOR" $F)"
  done
} > /tmp/gj2-before.txt

# onClick precise set
grep -oE 'onClick=\{[^}]+\}' $F | sort | uniq > /tmp/gj2-clicks-before.txt

# SKELETON_STYLE const def
echo "SKELETON_STYLE const def: $(grep -c '^const SKELETON_STYLE' $F)"  # MUST = 1

# emoji 변동 0 룰 (RemediationPage 는 이미 Lucide 적용 → emoji = 0 예상)
echo "emoji before: $(grep -cE '✓|✗|✕|🔒|💾|🔥|⏰|📋|✅|⚠️|❌|🔧|🚨|🔍|🧯|📊|▸|◂|▴|▾' $F)"

# 데스크톱 zone inline 수 (line range L325-L537)
DSK_START=325
DSK_END=537
echo "desktop zone before: $(sed -n "${DSK_START},${DSK_END}p" $F | grep -c 'style={{')"  # expect 14
```

### Step 2: Edit RemediationPage.tsx — 모바일 zone 11곳

**절대 변경 금지 (데스크톱 zone, L325-L537, 14곳)**:
- L341 desktop list scrollbar
- L371 detail-empty fontSize
- L379 detail-pane padding
- L401 kv-table
- L414 / L416 (loop th/td)
- L444 / L445 / L448 / L449 / L452 / L453 / L456 / L457 (resolved branch th/td)

**보존 영역의 inline / span / button / className 도 절대 변경 0** — git diff hunk 가 L325 이전 또는 L538 이후만 포함해야 함.

#### 변환 매핑 (모바일 zone 11곳)

**Pre-L325 (shared helpers) — 5곳**

**L183 — renderCard 색바** (helper L167 안)
- 기존 JSX (L181-187):
```tsx
<div
  className="w-1 rounded-[2px] shrink-0"
  style={{
    background: record.status === 'open' ? 'var(--status-fire-bar)' : 'var(--status-safe-bar)',
    alignSelf: 'stretch',
  }}
/>
```
- After:
```tsx
<div
  className={`w-1 rounded-[2px] shrink-0 self-stretch ${record.status === 'open' ? 'bg-fire-bar' : 'bg-safe-bar'}`}
/>
```

**L247 — filterBar 외부 wrap** (helper L244 안)
- 기존:
```tsx
<div
  className="shrink-0 border-b border-border-default"
  style={{ background: 'var(--surface-raised)' }}
>
```
- After:
```tsx
<div className="shrink-0 border-b border-border-default bg-surface-raised">
```

**L255 — filterBar STATUS_TABS 버튼**
- 기존:
```tsx
<button
  key={tab.key}
  onClick={() => setStatusTab(tab.key)}
  style={{
    flex: 1,
    height: 44,
    border: 'none',
    fontSize: 13,
    fontWeight: 600,
    cursor: 'pointer',
    transition: 'color .13s',
    background: statusTab === tab.key ? 'var(--surface-active)' : 'transparent',
    color: statusTab === tab.key ? 'var(--text-primary)' : 'var(--text-tertiary)',
    borderBottom: statusTab === tab.key ? '2px solid var(--accent)' : '2px solid transparent',
  }}
>
```
- After:
```tsx
<button
  key={tab.key}
  onClick={() => setStatusTab(tab.key)}
  className={`flex-1 h-11 border-0 text-[13px] font-semibold cursor-pointer transition-colors duration-[130ms] border-b-2 ${statusTab === tab.key ? 'bg-surface-active text-text-primary border-accent' : 'bg-transparent text-text-tertiary border-transparent'}`}
>
```

**L280 — filterBar category select**
- 기존:
```tsx
<select
  value={categoryFilter}
  onChange={e => setCategoryFilter(e.target.value)}
  style={{
    flex: 1,
    height: 36,
    background: 'var(--surface-sunken)',
    border: '1px solid var(--border-strong)',
    borderRadius: 8,
    color: 'var(--text-primary)',
    fontSize: 12,
    padding: '0 8px',
    cursor: 'pointer',
    minWidth: 0,
  }}
>
```
- After:
```tsx
<select
  value={categoryFilter}
  onChange={e => setCategoryFilter(e.target.value)}
  className="flex-1 h-9 bg-surface-sunken border border-border-strong rounded-sm text-text-primary text-[12px] px-2 cursor-pointer min-w-0"
>
```

**L301 — filterBar PERIOD_BUTTONS 버튼**
- 기존:
```tsx
<button
  key={btn.value}
  onClick={() => setDays(btn.value)}
  style={{
    height: 32,
    padding: '0 12px',
    borderRadius: 8,
    border: 'none',
    fontSize: 12,
    fontWeight: 700,
    cursor: 'pointer',
    whiteSpace: 'nowrap',
    background: days === btn.value ? 'var(--surface-active)' : 'transparent',
    color: days === btn.value ? 'var(--text-primary)' : 'var(--text-tertiary)',
  }}
>
```
- After:
```tsx
<button
  key={btn.value}
  onClick={() => setDays(btn.value)}
  className={`h-7 px-3 rounded-sm border-0 text-[12px] font-bold cursor-pointer whitespace-nowrap ${days === btn.value ? 'bg-surface-active text-text-primary' : 'bg-transparent text-text-tertiary'}`}
>
```

**Post-L537 mobile-only — 6곳**

**L548 — mobile filter bar wrap (sticky + bg)**
- 기존:
```tsx
<div
  className="shrink-0 border-b border-border-default"
  style={{ background: 'var(--surface-raised)', position: 'sticky', top: 0, zIndex: 10 }}
>
```
- After:
```tsx
<div className="shrink-0 border-b border-border-default bg-surface-raised sticky top-0 z-10">
```

**L556 — mobile STATUS_TABS 버튼** (L255 와 동일 변환 적용)
- After:
```tsx
<button
  key={tab.key}
  onClick={() => setStatusTab(tab.key)}
  className={`flex-1 h-11 border-0 text-[13px] font-semibold cursor-pointer transition-colors duration-[130ms] border-b-2 ${statusTab === tab.key ? 'bg-surface-active text-text-primary border-accent' : 'bg-transparent text-text-tertiary border-transparent'}`}
>
```

**L581 — mobile category select** (L280 와 동일 변환 적용)
- After:
```tsx
<select
  value={categoryFilter}
  onChange={e => setCategoryFilter(e.target.value)}
  className="flex-1 h-9 bg-surface-sunken border border-border-strong rounded-sm text-text-primary text-[12px] px-2 cursor-pointer min-w-0"
>
```

**L605 — mobile PERIOD_BUTTONS 버튼** (L301 + `transition: 'all 0.15s'`)
- After:
```tsx
<button
  key={btn.value}
  onClick={() => setDays(btn.value)}
  className={`h-7 px-3 rounded-sm border-0 text-[12px] font-bold cursor-pointer whitespace-nowrap transition-all duration-150 ${days === btn.value ? 'bg-surface-active text-text-primary' : 'bg-transparent text-text-tertiary'}`}
>
```

**L629 — mobile list scrollbar** — **옵션 N 잔존**
- 변경 없음 (scrollbarWidth Tailwind 미지원). inline 유지.
- 단, L341 (desktop zone) 의 동일 패턴도 보존됨

**L668 — mobile card 색바** (L183 와 동일 패턴, mobile view 의 별도 inline render)
- 기존 (L666-671):
```tsx
<div
  className="w-1 rounded-[2px] shrink-0"
  style={{
    background: record.status === 'open' ? 'var(--status-fire-bar)' : 'var(--status-safe-bar)',
    alignSelf: 'stretch',
  }}
/>
```
- After:
```tsx
<div
  className={`w-1 rounded-[2px] shrink-0 self-stretch ${record.status === 'open' ? 'bg-fire-bar' : 'bg-safe-bar'}`}
/>
```

**중요 보존**:
- L109-114 `const SKELETON_STYLE` 정의 그대로
- L343 `<div style={SKELETON_STYLE} />` × 3 (desktop list, desktop zone — 자동 보존)
- L633/634/635 `<div style={SKELETON_STYLE} />` × 3 (mobile, 이미 inline 아님 — verify 에 안 잡힘, 손대지 말 것)

### Step 3: After verification

```bash
F=src/pages/RemediationPage.tsx

# 1. style count drop
A=$(grep -c 'style={{' $F)
echo "after: $A (expect 14-16, target ~15)"

# 2. 데스크톱 zone 보존 검증 (line range L325-L537)
DSK_START=325
DSK_END=537
DSK_AFTER=$(sed -n "${DSK_START},${DSK_END}p" $F | grep -c 'style={{')
echo "desktop zone after: $DSK_AFTER (MUST = 14)"

# 3. 비즈 anchor identity
{
  for ANCHOR in 'onClick=\{[^}]+\}' 'useState\(' 'useRef\(' 'useEffect\(' 'useMutation\(' 'useQuery\(' 'useNavigate\(' 'useParams\(' 'fetch\('; do
    echo "  $ANCHOR : $(grep -cE "$ANCHOR" $F)"
  done
} > /tmp/gj2-after.txt
diff /tmp/gj2-before.txt /tmp/gj2-after.txt  # MUST empty

# 4. onClick handler bodies precise diff
grep -oE 'onClick=\{[^}]+\}' $F | sort | uniq > /tmp/gj2-clicks-after.txt
diff /tmp/gj2-clicks-before.txt /tmp/gj2-clicks-after.txt  # MUST empty

# 5. emoji 변동 0 (이미 0 이어야 함)
echo "emoji after: $(grep -cE '✓|✗|✕|🔒|💾|🔥|⏰|📋|✅|⚠️|❌|🔧|🚨|🔍|🧯|📊|▸|◂|▴|▾' $F)"

# 6. 비색
echo "비색: $(grep -cE 'bg-warning[^-]|border-safe[^-]|border-warning[^-]|border-danger[^-]' $F)"  # MUST = 0

# 7. SKELETON_STYLE const 보존
echo "SKELETON_STYLE const: $(grep -c '^const SKELETON_STYLE' $F)"  # MUST = 1

# 8. diff hunk 데스크톱 zone 영역 변경 0 검증
git diff $F > /tmp/gj2-diff.txt
echo "=== diff hunk headers ==="
grep -E '^@@' /tmp/gj2-diff.txt
# 모든 hunk header 의 라인 번호가 L325 이전 또는 L538 이후만 포함하는지 사람이 검토
# 자동 검증: hunk의 @@ -X,Y 에서 X+Y 가 325-537 영역과 겹치는지 확인
# 단순 grep 으로 desktop zone 라인이 diff 에 있는지 확인:
awk '/^@@/ {
  match($0, /-([0-9]+),([0-9]+)/, m);
  start=m[1]; len=m[2];
  end=start+len-1;
  if ((start >= 325 && start <= 537) || (end >= 325 && end <= 537) || (start < 325 && end > 537)) {
    print "DESKTOP ZONE TOUCHED: " $0
  }
}' /tmp/gj2-diff.txt
# (출력 없으면 OK)

# 9. TypeScript
./node_modules/.bin/tsc --noEmit 2>&1 | grep -c "error TS"  # MUST = 0

# 10. off-scope 변경
cd .. && git diff --name-only HEAD | grep -v "cha-bio-safety/src/pages/RemediationPage.tsx" | grep -v ".planning/" | wc -l  # MUST = 0
cd cha-bio-safety && git diff --name-only HEAD  # MUST = src/pages/RemediationPage.tsx 1줄만
```

### Step 4: Atomic commit

```bash
cd /Users/jykevin/Documents/cbc7119-design
git add cha-bio-safety/src/pages/RemediationPage.tsx
git commit --no-verify -m "$(cat <<'EOF'
feat(260529-gj2-01): Phase B Wave 14a — Remediation 모바일 zone (11 inline) → tailwind

Tier 2 세 번째 wave (Wave 12a StaffManage / 13a Checkpoints 직후). 모바일 zone 11 sweep.
데스크톱 zone 14곳 절대 변경 0 (Wave 14b 후행: kv-table 11 + scrollbar 1 + detail 2).
SKELETON_STYLE module const 정의 + 직접 참조 6건 (desktop 3 + mobile 3) 보존.
토큰 alias 매핑 (var(--surface-raised/sunken/active/page) / var(--text-*) / var(--accent) / var(--border-*) / var(--status-fire-bar/safe-bar) → tailwind tokens).
옵션 X+P+M+색변수N + scrollbarWidth 옵션 N 1건 잔존 (L629). 시각 0 byte 변경.
early-return `if (isDesktop) { return }` 구조 — desktop view (L325-L537) 절대 보존, default mobile return 변환, helper (renderCard/filterBar) 양 view 공용 변환.
비즈 anchor identical. emoji 0 유지. TypeScript 0 error.
EOF
)"
```

### Step 5: SUMMARY.md

Write `cha-bio-safety/.planning/quick/260529-gj2-phase-b-wave-14a/260529-gj2-SUMMARY.md`.

Tier 2 세 번째 wave — early-return 구조 첫 사례. zone 분할 패턴 15번째 atomic precedent.
PLAN.md 의 zone 매핑 표 + 실제 diff 통계 + 옵션 N 잔존 라인 + 데스크톱 보존 검증 결과 + early-return 패턴 안내 포함.

Do NOT commit SUMMARY.md.

  </action>
  <verify>
    <automated>cd /Users/jykevin/Documents/cbc7119-design/cha-bio-safety && S=$(grep -c 'style={{' src/pages/RemediationPage.tsx) && echo "after=$S (was 25, expect 14-16)" && [ "$S" -ge "14" ] && [ "$S" -le "16" ] && DSK=$(sed -n '325,537p' src/pages/RemediationPage.tsx | grep -c 'style={{') && echo "desktop=$DSK (MUST=14)" && [ "$DSK" = "14" ] && [ "$(./node_modules/.bin/tsc --noEmit 2>&1 | grep -c 'error TS')" = "0" ] && [ "$(grep -cE 'bg-warning[^-]|border-safe[^-]|border-warning[^-]|border-danger[^-]' src/pages/RemediationPage.tsx)" = "0" ] && [ "$(grep -c '^const SKELETON_STYLE' src/pages/RemediationPage.tsx)" = "1" ] && cd .. && [ "$(git diff --name-only HEAD | grep -v 'cha-bio-safety/src/pages/RemediationPage.tsx' | grep -v '.planning/' | wc -l | tr -d ' ')" = "0" ]</automated>
  </verify>
  <done>
    - RemediationPage inline 14-16 (목표 ~15; 25 - 10 sweep + scrollbarWidth 옵션 N 1 잔존)
    - 데스크톱 zone (L325-L537, 14곳) 변경 0
    - SKELETON_STYLE module const 정의 보존 (1 const def)
    - SKELETON_STYLE 직접 참조 6건 (desktop 3 + mobile 3) 보존 (inline 아님, verify 에 안 잡힘)
    - 비즈 anchor precise diff = empty (onClick/useState/useQuery/useNavigate 모두)
    - emoji 0 유지 (RemediationPage 는 이미 Lucide)
    - 비색 0 (status- prefix 없는 토큰 0)
    - TypeScript 0 error
    - 변경 파일 = 1 .tsx 만 (off-scope = 0)
    - 단일 atomic commit
    - diff hunk 데스크톱 zone 영역 (L325-L537) 변경 0 (awk 검증 출력 empty)
  </done>
</task>

</tasks>
