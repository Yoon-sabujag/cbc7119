---
phase: 260528-a3v-phase-b-wave-1-qrscanpage-divpage-reportspage
plan: 01
type: execute
wave: 1
depends_on: [260528-01h]
files_modified:
  - src/pages/QRScanPage.tsx
  - src/pages/DivPage.tsx
  - src/pages/ReportsPage.tsx
autonomous: true
requirements:
  - QUICK-260528-a3v-PHASE-B-WAVE-1
roadmap-wave: Tier 1 / Wave 1 (워밍업)
---

<objective>
**Phase B Wave 1 — 워밍업.** 마스터 로드맵 v2 (`260528-0hr`) Tier 1 첫 번째 wave. 3 소형 페이지 동시 sweep.

- QRScanPage (280줄, 1 inline)
- DivPage (1140줄, 4 inline)
- ReportsPage (352줄, 3 inline)

**옵션 X+P+M+색변수N 승계** (wdc 260527-wdc + 01h 260528-01h 룰 그대로).
시각 결과 0 byte. 비즈 anchor identity. Phase A 보존.
</objective>

<context>

### Inline style 위치 분석 (이미 grep 완료)

**QRScanPage.tsx (1곳)**
- L205: `<div id={QR_REGION_ID} style={{ width:'100%' }} />` → className 에 `w-full` 추가, style 제거

**DivPage.tsx (4곳)** — 모두 동적 색/사이즈 변수
- L471: `style={{ color }}` — `color` 는 인접 함수의 인자/계산값 (동적)
- L611: `style={{ color }}` — 동일
- L720: `style={{ color }}` — 동일
- L723: `style={{ height: chartH }}` — `chartH = topPad + barMaxH + 70` (L716, 동적 계산)
- **결론: 4곳 모두 옵션 N 잔존** (변환 0)

**ReportsPage.tsx (3곳)** — 모두 정적, 변환 가능
- L178: `style={{ display: 'flex', flexDirection: 'column', height: '100%', overflow: 'hidden' }}` → `flex flex-col h-full overflow-hidden`
- L278: 동일 + `background: 'var(--surface-page)'` → `bg-surface-page flex flex-col h-full overflow-hidden`
- L301: `style={{ flex: 1, overflowY: 'auto' }}` → `flex-1 overflow-y-auto`

### 예상 결과

| 파일 | Before | After | 변환 |
|---|---:|---:|---|
| QRScanPage | 1 | 0 | 1곳 |
| DivPage | 4 | 4 | 0곳 (모두 동적 잔존) |
| ReportsPage | 3 | 0 | 3곳 |
| **합계** | **8** | **4** | **4곳 (-50%)** |

### 룰 (locked, all wave 공통)
- 옵션 X (정확값 arbitrary)
- 옵션 P (leading 명시 보존)
- 옵션 M (className conditional + 색변수 N)
- 비즈 anchor: onClick / useState / useMutation / useQuery / useEffect / useNavigate / useRef / fetch / *Api — precise grep identical
- Phase A 보존: Lucide imports / 색 토큰 `-bar` / emoji 0
- TypeScript 0 error
- 변경 파일 = 3 .tsx 만

### 메모리 anchors
- `feedback_tailwind_w8_h8_is_48px.md` — w-7=32, w-8=48
- `feedback_tailwind_token_class_pattern.md` — status- prefix 없음

</context>

<tasks>

<task type="auto">
  <name>Task 1: Bulk apply — QRScan/Div/Reports inline → tailwind</name>
  <files>
    src/pages/QRScanPage.tsx
    src/pages/DivPage.tsx
    src/pages/ReportsPage.tsx
  </files>
  <action>

### Step 1: Before snapshot

```bash
cd /Users/jykevin/Documents/cbc7119-design/cha-bio-safety

# style count
for F in src/pages/QRScanPage.tsx src/pages/DivPage.tsx src/pages/ReportsPage.tsx; do
  echo "$F before: $(grep -c 'style={{' $F)"
done

# 비즈 anchor counts (precise) — wave 별 동일 룰
for F in src/pages/QRScanPage.tsx src/pages/DivPage.tsx src/pages/ReportsPage.tsx; do
  echo "=== $F ==="
  for ANCHOR in 'onClick=\{[^}]+\}' 'useState\(' 'useRef\(' 'useEffect\(' 'useMutation\(' 'useQuery\(' 'useNavigate\(' 'useParams\(' 'fetch\('; do
    echo "  $ANCHOR : $(grep -cE "$ANCHOR" $F)"
  done
done > /tmp/a3v-before.txt
cat /tmp/a3v-before.txt

# onClick precise extraction
for F in src/pages/QRScanPage.tsx src/pages/DivPage.tsx src/pages/ReportsPage.tsx; do
  grep -oE 'onClick=\{[^}]+\}' $F | sort | uniq > /tmp/a3v-clicks-$(basename $F .tsx).txt
done
```

### Step 2: Edit QRScanPage.tsx L205

Find:
```tsx
<div id={QR_REGION_ID} style={{ width:'100%' }} />
```
Replace with:
```tsx
<div id={QR_REGION_ID} className="w-full" />
```

### Step 3: DivPage.tsx — NO CHANGES

4 inline 모두 동적 변수 (color / chartH) — 옵션 N 룰 적용으로 잔존. **DivPage 는 수정하지 않음**.

단 만약 동적 변수가 사실 상수로 판명되면 옵션 X 로 변환 가능. 작업 중 확인:
- L471/L611/L720 의 `color` 는 어떻게 정의되는지 (props/calc/const) 확인
- 상수 hex 면 `text-[#hex]` arbitrary 로 변환 가능. 단 동적이면 그대로.

읽어보고 상수이면 변환, 동적이면 잔존.

### Step 4: Edit ReportsPage.tsx

**L178** — Find:
```tsx
<div style={{ display: 'flex', flexDirection: 'column', height: '100%', overflow: 'hidden' }}>
```
Replace with (기존 className 없으면 새로):
```tsx
<div className="flex flex-col h-full overflow-hidden">
```

**L278** — Find:
```tsx
<div style={{ height: '100%', display: 'flex', flexDirection: 'column', overflow: 'hidden', background: 'var(--surface-page)' }}>
```
Replace with:
```tsx
<div className="bg-surface-page flex flex-col h-full overflow-hidden">
```
(`var(--surface-page)` → tailwind 토큰 `bg-surface-page` — tailwind.config.js extend.colors 에 정의됨)

**L301** — Find:
```tsx
<div className="page-body" style={{ flex: 1, overflowY: 'auto' }}>
```
Replace with (기존 className 합병):
```tsx
<div className="page-body flex-1 overflow-y-auto">
```

### Step 5: After verification

```bash
cd /Users/jykevin/Documents/cbc7119-design/cha-bio-safety

# 1. style count drop
for F in src/pages/QRScanPage.tsx src/pages/DivPage.tsx src/pages/ReportsPage.tsx; do
  echo "$F after: $(grep -c 'style={{' $F)"
done
# 예상: QRScan 0 / Div 4 (잔존) / Reports 0
# 단 DivPage 4 가 사실은 상수면 더 줄어들 수 있음

# 2. 비즈 anchor identity (precise)
for F in src/pages/QRScanPage.tsx src/pages/DivPage.tsx src/pages/ReportsPage.tsx; do
  echo "=== $F ==="
  for ANCHOR in 'onClick=\{[^}]+\}' 'useState\(' 'useRef\(' 'useEffect\(' 'useMutation\(' 'useQuery\(' 'useNavigate\(' 'useParams\(' 'fetch\('; do
    echo "  $ANCHOR : $(grep -cE "$ANCHOR" $F)"
  done
done > /tmp/a3v-after.txt
diff /tmp/a3v-before.txt /tmp/a3v-after.txt
# MUST be empty

# 3. onClick precise
for F in src/pages/QRScanPage.tsx src/pages/DivPage.tsx src/pages/ReportsPage.tsx; do
  grep -oE 'onClick=\{[^}]+\}' $F | sort | uniq > /tmp/a3v-clicks-$(basename $F .tsx)-after.txt
  diff /tmp/a3v-clicks-$(basename $F .tsx).txt /tmp/a3v-clicks-$(basename $F .tsx)-after.txt
done
# All MUST be empty

# 4. emoji + 비색 (Phase A 보존)
for F in src/pages/QRScanPage.tsx src/pages/DivPage.tsx src/pages/ReportsPage.tsx; do
  echo "$F emoji: $(grep -cE '✓|✗|🔒|💾|🔥|⏰|📋|✅|⚠️|❌|🔧|🚨|🔍|🧯|📊' $F)"
  echo "$F 비색: $(grep -cE 'bg-warning[^-]|border-safe[^-]|border-warning[^-]|border-danger[^-]' $F)"
done
# 모두 0

# 5. TypeScript
./node_modules/.bin/tsc --noEmit 2>&1 | grep -E "src/pages/(QRScan|Div|Reports)Page\.tsx" | grep -c "error TS"
# MUST = 0
./node_modules/.bin/tsc --noEmit 2>&1 | grep -c "error TS"
# 전체 0

# 6. file scope
cd .. && git diff --name-only HEAD | grep -v "cha-bio-safety/src/pages/\(QRScan\|Div\|Reports\)Page.tsx" | grep -v ".planning/" | wc -l
# MUST = 0
```

### Step 6: Atomic commit

```bash
cd /Users/jykevin/Documents/cbc7119-design
git add cha-bio-safety/src/pages/QRScanPage.tsx cha-bio-safety/src/pages/DivPage.tsx cha-bio-safety/src/pages/ReportsPage.tsx
git commit --no-verify -m "$(cat <<'EOF'
feat(260528-a3v-01): Phase B Wave 1 — 워밍업 (QRScan + Div + Reports) inline style → tailwind

QRScanPage 1→0 (w-full) + DivPage 4→N (동적 color/chartH 옵션 N 잔존) + ReportsPage 3→0 (flex-col h-full overflow-hidden + bg-surface-page).
옵션 X+P+M+색변수N 승계. 시각 0 byte. 비즈 anchor identical (precise grep).
EOF
)"
```

### Step 7: SUMMARY.md

Write `cha-bio-safety/.planning/quick/260528-a3v-phase-b-wave-1/260528-a3v-SUMMARY.md`:
- 사용자 선택 옵션 (X+P+M+색변수N) — wdc/01h 승계
- Before/After grep count 표 (3 파일 + 합계)
- 변환 매핑 (변경된 줄별 Before/After)
- 잔존 inline style 목록 (DivPage 동적 케이스)
- 비즈 anchor identity 확인 (precise diff 결과)
- Phase A 보존 확인
- Self-Check: PASSED
- Next: Wave 2 (LoginPage + SplashScreen)

**Reference format:** `cha-bio-safety/.planning/quick/260528-01h-legalfindingspage-legalfindingdetailpage/260528-01h-SUMMARY.md`

Do NOT commit SUMMARY.md — orchestrator handles docs commit.

  </action>
  <verify>
    <automated>cd /Users/jykevin/Documents/cbc7119-design/cha-bio-safety && QR=$(grep -c 'style={{' src/pages/QRScanPage.tsx) && RP=$(grep -c 'style={{' src/pages/ReportsPage.tsx) && echo "QR=$QR RP=$RP (expected QR=0 RP=0)" && [ "$QR" = "0" ] && [ "$RP" = "0" ] && [ "$(./node_modules/.bin/tsc --noEmit 2>&1 | grep -c 'error TS')" = "0" ] && cd .. && [ "$(git diff --name-only HEAD | grep -v 'cha-bio-safety/src/pages/' | grep -v '.planning/' | wc -l | tr -d ' ')" = "0" ]</automated>
  </verify>
  <done>
    - QRScanPage.tsx inline style 0 (1→0)
    - ReportsPage.tsx inline style 0 (3→0)
    - DivPage.tsx inline style ≤4 (대부분 동적 잔존)
    - 비즈 anchor precise diff = empty (3 파일)
    - emoji 0 + 비색 0 (Phase A 보존)
    - TypeScript 0 error
    - 변경 파일 = 3 .tsx 만
  </done>
</task>

</tasks>

<commits>
- Pre-dispatch: `docs(260528-a3v): pre-dispatch plan for Phase B Wave 1`
- Task 1: `feat(260528-a3v-01): Phase B Wave 1 — 워밍업 ...`
- Docs: `docs(quick-260528-a3v): Wave 1 완료 — SUMMARY + STATE`
</commits>
