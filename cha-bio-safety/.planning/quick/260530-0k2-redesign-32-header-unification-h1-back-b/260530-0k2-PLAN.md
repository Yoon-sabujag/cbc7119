---
phase: 260530-0k2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/styles/components.css
autonomous: true
requirements:
  - HEADER-UNIFY-H1
must_haves:
  truths:
    - "DailyReportPage / ReportsPage / WorkLogPage 의 back 버튼은 시각적으로 32×32 정사각, 모서리 radius 7px 로 통일된다 (sketch-v2 §케이스 5 AFTER = .b-icon 표준)."
    - "src/styles/components.css 안 .back-btn 정의는 width:32px / height:32px / border-radius:7px 로 변경되어 있다."
    - "그 외 .back-btn 의 background / border / color / flex-shrink / display 속성은 한 byte 도 변경되지 않는다."
    - "cbc7119-preview.pages.dev 에서 3 페이지의 백버튼이 모두 동일 사이즈로 렌더된다 (사용자 시각 확인은 main push 자동 배포 후)."
  artifacts:
    - path: "src/styles/components.css"
      provides: ".back-btn 정의 (line 24 부근, §1. 자체 헤더 블록)"
      contains: "width: 32px; height: 32px; border-radius: 7px"
  key_links:
    - from: "src/styles/components.css :: .back-btn"
      to: "src/pages/DailyReportPage.tsx (1 use) / src/pages/ReportsPage.tsx (1 use) / src/pages/WorkLogPage.tsx (2 uses)"
      via: "className=\"back-btn\" 직접 참조"
      pattern: "className=\"back-btn\""
---

<objective>
Wave H1 (헤더 통일 v2 sketch). `.back-btn` CSS 정의의 width/height 를 34→32 로 줄이고 border-radius 를 7px 로 정정한다.

3 페이지 (DailyReport / Reports / WorkLog) 가 공유하는 단일 CSS class 한 곳만 수정 → 자동으로 3 페이지에 적용. 시각적으로 sketch-v2 §케이스 5 AFTER 의 `.b-icon` (표준 32×32 정사각 back/menu/icon-only 버튼) 과 동일해진다.

Purpose: 헤더 안 모든 버튼 세로 32px 통일 룰의 첫 wave. 가장 low risk + big win (CSS 1 곳 → 3 페이지) 부터 시작.

Output: components.css 1 파일 1 줄 수정. 시각 변화 = 백버튼 2px 가로/세로 축소 + radius 1px 축소.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/quick/260529-header-unification/sketch-v2.html

# 작업 워크트리 룰 (필수)
@CLAUDE.local.md

# Wave H1 spec (sketch-v2 line 685~720, 729~730 verbatim)
# 케이스 5: page-header CSS (.back-btn 34 → 32)
# BEFORE: <button class="b-34">  (line 178: width:34px; height:34px; border-radius:6px)
# AFTER:  <button class="b-icon"> (line 134: width:32px; height:32px; border-radius:7px)
#
# Wave H1 (low risk, big win). CSS 정의 1 곳 → 3 페이지
#   .back-btn CSS width/height 34 → 32 + border-radius 정정. DailyReport/Reports/WorkLog 일괄.

<interfaces>
<!-- 현재 정의 (수정 대상) — src/styles/components.css line 24 -->
<!-- §1. 자체 헤더 (W2/W3) 블록 안 -->

```css
.back-btn { width: 34px; height: 34px; border-radius: 8px; flex-shrink: 0; background: var(--surface-sunken); border: 1px solid var(--border-default); color: var(--text-secondary); cursor: pointer; display: flex; align-items: center; justify-content: center; }
```

<!-- AFTER (Wave H1 적용 후) -->
<!-- 변경: width 34→32 / height 34→32 / border-radius 8→7 -->
<!-- 보존: flex-shrink:0, background, border, color, cursor, display, align-items, justify-content -->

```css
.back-btn { width: 32px; height: 32px; border-radius: 7px; flex-shrink: 0; background: var(--surface-sunken); border: 1px solid var(--border-default); color: var(--text-secondary); cursor: pointer; display: flex; align-items: center; justify-content: center; }
```

<!-- 비즈 anchor — 변경 없음 -->
<!-- QRPrintPage 는 `qr-print-page-back-btn` 별도 class 사용 (공유 .back-btn 영향 X) -->
<!-- 다른 페이지 (Education / LegalFindings / RemediationDetail / WorkShift / AnnualPlan) 는 별도 wave (H2~H6) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: .back-btn CSS width/height 34→32, border-radius 8→7</name>
  <files>src/styles/components.css</files>
  <action>
src/styles/components.css line 24 의 `.back-btn` 정의를 다음과 같이 수정한다 (Edit 도구 사용, 한 줄 교체):

BEFORE (그대로 매칭):
```
  .back-btn { width: 34px; height: 34px; border-radius: 8px; flex-shrink: 0; background: var(--surface-sunken); border: 1px solid var(--border-default); color: var(--text-secondary); cursor: pointer; display: flex; align-items: center; justify-content: center; }
```

AFTER (교체):
```
  .back-btn { width: 32px; height: 32px; border-radius: 7px; flex-shrink: 0; background: var(--surface-sunken); border: 1px solid var(--border-default); color: var(--text-secondary); cursor: pointer; display: flex; align-items: center; justify-content: center; }
```

변경 사항 (정확히 3 곳, 그 외 한 byte 도 변경 금지):
1. `width: 34px` → `width: 32px`
2. `height: 34px` → `height: 32px`
3. `border-radius: 8px` → `border-radius: 7px`

보존 (변경 금지):
- 들여쓰기 2 spaces
- `flex-shrink: 0`
- `background: var(--surface-sunken)`
- `border: 1px solid var(--border-default)`
- `color: var(--text-secondary)`
- `cursor: pointer`
- `display: flex`
- `align-items: center`
- `justify-content: center`
- 같은 줄에 한 줄 단일 정의 형태 유지 (multi-line 변환 금지)
- `@layer components { ... }` 블록 안 위치 유지
- `/* ── §1. 자체 헤더 (W2/W3) ──────────────────────── */` 주석 바로 다음 줄 위치 유지

라인 84 의 메모리 룰 주석 `feedback_tailwind_w8_h8_is_48px (.date-nav-btn 28 / .back-btn 34 명시 px)` 은 SW1 컨텍스트 박제이므로 그대로 보존 (이 wave 의 변경 사실은 STATE.md / commit 메시지로 박제).

다른 파일 (DailyReportPage.tsx / ReportsPage.tsx / WorkLogPage.tsx) 은 수정하지 않는다. 그 파일들이 사용하는 `className="back-btn"` 참조는 그대로 두고, CSS 정의 변경만으로 자동 반영된다.
  </action>
  <verify>
    <automated>grep -n "\.back-btn" src/styles/components.css | head -5 && grep -c "width: 32px; height: 32px; border-radius: 7px" src/styles/components.css && grep -c "width: 34px; height: 34px; border-radius: 8px" src/styles/components.css</automated>
  </verify>
  <done>
- `grep -n "\.back-btn" src/styles/components.css` 가 line 24 부근에 단 1 곳의 정의를 보여준다 (라인 84 의 주석은 별도, 정의 아님)
- `grep -c "width: 32px; height: 32px; border-radius: 7px" src/styles/components.css` 결과 ≥ 1 (line 24 .back-btn 새 정의 매칭)
- `grep -c "width: 34px; height: 34px; border-radius: 8px" src/styles/components.css` 결과 = 0 (이전 정의 완전 제거)
- 다음 비즈 anchor grep 들이 그대로 유지된다 (한 byte 변경 0):
  - `grep -c "var(--surface-sunken)" src/styles/components.css` (기존 카운트와 동일)
  - `grep -c "var(--border-default)" src/styles/components.css` (기존 카운트와 동일)
  - `grep -c "flex-shrink: 0" src/styles/components.css` (기존 카운트와 동일)
- DailyReportPage / ReportsPage / WorkLogPage 의 `className="back-btn"` 참조는 변경되지 않는다 (4 곳 합 = 1+1+2):
  - `grep -c 'className="back-btn"' src/pages/DailyReportPage.tsx src/pages/ReportsPage.tsx src/pages/WorkLogPage.tsx` 합 = 4
- QRPrintPage 는 무관 (`qr-print-page-back-btn` 별도 class):
  - `grep -c '"back-btn"' src/pages/QRPrintPage.tsx` 결과 = 0 (`qr-print-page-back-btn` 만 매칭, exact `"back-btn"` 0)
- `npm run build` (또는 vite build) PASS — CSS 파싱 오류 없음
- TypeScript 0 error
  </done>
</task>

</tasks>

<verification>
## 자동 검증 (grep gate)

```bash
# 1. 새 정의 1 곳 존재
grep -c "width: 32px; height: 32px; border-radius: 7px" src/styles/components.css
# 기대: 1 이상 (.back-btn 라인 매칭)

# 2. 이전 정의 0 곳 (완전 제거)
grep -c "width: 34px; height: 34px; border-radius: 8px" src/styles/components.css
# 기대: 0

# 3. .back-btn 정의 라인 위치 확인
grep -n "\.back-btn {" src/styles/components.css
# 기대: line 24 부근 단 1 곳

# 4. 비즈 anchor 보존
grep -c "var(--surface-sunken)" src/styles/components.css  # 변경 0
grep -c "var(--border-default)" src/styles/components.css  # 변경 0

# 5. 참조 페이지 변경 0 (back-btn 사용 합 4)
grep -c 'className="back-btn"' src/pages/DailyReportPage.tsx src/pages/ReportsPage.tsx src/pages/WorkLogPage.tsx
# 기대: 1, 1, 2 (합 4)

# 6. QRPrintPage 무관 (exact 매칭 0)
grep -E 'className="back-btn"' src/pages/QRPrintPage.tsx
# 기대: 매칭 없음 (qr-print-page-back-btn 만 존재)

# 7. 빌드 PASS
npm run build
# 기대: exit 0, vite build PASS
```

## 시각 검증 (사용자, main push 자동 배포 후)

- cbc7119-preview.pages.dev 접속
- /reports / /daily-report / /worklog 3 페이지 진입
- 헤더 좌측 백버튼 시각: 34×34 → 32×32 (2px 작아짐) + radius 8→7 (살짝 더 각짐)
- 그 외 헤더 시각 변화 없음 (page-title / year-pager 그대로)
- DailyReport 의 캘리브 좌표 / WorkLog 의 calib 시스템 영향 없음 (헤더 영역 외부 좌표 변경 0)
</verification>

<success_criteria>
- [ ] src/styles/components.css 의 `.back-btn` 정의가 `width: 32px; height: 32px; border-radius: 7px` 로 변경됨
- [ ] 이전 정의 (`width: 34px; height: 34px; border-radius: 8px`) 가 파일에서 0 곳 (완전 제거)
- [ ] 그 외 `.back-btn` 의 9 개 속성 (flex-shrink / background / border / color / cursor / display / align-items / justify-content / 같은 줄 형태) 100% 보존
- [ ] DailyReportPage / ReportsPage / WorkLogPage 3 파일은 무수정 (className="back-btn" 4 참조 식별)
- [ ] QRPrintPage 는 별도 class (qr-print-page-back-btn) 라 영향 0
- [ ] `npm run build` PASS — CSS 파싱 / TypeScript 0 error
- [ ] git commit (메시지: feat(redesign/32-header-unification-h1): .back-btn 34→32, radius 8→7 (3 페이지 일괄))
- [ ] main 머지 → GitHub Actions 자동 cbc7119-preview 배포 (wrangler 명령 X — CLAUDE.local.md 워크트리 룰)
- [ ] STATE.md `Quick Tasks Completed` 표에 260530-0k2 행 추가
</success_criteria>

<output>
After completion, create `.planning/quick/260530-0k2-redesign-32-header-unification-h1-back-b/260530-0k2-SUMMARY.md`.

SUMMARY 에 박제할 항목:
- Wave H1 (헤더 통일 v2 sketch 첫 wave) 단일 atomic 완결
- 변경: components.css 1 파일 line 24 / 3 곳 (width/height/border-radius)
- 영향: 3 페이지 (DailyReport / Reports / WorkLog) 백버튼 시각 일괄 32×32 r:7 표준화
- 보존: 9 속성 (flex-shrink/background/border/color/cursor/display/align-items/justify-content + 한 줄 형태)
- 비즈 anchor: className="back-btn" 4 참조 (1+1+2) 변경 0, QRPrintPage 무관
- 다음 wave: H2 (Education/LegalFindings 44→32) 또는 사용자 결정에 따라 H3~H6 순차 진행
- 패턴: "공유 CSS class 단일 곳 수정 → N 페이지 일괄" 첫 사례 (rollback 매우 안전, low risk + big win)
</output>
