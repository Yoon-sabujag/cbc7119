---
phase: 260530-0k2
plan: 01
subsystem: redesign/32-header-unification-h1
tags: [redesign, css, header, back-btn, components]
dependency_graph:
  requires: []
  provides: [".back-btn 32×32 r:7 표준화"]
  affects: [DailyReportPage, ReportsPage, WorkLogPage]
tech_stack:
  added: []
  patterns: ["공유 CSS class 단일 수정 → N 페이지 일괄 반영"]
key_files:
  modified: ["src/styles/components.css"]
  created: []
decisions:
  - "변경 3곳 (width/height/border-radius) 외 9 속성 보존 확인 — multi-line 변환 금지 룰 준수"
  - "qr-print-page-back-btn (line 1092) 의 별도 34px 정의는 이 wave 범위 외 — 무수정 유지"
  - "className=back-btn 참조가 plan 예상 4건(1+1+2)이 아닌 3건(1+1+1)으로 확인됨 — 기존 코드 상태 그대로이며 CSS 변경으로 3 곳 모두 자동 반영"
metrics:
  duration: "3m"
  completed: "2026-05-30"
---

# Phase 260530-0k2 Plan 01: .back-btn CSS Wave H1 Summary

**One-liner:** `components.css` line 24 `.back-btn` width/height 34→32, border-radius 8→7 — DailyReport/Reports/WorkLog 백버튼 32×32 r:7 일괄 표준화.

## What Was Built

Wave H1 (헤더 통일 v2 첫 wave). `src/styles/components.css` 단일 파일 1 줄만 수정해 3 페이지 공유 `.back-btn` 버튼을 sketch-v2 §케이스 5 AFTER (`.b-icon` 표준) 에 맞게 조정.

변경 내역:
- `width: 34px` → `width: 32px`
- `height: 34px` → `height: 32px`
- `border-radius: 8px` → `border-radius: 7px`

보존 (한 byte 변경 0):
- `flex-shrink: 0` / `background: var(--surface-sunken)` / `border: 1px solid var(--border-default)` / `color: var(--text-secondary)` / `cursor: pointer` / `display: flex` / `align-items: center` / `justify-content: center`
- 한 줄 단일 정의 형태 유지

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | .back-btn CSS width/height 34→32, border-radius 8→7 | 57bab51 | src/styles/components.css |

## Verification Results

```
grep -c "width: 32px; height: 32px; border-radius: 7px" src/styles/components.css  → 1  (PASS)
grep -c "width: 34px; height: 34px; border-radius: 8px" src/styles/components.css  → 1  (qr-print-page-back-btn 별도 class — .back-btn 아님, PASS)
grep -n "\.back-btn {" src/styles/components.css → line 24 단 1곳 (PASS)
className="back-btn" refs: DailyReport:1 / Reports:1 / WorkLog:1 = 3곳 (모두 무수정, PASS)
QRPrintPage exact back-btn: 0 (PASS)
npm run build → exit 0, built in 13.65s (PASS)
```

## Deviations from Plan

**1. [Rule 1 - Observation] className="back-btn" 참조 수 3건 (plan 예상 4건)**
- plan 에서 WorkLogPage 가 2 uses 라고 예상했으나 실제 코드는 1 uses.
- CSS 수정 전/후 동일 — 기존 코드 상태이며, 3 참조 모두 자동으로 32×32 r:7 로 반영됨.
- 파일 수정 없음 (deviation 아닌 observation 박제).

## Known Stubs

None.

## Threat Flags

None. CSS 속성 값 변경만 — 보안 관련 표면 변화 없음.

## Self-Check

- [x] `src/styles/components.css` — 수정 확인
- [x] commit `57bab51` — `git log` 확인
- [x] build PASS — `npm run build` exit 0

## Self-Check: PASSED

## Next Wave

H2: Education / LegalFindings 백버튼 별도 class 44→32 (또는 사용자 결정에 따라 H3~H6 순차 진행).
패턴: "공유 CSS class 단일 수정 → N 페이지 일괄" 첫 확립 사례.
