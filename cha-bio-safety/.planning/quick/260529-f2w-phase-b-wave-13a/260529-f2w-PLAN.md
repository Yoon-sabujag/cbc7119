---
phase: 260529-f2w-phase-b-wave-13a-checkpoints-mobile
plan: 01
type: execute
wave: 13a
depends_on: [260529-epe]
files_modified:
  - src/pages/CheckpointsPage.tsx
autonomous: true
roadmap-wave: Tier 2 / Wave 13a (Checkpoints 모바일 zone — Tier 2 두 번째 wave)
---

<objective>
**Phase B Wave 13a — CheckpointsPage 모바일 zone.** Tier 2 두 번째 wave (Wave 12a StaffManage 직후).

CheckpointsPage (696줄, **80 total inline**):
- **모바일 zone (sweep 대상): 42 inline** = shared 36 + mobile-only 6
- **데스크톱 zone (skip, Wave 13b 후행): 38 inline**
  - L516-548 desktop-header (8)
  - L554-579 mobile-header (ternary alternate boundary — 8, Wave 13b 함께 처리)
  - L607-650 desktop-content data-table (22)

옵션 X+P+M+색변수N + INPUT_STYLE/LABEL_STYLE/SKELETON_STYLE 옵션 N 승계 (14번째 atomic).

### 토큰 alias (Wave 11 nkv + 12a epe precedent 그대로)
- `var(--bg)` → `bg-surface-page`
- `var(--bg2)` → `bg-surface-raised`
- `var(--bg3)` → `bg-surface-sunken`
- `var(--bd)` → `border-border-default`
- `var(--bd2)` → `border-border-strong`
- `var(--t1)` → `text-text-primary`
- `var(--t2)` → `text-text-secondary`
- `var(--t3)` → `text-text-tertiary`
- `var(--acl)` / `var(--accent)` → `accent`
- `var(--status-danger)` → `danger-bar`
- `var(--status-safe-bar)` → `safe-bar`
- `var(--surface-active)` → `surface-active`
- `var(--surface-sunken)` → `surface-sunken`
- `var(--text-primary/secondary/tertiary)` → `text-text-primary/secondary/tertiary`
- `var(--border-default)` → `border-border-default`

### 위험 anchor
- 데스크톱 zone L516-548 desktop-header + L554-579 mobile-header (boundary) + L607-650 desktop-content — **변경 절대 0** (Wave 13b 책임)
- module const `INPUT_STYLE` (L61) / `LABEL_STYLE` (L66) / `SKELETON_STYLE` (L421) — **정의 보존**, spread (L253/L274/L286 등) + 직접 참조 (L252/L259/L273 등 LABEL_STYLE 13곳, L298/L304/L310/L316/L322/L328/L336/L342/L346 INPUT_STYLE 9곳, L594/L595/L596 SKELETON_STYLE 3곳) 옵션 N 잔존
- 비즈 anchor (useState/useEffect/useMutation/useQuery/useNavigate/onClick) IDENTICAL

</objective>

<context>

### 모바일 zone (sweep 대상 — 42 inline)

#### Shared 36곳 — 모달/카드/스켈레톤 (양 모드 공통 렌더)

**BottomSheet (L29-36)**: 5곳
- L29 backdrop `fixed inset-0 bg-[rgba(0,0,0,0.6)] z-[50] flex flex-col justify-end`
- L32 sheet `bg-surface-raised rounded-t-2xl [animation:slideUp_0.28s_ease-out_both] max-h-[90vh] overflow-y-auto`
- L33 handle area `flex justify-center pt-3`
- L34 handle bar `w-8 h-1 bg-border-strong rounded-sm` (note: `w-8`=48px config override, `h-1`=4px OK, but `width:32` is from raw value → arbitrary `w-[32px]` if w-8≠32; 실제 tailwind.config: spacing.8=48, spacing.7=32 → handle bar 32px width 는 `w-[32px]` arbitrary 필수)
- L36 title `text-[16px] font-bold text-text-primary px-4 pt-3 pb-0` (또는 `text-base font-bold`)

**DesktopModal (L49-53)**: 3곳
- L49 backdrop `fixed inset-0 bg-[rgba(0,0,0,0.5)] z-[50] flex items-center justify-center`
- L52 modal `bg-surface-raised rounded-md w-[440px] max-h-[85vh] overflow-y-auto shadow-[0_8px_32px_rgba(0,0,0,0.18)]`
- L53 title `text-[16px] font-bold text-text-primary px-6 pt-5 pb-0`

**CheckPointModalContent (L250-387)**: 19곳
- L250 form-body `px-4 pt-4 pb-0 flex flex-col gap-3`
- L252 required-star `text-danger-bar`
- L253 select spread (옵션 N 잔존): `style={{ ...INPUT_STYLE, appearance: 'none', cursor: 'pointer' }}` → **그대로** (옵션 N — appearance:none+cursor:pointer non-config dynamic combo)
- L260 zone-row `flex gap-0 rounded-md overflow-hidden border border-border-default`
- L264 zone btn — **옵션 M 동적**: template literal conditional className `flex-1 h-9 border-0 cursor-pointer transition-[all_0.15s] ${form.zone === z ? 'bg-accent text-white' : 'bg-surface-active text-text-tertiary'}` (height:36 → h-9)
- L274 select spread (옵션 N 잔존) — 그대로
- L285 required-star `text-danger-bar`
- L286 select spread (옵션 N 잔존) — 그대로
- L335 required-star `text-danger-bar`
- L353 action-row `p-4 flex flex-col gap-2`
- L354 action-btns `flex gap-2`
- L357 btn-cancel `flex-1 h-11 bg-surface-active text-text-secondary border-0 rounded-md cursor-pointer` (height:44 → h-11)
- L362 btn-save — **옵션 M 동적**: `flex-1 h-11 bg-accent text-white border-0 rounded-md ${canSave && !isBusy ? 'cursor-pointer opacity-100' : 'cursor-not-allowed opacity-40'}` (또는 cursor/opacity 옵션 N spread 잔존)
- L369 btn-deactivate `w-full h-10 bg-[rgba(239,68,68,0.08)] text-danger-bar border-0 rounded-md cursor-pointer` (height:40 → h-10)
- L375 deactivate-confirm-box `p-4`
- L376 confirm-banner `bg-[rgba(239,68,68,0.08)] rounded-md p-3 text-text-secondary mb-2`
- L379 action-btns `flex gap-2`
- L382 btn-cancel `flex-1 h-11 bg-surface-active text-text-secondary border-0 rounded-md cursor-pointer`
- L387 btn-deactivate-confirm — **옵션 M 동적 opacity**: `flex-1 h-11 bg-danger-bar text-white border-0 rounded-md cursor-pointer ${deactivateMutation.isPending ? 'opacity-60' : 'opacity-100'}`

**CheckPointCard (L402-415)**: 8곳
- L402 card root `bg-surface-sunken rounded-xl px-4 py-3 min-h-12 flex items-center gap-2.5 cursor-pointer` + **옵션 M opacity**: `${cp.isActive === 0 ? 'opacity-45' : 'opacity-100'}` (minHeight:48 → min-h-12, gap:10 → gap-2.5)
- L403 cp-dot — **옵션 M 동적 bg**: `flex-shrink-0 ${cp.isActive !== 0 ? 'bg-safe-bar' : 'bg-text-tertiary'}` (note: w-[8px]/h-[8px]/rounded-full 는 이미 className)
- L404 cp-content `flex-1 min-w-0` (이미 className 있음, style merge)
- L405 cp-top `flex items-center gap-1.5 mb-0.5` (gap:6 → gap-1.5, marginBottom:2 → mb-0.5)
- L406 cp-location `text-text-primary overflow-hidden text-ellipsis whitespace-nowrap` (이미 text-body-sm font-bold className 있음)
- L407 cp-cat-badge `px-1.5 py-0.5 rounded-sm bg-[rgba(59,130,246,0.13)] text-accent flex-shrink-0` (padding:'2px 5px' → px-[5px] py-0.5; 또는 px-1.5+arbitrary)
- L411 cp-meta `text-text-secondary`
- L415 cp-action `text-accent font-bold flex-shrink-0`

**Skeleton wrap (L593)**: 1곳
- L593 skeleton-wrap `px-4 py-3 flex flex-col gap-2` (padding:'12px 16px' → px-4 py-3, gap:8 → gap-2)

#### Mobile-only 6곳 (L662-686)
- L662 card-list `px-4 pb-20 flex flex-col gap-2` (padding:'0 16px 80px' → px-4 pb-20, gap:8 → gap-2)
- L664 empty wrap `flex-1 flex flex-col items-center justify-center gap-2 px-4 py-10` (gap:8 → gap-2, padding:'40px 16px' → px-4 py-10)
- L665 empty title `text-text-primary` (이미 text-body font-bold className)
- L666 empty desc `text-text-secondary text-center` (이미 text-caption className)
- L678 mobile-fab-wrap `sticky bottom-0 px-4 pb-[calc(16px+var(--sab))] bg-surface-page` (paddingBottom calc 보존)
- L681 mobile-fab-btn (옵션 M cursor): `border-0 cursor-pointer` (이미 w-full h-[52px] bg-accent text-white rounded-xl flex items-center justify-center gap-2 text-body-sm font-bold className 있음 — 옵션 X 잔존 가능, border:none + cursor:pointer 만 남기면 단순)

### 데스크톱 zone — **변경 절대 0** (38곳, Wave 13b)

#### desktop-header (L516-548): 8곳
- L516 desktop-header wrap `flexShrink: 0`
- L517 cat-select-wrap `position: relative, width: 220`
- L522 select spread `{ ...INPUT_STYLE, height: 36, appearance: 'none', cursor: 'pointer', paddingRight: 32 }`
- L527 chevron-icon `position: absolute, right: 10, top: '50%', transform: 'translateY(-50%)', pointerEvents: 'none'`
- L532 filter-select `height: 36, padding: '0 10px', borderRadius: 8, border: '1px solid var(--border-default)', background: 'var(--surface-sunken)', color: 'var(--text-primary)', cursor: 'pointer', appearance: 'none' as any`
- L539 filter-select (동일 패턴)
- L543 count-label `flex: 1, color: 'var(--text-tertiary)'`
- L548 add-btn `height: 36, border: 'none', cursor: 'pointer'`

#### mobile-header (boundary ternary alternate, L554-579): 8곳
- L554 mobile-header wrap `flexShrink: 0`
- L555 mob-cat-wrap `position: 'relative'`
- L557 select spread `{ ...INPUT_STYLE, appearance: 'none', cursor: 'pointer', paddingRight: 36 }`
- L561 chevron-icon (동일 패턴)
- L566 mob-filter-row `display: 'flex', gap: 6`
- L568 mob-filter-select (동일 패턴)
- L575 mob-filter-select (동일 패턴)
- L579 mob-count `color: 'var(--text-tertiary)', alignSelf: 'center', whiteSpace: 'nowrap'`

#### desktop-content data-table (L607-650): 22곳
- L607, L609, L612-L618 (7 th cells), L623 (empty td), L629 (row), L633 (td location), L634 (td category-badge), L635 (cat badge span), L639, L640, L641 (td locationno), L642 (td status-cell), L644 (status span), L645 (status-dot), L649 (td action-cell), L650 (action span) = 22

**총 데스크톱 보존: 8 + 8 + 22 = 38 ✓**

### 예상

| 구역 | Before | After | 변환 |
|---|---:|---:|---:|
| BottomSheet (shared) | 5 | 0 | 5 |
| DesktopModal (shared) | 3 | 0 | 3 |
| CheckPointModalContent (shared) | 19 | ~3 (옵션 N spread 잔존 — L253/L274/L286) | ~16 |
| CheckPointCard (shared) | 8 | 0-1 | 7-8 |
| Skeleton wrap (shared) | 1 | 0 | 1 |
| **Shared subtotal** | **36** | **~3-4** | **~32-33** |
| Mobile-only | 6 | 0-1 | 5-6 |
| **합계 sweep** | **42** | **~3-5** | **~37-39** |
| Desktop-only (skip) | 38 | 38 | 0 |
| **전체 file** | **80** | **~41-43** | **~37-39** |

### 룰 (locked)
- 옵션 X+P+M+색변수N + INPUT_STYLE/LABEL_STYLE/SKELETON_STYLE 옵션 N
- 데스크톱 zone 절대 변경 0 (Wave 13b 책임)
- 비즈 anchor precise diff = empty
- emoji 카운트 변동 0 (Phase A 보존)
- 비표준 색 토큰 (no-suffix warning/safe/danger) = 0
- TypeScript 0 error
- 변경 파일 = 1 .tsx 만 (off-scope = 0)
- 단일 atomic commit (`feat(260529-f2w-01): Phase B Wave 13a — Checkpoints 모바일 zone (42 inline) → tailwind`)

### 함정 회피
- `w-7=32/w-8=48` config override → handle bar `width:32` 는 `w-[32px]` arbitrary, `width:32 height:4` 는 `w-[32px] h-1`
- `height:36` → `h-9` (36px=h-9 OK), `height:44` → `h-11`, `height:40` → `h-10`
- `minHeight:48` → `min-h-12` (h-12=48 OK)
- `gap:8` → `gap-2`, `gap:10` → `gap-2.5`, `gap:6` → `gap-1.5`
- `padding:'12px 16px'` → `px-4 py-3`, `padding:'40px 16px'` → `px-4 py-10`
- `[animation:slideUp_0.28s_ease-out_both]` underscore 필수
- `bg-[rgba(0,0,0,0.6)]` 공백 제거 + 소수점 0 prefix
- `text-caption leading-none` 패턴 유지 (작은 컨테이너 안)
- `boxShadow: '0 8px 32px rgba(0,0,0,.18)'` → `shadow-[0_8px_32px_rgba(0,0,0,0.18)]`
- `paddingBottom: 'calc(16px + var(--sab))'` → `pb-[calc(16px+var(--sab))]`

### 메모리 anchors
- `feedback_tailwind_w8_h8_is_48px.md` — w-8=48 config override
- `feedback_tailwind_token_class_pattern.md` — `text-danger-bar` (status- prefix 없음)
- `feedback_text_caption_leading_none.md` — text-caption 작은 컨테이너 leading-none 명시
- `feedback_design_changes_ask_first.md` — 시각 0 byte 룰 100%
- Wave 6 hbv (Schedule inp/lbl precedent) — module const 옵션 N
- Wave 11 nkv + 12a epe — token alias 매핑 precedent

</context>

<tasks>

<task type="auto">
  <name>Task 1: Sweep CheckpointsPage 모바일 zone — 42 inline (shared 36 + mobile-only 6)</name>
  <files>src/pages/CheckpointsPage.tsx</files>
  <action>

### Step 1: Before snapshot

```bash
cd /Users/jykevin/Documents/cbc7119-design/cha-bio-safety
F=src/pages/CheckpointsPage.tsx
echo "style before: $(grep -c 'style={{' $F)"

for ANCHOR in 'onClick=\{[^}]+\}' 'useState\(' 'useRef\(' 'useEffect\(' 'useMutation\(' 'useQuery\(' 'useNavigate\(' 'useParams\(' 'fetch\('; do
  echo "  $ANCHOR : $(grep -cE "$ANCHOR" $F)"
done > /tmp/f2w-before.txt

grep -oE 'onClick=\{[^}]+\}' $F | sort | uniq > /tmp/f2w-clicks.txt

# emoji 보존 (Phase A 후 화살표 ▸ 등 보존 룰)
echo "emoji before: $(grep -cE '✓|✗|✕|🔒|💾|🔥|⏰|📋|✅|⚠️|❌|🔧|🚨|🔍|🧯|📊|▸|◂|▴|▾' $F)" > /tmp/f2w-emoji-before.txt

# module const 정의 보존 확인
grep -c "^const INPUT_STYLE\|^const LABEL_STYLE\|^const SKELETON_STYLE" $F > /tmp/f2w-const-before.txt
# MUST = 3
```

### Step 2: Edit CheckpointsPage.tsx — 모바일 zone 만 (42곳)

**절대 변경 금지 (데스크톱 zone, 38곳)**:
- L516-552 desktop-header (isDesktop true branch, 8 inline)
- L554-582 mobile-header (ternary alternate boundary, 8 inline — Wave 13b 함께 처리)
- L606-658 desktop-content data-table (`{isDesktop && ...}` block, 22 inline)

**모바일 zone 변환 (42곳)**: PLAN context 매핑 그대로

#### Shared 변환 (36곳)

**BottomSheet (L29-36, 5곳)**:
- L29 → `className="fixed inset-0 bg-[rgba(0,0,0,0.6)] z-[50] flex flex-col justify-end"`
- L32 → `className="bg-surface-raised rounded-t-2xl [animation:slideUp_0.28s_ease-out_both] max-h-[90vh] overflow-y-auto"`
- L33 → `className="flex justify-center pt-3"`
- L34 → `className="w-[32px] h-1 bg-border-strong rounded-sm"` (width:32 = w-[32px] arbitrary; h-1=4 OK; rounded-sm=8? config 확인 — `borderRadius: 2`는 `rounded-[2px]` arbitrary)
  - 정확값: `className="w-[32px] h-1 bg-border-strong rounded-[2px]"`
- L36 → `className="text-base font-bold text-text-primary px-4 pt-3 pb-0"` (fontSize:16=text-base OK, fontWeight:700=font-bold OK)

**DesktopModal (L49-53, 3곳)**:
- L49 → `className="fixed inset-0 bg-[rgba(0,0,0,0.5)] z-[50] flex items-center justify-center"`
- L52 → `className="bg-surface-raised rounded-xl w-[440px] max-h-[85vh] overflow-y-auto shadow-[0_8px_32px_rgba(0,0,0,0.18)]"` (borderRadius:12=rounded-xl? config 확인 — tailwind default rounded-xl=12, but project override 가능. 안전: `rounded-[12px]` arbitrary)
- L53 → `className="text-base font-bold text-text-primary px-6 pt-5 pb-0"`

**CheckPointModalContent (L250-387, 19곳 → 옵션 N spread 3곳 잔존)**:
- L250 form-body → `className="form-body px-4 pt-4 pb-0 flex flex-col gap-3"` (padding:'16px 16px 0' → px-4 pt-4 pb-0; gap:12 → gap-3)
- L252 required-star → `className="required-star text-danger-bar"` (var(--status-danger) → text-danger-bar)
- L253 **잔존 옵션 N**: `style={{ ...INPUT_STYLE, appearance: 'none', cursor: 'pointer' }}` 그대로 (INPUT_STYLE spread + dynamic non-config combo)
- L260 zone-row → `className="zone-row flex gap-0 rounded-[8px] overflow-hidden border border-border-default"` (borderRadius:8 → rounded-[8px] 또는 rounded-md)
- L264 zone btn → **옵션 M template literal**: `className={` + `\`flex-1 h-9 border-0 cursor-pointer transition-[all_0.15s] ${form.zone === z ? 'bg-accent text-white' : 'bg-surface-active text-text-tertiary'}\`` + `}` (height:36=h-9, `(form.zone === z ? 'zone-btn active' : 'zone-btn inactive')` 의 기존 className 부분도 병합)
- L274 **잔존 옵션 N**: `style={{ ...INPUT_STYLE, appearance: 'none', cursor: 'pointer' }}` 그대로
- L285 required-star → `className="required-star text-danger-bar"`
- L286 **잔존 옵션 N**: `style={{ ...INPUT_STYLE, appearance: 'none', cursor: 'pointer' }}` 그대로
- L335 required-star → `className="required-star text-danger-bar"`
- L353 action-row → `className="action-row p-4 flex flex-col gap-2"` (gap:8=gap-2)
- L354 action-btns → `className="action-btns flex gap-2"`
- L357 btn-cancel → `className="btn-cancel flex-1 h-11 bg-surface-active text-text-secondary border-0 rounded-[8px] cursor-pointer"` (height:44=h-11)
- L362 btn-save → **옵션 M**: `className={` + `\`flex-1 h-11 bg-accent text-white border-0 rounded-[8px] ${canSave && !isBusy ? 'cursor-pointer opacity-100' : 'cursor-not-allowed opacity-40'}\`` + `}` (기존 `canSave && !isBusy ? 'btn-save' : 'btn-save disabled'` className 도 병합)
- L369 btn-deactivate → `className="btn-deactivate w-full h-10 bg-[rgba(239,68,68,0.08)] text-danger-bar border-0 rounded-[8px] cursor-pointer"` (height:40=h-10)
- L375 deactivate-confirm-box → `className="deactivate-confirm-box p-4"`
- L376 confirm-banner → `className="bg-[rgba(239,68,68,0.08)] rounded-[8px] p-3 text-text-secondary mb-2"`
- L379 action-btns → `className="action-btns flex gap-2"`
- L382 btn-cancel → `className="btn-cancel flex-1 h-11 bg-surface-active text-text-secondary border-0 rounded-[8px] cursor-pointer"`
- L387 btn-deactivate-confirm → **옵션 M opacity**: `className={` + `\`btn-deactivate-confirm flex-1 h-11 bg-danger-bar text-white border-0 rounded-[8px] cursor-pointer ${deactivateMutation.isPending ? 'opacity-60' : 'opacity-100'}\`` + `}`

**CheckPointCard (L402-415, 8곳)**:
- L402 card root → **옵션 M opacity**: `className={` + `\`${cp.isActive === 0 ? 'cp-card inactive opacity-45' : 'cp-card opacity-100'} bg-surface-sunken rounded-xl px-4 py-3 min-h-12 flex items-center gap-2.5 cursor-pointer\`` + `}` (borderRadius:12=rounded-xl 또는 rounded-[12px]; minHeight:48=min-h-12; padding:'12px 16px'=px-4 py-3; gap:10=gap-2.5)
- L403 cp-dot → **옵션 M bg**: `className={` + `\`cp-dot w-[8px] h-[8px] rounded-full flex-shrink-0 ${cp.isActive !== 0 ? 'bg-safe-bar' : 'bg-text-tertiary'}\`` + `}` (var(--status-safe-bar)=bg-safe-bar, var(--text-tertiary)=bg-text-tertiary)
- L404 cp-content → 기존 className 에 `flex-1 min-w-0` 병합
- L405 cp-top → `className="cp-top flex items-center gap-1.5 mb-0.5"` (gap:6=gap-1.5; marginBottom:2=mb-0.5)
- L406 cp-location → 기존 className 에 `text-text-primary overflow-hidden text-ellipsis whitespace-nowrap` 병합
- L407 cp-cat-badge → `className="cp-cat-badge text-caption leading-none px-[5px] py-0.5 rounded-[4px] bg-[rgba(59,130,246,0.13)] text-accent flex-shrink-0"` (padding:'2px 5px' → px-[5px] py-0.5; borderRadius:4=rounded-[4px])
- L411 cp-meta → 기존 className 에 `text-text-secondary` 병합
- L415 cp-action → 기존 className 에 `text-accent font-bold flex-shrink-0` 병합 (fontWeight:700=font-bold)

**Skeleton wrap (L593, 1곳)**:
- L593 → `className="skeleton-wrap px-4 py-3 flex flex-col gap-2"`

#### Mobile-only 변환 (6곳)

- L662 card-list → `className="card-list px-4 pb-20 flex flex-col gap-2"` (padding:'0 16px 80px'=px-4 pb-20 — `0` top 은 default 이므로 생략 OK)
- L664 empty wrap → `className="flex-1 flex flex-col items-center justify-center gap-2 px-4 py-10"` (gap:8=gap-2; padding:'40px 16px'=px-4 py-10)
- L665 empty title → 기존 className 에 `text-text-primary` 병합
- L666 empty desc → 기존 className 에 `text-text-secondary text-center` 병합
- L678 mobile-fab-wrap → `className="mobile-fab-wrap sticky bottom-0 px-4 pb-[calc(16px+var(--sab))] bg-surface-page"` (paddingBottom calc 보존; padding:'0 16px'=px-4)
- L681 mobile-fab-btn → 기존 className 에 `border-0 cursor-pointer` 병합

**중요 보존**:
- L61 `const INPUT_STYLE` 정의 그대로
- L66 `const LABEL_STYLE` 정의 그대로
- L421 `const SKELETON_STYLE` 정의 그대로
- L253/L274/L286 INPUT_STYLE spread 옵션 N 잔존
- L252/L259/L273/L285/L297/L303/L309/L315/L321/L327/L335/L341/L345 (13곳) `style={LABEL_STYLE}` 직접 참조 — 이미 인라인 아님, 손대지 말 것
- L298/L304/L310/L316/L322/L328/L336/L342/L346 (9곳) `style={INPUT_STYLE}` 직접 참조 — 이미 인라인 아님, 손대지 말 것
- L594/L595/L596 (3곳) `style={SKELETON_STYLE}` 직접 참조 — 이미 인라인 아님, 손대지 말 것

### Step 3: After verification

```bash
F=src/pages/CheckpointsPage.tsx
echo "style after: $(grep -c 'style={{' $F)"

for ANCHOR in 'onClick=\{[^}]+\}' 'useState\(' 'useRef\(' 'useEffect\(' 'useMutation\(' 'useQuery\(' 'useNavigate\(' 'useParams\(' 'fetch\('; do
  echo "  $ANCHOR : $(grep -cE "$ANCHOR" $F)"
done > /tmp/f2w-after.txt
diff /tmp/f2w-before.txt /tmp/f2w-after.txt

grep -oE 'onClick=\{[^}]+\}' $F | sort | uniq > /tmp/f2w-clicks-after.txt
diff /tmp/f2w-clicks.txt /tmp/f2w-clicks-after.txt

echo "emoji after: $(grep -cE '✓|✗|✕|🔒|💾|🔥|⏰|📋|✅|⚠️|❌|🔧|🚨|🔍|🧯|📊|▸|◂|▴|▾' $F)"
diff /tmp/f2w-emoji-before.txt <(echo "emoji after: $(grep -cE '✓|✗|✕|🔒|💾|🔥|⏰|📋|✅|⚠️|❌|🔧|🚨|🔍|🧯|📊|▸|◂|▴|▾' $F)" | sed 's/after/before/')

echo "비색: $(grep -cE 'bg-warning[^-]|border-safe[^-]|border-warning[^-]|border-danger[^-]' $F)"

# module const 정의 보존
echo "const def: $(grep -c '^const INPUT_STYLE\|^const LABEL_STYLE\|^const SKELETON_STYLE' $F)"
# MUST = 3

# 데스크톱 zone 보존 검증 (L516-548 + L554-582 + L606-658 변경 0)
# git diff 라인 보고
git diff src/pages/CheckpointsPage.tsx > /tmp/f2w-diff.txt
# 검증: diff 의 모든 hunk header 가 L516 이전 또는 L583-605 사이 또는 L659 이후만 포함하는지 검토
# 데스크톱 zone 안에 +/- 라인이 있으면 실패
grep -E '^@@' /tmp/f2w-diff.txt

./node_modules/.bin/tsc --noEmit 2>&1 | grep -c "error TS"
# MUST = 0

# vite build sanity (선택 — atomic commit 후 fail 시 revert)
# npm run build

# off-scope 변경 검증
cd .. && git diff --name-only HEAD | grep -v "cha-bio-safety/src/pages/CheckpointsPage.tsx" | grep -v ".planning/" | wc -l
# MUST = 0
```

### Step 4: Atomic commit

```bash
cd /Users/jykevin/Documents/cbc7119-design
git add cha-bio-safety/src/pages/CheckpointsPage.tsx
git commit --no-verify -m "$(cat <<'EOF'
feat(260529-f2w-01): Phase B Wave 13a — Checkpoints 모바일 zone (42 inline) → tailwind

Tier 2 두 번째 wave (Wave 12a StaffManage 직후). 모바일 zone (shared 36 + mobile-only 6) sweep.
데스크톱 zone 38곳 절대 변경 0 (Wave 13b 후행: desktop-header 8 + mobile-header boundary 8 + data-table 22).
INPUT_STYLE/LABEL_STYLE/SKELETON_STYLE module const 정의 + spread/직접참조 옵션 N 잔존.
토큰 alias 매핑 (var(--bg2/bg3/bd/bd2/t1/t2/t3/accent/status-danger/status-safe-bar 등) → tailwind tokens).
옵션 X+P+M+색변수N+module const N. 시각 0 byte 변경 (모바일 zone).
비즈 anchor identical. emoji 0 변동. TypeScript 0 error.
EOF
)"
```

### Step 5: SUMMARY.md

Write `cha-bio-safety/.planning/quick/260529-f2w-phase-b-wave-13a/260529-f2w-SUMMARY.md`.

Tier 2 두 번째 wave — zone 분할 패턴 14번째 atomic precedent.
PLAN.md 의 zone 매핑 표 + 실제 diff 통계 + 옵션 N 잔존 라인 + 데스크톱 보존 검증 결과 포함.

Do NOT commit SUMMARY.md.

  </action>
  <verify>
    <automated>cd /Users/jykevin/Documents/cbc7119-design/cha-bio-safety && S=$(grep -c 'style={{' src/pages/CheckpointsPage.tsx) && echo "after=$S (was 80, expect ~41-46)" && [ "$S" -ge "38" ] && [ "$S" -le "46" ] && [ "$(./node_modules/.bin/tsc --noEmit 2>&1 | grep -c 'error TS')" = "0" ] && [ "$(grep -cE 'bg-warning[^-]|border-safe[^-]|border-warning[^-]|border-danger[^-]' src/pages/CheckpointsPage.tsx)" = "0" ] && [ "$(grep -c '^const INPUT_STYLE\|^const LABEL_STYLE\|^const SKELETON_STYLE' src/pages/CheckpointsPage.tsx)" = "3" ] && cd .. && [ "$(git diff --name-only HEAD | grep -v 'cha-bio-safety/src/pages/CheckpointsPage.tsx' | grep -v '.planning/' | wc -l | tr -d ' ')" = "0" ]</automated>
  </verify>
  <done>
    - CheckpointsPage inline 38-46 (목표 ~41-43; 80 - 42 sweep + spread/leftover 잔존)
    - 데스크톱 zone (L516-548 + L554-582 + L606-658, 38곳) 변경 0
    - INPUT_STYLE / LABEL_STYLE / SKELETON_STYLE 정의 보존 (3 const def)
    - INPUT_STYLE spread (L253/L274/L286) + 직접 참조 (LABEL_STYLE 13 + INPUT_STYLE 9 + SKELETON_STYLE 3 = 25곳) 보존
    - 비즈 anchor precise diff = empty (onClick/useState/useEffect/useMutation/useQuery/useNavigate)
    - emoji 변동 0 (▸ 등 보존)
    - 비색 0 (status- prefix 없는 토큰 0)
    - TypeScript 0 error
    - 변경 파일 = 1 .tsx 만 (off-scope = 0)
    - 단일 atomic commit
  </done>
</task>

</tasks>
