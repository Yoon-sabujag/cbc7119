---
phase: 260528-h3z-phase-b-wave-5-remediationdetail
plan: 01
type: execute
wave: 5
depends_on: [260528-gsh]
files_modified:
  - src/pages/RemediationDetailPage.tsx
autonomous: true
roadmap-wave: Tier 1 / Wave 5 (조치 상세 — 단일 파일)
---

<objective>
**Phase B Wave 5 — 조치 상세.** RemediationDetailPage 11 inline → tailwind. 위험 anchor 없음.

옵션 X+P+M+색변수N 승계. 시각 0 byte.
</objective>

<context>

### RemediationDetailPage 11 inline 변환 매핑

**변환 (7건)**
- L269 root (기존 className `bg-surface-page`) → 합병 `flex-1 flex flex-col h-full overflow-hidden bg-surface-page`
- L284 `flex: 1, display: 'flex', alignItems: 'center', justifyContent: 'center'` → `flex-1 flex items-center justify-center`
- L285 (기존 `border-2 border-border-strong border-t-accent rounded-full`) `width: 28, height: 28, animation: 'spin .7s linear infinite'` → 합병 `border-2 border-border-strong border-t-accent rounded-full w-[28px] h-[28px] [animation:spin_.7s_linear_infinite]`
- L292 (기존 `text-body-sm text-text-secondary`) `flex: 1, display: 'flex', alignItems: 'center', justifyContent: 'center', padding: '0 24px', textAlign: 'center'` → 합병 `flex-1 flex items-center justify-center px-6 text-center text-body-sm text-text-secondary`
- L352 `whiteSpace: 'pre-wrap'` → `whitespace-pre-wrap`
- L355 동일 → `whitespace-pre-wrap`
- L512 `paddingBottom: 'calc(12px + var(--sab, 0px))'` → `pb-[calc(12px+var(--sab,0px))]`

**동적 conditional (옵션 M)**
- L301 multiline `paddingBottom: record.status === 'open' ? 'calc(72px + var(--sab, 0px))' : 24` (기존 className `flex-1 overflow-y-auto`) → 합병 `flex-1 overflow-y-auto ${record.status === 'open' ? 'pb-[calc(72px+var(--sab,0px))]' : 'pb-6'}` (옵션 M template literal)
- L518 `cursor: submitting ? 'not-allowed' : 'pointer', opacity: submitting ? 0.5 : 1` → conditional className `${submitting ? 'cursor-not-allowed opacity-50' : 'cursor-pointer opacity-100'}` (옵션 M)

**옵션 N 잔존 (2건)**
- L486 `fontFamily: 'inherit'` — tailwind 표현 불가 (Wave 01h LegalFindingDetail 동일 패턴)
- L496 동일

### 예상

| Before | After | 변환 |
|---:|---:|---:|
| 11 | 2 | 9 (-82%) |

### 룰 (locked)
- 옵션 X+P+M+색변수N 승계
- 비즈 anchor precise diff = empty
- Phase A 보존
- TypeScript 0 error
- 변경 파일 = 1 .tsx 만

### 메모리 anchors
- `feedback_tailwind_w8_h8_is_48px.md` (w-7=32 / w-8=48 — 28px arbitrary)
- `feedback_tailwind_token_class_pattern.md`

</context>

<tasks>

<task type="auto">
  <name>Task 1: Bulk apply RemediationDetailPage inline → tailwind</name>
  <files>src/pages/RemediationDetailPage.tsx</files>
  <action>

### Step 1: Before snapshot

```bash
cd /Users/jykevin/Documents/cbc7119-design/cha-bio-safety
F=src/pages/RemediationDetailPage.tsx
echo "before: $(grep -c 'style={{' $F)"
for ANCHOR in 'onClick=\{[^}]+\}' 'useState\(' 'useRef\(' 'useEffect\(' 'useMutation\(' 'useQuery\(' 'useNavigate\(' 'useParams\(' 'fetch\('; do
  echo "  $ANCHOR : $(grep -cE "$ANCHOR" $F)"
done > /tmp/h3z-before.txt
grep -oE 'onClick=\{[^}]+\}' $F | sort | uniq > /tmp/h3z-clicks.txt
```

### Step 2: Edit RemediationDetailPage.tsx

PLAN context "변환 매핑" 표 그대로 적용.

핵심:
- L285 spinner — w-[28px] arbitrary (w-7=32 함정 회피)
- L301 conditional paddingBottom — 옵션 M template literal
- L486/L496 fontFamily: 'inherit' — 옵션 N 잔존
- L518 cursor + opacity conditional — 옵션 M

### Step 3: Verification

```bash
F=src/pages/RemediationDetailPage.tsx
echo "after: $(grep -c 'style={{' $F)"
# 예상: 2

for ANCHOR in 'onClick=\{[^}]+\}' 'useState\(' 'useRef\(' 'useEffect\(' 'useMutation\(' 'useQuery\(' 'useNavigate\(' 'useParams\(' 'fetch\('; do
  echo "  $ANCHOR : $(grep -cE "$ANCHOR" $F)"
done > /tmp/h3z-after.txt
diff /tmp/h3z-before.txt /tmp/h3z-after.txt
# MUST empty

grep -oE 'onClick=\{[^}]+\}' $F | sort | uniq > /tmp/h3z-clicks-after.txt
diff /tmp/h3z-clicks.txt /tmp/h3z-clicks-after.txt
# MUST empty

echo "emoji: $(grep -cE '✓|✗|🔒|💾|🔥|⏰|📋|✅|⚠️|❌|🔧|🚨|🔍|🧯|📊' $F)"
echo "비색: $(grep -cE 'bg-warning[^-]|border-safe[^-]|border-warning[^-]|border-danger[^-]' $F)"
# 모두 0

./node_modules/.bin/tsc --noEmit 2>&1 | grep -c "error TS"
# MUST 0

cd .. && git diff --name-only HEAD | grep -v "cha-bio-safety/src/pages/RemediationDetailPage.tsx" | grep -v ".planning/" | wc -l
# MUST 0
```

### Step 4: Atomic commit

```bash
cd /Users/jykevin/Documents/cbc7119-design
git add cha-bio-safety/src/pages/RemediationDetailPage.tsx
git commit --no-verify -m "$(cat <<'EOF'
feat(260528-h3z-01): Phase B Wave 5 — RemediationDetailPage 11 inline → tailwind

11→2 (fontFamily inherit 2건 L486/L496 옵션 N 잔존). 9건 변환 (root flex + spinner animation + whitespace pre-wrap + paddingBottom calc + record.status / submitting conditional 옵션 M).
옵션 X+P+M+색변수N 승계. 시각 0 byte. 비즈 anchor identical. TypeScript 0 error.
EOF
)"
```

### Step 5: SUMMARY.md

Write `cha-bio-safety/.planning/quick/260528-h3z-phase-b-wave-5/260528-h3z-SUMMARY.md`.

Do NOT commit SUMMARY.md.

  </action>
  <verify>
    <automated>cd /Users/jykevin/Documents/cbc7119-design/cha-bio-safety && C=$(grep -c 'style={{' src/pages/RemediationDetailPage.tsx) && echo "after=$C" && [ "$C" -le "3" ] && [ "$(./node_modules/.bin/tsc --noEmit 2>&1 | grep -c 'error TS')" = "0" ] && [ "$(grep -cE 'bg-warning[^-]|border-safe[^-]|border-warning[^-]|border-danger[^-]' src/pages/RemediationDetailPage.tsx)" = "0" ] && cd .. && [ "$(git diff --name-only HEAD | grep -v 'cha-bio-safety/src/pages/' | grep -v '.planning/' | wc -l | tr -d ' ')" = "0" ]</automated>
  </verify>
  <done>
    - RemediationDetailPage inline ≤3 (11→2 예상)
    - 비즈 anchor precise diff = empty
    - emoji 0 + 비색 0
    - TypeScript 0 error
    - 변경 파일 = 1 .tsx 만
  </done>
</task>

</tasks>

<commits>
- Pre-dispatch: `docs(260528-h3z): pre-dispatch plan for Phase B Wave 5`
- Task 1: `feat(260528-h3z-01): Phase B Wave 5 — RemediationDetailPage 11 inline → tailwind`
- Docs: `docs(quick-260528-h3z): Wave 5 완료 — SUMMARY + STATE`
</commits>
