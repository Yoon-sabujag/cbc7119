---
phase: 260530-3y5
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/styles/components.css
autonomous: true
requirements:
  - HEADER-UNIFY-H1.5
must_haves:
  truths:
    - "ReportsPage / DailyReportPage / WorkLogPage 의 헤더 우측 nav 버튼 (‹ ›) 은 시각적으로 32×32 정사각, radius 7px, 화살표 글리프 18px 로 통일된다 (sketch-v3-nav-buttons.html 옵션 B 권장값 = .year-nav-btn-32 / .date-nav-btn-32 / .month-nav-btn-32 + .fs-18 표준)."
    - "src/styles/components.css 안 .year-nav-btn / .date-nav-btn / .date-nav-spacer / .month-nav-btn 4 정의가 width:32px, height:32px, border-radius:7px (spacer 는 radius 없음), font-size:18px (spacer 는 fs 없음) 로 통일된다."
    - "그 외 4 정의 의 border / background / color / font-weight / cursor / display / align-items / justify-content / line-height / flex-shrink / 들여쓰기 / 멀티-단일 라인 형태 / sketch 출처 주석은 한 byte 도 변경되지 않는다."
    - ".year-pager-slot (L27 width:24px) / .back-btn (L24 Wave H1 적용 32×32) / .page-header (L23) / .date-display / .month-display / .year-label / .date-nav / .month-nav / .year-pager / .month-nav-btn:disabled / .month-picker-trigger 는 무수정 (이번 wave 범위 외)."
    - "ReportsPage.tsx L289/295 (‹ ›) / DailyReportPage.tsx L349/355 (‹ ›) / WorkLogPage.tsx L598/620 (‹ ›) 6 곳의 className 참조는 무수정 — CSS 단일 곳 변경으로 자동 일괄 반영된다."
    - "cbc7119-preview.pages.dev 에서 3 페이지 헤더 nav 버튼이 모두 동일 사이즈 (32×32 r:7) + 동일 글리프 크기 (18px) 로 렌더된다 (사용자 시각 확인은 main push 자동 배포 후)."
  artifacts:
    - path: "src/styles/components.css"
      provides: ".year-nav-btn (L28) / .date-nav-btn (L95) / .date-nav-spacer (L120) / .month-nav-btn (L522) — 4 정의 32×32 r:7 fs:18 통일"
      contains: "width: 32px"
  key_links:
    - from: "src/styles/components.css :: .year-nav-btn"
      to: "src/pages/ReportsPage.tsx (L289/295 — 2 use, ‹ ›)"
      via: "className=\"year-nav-btn\" 직접 참조"
      pattern: "className=\"year-nav-btn\""
    - from: "src/styles/components.css :: .date-nav-btn"
      to: "src/pages/DailyReportPage.tsx (L349/355 — 2 use, ‹ ›)"
      via: "className=\"date-nav-btn\" 직접 참조"
      pattern: "className=\"date-nav-btn\""
    - from: "src/styles/components.css :: .date-nav-spacer"
      to: "src/pages/DailyReportPage.tsx (정렬용 invisible spacer)"
      via: "className=\"date-nav-spacer\" 직접 참조"
      pattern: "className=\"date-nav-spacer\""
    - from: "src/styles/components.css :: .month-nav-btn"
      to: "src/pages/WorkLogPage.tsx (L598/620 — 2 use, ‹ ›)"
      via: "className=\"month-nav-btn\" 직접 참조"
      pattern: "className=\"month-nav-btn\""
---

<objective>
Wave H1.5 (헤더 통일 v3 sketch — nav 버튼 hotfix). 헤더 우측 4 종 nav 버튼 (.year-nav-btn / .date-nav-btn / .date-nav-spacer / .month-nav-btn) 의 width/height 를 28→32 로 키우고, radius 를 7 로 통일 (date/month 6→7, year/spacer 는 유지), ‹ › 화살표 font-size 를 16→18 로 키운다 (sketch-v3 옵션 B 권장).

3 페이지 (Reports / DailyReport / WorkLog) 가 공유하는 단일 CSS 4 정의만 수정 → 자동으로 3 페이지에 적용. 시각적으로 Wave H1 의 .back-btn (32×32 r:7) 과 헤더 우측 nav 버튼이 동일한 표준 32 사이즈로 일치되어, 헤더 안 모든 버튼 32×32 룰이 완성된다.

Purpose: 헤더 통일 룰의 2 번째 wave. Wave H1 (back-btn 34→32) 직후 우측 nav 버튼을 같은 32 표준으로 맞춰 헤더 좌/우 시각 균형을 잡는다. sketch-v3 옵션 B (32+18px) 가 사용자 권장. CSS 4 정의 → 3 페이지 자동 일괄 적용.

Output: components.css 1 파일 4 정의 (L28 / L95 / L120 / L522) 부분 수정. 시각 변화 = nav 버튼 4px 가로/세로 확대 + radius 1px 통일 (date/month) + 화살표 글리프 2px 확대. .tsx 파일 무수정.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/quick/260529-header-unification/sketch-v3-nav-buttons.html
@.planning/quick/260530-0k2-redesign-32-header-unification-h1-back-b/260530-0k2-SUMMARY.md

# 작업 워크트리 룰 (필수)
@CLAUDE.local.md

# Wave H1.5 spec (sketch-v3 옵션 B 권장값)
# sketch-v3 line 168~178: .year-nav-btn-32 / .date-nav-btn-32 / .month-nav-btn-32 / .date-nav-spacer-32
#   = width:32px / height:32px / border-radius:7px / font-size:18px (.fs-18 결합)
#
# Wave H1.5 = Wave H1 와 동일 패턴 ("공유 CSS class 단일 곳 수정 → N 페이지 자동 일괄")
#   변경 정확히 4 정의:
#     .year-nav-btn  (L28)  width 28→32, height 28→32, font-size 16→18, radius 7 유지 (이미 7)
#     .date-nav-btn  (L95)  width 28→32, height 28→32, border-radius 6→7, font-size 16→18
#     .date-nav-spacer (L120) width 28→32, height 28→32 (radius/fs 없음 — 형태 유지)
#     .month-nav-btn (L522) width 28→32, height 28→32, border-radius 6→7, font-size 16→18

<interfaces>
<!-- 현재 정의 (수정 대상) — src/styles/components.css -->

<!-- L28 .year-nav-btn (단일 라인 형태) — §1. 자체 헤더 (W2/W3) 블록 -->
```css
  .year-nav-btn { width: 28px; height: 28px; border-radius: 7px; border: 1px solid var(--border-default); background: var(--surface-sunken); color: var(--text-primary); font-size: 16px; font-weight: 700; cursor: pointer; display: flex; align-items: center; justify-content: center; line-height: 1; }
```

<!-- L95~L109 .date-nav-btn (멀티 라인 형태) — §7. 15-daily-report W2 dateNav -->
```css
  .date-nav-btn {
    width: 28px;
    height: 28px;
    border-radius: 6px;  /* source navBtn 7 → 6 (sm-token 일관) */
    border: 1px solid var(--border-default);
    background: var(--surface-sunken);
    color: var(--text-primary);
    font-size: 16px;
    font-weight: 700;
    cursor: pointer;
    display: flex;
    align-items: center;
    justify-content: center;
    line-height: 1;  /* source line 40 lineHeight:'1' verbatim + memory feedback_text_caption_leading_none */
  }
```

<!-- L120~L124 .date-nav-spacer (멀티 라인 형태) — §7. 15-daily-report W2 dateNav -->
```css
  .date-nav-spacer {
    width: 28px;  /* source line 397 width:28 verbatim */
    height: 28px;
    flex-shrink: 0;
  }
```

<!-- L522~L534 .month-nav-btn (멀티 라인 형태) — §13.1 18-worklog W2 monthNav -->
```css
  .month-nav-btn {
    width: 28px;  /* source line 25 width:28 verbatim */
    height: 28px;
    border-radius: 6px; /* source navBtn radius 7 → 6 (sm-token 일관) */
    border: 1px solid var(--border-default);
    background: var(--surface-sunken);
    color: var(--text-primary);
    font-size: 16px;
    font-weight: 700;
    cursor: pointer;
    display: flex; align-items: center; justify-content: center;
    line-height: 1; /* source line 27 lineHeight:'1' verbatim + memory feedback_text_caption_leading_none */
  }
```

<!-- AFTER (Wave H1.5 적용 후) — 변경 사실 -->
<!-- .year-nav-btn:   width 28→32, height 28→32, font-size 16→18 (radius 7 유지) -->
<!-- .date-nav-btn:   width 28→32, height 28→32, border-radius 6→7, font-size 16→18 -->
<!-- .date-nav-spacer: width 28→32, height 28→32 -->
<!-- .month-nav-btn:  width 28→32, height 28→32, border-radius 6→7, font-size 16→18 -->

<!-- 보존 (한 byte 변경 0): -->
<!-- - border / background / color / font-weight / cursor / display / align-items / justify-content / line-height / flex-shrink -->
<!-- - 들여쓰기 (2 spaces / 4 spaces inner) -->
<!-- - 멀티 라인 형태 (.date-nav-btn / .date-nav-spacer / .month-nav-btn) 유지 -->
<!-- - 단일 라인 형태 (.year-nav-btn) 유지 -->
<!-- - 모든 sketch 출처 주석 verbatim 유지 -->

<!-- 무수정 (이번 wave 범위 외) -->
<!-- - .page-header (L23) — Wave H4 별도 처리 예정 -->
<!-- - .back-btn (L24) — Wave H1 에서 이미 32 적용 완료 (commit 260530-0k2) -->
<!-- - .page-title (L25) — 무관 -->
<!-- - .year-pager / .year-pager-slot (L26/L27) — 사용자 결정 무수정 (L27 24px 그대로) -->
<!-- - .year-label / .date-display / .month-display — 라벨 영역, nav 버튼 아님 -->
<!-- - .date-nav / .month-nav (래퍼) / .month-nav-btn:disabled / .month-picker-trigger — 무관 -->

<!-- 영향 .tsx (무수정) -->
<!-- - src/pages/ReportsPage.tsx L289/295 — className="year-nav-btn" ‹ › -->
<!-- - src/pages/DailyReportPage.tsx L349/355 — className="date-nav-btn" ‹ › + .date-nav-spacer 정렬용 -->
<!-- - src/pages/WorkLogPage.tsx L598/620 — className="month-nav-btn" ‹ › -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: nav-btn 4 정의 28→32, radius 7 통일, ‹ › fs 16→18 (components.css 부분 수정)</name>
  <files>src/styles/components.css</files>
  <action>
src/styles/components.css 의 4 개 CSS 정의를 다음과 같이 부분 수정한다 (Edit 도구로 정확히 4 번 적용, 각 정의별 1 회).

────────────────────────────────────────────────────────
변경 #1 — L28 .year-nav-btn (단일 라인 형태, in-place 2 곳 변경)
────────────────────────────────────────────────────────

BEFORE (정확 매칭, 단일 라인):
```
  .year-nav-btn { width: 28px; height: 28px; border-radius: 7px; border: 1px solid var(--border-default); background: var(--surface-sunken); color: var(--text-primary); font-size: 16px; font-weight: 700; cursor: pointer; display: flex; align-items: center; justify-content: center; line-height: 1; }
```

AFTER (정확 교체):
```
  .year-nav-btn { width: 32px; height: 32px; border-radius: 7px; border: 1px solid var(--border-default); background: var(--surface-sunken); color: var(--text-primary); font-size: 18px; font-weight: 700; cursor: pointer; display: flex; align-items: center; justify-content: center; line-height: 1; }
```

변경 사실 (3 byte):
- `width: 28px` → `width: 32px`
- `height: 28px` → `height: 32px`
- `font-size: 16px` → `font-size: 18px`

보존 (한 byte 변경 0):
- `border-radius: 7px` (이미 7 — 그대로)
- `border: 1px solid var(--border-default)`
- `background: var(--surface-sunken)`
- `color: var(--text-primary)`
- `font-weight: 700`
- `cursor: pointer`
- `display: flex`
- `align-items: center`
- `justify-content: center`
- `line-height: 1`
- 들여쓰기 2 spaces / 단일 라인 형태 / 같은 줄 모든 속성

────────────────────────────────────────────────────────
변경 #2 — L95~L109 .date-nav-btn (멀티 라인 형태, 4 줄 변경)
────────────────────────────────────────────────────────

BEFORE (정확 매칭, 멀티 라인 — 라인 95~109):
```
  .date-nav-btn {
    width: 28px;
    height: 28px;
    border-radius: 6px;  /* source navBtn 7 → 6 (sm-token 일관) */
    border: 1px solid var(--border-default);
    background: var(--surface-sunken);
    color: var(--text-primary);
    font-size: 16px;
    font-weight: 700;
    cursor: pointer;
    display: flex;
    align-items: center;
    justify-content: center;
    line-height: 1;  /* source line 40 lineHeight:'1' verbatim + memory feedback_text_caption_leading_none */
  }
```

AFTER (정확 교체):
```
  .date-nav-btn {
    width: 32px;
    height: 32px;
    border-radius: 7px;  /* source navBtn 7 → 6 (sm-token 일관) */
    border: 1px solid var(--border-default);
    background: var(--surface-sunken);
    color: var(--text-primary);
    font-size: 18px;
    font-weight: 700;
    cursor: pointer;
    display: flex;
    align-items: center;
    justify-content: center;
    line-height: 1;  /* source line 40 lineHeight:'1' verbatim + memory feedback_text_caption_leading_none */
  }
```

변경 사실 (4 byte):
- `width: 28px;` → `width: 32px;`
- `height: 28px;` → `height: 32px;`
- `border-radius: 6px;` → `border-radius: 7px;`
- `font-size: 16px;` → `font-size: 18px;`

보존 (한 byte 변경 0):
- 주석 `/* source navBtn 7 → 6 (sm-token 일관) */` 그대로 유지 (sketch 출처 박제 — 변경 사실은 commit 메시지로 박제)
- 주석 `/* source line 40 lineHeight:'1' verbatim + memory feedback_text_caption_leading_none */` 그대로
- 멀티 라인 형태 / 들여쓰기 4 spaces / 모든 속성 순서

────────────────────────────────────────────────────────
변경 #3 — L120~L124 .date-nav-spacer (멀티 라인 형태, 2 줄 변경)
────────────────────────────────────────────────────────

BEFORE (정확 매칭, 멀티 라인 — 라인 120~124):
```
  .date-nav-spacer {
    width: 28px;  /* source line 397 width:28 verbatim */
    height: 28px;
    flex-shrink: 0;
  }
```

AFTER (정확 교체):
```
  .date-nav-spacer {
    width: 32px;  /* source line 397 width:28 verbatim */
    height: 32px;
    flex-shrink: 0;
  }
```

변경 사실 (2 byte):
- `width: 28px;` → `width: 32px;`
- `height: 28px;` → `height: 32px;`

보존:
- 주석 `/* source line 397 width:28 verbatim */` 그대로 유지 (sketch 출처 박제)
- `flex-shrink: 0;` 그대로
- 멀티 라인 형태 / 들여쓰기

────────────────────────────────────────────────────────
변경 #4 — L522~L534 .month-nav-btn (멀티 라인 형태, 4 줄 변경)
────────────────────────────────────────────────────────

BEFORE (정확 매칭, 멀티 라인):
```
  .month-nav-btn {
    width: 28px;  /* source line 25 width:28 verbatim */
    height: 28px;
    border-radius: 6px; /* source navBtn radius 7 → 6 (sm-token 일관) */
    border: 1px solid var(--border-default);
    background: var(--surface-sunken);
    color: var(--text-primary);
    font-size: 16px;
    font-weight: 700;
    cursor: pointer;
    display: flex; align-items: center; justify-content: center;
    line-height: 1; /* source line 27 lineHeight:'1' verbatim + memory feedback_text_caption_leading_none */
  }
```

AFTER (정확 교체):
```
  .month-nav-btn {
    width: 32px;  /* source line 25 width:28 verbatim */
    height: 32px;
    border-radius: 7px; /* source navBtn radius 7 → 6 (sm-token 일관) */
    border: 1px solid var(--border-default);
    background: var(--surface-sunken);
    color: var(--text-primary);
    font-size: 18px;
    font-weight: 700;
    cursor: pointer;
    display: flex; align-items: center; justify-content: center;
    line-height: 1; /* source line 27 lineHeight:'1' verbatim + memory feedback_text_caption_leading_none */
  }
```

변경 사실 (4 byte):
- `width: 28px;` → `width: 32px;`
- `height: 28px;` → `height: 32px;`
- `border-radius: 6px;` → `border-radius: 7px;`
- `font-size: 16px;` → `font-size: 18px;`

보존:
- 모든 주석 verbatim 유지 (sketch 출처 박제 + 메모리 룰 박제)
- 한 줄 결합 형태 `display: flex; align-items: center; justify-content: center;` 그대로
- 멀티 라인 형태 / 들여쓰기

────────────────────────────────────────────────────────
범위 외 (절대 수정 금지)
────────────────────────────────────────────────────────

- `.year-pager-slot` (L27 width:24px) — 사용자 결정으로 무수정
- `.back-btn` (L24) — Wave H1 (260530-0k2) 에서 이미 32×32 r:7 적용 완료
- `.page-header` (L23) — Wave H4 별도 처리 예정
- `.page-title` (L25)
- `.year-pager` (L26) / `.year-label` (L29)
- `.date-nav` (L90~L94) / `.date-display` (L110~L119)
- `.month-nav` (L517~L521) / `.month-display` (L542~L549) / `.month-nav-btn:disabled` (L535~L538) / `.month-picker-trigger` (L539~L541)
- 그 외 components.css 의 모든 다른 정의

────────────────────────────────────────────────────────
.tsx 파일 (무수정 — 자동 반영)
────────────────────────────────────────────────────────

- src/pages/ReportsPage.tsx L289/295 — `className="year-nav-btn"` 2 곳 그대로
- src/pages/DailyReportPage.tsx L349/355 — `className="date-nav-btn"` 2 곳 + `className="date-nav-spacer"` 정렬용 그대로
- src/pages/WorkLogPage.tsx L598/620 — `className="month-nav-btn"` 2 곳 그대로

CSS 4 정의 변경만으로 3 페이지 6 곳 nav 버튼이 자동 일괄 32×32 r:7 fs:18 로 렌더된다.
  </action>
  <verify>
    <automated>cd cha-bio-safety && grep -n "\.year-nav-btn\|\.date-nav-btn\|\.date-nav-spacer\|\.month-nav-btn" src/styles/components.css | head -10 && echo "--- new 32px count ---" && grep -c "width: 32px" src/styles/components.css && echo "--- old 28px count (must be 0 in nav defs) ---" && grep -cE "width: 28px" src/styles/components.css && echo "--- new fs:18 count ---" && grep -c "font-size: 18px" src/styles/components.css && echo "--- old fs:16 in nav defs (must check context) ---" && grep -c "font-size: 16px" src/styles/components.css && echo "--- radius 7 count ---" && grep -c "border-radius: 7px" src/styles/components.css && echo "--- radius 6 count (must drop by 2) ---" && grep -c "border-radius: 6px" src/styles/components.css && echo "--- tsx unchanged ---" && grep -c 'className="year-nav-btn"' src/pages/ReportsPage.tsx && grep -c 'className="date-nav-btn"' src/pages/DailyReportPage.tsx && grep -c 'className="month-nav-btn"' src/pages/WorkLogPage.tsx && echo "--- build ---" && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
- `grep -n "\.year-nav-btn\|\.date-nav-btn\|\.date-nav-spacer\|\.month-nav-btn" src/styles/components.css` 가 정의 라인 4 곳 매칭 (L28 / L95 / L120 / L522 부근)
- 4 개 nav 정의 모두 `width: 32px` / `height: 32px` 로 변경됨 (이전 28×28 → 32×32)
- 4 개 nav 정의 모두 `border-radius: 7px` 로 통일됨 (.year-nav-btn 은 이미 7 / .date-nav-btn 과 .month-nav-btn 은 6→7 / .date-nav-spacer 는 radius 없음 그대로)
- 3 개 nav 버튼 (.year-nav-btn / .date-nav-btn / .month-nav-btn) 모두 `font-size: 18px` 로 변경 (.date-nav-spacer 는 fs 없음 그대로)
- 그 외 nav 정의 의 모든 속성 (border / background / color / font-weight / cursor / display / align-items / justify-content / line-height / flex-shrink / 모든 주석 / 들여쓰기 / 멀티-단일 라인 형태) 100% 보존
- `.year-pager-slot` (L27 width:24px) / `.back-btn` (L24) / `.page-header` (L23) / `.year-label` / `.date-display` / `.month-display` / `.date-nav` / `.month-nav` / `.year-pager` 무수정 확인
- 3 페이지 .tsx (ReportsPage / DailyReportPage / WorkLogPage) 무수정 확인:
  - `grep -c 'className="year-nav-btn"' src/pages/ReportsPage.tsx` = 2 (L289/295)
  - `grep -c 'className="date-nav-btn"' src/pages/DailyReportPage.tsx` = 2 (L349/355)
  - `grep -c 'className="month-nav-btn"' src/pages/WorkLogPage.tsx` = 2 (L598/620)
- `npm run build` PASS (vite build exit 0, CSS 파싱 오류 / TypeScript 0 error)
  </done>
</task>

</tasks>

<verification>
## 자동 검증 (grep gate)

```bash
# 1. 4 개 nav 정의 위치 확인 (정확히 4 정의 매칭)
grep -n "\.year-nav-btn\|\.date-nav-btn\|\.date-nav-spacer\|\.month-nav-btn" src/styles/components.css
# 기대: .year-nav-btn (L28), .date-nav-btn (L95), .date-nav-spacer (L120), .month-nav-btn (L522) 부근

# 2. 새 32px 추가 확인 (nav 4 정의 × 2 = 8 곳 추가)
# 기존 32px 카운트와 비교 (Wave H1 .back-btn 1 정의 = 1 곳 이미 있음 → 변경 후 총 9 곳 이상)
grep -c "width: 32px" src/styles/components.css
# 기대: 변경 전 카운트 + 4 이상 (4 nav 정의의 width 32px 8 곳 — width+height 합 8, 단 width 만 검색이므로 +4)

# 3. nav 정의 안 width 28px 완전 제거 확인
grep -nE "^  \.(year-nav-btn|date-nav-btn|date-nav-spacer|month-nav-btn)" src/styles/components.css
# 위 정의 라인 +1~+3 줄에서 28px 가 0 곳이어야 함 (수동 확인)

# 4. 새 font-size: 18px 3 곳 추가 (year/date/month nav-btn 3 곳, spacer 제외)
grep -c "font-size: 18px" src/styles/components.css
# 기대: 변경 전 카운트 + 3

# 5. radius 7 통일 확인 — date-nav-btn / month-nav-btn 의 radius 6 → 7
grep -c "border-radius: 7px" src/styles/components.css
# 기대: 변경 전 카운트 + 2 (date-nav-btn / month-nav-btn 2 정의 6→7)
grep -c "border-radius: 6px" src/styles/components.css
# 기대: 변경 전 카운트 - 2 (date-nav-btn / month-nav-btn 의 6 이 제거됨)

# 6. 보존 anchor (한 byte 변경 0)
grep -c "var(--surface-sunken)" src/styles/components.css       # 변경 0
grep -c "var(--border-default)" src/styles/components.css       # 변경 0
grep -c "line-height: 1" src/styles/components.css              # 변경 0
grep -c "/* source navBtn 7 → 6 (sm-token 일관) */" src/styles/components.css  # 변경 0 (주석 verbatim 유지)
grep -c "/* source line 397 width:28 verbatim */" src/styles/components.css     # 변경 0 (주석 verbatim 유지)
grep -c "/* source line 25 width:28 verbatim */" src/styles/components.css      # 변경 0 (주석 verbatim 유지)

# 7. 범위 외 정의 무수정
grep -n "\.year-pager-slot " src/styles/components.css     # L27 width:24px 그대로
grep -n "\.back-btn " src/styles/components.css            # L24 Wave H1 의 32×32 r:7 그대로
grep -n "\.page-header " src/styles/components.css         # L23 그대로

# 8. .tsx 6 곳 참조 무수정 (className 변경 0)
grep -c 'className="year-nav-btn"' src/pages/ReportsPage.tsx        # 기대: 2
grep -c 'className="date-nav-btn"' src/pages/DailyReportPage.tsx    # 기대: 2
grep -c 'className="month-nav-btn"' src/pages/WorkLogPage.tsx       # 기대: 2
grep -c 'className="date-nav-spacer"' src/pages/DailyReportPage.tsx # 기대: 1 이상

# 9. 빌드 PASS
npm run build
# 기대: exit 0, vite build PASS
```

## 시각 검증 (사용자, main push 자동 배포 후)

- cbc7119-preview.pages.dev 접속
- /reports / /daily-report / /worklog 3 페이지 진입
- 헤더 우측 ‹ › 버튼 시각:
  - 28×28 → 32×32 (4px 확대)
  - radius 6→7 (date/month 만, year 는 이미 7)
  - 화살표 글리프 16→18 (2px 확대)
- 헤더 좌측 백버튼 (Wave H1 의 32×32 r:7) 와 우측 nav 버튼이 동일 사이즈로 시각 일치
- 나머지 헤더 요소 (page-title / year-pager / date-display / month-display) 그대로
- DailyReport 의 캘리브 좌표 / WorkLog 의 calib 시스템 영향 없음 (헤더 영역 외부 좌표 변경 0)
- ReportsPage 의 A4 미리보기 / 일자별 카드 그리드 영향 없음
</verification>

<success_criteria>
- [ ] src/styles/components.css 의 4 개 nav 정의가 모두 32×32 r:7 (spacer 는 radius 없음) 로 통일됨
- [ ] 3 nav 버튼 (.year-nav-btn / .date-nav-btn / .month-nav-btn) 의 font-size 가 18px 로 통일됨
- [ ] 4 정의 의 그 외 속성 (border / background / color / font-weight / cursor / display / align-items / justify-content / line-height / flex-shrink / 들여쓰기 / 멀티-단일 라인 형태) 100% 보존
- [ ] 모든 sketch 출처 주석 verbatim 유지 (변경 사실은 commit 메시지로 박제 — 메모리 룰 feedback_planner_prompt_sketch_verbatim 패턴 일관)
- [ ] `.year-pager-slot` (L27) / `.back-btn` (L24) / `.page-header` (L23) / `.year-label` / `.date-display` / `.month-display` / `.date-nav` / `.month-nav` / `.year-pager` 무수정
- [ ] 3 페이지 .tsx (ReportsPage / DailyReportPage / WorkLogPage) 무수정 — className 참조 6 곳 + .date-nav-spacer 정렬용 그대로
- [ ] `npm run build` PASS — CSS 파싱 / TypeScript 0 error
- [ ] git commit (제안 메시지: `feat(redesign/32-header-unification-h1.5): nav-btn 28→32 (year/date/month) + radius 통일 + ‹ › fs 18`)
- [ ] main 직접 머지 → GitHub Actions 자동 cbc7119-preview 배포 (wrangler 명령 X — CLAUDE.local.md 워크트리 룰)
- [ ] STATE.md `Quick Tasks Completed` 표에 260530-3y5 행 추가
</success_criteria>

<output>
After completion, create `.planning/quick/260530-3y5-redesign-32-header-unification-h1-5-nav-/260530-3y5-SUMMARY.md`.

SUMMARY 에 박제할 항목:
- Wave H1.5 (헤더 통일 v3 sketch — nav 버튼 hotfix) 단일 atomic 완결
- 변경: components.css 1 파일 4 정의 (L28 .year-nav-btn / L95 .date-nav-btn / L120 .date-nav-spacer / L522 .month-nav-btn) 부분 수정
- 사이즈 통일: 28→32 (4 정의 width/height 8 byte) + radius 6→7 통일 (date/month 2 정의) + ‹ › font-size 16→18 (3 정의)
- 영향: 3 페이지 6 곳 nav 버튼 (Reports L289/295, DailyReport L349/355, WorkLog L598/620) 시각 자동 일괄 32×32 r:7 fs:18 표준화
- 보존: 4 정의 의 border / background / color / font-weight / cursor / display / align-items / justify-content / line-height / flex-shrink / 모든 sketch 출처 주석 / 들여쓰기 / 멀티-단일 라인 형태 100%
- 비즈 anchor: className 6 참조 변경 0, .date-nav-spacer 정렬용 참조 변경 0, .tsx 파일 무수정
- 헤더 좌/우 통일 완성: Wave H1 의 .back-btn 32×32 r:7 + Wave H1.5 의 nav-btn 32×32 r:7 → 헤더 안 모든 버튼 32×32 표준 룰 완성
- 패턴 재현: Wave H1 의 "공유 CSS class 단일 곳 → N 페이지 자동 일괄" 2 번째 사례 (rollback 매우 안전, 4 정의 부분 수정)
- 다음 wave 후보: H2 (Education / LegalFindings 44→32) / H3 / H4 (.page-header) / 사용자 결정에 따라 순차 진행
</output>
