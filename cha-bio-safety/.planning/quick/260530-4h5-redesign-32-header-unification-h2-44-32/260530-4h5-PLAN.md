---
phase: 260530-4h5
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/EducationPage.tsx
  - src/pages/LegalFindingsPage.tsx
autonomous: true
requirements:
  - REDESIGN-32-H2
must_haves:
  truths:
    - "보수교육 (/education) 모바일 헤더 백버튼이 시각적으로 32×32 정사각 — bg-surface-sunken 배경 + 7px 모서리 (GlobalHeader 햄버거와 짝)"
    - "소방점검 관리-지적사항 (/legal-findings) 모바일 헤더 백버튼이 시각적으로 32×32 정사각 — bg-surface-sunken 배경 + 7px 모서리"
    - "보수교육 헤더 우측 invisible spacer 가 w-7 (32px) 로 좌측 백버튼과 대칭 유지 (타이틀 중앙 정렬 보존)"
    - "양쪽 백버튼 ChevronLeft size={20} 그대로 (아이콘 크기 미변경)"
    - "양쪽 onClick={() => navigate(-1)} / aria-label / children 무수정 (비즈 anchor identical)"
    - "헤더 컨테이너 (Education h-12 / LegalFindings h-8) + 다른 헤더 자식 (title span / desktop block) 무수정"
    - "vite build PASS / TypeScript 0 error"
  artifacts:
    - path: "src/pages/EducationPage.tsx"
      provides: "보수교육 페이지 — 모바일 헤더 back 32, spacer 32"
      contains: "w-7 h-7 rounded-[7px] bg-surface-sunken"
    - path: "src/pages/LegalFindingsPage.tsx"
      provides: "소방점검 관리-지적사항 페이지 — 모바일 헤더 back 32"
      contains: "w-7 h-7 rounded-[7px] bg-surface-sunken"
  key_links:
    - from: "src/pages/EducationPage.tsx L462"
      to: "sketch-v2 §케이스 2 표준"
      via: "w-7 h-7 rounded-[7px] bg-surface-sunken — text-text-secondary 유지"
      pattern: "w-7 h-7 rounded-\\[7px\\] bg-surface-sunken"
    - from: "src/pages/EducationPage.tsx L469"
      to: "백버튼 대칭 spacer"
      via: "w-7 — 좌:back 32 / 우:spacer 32 대칭 유지"
      pattern: "<div className=\"w-7\" />"
    - from: "src/pages/LegalFindingsPage.tsx L293"
      to: "sketch-v2 §케이스 2 표준"
      via: "w-7 h-7 rounded-[7px] bg-surface-sunken — text-text-primary 유지 (페이지 컨벤션)"
      pattern: "w-7 h-7 rounded-\\[7px\\] bg-surface-sunken"
---

<objective>
Wave H2 — sketch v2 §케이스 2 표준 적용. EducationPage(보수교육) + LegalFindingsPage(소방점검 관리-지적사항) 두 페이지의 모바일 헤더 백버튼 사이즈를 44 → 32 로 통일하고 배경(`bg-surface-sunken`) + 모서리(`rounded-[7px]`) 를 GlobalHeader 햄버거 표준에 맞춤. 단일 wave atomic, 3 줄 정확 수정.

Purpose:
- v2 단일 룰("헤더 안 모든 버튼 세로 = 32px") 적용 중 가장 단순+낮은 위험 wave. 페이지 2 곳 / className 3 곳만 수정.
- Wave H1 / H1.5 의 "공유 CSS 1 곳 → N 페이지" 패턴과 달리 이번은 페이지 직접 수정 (공유 CSS 없음).
- 노안 친화 트레이드오프 — 사용자는 일관성을 우선 결정. 44px 큰 터치 영역은 포기.

Output:
- 2 파일 / 3 줄 변경 (EducationPage L462 + L469 + LegalFindingsPage L293)
- atomic commit: `feat(redesign/32-header-unification-h2): 보수교육 + 지적사항 백버튼 44→32 (sketch v2 §케이스 2 표준)`
- cbc7119-preview 자동 배포 (main push 트리거, wrangler 사용 X)
- SUMMARY.md (.planning/quick/260530-4h5-redesign-32-header-unification-h2-44-32/260530-4h5-SUMMARY.md)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@CLAUDE.local.md

# sketch v2 §케이스 2 (576-605 줄) — 이번 wave 의 표준 정의
@.planning/quick/260529-header-unification/sketch-v2.html

# 변경 대상 파일 (L455~470 + L285~298)
@src/pages/EducationPage.tsx
@src/pages/LegalFindingsPage.tsx

<interfaces>
<!-- Wave H2 가 정확히 만지는 3 위치 + 보존해야 할 className 토큰 -->

EducationPage.tsx L460-465 (모바일 헤더 back 버튼) — 변경 위치 1:
```tsx
<button
  onClick={() => navigate(-1)}
  className="text-text-secondary w-11 h-11 bg-transparent border-0 cursor-pointer flex items-center justify-center"
>
  <ChevronLeft size={20} />
</button>
```
→ AFTER (정확):
```tsx
<button
  onClick={() => navigate(-1)}
  className="text-text-secondary w-7 h-7 rounded-[7px] bg-surface-sunken border-0 cursor-pointer flex items-center justify-center"
>
  <ChevronLeft size={20} />
</button>
```

EducationPage.tsx L469 (invisible spacer) — 변경 위치 2:
```tsx
<div className="w-11" />
```
→ AFTER:
```tsx
<div className="w-7" />
```

LegalFindingsPage.tsx L290-294 (모바일 헤더 back 버튼) — 변경 위치 3:
```tsx
<button
  aria-label="뒤로 가기"
  onClick={() => navigate(-1)}
  className="text-text-primary absolute left-2 w-[44px] h-[44px] border-0 bg-transparent cursor-pointer flex items-center justify-center"
><ChevronLeft size={20} /></button>
```
→ AFTER (정확):
```tsx
<button
  aria-label="뒤로 가기"
  onClick={() => navigate(-1)}
  className="text-text-primary absolute left-2 w-7 h-7 rounded-[7px] bg-surface-sunken border-0 cursor-pointer flex items-center justify-center"
><ChevronLeft size={20} /></button>
```

토큰 함정 (CLAUDE.local.md 메모리 anchor 박제):
- `w-7` / `h-7` = **32px** (tailwind.config spacing override — Tailwind 기본 28 X)
- `w-8` / `h-8` = 48px (사용 금지 — override 함정)
- `w-11` / `h-11` = 44px

색 토큰 보존 차이 (페이지 컨벤션 — sketch v2 짝):
- EducationPage: `text-text-secondary` 유지 (GlobalHeader 표준 짝)
- LegalFindingsPage: `text-text-primary` 유지 (페이지 컨벤션, sketch 표준에 맞춰 강제 변경하지 않음 — 사용자 결정)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 보수교육 + 지적사항 모바일 헤더 백버튼 44→32 atomic 적용 (3 위치)</name>
  <files>src/pages/EducationPage.tsx, src/pages/LegalFindingsPage.tsx</files>
  <action>
sketch v2 §케이스 2 (cha-bio-safety/.planning/quick/260529-header-unification/sketch-v2.html L576-605) 의 표준을 두 페이지의 모바일 헤더 백버튼에 정확히 적용. 변경 정확히 3 위치, 다른 어떤 라인도 만지지 말 것.

### 변경 위치 1: src/pages/EducationPage.tsx L462 (보수교육 모바일 헤더 back)

Edit 도구 사용. old_string / new_string 정확히 다음 한 줄만 교체 (앞뒤 컨텍스트 줄 포함 권장 — Read 후 unique 한 5-7 줄 묶어서 교체):

BEFORE (L462 단일 라인):
```
          className="text-text-secondary w-11 h-11 bg-transparent border-0 cursor-pointer flex items-center justify-center"
```

AFTER (L462 단일 라인):
```
          className="text-text-secondary w-7 h-7 rounded-[7px] bg-surface-sunken border-0 cursor-pointer flex items-center justify-center"
```

변경 (4 개):
- `w-11` → `w-7` (44 → 32)
- `h-11` → `h-7` (44 → 32)
- `bg-transparent` → `bg-surface-sunken`
- `rounded-[7px]` 추가 (border-0 앞에 삽입 — 위 AFTER 라인 순서 그대로)

보존 (7 개 — 한 byte 변경 0):
- `text-text-secondary` (페이지 컨벤션, sketch §케이스 2 색 토큰)
- `border-0`
- `cursor-pointer`
- `flex`
- `items-center`
- `justify-center`
- 8개 단어 띄어쓰기 한 byte 변경 0

### 변경 위치 2: src/pages/EducationPage.tsx L469 (invisible spacer for 시각 균형)

BEFORE (L469 단일 라인):
```
        <div className="w-11" />
```

AFTER (L469 단일 라인):
```
        <div className="w-7" />
```

변경 (1 개):
- `w-11` → `w-7` (백버튼과 대칭 유지 — 좌:32 / 우:32, 타이틀 중앙 정렬 보존)

보존: `<div`, `className=`, `/ >` (괄호 형태 byte 변경 0)

### 변경 위치 3: src/pages/LegalFindingsPage.tsx L293 (지적사항 모바일 헤더 back)

BEFORE (L293 단일 라인):
```
            className="text-text-primary absolute left-2 w-[44px] h-[44px] border-0 bg-transparent cursor-pointer flex items-center justify-center"
```

AFTER (L293 단일 라인):
```
            className="text-text-primary absolute left-2 w-7 h-7 rounded-[7px] bg-surface-sunken border-0 cursor-pointer flex items-center justify-center"
```

변경 (4 개):
- `w-[44px]` → `w-7` (arbitrary → spacing 토큰, 44 → 32)
- `h-[44px]` → `h-7` (44 → 32)
- `bg-transparent` → `bg-surface-sunken`
- `rounded-[7px]` 추가 (border-0 앞에 삽입)

보존 (8 개 — 한 byte 변경 0):
- `text-text-primary` (페이지 컨벤션 유지 — sketch §케이스 2 의 secondary 와 다르지만 의도된 trade-off, 사용자 결정)
- `absolute` (페이지 레이아웃 — title 중앙 정렬 위해 필요)
- `left-2`
- `border-0`
- `cursor-pointer`
- `flex`
- `items-center`
- `justify-center`
- aria-label="뒤로 가기" / onClick / ChevronLeft size={20} / 한 줄 인라인 children 형태 (byte 변경 0)

### 무수정 (이번 wave 스코프 밖)

- 헤더 컨테이너: EducationPage L459 `h-12` / LegalFindingsPage L288 `h-8` — H4/H5 별도 wave (H2 스코프 X)
- 데스크톱 헤더 (LegalFindingsPage L300-): `{isDesktop && ...}` 블록 byte 변경 0
- ChevronLeft size={20} 양쪽 보존 — sketch v2 카탈로그 (§① back: `<svg class="ic lg">` 18px) 와 약간 다르지만 별도 결정 anchor (H2 스코프 X)
- 모든 onClick / aria-label / children / spread / 다른 className / import 무수정

### 사고 방지 가드 (CLAUDE.local.md 메모리 anchor)

1. **cwd 혼란 (260530-3y5 사고 재발 방지)**: 모든 작업은 `cha-bio-safety/` 워크트리 안에서. SUMMARY.md 절대 `cbc7119-design/.planning/` 에 만들지 말 것. 정확한 path: `cha-bio-safety/.planning/quick/260530-4h5-redesign-32-header-unification-h2-44-32/260530-4h5-SUMMARY.md`.

2. **w-7/h-7 = 32px 함정** (feedback_tailwind_w8_h8_is_48px.md): w-8 은 48px override 임. 32px 이 필요할 때는 반드시 `w-7 h-7` 사용. arbitrary `w-[32px] h-[32px]` 도 가능하지만 이번은 토큰 `w-7 h-7` 채택 (sketch §케이스 2 패턴 동일).

3. **wrangler 금지** (CLAUDE.local.md): 배포는 main push 자동 cbc7119-preview 만. `wrangler` / `npm run deploy` 절대 X.

4. **디자인 변경 전 상의** (feedback_design_changes_ask_first.md): 이번은 사용자가 사전 결정 + 시안 v2 §케이스 2 컨펌된 wave 라 그대로 진행. 다른 곳 손대지 말 것.

5. **자동 commit + push** — wave H1 / H1.5 와 동일하게 atomic 1 commit. 메시지:
```
feat(redesign/32-header-unification-h2): 보수교육 + 지적사항 백버튼 44→32 (sketch v2 §케이스 2 표준)

- EducationPage L462: w-11 h-11 bg-transparent → w-7 h-7 rounded-[7px] bg-surface-sunken
- EducationPage L469: w-11 → w-7 (spacer 대칭 유지)
- LegalFindingsPage L293: w-[44px] h-[44px] bg-transparent → w-7 h-7 rounded-[7px] bg-surface-sunken
- ChevronLeft size={20} / onClick / aria-label / 컨테이너 무수정
- sketch v2 §케이스 2 표준 적용. 노안 친화 ↔ 일관성 trade-off — 일관성 우선 (사용자 결정)
```
  </action>
  <verify>
    <automated>
cd /Users/jykevin/Documents/cbc7119-design/cha-bio-safety && \
  # Gate 1: 변경 적용 검증 — 2 파일에 정확한 AFTER 패턴 존재
  grep -F 'text-text-secondary w-7 h-7 rounded-[7px] bg-surface-sunken border-0 cursor-pointer flex items-center justify-center' src/pages/EducationPage.tsx | grep -vc '^#' | { read n; [ "$n" -eq 1 ] || { echo "FAIL: EducationPage AFTER 패턴 1회 출현 기대 (실제 $n)"; exit 1; }; } && \
  grep -F 'text-text-primary absolute left-2 w-7 h-7 rounded-[7px] bg-surface-sunken border-0 cursor-pointer flex items-center justify-center' src/pages/LegalFindingsPage.tsx | grep -vc '^#' | { read n; [ "$n" -eq 1 ] || { echo "FAIL: LegalFindingsPage AFTER 패턴 1회 출현 기대 (실제 $n)"; exit 1; }; } && \
  # Gate 2: BEFORE 잔존 0 — 두 파일 어디에도 w-11 h-11 bg-transparent / w-[44px] h-[44px] bg-transparent 없음
  ! grep -E 'w-11 h-11 bg-transparent' src/pages/EducationPage.tsx && \
  ! grep -E 'w-\[44px\] h-\[44px\] bg-transparent' src/pages/LegalFindingsPage.tsx && \
  # Gate 3: EducationPage spacer w-7 (L469 부근)
  grep -F '<div className="w-7" />' src/pages/EducationPage.tsx | grep -vc '^#' | { read n; [ "$n" -ge 1 ] || { echo "FAIL: EducationPage spacer w-7 미발견"; exit 1; }; } && \
  # Gate 4: BEFORE spacer w-11 잔존 0
  ! grep -F '<div className="w-11" />' src/pages/EducationPage.tsx && \
  # Gate 5: 비즈 anchor 보존 — ChevronLeft size={20} 양쪽 그대로
  grep -F 'ChevronLeft size={20}' src/pages/EducationPage.tsx >/dev/null && \
  grep -F 'ChevronLeft size={20}' src/pages/LegalFindingsPage.tsx >/dev/null && \
  # Gate 6: onClick navigate(-1) 양쪽 보존
  grep -F 'onClick={() => navigate(-1)}' src/pages/EducationPage.tsx >/dev/null && \
  grep -F 'onClick={() => navigate(-1)}' src/pages/LegalFindingsPage.tsx >/dev/null && \
  # Gate 7: aria-label 보존 (LegalFindingsPage)
  grep -F 'aria-label="뒤로 가기"' src/pages/LegalFindingsPage.tsx >/dev/null && \
  # Gate 8: TypeScript / build PASS
  npm run build 2>&1 | tail -5 && \
  echo "ALL GATES PASS"
    </automated>
  </verify>
  <done>
- src/pages/EducationPage.tsx 한 위치 (모바일 헤더 back L462) + 한 위치 (spacer L469) 변경 적용. 다른 byte 0 변경.
- src/pages/LegalFindingsPage.tsx 한 위치 (모바일 헤더 back L293) 변경 적용. 다른 byte 0 변경.
- npm run build PASS (TypeScript 0 error, PWA precache 생성).
- 8 grep gate 전부 통과.
- atomic commit 생성 + push (main → cbc7119-preview 자동 배포 트리거).
- SUMMARY.md (.planning/quick/260530-4h5-redesign-32-header-unification-h2-44-32/260530-4h5-SUMMARY.md) 작성.
  </done>
</task>

</tasks>

<verification>
phase 전체 verify (task verify Gate 1-8 외 추가):

1. **시각 검증** (사용자, cbc7119-preview 배포 후):
   - 보수교육 (/education) 모바일: 좌측 백버튼 32×32 + 둥근 모서리 7px + 어두운 회색 배경. ChevronLeft 가운데 정렬.
   - 보수교육 헤더 타이틀 "보수교육" 중앙 정렬 유지 (spacer 32 대칭 효과).
   - 소방점검 관리-지적사항 (/legal-findings) 모바일: 좌측 백버튼 32×32 + 둥근 모서리 7px + 어두운 회색 배경.
   - 두 페이지 헤더 컨테이너 (h-12 / h-8) 자체 사이즈 변화 0 — 안쪽 백버튼만 작아짐.

2. **회귀 가드** (자동 grep, Task 1 verify 안 포함):
   - 다른 페이지의 `w-11 h-11` / `w-[44px] h-[44px]` 패턴 영향 0 (이번 wave 스코프 X).
   - GlobalHeader.tsx 무수정.
   - components.css 무수정 (Wave H1/H1.5 와 다름).

3. **PWA 캐시 가드** (feedback_pwa_cache_invalidation.md): 배포 후 사용자가 "여전히 똑같다" 보고 시 PWA SW 캐시 의심. grep 으로 dist/ 검증 후 사용자에게 앱 재설치 유도.
</verification>

<success_criteria>
- [ ] EducationPage.tsx L462: `w-7 h-7 rounded-[7px] bg-surface-sunken` 정확히 1회 (다른 곳 X)
- [ ] EducationPage.tsx L469: `<div className="w-7" />` 정확히 1회 (BEFORE `w-11` 잔존 0)
- [ ] LegalFindingsPage.tsx L293: `w-7 h-7 rounded-[7px] bg-surface-sunken` 정확히 1회
- [ ] 양쪽 `text-*` 색 토큰 보존 (Education: secondary / LegalFindings: primary)
- [ ] 양쪽 onClick / aria-label / ChevronLeft size={20} byte 변경 0
- [ ] 헤더 컨테이너 (Education L459 h-12 / LegalFindings L288 h-8) byte 변경 0
- [ ] npm run build PASS (TypeScript 0 error)
- [ ] atomic commit (단일) 생성 + push 완료
- [ ] SUMMARY.md 작성 (cha-bio-safety/.planning/quick/260530-4h5-redesign-32-header-unification-h2-44-32/ 안)
- [ ] cbc7119-preview 자동 배포 트리거 (wrangler 사용 0)
</success_criteria>

<output>
After completion, create `.planning/quick/260530-4h5-redesign-32-header-unification-h2-44-32/260530-4h5-SUMMARY.md` (cha-bio-safety/ 워크트리 안의 경로). cbc7119-design/.planning/ 에 만들면 사고 (260530-3y5 패턴 재발). SUMMARY 안에:
- 변경 3 위치 (파일:라인 + BEFORE/AFTER)
- 보존된 비즈 anchor 리스트 (onClick / aria-label / ChevronLeft / 다른 className)
- 사고 박제 (있으면)
- 다음 wave 후보 (H3 = ElevatorPage 데스크톱 48→32 가장 큰 변화, 사용자 컨펌 필요 / H4 = WorkShift/AnnualPlan 모바일 헤더 컨테이너 + 버튼 통합)
</output>
