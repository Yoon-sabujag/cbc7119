---
phase: 260527-tb3-legalpage-sweep-emoji-8-lucide-4
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/quick/260527-tb3-legalpage-sweep-emoji-8-lucide-4/sketch/legalpage-sweep.html
  - src/pages/LegalPage.tsx
autonomous: false
requirements:
  - QUICK-260527-tb3-EMOJI-SWEEP
  - QUICK-260527-tb3-COLOR-TOKEN-SWEEP

must_haves:
  truths:
    - "LegalPage.tsx 안에 unicode emoji (✓ ✗ 🔒 💾) 가 단 하나도 남아 있지 않다"
    - "L380/L404/L407 의 ✓/✗ 가 Lucide <Check> / <X> 컴포넌트로 교체되어 있다"
    - "L414/L538/L676/L1024 의 🔒 가 Lucide <Lock> 컴포넌트 (적절한 size) 로 교체되어 있다"
    - "L599 의 💾 저장하기 가 사용자 선택안에 따라 Lucide <Save> 또는 텍스트만으로 정리되어 있다"
    - "L596/L1033 의 bg-warning (비표준 토큰) 이 사용자가 시안에서 고른 옵션 (A 또는 B) 으로 교체되어 있다"
    - "L1013/L1043 의 border-safe / border-warning (비표준) 이 사용자 결정에 따라 border 제거 또는 -bar 변종으로 교체되어 있다"
    - "lucide-react import 줄(line 5)에 Check, X, Lock, Save 가 추가되어 있다"
    - "submission-ppt 기능 (onClick / useMutation / useQuery / useState / useRef / useEffect / API 호출) 변경 0 byte"
    - "inline style 141 곳 영역은 변경 0 byte (별도 phase 작업)"
    - "TypeScript 컴파일 통과 (관련 신규 에러 0)"
  artifacts:
    - path: ".planning/quick/260527-tb3-legalpage-sweep-emoji-8-lucide-4/sketch/legalpage-sweep.html"
      provides: "Standalone Old/New 비교 시안 — 4 패턴 + 옵션 A/B + pill border 분기"
      min_lines: 200
    - path: "src/pages/LegalPage.tsx"
      provides: "§7.1 emoji 8곳 → Lucide + §2.3 색 토큰 4곳 정리 완료"
      contains: "import { ChevronLeft, Camera, Loader2, Check, X, Lock, Save } from 'lucide-react'"
  key_links:
    - from: "sketch/legalpage-sweep.html"
      to: "src/pages/LegalPage.tsx"
      via: "Task 2 checkpoint 에서 사용자가 옵션 A/B + pill border 결정 → Task 3 가 그 결정대로 TSX 패치"
      pattern: "checkpoint approval → conditional TSX sweep"
    - from: "src/pages/LegalPage.tsx import 줄"
      to: "lucide-react package"
      via: "ES module import (line 5)"
      pattern: "from 'lucide-react'"
---

<objective>
LegalPage.tsx (production submission-ppt 트랙 W1~W9 완료 페이지) 의 디자인 sweep.
잔존하는 unicode emoji 8곳을 §7.1 Iconography 룰에 따라 Lucide 아이콘으로,
비표준 색 토큰 4곳을 §2.3 토큰 룰에 따라 정리한다.

Purpose:
- §7.1 (이모지 금지, Lucide 통일) 룰 enforce — submission-ppt 트랙이 빠르게 진행되며 누락된 8곳 마무리
- §2.3 (색 토큰만 사용) 룰 enforce — bg-warning / border-safe / border-warning 비표준 4곳 정리
- redesign workflow 준수: 시안 HTML 먼저 → 사용자 컨펌 → TSX 적용
- 비즈니스 로직 / API / state 변경 0 byte 보장

Output:
1. `.planning/quick/260527-tb3-.../sketch/legalpage-sweep.html` — 4 패턴 Old/New + 옵션 분기 시안
2. `src/pages/LegalPage.tsx` — emoji 0 / 비표준 색 토큰 0 / 비즈니스 로직 동일
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@../../CLAUDE.local.md
@.planning/STATE.md
@docs/redesign-context/01-dashboard/design-system.md
@src/pages/LegalPage.tsx
@src/styles/tokens.css

<interfaces>
<!-- 현재 LegalPage.tsx 의 lucide import (line 5) — Task 3 가 확장할 대상 -->

From src/pages/LegalPage.tsx line 5 (현재):
```typescript
import { ChevronLeft, Camera, Loader2 } from 'lucide-react'
```

Task 3 변환 후 (확장):
```typescript
import { ChevronLeft, Camera, Loader2, Check, X, Lock, Save } from 'lucide-react'
```

<!-- 8곳 emoji 위치 (orchestrator pre-grep 으로 검증됨, 라인 번호 정확) -->

Line 380:  >{isSelected ? '✓' : ''}</button>
Line 404:  조치 전 {hasBefore ? '✓' : '✗'}
Line 407:  조치 후 {hasAfter ? '✓' : '✗'}
Line 414:  <span className="text-text-tertiary">🔒 제출 완료</span>
Line 538:  if (isLocked) return '🔒 제출 완료'                    -- string return, JSX 변환 또는 호출처 추적 필요
Line 599:  {genState === 'saving' ? '저장중...' : '💾 저장하기'}
Line 676:  🔒 제출 완료된 점검 — 재생성 불가
Line 1024: >🔒 종결</button>

<!-- 4곳 비표준 색 토큰 위치 -->

Line 596:  ... 'bg-warning text-text-on-accent' : 'bg-accent text-text-on-accent'  -- 저장 버튼 dirty/error 상태
Line 1013: 'bg-safe-bg text-safe border-safe' : 'bg-warning-bg text-warning border-warning'  -- pill
Line 1033: isDirty ? 'bg-warning text-text-on-accent' : 'bg-accent text-text-on-accent'  -- 저장 버튼 (반복)
Line 1043: 'bg-safe-bg text-safe border-safe' : 'bg-warning-bg text-warning border-warning'  -- pill (반복)
</interfaces>

<sketch_context>
참고용 (의도된 디자인 컨텍스트):
- /Users/jykevin/Documents/cbc7119-design/.planning/submission-ppt-sketches/s1-mobile-card.html
- /Users/jykevin/Documents/cbc7119-design/.planning/submission-ppt-sketches/s2-desktop-col1.html
- /Users/jykevin/Documents/cbc7119-design/.planning/submission-ppt-sketches/s3-desktop-col2-internal.html
- /Users/jykevin/Documents/cbc7119-design/.planning/submission-ppt-sketches/s4-desktop-col2-submission.html
- /Users/jykevin/Documents/cbc7119-design/.planning/submission-ppt-sketches/s5-desktop-col3-ppt-preview.html

룰 우선순위: 의도 sketch 가 §7.1 위반이어도 §7.1 따른다 (emoji 0).
</sketch_context>

<design_tokens>
색 토큰 (tokens.css 기반 — 시안 인라인 CSS 로 사용):
- --surface-page: #0a0d12
- --surface-raised: #1a1f27
- --surface-sunken: #0f1419
- --text-primary: #e6edf3
- --text-secondary: #adb6c0
- --text-tertiary: #8b949e
- --text-disabled: #6e7681
- --text-on-accent: #ffffff
- --accent: #3b82f6
- --status-safe-bar: #22c55e
- --status-safe-bg: rgba(34, 197, 94, 0.16)
- --status-warning-bar: #f59e0b
- --status-warning-bg: rgba(245, 158, 11, 0.16)
- --status-fire-bar: #ef4444
</design_tokens>
</context>

<tasks>

<task type="auto">
  <name>Task 1: LegalPage emoji + 색 토큰 sweep 시안 HTML 작성</name>
  <files>.planning/quick/260527-tb3-legalpage-sweep-emoji-8-lucide-4/sketch/legalpage-sweep.html</files>
  <action>
Standalone HTML 시안 작성. 외부 의존 0 (CSS/JS/이미지 인라인). 다크 surface 토큰 hex 직접 사용.

**구조 (위에서 아래로):**

1. **헤더 영역** — 페이지 제목 "LegalPage 디자인 sweep (260527-tb3)" + 부제 "§7.1 Iconography + §2.3 색 토큰 정리" + 한 줄 설명 "이모지 8곳 + 비표준 색 토큰 4곳. submission-ppt 기능 자체 무수정."
   - **금지:** "approved" / "거의 일치" / "완성" 같은 자신감 표현 X (메모리 `feedback_avoid_premature_confirmation.md`)

2. **§7.1 Pattern 1 — 체크박스 ✓ (L380)**
   - 좌측 Old: 4 칸 grid 체크박스, 일부 selected (`background: #3b82f6; color: white;`), unicode `✓` 텍스트로 렌더 — 폰트 굵기 차이 / baseline misalign 시각화
   - 우측 New: 동일 grid, selected 박스 안에 inline SVG (Lucide `Check`, stroke=2, round caps, size=14×14) — center align
   - 캡션: "L380 `{isSelected ? '✓' : ''}` → `<Check size={14} className='inline-block' />`"

3. **§7.1 Pattern 2 — 사진 상태 ✓/✗ (L404, L407)**
   - 좌측 Old: 카드 안에 "조치 전 ✓" "조치 후 ✗" 텍스트 (한국어 + emoji)
   - 우측 New: "조치 전 [Check 12px green]" "조치 후 [X 12px red]" — 색은 `--status-safe-bar` / `--status-fire-bar`
   - 캡션: "L404, L407 `{has? '✓' : '✗'}` → `{has ? <Check size={12} className='inline-block' /> : <X size={12} className='inline-block' />}`"

4. **§7.1 Pattern 3 — 잠금 🔒 (L414, L538, L676, L1024)**
   - 4개 변종 row 로 나란히:
     - **L414** (text-text-tertiary span 안 라벨): Old "🔒 제출 완료" / New "[Lock 12px] 제출 완료" (`align-text-bottom mr-1`)
     - **L538** (string return — 호출처가 받아 렌더): Old `return '🔒 제출 완료'` 텍스트만 / New "함수 시그니처 JSX 반환으로 변경" 또는 "호출처에서 Lock + 텍스트 분리" 두 패턴 모두 표시
     - **L676** (헤더 메시지 inline): Old "🔒 제출 완료된 점검 — 재생성 불가" / New "[Lock 14px] 제출 완료된 점검 — 재생성 불가"
     - **L1024** (button 텍스트): Old "🔒 종결" 버튼 / New "[Lock 12px] 종결" 버튼

5. **§7.1 + §2.3 Pattern 4 — 저장 💾 + 버튼 색 (L599, L596, L1033) — 옵션 분기 here**
   - 시각 비교: 3 row
     - **Row A — 옵션 A (단순)**:
       - Old (L596/L1033): `bg-warning` 주황 버튼 + "💾 저장하기" 텍스트
       - New (옵션 A): `bg-accent` 파랑 버튼 + "저장하기" 텍스트 (emoji 없음, dirty/clean 구분 텍스트 또는 disabled 처리)
       - 캡션: "옵션 A — bg-warning 폐기, 단일 `bg-accent`. dirty 구분은 disabled 상태나 텍스트 변화로."
     - **Row B — 옵션 B (의미 보존)**:
       - Old: 동일
       - New (옵션 B): dirty 일 때 → `bg-warning-bg text-warning` (outline 톤, 1013/1043 pill 과 일관) + "[Save 14px] 저장하기" / clean 일 때 → `bg-accent text-text-on-accent` (CTA)
       - 캡션: "옵션 B — dirty 의미 보존, outline 톤으로 변환. 1013/1043 pill 과 일관된 outline."
   - **사용자 결정 요청 명시:** "옵션 A 또는 옵션 B 중 선택 부탁드립니다. Save 아이콘 사용/생략도 같이 결정."

6. **§2.3 Pattern 5 — pill border (L1013, L1043) — 분기 here**
   - 사이드 by 사이드:
     - 좌측 Old: `bg-safe-bg text-safe border-safe` — pill (border-safe 가 tailwind config 에 정의 안 됨, fallback 깨질 위험)
     - 중앙 Option 1 (border 제거): `bg-safe-bg text-safe` 만 — 다른 페이지 pill 패턴 일관
     - 우측 Option 2 (-bar 변종): `bg-safe-bg text-safe border-safe-bar` — border 색 명시
   - 같은 비교 warning 톤도 한 줄 (`bg-warning-bg text-warning ...`)
   - 캡션: "옵션 1 또는 옵션 2 중 선택 부탁드립니다. 다른 페이지 (대시보드 status pill 등) 와 일관성 고려."

7. **푸터** — "Task 2 checkpoint 에서 결정사항: (a) emoji 매핑 OK, (b) 저장 버튼 옵션 A 또는 B, (c) pill border 옵션 1 또는 2. 결정 후 Task 3 TSX 적용."

**Lucide SVG inline 가이드 (시안 안에서 직접 그릴 것):**
- Check: `<svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"/></svg>`
- X: `<svg ... viewBox="0 0 24 24" ...><line x1="18" y1="6" x2="6" y2="18"/><line x1="6" y1="6" x2="18" y2="18"/></svg>`
- Lock: `<svg ... viewBox="0 0 24 24" ...><rect x="3" y="11" width="18" height="11" rx="2" ry="2"/><path d="M7 11V7a5 5 0 0 1 10 0v4"/></svg>`
- Save: `<svg ... viewBox="0 0 24 24" ...><path d="M19 21H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h11l5 5v11a2 2 0 0 1-2 2z"/><polyline points="17 21 17 13 7 13 7 21"/><polyline points="7 3 7 8 15 8"/></svg>`

**Spacing/Layout:**
- max-width: 1200px center
- 각 Pattern section: padding 24px / border-radius 12px / background `var(--surface-raised)` (#1a1f27)
- Old/New 좌우 분할: grid-template-columns 1fr 1fr / gap 16px
- 캡션은 text-tertiary (#8b949e), font-size 12px

**금지:**
- 외부 CSS/JS/이미지/font (모두 inline 또는 system font)
- "approved" / "완성" / "거의 일치" 헤더 X
- inline style 141 곳 리팩토링 시각화 X (이번 sketch 범위 밖 — 만약 보일 일 있어도 "이번 작업 제외" 명시)
- submission-ppt 기능 동작 시뮬레이션 X (디자인만)
  </action>
  <verify>
    <automated>test -f .planning/quick/260527-tb3-legalpage-sweep-emoji-8-lucide-4/sketch/legalpage-sweep.html && grep -q "polyline points=\"20 6 9 17 4 12\"" .planning/quick/260527-tb3-legalpage-sweep-emoji-8-lucide-4/sketch/legalpage-sweep.html && grep -q "옵션 A" .planning/quick/260527-tb3-legalpage-sweep-emoji-8-lucide-4/sketch/legalpage-sweep.html && grep -q "옵션 B" .planning/quick/260527-tb3-legalpage-sweep-emoji-8-lucide-4/sketch/legalpage-sweep.html && grep -q "border-safe-bar" .planning/quick/260527-tb3-legalpage-sweep-emoji-8-lucide-4/sketch/legalpage-sweep.html && [ "$(grep -cE 'approved|완성|거의 일치' .planning/quick/260527-tb3-legalpage-sweep-emoji-8-lucide-4/sketch/legalpage-sweep.html)" = "0" ]</automated>
  </verify>
  <done>
    - sketch/legalpage-sweep.html 파일 존재
    - 4 Pattern (체크박스 / 사진 상태 / 잠금 4변종 / 저장 버튼) + pill border 5번째 모두 Old/New 비교 표시
    - 옵션 A vs B (저장 버튼 색) 분기 명확히 보이고 사용자 결정 요청 명시
    - pill border 옵션 1 (제거) vs 옵션 2 (-bar) 분기 명확
    - Lucide SVG inline 렌더 (Check / X / Lock / Save)
    - 외부 의존 0, 인라인 CSS 만
    - "approved" / "완성" 자신감 헤더 0
    - 이 시안 자체에는 emoji 0 (의도 Old 비교 영역 제외 — 실제 unicode 문자 ✓✗🔒💾 가 Old 영역에 있어야 비교 의도)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 시안 컨펌 + 옵션 결정</name>
  <what-built>
Task 1 이 생성한 sketch HTML:
- 4 패턴 emoji → Lucide 변환 시각 비교 (8곳 모두)
- 저장 버튼 색 옵션 A (bg-accent 단일) vs 옵션 B (bg-warning-bg outline)
- pill border 옵션 1 (제거) vs 옵션 2 (-bar 변종)
  </what-built>
  <how-to-verify>
1. 브라우저로 `.planning/quick/260527-tb3-legalpage-sweep-emoji-8-lucide-4/sketch/legalpage-sweep.html` 열기
   - macOS: `open .planning/quick/260527-tb3-legalpage-sweep-emoji-8-lucide-4/sketch/legalpage-sweep.html`
2. 4 패턴 (체크박스 / 사진 상태 / 잠금 4변종 / 저장 버튼) 각각 Old → New 시각 비교 확인:
   - emoji 가 Lucide 로 자연스럽게 대체되는지
   - size (14/12) 가 컨텍스트별로 적절한지
   - align (inline-block / align-text-bottom) 이 baseline 깨지지 않는지
3. 저장 버튼 — **옵션 A vs B 선택**:
   - 옵션 A (단순, bg-accent 단일): "dirty 상태 시각 cue 가 사라져도 OK 인가?"
   - 옵션 B (의미 보존, bg-warning-bg outline): "dirty 일 때 pill 톤 outline 으로 변환 OK 인가?"
4. pill 1013/1043 — **옵션 1 vs 2 선택**:
   - 옵션 1 (border 제거, bg+text 만): "다른 페이지 pill 패턴과 일관"
   - 옵션 2 (border-safe-bar / border-warning-bar): "border 색 명시"
5. Save 아이콘 사용/생략 결정:
   - L599 에 `<Save size={14}>` 추가할지 / 텍스트만 갈지

응답 예시:
- "approved. 옵션 B + pill 옵션 1 + Save 아이콘 사용"
- "options revise: 저장 버튼 옵션 A 로, pill 옵션 2, Save 생략"
- "sketch fix: L538 호출처 분리 패턴 다시 보여줘"
  </how-to-verify>
  <resume-signal>"approved. 옵션 [A|B] + pill 옵션 [1|2] + Save [사용|생략]" 또는 수정 요청 메시지</resume-signal>
  <done>
    사용자가 (a) emoji 매핑 OK, (b) 저장 버튼 옵션 (A/B), (c) pill border 옵션 (1/2), (d) Save 아이콘 사용 여부 모두 확정.
    Task 3 는 이 결정대로 TSX 패치.
  </done>
</task>

<task type="auto">
  <name>Task 3: LegalPage TSX sweep — emoji 8곳 + 색 토큰 4곳</name>
  <files>src/pages/LegalPage.tsx</files>
  <action>
사용자가 Task 2 에서 결정한 옵션에 따라 LegalPage.tsx 패치.

**1. lucide import 확장 (line 5):**
```typescript
// 변경 전
import { ChevronLeft, Camera, Loader2 } from 'lucide-react'

// 변경 후
import { ChevronLeft, Camera, Loader2, Check, X, Lock, Save } from 'lucide-react'
```
Save 는 사용자가 "생략" 결정 시 제외.

**2. emoji 8곳 일괄 교체:**

- **L380** `{isSelected ? '✓' : ''}` → `{isSelected ? <Check size={14} className="inline-block" /> : null}`
- **L404** `조치 전 {hasBefore ? '✓' : '✗'}` → `조치 전 {hasBefore ? <Check size={12} className="inline-block" /> : <X size={12} className="inline-block" />}`
  (단, 색은 인근 className 에 status-safe / status-fire 가 이미 있으면 그대로 — currentColor 상속)
- **L407** L404 와 동일 패턴
- **L414** `<span className="text-text-tertiary">🔒 제출 완료</span>` → `<span className="text-text-tertiary"><Lock size={12} className="inline-block align-text-bottom mr-1" />제출 완료</span>`
- **L538** `if (isLocked) return '🔒 제출 완료'` — **함수 시그니처 처리**:
  - 이 함수는 string 을 return 하는 helper (genStateLabel 같은 이름). 호출처 추적:
    ```bash
    grep -n "getGenStateLabel\|genStateLabel\|getGenState\|getLockState" src/pages/LegalPage.tsx
    ```
  - 호출처가 단순 `{label}` 형태로 렌더하면 → 함수 반환 타입을 `ReactNode` 로 바꾸고 `return <><Lock size={12} className="inline-block align-text-bottom mr-1" />제출 완료</>` 로 변환
  - 만약 호출처가 여러 곳이고 다 단순 텍스트 렌더면 위 방식 OK
  - 만약 호출처에서 추가 변환 (예: `.toUpperCase()`) 하면 위험 → string 반환 유지하고 호출처에서 `{label.includes('제출 완료') ? <><Lock ... />{label.replace('🔒 ', '')}</> : label}` 같이 처리. 사용자 결정 필요시 일단 단순 변환 시도.
- **L599** `'💾 저장하기'` → 사용자 결정에 따라:
  - Save 사용: `<><Save size={14} className="inline-block align-text-bottom mr-1" />저장하기</>`
  - Save 생략: `'저장하기'`
- **L676** `🔒 제출 완료된 점검 — 재생성 불가` → `<Lock size={14} className="inline-block align-text-bottom mr-1" />제출 완료된 점검 — 재생성 불가`
- **L1024** `>🔒 종결</button>` → `><Lock size={12} className="inline-block align-text-bottom mr-1" />종결</button>`

**3. 색 토큰 4곳 (사용자 옵션 적용):**

- **L596** `'bg-warning text-text-on-accent'` →
  - 옵션 A: `'bg-accent text-text-on-accent'` (단일색, dirty/error 구분 제거)
  - 옵션 B: `'bg-warning-bg text-warning'` (outline 톤)
  - 삼항 분기 패턴은 보존, dirty/error 분기 자체는 옵션 A 의 경우에도 disabled 상태 유지하기 위해 className 구조 유지 (단순 색 치환만)

- **L1033** L596 과 같은 옵션 적용

- **L1013** `'bg-safe-bg text-safe border-safe' : 'bg-warning-bg text-warning border-warning'` →
  - 옵션 1: `'bg-safe-bg text-safe' : 'bg-warning-bg text-warning'` + 부모 className `border` 토큰도 빼기 (`border` 자체가 className 에 있으면 같이 제거)
  - 옵션 2: `'bg-safe-bg text-safe border-safe-bar' : 'bg-warning-bg text-warning border-warning-bar'` (border 색만 변경, border 클래스 유지)

- **L1043** L1013 과 같은 옵션 적용

**4. DO NOT TOUCH (확인 필수):**
- `onClick`, `onChange`, `useMutation`, `useQuery`, `useState`, `useRef`, `useEffect`, `useNavigate`, `useParams` 등 React 훅 / 핸들러 함수 호출 — 변경 0
- `legalApi.*`, `inspectionApi.*`, fetch 직접 호출 — 변경 0
- inline style (`style={{ ... }}`) 141 곳 — 변경 0 byte (별도 phase B 작업)
- LegalFindingsPage / LegalFindingDetailPage / 다른 파일 — 변경 0
- 함수 시그니처 / 컴포넌트 props / state shape — 변경 0 (L538 함수 반환 타입 string → ReactNode 는 예외, 그 외 0)
- submission-ppt 트랙에서 추가된 새 state / 새 함수 / 새 API 호출 — 모두 그대로

**5. 변경 후 자체 검증 (executor 가 직접 수행):**
- 변경 라인 git diff 가 ±50 줄 이내 (대략) — 그 이상이면 inline style 영역 침범 의심 → 롤백
- `grep -c "style=" src/pages/LegalPage.tsx` 변경 전후 동일해야 함
  </action>
  <verify>
    <automated>cd /Users/jykevin/Documents/cbc7119-design/cha-bio-safety && [ "$(grep -cE '✓|✗|🔒|💾' src/pages/LegalPage.tsx)" = "0" ] && [ "$(grep -cE 'bg-warning[^-]|border-safe[^-]|border-warning[^-]' src/pages/LegalPage.tsx)" = "0" ] && grep -q "Check" src/pages/LegalPage.tsx && grep -q "Lock" src/pages/LegalPage.tsx && grep -E "from 'lucide-react'" src/pages/LegalPage.tsx | grep -q "Check" && grep -E "from 'lucide-react'" src/pages/LegalPage.tsx | grep -q "Lock" && npx tsc --noEmit 2>&1 | grep -v "node_modules" | grep -E "src/pages/LegalPage.tsx" | grep -E "error TS" | wc -l | tr -d ' ' | grep -q "^0$"</automated>
  </verify>
  <done>
    - `grep -E '✓|✗|🔒|💾' src/pages/LegalPage.tsx` 결과 0 (이모지 0)
    - `grep -E 'bg-warning[^-]|border-safe[^-]|border-warning[^-]' src/pages/LegalPage.tsx` 결과 0 (비표준 색 토큰 0; `bg-warning-bg` / `border-safe-bar` 는 OK 이므로 negative lookahead `[^-]` 로 구분)
    - lucide-react import 줄에 `Check`, `X`, `Lock` 모두 포함 (Save 는 사용자 옵션)
    - L380/L404/L407 에 `<Check>` 또는 `<X>` 렌더
    - L414/L676/L1024 에 `<Lock>` 렌더
    - L538 함수 반환 또는 호출처에서 Lock + 텍스트 분리 처리
    - L596/L1033 에 사용자 선택 옵션 (A 또는 B) 적용
    - L1013/L1043 에 사용자 선택 옵션 (1 또는 2) 적용
    - `npx tsc --noEmit` LegalPage.tsx 신규 에러 0
    - `grep -c "style=" src/pages/LegalPage.tsx` 변경 전후 동일 (inline style 141곳 변경 0)
    - 다른 파일 변경 0 (`git diff --name-only HEAD` 에 LegalPage.tsx 만)
    - 비즈니스 로직 grep 변경 0 (`onClick`, `useMutation`, `legalApi`, `fetch` 호출 라인 grep 결과 변경 전후 동일)
  </done>
</task>

</tasks>

<verification>
**플랜 전체 검증 (Task 3 완료 후):**

1. **§7.1 통과:**
   - `grep -E '✓|✗|🔒|💾|🔥|⏰|📋|✅|⚠️|❌|🔧|🚨|🔍|🧯|📊' src/pages/LegalPage.tsx | wc -l` → 0

2. **§2.3 통과:**
   - `grep -E 'bg-warning[^-]|border-safe[^-]|border-warning[^-]' src/pages/LegalPage.tsx | wc -l` → 0
   - `bg-warning-bg`, `border-safe-bar`, `border-warning-bar` 는 valid 토큰이므로 통과

3. **submission-ppt 기능 무수정:**
   - `git diff HEAD -- src/pages/LegalPage.tsx | grep -E '^[+-]' | grep -E 'onClick|useMutation|useQuery|legalApi|fetch|useState|useRef|useEffect'` → 변경 라인 0 (이상적), 단 emoji 인접 onClick 핸들러는 className/JSX 만 바뀌므로 OK

4. **TypeScript:**
   - `npx tsc --noEmit` 통과 (LegalPage.tsx 신규 에러 0)

5. **inline style 변경 0:**
   - `grep -c "style=" src/pages/LegalPage.tsx` 변경 전 = 변경 후

6. **다른 파일 변경 0:**
   - `git diff --name-only HEAD` → `src/pages/LegalPage.tsx` 와 sketch HTML 만
</verification>

<success_criteria>
- [ ] sketch HTML 작성 + 사용자 컨펌 완료 (옵션 결정)
- [ ] LegalPage.tsx 안 unicode emoji 0
- [ ] LegalPage.tsx 안 비표준 색 토큰 (`bg-warning[^-]`, `border-safe[^-]`, `border-warning[^-]`) 0
- [ ] lucide-react import 확장 (Check, X, Lock, Save? 적용)
- [ ] 사용자가 선택한 옵션 (저장 버튼 A/B, pill border 1/2) 정확히 적용
- [ ] submission-ppt 기능 동작 변경 0 byte (수동 확인 — Task 3 이후 별도 dev 서버 확인 권장하지만 plan 범위는 아님)
- [ ] `npx tsc --noEmit` 통과 (신규 에러 0)
- [ ] inline style 141 곳 grep count 변경 0
- [ ] 변경 파일: `src/pages/LegalPage.tsx` 와 sketch HTML 단 둘
</success_criteria>

<commits>
- Task 1 완료 시: `feat(260527-tb3-01): LegalPage emoji + 색 토큰 sweep 시안 (4 패턴 + 옵션 분기)`
- Task 3 완료 시: `feat(260527-tb3-03): LegalPage §7.1 emoji 8곳 → Lucide + §2.3 색 토큰 4곳 정리 (옵션 [A|B] + pill [1|2])`
- Task 2 (checkpoint) 는 자체 commit 없음 — 사용자 결정 사항을 Task 3 commit message 에 기록
</commits>

<output>
After completion, create `.planning/quick/260527-tb3-legalpage-sweep-emoji-8-lucide-4/260527-tb3-SUMMARY.md`:
- emoji 8곳 정확 매핑 결과 (어떤 Lucide / 어떤 size)
- 사용자 선택 옵션 (A/B, 1/2, Save 사용 여부)
- 변경 라인 / inline style 무수정 / 다른 파일 무수정 검증 결과
- submission-ppt 기능 grep 검증 결과 (onClick / useMutation / legalApi 변경 0)
- 다음 작업 후보 (inline style 141곳 별도 phase B / 또는 LegalFindingsPage / LegalFindingDetailPage 동일 sweep)
</output>
