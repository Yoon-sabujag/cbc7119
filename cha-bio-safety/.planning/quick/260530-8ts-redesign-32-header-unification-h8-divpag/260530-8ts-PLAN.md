---
phase: 260530-8ts-redesign-32-header-unification-h8-divpag
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/DivPage.tsx
  - src/pages/LegalPage.tsx
  - src/styles/components.css
autonomous: true
requirements: [REDESIGN-32-H8]
must_haves:
  truths:
    - "DivPage 모바일 헤더 타이틀이 text-title font-semibold (18px/600) 로 표시된다"
    - "LegalPage 모바일 헤더가 h-12 + px-3 컨테이너, 백버튼 w-7 h-7 rounded-[7px] bg-surface-sunken, 타이틀 text-title font-semibold 로 통일된다"
    - "QRPrintPage CSS 헤더 (height 48px / 백버튼 32×32 radius 7 border 제거 / 타이틀 18px 600 lh 1.4) 가 GlobalHeader 표준과 일치한다"
    - "vite build 가 TypeScript 0 error 로 통과한다"
  artifacts:
    - path: "src/pages/DivPage.tsx"
      provides: "DivPage 모바일 헤더 타이틀 text-title font-semibold"
      contains: "text-title font-semibold text-text-primary ml-2"
    - path: "src/pages/LegalPage.tsx"
      provides: "LegalPage 모바일 헤더 (h-12 px-3 / w-7 h-7 rounded-[7px] / text-title font-semibold)"
      contains: "h-12 px-3 flex items-center justify-center"
    - path: "src/styles/components.css"
      provides: "qr-print-page-header / -back-btn / -title GlobalHeader 표준 통일"
      contains: "height: 48px"
  key_links:
    - from: "DivPage 모바일 헤더"
      to: "헤더 통일 표준 (text-title)"
      via: "Tailwind class swap"
      pattern: "text-title font-semibold text-text-primary ml-2"
    - from: "LegalPage 모바일 헤더"
      to: "GlobalHeader 표준 (h-12 + 32×32 백버튼 + text-title)"
      via: "Tailwind class swap"
      pattern: "w-7 h-7 rounded-\\[7px\\] bg-surface-sunken"
    - from: "QRPrintPage CSS 헤더"
      to: "GlobalHeader 표준 (48px / 32×32 r7 border 0 / 18px 600 lh 1.4)"
      via: "components.css 정의 변경"
      pattern: "\\.qr-print-page-title.*font-size: 18px.*font-weight: 600"
---

<objective>
H1~H7 헤더 통일 wave 종결 후 누락된 3 영역 (DivPage 모바일 헤더 / LegalPage 모바일 헤더 / QRPrintPage CSS) 을 GlobalHeader 표준에 맞춰 7 곳 일괄 정렬한다.

Purpose: 헤더 통일 wave 의 시각 일관성을 마무리. H1~H7 으로 14 페이지 + 22 곳 타이틀이 정렬되었으나 DivPage 모바일/ LegalPage 모바일/ QRPrintPage CSS 3 영역이 누락되어 GlobalHeader 와 사이즈/굵기/높이 mismatch 가 남아 있다.
Output: 3 파일에 정확히 7 곳 class/CSS 변경. atomic commit 1 건.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@CLAUDE.md
@CLAUDE.local.md

# 표준 reference (이미 적용된 DivPage 모바일 헤더 컨테이너 / GlobalHeader 햄버거 사이즈 / text-title 토큰)
@src/pages/DivPage.tsx
@src/pages/LegalPage.tsx
@src/styles/components.css

<interfaces>
<!-- 변경 대상 anchor 3 영역. text-title 토큰 = font-size:18px / line-height:1.4. font-semibold = 600. w-7 h-7 = 32px (tailwind.config spacing override). -->

DivPage.tsx 모바일 헤더 (참고용 — 이미 표준):
```tsx
<header className="flex-shrink-0 flex items-center h-12 px-3 bg-surface-raised border-b border-border-default">
  <button onClick={() => navigate(-1)} className="w-[32px] h-[32px] rounded-[7px] bg-surface-sunken flex items-center justify-center text-text-secondary cursor-pointer border-0">
    <svg width={15} height={15} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}><path strokeLinecap="round" strokeLinejoin="round" d="M15 19l-7-7 7-7"/></svg>
  </button>
  <span className="flex-1 text-[13px] font-bold text-text-primary ml-2">DIV 압력 관리</span>  {/* ← 타이틀만 text-title font-semibold 로 변경 */}
</header>
```

GlobalHeader 표준 (H1~H7 wave 적용 결과):
- 컨테이너: h-12 + px-3
- 백버튼/햄버거: 32×32 + rounded-[7px] + bg-surface-sunken + border 0
- 타이틀: text-title (18px) + font-semibold (600) + lh 1.4
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 누락 페이지 헤더 7 곳 통일 (DivPage + LegalPage + components.css)</name>
  <files>src/pages/DivPage.tsx, src/pages/LegalPage.tsx, src/styles/components.css</files>
  <action>
정확히 7 곳 class/CSS 만 변경. 1 byte라도 다른 변경 금지.

## 영역 1 — src/pages/DivPage.tsx (1 곳)

### 변경 1/7 — L1109 모바일 헤더 타이틀
BEFORE (전체 라인):
```tsx
        <span className="flex-1 text-[13px] font-bold text-text-primary ml-2">DIV 압력 관리</span>
```

AFTER:
```tsx
        <span className="flex-1 text-title font-semibold text-text-primary ml-2">DIV 압력 관리</span>
```

- 변경: `text-[13px] font-bold` → `text-title font-semibold` (13→18 / 700→600)
- 보존: `flex-1`, `text-text-primary`, `ml-2`, 텍스트 "DIV 압력 관리"
- 같은 모바일 헤더 안 다른 항목 무수정:
  - L1105 `<header className="flex-shrink-0 flex items-center h-12 px-3 ...">` 무수정 (이미 표준 h-12 + px-3)
  - L1106 백버튼 `w-[32px] h-[32px] rounded-[7px] bg-surface-sunken ... border-0` 무수정 (이미 표준)
  - L1107 svg 무수정
  - L1103-1104 주석 무수정

## 영역 2 — src/pages/LegalPage.tsx (3 곳)

### 변경 2/7 — 모바일 헤더 컨테이너 (L1148)
BEFORE:
```tsx
      <div className="bg-surface-raised border-b border-border-default h-[48px] flex items-center justify-center relative shrink-0">
```

AFTER:
```tsx
      <div className="bg-surface-raised border-b border-border-default h-12 px-3 flex items-center justify-center relative shrink-0">
```

- 변경: `h-[48px]` → `h-12 px-3` (h-12 = 48px 동일, 토큰 표기 + 좌우 padding 12px 추가)
- 보존: `bg-surface-raised`, `border-b`, `border-border-default`, `flex`, `items-center`, `justify-center`, `relative`, `shrink-0`

### 변경 3/7 — 백버튼 className (L1152)
BEFORE:
```tsx
          className="text-text-primary absolute left-2 w-[44px] h-[44px] border-0 bg-transparent cursor-pointer flex items-center justify-center"
```

AFTER:
```tsx
          className="text-text-primary absolute left-2 w-7 h-7 rounded-[7px] bg-surface-sunken border-0 cursor-pointer flex items-center justify-center"
```

- 변경: `w-[44px] h-[44px]` → `w-7 h-7` (44→32 — tailwind.config spacing override: w-7=32px), `bg-transparent` → `bg-surface-sunken`, `rounded-[7px]` 추가
- 보존: `text-text-primary`, `absolute`, `left-2`, `border-0`, `cursor-pointer`, `flex`, `items-center`, `justify-center`
- ChevronLeft size={20} (다음 라인) 무수정
- aria-label, onClick prop 무수정

### 변경 4/7 — 모바일 헤더 타이틀 (L1154)
BEFORE:
```tsx
        <span className="text-body font-bold text-text-primary">소방 점검 관리</span>
```

AFTER:
```tsx
        <span className="text-title font-semibold text-text-primary">소방 점검 관리</span>
```

- 변경: `text-body font-bold` → `text-title font-semibold` (16→18 / 700→600)
- 보존: `text-text-primary`, 텍스트 "소방 점검 관리"

## 영역 3 — src/styles/components.css (3 곳)

### 변경 5/7 — L1091 `.qr-print-page-header`
BEFORE (전체 라인):
```css
  .qr-print-page-header { padding: 8px 12px; background: var(--surface-raised); border-bottom: 1px solid var(--border-default); display: flex; align-items: center; gap: 10px; flex-shrink: 0; }
```

AFTER:
```css
  .qr-print-page-header { height: 48px; padding: 0 12px; background: var(--surface-raised); border-bottom: 1px solid var(--border-default); display: flex; align-items: center; gap: 10px; flex-shrink: 0; }
```

- 변경: `padding: 8px 12px;` → `height: 48px; padding: 0 12px;` (높이 명시 + vertical padding 제거)
- 보존: `background`, `border-bottom`, `display`, `align-items`, `gap`, `flex-shrink`
- 들여쓰기 2 space 보존

### 변경 6/7 — L1092 `.qr-print-page-back-btn`
BEFORE (전체 라인):
```css
  .qr-print-page-back-btn { width: 34px; height: 34px; border-radius: 8px; background: var(--surface-sunken); border: 1px solid var(--border-default); color: var(--text-secondary); cursor: pointer; display: flex; align-items: center; justify-content: center; flex-shrink: 0; }
```

AFTER:
```css
  .qr-print-page-back-btn { width: 32px; height: 32px; border-radius: 7px; background: var(--surface-sunken); color: var(--text-secondary); cursor: pointer; display: flex; align-items: center; justify-content: center; flex-shrink: 0; }
```

- 변경:
  - `width: 34px;` → `width: 32px;`
  - `height: 34px;` → `height: 32px;`
  - `border-radius: 8px;` → `border-radius: 7px;`
  - `border: 1px solid var(--border-default);` 제거 (GlobalHeader 햄버거와 일관 — border 없음)
- 보존: `background`, `color`, `cursor`, `display`, `align-items`, `justify-content`, `flex-shrink`

### 변경 7/7 — L1093 `.qr-print-page-title`
BEFORE (전체 라인):
```css
  .qr-print-page-title { font-size: 15px; font-weight: 700; color: var(--text-primary); line-height: 1; }
```

AFTER:
```css
  .qr-print-page-title { font-size: 18px; font-weight: 600; color: var(--text-primary); line-height: 1.4; }
```

- 변경: `font-size: 15px` → `18px`, `font-weight: 700` → `600`, `line-height: 1` → `1.4` (text-title 토큰 spec 동일)
- 보존: `color`

## 보존 한 byte 변경 0 — 명시 금지 영역

- **DivPage.tsx**: L1101-1108 (헤더 컨테이너 + 백버튼 + svg + 주석), L1112+ (탭 영역 / 본문 / 다른 모든 코드)
- **LegalPage.tsx**: L1145-1147 (style 블록 포함), L1149-1151 (button 의 aria-label / onClick / 들여쓰기), L1153 (`<ChevronLeft size={20} />`), L1155 (`</div>`), L1157+ (필터 영역 / 본문)
- **QRPrintPage.tsx**: 무수정 (CSS class 만 변경)
- **components.css**: L1090 `.qr-print-page` 정의, L1094+ (`-info-wrap` / `-info-box` / `-list` / `-list-inner` 등 sub class), L1087-1089 주석/머리 정의 무수정
- 위 외 모든 코드

## OQ 주석 처리

components.css L1088 주석 `OQ #3 LOCKED: 뒤로가기 width:34px height:34px 명시 (memory feedback_tailwind_w8_h8_is_48px)` 는 이번 변경으로 width/height 가 32px 로 바뀌므로 의미가 어긋난다.

이 주석 자체는 **수정하지 마라** (상위 머리 주석 블록 일부, 별도 변경 7 곳 외). 사용자 후속 정리 대상으로 둔다.

## 작업 순서

1. DivPage.tsx 변경 1 곳 (Edit)
2. LegalPage.tsx 변경 3 곳 (3 회 Edit 또는 1 회 다중 Edit)
3. components.css 변경 3 곳 (3 회 Edit 또는 1 회 다중 Edit)
4. vite build 실행
5. grep 으로 변경 7 곳 확인 + 보존 영역 미변경 확인
6. atomic commit
  </action>
  <verify>
    <automated>
cd /Users/jykevin/Documents/cbc7119-design/cha-bio-safety && \
  npm run build 2>&1 | tail -20 && \
  echo "=== DivPage L1109 변경 확인 ===" && \
  grep -n 'text-title font-semibold text-text-primary ml-2">DIV 압력 관리' src/pages/DivPage.tsx && \
  echo "=== DivPage 이전 패턴 잔존 0 확인 (must be 0) ===" && \
  grep -c 'text-\[13px\] font-bold text-text-primary ml-2">DIV 압력 관리' src/pages/DivPage.tsx && \
  echo "=== LegalPage 컨테이너 변경 확인 ===" && \
  grep -n 'border-border-default h-12 px-3 flex items-center justify-center relative shrink-0' src/pages/LegalPage.tsx && \
  echo "=== LegalPage 이전 컨테이너 잔존 0 (must be 0) ===" && \
  grep -c 'h-\[48px\] flex items-center justify-center relative shrink-0' src/pages/LegalPage.tsx && \
  echo "=== LegalPage 백버튼 변경 확인 ===" && \
  grep -n 'absolute left-2 w-7 h-7 rounded-\[7px\] bg-surface-sunken border-0 cursor-pointer' src/pages/LegalPage.tsx && \
  echo "=== LegalPage 이전 백버튼 잔존 0 (must be 0) ===" && \
  grep -c 'w-\[44px\] h-\[44px\] border-0 bg-transparent' src/pages/LegalPage.tsx && \
  echo "=== LegalPage 타이틀 변경 확인 ===" && \
  grep -n 'text-title font-semibold text-text-primary">소방 점검 관리' src/pages/LegalPage.tsx && \
  echo "=== LegalPage 이전 타이틀 잔존 0 (must be 0) ===" && \
  grep -c 'text-body font-bold text-text-primary">소방 점검 관리' src/pages/LegalPage.tsx && \
  echo "=== components.css qr-print-page-header 변경 확인 ===" && \
  grep -n '\.qr-print-page-header { height: 48px; padding: 0 12px;' src/styles/components.css && \
  echo "=== qr-print-page-header 이전 패턴 잔존 0 (must be 0) ===" && \
  grep -c '\.qr-print-page-header { padding: 8px 12px;' src/styles/components.css && \
  echo "=== components.css qr-print-page-back-btn 변경 확인 ===" && \
  grep -n '\.qr-print-page-back-btn { width: 32px; height: 32px; border-radius: 7px; background: var(--surface-sunken); color:' src/styles/components.css && \
  echo "=== qr-print-page-back-btn border 제거 확인 (must be 0) ===" && \
  grep -c '\.qr-print-page-back-btn.*border: 1px solid' src/styles/components.css && \
  echo "=== components.css qr-print-page-title 변경 확인 ===" && \
  grep -n '\.qr-print-page-title { font-size: 18px; font-weight: 600; color: var(--text-primary); line-height: 1.4;' src/styles/components.css && \
  echo "=== qr-print-page-title 이전 잔존 0 (must be 0) ===" && \
  grep -c '\.qr-print-page-title { font-size: 15px; font-weight: 700' src/styles/components.css && \
  echo "=== QRPrintPage.tsx 무수정 확인 (git diff empty) ===" && \
  git diff --stat src/pages/QRPrintPage.tsx
    </automated>
  </verify>
  <done>
- vite build PASS (TypeScript 0 error)
- 7 grep AFTER 패턴이 모두 hit (정확히 1 곳씩, components.css back-btn 은 라인 prefix 매칭)
- 7 grep BEFORE 패턴이 모두 0 곳 (잔존 0)
- QRPrintPage.tsx git diff 비어 있음 (CSS 만 변경, TSX 무수정)
- atomic commit 1 건: `feat(redesign/32-header-unification-h8): 누락 페이지 헤더 통일 (DivPage 모바일 + LegalPage 모바일 + QRPrintPage CSS, 7 곳)`
  </done>
</task>

</tasks>

<verification>
- 7 곳 정확히 변경, 한 byte 추가/누락 0
- vite build PASS
- grep gate 14 건 (7 AFTER hit + 7 BEFORE 0) 모두 통과
- QRPrintPage.tsx 무수정 (git diff 빈 stat)
- wrangler / npm run deploy 호출 0 (디자인 워크트리 룰)
</verification>

<success_criteria>
- DivPage 모바일 헤더 타이틀이 GlobalHeader 표준 (text-title font-semibold) 로 표시됨
- LegalPage 모바일 헤더가 h-12 + 32×32 백버튼 + text-title 표준으로 통일됨
- QRPrintPage CSS 헤더가 GlobalHeader 표준 (height 48px / 32×32 r7 border 0 / 18px 600 lh 1.4) 와 일치
- main 머지 후 GitHub Actions 가 cbc7119-preview 로 자동 배포 (사용자 컨펌 후)
</success_criteria>

<output>
After completion, create `.planning/quick/260530-8ts-redesign-32-header-unification-h8-divpag/260530-8ts-SUMMARY.md`
</output>
