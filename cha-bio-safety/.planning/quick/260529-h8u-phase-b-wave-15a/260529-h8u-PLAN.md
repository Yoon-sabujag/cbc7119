---
phase: 260529-h8u-phase-b-wave-15a-elevator-mobile
type: execute
wave: 15a
depends_on: [260529-gj2]
files_modified:
  - src/pages/ElevatorPage.tsx
autonomous: true
roadmap-wave: Tier 2 / Wave 15a (ElevatorPage 모바일 zone — Tier 2 네 번째 wave, 메가 페이지 분할)
split-decision: B (15a-1 + 15a-2 두 atomic) — 메가 페이지 안전 분할
---

<objective>
**Phase B Wave 15a — ElevatorPage 모바일 zone.** Tier 2 네 번째 wave (Wave 12a StaffManage / 13a Checkpoints / 14a Remediation 직후). 메가 페이지 (3489줄, 206 total inline).

### Zone 분류 (style_zones.py + python brace-tracking 검증 완료)

| zone | count | 위치 |
|---|---:|---|
| **shared (변환 대상)** | **101** | 모바일 zone — `if (isDesktop)` 블록 밖 |
| **desktop-only (절대 보존, Wave 15b 후행)** | **105** | `if (isDesktop) { return ... }` 블록 안 (L527-L1024 단일 블록) |
| **mobile-only** | **0** | 없음 (단일 `if (isDesktop)` early-return 구조) |
| **전체** | **206** | |

### 데스크톱 zone 정확 boundary (단일 블록)

- **main ElevatorPage `if (isDesktop)` early-return: L527 ~ L1024** (498 lines, 105 inline)
  - L527: `if (isDesktop) {` opens
  - L1024: `  }` closes (matching brace via python depth-tracking)
  - 이 블록 안에서 desktop view 가 `return (...)` 으로 즉시 반환됨
  - **이 라인 범위 안의 inline 모든 105건 절대 변경 0** (Wave 15b 책임)

- **EvDetailModal L1806 `isDesktop`**: prop 전달 + className conditional 만 사용 (zone-blocking inline guard 아님). EvDetailModal 안의 모든 inline 은 mobile zone (양 view 공용).
- **ModalWrap L2655 `isDesktop`**: className conditional `isDesktop ? 'sticky top-0 z-[1]' : ''` 만 사용 (inline 아님). ModalWrap 안의 모든 inline 은 mobile zone.
- 즉, `if (isDesktop) { return }` 단 1곳만 zone-blocking guard 임이 확인됨

### 분할 vs 단일 결정 — **B (분할)** 채택

근거:
1. **컨텍스트 부담**: 101 inline 한 번에 변환 시 PLAN.md 본문 ~1500줄, 매핑 표 거대화. 실행 단계에서 executor 가 3489줄 파일 + 101 매핑 + 105 desktop 보존 동시 holding → context budget 위험
2. **검증 복잡도**: 단일 atomic diff 가 100+ hunk → desktop zone overlap 검증 awk 가 노이즈 ↑. 두 atomic 으로 나누면 각자 ~50 hunk, 검증 깔끔
3. **비례 추정**: Wave 14a (11 inline ~10분 atomic) × 9 = 약 90분 매핑 + 변환. Wave 12a (52 inline ~20분) 대비 ~2배 — 단일로는 무리
4. **자연스러운 의미 boundary**: 컴포넌트 17개를 두 그룹으로 깔끔히 나눌 수 있음 — fault/repair/findings 트랙 vs cert/info/annual 트랙
5. **rollback 안전성**: 한쪽에 시각 회귀 발견 시 한 atomic 만 revert 가능
6. **Wave 13a/14a precedent**: 모두 단일 atomic 이었으나 inline 수 11~52건 (101 의 절반 이하). 14a → 15a 비례 룰 적용

분할 구조 (컴포넌트 알라인 — line range 가 아닌 의미 단위):

#### Plan 01 (15a-1) — **Fault / Repair / Findings 트랙** (51 inline)
- ElevatorPage main mobile view stragglers: L1060/1357/1744/1754 (4)
- EvDetailModal: L1874/1934 (2)
- EvSelector: L2162 (1)
- FaultNewModal: L2249/2286 (2)
- FaultNewFullscreen: L2328/2370/2413 (3)
- FaultResolveModal: L2467 (1)
- CertViewerModal: L2483/2484/2486/2487/2488/2489/2490/2494/2498/2499 (10)
- FindingCountBadge: L2519 (1)
- FindingsPanel: L2596/2597/2598/2599/2602/2605/2606/2607/2608/2615/2620/2621/2624/2625/2628/2632/2633/2640/2643/2645 (20)
- RepairListSection: L3040 (1)
- RepairImageViewer: L3119/3120/3121/3137/3139 (5)
- RepairNewModal: L3355 (1)
- **합계: 51 inline**

#### Plan 02 (15a-2) — **Cert / Info / Annual 트랙** (50 inline)
- CertSummary: L2727 (1)
- CertBlock: L2767/2769/2770/2771/2774/2782/2783/2786/2791/2792/2793/2795/2796/2808/2809/2812 (16)
- ElevatorInfoCard: L2883/2884/2886/2889/2894/2895/2904/2905/2908/2910/2912/2917/2918/2919/2920 (15)
- MinwonFindingsPanel: L3430/3431/3438/3439/3440/3441/3444/3448/3455/3461/3462/3463/3466/3467/3471/3478/3481/3483 (18)
- **합계: 50 inline**

### 옵션 승계 (16번째 atomic — 재컨펌 불필요)

- 옵션 X (정확값 arbitrary `[Npx]` / `[#hex]` / `[rgba(...)]`)
- 옵션 P (`leading-none` 명시 보존)
- 옵션 M (template literal conditional 1-2 prop)
- 옵션 N (색 변수만) — `var(--xxx)` 단일 → tailwind token
- module const N — 본 파일 const 정의 보존 (STATUS_STYLE/OVERALL_STYLE/RESULT_STYLE/INSPECT_TYPE_LABEL/TYPE_ICON/TYPE_ICON_COMPONENT/CHECK_ITEMS_EV/CHECK_ITEMS_ES/HISTORY_TABS/EV_FLOORS/EV_GROUPS_FAULT/EV_GROUPS_ANNUAL/ES_NODES_FAULT/ES_NODES_ANNUAL/CHECK_ITEM_LABELS/PERIOD_OPTIONS 등 16개)

### 토큰 alias 확정 매핑

| Before | After | Source |
|---|---|---|
| `var(--bg)` | `bg-surface-page` | nkv/epe/f2w precedent |
| `var(--bg2)` | `bg-surface-raised` | precedent |
| `var(--bg3)` | `bg-surface-sunken` | precedent |
| `var(--bd)` | `border-border-default` | precedent |
| `var(--t1)` | `text-text-primary` | precedent |
| `var(--t2)` | `text-text-secondary` | precedent |
| `var(--t3)` | `text-text-tertiary` | precedent |
| `var(--acl)` | `text-accent` / `bg-accent` | tokens.css L187 alias |
| `var(--accent-active)` | `bg-accent-active` (L1754 만) | tailwind.config L62 |
| `var(--safe)` | `text-safe-bar` / `bg-safe-bar` | tokens.css L189 alias |
| `var(--warn)` | `text-warning-bar` / `bg-warning-bar` | tokens.css L190 alias |
| `var(--danger)` | `text-danger-bar` / `bg-danger-bar` | tokens.css L191 alias |
| `var(--info)` | `text-info-bar` / `bg-info-bar` | tokens.css L188 alias |

### 위험 anchor

- **데스크톱 zone L527-L1024 의 105건 — 변경 절대 0** (Wave 15b 책임). diff hunk awk overlap check 로 자동 검증
- **module const 16개 정의 보존** — STATUS_STYLE / OVERALL_STYLE / RESULT_STYLE / INSPECT_TYPE_LABEL / TYPE_ICON / TYPE_ICON_COMPONENT / CHECK_ITEMS_EV / CHECK_ITEMS_ES / HISTORY_TABS / EV_FLOORS / EV_GROUPS_FAULT / EV_GROUPS_ANNUAL / ES_NODES_FAULT / ES_NODES_ANNUAL / CHECK_ITEM_LABELS / PERIOD_OPTIONS. const def grep 카운트 검증 (각 plan 별)
- **TYPE_ICON 의 emoji 4건 (🛗 📦 🔲 ↕️) 절대 보존** — 본 변환에서 손대지 말 것. 변동 0
- **이모지 본문 사용 (📄/📚/🏢/👤/✕ 등) 보존** — 본 wave 는 emoji sweep 아님, 이미 있는 emoji 변동 0 룰
- **비즈 anchor (onClick/useState/useRef/useEffect/useMutation/useQuery/useNavigate/useParams/useSearchParams/fetch) IDENTICAL**
- **`linear-gradient` 보존** — 옵션 N 잔존 후보. L614/L622 (desktop zone 안, 자동 보존) + L1744/L1754/L2286/L2413 (mobile zone, 옵션 N 잔존 가능). Tailwind arbitrary `bg-[linear-gradient(...)]` 매핑 가능하나 매우 길어 시각 0 byte 보장 위해 inline 유지 권장
- **`...spread` 패턴 (cellSt/kSt/vSt 등)** — 옵션 N 잔존. CertBlock L2791/L2792/L2793/L2795/L2796 (cellSt spread) + ElevatorInfoCard L2894/L2895/L2917/L2918/L2919/L2920 (kSt/vSt spread). 모두 변경 0 — 옵션 N 잔존 (전체 11건 spread)
- **WebkitOverflowScrolling / overscrollBehavior** — L2889. Tailwind 미지원. 옵션 N 잔존
- **`gridTemplateColumns: ...` 동적/비표준** — 변환 가능한 일부 (정적값) 만 arbitrary, 동적 (L2162) 은 옵션 N 잔존

### 예상 잔존

| Plan | Before | After (예상) | 변환 | 옵션 N 잔존 |
|---|---:|---:|---:|---:|
| 15a-1 (51 mobile + 105 desktop) | 156 | ~111-115 | ~40-45 | ~5-8 (gradient × 4 + spread/transform × 2-4) |
| 15a-2 (50 mobile, 105 desktop 그대로) | 105 | ~62-65 (105 desktop + ~ 0 + ~10 옵션 N spread + scrollbar) | ~35-40 | ~10-13 (cellSt/kSt/vSt spread 11 + WebkitOverflowScrolling 1) |
| **합계 (양 plan)** | **206** | **~100-110** | **~80-85** | **~15-20** |

전체 목표: **206 → ~100-110** (모바일 zone 101 → ~0-5 변환 후, desktop 105 보존, 옵션 N 잔존 ~15-20)

### 메모리 anchor 적용

- `feedback_tailwind_w8_h8_is_48px.md` — w-8/h-8=48 override. 본 page 의 height:48→`h-[48px]` arbitrary 또는 standard `h-12`. height:44→`h-11`, height:36→`h-9`, height:32→`h-8` (X! 8=48). height:28→`w-7 h-7`(arbitrary 권장 `w-[28px] h-[28px]` 또는 `h-7=32 X`). 매핑 표 정확
- `feedback_tailwind_token_class_pattern.md` — `text-info-bar` / `bg-safe-bar` / `bg-danger-bar` / `text-warning-bar` / `text-accent` 등 status- prefix 없음. lucide `size={N}` prop 보존
- `feedback_text_caption_leading_none.md` — text-caption + leading-none 작은 컨테이너 안 라벨 케이스에 적용
- `feedback_inspection_unresolved_color.md` — 미조치 = `text-fire-bar`/`bg-fire-bar` 룰. 단 ElevatorPage 는 fault 상태에 `var(--danger)` 사용 → `text-danger-bar` (메인 칩 = danger). 본 페이지는 fire 가 아닌 danger 톤
- `feedback_design_changes_ask_first.md` — 시각 0 byte 룰 100%
- Wave 14a precedent — early-return zone 분할, helper shared zone 부수효과, diff hunk overlap awk 검증

</objective>

<context>

### Mobile inline 카테고리별 분배

#### Plan 01 (15a-1) 51 inline — Fault / Repair / Findings

**ElevatorPage main mobile view stragglers (4건)**
| Line | 위치 | 패턴 |
|----:|---|---|
| L1060 | main paddingBottom calc | `paddingBottom: 'calc(80px + var(--sab, 0px))'` → `pb-[calc(80px+var(--sab,0px))]` |
| L1357 | inspect tab gridTemplateColumns | `gridTemplateColumns:'50px 1fr auto'` → arbitrary `[grid-template-columns:50px_1fr_auto]` 또는 옵션 N 잔존 |
| L1744 | fault FAB linear-gradient | `background:'linear-gradient(135deg,#991b1b,#ef4444)',boxShadow:...` → **옵션 N 잔존** (gradient + shadow) |
| L1754 | repair FAB linear-gradient | `background:'linear-gradient(135deg, var(--accent-active), var(--accent))',boxShadow:...` → **옵션 N 잔존** |

**EvDetailModal (2건)**
| Line | 패턴 |
|----:|---|
| L1874 | modal overlay 3-prop `position:fixed/inset:0/zIndex:90` → `fixed inset-0 z-[90]` |
| L1934 | (EvDetailModal 내부 어느 위치) → grep 후 결정 |

**EvSelector L2162** — dynamic gridTemplateColumns `repeat(${groupEvs.length}, 1fr)` → **옵션 N 잔존** (동적)

**FaultNewModal L2249/L2286**
| Line | 패턴 |
|----:|---|
| L2249 | Field style `{ flex:1 }` → `flex-1` className 또는 옵션 N (Field 컴포넌트 prop) |
| L2286 | gradient `linear-gradient(135deg,#991b1b,#ef4444)` + dynamic opacity → **옵션 N 잔존** |

**FaultNewFullscreen L2328/L2370/L2413**
| Line | 패턴 |
|----:|---|
| L2328 | fullscreen wrap `{ top:0,left:0,right:0,bottom:NAV_H,zIndex:100,paddingTop:'var(--sat,44px)',boxSizing:'border-box' }` → 대부분 className 매핑 가능, bottom:NAV_H 만 옵션 N 잔존 (NAV_H = 'calc(54px + env(safe-area-inset-bottom, 20px))' const 참조). 또는 `[bottom:calc(54px+env(safe-area-inset-bottom,20px))]` arbitrary |
| L2370 | Field `{ flex:1 }` → `flex-1` |
| L2413 | gradient + opacity → 옵션 N 잔존 |

**FaultResolveModal L2467**
- opacity dynamic `{ opacity:(!repairDetail.trim()||loading)?0.5:1 }` → 옵션 M template literal `${!repairDetail.trim()||loading ? 'opacity-50' : 'opacity-100'}` (className 분리)

**CertViewerModal L2483-L2499 (10건)**
| Line | 패턴 |
|----:|---|
| L2483 | overlay `position:fixed/inset:0/background:rgba(0,0,0,.85)/zIndex:200` → `fixed inset-0 bg-[rgba(0,0,0,0.85)] z-[200]` |
| L2484 | container `position:fixed/inset:0/zIndex:201/display:flex/flexDirection:column/paddingTop:var(--sat,44px)/paddingBottom:var(--sab,0px)` → `fixed inset-0 z-[201] flex flex-col pt-[var(--sat,44px)] pb-[var(--sab,0px)]` |
| L2486 | header 6-prop `flexShrink:0/display:flex/items-center/justify-between/padding:'10px 16px'/bg:'rgba(22,27,34,0.97)'/border-b` → `shrink-0 flex items-center justify-between px-4 py-2.5 bg-[rgba(22,27,34,0.97)] border-b border-border-default` |
| L2487 | title `fontSize:14/fontWeight:700/color:var(--t1)` → `text-body-sm font-bold text-text-primary` |
| L2488 | btn-row `display:flex/gap:8` → `flex gap-2` |
| L2489 | "새 탭 열기" a tag 7-prop → arbitrary mix |
| L2490 | close btn `background:'none'/border:'none'/color:var(--t3)/cursor:pointer/fontSize:20` → `bg-none border-0 text-text-tertiary cursor-pointer text-[20px]` |
| L2494 | viewer area 5-prop → className |
| L2498 | inner wrap 6-prop center → flex center |
| L2499 | img `maxWidth:100%/maxHeight:100%/objectFit:contain/display:block` → `max-w-full max-h-full object-contain block` |

**FindingCountBadge L2519**
- 5-prop badge → `text-[9px] font-bold px-1.5 py-px rounded-md bg-[rgba(239,68,68,0.12)] text-danger-bar`

**FindingsPanel L2596-L2645 (20건)** — 가장 큰 단일 컴포넌트. 매핑 표 (요약):
| Line | 패턴 |
|----:|---|
| L2596 | wrap padding `px-2 py-1.5` |
| L2597 | row flex items-center justify-between mb-1 |
| L2598 | label `text-[10px] font-bold ${inspectionResult === 'fail' ? 'text-danger-bar' : 'text-warning-bar'}` (옵션 M) |
| L2599 | count `text-[9px] text-text-tertiary` |
| L2602 | loading msg `text-[9px] text-text-tertiary` |
| L2605 | finding row `px-1.5 py-1 bg-surface-raised rounded-[5px] mb-[3px]` |
| L2606 | inner flex `flex items-center gap-1 text-[10px]` |
| L2607 | desc span ellipsis 5-prop |
| L2608 | status chip 7-prop dynamic bg+color (옵션 M) `${f.status === 'open' ? 'bg-[rgba(239,68,68,0.12)] text-danger-bar' : 'bg-[rgba(34,197,94,0.12)] text-safe-bar'}` |
| L2615 | "수리이력 연결" btn 7-prop |
| L2620 | linking dropdown 7-prop `mt-[3px] bg-surface-sunken rounded-[4px] p-1 max-h-[100px] overflow-y-auto border` |
| L2621 | "수리 이력 없음" msg |
| L2624-L2625 | repair option list 4-prop |
| L2628 | 취소 btn 7-prop `bg-none border` |
| L2632 | resolved msg flex items-center gap-1 text-[9px] text-safe-bar mt-[2px] |
| L2633 | CheckCircle2 wrapper `style={{ flexShrink:0 }}` → `className="shrink-0"` (Lucide prop) |
| L2640 | input row flex gap-1 mt-[3px] |
| L2643 | input 8-prop |
| L2645 | submit btn 9-prop dynamic bg+opacity (옵션 M+X) `${inspectionResult === 'fail' ? 'bg-danger-bar' : 'bg-warning-bar'}` + `${!newDesc.trim() || addingFinding ? 'opacity-50' : 'opacity-100'}` |

**RepairListSection L3040** — 1건 status chip dynamic `background: st.color + '18', color: st.color` → **옵션 N 잔존** (run-time color concat)

**RepairImageViewer L3119-L3139 (5건)**
| Line | 패턴 |
|----:|---|
| L3119 | fullscreen overlay `fixed inset-0 z-[300] bg-[rgba(0,0,0,0.95)] flex flex-col` |
| L3120 | header `shrink-0 flex justify-end px-4 py-3 pt-[calc(12px+var(--sat,44px))]` |
| L3121 | close btn `bg-none border-0 text-white text-[24px] cursor-pointer` |
| L3137 | content area `flex-1 overflow-hidden flex items-center justify-center` + `touchAction:'none'` → 옵션 N 잔존 (touchAction CSS prop not all tailwind) 또는 `touch-none` 매핑 |
| L3139 | img 5-prop `maxWidth:100%/maxHeight:100%/objectFit:contain/transform:translate/userSelect:none` + dynamic transform/transition → **옵션 N 잔존** (dynamic transform 으로 className 매핑 불가) |

**RepairNewModal L3355**
- opacity dynamic `{ opacity: canSubmit ? 1 : 0.5 }` → 옵션 M `${canSubmit ? 'opacity-100' : 'opacity-50'}` className

#### Plan 02 (15a-2) 50 inline — Cert / Info / Annual

**CertSummary L2727**
- `display:flex/flexDirection:column/marginTop:12/gap:14` → `flex flex-col mt-3 gap-3.5`

**CertBlock L2767-L2812 (16건)**
| Line | 패턴 |
|----:|---|
| L2767 | wrap flex flex-col gap-2.5 |
| L2769 | title row flex items-center gap-2 pt-1 |
| L2770 | accent bar `w-[3px] h-[14px] bg-accent rounded-[2px]` (또는 dynamic `bg-[${accent ?? 'var(--acl)'}]` → 옵션 N 잔존 if accent is dynamic prop) |
| L2771 | title span `text-caption font-bold text-accent` (dynamic color → 옵션 N) |
| L2774 | "PDF 보기" btn 7-prop `ml-auto text-[10px] font-bold px-2 py-0.5 rounded-md bg-[rgba(34,197,94,0.1)] border border-[rgba(34,197,94,0.3)] text-safe-bar cursor-pointer` |
| L2782 | items wrap border rounded-md overflow-hidden |
| L2783 | header `px-2.5 py-1.5 bg-surface-raised border-b border-border-default text-[10.5px] font-bold text-text-secondary` |
| L2786 | grid `grid bg-surface-sunken [grid-template-columns:40px_1fr_auto]` |
| L2791-L2793 | cellSt spread — **옵션 N 잔존** (3건, ...cellSt + dynamic color/fontWeight) |
| L2795-L2796 | findings spacer + content (gridColumn + borderBottom dynamic) — **옵션 N 잔존** (2건) |
| L2808 | info wrap (hasInfo branch) border rounded-md overflow-hidden |
| L2809 | info header `px-2.5 py-1.5 bg-surface-raised border-b border-border-default text-[10.5px] font-bold text-text-secondary` |
| L2812 | info grid `grid [grid-template-columns:112px_minmax(0,1fr)] bg-surface-sunken` |

**ElevatorInfoCard L2883-L2920 (15건)**
| Line | 패턴 |
|----:|---|
| L2883 | compact wrap `border border-border-default rounded-md overflow-hidden shrink-0` |
| L2884 | compact header 7-prop |
| L2886 | "↕ 스크롤" hint `text-[9px] font-medium text-text-tertiary` |
| L2889 | scrollable grid 8-prop with WebkitOverflowScrolling + overscrollBehavior — **옵션 N 잔존** (non-standard CSS props) |
| L2894-L2895 | kSt/vSt spread + lastRowSt — **옵션 N 잔존** (2건) |
| L2904 | normal wrap `border border-border-default rounded-md overflow-hidden` |
| L2905 | normal header 5-prop |
| L2908 | normal grid 4-col |
| L2910 | vSt spread + gridColumn — **옵션 N 잔존** |
| L2912 | vSt spread + gridColumn — **옵션 N 잔존** |
| L2917-L2920 | kSt/vSt spread × 4 — **옵션 N 잔존** (4건) |

**MinwonFindingsPanel L3430-L3483 (18건)**
| Line | 패턴 |
|----:|---|
| L3430 | wrap warm banner `bg-[rgba(245,158,11,0.06)] border border-[rgba(245,158,11,0.2)] rounded-lg px-3.5 py-3 mt-1` |
| L3431 | title row 5-prop + AlertTriangle wrapper |
| L3438 | finding row `bg-surface-raised border border-border-default rounded-md px-3 py-2 mb-1.5` |
| L3439 | inner flex 4-prop |
| L3440 | desc span `text-[11px] font-bold text-text-primary flex-1` |
| L3441 | status chip dynamic — 옵션 M |
| L3444 | delete btn `bg-none border-0 text-text-tertiary text-body-sm cursor-pointer p-0` |
| L3448 | resolved msg `text-[10px] text-safe-bar mt-0.5` |
| L3455 | "수리이력에서 조치 연결" btn 7-prop |
| L3461 | linking dropdown 6-prop |
| L3462 | header `text-[10px] font-bold text-text-tertiary mb-1` |
| L3463 | empty msg `text-[10px] text-text-tertiary` |
| L3466-L3467 | repair option list 4-prop |
| L3471 | 취소 btn 7-prop |
| L3478 | input row flex gap-1.5 mt-1 |
| L3481 | input 8-prop |
| L3483 | submit btn 9-prop dynamic opacity (옵션 M+X) |

### 데스크톱 zone L527-L1024 — 변경 절대 0 (105건, Wave 15b 후행)

전체 상세 매핑은 Wave 15b PLAN.md 책임. 본 wave 는 보존만 검증.

데스크톱 zone 안 inline 분포 요약 (참고용):
- L614/622: 모드 토글 buttons (gradient bg)
- L668-L709: 우측 헤더/탭/카운트
- L716-L728: fault 카드 + 처리 btn
- L741-L799: repair 카드 + photo grid + expanded panel
- L823-L872: annual KOELSA 카드 + issues table
- L896-L989: safety manager card + edu 카드 + reg/contact 카드
- L1005-L1024: footer/refresh

### 비즈 anchor 카운트 (precise diff 검증 baseline — 양 plan 공통)

```
onClick=\{...\} : ~50+ (HEAD 측정 필요)
useState\( : ~15+
useRef\( : ~5+
useEffect\( : ~5+
useMutation\( : ~5+
useQuery\( : ~10+
useNavigate\( : 1
useParams\( : 0
useSearchParams\( : 0
fetch\( : 0+
```

각 plan 의 Step 1 (before snapshot) 에서 정확 카운트 측정 후 Step 3 (after) 와 diff = empty 검증.

### 룰 (locked)
- 옵션 X+P+M+색변수N + 16 module const 정의 보존
- 데스크톱 zone L527-L1024 절대 변경 0 (Wave 15b 책임)
- diff hunk awk overlap check 로 데스크톱 영역 침범 자동 검증
- 비즈 anchor precise diff = empty
- emoji 변동 0 (TYPE_ICON 의 🛗📦🔲↕️ 보존 + 본문 emoji 보존)
- 비표준 색 토큰 (warning/safe/danger no-suffix) 0
- TypeScript 0 error
- 변경 파일 = 1 .tsx 만 (off-scope = 0)
- 각 plan 단일 atomic commit

### 함정 회피

- **w-7=32 / w-8=48 spacing override** — height:28 → `w-7=32 ≠ 28`, arbitrary `[h:28px]` 필요. height:32 → `h-7`. height:36 → `h-9` (standard). height:44 → `h-11`. height:48 → `h-12` (standard, w-12=48 spacing override). w-8/h-8 = 48 함정 박제 적용
- **`bg-fire-bar` vs `bg-danger-bar`** — ElevatorPage 의 미조치/fault 는 `var(--danger)` 톤 (memory: 미조치 색 fire 룰은 InspectionPage 메인 칩만 적용, ElevatorPage 는 자체 danger 톤 유지) → `text-danger-bar`/`bg-danger-bar` 적용
- **dynamic color concat (`st.color + '18'`)** — Tailwind arbitrary 로 변환 불가. **옵션 N 잔존** (L3040 / L770 accent dynamic prop / L771 title color dynamic)
- **`...cellSt` / `...kSt` / `...vSt` spread** — Tailwind 와 무관, 옵션 N 잔존
- **`linear-gradient` 4건 (L1744/L1754/L2286/L2413)** — arbitrary 가능하나 길어서 시각 0 byte 보장 위해 옵션 N 잔존 권장
- **`NAV_H` const 참조** (L2328) — `bottom: NAV_H` → arbitrary `[bottom:calc(54px+env(safe-area-inset-bottom,20px))]` 가능하나 const 의미 흐려짐. 옵션 N 잔존 권장
- **`WebkitOverflowScrolling: 'touch'` / `overscrollBehavior: 'contain'`** (L2889) — Tailwind 미지원, 옵션 N 잔존
- **dynamic transform `translate(${pos.x}px,${pos.y}px) scale(${scale})`** (L3139) — 런타임 동적, 옵션 N 잔존
- **dynamic `gridTemplateColumns: \`repeat(${groupEvs.length}, 1fr)\``** (L2162) — 런타임 동적, 옵션 N 잔존
- **MOBILE/DESKTOP duplicate inline 없음** — single early-return 구조라 helper-shared zone 없음 (Wave 14a 와 다름)
- **var(--accent-active) → bg-accent-active** — tailwind.config L62 매핑 확인됨

</context>

<tasks>

<task type="auto">
  <name>Task 1 (Plan 01 = 15a-1): Sweep ElevatorPage Fault/Repair/Findings 트랙 — 51 inline (mobile zone partial)</name>
  <files>src/pages/ElevatorPage.tsx</files>
  <action>

### Step 1: Before snapshot

```bash
cd /Users/jykevin/Documents/cbc7119-design/cha-bio-safety
F=src/pages/ElevatorPage.tsx

# 1. inline count baseline
echo "style before: $(grep -c 'style={{' $F)"  # expect 206

# 2. desktop zone inline count (L527-L1024)
echo "desktop zone before: $(sed -n '527,1024p' $F | grep -c 'style={{')"  # expect 105

# 3. anchor counts (precise)
{
  for ANCHOR in 'onClick=\{[^}]+\}' 'useState\(' 'useRef\(' 'useEffect\(' 'useMutation\(' 'useQuery\(' 'useNavigate\(' 'useParams\(' 'useSearchParams\(' 'fetch\('; do
    echo "  $ANCHOR : $(grep -cE "$ANCHOR" $F)"
  done
} > /tmp/h8u-01-before.txt
cat /tmp/h8u-01-before.txt

# 4. onClick precise set
grep -oE 'onClick=\{[^}]+\}' $F | sort | uniq > /tmp/h8u-01-clicks-before.txt
echo "unique onClick callsites: $(wc -l < /tmp/h8u-01-clicks-before.txt)"

# 5. module const def counts
grep -cE '^const (STATUS_STYLE|OVERALL_STYLE|RESULT_STYLE|INSPECT_TYPE_LABEL|TYPE_ICON|TYPE_ICON_COMPONENT|CHECK_ITEMS_EV|CHECK_ITEMS_ES|HISTORY_TABS|EV_FLOORS|EV_GROUPS_FAULT|EV_GROUPS_ANNUAL|ES_NODES_FAULT|ES_NODES_ANNUAL|CHECK_ITEM_LABELS|PERIOD_OPTIONS):' $F

# 6. emoji baseline (TYPE_ICON + 본문)
grep -cE '🛗|📦|🔲|↕️|📄|📚|🏢|👤|✕|✓|✗|🔒|💾|🔥|⏰|📋|✅|⚠️|❌|🔧|🚨|🔍|🧯|📊' $F

# 7. 비색
echo "비색: $(grep -cE 'bg-warning[^-]|border-safe[^-]|border-warning[^-]|border-danger[^-]' $F)"
```

### Step 2: Edit ElevatorPage.tsx — Fault/Repair/Findings 트랙 51 inline

**절대 변경 금지 (데스크톱 zone L527-L1024, 105건)** — Wave 15b 책임.

#### 변환 (51 mobile inline — Fault/Repair/Findings 트랙)

**ElevatorPage main mobile stragglers (4건)**

**L1060** — main paddingBottom
```tsx
// Before
<main className="flex-1 min-h-0 overflow-y-auto px-3 py-2.5 flex flex-col gap-2" style={{ paddingBottom: 'calc(80px + var(--sab, 0px))' }}>
// After
<main className="flex-1 min-h-0 overflow-y-auto px-3 py-2.5 flex flex-col gap-2 pb-[calc(80px+var(--sab,0px))]">
```

**L1357** — inspect tab grid template (정적값)
```tsx
// Before
<div className="bg-surface-sunken grid" style={{ gridTemplateColumns:'50px 1fr auto' }}>
// After
<div className="bg-surface-sunken grid [grid-template-columns:50px_1fr_auto]">
```

**L1744** — fault FAB gradient + shadow → **옵션 N 잔존** (gradient inline 유지, boxShadow 도 함께)
```tsx
// Before (unchanged — 옵션 N)
style={{ background:'linear-gradient(135deg,#991b1b,#ef4444)', boxShadow:'0 4px 16px rgba(239,68,68,.4)' }}
// After (no change — 옵션 N 잔존, gradient + shadow inline 유지)
```

**L1754** — repair FAB gradient → **옵션 N 잔존** (var(--accent-active) 사용 gradient + shadow)
```tsx
// Before (unchanged — 옵션 N)
style={{ background:'linear-gradient(135deg, var(--accent-active), var(--accent))', boxShadow:'0 4px 16px rgba(59,130,246,.4)' }}
// After (no change — 옵션 N 잔존)
```

**EvDetailModal (2건)**

**L1874** — overlay 3-prop
```tsx
// Before
<div onClick={onClose} className="bg-surface-overlay" style={{ position:'fixed', inset:0, zIndex:90 }} />
// After
<div onClick={onClose} className="bg-surface-overlay fixed inset-0 z-[90]" />
```

**L1934** — EvDetailModal 내부 (Read 후 정확 매핑 결정. ElevatorInfoCard 의 compact prop pass 직전 div)
- grep 으로 L1930-L1940 컨텍스트 읽고 인라인 패턴 식별 후 옵션 X/P/M/N/token 매핑
- 단순 layout 이면 className 변환, 동적이면 옵션 M

**EvSelector L2162** — dynamic grid → **옵션 N 잔존**
```tsx
// Before (unchanged — 동적 값)
<div className="grid gap-2" style={{ gridTemplateColumns: `repeat(${groupEvs.length}, 1fr)` }}>
// After (no change — 옵션 N 잔존)
```

**FaultNewModal L2249/L2286**

**L2249** — Field style prop `{ flex:1 }` → Field 컴포넌트가 style prop 받음. inline 잔존 또는 className 으로 변환 (Field 시그니처 확인 필요)
- Field props: `function Field({ label, children, style }: { label:string; children:React.ReactNode; style?:React.CSSProperties })` (L2690)
- **옵션 N 잔존** (Field props 시그니처 변경 X, 시각 0 byte 보장)

**L2286** — submit btn gradient + dynamic opacity → **옵션 N 잔존** (gradient + dynamic)

**FaultNewFullscreen L2328/L2370/L2413**

**L2328** — fullscreen wrap 6-prop `{ top:0, left:0, right:0, bottom:NAV_H, zIndex:100, paddingTop:'var(--sat, 44px)', boxSizing:'border-box' }`
- top:0/left:0/right:0 + zIndex:100 + paddingTop arbitrary 변환 가능
- bottom:NAV_H (const 참조) → **옵션 N 잔존** (NAV_H 의미 보존)
- 부분 변환: className 에 `top-0 left-0 right-0 z-[100] pt-[var(--sat,44px)] box-border` 추가, inline 에 `{ bottom: NAV_H }` 만 잔존

```tsx
// Before
<div className="fixed flex flex-col bg-surface-page" style={{ top:0, left:0, right:0, bottom:NAV_H, zIndex:100, paddingTop:'var(--sat, 44px)', boxSizing:'border-box' }}>
// After
<div className="fixed flex flex-col bg-surface-page top-0 left-0 right-0 z-[100] pt-[var(--sat,44px)] box-border" style={{ bottom: NAV_H }}>
```

**L2370** — Field `{ flex:1 }` → 옵션 N 잔존 (L2249 동일 사유)

**L2413** — submit btn gradient + opacity → 옵션 N 잔존

**FaultResolveModal L2467**
```tsx
// Before
<button onClick={...} disabled={!repairDetail.trim()||loading}
  style={{ opacity:(!repairDetail.trim()||loading)?0.5:1 }}
  className="w-full h-[48px] rounded-lg font-bold text-body bg-accent text-text-on-accent border-none cursor-pointer transition">
// After (옵션 M)
<button onClick={...} disabled={!repairDetail.trim()||loading}
  className={`w-full h-[48px] rounded-lg font-bold text-body bg-accent text-text-on-accent border-none cursor-pointer transition ${(!repairDetail.trim()||loading) ? 'opacity-50' : 'opacity-100'}`}>
```

**CertViewerModal L2483-L2499 (10건)**

**L2483** — overlay
```tsx
// Before
<div onClick={onClose} style={{ position:'fixed', inset:0, background:'rgba(0,0,0,.85)', zIndex:200 }} />
// After
<div onClick={onClose} className="fixed inset-0 bg-[rgba(0,0,0,0.85)] z-[200]" />
```

**L2484** — container
```tsx
// Before
<div style={{ position:'fixed', inset:0, zIndex:201, display:'flex', flexDirection:'column', paddingTop:'var(--sat, 44px)', paddingBottom:'var(--sab, 0px)' }}>
// After
<div className="fixed inset-0 z-[201] flex flex-col pt-[var(--sat,44px)] pb-[var(--sab,0px)]">
```

**L2486** — header
```tsx
// Before
<div style={{ flexShrink:0, display:'flex', alignItems:'center', justifyContent:'space-between', padding:'10px 16px', background:'rgba(22,27,34,0.97)', borderBottom:'1px solid var(--bd)' }}>
// After
<div className="shrink-0 flex items-center justify-between px-4 py-2.5 bg-[rgba(22,27,34,0.97)] border-b border-border-default">
```

**L2487** — title
```tsx
// Before
<span style={{ fontSize:14, fontWeight:700, color:'var(--t1)' }}>검사성적서</span>
// After
<span className="text-body-sm font-bold text-text-primary">검사성적서</span>
```

**L2488** — btn-row
```tsx
// Before
<div style={{ display:'flex', gap:8 }}>
// After
<div className="flex gap-2">
```

**L2489** — "새 탭 열기" anchor
```tsx
// Before
<a href={url} target="_blank" rel="noopener" style={{ fontSize:11, fontWeight:700, color:'var(--acl)', padding:'6px 12px', borderRadius:8, background:'rgba(59,130,246,.15)', border:'1px solid rgba(59,130,246,.3)', textDecoration:'none' }}>새 탭 열기</a>
// After
<a href={url} target="_blank" rel="noopener" className="text-[11px] font-bold text-accent px-3 py-1.5 rounded-md bg-[rgba(59,130,246,0.15)] border border-[rgba(59,130,246,0.3)] no-underline">새 탭 열기</a>
```

**L2490** — close btn
```tsx
// Before
<button onClick={onClose} style={{ background:'none', border:'none', color:'var(--t3)', cursor:'pointer', fontSize:20 }}>✕</button>
// After
<button onClick={onClose} className="bg-none border-0 text-text-tertiary cursor-pointer text-[20px]">✕</button>
```

**L2494** — viewer area
```tsx
// Before
<div style={{ flex:1, minHeight:0, overflow:'hidden', position:'relative', background:'var(--bg)' }}>
// After
<div className="flex-1 min-h-0 overflow-hidden relative bg-surface-page">
```

**L2498** — inner wrap (img branch)
```tsx
// Before
<div style={{ width:'100%', height:'100%', display:'flex', alignItems:'center', justifyContent:'center', overflow:'auto' }}>
// After
<div className="w-full h-full flex items-center justify-center overflow-auto">
```

**L2499** — img
```tsx
// Before
<img src={url} alt="인증서" style={{ maxWidth:'100%', maxHeight:'100%', objectFit:'contain', display:'block' }} />
// After
<img src={url} alt="인증서" className="max-w-full max-h-full object-contain block" />
```

**FindingCountBadge L2519**
```tsx
// Before
<span style={{ fontSize:9, fontWeight:700, padding:'1px 6px', borderRadius:6, background:'rgba(239,68,68,.12)', color:'var(--danger)' }}>
// After
<span className="text-[9px] font-bold px-1.5 py-px rounded-md bg-[rgba(239,68,68,0.12)] text-danger-bar">
```

**FindingsPanel L2596-L2645 (20건)** — Read 후 컴포넌트 정독해서 각 매핑 적용

**L2596** wrap → `className="px-2 py-1.5"`
**L2597** row → `className="flex items-center justify-between mb-1"`
**L2598** label dynamic color → 옵션 M `className={\`text-[10px] font-bold ${inspectionResult === 'fail' ? 'text-danger-bar' : 'text-warning-bar'}\`}`
**L2599** count → `className="text-[9px] text-text-tertiary"`
**L2602** loading → `className="text-[9px] text-text-tertiary"`
**L2605** finding row → `className="px-1.5 py-1 bg-surface-raised rounded-[5px] mb-[3px]"`
**L2606** inner flex → `className="flex items-center gap-1 text-[10px]"`
**L2607** desc → `className="flex-1 text-text-primary font-semibold overflow-hidden text-ellipsis whitespace-nowrap"`
**L2608** status chip dynamic 2-prop (bg+color) — 옵션 M
```tsx
// Before
<span style={{ fontSize:8, fontWeight:700, padding:'1px 4px', borderRadius:6, flexShrink:0,
  background: f.status === 'open' ? 'rgba(239,68,68,.12)' : 'rgba(34,197,94,.12)',
  color: f.status === 'open' ? 'var(--danger)' : 'var(--safe)',
}}>
// After
<span className={`text-[8px] font-bold px-1 py-px rounded-md shrink-0 ${f.status === 'open' ? 'bg-[rgba(239,68,68,0.12)] text-danger-bar' : 'bg-[rgba(34,197,94,0.12)] text-safe-bar'}`}>
```
**L2615** "수리이력 연결" btn → `className="mt-0.5 px-1.5 py-0.5 rounded bg-[rgba(59,130,246,0.08)] border border-[rgba(59,130,246,0.15)] text-info-bar text-[9px] font-semibold cursor-pointer"`
**L2620** linking dropdown → `className="mt-[3px] bg-surface-sunken rounded p-1 max-h-[100px] overflow-y-auto border border-border-default"`
**L2621** empty msg → `className="text-[9px] text-text-tertiary p-0.5"`
**L2624** repair option list → `className="px-1.5 py-0.5 rounded-sm cursor-pointer mb-px bg-surface-raised text-[9px]"`
**L2625** date/title spans (2개) → 각자 className
**L2628** 취소 btn → `className="mt-0.5 px-1.5 py-px rounded-sm bg-none border border-border-default text-text-tertiary text-[8px] cursor-pointer"`
**L2632** resolved msg → `className="text-[9px] text-safe-bar mt-0.5 flex items-center gap-[3px]"`
**L2633** CheckCircle2 wrapper → Lucide style prop 제거, `className="shrink-0"` 만 사용
```tsx
// Before
<CheckCircle2 size={10} style={{ flexShrink:0 }} />
// After
<CheckCircle2 size={10} className="shrink-0" />
```
**L2640** input row → `className="flex gap-1 mt-[3px]"`
**L2643** input → `className="flex-1 px-1.5 py-[3px] rounded border border-border-default bg-surface-page text-text-primary text-[10px] outline-none"`
**L2645** submit btn 9-prop dynamic — 옵션 M+X
```tsx
// Before
<button onClick={handleAddFinding} disabled={!newDesc.trim() || addingFinding}
  style={{ padding:'3px 8px', borderRadius:4, border:'none', background: inspectionResult === 'fail' ? 'var(--danger)' : 'var(--warn)', color:'#fff', fontSize:9, fontWeight:700, cursor:'pointer', opacity: !newDesc.trim() || addingFinding ? 0.5 : 1 }}>
// After
<button onClick={handleAddFinding} disabled={!newDesc.trim() || addingFinding}
  className={`px-2 py-[3px] rounded border-0 text-white text-[9px] font-bold cursor-pointer ${inspectionResult === 'fail' ? 'bg-danger-bar' : 'bg-warning-bar'} ${!newDesc.trim() || addingFinding ? 'opacity-50' : 'opacity-100'}`}>
```

**RepairListSection L3040** — dynamic color concat → **옵션 N 잔존**
```tsx
// Before (unchanged — st.color is dynamic prop)
<span className="text-caption font-bold px-1.5 py-0.5 rounded-sm" style={{ background: st.color + '18', color: st.color }}>
// After (no change — 옵션 N)
```

**RepairImageViewer L3119-L3139 (5건)**

**L3119** — fullscreen overlay
```tsx
// Before
<div style={{ position:'fixed', inset:0, zIndex:300, background:'rgba(0,0,0,0.95)', display:'flex', flexDirection:'column' }}>
// After
<div className="fixed inset-0 z-[300] bg-[rgba(0,0,0,0.95)] flex flex-col">
```

**L3120** — header
```tsx
// Before
<div style={{ flexShrink:0, display:'flex', justifyContent:'flex-end', padding:'12px 16px', paddingTop:'calc(12px + var(--sat, 44px))' }}>
// After
<div className="shrink-0 flex justify-end px-4 pt-[calc(12px+var(--sat,44px))] pb-3">
```

**L3121** — close btn
```tsx
// Before
<button onClick={onClose} style={{ background:'none', border:'none', color:'#fff', fontSize:24, cursor:'pointer' }}>✕</button>
// After
<button onClick={onClose} className="bg-none border-0 text-white text-[24px] cursor-pointer">✕</button>
```

**L3137** — content area (touchAction:'none')
```tsx
// Before
style={{ flex:1, overflow:'hidden', display:'flex', alignItems:'center', justifyContent:'center', touchAction:'none' }}
// After
className="flex-1 overflow-hidden flex items-center justify-center touch-none"
```

**L3139** — img dynamic transform → **옵션 N 잔존** (dynamic transform/transition)
```tsx
// Before (unchanged — dynamic transform)
<img src={src} alt="" draggable={false} style={{ maxWidth:'100%', maxHeight:'100%', objectFit:'contain', transform:`translate(${pos.x}px,${pos.y}px) scale(${scale})`, transition: dragging ? 'none' : 'transform 0.15s', userSelect:'none' }} />
// After (partial — className for static, inline for dynamic)
<img src={src} alt="" draggable={false} className="max-w-full max-h-full object-contain select-none" style={{ transform:`translate(${pos.x}px,${pos.y}px) scale(${scale})`, transition: dragging ? 'none' : 'transform 0.15s' }} />
```

**RepairNewModal L3355** — dynamic opacity (옵션 M)
```tsx
// Before
<button onClick={handleSubmit} disabled={!canSubmit}
  style={{ opacity: canSubmit ? 1 : 0.5 }}
  className="w-full py-[13px] rounded-md border-none bg-accent hover:bg-accent-hover text-white text-body-sm font-bold cursor-pointer transition-colors disabled:cursor-not-allowed">
// After (옵션 M)
<button onClick={handleSubmit} disabled={!canSubmit}
  className={`w-full py-[13px] rounded-md border-none bg-accent hover:bg-accent-hover text-white text-body-sm font-bold cursor-pointer transition-colors disabled:cursor-not-allowed ${canSubmit ? 'opacity-100' : 'opacity-50'}`}>
```

### Step 3: After verification

```bash
F=src/pages/ElevatorPage.tsx

# 1. style count drop (전체)
A=$(grep -c 'style={{' $F)
echo "after: $A (expect ~158-165; was 206, -41 to -48 변환)"

# 2. 데스크톱 zone 보존 검증 (L527-L1024)
DSK=$(sed -n '527,1024p' $F | grep -c 'style={{')
echo "desktop zone after: $DSK (MUST = 105)"
[ "$DSK" = "105" ] && echo "DESKTOP ZONE OK" || echo "DESKTOP ZONE BROKEN ❌"

# 3. 비즈 anchor identity
{
  for ANCHOR in 'onClick=\{[^}]+\}' 'useState\(' 'useRef\(' 'useEffect\(' 'useMutation\(' 'useQuery\(' 'useNavigate\(' 'useParams\(' 'useSearchParams\(' 'fetch\('; do
    echo "  $ANCHOR : $(grep -cE "$ANCHOR" $F)"
  done
} > /tmp/h8u-01-after.txt
diff /tmp/h8u-01-before.txt /tmp/h8u-01-after.txt  # MUST empty

# 4. onClick precise diff
grep -oE 'onClick=\{[^}]+\}' $F | sort | uniq > /tmp/h8u-01-clicks-after.txt
diff /tmp/h8u-01-clicks-before.txt /tmp/h8u-01-clicks-after.txt  # MUST empty

# 5. emoji 변동 0 (TYPE_ICON 4개 + 본문 emoji)
echo "emoji after: $(grep -cE '🛗|📦|🔲|↕️|📄|📚|🏢|👤|✕|✓|✗|🔒|💾|🔥|⏰|📋|✅|⚠️|❌|🔧|🚨|🔍|🧯|📊' $F)"

# 6. 비색
echo "비색: $(grep -cE 'bg-warning[^-]|border-safe[^-]|border-warning[^-]|border-danger[^-]' $F)"  # MUST = 0

# 7. module const def 보존 (16개)
CONST_COUNT=$(grep -cE '^const (STATUS_STYLE|OVERALL_STYLE|RESULT_STYLE|INSPECT_TYPE_LABEL|TYPE_ICON|TYPE_ICON_COMPONENT|CHECK_ITEMS_EV|CHECK_ITEMS_ES|HISTORY_TABS|EV_FLOORS|EV_GROUPS_FAULT|EV_GROUPS_ANNUAL|ES_NODES_FAULT|ES_NODES_ANNUAL|CHECK_ITEM_LABELS|PERIOD_OPTIONS):' $F)
echo "module const: $CONST_COUNT (MUST = 16)"

# 8. diff hunk 데스크톱 zone 영역 침범 검증
git diff $F > /tmp/h8u-01-diff.txt
echo "=== diff hunk headers ==="
grep -E '^@@' /tmp/h8u-01-diff.txt | head -50
awk '/^@@/ {
  match($0, /\-([0-9]+),([0-9]+)/, m);
  start=m[1]; len=m[2];
  end=start+len-1;
  if ((start >= 527 && start <= 1024) || (end >= 527 && end <= 1024) || (start < 527 && end > 1024)) {
    print "DESKTOP ZONE TOUCHED: " $0
  }
}' /tmp/h8u-01-diff.txt
# (출력 없으면 OK)

# 9. TypeScript
./node_modules/.bin/tsc --noEmit 2>&1 | grep -c "error TS"  # MUST = 0

# 10. off-scope
cd .. && git diff --name-only HEAD | grep -v "cha-bio-safety/src/pages/ElevatorPage.tsx" | grep -v ".planning/" | wc -l  # MUST = 0
cd cha-bio-safety && git diff --name-only HEAD  # MUST = src/pages/ElevatorPage.tsx 1줄만

# 11. vite build (메가 페이지 — 권장)
npm run build 2>&1 | tail -10  # MUST succeed
```

### Step 4: Atomic commit

```bash
cd /Users/jykevin/Documents/cbc7119-design
git add cha-bio-safety/src/pages/ElevatorPage.tsx
git commit --no-verify -m "$(cat <<'EOF'
feat(260529-h8u-01): Phase B Wave 15a-1 — ElevatorPage Fault/Repair/Findings 트랙 (51 inline) → tailwind

Tier 2 네 번째 wave 15a 의 메가 페이지 분할 1/2. ElevatorPage Fault/Repair/Findings 트랙 51 mobile inline sweep.
데스크톱 zone 105곳 (L527-L1024 `if (isDesktop) { return }` 단일 블록) 절대 변경 0 (Wave 15b 후행).
16 module const (STATUS_STYLE/OVERALL_STYLE/RESULT_STYLE/INSPECT_TYPE_LABEL/TYPE_ICON/TYPE_ICON_COMPONENT/CHECK_ITEMS_EV/CHECK_ITEMS_ES/HISTORY_TABS/EV_FLOORS/EV_GROUPS_FAULT/EV_GROUPS_ANNUAL/ES_NODES_FAULT/ES_NODES_ANNUAL/CHECK_ITEM_LABELS/PERIOD_OPTIONS) 정의 + TYPE_ICON emoji 4 (🛗📦🔲↕️) 보존.
토큰 alias 매핑 (var(--bg/bg2/bg3/bd/t1/t2/t3/acl/accent-active/safe/warn/danger/info) → tailwind tokens via tokens.css L187-191 + tailwind.config L60-82).
옵션 X+P+M+색변수N + linear-gradient/spread/dynamic-transform 옵션 N 잔존. 시각 0 byte 변경.
대상: ElevatorPage main mobile stragglers (4) + EvDetailModal (2) + EvSelector (1) + Fault modals (6) + CertViewerModal (10) + FindingCountBadge (1) + FindingsPanel (20) + Repair components (7).
비즈 anchor identical. TypeScript 0 error. vite build succeeded.
EOF
)"
```

### Step 5: SUMMARY.md (atomic 단위)

Write `cha-bio-safety/.planning/quick/260529-h8u-phase-b-wave-15a/260529-h8u-01-SUMMARY.md` (plan 01 only).

  </action>
  <verify>
    <automated>cd /Users/jykevin/Documents/cbc7119-design/cha-bio-safety && S=$(grep -c 'style={{' src/pages/ElevatorPage.tsx) && echo "after=$S (was 206)" && DSK=$(sed -n '527,1024p' src/pages/ElevatorPage.tsx | grep -c 'style={{') && echo "desktop=$DSK (MUST=105)" && [ "$DSK" = "105" ] && [ "$(./node_modules/.bin/tsc --noEmit 2>&1 | grep -c 'error TS')" = "0" ] && [ "$(grep -cE 'bg-warning[^-]|border-safe[^-]|border-warning[^-]|border-danger[^-]' src/pages/ElevatorPage.tsx)" = "0" ] && [ "$(grep -cE '^const (STATUS_STYLE|OVERALL_STYLE|RESULT_STYLE|INSPECT_TYPE_LABEL|TYPE_ICON|TYPE_ICON_COMPONENT|CHECK_ITEMS_EV|CHECK_ITEMS_ES|HISTORY_TABS|EV_FLOORS|EV_GROUPS_FAULT|EV_GROUPS_ANNUAL|ES_NODES_FAULT|ES_NODES_ANNUAL|CHECK_ITEM_LABELS|PERIOD_OPTIONS):' src/pages/ElevatorPage.tsx)" = "16" ] && cd .. && [ "$(git diff --name-only HEAD | grep -v 'cha-bio-safety/src/pages/ElevatorPage.tsx' | grep -v '.planning/' | wc -l | tr -d ' ')" = "0" ]</automated>
  </verify>
  <done>
    - ElevatorPage total inline ~158-165 (목표 ~160; 206 - 41~48 변환)
    - 데스크톱 zone L527-L1024 = 105 (변경 0)
    - 16 module const def 보존
    - TYPE_ICON emoji 4 + 본문 emoji 변동 0
    - 비즈 anchor precise diff = empty (10 anchors)
    - 비색 0
    - TypeScript 0 error
    - 변경 파일 = 1 .tsx 만 (off-scope = 0)
    - 단일 atomic commit (15a-1)
    - diff hunk 데스크톱 zone 영역 변경 0 (awk 검증 출력 empty)
    - vite build succeeded
  </done>
</task>

<task type="auto">
  <name>Task 2 (Plan 02 = 15a-2): Sweep ElevatorPage Cert/Info/Annual 트랙 — 50 inline (mobile zone remainder)</name>
  <files>src/pages/ElevatorPage.tsx</files>
  <action>

### Step 1: Before snapshot (post-15a-1)

```bash
cd /Users/jykevin/Documents/cbc7119-design/cha-bio-safety
F=src/pages/ElevatorPage.tsx

# 1. inline count baseline (post-15a-1)
echo "style before 15a-2: $(grep -c 'style={{' $F)"  # expect ~158-165

# 2. desktop zone 보존 확인
echo "desktop zone before: $(sed -n '527,1024p' $F | grep -c 'style={{')"  # expect 105

# 3. anchor counts
{
  for ANCHOR in 'onClick=\{[^}]+\}' 'useState\(' 'useRef\(' 'useEffect\(' 'useMutation\(' 'useQuery\(' 'useNavigate\(' 'useParams\(' 'useSearchParams\(' 'fetch\('; do
    echo "  $ANCHOR : $(grep -cE "$ANCHOR" $F)"
  done
} > /tmp/h8u-02-before.txt

# 4. onClick precise set
grep -oE 'onClick=\{[^}]+\}' $F | sort | uniq > /tmp/h8u-02-clicks-before.txt

# 5. const def
grep -cE '^const (STATUS_STYLE|OVERALL_STYLE|RESULT_STYLE|INSPECT_TYPE_LABEL|TYPE_ICON|TYPE_ICON_COMPONENT|CHECK_ITEMS_EV|CHECK_ITEMS_ES|HISTORY_TABS|EV_FLOORS|EV_GROUPS_FAULT|EV_GROUPS_ANNUAL|ES_NODES_FAULT|ES_NODES_ANNUAL|CHECK_ITEM_LABELS|PERIOD_OPTIONS):' $F
```

### Step 2: Edit ElevatorPage.tsx — Cert/Info/Annual 트랙 50 inline

**절대 변경 금지 (데스크톱 zone L527-L1024, 105건)** — Wave 15b 책임.

#### 변환 (50 mobile inline — Cert/Info/Annual 트랙)

> **주의: 15a-1 commit 으로 라인 번호가 일부 이동했을 수 있음. 변환 전 grep 으로 정확 라인 식별 후 매핑.**

**CertSummary L2727 (post-15a-1 라인 이동 가능)**
```tsx
// Before
<div style={{ marginTop:12, display:'flex', flexDirection:'column', gap:14 }}>
// After
<div className="mt-3 flex flex-col gap-3.5">
```

**CertBlock L2767-L2812 (16건, post-15a-1 라인 이동 가능)**

**L2767** wrap → `className="flex flex-col gap-2.5"`

**L2769** title row → `className="flex items-center gap-2 pt-1"`

**L2770** accent bar — dynamic accent → 옵션 N (accent prop 동적 가능). 단순 색 변수면 옵션 M:
```tsx
// Before
<div style={{ width:3, height:14, background: accent ?? 'var(--acl)', borderRadius:2 }} />
// After (옵션 M conditional, accent 가 prop string)
<div className="w-[3px] h-[14px] rounded-[2px]" style={{ background: accent ?? 'var(--acl)' }} />
// → accent dynamic 이므로 background 만 옵션 N 잔존, 나머지 className 변환
```

**L2771** title span — dynamic color → 옵션 N (color prop 동적)
```tsx
// Before
<span style={{ fontSize:12, fontWeight:700, color: accent ?? 'var(--t1)' }}>{title}</span>
// After (옵션 N partial)
<span className="text-caption font-bold" style={{ color: accent ?? 'var(--t1)' }}>{title}</span>
```

**L2774** "PDF 보기" btn → `className="ml-auto text-[10px] font-bold px-2 py-[3px] rounded-md bg-[rgba(34,197,94,0.1)] border border-[rgba(34,197,94,0.3)] text-safe-bar cursor-pointer"`

**L2782** items wrap → `className="border border-border-default rounded-md overflow-hidden"`

**L2783** header → `className="px-2.5 py-1.5 bg-surface-raised border-b border-border-default text-[10.5px] font-bold text-text-secondary"`

**L2786** grid → `className="grid bg-surface-sunken [grid-template-columns:40px_1fr_auto]"`

**L2791-L2793** cellSt spread (3건) → **옵션 N 잔존** (`...cellSt` spread + dynamic color/fontWeight)
```tsx
// Before/After unchanged — 옵션 N 잔존
<div key={`${idx}-no`} style={{ ...cellSt, color:'var(--t3)', fontWeight:600 }}>{it.no}</div>
<div key={`${idx}-name`} style={{ ...cellSt, color:'var(--t1)' }}>{it.name}</div>
<div key={`${idx}-result`} style={{ ...cellSt, color:resultColor(it.result), fontWeight:700 }}>{it.result}</div>
```

**L2795** findings spacer → 옵션 N (gridColumn + borderBottom dynamic)
**L2796** findings content → 옵션 N (gridColumn + borderBottom dynamic)

**L2808** info wrap → `className="border border-border-default rounded-md overflow-hidden"`

**L2809** info header → `className="px-2.5 py-1.5 bg-surface-raised border-b border-border-default text-[10.5px] font-bold text-text-secondary"`

**L2812** info grid → `className="grid bg-surface-sunken [grid-template-columns:112px_minmax(0,1fr)]"`

**ElevatorInfoCard L2883-L2920 (15건)**

**L2883** compact wrap → `className="border border-border-default rounded-md overflow-hidden shrink-0"`

**L2884** compact header 7-prop → `className="px-2.5 py-[7px] bg-surface-raised border-b border-border-default text-[11px] font-bold text-text-primary flex items-center justify-between"`

**L2886** "↕ 스크롤" hint → `className="text-[9px] font-medium text-text-tertiary"`

**L2889** scrollable grid with WebkitOverflowScrolling/overscrollBehavior → **옵션 N 잔존** (non-standard CSS)
```tsx
// Before (unchanged — Tailwind 미지원 props)
<div style={{ maxHeight:170, overflowY:'auto', WebkitOverflowScrolling:'touch', overscrollBehavior:'contain', display:'grid', gridTemplateColumns:'112px minmax(0, 1fr)', background:'var(--bg3)' } as React.CSSProperties}>
// After (옵션 N 잔존 — WebkitOverflowScrolling 외 partial 도 가능하나 깔끔히 옵션 N 유지)
```

**L2894-L2895** kSt/vSt spread (2건) → **옵션 N 잔존**

**L2904** normal wrap → `className="border border-border-default rounded-md overflow-hidden"`

**L2905** normal header → `className="px-2.5 py-[7px] bg-surface-raised border-b border-border-default text-[11px] font-bold text-text-primary"`

**L2908** normal grid → `className="grid bg-surface-sunken [grid-template-columns:max-content_minmax(0,1fr)_max-content_minmax(0,1fr)]"`

**L2910/L2912** vSt spread + gridColumn → **옵션 N 잔존** (2건)

**L2917-L2920** kSt/vSt spread × 4 → **옵션 N 잔존** (4건)

**MinwonFindingsPanel L3430-L3483 (18건)**

**L3430** wrap warm banner → `className="bg-[rgba(245,158,11,0.06)] border border-[rgba(245,158,11,0.2)] rounded-[10px] px-3.5 py-3 mt-1"`

**L3431** title row → `className="text-caption font-bold text-warning-bar mb-2 flex items-center gap-1"`

**L3438** finding row → `className="bg-surface-raised border border-border-default rounded-md px-3 py-2 mb-1.5"`

**L3439** inner flex → `className="flex items-center gap-1.5 mb-1"`

**L3440** desc → `className="text-[11px] font-bold text-text-primary flex-1"`

**L3441** status chip dynamic — 옵션 M
```tsx
// Before
<span style={{ fontSize:9, fontWeight:700, padding:'1px 6px', borderRadius:4, background: f.status === 'resolved' ? 'rgba(34,197,94,.12)' : 'rgba(239,68,68,.12)', color: f.status === 'resolved' ? 'var(--safe)' : 'var(--danger)' }}>
// After
<span className={`text-[9px] font-bold px-1.5 py-px rounded ${f.status === 'resolved' ? 'bg-[rgba(34,197,94,0.12)] text-safe-bar' : 'bg-[rgba(239,68,68,0.12)] text-danger-bar'}`}>
```

**L3444** delete btn → `className="bg-none border-0 text-text-tertiary text-body-sm cursor-pointer p-0"`

**L3448** resolved msg → `className="text-[10px] text-safe-bar mt-0.5"`

**L3455** "수리이력에서 조치 연결" btn → `className="mt-1 px-2.5 py-1 rounded-md bg-[rgba(59,130,246,0.08)] border border-[rgba(59,130,246,0.2)] text-info-bar text-[10px] font-semibold cursor-pointer"`

**L3461** linking dropdown → `className="mt-1.5 bg-surface-sunken rounded-md p-2 max-h-[150px] overflow-y-auto"`

**L3462** header → `className="text-[10px] font-bold text-text-tertiary mb-1"`

**L3463** empty msg → `className="text-[10px] text-text-tertiary"`

**L3466** repair option row → `className="px-2 py-1.5 rounded-[5px] cursor-pointer text-[10px] text-text-primary mb-0.5 bg-surface-raised border border-border-default"`

**L3467** date span (inner span) → `className="font-semibold"`

**L3471** 취소 btn → `className="mt-1 px-2 py-[3px] rounded-[5px] bg-none border border-border-default text-text-tertiary text-[9px] cursor-pointer"`

**L3478** input row → `className="flex gap-1.5 mt-1"`

**L3481** input → `className="flex-1 px-2.5 py-[7px] rounded-md border border-border-default bg-surface-page text-text-primary text-[11px] outline-none"`

**L3483** submit btn dynamic — 옵션 M+X
```tsx
// Before
<button onClick={handleAdd} disabled={!newDesc.trim() || saving}
  style={{ padding:'7px 14px', borderRadius:7, border:'none', background:'var(--warn)', color:'#fff', fontSize:11, fontWeight:700, cursor:'pointer', opacity: !newDesc.trim() || saving ? 0.5 : 1 }}>
// After
<button onClick={handleAdd} disabled={!newDesc.trim() || saving}
  className={`px-3.5 py-[7px] rounded-md border-0 bg-warning-bar text-white text-[11px] font-bold cursor-pointer ${!newDesc.trim() || saving ? 'opacity-50' : 'opacity-100'}`}>
```

### Step 3: After verification

```bash
F=src/pages/ElevatorPage.tsx

# 1. style count drop
A=$(grep -c 'style={{' $F)
echo "after 15a-2: $A (expect ~100-115; 모바일 zone 거의 0+옵션 N ~15-20 + desktop 105)"

# 2. 데스크톱 zone 보존 (L527-L1024 - 라인 이동 후 재계산 필요)
# 주의: 15a-1 commit 으로 desktop zone start/end 라인 이동했을 가능성. python 으로 재식별
python3 << 'PYEOF'
with open('src/pages/ElevatorPage.tsx') as f:
    lines = f.readlines()
import re
for i, line in enumerate(lines, 1):
    if re.search(r'^\s+if \(isDesktop\)\s*\{', line):
        depth = 0; started = False
        for j in range(i-1, len(lines)):
            for ch in lines[j]:
                if ch == '{': depth += 1; started = True
                elif ch == '}':
                    depth -= 1
                    if started and depth == 0:
                        print(f"Desktop zone (post-15a-2): L{i}-L{j+1}")
                        desktop_zone_count = sum(1 for k in range(i-1, j) if 'style={{' in lines[k])
                        print(f"Desktop zone inline count: {desktop_zone_count}")
                        break
            else: continue
            break
        break
PYEOF

# 3. 비즈 anchor identity
{
  for ANCHOR in 'onClick=\{[^}]+\}' 'useState\(' 'useRef\(' 'useEffect\(' 'useMutation\(' 'useQuery\(' 'useNavigate\(' 'useParams\(' 'useSearchParams\(' 'fetch\('; do
    echo "  $ANCHOR : $(grep -cE "$ANCHOR" $F)"
  done
} > /tmp/h8u-02-after.txt
diff /tmp/h8u-02-before.txt /tmp/h8u-02-after.txt  # MUST empty

# 4. onClick precise
grep -oE 'onClick=\{[^}]+\}' $F | sort | uniq > /tmp/h8u-02-clicks-after.txt
diff /tmp/h8u-02-clicks-before.txt /tmp/h8u-02-clicks-after.txt  # MUST empty

# 5. emoji 변동 0
echo "emoji after: $(grep -cE '🛗|📦|🔲|↕️|📄|📚|🏢|👤|✕|✓|✗|🔒|💾|🔥|⏰|📋|✅|⚠️|❌|🔧|🚨|🔍|🧯|📊' $F)"

# 6. 비색
echo "비색: $(grep -cE 'bg-warning[^-]|border-safe[^-]|border-warning[^-]|border-danger[^-]' $F)"  # MUST = 0

# 7. const def
echo "module const: $(grep -cE '^const (STATUS_STYLE|OVERALL_STYLE|RESULT_STYLE|INSPECT_TYPE_LABEL|TYPE_ICON|TYPE_ICON_COMPONENT|CHECK_ITEMS_EV|CHECK_ITEMS_ES|HISTORY_TABS|EV_FLOORS|EV_GROUPS_FAULT|EV_GROUPS_ANNUAL|ES_NODES_FAULT|ES_NODES_ANNUAL|CHECK_ITEM_LABELS|PERIOD_OPTIONS):' $F)"  # MUST = 16

# 8. diff hunk 데스크톱 zone 침범 검증 (python 동적 라인 식별 후)
git diff HEAD $F > /tmp/h8u-02-diff.txt
# 데스크톱 zone 라인 범위는 Step 2 의 python 출력값 사용 (예: L527-L1024)
DSK_START=527
DSK_END=1024
echo "=== diff hunk overlap check (L$DSK_START-L$DSK_END) ==="
awk -v ds=$DSK_START -v de=$DSK_END '/^@@/ {
  match($0, /\-([0-9]+),([0-9]+)/, m);
  start=m[1]; len=m[2];
  end=start+len-1;
  if ((start >= ds && start <= de) || (end >= ds && end <= de) || (start < ds && end > de)) {
    print "DESKTOP ZONE TOUCHED: " $0
  }
}' /tmp/h8u-02-diff.txt

# 9. TypeScript
./node_modules/.bin/tsc --noEmit 2>&1 | grep -c "error TS"  # MUST = 0

# 10. off-scope
cd .. && git diff --name-only HEAD | grep -v "cha-bio-safety/src/pages/ElevatorPage.tsx" | grep -v ".planning/" | wc -l  # MUST = 0
cd cha-bio-safety && git diff --name-only HEAD

# 11. vite build
npm run build 2>&1 | tail -10  # MUST succeed
```

### Step 4: Atomic commit

```bash
cd /Users/jykevin/Documents/cbc7119-design
git add cha-bio-safety/src/pages/ElevatorPage.tsx
git commit --no-verify -m "$(cat <<'EOF'
feat(260529-h8u-02): Phase B Wave 15a-2 — ElevatorPage Cert/Info/Annual 트랙 (50 inline) → tailwind

Tier 2 네 번째 wave 15a 의 메가 페이지 분할 2/2. ElevatorPage Cert/Info/Annual 트랙 50 mobile inline sweep.
데스크톱 zone 105곳 (단일 `if (isDesktop) { return }` 블록) 절대 변경 0 (Wave 15b 후행).
16 module const + emoji 4 (🛗📦🔲↕️) + 본문 emoji 보존.
대상: CertSummary (1) + CertBlock (16) + ElevatorInfoCard (15) + MinwonFindingsPanel (18).
옵션 X+P+M+색변수N + cellSt/kSt/vSt spread/dynamic color (accent prop) 옵션 N 잔존 ~10-13건.
시각 0 byte 변경. 비즈 anchor identical. TypeScript 0 error. vite build succeeded.
**Wave 15a 메가 페이지 분할 완결** — 모바일 zone 101 → ~0-5 (옵션 N ~15-20), 데스크톱 zone 105 보존, 전체 206 → ~100-110.
EOF
)"
```

### Step 5: SUMMARY.md (양 plan 통합)

Write `cha-bio-safety/.planning/quick/260529-h8u-phase-b-wave-15a/260529-h8u-SUMMARY.md` (양 plan 통합 — 메가 페이지 분할 완결 요약).

Wave 15a 분할 완결 — 메가 페이지 안전 분할 첫 사례 + 16번째 atomic (단, 2 atomic commit 사용) + zone-aware sweep 4번째 wave.

  </action>
  <verify>
    <automated>cd /Users/jykevin/Documents/cbc7119-design/cha-bio-safety && S=$(grep -c 'style={{' src/pages/ElevatorPage.tsx) && echo "after 15a-2=$S (expect 100-115)" && [ "$S" -ge "95" ] && [ "$S" -le "120" ] && [ "$(./node_modules/.bin/tsc --noEmit 2>&1 | grep -c 'error TS')" = "0" ] && [ "$(grep -cE 'bg-warning[^-]|border-safe[^-]|border-warning[^-]|border-danger[^-]' src/pages/ElevatorPage.tsx)" = "0" ] && [ "$(grep -cE '^const (STATUS_STYLE|OVERALL_STYLE|RESULT_STYLE|INSPECT_TYPE_LABEL|TYPE_ICON|TYPE_ICON_COMPONENT|CHECK_ITEMS_EV|CHECK_ITEMS_ES|HISTORY_TABS|EV_FLOORS|EV_GROUPS_FAULT|EV_GROUPS_ANNUAL|ES_NODES_FAULT|ES_NODES_ANNUAL|CHECK_ITEM_LABELS|PERIOD_OPTIONS):' src/pages/ElevatorPage.tsx)" = "16" ] && cd .. && [ "$(git diff --name-only HEAD | grep -v 'cha-bio-safety/src/pages/ElevatorPage.tsx' | grep -v '.planning/' | wc -l | tr -d ' ')" = "0" ]</automated>
  </verify>
  <done>
    - ElevatorPage total inline ~100-115 (목표 ~110; 206 - 85~100 변환, 옵션 N ~15-20)
    - 데스크톱 zone (재식별 후) 105 보존 (변경 0)
    - 16 module const def 보존
    - TYPE_ICON emoji 4 + 본문 emoji 변동 0
    - 비즈 anchor precise diff = empty
    - 비색 0
    - TypeScript 0 error
    - 변경 파일 = 1 .tsx 만 (off-scope = 0)
    - 단일 atomic commit (15a-2)
    - diff hunk 데스크톱 zone 침범 0 (awk overlap check empty)
    - vite build succeeded
    - **Wave 15a 메가 페이지 분할 완결**
  </done>
</task>

</tasks>
