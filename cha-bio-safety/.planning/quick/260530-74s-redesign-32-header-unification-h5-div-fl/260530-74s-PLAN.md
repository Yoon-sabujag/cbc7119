---
phase: quick
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/DivPage.tsx
  - src/pages/FloorPlanPage.tsx
  - src/pages/WorkShiftPage.tsx
  - src/pages/RemediationDetailPage.tsx
  - src/pages/StaffManagePage.tsx
autonomous: true
requirements:
  - REDESIGN-32-H5
must_haves:
  truths:
    - "DivPage 데스크톱 헤더 (DIV 압력 관리) 가 h-14 (56px) 로 ElevatorPage 데스크톱 표준과 일관"
    - "FloorPlanPage 데스크톱 분기 헤더가 h-14, 모바일 분기 헤더가 h-12 px-3 표준"
    - "WorkShiftPage 데스크톱 분기 헤더가 h-14 (모바일 h-12 보존)"
    - "RemediationDetailPage 모바일 back 버튼이 w-7 h-7 (32px)"
    - "StaffManagePage 데스크톱 헤더 컨테이너가 h-14 px-5, 직원 추가 버튼이 h-7 (32px)"
    - "vite build PASS / TypeScript 0 error"
    - "그 외 본문/모달/사이드바/모바일/데스크톱 보존 영역 한 byte 변경 0"
  artifacts:
    - path: "src/pages/DivPage.tsx"
      provides: "L957 데스크톱 헤더 h-14"
      contains: "h-14 bg-surface-raised border-b border-border-default px-5"
    - path: "src/pages/FloorPlanPage.tsx"
      provides: "L937 데스크톱 + L938 모바일 헤더 표준화"
      contains: "h-14 px-5"
    - path: "src/pages/WorkShiftPage.tsx"
      provides: "L90 데스크톱 분기 h-14"
      contains: "isDesktop ? 'h-14 px-5 gap-2.5'"
    - path: "src/pages/RemediationDetailPage.tsx"
      provides: "L275 back 버튼 w-7 h-7"
      contains: "absolute left-3 w-7 h-7"
    - path: "src/pages/StaffManagePage.tsx"
      provides: "L393 데스크톱 헤더 + L398 추가 버튼 표준화"
      contains: "h-14 px-5"
  key_links:
    - from: "DivPage L957 / FloorPlanPage L937 / WorkShiftPage L90 / StaffManagePage L393"
      to: "ElevatorPage 데스크톱 헤더 표준 (h-14)"
      via: "sketch v2 §데스크톱 스펙"
      pattern: "h-14"
    - from: "FloorPlanPage L938"
      to: "모바일 헤더 표준 (h-12 px-3)"
      via: "sketch v2 §모바일 스펙"
      pattern: "h-12 px-3"
    - from: "RemediationDetailPage L275 / StaffManagePage L398"
      to: "back/action 버튼 표준 (w-7 h-7 / h-7)"
      via: "sketch v2 §케이스 1/4 spec"
      pattern: "w-7 h-7"
---

<objective>
Wave H5 — 데스크톱 헤더 `h-[54px]` → `h-14` 표준 통일 + 잔여 헤더 보정 (FloorPlan 모바일 분기 / RemediationDetail back / StaffManage 데스크톱).

5 파일 7 곳 정확한 byte-level 수정. 데스크톱 헤더 ElevatorPage 표준 (`h-14` = 56px) 정렬 + 모바일 헤더 표준 (`h-12 px-3`) + 백버튼 표준 (`w-7 h-7` = 32px) + 액션 버튼 표준 (`h-7` = 32px).

Purpose: redesign/32-header-unification 시리즈 H1~H4 완결 이후 잔여 헤더 표준화. 헤더 안 컴포넌트 통일은 H1~H4 에서 끝났고, 이번 wave 는 컨테이너 height + 그 위 잔여 버튼/링크.
Output: 5 파일 atomic commit + SUMMARY.md (워크트리 상대 경로).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@CLAUDE.local.md
@.planning/STATE.md
@.planning/quick/260529-header-unification/sketch-v2.html

# 현재 변경 대상 위치 (cwd = cha-bio-safety/)
@src/pages/DivPage.tsx
@src/pages/FloorPlanPage.tsx
@src/pages/WorkShiftPage.tsx
@src/pages/RemediationDetailPage.tsx
@src/pages/StaffManagePage.tsx

<interfaces>
<!-- sketch v2 §데스크톱 + §케이스 1/4 spec (verbatim 인용) -->
<!-- ElevatorPage 데스크톱 헤더 = h-14 (56px) — H3 (8bf69c6) 에서 검증된 표준 -->
<!-- 모바일 헤더 = h-12 px-3 (DivPage L1105) — H4 (f2ae71d) 에서 검증된 표준 -->
<!-- 백버튼 = w-7 h-7 (32px) — H1/H1.5/H2 (57bab51/260ee45/f28ae74) 에서 검증된 표준 -->
<!-- 액션 버튼 = h-7 (32px) — H3 (8bf69c6) 에서 검증된 표준 -->
<!-- ⚠ tailwind.config.js spacing override: w-8/h-8 = 48px (기본 32px 아님), w-7/h-7 = 32px (기본 28px 아님) -->
<!-- ⚠ feedback_tailwind_w8_h8_is_48px.md anchor 참조 -->

DivPage.tsx 현재 (L955~960):
```tsx
<div className="h-full flex flex-col overflow-hidden bg-surface-page">
  {/* 헤더 — 데스크톱 표준 (sketch 07a) */}
  <header className="flex-shrink-0 h-[54px] bg-surface-raised border-b border-border-default px-5 flex items-center gap-2.5">
    <span className="flex-1 text-[16px] font-bold text-text-primary">DIV 압력 관리</span>
  </header>
```
→ L957 의 `h-[54px]` 만 `h-14` 로 (그 외 className 보존).
→ L1105 모바일 헤더 (`h-12 px-3`) 이미 표준 — 무수정.

FloorPlanPage.tsx 현재 (L935~939):
```tsx
<header
  className={isDesktop
    ? "flex items-center gap-2.5 h-[54px] px-5 bg-surface-page border-b border-border-default flex-shrink-0"
    : "flex items-center gap-2.5 px-4 py-2.5 bg-surface-page border-b border-border-default flex-shrink-0"}
>
```
→ L937 의 `h-[54px]` 만 `h-14` 로.
→ L938 의 `px-4 py-2.5` 만 `h-12 px-3` 로 (모바일 표준).
→ bg-surface-page (다른 페이지의 bg-surface-raised 와 다른 페이지 컨벤션) 보존.

WorkShiftPage.tsx 현재 (L89~91):
```tsx
<header
  className={`bg-surface-raised border-b border-border-default flex items-center shrink-0 ${isDesktop ? 'h-[54px] px-5 gap-2.5' : 'h-12 px-3 gap-2'}`}
>
```
→ L90 데스크톱 분기 `h-[54px]` 만 `h-14` 로.
→ 모바일 분기 (`h-12 px-3 gap-2`) 무수정 (H4 결과 보존).

RemediationDetailPage.tsx 현재 (L271~280):
```tsx
<div className="h-12 bg-surface-raised border-b border-border-default flex items-center justify-center relative flex-shrink-0 px-3">
  <button
    aria-label="목록으로 돌아가기"
    onClick={() => navigate(-1)}
    className="absolute left-3 w-9 h-9 border-none bg-transparent cursor-pointer text-text-primary flex items-center justify-center"
  >
    <ChevronLeft size={20} />
  </button>
  <span className="text-base font-bold leading-tight text-text-primary">조치 상세</span>
</div>
```
→ L275 의 `w-9 h-9` 만 `w-7 h-7` 로 (36 → 32).
→ bg-transparent 보존 — sketch v2 §케이스 4 AFTER 의 bg-surface-sunken 적용 대신 페이지 디자인 의도 보존.
→ L271 컨테이너 (`h-12`) 이미 표준 무수정.
→ L277 ChevronLeft size={20} 보존.
→ L279 "조치 상세" title 보존.

StaffManagePage.tsx 현재 (L392~402):
```tsx
{isDesktop ? (
  <div className="desktop-header shrink-0 hidden lg:flex items-center px-6 py-3 border-b border-border-default">
    <span className="desktop-header-title flex-1 text-body-sm font-bold text-text-primary">
      직원 관리 <span className="desktop-header-count text-caption text-text-tertiary ml-2">{staffList.length}명</span>
    </span>
    <button onClick={() => setModal({ open: true, mode: 'add' })}
      className="desktop-add-btn flex items-center gap-2 h-10 px-3 rounded-sm bg-accent text-white text-label font-bold border-0 cursor-pointer">
      <UserPlus size={16} color="#fff" />
      직원 추가
    </button>
  </div>
) : (
  <div className="mobile-header shrink-0 flex lg:hidden items-center px-4 py-2">
    <span className="mobile-header-count flex-1 text-caption text-text-tertiary">{staffList.length}명</span>
  </div>
)}
```
→ L393 의 `px-6 py-3` 만 `h-14 px-5` 로 (sketch v2 §데스크톱 표준).
→ L398 의 `h-10` 만 `h-7` 로 (40 → 32, sketch v2 §케이스 1 액션 버튼).
→ desktop-header / desktop-add-btn CSS class 박제 보존.
→ L404 모바일 헤더 (백버튼 없는 작은 카운트만) 무수정.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 데스크톱 헤더 h-14 통일 + 잔여 헤더 보정 (5 파일 7 곳 정확한 변경)</name>
  <files>
    src/pages/DivPage.tsx,
    src/pages/FloorPlanPage.tsx,
    src/pages/WorkShiftPage.tsx,
    src/pages/RemediationDetailPage.tsx,
    src/pages/StaffManagePage.tsx
  </files>
  <action>
정확히 7 곳 변경. 변경 외 모든 className/JSX/onClick/타이틀 텍스트/본문/모달/사이드바 한 byte 도 건드리지 말 것.

## 변경 1 — src/pages/DivPage.tsx L957

**BEFORE (L957):**
```
        <header className="flex-shrink-0 h-[54px] bg-surface-raised border-b border-border-default px-5 flex items-center gap-2.5">
```
**AFTER (L957):**
```
        <header className="flex-shrink-0 h-14 bg-surface-raised border-b border-border-default px-5 flex items-center gap-2.5">
```
→ 변경: `h-[54px]` → `h-14` (54px → 56px = +2px, ElevatorPage 데스크톱 표준 일관).
→ 보존: flex-shrink-0, bg-surface-raised, border-b, border-border-default, px-5, flex, items-center, gap-2.5.
→ L958 "DIV 압력 관리" title (`<span className="flex-1 text-[16px] font-bold text-text-primary">DIV 압력 관리</span>`) 무수정.
→ L1105 모바일 헤더 (`h-12 px-3`) 이미 표준 — 무수정.

## 변경 2 — src/pages/FloorPlanPage.tsx L937

**BEFORE (L937):**
```
          ? "flex items-center gap-2.5 h-[54px] px-5 bg-surface-page border-b border-border-default flex-shrink-0"
```
**AFTER (L937):**
```
          ? "flex items-center gap-2.5 h-14 px-5 bg-surface-page border-b border-border-default flex-shrink-0"
```
→ 변경: `h-[54px]` → `h-14`.
→ 보존: flex, items-center, gap-2.5, px-5, bg-surface-page (페이지 컨벤션 — 다른 페이지의 bg-surface-raised 와 다름. 변경 X), border-b, border-border-default, flex-shrink-0.

## 변경 3 — src/pages/FloorPlanPage.tsx L938

**BEFORE (L938):**
```
          : "flex items-center gap-2.5 px-4 py-2.5 bg-surface-page border-b border-border-default flex-shrink-0"}
```
**AFTER (L938):**
```
          : "flex items-center gap-2.5 h-12 px-3 bg-surface-page border-b border-border-default flex-shrink-0"}
```
→ 변경: `px-4 py-2.5` → `h-12 px-3` (모바일 헤더 표준 적용).
→ 보존: flex, items-center, gap-2.5, bg-surface-page, border-b, border-border-default, flex-shrink-0.
→ L943 모바일 back 이미 `w-7 h-7` (32) 무수정.
→ L945 ChevronLeft size={14} 무수정.
→ L951+ 우측 토글 이미 `h-7` (32) 무수정.

## 변경 4 — src/pages/WorkShiftPage.tsx L90

**BEFORE (L90):**
```
        className={`bg-surface-raised border-b border-border-default flex items-center shrink-0 ${isDesktop ? 'h-[54px] px-5 gap-2.5' : 'h-12 px-3 gap-2'}`}
```
**AFTER (L90):**
```
        className={`bg-surface-raised border-b border-border-default flex items-center shrink-0 ${isDesktop ? 'h-14 px-5 gap-2.5' : 'h-12 px-3 gap-2'}`}
```
→ 변경: 데스크톱 분기 `h-[54px]` → `h-14`.
→ 모바일 분기 (`h-12 px-3 gap-2`) 무수정 (H4 결과 보존).
→ template literal 외 모든 prefix className 보존.

## 변경 5 — src/pages/RemediationDetailPage.tsx L275

**BEFORE (L275):**
```
          className="absolute left-3 w-9 h-9 border-none bg-transparent cursor-pointer text-text-primary flex items-center justify-center"
```
**AFTER (L275):**
```
          className="absolute left-3 w-7 h-7 border-none bg-transparent cursor-pointer text-text-primary flex items-center justify-center"
```
→ 변경: `w-9 h-9` → `w-7 h-7` (36 → 32).
→ 보존: absolute, left-3, border-none, bg-transparent (페이지 디자인 의도 — sketch v2 §케이스 4 AFTER bg-surface-sunken 대신 transparent + absolute 페이지 컨벤션 보존), cursor-pointer, text-text-primary, flex, items-center, justify-center.
→ L271 컨테이너 (`h-12 bg-surface-raised ... px-3`) 이미 표준 무수정.
→ L272~274 aria-label/onClick 무수정.
→ L277 ChevronLeft size={20} 무수정.
→ L279 "조치 상세" title 무수정.

## 변경 6 — src/pages/StaffManagePage.tsx L393

**BEFORE (L393):**
```
        <div className="desktop-header shrink-0 hidden lg:flex items-center px-6 py-3 border-b border-border-default">
```
**AFTER (L393):**
```
        <div className="desktop-header shrink-0 hidden lg:flex items-center h-14 px-5 border-b border-border-default">
```
→ 변경: `px-6 py-3` → `h-14 px-5` (sketch v2 §데스크톱 표준).
→ 보존: desktop-header (CSS class 박제), shrink-0, hidden, lg:flex, items-center, border-b, border-border-default.

## 변경 7 — src/pages/StaffManagePage.tsx L398

**BEFORE (L398):**
```
            className="desktop-add-btn flex items-center gap-2 h-10 px-3 rounded-sm bg-accent text-white text-label font-bold border-0 cursor-pointer">
```
**AFTER (L398):**
```
            className="desktop-add-btn flex items-center gap-2 h-7 px-3 rounded-sm bg-accent text-white text-label font-bold border-0 cursor-pointer">
```
→ 변경: `h-10` → `h-7` (40 → 32, sketch v2 §케이스 1 액션 버튼 spec).
→ 보존: desktop-add-btn (CSS class 박제), flex, items-center, gap-2, px-3, rounded-sm, bg-accent, text-white, text-label, font-bold, border-0, cursor-pointer.
→ L394~396 title (`직원 관리 <span>{staffList.length}명</span>`) 무수정.
→ L397 onClick (`setModal({ open: true, mode: 'add' })`) 무수정.
→ L399 UserPlus size={16} color="#fff" 무수정.
→ L400 "직원 추가" 텍스트 무수정.
→ L404 모바일 헤더 (백버튼 없는 작은 카운트만) 무수정.

## 보존 (한 byte 변경 0 — 자체 검증 필수)

- DivPage L1105 모바일 헤더 (이미 표준 h-12).
- FloorPlanPage L943 모바일 back (이미 w-7 h-7).
- FloorPlanPage L945 ChevronLeft size=14.
- FloorPlanPage L951+ 우측 토글 (이미 h-7).
- WorkShiftPage 모바일 분기 (`h-12 px-3 gap-2`).
- WorkShiftPage L97 ChevronLeft size={15} className.
- RemediationDetailPage L277 ChevronLeft size={20}.
- RemediationDetailPage L271 컨테이너.
- StaffManagePage L404 모바일 헤더.
- 데스크톱 분기의 다른 className (h-[54px] / px-6 py-3 / h-10 / w-9 h-9 만 명시된 대로 변경).
- 그 외 모든 본문/모달/사이드바/모바일/데스크톱 코드.

## 자체 검증 (write 후 grep 으로 다음 모두 확인)

1. `grep -c 'h-\[54px\]' src/pages/DivPage.tsx src/pages/FloorPlanPage.tsx src/pages/WorkShiftPage.tsx` → 데스크톱 헤더에 더이상 없어야 함 (다른 곳에 h-[54px] 잔존 가능성 0 — 위 3 파일 모두 헤더 1 곳뿐).
2. `grep -n 'h-14' src/pages/DivPage.tsx src/pages/FloorPlanPage.tsx src/pages/WorkShiftPage.tsx` → 각 파일 데스크톱 헤더 라인 등장.
3. `grep -n 'w-9 h-9' src/pages/RemediationDetailPage.tsx` → 0 hit.
4. `grep -n 'w-7 h-7' src/pages/RemediationDetailPage.tsx` → L275 hit.
5. `grep -n 'px-6 py-3 border-b' src/pages/StaffManagePage.tsx` → 0 hit (L393 만 매치되던 패턴).
6. `grep -n 'h-14 px-5 border-b' src/pages/StaffManagePage.tsx` → L393 hit.
7. `grep -n 'h-10 px-3 rounded-sm bg-accent' src/pages/StaffManagePage.tsx` → 0 hit.
8. `grep -n 'h-7 px-3 rounded-sm bg-accent' src/pages/StaffManagePage.tsx` → L398 hit.
9. FloorPlanPage 모바일 분기: `grep -n 'h-12 px-3 bg-surface-page' src/pages/FloorPlanPage.tsx` → L938 hit.

## 빌드 검증

`cd /Users/jykevin/Documents/cbc7119-design/cha-bio-safety && npm run build` 실행.
- vite build PASS / TypeScript 0 error 필수.
- 빌드 실패 시 변경 위치 재확인 (offset 1 오차 가능성).
  </action>
  <verify>
    <automated>cd /Users/jykevin/Documents/cbc7119-design/cha-bio-safety && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>
- DivPage L957 = `h-14` (h-[54px] 사라짐), L958 title 보존
- FloorPlanPage L937 = `h-14`, L938 = `h-12 px-3` (px-4 py-2.5 사라짐), bg-surface-page 보존
- WorkShiftPage L90 데스크톱 분기 = `h-14`, 모바일 분기 `h-12 px-3 gap-2` 보존
- RemediationDetailPage L275 = `w-7 h-7` (w-9 h-9 사라짐), bg-transparent 보존, ChevronLeft size=20 보존
- StaffManagePage L393 = `h-14 px-5` (px-6 py-3 사라짐), desktop-header class 보존
- StaffManagePage L398 = `h-7` (h-10 사라짐), desktop-add-btn class 보존, UserPlus size=16 보존
- 보존 영역 9 항목 모두 한 byte 변경 0
- `npm run build` PASS / TypeScript 0 error
- 한 atomic commit 으로 묶음. 커밋 메시지:
  `feat(redesign/32-header-unification-h5): 데스크톱 헤더 h-14 통일 + RemediationDetail back 36→32 + StaffManage 데스크 표준 (7 곳)`
- wrangler 명령 금지 / `npm run deploy` 금지 — main push 자동 cbc7119-preview 배포만 (CLAUDE.local.md 룰)
  </done>
</task>

</tasks>

<verification>
1. **변경 위치 정확성** (grep 9 항목 위와 동일).
2. **빌드 PASS** — `npm run build` 가 vite build + TypeScript check 모두 통과.
3. **보존 영역 0 byte 변경** — `git diff --stat` 확인 시 5 파일만 hit, 라인 변경량 최소 (라인당 단어 1~2 개만).
4. **commit atomic 단일** — `git log --oneline -1` 가 위 메시지 1 줄.
</verification>

<success_criteria>
- 5 파일 7 라인 정확히 명시된 byte 만 변경.
- 보존 9 영역 한 byte 변경 0 (grep 검증).
- vite build PASS + TypeScript 0 error.
- atomic commit 1 개 (위 메시지).
- wrangler / npm run deploy 명령 0회 호출.
- main push 는 사용자 명시 컨펌 후에만 (redesign 트랙 = 자동 cbc7119-preview 만 배포 대상).
</success_criteria>

<output>
After completion, create:
`.planning/quick/260530-74s-redesign-32-header-unification-h5-div-fl/260530-74s-SUMMARY.md`

SUMMARY.md 의 모든 파일/path 참조는 워크트리 상대 경로 (e.g., `src/pages/DivPage.tsx`, `.planning/quick/260530-74s-...`) 사용.
SUMMARY.md 작성 위치 자체도 cha-bio-safety/ 워크트리 안 (절대 cbc7119-design/.planning/ 에 만들지 말 것 — gsd-executor cwd 혼란 박제: feedback `0k2 → 3y5` SUMMARY 잘못된 위치 사고).
</output>
