---
phase: 260528-01h-legalfindingspage-legalfindingdetailpage
plan: 01
subsystem: redesign/phase-b-sweep
status: complete
tags: [legal-findings, inline-style-to-tailwind, no-op-refactor, phase-b-followup]
requires:
  - 260527-wdc-legalpage-phase-b-inline-style-141-tailw 완료 (184e548 / Phase A 47b9088)
provides:
  - LegalFindingsPage.tsx Phase B 완료 (29 → 2 잔존 = 색변수 N)
  - LegalFindingDetailPage.tsx Phase B 완료 (35 → 4 잔존 = 색변수 N)
affects:
  - src/pages/LegalFindingsPage.tsx
  - src/pages/LegalFindingDetailPage.tsx
tech-stack:
  added: []
  patterns:
    - "옵션 X (정확값 arbitrary) — 스케일 외 값은 모두 [Npx] 명시 (시각 0 byte 보장)"
    - "옵션 P (leading 명시 보존) — leading-[1.5] textarea 잔존"
    - "옵션 M (className conditional) — 동적 분기는 template literal로 className 안 흡수"
    - "옵션 N (색 변수만) — linear-gradient(135deg, ...) / rgba(0,0,0,0.4) / fontFamily: 'inherit' 6건 style 잔존"
key-files:
  created:
    - .planning/quick/260528-01h-legalfindingspage-legalfindingdetailpage/260528-01h-SUMMARY.md
  modified:
    - src/pages/LegalFindingsPage.tsx
    - src/pages/LegalFindingDetailPage.tsx
decisions:
  - "wdc Phase B 옵션 X+P+M+색변수N 승계 — 사용자 재컨펌 불필요 (직전 quick 확정 + 6 패턴 sketch 매핑)"
metrics:
  duration: "약 20분 (Task 1 atomic 단일)"
  completed-date: 2026-05-28
  tasks-completed: "1/1"
  files-modified: 2
  lines-changed: "68 ins / 124 del (net -56 lines)"
---

# Phase 260528-01h Plan 01: Sibling Legal pages Phase B Summary

LegalFindingsPage.tsx + LegalFindingDetailPage.tsx 두 sibling 페이지의 정적 inline style 64곳을 wdc Phase B 결정 옵션 X+P+M+색변수N 그대로 적용해 tailwind className 으로 일괄 변환. 시각 결과 0 byte 변경 (no-op refactor). Phase A 결과 (Lucide / 색 토큰 -bar / emoji 0) 및 비즈니스 로직 모두 보존.

## User Decisions (승계 — wdc 재확인 불필요)

| ID  | 선택                                                          | 출처                      |
| --- | ------------------------------------------------------------- | ------------------------- |
| (b) | **옵션 X** — 정확값 arbitrary `[Npx]` (시각 0 byte)            | wdc Phase B Task 2 결정    |
| (c) | **옵션 P** — `leading-[1.5]` 명시 보존 (text-* 토큰과 중복이라도) | wdc Phase B Task 2 결정    |
| (d) | **옵션 M + 색 변수만 N** — template literal conditional 우선   | wdc Phase B Task 2 결정    |

## Before / After 카운트

| Metric (`style={{` count)           | Before | After   | Diff               |
| ----------------------------------- | ------ | ------- | ------------------ |
| LegalFindingsPage.tsx               | **29** | **2**   | -27 (-93.1%)        |
| LegalFindingDetailPage.tsx          | **35** | **4**   | -31 (-88.6%)        |
| **합계**                             | **64** | **6**   | **-58 (-90.6%)**    |

총 변경: 2 files, 68 ins / 124 del, net -56 lines.

## 패턴별 변환 통계 (sketch §2-P1~P6)

| Pattern                          | Findings | Detail   | 비고                                              |
| -------------------------------- | -------- | -------- | ------------------------------------------------- |
| §2-P1 Padding 상수               | ~7       | ~7       | `px-N py-N` (scale) / `px-3 py-[6px]` (arbitrary) |
| §2-P2 Flex layout                | ~10      | ~12      | `flex / flex-col / gap-N / items-X / justify-X`   |
| §2-P3 Sizing (옵션 X)            | ~5       | ~8       | `w-[44px] h-[44px]`, `w-[72px] h-[72px]`, `h-[36px]`, `text-[13px]/[14px]/[22px]` |
| §2-P4 leading 명시 (옵션 P)      | 0        | 1        | `leading-[1.5]` textarea (Detail L230)             |
| §2-P5 Button reset               | ~5       | ~3       | `bg-transparent border-0 cursor-pointer`           |
| §2-P6 Dynamic (옵션 M / N)       | ~5 / 1   | ~5 / 3   | className conditional / 색 변수만 style 잔존      |

## 잔존 inline style 6곳 (옵션 N — 색 변수 / CSS only)

| 파일                          | Line  | 잔존 이유                                                                |
| ----------------------------- | ----- | ------------------------------------------------------------------------ |
| LegalFindingsPage.tsx         | 273   | `background: 'linear-gradient(135deg, #1d4ed8, #0ea5e9)'` (등록 버튼)     |
| LegalFindingsPage.tsx         | 317   | `{ ...SKELETON_STYLE, display: 'none' }` (박제, 외부 사용처 없음 — display:none) |
| LegalFindingDetailPage.tsx    | 231   | `fontFamily: 'inherit'` (textarea 한글 폰트 상속)                         |
| LegalFindingDetailPage.tsx    | 250   | `background: 'rgba(0,0,0,0.4)'` (업로드 중 오버레이)                       |
| LegalFindingDetailPage.tsx    | 272   | `background: 'linear-gradient(135deg, #1d4ed8, #0ea5e9)'` (데스크톱 조치 완료 버튼) |
| LegalFindingDetailPage.tsx    | 306   | `transition: 'opacity 0.15s', background: 'linear-gradient(135deg, #1d4ed8, #0ea5e9)'` (모바일 조치 완료 버튼 — tailwind `transition-opacity` timing-function이 CSS 기본 `ease`와 달라서 시각 0 byte 위해 style 잔존) |

## Verification Results (모든 게이트 PASS)

| Check                                                                                              | Result        | 비고                                                       |
| -------------------------------------------------------------------------------------------------- | ------------- | ---------------------------------------------------------- |
| `grep -c 'style={{' LegalFindingsPage.tsx`                                                          | **2**         | 29 → 2 (-27)                                               |
| `grep -c 'style={{' LegalFindingDetailPage.tsx`                                                     | **4**         | 35 → 4 (-31)                                               |
| 비즈 anchor count diff (10종 × 2 파일)                                                              | **IDENTICAL** | onClick / useMutation / useQuery / useEffect / useRef / useNavigate / useState / legalApi / inspectionApi / fetch 양쪽 파일 0 차이 |
| onClick handler bodies precise diff (Findings 9건 / Detail 5건)                                     | **IDENTICAL** | `grep -oE 'onClick=\{[^}]+\}' \| sort \| uniq` 양쪽 파일 empty diff |
| `grep -cE '✓\|✗\|🔒\|💾\|🔥\|⏰\|📋\|✅\|⚠️\|❌\|🔧\|🚨\|🔍\|🧯\|📊'` 양쪽                            | **0**         | Phase A §7.1 결과 보존 (✕ U+2715 는 watched set 외 — 원본 보존)   |
| 비표준 색 토큰 grep (`bg-warning[^-]\|border-safe[^-]\|border-warning[^-]\|border-danger[^-]`) 양쪽 | **0**         | Phase A §2.3 + audit followup 결과 보존                     |
| TypeScript `tsc --noEmit` 전체 에러                                                                  | **0**         | 신규 에러 0                                                  |
| 변경 파일 범위 (`.tsx` + `.planning/` 외)                                                              | **0**         | 두 .tsx 만                                                  |
| post-commit deletions (`git diff --diff-filter=D HEAD~1 HEAD`)                                     | **none**      | 의도하지 않은 삭제 없음                                       |

### 비즈 anchor identity 상세

```
=== LegalFindingsPage.tsx (양쪽 동일) ===
  onClick=\{[^}]+\} : 9
  useMutation\( : 0
  useQuery\( : 2
  useEffect\( : 0
  useRef\( : 0
  useNavigate\( : 1
  useState\( : 3
  legalApi\. : 5
  inspectionApi\. : 0
  fetch\( : 3

=== LegalFindingDetailPage.tsx (양쪽 동일) ===
  onClick=\{[^}]+\} : 7
  useMutation\( : 1
  useQuery\( : 1
  useEffect\( : 0
  useRef\( : 0
  useNavigate\( : 1
  useState\( : 2
  legalApi\. : 2
  inspectionApi\. : 0
  fetch\( : 2
```

diff before↔after = 단 2줄 (`style count:` 카운트 변화) 외 모두 identical.

## Memory anchor 4건 적용 확인

| Anchor                                          | 적용 사례                                                                                                  |
| ----------------------------------------------- | ---------------------------------------------------------------------------------------------------------- |
| `feedback_tailwind_w8_h8_is_48px.md`            | `h-8` (config override 48px) — addButton mobile, 데스크톱 조치 완료, 모바일 조치 완료. `w-5 h-5` (override 20px) — 사진 제거 ✕ 버튼. 32/48 함정 회피하면서 헤더 높이 (height: 48) 와 일관 적용. |
| `feedback_tailwind_token_class_pattern.md`      | `text-danger` / `bg-danger-bg` / `bg-safe-bg` / `text-safe` / `bg-danger-bar` — status- prefix 없는 short form 보존. `<Camera size={22} />` / `<Download size={18} />` / `<ChevronLeft size={20} />` lucide size prop 보존. |
| `feedback_text_caption_leading_none.md`         | text-caption 컨테이너 안 `leading-none` 명시 보존 — 미조치/완료 배지 (Findings L248), 수정/삭제 버튼 (Findings L257/261), section header (Detail L36), 사진 첨부 버튼 (Detail L258) 등 다수. |
| `project_redesign_16_workshift_status.md`       | `text-[13px]`, `text-[14px]`, `text-[22px]`, `w-[44px]`, `w-[64px]`, `w-[72px]`, `h-[36px]`, `rounded-[10px]` 등 token 외 사이즈 arbitrary fallback 일관 사용. |

## Phase A 보존 확인 (별도 점검)

| Phase A 항목                                                                  | LegalFindingsPage | LegalFindingDetailPage | 비고                                     |
| ----------------------------------------------------------------------------- | ----------------- | ---------------------- | ---------------------------------------- |
| Lucide import (line 5)                                                        | OK                | OK                     | 0 byte change                            |
| `<ChevronLeft>` / `<Loader2>` / `<Download>` / `<Camera>` JSX                  | OK                | OK                     | 모든 size prop 동일                       |
| `bg-danger-bg text-danger` / `bg-safe-bg text-safe` (status badge)             | OK (L248)         | n/a                    |                                          |
| `border-danger-bar` / `border-safe-bar` (left bar)                            | OK (L243)         | n/a                    |                                          |
| `bg-danger-bar` (사진 제거 ✕ 버튼)                                              | n/a               | OK (L245)              | Phase A `bg-danger-bar` 일관 적용         |
| Emoji 0 (watched set)                                                         | OK                | OK                     | grep -cE 0                              |
| 비표준 색 토큰 0 (border-warning, border-safe short form 부재)                  | OK                | OK                     | grep -cE 0                              |

## 비즈니스 로직 0 byte 확인 (precise)

원본 9 + 5 = 14건 onClick handler 본체를 `grep -oE 'onClick=\{[^}]+\}' | sort | uniq` 으로 추출 후 diff:
- LegalFindingsPage: 9건 모두 IDENTICAL (navigate / setShowSheet / reportInputRef / handleSaveResult / handleReportUpload / handleZipDownload / handleDeleteFinding / setEditingFinding / window.open)
- LegalFindingDetailPage: 5건 모두 IDENTICAL (navigate / handleDownload / handleResolve / openPicker / removeSlot)

또한 useState / useRef / useEffect / useMutation / useQuery / useNavigate / legalApi / fetch 호출 카운트도 양쪽 파일 모두 byte-identical (위 표).

## Commits

| Hash    | Subject                                                                                                |
| ------- | ------------------------------------------------------------------------------------------------------ |
| 894c9d0 | `feat(260528-01h-01): sibling Legal pages Phase B — inline style 64곳 → tailwind class (옵션 X+P+M, 색변수 N)` |

## Deviations from Plan

### None — plan executed exactly as written.

옵션 X+P+M+색변수N (wdc 승계) 그대로 적용. scope expansion / 다른 파일 변경 / 자동 추가 기능 / 자동 fix 모두 0.

### 한 가지 보완 결정 (단순화 명시)

`transition: 'opacity 0.15s'` (LegalFindingDetailPage L319 원본) 을 tailwind `transition-opacity duration-150` 로 처음에 변환했으나 tailwind 의 default `transition-timing-function: cubic-bezier(.4,0,.2,1)` 이 CSS 기본 `ease` (`cubic-bezier(.25,.1,.25,1)`) 와 미세 차이 — 시각 0 byte 보장 위해 style 잔존으로 환원 (L306). 옵션 X+N 의 정확값 보존 원칙 그대로.

## 배포 계획

- **이 워크트리 (디자인 트랙):** main 머지 → cbc7119-preview.pages.dev 자동 배포 (GitHub Actions). 시각 0 byte 보장이므로 preview 검증은 페이지 정상 렌더 + 빌드 통과 정도로 충분.
- **production cherry-pick 후보 묶음:**
  - 47b9088 (Phase A 색 토큰 / emoji sweep) 
  - 184e548 (wdc Phase B LegalPage)
  - **894c9d0 (이번 작업 — sibling Legal pages Phase B)**
- **이 워크트리에서는 wrangler 명령 금지 / production deploy 금지** — `.claude/settings.local.json` deny 룰 준수.

## Next Steps (out of scope)

- **production cherry-pick** (사용자 컨펌 후): 위 3개 묶음 — inline style 변환 후 시각 0 byte 이므로 충돌 위험 낮음. 단 LegalFindingDetailPage 는 메모리 anchor `project_08_finding_detail_deprecated.md` 와 별개 (해당 anchor 는 ElevatorFindingDetailPage, 이번은 LegalFindingDetailPage) — sweep 가치 있음.
- **다른 페이지 inline style sweep** — InspectionPage / ElevatorPage 등 대량 inline style 페이지에 동일 옵션 X/P/M 룰 적용 (별도 quick task).
- **잔존 6곳 후속 정리** (선택사항):
  - linear-gradient 3곳: tailwind `from-* via-* to-*` 변환 가능하나 #1d4ed8 / #0ea5e9 토큰 없어 arbitrary 필요 → 현 style 잔존 권장
  - fontFamily inherit / rgba background / opacity transition: tailwind plugin / arbitrary 가능하나 ROI 낮음

## Self-Check: PASSED

**Files:**
- FOUND: cha-bio-safety/src/pages/LegalFindingsPage.tsx (modified, 364 lines)
- FOUND: cha-bio-safety/src/pages/LegalFindingDetailPage.tsx (modified, 314 lines)
- FOUND: cha-bio-safety/.planning/quick/260528-01h-legalfindingspage-legalfindingdetailpage/260528-01h-SUMMARY.md (this file)

**Commits:**
- FOUND: 894c9d0 (Task 1 atomic — sibling Legal pages Phase B)
