---
phase: 260528-01h-legalfindingspage-legalfindingdetailpage
plan: 01
type: execute
wave: 1
depends_on: [260527-wdc-legalpage-phase-b-inline-style-141-tailw]
files_modified:
  - src/pages/LegalFindingsPage.tsx
  - src/pages/LegalFindingDetailPage.tsx
autonomous: true
requirements:
  - QUICK-260528-01h-SIBLING-LEGAL-PHASE-B
---

<objective>
**Phase B sweep — sibling Legal pages.** 직전 quick `260527-wdc` 의 LegalPage.tsx Phase B (141 → 6, 옵션 X+P+M+색변수N) 완결 후, 동일 라우팅 트리의 sibling 두 페이지에 같은 룰 적용.

- `src/pages/LegalFindingsPage.tsx` (29 inline style / 407 lines, `/legal/:id` 라우트)
- `src/pages/LegalFindingDetailPage.tsx` (35 inline style / 327 lines, `/legal/:id/finding/:fid` 라우트)

**시각 결과 0 byte 변경 (no-op refactor).** 비즈니스 로직 / 함수 시그니처 / state shape / Phase A 결과 (Lucide / 색 토큰 -bar / emoji 0) 보존.

Sketch / checkpoint 생략 — 옵션 (X+P+M+색변수N) 이 직전 wdc 에서 사용자 확정 + 6 패턴 매핑 (P1~P6) 이 wdc sketch (`sketch/legalpage-phase-b-tailwind.html`) 에 정리됨.
</objective>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/quick/260527-wdc-legalpage-phase-b-inline-style-141-tailw/260527-wdc-PLAN.md
@.planning/quick/260527-wdc-legalpage-phase-b-inline-style-141-tailw/260527-wdc-SUMMARY.md
@.planning/quick/260527-wdc-legalpage-phase-b-inline-style-141-tailw/sketch/legalpage-phase-b-tailwind.html
@tailwind.config.js
@src/pages/LegalFindingsPage.tsx
@src/pages/LegalFindingDetailPage.tsx

## Locked decisions (from wdc Phase B precedent)

| ID | 결정 | 적용 |
|----|------|------|
| (b) sizing | **옵션 X** — 정확값 arbitrary | 스케일 외 값 (28/36/44 등) 모두 `[Npx]`. 시각 0 byte. |
| (c) lineHeight | **옵션 P** — leading 명시 보존 | `lineHeight: 1.5` 같은 inline 명시는 `leading-[1.5]` 로 변환. text-* 토큰과 중복이라도 보존. |
| (d) dynamic | **옵션 M + 색변수만 N** | template literal conditional 우선, 색 변수 (`background: var(...)` / `borderBottom: var(--accent)`) 와 `linear-gradient` / `fontFamily: inherit` 등 tailwind 표현 불가 케이스만 style 잔존. |

## Pattern matrix (wdc sketch §2 그대로 적용)

- **§2-P1 Padding** — `padding: '2px 8px'` → `px-2 py-[2px]`
- **§2-P2 Flex layout** — `display: 'flex', gap: 12, alignItems: 'flex-start'` → `flex gap-3 items-start`
- **§2-P3 Sizing** — 옵션 X: 스케일 외 모두 arbitrary `[Npx]`
- **§2-P4 flex-1 + leading** — 옵션 P: `leading-[1.5]` 명시 보존
- **§2-P5 Button reset** — `background: 'none', border: 'none', cursor: 'pointer'` → `bg-transparent border-0 cursor-pointer`
- **§2-P6 Dynamic** — 옵션 M: template literal conditional

## tailwind.config.js spacing scale (메모리 anchor `feedback_tailwind_w8_h8_is_48px.md`)

- w/h/p/m/gap -1=4, -2=8, -3=12, -4=16, -5=20, -6=24, **-7=32**, **-8=48**
- 기본 tailwind (override 안 된 키): w-9=36, w-11=44, w-12=48, w-16=64

⚠️ **28px → `w-[28px]` arbitrary (스케일 외)**
⚠️ **36px → `w-9` (default 36px) 또는 `w-[36px]` (옵션 X 룰: `w-[36px]` 권장)**
⚠️ **44px → `w-11` (default 44px) 또는 `w-[44px]` (옵션 X 룰: `w-[44px]` 권장)**
⚠️ **48px → `w-8` (config override) 또는 `w-12` (default) 또는 `w-[48px]` — wdc 사례 따라 arbitrary 권장**

## Phase A 결과 보존 (양쪽 파일)

- Lucide import 줄 (있다면 그대로)
- `<Check>`, `<X>`, `<Lock>`, `<Save>` JSX (있다면)
- `bg-warning-bg`, `text-warning`, `bg-safe-bg`, `text-safe`, `bg-danger-bg`, `text-danger`
- `border-safe-bar`, `border-warning-bar`, `border-danger-bar`
- emoji 0 (✓ ✗ 🔒 💾 🔥 ⏰ 📋 ✅ ⚠️ ❌ 🔧 🚨 🔍 🧯 📊 grep = 0)

## 비즈니스 로직 절대 변경 금지

- `onClick`, `onChange`, `onSubmit`, `onKeyDown` 핸들러 본체 0 byte
- `useMutation`, `useQuery`, `useState`, `useRef`, `useEffect`, `useNavigate`, `useParams` 호출 0 byte
- `legalApi.*`, `inspectionApi.*`, `fetch(` 호출 0 byte
- 함수 시그니처 / props 타입 / state shape 0 byte
- JSX element 추가/삭제 0 byte (only className/style attribute 수정)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bulk apply — sibling Legal pages inline style → tailwind className</name>
  <files>
    src/pages/LegalFindingsPage.tsx
    src/pages/LegalFindingDetailPage.tsx
  </files>
  <action>
두 파일의 정적 inline style 모두 tailwind className 으로 변환. 옵션 X+P+M+색변수N 적용.

### Step-by-step

1. **Before snapshot** (from `cha-bio-safety/`):
   ```bash
   B1=$(grep -c 'style={{' src/pages/LegalFindingsPage.tsx)  # 29
   B2=$(grep -c 'style={{' src/pages/LegalFindingDetailPage.tsx)  # 35
   echo "Before: Findings=$B1 Detail=$B2 total=$((B1+B2))"

   # 비즈 anchor counts (precise — handler/hook 호출 token 단위)
   for ANCHOR in 'useMutation\(' 'useQuery\(' 'useEffect\(' 'useRef\(' 'useNavigate\(' 'useState\(' 'legalApi\.' 'inspectionApi\.' 'fetch\('; do
     F=$(grep -cE "$ANCHOR" src/pages/LegalFindingsPage.tsx)
     D=$(grep -cE "$ANCHOR" src/pages/LegalFindingDetailPage.tsx)
     echo "$ANCHOR : F=$F D=$D"
   done > /tmp/01h-biz-before.txt
   cat /tmp/01h-biz-before.txt

   # onClick handler bodies (precise extraction — wdc 검증 패턴)
   grep -oE 'onClick=\{[^}]+\}' src/pages/LegalFindingsPage.tsx | sort | uniq > /tmp/01h-findings-onclick-before.txt
   grep -oE 'onClick=\{[^}]+\}' src/pages/LegalFindingDetailPage.tsx | sort | uniq > /tmp/01h-detail-onclick-before.txt
   ```

2. **Edit LegalFindingsPage.tsx** — 29 inline style 변환. Read file once, identify each `style={{`, apply pattern P1~P6 per 옵션 X+P+M.

3. **Edit LegalFindingDetailPage.tsx** — 35 inline style 변환. 동일 룰.

4. **className 합병 룰** (양쪽 파일):
   - 기존 className 있으면 같은 attribute 안에 추가
   - 없으면 새로 생성
   - 동적: `className={\`정적 ${condition ? 'A' : 'B'}\`}` template literal

5. **Phase A 결과 + 비즈니스 로직 절대 변경 금지** (context 참조)

6. **Memory anchors:**
   - `feedback_tailwind_w8_h8_is_48px.md` — w-7=32, w-8=48
   - `feedback_tailwind_token_class_pattern.md` — status- prefix 없음
   - `feedback_text_caption_leading_none.md` — 작은 컨테이너 안 leading-none
   - `project_redesign_16_workshift_status.md` — tokens.css 불일치 시 `text-[#hex]` fallback

### After verification (executor 자체 검증, from `cha-bio-safety/`)

```bash
# 1. inline style count drop
A1=$(grep -c 'style={{' src/pages/LegalFindingsPage.tsx)
A2=$(grep -c 'style={{' src/pages/LegalFindingDetailPage.tsx)
echo "After: Findings=$A1 Detail=$A2 total=$((A1+A2)) (was 64)"
# 목표: 잔존 = 색변수 케이스만 (Findings 약 0-5, Detail 약 0-5, total < 15)

# 2. 비즈 anchor counts identical (precise — wdc 패턴)
for ANCHOR in 'useMutation\(' 'useQuery\(' 'useEffect\(' 'useRef\(' 'useNavigate\(' 'useState\(' 'legalApi\.' 'inspectionApi\.' 'fetch\('; do
  F=$(grep -cE "$ANCHOR" src/pages/LegalFindingsPage.tsx)
  D=$(grep -cE "$ANCHOR" src/pages/LegalFindingDetailPage.tsx)
  echo "$ANCHOR : F=$F D=$D"
done > /tmp/01h-biz-after.txt
diff /tmp/01h-biz-before.txt /tmp/01h-biz-after.txt
# MUST be empty (identical)

# 3. onClick handlers identical
grep -oE 'onClick=\{[^}]+\}' src/pages/LegalFindingsPage.tsx | sort | uniq > /tmp/01h-findings-onclick-after.txt
grep -oE 'onClick=\{[^}]+\}' src/pages/LegalFindingDetailPage.tsx | sort | uniq > /tmp/01h-detail-onclick-after.txt
diff /tmp/01h-findings-onclick-before.txt /tmp/01h-findings-onclick-after.txt
diff /tmp/01h-detail-onclick-before.txt /tmp/01h-detail-onclick-after.txt
# Both MUST be empty (handlers identical)

# 4. emoji 0 (Phase A §7.1 보존)
grep -cE '✓|✗|🔒|💾|🔥|⏰|📋|✅|⚠️|❌|🔧|🚨|🔍|🧯|📊' src/pages/LegalFindingsPage.tsx
grep -cE '✓|✗|🔒|💾|🔥|⏰|📋|✅|⚠️|❌|🔧|🚨|🔍|🧯|📊' src/pages/LegalFindingDetailPage.tsx
# Both MUST = 0

# 5. 비표준 색 토큰 0 (Phase A §2.3 + audit followup 보존)
grep -cE 'bg-warning[^-]|border-safe[^-]|border-warning[^-]|border-danger[^-]' src/pages/LegalFindingsPage.tsx
grep -cE 'bg-warning[^-]|border-safe[^-]|border-warning[^-]|border-danger[^-]' src/pages/LegalFindingDetailPage.tsx
# Both MUST = 0

# 6. TypeScript 컴파일
./node_modules/.bin/tsc --noEmit 2>&1 | grep -E "src/pages/LegalFindings(|Detail)Page.tsx" | grep "error TS" | wc -l
# MUST = 0

# 7. 변경 파일 범위
cd .. && git diff --name-only HEAD | grep -v "cha-bio-safety/src/pages/LegalFindings.*Page.tsx" | grep -v ".planning/" | wc -l
# MUST = 0 (두 파일 외 변경 0)
```

**If any gate fails, STOP and report — do NOT commit.**

### Commit (atomic single commit)

```bash
cd /Users/jykevin/Documents/cbc7119-design
git add cha-bio-safety/src/pages/LegalFindingsPage.tsx cha-bio-safety/src/pages/LegalFindingDetailPage.tsx
git commit --no-verify -m "$(cat <<'EOF'
feat(260528-01h-01): sibling Legal pages Phase B — inline style 64곳 → tailwind class (옵션 X+P+M, 색변수 N)

LegalFindingsPage (29) + LegalFindingDetailPage (35) — wdc Phase B 결정 그대로 적용.
시각 결과 0 byte 변경 (no-op refactor). Phase A 결과 (Lucide + 색 토큰 -bar + emoji 0) 보존.
비즈니스 로직 0 byte (onClick handlers + hooks + legalApi + fetch 모두 identical, precise grep 검증).
EOF
)"
```

### SUMMARY.md

Write `cha-bio-safety/.planning/quick/260528-01h-legalfindingspage-legalfindingdetailpage/260528-01h-SUMMARY.md`:

- frontmatter: phase / plan / subsystem / status: complete / tech-stack / key-files (modified) / decisions (옵션 X+P+M 승계) / metrics
- 사용자 선택 옵션 (X+P+M+색변수N) 명시 — wdc 승계
- Before/After grep count 표 (양쪽 파일 + 합계)
- 패턴별 변환 통계 (P1/P2/P3/P4/P5/P6 양쪽 파일 각각)
- 잔존 inline style 목록 (색 변수 케이스)
- 메모리 anchor 4건 적용 결과
- Phase A 보존 확인 (양쪽 파일)
- 비즈 anchor identity 확인 (precise grep 결과)
- Self-Check: PASSED (FOUND list)
- Next Steps: production cherry-pick 후보 (47b9088 + 184e548 + this commit)

**Reference format:** `cha-bio-safety/.planning/quick/260527-wdc-legalpage-phase-b-inline-style-141-tailw/260527-wdc-SUMMARY.md`

Do NOT commit SUMMARY.md — orchestrator handles docs commit.
  </action>
  <verify>
    <automated>cd /Users/jykevin/Documents/cbc7119-design/cha-bio-safety && A1=$(grep -c 'style={{' src/pages/LegalFindingsPage.tsx) && A2=$(grep -c 'style={{' src/pages/LegalFindingDetailPage.tsx) && echo "After Findings=$A1 Detail=$A2 (was 29+35=64)" && [ "$(grep -cE '✓|✗|🔒|💾|🔥|⏰|📋|✅|⚠️|❌|🔧|🚨|🔍|🧯|📊' src/pages/LegalFindingsPage.tsx)" = "0" ] && [ "$(grep -cE '✓|✗|🔒|💾|🔥|⏰|📋|✅|⚠️|❌|🔧|🚨|🔍|🧯|📊' src/pages/LegalFindingDetailPage.tsx)" = "0" ] && [ "$(grep -cE 'bg-warning[^-]|border-safe[^-]|border-warning[^-]|border-danger[^-]' src/pages/LegalFindingsPage.tsx)" = "0" ] && [ "$(grep -cE 'bg-warning[^-]|border-safe[^-]|border-warning[^-]|border-danger[^-]' src/pages/LegalFindingDetailPage.tsx)" = "0" ] && [ "$(./node_modules/.bin/tsc --noEmit 2>&1 | grep -E 'src/pages/LegalFindings(|Detail)Page.tsx' | grep -c 'error TS')" = "0" ] && cd .. && [ "$(git diff --name-only HEAD | grep -v 'cha-bio-safety/src/pages/LegalFindings.*Page.tsx' | grep -v '.planning/' | wc -l | tr -d ' ')" = "0" ]</automated>
  </verify>
  <done>
    - LegalFindingsPage.tsx 정적 inline style 0 (잔존 = 색변수 케이스만)
    - LegalFindingDetailPage.tsx 정적 inline style 0 (잔존 = 색변수 케이스만)
    - 비즈 anchor count diff 양쪽 파일 = empty (identical)
    - onClick handlers precise diff 양쪽 파일 = empty (identical)
    - emoji 0 + 비표준 색 토큰 0 (Phase A 보존)
    - TypeScript LegalFindingsPage / LegalFindingDetailPage 신규 에러 0
    - 변경 파일 = 두 .tsx 만 (다른 파일 0)
    - 메모리 anchor 4건 적용
  </done>
</task>

</tasks>

<verification>
**플랜 전체 검증:**

1. inline style 양쪽 합계 64 → < 15 (색변수 잔존만)
2. 비즈 anchor (10종) before/after byte-identical 양쪽 파일
3. emoji 0 + 비표준 색 토큰 0 (Phase A §7.1/§2.3 보존)
4. TypeScript 신규 에러 0
5. 변경 파일 범위 = 두 .tsx 만
6. 시각 검증 (수동) — cbc7119-preview 배포 후 LegalFindings + LegalFindingDetail 페이지 픽셀 단위 동일 확인
</verification>

<success_criteria>
- [ ] LegalFindingsPage.tsx 정적 inline style 0
- [ ] LegalFindingDetailPage.tsx 정적 inline style 0
- [ ] 비즈 anchor + onClick handlers precise diff = empty (양쪽 파일)
- [ ] Phase A 보존 (Lucide / 색 토큰 / emoji 0)
- [ ] TypeScript 0 error
- [ ] 변경 파일 = 두 .tsx 만
- [ ] atomic 단일 commit
- [ ] SUMMARY.md 작성
</success_criteria>

<commits>
- **Pre-dispatch (orchestrator)**: `docs(260528-01h): pre-dispatch plan for sibling Legal pages Phase B`
- **Task 1 (executor)**: `feat(260528-01h-01): sibling Legal pages Phase B — inline style 64곳 → tailwind class (옵션 X+P+M, 색변수 N)`
- **Docs (orchestrator)**: `docs(quick-260528-01h): sibling Legal pages Phase B 완료 — SUMMARY + STATE.md`
</commits>

<output>
After completion, `260528-01h-SUMMARY.md` 작성. 다음 작업 후보:
- production cherry-pick 묶음 (47b9088 + 184e548 + this commit) — 직원 도메인 sync
- InspectionPage / ElevatorPage 등 대량 inline style 페이지 동일 룰 적용 (별도 quick task)
</output>
