---
phase: 260420-e72
plan: 01
subsystem: dashboard
tags: [desktop, dashboard, elevator, badge, ui, quick]
requires: [src/pages/DashboardPage.tsx]
provides: [데스크톱 승강기 고장 카드 검사도래 배지]
affects: [DashboardPage 데스크톱 statCards 렌더링]
tech-stack:
  added: []
  patterns: [조건부 JSX 렌더링, 모바일/데스크톱 스타일 토큰 공유]
key-files:
  created: []
  modified:
    - src/pages/DashboardPage.tsx
decisions:
  - 라벨 문자열 일치(c.label === '승강기 고장')로 최소 변경 타겟팅 — statCards 스키마 확장(예: showInspBadge) 회피
  - marginTop 8(desktop) vs 4(mobile): 데스크톱 카드 내부 여백에 맞춰 살짝 키움. 카드 높이는 grid가 자동 동기화
metrics:
  duration: "80s"
  completed: "2026-04-20"
  tasks: 1
  files: 1
---

# 260420-e72 Plan 01: Desktop 승강기 검사도래 배지 Summary

데스크톱 대시보드 '승강기 고장' 카드에 모바일과 동일한 주황 '검사도래 N' 배지를 추가해 모바일/데스크톱 정보 격차를 해소했다.

## Changes

### src/pages/DashboardPage.tsx

**위치:** 데스크톱 분기 `statCards.map(c => ...)` 내부, 숫자/단위 row와 하단 액센트 바 사이 (L207 이후 신규 삽입 블록)

**추가된 블록 (7 lines, 조건부 JSX):**

```tsx
{c.label === '승강기 고장' && stats.elevInspDueSoon > 0 && (
  <div style={{ display:'flex', justifyContent:'flex-end', marginTop:8 }}>
    <span style={{ background:'#fff3e0', color:'#e65100', padding:'2px 6px', borderRadius:6, fontSize:11, fontWeight:600 }}>
      검사도래 {stats.elevInspDueSoon}
    </span>
  </div>
)}
```

**diff stat:** `1 file changed, 7 insertions(+)`

**변경 범위:**
- 추가 라인: 7줄 (L207 직전에 조건부 배지 블록 삽입)
- 기존 라인 수정/삭제: 없음
- 모바일 배지 블록(L435-441 원본, 이제 L442-448): 변경 없음

## Verification

- **Automated:** `npx tsc --noEmit` → EXIT=0 (타입 에러 없음)
- **Grep 검증:**
  - `grep -n "검사도래" src/pages/DashboardPage.tsx` → 2 matches (데스크톱 L210 신규 + 모바일 L445 기존) ✅
  - `grep -c "c.label === '승강기 고장'" src/pages/DashboardPage.tsx` → 1 ✅

## Must-Haves Check

- ✅ `elevInspDueSoon > 0`일 때 '승강기 고장' 카드 안에 '검사도래 N' 주황 배지 렌더
- ✅ `elevInspDueSoon === 0`일 때 배지 렌더링 안 됨 (`&& stats.elevInspDueSoon > 0` 가드)
- ✅ 나머지 3개 카드(점검 미완료/미조치/오늘 일정)에는 배지 없음 (`c.label === '승강기 고장'` 가드)
- ✅ 스타일 토큰(#fff3e0, #e65100, 2px 6px 패딩, 6px radius, 11px/600)이 모바일과 bit-identical
- ✅ 하단 액센트 바(`position:'absolute'`)는 배지와 겹치지 않음 — 배지는 일반 플로우로 추가됨

## Deviations from Plan

None - 플랜대로 정확히 실행.

## User Actions Required

사용자 검토 후 배포:

```bash
npm run deploy -- --branch production
```

배포 후 시각 확인:
1. 데스크톱(1920x1080)에서 `/` 대시보드 접속
2. `elevInspDueSoon > 0` 상황이면 '승강기 고장' 카드 우측에 주황 '검사도래 N' 배지 확인
3. 모바일 뷰포트 회귀 확인(기존 배지 그대로 표시)

## Commits

- `72bd198` feat(260420-e72-01): add elev inspection badge to desktop dashboard

## Self-Check: PASSED

- FOUND: src/pages/DashboardPage.tsx (수정됨, +7 lines)
- FOUND: commit 72bd198 on main
- FOUND: 검사도래 문자열 2회 (desktop + mobile)
- FOUND: 승강기 고장 라벨 일치 가드 1회
- TS compile: EXIT=0 ✅
