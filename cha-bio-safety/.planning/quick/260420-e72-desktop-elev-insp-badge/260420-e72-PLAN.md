---
phase: 260420-e72
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/DashboardPage.tsx
autonomous: true
requirements:
  - QUICK-260420-e72
must_haves:
  truths:
    - "데스크톱 대시보드에서 `elevInspDueSoon > 0`일 때 '승강기 고장' 카드 안에 주황 배지 '검사도래 N'이 보인다"
    - "`elevInspDueSoon === 0`일 때는 배지가 렌더링되지 않는다"
    - "나머지 3개 통계 카드(점검 미완료, 미조치 항목, 오늘 일정)에는 배지가 나타나지 않는다"
    - "배지 시각 스타일이 모바일(L437-439)과 동일하다 (#fff3e0 배경, #e65100 글자, 2px 6px 패딩, 6px radius, 11px/600)"
    - "배지가 카드 하단 컬러 액센트 바와 겹치지 않고 그 위에 위치한다"
  artifacts:
    - path: "src/pages/DashboardPage.tsx"
      provides: "데스크톱 통계 카드 배지 렌더링"
      contains: "검사도래"
  key_links:
    - from: "DashboardPage.tsx 데스크톱 statCards 렌더링"
      to: "stats.elevInspDueSoon"
      via: "조건부 JSX (label === '승강기 고장' && stats.elevInspDueSoon > 0)"
      pattern: "elevInspDueSoon"
---

<objective>
데스크톱 대시보드의 '승강기 고장' 카드에 모바일과 동일한 주황 '검사도래 N' 배지를 추가한다.

Purpose: 모바일에서는 이미 제공되는 승강기 정기검사 도래 알림을 데스크톱 사용자도 대시보드 한눈에 확인할 수 있게 하여 모바일/데스크톱 정보 격차를 해소한다.
Output: `src/pages/DashboardPage.tsx` 수정 — 데스크톱 statCards 루프에서 '승강기 고장' 카드에 한해 조건부 배지 렌더링 추가.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@CLAUDE.md
@src/pages/DashboardPage.tsx

<interfaces>
<!-- 데스크톱 statCards (L116-121): 단순 객체 배열. '승강기 고장'은 index 3, label === '승강기 고장' -->
<!-- 데스크톱 카드 렌더링 구조 (L194-209): -->
```tsx
{statCards.map(c => (
  <div key={c.label} onClick={() => navigate(c.path)} style={{
    background:'var(--bg2)', border:'1px solid var(--bd)', borderRadius:16,
    padding:'20px 22px', cursor:'pointer', position:'relative', overflow:'hidden',
    ...
  }}>
    <div>{c.label}</div>                                   {/* 라벨 */}
    <div style={{ display:'flex', alignItems:'baseline' }}>
      <span>{c.val}</span>                                  {/* 숫자 */}
      <span>{c.sub}</span>                                  {/* 단위 */}
    </div>
    <div style={{ position:'absolute', bottom:0, ..., height:3, background:c.color }} />  {/* 하단 액센트 바 */}
  </div>
))}
```

<!-- 모바일 배지 템플릿 (L435-441) — 이 스타일을 그대로 재사용한다: -->
```tsx
{stats.elevInspDueSoon > 0 && (
  <div style={{ display:'flex', justifyContent:'flex-end', marginTop:4 }}>
    <span style={{ background:'#fff3e0', color:'#e65100', padding:'2px 6px', borderRadius:6, fontSize:11, fontWeight:600 }}>
      검사도래 {stats.elevInspDueSoon}
    </span>
  </div>
)}
```

<!-- stats 객체는 이미 데스크톱 분기에서 접근 가능 (dashboardApi에서 로드됨). elevInspDueSoon 필드 존재 확정 — 모바일 레이아웃에서 이미 사용 중. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 데스크톱 '승강기 고장' 카드에 검사도래 배지 추가</name>
  <files>src/pages/DashboardPage.tsx</files>
  <action>
`src/pages/DashboardPage.tsx` 데스크톱 분기 L194-209의 `statCards.map(c => ...)` 카드 렌더링 JSX를 수정한다. 카드 내부, 숫자/단위 row(L203-206) 바로 아래이며 하단 액센트 바(L207) **위**에, 다음 조건부 블록을 삽입한다:

```tsx
{c.label === '승강기 고장' && stats.elevInspDueSoon > 0 && (
  <div style={{ display:'flex', justifyContent:'flex-end', marginTop:8 }}>
    <span style={{ background:'#fff3e0', color:'#e65100', padding:'2px 6px', borderRadius:6, fontSize:11, fontWeight:600 }}>
      검사도래 {stats.elevInspDueSoon}
    </span>
  </div>
)}
```

핵심 포인트:
- 배경/글자색/패딩/폰트 스타일은 모바일 버전(L437-439)과 완전 동일하게 유지 — 단일 진리값.
- `marginTop:8`은 데스크톱 카드 여백에 맞춰 모바일(4)보다 약간 크게. 다른 카드들과 높이 차가 생기더라도 CSS grid(`gridTemplateColumns:'repeat(4,1fr)'`)가 카드 높이를 자동 동기화하므로 문제없음.
- 하단 액센트 바는 `position:'absolute'`이므로 배지가 일반 플로우에 추가돼도 겹치지 않는다.
- `c.label === '승강기 고장'` 문자열 일치로 타겟팅 — statCards 배열에 새 필드(예: `showInspBadge`) 추가하는 대신 최소 변경으로 끝낸다. 라벨 문자열은 L120에 정의되어 있다.
- statCards 구조(배열/타입) 자체는 변경하지 않는다 — 기존 4개 속성(label, val, sub, color, path) 그대로.
- 모바일 쪽 배지(L435-441)는 **절대 수정하지 않는다**. 데스크톱 분기 내부 변경만.
- 배포(`npm run deploy`)는 수행하지 않는다 — 사용자가 검토 후 직접 배포.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    추가 수동 확인 (검증 후):
    1. `grep -n "검사도래" src/pages/DashboardPage.tsx` → 2개 매치 (데스크톱 신규 + 모바일 기존)
    2. `grep -c "c.label === '승강기 고장'" src/pages/DashboardPage.tsx` → 1
  </verify>
  <done>
    - DashboardPage.tsx 데스크톱 statCards.map 내부에 `c.label === '승강기 고장' && stats.elevInspDueSoon > 0` 가드 배지 JSX가 추가됨
    - 배지 스타일 토큰(`#fff3e0`, `#e65100`, 6/11/600)이 모바일과 bit-identical
    - 모바일 배지 블록(L435-441)은 그대로 유지
    - `npx tsc --noEmit` 통과 (타입 에러 없음)
    - 파일 내 기타 라인은 변경되지 않음 (diff 최소화)
  </done>
</task>

</tasks>

<verification>
1. 타입체크: `npx tsc --noEmit` 성공
2. 시각 확인 (사용자 배포 후):
   - 데스크톱(1920x1080)에서 `/` 대시보드 접속
   - `elevInspDueSoon > 0`일 때 '승강기 고장' 카드에 주황 배지 '검사도래 N'이 우측 정렬로 보임
   - 나머지 3개 카드(점검 미완료/미조치/오늘 일정)에는 배지 없음
   - `elevInspDueSoon === 0`이면 '승강기 고장' 카드에도 배지 없음
   - 하단 컬러 액센트 바가 배지와 겹치지 않음
3. 모바일 회귀 확인: 모바일 뷰포트에서 기존 배지가 이전과 동일하게 표시됨
</verification>

<success_criteria>
- 데스크톱 '승강기 고장' 카드가 `stats.elevInspDueSoon > 0` 조건에서만 모바일과 시각적으로 동일한 주황 '검사도래 N' 배지를 표시한다
- 기타 카드/모바일 레이아웃/statCards 스키마에 부작용 없음
- TypeScript 컴파일 에러 없음
</success_criteria>

<output>
After completion, create `.planning/quick/260420-e72-desktop-elev-insp-badge/260420-e72-01-SUMMARY.md` with: diff 요약, 변경 라인 범위, `npx tsc --noEmit` 결과, 사용자 배포 안내(`npm run deploy -- --branch production`).
</output>
