---
phase: quick-260429-n1e
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - migrations/0075_extinguisher_inaccessible_marks.sql
  - src/pages/InspectionPage.tsx
autonomous: true
requirements:
  - QUICK-260429-n1e
must_haves:
  truths:
    - "13개 소화기 cp의 description 이 '분말 [접근불가]' / '할로겐 [접근불가]' 로 갱신되는 마이그레이션 SQL 파일이 존재한다"
    - "사용자가 점검 카테고리에서 '소화기' 그룹을 선택하면, 접근불가 표시된 미점검 cp 가 자동으로 'normal' (memo='접근불가 개소 자동 정상처리') 로 1회 기록된다"
    - "기존 완강기 자동완료 useEffect (line 4037~4063) 의 동작/시그니처는 변경되지 않는다"
    - "AccessBlockedPopup 트리거(`description?.includes('접근불가')`) 는 신규 description 포맷('분말 [접근불가]', '할로겐 [접근불가]') 도 그대로 매치한다 (코드 수정 불필요)"
    - "npm run build 가 성공한다"
  artifacts:
    - path: "migrations/0075_extinguisher_inaccessible_marks.sql"
      provides: "13개 소화기 cp description 갱신 (분말 9 + 할로겐 4)"
      contains: "UPDATE check_points SET description"
    - path: "src/pages/InspectionPage.tsx"
      provides: "feAutoRef 선언 + 소화기 카테고리 자동완료 useEffect"
      contains: "feAutoRef"
  key_links:
    - from: "src/pages/InspectionPage.tsx (소화기 useEffect)"
      to: "inspectionApi.submitRecord"
      via: "ensureSession() 후 Promise.all submitRecord({ result: 'normal', memo: '접근불가 개소 자동 정상처리' })"
      pattern: "inspectionApi\\.submitRecord"
    - from: "src/pages/InspectionPage.tsx (소화기 useEffect)"
      to: "loadTodayRecords"
      via: "Promise.all 완료 후 호출 → 자동완료 결과 즉시 화면 반영"
      pattern: "loadTodayRecords\\(\\)"
    - from: "AccessBlockedPopup trigger (line 2942 isAccessBlocked)"
      to: "신규 description 포맷"
      via: "description?.includes('접근불가') — '분말 [접근불가]' / '할로겐 [접근불가]' 매치"
      pattern: "includes\\('접근불가'\\)"
---

<objective>
사용자가 14개 미점검 소화기를 모두 접근불가 구역으로 보고함. 완강기 접근불가 자동완료 패턴(line 4037~4063)을 소화기에 동일 적용한다.

Purpose:
- 매월 점검 시 접근불가 소화기를 사용자가 수동으로 정상 처리하지 않아도 자동 기록되어 점검 완료율을 정확하게 반영.
- 데이터 정의(description) + UI 자동완료 로직 두 축을 동시에 정렬.

Output:
- migrations/0075_extinguisher_inaccessible_marks.sql (신규)
- src/pages/InspectionPage.tsx (feAutoRef 선언 + 소화기 useEffect 추가)
- 단일 atomic 커밋: `fix(quick-260429-n1e): 소화기 접근불가 개소 자동완료 + 14개 cp 마이그레이션`

Out of scope:
- 원격 D1 적용 (사용자 배포 단계에서 별도 실행)
- AccessBlockedPopup 코드 수정 (`description?.includes('접근불가')` 가 이미 신규 포맷 매치)
- ROADMAP.md / docs 갱신 (Do NOT commit docs per task brief)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/STATE.md
@src/pages/InspectionPage.tsx
@src/components/AccessBlockedPopup.tsx
@functions/api/check-points/[id].ts
@migrations/0074_elevator_fault_photos.sql

<interfaces>
<!-- 핵심 컨트랙트 — 코드베이스 탐색 없이 그대로 사용. -->

src/pages/InspectionPage.tsx 기존 (변경 금지):

line 3899:
```ts
const wkAutoRef = useRef(false)
```

line 4037~4063 (완강기 자동완료 useEffect, 참조용):
```ts
// 완강기 카테고리 선택 시 접근불가 개소 자동 정상처리 (1회)
useEffect(() => {
  if (selectedGroupIdx === null || wkAutoRef.current || allCheckpoints.length === 0) return
  if (!CATEGORY_GROUPS[selectedGroupIdx].categories.includes('완강기')) return

  const inaccessible = allCheckpoints.filter(
    cp => cp.category === '완강기' && cp.description === '접근불가' && !records[cp.id]
  )
  if (inaccessible.length === 0) { wkAutoRef.current = true; return }

  wkAutoRef.current = true
  ;(async () => {
    try {
      const sid = await ensureSession()
      await Promise.all(
        inaccessible.map(cp =>
          inspectionApi.submitRecord(sid, {
            checkpointId: cp.id,
            result: 'normal',
            memo: '접근불가 개소 자동 정상처리',
          })
        )
      )
      loadTodayRecords()
    } catch { wkAutoRef.current = false }
  })()
}, [selectedGroupIdx, allCheckpoints]) // eslint-disable-line
```

기존 정의된 동일 스코프 식별자 (그대로 사용):
- `selectedGroupIdx`, `allCheckpoints`, `records`, `CATEGORY_GROUPS`
- `ensureSession()` (line 4065)
- `inspectionApi.submitRecord(sid, { checkpointId, result, memo })`
- `loadTodayRecords()`
- `useRef`, `useEffect` (이미 import)

migrations/0074_elevator_fault_photos.sql (포맷 참고):
```sql
-- Migration 0074: ...
-- 한국어 설명 한 줄
ALTER TABLE ... ;
```

소화기 description 의 두 포맷 차이:
- 완강기: `cp.description === '접근불가'` (정확 일치)
- 소화기: `cp.description?.includes('[접근불가]')` (대괄호 포함 부분 매치 — 분말/할로겐 prefix 보존)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 0075 마이그레이션 SQL 파일 작성</name>
  <files>migrations/0075_extinguisher_inaccessible_marks.sql</files>
  <action>
신규 파일 작성. 0074 형식과 동일하게 헤더 주석 + 두 개의 UPDATE 문.

파일 정확 내용 (이 외에는 추가/변경 금지):

```sql
-- Migration 0075: Extinguisher inaccessible marks
-- 14개 미점검 소화기 중 13개 cp description 갱신 (CP-FE-0427 은 이미 갱신됨, 제외)
-- 분말 9개 + 할로겐 4개. 자동완료 useEffect 의 includes('[접근불가]') 패턴과 정렬.

-- 분말 9개 — research 7F, 8F
UPDATE check_points SET description = '분말 [접근불가]' WHERE id IN (
  'CP-FE-0044', 'CP-FE-0407', 'CP-FE-0408', 'CP-FE-0005',
  'CP-FE-0010', 'CP-FE-0011', 'CP-FE-0414', 'CP-FE-0012', 'CP-FE-0415'
);

-- 할로겐 4개 — office 7F 청정구역, research 1F GMP
UPDATE check_points SET description = '할로겐 [접근불가]' WHERE id IN (
  'CP-FE-0416', 'CP-FE-0417', 'CP-FE-0418', 'CP-FE-0419'
);
```

원격 적용은 보류 — 파일 작성만. wrangler d1 execute 호출하지 말 것.
  </action>
  <verify>
    <automated>test -f migrations/0075_extinguisher_inaccessible_marks.sql &amp;&amp; [ "$(grep -c 'UPDATE check_points' migrations/0075_extinguisher_inaccessible_marks.sql)" = "2" ] &amp;&amp; grep -q "'분말 \[접근불가\]'" migrations/0075_extinguisher_inaccessible_marks.sql &amp;&amp; grep -q "'할로겐 \[접근불가\]'" migrations/0075_extinguisher_inaccessible_marks.sql &amp;&amp; grep -q 'CP-FE-0044' migrations/0075_extinguisher_inaccessible_marks.sql &amp;&amp; grep -q 'CP-FE-0419' migrations/0075_extinguisher_inaccessible_marks.sql</automated>
  </verify>
  <done>
파일 존재. UPDATE 문 정확히 2건. 분말 9개 (CP-FE-0044, 0407, 0408, 0005, 0010, 0011, 0414, 0012, 0415) + 할로겐 4개 (CP-FE-0416, 0417, 0418, 0419) 모두 포함. CP-FE-0427 은 미포함 (이미 갱신됨).
  </done>
</task>

<task type="auto">
  <name>Task 2: feAutoRef 선언 + 소화기 자동완료 useEffect 추가</name>
  <files>src/pages/InspectionPage.tsx</files>
  <action>
**2-1. feAutoRef 선언 추가 (line 3899 직후)**

기존:
```ts
const wkAutoRef = useRef(false)
```

다음 줄에 추가:
```ts
const feAutoRef = useRef(false)
```

**2-2. 소화기 자동완료 useEffect 추가 (line 4063 직후, 완강기 useEffect 끝나는 라인 바로 다음)**

완강기 useEffect 의 닫는 `}, [selectedGroupIdx, allCheckpoints]) // eslint-disable-line` 직후, `const ensureSession = async ...` 선언 (line 4065) 직전에 다음 블록 삽입:

```ts
  // 소화기 카테고리 선택 시 접근불가 개소 자동 정상처리 (1회)
  useEffect(() => {
    if (selectedGroupIdx === null || feAutoRef.current || allCheckpoints.length === 0) return
    if (!CATEGORY_GROUPS[selectedGroupIdx].categories.includes('소화기')) return

    const inaccessible = allCheckpoints.filter(
      cp => cp.category === '소화기' && cp.description?.includes('[접근불가]') && !records[cp.id]
    )
    if (inaccessible.length === 0) { feAutoRef.current = true; return }

    feAutoRef.current = true
    ;(async () => {
      try {
        const sid = await ensureSession()
        await Promise.all(
          inaccessible.map(cp =>
            inspectionApi.submitRecord(sid, {
              checkpointId: cp.id,
              result: 'normal',
              memo: '접근불가 개소 자동 정상처리',
            })
          )
        )
        loadTodayRecords()
      } catch { feAutoRef.current = false }
    })()
  }, [selectedGroupIdx, allCheckpoints]) // eslint-disable-line
```

**중요 제약:**
- 완강기 useEffect (line 4037~4063) 는 한 글자도 건드리지 말 것.
- description 매칭 패턴 차이는 의도된 것:
  - 완강기 = `cp.description === '접근불가'`
  - 소화기 = `cp.description?.includes('[접근불가]')` (대괄호 포함, 분말/할로겐 prefix 보존)
- 카테고리 매칭은 `'소화기'` (한국어 그대로). category 명은 기존 데이터/CATEGORY_GROUPS 와 동일.
- `ensureSession`, `inspectionApi.submitRecord`, `loadTodayRecords`, `records`, `selectedGroupIdx`, `allCheckpoints`, `CATEGORY_GROUPS` 모두 기존 스코프 그대로 사용. 새 import 불필요.
- AccessBlockedPopup 트리거 (`isAccessBlocked = !!currentSelCP?.description?.includes('접근불가')`) 는 신규 포맷도 매치하므로 코드 수정 불필요. 이 task 범위 밖.
  </action>
  <verify>
    <automated>cd /Users/jykevin/Documents/20260328/cha-bio-safety &amp;&amp; [ "$(grep -c 'feAutoRef' src/pages/InspectionPage.tsx)" -ge "5" ] &amp;&amp; grep -q "const feAutoRef = useRef(false)" src/pages/InspectionPage.tsx &amp;&amp; grep -q "소화기 카테고리 선택 시 접근불가 개소 자동 정상처리" src/pages/InspectionPage.tsx &amp;&amp; grep -q "cp.category === '소화기' &amp;&amp; cp.description?.includes('\[접근불가\]')" src/pages/InspectionPage.tsx &amp;&amp; [ "$(grep -c 'wkAutoRef' src/pages/InspectionPage.tsx)" = "5" ] &amp;&amp; npm run build</automated>
  </verify>
  <done>
- `feAutoRef` 선언 1건 + useEffect 내부 사용 ≥ 4건 = 총 5+ 건.
- 완강기 `wkAutoRef` 출현 횟수는 정확히 5건 (선언 + line 4039/4045/4047/4061 사용 = 변경 없음).
- 소화기 useEffect 의 description 매칭이 `includes('[접근불가]')` (대괄호 포함).
- `npm run build` 가 에러 없이 통과.
  </done>
</task>

<task type="auto">
  <name>Task 3: Atomic 커밋</name>
  <files>migrations/0075_extinguisher_inaccessible_marks.sql, src/pages/InspectionPage.tsx</files>
  <action>
git status 로 변경 파일이 정확히 두 개인지 확인 (`migrations/0075_extinguisher_inaccessible_marks.sql` 신규 + `src/pages/InspectionPage.tsx` 수정).

다른 파일이 변경되어 있다면 (예: ROADMAP.md, package-lock.json, .planning/) 절대 stage 하지 말 것. Task brief 명시: Do NOT commit docs / Do NOT update ROADMAP.md.

스테이징은 두 파일만 명시적으로 추가:
```bash
git add migrations/0075_extinguisher_inaccessible_marks.sql src/pages/InspectionPage.tsx
```

커밋 메시지 (정확히 이대로):
```
fix(quick-260429-n1e): 소화기 접근불가 개소 자동완료 + 14개 cp 마이그레이션

13개 cp description 을 '분말/할로겐 [접근불가]' 로 갱신하는 0075 마이그레이션
+ InspectionPage 에 소화기 카테고리 자동완료 useEffect 추가 (완강기 패턴 동일).
description 매칭은 includes('[접근불가]') 로 prefix 보존. CP-FE-0427 은 이미 갱신됨.

원격 D1 적용은 사용자 배포 단계에서 별도 실행.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
```

HEREDOC 으로 메시지 전달:
```bash
git commit -m "$(cat <<'EOF'
fix(quick-260429-n1e): 소화기 접근불가 개소 자동완료 + 14개 cp 마이그레이션

13개 cp description 을 '분말/할로겐 [접근불가]' 로 갱신하는 0075 마이그레이션
+ InspectionPage 에 소화기 카테고리 자동완료 useEffect 추가 (완강기 패턴 동일).
description 매칭은 includes('[접근불가]') 로 prefix 보존. CP-FE-0427 은 이미 갱신됨.

원격 D1 적용은 사용자 배포 단계에서 별도 실행.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

배포(`npm run deploy`)는 이 task 범위 밖 — 사용자 지시 시에만 실행.
  </action>
  <verify>
    <automated>cd /Users/jykevin/Documents/20260328/cha-bio-safety &amp;&amp; git log -1 --pretty=%B | grep -q "quick-260429-n1e" &amp;&amp; git log -1 --name-only --pretty=format: | grep -q "migrations/0075_extinguisher_inaccessible_marks.sql" &amp;&amp; git log -1 --name-only --pretty=format: | grep -q "src/pages/InspectionPage.tsx" &amp;&amp; [ "$(git log -1 --name-only --pretty=format: | grep -v '^$' | wc -l | tr -d ' ')" = "2" ]</automated>
  </verify>
  <done>
HEAD 커밋이 'quick-260429-n1e' 포함, 정확히 두 파일 변경 (migrations/0075 + InspectionPage.tsx). 작업 트리는 클린 또는 추적 외 파일만 남음.
  </done>
</task>

</tasks>

<verification>
**End-to-end 자동 검증:**
1. `test -f migrations/0075_extinguisher_inaccessible_marks.sql` — 파일 존재
2. `grep -c 'UPDATE check_points' migrations/0075_extinguisher_inaccessible_marks.sql` == 2
3. `grep -c "feAutoRef" src/pages/InspectionPage.tsx` ≥ 5 (선언 + 사용 4건)
4. `grep -c "wkAutoRef" src/pages/InspectionPage.tsx` == 5 (변경 없음)
5. `grep -q "소화기 카테고리 선택 시 접근불가 개소 자동 정상처리" src/pages/InspectionPage.tsx`
6. `npm run build` 성공
7. `git log -1` HEAD 커밋이 정확히 두 파일만 포함

**휴먼 검증 (배포 후 사용자):** 
- 원격 D1 에 0075 적용
- PWA 재설치 (서비스워커 캐시 새로고침 — feedback_pwa_cache_invalidation.md 참고)
- 소화기 카테고리 진입 → 14개 미점검 소화기 자동으로 'normal' 기록됨 확인
- 점검 화면에서 해당 cp 선택 시 AccessBlockedPopup 정상 노출 확인
</verification>

<success_criteria>
- [ ] migrations/0075_extinguisher_inaccessible_marks.sql 작성 (UPDATE 2건, 13개 cp)
- [ ] src/pages/InspectionPage.tsx 에 feAutoRef + 소화기 useEffect 추가
- [ ] 완강기 useEffect (line 4037~4063) 미변경
- [ ] AccessBlockedPopup 트리거는 코드 수정 없이 신규 포맷 매치 (분석 완료, 수정 task 없음)
- [ ] `npm run build` 성공
- [ ] 단일 atomic 커밋: `fix(quick-260429-n1e): 소화기 접근불가 개소 자동완료 + 14개 cp 마이그레이션`
- [ ] 두 파일만 변경됨 (migrations/0075 + InspectionPage.tsx). docs/ROADMAP.md/원격 D1 적용 모두 없음.
</success_criteria>

<output>
After completion, create `.planning/quick/260429-n1e-extinguisher-inaccessible/260429-n1e-SUMMARY.md`.
</output>
