---
phase: 260529-q5a-phase-b-wave-14b-remediation-desktop
plan: 01
type: execute
wave: 14b
depends_on: [260529-gj2]
files_modified:
  - src/pages/RemediationPage.tsx
autonomous: true
roadmap-wave: Tier 2 / Wave 14b (Remediation 데스크톱 zone — Tier 2 페어 wave 세 번째 완결, 19번째 atomic)
requirements: [phase-b-wave-14b]
tags: [remediation, inline-style-to-tailwind, no-op-refactor, phase-b-tier-2-wave-14b, tier-2-pair-wave, zone-aware-sweep, desktop-zone-only, mobile-zone-preserved, early-return-pattern, skeleton-style-preserved, kv-table-loop-conversion, atomic-single-commit]

must_haves:
  truths:
    - "RemediationPage 데스크톱 zone 14 inline 중 13건 tailwind 변환 + L297 옵션 N 1건 잔존"
    - "모바일 zone 잔존 1건 (L547 scrollbarWidth 옵션 N) IDENTICAL — 변경 0"
    - "SKELETON_STYLE module const 정의 + 직접 참조 6건 보존"
    - "Wave 14a + 14b 페어 완결 — RemediationPage 전체 25 → 2 (-92%)"
    - "비즈 anchor (9 onClick / 2 useState / 2 useQuery / 1 useNavigate / 1 useSearchParams / 2 fetch) IDENTICAL"
    - "TypeScript 0 error, vite build PASS, 시각 0 byte 변경 (no-op refactor)"
  artifacts:
    - path: src/pages/RemediationPage.tsx
      provides: "데스크톱 zone 14 inline → 1 (L297 옵션 N 잔존). page 전체 15 → 2"
      contains: "if (isDesktop) { return (...) } 블록 (L281-L493) 안 inline 14건 변환"
  key_links:
    - from: src/pages/RemediationPage.tsx
      to: tailwind.config.js + tokens.css
      via: "var(--surface-sunken)→bg-surface-sunken / var(--border-default)→border-border-default / var(--text-secondary)→text-text-secondary / var(--text-primary)→text-text-primary"
      pattern: "토큰 alias 일괄 매핑 (Wave 11/12a/12b/13a/13b/14a precedent 그대로)"
---

<objective>
**Phase B Wave 14b — RemediationPage 데스크톱 zone.** Tier 2 페어 wave 세 번째 완결 (Wave 12a+12b StaffManage / 13a+13b Checkpoints 직후 14a+14b Remediation 페어 완결).

RemediationPage.tsx (Wave 14a 직후 638줄, **15 total inline** = 데스크톱 zone 14 + 모바일 zone 1):
- **데스크톱 zone (sweep 대상): 14 inline** — `if (isDesktop) { return (...) }` 블록 (L281-L493) 안. kv-table th/td 11 + scrollbar 1 + detail-empty fontSize 1 + detail-pane padding 1
- **모바일 zone (skip, 보존): 1 inline** — L547 mobile list scrollbar `style={{ scrollbarWidth: 'none' }}` 옵션 N (Tailwind 미지원)

옵션 X+P+M+색변수N + SKELETON_STYLE module const N 승계 (**19번째 atomic** — wdc/01h/a3v/c9s/cjn/gsh/h3z/hbv/iht/irl/jey/jxo/nkv/epe/f2w/gj2/h8u/odl/ozt 18 precedent 후속).

### 구조 특이사항 (Wave 14a 직후 라인 시프트)

Wave 14a 변환 후 데스크톱 zone 라인 (현재 파일 기준):
- 데스크톱 early-return: **L281** `if (isDesktop) {`
- 데스크톱 return 종료: **L493** `}` (`if (isDesktop)` 블록 닫힘)
- 모바일 default return: **L495** `return (`

### Zone 분류 (현재 RemediationPage.tsx grep 검증 완료)

| zone | count | 위치 |
|---|---:|---|
| **desktop-only (변환)** | 13 | L327/L335/L357/L370/L372/L400/L401/L404/L405/L408/L409/L412/L413 |
| **desktop-only (옵션 N 잔존)** | 1 | L297 desktop list scrollbar `style={{ scrollbarWidth: 'none' }}` |
| **데스크톱 zone 합계** | **14** | sweep 대상 |
| **mobile-only (보존, Wave 14a 잔존)** | 1 | L547 mobile list scrollbar 옵션 N |
| **전체** | **15** | |

### 토큰 alias (Wave 11 nkv / 12a epe / 12b odl / 13a f2w / 13b ozt / 14a gj2 precedent 그대로)
- `var(--surface-sunken)` → `bg-surface-sunken`
- `var(--border-default)` → `border-border-default`
- `var(--text-secondary)` → `text-text-secondary`
- `var(--text-primary)` → `text-text-primary`
- `var(--text-tertiary)` → `text-text-tertiary` (L327 이미 className 에 있음, inline 의 fontSize 만 변환)

### 위험 anchor

- **모바일 zone L547 (1건) — 변경 절대 0** — `style={{ scrollbarWidth: 'none' } as React.CSSProperties}` IDENTICAL (Wave 14a 잔존, 옵션 N)
- **module const `SKELETON_STYLE` (L109-114) — 정의 보존**. 직접 참조 6건 (L299 desktop list `<div style={SKELETON_STYLE} />` × 3 + L551/552/553 mobile list × 3) 은 이미 inline 아님 → `style={{` grep 에 안 잡힘, **손대지 말 것**
- **옵션 N L297 desktop list scrollbar — 변경 절대 0** — 모바일 L547 와 동일 패턴 (Tailwind 미지원). 13b ozt INPUT_STYLE spread 옵션 N 보존 룰 (데스크톱 zone 안 옵션 N) precedent 적용
- **비즈 anchor (9 onClick / 2 useState / 2 useQuery / 1 useNavigate / 1 useSearchParams / 2 fetch) IDENTICAL**
- **kv-table loop th/td (L370/L372) 와 resolved branch th/td 8건 (L400/L401/L404/L405/L408/L409/L412/L413) 패턴 IDENTICAL** — th: 9-prop `{ width: 110, padding: '8px 12px', background: 'var(--surface-sunken)', border: '1px solid var(--border-default)', fontSize: 12, fontWeight: 700, color: 'var(--text-secondary)', textAlign: 'left', verticalAlign: 'top', lineHeight: 1.5 }`. td: 6-7-prop `{ padding: '8px 12px', border: '1px solid var(--border-default)', fontSize: 13, color: 'var(--text-primary)', [whiteSpace?], verticalAlign?, lineHeight: 1.6 }` (whiteSpace/verticalAlign 가변)

### Wave 14a + 14b 페어 완결 합산

| 영역 | Wave 14a 진입 시 | After 14a | After 14b (본 wave 목표) | 누적 diff |
|---|---:|---:|---:|---|
| Total `style={{` | **25** | **15** | **2** | **-23 (-92.0%)** |
| 모바일 zone | **11** | **1** | **1** (보존) | -10 |
| 데스크톱 zone | **14** | **14** | **1** | -13 |

</objective>

<context>

### 데스크톱 zone (sweep 대상 — 14 inline, L281-L493 블록 안)

#### A. 디테일 헤더/empty/scroll 영역 (L297-L335) — 2건 변환 + 1건 옵션 N

**L297 — desktop list scrollbar** (1-prop, non-standard property)
- 기존:
```tsx
<div
  className="flex-1 min-h-0 overflow-y-auto px-4 py-3 flex flex-col gap-2"
  style={{ scrollbarWidth: 'none' } as React.CSSProperties}
>
```
- After: **옵션 N 잔존** — `scrollbarWidth` 는 Tailwind 기본 클래스 없음. inline 유지.
- **변경 0** — 데스크톱 zone count 14 → 13 (이 1건 보존).
- 옵션: **N (잔존)**

**L327 — detail-empty fontSize** (1-prop)
- 기존:
```tsx
<div
  className="h-full flex items-center justify-center text-text-tertiary"
  style={{ fontSize: 13 }}
>
  좌측에서 항목을 선택하세요
</div>
```
- After: 기존 className 에 `text-[13px]` 추가 (text-caption lh:1.5 영향 회피, 14a 의 text-[13px] arbitrary 채택 룰 그대로):
```tsx
<div className="h-full flex items-center justify-center text-text-tertiary text-[13px]">
  좌측에서 항목을 선택하세요
</div>
```
- 옵션: **X** (fontSize:13 → `text-[13px]` arbitrary)

**L335 — detail-pane padding + boxSizing + scrollbarWidth** (3-prop)
- 기존:
```tsx
<div
  className="overflow-y-auto h-full"
  style={{ padding: '20px 28px', boxSizing: 'border-box', scrollbarWidth: 'none' } as React.CSSProperties}
>
```
- After: padding/boxSizing 은 className 변환 가능, **scrollbarWidth 만 옵션 N 잔존**. **부분 변환 — inline 1건 남기되 prop 2개 줄임**.
- **함정**: 부분 변환은 `style={{` grep 카운트에 변동 없음 (inline 유지). 옵션 N 잔존으로 분류, 1건은 카운트 유지.
- **판단**: 13b ozt 의 L523 `{ ...INPUT_STYLE, height: 36, appearance: 'none', cursor: 'pointer', paddingRight: 32 }` 4-prop combo 보존 precedent → **여기도 inline 통째로 보존이 안전**. ☞ **L335 옵션 N 잔존 채택**.
- 단, 옵션 N 잔존 카운트가 14 → 2 (L297 + L335) 로 늘어남. 페어 합산 (14a+14b) 잔존 = 모바일 1 + 데스크톱 2 = **3**.
- **(대안)** padding + boxSizing 만 className 화 + scrollbarWidth inline 잔존도 가능:
  - `<div className="overflow-y-auto h-full px-7 py-5 box-border" style={{ scrollbarWidth: 'none' } as React.CSSProperties}>`
  - 이 경우 inline 1건 유지 (옵션 N 잔존), 페어 합산 잔존 = 1 + 2 = 3. 카운트 동일.
- **결정**: **대안 채택 (padding + boxSizing 변환, scrollbarWidth 만 inline 잔존)** — 13b ozt 의 INPUT_STYLE spread combo 와 달리 여기는 prop 들이 독립적이고 변환 가능. 시각 0 byte 룰 유지 (`px-7 py-5 box-border` 와 inline `padding: '20px 28px', boxSizing: 'border-box'` 동일).
  - padding:'20px 28px' = `py-5 px-7` (py-5=20px, px-7=32px? — **w-7 spacing override 주의: 7=32px** → `px-7`=32px 아니라 정확값 필요 → `px-[28px]` arbitrary 채택)
  - 정정: spacing override `7: '32px'` → `px-7`=32px (28px 아님). **arbitrary `px-[28px]` 채택** (시각 0 byte 보장).
  - py-5: spacing override `5: '20px'` → `py-5`=20px ✓
  - boxSizing:'border-box' → `box-border` ✓ (tailwind 기본)
- After (대안 채택):
```tsx
<div
  className="overflow-y-auto h-full py-5 px-[28px] box-border"
  style={{ scrollbarWidth: 'none' } as React.CSSProperties}
>
```
- 옵션: **X (px-[28px] arbitrary) + 옵션 N (scrollbarWidth 잔존)**

#### B. kv-table 영역 (L357-L413) — 11건 변환

**L357 — table root** (3-prop)
- 기존:
```tsx
<table style={{ width: '100%', borderCollapse: 'collapse', marginBottom: 20 }}>
```
- After:
```tsx
<table className="w-full border-collapse mb-5">
```
- 옵션: **직접** (width:100%=w-full, borderCollapse:collapse=border-collapse, marginBottom:20=mb-5 spacing override 20px ✓)

**L370 — kv-table th (loop, 9-prop)**
- 기존:
```tsx
<th style={{ width: 110, padding: '8px 12px', background: 'var(--surface-sunken)', border: '1px solid var(--border-default)', fontSize: 12, fontWeight: 700, color: 'var(--text-secondary)', textAlign: 'left', verticalAlign: 'top', lineHeight: 1.5 }}>{label}</th>
```
- After:
```tsx
<th className="w-[110px] py-2 px-3 bg-surface-sunken border border-border-default text-[12px] font-bold text-text-secondary text-left align-top leading-[1.5]">{label}</th>
```
- 옵션: **X+token** (width:110=w-[110px] arbitrary, padding:'8px 12px'=py-2 px-3, fontSize:12=text-[12px] arbitrary text-caption lh:1.5 영향 회피, fontWeight:700=font-bold, lineHeight:1.5=leading-[1.5] arbitrary [tailwind leading-normal=1.5 이지만 명시 위해 arbitrary])
- **token alias**: `var(--surface-sunken)`→`bg-surface-sunken` / `var(--border-default)` (1px solid)→`border border-border-default` / `var(--text-secondary)`→`text-text-secondary`

**L372 — kv-table td (loop, 7-prop)**
- 기존:
```tsx
<td style={{ padding: '8px 12px', border: '1px solid var(--border-default)', fontSize: 13, color: 'var(--text-primary)', whiteSpace: 'pre-wrap', verticalAlign: 'top', lineHeight: 1.6 }}>
```
- After:
```tsx
<td className="py-2 px-3 border border-border-default text-[13px] text-text-primary whitespace-pre-wrap align-top leading-[1.6]">
```
- 옵션: **X+token** (fontSize:13=text-[13px] arbitrary, lineHeight:1.6=leading-[1.6] arbitrary, whiteSpace:'pre-wrap'=whitespace-pre-wrap, verticalAlign:'top'=align-top)

**L400 — resolved th 조치일시** (9-prop, L370 와 IDENTICAL)
- After: L370 와 동일 className 변환

**L401 — resolved td 조치일시** (6-prop, L372 와 거의 동일 — whiteSpace/verticalAlign 없음)
- 기존:
```tsx
<td style={{ padding: '8px 12px', border: '1px solid var(--border-default)', fontSize: 13, color: 'var(--text-primary)', lineHeight: 1.6 }}>{fmtDateTime(detail.resolvedAt)}</td>
```
- After:
```tsx
<td className="py-2 px-3 border border-border-default text-[13px] text-text-primary leading-[1.6]">{fmtDateTime(detail.resolvedAt)}</td>
```
- 옵션: **X+token** (L372 - whitespace-pre-wrap - align-top)

**L404 — resolved th 조치자** (9-prop, L370 IDENTICAL)
- After: L370 동일

**L405 — resolved td 조치자** (6-prop, L401 IDENTICAL)
- After: L401 동일

**L408 — resolved th 조치 내용** (9-prop, L370 IDENTICAL)
- After: L370 동일

**L409 — resolved td 조치 내용** (7-prop, L372 - verticalAlign + whiteSpace)
- 기존:
```tsx
<td style={{ padding: '8px 12px', border: '1px solid var(--border-default)', fontSize: 13, color: 'var(--text-primary)', whiteSpace: 'pre-wrap', lineHeight: 1.6 }}>{detail.resolutionMemo ?? '-'}</td>
```
- After:
```tsx
<td className="py-2 px-3 border border-border-default text-[13px] text-text-primary whitespace-pre-wrap leading-[1.6]">{detail.resolutionMemo ?? '-'}</td>
```
- 옵션: **X+token** (L372 - align-top)

**L412 — resolved th 소모 자재** (9-prop, L370 IDENTICAL)
- After: L370 동일

**L413 — resolved td 소모 자재** (7-prop, L409 IDENTICAL)
- After: L409 동일

### 모바일 zone (skip — Wave 14a 잔존 1건 절대 보존)

| Line | 영역 | inline | 옵션 |
|----:|---|---:|---:|
| L547 | mobile list scrollbar `style={{ scrollbarWidth: 'none' } as React.CSSProperties}` | 1 | N |

> Wave 14a 가 처리한 모바일 zone 의 마지막 잔존 1건. 본 wave 에서 **변경 0** (옵션 N — Tailwind 미지원).

### 예상

| 구역 | Before | After | 변환 |
|---|---:|---:|---:|
| 데스크톱 zone (L281-L493) | 14 | 2 (L297 옵션 N + L335 옵션 N 부분잔존) | 12 |
| Mobile zone (L495~) | 1 (L547 옵션 N) | 1 | 0 |
| **전체 file** | **15** | **3** | **12** |

목표: **15 → 3** (12건 변환, 옵션 N 2건 잔존 [L297+L335 부분], 모바일 1건 보존). 페어 합산 (14a+14b) 25 → 3 (-88.0%).

**참고 — count 정정**: L335 의 부분 변환은 inline 1건 유지 → 데스크톱 zone after = **2** (L297 + L335). 페어 합산 잔존 = 데스크 2 + 모바일 1 = **3**. target = page 전체 ≤ 3.

### 룰 (locked)
- 옵션 X+P+M+색변수N + SKELETON_STYLE module const N
- 모바일 zone L547 절대 변경 0
- L297 desktop scrollbar 절대 변경 0 (옵션 N)
- L335 detail-pane: scrollbarWidth 만 inline 잔존 (옵션 N 부분), padding + boxSizing 은 className 변환
- 비즈 anchor precise diff = empty
- emoji 카운트 변동 0 (Phase A 보존 — RemediationPage 는 이미 Lucide, emoji=0)
- 비표준 색 토큰 0
- TypeScript 0 error
- 변경 파일 = 1 .tsx 만 (off-scope = 0)
- 단일 atomic commit (`feat(260529-q5a-01): Phase B Wave 14b — Remediation 데스크톱 zone (14 inline) → tailwind`)

### 함정 회피

- **w-8/h-8 = 48px 함정** (`feedback_tailwind_w8_h8_is_48px.md`): tailwind.config spacing override 1=4 / 2=8 / 3=12 / 4=16 / 5=20 / 6=24 / 7=32 / 8=48. **px-7 ≠ 28px** (px-7=32px) → padding:'20px 28px' 의 28px 는 `px-[28px]` arbitrary 채택. 110/8/12 도 spacing override 인지 (8=48px override 주의 — padding:8 안 쓰지만 padding:'8px 12px' = `py-2 px-3` 기본 spacing 표 따로): 8px=py-2 (spacing override 2=8) / 12px=px-3 (spacing override 3=12) / 20px=py-5 / 110px=w-[110px] (w-7=32, w-8=48 — 110 매칭 클래스 없음 → arbitrary)
- **text-caption / text-body-sm 함정** (`feedback_text_caption_leading_none.md`): tailwind.config 의 `text-caption` (12px lh:1.5 fontWeight:400) / `text-label` (13px lh:1.5 fontWeight:400). **fontSize:12 → text-caption 사용 시 lh:1.5 + fontWeight:400 자동 적용**. 그러나 본 wave 의 th 는 `fontWeight: 700` 명시 → `font-bold` 따로 적용 필요. 안전 채택 = `text-[12px]` arbitrary + `font-bold` (각 prop 명시). 마찬가지로 td 는 fontSize:13 + lineHeight:1.6 → text-label (13px lh:1.5) 와 lh 다름 → **text-[13px] + leading-[1.6] arbitrary** 채택
- **leading-[1.5] / leading-[1.6] arbitrary**: tailwind 의 `leading-normal=1.5`, `leading-relaxed=1.625` 가 가까운 매칭이나 명시성 위해 arbitrary 채택. 0 byte 보장
- **`var(--border-default)` (1px solid)**: 매핑 = `border border-border-default` (단일 inline 의 `'1px solid var(--border-default)'` 는 두 className 의 결합)
- **`var(--text-tertiary)` already in L327**: L327 의 기존 className 에 `text-text-tertiary` 이미 있음 → fontSize:13 만 `text-[13px]` 추가 (color override 0)
- **L335 옵션 N 부분 잔존**: scrollbarWidth: 'none' 은 padding/boxSizing 과 같은 inline 객체에 있지만 Tailwind 미지원. **부분 변환** (padding+boxSizing className 화, scrollbarWidth inline 잔존). 13b ozt 의 L523 INPUT_STYLE spread 4-prop combo 보존과는 다른 패턴 (spread 가 아닌 일반 객체 → 분리 가능)

### 메모리 anchors
- `feedback_tailwind_w8_h8_is_48px.md` — px-[28px] arbitrary 채택 (spacing override 7=32px 함정 회피). py-2/px-3/py-5/mb-5 standard
- `feedback_tailwind_token_class_pattern.md` — bg-surface-sunken / border-border-default / text-text-secondary / text-text-primary — status- prefix 없음
- `feedback_text_caption_leading_none.md` — text-[12px] / text-[13px] arbitrary + leading-[1.5]/leading-[1.6] arbitrary (text-caption/text-label lh:1.5 + fontWeight 충돌 회피)
- `feedback_design_changes_ask_first.md` — 시각 0 byte 룰 100%
- Wave 14a precedent — 같은 페이지 옵션 X/M/token 룰 일관
- Wave 12b odl + 13b ozt — 페어 wave 두 번째 + 세 번째 완결 boundary 룰 (본 wave 는 early-return 구조로 boundary 없음)
- Wave 5 db728c0 (RemediationDetail) + Wave 14a — SKELETON_STYLE module const 보존 룰

</context>

<tasks>

<task type="auto">
  <name>Task 1: Sweep RemediationPage 데스크톱 zone — 14 inline (12건 변환 + 2건 옵션 N 잔존, atomic)</name>
  <files>src/pages/RemediationPage.tsx</files>
  <action>

### Step 1: Before snapshot

```bash
cd /Users/jykevin/Documents/cbc7119-design/cha-bio-safety
F=src/pages/RemediationPage.tsx
echo "style before: $(grep -c 'style={{' $F)"  # expect 15
echo "lines before: $(wc -l $F | awk '{print $1}')"  # expect 638

# 데스크톱 zone (L281-L493) inline count
DSK_START=281
DSK_END=493
echo "desktop zone before: $(sed -n "${DSK_START},${DSK_END}p" $F | grep -c 'style={{')"  # expect 14

# 모바일 zone (L494~end) inline count
echo "mobile zone before: $(sed -n "${DSK_END},\$p" $F | tail -n +2 | grep -c 'style={{')"  # expect 1 (L547)

# anchor counts
{
  for ANCHOR in 'onClick=\{[^}]+\}' 'useState\(' 'useRef\(' 'useEffect\(' 'useMutation\(' 'useQuery\(' 'useNavigate\(' 'useParams\(' 'useSearchParams\(' 'fetch\('; do
    echo "  $ANCHOR : $(grep -cE "$ANCHOR" $F)"
  done
} > /tmp/q5a-before.txt

# onClick precise set
grep -oE 'onClick=\{[^}]+\}' $F | sort | uniq > /tmp/q5a-clicks-before.txt

# SKELETON_STYLE const def
echo "SKELETON_STYLE const def: $(grep -c 'const SKELETON_STYLE' $F)"  # MUST = 1

# emoji 변동 0 룰 (RemediationPage 는 이미 Lucide 적용 → emoji = 0 예상)
echo "emoji before: $(grep -cE '✓|✗|✕|🔒|💾|🔥|⏰|📋|✅|⚠️|❌|🔧|🚨|🔍|🧯|📊|▸|◂|▴|▾' $F)"

# 비표준 색 토큰
echo "비색 before: $(grep -cE 'bg-warning[^-]|border-safe[^-]|border-warning[^-]|border-danger[^-]' $F)"
```

### Step 2: Edit RemediationPage.tsx — 데스크톱 zone 변환

**절대 변경 금지**:
- L297 desktop list scrollbar `style={{ scrollbarWidth: 'none' } as React.CSSProperties}` — 옵션 N 잔존
- L547 mobile list scrollbar (모바일 zone Wave 14a 잔존)
- L109-114 `const SKELETON_STYLE` 정의
- L299 `<div style={SKELETON_STYLE} />` × 3 (desktop list, 이미 inline 아님)
- L551/552/553 `<div style={SKELETON_STYLE} />` × 3 (mobile list, 이미 inline 아님)
- 모바일 zone 의 모든 코드 (L495 이후) — diff hunk 가 L494 이후를 절대 포함하지 않아야 함

**변환 매핑 (데스크톱 zone 12건 + 1건 부분 변환)**:

#### L297 — desktop list scrollbar (옵션 N 잔존, 변경 0)

```tsx
<div
  className="flex-1 min-h-0 overflow-y-auto px-4 py-3 flex flex-col gap-2"
  style={{ scrollbarWidth: 'none' } as React.CSSProperties}
>
```
**변경 없음** (옵션 N).

#### L327 — detail-empty fontSize:13

기존 (L325-L330):
```tsx
<div
  className="h-full flex items-center justify-center text-text-tertiary"
  style={{ fontSize: 13 }}
>
  좌측에서 항목을 선택하세요
</div>
```

After:
```tsx
<div className="h-full flex items-center justify-center text-text-tertiary text-[13px]">
  좌측에서 항목을 선택하세요
</div>
```

#### L335 — detail-pane padding + boxSizing + scrollbarWidth (부분 변환 — scrollbarWidth만 inline 잔존)

기존 (L333-L336):
```tsx
<div
  className="overflow-y-auto h-full"
  style={{ padding: '20px 28px', boxSizing: 'border-box', scrollbarWidth: 'none' } as React.CSSProperties}
>
```

After:
```tsx
<div
  className="overflow-y-auto h-full py-5 px-[28px] box-border"
  style={{ scrollbarWidth: 'none' } as React.CSSProperties}
>
```

- padding:'20px 28px' → `py-5 px-[28px]` (py-5=20px spacing override ✓ / px-7=32px 함정 회피 → `px-[28px]` arbitrary)
- boxSizing:'border-box' → `box-border`
- scrollbarWidth: 'none' → **inline 잔존 (옵션 N)**

**옵션**: X (px-[28px] arbitrary) + N (scrollbarWidth 부분 잔존)

#### L357 — table root

기존:
```tsx
<table style={{ width: '100%', borderCollapse: 'collapse', marginBottom: 20 }}>
```

After:
```tsx
<table className="w-full border-collapse mb-5">
```

#### L370 — kv-table th (loop, 9-prop)

기존:
```tsx
<th style={{ width: 110, padding: '8px 12px', background: 'var(--surface-sunken)', border: '1px solid var(--border-default)', fontSize: 12, fontWeight: 700, color: 'var(--text-secondary)', textAlign: 'left', verticalAlign: 'top', lineHeight: 1.5 }}>{label}</th>
```

After:
```tsx
<th className="w-[110px] py-2 px-3 bg-surface-sunken border border-border-default text-[12px] font-bold text-text-secondary text-left align-top leading-[1.5]">{label}</th>
```

#### L372 — kv-table td (loop, 7-prop)

기존:
```tsx
<td style={{ padding: '8px 12px', border: '1px solid var(--border-default)', fontSize: 13, color: 'var(--text-primary)', whiteSpace: 'pre-wrap', verticalAlign: 'top', lineHeight: 1.6 }}>
```

After:
```tsx
<td className="py-2 px-3 border border-border-default text-[13px] text-text-primary whitespace-pre-wrap align-top leading-[1.6]">
```

#### L400 — resolved th 조치일시 (L370 IDENTICAL)

기존:
```tsx
<th style={{ width: 110, padding: '8px 12px', background: 'var(--surface-sunken)', border: '1px solid var(--border-default)', fontSize: 12, fontWeight: 700, color: 'var(--text-secondary)', textAlign: 'left', verticalAlign: 'top', lineHeight: 1.5 }}>조치일시</th>
```

After:
```tsx
<th className="w-[110px] py-2 px-3 bg-surface-sunken border border-border-default text-[12px] font-bold text-text-secondary text-left align-top leading-[1.5]">조치일시</th>
```

#### L401 — resolved td 조치일시 (6-prop, L372 - whitespace - align-top)

기존:
```tsx
<td style={{ padding: '8px 12px', border: '1px solid var(--border-default)', fontSize: 13, color: 'var(--text-primary)', lineHeight: 1.6 }}>{fmtDateTime(detail.resolvedAt)}</td>
```

After:
```tsx
<td className="py-2 px-3 border border-border-default text-[13px] text-text-primary leading-[1.6]">{fmtDateTime(detail.resolvedAt)}</td>
```

#### L404 — resolved th 조치자 (L370 IDENTICAL)

기존:
```tsx
<th style={{ width: 110, padding: '8px 12px', background: 'var(--surface-sunken)', border: '1px solid var(--border-default)', fontSize: 12, fontWeight: 700, color: 'var(--text-secondary)', textAlign: 'left', verticalAlign: 'top', lineHeight: 1.5 }}>조치자</th>
```

After:
```tsx
<th className="w-[110px] py-2 px-3 bg-surface-sunken border border-border-default text-[12px] font-bold text-text-secondary text-left align-top leading-[1.5]">조치자</th>
```

#### L405 — resolved td 조치자 (L401 IDENTICAL)

기존:
```tsx
<td style={{ padding: '8px 12px', border: '1px solid var(--border-default)', fontSize: 13, color: 'var(--text-primary)', lineHeight: 1.6 }}>{detail.resolvedBy ?? '-'}</td>
```

After:
```tsx
<td className="py-2 px-3 border border-border-default text-[13px] text-text-primary leading-[1.6]">{detail.resolvedBy ?? '-'}</td>
```

#### L408 — resolved th 조치 내용 (L370 IDENTICAL)

기존:
```tsx
<th style={{ width: 110, padding: '8px 12px', background: 'var(--surface-sunken)', border: '1px solid var(--border-default)', fontSize: 12, fontWeight: 700, color: 'var(--text-secondary)', textAlign: 'left', verticalAlign: 'top', lineHeight: 1.5 }}>조치 내용</th>
```

After:
```tsx
<th className="w-[110px] py-2 px-3 bg-surface-sunken border border-border-default text-[12px] font-bold text-text-secondary text-left align-top leading-[1.5]">조치 내용</th>
```

#### L409 — resolved td 조치 내용 (7-prop, L372 - align-top)

기존:
```tsx
<td style={{ padding: '8px 12px', border: '1px solid var(--border-default)', fontSize: 13, color: 'var(--text-primary)', whiteSpace: 'pre-wrap', lineHeight: 1.6 }}>{detail.resolutionMemo ?? '-'}</td>
```

After:
```tsx
<td className="py-2 px-3 border border-border-default text-[13px] text-text-primary whitespace-pre-wrap leading-[1.6]">{detail.resolutionMemo ?? '-'}</td>
```

#### L412 — resolved th 소모 자재 (L370 IDENTICAL)

기존:
```tsx
<th style={{ width: 110, padding: '8px 12px', background: 'var(--surface-sunken)', border: '1px solid var(--border-default)', fontSize: 12, fontWeight: 700, color: 'var(--text-secondary)', textAlign: 'left', verticalAlign: 'top', lineHeight: 1.5 }}>소모 자재</th>
```

After:
```tsx
<th className="w-[110px] py-2 px-3 bg-surface-sunken border border-border-default text-[12px] font-bold text-text-secondary text-left align-top leading-[1.5]">소모 자재</th>
```

#### L413 — resolved td 소모 자재 (L409 IDENTICAL)

기존:
```tsx
<td style={{ padding: '8px 12px', border: '1px solid var(--border-default)', fontSize: 13, color: 'var(--text-primary)', whiteSpace: 'pre-wrap', lineHeight: 1.6 }}>{detail.materialsUsed ?? '-'}</td>
```

After:
```tsx
<td className="py-2 px-3 border border-border-default text-[13px] text-text-primary whitespace-pre-wrap leading-[1.6]">{detail.materialsUsed ?? '-'}</td>
```

**중요 보존**:
- L109-114 `const SKELETON_STYLE: React.CSSProperties = { ... }` 정의 그대로
- L299 desktop list `<div style={SKELETON_STYLE} />` × 3 (이미 inline 아님)
- L551/552/553 mobile `<div style={SKELETON_STYLE} />` × 3 (이미 inline 아님, 모바일 zone 절대 보존)
- L297 desktop scrollbar inline (옵션 N 잔존)
- L547 mobile scrollbar inline (모바일 zone 절대 보존)

### Step 3: After verification

```bash
cd /Users/jykevin/Documents/cbc7119-design/cha-bio-safety
F=src/pages/RemediationPage.tsx

# 1. style count drop
A=$(grep -c 'style={{' $F)
echo "after: $A (expect 3, target ≤ 3)"
[ "$A" -le "3" ] && echo "PASS inline count" || echo "FAIL inline count"

# 2. 데스크톱 zone 잔존 검증 (L281-L493 영역 — Wave 14a 후 라인 시프트 거의 없음)
# 변환 후 라인 시프트 가능 (multi-line → single-line 으로 인해 줄 감소)
# 새 데스크톱 zone end 라인 = `if (isDesktop)` 블록 닫힘 `}`
DSK_END_NEW=$(grep -n '^  }$' $F | awk -F: '$1 < 500 {print $1; exit}')
echo "desktop zone end (after): line $DSK_END_NEW"
DSK_AFTER=$(sed -n "281,${DSK_END_NEW}p" $F | grep -c 'style={{')
echo "desktop zone inline after: $DSK_AFTER (MUST = 2: L297 옵션 N + L335 부분 잔존)"
[ "$DSK_AFTER" = "2" ] && echo "PASS desktop zone" || echo "FAIL desktop zone"

# 3. 모바일 zone L547 (또는 시프트된 새 라인) IDENTICAL 검증
MOBILE_SCROLL=$(grep -n "scrollbarWidth: 'none' } as React.CSSProperties" $F | tail -1 | awk -F: '{print $1}')
echo "mobile scrollbar line (after): L$MOBILE_SCROLL"
# 모바일 inline 카운트 = 1
MOB_AFTER=$(sed -n "$((DSK_END_NEW+1)),\$p" $F | grep -c 'style={{')
echo "mobile zone inline after: $MOB_AFTER (MUST = 1)"
[ "$MOB_AFTER" = "1" ] && echo "PASS mobile zone preserved" || echo "FAIL mobile zone"

# 4. 비즈 anchor identity
{
  for ANCHOR in 'onClick=\{[^}]+\}' 'useState\(' 'useRef\(' 'useEffect\(' 'useMutation\(' 'useQuery\(' 'useNavigate\(' 'useParams\(' 'useSearchParams\(' 'fetch\('; do
    echo "  $ANCHOR : $(grep -cE "$ANCHOR" $F)"
  done
} > /tmp/q5a-after.txt
diff /tmp/q5a-before.txt /tmp/q5a-after.txt  # MUST empty
[ "$(diff /tmp/q5a-before.txt /tmp/q5a-after.txt | wc -l | tr -d ' ')" = "0" ] && echo "PASS biz anchor diff" || echo "FAIL biz anchor diff"

# 5. onClick handler bodies precise diff
grep -oE 'onClick=\{[^}]+\}' $F | sort | uniq > /tmp/q5a-clicks-after.txt
diff /tmp/q5a-clicks-before.txt /tmp/q5a-clicks-after.txt  # MUST empty
[ "$(diff /tmp/q5a-clicks-before.txt /tmp/q5a-clicks-after.txt | wc -l | tr -d ' ')" = "0" ] && echo "PASS onClick set diff" || echo "FAIL onClick set diff"

# 6. emoji 변동 0 (이미 0 이어야 함)
echo "emoji after: $(grep -cE '✓|✗|✕|🔒|💾|🔥|⏰|📋|✅|⚠️|❌|🔧|🚨|🔍|🧯|📊|▸|◂|▴|▾' $F)"

# 7. 비색
echo "비색: $(grep -cE 'bg-warning[^-]|border-safe[^-]|border-warning[^-]|border-danger[^-]' $F)"  # MUST = 0

# 8. SKELETON_STYLE const 보존
echo "SKELETON_STYLE const: $(grep -c 'const SKELETON_STYLE' $F)"  # MUST = 1
echo "SKELETON_STYLE refs: $(grep -c 'style={SKELETON_STYLE}' $F)"  # MUST = 6 (desktop 3 + mobile 3)

# 9. diff hunk 모바일 zone 영역 변경 0 검증
git diff $F > /tmp/q5a-diff.txt
echo "=== diff hunk headers ==="
grep -E '^@@' /tmp/q5a-diff.txt
# 자동 검증: 모바일 zone start = 변환 후 약 L495 이후
# 모든 hunk 가 L494 이전만 포함하는지 awk 검증
MOBILE_START=494
awk -v ms=$MOBILE_START '/^@@/ {
  match($0, /-([0-9]+),([0-9]+)/, m);
  start=m[1]; len=m[2];
  end=start+len-1;
  if (end >= ms) {
    print "MOBILE ZONE TOUCHED: " $0
  }
}' /tmp/q5a-diff.txt
# (출력 없으면 OK — 모바일 zone 절대 보존 확인)

# 10. TypeScript
TSC=$(./node_modules/.bin/tsc --noEmit 2>&1 | grep -c "error TS")
echo "TypeScript errors: $TSC"  # MUST = 0
[ "$TSC" = "0" ] && echo "PASS tsc" || echo "FAIL tsc"

# 11. off-scope 변경
cd /Users/jykevin/Documents/cbc7119-design
OFF=$(git diff --name-only HEAD | grep -v "cha-bio-safety/src/pages/RemediationPage.tsx" | grep -v ".planning/" | wc -l | tr -d ' ')
echo "off-scope files: $OFF (MUST = 0)"
[ "$OFF" = "0" ] && echo "PASS off-scope" || echo "FAIL off-scope"
cd cha-bio-safety
git diff --name-only HEAD  # MUST = src/pages/RemediationPage.tsx 1줄만 (외 .planning 가능)

# 12. vite build
./node_modules/.bin/vite build 2>&1 | tail -5
```

### Step 4: Atomic commit

```bash
cd /Users/jykevin/Documents/cbc7119-design
git add cha-bio-safety/src/pages/RemediationPage.tsx
git commit --no-verify -m "$(cat <<'EOF'
feat(260529-q5a-01): Phase B Wave 14b — Remediation 데스크톱 zone (14 inline) → tailwind

Tier 2 페어 wave 세 번째 완결 (Wave 12a+12b StaffManage / 13a+13b Checkpoints 직후 14a+14b Remediation 완결).
데스크톱 zone 14건 중 12건 변환 + 2건 옵션 N 잔존 (L297 scrollbarWidth + L335 부분 잔존 scrollbarWidth).
모바일 zone L547 절대 변경 0 (Wave 14a 잔존 옵션 N IDENTICAL).
SKELETON_STYLE module const 정의 + 직접 참조 6건 (desktop 3 + mobile 3) 보존.
토큰 alias 매핑 (var(--surface-sunken/border-default/text-secondary/text-primary) → bg-/border-/text-* tailwind tokens).
kv-table th/td 11건 loop+resolved branch IDENTICAL 패턴 일괄 변환 (옵션 X+token).
옵션 X+P+M+색변수N + 19번째 atomic 자동 도달.
시각 0 byte 변경. 비즈 anchor identical (9 onClick / 2 useState / 2 useQuery / 1 useNavigate / 1 useSearchParams / 2 fetch).
페어 합산 (14a+14b) 25 → 3 (-88.0%) RemediationPage 완결.
emoji 0 유지. TypeScript 0 error.
EOF
)"
```

### Step 5: SUMMARY.md

Write `cha-bio-safety/.planning/quick/260529-q5a-phase-b-wave-14b/260529-q5a-SUMMARY.md`.

Tier 2 페어 wave 세 번째 완결. 14a (gj2) + 14b (q5a) 페어 합산 25 → 3 (-88.0%). 19번째 atomic precedent.
PLAN.md 의 zone 매핑 표 + 실제 diff 통계 + 옵션 N 잔존 라인 (L297/L335/L547) + 모바일 zone 보존 검증 + 부분 변환 패턴 (L335) 박제.

Do NOT commit SUMMARY.md.

  </action>
  <verify>
    <automated>cd /Users/jykevin/Documents/cbc7119-design/cha-bio-safety && S=$(grep -c 'style={{' src/pages/RemediationPage.tsx) && echo "after=$S (was 15, expect 3)" && [ "$S" -le "3" ] && DSK_END_NEW=$(grep -n '^  }$' src/pages/RemediationPage.tsx | awk -F: '$1 < 500 {print $1; exit}') && DSK=$(sed -n "281,${DSK_END_NEW}p" src/pages/RemediationPage.tsx | grep -c 'style={{') && echo "desktop=$DSK (MUST=2)" && [ "$DSK" = "2" ] && MOB=$(sed -n "$((DSK_END_NEW+1)),\$p" src/pages/RemediationPage.tsx | grep -c 'style={{') && echo "mobile=$MOB (MUST=1)" && [ "$MOB" = "1" ] && [ "$(./node_modules/.bin/tsc --noEmit 2>&1 | grep -c 'error TS')" = "0" ] && [ "$(grep -cE 'bg-warning[^-]|border-safe[^-]|border-warning[^-]|border-danger[^-]' src/pages/RemediationPage.tsx)" = "0" ] && [ "$(grep -c 'const SKELETON_STYLE' src/pages/RemediationPage.tsx)" = "1" ] && [ "$(grep -c 'style={SKELETON_STYLE}' src/pages/RemediationPage.tsx)" = "6" ] && cd .. && [ "$(git diff --name-only HEAD | grep -v 'cha-bio-safety/src/pages/RemediationPage.tsx' | grep -v '.planning/' | wc -l | tr -d ' ')" = "0" ]</automated>
  </verify>
  <done>
    - RemediationPage inline 15 → 3 (target ≤ 3; 데스크 14 → 2 + 모바일 1 → 1)
    - 데스크톱 zone (L281-L493 → 변환 후 라인 시프트) 잔존 2건 (L297 옵션 N + L335 부분 옵션 N)
    - 모바일 zone L547 (또는 시프트 후 라인) 변경 0 (옵션 N IDENTICAL)
    - SKELETON_STYLE module const 정의 보존 (1 const def) + 직접 참조 6건 보존 (3 desktop + 3 mobile)
    - 비즈 anchor precise diff = empty (9 onClick / 2 useState / 2 useQuery / 1 useNavigate / 1 useSearchParams / 2 fetch IDENTICAL)
    - onClick handler set diff = empty (7 unique handlers all preserved)
    - emoji 0 유지 (RemediationPage 는 이미 Lucide)
    - 비색 0 (status- prefix 없는 토큰 0)
    - TypeScript 0 error
    - vite build PASS
    - 변경 파일 = 1 .tsx 만 (off-scope = 0)
    - 단일 atomic commit (19번째)
    - diff hunk 모바일 zone 영역 변경 0 (awk 검증 출력 empty)
    - 페어 wave (14a+14b) 합산 25 → 3 (-88.0%) RemediationPage 완결
  </done>
</task>

</tasks>

<verification>

| 게이트 | 기댓값 |
|---|---|
| `style={{` total | 15 → 3 (-12, -80.0%) |
| 데스크톱 zone (L281-L493) | 14 → 2 (L297 옵션 N + L335 부분 옵션 N) |
| 모바일 zone (L494~) | 1 → 1 (L547 IDENTICAL) |
| SKELETON_STYLE const def | 1 (보존) |
| SKELETON_STYLE refs | 6 (보존) |
| 비즈 anchor diff | empty |
| onClick set diff | empty |
| emoji | 0 → 0 |
| 비색 | 0 |
| TypeScript errors | 0 |
| vite build | PASS |
| off-scope 변경 | 0 |
| 단일 atomic commit | 1 |
| 페어 합산 (14a+14b) | 25 → 3 (-88.0%) |

</verification>

<success_criteria>

- RemediationPage 데스크톱 zone 12 inline tailwind 변환 (kv-table th/td 11 + table root 1)
- 옵션 N 잔존 2건 (L297 desktop scrollbar + L335 부분 scrollbarWidth)
- 모바일 zone L547 IDENTICAL (옵션 N)
- SKELETON_STYLE module const 정의 + 직접 참조 6건 보존
- 페어 wave 14a + 14b 합산 25 → 3 (-88.0%) RemediationPage 완결
- 19번째 atomic commit 자동 도달
- 시각 0 byte 변경 (no-op refactor)
- TypeScript 0 error, vite build PASS

</success_criteria>

<output>
After completion, create `cha-bio-safety/.planning/quick/260529-q5a-phase-b-wave-14b/260529-q5a-SUMMARY.md` with:
- Before / After 카운트 표 (페어 14a+14b 합산 포함)
- 데스크톱 zone sweep 매핑 표 (12건 변환 + 2건 옵션 N)
- 모바일 zone 보존 1건 (L547) IDENTICAL 확인
- 옵션 N 잔존 3건 (L297 + L335 + L547) 영역 표
- 부분 변환 패턴 (L335 scrollbarWidth) 박제 — 첫 사례
- SKELETON_STYLE module const 보존 확인
- 자동 검증 결과 (게이트 12개 PASS)
- commit hash
- Tier 2 페어 wave 누적 진행 표 (12a/12b/13a/13b/14a/14b)
- 14a + 14b 페어 완결 패턴 박제 (세 번째 페어, early-return 구조 첫 페어)
</output>
