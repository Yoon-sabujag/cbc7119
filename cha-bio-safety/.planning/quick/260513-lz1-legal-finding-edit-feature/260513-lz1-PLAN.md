---
phase: quick-260513-lz1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - functions/api/legal/[id]/findings/[fid].ts
  - src/components/FindingFormSheet.tsx
  - src/pages/LegalFindingsPage.tsx
  - src/pages/LegalPage.tsx
autonomous: true
requirements:
  - LEGAL-FINDING-EDIT
must_haves:
  truths:
    - "사용자(yoon, assistant)가 종합정밀/작동기능 라운드의 지적사항 카드에서 '수정' 버튼을 누르면 등록 시 사용한 것과 동일한 picker UX(구역/층/지적항목/위치상세/내용/사진 5장)가 prefill 된 상태로 열린다"
    - "수정 sheet 에서 저장하면 PUT /api/legal/:id/findings/:fid 가 호출되고 목록(legal-findings, legal-round, legal-rounds) 이 즉시 갱신된다"
    - "수정 버튼은 모바일(LegalFindingsPage)과 데스크톱(LegalPage > FindingsPanel) 양쪽 카드 모두 '삭제' 왼쪽에 표시된다"
    - "assistant 권한 사용자도 수정에 성공한다 (PUT admin-only 체크 제거됨)"
    - "기존 지적사진은 미리보기 타일로 보이고 각각 ✕로 제거 가능하며, 새 사진과 합쳐 총 5장 이하 제약이 유지된다"
    - "description / location 의 reverse parsing 결과가 picker 에 prefill 되고, 매칭 실패한 토큰은 description(전체) 또는 locationDetail 로 fallback 된다"
  artifacts:
    - path: "src/components/FindingFormSheet.tsx"
      provides: "공유 finding form sheet (등록/수정 mode 겸용)"
      contains: "export function FindingFormSheet"
    - path: "functions/api/legal/[id]/findings/[fid].ts"
      provides: "PUT admin-only 제거 (DELETE 와 정합)"
      contains: "onRequestPut"
  key_links:
    - from: "src/pages/LegalFindingsPage.tsx 카드 '수정' 버튼"
      to: "FindingFormSheet (mode='edit', initial finding)"
      via: "useState<LegalFinding | null>(editingFinding)"
      pattern: "FindingFormSheet.*mode=.edit"
    - from: "src/pages/LegalPage.tsx FindingsPanel 카드 '수정' 버튼"
      to: "FindingFormSheet (mode='edit', initial finding)"
      via: "useState<LegalFinding | null>(editingFinding)"
      pattern: "FindingFormSheet.*mode=.edit"
    - from: "FindingFormSheet (mode='edit') 저장"
      to: "legalApi.updateFinding"
      via: "useMutation"
      pattern: "legalApi\\.updateFinding"
---

<objective>
종합정밀/작동기능 점검 라운드의 지적사항 카드에 '수정' 기능을 추가한다.
등록 시 사용한 picker UX 를 동일하게 재사용하며 (B안), 모바일·데스크톱 양쪽 진입점에서 동작하고 assistant 권한 사용자도 수정할 수 있어야 한다.

Purpose: 현재는 카드에 '삭제' 버튼만 있고 잘못 입력한 description / location / 사진을 고치려면 삭제 후 재등록해야 함 → 5월 법정 운영 모드 중 실 사용 불편 해소.
Output: FindingFormSheet 공유 컴포넌트 + LegalFindingsPage / LegalPage 카드 '수정' 버튼 + 백엔드 PUT admin 체크 제거.
</objective>

<context>
@.planning/STATE.md
@CLAUDE.md
@src/pages/LegalFindingsPage.tsx
@src/pages/LegalPage.tsx
@src/utils/api.ts
@functions/api/legal/[id]/findings/[fid].ts
@src/hooks/useMultiPhotoUpload.ts
@src/types/index.ts

<interfaces>
<!-- Extracted from existing code — executor uses directly, no exploration needed. -->

From src/types/index.ts:
```typescript
export type LegalFindingStatus = 'open' | 'resolved'
export interface LegalFinding {
  id: string
  scheduleItemId: string
  description: string
  location: string | null
  photoKey: string | null
  photoKeys: string[]              // array of R2 keys (multi-photo)
  resolutionMemo: string | null
  resolutionPhotoKey: string | null
  resolutionPhotoKeys: string[]
  status: LegalFindingStatus
  resolvedAt: string | null
  resolvedBy: string | null
  resolvedByName: string | null
  createdBy: string
  createdByName: string | null
  createdAt: string
}
```

From src/utils/api.ts:
```typescript
legalApi.createFinding(scheduleItemId, { description, location?, photo_keys? }): Promise<{id:string}>
legalApi.updateFinding(scheduleItemId, fid, body: Record<string,any>): Promise<void>
// 백엔드 PUT 가 받는 필드: description, location, photo_keys (0-5)
```

From src/hooks/useMultiPhotoUpload.ts (already in use by existing FindingBottomSheet):
```typescript
const photos = useMultiPhotoUpload()
// photos.slots, photos.canAdd (slots.length < 5), photos.handleFiles,
// photos.removeSlot, photos.uploadAll(): Promise<string[]>, photos.reset()
```

From src/pages/LegalFindingsPage.tsx (constants that must be re-exported or duplicated in the shared component):
```typescript
const ZONES = [
  { key: 'research', label: '연구동' },
  { key: 'office',   label: '사무동' },
  { key: 'bridge',   label: '브릿지' },
  { key: 'basement', label: '지하' },
] as const
const ZONE_FLOORS: Record<string, string[]> = {
  research: ['8-1F','8F','7F','6F','5F','3F','2F','1F'],
  office:   ['8-1F','8F','7F','6F','5F','3F','2F','1F'],
  bridge:   ['7F','6F','5F','3F'],
  basement: ['B1','M','B2','B3','B4','B5'],
}
const FINDING_ITEMS = [
  '직접입력',
  '감지기(불꽃)','감지기(열)','감지기(연기)',
  '방화문','방화셔터','비상방송설비','비상콘센트',
  'DIV','소방펌프','소화기','소화전','스프링클러','시각경보기',
  '완강기','유도등','자동화재탐지설비',
  '전실제연댐퍼','청정소화약제',
]
```

From src/components/PhotoSourceModal.tsx (already imported by both pages):
```typescript
<PhotoSourceModal open onClose onCamera onAlbum />
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 백엔드 PUT admin 체크 제거</name>
  <files>functions/api/legal/[id]/findings/[fid].ts</files>
  <action>
`functions/api/legal/[id]/findings/[fid].ts` 의 `onRequestPut` 핸들러에서 admin-only 가드를 제거한다.

- 현재 80-82 라인의 다음 블록을 삭제:
  ```ts
  if (role !== 'admin') {
    return Response.json({ success: false, error: '관리자만 가능합니다' }, { status: 403 })
  }
  ```
- 동시에 75 라인 시그니처에서 더 이상 사용하지 않는 `data` 구조분해 변수 `role` 도 제거 (예: `data` 자체를 destructure 하지 않거나, `const { } = data as any` 식이 아닌 그냥 `data` 를 안 받게 정리).
  실제로 `data` 가 이 핸들러에서 다른 용도로 쓰이지 않으므로 `({ request, env, params })` 만 받도록 단순화한다.
- DELETE 핸들러(137 라인부터)에 admin 체크가 없는 것과 정합되도록 한 후속 결정에 따른 것이며, 새 권한 체크를 추가하지 않는다.
- 그 외 기존 동작(존재 확인 → COALESCE 부분 업데이트 → photo_keys 0-5 배열 검증)은 그대로 유지.
- 파일 다른 핸들러(`onRequestGet`, `onRequestDelete`) 는 손대지 않는다.
  </action>
  <verify>
    <automated>grep -n "관리자만 가능합니다" functions/api/legal/\[id\]/findings/\[fid\].ts; test $(grep -c "role !== 'admin'" functions/api/legal/\[id\]/findings/\[fid\].ts) -eq 0</automated>
  </verify>
  <done>
- `onRequestPut` 에서 admin 체크 블록이 제거되어 assistant 토큰으로도 PUT 200/success 가 돌아온다 (런타임 검증은 Task 3 의 빌드 + 사용자 수동 PWA 확인에서 커버).
- `npm run build` 가 TypeScript 에러 없이 통과한다.
  </done>
</task>

<task type="auto">
  <name>Task 2: FindingFormSheet 공유 컴포넌트 추출 (등록/수정 mode + reverse parse + existingKeys)</name>
  <files>src/components/FindingFormSheet.tsx</files>
  <action>
새 파일 `src/components/FindingFormSheet.tsx` 를 생성한다. 이는 LegalFindingsPage 내부에 inlined 되어 있던 `FindingBottomSheet` 컴포넌트(66-322 라인) 를 그대로 추출 + 확장한 것으로, 등록(create) 과 수정(edit) 양쪽 mode 를 처리한다.

**Props (정확히 이 시그니처):**
```ts
import type { LegalFinding } from '../types'
export interface FindingFormSheetProps {
  scheduleItemId: string
  mode: 'create' | 'edit'
  finding?: LegalFinding   // mode='edit' 시 필수
  onClose: () => void
}
export function FindingFormSheet(props: FindingFormSheetProps): JSX.Element
```

**복사 베이스:** `src/pages/LegalFindingsPage.tsx` 66-322 라인의 `FindingBottomSheet` 마크업/스타일/PhotoSourceModal/floorChipStyle/chipStyle/inputStyle/lblStyle/isDesktopSheet 분기 등을 그대로 가져온다. ZONES / ZONE_FLOORS / FINDING_ITEMS 상수도 이 파일에 그대로 둔다 (LegalFindingsPage 측에서는 Task 3 에서 그대로 두거나 import — 아래 Task 3 지시 참고).

**create mode (기존 동작 그대로 보존):**
- 모든 입력값은 빈 문자열로 시작
- 등록 버튼 라벨 "등록", 헤더 타이틀 "지적사항 등록"
- mutation: `legalApi.createFinding(scheduleItemId, { description: [item, description.trim()].filter(Boolean).join(' — '), location: loc || undefined, photo_keys: photoKeys.length>0 ? photoKeys : undefined })`
- onSuccess invalidate: ['legal-findings', scheduleItemId], ['legal-rounds'], ['legal-round', scheduleItemId]
- toast: '지적사항이 등록되었습니다.' / '등록에 실패했습니다. 다시 시도해 주세요.'

**edit mode (신규):**
- 헤더 타이틀 "지적사항 수정", 제출 버튼 라벨 "저장"
- `existingKeys: string[]` 로컬 state: 초기값 = `finding!.photoKeys` (LegalFinding.photoKeys 배열 그대로)
- mutation: 새로 업로드된 키와 합쳐서 `legalApi.updateFinding(scheduleItemId, finding!.id, { description: ..., location: ... (없으면 null), photo_keys: [...existingKeys, ...newKeys] })`. 백엔드는 photo_keys 0-5 검증함.
- onSuccess invalidate: ['legal-findings', scheduleItemId], ['legal-rounds'], ['legal-round', scheduleItemId], 그리고 `['legal-finding', scheduleItemId, finding!.id]` (LegalPage FindingDetailPanel 캐시).
- toast: '수정되었습니다.' / '수정에 실패했습니다. 다시 시도해 주세요.'

**Reverse parse helper (파일 상단 또는 컴포넌트 위에 둠):**
```ts
function parseFindingForEdit(
  f: LegalFinding,
  zones: typeof ZONES,
  zoneFloors: typeof ZONE_FLOORS,
  findingItems: string[],
): {
  zone: string
  floor: string
  locationDetail: string
  inspectionItem: string
  customItem: string
  description: string
} {
  // description: 첫 ' — ' 토큰이 FINDING_ITEMS 안에 있으면 inspectionItem 으로,
  // 그 외 케이스에선 직접입력 처리 없이 전체를 description 에 둔다.
  let inspectionItem = ''
  let customItem = ''
  let description = f.description ?? ''
  const sep = ' — '
  const idx = description.indexOf(sep)
  if (idx > 0) {
    const head = description.slice(0, idx)
    const tail = description.slice(idx + sep.length)
    if (findingItems.includes(head) && head !== '직접입력') {
      inspectionItem = head
      description = tail
    }
    // head 가 리스트에 없으면 전체를 description 에 유지 (직접입력으로 강제하지 않음)
  }

  // location: "${zoneLabel} ${floor} ${rest...}" 첫 토큰을 ZONES.label 와 매칭,
  // 두 번째 토큰을 ZONE_FLOORS[zone] 와 매칭, 나머지는 locationDetail.
  let zone = ''
  let floor = ''
  let locationDetail = ''
  const loc = (f.location ?? '').trim()
  if (loc) {
    const parts = loc.split(/\s+/)
    const matchedZone = zones.find(z => z.label === parts[0])
    if (matchedZone) {
      zone = matchedZone.key
      const floors = zoneFloors[matchedZone.key] ?? []
      if (parts[1] && floors.includes(parts[1])) {
        floor = parts[1]
        locationDetail = parts.slice(2).join(' ')
      } else {
        locationDetail = parts.slice(1).join(' ')
      }
    } else {
      locationDetail = loc
    }
  }

  return { zone, floor, locationDetail, inspectionItem, customItem, description }
}
```

useState 초기값 셋업:
```ts
const initial = props.mode === 'edit' && props.finding
  ? parseFindingForEdit(props.finding, ZONES, ZONE_FLOORS, FINDING_ITEMS)
  : { zone:'', floor:'', locationDetail:'', inspectionItem:'', customItem:'', description:'' }
const [zone, setZone] = useState(initial.zone)
const [floor, setFloor] = useState(initial.floor)
const [locationDetail, setLocationDetail] = useState(initial.locationDetail)
const [inspectionItem, setInspectionItem] = useState(initial.inspectionItem)
const [customItem, setCustomItem] = useState(initial.customItem)
const [description, setDescription] = useState(initial.description)
const [existingKeys, setExistingKeys] = useState<string[]>(
  props.mode === 'edit' && props.finding ? [...props.finding.photoKeys] : []
)
```

**기존 사진 미리보기 (edit mode 전용):**
- "지적 사진 (최대 5장)" 섹션에서 photos.slots 렌더 직전에 existingKeys 를 동일한 72×72 타일로 렌더:
  ```tsx
  {existingKeys.map((k, i) => (
    <div key={`ex-${k}`} style={{ position: 'relative', flexShrink: 0 }}>
      <img src={`/api/uploads/${k}`} alt="" style={{ width:72, height:72, objectFit:'cover', borderRadius:10, border:'1px solid var(--bd)', display:'block' }} />
      <button
        aria-label="기존 사진 제거"
        onClick={() => setExistingKeys(prev => prev.filter((_, j) => j !== i))}
        style={{ position:'absolute', top:-6, right:-6, width:20, height:20, borderRadius:'50%', background:'var(--danger)', border:'none', color:'#fff', fontSize:11, fontWeight:700, cursor:'pointer', display:'flex', alignItems:'center', justifyContent:'center', lineHeight:1 }}
      >✕</button>
    </div>
  ))}
  ```
- `canAdd` 조건을 `existingKeys.length + photos.slots.length < 5` 로 교체. 즉 기존 코드의 `{photos.canAdd && (...)}` 를 `{(existingKeys.length + photos.slots.length < 5) && (...)}` 로 바꾼다 (photos.canAdd 자체는 hook 내부 보조용으로 남겨둬도 되지만 add 버튼 렌더 조건은 합산 기준).
- create mode 에선 existingKeys 가 항상 빈 배열이므로 동작이 기존과 동일.

**submit 분기:**
```ts
const handleSubmit = () => {
  if (!description.trim()) { toast.error('지적 내용을 입력하세요'); return }
  if (props.mode === 'edit' && existingKeys.length + photos.slots.length > 5) {
    toast.error('사진은 최대 5장입니다'); return
  }
  mutation.mutate()
}
```

mutation 내부:
```ts
const newKeys = await photos.uploadAll()
const loc = [
  ZONES.find(z => z.key === zone)?.label,
  floor,
  locationDetail.trim(),
].filter(Boolean).join(' ')
const item = inspectionItem === '직접입력' ? customItem.trim() : inspectionItem
const desc = [item, description.trim()].filter(Boolean).join(' — ')

if (props.mode === 'create') {
  return legalApi.createFinding(scheduleItemId, {
    description: desc,
    location: loc || undefined,
    photo_keys: newKeys.length > 0 ? newKeys : undefined,
  })
}
// edit
const mergedKeys = [...existingKeys, ...newKeys]
return legalApi.updateFinding(scheduleItemId, props.finding!.id, {
  description: desc,
  location: loc.length > 0 ? loc : null,
  photo_keys: mergedKeys,
})
```

**비주얼 / 시트 구조:**
- isDesktopSheet 분기, 모바일 슬라이드업 시트, 데스크톱 중앙 모달, 닫기 ✕, 취소 버튼, slideUp keyframes 등 모두 기존 FindingBottomSheet 와 동일하게 유지.
- 헤더 타이틀과 submit 버튼 라벨만 mode 에 따라 다르게 ('지적사항 등록' / '지적사항 수정', '등록' / '저장').
- 이 컴포넌트는 PWA 캐시 우려를 줄이기 위해 새 파일로 만들고 (memo MEMORY: feedback_pwa_cache_invalidation 참고), 기존 인라인 컴포넌트를 Task 3 에서 제거한다.

**Import 정리:**
- `import { useState } from 'react'`
- `import { useMutation, useQueryClient } from '@tanstack/react-query'`
- `import toast from 'react-hot-toast'`
- `import { legalApi } from '../utils/api'`
- `import { useMultiPhotoUpload } from '../hooks/useMultiPhotoUpload'`
- `import { PhotoSourceModal } from './PhotoSourceModal'`
- `import type { LegalFinding } from '../types'`

레이아웃/CSS 변경(시안 먼저 룰) 없음 — 기존 시트 구조 그대로, 텍스트/조건만 mode 분기.
  </action>
  <verify>
    <automated>test -f src/components/FindingFormSheet.tsx && grep -q "export function FindingFormSheet" src/components/FindingFormSheet.tsx && grep -q "parseFindingForEdit" src/components/FindingFormSheet.tsx && grep -q "legalApi.updateFinding" src/components/FindingFormSheet.tsx && grep -q "existingKeys" src/components/FindingFormSheet.tsx && grep -q "mode === 'edit'" src/components/FindingFormSheet.tsx</automated>
  </verify>
  <done>
- `src/components/FindingFormSheet.tsx` 가 존재하고 `FindingFormSheet` 를 named export 한다.
- create mode 는 기존 `FindingBottomSheet` 와 동일한 페이로드/invalidation/토스트로 동작.
- edit mode 는 prefill, 기존 사진 개별 제거, 5장 합산 제한, mergedKeys PUT 까지 수행.
- 이 task 만으로는 아직 사용처가 없으므로 빌드는 다음 task 까지 통과 보장.
  </done>
</task>

<task type="auto">
  <name>Task 3: LegalFindingsPage / LegalPage 카드에 '수정' 버튼 + 인라인 FindingBottomSheet 제거 + build 검증</name>
  <files>src/pages/LegalFindingsPage.tsx, src/pages/LegalPage.tsx</files>
  <action>
**(A) src/pages/LegalFindingsPage.tsx:**
1. 인라인 컴포넌트 `FindingBottomSheet` 정의(66-322 라인) 와 그 위의 `BottomSheetProps` 인터페이스 제거. ZONES / ZONE_FLOORS / FINDING_ITEMS 상수는 그대로 둬도 되고 제거해도 무방하지만, **현재 이 파일 내 다른 곳에서 참조하지 않으므로 모두 제거**한다 (FindingFormSheet 가 내부에 자체 보유). 단 fmtDate / fmtMonthOnly / Spinner / SKELETON_STYLE / handleDeleteFinding 등 다른 로직은 유지.
2. 상단 import 에 다음을 추가:
   ```ts
   import { FindingFormSheet } from '../components/FindingFormSheet'
   ```
   더 이상 사용하지 않는 import 정리:
   - `useMultiPhotoUpload`, `PhotoSourceModal` 는 이 파일에서 다른 곳에 안 쓰이면 제거. (현재 BottomSheet 외에는 사용 안 함 → 제거)
   - `LegalFinding` 타입은 카드 props 와 handleDeleteFinding 시그니처에 쓰이고 있으므로 유지.
3. 상단의 `useState/useRef` 이미 있는 곳 옆에:
   ```ts
   const [editingFinding, setEditingFinding] = useState<LegalFinding | null>(null)
   ```
   기존 `const [showSheet, setShowSheet] = useState(false)` 는 등록 시트용으로 그대로 유지.
4. `findingCard` 내부, 카드 하단의 메타 + 삭제 버튼이 있는 row(540-543 라인 즈음) 에서 삭제 버튼 **왼쪽**에 '수정' 버튼을 추가. 두 버튼은 같은 우측 그룹으로 묶는다:
   ```tsx
   <div style={{ display:'flex', alignItems:'center', gap: 8 }}>
     <button
       onClick={(e) => { e.stopPropagation(); setEditingFinding(finding) }}
       style={{ fontSize: 10, color: 'var(--t3)', background: 'none', border: 'none', cursor: 'pointer', padding: '2px 4px' }}
     >수정</button>
     <button onClick={(e) => handleDeleteFinding(e, finding)} style={{ fontSize: 10, color: 'var(--t3)', background: 'none', border: 'none', cursor: 'pointer', padding: '2px 4px' }}>삭제</button>
   </div>
   ```
   즉 기존 메타 row 의 우측 child 가 단일 `<button>삭제</button>` 였던 것을 위 wrapper 로 교체. 좌측 작성자/날짜 span 은 그대로 유지. 스타일/사이즈/색은 삭제 버튼과 동일하게 맞춤(시안 먼저 룰: 새 비주얼 추가 없음, 동일 톤 재사용).
5. 페이지 하단의 등록 시트 렌더 부분을 다음으로 교체:
   ```tsx
   {showSheet && id && (
     <FindingFormSheet scheduleItemId={id} mode="create" onClose={() => setShowSheet(false)} />
   )}
   {editingFinding && id && (
     <FindingFormSheet
       scheduleItemId={id}
       mode="edit"
       finding={editingFinding}
       onClose={() => setEditingFinding(null)}
     />
   )}
   ```

**(B) src/pages/LegalPage.tsx (FindingsPanel 데스크톱 카드):**
1. 상단 import 추가:
   ```ts
   import { FindingFormSheet } from '../components/FindingFormSheet'
   ```
   `FindingsPanel` 컴포넌트 내부에 다음 state 추가 (handleDelete 정의 근처):
   ```ts
   const [editingFinding, setEditingFinding] = useState<LegalFinding | null>(null)
   ```
2. 카드 메타 row(201-204 라인, 날짜 span + 삭제 button) 우측의 삭제 버튼 왼쪽에 동일 톤의 '수정' 버튼 추가. wrapper 패턴 동일:
   ```tsx
   <div style={{ display:'flex', alignItems:'center', gap: 6 }}>
     <button
       onClick={e => { e.stopPropagation(); setEditingFinding(f) }}
       style={{ fontSize: 10, color: 'var(--t3)', background: 'none', border: 'none', cursor: 'pointer', padding: '1px 3px' }}
     >수정</button>
     <button onClick={e => { e.stopPropagation(); handleDelete(f) }} style={{ fontSize: 10, color: 'var(--t3)', background: 'none', border: 'none', cursor: 'pointer', padding: '1px 3px' }}>삭제</button>
   </div>
   ```
   기존 날짜 span 은 그대로 좌측에 유지 (`<span style={{ fontSize: 10, color: 'var(--t3)' }}>{fmtDate(f.createdAt)}</span>`).
3. `FindingsPanel` return 의 최상위 `<div>` 끝(208 라인 직전) 안쪽에 모달 렌더 추가 (목록 div 와 동일 부모):
   ```tsx
   {editingFinding && (
     <FindingFormSheet
       scheduleItemId={roundId}
       mode="edit"
       finding={editingFinding}
       onClose={() => setEditingFinding(null)}
     />
   )}
   ```
   FindingFormSheet 가 자체로 fixed/inset:0 오버레이를 그리므로 추가 wrapper 없이 OK.
4. `LegalRound` 등 다른 import 는 손대지 않는다.

**(C) 빌드 검증:**
- `npm run build` 통과해야 함. TypeScript strict:false 이지만 unused import 가 esbuild/vite-pwa 단계에서 문제될 수 있음 — A 단계의 import 정리에 신경.
- iOS PWA 캐시 우회 위해 사용자 안내 메시지(터미널 출력)는 따로 필요 없음. 다만 commit 메시지에 변경 사실이 드러나야 함.

**(D) 디자인 변경 가드 (MEMORY: feedback_design_changes_ask_first, feedback_design_sketch_first):**
- 이번 변경은 "삭제 버튼 옆에 같은 톤의 텍스트 버튼 1개 추가" 와 "기존 시트 컴포넌트를 별도 파일로 이동" 두 가지로, 레이아웃/카드 구조/타이포는 그대로 유지된다.
- 카드 padding/높이/줄간격/border 등 어떤 시각 변수도 새로 만들거나 변경하지 않는다.
  </action>
  <verify>
    <automated>grep -q "FindingFormSheet" src/pages/LegalFindingsPage.tsx && grep -q "FindingFormSheet" src/pages/LegalPage.tsx && grep -q "setEditingFinding" src/pages/LegalFindingsPage.tsx && grep -q "setEditingFinding" src/pages/LegalPage.tsx && test $(grep -c "function FindingBottomSheet" src/pages/LegalFindingsPage.tsx) -eq 0 && npm run build</automated>
  </verify>
  <done>
- 모바일 카드의 카드 메타 row 우측에 '수정' / '삭제' 두 버튼이 순서대로 표시되고, '수정' 클릭 시 FindingFormSheet(edit) 가 열려 finding 의 description/location/photoKeys 가 prefill 된다.
- 데스크톱 FindingsPanel 카드도 동일하게 동작.
- 인라인 `FindingBottomSheet` 정의가 LegalFindingsPage 에서 제거되고, 동일 페이지의 등록 동선은 FindingFormSheet(mode='create') 로 대체되었음에도 기존 등록 흐름이 회귀 없이 동작한다 (필드/페이로드 동일).
- `npm run build` 가 TypeScript / vite-pwa 빌드 단계까지 에러 없이 통과한다.
  </done>
</task>

</tasks>

<verification>
- `npm run build` 가 성공한다 (TypeScript + vite + PWA precache 단계 통과).
- `grep -c "function FindingBottomSheet" src/pages/LegalFindingsPage.tsx` 결과가 0.
- `grep -c "FindingFormSheet" src/pages/LegalFindingsPage.tsx src/pages/LegalPage.tsx` 각 파일별 1 이상.
- `grep -c "role !== 'admin'" functions/api/legal/\[id\]/findings/\[fid\].ts` 결과가 0.
- 수동 PWA 검증 (실배포 전, 사용자 컨펌 후 production 배포 — feedback_deploy_test 룰):
  1. assistant 계정으로 모바일 PWA 종합정밀 라운드 진입 → 지적사항 카드 '수정' 탭.
  2. 시트가 열리고 description/location/사진이 prefill 되는지 확인.
  3. 사진 한 장 제거, 새 사진 한 장 추가, 텍스트 일부 수정 후 저장.
  4. 카드 description / 사진 카운트가 갱신되고 토스트 '수정되었습니다.' 표시.
  5. 데스크톱에서 동일 라운드의 카드 '수정' 탭 후 동일 시나리오 반복.
</verification>

<success_criteria>
- assistant 권한 사용자가 모바일/데스크톱 양쪽에서 종합정밀·작동기능 지적사항을 수정할 수 있다.
- 수정 UX 가 등록 UX 와 동일한 picker/사진 인터페이스 (B안) 를 사용한다.
- 기존 등록 흐름은 회귀 없이 동작한다.
- 기존 디자인 톤이 유지된다 (수정 버튼은 삭제 버튼과 동일한 텍스트 버튼 스타일).
- `npm run build` 통과.
</success_criteria>

<output>
After completion, create `.planning/quick/260513-lz1-legal-finding-edit-feature/260513-lz1-SUMMARY.md` summarizing:
- 추가된 파일: src/components/FindingFormSheet.tsx
- 변경된 파일: src/pages/LegalFindingsPage.tsx, src/pages/LegalPage.tsx, functions/api/legal/[id]/findings/[fid].ts
- 백엔드 PUT admin 체크 제거 사실 (DELETE 와 정합)
- reverse parsing 룰 (description ' — ' split, location 공백 split + ZONES/ZONE_FLOORS 매칭)
- 사진 5장 합산 제한 (existingKeys + new slots)
- iOS PWA 캐시 우려 → 사용자 PWA 앱 재설치 안내 필요할 수 있음 (배포 후 검증).
</output>
