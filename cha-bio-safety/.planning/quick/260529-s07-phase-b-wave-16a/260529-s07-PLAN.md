---
phase: 260529-s07-phase-b-wave-16a-findings-modal-batch
plan: 01+02
type: execute
subsystem: redesign/phase-b-sweep
wave: 16a (Tier 3 첫 wave — components batch)
status: ready
tags: [findings, finding-edit-modal, finding-form-sheet, inline-style-to-tailwind, no-op-refactor, phase-b-tier-3-wave-16a, component-batch, dual-render-pattern, isdesktopsheet-ternary, mobile-desktop-shared, input-style-spread-preserved, partial-conversion-pattern, atomic-split]
quick-id: 260529-s07
slug: phase-b-wave-16a
created: 2026-05-29
predecessors:
  - 260529-qn3 (Wave 15b ElevatorPage desktop, Tier 2 종결 — 메가 페이지 분할 패턴 박제, 21번째+22번째 atomic)
  - 260529-q5a (Wave 14b Remediation desktop, partial conversion 첫 사례 — L332 padding+boxSizing className 화 + scrollbarWidth inline 잔존)
  - 260529-ozt (Wave 13b Checkpoints desktop, INPUT_STYLE spread 데스크톱 zone 옵션 N 확장 + boundary paired conversion)
  - 260528-0hr (Phase B 마스터 로드맵 v2 — Tier 1/2/3 분류 + 옵션 X+P+M+색변수N locked)
files-modified:
  - src/components/FindingEditModal.tsx (Plan 01)
  - src/components/FindingFormSheet.tsx (Plan 02)
autonomous: true
requirements:
  - REQ-PHASE-B-T3-16a-1-FindingEditModal: 43 inline → tailwind className (옵션 X+P+M+색변수N 승계), 시각 0 byte, 비즈 anchor IDENTICAL, single atomic commit
  - REQ-PHASE-B-T3-16a-2-FindingFormSheet: 34 inline → tailwind className (옵션 X+P+M+색변수N 승계), 시각 0 byte, 비즈 anchor IDENTICAL, single atomic commit
  - REQ-EMOJI-PRESERVED: 양 컴포넌트 모두 emoji (📷 / ✕) 변동 0 — Wave 16a 는 emoji sweep wave 아님
  - REQ-MODULE-CONST-N: 양 컴포넌트의 lblStyle / inputStyle (FindingFormSheet 추가 chipStyle/floorChipStyle) 함수 스코프 const 정의 보존 + 직접 참조 보존

split-decision:
  result: B (분할) — Plan 01 (FindingEditModal 43 atomic) + Plan 02 (FindingFormSheet 34 atomic)
  rationale: |
    1. **77 inline borderline** — q5a (14 inline) 약 10분 / qn3-02 (62 inline) 약 25분 → 77 inline 단일 atomic 시 약 30-35분 예상. 50+ inline 분할 임계점 (15a/15b precedent 50+51, 42+62 모두 분할) 안전 마진.
    2. **컴포넌트 자연 boundary** — 두 파일이 별 컴포넌트 (FindingEditModal vs FindingFormSheet). share 룰 X. 각자 lblStyle/inputStyle 자체 정의. 변환 패턴은 IDENTICAL 이지만 rollback 부담 분리 가치 있음.
    3. **시각 검증 분리** — 각 컴포넌트 별도 모달/시트 UI. 한쪽 시각 회귀 시 한 atomic revert.
    4. **partial conversion 패턴 다수** — FindingFormSheet 의 `{...inputStyle, resize: 'vertical'}` × 1, `{...inputStyle, marginTop: 8}` × 1 + FindingEditModal 의 `{...inputStyle, resize: 'vertical'}` × 1. 검증 부담 분산.
    5. **q5a partial conversion precedent + 13b/12b boundary paired 룰 적용** — 각 atomic 에서 명확하게 partial spread combo 정리.
  alternative-considered: A (단일 atomic 77 inline) — 단일 commit history 깔끔하나 30-35분 추정 + 시각 검증 부담 집중 → 채택 X

user_setup: []

must_haves:
  truths:
    - "FindingEditModal.tsx style={{ 카운트 43 → 0~3 잔존 (옵션 N 후보: {...inputStyle, resize:'vertical'} × 1 partial 또는 잔존)"
    - "FindingFormSheet.tsx style={{ 카운트 34 → 0~4 잔존 (옵션 N 후보: {...inputStyle, resize:'vertical'} × 1, {...inputStyle, marginTop:8} × 1)"
    - "두 컴포넌트 시각 결과 0 byte 변경 — 모바일 BottomSheet + 데스크톱 모달 양쪽 모두 픽셀 1:1"
    - "비즈 anchor (FindingEditModal 13 onClick / 3 useState / 1 useMutation, FindingFormSheet 13 onClick / 6 useState / 1 useMutation) IDENTICAL"
    - "양 컴포넌트 emoji (📷 / ✕) 변동 0 — Wave 16a 는 emoji sweep wave 아님"
    - "TypeScript 0 error"
    - "off-scope 파일 변경 0"
    - "vite build PWA generation PASS"
    - "양 atomic commit hash 기록 (Plan 01 + Plan 02 각 1건 = 2 commit)"
  artifacts:
    - path: "src/components/FindingEditModal.tsx"
      provides: "FindingEditModal 변환 후 inline ~3 이하 잔존 + 시각 0 byte"
      contains: "shrink-0 / bg-surface-raised / bg-surface-sunken / text-text-primary / text-text-tertiary / text-danger-bar / w-[72px] / h-[72px] / h-12 etc"
    - path: "src/components/FindingFormSheet.tsx"
      provides: "FindingFormSheet 변환 후 inline ~4 이하 잔존 + 시각 0 byte"
      contains: "FINDING_ITEMS module const 보존 / ZONES / ZONE_FLOORS / parseFindingForEdit 함수 0 byte"
    - path: ".planning/quick/260529-s07-phase-b-wave-16a/260529-s07-PLAN.md"
      provides: "본 PLAN.md (Plan 01 + Plan 02 unified)"
  key_links:
    - from: "Plan 01 task 01 (FindingEditModal sweep)"
      to: "git commit feat(260529-s07-01) — atomic single commit"
      via: "git add src/components/FindingEditModal.tsx && git commit"
    - from: "Plan 02 task 01 (FindingFormSheet sweep)"
      to: "git commit feat(260529-s07-02) — atomic single commit"
      via: "git add src/components/FindingFormSheet.tsx && git commit"
    - from: "양 commit"
      to: "GitHub Actions auto deploy cbc7119-preview"
      via: "main push (사용자 명시 컨펌 후)"
---

<objective>
Phase B Tier 3 Wave 16a 첫 wave — **findings 모달 batch 컴포넌트 변환**.

- FindingEditModal.tsx (257줄, 43 inline) + FindingFormSheet.tsx (394줄, 34 inline) = 77 inline 정적 inline style → tailwind className 변환
- 옵션 X+P+M+색변수N + module const N (lblStyle/inputStyle/chipStyle/floorChipStyle) 승계
- **분할 B 채택**: Plan 01 (FindingEditModal 43 atomic) + Plan 02 (FindingFormSheet 34 atomic) = 2 atomic commit
- 컴포넌트 = 모바일/데스크톱 공통 사용 — zone-aware sweep 불필요 (단순 sweep)
- isDesktopSheet 분기 (양쪽 wrapper 모달/시트) 양쪽 모두 변환 (시각 검증 양쪽 분리)

**Purpose:** Tier 3 components batch 시작. **모바일+데스크톱 공통 컴포넌트** 변환 첫 사례. 22번째+23번째 atomic.
**Output:** 2 PR-ready atomic commits — FindingEditModal 변환 + FindingFormSheet 변환. cbc7119-preview 자동 배포 대상.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
## CWD 필수
```
/Users/jykevin/Documents/cbc7119-design/cha-bio-safety
```

## 승계 결정 (재컨펌 불필요 — 0hr roadmap locked)

| ID | 선택 | 출처 |
|---|---|---|
| (b) | **옵션 X** — 정확값 arbitrary `[Npx]` (시각 0 byte) | wdc Phase B Task 2 |
| (c) | **옵션 P** — `leading-none` 명시 보존 | wdc Phase B Task 2 |
| (d) | **옵션 M + 색 변수만 N** — template literal conditional | wdc Phase B Task 2 |
| - | **a3v~qn3 22 wave 승계 적용** | 260528-0hr roadmap v2 |
| - | **module const N (lblStyle/inputStyle/chipStyle/floorChipStyle)** — 함수 스코프 const 정의 보존 | hbv/12a/13a/14a/15a 정의 보존 룰 적용 |
| - | **partial conversion 룰 (q5a precedent)** — `{...inputStyle, resize: 'vertical'}` 또는 `{...inputStyle, marginTop: 8}` 의 spread 는 옵션 N 보존 (분리 불가 — spread combo) |
| - | **emoji sweep 아님** — 📷 / ✕ 모두 보존 (변동 0) |

## 토큰 alias 매핑 (precedent IDENTICAL)
- `var(--bg2)` → `bg-surface-raised`
- `var(--bg3)` → `bg-surface-sunken`
- `var(--bd)` → `border-border-default`
- `var(--bd2)` → `border-border-strong`
- `var(--t1)` → `text-text-primary`
- `var(--t2)` → `text-text-secondary`
- `var(--t3)` → `text-text-tertiary`
- `var(--danger)` → `text-danger-bar` (대부분) 또는 `bg-danger-bar` (✕ 버튼 배경)
- `var(--acl)` → `bg-accent` 또는 `text-accent`

## Spacing override 인지
- 1=4 / 2=8 / 3=12 / 4=16 / 5=20 / 6=24 / 7=32 / 8=48
- `w-8/h-8` = **48px** (override) → 함정 회피
- `h-9` = 36px (Tailwind 기본, override 없음)
- `h-11` = 44px (Tailwind 기본, override 없음)
- `h-12` = 48px (Tailwind 기본, spacing override 8 과 동일)
- `gap-1.5` = 6px / `gap-3.5` = 14px (Tailwind 기본)
- 72px = `w-[72px] h-[72px]` arbitrary (w-18 미존재)
- 28px = `w-[28px] h-[28px]` arbitrary (w-7=32 함정)
- 20px = `w-5 h-5` (spacing override 5=20 OK) — `-top-1.5 -right-1.5` (top/right -6)
- 9px borderRadius (inputStyle) = `rounded-[9px]` arbitrary (rounded-sm=8, rounded-md=12)
- 10px borderRadius = `rounded-[10px]` arbitrary
- 11px fontSize = `text-[11px]` arbitrary
- 22px fontSize (📷 emoji) = `text-[22px]` arbitrary

## 메모리 anchor 적용

- **`feedback_tailwind_w8_h8_is_48px.md`** — w-8=48 / w-7=32. `width:20, height:20` → `w-5 h-5` (OK, spacing override 5=20). `width:72, height:72` → `w-[72px] h-[72px]` arbitrary (w-18 미존재). `width:28, height:28` → `w-[28px] h-[28px]` arbitrary. **height:48 → `h-12` (Tailwind 기본 12=48, spacing override 와 동일 — h-8=48 도 동일하지만 의도 명확성)**
- **`feedback_tailwind_token_class_pattern.md`** — `text-danger-bar` (NOT text-status-danger-bar). Lucide size={N} prop (해당 없음 — 본 wave 는 Lucide 없음)
- **`feedback_text_caption_leading_none.md`** — 작은 컨테이너 (w-5/h-5 ✕ 버튼) 내 텍스트 `text-[11px]` 는 `leading-none` 명시. lblStyle 의 `fontSize:12, fontWeight:700` 는 컨테이너 wrapper 라 leading-none 불필요 (text-[12px] font-bold 만)
- **`feedback_inspection_unresolved_color.md`** — 본 wave 무관 (status 색 없음, 모달 컴포넌트)
- **`feedback_design_changes_ask_first.md`** — 시각 0 byte 룰 100%
- **q5a partial conversion precedent** — `{...inputStyle, resize:'vertical'}` spread 는 분리 불가 → 옵션 N 보존. `style={inputStyle}` 단독은 className 화 가능 (단, inputStyle const 자체는 정의 보존)
- **13b INPUT_STYLE spread precedent** — spread combo (e.g. `{...inputStyle, resize:'vertical'}`) 는 zone 무관 옵션 N 보존
- **15a/15b precedent (메가 분할)** — 77 inline borderline → 분할 B 채택 (q5a/qn3 비례)
- **`feedback_check_branch_before_edit.md`** — 작업 시작 전 main 브랜치 확인 + dirty 없음 확인

## 양 컴포넌트 inline 분포 (정확)

### Plan 01 — FindingEditModal.tsx (43 inline)

| 영역 | Lines | Count |
|------|------:|------:|
| **함수 스코프 const 정의** | L85 (lblStyle) + L86-101 (inputStyle) | 0 (const 정의는 style={{ 매칭 X) |
| **style={lblStyle} 참조** | L107, L120, L131, L170, L181 | 5 (참조, 보존) |
| **style={inputStyle} 참조** | L126 | 1 (참조, 보존) |
| **style={...inputStyle} spread (resize:'vertical')** | L115, L176 | 2 (옵션 N 후보 — partial 또는 잔존) |
| **formContent root padding** | L105 | 1 |
| **지적 내용 * span (color)** | L108 | 1 |
| **사진 input × 2 (display:none)** | L132, L133 | 2 |
| **사진 grid wrapper** | L135 | 1 |
| **existing photo wrap (relative)** | L137 | 1 |
| **existing photo img** | L138 | 1 |
| **existing photo ✕ btn (20x20 danger circle)** | L142 | 1 |
| **slot photo wrap** | L147 | 1 |
| **slot photo img** | L148 | 1 |
| **slot photo ✕ btn** | L152 | 1 |
| **uploading overlay** | L154 | 1 |
| **add photo btn (72x72 dashed)** | L158 | 1 |
| **📷 emoji span (text-[22px])** | L159 | 1 |
| **resolved divider** | L167 | 1 |
| **resolution photo input × 2** | L182, L183 | 2 |
| **resolution photo grid** | L185 | 1 |
| **resolution existing wrap** | L187 | 1 |
| **resolution existing img** | L188 | 1 |
| **resolution existing ✕ btn** | L192 | 1 |
| **resolution slot wrap** | L197 | 1 |
| **resolution slot img** | L198 | 1 |
| **resolution slot ✕ btn** | L202 | 1 |
| **resolution uploading overlay** | L204 | 1 |
| **resolution add photo btn** | L208 | 1 |
| **resolution 📷 emoji span** | L209 | 1 |
| **button area wrapper** | L218 | 1 |
| **저장 button (h-12 acl)** | L219 | 1 |
| **취소 button (h-12 transparent border)** | L222 | 1 |
| **isDesktopSheet wrapper backdrop** | L231 | 1 |
| **isDesktopSheet modal box** | L232 | 1 |
| **isDesktopSheet header row** | L233 | 1 |
| **isDesktopSheet title** | L234 | 1 |
| **isDesktopSheet ✕ button (28x28)** | L235 | 1 |
| **mobile BottomSheet backdrop** | L244 | 1 |
| **mobile BottomSheet inner** | L245 | 1 |
| **mobile drag handle row** | L246 | 1 |
| **mobile drag handle bar (32x4)** | L247 | 1 |
| **mobile header wrapper** | L249 | 1 |
| **mobile title** | L250 | 1 |

**합계: 43 inline 정확** (style={{ 매칭 only — style={lblStyle}/style={inputStyle} 5+1=6 참조는 별도 grep 으로 보존 검증)

### Plan 02 — FindingFormSheet.tsx (34 inline)

| 영역 | Lines | Count |
|------|------:|------:|
| **module const ZONES / ZONE_FLOORS / FINDING_ITEMS** | L10-31 | 0 (data, style 아님) |
| **함수 스코프 const** | L178 (lblStyle) + L179-194 (inputStyle) + L195-207 (chipStyle) + L208-220 (floorChipStyle) | 0 (const 정의는 style={{ 매칭 X) |
| **style={lblStyle} 참조** | L234, L245, L256, L268, L303(...), L317 | 6 (참조, 보존) |
| **style={chipStyle(...)} 참조** | L237 | 1 (참조, 보존) — chipStyle 함수형 |
| **style={floorChipStyle(...)} 참조** | L248 | 1 (참조, 보존) |
| **style={inputStyle} 참조** | L263 | 1 (참조, 보존) |
| **style={...inputStyle} spread (resize:'vertical')** | L311 | 1 (옵션 N 후보) |
| **style={...inputStyle} spread (marginTop:8)** | L296 | 1 (옵션 N 후보 — q5a partial 적용 가능 — marginTop 만 className `mt-2` 분리, inputStyle spread 보존) |
| **formContent root padding** | L231 | 1 |
| **구역 row wrap (flex gap flex-wrap)** | L235 | 1 |
| **층 row wrap (flex gap)** | L246 | 1 |
| **지적 항목 container (border + maxHeight 123 + overflow)** | L269 | 1 |
| **지적 항목 button (loop, dynamic background/color/fontWeight)** | L275 | 1 (옵션 M — inspectionItem === item conditional) |
| **지적 내용 * span (color)** | L304 | 1 |
| **사진 input × 2** | L318, L319 | 2 |
| **사진 grid wrap** | L321 | 1 |
| **existing photo wrap (relative)** | L324 | 1 |
| **existing photo img** | L325 | 1 |
| **existing photo ✕ btn (20x20)** | L329 | 1 |
| **slot photo wrap** | L335 | 1 |
| **slot photo img** | L336 | 1 |
| **slot photo ✕ btn** | L340 | 1 |
| **uploading overlay** | L342 | 1 |
| **add photo btn (72x72 dashed)** | L346 | 1 |
| **📷 emoji span (text-[22px])** | L347 | 1 |
| **button area wrapper** | L355 | 1 |
| **저장 button (h-12 acl)** | L356 | 1 |
| **취소 button (h-12 transparent border)** | L359 | 1 |
| **isDesktopSheet wrapper backdrop** | L368 | 1 |
| **isDesktopSheet modal box** | L369 | 1 |
| **isDesktopSheet header row** | L370 | 1 |
| **isDesktopSheet title** | L371 | 1 |
| **isDesktopSheet ✕ button (28x28)** | L372 | 1 |
| **mobile BottomSheet backdrop** | L381 | 1 |
| **mobile BottomSheet inner** | L382 | 1 |
| **mobile drag handle row** | L383 | 1 |
| **mobile drag handle bar (32x4)** | L384 | 1 |
| **mobile header wrapper** | L386 | 1 |
| **mobile title** | L387 | 1 |

**합계: 34 inline 정확** (style={{ 매칭 only — style={lblStyle}/style={chipStyle(...)}/style={floorChipStyle(...)}/style={inputStyle} 6+1+1+1=9 참조는 별도 grep 으로 보존 검증)

## 양 컴포넌트 비즈 anchor (precise, IDENTICAL 보존 필수)

### FindingEditModal.tsx
- onClick: 13
- useState: 3 (description, location, existingKeys 등 — useMutation 안 set 제외)
- useRef: 0
- useEffect: 0
- useMutation: 1
- useQuery: 0
- useNavigate: 0
- useParams: 0
- fetch: 0
- emoji (📷 / ✕): 5 ✕ + 2 📷 = 7 (정확 카운트)

### FindingFormSheet.tsx
- onClick: 13
- useState: 6
- useRef: 0
- useEffect: 0
- useMutation: 1
- useQuery: 0
- useNavigate: 0
- useParams: 0
- fetch: 0
- emoji (📷 / ✕): 3 ✕ + 1 📷 = 4 (정확 카운트)

## 컴포넌트 특이사항

- **두 파일 모두 컴포넌트 (페이지 아님)** — 모바일/데스크톱 공통 사용
- **isDesktopSheet 분기 = 양쪽 wrapper render** — `if (isDesktopSheet) return <desktop>` + default `return <mobile bottom sheet>`. 양쪽 모두 변환 필수 (zone-aware sweep 무관, 둘 다 동일 컴포넌트 인스턴스)
- **formContent JSX 공유** — `const formContent = (<>...</>)` 변수로 양 wrapper 에 공통 삽입. formContent 내부 변환 = 양 view 자동 적용
- **wrapper 영역 (isDesktopSheet 분기 안) 별도 변환** — desktop header (L233-L235) + mobile header (L244-L250) 양쪽 모두 sweep
- **`isDesktopSheet = window.matchMedia(...).matches`** — runtime detect, 변경 0 보존

## 토큰/색 매핑 세부

| inline | className |
|---|---|
| `fontSize: 12, fontWeight: 700, color: 'var(--t3)', marginBottom: 6` (lblStyle 내부) | (const 정의 보존 — style={lblStyle} 참조 그대로) |
| `background: 'var(--bg3)', borderRadius: 9, padding: '10px 12px', border: '1px solid var(--bd2)', width: '100%', color: 'var(--t1)', fontSize: 13, ...` (inputStyle) | (const 정의 보존 — style={inputStyle} 참조 그대로) |
| `color: 'var(--danger)'` (* span) | `text-danger-bar` |
| `position: 'relative', flexShrink: 0` (photo wrap) | `relative shrink-0` |
| `width: 72, height: 72, objectFit: 'cover', borderRadius: 10, border: '1px solid var(--bd)', display: 'block'` (img) | `w-[72px] h-[72px] object-cover rounded-[10px] border border-border-default block` |
| `position: 'absolute', top: -6, right: -6, width: 20, height: 20, borderRadius: '50%', background: 'var(--danger)', border: 'none', color: '#fff', fontSize: 11, fontWeight: 700, cursor: 'pointer', display: 'flex', alignItems: 'center', justifyContent: 'center', lineHeight: 1` (✕ btn 20x20) | `absolute -top-1.5 -right-1.5 w-5 h-5 rounded-full bg-danger-bar border-0 text-white text-[11px] font-bold cursor-pointer flex items-center justify-center leading-none` |
| `position: 'absolute', inset: 0, background: 'rgba(0,0,0,0.4)', borderRadius: 10, display: 'flex', alignItems: 'center', justifyContent: 'center', fontSize: 10, color: '#fff'` (uploading overlay) | `absolute inset-0 bg-[rgba(0,0,0,0.4)] rounded-[10px] flex items-center justify-center text-[10px] text-white` |
| `width: 72, height: 72, borderRadius: 10, background: 'var(--bg3)', border: '1px dashed var(--bd2)', color: 'var(--t3)', fontSize: 11, fontWeight: 600, cursor: 'pointer', display: 'flex', flexDirection: 'column', alignItems: 'center', justifyContent: 'center', gap: 4, flexShrink: 0` (add photo btn) | `w-[72px] h-[72px] rounded-[10px] bg-surface-sunken border border-dashed border-border-strong text-text-tertiary text-[11px] font-semibold cursor-pointer flex flex-col items-center justify-center gap-1 shrink-0` |
| `fontSize: 22` (📷 span) | `text-[22px]` |
| `height: 1, background: 'var(--bd)', margin: '4px 0'` (divider) | `h-px bg-border-default my-1` (h-px = 1px Tailwind 기본) |
| `width: '100%', height: 48, background: 'var(--acl)', borderRadius: 10, border: 'none', color: '#fff', fontWeight: 700, fontSize: 14, cursor: isSubmitting ? 'not-allowed' : 'pointer', opacity: isSubmitting ? 0.6 : 1` (저장 btn) | **옵션 M** — `w-full h-12 bg-accent rounded-[10px] border-0 text-white font-bold text-[14px] ${isSubmitting ? 'cursor-not-allowed opacity-60' : 'cursor-pointer opacity-100'}` |
| `width: '100%', height: 48, background: 'transparent', border: '1px solid var(--bd2)', borderRadius: 10, color: 'var(--t2)', fontSize: 14, cursor: 'pointer'` (취소 btn) | `w-full h-12 bg-transparent border border-border-strong rounded-[10px] text-text-secondary text-[14px] cursor-pointer` |
| `padding: isDesktopSheet ? '8px 24px 24px' : '4px 16px 32px', display: 'flex', flexDirection: 'column', gap: 8` (button area) | **옵션 M (padding 조건)** — `flex flex-col gap-2 ${isDesktopSheet ? 'pt-2 px-6 pb-6' : 'pt-1 px-4 pb-8'}` |
| `padding: pad, display: 'flex', flexDirection: 'column', gap: 14` (formContent root, pad=isDesktopSheet ternary) | **옵션 M (padding 조건)** — `flex flex-col gap-3.5 ${isDesktopSheet ? 'px-6' : 'pt-3 px-4'}` (단, pad 의 '0 24px' vs '12px 16px' 차이가 모바일 pt-3 = 12px 동일 / px = OK 검증) |
| `position: 'fixed', inset: 0, background: 'rgba(0,0,0,0.5)', display: 'flex', alignItems: 'center', justifyContent: 'center', zIndex: 50` (desktop backdrop) | `fixed inset-0 bg-[rgba(0,0,0,0.5)] flex items-center justify-center z-50` |
| `background: 'var(--bg2)', borderRadius: 12, width: 520, maxHeight: '85vh', overflowY: 'auto', boxShadow: '0 8px 32px rgba(0,0,0,.18)'` (desktop modal) | `bg-surface-raised rounded-xl w-[520px] max-h-[85vh] overflow-y-auto shadow-[0_8px_32px_rgba(0,0,0,.18)]` (rounded-xl=12 Tailwind 기본) |
| `padding: '20px 24px 12px', display: 'flex', alignItems: 'center', justifyContent: 'space-between'` (header row) | `pt-5 px-6 pb-3 flex items-center justify-between` |
| `fontSize: 16, fontWeight: 700, color: 'var(--t1)'` (title) | `text-[16px] font-bold text-text-primary` |
| `width: 28, height: 28, borderRadius: 7, background: 'var(--bg3)', border: 'none', color: 'var(--t2)', cursor: 'pointer', display: 'flex', alignItems: 'center', justifyContent: 'center', fontSize: 15` (header ✕ btn 28x28) | `w-[28px] h-[28px] rounded-[7px] bg-surface-sunken border-0 text-text-secondary cursor-pointer flex items-center justify-center text-[15px]` |
| `position: 'fixed', inset: 0, background: 'rgba(0,0,0,0.6)', display: 'flex', flexDirection: 'column', justifyContent: 'flex-end', zIndex: 50, overscrollBehavior: 'contain'` (mobile BottomSheet backdrop) | **옵션 N partial** — `fixed inset-0 bg-[rgba(0,0,0,0.6)] flex flex-col justify-end z-50` + style={{ overscrollBehavior: 'contain' }} 잔존 (Tailwind 기본 미지원 — overscroll-contain 이 Tailwind v3 에 있음 → 확인 후 적용. 있으면 직접) |
| `background: 'var(--bg2)', borderRadius: '16px 16px 0 0', animation: 'slideUp 0.28s ease-out both', maxHeight: '90vh', overflowY: 'auto', overscrollBehavior: 'contain'` (mobile inner) | **옵션 N partial** — `bg-surface-raised rounded-t-[16px] max-h-[90vh] overflow-y-auto` + style={{ animation: 'slideUp 0.28s ease-out both', overscrollBehavior: 'contain' }} 잔존 (animation은 custom keyframe + Tailwind animate-* 미매칭, overscrollBehavior 확인) |
| `display: 'flex', justifyContent: 'center', paddingTop: 12` (drag handle row) | `flex justify-center pt-3` |
| `width: 32, height: 4, background: 'var(--bd2)', borderRadius: 2` (drag handle bar) | `w-8 h-1 bg-border-strong rounded-sm` (w-8=48 함정 — 32px ≠ w-8! → **`w-[32px] h-1`** arbitrary 필수, h-1=4px OK). rounded-sm=8px → rounded-[2px] arbitrary 필수 |
| `padding: '12px 16px 0'` (mobile header wrap) | `pt-3 px-4` |

### FindingFormSheet 추가 (chipStyle / floorChipStyle / FINDING_ITEMS button)

| inline | className |
|---|---|
| `display: 'flex', gap: 6, flexWrap: 'wrap'` (구역 row) | `flex gap-1.5 flex-wrap` |
| `display: 'flex', gap: 4` (층 row) | `flex gap-1` |
| `display: 'flex', flexDirection: 'column', gap: 0, border: '1px solid var(--bd2)', borderRadius: 9, overflow: 'hidden', maxHeight: 123, overflowY: 'auto'` (지적 항목 container) | `flex flex-col gap-0 border border-border-strong rounded-[9px] overflow-hidden max-h-[123px] overflow-y-auto` |
| `padding: '10px 12px', background: inspectionItem === item ? 'rgba(59,130,246,.1)' : 'var(--bg3)', border: 'none', borderBottom: i < FINDING_ITEMS.length - 1 ? '1px solid var(--bd)' : 'none', color: inspectionItem === item ? 'var(--acl)' : 'var(--t1)', fontSize: 13, fontWeight: inspectionItem === item ? 700 : 400, textAlign: 'left', cursor: 'pointer'` (지적 항목 button — 6-prop conditional + 1 dynamic) | **옵션 M + 동적 border** — `py-2.5 px-3 border-0 text-[13px] text-left cursor-pointer ${inspectionItem === item ? 'bg-[rgba(59,130,246,.1)] text-accent font-bold' : 'bg-surface-sunken text-text-primary font-normal'} ${i < FINDING_ITEMS.length - 1 ? 'border-b border-border-default' : ''}` |
| `{ ...inputStyle, marginTop: 8 }` (직접입력 input) | **옵션 N partial** — `style={inputStyle}` + className 추가 `mt-2` OR style={{ ...inputStyle, marginTop: 8 }} 보존 (q5a 룰: spread 분리 어려우면 보존). **결정: spread 보존 (옵션 N)** — inputStyle 정의 자체에 marginTop 가 없어 spread 가 layout 가산. 분리하면 의도 불분명. |
| `{ ...inputStyle, resize: 'vertical' }` (textarea) | **옵션 N** — spread 보존 (resize 는 Tailwind 기본 `resize-y` 가 있으나 spread 분리 시 inputStyle 의 다른 prop 보존 위해 spread 유지) |

**chipStyle / floorChipStyle 정의 보존 — 함수형 const (active boolean → CSSProperties).** style={chipStyle(active)} / style={floorChipStyle(active)} 참조 보존. inline 카운트 매칭 X (style={{ 패턴 아님).

## 잔존 옵션 N 예상 (Plan 별)

### Plan 01 (FindingEditModal) — 0~3 잔존 예상
- L115 `{...inputStyle, resize: 'vertical'}` — spread combo 옵션 N 후보 (보존 권장)
- L176 `{...inputStyle, resize: 'vertical'}` — spread combo (보존 권장)
- L244 / L245 mobile BottomSheet `overscrollBehavior: 'contain'` 또는 `animation: 'slideUp...'` — Tailwind 미지원 prop 옵션 N partial 후보

**결정 가이드:** Tailwind v3 에 `overscroll-contain` 이 있다. `animate-[slideUp_0.28s_ease-out_both]` arbitrary 도 가능하나 keyframe 정의가 `<style>` 안 → animate 보존 권장 (animation prop inline 으로). 결과적으로 mobile backdrop L244 + mobile inner L245 의 `overscrollBehavior` 만 `overscroll-contain` 으로 className 화 가능, animation 만 inline 잔존.

**최종 예상 잔존 카운트:** 1~3건 (resize spread × 2 + animation × 1)

### Plan 02 (FindingFormSheet) — 0~4 잔존 예상
- L311 `{...inputStyle, resize: 'vertical'}` — spread combo
- L296 `{...inputStyle, marginTop: 8}` — spread combo
- L381 / L382 mobile BottomSheet `overscrollBehavior` / `animation`

**최종 예상 잔존 카운트:** 1~4건 (spread × 2 + animation × 1 + 가능 추가 1)

## 자동 검증 block (PLAN.md 필수)

```bash
cd /Users/jykevin/Documents/cbc7119-design/cha-bio-safety

# === 1. inline count drop ===
for F in src/components/FindingEditModal.tsx src/components/FindingFormSheet.tsx; do
  B=$(git show HEAD:$F | grep -c 'style={{')
  A=$(grep -c 'style={{' $F)
  echo "$F: $B → $A"
done

# === 2. 비즈 anchor identity ===
for F in src/components/FindingEditModal.tsx src/components/FindingFormSheet.tsx; do
  for ANCHOR in 'onClick=\{[^}]+\}' 'useState\(' 'useRef\(' 'useEffect\(' 'useMutation\(' 'useQuery\(' 'useNavigate\(' 'useParams\(' 'fetch\('; do
    B=$(git show HEAD:$F | grep -cE "$ANCHOR")
    A=$(grep -cE "$ANCHOR" $F)
    [ "$B" = "$A" ] && echo "$F $ANCHOR: $B (OK)" || echo "$F $ANCHOR: $B→$A MISMATCH ❌"
  done
done

# === 3. precise onClick diff ===
for F in src/components/FindingEditModal.tsx src/components/FindingFormSheet.tsx; do
  git show HEAD:$F | grep -oE 'onClick=\{[^}]+\}' | sort | uniq > /tmp/before-${F##*/}.txt
  grep -oE 'onClick=\{[^}]+\}' $F | sort | uniq > /tmp/after-${F##*/}.txt
  diff /tmp/before-${F##*/}.txt /tmp/after-${F##*/}.txt && echo "$F onClick diff EMPTY ✓" || echo "$F onClick diff MISMATCH ❌"
done

# === 4. emoji + 비색 (변동 0 — Wave 16a 는 emoji sweep 아님) ===
for F in src/components/FindingEditModal.tsx src/components/FindingFormSheet.tsx; do
  echo "$F emoji: $(grep -cE '✓|✗|🔒|💾|🔥|⏰|📋|✅|⚠️|❌|🔧|🚨|🔍|🧯|📊|✕|📷' $F)"
  echo "$F 비색: $(grep -cE 'bg-warning[^-]|border-safe[^-]|border-warning[^-]|border-danger[^-]' $F)"
done

# === 5. const 정의 + 참조 보존 ===
# FindingEditModal: lblStyle const def 1, inputStyle const def 1, style={lblStyle} ref 5, style={inputStyle} ref 1
echo "FindingEditModal lblStyle def: $(grep -c 'const lblStyle' src/components/FindingEditModal.tsx)"
echo "FindingEditModal inputStyle def: $(grep -c 'const inputStyle' src/components/FindingEditModal.tsx)"
echo "FindingEditModal style={lblStyle} ref: $(grep -c 'style={lblStyle}' src/components/FindingEditModal.tsx)"
echo "FindingEditModal style={inputStyle} ref: $(grep -c 'style={inputStyle}' src/components/FindingEditModal.tsx)"

# FindingFormSheet: lblStyle def 1, inputStyle def 1, chipStyle def 1, floorChipStyle def 1, ref counts
echo "FindingFormSheet lblStyle def: $(grep -c 'const lblStyle' src/components/FindingFormSheet.tsx)"
echo "FindingFormSheet inputStyle def: $(grep -c 'const inputStyle' src/components/FindingFormSheet.tsx)"
echo "FindingFormSheet chipStyle def: $(grep -c 'const chipStyle' src/components/FindingFormSheet.tsx)"
echo "FindingFormSheet floorChipStyle def: $(grep -c 'const floorChipStyle' src/components/FindingFormSheet.tsx)"
echo "FindingFormSheet style={lblStyle} ref: $(grep -c 'style={lblStyle}' src/components/FindingFormSheet.tsx)"
echo "FindingFormSheet style={inputStyle} ref: $(grep -c 'style={inputStyle}' src/components/FindingFormSheet.tsx)"
echo "FindingFormSheet style={chipStyle ref: $(grep -cE 'style=\{chipStyle' src/components/FindingFormSheet.tsx)"
echo "FindingFormSheet style={floorChipStyle ref: $(grep -cE 'style=\{floorChipStyle' src/components/FindingFormSheet.tsx)"

# FindingFormSheet module data const (ZONES / ZONE_FLOORS / FINDING_ITEMS) 보존
echo "FindingFormSheet ZONES def: $(grep -c 'const ZONES' src/components/FindingFormSheet.tsx)"
echo "FindingFormSheet ZONE_FLOORS def: $(grep -c 'const ZONE_FLOORS' src/components/FindingFormSheet.tsx)"
echo "FindingFormSheet FINDING_ITEMS def: $(grep -c 'const FINDING_ITEMS' src/components/FindingFormSheet.tsx)"
echo "FindingFormSheet parseFindingForEdit def: $(grep -c 'function parseFindingForEdit' src/components/FindingFormSheet.tsx)"

# === 6. TypeScript 0 error ===
./node_modules/.bin/tsc --noEmit 2>&1 | grep -E 'error TS' | wc -l

# === 7. off-scope ===
cd .. && git diff --name-only HEAD | grep -v 'cha-bio-safety/' | wc -l  # MUST = 0
```
</context>

<tasks>

<task type="auto">
  <name>Task 1 (Plan 01): FindingEditModal 43 inline → tailwind className 단일 atomic</name>
  <files>src/components/FindingEditModal.tsx</files>
  <action>
    **CWD = /Users/jykevin/Documents/cbc7119-design/cha-bio-safety 필수.**

    ### 단계 1: 사전 검증
    ```bash
    cd /Users/jykevin/Documents/cbc7119-design/cha-bio-safety
    git status  # main 브랜치 + dirty 없음 확인 (메모리 anchor: feedback_check_branch_before_edit.md)
    git log -1 --oneline  # HEAD = b6b7e36 (또는 직전) 확인
    grep -c 'style={{' src/components/FindingEditModal.tsx  # 43 확인
    ```

    ### 단계 2: 함수 스코프 const 정의 보존
    `lblStyle: React.CSSProperties = { fontSize: 12, fontWeight: 700, color: 'var(--t3)', marginBottom: 6 }` (L85) 와 `inputStyle: React.CSSProperties = { ... }` (L86-101) **정의 0 byte 변경**. `style={lblStyle}` 5건 (L107/L120/L131/L170/L181) + `style={inputStyle}` 1건 (L126) **참조 그대로 보존**.

    ### 단계 3: spread combo 잔존 결정
    - L115 `style={{ ...inputStyle, resize: 'vertical' }}` — **옵션 N 보존** (spread combo, 분리 시 inputStyle 의 다른 prop 보존 위해 spread 유지)
    - L176 `style={{ ...inputStyle, resize: 'vertical' }}` — **옵션 N 보존** (위와 동일)
    - 예외 대안: resize 만 className `resize-y` 분리 + spread 보존 시 inline 카운트 그대로 → spread 보존이 깔끔

    ### 단계 4: formContent 내부 41건 변환 (배포 매핑 표 그대로)

    **formContent root padding (L105):**
    - 옵션 M conditional: `pad = isDesktopSheet ? '0 24px' : '12px 16px'`
    - `style={{ padding: pad, display: 'flex', flexDirection: 'column', gap: 14 }}`
    - → `<div className={`flex flex-col gap-3.5 ${isDesktopSheet ? 'px-6' : 'pt-3 px-4'}`}>`
    - (검증: pad '0 24px' = pt-0 + px-6 + pb-0 / pad '12px 16px' = pt-3 + px-4 + pb-3 — pb-3 누락! → 정확히 매핑: `${isDesktopSheet ? 'px-6' : 'py-3 px-4'}`. pb-3 = pt-3 + py-3 동일 — `py-3 px-4` 정확)

    **지적 내용 * span (L108):** `style={{ color: 'var(--danger)' }}` → `<span className="text-danger-bar">`

    **textarea (L115):** **옵션 N spread 보존** — style 변경 0 (단, 컴테이너 div 의 className 은 변환)

    **위치 input (L126):** `style={inputStyle}` 그대로 보존 (참조 변경 0)

    **사진 input × 2 (L132/L133):** `style={{ display: 'none' }}` → `className="hidden"`

    **사진 grid wrap (L135):** `style={{ display: 'flex', gap: 8, overflowX: 'auto', paddingBottom: 4 }}` → `<div className="flex gap-2 overflow-x-auto pb-1">`

    **existing photo wrap (L137):** `style={{ position: 'relative', flexShrink: 0 }}` → `className="relative shrink-0"`

    **existing photo img (L138):** `style={{ width: 72, height: 72, objectFit: 'cover', borderRadius: 10, border: '1px solid var(--bd)', display: 'block' }}` → `className="w-[72px] h-[72px] object-cover rounded-[10px] border border-border-default block"`

    **existing photo ✕ btn (L142, 20x20 danger circle):** `style={{ position: 'absolute', top: -6, right: -6, width: 20, height: 20, borderRadius: '50%', background: 'var(--danger)', border: 'none', color: '#fff', fontSize: 11, fontWeight: 700, cursor: 'pointer', display: 'flex', alignItems: 'center', justifyContent: 'center', lineHeight: 1 }}` → `className="absolute -top-1.5 -right-1.5 w-5 h-5 rounded-full bg-danger-bar border-0 text-white text-[11px] font-bold cursor-pointer flex items-center justify-center leading-none"` (메모리 anchor `feedback_text_caption_leading_none.md` 적용 — leading-none 명시. w-5/h-5 = 20px spacing override OK)

    **slot photo wrap (L147):** existing photo wrap L137 IDENTICAL

    **slot photo img (L148):** existing photo img L138 IDENTICAL

    **slot photo ✕ btn (L152):** existing photo ✕ btn L142 IDENTICAL

    **uploading overlay (L154):** `style={{ position: 'absolute', inset: 0, background: 'rgba(0,0,0,0.4)', borderRadius: 10, display: 'flex', alignItems: 'center', justifyContent: 'center', fontSize: 10, color: '#fff' }}` → `className="absolute inset-0 bg-[rgba(0,0,0,0.4)] rounded-[10px] flex items-center justify-center text-[10px] text-white"`

    **add photo btn (L158, 72x72 dashed):** `style={{ width: 72, height: 72, borderRadius: 10, background: 'var(--bg3)', border: '1px dashed var(--bd2)', color: 'var(--t3)', fontSize: 11, fontWeight: 600, cursor: 'pointer', display: 'flex', flexDirection: 'column', alignItems: 'center', justifyContent: 'center', gap: 4, flexShrink: 0 }}` → `className="w-[72px] h-[72px] rounded-[10px] bg-surface-sunken border border-dashed border-border-strong text-text-tertiary text-[11px] font-semibold cursor-pointer flex flex-col items-center justify-center gap-1 shrink-0"`

    **📷 emoji span (L159):** `style={{ fontSize: 22 }}` → `<span className="text-[22px]">📷</span>`

    **resolved divider (L167):** `style={{ height: 1, background: 'var(--bd)', margin: '4px 0' }}` → `<div className="h-px bg-border-default my-1" />` (h-px = 1px Tailwind 기본, my-1 = 4px)

    **resolution textarea (L176):** **옵션 N spread 보존** (L115 동일)

    **resolution input × 2 (L182/L183):** L132/L133 IDENTICAL

    **resolution photo grid (L185):** L135 IDENTICAL

    **resolution existing wrap/img/✕ btn (L187/L188/L192):** L137/L138/L142 IDENTICAL

    **resolution slot wrap/img/✕ btn (L197/L198/L202):** L147/L148/L152 IDENTICAL

    **resolution uploading overlay (L204):** L154 IDENTICAL

    **resolution add photo btn (L208):** L158 IDENTICAL

    **resolution 📷 emoji span (L209):** L159 IDENTICAL

    **button area wrapper (L218):** **옵션 M** — `style={{ padding: isDesktopSheet ? '8px 24px 24px' : '4px 16px 32px', display: 'flex', flexDirection: 'column', gap: 8 }}` → `className={`flex flex-col gap-2 ${isDesktopSheet ? 'pt-2 px-6 pb-6' : 'pt-1 px-4 pb-8'}`}`

    **저장 button (L219, h-12 acl):** **옵션 M** — `style={{ width: '100%', height: 48, background: 'var(--acl)', borderRadius: 10, border: 'none', color: '#fff', fontWeight: 700, fontSize: 14, cursor: isSubmitting ? 'not-allowed' : 'pointer', opacity: isSubmitting ? 0.6 : 1 }}` → `className={`w-full h-12 bg-accent rounded-[10px] border-0 text-white font-bold text-[14px] ${isSubmitting ? 'cursor-not-allowed opacity-60' : 'cursor-pointer opacity-100'}`}`

    **취소 button (L222):** `style={{ width: '100%', height: 48, background: 'transparent', border: '1px solid var(--bd2)', borderRadius: 10, color: 'var(--t2)', fontSize: 14, cursor: 'pointer' }}` → `className="w-full h-12 bg-transparent border border-border-strong rounded-[10px] text-text-secondary text-[14px] cursor-pointer"`

    ### 단계 5: isDesktopSheet wrapper 영역 변환 (10건)

    **desktop wrapper backdrop (L231):** `style={{ position: 'fixed', inset: 0, background: 'rgba(0,0,0,0.5)', display: 'flex', alignItems: 'center', justifyContent: 'center', zIndex: 50 }}` → `className="fixed inset-0 bg-[rgba(0,0,0,0.5)] flex items-center justify-center z-50"`

    **desktop modal box (L232):** `style={{ background: 'var(--bg2)', borderRadius: 12, width: 520, maxHeight: '85vh', overflowY: 'auto', boxShadow: '0 8px 32px rgba(0,0,0,.18)' }}` → `className="bg-surface-raised rounded-xl w-[520px] max-h-[85vh] overflow-y-auto shadow-[0_8px_32px_rgba(0,0,0,.18)]"` (rounded-xl=12 Tailwind 기본)

    **desktop header row (L233):** `style={{ padding: '20px 24px 12px', display: 'flex', alignItems: 'center', justifyContent: 'space-between' }}` → `className="pt-5 px-6 pb-3 flex items-center justify-between"`

    **desktop title (L234):** `style={{ fontSize: 16, fontWeight: 700, color: 'var(--t1)' }}` → `className="text-[16px] font-bold text-text-primary"`

    **desktop ✕ btn (L235, 28x28):** `style={{ width: 28, height: 28, borderRadius: 7, background: 'var(--bg3)', border: 'none', color: 'var(--t2)', cursor: 'pointer', display: 'flex', alignItems: 'center', justifyContent: 'center', fontSize: 15 }}` → `className="w-[28px] h-[28px] rounded-[7px] bg-surface-sunken border-0 text-text-secondary cursor-pointer flex items-center justify-center text-[15px]"` (w-7 = 32 함정 회피, arbitrary 필수)

    **mobile BottomSheet backdrop (L244):** **옵션 N partial** — `style={{ position: 'fixed', inset: 0, background: 'rgba(0,0,0,0.6)', display: 'flex', flexDirection: 'column', justifyContent: 'flex-end', zIndex: 50, overscrollBehavior: 'contain' }}` → `className="fixed inset-0 bg-[rgba(0,0,0,0.6)] flex flex-col justify-end z-50 overscroll-contain"` (Tailwind v3 overscroll-contain 사용 — 전부 변환)
    - 검증: Tailwind config 확인 — `overscroll-contain` 은 core utility (v2+). 사용 가능 → inline 0 잔존

    **mobile BottomSheet inner (L245):** **옵션 N partial** — `style={{ background: 'var(--bg2)', borderRadius: '16px 16px 0 0', animation: 'slideUp 0.28s ease-out both', maxHeight: '90vh', overflowY: 'auto', overscrollBehavior: 'contain' }}` → `className="bg-surface-raised rounded-t-[16px] max-h-[90vh] overflow-y-auto overscroll-contain" style={{ animation: 'slideUp 0.28s ease-out both' }}` (animation 만 inline 잔존 — custom keyframe slideUp 은 컴포넌트 안 `<style>` 안 정의됨, Tailwind animate-[xxx] arbitrary 도 가능하나 keyframe 정의가 동적이라 보존 권장)

    **mobile drag handle row (L246):** `style={{ display: 'flex', justifyContent: 'center', paddingTop: 12 }}` → `className="flex justify-center pt-3"`

    **mobile drag handle bar (L247, 32x4):** `style={{ width: 32, height: 4, background: 'var(--bd2)', borderRadius: 2 }}` → `className="w-[32px] h-1 bg-border-strong rounded-[2px]"` (**w-8 = 48 함정 회피 — width 32 ≠ w-8, arbitrary 필수**, h-1=4 OK, rounded-sm=8 ≠ 2 arbitrary 필수)

    **mobile header wrap (L249):** `style={{ padding: '12px 16px 0' }}` → `className="pt-3 px-4"` (pb-0 default)

    **mobile title (L250):** `style={{ fontSize: 16, fontWeight: 700, color: 'var(--t1)' }}` → desktop title L234 IDENTICAL

    ### 단계 6: 자동 검증 (verify gate 실행)
    위 context 의 verify block 그대로 실행. 모든 게이트 PASS 확인:
    - inline 카운트 43 → 1~3 잔존 (옵션 N: resize spread × 2 + animation × 1 = 최대 3)
    - 비즈 anchor (13 onClick / 3 useState / 1 useMutation 모두 IDENTICAL)
    - emoji 변동 0 (✕ 5 + 📷 2 = 7 보존)
    - lblStyle/inputStyle const 정의 + 참조 (5+1=6) IDENTICAL
    - TypeScript 0 error
    - off-scope 0
    - vite build PASS

    ### 단계 7: 단일 atomic commit
    ```bash
    cd /Users/jykevin/Documents/cbc7119-design/cha-bio-safety
    git add src/components/FindingEditModal.tsx
    git commit -m "$(cat <<'EOF'
feat(260529-s07-01): Phase B Wave 16a-1 — FindingEditModal (43 inline) → tailwind

- Plan 01 — FindingEditModal.tsx 43 inline → ~1-3 잔존 (옵션 N: resize spread × 2 + animation × 1)
- 옵션 X+P+M+색변수N+module const N (lblStyle/inputStyle) 승계 22 wave 자동 도달
- 비즈 anchor IDENTICAL (13 onClick / 3 useState / 1 useMutation)
- 시각 0 byte (모바일 BottomSheet + 데스크톱 모달 양쪽 픽셀 1:1)
- emoji ✕ 5 + 📷 2 = 7 보존 (Wave 16a 는 emoji sweep 아님)
- Tier 3 components batch 첫 wave — 22번째 atomic
EOF
)"
    ```
  </action>
  <verify>
    <automated>cd /Users/jykevin/Documents/cbc7119-design/cha-bio-safety && B=$(git show HEAD~1:src/components/FindingEditModal.tsx | grep -c 'style={{') && A=$(grep -c 'style={{' src/components/FindingEditModal.tsx) && echo "inline: $B → $A" && [ "$A" -le 3 ] && echo "PASS inline ≤ 3" && for ANCHOR in 'onClick=\{[^}]+\}' 'useState\(' 'useRef\(' 'useEffect\(' 'useMutation\(' 'useQuery\(' 'useNavigate\(' 'useParams\(' 'fetch\('; do BB=$(git show HEAD~1:src/components/FindingEditModal.tsx | grep -cE "$ANCHOR") && AA=$(grep -cE "$ANCHOR" src/components/FindingEditModal.tsx) && [ "$BB" = "$AA" ] && echo "  $ANCHOR: $BB OK" || echo "  $ANCHOR: $BB→$AA MISMATCH"; done && echo "lblStyle ref: $(grep -c 'style={lblStyle}' src/components/FindingEditModal.tsx) (expect 5)" && echo "inputStyle ref: $(grep -c 'style={inputStyle}' src/components/FindingEditModal.tsx) (expect 1)" && echo "lblStyle def: $(grep -c 'const lblStyle' src/components/FindingEditModal.tsx) (expect 1)" && echo "inputStyle def: $(grep -c 'const inputStyle' src/components/FindingEditModal.tsx) (expect 1)" && echo "emoji: $(grep -cE '✕|📷' src/components/FindingEditModal.tsx) (expect 7)" && ./node_modules/.bin/tsc --noEmit 2>&1 | grep -E 'error TS' | wc -l && cd .. && git diff --name-only HEAD | grep -v 'cha-bio-safety/' | wc -l</automated>
  </verify>
  <done>
    - FindingEditModal.tsx inline count: 43 → 1~3 잔존 (옵션 N: resize spread × 2 + animation × 1 — 본 plan 결과)
    - 비즈 anchor (13 onClick / 3 useState / 1 useMutation) IDENTICAL
    - lblStyle/inputStyle const 정의 보존 + 참조 5+1=6 IDENTICAL
    - emoji ✕ 5 + 📷 2 = 7 보존 (변동 0)
    - 비표준 색 토큰 0
    - TypeScript 0 error
    - off-scope 0
    - vite build PWA generation PASS
    - 단일 atomic commit (260529-s07-01) ✓
  </done>
</task>

<task type="auto">
  <name>Task 2 (Plan 02): FindingFormSheet 34 inline → tailwind className 단일 atomic</name>
  <files>src/components/FindingFormSheet.tsx</files>
  <action>
    **CWD = /Users/jykevin/Documents/cbc7119-design/cha-bio-safety 필수.**

    **선행 의존:** Task 1 (Plan 01) commit 완료 후 진행. HEAD = Plan 01 commit hash 확인.

    ### 단계 1: 사전 검증
    ```bash
    cd /Users/jykevin/Documents/cbc7119-design/cha-bio-safety
    git status  # main 브랜치 + dirty 없음 + Plan 01 commit 완료 확인
    grep -c 'style={{' src/components/FindingFormSheet.tsx  # 34 확인
    ```

    ### 단계 2: 함수 스코프 const + module data const 정의 보존

    **module data const (L10-31):** ZONES / ZONE_FLOORS / FINDING_ITEMS — 변경 0.
    **module 함수 (L38-88):** parseFindingForEdit — 변경 0.
    **함수 스코프 const:**
    - `lblStyle: React.CSSProperties = { fontSize: 12, fontWeight: 700, color: 'var(--t3)', marginBottom: 6 }` (L178) — 정의 0 byte 보존
    - `inputStyle: React.CSSProperties = { ... }` (L179-194) — 정의 0 byte 보존
    - `chipStyle = (active: boolean): React.CSSProperties => ({ ... })` (L195-207) — 정의 0 byte 보존 (함수형 const)
    - `floorChipStyle = (active: boolean): React.CSSProperties => ({ ... })` (L208-220) — 정의 0 byte 보존

    참조 보존:
    - `style={lblStyle}` 6건 (L234/L245/L256/L268/L303/L317) — 그대로
    - `style={chipStyle(zone === z.key)}` 1건 (L237) — 그대로
    - `style={floorChipStyle(floor === f)}` 1건 (L248) — 그대로
    - `style={inputStyle}` 1건 (L263) — 그대로

    ### 단계 3: spread combo 잔존 결정
    - L296 `style={{ ...inputStyle, marginTop: 8 }}` (직접입력 input) — **옵션 N 보존** (spread combo, q5a partial 룰: 분리 가능하나 spread 보존이 의도 명확. inputStyle 정의 자체에 marginTop 없음 → spread 가 layout 가산)
    - L311 `style={{ ...inputStyle, resize: 'vertical' }}` (지적 내용 textarea) — **옵션 N 보존**

    ### 단계 4: formContent 내부 변환 (28건)

    **formContent root padding (L231):** L105 (Plan 01) IDENTICAL — `<div className={`flex flex-col gap-3.5 ${isDesktopSheet ? 'px-6' : 'py-3 px-4'}`}>`

    **구역 row wrap (L235):** `style={{ display: 'flex', gap: 6, flexWrap: 'wrap' }}` → `className="flex gap-1.5 flex-wrap"`

    **층 row wrap (L246):** `style={{ display: 'flex', gap: 4 }}` → `className="flex gap-1"`

    **위치 input (L263):** `style={inputStyle}` 그대로 보존 (참조)

    **지적 항목 container (L269):** `style={{ display: 'flex', flexDirection: 'column', gap: 0, border: '1px solid var(--bd2)', borderRadius: 9, overflow: 'hidden', maxHeight: 123, overflowY: 'auto' }}` → `className="flex flex-col gap-0 border border-border-strong rounded-[9px] overflow-hidden max-h-[123px] overflow-y-auto"`

    **지적 항목 button (L275, loop conditional + dynamic borderBottom):** 7-prop combo 중 6-prop conditional + 1 dynamic
    - 옵션 M template literal:
      ```
      className={`py-2.5 px-3 border-0 text-[13px] text-left cursor-pointer ${
        inspectionItem === item
          ? 'bg-[rgba(59,130,246,.1)] text-accent font-bold'
          : 'bg-surface-sunken text-text-primary font-normal'
      } ${
        i < FINDING_ITEMS.length - 1 ? 'border-b border-border-default' : ''
      }`}
      ```

    **직접입력 input (L296):** **옵션 N spread 보존** — `style={{ ...inputStyle, marginTop: 8 }}` 그대로

    **지적 내용 * span (L304):** `style={{ color: 'var(--danger)' }}` → `<span className="text-danger-bar">`

    **textarea (L311):** **옵션 N spread 보존** (resize:'vertical')

    **사진 input × 2 (L318/L319):** `style={{ display: 'none' }}` → `className="hidden"`

    **사진 grid wrap (L321):** L135 (Plan 01) IDENTICAL — `className="flex gap-2 overflow-x-auto pb-1"`

    **existing photo wrap (L324):** Plan 01 L137 IDENTICAL — `className="relative shrink-0"`

    **existing photo img (L325):** Plan 01 L138 IDENTICAL

    **existing photo ✕ btn (L329):** Plan 01 L142 IDENTICAL

    **slot photo wrap/img/✕ btn (L335/L336/L340):** Plan 01 L147/L148/L152 IDENTICAL

    **uploading overlay (L342):** Plan 01 L154 IDENTICAL

    **add photo btn (L346):** Plan 01 L158 IDENTICAL

    **📷 emoji span (L347):** Plan 01 L159 IDENTICAL

    **button area wrapper (L355):** Plan 01 L218 IDENTICAL — 옵션 M conditional

    **저장 button (L356):** Plan 01 L219 IDENTICAL — 옵션 M conditional

    **취소 button (L359):** Plan 01 L222 IDENTICAL

    ### 단계 5: isDesktopSheet wrapper 영역 변환 (10건)

    Plan 01 L231-L250 IDENTICAL 매핑:
    - L368 desktop backdrop → Plan 01 L231 동일
    - L369 desktop modal → Plan 01 L232 동일
    - L370 desktop header row → Plan 01 L233 동일
    - L371 desktop title → Plan 01 L234 동일
    - L372 desktop ✕ btn → Plan 01 L235 동일
    - L381 mobile backdrop → Plan 01 L244 동일 (overscroll-contain 전부 변환)
    - L382 mobile inner → Plan 01 L245 동일 (animation 만 inline 잔존)
    - L383 mobile drag handle row → Plan 01 L246 동일
    - L384 mobile drag handle bar → Plan 01 L247 동일 (w-[32px] h-1 arbitrary)
    - L386 mobile header wrap → Plan 01 L249 동일
    - L387 mobile title → Plan 01 L250 동일

    ### 단계 6: 자동 검증
    - inline 카운트 34 → 1~4 잔존 (옵션 N: marginTop spread × 1 + resize spread × 1 + animation × 1 = 최대 3 또는 4)
    - 비즈 anchor (13 onClick / 6 useState / 1 useMutation IDENTICAL)
    - emoji 변동 0 (✕ 3 + 📷 1 = 4 보존)
    - lblStyle/inputStyle/chipStyle/floorChipStyle const 정의 보존 + 참조 6+1+1+1=9 IDENTICAL
    - ZONES/ZONE_FLOORS/FINDING_ITEMS/parseFindingForEdit IDENTICAL
    - TypeScript 0 error
    - off-scope 0
    - vite build PASS

    ### 단계 7: 단일 atomic commit
    ```bash
    cd /Users/jykevin/Documents/cbc7119-design/cha-bio-safety
    git add src/components/FindingFormSheet.tsx
    git commit -m "$(cat <<'EOF'
feat(260529-s07-02): Phase B Wave 16a-2 — FindingFormSheet (34 inline) → tailwind

- Plan 02 — FindingFormSheet.tsx 34 inline → ~1-4 잔존 (옵션 N: marginTop spread + resize spread + animation)
- 옵션 X+P+M+색변수N+module const N (lblStyle/inputStyle/chipStyle/floorChipStyle) 승계
- ZONES / ZONE_FLOORS / FINDING_ITEMS / parseFindingForEdit module-scope 정의 0 byte 보존
- 비즈 anchor IDENTICAL (13 onClick / 6 useState / 1 useMutation)
- 시각 0 byte (모바일 BottomSheet + 데스크톱 모달 양쪽 픽셀 1:1)
- emoji ✕ 3 + 📷 1 = 4 보존
- Tier 3 components batch — 23번째 atomic
- Wave 16a 페어 완결 (Plan 01 + Plan 02 = 77 inline batch 분할 atomic)
EOF
)"
    ```
  </action>
  <verify>
    <automated>cd /Users/jykevin/Documents/cbc7119-design/cha-bio-safety && B=$(git show HEAD~1:src/components/FindingFormSheet.tsx | grep -c 'style={{') && A=$(grep -c 'style={{' src/components/FindingFormSheet.tsx) && echo "inline: $B → $A" && [ "$A" -le 4 ] && echo "PASS inline ≤ 4" && for ANCHOR in 'onClick=\{[^}]+\}' 'useState\(' 'useRef\(' 'useEffect\(' 'useMutation\(' 'useQuery\(' 'useNavigate\(' 'useParams\(' 'fetch\('; do BB=$(git show HEAD~1:src/components/FindingFormSheet.tsx | grep -cE "$ANCHOR") && AA=$(grep -cE "$ANCHOR" src/components/FindingFormSheet.tsx) && [ "$BB" = "$AA" ] && echo "  $ANCHOR: $BB OK" || echo "  $ANCHOR: $BB→$AA MISMATCH"; done && echo "lblStyle ref: $(grep -c 'style={lblStyle}' src/components/FindingFormSheet.tsx) (expect 6)" && echo "inputStyle ref: $(grep -c 'style={inputStyle}' src/components/FindingFormSheet.tsx) (expect 1)" && echo "chipStyle ref: $(grep -cE 'style=\{chipStyle' src/components/FindingFormSheet.tsx) (expect 1)" && echo "floorChipStyle ref: $(grep -cE 'style=\{floorChipStyle' src/components/FindingFormSheet.tsx) (expect 1)" && echo "ZONES def: $(grep -c 'const ZONES' src/components/FindingFormSheet.tsx) (expect 1)" && echo "ZONE_FLOORS def: $(grep -c 'const ZONE_FLOORS' src/components/FindingFormSheet.tsx) (expect 1)" && echo "FINDING_ITEMS def: $(grep -c 'const FINDING_ITEMS' src/components/FindingFormSheet.tsx) (expect 1)" && echo "parseFindingForEdit def: $(grep -c 'function parseFindingForEdit' src/components/FindingFormSheet.tsx) (expect 1)" && echo "emoji: $(grep -cE '✕|📷' src/components/FindingFormSheet.tsx) (expect 4)" && ./node_modules/.bin/tsc --noEmit 2>&1 | grep -E 'error TS' | wc -l && cd .. && git diff --name-only HEAD | grep -v 'cha-bio-safety/' | wc -l</automated>
  </verify>
  <done>
    - FindingFormSheet.tsx inline count: 34 → 1~4 잔존 (옵션 N spread × 2 + animation × 1~2)
    - 비즈 anchor (13 onClick / 6 useState / 1 useMutation) IDENTICAL
    - lblStyle/inputStyle/chipStyle/floorChipStyle const 정의 보존 + 참조 6+1+1+1=9 IDENTICAL
    - ZONES/ZONE_FLOORS/FINDING_ITEMS/parseFindingForEdit module-scope 0 byte
    - emoji ✕ 3 + 📷 1 = 4 보존
    - 비표준 색 토큰 0
    - TypeScript 0 error
    - off-scope 0
    - vite build PWA generation PASS
    - 단일 atomic commit (260529-s07-02) ✓
    - **Wave 16a 페어 완결 (Plan 01 + Plan 02)** — Tier 3 components batch 첫 wave 종결
  </done>
</task>

</tasks>

<verification>
## 최종 verify gate (Wave 16a 페어 완결 — Plan 01 + Plan 02 합산)

```bash
cd /Users/jykevin/Documents/cbc7119-design/cha-bio-safety

# === 1. inline count drop ===
for F in src/components/FindingEditModal.tsx src/components/FindingFormSheet.tsx; do
  B=$(git show HEAD~2:$F | grep -c 'style={{')
  A=$(grep -c 'style={{' $F)
  echo "$F: $B → $A"
done
# 예상: FindingEditModal 43 → 1~3, FindingFormSheet 34 → 1~4

# === 2. 비즈 anchor identity (양 파일) ===
for F in src/components/FindingEditModal.tsx src/components/FindingFormSheet.tsx; do
  for ANCHOR in 'onClick=\{[^}]+\}' 'useState\(' 'useRef\(' 'useEffect\(' 'useMutation\(' 'useQuery\(' 'useNavigate\(' 'useParams\(' 'fetch\('; do
    B=$(git show HEAD~2:$F | grep -cE "$ANCHOR")
    A=$(grep -cE "$ANCHOR" $F)
    [ "$B" = "$A" ] && echo "$F $ANCHOR: $B (OK)" || echo "$F $ANCHOR: $B→$A MISMATCH ❌"
  done
done

# === 3. precise onClick diff (양 파일) ===
for F in src/components/FindingEditModal.tsx src/components/FindingFormSheet.tsx; do
  git show HEAD~2:$F | grep -oE 'onClick=\{[^}]+\}' | sort | uniq > /tmp/before-${F##*/}.txt
  grep -oE 'onClick=\{[^}]+\}' $F | sort | uniq > /tmp/after-${F##*/}.txt
  diff /tmp/before-${F##*/}.txt /tmp/after-${F##*/}.txt
done

# === 4. emoji + 비색 (변동 0) ===
for F in src/components/FindingEditModal.tsx src/components/FindingFormSheet.tsx; do
  echo "$F emoji: $(grep -cE '✓|✗|🔒|💾|🔥|⏰|📋|✅|⚠️|❌|🔧|🚨|🔍|🧯|📊|✕|📷' $F)"
  echo "$F 비색: $(grep -cE 'bg-warning[^-]|border-safe[^-]|border-warning[^-]|border-danger[^-]' $F)"
done
# 예상: FindingEditModal emoji 7 / 비색 0, FindingFormSheet emoji 4 / 비색 0

# === 5. const 정의 + 참조 보존 ===
echo "=== FindingEditModal ==="
echo "  lblStyle def: $(grep -c 'const lblStyle' src/components/FindingEditModal.tsx)"
echo "  inputStyle def: $(grep -c 'const inputStyle' src/components/FindingEditModal.tsx)"
echo "  style={lblStyle} ref: $(grep -c 'style={lblStyle}' src/components/FindingEditModal.tsx)"
echo "  style={inputStyle} ref: $(grep -c 'style={inputStyle}' src/components/FindingEditModal.tsx)"

echo "=== FindingFormSheet ==="
echo "  lblStyle def: $(grep -c 'const lblStyle' src/components/FindingFormSheet.tsx)"
echo "  inputStyle def: $(grep -c 'const inputStyle' src/components/FindingFormSheet.tsx)"
echo "  chipStyle def: $(grep -c 'const chipStyle' src/components/FindingFormSheet.tsx)"
echo "  floorChipStyle def: $(grep -c 'const floorChipStyle' src/components/FindingFormSheet.tsx)"
echo "  ZONES def: $(grep -c 'const ZONES' src/components/FindingFormSheet.tsx)"
echo "  ZONE_FLOORS def: $(grep -c 'const ZONE_FLOORS' src/components/FindingFormSheet.tsx)"
echo "  FINDING_ITEMS def: $(grep -c 'const FINDING_ITEMS' src/components/FindingFormSheet.tsx)"
echo "  parseFindingForEdit def: $(grep -c 'function parseFindingForEdit' src/components/FindingFormSheet.tsx)"

# === 6. TypeScript ===
./node_modules/.bin/tsc --noEmit 2>&1 | grep -E 'error TS' | wc -l

# === 7. off-scope ===
cd .. && git diff --name-only HEAD~2 | grep -v 'cha-bio-safety/' | wc -l  # MUST = 0

# === 8. vite build PWA generation ===
cd cha-bio-safety && npm run build 2>&1 | tail -20

# === 9. commit count = 2 (Plan 01 + Plan 02 atomic) ===
git log --oneline -3 | head -2
```
</verification>

<success_criteria>
## Wave 16a 페어 완결 Success Criteria (Plan 01 + Plan 02)

### Plan 01 (FindingEditModal 43 → 1~3 잔존)
- [ ] inline 카운트 43 → 1~3 잔존 (옵션 N spread × 2 + 가능 animation × 1)
- [ ] 비즈 anchor 13 onClick / 3 useState / 1 useMutation IDENTICAL
- [ ] lblStyle const def 1 + 참조 5 보존
- [ ] inputStyle const def 1 + 참조 1 보존
- [ ] emoji ✕ 5 + 📷 2 = 7 보존
- [ ] TypeScript 0 error
- [ ] 단일 atomic commit (260529-s07-01)

### Plan 02 (FindingFormSheet 34 → 1~4 잔존)
- [ ] inline 카운트 34 → 1~4 잔존 (옵션 N spread × 2 + 가능 animation × 1~2)
- [ ] 비즈 anchor 13 onClick / 6 useState / 1 useMutation IDENTICAL
- [ ] lblStyle/inputStyle/chipStyle/floorChipStyle const def + 참조 9 보존
- [ ] ZONES/ZONE_FLOORS/FINDING_ITEMS/parseFindingForEdit 0 byte
- [ ] emoji ✕ 3 + 📷 1 = 4 보존
- [ ] TypeScript 0 error
- [ ] 단일 atomic commit (260529-s07-02)

### 페어 합산
- [ ] 양 commit 모두 main 브랜치 직접 commit (cbc7119-preview 자동 deploy 대상)
- [ ] off-scope 변경 0 (두 .tsx 만)
- [ ] vite build PWA generation PASS
- [ ] **Tier 3 components batch 첫 wave 완결 마커** — 22번째+23번째 atomic 누적
- [ ] 직원 도메인 cbc7119 production 영향 0 (main → production 별도 cherry-pick 시까지)

### 컨펌 게이트 (사용자 — 본 atomic 완료 후)
- cbc7119-preview 자동 deploy 대기 (GitHub Actions)
- 모바일 BottomSheet 시각 검증 (FindingEditModal + FindingFormSheet 양쪽 — legal-findings page 진입 → 지적사항 등록/수정 모달 열기)
- 데스크톱 모달 시각 검증 (양쪽)
- 시각 회귀 0 확인 후 다음 Wave 16b dispatch 또는 묶음 D production cherry-pick 결정
</success_criteria>

<output>
After completion, create:
- `.planning/quick/260529-s07-phase-b-wave-16a/260529-s07-SUMMARY.md` (unified — Plan 01 + Plan 02 통합 SUMMARY)

SUMMARY 항목 (precedent qn3 IDENTICAL):
- Before/After 카운트 표 (양 plan 합산)
- 변환 매핑 표 (Plan 01 41건 + Plan 02 28건)
- 옵션 N 잔존 매핑 표 (양 plan 합산 3~7건)
- Verify gate 결과 (양 plan 합산 12+ 게이트)
- Phase B Tier 3 누적 atomic 표 (Wave 16a 페어 완결 마커)
- 신규 패턴 박제 (components batch 첫 사례 + dual-render isDesktopSheet 페어 변환 + module 함수 const 보존 + ZONES/ZONE_FLOORS data const 변경 0)
- 다음 단계 (Wave 16b — InstallPrompt + ExcelPreview + DesktopSidebar 41+18+17=76 inline)
- Self-Check
</output>
