---
phase: 260530-7ca-redesign-32-header-unification-h6a-11-px
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/InspectionPage.tsx
autonomous: true
requirements:
  - H6A-01
must_haves:
  truths:
    - "InspectionPage 카테고리 헤더 11 곳이 sketch v2 §케이스 3 표준 (h-12 px-3) 으로 통일된다"
    - "px-4 py-2.5 가 InspectionPage.tsx 안에서 카테고리 헤더 11 곳 전부에서 제거된다"
    - "L4437 (조치 결과 모달 CP info row) 의 px-4 py-2.5 는 보존된다 — 다른 의미의 컨테이너"
    - "헤더 안 카테고리 아이콘 / 타이틀 텍스트 / 우측 액션 / ChevronLeft 4 곳은 한 byte도 변하지 않는다"
    - "px-3.5 py-2 서브 헤더 17 곳 (H6b 별도 wave) 은 한 byte도 변하지 않는다"
    - "본문 5000+ 줄 (CP 카드 / 모달 / 폼) 은 한 byte도 변하지 않는다"
    - "vite build 와 TypeScript 검사가 모두 통과한다"
  artifacts:
    - path: "src/pages/InspectionPage.tsx"
      provides: "카테고리 헤더 11 곳 h-12 px-3 통일된 InspectionPage"
      contains: "h-12 px-3 bg-surface-page"
  key_links:
    - from: "src/pages/InspectionPage.tsx 카테고리 헤더 11 곳"
      to: "tailwind.config spacing — h-12 (48px) / px-3 (12px)"
      via: "Tailwind 유틸 클래스"
      pattern: "h-12 px-3 bg-surface-page"
---

<objective>
InspectionPage.tsx (6047줄 메가 파일) 안 카테고리 헤더 11 곳의 컨테이너 패딩을
`px-4 py-2.5` → `h-12 px-3` 으로 통일한다.

**Purpose:** sketch v2 §케이스 3 표준 (h-12 px-3, 48px height, 12px horizontal padding)
은 이미 GlobalHeader / DivPage / AnnualPlanPage / WorkShiftPage / FloorPlanPage 등에
적용되어 있다. InspectionPage 카테고리 헤더 11 곳만 잔존 — H6 wave 의 H6a 부분.
서브 헤더 (px-3.5 py-2) 17 곳은 H6b 별도 wave 에서 처리, 이번엔 무수정.

**Output:** InspectionPage.tsx 한 파일에서 11 곳 className 만 변경. 본문 / 안 버튼 /
아이콘 / 텍스트 0 변경.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/quick/260529-header-unification/sketch-v2.html

<!-- 11 카테고리 헤더 카탈로그 (Wave 0 grep 결과) -->
<interfaces>
src/pages/InspectionPage.tsx 안 `px-4 py-2.5` 매칭 12 곳:

L364:  flex items-center gap-2.5 px-4 py-2.5 bg-surface-page border-b border-border-default flex-shrink-0    [HEADER #1]
L613:  flex items-center gap-2.5 px-4 py-2.5 bg-surface-page border-b border-border-default flex-shrink-0    [HEADER #2]
L836:  flex items-center gap-2.5 px-4 py-2.5 bg-surface-page border-b border-border-default flex-shrink-0    [HEADER #3]
L1543: flex items-center gap-2.5 px-4 py-2.5 bg-surface-page border-b border-border-default flex-shrink-0    [HEADER #4]
L2041: flex items-center gap-2.5 px-4 py-2.5 bg-surface-page border-b border-border-default flex-shrink-0    [HEADER #5]
L2365: flex items-center px-4 py-2.5 bg-surface-page border-b border-border-default flex-shrink-0 gap-2.5    [HEADER #6 — 순서 다름]
L2617: flex items-center px-4 py-2.5 bg-surface-page border-b border-border-default flex-shrink-0 gap-2.5    [HEADER #7 — 순서 다름]
L3004: flex items-center gap-2.5 px-4 py-2.5 bg-surface-page border-b border-border-default flex-shrink-0    [HEADER #8]
L3846: shrink-0 bg-surface-page border-b border-border-default px-4 py-2.5 flex items-center gap-2.5         [HEADER #9 — shrink-0 prefix + 순서 매우 다름]
L4437: px-4 py-2.5 border-b border-border-default shrink-0 flex items-center gap-2                           [⚠ 모달 CP info row — 보존 대상]
L5366: flex items-center gap-2.5 px-4 py-2.5 bg-surface-page border-b border-border-default flex-shrink-0    [HEADER #10]

(grep 결과 11 라인 — 사용자가 "11 곳" 이라고 한 것은 카테고리 헤더 기준,
 L4437 은 별개. 따라서 총 grep 매칭 11 → 변환 대상 10 + 보존 1 = "10 변환".
 그러나 카테고리 헤더 11 곳 spec 와 일치하려면 1 곳 추가 grep 필요)

추가 grep 필요 — `bg-surface-page` 검색으로 누락된 카테고리 헤더 1 곳 확인:
  grep -n 'bg-surface-page' src/pages/InspectionPage.tsx | grep -v 'px-4 py-2.5' | grep 'border-b'
</interfaces>

<!-- 식별 마커 분석 -->
<discrimination>
**카테고리 헤더 (변환 대상)** 의 공통 마커:
  - `bg-surface-page` (Header #1~10 모두 포함)
  - `border-b border-border-default`
  - `flex items-center`
  - `gap-2.5`
  - `flex-shrink-0` 또는 `shrink-0`

**L4437 모달 CP info row (보존)** 의 차별점:
  - `bg-surface-page` 가 **없음** ← 가장 강한 차별 마커
  - `gap-2` (2.5 아님)
  - 부모가 별개 모달 컨테이너 (조치 결과 모달)

**결론:** `px-4 py-2.5 bg-surface-page` 를 검색 키로 쓰면 L4437 자동 제외.
</discrimination>

<sketch_spec>
sketch-v2.html §케이스 3 (line 304):
```
모바일 헤더: className="h-12 px-3 bg-surface-raised border-b border-border-default flex items-center gap-2.5 shrink-0"
```

InspectionPage 헤더는 `bg-surface-page` (raised 아님) 를 쓰는 점만 다름.
이는 의도적 — 카테고리 페이지가 페이지 배경(투명한 어두운 톤) 위에 있는 헤더이기 때문.
sketch v2 line 745: "FloorPlanPage 모바일 px-4 py-2.5 → h-12 px-3" 와 같은 변환 패턴.
sketch v2 line 748: "InspectionPage 16 헤더 (카테고리 8 + 서브 8) px-4 py-2.5 / px-3.5 py-2 → h-12 px-3"

이 wave (H6a) 는 카테고리 헤더 부분만 처리. H6b 는 서브 헤더 17 곳 (px-3.5 py-2) 별도.
</sketch_spec>

</context>

<tasks>

<task type="auto">
  <name>Task 1: InspectionPage 카테고리 헤더 11 곳 px-4 py-2.5 → h-12 px-3 통일</name>
  <files>src/pages/InspectionPage.tsx</files>

  <action>
**STEP 0 — Wave 0 사전 검증 (변경 전 grep gate):**

```bash
# 0-1. 전체 px-4 py-2.5 매칭 카탈로그 출력
grep -n 'px-4 py-2.5' src/pages/InspectionPage.tsx

# 0-2. 카테고리 헤더 마커 (`bg-surface-page` + `border-b`) 확인
grep -nE 'bg-surface-page.*border-b|border-b.*bg-surface-page' src/pages/InspectionPage.tsx | head -20

# 0-3. px-4 py-2.5 가 아닌데 bg-surface-page + border-b 인 곳 (이미 h-12 px-3 인지 등 누락 후보)
grep -n 'bg-surface-page' src/pages/InspectionPage.tsx | grep -v 'px-4 py-2.5'
```

**기대 결과:**
- 0-1: 11 줄 (L364, L613, L836, L1543, L2041, L2365, L2617, L3004, L3846, L4437, L5366)
- 0-3: 만약 추가로 `h-12 px-3 bg-surface-page` 또는 `bg-surface-page` 만 있는 카테고리 헤더가 나오면 plan 의 "11 곳" 정의와 비교

만약 0-1 결과가 11 줄 정확히 (위 카탈로그 일치) → 진행.
만약 다르면 → STOP, 결과를 사용자에게 보고하고 카탈로그 갱신 후 진행.

---

**STEP 1 — 변환 (Edit replace_all, 3 단계):**

이 변환은 `px-4 py-2.5` 자체를 globally replace 하면 L4437 (모달 CP info — `bg-surface-page` 없음, 보존 대상) 가 잘못 매칭됨.
따라서 **마커 `bg-surface-page` 포함된 패턴만** 매칭하도록 3 단계로 분리:

**1-A. 표준 순서 (Header #1~5, #8, #10 — 총 7 곳):**
```
Edit:
  old_string: "flex items-center gap-2.5 px-4 py-2.5 bg-surface-page border-b border-border-default flex-shrink-0"
  new_string: "flex items-center gap-2.5 h-12 px-3 bg-surface-page border-b border-border-default flex-shrink-0"
  replace_all: true
```
→ 7 곳 매칭 예상 (L364, L613, L836, L1543, L2041, L3004, L5366)

**1-B. 순서 다름 — gap-2.5 후미 (Header #6, #7 — 총 2 곳):**
```
Edit:
  old_string: "flex items-center px-4 py-2.5 bg-surface-page border-b border-border-default flex-shrink-0 gap-2.5"
  new_string: "flex items-center h-12 px-3 bg-surface-page border-b border-border-default flex-shrink-0 gap-2.5"
  replace_all: true
```
→ 2 곳 매칭 예상 (L2365, L2617)

**1-C. shrink-0 prefix (Header #9 — 1 곳):**
```
Edit:
  old_string: "shrink-0 bg-surface-page border-b border-border-default px-4 py-2.5 flex items-center gap-2.5"
  new_string: "shrink-0 bg-surface-page border-b border-border-default h-12 px-3 flex items-center gap-2.5"
  replace_all: true
```
→ 1 곳 매칭 예상 (L3846)

**합계:** 7 + 2 + 1 = 10 변환.

⚠ **사용자 prompt 는 "11 곳"** — STEP 0 의 0-3 grep 결과에 따라 11번째 카테고리 헤더 1 곳을 추가 발견하면 동일 패턴으로 4번째 Edit 추가.
만약 발견 안 되면 사용자 카탈로그 (L364, L613, L836, L1543, L2041, L2365, L2617, L3004, L3846, L5366) 가 10 곳 — "11 곳" 표현은 L4437 포함한 grep 카운트일 가능성.
이 경우 10 곳 변환 + L4437 보존이 의도된 결과.

---

**STEP 2 — 보존 확인 (L4437 모달 CP info row):**

```bash
# L4437 부근의 className 이 그대로인지 확인
grep -n 'px-4 py-2.5' src/pages/InspectionPage.tsx
```

**기대 결과:** 정확히 1 줄 — L4437 의 `className="px-4 py-2.5 border-b border-border-default shrink-0 flex items-center gap-2"`.
이 1 줄만 남으면 보존 성공.

---

**STEP 3 — 본문 0 byte 변경 검증:**

```bash
# px-3.5 py-2 서브 헤더 17 곳 모두 보존
grep -c 'px-3.5 py-2' src/pages/InspectionPage.tsx
# 기대: 17 (또는 STEP 0 grep 시 카운트와 동일)

# ChevronLeft 4 곳 보존
grep -c 'ChevronLeft' src/pages/InspectionPage.tsx
# 기대: 변경 전과 동일

# w-7 h-7 (back 버튼 표준 크기) 보존
grep -c 'w-7 h-7' src/pages/InspectionPage.tsx
# 기대: 변경 전과 동일
```

---

**주의 사항 (per 메모리 룰):**
- L4437 의 `px-4 py-2.5` 는 **반드시 보존** — 조치 결과 모달의 CP info row 는 카테고리 헤더가 아니다 (다른 의미, 다른 모양).
- src/pages/FloorPlanPage.tsx:934 의 주석 `/* 모바일: px-4 py-2.5 + 뒤로가기 */` 와 src/pages/ElevatorPage.tsx:2492 의 ElevatorPage 헤더 (다른 파일) 는 이 wave 의 scope 가 아니다. InspectionPage.tsx 만 수정.
- 사용자 메모리 룰 (feedback_design_changes_ask_first): 본 wave 는 sketch v2 표준 적용으로 사전 컨펌된 작업. 추가 디자인 변경 (예: 다른 헤더로 통일, 색상 변경 등) 발견 시 STOP + 사용자 확인.
- 사용자 메모리 룰 (feedback_tailwind_w8_h8_is_48px): h-12 = 48px 가 맞다 (tailwind.config spacing override 없음 — 12*4=48).
- 사용자 메모리 룰 (feedback_tailwind_token_class_pattern): `bg-surface-page` 그대로 유지 (status- prefix 패턴 아님).
- wrangler 금지, npm run deploy 금지 (이 워크트리 룰).
  </action>

  <verify>
<automated>
# Gate 1: px-4 py-2.5 잔존 = 정확히 1 (L4437 모달 CP info row 만 보존)
test "$(grep -v '^#' src/pages/InspectionPage.tsx | grep -c 'px-4 py-2.5')" -eq 1 && echo "GATE 1 PASS" || echo "GATE 1 FAIL"

# Gate 2: h-12 px-3 추가 = 정확히 10 (또는 STEP 0 에서 11번째 발견 시 11)
# 변경 전 0 가정. 변경 후 10 (또는 11) 이면 PASS.
test "$(grep -v '^#' src/pages/InspectionPage.tsx | grep -c 'h-12 px-3 bg-surface-page')" -ge 10 && echo "GATE 2 PASS" || echo "GATE 2 FAIL"

# Gate 3: bg-surface-page 가 카테고리 헤더 마커로 보존되는지
test "$(grep -v '^#' src/pages/InspectionPage.tsx | grep -c 'bg-surface-page border-b')" -ge 10 && echo "GATE 3 PASS" || echo "GATE 3 FAIL"

# Gate 4: px-3.5 py-2 서브 헤더 17 곳 무수정 (H6b 별도 wave)
test "$(grep -v '^#' src/pages/InspectionPage.tsx | grep -c 'px-3.5 py-2')" -eq 17 && echo "GATE 4 PASS" || echo "GATE 4 FAIL (expected 17 - confirm pre-change count first)"

# Gate 5: TypeScript / Vite build PASS
npm run build 2>&1 | tail -20
</automated>
  </verify>

  <done>
- src/pages/InspectionPage.tsx 의 카테고리 헤더 10~11 곳 모두 `h-12 px-3 bg-surface-page` 로 통일
- L4437 모달 CP info row 의 `px-4 py-2.5` 보존
- 서브 헤더 17 곳 (`px-3.5 py-2`) 0 byte 변경
- 헤더 안 아이콘 / 타이틀 / 우측 액션 / ChevronLeft 0 byte 변경
- 본문 5000+ 줄 0 byte 변경 (git diff 검토 — 변경 라인 수 ≤ 11)
- `npm run build` 성공, TypeScript 0 error
- atomic commit:
  `feat(redesign/32-header-unification-h6a): 소방안전 점검 카테고리 헤더 11 곳 px-4 py-2.5 → h-12 px-3 (sketch v2 §케이스 3 표준)`
  </done>
</task>

</tasks>

<verification>
**Phase-level checks:**
1. `git diff src/pages/InspectionPage.tsx --stat` 변경 라인 수 ≤ 11 (각 헤더 1 줄씩)
2. `git diff src/pages/InspectionPage.tsx | grep -c '^+'` 추가된 라인 = 변경된 헤더 수
3. `git diff src/pages/InspectionPage.tsx | grep -c '^-'` 제거된 라인 = 변경된 헤더 수
4. `grep -c 'px-4 py-2.5' src/pages/InspectionPage.tsx` → 1 (L4437 보존 표시)
5. `npm run build` PASS
6. 시각 검증 (cbc7119-preview 배포 후): 소방안전 점검 16 카테고리 진입 시 헤더 통일성 확인

**Out of scope (no changes):**
- src/pages/FloorPlanPage.tsx
- src/pages/ElevatorPage.tsx
- src/pages/InspectionPage.tsx 서브 헤더 17 곳 (`px-3.5 py-2` — H6b 별도 wave)
- InspectionPage 헤더 안 모든 자식 요소 (아이콘 / 타이틀 / 액션 버튼 / ChevronLeft)
</verification>

<success_criteria>
- 11 grep 매칭 중 10 (또는 STEP 0 결과에 따라 11) 곳이 `h-12 px-3` 으로 변경
- L4437 1 곳은 `px-4 py-2.5` 그대로 보존
- vite build 성공 + TypeScript 0 error
- 사용자 메모리 룰 (Phase 폐기 / wrangler 금지 / npm run deploy 금지) 준수
- atomic 1 commit, redesign/32-header-unification-h6a-11-px 또는 redesign/32-header-unification-h6a 브랜치
- 메인 브랜치 머지 → GitHub Actions → cbc7119-preview.pages.dev 자동 배포 (직원 도메인 cbc7119 영향 0)
</success_criteria>

<output>
After completion, create `.planning/quick/260530-7ca-redesign-32-header-unification-h6a-11-px/260530-7ca-SUMMARY.md` documenting:
- 사전 grep gate 결과 (정확히 몇 곳 매칭됐는지)
- 실제 변환 곳 수 (10 vs 11 — STEP 0 결과에 따라)
- L4437 보존 확인
- git diff 라인 수
- vite build 결과
- 다음 wave (H6b 서브 헤더 17 곳 px-3.5 py-2 → h-12 px-3) 진입 가능 여부
</output>
