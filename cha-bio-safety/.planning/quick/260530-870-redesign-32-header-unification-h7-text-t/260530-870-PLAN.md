---
phase: quick-260530-870
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/styles/components.css
  - src/pages/SchedulePage.tsx
  - src/pages/EducationPage.tsx
  - src/pages/LegalFindingsPage.tsx
  - src/pages/ElevatorPage.tsx
  - src/pages/WorkShiftPage.tsx
  - src/pages/AnnualPlanPage.tsx
  - src/pages/DivPage.tsx
  - src/pages/FloorPlanPage.tsx
  - src/pages/RemediationDetailPage.tsx
  - src/pages/StaffManagePage.tsx
  - src/pages/InspectionPage.tsx
autonomous: true
requirements:
  - REDESIGN-32-H7
tags:
  - redesign
  - header-unification
  - typography
must_haves:
  truths:
    - "12+3 페이지 헤더 타이틀이 모두 18px / 600 / lh 1.4 (text-title font-semibold) GlobalHeader 표준으로 통일된다"
    - "components.css L25 .page-title 정의가 font-weight 700 → 600 으로 바뀌어 일일업무일지/점검일지출력/업무수행기록 3 페이지에 일괄 적용된다"
    - "SchedulePage / EducationPage / LegalFindingsPage / ElevatorPage 데스크 / WorkShiftPage / AnnualPlanPage / DivPage 데스크 / FloorPlanPage / RemediationDetailPage / StaffManagePage 데스크 10 곳의 헤더 타이틀이 직접 className 수정으로 통일된다"
    - "InspectionPage 카테고리 헤더 안 타이틀 9 곳 (L367 / L616 / L839 / L1545 / L2043 / L2368 / L2620 / L3007 / L3849) 가 동일 표준으로 통일된다"
    - "본문 안 (모달/카드/표) `text-body font-bold text-text-primary` 패턴은 한 byte 도 변경되지 않는다"
    - "vite build PASS / TypeScript 0 error"
  artifacts:
    - path: "src/styles/components.css"
      provides: ".page-title font-weight 600"
      contains: "font-weight: 600"
    - path: "src/pages/SchedulePage.tsx"
      provides: "월간 점검 계획 헤더 통일"
      contains: "text-title font-semibold text-text-primary"
    - path: "src/pages/EducationPage.tsx"
      provides: "보수교육 헤더 통일"
      contains: "text-title font-semibold text-text-primary flex-1 text-center"
    - path: "src/pages/LegalFindingsPage.tsx"
      provides: "{headerTitle} 모바일 헤더 통일"
      contains: "text-title font-semibold text-text-primary"
    - path: "src/pages/ElevatorPage.tsx"
      provides: "승강기 관리 데스크 헤더 통일"
      contains: "flex-1 text-title font-semibold text-text-primary"
    - path: "src/pages/WorkShiftPage.tsx"
      provides: "월간 출근부 헤더 통일"
      contains: "text-title font-semibold text-text-primary flex-1"
    - path: "src/pages/AnnualPlanPage.tsx"
      provides: "연간 업무 추진 계획 헤더 통일"
      contains: "text-title font-semibold text-text-primary flex-1"
    - path: "src/pages/DivPage.tsx"
      provides: "DIV 압력 관리 데스크 헤더 통일 (text-[16px] arbitrary 제거)"
      contains: "flex-1 text-title font-semibold text-text-primary"
    - path: "src/pages/FloorPlanPage.tsx"
      provides: "소방 시설 도면 헤더 통일 (truncate 보존)"
      contains: "text-title font-semibold text-text-primary truncate"
    - path: "src/pages/RemediationDetailPage.tsx"
      provides: "조치 상세 헤더 통일 (text-base + leading-tight 제거)"
      contains: "text-title font-semibold text-text-primary"
    - path: "src/pages/StaffManagePage.tsx"
      provides: "직원 관리 데스크 헤더 통일 (text-body-sm → text-title, 14 → 18)"
      contains: "desktop-header-title flex-1 text-title font-semibold text-text-primary"
    - path: "src/pages/InspectionPage.tsx"
      provides: "9 개 카테고리 헤더 안 타이틀 통일"
      contains: "text-title font-semibold text-text-primary"
  key_links:
    - from: "src/styles/components.css L25"
      to: "DailyReportPage / WorkLogPage / ReportsPage (.page-title 사용 페이지)"
      via: "CSS class .page-title 단일 정의"
      pattern: "font-weight: 600"
    - from: "각 페이지 헤더 span/div"
      to: "디자인 시스템 토큰 text-title (18px / lh 1.4)"
      via: "Tailwind utility class"
      pattern: "text-title font-semibold text-text-primary"
---

<objective>
12 파일 + 1 CSS + InspectionPage 9 곳 = 헤더 타이틀 글자 전수 통일 (옵션 A = GlobalHeader 표준 `text-title font-semibold` = 18px / 600 / lh 1.4).

Purpose: 헤더 통일 시리즈 H1~H6a 누적 14 페이지 통일 마무리. 글자 weight / size 표준화로 페이지 간 시각 일관성 확보.
Output: 22 곳 헤더 타이틀이 단일 atomic commit 으로 동일 spec 적용. 본문 안 동일 패턴은 한 byte 무수정.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md
@./CLAUDE.local.md
@.planning/quick/260529-header-unification/sketch-v5-header-titles.html
@src/styles/components.css
@src/pages/SchedulePage.tsx
@src/pages/EducationPage.tsx
@src/pages/LegalFindingsPage.tsx
@src/pages/ElevatorPage.tsx
@src/pages/WorkShiftPage.tsx
@src/pages/AnnualPlanPage.tsx
@src/pages/DivPage.tsx
@src/pages/FloorPlanPage.tsx
@src/pages/RemediationDetailPage.tsx
@src/pages/StaffManagePage.tsx
@src/pages/InspectionPage.tsx

# 표준 / 규칙
- 핵심 표준 = `text-title font-semibold text-text-primary` (+ 필요 시 `truncate` / `text-center` / `flex-1` 보존)
- 본문 (모달/카드/표) 안 동일 패턴 `text-body font-bold text-text-primary` 는 **한 byte 변경 0** — 헤더 영역만
- 워크트리: cha-bio-safety/ (cbc7119-design 디자인 격리 리포)
- wrangler 명령 금지, npm run deploy 금지
- 직접 commit + push (cbc7119-preview 자동 배포)
</context>

<tasks>

<task type="auto">
  <name>Task 1: 헤더 타이틀 22 곳 일괄 통일 (text-title font-semibold)</name>
  <files>src/styles/components.css, src/pages/SchedulePage.tsx, src/pages/EducationPage.tsx, src/pages/LegalFindingsPage.tsx, src/pages/ElevatorPage.tsx, src/pages/WorkShiftPage.tsx, src/pages/AnnualPlanPage.tsx, src/pages/DivPage.tsx, src/pages/FloorPlanPage.tsx, src/pages/RemediationDetailPage.tsx, src/pages/StaffManagePage.tsx, src/pages/InspectionPage.tsx</files>
  <action>
13 곳 직접 변경 (12 파일) + InspectionPage 9 곳 = 22 곳. 아래 BEFORE/AFTER 완전 verbatim 매칭.

──────────────────────────────────────────────
### 1. src/styles/components.css L25 (.page-title CSS — 3 페이지 공유)

BEFORE (L25):
```
  .page-title { flex: 1; font-size: 18px; font-weight: 700; color: var(--text-primary); line-height: 1.4; }
```
AFTER:
```
  .page-title { flex: 1; font-size: 18px; font-weight: 600; color: var(--text-primary); line-height: 1.4; }
```
변경: `font-weight: 700` → `font-weight: 600` (한 토큰)
영향: 일일업무일지 / 점검일지출력 / 업무수행기록 3 페이지 자동 반영 (자체 tsx 0 byte)

──────────────────────────────────────────────
### 2. src/pages/SchedulePage.tsx L583

BEFORE:
```
        <span className="flex-1 text-title font-bold text-text-primary">월간 점검 계획</span>
```
AFTER:
```
        <span className="flex-1 text-title font-semibold text-text-primary">월간 점검 계획</span>
```
변경: `font-bold` → `font-semibold` (size 이미 text-title 동일)

──────────────────────────────────────────────
### 3. src/pages/EducationPage.tsx L466

BEFORE:
```
        <span className="text-body font-bold text-text-primary flex-1 text-center">
```
AFTER:
```
        <span className="text-title font-semibold text-text-primary flex-1 text-center">
```
변경: `text-body font-bold` → `text-title font-semibold`. `flex-1 text-center` 순서/잔존 보존.

──────────────────────────────────────────────
### 4. src/pages/LegalFindingsPage.tsx L295

BEFORE:
```
          <span className="text-body font-bold text-text-primary">{headerTitle}</span>
```
AFTER:
```
          <span className="text-title font-semibold text-text-primary">{headerTitle}</span>
```

──────────────────────────────────────────────
### 5. src/pages/ElevatorPage.tsx L603 (데스크 헤더)

BEFORE:
```
          <span className="flex-1 text-body font-bold text-text-primary">승강기 관리</span>
```
AFTER:
```
          <span className="flex-1 text-title font-semibold text-text-primary">승강기 관리</span>
```

──────────────────────────────────────────────
### 6. src/pages/WorkShiftPage.tsx L100

BEFORE:
```
        <span className="text-body font-bold text-text-primary flex-1">월간 출근부</span>
```
AFTER:
```
        <span className="text-title font-semibold text-text-primary flex-1">월간 출근부</span>
```

──────────────────────────────────────────────
### 7. src/pages/AnnualPlanPage.tsx L139

BEFORE:
```
        <span className="text-body font-bold text-text-primary flex-1">연간 업무 추진 계획</span>
```
AFTER:
```
        <span className="text-title font-semibold text-text-primary flex-1">연간 업무 추진 계획</span>
```

──────────────────────────────────────────────
### 8. src/pages/DivPage.tsx L958 (데스크 헤더, text-[16px] arbitrary 정리)

BEFORE:
```
          <span className="flex-1 text-[16px] font-bold text-text-primary">DIV 압력 관리</span>
```
AFTER:
```
          <span className="flex-1 text-title font-semibold text-text-primary">DIV 압력 관리</span>
```
변경: `text-[16px] font-bold` → `text-title font-semibold` (size 16 → 18, arbitrary 제거)

──────────────────────────────────────────────
### 9. src/pages/FloorPlanPage.tsx L949 (truncate 보존)

BEFORE:
```
          <div className="text-body font-bold text-text-primary truncate">소방 시설 도면</div>
```
AFTER:
```
          <div className="text-title font-semibold text-text-primary truncate">소방 시설 도면</div>
```
변경: 앞 두 토큰만 교체. `truncate` 잔존.

──────────────────────────────────────────────
### 10. src/pages/RemediationDetailPage.tsx L279 (leading-tight 제거)

BEFORE:
```
        <span className="text-base font-bold leading-tight text-text-primary">조치 상세</span>
```
AFTER:
```
        <span className="text-title font-semibold text-text-primary">조치 상세</span>
```
변경: `text-base` → `text-title`, `font-bold` → `font-semibold`, `leading-tight` **제거** (text-title 자체 lh 1.4 — 디자인 시스템 토큰). 토큰 순서 정리 (text → font → color).

──────────────────────────────────────────────
### 11. src/pages/StaffManagePage.tsx L394 (데스크 헤더, 가장 큰 변화 14 → 18)

BEFORE:
```
          <span className="desktop-header-title flex-1 text-body-sm font-bold text-text-primary">
```
AFTER:
```
          <span className="desktop-header-title flex-1 text-title font-semibold text-text-primary">
```
변경: `text-body-sm font-bold` → `text-title font-semibold`. **size 14 → 18 (가장 큰 변화)**. `desktop-header-title flex-1` 보존.

──────────────────────────────────────────────
### 12. src/pages/InspectionPage.tsx 9 곳 (카테고리 헤더 안 타이틀)

각 line BEFORE → AFTER 정확 매칭. 본문 안 동일 패턴 보존 (L1148/L1672/L2129/L2143/L3940/L4288 등 + 모달 L1528/L2026 `text-title font-bold` — 모달이라 보존).

**L367** (leading-tight + truncate 보존, leading-tight 는 카테고리 헤더 다중 라벨 처리용 의도 보존)
BEFORE:
```
          <div className="text-body font-bold text-text-primary leading-tight truncate">
```
AFTER:
```
          <div className="text-title font-semibold text-text-primary leading-tight truncate">
```
※ 주의: 다른 헤더(L2620/L3007/L3849)는 leading-tight 없음. L367 만 leading-tight 보존 (카테고리 그룹 라벨 다중 표시 컨테이너 의도). 만약 단일 라벨 표시면 lh 토큰 시스템과 충돌하지 않음 — 시각 검증 후 단순화 가능하나 본 wave 는 보존.

**L616** (truncate 보존, 단일 라벨)
BEFORE:
```
          <div className="text-body font-bold text-text-primary truncate">CCTV 점검</div>
```
AFTER:
```
          <div className="text-title font-semibold text-text-primary truncate">CCTV 점검</div>
```

**L839** (truncate 없음)
BEFORE:
```
          <div className="text-body font-bold text-text-primary">{group.labels[0]}</div>
```
AFTER:
```
          <div className="text-title font-semibold text-text-primary">{group.labels[0]}</div>
```

**L1545** (span, DIV 점검)
BEFORE:
```
        <span className="text-body font-bold text-text-primary">DIV 점검</span>
```
AFTER:
```
        <span className="text-title font-semibold text-text-primary">DIV 점검</span>
```

**L2043** (span, 컴프레셔 점검)
BEFORE:
```
        <span className="text-body font-bold text-text-primary">컴프레셔 점검</span>
```
AFTER:
```
        <span className="text-title font-semibold text-text-primary">컴프레셔 점검</span>
```

**L2368** (truncate 없음)
BEFORE:
```
          <div className="text-body font-bold text-text-primary">{group.labels[0]}</div>
```
AFTER:
```
          <div className="text-title font-semibold text-text-primary">{group.labels[0]}</div>
```

**L2620** (truncate 보존)
BEFORE:
```
          <div className="text-body font-bold text-text-primary truncate">
```
AFTER:
```
          <div className="text-title font-semibold text-text-primary truncate">
```

**L3007** (truncate 보존)
BEFORE:
```
          <div className="text-body font-bold text-text-primary truncate">
```
AFTER:
```
          <div className="text-title font-semibold text-text-primary truncate">
```

**L3849** (truncate 보존)
BEFORE:
```
          <div className="text-body font-bold text-text-primary truncate">
```
AFTER:
```
          <div className="text-title font-semibold text-text-primary truncate">
```

──────────────────────────────────────────────
### 보존 룰 (한 byte 변경 0)

다음 패턴은 **건드리지 말 것**:

1. **InspectionPage 본문 안** `text-body font-bold text-text-primary` (모달/카드/표):
   - L1148, L1672, L2129, L2143, L3940, L4288 등 본문/모달/카드 내부
   - 카테고리 헤더 9 곳 (위 명시 line) **외 모든** 매칭은 본문이므로 보존

2. **InspectionPage 모달 안** `text-title font-bold` (점검 완료 모달 등):
   - L1528, L2026 — 헤더 아님, 모달 타이틀이라 보존

3. **DivPage 모달/본문**:
   - L429 / L897 `text-[16px] font-bold` (모달/본문 안)

4. **EducationPage 본문 안**:
   - L101 / L237 (본문 안 `text-body font-bold`)

5. **DashboardPage 본문 안** `text-body-sm font-bold` 다수:
   - L256, L266, L308, L707 등 — 카드/라벨

6. **그 외 모든 본문 / 모달 / 카드 / 표 안** 동일 패턴 — 보존

### 변경 후 grep 검증 (verify 에서 자동화)
- 헤더 영역만 22 곳 변환 확인
- 본문 패턴은 변환 전과 동일 (`text-body font-bold text-text-primary` 매칭 수가 22 만큼만 감소)

### 변경 방식 (executor 가이드)
- Edit 툴 직접 사용. 절대 grep 으로 카운트한 후 sed 일괄 치환 금지 (본문 패턴 오염 위험).
- 각 위치 BEFORE/AFTER 1:1 매칭으로 정확 line 변경.
- 텍스트 내용 ("월간 출근부" / "연간 업무 추진 계획" / "DIV 압력 관리" / "조치 상세" / "직원 관리" 등) 으로 헤더 컨텍스트 확실히 식별.

### 사고 회피
- DivPage L429 / L897 의 `text-[16px] font-bold` 본문 패턴 보존 (L958 데스크 헤더만 변경)
- EducationPage L466 (헤더, text-body) + L101/L237 (본문, text-body) 구분 확실
- InspectionPage 카테고리 헤더 9 곳 외 동일 패턴 (~80 곳 추정) 모두 보존
- StaffManagePage 데스크 헤더만 — 모바일 zone (L506+) 헤더는 별도 wave 책임
  </action>
  <verify>
    <automated>cd /Users/jykevin/Documents/cbc7119-design/cha-bio-safety && npm run build 2>&1 | tail -20 && npx tsc --noEmit 2>&1 | tail -5 && echo "---grep verify---" && echo "1. components.css .page-title font-weight 600 (must = 1):" && grep -c "font-weight: 600" src/styles/components.css && echo "2. SchedulePage 월간 점검 계획 헤더 (must = 1):" && grep -c "flex-1 text-title font-semibold text-text-primary\">월간 점검 계획" src/pages/SchedulePage.tsx && echo "3. EducationPage 보수교육 헤더 (must = 1):" && grep -c "text-title font-semibold text-text-primary flex-1 text-center" src/pages/EducationPage.tsx && echo "4. LegalFindingsPage headerTitle (must = 1):" && grep -c "text-title font-semibold text-text-primary\">{headerTitle}" src/pages/LegalFindingsPage.tsx && echo "5. ElevatorPage 승강기 관리 (must = 1):" && grep -c "flex-1 text-title font-semibold text-text-primary\">승강기 관리" src/pages/ElevatorPage.tsx && echo "6. WorkShiftPage 월간 출근부 (must = 1):" && grep -c "text-title font-semibold text-text-primary flex-1\">월간 출근부" src/pages/WorkShiftPage.tsx && echo "7. AnnualPlanPage 연간 업무 추진 계획 (must = 1):" && grep -c "text-title font-semibold text-text-primary flex-1\">연간 업무 추진 계획" src/pages/AnnualPlanPage.tsx && echo "8. DivPage DIV 압력 관리 (must = 1):" && grep -c "flex-1 text-title font-semibold text-text-primary\">DIV 압력 관리" src/pages/DivPage.tsx && echo "9. FloorPlanPage 소방 시설 도면 (must = 1):" && grep -c "text-title font-semibold text-text-primary truncate\">소방 시설 도면" src/pages/FloorPlanPage.tsx && echo "10. RemediationDetailPage 조치 상세 (must = 1):" && grep -c "text-title font-semibold text-text-primary\">조치 상세" src/pages/RemediationDetailPage.tsx && echo "11. StaffManagePage desktop-header-title (must = 1):" && grep -c "desktop-header-title flex-1 text-title font-semibold text-text-primary" src/pages/StaffManagePage.tsx && echo "12. InspectionPage text-title font-semibold 카테고리 헤더 (must >= 9):" && grep -c "text-title font-semibold text-text-primary" src/pages/InspectionPage.tsx && echo "---보존 검증 (변환된 패턴 잔존 수)---" && echo "InspectionPage text-body font-bold text-text-primary 잔존 (헤더 9 - 본문 다수, must >= 5):" && grep -c "text-body font-bold text-text-primary" src/pages/InspectionPage.tsx && echo "DivPage text-[16px] font-bold 잔존 본문 (must >= 2):" && grep -c "text-\[16px\] font-bold" src/pages/DivPage.tsx && echo "InspectionPage text-title font-bold 잔존 모달 타이틀 (must >= 2):" && grep -c "text-title font-bold" src/pages/InspectionPage.tsx</automated>
  </verify>
  <done>
- components.css L25 .page-title 가 font-weight 600 으로 변경됨 (700 → 600 한 토큰)
- 12 파일 11 곳 + InspectionPage 9 곳 = 22 곳 모두 `text-title font-semibold text-text-primary` 적용
- 본문 / 모달 / 카드 안 `text-body font-bold text-text-primary` 다수 잔존 (보존됨)
- DivPage 본문 `text-[16px] font-bold` 2 곳 (L429/L897) 잔존
- InspectionPage 모달 `text-title font-bold` 2 곳 (L1528/L2026) 잔존
- npm run build PASS / TypeScript 0 error
- git diff 합계 약 13 ins / 13 del (CSS 1 + tsx 12) 수준 — 라인 수 변동 0
  </done>
</task>

</tasks>

<verification>
- vite build PASS / TypeScript 0 error
- 22 곳 헤더 타이틀 모두 표준 통일 확인
- 본문 / 모달 / 카드 안 동일 패턴 한 byte 변경 0 (잔존 grep 카운트 확인)
- 시각 확인 cbc7119-preview (사용자 검증):
  - 일일업무일지 / 점검일지출력 / 업무수행기록 3 페이지 .page-title 자동 반영
  - SchedulePage / EducationPage / LegalFindingsPage / WorkShiftPage / AnnualPlanPage / FloorPlanPage / RemediationDetailPage 모바일 헤더
  - ElevatorPage / DivPage / StaffManagePage 데스크 헤더
  - InspectionPage 카테고리 헤더 9 종 (CCTV / 외부방화시설 / DIV / 컴프레셔 / 자탐 / 승강기 / 방화구획 / 일반 카테고리 등)
</verification>

<success_criteria>
- 22 곳 헤더 타이틀 18px / 600 / lh 1.4 통일
- 본문 패턴 한 byte 변경 0
- npm run build PASS / TypeScript 0 error
- atomic commit: `feat(redesign/32-header-unification-h7): 헤더 타이틀 통일 text-title font-semibold (18/600 GlobalHeader 표준, 13 곳 + InspectionPage 9 = 22 곳)`
- main push → cbc7119-preview 자동 배포 (wrangler 금지)
</success_criteria>

<output>
SUMMARY 위치 (워크트리 cha-bio-safety/ 상대):
.planning/quick/260530-870-redesign-32-header-unification-h7-text-t/260530-870-SUMMARY.md
</output>
