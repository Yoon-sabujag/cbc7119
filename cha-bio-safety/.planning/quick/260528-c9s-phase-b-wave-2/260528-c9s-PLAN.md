---
phase: 260528-c9s-phase-b-wave-2-loginpage-splashscreen
plan: 01
type: execute
wave: 2
depends_on: [260528-a3v]
files_modified:
  - src/pages/LoginPage.tsx
  - src/pages/SplashScreen.tsx
autonomous: true
requirements:
  - QUICK-260528-c9s-PHASE-B-WAVE-2
roadmap-wave: Tier 1 / Wave 2 (인증/스플래시 - 캘리브 위험)
---

<objective>
**Phase B Wave 2 — 인증/스플래시.** 마스터 로드맵 v2 Tier 1 두 번째 wave.

- LoginPage (223줄, 28 inline / 21 모바일+7 데스크톱)
- SplashScreen (89줄, 13 inline / 모두 shared)

**옵션 X+P+M+색변수N 승계.** 시각 0 byte. 비즈 anchor identity. Phase A 보존.

### 위험 anchor
- **SplashScreen**: 메모리 `project_redesign_28_splash_status.md` — 비즈 anchor 16건 1 byte 변경 0 precedent + cherry-pick 사고 6회 패턴. 옵션 X (정확값 arbitrary) 가 시각 0 byte 룰 보장.
- **LoginPage**: 메모리 `project_redesign_27_login_status.md` — 단순 페이지 축소 패턴 (단일 atomic precedent).
</objective>

<context>

### LoginPage 28 inline 변환 매핑

**P1 Padding 상수** (대부분 변환 가능)
- L85 `borderRadius:16, padding:16, marginBottom:12` → `rounded-lg p-4 mb-3`
- L86 `marginBottom:12` → `mb-3`
- L108 `marginBottom:4` → `mb-1`
- L118 `borderRadius:16, padding:16` → `rounded-lg p-4`
- L121 `display:'block', marginBottom:6` → `block mb-[6px]`
- L133 `display:'block', marginBottom:6` → `block mb-[6px]`
- L170 `textAlign:'center', marginTop:20` → `text-center mt-5`
- L190 `marginTop:2` → `mt-[2px]`
- L195 `padding:'16px 16px 24px'` → `px-4 pt-4 pb-6`
- L214 `marginTop:2` → `mt-[2px]`
- L207 `padding:'16px 20px 24px'` → `pt-4 px-5 pb-6`

**P2 Flex layout**
- L87 `display:'grid', gridTemplateColumns:'1fr 1fr', gap:8` → `grid grid-cols-2 gap-2`
- L119 `display:'flex', flexDirection:'column', gap:12` → `flex flex-col gap-3`
- L184 `gap:12` → `gap-3`
- L208 `gap:12, marginTop:16` → `gap-3 mt-4`
- L218 `flex:1, padding:'16px 16px 32px', overflowY:'auto'` → `flex-1 px-4 pt-4 pb-8 overflow-y-auto`

**P3 Sizing**
- L134 `position:'relative'` → `relative`
- L180 `minHeight:'100dvh', display:'flex', alignItems:'center', justifyContent:'center', padding:'20px'` → `min-h-[100dvh] flex items-center justify-center p-5`
- L181 `maxWidth:420, width:'100%', borderRadius:20, boxShadow:'0 8px 32px rgba(0,0,0,0.4)', overflow:'hidden'` → `max-w-[420px] w-full rounded-[20px] shadow-[0_8px_32px_rgba(0,0,0,0.4)] overflow-hidden`
- L183 `padding:'24px 24px 20px'` → `pt-6 px-6 pb-5`
- L205 `minHeight:'100dvh', display:'flex', flexDirection:'column'` → `min-h-[100dvh] flex flex-col`

**P5 Button reset / 정적 input**
- L143 `padding:'12px 44px 12px 14px', borderRadius:12, fontSize:14, outline:'none', transition:'border-color .15s'` → `pl-[14px] pr-[44px] py-3 rounded-md text-[14px] outline-none transition-[border-color] duration-150` (transition arbitrary)
- L149 `position:'absolute', right:12, top:'50%', transform:'translateY(-50%)', background:'none', border:'none', cursor:'pointer', fontSize:13` → `absolute right-3 top-1/2 -translate-y-1/2 bg-transparent border-0 cursor-pointer text-[13px]`

**P6 Dynamic (옵션 M template literal)**
- L97 button: `border:1px solid ${isSelected ? '...' : c.border}, background: isSelected ? '...' : c.color, cursor:'pointer', textAlign:'left', transition:'all .13s'` — 정적 부분 `flex items-center gap-2.5 p-2.5 rounded-md border cursor-pointer text-left transition-all duration-[130ms]` + 동적 border-color / background **잔존 옵션 N** (rgba 동적 변수)
- L104 `background:isSelected?'#2563eb':c.border` — 동적 색 변수 **잔존 옵션 N**, 정적 `display:'flex', alignItems:'center', justifyContent:'center', color:'#fff', flexShrink:0` → `flex items-center justify-center text-white shrink-0`
- L159 button: 정적 `padding:'14px', borderRadius:12, border:'none', fontSize:14, transition:'all .13s', marginTop:4` + 동적 `cursor:loading?'not-allowed':'pointer'` → `p-[14px] rounded-md border-0 text-[14px] transition-all duration-[130ms] mt-1 ${loading ? 'cursor-not-allowed' : 'cursor-pointer'}`

**P3 Sizing 정적**
- L185 `background:'rgba(37,99,235,0.2)', border:'1px solid rgba(59,130,246,0.3)', display:'flex', alignItems:'center', justifyContent:'center', overflow:'hidden', flexShrink:0` → `bg-[rgba(37,99,235,0.2)] border border-[rgba(59,130,246,0.3)] flex items-center justify-center overflow-hidden shrink-0`
- L209 동일 패턴

### LoginPage 예상: 28 → 잔존 ~2-3 (옵션 N 색변수)

### SplashScreen 13 inline 변환 매핑

**P3 Sizing (정확값 — 캘리브 보존)**
- L40 root: `minHeight:'100dvh', display:'flex', flexDirection:'column', alignItems:'center', justifyContent:'center', gap:0` → `min-h-[100dvh] flex flex-col items-center justify-center gap-0`
- L48 로고 wrapper: `animation:'slideUp .4s ease-out', display:'flex', flexDirection:'column', alignItems:'center', gap:20, marginBottom:56` → `[animation:slideUp_.4s_ease-out] flex flex-col items-center gap-5 mb-14`
- L49 `position:'relative'` → `relative`
- L52 로고 박스: `width:88, height:88, background:'rgba(37,99,235,0.2)', border:'1px solid rgba(59,130,246,0.3)', display:'flex', alignItems:'center', justifyContent:'center', overflow:'hidden'` → `w-[88px] h-[88px] bg-[rgba(37,99,235,0.2)] border border-[rgba(59,130,246,0.3)] flex items-center justify-center overflow-hidden`
- L60 img: `width: 64, height: 64, borderRadius: 14` → `w-16 h-16 rounded-[14px]` (w-16=64 default)
- L63 `textAlign:'center'` → `text-center`
- L64 `margin:0` → `m-0`
- L65 `margin:'6px 0 0', letterSpacing:'.1em'` → `mt-[6px] tracking-[.1em]`
- L70 `width:160` → `w-40` (w-40=160 default)
- L71 `height:2, background:'rgba(255,255,255,0.07)', borderRadius:2, overflow:'hidden'` → `h-[2px] bg-[rgba(255,255,255,0.07)] rounded-[2px] overflow-hidden`
- L74 `textAlign:'center', marginTop:10` → `text-center mt-[10px]`
- L79 `position:'absolute', bottom:20` → `absolute bottom-5`

**P6 Dynamic (옵션 N 잔존 — width 가 `${pct}%`)**
- L72 progress bar: 정적 부분 `height:'100%', borderRadius:2, transition:'width .05s linear'` → className `h-full rounded-[2px] transition-[width] duration-[50ms] ease-linear`, 동적 `width:\`${pct}%\`` **잔존 옵션 N**

### SplashScreen 예상: 13 → 잔존 1 (L72 width pct)

### 합계 예상

| 파일 | Before | After | 변환 | 잔존 |
|---|---:|---:|---:|---|
| LoginPage | 28 | 2-3 | 25-26 | 색변수 (L97 border/bg + L104 background) |
| SplashScreen | 13 | 1 | 12 | width pct (L72) |
| **합계** | **41** | **3-4** | **37-38** | **(-91%)** |

### 룰 (locked, all wave 공통)
- 옵션 X (정확값 arbitrary) — 캘리브 보존
- 옵션 P (leading 명시 보존)
- 옵션 M (className conditional + 색변수 N)
- 비즈 anchor precise diff = empty
- Phase A 보존 (Lucide / 색 토큰 -bar / emoji 0)
- TypeScript 0 error
- 변경 파일 = 2 .tsx 만

### 메모리 anchors
- `feedback_tailwind_w8_h8_is_48px.md`
- `feedback_tailwind_token_class_pattern.md`
- `feedback_text_caption_leading_none.md` (작은 컨테이너 leading-none)
- `project_redesign_28_splash_status.md` (캘리브 16건 1 byte 0)
- `project_redesign_27_login_status.md` (단순 페이지 단일 atomic)

</context>

<tasks>

<task type="auto">
  <name>Task 1: Bulk apply LoginPage + SplashScreen inline → tailwind</name>
  <files>
    src/pages/LoginPage.tsx
    src/pages/SplashScreen.tsx
  </files>
  <action>

### Step 1: Before snapshot

```bash
cd /Users/jykevin/Documents/cbc7119-design/cha-bio-safety
for F in src/pages/LoginPage.tsx src/pages/SplashScreen.tsx; do
  echo "$F before: $(grep -c 'style={{' $F)"
done

for F in src/pages/LoginPage.tsx src/pages/SplashScreen.tsx; do
  echo "=== $F ==="
  for ANCHOR in 'onClick=\{[^}]+\}' 'useState\(' 'useRef\(' 'useEffect\(' 'useMutation\(' 'useQuery\(' 'useNavigate\(' 'useParams\(' 'fetch\('; do
    echo "  $ANCHOR : $(grep -cE "$ANCHOR" $F)"
  done
done > /tmp/c9s-before.txt

for F in src/pages/LoginPage.tsx src/pages/SplashScreen.tsx; do
  grep -oE 'onClick=\{[^}]+\}' $F | sort | uniq > /tmp/c9s-clicks-$(basename $F .tsx).txt
done
```

### Step 2: Edit LoginPage.tsx

Apply 각 매핑 (context 참조). 28 inline 변환:
- P1/P2/P3 정적 (대부분) — className 으로 변환 + 기존 className 과 합병
- P6 동적 색변수 (L97/L104) — 정적 부분만 변환, 색변수 style 잔존 (옵션 N)
- L159 동적 cursor — 옵션 M template literal

특히 주의:
- L97 button: 정적 className + 동적 style (border 색/background) 잔존
- L104 div: 정적 className (이미 있음 `w-[34px] h-[34px] rounded-[10px]`) + 정적 flex 부분 추가 + 동적 background 잔존
- 캘리브 risk 룰: 모든 사이즈/패딩 값 정확값 보존

### Step 3: Edit SplashScreen.tsx

13 inline 변환 (context 참조). 캘리브 좌표 16건 1 byte 변경 0 precedent.
- L40 root style → 통째 className
- L48 animation property → arbitrary class `[animation:slideUp_.4s_ease-out]`
- L52 로고 박스 → arbitrary rgba 색 + 사이즈 보존
- L60 img → `w-16 h-16 rounded-[14px]`
- L72 progress bar inner — width: `${pct}%` 만 style 잔존, 나머지 className
- L79 절대 위치 → `absolute bottom-5`

### Step 4: After verification

```bash
cd /Users/jykevin/Documents/cbc7119-design/cha-bio-safety

# 1. style count
for F in src/pages/LoginPage.tsx src/pages/SplashScreen.tsx; do
  echo "$F after: $(grep -c 'style={{' $F)"
done
# 예상: Login 2-3 / Splash 1

# 2. 비즈 anchor identity
for F in src/pages/LoginPage.tsx src/pages/SplashScreen.tsx; do
  echo "=== $F ==="
  for ANCHOR in 'onClick=\{[^}]+\}' 'useState\(' 'useRef\(' 'useEffect\(' 'useMutation\(' 'useQuery\(' 'useNavigate\(' 'useParams\(' 'fetch\('; do
    echo "  $ANCHOR : $(grep -cE "$ANCHOR" $F)"
  done
done > /tmp/c9s-after.txt
diff /tmp/c9s-before.txt /tmp/c9s-after.txt
# MUST be empty

# 3. onClick precise
for F in src/pages/LoginPage.tsx src/pages/SplashScreen.tsx; do
  grep -oE 'onClick=\{[^}]+\}' $F | sort | uniq > /tmp/c9s-clicks-$(basename $F .tsx)-after.txt
  diff /tmp/c9s-clicks-$(basename $F .tsx).txt /tmp/c9s-clicks-$(basename $F .tsx)-after.txt
done
# MUST all empty

# 4. emoji + 비색
for F in src/pages/LoginPage.tsx src/pages/SplashScreen.tsx; do
  echo "$F emoji: $(grep -cE '✓|✗|🔒|💾|🔥|⏰|📋|✅|⚠️|❌|🔧|🚨|🔍|🧯|📊' $F)"
  echo "$F 비색: $(grep -cE 'bg-warning[^-]|border-safe[^-]|border-warning[^-]|border-danger[^-]' $F)"
done
# 모두 0

# 5. TypeScript
./node_modules/.bin/tsc --noEmit 2>&1 | grep -c "error TS"
# MUST = 0

# 6. file scope
cd .. && git diff --name-only HEAD | grep -v "cha-bio-safety/src/pages/\(LoginPage\|SplashScreen\).tsx" | grep -v ".planning/" | wc -l
# MUST = 0
```

### Step 5: Atomic commit

```bash
cd /Users/jykevin/Documents/cbc7119-design
git add cha-bio-safety/src/pages/LoginPage.tsx cha-bio-safety/src/pages/SplashScreen.tsx
git commit --no-verify -m "$(cat <<'EOF'
feat(260528-c9s-01): Phase B Wave 2 — LoginPage 28 + SplashScreen 13 inline → tailwind

LoginPage 28→N (옵션 M conditional + 동적 색변수 N 잔존) + SplashScreen 13→N (캘리브 룰 옵션 X 정확값 보존, width pct N 잔존).
옵션 X+P+M+색변수N 승계. 시각 0 byte. 캘리브 anchor 보존.
비즈 anchor identical (precise grep). TypeScript 0 error.
EOF
)"
```

### Step 6: SUMMARY.md

Write `cha-bio-safety/.planning/quick/260528-c9s-phase-b-wave-2/260528-c9s-SUMMARY.md`:
- 옵션 X+P+M+색변수N 명시
- Before/After grep count 표
- 변환 매핑 (per line)
- 잔존 inline style 목록 (옵션 N 케이스)
- 비즈 anchor identity 결과 (precise diff)
- 캘리브 anchor 보존 확인
- Phase A 보존 확인
- Self-Check: PASSED
- Next: Wave 3 (WorkShift + Annual)

Reference: `.../260528-a3v-phase-b-wave-1/260528-a3v-SUMMARY.md`

Do NOT commit SUMMARY.md — orchestrator handles.

  </action>
  <verify>
    <automated>cd /Users/jykevin/Documents/cbc7119-design/cha-bio-safety && LG=$(grep -c 'style={{' src/pages/LoginPage.tsx) && SP=$(grep -c 'style={{' src/pages/SplashScreen.tsx) && echo "Login=$LG Splash=$SP" && [ "$LG" -le "4" ] && [ "$SP" -le "2" ] && [ "$(./node_modules/.bin/tsc --noEmit 2>&1 | grep -c 'error TS')" = "0" ] && [ "$(grep -cE 'bg-warning[^-]|border-safe[^-]|border-warning[^-]|border-danger[^-]' src/pages/LoginPage.tsx src/pages/SplashScreen.tsx | awk -F: '{s+=$2} END{print s}')" = "0" ] && cd .. && [ "$(git diff --name-only HEAD | grep -v 'cha-bio-safety/src/pages/' | grep -v '.planning/' | wc -l | tr -d ' ')" = "0" ]</automated>
  </verify>
  <done>
    - LoginPage.tsx inline style ≤4 (Login 28→2-3 예상)
    - SplashScreen.tsx inline style ≤2 (Splash 13→1 예상)
    - 비즈 anchor precise diff = empty (2 파일)
    - emoji 0 + 비색 0 (Phase A 보존)
    - 캘리브 anchor 시각 0 byte 보존
    - TypeScript 0 error
    - 변경 파일 = 2 .tsx 만
  </done>
</task>

</tasks>

<commits>
- Pre-dispatch: `docs(260528-c9s): pre-dispatch plan for Phase B Wave 2`
- Task 1: `feat(260528-c9s-01): Phase B Wave 2 — LoginPage 28 + SplashScreen 13 ...`
- Docs: `docs(quick-260528-c9s): Wave 2 완료 — SUMMARY + STATE`
</commits>
