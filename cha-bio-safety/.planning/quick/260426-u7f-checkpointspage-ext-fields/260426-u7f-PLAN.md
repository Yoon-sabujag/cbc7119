---
phase: 260426-u7f
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/CheckpointsPage.tsx
autonomous: true
requirements:
  - U7F-01  # 카테고리=소화기 선택 시 소화기 전용 필드 노출 + extinguisherApi.create 라우팅
must_haves:
  truths:
    - "/checkpoints 개소 추가 모달에서 카테고리 '소화기' 선택 시 종류 셀렉트(분말 20kg/분말 3.3kg/할로겐/K급) + 6개 선택 필드(제조업체/제조년월/형식승인번호/접두문자/증지번호/제조번호)가 노출된다"
    - "카테고리 '소화기' + 종류 + zone + floor + 개소명을 입력하고 저장하면 extinguishers 행과 check_points 행이 동시에 생성된다 (서버가 mgmt_no/cpId/qrCode 자동 발급)"
    - "성공 토스트에 자동 발급된 mgmt_no 가 노출된다 (예: '소화기 등록 완료 (연-07-03)')"
    - "카테고리 '소화기' 일 때 위치번호/설명 필드는 숨겨진다 (자동 발급되므로 무의미)"
    - "카테고리가 소화기가 아닐 때(소화전/스프링클러/자동화재탐지설비/등) 추가 흐름과 UI 는 변경 전과 100% 동일하다"
    - "mode='edit' 흐름은 변경 전과 100% 동일하다 (소화기 전용 필드 노출 X, updateMutation/deactivateMutation 무수정)"
    - "FPM- 마커 분기는 변경 전과 100% 동일하다"
    - "TypeScript 타입체크 PASS, 프로덕션 빌드 PASS"
  artifacts:
    - path: "src/pages/CheckpointsPage.tsx"
      provides: "카테고리=소화기 분기 처리된 CheckPointModalContent (createMutation 분기, ExtState, conditional JSX 블록)"
      contains: "extinguisherApi.create"
  key_links:
    - from: "src/pages/CheckpointsPage.tsx (CheckPointModalContent.createMutation)"
      to: "extinguisherApi.create"
      via: "form.category === '소화기' 일 때 분기 호출"
      pattern: "extinguisherApi\\.create"
    - from: "src/pages/CheckpointsPage.tsx (extForm state)"
      to: "ExtState 7개 필드 (type/manufacturer/manufactured_at/approval_no/prefix_code/seal_no/serial_no)"
      via: "useState<ExtState>"
      pattern: "extForm\\.type"
    - from: "form.zone (office/research/common)"
      to: "extinguisherApi.create body.zone (연/사/공)"
      via: "zoneMap 변환 — FloorPlanPage 705 동일"
      pattern: "zoneMap.*research.*연"
---

<objective>
`/checkpoints` 페이지의 "개소 추가" 모달에서 카테고리="소화기" 선택 시에만 소화기 전용 필드(종류 + 6개 선택 필드)를 노출하고, 저장 시 `checkPointApi.create` 대신 `extinguisherApi.create` 로 라우팅하여 `extinguishers` 행과 `check_points` 행이 동시에 생성되도록 한다. 그 외 카테고리는 기존 동작 무수정. mode='edit' 흐름과 FPM- 마커 분기도 무수정.

Purpose: 현재 `/checkpoints` 에서 소화기 개소를 추가해도 `extinguishers` 행이 안 만들어져 제조사/형식승인/증지번호/제조번호 등 소화기 상세정보를 보유하지 못한다. `/floorplan` "신규 등록"(FloorPlanPage) 에는 이미 양쪽 행을 만드는 흐름이 있으나, 운영 중에는 `/checkpoints` 에서 추가하는 경우가 많다. 두 진입점이 동등한 데이터 무결성을 갖도록 맞춘다.

Output: 수정된 `src/pages/CheckpointsPage.tsx` + 프로덕션 배포된 PWA. 사용자가 앱 재설치 후 검증.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
# Project / current state
@CLAUDE.md

# 수정 대상 파일 (CheckPointModalContent — 100~265 줄에 분기 추가)
@src/pages/CheckpointsPage.tsx

# 참조 — newExt state 패턴 (별도 컴포넌트 추출 X, 인라인)
@src/pages/FloorPlanPage.tsx

# extinguisherApi.create 시그니처 (api.ts 177~195)
@src/utils/api.ts

# 서버 자동 발급 규칙 (mgmt_no, cpId, qrCode)
@functions/api/extinguishers/create.ts

<interfaces>
<!-- 실제 코드에서 추출한 contracts. 실행자가 코드베이스 재탐색 없이 그대로 사용 -->

src/utils/api.ts:177-195 (이미 export 되어 있음 — import 만 추가):
```typescript
export const extinguisherApi = {
  getDetail: (checkPointId: string) => api.get<ExtinguisherDetail | null>(`/extinguishers/${checkPointId}`),
  list: (params?: {...}) => ...,
  create: (data: {
    floor: string; zone: string; location: string; type: string;
    approval_no?: string; manufactured_at?: string; manufacturer?: string;
    prefix_code?: string; seal_no?: string; serial_no?: string; note?: string;
    category?: string;
  }) => api.post<{ checkPointId: string; mgmtNo: string }>('/extinguishers/create', data),
}
```

src/pages/CheckpointsPage.tsx:1-8 — 현재 import (extinguisherApi 추가 필요):
```typescript
import { checkPointApi, floorPlanMarkerApi } from '../utils/api'
```
→ 다음으로 변경:
```typescript
import { checkPointApi, floorPlanMarkerApi, extinguisherApi } from '../utils/api'
```

src/pages/CheckpointsPage.tsx:87-98 — 기존 form state 타입:
```typescript
interface CpFormState {
  location: string; category: string; zone: string; floor: string;
  description: string; locationNo: string;
}
const EMPTY_CP_FORM: CpFormState = { location: '', category: '', zone: '', floor: '', description: '', locationNo: '' }
const ZONE_FLOORS: Record<string, string[]> = {
  office: ['8-1F', '8F', '7F', '6F', '5F', '3F', '2F', '1F'],
  research: ['8-1F', '8F', '7F', '6F', '5F', '3F', '2F', '1F'],
  common: ['B1F', 'M', 'B2F', 'B3F', 'B4F', 'B5F'],
}
```

src/pages/FloorPlanPage.tsx:703-710 — zone 매핑 + extinguisherApi.create 호출 패턴 (그대로 따라가기):
```typescript
const zoneMap: Record<string, string> = { research: '연', office: '사', common: '공' }
const zoneChar = zoneMap[addZone] ?? '연'
const res = await extinguisherApi.create({
  floor, zone: zoneChar, location: newExt.location, type: newExt.type,
  approval_no: newExt.approval_no || undefined, manufactured_at: newExt.manufactured_at || undefined,
  manufacturer: newExt.manufacturer || undefined, prefix_code: newExt.prefix_code || undefined,
  seal_no: newExt.seal_no || undefined, serial_no: newExt.serial_no || undefined,
})
```

functions/api/extinguishers/create.ts (서버) — 자동 발급 규칙 요약:
- floor + zone + location + type 필수
- mgmt_no = `${zone}-${extFloor}-${순번}` (예: 연-07-03)
- cpId = `CP-FE-${seq}` (전체 seq_no 기준)
- qrCode = `QR-${zone}-${floorNum}-${순번}`
- check_points 의 zone 은 영문(research/office/common)으로 자동 변환됨
- check_points + extinguishers 양쪽 INSERT (트랜잭션 아님이지만 D1 단일 워커이므로 사실상 안전)
- 응답: `{ checkPointId, mgmtNo }`

src/pages/CheckpointsPage.tsx:152-160 — 현재 createMutation (소화기 분기 추가 대상):
```typescript
const createMutation = useMutation({
  mutationFn: () => {
    const id = `cp_${Date.now()}`
    const qrCode = `QR-${id}`
    return checkPointApi.create({ id, qrCode, floor: form.floor, zone: form.zone as BuildingZone, location: form.location, category: form.category, description: form.description || undefined, locationNo: form.locationNo || undefined })
  },
  onSuccess: () => { qc.invalidateQueries({ queryKey: ['check-points'] }); qc.invalidateQueries({ queryKey: ['check-point-categories'] }); toast.success('개소가 추가되었습니다'); onClose() },
  onError: () => toast.error('저장에 실패했습니다. 입력값을 확인해 주세요'),
})
```

src/pages/CheckpointsPage.tsx:209 — 현재 canSave (소화기 분기 추가 대상):
```typescript
const canSave = form.location.trim() !== '' && form.category !== ''
```

src/pages/CheckpointsPage.tsx:253-264 — 위치번호/설명 필드 위치 (소화기일 때 숨김 처리 대상):
```jsx
<div>
  <label style={LABEL_STYLE}>개소명 <span style={{ color: 'var(--danger)' }}>*</span></label>
  <input style={INPUT_STYLE} value={form.location} onChange={setField('location')} placeholder="1층 로비 소화기" />
</div>
<div>
  <label style={LABEL_STYLE}>위치번호</label>
  <input style={INPUT_STYLE} value={form.locationNo} onChange={setField('locationNo')} placeholder="001 (선택)" />
</div>
<div>
  <label style={LABEL_STYLE}>설명</label>
  <input style={INPUT_STYLE} value={form.description} onChange={setField('description')} placeholder="메모 (선택)" />
</div>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CheckpointsPage 카테고리=소화기 분기 추가 (state, mutation, canSave, JSX, import)</name>
  <files>src/pages/CheckpointsPage.tsx</files>
  <action>
`src/pages/CheckpointsPage.tsx` 의 `CheckPointModalContent` 컴포넌트에 카테고리="소화기" 분기를 추가한다. 다른 컴포넌트는 건드리지 않는다.

### A) Import 수정 (line 6)
변경 전: `import { checkPointApi, floorPlanMarkerApi } from '../utils/api'`
변경 후: `import { checkPointApi, floorPlanMarkerApi, extinguisherApi } from '../utils/api'`

### B) ExtState 타입 + 상수 추가 (98~99 줄 사이, ZONE_FLOORS 상수 직후)
```typescript
interface ExtState {
  type: string
  manufacturer: string
  manufactured_at: string
  approval_no: string
  prefix_code: string
  seal_no: string
  serial_no: string
}
const EMPTY_EXT: ExtState = { type: '', manufacturer: '', manufactured_at: '', approval_no: '', prefix_code: '', seal_no: '', serial_no: '' }
```

### C) extForm state + isExtCategory 파생값 추가 (109 줄 confirmDeactivate 직후)
```typescript
const [extForm, setExtForm] = useState<ExtState>(EMPTY_EXT)
const isExtCategory = form.category === '소화기'
```

(카테고리 변경 시 extForm 초기화는 불필요 — 이전 입력 유지가 자연스럽다. 단, 다른 카테고리로 바꾸면 isExtCategory 가 false 가 되어 자동 무시됨. 추가 reset useEffect 만들지 말 것.)

### D) createMutation 분기 (152~160 줄 교체)
```typescript
const createMutation = useMutation({
  mutationFn: async () => {
    if (isExtCategory) {
      // zone 영문 → 한글 (연/사/공) — FloorPlanPage 703~710 패턴 동일
      const zoneMap: Record<string, string> = { research: '연', office: '사', common: '공' }
      const zoneChar = zoneMap[form.zone] ?? '연'
      return extinguisherApi.create({
        floor: form.floor,
        zone: zoneChar,
        location: form.location,
        type: extForm.type,
        approval_no: extForm.approval_no || undefined,
        manufactured_at: extForm.manufactured_at || undefined,
        manufacturer: extForm.manufacturer || undefined,
        prefix_code: extForm.prefix_code || undefined,
        seal_no: extForm.seal_no || undefined,
        serial_no: extForm.serial_no || undefined,
      })
    }
    // 비-소화기: 기존 흐름 유지
    const id = `cp_${Date.now()}`
    const qrCode = `QR-${id}`
    return checkPointApi.create({ id, qrCode, floor: form.floor, zone: form.zone as BuildingZone, location: form.location, category: form.category, description: form.description || undefined, locationNo: form.locationNo || undefined })
  },
  onSuccess: (data: any) => {
    qc.invalidateQueries({ queryKey: ['check-points'] })
    qc.invalidateQueries({ queryKey: ['check-point-categories'] })
    if (isExtCategory && data?.mgmtNo) {
      toast.success(`소화기 등록 완료 (${data.mgmtNo})`)
    } else {
      toast.success('개소가 추가되었습니다')
    }
    onClose()
  },
  onError: () => toast.error('저장에 실패했습니다. 입력값을 확인해 주세요'),
})
```

### E) canSave 분기 (209 줄 교체)
변경 전:
```typescript
const canSave = form.location.trim() !== '' && form.category !== ''
```
변경 후:
```typescript
const canSave =
  form.location.trim() !== '' &&
  form.category !== '' &&
  (!isExtCategory || (extForm.type !== '' && form.zone !== '' && form.floor !== ''))
```

(소화기일 때만 종류/zone/floor 추가 필수. 그 외는 기존 룰 그대로.)

### F) JSX 수정 — 소화기 전용 필드 추가 + 위치번호/설명 숨김 (252~264 줄)

`{form.zone && (층 셀렉트)}` 블록(242~252) 직후 + `<div><label>개소명...` 블록(253~256) 직전 사이에 다음을 추가:

```tsx
{isExtCategory && (
  <>
    <div>
      <label style={LABEL_STYLE}>종류 <span style={{ color: 'var(--danger)' }}>*</span></label>
      <select style={{ ...INPUT_STYLE, appearance: 'none', cursor: 'pointer' }}
        value={extForm.type}
        onChange={e => setExtForm(p => ({ ...p, type: e.target.value }))}>
        <option value="">종류 선택</option>
        <option value="분말 20kg">분말 20kg</option>
        <option value="분말 3.3kg">분말 3.3kg</option>
        <option value="할로겐">할로겐</option>
        <option value="K급">K급</option>
      </select>
    </div>
    <div>
      <label style={LABEL_STYLE}>제조업체</label>
      <input style={INPUT_STYLE} value={extForm.manufacturer}
        onChange={e => setExtForm(p => ({ ...p, manufacturer: e.target.value }))}
        placeholder="예: 한울방재" />
    </div>
    <div>
      <label style={LABEL_STYLE}>제조년월</label>
      <input style={INPUT_STYLE} value={extForm.manufactured_at}
        onChange={e => setExtForm(p => ({ ...p, manufactured_at: e.target.value }))}
        placeholder="예: 2024-04 (YYYY-MM)" />
    </div>
    <div>
      <label style={LABEL_STYLE}>형식승인번호</label>
      <input style={INPUT_STYLE} value={extForm.approval_no}
        onChange={e => setExtForm(p => ({ ...p, approval_no: e.target.value }))}
        placeholder="예: 수소10-19-11" />
    </div>
    <div>
      <label style={LABEL_STYLE}>접두문자</label>
      <input style={INPUT_STYLE} value={extForm.prefix_code}
        onChange={e => setExtForm(p => ({ ...p, prefix_code: e.target.value }))}
        placeholder="예: BEQV" />
    </div>
    <div>
      <label style={LABEL_STYLE}>증지번호</label>
      <input style={INPUT_STYLE} value={extForm.seal_no}
        onChange={e => setExtForm(p => ({ ...p, seal_no: e.target.value }))}
        placeholder="예: 72605" />
    </div>
    <div>
      <label style={LABEL_STYLE}>제조번호</label>
      <input style={INPUT_STYLE} value={extForm.serial_no}
        onChange={e => setExtForm(p => ({ ...p, serial_no: e.target.value }))}
        placeholder="예: 68605" />
    </div>
  </>
)}
```

그리고 위치번호/설명 두 블록(257~264 줄)을 `{!isExtCategory && (...)}` 로 감싸서 소화기일 때 숨긴다:

변경 전:
```tsx
<div>
  <label style={LABEL_STYLE}>위치번호</label>
  <input style={INPUT_STYLE} value={form.locationNo} onChange={setField('locationNo')} placeholder="001 (선택)" />
</div>
<div>
  <label style={LABEL_STYLE}>설명</label>
  <input style={INPUT_STYLE} value={form.description} onChange={setField('description')} placeholder="메모 (선택)" />
</div>
```
변경 후:
```tsx
{!isExtCategory && (
  <>
    <div>
      <label style={LABEL_STYLE}>위치번호</label>
      <input style={INPUT_STYLE} value={form.locationNo} onChange={setField('locationNo')} placeholder="001 (선택)" />
    </div>
    <div>
      <label style={LABEL_STYLE}>설명</label>
      <input style={INPUT_STYLE} value={form.description} onChange={setField('description')} placeholder="메모 (선택)" />
    </div>
  </>
)}
```

(개소명 필드는 양쪽 카테고리 공통 → 그대로 유지.)

### G) 회귀 가드 — 변경 금지 항목 (재확인)

다음 항목은 **수정 금지**:
- `updateMutation` (166~187 줄) — mode='edit' 분기는 그대로
- `deactivateMutation` (189~207 줄) — 비활성화 흐름 그대로
- `isMarker` 분기 (164 줄, FPM- 마커) — 마커 처리 그대로
- `handleSave` (212~219 줄) — 분기 로직은 createMutation 내부로 옮겨졌으므로 handleSave 자체는 무수정
- 그 외 페이지 레벨 코드(목록 렌더링, 검색, 필터, 정렬 등)
- mode='edit' 모달의 JSX 본문 (소화기 카테고리여도 추가 필드 노출 안 함 — extinguishers update API 가 아직 없음)

### H) 자동 기본값 useEffect 와의 상호작용 확인 (125~147 줄)

기존 useEffect 는 `mode === 'add' && form.category` 조건이므로 카테고리="소화기" 일 때도 동작한다. 이 useEffect 가 `form.locationNo` 와 `form.location` 을 자동 채우는데, 소화기일 때 위치번호 필드는 숨겨지지만 form.locationNo 자체는 set 되어도 무해(서버 호출에 사용 안 함)하므로 useEffect 는 그대로 유지. **수정 금지.**

### 검증

- 변경 후 `npx tsc --noEmit` 으로 타입체크 PASS 확인
- 변경 부위 외 diff 가 발생하지 않았는지 `git diff src/pages/CheckpointsPage.tsx` 로 확인 (들여쓰기/공백 변경 최소화)
  </action>
  <verify>
    <automated>cd /Users/jykevin/Documents/20260328/cha-bio-safety && npx tsc --noEmit 2>&1 | head -30</automated>
    수동 검증:
    - `grep -n "extinguisherApi" src/pages/CheckpointsPage.tsx` 가 import 1개 + create 호출 1개 노출 (총 2회 이상)
    - `grep -n "isExtCategory" src/pages/CheckpointsPage.tsx` 가 5회 이상 (state/mutation/canSave/JSX 분기 2개)
    - `grep -n "분말 20kg\\|분말 3.3kg\\|할로겐\\|K급" src/pages/CheckpointsPage.tsx` 4개 옵션 모두 노출
    - `grep -nE "^import" src/pages/CheckpointsPage.tsx | head -10` 으로 import 라인 정상
  </verify>
  <done>
    - tsc --noEmit 0 에러
    - extinguisherApi import + extinguisherApi.create 호출 모두 존재
    - 종류 4개 옵션(분말 20kg/분말 3.3kg/할로겐/K급) 모두 JSX 에 등장
    - 6개 선택 필드(manufacturer/manufactured_at/approval_no/prefix_code/seal_no/serial_no) 모두 JSX 에 등장
    - 비-소화기 카테고리 createMutation 흐름은 변경 전과 동일(checkPointApi.create 호출)
    - updateMutation/deactivateMutation/isMarker 분기 무수정
  </done>
</task>

<task type="auto">
  <name>Task 2: 프로덕션 빌드 + Cloudflare Pages 배포 (--branch production, ASCII commit)</name>
  <files>(빌드 산출물 dist/ — repo 미포함, 배포만 수행)</files>
  <action>
프로덕션 빌드 + Cloudflare Pages 배포를 수행한다. 운영 메모리 제약 준수:
1. **--branch production 필수** — 안 붙이면 Preview 로 감
2. **--commit-message 는 ASCII 만** — wrangler 가 한글 커밋 메시지 거부

### 명령어

```bash
cd /Users/jykevin/Documents/20260328/cha-bio-safety
npm run build
npx wrangler pages deploy dist --branch production --commit-message "checkpoints add modal: extinguisher fields and routing"
```

### 검증 절차

배포 성공 시 `https://*.cha-bio-safety.pages.dev` (또는 커스텀 도메인) URL 출력됨. 사용자가 PWA 재설치 후 검증한다.

**중요 (운영 메모리 — feedback_pwa_cache_invalidation):** 사용자에게 "PWA 캐시 때문에 즉시 반영 안 될 수 있음. 홈 화면 아이콘 삭제 후 재추가 필요" 안내.

### 빌드 실패 시

- TypeScript 에러: Task 1 으로 회귀하여 수정
- Vite 빌드 에러: 에러 메시지 확인 후 수정
- wrangler 배포 에러:
  - "한글" 관련 에러 → --commit-message 에 한글 포함됐는지 확인
  - "Preview" 관련 → --branch production 누락 확인

### 회귀 금지

- 빌드 산출물(dist/)은 repo 에 커밋하지 않음 (.gitignore 처리되어 있음)
- 소스 변경은 Task 1 의 src/pages/CheckpointsPage.tsx 만
  </action>
  <verify>
    <automated>cd /Users/jykevin/Documents/20260328/cha-bio-safety && npm run build 2>&1 | tail -20</automated>
    배포 검증:
    - `npx wrangler pages deploy dist --branch production ...` 출력에 `Deployment complete` 또는 `https://*.pages.dev` URL 노출
    - 출력에 "Preview" 단어가 없음 (production 으로 배포됐다는 의미)
  </verify>
  <done>
    - npm run build 성공 (dist/ 생성)
    - wrangler pages deploy 성공 (production 브랜치)
    - 사용자에게 배포된 URL + PWA 재설치 안내 전달
  </done>
</task>

</tasks>

<verification>
## 코드 변경 검증

```bash
cd /Users/jykevin/Documents/20260328/cha-bio-safety

# 1. 타입체크
npx tsc --noEmit

# 2. 변경 부위 grep 검증
grep -n "extinguisherApi" src/pages/CheckpointsPage.tsx     # import + create 호출 = 2건 이상
grep -n "isExtCategory"   src/pages/CheckpointsPage.tsx     # 5회 이상
grep -n "분말 20kg"       src/pages/CheckpointsPage.tsx     # 1회
grep -n "K급"             src/pages/CheckpointsPage.tsx     # 1회

# 3. 회귀 가드 — 다음은 변경되지 않아야 함
grep -n "updateMutation\\|deactivateMutation\\|isMarker"    src/pages/CheckpointsPage.tsx
git diff src/pages/CheckpointsPage.tsx                      # 변경량 ≈ 80~110 라인 추가 (+2 import 글자)
```

## 사용자 수동 검증 (PWA 재설치 후)

1. `/checkpoints` 페이지 진입 → 우상단 "+" → 모달 오픈
2. **카테고리 = 소화전 (대조군):**
   - 종류/제조업체/… 등 추가 필드 노출 X
   - 위치번호/설명 필드 노출 O
   - 저장 → "개소가 추가되었습니다" 토스트 (변경 전과 동일)
3. **카테고리 = 소화기 (실험군):**
   - 종류 셀렉트 노출 (4개 옵션: 분말 20kg / 분말 3.3kg / 할로겐 / K급)
   - 제조업체/제조년월/형식승인번호/접두문자/증지번호/제조번호 6개 필드 노출
   - 위치번호/설명 필드 숨김
   - 종류 미선택 시 "저장" 비활성화
   - zone(연/사/공), 층, 개소명, 종류 필수 4개 입력 후 저장
   - "소화기 등록 완료 (연-XX-NN)" 토스트 (mgmt_no 자동 노출)
   - `/extinguishers` 페이지에서 새 행 확인
   - `/checkpoints` 목록에서 새 개소 확인 (CP-FE-XXXX id)
4. **mode='edit' (회귀 가드):**
   - 기존 소화기 개소 클릭 → 편집 모달
   - 추가 필드 노출 X (변경 전과 동일)
   - 저장 → 정상 동작
</verification>

<success_criteria>
- TypeScript 타입체크 PASS
- 프로덕션 빌드 PASS
- Cloudflare Pages production 브랜치 배포 성공
- `/checkpoints` 개소 추가 모달에서 카테고리=소화기 선택 시:
  - 종류 셀렉트(4개 옵션) + 6개 선택 필드 노출
  - 위치번호/설명 필드 숨김
  - 저장 시 `extinguisherApi.create` 호출 → extinguishers + check_points 양쪽 행 생성
  - 토스트에 자동 발급된 mgmt_no 노출
- 카테고리=소화기 외 흐름은 변경 전과 100% 동일
- mode='edit' 흐름은 변경 전과 100% 동일
- FPM- 마커 분기는 변경 전과 100% 동일
</success_criteria>

<output>
완료 후 `.planning/quick/260426-u7f-checkpointspage-ext-fields/260426-u7f-SUMMARY.md` 생성:

- 변경된 파일: `src/pages/CheckpointsPage.tsx` (단일 파일, ~80~110 라인 추가)
- 추가 항목 요약: ExtState 타입, extForm state, isExtCategory, createMutation 분기, canSave 분기, 소화기 전용 JSX 7필드, 위치번호/설명 conditional 숨김, extinguisherApi import
- 배포 URL + 배포 시각
- 사용자 PWA 재설치 안내 (홈 화면 아이콘 삭제 → 재추가)
- 회귀 가드 통과 항목: updateMutation 무수정, deactivateMutation 무수정, FPM- 마커 분기 무수정, mode='edit' 무수정
- 잠재 후속 작업 (Deferred — 본 plan 에서 다루지 않음):
  - mode='edit' 에서도 소화기 전용 필드 편집 가능하게 (서버 update API 추가 필요)
  - 종류 옵션 동적화 (현재 4개 하드코딩)
</output>
