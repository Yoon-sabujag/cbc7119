---
phase: 260530-5b4
plan: 01
subsystem: ElevatorPage desktop header
tags: [redesign, header-unification, h3, elevator, tailwind-override]
key-files:
  modified:
    - src/pages/ElevatorPage.tsx
decisions:
  - "h-7 (32px) 선택 — tailwind.config spacing override h-7=32/h-8=48 함정 회피"
metrics:
  duration: "~5m"
  completed: "2026-05-30"
  tasks: 1
  files: 1
---

# Phase 260530-5b4 Plan 01: ElevatorPage 데스크톱 헤더 48→32 Summary

One-liner: ElevatorPage 데스크톱 헤더 안 미해결 배지(py-1 제거+h-7) + 고장 접수/수리 기록 버튼(h-8→h-7) 3곳 sketch v2 §케이스 1 AFTER 표준 32px 통일.

## Changes Made

정확히 3곳만 수정. 다른 영역 0 byte 변경.

### Change 1 — L605 미해결 배지 (py-1 제거 + h-7 추가)

**BEFORE:**
```tsx
<span className="inline-flex items-center gap-1 text-caption font-bold text-fire bg-fire-bg border border-fire-bar rounded-pill px-2.5 py-1">
```

**AFTER:**
```tsx
<span className="inline-flex items-center gap-1 h-7 text-caption font-bold text-fire bg-fire-bg border border-fire-bar rounded-pill px-2.5">
```

변경: `py-1` 제거 + `gap-1` 다음 `h-7` 삽입. 높이 ~26px(auto) → 32px 명시.

### Change 2 — L612 고장 접수 버튼 (h-8 → h-7)

**BEFORE:**
```tsx
className="inline-flex items-center gap-1.5 h-8 px-3.5 rounded-sm border-0 text-text-on-accent text-caption font-bold cursor-pointer"
```

**AFTER:**
```tsx
className="inline-flex items-center gap-1.5 h-7 px-3.5 rounded-sm border-0 text-text-on-accent text-caption font-bold cursor-pointer"
```

변경: `h-8`(48px) → `h-7`(32px). 나머지 className, style, onClick, icon 무수정.

### Change 3 — L620 수리 기록 버튼 (h-8 → h-7)

**BEFORE:**
```tsx
className="inline-flex items-center gap-1.5 h-8 px-3.5 rounded-sm border-0 text-text-on-accent text-caption font-bold cursor-pointer"
```

**AFTER:**
```tsx
className="inline-flex items-center gap-1.5 h-7 px-3.5 rounded-sm border-0 text-text-on-accent text-caption font-bold cursor-pointer"
```

변경: `h-8`(48px) → `h-7`(32px). 나머지 className, style, onClick, icon 무수정.

## Grep Verification

### BEFORE Greps

| # | Pattern | Expected | Result |
|---|---------|----------|--------|
| 1 | `inline-flex...rounded-pill px-2.5 py-1` | 1 match (L605) | 1 match (L605) PASS |
| 2 | `h-8 px-3.5 rounded-sm...cursor-pointer` | 2 matches (L612, L620) | 2 matches (L612, L620) PASS |
| 3 | `linear-gradient(135deg,#991b1b,#ef4444)` | 1 match in header | 1 match (L613) PASS |
| 4 | `linear-gradient(135deg,#854d0e,#eab308)` | 1 match | 1 match (L621) PASS |

### AFTER Greps

| # | Pattern | Expected | Result |
|---|---------|----------|--------|
| 1 | `h-7 text-caption...rounded-pill px-2.5"` | 1 match (L605) | 1 match (L605) PASS |
| 2 | `h-7 px-3.5 rounded-sm...cursor-pointer` | 2 matches (L612, L620) | 2 matches (L612, L620) PASS |
| 3 | py-1 잔존 (border-fire-bar 포함) | 0 (OK 출력) | OK: no residual py-1 on fire badge PASS |
| 4 | `h-8 px-3.5 rounded-sm` 잔존 count | 0 | 0 PASS |

## Gradient / onClick / Lucide 보존 검증

| 항목 | 위치 | 결과 |
|------|------|------|
| `linear-gradient(135deg,#991b1b,#ef4444)` | L613 보존 | PASS |
| `linear-gradient(135deg,#854d0e,#eab308)` | L621 보존 | PASS |
| `setModal('fault_new')` onClick | L611 보존 | PASS |
| `setModal('repair_new')` onClick | L619 보존 | PASS |
| `AlertTriangle size={12}` | L606 보존 | PASS |
| `AlertTriangle size={14}` | L615 보존 | PASS |
| `Wrench size={14}` | L623 보존 | PASS |

## Build

- `npm run build` PASS (15.53s)
- TypeScript 0 error
- 경고만 (chunk size) — 기존 프로젝트 정상 범위

## git diff

- 변경 라인 수: 3 deletions + 3 additions = 6 lines (헤더 파일 2줄 포함 wc=8, 실질 6)
- 삭제 라인: L605 py-1 배지 / L612 h-8 버튼 / L620 h-8 버튼 (정확히 3곳)
- 파일 삭제 없음 (post-commit deletion check PASS)

## Commit

```
8bf69c6  feat(redesign/32-header-unification-h3): 승강기 관리 데스크톱 헤더 48→32 (고장/수리/배지 sketch v2 §케이스 1)
1 file changed, 3 insertions(+), 3 deletions(-)
```

## 사용자 컨펌 대기

main 머지 + push 는 사용자 명시 컨펌 후 진행 (CLAUDE.local.md 룰 — redesign 트랙은 사용자 명시 후 main 머지+배포).

## 메모리 룰 앵커

- `feedback_tailwind_w8_h8_is_48px.md` — h-7=32px / h-8=48px (tailwind.config spacing override 함정). 32px 목표 = h-7 정확.
- `feedback_design_changes_ask_first.md` — 디자인 변경 컨펌 룰. push 전 사용자 컨펌 대기.

## Deviations from Plan

None — plan executed exactly as written.

## Self-Check: PASSED

- src/pages/ElevatorPage.tsx modified: FOUND
- Commit 8bf69c6: FOUND (git log confirmed)
- BEFORE/AFTER grep counts all match expected
- Build PASS, TypeScript 0 error
