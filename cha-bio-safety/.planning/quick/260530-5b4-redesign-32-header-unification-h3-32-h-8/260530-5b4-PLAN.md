---
phase: 260530-5b4
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/ElevatorPage.tsx
autonomous: true
requirements:
  - REDESIGN-32-H3
must_haves:
  truths:
    - "승강기 관리 데스크톱 헤더(L602 h-14)의 액션 버튼 2개와 미해결 배지 1개가 sketch v2 §케이스 1 AFTER 표준대로 세로 32px로 통일된다"
    - "AlertTriangle/Wrench Lucide 아이콘 size prop (12/14)이 정확히 유지된다"
    - "고장 접수 / 수리 기록 버튼의 gradient background (red 991b1b→ef4444 / amber 854d0e→eab308)가 verbatim 보존된다"
    - "두 버튼의 onClick (setSelectedEv + setModal('fault_new'|'repair_new')) 핸들러가 0 byte 변경된다"
    - "헤더 컨테이너 L602 (h-14 bg-surface-raised border-b border-border-default px-5 flex items-center gap-2.5)는 무수정"
    - "vite build PASS / TypeScript 0 error"
    - "ChevronLeft/AlertTriangle/Wrench 외 다른 영역은 한 byte 변경 0"
  artifacts:
    - path: "src/pages/ElevatorPage.tsx"
      provides: "데스크톱 헤더 영역 3 곳 (배지 + 고장 접수 + 수리 기록) 32px 통일"
      contains: "h-7 ... rounded-pill px-2.5"
  key_links:
    - from: "src/pages/ElevatorPage.tsx L605 미해결 배지"
      to: "sketch v2 §케이스 1 AFTER badge spec"
      via: "py-1 제거 + h-7 명시"
      pattern: "h-7.*text-caption.*text-fire.*bg-fire-bg.*border-fire-bar.*rounded-pill.*px-2.5"
    - from: "src/pages/ElevatorPage.tsx L612 고장 접수 버튼"
      to: "sketch v2 §케이스 1 AFTER action 버튼 spec"
      via: "h-8 → h-7 (codebase override h-8=48px / h-7=32px)"
      pattern: "h-7 px-3.5 rounded-sm.*background: 'linear-gradient\\(135deg,#991b1b,#ef4444\\)'"
    - from: "src/pages/ElevatorPage.tsx L620 수리 기록 버튼"
      to: "sketch v2 §케이스 1 AFTER action 버튼 spec"
      via: "h-8 → h-7"
      pattern: "h-7 px-3.5 rounded-sm.*background: 'linear-gradient\\(135deg,#854d0e,#eab308\\)'"
---

<objective>
승강기 관리 (ElevatorPage) 데스크톱 헤더 안 액션 버튼 2개 (고장 접수 / 수리 기록) + 미해결 배지 1개의 세로 높이를 sketch v2 §케이스 1 AFTER 표준 = 32px (h-7) 로 통일.

현재: L605 배지 = py-1 (h-auto, 시각 ~26px) / L612, L620 버튼 = h-8 (codebase override = 48px)
목표: 모두 h-7 (codebase override = 32px) 명시.

Purpose: 헤더 통일 트랙 Wave H3 — 데스크톱 헤더 안 가장 큰 시각 변화. sketch v2 §케이스 1 (48px 액션 → 32px) AFTER 표준 매칭. 이전 wave (H1 .back-btn 34→32 공유 CSS / H1.5 nav-btn 28→32 공유 CSS / H2 백버튼 44→32 직접 className) 와 일관된 32px 룰.

Output: src/pages/ElevatorPage.tsx 정확히 3 곳 className 수정 + atomic commit + 사용자 확인 후 push.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@./CLAUDE.local.md
@.planning/STATE.md

# Sketch (AFTER 표준)
@.planning/quick/260529-header-unification/sketch-v2.html

# Target file (정확 라인 600~626 데스크톱 헤더 영역)
@src/pages/ElevatorPage.tsx

<interfaces>
<!-- 현재 코드 (L600~626) — verbatim 인용. executor 는 이 BEFORE 를 grep 검증 후 AFTER 로 교체. -->

L600-L626 (확인 완료):
```tsx
    return (
      <div className="flex-1 min-h-0 flex flex-col overflow-hidden">
        {/* 헤더 — 데스크톱 표준 (height 54, padding '0 20px', title 14/700) */}
        <header className="flex-shrink-0 h-14 bg-surface-raised border-b border-border-default px-5 flex items-center gap-2.5">
          <span className="flex-1 text-body font-bold text-text-primary">승강기 관리</span>
          {unresolvedCount > 0 && (
            <span className="inline-flex items-center gap-1 text-caption font-bold text-fire bg-fire-bg border border-fire-bar rounded-pill px-2.5 py-1">
              <AlertTriangle size={12} />
              미해결 고장 {unresolvedCount}건
            </span>
          )}
          <button
            onClick={() => { setSelectedEv(selectedDesktopEv); setModal('fault_new') }}
            className="inline-flex items-center gap-1.5 h-8 px-3.5 rounded-sm border-0 text-text-on-accent text-caption font-bold cursor-pointer"
            style={{ background: 'linear-gradient(135deg,#991b1b,#ef4444)' }}
          >
            <AlertTriangle size={14} />
            고장 접수
          </button>
          <button
            onClick={() => { setSelectedEv(selectedDesktopEv); setModal('repair_new') }}
            className="inline-flex items-center gap-1.5 h-8 px-3.5 rounded-sm border-0 text-text-on-accent text-caption font-bold cursor-pointer"
            style={{ background: 'linear-gradient(135deg,#854d0e,#eab308)' }}
          >
            <Wrench size={14} />
            수리 기록
          </button>
        </header>
```

<!-- Tailwind override 함정 reference (메모리 박제 feedback_tailwind_w8_h8_is_48px.md) -->
- h-7 = 32px (config override; tailwind 기본 28 아님)
- h-8 = 48px (config override; tailwind 기본 32 아님)
- 따라서 32px 목표 = `h-7` 정확. h-8 그대로 두면 48px (현 상태).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ElevatorPage 데스크톱 헤더 3 곳 32px 통일</name>
  <files>src/pages/ElevatorPage.tsx</files>
  <action>
src/pages/ElevatorPage.tsx 의 데스크톱 헤더 영역 (L600 wrapper 안, L602 header 자식) 정확히 3 곳만 수정. 다른 곳 한 byte도 건드리지 말 것.

**Step 0 — BEFORE grep 검증 (실행 전 필수)**:
```bash
grep -n "inline-flex items-center gap-1 text-caption font-bold text-fire bg-fire-bg border border-fire-bar rounded-pill px-2.5 py-1" src/pages/ElevatorPage.tsx
grep -n "h-8 px-3.5 rounded-sm border-0 text-text-on-accent text-caption font-bold cursor-pointer" src/pages/ElevatorPage.tsx
grep -n "linear-gradient(135deg,#991b1b,#ef4444)" src/pages/ElevatorPage.tsx
grep -n "linear-gradient(135deg,#854d0e,#eab308)" src/pages/ElevatorPage.tsx
```
- 1번 grep: 정확 1 매치 (L605)
- 2번 grep: 정확 2 매치 (L612, L620) — 이 파일 안에서 이 className 조합은 L612/L620 두 곳에만 존재 검증
- 3번 grep: 1 매치 (L613 red gradient)
- 4번 grep: 1 매치 (L621 amber gradient)

매치 수가 다르면 즉시 중단하고 사용자에게 보고.

**Change 1 — L605 미해결 배지** (heading 안 inline-flex span):
BEFORE:
```
className="inline-flex items-center gap-1 text-caption font-bold text-fire bg-fire-bg border border-fire-bar rounded-pill px-2.5 py-1"
```
AFTER:
```
className="inline-flex items-center gap-1 h-7 text-caption font-bold text-fire bg-fire-bg border border-fire-bar rounded-pill px-2.5"
```
- 변경: `py-1` 삭제 + `gap-1` 다음 `h-7` 삽입 (`gap-1 h-7 text-caption` 순서)
- 보존: inline-flex / items-center / gap-1 / text-caption / font-bold / text-fire / bg-fire-bg / border / border-fire-bar / rounded-pill / px-2.5
- AlertTriangle size={12} 보존 / `미해결 고장 {unresolvedCount}건` 텍스트 보존 / `{unresolvedCount > 0 && (...)}` 조건 보존

**Change 2 — L612 고장 접수 버튼**:
- className 안 `h-8` 토큰 → `h-7` (정확 1 토큰 치환)
- 나머지 className (inline-flex items-center gap-1.5 px-3.5 rounded-sm border-0 text-text-on-accent text-caption font-bold cursor-pointer) 그대로
- style={{ background: 'linear-gradient(135deg,#991b1b,#ef4444)' }} verbatim 보존
- onClick 핸들러 verbatim 보존
- AlertTriangle size={14} 보존 / "고장 접수" 텍스트 보존

**Change 3 — L620 수리 기록 버튼**:
- className 안 `h-8` 토큰 → `h-7` (정확 1 토큰 치환)
- 나머지 className 그대로
- style={{ background: 'linear-gradient(135deg,#854d0e,#eab308)' }} verbatim 보존
- onClick 핸들러 verbatim 보존
- Wrench size={14} 보존 / "수리 기록" 텍스트 보존

**Negative gates (한 byte도 건드리지 말 것)**:
- L600 wrapper div className (flex-1 min-h-0 flex flex-col overflow-hidden) 무수정
- L601 주석 verbatim 보존
- L602 header className (flex-shrink-0 h-14 bg-surface-raised border-b border-border-default px-5 flex items-center gap-2.5) 무수정 — 헤더 컨테이너 h-14 (54px) 는 이미 데스크톱 표준
- L603 title span className (flex-1 text-body font-bold text-text-primary) + "승강기 관리" 텍스트 무수정
- ChevronLeft / 다른 헤더 (모바일/태블릿 분기) / 그 외 5800+ 줄 무수정
- 다른 h-8 발생 위치 (있다면) 무수정 — 이 task 는 L612/L620 두 곳만

**Step 4 — AFTER grep 검증 (수정 후 필수)**:
```bash
grep -n "h-7 text-caption font-bold text-fire bg-fire-bg border border-fire-bar rounded-pill px-2.5\"" src/pages/ElevatorPage.tsx
grep -n "h-7 px-3.5 rounded-sm border-0 text-text-on-accent text-caption font-bold cursor-pointer" src/pages/ElevatorPage.tsx
grep -n "py-1\"" src/pages/ElevatorPage.tsx | grep -i "border-fire-bar" || echo "OK: no residual py-1 on fire badge"
grep -c "h-8 px-3.5 rounded-sm" src/pages/ElevatorPage.tsx
```
- 1번 grep: 정확 1 매치 (L605 변환됨)
- 2번 grep: 정확 2 매치 (L612, L620 변환됨)
- 3번 grep: "OK: no residual py-1 on fire badge" 출력 (배지에 py-1 잔존 0)
- 4번 grep: 결과 = 0 (h-8 px-3.5 rounded-sm 조합 잔존 0)

**Step 5 — gradient + onClick + Lucide 무수정 검증**:
```bash
grep -n "linear-gradient(135deg,#991b1b,#ef4444)" src/pages/ElevatorPage.tsx
grep -n "linear-gradient(135deg,#854d0e,#eab308)" src/pages/ElevatorPage.tsx
grep -n "setModal('fault_new')" src/pages/ElevatorPage.tsx
grep -n "setModal('repair_new')" src/pages/ElevatorPage.tsx
grep -n "AlertTriangle size={12}" src/pages/ElevatorPage.tsx
grep -n "AlertTriangle size={14}" src/pages/ElevatorPage.tsx
grep -n "Wrench size={14}" src/pages/ElevatorPage.tsx
```
모두 1+ 매치 (이전과 동일 라인) — gradient/onClick/Lucide 한 byte 변경 0 검증.

**Step 6 — build 검증**:
```bash
npm run build
```
PASS 필수. TypeScript 0 error.

**Step 7 — atomic commit**:
```bash
git add src/pages/ElevatorPage.tsx
git commit -m "feat(redesign/32-header-unification-h3): 승강기 관리 데스크톱 헤더 48→32 (고장/수리/배지 sketch v2 §케이스 1)"
```
push 는 사용자 컨펌 후 (CLAUDE.local.md 룰 — redesign 트랙은 main 머지+배포 = 사용자 명시 컨펌).
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -20 && grep -c "h-8 px-3.5 rounded-sm" src/pages/ElevatorPage.tsx</automated>
  </verify>
  <done>
- BEFORE grep 4 종 매치 수 정확 (1/2/1/1)
- AFTER grep 4 종 매치 수 정확 (1/2/OK/0)
- gradient + onClick + Lucide 보존 grep 7 종 모두 매치
- npm run build PASS (TypeScript 0 error)
- L600/L601/L602/L603 header 컨테이너 무수정 검증 (git diff 라인 수 ≤ 3, 또는 변경 라인이 L605/L612/L620 만 포함)
- atomic commit 1 개 생성 (제목: "feat(redesign/32-header-unification-h3): 승강기 관리 데스크톱 헤더 48→32 (고장/수리/배지 sketch v2 §케이스 1)")
  </done>
</task>

</tasks>

<verification>
**Phase 종합 검증**:

1. **시각 회귀 — 헤더 컨테이너 무수정**:
```bash
git diff src/pages/ElevatorPage.tsx | grep -E "^[+-]" | grep -v "^[+-]{3}" | wc -l
```
변경 라인 수 ≤ 6 (3 곳 × 각 1~2 라인). 4 이상이면 의도치 않은 변경 의심.

2. **3 곳 정확 매치**:
```bash
git diff src/pages/ElevatorPage.tsx | grep -E "^-" | grep -v "^---" | head -10
```
삭제 라인 3 개 = (1) py-1 포함 배지 className, (2) L612 h-8 className, (3) L620 h-8 className 만.

3. **build PASS + 0 error**:
```bash
npm run build 2>&1 | tail -5
```
"✓ built in ..." 출력 + warning 만 있을 수 있음 / TypeScript error 0.

4. **gradient + onClick + Lucide 보존**:
```bash
grep -c "linear-gradient(135deg,#991b1b,#ef4444)\|linear-gradient(135deg,#854d0e,#eab308)\|setModal('fault_new')\|setModal('repair_new')\|AlertTriangle size={12}\|AlertTriangle size={14}\|Wrench size={14}" src/pages/ElevatorPage.tsx
```
≥ 7 (각 토큰 최소 1 매치).
</verification>

<success_criteria>
- 정확 3 곳 변경 (L605 배지 + L612 고장 접수 + L620 수리 기록)
- L605: py-1 제거 + h-7 추가 (높이 32px 명시)
- L612, L620: h-8 → h-7 (48px → 32px)
- gradient (red 991b1b→ef4444, amber 854d0e→eab308) verbatim 보존
- onClick (setSelectedEv + setModal('fault_new'|'repair_new')) verbatim 보존
- AlertTriangle (size=12, 14) + Wrench (size=14) Lucide 아이콘 verbatim 보존
- 헤더 컨테이너 L602 (h-14) + L603 title + L600 wrapper 무수정
- 다른 영역 (모바일 헤더, 본문, 모달, 5800+ 줄 나머지) 한 byte 변경 0
- npm run build PASS
- TypeScript 0 error
- atomic commit 1 개 (제목 verbatim)
- wrangler / npm run deploy 실행 0 (CLAUDE.local.md 룰)
</success_criteria>

<output>
After completion, create `.planning/quick/260530-5b4-redesign-32-header-unification-h3-32-h-8/260530-5b4-SUMMARY.md` (워크트리 cwd `cha-bio-safety/` 상대).

SUMMARY.md 에 포함:
- 변경 3 곳 BEFORE/AFTER 인용 (L605/L612/L620)
- BEFORE grep 매치 수 / AFTER grep 매치 수 표
- gradient/onClick/Lucide 보존 검증 결과
- build 통과 / TypeScript 0 error 확인
- git diff 라인 수 (예상 ≤ 6)
- atomic commit hash + 메시지
- "사용자 컨펌 대기 — main 머지+push 는 사용자 명시 후" 마커
- 메모리 룰 anchor: feedback_tailwind_w8_h8_is_48px.md (h-7=32 / h-8=48 함정 회피), feedback_design_changes_ask_first.md (디자인 변경 컨펌)
</output>
