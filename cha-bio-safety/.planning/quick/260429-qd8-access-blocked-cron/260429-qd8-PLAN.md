---
phase: quick-260429-qd8
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ../cbc-cron-worker/wrangler.toml
  - ../cbc-cron-worker/src/index.ts
autonomous: true
requirements:
  - QD8-01  # 매일 KST 15:00 cron 추가 (UTC 06:00)
  - QD8-02  # 그 달 마지막 점검일에만 발동
  - QD8-03  # 카테고리별 접근불가 cp 자동 normal 기록 (이번 달 미기록 한정)
  - QD8-04  # staff_id = 점검일 당시 소방안전관리자 (assignee_id)
  - QD8-05  # 카테고리당 1 inspection_session + 다수 check_records (D1 batch atomic)
  - QD8-06  # 클라이언트 자동완료(InspectionPage)와 memo 문자열 일관

must_haves:
  truths:
    - "wrangler.toml triggers.crons 배열에 '0 6 * * *' 가 추가되었고, 기존 두 cron('45 23 * * *', '*/5 * * * *')은 보존됨"
    - "src/index.ts 의 scheduled 핸들러 switch 에 '0 6 * * *' 분기가 존재하고 handleAccessBlockedAutoComplete 를 ctx.waitUntil 로 호출함"
    - "handleAccessBlockedAutoComplete 가 KST 기준 오늘 날짜를 계산하고, '오늘이 그 달 마지막 점검일인 카테고리'만 처리함"
    - "각 대상 카테고리에서 description LIKE '%접근불가%' AND is_active=1 이고 이번 달 check_records 가 없는 cp 만 자동완료 대상으로 잡힘"
    - "카테고리당 inspection_sessions 1행 + cp 별 check_records N행이 D1 batch 로 atomic 하게 insert 됨"
    - "삽입된 check_records 의 result='normal', memo='접근불가 개소 자동 정상처리' (InspectionPage line 4057/4085 와 동일 문자열)"
    - "삽입된 inspection_sessions 의 staff_id = 그 카테고리 마지막 점검일의 schedule_items.assignee_id 이며, assignee_id 가 NULL 이면 해당 카테고리는 skip + 경고 log"
    - "cbc-cron-worker 가 typecheck (npx tsc --noEmit) 를 통과함"
  artifacts:
    - path: "../cbc-cron-worker/wrangler.toml"
      provides: "신규 cron schedule '0 6 * * *' 등록"
      contains: '"0 6 * * *"'
    - path: "../cbc-cron-worker/src/index.ts"
      provides: "handleAccessBlockedAutoComplete 핸들러 + scheduled switch 분기"
      contains: "handleAccessBlockedAutoComplete"
  key_links:
    - from: "../cbc-cron-worker/src/index.ts (scheduled handler)"
      to: "handleAccessBlockedAutoComplete"
      via: "switch (controller.cron) case '0 6 * * *' → ctx.waitUntil(handleAccessBlockedAutoComplete(env))"
      pattern: "case '0 6 \\* \\* \\*'"
    - from: "handleAccessBlockedAutoComplete"
      to: "schedule_items.inspection_category + assignee_id"
      via: "WITH cat_last AS (...) JOIN schedule_items ON inspection_category"
      pattern: "inspection_category"
    - from: "handleAccessBlockedAutoComplete"
      to: "check_records / inspection_sessions"
      via: "env.DB.batch([insertSession, ...insertRecords])"
      pattern: "env\\.DB\\.batch"
---

<objective>
cbc-cron-worker 에 매일 KST 15:00 (UTC 06:00) 발동하는 신규 cron 을 추가해서, **그 달의 마지막 점검일** 에 한해 카테고리별 접근불가 개소(check_points.description LIKE '%접근불가%') 중 해당 월 점검 기록이 없는 cp 들을 자동으로 result='normal' / memo='접근불가 개소 자동 정상처리' 로 D1 batch insert 한다.

Purpose:
- 클라이언트(InspectionPage)에서 카테고리 진입 시점에만 동작하던 자동완료 로직이, 사용자가 그 달 마지막 점검일에 해당 카테고리를 한번도 진입하지 않으면 누락되는 문제를 cron 으로 보완.
- 당직자 / 보조자가 어떤 단말로 들어오든 무관하게 서버 시각 기준 일관된 자동 정상처리를 보장.

Output:
- ../cbc-cron-worker/wrangler.toml (cron schedule 1개 추가)
- ../cbc-cron-worker/src/index.ts (신규 handler + scheduled 분기)
- 단일 atomic 커밋 (cbc-cron-worker 두 파일만)
- 배포는 사용자 별도 지시까지 보류

Out of scope (별도 task):
- 클라이언트 폴백 (Q7)
- 도면 페이지 접근불가 팝업 (Q8)
- 기존 클라이언트 자동완료(InspectionPage line 4057~4083)는 손대지 않음
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md

# 작업 대상 파일 — 형제 디렉토리 cbc-cron-worker
@../cbc-cron-worker/wrangler.toml
@../cbc-cron-worker/src/index.ts
@../cbc-cron-worker/package.json

# 일관성 참조 (수정 X — memo 문자열만 동일하게)
@src/pages/InspectionPage.tsx

<interfaces>
<!-- 핵심 사실: 추가 의존성 / 추가 import 필요 없음. -->
<!-- crypto.randomUUID() 는 Workers 표준 API. cbc-cron-worker 는 nanoid 미사용 (package.json 확인 완료). -->
<!-- @block65/webcrypto-web-push 는 기존 두 cron 이 사용. 신규 cron 은 push 발송 없으므로 미사용. -->

cbc-cron-worker/src/index.ts (line 1~7) Env 타입 — 그대로 사용:
```typescript
export interface Env {
  DB: D1Database
  VAPID_PUBLIC_KEY: string
  VAPID_PRIVATE_KEY: string
}
```

cbc-cron-worker/src/index.ts (line 340~351) 기존 scheduled handler — 분기 추가만:
```typescript
export default {
  async scheduled(controller: ScheduledController, env: Env, ctx: ExecutionContext) {
    switch (controller.cron) {
      case '45 23 * * *':
        ctx.waitUntil(handleDailyNotifications(env))
        break
      case '*/5 * * * *':
        ctx.waitUntil(handleEventNotifications(env))
        break
    }
  },
}
```

D1 스키마 (메인 앱 migrations 기반 — cbc-cron-worker 는 동일 D1 'cha-bio-db' 를 binding 'DB' 로 사용):

inspection_sessions (migrations/0001_init.sql line 32~40):
- id          TEXT PRIMARY KEY
- date        TEXT NOT NULL  (YYYY-MM-DD)
- staff_id    TEXT NOT NULL REFERENCES staff(id)
- floor       TEXT NULL
- zone        TEXT NULL
- completed_at TEXT NULL
- created_at  TEXT NOT NULL DEFAULT datetime('now')

check_records (migrations/0001_init.sql line 43~53 + 0012 status 추가):
- id            TEXT PRIMARY KEY
- session_id    TEXT NOT NULL REFERENCES inspection_sessions(id)
- checkpoint_id TEXT NOT NULL REFERENCES check_points(id)
- staff_id      TEXT NOT NULL REFERENCES staff(id)
- result        TEXT NOT NULL CHECK(result IN ('normal','caution','bad','unresolved','missing'))
- memo          TEXT NULL
- photo_key     TEXT NULL
- checked_at    TEXT NOT NULL DEFAULT datetime('now')
- created_at    TEXT NOT NULL DEFAULT datetime('now')
- status        TEXT NOT NULL DEFAULT 'open'  (0012 추가)
  → INSERT 시 'open' 명시 또는 생략(DEFAULT) 모두 가능. 본 plan 은 명시함 (task brief 일관).

check_points (migrations/0001_init.sql line 19~28):
- id, category(NOT NULL), description(NULL), is_active(DEFAULT 1)
- description 패턴 두 가지 공존:
  - 완강기: '접근불가' (정확히)  — migrations/0022
  - 소화기: '분말 [접근불가]' / '할로겐 [접근불가]' — migrations/0075
  - 유도등: '... [접근불가]'                       — migrations/0025
  → LIKE '%접근불가%' 가 양쪽 다 매칭 (안전).

schedule_items (migrations/0001_init.sql + 0029 inspection_category 추가):
- id, title, date, time, assignee_id (NOT NULL REFERENCES staff(id)), category, status, ...
- inspection_category TEXT NULL  (0029 추가) — 점검 일정에 한해 채워짐 (예: '소화기', '유도등', '완강기', '소방용전원공급반' 등)

InspectionPage memo 문자열 (src/pages/InspectionPage.tsx line 4057, 4085):
```typescript
memo: '접근불가 개소 자동 정상처리'
```
→ cron 도 정확히 동일 문자열 사용 (대소문자 / 띄어쓰기 / '개소' / 따옴표 일치).

cbc-cron-worker package.json 의존성 (확인 완료):
- @block65/webcrypto-web-push  → 본 task 미사용
- nanoid 없음                   → crypto.randomUUID() 사용
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: wrangler.toml 에 신규 cron schedule '0 6 * * *' 추가</name>
  <files>../cbc-cron-worker/wrangler.toml</files>
  <action>
../cbc-cron-worker/wrangler.toml 의 [triggers] 섹션 crons 배열에 '0 6 * * *' 를 추가한다.

기존:
```
crons = ["45 23 * * *", "*/5 * * * *"]
```

수정 후:
```
crons = ["45 23 * * *", "*/5 * * * *", "0 6 * * *"]
```

규칙:
- 기존 두 cron 문자열은 한 글자도 변경하지 말 것 (큰따옴표/공백 포함).
- 다른 라인(name, compatibility_date, compatibility_flags, main, [[d1_databases]])은 절대 건드리지 말 것.
- 줄바꿈/포맷은 기존 스타일 유지 (한 줄 배열).
  </action>
  <verify>
    <automated>grep -c '"0 6 \* \* \*"' /Users/jykevin/Documents/20260328/cbc-cron-worker/wrangler.toml</automated>
    <automated>grep -c '"45 23 \* \* \*"' /Users/jykevin/Documents/20260328/cbc-cron-worker/wrangler.toml</automated>
    <automated>grep -c '"\*/5 \* \* \* \*"' /Users/jykevin/Documents/20260328/cbc-cron-worker/wrangler.toml</automated>
  </verify>
  <done>
- wrangler.toml 에 cron 3개("45 23 * * *", "*/5 * * * *", "0 6 * * *") 모두 존재
- 기존 d1_databases binding 변경 없음
- 위 grep 3개 모두 1 반환
  </done>
</task>

<task type="auto">
  <name>Task 2: handleAccessBlockedAutoComplete 핸들러 추가 + scheduled 분기 등록</name>
  <files>../cbc-cron-worker/src/index.ts</files>
  <action>
../cbc-cron-worker/src/index.ts 에 신규 함수 `handleAccessBlockedAutoComplete(env: Env): Promise<void>` 를 추가하고, 파일 하단의 export default scheduled 핸들러 switch 에 '0 6 * * *' 분기를 추가한다.

### 1) 신규 함수 위치
`handleEventNotifications` (line 282~337) 직후, `// ── Main export ──` 코멘트 (line 339) 직전에 삽입.
즉, 새 섹션 헤더와 함수를 한 블록으로 추가:

```typescript
// ── Access-blocked auto-complete (every day 15:00 KST = 06:00 UTC) ─────
async function handleAccessBlockedAutoComplete(env: Env): Promise<void> {
  // KST 오늘 날짜 (UTC+9). UTC 06:00 발동 → KST 15:00 (같은 날).
  const now = new Date()
  const kstMs = now.getTime() + 9 * 3600_000
  const kst = new Date(kstMs)
  const today = `${kst.getUTCFullYear()}-${String(kst.getUTCMonth() + 1).padStart(2, '0')}-${String(kst.getUTCDate()).padStart(2, '0')}`
  const yyyymm = today.slice(0, 7)

  // 1) 이번 달 카테고리별 마지막 점검일 + 그 날의 assignee_id 조회
  //    (같은 카테고리/같은 날에 schedule_items 행이 여러 건이어도 MIN(assignee_id) 로 안정적 1행 보장)
  //    cl.last_date = today 인 카테고리만 추림 → 오늘이 마지막 점검일이 아니면 빈 결과.
  const catRows = await env.DB.prepare(
    `WITH cat_last AS (
       SELECT inspection_category, MAX(date) AS last_date
       FROM schedule_items
       WHERE inspection_category IS NOT NULL
         AND substr(date, 1, 7) = ?
       GROUP BY inspection_category
     )
     SELECT cl.inspection_category AS category,
            MIN(si.assignee_id)    AS assignee_id
     FROM cat_last cl
     JOIN schedule_items si
       ON si.inspection_category = cl.inspection_category
      AND si.date = cl.last_date
     WHERE cl.last_date = ?
     GROUP BY cl.inspection_category`
  ).bind(yyyymm, today).all<{ category: string; assignee_id: string | null }>()

  const targets = (catRows.results ?? []).filter(r => !!r.category)
  if (targets.length === 0) return

  // 2) 카테고리별 처리
  for (const row of targets) {
    const category = row.category
    const assigneeId = row.assignee_id
    if (!assigneeId) {
      console.warn(`[access-blocked-auto] ${category}: assignee_id NULL — skip`)
      continue
    }

    // 2a) 자동완료 대상 cp: description 에 '접근불가' 포함 + active + 이번 달 미기록
    const cpRows = await env.DB.prepare(
      `SELECT id FROM check_points
       WHERE category = ?
         AND description LIKE '%접근불가%'
         AND is_active = 1
         AND id NOT IN (
           SELECT checkpoint_id FROM check_records
           WHERE substr(checked_at, 1, 7) = ?
         )`
    ).bind(category, yyyymm).all<{ id: string }>()

    const cpIds = (cpRows.results ?? []).map(r => r.id)
    if (cpIds.length === 0) {
      console.log(`[access-blocked-auto] ${category}: 0 cps (already complete)`)
      continue
    }

    // 2b) 카테고리당 inspection_session 1건 + check_records N건 atomic insert
    const sessionId = crypto.randomUUID()

    const sessionStmt = env.DB.prepare(
      `INSERT INTO inspection_sessions (id, date, floor, zone, staff_id, created_at)
       VALUES (?, ?, NULL, NULL, ?, datetime('now'))`
    ).bind(sessionId, today, assigneeId)

    const recordStmts = cpIds.map(cpId =>
      env.DB.prepare(
        `INSERT INTO check_records (id, session_id, checkpoint_id, staff_id, result, memo, checked_at, created_at, status)
         VALUES (?, ?, ?, ?, 'normal', '접근불가 개소 자동 정상처리', datetime('now'), datetime('now'), 'open')`
      ).bind(crypto.randomUUID(), sessionId, cpId, assigneeId)
    )

    try {
      await env.DB.batch([sessionStmt, ...recordStmts])
      console.log(`[access-blocked-auto] ${category}: ${cpIds.length} cps auto-completed (assignee=${assigneeId}, session=${sessionId})`)
    } catch (e) {
      console.error(`[access-blocked-auto] ${category}: batch failed`, e)
      // 카테고리 단위로만 fail — 다음 카테고리는 계속 진행
    }
  }
}
```

### 2) scheduled handler 분기 추가
파일 하단 (현재 line 340~351):

기존:
```typescript
export default {
  async scheduled(controller: ScheduledController, env: Env, ctx: ExecutionContext) {
    switch (controller.cron) {
      case '45 23 * * *':
        ctx.waitUntil(handleDailyNotifications(env))
        break
      case '*/5 * * * *':
        ctx.waitUntil(handleEventNotifications(env))
        break
    }
  },
}
```

수정 후 (case 추가):
```typescript
export default {
  async scheduled(controller: ScheduledController, env: Env, ctx: ExecutionContext) {
    switch (controller.cron) {
      case '45 23 * * *':
        ctx.waitUntil(handleDailyNotifications(env))
        break
      case '*/5 * * * *':
        ctx.waitUntil(handleEventNotifications(env))
        break
      case '0 6 * * *':
        ctx.waitUntil(handleAccessBlockedAutoComplete(env))
        break
    }
  },
}
```

### 3) 절대 변경 금지 항목
- handleDailyNotifications 함수 본문/시그니처
- handleEventNotifications 함수 본문/시그니처
- sendPush, getShiftRaw, getWorkingStaffIds, isWeekend
- SHIFT_REF / CYCLE 상수
- import 문 (신규 import 불필요 — crypto.randomUUID 는 Workers 전역, D1Database/ScheduledController/ExecutionContext 는 @cloudflare/workers-types 가 글로벌 제공)
- Env 인터페이스

### 4) 주의 사항
- 추가 import 절대 금지 (특히 nanoid — package.json 미설치).
- crypto.randomUUID() 는 Workers Runtime 표준 API (compatibility_date 2024-09-23 OK).
- KST 변환 로직은 기존 handleDailyNotifications (line 105) 와 동일 패턴 (+9*3600*1000) 이지만, 본 핸들러는 ISO 가 아니라 getUTCFullYear/Month/Date 직접 조합 (task brief 명시).
- D1 batch 는 카테고리당 1회. 카테고리 간은 for-of 순차 (atomic 보장은 카테고리 단위).
- assignee_id NULL → skip + console.warn (해당 카테고리 자동완료 누락은 운영자 로그로 추적).
- batch 예외 발생 시 그 카테고리만 실패하고 다음 카테고리 계속 (try/catch 로 isolate).
- TypeScript strict 미사용이지만 명시 타입(Promise<void>, generic .all<{}>) 유지.

### 5) 컴파일 확인
명령:
```
cd /Users/jykevin/Documents/20260328/cbc-cron-worker && npx tsc --noEmit
```
0 errors 여야 함.
  </action>
  <verify>
    <automated>cd /Users/jykevin/Documents/20260328/cbc-cron-worker && npx tsc --noEmit</automated>
    <automated>grep -c "handleAccessBlockedAutoComplete" /Users/jykevin/Documents/20260328/cbc-cron-worker/src/index.ts</automated>
    <automated>grep -c "접근불가 개소 자동 정상처리" /Users/jykevin/Documents/20260328/cbc-cron-worker/src/index.ts</automated>
    <automated>grep -c "case '0 6 \* \* \*'" /Users/jykevin/Documents/20260328/cbc-cron-worker/src/index.ts</automated>
    <automated>grep -c "handleDailyNotifications" /Users/jykevin/Documents/20260328/cbc-cron-worker/src/index.ts</automated>
    <automated>grep -c "handleEventNotifications" /Users/jykevin/Documents/20260328/cbc-cron-worker/src/index.ts</automated>
    <automated>grep -c "env.DB.batch" /Users/jykevin/Documents/20260328/cbc-cron-worker/src/index.ts</automated>
    <automated>grep -c "crypto.randomUUID" /Users/jykevin/Documents/20260328/cbc-cron-worker/src/index.ts</automated>
    <automated>grep -c "nanoid" /Users/jykevin/Documents/20260328/cbc-cron-worker/src/index.ts</automated>
  </verify>
  <done>
- npx tsc --noEmit 통과 (0 errors)
- grep "handleAccessBlockedAutoComplete" >= 2 (정의 1 + 호출 1)
- grep "접근불가 개소 자동 정상처리" >= 1 (memo 문자열, InspectionPage 와 일치)
- grep "case '0 6 * * *'" == 1
- grep "handleDailyNotifications" >= 2 (정의 1 + 호출 1) — 기존 보존
- grep "handleEventNotifications" >= 2 (정의 1 + 호출 1) — 기존 보존
- grep "env.DB.batch" >= 1
- grep "crypto.randomUUID" >= 2 (session id + record ids)
- grep "nanoid" == 0 (의존성 추가 없음 확인)
  </done>
</task>

<task type="auto">
  <name>Task 3: 통합 검증 — wrangler.toml + index.ts 매크로 grep 일괄 확인</name>
  <files></files>
  <action>
두 파일이 함께 일관된 상태인지 최종 확인. 단순 grep + 줄 수 점검.

확인 항목:
1) wrangler.toml 한 줄짜리 crons 배열 안에 cron 3개 모두 존재 (Task 1 결과 재확인)
2) index.ts 의 scheduled switch 안에 case 3개 모두 존재
3) cbc-cron-worker package.json 에 새 의존성 없음 (이번 task 가 다른 파일 안 건드렸는지 sanity)
4) 메인 앱 src/pages/InspectionPage.tsx 미수정 (Q7 별도 task 라는 task_brief 제약)
  </action>
  <verify>
    <automated>cd /Users/jykevin/Documents/20260328/cbc-cron-worker && git status --porcelain | sort</automated>
    <automated>cd /Users/jykevin/Documents/20260328/cha-bio-safety && git status --porcelain | grep -c "InspectionPage.tsx"</automated>
    <automated>grep -E "^[[:space:]]*case '" /Users/jykevin/Documents/20260328/cbc-cron-worker/src/index.ts | wc -l</automated>
    <automated>grep -c '"0 6 \* \* \*"\|"45 23 \* \* \*"\|"\*/5 \* \* \* \*"' /Users/jykevin/Documents/20260328/cbc-cron-worker/wrangler.toml</automated>
  </verify>
  <done>
- cbc-cron-worker git status: 정확히 두 파일만 수정 (wrangler.toml, src/index.ts).
  - 출력에 다른 파일 (package.json, package-lock.json, tsconfig 등) 변경 없음
- cha-bio-safety 의 InspectionPage.tsx 미수정 (grep 결과 == 0)
- index.ts 의 case 라인 수 >= 3 (45 23 / */5 / 0 6)
- wrangler.toml 한 줄에 cron 3개 모두 매칭
  </done>
</task>

</tasks>

<verification>
## 자동 검증 (executor 가 모든 task 완료 후 종합)

```bash
# 1) cron 3개 등록
grep -c '"0 6 \* \* \*"' /Users/jykevin/Documents/20260328/cbc-cron-worker/wrangler.toml      # 1
grep -c '"45 23 \* \* \*"' /Users/jykevin/Documents/20260328/cbc-cron-worker/wrangler.toml    # 1
grep -c '"\*/5 \* \* \* \*"' /Users/jykevin/Documents/20260328/cbc-cron-worker/wrangler.toml  # 1

# 2) 핸들러 + 분기
grep -c "handleAccessBlockedAutoComplete" /Users/jykevin/Documents/20260328/cbc-cron-worker/src/index.ts  # >=2
grep -c "case '0 6 \* \* \*'"              /Users/jykevin/Documents/20260328/cbc-cron-worker/src/index.ts  # 1
grep -c "접근불가 개소 자동 정상처리"        /Users/jykevin/Documents/20260328/cbc-cron-worker/src/index.ts  # >=1

# 3) 기존 cron 보존
grep -c "handleDailyNotifications" /Users/jykevin/Documents/20260328/cbc-cron-worker/src/index.ts  # >=2
grep -c "handleEventNotifications" /Users/jykevin/Documents/20260328/cbc-cron-worker/src/index.ts  # >=2

# 4) 의존성 추가 없음
grep -c "nanoid" /Users/jykevin/Documents/20260328/cbc-cron-worker/src/index.ts                    # 0
diff <(git show HEAD:cbc-cron-worker/package.json 2>/dev/null) /Users/jykevin/Documents/20260328/cbc-cron-worker/package.json  # 변화 없음 (또는 git status 로 확인)

# 5) D1 batch 사용
grep -c "env.DB.batch" /Users/jykevin/Documents/20260328/cbc-cron-worker/src/index.ts              # >=1

# 6) crypto.randomUUID 사용
grep -c "crypto.randomUUID" /Users/jykevin/Documents/20260328/cbc-cron-worker/src/index.ts        # >=2

# 7) typecheck
cd /Users/jykevin/Documents/20260328/cbc-cron-worker && npx tsc --noEmit                          # 0 errors

# 8) 메인 앱 무수정
cd /Users/jykevin/Documents/20260328/cha-bio-safety && git status --porcelain | grep -c "InspectionPage.tsx"  # 0
```

## 수동 검증 (배포 후 — 본 plan 범위 밖. 사용자 별도 지시 시.)
- wrangler tail 로 매일 06:00 UTC 발동 시 로그 확인:
  - `[access-blocked-auto] {category}: {N} cps auto-completed (assignee=..., session=...)`
  - 또는 `[access-blocked-auto] {category}: 0 cps (already complete)`
  - 또는 `[access-blocked-auto] {category}: assignee_id NULL — skip`
- D1 콘솔: 마지막 점검일 다음 04:00 KST 시점에 inspection_sessions / check_records 신규 행 존재
</verification>

<success_criteria>
- [ ] wrangler.toml triggers.crons 에 '0 6 * * *' 추가, 기존 둘 보존
- [ ] src/index.ts 에 handleAccessBlockedAutoComplete 함수 정의 (KST 변환 + cat_last CTE + cp 필터 + D1 batch)
- [ ] scheduled handler switch 에 '0 6 * * *' 분기 + ctx.waitUntil 호출
- [ ] check_records 삽입 시 result='normal', memo='접근불가 개소 자동 정상처리' (InspectionPage 일관)
- [ ] inspection_sessions.staff_id = schedule_items.assignee_id (점검일 당시 소방안전관리자)
- [ ] D1 batch 카테고리당 atomic, 카테고리 간 try/catch isolate
- [ ] assignee_id NULL → console.warn + skip
- [ ] 기존 두 cron 핸들러 (handleDailyNotifications, handleEventNotifications) 한 글자도 변경 없음
- [ ] 신규 import / 신규 의존성 (nanoid 등) 추가 없음 (crypto.randomUUID 사용)
- [ ] npx tsc --noEmit 통과
- [ ] cbc-cron-worker 두 파일만 수정 (package.json 등 안 건드림)
- [ ] cha-bio-safety/src/pages/InspectionPage.tsx 무수정 (Q7 별도 task)
- [ ] 단일 atomic 커밋 `feat(quick-260429-qd8): 접근불가 개소 자동완료 cron 추가 (cbc-cron-worker)`
- [ ] ROADMAP.md 변경 없음, 문서 커밋 없음, 원격 배포 보류
</success_criteria>

<output>
완료 후 SUMMARY 작성: `.planning/quick/260429-qd8-access-blocked-cron/260429-qd8-SUMMARY.md`

SUMMARY 에 포함:
- 추가된 cron schedule (0 6 * * * UTC = 15:00 KST)
- 동작 로직 요약 (그 달 마지막 점검일 한정 → 카테고리별 접근불가 cp 자동 normal)
- 결정 사항 (assignee_id = 점검일 당시 소방안전관리자, crypto.randomUUID, D1 batch atomic)
- Out of scope 명시 (Q7 클라이언트 폴백, Q8 도면 페이지 팝업)
- 운영 메모: 배포는 사용자 별도 지시까지 보류 / 배포 시 `npx wrangler deploy --branch production` 주의
</output>
