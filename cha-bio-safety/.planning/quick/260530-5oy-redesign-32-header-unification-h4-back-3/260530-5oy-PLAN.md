---
phase: 260530-5oy-redesign-32-header-unification-h4-back-3
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/WorkShiftPage.tsx
  - src/pages/AnnualPlanPage.tsx
  - src/pages/SchedulePage.tsx
autonomous: true
requirements:
  - redesign/32-header-unification-h4
must_haves:
  truths:
    - "WorkShiftPage 모바일 헤더 컨테이너 세로 32 (h-12 = padding 8/8 + 32 child)"
    - "WorkShiftPage 모바일 back 버튼 32×32 (w-7 h-7)"
    - "WorkShiftPage 모바일 엑셀 저장 버튼 세로 32 (h-7)"
    - "AnnualPlanPage 모바일 헤더 컨테이너 세로 32 (h-12)"
    - "AnnualPlanPage 모바일 back 버튼 32×32 (w-7 h-7)"
    - "AnnualPlanPage 모바일 '위치 조정' 토글 세로 32 (h-7 + items-center)"
    - "SchedulePage 모바일 헤더 컨테이너 세로 32 (h-12)"
    - "SchedulePage 모바일 엑셀 다운로드 버튼 세로 32 (h-7)"
    - "WorkShiftPage 데스크톱 분기 (h-[54px] px-5 gap-2.5) 보존 — H5 별도 wave"
    - "AnnualPlanPage 캘리브 좌표 시그니처 (yearPos 등) 0 byte 변경"
    - "SchedulePage L579 back 버튼 (이미 w-7 h-7 표준) 0 byte 변경"
    - "ChevronLeft size=15 / Download size=13 / strokeWidth 모두 보존"
    - "vite build PASS / TypeScript 0 error"
  artifacts:
    - path: "src/pages/WorkShiftPage.tsx"
      provides: "모바일 헤더 32 통일 (3 곳: L90 컨테이너 / L95 back / L104 엑셀 저장)"
      contains: "h-12 px-3 gap-2"
    - path: "src/pages/AnnualPlanPage.tsx"
      provides: "모바일 헤더 32 통일 (3 곳: L132 컨테이너 / L135 back / L142 위치 조정)"
      contains: "h-12 px-3 gap-2"
    - path: "src/pages/SchedulePage.tsx"
      provides: "모바일 헤더 32 통일 (2 곳: L576 컨테이너 / L587 엑셀 다운로드)"
      contains: "h-12 bg-surface-raised"
  key_links:
    - from: "WorkShiftPage.tsx L90 컨테이너 ternary"
      to: "모바일 분기 h-12 px-3 gap-2"
      via: "isDesktop ? 'h-[54px] px-5 gap-2.5' : 'h-12 px-3 gap-2'"
      pattern: "h-12 px-3 gap-2"
    - from: "AnnualPlanPage.tsx L142 위치 조정 토글"
      to: "h-7 inline-flex items-center (세로 정렬)"
      via: "py-[6px] → h-7 + items-center 추가"
      pattern: "h-7 inline-flex items-center"
    - from: "SchedulePage.tsx L587 엑셀 다운로드"
      to: "py-1.5 → h-7"
      via: "h-7 명시"
      pattern: "px-3 h-7 rounded-sm text-body-sm font-bold"
---

<objective>
**redesign/32-header-unification-h4** — 근무표 (WorkShiftPage) + 연간 업무 추진 계획 (AnnualPlanPage) + 일정 (SchedulePage) **모바일** 헤더 컨테이너 세로 + back 버튼 + 액션 버튼을 32px 표준으로 통일.

Purpose:
- 헤더 통일 wave 4번째 (H1/H1.5/H2/H3 완결 → H4). 사용자 결정 spec = sketch v2 §케이스 2 (`h-12` 컨테이너 + `w-7 h-7` back + `h-7` 액션).
- 9 카테고리 세로폭 = 32 (h-7) 룰을 모바일 헤더 영역까지 확장. 시각 일관성 + 노안 fit.
- 보수적 변경 — radius/border/페이지 컨벤션 (데스크톱 분기 / 캘리브 좌표) 모두 보존. 한 byte 비정상 변경 사고 (cherry-pick 패턴, memory anchor `feedback_planner_prompt_sketch_verbatim.md` / `project_redesign_17_annual_plan_status.md`) 제로.

Output: 단일 atomic commit (3 파일, **정확히 8 곳** 변경).
- WorkShift 3 곳 (L90 / L95 / L104)
- AnnualPlan 3 곳 (L132 / L135 / L142)
- Schedule 2 곳 (L576 / L587). **L579 back 은 이미 w-7 h-7 표준 — 무수정**.

본 plan 의 변경은 **세로폭 통일만**. radius (rounded-sm 보존), border-style, conditional logic (dlLoading / planLoading / calibMode), 텍스트, icon size, callback, 데스크톱 분기 모두 0 byte 변경.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/quick/260529-header-unification/sketch-v2.html

# 직접 변경 대상 (verbatim 인용 — executor 가 추측하지 말 것)
@src/pages/WorkShiftPage.tsx
@src/pages/AnnualPlanPage.tsx
@src/pages/SchedulePage.tsx

<interfaces>
<!-- 변경 대상 라인 verbatim (사고 예방). executor 는 grep 으로 한 곳씩 매치해 정확히 1건 치환할 것. -->

## WorkShiftPage.tsx

L89-91 (컨테이너 — 모바일 분기만 수정):
```tsx
      <header
        className={`bg-surface-raised border-b border-border-default flex items-center shrink-0 ${isDesktop ? 'h-[54px] px-5 gap-2.5' : 'pt-2 px-3 pb-[9px] gap-2'}`}
      >
```
→ 모바일 분기 `pt-2 px-3 pb-[9px] gap-2` → `h-12 px-3 gap-2`
→ 데스크톱 분기 `h-[54px] px-5 gap-2.5` 보존 (한 byte 변경 금지, H5 wave 책임)

L93-98 (back 버튼):
```tsx
        {!isDesktop && (
          <button
            onClick={() => navigate(-1)}
            className="w-[34px] h-[34px] rounded-sm bg-surface-sunken border border-border-default flex items-center justify-center cursor-pointer"
          >
            <ChevronLeft size={15} className="text-text-secondary" />
```
→ `w-[34px] h-[34px]` → `w-7 h-7`
→ rounded-sm / bg-surface-sunken / border / border-border-default / flex / items-center / justify-center / cursor-pointer 모두 보존
→ ChevronLeft size=15 + text-text-secondary 보존

L101-106 (엑셀 저장):
```tsx
        <button
          onClick={handleExcel}
          disabled={dlLoading}
          className={`bg-safe-bar text-text-on-accent text-caption font-bold leading-none rounded-sm border-0 h-[34px] px-[14px] ${dlLoading ? 'opacity-60 cursor-not-allowed' : 'cursor-pointer'}`}
        >
          {dlLoading ? '생성중...' : '엑셀 저장'}
```
→ `h-[34px]` → `h-7`
→ bg-safe-bar / text-text-on-accent / text-caption / font-bold / leading-none / rounded-sm / border-0 / px-[14px] / dlLoading conditional 모두 보존

## AnnualPlanPage.tsx

L132 (컨테이너):
```tsx
      <header className="bg-surface-raised border-b border-border-default flex items-center shrink-0 pt-2 px-3 pb-[9px] gap-2">
```
→ `pt-2 px-3 pb-[9px] gap-2` → `h-12 px-3 gap-2`
→ bg-surface-raised / border-b / border-border-default / flex / items-center / shrink-0 모두 보존

L133-137 (back 버튼):
```tsx
        <button
          onClick={() => navigate(-1)}
          className="w-[34px] h-[34px] rounded-sm bg-surface-sunken border border-border-default flex items-center justify-center shrink-0 cursor-pointer"
        >
          <ChevronLeft size={15} className="text-text-secondary" />
```
→ `w-[34px] h-[34px]` → `w-7 h-7`
→ rounded-sm / bg-surface-sunken / border / border-border-default / flex / items-center / justify-center / shrink-0 / cursor-pointer 모두 보존

L140-144 ("위치 조정" 토글):
```tsx
        <button
          onClick={() => setCalibMode(m => !m)}
          className={`text-caption font-bold leading-none rounded-sm border px-2.5 py-[6px] cursor-pointer ${calibMode ? 'border-accent bg-accent/10 text-accent' : 'bg-surface-sunken border-border-strong text-text-secondary'}`}
        >
          {calibMode ? '취소' : '위치 조정'}
```
→ `py-[6px]` → `h-7 inline-flex items-center`
→ 이유: h-7 만 추가하면 내부 텍스트가 baseline 정렬 안 됨 (이 버튼은 기존에 items-center 클래스가 없음). inline-flex items-center 추가 필수.
→ text-caption / font-bold / leading-none / rounded-sm / border / px-2.5 / cursor-pointer / calibMode conditional (border-accent bg-accent/10 text-accent ↔ bg-surface-sunken border-border-strong text-text-secondary) 모두 보존
→ onClick / 텍스트 "취소" / "위치 조정" 보존

## SchedulePage.tsx

L576 (컨테이너):
```tsx
      <header className="flex flex-shrink-0 items-center gap-2 px-3 pt-2 pb-[9px] bg-surface-raised border-b border-border-default">
```
→ `pt-2 pb-[9px]` → `h-12` (위치는 `px-3` 뒤로)
→ AFTER: `flex flex-shrink-0 items-center gap-2 px-3 h-12 bg-surface-raised border-b border-border-default`
→ flex / flex-shrink-0 / items-center / gap-2 / px-3 / bg-surface-raised / border-b / border-border-default 모두 보존

L577-581 (back 버튼 — **무수정, verify gate 만**):
```tsx
        <button
          onClick={() => navigate(-1)}
          className="w-7 h-7 flex-shrink-0 flex items-center justify-center rounded-sm bg-surface-sunken border border-border-default cursor-pointer"
        >
          <ChevronLeft size={15} strokeWidth={2} className="text-text-secondary" />
```
→ **변경 없음**. 이미 w-7 h-7 = 32 표준. 한 byte 변경 금지.

L584-594 (엑셀 다운로드):
```tsx
        <button
          onClick={handlePlanDownload}
          disabled={planLoading}
          className={`flex items-center gap-1.5 px-3 py-1.5 rounded-sm text-body-sm font-bold ${
            planLoading
              ? 'bg-surface-sunken text-text-tertiary cursor-default'
              : 'bg-safe-bar text-text-on-accent cursor-pointer'
          }`}
        >
          <Download size={13} />
          {planLoading ? '생성 중...' : '엑셀 다운로드'}
```
→ `py-1.5` → `h-7`
→ flex / items-center / gap-1.5 / px-3 / rounded-sm / text-body-sm / font-bold / planLoading conditional / Download size=13 / 텍스트 모두 보존
</interfaces>

<sketch_spec>
<!-- sketch v2 §케이스 2 spec (verbatim 인용) -->
- 헤더 컨테이너 = `h-12` (48px) — child 32 + 위아래 8 padding (visual centering)
- back 버튼 / 좌측 표준 = `w-7 h-7` (28→32? Tailwind override per memory: `w-7 = 32px`) + `rounded-sm` + `bg-surface-sunken border border-border-default` + `flex items-center justify-center` + ChevronLeft size=15 + `text-text-secondary`
- 우측 액션 = `h-7` + `inline-flex items-center` (또는 기존 flex 패턴) + `rounded-sm` + 페이지별 fill (bg-safe-bar 등) + `text-caption font-bold leading-none` + `border-0` 또는 `border`
- 컨테이너 `gap-2` (8) + `px-3` (12) 좌우 — 모바일 표준
- **anchor memory: `feedback_tailwind_w8_h8_is_48px.md` — tailwind.config spacing override: w-7=32, h-7=32, h-12=48.**
</sketch_spec>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 3 페이지 모바일 헤더 컨테이너 + back + 액션 세로 32 통일 (단일 atomic, 8 곳)</name>
  <files>src/pages/WorkShiftPage.tsx, src/pages/AnnualPlanPage.tsx, src/pages/SchedulePage.tsx</files>

  <action>
**3 파일 동시 수정. 각 변경은 verbatim 매치 → 정확히 1건 치환. grep 으로 결과 검증.**

### WorkShiftPage.tsx (3 곳)

**A. L90 컨테이너 모바일 분기:**

BEFORE (정확히):
```
        className={`bg-surface-raised border-b border-border-default flex items-center shrink-0 ${isDesktop ? 'h-[54px] px-5 gap-2.5' : 'pt-2 px-3 pb-[9px] gap-2'}`}
```

AFTER:
```
        className={`bg-surface-raised border-b border-border-default flex items-center shrink-0 ${isDesktop ? 'h-[54px] px-5 gap-2.5' : 'h-12 px-3 gap-2'}`}
```

→ 변경: `pt-2 px-3 pb-[9px] gap-2` → `h-12 px-3 gap-2` (모바일 분기만)
→ 데스크톱 분기 `h-[54px] px-5 gap-2.5` 1 byte 변경 금지 (H5 wave 책임)

**B. L95 back 버튼 className:**

BEFORE:
```
            className="w-[34px] h-[34px] rounded-sm bg-surface-sunken border border-border-default flex items-center justify-center cursor-pointer"
```

AFTER:
```
            className="w-7 h-7 rounded-sm bg-surface-sunken border border-border-default flex items-center justify-center cursor-pointer"
```

→ `w-[34px] h-[34px]` → `w-7 h-7` (w-7=32, h-7=32 per tailwind override)
→ 나머지 토큰/공백/콤마 0 byte 변경

**C. L104 엑셀 저장 버튼 className:**

BEFORE:
```
          className={`bg-safe-bar text-text-on-accent text-caption font-bold leading-none rounded-sm border-0 h-[34px] px-[14px] ${dlLoading ? 'opacity-60 cursor-not-allowed' : 'cursor-pointer'}`}
```

AFTER:
```
          className={`bg-safe-bar text-text-on-accent text-caption font-bold leading-none rounded-sm border-0 h-7 px-[14px] ${dlLoading ? 'opacity-60 cursor-not-allowed' : 'cursor-pointer'}`}
```

→ `h-[34px]` → `h-7`
→ dlLoading conditional / px-[14px] / 나머지 토큰 0 byte 변경

### AnnualPlanPage.tsx (3 곳)

**D. L132 컨테이너:**

BEFORE:
```
      <header className="bg-surface-raised border-b border-border-default flex items-center shrink-0 pt-2 px-3 pb-[9px] gap-2">
```

AFTER:
```
      <header className="bg-surface-raised border-b border-border-default flex items-center shrink-0 h-12 px-3 gap-2">
```

→ `pt-2 px-3 pb-[9px] gap-2` → `h-12 px-3 gap-2`
→ 나머지 0 byte

**E. L135 back 버튼 className:**

BEFORE:
```
          className="w-[34px] h-[34px] rounded-sm bg-surface-sunken border border-border-default flex items-center justify-center shrink-0 cursor-pointer"
```

AFTER:
```
          className="w-7 h-7 rounded-sm bg-surface-sunken border border-border-default flex items-center justify-center shrink-0 cursor-pointer"
```

→ `w-[34px] h-[34px]` → `w-7 h-7`
→ shrink-0 / 나머지 0 byte

**F. L142 "위치 조정" 토글 className:**

BEFORE:
```
          className={`text-caption font-bold leading-none rounded-sm border px-2.5 py-[6px] cursor-pointer ${calibMode ? 'border-accent bg-accent/10 text-accent' : 'bg-surface-sunken border-border-strong text-text-secondary'}`}
```

AFTER:
```
          className={`text-caption font-bold leading-none rounded-sm border px-2.5 h-7 inline-flex items-center cursor-pointer ${calibMode ? 'border-accent bg-accent/10 text-accent' : 'bg-surface-sunken border-border-strong text-text-secondary'}`}
```

→ `py-[6px]` → `h-7 inline-flex items-center`
→ 이유: 기존에 items-center 가 없었음 — h-7 만 적용하면 텍스트 baseline 정렬 깨짐. inline-flex items-center 추가 필수.
→ calibMode conditional / 나머지 토큰 0 byte 변경

### SchedulePage.tsx (2 곳 — L579 back 무수정)

**G. L576 컨테이너:**

BEFORE:
```
      <header className="flex flex-shrink-0 items-center gap-2 px-3 pt-2 pb-[9px] bg-surface-raised border-b border-border-default">
```

AFTER:
```
      <header className="flex flex-shrink-0 items-center gap-2 px-3 h-12 bg-surface-raised border-b border-border-default">
```

→ `pt-2 pb-[9px]` → `h-12` (위치는 `px-3` 뒤)
→ flex / flex-shrink-0 / items-center / gap-2 / px-3 / bg-surface-raised / border-b / border-border-default 보존

**H. L587 엑셀 다운로드 className:**

BEFORE (multiline — 정확히 매치):
```
          className={`flex items-center gap-1.5 px-3 py-1.5 rounded-sm text-body-sm font-bold ${
            planLoading
              ? 'bg-surface-sunken text-text-tertiary cursor-default'
              : 'bg-safe-bar text-text-on-accent cursor-pointer'
          }`}
```

AFTER:
```
          className={`flex items-center gap-1.5 px-3 h-7 rounded-sm text-body-sm font-bold ${
            planLoading
              ? 'bg-surface-sunken text-text-tertiary cursor-default'
              : 'bg-safe-bar text-text-on-accent cursor-pointer'
          }`}
```

→ `py-1.5` → `h-7`
→ multiline conditional / Download size=13 / 텍스트 / 나머지 0 byte 변경

### 변경 후 검증 (action 안에서 수행)

```bash
# 1. WorkShift — 모바일 분기 패턴 확인
grep -n "h-12 px-3 gap-2" src/pages/WorkShiftPage.tsx  # ≥1 (L90 모바일 분기)
grep -n "w-7 h-7 rounded-sm bg-surface-sunken" src/pages/WorkShiftPage.tsx  # =1 (L95)
grep -n "h-7 px-\[14px\]" src/pages/WorkShiftPage.tsx  # =1 (L104)
grep -n "pt-2 px-3 pb-\[9px\] gap-2" src/pages/WorkShiftPage.tsx  # =0 (이전 모바일 패딩 사라짐)
grep -n "w-\[34px\] h-\[34px\]" src/pages/WorkShiftPage.tsx  # =0
grep -n "h-\[34px\]" src/pages/WorkShiftPage.tsx  # =0 (back + 엑셀 둘 다 사라짐)
grep -n "h-\[54px\] px-5 gap-2.5" src/pages/WorkShiftPage.tsx  # =1 (데스크톱 분기 보존)

# 2. AnnualPlan
grep -n "h-12 px-3 gap-2" src/pages/AnnualPlanPage.tsx  # =1 (L132)
grep -n "w-7 h-7 rounded-sm bg-surface-sunken" src/pages/AnnualPlanPage.tsx  # =1 (L135)
grep -n "h-7 inline-flex items-center cursor-pointer" src/pages/AnnualPlanPage.tsx  # =1 (L142)
grep -n "pt-2 px-3 pb-\[9px\] gap-2" src/pages/AnnualPlanPage.tsx  # =0
grep -n "w-\[34px\] h-\[34px\]" src/pages/AnnualPlanPage.tsx  # =0
grep -n "py-\[6px\] cursor-pointer" src/pages/AnnualPlanPage.tsx  # =0 (L142 이전 패턴 사라짐)

# 3. Schedule
grep -n "gap-2 px-3 h-12 bg-surface-raised" src/pages/SchedulePage.tsx  # =1 (L576)
grep -n "px-3 h-7 rounded-sm text-body-sm font-bold" src/pages/SchedulePage.tsx  # =1 (L587)
grep -n "gap-2 px-3 pt-2 pb-\[9px\] bg-surface-raised" src/pages/SchedulePage.tsx  # =0
grep -n "px-3 py-1.5 rounded-sm text-body-sm font-bold" src/pages/SchedulePage.tsx  # =0
# L579 back 무수정 확인:
grep -n "w-7 h-7 flex-shrink-0 flex items-center justify-center rounded-sm bg-surface-sunken border border-border-default cursor-pointer" src/pages/SchedulePage.tsx  # =1 (L579 그대로)

# 4. 캘리브 좌표 시그니처 보존 (AnnualPlan)
grep -n "yearPos" src/pages/AnnualPlanPage.tsx  # ≥1 (변경 없이 유지)

# 5. icon size / strokeWidth 보존
grep -n "ChevronLeft size={15}" src/pages/WorkShiftPage.tsx  # =1
grep -n "ChevronLeft size={15}" src/pages/AnnualPlanPage.tsx  # =1
grep -n "ChevronLeft size={15} strokeWidth={2}" src/pages/SchedulePage.tsx  # =1
grep -n "Download size={13}" src/pages/SchedulePage.tsx  # =1

# 6. 텍스트 보존
grep -n "월간 출근부" src/pages/WorkShiftPage.tsx  # =1
grep -n "엑셀 저장" src/pages/WorkShiftPage.tsx  # =1
grep -n "연간 업무 추진 계획" src/pages/AnnualPlanPage.tsx  # =1
grep -n "위치 조정" src/pages/AnnualPlanPage.tsx  # =1
grep -n "월간 점검 계획" src/pages/SchedulePage.tsx  # =1
grep -n "엑셀 다운로드" src/pages/SchedulePage.tsx  # =1
```

### 빌드 검증

```bash
npm run build 2>&1 | tail -30
```

→ vite build PASS, TypeScript 0 error 확인.

### atomic commit

3 파일 모두 stage 후 단일 commit:

```bash
git add src/pages/WorkShiftPage.tsx src/pages/AnnualPlanPage.tsx src/pages/SchedulePage.tsx
git commit -m "$(cat <<'EOF'
feat(redesign/32-header-unification-h4): 근무표 + 연간 업무 추진 계획 + 일정 모바일 헤더 컨테이너 + 액션 32 통일 (8 곳)

- WorkShiftPage 모바일 분기 (3): 컨테이너 h-12, back w-7 h-7, 엑셀 저장 h-7
- AnnualPlanPage (3): 컨테이너 h-12, back w-7 h-7, 위치 조정 h-7 inline-flex items-center
- SchedulePage (2): 컨테이너 h-12, 엑셀 다운로드 h-7 (L579 back 무수정)
- WorkShift 데스크톱 분기 (h-[54px] px-5 gap-2.5) 보존 — H5 wave 책임
- AnnualPlan 캘리브 좌표 시그니처 (yearPos) 0 byte 변경
- ChevronLeft size=15 / Download size=13 / strokeWidth 보존
- radius (rounded-sm) / border / conditional logic / 텍스트 / onClick 모두 보존
EOF
)"
```

→ wrangler 명령 금지 / npm run deploy 금지 (cbc7119-design 워크트리 룰). push 는 사용자 컨펌 후.
  </action>

  <verify>
    <automated>
# 1. 모든 변경 grep gate
grep -cE "h-12 px-3 gap-2" src/pages/WorkShiftPage.tsx | grep -q "^[1-9]" && \
grep -cE "w-7 h-7 rounded-sm bg-surface-sunken" src/pages/WorkShiftPage.tsx | grep -q "^1$" && \
grep -cE "h-7 px-\[14px\]" src/pages/WorkShiftPage.tsx | grep -q "^1$" && \
grep -cE "pt-2 px-3 pb-\[9px\]" src/pages/WorkShiftPage.tsx | grep -q "^0$" && \
grep -cE "w-\[34px\] h-\[34px\]" src/pages/WorkShiftPage.tsx | grep -q "^0$" && \
grep -cE "h-\[34px\]" src/pages/WorkShiftPage.tsx | grep -q "^0$" && \
grep -cE "h-\[54px\] px-5 gap-2.5" src/pages/WorkShiftPage.tsx | grep -q "^1$" && \
grep -cE "h-12 px-3 gap-2" src/pages/AnnualPlanPage.tsx | grep -q "^1$" && \
grep -cE "w-7 h-7 rounded-sm bg-surface-sunken" src/pages/AnnualPlanPage.tsx | grep -q "^1$" && \
grep -cE "h-7 inline-flex items-center cursor-pointer" src/pages/AnnualPlanPage.tsx | grep -q "^1$" && \
grep -cE "pt-2 px-3 pb-\[9px\]" src/pages/AnnualPlanPage.tsx | grep -q "^0$" && \
grep -cE "w-\[34px\] h-\[34px\]" src/pages/AnnualPlanPage.tsx | grep -q "^0$" && \
grep -cE "py-\[6px\] cursor-pointer" src/pages/AnnualPlanPage.tsx | grep -q "^0$" && \
grep -cE "gap-2 px-3 h-12 bg-surface-raised" src/pages/SchedulePage.tsx | grep -q "^1$" && \
grep -cE "px-3 h-7 rounded-sm text-body-sm font-bold" src/pages/SchedulePage.tsx | grep -q "^1$" && \
grep -cE "gap-2 px-3 pt-2 pb-\[9px\]" src/pages/SchedulePage.tsx | grep -q "^0$" && \
grep -cE "px-3 py-1.5 rounded-sm text-body-sm font-bold" src/pages/SchedulePage.tsx | grep -q "^0$" && \
grep -cE "w-7 h-7 flex-shrink-0 flex items-center justify-center rounded-sm bg-surface-sunken border border-border-default cursor-pointer" src/pages/SchedulePage.tsx | grep -q "^1$" && \
echo "GREP GATES PASS" && \
# 2. icon/text 보존
grep -q "ChevronLeft size={15}" src/pages/WorkShiftPage.tsx && \
grep -q "ChevronLeft size={15}" src/pages/AnnualPlanPage.tsx && \
grep -q "ChevronLeft size={15} strokeWidth={2}" src/pages/SchedulePage.tsx && \
grep -q "Download size={13}" src/pages/SchedulePage.tsx && \
grep -q "월간 출근부" src/pages/WorkShiftPage.tsx && \
grep -q "엑셀 저장" src/pages/WorkShiftPage.tsx && \
grep -q "연간 업무 추진 계획" src/pages/AnnualPlanPage.tsx && \
grep -q "위치 조정" src/pages/AnnualPlanPage.tsx && \
grep -q "월간 점검 계획" src/pages/SchedulePage.tsx && \
grep -q "엑셀 다운로드" src/pages/SchedulePage.tsx && \
echo "PRESERVATION GATES PASS" && \
# 3. 캘리브 좌표 보존 (AnnualPlan)
grep -q "yearPos" src/pages/AnnualPlanPage.tsx && \
echo "CALIB SIGNATURE PRESERVED" && \
# 4. vite build
npm run build 2>&1 | tail -5
    </automated>
  </verify>

  <done>
- WorkShift 3 곳 (L90 / L95 / L104) 정확히 h-12 / w-7 h-7 / h-7 로 통일
- AnnualPlan 3 곳 (L132 / L135 / L142) 정확히 h-12 / w-7 h-7 / h-7 inline-flex items-center 로 통일
- Schedule 2 곳 (L576 / L587) 정확히 h-12 / h-7 로 통일, L579 back 0 byte 변경
- WorkShift 데스크톱 분기 h-[54px] px-5 gap-2.5 보존
- AnnualPlan 캘리브 좌표 시그니처 (yearPos) 0 byte 변경
- ChevronLeft size=15 / Download size=13 / strokeWidth / 텍스트 / onClick / disabled / conditional logic 모두 보존
- vite build PASS, TypeScript 0 error
- 단일 atomic commit
- wrangler / npm run deploy 0회
  </done>
</task>

</tasks>

<verification>
- `npm run build` PASS (vite + TypeScript)
- grep gate (위 verify <automated>) 통과
- `git diff HEAD~1 -- src/pages/WorkShiftPage.tsx src/pages/AnnualPlanPage.tsx src/pages/SchedulePage.tsx` 검토:
  - WorkShift = 3 hunk (L90 모바일 분기 / L95 / L104)
  - AnnualPlan = 3 hunk (L132 / L135 / L142)
  - Schedule = 2 hunk (L576 / L587)
  - **총 8 hunk**. 본문 / 캘리브 / 데스크톱 분기 / L579 back 영역 hunk 0
- 사용자 (윤종엽) 시각 확인 — preview 도메인 자동 배포 후 모바일 chrome 3 페이지 (월간 출근부 / 연간 업무 추진 계획 / 월간 점검 계획) 헤더 세로 통일감 확인
</verification>

<success_criteria>
- 3 파일 8 곳 변경, 단일 atomic commit
- 모바일 헤더 컨테이너 = h-12 (48px = child 32 + 위아래 8)
- 모바일 back/액션 버튼 = h-7 (32px)
- 데스크톱 분기 (WorkShift) + 캘리브 좌표 시그니처 (AnnualPlan) + 표준 back (Schedule L579) 0 byte 변경
- vite build PASS, TypeScript 0 error
- wrangler / npm run deploy 0회 (cbc7119-design 워크트리 룰)
- redesign/32-header-unification-h4 wave 종결 → H5 wave (데스크톱 헤더 + RemediationDetail absolute + StaffManage 잔여) 진입 가능
</success_criteria>

<output>
After completion, create `.planning/quick/260530-5oy-redesign-32-header-unification-h4-back-3/260530-5oy-SUMMARY.md` (워크트리 상대경로, cbc7119-design/.planning 사고 방지).
</output>
