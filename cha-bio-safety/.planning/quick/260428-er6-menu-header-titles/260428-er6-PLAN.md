---
phase: quick-260428-er6
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/App.tsx
  - src/pages/SchedulePage.tsx
  - src/pages/ReportsPage.tsx
  - src/pages/WorkShiftPage.tsx
  - src/pages/FloorPlanPage.tsx
  - src/pages/DailyReportPage.tsx
  - src/pages/WorkLogPage.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "사용자가 사이드 메뉴에서 '월간 출근부'를 누르면 페이지 헤더에 '월간 출근부'가 보인다 (모바일 자체 헤더 + 데스크톱 GlobalHeader 둘 다)"
    - "사용자가 사이드 메뉴에서 '소방 시설 도면'을 누르면 페이지 헤더에 '소방 시설 도면'이 보인다"
    - "사용자가 사이드 메뉴에서 '일일 업무 일지'를 누르면 페이지 헤더에 '일일 업무 일지'가 보인다 (모바일 + 데스크톱 좌측 패널)"
    - "사용자가 사이드 메뉴에서 '업무 수행 기록표'를 누르면 페이지 헤더에 '업무 수행 기록표'가 보인다 (모바일 + 데스크톱 좌측 패널)"
    - "사용자가 사이드 메뉴에서 '월간 점검 계획'을 누르면 페이지 헤더에 '월간 점검 계획'이 보인다 (모바일 + 데스크톱 자체 헤더)"
    - "사용자가 사이드 메뉴에서 '점검 일지 출력'을 누르면 페이지 헤더에 '점검 일지 출력'이 보인다"
  artifacts:
    - path: "src/App.tsx"
      provides: "PAGE_TITLES (데스크톱 GlobalHeader/모바일 일반 페이지 헤더 텍스트 SOT)"
      contains: "'/workshift': '월간 출근부'"
    - path: "src/pages/SchedulePage.tsx"
      provides: "월간 점검 계획 페이지 자체 헤더 (mobile + desktop 변형)"
      contains: "월간 점검 계획"
    - path: "src/pages/ReportsPage.tsx"
      provides: "점검 일지 출력 페이지 자체 헤더"
      contains: "점검 일지 출력"
    - path: "src/pages/WorkShiftPage.tsx"
      provides: "월간 출근부 페이지 자체 헤더"
      contains: "월간 출근부"
    - path: "src/pages/FloorPlanPage.tsx"
      provides: "소방 시설 도면 페이지 자체 헤더"
      contains: "소방 시설 도면"
    - path: "src/pages/DailyReportPage.tsx"
      provides: "일일 업무 일지 페이지 자체 헤더 (mobile + desktop 변형)"
      contains: "일일 업무 일지"
    - path: "src/pages/WorkLogPage.tsx"
      provides: "업무 수행 기록표 페이지 자체 헤더 (mobile + desktop 변형)"
      contains: "업무 수행 기록표"
  key_links:
    - from: "src/components/SideMenu.tsx (MENU 배열, 변경 금지 — SOT)"
      to: "src/App.tsx PAGE_TITLES + 6개 페이지 자체 <header>"
      via: "동일 한글 문자열 (사이드 메뉴 라벨 = 페이지 헤더 텍스트)"
      pattern: "월간 출근부|소방 시설 도면|일일 업무 일지|업무 수행 기록표|월간 점검 계획|점검 일지 출력"
---

<objective>
사이드 메뉴(SideMenu.tsx 의 MENU 배열) 라벨을 단일 진실 원천(SOT)으로, 동일 페이지의 헤더 표기를 정확히 동일한 한글 문자열로 맞춘다.

조사 결과 6개 라우트에서 불일치가 확인됨:

1. `/workshift` — 메뉴 "월간 출근부" / 헤더 "근무표"(PAGE_TITLES) + "월 근무표"(WorkShiftPage)
2. `/floorplan` — 메뉴 "소방 시설 도면" / 헤더 "건물 도면"(PAGE_TITLES + FloorPlanPage)
3. `/daily-report` — 메뉴 "일일 업무 일지" / 헤더 "일일업무일지"(PAGE_TITLES + DailyReportPage 모바일/데스크톱)
4. `/worklog` — 메뉴 "업무 수행 기록표" / 헤더 "업무수행기록표"(PAGE_TITLES + WorkLogPage 모바일/데스크톱)
5. `/schedule` — 메뉴 "월간 점검 계획" / 헤더 "점검 계획 관리"(SchedulePage 모바일/데스크톱) — PAGE_TITLES 는 이미 일치
6. `/reports` — 메뉴 "점검 일지 출력" / 헤더 "점검 기록 출력"(ReportsPage) — PAGE_TITLES 는 이미 일치

Purpose: 사용자가 같은 메뉴를 눌렀는데 헤더 표기가 다르면 인지 부담이 커지고 "다른 페이지인가?" 의심하게 됨. 운영 관찰 모드(2026-04-20~) 기조에 맞는 폴리싱.

Output: 7개 파일에서 한글 문자열만 치환. 빌드/타입체크 영향 없음.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@src/components/SideMenu.tsx

<!-- 조사 단계에서 확정된 정확한 라인/컨텍스트:
SideMenu.tsx MENU 배열 (변경 금지, SOT):
  /workshift   → '월간 출근부'      (line 37)
  /floorplan   → '소방 시설 도면'    (line 29)
  /daily-report→ '일일 업무 일지'    (line 34)
  /worklog     → '업무 수행 기록표'  (line 35)
  /schedule    → '월간 점검 계획'    (line 36)
  /reports     → '점검 일지 출력'    (line 40)

src/App.tsx PAGE_TITLES (line 68-91):
  line 78: '/workshift': '근무표',           ← '월간 출근부'
  line 80: '/floorplan': '건물 도면',         ← '소방 시설 도면'
  line 77: '/daily-report': '일일업무일지',   ← '일일 업무 일지'
  line 90: '/worklog': '업무수행기록표',      ← '업무 수행 기록표'
  (line 75 '/schedule' 와 line 76 '/reports' 는 이미 메뉴와 일치)

페이지 자체 헤더 (MOBILE_NO_NAV_PATHS 등록되어 모바일에서 자체 <header> 렌더):
  src/pages/SchedulePage.tsx
    line 427 (데스크톱 변형): "점검 계획 관리"  ← "월간 점검 계획"
    line 467 (모바일 변형):   "점검 계획 관리"  ← "월간 점검 계획"
  src/pages/ReportsPage.tsx
    line 337 (모바일 변형):   "점검 기록 출력"  ← "점검 일지 출력"
  src/pages/WorkShiftPage.tsx
    line 94  (모바일 변형):   "월 근무표"      ← "월간 출근부"
  src/pages/FloorPlanPage.tsx
    line 751 (모바일 변형):   "건물 도면"      ← "소방 시설 도면"
  src/pages/DailyReportPage.tsx
    line 411 (데스크톱 변형, 좌측 패널 상단): "일일업무일지" ← "일일 업무 일지"
    line 457 (모바일 변형):                  "일일업무일지" ← "일일 업무 일지"
  src/pages/WorkLogPage.tsx
    line 741 (데스크톱 변형, 좌측 패널 상단): "업무수행기록표" ← "업무 수행 기록표"
    line 808 (모바일 변형):                  "업무수행기록표" ← "업무 수행 기록표"

⚠ replace_all 금지 이유: 같은 한글 문자열이 다른 곳(파일명 코멘트, 다운로드 버튼 라벨 등)에 있을 수 있음.
  - DailyReportPage.tsx line 1, 347, 376 에 "일일업무일지" 있음 — 코멘트와 다운로드 버튼은 변경 금지
  - WorkLogPage.tsx line 1 에 "업무수행기록표" 있음 — 코멘트는 변경 금지
  각 Edit 호출은 헤더의 <span style={...}> 컨텍스트를 포함해서 정확히 1곳만 치환해야 함.
-->
</context>

<tasks>

<task type="auto">
  <name>Task 1: PAGE_TITLES 4개 키 + 6개 페이지 자체 헤더 span 텍스트 치환</name>
  <files>src/App.tsx, src/pages/SchedulePage.tsx, src/pages/ReportsPage.tsx, src/pages/WorkShiftPage.tsx, src/pages/FloorPlanPage.tsx, src/pages/DailyReportPage.tsx, src/pages/WorkLogPage.tsx</files>
  <action>
사이드 메뉴 라벨 = 페이지 헤더 의 단일 진실 원칙을 적용한다. SideMenu.tsx 의 MENU 배열은 SOT 이므로 절대 수정하지 않는다.

각 위치를 Edit 도구로 정확히 1곳씩 치환 (replace_all 사용 금지). old_string 에는 헤더 라인 전체 또는 충분한 주변 문자열을 포함해서 다른 위치와 충돌하지 않도록 한다.

### A. src/App.tsx — PAGE_TITLES 4개 키 (line 68-91 블록 안)

Edit 1: `/daily-report` 키
  old: `  '/daily-report': '일일업무일지',`
  new: `  '/daily-report': '일일 업무 일지',`

Edit 2: `/workshift` 키
  old: `  '/workshift': '근무표',`
  new: `  '/workshift': '월간 출근부',`

Edit 3: `/floorplan` 키
  old: `  '/floorplan': '건물 도면',`
  new: `  '/floorplan': '소방 시설 도면',`

Edit 4: `/worklog` 키
  old: `  '/worklog': '업무수행기록표',`
  new: `  '/worklog': '업무 수행 기록표',`

### B. src/pages/SchedulePage.tsx — 2곳 (데스크톱 + 모바일)

Edit 5 (line 427, 데스크톱 fontSize:15):
  old: `          <span style={{ flex:1, fontSize:15, fontWeight:700, color:'var(--t1)' }}>점검 계획 관리</span>`
  new: `          <span style={{ flex:1, fontSize:15, fontWeight:700, color:'var(--t1)' }}>월간 점검 계획</span>`

Edit 6 (line 467, 모바일 fontSize:14):
  old: `        <span style={{ flex:1, fontSize:14, fontWeight:700, color:'var(--t1)' }}>점검 계획 관리</span>`
  new: `        <span style={{ flex:1, fontSize:14, fontWeight:700, color:'var(--t1)' }}>월간 점검 계획</span>`

### C. src/pages/ReportsPage.tsx — 1곳

Edit 7 (line 337):
  old: `        <span style={{ flex: 1, fontSize: 14, fontWeight: 700, color: 'var(--t1)' }}>점검 기록 출력</span>`
  new: `        <span style={{ flex: 1, fontSize: 14, fontWeight: 700, color: 'var(--t1)' }}>점검 일지 출력</span>`

### D. src/pages/WorkShiftPage.tsx — 1곳

Edit 8 (line 94):
  old: `        <span style={{ flex:1, fontSize:14, fontWeight:700, color:'var(--t1)' }}>월 근무표</span>`
  new: `        <span style={{ flex:1, fontSize:14, fontWeight:700, color:'var(--t1)' }}>월간 출근부</span>`

### E. src/pages/FloorPlanPage.tsx — 1곳

Edit 9 (line 751):
  old: `        <span style={{ flex: 1, fontSize: 14, fontWeight: 700, color: 'var(--t1)' }}>건물 도면</span>`
  new: `        <span style={{ flex: 1, fontSize: 14, fontWeight: 700, color: 'var(--t1)' }}>소방 시설 도면</span>`

### F. src/pages/DailyReportPage.tsx — 2곳 (데스크톱 + 모바일)

Edit 10 (line 411, 데스크톱 fontSize: 15):
  old: `            <span style={{ fontSize: 15, fontWeight: 700, color: 'var(--t1)' }}>일일업무일지</span>`
  new: `            <span style={{ fontSize: 15, fontWeight: 700, color: 'var(--t1)' }}>일일 업무 일지</span>`

Edit 11 (line 457, 모바일 fontSize: 14, flex: 1):
  old: `        <span style={{ flex: 1, fontSize: 14, fontWeight: 700, color: 'var(--t1)' }}>일일업무일지</span>`
  new: `        <span style={{ flex: 1, fontSize: 14, fontWeight: 700, color: 'var(--t1)' }}>일일 업무 일지</span>`

⚠ DailyReportPage.tsx line 1 (`// ── 일일업무일지 페이지 ───────`), line 347/376 (`⬇ 일일업무일지(${mm}월) 다운로드`) 는 **수정하지 않는다**. 코멘트와 다운로드 버튼 라벨은 별도 의미를 가진다.

### G. src/pages/WorkLogPage.tsx — 2곳 (데스크톱 + 모바일)

Edit 12 (line 741, 데스크톱 fontSize: 15):
  old: `            <span style={{ fontSize: 15, fontWeight: 700, color: 'var(--t1)' }}>업무수행기록표</span>`
  new: `            <span style={{ fontSize: 15, fontWeight: 700, color: 'var(--t1)' }}>업무 수행 기록표</span>`

Edit 13 (line 808, 모바일 fontSize: 14, flex: 1):
  old: `        <span style={{ flex: 1, fontSize: 14, fontWeight: 700, color: 'var(--t1)' }}>업무수행기록표</span>`
  new: `        <span style={{ flex: 1, fontSize: 14, fontWeight: 700, color: 'var(--t1)' }}>업무 수행 기록표</span>`

⚠ WorkLogPage.tsx line 1 (`// ── 업무수행기록표 페이지 ───────`) 는 **수정하지 않는다**.

### 총 13개 Edit 호출, 7개 파일.
  </action>
  <verify>
    <automated>
# 옛 문자열이 헤더 위치에서 0건이어야 하고, 새 문자열이 ≥1건 있어야 함.

# 1. PAGE_TITLES 4개 키 (App.tsx 블록 내) — 옛 문자열 0건
grep -n "'/workshift': '근무표'" src/App.tsx | wc -l   # → 0
grep -n "'/floorplan': '건물 도면'" src/App.tsx | wc -l   # → 0
grep -n "'/daily-report': '일일업무일지'" src/App.tsx | wc -l   # → 0
grep -n "'/worklog': '업무수행기록표'" src/App.tsx | wc -l   # → 0

# PAGE_TITLES 새 문자열 4건
grep -c "'/workshift': '월간 출근부'" src/App.tsx   # → 1
grep -c "'/floorplan': '소방 시설 도면'" src/App.tsx   # → 1
grep -c "'/daily-report': '일일 업무 일지'" src/App.tsx   # → 1
grep -c "'/worklog': '업무 수행 기록표'" src/App.tsx   # → 1

# 2. 페이지 자체 헤더 — 옛 문자열이 <span ... color:'var(--t1)' }}>...</span> 패턴에서 0건
grep -E "color: ?'var\(--t1\)' \}\}>점검 계획 관리</span>" src/pages/SchedulePage.tsx | wc -l   # → 0
grep -E "color: ?'var\(--t1\)' \}\}>점검 기록 출력</span>" src/pages/ReportsPage.tsx | wc -l   # → 0
grep -E "color: ?'var\(--t1\)' \}\}>월 근무표</span>" src/pages/WorkShiftPage.tsx | wc -l   # → 0
grep -E "color: ?'var\(--t1\)' \}\}>건물 도면</span>" src/pages/FloorPlanPage.tsx | wc -l   # → 0
grep -E "color: ?'var\(--t1\)' \}\}>일일업무일지</span>" src/pages/DailyReportPage.tsx | wc -l   # → 0
grep -E "color: ?'var\(--t1\)' \}\}>업무수행기록표</span>" src/pages/WorkLogPage.tsx | wc -l   # → 0

# 페이지 자체 헤더 — 새 문자열 ≥1건
grep -c "월간 점검 계획</span>" src/pages/SchedulePage.tsx   # → 2 (데스크톱+모바일)
grep -c "점검 일지 출력</span>" src/pages/ReportsPage.tsx   # → 1
grep -c "월간 출근부</span>" src/pages/WorkShiftPage.tsx   # → 1
grep -c "소방 시설 도면</span>" src/pages/FloorPlanPage.tsx   # → 1
grep -c "일일 업무 일지</span>" src/pages/DailyReportPage.tsx   # → 2 (데스크톱+모바일)
grep -c "업무 수행 기록표</span>" src/pages/WorkLogPage.tsx   # → 2 (데스크톱+모바일)

# 3. 보존 검증 — 코멘트/다운로드 버튼은 그대로 유지
grep -c "// ── 일일업무일지 페이지" src/pages/DailyReportPage.tsx   # → 1 (보존됨)
grep -c "일일업무일지(\${mm}월) 다운로드" src/pages/DailyReportPage.tsx   # → 2 (다운로드 버튼 라벨 보존)
grep -c "// ── 업무수행기록표 페이지" src/pages/WorkLogPage.tsx   # → 1 (보존됨)

# 4. SideMenu.tsx (SOT) 는 변경되지 않음 — git diff 로 확인
git diff --stat src/components/SideMenu.tsx   # → (empty 출력, 변경 없음)
    </automated>
  </verify>
  <done>
- src/App.tsx PAGE_TITLES 의 4개 키('/workshift', '/floorplan', '/daily-report', '/worklog') 가 사이드 메뉴 라벨과 정확히 동일한 문자열을 가짐
- 6개 페이지 파일의 헤더 <span> 텍스트가 사이드 메뉴 라벨과 일치 (모바일/데스크톱 변형 모두)
- DailyReportPage 의 // 코멘트와 다운로드 버튼 라벨, WorkLogPage 의 // 코멘트는 그대로 유지됨
- src/components/SideMenu.tsx 는 변경되지 않음 (SOT 보존)
- 모든 grep 검증 명령이 위 기대값과 일치
  </done>
</task>

</tasks>

<verification>
종합 검증: 6개 라우트(/workshift, /floorplan, /daily-report, /worklog, /schedule, /reports) 각각에서 사이드 메뉴 라벨과 페이지 진입 후 헤더 표기가 글자 단위로 동일한지 확인.

```bash
# 한 번에 모든 옛 문자열이 사라졌는지 (헤더 위치 한정)
grep -rE "'/workshift': '근무표'|'/floorplan': '건물 도면'|'/daily-report': '일일업무일지'|'/worklog': '업무수행기록표'" src/App.tsx | wc -l   # → 0
grep -rE "color: ?'var\(--t1\)' \}\}>(점검 계획 관리|점검 기록 출력|월 근무표|건물 도면|일일업무일지|업무수행기록표)</span>" src/pages/ | wc -l   # → 0

# TypeScript 영향 없음 확인 (선택)
npx tsc --noEmit 2>&1 | tail -5   # → 새로운 에러 없음
```

수동 (사용자 단계, 배포 후): 모바일 + 데스크톱 둘 다에서 사이드 메뉴 → 6개 메뉴 항목 클릭 → 헤더 텍스트가 메뉴 라벨과 동일한지 육안 확인.
</verification>

<success_criteria>
1. 사용자가 사이드 메뉴에서 6개 항목 중 어느 것을 눌러도 페이지 헤더 텍스트가 메뉴 라벨과 글자 단위로 동일
2. SideMenu.tsx (MENU 배열) 변경 0건
3. 다운로드 버튼 라벨, 파일 상단 코멘트 등 헤더가 아닌 위치의 한글 문자열은 그대로 유지
4. TypeScript 컴파일 에러 없음 (단순 한글 치환이므로 자동 충족)
</success_criteria>

<output>
After completion, create `.planning/quick/260428-er6-menu-header-titles/260428-er6-SUMMARY.md` with the 13 Edit operations performed and grep verification results.
</output>
