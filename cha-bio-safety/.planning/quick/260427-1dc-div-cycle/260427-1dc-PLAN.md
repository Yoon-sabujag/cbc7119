---
phase: quick-260427-1dc
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - functions/api/dashboard/stats.ts
  - src/utils/inspectionProgress.ts
  - src/components/ui/index.tsx
  - src/pages/DashboardPage.tsx
  - src/pages/InspectionPage.tsx
autonomous: false
requirements:
  - QUICK-260427-1dc
tags:
  - dashboard
  - inspection
  - donut
  - cycle-window

must_haves:
  truths:
    - "대시보드 '오늘 현황 점검 미완료' 가 DIV/컴프레셔 카테고리에 대해 현재(또는 직전) cycle window 의 완료 개수만 카운트한다"
    - "점검 페이지 점검 항목 카드(DIV/컴프레셔) 의 done/total 표시가 현재(또는 직전) cycle window 기준으로 동작한다"
    - "대시보드 '이번 달 점검 현황' 도넛이 DIV/컴프레셔에 대해 0~200% two-lap 시각화(월초 색A 한 바퀴 → 월말 색B 가 같은 위치에 덮어쓰기)를 보여준다"
    - "DIV/컴프레셔 외 모든 카테고리의 통계/카드/도넛 동작은 변경 전과 100% 동일하다 (회귀 없음)"
    - "Donut 컴포넌트는 doubleCycle prop 미전달 시 기존 단일 arc 동작을 100% 보존한다 (backward compatible)"
    - "이번 달 해당 카테고리 inspect 일정 자체가 없을 경우 기존 monthly 윈도우([monthStart, monthEnd]) 로 폴백한다"
    - "TypeScript 빌드(tsc --noEmit 포함 npm run build) 가 에러 없이 통과한다"
    - "프로덕션 배포가 --branch production 으로 성공하며 사용자 PWA 재설치 후 변경 사항이 보인다"
  artifacts:
    - path: "functions/api/dashboard/stats.ts"
      provides: "서버측 cycle window 분기 로직 (inspDoneN + monthlyItems doubleCycle)"
      contains: "CYCLE_CATEGORIES"
    - path: "src/utils/inspectionProgress.ts"
      provides: "클라이언트측 cycle window JS 로직 (computeCardCompletion 확장)"
      contains: "CYCLE_CATEGORIES"
    - path: "src/components/ui/index.tsx"
      provides: "Donut doubleCycle prop overlay 렌더 로직"
      contains: "doubleCycle"
    - path: "src/pages/DashboardPage.tsx"
      provides: "monthlyItems.doubleCycle 분기 → Donut prop 전달"
    - path: "src/pages/InspectionPage.tsx"
      provides: "computeCardCompletion 호출부에 scheduleItems + today 인자 추가"
  key_links:
    - from: "functions/api/dashboard/stats.ts (getCycleRange)"
      to: "src/utils/inspectionProgress.ts (getCycleRangeJS)"
      via: "동일한 cycle window 계산 로직 — 양쪽이 같은 결과 산출"
      pattern: "consecutive date block grouping"
    - from: "functions/api/dashboard/stats.ts (monthlyItems push 시 doubleCycle:true)"
      to: "src/pages/DashboardPage.tsx (Donut doubleCycle prop)"
      via: "API JSON → MonthlyItem.doubleCycle → Donut prop"
      pattern: "m.doubleCycle ? <Donut doubleCycle={...} /> : <Donut .../>"
    - from: "src/pages/InspectionPage.tsx:4418 (computeCardCompletion 호출)"
      to: "src/utils/inspectionProgress.ts (computeCardCompletion)"
      via: "scheduleItems + today 인자 전달"
      pattern: "computeCardCompletion({ cps, monthRecordDates, scheduleItems, today })"
---

<objective>
DIV/컴프레셔 카테고리(월 2 cycle 구조)의 진행률 표시를 cycle window 기준으로 정확히 인식하도록 수정한다. 현재는 모든 카테고리가 monthly 윈도우로 일괄 집계돼 DIV/컴프 두 cycle 데이터가 섞여서 표시된다. 다음 3 곳을 수정:

1. 대시보드 "오늘 현황 점검 미완료" (`functions/api/dashboard/stats.ts` `inspDoneN`) — DIV/컴프만 현재/직전 cycle window 적용
2. 대시보드 "이번 달 점검 현황" 도넛 (`functions/api/dashboard/stats.ts` `monthlyItems`) — DIV/컴프만 0~200% two-lap (월초/월말 별도 색)
3. 점검 페이지 점검 항목 카드 (`src/utils/inspectionProgress.ts` `computeCardCompletion`) — DIV/컴프만 현재/직전 cycle window 적용

룰: cycle window 는 "이번 달 inspect 일정 중 연속된 날짜 블록 → today 포함 블록(없으면 today 이전 가장 최근 블록)" 으로 정의. 양쪽 헬퍼(`getCycleRange` 서버 / `getCycleRangeJS` 클라이언트) 동일 로직.

Purpose: DIV/컴프 격주 점검 사이클을 정확히 반영해 "지난 cycle 끝났는데 도넛이 100% 안 차는" 문제 / "다음 cycle 안 시작했는데 카드가 미완료로 보이는" 문제를 해결.
Output: 빌드 PASS, --branch production 배포, 사용자 PWA 재설치 후 검증.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@functions/api/dashboard/stats.ts
@src/utils/inspectionProgress.ts
@src/components/ui/index.tsx
@src/pages/DashboardPage.tsx
@src/pages/InspectionPage.tsx
@src/types/index.ts

<interfaces>
<!-- 기존 코드에서 추출한 핵심 시그니처. 실행자는 이 contract 그대로 사용. -->

From src/types/index.ts:
```typescript
export interface ScheduleItem {
  id: string
  title: string
  date: string
  endDate?: string
  time?: string
  assigneeId?: string
  category: ScheduleCategory          // 'event' | 'repair' | 'inspect' | 'task' | ...
  status: ScheduleStatus               // 'pending' | 'in_progress' | 'done' | 'overdue'
  inspectionCategory?: string         // 'DIV' | '컴프레셔' | '소화기' | ...
  memo?: string
}

export interface CheckPoint {
  id: string
  category: string                    // 'DIV' | '컴프레셔' | ...
  defaultResult?: string | null
  description?: string | null
  // ...
}
```

From src/utils/inspectionProgress.ts (현재 시그니처 — 확장 대상):
```typescript
export function computeCardCompletion(options: {
  cps: CheckPoint[]
  monthRecordDates: Record<string, string[]>   // cp.id → ['YYYY-MM-DD', ...]
}): number
```

From src/components/ui/index.tsx (현재 Donut 시그니처 — 확장 대상):
```typescript
interface DonutProps {
  pct: number
  color: string
  size?: number       // default 40
  strokeWidth?: number // default 5
}
export function Donut({ pct, color, size = 40, strokeWidth = 5 }: DonutProps)
```

From src/pages/DashboardPage.tsx (현재 MonthlyItem 타입 — 확장 대상):
```typescript
interface MonthlyItem { label:string; pct:number; color:string; total:number; done:number }
// 신규 필드: doubleCycle?: true; early_done?, late_done?, early_pct?, late_pct?, early_color?, late_color?
```

From functions/api/dashboard/stats.ts (현재 monthlyItems 배열 타입):
```typescript
const monthlyItems: {label:string; pct:number; color:string; total:number; done:number}[] = []
// 확장: doubleCycle?: true; early_done?, late_done?, early_pct?, late_pct?, early_color?, late_color? 추가
```

InspectionPage 호출부 (line 4418, 호출 위치 컨텍스트):
```typescript
// line 4418 (변경 대상)
doneCnt = computeCardCompletion({ cps, monthRecordDates })

// scheduleItems 는 이미 같은 컴포넌트 안에 useQuery 로 로드돼 있음 (line 3936)
const { data: scheduleItems = [] } = useQuery({ ... })
// today 는 line 4097 등에서 KST 기반 'YYYY-MM-DD' 로 만들어짐 — 동일 패턴으로 만들 것:
//   const n = new Date()
//   const today = `${n.getFullYear()}-${String(n.getMonth()+1).padStart(2,'0')}-${String(n.getDate()).padStart(2,'0')}`
```

stats.ts 컨텍스트:
- line 161-166: `_monthStart`, `_monthEnd` 계산 (기존, inspDoneN 직전)
- line 167-170: `todayCats` 쿼리 (오늘 inspect 카테고리)
- line 172-193: `inspDoneN` 루프 (수정 대상 #1) — `recQ` + `autoQ` 가 `_monthStart..._monthEnd` 범위 사용
- line 238-243: `monthStart`, `monthEnd` (월간 카드용, 기존)
- line 246-251: `monthScheds` 쿼리 (이번 달 inspect 일정)
- line 263-346: `for (const sched of monthScheds.results)` 루프 — 수정 대상 #2 (cpCategory 가 DIV/컴프 인 경우 doubleCycle 분기)
- line 322-329: `d` (record count) 와 line 330-332: `autoN` (자동완료) — 분기 안에서 cycle 별로 동일한 패턴으로 다시 호출

InspectionPage 컨텍스트:
- line 3936: `scheduleItems` useQuery (이미 당월치 로드됨)
- line 4097: `today` 문자열 생성 패턴 (KST local date)
- line 4400-4419: 카테고리 그리드 카드 렌더 — line 4418 이 호출부
- 같은 컴포넌트 함수 안에 한 번만 today 변수 만들어서 쓸 것 (loadTodayRecords 안의 로컬 `today` 와 충돌하지 않게 별도 const 이름 사용)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 서버 stats.ts — cycle window 헬퍼 + inspDoneN 분기 + monthlyItems doubleCycle 분기</name>
  <files>functions/api/dashboard/stats.ts</files>
  <action>
파일 상단(import 아래, calcStreakDays 위)에 다음 추가:

```ts
const CYCLE_CATEGORIES = new Set(['DIV', '컴프레셔'])

// 이번 달 inspect 일정에서 today 가 속한(또는 가장 가까운 직전) 연속 블록 [first, last] 반환.
// today 가 어떤 블록에도 속하지 않으면 today 이전의 가장 최근 블록(block_last < today) 반환.
// 일정이 아예 없으면 폴백 [monthStart, monthEnd].
async function getCycleRange(
  env: { DB: D1Database },
  cat: string,
  today: string,
  monthStart: string,
  monthEnd: string,
): Promise<[string, string]> {
  const rows = await env.DB.prepare(
    `SELECT DISTINCT date FROM schedule_items
     WHERE category='inspect' AND inspection_category=?
       AND date BETWEEN ? AND ? ORDER BY date ASC`
  ).bind(cat, monthStart, monthEnd).all<{ date: string }>()
  const sorted = (rows.results ?? []).map(r => r.date)
  if (sorted.length === 0) return [monthStart, monthEnd]

  const dayDiff = (a: string, b: string) => {
    const da = new Date(a + 'T00:00:00Z').getTime()
    const db = new Date(b + 'T00:00:00Z').getTime()
    return Math.round((db - da) / 86400000)
  }
  const blocks: string[][] = []
  let cur: string[] = []
  for (const d of sorted) {
    if (cur.length === 0 || dayDiff(cur[cur.length - 1], d) === 1) {
      cur.push(d)
    } else {
      blocks.push(cur); cur = [d]
    }
  }
  if (cur.length) blocks.push(cur)

  // today 포함 블록
  for (const b of blocks) {
    if (today >= b[0] && today <= b[b.length - 1]) return [b[0], b[b.length - 1]]
  }
  // today 이전 가장 최근 블록 (block_last < today)
  let candidate: string[] | null = null
  for (const b of blocks) {
    if (b[b.length - 1] < today) candidate = b
  }
  if (candidate) return [candidate[0], candidate[candidate.length - 1]]
  return [monthStart, monthEnd]
}

// (monthlyItems 분기에서 사용) 이번 달 inspect 일정의 모든 연속 블록 반환.
async function getMonthlyBlocks(
  env: { DB: D1Database },
  cat: string,
  monthStart: string,
  monthEnd: string,
): Promise<string[][]> {
  const rows = await env.DB.prepare(
    `SELECT DISTINCT date FROM schedule_items
     WHERE category='inspect' AND inspection_category=?
       AND date BETWEEN ? AND ? ORDER BY date ASC`
  ).bind(cat, monthStart, monthEnd).all<{ date: string }>()
  const sorted = (rows.results ?? []).map(r => r.date)
  if (sorted.length === 0) return []
  const dayDiff = (a: string, b: string) => {
    const da = new Date(a + 'T00:00:00Z').getTime()
    const db = new Date(b + 'T00:00:00Z').getTime()
    return Math.round((db - da) / 86400000)
  }
  const blocks: string[][] = []
  let cur: string[] = []
  for (const d of sorted) {
    if (cur.length === 0 || dayDiff(cur[cur.length - 1], d) === 1) cur.push(d)
    else { blocks.push(cur); cur = [d] }
  }
  if (cur.length) blocks.push(cur)
  return blocks
}
```

**수정 #1 — inspDoneN 루프 (line ~172-193):**

기존 루프를 다음으로 교체. DIV/컴프 카테고리만 cycle window 사용, 나머지는 기존 monthly 윈도우 그대로 (회귀 없음):

```ts
let inspDoneN = 0
for (const row of (todayCats.results ?? [])) {
  const cat = row.inspection_category
  const [startDate, endDate] = CYCLE_CATEGORIES.has(cat)
    ? await getCycleRange(env, cat, today, _monthStart, _monthEnd)
    : [_monthStart, _monthEnd]
  const recQ = await env.DB.prepare(
    `SELECT COUNT(DISTINCT cr.checkpoint_id) as n
     FROM check_records cr
     JOIN check_points cp ON cr.checkpoint_id=cp.id
     WHERE cp.category=? AND (cr.result IN ('normal','caution') OR (cr.result='bad' AND cr.status='resolved'))
       AND date(cr.checked_at) BETWEEN ? AND ?`
  ).bind(cat, startDate, endDate).first<{ n: number }>()
  const autoQ = await env.DB.prepare(
    `SELECT COUNT(*) as n FROM check_points cp
     WHERE cp.is_active=1 AND cp.category=?
       AND (cp.default_result IS NOT NULL OR cp.description LIKE '%접근불가%')
       AND cp.id NOT IN (
         SELECT checkpoint_id FROM check_records
         WHERE date(checked_at) BETWEEN ? AND ?
       )`
  ).bind(cat, startDate, endDate).first<{ n: number }>()
  inspDoneN += (recQ?.n ?? 0) + (autoQ?.n ?? 0)
}
```

CATEGORY_ALIAS 매핑은 DIV/컴프에 적용되지 않음 (둘 다 ALIAS 없음) — 기존 코드도 inspDoneN 에선 cat 을 직접 사용 중이라 무손실.

**수정 #2 — monthlyItems 루프 (line ~263-346) 안에서 doubleCycle 분기 추가:**

`cpTotal === 0` 체크 직후, 기존 "check_points 기반 카테고리: 기존 로직" (line 322-) 직전에 다음 분기 추가. **DIV/컴프 가 아닌 경우 기존 코드 100% 그대로 흘러가야 함**:

```ts
// DIV/컴프레셔: 0~200% two-lap (월초 cycle + 월말 cycle 별도 집계)
if (CYCLE_CATEGORIES.has(cpCategory)) {
  const blocks = await getMonthlyBlocks(env, sched.inspection_category, monthStart, monthEnd)
  const earlyBlock = blocks[0] ?? []
  const lateBlock  = blocks[1] ?? []

  const cycleDone = async (block: string[]): Promise<number> => {
    if (block.length === 0) return 0
    const s = block[0]
    const e = block[block.length - 1]
    const r = await env.DB.prepare(`
      SELECT COUNT(DISTINCT cr.checkpoint_id) as n
      FROM check_records cr
      JOIN check_points cp ON cr.checkpoint_id=cp.id
      WHERE cp.category=? AND date(cr.checked_at) BETWEEN ? AND ?
        AND (cr.result IN ('normal','caution') OR (cr.result='bad' AND cr.status='resolved'))
    `).bind(cpCategory, s, e).first<{n:number}>()
    const a = await env.DB.prepare(
      `SELECT COUNT(*) as n FROM check_points cp WHERE cp.category=? AND cp.is_active=1
         AND (cp.default_result IS NOT NULL OR cp.description LIKE '%접근불가%')
         AND cp.id NOT IN (SELECT checkpoint_id FROM check_records cr2 JOIN check_points cp2 ON cr2.checkpoint_id=cp2.id WHERE cp2.category=? AND date(cr2.checked_at) BETWEEN ? AND ?)`
    ).bind(cpCategory, cpCategory, s, e).first<{n:number}>()
    return (r?.n ?? 0) + (a?.n ?? 0)
  }

  const early_done = await cycleDone(earlyBlock)
  const late_done  = await cycleDone(lateBlock)
  const total = cpTotal
  const early_pct = total > 0 ? Math.min(Math.round((early_done / total) * 100), 100) : 0
  const late_pct  = total > 0 ? Math.min(Math.round((late_done  / total) * 100), 100) : 0
  // 호환성: pct = (early_pct + late_pct) / 2 (0~100), done = early_done + late_done
  const pct = Math.round((early_pct + late_pct) / 2)
  const label = sched.title.length > 10 ? sched.title.slice(0, 10) + '…' : sched.title
  const colorIdx = monthlyItems.length % ITEM_COLORS.length
  monthlyItems.push({
    label,
    pct,
    color: pct === 0 ? '#52525b' : ITEM_COLORS[colorIdx],
    total,
    done: early_done + late_done,
    // doubleCycle 메타 — 클라이언트가 두 색 overlay arc 렌더
    doubleCycle: true,
    early_done,
    late_done,
    early_pct,
    late_pct,
    early_color: 'var(--info)',
    late_color: 'var(--warn)',
  } as any)
  continue
}
```

`monthlyItems` 배열 타입 시그니처(line 254)를 확장:

```ts
const monthlyItems: {
  label:string; pct:number; color:string; total:number; done:number;
  doubleCycle?: boolean;
  early_done?: number; late_done?: number;
  early_pct?: number; late_pct?: number;
  early_color?: string; late_color?: string;
}[] = []
```

회귀 검증 포인트:
- DIV/컴프 외 카테고리는 기존 코드 분기(유도등 / cpTotal===0 / 일반) 그대로 흐름
- `_monthStart/_monthEnd` 변수와 `monthStart/monthEnd` 변수 둘 다 그대로 유지 (이름 충돌 X — 기존 코드의 두 블록이 각각 사용)
- CATEGORY_ALIAS 분기에 DIV/컴프 추가 안 함 (이미 매핑 없음)
  </action>
  <verify>
    <automated>cd /Users/jykevin/Documents/20260328/cha-bio-safety && npx tsc --noEmit -p . 2>&1 | head -50</automated>
  </verify>
  <done>
- functions/api/dashboard/stats.ts 에 `CYCLE_CATEGORIES`, `getCycleRange`, `getMonthlyBlocks` 정의 추가
- inspDoneN 루프가 DIV/컴프에 한해 cycle window 사용
- monthlyItems 루프 안에 DIV/컴프 doubleCycle 분기 추가, monthlyItems 배열 타입 확장
- 다른 카테고리 코드 경로 변경 없음 (회귀 없음)
- TypeScript 빌드 에러 없음
  </done>
</task>

<task type="auto">
  <name>Task 2: 클라이언트 — inspectionProgress 확장 + Donut doubleCycle prop + DashboardPage/InspectionPage 호출부</name>
  <files>src/utils/inspectionProgress.ts, src/components/ui/index.tsx, src/pages/DashboardPage.tsx, src/pages/InspectionPage.tsx</files>
  <action>

**A) src/utils/inspectionProgress.ts — computeCardCompletion 시그니처 확장 + cycle window JS 헬퍼 추가:**

기존 함수를 다음으로 교체:

```ts
import type { CheckPoint, ScheduleItem } from '../types'

const CYCLE_CATEGORIES = new Set(['DIV', '컴프레셔'])

// 서버 getCycleRange 와 동일 로직의 클라이언트 버전.
// scheduleItems 는 InspectionPage 의 useQuery 로 당월치만 로드된 배열.
function getCycleRangeJS(
  scheduleItems: ScheduleItem[],
  cat: string,
  today: string,
): [string, string] | null {
  const dates = Array.from(new Set(
    scheduleItems
      .filter(s => s.category === 'inspect' && s.inspectionCategory === cat)
      .map(s => s.date)
  )).sort()
  if (dates.length === 0) return null

  const dayDiff = (a: string, b: string) => {
    const da = new Date(a + 'T00:00:00Z').getTime()
    const db = new Date(b + 'T00:00:00Z').getTime()
    return Math.round((db - da) / 86400000)
  }
  const blocks: string[][] = []
  let cur: string[] = []
  for (const d of dates) {
    if (cur.length === 0 || dayDiff(cur[cur.length - 1], d) === 1) cur.push(d)
    else { blocks.push(cur); cur = [d] }
  }
  if (cur.length) blocks.push(cur)

  for (const b of blocks) {
    if (today >= b[0] && today <= b[b.length - 1]) return [b[0], b[b.length - 1]]
  }
  let candidate: string[] | null = null
  for (const b of blocks) {
    if (b[b.length - 1] < today) candidate = b
  }
  if (candidate) return [candidate[0], candidate[candidate.length - 1]]
  return null
}

// 대시보드 월간 카드와 동일 기준의 완료 개수 계산 (모바일 일반점검 카드용).
// - cp.defaultResult 또는 description 에 '접근불가' 포함 시 자동 완료
// - 그 외에는 기록 윈도우 안에 완료 기록(normal | caution | bad+resolved) 이 하나라도 있으면 완료
// - DIV/컴프레셔 카테고리는 cycle window (현재/직전 연속 블록) 기준,
//   다른 카테고리는 monthRecordDates 그대로 (월 전체).
// - scheduleItems / today 미전달 시 cycle 분기 비활성 (기존 monthly 동작 유지 — backward compat).
export function computeCardCompletion(options: {
  cps: CheckPoint[]
  monthRecordDates: Record<string, string[]>
  scheduleItems?: ScheduleItem[]
  today?: string
}): number {
  const { cps, monthRecordDates, scheduleItems, today } = options
  const completedIds = new Set<string>()

  for (const cp of cps) {
    if (cp.defaultResult || cp.description?.includes('접근불가')) {
      completedIds.add(cp.id)
      continue
    }
    const dates = monthRecordDates[cp.id] ?? []
    if (dates.length === 0) continue

    if (CYCLE_CATEGORIES.has(cp.category) && scheduleItems && today) {
      const range = getCycleRangeJS(scheduleItems, cp.category, today)
      if (range) {
        const [s, e] = range
        if (dates.some(d => d >= s && d <= e)) completedIds.add(cp.id)
        // 폴백: cycle 일정 자체가 없으면 (range === null) → monthly 동작
      } else {
        completedIds.add(cp.id)
      }
    } else {
      completedIds.add(cp.id)  // 기존 monthly 동작
    }
  }

  return completedIds.size
}
```

`CheckPoint` 의 category 필드가 string 으로 정의돼 있는지 확인. (확인됨 — `category: string`)

**B) src/components/ui/index.tsx — Donut doubleCycle prop overlay 렌더:**

기존 Donut 함수를 다음으로 교체. **doubleCycle undefined 시 기존 동작 100% 보존** (텍스트 / 색 / size 기본값 / strokeWidth 기본값 / "0% 일 때 회색 + 'var(--t3)'" 처리 모두 유지):

```tsx
interface DonutProps {
  pct: number
  color: string
  size?: number
  strokeWidth?: number
  doubleCycle?: {
    earlyPct: number
    latePct: number
    earlyColor: string
    lateColor: string
  }
}
export function Donut({ pct, color, size = 40, strokeWidth = 5, doubleCycle }: DonutProps) {
  const r    = (size - strokeWidth * 2) / 2
  const circ = 2 * Math.PI * r
  const cx   = size / 2

  if (doubleCycle) {
    const { earlyPct, latePct, earlyColor, lateColor } = doubleCycle
    const earlyDash = (Math.min(earlyPct, 100) / 100) * circ
    const lateDash  = (Math.min(latePct, 100) / 100) * circ
    const allZero = earlyPct === 0 && latePct === 0
    return (
      <div style={{ position:'relative', width:size, height:size }}>
        <svg
          width={size} height={size}
          viewBox={`0 0 ${size} ${size}`}
          style={{ transform:'rotate(-90deg)' }}
        >
          {/* 배경 트랙 */}
          <circle cx={cx} cy={cx} r={r} fill="none" stroke="var(--bg4)" strokeWidth={strokeWidth} />
          {/* 월초 arc — 색A (한 바퀴 채우면 100%) */}
          {earlyPct > 0 && (
            <circle
              cx={cx} cy={cx} r={r} fill="none"
              stroke={earlyColor}
              strokeWidth={strokeWidth}
              strokeLinecap="round"
              strokeDasharray={`${earlyDash.toFixed(2)} ${(circ - earlyDash).toFixed(2)}`}
            />
          )}
          {/* 월말 arc — 색B 가 같은 시작점에서 overlay (두 번째 바퀴 시각화) */}
          {latePct > 0 && (
            <circle
              cx={cx} cy={cx} r={r} fill="none"
              stroke={lateColor}
              strokeWidth={strokeWidth}
              strokeLinecap="round"
              strokeDasharray={`${lateDash.toFixed(2)} ${(circ - lateDash).toFixed(2)}`}
            />
          )}
        </svg>
        <div style={{
          position:'absolute', inset:0, display:'flex', alignItems:'center', justifyContent:'center',
          fontFamily:'JetBrains Mono, monospace', fontSize: Math.max(8, Math.round(size * 0.18)), fontWeight:600,
          color: allZero ? 'var(--t3)' : 'var(--t2)', whiteSpace:'nowrap',
        }}>
          {earlyPct}/{latePct}
        </div>
      </div>
    )
  }

  // ── 기존 단일 arc 동작 (변경 금지) ──
  const dash = (pct / 100) * circ
  const zero = pct === 0
  return (
    <div style={{ position:'relative', width:size, height:size }}>
      <svg
        width={size} height={size}
        viewBox={`0 0 ${size} ${size}`}
        style={{ transform:'rotate(-90deg)' }}
      >
        <circle cx={cx} cy={cx} r={r} fill="none" stroke="var(--bg4)" strokeWidth={strokeWidth} />
        <circle
          cx={cx} cy={cx} r={r} fill="none"
          stroke={zero ? '#2a2f37' : color}
          strokeWidth={strokeWidth}
          strokeLinecap="round"
          strokeDasharray={`${dash.toFixed(2)} ${(circ - dash).toFixed(2)}`}
        />
      </svg>
      <div style={{
        position:'absolute', inset:0, display:'flex', alignItems:'center', justifyContent:'center',
        fontFamily:'JetBrains Mono, monospace', fontSize:10, fontWeight:600,
        color: zero ? 'var(--t3)' : color,
      }}>
        {pct}%
      </div>
    </div>
  )
}
```

핵심: doubleCycle 분기에서 latePct arc 가 earlyPct arc 위에 덮어 그려짐 (overlay). earlyPct=100 일 때 트랙 전체가 색A → latePct 가 0~100 % 차오르며 같은 자리를 색B 로 덮음. 사용자가 보는 "200% two-lap" 효과.

**C) src/pages/DashboardPage.tsx — MonthlyItem 타입 확장 + Donut doubleCycle prop 전달:**

line 26 의 MonthlyItem 인터페이스 확장:

```ts
interface MonthlyItem {
  label: string
  pct: number
  color: string
  total: number
  done: number
  doubleCycle?: boolean
  early_done?: number
  late_done?: number
  early_pct?: number
  late_pct?: number
  early_color?: string
  late_color?: string
}
```

line ~255 (3행 그리드 desktop) 와 line ~525 (mobile horizontal scroll) 두 곳의 `<Donut .../>` 호출을 분기로 변경:

```tsx
{m.doubleCycle ? (
  <Donut
    pct={m.pct}
    color={m.color}
    size={52} /* 또는 mobile: 44 */
    doubleCycle={{
      earlyPct: m.early_pct ?? 0,
      latePct:  m.late_pct  ?? 0,
      earlyColor: m.early_color ?? 'var(--info)',
      lateColor:  m.late_color  ?? 'var(--warn)',
    }}
  />
) : (
  <Donut pct={m.pct} color={m.color} size={52} /* 또는 44 */ />
)}
```

각 호출부의 size 값(52 desktop / 44 mobile)은 기존 그대로 유지.

done/total 표시는 기존 그대로 (`{m.done}/{m.total}` — done 은 early+late 합산, total 은 cpTotal).

**D) src/pages/InspectionPage.tsx:4418 — computeCardCompletion 호출부에 scheduleItems + today 추가:**

line 4418 한 줄 변경. 같은 컴포넌트 안에 `scheduleItems` 가 line 3936 에서 useQuery 로 이미 로드돼 있고, today 는 line 4097 패턴으로 만들 것. **선언 위치 주의**: line 4400 `<div ... gridTemplateColumns:'1fr 1fr 1fr' ...>` 직전(예: line 4395 `loading` 체크 직전 또는 4399 `점검 항목 선택` 라벨 직전)에 today 변수 한 번만 선언:

```tsx
// (loading 체크 또는 카테고리 그리드 직전)
const _todayForCycle = (() => {
  const n = new Date()
  return `${n.getFullYear()}-${String(n.getMonth()+1).padStart(2,'0')}-${String(n.getDate()).padStart(2,'0')}`
})()
```

이름은 `today` 가 같은 함수 안에 충돌 가능성 있으면 `_todayForCycle` 또는 `cycleToday` 등으로. 호출부:

```tsx
doneCnt = computeCardCompletion({ cps, monthRecordDates, scheduleItems, today: _todayForCycle })
```

scheduleItems 는 line 3936 의 `const { data: scheduleItems = [] } = useQuery(...)` 로 이미 사용 가능. 추가 import 불필요.

회귀 검증:
- DIV/컴프 외 카테고리(소화기/소화전/스프링클러/유도등/감지기/제연/방화셔터/등)의 카드 done/total 동일
- 유도등 분기(line 4410-4416 isGL) 코드 변경 없음
- monthRecordDates 채움 로직(line 4014 인근) 변경 없음
  </action>
  <verify>
    <automated>cd /Users/jykevin/Documents/20260328/cha-bio-safety && npx tsc --noEmit -p . 2>&1 | head -50 && npm run build 2>&1 | tail -30</automated>
  </verify>
  <done>
- src/utils/inspectionProgress.ts 에 CYCLE_CATEGORIES + getCycleRangeJS + 확장된 computeCardCompletion 정의
- src/components/ui/index.tsx Donut 에 doubleCycle prop 추가 + overlay 분기, 단일 arc 동작 보존
- src/pages/DashboardPage.tsx MonthlyItem 타입 확장 + 두 호출부(desktop/mobile)에 doubleCycle 분기
- src/pages/InspectionPage.tsx:4418 computeCardCompletion 호출부에 scheduleItems + today 인자 추가
- 다른 카테고리 카드/도넛 동작 변경 없음
- npm run build 통과 (TypeScript 에러 없음, vite 빌드 성공)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 프로덕션 배포 + 사용자 PWA 재설치 검증</name>
  <what-built>
- 서버: DIV/컴프레셔 카테고리에 대해 cycle window 기반 inspDoneN 집계 + monthlyItems doubleCycle 메타 반환
- 클라이언트: computeCardCompletion 이 cycle window 분기, Donut 이 두 색 overlay arc 렌더, DashboardPage 두 호출부 분기
- 다른 카테고리 회귀 없음 (코드 경로 그대로)
- TypeScript / vite 빌드 통과
  </what-built>
  <how-to-verify>

**A) 빌드 + 배포 (Claude 가 실행)**

```bash
cd /Users/jykevin/Documents/20260328/cha-bio-safety
npm run build
npx wrangler pages deploy dist --branch production --commit-message "div compressor cycle recognition"
```

ASCII commit message 필수 (한글 거부 이슈 — feedback_wrangler_commit_utf8). `--branch production` 빠지면 Preview 로 감 (feedback_deploy_branch).

**B) 사용자 검증 (PWA 재설치 후 — feedback_pwa_cache_invalidation)**

PWA 재설치(앱 삭제 → cha-bio-safety.pages.dev 다시 추가) 후 다음 확인:

1. **대시보드 "오늘 현황 점검 미완료" (DIV/컴프 일정이 있는 날)**:
   - 오늘이 DIV/컴프 cycle 안 → cycle window 시작일 ~ 오늘 까지 기록만 카운트
   - 오늘이 cycle 사이 gap → 직전 cycle 의 완료 개수 표시
   - 다른 카테고리(소화기/감지기/등) 일정인 날엔 변화 없음

2. **대시보드 "이번 달 점검 현황" 도넛**:
   - DIV/컴프 카드: 두 색 arc 가 보임 (월초 cycle = `var(--info)` 파랑, 월말 cycle = `var(--warn)` 주황)
   - 월초 cycle 끝: 파랑 한 바퀴 채워짐 (100%)
   - 월말 cycle 진행: 같은 자리에 주황 arc 가 0~100% 채우며 덮음
   - 월말 cycle 끝: 두 번째 바퀴(주황) 도 한 바퀴 채워짐
   - 다른 카테고리 도넛: 기존 단일 arc 그대로 (회귀 없음)

3. **점검 페이지 점검 항목 카드 (DIV / 컴프레셔 카드)**:
   - 현재 cycle 안: cycle window 안 기록만 카운트해서 done/total 표시
   - cycle 사이 gap: 직전 cycle 의 완료 개수 표시
   - 다른 카테고리 카드(소화기/감지기/등): 기존 monthly 카운트 그대로

4. **회귀 체크 (절대 변하면 안 됨)**:
   - 소화기/소화전/스프링클러/유도등/감지기/제연/방화셔터 카테고리의 done/total 변화 없음
   - "오늘 현황 점검 미완료" 가 DIV/컴프 외 카테고리 일정인 날엔 기존 숫자 그대로
   - "이번 달 점검 현황" 의 DIV/컴프 외 카드는 단일 arc 그대로
   - 점검 기록 자체(check_records) 변경 없음

5. **이번 달 DIV/컴프 일정이 아예 없는 경우 (방어)**:
   - 폴백: monthly 윈도우 사용 → 도넛은 doubleCycle 메타 안 받음 (서버에서 cpTotal 기반 일반 분기로 흐름) — 실제로는 매월 inspect 일정이 등록돼 있어 발생 X. 발생해도 기존 monthly 동작 유지.

**C) 사용자 메모**:
- 4월 26일(오늘) 기준 DIV/컴프 4월 cycle 일정 위치 확인 후 검증 (월초 vs 월말 vs gap)
- 5월 법정점검 실전 검증이 1순위 — 4월 잔여 cycle 로 미리 확인 (project_operation_mode)
  </how-to-verify>
  <resume-signal>"approved" 또는 이슈 보고 (예: "DIV 카드 0/34 으로 보임 — cycle window 적용 안 됨" 등)</resume-signal>
</task>

</tasks>

<verification>
- TypeScript 빌드: `npx tsc --noEmit -p .` 에러 0
- 프로덕션 빌드: `npm run build` 성공
- 배포: `wrangler pages deploy ... --branch production` 성공 (Production URL 확인)
- 사용자 PWA 재설치 후 위 5 항목 검증 PASS
- 회귀 체크: DIV/컴프 외 카테고리 동작 동일
</verification>

<success_criteria>
- 대시보드 "오늘 현황 점검 미완료" 가 DIV/컴프에 대해 cycle window 카운트
- 대시보드 "이번 달 점검 현황" DIV/컴프 도넛이 두 색 overlay arc (월초 파랑, 월말 주황) 렌더
- 점검 페이지 DIV/컴프 카드 done/total 이 cycle window 기준
- DIV/컴프 외 모든 카테고리 회귀 없음
- Donut 컴포넌트 backward compat (doubleCycle 미전달 = 기존 단일 arc)
- 빌드 + 배포 + PWA 재설치 검증 완료
</success_criteria>

<output>
After completion, create `.planning/quick/260427-1dc-div-cycle/260427-1dc-SUMMARY.md` covering:
- 변경 파일 5개 + 핵심 변경 요약 (CYCLE_CATEGORIES + getCycleRange/JS + Donut doubleCycle + 호출부)
- 서버/클라이언트 cycle window 로직 동기화 (양쪽 동일 결과)
- monthlyItems doubleCycle 메타 + Donut overlay 시각화 흐름
- 회귀 체크 결과 (DIV/컴프 외 카테고리 동작 동일 확인)
- 배포 명령 + ASCII commit + --branch production
- 사용자 PWA 재설치 안내 + 검증 결과 (스크린샷/숫자)
- 5월 법정점검 실전 검증 시 추가 관찰 포인트 (gap 기간 표시 / 두 cycle 색 가독성)
</output>
